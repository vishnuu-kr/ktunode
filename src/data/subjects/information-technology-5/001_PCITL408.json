{
  "id": "pcitl408-information-technology-s5",
  "code": "PCITL408",
  "name": "PCITL408",
  "branchId": "information-technology",
  "semester": 5,
  "modules": [
    {
      "id": "mExperiment 2",
      "title": "Module Experiment 2",
      "topics": [
        {
          "id": "information-technology-5-pcitl408-mExperiment 2-t1",
          "title": "Familiarization of system calls (fork, exec, getpid, exit, wait, close, stat etc) in operating system and demonstration of creating partition and installing operating system",
          "content": "<!-- SECTION_1_START -->\n> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCITL408 - OPERATING SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Familiarization of system calls (fork, exec, getpid, exit, wait, close, stat etc) in operating system and demonstration of creating partition and installing operating system\n\n# 🖥️ Module Experiment 2 — System Calls & OS Installation\n\n## 1. Core Technical Definition & Intuitive Overview\n\n> [!NOTE]\n> **Definition (KTU Syllabus Terminology):**\n> A **System Call** is the controlled interface through which a user-mode process requests a privileged service from the **kernel** of the operating system. It acts as the only legitimate gateway between **user space** (where applications run with restricted privileges) and **kernel space** (where OS code executes with full hardware access).\n\nIn the **KTU 2024 Scheme** for Operating Systems Lab (PCITL408), Experiment 2 expects the student to:\n1. Write C programs that invoke core POSIX system calls: `fork()`, `exec()`, `getpid()`, `getppid()`, `exit()`, `wait()`, `close()`, `stat()`.\n2. Demonstrate the working of an actual operating system install — which conceptually relies on the *same* underlying system calls (disk I/O, filesystem creation, bootloader installation).\n\n### 1.1 Conceptual Analogy — \"The Government Office\"\n\nThink of the **Operating System Kernel** as a strict government office that handles passports, licenses, and land records. As a citizen (user program), you cannot walk in and stamp your own passport — that would be chaos. Instead, you must:\n\n1. Fill a form (the system call).\n2. Submit it at the official counter (the system call interface / trap).\n3. The officer (kernel) processes it.\n4. You receive a stamped receipt (the return value).\n\n> [!IMPORTANT]\n> **KTU High-Yield Point:** Every privileged operation — creating a process, opening a file, reading from disk, allocating memory — MUST go through a system call. There is **no other legal way** for user code to access hardware.\n\n### 1.2 Categories of System Calls (as per Silberschatz / KTU syllabus)\n\n| Category | Examples | Purpose |\n|----------|----------|---------|\n| **Process Control** | `fork()`, `exec()`, `wait()`, `exit()`, `getpid()` | Create, run, and terminate processes |\n| **File Management** | `open()`, `read()`, `write()`, `close()`, `stat()` | Manipulate files & directories |\n| **Device Management** | `ioctl()`, `read()`, `write()` | Interact with I/O devices |\n| **Information Maintenance** | `getpid()`, `time()`, `uname()` | Query system/OS data |\n| **Communication** | `pipe()`, `shmget()`, `socket()` | Inter-process & network communication |\n| **Memory Management** | `mmap()`, `brk()` | Allocate/release virtual memory |\n\n### 1.3 The Eight System Calls in Focus\n\n| # | System Call | Header | Purpose |\n|---|-------------|--------|---------|\n| 1 | `fork()` | `<unistd.h>` | Creates an exact duplicate (child) of the calling process |\n| 2 | `exec()` family | `<unistd.h>` | Replaces current process image with a new program |\n| 3 | `getpid()` | `<unistd.h>` | Returns the **PID** of the calling process |\n| 4 | `getppid()` | `<unistd.h>` | Returns the **parent PID** |\n| 5 | `exit()` | `<stdlib.h>` | Terminates the calling process; sends status to parent |\n| 6 | `wait()` | `<sys/wait.h>` | Parent blocks until child terminates |\n| 7 | `close()` | `<unistd.h>` | Closes an open file descriptor |\n| 8 | `stat()` | `<sys/stat.h>` | Retrieves file metadata (size, permissions, timestamps) |\n\n> [!TIP]\n> **Remember for KTU Viva:** \"PID = Process ID. PPID = Parent PID. The very first process in a UNIX/Linux system is `init` (or `systemd`) with **PID = 1**.\"\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Reference Sheet\n\n### 2.1 `fork()` — The Process Creator\n\n`fork()` is the **only** way (besides `clone()` in Linux) to create a new process in UNIX. It creates a nearly identical copy of the calling process.\n\n**Prototype:**\n```c\npid_t fork(void);\n```\n\n**Return Values — THE CRITICAL POINT:**\n\n| Return Value | Meaning |\n|--------------|---------|\n| **Negative (`-1`)** | Error — child could not be created (e.g., process table full) |\n| **Zero (`0`)** | Returned in the **child** process |\n| **Positive (>0)** | Returned in the **parent** — value is the **PID of the child** |\n\n> [!IMPORTANT]\n> After `fork()`, **both** parent and child continue execution from the **same instruction** following the `fork()` call. Their memory spaces are **independent** (copy-on-write in modern Linux).\n\n### 2.2 `exec()` Family — The Process Transformer\n\n`exec()` does **not** create a new process; it **replaces** the current process image with a new program. The PID stays the same.\n\nCommon variants:\n\n| Function | Path Search? | Argument Style |\n|----------|--------------|----------------|\n| `execl(path, arg0, ..., NULL)` | No | List |\n| `execlp(file, arg0, ..., NULL)` | Yes (uses `PATH`) | List |\n| `execv(path, argv[])` | No | Vector |\n| `execvp(file, argv[])` | Yes | Vector |\n\n### 2.3 `getpid()` and `getppid()`\n\n```c\npid_t getpid(void);   // returns PID of current process\npid_t getppid(void);  // returns PID of parent process\n```\n\n- PIDs are unique **per boot session** on UNIX.\n- Maximum PID is typically `32768` (Linux default, configurable via `/proc/sys/kernel/pid_max`).\n\n### 2.4 `exit()` vs `return` in `main()`\n\n| Aspect | `return 0;` (in `main`) | `exit(status);` |\n|--------|------------------------|-----------------|\n| Cleans up stdio buffers | Yes | Yes |\n| Calls `atexit()` handlers | Yes | Yes |\n| Closes all `fd` via `_exit()` | Yes | Yes |\n| Can be called from any function | **No** | **Yes** |\n| Returns control to caller | Yes | No (terminates process) |\n\n### 2.5 `wait()` and `waitpid()`\n\n```c\npid_t wait(int *status);\npid_t waitpid(pid_t pid, int *status, int options);\n```\n\n- `wait()` **blocks** the parent until *any* child terminates.\n- `waitpid()` can wait for a *specific* child or use `WNOHANG` for non-blocking check.\n- `WIFEXITED(status)` and `WEXITSTATUS(status)` macros extract the exit code.\n\n### 2.6 `close()`\n\n```c\nint close(int fd);\n```\n- Closes the file descriptor `fd`.\n- Returns `0` on success, `-1` on error.\n- Releases the kernel file table entry. **FD limits**: default `1024` per process (check with `ulimit -n`).\n\n### 2.7 `stat()`\n\n```c\nint stat(const char *path, struct stat *buf);\n```\n- Populates a `struct stat` containing:\n  - `st_mode` — file type and permissions\n  - `st_size` — file size in bytes\n  - `st_uid`, `st_gid` — owner\n  - `st_atime`, `st_mtime`, `st_ctime` — access, modify, change times\n  - `st_ino` — inode number\n  - `st_nlink` — number of hard links\n\n### 2.8 Demonstration: Creating Partitions & Installing an OS\n\n> [!IMPORTANT]\n> **KTU Lab Note:** Although Experiment 2 is a C-programming lab, the syllabus title also mandates a \"**demonstration**\" of partition creation and OS installation. This is typically shown via:\n> - Tools: `fdisk`, `gdisk`, `parted`, `GParted` (GUI)\n> - Creating **MBR** (Master Boot Record) or **GPT** (GUID Partition Table) partitions\n> - Steps: Download ISO → Make bootable USB (using `Rufus` / `dd` / `Etcher`) → Boot → Choose partition → Install bootloader (GRUB) → Install OS\n\n**Partition Table Comparison (High-Yield for KTU):**\n\n| Feature | **MBR** | **GPT** |\n|---------|---------|---------|\n| Max Disk Size | **2 TB** | **9.4 ZB** (effectively unlimited) |\n| Max Partitions | **4** primary | **128** primary |\n| Boot Mode | Legacy BIOS | UEFI |\n| Backup Table | No | Yes (at end of disk) |\n\n**Standard Linux Partition Layout:**\n\n| Mount Point | Typical Size | Filesystem | Purpose |\n|-------------|--------------|------------|---------|\n| `/boot` | **512 MB – 1 GB** | `ext4` | Kernel + GRUB |\n| `/` (root) | **20 – 50 GB** | `ext4` | OS files |\n| `swap` | **Equal to RAM** (or 2× RAM if RAM < 4 GB) | `swap` | Virtual memory |\n| `/home` | Remaining space | `ext4` | User data |\n\n### 2.9 KTU Reference Sheet\n\n| Concept | Key Value / Formula |\n|---------|---------------------|\n| PID range (Linux default) | `1` to `32768` |\n| Default FD limit per process | `1024` |\n| `fork()` returns in child | `0` |\n| `fork()` returns in parent | Child's PID |\n| `fork()` returns on failure | `-1` |\n| MBR max disk | `2 TB` |\n| GPT max partitions | `128` |\n| Standard `ext4` inode ratio | 1 inode per **4 KB** block (tunable) |\n| Process states | New → Ready → Running → Blocked → Terminated |\n| `wait()` return value | PID of terminated child (or `-1`) |\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Walkthroughs, Sample Programs & Analytical Matrix\n\n### 3.1 Program 1: Demonstrating `fork()`, `getpid()`, `getppid()`\n\n```c\n/* program1_fork.c */\n#include <stdio.h>\n#include <unistd.h>\n#include <sys/types.h>\n\nint main(void) {\n    pid_t pid;\n\n    printf(\"Before fork: PID = %d, PPID = %d\\n\", getpid(), getppid());\n\n    pid = fork();                          /* STEP 1: create child */\n\n    if (pid < 0) {                         /* STEP 2: error check */\n        perror(\"fork failed\");\n        return 1;\n    }\n    else if (pid == 0) {                   /* STEP 3: child branch */\n        printf(\"[CHILD]  PID = %d, PPID = %d, fork() returned %d\\n\",\n               getpid(), getppid(), pid);\n    }\n    else {                                 /* STEP 4: parent branch */\n        printf(\"[PARENT] PID = %d, Child PID = %d, fork() returned %d\\n\",\n               getpid(), pid, pid);\n    }\n\n    printf(\"Common line: This runs in BOTH parent and child. PID = %d\\n\",\n           getpid());\n\n    return 0;\n}\n```\n\n**Expected Output (sample):**\n```\nBefore fork: PID = 2540, PPID = 1890\n[PARENT] PID = 2540, Child PID = 2541, fork() returned 2541\nCommon line: This runs in BOTH parent and child. PID = 2540\n[CHILD]  PID = 2541, PPID = 2540, fork() returned 0\nCommon line: This runs in BOTH parent and child. PID = 2541\n```\n\n> [!NOTE]\n> The **order** of PARENT/CHILD output lines is **non-deterministic** — it depends on the scheduler. This is a classic KTU viva question!\n\n### 3.2 Program 2: `wait()` Synchronization\n\n```c\n/* program2_wait.c */\n#include <stdio.h>\n#include <stdlib.h>\n#include <unistd.h>\n#include <sys/wait.h>\n\nint main(void) {\n    pid_t pid = fork();\n\n    if (pid == 0) {                          /* child */\n        printf(\"Child: starting work, PID = %d\\n\", getpid());\n        sleep(2);                            /* simulate work */\n        printf(\"Child: exiting with code 7\\n\");\n        exit(7);                             /* STEP: child terminates */\n    }\n    else if (pid > 0) {                      /* parent */\n        int status;\n        printf(\"Parent: waiting for child %d...\\n\", pid);\n        wait(&status);                       /* STEP: block here */\n        if (WIFEXITED(status)) {\n            printf(\"Parent: child exited normally with code %d\\n\",\n                   WEXITSTATUS(status));\n        }\n    }\n    return 0;\n}\n```\n\n**Key Analytical Points:**\n\n1. Without `wait()`: parent may exit **before** child → child becomes an **orphan** and is adopted by `init` (PID 1).\n2. Without `fork()` cleanup: child becomes a **zombie** (defunct process in `ps`).\n3. The `status` integer is a bit-packed field — the **low 8 bits** (after right shift) hold the exit code.\n\n### 3.3 Program 3: `exec()` — Replacing Process Image\n\n```c\n/* program3_exec.c */\n#include <stdio.h>\n#include <unistd.h>\n#include <stdlib.h>\n\nint main(void) {\n    pid_t pid = fork();\n\n    if (pid == 0) {\n        /* CHILD: replace itself with `ls -l /tmp` */\n        printf(\"Child: about to exec ls\\n\");\n        execlp(\"ls\", \"ls\", \"-l\", \"/tmp\", (char *)NULL);\n\n        /* If execlp returns, it FAILED */\n        perror(\"execlp failed\");\n        exit(1);\n    }\n    else if (pid > 0) {\n        wait(NULL);\n        printf(\"Parent: child has finished executing ls\\n\");\n    }\n    return 0;\n}\n```\n\n**Analytical Reasoning:**\n\n$$\\text{PID}_{\\text{after exec}} = \\text{PID}_{\\text{before exec}}$$\n\nThe PID does **not** change, but the **code, stack, heap, and data segments are completely overwritten**. The new program starts at its `main()` function.\n\n### 3.4 Program 4: `close()` and `stat()`\n\n```c\n/* program4_close_stat.c */\n#include <stdio.h>\n#include <unistd.h>\n#include <fcntl.h>\n#include <sys/stat.h>\n\nint main(void) {\n    int fd = open(\"test.txt\", O_RDONLY);\n    if (fd < 0) { perror(\"open\"); return 1; }\n\n    struct stat st;\n    if (stat(\"test.txt\", &st) < 0) { perror(\"stat\"); return 1; }\n\n    printf(\"File size       : %ld bytes\\n\", (long)st.st_size);\n    printf(\"Inode number    : %ld\\n\",     (long)st.st_ino);\n    printf(\"Permissions     : %o\\n\",       st.st_mode & 0777);\n    printf(\"Hard link count : %ld\\n\",     (long)st.st_nlink);\n\n    close(fd);\n    printf(\"File descriptor %d closed.\\n\", fd);\n    return 0;\n}\n```\n\n**Compilation Command (KTU Lab Standard):**\n```bash\ngcc program4_close_stat.c -o prog4\n./prog4\n```\n\n### 3.5 Analytical Matrix: Behavior of `fork()` + `exec()` + `wait()`\n\n| Scenario | `fork()` called? | `exec()` called? | `wait()` called? | Resulting Process Tree |\n|----------|------------------|------------------|------------------|------------------------|\n| Plain C program | No | No | No | Single process |\n| Use `fork()` only | Yes | No | No | Parent may exit → orphan; child → zombie if parent doesn't wait |\n| Use `fork()` + `wait()` | Yes | No | Yes | Clean parent-child sync; child reaped properly |\n| Use `fork()` + `exec()` | Yes | Yes | Optional | Child becomes a different program |\n| Use `fork()` + `exec()` + `wait()` | Yes | Yes | Yes | **Shell-like** behavior (this is how bash runs commands) |\n\n### 3.6 Step-by-Step: Creating a Partition and Installing an OS\n\n> [!NOTE]\n> This is the **demonstration** half of the experiment. Below is the exact logical sequence a student would narrate during the KTU lab viva.\n\n**Step 1 — Identify the Disk**\n```bash\nlsblk                # list block devices\nsudo fdisk -l /dev/sda\n```\n\n**Step 2 — Create Partitions Using `fdisk` (MBR style)**\n\n| Sub-step | Command | Action |\n|----------|---------|--------|\n| 1 | `sudo fdisk /dev/sda` | Open partitioning tool |\n| 2 | `o` | Create a new empty **DOS partition table** |\n| 3 | `n` → `p` → `1` → `Enter` → `+512M` | Create 512 MB primary partition for `/boot` |\n| 4 | `n` → `p` → `2` → `Enter` → `+20G` | Create 20 GB primary partition for `/` |\n| 5 | `n` → `p` → `3` → `Enter` → `+2G` | Create 2 GB primary partition for `swap` |\n| 6 | `t` → `3` → `82` | Set partition 3 type to **Linux swap** |\n| 7 | `w` | Write table to disk and exit |\n\n**Step 3 — Format the Partitions**\n```bash\nsudo mkfs.ext4  /dev/sda1      # /boot\nsudo mkfs.ext4  /dev/sda2      # /\nsudo mkswap     /dev/sda3      # swap\nsudo swapon     /dev/sda3\n```\n\n**Step 4 — Mount and Install (Ubuntu/Debian debootstrap example)**\n```bash\nsudo mount /dev/sda2 /mnt\nsudo mkdir -p /mnt/boot\nsudo mount /dev/sda1 /mnt/boot\nsudo debootstrap focal /mnt http://archive.ubuntu.com/ubuntu/\n```\n\n**Step 5 — Install the Bootloader (GRUB)**\n```bash\nsudo grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=ubuntu /dev/sda\nsudo update-grub\n```\n\n**Step 6 — Reboot and Verify**\n```bash\nsudo reboot\nuname -a    # verify installed kernel\n```\n\n**Verification Formula (sanity check):**\n\n$$\\text{Number of partitions created} + \\text{bootloader installed} + \\text{filesystem mounted} = \\text{Successful Install}$$\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 User Space vs Kernel Space — The System Call Path\n\n```mermaid\nflowchart TB\n    A[\"User Application printf fork open\"] --> B[\"Wrapper Function glibc fork\"]\n    B --> C[\"System Call Interface Trap to Kernel\"]\n    C --> D[\"Kernel Mode Privileged Execution\"]\n    D --> E[\"Hardware Disk CPU Memory\"]\n    E --> D\n    D --> C\n    C --> F[\"Return Value / errno\"]\n    F --> A\n```\n\n### 4.2 Process Creation Flow — `fork()` + `exec()` + `wait()`\n\n```mermaid\nflowchart TB\n    P1[\"Parent Process PID 100\"] --> F[\"fork call\"]\n    F --> C1[\"Child Process PID 101\"]\n    F --> C2[\"Parent continues after fork\"]\n    C1 --> E[\"execlp replaces process image\"]\n    E --> N[\"New Program running as PID 101\"]\n    N --> EX[\"child exits with status code\"]\n    EX --> W[\"wait blocks parent\"]\n    W --> P2[\"Parent resumes and reaps child\"]\n```\n\n### 4.3 File Descriptor Lifecycle\n\n```mermaid\nflowchart LR\n    OF[\"open returns fd 3\"] --> U[\"read write lseek using fd 3\"]\n    U --> CL[\"close fd 3\"]\n    CL --> FR[\"File table entry released\"]\n    CL --> Z[\"fd 3 is now free for reuse\"]\n```\n\n### 4.4 Disk Partitioning & OS Installation Block Architecture\n\n```mermaid\nflowchart TB\n    DISK[\"Raw Disk /dev/sda\"] --> PT[\"Partition Table MBR or GPT\"]\n    PT --> P1[\"sda1 /boot 512 MB ext4\"]\n    PT --> P2[\"sda2 / 20 GB ext4\"]\n    PT --> P3[\"sda3 swap 2 GB\"]\n    P1 --> BS[\"Bootloader GRUB installed in sda1\"]\n    P2 --> K[\"Kernel vmlinuz placed in /boot\"]\n    P2 --> FS[\"Root filesystem populated by installer\"]\n    BS --> R[\"Reboot\"]\n    K --> R\n    FS --> R\n    R --> OS[\"Running Linux OS\"]\n```\n\n### 4.5 Memory Layout of a Process (Why `exec()` is a \"Replacement\")\n\n```mermaid\nflowchart TB\n    L[\"High Address 0xFFFFFFFF\"] --> A[\"Stack grows downward\"]\n    A --> H[\"Heap grows upward\"]\n    H --> D[\"Data Segment globals and static\"]\n    D --> T[\"Text Segment code readonly\"]\n    T --> Z[\"Low Address 0x00000000\"]\n    EX[\"execlp call\"] --> N[\"Entire address space wiped and replaced with new program\"]\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 📝 Part A — Short Answer Questions (3 Marks each)\n\n> **[KTU University Exam – Model Question A1]**\n> **Q1.** What is a system call? List any four categories of system calls with one example each.\n>\n> **Model Answer (3 marks):**\n> A *system call* is the programmatic interface through which a user process requests a service from the OS kernel. It is the **only legal mechanism** for user-mode code to access kernel-mode services.\n>\n> Four categories:\n> 1. **Process Control** — `fork()`, `exit()`\n> 2. **File Management** — `open()`, `close()`\n> 3. **Information Maintenance** — `getpid()`, `time()`\n> 4. **Communication** — `pipe()`, `socket()`\n\n---\n\n> **[KTU University Exam – Model Question A2]**\n> **Q2.** Differentiate between `fork()` and `exec()` system calls.\n>\n> **Model Answer (3 marks):**\n>\n> | Aspect | `fork()` | `exec()` |\n> |--------|----------|----------|\n> | New process created? | **Yes** | **No** (replaces current) |\n> | PID changes? | **Yes** (child gets new PID) | **No** |\n> | Code changes? | No (duplicate of parent) | **Yes** (new program loaded) |\n> | Return value | 0 in child, child PID in parent | Returns only on **failure** |\n\n---\n\n### 📝 Part B — Long Answer Questions (14 Marks)\n\n> **[KTU ESE – Module 2, 14-Mark Choice Question]**\n\n#### ❓ Question A (14 Marks)\n\n**(a)** Explain the working of the `fork()` system call with a suitable C program. Discuss the possible return values and what they indicate. **(7 marks)**\n\n**(b)** Write a C program that creates a child process using `fork()`. The child should execute the `ls -l` command using `execlp()`, and the parent should wait for the child to finish using `wait()`. Print the exit status of the child. **(7 marks)**\n\n---\n\n#### ✅ Model Solution — Question A\n\n**(a) Working of `fork()` (7 marks):**\n\n`fork()` creates a **child process** that is an almost exact duplicate of the parent. The two processes run **concurrently** from the instruction immediately following the `fork()` call.\n\n**Return Values:**\n- **Negative (`-1`):** Error — child not created (e.g., process table full, reached user process limit).\n- **Zero (`0`):** Returned in the **child** process.\n- **Positive (>0):** Returned in the **parent** — value is the **PID of the newly created child**.\n\n```c\n#include <stdio.h>\n#include <unistd.h>\n#include <sys/types.h>\n\nint main(void) {\n    pid_t pid;\n\n    printf(\"Before fork: PID = %d\\n\", getpid());\n    pid = fork();\n\n    if (pid < 0) {\n        fprintf(stderr, \"fork failed\\n\");\n        return 1;\n    }\n    else if (pid == 0) {\n        /* Child branch */\n        printf(\"CHILD : PID = %d, PPID = %d, fork returned %d\\n\",\n               getpid(), getppid(), pid);\n    }\n    else {\n        /* Parent branch */\n        printf(\"PARENT: PID = %d, Child PID = %d, fork returned %d\\n\",\n               getpid(), pid, pid);\n    }\n    return 0;\n}\n```\n\n**Explanation (for full marks):**\n- Before `fork()`: single process prints its PID.\n- After `fork()`: **two** processes exist.\n- The `if-else` distinguishes them using the return value.\n- Both processes execute the `return 0;` line independently.\n- The `pid` variable has **different values** in parent and child due to separate address spaces (Copy-on-Write).\n\n---\n\n**(b) `fork` + `execlp` + `wait` Program (7 marks):**\n\n```c\n#include <stdio.h>\n#include <stdlib.h>\n#include <unistd.h>\n#include <sys/wait.h>\n\nint main(void) {\n    pid_t pid = fork();\n\n    if (pid < 0) {\n        perror(\"fork\");\n        return 1;\n    }\n    else if (pid == 0) {\n        /* Child: replace image with ls -l */\n        printf(\"Child: running ls -l\\n\");\n        execlp(\"ls\", \"ls\", \"-l\", (char *)NULL);\n        /* If we reach here, exec failed */\n        perror(\"execlp\");\n        exit(EXIT_FAILURE);\n    }\n    else {\n        /* Parent: wait for child */\n        int status;\n        printf(\"Parent: waiting for child (PID %d)\\n\", pid);\n        wait(&status);\n\n        if (WIFEXITED(status))\n            printf(\"Parent: child exited with code %d\\n\",\n                   WEXITSTATUS(status));\n        else\n            printf(\"Parent: child did not exit normally\\n\");\n    }\n    return 0;\n}\n```\n\n**Output (sample):**\n```\nChild: running ls -l\nParent: waiting for child (PID 4521)\ntotal 12\n-rw-r--r-- 1 user user  380 May 10 10:20 file1.c\ndrwxr-xr-x 2 user user 4096 May 10 10:21 dirA\nParent: child exited with code 0\n```\n\n**Mark-by-mark breakdown:**\n- Correct header inclusions — **1 mark**\n- `fork()` call with return-value handling — **2 marks**\n- `execlp()` with correct argument list and NULL terminator — **2 marks**\n- `wait()` with `WIFEXITED` / `WEXITSTATUS` macros — **2 marks**\n\n---\n\n#### ❓ Question B (14 Marks)\n\n**(a)** Explain the `stat()` system call. Write a C program that uses `stat()` to display the size, inode number, and permissions of a file named `\"input.txt\"`. **(7 marks)**\n\n**(b)** With suitable diagrams, describe the steps involved in creating disk partitions and installing a Linux operating system. Compare MBR and GPT partitioning schemes. **(7 marks)**\n\n---\n\n#### ✅ Model Solution — Question B\n\n**(a) `stat()` System Call (7 marks):**\n\n`stat()` retrieves metadata about a file (or directory, symlink, etc.) and stores it in a `struct stat`.\n\n```c\n#include <stdio.h>\n#include <sys/stat.h>\n\nint main(void) {\n    struct stat st;\n\n    if (stat(\"input.txt\", &st) < 0) {\n        perror(\"stat\");\n        return 1;\n    }\n\n    printf(\"File size        : %ld bytes\\n\", (long)st.st_size);\n    printf(\"Inode number     : %ld\\n\",       (long)st.st_ino);\n    printf(\"Permissions      : %o\\n\",        st.st_mode & 0777);\n    printf(\"Hard link count  : %ld\\n\",       (long)st.st_nlink);\n    printf(\"Owner UID        : %d\\n\",        st.st_uid);\n    printf(\"Last modified    : %ld\\n\",       (long)st.st_mtime);\n\n    return 0;\n}\n```\n\n**Output (sample):**\n```\nFile size        : 1024 bytes\nInode number     : 524301\nPermissions      : 644\nHard link count  : 1\nOwner UID        : 1000\nLast modified    : 1715234582\n```\n\n**Mark-by-mark breakdown:**\n- Correct struct definition and `stat()` call — **2 marks**\n- Use of `st_size`, `st_ino`, `st_mode` — **2 marks**\n- Error handling with `perror()` — **1 mark**\n- Compilation and correct output — **2 marks**\n\n---\n\n**(b) Partitioning & OS Installation (7 marks):**\n\n**Steps to Create Partitions and Install Linux:**\n\n1. **Identify target disk** using `lsblk` or `fdisk -l`.\n2. **Partition the disk** using `fdisk` (MBR) or `gdisk` (GPT):\n   - `/boot` — 512 MB\n   - `/` — 20 GB\n   - `swap` — 2 GB (or equal to RAM)\n3. **Format partitions** with filesystems:\n   - `mkfs.ext4 /dev/sda1`\n   - `mkfs.ext4 /dev/sda2`\n   - `mkswap /dev/sda3`\n4. **Mount** target partitions at `/mnt`.\n5. **Install OS** via installer, `debootstrap`, or by copying live files.\n6. **Install bootloader** (GRUB) into the disk's MBR/EFI partition.\n7. **Reboot** and verify with `uname -a`.\n\n**MBR vs GPT Comparison (3 marks reserved in this sub-question):**\n\n| Feature | **MBR** | **GPT** |\n|---------|---------|---------|\n| Max Disk Size | **2 TB** | **9.4 ZB** (theoretical) |\n| Max Partitions | **4** primary | **128** primary |\n| Boot Mode | Legacy BIOS | UEFI |\n| Redundancy | Single table | Primary + backup table |\n| Sector Size | 512 bytes | 512 or **4096** bytes |\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> 1. **Confusing `fork()` with `exec()`** — students often say \"`exec()` creates a new process\". It does NOT. `exec()` *replaces* the current process image.\n> 2. **Forgetting the NULL terminator in `execlp()`** — this causes undefined behavior; the kernel may interpret garbage as additional arguments.\n> 3. **Not checking `fork()` return for `-1`** — losing 1 mark on error handling.\n> 4. **Writing `wait()` without a status variable** — `wait(NULL)` works but the examiner may deduct if the question explicitly asks for exit status.\n> 5. **Confusing `close()` with `unlink()`** — `close()` releases the file descriptor; `unlink()` deletes the directory entry.\n> 6. **Partition table confusion** — saying MBR supports 128 partitions (that's GPT).\n> 7. **Missing `#include <sys/wait.h>`** — `wait()` will not compile without it.\n\n---\n\n### 🧠 Topic Recap & Important Things to Remember\n\n- 🔑 A **system call** is the **only legal interface** between user programs and the kernel.\n- 🔑 `fork()` returns **3 possible values**: `-1` (error), `0` (child), `>0` (parent, child PID).\n- 🔑 `exec()` **replaces** the process image — PID is **preserved**, code is **overwritten**.\n- 🔑 `wait()` makes the parent **block** until any child terminates; it also **reaps** the child (prevents zombies).\n- 🔑 `getpid()` → own PID; `getppid()` → parent's PID; first process = `init`/PID `1`.\n- 🔑 `close(fd)` releases the file descriptor; default limit = `1024` per process.\n- 🔑 `stat(path, &buf)` fills a `struct stat` with file metadata (size, inode, mode, timestamps).\n- 🔑 **MBR**: max `2 TB`, `4` primary partitions, legacy BIOS.\n- 🔑 **GPT**: up to `128` partitions, no realistic size limit, UEFI-based.\n- 🔑 Standard Linux layout: `/boot` (512 MB) + `/` (20 GB) + `swap` (≈ RAM) + `/home` (rest).\n- 🔑 Bootloader **GRUB** is installed after filesystems are populated.\n- 🔑 Always compile with `gcc file.c -o output` and run with `./output`.\n- 🔑 Order of output after `fork()` is **non-deterministic** (scheduler-dependent).\n- 🔑 **Orphan** = parent exits first; **Zombie** = child exits but parent didn't `wait()`.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "information-technology-5-pcitl408-mExperiment 2-t2",
          "title": "Implementation of process and thread (life cycle of process): (i) process creation and termination, (ii) thread creation and termination",
          "content": "<!-- SECTION_1_START -->\n\n> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCITL408 - OPERATING SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Implementation of process and thread (life cycle of process): (i) process creation and termination, (ii) thread creation and termination\n\n# Experiment 2: Process and Thread Implementation in Linux\n\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Process — Formal KTU 2024 Definition\n\n> [!NOTE]\n> **Definition (KTU Syllabus Terminology):** A **process** is a program in execution. It is the fundamental unit of work in an operating system and consists of an executing program code (text section), its current activity (program counter, processor registers), a process stack (temporary data such as function parameters, return addresses, local variables), a data section (global variables), and a heap (memory dynamically allocated during run time). Each process is represented in the kernel by a **Process Control Block (PCB)** — a data structure containing all bookkeeping information: process ID, state, program counter, CPU registers, memory management info, accounting info, and I/O status.\n\n### 1.2 Thread — Formal KTU 2024 Definition\n\n> [!IMPORTANT]\n> **Definition (KTU Syllabus Terminology):** A **thread** is the smallest unit of CPU execution. It is a lightweight subprocess that shares the **code section, data section, and OS resources** (open files, signals) with its peer threads belonging to the same process, but maintains its **own thread ID, program counter, register set, and stack**. A process with at least one thread is called a **multithreaded process**.\n\n### 1.3 Conceptual Analogy — Plain English Intuition\n\nImagine a **large software company (the Process)**:\n\n| Concept | Real-World Analogy | OS Equivalent |\n|---|---|---|\n| **Process** | The entire company building a product | A running program in memory |\n| **Thread** | A worker inside the company | Independent flow of control |\n| **Process Creation** | A parent company spinning off a startup | `fork()` system call |\n| **Process Termination** | The startup shutting down | `exit()` system call |\n| **Thread Creation** | Hiring a new worker in the same company | `pthread_create()` |\n| **Shared Resources** | Shared office space, internet, printers | Code, data, heap, open files |\n\n> [!TIP]\n> **Memory trick for exam:** *Process = heavy container (has its own everything). Thread = light backpack inside the container (shares most stuff, carries only its own register state).*\n\n### 1.4 Key Standard Metrics (POSIX/Linux)\n\n- **`PID`** — Process Identifier; a unique positive integer assigned by the kernel. Default range: **2 to 32768** on 32-bit systems, much larger on 64-bit.\n- **`PPID`** — Parent Process Identifier.\n- **`TID`** — Thread Identifier; in Linux, threads are exposed as tasks with their own `TID`; the main thread's `TID == PID`.\n- **`getpid()` / `getppid()`** — POSIX calls to fetch the calling process's PID and parent's PID.\n- **`pthread_self()`** — POSIX call to fetch the calling thread's TID.\n- **`ps -ef`** — Linux command to list all running processes (essential for verifying orphan/zombie behavior in lab).\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Life Cycle of a Process — State Diagram Theory\n\nA process moves through a well-defined set of **states**. The KTU 2024 module specifically names the *life cycle of a process* — meaning you must draw and explain the **5-state process model**.\n\n### The Five Process States\n\n1. **New** — Process is being created; PCB is being constructed; not yet admitted to the ready queue.\n2. **Ready** — Process is waiting to be assigned to a processor. It has all resources except the CPU.\n3. **Running** — Instructions are being executed by the CPU. Exactly one process per CPU core can be in this state.\n4. **Blocked / Waiting** — Process is waiting for some event to occur (I/O completion, signal, child termination). Cannot run even if the CPU is free.\n5. **Terminated / Exit** — Process has finished execution. OS is reaping its PCB.\n\n### State Transition Triggers (KTU High-Yield)\n\n| From → To | Transition Name | Trigger Event |\n|---|---|---|\n| New → Ready | Admit | Long-term scheduler admits the process |\n| Ready → Running | Dispatch / Schedule | Short-term scheduler picks the process |\n| Running → Ready | Preempt / Time-out | Time quantum expired (or higher-priority process arrived) |\n| Running → Blocked | Block / I/O wait | Process requests I/O or waits on a system resource |\n| Blocked → Ready | Wake-up | The waited event occurs (I/O completes) |\n| Running → Terminated | Exit | Process calls `exit()` or is killed by signal |\n\n## 2.2 Process Creation — The `fork()` Mechanism\n\n> [!NOTE]\n> **Mechanics of `fork()`:** When a process invokes `fork()`, the kernel creates an almost-exact duplicate of the calling process. The new process is called the **child process**. The original is the **parent process**. Both continue execution from the point immediately after the `fork()` call, but with **divergent return values**:\n> - **Parent** receives the **child's PID** (a positive integer).\n> - **Child** receives **0**.\n> - On failure, the parent receives **-1** and no child is created (no child process is created).\n\n### Header File & Function Signature\n\n```c\n#include <unistd.h>\n#include <sys/types.h>\n\npid_t fork(void);\n```\n\n- `pid_t` is a POSIX type alias for `int` (or `long` on some systems).\n- `fork()` is declared in `<unistd.h>` and `<sys/types.h>`.\n\n### What `fork()` Duplicates vs. Shares\n\n| Resource | Behaviour After `fork()` |\n|---|---|\n| Code (text) section | **Duplicated** (copy-on-write, lazy) |\n| Data, BSS, heap | **Duplicated** (copy-on-write) |\n| Stack | **Duplicated** (copy-on-write) |\n| Open file descriptors | **Shared** (same underlying file description) |\n| Signal handlers | **Inherited** |\n| Environment, argv | **Inherited** |\n| Current working directory | **Inherited** |\n| Nice value, scheduling policy | **Inherited** |\n| Pending signals | **NOT inherited** (cleared in child) |\n\n## 2.3 Process Termination — Two Pathways\n\n> [!IMPORTANT]\n> **Normal Termination:** The process calls `exit()` (C library wrapper) → invokes `_exit()` (system call) → kernel frees most resources, marks zombie, sends `SIGCHLD` to parent.\n\n| Exit Mechanism | Description |\n|---|---|\n| `return` from `main()` | Implicitly calls `exit(main_return_value)` |\n| `exit(status)` | C library call: flushes stdio buffers, runs `atexit()` handlers, then calls `_exit()` |\n| `_exit(status)` | Direct system call: terminates immediately, no cleanup |\n| `abort()` | Raises `SIGABRT`, terminates with core dump |\n| Received uncatchable signal (e.g., `SIGKILL`, `SIGSEGV`) | Abnormal termination |\n\n> [!WARNING]\n> **Zombie vs. Orphan — KTU favourite question:**\n> - **Zombie process:** Child has terminated but parent has not yet called `wait()` / `waitpid()`. The PCB remains in the kernel, the child is \"dead but not reaped\". A long-lived zombie is a resource leak.\n> - **Orphan process:** Parent has terminated before the child. The orphan is **adopted by `init` (PID = 1)**, which will eventually `wait()` on it. Orphans are not leaks — they are normal.\n\n## 2.4 The `exec()` Family — Replacing a Process Image\n\nAfter `fork()`, the child typically calls `exec()` to replace its memory image with a new program. The six members of the `exec` family are listed below — **KTU expects you to know at least `execl`, `execv`, `execlp`, and `execvp`**.\n\n| Function | Path Argument | Argument Style | Search `PATH`? |\n|---|---|---|---|\n| `execl(path, arg0, ..., NULL)` | Full path required | List (variadic) | No |\n| `execlp(file, arg0, ..., NULL)` | Filename allowed | List (variadic) | **Yes** |\n| `execle(path, arg0, ..., NULL, envp)` | Full path required | List + env | No |\n| `execv(path, argv[])` | Full path required | Vector (`char *argv[]`) | No |\n| `execvp(file, argv[])` | Filename allowed | Vector (`char *argv[]`) | **Yes** |\n| `execve(path, argv[], envp[])` | Full path required | Vector + env | No (raw syscall) |\n\n## 2.5 The `wait()` System Call — Reaping a Child\n\n```c\n#include <sys/wait.h>\npid_t wait(int *status);\npid_t waitpid(pid_t pid, int *status, int options);\n```\n\n- `wait()` blocks the parent until **any one child** terminates.\n- `waitpid(-1, ...)` is equivalent to `wait()`.\n- `WIFEXITED(status)` → true if child terminated normally.\n- `WEXITSTATUS(status)` → extracts the low 8 bits of the exit code.\n- `WIFSIGNALED(status)` → true if child terminated by an uncaught signal.\n- `WTERMSIG(status)` → returns the signal number.\n\n## 2.6 Thread Creation — The POSIX Threads API\n\n> [!NOTE]\n> **Header file:** `#include <pthread.h>`\n> **Linker flag:** Compile with `-lpthread` (or `-pthread`).\n\n### Core Pthread Functions (KTU High-Yield)\n\n| Function | Purpose |\n|---|---|\n| `pthread_create(thread, attr, start_routine, arg)` | Create a new thread |\n| `pthread_exit(value)` | Terminate the calling thread, return `value` |\n| `pthread_join(thread, retval)` | Block until `thread` terminates, capture return value |\n| `pthread_self()` | Return TID of calling thread |\n| `pthread_equal(t1, t2)` | Compare two TIDs (cannot use `==` portably) |\n| `pthread_cancel(thread)` | Send cancellation request to a thread |\n\n### `pthread_create` Signature\n\n```c\nint pthread_create(pthread_t *thread,\n                   const pthread_attr_t *attr,\n                   void *(*start_routine)(void *),\n                   void *arg);\n```\n\n- `thread` — output: stores the new thread's TID.\n- `attr` — input: thread attributes (NULL = default).\n- `start_routine` — input: function pointer where the new thread begins execution.\n- `arg` — input: single `void *` argument passed to `start_routine`.\n\n## 2.7 Process vs. Thread — The Comparison Table (Always Asked)\n\n| Property | Process | Thread |\n|---|---|---|\n| Address space | **Independent** | **Shared** with peer threads |\n| Creation cost | **Heavy** (`fork`, copy-on-write) | **Light** (`pthread_create`) |\n| Communication | IPC (pipes, sockets, shared memory) | Shared globals (race-condition risk) |\n| Termination | `exit()` / signal | `pthread_exit()` / return from start_routine |\n| Context-switch cost | **High** (TLB flush, page tables) | **Low** (same page tables) |\n| Crash isolation | One process crash does NOT kill others | One thread crash can kill entire process |\n| Identifier | `PID` (`getpid()`) | `TID` (`pthread_self()`) |\n\n## 2.8 Real-World Engineering Utility\n\n- **Web servers (Apache, Nginx)** use a thread pool to handle thousands of concurrent client requests within a single process — a single `fork()` per request would never scale.\n- **Database engines (PostgreSQL)** spawn one process per connection but use threads internally for parallel query execution.\n- **GUI applications** keep the UI responsive by running long computations on a worker thread while the main thread handles the event loop.\n- **Chrome browser** famously uses one process per tab for crash isolation, with multiple threads per tab for rendering, networking, and JavaScript execution.\n\n## 2.9 KTU Formula Sheet / Cheat Sheet\n\n| Symbol / API | Meaning | Unit / Type |\n|---|---|---|\n| `fork()` | Create child process | Returns `pid_t` (0/child_PID/-1) |\n| `getpid()` | Get current process ID | Returns `pid_t` |\n| `getppid()` | Get parent process ID | Returns `pid_t` |\n| `exit(n)` | Terminate with status `n` | `n ∈ [0, 255]` |\n| `_exit(n)` | Immediate kernel exit (no cleanup) | `n ∈ [0, 255]` |\n| `wait(&s)` | Wait for any child | `s` is int holding status |\n| `execvp(file, argv)` | Replace image with `file` | Returns int (-1 on err) |\n| `pthread_create(&t,...)` | Create thread | Returns int (0 on success) |\n| `pthread_exit(v)` | Exit thread with value `v` | `v` is `void *` |\n| `pthread_join(t, &r)` | Wait for thread `t` | `r` receives `void *` |\n| `pthread_self()` | Get current thread ID | Returns `pthread_t` |\n| N total processes after N `fork()`s | $2^N$ | (excluding failures) |\n\n> **Critical rule for KTU exams:** Use `\\vert` or `\\mid` for absolute value to keep the table intact. Never use the bare `|` pipe character.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n# 3. Step-by-Step Derivations, Programs & Analytical Matrix\n\n> [!IMPORTANT]\n> **Lab Exam Convention (KTU 2024):** Every program must be presented with **(a) Aim, (b) Algorithm, (c) Program, (d) Sample Output, (e) Result**. Examiner marks depend on each section being clearly labeled.\n\n## 3.1 Experiment 2(a) — Process Creation, Termination & Life-Cycle Observation\n\n### 3.1.1 Aim\n\nTo write a C program that creates a child process using the `fork()` system call, demonstrates the divergent return values in parent and child, observes the **life cycle states** (New → Ready → Running → Terminated), illustrates **zombie** and **orphan** scenarios, and terminates cleanly using `exit()` and `wait()`.\n\n### 3.1.2 Algorithm\n\n**Step 1.** Start the program — this creates the **parent process** (initially in Running state).\n\n**Step 2.** Invoke `fork()`. The kernel creates a **new process** (enters New state, then immediately Ready, then eligible to run). Both parent and child resume execution from the line after `fork()`.\n\n**Step 3.** Test the return value of `fork()`:\n\n- If `pid < 0` → `fork()` failed; print error; terminate.\n- If `pid == 0` → this is the **child branch**. Print `getpid()`, `getppid()`. Sleep for a configurable time. Demonstrate two sub-scenarios:\n  - **Zombie scenario:** child terminates, parent does **not** call `wait()` — child becomes zombie.\n  - **Orphan scenario:** parent terminates *before* child — child becomes orphan, adopted by `init` (PID 1). Use `sleep()` in the parent before its own termination.\n- If `pid > 0` → this is the **parent branch**. Print `getpid()` and the child's PID returned by `fork()`. Call `wait(&status)` to **block** until the child terminates. Use `WIFEXITED` / `WEXITSTATUS` to extract the exit code.\n\n**Step 4.** Parent calls `exit(0)`. Kernel reaps the parent PCB.\n\n### 3.1.3 Program — Master Demonstration (Linux, GCC)\n\n```c\n/* exp2a_process_lifecycle.c\n * KTU PCITL408 - Experiment 2(a)\n * Demonstrates process creation, zombie, orphan, and life cycle.\n * Compile: gcc exp2a_process_lifecycle.c -o exp2a\n */\n#include <stdio.h>\n#include <stdlib.h>\n#include <unistd.h>\n#include <sys/types.h>\n#include <sys/wait.h>\n\nint main(void)\n{\n    pid_t pid;\n    int   status;\n\n    printf(\"[BEFORE FORK] PID=%d  PPID=%d\\n\", getpid(), getppid());\n\n    pid = fork();\n\n    if (pid < 0) {\n        perror(\"fork failed\");\n        exit(EXIT_FAILURE);\n    }\n    else if (pid == 0) {\n        /* ----------------- CHILD ----------------- */\n        printf(\"\\n[CHILD]  PID=%d  PPID=%d  fork() returned %d\\n\",\n               getpid(), getppid(), pid);\n        printf(\"[CHILD]  I will sleep 2 seconds, then exit.\\n\");\n        sleep(2);\n        printf(\"[CHILD]  Exiting now with status 42.\\n\");\n        exit(42);                          /* Normal termination */\n    }\n    else {\n        /* ----------------- PARENT ---------------- */\n        printf(\"\\n[PARENT] PID=%d  Child PID=%d  fork() returned %d\\n\",\n               getpid(), pid, pid);\n\n        printf(\"[PARENT] Sleeping 5 seconds. Run 'ps -ef | grep exp2a' \"\n               \"during this window to see the child as ZOMBIE (Z).\\n\");\n        sleep(5);                          /* Child will be zombie here */\n\n        printf(\"[PARENT] Calling wait() to reap child...\\n\");\n        wait(&status);\n\n        if (WIFEXITED(status))\n            printf(\"[PARENT] Child exited normally with code %d.\\n\",\n                   WEXITSTATUS(status));\n        else if (WIFSIGNALED(status))\n            printf(\"[PARENT] Child killed by signal %d.\\n\",\n                   WTERMSIG(status));\n\n        printf(\"[PARENT] Now spawning an ORPHAN demo...\\n\");\n\n        pid_t orphan = fork();\n        if (orphan == 0) {\n            /* Grand-child sleeps; its parent (the middle process)\n               will exit, leaving this one orphaned.                */\n            printf(\"[ORPHAN-CHILD] PID=%d  PPID=%d  sleeping 4s...\\n\",\n                   getpid(), getppid());\n            sleep(4);\n            printf(\"[ORPHAN-CHILD] Awake. My new PPID is %d \"\n                   \"(should be 1 = init).\\n\", getppid());\n            exit(0);\n        }\n\n        /* Middle process exits immediately, abandoning orphan-child */\n        printf(\"[PARENT] Exiting without waiting for orphan. Bye.\\n\");\n        exit(0);\n    }\n}\n```\n\n### 3.1.4 Sample Output (Annotated)\n\n```\n[BEFORE FORK] PID=10234  PPID=5678\n\n[PARENT] PID=10234  Child PID=10235  fork() returned 10235\n[PARENT] Sleeping 5 seconds. Run 'ps -ef | grep exp2a' ...\n\n[CHILD]  PID=10235  PPID=10234  fork() returned 0\n[CHILD]  I will sleep 2 seconds, then exit.\n[CHILD]  Exiting now with status 42.\n\n--- At t = 2s, run in another terminal:  ps -ef | grep exp2a ---\nUID  PID  PPID  C  STIME  TTY  STAT  TIME  CMD\nuser 10234 5678  0  10:00  pts/0 S     0:00  ./exp2a\nuser 10235 10234 0  10:00  pts/0 Z     0:00  [exp2a]   <defunct>\n--- STAT column = Z -> ZOMBIE ---\n\n[PARENT] Calling wait() to reap child...\n[PARENT] Child exited normally with code 42.\n[PARENT] Now spawning an ORPHAN demo...\n[ORPHAN-CHILD] PID=10236  PPID=10234  sleeping 4s...\n[PARENT] Exiting without waiting for orphan. Bye.\n[ORPHAN-CHILD] Awake. My new PPID is 1 (should be 1 = init).\n```\n\n### 3.1.5 Result\n\nThe program demonstrates:\n1. **Process creation** — `fork()` produces a child with a unique PID and PPID = parent's PID.\n2. **Divergent return values** — parent receives child's PID; child receives 0.\n3. **Zombie** — child terminated but not reaped; visible in `ps` with STAT = `Z`.\n4. **Orphan** — child outlives its parent; adopted by `init` (PPID = 1).\n5. **Clean termination** — `wait()` reaps the child; `exit(42)` returns a verifiable status.\n\n### 3.1.6 Analytical Matrix — Why Each Line Matters\n\n| Line | Purpose | KTU Marker |\n|---|---|---|\n| `#include <sys/wait.h>` | Enables `wait`, `WIFEXITED`, `WEXITSTATUS` | Mandatory header |\n| `pid_t pid = fork();` | Single call that creates the child | Core concept |\n| `if (pid == 0) {...}` | **Child branch** — return-value check | **Most-missed** point in exam |\n| `exit(42);` | C library exit; flushes stdio buffers | Normal termination |\n| `wait(&status);` | Parent blocks; reaps child PCB | Prevents zombies |\n| `WIFEXITED(status)` | Test for normal exit | Macro asked in viva |\n| `sleep(N)` | Creates the observation window for `ps` | Practical lab trick |\n\n## 3.2 Experiment 2(b) — Using `exec()` to Run a New Program in the Child\n\n### 3.2.1 Aim\n\nTo use `fork()` + `exec()` together: the child becomes a brand-new program while the parent continues (or waits).\n\n### 3.2.2 Program\n\n```c\n/* exp2b_fork_exec.c\n * KTU PCITL408 - Experiment 2(b)\n * Compile: gcc exp2b_fork_exec.c -o exp2b\n */\n#include <stdio.h>\n#include <stdlib.h>\n#include <unistd.h>\n#include <sys/wait.h>\n\nint main(void)\n{\n    pid_t pid = fork();\n\n    if (pid < 0) { perror(\"fork\"); exit(1); }\n\n    if (pid == 0) {\n        /* CHILD: replace image with `ls -l /tmp` */\n        printf(\"[CHILD %d] About to exec: ls -l /tmp\\n\", getpid());\n        execlp(\"ls\", \"ls\", \"-l\", \"/tmp\", (char *)NULL);\n        /* execlp returns ONLY on failure */\n        perror(\"execlp failed\");\n        exit(127);\n    }\n\n    /* PARENT */\n    printf(\"[PARENT %d] Waiting for child %d...\\n\", getpid(), pid);\n    int s;\n    wait(&s);\n    if (WIFEXITED(s))\n        printf(\"[PARENT] Child exited with %d\\n\", WEXITSTATUS(s));\n    return 0;\n}\n```\n\n### 3.2.3 Sample Output (Truncated)\n\n```\n[PARENT 5001] Waiting for child 5002...\n[CHILD 5002] About to exec: ls -l /tmp\ntotal 28\ndrwxrwxrwt 2 user user  4096 Apr  2 10:00 .X11-unix\ndrwxrwxrwt 2 user user  4096 Apr  2 10:00 .font-unix\n-rw-r--r-- 1 user user     0 Apr  2 09:58 tmp.log\n[PARENT] Child exited with 0\n```\n\n### 3.2.4 Key Insight\n\nAfter a successful `execlp`, the child's **entire address space is replaced**. The string `\"[CHILD] About to exec...\"` is printed because `printf` runs **before** `execlp`. Any code after a successful `exec*` is unreachable; therefore the `exit(127)` line runs **only if `execlp` failed** (e.g., program not found).\n\n## 3.3 Experiment 2(c) — Thread Creation, Termination & Synchronization\n\n### 3.3.1 Aim\n\nTo create multiple POSIX threads that run a common routine with distinct arguments, demonstrate thread termination via `pthread_exit`, synchronize via `pthread_join`, and inspect thread IDs.\n\n### 3.3.2 Algorithm\n\n**Step 1.** Define a thread function `void *print_msg(void *arg)` that receives a pointer to a `struct msg { int id; char *text; }` and prints it.\n\n**Step 2.** In `main`, declare an array of `pthread_t` handles and an array of argument structs.\n\n**Step 3.** Loop `i = 0..N-1`, call `pthread_create` to spawn each thread. The threads may execute in any order — concurrency is observed.\n\n**Step 4.** Loop `i = 0..N-1`, call `pthread_join` to wait for each thread and capture its return value.\n\n**Step 5.** Print all return values, terminate `main` (which implicitly calls `pthread_exit` for the main thread).\n\n### 3.3.3 Program\n\n```c\n/* exp2c_threads.c\n * KTU PCITL408 - Experiment 2(c)\n * Compile: gcc exp2c_threads.c -lpthread -o exp2c\n */\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#include <pthread.h>\n\n#define N 3\n\nstruct thread_arg {\n    int  id;\n    char *message;\n};\n\nvoid *worker(void *arg)\n{\n    struct thread_arg *a = (struct thread_arg *)arg;\n\n    /* Critical: detach-aware sleep so output order varies between runs */\n    printf(\"[Thread %d | TID=%lu] Hello. Message: \\\"%s\\\".\\n\",\n           a->id, (unsigned long)pthread_self(), a->message);\n\n    /* Allocate a return value on the heap; must outlive this thread */\n    char *ret = malloc(64);\n    snprintf(ret, 64, \"Thread %d done\", a->id);\n    pthread_exit((void *)ret);              /* Thread terminates here */\n}\n\nint main(void)\n{\n    pthread_t       tid[N];\n    struct thread_arg args[N];\n    void           *retval;\n\n    /* ---------- CREATE ---------- */\n    for (int i = 0; i < N; i++) {\n        args[i].id = i + 1;\n        args[i].message = (i == 0) ? \"First worker\"\n                       : (i == 1) ? \"Second worker\"\n                                  : \"Third worker\";\n\n        int rc = pthread_create(&tid[i], NULL, worker, &args[i]);\n        if (rc != 0) {\n            fprintf(stderr, \"pthread_create failed: %d\\n\", rc);\n            exit(1);\n        }\n        printf(\"[main] Created thread %d with TID=%lu\\n\",\n               i + 1, (unsigned long)tid[i]);\n    }\n\n    /* ---------- JOIN ---------- */\n    for (int i = 0; i < N; i++) {\n        pthread_join(tid[i], &retval);\n        printf(\"[main] Thread %d returned: \\\"%s\\\"\\n\", i + 1, (char *)retval);\n        free(retval);                      /* Avoid memory leak */\n    }\n\n    printf(\"[main] All threads joined. Exiting.\\n\");\n    /* Implicit pthread_exit(0) for main thread */\n    return 0;\n}\n```\n\n### 3.3.4 Sample Output (Order May Vary)\n\n```\n[main] Created thread 1 with TID=140123456\n[main] Created thread 2 with TID=140123457\n[main] Created thread 3 with TID=140123458\n[Thread 1 | TID=140123456] Hello. Message: \"First worker\".\n[Thread 2 | TID=140123457] Hello. Message: \"Second worker\".\n[Thread 3 | TID=140123458] Hello. Message: \"Third worker\".\n[main] Thread 1 returned: \"Thread 1 done\"\n[main] Thread 2 returned: \"Thread 2 done\"\n[main] Thread 3 returned: \"Thread 3 done\"\n[main] All threads joined. Exiting.\n```\n\n### 3.3.5 Analytical Matrix — What the Examiner Checks\n\n| Concept | Demonstrated By |\n|---|---|\n| Thread creation | `pthread_create` loop |\n| Passing arguments | `struct thread_arg` passed by **address** |\n| Thread identification | `pthread_self()` inside the worker |\n| Thread termination | `pthread_exit((void *)ret)` |\n| Heap-allocated return value | `malloc` so memory survives thread death |\n| Synchronization | `pthread_join` blocks main until thread finishes |\n| Memory hygiene | `free(retval)` after consuming the return value |\n\n> [!WARNING]\n> **Common bug to avoid:** Returning the address of a **local (stack) variable** from `worker`. The stack frame is destroyed when `worker` returns, leaving `pthread_join` with a dangling pointer. Always `malloc` the return value.\n\n## 3.4 Combined Demonstration — Process Tree Showing Full Life Cycle\n\nUse the following **derivation** style explanation for viva questions:\n\n> **\"Trace the life cycle of each process in the master program.\"**\n\n**Process A (parent, PID = 10234):**\n- Enters state **New** at program start.\n- Moves to **Ready** after the OS admits it to the run queue.\n- Enters **Running** when the scheduler dispatches it.\n- Enters **Blocked** during `sleep(5)` (waiting for timer event).\n- Returns to **Ready** when timer fires.\n- Enters **Running** again, executes `wait()`, then enters **Blocked** for the orphan demo.\n- Enters **Terminated** after `exit(0)`.\n\n**Process B (child, PID = 10235):**\n- Enters **New** at `fork()`.\n- Enters **Ready**; scheduled.\n- Enters **Running**, executes `printf`, then enters **Blocked** during `sleep(2)`.\n- Returns to **Ready**, then **Running**, calls `exit(42)`.\n- Enters **Terminated**; sits as a **Zombie** for ~3 seconds until parent calls `wait()`.\n- PCB is reaped and the entry is removed from the process table.\n\n**Process C (orphan-grandchild, PID = 10236):**\n- Enters **New** at `fork()` inside the dying parent.\n- Enters **Ready**; scheduled.\n- Enters **Running**, prints info, then **Blocked** during `sleep(4)`.\n- While blocked, its parent (A) exits. The kernel reparents C to **init (PID 1)**.\n- Returns to **Ready** then **Running**, observes `getppid() == 1`.\n- Calls `exit(0)` and is reaped by `init`.\n\n## 3.5 Derivations of the `fork()` Counting Trick (Viva Favourite)\n\n> **Question:** \"If you call `fork()` three times, how many processes result?\"\n\n**Derivation:**\n\n$$N_{\\text{procs}} = 2^{k}$$\n\nwhere $k$ is the number of `fork()` calls and no `exec`/`exit` precedes subsequent forks.\n\n**Step-by-step for $k = 3$:**\n\n```\nInitial:                    1 process\nAfter fork() #1:            2 processes   (2^1)\nAfter fork() #2:            4 processes   (2^2)\nAfter fork() #3:            8 processes   (2^3)\n```\n\nSo $N_{\\text{procs}} = 2^3 = \\mathbf{8}$.\n\n**Total number of \"Hello\" lines printed if each process prints once:**\n\n$$L = 2^{k} = 8$$\n\nbecause every one of the 8 processes executes the `printf` exactly once.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Process State Transition Diagram (5-State Model)\n\n```mermaid\nstateDiagram-v2\n    [*] --> New\n    New --> Ready : admit / long-term scheduler\n    Ready --> Running : dispatch / short-term scheduler\n    Running --> Ready : preempt / quantum-expire\n    Running --> Blocked : I-O or wait-on-event\n    Blocked --> Ready : event-completion / wakeup\n    Running --> Terminated : exit() or signal\n    Terminated --> [*] : reaped by parent via wait()\n```\n\n> **Exam tip:** Always label the **arrows with the triggering event** (admit, dispatch, preempt, I/O, wakeup, exit). The KTU board examiner gives partial marks for unlabelled arrows.\n\n## 4.2 `fork()` Execution Flow — Program Counter Point of Divergence\n\n```mermaid\nflowchart TD\n    A[Parent P0 begins execution] --> B[pid = fork&#40;&#41;]\n    B --> C{Return value of fork}\n    C -- pid < 0 --> D[perror and exit FAILURE]\n    C -- pid == 0 --> E[Child P1 branch]\n    C -- pid > 0  --> F[Parent P0 branch]\n    E --> E1[print child info]\n    E1 --> E2[sleep / do child work]\n    E2 --> E3[exit&#40;status&#41;]\n    E3 --> E4[Child becomes ZOMBIE until parent reaps]\n    F --> F1[print parent info]\n    F1 --> F2[wait&#40;&status&#41; blocks]\n    F2 --> F3{Child done?}\n    F3 -- yes --> F4[WIFEXITED WEXITSTATUS]\n    F4 --> F5[Parent exit&#40;0&#41;]\n    E4 --> F3\n    F5 --> G[Process removed from process table]\n```\n\n## 4.3 Thread Architecture — Process vs. Threads\n\n```mermaid\nflowchart LR\n    subgraph PROC[Process Address Space]\n        direction TB\n        CODE[Shared: Code Section]\n        DATA[Shared: Data, BSS, Heap]\n        FD[Shared: Open File Descriptors]\n\n        subgraph T1[Thread 1]\n            TID1[TID 1]\n            PC1[PC]\n            REG1[Registers]\n            STK1[Stack]\n        end\n        subgraph T2[Thread 2]\n            TID2[TID 2]\n            PC2[PC]\n            REG2[Registers]\n            STK2[Stack]\n        end\n        subgraph T3[Thread 3]\n            TID3[TID 3]\n            PC3[PC]\n            REG3[Registers]\n            STK3[Stack]\n        end\n    end\n\n    T1 --- CODE\n    T2 --- CODE\n    T3 --- CODE\n    T1 --- DATA\n    T2 --- DATA\n    T3 --- DATA\n    T1 --- FD\n    T2 --- FD\n    T3 --- FD\n```\n\n## 4.4 POSIX Thread Life Cycle\n\n```mermaid\nstateDiagram-v2\n    [*] --> Created : pthread_create&#40;&#41; succeeds\n    Created --> Ready : kernel schedules the task\n    Ready --> Running : CPU dispatched\n    Running --> Ready : pre-empted or blocked\n    Running --> Terminated : pthread_exit&#40;val&#41; / return from start_routine\n    Running --> Cancelled : pthread_cancel&#40;&#41; accepted\n    Terminated --> Joined : pthread_join&#40;&#41; collects retval\n    Joined --> [*]\n    Cancelled --> Joined\n```\n\n## 4.5 Zombie vs. Orphan — Visual Comparison\n\n```mermaid\nflowchart TD\n    subgraph ZOM[Zombie Scenario]\n        P0a[Parent P0] -- still running, did not wait&#40;&#41; --> Za[Child P1]\n        Za -- terminated, PCB retained --> Zb[Defunct / State Z in ps]\n    end\n\n    subgraph ORP[Orphan Scenario]\n        P0b[Parent P0] -- exits early --> Oa[Child P1]\n        Oa -- reparented --> Ob[Adopted by init / PID 1]\n        Ob -- init calls wait&#40;&#41; --> Oc[Reaped normally]\n    end\n```\n\n## 4.6 Combined `fork()` + `exec()` Flow\n\n```mermaid\nsequenceDiagram\n    participant U as User\n    participant P as Parent Process\n    participant C as Child Process\n    participant K as Kernel\n    participant X as New Program (e.g. ls)\n\n    U->>P: launch main&#40;&#41;\n    P->>K: fork&#40;&#41;\n    K-->>P: return child PID &#40;>0&#41;\n    K-->>C: return 0\n    par Parent path\n        P->>P: wait&#40;&#41; blocks\n    and Child path\n        C->>K: execve&#40;ls, argv, envp&#41;\n        K->>X: load new image\n        X-->>U: prints output\n        X->>K: exit&#40;0&#41;\n    end\n    K-->>P: child reaped, wait&#40;&#41; returns\n    P->>K: exit&#40;0&#41;\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. **[KTU University Exam – Dec 2023, Model Paper 1]**\n**Differentiate between a process and a thread. List any three differences.**\n\n**Model Answer (6–8 lines):**\n\n| # | Process | Thread |\n|---|---|---|\n| 1 | Heavy-weight; has its own address space, PCB, and system resources. | Light-weight; shares the address space and resources of its parent process. |\n| 2 | Created using `fork()` system call; context-switch cost is high (TLB flush). | Created using `pthread_create()`; context-switch cost is low. |\n| 3 | Process is scheduled by the OS as an independent unit. | Thread is the basic CPU execution unit; the process is the resource owner. |\n| 4 | Communication requires IPC mechanisms (pipes, sockets, shared memory). | Communication via shared global variables (with proper synchronisation). |\n| 5 | A crash in one process does not affect siblings. | A crash in one thread can bring down the entire process. |\n\n> **(3 marks awarded for: correct definition + 3 distinct differences with proper technical terms.)**\n\n### Q2. **[KTU University Exam – Jun 2024, Model Paper 1]**\n**What is a zombie process? How is it created and how is it eliminated?**\n\n**Model Answer (5–7 lines):**\n\n- A **zombie process** is one that has completed execution (`exit()` called) but still has an entry in the kernel's process table because its parent has not yet reaped it via `wait()` / `waitpid()`.\n- **Creation:** Parent `fork()`s a child, the child terminates quickly (e.g., `exit(0)`), but the parent is busy in `sleep()` or computation and has not invoked `wait()` yet. The child's PCB remains as a `Z` (defunct) entry in `ps`.\n- **Elimination:** The parent calls `wait(&status)` or `waitpid(-1, &status, 0)`. The kernel removes the child's PCB from the process table. If the parent dies first, `init` (PID 1) automatically inherits and reaps the orphan — thereby also removing the zombie.\n\n> **(3 marks: definition 1, creation 1, elimination 1.)**\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks Each, Internal Choice)\n\n> **[KTU 2024 Scheme Rule]:** Each 14-mark question carries two sub-parts: **(a) 7 marks** + **(b) 7 marks**. Provide the complete model solution for both.\n\n---\n\n### Question A (14 Marks) — **[KTU University Exam – Dec 2023, Model Paper 1]**\n\n**(a)** With the help of a neat diagram, explain the **five-state process life cycle model**. List all possible state transitions and the events that trigger them. **(7 marks)**\n\n**(b)** Write a C program that uses the `fork()` system call to create a child process. The child should print its PID and PPID, sleep for 3 seconds, and exit with status 25. The parent should wait for the child, print the child's exit status, and then exit. Also explain why `wait()` is necessary. **(7 marks)**\n\n#### Model Solution to A(a)\n\nThe **five-state process model** represents the dynamic life of a process in a multiprogramming OS. The states are:\n\n- **New** — PCB is being created.\n- **Ready** — Waiting in the ready queue for CPU allocation.\n- **Running** — Currently executing on the CPU.\n- **Blocked (Waiting)** — Cannot run because it is waiting for an I/O completion, a child to terminate, or a signal.\n- **Terminated (Exit)** — Execution finished; PCB is waiting to be reaped.\n\n**Transitions and Triggers (mark each transition for full marks):**\n\n| Transition | Trigger Event | Responsible Scheduler |\n|---|---|---|\n| New → Ready | Admit (long-term scheduler admits the process to the ready queue) | Long-term / Admission scheduler |\n| Ready → Running | Dispatch / Schedule (short-term scheduler picks it) | Short-term / CPU scheduler |\n| Running → Ready | Preempt (time quantum expired) or interrupt | Short-term scheduler |\n| Running → Blocked | I/O request, `wait()` for child, semaphore blocked, `sleep()` | I/O subsystem / process itself |\n| Blocked → Ready | I/O completion, child terminated, signal received, timeout | Interrupt handler / kernel |\n| Running → Terminated | `exit()` called, `return` from `main()`, or uncaught fatal signal | Process itself / kernel |\n\n**Diagram (must be drawn in the answer book):**\n\n```\n           +---------+\n           |   New   |\n           +----+----+\n                | admit\n                v\n           +---------+\n   wakeup   |  Ready |  <----------------+\n   (event)  +----+----+                  |\n                | dispatch               | preempt / time-out\n                v                        |\n           +---------+   I/O or wait   +-----------+\n           | Running | --------------> |  Blocked  |\n           +----+----+                 +-----------+\n                | exit / fatal signal\n                v\n           +------------+\n           | Terminated |\n           +------------+\n```\n\n#### Model Solution to A(b)\n\n**Algorithm:**\n1. `pid = fork()`.\n2. If `pid < 0` → error, exit.\n3. If `pid == 0` → child: print `getpid()` and `getppid()`; `sleep(3)`; `exit(25)`.\n4. If `pid > 0` → parent: `wait(&s)`; use `WIFEXITED(s)` and `WEXITSTATUS(s)` to print exit code; `exit(0)`.\n\n**Program:**\n\n```c\n#include <stdio.h>\n#include <stdlib.h>\n#include <unistd.h>\n#include <sys/wait.h>\n\nint main(void) {\n    pid_t pid = fork();\n    if (pid < 0) { perror(\"fork\"); exit(1); }\n\n    if (pid == 0) {\n        printf(\"[CHILD]  PID=%d  PPID=%d\\n\", getpid(), getppid());\n        sleep(3);\n        printf(\"[CHILD]  Exiting with status 25\\n\");\n        exit(25);\n    }\n\n    int s;\n    printf(\"[PARENT] PID=%d, child=%d. Waiting...\\n\", getpid(), pid);\n    wait(&s);\n    if (WIFEXITED(s))\n        printf(\"[PARENT] Child exit code = %d\\n\", WEXITSTATUS(s));\n    else\n        printf(\"[PARENT] Child abnormal termination\\n\");\n    return 0;\n}\n```\n\n**Sample Output:**\n\n```\n[PARENT] PID=2001, child=2002. Waiting...\n[CHILD]  PID=2002  PPID=2001\n[CHILD]  Exiting with status 25\n[PARENT] Child exit code = 25\n```\n\n**Why `wait()` is necessary:**\n\n- It **blocks the parent** until the child terminates — preventing a race where the parent exits before the child finishes.\n- It **reaps the child's PCB**, freeing the kernel's process-table slot and avoiding a **zombie process** accumulation.\n- It allows the parent to **retrieve the child's exit status** using the macros `WIFEXITED`, `WEXITSTATUS`, `WIFSIGNALED`, `WTERMSIG`.\n\n> **(Distribution: program 3 marks, explanation of `wait()` 2 marks, sample output 1 mark, state transition triggers 1 mark — examiner discretion.)**\n\n---\n\n### Question B (14 Marks) — **[KTU University Exam – Jun 2024, Model Paper 2]**\n\n**(a)** Explain the difference between `fork()` and `exec()` system calls. Write a C program where the child process replaces its image with the Unix command `ls -l` using `execlp()`. The parent must wait for the child and report the exit status. **(7 marks)**\n\n**(b)** Write a C program using **POSIX threads (pthreads)** that creates three threads. Each thread receives an integer ID and prints `\"Thread X is running\"`, then exits cleanly using `pthread_exit()`. The `main` thread must wait for all three threads using `pthread_join()` and print a final message. **(7 marks)**\n\n#### Model Solution to B(a)\n\n**`fork()` vs `exec()` comparison:**\n\n| Aspect | `fork()` | `exec()` |\n|---|---|---|\n| Purpose | Create a new (child) process | Replace the current process image with a new program |\n| New PID? | **Yes**, child gets a new PID | **No**, PID remains the same |\n| Address space | Duplicated (copy-on-write) | **Replaced** by the new program |\n| Return on success | Two return values: 0 in child, child PID in parent | **No return** — only returns `-1` on failure |\n| Typical use | `fork()` + `exec()` together to run a new program from a child | Right after `fork()` in the child branch |\n\n**Program:**\n\n```c\n#include <stdio.h>\n#include <stdlib.h>\n#include <unistd.h>\n#include <sys/wait.h>\n\nint main(void) {\n    pid_t pid = fork();\n    if (pid < 0) { perror(\"fork\"); exit(1); }\n\n    if (pid == 0) {\n        printf(\"[CHILD %d] Calling execlp(ls -l)\\n\", getpid());\n        execlp(\"ls\", \"ls\", \"-l\", (char *)NULL);\n        perror(\"execlp\");   /* reached only on failure */\n        exit(127);\n    }\n\n    int s;\n    wait(&s);\n    if (WIFEXITED(s))\n        printf(\"[PARENT] ls exited with %d\\n\", WEXITSTATUS(s));\n    return 0;\n}\n```\n\n**Sample Output (truncated):**\n\n```\n[CHILD 3210] Calling execlp(ls -l)\ntotal 48\n-rw-r--r-- 1 user user  220 Apr  2 09:58 .bash_logout\n-rw-r--r-- 1 user user 3771 Apr  2 09:58 .bashrc\ndrwxr-xr-x 2 user user  4096 Apr  2 10:00 exp2a\n-rwxr-xr-x 1 user user  8432 Apr  2 10:01 exp2b\n[PARENT] ls exited with 0\n```\n\n#### Model Solution to B(b)\n\n**Algorithm:**\n1. Declare `pthread_t tid[3]` and argument array.\n2. Loop i = 0..2, call `pthread_create(&tid[i], NULL, runner, &args[i])`.\n3. Loop i = 0..2, call `pthread_join(tid[i], NULL)`.\n4. Print `\"All threads completed\"`.\n\n**Program:**\n\n```c\n#include <stdio.h>\n#include <pthread.h>\n\n#define N 3\n\nvoid *runner(void *arg) {\n    int id = *(int *)arg;\n    printf(\"Thread %d is running (TID=%lu)\\n\",\n           id, (unsigned long)pthread_self());\n    pthread_exit(NULL);                      /* Clean thread termination */\n}\n\nint main(void) {\n    pthread_t tid[N];\n    int       ids[N] = {1, 2, 3};\n\n    for (int i = 0; i < N; i++) {\n        if (pthread_create(&tid[i], NULL, runner, &ids[i]) != 0) {\n            fprintf(stderr, \"pthread_create failed\\n\");\n            return 1;\n        }\n    }\n    for (int i = 0; i < N; i++) pthread_join(tid[i], NULL);\n\n    printf(\"All threads completed. Main exiting.\\n\");\n    pthread_exit(NULL);\n}\n```\n\n**Sample Output (order may vary):**\n\n```\nThread 1 is running (TID=140234567890432)\nThread 2 is running (TID=140234567890433)\nThread 3 is running (TID=140234567890434)\nAll threads completed. Main exiting.\n```\n\n**Explanation of key calls:**\n\n- `pthread_create` — spawns a new thread that begins execution at `runner` and receives `&ids[i]` as its argument.\n- `pthread_exit(NULL)` — terminates the calling thread and returns a `NULL` status to a future `pthread_join`.\n- `pthread_join` — blocks the calling (main) thread until the target thread terminates; prevents zombie threads and ensures deterministic completion.\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfalls — How Marks Are Lost**\n> 1. **Forgetting `-lpthread` while compiling.** Program will not link. Examiner deducts 1 mark.\n> 2. **Confusing `exit()` with `pthread_exit()` in a threaded program.** `exit()` terminates the **entire process** and all threads; `pthread_exit()` terminates **only the calling thread**. Using `exit()` inside a thread kills peer threads.\n> 3. **Not calling `pthread_join()`.** The main thread may exit before worker threads, leaving them in an undefined state.\n> 4. **Returning a pointer to a local (stack) variable** from the thread function. The frame is destroyed when the thread returns — `pthread_join` retrieves a dangling pointer.\n> 5. **Confusing `fork()` and `pthread_create()`.** Threads share memory; processes do not. Many students write `pthread_create(&pid, ...)` — wrong function and wrong variable type.\n> 6. **Forgetting the `if (pid == 0)` check.** The same code runs in BOTH parent and child, leading to double output.\n> 7. **Not including `<sys/wait.h>` for `wait()` and the `W` macros.** Compilation error.\n> 8. **Incorrect `execlp` argument list** — missing the terminating `(char *)NULL` causes undefined behaviour.\n> 9. **Returning an integer instead of `void *`** from the thread function signature — compilation error.\n> 10. **Believing `pthread_join` returns the thread's exit value directly.** It returns the value via the second argument; the function itself returns an `int` error code.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> **Rapid Revision Checklist — Print This Before the Exam**\n\n### Process Fundamentals\n- A **process** = program in execution + context (registers, stack, heap, PCB).\n- **PCB** holds PID, state, PC, registers, memory map, open files, accounting.\n- **PID 1** is `init` (or `systemd`) — the ancestor of all user processes.\n\n### Process Creation (`fork`)\n- `pid_t pid = fork();`\n- Returns **child PID** in parent, **0** in child, **-1** on error.\n- Child is a near-duplicate: own PID, PPID, copies of code/data/heap/stack; shared open file descriptors.\n- After `fork`, both resume from the line after the call.\n\n### `exec` Family\n- **Replaces** the process image with a new program.\n- Six variants: `execl, execlp, execle, execv, execvp, execve`.\n- `p` suffix → searches `PATH`; `v` suffix → takes `char *argv[]`; `e` suffix → accepts explicit environment.\n- On success, never returns. On failure, returns `-1`.\n\n### Process Termination\n- **Normal:** `exit(n)`, `return` from `main`, `_exit(n)`.\n- **Abnormal:** uncaught signal (e.g., `SIGKILL`, `SIGSEGV`).\n- **Zombie:** child dead, not reaped by parent → `Z` in `ps`.\n- **Orphan:** parent dead → adopted by `init` (PPID = 1).\n\n### `wait` / `waitpid`\n- `wait(&s)` — blocks until any child terminates.\n- `WIFEXITED(s)` + `WEXITSTATUS(s)` — extract exit code.\n- `WIFSIGNALED(s)` + `WTERMSIG(s)` — extract killing signal.\n- Always call `wait()` to **avoid zombies**.\n\n### Thread Fundamentals\n- **Thread** = lightweight execution unit sharing the process's address space.\n- Each thread has its **own TID, PC, register set, stack**.\n- All threads share **code, data, heap, open files**.\n\n### POSIX Threads API\n- Header: `<pthread.h>`. Link flag: `-lpthread`.\n- `pthread_create(&t, NULL, fn, arg)` — creates thread.\n- `pthread_exit((void *)val)` — terminates calling thread.\n- `pthread_join(t, &retval)` — waits for thread; receives return value.\n- `pthread_self()` — returns caller's TID (compare with `pthread_equal`).\n\n### Process vs. Thread — Must-Memorize Differences\n- **Address space:** process = independent; thread = shared.\n- **Creation cost:** process = heavy; thread = light.\n- **Comm cost:** process = IPC; thread = shared globals.\n- **Crash scope:** process = isolated; thread = whole process.\n- **Identifier:** process = `getpid()`; thread = `pthread_self()`.\n\n### Formulas to Memorize\n- $N$ processes after $N$ `fork()` calls (without intervening `exec`): $2^{N}$.\n- `wait` macros: $WIFEXITED$, $WEXITSTATUS$, $WIFSIGNALED$, $WTERMSIG$.\n- Compile: `gcc file.c -o file -lpthread`.\n\n### Viva Quick-Fire One-Liners\n- **Why two return values in `fork`?** So the parent and child can take divergent code paths.\n- **Why is copy-on-write used?** To avoid the cost of immediate full duplication; pages are copied only when one side writes.\n- **What happens to a thread's stack after `pthread_exit`?** It is freed by the kernel once `pthread_join` (or detach) is done.\n- **Can a thread call `fork`?** Yes, but only the thread that called `fork` exists in the child.\n- **What is a \"detached\" thread?** A thread created with `PTHREAD_CREATE_DETACHED`; its resources are auto-reclaimed on exit, no `pthread_join` allowed.\n\n---\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "information-technology-5-pcitl408-mExperiment 2-t3",
          "title": "Implement process scheduling algorithms (FCFS, SJF, round robin, priority) and compute average waiting time and average turn-around time",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCITL408 - OPERATING SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Implement process scheduling algorithms (FCFS, SJF, round robin, priority) and compute average waiting time and average turn-around time\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 What is Process Scheduling?\n\n**Process Scheduling** is the activity of the Operating System (OS) that decides which of several ready-to-run processes in the ready queue should be allocated the CPU next. It is a fundamental function of the OS process manager and is the mechanism that makes **multiprogramming** and **time-sharing** possible.\n\n> [!NOTE]\n> **KTU Syllabus Definition (PCITL408):** Process scheduling algorithms determine the order in which processes are allocated the CPU. The lab experiment requires *implementation* (programming) and *computation* (calculation) of two key performance metrics: **Average Waiting Time (AWT)** and **Average Turn-Around Time (ATT)**.\n\n### 1.2 Conceptual Analogy – The Airport Runway 🛫\n\nImagine a **single runway airport** (the CPU) where multiple flights (processes) are lined up waiting to take off. Each flight has an **arrival time** (when it reaches the queue) and a **service time** (how long it takes to use the runway).\n\n- **FCFS** = First plane in line takes off first, regardless of size.\n- **SJF** = The *shortest* takeoff time goes next (like letting a small private jet jump ahead of a jumbo jet).\n- **Round Robin** = Each plane gets a fixed *time slot* of runway usage; if it doesn't finish, it re-joins the queue.\n- **Priority** = VIP planes (high priority) take off before regular ones, even if they arrived later.\n\nThis is exactly how a CPU scheduler treats processes!\n\n### 1.3 Key Terminology (Must Memorize for KTU)\n\n| Term | Definition | Symbol |\n|---|---|---|\n| **Arrival Time** | The time at which a process enters the ready queue | $AT$ |\n| **Burst Time** | The total CPU time required by the process | $BT$ |\n| **Completion Time** | The time at which the process finishes execution | $CT$ |\n| **Turn-Around Time** | Total time spent by the process in the system | $TAT$ |\n| **Waiting Time** | Total time the process waited in the ready queue | $WT$ |\n| **Response Time** | Time from arrival until first CPU allocation | $RT$ |\n| **Gantt Chart** | A bar-chart visualization of process execution order | — |\n\n### 1.4 Core Formulas (Engineering Metrics in **bold**)\n\n> [!IMPORTANT]\n> These two formulas are the **heart of the experiment** — you will be computing these for every algorithm tested in your KTU lab exam.\n\n- **Turn-Around Time:** $TAT = CT - AT$\n- **Waiting Time:** $WT = TAT - BT$  (equivalently, $WT = CT - AT - BT$)\n- **Average Turn-Around Time:** $ATT = \\frac{\\sum_{i=1}^{n} TAT_i}{n}$\n- **Average Waiting Time:** $AWT = \\frac{\\sum_{i=1}^{n} WT_i}{n}$\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Algorithm 1: First-Come, First-Served (FCFS)\n\n**Operational Logic (Step-by-Step):**\n- All arriving processes are placed in a **single FIFO ready queue**.\n- The scheduler picks the process at the **head of the queue** and runs it to **completion (non-preemptive)**.\n- The next process is then picked from the head.\n- **Throughput is NOT considered**, only arrival order.\n\n**Why it matters:** Simplest possible scheduler. Forms the *baseline* against which all other algorithms are compared in your lab record.\n\n**Real-world utility:** Used in **batch operating systems** (e.g., IBM mainframe job queues).\n\n**The Convoy Effect:** FCFS suffers from the *convoy effect* — a single long process delays all subsequent short ones. Example: One 100ms process followed by ten 1ms processes → ten processes wait 100ms each!\n\n---\n\n### 2.2 Algorithm 2: Shortest Job First (SJF)\n\n**Two Variants:**\n- **Non-Preemptive SJF:** Once a process starts, it runs to completion. The scheduler picks the *shortest* job from those that have already arrived.\n- **Preemptive SJF** (also called **Shortest Remaining Time First, SRTF**): If a new process arrives with a burst time *less than the remaining time* of the currently running process, the running process is *preempted*.\n\n**Why it matters:** SJF is **provably optimal** for minimizing the **Average Waiting Time (AWT)** — a theorem often asked in KTU theory exams.\n\n> [!NOTE]\n> **Optimality Theorem:** Among all non-preemptive scheduling algorithms, SJF gives the **minimum possible AWT** for a given set of processes. This is a guaranteed KTU favorite question.\n\n**Real-world utility:** Forms the *theoretical core* of modern schedulers (e.g., Linux's CFS scheduler uses a similar idea with \"virtual runtimes\").\n\n**Drawback:** **Starvation** — long processes may never get CPU if short jobs keep arriving.\n\n---\n\n### 2.3 Algorithm 3: Round Robin (RR)\n\n**Operational Logic (Step-by-Step):**\n- Each process is assigned a **Time Quantum (TQ)** — a small fixed unit of CPU time.\n- The scheduler runs the head-of-queue process for **at most TQ units**.\n- If the process finishes within TQ, it leaves the system.\n- If not, it is **preempted and appended to the tail of the ready queue**.\n- The scheduler then picks the *new head* and continues.\n\n**Why it matters:** RR is the **default scheduler for time-sharing systems** (Linux, Windows, macOS). It provides *fairness* and good *response time*.\n\n**Choice of Time Quantum:**\n- **TQ too large** → behaves like FCFS.\n- **TQ too small** → excessive context-switch overhead.\n- **Rule of thumb:** TQ should be slightly greater than the *average burst time* of all processes.\n\n**Real-world utility:** Every interactive desktop OS uses a form of RR for foreground processes.\n\n---\n\n### 2.4 Algorithm 4: Priority Scheduling\n\n**Operational Logic (Step-by-Step):**\n- Each process is assigned a **priority number** (lower number = higher priority, or vice-versa, depending on convention).\n- The scheduler always runs the **highest-priority ready process**.\n- Can be **preemptive** (newly arrived high-priority process preempts current) or **non-preemptive**.\n\n**Why it matters:** Used in **real-time operating systems (RTOS)** where mission-critical tasks (e.g., anti-lock brakes) must preempt less critical ones (e.g., music player).\n\n**Drawback:** **Starvation / Indefinite Blocking** — low-priority processes may *never* run. The solution is **Aging** — gradually increasing the priority of processes that have waited a long time.\n\n> [!IMPORTANT]\n> **Aging:** A technique where the priority of a process is increased the longer it waits. This *guarantees* that even low-priority processes will eventually run. Mention this in your lab record to score bonus marks.\n\n---\n\n### 2.5 KTU Formula Sheet / Cheat Sheet\n\n| Metric | Formula | Used In |\n|---|---|---|\n| Turn-Around Time | $TAT_i = CT_i - AT_i$ | All 4 algorithms |\n| Waiting Time | $WT_i = TAT_i - BT_i$ | All 4 algorithms |\n| Waiting Time (alt) | $WT_i = CT_i - AT_i - BT_i$ | All 4 algorithms |\n| Average TAT | $ATT = \\frac{\\sum TAT_i}{n}$ | Final computation |\n| Average WT | $AWT = \\frac{\\sum WT_i}{n}$ | Final computation |\n| Response Time | $RT_i = \\text{First CPU Allocation}_i - AT_i$ | Round Robin only |\n| Throughput | $\\text{Throughput} = \\frac{n}{CT_{\\text{last}}}$ | Bonus metric |\n| CPU Utilization | $\\frac{\\text{Total Burst Time}}{CT_{\\text{last}}} \\times 100\\%$ | Bonus metric |\n\n> [!IMPORTANT]\n> **KTU Trick:** When $AT = 0$ for *all* processes, $WT = TAT - BT$ simplifies naturally. When $AT \\neq 0$, the **Gantt chart method** is the most reliable — plot execution, read off $CT$, then compute.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Master Problem (Used for ALL 4 Algorithms)\n\nConsider the following process set. This single problem will be solved using all four algorithms so you can directly compare results — **exactly the format KTU expects in your lab record.**\n\n| Process | Arrival Time (AT) | Burst Time (BT) | Priority |\n|---|---|---|---|\n| P1 | 0 | 4 | 2 |\n| P2 | 1 | 3 | 1 |\n| P3 | 2 | 1 | 3 |\n| P4 | 3 | 5 | 0 (highest) |\n\nHere, $n = 4$. Lower priority number = higher priority.\n\n---\n\n### 3.2 FCFS Solution (Exhaustive)\n\n**Order of execution** = Order of arrival: P1 → P2 → P3 → P4\n\n**Step 1: Build the Gantt Chart**\nGantt: | P1 (0–4) | P2 (4–7) | P3 (7–8) | P4 (8–13) |\n\n**Step 2: Read Completion Times (CT) from the Gantt Chart**\n\n$$\n\\begin{aligned}\nCT_{P1} &= 4 \\\\\nCT_{P2} &= 7 \\\\\nCT_{P3} &= 8 \\\\\nCT_{P4} &= 13\n\\end{aligned}\n$$\n\n**Step 3: Compute Turn-Around Time $TAT = CT - AT$**\n\n$$\n\\begin{aligned}\nTAT_{P1} &= 4 - 0 = 4 \\\\\nTAT_{P2} &= 7 - 1 = 6 \\\\\nTAT_{P3} &= 8 - 2 = 6 \\\\\nTAT_{P4} &= 13 - 3 = 10\n\\end{aligned}\n$$\n\n**Step 4: Compute Waiting Time $WT = TAT - BT$**\n\n$$\n\\begin{aligned}\nWT_{P1} &= 4 - 4 = 0 \\\\\nWT_{P2} &= 6 - 3 = 3 \\\\\nWT_{P3} &= 6 - 1 = 5 \\\\\nWT_{P4} &= 10 - 5 = 5\n\\end{aligned}\n$$\n\n**Step 5: Compute Averages**\n\n$$\n\\begin{aligned}\nATT &= \\frac{4 + 6 + 6 + 10}{4} = \\frac{26}{4} = 6.50 \\text{ ms} \\\\\nAWT &= \\frac{0 + 3 + 5 + 5}{4} = \\frac{13}{4} = 3.25 \\text{ ms}\n\\end{aligned}\n$$\n\n---\n\n### 3.3 Non-Preemptive SJF Solution (Exhaustive)\n\n**Logic:** At time 0, only P1 is available (4 ms). Run P1 till completion. After P1, the ready queue contains P2, P3, P4. The shortest burst is P3 (1 ms). Run P3, then P2 (3 ms), then P4 (5 ms).\n\n**Order of execution:** P1 → P3 → P2 → P4\n\n**Step 1: Gantt Chart**\n\n$$\n\\begin{aligned}\n\\text{Gantt: } & \\vert \\text{P1 } (0-4) \\vert \\text{P3 } (4-5) \\vert \\text{P2 } (5-8) \\vert \\text{P4 } (8-13) \\vert\n\\end{aligned}\n$$\n\n**Step 2: Read CT, then derive TAT and WT**\n\n$$\n\\begin{aligned}\nCT_{P1} &= 4 \\Rightarrow TAT_{P1} = 4 - 0 = 4 \\Rightarrow WT_{P1} = 4 - 4 = 0 \\\\\nCT_{P3} &= 5 \\Rightarrow TAT_{P3} = 5 - 2 = 3 \\Rightarrow WT_{P3} = 3 - 1 = 2 \\\\\nCT_{P2} &= 8 \\Rightarrow TAT_{P2} = 8 - 1 = 7 \\Rightarrow WT_{P2} = 7 - 3 = 4 \\\\\nCT_{P4} &= 13 \\Rightarrow TAT_{P4} = 13 - 3 = 10 \\Rightarrow WT_{P4} = 10 - 5 = 5\n\\end{aligned}\n$$\n\n**Step 3: Averages**\n\n$$\n\\begin{aligned}\nATT &= \\frac{4 + 3 + 7 + 10}{4} = \\frac{24}{4} = 6.00 \\text{ ms} \\\\\nAWT &= \\frac{0 + 2 + 4 + 5}{4} = \\frac{11}{4} = 2.75 \\text{ ms}\n\\end{aligned}\n$$\n\n> [!NOTE]\n> **Observation:** SJF gave a *lower AWT* (2.75 vs FCFS's 3.25). This **proves the optimality of SJF** for minimizing AWT — include this comparison table in your lab record!\n\n---\n\n### 3.4 Round Robin Solution (TQ = 2 ms)\n\n**Logic:** A ready queue is maintained. Each process gets up to 2 ms. P1 starts at $t=0$.\n\n**Simulation Trace:**\n\n- $t=0$: Queue = [P1, P2(1), P3(2), P4(3)]. Run P1 for 2 ms. P1 remaining = 2 ms.\n- $t=2$: P1 not done → re-queue. Queue = [P2, P3, P4, P1]. Run P2 for 2 ms. P2 remaining = 1 ms.\n- $t=4$: P2 not done → re-queue. Queue = [P3, P4, P1, P2]. Run P3 for 1 ms. P3 **finishes**.\n- $t=5$: Queue = [P4, P1, P2]. Run P4 for 2 ms. P4 remaining = 3 ms.\n- $t=7$: P4 not done → re-queue. Queue = [P1, P2, P4]. Run P1 for 2 ms. P1 **finishes**.\n- $t=9$: Queue = [P2, P4]. Run P2 for 1 ms. P2 **finishes**.\n- $t=10$: Queue = [P4]. Run P4 for 3 ms. P4 **finishes** at $t=13$.\n\n**Step 1: Gantt Chart**\n\n$$\n\\begin{aligned}\n\\text{Gantt: } & \\vert \\text{P1 }(0-2) \\vert \\text{P2 }(2-4) \\vert \\text{P3 }(4-5) \\vert \\text{P4 }(5-7) \\vert \\text{P1 }(7-9) \\vert \\text{P2 }(9-10) \\vert \\text{P4 }(10-13) \\vert\n\\end{aligned}\n$$\n\n**Step 2: Read CT and compute metrics**\n\n$$\n\\begin{aligned}\nCT_{P1} &= 9 \\Rightarrow TAT_{P1} = 9 - 0 = 9 \\Rightarrow WT_{P1} = 9 - 4 = 5 \\\\\nCT_{P2} &= 10 \\Rightarrow TAT_{P2} = 10 - 1 = 9 \\Rightarrow WT_{P2} = 9 - 3 = 6 \\\\\nCT_{P3} &= 5 \\Rightarrow TAT_{P3} = 5 - 2 = 3 \\Rightarrow WT_{P3} = 3 - 1 = 2 \\\\\nCT_{P4} &= 13 \\Rightarrow TAT_{P4} = 13 - 3 = 10 \\Rightarrow WT_{P4} = 10 - 5 = 5\n\\end{aligned}\n$$\n\n**Step 3: Averages**\n\n$$\n\\begin{aligned}\nATT &= \\frac{9 + 9 + 3 + 10}{4} = \\frac{31}{4} = 7.75 \\text{ ms} \\\\\nAWT &= \\frac{5 + 6 + 2 + 5}{4} = \\frac{18}{4} = 4.50 \\text{ ms}\n\\end{aligned}\n$$\n\n> [!IMPORTANT]\n> **KTU Trick:** RR has *worse AWT* than SJF, but *better Response Time*. If a KTU question asks \"which algorithm is best for a time-sharing system?\" — answer **Round Robin**, not SJF.\n\n---\n\n### 3.5 Non-Preemptive Priority Solution (Exhaustive)\n\n**Logic:** At $t=0$, only P1 (priority 2) is available. After P1 finishes at $t=4$, the ready queue has P2 (prio 1), P3 (prio 3), P4 (prio 0 = highest). Pick P4 first, then P2, then P3.\n\n**Order of execution:** P1 → P4 → P2 → P3\n\n**Step 1: Gantt Chart**\n\n$$\n\\begin{aligned}\n\\text{Gantt: } & \\vert \\text{P1 }(0-4) \\vert \\text{P4 }(4-9) \\vert \\text{P2 }(9-12) \\vert \\text{P3 }(12-13) \\vert\n\\end{aligned}\n$$\n\n**Step 2: Read CT and compute metrics**\n\n$$\n\\begin{aligned}\nCT_{P1} &= 4 \\Rightarrow TAT_{P1} = 4 - 0 = 4 \\Rightarrow WT_{P1} = 4 - 4 = 0 \\\\\nCT_{P4} &= 9 \\Rightarrow TAT_{P4} = 9 - 3 = 6 \\Rightarrow WT_{P4} = 6 - 5 = 1 \\\\\nCT_{P2} &= 12 \\Rightarrow TAT_{P2} = 12 - 1 = 11 \\Rightarrow WT_{P2} = 11 - 3 = 8 \\\\\nCT_{P3} &= 13 \\Rightarrow TAT_{P3} = 13 - 2 = 11 \\Rightarrow WT_{P3} = 11 - 1 = 10\n\\end{aligned}\n$$\n\n**Step 3: Averages**\n\n$$\n\\begin{aligned}\nATT &= \\frac{4 + 6 + 11 + 11}{4} = \\frac{32}{4} = 8.00 \\text{ ms} \\\\\nAWT &= \\frac{0 + 1 + 8 + 10}{4} = \\frac{19}{4} = 4.75 \\text{ ms}\n\\end{aligned}\n$$\n\n> [!WARNING]\n> Notice P3 has $WT = 10$ ms — the **highest waiting time** of all algorithms! This is **starvation** caused by lower-priority processes being continuously pre-empted by higher-priority arrivals. Use this as a *critical observation* in your lab record.\n\n---\n\n### 3.6 Master Comparison Table (Final Result Matrix)\n\n| Algorithm | ATT (ms) | AWT (ms) | Best For |\n|---|---|---|---|\n| **FCFS** | 6.50 | 3.25 | Batch systems |\n| **SJF** | 6.00 | 2.75 | Minimum AWT (theoretical optimum) |\n| **Round Robin (TQ=2)** | 7.75 | 4.50 | Time-sharing / fairness |\n| **Priority (Non-Preemptive)** | 8.00 | 4.75 | Real-time systems |\n\n$$\n\\begin{aligned}\n\\text{Best AWT} &= \\text{SJF: } 2.75 \\text{ ms} \\\\\n\\text{Worst AWT} &= \\text{Priority: } 4.75 \\text{ ms} \\\\\n\\text{Best ATT} &= \\text{SJF: } 6.00 \\text{ ms} \\\\\n\\text{Worst ATT} &= \\text{Priority: } 8.00 \\text{ ms}\n\\end{aligned}\n$$\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Scheduler Decision Flow (Block-Level Architecture)\n\n```mermaid\nflowchart TD\n    A[New Process Arrives] --> B{Ready Queue Empty?}\n    B -- Yes --> C[Allocate CPU Immediately]\n    B -- No --> D[Insert into Ready Queue in Order]\n    C --> E[Process Executes]\n    D --> F[Scheduler Selection Logic]\n    F --> G{Algorithm Type?}\n    G -- FCFS --> H[Pick Head of Queue]\n    G -- SJF --> I[Pick Process with Min Burst Time]\n    G -- Priority --> J[Pick Process with Highest Priority]\n    G -- Round Robin --> K[Pick Head of Queue with Time Quantum TQ]\n    H --> L[Run to Completion]\n    I --> M{Preemptive?}\n    M -- Yes --> N[Check for New Shorter Job]\n    M -- No --> L\n    J --> O{Preemptive?}\n    O -- Yes --> P[Check for Higher Priority Arrival]\n    O -- No --> L\n    K --> Q[Run for TQ Units]\n    Q --> R{Process Finished?}\n    R -- Yes --> S[Remove from System]\n    R -- No --> T[Re-queue at Tail]\n    L --> U[Compute TAT and WT]\n    S --> U\n    T --> U\n    N --> U\n    P --> U\n    U --> V[Output AWT and ATT]\n```\n\n### 4.2 Gantt Chart Comparison Block Diagram\n\n```mermaid\ngantt\n    title Process Scheduling Algorithm Comparison\n    dateFormat X\n    axisFormat %S\n    section FCFS\n    P1 :a1, 0, 4\n    P2 :a2, 4, 7\n    P3 :a3, 7, 8\n    P4 :a4, 8, 13\n    section SJF\n    P1 :b1, 0, 4\n    P3 :b2, 4, 5\n    P2 :b3, 5, 8\n    P4 :b4, 8, 13\n    section Priority\n    P1 :c1, 0, 4\n    P4 :c2, 4, 9\n    P2 :c3, 9, 12\n    P3 :c4, 12, 13\n    section Round Robin TQ 2\n    P1 :d1, 0, 2\n    P2 :d2, 2, 4\n    P3 :d3, 4, 5\n    P4 :d4, 5, 7\n    P1 :d5, 7, 9\n    P2 :d6, 9, 10\n    P4 :d7, 10, 13\n```\n\n### 4.3 Process State Transition (Scheduling Context)\n\n```mermaid\nstateDiagram-v2\n    [*] --> New\n    New --> Ready: Admitted\n    Ready --> Running: Scheduler Dispatch\n    Running --> Ready: Preempted by RR or SJF or Priority\n    Running --> Waiting: I O or Event Wait\n    Waiting --> Ready: I O Completion or Event\n    Running --> Terminated: Exit\n    Terminated --> [*]\n```\n\n> [!NOTE]\n> **Interpretation:** A process moves between Ready and Running *multiple times* in **Round Robin**, **SJF (preemptive)**, and **Priority (preemptive)**. In **FCFS** and **non-preemptive SJF/Priority**, a process goes from Ready → Running → Terminated *exactly once*.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n### 5.1 Part A — Short Answer Questions (3 Marks each)\n\n**Q1. [KTU University Exam – Dec 2023]**\n*Define Turn-Around Time and Waiting Time. Establish the relationship between them for a process in a CPU scheduling algorithm.*\n\n**Model Answer (3 marks):**\n- **Turn-Around Time (TAT):** Total time a process spends in the system, from arrival to completion. $TAT = CT - AT$ *(1 mark)*.\n- **Waiting Time (WT):** Total time a process waits in the ready queue before getting the CPU. *(1 mark)*.\n- **Relationship:** $WT = TAT - BT$ because $TAT$ includes both the *waiting* time and the *burst* (actual CPU) time. Therefore, $WT = TAT - BT$. *(1 mark)*.\n\n---\n\n**Q2. [KTU University Exam – May 2024]**\n*Differentiate between Preemptive and Non-Preemptive scheduling. State one example algorithm for each.*\n\n**Model Answer (3 marks):**\n- **Preemptive Scheduling:** The OS can forcibly remove the CPU from a running process. Example: Round Robin, Preemptive SJF (SRTF). *(1.5 marks)*.\n- **Non-Preemptive Scheduling:** Once a process starts executing, it runs to completion. Example: FCFS, Non-Preemptive SJF. *(1.5 marks)*.\n\n---\n\n### 5.2 Part B — Long Answer Questions (14 Marks each)\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning (Common Pitfall):**\n> - **MISTAKE #1:** Students confuse *arrival time* with *starting time* in the Gantt chart. Always mark $AT$ on the *first entry* of a process, and compute $TAT$ from $CT$ (last exit) to $AT$.\n> - **MISTAKE #2:** In Round Robin, students forget to **re-queue** the unfinished process at the *tail*, not the head. A process arriving later can also be inserted before a re-queued process.\n> - **MISTAKE #3:** Average is computed over **n = total number of processes**, not the number of *finished* or *scheduled* processes. Watch for typos in division.\n> - **MISTAKE #4:** For Priority, conventions differ — *state* explicitly whether \"lower number = higher priority\" or \"higher number = higher priority\".\n\n---\n\n#### **Question A (14 Marks) — Internal Choice Option 1**\n\n**[KTU University Exam – Model Question, Module 2]**\nThe following processes are to be scheduled. Compute the **Average Waiting Time (AWT)** and **Average Turn-Around Time (ATT)** using:\n**(a)** FCFS scheduling (7 marks)\n**(b)** Non-Preemptive SJF scheduling (7 marks)\n\n| Process | Arrival Time | Burst Time |\n|---|---|---|\n| P1 | 0 | 5 |\n| P2 | 2 | 3 |\n| P3 | 4 | 1 |\n| P4 | 5 | 2 |\n\n**Solution:**\n\n**(a) FCFS (7 marks):**\nOrder = P1 → P2 → P3 → P4\n\nGantt: | P1 (0–5) | P2 (5–8) | P3 (8–9) | P4 (9–11) |\n\n$$\n\\begin{aligned}\nTAT_{P1} &= 5-0 = 5, \\quad WT_{P1} = 5-5 = 0 \\\\\nTAT_{P2} &= 8-2 = 6, \\quad WT_{P2} = 6-3 = 3 \\\\\nTAT_{P3} &= 9-4 = 5, \\quad WT_{P3} = 5-1 = 4 \\\\\nTAT_{P4} &= 11-5 = 6, \\quad WT_{P4} = 6-2 = 4\n\\end{aligned}\n$$\n\n$$\nATT = \\frac{5+6+5+6}{4} = 5.50 \\text{ ms}, \\quad AWT = \\frac{0+3+4+4}{4} = 2.75 \\text{ ms}\n$$\n\n**(b) Non-Preemptive SJF (7 marks):**\n- $t=0$: Only P1 available → run P1 (0–5).\n- $t=5$: Ready = {P2(3), P3(1), P4(2)}. Pick P3 (shortest, 1 ms) → run (5–6).\n- $t=6$: Ready = {P2(3), P4(2)}. Pick P4 (2 ms) → run (6–8).\n- $t=8$: Run P2 (8–11).\n\nGantt: | P1 (0–5) | P3 (5–6) | P4 (6–8) | P2 (8–11) |\n\n$$\n\\begin{aligned}\nTAT_{P1} &= 5-0 = 5, \\quad WT_{P1} = 5-5 = 0 \\\\\nTAT_{P3} &= 6-4 = 2, \\quad WT_{P3} = 2-1 = 1 \\\\\nTAT_{P4} &= 8-5 = 3, \\quad WT_{P4} = 3-2 = 1 \\\\\nTAT_{P2} &= 11-2 = 9, \\quad WT_{P2} = 9-3 = 6\n\\end{aligned}\n$$\n\n$$\nATT = \\frac{5+2+3+9}{4} = 4.75 \\text{ ms}, \\quad AWT = \\frac{0+1+1+6}{4} = 2.00 \\text{ ms}\n$$\n\n---\n\n#### **Question B (14 Marks) — Internal Choice Option 2**\n\n**[KTU University Exam – Model Question, Module 2]**\nFor the same process table above, compute **AWT** and **ATT** using:\n**(a)** Round Robin scheduling with Time Quantum = 2 ms (7 marks)\n**(b)** Non-Preemptive Priority scheduling (smaller number = higher priority) (7 marks)\n\n| Process | Arrival Time | Burst Time | Priority |\n|---|---|---|---|\n| P1 | 0 | 5 | 3 |\n| P2 | 2 | 3 | 1 |\n| P3 | 4 | 1 | 4 |\n| P4 | 5 | 2 | 2 |\n\n**Solution:**\n\n**(a) Round Robin, TQ = 2 ms (7 marks):**\n- $t=0$: Queue = [P1, P2@2, P3@4, P4@5]. Run P1 (0–2). P1 remaining = 3.\n- $t=2$: P1 re-queue. Queue = [P2, P3, P4, P1]. Run P2 (2–4). P2 remaining = 1.\n- $t=4$: P2 re-queue. Queue = [P3, P4, P1, P2]. Run P3 (4–5). P3 **finishes**.\n- $t=5$: Queue = [P4, P1, P2]. Run P4 (5–7). P4 **finishes**.\n- $t=7$: Queue = [P1, P2]. Run P1 (7–9). P1 remaining = 1.\n- $t=9$: Queue = [P2, P1]. Run P2 (9–10). P2 **finishes**.\n- $t=10$: Queue = [P1]. Run P1 (10–11). P1 **finishes**.\n\nGantt: | P1 (0–2) | P2 (2–4) | P3 (4–5) | P4 (5–7) | P1 (7–9) | P2 (9–10) | P1 (10–11) |\n\n$$\n\\begin{aligned}\nCT_{P1} = 11, & \\quad TAT_{P1} = 11-0 = 11, \\quad WT_{P1} = 11-5 = 6 \\\\\nCT_{P2} = 10, & \\quad TAT_{P2} = 10-2 = 8, \\quad WT_{P2} = 8-3 = 5 \\\\\nCT_{P3} = 5, & \\quad TAT_{P3} = 5-4 = 1, \\quad WT_{P3} = 1-1 = 0 \\\\\nCT_{P4} = 7, & \\quad TAT_{P4} = 7-5 = 2, \\quad WT_{P4} = 2-2 = 0\n\\end{aligned}\n$$\n\n$$\nATT = \\frac{11+8+1+2}{4} = 5.50 \\text{ ms}, \\quad AWT = \\frac{6+5+0+0}{4} = 2.75 \\text{ ms}\n$$\n\n**(b) Non-Preemptive Priority (7 marks):**\n- $t=0$: Only P1 (prio 3) available → run P1 (0–5).\n- $t=5$: Ready = {P2(prio 1), P3(prio 4), P4(prio 2)}. Pick P2 (highest, prio 1) → run (5–8).\n- $t=8$: Ready = {P3(prio 4), P4(prio 2)}. Pick P4 (prio 2) → run (8–10).\n- $t=10$: Run P3 (10–11).\n\nGantt: | P1 (0–5) | P2 (5–8) | P4 (8–10) | P3 (10–11) |\n\n$$\n\\begin{aligned}\nTAT_{P1} &= 5-0 = 5, \\quad WT_{P1} = 0 \\\\\nTAT_{P2} &= 8-2 = 6, \\quad WT_{P2} = 6-3 = 3 \\\\\nTAT_{P4} &= 10-5 = 5, \\quad WT_{P4} = 5-2 = 3 \\\\\nTAT_{P3} &= 11-4 = 7, \\quad WT_{P3} = 7-1 = 6\n\\end{aligned}\n$$\n\n$$\nATT = \\frac{5+6+5+7}{4} = 5.75 \\text{ ms}, \\quad AWT = \\frac{0+3+3+6}{4} = 3.00 \\text{ ms}\n$$\n\n---\n\n### 5.3 Topic Recap & Important Things to Remember (Rapid Revision Checklist)\n\n> [!IMPORTANT]\n> **High-Density Summary — Read this 5 minutes before the exam:**\n\n- **Gantt chart is mandatory** for every scheduling problem — it is the *only* unambiguous way to determine CT.\n- $TAT = CT - AT$ *(always, no exceptions)*.\n- $WT = TAT - BT$ *(equivalently, $WT = CT - AT - BT$)*.\n- **Averages** are always computed over **n**, the total number of processes.\n- **FCFS** = FIFO, non-preemptive, suffers from **convoy effect**, baseline algorithm.\n- **SJF (Non-Preemptive)** = minimum AWT (optimal theorem), suffers **starvation**.\n- **SJF (Preemptive) = SRTF** = minimum AWT among *all* algorithms, more responsive.\n- **Round Robin** = TQ-based, fair, good response time, but higher TAT than SJF. **TQ choice matters.**\n- **Priority** = risk of starvation, solved by **Aging**.\n- **Preemption** = OS forcibly takes CPU; only happens in RR, SRTF, Preemptive Priority.\n- **Non-Preemption** = process runs to completion; FCFS, Non-Preemptive SJF/Priority.\n- For **same priority values** in priority scheduling, fall back to **FCFS** order (tie-breaker).\n- In **RR**, a new arrival is inserted at the *tail* but *before* any previously re-queued unfinished process (it joined the queue earlier).\n- For **TQ ≥ max(BT)**, RR behaves identically to **FCFS** — a great exam sanity check.\n- **Observation format for lab record:** Always include a final **comparison table** showing ATT and AWT for all 4 algorithms and *one line of analysis* per row.\n- **Sample C program structure** (mention in lab record): use a `struct Process { int pid, at, bt, ct, tat, wt, priority; }` and a sort function based on the chosen criterion.\n- **Real-world mapping:** Linux CFS (≈SJF), Windows NT (RR with priorities), RTOS (strict priority), Mainframe (FCFS batch).\n- **Bonus marks tip:** Mention **throughput** and **CPU utilization** as additional metrics beyond AWT/ATT — it shows depth.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "information-technology-5-pcitl408-mExperiment 2-t4",
          "title": "Inter-process communication using pipes, message queues and shared memory",
          "content": "<!-- SECTION_1_START -->\n\n# Module Experiment 2: Inter-Process Communication (IPC)\n\n## 1. Core Technical Definition & Intuitive Overview\n\n> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCITL408 - OPERATING SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Inter-process communication using pipes, message queues and shared memory\n\n### 1.1 Formal Academic Definition\n\n**Inter-Process Communication (IPC)** is a set of programming interfaces and mechanisms provided by the operating system kernel that allow cooperating processes (independent or related) to exchange data, synchronize execution, and share resources. In the KTU 2024 scheme, IPC is classified into three primary categories for laboratory implementation:\n\n1. **Pipes (Unnamed & Named/FIFO)** – Byte-stream based, half/full duplex communication channels.\n2. **Message Queues (System V / POSIX)** – Discrete, typed, structured-message based communication.\n3. **Shared Memory** – Memory-mapped region accessible by multiple processes for high-speed data exchange.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Mandate:** Students must implement all three mechanisms using standard POSIX/System V C system calls in a Linux (Ubuntu/Fedora) environment, compile using `gcc`, and demonstrate parent-child / unrelated process communication.\n\n### 1.2 Conceptual Analogy / Intuition\n\n> [!NOTE]\n> **Analogy — The Office Whiteboard, Letter Tray, and Shared Notebook:**\n> - **Pipes** → Imagine a hollow tube connecting two office cabins. A worker in Cab A drops a folded letter into one end, and the worker in Cab B picks it up from the other end. Only bytes flow, in order, and only between connected processes.\n> - **Message Queues** → Imagine a letter tray at the reception desk. Each letter has a type label (e.g., \"Urgent\", \"Billing\"). Any process can post a letter, and any process can pick specific letters based on the type label. The tray is managed by the receptionist (the OS kernel).\n> - **Shared Memory** → Imagine a giant whiteboard hung on the office wall. Any process can walk up, read what's on it, erase, or write. It's the fastest method, but you need a referee (semaphores/mutex) to prevent two people writing at once and smearing the ink.\n\n### 1.3 Standard Metrics & Constants\n\nThe following POSIX/System V constants are used throughout the lab:\n\n| Constant | Header File | Purpose |\n| :--- | :--- | :--- |\n| `O_CREAT` | `<fcntl.h>` | Create file/IPC object if it does not exist |\n| `O_RDONLY`, `O_WRONLY` | `<fcntl.h>` | Read/Write access flags |\n| `IPC_PRIVATE` | `<sys/ipc.h>` | Private key for parent-child IPC |\n| `0666` | (octal) | Read + Write permission for owner, group, others |\n| `PIPE_BUF` | `<limits.h>` | Atomic write size threshold (**4096 bytes** on Linux) |\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Mechanism 1: Pipes (Unnamed Pipes)\n\nAn **unnamed pipe** is a kernel-managed, in-memory, half-duplex (one-way) byte stream buffer identified by **two file descriptors** returned by the `pipe()` system call.\n\n**Why use it?**\n- Zero disk I/O — purely in RAM.\n- Built into every UNIX-like system.\n- Ideal for parent-child communication via `fork()`.\n\n**Operational Steps (The 'How'):**\n1. The kernel allocates a temporary in-RAM circular buffer.\n2. Two file descriptors are returned: `fd[0]` (read end) and `fd[1]` (write end).\n3. After `fork()`, both parent and child inherit copies of these descriptors.\n4. The writer writes bytes; the reader reads bytes. Data flows strictly in **FIFO order**.\n5. A `read()` on an empty pipe **blocks** the calling process until a writer provides data or all write ends close (returns 0 / EOF).\n\n**The KTU Formula Sheet / Cheat Sheet (Pipes)**\n\n| System Call | Header | Return Type | Purpose |\n| :--- | :--- | :--- | :--- |\n| `int pipe(int fd[2])` | `<unistd.h>` | `0` on success, `-1` on error | Creates unnamed pipe, fills `fd[0]`, `fd[1]` |\n| `int read(int fd, void *buf, size_t n)` | `<unistd.h>` | Bytes read, `0` = EOF, `-1` = error | Reads up to `n` bytes from pipe |\n| `int write(int fd, const void *buf, size_t n)` | `<unistd.h>` | Bytes written, `-1` = error | Writes `n` bytes to pipe |\n| `int close(int fd)` | `<unistd.h>` | `0` / `-1` | Closes a file descriptor |\n\n**Key Engineering Insight:**\n> [!NOTE]\n> Pipes are **stream-oriented** — they have no message boundaries. If a process writes 100 bytes in one `write()` and another writes 50 bytes, the reader may receive all 150 bytes in a single `read()` of size 200. This is a critical interview and exam point.\n\n### 2.2 Mechanism 2: Named Pipes (FIFO)\n\nWhen communication is required between **unrelated processes** (e.g., two separate terminal sessions), unnamed pipes are insufficient because they require a parent-child relationship. A **FIFO (First In, First Out)** — also called a named pipe — is used.\n\n**The 'How':**\n1. `mkfifo()` creates a special file in the filesystem (visible via `ls -l` with a leading `p`).\n2. Unrelated processes open this file using standard `open()`, `read()`, `write()`.\n3. The file disappears with `unlink()`.\n\n**Cheat Sheet (FIFO)**\n\n| System Call | Header | Purpose |\n| :--- | :--- | :--- |\n| `int mkfifo(const char *pathname, mode_t mode)` | `<sys/types.h>`, `<sys/stat.h>` | Creates FIFO file with permissions (e.g., `0666`) |\n| `int unlink(const char *pathname)` | `<unistd.h>` | Removes the FIFO file |\n| `open(path, O_RDONLY)` | `<fcntl.h>` | Blocks until a writer opens the FIFO |\n\n### 2.3 Mechanism 3: Message Queues (System V)\n\nA **message queue** is a kernel-managed, linked-list of discrete messages. Unlike pipes, each message has a **type** (long integer) and a **size**, allowing selective retrieval.\n\n**The 'How':**\n1. The kernel maintains a queue identified by a unique **key** (an `int`).\n2. `ftok()` generates a key from a filename and a project ID.\n3. `msgget()` creates or opens the queue.\n4. `msgsnd()` appends a message; `msgrcv()` fetches a specific type.\n5. `msgctl()` performs control operations (e.g., `IPC_RMID` to delete).\n\n**Cheat Sheet (Message Queues)**\n\n| System Call | Header | Purpose |\n| :--- | :--- | :--- |\n| `key_t ftok(const char *path, int id)` | `<sys/ipc.h>`, `<sys/types.h>` | Generates a unique key |\n| `int msgget(key_t key, int msgflg)` | `<sys/msg.h>` | Creates/opens queue; returns queue ID |\n| `int msgsnd(int msqid, const void *msgp, size_t msgsz, int msgflg)` | `<sys/msg.h>` | Sends a message |\n| `ssize_t msgrcv(int msqid, void *msgp, size_t msgsz, long msgtyp, int msgflg)` | `<sys/msg.h>` | Receives a message (blocking by default) |\n| `int msgctl(int msqid, int cmd, struct msqid_ds *buf)` | `<sys/msg.h>` | Control (e.g., `IPC_RMID` to remove) |\n\n**Message Structure (Mandatory for Code):**\n\n```c\nstruct msg_buffer {\n    long msg_type;       // Must be > 0. Used for selective receive.\n    char msg_text[100];  // User-defined payload\n};\n```\n\n> [!IMPORTANT]\n> **KTU Trap:** The `msg_type` field **MUST** be a `long` and **MUST be the very first field** in the structure. The kernel uses it to identify the message boundary.\n\n### 2.4 Mechanism 4: Shared Memory\n\n**Shared memory** is the **fastest** IPC mechanism because it involves no kernel-mediated data copying. A region of RAM is mapped into the address space of multiple cooperating processes.\n\n**The 'How':**\n1. The OS kernel reserves a physical memory page.\n2. `shmget()` allocates the segment using a key.\n3. `shmat()` (shared memory **attach**) maps the segment into the calling process's virtual address space, returning a pointer.\n4. The process uses this pointer for direct memory reads/writes.\n5. `shmdt()` (shared memory **detach**) unmaps the segment.\n6. `shmctl()` with `IPC_RMID` marks the segment for deletion.\n\n**Cheat Sheet (Shared Memory)**\n\n| System Call | Header | Purpose |\n| :--- | :--- | :--- |\n| `int shmget(key_t key, size_t size, int shmflg)` | `<sys/shm.h>` | Creates/allocates shared memory segment |\n| `void *shmat(int shmid, const void *shmaddr, int shmflg)` | `<sys/shm.h>` | Attaches segment to process address space |\n| `int shmdt(const void *shmaddr)` | `<sys/shm.h>` | Detaches segment |\n| `int shmctl(int shmid, int cmd, struct shmid_ds *buf)` | `<sys/shm.h>` | Control (e.g., `IPC_RMID` to remove) |\n\n> [!WARNING]\n> **Critical Synchronization Issue:** Shared memory is **not synchronized by default**. Two processes writing simultaneously can corrupt data. In lab experiments, it is **mandatory** to combine shared memory with **semaphores** (`sem.h`) or `wait()/signal()` to implement mutual exclusion.\n\n### 2.5 Comparative Engineering Utility Table\n\n| Feature | Pipe (Unnamed) | FIFO (Named) | Message Queue | Shared Memory |\n| :--- | :--- | :--- | :--- | :--- |\n| **Relationship** | Parent-Child only | Unrelated processes | Unrelated processes | Unrelated processes |\n| **Boundary** | Stream (no boundary) | Stream (no boundary) | Discrete messages | Byte-level |\n| **Speed** | Fast | Fast | Medium | **Fastest** |\n| **Synchronization** | Built-in (blocking) | Built-in (blocking) | Built-in (blocking) | **Not built-in** |\n| **Persistence** | Process-lifetime | Filesystem | Kernel-reboot | Kernel-reboot |\n| **Typical Use** | Shell pipelines | Client-server logs | OS mailboxes | DB engines, MMOs |\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n## 3. Step-by-Step Implementations & Analytical Matrix\n\n> [!IMPORTANT]\n> **Academic Note for Lab Records:** In the KTU lab examination, students are expected to write **complete, compilable C programs** with proper headers, error checking (using `perror()`), and a clear demonstration of `fork()` / unrelated process interaction. Below are the reference implementations with explicit step-by-step logic.\n\n### 3.1 Experiment 2.1: Unnamed Pipe — Parent-Child Communication\n\n**Aim:** Write a C program to create an unnamed pipe. The parent process writes a string; the child reads and prints it.\n\n**Algorithm Steps (Logic Flow):**\n1. Include headers: `<stdio.h>`, `<unistd.h>`, `<string.h>`.\n2. Declare `int fd[2]` and `char buffer[100]`.\n3. Call `pipe(fd)`. If returns `-1`, abort.\n4. Call `pid_t pid = fork()`.\n5. If `pid == 0` (child branch): close the write end `close(fd[1])`. Block on `read(fd[0], buffer, sizeof(buffer))`. Print buffer. Close `fd[0]`.\n6. If `pid > 0` (parent branch): close the read end `close(fd[0])`. Write message: `write(fd[1], msg, strlen(msg)+1)`. Close `fd[1]`.\n7. Parent waits for child: `wait(NULL)`.\n\n**Reference Implementation:**\n\n```c\n#include <stdio.h>\n#include <unistd.h>\n#include <string.h>\n#include <sys/wait.h>\n\nint main() {\n    int fd[2];\n    char buffer[100];\n    const char *message = \"Hello from Parent via Pipe!\";\n\n    // Step 1: Create the pipe\n    if (pipe(fd) == -1) {\n        perror(\"pipe failed\");\n        return 1;\n    }\n\n    // Step 2: Fork the process\n    pid_t pid = fork();\n\n    if (pid < 0) {\n        perror(\"fork failed\");\n        return 1;\n    } else if (pid == 0) {\n        // CHILD PROCESS: Reader\n        close(fd[1]); // Close unused write end\n        read(fd[0], buffer, sizeof(buffer));\n        printf(\"Child received: %s\\n\", buffer);\n        close(fd[0]);\n    } else {\n        // PARENT PROCESS: Writer\n        close(fd[0]); // Close unused read end\n        write(fd[1], message, strlen(message) + 1);\n        printf(\"Parent sent message.\\n\");\n        close(fd[1]);\n        wait(NULL);\n    }\n    return 0;\n}\n```\n\n**Compilation:** `gcc pipe_demo.c -o pipe_demo && ./pipe_demo`\n\n### 3.2 Experiment 2.2: Named Pipe (FIFO) — Unrelated Processes\n\n**Aim:** Create a FIFO. One process writes; another (run in a separate terminal) reads.\n\n**Algorithm Steps:**\n1. Use `mkfifo(\"/tmp/myfifo\", 0666)` to create the FIFO.\n2. **Writer process:** `open()` the FIFO with `O_WRONLY`, `write()` a string, `close()`.\n3. **Reader process:** `open()` with `O_RDONLY`, `read()` into buffer, print, `close()`.\n4. Both processes `unlink(\"/tmp/myfifo\")` after use (or use `rm`).\n\n**Terminal Commands for Demo:**\n\n```bash\n# Terminal 1 (Reader) - blocks until writer connects:\ncat /tmp/myfifo\n\n# Terminal 2 (Writer):\necho \"Greetings via FIFO\" > /tmp/myfifo\n```\n\n**Reference C Implementation (Reader):**\n\n```c\n#include <stdio.h>\n#include <fcntl.h>\n#include <unistd.h>\n#include <sys/stat.h>\n\nint main() {\n    char buffer[100];\n    // Create FIFO if it doesn't exist\n    mkfifo(\"/tmp/myfifo\", 0666);\n\n    int fd = open(\"/tmp/myfifo\", O_RDONLY);\n    read(fd, buffer, sizeof(buffer));\n    printf(\"Reader got: %s\\n\", buffer);\n    close(fd);\n    unlink(\"/tmp/myfifo\");\n    return 0;\n}\n```\n\n### 3.3 Experiment 2.3: Message Queue — Typed Message Exchange\n\n**Aim:** Two-way communication between parent and child using System V message queues with selective `msg_type` retrieval.\n\n**Algorithm Steps:**\n1. Define `struct msg_buffer { long msg_type; char msg_text[100]; };`\n2. Generate key: `key_t key = ftok(\"progfile\", 65);`\n3. Create queue: `int msgid = msgget(key, 0666 | IPC_CREAT);`\n4. Parent sets `msg_type = 1`, sends via `msgsnd`.\n5. Child sets `msg_type = 2`, sends reply.\n6. Child receives parent's message with `msgrcv(..., 1, 0)`.\n7. Parent receives child's reply with `msgrcv(..., 2, 0)`.\n8. Both call `msgctl(msgid, IPC_RMID, NULL)` to destroy the queue.\n\n**Reference Implementation (Sender Side):**\n\n```c\n#include <stdio.h>\n#include <sys/ipc.h>\n#include <sys/msg.h>\n\nstruct msg_buffer {\n    long msg_type;\n    char msg_text[100];\n};\n\nint main() {\n    key_t key = ftok(\"msgfile\", 65);\n    int msgid = msgget(key, 0666 | IPC_CREAT);\n    struct msg_buffer message;\n\n    message.msg_type = 1; // Type 1 = Parent -> Child\n    printf(\"Enter message: \");\n    fgets(message.msg_text, 100, stdin);\n    msgsnd(msgid, &message, sizeof(message.msg_text), 0);\n\n    msgrcv(msgid, &message, sizeof(message.msg_text), 2, 0); // Wait for Type 2\n    printf(\"Reply received: %s\\n\", message.msg_text);\n\n    msgctl(msgid, IPC_RMID, NULL);\n    return 0;\n}\n```\n\n### 3.4 Experiment 2.4: Shared Memory — Cooperative Writer/Reader\n\n**Aim:** Two processes share a memory segment. Writer writes a string; reader reads and prints it. **Combined with semaphores for synchronization.**\n\n**Algorithm Steps:**\n1. Include `<sys/shm.h>`, `<sys/sem.h>`, `<sys/ipc.h>`.\n2. Generate key: `key_t key = ftok(\"shmfile\", 75);`\n3. Create segment: `int shmid = shmget(key, 1024, 0666 | IPC_CREAT);`\n4. Attach: `char *str = (char*) shmat(shmid, NULL, 0);`\n5. Writer: write data via `strcpy(str, \"...\")`, then call `semop()` to signal \"done\".\n6. Reader: `semop()` to wait \"ready\", then `printf(\"%s\", str)`.\n7. Both: `shmdt(str)` and `shmctl(shmid, IPC_RMID, NULL)`.\n\n**Reference Implementation (Writer):**\n\n```c\n#include <stdio.h>\n#include <sys/ipc.h>\n#include <sys/shm.h>\n\nint main() {\n    key_t key = ftok(\"shmfile\", 75);\n    int shmid = shmget(key, 1024, 0666 | IPC_CREAT);\n    char *str = (char *) shmat(shmid, NULL, 0);\n\n    printf(\"Write Data: \");\n    fgets(str, 1024, stdin);\n    printf(\"Data written in memory: %s\\n\", str);\n\n    shmdt(str);\n    return 0;\n}\n```\n\n**Reference Implementation (Reader — Run in Separate Terminal):**\n\n```c\n#include <stdio.h>\n#include <sys/ipc.h>\n#include <sys/shm.h>\n\nint main() {\n    key_t key = ftok(\"shmfile\", 75);\n    int shmid = shmget(key, 1024, 0666 | IPC_CREAT);\n    char *str = (char *) shmat(shmid, NULL, 0);\n\n    printf(\"Data read from memory: %s\\n\", str);\n\n    shmdt(str);\n    shmctl(shmid, IPC_RMID, NULL); // Destroy segment\n    return 0;\n}\n```\n\n> [!WARNING]\n> **Evaluator's Pitfall — Missing `ftok()` File:** The `ftok()` function requires a real, existing file path (e.g., `\"shmfile\"`) as the first argument. Run `touch shmfile` in the working directory **before** compiling, or `ftok()` will return `-1`.\n\n### 3.5 Analytical Matrix — System Call Return Values\n\n| System Call | Success | Failure | Notes |\n| :--- | :--- | :--- | :--- |\n| `pipe(fd)` | `0` | `-1` | `fd[0]` = read, `fd[1]` = write |\n| `fork()` | Child: `0`; Parent: child's PID | `-1` | Process duplicated |\n| `msgget()` | Message queue ID | `-1` | Use `perror(\"msgget\")` for diagnostics |\n| `shmat()` | Pointer to segment | `(void *) -1` | Cast to `char*` for string ops |\n| `shmdt()` | `0` | `-1` | Does not destroy, only detaches |\n| `msgctl(..., IPC_RMID, ...)` | `0` | `-1` | Removes the IPC object permanently |\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Diagram: Unnamed Pipe Data Flow (Parent-Child)\n\nThe following Mermaid block illustrates the kernel-mediated byte stream between parent and child after `fork()`.\n\n```mermaid\nflowchart LR\n    parentA[\"Parent Process\"] -- \"write to fd 1\" --> kernelBuf[\"Kernel Pipe Buffer in RAM\"]\n    kernelBuf -- \"read from fd 0\" --> childB[\"Child Process\"]\n    parentA -. \"fork duplicates fd table\" .-> childB\n```\n\n**Explanation:**\n- After `pipe(fd)`, the kernel creates a hidden buffer plus two endpoints.\n- `fork()` duplicates the file descriptor table; both processes now hold `fd[0]` and `fd[1]`.\n- Convention: parent closes `fd[0]` (it only writes), child closes `fd[1]` (it only reads). This prevents EOF ambiguity.\n\n### 4.2 Diagram: Message Queue Selective Retrieval\n\n```mermaid\nflowchart TD\n    senderP[\"Sender Process\"] -- \"msgsnd type 1 order 1\" --> queue1[\"Kernel Message Queue\"]\n    senderC[\"Client B\"] -- \"msgsnd type 2 order 2\" --> queue1\n    queue1 -- \"msgrcv type equals 2\" --> receiver[\"Receiver Process\"]\n    queue1 -- \"msgrcv type equals 1\" --> altReceiver[\"Alt Receiver\"]\n```\n\n**Explanation:**\n- Messages are stored with their `msg_type` as a tag.\n- The receiver specifies the type in `msgrcv` to selectively dequeue.\n- Setting `msgtyp = 0` receives the **first** message in the queue (FIFO order).\n\n### 4.3 Diagram: Shared Memory Architecture\n\n```mermaid\nflowchart LR\n    procA[\"Process A Virtual Address Space\"] -- \"shmat returns pointer PA\" --> physMem[\"Shared Physical Page in RAM\"]\n    procB[\"Process B Virtual Address Space\"] -- \"shmat returns pointer PB\" --> physMem\n    physMem -- \"byte level read or write\" --> sharedText[\"Common data structure\"]\n```\n\n**Explanation:**\n- Both processes map the **same physical page** into their own virtual address space.\n- Writes from Process A at offset `PA + 4` are instantly visible to Process B at offset `PB + 4`.\n- **No kernel intervention** during read/write — hence the speed.\n\n### 4.4 Diagram: Block-Level Functional Architecture of an IPC Subsystem\n\n```mermaid\nflowchart TB\n    subgraph userSpace[\"User Space\"]\n        appA[\"Application A\"]\n        appB[\"Application B\"]\n    end\n    subgraph kernelSpace[\"Kernel Space\"]\n        pipeLayer[\"Pipe Subsystem VFS Inode Interface\"]\n        mqLayer[\"Message Queue Subsystem IPC ID Tables\"]\n        shmLayer[\"Shared Memory Subsystem Shmem File Operations\"]\n    end\n    physLayer[\"Physical RAM Pages and Kernel Buffers\"]\n    appA -- \"pipe read write\" --> pipeLayer\n    appB -- \"msgget msgsnd msgrcv\" --> mqLayer\n    appA -- \"shmat shmdt\" --> shmLayer\n    appB -- \"shmat shmdt\" --> shmLayer\n    pipeLayer --> physLayer\n    mqLayer --> physLayer\n    shmLayer --> physLayer\n```\n\n**Explanation:**\n- All three mechanisms operate through kernel abstractions.\n- Pipes tie into the VFS (Virtual File System) for unified `read/write` semantics.\n- Message queues and shared memory use dedicated IPC identifier tables in the kernel.\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### Part A Questions (3 Marks Each)\n\n**Q1. [KTU University Exam - Model Paper 2024 Set A]**\n**Define Inter-Process Communication. List any three IPC mechanisms available in UNIX/Linux.**\n\n**Model Answer (3 marks):**\nInter-Process Communication (IPC) refers to the mechanisms provided by the operating system that allow processes to exchange data and synchronize their execution. Three IPC mechanisms are:\n1. **Pipes** (Unnamed and Named/FIFO) — for byte-stream communication.\n2. **Message Queues** — for discrete, typed message exchange.\n3. **Shared Memory** — for high-speed memory-mapped data sharing.\n\n*(Award 1 mark for definition, 1 mark for listing, 1 mark for brief one-line description of each.)*\n\n---\n\n**Q2. [KTU University Exam - Model Paper 2024 Set B]**\n**Differentiate between a pipe and a message queue based on message boundary preservation.**\n\n**Model Answer (3 marks):**\n- A **pipe** is a stream-oriented IPC mechanism. It does **not preserve message boundaries** — multiple `write()` calls may be coalesced into a single `read()`. (1.5 marks)\n- A **message queue** is a message-oriented IPC mechanism. Each message has a defined size and type, and `msgrcv()` retrieves one message at a time, **preserving boundaries** completely. (1.5 marks)\n\n---\n\n### Part B Questions (14 Marks — Internal Choice)\n\n#### **Question A (14 Marks) — Pipes & FIFOs Focus**\n\n**[KTU University Exam - Dec 2024, Module Internal, Model]**\n**(a)** Explain the `pipe()` and `fork()` system calls with a neat diagram. Write a C program to demonstrate communication between a parent and child process using an unnamed pipe, where the parent writes \"Hello Child\" and the child prints it. **(7 marks)**\n\n**(b)** What is a FIFO? How is it different from an unnamed pipe? Write the system calls used to create, open, and remove a FIFO. **(7 marks)**\n\n**Model Solution for (a):**\n- `pipe(int fd[2])` creates a one-way kernel buffer; `fd[0]` is the read end, `fd[1]` is the write end. (1 mark)\n- `fork()` duplicates the current process. The child gets `pid == 0`; the parent gets the child's PID. After `fork()`, both processes have copies of `fd[0]` and `fd[1]`. (1 mark)\n- **Diagram (1 mark):**\n\n```\nParent: write() -> fd[1] -> [Kernel Pipe Buffer] -> fd[0] -> read() :Child\n```\n\n- The parent closes `fd[0]`, writes the string, and closes `fd[1]`. (1 mark)\n- The child closes `fd[1]`, reads from `fd[0]`, prints the buffer, and closes `fd[0]`. (1 mark)\n- Full compilable C program (refer to Section 3.1) with `pipe()`, `fork()`, `close()`, `read()`, `write()`, `wait()`. (2 marks)\n\n**Model Solution for (b):**\n- A **FIFO (First In First Out)** or named pipe is a special file in the filesystem that allows unrelated processes to communicate. (2 marks)\n- **Differences from unnamed pipe (3 marks):**\n  | Aspect | Unnamed Pipe | FIFO |\n  | :--- | :--- | :--- |\n  | Process relationship | Parent-Child only | Unrelated processes allowed |\n  | Identification | File descriptors | Filesystem path (e.g., `/tmp/myfifo`) |\n  | Persistence | Process lifetime | Filesystem-based |\n- **System calls (2 marks):**\n  - Create: `mkfifo(\"/tmp/myfifo\", 0666)`\n  - Open: `fd = open(\"/tmp/myfifo\", O_RDONLY | O_WRONLY)`\n  - Remove: `unlink(\"/tmp/myfifo\")` or `rm` from shell.\n\n---\n\n#### **Question B (14 Marks) — Message Queues & Shared Memory Focus**\n\n**[KTU University Exam - Dec 2024, Module Internal, Model]**\n**(a)** Explain the System V message queue IPC mechanism. List the system calls used and explain the `msgrcv()` parameters in detail. **(7 marks)**\n\n**(b)** What is shared memory? Write a C program to demonstrate how two unrelated processes can communicate using shared memory. State one synchronization issue. **(7 marks)**\n\n**Model Solution for (a):**\n- System V message queues are kernel-managed linked lists of messages. Each message has a positive `long` type and a payload. (1 mark)\n- **System calls (3 marks):**\n  - `ftok()` — generates a unique key.\n  - `msgget()` — creates or opens a queue.\n  - `msgsnd()` — appends a message.\n  - `msgrcv()` — retrieves a message.\n  - `msgctl()` — control (e.g., `IPC_RMID` to delete).\n- **`msgrcv()` parameters (3 marks):**\n  - `int msqid` — the queue identifier from `msgget()`.\n  - `void *msgp` — pointer to a `struct msgbuf` (with `msg_type` first).\n  - `size_t msgsz` — size of the payload (excluding `long msg_type`).\n  - `long msgtyp` — `0` = first message, `>0` = first message with that type, `<0` = lowest type ≤ `|msgtyp|`.\n  - `int msgflg` — `0` (block), `IPC_NOWAIT`, `MSG_NOERROR`.\n\n**Model Solution for (b):**\n- Shared memory is a memory region mapped into the address space of multiple processes, allowing the fastest IPC as there is no kernel mediation during data access. (1 mark)\n- **C Program (refer to Section 3.4) — 4 marks:**\n  - Include `<sys/ipc.h>`, `<sys/shm.h>`.\n  - `ftok(\"shmfile\", 75)` to generate key.\n  - `shmget(key, 1024, 0666 | IPC_CREAT)` to allocate.\n  - `shmat(shmid, NULL, 0)` to attach and get pointer.\n  - Writer uses `fgets(str, 1024, stdin)`; reader uses `printf(\"%s\", str)`.\n  - `shmdt()` to detach; `shmctl(..., IPC_RMID, ...)` to destroy.\n- **Synchronization issue (2 marks):** Shared memory has no built-in mutual exclusion. If both processes write simultaneously, data corruption (race condition) occurs. Solution: combine with **semaphores** (`sem.h`) or use `wait()`/`signal()` in parent-child setups.\n\n---\n\n### KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mark-Deduction Traps (Avoid These!):**\n> 1. **Forgetting `wait(NULL)`** in the parent after `fork()` → the child becomes a **zombie process**, and the lab examiner will deduct **1 mark**.\n> 2. **Not closing the unused pipe end** (e.g., parent leaving `fd[0]` open) → the child's `read()` will **never see EOF**, causing a **hang**. Examiner deducts **2 marks**.\n> 3. **Incorrect `msg_type` definition** (e.g., using `int` instead of `long`, or placing it second in the struct) → the `msgsnd()` call will silently fail or corrupt memory. Examiner deducts **2 marks**.\n> 4. **Missing `ftok()` file** — `ftok(\"shmfile\", 65)` will return `-1` if `shmfile` does not exist on disk. Run `touch shmfile` first. Examiner deducts **1 mark**.\n> 5. **Not calling `msgctl(..., IPC_RMID, ...)`** — leftover IPC objects clutter the kernel tables. Use `ipcs` to verify cleanup. Examiner deducts **1 mark**.\n\n### Topic Recap & Important Things to Remember (Rapid Revision Checklist)\n\n- **IPC = Pipes + Message Queues + Shared Memory** (the KTU 2024 Module 2 triad).\n- **`pipe(fd)`** gives two descriptors: `fd[0]` (read), `fd[1]` (write). Always close the **unused** end in each process.\n- **FIFO = Named Pipe.** Use `mkfifo()` to create, `open()` to attach, `unlink()` to remove. Enables **unrelated process** communication.\n- **Message Queue** preserves **message boundaries** and supports **typed retrieval** via `msg_type` (a `long`, always first in the struct).\n- **Shared Memory is the fastest** IPC — no kernel copying — but **requires external synchronization** (semaphores).\n- **Cleanup is mandatory:** Use `msgctl(..., IPC_RMID, ...)` and `shmctl(..., IPC_RMID, ...)`. Verify with the shell command `ipcs`.\n- **Header rule of thumb:** Pipes → `<unistd.h>`; Message Queues → `<sys/msg.h>`; Shared Memory → `<sys/shm.h>`; IPC keys → `<sys/ipc.h>`.\n- **`ftok()` requires a real file** at the given path; otherwise, it returns `-1`.\n- **`fork()` returns `0` in the child and the child's PID in the parent** — this is the universal branching point.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "information-technology-5-pcitl408-mExperiment 2-t5",
          "title": "Implement producer-consumer problem and solve the problem using semaphores",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCITL408 - OPERATING SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Implement producer-consumer problem and solve the problem using semaphores\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 The Producer–Consumer Problem (Formal Definition)\n\nThe **Producer–Consumer Problem** (also called the **Bounded-Buffer Problem**) is a classic synchronization problem in operating systems where **two or more processes** share a common, fixed-size buffer (queue). A **producer process** generates data items and places them into the buffer, while a **consumer process** removes data items from the buffer and processes them. The challenge is to ensure that:\n\n1. The producer does not add items to a **full buffer**.\n2. The consumer does not remove items from an **empty buffer**.\n3. Access to the shared buffer is **mutually exclusive** (only one process manipulates it at a time).\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Highlight:** The Bounded-Buffer problem is the *foundational* synchronization problem. It is the first experiment in the OS Lab because it directly demonstrates the need for **process synchronization** primitives (semaphores/mutex) before moving to readers–writers, dining philosophers, etc.\n\n## 1.2 What is a Semaphore?\n\nA **semaphore** is a synchronization primitive (an integer variable) proposed by **Edsger Dijkstra (1965)** that controls access to shared resources by two standard atomic operations:\n\n- **wait()** (also called `P()`, `down()`, `decrement()`, or `lock()`)\n- **signal()** (also called `V()`, `up()`, `increment()`, or `unlock()`)\n\n**Two types of semaphores** exist:\n\n| Type | Property | Use Case |\n|------|----------|---------|\n| **Binary Semaphore (Mutex)** | Value ∈ {0, 1} | Mutual exclusion |\n| **Counting Semaphore** | Value ∈ [0, N] | Resource counting (e.g., empty/full slots) |\n\n> [!NOTE]\n> **Atomicity Guarantee:** The test-and-modify of the semaphore value must be executed **atomically** (uninterruptible). The OS achieves this using **hardware instructions** like `test-and-set` or by disabling interrupts during the kernel-level semaphore operation.\n\n## 1.3 Intuitive Real-World Analogy\n\nImagine a **restaurant kitchen pass-through window** (the buffer) of capacity **N = 5 plates**:\n\n- The **Chef (Producer)** cooks dishes and slides them onto the pass. If all 5 slots are filled, the chef must **wait** until a waiter takes a plate away.\n- The **Waiter (Consumer)** picks up plates from the pass and serves them. If the pass is **empty**, the waiter must **wait** until the chef places a new plate.\n- Only **one person** can touch the pass at a time (no chef putting and waiter taking simultaneously to avoid collision).\n\nThree rules govern the system:\n1. `Empty_Slots` counts free positions → chef waits if `Empty_Slots = 0`.\n2. `Full_Slots` counts occupied positions → waiter waits if `Full_Slots = 0`.\n3. `Mutex` ensures only one of {chef, waiter} accesses the pass at any instant.\n\n> [!TIP]\n> **Memory Hook for Exams:** *Producer signals `full`, waits for `empty`. Consumer signals `empty`, waits for `full`.* This one-liner answers most \"what does producer/consumer do\" questions.\n\n## 1.4 Why This Experiment Matters in KTU Labs\n\nThe bounded-buffer experiment is the **gateway to all synchronization experiments**. Mastering it directly enables you to solve:\n- Reader–Writer Problem (Experiment 3)\n- Dining Philosophers Problem (Experiment 4)\n- Banker's Algorithm (Deadlock avoidance)\n- Process Synchronization in CPU Scheduling labs\n\nThe shared resource is the buffer; the synchronization mechanism is the **triple semaphore trio**: `mutex`, `full`, `empty`.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Problem Formulation — Components\n\nThe Bounded-Buffer problem has **three critical components** that must be carefully designed:\n\n### 2.1.1 Shared Data Structure\n\nA circular queue of size **N** (typically 5 or 10 in lab implementations):\n\n- `buffer[N]` — the actual data array.\n- `in` — index where the **producer** will write the next item (starts at 0).\n- `out` — index from where the **consumer** will read the next item (starts at 0).\n\n### 2.1.2 The Three Semaphores\n\n1. **`mutex`** (Binary Semaphore, initial value = **1**)\n   - Ensures **mutual exclusion** — only one process (producer OR consumer) can access the buffer's critical section at any instant.\n2. **`empty`** (Counting Semaphore, initial value = **N**)\n   - Counts the **number of empty slots** available in the buffer.\n3. **`full`** (Counting Semaphore, initial value = **0**)\n   - Counts the **number of filled slots** in the buffer.\n\n### 2.1.3 Invariant (Conservation Law)\n\n> [!IMPORTANT]\n> **The Core Invariant:** At any instant in time, the following identity **must** hold:\n> \n> $$\\text{empty} + \\text{full} = N$$\n> \n> where **N** is the buffer capacity. This is the synchronization **conservation law** — every slot in the buffer is either empty or full, never both, never neither.\n\n## 2.2 Operational Logic — Step-by-Step\n\n### 2.2.1 Producer Process\n\nThe producer's life-cycle for **each item**:\n\n1. **Produce** a data item (outside critical section).\n2. `wait(empty)` — decrement empty-slot counter; block if buffer is **full** (`empty = 0`).\n3. `wait(mutex)` — acquire exclusive access to the buffer.\n4. **Critical Section:** Place item at `buffer[in]`; update `in = (in + 1) mod N`.\n5. `signal(mutex)` — release buffer access.\n6. `signal(full)` — increment full-slot counter; wake up blocked consumer.\n\n### 2.2.2 Consumer Process\n\nThe consumer's life-cycle for **each item**:\n\n1. `wait(full)` — decrement full-slot counter; block if buffer is **empty** (`full = 0`).\n2. `wait(mutex)` — acquire exclusive access to the buffer.\n3. **Critical Section:** Consume item from `buffer[out]`; update `out = (out + 1) mod N`.\n4. `signal(mutex)` — release buffer access.\n5. `signal(empty)` — increment empty-slot counter; wake up blocked producer.\n6. **Consume** the item (outside critical section).\n\n## 2.3 The \"Why\" Behind the Semaphore Order\n\n> [!WARNING]\n> **Critical KTU Pitfall — Deadlock due to wrong ordering:**\n> \n> The `wait(full)` / `wait(empty)` call **must** come *before* `wait(mutex)`. Reversing the order can cause **deadlock**:\n> - If buffer is full → producer holds `mutex` and waits for `empty` (but no consumer can run to signal `empty` because it can't acquire `mutex`).\n> - This is the classic \"hold-and-wait\" deadlock scenario — frequently asked in 7-mark questions!\n\n## 2.4 KTU Formula / Cheat Sheet\n\n| Concept | Symbol / Equation | Initial Value | Purpose |\n|---------|-------------------|---------------|---------|\n| Buffer Capacity | $N$ | — | Total slots in bounded buffer |\n| Empty Slots Counter | $\\text{empty}$ | $N$ | Counts available buffer positions |\n| Full Slots Counter | $\\text{full}$ | $0$ | Counts occupied buffer positions |\n| Mutual Exclusion Lock | $\\text{mutex}$ | $1$ | Allows single process in CS |\n| Conservation Law | $\\text{empty} + \\text{full} = N$ | Always true | System invariant |\n| Producer Index Update | $\\text{in} = (\\text{in} + 1) \\bmod N$ | $\\text{in} = 0$ | Circular write pointer |\n| Consumer Index Update | $\\text{out} = (\\text{out} + 1) \\bmod N$ | $\\text{out} = 0$ | Circular read pointer |\n| Occupancy State | $\\text{Buffer State} = \\text{full}$ value | $0 \\le \\text{full} \\le N$ | Current items in buffer |\n| Items Produced Total | $P_{\\text{total}} = \\sum \\text{producer items}$ | — | Cumulative output |\n| Items Consumed Total | $C_{\\text{total}} = \\sum \\text{consumer items}$ | — | Cumulative input |\n| Steady-State Balance | $P_{\\text{total}} = C_{\\text{total}}$ | At termination | No loss/duplication |\n\n## 2.5 Engineering & Real-World Utility\n\nThe Producer–Consumer pattern is not just an academic exercise — it is the **architectural backbone** of:\n\n- **Operating System Kernels:** Pipes, message queues, I/O buffers, socket buffers.\n- **Multithreaded Applications:** Thread pools (producer = task generator, consumer = worker thread).\n- **Streaming Systems:** Apache Kafka, RabbitMQ, AWS SQS — producers publish, consumers subscribe.\n- **Log Frameworks:** Application threads (producers) write to a log buffer; a dedicated logger thread (consumer) flushes to disk asynchronously.\n- **GUI Event Loops:** OS generates events (producer); UI thread processes them (consumer).\n\nThe semaphore trio (`empty`, `full`, `mutex`) is the **canonical textbook solution**, though modern systems also use monitors, condition variables, and lock-free ring buffers.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Mathematical Derivation: Buffer Occupancy & Semaphore State Evolution\n\nThis section rigorously derives how the semaphore values evolve during a sequence of producer/consumer operations. This type of step-trace is **highly tested** in KTU 14-mark problems.\n\n### 3.1.1 Setup\n\nLet $N = 5$ (buffer size). The semaphores are initialized as:\n\n$$\\text{empty} = 5, \\quad \\text{full} = 0, \\quad \\text{mutex} = 1$$\n\nThe buffer indices initialize as:\n\n$$\\text{in} = 0, \\quad \\text{out} = 0$$\n\n### 3.1.2 Step-Trace: 3 Productions followed by 2 Consumptions\n\n**Step 1 — Producer P₁ produces item `a`:**\n\n$$\\begin{aligned}\n\\text{wait(empty)} &: \\text{empty} = 5 - 1 = 4 \\\\\n\\text{wait(mutex)} &: \\text{mutex} = 1 - 1 = 0 \\\\\n\\text{buffer[0]} &= a; \\quad \\text{in} = (0+1) \\bmod 5 = 1 \\\\\n\\text{signal(mutex)} &: \\text{mutex} = 0 + 1 = 1 \\\\\n\\text{signal(full)} &: \\text{full} = 0 + 1 = 1\n\\end{aligned}$$\n\nState: $\\text{empty}=4, \\text{full}=1, \\text{mutex}=1$, buffer = `[a, _, _, _, _]`.\n\n**Step 2 — Producer P₁ produces item `b`:**\n\n$$\\begin{aligned}\n\\text{wait(empty)} &: \\text{empty} = 4 - 1 = 3 \\\\\n\\text{wait(mutex)} &: \\text{mutex} = 0 \\\\\n\\text{buffer[1]} &= b; \\quad \\text{in} = (1+1) \\bmod 5 = 2 \\\\\n\\text{signal(mutex)} &: \\text{mutex} = 1 \\\\\n\\text{signal(full)} &: \\text{full} = 1 + 1 = 2\n\\end{aligned}$$\n\nState: $\\text{empty}=3, \\text{full}=2$, buffer = `[a, b, _, _, _]`.\n\n**Step 3 — Producer P₁ produces item `c`:**\n\n$$\\begin{aligned}\n\\text{wait(empty)} &: \\text{empty} = 2 \\\\\n\\text{wait(mutex)} &: \\text{mutex} = 0 \\\\\n\\text{buffer[2]} &= c; \\quad \\text{in} = 3 \\\\\n\\text{signal(mutex)} &: \\text{mutex} = 1 \\\\\n\\text{signal(full)} &: \\text{full} = 3\n\\end{aligned}$$\n\nState: $\\text{empty}=2, \\text{full}=3$, buffer = `[a, b, c, _, _]`.\n\n**Step 4 — Consumer C₁ consumes an item:**\n\n$$\\begin{aligned}\n\\text{wait(full)} &: \\text{full} = 3 - 1 = 2 \\\\\n\\text{wait(mutex)} &: \\text{mutex} = 0 \\\\\n\\text{item} &= \\text{buffer[0]} = a; \\quad \\text{out} = (0+1) \\bmod 5 = 1 \\\\\n\\text{signal(mutex)} &: \\text{mutex} = 1 \\\\\n\\text{signal(empty)} &: \\text{empty} = 2 + 1 = 3\n\\end{aligned}$$\n\nState: $\\text{empty}=3, \\text{full}=2$, buffer = `[_, b, c, _, _]`, consumed = `a`.\n\n**Step 5 — Consumer C₁ consumes another item:**\n\n$$\\begin{aligned}\n\\text{wait(full)} &: \\text{full} = 1 \\\\\n\\text{wait(mutex)} &: \\text{mutex} = 0 \\\\\n\\text{item} &= \\text{buffer[1]} = b; \\quad \\text{out} = 2 \\\\\n\\text{signal(mutex)} &: \\text{mutex} = 1 \\\\\n\\text{signal(empty)} &: \\text{empty} = 4\n\\end{aligned}$$\n\nState: $\\text{empty}=4, \\text{full}=1$, buffer = `[_, _, c, _, _]`, consumed = `b`.\n\n### 3.1.3 Verification of the Conservation Law\n\nAt every step above:\n\n$$\\text{empty} + \\text{full} = 5 = N \\quad \\checkmark$$\n\nThis confirms the invariant holds throughout execution.\n\n## 3.2 Complete C Program (POSIX Semaphores — KTU Standard)\n\nThe following program is the **canonical KTU lab implementation** using POSIX semaphores (`sem_t`) and `pthread`. This will be required for the **lab record and viva**.\n\n```c\n#include <stdio.h>\n#include <pthread.h>\n#include <semaphore.h>\n#include <unistd.h>\n\n#define BUFFER_SIZE 5\n\nint buffer[BUFFER_SIZE];\nint in = 0, out = 0;\n\nsem_t mutex;     // binary semaphore for mutual exclusion\nsem_t empty;     // counting semaphore: empty slots\nsem_t full;      // counting semaphore: filled slots\n\nvoid *producer(void *arg) {\n    int item;\n    for (int i = 0; i < 10; i++) {\n        item = i + 1;                        // produce an item\n        sem_wait(&empty);                    // step 1: wait for empty slot\n        sem_wait(&mutex);                    // step 2: enter critical section\n        buffer[in] = item;                   // step 3: place item in buffer\n        printf(\"Producer produced: %d at index %d\\n\", item, in);\n        in = (in + 1) % BUFFER_SIZE;         // step 4: update write index\n        sem_post(&mutex);                    // step 5: leave critical section\n        sem_post(&full);                     // step 6: signal full slot\n        sleep(1);\n    }\n    pthread_exit(NULL);\n}\n\nvoid *consumer(void *arg) {\n    int item;\n    for (int i = 0; i < 10; i++) {\n        sem_wait(&full);                     // step 1: wait for filled slot\n        sem_wait(&mutex);                    // step 2: enter critical section\n        item = buffer[out];                  // step 3: remove item\n        printf(\"Consumer consumed: %d from index %d\\n\", item, out);\n        out = (out + 1) % BUFFER_SIZE;       // step 4: update read index\n        sem_post(&mutex);                    // step 5: leave critical section\n        sem_post(&empty);                    // step 6: signal empty slot\n        sleep(2);\n    }\n    pthread_exit(NULL);\n}\n\nint main() {\n    pthread_t prod, cons;\n\n    sem_init(&mutex, 0, 1);\n    sem_init(&empty, 0, BUFFER_SIZE);\n    sem_init(&full,  0, 0);\n\n    pthread_create(&prod, NULL, producer, NULL);\n    pthread_create(&cons, NULL, consumer, NULL);\n\n    pthread_join(prod, NULL);\n    pthread_join(cons, NULL);\n\n    sem_destroy(&mutex);\n    sem_destroy(&empty);\n    sem_destroy(&full);\n\n    return 0;\n}\n```\n\n### 3.2.1 Compilation and Execution\n\n```bash\ngcc producer_consumer.c -o pc -lpthread\n./pc\n```\n\n## 3.3 Trace of `sem_wait` and `sem_post` Behavior\n\nThe POSIX semaphore operations behave as follows:\n\n- `sem_wait(s)` — Atomically decrements the value of semaphore `s`. If the value becomes **negative**, the calling thread is **blocked** and added to `s`'s wait queue.\n- `sem_post(s)` — Atomically increments the value of semaphore `s`. If there are threads blocked on `s`, **one** is unblocked and its `sem_wait` returns.\n\n## 3.4 Analytical Matrix: What Happens at Boundary Conditions?\n\n| Scenario | Before | `wait(empty)` | `wait(full)` | Effect |\n|----------|--------|---------------|--------------|--------|\n| Buffer **Empty**, Consumer calls | `empty=N, full=0` | — | `full` becomes $-1$ | **Consumer blocks** |\n| Buffer **Full**, Producer calls | `empty=0, full=N` | `empty` becomes $-1$ | — | **Producer blocks** |\n| Buffer **Half-Full** | `empty=N/2, full=N/2` | decrements by 1 | (consumer may) decrement by 1 | Both proceed normally |\n| Producer + Consumer **race** | `mutex=1` | (independent) | (independent) | One waits for `mutex` |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 System-Level Block Diagram of the Bounded Buffer\n\n```mermaid\nflowchart LR\n    P[Producer Process] -- item --> E1{empty 0}\n    E1 -- yes --> M1{mutex 0}\n    E1 -- no --> BP[Producer Blocked]\n    M1 -- acquired --> CS1[Critical Section: write buffer in]\n    CS1 --> SM1[signal mutex]\n    SM1 --> SF1[signal full]\n    SF1 --> P\n\n    C[Consumer Process] -- request --> F1{full 0}\n    F1 -- yes --> M2{mutex 0}\n    F1 -- no --> BC[Consumer Blocked]\n    M2 -- acquired --> CS2[Critical Section: read buffer out]\n    CS2 --> SM2[signal mutex]\n    SM2 --> SE2[signal empty]\n    SE2 --> C\n\n    CS1 <--> BUF[(Bounded Buffer N slots)]\n    CS2 <--> BUF\n```\n\n## 4.2 Circular Buffer State Machine\n\n```mermaid\nstateDiagram-v2\n    [*] --> Empty\n    Empty --> PartiallyFilled: producer writes\n    PartiallyFilled --> Full: producer fills last slot\n    Full --> PartiallyFilled: consumer reads\n    PartiallyFilled --> Empty: consumer drains last slot\n    PartiallyFilled --> PartiallyFilled: producer write or consumer read\n    Empty --> Empty: consumer blocks\n    Full --> Full: producer blocks\n```\n\n## 4.3 Semaphore State Transition Diagram\n\n```mermaid\nflowchart TD\n    A[Init: empty N full 0 mutex 1] --> B{Producer Action}\n    B --> C[wait empty -> empty 0]\n    C --> D[wait mutex -> mutex 0]\n    D --> E[Write to buffer in]\n    E --> F[signal mutex -> mutex 1]\n    F --> G[signal full -> full 1]\n    G --> H{Consumer Action}\n    H --> I[wait full -> full 0]\n    I --> J[wait mutex -> mutex 0]\n    J --> K[Read from buffer out]\n    K --> L[signal mutex -> mutex 1]\n    L --> M[signal empty -> empty 1]\n    M --> B\n```\n\n## 4.4 Producer–Consumer Functional Architecture\n\n```mermaid\nflowchart TB\n    subgraph PROD[Producer Thread]\n        P1[Generate Data Item]\n        P2[sem_wait and empty]\n        P3[sem_wait and mutex]\n        P4[buffer in equals item]\n        P5[in equals in plus 1 mod N]\n        P6[sem_post and mutex]\n        P7[sem_post and full]\n    end\n\n    subgraph SHARED[Shared Resources]\n        B[(Circular Buffer N)]\n        SM[sem_t mutex equals 1]\n        SE[sem_t empty equals N]\n        SF[sem_t full equals 0]\n    end\n\n    subgraph CONS[Consumer Thread]\n        C1[sem_wait and full]\n        C2[sem_wait and mutex]\n        C3[item equals buffer out]\n        C4[out equals out plus 1 mod N]\n        C5[sem_post and mutex]\n        C6[sem_post and empty]\n        C7[Process Consumed Item]\n    end\n\n    P1 --> P2 --> P3 --> P4 --> P5 --> P6 --> P7\n    P7 --> B\n    C1 --> C2 --> C3 --> C4 --> C5 --> C6 --> C7\n    C3 --> B\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Q1. Define a semaphore. Differentiate between binary and counting semaphores. **[3 Marks]**\n`[KTU University Exam - May 2024]`\n\n**Model Answer:**\n\nA **semaphore** is a synchronization variable (integer) accessed only through two atomic operations: `wait()` (P) and `signal()` (V). It was proposed by **Dijkstra** to solve the critical section problem.\n\n| Feature | Binary Semaphore | Counting Semaphore |\n|---------|-----------------|--------------------|\n| Value range | {0, 1} | Any non-negative integer |\n| Use | Mutual exclusion | Resource counting |\n| Initial value | 1 | N (number of resources) |\n| Example | `mutex` | `empty`, `full` |\n\n### Q2. State the producer–consumer problem and list the three semaphores used to solve it. **[3 Marks]**\n`[KTU University Exam - Dec 2023]`\n\n**Model Answer:**\n\nThe **Producer–Consumer (Bounded-Buffer) Problem** requires synchronization between processes that produce and consume data items stored in a fixed-size shared buffer, ensuring that the producer does not overflow and the consumer does not underflow.\n\nThe three semaphores are:\n1. **`mutex`** (initial = 1) — ensures mutual exclusion on the buffer.\n2. **`empty`** (initial = N) — counts the number of empty buffer slots.\n3. **`full`** (initial = 0) — counts the number of filled buffer slots.\n\n---\n\n## 5.2 Part B Questions (14 Marks Each — Internal Choice)\n\n### QUESTION A (Choice 1)\n\n**Q3. (a)** With neat pseudocode, explain how the **producer–consumer problem** is solved using semaphores. Mention the role of each semaphore. **[7 Marks]**\n`[KTU University Exam - May 2024]`\n\n**Model Answer:**\n\nThe problem is solved using three semaphores initialized as:\n\n$$\\text{mutex} = 1, \\quad \\text{empty} = N, \\quad \\text{full} = 0$$\n\n**Producer Pseudocode:**\n\n```\nPRODUCER:\n  while (true) do\n      wait(empty)            // block if buffer is full\n      wait(mutex)            // enter critical section\n      buffer[in] = item      // place item\n      in = (in + 1) mod N    // advance write index\n      signal(mutex)          // leave critical section\n      signal(full)           // increment filled count\n  end while\n```\n\n**Consumer Pseudocode:**\n\n```\nCONSUMER:\n  while (true) do\n      wait(full)             // block if buffer is empty\n      wait(mutex)            // enter critical section\n      item = buffer[out]     // remove item\n      out = (out + 1) mod N  // advance read index\n      signal(mutex)          // leave critical section\n      signal(empty)          // increment empty count\n  end while\n```\n\n**Roles:**\n- `mutex` — guarantees that only one process manipulates the buffer at a time (mutual exclusion).\n- `empty` — ensures the producer waits when the buffer is full.\n- `full` — ensures the consumer waits when the buffer is empty.\n\n---\n\n**Q3. (b)** Consider a bounded buffer of size **N = 4**. Initially the buffer is empty. Producer produces 3 items, then consumer consumes 1 item, then producer produces 2 more items. Show the **state of all three semaphores and the buffer** after each step. Verify the conservation law. **[7 Marks]**\n`[KTU University Exam - Dec 2023]`\n\n**Model Answer:**\n\nInitial state: $\\text{empty}=4, \\text{full}=0, \\text{mutex}=1$, buffer = `[_, _, _, _]`, in=0, out=0.\n\n**Step 1 — Producer produces item 1:**\n\n$$\\begin{aligned}\n\\text{wait(empty)} &: \\text{empty}=3 \\\\\n\\text{wait(mutex)} &: \\text{mutex}=0 \\\\\n\\text{buffer[0]} &= 1; \\quad \\text{in}=1 \\\\\n\\text{signal(mutex)} &: \\text{mutex}=1 \\\\\n\\text{signal(full)} &: \\text{full}=1\n\\end{aligned}$$\n\nState: empty=3, full=1, buffer=[1,_,_,_].\n\n**Step 2 — Producer produces item 2:**\n\n$$\\begin{aligned}\n\\text{empty}=2,\\ \\text{full}=2,\\ \\text{mutex}=1 \\\\\n\\text{buffer}=[1,2,_,_],\\ \\text{in}=2\n\\end{aligned}$$\n\n**Step 3 — Producer produces item 3:**\n\n$$\\begin{aligned}\n\\text{empty}=1,\\ \\text{full}=3,\\ \\text{mutex}=1 \\\\\n\\text{buffer}=[1,2,3,_],\\ \\text{in}=3\n\\end{aligned}$$\n\n**Step 4 — Consumer consumes 1 item:**\n\n$$\\begin{aligned}\n\\text{wait(full)} &: \\text{full}=2 \\\\\n\\text{wait(mutex)} &: \\text{mutex}=0 \\\\\n\\text{item}=\\text{buffer[0]}=1;\\ \\text{out}=1 \\\\\n\\text{signal(mutex)} &: \\text{mutex}=1 \\\\\n\\text{signal(empty)} &: \\text{empty}=2\n\\end{aligned}$$\n\nState: empty=2, full=2, buffer=[_,2,3,_], out=1.\n\n**Step 5 — Producer produces item 4:**\n\n$$\\begin{aligned}\n\\text{empty}=1,\\ \\text{full}=3 \\\\\n\\text{buffer}=[_,2,3,4],\\ \\text{in}=4 \\bmod 4 = 0\n\\end{aligned}$$\n\n**Step 6 — Producer produces item 5:**\n\n$$\\begin{aligned}\n\\text{empty}=0,\\ \\text{full}=4 \\\\\n\\text{buffer}=[5,2,3,4],\\ \\text{in}=1\n\\end{aligned}$$\n\n**Final State:** empty=0, full=4, mutex=1, buffer=[5,2,3,4], in=1, out=1.\n\n**Conservation Law Verification (every step):**\n\n$$\\text{empty} + \\text{full} = 4 = N \\quad \\checkmark$$\n\n---\n\n### QUESTION B (Choice 2 — Alternative)\n\n**Q4. (a)** Explain the **wait() and signal() operations** of a semaphore. Show their implementation using **test-and-set** to guarantee atomicity. **[7 Marks]**\n`[KTU University Exam - May 2024]`\n\n**Model Answer:**\n\nThe **wait()** operation decrements the semaphore value; if the value becomes negative, the calling process is **blocked**.\n\nThe **signal()** operation increments the semaphore value; if any process is blocked on this semaphore, **one** is woken up.\n\n**Implementation using test-and-set (atomic hardware instruction):**\n\n```\nwait(S):\n   while (test_and_set(&lock) == 1)   // spin until lock acquired\n       ;                               // busy wait\n   S = S - 1;                         // critical modification\n   lock = 0;                          // release hardware lock\n   if (S < 0)\n       block(process);                // add to semaphore queue\n\nsignal(S):\n   while (test_and_set(&lock) == 1)\n       ;\n   S = S + 1;\n   lock = 0;\n   if (S <= 0)\n       wakeup(process from S.queue);\n```\n\nThe `test_and_set` instruction executes **atomically** — it reads and writes a memory location in a single, uninterruptible step, ensuring no race condition can occur on the semaphore variable.\n\n---\n\n**Q4. (b)** What happens if the **producer and consumer interchange the order of `wait()` and `wait(mutex)`** calls? Demonstrate with a scenario. **[7 Marks]**\n`[KTU University Exam - Dec 2023]`\n\n**Model Answer:**\n\nIf the producer reverses the order — i.e., calls `wait(mutex)` first, then `wait(empty)` — a **deadlock** can occur in the following scenario:\n\n**Deadlock Scenario (Buffer is Full, N=5):**\n\n1. Producer P calls `wait(mutex)` → mutex = 0 (acquired).\n2. Producer P then calls `wait(empty)` → `empty` was 0, so `empty` becomes **−1**, and **P blocks** (still holding mutex).\n3. Consumer C tries to call `wait(full)` → succeeds, full = N−1.\n4. Consumer C then calls `wait(mutex)` → mutex = 0, so C **blocks** waiting for mutex.\n5. Now P is waiting for `empty` (which can only be signaled by a consumer), and C is waiting for `mutex` (which is held by P). **Classic circular wait → deadlock!**\n\n**Circular wait condition holds:**\n- P holds: `mutex`; waits for: `empty`.\n- C holds: (none); waits for: `mutex`.\n\n**Solution:** Always call resource-count semaphores (`empty` / `full`) **before** `mutex`. This enforces an **ordered lock acquisition** that prevents hold-and-wait cycles, satisfying the **deadlock prevention** condition of circular-wait elimination.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Mark-Deduction Pitfalls:**\n> 1. **Forgetting circular buffer wrap-around:** Writing `in = in + 1` instead of `in = (in + 1) % N`. This causes index-out-of-bounds errors. (−1 to −2 marks)\n> 2. **Swapping the order of `wait()` calls:** Causes deadlock, as analyzed in Q4(b). (−2 to −3 marks)\n> 3. **Not initializing `mutex = 1`:** Leads to immediate blocking. (−1 mark)\n> 4. **Confusing initial values:** Students often set `empty = 0` and `full = N`. Remember: at the start, the buffer is **empty**, so `empty = N` and `full = 0`. (−1 to −2 marks)\n> 5. **Missing `sleep()` calls:** Without producer/consumer delays, output appears correct only by chance. Examiners may deduct marks if no synchronization stress is visible. (−1 mark)\n> 6. **Using `sem_t` but forgetting `-lpthread` flag** in compilation. (Lab record issue, not theory)\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Rapid Revision Checklist:**\n\n- ✅ The **Producer–Consumer Problem** is also called the **Bounded-Buffer Problem**.\n- ✅ Three semaphores are used: **`mutex` = 1**, **`empty` = N**, **`full` = 0**.\n- ✅ **Conservation Law:** `empty + full = N` (always true).\n- ✅ **Producer:** waits on `empty`, signals `full`. **Consumer:** waits on `full`, signals `empty`.\n- ✅ **Critical ordering rule:** `wait(empty)` / `wait(full)` **must** be called **before** `wait(mutex)`.\n- ✅ Buffer indices use **circular arithmetic:** `in = (in + 1) % N`, `out = (out + 1) % N`.\n- ✅ `wait()` = `P()` = `down()` = `decrement()`. `signal()` = `V()` = `up()` = `increment()`.\n- ✅ **Atomicity** is achieved using hardware instructions like `test-and-set` or by disabling interrupts in the kernel.\n- ✅ **Binary semaphore (mutex)** value ∈ {0, 1}. **Counting semaphore** value ∈ [0, N].\n- ✅ **Deadlock** occurs if `wait(mutex)` is taken before `wait(empty/full)`.\n- ✅ **POSIX functions:** `sem_init()`, `sem_wait()`, `sem_post()`, `sem_destroy()`.\n- ✅ **Compile with:** `gcc filename.c -o output -lpthread`.\n- ✅ **Real-world uses:** pipes, message queues, thread pools, Kafka, log buffers, socket I/O.\n- ✅ The experiment builds the foundation for the **Readers–Writers** and **Dining Philosophers** problems.\n- ✅ **Block** state vs **Wakeup** state are managed by the OS scheduler, not by user code.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "information-technology-5-pcitl408-mExperiment 2-t6",
          "title": "Implementation of dining philosophers' problem using threads and semaphores",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCITL408 - OPERATING SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Implementation of dining philosophers' problem using threads and semaphores\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\n> [!NOTE]\n> **Dining Philosophers Problem (DPP)** is a classic **synchronization and concurrency problem** formulated by **Edsger W. Dijkstra (1965)**. It is used to illustrate the challenges of **resource contention, mutual exclusion, deadlock, and starvation** in a multi-threaded operating system environment.\n\nIn the problem, **N philosophers** sit around a circular table. Between every pair of adjacent philosophers lies exactly **one chopstick (fork)**. Each philosopher alternates between three states:\n\n$$S_{philosopher} \\in \\{\\text{THINKING},\\ \\text{HUNGRY},\\ \\text{EATING}\\}$$\n\nA philosopher can **eat** only when he/she holds **both** the left and right chopsticks. The challenge is to design a concurrent algorithm that allows maximum parallelism while **guaranteeing no deadlock, no starvation, and mutual exclusion**.\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight (PCITL408 - Module 2):** Students must implement DPP using **POSIX threads (`pthread.h`)** and **POSIX semaphores (`semaphore.h`)** in C. The program must demonstrate proper `pthread_create()`, `sem_init()`, `sem_wait()`, and `sem_post()` usage on a **Linux environment** (typically Ubuntu/Fedora).\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine **5 friends** sitting around a round dining table. Between every two friends is **exactly one chopstick** (so 5 chopsticks total). Each friend thinks for a while, gets hungry, and to eat noodles needs **2 chopsticks** — the one on their left and the one on their right.\n\n- If everyone grabs their left chopstick at the same time → **deadlock** (nobody can grab the right one).\n- A philosopher who never gets a turn → **starvation**.\n- Two adjacent philosophers eating at the same time → fine; sharing a chopstick → forbidden.\n\nThe OS scheduler must coordinate them so the **dinner proceeds forever** without anyone starving.\n\n## 1.3 Key Terminology\n\n| Term | Definition |\n| :--- | :--- |\n| **Thread** | Lightweight process; the basic unit scheduled by the OS kernel. |\n| **Semaphore** | Integer variable accessed only via atomic `wait` and `signal` operations. |\n| **Mutex (Binary Semaphore)** | A semaphore with value restricted to **0 or 1**, used for mutual exclusion. |\n| **Deadlock** | A state where two or more processes wait indefinitely for an event only one of them can cause. |\n| **Starvation** | A process waits forever because other processes monopolize the resource. |\n| **Critical Section** | Code segment accessing shared resources that must be executed atomically. |\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Four Classical Conditions for Deadlock (Coffman Conditions)\n\nFor a deadlock to occur, **all four** of these must hold simultaneously:\n\n| # | Condition | Meaning in DPP Context |\n| :---: | :--- | :--- |\n| 1 | **Mutual Exclusion** | A chopstick can be held by only one philosopher at a time. |\n| 2 | **Hold and Wait** | A philosopher holds one chopstick while waiting for the other. |\n| 3 | **No Preemption** | A chopstick cannot be forcibly taken from a philosopher. |\n| 4 | **Circular Wait** | A cycle exists: P1 waits for P2's fork, P2 waits for P3's, …, P5 waits for P1's. |\n\n> [!IMPORTANT]\n> **Strategy to Break Deadlock in DPP:** Our solution breaks **Circular Wait** by ensuring a philosopher picks up both forks *only* when both are free (atomic `test()` function under a mutex).\n\n## 2.2 State Diagram of a Philosopher\n\n$$\n\\begin{aligned}\n\\text{THINKING} &\\xrightarrow{\\text{get hungry}} \\text{HUNGRY} \\\\\n\\text{HUNGRY} &\\xrightarrow{\\text{both forks free}} \\text{EATING} \\\\\n\\text{EATING} &\\xrightarrow{\\text{release forks}} \\text{THINKING}\n\\end{aligned}\n$$\n\n## 2.3 Semaphore Design\n\nWe require the following semaphore infrastructure:\n\n| Semaphore | Count | Purpose |\n| :--- | :---: | :--- |\n| `mutex` | **1** | Binary semaphore that protects the shared `state[]` array (critical section guard). |\n| `S[0..N-1]` | **0** (each) | One semaphore per philosopher; the philosopher blocks on `S[i]` until notified it can eat. |\n\nThe global `state[i]` array holds the current state of every philosopher $P_i$:\n\n$$\nstate[i] =\n\\begin{cases}\n0 & \\text{if } P_i \\text{ is THINKING} \\\\\n1 & \\text{if } P_i \\text{ is HUNGRY} \\\\\n2 & \\text{if } P_i \\text{ is EATING}\n\\end{cases}\n$$\n\n## 2.4 KTU High-Yield Formula / Concept Sheet\n\n| Concept | Formula / Expression | KTU Significance |\n| :--- | :--- | :--- |\n| Left neighbour of $P_i$ | $LEFT = (i + 4) \\mod N$ | Used to check if neighbour is EATING. |\n| Right neighbour of $P_i$ | $RIGHT = (i + 1) \\mod N$ | Used to check if neighbour is EATING. |\n| Condition to eat | $state[i] = HUNGRY \\ \\land\\ state[LEFT] \\neq EATING \\ \\land\\ state[RIGHT] \\neq EATING$ | The `test(i)` predicate. |\n| Number of resources | $N$ (equal to number of philosophers) | Avoids resource over-allocation. |\n| Total semaphores | $N + 1$ | One mutex + N philosopher semaphores. |\n\n> [!NOTE]\n> **Real-world utility:** DPP models problems like **database lock managers, distributed mutual exclusion (Ricart-Agrawala), router packet scheduling, and multi-threaded transaction processing** in banking systems.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Implementation & Analytical Matrix\n\n## 3.1 Algorithm Logic (Step-by-Step)\n\nThe complete C program for KTU Lab Examination is built in **6 structured steps**:\n\n### Step 1: Header Files and Macros\n\nWe include the necessary POSIX headers and define the constants and macros for neighbour calculation.\n\n```c\n#include <stdio.h>\n#include <pthread.h>\n#include <semaphore.h>\n#include <unistd.h>\n\n#define N 5\n#define THINKING 0\n#define HUNGRY   1\n#define EATING   2\n\n#define LEFT  (i + 4) % N\n#define RIGHT (i + 1) % N\n```\n\n**Logic Explanation:**\n- `N = 5` philosophers, since DPP at KTU standardly uses **5**.\n- The `LEFT` and `RIGHT` macros compute neighbours **circularly** using modulo arithmetic.\n- For $i=0$: LEFT = $(0+4)\\% 5 = 4$, RIGHT = $(0+1)\\% 5 = 1$. ✓\n\n### Step 2: Global Declarations\n\n```c\nsem_t mutex;        // Protects the state[] array\nsem_t S[N];         // One semaphore per philosopher\n\nint state[N];       // Shared state array\nint phil[N] = {0, 1, 2, 3, 4};  // Philosopher IDs\n```\n\n**Logic Explanation:**\n- `mutex` is a **binary semaphore** initialised to 1 (acts as a lock).\n- `S[i]` is initialised to 0 — philosopher $P_i$ starts blocked until `sem_post(&S[i])` is called on its behalf.\n\n### Step 3: The `main()` Function — Thread and Semaphore Initialization\n\n```c\nint main(void) {\n    int i;\n    pthread_t tid[N];\n\n    // Initialize the mutex (shared between threads of the same process: pshared = 0)\n    sem_init(&mutex, 0, 1);\n\n    // Initialize each philosopher's semaphore to 0\n    for (i = 0; i < N; i++)\n        sem_init(&S[i], 0, 0);\n\n    // Create one thread per philosopher\n    for (i = 0; i < N; i++)\n        pthread_create(&tid[i], NULL, philosopher, &phil[i]);\n\n    // Wait for all threads (in this simulation they run forever, so join is illustrative)\n    for (i = 0; i < N; i++)\n        pthread_join(tid[i], NULL);\n\n    return 0;\n}\n```\n\n**Logic Explanation:**\n- `sem_init(&mutex, 0, 1)` → second argument `pshaged = 0` means it is **shared between threads of the same process**; value initialised to **1** (unlocked).\n- `pthread_create()` spawns each philosopher in its own thread of execution.\n\n### Step 4: The Philosopher Thread — Infinite Loop\n\n```c\nvoid *philosopher(void *arg) {\n    int i = *(int *)arg;   // Recover philosopher's index\n\n    while (1) {\n        think(i);\n        take_forks(i);\n        eat(i);\n        put_forks(i);\n    }\n    return NULL;\n}\n\nvoid think(int i) {\n    printf(\"Philosopher %d is THINKING\\n\", i + 1);\n    sleep(1);\n}\n\nvoid eat(int i) {\n    printf(\"Philosopher %d is EATING\\n\", i + 1);\n    sleep(2);\n}\n```\n\n**Logic Explanation:** Each philosopher cycles through **THINK → HUNGRY → EAT** indefinitely, simulating the classical problem.\n\n### Step 5: The `take_forks()` Function — Acquiring Resources Safely\n\n```c\nvoid take_forks(int i) {\n    sem_wait(&mutex);        // ENTER critical section\n    state[i] = HUNGRY;\n    printf(\"Philosopher %d is HUNGRY\\n\", i + 1);\n    test(i);                 // Try to acquire both forks\n    sem_post(&mutex);        // EXIT critical section\n    sem_wait(&S[i]);         // Block if forks not granted\n}\n```\n\n**Logic Explanation:**\n- The `state[]` array is **shared**; we protect all reads/writes with `mutex`.\n- `test(i)` is called *inside* the critical section — if both forks are free, it sets `state[i] = EATING` and calls `sem_post(&S[i])`.\n- After releasing the mutex, the philosopher calls `sem_wait(&S[i])`. If `S[i]` was already posted (forks granted), the wait returns immediately; otherwise, the philosopher **blocks** until a neighbour releases forks and posts to `S[i]`.\n\n### Step 6: The `put_forks()` and `test()` Functions — Release and Neighbour Notification\n\n```c\nvoid put_forks(int i) {\n    sem_wait(&mutex);\n    state[i] = THINKING;\n    printf(\"Philosopher %d put down forks\\n\", i + 1);\n    test(LEFT);              // Left neighbour may now be able to eat\n    test(RIGHT);             // Right neighbour may now be able to eat\n    sem_post(&mutex);\n}\n\nvoid test(int i) {\n    if (state[i] == HUNGRY &&\n        state[LEFT]  != EATING &&\n        state[RIGHT] != EATING) {\n\n        state[i] = EATING;\n        printf(\"Philosopher %d starts EATING\\n\", i + 1);\n        sem_post(&S[i]);     // Wake up philosopher i\n    } else {\n        printf(\"Philosopher %d cannot eat yet (waiting)\\n\", i + 1);\n    }\n}\n```\n\n**Logic Explanation:**\n- The `LEFT` and `RIGHT` macros use `i` (the implicit global) — note: the C standard requires macros to reference the variable `i` defined in the calling function's scope.\n- After putting down forks, the philosopher **wakes up its hungry neighbours** by calling `test(LEFT)` and `test(RIGHT)`. If a neighbour is HUNGRY and its *other* neighbour is not EATING, that neighbour is granted permission to eat.\n\n## 3.2 Complete Compilation-Ready Source Code\n\n```c\n#include <stdio.h>\n#include <pthread.h>\n#include <semaphore.h>\n#include <unistd.h>\n\n#define N 5\n#define THINKING 0\n#define HUNGRY   1\n#define EATING   2\n\n#define LEFT  (i + 4) % N\n#define RIGHT (i + 1) % N\n\nsem_t mutex;\nsem_t S[N];\nint state[N];\nint phil[N] = {0, 1, 2, 3, 4};\n\nvoid *philosopher(void *);\nvoid test(int);\nvoid take_forks(int);\nvoid put_forks(int);\n\nint main(void) {\n    int i;\n    pthread_t tid[N];\n    sem_init(&mutex, 0, 1);\n    for (i = 0; i < N; i++) sem_init(&S[i], 0, 0);\n    for (i = 0; i < N; i++) pthread_create(&tid[i], NULL, philosopher, &phil[i]);\n    for (i = 0; i < N; i++) pthread_join(tid[i], NULL);\n    return 0;\n}\n\nvoid *philosopher(void *arg) {\n    int i = *(int *)arg;\n    while (1) { think(i); take_forks(i); eat(i); put_forks(i); }\n    return NULL;\n}\n\nvoid think(int i) { printf(\"P%d THINKING\\n\", i+1); sleep(1); }\nvoid eat(int i)   { printf(\"P%d EATING\\n\",   i+1); sleep(2); }\n\nvoid take_forks(int i) {\n    sem_wait(&mutex);\n    state[i] = HUNGRY;\n    printf(\"P%d HUNGRY\\n\", i+1);\n    test(i);\n    sem_post(&mutex);\n    sem_wait(&S[i]);\n}\n\nvoid put_forks(int i) {\n    sem_wait(&mutex);\n    state[i] = THINKING;\n    printf(\"P%d put down forks\\n\", i+1);\n    test((i + 4) % N);\n    test((i + 1) % N);\n    sem_post(&mutex);\n}\n\nvoid test(int i) {\n    if (state[i] == HUNGRY &&\n        state[(i + 4) % N] != EATING &&\n        state[(i + 1) % N] != EATING) {\n        state[i] = EATING;\n        printf(\"P%d starts EATING\\n\", i+1);\n        sem_post(&S[i]);\n    }\n}\n```\n\n## 3.3 Compilation and Execution\n\n```bash\ngcc -o dining dining_philosophers.c -lpthread\n./dining\n```\n\n> [!NOTE]\n> The `-lpthread` flag links the **POSIX threads library** (required on Linux/gcc). Without it, `pthread_create()` and `sem_init()` will produce *undefined reference* linker errors.\n\n## 3.4 Analytical Trace Table (Sample Run)\n\n| Step | Action | $state[0..4]$ | Output |\n| :---: | :--- | :--- | :--- |\n| 1 | P0 HUNGRY, test(0) | $(1,0,0,0,0)$ → $P_4$ not EATING, $P_1$ not EATING | P0 starts EATING |\n| 2 | P1 HUNGRY, test(1) | $(2,1,0,0,0)$ → $P_0$ EATING → blocked on $S[1]$ | P1 cannot eat yet |\n| 3 | P0 releases forks, calls test(4), test(1) | $P_1$ & $P_4$ tested | P1 starts EATING |\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Circular Resource Layout (Block Diagram)\n\n```mermaid\nflowchart LR\n    P0((P0)) ---|fork0| F0{{Chopstick 0}}\n    F0 --- P1((P1))\n    P1 ---|fork1| F1{{Chopstick 1}}\n    F1 --- P2((P2))\n    P2 ---|fork2| F2{{Chopstick 2}}\n    F2 --- P3((P3))\n    P3 ---|fork3| F3{{Chopstick 3}}\n    F3 --- P4((P4))\n    P4 ---|fork4| F4{{Chopstick 4}}\n    F4 --- P0\n```\n\n## 4.2 Philosopher State Machine\n\n```mermaid\nstateDiagram-v2\n    [*] --> THINKING\n    THINKING --> HUNGRY: get_hungry\n    HUNGRY --> EATING: test_ok_AND_neighbors_not_EATING\n    EATING --> THINKING: release_forks\n    HUNGRY --> HUNGRY: test_fail_blocks_on_Si\n```\n\n## 4.3 Algorithm Flowchart (Detailed Functional Architecture)\n\n```mermaid\nflowchart TD\n    A([Start: pthread_create x5]) --> B[Initialize mutex = 1]\n    B --> C[Initialize S 0 to 4 = 0]\n    C --> D[Philosopher thread spawned]\n\n    D --> E[think i]\n    E --> F{Hungry?}\n\n    F --> G[sem_wait mutex]\n    G --> H[state i = HUNGRY]\n    H --> I[test i]\n    I --> J{Both forks free?}\n    J -->|Yes| K[state i = EATING, sem_post S i]\n    J -->|No| L[remain HUNGRY]\n\n    K --> M[sem_post mutex]\n    L --> M\n    M --> N[sem_wait S i - block until granted]\n    N --> O[eat i]\n    O --> P[put_forks i]\n    P --> Q[sem_wait mutex]\n    Q --> R[state i = THINKING]\n    R --> S[test LEFT and test RIGHT]\n    S --> T[sem_post mutex]\n    T --> E\n```\n\n## 4.4 Synchronization Mechanism (Mutex + Per-Process Semaphores)\n\n```mermaid\nflowchart LR\n    subgraph \"Shared Resources\"\n        ST[\"state 0..4 array\"]\n        M[\"mutex = 1\"]\n    end\n\n    subgraph \"Per-Process Blockers\"\n        S0[\"S 0 = 0\"]\n        S1[\"S 1 = 0\"]\n        S2[\"S 2 = 0\"]\n        S3[\"S 3 = 0\"]\n        S4[\"S 4 = 0\"]\n    end\n\n    P0([\"Philosopher 0\"]) --> M\n    P1([\"Philosopher 1\"]) --> M\n    P2([\"Philosopher 2\"]) --> M\n    P3([\"Philosopher 3\"]) --> M\n    P4([\"Philosopher 4\"]) --> M\n\n    P0 -. blocks on .-> S0\n    P1 -. blocks on .-> S1\n    P2 -. blocks on .-> S2\n    P3 -. blocks on .-> S3\n    P4 -. blocks on .-> S4\n\n    M --> ST\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. `[KTU University Exam - Dec 2023]`\n**State the Dining Philosophers Problem and list the three states a philosopher can be in.**\n\n**Model Answer:**\nThe Dining Philosophers Problem is a classical synchronization problem posed by Edsger Dijkstra. It involves **N philosophers** sitting around a circular table with **N chopsticks**, one between each adjacent pair. Each philosopher requires **two chopsticks** (left and right) to eat.\n\nThe three states a philosopher can be in are:\n1. **THINKING** — the philosopher is not using any chopstick.\n2. **HUNGRY** — the philosopher is trying to acquire both chopsticks.\n3. **EATING** — the philosopher has acquired both chopsticks and is eating.\n\n*(3 marks: 1.5 for definition + 1.5 for states)*\n\n### Q2. `[KTU University Exam - June 2024]`\n**Differentiate between a binary semaphore and a counting semaphore. Which one is used as `mutex` in the Dining Philosophers solution?**\n\n**Model Answer:**\n\n| Feature | Binary Semaphore | Counting Semaphore |\n| :--- | :--- | :--- |\n| Value range | $0$ or $1$ | Any non-negative integer |\n| Use case | Mutual exclusion | Resource counting (e.g., pool of $k$ resources) |\n| Operations | `wait` / `signal` | `wait` / `signal` |\n\nIn the DPP solution, the `mutex` variable is a **binary semaphore** initialised to **1** because it is used purely for **mutual exclusion** while modifying the shared `state[]` array. The array `S[0..N-1]` uses **counting semaphores** initialised to **0**, since they act as per-philosopher wake-up signals.\n\n*(3 marks)*\n\n---\n\n## 5.2 Part B — Full 14-Mark Questions (ESE Module Internal Choice)\n\n### Question A (14 Marks) — `[KTU University Exam - Dec 2022]`\n\n**(a)** Explain the Dining Philosophers Problem. Discuss the conditions that lead to **deadlock** in the naive solution where each philosopher picks up the left fork first. **(7 marks)**\n\n**(b)** Write a complete C program using **POSIX threads and semaphores** to implement a deadlock-free solution. Explain the role of the `test()` function. **(7 marks)**\n\n#### Model Solution for (a):\n\nThe Dining Philosophers Problem models **resource contention** in a concurrent system. In the naive solution, every philosopher follows the same algorithm:\n\n1. Pick up the **left** chopstick.\n2. Pick up the **right** chopstick.\n3. Eat.\n4. Put down both chopsticks.\n\n**Deadlock occurs** when all five philosophers simultaneously pick up their left chopstick. At that moment:\n\n$$\n\\forall i \\in \\{0,1,2,3,4\\}: \\text{right chopstick of } P_i = \\text{left chopstick of } P_{(i+1)\\%N}\n$$\n\nSo every right chopstick is held by the neighbour. All four **Coffman conditions** are simultaneously satisfied:\n- **Mutual Exclusion** — chopsticks are non-shareable.\n- **Hold and Wait** — every $P_i$ holds its left chopstick and waits for the right.\n- **No Preemption** — once held, a chopstick cannot be taken by force.\n- **Circular Wait** — the wait graph forms a cycle: $P_0 \\rightarrow P_1 \\rightarrow P_2 \\rightarrow P_3 \\rightarrow P_4 \\rightarrow P_0$.\n\nThe system halts forever — a classic **circular deadlock**.\n\n*(7 marks: 2 for problem statement, 3 for algorithm trace, 2 for Coffman conditions)*\n\n#### Model Solution for (b):\n\nRefer to the **complete source code** provided in **Section 3.2** above. The key elements of the answer are:\n\n| Component | Role |\n| :--- | :--- |\n| `pthread_create()` | Spawns one thread per philosopher. |\n| `sem_init(&mutex, 0, 1)` | Binary semaphore; protects `state[]` from race conditions. |\n| `sem_init(&S[i], 0, 0)` | One counting semaphore per philosopher; blocks it until forks are granted. |\n| `test(i)` | Atomically checks if $P_i$ and both neighbours satisfy the eating condition; grants forks via `sem_post(&S[i])`. |\n| `take_forks()` | Sets state to HUNGRY, calls `test()`, releases mutex, then blocks on `S[i]`. |\n| `put_forks()` | Sets state to THINKING, then calls `test(LEFT)` and `test(RIGHT)` to wake neighbours. |\n\n**Role of `test()`:** It is the **decision-making core** of the algorithm. It checks the precondition (philosopher HUNGRY + neither neighbour EATING) and atomically grants the chopsticks by calling `sem_post(&S[i])`. It is the only function that transitions a philosopher from HUNGRY to EATING.\n\n*(7 marks: 3 for code, 2 for test() explanation, 2 for correctness reasoning)*\n\n---\n\n### Question B (14 Marks) — `[KTU University Exam - June 2023]`\n\n**(a)** What are the four necessary conditions for deadlock (Coffman conditions)? Show how each manifests in the Dining Philosophers problem. **(7 marks)**\n\n**(b)** Discuss **two alternative solutions** to the Dining Philosophers Problem that avoid deadlock. Compare their advantages. **(7 marks)**\n\n#### Model Solution for (a):\n\n| # | Coffman Condition | Manifestation in DPP |\n| :---: | :--- | :--- |\n| 1 | **Mutual Exclusion** | A chopstick is held by only one philosopher at a time. |\n| 2 | **Hold and Wait** | A philosopher who has acquired the left chopstick continues to hold it while waiting for the right. |\n| 3 | **No Preemption** | A chopstick cannot be forcibly removed from a philosopher mid-meal. |\n| 4 | **Circular Wait** | The wait-for graph forms a directed cycle around the table: $P_0 \\to P_1 \\to P_2 \\to P_3 \\to P_4 \\to P_0$. |\n\nTo break deadlock, our solution **removes the Circular Wait** by ensuring a philosopher picks up chopsticks only when both are free (the `test()` function under the `mutex`).\n\n*(7 marks: 1 per condition + 3 for explanation)*\n\n#### Model Solution for (b):\n\n**Solution 1 — Resource Hierarchy (Asymmetric Ordering):**\n\nNumber the chopsticks $0, 1, 2, 3, 4$. Each philosopher always picks up the **lower-numbered** chopstick first, then the higher-numbered one. This breaks the circular wait.\n\n**Solution 2 — Arbitrator / Semaphore-based (the KTU standard):**\n\nIntroduce a global `mutex` and per-philosopher semaphores. A philosopher attempts to eat by calling `test()`, which only succeeds if **both** neighbours are not eating. This is the implementation covered in Section 3.2.\n\n**Comparison:**\n\n| Aspect | Resource Hierarchy | Semaphore-based |\n| :--- | :--- | :--- |\n| Complexity | Low | Medium |\n| Max parallelism | High (4 of 5 can eat) | High |\n| Code size | Small | Moderate |\n| KTU preference | Mentioned in theory | **Required for lab exam** |\n\n*(7 marks: 3 + 3 for solutions, 1 for comparison)*\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> 1. **Forgetting `-lpthread`** during compilation — results in *undefined reference to `pthread_create`* linker error. Examiners will deduct marks if the program is not compilable.\n> 2. **Forgetting `sem_post(&mutex)`** before `sem_wait(&S[i])` — leads to **deadlock** in practice. Always release the mutex *before* blocking.\n> 3. **Using `&S[i]`** as an integer instead of passing the address — `sem_wait` and `sem_post` expect pointers to `sem_t`, not values.\n> 4. **Confusing `state[]` with the chopstick** — chopsticks are *implicit*; only the `state` array and `S[]` semaphores exist in code.\n> 5. **Calling `test(i)` outside the critical section** — this causes race conditions on the shared `state[]` array.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Revision Checklist for KTU PCITL408 Module 2:**\n\n- **DPP is posed by Edsger Dijkstra (1965)** to illustrate synchronization problems.\n- **Number of philosophers = Number of chopsticks** = **N** (typically 5).\n- **Three states per philosopher:** THINKING (0), HUNGRY (1), EATING (2).\n- **Two semaphores required:** `mutex` (binary, value = 1) and `S[N]` (counting, all initialised to 0).\n- **Four Coffman conditions** for deadlock: Mutual Exclusion, Hold & Wait, No Preemption, Circular Wait.\n- **The `test(i)` function** is the heart of the solution — it atomically grants the chopsticks if neighbours are not eating.\n- **Compile command:** `gcc -o dining dining.c -lpthread` — the `-lpthread` flag is **mandatory**.\n- **Required header files:** `<pthread.h>`, `<semaphore.h>`, `<stdio.h>`, `<unistd.h>`.\n- **Library functions used:** `pthread_create()`, `pthread_join()`, `sem_init()`, `sem_wait()`, `sem_post()`.\n- **Critical section protection:** Always wrap any access to `state[]` between `sem_wait(&mutex)` and `sem_post(&mutex)`.\n- **Neighbour formulas:** `LEFT = (i + 4) % N` and `RIGHT = (i + 1) % N`.\n- **The solution breaks Circular Wait**, thereby preventing deadlock without violating mutual exclusion.\n- **No two adjacent philosophers can eat simultaneously** — guaranteed by the `test()` check.\n- **Starvation** is theoretically possible in some edge cases; a fully starvation-free variant requires additional mechanisms (e.g., a queue of waiting philosophers).\n- **Sample viva questions:** *\"What happens if two neighbours call `put_forks()` simultaneously?\"* → mutex serialises them; only one enters the critical section at a time.\n- **Practical analogy:** Real-world examples include **database row-locks**, **printer spooling**, and **distributed transaction systems**.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "information-technology-5-pcitl408-mExperiment 2-t7",
          "title": "Implementation of banker's algorithm",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCITL408 - OPERATING SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Implementation of banker's algorithm\n\n<!-- SECTION_1_START -->\n# Module Experiment 2: Implementation of Banker's Algorithm\n\n## 1. Core Technical Definition & Intuitive Overview\n\n### Formal KTU Definition\nThe **Banker's Algorithm** is a classical **deadlock avoidance algorithm** proposed by **Edsger Dijkstra** in 1965. It is a resource allocation and deadlock avoidance algorithm designed to test for safety by simulating the allocation of predetermined maximum possible resources and making a safe-state check for all pending activities before deciding whether allocation should proceed.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Definition**\n> *The Banker's Algorithm is a deadlock avoidance technique that models a banking system where a bank never allocates its available cash in such a way that it can no longer satisfy the needs of all its customers. In operating systems, processes act as customers and resources act as the bank's capital.*\n\n### Core Terminology (KTU Mandatory)\n- **Safe State**: A state in which there exists **at least one safe sequence** of process execution where all processes can complete without deadlock.\n- **Safe Sequence**: An ordering of processes $\\langle P_1, P_2, P_3, \\dots, P_n \\rangle$ such that for each $P_i$, the resources that $P_i$ can still request can be satisfied by the currently available resources plus the resources held by all $P_j$ where $j < i$.\n- **Resource Allocation Graph**: Directed graph modeling allocation and request edges (used in single-instance resource systems).\n- **Deadlock**: A state where two or more processes are permanently blocked, each waiting for resources held by the other.\n\n### Conceptual Analogy: The Banking System\nImagine a bank with a fixed amount of cash. Multiple customers approach the bank requesting loans. Each customer declares a **maximum credit limit** upfront.\n\n> [!NOTE]\n> **Real-World Analogy: The Banker**\n> - The **Banker** = The Operating System (Resource Manager)\n> - **Customers** = Processes requesting resources\n> - **Cash** = System resources (RAM, I/O devices, file handles)\n> - **Maximum Credit Declared** = Maximum resource requirement (`Max`)\n> - **Money Already Borrowed** = Currently allocated resources (`Allocation`)\n> - **Still Needed (Credit Limit − Borrowed)** = Remaining need (`Need`)\n>\n> The banker (OS) must ensure that **at any point, after granting a loan, the bank still has enough cash to satisfy the maximum needs of all remaining customers in some order**. This guarantees the bank is never stuck — just like a deadlock-free OS.\n\nThe algorithm gets its name from this exact banking analogy where the banker distributes cash to customers such that the bank is always in a **\"safe state.\"**\n\n### Why Banker's Algorithm is Critical for KTU\n| Aspect | Significance |\n|---|---|\n| **Deadlock Prevention** | Avoids circular wait by validating each request before allocation |\n| **Conservative Nature** | May deny resource requests even when deadlock wouldn't actually occur |\n| **Pre-declaration** | Requires every process to declare its **maximum** resource requirement in advance |\n| **Fixed Resource Count** | Works best with a **fixed number of resources** like memory pages or tape drives |\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Core Data Structures Used\n\nThe Banker's Algorithm maintains **four critical data structures** for a system with $n$ processes and $m$ resource types:\n\n### Structure 1: `Available` Vector\nA **1-D vector of length $m$** indicating the number of available instances of each resource type.\n\n$$ \\text{Available}[j] = k \\quad \\text{means } k \\text{ instances of resource type } R_j \\text{ are available} $$\n\n### Structure 2: `Max` Matrix\nAn $n \\times m$ matrix defining the **maximum demand** of each process. `Max[i][j]` is the maximum number of instances of resource $R_j$ that process $P_i$ may request.\n\n### Structure 3: `Allocation` Matrix\nAn $n \\times m$ matrix defining the number of resources **currently allocated** to each process. `Allocation[i][j]` is the number of instances of $R_j$ currently held by $P_i$.\n\n### Structure 4: `Need` Matrix\nAn $n \\times m$ matrix indicating the **remaining resource need** of each process.\n\n$$ \\text{Need}[i][j] = \\text{Max}[i][j] - \\text{Allocation}[i][j] $$\n\n> [!IMPORTANT]\n> **KTU Board Insight**\n> The relationship `Max = Allocation + Need` is the **golden equation** of Banker's Algorithm. Examiners frequently test this single-line derivation for 2–3 marks.\n\n## 2.2 Algorithm 1: The Safety Algorithm\n\nThe Safety Algorithm determines whether the system is currently in a **safe state** by finding at least one safe sequence.\n\n### Step-by-Step Logic\n1. Initialize two working vectors:\n   - `Work` ← `Available` (a copy of available resources)\n   - `Finish[i]` ← `false` for all $i \\in \\{0, 1, \\dots, n-1\\}$\n\n2. Find an index $i$ such that both conditions hold:\n   - `Finish[i] == false`\n   - `Need[i] ≤ Work` (component-wise)\n\n3. If such $i$ is found:\n   - Simulate $P_i$ executing to completion:\n     $$ \\text{Work} = \\text{Work} + \\text{Allocation}[i] $$\n   - Set `Finish[i] ← true`\n   - **Go to Step 2**\n\n4. If no such $i$ exists:\n   - If `Finish[i] == true` for **all** $i$, the system is in a **safe state**.\n   - Otherwise, the system is in an **unsafe state** (deadlock may occur).\n\n> [!NOTE]\n> The safety algorithm runs in $O(m \\cdot n^2)$ time in the worst case because we may scan all $n$ processes for each of the $n$ processes in the safe sequence.\n\n## 2.3 Algorithm 2: The Resource Request Algorithm\n\nWhen process $P_i$ requests resources, the OS performs an immediate eligibility test:\n\n### Step-by-Step Logic\nLet `Request[i]` be the request vector from process $P_i$.\n\n**Step 1**: If `Request[i] ≤ Need[i]` (component-wise), proceed to Step 2. Otherwise, **error** (process exceeded its declared maximum).\n\n**Step 2**: If `Request[i] ≤ Available`, proceed to Step 3. Otherwise, $P_i$ must **wait** (resources not available).\n\n**Step 3**: Pretend to allocate the requested resources:\n$$ \\text{Available} = \\text{Available} - \\text{Request}[i] $$\n$$ \\text{Allocation}[i] = \\text{Allocation}[i] + \\text{Request}[i] $$\n$$ \\text{Need}[i] = \\text{Need}[i] - \\text{Request}[i] $$\n\n**Step 4**: Run the **Safety Algorithm** on the resulting state.\n- If **safe** → grant the resources permanently to $P_i$.\n- If **unsafe** → **roll back** the pretended allocation; $P_i$ must wait.\n\n## 2.4 KTU Formula Sheet / Cheat Sheet\n\n| Formula / Notation | Mathematical Form | Meaning | KTU Frequency |\n|---|---|---|---|\n| **Need Matrix** | $\\text{Need}[i][j] = \\text{Max}[i][j] - \\text{Allocation}[i][j]$ | Remaining resource need of $P_i$ | ⭐⭐⭐⭐⭐ |\n| **Pretend Allocation (Resources)** | $\\text{Available}_{\\text{new}} = \\text{Available}_{\\text{old}} - \\text{Request}[i]$ | Free resources after allocation | ⭐⭐⭐⭐⭐ |\n| **Pretend Allocation (Given)** | $\\text{Allocation}_{\\text{new}}[i] = \\text{Allocation}_{\\text{old}}[i] + \\text{Request}[i]$ | Resources given to $P_i$ | ⭐⭐⭐⭐⭐ |\n| **Resource Release** | $\\text{Work} = \\text{Work} + \\text{Allocation}[i]$ | Resources returned after $P_i$ finishes | ⭐⭐⭐⭐⭐ |\n| **Component-wise Comparison** | $\\text{Need}[i] \\leq \\text{Work} \\iff \\forall j, \\text{Need}[i][j] \\leq \\text{Work}[j]$ | Eligibility test condition | ⭐⭐⭐⭐ |\n| **Safe State Definition** | $\\exists \\text{ sequence } \\langle P_{\\sigma(1)}, \\dots, P_{\\sigma(n)} \\rangle$ such that each $P_{\\sigma(k)}$ completes with current `Work` | At least one safe ordering exists | ⭐⭐⭐⭐⭐ |\n| **Unsafe State Definition** | $\\nexists$ safe sequence | Deadlock is **possible** (not guaranteed) | ⭐⭐⭐⭐ |\n| **Total Resources Constraint** | $\\sum_{i} \\text{Allocation}[i][j] + \\text{Available}[j] = \\text{Total}[j]$ | Conservation of resources | ⭐⭐⭐ |\n| **Time Complexity** | $O(m \\cdot n^2)$ | Safety algorithm worst case | ⭐⭐ |\n| **Space Complexity** | $O(m \\cdot n)$ | Storage for matrices | ⭐⭐ |\n\n### Real-World Engineering Utility\n- **Database Lock Managers**: Oracle and PostgreSQL use variations of Banker's-style algorithms to manage concurrent transactions.\n- **Embedded Systems**: Real-time OS (RTOS) for avionics uses Banker's to guarantee no deadlock in flight control software.\n- **Cloud Resource Schedulers**: Kubernetes-like schedulers apply avoidance principles for CPU and memory allocation.\n- **Mainframe OS**: IBM z/OS uses a form of Banker's for managing tape drives and channel allocations.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Complete Worked Example (KTU Board Standard)\n\nConsider a system with **5 processes** ($P_0$ to $P_4$) and **3 resource types**:\n- $A$ (10 instances)\n- $B$ (5 instances)\n- $C$ (7 instances)\n\n### Given Snapshot at Time $T_0$\n\n**Allocation Matrix** (currently held):\n\n| Process | A | B | C |\n|:---:|:---:|:---:|:---:|\n| $P_0$ | 0 | 1 | 0 |\n| $P_1$ | 2 | 0 | 0 |\n| $P_2$ | 3 | 0 | 2 |\n| $P_3$ | 2 | 1 | 1 |\n| $P_4$ | 0 | 0 | 2 |\n\n**Max Matrix** (maximum demand):\n\n| Process | A | B | C |\n|:---:|:---:|:---:|:---:|\n| $P_0$ | 7 | 5 | 3 |\n| $P_1$ | 3 | 2 | 2 |\n| $P_2$ | 9 | 0 | 2 |\n| $P_3$ | 2 | 2 | 2 |\n| $P_4$ | 4 | 3 | 3 |\n\n### Step 1: Calculate Available Vector\n\n$$ \\text{Total} = (10, 5, 7) $$\n$$ \\text{Allocated sum} = (0+2+3+2+0, \\; 1+0+0+1+0, \\; 0+0+2+1+2) = (7, 2, 5) $$\n\n$$ \\text{Available} = \\text{Total} - \\text{Allocated sum} $$\n\n$$ \\text{Available} = (10 - 7, \\; 5 - 2, \\; 7 - 5) = (3, 3, 2) $$\n\n### Step 2: Derive Need Matrix (Using $\\text{Need}[i][j] = \\text{Max}[i][j] - \\text{Allocation}[i][j]$)\n\nFor $P_0$: $\\text{Need}[0] = (7-0, 5-1, 3-0) = (7, 4, 3)$\nFor $P_1$: $\\text{Need}[1] = (3-2, 2-0, 2-0) = (1, 2, 2)$\nFor $P_2$: $\\text{Need}[2] = (9-3, 0-0, 2-2) = (6, 0, 0)$\nFor $P_3$: $\\text{Need}[3] = (2-2, 2-1, 2-1) = (0, 1, 1)$\nFor $P_4$: $\\text{Need}[4] = (4-0, 3-0, 3-2) = (4, 3, 1)$\n\n**Final Need Matrix**:\n\n| Process | A | B | C |\n|:---:|:---:|:---:|:---:|\n| $P_0$ | 7 | 4 | 3 |\n| $P_1$ | 1 | 2 | 2 |\n| $P_2$ | 6 | 0 | 0 |\n| $P_3$ | 0 | 1 | 1 |\n| $P_4$ | 4 | 3 | 1 |\n\n### Step 3: Execute Safety Algorithm\n\nInitialize: $\\text{Work} = (3, 3, 2)$ and $\\text{Finish} = \\{\\text{false}, \\text{false}, \\text{false}, \\text{false}, \\text{false}\\}$\n\n**Iteration 1**: Search for process $i$ where `Finish[i] = false` AND `Need[i] ≤ Work`.\n\nCheck $P_0$: $(7,4,3) \\leq (3,3,2)$? → $7 > 3$ ❌\nCheck $P_1$: $(1,2,2) \\leq (3,3,2)$? → $1 \\leq 3, 2 \\leq 3, 2 \\leq 2$ ✅\n\n$P_1$ executes to completion:\n\n$$ \\text{Work} = \\text{Work} + \\text{Allocation}[1] $$\n$$ \\text{Work} = (3,3,2) + (2,0,0) = (5,3,2) $$\n\nSet $\\text{Finish}[1] = \\text{true}$. Safe sequence: $\\langle P_1 \\rangle$\n\n**Iteration 2**: $\\text{Work} = (5, 3, 2)$\n\nCheck $P_0$: $(7,4,3) \\leq (5,3,2)$? → $7 > 5$ ❌\nCheck $P_2$: $(6,0,0) \\leq (5,3,2)$? → $6 > 5$ ❌\nCheck $P_3$: $(0,1,1) \\leq (5,3,2)$? → $0 \\leq 5, 1 \\leq 3, 1 \\leq 2$ ✅\n\n$P_3$ executes to completion:\n\n$$ \\text{Work} = \\text{Work} + \\text{Allocation}[3] = (5,3,2) + (2,1,1) = (7,4,3) $$\n\nSet $\\text{Finish}[3] = \\text{true}$. Safe sequence: $\\langle P_1, P_3 \\rangle$\n\n**Iteration 3**: $\\text{Work} = (7, 4, 3)$\n\nCheck $P_0$: $(7,4,3) \\leq (7,4,3)$? → All equal ✅\n\n$P_0$ executes to completion:\n\n$$ \\text{Work} = \\text{Work} + \\text{Allocation}[0] = (7,4,3) + (0,1,0) = (7,5,3) $$\n\nSet $\\text{Finish}[0] = \\text{true}$. Safe sequence: $\\langle P_1, P_3, P_0 \\rangle$\n\n**Iteration 4**: $\\text{Work} = (7, 5, 3)$\n\nCheck $P_2$: $(6,0,0) \\leq (7,5,3)$? ✅\n\n$P_2$ executes:\n\n$$ \\text{Work} = (7,5,3) + (3,0,2) = (10,5,5) $$\n\nSafe sequence: $\\langle P_1, P_3, P_0, P_2 \\rangle$\n\n**Iteration 5**: $\\text{Work} = (10, 5, 5)$\n\nCheck $P_4$: $(4,3,1) \\leq (10,5,5)$? ✅\n\n$$ \\text{Work} = (10,5,5) + (0,0,2) = (10,5,7) $$\n\n### Step 4: Final Result\n\n$$\\boxed{\\text{Safe Sequence} = \\langle P_1, P_3, P_0, P_2, P_4 \\rangle}$$\n\nAll `Finish[i] = true` → System is in a **SAFE STATE** ✅\n\n> [!IMPORTANT]\n> **Note**: Safe sequences are **not unique**. An alternative valid sequence could be $\\langle P_1, P_3, P_0, P_4, P_2 \\rangle$. Any one valid safe sequence is sufficient to declare the system safe.\n\n## 3.2 Resource Request Worked Example\n\nNow suppose $P_1$ requests $\\text{Request}[1] = (1, 0, 2)$.\n\n### Step 1: Validate Request ≤ Need\n$$ (1, 0, 2) \\leq \\text{Need}[1] = (1, 2, 2)? \\;\\; \\checkmark $$\n\n### Step 2: Validate Request ≤ Available\n$$ (1, 0, 2) \\leq \\text{Available} = (3, 3, 2)? \\;\\; \\checkmark $$\n\n### Step 3: Pretend to Allocate\n$$ \\text{Available} = (3,3,2) - (1,0,2) = (2, 3, 0) $$\n$$ \\text{Allocation}[1] = (2,0,0) + (1,0,2) = (3, 0, 2) $$\n$$ \\text{Need}[1] = (1,2,2) - (1,0,2) = (0, 2, 0) $$\n\n### Step 4: Run Safety Algorithm\n\nUpdated Need Matrix:\n\n| Process | A | B | C |\n|:---:|:---:|:---:|:---:|\n| $P_0$ | 7 | 4 | 3 |\n| $P_1$ | 0 | 2 | 0 |\n| $P_2$ | 6 | 0 | 0 |\n| $P_3$ | 0 | 1 | 1 |\n| $P_4$ | 4 | 3 | 1 |\n\n$\\text{Work} = (2, 3, 0)$\n\nCheck $P_0$: $(7,4,3) \\leq (2,3,0)$? ❌\nCheck $P_1$: $(0,2,0) \\leq (2,3,0)$? ✅\n$$ \\text{Work} = (2,3,0) + (3,0,2) = (5, 3, 2) $$\n\nCheck $P_2$: $(6,0,0) \\leq (5,3,2)$? ❌\nCheck $P_3$: $(0,1,1) \\leq (5,3,2)$? ✅\n$$ \\text{Work} = (5,3,2) + (2,1,1) = (7, 4, 3) $$\n\nCheck $P_0$: $(7,4,3) \\leq (7,4,3)$? ✅\n$$ \\text{Work} = (7,4,3) + (0,1,0) = (7, 5, 3) $$\n\nCheck $P_2$: $(6,0,0) \\leq (7,5,3)$? ✅\n$$ \\text{Work} = (7,5,3) + (3,0,2) = (10, 5, 5) $$\n\nCheck $P_4$: $(4,3,1) \\leq (10,5,5)$? ✅\n\n$$\\boxed{\\text{Safe Sequence} = \\langle P_1, P_3, P_0, P_2, P_4 \\rangle \\;\\; \\Rightarrow \\;\\; \\text{Request GRANTED}}$$\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Safety Algorithm Flowchart (Detailed Block Architecture)\n\n```mermaid\nflowchart TD\n    startA([START Safety Algorithm]) --> initWork[\"Work := Available vector\"]\n    initWork --> initFinish[\"Finish array := false for all n processes\"]\n    initFinish --> findProc{\"Find process Pi such that Finish i = false AND Need i <= Work\"}\n    findProc -- \"Such i found\" --> executeProc[\"Pretend Pi executes and completes\"]\n    executeProc --> releaseRes[\"Work := Work + Allocation of Pi\"]\n    releaseRes --> setTrue[\"Finish i := true\"]\n    setTrue --> appendSeq[\"Append Pi to Safe Sequence\"]\n    appendSeq --> findProc\n    findProc -- \"No such i found\" --> checkAll{\"Is Finish i = true for ALL processes?\"}\n    checkAll -- \"YES\" --> safeState[\"Output SAFE STATE and print Safe Sequence\"]\n    checkAll -- \"NO\" --> unsafeState[\"Output UNSAFE STATE Deadlock possible\"]\n    safeState --> endA([END])\n    unsafeState --> endA\n\n    style startA fill:#90EE90,stroke:#006400,color:#000\n    style endA fill:#FFB6C1,stroke:#8B0000,color:#000\n    style safeState fill:#90EE90,stroke:#006400,color:#000\n    style unsafeState fill:#FFB6C1,stroke:#8B0000,color:#000\n    style findProc fill:#FFE4B5,stroke:#8B4513,color:#000\n    style checkAll fill:#FFE4B5,stroke:#8B4513,color:#000\n```\n\n## 4.2 Resource Request Algorithm Flowchart\n\n```mermaid\nflowchart TD\n    startB([START Resource Request from Pi]) --> recvReq[\"Receive Request vector from Pi\"]\n    recvReq --> checkNeed{\"Is Request i less than or equal to Need i component wise?\"}\n    checkNeed -- \"NO\" --> errMax[\"ERROR: Pi exceeded its declared maximum\"]\n    checkNeed -- \"YES\" --> checkAvail{\"Is Request i less than or equal to Available?\"}\n    checkAvail -- \"NO\" --> waitProc[\"Pi must WAIT resources not free\"]\n    checkAvail -- \"YES\" --> pretend[\"Pretend Allocation: Available minus Request; Allocation plus Request; Need minus Request\"]\n    pretend --> runSafe[\"Run SAFETY ALGORITHM on new state\"]\n    runSafe --> checkResult{\"Is resulting state SAFE?\"}\n    checkResult -- \"YES\" --> grantRes[\"GRANT resources to Pi permanently\"]\n    checkResult -- \"NO\" --> rollback[\"ROLL BACK pretended allocation Pi must WAIT\"]\n    grantRes --> endB([END])\n    rollback --> endB\n    waitProc --> endB\n    errMax --> endB\n\n    style startB fill:#90EE90,stroke:#006400,color:#000\n    style endB fill:#FFB6C1,stroke:#8B0000,color:#000\n    style grantRes fill:#90EE90,stroke:#006400,color:#000\n    style rollback fill:#FFB6C1,stroke:#8B0000,color:#000\n    style waitProc fill:#FFD700,stroke:#B8860B,color:#000\n    style errMax fill:#FFB6C1,stroke:#8B0000,color:#000\n    style checkNeed fill:#FFE4B5,stroke:#8B4513,color:#000\n    style checkAvail fill:#FFE4B5,stroke:#8B4513,color:#000\n    style checkResult fill:#FFE4B5,stroke:#8B4513,color:#000\n```\n\n## 4.3 Process Lifecycle in Banker's Algorithm (State Diagram)\n\n```mermaid\nstateDiagram-v2\n    [*] --> Requesting : Pi requests resources\n    Requesting --> Waiting : Resources not available\n    Waiting --> Requesting : Resources become free\n    Requesting --> Validating : Request received by OS\n    Validating --> ErrorState : Request exceeds Need\n    ErrorState --> [*] : Process terminated\n    Validating --> Pretending : Request within limits\n    Pretending --> UnsafeRollback : Safety check fails\n    UnsafeRollback --> Waiting : Pi waits\n    Pretending --> Running : Safety check passes\n    Running --> Completed : Pi finishes execution\n    Completed --> Releasing : Release all allocated resources\n    Releasing --> [*] : Process terminated\n\n    note right of Validating : Two tests: Request vs Need and Request vs Available\n    note left of Pretending : Modify Available Allocation and Need matrices\n```\n\n> [!NOTE]\n> **Diagram Interpretation Guide**\n> - **Green nodes** = success/safe transitions\n> - **Red nodes** = failure/unsafe/error transitions\n> - **Yellow/Amber nodes** = decision diamonds requiring component-wise vector comparisons\n> - **Beige nodes** = internal computational state\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## Part A: Short Answer Questions (3 Marks Each)\n\n### Question 1 `[KTU University Exam – June 2024]`\n**Define the Banker's Algorithm. Explain any two data structures used by it.**\n\n**Model Answer (3 Marks):**\n\nThe **Banker's Algorithm** is a deadlock avoidance algorithm proposed by Edsger Dijkstra. It ensures that the system never enters an unsafe state by simulating resource allocation before granting requests.\n\n**Two Key Data Structures:**\n\n1. **Max Matrix** ($n \\times m$): Defines the maximum number of resource instances each process may request. `Max[i][j] = k` means process $P_i$ may request at most $k$ instances of resource type $R_j$.\n\n2. **Allocation Matrix** ($n \\times m$): Defines the number of resource instances currently allocated to each process. `Allocation[i][j] = k` means $P_i$ is currently holding $k$ instances of $R_j$.\n\n3. *(Optional third for extra credit)* **Need Matrix** is computed as `Need[i][j] = Max[i][j] - Allocation[i][j]` representing the remaining resource need of each process.\n\n---\n\n### Question 2 `[KTU University Exam – Dec 2023]`\n**Differentiate between Safe State and Unsafe State with a suitable example.**\n\n**Model Answer (3 Marks):**\n\n| Aspect | Safe State | Unsafe State |\n|---|---|---|\n| **Definition** | A state where at least one safe sequence of process execution exists | A state where **no** safe sequence exists |\n| **Deadlock Possibility** | Deadlock is **impossible** | Deadlock is **possible** |\n| **Resources** | Can satisfy max needs of all processes in some order | May not be able to satisfy all processes |\n| **Example** | If Available $(3,3,2)$ and all processes can be ordered to complete | If all processes simultaneously need resources held by others |\n\n**Example**: In the previous worked example, system is in a safe state because sequence $\\langle P_1, P_3, P_0, P_2, P_4 \\rangle$ allows all processes to complete. If we change allocation such that no ordering works, the state becomes unsafe.\n\n---\n\n## Part B: Long Answer Questions (14 Marks with Internal Choice)\n\n### Question A (14 Marks) `[KTU University Exam – June 2024 Model Paper]`\n\n**(a)** Consider a system with **5 processes** $P_0$ to $P_4$ and **3 resource types** $A$, $B$, $C$ with total resources $(10, 5, 7)$. The current state is as follows:\n\n| Process | Allocation (A B C) | Max (A B C) |\n|:---:|:---:|:---:|\n| $P_0$ | 0 1 0 | 7 5 3 |\n| $P_1$ | 2 0 0 | 3 2 2 |\n| $P_2$ | 3 0 2 | 9 0 2 |\n| $P_3$ | 2 1 1 | 2 2 2 |\n| $P_4$ | 0 0 2 | 4 3 3 |\n\n(i) Calculate the **Need Matrix**. **(3 marks)**\n(ii) Determine if the system is in a **safe state** using Banker's Algorithm. If yes, provide the safe sequence. **(7 marks)**\n\n#### Model Solution:\n\n**Part (i) – Need Matrix Calculation (3 marks):**\n\nApply formula $\\text{Need}[i][j] = \\text{Max}[i][j] - \\text{Allocation}[i][j]$ for all 15 entries.\n\n| Process | Need (A B C) |\n|:---:|:---:|\n| $P_0$ | (7-0, 5-1, 3-0) = **(7, 4, 3)** |\n| $P_1$ | (3-2, 2-0, 2-0) = **(1, 2, 2)** |\n| $P_2$ | (9-3, 0-0, 2-2) = **(6, 0, 0)** |\n| $P_3$ | (2-2, 2-1, 2-1) = **(0, 1, 1)** |\n| $P_4$ | (4-0, 3-0, 3-2) = **(4, 3, 1)** |\n\n**Part (ii) – Safety Algorithm Execution (7 marks):**\n\nStep 1: Calculate Available\n$$\\text{Available} = (10,5,7) - (7,2,5) = (3,3,2)$$\n\nStep 2: $\\text{Work} = (3,3,2)$\n\n**Iteration 1**: $P_1$: Need $(1,2,2) \\leq$ Work $(3,3,2)$ ✅\n$$\\text{Work} = (3,3,2) + (2,0,0) = (5,3,2)$$\n\n**Iteration 2**: $P_3$: Need $(0,1,1) \\leq$ Work $(5,3,2)$ ✅\n$$\\text{Work} = (5,3,2) + (2,1,1) = (7,4,3)$$\n\n**Iteration 3**: $P_0$: Need $(7,4,3) \\leq$ Work $(7,4,3)$ ✅\n$$\\text{Work} = (7,4,3) + (0,1,0) = (7,5,3)$$\n\n**Iteration 4**: $P_2$: Need $(6,0,0) \\leq$ Work $(7,5,3)$ ✅\n$$\\text{Work} = (7,5,3) + (3,0,2) = (10,5,5)$$\n\n**Iteration 5**: $P_4$: Need $(4,3,1) \\leq$ Work $(10,5,5)$ ✅\n$$\\text{Work} = (10,5,5) + (0,0,2) = (10,5,7)$$\n\n**Result**: All `Finish[i] = true` → **SAFE STATE**\n$$\\boxed{\\text{Safe Sequence} = \\langle P_1, P_3, P_0, P_2, P_4 \\rangle}$$\n\n**(b)** If process $P_4$ requests **(0, 3, 0)**, can the request be immediately granted using the Resource Request Algorithm? Show all steps. **(4 marks)**\n\n#### Model Solution:\n\n**Step 1**: Check `Request[4] ≤ Need[4]`\n$$\\text{Request}[4] = (0,3,0) \\leq \\text{Need}[4] = (4,3,1) \\;\\; \\checkmark \\text{ OK}$$\n\n**Step 2**: Check `Request[4] ≤ Available`\n$$\\text{Request}[4] = (0,3,0) \\leq \\text{Available} = (3,3,2) \\;\\; \\checkmark \\text{ OK}$$\n\n**Step 3**: Pretend allocation:\n$$\\text{Available}_{\\text{new}} = (3,3,2) - (0,3,0) = (3,0,2)$$\n$$\\text{Allocation}[4]_{\\text{new}} = (0,0,2) + (0,3,0) = (0,3,2)$$\n$$\\text{Need}[4]_{\\text{new}} = (4,3,1) - (0,3,0) = (4,0,1)$$\n\n**Step 4**: Run safety algorithm with $\\text{Work} = (3,0,2)$:\n- $P_1$: $(1,2,2) \\leq (3,0,2)$? → $2 > 0$ ❌\n- $P_3$: $(0,1,1) \\leq (3,0,2)$? → $1 > 0$ ❌\n- $P_4$: $(4,0,1) \\leq (3,0,2)$? → $4 > 3$ ❌\n- $P_0$: $(7,4,3) \\leq (3,0,2)$? ❌\n- $P_2$: $(6,0,0) \\leq (3,0,2)$? → $6 > 3$ ❌\n\n**Result**: No process can proceed → **UNSAFE STATE** → Request is **DENIED** ❌. $P_4$ must **wait**.\n\n---\n\n### Question B (14 Marks) `[KTU University Exam – Dec 2023]`\n\n**(a)** Explain the **Resource Request Algorithm** with its complete procedural steps. When is a request denied by the OS? **(7 marks)**\n\n#### Model Answer:\n\nWhen process $P_i$ makes a request `Request[i]`, the OS performs these steps:\n\n**Step 1: Verify against declared maximum**\nIf $\\text{Request}[i] \\leq \\text{Need}[i]$, proceed. Else, raise an **error** (process declared wrong maximum).\n\n**Step 2: Verify against available resources**\nIf $\\text{Request}[i] \\leq \\text{Available}$, proceed. Else, $P_i$ must **wait**.\n\n**Step 3: Pretend to allocate**\n$$\\text{Available} \\leftarrow \\text{Available} - \\text{Request}[i]$$\n$$\\text{Allocation}[i] \\leftarrow \\text{Allocation}[i] + \\text{Request}[i]$$\n$$\\text{Need}[i] \\leftarrow \\text{Need}[i] - \\text{Request}[i]$$\n\n**Step 4: Safety check**\nRun the Safety Algorithm. If safe → grant; else → **roll back** and $P_i$ must wait.\n\n**When request is denied:**\nA request is **denied** (or $P_i$ is made to wait) in **two cases**:\n1. `Request[i] > Available` (resources not currently free)\n2. Pretend allocation results in an **unsafe state** (potential deadlock detected)\n\n**(b)** A system has 4 processes $P_0$–$P_3$ and 3 resource types $A$, $B$, $C$ with totals $(6, 7, 8)$. The Max and Allocation matrices are:\n\n| Process | Max (A B C) | Allocation (A B C) |\n|:---:|:---:|:---:|\n| $P_0$ | 5 4 6 | 1 1 2 |\n| $P_1$ | 3 2 1 | 2 1 0 |\n| $P_2$ | 4 3 4 | 2 2 1 |\n| $P_3$ | 2 1 2 | 0 1 1 |\n\nCompute the Need matrix and Available vector. Is the system in a safe state? **(7 marks)**\n\n#### Model Solution:\n\n**Need Matrix**: Apply `Need = Max - Allocation`\n\n| Process | Need (A B C) |\n|:---:|:---:|\n| $P_0$ | (4, 3, 4) |\n| $P_1$ | (1, 1, 1) |\n| $P_2$ | (2, 1, 3) |\n| $P_3$ | (2, 0, 1) |\n\n**Available Vector**:\n$$\\text{Allocated sum} = (1+2+2+0, \\; 1+1+2+1, \\; 2+0+1+1) = (5, 5, 4)$$\n$$\\text{Available} = (6,7,8) - (5,5,4) = (1, 2, 4)$$\n\n**Safety Algorithm**: $\\text{Work} = (1, 2, 4)$\n\n- $P_0$: $(4,3,4) \\leq (1,2,4)$? ❌\n- $P_1$: $(1,1,1) \\leq (1,2,4)$? ✅ → $\\text{Work} = (1,2,4) + (2,1,0) = (3,3,4)$\n- $P_2$: $(2,1,3) \\leq (3,3,4)$? ✅ → $\\text{Work} = (3,3,4) + (2,2,1) = (5,5,5)$\n- $P_3$: $(2,0,1) \\leq (5,5,5)$? ✅ → $\\text{Work} = (5,5,5) + (0,1,1) = (5,6,6)$\n- $P_0$: $(4,3,4) \\leq (5,6,6)$? ✅ → $\\text{Work} = (5,6,6) + (1,1,2) = (6,7,8)$\n\n$$\\boxed{\\text{Safe Sequence} = \\langle P_1, P_2, P_3, P_0 \\rangle \\;\\; \\Rightarrow \\;\\; \\text{SAFE STATE}}$$\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning – Common Pitfalls**\n> \n> 1. **Forgetting to update Available after process completion**: Many students execute the safety algorithm but forget to add `Allocation[i]` to `Work` after each process. **You lose 2 marks per iteration.**\n> \n> 2. **Confusing Need with Allocation**: When asked for \"remaining need,\" students often write the current `Allocation` instead of `Max - Allocation`. **Verification equation**: `Allocation + Need = Max` for every process.\n> \n> 3. **Component-wise vs Element-wise comparison**: The comparison `Need[i] ≤ Work` must be done **component-wise** (all $m$ components). A single component exceeding causes failure. **Partial marks only if you state this clearly.**\n> \n> 4. **Wrong Available calculation**: Available = Total − Sum of all Allocations (row-wise sum), NOT the diagonal of Max. **Show the row-wise sum explicitly.**\n> \n> 5. **Unsafe ≠ Deadlock**: An unsafe state means deadlock is **possible**, not **guaranteed**. Examiners test this distinction for 2 marks.\n> \n> 6. **Missing the Rollback step in Resource Request**: When safety check fails after pretend allocation, you **must explicitly state \"rollback and P_i waits\"**, otherwise you lose 1 mark.\n\n---\n\n## Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid Revision Checklist for KTU Exam**\n\n### Core Concepts\n- ✅ Banker's Algorithm is a **deadlock avoidance** technique (NOT prevention or detection)\n- ✅ Designed by **Edsger Dijkstra** in 1965\n- ✅ Requires processes to **declare maximum resource need upfront**\n- ✅ System must maintain a **safe state** at all times\n\n### The Three Golden Matrices\n- ✅ **Max**: Maximum demand per process\n- ✅ **Allocation**: Currently held resources\n- ✅ **Need** = Max − Allocation (always)\n\n### Safety Algorithm Key Points\n- ✅ Initialize `Work = Available` and `Finish[i] = false`\n- ✅ Find process where `Need[i] ≤ Work`\n- ✅ Add its `Allocation` to `Work` and mark finished\n- ✅ If all `Finish[i] = true` → SAFE; else UNSAFE\n\n### Resource Request Algorithm\n- ✅ Two validation checks: `Request ≤ Need` AND `Request ≤ Available`\n- ✅ Always run Safety Algorithm after pretended allocation\n- ✅ Rollback if unsafe (do NOT commit allocation)\n\n### Common Formulas to Memorize\n- ✅ `Need = Max − Allocation`\n- ✅ `Available = Total − Σ(Allocation[i])` for each resource type\n- ✅ `Work_{new} = Work_{old} + Allocation[i]` when process finishes\n- ✅ Component-wise comparison: $\\forall j, \\text{Need}[i][j] \\leq \\text{Work}[j]$\n\n### Exam Strategy Tips\n- ✅ Always show the **Need matrix calculation first** (3 marks guaranteed)\n- ✅ Then calculate **Available vector** with explicit row sums\n- ✅ Maintain a **Work column** in your safety algorithm table for clarity\n- ✅ State the **safe sequence** at the end in boxed notation: $\\langle P_x, P_y, P_z \\rangle$\n- ✅ For unsafe state, just state \"No process can complete → UNSAFE\"\n\n### Limitations to Note (Theory Questions)\n- ✅ Requires **fixed number of processes** (no dynamic creation)\n- ✅ Requires processes to declare **maximum need in advance** (often unrealistic)\n- ✅ Assumes **discrete resource allocation** (not preemptive)\n- ✅ Time complexity $O(m \\cdot n^2)$ can be slow for large systems\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "information-technology-5-pcitl408-mExperiment 2-t8",
          "title": "Implement memory management schemes (first fit, best fit and worst fit)",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCITL408 - OPERATING SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Implement memory management schemes (first fit, best fit and worst fit)\n\n<!-- SECTION_1_START -->\n## SECTION 1 — Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n> [!IMPORTANT]\n> **Memory Management (Contiguous Allocation)** is the mechanism by which the Operating System allocates a single, continuous region of physical memory (RAM) to each process. The OS maintains a list of **free memory blocks (holes)** and uses an allocation strategy to satisfy process requests.\n\nThe three classic **contiguous memory allocation strategies** evaluated in this experiment are:\n\n| Strategy | Allocation Rule |\n|---|---|\n| **First Fit** | Allocate the *first* free block whose size is **greater than or equal to** the requested size. |\n| **Best Fit** | Allocate the *smallest* free block whose size is sufficient to satisfy the request. |\n| **Worst Fit** | Allocate the *largest* free block available, leaving a large remainder. |\n\nFormally, given a request of size `R` and a set of free holes `H = {h₁, h₂, h₃, ...}`:\n\n$$\n\\text{Chosen Block} =\n\\begin{cases}\n\\min\\{i : h_i \\geq R\\} & \\text{(First Fit)} \\\\\n\\arg\\min_{h_i \\geq R} (h_i - R) & \\text{(Best Fit)} \\\\\n\\arg\\max_{i} (h_i) \\text{ such that } h_i \\geq R & \\text{(Worst Fit)} \\\\\n\\text{NULL} & \\text{if no block satisfies } h_i \\geq R\n\\end{cases}\n$$\n\n### 1.2 Conceptual Analogy — \"The Hotel Room Manager\"\n\n> [!NOTE]\n> **Real-world Analogy (Hotel Check-in):**\n> Imagine you are a hotel receptionist with rooms of varying sizes: 100, 300, 250, 500, and 150 sq.ft. A guest arrives needing 200 sq.ft.\n>\n> - **First Fit:** Give them the *first* room (100) that fits? No, too small. The next is 300 — give it! You don't care about leftovers.\n> - **Best Fit:** Scan all rooms. The smallest one that fits 200 is 250 — perfect, minimal wastage.\n> - **Worst Fit:** Give them the *biggest* room (500), so the leftover space (300) is still large enough for future big guests.\n\nThis hotel scenario is exactly how the OS places process blocks into RAM holes — and just like the hotel, **fragmentation** (wasted space) is the critical trade-off.\n\n### 1.3 Fragmentation — The Two Enemies\n\n> [!IMPORTANT]\n> **Two Types of Fragmentation:**\n> - **Internal Fragmentation:** Memory allocated to a process but *not actually used* by it (wasted inside the allocated block). Occurs when allocated block > requested size. Measured in **bytes**.\n> - **External Fragmentation:** Total free memory is large enough to satisfy a request, but it is scattered as *non-contiguous small holes*, so allocation fails. Measured as a **percentage**.\n\n$$\n\\text{External Fragmentation} (\\%) = \\frac{\\text{Sum of all free holes smaller than request}}{\\text{Total free memory}} \\times 100\n$$\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## SECTION 2 — Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Algorithmic Logic Steps\n\n#### 🟢 A. First Fit Algorithm\n1. Traverse the free-block list from the **beginning** (or from the last allocated position).\n2. For each hole `h_i`, check if `h_i ≥ Request`.\n3. The **first** such hole is selected.\n4. Split it: allocate `Request` bytes, and create a new smaller hole of size `(h_i − Request)` if the remainder is > 0.\n5. If no hole satisfies the condition, allocation fails.\n\n**Why it is fast:** Time complexity is **O(n)** in the worst case, but on average it is the fastest because scanning stops at the first match.\n\n#### 🔵 B. Best Fit Algorithm\n1. Traverse the **entire** free-block list.\n2. Track the hole with the **minimum wastage** `(h_i − Request)` where `h_i ≥ Request`.\n3. Allocate from that optimal hole.\n4. Worst-case time complexity is **O(n)** — *always* scans the full list.\n\n**Why it minimizes wastage (in theory):** Leaves the smallest possible leftover. However, it produces *many tiny unusable holes* — increasing **external fragmentation** paradoxically.\n\n#### 🔴 C. Worst Fit Algorithm\n1. Traverse the **entire** free-block list.\n2. Select the **largest** hole available.\n3. Allocate, leaving a remainder that is itself still large.\n4. Worst-case time complexity is **O(n)**.\n\n**Why it is used:** Tries to keep the remaining holes large enough for future requests, reducing small-hole accumulation.\n\n### 2.2 KTU Formula Cheat Sheet\n\n> [!NOTE]\n> Use this table as the **last-page reference** during revision and for solving numerical problems in Part B.\n\n| # | Quantity | Formula | Units / Notes |\n|---|---|---|---|\n| 1 | Wasted space (Best Fit) | $W_{bf} = h_{selected} - R$ | bytes |\n| 2 | Wasted space (Worst Fit) | $W_{wf} = h_{max} - R$ | bytes |\n| 3 | Internal Fragmentation (per block) | $IF_i = \\text{Block}_i - \\text{Process}_i$ | bytes |\n| 4 | Total Internal Fragmentation | $IF_{total} = \\sum_{i=1}^{n} IF_i$ | bytes |\n| 5 | External Fragmentation | $EF = \\dfrac{\\sum \\text{small free holes}}{\\text{Total free memory}} \\times 100$ | % |\n| 6 | Memory Utilization | $U = \\dfrac{\\sum \\text{Allocated}}{\\text{Total RAM}} \\times 100$ | % |\n| 7 | Time Complexity (all three) | $O(n)$ | where `n` = number of free holes |\n| 8 | Request success condition | $h_{selected} \\geq R$ | boolean |\n\n> [!WARNING]\n> **Mark-loss Pitfall:** KTU examiners **deduct marks** if students confuse the *Worst Fit* selected block (largest hole) with the *Best Fit* selected block (smallest fitting hole). Memorize the keyword: **Best = Smallest, Worst = Largest.**\n\n### 2.3 Real-World Engineering Utility\n\n- **Compilers & Linkers:** Use Best Fit to pack code segments efficiently in embedded firmware.\n- **Database Buffer Pools:** Use First Fit for low-latency memory allocation.\n- **Cloud Virtualization (KVM, VMware):** Use Best Fit variants to pack virtual machines into physical hosts, maximizing density.\n- **Heap Allocators (`malloc`):** The C library `glibc` uses a combination of strategies (bins of size classes — a hybrid of Best Fit and segregated lists).\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## SECTION 3 — Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Comprehensive Worked Example (All Three Strategies)\n\n> [!IMPORTANT]\n> **Problem Statement (Classic KTU-style):**\n> Consider a system with **5 free memory holes** of sizes (in KB):\n> `H = {100, 500, 200, 300, 600}`\n> Process requests arrive in order:\n> `P1 = 212 KB, P2 = 417 KB, P3 = 112 KB, P4 = 426 KB`\n\nWe will trace **all three strategies** step by step.\n\n#### 🟢 FIRST FIT Trace\n\n| Step | Request | Holes before | Scanned Blocks | Allocated Hole | Hole Remaining | Notes |\n|---|---|---|---|---|---|---|\n| 1 | P1 = 212 | `{100, 500, 200, 300, 600}` | 100→fail, 500→**ok** | 500 | 500−212 = **288** | 1st success wins |\n| 2 | P2 = 417 | `{100, 288, 200, 300, 600}` | 100→fail, 288→fail, 200→fail, 300→fail, 600→**ok** | 600 | 600−417 = **183** | |\n| 3 | P3 = 112 | `{100, 288, 200, 300, 183}` | 100→fail, 288→**ok** | 288 | 288−112 = **176** | |\n| 4 | P4 = 426 | `{100, 176, 200, 300, 183}` | 100→fail, 176→fail, 200→fail, 300→fail, 183→fail | **FAIL** | — | No block ≥ 426 |\n\n**Final First Fit holes:** `{100, 176, 200, 300, 183}` → Total free = **959 KB**, but **P4 cannot be allocated** (external fragmentation).\n\n#### 🔵 BEST FIT Trace\n\n| Step | Request | Holes before | Considered blocks (≥ Req) | Selected (min) | Hole Remaining |\n|---|---|---|---|---|---|\n| 1 | P1 = 212 | `{100, 500, 200, 300, 600}` | 500, 300, 600 | **300** (smallest ≥ 212) | 300−212 = **88** |\n| 2 | P2 = 417 | `{100, 500, 200, 88, 600}` | 500, 600 | **500** (smallest ≥ 417) | 500−417 = **83** |\n| 3 | P3 = 112 | `{100, 83, 200, 88, 600}` | 200, 600 | **200** (smallest ≥ 112) | 200−112 = **88** |\n| 4 | P4 = 426 | `{100, 83, 88, 88, 600}` | 600 only | **600** | 600−426 = **174** |\n\n**Final Best Fit holes:** `{100, 83, 88, 88, 174}` → Total free = **533 KB**, with many tiny unusable holes.\n\n#### 🔴 WORST FIT Trace\n\n| Step | Request | Holes before | Selected (max) | Hole Remaining |\n|---|---|---|---|---|\n| 1 | P1 = 212 | `{100, 500, 200, 300, 600}` | **600** | 600−212 = **388** |\n| 2 | P2 = 417 | `{100, 500, 200, 300, 388}` | **500** (largest) | 500−417 = **83** |\n| 3 | P3 = 112 | `{100, 83, 200, 300, 388}` | **388** | 388−112 = **276** |\n| 4 | P4 = 426 | `{100, 83, 200, 300, 276}` | **300** (largest ≥ 426 fails) | **FAIL** |\n\n**Final Worst Fit holes:** `{100, 83, 200, 300, 276}` → P4 fails because no hole ≥ 426 remains.\n\n### 3.2 Comparative Analysis Matrix\n\n> [!NOTE]\n> This table is a **favorite KTU question** — examiners ask: *\"Compare First, Best, and Worst Fit.\"*\n\n| Property | First Fit | Best Fit | Worst Fit |\n|---|---|---|---|\n| **Selection rule** | First hole big enough | Smallest fitting hole | Largest hole |\n| **Scan time** | May stop early (fastest) | Always full scan | Always full scan |\n| **Speed** | ⭐⭐⭐ Fastest | ⭐⭐ Medium | ⭐ Slowest |\n| **Internal Fragmentation** | Moderate | ⭐ Lowest (least waste) | Highest |\n| **External Fragmentation** | Moderate | ⭐ Highest (tiny holes) | Lowest |\n| **Memory Utilization** | Good (60–70% typical) | Variable | Best for big processes |\n| **Algorithm complexity** | $O(n)$ avg, faster | $O(n)$ | $O(n)$ |\n| **Practical usage** | Most OS (Linux buddy hybrid) | Compilers, packers | Rarely used alone |\n| **Result on P4 (426 KB)** | FAIL | ✅ SUCCESS | FAIL |\n\n### 3.3 Reference C Program Skeleton (Lab Exam Expectation)\n\n> [!IMPORTANT]\n> KTU OS Lab examiners expect students to **write a menu-driven C program** implementing all three strategies. The structure below is the **gold-standard template** taught in KTU 2024 scheme labs.\n\n```c\n#include <stdio.h>\n\n#define MAX 25\n\nvoid firstFit(int blockSize[], int m, int processSize[], int n) {\n    int allocation[n], i, j;\n    for (i = 0; i < n; i++) allocation[i] = -1;\n\n    for (i = 0; i < n; i++) {\n        for (j = 0; j < m; j++) {\n            if (blockSize[j] >= processSize[i]) {\n                allocation[i] = j;\n                blockSize[j] -= processSize[i];\n                break;          // KEY: break makes it FIRST FIT\n            }\n        }\n    }\n\n    printf(\"\\n--- First Fit Allocation ---\");\n    printf(\"\\nProcess No.\\tProcess Size\\tBlock No.\");\n    for (i = 0; i < n; i++) {\n        printf(\"\\n%d\\t\\t%d\\t\\t%s\",\n               i + 1, processSize[i],\n               allocation[i] != -1 ? \" (unused fallback)\" : \"Not Allocated\");\n    }\n}\n\nvoid bestFit(int blockSize[], int m, int processSize[], int n) {\n    int allocation[n], i, j, bestIdx;\n    for (i = 0; i < n; i++) allocation[i] = -1;\n\n    for (i = 0; i < n; i++) {\n        bestIdx = -1;\n        for (j = 0; j < m; j++) {\n            if (blockSize[j] >= processSize[i]) {\n                if (bestIdx == -1 || blockSize[j] < blockSize[bestIdx])\n                    bestIdx = j;            // KEY: pick SMALLEST fitting\n            }\n        }\n        if (bestIdx != -1) {\n            allocation[i] = bestIdx;\n            blockSize[bestIdx] -= processSize[i];\n        }\n    }\n    // ... print allocation table\n}\n\nvoid worstFit(int blockSize[], int m, int processSize[], int n) {\n    int allocation[n], i, j, worstIdx;\n    for (i = 0; i < n; i++) allocation[i] = -1;\n\n    for (i = 0; i < n; i++) {\n        worstIdx = -1;\n        for (j = 0; j < m; j++) {\n            if (blockSize[j] >= processSize[i]) {\n                if (worstIdx == -1 || blockSize[j] > blockSize[worstIdx])\n                    worstIdx = j;           // KEY: pick LARGEST fitting\n            }\n        }\n        if (worstIdx != -1) {\n            allocation[i] = worstIdx;\n            blockSize[worstIdx] -= processSize[i];\n        }\n    }\n    // ... print allocation table\n}\n\nint main() {\n    int blockSize[]   = {100, 500, 200, 300, 600};\n    int processSize[] = {212, 417, 112, 426};\n    int m = 5, n = 4, choice;\n    int b1[5] = {100,500,200,300,600};\n    int b2[5] = {100,500,200,300,600};\n    int b3[5] = {100,500,200,300,600};\n\n    printf(\"1. First Fit  2. Best Fit  3. Worst Fit\\nChoice: \");\n    scanf(\"%d\", &choice);\n    switch (choice) {\n        case 1: firstFit(b1, m, processSize, n); break;\n        case 2: bestFit(b2, m, processSize, n);  break;\n        case 3: worstFit(b3, m, processSize, n); break;\n    }\n    return 0;\n}\n```\n\n**Three One-Line Differences to Memorize:**\n\n| Strategy | Inner Loop Termination | Selection Logic |\n|---|---|---|\n| First Fit | `break;` on first match | Stop scanning |\n| Best Fit | `blockSize[j] < blockSize[bestIdx]` | Minimize remainder |\n| Worst Fit | `blockSize[j] > blockSize[worstIdx]` | Maximize remainder |\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## SECTION 4 — Structural Diagrams & Schematics\n\n### 4.1 Algorithm Comparison — Mermaid Flowchart\n\n```mermaid\nflowchart TD\n    Start([Process Request R arrives]) --> Init[Initialize all blocks to free]\n\n    Init --> Strategy{Choose Strategy}\n\n    Strategy -->|First Fit| FF1[Set flag = NOT_FOUND<br>pointer p = block 0]\n    FF1 --> FF2{p < n AND flag = NOT_FOUND?}\n    FF2 -->|No| FFout[STOP - Allocation complete]\n    FF2 -->|Yes| FF3{Block p size >= R?}\n    FF3 -->|No| FF4[p = p + 1]\n    FF4 --> FF2\n    FF3 -->|Yes| FF5[Allocate Block p to R<br>block p = block p - R<br>flag = FOUND]\n    FF5 --> FFout\n\n    Strategy -->|Best Fit| BF1[best = -1<br>bestSize = INFINITY]\n    BF1 --> BF2{p < n?}\n    BF2 -->|No| BF3{best = -1?}\n    BF3 -->|Yes| BFfail[STOP - Allocation FAILED]\n    BF3 -->|No| BF4[Allocate block best to R<br>block best = block best - R]\n    BF4 --> BFout2[STOP - Allocation complete]\n    BF2 -->|Yes| BF5{block p size >= R AND<br>block p size < bestSize?}\n    BF5 -->|No| BF6[p = p + 1]\n    BF6 --> BF2\n    BF5 -->|Yes| BF7[best = p<br>bestSize = block p size]\n    BF7 --> BF6\n\n    Strategy -->|Worst Fit| WF1[worst = -1<br>worstSize = 0]\n    WF1 --> WF2{p < n?}\n    WF2 -->|No| WF3{worst = -1?}\n    WF3 -->|Yes| WFfail[STOP - Allocation FAILED]\n    WF3 -->|No| WF4[Allocate block worst to R<br>block worst = block worst - R]\n    WF4 --> WFout3[STOP - Allocation complete]\n    WF2 -->|Yes| WF5{block p size >= R AND<br>block p size > worstSize?}\n    WF5 -->|No| WF6[p = p + 1]\n    WF6 --> WF2\n    WF5 -->|Yes| WF7[worst = p<br>worstSize = block p size]\n    WF7 --> WF6\n```\n\n### 4.2 Memory State Transition Diagram (After Allocation)\n\n```mermaid\nflowchart LR\n    subgraph INITIAL[\"Initial Free Holes (KB)\"]\n        A1[Block1: 100] --- A2[Block2: 500] --- A3[Block3: 200] --- A4[Block4: 300] --- A5[Block5: 600]\n    end\n\n    subgraph FF[\"After First Fit\"]\n        B1[100 free] --- B2[288 free<br>was 500-212] --- B3[200 free] --- B4[300 free] --- B5[183 free<br>was 600-417]\n    end\n\n    subgraph BF[\"After Best Fit\"]\n        C1[100 free] --- C2[83 free<br>was 500-417] --- C3[88 free<br>was 200-112] --- C4[88 free<br>was 300-212] --- C5[174 free<br>was 600-426]\n    end\n\n    subgraph WF[\"After Worst Fit\"]\n        D1[100 free] --- D2[83 free<br>was 500-417] --- D3[200 free] --- D4[300 free] --- D5[276 free<br>was 388-112]\n    end\n\n    INITIAL --> FF\n    INITIAL --> BF\n    INITIAL --> WF\n```\n\n### 4.3 Block-Level Functional Architecture of the Allocation Engine\n\n```mermaid\nflowchart TB\n    subgraph INPUT[\"Input Layer\"]\n        I1[Memory Block Array]\n        I2[Process Request Queue]\n    end\n\n    subgraph CTRL[\"Controller - menu driven main function\"]\n        C1[Strategy Selector]\n    end\n\n    subgraph ENGINES[\"Allocation Engines\"]\n        E1[First Fit Engine<br>break on first match]\n        E2[Best Fit Engine<br>min waste search]\n        E3[Worst Fit Engine<br>max remaining search]\n    end\n\n    subgraph OUTPUT[\"Output Layer\"]\n        O1[Allocation Table]\n        O2[Updated Free Holes]\n        O3[Fragmentation Stats]\n    end\n\n    I1 --> C1\n    I2 --> C1\n    C1 -->|choice 1| E1\n    C1 -->|choice 2| E2\n    C1 -->|choice 3| E3\n    E1 --> O1\n    E2 --> O1\n    E3 --> O1\n    E1 --> O2\n    E2 --> O2\n    E3 --> O2\n    E1 --> O3\n    E2 --> O3\n    E3 --> O3\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## SECTION 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **Q1. [KTU University Exam — May 2024]**\n> *Differentiate between internal and external fragmentation. Which of the three contiguous allocation strategies (First, Best, Worst Fit) is most likely to suffer from external fragmentation, and why?*\n\n**Model Answer (3 marks):**\n\n| Fragmentation Type | Definition | Example |\n|---|---|---|\n| **Internal** | Allocated memory that the process does **not** use (waste inside block) | Process requests 212 KB, gets 500 KB block → 288 KB wasted inside |\n| **External** | Free memory is sufficient in total, but scattered as **non-contiguous small holes** such that no single hole is large enough | Total free = 533 KB, but largest hole is 174 KB → cannot allocate 426 KB |\n\n**Strategy most prone to external fragmentation:** **Best Fit** ✅\n**Reason (1 mark):** Best Fit always chooses the smallest sufficient hole, creating many *tiny leftover holes* that are too small for future requests. These accumulate over time, increasing external fragmentation.\n\n> **Q2. [KTU University Exam — Dec 2023]**\n> *With a neat diagram, explain the working of the First Fit memory allocation algorithm. State one advantage and one disadvantage.*\n\n**Model Answer (3 marks):**\n\n**Working (2 marks):** The allocator scans the free-block list from the beginning and allocates the process to the **first** hole whose size is ≥ process size. The hole is split: the process gets the requested amount, and the remainder stays as a new (smaller) free hole.\n\n**Diagram (text representation — ½ mark):**\n```\n[ Proc1: 200 ][ Hole: 100 ][ Proc2: 300 ][ Hole: 175 ] ...\n                    ↑\n            First hole ≥ Request (P3 = 120)\n            New: [Proc3: 120][Hole: 100][Hole: 55]\n```\n\n**Advantage (½ mark):** Fastest among the three — early termination on first match.\n**Disadvantage (½ mark):** Large holes at the end of the list get underutilized, increasing fragmentation.\n\n---\n\n### 5.2 Part B — Module Choice Questions (14 Marks)\n\n> **Module Choice: Implement memory management schemes (first fit, best fit and worst fit)**\n\n---\n\n#### ⭐ **Question A (14 Marks) — [KTU University Exam — June 2024]**\n\n**(a) [7 Marks]** Given the memory blocks of sizes `{300, 600, 350, 200, 750, 125}` KB and process requests of sizes `{115, 500, 358, 200, 375}` KB:\n   (i) Simulate **First Fit** allocation and compute the total internal fragmentation.\n   (ii) Simulate **Worst Fit** allocation and compute the total internal fragmentation.\n   (iii) Which strategy yields better memory utilization? Justify with calculations.\n\n**(b) [7 Marks]** Write the C program logic (or pseudo-code) implementing the **Best Fit** algorithm. Explain how your code avoids the pitfalls of First Fit and Worst Fit.\n\n---\n\n#### **Model Solution — Question A (a) [7 Marks]**\n\n**(i) First Fit Trace [3 marks]:**\n\n| Request | Block scan order | Allocated Block | Remaining hole | Internal Frag |\n|---|---|---|---|---|\n| 115 | 300→✅ | 300 | 185 | 0 |\n| 500 | 185→fail, 600→✅ | 600 | 100 | 0 |\n| 358 | 100→fail, 350→fail, 200→fail, 750→✅ | 750 | 392 | 0 |\n| 200 | 392→✅ | 392 | 192 | 0 |\n| 375 | 192→fail, 350→✅ | 350 | (350−375) = **−25 FAIL** | — |\n\n**Result:** 4 of 5 processes allocated. **Total Internal Fragmentation (First Fit) = 185 + 100 + 392 + 192 = 869 KB** (these are unallocated remainders *inside* allocated blocks).\n\n**(ii) Worst Fit Trace [3 marks]:**\n\n| Request | Allocated Block (largest) | Remaining | Internal Frag |\n|---|---|---|---|\n| 115 | 750 | 635 | 0 |\n| 500 | 635 (now max) | 135 | 0 |\n| 358 | 600 (now max) | 242 | 0 |\n| 200 | 635→wait, 350 (now max) | 150 | 0 |\n| 375 | 600? No, used. Next max unused: 300 | (300−375) FAIL | — |\n\n**Result:** 3 of 5 processes allocated. **Total Internal Frag (Worst Fit) = 635 + 135 + 242 + 150 = 1162 KB.**\n\n**(iii) Comparison [1 mark]:**\n\n$$\n\\text{Memory Utilization}_{FF} = \\frac{115+500+358+200}{2325} \\times 100 = \\frac{1173}{2325} \\times 100 \\approx 50.45\\%\n$$\n\n$$\n\\text{Memory Utilization}_{WF} = \\frac{115+500+358}{2325} \\times 100 = \\frac{973}{2325} \\times 100 \\approx 41.85\\%\n$$\n\n**First Fit is better** because it successfully allocated more processes (4 vs 3), giving higher memory utilization.\n\n---\n\n#### **Model Solution — Question A (b) [7 Marks]**\n\n**Best Fit C Logic [5 marks]:**\n\n```c\nvoid bestFit(int blockSize[], int m, int processSize[], int n) {\n    int allocation[n];\n    for (int i = 0; i < n; i++) allocation[i] = -1;\n\n    for (int i = 0; i < n; i++) {\n        int bestIdx = -1;\n        for (int j = 0; j < m; j++) {\n            if (blockSize[j] >= processSize[i]) {\n                if (bestIdx == -1 || blockSize[j] < blockSize[bestIdx])\n                    bestIdx = j;            // smallest fitting block\n            }\n        }\n        if (bestIdx != -1) {\n            allocation[i] = bestIdx;\n            blockSize[bestIdx] -= processSize[i];\n        }\n    }\n}\n```\n\n**Avoiding pitfalls [2 marks]:**\n- **Avoids First Fit's drawback:** Does NOT `break` early — scans all blocks to find the *minimum* waste, not just the first acceptable block.\n- **Avoids Worst Fit's drawback:** Uses `<` (less than) comparison to pick the *smallest* fitting hole, not the *largest*. This minimizes leftover waste and improves internal packing.\n\n---\n\n#### ⭐ **Question B (14 Marks) — [KTU University Exam — Dec 2023]**\n\n**(a) [7 Marks]** Define the term **dynamic memory allocation**. With suitable examples, explain how the **Best Fit** algorithm allocates memory blocks to processes. Compute the total external fragmentation (as a percentage) after allocating processes of sizes `{120, 200, 50, 350}` KB from initial free blocks `{100, 300, 250, 400, 150}` KB using Best Fit.\n\n**(b) [7 Marks]** Compare and contrast First Fit, Best Fit, and Worst Fit algorithms in terms of (i) time complexity, (ii) memory utilization, (iii) external fragmentation, and (iv) suitability for real-time OS. Recommend the best strategy for an embedded system with 512 KB RAM and justify.\n\n---\n\n#### **Model Solution — Question B (a) [7 Marks]**\n\n**Definition [1 mark]:** Dynamic memory allocation is the runtime assignment of memory blocks to processes whose sizes are not known at compile time. The OS maintains a list of free holes and allocates on-demand.\n\n**Best Fit Explanation [2 marks]:** Best Fit scans the *entire* free list and chooses the hole whose size is closest to (but ≥) the request. This minimizes the leftover waste.\n\n**Computation [4 marks]:**\n\n| Step | Request | Considered holes (≥ Req) | Selected (smallest) | Remaining | Holes state |\n|---|---|---|---|---|---|\n| 1 | 120 | 300, 250, 400, 150 | **150** | 30 | {100, 300, 250, 400, 30} |\n| 2 | 200 | 300, 250, 400 | **250** | 50 | {100, 300, 50, 400, 30} |\n| 3 | 50 | 100, 300, 50, 400, 30 | **50** | 0 (delete hole) | {100, 300, 400, 30} |\n| 4 | 350 | 400 | **400** | 50 | {100, 300, 50, 30} |\n\n**Final free holes:** `{100, 300, 50, 30}` — Total = **480 KB**, but **largest single hole = 300 KB**.\n\n**External Fragmentation Calculation:**\n\n$$\nEF(\\%) = \\frac{\\text{Free holes smaller than next request (350 KB)}}{\\text{Total free memory}} \\times 100\n$$\n\nHoles < 350 KB = 100 + 50 + 30 = **180 KB**\nTotal free memory = **480 KB**\n\n$$\nEF = \\frac{180}{480} \\times 100 = 37.5\\%\n$$\n\n---\n\n#### **Model Solution — Question B (b) [7 Marks]**\n\n**Comparison Table [4 marks]:**\n\n| Criterion | First Fit | Best Fit | Worst Fit |\n|---|---|---|---|\n| (i) Time Complexity | $O(n)$ average, faster (early stop) | $O(n)$ — full scan | $O(n)$ — full scan |\n| (ii) Memory Utilization | High (60–70%) | Variable (good short-term) | Poor (large leftovers wasted) |\n| (iii) External Fragmentation | Moderate | **Highest** (tiny holes) | Lowest |\n| (iv) Real-time OS Suitability | ✅ Good (predictable, fast) | ❌ Poor (unpredictable scan) | ❌ Poor (unpredictable scan) |\n\n**Embedded System Recommendation [3 marks]:**\n\n> [!IMPORTANT]\n> **Recommendation: First Fit** ✅\n> **Justification:** Embedded systems have **strict latency and determinism** requirements.\n> 1. **Speed:** First Fit's early termination gives the *fastest allocation time*, which is critical for ISR (Interrupt Service Routines) and real-time tasks.\n> 2. **Predictability:** Unlike Best Fit, First Fit's allocation time is bounded (best case O(1) if a hole is found at the start).\n> 3. **Simplicity:** With only 512 KB RAM, the free-list is small (a few holes), so First Fit's full-scan overhead is negligible.\n> 4. **Industry Practice:** Real-time OS like **FreeRTOS** and **VxWorks** use First Fit variants (`heap_4.c` in FreeRTOS).\n\n**Counter-argument:** Best Fit could pack processes more tightly, but the *unpredictable scan time* violates real-time deadlines.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfalls — Common Mark Losses**\n> 1. **Forgetting to update the hole after allocation** — examiners deduct **1 mark** if the remainder is not shown.\n> 2. **Confusing `break` vs full scan** — students often use `break` in Best Fit by mistake, making it equivalent to First Fit. **Avoid this.**\n> 3. **Skipping the \"Allocation Failed\" case** — always explicitly state when no block satisfies the request.\n> 4. **Using absolute value bars `\\|x\\|` inside markdown tables** — this breaks table rendering. Use `\\vert x \\vert` in LaTeX.\n> 5. **Not showing fragmentation calculation** — KTU awards **dedicated marks** for the EF / IF formula. Always include the numerical answer with units.\n\n---\n\n### 5.3 Topic Recap & Important Things to Remember\n\n> [!NOTE]\n> **High-Density Revision Checklist (Last-Minute KTU Prep)**\n\n- **Three strategies:** First Fit, Best Fit, Worst Fit — all have $O(n)$ time complexity.\n- **Selection keywords:** First = *first acceptable*, Best = *smallest fitting*, Worst = *largest hole*.\n- **Speed ranking:** First Fit (fastest) > Best Fit ≈ Worst Fit.\n- **Internal Fragmentation leader:** Worst Fit (most waste per allocation).\n- **External Fragmentation leader:** Best Fit (produces tiny holes).\n- **Memory Utilization leader:** First Fit (in most practical cases).\n- **Real-time OS choice:** **First Fit** — predictable and fast.\n- **C code distinction:** `break` = First Fit; `if (size < bestSize)` = Best Fit; `if (size > worstSize)` = Worst Fit.\n- **EF Formula:** $EF = \\dfrac{\\text{holes} < \\text{request}}{\\text{total free}} \\times 100$.\n- **IF Formula:** $IF = \\sum (\\text{block allocated} - \\text{process size})$.\n- **Linux `malloc` (glibc):** Uses a hybrid — segregated free lists (a form of Best Fit) plus mmap for large allocations.\n- **Examiner mantra:** \"Show every step — hole list before AND after each allocation.\"\n- **Sample data to memorize:** Holes `{100, 500, 200, 300, 600}`; Requests `{212, 417, 112, 426}` — classic KTU problem.\n- **Practical implication:** Worst Fit is *rarely used in production*; First Fit and Best Fit dominate.\n\n---\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "information-technology-5-pcitl408-mExperiment 2-t9",
          "title": "Implement page replacement algorithms: FIFO, LRU and optimal",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCITL408 - OPERATING SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Implement page replacement algorithms: FIFO, LRU and optimal\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n> [!NOTE]\n> **Page Replacement Algorithm:** A memory management strategy used by the Operating System when a requested page is **not present** in the physical memory (RAM), causing a **page fault**. The OS must then decide *which existing page in memory to evict* to make room for the newly required page, with the goal of minimizing the number of page faults.\n\nIn a system using **Demand Paging**, processes are loaded into memory only when their pages are needed. When all frames are occupied and a new page is requested, the OS invokes a page replacement algorithm. The efficiency of the entire memory subsystem is heavily dependent on this choice — it directly influences the **Page Fault Rate** and consequently the system's **Effective Access Time (EAT)**.\n\n### 1.2 Key Terminology\n\n| Term | Definition |\n|------|------------|\n| **Page Fault** | A type of interrupt (trap) raised when a process tries to access a page that is **not mapped** in physical memory (RAM). |\n| **Page Hit** | The requested page is already present in a physical frame — **zero disk I/O** required. |\n| **Page Miss** | Synonym for Page Fault (older textbooks). |\n| **Belady's Anomaly** | An unusual phenomenon where **increasing the number of frames can increase the page fault count** (observed in FIFO). |\n\n### 1.3 Intuitive Analogy — The Library Reading Room\n\n> [!IMPORTANT]\n> Imagine a **small study table with only 3 slots** (your physical frames). You are revising from **20 different textbooks** (pages) one chapter at a time (reference string). When the table is full and a new textbook arrives, you must **remove one** to make space. Which one do you remove?\n>\n> - **FIFO** → Remove the one you placed on the table **earliest in time** (queue order).\n> - **LRU (Least Recently Used)** → Remove the textbook you **haven't opened for the longest time** (recency heuristic).\n> - **Optimal (OPT / Belady's Algorithm)** → Remove the textbook that you **will not need for the longest time in the future** (clairvoyant / oracle).\n>\n> The fewer times you have to shuffle textbooks, the **better your study efficiency** — analogous to lower page faults and faster execution.\n\n### 1.4 Real-World Engineering Relevance\n\n- **Database Buffer Pools:** MySQL InnoDB and PostgreSQL use variants of LRU (e.g., Clock-Sweep) to keep hot indexes in RAM.\n- **Web Browser Caching:** Chrome & Firefox evict the least-recently-used tab from memory.\n- **CDNs (Cloudflare, Akamai):** Eviction policies on edge servers are direct descendants of these algorithms.\n- **CPU Translation Lookaside Buffer (TLB):** Even hardware caches use aging/recency schemes similar to LRU.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Operational Flow (Common to All Three Algorithms)\n\n1. Process reference string **R = r₁, r₂, r₃, …, rₙ** is generated.\n2. Maintain a frame table (size = **m** frames), initially empty.\n3. For each reference **rᵢ**:\n   - **If rᵢ ∈ frames** → *Page Hit* (cost ≈ 1 memory access).\n   - **Else if frames < m** → *Page Fault*, load **rᵢ** into an empty frame.\n   - **Else frames == m** → *Page Fault*, evict one page using the chosen policy, then load **rᵢ**.\n\n### 2.2 The Three Algorithms — Logic Breakdown\n\n#### (A) FIFO (First-In, First-Out)\n- Treats frames as a **circular queue**.\n- The page that **arrived earliest** is replaced, regardless of how often it has been used.\n- **Pros:** Trivially simple to implement (just an index counter).\n- **Cons:** Suffers from **Belady's Anomaly** — more frames ⇒ more faults (counter-intuitive!).\n\n#### (B) LRU (Least Recently Used)\n- Uses the **recency** of use — replaces the page whose **last reference was the farthest in the past**.\n- Based on the principle of **temporal locality**.\n- **Pros:** No Belady's anomaly; very good practical hit ratio.\n- **Cons:** Expensive hardware — requires a timestamp/counter on every reference or a stack.\n\n#### (C) Optimal (OPT / Belady's Algorithm)\n- A **theoretical oracle** — replaces the page that will **not be used for the longest time in the future**.\n- **Pros:** Guarantees the **minimum possible page faults** for any given reference string.\n- **Cons:** Requires future knowledge → **impossible to implement** in a real running system. Used only as a benchmark to measure other algorithms.\n\n### 2.3 KTU Formula Sheet / Cheat Sheet\n\n| Concept | Equation / Definition | Notes |\n|---|---|---|\n| **Page Fault Rate (PFR)** | $\\text{PFR} = \\dfrac{\\text{Number of Page Faults}}{\\text{Total References}}$ | Range: 0 to 1 |\n| **Page Hit Rate (PHR)** | $\\text{PHR} = 1 - \\text{PFR}$ | Useful for comparing algorithms |\n| **Effective Access Time (EAT)** | $\\text{EAT} = (1 - p) \\cdot \\text{mem\\_access} + p \\cdot \\text{page\\_fault\\_service}$ | $p$ = page fault rate |\n| **Typical Page Fault Service Time** | $\\approx 1\\text{ to } 10$ **milliseconds** | Includes disk I/O |\n| **Typical Memory Access Time** | $\\approx 10\\text{ to } 100$ **nanoseconds** | Crucial ratio difference |\n| **Belady's Anomaly** | $\\text{Faults}(m{+}1) > \\text{Faults}(m)$ for some $m$ | Happens only with FIFO |\n| **Stack Property** | $\\text{Frames in OPT}(m) \\subseteq \\text{Frames in OPT}(m{+}1)$ | OPT & LRU satisfy this |\n\n> [!IMPORTANT]\n> **Critical Rule for KTU:** Always express Page Fault Rate as a **fraction** or **percentage**, never as a raw decimal alone. Example: *\"Out of 20 references, 9 faults → PFR = 9/20 = 45%\"*.\n\n### 2.4 Engineering Utility & Why It Matters\n\n| Field | Application |\n|---|---|\n| **Cloud Computing** | AWS Lambda cold-start latency governed by cache replacement |\n| **Mobile OS** | Android uses **LRU-style eviction** for app memory trimming |\n| **Compiler Design** | Instruction cache prefetching uses similar heuristics |\n| **Embedded Systems** | RTOS use simpler FIFO/Clock due to hardware constraints |\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Worked Example — The \"Classic\" KTU Reference String\n\nLet us solve using the textbook reference string:\n\n**Reference String:** $R = 7, 0, 1, 2, 0, 3, 0, 4, 2, 3, 0, 3, 2, 1, 2, 0, 1, 7, 0, 1$\n\n**Number of Frames:** $m = 3$\n\n**Total References:** $n = 20$\n\n---\n\n### 3.2 (A) FIFO — Full Trace\n\n> [!NOTE]\n> **Rule:** Evict the page that was loaded *earliest*. We use a counter to track arrival order. The page with the smallest counter is replaced.\n\n| Step | Ref | Frame 1 | Frame 2 | Frame 3 | Hit/Fault | Evicted |\n|:---:|:---:|:---:|:---:|:---:|:---:|:---:|\n| 1 | **7** | 7 | – | – | ❌ Fault | – |\n| 2 | **0** | 7 | 0 | – | ❌ Fault | – |\n| 3 | **1** | 7 | 0 | 1 | ❌ Fault | – |\n| 4 | **2** | 2 | 0 | 1 | ❌ Fault | **7** (oldest) |\n| 5 | **0** | 2 | 0 | 1 | ✅ Hit | – |\n| 6 | **3** | 2 | 3 | 1 | ❌ Fault | **0** |\n| 7 | **0** | 2 | 3 | 0 | ❌ Fault | **1** |\n| 8 | **4** | 4 | 3 | 0 | ❌ Fault | **2** |\n| 9 | **2** | 4 | 2 | 0 | ❌ Fault | **3** |\n| 10 | **3** | 4 | 2 | 3 | ❌ Fault | **0** |\n| 11 | **0** | 0 | 2 | 3 | ❌ Fault | **4** |\n| 12 | **3** | 0 | 2 | 3 | ✅ Hit | – |\n| 13 | **2** | 0 | 2 | 3 | ✅ Hit | – |\n| 14 | **1** | 0 | 1 | 3 | ❌ Fault | **2** |\n| 15 | **2** | 0 | 1 | 2 | ❌ Fault | **3** |\n| 16 | **0** | 0 | 1 | 2 | ✅ Hit | – |\n| 17 | **1** | 0 | 1 | 2 | ✅ Hit | – |\n| 18 | **7** | 7 | 1 | 2 | ❌ Fault | **0** |\n| 19 | **0** | 7 | 0 | 2 | ❌ Fault | **1** |\n| 20 | **1** | 7 | 0 | 1 | ❌ Fault | **2** |\n\n**Total Page Faults (FIFO, m=3):** $\\text{PF}_{\\text{FIFO}} = 15$\n\n$$\n\\text{PFR}_{\\text{FIFO}} = \\frac{15}{20} = 0.75 = 75\\%\n$$\n\n---\n\n### 3.3 (B) LRU — Full Trace\n\n> [!NOTE]\n> **Rule:** Evict the page whose **last reference was the farthest in the past**. Track a \"recent-use timestamp\" for each page in memory.\n\n| Step | Ref | Frame 1 | Frame 2 | Frame 3 | Hit/Fault | Evicted (LRU) |\n|:---:|:---:|:---:|:---:|:---:|:---:|:---:|\n| 1 | **7** | 7 | – | – | ❌ Fault | – |\n| 2 | **0** | 7 | 0 | – | ❌ Fault | – |\n| 3 | **1** | 7 | 0 | 1 | ❌ Fault | – |\n| 4 | **2** | 2 | 0 | 1 | ❌ Fault | **7** (used at t=1) |\n| 5 | **0** | 2 | 0 | 1 | ✅ Hit (refresh 0) | – |\n| 6 | **3** | 2 | 0 | 3 | ❌ Fault | **1** (used at t=3) |\n| 7 | **0** | 2 | 0 | 3 | ✅ Hit (refresh 0) | – |\n| 8 | **4** | 4 | 0 | 3 | ❌ Fault | **2** (used at t=4) |\n| 9 | **2** | 4 | 0 | 2 | ❌ Fault | **3** (used at t=6) |\n| 10 | **3** | 4 | 3 | 2 | ❌ Fault | **0** (used at t=7) |\n| 11 | **0** | 0 | 3 | 2 | ❌ Fault | **4** (used at t=8) |\n| 12 | **3** | 0 | 3 | 2 | ✅ Hit (refresh 3) | – |\n| 13 | **2** | 0 | 3 | 2 | ✅ Hit (refresh 2) | – |\n| 14 | **1** | 1 | 3 | 2 | ❌ Fault | **0** (used at t=11) |\n| 15 | **2** | 1 | 3 | 2 | ✅ Hit (refresh 2) | – |\n| 16 | **0** | 1 | 0 | 2 | ❌ Fault | **3** (used at t=12) |\n| 17 | **1** | 1 | 0 | 2 | ✅ Hit (refresh 1) | – |\n| 18 | **7** | 7 | 0 | 2 | ❌ Fault | **2** (used at t=15) |\n| 19 | **0** | 7 | 0 | 2 | ✅ Hit (refresh 0) | – |\n| 20 | **1** | 7 | 0 | 1 | ❌ Fault | **2** (used at t=18) |\n\n**Total Page Faults (LRU, m=3):** $\\text{PF}_{\\text{LRU}} = 12$\n\n$$\n\\text{PFR}_{\\text{LRU}} = \\frac{12}{20} = 0.60 = 60\\%\n$$\n\n---\n\n### 3.4 (C) OPTIMAL (Belady's Algorithm) — Full Trace\n\n> [!NOTE]\n> **Rule:** Evict the page whose **next use is farthest in the future** (or never used again). Requires looking ahead at the **entire remaining** reference string.\n\n| Step | Ref | Frame 1 | Frame 2 | Frame 3 | Hit/Fault | Evicted (Optimal) |\n|:---:|:---:|:---:|:---:|:---:|:---:|:---:|\n| 1 | **7** | 7 | – | – | ❌ Fault | – |\n| 2 | **0** | 7 | 0 | – | ❌ Fault | – |\n| 3 | **1** | 7 | 0 | 1 | ❌ Fault | – |\n| 4 | **2** | 2 | 0 | 1 | ❌ Fault | **7** (next use at t=18) |\n| 5 | **0** | 2 | 0 | 1 | ✅ Hit | – |\n| 6 | **3** | 2 | 0 | 3 | ❌ Fault | **1** (next use at t=14) |\n| 7 | **0** | 2 | 0 | 3 | ✅ Hit | – |\n| 8 | **4** | 4 | 0 | 3 | ❌ Fault | **2** (next use at t=9) |\n| 9 | **2** | 2 | 0 | 3 | ❌ Fault | **4** (never used again) |\n| 10 | **3** | 2 | 0 | 3 | ✅ Hit | – |\n| 11 | **0** | 2 | 0 | 3 | ✅ Hit | – |\n| 12 | **3** | 2 | 0 | 3 | ✅ Hit | – |\n| 13 | **2** | 2 | 0 | 3 | ✅ Hit | – |\n| 14 | **1** | 2 | 0 | 1 | ❌ Fault | **3** (never used again) |\n| 15 | **2** | 2 | 0 | 1 | ✅ Hit | – |\n| 16 | **0** | 2 | 0 | 1 | ✅ Hit | – |\n| 17 | **1** | 2 | 0 | 1 | ✅ Hit | – |\n| 18 | **7** | 7 | 0 | 1 | ❌ Fault | **2** (never used again) |\n| 19 | **0** | 7 | 0 | 1 | ✅ Hit | – |\n| 20 | **1** | 7 | 0 | 1 | ✅ Hit | – |\n\n**Total Page Faults (OPT, m=3):** $\\text{PF}_{\\text{OPT}} = 9$\n\n$$\n\\text{PFR}_{\\text{OPT}} = \\frac{9}{20} = 0.45 = 45\\%\n$$\n\n---\n\n### 3.5 Comparative Summary Matrix\n\n$$\n\\text{For } R = \\{7,0,1,2,0,3,0,4,2,3,0,3,2,1,2,0,1,7,0,1\\},\\; m = 3:\n$$\n\n| Algorithm | Total Page Faults | Page Fault Rate | Stack Property? | Belady-Safe? | Realizable? |\n|:---|:---:|:---:|:---:|:---:|:---:|\n| **FIFO** | 15 | **75%** | ❌ No | ❌ No (Anomaly possible) | ✅ Yes |\n| **LRU** | 12 | **60%** | ✅ Yes | ✅ Yes | ✅ Yes (costly) |\n| **OPTIMAL** | 9 | **45%** | ✅ Yes | ✅ Yes | ❌ No (oracle) |\n\n> [!IMPORTANT]\n> **Verification of Optimality:** OPTIMAL always yields the **minimum possible page faults** for a given reference string. The inequality $\\text{PF}_{\\text{OPT}} \\le \\text{PF}_{\\text{LRU}} \\le \\text{PF}_{\\text{FIFO}}$ is **not always strict**, but in our example: $9 < 12 < 15$. ✔\n\n---\n\n### 3.6 Belady's Anomaly — Demonstration\n\nUsing a **shorter reference string** $R' = 1, 2, 3, 4, 1, 2, 5, 1, 2, 3, 4, 5$:\n\n**FIFO with $m = 3$ frames:**\n\n$$\n\\text{Trace} \\Rightarrow \\text{Faults at } t = 1,2,3,4,5,6,7,9,10 \\Rightarrow \\boxed{\\text{PF} = 9}\n$$\n\n**FIFO with $m = 4$ frames:**\n\n$$\n\\text{Trace} \\Rightarrow \\text{Faults at } t = 1,2,3,4,6,7,8,9,10 \\Rightarrow \\boxed{\\text{PF} = 10}\n$$\n\n**Result:** Adding a frame (3 → 4) **increased** faults (9 → 10). This counter-intuitive behavior is **Belady's Anomaly**.\n\n$$\n\\exists R' \\;\\text{such that}\\; \\text{FIFO}_{m{+}1}(R') > \\text{FIFO}_{m}(R')\n$$\n\nLRU and OPT do **not** suffer from this anomaly because they satisfy the **stack property** (a set of frames for $m{+}1$ is a superset of the set for $m$).\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 High-Level Block Diagram — Demand Paging with Replacement\n\n```mermaid\nflowchart TD\n    A[\"CPU generates virtual address r_i\"] --> B{\"Page r_i in any frame?\"}\n    B -- \"YES - Hit\" --> C[\"Access physical memory directly\"]\n    B -- \"NO - Fault\" --> D{\"All frames occupied?\"}\n    D -- \"NO - Free frame\" --> E[\"Load page r_i into free frame\"]\n    D -- \"YES - All full\" --> F[\"Invoke Replacement Algorithm\"]\n    F --> G1[\"FIFO: evict oldest\"]\n    F --> G2[\"LRU: evict least recently used\"]\n    F --> G3[\"OPT: evict farthest future use\"]\n    G1 --> H[\"Update frame table\"]\n    G2 --> H\n    G3 --> H\n    H --> I[\"Load r_i into vacated frame\"]\n    I --> J[\"Resume process\"]\n    C --> K[\"Increment hit counter\"]\n    E --> L[\"Increment fault counter\"]\n    J --> L\n    K --> M[\"Report PFR at end\"]\n    L --> M\n```\n\n### 4.2 Comparative Algorithm Flowchart\n\n```mermaid\nflowchart LR\n    subgraph ALG[\"Page Replacement Policies\"]\n        direction TB\n        P1[\"FIFO Policy\"]\n        P2[\"LRU Policy\"]\n        P3[\"OPT Policy\"]\n    end\n    P1 --- D1[\"Evict page with smallest arrival timestamp\"]\n    P2 --- D2[\"Evict page with oldest last-use timestamp\"]\n    P3 --- D3[\"Evict page with farthest future reference\"]\n    ALG --> OUT[\"Output: Frame table after each reference + Total Faults\"]\n```\n\n### 4.3 State-Transition Diagram of a Frame Slot\n\n```mermaid\nstateDiagram-v2\n    [*] --> Empty\n    Empty --> Occupied: Page loaded on Fault\n    Occupied --> Occupied: Hit refresh timestamp\n    Occupied --> Empty: Evicted by FIFO or LRU or OPT\n    Empty --> Occupied: New page loaded\n    Occupied --> [*]: Process termination\n```\n\n### 4.4 C Implementation Skeleton (Reference for Lab Viva)\n\n```c\n#include <stdio.h>\n\nint main(void) {\n    int frames, n, i, j, k, page_faults = 0, found, oldest, pos, farthest, future_use;\n    int reference_string[] = {7, 0, 1, 2, 0, 3, 0, 4, 2, 3,\n                              0, 3, 2, 1, 2, 0, 1, 7, 0, 1};\n    int frame[10], counter[10];      /* counter holds timestamp for LRU */\n    int use_future[10];              /* holds next-use distance for OPT */\n\n    printf(\"Enter number of frames: \");\n    scanf(\"%d\", &frames);\n\n    for (i = 0; i < frames; i++) {\n        frame[i]   = -1;            /* empty */\n        counter[i] = 0;\n    }\n\n    n = sizeof(reference_string) / sizeof(reference_string[0]);\n\n    /* ----------- FIFO LOGIC ----------- */\n    for (i = 0; i < n; i++) {\n        found = 0;\n        for (j = 0; j < frames; j++)\n            if (frame[j] == reference_string[i]) { found = 1; break; }\n\n        if (!found) {\n            /* find empty slot or oldest */\n            pos = -1;\n            for (j = 0; j < frames; j++)\n                if (frame[j] == -1) { pos = j; break; }\n            if (pos == -1) {\n                pos = 0;\n                for (j = 1; j < frames; j++)\n                    if (counter[j] < counter[pos]) pos = j;\n            }\n            frame[pos]   = reference_string[i];\n            counter[pos] = i;        /* arrival order = i */\n            page_faults++;\n        }\n        printf(\"Step %2d -> \", i + 1);\n        for (k = 0; k < frames; k++) printf(\"%2d \", frame[k]);\n        printf(found ? \" (Hit)\\n\" : \" (Fault)\\n\");\n    }\n    printf(\"\\nTotal Page Faults (FIFO) = %d\\n\", page_faults);\n    return 0;\n}\n```\n\n> [!NOTE]\n> **Adaptation for Lab Records:** Replace the FIFO block with two analogous blocks for **LRU** (use `i` as access timestamp directly) and **OPT** (loop through `reference_string[i+1..n-1]` to compute next-use distance). Always show **a hand-traced table** in the record alongside the code output for full marks.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n---\n\n**Q1. [KTU University Exam – May 2024]**\nDefine a **page fault**. What is the role of a page replacement algorithm when a page fault occurs?\n\n**Model Answer (3 Marks):**\n\n> A **page fault** is a hardware interrupt (trap) generated by the Memory Management Unit (MMU) when a process references a page that is **not currently resident** in any physical frame of RAM.\n>\n> When a page fault occurs, the OS kernel is invoked. If a **free frame** is available, the page is simply loaded from secondary storage. However, if **all frames are occupied**, the OS must call a **page replacement algorithm** to decide *which existing page to evict (swap out to disk)* in order to make room for the newly demanded page. The replacement algorithm directly affects the **page fault rate** and overall system throughput.\n\n*(Marks distribution: 1 mark definition + 2 marks role)*\n\n---\n\n**Q2. [KTU University Exam – Dec 2023]**\nDistinguish between **FIFO** and **LRU** page replacement algorithms. State **one advantage** and **one disadvantage** of each.\n\n**Model Answer (3 Marks):**\n\n| Aspect | FIFO | LRU |\n|---|---|---|\n| **Eviction Policy** | Replace page loaded earliest | Replace page unused for the longest time |\n| **Hardware Support** | Simple counter/queue | Expensive timestamps or stack |\n| **Advantage** | Easy to implement; minimal overhead | No Belady's anomaly; good hit ratio |\n| **Disadvantage** | Suffers from Belady's Anomaly | High implementation cost |\n\n*(Marks distribution: 1 mark distinction + 1 mark FIFO points + 1 mark LRU points)*\n\n---\n\n### 5.2 Part B — Long Answer Questions (14 Marks)\n\n> Each Part B question carries **14 marks**, split into **(a) 7 marks** and **(b) 7 marks**. Both choices below are independent modules from the same experiment.\n\n---\n\n### 📘 QUESTION A — 14 Marks\n\n**[KTU University Exam – June 2024, Modified for Lab Module 2]**\n\n**(a)** Consider the reference string: $R = 1, 2, 3, 4, 1, 2, 5, 1, 2, 3, 4, 5$ with **3 page frames**. Compute the number of page faults for the **FIFO** algorithm and show the frame contents after each reference. **(7 Marks)**\n\n**(b)** Repeat part (a) for the **LRU** algorithm. Compare the page fault counts and explain which algorithm performed better and why. **(7 Marks)**\n\n---\n\n#### ✅ MODEL SOLUTION — Question A\n\n**(a) FIFO with m = 3 (7 Marks)**\n\n| Step | Ref | F1 | F2 | F3 | Hit/Fault | Replaced |\n|:---:|:---:|:---:|:---:|:---:|:---:|:---:|\n| 1 | **1** | 1 | – | – | ❌ Fault | – |\n| 2 | **2** | 1 | 2 | – | ❌ Fault | – |\n| 3 | **3** | 1 | 2 | 3 | ❌ Fault | – |\n| 4 | **4** | 4 | 2 | 3 | ❌ Fault | 1 |\n| 5 | **1** | 4 | 1 | 3 | ❌ Fault | 2 |\n| 6 | **2** | 4 | 1 | 2 | ❌ Fault | 3 |\n| 7 | **5** | 5 | 1 | 2 | ❌ Fault | 4 |\n| 8 | **1** | 5 | 1 | 2 | ✅ Hit | – |\n| 9 | **2** | 5 | 1 | 2 | ✅ Hit | – |\n| 10 | **3** | 5 | 3 | 2 | ❌ Fault | 1 |\n| 11 | **4** | 5 | 3 | 4 | ❌ Fault | 2 |\n| 12 | **5** | 5 | 3 | 4 | ✅ Hit | – |\n\n**Total Page Faults (FIFO):** $\\boxed{9}$\n\n---\n\n**(b) LRU with m = 3 (7 Marks)**\n\n| Step | Ref | F1 | F2 | F3 | Hit/Fault | Replaced (LRU) |\n|:---:|:---:|:---:|:---:|:---:|:---:|:---:|\n| 1 | **1** | 1 | – | – | ❌ Fault | – |\n| 2 | **2** | 1 | 2 | – | ❌ Fault | – |\n| 3 | **3** | 1 | 2 | 3 | ❌ Fault | – |\n| 4 | **4** | 4 | 2 | 3 | ❌ Fault | 1 (used at t=1) |\n| 5 | **1** | 4 | 1 | 3 | ❌ Fault | 2 (used at t=2) |\n| 6 | **2** | 4 | 1 | 2 | ❌ Fault | 3 (used at t=3) |\n| 7 | **5** | 5 | 1 | 2 | ❌ Fault | 4 (used at t=4) |\n| 8 | **1** | 5 | 1 | 2 | ✅ Hit | – |\n| 9 | **2** | 5 | 1 | 2 | ✅ Hit | – |\n| 10 | **3** | 3 | 1 | 2 | ❌ Fault | 5 (used at t=7) |\n| 11 | **4** | 3 | 4 | 2 | ❌ Fault | 1 (used at t=8) |\n| 12 | **5** | 3 | 4 | 5 | ❌ Fault | 2 (used at t=9) |\n\n**Total Page Faults (LRU):** $\\boxed{10}$\n\n---\n\n**Comparison (1 mark within part b):**\n$$\n\\text{PF}_{\\text{FIFO}} = 9 \\;<\\; \\text{PF}_{\\text{LRU}} = 10\n$$\nFIFO outperformed LRU here — a rare case showing FIFO is **not strictly worse** than LRU for *every* reference string. However, this string also demonstrates **Belady's Anomaly** (with 4 frames, FIFO gives 10 faults, worse than 3 frames). LRU remains more robust in general.\n\n---\n\n### 📘 QUESTION B — 14 Marks (Alternative Choice)\n\n**[KTU University Exam – Dec 2023, Modified for Lab Module 2]**\n\n**(a)** What is **Belady's Anomaly**? Demonstrate it using the reference string $R = 1, 2, 3, 4, 1, 2, 5, 1, 2, 3, 4, 5$ for **FIFO** with **3 frames** and **4 frames**. Show frame contents for both cases. **(7 Marks)**\n\n**(b)** For the same reference string $R = 1, 2, 3, 4, 1, 2, 5, 1, 2, 3, 4, 5$ and **3 frames**, compute the number of page faults using the **OPTIMAL** algorithm. Comment on whether the result satisfies the optimality property. **(7 Marks)**\n\n---\n\n#### ✅ MODEL SOLUTION — Question B\n\n**(a) Belady's Anomaly — Definition + Demonstration (7 Marks)**\n\n> **Definition (2 marks):** Belady's Anomaly is the phenomenon in which, for certain reference strings, **increasing the number of page frames causes an *increase* in the number of page faults** when using the FIFO algorithm.\n\n**FIFO with m = 3 (already shown above) → PF = 9**\n\n**FIFO with m = 4 (5 marks table):**\n\n| Step | Ref | F1 | F2 | F3 | F4 | Hit/Fault | Replaced |\n|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|\n| 1 | **1** | 1 | – | – | – | ❌ Fault | – |\n| 2 | **2** | 1 | 2 | – | – | ❌ Fault | – |\n| 3 | **3** | 1 | 2 | 3 | – | ❌ Fault | – |\n| 4 | **4** | 1 | 2 | 3 | 4 | ❌ Fault | – |\n| 5 | **1** | 1 | 2 | 3 | 4 | ✅ Hit | – |\n| 6 | **2** | 1 | 2 | 3 | 4 | ✅ Hit | – |\n| 7 | **5** | 5 | 2 | 3 | 4 | ❌ Fault | 1 |\n| 8 | **1** | 5 | 1 | 3 | 4 | ❌ Fault | 2 |\n| 9 | **2** | 5 | 1 | 2 | 4 | ❌ Fault | 3 |\n| 10 | **3** | 5 | 1 | 2 | 3 | ❌ Fault | 4 |\n| 11 | **4** | 4 | 1 | 2 | 3 | ❌ Fault | 5 |\n| 12 | **5** | 4 | 5 | 2 | 3 | ❌ Fault | 1 |\n\n**Total Page Faults (FIFO, m=4):** $\\boxed{10}$\n\n**Conclusion:** $m=3 \\Rightarrow 9$ faults; $m=4 \\Rightarrow 10$ faults. With **more frames**, **more page faults** occurred → this is **Belady's Anomaly**.\n\n---\n\n**(b) OPTIMAL with m = 3 (7 Marks)**\n\n| Step | Ref | F1 | F2 | F3 | Hit/Fault | Replaced (Optimal) |\n|:---:|:---:|:---:|:---:|:---:|:---:|:---:|\n| 1 | **1** | 1 | – | – | ❌ Fault | – |\n| 2 | **2** | 1 | 2 | – | ❌ Fault | – |\n| 3 | **3** | 1 | 2 | 3 | ❌ Fault | – |\n| 4 | **4** | 4 | 2 | 3 | ❌ Fault | 1 (next at t=5) |\n| 5 | **1** | 4 | 1 | 3 | ❌ Fault | 2 (next at t=6) |\n| 6 | **2** | 4 | 1 | 2 | ❌ Fault | 3 (next at t=10) |\n| 7 | **5** | 5 | 1 | 2 | ❌ Fault | 4 (never again) |\n| 8 | **1** | 5 | 1 | 2 | ✅ Hit | – |\n| 9 | **2** | 5 | 1 | 2 | ✅ Hit | – |\n| 10 | **3** | 3 | 1 | 2 | ❌ Fault | 5 (never again) |\n| 11 | **4** | 3 | 4 | 2 | ❌ Fault | 1 (next at t=12) |\n| 12 | **5** | 3 | 4 | 5 | ❌ Fault | 2 (never again) |\n\n**Total Page Faults (OPT):** $\\boxed{7}$\n\n**Comment (2 marks):**\n$$\n\\text{PF}_{\\text{OPT}} = 7 \\;\\le\\; \\text{PF}_{\\text{FIFO}} = 9 \\;\\le\\; \\text{PF}_{\\text{LRU}} = 10\n$$\nThe OPTIMAL algorithm yields the **fewest faults** for this reference string, confirming the **Optimality Property**:\n$$\n\\forall R,\\;\\forall m,\\;\\text{PF}_{\\text{OPT}}(R, m) = \\min_{\\pi \\in \\Pi} \\text{PF}_{\\pi}(R, m)\n$$\nwhere $\\Pi$ is the set of all possible replacement policies.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Pitfall Callout — Where Students Lose Marks**\n> 1. **Forgetting to mark Hits vs Faults in the trace table** → KTU examiner expects a Hit/Fault column with ❌/✅ marks. Omitting it = −1 mark.\n> 2. **Misapplying LRU** → LRU evicts based on *last USE time*, **not** the page loaded *earliest*. Conflating it with FIFO = full −7 on part (b).\n> 3. **Belady's Anomaly** → Only FIFO shows it. Writing that \"LRU also shows anomaly\" = 0 for definition.\n> 4. **OPT's \"next use\"** → If a page is **never used again**, it is the immediate choice to evict. Students often skip this case.\n> 5. **Page Fault Rate** → Always quote as `faults/total` or percentage. Just writing \"75\" without denominator = −1 mark.\n> 6. **Empty frames on first load** → Don't count an empty frame as a \"fault replacement\" — it is just loading into a free slot. The first $m$ loads are mandatory faults.\n\n---\n\n### 5.3 📌 Topic Recap & Important Things to Remember (Rapid Revision Checklist)\n\n> [!IMPORTANT]\n> **High-Density Revision Block — Re-read before every KTU exam**\n\n- ✅ **Page Fault** = page not in RAM → causes disk I/O (~ms cost). **Page Hit** = in RAM → fast (~ns cost).\n- ✅ The **Effective Access Time (EAT)** is **dominated** by page faults; even a 1% fault rate can slow the system by 10×.\n- ✅ **FIFO** = evict the page that has been in memory the longest. Simple but suffers from **Belady's Anomaly**.\n- ✅ **LRU** = evict the page unused for the longest time. Uses **temporal locality**. No anomaly, but hardware-expensive.\n- ✅ **OPTIMAL** = evict the page not needed for the longest time in future. **Theoretical benchmark only** — never implementable.\n- ✅ The **inequality** $\\text{PF}_{\\text{OPT}} \\le \\text{PF}_{\\text{LRU}} \\le \\text{PF}_{\\text{FIFO}}$ is generally observed (but NOT a strict universal law).\n- ✅ For the **classic KTU string** $7,0,1,2,0,3,0,4,2,3,0,3,2,1,2,0,1,7,0,1$ with **3 frames**:\n  - FIFO = **15 faults**\n  - LRU = **12 faults**\n  - OPT = **9 faults**\n- ✅ **Belady's Anomaly proof string:** $R = 1,2,3,4,1,2,5,1,2,3,4,5$ → FIFO(3 frames)=9 faults, FIFO(4 frames)=10 faults.\n- ✅ **Stack property** holds for LRU and OPT but **not** for FIFO. This is the formal reason for Belady's Anomaly.\n- ✅ In **lab practicals**, always show: (i) **trace table**, (ii) **total faults**, (iii) **PFR**, (iv) **sample I/O**.\n- ✅ **Real-world use:** Android, MySQL InnoDB, and CDN edge servers all use **LRU or clock-proximity** variants — never naive FIFO.\n- ✅ Be ready to **write the C program** for any of the three algorithms under exam conditions — practice the optimal version's lookahead loop carefully.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "information-technology-5-pcitl408-mExperiment 2-t10",
          "title": "Implementation of disk scheduling using FCFS, SCAN, C-SCAN, LOOK, C-LOOK and SSTF algorithm",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCITL408 - OPERATING SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Implementation of disk scheduling using FCFS, SCAN, C-SCAN, LOOK, C-LOOK and SSTF algorithm\n\n<!-- SECTION_1_START -->\n\n# 💽 Disk Scheduling Algorithms — Module Experiment 2\n\n## 1.1 Core Technical Definition\n\n> [!NOTE]\n> **Disk Scheduling** is the process of determining the order in which pending I/O requests for the disk are serviced. The OS uses a *disk controller* and a *disk-arm scheduling algorithm* to minimize the total **seek time** — the time taken by the read/write head to move from its current cylinder to the requested cylinder.\n\nIn KTU 2024 terminology, disk scheduling is a core **I/O management** function of the OS kernel. The goal is to **minimize mechanical arm movement**, since the physical rotation and radial translation of the disk arm dominate I/O latency. Six classical algorithms form the syllabus mandate:\n\n1. **FCFS** — First-Come, First-Served\n2. **SSTF** — Shortest Seek Time First\n3. **SCAN** — Elevator Algorithm\n4. **C-SCAN** — Circular SCAN\n5. **LOOK**\n6. **C-LOOK**\n\n> [!IMPORTANT]\n> **Standard KTU Assumption:** Disk cylinders are numbered linearly (e.g., **0 to 199**), and the *initial head position* is given. Total head movement (in cylinders) is the **performance metric**.\n\n## 1.2 Intuitive Analogy — The Elevator & The Librarian\n\nImagine you are a **librarian** standing at aisle **53** of a long library with aisles 0–199. Eight patrons simultaneously shout their aisle numbers: *98, 183, 37, 122, 14, 124, 65, 67*.\n\n- **FCFS** → You service them in the exact order they shouted (ignoring distance).\n- **SSTF** → You always run to the *nearest* shouting patron next.\n- **SCAN** → You walk to one end of the library, servicing everyone along the way, then walk back. Like a **lift/elevator**.\n- **C-SCAN** → You walk to the far end, then **teleport** back to the start and walk forward again. Fairer waiting times.\n- **LOOK / C-LOOK** → The *smart* elevator — it doesn't crawl all the way to the walls if the last request is before the wall.\n\n> [!TIP]\n> **Real-world engineering parallel:** Modern SSDs (Solid-State Drives) do **not** need disk scheduling because they have *no mechanical arm*. The algorithms studied here apply to **HDDs, magnetic tapes, optical drives, and robotic warehouse arms**.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n# 2. Deep Theoretical Analysis & Formula Sheet\n\n## 2.1 Operational Logic of Each Algorithm\n\n### 🟢 FCFS (First-Come, First-Served)\n- Services requests in the **exact order of arrival**.\n- Pros: *Fair, starvation-free, simplest implementation*.\n- Cons: *Wild head swings — worst average seek time*.\n- **Starvation:** ❌ None.\n\n### 🟡 SSTF (Shortest Seek Time First)\n- A **greedy, non-preemptive** shortest-distance-first policy.\n- Picks the pending request with the **minimum absolute difference** from the current head.\n- Pros: *Lower average seek time than FCFS*.\n- Cons: *May cause starvation of far-away requests*.\n- **Starvation:** ⚠️ Possible.\n\n### 🔵 SCAN (Elevator Algorithm)\n- Head moves in a **fixed direction** (e.g., towards higher cylinders), servicing all requests en route, until it hits the **last cylinder**.\n- Then **reverses** direction.\n- Pros: *No starvation, low variance in wait time*.\n- Cons: *Requests just behind the head must wait for a full sweep*.\n\n### 🟣 C-SCAN (Circular SCAN)\n- Head moves towards the higher end, servicing requests, until the last cylinder.\n- Then **jumps** back to cylinder 0 **without servicing** any request on the return path, and starts moving up again.\n- Provides a **more uniform wait time** (treating cylinders as a circular list).\n\n### 🟠 LOOK\n- Like SCAN, but the head **does not travel all the way to the end of the disk**. It only goes as far as the **last request** in that direction, then reverses.\n- Saves unnecessary traversal.\n\n### 🔴 C-LOOK\n- A hybrid: head moves in one direction servicing all requests up to the **last request** (not the disk end), then **jumps** to the **lowest pending request** and continues forward.\n\n## 2.2 KTU High-Yield Formula & Boundary Cheat Sheet\n\n| **Parameter** | **Formula / Rule** | **Unit** |\n|---|---|---|\n| Seek distance between two requests $r_i \\rightarrow r_{i+1}$ | $d_i = \\vert r_{i+1} - r_i \\vert$ | cylinders |\n| Total Seek Time (TST) | $TST = \\sum_{i=1}^{n-1} \\vert r_{i+1} - r_i \\vert$ | cylinders |\n| Average Seek Time (AST) | $AST = \\frac{TST}{n}$ | cylinders/request |\n| SCAN — end reached | $d_{end} = \\vert C_{max} - r_{last} \\vert$ where $C_{max} = 199$ | cylinders |\n| C-SCAN — circular jump | Includes $\\vert 199 - 0 \\vert = 199$ once | cylinders |\n| LOOK / C-LOOK | End point = last request in that direction (not $C_{max}$) | cylinders |\n| SSTF — selection rule | $\\min_{r \\in Q} \\vert r - \\text{head}_{cur} \\vert$ | cylinders |\n\n> [!IMPORTANT]\n> **Boundary condition for KTU 2024 labs:** Unless stated otherwise, the disk has **200 cylinders (0–199)** and the head *starts at the given initial position*. The head is assumed to move at **uniform speed** (linear model).\n\n## 2.3 Real-World Engineering Utility\n\n- **Database Systems:** DB2, Oracle, and PostgreSQL use variants of LOOK/SCAN for multi-disk request queuing.\n- **Operating Systems:** Linux's default *Deadline scheduler* and CFQ use SCAN-like fairness.\n- **Robotics & CNC machines:** LOOK is the literal movement model of gantry robots that pick components.\n- **Magnetic tape drives:** Used in legacy backup systems — same head-movement problem.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n# 3. Step-by-Step Analytical Solution (Worked Example)\n\n## 3.1 Problem Statement\n\n> Service the request queue: **98, 183, 37, 122, 14, 124, 65, 67**\n> Initial head position: **53**\n> Disk range: **0 – 199**\n> Compute **Total Head Movement (THM)** for all six algorithms.\n\n---\n\n### 3.2 🟢 FCFS — Order is the arrival order\n\nSequence served: $53 \\rightarrow 98 \\rightarrow 183 \\rightarrow 37 \\rightarrow 122 \\rightarrow 14 \\rightarrow 124 \\rightarrow 65 \\rightarrow 67$\n\n$$\n\\begin{aligned}\n\\text{THM}_{FCFS} &= \\vert 98-53 \\vert + \\vert 183-98 \\vert + \\vert 37-183 \\vert + \\vert 122-37 \\vert \\\\\n&\\quad + \\vert 14-122 \\vert + \\vert 124-14 \\vert + \\vert 65-124 \\vert + \\vert 67-65 \\vert \\\\\n&= 45 + 85 + 146 + 85 + 108 + 110 + 59 + 2 \\\\\n&= \\mathbf{640 \\text{ cylinders}}\n\\end{aligned}\n$$\n\n---\n\n### 3.3 🟡 SSTF — Always pick nearest pending request\n\n| **Step** | **Current Head** | **Pending Distances** | **Chosen Next** | **Movement** |\n|---|---|---|---|---|\n| 1 | 53 | 45, 130, 16, 69, 39, 71, 12, 14 | **65** | 12 |\n| 2 | 65 | 33, 118, 28, 57, 59, **2**, 2 | **67** | 2 |\n| 3 | 67 | 31, 116, 30, 55, 57, — , — | **37** | 30 |\n| 4 | 37 | 61, 85, 85, 23, 87, — , — | **14** | 23 |\n| 5 | 14 | 84, 108, 169, 110, 108, — , — | **98** | 84 |\n| 6 | 98 | — , 85, 24, 24, 26, — , — | **122** | 24 |\n| 7 | 122 | — , 61, 2, **2** | **124** | 2 |\n| 8 | 124 | — , 59, — , — | **183** | 59 |\n\nServed order: **65, 67, 37, 14, 98, 122, 124, 183**\n\n$$\n\\begin{aligned}\n\\text{THM}_{SSTF} &= 12 + 2 + 30 + 23 + 84 + 24 + 2 + 59 \\\\\n&= \\mathbf{236 \\text{ cylinders}}\n\\end{aligned}\n$$\n\n---\n\n### 3.4 🔵 SCAN (moving towards higher end first, then reversing)\n\nPending requests sorted: $\\{14, 37, 65, 67, 98, 122, 124, 183\\}$\n\n**Phase 1 (head 53 → 199):** Service 65, 67, 98, 122, 124, 183, then travel to 199\n**Phase 2 (199 → 0):** Service 37, 14\n\nServed order: **65, 67, 98, 122, 124, 183, 199, 37, 14**\n\n$$\n\\begin{aligned}\n\\text{THM}_{SCAN} &= \\vert 65-53 \\vert + \\vert 67-65 \\vert + \\vert 98-67 \\vert + \\vert 122-98 \\vert + \\vert 124-122 \\vert \\\\\n&\\quad + \\vert 183-124 \\vert + \\vert 199-183 \\vert + \\vert 199-37 \\vert + \\vert 37-14 \\vert \\\\\n&= 12 + 2 + 31 + 24 + 2 + 59 + 16 + 162 + 23 \\\\\n&= \\mathbf{331 \\text{ cylinders}}\n\\end{aligned}\n$$\n\n---\n\n### 3.5 🟣 C-SCAN (towards higher end, circular jump to 0, then up again)\n\nServed order: **65, 67, 98, 122, 124, 183, 199, 0, 14, 37**\n\n$$\n\\begin{aligned}\n\\text{THM}_{C\\text{-}SCAN} &= \\vert 65-53 \\vert + \\vert 67-65 \\vert + \\vert 98-67 \\vert + \\vert 122-98 \\vert + \\vert 124-122 \\vert \\\\\n&\\quad + \\vert 183-124 \\vert + \\vert 199-183 \\vert + \\vert 199-0 \\vert + \\vert 14-0 \\vert + \\vert 37-14 \\vert \\\\\n&= 12 + 2 + 31 + 24 + 2 + 59 + 16 + 199 + 14 + 23 \\\\\n&= \\mathbf{382 \\text{ cylinders}}\n\\end{aligned}\n$$\n\n> [!NOTE]\n> Notice the **circular jump** term $\\vert 199-0 \\vert = 199$ — this is the defining extra cost of C-SCAN.\n\n---\n\n### 3.6 🟠 LOOK (mirror of SCAN, but stops at last request)\n\nServed order: **65, 67, 98, 122, 124, 183, 37, 14**\n(Head reverses at 183 — not at 199 — since 183 is the last request in the upward direction.)\n\n$$\n\\begin{aligned}\n\\text{THM}_{LOOK} &= 12 + 2 + 31 + 24 + 2 + 59 + \\vert 37-183 \\vert + 23 \\\\\n&= 12 + 2 + 31 + 24 + 2 + 59 + 146 + 23 \\\\\n&= \\mathbf{299 \\text{ cylinders}}\n\\end{aligned}\n$$\n\n> [!TIP]\n> LOOK saves SCAN's wasteful $199 \\rightarrow 183 = 16$ cylinder trip on the return phase. (Note: in this particular problem, the last request 183 equals 199 only conceptually; the savings appear on the *reverse* sweep.)\n\n---\n\n### 3.7 🔴 C-LOOK\n\nServed order: **65, 67, 98, 122, 124, 183, 14, 37**\n(After serving 183, jump back to lowest pending request 14, then continue forward.)\n\n$$\n\\begin{aligned}\n\\text{THM}_{C\\text{-}LOOK} &= 12 + 2 + 31 + 24 + 2 + 59 + \\vert 14-183 \\vert + \\vert 37-14 \\vert \\\\\n&= 12 + 2 + 31 + 24 + 2 + 59 + 169 + 23 \\\\\n&= \\mathbf{322 \\text{ cylinders}}\n\\end{aligned}\n$$\n\n---\n\n## 3.8 📊 Comparative Performance Matrix\n\n| **Algorithm** | **Service Order** | **Total Head Movement (cylinders)** | **Starvation** |\n|---|---|---|---|\n| FCFS | 98, 183, 37, 122, 14, 124, 65, 67 | **640** | None |\n| SSTF | 65, 67, 37, 14, 98, 122, 124, 183 | **236** ✅ Best | Possible |\n| SCAN | 65, 67, 98, 122, 124, 183, 199, 37, 14 | **331** | None |\n| C-SCAN | 65, 67, 98, 122, 124, 183, 199, 0, 14, 37 | **382** | None |\n| LOOK | 65, 67, 98, 122, 124, 183, 37, 14 | **299** | None |\n| C-LOOK | 65, 67, 98, 122, 124, 183, 14, 37 | **322** | None |\n\n> [!IMPORTANT]\n> **For KTU 2024 Lab Viva:** If a question states *\"using initial head at 53 and disk range 0–199\"*, the answer keys above are the **expected reference solutions**. Always show the *sorted request set* first.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Block-Level Functional Architecture of a Disk Scheduler\n\n```mermaid\nflowchart TD\n    A[Process Issues I/O Request] --> B[Request Queue FIFO Buffer]\n    B --> C{Scheduling Algorithm}\n    C --> C1[FCFS Module]\n    C --> C2[SSTF Module]\n    C --> C3[SCAN Module]\n    C --> C4[CSCAN Module]\n    C --> C5[LOOK Module]\n    C --> C6[CLOOK Module]\n    C1 --> D[Sorted Service Sequence]\n    C2 --> D\n    C3 --> D\n    C4 --> D\n    C5 --> D\n    C6 --> D\n    D --> E[Disk Controller]\n    E --> F[Disk Arm Physical Movement]\n    F --> G[Total Seek Time Computation]\n```\n\n## 4.2 SCAN Algorithm — Head Movement Trace\n\n```mermaid\nflowchart LR\n    A0[Cylinder 0] --- A14[14] --- A37[37] --- A53((Head Start 53)) --- A65[65] --- A67[67] --- A98[98] --- A122[122] --- A124[124] --- A183[183] --- A199[199]\n    A53 -.->|move up| A65\n    A65 -.->|2| A67\n    A67 -.->|31| A98\n    A98 -.->|24| A122\n    A122 -.->|2| A124\n    A124 -.->|59| A183\n    A183 -.->|16| A199\n    A199 -.->|reverse 162| A37\n    A37 -.->|23| A14\n```\n\n## 4.3 C-LOOK Algorithm — Circular Trace\n\n```mermaid\nflowchart LR\n    A14[14] --- A37[37] --- A53((Head Start 53)) --- A65[65] --- A67[67] --- A98[98] --- A122[122] --- A124[124] --- A183[183]\n    A53 -.->|12| A65\n    A65 -.->|2| A67\n    A67 -.->|31| A98\n    A98 -.->|24| A122\n    A122 -.->|2| A124\n    A124 -.->|59| A183\n    A183 -.->|circular jump 169| A14\n    A14 -.->|23| A37\n```\n\n## 4.4 Comparative Flow of Algorithm Selection Logic\n\n```mermaid\nflowchart TD\n    Q[Pending Request Queue] --> R{Fairness Priority?}\n    R -->|Strict Order| FCFS[FCFS Algorithm]\n    R -->|Performance| SSTF[SSTF Greedy Pick]\n    SSTF --> S{Starvation Concern?}\n    S -->|Yes| SCAN[SCAN Elevator Sweep]\n    S -->|Uniform Wait| CSCAN[C-SCAN Circular]\n    SCAN --> L{Need Edge Traversal?}\n    L -->|No| LOOK[LOOK Reverse at Last Req]\n    L -->|Yes| SCAN\n    CSCAN --> CL{Need Edge Traversal?}\n    CL -->|No| CLOOK[C-LOOK Jump to Lowest]\n    CL -->|Yes| CSCAN\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 📝 Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. **[KTU University Exam — May 2024]**\n*What is disk scheduling? Why is it necessary in operating systems?*\n\n**Model Answer (3 marks):**\n- **Definition (1 mark):** Disk scheduling is the technique used by the OS to determine the order in which pending disk I/O requests are serviced, with the goal of minimizing total seek time.\n- **Necessity (2 marks):** Since the read/write head is a *mechanical component*, its radial movement across platters dominates I/O latency. With multiple concurrent processes issuing requests, an optimal order **reduces arm movement**, thereby improving **throughput** and reducing **average response time**. Without scheduling, random ordering (FCFS) causes wild head swings and poor performance.\n\n---\n\n### Q2. **[KTU University Exam — Dec 2023]**\n*Differentiate between SCAN and C-SCAN disk scheduling algorithms.*\n\n**Model Answer (3 marks):**\n\n| **Aspect** | **SCAN** | **C-SCAN** |\n|---|---|---|\n| Return path | Services requests on the way back | Jumps to cylinder 0, services only while moving up |\n| Wait time variance | Higher (cylinders near start wait longer) | Uniform (treats queue as circular) |\n| Extra movement | Reverse sweep cost | $199 \\rightarrow 0$ circular jump (199 cyl) |\n\n*(1 mark for each valid difference; minimum two differences needed.)*\n\n---\n\n## 📝 Part B — Long Answer Questions (14 Marks Each)\n\n> **[KTU University Exam — Model Question Paper, Module 2]**\n> *Given the disk request sequence for cylinders: **98, 183, 37, 122, 14, 124, 65, 67**. The initial head position is at cylinder **53** and the disk has **200 cylinders (0–199)**.*\n\n---\n\n### 🅰️ Question A — 14 Marks\n\n**(a) Compute the total head movement using the FCFS and SSTF algorithms. Show the order of service in each case. (7 marks)**\n\n**Solution:**\n\n**FCFS (3 marks):**\nServed in arrival order: $53 \\rightarrow 98 \\rightarrow 183 \\rightarrow 37 \\rightarrow 122 \\rightarrow 14 \\rightarrow 124 \\rightarrow 65 \\rightarrow 67$\n\n$$\n\\text{THM}_{FCFS} = 45 + 85 + 146 + 85 + 108 + 110 + 59 + 2 = \\mathbf{640 \\text{ cylinders}}\n$$\n\n**SSTF (4 marks):**\nAt head 53, closest is 65 (dist 12), then 67 (2), then 37 (30), then 14 (23), then 98 (84), then 122 (24), then 124 (2), then 183 (59).\n\n$$\n\\text{THM}_{SSTF} = 12 + 2 + 30 + 23 + 84 + 24 + 2 + 59 = \\mathbf{236 \\text{ cylinders}}\n$$\n\n---\n\n**(b) Compute the total head movement using SCAN and C-SCAN algorithms, assuming the head moves towards the higher-numbered cylinder first. (7 marks)**\n\n**Solution:**\n\n**SCAN (3.5 marks):**\nServed: $53 \\rightarrow 65 \\rightarrow 67 \\rightarrow 98 \\rightarrow 122 \\rightarrow 124 \\rightarrow 183 \\rightarrow 199 \\rightarrow 37 \\rightarrow 14$\n\n$$\n\\text{THM}_{SCAN} = 12 + 2 + 31 + 24 + 2 + 59 + 16 + 162 + 23 = \\mathbf{331 \\text{ cylinders}}\n$$\n\n**C-SCAN (3.5 marks):**\nServed: $53 \\rightarrow 65 \\rightarrow 67 \\rightarrow 98 \\rightarrow 122 \\rightarrow 124 \\rightarrow 183 \\rightarrow 199 \\rightarrow 0 \\rightarrow 14 \\rightarrow 37$\n\n$$\n\\text{THM}_{C\\text{-}SCAN} = 12 + 2 + 31 + 24 + 2 + 59 + 16 + 199 + 14 + 23 = \\mathbf{382 \\text{ cylinders}}\n$$\n\n---\n\n### 🅱️ Question B — 14 Marks (Alternative Choice)\n\n**(a) Compute the total head movement using the LOOK and C-LOOK algorithms for the same request sequence and initial head position. (7 marks)**\n\n**Solution:**\n\n**LOOK (3.5 marks):**\nServed: $53 \\rightarrow 65 \\rightarrow 67 \\rightarrow 98 \\rightarrow 122 \\rightarrow 124 \\rightarrow 183 \\rightarrow 37 \\rightarrow 14$\n(Note: head reverses at 183, not 199.)\n\n$$\n\\text{THM}_{LOOK} = 12 + 2 + 31 + 24 + 2 + 59 + 146 + 23 = \\mathbf{299 \\text{ cylinders}}\n$$\n\n**C-LOOK (3.5 marks):**\nServed: $53 \\rightarrow 65 \\rightarrow 67 \\rightarrow 98 \\rightarrow 122 \\rightarrow 124 \\rightarrow 183 \\rightarrow 14 \\rightarrow 37$\n\n$$\n\\text{THM}_{C\\text{-}LOOK} = 12 + 2 + 31 + 24 + 2 + 59 + 169 + 23 = \\mathbf{322 \\text{ cylinders}}\n$$\n\n---\n\n**(b) Tabulate the total head movement for all six algorithms and identify the best-performing algorithm. Justify. (7 marks)**\n\n**Solution:**\n\n| **Algorithm** | **Total Head Movement (cyl)** |\n|---|---|\n| FCFS | 640 |\n| SSTF | **236** ✅ |\n| SCAN | 331 |\n| C-SCAN | 382 |\n| LOOK | 299 |\n| C-LOOK | 322 |\n\n**Best Performer: SSTF (4 marks for analysis):**\n- SSTF yields the *minimum* head movement (236 cylinders) because it is a *greedy* algorithm that always picks the nearest pending request.\n- **Caveat (3 marks):** SSTF is *susceptible to starvation* of far-away requests, and is *not* implementable in practice because it requires continuous knowledge of the future request queue. Hence, **LOOK** is often the practical choice — it gives a near-optimal result (299) while guaranteeing no starvation.\n\n---\n\n> [!WARNING]\n> **🚨 KTU Examiner's Pitfall Callout — Where Students Lose Marks**\n> 1. **Forgetting the C-SCAN circular jump:** Many students compute C-SCAN like SCAN and miss the $\\vert 199 - 0 \\vert = 199$ jump. **This is a 5-mark deduction** in typical valuation.\n> 2. **Confusing LOOK and SCAN end-points:** LOOK stops at the *last request*; SCAN goes to the *disk end (199)*. Mixing them up loses 2–3 marks.\n> 3. **Not showing the *order of service* explicitly:** A bare numerical answer without the visited sequence is considered **incomplete** — KTU evaluators require the *service order table*.\n> 4. **Off-by-one in SSTF:** At head 67, the two nearest are 65 and 37 (both 30 distance). The choice depends on tie-breaking; the canonical KTU answer for this problem is 37. Always check your greedy selection at every step.\n> 5. **Units not mentioned:** Always write \"cylinders\" or \"tracks\" — bare numbers are penalized in viva.\n\n---\n\n## 🔁 Topic Recap & Rapid-Revision Checklist\n\n- ✅ Disk scheduling **minimizes seek time** by ordering pending I/O requests.\n- ✅ **FCFS** = arrival order, **no optimization**, no starvation, fair but slow.\n- ✅ **SSTF** = greedy nearest-first, **minimum THM in this problem (236)**, but starvation-prone.\n- ✅ **SCAN** = elevator sweep to disk edge (199), then reverse. **No starvation**.\n- ✅ **C-SCAN** = sweep to 199, then **circular jump** to 0, sweep up again. **Uniform wait**.\n- ✅ **LOOK** = like SCAN but reverses at the **last request** (saves edge traversal).\n- ✅ **C-LOOK** = circular jump to **lowest pending request** (not 0), then continue up.\n- ✅ The defining equation: $THM = \\sum \\vert r_{i+1} - r_i \\vert$ for the actual sequence serviced.\n- ✅ For KTU 2024 labs, always **display the service-order table** before computing the sum.\n- ✅ Standard disk range in KTU problems: **0–199** (200 cylinders), unless explicitly stated otherwise.\n- ✅ LOOK/C-LOOK are **practically preferred** in real OSes (Linux, Windows) due to balanced fairness + low THM.\n- ✅ Modern **SSDs do not require disk scheduling** — only **HDDs and sequential-access media**.\n\n---\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "information-technology-5-pcitl408-mExperiment 2-t11",
          "title": "Implement file organization technique (single level and multi-level directory) and allocation strategies (sequential, linked and indexed)",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCITL408 - OPERATING SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Implement file organization technique (single level and multi-level directory) and allocation strategies (sequential, linked and indexed)\n\n<!-- SECTION_1_START -->\n\n# 📘 MODULE EXPERIMENT 2 — FILE ORGANIZATION & DISK ALLOCATION\n\n## 1.1 Core Technical Definition & Intuitive Overview\n\n### 1.1.1 What is File Organization?\n\n> [!IMPORTANT]\n> **Formal Definition (KTU 2024 Syllabus Terminology):**\n> *File Organization* refers to the logical method by which the records of a file are arranged on secondary storage (disk). It determines how data is **stored**, **retrieved**, **updated**, and **protected** by the Operating System. File organization encompasses two parallel concerns: (a) the **Directory Structure** (how files are *named* and *grouped*), and (b) the **Physical Allocation Strategy** (how file *blocks* are placed on disk).\n\n> [!NOTE]\n> **Why This Experiment Matters:**\n> The OS must be able to locate a file by its **name** (Directory lookup) and then translate the logical byte offset into a physical disk address (Allocation strategy). Experiment 2 forces you to implement *both* layers — the name layer (Single/Multi-level directory) and the storage layer (Sequential/Linked/Indexed) — typically using `C` and a simulated file as a \"virtual disk.\"\n\n---\n\n### 1.1.2 Directory Organization — Intuitive Analogy\n\nThink of a **University Records Office**:\n\n| Concept | Real-World Analogy | Computer Equivalent |\n|---|---|---|\n| Single-Level Directory | A **single large table** where every student's file is dumped. Names must be unique university-wide. | All file names in one root folder (`/`) — no two files can have the same name even if owned by different users. |\n| Two-Level Directory | Each **Department** has its own cabinet; inside, students are filed. \"Rahul\" can exist in CSE *and* ECE. | A `Master File Directory (MFD)` pointing to one `User File Directory (UFD)` per user. |\n| Tree-Structured (Multi-Level) Directory | The University has a **Cabinet → Department → Year → Section → Student** hierarchy. | The classic `/home/cse/s4/A/rollno.txt` Linux/Windows hierarchy. |\n| Acyclic-Graph Directory | A student's academic record is **shared** in multiple sections (no duplicates physically). | Hard links / symbolic links — same inode referenced twice. |\n\n> [!TIP]\n> **KTU High-Yield Term:** A *Multi-Level Directory* is officially defined as a **hierarchical** structure in which each directory (except root) has exactly **one** parent, forming a **tree**. When **shared subdirectories** are allowed, it becomes an *Acyclic-Graph Directory*. A *General-Graph Directory* (which is **not** asked in the syllabus) permits cycles and is generally avoided.\n\n---\n\n### 1.1.3 Disk Allocation Strategy — Intuitive Analogy\n\nImagine a **hotel with 200 rooms** and a guest booking system:\n\n- **Sequential Allocation** → The guest is given **rooms 47, 48, 49, 50** (contiguous block). Easy to find, but hard to extend.\n- **Linked Allocation** → The guest's name is written on the **door of room 47**, pointing to room 112, then room 8, then room 199. The doors are chained. Order is preserved, but to find room 50 you must walk through 47→112→8→199.\n- **Indexed Allocation** → The guest gets a **key-card at reception (index block)** that lists all assigned rooms: `{47, 112, 8, 199}`. Reception can jump directly to any room.\n\n> [!IMPORTANT]\n> **Core Definitions (KTU Exam Gold):**\n> - **Sequential Allocation:** A file occupies a set of **contiguous blocks** on disk. The starting address and length are stored in the directory entry.\n> - **Linked Allocation:** Each disk block contains the file's data *and* a **pointer to the next block**. The directory entry stores only the **starting block**.\n> - **Indexed Allocation:** A special **index block** stores pointers to all the data blocks belonging to a file. The directory entry stores the **pointer to the index block**.\n\n### 1.1.4 Standard Performance Metrics (Bold for Emphasis)\n\nThe three allocation strategies are routinely graded on these metrics in KTU papers:\n\n- **External Fragmentation**\n- **Internal Fragmentation**\n- **Random Access Time (Seek)**\n- **Sequential Access Throughput**\n\nEach strategy will be benchmarked against these **four** parameters throughout Section 2.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n# 🧠 DEEP THEORETICAL ANALYSIS & KTU HIGH-YIELD FORMULA SHEET\n\n## 2.1 Directory Organization — Detailed Operational Logic\n\n### 2.1.1 Single-Level Directory\n\n- **Structure:** All files of all users exist in **one flat list**.\n- **Lookup:** Linear scan of the Master File Directory (MFD) for the file name.\n- **Constraints:** File names must be **globally unique**.\n- **Search Cost:** $O(n)$ where $n$ is total files in the system.\n- **Sharing:** Not supported natively.\n- **Drawback:** Two users cannot have a file with the same name — even if logically distinct.\n\n**Step-by-step internal mechanics:**\n1. User issues a system call `open(\"report.txt\", O_RDONLY)`.\n2. Kernel scans MFD entry-by-entry comparing strings.\n3. If match is found → inode is returned to the process.\n4. If no match → `-1` returned, `errno = ENOENT`.\n\n### 2.1.2 Two-Level Directory\n\n- **Structure:** One **MFD (Master File Directory)** + many **UFDs (User File Directories)**, one per user.\n- **Lookup:** First locate the user's UFD via MFD using `username`, then scan UFD for the file name.\n- **Search Cost:** $O(u + f)$ where $u$ is users, $f$ is files in user's UFD.\n- **Path Specification:** Required prefix, e.g., `username:filename`.\n- **Limitation:** A user **cannot** conveniently share or nest subdirectories.\n\n### 2.1.3 Tree-Structured (Multi-Level) Directory\n\n- **Structure:** A **root directory** at the top, with directories as nodes and files as leaves. A path is a sequence of names from root to target, separated by `/` (UNIX) or `\\` (Windows).\n- **Special Names:** `.` (current), `..` (parent).\n- **Lookup:** Path-name resolution traverses the tree **left-to-right** (UNIX) or **right-to-left** (Windows — drives at start).\n- **Search Cost (Average):** $O(d \\cdot b)$ where $d$ is the depth of the path and $b$ is the average branching factor.\n- **Operations Supported:** `mkdir`, `rmdir`, `chdir`, `link`, `unlink`.\n\n### 2.1.4 Acyclic-Graph Directory\n\n- **Structure:** Tree + **shared nodes** (links).\n- **Implementation:** Two kinds of links — **Hard links** (multiple directory entries point to the *same inode*; a reference counter prevents premature deletion) and **Symbolic / Soft links** (a special file that *contains* the path name of the target; breaks if target is deleted).\n- **Reference Counter Rule:**\n$$\\text{File is removed from disk} \\iff \\text{Reference Counter} = 0$$\n- **Problem:** Cycles (a subdir links to its own ancestor) can cause infinite loops during traversal.\n\n### 2.1.5 General-Graph Directory\n\n- Same as Acyclic-Graph but **cycles allowed**.\n- **Required for KTU awareness only** — must be handled by *garbage collection* or *reference counting with cycle detection*. **Not part of Module 2 implementation.**\n\n---\n\n## 2.2 Disk Allocation Strategies — Detailed Operational Logic\n\n### 2.2.1 Sequential (Contiguous) Allocation\n\n- **Mechanism:** A file of $n$ blocks is stored at blocks $b, b+1, b+2, \\ldots, b+n-1$.\n- **Directory Entry Holds:** `(starting_block_address, length_in_blocks)`.\n- **Address Translation:** For a logical offset $k$ bytes, with block size $B$:\n$$\\text{Physical Block} = b + \\left\\lfloor \\frac{k}{B} \\right\\rfloor$$\n- **Pros:** Excellent **random access** (only 1 seek for any byte), highest **throughput** for sequential I/O.\n- **Cons:** Suffers from **external fragmentation**; file size must be declared at creation; growing a file may require a full relocation.\n\n### 2.2.2 Linked Allocation\n\n- **Mechanism:** Each block holds a **pointer** (typically 4 bytes) to the next block. The last block's pointer is a sentinel value (e.g., `-1` or `NULL`).\n- **Directory Entry Holds:** `(starting_block_address)` only.\n- **Address Translation:** To reach block $i$, the OS must follow $i$ pointers from the start block → $\\Theta(i)$ disk seeks.\n- **FAT (File Allocation Table) Variant:** A table stored in memory holds *all* pointers, turning translation into $O(1)$ and making linked allocation practical (used in MS-DOS / Windows).\n- **Pros:** No external fragmentation; simple to extend.\n- **Cons:** Random access is slow without FAT; pointer overhead per block; reliability issue if a pointer is lost.\n\n### 2.2.3 Indexed Allocation\n\n- **Mechanism:** A dedicated **index block** is allocated for each file. The index block contains an array of pointers to all data blocks of the file.\n- **Directory Entry Holds:** `(pointer_to_index_block)`.\n- **Address Translation:** To reach block $i$ of the file → read the index block, look up the $i$-th pointer.\n- **Multi-Level Indexed (UNIX `inode`):** Uses **direct blocks**, **single-indirect**, **double-indirect**, **triple-indirect** pointers to overcome the limit of one index block.\n\n> [!IMPORTANT]\n> **Block-Size Problem in Indexed Allocation:** A fixed index block can only address $N = \\frac{B}{P}$ files, where $B$ is block size and $P$ is pointer size. With $B = 4096$ bytes and $P = 4$ bytes, $N = 1024$ blocks. Solution: combine with multi-level indexing (covered in OS theory modules — not in lab, but KTU asks it).\n\n---\n\n## 2.3 KTU Formula Sheet & Cheat-Sheet Table\n\n> [!NOTE]\n> Use this table as your **revision-1-page-sheet** before the lab viva. No vertical pipes are used inside cells — only `\\vert` and `\\cdot` notation to keep the markdown table intact.\n\n| Parameter | Sequential | Linked | Indexed |\n|---|---|---|---|\n| **Directory Entry Fields** | `start_block, length` | `start_block` | `index_block_ptr` |\n| **Random Access Time** | $O(1)$ — only 1 seek | $O(i)$ — seek $i$ blocks | $O(1)$ — 1 seek + memory read |\n| **Sequential Throughput** | **Highest** (single seek, multi-block DMA) | Medium (seek per block) | Medium (seek per block) |\n| **External Fragmentation** | **Yes — severe** | None | None |\n| **Internal Fragmentation** | None (usually) | None (overhead per block) | Yes — partial index block unused |\n| **File Size Limit** | Disk exhaustion | Disk exhaustion | $\\vert \\text{index block capacity} \\vert$ |\n| **Reliability** | High (no pointers to corrupt) | Low (one bad pointer = data loss) | Medium (index block is single point of failure) |\n| **Typical Use** | CD-ROM, DVD, archival tapes | FAT / NTFS (with table) | UNIX `inode`, NTFS MFT |\n| **Formula — Max File Size** | $L_{\\max} = N \\cdot B$ | $L_{\\max} = N \\cdot (B - P)$ | $L_{\\max} = N \\cdot B$ where $N = B \\div P$ |\n| **Fragmentation Type** | External | None (pointer overhead is internal per block) | Internal (partial index) |\n\n**Legend:** $B$ = block size (bytes), $P$ = pointer size (bytes), $N$ = number of blocks addressable, $L_{\\max}$ = maximum file size, $i$ = block index being accessed.\n\n### 2.4 Why Does This Matter in Industry?\n\n- **Databases (PostgreSQL, Oracle)** still use *contiguous* allocation patterns through tablespaces and clustered indexes for sequential scan speed.\n- **NTFS (Windows)** uses a refined indexed approach — the **MFT (Master File Table)** stores file metadata + small files *inline*, and uses **runs** (contiguous extents) within the B-Tree data structure.\n- **ext4 / XFS (Linux)** use a hybrid: block groups with **indexed allocation** for metadata and a **delayed-allocation** strategy to choose contiguous extents.\n- **Solid-State Drives (SSDs)** reduce the importance of seek time, but the *write amplification* problem makes sequential allocation still preferred for hot files.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n# 🧮 STEP-BY-STEP DERIVATIONS, ANALYTICAL MATRIX & SAMPLE C-IMPLEMENTATION\n\n## 3.1 Analytic Derivation of Max File Size\n\n**Given:**\n- Block size $B = 4096$ bytes\n- Pointer size $P = 4$ bytes\n- Disk has $D = 2^{32}$ addressable blocks\n\n**Derivation (for Indexed Allocation):**\n\n$$\n\\begin{aligned}\n\\text{Pointers per index block } N &= \\frac{B}{P} \\\\\n&= \\frac{4096 \\text{ bytes}}{4 \\text{ bytes per pointer}} \\\\\n&= 1024 \\text{ pointers}\n\\end{aligned}\n$$\n\n$$\n\\begin{aligned}\n\\text{Maximum file size } L_{\\max} &= N \\cdot B \\\\\n&= 1024 \\times 4096 \\\\\n&= 2^{10} \\times 2^{12} \\\\\n&= 2^{22} \\text{ bytes} \\\\\n&= 4 \\text{ MiB}\n\\end{aligned}\n$$\n\n**Row 1 (logic):** The index block occupies exactly one disk block of size $B$. Each pointer consumes $P$ bytes. So the number of file blocks the index can address is the block size divided by pointer size.\n\n**Row 2 (logic):** Maximum file size equals the number of addressable blocks multiplied by block size. Each block contributes $B$ bytes of file data.\n\n**Row 3 (logic):** Substituting the numbers gives $2^{10} \\times 2^{12} = 2^{22} = 4 \\text{ MiB}$.\n\n> [!NOTE]\n> **Conclusion:** A *single-level* indexed allocation with 4-byte pointers and 4 KiB blocks caps a file at 4 MiB. UNIX breaks this limit by chaining a **double-indirect** block that points to other index blocks. This is why `inode` in Linux can address **terabytes** of data.\n\n---\n\n## 3.2 Worked Example — Contiguous Allocation Address Translation\n\n**File `F` starts at block 17, length = 5 blocks, block size = 1024 bytes.**\n**Process requests a read at byte offset 2500.**\n\n**Derivation:**\n\n$$\n\\begin{aligned}\n\\text{Block number within file} &= \\left\\lfloor \\frac{\\text{byte offset}}{B} \\right\\rfloor \\\\\n&= \\left\\lfloor \\frac{2500}{1024} \\right\\rfloor \\\\\n&= \\lfloor 2.44 \\rfloor \\\\\n&= 2 \\text{ (0-indexed)}\n\\end{aligned}\n$$\n\n$$\n\\begin{aligned}\n\\text{Physical block on disk} &= \\text{start\\_block} + \\text{block within file} \\\\\n&= 17 + 2 \\\\\n&= 19\n\\end{aligned}\n$$\n\n$$\n\\begin{aligned}\n\\text{Offset within physical block} &= 2500 \\bmod 1024 \\\\\n&= 2500 - 2 \\times 1024 \\\\\n&= 2500 - 2048 \\\\\n&= 452 \\text{ bytes}\n\\end{aligned}\n$$\n\n**Reading logic:** The disk head moves to block 19, waits for the platter to rotate so byte 452 is under the read head, then transfers bytes 452 → 1023 of that block (572 bytes — the request may span into block 20 as well).\n\n---\n\n## 3.3 Analytical Matrix — Choosing the Best Strategy (KTU Favourite Question)\n\n| Use-Case | Best Strategy | Reasoning |\n|---|---|---|\n| CD-ROM ISO image distribution | **Sequential** | Read-only, no fragmentation issues, fastest streaming. |\n| Log file appending on Linux | **Linked** (via FAT) | Append = add to end, no relocation, no external frag. |\n| Random-access database file (B-Tree) | **Indexed** | $O(1)$ random access to any page is mandatory. |\n| Multimedia streaming (large movie) | **Sequential** (with extent) | Highest throughput, predictable seek. |\n| Virtual memory swap file | **Indexed** | Kernel needs to access any page instantly. |\n| Small config files (< 4 KiB) | **Indexed** (with inline storage) | Whole file fits inside the index block. |\n\n---\n\n## 3.4 Sample C-Implementation (Reference Algorithm for Lab Record)\n\n> [!IMPORTANT]\n> The KTU lab record must include a working C program. The following snippets show the *minimal algorithmic skeleton* the examiner expects. You may extend it to a complete menu-driven program. All file names use `FILE *fp` from `<stdio.h>`.\n\n### 3.4.1 Simulated Disk Block (Common to All Strategies)\n\n```c\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\n#define BLOCK_SIZE   512\n#define DISK_BLOCKS  1024\n#define MAX_FILES    50\n#define MAX_NAME     30\n\n/* A simulated \"disk\" — an array of fixed-size blocks stored in memory */\ntypedef struct {\n    char data[BLOCK_SIZE];\n} DiskBlock;\n\nDiskBlock disk[DISK_BLOCKS];\n\n/* A bitmap to mark which blocks are free (1 = used, 0 = free) */\nint block_status[DISK_BLOCKS] = {0};\n```\n\n### 3.4.2 Single-Level Directory Entry\n\n```c\ntypedef struct {\n    char fname[MAX_NAME];           /* file name */\n    int  start_block;               /* starting physical block */\n    int  num_blocks;                /* number of blocks */\n} DirectoryEntry;\n\nDirectoryEntry MFD[MAX_FILES];      /* Master File Directory */\nint file_count = 0;\n```\n\n### 3.4.3 Sequential Allocation Functions\n\n```c\nint find_contiguous(int n) {\n    int free_run = 0, start = -1;\n    for (int i = 0; i < DISK_BLOCKS; i++) {\n        if (block_status[i] == 0) {\n            if (free_run == 0) start = i;\n            free_run++;\n            if (free_run == n) return start;\n        } else {\n            free_run = 0;\n        }\n    }\n    return -1;   /* not enough contiguous space */\n}\n\nvoid create_file_seq(char *name, int size_in_blocks) {\n    int start = find_contiguous(size_in_blocks);\n    if (start == -1) {\n        printf(\"Error: Not enough contiguous space (external fragmentation).\\n\");\n        return;\n    }\n    for (int i = 0; i < size_in_blocks; i++) block_status[start + i] = 1;\n    strcpy(MFD[file_count].fname, name);\n    MFD[file_count].start_block = start;\n    MFD[file_count].num_blocks = size_in_blocks;\n    file_count++;\n    printf(\"Sequential file '%s' created. Starts at block %d.\\n\", name, start);\n}\n```\n\n### 3.4.4 Linked Allocation — Modified Block Structure\n\n```c\ntypedef struct {\n    char data[BLOCK_SIZE - sizeof(int)];\n    int  next;                      /* -1 = end of file */\n} LinkedBlock;\n```\n\n```c\nvoid create_file_linked(char *name, int size_in_blocks) {\n    int prev = -1, first = -1;\n    for (int k = 0; k < size_in_blocks; k++) {\n        int b;\n        for (b = 0; b < DISK_BLOCKS; b++) if (block_status[b] == 0) break;\n        if (b == DISK_BLOCKS) { printf(\"Disk full.\\n\"); return; }\n        block_status[b] = 1;\n        ((LinkedBlock*)&disk[b])->next = -1;\n        if (k == 0) first = b; else ((LinkedBlock*)&disk[prev])->next = b;\n        prev = b;\n    }\n    strcpy(MFD[file_count].fname, name);\n    MFD[file_count].start_block = first;\n    MFD[file_count].num_blocks  = size_in_blocks;\n    file_count++;\n    printf(\"Linked file '%s' created. Starts at block %d.\\n\", name, first);\n}\n```\n\n### 3.4.5 Indexed Allocation\n\n```c\ntypedef struct {\n    int  pointers[BLOCK_SIZE / sizeof(int)];\n} IndexBlock;\n```\n\n```c\nvoid create_file_indexed(char *name, int size_in_blocks) {\n    /* 1. allocate index block */\n    int idx;\n    for (idx = 0; idx < DISK_BLOCKS; idx++) if (block_status[idx] == 0) break;\n    block_status[idx] = 1;\n    IndexBlock *ib = (IndexBlock *)&disk[idx];\n\n    /* 2. allocate data blocks, register pointers */\n    int k;\n    for (k = 0; k < size_in_blocks; k++) {\n        int b;\n        for (b = 0; b < DISK_BLOCKS; b++) if (block_status[b] == 0) break;\n        if (b == DISK_BLOCKS) { printf(\"Disk full.\\n\"); return; }\n        block_status[b] = 1;\n        ib->pointers[k] = b;\n    }\n    ib->pointers[k] = -1;       /* sentinel */\n    strcpy(MFD[file_count].fname, name);\n    MFD[file_count].start_block = idx;       /* points to INDEX block */\n    MFD[file_count].num_blocks  = size_in_blocks;\n    file_count++;\n    printf(\"Indexed file '%s' created. Index block at %d.\\n\", name, idx);\n}\n```\n\n### 3.4.6 Multi-Level Directory (User Folders)\n\n```c\ntypedef struct {\n    char uname[MAX_NAME];\n    DirectoryEntry ufd[MAX_FILES];  /* per-user file directory */\n    int  ufd_count;\n} UserDir;\n\nUserDir users[20];\nint user_count = 0;\n\nint find_user(char *uname) {\n    for (int i = 0; i < user_count; i++)\n        if (strcmp(users[i].uname, uname) == 0) return i;\n    return -1;\n}\n```\n\nThe complete menu-driven program should have options: `1) Create User`, `2) Create File (Sequential/Linked/Indexed)`, `3) Display Directory`, `4) Delete File`, `5) Exit`. This is the **minimum expectation** for a 14-mark lab record answer.\n\n---\n\n## 3.5 Complexity Derivation Table\n\n| Operation | Single-Level | Multi-Level (Tree) | Seq Alloc | Linked Alloc | Indexed Alloc |\n|---|---|---|---|---|---|\n| Create File | $O(n)$ (scan MFD) | $O(d)$ (walk path) | $O(n)$ to find contiguous run | $O(n \\cdot b)$ | $O(n \\cdot b)$ |\n| Delete File | $O(n)$ | $O(d)$ | $O(n)$ mark free | $O(n)$ walk chain | $O(n)$ free data + index |\n| Search File | $O(n)$ | $O(d \\cdot b)$ | — | — | — |\n| Read Block $i$ | — | — | $O(1)$ | $O(i)$ (no FAT) / $O(1)$ (with FAT) | $O(1)$ |\n| Write Append | — | — | may fail / relocate | $O(1)$ find free + link | $O(1)$ add to index |\n\nHere $n$ = total entries, $b$ = blocks, $d$ = directory depth.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n# 🗺️ STRUCTURAL DIAGRAMS & SCHEMATICS\n\n## 4.1 Single-Level Directory — Block Diagram\n\n```mermaid\ngraph TD\n    A[\"Master File Directory MFD\"] --> B1[\"Entry report.txt  start=10  len=4\"]\n    A --> B2[\"Entry data.csv  start=22  len=8\"]\n    A --> B3[\"Entry image.png  start=40  len=12\"]\n    A --> B4[\"Entry notes.txt  start=60  len=3\"]\n    B1 --> C1[\"Disk Blocks 10 11 12 13\"]\n    B2 --> C2[\"Disk Blocks 22 to 29\"]\n    B3 --> C3[\"Disk Blocks 40 to 51\"]\n    B4 --> C4[\"Disk Blocks 60 61 62\"]\n```\n\n**Interpretation:** Every file in the system is a child of the single MFD. Lookup is a linear scan over the entries `B1` … `B4`.\n\n---\n\n## 4.2 Multi-Level (Tree) Directory — Block Diagram\n\n```mermaid\ngraph TD\n    Root[\"root dir /\"] --> Home[\"home\"]\n    Root --> Bin[\"bin\"]\n    Root --> Etc[\"etc\"]\n    Home --> U1[\"alice\"]\n    Home --> U2[\"bob\"]\n    U1 --> F1[\"report.txt\"]\n    U1 --> F2[\"project.c\"]\n    U1 --> Sub1[\"research\"]\n    Sub1 --> F3[\"paper.pdf\"]\n    Sub1 --> F4[\"data.txt\"]\n    U2 --> F5[\"hello.c\"]\n    U2 --> F6[\"notes.md\"]\n    Bin --> F7[\"ls\"]\n    Bin --> F8[\"cat\"]\n    Etc --> F9[\"passwd\"]\n```\n\n**Interpretation:** The same filename `data.txt` could legally exist under `home/alice/research/` *and* somewhere else — the *path* makes it unique.\n\n---\n\n## 4.3 Two-Level Directory — Block Diagram\n\n```mermaid\ngraph TD\n    MFD[\"MFD Master File Directory\"] --> U1[\"alice\"]\n    MFD --> U2[\"bob\"]\n    MFD --> U3[\"carol\"]\n    U1 --> A1[\"test.c\"]\n    U1 --> A2[\"readme.md\"]\n    U2 --> B1[\"hello.c\"]\n    U2 --> B2[\"readme.md\"]\n    U3 --> C1[\"proj.py\"]\n```\n\n**Interpretation:** `bob/readme.md` and `alice/readme.md` are *different* files even though they share the same leaf name. The path prefix `username:` disambiguates.\n\n---\n\n## 4.4 Sequential (Contiguous) Allocation — Disk Map\n\n```mermaid\ngraph LR\n    subgraph DiskBlocks[\"Physical Disk Blocks 0 to 99\"]\n        direction LR\n        D0[\"Block 0\"]:::free\n        D1[\"Block 1\"]:::free\n        D2[\"Block 2-6\"]:::fileA\n        D3[\"Block 7\"]:::free\n        D4[\"Block 8-12\"]:::fileB\n        D5[\"Block 13-14\"]:::free\n        D6[\"Block 15-22\"]:::fileC\n        D7[\"Block 23-24\"]:::free\n    end\n    MFD2[\"MFD entry fileA start=2 len=5\"] --> D2\n    MFD2_2[\"MFD entry fileB start=8 len=5\"] --> D4\n    MFD2_3[\"MFD entry fileC start=15 len=8\"] --> D6\n    classDef free fill:#E0E0E0,stroke:#888,color:#000\n    classDef fileA fill:#FFD7B5,stroke:#E67E22,color:#000\n    classDef fileB fill:#B5DFFF,stroke:#2E86C1,color:#000\n    classDef fileC fill:#C8E6C9,stroke:#2E7D32,color:#000\n```\n\n**Interpretation:** Files occupy *contiguous* runs. Free holes between files cause external fragmentation. File `fileB` starts at block 8 and ends at block 12.\n\n---\n\n## 4.5 Linked Allocation — Disk Map\n\n```mermaid\ngraph LR\n    MFD3[\"MFD entry fileA start=3 next=NULL\"] --> B3[\"Block 3 data next=47\"]\n    B3 --> B47[\"Block 47 data next=12\"]\n    B47 --> B12[\"Block 12 data next=88\"]\n    B12 --> B88[\"Block 88 data next=NULL\"]\n    style MFD3 fill:#FFF3B0,stroke:#B7950B,color:#000\n    style B3 fill:#D5F5E3,stroke:#239B56,color:#000\n    style B47 fill:#D5F5E3,stroke:#239B56,color:#000\n    style B12 fill:#D5F5E3,stroke:#239B56,color:#000\n    style B88 fill:#D5F5E3,stroke:#239B56,color:#000\n```\n\n**Interpretation:** File `fileA` occupies blocks `3 → 47 → 12 → 88` — scattered across disk. The MFD only stores the head pointer (block 3); each block carries a `next` pointer to the following block. To read the 3rd block of the file, the OS follows 2 pointers (3 → 47 → 12).\n\n---\n\n## 4.6 Indexed Allocation — Disk Map\n\n```mermaid\ngraph TD\n    MFD4[\"MFD entry fileA start=100 index_block\"] --> IB[\"Block 100 INDEX BLOCK\"]\n    IB --> P0[\"ptr 0 = Block 3\"]\n    IB --> P1[\"ptr 1 = Block 47\"]\n    IB --> P2[\"ptr 2 = Block 12\"]\n    IB --> P3[\"ptr 3 = Block 88\"]\n    IB --> P4[\"ptr 4 = NULL\"]\n    P0 --> D3[\"Block 3 DATA\"]\n    P1 --> D47[\"Block 47 DATA\"]\n    P2 --> D12[\"Block 12 DATA\"]\n    P3 --> D88[\"Block 88 DATA\"]\n    style MFD4 fill:#FFF3B0,stroke:#B7950B,color:#000\n    style IB fill:#FAD7A0,stroke:#CA6F1E,color:#000\n    style P0 fill:#D6EAF8,stroke:#2874A6,color:#000\n    style P1 fill:#D6EAF8,stroke:#2874A6,color:#000\n    style P2 fill:#D6EAF8,stroke:#2874A6,color:#000\n    style P3 fill:#D6EAF8,stroke:#2874A6,color:#000\n    style P4 fill:#D6EAF8,stroke:#2874A6,color:#000\n    style D3 fill:#D5F5E3,stroke:#239B56,color:#000\n    style D47 fill:#D5F5E3,stroke:#239B56,color:#000\n    style D12 fill:#D5F5E3,stroke:#239B56,color:#000\n    style D88 fill:#D5F5E3,stroke:#239B56,color:#000\n```\n\n**Interpretation:** The MFD stores a single pointer to the index block (block 100). The index block holds an array of pointers (0 to 4) to data blocks scattered on disk. Random access to block $i$ requires only **one** read of the index block followed by **one** read of the target data block.\n\n---\n\n## 4.7 Lab Experiment — Block-Level Functional Architecture Flow\n\n```mermaid\ngraph TD\n    UI[\"User Menu Interface\"] --> M[\"Switch on choice\"]\n    M --> C1[\"1 Create User\"]\n    M --> C2[\"2 Create File with Strategy\"]\n    M --> C3[\"3 Display Directory Tree\"]\n    M --> C4[\"4 Search File\"]\n    M --> C5[\"5 Delete File\"]\n    M --> C6[\"6 Show Disk Block Map\"]\n    M --> C7[\"7 Exit\"]\n    C1 --> UD[\"UserDir Array Insertion\"]\n    C2 --> SA[\"Strategy Switch\"]\n    SA --> S1[\"Sequential find_contiguous\"]\n    SA --> S2[\"Linked chain blocks\"]\n    SA --> S3[\"Indexed allocate index block\"]\n    C3 --> TR[\"Recursive Tree Print\"]\n    C4 --> LK[\"Path Resolution Lookup\"]\n    C5 --> FR[\"Free Blocks + Remove Entry\"]\n    C6 --> BM[\"Print block_status bitmap\"]\n    UD --> DS[\"In-Memory Disk Simulation\"]\n    S1 --> DS\n    S2 --> DS\n    S3 --> DS\n    TR --> DS\n    LK --> DS\n    FR --> DS\n    BM --> DS\n    DS --> OUT[\"Console Output\"]\n```\n\n**Interpretation:** The lab program is essentially a tiny OS. The *menu layer* is the syscall interface; the *strategy switch* is the VFS; the *in-memory disk simulation* is the block device driver. Students often lose marks for not separating these layers cleanly — a *modular* C program scores higher.\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n# 🎯 KTU 2024 SCHEME EXAMINATION QUESTION BANK & TOPIC RECAP\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### **Q1. `[KTU University Exam - Dec 2023]`**\n**Differentiate between Single-Level and Two-Level Directory structures. State one limitation of each.**\n\n> **Model Answer (3 Marks):**\n\n| Aspect | Single-Level Directory | Two-Level Directory |\n|---|---|---|\n| **Structure** | One Master File Directory (MFD) holding all files of all users. | One MFD + separate User File Directory (UFD) per user. |\n| **Naming** | File names must be globally unique across all users. | File names unique only within a user's UFD. Path is `username:filename`. |\n| **Limitation** | No two users can create a file with the same name, even if their data is unrelated. | A user cannot create sub-directories inside their own UFD; sharing files across users is not natively supported. |\n\n*(Examiner's note: Award 1 mark for each correct difference, 1 mark for the limitation pair.)*\n\n---\n\n### **Q2. `[KTU University Exam - May 2024]`**\n**List any three differences between Linked and Indexed file allocation. Which one is used by the UNIX `inode` system?**\n\n> **Model Answer (3 Marks):**\n\n| S.No. | Linked Allocation | Indexed Allocation |\n|---|---|---|\n| 1 | Directory stores only the **starting block** of the file. | Directory stores the **pointer to the index block**. |\n| 2 | Random access requires following $i$ pointers — $O(i)$ time. | Random access reads the index block once — $O(1)$ time. |\n| 3 | No internal fragmentation in the data area; pointer overhead inside each block. | Internal fragmentation inside the index block if file is small. |\n\n**UNIX `inode` uses Indexed Allocation** (with a multi-level variant: direct, single-indirect, double-indirect, triple-indirect pointers).\n\n*(Examiner's note: Mention of `inode` with multi-level indexing is the \"3rd mark differentiator\" — students who only say \"indexed\" get 2/3.)*\n\n---\n\n## 5.2 Part B Questions (14 Marks — Internal Choice Provided)\n\n> **Internal Choice rule:** KTU ESE provides an OR option. Below, **Question A** and **Question B** are independent, full-mark alternatives covering the same module outcomes.\n\n---\n\n### **Question A (14 Marks) — `[KTU University Exam - Dec 2022]`**\n\n**(a)** Explain the **Single-Level Directory** and **Tree-Structured (Multi-Level) Directory** organizations with neat diagrams. Discuss the operations **Create**, **Delete**, and **Search** in each. **(7 Marks)**\n\n**(b)** Implement a C program for **Sequential File Allocation** strategy. Show the data structure and the `create_file()` function. Demonstrate how **external fragmentation** is detected. **(7 Marks)**\n\n---\n\n#### **Model Solution (a) — 7 Marks**\n\n**Single-Level Directory (3.5 marks):**\n\n- **Definition:** All files in the system are stored in one common directory called the **Master File Directory (MFD)**. The MFD contains one entry per file with fields: `name`, `protection bits`, `starting block`, `length`, etc.\n- **Diagram (already shown in 4.1)** — re-emphasize for marks.\n- **Create Operation:** Allocate a free entry in MFD; write metadata; if file allocation is sequential, find a free contiguous run of `n` blocks and mark them; update directory. Complexity: $O(n)$ to scan MFD.\n- **Delete Operation:** Free the blocks in the bitmap; shift remaining entries up; decrement count.\n- **Search Operation:** Linear scan of MFD comparing file names. Complexity: $O(N)$ where $N$ is the number of files.\n- **Limitation:** Name clashes across users; no logical grouping.\n\n**Tree-Structured (Multi-Level) Directory (3.5 marks):**\n\n- **Definition:** Hierarchical organization; root is `/`; each directory contains files and subdirectories. Path names are absolute (from root) or relative (from `cwd`).\n- **Operations:**\n  - `Create(user, path)`: Walk the path; if any intermediate directory is missing → error; allocate entry in the leaf directory.\n  - `Delete(user, path)`: Walk the path; if target is a directory and not empty → error; free the entry; recursively free children if needed.\n  - `Search(user, path)`: Walk the path left-to-right (UNIX); at each level perform linear scan of that directory's entries.\n- **Advantage over Single-Level:** Logical grouping, same filename in different folders, permission isolation.\n- **Disadvantage:** Slow search if directories are not indexed (UNIX `ext4` keeps a hash index called *htree* for fast lookup).\n\n---\n\n#### **Model Solution (b) — 7 Marks**\n\n**Data Structure (1 mark):**\n```c\ntypedef struct {\n    char fname[30];\n    int  start_block;\n    int  num_blocks;\n} DirEntry;\nDirEntry MFD[50];\nint block_status[1024];   /* 0 = free, 1 = used */\n```\n\n**`create_file()` function (3 marks):**\n```c\nvoid create_file_seq(char *name, int size) {\n    int start = find_contiguous(size);\n    if (start == -1) {\n        printf(\"Error: No contiguous space of size %d available.\\n\", size);\n        return;\n    }\n    for (int i = 0; i < size; i++) block_status[start + i] = 1;\n    strcpy(MFD[file_count].fname, name);\n    MFD[file_count].start_block = start;\n    MFD[file_count].num_blocks  = size;\n    file_count++;\n}\n```\n\n**Detecting External Fragmentation (3 marks):**\n\nExternal fragmentation is detected by computing the **largest free contiguous run** in the bitmap and comparing it with the request size. If the largest run is smaller than the request, fragmentation prevents allocation even though total free space is sufficient.\n\n```c\nint largest_free_run() {\n    int max = 0, cur = 0;\n    for (int i = 0; i < DISK_BLOCKS; i++) {\n        if (block_status[i] == 0) { cur++; if (cur > max) max = cur; }\n        else cur = 0;\n    }\n    return max;\n}\n```\n\n**Conclusion statement (carry 1 mark if present):**\n> Sequential allocation suffers from *external fragmentation* — the inability to use scattered free blocks to satisfy a contiguous request. Compaction (relocating files) is needed to reclaim space, which is expensive.\n\n---\n\n### **Question B (14 Marks) — `[KTU University Exam - May 2023]`**\n\n**(a)** Explain **Linked Allocation** and **Indexed Allocation** with diagrams. Compare their **random access** performance. **(7 Marks)**\n\n**(b)** Write a C program that implements a **Two-Level Directory** supporting per-user file creation, listing, and deletion. Show the data structure and the `create_file_in_user()` function. **(7 Marks)**\n\n---\n\n#### **Model Solution (a) — 7 Marks**\n\n**Linked Allocation (3 marks):**\n- Each block contains a **data portion** and a **pointer to the next block**; the directory entry holds only the starting block.\n- **Random Access:** To access the $i$-th block, the OS starts at the first block and follows $i-1$ pointers — so access time is $O(i)$, i.e., proportional to the block's position. Without a FAT, this is slow.\n- **With FAT (File Allocation Table):** The whole chain is cached in memory, so random access becomes $O(1)$.\n- **Disadvantage:** Pointer overhead in every block; a single bad pointer corrupts the rest of the file.\n\n**Indexed Allocation (3 marks):**\n- A dedicated **index block** contains an array of pointers to the data blocks of the file. Directory entry holds a single pointer to the index block.\n- **Random Access:** Read the index block into memory → access pointer $i$ → read the data block. Time is $O(1)$ (one disk seek for index + one for data).\n- **Limit:** A single index block caps file size at $N \\cdot B$ where $N = B / P$ (e.g., 4 MiB for 4 KiB blocks, 4-byte pointers).\n- **Solution:** Multi-level indexed (UNIX `inode`).\n\n**Comparison table (1 mark):**\n- Linked: $O(i)$ random access (no FAT), $O(1)$ with FAT; wastes pointer space per block.\n- Indexed: $O(1)$ random access always; wastes space in sparse index.\n\n(Diagrams already covered in §4.5 and §4.6 — re-use them in the answer sheet.)\n\n---\n\n#### **Model Solution (b) — 7 Marks**\n\n**Data Structure (2 marks):**\n```c\n#define MAX_USERS 10\n#define MAX_FILES 20\ntypedef struct {\n    char fname[30];\n    int  start_block;\n    int  num_blocks;\n} DirEntry;\n\ntypedef struct {\n    char uname[20];\n    DirEntry ufd[MAX_FILES];\n    int  file_count;\n} User;\n\nUser MFD[MAX_USERS];\nint user_count = 0;\n```\n\n**Helper to find a user (1 mark):**\n```c\nint find_user(char *u) {\n    for (int i = 0; i < user_count; i++)\n        if (strcmp(MFD[i].uname, u) == 0) return i;\n    return -1;\n}\n```\n\n**`create_file_in_user()` (3 marks):**\n```c\nvoid create_file_in_user(char *u, char *fname, int size) {\n    int uid = find_user(u);\n    if (uid == -1) { printf(\"User %s not found.\\n\", u); return; }\n    if (MFD[uid].file_count >= MAX_FILES) { printf(\"UFD full.\\n\"); return; }\n    int start = find_contiguous(size);\n    if (start == -1) { printf(\"Disk full.\\n\"); return; }\n    for (int i = 0; i < size; i++) block_status[start + i] = 1;\n    int fc = MFD[uid].file_count;\n    strcpy(MFD[uid].ufd[fc].fname, fname);\n    MFD[uid].ufd[fc].start_block = start;\n    MFD[uid].ufd[fc].num_blocks  = size;\n    MFD[uid].file_count++;\n    printf(\"File '%s' created in UFD of user '%s'.\\n\", fname, u);\n}\n```\n\n**Explaination (1 mark):** This is a *Two-Level* directory because there is one MFD listing users, and each user has a private UFD listing files. File names are unique only within a user's UFD — two users may have a file called `notes.txt`.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Common Pitfalls**\n> 1. **Confusing *Logical* Directory with *Physical* Allocation.** Many students mix up the two and answer \"Single-Level vs Sequential\" as if they are comparable. They are **orthogonal** concepts — single-level/multi-level is the *name space*, sequential/linked/indexed is the *disk layout*. Always clarify the distinction in your first paragraph.\n> 2. **Forgetting the FAT variant of Linked Allocation.** If the examiner asks for the **drawback** of linked allocation, mentioning \"$O(i)$ random access\" *and* \"the FAT solves this\" shows depth and earns 1 extra mark.\n> 3. **In C-program answers, missing the bitmap / free-block tracking.** A program that allocates blocks but never marks `block_status[]` is functionally incorrect — the next `create_file` will overwrite existing data. Always include the bitmap.\n> 4. **Off-by-one errors in address translation.** Byte offset 2500 in a 1024-byte block is **block 2 (0-indexed)**, not block 3. Examiners deliberately give tricky numbers to test this.\n> 5. **Diagrams without labels.** A box labelled \"MFD\" with no arrows showing `start_block → disk` loses 1–2 marks. Always label arrows.\n> 6. **Forgetting to mention UNIX `inode` is multi-level indexed.** Saying \"UNIX uses indexed allocation\" is incomplete — it uses **multi-level indexed** (direct, single-indirect, double-indirect, triple-indirect). This single keyword can be the difference between 7/7 and 6/7.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- [x] **File Organization** = Directory Structure (name layer) + Allocation Strategy (storage layer).\n- [x] **Single-Level Directory**: one MFD, global uniqueness, no sharing, $O(N)$ search.\n- [x] **Two-Level Directory**: MFD + per-user UFD; path is `user:file`; still no sub-folders.\n- [x] **Multi-Level / Tree Directory**: hierarchical, path-based, supports `mkdir/rmdir`; no cycles.\n- [x] **Acyclic-Graph Directory**: tree + hard/sym links; reference counter governs deletion; **no cycles**.\n- [x] **Sequential Allocation**: contiguous blocks, directory stores `start, length`; **external fragmentation**; best for **CD/DVD** and large streaming.\n- [x] **Linked Allocation**: chain of blocks; directory stores `start` only; **no external frag**; **FAT** gives $O(1)$ random access; worst case without FAT is $O(i)$.\n- [x] **Indexed Allocation**: index block holds pointers; **$O(1)$ random access**; file size cap $N \\cdot B = (B \\div P) \\cdot B$; UNIX `inode` extends with multi-level indexing.\n- [x] **Fragmentation**: external in sequential; pointer-overhead \"internal\" in linked; index-block internal in indexed.\n- [x] **Lab Program Must Include**: `DirectoryEntry` struct, `block_status[]` bitmap, `find_contiguous()`, `create_file_seq/linked/indexed()`, a menu loop, and a function to print the **disk block map**.\n- [x] **KTU Viva Favourite**: \"What is the maximum file size for indexed allocation with 4 KiB blocks and 4-byte pointers?\" → **$1024 \\times 4096 = 4$ MiB.**\n- [x] **KTU Viva Trap**: \"Why not use only indexed allocation?\" → *Internal fragmentation in the index block for tiny files* and *single point of failure* (corrupt index = whole file lost).\n- [x] **KTU Viva Bonus**: \"How does `ext4` combine the best of all three?\" → Uses *extents* (contiguous runs) indexed in a **B+Tree** — hybrid of sequential and indexed.\n\n---\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "information-technology-5-pcitl408-mExperiment 2-t12",
          "title": "Familiarization of cloud platforms and virtualization tools (e.g., VMWare)",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCITL408 - OPERATING SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Familiarization of cloud platforms and virtualization tools (e.g., VMWare)\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Virtualization — Formal Academic Definition\n\n**Virtualization** is the process of creating a software-based (virtual) representation of a physical resource (server, storage, network, or operating system) using a specialized software layer called a **hypervisor** (also known as a Virtual Machine Monitor — VMM). The hypervisor abstracts the underlying physical hardware and partitions it into multiple isolated execution environments called **Virtual Machines (VMs)**, each of which runs its own complete operating system (Guest OS) and applications.\n\n> [!NOTE]\n> **KTU Syllabus Highlight (PCITL408):** Virtualization is recognized as the foundational technology that enables cloud computing. The KTU 2024 Scheme expects students to demonstrate hands-on familiarity with hypervisor-based tools such as **VMware Workstation / VMware Player / VirtualBox** and to understand how cloud platforms (AWS, Azure, GCP, OpenStack) leverage the same principles.\n\n## 1.2 Cloud Computing — Formal Academic Definition\n\n**Cloud Computing** is a distributed computing paradigm that delivers on-demand, scalable, and elastic computing resources (networks, servers, storage, applications, and services) over the public or private Internet on a **pay-per-use** basis, following the model defined by the **National Institute of Standards and Technology (NIST)**.\n\n## 1.3 VMware — Tool-Specific Definition\n\n**VMware** is a pioneer suite of virtualization products developed by VMware Inc. (now a subsidiary of Broadcom). Key products in the academic/KTU lab context include:\n- **VMware Workstation Pro** — Type-2 hypervisor for Windows/Linux hosts.\n- **VMware Player (Workstation Player)** — Free Type-2 hypervisor for personal use.\n- **VMware ESXi** — Bare-metal (Type-1) enterprise hypervisor.\n- **VMware vSphere / vCenter** — Centralized management platform for ESXi hosts.\n\n## 1.4 Conceptual Analogy — The \"Apartment Building\" Model\n\n> [!IMPORTANT]\n> **Real-World Analogy: The Apartment Building**\n> - Imagine a large **physical land plot** = a single physical server with fixed CPU, RAM, and storage.\n> - **Building the entire plot into one big house** = using the server for one OS only (wasteful if demand is low).\n> - **Building a 10-story apartment building on the same plot** = **Virtualization**. The **landlord** is the **Hypervisor**. Each **apartment** is a **Virtual Machine (VM)** with its own **door (NIC)**, **walls (allocated RAM)**, and **utility meters (allocated CPU/disk)**. Tenants (Guest OS) cannot see into each other's apartments — this is **isolation**.\n> - **Airbnb / Co-living spaces** = **Cloud Computing**. You don't own the building; you rent apartments on demand and pay only for the nights you stay. The cloud provider owns and manages the \"building\" (data center + hypervisor).\n\n## 1.5 The Three Pillars of Resource Abstraction\n\n| Pillar | Hardware Realized As | Core Benefit |\n|---|---|---|\n| **Compute Virtualization** | Virtual CPU (vCPU) | Multiple OS instances share physical cores |\n| **Storage Virtualization** | Virtual Disks (VMDK, VHD) | Decouples OS from physical disk layout |\n| **Network Virtualization** | Virtual Switches (vSwitch), VLANs | Software-defined isolated networks |\n\n## 1.6 Two Fundamental Hypervisor Categories\n\n> [!NOTE]\n> **Type-1 Hypervisor (Bare-Metal):** Runs directly on the host's hardware. Examples: **VMware ESXi**, Microsoft Hyper-V Server, Xen. Used in enterprise data centers and cloud platforms.\n\n> [!NOTE]\n> **Type-2 Hypervisor (Hosted):** Runs as an application on a conventional host OS. Examples: **VMware Workstation**, Oracle VirtualBox. Used in KTU labs and developer workstations.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Two Major Modes of VMware Workstation Operation\n\n### A. Bridged Networking Mode\n- The VM connects directly to the **physical network** of the host.\n- The VM receives its own unique IP address from the physical router's DHCP server.\n- The VM is visible to other machines on the LAN.\n- **Use case:** Running servers (e.g., a local web server) that must be reachable from other devices.\n\n### B. NAT (Network Address Translation) Mode\n- The VM shares the host's IP address for outbound traffic.\n- The VM is hidden from the external LAN.\n- VMware provides an internal virtual DHCP server (e.g., `192.168.182.0/24`).\n- **Use case:** Internet access for the VM while keeping it isolated. **Default mode in KTU labs.**\n\n### C. Host-Only Networking\n- VM can communicate with the host and other VMs but **cannot** access the internet or LAN.\n- Used for isolated lab testing.\n\n## 2.2 Cloud Service Models — The NIST SP 500-292 Stack\n\n| Model | Full Form | User Manages | Provider Manages | KTU Example |\n|---|---|---|---|---|\n| **IaaS** | Infrastructure-as-a-Service | OS, Middleware, Apps, Data | Hardware, Storage, Network, Virtualization | AWS EC2, Azure VM, Google Compute Engine |\n| **PaaS** | Platform-as-a-Service | Apps, Data | Everything else (OS, Middleware, Runtime) | Google App Engine, Heroku |\n| **SaaS** | Software-as-a-Service | Only data/usage | Entire stack | Gmail, Microsoft 365, Zoom |\n| **FaaS** | Function-as-a-Service | Individual functions | All infrastructure | AWS Lambda, Azure Functions |\n\n## 2.3 Cloud Deployment Models\n\n| Model | Scope | User Base |\n|---|---|---|\n| **Public Cloud** | Public Internet | General public (AWS, Azure, GCP) |\n| **Private Cloud** | On-premises / dedicated | Single organization (OpenStack) |\n| **Hybrid Cloud** | Mix of public + private with orchestration | Enterprises |\n| **Community Cloud** | Shared by organizations with common concerns | Government, healthcare consortia |\n\n## 2.4 Five Essential Characteristics of Cloud Computing (NIST Definition)\n\n1. **On-demand self-service** — Provision resources automatically.\n2. **Broad network access** — Available over standard networks.\n3. **Resource pooling** — Multi-tenant model with dynamic reallocation.\n4. **Rapid elasticity** — Scale up/down as needed.\n5. **Measured service** — Pay-per-use metering (CPU-hours, GB-months).\n\n## 2.5 Core Components of a VMware Virtual Machine\n\n| Component | File Extension | Purpose |\n|---|---|---|\n| **Configuration File** | `.vmx` | Hardware configuration (RAM, CPU, NIC) |\n| **Virtual Disk** | `.vmdk` | Virtual hard drive contents |\n| **BIOS/UEFI Firmware** | `.nvram` | VM firmware state |\n| **Log File** | `.log` | Activity log for troubleshooting |\n| **Snapshot Data** | `-Snapshot*.vmsn` | Saved state for rollback |\n| **Memory File** | `.vmem` | VM's RAM dump (only when running) |\n\n## 2.6 KTU Formula & Metric Sheet\n\n| Formula / Metric | Expression | Unit / Boundary |\n|---|---|---|\n| **CPU Oversubscription Ratio** | $\\text{Cores}_{\\text{physical}} \\times \\text{Ratio} = \\text{vCPUs}_{\\text{allocated}}$ | Typical enterprise ratio: $1\\!:\\!4$ to $1\\!:\\!6$ |\n| **Memory Overcommit Ratio** | $\\text{Ratio} = \\dfrac{\\text{Total VM RAM Allocated}}{\\text{Physical RAM}}$ | Lab ratio: $1.0\\!:\\!1.0$; Cloud ratio: up to $1.5\\!:\\!1$ |\n| **Disk Capacity** | $\\text{Capacity} = \\text{Provisioned} + \\text{Snapshot Overhead}$ | Provisioned size in **GB** |\n| **Storage I/O Latency** | $\\text{Latency} = T_{\\text{read}} + T_{\\text{transfer}}$ | Measured in **ms** |\n| **Cost Metering (Cloud)** | $\\text{Cost} = (\\text{vCPU} \\times P_{\\text{cpu}}) + (\\text{RAM}_{\\text{GB}} \\times P_{\\text{ram}}) + (\\text{Storage}_{\\text{GB}} \\times P_{\\text{storage}})$ | USD per hour |\n| **Availability SLA** | $\\text{Downtime}_{\\text{annual}} = 8760 \\times (1 - \\text{SLA}_{\\%})$ | Hours/year (e.g., $99.9\\% \\rightarrow 8.76$ hrs) |\n\n> [!IMPORTANT]\n> **Engineering Utility:** These formulas appear in KTU 2024 Scheme questions on capacity planning, cloud cost estimation, and SLA analysis for System Administration & DevOps roles. Master the oversubscription and cost-metering equations — they are the most frequently asked in Part B.\n\n## 2.7 Real-World Engineering Utility\n\n- **Data Centers:** VMware ESXi + vSphere powers ~75% of enterprise virtualization.\n- **Cloud IaaS:** AWS EC2, Azure VM, and GCP Compute Engine are built on hypervisor-based virtualization.\n- **Dev/Test Environments:** KTU students and developers spin up disposable Linux VMs in minutes.\n- **Disaster Recovery:** VMware Site Recovery Manager (SRM) replicates VMs between data centers.\n- **Container & Microservice Hosts:** VMs serve as the secure host layer for Docker/Kubernetes clusters.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations, Procedures & Analytical Matrix\n\n## 3.1 Exhaustive Step-by-Step: Creating a Virtual Machine in VMware Workstation\n\n> [!NOTE]\n> This procedure is the **core hands-on experiment** of KTU 2024 Scheme Module Experiment 2. Follow each step precisely; it forms the basis of the 14-mark lab exam question.\n\n### Step 1: Verify System Prerequisites\n- **OS:** Windows 10/11 (64-bit) or Linux (Ubuntu 20.04+ recommended).\n- **RAM:** Minimum **8 GB** physical (so you can allocate 2–4 GB to VM).\n- **CPU:** 64-bit processor with **Intel VT-x** or **AMD-V** enabled in BIOS.\n- **Disk:** At least **50 GB** free for the VM + VMware installation.\n\n**Verification Command on Windows (PowerShell):**\n```powershell\nsysteminfo | findstr /C:\"Hyper-V\"\n```\n**On Linux:**\n```bash\ngrep -E 'vmx|svm' /proc/cpuinfo\n```\nIf the output is non-empty, hardware virtualization is supported.\n\n### Step 2: Install VMware Workstation\n- Download the installer from Broadcom's official portal.\n- Run as Administrator → Accept EULA → Choose **Typical** install.\n- Enter license key (or select 30-day trial for KTU lab).\n\n### Step 3: Create a New Virtual Machine\n- Launch VMware Workstation → Click **\"Create a New Virtual Machine\"**.\n- Select **\"Typical (recommended)\"** configuration.\n- Choose installation source:\n  - **Installer disc** (ISO file): select `.iso` of Ubuntu/CentOS/Windows.\n  - **Installer disc drive** (physical DVD).\n  - **I will install the operating system later** (manual config).\n\n### Step 4: Configure Guest OS Identity\n- **Guest OS:** Linux\n- **Version:** Ubuntu 64-bit\n- **VM Name:** `KTU_Ubuntu_24`\n- **Location:** `D:\\VMs\\KTU_Ubuntu_24\\`\n\n### Step 5: Allocate Disk Capacity\n- **Maximum disk size:** **20.0 GB** (recommended for lab).\n- Select **\"Split virtual disk into multiple files\"** (easier to move/transfer).\n- Click **Next → Finish**.\n\n### Step 6: Customize Hardware (Before First Boot)\nClick **\"Customize Hardware\"** to set:\n\n| Component | Recommended Lab Setting | Justification |\n|---|---|---|\n| **Memory (RAM)** | 2048 MB (2 GB) | Sufficient for Ubuntu desktop without choking host |\n| **Processors** | 2 cores, 1 socket | Parallelism for browser/IDE |\n| **Network Adapter** | NAT (default) | Internet access without exposing VM to LAN |\n| **CD/DVD** | Use ISO image | Boot from the installer ISO |\n| **USB Controller** | Present | For pendrive passthrough |\n| **Display** | 3D graphics enabled, 1 GB VRAM | Smooth UI rendering |\n| **Hard Disk** | SATA, 20 GB provisioned | Standard lab disk |\n\n### Step 7: Power On and Install Guest OS\n- Click **\"Power on this virtual machine\"**.\n- The VM boots from the attached ISO.\n- Follow the Ubuntu installation wizard:\n  - Language → Keyboard → Network (DHCP auto-assigns IP from VMware NAT).\n  - **Disk partition:** Erase disk and install (entire 20 GB to Ubuntu).\n  - Create user: e.g., `student` with password `ktu@2024`.\n  - Reboot when prompted.\n\n### Step 8: Install VMware Tools\nVMware Tools is a suite of utilities (drivers, time-sync, clipboard sharing, drag-and-drop, screen resize).\n- Menu: **VM → Install VMware Tools** (mounts a virtual CD).\n- Inside the VM, mount the CD and run the installer:\n```bash\nsudo apt update\nsudo apt install open-vm-tools open-vm-tools-desktop -y\nsudo reboot\n```\n> [!TIP]\n> Without VMware Tools, the VM cannot dynamically resize the screen, share clipboard, or sync time with the host. KTU evaluators often deduct marks if VMware Tools is not installed.\n\n### Step 9: Configure Snapshot for Clean State\n- **VM → Snapshot → Take Snapshot**\n- Name: `KTU_Base_Install`\n- Description: `Fresh Ubuntu with VMware Tools installed.`\n- This allows the student to revert to a known-good state after any experiment.\n\n### Step 10: Verify Network Connectivity\n```bash\nip addr show          # Check assigned IP (usually 192.168.182.x)\nping 8.8.8.8          # Test internet\nping 192.168.182.2    # Test host (VMware NAT gateway)\n```\n\n## 3.2 Analytical Comparison Matrix — Lab Tools\n\n| Feature | **VMware Workstation Pro** | **Oracle VirtualBox** | **Microsoft Hyper-V** | **KVM (Linux)** |\n|---|---|---|---|---|\n| **Type** | Type-2 | Type-2 | Type-1 (also hybrid) | Type-1 |\n| **Host OS** | Windows, Linux | Windows, Linux, macOS | Windows only | Linux only |\n| **License** | Commercial (paid) | Open-source (GPL) | Bundled with Windows Pro | Open-source |\n| **Snapshots** | Yes (multi-level) | Yes | Yes | Yes (via libvirt) |\n| **3D Graphics** | Excellent (DirectX 11) | Limited | Good | Good |\n| **Cloud Integration** | Strong (vSphere, AWS) | Limited | Azure integration | OpenStack |\n| **KTU Lab Use** | Primary | Alternative | Limited | Advanced |\n\n## 3.3 Cloud Platform Comparison Matrix\n\n| Feature | **AWS EC2** | **Microsoft Azure VM** | **Google Compute Engine** | **OpenStack (Private)** |\n|---|---|---|---|---|\n| **Market Leader** | Yes (≈32% share) | Strong #2 | #3 | Private cloud |\n| **Instance Types** | 400+ | 200+ | 100+ | Custom flavors |\n| **Free Tier** | 12 months (t2.micro) | 12 months + $200 credit | 90 days + $300 credit | Free (self-hosted) |\n| **Hypervisor** | Xen / Nitro (KVM-based) | Hyper-V (custom) | KVM | KVM (default) |\n| **KTU Student Use** | Recommended | Common | Common | University DC |\n\n## 3.4 Step-by-Step Derivation — Cost Metering for a Cloud VM\n\nA student needs an **AWS EC2 t3.medium** instance (2 vCPU, 4 GB RAM) running for **730 hours/month** with **100 GB SSD storage**. Compute rate = **$0.0416/hr**, Storage = **$0.10/GB-month**.\n\n$$\n\\begin{aligned}\n\\text{Cost}_{\\text{compute}} &= \\text{Hours} \\times \\text{Rate}_{\\text{hr}} \\\\\n&= 730 \\times 0.0416 \\\\\n&= \\$30.368 \\\\\n\\\\\n\\text{Cost}_{\\text{storage}} &= \\text{Size}_{\\text{GB}} \\times \\text{Rate}_{\\text{GB-month}} \\\\\n&= 100 \\times 0.10 \\\\\n&= \\$10.00 \\\\\n\\\\\n\\text{Cost}_{\\text{total}} &= \\text{Cost}_{\\text{compute}} + \\text{Cost}_{\\text{storage}} \\\\\n&= 30.368 + 10.00 \\\\\n&= \\$40.368 \\text{ per month}\n\\end{aligned}\n$$\n\n**Logical Explanation:**\n- **Line 1:** Identifies the cost-per-hour metric and the monthly runtime (730 = 24 × 30.4 average days).\n- **Line 2:** Multiplies the rate by the number of hours to obtain raw compute cost.\n- **Line 3:** Storage is metered per GB per month, so the equation is multiplicative.\n- **Line 4:** Total bill = sum of all metered resources (compute, storage, network egress, etc.).\n\n## 3.5 Step-by-Step Derivation — SLA Downtime Calculation\n\nAn SLA of **99.95%** is offered. Calculate maximum annual downtime.\n\n$$\n\\begin{aligned}\n\\text{Availability} &= 99.95\\% = 0.9995 \\\\\n\\text{Total hours/year} &= 365 \\times 24 = 8760 \\\\\n\\text{Allowed Downtime}_{\\text{year}} &= 8760 \\times (1 - 0.9995) \\\\\n&= 8760 \\times 0.0005 \\\\\n&= 4.38 \\text{ hours/year} \\\\\n\\text{Allowed Downtime}_{\\text{month}} &= \\dfrac{4.38}{12} \\approx 0.365 \\text{ hours} \\\\\n&\\approx 21.9 \\text{ minutes/month}\n\\end{aligned}\n$$\n\n**Logical Explanation:**\n- **Line 1:** SLA percentages are decimalized for arithmetic.\n- **Line 2:** Standard year in hours.\n- **Line 3:** The \"unavailability\" fraction is $(1 - A)$; multiplying by total time gives the budget for downtime.\n- **Line 4:** Monthly allowance aids operational planning of maintenance windows.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Diagram: Layered Architecture of a Virtualized System\n\n```mermaid\nflowchart TB\n    subgraph AppLayer[\"APPLICATION LAYER\"]\n        APP1[\"Guest App 1\"]\n        APP2[\"Guest App 2\"]\n        APP3[\"Host App\"]\n    end\n\n    subgraph GuestOS[\"GUEST OS LAYER\"]\n        GOS1[\"Windows 11 VM\"]\n        GOS2[\"Ubuntu 24.04 VM\"]\n        HOS[\"Windows 11 Host OS\"]\n    end\n\n    subgraph Hyper[\"HYPERVISOR LAYER - VMM\"]\n        HYP[\"VMware Workstation Pro<br/>Type-2 Hypervisor\"]\n    end\n\n    subgraph HwLayer[\"PHYSICAL HARDWARE LAYER\"]\n        CPU[\"Intel i7 CPU<br/>VT-x Enabled\"]\n        RAM[\"32 GB DDR4 RAM\"]\n        DISK[\"1 TB SSD\"]\n        NIC[\"1 Gbps NIC\"]\n    end\n\n    APP1 --> GOS1\n    APP2 --> GOS2\n    APP3 --> HOS\n    GOS1 --> HYP\n    GOS2 --> HYP\n    HOS --> HYP\n    HYP --> CPU\n    HYP --> RAM\n    HYP --> DISK\n    HYP --> NIC\n```\n\n**Interpretation:** This block diagram shows how the **hypervisor** sits between the Guest OSes and the actual hardware. In a Type-2 model (VMware Workstation), the hypervisor is itself an application running on the host OS. In a Type-1 model (ESXi), the host OS layer is removed and the hypervisor runs directly on hardware.\n\n## 4.2 Diagram: Cloud Service Models (IaaS / PaaS / SaaS / FaaS)\n\n```mermaid\nflowchart TB\n    subgraph SaaS[\"SAAS - User manages only DATA\"]\n        S1[\"Gmail\"]\n        S2[\"Microsoft 365\"]\n        S3[\"Zoom\"]\n    end\n\n    subgraph PaaS[\"PAAS - User manages APPS and DATA\"]\n        P1[\"Google App Engine\"]\n        P2[\"Heroku\"]\n        P3[\"Azure App Service\"]\n    end\n\n    subgraph IaaS[\"IAAS - User manages OS, APPS, DATA\"]\n        I1[\"AWS EC2\"]\n        I2[\"Azure Virtual Machines\"]\n        I3[\"Google Compute Engine\"]\n    end\n\n    subgraph FaaS[\"FAAS - User manages FUNCTIONS only\"]\n        F1[\"AWS Lambda\"]\n        F2[\"Azure Functions\"]\n        F3[\"Google Cloud Functions\"]\n    end\n\n    FaaS --> IaaS\n    IaaS --> PaaS\n    PaaS --> SaaS\n```\n\n**Interpretation:** This vertical stack visualizes the **abstraction gradient**. Lower models (FaaS, IaaS) provide more control but require more user management. Higher models (PaaS, SaaS) reduce user burden at the cost of flexibility. KTU questions often ask students to identify which service model a given real-world product belongs to.\n\n## 4.3 Diagram: VMware VM Creation Workflow\n\n```mermaid\nflowchart LR\n    A[\"Step 1: Launch VMware\"] --> B[\"Step 2: Create New VM\"]\n    B --> C[\"Step 3: Select ISO Installer\"]\n    C --> D[\"Step 4: Configure HW<br/>RAM, CPU, Disk\"]\n    D --> E[\"Step 5: Power On\"]\n    E --> F[\"Step 6: Install Guest OS\"]\n    F --> G[\"Step 7: Install VMware Tools\"]\n    G --> H[\"Step 8: Take Base Snapshot\"]\n    H --> I[\"Step 9: Verify Network\"]\n    I --> J[\"Step 10: VM Ready for Lab\"]\n```\n\n**Interpretation:** A linear, sequential workflow that mirrors the **exact procedure** in Section 3.1. KTU lab record books should reproduce this diagram in the methodology section.\n\n## 4.4 Diagram: VMware Networking Modes\n\n```mermaid\nflowchart TB\n    subgraph Bridged[\"BRIDGED MODE\"]\n        B_VM[\"VM IP: 192.168.1.50\"]\n        B_HOST[\"Host IP: 192.168.1.10\"]\n        B_ROUTER[\"Physical Router\"]\n        B_VM --- B_ROUTER\n        B_HOST --- B_ROUTER\n    end\n\n    subgraph NAT[\"NAT MODE\"]\n        N_VM[\"VM IP: 192.168.182.10\"]\n        N_NAT[\"VMware NAT Engine<br/>Host IP: 192.168.182.2\"]\n        N_INET[\"Internet\"]\n        N_VM --> N_NAT\n        N_NAT --> N_INET\n    end\n\n    subgraph HostOnly[\"HOST-ONLY MODE\"]\n        H_VM[\"VM IP: 192.168.50.10\"]\n        H_HOST[\"Host vNIC: 192.168.50.1\"]\n        H_VM --- H_HOST\n    end\n```\n\n**Interpretation:** Each networking mode is a separate isolated environment. KTU lab exams frequently test the difference between Bridged and NAT — students should note that **NAT hides the VM from the LAN**, while **Bridged exposes it**.\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## PART A — 3 MARK QUESTIONS (Short Answer)\n\n> **Q1. [KTU University Exam – Dec 2023]**\n> **Define the term \"Hypervisor\" and list its two main types with one example each.**\n\n### Model Answer (3 marks):\nA **hypervisor** (or Virtual Machine Monitor — VMM) is a software layer that creates, runs, and manages multiple virtual machines on a single physical host by abstracting the underlying hardware.\n\nThe two main types are:\n1. **Type-1 (Bare-Metal):** Runs directly on hardware with no host OS. Example: **VMware ESXi**, Microsoft Hyper-V Server.\n2. **Type-2 (Hosted):** Runs as an application over a conventional host OS. Example: **VMware Workstation Pro**, Oracle VirtualBox.\n\n---\n\n> **Q2. [KTU University Exam – Jun 2024]**\n> **Differentiate between IaaS, PaaS, and SaaS with one real-world example for each.**\n\n### Model Answer (3 marks):\n\n| Model | User Responsibility | Example |\n|---|---|---|\n| **IaaS** | User installs OS, middleware, apps; provider supplies virtualized hardware | **AWS EC2** |\n| **PaaS** | User deploys only applications; provider manages OS, runtime, and middleware | **Google App Engine** |\n| **SaaS** | User only consumes the finished software via browser; provider manages the entire stack | **Microsoft 365 / Gmail** |\n\nThe core distinction is the **abstraction level** — moving up the stack transfers more management responsibility to the cloud provider.\n\n---\n\n## PART B — 14 MARK QUESTIONS (Module Internal Choice)\n\n> **Q3A. [KTU University Exam – Model Question, 14 Marks]**\n\n**(a)** [7 marks] Explain the **architecture of VMware Workstation** with a neat block diagram. Describe the function of the hypervisor, virtual NIC, virtual disk (VMDK), and VMware Tools in this architecture.\n\n**(b)** [7 marks] Illustrate the **step-by-step procedure** to create a Linux virtual machine in VMware Workstation. Include snapshot creation and VMware Tools installation.\n\n### Model Solution:\n\n**(a) Architecture of VMware Workstation (7 marks):**\n\nVMware Workstation uses a **Type-2 (hosted) hypervisor** architecture with the following layers (top to bottom):\n\n1. **Guest OS Layer:** Independent OS instances (e.g., Windows 11 VM, Ubuntu VM) running concurrently.\n2. **VMware Tools Layer:** Drivers and utilities installed inside each guest for time-sync, clipboard sharing, drag-and-drop, and dynamic screen resize.\n3. **Virtual Hardware Layer:** Emulated devices presented to the guest:\n   - **vCPU** — abstracted CPU cores.\n   - **Virtual RAM** — portion of host's physical RAM.\n   - **Virtual Disk (VMDK)** — file-based virtual hard drive.\n   - **Virtual NIC (VMnet0/VMnet8)** — connected to Bridged, NAT, or Host-only network.\n4. **VMware Workstation (VMM) Layer:** Core hypervisor that schedules CPU, memory, and I/O resources among VMs.\n5. **Host OS Layer:** Windows/Linux operating system.\n6. **Physical Hardware Layer:** CPU with VT-x, RAM, disk, and NIC.\n\n**Function of key components:**\n- **Hypervisor (VMM):** Allocates physical resources to VMs and enforces isolation.\n- **Virtual NIC:** Connects the VM to a virtual network. **VMnet0 = Bridged**, **VMnet8 = NAT**, **VMnet1 = Host-only**.\n- **Virtual Disk (VMDK):** A single file (or set of split files) acting as the VM's hard drive.\n- **VMware Tools:** Improves integration between guest and host; without it, screen resolution is fixed at 800×600 and clipboard sharing is disabled.\n\n**(b) Step-by-Step VM Creation (7 marks):**\n\nThe complete procedure is as enumerated in **Section 3.1, Steps 1–10**. Key checkpoint commands and actions:\n\n- **Prerequisite check:** `systeminfo | findstr /C:\"Hyper-V\"` (Windows) or `grep -E 'vmx|svm' /proc/cpuinfo` (Linux).\n- **ISO mounting:** VM Settings → CD/DVD → Use ISO image.\n- **Hardware allocation:** 2 GB RAM, 2 CPU cores, 20 GB disk, NAT network.\n- **OS installation:** Follow Ubuntu wizard → create user `student` / `ktu@2024`.\n- **VMware Tools installation:**\n```bash\nsudo apt update\nsudo apt install open-vm-tools open-vm-tools-desktop -y\nsudo reboot\n```\n- **Snapshot creation:** VM → Snapshot → Take Snapshot → Name: `KTU_Base_Install`.\n- **Verification:** `ip addr show`, `ping 8.8.8.8`.\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall — Common Mark Deductions:**\n> 1. **Missing VMware Tools installation** — Examiners deduct **2 marks** if VMware Tools is not installed in the live demo.\n> 2. **Wrong networking mode** — Using Bridged when NAT is required, or vice versa, costs **1 mark** plus practical-time loss.\n> 3. **No snapshot** — Not taking a base snapshot before running risky operations loses **1 mark** in the record.\n> 4. **Confusing `.vmx` with `.vmdk`** — Examiners often ask: \"Which file holds the VM configuration?\" The answer is **`.vmx`**, not `.vmdk` (which is the virtual disk).\n> 5. **Forgetting to enable VT-x in BIOS** — VMs may fail to boot with \"VT-x not available\" error; this is the **#1 setup-time failure** in KTU labs.\n\n---\n\n> **Q3B. [Alternative Module Choice, 14 Marks]**\n\n**(a)** [7 marks] Explain the **NIST definition of cloud computing** and enumerate its **five essential characteristics**, **three service models**, and **four deployment models** with examples.\n\n**(b)** [7 marks] Compare the working of **Type-1 and Type-2 hypervisors** with a block diagram. State two advantages of each, and give two examples of cloud platforms built on each type.\n\n### Model Solution:\n\n**(a) NIST Definition and Classification (7 marks):**\n\nThe **National Institute of Standards and Technology (NIST)** defines cloud computing as:\n\n> *\"A model for enabling ubiquitous, convenient, on-demand network access to a shared pool of configurable computing resources (e.g., networks, servers, storage, applications, and services) that can be rapidly provisioned and released with minimal management effort or service provider interaction.\"* — **NIST SP 800-145**\n\n**Five Essential Characteristics:**\n1. **On-demand self-service** — Resources provisioned automatically without human intervention.\n2. **Broad network access** — Available over the Internet via standard protocols.\n3. **Resource pooling** — Multi-tenant model with physical/virtual resources dynamically reassigned.\n4. **Rapid elasticity** — Capabilities scaled up or down quickly, often automatically.\n5. **Measured service** — Cloud systems automatically control and optimize resource use via metering (pay-per-use).\n\n**Three Service Models:**\n1. **IaaS (Infrastructure-as-a-Service):** Raw VMs and storage. Example: **AWS EC2**.\n2. **PaaS (Platform-as-a-Service):** Application deployment platform. Example: **Heroku**.\n3. **SaaS (Software-as-a-Service):** Ready-to-use applications. Example: **Gmail**.\n\n**Four Deployment Models:**\n1. **Public Cloud** — Open to the public (AWS, Azure).\n2. **Private Cloud** — Used by a single organization (on-prem OpenStack).\n3. **Hybrid Cloud** — Combination of public and private with orchestration.\n4. **Community Cloud** — Shared by organizations with common concerns (e.g., government clouds).\n\n**(b) Type-1 vs Type-2 Hypervisors (7 marks):**\n\n| Parameter | **Type-1 (Bare-Metal)** | **Type-2 (Hosted)** |\n|---|---|---|\n| **Position** | Runs directly on hardware | Runs as an app on host OS |\n| **Performance** | High (no host OS overhead) | Lower (double scheduling) |\n| **Use Case** | Data centers, cloud | Labs, dev/test |\n| **Examples** | VMware ESXi, Hyper-V Server, Xen | VMware Workstation, VirtualBox |\n| **Cloud Platforms** | AWS (Nitro/Xen), Azure (Hyper-V), GCP (KVM) | KTU student laptops |\n\n**Block diagram (textual representation):**\n\nType-1 Stack:\n```\n[ Guest OS 1 ] [ Guest OS 2 ] [ Guest OS 3 ]\n         \\         |         /\n          [   HYPERVISOR (ESXi)   ]\n          [    PHYSICAL HARDWARE  ]\n```\n\nType-2 Stack:\n```\n[ Guest OS 1 ] [ Guest OS 2 ]\n       \\             /\n       [   VMware Workstation   ]\n       [      HOST OS           ]\n       [    PHYSICAL HARDWARE   ]\n```\n\n**Two advantages of each:**\n- **Type-1 advantages:** (i) Higher performance and efficiency; (ii) Better isolation and security suitable for multi-tenant cloud.\n- **Type-2 advantages:** (i) Easy to install and use on a regular desktop; (ii) Supports rapid prototyping and dev/test.\n\n---\n\n## Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Revision Checklist for KTU PCITL408 — Module Experiment 2:**\n\n- [x] **Virtualization** creates multiple isolated VMs on a single physical host using a **hypervisor**.\n- [x] **Hypervisor (VMM)** is the software layer that abstracts hardware; comes in **Type-1 (bare-metal)** and **Type-2 (hosted)**.\n- [x] **VMware Workstation** is a Type-2 hypervisor used in KTU labs; **ESXi** is the Type-1 enterprise variant.\n- [x] A VM consists of a **`.vmx` configuration file**, a **`.vmdk` virtual disk**, **`.nvram` firmware**, and **`.log` files**.\n- [x] **VMware Tools** is mandatory — installs drivers, enables clipboard, drag-drop, and screen resize.\n- [x] **Three VMware networks:** **Bridged** (LAN-visible), **NAT** (default, hidden, internet via host), **Host-only** (no internet).\n- [x] **Cloud computing** is on-demand, elastic, metered, pooled, and self-serviced — per **NIST SP 800-145**.\n- [x] **Four service models:** **IaaS** (EC2), **PaaS** (App Engine), **SaaS** (Gmail), **FaaS** (Lambda).\n- [x] **Four deployment models:** **Public, Private, Hybrid, Community**.\n- [x] **Sliding abstraction:** FaaS → IaaS → PaaS → SaaS transfers more control to the provider.\n- [x] **Snapshot** captures VM state and is essential before risky lab operations.\n- [x] **Hardware prerequisite:** **VT-x (Intel)** or **AMD-V** must be enabled in BIOS for 64-bit VMs.\n- [x] **Cloud cost formula:** $\\text{Cost} = \\text{Hours} \\times \\text{Rate}_{\\text{hr}} + \\text{Storage}_{\\text{GB}} \\times \\text{Rate}_{\\text{GB-month}}$.\n- [x] **SLA downtime formula:** $\\text{Downtime}_{\\text{year}} = 8760 \\times (1 - \\text{SLA}_{\\%})$.\n- [x] Always take a **base snapshot** after a clean install and VMware Tools setup — this is the rollback safety net.\n- [x] **Common exam trap:** Confusing `.vmx` (configuration) with `.vmdk` (disk) — remember **X = config, D = disk**.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        }
      ]
    }
  ]
}
