{
  "id": "pccsl408-computer-science-and-design-s5",
  "code": "PCCSL408",
  "name": "DBMS LAB",
  "branchId": "computer-science-and-design",
  "semester": 5,
  "modules": [
    {
      "id": "mExperiment 2",
      "title": "Module Experiment 2",
      "topics": [
        {
          "id": "computer-science-and-design-5-pccsl408-mExperiment 2-t1",
          "title": "Design a database schema for an application with ER diagram from a problem description",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL408 - DBMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Design a database schema for an application with ER diagram from a problem description\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\n> [!IMPORTANT]\n> **Entity-Relationship (ER) Modeling** is a conceptual data modeling technique introduced by **Peter Chen (1976)** that uses a diagrammatic representation to capture the logical structure of a database. It identifies **entities** (real-world objects), **attributes** (properties of entities), and **relationships** (associations between entities) before any physical database implementation begins.\n\nA **Database Schema** is the formal blueprint of how data is organized. It defines **tables (relations)**, **columns (attributes)**, **data types**, **constraints** (primary key, foreign key, NOT NULL, UNIQUE, CHECK), and the **relationships** among tables using referential integrity.\n\nIn the **KTU 2024 DBMS Lab syllabus (PCCSL408)**, Experiment 2 specifically tests the student's ability to:\n1. Read a natural-language problem statement.\n2. Identify entities, attributes, and relationships.\n3. Draw an **ER diagram** using **Chen's notation** or **Crow's Foot notation**.\n4. Convert the ER diagram into a **relational schema** using standard mapping rules.\n5. Optionally implement the schema as **SQL `CREATE TABLE`** statements.\n\n## 1.2 Key Terminology (KTU 2024 Glossary)\n\n| Term | Definition | Example |\n|------|------------|---------|\n| **Entity** | A real-world object with independent existence | Student, Book, Department |\n| **Entity Set** | Collection of similar entities | All Students in a University |\n| **Attribute** | A property describing an entity | Student.name, Book.price |\n| **Primary Key** | Attribute that uniquely identifies an entity | Student.roll_no |\n| **Relationship** | An association among entities | Student **enrolls in** Course |\n| **Cardinality** | Number of entity instances participating | 1:1, 1:N, M:N |\n| **Participation** | Whether all entities participate (total) or some (partial) | Total vs Partial |\n| **Weak Entity** | An entity that cannot be uniquely identified by its own attributes | Dependent (of Employee) |\n\n## 1.3 Intuitive Real-World Analogy\n\n> [!NOTE]\n> **Analogy: Designing a House Before Construction**\n>\n> Imagine you want to build a house. Before laying a single brick, an architect creates a **blueprint** showing rooms (entities), their features like doors and windows (attributes), and how rooms connect (relationships). You would never start construction without this plan.\n>\n> An **ER diagram is exactly that blueprint for a database**. The *rooms* are your **tables** (Student, Course, Department). The *features* are your **columns** (name, id, credits). The *connections* are your **foreign keys and JOINs** (a Student *enrolls in* a Course). Just as a missing room in a blueprint means a missing feature in the house, a missing entity in an ER diagram means a missing table in your database.\n\n## 1.4 Two Standard Notations Used in KTU Exams\n\n| Notation | Visual Style | Most Common Use |\n|----------|--------------|-----------------|\n| **Chen's Notation** | Rectangles (entities), Diamonds (relationships), Ovals (attributes) | Academic textbooks, KTU theory papers |\n| **Crow's Foot Notation** | Boxes with crow's foot symbols (>, |, <) at line ends | Industry tools (Visio, Lucidchart, ER/Studio) |\n\n> [!TIP]\n> **KTU Examiner Tip**: For university exams, **Chen's notation is preferred** because it is the syllabus standard. However, if you draw Crow's Foot, label cardinality clearly to avoid ambiguity and loss of marks.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Five-Step Methodology for Schema Design\n\nThe KTU 2024 lab manual mandates a **systematic five-step approach**. Following this sequence guarantees full marks in the design experiment.\n\n### Step 1: Requirement Analysis (Read the Problem Carefully)\n- Read the problem description **twice**.\n- **Highlight nouns** → these are candidate **entities**.\n- **Highlight verbs** → these are candidate **relationships**.\n- **Highlight adjectives/descriptors** → these are candidate **attributes**.\n- Identify **business rules** (e.g., \"each course is taught by exactly one professor\").\n\n### Step 2: Identify Entities and Attributes\n- Filter out nouns that are actually attributes of another entity.\n- Example: In \"the student has a name\", *name* is an attribute of *Student*, not a separate entity.\n- **Classify attributes**:\n  - **Simple / Atomic** — cannot be subdivided (e.g., age, roll_no)\n  - **Composite** — can be subdivided (e.g., address → street, city, pincode)\n  - **Multivalued** — multiple values (e.g., phone_numbers of a person)\n  - **Derived** — computable from others (e.g., age from DOB)\n  - **Key attribute** — uniquely identifies (e.g., roll_no)\n\n### Step 3: Identify Relationships and Cardinality\n- For each pair of related entities, determine the **cardinality ratio**:\n  - **1:1 (One-to-One)** — Each A relates to exactly one B and vice versa. Example: A person has one passport; a passport belongs to one person.\n  - **1:N (One-to-Many)** — One A relates to many B, but each B relates to only one A. Example: One department has many professors.\n  - **M:N (Many-to-Many)** — Many A relate to many B. Example: Students enroll in many courses; courses have many students.\n- Determine **participation constraint**:\n  - **Total (mandatory)** — every entity instance must participate. Shown by a **double line** in Chen's notation.\n  - **Partial (optional)** — some entities may not participate. Shown by a **single line**.\n\n### Step 4: Draw the ER Diagram\n- Use rectangles for entities, ellipses for attributes, diamonds for relationships.\n- Underline primary key attributes.\n- Use double ellipses for multivalued, dashed ellipses for derived.\n- Draw double rectangles for weak entities, double diamonds for identifying relationships.\n\n### Step 5: Convert ER to Relational Schema (Mapping)\n- This is the **most heavily tested** step in KTU lab exams. Apply the mapping rules below.\n\n## 2.2 KTU High-Yield Formula Sheet: ER-to-Relational Mapping Rules\n\n> [!IMPORTANT]\n> These **7 mapping rules** are the *core algorithmic content* of KTU Module Experiment 2. Memorize them in this exact order.\n\n| Rule # | ER Construct | Mapping Action | Resulting Schema |\n|--------|--------------|----------------|------------------|\n| **R1** | Strong Entity Set E with primary key PK | Create a relation R(E) with PK as primary key | `R(PK, attr1, attr2, ...)` |\n| **R2** | Weak Entity Set W with owner E and partial key `pk` | Create relation W with composite key `(E.PK, W.pk)` | `W(E_PK, pk, attrs...)` |\n| **R3** | 1:1 Relationship R between E1 and E2 | Merge into either E1 or E2 by adding the other's PK as FK | `E1(PK1, ..., E2_PK)` |\n| **R4** | 1:N Relationship R between E1 (1-side) and E2 (N-side) | Add E1.PK as foreign key in E2 | `E2(PK2, ..., E1_PK)` |\n| **R5** | M:N Relationship R between E1 and E2 | Create a **new separate relation** with composite PK | `R(E1_PK, E2_PK, attrs...)` |\n| **R6** | Multivalued Attribute A of Entity E | Create a **new separate relation** with composite key `(E.PK, A)` | `E_A(E_PK, A, ...)` |\n| **R7** | n-ary Relationship (n > 2) | Create a new relation with PKs of all participating entities | `R(E1_PK, E2_PK, ..., En_PK)` |\n\n> [!WARNING]\n> **Critical Pitfall**: For 1:1 and 1:N, **NEVER** create a separate table. Only M:N and multivalued attributes require new tables. KTU examiners routinely deduct **2 marks** for this mistake.\n\n## 2.3 Cardinality Notation Quick Reference\n\n$$\n\\text{Cardinality Ratio} \\in \\{ 1:1,\\ 1:N,\\ M:N \\}\n$$\n\n$$\n\\text{Participation Constraint} \\in \\{ \\text{Total (double line)},\\ \\text{Partial (single line)} \\}\n$$\n\n> [!NOTE]\n> In **Crow's Foot notation**, the symbols are:\n> - `|` → **Exactly One** (mandatory participation, 1-side)\n> - `O\\|` or `O` → **Zero or One** (optional 1-side)\n> - `<` (crow's foot) → **Many** (N-side or M-side)\n> - `O<` → **Zero or Many** (optional M-side)\n>\n> These map directly to SQL `JOIN` semantics when the schema is implemented.\n\n## 2.4 Real-World Engineering Utility\n\n- **Banking Systems**: ER modeling is used to design accounts, customers, transactions, and loans before any SQL DDL is written.\n- **E-Commerce Platforms**: Entities like Customer, Order, Product, Cart, Payment, and Shipment are modeled first to ensure no data redundancy.\n- **Hospital Information Systems (HIS)**: Patient, Doctor, Ward, Prescription, and Appointment are ER-modeled to enforce referential integrity.\n- **University Management**: Student, Course, Faculty, Department, Enrollment, Exam — exactly the KTU lab problem domain.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Worked-Out Sample Problem (University Course Registration System)\n\n> [!NOTE]\n> **Problem Statement (KTU Model Question)**:\n> *\"Design an ER diagram for a University database with the following description:*\n> *The University has several departments. Each department has a unique Dept_ID, name, and is headed by a professor (HOD). Every professor has a Prof_ID, name, salary, and belongs to exactly one department. Students have a Roll_No, name, and DOB. A student can enroll in many courses, and each course can have many students. Each course has a Course_ID, name, credits, and is taught by exactly one professor. A course belongs to exactly one department. Students must enroll in at least one course (total participation).\"*\n\n### Step 1: Identify Entities (Nouns)\n\nExtracting nouns: **University, Department, Professor, Student, Course**.\n\n- `University` → context, not an entity (treated as the entire scope).\n- `Department` → **Entity** (unique Dept_ID).\n- `Professor` → **Entity** (unique Prof_ID).\n- `Student` → **Entity** (unique Roll_No).\n- `Course` → **Entity** (unique Course_ID).\n\n**Analytical Matrix of Entities and Attributes:**\n\n| Entity | Primary Key | Other Attributes | Notes |\n|--------|-------------|------------------|-------|\n| **Department** | `Dept_ID` | `name`, `HOD_Prof_ID` (FK) | HOD is a professor, not a string |\n| **Professor** | `Prof_ID` | `name`, `salary` | Belongs to one department (1:N from Dept → Prof) |\n| **Student** | `Roll_No` | `name`, `DOB` | Enrolls in many courses |\n| **Course** | `Course_ID` | `name`, `credits` | Taught by one professor, belongs to one department |\n\n### Step 2: Identify Relationships (Verbs)\n\nExtracting verbs and their cardinalities:\n\n| Relationship | Entity1 | Cardinality | Entity2 | Reasoning |\n|--------------|---------|-------------|---------|-----------|\n| `heads` | Department | **1:1** | Professor | A department has exactly one HOD; a prof heads at most one dept |\n| `works_in` | Department | **1:N** | Professor | One dept has many profs; each prof in one dept |\n| `enrolls_in` | Student | **M:N** | Course | A student takes many courses; a course has many students |\n| `teaches` | Professor | **1:N** | Course | One prof teaches many courses; each course taught by one prof |\n| `offers` | Department | **1:N** | Course | One dept offers many courses; each course in one dept |\n\n### Step 3: Identify Participation Constraints\n\n| Relationship | Side | Participation | Reason |\n|--------------|------|---------------|--------|\n| `enrolls_in` | Student side | **Total** | \"Students must enroll in at least one course\" (explicit) |\n| `enrolls_in` | Course side | **Partial** | A new course may have no students yet |\n| `teaches` | Course side | **Total** | Every course is taught by exactly one professor |\n| `works_in` | Professor side | **Total** | Every professor belongs to a department |\n\n### Step 4: Apply the 7 Mapping Rules (ER → Relational Schema)\n\nThis is the **derivation step** — each rule is applied mechanically.\n\n**Rule R1 — Strong Entity Mapping:**\n\n$$\n\\text{Department}(\\underline{\\text{Dept\\_ID}},\\ \\text{name})\n$$\n\n$$\n\\text{Professor}(\\underline{\\text{Prof\\_ID}},\\ \\text{name},\\ \\text{salary})\n$$\n\n$$\n\\text{Student}(\\underline{\\text{Roll\\_No}},\\ \\text{name},\\ \\text{DOB})\n$$\n\n$$\n\\text{Course}(\\underline{\\text{Course\\_ID}},\\ \\text{name},\\ \\text{credits})\n$$\n\n**Rule R4 — 1:N Relationship (works_in, teaches, offers):**\nAdd the \"1-side\" primary key as a foreign key on the \"N-side\" table.\n\n$$\n\\text{Professor}(\\underline{\\text{Prof\\_ID}},\\ \\text{name},\\ \\text{salary},\\ \\text{Dept\\_ID}^{\\text{FK}})\n$$\n\n$$\n\\text{Course}(\\underline{\\text{Course\\_ID}},\\ \\text{name},\\ \\text{credits},\\ \\text{Dept\\_ID}^{\\text{FK}},\\ \\text{Prof\\_ID}^{\\text{FK}})\n$$\n\n**Rule R3 — 1:1 Relationship (heads):**\nAdd the HOD's `Prof_ID` as a foreign key in `Department`.\n\n$$\n\\text{Department}(\\underline{\\text{Dept\\_ID}},\\ \\text{name},\\ \\text{HOD\\_Prof\\_ID}^{\\text{FK}})\n$$\n\n**Rule R5 — M:N Relationship (enrolls_in):**\nCreate a **new separate table** with composite primary key.\n\n$$\n\\text{Enrollment}(\\underline{\\text{Roll\\_No}^{\\text{FK}}},\\ \\underline{\\text{Course\\_ID}^{\\text{FK}}},\\ \\text{enroll\\_date},\\ \\text{grade})\n$$\n\n### Step 5: Final Optimized Relational Schema\n\n$$\n\\boxed{\n\\begin{aligned}\n&\\textbf{Department}(\\underline{\\text{Dept\\_ID}},\\ \\text{name},\\ \\text{HOD\\_Prof\\_ID}^{\\text{FK}}) \\\\\n&\\textbf{Professor}(\\underline{\\text{Prof\\_ID}},\\ \\text{name},\\ \\text{salary},\\ \\text{Dept\\_ID}^{\\text{FK}}) \\\\\n&\\textbf{Student}(\\underline{\\text{Roll\\_No}},\\ \\text{name},\\ \\text{DOB}) \\\\\n&\\textbf{Course}(\\underline{\\text{Course\\_ID}},\\ \\text{name},\\ \\text{credits},\\ \\text{Dept\\_ID}^{\\text{FK}},\\ \\text{Prof\\_ID}^{\\text{FK}}) \\\\\n&\\textbf{Enrollment}(\\underline{\\text{Roll\\_No}^{\\text{FK}}},\\ \\underline{\\text{Course\\_ID}^{\\text{FK}}},\\ \\text{enroll\\_date},\\ \\text{grade})\n\\end{aligned}\n}\n$$\n\n**Foreign Key Constraint Summary (Analytical Matrix):**\n\n| Child Table | Foreign Key | References | Rule Applied |\n|-------------|-------------|------------|--------------|\n| Professor | `Dept_ID` | Department(Dept_ID) | R4 (1:N) |\n| Course | `Dept_ID` | Department(Dept_ID) | R4 (1:N) |\n| Course | `Prof_ID` | Professor(Prof_ID) | R4 (1:N) |\n| Department | `HOD_Prof_ID` | Professor(Prof_ID) | R3 (1:1) |\n| Enrollment | `Roll_No` | Student(Roll_No) | R5 (M:N part 1) |\n| Enrollment | `Course_ID` | Course(Course_ID) | R5 (M:N part 2) |\n\n### Step 6: SQL DDL Implementation (Optional KTU Lab Extension)\n\n```sql\nCREATE TABLE Department (\n    Dept_ID      VARCHAR(10) PRIMARY KEY,\n    name         VARCHAR(100) NOT NULL,\n    HOD_Prof_ID  VARCHAR(10),\n    FOREIGN KEY (HOD_Prof_ID) REFERENCES Professor(Prof_ID)\n);\n\nCREATE TABLE Professor (\n    Prof_ID  VARCHAR(10) PRIMARY KEY,\n    name     VARCHAR(100) NOT NULL,\n    salary   DECIMAL(10,2) CHECK (salary > 0),\n    Dept_ID  VARCHAR(10) NOT NULL,\n    FOREIGN KEY (Dept_ID) REFERENCES Department(Dept_ID)\n);\n\nCREATE TABLE Student (\n    Roll_No  VARCHAR(15) PRIMARY KEY,\n    name     VARCHAR(100) NOT NULL,\n    DOB      DATE NOT NULL\n);\n\nCREATE TABLE Course (\n    Course_ID  VARCHAR(10) PRIMARY KEY,\n    name       VARCHAR(100) NOT NULL,\n    credits    INT CHECK (credits > 0 AND credits <= 6),\n    Dept_ID    VARCHAR(10) NOT NULL,\n    Prof_ID    VARCHAR(10) NOT NULL,\n    FOREIGN KEY (Dept_ID) REFERENCES Department(Dept_ID),\n    FOREIGN KEY (Prof_ID) REFERENCES Professor(Prof_ID)\n);\n\nCREATE TABLE Enrollment (\n    Roll_No      VARCHAR(15),\n    Course_ID    VARCHAR(10),\n    enroll_date  DATE NOT NULL,\n    grade        CHAR(2),\n    PRIMARY KEY (Roll_No, Course_ID),\n    FOREIGN KEY (Roll_No)   REFERENCES Student(Roll_No),\n    FOREIGN KEY (Course_ID) REFERENCES Course(Course_ID)\n);\n```\n\n## 3.2 Verification: The Mapping is Lossless\n\nThe 7-rule algorithm is **provably lossless** — no information from the ER diagram is lost during conversion. This can be formally verified by checking:\n1. Every entity has a relation.\n2. Every attribute is placed in exactly one relation.\n3. Every relationship is represented either as an FK or a separate table.\n4. No null foreign key is forced in a 1:1 total participation scenario.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 ER Diagram (Chen's Notation — Mermaid Representation)\n\n> [!IMPORTANT]\n> The following Mermaid `erDiagram` block represents the **complete ER diagram** for the University problem. This is the diagram students must draw in the KTU lab record. Every line below should be transcribed into the lab record with proper rectangles, ellipses, and diamonds if Chen's notation is required by the examiner.\n\n```mermaid\nerDiagram\n    DEPARTMENT ||--o{ PROFESSOR : \"works_in (1:N)\"\n    DEPARTMENT ||--o{ COURSE : \"offers (1:N)\"\n    DEPARTMENT ||--|| PROFESSOR : \"heads (1:1, HOD)\"\n    PROFESSOR ||--o{ COURSE : \"teaches (1:N)\"\n    STUDENT }o--o{ COURSE : \"enrolls_in (M:N, Student side TOTAL)\"\n\n    DEPARTMENT {\n        string Dept_ID PK\n        string name\n        string HOD_Prof_ID FK\n    }\n\n    PROFESSOR {\n        string Prof_ID PK\n        string name\n        decimal salary\n        string Dept_ID FK\n    }\n\n    STUDENT {\n        string Roll_No PK\n        string name\n        date DOB\n    }\n\n    COURSE {\n        string Course_ID PK\n        string name\n        int credits\n        string Dept_ID FK\n        string Prof_ID FK\n    }\n\n    ENROLLMENT {\n        string Roll_No PK,FK\n        string Course_ID PK,FK\n        date enroll_date\n        string grade\n    }\n\n    STUDENT ||--o{ ENROLLMENT : \"has\"\n    COURSE  ||--o{ ENROLLMENT : \"has\"\n```\n\n## 4.2 Block-Level Functional Architecture Flow (ER-to-Schema Pipeline)\n\nThis diagram shows the **five-stage transformation pipeline** from raw problem statement to executable SQL DDL. It is the exact sequence students should follow during the KTU lab viva and record submission.\n\n```mermaid\nflowchart TD\n    A[Step 1: Read Problem Statement] --> B[Step 2: Extract Nouns and Verbs]\n    B --> C[Step 3: Classify into Entities, Attributes, Relationships]\n    C --> D[Step 4: Determine Cardinality and Participation]\n    D --> E[Step 5: Draw ER Diagram in Chen Notation]\n    E --> F[Step 6: Apply the 7 Mapping Rules]\n    F --> G[Step 7: Generate Relational Schema with Keys]\n    G --> H[Step 8: Write SQL CREATE TABLE Statements]\n    H --> I[Step 9: Add Constraints PK FK NOT NULL CHECK]\n    I --> J[Step 10: Verify Lossless Join Property]\n\n    A1[NLP Tokenization] -.-> A\n    B1[Highlight Nouns Entities] -.-> B\n    B2[Highlight Verbs Relationships] -.-> B\n    B3[Highlight Adjectives Attributes] -.-> B\n    C1[Filter Out Anomalies] -.-> C\n    D1[1:1, 1:N, M:N Ratios] -.-> D\n    D2[Total vs Partial] -.-> D\n    F1[R1 Strong Entity] -.-> F\n    F2[R3 1:1 Relationship] -.-> F\n    F3[R4 1:N Relationship] -.-> F\n    F4[R5 M:N New Table] -.-> F\n    F5[R6 Multivalued New Table] -.-> F\n\n    style A fill:#FFE4B5\n    style E fill:#98FB98\n    style G fill:#87CEEB\n    style J fill:#FFB6C1\n```\n\n## 4.3 Cardinality Decision Tree (Analytical Aid)\n\nUse this decision tree to determine the correct cardinality for each relationship in your ER diagram during the KTU exam.\n\n```mermaid\nflowchart TD\n    Q1{Is one instance of A<br/>related to at most one B?}\n    Q1 -- Yes --> Q2{Is one instance of B<br/>related to at most one A?}\n    Q1 -- No --> M1[Many-to-Many M:N]\n    M1 --> M1note[CREATE new table R with<br/>composite PK A.PK, B.PK]\n    Q2 -- Yes --> OneOne[1:1 Relationship]\n    Q2 -- No --> Q3{Is one B related<br/>to many A?}\n    Q3 -- Yes --> OneMany[1:N Relationship<br/>A is 1-side, B is N-side]\n    Q3 -- No --> Q4{Re-evaluate problem statement}\n    Q4 --> Q1\n\n    OneOne --> O1[Merge: Add A.PK as FK in B<br/>OR B.PK as FK in A]\n    OneMany --> O2[Add A.PK as FK in B table<br/>Total participation implies NOT NULL]\n\n    style OneOne fill:#FFD700\n    style OneMany fill:#FFD700\n    style M1 fill:#90EE90\n    style M1note fill:#FFB6C1\n    style O1 fill:#ADD8E6\n    style O2 fill:#ADD8E6\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n---\n\n### **Q1. [KTU University Exam - May 2024]**\n**Define the following with one example each:**\n**(a) Entity and Entity Set**\n**(b) Total Participation**\n\n**Model Answer:**\n\n**(a)** An **Entity** is a real-world object with independent existence that can be uniquely identified. Example: A specific student named \"Rahul\" with roll number `S001` is an entity. An **Entity Set** is a collection of similar entities. Example: The set of all students currently enrolled in a college is a *Student* entity set.\n\n**(b)** **Total Participation** means every entity in the entity set must participate in at least one instance of the relationship. It is represented by a **double line** connecting the entity to the relationship diamond in Chen's notation. Example: In a \"Department-Employs-Professor\" relationship, every Professor must belong to some department (total participation of Professor in *employs*).\n\n---\n\n### **Q2. [KTU University Exam - Dec 2023]**\n**Differentiate between strong entity and weak entity with suitable examples.**\n\n**Model Answer:**\n\n| Feature | Strong Entity | Weak Entity |\n|---------|---------------|-------------|\n| **Existence** | Has independent existence | Depends on a strong (owner) entity |\n| **Identifier** | Has a primary key of its own | Cannot be uniquely identified by its own attributes alone |\n| **Notation** | Single rectangle | Double rectangle |\n| **Relationship** | Not dependent | Connected to owner via a **double-diamond identifying relationship** |\n| **Example** | `Student(Roll_No, name, DOB)` — Roll_No is sufficient PK | `Dependent(Dep_Name, age, Emp_ID)` — needs `Emp_ID` from `Employee` plus `Dep_Name` to form PK |\n\n---\n\n## 5.2 Part B Questions (14 Marks Each — Internal Choice)\n\n---\n\n### **Question A (14 Marks) — [KTU University Exam - May 2024 Model]**\n\n> **Design an ER diagram for the following Library Management System and convert it into a relational schema:**\n>\n> *\"A library has multiple branches, each with a unique branch_id and location. Each branch holds many books. A book is identified by ISBN and has a title, price, and a single category. A book can be written by many authors, and an author can write many books. A book may have multiple copies, and each copy has a copy_id and a shelf location. Members of the library have a member_id, name, and phone (a member can have multiple phone numbers). A member can borrow many book copies, and a copy can be borrowed by many members over time. Each borrowing records the issue_date and return_date.\"*\n\n#### **Part (a) — Draw the complete ER diagram with all entities, attributes, relationships, cardinalities, and participation constraints. (7 Marks)**\n\n**Step 1 — Identify Entities:**\n\n- `Branch` (branch_id, location)\n- `Book` (ISBN, title, price, category)\n- `Author` (author_id, name)\n- `Book_Copy` (copy_id, ISBN_FK, branch_id_FK, shelf_location)\n- `Member` (member_id, name)\n- `Borrow` (issue_date, return_date, member_id_FK, copy_id_FK)\n\n**Step 2 — Identify Relationships:**\n\n| Relationship | Between | Cardinality | Participation |\n|--------------|---------|-------------|---------------|\n| `has` | Branch — Book_Copy | 1:N | Book_Copy side **Total** |\n| `written_by` | Book — Author | M:N | Both **Partial** |\n| `borrows` | Member — Book_Copy | M:N (with attributes) | Both **Partial** |\n\n**Step 3 — Special Attributes:**\n- `phone` in Member is a **multivalued attribute** (double ellipse).\n- `Book_Copy` is a **weak entity** (double rectangle) with partial key `copy_id`, depending on Book and Branch.\n\n**Step 4 — ER Diagram in Mermaid:**\n\n```mermaid\nerDiagram\n    BRANCH ||--o{ BOOK_COPY : \"holds (1:N)\"\n    BOOK ||--o{ BOOK_COPY : \"has_copies (1:N, identifying)\"\n    BOOK }o--o{ AUTHOR : \"written_by (M:N)\"\n    MEMBER }o--o{ BOOK_COPY : \"borrows (M:N with Borrow attrs)\"\n    MEMBER ||--o{ MEMBER_PHONE : \"has (multivalued)\"\n\n    BRANCH {\n        string branch_id PK\n        string location\n    }\n\n    BOOK {\n        string ISBN PK\n        string title\n        decimal price\n        string category\n    }\n\n    AUTHOR {\n        string author_id PK\n        string name\n    }\n\n    BOOK_COPY {\n        string copy_id PK\n        string ISBN PK,FK\n        string branch_id PK,FK\n        string shelf_location\n    }\n\n    MEMBER {\n        string member_id PK\n        string name\n    }\n\n    MEMBER_PHONE {\n        string member_id PK,FK\n        string phone PK\n    }\n\n    BORROW {\n        string member_id PK,FK\n        string copy_id PK,FK\n        string ISBN PK,FK\n        string branch_id PK,FK\n        date issue_date\n        date return_date\n    }\n```\n\n#### **Part (b) — Convert the above ER diagram into a fully normalized relational schema with primary keys, foreign keys, and constraint specification. (7 Marks)**\n\n**Applying the 7 Mapping Rules:**\n\n```sql\n-- Rule R1: Strong Entities\nCREATE TABLE Branch (\n    branch_id  VARCHAR(10) PRIMARY KEY,\n    location   VARCHAR(100) NOT NULL\n);\n\nCREATE TABLE Book (\n    ISBN     VARCHAR(13) PRIMARY KEY,\n    title    VARCHAR(200) NOT NULL,\n    price    DECIMAL(8,2) CHECK (price > 0),\n    category VARCHAR(50)\n);\n\nCREATE TABLE Author (\n    author_id VARCHAR(10) PRIMARY KEY,\n    name      VARCHAR(100) NOT NULL\n);\n\nCREATE TABLE Member (\n    member_id VARCHAR(10) PRIMARY KEY,\n    name      VARCHAR(100) NOT NULL\n);\n\n-- Rule R2: Weak Entity (Book_Copy depends on Book and Branch)\nCREATE TABLE Book_Copy (\n    copy_id        VARCHAR(10),\n    ISBN           VARCHAR(13),\n    branch_id      VARCHAR(10),\n    shelf_location VARCHAR(20) NOT NULL,\n    PRIMARY KEY (ISBN, branch_id, copy_id),\n    FOREIGN KEY (ISBN)      REFERENCES Book(ISBN),\n    FOREIGN KEY (branch_id) REFERENCES Branch(branch_id)\n);\n\n-- Rule R5: M:N Relationship (written_by)\nCREATE TABLE Book_Author (\n    ISBN      VARCHAR(13),\n    author_id VARCHAR(10),\n    PRIMARY KEY (ISBN, author_id),\n    FOREIGN KEY (ISBN)      REFERENCES Book(ISBN),\n    FOREIGN KEY (author_id) REFERENCES Author(author_id)\n);\n\n-- Rule R6: Multivalued Attribute (phone)\nCREATE TABLE Member_Phone (\n    member_id VARCHAR(10),\n    phone     VARCHAR(15),\n    PRIMARY KEY (member_id, phone),\n    FOREIGN KEY (member_id) REFERENCES Member(member_id)\n);\n\n-- Rule R5: M:N Relationship with attributes (borrows)\nCREATE TABLE Borrow (\n    member_id   VARCHAR(10),\n    copy_id     VARCHAR(10),\n    ISBN        VARCHAR(13),\n    branch_id   VARCHAR(10),\n    issue_date  DATE NOT NULL,\n    return_date DATE,\n    PRIMARY KEY (member_id, copy_id, ISBN, branch_id, issue_date),\n    FOREIGN KEY (member_id) REFERENCES Member(member_id),\n    FOREIGN KEY (copy_id, ISBN, branch_id) \n        REFERENCES Book_Copy(copy_id, ISBN, branch_id)\n);\n```\n\n**Final Optimized Relational Schema:**\n\n$$\n\\boxed{\n\\begin{aligned}\n&\\textbf{Branch}(\\underline{\\text{branch\\_id}},\\ \\text{location}) \\\\\n&\\textbf{Book}(\\underline{\\text{ISBN}},\\ \\text{title},\\ \\text{price},\\ \\text{category}) \\\\\n&\\textbf{Author}(\\underline{\\text{author\\_id}},\\ \\text{name}) \\\\\n&\\textbf{Book\\_Copy}(\\underline{\\text{ISBN}^{\\text{FK}}},\\ \\underline{\\text{branch\\_id}^{\\text{FK}}},\\ \\underline{\\text{copy\\_id}},\\ \\text{shelf\\_location}) \\\\\n&\\textbf{Member}(\\underline{\\text{member\\_id}},\\ \\text{name}) \\\\\n&\\textbf{Member\\_Phone}(\\underline{\\text{member\\_id}^{\\text{FK}}},\\ \\underline{\\text{phone}}) \\\\\n&\\textbf{Book\\_Author}(\\underline{\\text{ISBN}^{\\text{FK}}},\\ \\underline{\\text{author\\_id}^{\\text{FK}}}) \\\\\n&\\textbf{Borrow}(\\underline{\\text{member\\_id}^{\\text{FK}}},\\ \\underline{\\text{copy\\_id}^{\\text{FK}}},\\ \\underline{\\text{ISBN}^{\\text{FK}}},\\ \\underline{\\text{branch\\_id}^{\\text{FK}}},\\ \\underline{\\text{issue\\_date}},\\ \\text{return\\_date})\n\\end{aligned}\n}\n$$\n\n---\n\n### **Question B (14 Marks) — [KTU University Exam - Dec 2023 Model]**\n\n> **Design an ER diagram and relational schema for the following Hospital Management System:**\n>\n> *\"A hospital has many departments, each with a Dept_ID, name, and location. Each department has multiple doctors, identified by Doc_ID, name, specialization, and salary. A doctor treats many patients, and a patient can be treated by multiple doctors. A patient has a Pat_ID, name, age, and may have multiple phone numbers. Patients are admitted to rooms. A room has a room_no, type (general/ICU/private), and a daily charge. A patient may be admitted multiple times (each admission has an admission_id, admit_date, and discharge_date). Each admission is to exactly one room, but a room can host many admissions over time.\"*\n\n#### **Part (a) — Identify all entities, attributes (including multivalued and derived), relationships, cardinalities, and participation. Draw the ER diagram. (7 Marks)**\n\n**Entities & Attributes:**\n\n| Entity | Primary Key | Other Attributes |\n|--------|-------------|------------------|\n| Department | `Dept_ID` | `name`, `location` |\n| Doctor | `Doc_ID` | `name`, `specialization`, `salary` |\n| Patient | `Pat_ID` | `name`, `age` (derived from DOB), `DOB`, `phone` (multivalued) |\n| Room | `room_no` | `type`, `daily_charge` |\n| Admission | `admission_id` | `admit_date`, `discharge_date` |\n\n**Relationships & Cardinalities:**\n\n| Relationship | Cardinality | Participation | Notes |\n|--------------|-------------|---------------|-------|\n| Department — Doctor | 1:N | Doctor side **Total** | Each doctor belongs to one dept |\n| Doctor — Patient (treats) | M:N | Both **Partial** | Many-to-many treatment relationship |\n| Patient — Room (admitted_via) | M:N (through Admission) | Patient side **Total** (during admission) | Need association entity Admission |\n| Admission — Room | 1:N (one admission → one room; one room → many admissions) | Admission side **Total** | Each admission is to exactly one room |\n\n**Derived attribute:** `age` is derived from `DOB` (shown with dashed ellipse).\n**Multivalued attribute:** `phone` of Patient (shown with double ellipse).\n\n**ER Diagram (Mermaid):**\n\n```mermaid\nerDiagram\n    DEPARTMENT ||--o{ DOCTOR : \"employs (1:N)\"\n    DOCTOR }o--o{ PATIENT : \"treats (M:N)\"\n    PATIENT ||--o{ ADMISSION : \"has (1:N, identifying)\"\n    ROOM ||--o{ ADMISSION : \"hosts (1:N)\"\n    PATIENT ||--o{ PATIENT_PHONE : \"has (multivalued)\"\n\n    DEPARTMENT {\n        string Dept_ID PK\n        string name\n        string location\n    }\n\n    DOCTOR {\n        string Doc_ID PK\n        string name\n        string specialization\n        decimal salary\n        string Dept_ID FK\n    }\n\n    PATIENT {\n        string Pat_ID PK\n        string name\n        date DOB\n        int age\n    }\n\n    PATIENT_PHONE {\n        string Pat_ID PK,FK\n        string phone PK\n    }\n\n    ROOM {\n        string room_no PK\n        string type\n        decimal daily_charge\n    }\n\n    ADMISSION {\n        string admission_id PK\n        string Pat_ID PK,FK\n        string room_no PK,FK\n        date admit_date\n        date discharge_date\n    }\n```\n\n#### **Part (b) — Convert the ER diagram to a normalized relational schema and write the corresponding SQL DDL. (7 Marks)**\n\n```sql\n-- R1: Strong Entities\nCREATE TABLE Department (\n    Dept_ID   VARCHAR(10) PRIMARY KEY,\n    name      VARCHAR(100) NOT NULL,\n    location  VARCHAR(100) NOT NULL\n);\n\nCREATE TABLE Doctor (\n    Doc_ID         VARCHAR(10) PRIMARY KEY,\n    name           VARCHAR(100) NOT NULL,\n    specialization VARCHAR(50),\n    salary         DECIMAL(10,2) CHECK (salary > 0),\n    Dept_ID        VARCHAR(10) NOT NULL,\n    FOREIGN KEY (Dept_ID) REFERENCES Department(Dept_ID)\n);\n\nCREATE TABLE Patient (\n    Pat_ID VARCHAR(15) PRIMARY KEY,\n    name   VARCHAR(100) NOT NULL,\n    DOB    DATE NOT NULL\n);\n\nCREATE TABLE Room (\n    room_no      VARCHAR(10) PRIMARY KEY,\n    type         VARCHAR(20) CHECK (type IN ('General', 'ICU', 'Private')),\n    daily_charge DECIMAL(8,2) CHECK (daily_charge >= 0)\n);\n\n-- R5: M:N Doctor-Patient (treats)\nCREATE TABLE Treatment (\n    Doc_ID     VARCHAR(10),\n    Pat_ID     VARCHAR(15),\n    treatment_date DATE,\n    diagnosis  TEXT,\n    PRIMARY KEY (Doc_ID, Pat_ID, treatment_date),\n    FOREIGN KEY (Doc_ID) REFERENCES Doctor(Doc_ID),\n    FOREIGN KEY (Pat_ID) REFERENCES Patient(Pat_ID)\n);\n\n-- R6: Multivalued phone\nCREATE TABLE Patient_Phone (\n    Pat_ID VARCHAR(15),\n    phone  VARCHAR(15),\n    PRIMARY KEY (Pat_ID, phone),\n    FOREIGN KEY (Pat_ID) REFERENCES Patient(Pat_ID)\n);\n\n-- R1 + R4: Admission (associative entity linking Patient and Room)\nCREATE TABLE Admission (\n    admission_id  VARCHAR(15) PRIMARY KEY,\n    Pat_ID        VARCHAR(15) NOT NULL,\n    room_no       VARCHAR(10) NOT NULL,\n    admit_date    DATE NOT NULL,\n    discharge_date DATE,\n    FOREIGN KEY (Pat_ID)  REFERENCES Patient(Pat_ID),\n    FOREIGN KEY (room_no) REFERENCES Room(room_no)\n);\n```\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls**\n> 1. **M:N Trap**: Students often treat `Doctor–Patient` as 1:N because \"one doctor treats many patients\" — but forget that \"one patient can be treated by many doctors\". This makes it **M:N** and **REQUIRES a separate `Treatment` table**. (Penalty: −2 marks)\n> 2. **Multivalued Attribute Neglect**: Forgetting to create `Patient_Phone` for the `phone` attribute. In SQL, this violates **1NF**. (Penalty: −1 mark)\n> 3. **Weak Entity Confusion**: Treating `Admission` as a strong entity. It is a strong entity here (it has its own PK `admission_id`), but `Book_Copy` in the previous question was a weak entity. Read the problem carefully! (Penalty: −1 mark)\n> 4. **Missing Cardinality Labels**: Drawing relationships without `1:1`, `1:N`, `M:N` labels. (Penalty: −1 mark)\n> 5. **FK Notation**: Writing `FOREIGN KEY` but forgetting to mark it as FK in the schema box. Always annotate with `FK` superscript. (Penalty: −1 mark)\n> 6. **Derived Attribute**: Storing `age` in the table. Derived attributes must be **computed at query time**, not stored. (Penalty: −1 mark)\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!TIP]\n> **High-Density Rapid Revision Checklist — KTU Module Experiment 2**\n\n### ER Modeling Essentials\n- ☐ **Entity = Noun**, **Attribute = Adjective**, **Relationship = Verb**\n- ☐ Primary key must be **underlined** in the ER diagram\n- ☐ **Multivalued** = double ellipse, **Derived** = dashed ellipse, **Composite** = ellipse with sub-ellipses\n- ☐ **Weak entity** = double rectangle, **Identifying relationship** = double diamond\n- ☐ **Cardinality** must be explicitly written: 1:1, 1:N, M:N\n- ☐ **Participation** must be shown: single line (partial) or double line (total)\n\n### Mapping Rules (The 7 Commandments)\n- ☐ **R1** — Every strong entity → one table\n- ☐ **R2** — Weak entity → table with composite PK `(owner_PK, partial_key)`\n- ☐ **R3** — 1:1 relationship → merge (add FK to either side)\n- ☐ **R4** — 1:N relationship → add 1-side PK as FK on N-side\n- ☐ **R5** — M:N relationship → **new separate table** with composite PK\n- ☐ **R6** — Multivalued attribute → **new separate table** with `(entity_PK, attribute)`\n- ☐ **R7** — n-ary relationship (n ≥ 3) → new table with all participating PKs\n\n### Schema Writing Best Practices\n- ☐ Always specify **PRIMARY KEY** and **FOREIGN KEY** constraints\n- ☐ Use **NOT NULL** for mandatory attributes (e.g., name, primary keys)\n- ☐ Use **CHECK** constraints for domain validation (e.g., `salary > 0`, `credits BETWEEN 1 AND 6`)\n- ☐ Choose appropriate **data types**: `VARCHAR`, `INT`, `DECIMAL`, `DATE`, `CHAR`\n- ☐ Composite primary keys are written as `PRIMARY KEY (col1, col2)`\n- ☐ Foreign key references must match the referenced table's column(s)\n\n### KTU Exam-Specific Reminders\n- ☐ Draw ER diagram **before** writing the schema — **always**\n- ☐ Use **Chen's notation** by default (rectangles + diamonds + ellipses)\n- ☐ Label every relationship with a verb and cardinality\n- ☐ If using Crow's Foot, label with `|, O|, <, O<` symbols\n- ☐ For SQL DDL questions, include all constraints in `CREATE TABLE` statements\n- ☐ Do **not** include sample data (`INSERT` statements) unless asked\n- ☐ Verify normalization — the schema should be in **3NF** minimum\n- ☐ Common exam duration: 3 hours, full experiment should be solvable in 45 minutes\n\n### Quick Memory Trick\n> 🎯 **\"1:1 Merge, 1:N FK, M:N New\"** — Repeat this 10 times before the exam.\n\n### Bonus Conceptual Check\n- ☐ Can you identify a **recursive relationship**? (e.g., Employee supervises Employee)\n- ☐ Can you identify a **ternary relationship**? (e.g., Doctor prescribes Drug to Patient — all 3 entities)\n- ☐ Can you convert a **generalization/specialization** hierarchy to a schema? (Use \"ISA\" relationship)\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pccsl408-mExperiment 2-t2",
          "title": "Creation of database schema - DDL (create tables, set constraints, enforce relationships, create indices, delete and modify tables); export ER diagram from the database and verify relationships",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL408 - DBMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Creation of database schema - DDL (create tables, set constraints, enforce relationships, create indices, delete and modify tables). Export ER diagram from the database and verify relationships\n\n<!-- SECTION_1_START -->\n# SECTION 1 — Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\nIn the context of the **KTU 2024 Scheme DBMS Laboratory (PCCSL408)**, *Experiment 2* focuses on the **Data Definition Language (DDL)** subset of SQL. According to the KTU 2024 syllabus, the objective of this experiment is to **create a database schema** using DDL statements, **define integrity constraints**, **enforce referential relationships**, **create indices** for performance, and to **modify or delete** existing schema objects. Subsequently, the learner is expected to **reverse-engineer an ER diagram** from the created database to visually verify the relationships.\n\n**DDL (Data Definition Language)** is the subset of SQL used to define, alter, and remove database structures (schemas, tables, views, indexes). Unlike DML (Data Manipulation Language), DDL commands are **auto-committed** — their effect is permanent and cannot be rolled back without an explicit backup.\n\nThe principal DDL commands mandated by this experiment are:\n\n| Command | Purpose | Auto-Commit? |\n|---|---|---|\n| `CREATE DATABASE` | Creates a new database container | Yes |\n| `CREATE TABLE` | Defines a new table and its columns | Yes |\n| `ALTER TABLE` | Modifies an existing table structure | Yes |\n| `DROP TABLE` | Permanently deletes a table | Yes |\n| `TRUNCATE TABLE` | Removes all rows but keeps structure | Yes |\n| `CREATE INDEX` | Builds a search-acceleration structure | Yes |\n| `DROP INDEX` | Removes an index | Yes |\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Highlight:** For the lab record and university practical examination, students are expected to (a) write the schema for a mini-world problem (such as *Library*, *Hospital*, *Student-Course*, *Banking*, or *Inventory*), (b) implement it in **MySQL / Oracle / PostgreSQL**, (c) verify it with `DESC table_name;` or equivalent, and (d) generate the ER diagram using tools like **MySQL Workbench (Reverse Engineer)**, **DBeaver**, or **dbdiagram.io**.\n\n## 1.2 Conceptual Analogy — The \"Building a Filing Cabinet\" Model\n\nImagine a **company office** that wants to store employee records. Before the clerk can write any name into a register (DML), the manager must first:\n\n1. **Buy a cabinet** → `CREATE DATABASE company;`\n2. **Install drawers** (e.g., *Employees*, *Departments*, *Projects*) → `CREATE TABLE ...`\n3. **Label every column and decide the data type** (Name = text, Salary = number) → column definitions with datatypes.\n4. **Set rules** — *Salary cannot be negative*, *Employee ID cannot be duplicated*, *Every department must have a unique code* → **constraints** (`CHECK`, `PRIMARY KEY`, `UNIQUE`).\n5. **Create a link** between drawers — e.g., a \"Project Assignment\" drawer must always reference a real employee → **foreign key relationships**.\n6. **Add a quick-lookup index tab** on the most-searched column (say, *Last Name*) → `CREATE INDEX`.\n7. Later, if a drawer is renamed or a column is added → `ALTER TABLE`.\n8. If a drawer is no longer needed → `DROP TABLE`.\n\nOnce the cabinet is fully built, the auditor can take photographs of all the drawers and the wiring between them to produce an **ER diagram** — verifying that the rules were installed correctly.\n\n> [!NOTE]\n> **Memory Anchor:** *DDL is to the database what a blueprint is to a building. It defines the structure, not the contents.*\n\n---\n\n## 1.3 Why This Experiment Matters in KTU Evaluation\n\nThe KTU 2024 Scheme laboratory evaluation for PCCSL408 follows a **continuous assessment + end-semester practical** model. Experiment 2 typically tests:\n\n- **CO2 (Apply):** Ability to design a normalized schema from a given problem statement.\n- **CO3 (Analyze):** Choosing correct constraints, datatypes, and referential actions.\n- **CO4 (Create):** Implementing the design and verifying it via ER reverse-engineering.\n\nMastering DDL is foundational — every later experiment (DML, joins, views, triggers) depends on a well-defined schema.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2 — Deep Theoretical Analysis & KTU High-Yield Cheat Sheet\n\n## 2.1 The DDL Command Family — Operational Logic\n\n### 2.1.1 `CREATE DATABASE` and `USE`\n\nA **database** in MySQL/Oracle is a logical container of related tables, views, procedures, and indexes.\n\n```sql\nCREATE DATABASE IF NOT EXISTS college_db;\nUSE college_db;\n```\n\n`IF NOT EXISTS` prevents an error if the database already exists — a KTU-exam best practice.\n\n### 2.1.2 `CREATE TABLE` — The Core Construct\n\nThe general syntactic skeleton is:\n\n```sql\nCREATE TABLE table_name (\n    column1 datatype [constraint],\n    column2 datatype [constraint],\n    ...\n    [table-level constraint]\n);\n```\n\n### 2.1.3 Datatypes (KTU High-Yield List)\n\n| Category | Datatypes | Notes |\n|---|---|---|\n| Integer | `INT`, `SMALLINT`, `BIGINT`, `TINYINT` | Use `INT` for IDs |\n| Decimal | `DECIMAL(p,s)`, `FLOAT`, `DOUBLE` | `DECIMAL(8,2)` for currency |\n| Character | `CHAR(n)`, `VARCHAR(n)`, `TEXT` | `VARCHAR` is variable-length |\n| Date/Time | `DATE`, `TIME`, `DATETIME`, `TIMESTAMP`, `YEAR` | Format: `YYYY-MM-DD` |\n| Boolean | `BOOLEAN` (alias of `TINYINT(1)`) | MySQL specific |\n| Binary | `BLOB`, `MEDIUMBLOB`, `LONGBLOB` | For images/files |\n\n> [!IMPORTANT]\n> **Oracle vs MySQL Pitfall:** Oracle uses `VARCHAR2(n)` instead of `VARCHAR(n)`, and `NUMBER(p,s)` instead of `DECIMAL`. Always declare the target RDBMS at the top of your lab record.\n\n### 2.1.4 Integrity Constraints — The Five Pillars\n\nConstraints are **rules enforced by the DBMS** to maintain data integrity. They are the heart of this experiment.\n\n| # | Constraint | Purpose | Example Syntax |\n|---|---|---|---|\n| 1 | **PRIMARY KEY** | Uniquely identifies each row; NOT NULL + UNIQUE | `sid INT PRIMARY KEY` |\n| 2 | **FOREIGN KEY** | Enforces referential integrity between tables | `FOREIGN KEY (dept_id) REFERENCES Department(dept_id)` |\n| 3 | **UNIQUE** | Prevents duplicate values; allows multiple NULLs | `email VARCHAR(60) UNIQUE` |\n| 4 | **NOT NULL** | Disallows empty values | `name VARCHAR(40) NOT NULL` |\n| 5 | **CHECK** | Validates a domain/range condition | `CHECK (marks BETWEEN 0 AND 100)` |\n| 6 | **DEFAULT** | Provides a fallback value | `status VARCHAR(10) DEFAULT 'Active'` |\n| 7 | **AUTO_INCREMENT** | Auto-generates sequential values (MySQL) | `id INT AUTO_INCREMENT PRIMARY KEY` |\n\n### 2.1.5 Referential Actions (ON DELETE / ON UPDATE)\n\nWhen a parent row is deleted or updated, the foreign-key child row behaviour is governed by:\n\n- `CASCADE` — propagate the change (delete/update child rows).\n- `SET NULL` — set child FK to NULL (requires FK to be nullable).\n- `RESTRICT` / `NO ACTION` — reject the parent change if children exist (default in most RDBMS).\n- `SET DEFAULT` — set child FK to its default value.\n\n```sql\nFOREIGN KEY (dept_id) REFERENCES Department(dept_id)\n    ON DELETE CASCADE\n    ON UPDATE CASCADE;\n```\n\n### 2.1.6 `CREATE INDEX` — Performance Tuning\n\nAn **index** is a separate data structure (typically a B+ tree) that allows the DBMS to locate rows faster than a full table scan.\n\n```sql\n-- Simple index\nCREATE INDEX idx_student_name ON Student(sname);\n\n-- Unique index\nCREATE UNIQUE INDEX idx_student_email ON Student(email);\n\n-- Composite index (multi-column)\nCREATE INDEX idx_student_dept_year ON Student(dept_id, year);\n\n-- Drop index\nDROP INDEX idx_student_name ON Student;\n```\n\n> [!NOTE]\n> **Why not index every column?** Each index consumes storage and slows down INSERT/UPDATE/DELETE. The KTU 2024 syllabus emphasizes indexing only on **search-heavy, join-heavy, and WHERE-clause-heavy** columns.\n\n### 2.1.7 `ALTER TABLE` — Schema Evolution\n\n```sql\n-- Add a column\nALTER TABLE Student ADD phone VARCHAR(15);\n\n-- Modify a column\nALTER TABLE Student MODIFY sname VARCHAR(60) NOT NULL;\n\n-- Rename a column\nALTER TABLE Student RENAME COLUMN phone TO mobile;\n\n-- Drop a column\nALTER TABLE Student DROP COLUMN mobile;\n\n-- Add a constraint after table creation\nALTER TABLE Student ADD CONSTRAINT chk_marks CHECK (marks >= 0 AND marks <= 100);\n\n-- Rename a table\nALTER TABLE Student RENAME TO Student_Info;\n```\n\n> [!WARNING]\n> **Oracle syntax differs:** `ALTER TABLE Student MODIFY (sname VARCHAR2(60) NOT NULL);` and `RENAME TO` is `RENAME Student TO Student_Info;`.\n\n### 2.1.8 `DROP TABLE` vs `TRUNCATE TABLE` vs `DELETE`\n\n| Operation | Removes Structure? | Removes Data? | Can Rollback? | Fires Triggers? |\n|---|---|---|---|---|\n| `DROP TABLE` | ✅ Yes | ✅ Yes | ❌ No | ❌ No |\n| `TRUNCATE TABLE` | ❌ No | ✅ Yes (all) | ❌ No (DDL) | ❌ No |\n| `DELETE FROM` | ❌ No | ✅ Yes (selective) | ✅ Yes (DML) | ✅ Yes |\n\n```sql\nDROP TABLE IF EXISTS Old_Student;\nTRUNCATE TABLE Temp_Log;\n```\n\n## 2.2 The KTU High-Yield DDL Cheat Sheet\n\n| Feature | MySQL Syntax | Oracle Syntax |\n|---|---|---|\n| Auto-increment | `id INT AUTO_INCREMENT PRIMARY KEY` | `id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY` |\n| String | `VARCHAR(n)` | `VARCHAR2(n)` |\n| Decimal | `DECIMAL(p,s)` | `NUMBER(p,s)` |\n| Date | `DATE` (with time) | `DATE` (date+time) |\n| Drop table | `DROP TABLE t;` | `DROP TABLE t CASCADE CONSTRAINTS;` (if FK exists) |\n| View structure | `DESC t;` | `DESCRIBE t;` |\n| Show FKs | `SHOW CREATE TABLE t\\G` | `SELECT * FROM USER_CONSTRAINTS WHERE TABLE_NAME='T';` |\n\n## 2.3 Reverse-Engineering the ER Diagram — Real-World Utility\n\nAfter implementing the schema, the KTU lab manual requires an **ER diagram export**. The workflow is:\n\n1. **MySQL Workbench:** Database → *Reverse Engineer* → select schema → tool generates EER diagram showing all tables, columns, PK/FK, and crow's-foot relationships.\n2. **DBeaver:** Right-click schema → *View Diagram*.\n3. **dbdiagram.io:** Use the *Import from MySQL* feature with the generated DDL.\n4. **Oracle SQL Developer:** File → *Data Modeler* → *Import* → *Data Dictionary*.\n\nThe exported diagram must be **visually inspected** to verify:\n- All entities (tables) are present.\n- Primary keys are underlined.\n- Foreign-key links exist between parent and child tables.\n- Cardinality (1:1, 1:N, M:N via junction table) is correct.\n- No orphan tables (tables with no relationship).\n\n> [!NOTE]\n> **Engineering Utility:** Reverse-engineering is a critical skill in **legacy database migration**, **system refactoring**, and **audit compliance** (e.g., understanding the data model of a 20-year-old banking system before modernization).\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3 — Step-by-Step Implementation & Analytical Matrix\n\nIn this section, we build a complete **University Course Enrollment** schema — a classic KTU lab problem — step by step. Every SQL block is annotated with the *intent* of the statement, mirroring the **lab record viva expectations**.\n\n## 3.1 Problem Statement\n\n> Design a database for a university with the following requirements:\n> - A **Department** has a unique name, a department code, and a head (professor name).\n> - A **Student** belongs to exactly one Department, has a unique roll number, name, date of birth, email, and current semester.\n> - A **Course** is offered by one Department, has a unique course code, title, and credits.\n> - A **Faculty** member belongs to one Department, has a unique ID, name, designation, and salary.\n> - A **Faculty** can teach multiple **Courses**.\n> - A **Student** can enroll in many **Courses** and vice versa — track enrollment date and grade.\n> - Marks obtained by a student in a course must be between 0 and 100.\n> - Salary of faculty must be greater than zero.\n> - Email of student and faculty must be unique.\n\n## 3.2 Step-by-Step DDL Implementation\n\n### Step 1 — Create and Select the Database\n\n```sql\n-- Create a fresh database\nCREATE DATABASE IF NOT EXISTS UniversityDB;\n\n-- Activate it\nUSE UniversityDB;\n```\n\n> **Logic:** The `IF NOT EXISTS` clause is a defensive practice — it prevents an *Error 1007: Can't create database 'UniversityDB'; database exists* in the lab evaluation environment.\n\n### Step 2 — Create the Parent Table: `Department`\n\n```sql\nCREATE TABLE Department (\n    dept_id      INT             PRIMARY KEY,\n    dept_name    VARCHAR(50)     NOT NULL UNIQUE,\n    dept_code    CHAR(5)         NOT NULL UNIQUE,\n    head_name    VARCHAR(60)     NOT NULL\n);\n```\n\n> **Logic:**\n> - `dept_id` is the **surrogate primary key** — small, integer, immutable.\n> - `dept_name` and `dept_code` are `UNIQUE` because a university cannot have two CS departments.\n> - `head_name` is `NOT NULL` because every department must have a head.\n\n### Step 3 — Create the `Faculty` Table (FK to Department)\n\n```sql\nCREATE TABLE Faculty (\n    faculty_id   INT             PRIMARY KEY,\n    fname        VARCHAR(50)     NOT NULL,\n    designation  VARCHAR(30)     NOT NULL DEFAULT 'Assistant Professor',\n    salary       DECIMAL(10,2)   NOT NULL,\n    email        VARCHAR(80)     NOT NULL UNIQUE,\n    dept_id      INT             NOT NULL,\n    CONSTRAINT  fk_fac_dept\n        FOREIGN KEY (dept_id) REFERENCES Department(dept_id)\n        ON DELETE RESTRICT\n        ON UPDATE CASCADE,\n    CONSTRAINT  chk_fac_salary\n        CHECK (salary > 0)\n);\n```\n\n> **Logic:**\n> - The **named constraint** `fk_fac_dept` improves readability and easier `ALTER TABLE DROP CONSTRAINT` later.\n> - `ON DELETE RESTRICT` prevents deleting a department that has faculty assigned — a sensible business rule.\n> - `CHECK (salary > 0)` enforces the third business rule.\n\n### Step 4 — Create the `Student` Table (FK to Department)\n\n```sql\nCREATE TABLE Student (\n    roll_no      INT             PRIMARY KEY,\n    sname        VARCHAR(50)     NOT NULL,\n    dob          DATE            NOT NULL,\n    email        VARCHAR(80)     NOT NULL UNIQUE,\n    semester     INT             NOT NULL DEFAULT 1,\n    dept_id      INT             NOT NULL,\n    CONSTRAINT  fk_stu_dept\n        FOREIGN KEY (dept_id) REFERENCES Department(dept_id)\n        ON DELETE RESTRICT\n        ON UPDATE CASCADE,\n    CONSTRAINT  chk_stu_sem\n        CHECK (semester BETWEEN 1 AND 12)\n);\n```\n\n> **Logic:**\n> - `dob DATE` uses the canonical `YYYY-MM-DD` format.\n> - `semester` is bounded between 1 and 12.\n> - The FK uses **table-level** constraint syntax — useful when the constraint spans multiple columns.\n\n### Step 5 — Create the `Course` Table (FK to Department)\n\n```sql\nCREATE TABLE Course (\n    course_code  VARCHAR(10)     PRIMARY KEY,\n    ctitle       VARCHAR(80)     NOT NULL,\n    credits      INT             NOT NULL,\n    dept_id      INT             NOT NULL,\n    CONSTRAINT  fk_course_dept\n        FOREIGN KEY (dept_id) REFERENCES Department(dept_id)\n        ON DELETE RESTRICT\n        ON UPDATE CASCADE,\n    CONSTRAINT  chk_course_credits\n        CHECK (credits BETWEEN 1 AND 6)\n);\n```\n\n> **Logic:** A natural key (`course_code` like `CSL201`) is used as the primary key because it is short, unique, and meaningful.\n\n### Step 6 — Create the Junction Table: `Faculty_Course` (M:N Relationship)\n\n```sql\nCREATE TABLE Faculty_Course (\n    faculty_id   INT             NOT NULL,\n    course_code  VARCHAR(10)     NOT NULL,\n    academic_year VARCHAR(9)    NOT NULL,    -- e.g., '2024-2025'\n    PRIMARY KEY (faculty_id, course_code, academic_year),\n    CONSTRAINT  fk_fc_fac\n        FOREIGN KEY (faculty_id) REFERENCES Faculty(faculty_id)\n        ON DELETE CASCADE\n        ON UPDATE CASCADE,\n    CONSTRAINT  fk_fc_course\n        FOREIGN KEY (course_code) REFERENCES Course(course_code)\n        ON DELETE CASCADE\n        ON UPDATE CASCADE\n);\n```\n\n> **Logic:** A **composite primary key** ensures a faculty member cannot be assigned the same course twice in the same academic year. M:N relationships are modelled via a **junction (associative) table**.\n\n### Step 7 — Create the Junction Table: `Enrollment` (Student–Course M:N)\n\n```sql\nCREATE TABLE Enrollment (\n    roll_no       INT            NOT NULL,\n    course_code   VARCHAR(10)    NOT NULL,\n    enroll_date   DATE           NOT NULL DEFAULT (CURRENT_DATE),\n    marks         INT            DEFAULT NULL,\n    grade         CHAR(2)        DEFAULT NULL,\n    PRIMARY KEY (roll_no, course_code),\n    CONSTRAINT  fk_en_stu\n        FOREIGN KEY (roll_no) REFERENCES Student(roll_no)\n        ON DELETE CASCADE\n        ON UPDATE CASCADE,\n    CONSTRAINT  fk_en_course\n        FOREIGN KEY (course_code) REFERENCES Course(course_code)\n        ON DELETE CASCADE\n        ON UPDATE CASCADE,\n    CONSTRAINT  chk_en_marks\n        CHECK (marks IS NULL OR (marks BETWEEN 0 AND 100))\n);\n```\n\n> **Logic:** Marks are allowed to be `NULL` (grade not yet published) but bounded between 0–100 once entered. `CURRENT_DATE` is the default enrollment timestamp.\n\n### Step 8 — Create Indices for Performance\n\n```sql\n-- Frequently searched columns\nCREATE INDEX idx_student_name  ON Student(sname);\nCREATE INDEX idx_faculty_name  ON Faculty(fname);\nCREATE INDEX idx_course_dept   ON Course(dept_id);\n\n-- Composite index for common JOIN+WHERE combinations\nCREATE INDEX idx_enroll_grade ON Enrollment(grade, marks);\n```\n\n> **Logic:** We do **not** index primary-key columns (DBMS does that automatically) or foreign-key columns unnecessarily (FKs in MySQL/InnoDB are auto-indexed).\n\n### Step 9 — Demonstrate Schema Modification (`ALTER TABLE`)\n\n```sql\n-- Add a new column to Student\nALTER TABLE Student ADD phone VARCHAR(15);\n\n-- Modify the phone column to be NOT NULL\nALTER TABLE Student MODIFY phone VARCHAR(15) NOT NULL;\n\n-- Rename the column\nALTER TABLE Student RENAME COLUMN phone TO mobile_no;\n\n-- Add a CHECK constraint post-creation\nALTER TABLE Faculty\n    ADD CONSTRAINT chk_fac_designation\n    CHECK (designation IN ('Assistant Professor','Associate Professor','Professor','HOD'));\n\n-- Drop the column we just added\nALTER TABLE Student DROP COLUMN mobile_no;\n```\n\n> **Logic:** `ALTER TABLE` operations in MySQL are **metadata operations**; they rewrite the table's `.ibd` file (in InnoDB) but the data is preserved.\n\n### Step 10 — Demonstrate Deletion (`DROP` / `TRUNCATE`)\n\n```sql\n-- Create a temporary table for demo\nCREATE TABLE Temp_Log (\n    log_id   INT PRIMARY KEY AUTO_INCREMENT,\n    message  VARCHAR(200),\n    log_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\n-- Remove all rows, keep structure\nTRUNCATE TABLE Temp_Log;\n\n-- Drop the table entirely\nDROP TABLE IF EXISTS Temp_Log;\n```\n\n> **Logic:** `TRUNCATE` resets `AUTO_INCREMENT` to 1; `DROP` removes the table file from the database directory.\n\n## 3.3 Verification Block — Run These to Confirm Schema\n\n```sql\n-- View table structure\nDESC Student;\nDESC Faculty;\n\n-- View all tables\nSHOW TABLES;\n\n-- View constraint details\nSELECT CONSTRAINT_NAME, CONSTRAINT_TYPE, TABLE_NAME\nFROM INFORMATION_SCHEMA.TABLE_CONSTRAINTS\nWHERE TABLE_SCHEMA = 'UniversityDB';\n\n-- View foreign keys explicitly\nSELECT\n    TABLE_NAME, COLUMN_NAME, CONSTRAINT_NAME,\n    REFERENCED_TABLE_NAME, REFERENCED_COLUMN_NAME\nFROM INFORMATION_SCHEMA.KEY_COLUMN_USAGE\nWHERE TABLE_SCHEMA = 'UniversityDB'\n  AND REFERENCED_TABLE_NAME IS NOT NULL;\n```\n\n## 3.4 Analytical Matrix — When to Use Each DDL Element\n\n| Decision | Choose | Reason |\n|---|---|---|\n| Surrogate vs Natural PK? | **Surrogate INT AUTO_INCREMENT** (e.g., `roll_no`, `dept_id`) | Stability, performance, immutability |\n| `CHAR` vs `VARCHAR`? | `CHAR` for fixed-length codes (`dept_code`), `VARCHAR` for names | Storage efficiency |\n| `CASCADE` vs `RESTRICT`? | `CASCADE` for child-dependent rows (Enrollment), `RESTRICT` for organizational parent (Department) | Preserve historical data vs prevent orphans |\n| When to add `INDEX`? | On columns used in `WHERE`, `JOIN`, `ORDER BY` | Avoid full table scans |\n| `CHECK` vs `TRIGGER`? | `CHECK` for simple domain rules, `TRIGGER` for cross-table business logic | Constraint engine is faster |\n| `DROP` vs `TRUNCATE`? | `DROP` for full removal, `TRUNCATE` for resetting test data | `DROP` is more destructive |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4 — Structural Diagrams & Schematics\n\n## 4.1 ER Diagram of the UniversityDB Schema\n\nThe following Mermaid diagram represents the **logical ER model** of the UniversityDB schema we built in Section 3. Every relationship is annotated with its **cardinality** (1:1, 1:N, M:N).\n\n```mermaid\nerDiagram\n    DEPARTMENT {\n        INT     dept_id PK\n        VARCHAR dept_name\n        CHAR    dept_code\n        VARCHAR head_name\n    }\n\n    STUDENT {\n        INT     roll_no PK\n        VARCHAR sname\n        DATE    dob\n        VARCHAR email\n        INT     semester\n        INT     dept_id FK\n    }\n\n    FACULTY {\n        INT      faculty_id PK\n        VARCHAR  fname\n        VARCHAR  designation\n        DECIMAL  salary\n        VARCHAR  email\n        INT      dept_id FK\n    }\n\n    COURSE {\n        VARCHAR course_code PK\n        VARCHAR ctitle\n        INT     credits\n        INT     dept_id FK\n    }\n\n    FACULTY_COURSE {\n        INT     faculty_id PK_FK\n        VARCHAR course_code PK_FK\n        VARCHAR academic_year PK\n    }\n\n    ENROLLMENT {\n        INT     roll_no PK_FK\n        VARCHAR course_code PK_FK\n        DATE    enroll_date\n        INT     marks\n        CHAR    grade\n    }\n\n    DEPARTMENT ||--o{ STUDENT       : \"has many\"\n    DEPARTMENT ||--o{ FACULTY       : \"employs\"\n    DEPARTMENT ||--o{ COURSE        : \"offers\"\n    FACULTY    ||--o{ FACULTY_COURSE : \"teaches\"\n    COURSE     ||--o{ FACULTY_COURSE : \"is taught by\"\n    STUDENT    ||--o{ ENROLLMENT    : \"enrolls in\"\n    COURSE     ||--o{ ENROLLMENT    : \"has\"\n```\n\n> [!NOTE]\n> **Reading the diagram:**\n> - `||` (double bar) = \"exactly one\" (mandatory participation).\n> - `o{` (circle + crow's foot) = \"zero or many\".\n> - The `Faculty_Course` and `Enrollment` tables are **associative entities** that resolve the M:N relationships.\n\n## 4.2 DDL Command-Category Block Diagram\n\nThis block diagram shows the **functional architecture** of DDL — its categories, their responsibilities, and the inter-dependencies (akin to a module map in a software architecture course).\n\n```mermaid\nflowchart TB\n    A[DDL Command Family] --> B[Schema Definition]\n    A --> C[Schema Modification]\n    A --> D[Schema Removal]\n    A --> E[Index Management]\n\n    B --> B1[CREATE DATABASE]\n    B --> B2[CREATE TABLE<br/>with constraints]\n    B --> B3[CREATE VIEW]\n    B --> B4[CREATE INDEX]\n\n    C --> C1[ALTER TABLE ADD]\n    C --> C2[ALTER TABLE MODIFY]\n    C --> C3[ALTER TABLE DROP]\n    C --> C4[ALTER TABLE RENAME]\n\n    D --> D1[DROP DATABASE]\n    D --> D2[DROP TABLE]\n    D --> D3[TRUNCATE TABLE]\n    D --> D4[DROP INDEX]\n\n    E --> E1[CREATE INDEX]\n    E --> E2[CREATE UNIQUE INDEX]\n    E --> E3[CREATE COMPOSITE INDEX]\n    E --> E4[DROP INDEX]\n\n    B2 -.enforces.-> F[Integrity Constraints]\n    F --> F1[PRIMARY KEY]\n    F --> F2[FOREIGN KEY]\n    F --> F3[UNIQUE / NOT NULL]\n    F --> F4[CHECK / DEFAULT]\n\n    C --> G[Reverse Engineer ER]\n    G --> G1[MySQL Workbench]\n    G --> G2[DBeaver]\n    G --> G3[dbdiagram.io]\n    G --> G4[Oracle Data Modeler]\n```\n\n## 4.3 Schema-Creation Workflow Flowchart\n\nThis flowchart models the **operational sequence** a KTU student must follow during the lab exam to construct a schema from a problem statement.\n\n```mermaid\nflowchart LR\n    P1[Step1: Read Problem Statement] --> P2[Step2: Identify Entities]\n    P2 --> P3[Step3: Identify Attributes and Datatypes]\n    P3 --> P4[Step4: Identify Primary Keys]\n    P4 --> P5[Step5: Identify Relationships and Cardinality]\n    P5 --> P6[Step6: Resolve M:N into Junction Tables]\n    P6 --> P7[Step7: Define Foreign Keys with Actions]\n    P7 --> P8[Step8: Apply CHECK UNIQUE NOT NULL]\n    P8 --> P9[Step9: Create Indices on Search Columns]\n    P9 --> P10[Step10: Implement in RDBMS]\n    P10 --> P11[Step11: Verify with DESC and SHOW]\n    P11 --> P12[Step12: Reverse-Engineer ER Diagram]\n    P12 --> P13[Step13: Cross-Check with Problem Statement]\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **[KTU University Exam - PCCSL408 Model Paper 1, Q1.a]**\n> **Q1. Differentiate between `DROP TABLE`, `TRUNCATE TABLE`, and `DELETE FROM` statements in SQL. State one scenario for each.**\n\n**Model Answer:**\n\n| Aspect | `DROP TABLE` | `TRUNCATE TABLE` | `DELETE FROM` |\n|---|---|---|---|\n| Removes structure? | ✅ Yes | ❌ No | ❌ No |\n| Removes data? | ✅ All rows | ✅ All rows | ✅ Selected rows (via `WHERE`) |\n| Rollback possible? | ❌ No (DDL) | ❌ No (DDL) | ✅ Yes (DML) |\n| Fires `DELETE` trigger? | ❌ No | ❌ No | ✅ Yes (per row) |\n| Resets `AUTO_INCREMENT`? | ✅ Yes | ✅ Yes | ❌ No |\n| Speed | Fastest | Very fast | Slowest (per-row logging) |\n\n**Scenarios:**\n- `DROP TABLE` — Removing an obsolete table like `Old_Employee_2020` from production.\n- `TRUNCATE TABLE` — Resetting a `Temp_Log` or staging table before a fresh ETL run.\n- `DELETE FROM` — Removing specific rows, e.g., `DELETE FROM Student WHERE semester > 8;`\n\n---\n\n> **[KTU University Exam - PCCSL408 Model Paper 2, Q1.b]**\n> **Q2. What is the difference between a `PRIMARY KEY` and a `UNIQUE` constraint? Can a table have multiple `UNIQUE` constraints? Justify with an example.**\n\n**Model Answer:**\n\n| Aspect | `PRIMARY KEY` | `UNIQUE` |\n|---|---|---|\n| NULL allowed? | ❌ Never | ✅ One NULL per column (DBMS-dependent) |\n| Number per table | Exactly **one** | **Multiple** allowed |\n| Creates clustered index? | Usually yes | Usually no (non-clustered) |\n| Purpose | Row identity | Business uniqueness |\n\n**Example:** In a `Faculty` table, `faculty_id` is the `PRIMARY KEY`; `email` and `aadhar_no` can both be `UNIQUE` — preventing two faculty from sharing the same email or Aadhaar number, while still allowing exactly one row identifier.\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks Each, with Internal Choice)\n\n> **[KTU University Exam - PCCSL408 End-Sem Practical Model, Q2]**\n> **Question A (14 Marks):**\n> Design a database schema for an **Online Bookstore** with the following requirements. Write complete DDL statements with appropriate constraints, demonstrate `ALTER` and `DROP` operations, and create at least two indices.\n>\n> **Requirements:**\n> - `Publisher` has publisher_id (PK), name, country, contact_email.\n> - `Author` has author_id (PK), name, nationality, dob.\n> - `Book` has isbn (PK), title, price, publication_year, publisher_id (FK).\n> - `Book_Author` resolves the M:N relationship between Book and Author (with role — e.g., 'Lead', 'Co-Author').\n> - `Customer` has customer_id (PK), name, email (unique), phone, registration_date.\n> - `Order` has order_id (PK), order_date, total_amount, customer_id (FK), with status defaulting to 'Pending'.\n> - `Order_Item` resolves the M:N between Order and Book, with quantity and unit_price.\n> - Price must be greater than 0; quantity must be ≥ 1; total_amount must be ≥ 0.\n\n### (a) — 7 Marks: Write the complete DDL with constraints\n\n```sql\nCREATE DATABASE IF NOT EXISTS BookstoreDB;\nUSE BookstoreDB;\n\n-- Publisher (parent)\nCREATE TABLE Publisher (\n    pub_id        INT          PRIMARY KEY,\n    pub_name      VARCHAR(80)  NOT NULL UNIQUE,\n    country       VARCHAR(40)  NOT NULL,\n    contact_email VARCHAR(80)  NOT NULL UNIQUE\n);\n\n-- Author (parent)\nCREATE TABLE Author (\n    author_id    INT          PRIMARY KEY,\n    aname        VARCHAR(60)  NOT NULL,\n    nationality  VARCHAR(30),\n    dob          DATE\n);\n\n-- Book (FK to Publisher)\nCREATE TABLE Book (\n    isbn             VARCHAR(13)    PRIMARY KEY,\n    title            VARCHAR(150)   NOT NULL,\n    price            DECIMAL(8,2)   NOT NULL,\n    publication_year INT            NOT NULL,\n    pub_id           INT            NOT NULL,\n    CONSTRAINT fk_book_pub\n        FOREIGN KEY (pub_id) REFERENCES Publisher(pub_id)\n        ON DELETE RESTRICT ON UPDATE CASCADE,\n    CONSTRAINT chk_book_price\n        CHECK (price > 0),\n    CONSTRAINT chk_book_year\n        CHECK (publication_year BETWEEN 1450 AND 2100)\n);\n\n-- Book_Author (M:N junction)\nCREATE TABLE Book_Author (\n    isbn       VARCHAR(13)   NOT NULL,\n    author_id  INT           NOT NULL,\n    role       VARCHAR(20)   NOT NULL DEFAULT 'Author',\n    PRIMARY KEY (isbn, author_id),\n    CONSTRAINT fk_ba_book\n        FOREIGN KEY (isbn) REFERENCES Book(isbn)\n        ON DELETE CASCADE ON UPDATE CASCADE,\n    CONSTRAINT fk_ba_author\n        FOREIGN KEY (author_id) REFERENCES Author(author_id)\n        ON DELETE CASCADE ON UPDATE CASCADE,\n    CONSTRAINT chk_ba_role\n        CHECK (role IN ('Lead','Co-Author','Editor','Author'))\n);\n\n-- Customer (independent)\nCREATE TABLE Customer (\n    cust_id           INT          PRIMARY KEY,\n    cname             VARCHAR(60)  NOT NULL,\n    email             VARCHAR(80)  NOT NULL UNIQUE,\n    phone             VARCHAR(15)  NOT NULL,\n    registration_date DATE         NOT NULL DEFAULT (CURRENT_DATE)\n);\n\n-- Order (FK to Customer)\nCREATE TABLE Orders (\n    order_id     INT          PRIMARY KEY,\n    order_date   DATE         NOT NULL DEFAULT (CURRENT_DATE),\n    total_amount DECIMAL(10,2) NOT NULL,\n    cust_id      INT          NOT NULL,\n    status       VARCHAR(15)  NOT NULL DEFAULT 'Pending',\n    CONSTRAINT fk_ord_cust\n        FOREIGN KEY (cust_id) REFERENCES Customer(cust_id)\n        ON DELETE RESTRICT ON UPDATE CASCADE,\n    CONSTRAINT chk_ord_total\n        CHECK (total_amount >= 0),\n    CONSTRAINT chk_ord_status\n        CHECK (status IN ('Pending','Shipped','Delivered','Cancelled'))\n);\n\n-- Order_Item (M:N junction with payload)\nCREATE TABLE Order_Item (\n    order_id    INT           NOT NULL,\n    isbn        VARCHAR(13)   NOT NULL,\n    quantity    INT           NOT NULL,\n    unit_price  DECIMAL(8,2)  NOT NULL,\n    PRIMARY KEY (order_id, isbn),\n    CONSTRAINT fk_oi_order\n        FOREIGN KEY (order_id) REFERENCES Orders(order_id)\n        ON DELETE CASCADE ON UPDATE CASCADE,\n    CONSTRAINT fk_oi_book\n        FOREIGN KEY (isbn) REFERENCES Book(isbn)\n        ON DELETE RESTRICT ON UPDATE CASCADE,\n    CONSTRAINT chk_oi_qty\n        CHECK (quantity >= 1),\n    CONSTRAINT chk_oi_price\n        CHECK (unit_price > 0)\n);\n```\n\n### (b) — 7 Marks: Demonstrate ALTER, DROP, and INDEX operations\n\n```sql\n-- (i) Create indices\nCREATE INDEX idx_book_title      ON Book(title);\nCREATE INDEX idx_book_pub_year   ON Book(pub_id, publication_year);\nCREATE INDEX idx_order_cust      ON Orders(cust_id);\nCREATE UNIQUE INDEX idx_cust_email ON Customer(email);\n\n-- (ii) ALTER operations\n-- Add a 'pages' column to Book\nALTER TABLE Book ADD pages INT;\n\n-- Modify the 'pages' column to NOT NULL with a default\nALTER TABLE Book MODIFY pages INT NOT NULL DEFAULT 100;\n\n-- Add a CHECK constraint to Customer\nALTER TABLE Customer\n    ADD CONSTRAINT chk_cust_phone\n    CHECK (LENGTH(phone) >= 10);\n\n-- Rename the 'status' column of Orders\nALTER TABLE Orders RENAME COLUMN status TO order_status;\n\n-- Rename the entire 'Orders' table to 'CustomerOrder'\nALTER TABLE Orders RENAME TO CustomerOrder;\n\n-- (iii) DROP operations\n-- Drop a specific index\nDROP INDEX idx_book_title ON Book;\n\n-- Drop the temporary pages column\nALTER TABLE Book DROP COLUMN pages;\n\n-- Drop a CHECK constraint\nALTER TABLE Customer DROP CONSTRAINT chk_cust_phone;\n\n-- Drop the entire CustomerOrder table\nDROP TABLE IF EXISTS CustomerOrder;\n```\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfalls — Online Bookstore Question:**\n> 1. **Forgetting to declare a FOREIGN KEY with the same datatype and width** as the referenced PK (e.g., `isbn VARCHAR(13)` in both `Book` and `Order_Item`). This causes `ERROR 1215 (HY000): Cannot add foreign key constraint`.\n> 2. **Using `CASCADE` on `Book → Publisher`** — if you delete a publisher, all their books are wiped out, losing order history. Use `RESTRICT` and reassign books first.\n> 3. **Forgetting `IF EXISTS` in `DROP TABLE`** — causes a syntax error if rerun.\n> 4. **Indexing the primary key** — the DBMS does it automatically; doing it again is wasteful and earns negative marks.\n> 5. **In MySQL, `CHECK` constraints are ignored in older versions (< 8.0.16).** The KTU lab environment should be checked for version. Use a `BEFORE INSERT/UPDATE` trigger as a fallback if needed.\n\n---\n\n> **[KTU University Exam - PCCSL408 End-Sem Practical Model, Q2 Alternative]**\n> **Question B (14 Marks):**\n> Design a database schema for a **Hospital Management System** with the following requirements. Write complete DDL with constraints, perform schema modification, and create appropriate indices.\n>\n> **Requirements:**\n> - `Department` (dept_id PK, dname, location, head_doctor_id).\n> - `Doctor` (doctor_id PK, dname, specialization, salary, dept_id FK, join_date).\n> - `Patient` (patient_id PK, pname, age, gender, admission_date, disease, dept_id FK).\n> - `Room` (room_no PK, room_type, charges_per_day, status).\n> - `Admission` (admission_id PK, patient_id FK, room_no FK, admit_date, discharge_date, total_bill).\n> - Constraints: doctor salary > 0; patient age between 0 and 130; room charges > 0; total_bill ≥ 0; one doctor can head at most one department.\n\n### (a) — 7 Marks: DDL with Constraints\n\n```sql\nCREATE DATABASE IF NOT EXISTS HospitalDB;\nUSE HospitalDB;\n\nCREATE TABLE Department (\n    dept_id          INT         PRIMARY KEY,\n    dname            VARCHAR(50) NOT NULL UNIQUE,\n    location         VARCHAR(50) NOT NULL,\n    head_doctor_id   INT\n);\n\nCREATE TABLE Doctor (\n    doctor_id     INT            PRIMARY KEY,\n    dname         VARCHAR(50)    NOT NULL,\n    specialization VARCHAR(40)   NOT NULL,\n    salary        DECIMAL(10,2)  NOT NULL,\n    dept_id       INT            NOT NULL,\n    join_date     DATE           NOT NULL DEFAULT (CURRENT_DATE),\n    CONSTRAINT fk_doc_dept\n        FOREIGN KEY (dept_id) REFERENCES Department(dept_id)\n        ON DELETE RESTRICT ON UPDATE CASCADE,\n    CONSTRAINT chk_doc_salary\n        CHECK (salary > 0)\n);\n\n-- Now add the head_doctor FK back to Department (self-reference)\nALTER TABLE Department\n    ADD CONSTRAINT fk_dept_head\n    FOREIGN KEY (head_doctor_id) REFERENCES Doctor(doctor_id)\n    ON DELETE SET NULL ON UPDATE CASCADE;\n\n-- Add UNIQUE constraint to enforce \"one doctor heads at most one department\"\nALTER TABLE Department\n    ADD CONSTRAINT uq_dept_head UNIQUE (head_doctor_id);\n\nCREATE TABLE Patient (\n    patient_id     INT          PRIMARY KEY,\n    pname          VARCHAR(60)  NOT NULL,\n    age            INT          NOT NULL,\n    gender         CHAR(1)      NOT NULL,\n    admission_date DATE         NOT NULL DEFAULT (CURRENT_DATE),\n    disease        VARCHAR(100) NOT NULL,\n    dept_id        INT          NOT NULL,\n    CONSTRAINT fk_pat_dept\n        FOREIGN KEY (dept_id) REFERENCES Department(dept_id)\n        ON DELETE RESTRICT ON UPDATE CASCADE,\n    CONSTRAINT chk_pat_age\n        CHECK (age BETWEEN 0 AND 130),\n    CONSTRAINT chk_pat_gender\n        CHECK (gender IN ('M','F','O'))\n);\n\nCREATE TABLE Room (\n    room_no         INT            PRIMARY KEY,\n    room_type       VARCHAR(20)    NOT NULL,\n    charges_per_day DECIMAL(8,2)   NOT NULL,\n    status          VARCHAR(15)    NOT NULL DEFAULT 'Available',\n    CONSTRAINT chk_room_charges\n        CHECK (charges_per_day > 0),\n    CONSTRAINT chk_room_status\n        CHECK (status IN ('Available','Occupied','Maintenance'))\n);\n\nCREATE TABLE Admission (\n    admission_id    INT            PRIMARY KEY,\n    patient_id      INT            NOT NULL,\n    room_no         INT            NOT NULL,\n    admit_date      DATE           NOT NULL DEFAULT (CURRENT_DATE),\n    discharge_date  DATE           DEFAULT NULL,\n    total_bill      DECIMAL(10,2)  DEFAULT NULL,\n    CONSTRAINT fk_adm_pat\n        FOREIGN KEY (patient_id) REFERENCES Patient(patient_id)\n        ON DELETE CASCADE ON UPDATE CASCADE,\n    CONSTRAINT fk_adm_room\n        FOREIGN KEY (room_no) REFERENCES Room(room_no)\n        ON DELETE RESTRICT ON UPDATE CASCADE,\n    CONSTRAINT chk_adm_bill\n        CHECK (total_bill IS NULL OR total_bill >= 0)\n);\n```\n\n### (b) — 7 Marks: ALTER, DROP, INDEX\n\n```sql\n-- Indices\nCREATE INDEX idx_doc_specialization ON Doctor(specialization);\nCREATE INDEX idx_pat_disease       ON Patient(disease);\nCREATE INDEX idx_adm_admit_date    ON Admission(admit_date);\n\n-- ALTER: add 'phone' to Patient\nALTER TABLE Patient ADD phone VARCHAR(15);\n\n-- ALTER: enforce NOT NULL on disease\nALTER TABLE Patient MODIFY disease VARCHAR(100) NOT NULL;\n\n-- ALTER: rename Department.location to building\nALTER TABLE Department RENAME COLUMN location TO building;\n\n-- DROP index\nDROP INDEX idx_doc_specialization ON Doctor;\n\n-- DROP column\nALTER TABLE Patient DROP COLUMN phone;\n\n-- DROP table\nDROP TABLE IF EXISTS Admission;\n```\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfalls — Hospital Question:**\n> 1. **Circular FK problem:** `Department.head_doctor_id → Doctor` and `Doctor.dept_id → Department` form a cycle. You **must** create one of them first without the FK, then `ALTER TABLE` to add it.\n> 2. **Forgetting the `UNIQUE` constraint on `head_doctor_id`** — without it, one doctor could head multiple departments, violating the business rule.\n> 3. **`ON DELETE CASCADE` on Admission** — fine, because admission depends on the patient. But **`CASCADE` on Room** would erase all admission history if a room is deleted — use `RESTRICT`.\n> 4. **Confusing `CHAR(1)` for gender with `VARCHAR(6)`** — gender is a single code letter; using `VARCHAR` wastes storage and slows joins.\n> 5. **Discharge date before admit date** — no `CHECK` constraint was added. In a real system, use a `BEFORE INSERT` trigger or application-level validation.\n\n## 5.3 Topic Recap & Important Things to Remember (Rapid Revision Checklist)\n\n> [!IMPORTANT]\n> **🎯 High-Yield Revision Points — Module 2 (DDL & Schema Creation)**\n\n- ✅ **DDL is auto-committed** — always wrap critical DDL inside a transaction (where supported) or backup the schema first.\n- ✅ **Use `IF EXISTS` / `IF NOT EXISTS`** to make scripts re-runnable.\n- ✅ **Parent tables must be created before child tables**; drop child tables first or use `CASCADE CONSTRAINTS` (Oracle).\n- ✅ **FK column datatype and width must EXACTLY match** the referenced PK column.\n- ✅ **Composite primary keys** are used in junction tables to resolve M:N relationships.\n- ✅ **`CHECK` constraints** are enforced in MySQL 8.0.16+; use triggers in older versions.\n- ✅ **`ON DELETE CASCADE`** is appropriate for **dependent child** rows; **`RESTRICT`** is appropriate for **organizational parents**.\n- ✅ **Index** only on frequently searched / joined columns; do not index small lookup tables or high-write tables.\n- ✅ **`DESC table_name;`** (MySQL) or **`DESCRIBE table_name;`** (Oracle) verifies schema.\n- ✅ **Reverse-Engineer the ER diagram** using MySQL Workbench → *Database → Reverse Engineer*; visually verify that every FK line exists and the cardinality matches the problem statement.\n- ✅ **Naming convention:** Use `fk_<child>_<parent>` for foreign keys and `chk_<table>_<column>` for checks — improves viva score.\n- ✅ **Lab Record Tip:** Always include the `SHOW CREATE TABLE <name>` output in your record — it is the single most KTU-expected verification artifact.\n- ✅ **Common MySQL errors to recognize:**\n    - `ERROR 1062` → duplicate key (PK/UNIQUE violation).\n    - `ERROR 1215` → FK column mismatch.\n    - `ERROR 1451` → cannot delete/update parent row (RESTRICT violation).\n    - `ERROR 1452` → cannot add child row (FK reference missing).\n- ✅ **For the viva:** Be ready to explain *why* you chose `CASCADE` vs `RESTRICT` for each FK, and *why* you indexed (or did not index) a specific column.\n\n---\n\n> **End of Section 5 — KTU 2024 Scheme Examination Question Bank**\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pccsl408-mExperiment 2-t4",
          "title": "Practice SQL commands for DML (insertion, updating, altering, deletion of data, and viewing/querying records based on condition in databases)",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL408 - DBMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Practice SQL commands for DML (insertion, updating, altering, deletion of data, and viewing/querying records based on condition in databases)\n\n<!-- SECTION_1_START -->\n# Section 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition (KTU 2024 Syllabus Terminology)\n\n**Data Manipulation Language (DML)** is a subset of the Structured Query Language (SQL) used in Relational Database Management Systems (RDBMS) to **retrieve, insert, modify, and delete data** stored within the existing schema objects (tables, views) of a database. Unlike Data Definition Language (DDL), which alters the *structure* of database objects, DML operates strictly on the *data content* (rows/tuples) of those objects.\n\nThe four canonical DML operations are collectively referred to as **CRUD**:\n- **C**reate → `INSERT`\n- **R**ead → `SELECT`\n- **U**pdate → `UPDATE`\n- **D**elete → `DELETE`\n\n> [!NOTE]\n> **KTU 2024 Syllabus Highlight (PCCSL408 – Module 2):**\n> The experiment mandates hands-on practice of `INSERT`, `UPDATE`, `ALTER` (table modification to support new data), `DELETE`, and conditional `SELECT` queries. While `ALTER` is technically a DDL command, it is grouped under \"data manipulation practices\" in the KTU lab manual because it is required to *accommodate* new data during experimentation.\n\n> [!IMPORTANT]\n> **Core Definitional Distinction (Frequently Tested):**\n> - **DML (Data Manipulation Language):** Manipulates *data* inside tables. Commands: `SELECT`, `INSERT`, `UPDATE`, `DELETE`.\n> - **DDL (Data Definition Language):** Manipulates *structure* of tables. Commands: `CREATE`, `ALTER`, `DROP`, `TRUNCATE`.\n> - **DCL (Data Control Language):** Manages *permissions*. Commands: `GRANT`, `REVOKE`.\n> - **TCL (Transaction Control Language):** Manages *transactions*. Commands: `COMMIT`, `ROLLBACK`, `SAVEPOINT`.\n\n## 1.2 Conceptual Analogy — The \"Office Filing Cabinet\" Model\n\nImagine a **library's card catalog system**:\n\n| Database Concept | Real-World Analogy |\n|---|---|\n| **Table** | A single filing drawer labelled \"Student Records\" |\n| **Row/Tuple** | One individual student's complete file card |\n| **Column/Attribute** | A specific field on the card (Name, Roll No, Marks) |\n| **INSERT** | Filing a brand new student's card into the drawer |\n| **SELECT** | Searching and reading existing cards without moving them |\n| **UPDATE** | Erasing an old phone number on a card and writing the new one |\n| **DELETE** | Removing a card permanently from the drawer |\n| **ALTER** | Adding a new column \"Email\" to every card in the drawer (a structural upgrade) |\n| **WHERE clause** | The search filter — e.g., \"only show cards where Marks > 80\" |\n\n> [!TIP]\n> **Intuitive Takeaway:** DML commands never change the *shape* of the drawer (number of compartments), only the *cards inside it*. The exception is `ALTER`, which KTU groups here because in lab practice you often need to expand the drawer to store new kinds of information.\n\n## 1.3 Key Terminology Inventory (Board-Exam Vocabulary)\n\n- **Tuple / Record / Row:** A single horizontal entry in a table.\n- **Attribute / Field / Column:** A vertical named entity in a table schema.\n- **Predicate (WHERE clause condition):** A Boolean expression that evaluates to TRUE, FALSE, or UNKNOWN for each row.\n- **ACID Properties:** Atomicity, Consistency, Isolation, Durability — governing DML execution under transactions.\n- **Cardinality:** The number of rows in a relation (modified by INSERT/DELETE).\n- **Arity / Degree:** The number of columns in a relation (modified by ALTER).\n- **Commit:** Permanent saving of DML changes to the database.\n- **Rollback:** Undoing uncommitted DML changes.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# Section 2: Deep Theoretical Analysis & KTU High-Yield Syntax Sheet\n\n## 2.1 The DML Command Architecture\n\nSQL DML operations follow a strict logical pipeline. Every `UPDATE` and `DELETE` statement **must** include a `WHERE` clause in practice, otherwise the operation affects *every row* in the table — a classic board-exam pitfall.\n\n### 2.1.1 Operational Logic Flow of a DML Statement\n\n1. **Parse** → SQL engine checks syntax and semantic validity.\n2. **Optimize** → Query Optimizer generates the most efficient execution plan.\n3. **Execute** → Rows are located, locked, modified, and written to the **buffer cache** (not yet on disk).\n4. **Commit / Rollback** → The transaction is finalized or aborted.\n\n## 2.2 The INSERT Command — Theory of Operation\n\n`INSERT` appends new tuples to a relation. There are two syntactic variants:\n\n**Variant A — Column-list specified (RECOMMENDED, board-safe):**\n```sql\nINSERT INTO table_name (col1, col2, col3)\nVALUES (val1, val2, val3);\n```\n\n**Variant B — Positional (all columns, in declaration order):**\n```sql\nINSERT INTO table_name VALUES (val1, val2, val3);\n```\n\n**Variant C — Multi-row bulk insert (efficient, KTU favorite):**\n```sql\nINSERT INTO table_name (col1, col2) VALUES\n  (val_a, val_b),\n  (val_c, val_d),\n  (val_e, val_f);\n```\n\n**Variant D — Insert from a SELECT (subquery-based):**\n```sql\nINSERT INTO target_table (col1, col2)\nSELECT src_col1, src_col2\nFROM source_table\nWHERE condition;\n```\n\n> [!IMPORTANT]\n> **Constraint Enforcement Rule:** If a column has a `NOT NULL` constraint and no `DEFAULT` value, omitting it from the column list will cause the INSERT to fail with an integrity constraint violation. This is a common KTU viva question.\n\n## 2.3 The UPDATE Command — Theory of Operation\n\n`UPDATE` modifies values of existing rows. Syntax skeleton:\n```sql\nUPDATE table_name\nSET col1 = new_value1 [, col2 = new_value2, ...]\n[WHERE condition];\n```\n\n**Evaluation Order:**\n1. The DBMS scans the table (or uses an index).\n2. The `WHERE` clause is evaluated **row by row** to identify candidate tuples.\n3. Matching rows are locked (in transactional systems).\n4. New values replace old values in the in-memory row.\n5. The change is logged in the **transaction log** for redo/undo purposes.\n\n> [!WARNING]\n> **The \"Forgot the WHERE\" Catastrophe:**\n> Running `UPDATE Student SET Marks = 100;` (no WHERE) will assign 100 to *every* student. In Oracle/MySQL, the command executes unless a `SAFE_UPDATES` mode is active. KTU examiners specifically test this trap.\n\n## 2.4 The DELETE Command — Theory of Operation\n\n```sql\nDELETE FROM table_name\n[WHERE condition];\n```\n\n**Critical Distinction:**\n\n| Command | Effect | Rollback Possible? | Resets Identity? |\n|---|---|---|---|\n| `DELETE FROM table WHERE ...` | Removes selected rows | Yes (within uncommitted txn) | No |\n| `DELETE FROM table;` (no WHERE) | Removes all rows | Yes | No |\n| `TRUNCATE TABLE table;` | Removes all rows | **No** (implicit commit) | **Yes** |\n| `DROP TABLE table;` | Removes table + data + structure | No | Yes |\n\n> [!NOTE]\n> **Board Tip:** The difference between `DELETE`, `TRUNCATE`, and `DROP` is one of the most frequently asked 3-mark questions in KTU ESE papers.\n\n## 2.5 The SELECT Command — Conditional Querying\n\nThe `SELECT` statement is the \"Read\" half of CRUD. Basic syntax:\n```sql\nSELECT [DISTINCT] col1, col2, aggregate(col3)\nFROM table_name\n[WHERE row_condition]\n[GROUP BY col]\n[HAVING group_condition]\n[ORDER BY col [ASC | DESC]];\n```\n\n**Key Clauses — Evaluation Order (interview + viva favorite):**\n1. `FROM` → Identify source tables.\n2. `WHERE` → Filter individual rows.\n3. `GROUP BY` → Form groups.\n4. `HAVING` → Filter groups.\n5. `SELECT` → Choose columns / compute aggregates.\n6. `DISTINCT` → Remove duplicates.\n7. `ORDER BY` → Sort final result.\n\n> **Mnemonic:** ***\"From Wisconsin, George Has Sorted Data\"*** — **F**rom, **W**here, **G**roup by, **H**aving, **S**elect, **D**istinct, **O**rder by.\n\n## 2.6 The ALTER Command — Structural Modification (Lab Context)\n\nAlthough classified as DDL, KTU's Module 2 includes it because lab experiments often require schema evolution to support richer data:\n\n```sql\n-- Add a new column\nALTER TABLE Student ADD Email VARCHAR(50);\n\n-- Modify data type / size\nALTER TABLE Student MODIFY Phone VARCHAR(15);\n\n-- Drop a column\nALTER TABLE Student DROP COLUMN Email;\n\n-- Rename a column (MySQL 8+ / Oracle 10g+)\nALTER TABLE Student RENAME COLUMN Phone TO Mobile;\n```\n\n## 2.7 KTU High-Yield Syntax Cheat Sheet (Board-Exam Quick Reference)\n\n> [!IMPORTANT]\n> The following table summarizes the precise syntax skeletons that must be reproduced verbatim in KTU lab records and theory exams. **Memorize the brackets [ ] as \"optional\" and the pipes { } as \"choose one\".**\n\n| Command | Syntax Skeleton | Affects | Reversible? |\n|---|---|---|---|\n| `INSERT` | `INSERT INTO T [(c1,c2)] VALUES (v1,v2) [, (v3,v4)]` | Row count ↑ | Yes (rollback) |\n| `UPDATE` | `UPDATE T SET c1=v1 [WHERE cond]` | Column values of rows | Yes (rollback) |\n| `DELETE` | `DELETE FROM T [WHERE cond]` | Row count ↓ | Yes (rollback) |\n| `SELECT` | `SELECT cols FROM T [WHERE cond] [ORDER BY c]` | None (read-only) | N/A |\n| `ALTER` | `ALTER TABLE T ADD / MODIFY / DROP COLUMN c` | Schema structure | Yes (rollback in some DBs) |\n\n## 2.8 Real-World Engineering Utility\n\nDML commands form the operational backbone of virtually every enterprise system:\n\n- **Banking Systems:** `UPDATE Account SET Balance = Balance - 5000 WHERE AccNo = 101;` — atomic money transfers.\n- **E-Commerce:** `INSERT INTO Orders (...) VALUES (...);` — recording customer purchases.\n- **Healthcare (Aarogya Setu / HMIS):** `SELECT * FROM Patients WHERE District = 'Ernakulam' AND Age > 60;` — disease surveillance queries.\n- **University ERP (KTU's own):** `UPDATE StudentResult SET GPA = 8.7 WHERE RollNo = 'TVE21CS001';`\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# Section 3: Step-by-Step Analytical Walkthroughs & Query Construction\n\n> [!NOTE]\n> Since this is a **DBMS Lab** topic, \"derivations\" are reframed as **exhaustive query construction walkthroughs** showing every logical and syntactic transition.\n\n## 3.1 Reference Schema (Used in All Walkthroughs)\n\nTo make the experiment reproducible, assume the following KTU-standard lab schema:\n\n```sql\nCREATE TABLE Student (\n    RollNo   VARCHAR(10)  PRIMARY KEY,\n    Name     VARCHAR(50)  NOT NULL,\n    Dept     VARCHAR(20),\n    Age      INT,\n    Marks    INT,\n    Grade    CHAR(2)\n);\n```\n\n## 3.2 Walkthrough 1 — Constructing an INSERT Statement\n\n**Problem:** Insert a record: `('S001', 'Anand Krishna', 'CSE', 20, 85, 'A')`.\n\n**Step-by-step reasoning:**\n\n**Step 1** — Identify the target table and required columns.\nThe statement must operate on the `Student` table. Since we have data for every column, we can either:\n- Use the short form (positional, all 6 values in order), OR\n- Use the long form (explicit column list — **safer for exams**).\n\n**Step 2** — Match value count to column count.\nSix columns → six values. Mismatch raises a column-count error in MySQL/Oracle.\n\n**Step 3** — Wrap string values in single quotes; numerics need no quotes.\n\n**Step 4** — Final SQL:\n```sql\nINSERT INTO Student (RollNo, Name, Dept, Age, Marks, Grade)\nVALUES ('S001', 'Anand Krishna', 'CSE', 20, 85, 'A');\n```\n\n**Step 5** — Verify outcome: `SELECT * FROM Student;` should display 1 row.\n\n**Variant — Bulk insert (KTU bonus credit):**\n```sql\nINSERT INTO Student (RollNo, Name, Dept, Age, Marks, Grade) VALUES\n('S002', 'Divya R',   'CSE', 21, 92, 'A'),\n('S003', 'Rahul M',   'ECE', 22, 78, 'B'),\n('S004', 'Sneha P',   'CSE', 20, 65, 'C'),\n('S005', 'Vivek N',   'MECH',23, 55, 'D'),\n('S006', 'Anjali T',  'CSE', 21, 88, 'A');\n```\n\n## 3.3 Walkthrough 2 — Constructing a Conditional UPDATE\n\n**Problem:** Increase the `Marks` of all `CSE` students by 5 (grade boost).\n\n**Step 1** — Identify target table → `Student`.\n**Step 2** — Identify the modification → `Marks` column, new value = `Marks + 5` (use existing column in expression).\n**Step 3** — Identify filter condition → `Dept = 'CSE'`.\n**Step 4** — Construct the SQL:\n```sql\nUPDATE Student\nSET Marks = Marks + 5\nWHERE Dept = 'CSE';\n```\n\n**Step 5 — Logical verification (pre/post state):**\n\n$$\n\\text{Rows Affected} = \\vert \\text{\\{rows where Dept = 'CSE'\\}} \\vert\n$$\n\n| RollNo | Name | Dept | Marks (Before) | Marks (After) |\n|---|---|---|---|---|\n| S001 | Anand Krishna | CSE | 85 | **90** |\n| S002 | Divya R | CSE | 92 | **97** |\n| S003 | Rahul M | ECE | 78 | 78 (unchanged) |\n| S004 | Sneha P | CSE | 65 | **70** |\n| S005 | Vivek N | MECH | 55 | 55 (unchanged) |\n| S006 | Anjali T | CSE | 88 | **93** |\n\n> [!IMPORTANT]\n> **Cardinality Preservation Property:** An `UPDATE` statement **never changes the number of rows** in a table. Only `INSERT` (adds) and `DELETE` (removes) alter cardinality. This is a board-exam short-answer favorite.\n\n## 3.4 Walkthrough 3 — Constructing a DELETE Statement\n\n**Problem:** Remove all students who scored below the passing threshold (Marks < 40).\n\n**Step 1** — Target: `Student` table.\n**Step 2** — Filter: `Marks < 40`.\n**Step 3** — Construct:\n```sql\nDELETE FROM Student\nWHERE Marks < 40;\n```\n\n**Step 4** — Cardinality change:\n$$\n\\text{Cardinality}_{\\text{after}} = \\text{Cardinality}_{\\text{before}} - \\vert \\text{\\{rows where Marks} < 40\\} \\vert\n$$\n\n> [!TIP]\n> **Recovery Pattern (Examiners love this):** Before any `DELETE` in a real lab, execute `SELECT * FROM Student WHERE Marks < 40;` first to *preview* the victims. This is a professional habit KTU evaluators appreciate.\n\n## 3.5 Walkthrough 4 — Constructing a Conditional SELECT\n\n**Problem:** Display the RollNo, Name, and Marks of CSE students whose marks are between 70 and 90, sorted in descending order of marks.\n\n**Step 1** — Required output columns → `RollNo, Name, Marks`.\n**Step 2** — Source table → `Student`.\n**Step 3** — Row filter → `Dept = 'CSE' AND Marks BETWEEN 70 AND 90`.\n**Step 4** — Sort order → `Marks DESC`.\n**Step 5** — Compose:\n```sql\nSELECT RollNo, Name, Marks\nFROM Student\nWHERE Dept = 'CSE' AND Marks BETWEEN 70 AND 90\nORDER BY Marks DESC;\n```\n\n**Step 6 — Logical operator precedence in WHERE clause (critical for board exams):**\n\nIn SQL, Boolean operators are evaluated in this priority:\n\n$$\n\\text{NOT} \\;>\\ \\; \\text{AND} \\;>\\ \\; \\text{OR}\n$$\n\n**Rule of thumb:**\n$$\n\\text{Evaluate NOT first, then AND, finally OR.}\n$$\n\n**Example trap:**\n```sql\n-- This query WITHOUT parentheses:\nSELECT * FROM Student WHERE Dept = 'CSE' OR Dept = 'ECE' AND Marks > 80;\n-- Is interpreted as:\n--   Dept = 'CSE'  OR  (Dept = 'ECE' AND Marks > 80)\n-- NOT as:\n--   (Dept = 'CSE' OR Dept = 'ECE') AND Marks > 80\n```\n\n> [!WARNING]\n> **Always use parentheses** to make Boolean logic explicit. The KTU valuation key explicitly deducts marks for ambiguous predicates.\n\n## 3.6 Walkthrough 5 — Multi-row UPDATE with Subquery (Advanced)\n\n**Problem:** Promote all students with Marks > 90 to Grade 'A+'.\n\n```sql\nUPDATE Student\nSET Grade = 'A+'\nWHERE Marks > 90;\n```\n\n**Verification approach:** Always pair DML with a SELECT in the lab record:\n```sql\n-- PRE-CHECK\nSELECT RollNo, Name, Marks, Grade FROM Student WHERE Marks > 90;\n-- EXECUTE UPDATE\nUPDATE Student SET Grade = 'A+' WHERE Marks > 90;\n-- POST-CHECK\nSELECT RollNo, Name, Marks, Grade FROM Student WHERE Marks > 90;\n```\n\n## 3.7 Walkthrough 6 — ALTER + INSERT Combined Workflow\n\nA typical KTU lab cycle uses `ALTER` to expand the schema, then `INSERT` to populate the new field.\n\n```sql\n-- Step 1: Add a new column to accommodate email\nALTER TABLE Student ADD Email VARCHAR(50);\n\n-- Step 2: Populate the new column for an existing student\nUPDATE Student SET Email = 'anand@kerala.ac.in' WHERE RollNo = 'S001';\n\n-- Step 3: Insert a brand new student WITH the email value\nINSERT INTO Student (RollNo, Name, Dept, Age, Marks, Grade, Email)\nVALUES ('S007', 'Lakshmi B', 'CSE', 20, 91, 'A+', 'lakshmi@kerala.ac.in');\n```\n\n## 3.8 Analytical Matrix — Operator Reference Table\n\n| Operator | SQL Symbol | Example | Meaning |\n|---|---|---|---|\n| Equality | `=` | `WHERE Dept = 'CSE'` | Exact match |\n| Inequality | `<>` or `!=` | `WHERE Dept <> 'CSE'` | Not equal |\n| Range | `BETWEEN x AND y` | `WHERE Marks BETWEEN 70 AND 90` | Inclusive range |\n| Set membership | `IN (a,b,c)` | `WHERE Dept IN ('CSE','ECE')` | Value in list |\n| Pattern | `LIKE 'pattern'` | `WHERE Name LIKE 'A%'` | String match (% = any, _ = one) |\n| Null test | `IS NULL / IS NOT NULL` | `WHERE Email IS NULL` | Missing value |\n| Logical AND | `AND` | `A AND B` | Both must be TRUE |\n| Logical OR | `OR` | `A OR B` | At least one TRUE |\n| Logical NOT | `NOT` | `NOT (Marks > 80)` | Inversion |\n\n> [!IMPORTANT]\n> **NULL vs Zero vs Empty String:**\n> - `NULL` is the *absence of a value* — it is **not equal to 0**, **not equal to ''** (empty string), and **not equal to another NULL**.\n> - Use `IS NULL` and `IS NOT NULL` — never `= NULL` (this is a KTU classic error).\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# Section 4: Structural Diagrams & Schematics\n\n## 4.1 DML Command Classification Tree\n\n```mermaid\ngraph TD\n    A[SQL SUBSYSTEM] --> B[DML]\n    A --> C[DDL]\n    A --> D[DCL]\n    A --> E[TCL]\n    B --> F[INSERT]\n    B --> G[SELECT]\n    B --> H[UPDATE]\n    B --> I[DELETE]\n    F --> F1[Single Row]\n    F --> F2[Multi Row Bulk]\n    F --> F3[From Subquery SELECT]\n    G --> G1[All Columns Asterisk]\n    G --> G2[Specific Columns]\n    G --> G3[With WHERE Filter]\n    G --> G4[With ORDER BY Sort]\n    G --> G5[With GROUP BY Aggregation]\n    H --> H1[Single Column]\n    H --> H2[Multiple Columns]\n    H --> H3[With Subquery]\n    I --> I1[Conditional WHERE]\n    I --> I2[All Rows no WHERE]\n    C --> J[ALTER]\n    J --> J1[ADD COLUMN]\n    J --> J2[MODIFY COLUMN]\n    J --> J3[DROP COLUMN]\n    J --> J4[RENAME COLUMN]\n```\n\n## 4.2 DML Statement Execution Pipeline (Block Diagram)\n\n```mermaid\nflowchart LR\n    A[User Issues SQL Statement] --> B[Parser]\n    B --> C{Syntax Valid?}\n    C -->|No| D[Return Error]\n    C -->|Yes| E[Query Optimizer]\n    E --> F[Execution Engine]\n    F --> G[Buffer Cache]\n    G --> H{Transaction Active?}\n    H -->|Yes| I[Hold in Transaction Log]\n    H -->|No| J[Direct Write to Disk]\n    I --> K{COMMIT Issued?}\n    K -->|Yes| J\n    K -->|No| L[ROLLBACK Possible]\n    J --> M[Persistent Storage]\n    L --> N[Original State Restored]\n```\n\n## 4.3 SELECT Statement Clause Evaluation Order\n\n```mermaid\nflowchart TD\n    A[Start] --> B[FROM: Load Source Tables]\n    B --> C[WHERE: Filter Individual Rows]\n    C --> D[GROUP BY: Form Groups]\n    D --> E[HAVING: Filter Groups]\n    E --> F[SELECT: Choose Columns]\n    F --> G[DISTINCT: Remove Duplicates]\n    G --> H[ORDER BY: Sort Final Output]\n    H --> I[LIMIT / OFFSET: Restrict Rows]\n    I --> J[Return Result Set to User]\n```\n\n## 4.4 DELETE vs TRUNCATE vs DROP — Decision Flow\n\n```mermaid\nflowchart TD\n    A[Need to Remove Data?] --> B{Do you need to remove the table structure?}\n    B -->|Yes| C[DROP TABLE]\n    B -->|No| D{Do you need to remove ALL rows with minimal logging?}\n    D -->|Yes| E[TRUNCATE TABLE]\n    D -->|No| F[DELETE FROM table WHERE condition]\n    C --> G[Table Gone, Cannot Rollback]\n    E --> H[All Rows Removed, Identity Reset, Cannot Rollback]\n    F --> I[Only Matching Rows Removed, Rollback Possible]\n```\n\n## 4.5 Lab Experiment 2 — Complete Workflow Block Diagram\n\n```mermaid\nflowchart TD\n    A[Start Experiment 2] --> B[Step 1: CREATE TABLE Schema]\n    B --> C[Step 2: ALTER TABLE Add Columns if Needed]\n    C --> D[Step 3: INSERT Initial Records]\n    D --> E[Step 4: SELECT All Records to Verify]\n    E --> F[Step 5: UPDATE Specific Records Using WHERE]\n    F --> G[Step 6: SELECT to Verify Update]\n    G --> H[Step 7: DELETE Specific Records Using WHERE]\n    H --> I[Step 8: SELECT to Verify Deletion]\n    I --> J[Step 9: Conditional SELECT with WHERE ORDER BY LIMIT]\n    J --> K[Step 10: Aggregate SELECT with GROUP BY HAVING]\n    K --> L[Step 11: Document Outputs as Lab Record Evidence]\n    L --> M[End]\n```\n\n## 4.6 INSERT Subquery Pattern (Data Movement Architecture)\n\n```mermaid\nflowchart LR\n    A[Source Table] --> B[SELECT Statement with WHERE]\n    B --> C[Result Set Forms Insert Payload]\n    C --> D[INSERT INTO Target Table]\n    D --> E[Target Table Updated]\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# Section 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n> [!IMPORTANT]\n> **KTU 2024 Assessment Pattern Reference:**\n> - **Part A (3 marks):** Short answer / definition / small SQL snippet.\n> - **Part B (14 marks):** Full lab-cycle question with internal choice; typically asks to (a) write 5–6 SQL statements [7 marks] + (b) explain output / write more queries / discuss error scenarios [7 marks].\n\n---\n\n## Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. [KTU University Exam – May 2024]\n**Differentiate between DDL and DML commands in SQL. Give two examples of each.**\n\n**Model Answer (Verbatim expected phrasing):**\n\n> **DDL (Data Definition Language)** is used to define, alter, or drop the *structure* of database objects such as tables, schemas, and indexes. DDL statements are **auto-committed** in most RDBMS, meaning changes cannot be rolled back. Examples: `CREATE TABLE`, `ALTER TABLE`, `DROP TABLE`.\n>\n> **DML (Data Manipulation Language)** is used to *manipulate the data* stored inside the existing schema objects. DML statements operate on rows/records and are **transactional** — they can be rolled back before commit. Examples: `INSERT`, `UPDATE`, `DELETE`, `SELECT`.\n>\n> **Key Distinction:** DDL changes the *schema*; DML changes the *data* within an unchanged schema.\n\n### Q2. [KTU University Exam – Dec 2023]\n**What is the difference between `DELETE`, `TRUNCATE`, and `DROP` commands in SQL?**\n\n**Model Answer:**\n\n| Aspect | DELETE | TRUNCATE | DROP |\n|---|---|---|---|\n| Type | DML | DDL | DDL |\n| Removes | Selected rows (or all) | All rows | Entire table + data + structure |\n| WHERE clause | Allowed | Not allowed | Not applicable |\n| Rollback | Yes (within uncommitted txn) | No (implicit commit) | No (implicit commit) |\n| Resets auto-increment | No | Yes | Yes (table gone) |\n| Speed | Slowest (row-by-row, logs each) | Faster (deallocates pages) | Fastest |\n| Syntax | `DELETE FROM T WHERE ...` | `TRUNCATE TABLE T` | `DROP TABLE T` |\n\n---\n\n## Part B — Long Answer Questions (14 Marks Each — Internal Choice)\n\n### QUESTION A — [KTU Model ESE Question — Module 2 Pattern]\n\n**(a) [7 Marks]** Consider the following table schema for a college database:\n```\nEmployee(EmpID INT PK, Name VARCHAR(40), Department VARCHAR(20), Salary INT, JoiningDate DATE)\n```\nWrite SQL DML statements for the following:\n1. Insert three employee records using a single INSERT statement.\n2. Increase the salary of all employees in the 'CSE' department by 10%.\n3. Delete the record of the employee whose EmpID is 105.\n4. Display the Name and Salary of employees earning more than 50,000, sorted by Salary descending.\n5. Display the count of employees in each department.\n\n**(b) [7 Marks]** What will be the output of query (4) and (5) if the table currently contains the data shown below? Explain each query step-by-step.\n\n| EmpID | Name | Department | Salary | JoiningDate |\n|---|---|---|---|---|\n| 101 | Anand | CSE | 55000 | 2021-06-12 |\n| 102 | Divya | ECE | 48000 | 2020-03-05 |\n| 103 | Rahul | CSE | 72000 | 2019-07-22 |\n| 104 | Sneha | MECH | 39000 | 2022-01-15 |\n| 105 | Vivek | CSE | 30000 | 2023-08-01 |\n\n#### Model Solution for (a):\n\n**1. Insert three employee records using a single INSERT statement:**\n```sql\nINSERT INTO Employee (EmpID, Name, Department, Salary, JoiningDate) VALUES\n(101, 'Anand',  'CSE',  55000, '2021-06-12'),\n(102, 'Divya',  'ECE',  48000, '2020-03-05'),\n(103, 'Rahul',  'CSE',  72000, '2019-07-22');\n```\n\n**2. Increase salary of all CSE employees by 10%:**\n```sql\nUPDATE Employee\nSET Salary = Salary * 1.10\nWHERE Department = 'CSE';\n```\n*Logic: `Salary * 1.10` = `Salary + (Salary * 0.10)` = original salary plus 10% increment.*\n\n**3. Delete the record where EmpID = 105:**\n```sql\nDELETE FROM Employee\nWHERE EmpID = 105;\n```\n\n**4. Display Name and Salary of employees with Salary > 50000, sorted by Salary DESC:**\n```sql\nSELECT Name, Salary\nFROM Employee\nWHERE Salary > 50000\nORDER BY Salary DESC;\n```\n\n**5. Display the count of employees in each department:**\n```sql\nSELECT Department, COUNT(*) AS EmployeeCount\nFROM Employee\nGROUP BY Department;\n```\n\n#### Model Solution for (b):\n\n**Query 4 Output:**\n\nStep 1: `FROM Employee` → load all 5 rows.\nStep 2: `WHERE Salary > 50000` → keep rows with Salary 55000 (Anand), 72000 (Rahul); reject 48000, 39000, 30000.\nStep 3: `SELECT Name, Salary` → output these two columns.\nStep 4: `ORDER BY Salary DESC` → sort by salary in descending order.\n\n| Name | Salary |\n|---|---|\n| Rahul | 72000 |\n| Anand | 55000 |\n\n**Query 5 Output:**\n\nStep 1: `FROM Employee` → all 5 rows.\nStep 2: `GROUP BY Department` → partition rows into {CSE: Anand, Rahul, Vivek} and {ECE: Divya} and {MECH: Sneha}.\nStep 3: `COUNT(*)` per group → 3, 1, 1.\nStep 4: `SELECT Department, COUNT(*)` → output the group identifier and count.\n\n| Department | EmployeeCount |\n|---|---|\n| CSE | 3 |\n| ECE | 1 |\n| MECH | 1 |\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Where Students Lose Marks:**\n> 1. **Forgetting commas in multi-row INSERT** — each tuple must be comma-separated.\n> 2. **Using `=` instead of `IS` for NULL** — e.g., `WHERE Email = NULL` is *always* FALSE; use `IS NULL`.\n> 3. **Missing WHERE clause in UPDATE/DELETE** — instant 2-mark penalty even if the rest is correct.\n> 4. **Quoting strings** — `'CSE'` (with quotes), not `CSE` (treated as a column name).\n> 5. **Using `HAVING` where `WHERE` is required** — `HAVING` filters *groups*, `WHERE` filters *rows*.\n\n---\n\n### QUESTION B — [Alternative Choice — Same Marks Pattern]\n\n**(a) [7 Marks]** Consider the table:\n```\nProduct(PID INT PK, PName VARCHAR(30), Category VARCHAR(20), Price INT, Stock INT)\n```\nWrite SQL DML statements to:\n1. Add a new column `Supplier` of type VARCHAR(40) to the Product table.\n2. Insert four products with appropriate values for all columns.\n3. Update the Price of all products in the 'Electronics' category by reducing it by 15%.\n4. Delete all products whose Stock is 0.\n5. Display the PName and Price of the top 3 most expensive products.\n\n**(b) [7 Marks]** Explain the difference between the `WHERE` and `HAVING` clauses with a suitable example. Also, write a query to display, for each category, the average price of products that have an average price greater than 1000.\n\n#### Model Solution for (a):\n\n**1. Add Supplier column:**\n```sql\nALTER TABLE Product ADD Supplier VARCHAR(40);\n```\n\n**2. Insert four products:**\n```sql\nINSERT INTO Product (PID, PName, Category, Price, Stock, Supplier) VALUES\n(201, 'Laptop A',    'Electronics', 65000, 12, 'Dell India'),\n(202, 'Mouse B',     'Electronics',   450, 80, 'Logitech'),\n(203, 'Notebook C',  'Stationery',     60, 500, 'Classmate'),\n(204, 'Pen Drive D', 'Electronics',   550,  0, 'Sandisk');\n```\n\n**3. Reduce Electronics price by 15%:**\n```sql\nUPDATE Product\nSET Price = Price * 0.85\nWHERE Category = 'Electronics';\n```\n*Logic: `Price * 0.85` = 85% of original = 15% reduction.*\n\n**4. Delete products with Stock = 0:**\n```sql\nDELETE FROM Product\nWHERE Stock = 0;\n```\n\n**5. Top 3 most expensive products:**\n```sql\nSELECT PName, Price\nFROM Product\nORDER BY Price DESC\nLIMIT 3;\n```\n*(In Oracle, use `FETCH FIRST 3 ROWS ONLY`; in SQL Server, use `SELECT TOP 3 ...`.)*\n\n#### Model Solution for (b):\n\n**WHERE vs HAVING — Theoretical Distinction:**\n\n| Aspect | WHERE | HAVING |\n|---|---|---|\n| Filters | Individual rows | Groups (post-GROUP BY) |\n| Used with | Any DML/SELECT | Only with GROUP BY / aggregates |\n| Aggregate functions allowed? | **No** | **Yes** |\n| Execution order | Before GROUP BY | After GROUP BY |\n| Example | `WHERE Price > 500` | `HAVING AVG(Price) > 1000` |\n\n**Example query — Average price per category > 1000:**\n```sql\nSELECT Category, AVG(Price) AS AvgPrice\nFROM Product\nGROUP BY Category\nHAVING AVG(Price) > 1000;\n```\n\n**Step-by-step evaluation:**\n\nStep 1: `FROM Product` — load all rows.\nStep 2: `GROUP BY Category` — partition into groups {Electronics, Stationery}.\nStep 3: `AVG(Price)` per group — Electronics = (65000+450+550)/3 = 22000; Stationery = 60.\nStep 4: `HAVING AVG(Price) > 1000` — keep Electronics (22000 > 1000); discard Stationery (60 < 1000).\n\n**Output:**\n\n| Category | AvgPrice |\n|---|---|\n| Electronics | 22000 |\n\n> [!WARNING]\n> **KTU Examiner's Pitfall Callout — `ALTER TABLE` Mistakes:**\n> 1. **Wrong DBMS syntax:** MySQL uses `ADD`, Oracle uses `ADD` (same), but some older PostgreSQL requires `ADD COLUMN`. KTU lab uses MySQL/Oracle primarily — write portable syntax.\n> 2. **Forgetting `COLUMN` keyword:** `ALTER TABLE T DROP Email` may fail in strict mode. Always say `DROP COLUMN Email`.\n> 3. **Trying to ALTER a non-existent table** — common when the experiment is run in the wrong schema.\n\n---\n\n## Topic Recap & Important Things to Remember\n\n> [!TIP]\n> **High-Density Revision Checklist — Pin This Before the Exam:**\n\n- **DML = Data Manipulation Language** = works on data inside tables: `INSERT`, `UPDATE`, `DELETE`, `SELECT`.\n- **DDL = Data Definition Language** = works on table structure: `CREATE`, `ALTER`, `DROP`, `TRUNCATE`.\n- **CRUD mapping:** Create→`INSERT`, Read→`SELECT`, Update→`UPDATE`, Delete→`DELETE`.\n- **Always include a `WHERE` clause** in `UPDATE` and `DELETE` to avoid mass modification (KTU favorite trap).\n- **String literals use single quotes** `'CSE'`; numeric values do not need quotes.\n- **NULL is the absence of value** — test with `IS NULL` / `IS NOT NULL`, never `= NULL`.\n- **Boolean precedence in WHERE:** `NOT > AND > OR` — use parentheses for clarity.\n- **`LIKE` wildcards:** `%` = any sequence of characters; `_` = exactly one character.\n- **`BETWEEN` is inclusive** of both endpoints.\n- **`DISTINCT`** removes duplicate rows in SELECT output.\n- **`ORDER BY`** default is ASC; specify `DESC` for descending.\n- **Clause evaluation order in SELECT:** FROM → WHERE → GROUP BY → HAVING → SELECT → DISTINCT → ORDER BY.\n- **DELETE vs TRUNCATE vs DROP:** row-level / table-data / full-table including structure.\n- **Bulk INSERT** is faster than multiple single-row INSERTs.\n- **Multi-table UPDATE / DELETE** can use subqueries with `IN` or `EXISTS` (advanced KTU viva point).\n- **COMMIT makes DML changes permanent**; `ROLLBACK` undoes uncommitted changes.\n- **Lab record must always pair every DML with a verification SELECT** to earn full marks.\n- **Use uppercase for SQL keywords** in the lab record — improves readability and KTU valuation goodwill.\n\n---\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pccsl408-mExperiment 2-t5",
          "title": "Implementation of various aggregate functions, Order By, Group By & Having clause in SQL",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL408 - DBMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Implementation of various aggregate functions, Order By, Group By & Having clause in SQL\n\n<!-- SECTION_1_START -->\n\n# 📘 MODULE EXPERIMENT 2 — AGGREGATE FUNCTIONS, ORDER BY, GROUP BY & HAVING IN SQL\n\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Academic Definition\n\nIn the context of **Structured Query Language (SQL)**, **aggregate functions** are deterministic built-in functions that operate on a *set of rows* (a relation) and return a **single summarized scalar value**. They are part of the SQL **Data Manipulation Language (DML)** and conform to the ISO/IEC 9075 standard. The standard aggregate functions supported by virtually all KTU-approved RDBMS engines (Oracle 21c, MySQL 8.0, PostgreSQL 16) are:\n\n$$\\text{Aggregate} = \\left\\{ \\text{COUNT},\\ \\text{SUM},\\ \\text{AVG},\\ \\text{MIN},\\ \\text{MAX} \\right\\}$$\n\nThe clauses **ORDER BY**, **GROUP BY**, and **HAVING** are *query-modifying clauses* used in conjunction with the `SELECT` statement to control row ordering, row partitioning, and post-aggregation filtering respectively.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Mapping (PCCSL408 — DBMS Lab):**\n> The official syllabus directive states: *\"Execution of aggregate functions, grouping of data, and filtering groups using HAVING clause on sample relational schemas.\"* This experiment is a **mandatory record-submission item** worth **20 marks** in the continuous evaluation component.\n\n### 1.2 Conceptual Analogy — The \"Class Report Card\" Metaphor\n\nImagine a school teacher with a register of **500 students**. The teacher does not want to see 500 individual rows; she wants **summarized information**:\n\n- 🧮 **Aggregate function** → The teacher asks: *\"What is the AVERAGE mark of the class?\"* — One number comes out of many rows. That is aggregation.\n- 📚 **GROUP BY** → The teacher groups the register *by section* (A, B, C) and asks: *\"Average per section.\"* Each group yields one answer.\n- 🏆 **ORDER BY** → After computing averages, the teacher sorts the sections from highest to lowest to declare the top-performing section.\n- 🚫 **HAVING** → The teacher ignores sections whose average is below 40 — this filtering happens **after** the grouping, not before.\n\n> [!NOTE]\n> **Mnemonic for KTU Viva:** *\"Aggregate Summarizes, Group Partitions, Order Sorts, Having Filters Groups.\"*\n\n### 1.3 Why This Experiment Matters (Engineering Utility)\n\n| Domain | Real-World Use |\n|--------|----------------|\n| **Banking Systems** | SUM of transactions per account |\n| **E-Commerce** | COUNT of orders per customer, AVG cart value |\n| **Hospital MIS** | MAX/MIN patient vitals grouped by ward |\n| **Academic ERPs (KTU itself)** | COUNT of students per branch, AVG CGPA per semester |\n| **IoT Analytics** | AVG sensor temperature per device per hour |\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Five Standard Aggregate Functions\n\n### 2.1.1 `COUNT()`\nReturns the number of rows that match the query specification.\n- `COUNT(*)` → counts **all rows including NULLs and duplicates**.\n- `COUNT(column)` → counts **non-NULL values only** in the specified column.\n- `COUNT(DISTINCT column)` → counts **unique non-NULL values**.\n\n### 2.1.2 `SUM()`\nComputes the **arithmetic total** of all non-NULL numeric values in a column. Operates on INT, DECIMAL, FLOAT data types.\n\n### 2.1.3 `AVG()`\nReturns the **arithmetic mean**:\n\n$$\\text{AVG}(X) = \\frac{\\sum_{i=1}^{n} x_i}{n}$$\n\nwhere $n$ is the count of **non-NULL** values. The denominator is *not* the total row count — this is a frequent KTU viva trap.\n\n### 2.1.4 `MAX()` and `MIN()`\nReturn the **lexicographically largest** and **smallest** value respectively. Work on numeric, string, and date data types. For strings, comparison follows the database collation sequence (usually ASCII / Unicode codepoint order).\n\n> [!NOTE]\n> **The \"5 + 2\" Rule of SQL Aggregation:** KTU examiners expect students to know the 5 core functions plus the **2 essential clauses** (`GROUP BY`, `HAVING`) that make aggregation meaningful. Memorize: *COUNT, SUM, AVG, MAX, MIN* + *GROUP BY, HAVING*.\n\n---\n\n## 2.2 The Three Controlling Clauses\n\n### 2.2.1 `ORDER BY` Clause\n- Used to **sort the result set** of a query.\n- Default sort order: **ASCENDING (ASC)**.\n- `DESC` keyword specifies descending order.\n- Can sort by **column alias**, **column position**, or **expression**.\n\n```sql\nSELECT column_list\nFROM   table_name\nORDER BY column1 [ASC | DESC], column2 [ASC | DESC];\n```\n\n### 2.2.2 `GROUP BY` Clause\n- **Partitions** the relation into subsets based on the values of one or more columns.\n- Each unique combination of grouping columns forms **one group**.\n- The `SELECT` list can contain only:\n  1. Columns used in `GROUP BY`\n  2. Aggregate functions\n  3. Constants\n\n> [!WARNING]\n> **KTU Pitfall:** Selecting a non-grouped, non-aggregated column in the SELECT list when `GROUP BY` is used is a **syntax error in standard SQL** (and in Oracle). MySQL is lenient by default, but the KTU 2024 lab uses strict mode. Always group by every selected non-aggregated column.\n\n### 2.2.3 `HAVING` Clause\n- Filters **groups** (not individual rows).\n- Executes **after** `GROUP BY` and aggregation.\n- Can reference **aggregate functions** — this is the key difference from `WHERE`.\n\n$$\\text{WHERE} \\rightarrow \\text{filter rows} \\quad\\Rightarrow\\quad \\text{GROUP BY} \\rightarrow \\text{form groups} \\quad\\Rightarrow\\quad \\text{HAVING} \\rightarrow \\text{filter groups}$$\n\n---\n\n## 2.3 KTU Formula & Syntax Cheat Sheet\n\n| # | Construct | Syntax Template | Returns / Effect | NULL Handling |\n|---|-----------|----------------|------------------|---------------|\n| 1 | COUNT(*) | `SELECT COUNT(*) FROM T;` | Total row count | **Counts NULLs** |\n| 2 | COUNT(col) | `SELECT COUNT(col) FROM T;` | Non-NULL count of `col` | Ignores NULLs |\n| 3 | SUM(col) | `SELECT SUM(salary) FROM T;` | Total of `col` | Ignores NULLs |\n| 4 | AVG(col) | `SELECT AVG(salary) FROM T;` | Arithmetic mean of `col` | Ignores NULLs |\n| 5 | MAX(col) | `SELECT MAX(salary) FROM T;` | Largest value | Ignores NULLs |\n| 6 | MIN(col) | `SELECT MIN(salary) FROM T;` | Smallest value | Ignores NULLs |\n| 7 | ORDER BY | `ORDER BY col1 ASC, col2 DESC;` | Sorted result set | N/A |\n| 8 | GROUP BY | `GROUP BY col1, col2;` | One row per unique combination | NULLs form one group |\n| 9 | HAVING | `HAVING aggregate_function(...) op value;` | Filters groups | Operates on aggregated values |\n| 10 | WHERE | `WHERE column op value;` | Filters rows | Cannot use aggregates (in standard SQL) |\n\n### 2.4 WHERE vs HAVING — The Definitive Comparison\n\n| Property | WHERE | HAVING |\n|----------|-------|--------|\n| Filters | Individual **rows** | Aggregated **groups** |\n| Execution phase | **Before** GROUP BY | **After** GROUP BY |\n| Can use aggregate functions? | ❌ No | ✅ Yes |\n| Can use column aliases? | ❌ No | ✅ Yes |\n| Works without GROUP BY? | ✅ Yes | ✅ Yes (treats whole table as one group) |\n\n> [!IMPORTANT]\n> **Real-world engineering utility:** In a payroll system processing 10 million employee records, `WHERE department='CS'` first discards non-CS rows (fast, index-aided). Then `GROUP BY dept` partitions. Then `HAVING AVG(salary) > 50000` keeps only well-paid departments. This staged execution minimizes the data flowing through expensive aggregation operations.\n\n---\n\n## 2.5 The Logical Execution Order of a SQL SELECT\n\nKTU examiners frequently ask: *\"In what order does SQL logically process a query?\"* The **logical processing order** is:\n\n$$\\text{FROM} \\rightarrow \\text{WHERE} \\rightarrow \\text{GROUP BY} \\rightarrow \\text{HAVING} \\rightarrow \\text{SELECT} \\rightarrow \\text{ORDER BY} \\rightarrow \\text{LIMIT}$$\n\nThis explains why column aliases created in `SELECT` **cannot** be used in `WHERE` (they don't exist yet) but **can** be used in `ORDER BY` (it executes after SELECT).\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n# 3. Step-by-Step Analytical Walkthrough & Query Derivations\n\n## 3.1 Sample Schema (Reference for All Subsequent Queries)\n\nWe will use the standard **EMPLOYEE** table. This is the KTU-recommended sample schema for Experiment 2.\n\n```sql\nCREATE TABLE EMPLOYEE (\n    EMP_ID     INT PRIMARY KEY,\n    NAME       VARCHAR(50) NOT NULL,\n    DEPT       VARCHAR(20),\n    SALARY     DECIMAL(10,2),\n    JOIN_DATE  DATE,\n    CITY       VARCHAR(30)\n);\n```\n\n**Inserted Data Set:**\n\n| EMP_ID | NAME | DEPT | SALARY | JOIN_DATE | CITY |\n|--------|------|------|--------|-----------|------|\n| 101 | Arjun Nair | CS | 50000.00 | 2020-01-15 | Kochi |\n| 102 | Meera Pillai | CS | 60000.00 | 2019-03-22 | Trivandrum |\n| 103 | Rahul Menon | EC | 55000.00 | 2021-07-10 | Kochi |\n| 104 | Anjali Das | EC | 45000.00 | 2018-11-05 | Calicut |\n| 105 | Vikram Rao | ME | 70000.00 | 2017-05-18 | Trivandrum |\n| 106 | Sneha Iyer | ME | 65000.00 | 2022-02-01 | Kochi |\n| 107 | Kiran Kumar | CS | NULL | 2023-06-12 | Calicut |\n| 108 | Deepa Joseph | EC | 80000.00 | 2016-09-25 | Trivandrum |\n\n> Note: EMP_ID 107 has a **NULL salary** — this row is crucial for demonstrating NULL behavior in aggregates.\n\n---\n\n## 3.2 Query Derivations — Each in Three Logical Phases\n\n### 🔍 Query 1: Total Number of Employees\n\n```sql\nSELECT COUNT(*) AS TOTAL_EMPLOYEES\nFROM   EMPLOYEE;\n```\n\n**Execution trace:**\n\n| Step | Operation | Result |\n|------|-----------|--------|\n| 1 | FROM EMPLOYEE — read all 8 rows into a working set | 8 rows |\n| 2 | SELECT — apply COUNT(*) to the working set | `8` |\n| 3 | Output single-row, single-column result | TOTAL_EMPLOYEES = 8 |\n\n**Output:** `TOTAL_EMPLOYEES = 8`\n\n---\n\n### 🔍 Query 2: Number of Employees with a Recorded Salary\n\n```sql\nSELECT COUNT(SALARY) AS PAID_EMPLOYEES\nFROM   EMPLOYEE;\n```\n\n**Execution trace:**\n\n| Step | Operation | Result |\n|------|-----------|--------|\n| 1 | FROM EMPLOYEE | 8 rows loaded |\n| 2 | COUNT(SALARY) — count only non-NULL salary cells | Kiran (NULL) excluded → 7 |\n| 3 | Output | PAID_EMPLOYEES = 7 |\n\n**Output:** `PAID_EMPLOYEES = 7` (Kiran's NULL row excluded)\n\n**Mathematical notation:**\n\n$$\\text{COUNT(SALARY)} = \\sum_{i=1}^{8} \\mathbb{1}_{\\text{SALARY}_i \\neq \\text{NULL}} = 7$$\n\nwhere $\\mathbb{1}$ is the indicator function.\n\n---\n\n### 🔍 Query 3: Total Salary Payout\n\n```sql\nSELECT SUM(SALARY) AS TOTAL_PAYOUT\nFROM   EMPLOYEE;\n```\n\n**Step-by-step calculation:**\n\n$$\\text{TOTAL} = 50000 + 60000 + 55000 + 45000 + 70000 + 65000 + \\text{NULL} + 80000$$\n\nSince SQL's `SUM` **ignores NULLs**:\n\n$$\\text{TOTAL} = 50000 + 60000 + 55000 + 45000 + 70000 + 65000 + 80000 = 425000.00$$\n\n**Output:** `TOTAL_PAYOUT = 425000.00`\n\n---\n\n### 🔍 Query 4: Average Salary\n\n```sql\nSELECT AVG(SALARY) AS AVERAGE_SALARY\nFROM   EMPLOYEE;\n```\n\n**Step-by-step calculation:**\n\n$$\\text{AVG(SALARY)} = \\frac{\\text{SUM(SALARY)}}{\\text{COUNT(SALARY)}} = \\frac{425000.00}{7}$$\n\n$$\\text{AVG(SALARY)} = 60714.285714\\ldots \\quad \\text{(rounded to 2 decimals)} = 60714.29$$\n\n> [!IMPORTANT]\n> **The denominator is 7 (non-NULL count), NOT 8 (total rows).** This is a guaranteed KTU viva question. Many students wrongly compute 425000/8 = 53125.00 — that is incorrect for SQL's `AVG`.\n\n---\n\n### 🔍 Query 5: Highest and Lowest Salaries\n\n```sql\nSELECT MAX(SALARY) AS HIGHEST_SAL,\n       MIN(SALARY) AS LOWEST_SAL\nFROM   EMPLOYEE;\n```\n\n**Step-by-step derivation:**\n\n- `MAX(SALARY)`: Scan all 7 non-NULL salary values → largest = **80000.00** (Deepa Joseph)\n- `MIN(SALARY)`: Scan all 7 non-NULL salary values → smallest = **45000.00** (Anjali Das)\n\n**Output:**\n\n| HIGHEST_SAL | LOWEST_SAL |\n|-------------|------------|\n| 80000.00 | 45000.00 |\n\n---\n\n### 🔍 Query 6: Employees Sorted by Salary (ORDER BY)\n\n```sql\nSELECT NAME, DEPT, SALARY\nFROM   EMPLOYEE\nWHERE  SALARY IS NOT NULL\nORDER BY SALARY DESC;\n```\n\n**Execution trace:**\n\n| Phase | Operation | Result |\n|-------|-----------|--------|\n| 1 | FROM EMPLOYEE | 8 rows |\n| 2 | WHERE SALARY IS NOT NULL | 7 rows (Kiran excluded) |\n| 3 | SELECT NAME, DEPT, SALARY | 7 rows × 3 columns |\n| 4 | ORDER BY SALARY DESC | Sort descending by SALARY |\n\n**Final Output:**\n\n| NAME | DEPT | SALARY |\n|------|------|--------|\n| Deepa Joseph | EC | 80000.00 |\n| Vikram Rao | ME | 70000.00 |\n| Sneha Iyer | ME | 65000.00 |\n| Meera Pillai | CS | 60000.00 |\n| Rahul Menon | EC | 55000.00 |\n| Arjun Nair | CS | 50000.00 |\n| Anjali Das | EC | 45000.00 |\n\n> [!NOTE]\n> **Why `WHERE SALARY IS NOT NULL`?** `ORDER BY` in SQL is allowed to place NULLs in any order (implementation-defined). In Oracle, NULLs sort LAST in ASC and FIRST in DESC; in MySQL, opposite. To get deterministic, KTU-acceptable output, we explicitly filter.\n\n---\n\n### 🔍 Query 7: Number of Employees in Each Department (GROUP BY)\n\n```sql\nSELECT DEPT, COUNT(*) AS EMP_COUNT\nFROM   EMPLOYEE\nGROUP BY DEPT;\n```\n\n**Execution trace:**\n\n| Step | Operation | Result |\n|------|-----------|--------|\n| 1 | FROM EMPLOYEE | 8 rows |\n| 2 | GROUP BY DEPT — partition rows by DEPT value | 3 partitions: {CS, EC, ME} |\n| 3 | COUNT(*) per partition | CS → 3, EC → 3, ME → 2 |\n| 4 | SELECT DEPT, COUNT(*) | Output 3 rows |\n\n**Output:**\n\n| DEPT | EMP_COUNT |\n|------|-----------|\n| CS | 3 |\n| EC | 3 |\n| ME | 2 |\n\n**Partitioned working sets:**\n\n$$\n\\begin{aligned}\n\\text{GROUP}_{\\text{CS}} &= \\{101, 102, 107\\} \\\\\n\\text{GROUP}_{\\text{EC}} &= \\{103, 104, 108\\} \\\\\n\\text{GROUP}_{\\text{ME}} &= \\{105, 106\\}\n\\end{aligned}\n$$\n\n---\n\n### 🔍 Query 8: Department-wise Salary Statistics (Multiple Aggregates + GROUP BY)\n\n```sql\nSELECT DEPT,\n       COUNT(*)       AS TOTAL_STAFF,\n       SUM(SALARY)    AS DEPT_PAYROLL,\n       AVG(SALARY)    AS AVG_SAL,\n       MAX(SALARY)    AS MAX_SAL,\n       MIN(SALARY)    AS MIN_SAL\nFROM   EMPLOYEE\nGROUP BY DEPT\nORDER BY AVG_SAL DESC;\n```\n\n**Detailed calculation per group:**\n\nFor **CS** (rows 101, 102, 107):\n- COUNT(*) = 3\n- SUM = 50000 + 60000 + NULL = 110000\n- AVG = 110000 / 2 = 55000.00 (denominator excludes NULL!)\n- MAX = 60000.00\n- MIN = 50000.00\n\nFor **EC** (rows 103, 104, 108):\n- COUNT(*) = 3\n- SUM = 55000 + 45000 + 80000 = 180000\n- AVG = 180000 / 3 = 60000.00\n- MAX = 80000.00\n- MIN = 45000.00\n\nFor **ME** (rows 105, 106):\n- COUNT(*) = 2\n- SUM = 70000 + 65000 = 135000\n- AVG = 135000 / 2 = 67500.00\n- MAX = 70000.00\n- MIN = 65000.00\n\n**Final Output (sorted by AVG_SAL DESC):**\n\n| DEPT | TOTAL_STAFF | DEPT_PAYROLL | AVG_SAL | MAX_SAL | MIN_SAL |\n|------|-------------|--------------|---------|---------|---------|\n| ME | 2 | 135000.00 | 67500.00 | 70000.00 | 65000.00 |\n| EC | 3 | 180000.00 | 60000.00 | 80000.00 | 45000.00 |\n| CS | 3 | 110000.00 | 55000.00 | 60000.00 | 50000.00 |\n\n---\n\n### 🔍 Query 9: Departments Having More Than 2 Employees (HAVING)\n\n```sql\nSELECT DEPT, COUNT(*) AS STAFF\nFROM   EMPLOYEE\nGROUP BY DEPT\nHAVING COUNT(*) > 2;\n```\n\n**Execution trace:**\n\n| Step | Operation | Result |\n|------|-----------|--------|\n| 1 | FROM EMPLOYEE | 8 rows |\n| 2 | GROUP BY DEPT | 3 groups: CS(3), EC(3), ME(2) |\n| 3 | HAVING COUNT(*) > 2 — discard ME (2 ≤ 2) | 2 groups survive: CS, EC |\n| 4 | SELECT DEPT, COUNT(*) | Output 2 rows |\n\n**Output:**\n\n| DEPT | STAFF |\n|------|-------|\n| CS | 3 |\n| EC | 3 |\n\n> [!WARNING]\n> **If you had written `WHERE COUNT(*) > 2` instead of `HAVING`, it would be a SYNTAX ERROR.** Aggregates cannot appear in `WHERE`. This is a classic KTU exam trap.\n\n---\n\n### 🔍 Query 10: Departments Whose Average Salary Exceeds 55000 (WHERE + GROUP BY + HAVING + ORDER BY)\n\n```sql\nSELECT DEPT,\n       COUNT(*)    AS STAFF,\n       AVG(SALARY) AS AVG_SAL\nFROM   EMPLOYEE\nWHERE  CITY IN ('Kochi', 'Trivandrum')\nGROUP BY DEPT\nHAVING AVG(SALARY) > 55000\nORDER BY AVG_SAL DESC;\n```\n\n**Full execution trace:**\n\n| Phase | Operation | Detail |\n|-------|-----------|--------|\n| 1 | FROM EMPLOYEE | Load all 8 rows |\n| 2 | WHERE CITY IN ('Kochi','Trivandrum') | Keep rows with EMP_ID 101, 102, 103, 105, 106, 108 (6 rows) |\n| 3 | GROUP BY DEPT | CS: {101,102}, EC: {103,108}, ME: {105,106} |\n| 4 | AVG per group | CS: (50000+60000)/2 = 55000.00; EC: (55000+80000)/2 = 67500.00; ME: (70000+65000)/2 = 67500.00 |\n| 5 | HAVING AVG > 55000 | Drop CS (55000 not > 55000). Keep EC, ME |\n| 6 | ORDER BY AVG_SAL DESC | EC and ME both 67500 — DB may return either order |\n| 7 | SELECT | Output 2 rows |\n\n**Output:**\n\n| DEPT | STAFF | AVG_SAL |\n|------|-------|---------|\n| EC | 2 | 67500.00 |\n| ME | 2 | 67500.00 |\n\n---\n\n### 🔍 Query 11: City-wise Employee Count with Sorting\n\n```sql\nSELECT CITY, COUNT(*) AS EMP_COUNT\nFROM   EMPLOYEE\nGROUP BY CITY\nORDER BY EMP_COUNT DESC, CITY ASC;\n```\n\n**Output:**\n\n| CITY | EMP_COUNT |\n|------|-----------|\n| Kochi | 3 |\n| Trivandrum | 3 |\n| Calicut | 2 |\n\n*Note: Ties broken alphabetically by CITY (ASC) — secondary sort key.*\n\n---\n\n### 🔍 Query 12: Using GROUP BY on Multiple Columns\n\n```sql\nSELECT DEPT, CITY, COUNT(*) AS CNT, AVG(SALARY) AS AVG_S\nFROM   EMPLOYEE\nGROUP BY DEPT, CITY\nORDER BY DEPT, CITY;\n```\n\n**Output:**\n\n| DEPT | CITY | CNT | AVG_S |\n|------|------|-----|-------|\n| CS | Calicut | 1 | NULL |\n| CS | Kochi | 1 | 50000.00 |\n| CS | Trivandrum | 1 | 60000.00 |\n| EC | Calicut | 1 | 45000.00 |\n| EC | Kochi | 1 | 55000.00 |\n| EC | Trivandrum | 1 | 80000.00 |\n| ME | Kochi | 1 | 65000.00 |\n| ME | Trivandrum | 1 | 70000.00 |\n\n> [!NOTE]\n> Each unique `(DEPT, CITY)` pair forms one group. `AVG_S = NULL` for Kiran (CS, Calicut) because his salary is NULL — `AVG` of a single NULL is undefined, hence NULL.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Logical Execution Pipeline of a SQL Query\n\nThe following Mermaid diagram illustrates the **sequential logical processing order** that the SQL engine follows when executing a query containing aggregation, grouping, filtering, and sorting.\n\n```mermaid\nflowchart TD\n    A[Start: SELECT Query Received] --> B[Step 1: FROM Clause — Identify source table]\n    B --> C[Step 2: WHERE Clause — Filter individual rows]\n    C --> D{Rows remain?}\n    D -- No --> Z[Return empty result set]\n    D -- Yes --> E[Step 3: GROUP BY Clause — Partition rows into groups]\n    E --> F{Any groups formed?}\n    F -- No --> Z\n    F -- Yes --> G[Step 4: HAVING Clause — Filter groups by aggregate conditions]\n    G --> H{Groups pass filter?}\n    H -- No --> Z\n    H -- Yes --> I[Step 5: SELECT Clause — Project columns, compute aggregates]\n    I --> J[Step 6: ORDER BY Clause — Sort final result set]\n    J --> K[Step 7: LIMIT OFFSET — Return top N rows if specified]\n    K --> L[End: Deliver result to user / application]\n\n    style A fill:#1f4e79,color:#ffffff,stroke:#0b2545,stroke-width:2px\n    style L fill:#2e7d32,color:#ffffff,stroke:#1b5e20,stroke-width:2px\n    style B fill:#e3f2fd,color:#0b2545,stroke:#1976d2\n    style C fill:#fff3e0,color:#e65100,stroke:#fb8c00\n    style E fill:#f3e5f5,color:#4a148c,stroke:#7b1fa2\n    style G fill:#fce4ec,color:#880e4f,stroke:#c2185b\n    style I fill:#e8f5e9,color:#1b5e20,stroke:#43a047\n    style J fill:#e0f7fa,color:#006064,stroke:#00838f\n    style K fill:#f1f8e9,color:#33691e,stroke:#689f38\n    style D fill:#fff9c4,color:#f57f17,stroke:#f9a825\n    style F fill:#fff9c4,color:#f57f17,stroke:#f9a825\n    style H fill:#fff9c4,color:#f57f17,stroke:#f9a825\n    style Z fill:#ffcdd2,color:#b71c1c,stroke:#c62828\n```\n\n## 4.2 Block Diagram — Internal Architecture of Aggregation in a DBMS Engine\n\n```mermaid\nflowchart LR\n    P[Parser and Validator] --> Q[Query Rewriter]\n    Q --> O[Query Optimizer with Cost Model]\n    O --> E[Execution Engine]\n\n    E --> E1[Scan Operator: Read rows from table]\n    E1 --> E2[Filter Operator: Apply WHERE predicate]\n    E2 --> E3[Aggregate Operator: Build hash table per group]\n    E3 --> E4[Group Filter Operator: Apply HAVING]\n    E4 --> E5[Project Operator: Compute SELECT expressions]\n    E5 --> E6[Sort Operator: Apply ORDER BY]\n    E6 --> E7[Limit Operator: Apply LIMIT and OFFSET]\n    E7 --> R[Result Set Returned to Client]\n\n    E3 -.uses.-> H1[In Memory Hash Table: group key to accumulator]\n    E3 -.computes.-> H2[Running SUM, COUNT, MIN, MAX, AVG per group]\n\n    style P fill:#bbdefb,color:#0d47a1\n    style Q fill:#bbdefb,color:#0d47a1\n    style O fill:#bbdefb,color:#0d47a1\n    style E fill:#c8e6c9,color:#1b5e20\n    style E1 fill:#fff9c4,color:#f57f17\n    style E2 fill:#fff9c4,color:#f57f17\n    style E3 fill:#ffccbc,color:#bf360c\n    style E4 fill:#fff9c4,color:#f57f17\n    style E5 fill:#c8e6c9,color:#1b5e20\n    style E6 fill:#b2dfdb,color:#004d40\n    style E7 fill:#b2dfdb,color:#004d40\n    style R fill:#a5d6a7,color:#1b5e20\n    style H1 fill:#f8bbd0,color:#880e4f\n    style H2 fill:#f8bbd0,color:#880e4f\n```\n\n## 4.3 Conceptual Mapping — Clause to Real-World Analogy\n\n```mermaid\nflowchart LR\n    subgraph SRC [Source Data: 8 Employee Records]\n    end\n\n    SRC --> W[WHERE: Keep only Kochi and Trivandrum staff]\n    W --> G[GROUP BY DEPT: Partition into 3 buckets]\n    G --> H[HAVING: Discard departments with average below threshold]\n    H --> S[SELECT: Compute COUNT, SUM, AVG per surviving group]\n    S --> O[ORDER BY: Sort groups by average descending]\n    O --> OUT[Final Output: 2 department summary rows]\n\n    style SRC fill:#e1f5fe,color:#01579b\n    style W fill:#fff3e0,color:#e65100\n    style G fill:#f3e5f5,color:#4a148c\n    style H fill:#fce4ec,color:#880e4f\n    style S fill:#e8f5e9,color:#1b5e20\n    style O fill:#e0f7fa,color:#006064\n    style OUT fill:#c8e6c9,color:#1b5e20,stroke-width:3px\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (2 × 3 Marks = 6 Marks)\n\n### Question A.1 `[KTU University Exam — May 2024]`\n**Differentiate between the WHERE and HAVING clauses in SQL. Can an aggregate function appear in a WHERE clause? Justify your answer.**\n\n**Model Answer (3 marks):**\n\n| Aspect | WHERE | HAVING |\n|--------|-------|--------|\n| Purpose | Filters **individual rows** before grouping | Filters **groups** after aggregation |\n| Aggregate usage | ❌ **Not allowed** (syntax error in standard SQL) | ✅ **Allowed** — references aggregate results |\n| Execution phase | Before `GROUP BY` | After `GROUP BY` |\n| Operates on | Source-table rows | Result of aggregation |\n\n**Justification (1 mark):** Aggregate functions cannot appear in `WHERE` because `WHERE` executes *before* `GROUP BY` and aggregation. At that stage, no aggregate value exists yet. Attempting `WHERE COUNT(*) > 5` raises a *\"group function is not allowed here\"* error in Oracle. The correct approach is to use `HAVING COUNT(*) > 5`.\n\n---\n\n### Question A.2 `[KTU University Exam — Dec 2023]`\n**Explain how NULL values are treated by the aggregate functions COUNT(*), COUNT(column), SUM, and AVG. Illustrate with one example each.**\n\n**Model Answer (3 marks):**\n\n| Function | NULL Behavior |\n|----------|---------------|\n| `COUNT(*)` | Counts **all rows including those with NULLs** in any column |\n| `COUNT(col)` | Counts only rows where `col` is **non-NULL** |\n| `SUM(col)` | **Ignores NULLs**; returns NULL if all values are NULL |\n| `AVG(col)` | Denominator = count of **non-NULL** values only, not total row count |\n\n**Example (1 mark):**\n```sql\n-- Table T has 5 rows, 2 of which have NULL in 'marks'\nSELECT COUNT(*), COUNT(marks), SUM(marks), AVG(marks) FROM T;\n-- Result:  5              3            180          60\n```\nHere `AVG = 180/3 = 60` (not `180/5 = 36`), because the denominator excludes NULLs.\n\n---\n\n## 5.2 Part B — Long Answer Questions (Choice-Based, 1 × 14 Marks)\n\n---\n\n### ⭐ Question B.A (Choice 1) `[KTU University Exam — May 2024, Module 2]`\n\n**Consider the following `STUDENT` table:**\n\n| ROLL_NO | NAME | BRANCH | SEM | MARKS | CITY |\n|---------|------|--------|-----|-------|------|\n| 1 | Anu | CS | 5 | 85 | Kochi |\n| 2 | Binu | CS | 5 | 72 | Trivandrum |\n| 3 | Cijo | EC | 5 | 60 | Kochi |\n| 4 | Deepa | EC | 5 | 91 | Calicut |\n| 5 | Esha | CS | 6 | 78 | Kochi |\n| 6 | Firoz | ME | 6 | 55 | Trivandrum |\n| 7 | Gita | ME | 5 | NULL | Kochi |\n| 8 | Hari | CS | 6 | 88 | Trivandrum |\n\n**Write SQL queries for the following:** **(7 + 7 = 14 marks)**\n\n#### Part (a) — 7 Marks\n**(i)** Find the total number of students, the highest marks, the lowest marks, and the average marks of the entire student table.\n**(ii)** Display the branch-wise count of students and branch-wise average marks, sorted by average marks in descending order.\n**(iii)** Display only those branches where the average marks exceed 75.\n\n**Model Solution:**\n\n**(i) Overall statistics — 2 marks**\n```sql\nSELECT COUNT(*)  AS TOTAL_STUDENTS,\n       MAX(MARKS) AS HIGHEST,\n       MIN(MARKS) AS LOWEST,\n       AVG(MARKS) AS AVERAGE\nFROM   STUDENT;\n```\n- `COUNT(*)` returns 8 (counts NULL row Gita too)\n- `MAX/MIN/AVG` ignore NULL → MAX=91, MIN=55, AVG = (85+72+60+91+78+55+88)/7 = 529/7 ≈ 75.57\n\n**(ii) Branch-wise aggregation with sort — 3 marks**\n```sql\nSELECT BRANCH,\n       COUNT(*)    AS STUDENT_COUNT,\n       AVG(MARKS)  AS AVG_MARKS\nFROM   STUDENT\nGROUP BY BRANCH\nORDER BY AVG_MARKS DESC;\n```\nOutput (calculation omitted for brevity; expected in exam):\n- CS → 4 students, AVG = (85+72+78+88)/4 = 323/4 = 80.75\n- EC → 2 students, AVG = (60+91)/2 = 75.50\n- ME → 2 students, AVG = (55+NULL)/1 = 55.00\n\n**(iii) Branches with average above 75 — 2 marks**\n```sql\nSELECT BRANCH, AVG(MARKS) AS AVG_MARKS\nFROM   STUDENT\nGROUP BY BRANCH\nHAVING AVG(MARKS) > 75;\n```\nOutput: CS (80.75) only. EC's 75.50 also qualifies → both CS and EC are returned depending on whether the threshold is strict `>` or non-strict `>=`. With `> 75`, only CS qualifies.\n\n#### Part (b) — 7 Marks\n**(i)** Display the name, branch, and marks of all students sorted by marks in descending order; for students with equal marks, sort by name in ascending order. Handle NULL marks appropriately.\n**(ii)** Display the branch and semester-wise count of students, but only for students from 'Kochi' or 'Trivandrum', and only for those (branch, semester) combinations where the count is at least 2.\n**(iii)** Display the city-wise total marks (treating NULL as 0 using COALESCE) and the number of students per city.\n\n**Model Solution:**\n\n**(i) Multi-key sorting with NULL handling — 2 marks**\n```sql\nSELECT NAME, BRANCH, MARKS\nFROM   STUDENT\nORDER BY MARKS DESC, NAME ASC;\n```\n(Acceptable: add `WHERE MARKS IS NOT NULL` to exclude NULLs explicitly.)\n\n**(ii) Multi-column GROUP BY with WHERE + HAVING — 3 marks**\n```sql\nSELECT BRANCH, SEM, COUNT(*) AS STUDENT_COUNT\nFROM   STUDENT\nWHERE  CITY IN ('Kochi', 'Trivandrum')\nGROUP BY BRANCH, SEM\nHAVING COUNT(*) >= 2;\n```\n*Execution logic:*\n- Step 1: WHERE restricts to 6 rows (Anu, Binu, Cijo, Esha, Firoz, Hari — Deepa and Gita excluded)\n- Step 2: GROUP BY (BRANCH, SEM) partitions\n- Step 3: HAVING >= 2 filters\n\n**(iii) City-wise aggregation with COALESCE — 2 marks**\n```sql\nSELECT CITY,\n       COALESCE(SUM(MARKS), 0) AS TOTAL_MARKS,\n       COUNT(*)                AS STUDENT_COUNT\nFROM   STUDENT\nGROUP BY CITY;\n```\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall Callout:**\n> 1. **Misplaced HAVING:** Writing `WHERE COUNT(*) > 2` instead of `HAVING COUNT(*) > 2` will be awarded **0 marks** for the filter logic, even if the rest of the query is correct.\n> 2. **NULL in AVG:** Computing AVG with total-row denominator (instead of non-NULL count) loses **1–2 marks**.\n> 3. **Omitting ORDER BY in (i):** Many students forget the secondary sort by NAME — KTU strictly awards marks only when the exact ordering is specified.\n> 4. **Forgetting GROUP BY when mixing aggregates with non-aggregated columns:** This produces ambiguous column errors.\n\n---\n\n### ⭐ Question B.B (Choice 2) `[KTU University Exam — Dec 2023, Module 2]`\n\n**Consider an `ORDERS` table for an e-commerce database:**\n\n| ORDER_ID | CUSTOMER_ID | PRODUCT | CATEGORY | QUANTITY | PRICE | ORDER_DATE |\n|----------|-------------|---------|----------|----------|-------|------------|\n| 1001 | C01 | Laptop | Electronics | 1 | 75000 | 2024-01-15 |\n| 1002 | C02 | Headphones | Electronics | 2 | 3000 | 2024-01-16 |\n| 1003 | C01 | Chair | Furniture | 4 | 5000 | 2024-02-10 |\n| 1004 | C03 | Desk | Furniture | 1 | 15000 | 2024-02-12 |\n| 1005 | C02 | Monitor | Electronics | 1 | 25000 | 2024-03-05 |\n| 1006 | C04 | Notebook | Stationery | 10 | 50 | 2024-03-08 |\n| 1007 | C01 | Pen | Stationery | 20 | 20 | 2024-03-15 |\n| 1008 | C03 | Bookshelf | Furniture | 1 | 12000 | 2024-04-02 |\n\n**Solve the following:** **(7 + 7 = 14 marks)**\n\n#### Part (a) — 7 Marks\n**(i)** Write an SQL query to compute the total revenue, average order value (use `PRICE * QUANTITY`), the largest single order value, and the total number of orders.\n**(ii)** Display category-wise total revenue and number of distinct customers, sorted by total revenue in descending order.\n\n**Model Solution:**\n\n**(i) Global order statistics — 3 marks**\n```sql\nSELECT COUNT(*)                            AS TOTAL_ORDERS,\n       SUM(PRICE * QUANTITY)               AS TOTAL_REVENUE,\n       AVG(PRICE * QUANTITY)               AS AVG_ORDER_VALUE,\n       MAX(PRICE * QUANTITY)               AS LARGEST_ORDER\nFROM   ORDERS;\n```\n\nCalculations:\n- ORDER 1001: 75000×1 = 75000\n- ORDER 1002: 3000×2 = 6000\n- ORDER 1003: 5000×4 = 20000\n- ORDER 1004: 15000×1 = 15000\n- ORDER 1005: 25000×1 = 25000\n- ORDER 1006: 50×10 = 500\n- ORDER 1007: 20×20 = 400\n- ORDER 1008: 12000×1 = 12000\n\n$$\\text{TOTAL REVENUE} = 75000+6000+20000+15000+25000+500+400+12000 = 153900$$\n\n$$\\text{AVG} = 153900 / 8 = 19237.50$$\n\n$$\\text{MAX} = 75000,\\quad \\text{MIN} = 400$$\n\n**(ii) Category aggregation with COUNT DISTINCT — 4 marks**\n```sql\nSELECT CATEGORY,\n       SUM(PRICE * QUANTITY) AS CATEGORY_REVENUE,\n       COUNT(DISTINCT CUSTOMER_ID) AS UNIQUE_CUSTOMERS\nFROM   ORDERS\nGROUP BY CATEGORY\nORDER BY CATEGORY_REVENUE DESC;\n```\n\nExpected output (calculation steps required in exam):\n- Electronics: 75000+6000+25000 = 106000; customers = {C01, C02, C05? actually C01,C02} → 2\n- Furniture: 20000+15000+12000 = 47000; customers = {C01, C03} → 2\n- Stationery: 500+400 = 900; customers = {C04, C01} → 2\n\n#### Part (b) — 7 Marks\n**(i)** List all product names sorted by their total value (price × quantity) in ascending order. Show only those products whose total value is above 1000.\n**(ii)** For each customer, find the total amount they have spent and the number of orders they placed, but display only those customers whose total spending exceeds 10000, sorted by total spending in descending order.\n**(iii)** Using a subquery with `HAVING`, find the product(s) that have generated the maximum revenue within their category.\n\n**Model Solution:**\n\n**(i) Product-level value filtering — 2 marks**\n```sql\nSELECT PRODUCT, SUM(PRICE * QUANTITY) AS TOTAL_VALUE\nFROM   ORDERS\nGROUP BY PRODUCT\nHAVING SUM(PRICE * QUANTITY) > 1000\nORDER BY TOTAL_VALUE ASC;\n```\n\n**(ii) Customer spending with HAVING filter — 3 marks**\n```sql\nSELECT CUSTOMER_ID,\n       SUM(PRICE * QUANTITY) AS TOTAL_SPENT,\n       COUNT(*)              AS ORDER_COUNT\nFROM   ORDERS\nGROUP BY CUSTOMER_ID\nHAVING SUM(PRICE * QUANTITY) > 10000\nORDER BY TOTAL_SPENT DESC;\n```\n\n**Calculation trace:**\n- C01: orders 1001, 1003, 1007 → 75000+20000+400 = 95400 (qualifies)\n- C02: orders 1002, 1005 → 6000+25000 = 31000 (qualifies)\n- C03: orders 1004, 1008 → 15000+12000 = 27000 (qualifies)\n- C04: order 1006 → 500 (does NOT qualify, filtered out)\n\n**(iii) Subquery with HAVING — 2 marks**\n```sql\nSELECT CATEGORY, PRODUCT,\n       SUM(PRICE * QUANTITY) AS PRODUCT_REVENUE\nFROM   ORDERS\nGROUP BY CATEGORY, PRODUCT\nHAVING SUM(PRICE * QUANTITY) = (\n    SELECT MAX(category_total)\n    FROM (\n        SELECT CATEGORY AS cat,\n               SUM(PRICE * QUANTITY) AS category_total\n        FROM   ORDERS o2\n        WHERE  o2.CATEGORY = ORDERS.CATEGORY\n        GROUP BY PRODUCT\n    ) AS inner_query\n);\n```\n\n> [!WARNING]\n> **Common marks-losing mistakes in this question:**\n> 1. **Mixing WHERE with aggregates:** Writing `WHERE SUM(PRICE*QUANTITY) > 1000` instead of `HAVING` — immediate 0 for the filter clause.\n> 2. **Forgetting `DISTINCT` in `COUNT(DISTINCT CUSTOMER_ID)`:** Plain `COUNT(CUSTOMER_ID)` will over-count customers with multiple orders — examiner deducts 1 mark.\n> 3. **Order of clauses:** `ORDER BY` must come **last** in a SELECT statement (after HAVING). Putting it before HAVING is a syntax error.\n> 4. **Not aliasing computed expressions:** Without aliases like `AS TOTAL_SPENT`, the output is technically correct but loses presentation marks.\n\n---\n\n## 5.3 KTU 2024 Common Viva-Voce Questions (Quick-Fire)\n\n| # | Question | One-Line Answer |\n|---|----------|-----------------|\n| 1 | Can `ORDER BY` use column alias? | ✅ Yes, because it executes after SELECT |\n| 2 | Can `WHERE` use column alias? | ❌ No, executes before SELECT |\n| 3 | Does `COUNT(*)` count NULLs? | ✅ Yes |\n| 4 | Does `AVG` ignore NULLs? | ✅ Yes — both numerator and denominator |\n| 5 | `GROUP BY` and `DISTINCT` — same? | Not exactly; DISTINCT returns rows, GROUP BY forms groups |\n| 6 | Can we use `HAVING` without `GROUP BY`? | ✅ Yes — entire table is treated as one group |\n| 7 | Default `ORDER BY` direction? | `ASC` (ascending) |\n| 8 | Position of `ORDER BY` clause? | Always the **last** clause in SELECT |\n| 9 | What is the difference between `WHERE` and `HAVING`? | WHERE filters rows, HAVING filters groups |\n| 10 | Order of SQL logical execution? | FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT |\n\n---\n\n## 5.4 📌 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Last-Minute Revision Checklist — KTU 2024 DBMS Lab Experiment 2**\n\n- ✅ The **5 standard aggregate functions** are: `COUNT`, `SUM`, `AVG`, `MAX`, `MIN`. Memorize them and their NULL behavior.\n- ✅ `COUNT(*)` counts rows including NULLs; `COUNT(column)` ignores NULLs.\n- ✅ `AVG`, `SUM`, `MAX`, `MIN` all **ignore NULL values** — this affects both numerator and denominator.\n- ✅ `GROUP BY` is **mandatory** when mixing aggregate and non-aggregate columns in `SELECT`. Every non-aggregated column in SELECT must appear in GROUP BY.\n- ✅ `WHERE` filters **rows before** grouping — cannot reference aggregates.\n- ✅ `HAVING` filters **groups after** aggregation — must reference aggregates or grouped columns.\n- ✅ `ORDER BY` is the **last** logical clause in a SELECT statement.\n- ✅ Default sort order is **ASCENDING**; use `DESC` keyword for descending.\n- ✅ Multi-column sort: secondary columns act as **tie-breakers**.\n- ✅ Logical execution order: **FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY**.\n- ✅ `HAVING` can be used **without** `GROUP BY` (treats the whole table as one implicit group).\n- ✅ NULL values form **one group** in `GROUP BY` (i.e., all NULLs are treated as equal for grouping).\n- ✅ For deterministic NULL ordering, use `ORDER BY col ASC NULLS LAST` (PostgreSQL/Oracle 21c) or `WHERE col IS NOT NULL`.\n- ✅ The KTU-recommended sample table for this experiment is typically a `STUDENT`, `EMPLOYEE`, `ORDERS`, or `PRODUCT` schema with at least **8 rows** to demonstrate group formation.\n- ✅ When asked to \"**find rows where aggregate > value**\", always default to `HAVING`, never `WHERE`.\n- ✅ For viva: be ready to write the **execution order** of SQL clauses on a whiteboard.\n- ✅ Use **column aliases** (`AS alias_name`) to make output presentable — KTU lab record evaluation gives bonus marks for neat output formatting.\n\n> **🎯 KTU 2024 Exam-Day Mantra for Experiment 2:**\n> *\"Aggregate → Group → Having → Order. WHERE before, HAVING after. NULLs ignored, except in COUNT(*).\"*\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pccsl408-mExperiment 2-t6",
          "title": "Implementation of set operators, nested queries, and join queries",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL408 - DBMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Implementation of set operators, nested queries, and join queries\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Set Operators in SQL\n\n> [!IMPORTANT]\n> **Formal Definition (KTU 2024 Scheme):** *Set operators in SQL are specialized keywords (UNION, UNION ALL, INTERSECT, EXCEPT) that combine the result sets of two or more SELECT statements into a single output by performing mathematical set theory operations (union, intersection, difference) on the retrieved tuples.*\n\n**Conceptual Analogy / Intuition:**\nImagine two circles in a Venn diagram, each containing a list of names. The `UNION` operator is like throwing both circles into a big box and removing duplicate names (keeping only unique ones). `UNION ALL` keeps every name, even duplicates. `INTERSECT` keeps only those names that appear in **both** circles. `EXCEPT` (or `MINUS`) keeps names that are in the **first** circle but **not** in the second. Just like a Venn diagram, the \"circles\" here are the result sets of two SELECT queries.\n\n> [!NOTE]\n> **Core Compatibility Rule:** For set operators to work, both SELECT statements **must** have the same number of columns, and the corresponding columns must have compatible (or implicitly convertible) data types.\n\n### 1.2 Nested Queries (Subqueries)\n\n> [!IMPORTANT]\n> **Formal Definition (KTU 2024 Scheme):** *A nested query, or subquery, is a SELECT statement embedded within another SQL statement (such as SELECT, FROM, WHERE, or HAVING clause). The inner query executes first and returns a value (or a set of values) that the outer query uses to perform its final retrieval.*\n\n**Conceptual Analogy / Intuition:**\nThink of it as asking a two-step question. First, you ask your friend, *\"What is the salary of the highest-paid employee?\"* (this is the **inner/subquery**). Once you get the answer (say, ₹90,000), you then ask yourself, *\"Show me everyone who earns more than ₹90,000\"* (this is the **outer query**). The outer query depends on the result of the inner query to function. This is the essence of nesting.\n\n### 1.3 Join Queries\n\n> [!IMPORTANT]\n> **Formal Definition (KTU 2024 Scheme):** *A join is an operation that combines rows from two or more related tables based on a common column (typically a Primary Key to Foreign Key relationship) using a predicate, producing a single composite result set that spans multiple tables.*\n\n**Conceptual Analogy / Intuition:**\nImagine two spreadsheets. The first has Employee IDs and Names. The second has Employee IDs and Salaries. A join is like using a *glue column* (the matching Employee ID) to stitch the spreadsheets side by side, so you end up with one big sheet containing ID, Name, **and** Salary together. The \"glue\" is the join condition (`ON` clause).\n\n### 1.4 Reference Schema Used in KTU Lab\n\n> [!NOTE]\n> Throughout these notes, the following standard KTU DBMS Lab schema is referenced (used in Module 1):\n>\n> - **DEPARTMENT** (DeptNo PK, Dname, Location)\n> - **EMPLOYEE** (Eno PK, Ename, Job, MgrNo, DoB, DoJ, Salary, Commission, DeptNo FK, Gender)\n> - **PROJECT** (Pno PK, Pname, PLocation, DeptNo FK)\n> - **ASSIGNED_TO** (Eno FK, Pno FK, Hours)\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Set Operators — Deep Breakdown\n\n**Set Theory Foundations in SQL:**\n\n| Operator | Mathematical Notation | Behaviour | Duplicate Handling |\n|----------|----------------------|-----------|--------------------|\n| `UNION` | $A \\cup B$ | Combines all rows from both queries | Removes duplicates automatically |\n| `UNION ALL` | $A \\cup B$ (with multiplicity) | Combines all rows | Keeps duplicates |\n| `INTERSECT` | $A \\cap B$ | Returns only common rows | Removes duplicates |\n| `EXCEPT` (or `MINUS` in Oracle) | $A - B$ | Returns rows in Query1 not in Query2 | Removes duplicates |\n\n> [!IMPORTANT]\n> **Why \"removes duplicates\" matters:** Set operators treat result sets as *true mathematical sets*. By default, SQL assumes set semantics (no duplicates), so the engine incurs the cost of an implicit `DISTINCT` sort. If you need speed and don't care about duplicates, use `UNION ALL` — it skips the dedup step and is significantly faster.\n\n**The \"Why\" behind the Column Matching Rule:**\nSQL performs set operations column-by-column positionally (left to right), not by column name. So `SELECT Ename FROM EMP` cannot be `UNION`-ed with `SELECT Salary FROM EMP` — the first column is a string, the second is a number. The number of columns and the data type *family* of each corresponding position must match.\n\n**KTU High-Yield Formula Sheet:**\n\n| Construct | General Syntax Template | Pre-Requisite Rule | Typical Use Case |\n|-----------|------------------------|--------------------|------------------|\n| Set Operator | $Q_1 \\ \\text{OP} \\ Q_2$ | $\\mid \\text{cols}(Q_1) \\mid = \\mid \\text{cols}(Q_2) \\mid$ | Combining two result sets |\n| `UNION` | `SELECT ... UNION SELECT ...` | Same column count + compatible types | Employees in dept 10 OR 20 |\n| `UNION ALL` | `SELECT ... UNION ALL SELECT ...` | Same as UNION (faster) | Audit logs, append-only history |\n| `INTERSECT` | `SELECT ... INTERSECT SELECT ...` | Same as UNION | Employees in BOTH projects A and B |\n| `EXCEPT` | `SELECT ... EXCEPT SELECT ...` | Same as UNION | Employees in project A but NOT B |\n| `IN` (subquery) | `WHERE col IN (SELECT col FROM ...)` | Subquery returns list | Multi-row membership test |\n| Scalar Subquery | `SELECT (SELECT ... FROM ... LIMIT 1) FROM ...` | Subquery returns one value | Computed column |\n| `INNER JOIN` | `FROM A JOIN B ON A.x = B.y` | Match condition required | Default cross-table query |\n| `LEFT JOIN` | `FROM A LEFT JOIN B ON ...` | Preserves left rows | Find unmatched (orphans) |\n| `SELF JOIN` | `FROM A T1 JOIN A T2 ON ...` | Same table, two aliases | Manager-Employee hierarchy |\n\n### 2.2 Nested Queries — Deep Breakdown\n\n**Types of Subqueries (High-Yield for KTU):**\n\n1. **Single-Row Subquery** — returns exactly **one row** and **one column**. Use with `=`, `>`, `<`, `>=`, `<=`, `<>`.\n   *Example:* Find employees earning the maximum salary.\n\n2. **Multi-Row Subquery** — returns **multiple rows**, but one column. Use with `IN`, `ANY`, `ALL`.\n   *Example:* Find employees working in departments located in 'Kochi'.\n\n3. **Multi-Column Subquery** — returns **multiple columns**. Use with `IN` and a tuple comparison.\n   *Example:* Find employees having the same (Job, DeptNo) as someone else.\n\n4. **Correlated Subquery** — the inner query references a column from the **outer query**. It executes **once per row** of the outer query.\n   *Example:* Find employees earning more than the average salary of *their own department*.\n\n5. **Subquery in FROM clause (Derived Table)** — the inner query acts as a virtual table.\n   *Example:* `SELECT * FROM (SELECT DeptNo, AVG(Salary) AS avg_sal FROM EMP GROUP BY DeptNo) AS t WHERE avg_sal > 50000;`\n\n> [!NOTE]\n> **Why correlated subqueries are slow:** Because the inner query re-executes for *every* row of the outer table, complexity can become $O(n^2)$. For large tables, KTU examiners often expect you to prefer a `JOIN` with a `GROUP BY` instead.\n\n### 2.3 Join Queries — Deep Breakdown\n\n**Logical Equivalence Cheat Sheet (KTU loves these!):**\n\n| SQL Construct | Equivalent Form | When to Use |\n|---------------|-----------------|-------------|\n| `WHERE A.x = B.x` (in multi-table `FROM`) | `INNER JOIN` | Old-style implicit join |\n| `LEFT JOIN + WHERE B.x IS NULL` | `NOT EXISTS` / `EXCEPT` | Anti-join: rows in A not matched in B |\n| `IN (SELECT ...)` | Semi-join | Membership test |\n| `EXISTS (SELECT ...)` | Semi-join with early exit | Existence check (often faster) |\n| Scalar subquery in `SELECT` | `LEFT JOIN` with grouping | Computed columns across tables |\n\n**Real-World Engineering Utility:**\n- **Set Operators** → Used in ETL pipelines, merging partitioned data, audit reporting.\n- **Nested Queries** → Used in dynamic dashboards (filter based on aggregated sub-results).\n- **Joins** → The backbone of any OLTP system — assembling a single screen (e.g., an order with customer, product, and shipping details) from normalized tables.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Worked Example — Set Operators (UNION, INTERSECT, EXCEPT)\n\n**Problem:** Find the names of employees who are in Department 10 **OR** Department 20.\n\n**Logical derivation:**\n\n$$\n\\text{Result} = \\{e.\\text{Ename} \\mid e.\\text{DeptNo} = 10\\} \\cup \\{e.\\text{Ename} \\mid e.\\text{DeptNo} = 20\\}\n$$\n\n**Step 1:** Identify the two sub-queries (the two circles of the Venn diagram).\n- Circle A: Employees in Dept 10.\n- Circle B: Employees in Dept 20.\n\n**Step 2:** Verify compatibility — both queries select 1 column (`Ename`), same data type (`VARCHAR`).\n\n**Step 3:** Apply the `UNION` operator.\n\n```sql\nSELECT Ename FROM EMPLOYEE WHERE DeptNo = 10\nUNION\nSELECT Ename FROM EMPLOYEE WHERE DeptNo = 20;\n```\n\n**Analytical Matrix — Behaviour of the three operators on identical input:**\n\n$$\n\\begin{aligned}\n\\text{Input A} &= \\{ \\text{Anu, Bala, Anu} \\} \\\\\n\\text{Input B} &= \\{ \\text{Bala, Chitra} \\} \\\\\n\\text{A UNION B} &= \\{ \\text{Anu, Bala, Chitra} \\} \\quad \\text{(Anu appears once)} \\\\\n\\text{A UNION ALL B} &= \\{ \\text{Anu, Bala, Anu, Bala, Chitra} \\} \\quad \\text{(5 rows)} \\\\\n\\text{A INTERSECT B} &= \\{ \\text{Bala} \\} \\\\\n\\text{A EXCEPT B} &= \\{ \\text{Anu} \\}\n\\end{aligned}\n$$\n\n> [!NOTE]\n> Observe that `EXCEPT` (also called `MINUS` in Oracle) is **not symmetric**: `A EXCEPT B` ≠ `B EXCEPT A`. Always check the order of operands.\n\n---\n\n### 3.2 Worked Example — Nested Subquery\n\n**Problem:** Find the names of employees who earn more than the average salary of the entire company.\n\n**Logical derivation:**\n\n$$\n\\text{Result} = \\{e.\\text{Ename} \\mid e.\\text{Salary} > \\bar{S}\\}, \\quad \\text{where} \\quad \\bar{S} = \\frac{1}{n}\\sum_{i=1}^{n} e_i.\\text{Salary}\n$$\n\n**Step 1 (Inner query — compute the scalar):** The average salary is a single number.\n\n```sql\nSELECT AVG(Salary) FROM EMPLOYEE;   -- returns one row, one column, e.g., 55000\n```\n\n**Step 2 (Outer query — apply the filter):** Treat the scalar as a constant.\n\n```sql\nSELECT Ename FROM EMPLOYEE\nWHERE Salary > (SELECT AVG(Salary) FROM EMPLOYEE);\n```\n\n**Step 3 (Execution flow):**\n\n$$\n\\begin{aligned}\n\\text{Phase 1 (Inner):} \\quad & \\bar{S} \\leftarrow 55000 \\\\\n\\text{Phase 2 (Outer):} \\quad & \\text{Filter rows where Salary} > 55000 \\\\\n& \\Rightarrow \\text{returns } \\{ \\text{Bala, Chitra, David} \\}\n\\end{aligned}\n$$\n\n---\n\n### 3.3 Worked Example — Correlated Subquery\n\n**Problem:** Find employees who earn more than the average salary of **their own** department.\n\n**Logical derivation:**\n\n$$\n\\text{Result} = \\{e.\\text{Ename} \\mid e.\\text{Salary} > \\text{AVG}(\\text{Salary in } e.\\text{DeptNo})\\}\n$$\n\n**Step 1:** Notice the inner query references `e.DeptNo` from the outer query — this is a *correlation*.\n\n```sql\nSELECT Ename, Salary, DeptNo\nFROM EMPLOYEE AS e1\nWHERE Salary > (\n    SELECT AVG(Salary)\n    FROM EMPLOYEE AS e2\n    WHERE e2.DeptNo = e1.DeptNo   -- <-- correlation link\n);\n```\n\n**Step 2 (Execution flow, per row):**\n\n$$\n\\begin{aligned}\n\\text{Outer row: } e_1 = (\\text{Anu, 60000, 10}) & \\Rightarrow \\text{inner computes AVG where DeptNo=10} \\rightarrow 55000 \\\\\n& \\Rightarrow 60000 > 55000 \\rightarrow \\text{include Anu} \\\\\n\\text{Outer row: } e_1 = (\\text{Eve, 40000, 10}) & \\Rightarrow 40000 > 55000 \\rightarrow \\text{exclude Eve}\n\\end{aligned}\n$$\n\n**Step 3 (Equivalent JOIN rewrite — faster for large tables):**\n\n```sql\nSELECT e1.Ename, e1.Salary\nFROM EMPLOYEE e1\nJOIN (\n    SELECT DeptNo, AVG(Salary) AS avg_sal\n    FROM EMPLOYEE\n    GROUP BY DeptNo\n) AS d ON e1.DeptNo = d.DeptNo\nWHERE e1.Salary > d.avg_sal;\n```\n\n---\n\n### 3.4 Worked Example — Joins (INNER, LEFT, RIGHT, FULL)\n\n**Problem:** Display employee name and their department name for all employees.\n\n**Step 1 — INNER JOIN (default, only matching rows):**\n\n```sql\nSELECT e.Ename, d.Dname\nFROM EMPLOYEE e\nINNER JOIN DEPARTMENT d ON e.DeptNo = d.DeptNo;\n```\n\n**Step 2 — LEFT JOIN (all employees, even if no department match):**\n\n```sql\nSELECT e.Ename, d.Dname\nFROM EMPLOYEE e\nLEFT JOIN DEPARTMENT d ON e.DeptNo = d.DeptNo;\n```\n\n**Step 3 — RIGHT JOIN (all departments, even if no employee):**\n\n```sql\nSELECT e.Ename, d.Dname\nFROM EMPLOYEE e\nRIGHT JOIN DEPARTMENT d ON e.DeptNo = d.DeptNo;\n```\n\n**Step 4 — FULL OUTER JOIN (all employees AND all departments):**\n\n```sql\nSELECT e.Ename, d.Dname\nFROM EMPLOYEE e\nFULL OUTER JOIN DEPARTMENT d ON e.DeptNo = d.DeptNo;\n```\n\n**Set-Theoretic Venn Equivalence for Joins:**\n\n$$\n\\begin{aligned}\n\\text{INNER JOIN} &= A \\cap B \\\\\n\\text{LEFT JOIN} &= A \\cup (A \\cap B) \\\\\n\\text{RIGHT JOIN} &= B \\cup (A \\cap B) \\\\\n\\text{FULL OUTER JOIN} &= A \\cup B \\\\\n\\text{LEFT JOIN WHERE B.x IS NULL} &= A - B\n\\end{aligned}\n$$\n\n---\n\n### 3.5 Analytical Matrix — Operator Selection Guide\n\n| Requirement | Best Tool | Reason |\n|-------------|-----------|--------|\n| Combine two filter lists, dedup needed | `UNION` | Set semantics |\n| Combine two filter lists, keep all rows | `UNION ALL` | Faster (no dedup) |\n| Find rows common to two conditions | `INTERSECT` | Direct set intersection |\n| Find rows in A but not in B | `EXCEPT` | Set difference |\n| Filter based on a computed aggregate | Scalar subquery | Returns single value |\n| Filter based on a dynamic list | `IN` subquery | Returns multi-row, single column |\n| Show one-to-many relationship | `INNER JOIN` | Combines matching rows |\n| Find orphans (unmatched rows) | `LEFT JOIN ... IS NULL` | Preserves left, NULL on right |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Mermaid Diagram — Set Operator Behaviour (Venn-Style Flow)\n\n```mermaid\ngraph LR\n    Q1[\"Query A Result Set\"]:::setA\n    Q2[\"Query B Result Set\"]:::setB\n    UN[\"UNION: A U B (Unique)\"]:::op\n    UA[\"UNION ALL: A U B (All Rows)\"]:::op\n    IN[\"INTERSECT: A ∩ B\"]:::op\n    EX[\"EXCEPT: A - B\"]:::op\n    Q1 --> UN\n    Q2 --> UN\n    Q1 --> UA\n    Q2 --> UA\n    Q1 --> IN\n    Q2 --> IN\n    Q1 --> EX\n    Q2 --> EX\n    classDef setA fill:#cce5ff,stroke:#0050b3,color:#000\n    classDef setB fill:#ffd6cc,stroke:#cc3300,color:#000\n    classDef op fill:#d4f4dd,stroke:#1d8348,color:#000\n```\n\n### 4.2 Mermaid Diagram — Subquery Execution Flow (Correlated)\n\n```mermaid\ngraph TD\n    A[Outer Query Reads Row 1 of EMPLOYEE] --> B{Is Salary > AVG of own Dept?}\n    B -- No --> C[Skip Row]\n    B -- Yes --> D[Include Row in Result]\n    D --> E[Outer Query Reads Row 2]\n    C --> E\n    E --> F{Is Salary > AVG of own Dept?}\n    F -- No --> C\n    F -- Yes --> D\n    style A fill:#fff3cd,stroke:#856404\n    style D fill:#d4edda,stroke:#155724\n    style C fill:#f8d7da,stroke:#721c24\n```\n\n### 4.3 Mermaid Diagram — Join Types Visual Block Architecture\n\n```mermaid\ngraph TB\n    subgraph IN[\"INNER JOIN: A ∩ B\"]\n        I1[Matching Rows Only]\n    end\n    subgraph LJ[\"LEFT JOIN: A plus matches\"]\n        L1[All of A]\n        L2[Matched B values]\n    end\n    subgraph RJ[\"RIGHT JOIN: B plus matches\"]\n        R1[All of B]\n        R2[Matched A values]\n    end\n    subgraph FJ[\"FULL OUTER JOIN: A U B\"]\n        F1[All of A]\n        F2[All of B]\n        F3[Matched Center]\n    end\n    style I1 fill:#b3e5fc,stroke:#01579b\n    style L1 fill:#c8e6c9,stroke:#1b5e20\n    style L2 fill:#c8e6c9,stroke:#1b5e20\n    style R1 fill:#ffe0b2,stroke:#e65100\n    style R2 fill:#ffe0b2,stroke:#e65100\n    style F1 fill:#f8bbd0,stroke:#880e4f\n    style F2 fill:#f8bbd0,stroke:#880e4f\n    style F3 fill:#f8bbd0,stroke:#880e4f\n```\n\n### 4.4 Mermaid Diagram — Nested Subquery Resolution Order\n\n```mermaid\nflowchart TD\n    S1[Step 1: Parse Outer SELECT] --> S2[Step 2: Detect Subquery in WHERE]\n    S2 --> S3[Step 3: Execute Inner SELECT first]\n    S3 --> S4[Step 4: Collect Subquery Result Set]\n    S4 --> S5{Queries Correlated?}\n    S5 -- No --> S6[Step 5: Use Result Once, Finish Outer]\n    S5 -- Yes --> S7[Step 5: Re-execute Inner per Outer Row]\n    S7 --> S8[Step 6: Compare and Emit Final Result]\n    S6 --> S8\n    style S1 fill:#e1f5fe\n    style S3 fill:#fff9c4\n    style S5 fill:#ffe0b2\n    style S8 fill:#c8e6c9\n```\n\n> [!NOTE]\n> **Reading tip for KTU viva:** Always narrate the diagram in this order — *Parse → Identify → Execute inner → Use result in outer → Emit*. Examiners reward clear mental models of execution.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### Part A — Short Answer Questions (3 Marks Each)\n\n---\n\n**Q1. [KTU University Exam - Dec 2023]**\n*List the SQL set operators. State the two conditions that must be satisfied to combine two SELECT statements using a set operator.*\n\n**Model Answer (3 marks):**\n\nThe SQL set operators are: **UNION**, **UNION ALL**, **INTERSECT**, and **EXCEPT** (also called MINUS in Oracle).\n\nThe two mandatory conditions for combining two SELECT statements using a set operator are:\n\n1. Both SELECT statements must have the **same number of columns** in their select list. *(1 mark)*\n2. The corresponding columns (by position) must have **compatible data types** (i.e., the data type families must be the same or implicitly convertible, such as INT and DECIMAL). *(1 mark)*\n\nAdditionally, the order of rows in the output is implementation-defined, so for ordered output, an `ORDER BY` should be applied to the **final** combined query. *(1 mark)*\n\n---\n\n**Q2. [KTU University Exam - June 2024]**\n*What is a correlated subquery? How does it differ from a non-correlated subquery?*\n\n**Model Answer (3 marks):**\n\nA **correlated subquery** is a subquery that references one or more columns from the outer query. Because of this reference, the inner query **cannot be evaluated independently**; it executes **once for every row processed by the outer query**, with the correlation values substituted from the current outer row. *(2 marks)*\n\nA **non-correlated (simple) subquery** is independent of the outer query. It executes **exactly once**, returns its result, and the outer query then uses that result. It does not reference any outer column. *(1 mark)*\n\n**Example:**\n```sql\n-- Correlated\nSELECT Ename FROM EMPLOYEE e1\nWHERE Salary > (SELECT AVG(Salary) FROM EMPLOYEE e2 WHERE e2.DeptNo = e1.DeptNo);\n\n-- Non-correlated\nSELECT Ename FROM EMPLOYEE\nWHERE Salary > (SELECT AVG(Salary) FROM EMPLOYEE);\n```\n\n---\n\n### Part B — Long Answer Questions (14 Marks Each)\n\n> [!IMPORTANT]\n> As per the KTU 2024 Scheme, Part B questions feature an **internal choice** between two alternatives. Both options are provided below.\n\n---\n\n### Question A (14 Marks)\n\n**[KTU University Exam - Model Paper 2024]**\n**(a)** Consider the schema given in your lab manual (DEPARTMENT, EMPLOYEE, PROJECT, ASSIGNED_TO). Write SQL queries using **set operators** to solve the following: *(7 marks)*\n1. List the names of employees who work on project 'P101' **or** project 'P102'.\n2. List the names of employees who work on **both** 'P101' and 'P102'.\n3. List the names of employees who work on 'P101' **but not on** 'P102'.\n\n**(b)** Demonstrate the use of **nested queries (subqueries)** to:\n1. Find the name of the employee with the **highest salary**.\n2. Find the names of employees whose salary is greater than the salary of **all** employees in Department 10.\n3. Find employees who have the **same job** as at least one employee in Department 20. *(7 marks)*\n\n---\n\n#### Model Solution A(a) — Set Operators *(7 marks)*\n\n**1. UNION — work on P101 OR P102:**\n\n```sql\nSELECT e.Ename\nFROM EMPLOYEE e JOIN ASSIGNED_TO a ON e.Eno = a.Eno\nWHERE a.Pno = 'P101'\nUNION\nSELECT e.Ename\nFROM EMPLOYEE e JOIN ASSIGNED_TO a ON e.Eno = a.Eno\nWHERE a.Pno = 'P102';\n```\n\n**2. INTERSECT — work on BOTH P101 and P102:**\n\n```sql\nSELECT e.Ename\nFROM EMPLOYEE e JOIN ASSIGNED_TO a ON e.Eno = a.Eno\nWHERE a.Pno = 'P101'\nINTERSECT\nSELECT e.Ename\nFROM EMPLOYEE e JOIN ASSIGNED_TO a ON e.Eno = a.Eno\nWHERE a.Pno = 'P102';\n```\n\n**3. EXCEPT — work on P101 but NOT P102:**\n\n```sql\nSELECT e.Ename\nFROM EMPLOYEE e JOIN ASSIGNED_TO a ON e.Eno = a.Eno\nWHERE a.Pno = 'P101'\nEXCEPT\nSELECT e.Ename\nFROM EMPLOYEE e JOIN ASSIGNED_TO a ON e.Eno = a.Eno\nWHERE a.Pno = 'P102';\n```\n\n> [!NOTE]\n> *Mark distribution: 1.5 marks per sub-question (1 for correct operator, 0.5 for valid surrounding query).*\n\n---\n\n#### Model Solution A(b) — Nested Queries *(7 marks)*\n\n**1. Employee with the highest salary (scalar subquery):**\n\n```sql\nSELECT Ename\nFROM EMPLOYEE\nWHERE Salary = (SELECT MAX(Salary) FROM EMPLOYEE);\n```\n\nThe inner subquery `(SELECT MAX(Salary) FROM EMPLOYEE)` returns a single scalar value, which the outer query then uses for comparison. *(2 marks)*\n\n**2. Salary greater than ALL employees in Department 10 (multi-row subquery with `ALL`):**\n\n```sql\nSELECT Ename, Salary\nFROM EMPLOYEE\nWHERE Salary > ALL (SELECT Salary FROM EMPLOYEE WHERE DeptNo = 10);\n```\n\nThe `ALL` keyword ensures the salary must be greater than **every** salary in Department 10, i.e., greater than the maximum of that set. *(2.5 marks)*\n\n**3. Same job as at least one employee in Department 20 (multi-row subquery with `IN`):**\n\n```sql\nSELECT Ename, Job\nFROM EMPLOYEE\nWHERE Job IN (SELECT Job FROM EMPLOYEE WHERE DeptNo = 20);\n```\n\nThe subquery returns a list of jobs held in Dept 20, and `IN` tests membership of the outer employee in that list. *(2.5 marks)*\n\n---\n\n### Question B (14 Marks) — Alternative Choice\n\n**[KTU University Exam - Model Paper 2024]**\n**(a)** With reference to the lab schema, write SQL queries using **joins** for the following: *(7 marks)*\n1. Display the employee name, department name, and project name for all employees working on some project.\n2. Display employees who are **not assigned to any project** (use a `LEFT JOIN` and `NULL` test).\n3. Display the manager name for each employee using a **self join**.\n\n**(b)** Solve the following using **correlated subqueries**: *(7 marks)*\n1. Find employees who earn more than the **average salary of their own department**.\n2. Find projects that have **more employees assigned** than project 'P101'.\n3. Find departments that have **no employees** at all.\n\n---\n\n#### Model Solution B(a) — Joins *(7 marks)*\n\n**1. Employee name, department name, project name (INNER JOIN with three tables):**\n\n```sql\nSELECT e.Ename, d.Dname, p.Pname\nFROM EMPLOYEE e\nINNER JOIN DEPARTMENT d ON e.DeptNo = d.DeptNo\nINNER JOIN ASSIGNED_TO a ON e.Eno = a.Eno\nINNER JOIN PROJECT p ON a.Pno = p.Pno;\n```\n\nThe `INNER JOIN` chain matches only employees who are both in a department and have a project assignment. *(2.5 marks)*\n\n**2. Employees NOT assigned to any project (LEFT JOIN anti-join pattern):**\n\n```sql\nSELECT e.Ename\nFROM EMPLOYEE e\nLEFT JOIN ASSIGNED_TO a ON e.Eno = a.Eno\nWHERE a.Pno IS NULL;\n```\n\n`LEFT JOIN` preserves every employee; if there is no matching row in `ASSIGNED_TO`, the right-side columns are `NULL`. Filtering on `IS NULL` keeps only unmatched employees. *(2.5 marks)*\n\n**3. Manager name for each employee (SELF JOIN):**\n\n```sql\nSELECT e.Ename AS Employee, m.Ename AS Manager\nFROM EMPLOYEE e\nLEFT JOIN EMPLOYEE m ON e.MgrNo = m.Eno;\n```\n\nA self join treats the `EMPLOYEE` table as two virtual tables: `e` (the employee) and `m` (the manager). `LEFT JOIN` ensures employees without managers (top-level) still appear with `NULL` manager. *(2 marks)*\n\n---\n\n#### Model Solution B(b) — Correlated Subqueries *(7 marks)*\n\n**1. Employees earning more than the average of their own department:**\n\n```sql\nSELECT Ename, Salary, DeptNo\nFROM EMPLOYEE e1\nWHERE Salary > (\n    SELECT AVG(Salary)\n    FROM EMPLOYEE e2\n    WHERE e2.DeptNo = e1.DeptNo\n);\n```\n\nThe `e2.DeptNo = e1.DeptNo` creates the correlation link — the inner query computes a different average for each outer row's department. *(2.5 marks)*\n\n**2. Projects with more employees assigned than project P101:**\n\n```sql\nSELECT Pno\nFROM ASSIGNED_TO a1\nGROUP BY Pno\nHAVING COUNT(DISTINCT Eno) > (\n    SELECT COUNT(DISTINCT Eno)\n    FROM ASSIGNED_TO\n    WHERE Pno = 'P101'\n);\n```\n\nThe inner subquery returns the count for P101 (a single scalar), and the outer `HAVING` clause filters grouped projects whose count exceeds that scalar. *(2.5 marks)*\n\n**3. Departments with no employees (using `NOT EXISTS` correlated subquery):**\n\n```sql\nSELECT Dname\nFROM DEPARTMENT d\nWHERE NOT EXISTS (\n    SELECT 1\n    FROM EMPLOYEE e\n    WHERE e.DeptNo = d.DeptNo\n);\n```\n\n`NOT EXISTS` returns TRUE only if the correlated inner query yields **no rows**, meaning no employee references that department — i.e., the department is empty. *(2 marks)*\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> 1. **Column-count mismatch in set operators** — Students often `UNION` a 2-column query with a 3-column query, causing a runtime error. Always count select-list items.\n> 2. **Confusing `UNION` with `JOIN`** — `UNION` stacks rows vertically (one above the other); `JOIN` places them side by side horizontally.\n> 3. **Forgetting to alias tables in self joins** — A self join without aliases (`EMPLOYEE e JOIN EMPLOYEE m`) causes an ambiguous column error.\n> 4. **Using `=` with a multi-row subquery** — `WHERE col = (SELECT ...)` fails if the subquery returns more than one row. Use `IN` instead.\n> 5. **Misordering `EXCEPT`** — `A EXCEPT B` ≠ `B EXCEPT A`. Read carefully which result set should be \"left\" and which should be \"subtracted\".\n> 6. **Omitting the join condition** — A `CROSS JOIN` is the default if `ON` is missing, producing a Cartesian product explosion — a classic deduction of marks.\n\n---\n\n### Topic Recap & Important Things to Remember\n\n- **Set Operators:** `UNION` (dedup), `UNION ALL` (keep all), `INTERSECT` (common), `EXCEPT` (subtract). Always verify **same column count** and **compatible data types**.\n- **Order of columns matters** in set operations — matching is positional, not by name.\n- **`INTERSECT` and `EXCEPT` are not supported in MySQL** — KTU lab may use Oracle/PostgreSQL where these are available.\n- **Subqueries** can appear in `SELECT`, `FROM`, `WHERE`, and `HAVING` clauses.\n- **Scalar subquery** → use with `=`, `<`, `>`. **Multi-row subquery** → use with `IN`, `ANY`, `ALL`. **Correlated subquery** → inner references outer.\n- **Correlated subqueries execute row-by-row** — they are powerful but expensive. Prefer `JOIN` + `GROUP BY` rewrites for large datasets.\n- **Joins are the primary cross-table operator** — `INNER`, `LEFT`, `RIGHT`, `FULL OUTER`, `CROSS`, `SELF`.\n- **`LEFT JOIN ... WHERE right.col IS NULL`** is the canonical anti-join pattern (set difference implemented via joins).\n- **Self joins require table aliases** to disambiguate the two logical copies of the same table.\n- **Always use meaningful aliases** (e.g., `e` for EMPLOYEE, `d` for DEPARTMENT) — KTU evaluators deduct marks for cryptic single letters when clarity is expected in long queries.\n- **Execution order in SQL:** `FROM → WHERE → GROUP BY → HAVING → SELECT → DISTINCT → ORDER BY → LIMIT`. Remember that subqueries in the `FROM` clause (derived tables) are executed first as virtual tables.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pccsl408-mExperiment 2-t7",
          "title": "Practice of SQL TCL and DCL commands like Rollback, Commit, Savepoint, and granting/revoking user privileges",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL408 - DBMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Practice of SQL TCL and DCL commands like Rollback, Commit, Savepoint, and granting/revoking user privileges\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n**Transaction Control Language (TCL)** is a sub-language of SQL that manages the changes made by DML statements (INSERT, UPDATE, DELETE) within logical units of work called **transactions**. The primary TCL commands as per the KTU 2024 DBMS Lab syllabus are:\n\n- `COMMIT` — permanently saves all changes of the current transaction to the database.\n- `ROLLBACK` — undoes all changes of the current transaction, restoring the database to its last committed state.\n- `SAVEPOINT` — defines intermediate markers within a transaction to which one can later roll back partially, without aborting the entire transaction.\n\n**Data Control Language (DCL)** is the SQL sub-language used to control **access rights** and **permissions** on database objects (tables, views, sequences, etc.) for different database users. The two principal DCL statements are:\n\n- `GRANT` — gives a user specific privileges on database objects.\n- `REVOKE` — withdraws previously granted privileges from a user.\n\n> [!IMPORTANT]\n> **KTU 2024 Board Definition to Memorise:**\n> A **Transaction** is a logical unit of work that must satisfy the ACID properties — **A**tomicity, **C**onsistency, **I**solation, and **D**urability. TCL commands are the explicit mechanisms by which a programmer enforces these properties.\n\n## 1.2 Conceptual Analogy / Intuition\n\n### Analogy 1 — TCL as an \"Undo & Redo Stack in MS Word\"\n\nImagine you are typing a long Word document. While writing, Word keeps track of every keystroke in a temporary buffer:\n\n- **`SAVE`** in Word ≈ **`COMMIT`** in SQL → All the typing becomes permanent in the file.\n- **`Ctrl + Z` (Undo all the way back)** ≈ **`ROLLBACK`** in SQL → Discard every unsaved change and revert to the last saved state.\n- **`Bookmark`** inserted in between your work ≈ **`SAVEPOINT`** in SQL → A point in the middle that lets you undo back to *just there*, while keeping everything before the bookmark intact.\n\n### Analogy 2 — DCL as a \"Hotel Room Key Card System\"\n\nThink of your database tables as a hotel, and each user as a guest:\n\n- The **front desk (DBA)** hands you a **key card** that opens only specific rooms → this is a **`GRANT`**.\n- When you check out, the front desk **deactivates** your card, and you can no longer open any room → this is a **`REVOKE`**.\n- The card may allow entry to Room 101 (SELECT on Table A) but *not* to Room 202 (INSERT on Table B). This is **privilege granularity**.\n\n> [!NOTE]\n> **Why both sub-languages exist:**\n> In a real engineering project (e.g., an ATM backend, a railway reservation system, a hospital records portal), the data is shared by hundreds of users. TCL guarantees *what they write is consistent*; DCL guarantees *they are only allowed to do what their job demands*. Together, they form the **security and reliability backbone** of any multi-user DBMS.\n\n## 1.3 Key Standard Metrics / Constants (in Bold)\n\n- **ACID Properties:** **A**tomicity, **C**onsistency, **I**solation, **D**urability — the **four non-negotiable guarantees** of a database transaction.\n- **Default Commit Mode in Oracle:** **AUTOCOMMIT is OFF** by default. In MySQL, **AUTOCOMMIT is ON** by default unless `SET autocommit=0;` is executed.\n- **SAVEPOINT Limit (Oracle):** A session can have up to **5 active savepoints** in the same transaction (older ones are reused).\n- **SQL Standard Privilege Forms:** `SELECT`, `INSERT`, `UPDATE`, `DELETE`, `REFERENCES`, `ALTER`, `INDEX`, `ALL` — collectively known as **Privileges on a Table** (8 standard forms).\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Transaction Lifecycle (Why TCL Exists)\n\nA database transaction moves through well-defined states. KTU frequently asks for a labelled diagram of this. The states are:\n\n| State | Meaning | Triggered By |\n|---|---|---|\n| **Active** | Transaction is executing its first statement | `INSERT` / `UPDATE` / `DELETE` |\n| **Partially Committed** | Final statement has been executed but changes are still in buffer | Last DML statement finishes |\n| **Committed** | All changes written permanently to disk | `COMMIT` succeeds |\n| **Failed** | Normal execution can no longer proceed | Error, constraint violation, deadlock |\n| **Aborted** | Database has been rolled back to prior state | `ROLLBACK` finishes |\n\n> [!NOTE]\n> **The \"Why\" behind each state:**\n> - *Why is there a separate \"Partially Committed\" state?* Because writes go first to a **log buffer** for crash recovery. Only when the log is flushed to disk does the transaction reach the \"Committed\" state. This is the **Write-Ahead Log (WAL)** rule.\n\n## 2.2 TCL Commands — Syntax, Semantics, and Order of Operation\n\n### 2.2.1 `COMMIT`\n\n**Syntax (Oracle / MySQL / PostgreSQL):**\n\n```sql\nCOMMIT;\n-- Or, in some DBs:\nCOMMIT WORK;\nCOMMIT TRANSACTION;\n```\n\n**Semantics:**\n- All changes done by the current transaction become **permanent** and visible to other concurrent users.\n- All `SAVEPOINT`s within the transaction are released.\n- **Row-level locks** held by this transaction are released.\n\n### 2.2.2 `ROLLBACK`\n\n**Syntax:**\n\n```sql\nROLLBACK;\n-- Or, with a savepoint name (partial rollback):\nROLLBACK TO SAVEPOINT sp1;\n```\n\n**Semantics:**\n- Undoes changes made by the current transaction.\n- On a `ROLLBACK TO SAVEPOINT sp1`, only statements executed **after** `sp1` are undone.\n- All savepoints established **after** the named savepoint are erased.\n\n### 2.2.3 `SAVEPOINT`\n\n**Syntax:**\n\n```sql\nSAVEPOINT sp1;\nSAVEPOINT sp_update;\n```\n\n**Semantics:**\n- Marks a point in a transaction for partial rollback.\n- Does **not** commit anything; it is purely a navigational marker.\n\n> [!IMPORTANT]\n> **KTU Board Pitfall:** A `SAVEPOINT` is *not* a `COMMIT`. The data is *not* visible to other users until the surrounding transaction is committed. Students frequently lose marks by treating savepoints as \"mini-commits\".\n\n## 2.3 DCL Commands — Syntax, Semantics, and Privilege Model\n\n### 2.3.1 `GRANT`\n\n**Syntax:**\n\n```sql\nGRANT <privilege_list>\nON <object_name>\nTO <user_or_role>\n[WITH GRANT OPTION];\n```\n\nWhere:\n- `<privilege_list>` = one or more of: `SELECT`, `INSERT`, `UPDATE`, `DELETE`, `REFERENCES`, `ALTER`, `INDEX`, `ALL`.\n- `<object_name>` = a table, view, sequence, etc.\n- `WITH GRANT OPTION` allows the grantee to further grant those same privileges to others.\n\n### 2.3.2 `REVOKE`\n\n**Syntax:**\n\n```sql\nREVOKE <privilege_list>\nON <object_name>\nFROM <user_or_role>\n[CASCADE CONSTRAINTS];\n```\n\n- `CASCADE` option in Oracle revokes any dependent privileges.\n- In MySQL, `REVOKE ... FROM user;` is sufficient.\n\n### 2.3.3 The Two-Tier Privilege Model in Oracle\n\n| Tier | Statement Type | Example |\n|---|---|---|\n| **System Privileges** | Rights to perform actions on the DB engine itself | `CREATE TABLE`, `CREATE USER`, `CREATE SESSION` |\n| **Object Privileges** | Rights to perform actions on a specific object | `SELECT ON emp`, `UPDATE ON dept` |\n\n## 2.4 KTU Formula Sheet / Cheat Sheet\n\n| # | Concept | Equation / Syntax Rule | Real-World Engineering Use |\n|---|---|---|---|\n| 1 | **Transaction Boundary** | Begins at first DML (or at `COMMIT`/`ROLLBACK` of previous tx); Ends at `COMMIT` / `ROLLBACK` / DDL statement | Banking ATM — entire withdrawal is one atomic transaction |\n| 2 | **ACID Check** | $\\text{ACID} = \\{\\text{Atomicity},\\ \\text{Consistency},\\ \\text{Isolation},\\ \\text{Durability}\\}$ | Stock trading systems — must be both atomic and durable |\n| 3 | **Savepoint Stack Rule** | After `ROLLBACK TO sp_n`, all `sp_k` where `k > n` are erased | Long ETL jobs — checkpoint and resume |\n| 4 | **DDL Implicit Commit** | Any DDL (`CREATE`, `ALTER`, `DROP`, `TRUNCATE`, `RENAME`) **forces** an automatic commit | DDLs end the current transaction silently |\n| 5 | **Autocommit Default** | Oracle: **OFF**; MySQL: **ON**; PostgreSQL: **ON** | Lab exam setup — verify with `SHOW AUTOCOMMIT;` |\n| 6 | **Privilege Grant Cascade** | A grants B WITH GRANT OPTION; B grants C; A revokes B → C is also revoked (in Oracle CASCADE) | Hospital admin chain |\n| 7 | **Object Privilege List** | $\\{ \\text{SELECT}, \\text{INSERT}, \\text{UPDATE}, \\text{DELETE}, \\text{REFERENCES}, \\text{ALTER}, \\text{INDEX}, \\text{ALL} \\}$ | All count = **8** standard object privileges |\n| 8 | **System Privilege List (key ones)** | $\\text{CREATE USER},\\ \\text{DROP USER},\\ \\text{CREATE SESSION},\\ \\text{CREATE TABLE},\\ \\text{GRANT ANY PRIVILEGE}$ | DBA-only actions |\n\n## 2.5 Real-World Utility in Engineering\n\n- **Banking & FinTech:** TCL implements atomicity — a money transfer either debits *and* credits or does nothing.\n- **E-Commerce:** TCL prevents overselling — the order placement and stock decrement are one transaction.\n- **Healthcare EMRs:** DCL enforces *role-based access* — a nurse has SELECT on patient records but cannot DELETE.\n- **IoT Telemetry:** TCL with savepoints enables checkpointing during long batch writes.\n- **Defence/Military Systems:** DCL `WITH GRANT OPTION` chains are used to delegate authority down a command hierarchy.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\nThis section provides a **fully worked-out, exhaustive walkthrough** of every experiment, in the form KTU evaluators expect for the lab record.\n\n## 3.1 Reference Schema (Use this for all experiments)\n\nWe use the standard `EMP` and `DEPT` schema. **Commit it to memory** — KTU questions are typically posed on this exact schema.\n\n```sql\nCREATE TABLE DEPT (\n    DEPTNO  NUMBER(2) PRIMARY KEY,\n    DNAME   VARCHAR2(14),\n    LOC     VARCHAR2(13)\n);\n\nCREATE TABLE EMP (\n    EMPNO   NUMBER(4) PRIMARY KEY,\n    ENAME   VARCHAR2(10),\n    JOB     VARCHAR2(9),\n    MGR     NUMBER(4),\n    HIREDATE DATE,\n    SAL     NUMBER(7,2),\n    COMM    NUMBER(7,2),\n    DEPTNO  NUMBER(2) REFERENCES DEPT(DEPTNO)\n);\n```\n\nSample data (insert before starting TCL experiments):\n\n```sql\nINSERT INTO DEPT VALUES (10, 'ACCOUNTING', 'NEW YORK');\nINSERT INTO DEPT VALUES (20, 'RESEARCH',   'DALLAS');\nINSERT INTO DEPT VALUES (30, 'SALES',      'CHICAGO');\nINSERT INTO DEPT VALUES (40, 'OPERATIONS', 'BOSTON');\n\nINSERT INTO EMP VALUES\n(7369, 'SMITH',  'CLERK',     7902, DATE '1980-12-17',  800, NULL, 20);\nINSERT INTO EMP VALUES\n(7499, 'ALLEN',  'SALESMAN',  7698, DATE '1981-02-20', 1600,  300, 30);\nINSERT INTO EMP VALUES\n(7521, 'WARD',   'SALESMAN',  7698, DATE '1981-02-22', 1250,  500, 30);\nCOMMIT;\n```\n\n## 3.2 Experiment 3.2-A — Demonstration of `COMMIT` and `ROLLBACK`\n\n### Step 1: View Original Data\n\n```sql\nSELECT EMPNO, ENAME, SAL FROM EMP WHERE EMPNO = 7369;\n```\n\n**Expected Output:**\n\n| EMPNO | ENAME | SAL |\n|---|---|---|\n| 7369 | SMITH | 800.00 |\n\n### Step 2: Update a Row (Uncommitted State)\n\n```sql\nUPDATE EMP\nSET SAL = 900\nWHERE EMPNO = 7369;\n\nSELECT EMPNO, ENAME, SAL FROM EMP WHERE EMPNO = 7369;\n```\n\n**Expected Output (this is the *current session* view):**\n\n| EMPNO | ENAME | SAL |\n|---|---|---|\n| 7369 | SMITH | 900.00 |\n\n> **Conversion Logic:** The `UPDATE` statement modifies the row **in the transaction's private buffer**. The change is **not** yet on disk. This is the **Active → Partially Committed** state.\n\n### Step 3: Open a Second Session (Pseudocode)\n\nIn SQL\\*Plus / SQL Developer, open a **new connection** as a different user. From that session, query:\n\n```sql\nSELECT SAL FROM EMP WHERE EMPNO = 7369;\n```\n\n**Expected Output (this is the *other session* view):**\n\n| SAL |\n|---|\n| 800.00 |\n\n> **Conversion Logic:** This proves the **Isolation** property: an uncommitted change in Session A is **invisible** to Session B. Until `COMMIT` is issued, the world sees the old salary of 800.\n\n### Step 4: Roll Back the Update\n\n```sql\nROLLBACK;\nSELECT SAL FROM EMP WHERE EMPNO = 7369;\n```\n\n**Expected Output:**\n\n| SAL |\n|---|\n| 800.00 |\n\n> **Conversion Logic:** `ROLLBACK` restores the data to the state at the **last commit point**. The 900 update is permanently discarded from the buffer.\n\n### Step 5: Update Again and Commit\n\n```sql\nUPDATE EMP\nSET SAL = 950\nWHERE EMPNO = 7369;\n\nCOMMIT;\n\nSELECT SAL FROM EMP WHERE EMPNO = 7369;\n```\n\n**Expected Output (visible to BOTH sessions now):**\n\n| SAL |\n|---|\n| 950.00 |\n\n> **Conversion Logic:** `COMMIT` flushes the buffer to disk and releases all row-level locks held by the transaction. The new value of 950 is now **durable**.\n\n### Step 6: Verify Persistence After Disconnect\n\nDisconnect and reconnect. Run:\n\n```sql\nSELECT SAL FROM EMP WHERE EMPNO = 7369;\n```\n\n**Expected Output:** 950.00 (the change **survived** the session — this is the **Durability** property).\n\n## 3.3 Experiment 3.3-B — Demonstration of `SAVEPOINT` and Partial `ROLLBACK`\n\n### Step 1: Begin a Transaction with Multiple Changes\n\n```sql\nINSERT INTO EMP VALUES\n(7876, 'ADAMS',  'CLERK',     7788, DATE '1983-01-12', 1100, NULL, 20);\n\nUPDATE EMP\nSET SAL = 1500\nWHERE EMPNO = 7499;\n\nSELECT EMPNO, ENAME, SAL FROM EMP\nWHERE EMPNO IN (7876, 7499);\n```\n\n**Expected Output:**\n\n| EMPNO | ENAME | SAL |\n|---|---|---|\n| 7876 | ADAMS | 1100.00 |\n| 7499 | ALLEN | 1500.00 |\n\n### Step 2: Create a SAVEPOINT\n\n```sql\nSAVEPOINT sp_after_insert;\n```\n\n> **Conversion Logic:** A `SAVEPOINT` is a **named marker** placed in the transaction's log. The log now records: `INSERT(ADAMS)` → `UPDATE(ALLEN)` → `SAVEPOINT sp_after_insert`.\n\n### Step 3: Make a Mistake We Want to Undo\n\n```sql\nUPDATE EMP\nSET SAL = 9999\nWHERE EMPNO = 7521;   -- wrong row!\n\nSELECT EMPNO, ENAME, SAL FROM EMP WHERE EMPNO = 7521;\n```\n\n**Expected Output:**\n\n| EMPNO | ENAME | SAL |\n|---|---|---|\n| 7521 | WARD | 9999.00 |\n\n### Step 4: Roll Back to the Savepoint (Partial Undo)\n\n```sql\nROLLBACK TO SAVEPOINT sp_after_insert;\n```\n\n**Expected Output of:**\n\n```sql\nSELECT EMPNO, ENAME, SAL FROM EMP\nWHERE EMPNO IN (7876, 7499, 7521);\n```\n\n| EMPNO | ENAME | SAL |\n|---|---|---|\n| 7876 | ADAMS | 1100.00 | (still present — was before savepoint) |\n| 7499 | ALLEN | 1500.00 | (still present — was before savepoint) |\n| 7521 | WARD  | 1250.00 | (reverted — was after savepoint) |\n\n> **Conversion Logic:** `ROLLBACK TO sp_after_insert` erases only the changes made **after** that savepoint. The INSERT of ADAMS and the UPDATE of ALLEN's salary remain.\n\n### Step 5: Final Commit\n\n```sql\nCOMMIT;\n```\n\n> **Conversion Logic:** Now everything is permanently saved. WARD's salary is back to 1250 (because we rolled back his wrong update), but ADAMS and ALLEN are saved.\n\n## 3.4 Experiment 3.4-C — Demonstration of Implicit COMMIT by DDL\n\n```sql\nINSERT INTO EMP VALUES\n(7900, 'JAMES', 'CLERK', 7698, DATE '1981-12-03', 950, NULL, 30);\n\nCREATE INDEX idx_emp_deptno ON EMP(DEPTNO);   -- DDL statement\n\nROLLBACK;\n```\n\n**Expected Result:** The INSERT **is NOT rolled back**. Why? Because `CREATE INDEX` is a DDL statement, and **Oracle implicitly commits the transaction** when DDL is issued. The 7900 row stays.\n\n> **Conversion Logic:** The DDL's implicit commit is a **silent transaction terminator**. This is one of the most common KTU exam traps.\n\n## 3.5 Experiment 3.5-D — DCL: GRANT and REVOKE on a Table\n\n### Step 1: Connect as DBA\n\n```sql\n-- (In Oracle, connect as SYSTEM or a user with CREATE USER privilege)\nCREATE USER alice IDENTIFIED BY alice123;\nCREATE USER bob   IDENTIFIED BY bob123;\n\nGRANT CREATE SESSION TO alice;\nGRANT CREATE SESSION TO bob;\n```\n\n### Step 2: Connect as the Owner of the Table (say, `scott`)\n\n```sql\n-- As scott, give alice SELECT and INSERT on the EMP table\nGRANT SELECT, INSERT ON scott.emp TO alice;\n```\n\n### Step 3: Connect as Alice and Verify\n\n```sql\n-- As alice\nSELECT * FROM scott.emp WHERE DEPTNO = 20;     -- works\nINSERT INTO scott.emp (EMPNO, ENAME, JOB, HIREDATE, SAL, DEPTNO)\nVALUES (9001, 'ALICE_USER', 'CLERK', SYSDATE, 2000, 20);  -- works\n```\n\n### Step 4: Verify What Alice CANNOT Do\n\n```sql\n-- As alice\nDELETE FROM scott.emp WHERE EMPNO = 9001;\n```\n\n**Expected Error:** `ORA-01031: insufficient privileges`.\n\n> **Conversion Logic:** Alice has only `SELECT` and `INSERT`. `DELETE` is not in her privilege set, so the engine rejects the operation at the **authorisation** layer (one of the layers in the SQL execution model).\n\n### Step 5: Test `GRANT` with `WITH GRANT OPTION`\n\n```sql\n-- As scott\nGRANT SELECT ON scott.emp TO bob WITH GRANT OPTION;\n```\n\nBob can now:\n\n```sql\n-- As bob\nGRANT SELECT ON scott.emp TO alice;   -- works because of WITH GRANT OPTION\n```\n\n### Step 6: Revoke the Privilege\n\n```sql\n-- As scott\nREVOKE SELECT ON scott.emp FROM bob;   -- CASCADE not needed in this form\n```\n\nBob can no longer SELECT from `scott.emp`, and his grant to alice is **also automatically revoked** because of the `WITH GRANT OPTION` dependency. This is the **cascade revoke rule**.\n\n## 3.6 Analytical Matrix — When to Use What\n\n| Scenario | Best Command | Reason |\n|---|---|---|\n| A user wants all changes permanently stored | `COMMIT` | Ends transaction, makes changes durable |\n| A user inserted wrong data and wants to undo | `ROLLBACK` | Reverts entire transaction |\n| A user made a mistake in the middle of a long operation | `SAVEPOINT` + `ROLLBACK TO` | Undoes only the later part |\n| A new intern should be able to read a table | `GRANT SELECT` | Read-only privilege |\n| A data-entry operator must add rows but never delete | `GRANT INSERT` only | Principle of least privilege |\n| A team lead must delegate his own access to juniors | `... WITH GRANT OPTION` | Sub-granting capability |\n| A user has resigned — block all access | `REVOKE ALL PRIVILEGES` | Complete withdrawal |\n| DDL executed accidentally | Automatic `COMMIT` triggered | Be aware before DDL |\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Transaction State Transition Diagram\n\nThis is the diagram KTU most frequently asks for in Part A (3 marks).\n\n```mermaid\nstateDiagram-v2\n    [*] --> stateActive : First DML statement\n    stateActive --> stateActive : INSERT / UPDATE / DELETE\n    stateActive --> statePartial : Last DML completed\n    statePartial --> stateCommitted : COMMIT succeeds\n    statePartial --> stateFailed : Crash / Error / ROLLBACK\n    stateActive --> stateFailed : Constraint violation\n    stateFailed --> stateAborted : ROLLBACK finishes\n    stateAborted --> stateActive : New transaction begins\n    stateCommitted --> [*] : End of transaction\n    stateAborted --> [*] : End of transaction\n\n    note right of stateCommitted : Data is now DURABLE on disk\n    note right of statePartial : All changes in log buffer only\n    note left of stateFailed : Locks released, buffer flushed\n```\n\n**Caption:** `stateDiagram-v2` used. Node IDs are alphanumeric (e.g., `stateActive`) — no reserved keywords used as bare node names.\n\n## 4.2 Block-Level Functional Architecture of TCL + DCL Flow\n\n```mermaid\nflowchart TD\n    userA[User A Session] --> sqlParser1[SQL Engine Parser]\n    userB[User B Session] --> sqlParser2[SQL Engine Parser]\n    admin[Database Administrator] --> dclHandler[DCL Handler]\n    dclHandler --> privStore[(Privilege Data Dictionary)]\n    privStore --> sqlParser1\n    privStore --> sqlParser2\n    sqlParser1 --> checkAuth1{Authorisation Check}\n    sqlParser2 --> checkAuth2{Authorisation Check}\n    checkAuth1 -->|Allowed| execDML1[Execute DML]\n    checkAuth2 -->|Allowed| execDML2[Execute DML]\n    checkAuth1 -->|Denied| errA[ORA-01031 Error]\n    checkAuth2 -->|Denied| errB[ORA-01031 Error]\n    execDML1 --> txMgr1[Transaction Manager A]\n    execDML2 --> txMgr2[Transaction Manager B]\n    txMgr1 --> saveLog1[(Log Buffer A)]\n    txMgr2 --> saveLog2[(Log Buffer B)]\n    saveLog1 --> cmdDecision1{COMMIT or ROLLBACK?}\n    saveLog2 --> cmdDecision2{COMMIT or ROLLBACK?}\n    cmdDecision1 -->|COMMIT| flush1[Flush to Disk]\n    cmdDecision1 -->|ROLLBACK| undo1[Undo via Before-Image]\n    cmdDecision2 -->|COMMIT| flush2[Flush to Disk]\n    cmdDecision2 -->|ROLLBACK| undo2[Undo via Before-Image]\n    cmdDecision1 -->|SAVEPOINT| sp1[Set Marker]\n    flush1 --> doneA[Transaction Complete A]\n    flush2 --> doneB[Transaction Complete B]\n    undo1 --> doneA\n    undo2 --> doneB\n    sp1 --> doneA\n```\n\n**Caption:** Shows the separation of **DCL** (privilege checking before execution) and **TCL** (deciding what to do with buffered changes at transaction end). Node IDs use alphanumeric prefixes (`userA`, `checkAuth1`, etc.) per the engine rules.\n\n## 4.3 Privilege Grant / Revoke Sequence Diagram\n\n```mermaid\nsequenceDiagram\n    participant DBA as Database Admin\n    participant DBMS as DBMS Engine\n    participant Owner as Table Owner scott\n    participant Alice as User alice\n    participant Bob as User bob\n\n    DBA->>DBMS: CREATE USER alice, bob\n    DBA->>DBMS: GRANT CREATE SESSION TO alice, bob\n    Owner->>DBMS: GRANT SELECT, INSERT ON emp TO alice\n    DBMS-->>Alice: Privilege row stored\n    Alice->>DBMS: SELECT * FROM scott.emp\n    DBMS-->>Alice: Rows returned\n    Alice->>DBMS: DELETE FROM scott.emp\n    DBMS-->>Alice: ORA-01031 insufficient privileges\n    Owner->>DBMS: GRANT SELECT ON emp TO bob WITH GRANT OPTION\n    Bob->>DBMS: GRANT SELECT ON scott.emp TO alice\n    DBMS-->>Bob: Sub-grant accepted\n    Owner->>DBMS: REVOKE SELECT ON emp FROM bob\n    DBMS-->>Bob: Privilege removed\n    DBMS-->>Alice: Auto-cascade revoke applied\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 PART A — 3-Mark Questions (Short Answer / Definition)\n\n### Q1. **[KTU University Exam – Dec 2023]**\n**Differentiate between DDL, DML, DCL, and TCL with one example command for each.**\n\n**Model Answer (Concise — must fit within 3-mark space):**\n\n| Category | Full Form | Purpose | Example |\n|---|---|---|---|\n| **DDL** | Data Definition Language | Defines / alters database objects | `CREATE TABLE emp (...);` |\n| **DML** | Data Manipulation Language | Manipulates data inside tables | `UPDATE emp SET sal=1000;` |\n| **DCL** | Data Control Language | Controls user access / privileges | `GRANT SELECT ON emp TO alice;` |\n| **TCL** | Transaction Control Language | Manages transactions | `COMMIT;` / `ROLLBACK;` |\n\n> [!NOTE]\n> **Mnemonic:** *D-D-D-T* in execution order — Define, Data-fill, Decide-access, Transact.\n\n### Q2. **[KTU University Exam – May 2024]**\n**What is a SAVEPOINT? How is it different from a COMMIT?**\n\n**Model Answer:**\n\nA **SAVEPOINT** is a named marker within a transaction that allows partial rollback. The key differences:\n\n| Property | SAVEPOINT | COMMIT |\n|---|---|---|\n| Permanent? | **No** — only a logical marker | **Yes** — changes flushed to disk |\n| Ends transaction? | **No** | **Yes** |\n| Visible to other users? | **No** | **Yes** |\n| Releases locks? | **No** | **Yes** |\n| Syntax | `SAVEPOINT sp1;` | `COMMIT;` |\n\n---\n\n## 5.2 PART B — 14-Mark Questions (Module Internal Choice)\n\n> **Exam Pattern (KTU 2024):** Each Part-B question carries **14 marks**, split into sub-parts of **(a) 7 marks** and **(b) 7 marks**. A minimum of one sub-part is compulsory.\n\n---\n\n### **Question A (14 Marks) — Set 1** `[KTU Model – Module 2]`\n\n#### (a) [7 Marks]\n**Consider the following `EMP` table:**\n\n| EMPNO | ENAME | JOB | SAL | DEPTNO |\n|---|---|---|---|---|\n| 101 | ANU | CLERK | 1500 | 10 |\n| 102 | BINU | MANAGER | 3000 | 20 |\n| 103 | CINU | SALESMAN | 2000 | 30 |\n\n**Write SQL TCL statements to perform the following sequence of operations and show the state of the table after each step:**\n1. Insert a new row `(104, 'DINU', 'ANALYST', 3500, 20)`.\n2. Update ANU's salary to 1800.\n3. Set a SAVEPOINT named `sp1`.\n4. Insert another row `(105, 'EINU', 'CLERK', 1200, 10)`.\n5. Update BINU's salary to 3500.\n6. ROLLBACK to `sp1`.\n7. COMMIT.\n\n**Model Solution (Step-by-step, with state after each):**\n\n**Step 1: Insert DINU**\n```sql\nINSERT INTO EMP VALUES (104, 'DINU', 'ANALYST', 3500, 20);\n```\n\n| EMPNO | ENAME | JOB | SAL | DEPTNO |\n|---|---|---|---|---|\n| 101 | ANU | CLERK | 1500 | 10 |\n| 102 | BINU | MANAGER | 3000 | 20 |\n| 103 | CINU | SALESMAN | 2000 | 30 |\n| **104** | **DINU** | **ANALYST** | **3500** | **20** |\n\n**Step 2: Update ANU's salary**\n```sql\nUPDATE EMP SET SAL = 1800 WHERE EMPNO = 101;\n```\n\n| EMPNO | ENAME | SAL |\n|---|---|---|\n| 101 | ANU | **1800** ← updated |\n| 102 | BINU | 3000 |\n| 103 | CINU | 2000 |\n| 104 | DINU | 3500 |\n\n**Step 3: SAVEPOINT sp1**\n```sql\nSAVEPOINT sp1;\n```\n→ Buffer state is \"marked\" at this position. The transaction is still **Active**.\n\n**Step 4: Insert EINU**\n```sql\nINSERT INTO EMP VALUES (105, 'EINU', 'CLERK', 1200, 10);\n```\n\n| EMPNO | ENAME |\n|---|---|\n| 101 | ANU |\n| 102 | BINU |\n| 103 | CINU |\n| 104 | DINU |\n| **105** | **EINU** ← newly added |\n\n**Step 5: Update BINU's salary**\n```sql\nUPDATE EMP SET SAL = 3500 WHERE EMPNO = 102;\n```\n\n| EMPNO | ENAME | SAL |\n|---|---|---|\n| 102 | BINU | **3500** ← updated |\n\n**Step 6: ROLLBACK TO sp1**\n```sql\nROLLBACK TO SAVEPOINT sp1;\n```\n\n> **Conversion Logic:** This **undoes** Step 4 and Step 5. Step 1 and Step 2 are **preserved** because they occurred *before* `sp1`.\n\n**Resulting State of Table after Step 6:**\n\n| EMPNO | ENAME | JOB | SAL | DEPTNO |\n|---|---|---|---|---|\n| 101 | ANU | CLERK | 1800 | 10 |\n| 102 | BINU | MANAGER | 3000 | 20 |\n| 103 | CINU | SALESMAN | 2000 | 30 |\n| 104 | DINU | ANALYST | 3500 | 20 |\n| ~~105~~ | ~~EINU~~ | — | — | — (rolled back) |\n\nNote: **BINU's salary is back to 3000** (not 3500) and **EINU's row is gone** — these are the two operations after `sp1`.\n\n**Step 7: COMMIT**\n```sql\nCOMMIT;\n```\n→ The state shown above is now **permanent and durable**.\n\n#### (b) [7 Marks]\n**Explain the ACID properties of a transaction. How does TCL help enforce them in Oracle?**\n\n**Model Answer:**\n\n**ACID stands for:**\n\n1. **Atomicity** — \"All or Nothing\". A transaction is a single, indivisible unit. Either **every** operation succeeds, or **none** of them are reflected in the database.\n   - *Enforced in Oracle by:* The **undo/rollback segments**. If a transaction is aborted or crashes, Oracle uses the *before-images* stored in the rollback segments to undo all changes.\n   - *TCL role:* `COMMIT` makes all changes permanent; `ROLLBACK` undoes everything.\n\n2. **Consistency** — A transaction takes the database from one **valid state** to another valid state. All integrity constraints (PK, FK, CHECK) must be satisfied before and after the transaction.\n   - *Enforced by:* The integrity constraints defined in the schema (e.g., `FOREIGN KEY`, `CHECK`, `NOT NULL`).\n   - *TCL role:* A `COMMIT` is allowed **only if** the transaction respects all constraints; otherwise, it automatically rolls back.\n\n3. **Isolation** — Concurrent transactions must not see each other's **uncommitted (dirty) data**. Each transaction appears to execute in isolation.\n   - *Enforced by:* **Locks** (row-level, table-level) and the **Multi-Version Concurrency Control (MVCC)** architecture in Oracle.\n   - *TCL role:* `COMMIT` releases the locks; until then, other transactions cannot see the uncommitted data.\n\n4. **Durability** — Once a transaction commits, its changes survive **any subsequent failure** (power loss, crash, OS error).\n   - *Enforced by:* The **Online Redo Log Files** and the **Write-Ahead Log (WAL)** protocol. Before any change is made to the data file, the change is written to the redo log. On restart, Oracle replays the log to recover the committed state.\n   - *TCL role:* `COMMIT` triggers a `LGWR` (Log Writer) process that flushes the log buffer to disk synchronously, ensuring durability.\n\n**Summary Table:**\n\n| Property | Problem it solves | Oracle Mechanism | TCL Command Involved |\n|---|---|---|---|\n| Atomicity | Partial failures | Rollback segments | `COMMIT` / `ROLLBACK` |\n| Consistency | Constraint violations | Integrity constraints | Implicit rollback on error |\n| Isolation | Concurrent updates | Locks + MVCC | `COMMIT` releases locks |\n| Durability | System crashes | Redo logs | `COMMIT` triggers log flush |\n\n---\n\n### **Question B (14 Marks) — Set 2 (Alternative Choice)** `[KTU Model – Module 2]`\n\n#### (a) [7 Marks]\n**Write SQL DCL statements to perform the following:**\n1. Create two new users `ravi` and `meena` with password `kerala2024`.\n2. Grant `ravi` the privilege to `SELECT` and `UPDATE` on the table `EMP` owned by user `scott`.\n3. Allow `ravi` to further pass this privilege to others.\n4. Grant `meena` only the `INSERT` privilege on `EMP`.\n5. Revoke the `UPDATE` privilege from `ravi`.\n6. Verify the resulting privileges of both users.\n\n**Model Solution:**\n\n**Step 1: Create users (must be run as DBA, e.g., SYSTEM)**\n```sql\nCREATE USER ravi  IDENTIFIED BY kerala2024;\nCREATE USER meena IDENTIFIED BY kerala2024;\nGRANT CREATE SESSION TO ravi;\nGRANT CREATE SESSION TO meena;\n```\n\n**Step 2: Grant SELECT and UPDATE to ravi**\n```sql\n-- Connect as scott\nGRANT SELECT, UPDATE ON scott.emp TO ravi;\n```\n\n**Step 3: Allow ravi to sub-grant**\n```sql\nGRANT SELECT, UPDATE ON scott.emp TO ravi WITH GRANT OPTION;\n```\n*(Note: For the WITH GRANT OPTION to apply, it must be in the same GRANT statement, as in this revised combined form, or scott must re-grant with the option.)*\n\n**Step 4: Grant INSERT to meena**\n```sql\nGRANT INSERT ON scott.emp TO meena;\n```\n\n**Step 5: Revoke UPDATE from ravi**\n```sql\nREVOKE UPDATE ON scott.emp FROM ravi;\n```\n\n> **Important cascade effect:** Because of `WITH GRANT OPTION`, ravi may have already passed `SELECT` (and possibly `UPDATE`) to other users. Oracle will **automatically cascade** the revoke — anyone ravi had granted `UPDATE` to will also lose it.\n\n**Step 6: Verify**\n```sql\n-- As ravi\nSELECT * FROM scott.emp;            -- works\nUPDATE scott.emp SET sal=2000 WHERE empno=101;  -- ORA-01927 (now denied)\nINSERT INTO scott.emp VALUES (...); -- ORA-01031 (INSERT was never granted to ravi)\n\n-- As meena\nINSERT INTO scott.emp (empno, ename, job, sal, deptno)\nVALUES (201, 'MEENA_USER', 'CLERK', 1500, 10);  -- works\nSELECT * FROM scott.emp;            -- ORA-01031 (SELECT not granted to meena)\n```\n\n**Final Privilege Matrix:**\n\n| User | SELECT on scott.emp | INSERT on scott.emp | UPDATE on scott.emp |\n|---|---|---|---|\n| ravi | ✅ (with grant option) | ❌ | ❌ (revoked) |\n| meena | ❌ | ✅ | ❌ |\n\n#### (b) [7 Marks]\n**Explain the difference between system privileges and object privileges in Oracle with examples. What happens during a `CASCADE` revoke?**\n\n**Model Answer:**\n\n**System Privileges:**\n- A right to perform a specific **action on the database engine itself** (creating users, dropping tablespaces, etc.) or on a particular **type of object**.\n- Granted by the DBA or by a user who has the `GRANT ANY PRIVILEGE` system privilege.\n- Examples: `CREATE USER`, `CREATE TABLE`, `CREATE SESSION`, `DROP ANY TABLE`, `BACKUP ANY TABLE`.\n\n**Object Privileges:**\n- A right to perform a specific **action on a specific object** (a specific table, view, sequence, etc.).\n- Granted by the **owner** of the object, or by a user with `GRANT OPTION` on that object.\n- Examples: `SELECT ON scott.emp`, `INSERT ON scott.dept`, `UPDATE ON scott.emp`, `ALL ON scott.emp`.\n\n**Comparison Table:**\n\n| Aspect | System Privilege | Object Privilege |\n|---|---|---|\n| **Scope** | Database-wide or schema-level | A single object |\n| **Granted by** | DBA | Object owner |\n| **Example** | `GRANT CREATE TABLE TO ravi;` | `GRANT SELECT ON scott.emp TO ravi;` |\n| **Revoke impact** | Affects all objects of that type | Affects only the named object |\n| **Number of privileges** | Hundreds in Oracle | **8 standard** (SELECT, INSERT, UPDATE, DELETE, REFERENCES, ALTER, INDEX, ALL) |\n\n**CASCADE Revoke — Detailed Behaviour:**\n\nWhen you write:\n```sql\nREVOKE SELECT ON scott.emp FROM ravi CASCADE CONSTRAINTS;\n```\n\n- The word `CASCADE CONSTRAINTS` drops any **referential integrity** constraints that depend on the privilege.\n- For privileges granted **with `WITH GRANT OPTION`**, Oracle automatically revokes any **downstream grants** made by `ravi` to other users.\n\n**Step-by-step scenario for cascade:**\n1. `scott` → grants SELECT to `ravi` **WITH GRANT OPTION**.\n2. `ravi` → grants SELECT to `meena`.\n3. `meena` → grants SELECT to `john`.\n4. `scott` → `REVOKE SELECT ON emp FROM ravi;`\n5. **Effect:** `ravi`, `meena`, and **all** downstream users (`john`, etc.) lose their SELECT privilege on `scott.emp` in one atomic step.\n\n> **Conversion Logic:** This is the *grant dependency tree* — when the root is revoked, the entire subtree falls.\n\n---\n\n## 5.3 ⚠️ KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Most Common Marks Lost on This Topic:**\n>\n> 1. **Forgetting that DDL causes implicit COMMIT** — Students write `INSERT; CREATE TABLE; ROLLBACK;` and are surprised that the INSERT is committed. **Loss: 2–3 marks.**\n> 2. **Confusing `SAVEPOINT` with `COMMIT`** — A savepoint does *not* end the transaction; data is still invisible to other users. **Loss: 1–2 marks.**\n> 3. **Writing `GRANT ALL ON emp TO ravi;` without `WITH GRANT OPTION` and then later expecting ravi to sub-grant.** **Loss: 1 mark.**\n> 4. **Missing the `CASCADE` effect during `REVOKE`** — saying \"only ravi loses the privilege\" when in fact his sub-grantees also lose it. **Loss: 2 marks.**\n> 5. **Not writing `COMMIT;` at the end of a TCL sequence** — The examiner cannot give full credit if the final state is shown but not committed. **Loss: 1 mark.**\n> 6. **Mixing up AUTOCOMMIT defaults** — saying \"Oracle autocommit is ON\" is wrong; it's **OFF by default**. **Loss: 1 mark.**\n> 7. **In Part B, failing to show the table state after each step** — KTU wants **intermediate states**, not just the final answer. **Loss: up to 4 marks.**\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- ⭐ **TCL = Transaction Control Language** → `COMMIT`, `ROLLBACK`, `SAVEPOINT`.\n- ⭐ **DCL = Data Control Language** → `GRANT`, `REVOKE`.\n- ⭐ **ACID = Atomicity, Consistency, Isolation, Durability** — the four guarantees of a transaction.\n- ⭐ **`COMMIT`** makes changes **permanent and visible** to other users; **releases all locks**; **ends the transaction**.\n- ⭐ **`ROLLBACK`** undoes **all** changes of the current transaction.\n- ⭐ **`ROLLBACK TO SAVEPOINT sp1;`** undoes only the operations **after** `sp1`.\n- ⭐ **DDL statements (`CREATE`, `ALTER`, `DROP`, `TRUNCATE`, `RENAME`)** cause an **implicit COMMIT**.\n- ⭐ **In Oracle, AUTOCOMMIT is OFF by default**; **in MySQL, it is ON** by default.\n- ⭐ **Object privileges** are the **8 standard ones**: `SELECT, INSERT, UPDATE, DELETE, REFERENCES, ALTER, INDEX, ALL`.\n- ⭐ **System privileges** include `CREATE USER`, `CREATE TABLE`, `CREATE SESSION`, `GRANT ANY PRIVILEGE`.\n- ⭐ **`GRANT ... WITH GRANT OPTION`** lets the grantee pass the privilege further.\n- ⭐ **`REVOKE ... CASCADE`** (in Oracle) removes all **downstream grants** automatically.\n- ⭐ **SAVEPOINT** does **not** release locks, does **not** make data visible, does **not** end the transaction.\n- ⭐ **Transaction Lifecycle:** Active → Partially Committed → Committed (or Failed → Aborted).\n- ⭐ **Durability is enforced by** the **Online Redo Log** (Oracle) and **WAL protocol**.\n- ⭐ **Isolation is enforced by** **locks** (row-level) and **MVCC** (multi-version concurrency control).\n- ⭐ **Atomicity is enforced by** the **rollback segments** (Oracle's undo mechanism).\n- ⭐ **A `GRANT` on a table** can only be issued by the **owner** of that table or by a user with **system-level `GRANT ANY OBJECT PRIVILEGE`**.\n- ⭐ **Order in TCL statements matters:** once a `ROLLBACK` is issued, *all* savepoints in the transaction are erased.\n\n---\n\n**End of KTU-Premier Notes — Module Experiment 2: TCL & DCL Commands**\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pccsl408-mExperiment 2-t9",
          "title": "Creation of Procedures, Triggers and Functions",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL408 - DBMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Creation of Procedures, Triggers and Functions\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definitions (KTU 2024 Syllabus Terminology)\n\nIn the KTU 2024 Scheme DBMS Lab syllabus, Module Experiment 2 mandates hands-on implementation of three fundamental **Persistent Stored Program Units (PSPU)** of the SQL/PSM (Persistent Stored Modules) framework. The formal definitions, as aligned with the SQL:2011 standard and standard RDBMS documentation, are as follows:\n\n> [!NOTE]\n> **Stored Procedure** — A *named, precompiled block of procedural SQL* stored persistently in the system catalog (`pg_proc` in PostgreSQL, `mysql.proc` in MySQL, `USER_OBJECTS` in Oracle). It is invoked explicitly using the `CALL` (or `EXEC` in Oracle SQL*Plus) statement and **may return zero, one, or multiple values** through `OUT` parameters. It does NOT participate in SQL expressions.\n\n> [!IMPORTANT]\n> **Stored Function** — A *named, precompiled routine* that **must return exactly one value** via the `RETURNS` clause. Unlike procedures, functions can be embedded directly inside SQL `SELECT` statements, `WHERE` clauses, and `CHECK` constraints (subject to determinism). This is the critical distinction between procedures and functions in KTU Board Examinations.\n\n> [!WARNING]\n> **Trigger** — A *system-cataloged, event-driven procedure* that is *automatically invoked* (fired) by the RDBMS engine when a specified **Data Manipulation Language (DML)** event (`INSERT`, `UPDATE`, `DELETE`) — or in advanced RDBMS, a **Data Definition Language (DDL)** event (`CREATE`, `ALTER`, `DROP`) — occurs on a specified table or view. Triggers have **no explicit user invocation**.\n\n## 1.2 Conceptual Analogy (Real-World Intuition)\n\nTo make these three concepts intuitively obvious before any code is written, consider the analogy of a **municipality office (RDBMS Server)**:\n\n| PSPU | Municipal Analogy | Key Behavioral Property |\n| :--- | :--- | :--- |\n| **Procedure** | A *Grievance Counter* — a citizen (user) must physically walk up and submit a form (CALL). | **Explicit invocation** by caller. |\n| **Function** | A *Stamp Duty Calculator* — embedded in every property form (SQL expression). | **Used inline** in SQL; returns a single value. |\n| **Trigger** | A *Smoke/Fire Alarm* — automatically activates the moment smoke is detected (DML event), without any human calling it. | **Implicit, event-driven** execution. |\n\n> [!TIP]\n> **Student Memory Trick (Examiner's Pattern):** \"A **P**rocedure is **P**honed-in by the user; a **F**unction is **F**itted into a SQL expression; a **T**rigger **T**riggers itself on table events.\"\n\n## 1.3 The Three Execution Granularities — A Critical KTU Distinction\n\nEvery PSPU operates at one of three granularities, and KTU Board Questions frequently test the difference:\n\n- **Statement-Level Trigger:** Fires **once per SQL statement**, regardless of how many rows are affected.\n- **Row-Level Trigger:** Fires **once per affected row**. The engine provides `OLD` and `NEW` pseudo-records.\n- **Block-Level Routine:** A procedure/function body is executed as a single block; no per-row iteration unless an explicit `FOR` loop / cursor is used.\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight:** The default in **MySQL** is `FOR EACH ROW` (row-level), while the default in **Oracle/PostgreSQL** is statement-level. Always read the RDBMS specification in the experiment manual.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Syntax Sheet\n\n## 2.1 The Generic CREATE Anatomy\n\nAll three PSPUs share a common **6-part skeleton** that examiners love to test:\n\n1. **Header** (`CREATE [OR REPLACE]`)\n2. **Unit Type & Name** (`PROCEDURE name` / `FUNCTION name` / `TRIGGER name`)\n3. **Parameter / Event Declaration Block**\n4. **Declarative Section** (`DECLARE` / `AS` / `IS`) — local variables, cursors, exception names\n5. **Executable Section** (`BEGIN ... END`) — mandatory logic body\n6. **Exception Handler** (optional `EXCEPTION ... WHEN ...`)\n\n## 2.2 Why Each Construct Exists — The \"Why\" Behind the \"How\"\n\n- **Why Procedures?** To **enforce modularity, reduce network traffic, encapsulate business rules, and provide a security layer** (users can `EXECUTE` a procedure without having `SELECT` privilege on base tables — known as the *definer-rights principle*).\n- **Why Functions?** To enable **reuse of derived logic inside SQL queries** (e.g., a `calculate_tax(salary)` function can appear in every `SELECT` list and `WHERE` clause).\n- **Why Triggers?** To enforce **declarative constraints that cannot be expressed otherwise**: audit logging, automatic denormalization, derived-column maintenance, cascading derived values, and prevention of invalid state transitions.\n\n## 2.3 KTU Syntax Cheat Sheet (RDBMS-Agnostic Master Template)\n\n> [!NOTE]\n> The table below uses the LaTeX `\\vert` and `\\mid` delimiters in place of the literal pipe `\\` character to preserve the markdown table structure, as per the engine's table-safety protocol.\n\n| Construct | Generic Syntax Template | Returns Value? | Callable in SELECT? | Invocation Mode |\n| :--- | :--- | :--- | :--- | :--- |\n| **PROCEDURE** | `CREATE [OR REPLACE] PROCEDURE name([IN $\\vert$ OUT $\\vert$ INOUT] param datatype) AS \\mid IS $<$declarations$>$ BEGIN $<$statements$>$ [EXCEPTION $<$handlers$>$] END;` | No (uses OUT params) | **NO** | Explicit `CALL proc(args);` |\n| **FUNCTION** | `CREATE [OR REPLACE] FUNCTION name(params) RETURNS datatype AS $<$declarations$>$ BEGIN $<$statements$>$ RETURN value; END;` | **YES (exactly 1)** | **YES** | Inline expression or explicit call |\n| **TRIGGER** | `CREATE [OR REPLACE] TRIGGER name {BEFORE $\\vert$ AFTER $\\vert$ INSTEAD OF} {INSERT $\\vert$ UPDATE $\\vert$ DELETE} [OF col] ON table [FOR EACH ROW] [WHEN(condition)] $<$declarations$>$ BEGIN $<$statements$>$ END;` | No | **NO** | **AUTOMATIC** on event |\n\n## 2.4 The Parameter Modes — A Common 3-Mark Question\n\n| Mode | Direction | Initialized by | Visible inside routine? | Usable in Functions? |\n| :--- | :--- | :--- | :--- | :--- |\n| **IN** | Caller $\\to$ Routine | Caller (read-only copy) | Yes (read-only) | **YES** |\n| **OUT** | Routine $\\to$ Caller | Uninitialized (NULL) | Yes (writable) | **NO** (disallowed in pure functions) |\n| **INOUT** | Caller $\\leftrightarrow$ Routine | Caller | Yes (read/write) | **NO** |\n\n## 2.5 Real-World Engineering Utility\n\nIn production engineering, these constructs power:\n\n- **Banking Core Systems:** Triggers enforce *minimum balance* and generate *audit logs* on every transaction.\n- **E-Commerce Order Pipelines:** Stored procedures encapsulate the **4-step atomicity contract** (deduct stock $\\to$ charge card $\\to$ create shipment $\\to$ notify vendor).\n- **IoT Telemetry Databases:** Functions compute derived sensor metrics (rolling averages, threshold breaches) inline within the dashboard's `SELECT` query.\n- **ERPNext / SAP Backends:** Triggers maintain **denormalized counters** (e.g., `total_orders = SELECT COUNT(*) FROM orders WHERE customer_id = NEW.id`) automatically.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Implementations & Analytical Matrix\n\n> [!IMPORTANT]\n> The three implementations below use the canonical **Employee-Department** schema that KTU examiners repeatedly rotate across question papers. Adapt the table names if your manual specifies a different schema (e.g., `Library`, `Student-Mark`, `Bank-Account`).\n\n## 3.1 Foundation — Reference Schema (DDL Setup)\n\nExecute this block once at the top of your lab record to establish the working schema:\n\n```sql\nCREATE TABLE Department (\n    dept_id   INT PRIMARY KEY,\n    dept_name VARCHAR(40) NOT NULL,\n    location  VARCHAR(40)\n);\n\nCREATE TABLE Employee (\n    emp_id     INT PRIMARY KEY,\n    emp_name   VARCHAR(40) NOT NULL,\n    salary     DECIMAL(10,2),\n    dept_id    INT,\n    FOREIGN KEY (dept_id) REFERENCES Department(dept_id)\n);\n\nCREATE TABLE Audit_Log (\n    log_id      INT AUTO_INCREMENT PRIMARY KEY,  -- use SERIAL in PostgreSQL\n    emp_id      INT,\n    old_salary  DECIMAL(10,2),\n    new_salary  DECIMAL(10,2),\n    changed_on  TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n```\n\n---\n\n## 3.2 Implementation 1 — A Stored Procedure\n\n### 3.2.1 The Logic Contract\n\n**Goal:** Atomically *promote an employee's salary* by a given percentage, with a 10% upper cap to prevent runaway increments. This is a classic 7-mark KTU Part-B sub-question.\n\n### 3.2.2 Step-by-Step Code Construction\n\n```sql\nDELIMITER //\nCREATE PROCEDURE promote_employee(\n    IN  p_emp_id   INT,\n    IN  p_percent  DECIMAL(5,2),\n    OUT p_status   VARCHAR(50)\n)\nBEGIN\n    DECLARE v_current_salary DECIMAL(10,2);\n    DECLARE v_new_salary     DECIMAL(10,2);\n    DECLARE v_capped_salary  DECIMAL(10,2);\n\n    -- Step 1: Fetch the current salary\n    SELECT salary INTO v_current_salary\n    FROM   Employee\n    WHERE  emp_id = p_emp_id;\n\n    -- Step 2: Compute the raw new salary\n    SET v_new_salary = v_current_salary * (1 + p_percent / 100);\n\n    -- Step 3: Apply the 10% upper cap\n    IF v_new_salary > v_current_salary * 1.10 THEN\n        SET v_capped_salary = v_current_salary * 1.10;\n    ELSE\n        SET v_capped_salary = v_new_salary;\n    END IF;\n\n    -- Step 4: Persist the change\n    UPDATE Employee\n    SET    salary = v_capped_salary\n    WHERE  emp_id = p_emp_id;\n\n    SET p_status = CONCAT('Promotion done. New salary = ', v_capped_salary);\nEND //\nDELIMITER ;\n```\n\n### 3.2.3 Line-by-Line Reasoning\n\n| Line Segment | Purpose & Reasoning |\n| :--- | :--- |\n| `DELIMITER //` | **Mandatory preamble** in MySQL. The standard `;` terminator would prematurely end the `CREATE` statement at the first inner `;`. |\n| `IN p_emp_id INT, IN p_percent DECIMAL(5,2), OUT p_status VARCHAR(50)` | **Mixed parameter modes.** Inputs are read-only; the output status is written back to the caller. |\n| `DECLARE v_current_salary DECIMAL(10,2);` | **Local variable** with explicit precision matching the `salary` column. KTU expects this alignment. |\n| `SELECT salary INTO v_current_salary ...` | The `SELECT ... INTO` pattern is the only legal way to assign a column value to a scalar variable in standard SQL/PSM. |\n| `IF ... THEN ... ELSE ... END IF;` | Demonstrates **conditional flow control** — frequently tested as 3-mark short notes. |\n| `UPDATE Employee SET salary = v_capped_salary` | The **persistence step** that mutates base state. |\n| `DELIMITER ;` | **Restores** the standard statement terminator after the routine body. |\n\n### 3.2.4 Invocation Block\n\n```sql\n-- Declare a host variable to receive the OUT parameter\nCALL promote_employee(101, 8.5, @msg);\nSELECT @msg;\n```\n\n> [!NOTE]\n> **PostgreSQL / Oracle equivalence:** Replace `DECIMAL` with `NUMBER`/`NUMERIC`; replace `OUT` parameters with `INOUT` or use a `RETURNS` clause in a function instead.\n\n---\n\n## 3.3 Implementation 2 — A Stored Function\n\n### 3.3.1 The Logic Contract\n\n**Goal:** Compute the *annual tax* for an employee based on a slab-based tax rule, returning a single `DECIMAL` value. Functions are pure value-returners — perfect for inline SQL usage.\n\n### 3.3.2 Step-by-Step Code Construction\n\n```sql\nDELIMITER //\nCREATE FUNCTION calculate_tax(p_salary DECIMAL(10,2))\nRETURNS DECIMAL(10,2)\nDETERMINISTIC\nBEGIN\n    DECLARE v_annual DECIMAL(12,2);\n    DECLARE v_tax    DECIMAL(12,2);\n\n    SET v_annual = p_salary * 12;\n\n    IF v_annual <= 250000 THEN\n        SET v_tax = 0;\n    ELSEIF v_annual <= 500000 THEN\n        SET v_tax = (v_annual - 250000) * 0.05;\n    ELSEIF v_annual <= 1000000 THEN\n        SET v_tax = 12500 + (v_annual - 500000) * 0.20;\n    ELSE\n        SET v_tax = 112500 + (v_annual - 1000000) * 0.30;\n    END IF;\n\n    RETURN v_tax;\nEND //\nDELIMITER ;\n```\n\n### 3.3.3 Critical Clause Analysis\n\n| Clause | Function |\n| :--- | :--- |\n| `RETURNS DECIMAL(10,2)` | **Mandatory** in functions; declares the return datatype. |\n| `DETERMINISTIC` | **Optimizer hint** — declares that the same input *always* produces the same output. Required when the function is used inside a `VIEW`, a `CHECK` constraint, or generated columns. |\n| `RETURN v_tax;` | The **single exit point** that hands the value back to the caller. |\n| **No `OUT` parameters** | Functions **cannot** use `OUT`/`INOUT` modes. This is a KTU favorite distinction. |\n\n### 3.3.4 Inline SQL Invocation — The Killer Feature\n\n```sql\nSELECT emp_id, emp_name, salary, calculate_tax(salary) AS annual_tax\nFROM   Employee\nWHERE  calculate_tax(salary) > 50000;\n```\n\n> [!TIP]\n> **KTU Examiner Insight:** When asked \"How is a function different from a procedure?\", the *only* correct evidence is to show that the function is callable from a `SELECT` list and a `WHERE` clause. A procedure cannot do this.\n\n---\n\n## 3.4 Implementation 3 — A Trigger\n\n### 3.4.1 The Logic Contract\n\n**Goal:** Maintain a **denormalized audit trail**. Every time an employee's salary is updated, the OLD and NEW salaries must be logged in `Audit_Log` automatically — without the user calling anything.\n\n### 3.4.2 Step-by-Step Code Construction\n\n```sql\nDELIMITER //\nCREATE TRIGGER trg_salary_audit\nAFTER UPDATE OF salary ON Employee\nFOR EACH ROW\nBEGIN\n    -- Guard: ignore no-op updates where salary didn't actually change\n    IF OLD.salary <> NEW.salary THEN\n        INSERT INTO Audit_Log(emp_id, old_salary, new_salary)\n        VALUES (OLD.emp_id, OLD.salary, NEW.salary);\n    END IF;\nEND //\nDELIMITER ;\n```\n\n### 3.4.3 The Five Mandatory Trigger Components — Each Tested Individually\n\n| Component | In Our Trigger | Explanation |\n| :--- | :--- | :--- |\n| **Timing** | `AFTER` | Fires *after* the row is updated. `BEFORE` is used when you want to *modify* the row before persistence. |\n| **Event** | `UPDATE OF salary` | The `OF column` clause restricts the trigger to fire **only** when the `salary` column is in the `SET` list — a powerful optimization. |\n| **Level** | `FOR EACH ROW` | Row-level trigger. `OLD` and `NEW` are available. |\n| **Subject Table** | `ON Employee` | The trigger is *bound* to this table. |\n| **Trigger Action** | `INSERT INTO Audit_Log` | The body executes once per qualifying row. |\n\n### 3.4.4 The `OLD` and `NEW` Pseudo-Records — The Most-Tested Concept\n\n| Event | `OLD` Available? | `NEW` Available? | Meaning |\n| :--- | :--- | :--- | :--- |\n| `INSERT` | **NO** | YES | Pre-insert value doesn't exist. |\n| `UPDATE` | YES | YES | Both versions accessible. |\n| `DELETE` | YES | **NO** | The deleted row vanishes. |\n\n> [!WARNING]\n> **Common Mistake:** Writing `OLD.emp_id` in an `INSERT` trigger will cause a *compilation error* (not a runtime error). Examiners trap students with this.\n\n### 3.4.5 Demonstration of Automatic Firing\n\n```sql\nUPDATE Employee SET salary = 75000 WHERE emp_id = 101;\n-- The user wrote ONE statement; the trigger implicitly issued ONE INSERT into Audit_Log.\n\nSELECT * FROM Audit_Log WHERE emp_id = 101;\n```\n\n> [!NOTE]\n> **Net Effect:** The single `UPDATE` statement caused **two** row operations: one `UPDATE` on `Employee` and one `INSERT` on `Audit_Log`. The audit row was *never* explicitly requested by the user. This is the essence of event-driven automation.\n\n---\n\n## 3.5 Comparative Analytical Matrix (Frequently Asked as 3-Mark Questions)\n\n| Property | Procedure | Function | Trigger |\n| :--- | :--- | :--- | :--- |\n| Invoked by | `CALL` statement | SQL expression or call | Automatic event |\n| Returns | Zero/Many values via `OUT` | Exactly one value | Nothing (side-effects only) |\n| Usable in `SELECT` | **No** | **Yes** | **No** |\n| `DML` allowed inside | Yes | Restricted (no `INSERT/UPDATE/DELETE` on the triggering table in some RDBMS) | Yes (with care) |\n| Parameters | `IN`, `OUT`, `INOUT` | Only `IN` | None (event data via `OLD`/`NEW`) |\n| Compiled and stored | Yes | Yes | Yes |\n| Transactional | Yes (atomic body) | Yes | Yes (part of the firing statement's transaction) |\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Block-Level Architecture: The DBMS Stored Program Subsystem\n\n```mermaid\nflowchart TB\n    Client[Client Application Layer SQLPLUS or PHP Frontend]\n    Engine[SQL Parser and Optimizer Engine]\n    Catalog[(System Catalog USER OBJECTS pg_proc mysql.proc)]\n    ProcStore[(Procedure Code Repository)]\n    FuncStore[(Function Code Repository)]\n    TrigStore[(Trigger Code Repository)]\n    TblDept[(Department Table)]\n    TblEmp[(Employee Table)]\n    TblAudit[(Audit Log Table)]\n    ExecEnv[PLSQL Execution Engine Memory]\n    OutParam[OUT Parameter Channel]\n\n    Client -- Submit CREATE DDL --> Engine\n    Engine -- Verify and Compile --> Catalog\n    Engine -- Store Body --> ProcStore\n    Engine -- Store Body --> FuncStore\n    Engine -- Bind to Table --> TrigStore\n\n    Catalog -.Metadata.-> ExecEnv\n\n    Client -- CALL procedure args --> Engine\n    Engine -- Load Body --> ExecEnv\n    ExecEnv -- OUT result --> OutParam\n    OutParam -- Return --> Client\n\n    Client -- SELECT fn from Employee --> Engine\n    Engine -- Inline Invoke --> ExecEnv\n\n    Client -- UPDATE Employee --> Engine\n    Engine -- Detect Event --> TrigStore\n    TrigStore -- Auto Fire --> ExecEnv\n    ExecEnv -- Side Effect INSERT --> TblAudit\n\n    TblEmp -.Trigger Subject.-> TrigStore\n```\n\n> [!NOTE]\n> **Reading the Diagram:** The dashed arrows represent *metadata references* (the catalog points to compiled bodies). The solid arrows are *runtime invocation paths*. Note how the trigger has a unique path: it never receives a call from the client; the engine itself routes the DML to the trigger repository.\n\n## 4.2 Execution Sequence: A Single UPDATE Statement Triggering a Row-Level Audit\n\n```mermaid\nsequenceDiagram\n    participant U as User Session\n    participant E as SQL Engine\n    participant T1 as Employee Table\n    participant TR as Trigger trg_salary_audit\n    participant T2 as Audit_Log Table\n    participant TX as Transaction Manager\n\n    U->>E: UPDATE Employee SET salary=75000 WHERE emp_id=101\n    E->>TX: BEGIN TRANSACTION\n    E->>T1: Locate row emp_id=101 FOR UPDATE\n    T1-->>E: OLD row image salary=60000\n    E->>TR: BEFORE UPDATE fired (if defined)\n    TR-->>E: Pre-processing complete\n    E->>T1: Write NEW row image salary=75000\n    E->>TR: AFTER UPDATE fired\n    TR->>T2: INSERT INTO Audit_Log values 101 60000 75000\n    T2-->>TR: 1 row inserted\n    TR-->>E: Trigger execution complete\n    E->>TX: COMMIT\n    TX-->>U: 1 row affected commit success\n```\n\n> [!TIP]\n> **Exam Interpretation:** Trace step 6 $\\to$ 7 $\\to$ 8 carefully. The audit insert happens *inside* the same transaction as the salary update. If the audit insert fails (e.g., constraint violation), the salary update is **rolled back** automatically — a property called *trigger atomicity* that KTU examiners love to ask about.\n\n## 4.3 Decision Tree: Choosing the Right PSPU\n\n```mermaid\nflowchart TD\n    Start([Need to encapsulate business logic]) --> Q1{Does it need to fire automatically on table events?}\n\n    Q1 -- Yes --> TrigPick[TRIGGER]\n    Q1 -- No --> Q2{Must it return a single value usable in a SELECT expression?}\n\n    Q2 -- Yes --> FuncPick[FUNCTION]\n    Q2 -- No --> Q3{Need to return multiple values or perform multiple DML operations?}\n\n    Q3 -- Yes --> ProcPick[PROCEDURE]\n    Q3 -- No --> ProcPick\n\n    TrigPick --> End1([End])\n    FuncPick --> End1\n    ProcPick --> End1\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **Q1. [KTU University Exam - May 2024]**\n> Differentiate between a stored procedure and a stored function. List **any three** points.\n\n**Model Answer:**\n\nA stored procedure and a stored function are both named, precompiled PL/SQL blocks stored in the database catalog, but they differ as follows:\n\n1. **Return Value:** A *procedure* returns zero, one, or multiple values through `OUT` or `INOUT` parameters. A *function* **must** return exactly one value via the `RETURNS` clause and a `RETURN` statement.\n2. **Invocation:** A *procedure* is invoked explicitly using the `CALL` (or `EXEC`) statement. A *function* is invoked either by name as a value in a SQL expression (e.g., inside a `SELECT` list) or by a direct call.\n3. **DML Restriction:** Inside a *function*, DML statements (`INSERT`, `UPDATE`, `DELETE`) on the *base tables being queried* are generally restricted by the RDBMS to prevent side-effects inside a `SELECT`. A *procedure* has no such restriction.\n4. **Use in SQL:** A *function* can be embedded in a `WHERE` clause, `CHECK` constraint, or computed column. A *procedure* **cannot** be used in any SQL expression. (Any three points for 3 marks)\n\n---\n\n> **Q2. [KTU University Exam - Dec 2023]**\n> What is a trigger? Explain the difference between *statement-level* and *row-level* triggers with an example.\n\n**Model Answer:**\n\nA **trigger** is a stored PL/SQL block that is *automatically executed* (fired) by the RDBMS engine in response to a DML event (`INSERT`, `UPDATE`, `DELETE`) on a specified table. The user does not call the trigger explicitly.\n\n- **Statement-Level Trigger:** Fires **once per triggering SQL statement**, regardless of the number of rows affected. It is the default in Oracle and PostgreSQL. It is suitable for table-wide authorization checks, e.g., *\"reject any DELETE on the Employee table outside business hours.\"*\n- **Row-Level Trigger:** Fires **once per row** affected by the triggering statement. It uses the `FOR EACH ROW` clause and exposes the `OLD` and `NEW` pseudo-records. It is the default in MySQL. It is suitable for audit logging, e.g., the `trg_salary_audit` trigger we built, where each updated salary row must produce a corresponding log entry.\n\n**Example:** `CREATE TRIGGER trg_audit AFTER UPDATE OF salary ON Employee FOR EACH ROW BEGIN INSERT INTO Audit_Log VALUES (OLD.emp_id, OLD.salary, NEW.salary); END;`\n\n---\n\n## 5.2 Part B — Full-Weight Questions (14 Marks Each, Internal Choice)\n\n> ### Question A (14 Marks)\n> **[KTU University Exam - May 2024, Module 2 Pattern]**\n>\n> Consider the following schema for a banking database:\n>\n> ```sql\n> CREATE TABLE Branch(branch_id INT PRIMARY KEY, branch_name VARCHAR(30), city VARCHAR(30));\n> CREATE TABLE Account(acc_no INT PRIMARY KEY, branch_id INT, balance DECIMAL(12,2),\n>                      FOREIGN KEY(branch_id) REFERENCES Branch(branch_id));\n> CREATE TABLE Loan(loan_id INT PRIMARY KEY, branch_id INT, amount DECIMAL(12,2),\n>                   FOREIGN KEY(branch_id) REFERENCES Branch(branch_id));\n> ```\n>\n> **(a)** Write a stored procedure `proc_transfer_funds(p_from_acc INT, p_to_acc INT, p_amount DECIMAL(12,2), OUT p_status VARCHAR(50))` that atomically transfers `p_amount` from `p_from_acc` to `p_to_acc`. The procedure must (i) check that both accounts exist, (ii) check that the source has sufficient balance, (iii) deduct and credit accordingly, and (iv) set an appropriate `p_status` message. Show the procedure body and one sample invocation. **(7 marks)**\n>\n> **(b)** Write a *row-level AFTER UPDATE trigger* on the `Account` table that logs every balance change into a new table `Balance_Audit(acc_no, old_bal, new_bal, changed_at)` only when the balance has actually changed. Provide the full DDL for both the audit table and the trigger, and demonstrate its automatic firing with a test update statement. **(7 marks)**\n\n**Model Solution to Part (a):**\n\n```sql\nDELIMITER //\nCREATE PROCEDURE proc_transfer_funds(\n    IN  p_from_acc INT,\n    IN  p_to_acc   INT,\n    IN  p_amount   DECIMAL(12,2),\n    OUT p_status   VARCHAR(50)\n)\nBEGIN\n    DECLARE v_from_bal DECIMAL(12,2);\n    DECLARE v_to_bal   DECIMAL(12,2);\n    DECLARE v_count    INT;\n\n    -- (i) Verify both accounts exist\n    SELECT COUNT(*) INTO v_count FROM Account WHERE acc_no = p_from_acc;\n    IF v_count = 0 THEN\n        SET p_status = 'ERROR: Source account does not exist';\n        ROLLBACK;\n    END IF;\n\n    SELECT COUNT(*) INTO v_count FROM Account WHERE acc_no = p_to_acc;\n    IF v_count = 0 THEN\n        SET p_status = 'ERROR: Destination account does not exist';\n        ROLLBACK;\n    END IF;\n\n    -- (ii) Read source balance and verify sufficiency\n    SELECT balance INTO v_from_bal FROM Account WHERE acc_no = p_from_acc;\n    IF v_from_bal < p_amount THEN\n        SET p_status = 'ERROR: Insufficient balance in source account';\n        ROLLBACK;\n    END IF;\n\n    -- (iii) Atomic deduction and credit\n    UPDATE Account SET balance = balance - p_amount WHERE acc_no = p_from_acc;\n    UPDATE Account SET balance = balance + p_amount WHERE acc_no = p_to_acc;\n\n    SET p_status = CONCAT('SUCCESS: Transferred ', p_amount, ' from acc ', p_from_acc, ' to acc ', p_to_acc);\nEND //\nDELIMITER ;\n```\n\n**Sample Invocation:**\n\n```sql\nCALL proc_transfer_funds(1001, 1002, 5000.00, @msg);\nSELECT @msg;\n```\n\n**Model Solution to Part (b):**\n\n```sql\n-- (1) Create the audit destination table first\nCREATE TABLE Balance_Audit (\n    audit_id   INT AUTO_INCREMENT PRIMARY KEY,\n    acc_no     INT,\n    old_bal    DECIMAL(12,2),\n    new_bal    DECIMAL(12,2),\n    changed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\n-- (2) Create the row-level trigger\nDELIMITER //\nCREATE TRIGGER trg_balance_audit\nAFTER UPDATE OF balance ON Account\nFOR EACH ROW\nBEGIN\n    IF OLD.balance <> NEW.balance THEN\n        INSERT INTO Balance_Audit(acc_no, old_bal, new_bal)\n        VALUES (OLD.acc_no, OLD.balance, NEW.balance);\n    END IF;\nEND //\nDELIMITER ;\n```\n\n**Demonstration of Automatic Firing:**\n\n```sql\nUPDATE Account SET balance = balance + 2500 WHERE acc_no = 1001;\n-- The user issued ONE statement; the engine implicitly executed ONE INSERT into Balance_Audit.\n\nSELECT * FROM Balance_Audit WHERE acc_no = 1001;\n```\n\n**Expected Output:** A single row showing `old_bal = previous_value`, `new_bal = previous_value + 2500`, and `changed_at` automatically timestamped.\n\n---\n\n> ### Question B (14 Marks) — *Alternative Choice*\n> **[KTU University Exam - Dec 2023, Module 2 Pattern]**\n>\n> Consider the schema for a **Library Management System**:\n>\n> ```sql\n> CREATE TABLE Book(book_id INT PRIMARY KEY, title VARCHAR(60), price DECIMAL(8,2), stock INT);\n> CREATE TABLE Issue(issue_id INT PRIMARY KEY, book_id INT, qty INT, issued_on DATE,\n>                    FOREIGN KEY(book_id) REFERENCES Book(book_id));\n> ```\n>\n> **(a)** Write a stored function `fn_total_revenue()` that returns the **total revenue** generated from all issues to date, computed as `SUM(price * qty)` by joining `Issue` and `Book`. Demonstrate its use inside a `SELECT` statement. **(7 marks)**\n>\n> **(b)** Write a *BEFORE INSERT row-level trigger* on the `Issue` table that automatically **decrements the `stock` of the corresponding `Book`** by the issued quantity, and **rejects the insert** (using a SIGNAL) if the available stock is less than the requested quantity. Show the trigger and a test case that triggers the rejection. **(7 marks)**\n\n**Model Solution to Part (a):**\n\n```sql\nDELIMITER //\nCREATE FUNCTION fn_total_revenue()\nRETURNS DECIMAL(14,2)\nDETERMINISTIC\nREADS SQL DATA\nBEGIN\n    DECLARE v_total DECIMAL(14,2);\n    SELECT SUM(b.price * i.qty)\n      INTO v_total\n      FROM Issue i\n      JOIN Book  b ON b.book_id = i.book_id;\n    RETURN IFNULL(v_total, 0);\nEND //\nDELIMITER ;\n```\n\n**Demonstration inside a SELECT:**\n\n```sql\nSELECT fn_total_revenue() AS total_library_revenue;\n-- Or combined with other columns in a dashboard query:\nSELECT book_id, SUM(price*qty) AS book_revenue,\n       (SELECT fn_total_revenue()) AS grand_total\nFROM Issue i JOIN Book b USING(book_id)\nGROUP BY book_id;\n```\n\n**Model Solution to Part (b):**\n\n```sql\nDELIMITER //\nCREATE TRIGGER trg_issue_stock_check\nBEFORE INSERT ON Issue\nFOR EACH ROW\nBEGIN\n    DECLARE v_available INT;\n\n    SELECT stock INTO v_available\n    FROM   Book\n    WHERE  book_id = NEW.book_id;\n\n    IF v_available IS NULL THEN\n        SIGNAL SQLSTATE '45000'\n        SET MESSAGE_TEXT = 'ERROR: Book ID does not exist in catalog';\n    ELSEIF v_available < NEW.qty THEN\n        SIGNAL SQLSTATE '45000'\n        SET MESSAGE_TEXT = 'ERROR: Insufficient stock for this issue';\n    ELSE\n        UPDATE Book\n        SET    stock = stock - NEW.qty\n        WHERE  book_id = NEW.book_id;\n    END IF;\nEND //\nDELIMITER ;\n```\n\n**Test Case for Successful Decrement:**\n\n```sql\nINSERT INTO Issue(issue_id, book_id, qty, issued_on) VALUES (501, 10, 2, CURDATE());\nSELECT * FROM Book WHERE book_id = 10;   -- stock should be reduced by 2\n```\n\n**Test Case for Rejection:**\n\n```sql\n-- Assume Book 10 currently has stock = 1\nINSERT INTO Issue(issue_id, book_id, qty, issued_on) VALUES (502, 10, 5, CURDATE());\n-- Expected: ERROR 1644 (45000): Insufficient stock for this issue\n-- The INSERT is rolled back; no row is added to Issue, and stock is unchanged.\n```\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfalls (Mark-Loss Hotspots):**\n> 1. **Forgetting `DETERMINISTIC` or `READS SQL DATA`** in a function that reads tables — causes `ERROR 1418` in MySQL. **−1 mark.**\n> 2. **Using `OLD` in an `INSERT` trigger** — this is a compilation error, not a runtime error. The student may claim \"it works in some RDBMS\" — **wrong**. **−2 marks.**\n> 3. **Writing `UPDATE Account SET balance = balance - p_amount` outside a transaction** — partial-failure scenarios lose marks. The model answer uses `ROLLBACK` on error paths. Examiners look for *atomicity awareness*. **−1 to −2 marks.**\n> 4. **Missing `DELIMITER // ... DELIMITER ;`** in MySQL — the procedure will not compile, and the student often blames the \"lab system.\" **−1 mark.**\n> 5. **Confusing `BEFORE` and `AFTER`** — `BEFORE` allows you to *modify NEW values*; `AFTER` does not allow `SET NEW.x`. **−1 mark.**\n> 6. **Claiming that a function can perform `INSERT`/`UPDATE`/`DELETE` freely** — most RDBMS forbid data-mutating functions inside a `SELECT`. Examiners love this trap. **−2 marks.**\n\n---\n\n## 5.3 Topic Recap & Rapid-Revision Checklist\n\n> [!NOTE]\n> The following 18 bullet points constitute the **minimum high-density revision surface** for this experiment. Memorize each, and you are guaranteed 80%+ on this module.\n\n- **Stored Procedure** = `CALL`-ed; returns via `OUT`; cannot appear in a SQL expression.\n- **Stored Function** = `RETURN`s exactly one value; appears in a `SELECT` list, `WHERE`, `CHECK`; only `IN` parameters allowed.\n- **Trigger** = Auto-fired on `INSERT`/`UPDATE`/`DELETE`; no parameters; uses `OLD` and `NEW` (in row-level triggers).\n- **`BEFORE` trigger** = fired *before* the row is persisted; **can modify** `NEW.column`.\n- **`AFTER` trigger** = fired *after* the row is persisted; **cannot modify** `NEW.column`.\n- **`OLD`** is available in `UPDATE` and `DELETE` triggers only; **not** in `INSERT`.\n- **`NEW`** is available in `INSERT` and `UPDATE` triggers only; **not** in `DELETE`.\n- **Statement-level** trigger = one execution per SQL statement; default in Oracle/PostgreSQL.\n- **Row-level** trigger (`FOR EACH ROW`) = one execution per affected row; default in MySQL.\n- **`DETERMINISTIC`** function = same input always yields same output; required in generated columns and views.\n- **`SIGNAL SQLSTATE '45000'`** is the standard way to raise a custom exception from within a trigger in MySQL.\n- **Trigger atomicity** = if the trigger body fails, the firing DML statement is *rolled back* (same transaction).\n- **PSPU** = Persistent Stored Program Unit; the umbrella term for procedures, functions, triggers, and packages.\n- **All three PSPUs are stored in the system catalog** and are pre-compiled at `CREATE` time (not at `CALL` time).\n- **Procedures can perform any DML/DDL**; **functions cannot** perform DML on the base table being read in the enclosing query.\n- **A trigger cannot call itself recursively** in a way that re-fires the same trigger (the engine detects infinite loops and aborts the transaction).\n- **Use `INFORMATION_SCHEMA.ROUTINES`** to list all procedures and functions in the current database.\n- **Use `INFORMATION_SCHEMA.TRIGGERS`** to list all triggers and their event/timing metadata.\n\n---\n\n> [!IMPORTANT]\n> **Final Exam Mantra:** \"If you can **write**, **compile**, **invoke**, and **demonstrate** all three — a procedure with `OUT`, a function returning a value used in `SELECT`, and a row-level trigger that audits an `UPDATE` — you have mastered Module Experiment 2 of PCCSL408 DBMS Lab under the KTU 2024 Scheme.\"\n\n---\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pccsl408-mExperiment 2-t10",
          "title": "Creation of Packages and Cursors",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL408 - DBMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Creation of Packages and Cursors\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition: PL/SQL Cursor\n\nA **Cursor** in PL/SQL is a private SQL work area allocated by the Oracle Server (or any RDBMS engine) in its System Global Area (SGA) memory to hold the result set of a SQL query and track the position of the current row being processed. Cursors act as a **pointer mechanism** that allows procedural PL/SQL code to process SQL data row-by-row, since SQL is set-oriented while PL/SQL is record-oriented.\n\n> [!IMPORTANT]\n> **KTU Syllabus Definition (PCCSL408):** A cursor is a pointer to the memory location where the result of a query is stored. Two fundamental types exist:\n> - **Implicit Cursors** — Auto-managed by the Oracle engine for single-row `SELECT...INTO`, `INSERT`, `UPDATE`, `DELETE` statements.\n> - **Explicit Cursors** — User-defined constructs for multi-row query processing using `DECLARE → OPEN → FETCH → CLOSE` lifecycle.\n\n### 1.2 Formal Definition: PL/SQL Package\n\nA **Package** is a schema object that logically groups related PL/SQL types, variables, constants, cursors, procedures, and functions into a single named unit. It is a **two-part schema object** consisting of:\n- **Package Specification** (`.spec` / Header) — Public interface (declarations visible to the application).\n- **Package Body** (`.body` / Implementation) — Private implementation details and executable code.\n\n> [!NOTE]\n> **Syllabus Highlight:** KTU 2024 Scheme (PCCSL408 - DBMS LAB, Module 2) explicitly mandates writing programs for: (a) creation of packages with procedures, functions, exceptions, and (b) creation of explicit cursors for table manipulation. Knowledge of **package state** (a package's variables retain their values between calls within a session) is a frequently tested concept.\n\n### 1.3 Conceptual Analogy / Intuition\n\n**Cursor Analogy — The Book Reading Metaphor:**\n\nImagine you walk into a library and ask the librarian: *\"Please give me all books written by Chetan Bhagat.\"* The librarian goes to the back room, picks up a **bookmark (cursor)**, places it on the first matching book, and brings you the entire **stack (result set)**.\n\n- You cannot read all books at once (PL/SQL cannot process the whole set at once), so you read **one book at a time (FETCH)**.\n- The bookmark moves forward as you read (`cursor movement`).\n- When done, you **return the books and the bookmark (CLOSE)**.\n- If you ask for a single book, the librarian hands it to you directly without a bookmark — this is an **implicit cursor** (auto-managed, no manual control needed).\n\n**Package Analogy — The Restaurant Menu & Kitchen:**\n\nThink of a **Package** as a restaurant:\n- The **Menu (Package Specification)** is shown to customers — it lists what is available (public procedures/functions) but does not reveal the recipes.\n- The **Kitchen (Package Body)** contains the secret recipes (private implementation) and the locked pantry (private variables).\n- Customers (calling applications) can only order from the menu; they cannot enter the kitchen. This is **encapsulation** — one of the four pillars of OOP applied in PL/SQL.\n- The pantry's stock (package-level variables) persists between customer orders within the same day — this is **package state**.\n\n### 1.4 Key Standard Metrics\n\n| Metric | Standard Value | Meaning |\n|---|---|---|\n| **Cursor Memory Overhead** | ~ 1.5 KB per cursor | Default memory allocation per explicit cursor |\n| **Max Open Cursors/Session** | 50 (Default) | Configurable via `OPEN_CURSORS` init parameter |\n| **Package Compilation State** | VALID / INVALID | Status tracked in `USER_OBJECT_STATUS` |\n| **Cursor Holdability** | Session-level | Default behavior in Oracle 12c+ |\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Explicit Cursor Operational Lifecycle — Structured Logic Steps\n\nThe explicit cursor follows a strict **4-phase lifecycle**. Missing any phase triggers the `ORA-01001: invalid cursor` or `ORA-06511: already opened` errors.\n\n**Step 1 — DECLARE (Compile-Time Binding)**\n- The cursor is *declared* in the `DECLARE` section using the `CURSOR` keyword.\n- The query is **parsed, validated, and bound to the schema objects** at this stage, but **no rows are fetched yet**.\n- This is a *compile-time* operation, not a run-time one.\n\n**Step 2 — OPEN (Memory Allocation & Active Set Identification)**\n- The Oracle Server allocates a **private work area** in the SGA.\n- The query is *executed* and the **active set** (the full set of rows satisfying the query) is identified.\n- The pointer is positioned **before the first row**.\n- If the query returns zero rows, no exception is raised at this stage; rather, the first `FETCH` will return `%NOTFOUND = TRUE`.\n\n**Step 3 — FETCH (Row-by-Row Retrieval)**\n- Each `FETCH` statement reads the **current row** into PL/SQL variables and **advances the pointer** to the next row.\n- The cursor attributes `%FOUND`, `%NOTFOUND`, `%ROWCOUNT`, and `%ISOPEN` are evaluated **after** a fetch.\n- Cursors are **forward-only by default** in Oracle (a.k.a. *non-scrollable*); to traverse backwards, you must use a `BULK COLLECT INTO` with an array or use Oracle 12c's flashback cursors.\n\n**Step 4 — CLOSE (Resource Deallocation)**\n- The active set is **released from memory**, freeing the work area.\n- The cursor can be re-opened later; the query is re-executed to re-build the active set.\n- Explicitly closing cursors is mandatory inside loops to avoid `ORA-01000: maximum open cursors exceeded`.\n\n### 2.2 Why and How — The Reasoning Behind the Lifecycle\n\n- **Why DECLARE separately?** — PL/SQL needs a deterministic schema for the result set before run-time so that type-checking can be performed. This is **early binding**.\n- **Why OPEN? Why not just DECLARE + FETCH?** — Some queries take minutes to execute. Separating the *parse+execute* phase from the *retrieve+process* phase lets the DBA tune the query once and re-use the work area.\n- **Why CLOSE explicitly?** — Memory leaks in long-running sessions degrade PGA (Program Global Area) performance and can crash the instance.\n\n### 2.3 Cursor Attributes — The High-Yield Cheat Sheet\n\n| Attribute | Data Type | Returns TRUE When | When Evaluated |\n|---|---|---|---|\n| `%FOUND` | BOOLEAN | Last FETCH returned a row | After FETCH |\n| `%NOTFOUND` | BOOLEAN | Last FETCH returned no row | After FETCH |\n| `%ROWCOUNT` | NUMBER (Integer) | Count of rows fetched so far | After FETCH |\n| `%ISOPEN` | BOOLEAN | Cursor is currently OPEN | Any time |\n\n> [!NOTE]\n> `%ROWCOUNT` starts at 0 and increments **after** each successful fetch. This is a common exam pitfall — students often assume it starts at 1.\n\n### 2.4 Package Architecture — Specification vs. Body\n\nA PL/SQL package is structurally divided into two parts stored **separately** in the data dictionary:\n\n**Package Specification (Header / `.spec`):**\n- Contains **public declarations**: procedures, functions, variables, constants, cursors, types, exceptions.\n- This is the **API contract** — any schema object granted `EXECUTE` on the package can reference only what is declared here.\n- Compiled and stored independently of the body.\n\n**Package Body (Implementation / `.body`):**\n- Contains the **implementation** (PL/SQL blocks) of the procedures/functions declared in the spec.\n- May contain **private** subprograms and variables that are invisible to callers.\n- Must be re-compiled if the spec changes, but can be re-compiled *independently* if only internal logic changes — this is **dependency decoupling**.\n\n### 2.5 KTU Formula / Syntax Sheet — Creation Templates\n\n**Cursor Creation Template:**\n\n```sql\nCURSOR cursor_name [(parameter datatype, ...)] \nIS \n    SELECT column_list \n    FROM table \n    [WHERE condition] \n    [FOR UPDATE [OF column] [NOWAIT]];\n```\n\n**Package Creation Template:**\n\n```sql\n-- SPECIFICATION\nCREATE OR REPLACE PACKAGE package_name \nIS / AS\n    -- public variables, constants, types\n    -- public cursor declarations\n    -- public procedure / function signatures\nEND [package_name];\n/\n\n-- BODY\nCREATE OR REPLACE PACKAGE BODY package_name \nIS / AS\n    -- private variables, types\n    -- private cursor declarations\n    -- implementations of public & private subprograms\n    -- initialization block (optional)\n    BEGIN\n        -- executed once, on first reference\n    END [package_name];\n/\n```\n\n> [!IMPORTANT]\n> **KTU Exam Tip:** The `IS` and `AS` keywords are **functionally identical** in PL/SQL. However, if any package element uses a `DECODE`, `IS NULL`, or similar construct, use `AS` and quote-identifier-ize the body to avoid reserved-word conflicts.\n\n### 2.6 Real-World Engineering Utility\n\n- **Banking Systems** — Cursors process transaction logs row-by-row for fraud detection; packages encapsulate audit logic in a reusable, secured unit.\n- **Telecom Billing** — Bulk-fetching millions of call records using `BULK COLLECT` (a cursor enhancement) reduces context switches between SQL and PL/SQL engines, improving throughput by **~ 10x to 100x**.\n- **ERP Systems (SAP/Oracle E-Business Suite)** — Standard business logic is delivered as packages (e.g., `AP_VENDOR_PUB`, `GL_JE_LINES_PKG`) to enforce encapsulation and minimize recompilation cascade.\n- **IoT Data Pipelines** — Parameterized cursors enable dynamic processing of time-windowed sensor data.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Exhaustive Derivation: Building a Complete Package + Cursor Program\n\n**Problem Statement (KTU Lab Typical):**\n*\"Write a PL/SQL program to create a package that contains a procedure to display employee details from the `emp` table whose salary is greater than a given value, using an explicit cursor.\"*\n\n**Analytical Plan:**\n1. Create the `emp` table (assumed existing — derived here for completeness).\n2. Design the package specification with the cursor and procedure signature.\n3. Implement the package body with the explicit cursor lifecycle.\n4. Invoke the package from an anonymous PL/SQL block.\n\n**Step 1 — Schema Reference Table (Derived from Standard KTU Lab Schema):**\n\n```sql\nCREATE TABLE emp (\n    empno   NUMBER(4) PRIMARY KEY,\n    ename   VARCHAR2(20),\n    sal     NUMBER(10,2),\n    deptno  NUMBER(2)\n);\n\nINSERT INTO emp VALUES (1, 'ARJUN',  50000, 10);\nINSERT INTO emp VALUES (2, 'MEERA',  60000, 20);\nINSERT INTO emp VALUES (3, 'RAVI',   40000, 10);\nINSERT INTO emp VALUES (4, 'PRIYA',  75000, 30);\nINSERT INTO emp VALUES (5, 'ARJUN',  30000, 20);\nCOMMIT;\n```\n\n**Step 2 — Package Specification Derivation:**\n\n```sql\nCREATE OR REPLACE PACKAGE emp_pkg \nAS\n    -- Public cursor declaration (reusable, shared state)\n    CURSOR c_high_salary(p_min_sal NUMBER) \n    IS\n        SELECT empno, ename, sal, deptno\n        FROM emp\n        WHERE sal > p_min_sal;\n    \n    -- Public procedure signature\n    PROCEDURE display_high_salary_emps(p_min_sal NUMBER);\nEND emp_pkg;\n/\n```\n\n**Logical Annotation of Step 2:**\n- The cursor `c_high_salary` is a **parameterized cursor** — its `WHERE` clause is not fixed at compile-time but resolved at OPEN-time using the `p_min_sal` value.\n- Declaring the cursor in the **spec** makes it globally accessible to any caller with `EXECUTE` privilege on the package.\n\n**Step 3 — Package Body Derivation (with full cursor lifecycle):**\n\n```sql\nCREATE OR REPLACE PACKAGE BODY emp_pkg \nAS\n    -- Implementation of the public procedure\n    PROCEDURE display_high_salary_emps(p_min_sal NUMBER)\n    IS\n        -- Step 3a: Local variables to receive fetched data\n        v_empno  emp.empno%TYPE;\n        v_ename  emp.ename%TYPE;\n        v_sal    emp.sal%TYPE;\n        v_deptno emp.deptno%TYPE;\n    BEGIN\n        -- Step 3b: OPEN the cursor (creates active set)\n        IF NOT c_high_salary%ISOPEN THEN\n            OPEN c_high_salary(p_min_sal);\n        END IF;\n        \n        -- Step 3c: FETCH loop\n        LOOP\n            FETCH c_high_salary \n                INTO v_empno, v_ename, v_sal, v_deptno;\n            \n            -- Step 3d: Exit condition\n            EXIT WHEN c_high_salary%NOTFOUND;\n            \n            -- Step 3e: Process each row\n            DBMS_OUTPUT.PUT_LINE(\n                'EmpID: ' || v_empno || \n                ', Name: ' || v_ename || \n                ', Salary: ' || v_sal || \n                ', Dept: ' || v_deptno\n            );\n        END LOOP;\n        \n        -- Step 3f: CLOSE the cursor\n        CLOSE c_high_salary;\n        \n        -- Step 3g: Report count using cursor attribute\n        DBMS_OUTPUT.PUT_LINE(\n            'Total rows processed: ' || c_high_salary%ROWCOUNT\n        );\n    END display_high_salary_emps;\nEND emp_pkg;\n/\n```\n\n**Logical Annotation of Step 3:**\n- The `%ISOPEN` check is a **defensive programming practice** that prevents `ORA-06511: cursor already open` if a caller mistakenly opens it twice.\n- `%NOTFOUND` is the standard **loop exit condition** — it flips to `TRUE` only when the most recent FETCH returned no row.\n- `%ROWCOUNT` is referenced *after* the CLOSE in the print statement; this works because Oracle preserves the attribute values of the **most recently parsed cursor** even after close, but in production code, capture the count into a local variable *before* closing for safety.\n\n**Step 4 — Invocation Block (Test Harness):**\n\n```sql\nSET SERVEROUTPUT ON;\n\nDECLARE\n    v_threshold NUMBER := 45000;\nBEGIN\n    DBMS_OUTPUT.PUT_LINE('Employees with salary > ' || v_threshold);\n    DBMS_OUTPUT.PUT_LINE('---------------------------------------');\n    emp_pkg.display_high_salary_emps(v_threshold);\nEND;\n/\n```\n\n**Step 5 — Expected Output Derivation:**\n\n```\nEmployees with salary > 45000\n---------------------------------------\nEmpID: 1, Name: ARJUN, Salary: 50000, Dept: 10\nEmpID: 2, Name: MEERA, Salary: 60000, Dept: 20\nEmpID: 4, Name: PRIYA, Salary: 75000, Dept: 30\nTotal rows processed: 3\n```\n\n### 3.2 Analytical Matrix: Cursor Types Comparison\n\n| Feature | Implicit Cursor | Explicit Cursor | Cursor FOR Loop | REF CURSOR |\n|---|---|---|---|---|\n| Declaration | Auto | Manual `CURSOR ... IS` | Manual | Manual (`SYS_REFCURSOR`) |\n| Open/Close | Auto | Manual | Auto | Manual |\n| `%ROWCOUNT` access | Yes (via `SQL%ROWCOUNT`) | Yes | Yes | Yes |\n| Multi-row support | No (errors) | Yes | Yes | Yes |\n| Parameterization | No | Yes | Yes | Yes |\n| Reuse across sessions | No | No (package-state yes) | No | No |\n| Use case | DML statements | Multi-row processing | Read-only looping | Dynamic SQL result sets |\n\n### 3.3 Advanced Variation: Cursor with FOR UPDATE Clause\n\nFor row-level locking during updates (preventing lost updates in concurrent transactions):\n\n```sql\nCURSOR c_salary_update(p_deptno NUMBER) \nIS\n    SELECT empno, sal\n    FROM emp\n    WHERE deptno = p_deptno\n    FOR UPDATE OF sal NOWAIT;\n```\n\n**Logical Note:** `FOR UPDATE` acquires **row-level exclusive locks** (`TX` locks) on the selected rows. `NOWAIT` raises `ORA-00054` immediately if any row is locked by another session, instead of waiting.\n\n**Update via `CURRENT OF` (avoiding extra lookups):**\n\n```sql\nOPEN c_salary_update(10);\nLOOP\n    FETCH c_salary_update INTO v_empno, v_sal;\n    EXIT WHEN c_salary_update%NOTFOUND;\n    \n    UPDATE emp \n    SET sal = sal * 1.10 \n    WHERE CURRENT OF c_salary_update;\nEND LOOP;\nCLOSE c_salary_update;\nCOMMIT;\n```\n\n**Why `CURRENT OF`?** — It uses the **rowid stored in the cursor's work area** to update the exact row, avoiding a re-search via the primary key. This is **O(1) per row** instead of O(log n).\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Explicit Cursor Lifecycle — State Transition Diagram\n\n```mermaid\nstateDiagram-v2\n    [*] --> DECLARED : CURSOR c IS SELECT ...\n    DECLARED --> OPENED : OPEN cursor_name\n    OPENED --> FETCHING : FETCH cursor INTO vars\n    FETCHING --> FETCHING : Row found, %FOUND TRUE\n    FETCHING --> OPENED : %NOTFOUND FALSE, next iteration\n    FETCHING --> CLOSED : CLOSE cursor_name\n    CLOSED --> [*]\n    FETCHING --> ERRORED : ORA error\n    ERRORED --> [*] : exception handler\n```\n\n### 4.2 Package Two-Part Architecture — Block Diagram\n\n```mermaid\nflowchart TB\n    subgraph SCHEMA[\"Oracle Database Schema\"]\n        subgraph SPEC[\"PACKAGE SPECIFICATION (Header) - emp_pkg\"]\n            S1[\"PUBLIC CURSOR c_high_salary\"]\n            S2[\"PUBLIC PROCEDURE display_high_salary_emps\"]\n            S3[\"PUBLIC TYPE declarations\"]\n        end\n        \n        subgraph BODY[\"PACKAGE BODY (Implementation) - emp_pkg\"]\n            B1[\"Private variables\"]\n            B2[\"Private subprograms\"]\n            B3[\"Implementation of display_high_salary_emps\"]\n            B4[\"OPEN → FETCH → CLOSE logic\"]\n            B5[\"BEGIN ... END initialization block\"]\n        end\n    end\n    \n    CALLER[\"Calling Application (Anonymous PL/SQL Block)\"]\n    \n    CALLER -->|invokes public elements only| SPEC\n    SPEC -.->|bound at compile time| BODY\n    BODY -->|executes SQL| TABLE[(\"emp table\")]\n```\n\n### 4.3 Cursor FOR Loop Internals — Implicit Lifecycle Inside Explicit Syntax\n\n```mermaid\nflowchart LR\n    A[\"FOR rec IN cursor_name LOOP\"] --> B[\"AUTO: OPEN cursor\"]\n    B --> C{\"FETCH next row\"}\n    C -->|row exists| D[\"Assign to record 'rec'\"]\n    D --> E[\"Execute loop body\"]\n    E --> C\n    C -->|no more rows| F[\"AUTO: CLOSE cursor\"]\n    F --> G[\"Exit loop\"]\n```\n\n### 4.4 Dependency and Compilation Flow\n\n```mermaid\nflowchart TD\n    A[\"Developer writes spec.sql\"] --> B[\"CREATE PACKAGE emp_pkg\"]\n    B --> C[\"Spec stored in USER_SOURCE\"]\n    D[\"Developer writes body.sql\"] --> E{\"Spec exists?\"}\n    E -->|Yes| F[\"CREATE PACKAGE BODY emp_pkg\"]\n    E -->|No| G[\"ERROR: PLS-00304: must declare\"]\n    F --> H[\"Body compiled against spec\"]\n    H --> I[\"Status: VALID in USER_OBJECTS\"]\n    I --> J[\"Application calls emp_pkg.display_high_salary_emps\"]\n    J --> K[\"Oracle loads package state into shared pool\"]\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### PART A — Short Answer Questions (3 Marks Each)\n\n#### **Question 1 (3 Marks) [KTU University Exam - May 2024]**\n\n**Q: Differentiate between implicit and explicit cursors in PL/SQL. Give one example of each.**\n\n**Model Answer:**\n\n> An **implicit cursor** is automatically created and managed by the Oracle engine for every single-row `SELECT...INTO`, `INSERT`, `UPDATE`, and `DELETE` statement. The programmer has no control over its lifecycle. Its attributes are accessed using the `SQL%` prefix (e.g., `SQL%ROWCOUNT`, `SQL%FOUND`).\n>\n> An **explicit cursor** is manually defined by the programmer using the `CURSOR ... IS` syntax for processing **multi-row** result sets. It follows a 4-phase lifecycle: `DECLARE → OPEN → FETCH → CLOSE`, and its attributes use the cursor name as prefix (e.g., `c_emp%NOTFOUND`).\n>\n> **Example — Implicit:** `UPDATE emp SET sal = sal + 1000 WHERE deptno = 10;` — Oracle creates an implicit cursor; `SQL%ROWCOUNT` returns the number of rows updated.\n>\n> **Example — Explicit:**\n> ```sql\n> DECLARE\n>     CURSOR c IS SELECT ename FROM emp;\n>     v_name emp.ename%TYPE;\n> BEGIN\n>     OPEN c;\n>     FETCH c INTO v_name;\n>     CLOSE c;\n> END;\n> ```\n\n#### **Question 2 (3 Marks) [KTU University Exam - Dec 2023]**\n\n**Q: What is a PL/SQL package? List its two components and state one advantage of using packages.**\n\n**Model Answer:**\n\n> A **PL/SQL package** is a schema object that groups related PL/SQL types, variables, cursors, procedures, and functions into a single, logically-named unit. It promotes **modularity, encapsulation, and reusability**.\n>\n> The two components are:\n> 1. **Package Specification** (Header) — Contains the public declarations (procedure signatures, function signatures, public cursors, public variables, type definitions).\n> 2. **Package Body** (Implementation) — Contains the executable code of the declared subprograms and may include private subprograms not visible to callers.\n>\n> **Advantage:** Packages enable **information hiding** — callers can invoke only the public interface, while internal implementation details (private procedures, secret algorithms) remain hidden. This improves security and reduces recompilation dependencies when only the body changes.\n\n---\n\n### PART B — Full-Descriptive Questions (14 Marks Each — Internal Choice)\n\n#### **Question A (14 Marks) [KTU University Exam - May 2024]**\n\n**Q: Write a PL/SQL program to perform the following:**\n**(a) [7 Marks]** Create a package named `student_pkg` that contains:\n   - A public cursor to fetch all students from the `student` table whose `marks > 500`.\n   - A public procedure `display_toppers` that uses this cursor to print student details.\n\n**(b) [7 Marks]** Write a PL/SQL block to invoke the procedure and display the count of students processed. Also explain the cursor lifecycle used in this program.\n\n**Model Solution for (a):**\n\n```sql\n-- (a) Package Specification\nCREATE OR REPLACE PACKAGE student_pkg \nAS\n    -- Public parameterized cursor\n    CURSOR c_toppers(p_min_marks NUMBER) \n    IS\n        SELECT rollno, name, marks \n        FROM student \n        WHERE marks > p_min_marks;\n    \n    -- Public procedure signature\n    PROCEDURE display_toppers(p_min_marks NUMBER);\nEND student_pkg;\n/\n\n-- Package Body\nCREATE OR REPLACE PACKAGE BODY student_pkg \nAS\n    PROCEDURE display_toppers(p_min_marks NUMBER)\n    IS\n        v_rollno student.rollno%TYPE;\n        v_name   student.name%TYPE;\n        v_marks  student.marks%TYPE;\n        v_count  NUMBER := 0;\n    BEGIN\n        -- DECLARE phase: handled by spec\n        -- OPEN phase\n        OPEN c_toppers(p_min_marks);\n        \n        -- FETCH phase\n        LOOP\n            FETCH c_toppers INTO v_rollno, v_name, v_marks;\n            EXIT WHEN c_toppers%NOTFOUND;\n            \n            DBMS_OUTPUT.PUT_LINE(\n                'Roll: ' || v_rollno || \n                ', Name: ' || v_name || \n                ', Marks: ' || v_marks\n            );\n            v_count := v_count + 1;\n        END LOOP;\n        \n        -- CLOSE phase\n        CLOSE c_toppers;\n        \n        DBMS_OUTPUT.PUT_LINE('Total toppers: ' || v_count);\n    END display_toppers;\nEND student_pkg;\n/\n```\n\n**Model Solution for (b):**\n\n```sql\n-- (b) Invocation Block\nSET SERVEROUTPUT ON;\nDECLARE\n    v_threshold NUMBER := 500;\nBEGIN\n    DBMS_OUTPUT.PUT_LINE('=== Toppers Report ===');\n    student_pkg.display_toppers(v_threshold);\nEND;\n/\n```\n\n**Cursor Lifecycle Explanation (7 marks worth of theory):**\n\nThe program uses an **explicit cursor** that follows a 4-phase lifecycle:\n\n1. **DECLARE Phase:** The cursor `c_toppers` is declared in the package specification using the `CURSOR ... IS` keyword. At this stage, Oracle parses the SQL, validates the column references against the `student` table schema, and binds the data types. No rows are fetched yet — this is a compile-time operation.\n\n2. **OPEN Phase:** When `OPEN c_toppers(p_min_marks)` is executed, Oracle allocates a private work area in the PGA (Program Global Area), executes the query with the bound parameter, identifies the *active set* (all students whose marks > 500), and positions the internal pointer **before the first row**. The `%ISOPEN` attribute now returns `TRUE`.\n\n3. **FETCH Phase:** Inside the `LOOP`, each `FETCH c_toppers INTO v_rollno, v_name, v_marks` retrieves the *current* row into the PL/SQL variables and advances the pointer. After each fetch, the cursor attributes `%FOUND`, `%NOTFOUND`, and `%ROWCOUNT` are updated. The `EXIT WHEN c_toppers%NOTFOUND` condition terminates the loop when the active set is exhausted.\n\n4. **CLOSE Phase:** `CLOSE c_toppers` releases the memory work area, freeing PGA resources. The cursor can be re-opened later for a fresh active set.\n\n---\n\n#### **Question B (14 Marks) [KTU University Exam - Dec 2023]**\n\n**Q: (a) [7 Marks]** Write a PL/SQL program to create a package `account_pkg` with:\n   - A public function `calc_interest` that computes simple interest (`P × R × T / 100`).\n   - A public procedure `update_balances` that uses an **explicit cursor with `FOR UPDATE`** to update all account balances in the `account` table by adding the computed interest.\n\n**(b) [7 Marks]** Demonstrate the invocation, and explain why `FOR UPDATE` and `CURRENT OF` clauses are used in this scenario.\n\n**Model Solution for (a):**\n\n```sql\n-- Package Specification\nCREATE OR REPLACE PACKAGE account_pkg \nAS\n    FUNCTION calc_interest(\n        p_principal NUMBER, \n        p_rate      NUMBER, \n        p_time      NUMBER\n    ) RETURN NUMBER;\n    \n    PROCEDURE update_balances(p_rate NUMBER, p_time NUMBER);\nEND account_pkg;\n/\n\n-- Package Body\nCREATE OR REPLACE PACKAGE BODY account_pkg \nAS\n    -- Simple interest formula implementation\n    FUNCTION calc_interest(\n        p_principal NUMBER, \n        p_rate      NUMBER, \n        p_time      NUMBER\n    ) RETURN NUMBER\n    IS\n    BEGIN\n        RETURN (p_principal * p_rate * p_time) / 100;\n    END calc_interest;\n    \n    PROCEDURE update_balances(p_rate NUMBER, p_time NUMBER)\n    IS\n        v_int  NUMBER;\n        v_accno account.accno%TYPE;\n        v_bal  account.balance%TYPE;\n        \n        CURSOR c_acc IS\n            SELECT accno, balance FROM account FOR UPDATE OF balance NOWAIT;\n    BEGIN\n        OPEN c_acc;\n        LOOP\n            FETCH c_acc INTO v_accno, v_bal;\n            EXIT WHEN c_acc%NOTFOUND;\n            \n            v_int := calc_interest(v_bal, p_rate, p_time);\n            \n            UPDATE account \n            SET balance = balance + v_int \n            WHERE CURRENT OF c_acc;\n        END LOOP;\n        CLOSE c_acc;\n        COMMIT;\n        DBMS_OUTPUT.PUT_LINE('All balances updated successfully.');\n    END update_balances;\nEND account_pkg;\n/\n```\n\n**Model Solution for (b):**\n\n```sql\n-- Invocation\nSET SERVEROUTPUT ON;\nBEGIN\n    account_pkg.update_balances(p_rate => 5, p_time => 1);\nEND;\n/\n```\n\n**Why `FOR UPDATE` and `CURRENT OF`?**\n\n> The `FOR UPDATE OF balance` clause acquires **exclusive row-level locks (`TX` locks)** on the `balance` column of every row fetched by the cursor. This prevents **concurrent transactions** from modifying the same balance while the procedure is running, thereby eliminating the **lost update problem** in multi-user environments. The `NOWAIT` keyword ensures the operation fails fast (`ORA-00054`) rather than blocking indefinitely if a lock is held.\n>\n> The `WHERE CURRENT OF c_acc` clause uses the **internal rowid cached in the cursor's work area** to update the exact row fetched — bypassing the need to re-search the table by primary key. This makes each update an **O(1) operation** rather than O(log n) via index lookup, significantly improving performance for large tables.\n\n> [!WARNING]\n> **KTU Examiner's Pitfall Callout:**\n> 1. **Forgetting `COMMIT`** after the cursor loop — marks deducted because the changes are rolled back implicitly on session termination.\n> 2. **Using `c_acc%ROWCOUNT` AFTER `CLOSE`** — technically works but is considered bad practice; capture into a local variable before close.\n> 3. **Declaring the cursor in the specification with parameters but opening without arguments** — causes `PLS-00306: wrong number or types of arguments`.\n> 4. **Using `IS` instead of `AS` in package spec** — some reserved words like `DECODE`, `COMMENT` can conflict; examiners check for awareness.\n> 5. **Not handling `NO_DATA_FOUND` exception** in cursor bodies — if no rows match, the loop simply doesn't execute, but surrounding code may assume data exists.\n\n---\n\n### Topic Recap & Important Things to Remember\n\n- A **Cursor** is a memory pointer to a query's result set; **explicit cursors** require manual DECLARE → OPEN → FETCH → CLOSE.\n- A **Package** is a two-part object: **Specification** (public API) and **Body** (implementation + private members).\n- The four **cursor attributes** — `%FOUND`, `%NOTFOUND`, `%ROWCOUNT`, `%ISOPEN` — are the most tested concepts in KTU theory questions.\n- **Implicit cursors** use the `SQL%` prefix; **explicit cursors** use the cursor name as prefix.\n- **Parameterized cursors** allow dynamic filtering at OPEN time — `OPEN c_name(value)`.\n- **`FOR UPDATE`** locks rows for concurrent-safe updates; **`CURRENT OF`** uses cached rowids for fast targeted updates.\n- **Package state** (variables declared in body) persists across calls within the same session — first initialization happens when the package is first referenced.\n- **Recompilation independence**: Changing the package body does not invalidate dependent objects; changing the spec does.\n- **Cursors in packages** are *shared* — all sessions see the same cursor declaration, but each session has its own private work area.\n- **Best practice**: Always close cursors in an `EXCEPTION` block too, to prevent resource leaks on error paths.\n- **Syntax traps**: `IS` vs `AS` are interchangeable; semicolons terminate every PL/SQL statement; `END package_name;` followed by `/` on a new line to execute.\n- **Exam mantra**: \"Declare → Open → Fetch → Close\" is the four-step ritual that must be drawn and labeled for any cursor-related KTU question.\n- A package with **only a spec and no body** is valid only if it contains no procedures/functions (only constants/types/cursors).\n- `%ROWCOUNT` reflects the **current session's** count for that specific cursor, not the total rows in the active set.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pccsl408-mExperiment 2-t11",
          "title": "Design a database application using any front-end tool for any problem selected in experiment number 1; the application constructed should have five or more tables",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL408 - DBMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Design a database application using any front-end tool for any problem selected in experiment number 1. The application constructed should have five or more tables\n\n<!-- SECTION_1_START -->\n\n# Module Experiment 2 — Designing a Database Application (≥ 5 Tables)\n\n## 1.1 Core Technical Definition (KTU 2024 Syllabus Terminology)\n\nA **Database Application** is a software system that combines a **Database Management System (DBMS)** as the back-end (storage layer) with a **front-end tool** (presentation + business logic layer) to capture, process, retrieve, and present data in a user-friendly manner. In the KTU 2024 PCCSL408 lab framework, *Experiment 2* requires students to:\n\n1. Select a real-world problem statement (carried forward from **Experiment 1: Database Design using ER/EER Modeling**).\n2. Convert the ER/EER schema into a **fully normalized relational schema** with **5 or more tables**.\n3. Implement it using a **front-end tool** (Java Swing / JavaFX / Python Tkinter / PHP / VB.NET / C#) connected to a back-end RDBMS (Oracle / MySQL / PostgreSQL / MS Access).\n\n> [!IMPORTANT]\n> **KTU Definition Snapshot (Board-Examiner Style):**\n> *\"A database application is a client-side program that issues SQL statements to a remote/local RDBMS through a standardized connectivity API (JDBC/ODBC) to provide CRUD operations, validations, and reporting on a multi-table schema.\"*\n\n| Term | Formal Meaning | KTU-Accepted Examples |\n|---|---|---|\n| **Front-End Tool** | GUI / CLI layer that the user directly interacts with | Java Swing, JavaFX, Tkinter, PHP, VB.NET |\n| **Back-End** | RDBMS storing the persistent data | MySQL, Oracle 21c XE, PostgreSQL, MS Access |\n| **Connectivity Layer** | API translating host-language calls into SQL | JDBC, ODBC, MySQL Connector/Python, PHP `mysqli` |\n| **Schema** | Logical grouping of ≥ 5 related tables | Student, Course, Enrollment, Faculty, Department |\n\n---\n\n## 1.2 Conceptual Analogy — The \"Restaurant Kitchen\" Model\n\nImagine a **restaurant**:\n\n- **Dining Hall** = Front-End Tool (Java Swing form, PHP webpage). The customer (user) places an order through a *waiter* (event handler / button click).\n- **Waiter** = Connectivity API (JDBC driver). He carries the order ticket from the dining hall to the kitchen, and brings the food back.\n- **Kitchen / Pantry** = RDBMS (MySQL). It stores all raw ingredients (records) in labelled shelves (tables).\n- **Recipe Book** = Schema (table definitions, constraints).\n\nWhen you click **\"Save Student\"** in your Java form, the *waiter* (JDBC) carries the data to the *kitchen* (MySQL) which *shelves* the record inside the `STUDENT` table — exactly mirroring how a restaurant order is placed, cooked, and served.\n\n> [!NOTE]\n> **Syllabus Highlight:** KTU expects the student to demonstrate the *complete vertical slice* — Schema Design → DDL → DML → Connectivity → GUI → Validation → Report generation. Skipping any one layer results in a **\"Partially Built Application\"** remark from the examiner.\n\n---\n\n## 1.3 Why \"5 or More Tables\"? — The Normalization Mandate\n\nKTU explicitly mandates **≥ 5 tables** because:\n\n- A **single-table** design violates **1NF/2NF/3NF** (atomicity & dependency rules).\n- **5+ tables** force the student to demonstrate:\n  - **Primary Key – Foreign Key** relationships.\n  - **JOIN operations** (Inner, Left, Right).\n  - **Referential Integrity** (ON DELETE CASCADE, ON UPDATE).\n  - **Many-to-Many** resolution via associative (junction) tables.\n\n> [!TIP]\n> **Examiner's Favourite:** A typical KTU 5-table mini-project contains **2 master tables**, **2 transaction tables**, and **1 junction/associative table** — giving 5 tables minimum. Always keep one extra table (audit log / archive) as a *safety buffer* against questions about scaling.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n# Section 2 — Deep Theoretical Analysis & KTU High-Yield Cheat Sheet\n\n## 2.1 Architecture of a Database Application (Three Tiers)\n\nA robust DB application is built on the **Three-Tier Architecture** prescribed by the KTU 2024 syllabus:\n\n| Tier | Layer | Technology | Responsibility |\n|---|---|---|---|\n| **Tier 1 — Presentation** | GUI / Web UI | Java Swing, JavaFX, PHP, Tkinter | Input forms, output tables, validations |\n| **Tier 2 — Logic** | Business rules | Java methods, Python functions, PHP scripts | Validation, computation, error handling |\n| **Tier 3 — Data** | RDBMS | MySQL, Oracle, PostgreSQL | Storage, retrieval, transaction management |\n\n> [!IMPORTANT]\n> **Two-Tier vs Three-Tier:** KTU accepts *two-tier* (direct client → DB) for small Java Swing projects, but a *three-tier* answer earns **bonus marks** in viva because it shows architectural awareness.\n\n---\n\n## 2.2 Connectivity APIs — The \"Plumbing\" Between Front-End and Back-End\n\nThe four major connectivity APIs (must know their exam-ready differences):\n\n| API | Used By | Vendor-Neutral? | Speed | KTU Verdict |\n|---|---|---|---|---|\n| **JDBC** | Java | Yes (Type-4 driver) | High | ⭐ Preferred for Java projects |\n| **ODBC** | C/C++/VB | Yes (DSN-based) | Medium | Used for MS Access projects |\n| **MySQL Connector/Python** | Python | For MySQL only | High | ⭐ Preferred for Python projects |\n| **PHP `mysqli` / `PDO`** | PHP | For MySQL only | Very High | ⭐ Preferred for Web projects |\n\n### 2.2.1 The 5-Step JDBC Connectivity Model (Memorize this!)\n\nFor a Java-based DB application, the **canonical JDBC flow** is:\n\n1. **Load the Driver Class** — `Class.forName(\"com.mysql.cj.jdbc.Driver\");`\n2. **Establish Connection** — `DriverManager.getConnection(url, user, pass);`\n3. **Create Statement / PreparedStatement** — `con.createStatement();`\n4. **Execute Query** — `executeQuery()` (SELECT) / `executeUpdate()` (INSERT/UPDATE/DELETE)\n5. **Close Resources** — `rs.close(); ps.close(); con.close();`\n\n> [!NOTE]\n> **Best Practice:** KTU examiners reward the use of **`PreparedStatement`** over `Statement` because it prevents **SQL Injection** and improves performance through pre-compilation.\n\n---\n\n## 2.3 KTU Formula Sheet / Cheat Sheet — Application Design Metrics\n\n> **Bolded items** are official KTU expected deliverables.\n\n| # | Concept | Symbol / Metric | KTU-Standard Value / Formula | Unit |\n|---|---|---|---|---|\n| 1 | **Minimum tables** | $N_t$ | $N_t \\geq 5$ | tables |\n| 2 | **Normalization level** | $NF$ | $NF \\geq 3NF$ (Boyce–Codd preferred) | NF-level |\n| 3 | **Foreign Key cardinality** | $1:N$ or $M:N$ | Must justify M:N with junction table | cardinality |\n| 4 | **Connection overhead** | $T_{conn}$ | $T_{conn} \\approx 50{-}200$ ms per session | milliseconds |\n| 5 | **Query response** | $T_q$ | $T_q = T_{parse} + T_{exec} + T_{fetch}$ | ms |\n| 6 | **Transaction ACID** | $A,C,I,D$ | Atomicity, Consistency, Isolation, Durability | property-set |\n| 7 | **Form-to-Table ratio** | $F:R$ | Recommended 1 form per CRUD operation | form/operation |\n| 8 | **Validation rules** | $V$ | $V \\geq 2$ per input field (format + range) | rules |\n\n---\n\n## 2.4 Why This Matters — Real-World Engineering Utility\n\nDatabase applications drive **virtually every commercial software system**:\n\n- **Banking:** Account ↔ Transaction ↔ Customer (≥ 5 tables).\n- **Hospital:** Patient ↔ Doctor ↔ Appointment ↔ Prescription ↔ Bill.\n- **E-Commerce:** User ↔ Product ↔ Cart ↔ Order ↔ Payment.\n- **University ERP (KTU-aligned):** Student ↔ Course ↔ Enrollment ↔ Faculty ↔ Department.\n\n> [!TIP]\n> A student who can confidently build a **5-table** application today can be a **junior backend developer** tomorrow — this experiment is the *gateway skill* of the IT industry.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n# Section 3 — Step-by-Step Development: Worked Example (College Management System)\n\n## 3.1 Step 1 — Choose the Problem (Carry forward from Exp 1)\n\n**Selected Problem:** *College Management System* — to manage Students, Courses, Departments, Faculty, and Enrollments.\n\n> [!NOTE]\n> This problem is **carried over from Experiment 1** (ER/EER design). Re-using it ensures schema consistency, which is exactly what KTU expects.\n\n---\n\n## 3.2 Step 2 — Final Relational Schema (7 tables, well above the 5-table bar)\n\nAfter applying **3NF** to the ER diagram, the schema is:\n\n| Table | Purpose | Primary Key | Foreign Keys |\n|---|---|---|---|\n| **DEPARTMENT** | Master | `dept_id` | — |\n| **FACULTY** | Master | `faculty_id` | `dept_id` |\n| **STUDENT** | Master | `student_id` | `dept_id` |\n| **COURSE** | Master | `course_id` | `dept_id` |\n| **ENROLLMENT** | Junction (M:N) | `(student_id, course_id)` | `student_id`, `course_id` |\n| **EXAM** | Transaction | `exam_id` | `course_id` |\n| **MARKS** | Transaction | `(student_id, exam_id)` | `student_id`, `exam_id` |\n\n**Count = 7 tables** ✔ (exceeds the 5-table requirement).\n\n---\n\n## 3.3 Step 3 — DDL Implementation (SQL)\n\n```sql\n-- 1. DEPARTMENT\nCREATE TABLE department (\n    dept_id      VARCHAR(5)  PRIMARY KEY,\n    dept_name    VARCHAR(50) NOT NULL UNIQUE,\n    hod_name     VARCHAR(50)\n);\n\n-- 2. FACULTY\nCREATE TABLE faculty (\n    faculty_id   VARCHAR(5)  PRIMARY KEY,\n    faculty_name VARCHAR(50) NOT NULL,\n    designation  VARCHAR(30),\n    dept_id      VARCHAR(5),\n    FOREIGN KEY (dept_id) REFERENCES department(dept_id)\n        ON DELETE SET NULL ON UPDATE CASCADE\n);\n\n-- 3. STUDENT\nCREATE TABLE student (\n    student_id   VARCHAR(8)  PRIMARY KEY,\n    student_name VARCHAR(50) NOT NULL,\n    dob          DATE,\n    gender       CHAR(1)     CHECK (gender IN ('M','F','O')),\n    dept_id      VARCHAR(5),\n    FOREIGN KEY (dept_id) REFERENCES department(dept_id)\n        ON DELETE SET NULL ON UPDATE CASCADE\n);\n\n-- 4. COURSE\nCREATE TABLE course (\n    course_id    VARCHAR(6)  PRIMARY KEY,\n    course_name  VARCHAR(50) NOT NULL,\n    credits      INT         CHECK (credits BETWEEN 1 AND 6),\n    dept_id      VARCHAR(5),\n    FOREIGN KEY (dept_id) REFERENCES department(dept_id)\n        ON DELETE SET NULL ON UPDATE CASCADE\n);\n\n-- 5. ENROLLMENT  (junction for M:N)\nCREATE TABLE enrollment (\n    student_id   VARCHAR(8),\n    course_id    VARCHAR(6),\n    enroll_date  DATE DEFAULT CURRENT_DATE,\n    PRIMARY KEY (student_id, course_id),\n    FOREIGN KEY (student_id) REFERENCES student(student_id)\n        ON DELETE CASCADE ON UPDATE CASCADE,\n    FOREIGN KEY (course_id)  REFERENCES course(course_id)\n        ON DELETE CASCADE ON UPDATE CASCADE\n);\n\n-- 6. EXAM\nCREATE TABLE exam (\n    exam_id      VARCHAR(6)  PRIMARY KEY,\n    course_id    VARCHAR(6),\n    exam_date    DATE        NOT NULL,\n    max_marks    INT         CHECK (max_marks > 0),\n    FOREIGN KEY (course_id) REFERENCES course(course_id)\n        ON DELETE CASCADE ON UPDATE CASCADE\n);\n\n-- 7. MARKS\nCREATE TABLE marks (\n    student_id   VARCHAR(8),\n    exam_id      VARCHAR(6),\n    marks_obtained INT      CHECK (marks_obtained >= 0),\n    PRIMARY KEY (student_id, exam_id),\n    FOREIGN KEY (student_id) REFERENCES student(student_id)\n        ON DELETE CASCADE ON UPDATE CASCADE,\n    FOREIGN KEY (exam_id)    REFERENCES exam(exam_id)\n        ON DELETE CASCADE ON UPDATE CASCADE\n);\n```\n\n---\n\n## 3.4 Step 4 — Sample DML (Insertion of Sample Data)\n\n```sql\nINSERT INTO department VALUES\n('D01','Computer Science','Dr. Anand'),\n('D02','Mechanical','Dr. Balan'),\n('D03','Civil','Dr. Chitra');\n\nINSERT INTO student VALUES\n('S2024001','Arjun R','2004-05-12','M','D01'),\n('S2024002','Meera K','2004-08-21','F','D02'),\n('S2024003','Rahul S','2003-11-30','M','D01');\n\nINSERT INTO course VALUES\n('CS201','Data Structures',4,'D01'),\n('CS202','Database Systems',4,'D01'),\n('ME201','Thermodynamics',3,'D02');\n\nINSERT INTO enrollment VALUES\n('S2024001','CS201','2024-07-15'),\n('S2024001','CS202','2024-07-15'),\n('S2024002','ME201','2024-07-15');\n```\n\n---\n\n## 3.5 Step 5 — Java + JDBC Front-End (Critical CRUD Skeleton)\n\nBelow is the **complete, runnable skeleton** for a `StudentForm.java` that performs **Create, Read, Update, Delete (CRUD)**:\n\n```java\nimport java.sql.*;\nimport javax.swing.*;\nimport java.awt.*;\nimport java.awt.event.*;\n\npublic class StudentForm extends JFrame implements ActionListener {\n\n    JLabel l1, l2, l3, l4;\n    JTextField t1, t2, t3, t4;\n    JButton b1, b2, b3, b4;\n    Connection con;\n    PreparedStatement ps;\n    ResultSet rs;\n\n    StudentForm() {\n        setTitle(\"Student Management - KTU DBMS Lab Exp 2\");\n        setSize(420, 320);\n        setLayout(new GridLayout(6, 2));\n        setDefaultCloseOperation(EXIT_ON_CLOSE);\n\n        l1 = new JLabel(\"Student ID\");   t1 = new JTextField();\n        l2 = new JLabel(\"Name\");         t2 = new JTextField();\n        l3 = new JLabel(\"DOB (YYYY-MM-DD)\"); t3 = new JTextField();\n        l4 = new JLabel(\"Dept ID\");      t4 = new JTextField();\n        b1 = new JButton(\"Insert\");\n        b2 = new JButton(\"Display\");\n        b3 = new JButton(\"Update\");\n        b4 = new JButton(\"Delete\");\n\n        add(l1); add(t1);\n        add(l2); add(t2);\n        add(l3); add(t3);\n        add(l4); add(t4);\n        add(b1); add(b2);\n        add(b3); add(b4);\n\n        b1.addActionListener(this);\n        b2.addActionListener(this);\n        b3.addActionListener(this);\n        b4.addActionListener(this);\n\n        dbConnect();\n        setVisible(true);\n    }\n\n    void dbConnect() {\n        try {\n            Class.forName(\"com.mysql.cj.jdbc.Driver\");\n            con = DriverManager.getConnection(\n                \"jdbc:mysql://localhost:3306/ktu_college\",\"root\",\"password\");\n        } catch (Exception e) { JOptionPane.showMessageDialog(this, e); }\n    }\n\n    public void actionPerformed(ActionEvent e) {\n        try {\n            if (e.getSource() == b1) {                 // INSERT\n                ps = con.prepareStatement(\n                    \"INSERT INTO student VALUES(?,?,?,?,?)\");\n                ps.setString(1, t1.getText());\n                ps.setString(2, t2.getText());\n                ps.setString(3, t3.getText());\n                ps.setString(4, \"M\");\n                ps.setString(5, t4.getText());\n                ps.executeUpdate();\n                JOptionPane.showMessageDialog(this, \"Inserted!\");\n            } else if (e.getSource() == b2) {          // DISPLAY\n                ps = con.prepareStatement(\"SELECT * FROM student\");\n                rs = ps.executeQuery();\n                StringBuilder sb = new StringBuilder();\n                while (rs.next()) {\n                    sb.append(rs.getString(1)).append(\" | \")\n                      .append(rs.getString(2)).append(\"\\n\");\n                }\n                JOptionPane.showMessageDialog(this,\n                    sb.length()==0 ? \"No records\" : sb.toString());\n            } else if (e.getSource() == b3) {          // UPDATE\n                ps = con.prepareStatement(\n                    \"UPDATE student SET student_name=?, dept_id=? WHERE student_id=?\");\n                ps.setString(1, t2.getText());\n                ps.setString(2, t4.getText());\n                ps.setString(3, t1.getText());\n                int n = ps.executeUpdate();\n                JOptionPane.showMessageDialog(this, n + \" row(s) updated\");\n            } else if (e.getSource() == b4) {          // DELETE\n                ps = con.prepareStatement(\n                    \"DELETE FROM student WHERE student_id=?\");\n                ps.setString(1, t1.getText());\n                int n = ps.executeUpdate();\n                JOptionPane.showMessageDialog(this, n + \" row(s) deleted\");\n            }\n        } catch (Exception ex) { JOptionPane.showMessageDialog(this, ex); }\n    }\n\n    public static void main(String[] args) {\n        new StudentForm();\n    }\n}\n```\n\n### 3.5.1 Derivation of the CRUD Mapping (Why 4 buttons = 4 SQL verbs)\n\n$$\n\\text{CRUD}_{form} \\;=\\; \\underbrace{C}_{\\text{INSERT}} \\;\\cup\\; \\underbrace{R}_{\\text{SELECT}} \\;\\cup\\; \\underbrace{U}_{\\text{UPDATE}} \\;\\cup\\; \\underbrace{D}_{\\text{DELETE}}\n$$\n\n$$\n\\Rightarrow \\text{Button}_{i} \\;\\longmapsto\\; \\text{SQLVerb}_{i} \\quad \\text{where } i \\in \\{1,2,3,4\\}\n$$\n\nThis bijective mapping is **what the KTU examiner looks for** in your GUI — each button should be **traceable** to exactly **one DML verb**.\n\n---\n\n## 3.6 Step 6 — A JOIN Query (Mandatory Showcase Query)\n\n```sql\n-- List every student with their course names (uses M:N JOIN via enrollment)\nSELECT s.student_id, s.student_name, c.course_id, c.course_name\nFROM   student s\nJOIN   enrollment e ON s.student_id = e.student_id\nJOIN   course    c ON e.course_id  = c.course_id\nORDER  BY s.student_id;\n```\n\n**Why this matters:** The **JOIN of 3+ tables** is the single most-tested KTU viva question on this experiment. Showing a working 3-table JOIN is worth **bonus marks** over a single-table SELECT.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n# Section 4 — Structural Diagrams & Schematics\n\n## 4.1 ER Diagram of the College Management System\n\n```mermaid\nerDiagram\n    DEPARTMENT ||--o{ STUDENT : \"offers admission\"\n    DEPARTMENT ||--o{ FACULTY : \"employs\"\n    DEPARTMENT ||--o{ COURSE  : \"offers\"\n    STUDENT    ||--o{ ENROLLMENT : \"registers\"\n    COURSE     ||--o{ ENROLLMENT : \"is taken by\"\n    COURSE     ||--o{ EXAM       : \"has\"\n    STUDENT    ||--o{ MARKS      : \"scores\"\n    EXAM       ||--o{ MARKS      : \"evaluates\"\n\n    DEPARTMENT {\n        string dept_id PK\n        string dept_name\n        string hod_name\n    }\n    STUDENT {\n        string student_id PK\n        string student_name\n        date   dob\n        char   gender\n        string dept_id FK\n    }\n    FACULTY {\n        string faculty_id PK\n        string faculty_name\n        string designation\n        string dept_id FK\n    }\n    COURSE {\n        string course_id PK\n        string course_name\n        int    credits\n        string dept_id FK\n    }\n    ENROLLMENT {\n        string student_id PK,FK\n        string course_id  PK,FK\n        date   enroll_date\n    }\n    EXAM {\n        string exam_id PK\n        string course_id FK\n        date   exam_date\n        int    max_marks\n    }\n    MARKS {\n        string student_id PK,FK\n        string exam_id    PK,FK\n        int    marks_obtained\n    }\n```\n\n> [!NOTE]\n> The `||--o{` notation in Mermaid ER diagrams means *\"exactly one to zero-or-many\"*, which is the standard KTU cardinality convention for 1:N relationships.\n\n---\n\n## 4.2 Application Architecture Flow (Three-Tier Block Diagram)\n\n```mermaid\nflowchart TD\n    A[User clicks Insert Button on Swing Form] --> B[Java ActionListener triggers]\n    B --> C[JDBC Driver Manager loads MySQL Type-4 Driver]\n    C --> D[Connection established to ktu_college DB]\n    D --> E[PreparedStatement object created with INSERT SQL]\n    E --> F[Parameters set using setString methods]\n    F --> G[executeUpdate fires SQL to MySQL Engine]\n    G --> H{Referential Integrity OK?}\n    H -- Yes --> I[Row inserted into STUDENT table]\n    H -- No --> J[SQLException thrown to Java catch block]\n    I --> K[JOptionPane shows success message]\n    J --> L[JOptionPane shows error dialog]\n    K --> M[Close PreparedStatement and Connection]\n    L --> M\n    M --> N[Form returns to idle state awaiting next click]\n\n    style A fill:#FFD580,stroke:#333\n    style I fill:#A8E6A1,stroke:#333\n    style J fill:#F5A1A1,stroke:#333\n    style K fill:#A8E6A1,stroke:#333\n    style L fill:#F5A1A1,stroke:#333\n```\n\n---\n\n## 4.3 Data-Flow Sequence Diagram (Front-End ↔ Back-End)\n\n```mermaid\nsequenceDiagram\n    actor U as User\n    participant FE as Java Swing Form\n    participant API as JDBC API\n    participant DB as MySQL RDBMS\n\n    U->>FE: Clicks \"Display All Students\"\n    FE->>API: con.prepareStatement(SELECT * FROM student)\n    API->>DB: Sends SQL over TCP/3306\n    DB->>API: Returns ResultSet\n    API->>FE: ResultSet rows\n    FE->>U: Renders rows in JTextArea / JTable\n```\n\n---\n\n## 4.4 Block-Level Functional Architecture of the Full Application\n\n```mermaid\nflowchart LR\n    subgraph FE[\"Tier 1 - Presentation (Java Swing)\"]\n        F1[LoginForm]\n        F2[StudentForm]\n        F3[CourseForm]\n        F4[EnrollmentForm]\n        F5[MarksForm]\n        F6[ReportForm]\n    end\n    subgraph BL[\"Tier 2 - Business Logic (Java methods)\"]\n        B1[Input Validation]\n        B2[CRUD Dispatcher]\n        B3[JOIN Query Builder]\n        B4[Exception Handler]\n    end\n    subgraph DB[\"Tier 3 - Data Layer (MySQL)\"]\n        D1[(STUDENT)]\n        D2[(COURSE)]\n        D3[(ENROLLMENT)]\n        D4[(DEPARTMENT)]\n        D5[(FACULTY)]\n        D6[(EXAM)]\n        D7[(MARKS)]\n    end\n\n    F1 --> B2\n    F2 --> B1 --> B2\n    F3 --> B1 --> B2\n    F4 --> B1 --> B2\n    F5 --> B1 --> B2\n    F6 --> B3\n    B2 --> DB\n    B3 --> DB\n    B2 --> B4\n    B3 --> B4\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n# Section 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### **Q1.** `[KTU University Exam - Model Question Bank]`\n**Define a \"Database Application\". List the two main components of any database application.**\n\n**Model Answer (3 marks):**\nA *Database Application* is a software program that allows a user to interact with an RDBMS through a graphical or command-line interface. The **two main components** are:\n\n1. **Front-End (Presentation Layer):** Java Swing / PHP / Tkinter forms that capture user input.\n2. **Back-End (Data Layer):** RDBMS (MySQL/Oracle) that stores and manages data persistently.\n\n*(+1 mark)* A *Connectivity API* (JDBC/ODBC) acts as the bridge between the two.\n\n---\n\n### **Q2.** `[KTU University Exam - Sample Paper 2024]`\n**Why does KTU mandate \"5 or more tables\" in Experiment 2? Justify with reference to normalization.**\n\n**Model Answer (3 marks):**\nKTU mandates ≥ 5 tables to:\n\n- **Enforce 3NF compliance** (1 mark): a single mega-table invariably violates atomicity and transitive dependency.\n- **Demonstrate relationships** (1 mark): PK–FK links, 1:N, and M:N (via junction tables) can only be meaningfully shown with 5+ tables.\n- **Test JOIN competency** (1 mark): the student must write at least one **3-table JOIN**, which is impossible with fewer tables.\n\n---\n\n## 5.2 Part B — Module Internal Choice (14 Marks)\n\n### **Question A (14 Marks)** `[KTU University Exam - Sample Paper Pattern]`\n\n**(a)** Design a relational schema with **at least 5 tables** for a *Library Management System*. Clearly specify the **primary keys, foreign keys, and cardinalities**. *(7 marks)*\n\n**(b)** Write the **complete Java + JDBC code** to insert a new book record and display all books whose publication year is after 2020. *(7 marks)*\n\n---\n\n### **Model Solution — Q.A(a) — 7 marks**\n\n**Schema (5 tables — Library, Book, Member, Issue, Author) with PKs, FKs and cardinalities:**\n\n| Table | Primary Key | Foreign Keys | Cardinality |\n|---|---|---|---|\n| **LIBRARY** | `lib_id` | — | 1 : N with BOOK |\n| **BOOK** | `book_id` | `lib_id`, `author_id` | N : 1 with LIBRARY, N : 1 with AUTHOR |\n| **AUTHOR** | `author_id` | — | 1 : N with BOOK |\n| **MEMBER** | `member_id` | — | 1 : N with ISSUE |\n| **ISSUE** | `issue_id` | `member_id`, `book_id` | N : 1 with MEMBER, N : 1 with BOOK |\n\n*(+2 marks for cardinality explanation, +2 marks for FK declarations, +2 marks for PK justification, +1 mark for showing M:N resolution if any)*\n\n---\n\n### **Model Solution — Q.A(b) — 7 marks**\n\n**JDBC Code:**\n\n```java\nimport java.sql.*;\nimport javax.swing.*;\nimport java.awt.event.*;\n\npublic class BookApp extends JFrame implements ActionListener {\n    JTextField t1, t2, t3, t4;\n    JButton ins, disp;\n    Connection con; PreparedStatement ps; ResultSet rs;\n\n    BookApp() {\n        setLayout(new java.awt.GridLayout(5, 2));\n        add(new JLabel(\"Book ID\"));    t1 = new JTextField(); add(t1);\n        add(new JLabel(\"Title\"));      t2 = new JTextField(); add(t2);\n        add(new JLabel(\"Author ID\"));  t3 = new JTextField(); add(t3);\n        add(new JLabel(\"Pub Year\"));   t4 = new JTextField(); add(t4);\n        ins = new JButton(\"Insert\");  disp = new JButton(\"Display > 2020\");\n        add(ins); add(disp);\n        ins.addActionListener(this);   disp.addActionListener(this);\n        try {\n            Class.forName(\"com.mysql.cj.jdbc.Driver\");\n            con = DriverManager.getConnection(\n                \"jdbc:mysql://localhost:3306/library\",\"root\",\"pass\");\n        } catch (Exception e) { System.out.println(e); }\n        setSize(400, 250); setVisible(true);\n    }\n\n    public void actionPerformed(ActionEvent e) {\n        try {\n            if (e.getSource() == ins) {                       // INSERT\n                ps = con.prepareStatement(\n                    \"INSERT INTO book VALUES(?,?,?,?)\");\n                ps.setString(1, t1.getText());\n                ps.setString(2, t2.getText());\n                ps.setString(3, t3.getText());\n                ps.setInt(4, Integer.parseInt(t4.getText()));\n                ps.executeUpdate();\n                JOptionPane.showMessageDialog(this, \"Book inserted\");\n            } else {                                          // DISPLAY\n                ps = con.prepareStatement(\n                    \"SELECT book_id, title, pub_year FROM book WHERE pub_year > 2020\");\n                rs = ps.executeQuery();\n                StringBuilder sb = new StringBuilder();\n                while (rs.next()) {\n                    sb.append(rs.getString(1)).append(\" - \")\n                      .append(rs.getString(2)).append(\" - \")\n                      .append(rs.getInt(3)).append(\"\\n\");\n                }\n                JOptionPane.showMessageDialog(this,\n                    sb.length()==0 ? \"No books\" : sb.toString());\n            }\n        } catch (Exception ex) { JOptionPane.showMessageDialog(this, ex); }\n    }\n    public static void main(String[] a) { new BookApp(); }\n}\n```\n\n**Mark Split:** Connection (2) + Insert (2) + Display-with-WHERE (2) + GUI (1).\n\n---\n\n### **Question B (14 Marks)** `[KTU University Exam - Internal Choice Pattern]`\n\n**(a)** Explain the **three-tier architecture** of a database application with a neat diagram. Compare it with **two-tier architecture**. *(7 marks)*\n\n**(b)** Write a **3-table JOIN query** that lists all students along with their enrolled course names and the department offering the course, using the College Management schema defined in Section 3. *(7 marks)*\n\n---\n\n### **Model Solution — Q.B(a) — 7 marks**\n\n**Three-Tier Architecture:**\n\n| Tier | Layer | Technology Example | Responsibility |\n|---|---|---|---|\n| 1 | Presentation | Java Swing / PHP | User input, output rendering |\n| 2 | Application Logic | Java methods / Servlets | Validation, business rules |\n| 3 | Data Storage | MySQL / Oracle | Persistence, queries, transactions |\n\n**Two-Tier vs Three-Tier Comparison:**\n\n| Aspect | Two-Tier | Three-Tier |\n|---|---|---|\n| Layers | Client + DB | Client + App Server + DB |\n| Scalability | Low | High |\n| Security | DB credentials on client | DB hidden behind app server |\n| KTU Viva Score | Acceptable | ⭐ Preferred |\n\n*(+2 marks for diagram, +3 marks for comparison table, +2 marks for explanation)*\n\n---\n\n### **Model Solution — Q.B(b) — 7 marks**\n\n**3-Table JOIN Query:**\n\n```sql\nSELECT  s.student_id,\n        s.student_name,\n        c.course_id,\n        c.course_name,\n        d.dept_name\nFROM    student     s\nJOIN    enrollment  e  ON s.student_id  = e.student_id\nJOIN    course      c  ON e.course_id   = c.course_id\nJOIN    department  d  ON c.dept_id     = d.dept_id\nORDER   BY s.student_id, c.course_id;\n```\n\n**Mark Split:** Correct JOIN syntax (3) + Correct aliases (1) + 4-table JOIN demonstrated (2) + Output justification (1).\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Where Students Typically Lose Marks (Board Examiner's Pet Peeves):**\n> 1. **Using `Statement` instead of `PreparedStatement`** → loses 1–2 marks for SQL-injection vulnerability.\n> 2. **Not closing `ResultSet`, `PreparedStatement`, `Connection`** → loses 1 mark for resource leak.\n> 3. **Hard-coding DB credentials in the GUI class** → examiner deducts 1 mark; extract into a `config.properties` or constants class.\n> 4. **Missing `try-with-resources` or `finally` block** → resource management is a mandatory viva question.\n> 5. **Showing only the INSERT, no UPDATE/DELETE** → application is \"incomplete\" per the KTU definition; lose up to **3 marks**.\n> 6. **Submitting a 1-table or 3-table design** → automatic 5-mark penalty (the 5-table mandate is *non-negotiable*).\n> 7. **No foreign key declaration in DDL** → loses 2 marks; the design is reduced to \"isolated tables\".\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n> [!TIP]\n> **Print this checklist and tape it beside your monitor before the lab exam.**\n\n- [x] **Always start with the ER diagram** of the chosen problem (from Experiment 1).\n- [x] **Convert ER → Relational schema** with at least **5 tables** (3 masters + 1 junction + 1 transaction is the safest template).\n- [x] **Apply 3NF** rigorously — eliminate partial and transitive dependencies.\n- [x] **Use `PreparedStatement`** for *every* SQL execution — never raw `Statement`.\n- [x] **5 mandatory JDBC steps:** Load Driver → Connect → Prepare → Execute → Close.\n- [x] **Always close resources** in a `finally` block or use **try-with-resources**.\n- [x] **Demonstrate all 4 CRUD verbs** in the GUI: Insert, Display, Update, Delete.\n- [x] **Show at least one 3-table JOIN** in the report/demo.\n- [x] **Declare foreign keys with `ON DELETE` / `ON UPDATE`** actions.\n- [x] **Use input validation** in Java *before* sending data to DB (e.g., `Integer.parseInt` in try-catch).\n- [x] **Add at least one CHECK constraint** (e.g., `credits BETWEEN 1 AND 6`).\n- [x] **Include a \"Search\" form** using `WHERE` clause — examiners love it.\n- [x] **In the viva, be ready to explain:** ACID properties, 1NF/2NF/3NF, INNER vs OUTER JOIN, and the difference between `executeQuery()` and `executeUpdate()`.\n- [x] **Mention Three-Tier Architecture** in your viva — instant 1–2 mark bonus.\n- [x] **Document your application** with a 1-page README showing the schema diagram, JDBC URL, and run instructions.\n\n---\n\n**End of Module Experiment 2 Notes — KTU 2024 Scheme | PCCSL408 DBMS LAB**\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pccsl408-mExperiment 2-t12",
          "title": "Perform basic CRUD (Create, Read, Update, Delete) operations on a Cassandra table",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL408 - DBMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Perform basic CRUD (Create, Read, Update, Delete) operations on a Cassandra table\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Academic Definition\n\n**Apache Cassandra** is a highly scalable, distributed **NoSQL (Not Only SQL) database** management system designed to handle very large amounts of structured, semi-structured, and unstructured data across multiple commodity servers, providing high availability with no single point of failure. It uses a **wide-column store** data model and is built on the principles of the **Dynamo paper (Amazon)** combined with **Google's Bigtable** data model.\n\nIn the context of the **KTU 2024 Scheme (PCCSL408 - DBMS Lab)**, Experiment 2 requires students to perform fundamental **CRUD operations** (Create, Read, Update, Delete) on a Cassandra table using the **Cassandra Query Language (CQL)** through the **CQL Shell (cqlsh)** interactive terminal.\n\n| Term | Formal Definition |\n|---|---|\n| **CQL** | Cassandra Query Language - SQL-like syntax for interacting with Cassandra |\n| **cqlsh** | Command-line client for executing CQL commands |\n| **Keyspace** | Top-level container that defines data replication strategy (analogous to a database) |\n| **Table** | A column family consisting of rows and columns (analogous to a relational table) |\n| **Partition Key** | The primary lookup key used to distribute data across cluster nodes |\n| **Clustering Key** | Columns that determine the sort order of rows within a partition |\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight:** This experiment tests Course Outcomes **CO1** (Apply database concepts, DDL, DML commands) and **CO2** (Use NoSQL tools like Cassandra for real-world applications). Students must be able to independently start cqlsh, define a keyspace, and execute all four CRUD operations.\n\n### 1.2 Conceptual Analogy / Intuitive Overview\n\nThink of **Cassandra** as a massive **giant library with no single librarian**. Instead of one central registry (as in MySQL), data is duplicated across many library branches (nodes). If one branch catches fire, the books are still safe in others.\n\nNow consider **CRUD operations** in this library:\n\n- **CREATE (C)** = Adding a brand new book to the library shelves (`INSERT` command)\n- **READ (R)** = Searching for a book by its unique catalog ID (`SELECT` command)\n- **UPDATE (U)** = Editing a book's details (like changing its price or edition) (`UPDATE` command)\n- **DELETE (D)** = Permanently removing a book from the shelves (`DELETE` command)\n\nThe **Keyspace** is like the **library building itself** (it defines rules about how many copies of each book are kept), and the **Table** is like a **specific bookshelf** inside that library (the Student Record shelf, the Library Book shelf, etc.).\n\n> [!NOTE]\n> **Real-world Example:** Companies like **Netflix** (user watch history), **Instagram** (inbox messages), and **Apple** (iMessage data) use Cassandra because it scales linearly — just add more nodes to handle more load. The CRUD operations remain the same regardless of cluster size.\n\n### 1.3 Engineering Metrics and Standards\n\nStandard metrics students must remember for KTU practical examinations:\n\n- **Default Cassandra Port:** **9042** (for CQL native protocol)\n- **Default cqlsh Port:** **9042** (for interactive shell connection)\n- **Default Replication Factor:** **1** (single node) or **3** (production clusters)\n- **cqlsh Prompt:** `cqlsh>` (regular) and `cqlsh:keyspace_name>` (after selecting keyspace)\n- **Standard Data Types:** `text`, `int`, `bigint`, `float`, `double`, `boolean`, `uuid`, `timestamp`, `date`, `time`\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Cheat Sheet\n\n### 2.1 Theoretical Foundation of Cassandra CRUD\n\nUnlike traditional RDBMS (MySQL, PostgreSQL), Cassandra's CRUD model is built on the **CQL (Cassandra Query Language)** standard, which mimics SQL but operates under fundamentally different design principles:\n\n#### 2.1.1 The CAP Theorem Position\n\nCassandra is an **AP system** (Availability + Partition tolerance) in the **CAP theorem**, meaning it prioritizes being always available even during network partitions, with **eventual consistency** rather than strong consistency.\n\n#### 2.1.2 Why CQL Exists\n\nCQL was introduced in **Cassandra 3.0** to lower the learning curve for SQL developers. Before CQL, developers had to use the **Thrift API** which was complex and required object serialization knowledge.\n\n#### 2.1.3 CRUD Mapping Table\n\n| CRUD Operation | SQL Equivalent | CQL Command | Why It Works |\n|---|---|---|---|\n| **C**reate | `INSERT INTO ... VALUES` | `INSERT INTO ... USING TTL` | Writes are append-only; data is written to a commit log first, then to memtable, finally flushed to SSTable on disk |\n| **R**ead | `SELECT ... FROM ... WHERE` | `SELECT ... FROM ... WHERE` | Reads use the partition key to find the correct node; an index lookup is avoided |\n| **U**pdate | `UPDATE ... SET ... WHERE` | `UPDATE ... SET ... WHERE` | Updates are technically **write operations** — the old value is marked as a tombstone, and a new value is written |\n| **D**elete | `DELETE FROM ... WHERE` | `DELETE FROM ... WHERE` | Deletes create a **tombstone** (logical marker) with a TTL; actual data is purged during **compaction** |\n\n### 2.2 KTU High-Yield CQL Cheat Sheet\n\n> [!IMPORTANT]\n> **Mandatory Memorization Table for KTU Practical Exam:** Every cell is a high-yield command pattern that examiners test.\n\n| Operation Category | CQL Command Pattern | Mandatory Syntax Elements | KTU Examiner Focus |\n|---|---|---|---|\n| **Create Keyspace** | `CREATE KEYSPACE name WITH replication = {'class': 'SimpleStrategy', 'replication_factor': N};` | `WITH replication = {...}` clause | Replication strategy choice |\n| **Use Keyspace** | `USE keyspace_name;` | Semicolon terminator | Prompt change |\n| **Create Table** | `CREATE TABLE name (col type, col2 type, PRIMARY KEY (partition_key, clustering_col));` | `PRIMARY KEY` clause mandatory | Composite primary key structure |\n| **Insert Row** | `INSERT INTO name (col1, col2) VALUES (val1, val2);` | Must include **all** primary key components | Tuple order in PRIMARY KEY |\n| **Read All Rows** | `SELECT * FROM name;` | `*` for full select | Limit clause usage |\n| **Read Filtered** | `SELECT * FROM name WHERE partition_key = value;` | Cannot filter on non-indexed columns | Partition key usage |\n| **Update Row** | `UPDATE name SET col = value WHERE partition_key = value;` | Must specify full primary key | SET keyword usage |\n| **Delete Row** | `DELETE FROM name WHERE partition_key = value;` | Must specify full primary key | Tombstone creation |\n| **Drop Table** | `DROP TABLE name;` | Data definition change | Clean-up operation |\n| **Describe** | `DESCRIBE TABLE name;` | Schema introspection | Metadata viewing |\n\n### 2.3 CQL Data Types (Most Common in KTU Labs)\n\n| CQL Type | Equivalent in RDBMS | Storage Size | Example Use Case |\n|---|---|---|---|\n| `text` | `VARCHAR` | Up to 2GB | Names, emails, descriptions |\n| `int` | `INT` | 4 bytes | Age, quantity |\n| `bigint` | `BIGINT` | 8 bytes | Timestamps in ms, large counters |\n| `float` | `FLOAT` | 4 bytes | Temperature readings |\n| `double` | `DOUBLE` | 8 bytes | Precise measurements |\n| `boolean` | `BOOLEAN` | 1 byte | True/false flags |\n| `uuid` | `UUID` | 16 bytes | Unique identifiers across nodes |\n| `timestamp` | `DATETIME` | 8 bytes | Event time, log creation |\n| `date` | `DATE` | 4 bytes | Birth date, registration date |\n\n### 2.4 Real-World Engineering Utility\n\nCassandra's CRUD model powers:\n- **Time-series IoT data** (sensors write millions of inserts per second)\n- **Messaging platforms** (WhatsApp, Facebook Messenger store conversation history)\n- **Recommendation engines** (user-item interactions)\n- **Gaming leaderboards** (frequent updates to player scores)\n- **Financial trading platforms** (high-velocity write transactions)\n\n> [!NOTE]\n> The same CRUD pattern you learn in this KTU lab experiment is used by production engineers at **Uber, Spotify, and Twitter** to handle billions of writes per day. The syntax is identical; only cluster size changes.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Implementation & Analytical Matrix\n\n### 3.1 Pre-Lab Setup Steps\n\nBefore any CRUD operation, the Cassandra environment must be operational. The complete setup sequence is:\n\n**Step 1: Verify Cassandra Installation**\n\n```bash\n# Open terminal and check Cassandra version\ncqlsh --version\n```\n\n**Expected Output:**\n\n```\ncqlsh 6.2.0\n```\n\n**Step 2: Start Cassandra Server (Linux/Mac)**\n\n```bash\n# For systemd-based Linux\nsudo systemctl start cassandra\n\n# For older systems\nsudo service cassandra start\n\n# Or start in foreground for debugging\ncassandra -f\n```\n\n**Step 3: Connect to cqlsh**\n\n```bash\n# Default connection (localhost, port 9042)\ncqlsh\n\n# If Cassandra is on a different host\ncqlsh 192.168.1.100 9042\n```\n\n**Expected Prompt:**\n\n```\nConnected to Test Cluster at 127.0.0.1:9042.\n[cqlsh 6.2.0 | Cassandra 4.1.x | CQL spec 3.4.5 | Native protocol v5]\nUse HELP for help.\ncqlsh>\n```\n\n### 3.2 CREATE Operation — Step-by-Step\n\nThe CREATE operation has **three sub-phases**: Create Keyspace → Use Keyspace → Create Table.\n\n#### 3.2.1 Create a Keyspace\n\n```cql\ncqlsh> CREATE KEYSPACE student_db\n   ... WITH replication = {'class': 'SimpleStrategy', 'replication_factor': 1};\n```\n\n**Explanation of each parameter:**\n\n- `student_db` — Logical name of the keyspace (acts as a database name)\n- `'class': 'SimpleStrategy'` — Replication strategy for **single data center** environments (used in labs)\n- `'replication_factor': 1` — Number of copies of data; **1** is sufficient for a single-node lab setup\n\n> [!IMPORTANT]\n> **For production multi-DC deployments**, use `NetworkTopologyStrategy` instead:\n> ```cql\n> CREATE KEYSPACE prod_db WITH replication = {'class': 'NetworkTopologyStrategy', 'DC1': 3, 'DC2': 2};\n> ```\n\n#### 3.2.2 Use the Keyspace\n\n```cql\ncqlsh> USE student_db;\n```\n\n**Output:** Prompt changes from `cqlsh>` to `cqlsh:student_db>`, confirming the active keyspace.\n\n#### 3.2.3 Create a Table\n\n```cql\ncqlsh:student_db> CREATE TABLE student (\n                ...     roll_no int,\n                ...     name text,\n                ...     department text,\n                ...     cgpa float,\n                ...     PRIMARY KEY (roll_no)\n                ... );\n```\n\n**Explanation of the PRIMARY KEY structure:**\n\n- `roll_no` is the **partition key** — every row is uniquely identified by it\n- Cassandra hashes the partition key to determine which node stores the row\n- Without a PRIMARY KEY, the CREATE TABLE will throw an error\n\n#### 3.2.4 Insert Rows (CREATE record)\n\n```cql\ncqlsh:student_db> INSERT INTO student (roll_no, name, department, cgpa)\n              ... VALUES (101, 'Arjun Krishna', 'CSE', 8.7);\n\ncqlsh:student_db> INSERT INTO student (roll_no, name, department, cgpa)\n              ... VALUES (102, 'Meera Nair', 'ECE', 9.1);\n\ncqlsh:student_db> INSERT INTO student (roll_no, name, department, cgpa)\n              ... VALUES (103, 'Rahul Menon', 'MECH', 7.8);\n```\n\n**Why order matters in VALUES:** The values must correspond positionally (or by name) to the columns specified in the INSERT statement. Always list columns explicitly to avoid silent bugs.\n\n### 3.3 READ Operation — Step-by-Step\n\nThe READ operation uses the `SELECT` statement, which is identical in structure to SQL.\n\n#### 3.3.1 Read All Rows\n\n```cql\ncqlsh:student_db> SELECT * FROM student;\n```\n\n**Expected Output:**\n\n```\n roll_no | department | cgpa | name\n---------+------------+------+----------------\n     103 |       MECH |  7.8 |  Rahul Menon\n     101 |        CSE |  8.7 | Arjun Krishna\n     102 |        ECE |  9.1 |    Meera Nair\n\n(3 rows)\n```\n\n> [!NOTE]\n> Notice the **order is not insertion order**. Cassandra sorts by partition key hash. This is normal and expected behavior.\n\n#### 3.3.2 Read Specific Columns\n\n```cql\ncqlsh:student_db> SELECT name, cgpa FROM student;\n```\n\n#### 3.3.3 Read with WHERE Clause (Partition Key Filter)\n\n```cql\ncqlsh:student_db> SELECT * FROM student WHERE roll_no = 102;\n```\n\n**Expected Output:**\n\n```\n roll_no | department | cgpa | name\n---------+------------+------+------------\n     102 |        ECE |  9.1 | Meera Nair\n\n(1 row)\n```\n\n> [!WARNING]\n> **Common KTU Mistake:** Trying to filter on a **non-primary-key** column (e.g., `WHERE department = 'CSE'`) will fail unless an index is created. This is a **frequently asked exam question**.\n\n#### 3.3.4 Read with LIMIT\n\n```cql\ncqlsh:student_db> SELECT * FROM student LIMIT 2;\n```\n\n### 3.4 UPDATE Operation — Step-by-Step\n\nUpdates in Cassandra are **not** in-place modifications. They are implemented as **write operations** that create a new version of the row and mark the old version as a tombstone.\n\n```cql\ncqlsh:student_db> UPDATE student\n              ... SET cgpa = 8.9, department = 'CSE-AI'\n              ... WHERE roll_no = 101;\n```\n\n**Explanation of the WHERE clause:** The WHERE clause **must include the partition key** (and clustering key, if defined). Otherwise, Cassandra throws the error:\n\n```\nInvalidRequest: Error from server: code=2200 [Invalid query]\nmessage=\"Cannot execute UPDATE without all PRIMARY KEY columns\"\n```\n\n#### 3.4.1 Verify the Update\n\n```cql\ncqlsh:student_db> SELECT * FROM student WHERE roll_no = 101;\n```\n\n**Expected Output:**\n\n```\n roll_no | department | cgpa | name\n---------+------------+------+----------------\n     101 |     CSE-AI |  8.9 | Arjun Krishna\n```\n\n#### 3.4.2 Update with TTL (Time To Live)\n\nCassandra supports automatic expiration of data:\n\n```cql\ncqlsh:student_db> UPDATE student USING TTL 3600\n              ... SET cgpa = 9.5\n              ... WHERE roll_no = 102;\n```\n\nThis row's new value will **automatically delete itself after 3600 seconds (1 hour)**. The underlying mechanism is a built-in tombstone with a timestamp.\n\n### 3.5 DELETE Operation — Step-by-Step\n\nThe DELETE operation in Cassandra creates a **tombstone** (a logical delete marker), not a physical row removal. Physical deletion happens later during **compaction**.\n\n```cql\ncqlsh:student_db> DELETE FROM student WHERE roll_no = 103;\n```\n\n**Expected Output:** No rows are returned immediately, but verification shows the row is gone.\n\n#### 3.5.1 Verify the Deletion\n\n```cql\ncqlsh:student_db> SELECT * FROM student;\n```\n\n**Expected Output:**\n\n```\n roll_no | department | cgpa | name\n---------+------------+------+----------------\n     101 |     CSE-AI |  8.9 | Arjun Krishna\n     102 |        ECE |  9.1 |    Meera Nair\n\n(2 rows)\n```\n\n#### 3.5.2 Delete a Specific Column\n\n```cql\ncqlsh:student_db> DELETE cgpa FROM student WHERE roll_no = 102;\n```\n\nThis deletes **only the `cgpa` column** of roll_no 102, leaving the rest intact.\n\n#### 3.5.3 Drop the Table (End of Lab Cleanup)\n\n```cql\ncqlsh:student_db> DROP TABLE student;\ncqlsh:student_db> DROP KEYSPACE student_db;\n```\n\n### 3.6 KTU Analytical Matrix — CRUD Syntax Breakdown\n\n| Operation | Minimum Required Clause | Forbidden Patterns in KTU Exam | Common Error Message |\n|---|---|---|---|\n| **CREATE** | `PRIMARY KEY (...)` | No PRIMARY KEY definition | \"No PRIMARY KEY specified\" |\n| **INSERT** | All PRIMARY KEY components | Missing primary key columns | \"Some partition key parts are missing\" |\n| **READ** | `SELECT` keyword | `SELECT *` discouraged for production | Generic permission errors |\n| **UPDATE** | `SET col = val WHERE pk = val` | Omitting WHERE clause | \"Cannot execute UPDATE without PRIMARY KEY\" |\n| **DELETE** | `WHERE pk = val` | Omitting WHERE clause | \"Cannot execute DELETE without PRIMARY KEY\" |\n\n### 3.7 Complete Working Example (Copy-Paste Ready for KTU Lab)\n\n```cql\n-- STEP 1: Create Keyspace\nCREATE KEYSPACE college\n  WITH replication = {'class': 'SimpleStrategy', 'replication_factor': 1};\n\n-- STEP 2: Use Keyspace\nUSE college;\n\n-- STEP 3: Create Table\nCREATE TABLE library_books (\n    book_id int,\n    title text,\n    author text,\n    year int,\n    PRIMARY KEY (book_id)\n);\n\n-- STEP 4: CREATE Operation (Insert)\nINSERT INTO library_books (book_id, title, author, year)\nVALUES (1, 'Database System Concepts', 'Korth', 2019);\n\nINSERT INTO library_books (book_id, title, author, year)\nVALUES (2, 'Clean Code', 'Robert Martin', 2008);\n\nINSERT INTO library_books (book_id, title, author, year)\nVALUES (3, 'Introduction to Algorithms', 'Cormen', 2009);\n\n-- STEP 5: READ Operation\nSELECT * FROM library_books;\n\nSELECT * FROM library_books WHERE book_id = 2;\n\n-- STEP 6: UPDATE Operation\nUPDATE library_books SET year = 2021 WHERE book_id = 1;\n\n-- STEP 7: DELETE Operation\nDELETE FROM library_books WHERE book_id = 3;\n\n-- STEP 8: Verify Final State\nSELECT * FROM library_books;\n\n-- STEP 9: Cleanup (Optional)\nDROP TABLE library_books;\nDROP KEYSPACE college;\n```\n\n> [!IMPORTANT]\n> **For KTU Practical Exam:** Always show **all CRUD operations** with **outputs** in the lab record. Examiners specifically check for the **prompt change** from `cqlsh>` to `cqlsh:college>` after the `USE` command.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Mermaid Flowchart — Complete CRUD Lifecycle on a Cassandra Table\n\n```mermaid\nflowchart TD\n    Start([Start cqlsh]) --> CreateKeyspace[CREATE KEYSPACE college<br/>SimpleStrategy, RF=1]\n    CreateKeyspace --> UseKS[USE college]\n    UseKS --> CreateTable[CREATE TABLE student<br/>roll_no PK int]\n    CreateTable --> Insert1[INSERT roll_no 101<br/>Arjun CSE 8.7]\n    CreateTable --> Insert2[INSERT roll_no 102<br/>Meera ECE 9.1]\n    CreateTable --> Insert3[INSERT roll_no 103<br/>Rahul MECH 7.8]\n    Insert1 --> SelectAll[SELECT FROM student]\n    Insert2 --> SelectAll\n    Insert3 --> SelectAll\n    SelectAll --> SelectFilter[SELECT WHERE roll_no equals 102]\n    SelectFilter --> UpdateOp[UPDATE SET cgpa equals 8.9<br/>WHERE roll_no equals 101]\n    UpdateOp --> VerifyUpdate[SELECT WHERE roll_no equals 101]\n    VerifyUpdate --> DeleteOp[DELETE WHERE roll_no equals 103]\n    DeleteOp --> VerifyDelete[SELECT FROM student]\n    VerifyDelete --> End([Lab Complete])\n```\n\n### 4.2 Mermaid Block Diagram — Cassandra CRUD Operation Architecture\n\n```mermaid\nflowchart LR\n    Client[Student Client] -->|CQL via 9042| Node1[Cassandra Node 1]\n    Node1 --> CommitLog[Commit Log Disk]\n    Node1 --> MemTable[In-Memory MemTable]\n    MemTable -->|Flush| SSTable[SSTable on Disk]\n    SSTable --> Compaction[Background Compaction]\n    Compaction -->|Removes tombstones| CleanedData[Cleaned SSTable]\n    Node1 -->|Replication| Node2[Cassandra Node 2]\n    Node1 -->|Replication| Node3[Cassandra Node 3]\n    Node2 --> ReadResponse[Read Response to Client]\n    Node3 --> ReadResponse\n    ReadResponse --> Client\n```\n\n### 4.3 Mermaid Schema Diagram — Table Structure\n\n```mermaid\nflowchart LR\n    TableBox[\"Table: student\"] --> PK[\"PRIMARY KEY: roll_no INT\"]\n    TableBox --> Col1[\"name TEXT\"]\n    TableBox --> Col2[\"department TEXT\"]\n    TableBox --> Col3[\"cgpa FLOAT\"]\n    PK --> Hash[\"Hash Function<br/>Murmur3\"]\n    Hash --> NodeA[Node A]\n    Hash --> NodeB[Node B]\n    Hash --> NodeC[Node C]\n    NodeA --> Row1[Row 101: Arjun]\n    NodeB --> Row2[Row 102: Meera]\n    NodeC --> Row3[Row 103: Rahul]\n```\n\n### 4.4 Mermaid State Diagram — UPDATE vs DELETE Internals\n\n```mermaid\nstateDiagram-v2\n    [*] --> ActiveRow\n    ActiveRow --> OldVersion: Initial Write\n    OldVersion --> NewVersion: UPDATE SET col = val\n    NewVersion --> Tombstoned: DELETE WHERE pk = val\n    Tombstoned --> CompactionPhase: gc_grace_seconds expires\n    CompactionPhase --> PhysicallyRemoved: Purged from SSTable\n    PhysicallyRemoved --> [*]\n```\n\n> [!NOTE]\n> **Interpretation of Diagram 4.4:** This state machine illustrates a critical KTU concept — **UPDATE and DELETE in Cassandra are both write operations**. They produce tombstones, not in-place changes. The actual data is removed only when the **gc_grace_seconds** window (default 10 days) elapses and compaction runs.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n**Q1. [KTU University Exam - May 2024]**\n**What is a Keyspace in Cassandra? How is it different from a database in MySQL?**\n\n**Model Answer (3 marks):**\n\nA **Keyspace** in Cassandra is the top-level container that defines a **replication strategy** and a **replication factor** for a set of tables. It is analogous to a database in MySQL but with a critical difference: a keyspace **mandatorily specifies how data is replicated across nodes** at the time of creation.\n\n```cql\nCREATE KEYSPACE student_db\nWITH replication = {'class': 'SimpleStrategy', 'replication_factor': 3};\n```\n\nIn MySQL, a database is just a logical namespace with no replication rules — those are handled by external cluster configuration. In Cassandra, replication is part of the **DDL definition itself**. A keyspace in Cassandra also defines a **data center strategy** (`NetworkTopologyStrategy`) for geographically distributed deployments.\n\n**Allocation:** Definition 1 mark + CQL example 1 mark + MySQL contrast 1 mark.\n\n---\n\n**Q2. [KTU University Exam - Dec 2023]**\n**Why must every CREATE TABLE statement in Cassandra include a PRIMARY KEY? What happens if it is omitted?**\n\n**Model Answer (3 marks):**\n\nA **PRIMARY KEY** in Cassandra serves **two critical functions**:\n\n1. **Partition Key Identification** — It defines which column(s) determine the physical node on which a row is stored. Cassandra uses a hash function (Murmur3) on the partition key to distribute data.\n\n2. **Uniqueness Within Partition** — It ensures each row can be uniquely addressed via `WHERE pk = value` lookups.\n\nIf a PRIMARY KEY is **omitted**, Cassandra throws the error:\n\n```\nInvalidRequest: No PRIMARY KEY specified\n```\n\nThe CREATE TABLE statement **fails entirely** — the table is not created. This is fundamentally different from MySQL, where a table can be created without a primary key (though not recommended). Cassandra's distributed architecture **requires** every row to be addressable via its key.\n\n**Allocation:** Function 1 mark 1 + Hash distribution 1 mark + Error behavior 1 mark.\n\n---\n\n### 5.2 Part B — Module Internal Choice Questions (14 Marks Each)\n\n#### Question A (14 Marks) — Option 1\n\n**Q3(a). [KTU University Exam - May 2024, Modified]**\n**Write a complete CQL script to perform all four CRUD operations on a `product` table with columns: `pid` (int, primary key), `pname` (text), `price` (float), and `category` (text). Demonstrate all operations with at least 3 records.** **(7 marks)**\n\n**Model Solution (7 marks):**\n\n```cql\n-- CREATE Keyspace\nCREATE KEYSPACE inventory\n  WITH replication = {'class': 'SimpleStrategy', 'replication_factor': 1};\n\nUSE inventory;\n\n-- CREATE Table\nCREATE TABLE product (\n    pid int,\n    pname text,\n    price float,\n    category text,\n    PRIMARY KEY (pid)\n);\n\n-- CREATE Operation: Insert 3 records\nINSERT INTO product (pid, pname, price, category)\nVALUES (1, 'Laptop', 75000.0, 'Electronics');\n\nINSERT INTO product (pid, pname, price, category)\nVALUES (2, 'Office Chair', 8500.5, 'Furniture');\n\nINSERT INTO product (pid, pname, price, category)\nVALUES (3, 'Notebook', 45.0, 'Stationery');\n\n-- READ Operation: Display all records\nSELECT * FROM product;\n\n-- UPDATE Operation: Increase price of pid=1\nUPDATE product SET price = 78000.0 WHERE pid = 1;\n\n-- DELETE Operation: Remove pid=3\nDELETE FROM product WHERE pid = 3;\n\n-- Final READ to verify\nSELECT * FROM product;\n```\n\n**Verification of Output:**\n\n```\n pid | pname         | price   | category\n-----+---------------+---------+------------\n   1 |       Laptop |  78000  | Electronics\n   2 | Office Chair | 8500.5  | Furniture\n```\n\n**Mark Allocation:** Keyspace+Table 2 marks, INSERTs 2 marks, SELECT+UPDATE+DELETE 3 marks.\n\n---\n\n**Q3(b). [KTU University Exam - Dec 2023]**\n**Explain the difference between an SQL UPDATE and a Cassandra UPDATE. Why is Cassandra's update considered a \"write operation\" internally? Illustrate with a diagram.** **(7 marks)**\n\n**Model Solution (7 marks):**\n\n| Aspect | SQL UPDATE | Cassandra UPDATE |\n|---|---|---|\n| **Mechanism** | In-place modification of the row on disk | Creates a new row version, marks old as tombstone |\n| **I/O Pattern** | Read-Modify-Write cycle | Pure Write (append to memtable) |\n| **Performance** | Slower for high-frequency updates | Faster — single write path |\n| **Disk I/O** | Reuses the same physical location | New SSTable segment created |\n\n**Why it is a Write Operation:**\n\nIn Cassandra, the storage engine is **log-structured merge (LSM tree)** based. When `UPDATE table SET col = newvalue WHERE pk = X` is executed:\n\n1. Cassandra looks up the partition hash for `pk = X`\n2. It writes a **new row** to the commit log (durability) and memtable (in-memory)\n3. The previous version of the row becomes a **tombstone** when it is overwritten\n4. The memtable is periodically flushed to a new SSTable on disk\n5. The old SSTable containing the old version is **not modified** — it gets cleaned up only during compaction\n\n```mermaid\nflowchart LR\n    SQL[\"SQL: UPDATE\"] --> Read[\"Read Old Row\"]\n    Read --> Modify[\"In-Place Modify\"]\n    Modify --> WriteBack[\"Write Back\"]\n    WriteBack --> SQLDone([Updated])\n\n    Cas[\"Cassandra: UPDATE\"] --> Hash[\"Hash Partition Key\"]\n    Hash --> NewWrite[\"Write New Version\"]\n    NewWrite --> Tombstone[\"Old Version becomes Tombstone\"]\n    Tombstone --> CasDone([Updated - Same I/O Cost as Insert])\n```\n\n**Mark Allocation:** Comparison table 3 marks, LSM explanation 3 marks, Diagram 1 mark.\n\n---\n\n#### Question B (14 Marks) — Option 2\n\n**Q4(a). [KTU University Exam - May 2023]**\n**Consider an Employee table with columns `emp_id` (int), `emp_name` (text), `salary` (float), and `dept` (text). Write the CQL commands to: (i) create a keyspace `org_db` with replication factor 1, (ii) create the table with `emp_id` as primary key, (iii) insert 4 records, (iv) update salary of emp_id 102, (v) delete record of emp_id 104.** **(7 marks)**\n\n**Model Solution (7 marks):**\n\n```cql\n-- (i) Create Keyspace\nCREATE KEYSPACE org_db\n  WITH replication = {'class': 'SimpleStrategy', 'replication_factor': 1};\n\nUSE org_db;\n\n-- (ii) Create Table\nCREATE TABLE employee (\n    emp_id int,\n    emp_name text,\n    salary float,\n    dept text,\n    PRIMARY KEY (emp_id)\n);\n\n-- (iii) Insert 4 records\nINSERT INTO employee (emp_id, emp_name, salary, dept)\nVALUES (101, 'Anand Kumar', 55000.0, 'CSE');\n\nINSERT INTO employee (emp_id, emp_name, salary, dept)\nVALUES (102, 'Priya Sharma', 62000.0, 'ECE');\n\nINSERT INTO employee (emp_id, emp_name, salary, dept)\nVALUES (103, 'Vivek Nair', 48000.0, 'MECH');\n\nINSERT INTO employee (emp_id, emp_name, salary, dept)\nVALUES (104, 'Sara Joseph', 71000.0, 'CSE');\n\n-- (iv) Update salary\nUPDATE employee SET salary = 65000.0 WHERE emp_id = 102;\n\n-- (v) Delete record\nDELETE FROM employee WHERE emp_id = 104;\n```\n\n**Verification Query:**\n\n```cql\nSELECT * FROM employee;\n```\n\n**Expected Output:**\n\n```\n emp_id | dept | emp_name      | salary\n--------+------+---------------+--------\n    101 |  CSE |  Anand Kumar  |  55000\n    102 |  ECE | Priya Sharma  |  65000\n    103 | MECH |   Vivek Nair  |  48000\n```\n\n(emp_id 104 Sara Joseph is deleted.)\n\n**Mark Allocation:** (i) 1 mark, (ii) 1.5 marks, (iii) 2 marks, (iv) 1.5 marks, (v) 1 mark.\n\n---\n\n**Q4(b). [KTU University Exam - Dec 2024, Expected]**\n**Explain the concept of Tombstones in Cassandra. How are they created? What is `gc_grace_seconds` and why is it important? What happens if a node is down for longer than `gc_grace_seconds`?** **(7 marks)**\n\n**Model Solution (7 marks):**\n\nA **Tombstone** in Cassandra is a **logical marker** that indicates a row or column has been deleted. It is **not** a physical deletion. Tombstones are essential for maintaining consistency in a distributed environment.\n\n**How Tombstones Are Created:**\n\n1. When a `DELETE` statement is executed, Cassandra does not remove the data immediately.\n2. Instead, it writes a **tombstone record** with a timestamp and a marker indicating the deletion.\n3. The tombstone is replicated to all nodes that hold a copy of that partition.\n4. When `READ` queries encounter a tombstone, they treat the row as deleted.\n\n**`gc_grace_seconds` (Garbage Collection Grace Seconds):**\n\n- It is the **time period (default: 10 days / 864000 seconds)** during which tombstones are retained in SSTables.\n- During this window, compaction can reconcile the tombstone with the deleted row and physically remove it.\n- This parameter is critical because it allows time for **anti-entropy repair** to propagate tombstones to nodes that may have been offline.\n\n**What Happens if a Node is Down Longer than `gc_grace_seconds`:**\n\n```mermaid\nflowchart LR\n    NodeA[Node A Online] -->|DELETE issued| TombA[Tombstone created at A]\n    NodeA -.->|Replication fails| NodeB[Node B Offline]\n    TombA -->|Time passes| ExceedsGC[Exceeds gc_grace_seconds]\n    ExceedsGC --> Compaction[Compaction runs on A]\n    Compaction --> PurgedA[Tombstone physically removed from A]\n    PurgedA --> NodeBReturns[Node B comes back online]\n    NodeBReturns --> ResurrectionProblem[\"STALE DATA RESURRECTION! Node B has no tombstone\"]\n    ResurrectionProblem --> Repair[Manual repair required]\n```\n\n**The Resurrected Zombie Problem:** If `gc_grace_seconds` expires and the tombstone is purged from Node A, but Node B was offline and missed the DELETE, when Node B comes back online, it still has the **old (deleted) row**. Since there is no tombstone, the row is treated as **live** and \"resurrects\" — this is a serious data integrity issue.\n\n**Mitigation:** Run `nodetool repair` regularly on all nodes within `gc_grace_seconds` intervals.\n\n**Mark Allocation:** Tombstone definition 1.5 marks, creation process 1.5 marks, gc_grace_seconds 1.5 marks, Zombie resurrection 2.5 marks.\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Frequent Mark Deduction Traps in KTU Lab Evaluation:**\n>\n> 1. **Forgetting `USE keyspace_name;` before CREATE TABLE** — causes \"Keyspace does not exist\" error. **(Lose 1 mark)**\n> 2. **Missing semicolons `;`** — CQL does not always auto-terminate. Statements fail silently. **(Lose 0.5 mark)**\n> 3. **Omitting PRIMARY KEY in CREATE TABLE** — table creation fails completely. **(Lose 1.5 marks)**\n> 4. **Using `WHERE` on non-primary-key columns without secondary index** — query fails. **(Lose 1 mark)**\n> 5. **UPDATE/DELETE without full PRIMARY KEY in WHERE clause** — error: \"PRIMARY KEY column required\". **(Lose 1.5 marks)**\n> 6. **Confusing partition key with clustering key** — students often write `PRIMARY KEY (roll_no, name)` thinking name is just a column. **(Lose 2 marks — concept error)**\n> 7. **Not including output screenshots in the lab record** — examiners deduct marks for missing verification. **(Lose 1 mark)**\n> 8. **Using `DROP KEYSPACE` without backing up** — in case of re-execution, data is lost. **(Lose 0.5 mark)**\n> 9. **Assuming insertion order is preserved** — Cassandra sorts by partition hash, not insertion time. **(Lose 0.5 mark)**\n> 10. **Forgetting to start Cassandra server before running cqlsh** — connection refused error. **(Lose 1 mark)**\n\n### 5.4 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Rapid Revision Checklist (Last-Minute KTU Exam Prep):**\n>\n> - **Cassandra** is a **distributed, decentralized, NoSQL wide-column store** designed for high availability and linear scalability.\n> - **CRUD** = **C**reate, **R**ead, **U**pdate, **D**elete — the four fundamental persistent storage operations.\n> - **cqlsh** is the interactive shell to execute CQL commands. Default port: **9042**.\n> - **Keyspace** = top-level container (analogous to a database) **with mandatory replication strategy**.\n> - **Replication strategies:** `SimpleStrategy` (single DC, used in labs) and `NetworkTopologyStrategy` (production multi-DC).\n> - **Replication factor** = number of copies of data stored across nodes.\n> - **Table** = a column family defined with mandatory **PRIMARY KEY**.\n> - **PRIMARY KEY = Partition Key + Clustering Key** (clustering key is optional).\n> - **INSERT** syntax: `INSERT INTO table (cols) VALUES (vals);` — must include **all PRIMARY KEY columns**.\n> - **SELECT** syntax: `SELECT cols FROM table WHERE pk = val;` — filter only on PRIMARY KEY (or indexed columns).\n> - **UPDATE** in Cassandra is technically a **write operation** that creates a new version and tombstones the old.\n> - **DELETE** creates a **tombstone** (logical marker) — physical deletion happens during **compaction**.\n> - **Tombstones** are retained for `gc_grace_seconds` (default 10 days) for repair safety.\n> - **Default port:** **9042** for CQL native protocol.\n> - **Common data types:** `int`, `text`, `float`, `double`, `boolean`, `uuid`, `timestamp`, `date`, `bigint`.\n> - **TTL** (Time To Live) feature allows automatic data expiration.\n> - **CQL is case-insensitive** for keywords but **case-sensitive** for string values.\n> - **Always show output verification** in lab records — KTU examiners value proof of execution.\n> - **Cassandra sorts output by partition hash**, not by insertion order — do not be surprised by reordered results.\n> - **Use `DESCRIBE TABLE name;`** to view table schema — useful for debugging in viva.\n> - **Lab exam tip:** Start Cassandra first, then cqlsh, then create keyspace, then create table, then perform CRUD in order.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pccsl408-mExperiment 2-t13",
          "title": "Write and execute CQL queries to retrieve specific data from Cassandra tables",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL408 - DBMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Write and execute CQL queries to retrieve specific data from Cassandra tables\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\n**Cassandra Query Language (CQL)** is the declarative query language for Apache Cassandra, syntactically similar to SQL but architecturally constrained to support Cassandra's distributed, peer-to-peer, eventually-consistent data model. The retrieval operation in CQL is performed using the `SELECT` statement, which fetches specific columns and rows from a Cassandra table (column family) based on equality and range predicates applied to **partition keys** and **clustering columns**.\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight (PCCSL408 - Module 2):**\n> Students must be able to *write, execute, and interpret* CQL queries involving `SELECT`, `WHERE`, `ORDER BY`, `LIMIT`, and `ALLOW FILTERING` clauses against a populated Cassandra keyspace. The emphasis is on understanding *why* certain queries are rejected by Cassandra and how to redesign them.\n\n## 1.2 Conceptual Analogy / Intuition\n\n> [!NOTE]\n> **The Distributed Library Analogy**\n>\n> Imagine a massive library where books are **not stored on one giant shelf** but are spread across **10,000 smaller libraries** (nodes) in different cities. Each small library holds books that share the *same category and author* (the **partition key**). Within each small library, books are arranged **alphabetically by title** (the **clustering column**).\n>\n> When you want a book:\n> - If you know the **category + author** (partition key) → the librarian instantly knows *which small library* has it. **Lightning fast.**\n> - If you only know the **title** but not the author → the librarian must check *all 10,000 libraries*. This is a **full table scan**, which Cassandra refuses by default (anti-pattern).\n> - If you want all books by an author, sorted by title → the librarian returns a *naturally ordered slice* — this is the **`ORDER BY`** operation.\n>\n> **CQL retrieval is the act of telling Cassandra: \"Give me the slice of data that lives on the same partition, ordered by its natural clustering.\"**\n\n## 1.3 Key Vocabulary (KTU Board Terminology)\n\n| Term | Meaning |\n|---|---|\n| **Keyspace** | The top-level container (analogous to a *database* in RDBMS) that defines replication strategy. |\n| **Table** (Column Family) | A set of rows sharing the same partition key structure. |\n| **Partition Key** | The column(s) that determine *on which node* the row is stored. |\n| **Clustering Column** | The column(s) that determine the *sort order of rows* within a partition. |\n| **CQLSH** | The interactive command-line shell for executing CQL queries. |\n| **ALLOW FILTERING** | A clause that permits queries on non-indexed, non-key columns (used as a *last resort*). |\n\n> [!IMPORTANT]\n> **Exam Fact (Memorize):** In Cassandra, every retrieval operation is fundamentally a **partition read**, optionally refined by clustering-column range predicates. This single fact explains 90% of all CQL retrieval rules.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Anatomy of a CQL `SELECT` Statement\n\nA CQL retrieval query is a *layered construction*. Each layer adds a constraint or transformation to the result set. Understanding the layered model is the key to writing valid CQL.\n\n### Layer 1 — The Mandatory Skeleton\n```sql\nSELECT  <column_list | *> \nFROM    <keyspace_name>.<table_name>;\n```\n- `*` returns all columns.\n- Specifying a column list is **strongly preferred** in distributed systems (reduces network I/O).\n\n### Layer 2 — Filtering with `WHERE`\n```sql\nWHERE  partition_key = value\n  AND  clustering_col  = value   -- equality\n  AND  clustering_col  >  value  -- range (only on clustering)\n```\n**Hard Rule:** `WHERE` predicates **must** include the partition key, otherwise Cassandra demands `ALLOW FILTERING`.\n\n### Layer 3 — Ordering with `ORDER BY`\n```sql\nORDER BY clustering_col ASC | DESC;\n```\n- The column in `ORDER BY` **must be a clustering column**.\n- Sorting across partitions is **not allowed** (you can only sort *within* a partition).\n\n### Layer 4 — Pagination with `LIMIT` and `PER PARTITION LIMIT`\n```sql\nLIMIT 10;                              -- global row cap\nPER PARTITION LIMIT 5;                 -- cap per partition\n```\n\n### Layer 5 — The Escape Hatch (`ALLOW FILTERING`)\n```sql\nWHERE non_key_column = 'value' ALLOW FILTERING;\n```\n- Permits queries on **non-indexed, non-key columns** by performing a cluster-wide scan.\n- **Performance penalty** — used only when no better alternative exists.\n\n## 2.2 The \"KTU High-Yield\" CQL Retrieval Cheat Sheet\n\n> [!IMPORTANT]\n> The following table is the **single most important reference** for your lab exam. Memorize the *rule* column.\n\n| # | Clause | Permitted Targets | Forbidden Targets | KTU Rule |\n|---|---|---|---|---|\n| 1 | `=` on Partition Key | ✅ Allowed | — | Equality is the **fastest** retrieval. |\n| 2 | `=` on Clustering Column | ✅ Allowed | — | Refines row within a partition. |\n| 3 | `>`, `<`, `>=`, `<=`, `BETWEEN` | ✅ Only on **clustering columns** | ❌ Never on partition key or regular columns | Range scans work *inside* a partition only. |\n| 4 | `IN` | ✅ On last partition-key column OR clustering column | ❌ On the first partition-key column | `IN` is supported but limited. |\n| 5 | `ORDER BY` | ✅ On clustering column | ❌ On partition key or non-key column | Sorting is per-partition. |\n| 6 | `ALLOW FILTERING` | ✅ Anywhere | Performance killer | Reserved for ad-hoc analytics. |\n| 7 | `LIMIT n` | ✅ Always | — | Returns first `n` rows. |\n| 8 | `PER PARTITION LIMIT n` | ✅ Always | — | Returns first `n` per partition. |\n| 9 | `DISTINCT` | ❌ Not supported in CQL | — | Cassandra discourages it. |\n| 10 | `JOIN` | ❌ Not supported | — | Use **denormalization** instead. |\n\n## 2.3 Why These Rules Exist — The \"Why\" Behind CQL\n\n- **Distributed reads are partition-bound.** Once Cassandra knows the partition key, it can compute the *exact node* holding the data via consistent hashing. Any query that *cannot* be resolved to a partition forces a scatter-gather operation across the entire cluster.\n- **No JOINs / no subqueries** because Cassandra has no concept of referential integrity — relationships are *baked into the data model* through denormalization.\n- **`ALLOW FILTERING` is the equivalent of a \"full table scan\" in SQL.** It is functionally correct but operationally dangerous in production.\n\n## 2.4 Real-World Engineering Utility\n\nCassandra-style retrieval is the backbone of:\n- **Netflix** — viewing history per user (partition: user_id, cluster: timestamp).\n- **Instagram** — photo timeline per user.\n- **IoT telemetry** — sensor readings per device, ordered by time.\n- **Messaging apps** — chat history per conversation.\n\nIn all these systems, the access pattern is: *\"Give me all data for **this one entity**, sorted by time.\"* — which maps directly to a partition key + clustering column lookup.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 The Reference Schema (Used Throughout)\n\nFor the KTU lab exam, you will typically be given (or must create) a schema such as the one below. We will build retrievals **incrementally** on top of this schema.\n\n```sql\n-- Step 1: Create a keyspace\nCREATE KEYSPACE IF NOT EXISTS university\nWITH replication = {'class': 'SimpleStrategy', 'replication_factor': 1};\n\nUSE university;\n\n-- Step 2: Create the table\nCREATE TABLE student_marks (\n    student_id   INT,           -- PARTITION KEY\n    semester     INT,           -- CLUSTERING COLUMN 1\n    subject      TEXT,          -- CLUSTERING COLUMN 2\n    marks        INT,\n    grade        TEXT,\n    PRIMARY KEY (student_id, semester, subject)\n);\n\n-- Step 3: Insert sample data\nINSERT INTO student_marks (student_id, semester, subject, marks, grade)\nVALUES (101, 4, 'DBMS', 88, 'A');\nINSERT INTO student_marks (student_id, semester, subject, marks, grade)\nVALUES (101, 4, 'OS',  75, 'B');\nINSERT INTO student_marks (student_id, semester, subject, marks, grade)\nVALUES (101, 5, 'CN',  82, 'A');\nINSERT INTO student_marks (student_id, semester, subject, marks, grade)\nVALUES (102, 4, 'DBMS', 65, 'C');\nINSERT INTO student_marks (student_id, semester, subject, marks, grade)\nVALUES (102, 5, 'OS',  91, 'A');\nINSERT INTO student_marks (student_id, semester, subject, marks, grade)\nVALUES (103, 4, 'DBMS', 78, 'B');\n```\n\n### The Derived Storage Model\n\nOn disk, Cassandra stores the data as follows (conceptual view):\n\n| Partition Key `student_id` | Clustering `(semester, subject)` | `marks` | `grade` |\n|---|---|---|---|\n| 101 | (4, DBMS) | 88 | A |\n| 101 | (4, OS)   | 75 | B |\n| 101 | (5, CN)   | 82 | A |\n| 102 | (4, DBMS) | 65 | C |\n| 102 | (5, OS)   | 91 | A |\n| 103 | (4, DBMS) | 78 | B |\n\n**Derivation Step 1 — Identify the partition:** Every row with the *same* `student_id` lives on the *same* node and forms one partition.\n\n**Derivation Step 2 — Identify the clustering:** Within a partition, rows are sorted by `(semester, subject)` in **ascending** order by default.\n\n**Derivation Step 3 — Identify the query target:** A valid `SELECT` must always be *resolvable* to one or more specific partitions. If it cannot, the query must declare `ALLOW FILTERING`.\n\n## 3.2 Exhaustive Step-by-Step Query Derivations\n\n### Query 1 — Retrieve All Marks for a Single Student\n\n**Textual requirement:** *\"Show every mark obtained by student 101.\"*\n\n**Step-by-step construction:**\n\n$$\\text{Step 1: Identify the partition} \\Rightarrow \\text{student\\_id} = 101$$\n\n$$\\text{Step 2: No clustering restriction} \\Rightarrow \\text{omit WHERE on semester/subject}$$\n\n$$\\text{Step 3: Final CQL} \\Rightarrow \\text{SELECT} \\ * \\ \\text{FROM} \\ \\text{student\\_marks} \\ \\text{WHERE} \\ \\text{student\\_id} = 101;$$\n\n```sql\nSELECT * FROM student_marks WHERE student_id = 101;\n```\n\n**Output:**\n```\n student_id | semester | subject | marks | grade\n------------+----------+---------+-------+-------\n        101 |        4 |    CN   |    82 |     A\n        101 |        4 |   DBMS  |    88 |     A\n        101 |        5 |    OS   |    75 |     B\n```\n\n> [!NOTE]\n> **Why this works:** Cassandra reads **only the partition for student 101** — a single-node operation. No `ALLOW FILTERING` is needed.\n\n---\n\n### Query 2 — Retrieve Marks for a Student in a Specific Semester\n\n**Textual requirement:** *\"Show all subjects and marks of student 101 in semester 4.\"*\n\n$$\\text{Step 1: Partition} \\Rightarrow \\text{student\\_id} = 101$$\n\n$$\\text{Step 2: Clustering equality} \\Rightarrow \\text{semester} = 4$$\n\n$$\\text{Step 3: Final CQL}$$\n\n```sql\nSELECT subject, marks, grade\nFROM student_marks\nWHERE student_id = 101 AND semester = 4;\n```\n\n**Output:**\n```\n subject | marks | grade\n---------+-------+-------\n    CN   |    82 |     A\n   DBMS  |    88 |     A\n```\n\n---\n\n### Query 3 — Range Scan on a Clustering Column\n\n**Textual requirement:** *\"Show all marks of student 101 in semesters greater than or equal to 4.\"*\n\n$$\\text{Step 1: Partition} \\Rightarrow \\text{student\\_id} = 101$$\n\n$$\\text{Step 2: Range on clustering} \\Rightarrow \\text{semester} \\geq 4$$\n\n$$\\text{Step 3: Final CQL}$$\n\n```sql\nSELECT * FROM student_marks\nWHERE student_id = 101 AND semester >= 4;\n```\n\n> [!IMPORTANT]\n> **Critical Rule:** Range predicates are **only** permitted on **clustering columns** when the **partition key** is fully specified. This query is valid because `student_id` is pinned.\n\n---\n\n### Query 4 — Sorting Within a Partition\n\n**Textual requirement:** *\"Show student 101's marks, sorted by marks in descending order.\"*\n\n**Attempt 1 (Naïve — will FAIL):**\n```sql\nSELECT * FROM student_marks\nWHERE student_id = 101\nORDER BY marks DESC;   -- ❌ marks is NOT a clustering column\n```\n\n**Attempt 2 (Also FAILS):**\n```sql\nSELECT * FROM student_marks\nORDER BY marks DESC;   -- ❌ no partition key in WHERE\n```\n\n**Correct Approach (Denormalization):**\nCassandra **cannot sort on a non-clustering column**. The lab answer is to create a *materialized view* or a *second table* where `marks` is part of the clustering key.\n\n```sql\nCREATE MATERIALIZED VIEW student_marks_by_score AS\n    SELECT * FROM student_marks\n    WHERE student_id IS NOT NULL\n      AND semester IS NOT NULL\n      AND subject  IS NOT NULL\n      AND marks    IS NOT NULL\n    PRIMARY KEY (student_id, marks, semester, subject);\n```\n\n```sql\nSELECT * FROM student_marks_by_score\nWHERE student_id = 101\nORDER BY marks DESC;\n```\n\n> [!NOTE]\n> **Lab Tip:** This is one of the **most commonly tested viva questions** — *\"Can you sort by a non-clustering column?\"* The answer is *no, not directly; you must redesign the schema.*\n\n---\n\n### Query 5 — Using `ALLOW FILTERING` (Last Resort)\n\n**Textual requirement:** *\"Find all students who scored exactly 88 marks.\"*\n\n```sql\nSELECT * FROM student_marks\nWHERE marks = 88;            -- ❌ will throw InvalidRequest\n```\n\n```sql\nSELECT * FROM student_marks\nWHERE marks = 88\nALLOW FILTERING;            -- ✅ executes, but scans all partitions\n```\n\n> [!WARNING]\n> **Performance Impact:** `ALLOW FILTERING` triggers a **full cluster scan**. Acceptable in a lab demo; **never** acceptable in production without an index.\n\n---\n\n### Query 6 — Using an Index on a Non-Key Column\n\nTo avoid `ALLOW FILTERING`, create a **secondary index**:\n\n```sql\nCREATE INDEX ON student_marks (marks);\n\nSELECT * FROM student_marks WHERE marks = 88;\n-- Now works WITHOUT ALLOW FILTERING\n```\n\n---\n\n### Query 7 — `IN` Clause on a Partition Key\n\n**Textual requirement:** *\"Show all marks for students 101 and 102.\"*\n\n```sql\nSELECT * FROM student_marks\nWHERE student_id IN (101, 102);\n```\n\n> [!NOTE]\n> `IN` is permitted only on the **last** column of the partition key. The above is valid because `student_id` is the *only* partition-key column.\n\n---\n\n### Query 8 — `PER PARTITION LIMIT` (Advanced Pagination)\n\n**Textual requirement:** *\"For each student, show the top 2 subjects by semester.\"*\n\n```sql\nSELECT * FROM student_marks\nPER PARTITION LIMIT 2;\n```\n\n---\n\n## 3.3 Analytical Matrix — Query Validity Quick Reference\n\n| Query Intent | Partition Key in WHERE? | Clustering Predicate? | Valid CQL? | Notes |\n|---|---|---|---|---|\n| All data of one student | ✅ | None | ✅ | Fastest read. |\n| One student, one semester | ✅ | `=` | ✅ | Single partition, single slice. |\n| One student, semester range | ✅ | `>`, `<` | ✅ | Range scan inside partition. |\n| All students, sorted by marks | ❌ | N/A | ❌ | Requires materialized view. |\n| All students with marks = 88 | ❌ | N/A | ❌ unless `ALLOW FILTERING` or index | Use secondary index. |\n| All students, no filter | ❌ | N/A | ❌ | Refused by CQLSH. |\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 CQL Query Parsing & Execution Flow\n\nThe following Mermaid diagram illustrates how the CQLSH parser processes a `SELECT` statement and routes it to the correct execution path.\n\n```mermaid\nflowchart TD\n    A[User types SELECT statement in CQLSH] --> B[Parser tokenizes the CQL]\n    B --> C{Is the query syntactically valid?}\n    C -- No --> D[Parser throws SyntaxError]\n    C -- Yes --> E[Query Planner analyzes WHERE clause]\n    E --> F{Is the PARTITION KEY present in WHERE?}\n    F -- Yes --> G{Is there a CLUSTERING column predicate?}\n    G -- Yes --> H[Single-partition read: getIndexedSlices]\n    G -- No  --> I[Single-partition full scan: RangeSlice]\n    F -- No --> J{Is ALLOW FILTERING declared?}\n    J -- Yes --> K[Cluster-wide scan: RangeSlice over ALL nodes]\n    J -- No  --> L[InvalidRequest error: 'Cannot execute query']\n    H --> M[Coordinator node merges result]\n    I --> M\n    K --> M\n    M --> N[Rows returned to CQLSH client]\n    L --> O[Error message displayed]\n    D --> O\n```\n\n## 4.2 Logical Storage & Retrieval Architecture\n\nThis diagram shows the *internal mapping* from a CQL query to a partition on a specific node in the Cassandra ring.\n\n```mermaid\ngraph TB\n    subgraph ClientLayer[\"Client Layer\"]\n        CQLSH[\"CQLSH Client\"]\n    end\n\n    subgraph RingLayer[\"Cassandra Ring (Coordinator Node)\"]\n        COORD[\"Query Coordinator\"]\n        HASH[\"Consistent Hash Function: hash partition_key\"]\n    end\n\n    subgraph NodeLayer[\"Storage Nodes\"]\n        N1[\"Node A: partitions for keys 0-99\"]\n        N2[\"Node B: partitions for keys 100-199\"]\n        N3[\"Node C: partitions for keys 200-299\"]\n    end\n\n    CQLSH --> COORD\n    COORD --> HASH\n    HASH --> N2\n    N2 --> RESULT[\"SSTable + Memtable Lookup\"]\n    RESULT --> COORD\n    COORD --> CQLSH\n```\n\n## 4.3 Partition Slice Visualization (for `student_marks`)\n\n```mermaid\ngraph LR\n    P101[\"Partition: student_id = 101\"] --> R1[\"(4, CN, 82, A)\"]\n    P101 --> R2[\"(4, DBMS, 88, A)\"]\n    P101 --> R3[\"(5, OS, 75, B)\"]\n    P102[\"Partition: student_id = 102\"] --> R4[\"(4, DBMS, 65, C)\"]\n    P102 --> R5[\"(5, OS, 91, A)\"]\n    P103[\"Partition: student_id = 103\"] --> R6[\"(4, DBMS, 78, B)\"]\n```\n\n**Reading the diagram:**\n- Each **partition** (P101, P102, P103) is a self-contained sorted list.\n- A query `WHERE student_id = 101` returns *only* the rows inside P101.\n- A query `WHERE student_id = 101 AND semester = 4` returns *only* R1 and R2.\n- A query `WHERE student_id IN (101, 102)` returns rows from P101 **and** P102 — this is *not* a cross-partition JOIN, it is a *union of two partition reads*.\n\n## 4.4 CQL Clause Dependency Graph\n\n```mermaid\nflowchart LR\n    A[SELECT columns] --> B[FROM table]\n    B --> C{WHERE clause?}\n    C -- Yes --> D[Partition key predicate]\n    D --> E[Clustering column predicate]\n    E --> F{ORDER BY?}\n    F -- Yes --> G[Clustering column ASC/DESC]\n    G --> H{LIMIT?}\n    H -- Yes --> I[LIMIT n or PER PARTITION LIMIT n]\n    I --> J[Execute]\n    C -- No --> J\n    F -- No --> J\n    H -- No --> J\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. [KTU University Exam – Dec 2023, Model Paper]\n**\"Why does Cassandra reject the following query? Rewrite it in a valid form.**\n`SELECT * FROM student_marks WHERE marks > 80;`\"\n\n**Model Answer (3 marks):**\n\n> Cassandra rejects this query because **`marks` is neither a partition key nor a clustering column**. Range predicates are only permitted on clustering columns, and the **partition key (`student_id`) is missing** from the `WHERE` clause.\n>\n> **Valid rewrite using ALLOW FILTERING (acceptable in lab):**\n> ```sql\n> SELECT * FROM student_marks WHERE marks > 80 ALLOW FILTERING;\n> ```\n>\n> **Better production-grade rewrite** — create a secondary index:\n> ```sql\n> CREATE INDEX idx_marks ON student_marks (marks);\n> SELECT * FROM student_marks WHERE marks > 80;\n> ```\n> *(1 mark for identifying the cause, 1 mark for one valid rewrite, 1 mark for the index-based alternative.)*\n\n---\n\n### Q2. [KTU University Exam – June 2024]\n**\"Differentiate between a partition key and a clustering column in Cassandra with an example.\"**\n\n**Model Answer (3 marks):**\n\n| Aspect | Partition Key | Clustering Column |\n|---|---|---|\n| Purpose | Determines *which node* stores the row. | Determines the *order of rows* within a partition. |\n| Cardinality | Should be **high** (distributed evenly). | Can be low or high. |\n| Query role | Must be present in `WHERE` for efficient reads. | Supports `=`, range, `IN`, and `ORDER BY`. |\n| Example | `student_id` in `student_marks`. | `semester, subject` in `student_marks`. |\n\n*(1 mark for definition of each, 1 mark for the role in queries.)*\n\n---\n\n## 5.2 Part B — Full 14-Mark Questions (ESE Module Internal Choice)\n\n> [!IMPORTANT]\n> **KTU ESE Pattern:** Each Part-B question carries 14 marks, split as **Part (a) = 7 marks** (theory) and **Part (b) = 7 marks** (practical/lab). Students must answer *either* Question A *or* Question B.\n\n---\n\n### 🔹 Question A — 14 Marks\n\n**[KTU University Exam – Model Paper 2024, DBMS Lab]**\n\n**(a) [7 Marks]** *Consider a Cassandra table `product_sales(product_id, sale_date, region, quantity, revenue)` with `PRIMARY KEY (product_id, sale_date, region)`. Write CQL queries to:*\n- *(i) Retrieve all sales of product `P101`.*\n- *(ii) Retrieve sales of `P101` after 1st January 2024.*\n- *(iii) Retrieve sales of `P101` between two dates, sorted by revenue descending.*\n\n**(b) [7 Marks]** *Explain why the following query is invalid and provide two valid alternatives:*\n`SELECT * FROM product_sales ORDER BY revenue DESC;`\n\n---\n\n#### ✅ Model Solution — Question A\n\n**Part (a) Solutions:**\n\n**(i) All sales of product P101:**\n```sql\nSELECT * FROM product_sales WHERE product_id = 'P101';\n```\n**Explanation:** `product_id` is the partition key. The query targets one partition, no clustering restriction, so all rows within that partition are returned in their natural sort order of `(sale_date, region)`.\n\n**(ii) Sales of P101 after 1st January 2024:**\n```sql\nSELECT * FROM product_sales\nWHERE product_id = 'P101'\n  AND sale_date > '2024-01-01';\n```\n**Explanation:** `product_id` pins the partition; `sale_date` is the first clustering column and supports a range predicate. This is a **single-partition range scan**.\n\n**(iii) Sales of P101 between two dates, sorted by revenue DESC:**\n```sql\nSELECT * FROM product_sales\nWHERE product_id = 'P101'\n  AND sale_date BETWEEN '2024-01-01' AND '2024-03-31'\nORDER BY sale_date DESC;\n```\n**Explanation:** Sorting is applied **only** to the clustering column `sale_date`. Sorting by `revenue` is *not* allowed directly — refer to part (b) for the proper workaround.\n\n*(Awarding: 2 marks for (i), 2 marks for (ii), 3 marks for (iii) including explanation.)*\n\n---\n\n**Part (b) Solution:**\n\n**Why the query is invalid:**\n1. The `WHERE` clause is **missing the partition key** (`product_id`). Cassandra cannot determine which partition(s) to read.\n2. `revenue` is **not a clustering column**. Cassandra's `ORDER BY` clause may only sort on clustering columns of the queried table.\n3. Even if it were syntactically valid, sorting across partitions is **architecturally unsupported** in Cassandra.\n\n**Two Valid Alternatives:**\n\n**Alternative 1 — Materialized View (recommended for read-heavy patterns):**\n```sql\nCREATE MATERIALIZED VIEW product_sales_by_revenue AS\n    SELECT * FROM product_sales\n    WHERE product_id IS NOT NULL\n      AND sale_date  IS NOT NULL\n      AND region     IS NOT NULL\n    PRIMARY KEY (product_id, revenue, sale_date, region)\n    WITH CLUSTERING ORDER BY (revenue DESC);\n\nSELECT * FROM product_sales_by_revenue WHERE product_id = 'P101';\n```\n\n**Alternative 2 — Application-level denormalization:**\nCreate a second table with `revenue` as the leading clustering column, and have the application write to both tables.\n\n*(Awarding: 3 marks for invalidity reasoning, 2 marks each for the two alternatives.)*\n\n---\n\n### 🔹 Question B — 14 Marks (Alternative Choice)\n\n**[KTU University Exam – Model Paper 2024, DBMS Lab]**\n\n**(a) [7 Marks]** *With a suitable example, explain the CQL `SELECT` statement and the role of `ALLOW FILTERING`. When should it be used?*\n\n**(b) [7 Marks]** *Given a Cassandra table `library(book_id, member_id, borrow_date, return_date, status)` with `PRIMARY KEY (book_id, borrow_date, member_id)`, write CQL queries to:*\n- *(i) Find all borrowing history of book `B500`.*\n- *(ii) Find active (unreturned) books borrowed by member `M12`.*\n- *(iii) List the 5 most recent borrows of book `B500`.*\n\n---\n\n#### ✅ Model Solution — Question B\n\n**Part (a) Solution:**\n\nThe CQL `SELECT` statement is the **read operation** of Cassandra Query Language. Its full syntax is:\n\n```sql\nSELECT [DISTINCT] select_list\nFROM [keyspace_name.]table_name\n[WHERE clause]\n[ORDER BY clustering_col [ASC | DESC]]\n[PER PARTITION LIMIT n]\n[LIMIT n]\n[ALLOW FILTERING];\n```\n\n**Role of `ALLOW FILTERING`:**\n- Cassandra's storage model requires that every read be *resolvable* to specific partitions.\n- When a query references **non-key, non-indexed columns** (e.g., `WHERE status = 'active'`), the coordinator cannot know which partitions hold the matching rows.\n- `ALLOW FILTERING` overrides this safety check and instructs Cassandra to **scan every partition** on every node, then filter in memory.\n- **When to use it:**\n  - ✅ During **ad-hoc analytics** or **lab experiments**.\n  - ✅ On small dev clusters.\n  - ❌ **Never** in production hot paths — replace with a secondary index or denormalized table.\n\n**Example:**\n```sql\nSELECT * FROM library WHERE status = 'overdue' ALLOW FILTERING;\n```\n\n*(Awarding: 2 marks for syntax, 3 marks for role of ALLOW FILTERING, 2 marks for usage guidance.)*\n\n---\n\n**Part (b) Solutions:**\n\n**(i) Borrowing history of book B500:**\n```sql\nSELECT * FROM library WHERE book_id = 'B500';\n```\n**Explanation:** `book_id` is the partition key; the query reads one full partition.\n\n**(ii) Active (unreturned) books borrowed by member M12:**\n\nThis query **cannot be answered efficiently** with the current schema, because `member_id` is a clustering column, not a partition key. The proper solution is to **add a second table** keyed by member:\n\n```sql\nCREATE TABLE library_by_member (\n    member_id   TEXT,\n    borrow_date TIMESTAMP,\n    book_id     TEXT,\n    status      TEXT,\n    PRIMARY KEY (member_id, borrow_date, book_id)\n);\n\nSELECT * FROM library_by_member\nWHERE member_id = 'M12' AND status = 'active' ALLOW FILTERING;\n```\n\n**Explanation:** This demonstrates the **denormalization pattern** that is the standard CQL answer to \"JOIN-like\" requirements.\n\n**(iii) 5 most recent borrows of book B500:**\n```sql\nSELECT * FROM library\nWHERE book_id = 'B500'\nORDER BY borrow_date DESC\nLIMIT 5;\n```\n**Explanation:** `book_id` pins the partition; `ORDER BY` uses the clustering column `borrow_date`; `LIMIT 5` caps the result.\n\n*(Awarding: 2 marks for (i), 3 marks for (ii) including denormalization, 2 marks for (iii).)*\n\n---\n\n> [!WARNING]\n> **🛑 KTU Examiner's Valuation Warning — Common Pitfalls**\n>\n> 1. **Forgetting `ALLOW FILTERING`:** When asked to query by a non-key column, students often write only the `WHERE` clause and omit `ALLOW FILTERING`. CQLSH will throw `InvalidRequest: Cannot execute this query as it might involve data filtering`. **Lose 1–2 marks.**\n> 2. **Sorting on non-clustering columns:** A query like `ORDER BY marks DESC` (when `marks` is not in the primary key) is **rejected by Cassandra**. The correct answer involves either a **materialized view** or a **denormalized table**. Examiners *expect* you to know this.\n> 3. **Confusing `LIMIT` with `PER PARTITION LIMIT`:** Global `LIMIT 5` returns 5 rows total; `PER PARTITION LIMIT 5` returns 5 rows **per partition**. This distinction is a frequent viva question.\n> 4. **Using `JOIN` in CQL:** Cassandra does **not** support joins. Writing `... FROM table1 JOIN table2 ON ...` is a **fatal error** in CQL. Always denormalize.\n> 5. **Missing semicolon:** CQL statements must end with `;`. The shell will keep waiting for the next line. *(1-mark deduction in lab records.)*\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember (Rapid Revision Checklist)\n\n> [!NOTE]\n> **📌 The 10 Golden Rules of CQL Retrieval — Revise Before Every Lab Exam**\n>\n> 1. **`SELECT *` is legal but discouraged** — always project the columns you need.\n> 2. **Partition key MUST appear in `WHERE`** — otherwise, the query is rejected (or needs `ALLOW FILTERING`).\n> 3. **Equality (`=`) is the fastest retrieval** in Cassandra.\n> 4. **Range queries (`>`, `<`, `BETWEEN`) work only on clustering columns**, only when the partition key is fixed.\n> 5. **`ORDER BY` may only sort on clustering columns** — never on regular or partition-key columns.\n> 6. **`IN` is allowed only on the last column of the partition key** OR on a clustering column.\n> 7. **`ALLOW FILTERING` = full cluster scan** — use it for lab demos, not production.\n> 8. **For non-key column queries, create a secondary index** (`CREATE INDEX ON table(col);`).\n> 9. **For sorting on non-clustering columns, use a materialized view** or denormalized table.\n> 10. **No JOINs, no subqueries, no `DISTINCT`** — Cassandra forces denormalization by design.\n>\n> **Bonus — Viva One-Liners:**\n> - *\"What is the difference between partition key and clustering column?\"* → Partition key locates the *node*; clustering column locates the *row within the partition* and defines its sort order.\n> - *\"Why is `ALLOW FILTERING` slow?\"* → It forces Cassandra to scan **every** SSTable on **every** node.\n> - *\"How do you paginate in Cassandra?\"* → Use `LIMIT` with `OFFSET`-like stateful paging, or `PER PARTITION LIMIT` for per-partition paging.\n> - *\"Can you query across partitions in Cassandra?\"* → Yes, using `IN` on the partition key (or `ALLOW FILTERING`), but **not** via `JOIN`.\n\n---\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pccsl408-mExperiment 2-t14",
          "title": "Create a simple application using MongoDB with Python",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL408 - DBMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Create a simple application using MongoDB with Python\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n**MongoDB** is a document-oriented, NoSQL (Not Only SQL) database that stores data in flexible, JSON-like documents called **BSON (Binary JSON)** records. When integrated with **Python** through the official driver **PyMongo**, it enables developers to build scalable, schema-less backend applications where data is manipulated via Python objects (dictionaries and lists) rather than rigid SQL tables and rows.\n\nIn the context of **PCCSL408 – DBMS Lab**, Experiment 2 evaluates the student's ability to design, implement, and query a non-relational data store using the **CRUD paradigm** (Create, Read, Update, Delete) — demonstrating competency in mapping Python data structures onto a document database.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Highlight:** The experiment is a *hands-on lab*; the examination questions test the **connection string format**, **CRUD method signatures**, **BSON data types**, and **collection vs. document terminology** — *not* raw SQL.\n\n## 1.2 Conceptual Analogy / Intuition\n\nThink of MongoDB as a **giant digital filing cabinet** where:\n- The **Database** = the entire cabinet\n- The **Collection** = a single drawer (e.g., \"Students drawer\")\n- The **Document** = an individual file inside that drawer (a single student's record)\n- The **Field** = an entry on the file (Name, Age, CGPA, …)\n\nUnlike a relational SQL database — where every file in a drawer *must* follow the exact same paper template (the schema) — MongoDB allows each file in the drawer to have **whatever fields it wants**. One student's file may list \"Hobbies\", while another's may list \"Internship\" — the database doesn't care.\n\n**Python acts as the librarian** who decides:\n- Which file to create (`insert_one`)\n- Which file to read (`find`)\n- Which file to edit (`update_one`)\n- Which file to discard (`delete_one`)\n\nThe librarian (PyMongo driver) translates the librarian's English instructions into MongoDB's native command language, called the **MongoDB Wire Protocol**.\n\n> [!NOTE]\n> **Key Distinction to Remember:**\n> - SQL Database → **Table** → **Row** → **Column**\n> - MongoDB → **Collection** → **Document** → **Field**\n\n## 1.3 Standard Metrics & Defaults\n\n| Item | Default / Standard |\n|---|---|\n| Default MongoDB Port | **27017** |\n| Default PyMongo Driver | `pymongo` (official) |\n| Document Storage Format | **BSON** (max 16 MB per document) |\n| Unique Document Identifier | `_id` field (auto-generated **ObjectId**) |\n| Default Database Path | `/data/db` |\n| ObjectId Size | **12 bytes** (4-byte timestamp + 5-byte random + 3-byte counter) |\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The MongoDB-Python Communication Stack\n\nWhen a Python program talks to MongoDB, the data flows through **four logical layers**:\n\n1. **Application Layer (Python Script)**\n   - You write Python code using dictionaries and lists.\n2. **Driver Layer (PyMongo)**\n   - The official MongoDB driver for Python. It serializes Python objects into BSON, packs them into the MongoDB Wire Protocol, and sends them over a TCP socket (port **27017**).\n3. **Server Layer (mongod)**\n   - The MongoDB daemon process that listens for requests, executes queries, and returns BSON-encoded responses.\n4. **Storage Layer (WiredTiger Engine)**\n   - The default storage engine (since MongoDB 3.2) that handles on-disk persistence, compression, and concurrency via document-level locking.\n\n> [!NOTE]\n> **Why BSON over JSON?** BSON extends JSON with extra data types (int, long, float, ObjectId, Date, Binary) and is *binary-encoded* — making it faster to parse and richer in semantics. JSON only knows *strings, numbers, booleans, arrays, objects, and null*.\n\n## 2.2 CRUD Operations — The Core Methods\n\nCRUD is the **four-letter alphabet** of every database interaction. The KTU lab record expects you to implement all four.\n\n| Operation | SQL Equivalent | PyMongo Method | Return Value |\n|---|---|---|---|\n| **C** – Create | `INSERT INTO` | `insert_one()`, `insert_many()` | `InsertOneResult` with `inserted_id` |\n| **R** – Read | `SELECT` | `find_one()`, `find()` | Cursor / single document |\n| **U** – Update | `UPDATE` | `update_one()`, `update_many()` | `UpdateResult` with `modified_count` |\n| **D** – Delete | `DELETE` | `delete_one()`, `delete_many()` | `DeleteResult` with `deleted_count` |\n\n## 2.3 High-Yield Formula Sheet (Cheat Table)\n\n| Concept | Syntax Pattern | Important Note |\n|---|---|---|\n| Import Driver | `from pymongo import MongoClient` | Always required first step |\n| Create Connection | `client = MongoClient(\"mongodb://localhost:27017/\")` | NoSQL — no port typing in URI |\n| Access Database | `db = client[\"dbname\"]` or `client.dbname` | Lazy creation — DB exists on first write |\n| Access Collection | `col = db[\"colname\"]` or `db.colname` | Collection = Group of documents |\n| Insert One | `col.insert_one({\"k\": \"v\"})` | Auto-generates `_id` if not provided |\n| Insert Many | `col.insert_many([{...}, {...}])` | Pass a *list* of dicts |\n| Find All | `for d in col.find(): ...` | Returns a **Cursor** (iterable) |\n| Find Filtered | `col.find({\"field\": \"value\"})` | Filter is itself a document |\n| Update One | `col.update_one({\"k\": \"v\"}, {\"$set\": {\"k\": \"new\"}})` | Requires `$set` operator |\n| Delete One | `col.delete_one({\"k\": \"v\"})` | First matching document only |\n| Count Docs | `col.count_documents({})` | Replaces deprecated `count()` |\n| Close Connection | `client.close()` | Good practice — releases socket |\n\n## 2.4 MongoDB Update Operators (High-Yield)\n\n| Operator | Purpose | Example |\n|---|---|---|\n| `$set` | Sets field value | `{\"$set\": {\"age\": 21}}` |\n| `$inc` | Increments numeric field | `{\"$inc\": {\"age\": 1}}` |\n| `$unset` | Removes a field | `{\"$unset\": {\"age\": \"\"}}` |\n| `$push` | Appends to array | `{\"$push\": {\"tags\": \"new\"}}` |\n| `$pull` | Removes from array | `{\"$pull\": {\"tags\": \"old\"}}` |\n\n> [!IMPORTANT]\n> **Real-world Engineering Utility:** MongoDB with Python is the **de-facto stack** for modern web backends (used by Uber, eBay, Adobe, and Meta). It is the foundation of the **MEAN/MERN** web stack and is preferred for **unstructured/IoT data** because documents can absorb schema drift — when fields change shape over time, the database doesn't require an `ALTER TABLE` migration.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Setting Up the Environment\n\n### Step 1 — Install the PyMongo Driver\n\nThe Python interpreter must be linked to the MongoDB server through a driver library. Open the terminal and execute:\n\n```bash\npip install pymongo\n```\n\nPyMongo version **4.x+** is required for MongoDB 6.0/7.0 compatibility (the KTU 2024 lab standard).\n\n### Step 2 — Start the MongoDB Server\n\n```bash\nmongod\n```\n\nThe server starts listening on port **27017** by default. If successful, the terminal will display `Waiting for connections`.\n\n## 3.2 Building the Application — Full CRUD Walkthrough\n\nBelow is a complete, compilable Python application demonstrating every CRUD operation on a `students` collection inside a `ktu_lab` database.\n\n### Program: `mongo_app.py`\n\n```python\n# ---------- IMPORTS ----------\nfrom pymongo import MongoClient\nfrom bson.objectid import ObjectId\n\n# ---------- STEP A: ESTABLISH CONNECTION ----------\n# MongoClient opens a connection pool to the MongoDB server\nclient = MongoClient(\"mongodb://localhost:27017/\")\n\n# ---------- STEP B: SELECT DATABASE ----------\n# A database is created automatically when data is first written\ndb = client[\"ktu_lab\"]\n\n# ---------- STEP C: SELECT COLLECTION ----------\n# A collection is the NoSQL equivalent of a table\nstudents = db[\"students\"]\n\n# ---------- STEP 1: CREATE (Insert) ----------\nprint(\"--- CREATE ---\")\nstudent_1 = {\n    \"name\": \"Aravind\",\n    \"roll_no\": \"KTU2024CS001\",\n    \"cgpa\": 8.7,\n    \"branch\": \"CSE\",\n    \"skills\": [\"Python\", \"MongoDB\", \"React\"]\n}\nstudent_2 = {\n    \"name\": \"Meera\",\n    \"roll_no\": \"KTU2024CS002\",\n    \"cgpa\": 9.1,\n    \"branch\": \"CSE\",\n    \"skills\": [\"Java\", \"Spring\", \"SQL\"]\n}\nstudent_3 = {\n    \"name\": \"Rahul\",\n    \"roll_no\": \"KTU2024EC015\",\n    \"cgpa\": 7.9,\n    \"branch\": \"ECE\",\n    \"skills\": [\"C++\", \"Verilog\"]\n}\n\n# insert_one returns an InsertOneResult\nresult_one = students.insert_one(student_1)\nprint(f\"Inserted Aravind with _id: {result_one.inserted_id}\")\n\n# insert_many takes a Python list of dictionaries\nresult_many = students.insert_many([student_2, student_3])\nprint(f\"Inserted IDs: {result_many.inserted_ids}\")\n\n# ---------- STEP 2: READ (Query) ----------\nprint(\"\\n--- READ ---\")\n\n# find_one() returns a single document (a Python dict) or None\nmeera = students.find_one({\"name\": \"Meera\"})\nprint(f\"Found Meera: {meera}\")\n\n# find() returns a Cursor — an iterable of documents\nprint(\"All students with cgpa > 8.0:\")\nfor doc in students.find({\"cgpa\": {\"$gt\": 8.0}}):\n    print(f\"  {doc['name']} - CGPA: {doc['cgpa']}\")\n\n# Querying with multiple conditions\nprint(\"CSE students with cgpa >= 8.5:\")\nfor doc in students.find({\"branch\": \"CSE\", \"cgpa\": {\"$gte\": 8.5}}):\n    print(f\"  {doc['name']} - {doc['branch']}\")\n\n# ---------- STEP 3: UPDATE ----------\nprint(\"\\n--- UPDATE ---\")\n\n# $set operator updates only the specified fields\nupdate_res = students.update_one(\n    {\"roll_no\": \"KTU2024CS001\"},\n    {\"$set\": {\"cgpa\": 8.9, \"skills\": [\"Python\", \"MongoDB\", \"Django\"]}}\n)\nprint(f\"Matched: {update_res.matched_count}, Modified: {update_res.modified_count}\")\n\n# $inc operator increments a numeric field\nstudents.update_one({\"roll_no\": \"KTU2024EC015\"}, {\"$inc\": {\"cgpa\": 0.2}})\n\n# ---------- STEP 4: DELETE ----------\nprint(\"\\n--- DELETE ---\")\n\ndelete_res = students.delete_one({\"name\": \"Rahul\"})\nprint(f\"Deleted count: {delete_res.deleted_count}\")\n\n# Drop the entire collection (cleanup)\n# students.drop()\n\n# ---------- STEP E: CLOSE CONNECTION ----------\nclient.close()\nprint(\"\\nConnection closed.\")\n```\n\n## 3.3 Line-by-Line Analytical Matrix\n\n| Line | Code | Explanation |\n|---|---|---|\n| 1 | `from pymongo import MongoClient` | Imports the official MongoDB driver class. |\n| 2 | `from bson.objectid import ObjectId` | Imports ObjectId type for `_id` queries. |\n| 5 | `client = MongoClient(\"mongodb://localhost:27017/\")` | Creates a connection pool. URI scheme: `mongodb://host:port/`. |\n| 8 | `db = client[\"ktu_lab\"]` | Selects (or implicitly creates) a database. |\n| 11 | `students = db[\"students\"]` | Selects a collection within the database. |\n| 26 | `students.insert_one(student_1)` | Inserts a single document. Returns object with `inserted_id`. |\n| 31 | `students.insert_many([...])` | Inserts multiple documents at once. List of dicts required. |\n| 38 | `students.find_one({\"name\": \"Meera\"})` | Returns **one** matching document. |\n| 42 | `students.find({\"cgpa\": {\"$gt\": 8.0}})` | Cursor of all matches. `$gt` = greater than. |\n| 56 | `{\"$set\": {\"cgpa\": 8.9}}` | Update operator — only `cgpa` is touched. |\n| 66 | `students.delete_one({\"name\": \"Rahul\"})` | Deletes **first** matching document only. |\n| 75 | `client.close()` | Releases socket resources — best practice. |\n\n## 3.4 Logical Derivation — Why `$set` is Mandatory in Updates\n\nIn SQL, the `UPDATE` statement replaces *whole rows* implicitly:\n\n$$\n\\text{UPDATE students SET cgpa = 8.9 WHERE roll\\_no = 'KTU2024CS001';}\n$$\n\nIn MongoDB, since documents are **schema-less**, \"replacing\" a field would *destroy* all other unsupplied fields. The `$set` operator forces **explicit partial updates**:\n\n$$\n\\text{update\\_one}(\\{filter\\}, \\{\\$set: \\{field: new\\_value\\}\\})\n$$\n\nThis means a document with 5 fields, when updated with `{$set: {field2: \"new\"}}`, still retains its other 4 fields untouched. **Without `$set`, the entire document would be overwritten by the second argument** — a classic KTU exam pitfall.\n\n## 3.5 Query Operator Reference Derivation\n\nMongoDB uses **operator objects** inside the filter document:\n\n| Operator | MongoDB Syntax | SQL Equivalent |\n|---|---|---|\n| Greater Than | `{\"$gt\": value}` | `WHERE x > value` |\n| Less Than | `{\"$lt\": value}` | `WHERE x < value` |\n| Greater / Equal | `{\"$gte\": value}` | `WHERE x >= value` |\n| In Set | `{\"$in\": [v1, v2]}` | `WHERE x IN (v1, v2)` |\n| AND | Comma in filter | `AND` |\n| OR | `{\"$or\": [...]}` | `OR` |\n| Exists | `{\"$exists\": True}` | `IS NOT NULL` |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 MongoDB ↔ Python Application Architecture (Mermaid)\n\nThe following diagram illustrates the **logical flow** of a MongoDB-Python application, from script execution to data persistence.\n\n```mermaid\nflowchart TD\n    A[Python Script] --> B[PyMongo Driver]\n    B --> C[\"MongoClient Connection Pool\"]\n    C --> D[\"mongod Server (Port 27017)\"]\n    D --> E[\"Database: ktu_lab\"]\n    E --> F[\"Collection: students\"]\n    F --> G[\"Document 1: BSON\"]\n    F --> H[\"Document 2: BSON\"]\n    F --> I[\"Document N: BSON\"]\n    D --> J[\"WiredTiger Storage Engine\"]\n    J --> K[\"Disk: /data/db/*.wt\"]\n\n    L[CRUD Method Call] --> M{Operation Type}\n    M -->|Create| N[\"insert_one / insert_many\"]\n    M -->|Read| O[\"find / find_one\"]\n    M -->|Update| P[\"update_one / $set\"]\n    M -->|Delete| Q[\"delete_one / delete_many\"]\n\n    N --> F\n    O --> F\n    P --> F\n    Q --> F\n```\n\n## 4.2 CRUD Method Decision Flow\n\n```mermaid\nflowchart TD\n    Start([Need to manipulate data?]) --> Op{Which CRUD op?}\n    Op -->|Create| C1[Use insert_one for single doc]\n    Op -->|Create| C2[Use insert_many for list of docs]\n    Op -->|Read| R1[Use find_one for one record]\n    Op -->|Read| R2[Use find for multiple with cursor]\n    Op -->|Update| U1[Use update_one with $set]\n    Op -->|Update| U2[Use update_many for bulk updates]\n    Op -->|Delete| D1[Use delete_one for first match]\n    Op -->|Delete| D2[Use delete_many for all matches]\n    C1 --> End([Document inserted with _id])\n    C2 --> End\n    R1 --> End2([Returns dict or None])\n    R2 --> End3([Returns Cursor iterable])\n    U1 --> End4([modified_count >= 1])\n    U2 --> End4\n    D1 --> End5([deleted_count >= 1])\n    D2 --> End5\n```\n\n## 4.3 BSON Document Structure (Single Record)\n\n```mermaid\nflowchart LR\n    A[\"_id: ObjectId 507f1f77...\"] --> B[\"name: Aravind\"]\n    A --> C[\"roll_no: KTU2024CS001\"]\n    A --> D[\"cgpa: 8.7\"]\n    A --> E[\"branch: CSE\"]\n    A --> F[\"skills: array of 3 strings\"]\n```\n\n> [!NOTE]\n> **Diagram Reading Note:** Every MongoDB document is a tree of key-value pairs. The root `_id` is *always* the primary key. Unlike SQL, **sub-documents and arrays are first-class citizens** — no separate JOIN table is needed.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. Define MongoDB. How does it differ from a relational database? [KTU University Exam – May 2024]\n\n**Model Answer:**\n\n**MongoDB** is an open-source, document-oriented NoSQL database that stores data in flexible, schema-less **BSON (Binary JSON)** documents grouped into **collections**.\n\n| Feature | RDBMS (SQL) | MongoDB (NoSQL) |\n|---|---|---|\n| Data Model | Tables with fixed schema | Collections of dynamic documents |\n| Storage Unit | Row | Document (BSON) |\n| Schema | Rigid, pre-defined | Dynamic, schema-less |\n| Scaling | Vertical (bigger server) | Horizontal (sharding) |\n| Relationships | Joins (FOREIGN KEY) | Embedded documents / `$lookup` |\n| Query Language | SQL | MongoDB Query Language (MQL) |\n\n---\n\n### Q2. What is the role of the PyMongo driver in a MongoDB-Python application? [KTU University Exam – Dec 2023]\n\n**Model Answer:**\n\n**PyMongo** is the official MongoDB driver for Python. Its role is to:\n\n1. **Establish a TCP connection** to the MongoDB server (default port `27017`).\n2. **Serialize** Python dictionaries and lists into **BSON** format.\n3. **Translate** Python method calls (e.g., `insert_one()`) into the MongoDB Wire Protocol commands.\n4. **Deserialize** BSON responses back into native Python objects.\n\nIn short, PyMongo is the **translator** between the Python application and the MongoDB server.\n\n---\n\n## 5.2 Part B — Full 14-Mark Questions (Module Internal Choice)\n\n### Question A — 14 Marks [KTU University Exam – Dec 2024]\n\n**Q. (a)** Explain the architecture of a MongoDB-Python application. List and describe the main PyMongo methods used to perform CRUD operations on a collection. **(7 marks)**\n\n**Model Solution:**\n\n**Architecture Overview:**\n\nA MongoDB-Python application consists of three layers:\n\n1. **Application Layer:** The Python script containing business logic and data operations.\n2. **Driver Layer (PyMongo):** A Python package (`pymongo`) that acts as a bridge, serializing Python objects into BSON and communicating with the server over TCP.\n3. **Database Layer (mongod):** The MongoDB server process that stores documents in collections using the WiredTiger storage engine.\n\n**CRUD Methods:**\n\n| Operation | Method | Description |\n|---|---|---|\n| Create | `insert_one(doc)` | Inserts a single document; returns `InsertOneResult` with `inserted_id` |\n| Create | `insert_many([d1, d2])` | Inserts multiple documents from a list |\n| Read | `find_one(filter)` | Returns one matching document (dict) or `None` |\n| Read | `find(filter)` | Returns a Cursor of all matching documents |\n| Update | `update_one(filter, {\"$set\": {}})` | Modifies a single document using `$set` |\n| Update | `update_many(filter, update)` | Modifies all matching documents |\n| Delete | `delete_one(filter)` | Deletes the first matching document |\n| Delete | `delete_many(filter)` | Deletes all matching documents |\n\n**Diagram:**\n\n```mermaid\nflowchart LR\n    A[Python App] -->|PyMongo| B[TCP Socket :27017]\n    B --> C[mongod Server]\n    C --> D[WiredTiger Engine]\n    D --> E[Disk Storage]\n```\n\n---\n\n**Q. (b)** Write a complete Python program to connect to a MongoDB database named `college`, create a collection `employees`, insert three employee documents, and then retrieve only those whose `salary` is greater than 50,000. **(7 marks)**\n\n**Model Solution:**\n\n```python\nfrom pymongo import MongoClient\n\n# Step 1: Connect to MongoDB\nclient = MongoClient(\"mongodb://localhost:27017/\")\n\n# Step 2: Access database and collection\ndb = client[\"college\"]\nemployees = db[\"employees\"]\n\n# Step 3: Insert three documents\nemployees.insert_many([\n    {\"name\": \"Anil\", \"dept\": \"HR\", \"salary\": 45000},\n    {\"name\": \"Bindu\", \"dept\": \"IT\", \"salary\": 72000},\n    {\"name\": \"Chitra\", \"dept\": \"Finance\", \"salary\": 60000}\n])\n\n# Step 4: Query documents where salary > 50000\nprint(\"Employees with salary > 50,000:\")\nfor emp in employees.find({\"salary\": {\"$gt\": 50000}}):\n    print(f\"  {emp['name']} - {emp['dept']} - {emp['salary']}\")\n\n# Step 5: Close connection\nclient.close()\n```\n\n**Expected Output:**\n\n```\nEmployees with salary > 50,000:\n  Bindu - IT - 72000\n  Chitra - Finance - 60000\n```\n\n**Explanation of Key Lines:**\n\n- `MongoClient(...)` opens a connection to the local MongoDB instance.\n- `insert_many([...])` accepts a Python **list** of dictionaries.\n- `find({\"salary\": {\"$gt\": 50000}})` uses the `$gt` (greater than) query operator.\n- The result is a **Cursor** — we iterate it with a `for` loop.\n\n---\n\n### Question B — 14 Marks (Alternative Choice) [KTU University Exam – May 2023]\n\n**Q. (a)** Differentiate between SQL and MongoDB. Write Python code to demonstrate the `update_one()` method with the `$set` operator on a `library` database. **(7 marks)**\n\n**Model Solution:**\n\n**Differences:**\n\n| Parameter | SQL | MongoDB |\n|---|---|---|\n| Type | Relational (RDBMS) | Document-oriented (NoSQL) |\n| Data Unit | Table → Row → Column | Collection → Document → Field |\n| Schema | Fixed / Rigid | Dynamic / Schema-less |\n| Joins | Native | `$lookup` or embedded docs |\n| Transactions | ACID native | Multi-doc ACID (since v4.0) |\n| Horizontal Scaling | Complex | Native sharding |\n\n**Python Code for `update_one()` with `$set`:**\n\n```python\nfrom pymongo import MongoClient\n\nclient = MongoClient(\"mongodb://localhost:27017/\")\ndb = client[\"library\"]\nbooks = db[\"books\"]\n\n# Insert a sample book\nbooks.insert_one({\n    \"title\": \"Database Systems\",\n    \"author\": \"Korth\",\n    \"copies\": 5,\n    \"edition\": 3\n})\n\n# Update: change edition to 4 and increment copies by 2\nresult = books.update_one(\n    {\"title\": \"Database Systems\"},\n    {\"$set\": {\"edition\": 4}, \"$inc\": {\"copies\": 2}}\n)\n\nprint(f\"Matched: {result.matched_count}, Modified: {result.modified_count}\")\n\n# Verify the update\nprint(books.find_one({\"title\": \"Database Systems\"}))\n\nclient.close()\n```\n\n---\n\n**Q. (b)** Write a Python program using MongoDB to demonstrate the `delete_one()` and `delete_many()` methods. Also show how to drop an entire collection. **(7 marks)**\n\n**Model Solution:**\n\n```python\nfrom pymongo import MongoClient\n\nclient = MongoClient(\"mongodb://localhost:27017/\")\ndb = client[\"shop\"]\nproducts = db[\"products\"]\n\n# Insert sample products\nproducts.insert_many([\n    {\"name\": \"Laptop\", \"price\": 75000, \"category\": \"Electronics\"},\n    {\"name\": \"Mouse\", \"price\": 500, \"category\": \"Electronics\"},\n    {\"name\": \"Notebook\", \"price\": 50, \"category\": \"Stationery\"},\n    {\"name\": \"Pen\", \"price\": 20, \"category\": \"Stationery\"}\n])\n\n# 1. delete_one() — removes first matching document\ndel_one = products.delete_one({\"category\": \"Electronics\"})\nprint(f\"Deleted one electronics item. Count: {del_one.deleted_count}\")\n\n# 2. delete_many() — removes all matching documents\ndel_many = products.delete_many({\"category\": \"Stationery\"})\nprint(f\"Deleted all stationery items. Count: {del_many.deleted_count}\")\n\n# 3. Drop the entire collection\nproducts.drop()\nprint(\"Collection 'products' has been dropped.\")\n\n# 4. Verify: collection should now be empty\nprint(f\"Remaining documents: {products.count_documents({})}\")\n\nclient.close()\n```\n\n**Key Points:**\n\n- `delete_one()` removes **only the first match** of the filter.\n- `delete_many()` removes **all matches** of the filter.\n- `drop()` removes the entire collection — *irreversible* operation.\n- `count_documents({})` returns the live count (replaces the deprecated `count()`).\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mark Deduction Pitfalls in PCCSL408 Lab Exam:**\n> 1. **Forgetting `$set` in `update_one()`** — If you write `update_one(filter, {\"cgpa\": 9.0})` instead of `update_one(filter, {\"$set\": {\"cgpa\": 9.0}})`, MongoDB throws an error: *\"Updating the path 'cgpa' would create a conflict at 'cgpa'\"*. **Lose 2 marks.**\n> 2. **Forgetting square brackets in `insert_many()`** — You must pass a *list* of dicts. Writing `insert_many({...}, {...})` is a TypeError. **Lose 1 mark.**\n> 3. **Not closing the connection** — Always end your program with `client.close()`. The examiner checks for this hygiene. **Lose 1 mark.**\n> 4. **Confusing `find()` vs `find_one()`** — `find()` returns a Cursor, not a dict. Don't write `result = collection.find(...)` and then `print(result['name'])` — iterate it with a `for` loop.\n> 5. **Not importing `ObjectId`** — If you need to query by `_id`, you must use `ObjectId(\"...\")` from `bson.objectid` — otherwise the string `\"507f...\"` won't match.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n- **MongoDB = Document-Oriented NoSQL DB**, stores **BSON** documents inside **collections** inside **databases**.\n- **PyMongo** is the official Python driver; install with `pip install pymongo`.\n- **Default port:** **27017**; default server command: `mongod`.\n- **Connection:** `client = MongoClient(\"mongodb://localhost:27017/\")`\n- **CRUD Mapping:** `insert_one/insert_many`, `find_one/find`, `update_one/update_many`, `delete_one/delete_many`.\n- **Update operators** (mandatory): `$set`, `$inc`, `$unset`, `$push`, `$pull`.\n- **Query operators:** `$gt`, `$lt`, `$gte`, `$lte`, `$in`, `$or`, `$exists`.\n- **Every document MUST have an `_id`** field — auto-generated as a 12-byte **ObjectId** if not supplied.\n- **No schema enforcement** — fields can differ between documents in the same collection.\n- **Always close the connection** with `client.close()` to free the socket.\n- **Lab Record Tip:** When documenting this experiment in your record book, draw the **CRUD Decision Flow diagram** (Section 4.2) and include the **full Python program** (Section 3.2) with sample input/output — this combination routinely fetches full 14 marks in the external lab evaluation.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        }
      ]
    }
  ]
}
