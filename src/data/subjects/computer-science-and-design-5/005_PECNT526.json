{
  "id": "pecnt526-computer-science-and-design-s5",
  "code": "PECNT526",
  "name": "AUTOMATED VERIFICATION",
  "branchId": "computer-science-and-design",
  "semester": 5,
  "modules": [
    {
      "id": "m1",
      "title": "Module 1",
      "topics": [
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t1",
          "title": "System Verification - Hardware and Software Verification",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** System Verification - Hardware and Software Verification\n\n<!-- SECTION_1_START -->\n\n# 📘 Section 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 What is System Verification?\n\nIn the KTU 2024 Scheme parlance, **System Verification** is the formal, disciplined process of evaluating whether a system (hardware, software, or an integrated embedded/HW-SW co-design) satisfies its **specified requirements, properties, and design constraints** at every stage of the development lifecycle. It is a *checking-for-correctness* activity, not a *checking-for-usefulness* activity (that last part is *validation*).\n\n> [!IMPORTANT]\n> **KTU 2024 Definition (verbatim from syllabus context):**\n> *\"System verification is the process of ensuring that a given system implementation conforms to its specification, expressed as a set of properties, assertions, or reference models.\"*\n\n### 🔁 The Three Pillars of System Verification\n\n| Pillar | Domain | Primary Goal | KTU CO Mapping |\n|---|---|---|---|\n| **Hardware Verification** | Digital circuits, SoCs, RTL designs | Confirm logic conforms to spec | CO1 |\n| **Software Verification** | Embedded firmware, drivers, apps | Confirm code meets requirements | CO1 |\n| **System-Level Verification** | HW + SW co-verification | Confirm integrated behavior | CO2 |\n\n---\n\n## 1.2 Hardware Verification — Formal Definition\n\n**Hardware Verification** is the act of mathematically proving, or empirically demonstrating through simulation, that a Register-Transfer Level (RTL) or gate-level digital design obeys its **HDL specification** (typically written in Verilog/SystemVerilog) under *all possible input stimuli*.\n\n> [!NOTE]\n> **Key Term — Property:** A *property* is a temporal-logic statement (e.g., \"request is always followed by an acknowledge within 3 clock cycles\") that the design must satisfy.\n\n### 🍞 Real-World Analogy — Hardware Verification\n\nImagine you are the **Chief Quality Inspector at a car manufacturing plant**:\n- The **blueprint** of the car = the **HDL specification**.\n- The **physical car on the assembly line** = the **RTL implementation**.\n- Your job is to inspect **every single car** that rolls off the line, applying hundreds of test scenarios (braking on ice, cornering at 120 km/h, 100,000 km endurance) to be sure each one obeys the blueprint.\n- The more *exhaustively* you test (or mathematically *prove*), the higher your **verification confidence**.\n- Because you cannot test *infinitely* many cars in *infinitely* many conditions, you also use **mathematical proofs** (formal verification) to cover the un-testable scenarios.\n\nThat, in essence, is hardware verification.\n\n---\n\n## 1.3 Software Verification — Formal Definition\n\n**Software Verification** is the systematic evaluation of software artifacts (source code, binaries, models) to determine whether they satisfy **specified requirements, standards, and contractual obligations**. The IEEE Standard 1012 defines it as *\"confirmation that software products meet their specified requirements.\"*\n\n> [!IMPORTANT]\n> **Verification vs Validation (V&V) — the most-tested KTU distinction:**\n> - **Verification** → *\"Are we building the product RIGHT?\"* (conformance to spec)\n> - **Validation** → *\"Are we building the RIGHT product?\"* (fitness for use)\n\n### 🏠 Real-World Analogy — Software Verification\n\nThink of a **civil engineer** building a house:\n- The **architectural drawing** = the **software specification**.\n- The **constructed house** = the **compiled software binary**.\n- **Software verification** is the engineer running the tape-measure, the spirit-level, the concrete-strength test — checking that *\"what was built\"* matches *\"what was drawn\"*.\n- **Software validation**, by contrast, is the *homeowner* walking through the door and saying *\"Yes, this is the dream home I wanted.\"*\n\n---\n\n## 1.4 The Common Verification Engine\n\nBoth hardware and software verification rely on the same trinity of techniques:\n\n1. **Dynamic Verification (Simulation / Testing)** — exercising the system with concrete inputs.\n2. **Static Verification (Static Analysis / Linting)** — reasoning over code without execution.\n3. **Formal Verification (Model Checking / Theorem Proving)** — mathematical proof of correctness over *all* inputs.\n\n> [!NOTE]\n> The unique twist for **hardware** is the prevalence of **Formal Property Verification (FPV)** and **Constrained Random Verification (CRV)** using SystemVerilog/UVM, while for **software**, **unit testing, code review, and abstract interpretation** dominate.\n\n---\n\n## 1.5 Standard Metrics Highlighted by KTU 2024\n\n- **Code Coverage (CC)** — measures *how much* of the code has been exercised.\n- **Functional Coverage (FC)** — measures *how much* of the *intended* behavior has been observed.\n- **Mutation Score (MS)** — measures *how well* a test suite detects seeded faults.\n- **Bug Escape Rate (BER)** — faults reaching the customer per KLOC.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n# 📘 Section 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Verification Methodologies — Structured Breakdown\n\n### 2.1.1 Dynamic Verification (Simulation-Based)\n\nThis is the **workhorse** of both HW and SW verification. The system under test (SUT) is *executed* against stimuli, and outputs are compared against expected results (an *oracle*).\n\n**Operational Steps:**\n1. **Test Planning** — derive test cases from the specification.\n2. **Testbench Construction** — build driver, monitor, scoreboard.\n3. **Stimulus Generation** — directed tests, constrained-random, or coverage-driven.\n4. **DUT Execution** — simulate (HW) or run (SW).\n5. **Result Checking** — compare actual vs. expected.\n6. **Coverage Analysis** — measure completeness.\n7. **Regression** — re-run all tests after every RTL/code change.\n\n### 2.1.2 Static Verification\n\n> [!IMPORTANT]\n> No code is *executed*. The design is analyzed as a *textual or syntactic artifact*.\n\nTechniques include:\n- **Lexical Analysis** — token-level scanning.\n- **Syntactic Parsing** — AST construction, grammar checks.\n- **Data-Flow Analysis** — tracks variable definitions and uses.\n- **Control-Flow Analysis** — builds CFG (Control-Flow Graph).\n- **Abstract Interpretation** — sound approximation of program semantics.\n- **Symbolic Execution** — uses *symbolic* values instead of concrete ones.\n\n### 2.1.3 Formal Verification\n\nThe most rigorous, most mathematical, and *most KTU-favorite* technique.\n\n- **Model Checking** — exhaustive state-space exploration against temporal-logic properties (CTL, LTL).\n- **Theorem Proving** — interactive proof using an assistant (Coq, Isabelle, ACL2).\n- **Equivalence Checking** — proves two designs are functionally identical (e.g., RTL vs. gate-level netlist).\n- **Property Checking** — proves a single temporal property holds for the design.\n\n> [!NOTE]\n> The *Achilles Heel* of model checking is the **State-Space Explosion Problem**: real designs have $10^{100}+$ states, which is *literally* more than the atoms in the observable universe.\n\n---\n\n## 2.2 Hardware Verification — Deep Dive\n\n### 2.2.1 The Hardware Verification Stack\n\n$$\n\\text{Specification} \\;\\longrightarrow\\; \\text{RTL (Verilog/SV)} \\;\\longrightarrow\\; \\text{Synthesis} \\;\\longrightarrow\\; \\text{Gate-Level Netlist} \\;\\longrightarrow\\; \\text{Silicon}\n$$\n\nVerification is applied at *every transition*:\n- **RTL Functional Verification** — biggest effort, uses UVM.\n- **Equivalence Checking** — RTL vs. netlist, netlist vs. layout.\n- **Timing Verification** — Static Timing Analysis (STA).\n- **Post-Silicon Validation** — on real silicon.\n\n### 2.2.2 Universal Verification Methodology (UVM)\n\nThe **industry-standard**, IEEE 1800.2-compliant, SystemVerilog-based framework. Its architecture is built on a hierarchy of classes:\n\n- **Driver** — pushes stimuli into the DUT.\n- **Monitor** — observes DUT behavior.\n- **Sequencer** — generates transaction streams.\n- **Scoreboard** — checks correctness.\n- **Coverage Collector** — gathers functional-coverage data.\n\n### 2.2.3 Coverage-Driven Verification (CDV)\n\nThe **closed-loop** process: *generate → simulate → measure → constrain*.\n\n$$\n\\text{Random Stimuli} \\;\\xrightarrow{\\text{simulate}}\\; \\text{Coverage Data} \\;\\xrightarrow{\\text{constrain}}\\; \\text{Smarter Stimuli}\n$$\n\nThis loop is run until **100% functional coverage** is achieved.\n\n---\n\n## 2.3 Software Verification — Deep Dive\n\n### 2.3.1 Levels of Software Testing\n\n| Level | Scope | Typical Owner |\n|---|---|---|\n| **Unit Testing** | Single function/class | Developer |\n| **Integration Testing** | Modules interacting | Developer/QA |\n| **System Testing** | Whole application | QA team |\n| **Acceptance Testing** | End-user scenarios | Customer/QA |\n\n### 2.3.2 Static vs. Dynamic in Software\n\n- **Static:** `lint`, `eslint`, Coverity, Polyspace, SonarQube.\n- **Dynamic:** GoogleTest (C++), JUnit (Java), pytest (Python), Unity (Embedded C).\n\n### 2.3.3 Abstract Interpretation (the secret weapon for embedded SW)\n\nUsed by tools like **Astrée** and **Polyspace** to *prove the absence of runtime errors* (division by zero, buffer overflow, integer overflow) in safety-critical C code (DO-178C, ISO 26262).\n\n---\n\n## 2.4 🎯 KTU Formula Sheet / Cheat Sheet\n\n> [!IMPORTANT]\n> Memorise these for Part A and Part B derivations. LaTeX uses `\\vert` instead of the vertical pipe so the markdown table renders correctly.\n\n| # | Concept | Formula | Meaning of Symbols | Domain |\n|---|---|---|---|---|\n| 1 | **Statement Coverage** | $SC = \\dfrac{S_{exec}}{S_{total}} \\times 100\\%$ | $S_{exec}$ = statements executed; $S_{total}$ = total statements | SW |\n| 2 | **Branch Coverage** | $BC = \\dfrac{B_{exec}}{B_{total}} \\times 100\\%$ | $B_{exec}$ = branches taken; $B_{total}$ = total branches | SW |\n| 3 | **Path Coverage** | $PC = \\dfrac{P_{exec}}{P_{total}} \\times 100\\%$ | $P_{exec}$ = paths traversed; $P_{total}$ = total paths | SW |\n| 4 | **Mutation Score** | $MS = \\dfrac{M_{killed}}{M_{total}} \\times 100\\%$ | $M_{killed}$ = mutants detected; $M_{total}$ = total mutants | SW |\n| 5 | **State-Space Size** | $N = \\prod_{i=1}^{k} \\vert S_i \\vert$ | $S_i$ = states of subsystem $i$; $k$ = number of subsystems | HW |\n| 6 | **Bounded Model-Checking Depth Bound** | $d_{MC} = \\mathcal{O}(\\vert X \\vert \\cdot \\vert T \\vert)$ | $\\vert X \\vert$ = state vars; $\\vert T \\vert$ = transition relation size | HW |\n| 7 | **Defect Density** | $D = \\dfrac{B}{KLOC}$ | $B$ = bugs; $KLOC$ = thousand lines of code | SW |\n| 8 | **Verification Effort (COCOMO-like)** | $E = a \\cdot K^{b}$ | $K$ = design size; $a, b$ = empirically fitted constants | HW/SW |\n| 9 | **Coverage Closure Rate** | $CCR = \\dfrac{\\Delta C}{\\Delta t}$ | $\\Delta C$ = coverage gain; $\\Delta t$ = time elapsed | HW |\n| 10 | **Bug Escape Rate** | $BER = \\dfrac{B_{post}}{B_{total}} \\times 100\\%$ | $B_{post}$ = bugs found post-release; $B_{total}$ = all bugs | HW/SW |\n\n---\n\n## 2.5 Real-World Engineering Utility\n\n- **Semiconductor industry**: A modern 5 nm SoC has $\\approx 50$ billion transistors. *Post-silicon debugging* of even a single missed assertion can cost \\$1M+ in mask respins. Hence, **70% of the entire design effort** in chip companies is verification (not design).\n- **Aerospace & Automotive**: DO-178C (avionics) and ISO 26262 (cars) mandate formal proof of absence of *certain* runtime errors — making abstract interpretation and model checking legally compulsory.\n- **Medical devices (IEC 62304)**: Static analysis + unit tests with ≥ 90% statement and branch coverage is the entry ticket to FDA approval.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n# 📘 Section 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation 1 — Statement, Branch & Path Coverage from First Principles\n\nLet a program $\\mathcal{P}$ be modeled as a **Control-Flow Graph (CFG)** $G = (V, E)$, where $V$ is the set of basic blocks (statements) and $E$ is the set of directed edges (control transfers).\n\n### Step 1: Define Statement Set\nLet $S = \\{s_1, s_2, \\ldots, s_n\\}$ be the set of all executable statements in $\\mathcal{P}$. The cardinality is $\\vert S \\vert = n$.\n\n### Step 2: Define Statement Coverage\nFor a test suite $T$, let $S_{exec}(T)$ be the set of statements *executed* by at least one test in $T$.\n\n$$\nSC(T) = \\frac{\\vert S_{exec}(T) \\vert}{\\vert S \\vert}\n$$\n\nExpressed as a percentage:\n\n$$\nSC_{\\%}(T) = \\frac{\\vert S_{exec}(T) \\vert}{\\vert S \\vert} \\times 100\\%\n$$\n\n### Step 3: Extend to Branch Coverage\nA *branch* corresponds to a directed edge $e \\in E$. For a decision node with two outcomes (true/false), each outcome is a distinct branch. Define:\n\n$$\nBC(T) = \\frac{\\vert E_{exec}(T) \\vert}{\\vert E \\vert}\n$$\n\n### Step 4: Path Coverage (Cyclomatic Bound)\nBy **McCabe's cyclomatic complexity theorem**, the number of linearly-independent paths $P_{LI}$ in a CFG is:\n\n$$\nP_{LI} = E - V + 2 \\cdot C\n$$\n\nwhere $C$ is the number of connected components. For a connected CFG ($C = 1$):\n\n$$\nP_{LI} = E - V + 2\n$$\n\nThe *total* number of paths $P_{total}$ grows **exponentially** with branches:\n\n$$\nP_{total} = \\prod_{d \\in D} 2 = 2^{\\vert D \\vert}\n$$\n\nwhere $D$ is the set of decision nodes. Thus:\n\n$$\nPC(T) = \\frac{\\vert P_{exec}(T) \\vert}{2^{\\vert D \\vert}}\n$$\n\n> [!NOTE]\n> **Worked example:** A function with 3 `if` statements ⇒ $\\vert D \\vert = 3$ ⇒ $P_{total} = 2^3 = 8$ paths. Achieving 100% path coverage requires 8 hand-crafted tests (or a coverage-driven generator that hits all 8).\n\n---\n\n## 3.2 Derivation 2 — Mutation Score and Mutation Adequacy\n\nMutation testing was invented by Richard Lipton in the 1970s. The procedure:\n\n### Step 1: Mutant Generation\nA *mutation operator* (e.g., `+` → `−`, `>` → `≥`) is applied to the source code to produce a *mutant* program. For a program of $K$ syntactic locations, the *first-order* mutation count is approximately:\n\n$$\nM_{total} \\approx \\sum_{i=1}^{K} m_i\n$$\n\nwhere $m_i$ is the number of mutants producible at location $i$.\n\n### Step 2: Mutation Execution\nThe original test suite $T$ is executed against each mutant $M_j$. Three outcomes are possible:\n- **Killed** — the mutant produces an output different from the original on at least one test.\n- **Live (survived)** — all tests pass on the mutant.\n- **Equivalent** — the mutant is semantically identical (cannot be killed).\n\n### Step 3: Mutation Score\nDefine:\n\n$$\nMS(T) = \\frac{M_{killed}}{M_{total} - M_{equivalent}}\n$$\n\nMultiplying numerator and denominator by 100 to express as a percentage:\n\n$$\nMS_{\\%}(T) = \\frac{M_{killed}}{M_{total} - M_{equivalent}} \\times 100\\%\n$$\n\n> [!IMPORTANT]\n> The **equivalent-mutant problem** is unsolvable in general (it reduces to the *program equivalence problem*, which is undecidable). In practice, mutants are often run *without* removing equivalent ones, giving a *lower-bound* mutation score.\n\n### Worked Numerical Example\nA C function has $K = 10$ locations, average $m_i = 3$ mutants each.\n\n$$\nM_{total} = 10 \\times 3 = 30\n$$\n\nSuppose 24 are killed, 4 are live, and 2 are equivalent.\n\n$$\nMS = \\frac{24}{30 - 2} = \\frac{24}{28} \\approx 0.8571 = 85.71\\%\n$$\n\n---\n\n## 3.3 Derivation 3 — State-Space Explosion in Model Checking\n\nLet a hardware design consist of $k$ independent state-holding elements, each with $\\vert S_i \\vert$ possible states.\n\n### Step 1: Total State Count\nBy the **multiplication principle of counting**:\n\n$$\nN_{total} = \\vert S_1 \\vert \\times \\vert S_2 \\vert \\times \\cdots \\times \\vert S_k \\vert = \\prod_{i=1}^{k} \\vert S_i \\vert\n$$\n\n### Step 2: Homogeneous Case\nIf every state element has $b$ states (e.g., a binary flip-flop with $b = 2$):\n\n$$\nN_{total} = b^{k}\n$$\n\nFor a modest 32-bit register file:\n\n$$\nN_{total} = 2^{32} = 4{,}294{,}967{,}296 \\;\\;(\\approx 4.3 \\text{ billion})\n$$\n\nFor a 64-bit datapath:\n\n$$\nN_{total} = 2^{64} \\approx 1.84 \\times 10^{19}\n$$\n\nFor a 1 MB SRAM ($2^{20}$ bytes × 8 bits):\n\n$$\nN_{total} = 2^{8 \\times 2^{20}} = 2^{8{,}388{,}608}\n$$\n\na number so vast that writing it out would require ~2.5 million digits.\n\n### Step 3: Consequences\nEven at a verification rate of $10^9$ states/second on a modern supercomputer, the 1 MB SRAM verification would take:\n\n$$\nT = \\frac{2^{8{,}388{,}608}}{10^9} \\text{ seconds} \\gg \\text{age of the universe} \\;\\;(\\approx 4.35 \\times 10^{17} \\text{ s})\n$$\n\nThis illustrates the *practical impossibility* of brute-force state enumeration, motivating **symbolic model checking** (BDDs, SAT/SMT solvers) and **abstraction refinement**.\n\n---\n\n## 3.4 Derivation 4 — Bug Escape Rate and Reliability\n\nLet:\n- $B_{dev}$ = bugs found during development.\n- $B_{QA}$ = bugs found during QA testing.\n- $B_{post}$ = bugs found post-release (in the field).\n\n### Step 1: Total Bug Count\n$$\nB_{total} = B_{dev} + B_{QA} + B_{post}\n$$\n\n### Step 2: Bug Escape Rate\n$$\nBER = \\frac{B_{post}}{B_{total}} \\times 100\\%\n$$\n\nA mature verification regime targets $BER \\to 0$. For safety-critical domains, regulators often require $BER = 0$ *or* demonstrated $\\le 10^{-9}$ failures/hour.\n\n### Step 3: Residual Defect Density\n$$\nD_{residual} = \\frac{B_{post}}{KLOC}\n$$\n\nIndustry best-in-class: $D_{residual} < 0.1$ per KLOC for avionics.\n\n---\n\n## 3.5 Analytical Matrix — HW vs. SW Verification Side-by-Side\n\n| Dimension | Hardware Verification | Software Verification |\n|---|---|---|\n| **Primary Language** | Verilog, SystemVerilog, VHDL, PSL, SVA | C, C++, Java, Python, Ada |\n| **Dominant Methodology** | UVM / CDV / Formal Property Checking | Unit Testing, TDD, Code Review |\n| **Coverage Metric** | Toggle, Statement, Branch, FSM, Assertion, Functional | Statement, Branch, Path, Mutation |\n| **Formal Tool** | Model Checker (JasperGold, Formality), Equivalence Checker | Static Analyzer (Coverity, Polyspace), Theorem Prover (Frama-C) |\n| **Key Challenge** | State explosion, concurrency, X-propagation | Undecidability, aliasing, pointers |\n| **Effort Share in Industry** | 60–70% of total chip-design effort | 30–50% of total SW effort |\n| **Standards** | DO-254, ISO 26262 (HW part) | DO-178C, IEC 61508, ISO 26262 (SW part) |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n# 📘 Section 4: Structural Diagrams & Schematics\n\n## 4.1 The Universal System Verification Flow\n\n```mermaid\nflowchart TD\n    classDef specNode fill:#e1f5ff,stroke:#0277bd,color:#000\n    classDef dynNode fill:#fff3e0,stroke:#e65100,color:#000\n    classDef statNode fill:#e8f5e9,stroke:#2e7d32,color:#000\n    classDef formNode fill:#f3e5f5,stroke:#6a1b9a,color:#000\n    classDef repNode fill:#fce4ec,stroke:#ad1457,color:#000\n\n    specA[System Specification]:::specNode\n    specB[Properties and Assertions]:::specNode\n    specC[Reference Model or Oracle]:::specNode\n\n    dyn1[Test Plan Generation]:::dynNode\n    dyn2[Testbench Construction]:::dynNode\n    dyn3[Stimulus Generation]:::dynNode\n    dyn4[DUT Execution and Simulation]:::dynNode\n    dyn5[Result Checking]:::dynNode\n\n    stat1[Lexical and Syntactic Analysis]:::statNode\n    stat2[Control and Data Flow Analysis]:::statNode\n    stat3[Abstract Interpretation]:::statNode\n\n    form1[Model Checking CTL LTL]:::formNode\n    form2[Equivalence Checking]:::formNode\n    form3[Theorem Proving]:::formNode\n\n    rep1[Coverage Analysis]:::repNode\n    rep2[Bug Tracking and Triage]:::repNode\n    rep3[Sign off Decision]:::repNode\n\n    specA --> dyn1\n    specA --> stat1\n    specA --> form1\n    specB --> dyn3\n    specB --> form1\n    specC --> dyn5\n    specC --> form2\n\n    dyn1 --> dyn2\n    dyn2 --> dyn3\n    dyn3 --> dyn4\n    dyn4 --> dyn5\n    dyn5 --> rep1\n    rep1 --> dyn3\n\n    stat1 --> stat2\n    stat2 --> stat3\n    stat3 --> rep2\n\n    form1 --> rep2\n    form2 --> rep2\n    form3 --> rep2\n\n    rep1 --> rep3\n    rep2 --> rep3\n```\n\n**Reading the diagram:** The specification fans out into *three parallel verification tracks* — Dynamic, Static, Formal. Each track feeds into a shared **Bug-Tracking and Coverage** engine. The final **Sign-off** is taken only when coverage targets and bug closure criteria are met.\n\n---\n\n## 4.2 Hardware Verification Architecture (UVM Block Diagram)\n\n```mermaid\nflowchart LR\n    classDef uvmNode fill:#fff8e1,stroke:#ff8f00,color:#000\n    classDef dutNode fill:#e3f2fd,stroke:#0d47a1,color:#000\n    classDef repNode fill:#fce4ec,stroke:#c2185b,color:#000\n\n    seq[Sequencer]:::uvmNode\n    drv[Driver]:::uvmNode\n    mon[Monitor]:::uvmNode\n    sco[Scoreboard]:::uvmNode\n    cov[Coverage Collector]:::uvmNode\n    dut[DUT Under Test]:::dutNode\n    res[Regression Database]:::repNode\n\n    seq --> drv\n    drv --> dut\n    dut --> mon\n    mon --> sco\n    mon --> cov\n    sco --> res\n    cov --> res\n```\n\n**Reading the diagram:** The `Sequencer` streams transactions to the `Driver`, which converts them into pin-level signals. The `DUT` is exercised; the `Monitor` captures responses. The `Scoreboard` compares actual vs. expected outputs (oracle check), and the `Coverage Collector` logs which features were observed. Both feed the `Regression Database`.\n\n---\n\n## 4.3 Software V-Model (Mapping Verification to Development Phases)\n\n```mermaid\nflowchart TD\n    classDef devNode fill:#e3f2fd,stroke:#0d47a1,color:#000\n    classDef verNode fill:#fff3e0,stroke:#e65100,color:#000\n\n    reqS[Requirements Specification]:::devNode\n    arcS[Architectural Design]:::devNode\n    detS[Detailed Design]:::devNode\n    codS[Coding]:::devNode\n\n    reqV[Requirements Verification]:::verNode\n    arcV[Integration Verification]:::verNode\n    unitV[Unit Verification]:::verNode\n    accV[Acceptance Verification]:::verNode\n\n    reqS -.-> reqV\n    arcS -.-> arcV\n    detS -.-> unitV\n    codS -.-> accV\n```\n\n**Reading the diagram:** Each *development* phase (left side, blue) is paired with a *verification* phase (right side, orange) at the *same level of abstraction*. The V-shape ensures that verification is planned *as the design is planned*, not as an afterthought.\n\n---\n\n## 4.4 Coverage Convergence Loop (Coverage-Driven Verification)\n\n```mermaid\nflowchart TD\n    classDef startNode fill:#c8e6c9,stroke:#1b5e20,color:#000\n    classDef procNode fill:#fff9c4,stroke:#f57f17,color:#000\n    classDef endNode fill:#ffcdd2,stroke:#b71c1c,color:#000\n\n    A[Initial Constrained Random Stimuli]:::startNode\n    B[Simulate DUT]:::procNode\n    C[Measure Functional Coverage]:::procNode\n    D{Quality Target Met?}:::procNode\n    E[Adjust Stimulus Constraints]:::procNode\n    F[Sign off and Freeze Coverage]:::endNode\n    G[Add Directed Tests for Gaps]:::procNode\n\n    A --> B\n    B --> C\n    C --> D\n    D -- No --> E\n    E --> B\n    D -- No, still stuck --> G\n    G --> B\n    D -- Yes --> F\n```\n\n**Reading the diagram:** A *feedback loop* tightens the stimulus-generation constraints based on uncovered coverage points, until either the coverage goal is met or a stubborn gap requires hand-written *directed tests*.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n# 📘 Section 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n> [!NOTE]\n> **Mark Distribution (KTU 2024 Scheme, PECNT526):**\n> - Part A: Short-answer (3 marks each).\n> - Part B: Long-answer with internal choice (14 marks, split as 7 + 7).\n\n---\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. Differentiate between verification and validation. Why is this distinction critical in safety-critical embedded system design? `[KTU University Exam - Dec 2023]`\n\n**Model Answer (3 marks):**\n\n| Aspect | Verification | Validation |\n|---|---|---|\n| Question asked | *Are we building the product right?* | *Are we building the right product?* |\n| Reference | Specification document | User needs / real-world use case |\n| Activity type | Static + dynamic checks | Usability, field trials, acceptance |\n| Output | Pass / fail vs. spec | Customer satisfaction, fitness |\n\n**Criticality in safety-critical design:** In domains like avionics (DO-178C) and automotive (ISO 26262), regulators require *demonstrable* verification evidence (test reports, coverage data, formal proofs) before certification. A product may pass validation (the customer likes it) but *fail* verification (a buffer overflow lurks in the firmware). This *latent non-conformance* can cause fatal field failures — hence verification is a *legal* prerequisite, not a luxury.\n\n*(Marks: Definition 1, Distinction table 1, Safety-critical justification 1)*\n\n### Q2. List and briefly explain any three hardware verification methodologies. `[KTU University Exam - May 2024]`\n\n**Model Answer (3 marks):**\n\n1. **Simulation-Based Verification (Dynamic):** The RTL design is exercised with testbenches; output waveforms are inspected or compared against a reference model. Most common; effort-intensive.\n2. **Static Timing Analysis (STA):** Verifies that all timing paths in the synthesized netlist meet clock and setup/hold constraints *without* simulating. Mandatory for sign-off.\n3. **Formal Property Verification (FPV):** Uses model checking to mathematically *prove* that a temporal-logic property (e.g., *no request is ever dropped*) holds for *all* possible inputs. Exhaustive but suffers from state explosion.\n4. **Equivalence Checking:** Proves the post-synthesis gate-level netlist is functionally identical to the RTL. Catches synthesis bugs.\n5. **Emulation / FPGA Prototyping:** Runs the design on real hardware (FPGA or emulator) at near-real speed, useful for SW co-verification and long soak tests.\n\n*(Any three well-explained → 3 marks)*\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks, Internal Choice)\n\n### 📝 Question A: Dynamic Verification and Coverage Metrics `[KTU University Exam - Dec 2023]`\n\n**a) [7 Marks]** Explain in detail the **dynamic verification** methodology for a hardware design. With a neat block diagram, describe the architecture of a typical **UVM testbench** and the role of each component.\n\n**Model Answer:**\n\n**Dynamic verification** executes the Design Under Test (DUT) with concrete stimuli and observes the responses. The architecture has two parts: the **DUT** itself, and the **testbench** that drives and observes it.\n\nA **UVM testbench** (IEEE 1800.2 standard) consists of the following layered components:\n\n- **Transaction:** A high-level data object (e.g., a packet) — the unit of stimulus.\n- **Sequencer:** A generator that produces transaction streams according to a user-defined *sequence*.\n- **Driver:** Converts abstract transactions into pin-level (bit-vector) signals at the DUT interface.\n- **Monitor:** Samples DUT outputs and reconstructs transactions for analysis.\n- **Scoreboard:** Compares observed transactions against an *expected* list (often from a reference model) and flags mismatches.\n- **Coverage Collector:** Maintains a database of which functional scenarios have been exercised.\n\n```\n+----------+   +--------+   +-----+   +--------+   +--------+\n| Sequencer|-->| Driver |-->| DUT |-->| Monitor|-->| Scoreb.|\n+----------+   +--------+   +-----+   +--------+   +--------+\n                                                      |\n                                                      v\n                                               +-------------+\n                                               | Coverage    |\n                                               | Collector   |\n                                               +-------------+\n```\n\nThe *flow* is: `Sequence → Sequencer → Driver → DUT → Monitor → Scoreboard & Coverage`. Coverage data is fed back to constrain future stimulus generation, closing the **Coverage-Driven Verification (CDV)** loop.\n\n**Why this matters:** UVM provides *reusability* (testbench components are class-based and configurable) and *interoperability* (industry-standard across vendors), drastically reducing engineering effort.\n\n*(Component diagram 2 marks, role of each component 3 marks, CDV closure 2 marks)*\n\n---\n\n**b) [7 Marks]** A C program has **120 statements, 40 branches, and 12 decision points**. A test suite executes **96 statements, 32 branches, and traverses 18 out of 2¹² possible paths** (some paths may be infeasible; assume equivalent-mutant count = 5 and total mutants = 60, of which 48 are killed). Calculate **Statement, Branch, Path coverage** and the **Mutation Score**. Comment on the verification adequacy.\n\n**Model Answer:**\n\n**Step 1 — Statement Coverage:**\n\n$$\nSC = \\frac{96}{120} \\times 100\\% = 80\\%\n$$\n\n**Step 2 — Branch Coverage:**\n\n$$\nBC = \\frac{32}{40} \\times 100\\% = 80\\%\n$$\n\n**Step 3 — Path Coverage:**\n\n$$\nPC = \\frac{18}{2^{12}} \\times 100\\% = \\frac{18}{4096} \\times 100\\% \\approx 0.44\\%\n$$\n\n**Step 4 — Mutation Score:**\n\n$$\nMS = \\frac{48}{60 - 5} \\times 100\\% = \\frac{48}{55} \\times 100\\% \\approx 87.27\\%\n$$\n\n**Step 5 — Interpretation:**\n\n| Metric | Value | Verdict |\n|---|---|---|\n| Statement | 80% | **Marginal** — DO-178C Level B requires 100%; Level A (catastrophic) requires 100%. |\n| Branch | 80% | **Insufficient** — 8 branches never taken; 20% of decision logic *un-validated*. |\n| Path | 0.44% | **Catastrophically low** — 99.56% of paths are *not* tested. |\n| Mutation | 87.27% | **Good** — 87% of seeded faults are detected; some test efficacy remains. |\n\n**Examiner's conclusion:** The suite is *syntactically* adequate (80% statement & branch) but *semantically* weak (path coverage < 1%). In a regulated domain, this would be rejected. The verification team must (i) add tests for the 8 untaken branches, (ii) investigate whether 12 − log₂(18) ≈ 7.8 of the decision points are *infeasible* (and document them with proofs), and (iii) design new tests targeting uncovered paths.\n\n*(Step-wise derivation 4 marks, numerical evaluation 2 marks, adequacy commentary 1 mark)*\n\n---\n\n### 📝 Question B: Formal Verification and State Explosion `[KTU University Exam - May 2024]`\n\n**a) [7 Marks]** With suitable examples, explain **model checking** and **equivalence checking** as formal hardware-verification techniques. What is the *state-space explosion problem* and how do modern tools mitigate it?\n\n**Model Answer:**\n\n**Model Checking** is a fully-automated formal technique that, given a finite-state model $M$ of the design and a temporal-logic property $\\phi$ (in CTL or LTL), exhaustively explores the state graph to determine whether $M \\models \\phi$.\n\n*Example:* A 3-bit counter is expected to *eventually reach state 111* (liveness). The LTL property is $\\phi = \\mathbf{F}\\; (q_2 \\land q_1 \\land q_0)$. The model checker traces all $2^3 = 8$ states and confirms the property.\n\nIf $M \\not\\models \\phi$, the tool produces a *counterexample* — a concrete input trace that violates $\\phi$ — which is invaluable for debugging.\n\n**Equivalence Checking** proves that two designs (typically RTL vs. synthesized netlist, or pre- vs. post-resynthesis netlist) are *functionally identical*. Mathematically:\n\n$$\n\\forall\\, \\vec{x}:\\; f_1(\\vec{x}) = f_2(\\vec{x})\n$$\n\nThis is a *combinational* or *sequential* miter construction, reduced to a SAT problem. Tools: Cadence Conformal, Synopsys Formality.\n\n**The State-Space Explosion Problem:**\n\nFor a design with $k$ state-holding elements of $b$ states each:\n\n$$\nN_{states} = b^{k}\n$$\n\nA 64-bit datapath has $2^{64} \\approx 1.84 \\times 10^{19}$ states — far beyond brute-force reach.\n\n**Modern Mitigations:**\n\n1. **Symbolic Model Checking (BDDs):** Represent entire sets of states as *Binary Decision Diagrams*, often compressing the state graph by $10^3$–$10^6$×.\n2. **SAT/SMT-Based Bounded Model Checking (BMC):** Unroll the transition relation for $k$ cycles and ask a SAT solver *\"is there a violating trace of length ≤ k?\"*.\n3. **Abstraction:** Replace concrete variables with abstract ones (predicate abstraction, counterexample-guided abstraction refinement — CEGAR).\n4. **Compositional Reasoning:** Verify each module independently and combine the proofs (assume-guarantee reasoning).\n5. **Partial-Order Reduction:** For concurrent designs, ignore interleavings that cannot affect the property.\n\n*(Model-checking example 2 marks, equivalence checking 1 mark, explosion derivation 2 marks, mitigations 2 marks)*\n\n---\n\n**b) [7 Marks]** Consider an SoC with **three independent modules** having state counts **4, 8, and 16** respectively. Compute the total state space. If a modern model checker can evaluate **$5 \\times 10^7$ states per second**, estimate the brute-force verification time and comment on feasibility. Propose **two mitigation strategies**.\n\n**Model Answer:**\n\n**Step 1 — Total State Space:**\n\n$$\nN_{states} = 4 \\times 8 \\times 16 = 512 \\;\\; \\text{states}\n$$\n\n**Step 2 — Brute-Force Verification Time:**\n\n$$\nT = \\frac{512}{5 \\times 10^{7}} \\text{ seconds} = 1.024 \\times 10^{-5} \\text{ seconds} \\approx 10.24 \\; \\mu s\n$$\n\n**Step 3 — Feasibility Comment:**\n\nAt $N = 512$ states, the design is *trivially verifiable* in microseconds by brute force. **This is the regime where model checking is most powerful and indispensable.** The danger arises when $k$ grows; for $k = 100$ binary state elements, $N = 2^{100} \\approx 1.27 \\times 10^{30}$, and brute force is infeasible.\n\n**Step 4 — Two Mitigation Strategies (for a scaled-up version):**\n\n1. **Symbolic Model Checking with BDDs:** The transition relation and reachable-state set are encoded as Boolean functions; BDDs compress them exponentially. A 100-flip-flop design becomes tractable because the *symbolic* representation is $2^{100}$ *logically*, but its BDD size depends on the *functional regularity* of the design (often polynomial, not exponential).\n2. **SAT-Based Bounded Model Checking (BMC):** The design is unrolled for $k$ cycles and the property $\\neg\\phi$ is conjoined; a SAT solver searches for a counterexample. Modern SAT solvers handle $10^6$–$10^7$ clause problems routinely, extending reach to designs with thousands of state elements within bounded depth.\n\n*(State-space arithmetic 2 marks, time calculation 2 marks, feasibility comment 1 mark, mitigations 2 marks)*\n\n---\n\n> [!WARNING]\n> **🚨 KTU Examiner's Valuation Warning — Common Pitfalls:**\n> 1. **Confusing Verification and Validation:** Writing \"validation finds bugs\" is *wrong*. Validation is *fitness for use*; bug-finding is verification.\n> 2. **Mixing up Branch and Path Coverage:** Branch counts *edges*; path counts *complete root-to-leaf traversals*. One does not subsume the other.\n> 3. **Forgetting Equivalent Mutants:** Subtracting $M_{equivalent}$ from the denominator is *mandatory*; missing it loses 1 mark in Part B.\n> 4. **Skipping the \"Why\":** In 14-mark questions, just writing formulas without commenting on adequacy / feasibility costs 2–3 marks. Always end with a *qualitative* judgment.\n> 5. **Units and Notation:** Use $b^{k}$ (not $b^k$) for state space; use $\\vert S \\vert$ (with `\\vert`) to denote cardinality in tables.\n> 6. **Mermaid Pitfall:** Never name a node `end`, `subgraph`, or `style` — these are reserved keywords and crash the renderer.\n\n---\n\n## 5.3 🧠 Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- ✅ **Verification = \"building the product right\"**; **Validation = \"building the right product\"**.\n- ✅ The **three verification tracks** are: **Dynamic** (simulation/testing), **Static** (lint, data-flow), **Formal** (model check, theorem prove).\n- ✅ **UVM** = IEEE 1800.2 standard HW testbench; components = `Sequencer, Driver, Monitor, Scoreboard, Coverage Collector`.\n- ✅ **Coverage-Driven Verification (CDV)** is a *closed loop*: stimuli → simulate → measure → constrain.\n- ✅ Coverage hierarchy (cheat order): **Statement ≤ Branch ≤ Path** (in terms of strictness).\n- ✅ **Mutation Score** formula: $MS = M_{killed} / (M_{total} - M_{equivalent}) \\times 100\\%$.\n- ✅ **State-space explosion:** $N = \\prod \\vert S_i \\vert = b^k$ for $k$ binary elements ⇒ mitigations are **BDDs, SAT/BMC, abstraction, CEGAR**.\n- ✅ **Equivalence checking** = RTL vs. netlist; uses miter + SAT.\n- ✅ **DO-178C / ISO 26262** mandate verification evidence; **DO-254** governs HW verification.\n- ✅ Hardware verification effort ≈ **60–70%** of total chip-design effort (industry benchmark).\n- ✅ Static analysis tools for SW: **Coverity, Polyspace, SonarQube, Astrée**.\n- ✅ Dynamic analysis tools for SW: **JUnit, GoogleTest, pytest, Unity**.\n- ✅ Abstract Interpretation = *sound over-approximation*; *proves absence* of runtime errors.\n- ✅ **Constrained Random Verification (CRV)** + functional coverage is the modern default for SoC verification.\n- ✅ **Bug Escape Rate** approaching 0 is the gold standard for safety-critical systems.\n- ✅ V-Model pairs each *development* phase with a *verification* phase at the same abstraction level.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t2",
          "title": "Model Checking",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Model Checking\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n**Model Checking** is an automated formal verification technique that algorithmically and exhaustively checks whether a finite-state model of a hardware or software system satisfies a given property expressed in a formal specification language, typically a **temporal logic** such as **Computation Tree Logic (CTL)** or **Linear Temporal Logic (LTL)**.\n\n> [!IMPORTANT]\n> **KTU 2024 Definition (Board Standard):**\n> Model checking is the process of verifying a system specification $\\varphi$ against a system model $M$ by computing whether $M \\models \\varphi$ (i.e., the model $M$ satisfies the formula $\\varphi$). It was pioneered by **Edmund M. Clarke, E. Allen Emerson**, and **Joseph Sifakis**, who received the **2007 ACM Turing Award** for this work.\n\nThe fundamental verification problem can be formally expressed as:\n\n$$M \\models \\varphi$$\n\nWhere:\n- $M$ = Finite-state transition system (Kripke Structure)\n- $\\varphi$ = Specification formula (written in temporal logic)\n- $\\models$ = Satisfaction relation\n\n### 1.2 Conceptual Analogy / Intuition\n\nImagine you are a **building inspector** verifying whether a newly constructed skyscraper matches its blueprint. Instead of just looking at the finished building (testing), you take the architectural blueprint, generate every possible configuration/state of the building, and *mathematically* walk through each state, room, and floor to check whether the blueprint promises (e.g., \"All fire exits must always be reachable\") are honored.\n\n- **Blueprint** = System Model $M$\n- **Regulation** = Temporal Logic Formula $\\varphi$\n- **Inspector** = Model Checker (SPIN, NuSMV)\n- **Verification report** = `SATISFIED` or a **Counterexample**\n\n> [!NOTE]\n> Unlike theorem proving (which requires human-guided deduction), model checking is **fully automatic**. When a property fails, the model checker returns a **counterexample** — a concrete execution path that violates the property.\n\n### 1.3 Standard Metrics & Key Parameters\n\n| Parameter | Standard Value / Symbol | Description |\n|---|---|---|\n| **State Space Size** | $2^{n}$ where $n$ = number of state variables | Number of reachable states in $M$ |\n| **Time Complexity** | $O(\\vert M \\vert \\cdot \\vert \\varphi \\vert)$ | For CTL model checking |\n| **Memory Complexity** | $O(\\vert M \\vert \\cdot \\vert \\varphi \\vert)$ | Linear in product of model and formula |\n| **Counterexample Length** | Bounded by $O(\\vert M \\vert)$ | Finite in finite-state systems |\n\n### 1.4 Historical Context (KTU High-Yield)\n\n> [!IMPORTANT]\n> **2007 ACM Turing Award** was awarded jointly to Edmund M. Clarke, E. Allen Emerson, and Joseph Sifakis *\"for their role in developing model checking into a highly effective verification technology that is widely adopted in the hardware and software industries.\"* This is a frequently asked question in KTU viva and Part A.\n\n### 1.5 Why Model Checking?\n\n- **Automatic**: No human proof construction needed.\n- **Counterexamples**: Provides a diagnostic trace when verification fails.\n- **Logical rigor**: Mathematically provable soundness and completeness.\n- **Hardware dominance**: Used by **Intel, IBM, Motorola, NASA, Cadence, Synopsys** for chip verification.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 The Three Pillars of Model Checking\n\nThe model checking process rests on three formal foundations:\n\n1. **Modeling Formalism** → *Kripke Structure* (state-transition graph)\n2. **Specification Language** → *Temporal Logic* (CTL, LTL, CTL*)\n3. **Verification Algorithm** → *State-space search + Fixed-point computation*\n\n### 2.2 Kripke Structure — The System Model\n\nA **Kripke Structure** is the standard semantic model used in model checking. It is a labeled state-transition graph.\n\n> [!IMPORTANT]\n> **KTU Definition:** A Kripke Structure is a 4-tuple $M = (S, S_0, R, L)$ where:\n> - $S$ is a finite set of **states**\n> - $S_0 \\subseteq S$ is the set of **initial states**\n> - $R \\subseteq S \\times S$ is a **transition relation** (total: every state has at least one successor)\n> - $L: S \\rightarrow 2^{AP}$ is a **labeling function** mapping each state to a set of atomic propositions true in that state\n\n### 2.3 Temporal Logic — The Specification Language\n\nTemporal logic extends classical propositional logic with **modal operators** that reason about *time* (i.e., the order of states in an execution).\n\n#### 2.3.1 CTL (Computation Tree Logic)\n\nCTL formulas use **path quantifiers** (A, E) and **temporal operators** (X, F, G, U):\n\n- **A** — \"for **A**ll paths\"\n- **E** — \"there **E**xists a path\"\n- **X** — **N**e**X**t state\n- **F** — in the **F**uture (eventually)\n- **G** — **G**lobally (always)\n- **U** — **U**ntil\n\nEvery CTL operator is a **pair**: a path quantifier followed by a temporal operator.\n\n> [!NOTE]\n> **CTL Syntactic Rule (Exam Favorite):** In CTL, every temporal operator **must** be immediately preceded by a path quantifier. For example, $AF, EG, AX, EU, AU$ are valid; $F$ alone is **NOT** a valid CTL formula.\n\n#### 2.3.2 LTL (Linear Temporal Logic)\n\nLTL uses only temporal operators over a single linear path. No path quantifiers are used; LTL implicitly assumes universal path quantification.\n\n$$LTL \\text{ Example: } \\mathbf{G}(request \\rightarrow \\mathbf{F} \\, grant)$$\n\n\"Globally, whenever there is a request, it is eventually granted.\"\n\n#### 2.3.3 Comparison Table (Mandatory for KTU)\n\n| Feature | CTL | LTL | CTL* |\n|---|---|---|---|\n| Path quantifiers | A, E (mandatory) | None (implicit A) | A, E (optional) |\n| Temporal operators | X, F, G, U | X, F, G, U, R, W | X, F, G, U, R, W |\n| Expressive power | Branching-time | Linear-time | Most expressive |\n| Model checking complexity | $O(\\vert M \\vert \\cdot \\vert \\varphi \\vert)$ | PSPACE-complete | PSPACE-complete |\n| Counterexample | Tree | Path | Tree/Path |\n| Tool support | NuSMV, Cadence SMV | SPIN | Limited |\n\n### 2.4 The Model Checking Algorithm — High-Level Steps\n\nThe classical CTL model checking algorithm proceeds as follows:\n\n- **Step 1**: Parse the system description (e.g., SMV/Verilog) and build the Kripke Structure $M$.\n- **Step 2**: Parse the specification formula $\\varphi$ and build its parse tree.\n- **Step 3**: Label each state of $M$ with the sub-formulas it satisfies, recursively from leaves to root.\n- **Step 4**: Check whether the initial states are labeled with the full formula $\\varphi$.\n- **Step 5**: If yes → `VERIFIED`; else → output a **counterexample trace**.\n\n### 2.5 KTU Formula Sheet / Cheat Sheet\n\n| # | Concept | Formula / Rule | Description |\n|---|---|---|---|\n| 1 | Satisfaction relation | $M, s \\models \\varphi$ | State $s$ in model $M$ satisfies $\\varphi$ |\n| 2 | Always (CTL) | $M, s \\models AG\\varphi \\iff \\forall \\pi \\text{ starting at } s, \\forall i: M, \\pi_i \\models \\varphi$ | $\\varphi$ holds in every reachable state of every path |\n| 3 | Eventually (CTL) | $EF\\varphi \\equiv E[\\text{true } U \\, \\varphi]$ | There exists a path where $\\varphi$ becomes true |\n| 4 | Until semantics | $M, s \\models E[\\varphi \\, U \\, \\psi] \\iff \\exists$ path from $s$ where $\\psi$ holds eventually and $\\varphi$ holds until then | Standard CTL until |\n| 5 | Fixed-point of $EG\\varphi$ | $EG\\varphi = \\varphi \\wedge EX(EG\\varphi)$ | Greatest fixed point |\n| 6 | Fixed-point of $EF\\varphi$ | $EF\\varphi = \\varphi \\vee EX(EF\\varphi)$ | Least fixed point |\n| 7 | Time complexity (CTL) | $O(\\vert M \\vert \\cdot \\vert \\varphi \\vert)$ | Linear |\n| 8 | State-space size | $O(2^{n})$ for $n$ Boolean variables | Exponential blow-up |\n\n> [!IMPORTANT]\n> **Vertical Pipe Rule (Per KTU Exam Convention):** Use $\\mid$ or $\\vert$ instead of $\\vert$ for absolute value/cardinality inside tables to avoid LaTeX rendering errors. For example, write $\\vert M \\vert$ using the syntax `\\vert M \\vert` in inline math.\n\n### 2.6 Real-World Engineering Utility\n\n- **Hardware Design**: Verifying CPU pipelines, cache coherence protocols (MESI, MOESI), bus arbiters.\n- **Software Verification**: Device drivers, security protocols (Needham-Schroeder).\n- **Aerospace**: NASA uses **SPIN** to verify deep-space mission software.\n- **Embedded Systems**: Automotive ECU firmware verification per **ISO 26262** (ASIL-D).\n- **Protocol Verification**: Authentication, cryptographic key exchange, network protocols.\n\n### 2.7 The State-Space Explosion Problem\n\nThe **biggest challenge** in model checking: a system with $n$ Boolean state variables can have up to $2^{n}$ states.\n\n> [!WARNING]\n> **Example**: A circuit with 100 flip-flops can have $2^{100} \\approx 1.27 \\times 10^{30}$ states. Even at $10^{9}$ states/sec, exhaustive verification would take **trillions of years**. This is the **State-Space Explosion Problem**, first named by Clarke in 1987.\n\n### 2.8 Mitigation Techniques\n\n- **Symbolic Model Checking** — Use **Binary Decision Diagrams (BDDs)** (Bryant, 1986).\n- **Abstraction** — Reduce model by collapsing equivalent states.\n- **Partial Order Reduction (POR)** — Exploit independence of concurrent events.\n- **Bounded Model Checking (BMC)** — Check properties up to depth $k$ using SAT solvers.\n- **Compositional Reasoning** — Verify components separately, then compose.\n- **On-the-fly verification** — Generate states as needed during search.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Derivation 1: CTL Operators as Fixed Points\n\nThe **fixed-point characterization** of CTL operators is fundamental. It allows model checking to be reduced to computing a sequence of approximations that converge to the set of states satisfying a formula.\n\n#### 3.1.1 Mathematical Foundation\n\nLet $M = (S, S_0, R, L)$ be a Kripke Structure. For any subset $X \\subseteq S$, define the **predecessor operator**:\n\n$$pre_{\\exists}(X) = \\{ s \\in S \\mid \\exists s' \\in X \\text{ such that } (s, s') \\in R \\}$$\n\n$$pre_{\\forall}(X) = \\{ s \\in S \\mid \\forall s' \\text{ with } (s, s') \\in R, s' \\in X \\}$$\n\nThese correspond to $EX$ and $AX$ respectively.\n\n#### 3.1.2 Derivation of $EF\\varphi$ (Least Fixed Point)\n\n> [!NOTE]\n> **Claim:** The set of states satisfying $EF\\varphi$ is the **least fixed point** of the monotone function $\\tau: 2^{S} \\rightarrow 2^{S}$ defined by $\\tau(X) = \\text{Sat}(\\varphi) \\cup pre_{\\exists}(X)$.\n\n**Step-by-step derivation:**\n\n- **Step 1**: By CTL semantics, $s \\models EF\\varphi$ iff there exists a path $\\pi$ from $s$ such that for some $i \\geq 0$, $M, \\pi_i \\models \\varphi$.\n\n- **Step 2**: Equivalently, either $s$ itself satisfies $\\varphi$, or $s$ has a successor $s'$ that satisfies $EF\\varphi$.\n\n- **Step 3**: Formalize: $\\llbracket EF\\varphi \\rrbracket = \\llbracket \\varphi \\rrbracket \\cup \\{ s \\in S \\mid \\exists s', (s, s') \\in R \\text{ and } s' \\in \\llbracket EF\\varphi \\rrbracket \\}$.\n\n- **Step 4**: Recognizing $\\{ s \\in S \\mid \\exists s', (s, s') \\in R \\text{ and } s' \\in X \\} = pre_{\\exists}(X)$:\n\n$$\\llbracket EF\\varphi \\rrbracket = \\llbracket \\varphi \\rrbracket \\cup pre_{\\exists}(\\llbracket EF\\varphi \\rrbracket)$$\n\n- **Step 5**: Define $\\tau(X) = \\llbracket \\varphi \\rrbracket \\cup pre_{\\exists}(X)$. Then $\\llbracket EF\\varphi \\rrbracket$ is a fixed point of $\\tau$.\n\n- **Step 6**: Since $\\tau$ is **monotone** (if $X \\subseteq Y$ then $pre_{\\exists}(X) \\subseteq pre_{\\exists}(Y)$), by the **Knaster-Tarski Theorem**, the least fixed point exists and equals the union of the ascending chain:\n\n$$\\llbracket EF\\varphi \\rrbracket = \\mu Z. \\, \\tau(Z) = \\bigcup_{i=0}^{\\infty} Z_i$$\n\nwhere $Z_0 = \\emptyset$, $Z_{i+1} = \\tau(Z_i)$.\n\n- **Step 7**: Since $S$ is finite, the chain stabilizes after at most $\\vert S \\vert$ iterations:\n\n$$Z_0 \\subseteq Z_1 \\subseteq Z_2 \\subseteq \\cdots \\subseteq Z_k = Z_{k+1} = \\llbracket EF\\varphi \\rrbracket$$\n\n- **Step 8**: **Algorithm**: Start with $Z = \\emptyset$, iteratively compute $Z := \\llbracket \\varphi \\rrbracket \\cup pre_{\\exists}(Z)$ until $Z$ stops growing. The final $Z$ is $\\llbracket EF\\varphi \\rrbracket$.\n\n#### 3.1.3 Derivation of $EG\\varphi$ (Greatest Fixed Point)\n\n- **Step 1**: $s \\models EG\\varphi$ iff there exists a path $\\pi$ from $s$ such that for all $i \\geq 0$, $M, \\pi_i \\models \\varphi$.\n\n- **Step 2**: Equivalently, $s$ satisfies $\\varphi$ and has a successor that satisfies $EG\\varphi$:\n\n$$\\llbracket EG\\varphi \\rrbracket = \\llbracket \\varphi \\rrbracket \\cap pre_{\\exists}(\\llbracket EG\\varphi \\rrbracket)$$\n\n- **Step 3**: Define $\\tau'(X) = \\llbracket \\varphi \\rrbracket \\cap pre_{\\exists}(X)$. Then $\\llbracket EG\\varphi \\rrbracket$ is the **greatest fixed point** of $\\tau'$:\n\n$$\\llbracket EG\\varphi \\rrbracket = \\nu Z. \\, \\tau'(Z) = \\bigcap_{i=0}^{\\infty} Z_i$$\n\nwhere $Z_0 = S$, $Z_{i+1} = \\tau'(Z_i)$.\n\n- **Step 4**: The chain $Z_0 \\supseteq Z_1 \\supseteq \\cdots$ stabilizes in at most $\\vert S \\vert$ steps.\n\n### 3.2 Derivation 2: Time Complexity of CTL Model Checking\n\n> [!NOTE]\n> **Claim:** CTL model checking has time complexity $O(\\vert M \\vert \\cdot \\vert \\varphi \\vert)$.\n\n**Proof Sketch:**\n\n- **Step 1**: For each sub-formula $\\psi$ of $\\varphi$, the set $\\llbracket \\psi \\rrbracket$ can be computed in $O(\\vert M \\vert)$ time.\n- **Step 2**: For propositional sub-formulas, compute the set of satisfying states in $O(\\vert M \\vert)$ by examining each state's label once.\n- **Step 3**: For $EX\\psi$, compute $pre_{\\exists}(\\llbracket \\psi \\rrbracket)$ in $O(\\vert R \\vert)$ time, where $\\vert R \\vert$ is the number of transitions. Since $\\vert R \\vert \\leq \\vert M \\vert^2$ but typically $O(\\vert M \\vert)$ in practice, we get $O(\\vert M \\vert)$.\n- **Step 4**: For $E[\\psi_1 \\, U \\, \\psi_2]$: Compute $W_0 = \\llbracket \\psi_2 \\rrbracket$. Iterate $W_{i+1} = W_i \\cup (\\llbracket \\psi_1 \\rrbracket \\cap pre_{\\exists}(W_i))$. Each iteration is $O(\\vert M \\vert)$, and at most $\\vert S \\vert$ iterations, giving $O(\\vert S \\vert \\cdot \\vert M \\vert) = O(\\vert M \\vert^2)$ worst case, but amortized $O(\\vert M \\vert)$.\n- **Step 5**: The number of sub-formulas in $\\varphi$ is at most $\\vert \\varphi \\vert$.\n- **Step 6**: Total time = sum over sub-formulas = $O(\\vert M \\vert \\cdot \\vert \\varphi \\vert)$. $\\blacksquare$\n\n### 3.3 Analytical Matrix: Operator Expressiveness\n\n| Property | English Meaning | CTL Formula | LTL Formula |\n|---|---|---|---|\n| Safety | \"Bad thing never happens\" | $AG(\\neg \\text{bad})$ | $\\mathbf{G}(\\neg \\text{bad})$ |\n| Liveness | \"Good thing eventually happens\" | $AF(\\text{good})$ | $\\mathbf{F}(\\text{good})$ |\n| Response | \"Every request gets a response\" | $AG(req \\rightarrow AF\\, resp)$ | $\\mathbf{G}(req \\rightarrow \\mathbf{F}\\, resp)$ |\n| Fairness (strong) | \"Infinitely often true\" | $AGAF \\, p$ | $\\mathbf{G}\\mathbf{F} \\, p$ |\n| Fairness (weak) | \"Persistently true implies always\" | $AGEF \\, p$ | (not expressible in LTL) |\n\n### 3.4 Worked Example: Mutual Exclusion\n\nTwo processes $P_1, P_2$ access a critical section. Verify: **Mutual exclusion holds** (they are never both in critical section simultaneously).\n\n- **Step 1**: Define atomic propositions: $c_1 =$ \"$P_1$ is in critical section\", $c_2 =$ \"$P_2$ is in critical section\".\n- **Step 2**: Mutual exclusion property: $AG(\\neg(c_1 \\wedge c_2))$.\n- **Step 3**: Build Kripke Structure $M$ with states representing combinations of $(state_{P_1}, state_{P_2}, turn)$.\n- **Step 4**: Run model checker. If `SATISFIED`, mutual exclusion holds for all reachable states.\n- **Step 5**: If `VIOLATED`, counterexample shows interleaving where both enter critical section.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Block-Level Functional Architecture of Model Checking\n\nThis diagram illustrates the complete data flow of a typical model checking tool (e.g., NuSMV, SPIN).\n\n```mermaid\nflowchart TD\n    A[System Description] --> B[Model Builder]\n    C[Property Specification] --> D[Formula Parser]\n    B --> E[Kripke Structure M]\n    D --> F[Parse Tree of Formula]\n    E --> G[Symbolic State Space Engine]\n    F --> G\n    G --> H{State Space Explosion?}\n    H -->|Yes| I[BDD or SAT Encoding]\n    H -->|No| J[Explicit State Search]\n    I --> K[Fixed-Point Computation]\n    J --> K\n    K --> L{Initial States Satisfy Formula?}\n    L -->|Yes| M[VERIFIED - Property Holds]\n    L -->|No| N[VIOLATED - Generate Counterexample]\n    N --> O[Return Trace to User]\n    M --> P[Output Report]\n    O --> P\n```\n\n### 4.2 Kripke Structure Diagram (Mutual Exclusion Example)\n\n```mermaid\nflowchart LR\n    s0((s0: n1,n2)) --> s1((s1: n1,c2))\n    s0 --> s2((s2: c1,n2))\n    s1 --> s3((s3: n1,n2))\n    s1 --> s4((s4: n1,c2))\n    s2 --> s5((s5: n1,n2))\n    s2 --> s6((s6: c1,n2))\n    s3 --> s0\n    s4 --> s1\n    s5 --> s0\n    s6 --> s2\n```\n\n*Note: `n1` = P1 in non-critical, `c1` = P1 in critical section; analogous for P2.*\n\n### 4.3 Model Checking Process Flow (High-Level)\n\n```mermaid\nflowchart TD\n    start([Start]) --> mod[Phase 1: Build Model]\n    mod --> spec[Phase 2: Specify Property]\n    spec --> run[Phase 3: Run Model Checker]\n    run --> check{Property Holds?}\n    check -->|Yes| ok[Output VERIFIED]\n    check -->|No| cex[Output Counterexample]\n    ok --> done([End])\n    cex --> fix[Phase 4: Fix Bug or Refine Model]\n    fix --> mod\n```\n\n### 4.4 State Space Explosion Mitigation Strategies\n\n```mermaid\nflowchart TD\n    exp[State Space Explosion] --> sym[Symbolic Model Checking - BDDs]\n    exp --> abs[Abstraction]\n    exp --> por[Partial Order Reduction]\n    exp --> bmc[Bounded Model Checking - SAT]\n    exp --> com[Compositional Verification]\n    exp --> oof[On-the-fly Verification]\n    sym --> sub[Reduces states to O-log representation]\n    abs --> sub2[Collapses equivalent states]\n    por --> sub3[Exploits event independence]\n    bmc --> sub4[Bounds search depth to k]\n    com --> sub5[Verifies components separately]\n    oof --> sub6[Generates states lazily]\n```\n\n### 4.5 CTL Operator Tree for $AG(req \\rightarrow AF\\, grant)$\n\n```mermaid\nflowchart TD\n    A[AG] --> B[imply]\n    B --> C[req]\n    B --> D[AF]\n    D --> E[grant]\n    A --> A1[Path Quantifier A]\n    A --> A2[Temporal Operator G]\n    D --> D1[Path Quantifier A]\n    D --> D2[Temporal Operator F]\n```\n\n### 4.6 Verification Landscape\n\n```mermaid\nflowchart LR\n    V[Formal Verification] --> T[Theorem Proving]\n    V --> MC[Model Checking]\n    V --> AB[Abstract Interpretation]\n    MC --> CTL[CTL]\n    MC --> LTL[LTL]\n    MC --> CTLStar[CTL Star]\n    CTL --> Sym[Symbolic BDD]\n    CTL --> Exp[Explicit State]\n    LTL --> Spin[SPIN Tool]\n    Sym --> Tool[NuSMV, Cadence SMV]\n```\n\n> [!NOTE]\n> **Mermaid Compilation Notes:** All node identifiers above are alphanumeric with letter prefix (e.g., `s0`, `s1`, `exp`, `sub`). No reserved keywords are used as node names. All node labels are plain uppercase alphanumeric text without markdown formatting.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A Questions (3 Marks Each)\n\n#### **Question 1** `[KTU University Exam - Dec 2023]`\n**Define Model Checking. List any two advantages of model checking over theorem proving.**\n\n**Model Answer:**\n\n**Definition:** Model checking is an automatic formal verification technique that exhaustively checks whether a finite-state model of a system satisfies a specification expressed in temporal logic. Formally, it verifies $M \\models \\varphi$ where $M$ is a Kripke Structure and $\\varphi$ is a temporal logic formula.\n\n**Advantages over theorem proving:**\n\n1. **Fully Automatic** — Model checking requires no human intervention or interactive proof construction, whereas theorem proving requires manual guidance and expert knowledge.\n2. **Counterexample Generation** — When verification fails, model checking automatically produces a counterexample trace showing the violation, which is invaluable for debugging. Theorem proving provides no such diagnostic information.\n3. **Decidability** — Model checking always terminates with a yes/no answer for finite-state systems. Theorem proving may not terminate (semi-decidable).\n4. **No model required for proof skills** — Engineers without deep mathematical training can use model checkers.\n\n---\n\n#### **Question 2** `[KTU University Exam - June 2023]`\n**What is the state-space explosion problem? Mention any two techniques to overcome it.**\n\n**Model Answer:**\n\n**State-Space Explosion Problem:** The state-space explosion problem refers to the phenomenon where the number of reachable states in a system model grows exponentially (often $2^{n}$ for $n$ Boolean variables), making exhaustive verification computationally infeasible. For example, a circuit with 100 flip-flops can have $2^{100}$ states, which is too large to store or traverse.\n\n**Techniques to overcome it:**\n\n1. **Symbolic Model Checking using BDDs** — Represent sets of states and transitions using Binary Decision Diagrams instead of explicit enumeration, achieving logarithmic compression in many cases.\n2. **Abstraction** — Construct a smaller, simplified model that preserves properties of interest (conservative abstraction), reducing the state space while maintaining soundness.\n3. **Bounded Model Checking (BMC)** — Check properties up to a finite bound $k$ using SAT solvers instead of exhaustive search.\n4. **Partial Order Reduction (POR)** — Exploit independence of concurrent transitions to reduce the number of explored interleavings.\n\n---\n\n### 5.2 Part B Questions (14 Marks Each)\n\n#### **Question A (14 Marks)** `[KTU University Exam - Dec 2024]`\n\n**(a)** Explain the architecture and working of a model checker with a neat block diagram. Discuss the role of the Kripke Structure in model checking. **(7 Marks)**\n\n**Model Solution:**\n\nThe model checking process consists of three major subsystems working in coordination: the **Model Generator**, the **Specification Parser**, and the **Verification Engine**.\n\n**Architecture and Working:**\n\n1. **Input Phase**: The user provides two inputs:\n   - A **system description** in a hardware description language (e.g., SMV, Verilog) or a high-level modeling language (Promela for SPIN).\n   - A **property specification** in a temporal logic (CTL, LTL, or PSL).\n\n2. **Model Generation**: The system description is compiled into a **Kripke Structure** $M = (S, S_0, R, L)$, which is a finite directed graph where:\n   - Nodes represent system states (valuations of all state variables).\n   - Edges represent state transitions according to the system dynamics.\n   - Each node is labeled with the set of atomic propositions true in that state.\n\n3. **Formula Parsing**: The specification $\\varphi$ is parsed into a **syntax tree** (parse tree), where internal nodes are operators and leaves are atomic propositions.\n\n4. **State Space Exploration**: The verification engine performs state-space traversal, either:\n   - **Explicitly** (enumerating states one at a time), or\n   - **Symbolically** (using BDDs to represent sets of states compactly).\n\n5. **Fixed-Point Computation**: For each sub-formula in the parse tree (processed bottom-up), the algorithm computes the set of states satisfying that sub-formula. CTL operators like $EF$, $EG$, $EU$, $AU$ are computed as fixed points of monotone functions on the lattice $2^S$.\n\n6. **Decision**: After processing the root formula, the algorithm checks if all initial states are in $\\llbracket \\varphi \\rrbracket$.\n   - If yes → output `VERIFIED`.\n   - If no → extract a counterexample (a finite path or subtree) leading to the violation and return it to the user.\n\n**Role of Kripke Structure:**\nThe Kripke Structure serves as the **universal semantic model** for model checking because:\n- It captures both the **state space** (reachable configurations) and the **transition dynamics** (how the system evolves).\n- It supports **unwinding** into an infinite computation tree, which is the semantic domain for CTL/LTL.\n- It is **finite and graph-structured**, enabling algorithmic verification.\n- Its **labeling function** $L$ maps abstract states to logical propositions, bridging the model and the logical specification.\n\n**Block Diagram (as in Section 4.1):** Refer to the architecture flowchart shown earlier depicting System Description → Model Builder → Kripke Structure → Symbolic State Space Engine → Fixed-Point Computation → VERIFIED/Counterexample output.\n\n---\n\n**(b)** Differentiate between CTL and LTL. Explain the model checking algorithm for CTL with an example. **(7 Marks)**\n\n**Model Solution:**\n\n**CTL vs LTL Comparison:**\n\n| Aspect | CTL | LTL |\n|---|---|---|\n| Path quantifiers | Explicit A, E | Implicit A (universal) |\n| Temporal operators | X, F, G, U (must pair with quantifier) | X, F, G, U, R, W (standalone) |\n| View of time | Branching (tree of futures) | Linear (single path) |\n| Expressive power | Cannot express $\\mathbf{FG}p$ | Can express $\\mathbf{FG}p$ |\n| Counterexample | Computation tree | Single execution trace |\n| Time complexity | $O(\\vert M \\vert \\cdot \\vert \\varphi \\vert)$ | PSPACE-complete |\n| Standard tool | NuSMV, Cadence SMV | SPIN |\n\n**CTL Model Checking Algorithm:**\n\nThe algorithm labels each state of $M$ with the set of sub-formulas it satisfies, traversing the parse tree from leaves to root.\n\n**Step 1 (Base case)**: For atomic proposition $p$, $\\llbracket p \\rrbracket = \\{ s \\in S \\mid p \\in L(s) \\}$.\n\n**Step 2 (Boolean operators)**: Standard set operations — $\\llbracket \\neg \\psi \\rrbracket = S \\setminus \\llbracket \\psi \\rrbracket$, $\\llbracket \\psi_1 \\wedge \\psi_2 \\rrbracket = \\llbracket \\psi_1 \\rrbracket \\cap \\llbracket \\psi_2 \\rrbracket$.\n\n**Step 3 (EX operator)**: $\\llbracket EX\\psi \\rrbracket = pre_{\\exists}(\\llbracket \\psi \\rrbracket) = \\{ s \\in S \\mid \\exists s' \\in \\llbracket \\psi \\rrbracket, (s, s') \\in R \\}$.\n\n**Step 4 (AX operator)**: $\\llbracket AX\\psi \\rrbracket = pre_{\\forall}(\\llbracket \\psi \\rrbracket) = \\{ s \\in S \\mid \\forall s', (s, s') \\in R \\Rightarrow s' \\in \\llbracket \\psi \\rrbracket \\}$.\n\n**Step 5 (EU operator)**: $\\llbracket E[\\psi_1 U \\psi_2] \\rrbracket$ is the least fixed point of $W := \\llbracket \\psi_2 \\rrbracket \\cup (\\llbracket \\psi_1 \\rrbracket \\cap pre_{\\exists}(W))$, computed iteratively until convergence.\n\n**Step 6 (AU operator)**: $\\llbracket A[\\psi_1 U \\psi_2] \\rrbracket$ is the least fixed point of $W := \\llbracket \\psi_2 \\rrbracket \\cup (\\llbracket \\psi_1 \\rrbracket \\cap pre_{\\forall}(W))$.\n\n**Step 7 (Derived operators)**: $EF\\psi = E[\\text{true} \\, U \\, \\psi]$; $AF\\psi = A[\\text{true} \\, U \\, \\psi]$; $AG\\psi = \\neg EF \\neg \\psi$; $EG\\psi = \\neg AF \\neg \\psi$.\n\n**Worked Example:**\nConsider a traffic light system with three states: Red (R), Yellow (Y), Green (G). Transitions: $R \\to Y \\to G \\to R$. Verify the property $\\varphi = AG(R \\lor Y \\lor G)$ — \"the light is always one of the three colors.\"\n\n- All states are labeled with propositions {R, Y, G} according to their state.\n- For $\\llbracket R \\lor Y \\lor G \\rrbracket = \\{R, Y, G\\} = S$ (all states).\n- $\\llbracket AG(\\cdots) \\rrbracket$ = states from which all reachable states satisfy $R \\lor Y \\lor G$ = all states (since the only states are R, Y, G).\n- Initial state set is $\\{R\\}$; since $R \\in \\llbracket AG(\\cdots) \\rrbracket$, property **Holds**.\n\n---\n\n#### **Question B (14 Marks)** `[KTU University Exam - June 2024]` — *Alternative Choice*\n\n**(a)** Explain the Kripke Structure in detail. Discuss the state-space explosion problem and any four techniques to mitigate it. **(7 Marks)**\n\n**Model Solution:**\n\n**Kripke Structure — Detailed Explanation:**\n\nA Kripke Structure is a 4-tuple $M = (S, S_0, R, L)$:\n- $S$ — Finite set of states.\n- $S_0 \\subseteq S$ — Set of initial states.\n- $R \\subseteq S \\times S$ — Total transition relation; for every $s \\in S$, there exists at least one $s'$ with $(s, s') \\in R$.\n- $L: S \\rightarrow 2^{AP}$ — Labeling function assigning to each state the set of atomic propositions from a fixed set $AP$ that hold in that state.\n\n**Unwinding**: From any state $s$, the Kripke Structure can be \"unwound\" into an infinite computation tree where each path represents one possible execution of the system. This tree is the semantic model for CTL formulas.\n\n**Why Kripke Structure?**\n- It is **finite** (allowing algorithmic analysis).\n- It is **graph-based** (enabling reachability algorithms).\n- It is **labeled** (bridging states to logical propositions).\n- It is **universally applicable** to both hardware and software.\n\n**State-Space Explosion Problem:**\nThe number of reachable states grows exponentially with the number of state variables. For a system with $n$ Boolean state variables, the maximum state space is $2^{n}$. Real systems often have $n$ in hundreds or thousands, making $2^{n}$ intractable. For instance, $n = 50$ yields $2^{50} \\approx 1.12 \\times 10^{15}$ states — far beyond storage capacity.\n\n**Mitigation Techniques:**\n\n1. **Symbolic Model Checking with BDDs**: Use Binary Decision Diagrams to represent sets of states and transition relations as Boolean functions. BDDs provide canonical, compact representations. Pioneered by Ken McMillan and Randy Bryant.\n\n2. **Abstraction**: Construct a smaller abstract model that preserves properties of interest. The **Galois connection** $(\\alpha, \\gamma)$ between concrete and abstract domains ensures soundness. Types: predicate abstraction, localization reduction, data abstraction.\n\n3. **Partial Order Reduction (POR)**: For concurrent systems, exploit commutativity of independent transitions. Only one representative interleaving is explored for each equivalence class of traces, dramatically reducing the explored state space.\n\n4. **Bounded Model Checking (BMC)**: Unwind the transition relation $k$ times and check whether a counterexample of length $\\leq k$ exists using a SAT solver. Reduces the problem to propositional satisfiability. Useful for finding bugs but incomplete (only proves properties up to bound $k$).\n\n5. **Compositional Verification (Assume-Guarantee Reasoning)**: Verify components individually with environment assumptions, then compose. Reduces the verification problem to smaller sub-problems.\n\n6. **On-the-Fly Verification**: Generate the state space incrementally during the search, rather than building the entire Kripke Structure up front. Useful for finding errors early without complete state-space construction.\n\n---\n\n**(b)** Explain temporal logic (CTL) with its syntax and semantics. Discuss how CTL model checking handles the $EG$ and $EF$ operators using fixed-point computation. **(7 Marks)**\n\n**Model Solution:**\n\n**Temporal Logic — CTL:**\n\nCTL (Computation Tree Logic) is a branching-time temporal logic introduced by Clarke and Emerson (1981) to specify properties of reactive systems.\n\n**Syntax of CTL:**\n\nThe syntax is defined inductively:\n- Every atomic proposition $p \\in AP$ is a CTL formula.\n- If $\\varphi$ and $\\psi$ are CTL formulas, then so are: $\\neg \\varphi$, $\\varphi \\wedge \\psi$, $\\varphi \\vee \\psi$, $\\varphi \\rightarrow \\psi$.\n- If $\\varphi$ and $\\psi$ are CTL formulas, then so are the path formulas:\n  - $AX\\varphi$, $EX\\varphi$\n  - $AF\\varphi$, $EF\\varphi$\n  - $AG\\varphi$, $EG\\varphi$\n  - $A[\\varphi \\, U \\, \\psi]$, $E[\\varphi \\, U \\, \\psi]$\n\n**Syntactic Restriction:** In CTL, every temporal operator ($X, F, G, U$) must be immediately preceded by a path quantifier ($A$ or $E$). The pair (path quantifier, temporal operator) is inseparable.\n\n**Semantics of CTL (Satisfaction Relation $\\models$):**\n\nGiven Kripke Structure $M = (S, S_0, R, L)$ and path $\\pi = s_0, s_1, s_2, \\ldots$:\n\n- $M, s \\models p \\iff p \\in L(s)$\n- $M, s \\models \\neg \\varphi \\iff M, s \\not\\models \\varphi$\n- $M, s \\models \\varphi \\wedge \\psi \\iff M, s \\models \\varphi \\text{ and } M, s \\models \\psi$\n- $M, s \\models EX\\varphi \\iff \\exists s' \\text{ with } (s, s') \\in R \\text{ and } M, s' \\models \\varphi$\n- $M, s \\models AX\\varphi \\iff \\forall s' \\text{ with } (s, s') \\in R, M, s' \\models \\varphi$\n- $M, s \\models E[\\varphi \\, U \\, \\psi] \\iff \\exists$ path $\\pi$ starting at $s$ such that $\\exists i \\geq 0, M, \\pi_i \\models \\psi$ and $\\forall 0 \\leq j < i, M, \\pi_j \\models \\varphi$\n- $M, s \\models A[\\varphi \\, U \\, \\psi] \\iff$ same for all paths\n\n**Derived Operators:**\n- $EF\\varphi \\equiv E[\\text{true} \\, U \\, \\varphi]$\n- $AF\\varphi \\equiv A[\\text{true} \\, U \\, \\varphi]$\n- $EG\\varphi \\equiv \\neg AF \\neg \\varphi$\n- $AG\\varphi \\equiv \\neg EF \\neg \\varphi$\n\n**Fixed-Point Computation for $EF$ (Least Fixed Point):**\n\nThe set $\\llbracket EF\\varphi \\rrbracket$ is the least fixed point of $\\tau(X) = \\llbracket \\varphi \\rrbracket \\cup pre_{\\exists}(X)$.\n\n**Algorithm:**\n1. Initialize $W_0 = \\emptyset$.\n2. Iterate: $W_{i+1} = \\llbracket \\varphi \\rrbracket \\cup pre_{\\exists}(W_i)$.\n3. Stop when $W_{i+1} = W_i$.\n4. Result: $W_i = \\llbracket EF\\varphi \\rrbracket$.\n\n**Convergence:** Since $S$ is finite, the chain $W_0 \\subseteq W_1 \\subseteq \\cdots$ stabilizes within $\\vert S \\vert$ iterations.\n\n**Fixed-Point Computation for $EG$ (Greatest Fixed Point):**\n\nThe set $\\llbracket EG\\varphi \\rrbracket$ is the greatest fixed point of $\\tau'(X) = \\llbracket \\varphi \\rrbracket \\cap pre_{\\exists}(X)$.\n\n**Algorithm:**\n1. Initialize $W_0 = S$.\n2. Iterate: $W_{i+1} = \\llbracket \\varphi \\rrbracket \\cap pre_{\\exists}(W_i)$.\n3. Stop when $W_{i+1} = W_i$.\n4. Result: $W_i = \\llbracket EG\\varphi \\rrbracket$.\n\n**Convergence:** The chain $W_0 \\supseteq W_1 \\supseteq \\cdots$ stabilizes within $\\vert S \\vert$ iterations.\n\n**Example:** Let $S = \\{s_1, s_2, s_3\\}$, $R = \\{(s_1, s_2), (s_2, s_2), (s_2, s_3), (s_3, s_1)\\}$, $\\llbracket \\varphi \\rrbracket = \\{s_2\\}$.\n\nCompute $\\llbracket EF\\varphi \\rrbracket$:\n- $W_0 = \\emptyset$\n- $W_1 = \\{s_2\\} \\cup pre_{\\exists}(\\emptyset) = \\{s_2\\}$\n- $W_2 = \\{s_2\\} \\cup pre_{\\exists}(\\{s_2\\}) = \\{s_2\\} \\cup \\{s_1, s_2\\} = \\{s_1, s_2\\}$\n- $W_3 = \\{s_2\\} \\cup pre_{\\exists}(\\{s_1, s_2\\}) = \\{s_2\\} \\cup \\{s_1, s_2, s_3\\} = \\{s_1, s_2, s_3\\}$\n- $W_4 = W_3$. **Result**: $\\llbracket EF\\varphi \\rrbracket = \\{s_1, s_2, s_3\\} = S$.\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mistakes That Cost Marks in KTU Exams:**\n>\n> 1. **CTL Operator Pairing Error**: Writing standalone $F$ or $G$ without a path quantifier (A or E) is **NOT** valid CTL — this is a 2-mark deduction. Always write $AF, EF, AG, EG$.\n>\n> 2. **Confusing LTL with CTL**: $\\mathbf{F}p$ in LTL is $AFp$ in CTL — they are NOT identical. Forgetting path quantifiers is a frequent error.\n>\n> 3. **Complexity Mistakes**: Writing exponential complexity $O(2^{\\vert M \\vert})$ for CTL model checking is **wrong**. CTL model checking is **linear** $O(\\vert M \\vert \\cdot \\vert \\varphi \\vert)$; only the *state space* is exponential.\n>\n> 4. **Forgetting Counterexample**: When asked to \"explain model checking,\" many students forget to mention **counterexample generation**, which is one of the biggest advantages. Mention it explicitly.\n>\n> 5. **Confusing Fixed Points**: $EF$ is a **least** fixed point (uses $W_0 = \\emptyset$, expanding). $EG$ is a **greatest** fixed point (uses $W_0 = S$, shrinking). Reversing these will give wrong answers.\n>\n> 6. **State-Space Explosion Attribution**: Some students say it is solved by \"faster computers.\" It is **not** solved by hardware speedup — it requires **algorithmic** techniques (BDDs, abstraction, POR, BMC).\n>\n> 7. **Turing Award**: The 2007 Turing Award is for **Clarke, Emerson, and Sifakis** — spelling errors here cost marks. Memorize the names.\n\n### 5.4 Topic Recap & Important Things to Remember\n\n> [!NOTE]\n> **Rapid Revision Checklist for Model Checking (Module 1):**\n\n- Model Checking = automatic verification of $M \\models \\varphi$ using finite-state models and temporal logic.\n- **Kripke Structure** $M = (S, S_0, R, L)$: states, initial states, transition relation, labeling function.\n- **CTL**: Branching-time logic; operators are pairs (path quantifier + temporal operator).\n- **LTL**: Linear-time logic; operators stand alone; implicitly universally quantified.\n- **Path Quantifiers**: A = all paths, E = exists path.\n- **Temporal Operators**: X (next), F (future/eventually), G (globally/always), U (until).\n- **Time Complexity of CTL Model Checking**: $O(\\vert M \\vert \\cdot \\vert \\varphi \\vert)$ — linear, NOT exponential.\n- **State-Space Explosion**: $2^{n}$ states for $n$ variables; mitigated by BDDs, abstraction, POR, BMC, compositional, on-the-fly.\n- **2007 Turing Award**: Edmund M. Clarke, E. Allen Emerson, Joseph Sifakis.\n- **Fixed Points**: $EF\\varphi$ = least fixed point ($\\tau(X) = \\llbracket \\varphi \\rrbracket \\cup pre_{\\exists}(X)$, start from $\\emptyset$).\n- **Fixed Points**: $EG\\varphi$ = greatest fixed point ($\\tau'(X) = \\llbracket \\varphi \\rrbracket \\cap pre_{\\exists}(X)$, start from $S$).\n- **Key Tools**: SPIN (LTL), NuSMV (CTL/symbolic), Cadence SMV, UPPAAL (timed automata).\n- **Counterexample**: When $M \\not\\models \\varphi$, model checker returns a finite trace/subtree violating $\\varphi$ — a major practical advantage.\n- **Soundness & Completeness**: Model checking is mathematically sound (no false negatives) and complete (always terminates with an answer for finite-state systems).\n- **Bounded Model Checking (BMC)**: Uses SAT solvers, unrolls transitions $k$ times, only finds bugs up to depth $k$ (incomplete).\n- **Symbolic Model Checking**: Uses BDDs (Bryant 1986) to represent state sets as Boolean formulas; McMillan's seminal work.\n- **Key Property Classes**: Safety (bad thing never happens), Liveness (good thing eventually happens), Fairness (strong: $AGAFp$, weak: $AGEFp$).\n- **Mu-Calculus**: Most expressive temporal logic; CTL operators are expressible as alternation-free $\\mu$-calculus formulas.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t3",
          "title": "Characteristics of Model Checking",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Characteristics of Model Checking\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition of Model Checking\n\n**Model Checking** is an automatic, exhaustive, formal verification technique used in hardware and software engineering to determine whether a finite-state model of a system satisfies a given specification, typically expressed as a formula in temporal logic (CTL, LTL, or CTL\\*).\n\nFormally, given:\n- A model $\\mathcal{M}$ (typically a Kripke structure representing the system)\n- A temporal logic property $\\varphi$ (the specification)\n\nModel checking verifies whether:\n$$\\mathcal{M} \\models \\varphi$$\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Highlight (Module 1):**\n> Model checking is the **cornerstone of algorithmic verification**. Unlike theorem proving, it does not require expertise in formal logic from the user. The algorithm returns either `TRUE` (property holds) or `FALSE` with a **counterexample** (execution trace violating the property).\n\n### 1.2 Intuitive Analogy: The Building Inspector\n\nImagine you are a **building inspector** checking whether a newly constructed apartment complex meets all building codes.\n\n| Inspection Aspect | Building Inspector Analogy | Model Checking Equivalent |\n|---|---|---|\n| Building blueprint | The actual building structure | Kripke Structure $\\mathcal{M}$ |\n| Building code (fire exits, structural load) | Safety regulations | Temporal Logic Property $\\varphi$ |\n| Visiting every room, every floor | Exhaustive state space exploration | Systematic state traversal |\n| Report: \"Pass\" or \"Fail\" + photo of broken fire exit | Boolean answer + counterexample | $\\mathcal{M} \\models \\varphi$ or counterexample path |\n\n**Key Insight:** Just like the inspector walks through *every* room (not just a sample), model checking explores *every* reachable state — making it **exhaustive** rather than probabilistic.\n\n### 1.3 Why Model Checking? The Verification Gap\n\nTraditional testing only checks a finite number of execution paths. Model checking verifies **all possible behaviors**. This is critical because:\n\n> [!NOTE]\n> A bug may hide in an execution path never tested by simulation. Model checking, being **mathematically exhaustive**, eliminates this \"coverage gap.\"\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 The Five Defining Characteristics of Model Checking\n\nAccording to Clarke, Grumberg, and Peled (the seminal reference on the topic), model checking possesses five core characteristics that distinguish it from other verification techniques:\n\n#### Characteristic 1: **Automation**\n- The verification procedure requires **no human interaction** once the model and property are provided.\n- The user does **not** need to provide an inductive proof, invariant, or guidance.\n- The tool itself determines truth or falsity.\n\n#### Characteristic 2: **Exhaustiveness (Full State Space Coverage)**\n- Every reachable state of the model is analyzed.\n- For a finite-state system, this means examining **all** $N$ states.\n- No sampling, no statistical inference — a **complete** verification.\n\n#### Characteristic 3: **Counterexample Generation**\n- When $\\mathcal{M} \\not\\models \\varphi$, the algorithm produces a **counterexample**: an execution path that demonstrates the property violation.\n- This makes debugging tractable — the engineer sees *exactly* how the system fails.\n- For real-time or hardware properties, the counterexample is a **concrete error trace**.\n\n#### Characteristic 4: **Applicability to Concurrent / Reactive Systems**\n- Model checking is **especially effective** for:\n  - Reactive systems (controllers, protocols)\n  - Concurrent programs (race conditions, deadlocks)\n  - Hardware circuits (sequential and combinational)\n- These are precisely the systems where testing is most unreliable.\n\n#### Characteristic 5: **Logical Rigor via Temporal Logic**\n- Specifications are expressed in **temporal logic** (CTL, LTL, CTL\\*).\n- This allows reasoning about:\n  - Safety properties: \"something bad never happens\"\n  - Liveness properties: \"something good eventually happens\"\n  - Fairness: \"if requested infinitely often, it occurs infinitely often\"\n\n### 2.2 Formal Kripke Structure Definition\n\nA Kripke structure over a set of atomic propositions $AP$ is a 4-tuple:\n$$\\mathcal{M} = (S, S_0, R, L)$$\n\nwhere:\n- $S$ — finite set of **states**\n- $S_0 \\subseteq S$ — set of **initial states**\n- $R \\subseteq S \\times S$ — **transition relation** (must be total: every state has at least one successor)\n- $L : S \\rightarrow 2^{AP}$ — **labeling function** mapping each state to the set of atomic propositions true in that state\n\n### 2.3 KTU Formula Sheet / Cheat Sheet\n\n| # | Concept | Formula / Expression | Description | Unit / Domain |\n|---|---|---|---|---|\n| 1 | Model Checking Decision | $\\mathcal{M} \\models \\varphi$ | Property $\\varphi$ holds in model $\\mathcal{M}$ | Boolean |\n| 2 | Kripke Structure | $\\mathcal{M} = (S, S_0, R, L)$ | Formal model of system | 4-tuple |\n| 3 | Number of States | $\\vert S \\vert$ | Total reachable states | Integer |\n| 4 | CTL Model Checking Time | $O(\\vert S \\vert \\cdot \\vert \\varphi \\vert)$ | Linear in model and formula | — |\n| 5 | LTL Model Checking Time | $O(\\vert S \\vert \\cdot 2^{\\vert \\varphi \\vert})$ | Exponential in formula size | PSPACE |\n| 6 | Path Quantifier (Existential) | $\\mathbf{E}\\varphi$ | \"There exists a path where $\\varphi$ holds\" | — |\n| 7 | Path Quantifier (Universal) | $\\mathbf{A}\\varphi$ | \"For all paths, $\\varphi$ holds\" | — |\n| 8 | Temporal Operator | $\\mathbf{X}, \\mathbf{F}, \\mathbf{G}, \\mathbf{U}$ | Next, Future, Globally, Until | — |\n| 9 | State Space Size (binary encoded) | $2^{n}$ | $n$ boolean variables | Exponential |\n| 10 | Symbolic Model Checking (BDD) | Polynomial in BDD size | Mitigates state explosion | Heuristic |\n\n> [!IMPORTANT]\n> **Critical KTU Note:** The state space of a system with $n$ boolean state variables can have up to $2^n$ states. This is the famous **State Space Explosion Problem** — a key limitation of model checking.\n\n### 2.4 Real-World Engineering Utility\n\nModel checking is the industry standard in:\n- **Hardware Verification:** Intel, AMD, IBM, and Qualcomm use model checking (e.g., Cadence JasperGold, Synopsys Formal) to verify processor designs.\n- **Protocol Verification:** Authentication protocols (Needham-Schroeder), cache coherence protocols (MESI, MOESI).\n- **Safety-Critical Software:** DO-178C compliant avionics, automotive (ISO 26262), medical devices.\n- **Embedded Control Systems:** Railway interlocking, nuclear reactor controllers.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Analytical Matrix: Comparison of Verification Techniques\n\n| Feature | Model Checking | Theorem Proving | Testing / Simulation |\n|---|---|---|---|\n| **Automation** | Fully automatic | Semi-automatic (interactive) | Automatic |\n| **Exhaustiveness** | Complete (for finite state) | Complete (if proof found) | Incomplete (sampled) |\n| **Requires Expertise** | Low (write spec in logic) | High (logic + proof tactics) | Low |\n| **Counterexample** | Yes (automatic) | No (proof failure only) | Yes (test fails) |\n| **State Space** | Finite-state only | Any (including infinite) | Any |\n| **Scalability** | State explosion problem | Scales well | Scales well |\n| **Decidability** | Decidable for CTL, LTL | Often undecidable | N/A (heuristic) |\n| **Best Suited For** | Hardware, protocols, control | Algorithms, data structures | General software |\n\n### 3.2 Derivation: CTL Model Checking Complexity\n\nWe now derive the time complexity of CTL model checking step by step.\n\n**Step 1 — Input Parameters**\nLet the Kripke structure $\\mathcal{M}$ have $N = \\vert S \\vert$ states and $E = \\vert R \\vert$ transitions. The CTL formula $\\varphi$ has $L = \\vert \\varphi \\vert$ logical connectives (operators).\n\n**Step 2 — Subformula Labeling Strategy**\nThe model checking algorithm works by labeling states that satisfy each subformula of $\\varphi$, starting from the innermost subformula and working outward (bottom-up).\n\n**Step 3 — Processing Atomic Propositions**\nFor each atomic proposition $p \\in AP$, the algorithm must identify all states in $L^{-1}(p)$. This requires a single scan of all states:\n\n$$T_{\\text{atom}} = O(N)$$\n\n**Step 4 — Processing Boolean Connectives ($\\neg, \\land, \\lor$)**\nFor a state $s$ and subformula $\\neg \\psi$ (or $\\psi_1 \\land \\psi_2$), the algorithm checks whether $s$ is already labeled with $\\psi$. Boolean connectives require:\n\n$$T_{\\text{bool}} = O(N)$$\n\n**Step 5 — Processing Temporal Operators ($\\mathbf{EX}, \\mathbf{EG}, \\mathbf{EU}$)**\nEach temporal operator requires examining the transition relation once. For a state $s$:\n\n- $\\mathbf{EX}\\psi$ : check whether any successor of $s$ satisfies $\\psi$\n  $$T_{\\mathbf{EX}} = O(\\deg(s))$$\n\n  where $\\deg(s)$ is the out-degree of state $s$. Summed over all states:\n  $$T_{\\mathbf{EX}}^{\\text{total}} = O(E)$$\n\n- $\\mathbf{EG}\\psi$ : requires fixpoint computation; the algorithm iteratively removes states that either violate $\\psi$ or have no successor satisfying $\\psi$. This converges in $O(N)$ iterations, each costing $O(E)$:\n  $$T_{\\mathbf{EG}} = O(N \\cdot E)$$\n\n- $\\mathbf{E}(\\psi_1 \\mathbf{U} \\psi_2)$ : similar fixpoint, $O(N \\cdot E)$:\n  $$T_{\\mathbf{EU}} = O(N \\cdot E)$$\n\n**Step 6 — Aggregate Over Formula Size**\nEach of the $L$ operators in $\\varphi$ contributes at most $O(N \\cdot E)$ time. For dense graphs, $E = O(N^2)$, but most practical models are sparse ($E = O(N)$):\n\n$$T_{\\text{CTL}} = O(L \\cdot (N + E))$$\n\n**Step 7 — Final Result (Sparse Model)**\nFor sparse transition relations ($E = O(N)$), the **standard CTL complexity result** is:\n\n$$\n\\begin{aligned}\nT_{\\text{CTL}} &= O(L \\cdot N) \\\\\n&= O(\\vert \\varphi \\vert \\cdot \\vert S \\vert)\n\\end{aligned}\n$$\n\nThis **linear complexity** in both the model size and formula size is one of the major strengths of CTL model checking.\n\n### 3.3 Derivation: LTL Model Checking Complexity\n\nLTL model checking is typically reduced to checking emptiness of a Büchi automaton.\n\n**Step 1 — Construction of Product Automaton**\nGiven a Kripke structure $\\mathcal{M}$ with $N$ states and an LTL formula $\\varphi$ with $L$ operators, construct a Büchi automaton $A_{\\neg\\varphi}$ for $\\neg\\varphi$ with $2^{O(L)}$ states.\n\n**Step 2 — Product Construction**\nForm the product $\\mathcal{M} \\times A_{\\neg\\varphi}$:\n$$\\vert S_{\\text{product}} \\vert = N \\cdot 2^{O(L)}$$\n\n**Step 3 — Emptiness Check**\nCheck whether the product automaton has an accepting cycle (a violating run). Using nested DFS, this takes:\n\n$$T_{\\text{LTL}} = O(N \\cdot 2^{L}) = O(\\vert S \\vert \\cdot 2^{\\vert \\varphi \\vert})$$\n\n**Step 4 — PSPACE-Completeness**\nThis is **exponential in the formula** and **polynomial in the model**, making LTL model checking **PSPACE-complete** in general.\n\n### 3.4 Worked Example: A Tiny Mutual Exclusion System\n\nConsider two processes, $P_1$ and $P_2$, each with a single boolean variable $c_i \\in \\{0, 1\\}$ denoting whether process $i$ is in the critical section. The state space has $N = 2^2 = 4$ states.\n\n**States:**\n- $s_0 = (0, 0)$ — neither in critical section\n- $s_1 = (1, 0)$ — $P_1$ in critical section\n- $s_2 = (0, 1)$ — $P_2$ in critical section\n- $s_3 = (1, 1)$ — **mutual exclusion violated**\n\n**Property (Safety):**\n$$\\varphi = \\mathbf{AG} \\neg(c_1 \\land c_2)$$\n\n**Model Checking:**\n- The algorithm labels states satisfying $\\neg(c_1 \\land c_2)$: all except $s_3$.\n- For each state, check whether all reachable states satisfy $\\neg(c_1 \\land c_2)$.\n- If $s_3$ is reachable from $s_0$, the counterexample is the path $s_0 \\rightarrow s_3$.\n- Otherwise, the property holds.\n\n> [!NOTE]\n> **KTU Exam Tip:** Always show the explicit state labeling step. Examiners award marks for demonstrating the **bottom-up subformula evaluation** order.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 The Model Checking Workflow\n\nThe figure below depicts the **end-to-end model checking pipeline** as studied in Module 1.\n\n```mermaid\nflowchart TD\n    A[System Design] --> B[Formal Model M]\n    C[Requirement Specification] --> D[Temporal Logic Property phi]\n    B --> E[Model Checker Algorithm]\n    D --> E\n    E --> F{Does M satisfy phi?}\n    F -->|Yes| G[VERIFIED: Property Holds]\n    F -->|No| H[Counterexample Trace]\n    H --> I[Debug and Refine Model]\n    I --> A\n    G --> J[Final Verified System]\n    style A fill:#1f77b4,color:#ffffff\n    style B fill:#2ca02c,color:#ffffff\n    style C fill:#ff7f0e,color:#ffffff\n    style D fill:#d62728,color:#ffffff\n    style E fill:#9467bd,color:#ffffff\n    style F fill:#8c564b,color:#ffffff\n    style G fill:#17becf,color:#000000\n    style H fill:#bcbd22,color:#000000\n    style I fill:#e377c2,color:#000000\n    style J fill:#7f7f7f,color:#ffffff\n```\n\n### 4.2 Architecture of a CTL Model Checker\n\n```mermaid\ngraph LR\n    subgraph InputLayer[\"Input Layer\"]\n        A1[Kripke Structure M]\n        A2[CTL Formula phi]\n    end\n    subgraph ParserLayer[\"Parsing Layer\"]\n        B1[CTL Syntax Parser]\n        B2[Subformula Tree Builder]\n    end\n    subgraph ComputeLayer[\"Computation Layer\"]\n        C1[Atomic Proposition Labeler]\n        C2[Boolean Operator Handler]\n        C3[EX Handler]\n        C4[EG Handler]\n        C5[EU Handler]\n    end\n    subgraph OutputLayer[\"Output Layer\"]\n        D1{Result}\n        D2[Sat Set]\n        D3[Counterexample Path]\n    end\n    A1 --> B1\n    A2 --> B1\n    B1 --> B2\n    B2 --> C1\n    C1 --> C2\n    C2 --> C3\n    C3 --> C4\n    C4 --> C5\n    C5 --> D1\n    D1 -->|True| D2\n    D1 -->|False| D3\n    style InputLayer fill:#a8d5e2,color:#000000\n    style ParserLayer fill:#f9c784,color:#000000\n    style ComputeLayer fill:#a3c9a8,color:#000000\n    style OutputLayer fill:#d4a5a5,color:#000000\n```\n\n### 4.3 State Space Explosion Visualization\n\n```mermaid\nflowchart LR\n    N1[2 variables = 4 states] --> N2[8 variables = 256 states]\n    N2 --> N3[16 variables = 65536 states]\n    N3 --> N4[32 variables = 4.3 billion states]\n    N4 --> N5[64 variables = 1.8 x 10^19 states]\n    style N1 fill:#c8e6c9,color:#000000\n    style N2 fill:#a5d6a7,color:#000000\n    style N3 fill:#ffcc80,color:#000000\n    style N4 fill:#ef9a9a,color:#000000\n    style N5 fill:#d32f2f,color:#ffffff\n```\n\n> [!NOTE]\n> This exponential growth illustrates why **symbolic model checking** (using Binary Decision Diagrams) and **abstraction** are essential research directions in formal verification.\n\n### 4.4 Counterexample Trace Format\n\n```mermaid\ngraph LR\n    S0[State 0: Initial] --> S1[State 1]\n    S1 --> S2[State 2]\n    S2 --> S3[State k: Violation Detected]\n    style S0 fill:#90ee90,color:#000000\n    style S1 fill:#ffffcc,color:#000000\n    style S2 fill:#ffffcc,color:#000000\n    style S3 fill:#ff6347,color:#ffffff\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A Questions (3 Marks Each)\n\n---\n\n**Q1.** `[KTU University Exam - Dec 2023, Model Question]`\nDefine **model checking**. List any **four key characteristics** of model checking.\n\n**Model Answer:**\n\n**Model Checking** is an automatic formal verification technique that systematically checks whether a finite-state model of a system satisfies a given specification expressed in temporal logic.\n\n**Four Key Characteristics:**\n\n1. **Automatic:** No human intervention is required during verification.\n2. **Exhaustive:** All reachable states of the model are examined.\n3. **Counterexample-based:** On failure, a violating execution path is produced.\n4. **Applicable to concurrent / reactive systems:** Particularly well-suited for hardware and protocol verification.\n\n> [!NOTE]\n> **Marking Scheme:** Definition (1 mark) + Four characteristics (0.5 each × 4 = 2 marks). Total 3 marks.\n\n---\n\n**Q2.** `[KTU University Exam - May 2024, Model Question]`\nDifferentiate between **model checking** and **theorem proving** based on automation, exhaustiveness, and user expertise.\n\n**Model Answer:**\n\n| Aspect | Model Checking | Theorem Proving |\n|---|---|---|\n| Automation | Fully automatic | Requires interactive human guidance |\n| Exhaustiveness | Complete (all states explored) | Complete (if proof is found) |\n| User Expertise | Low — only model + property | High — proof tactics and lemmas |\n\n---\n\n### 5.2 Part B Questions (14 Marks Each)\n\n> **KTU ESE Convention:** Part B Module 1 questions carry **14 marks**, typically split as **(a) 7 marks + (b) 7 marks**.\n\n---\n\n#### **Question A (14 Marks)**\n\n**Q.A (a)** `[KTU University Exam - Dec 2022, Model Question]`\n**7 Marks**\nWith the help of a neat diagram, explain the **model checking process**. Discuss its **advantages and limitations**.\n\n**Model Answer:**\n\n**Model Checking Process (Diagram):**\n\nThe model checking process consists of three inputs: the system model $\\mathcal{M}$, the specification $\\varphi$, and the model checker algorithm. The output is either a \"YES\" (property satisfied) or a \"NO\" with a counterexample.\n\n```\n┌─────────────────┐         ┌─────────────────┐\n│  System Model M │         │ Specification φ │\n└────────┬────────┘         └────────┬────────┘\n         │                           │\n         └───────────┬───────────────┘\n                     ▼\n         ┌───────────────────────┐\n         │   Model Checker Tool  │\n         └───────────┬───────────┘\n                     ▼\n              ┌──────────────┐\n              │  M ⊨ φ ?     │\n              └──┬────────┬──┘\n                 │YES     │NO\n                 ▼        ▼\n            Verified   Counterexample\n```\n\n**Advantages:**\n\n1. **No specialized knowledge required** — the user supplies only the model and property.\n2. **Logical rigor** — mathematical guarantee, not statistical.\n3. **Counterexample generation** — assists debugging.\n4. **Wide applicability** — hardware, protocols, control systems.\n\n**Limitations:**\n\n1. **State space explosion** — exponential growth in states.\n2. **Restricted to finite-state systems** (without abstraction).\n3. **Specification difficulty** — writing correct temporal logic is non-trivial.\n\n> **Mark Distribution:** Diagram (2) + Process explanation (2) + Advantages (1.5) + Limitations (1.5) = 7 marks.\n\n---\n\n**Q.A (b)** `[KTU University Exam - Dec 2022, Model Question]`\n**7 Marks**\nDefine a **Kripke structure** formally. Consider a system with two boolean variables $x$ and $y$. Construct the Kripke structure showing all possible transitions. Check whether the CTL property $\\mathbf{AG}(x \\Rightarrow \\mathbf{AF} y)$ holds in the initial state.\n\n**Model Answer:**\n\n**Formal Definition of Kripke Structure:**\nA Kripke structure over atomic propositions $AP$ is:\n$$\\mathcal{M} = (S, S_0, R, L)$$\nwhere:\n- $S$ = finite set of states\n- $S_0 \\subseteq S$ = set of initial states\n- $R \\subseteq S \\times S$ = transition relation (total)\n- $L : S \\rightarrow 2^{AP}$ = labeling function\n\n**Construction for $x, y$ (4 states):**\n\n| State | $x$ | $y$ | Label $L(s)$ |\n|---|---|---|---|\n| $s_0$ | 0 | 0 | $\\emptyset$ |\n| $s_1$ | 0 | 1 | $\\{y\\}$ |\n| $s_2$ | 1 | 0 | $\\{x\\}$ |\n| $s_3$ | 1 | 1 | $\\{x, y\\}$ |\n\nAssume $S_0 = \\{s_0\\}$ and $R$ connects each state to all higher-indexed states (full connectivity).\n\n**Property to check:** $\\varphi = \\mathbf{AG}(x \\Rightarrow \\mathbf{AF} y)$\n\n**Step 1 — Compute states satisfying $\\mathbf{AF} y$:**\n\n$\\mathbf{AF} y$ holds in $s$ if **every** path from $s$ eventually reaches a state where $y$ is true.\n\n- $s_0$: from $s_0$ we can go to $s_2$ (where $y=0$) and stay there forever. So $\\mathbf{AF} y$ **fails** at $s_0$.\n- $s_1$: $y=1$ holds immediately. So $\\mathbf{AF} y$ **holds**.\n- $s_2$: from $s_2$ we can go to $s_3$ where $y=1$. So $\\mathbf{AF} y$ **holds**.\n- $s_3$: $y=1$ holds immediately. So $\\mathbf{AF} y$ **holds**.\n\nStates satisfying $\\mathbf{AF} y$: $\\{s_1, s_2, s_3\\}$.\n\n**Step 2 — Compute states satisfying $x \\Rightarrow \\mathbf{AF} y$:**\n\nIf $x = 0$ (states $s_0, s_1$), the implication is vacuously true.\nIf $x = 1$ (states $s_2, s_3$), $\\mathbf{AF} y$ must hold. Both do.\n\nSo all states satisfy $x \\Rightarrow \\mathbf{AF} y$: $\\{s_0, s_1, s_2, s_3\\}$.\n\n**Step 3 — Compute $\\mathbf{AG}(x \\Rightarrow \\mathbf{AF} y)$:**\n\nSince all states satisfy the inner formula, the property holds in all states, including the initial state $s_0$.\n\n**Conclusion:** $\\mathcal{M}, s_0 \\models \\mathbf{AG}(x \\Rightarrow \\mathbf{AF} y)$ — the property **HOLDS**.\n\n> **Mark Distribution:** Kripke structure definition (2) + Table construction (1) + Step-by-step evaluation (3) + Conclusion (1) = 7 marks.\n\n---\n\n#### **Question B (14 Marks) — Alternative Choice**\n\n**Q.B (a)** `[KTU University Exam - May 2023, Model Question]`\n**7 Marks**\nExplain the **state space explosion problem** in model checking. Discuss any **three techniques** used to mitigate it.\n\n**Model Answer:**\n\n**State Space Explosion Problem:**\nWhen a system has $n$ boolean state variables, the number of possible states grows as $2^n$. Even modest systems can have astronomical state spaces:\n\n- $n = 20$ → $2^{20} \\approx 10^6$ states\n- $n = 64$ → $2^{64} \\approx 1.8 \\times 10^{19}$ states (exceeds RAM of any supercomputer)\n\nThis makes explicit state enumeration infeasible for real-world systems.\n\n**Three Mitigation Techniques:**\n\n1. **Symbolic Model Checking (BDDs):**\n   - Uses **Binary Decision Diagrams** to represent the transition relation and state sets as Boolean functions.\n   - Enables verification of systems with $10^{100}$ states or more (e.g., hardware verification of processors).\n   - Pioneered by **McMillan (1992)** with the **SMV** tool.\n\n2. **Abstraction:**\n   - Constructs a smaller abstract model that preserves properties of interest.\n   - Two main types: **over-approximation** (may add behaviors) and **under-approximation** (may remove behaviors).\n   - **Counterexample-guided abstraction refinement (CEGAR)** iteratively refines the abstraction when spurious counterexamples are found.\n\n3. **Partial Order Reduction:**\n   - Exploits the independence of concurrent transitions to reduce interleavings.\n   - Different execution orders of independent events produce equivalent states.\n   - Reduces the explored state space by considering only one representative from each equivalence class.\n\n> **Mark Distribution:** Problem explanation (2) + Three techniques (1.5 each) + Conclusion (0.5) = 7 marks.\n\n---\n\n**Q.B (b)** `[KTU University Exam - May 2023, Model Question]`\n**7 Marks**\nCompare **CTL** and **LTL** model checking in terms of expressiveness, complexity, and algorithmic approach. Give **one example** of a property expressible in CTL but not in LTL, and vice versa.\n\n**Model Answer:**\n\n| Aspect | CTL | LTL |\n|---|---|---|\n| **Expressiveness** | Branching-time (tree of futures) | Linear-time (single execution path) |\n| **Path Quantifiers** | $\\mathbf{E}, \\mathbf{A}$ (both) | Implicitly $\\mathbf{A}$ (universal) |\n| **Operators** | $\\mathbf{AX}, \\mathbf{EX}, \\mathbf{AF}, \\mathbf{EF}, \\mathbf{AG}, \\mathbf{EG}, \\mathbf{EU}, \\mathbf{AU}$ | $\\mathbf{X}, \\mathbf{F}, \\mathbf{G}, \\mathbf{U}$ only |\n| **Complexity** | $O(\\vert S \\vert \\cdot \\vert \\varphi \\vert)$ — polynomial | PSPACE-complete (exponential in $\\vert \\varphi \\vert$) |\n| **Algorithm** | Labeling-based, bottom-up subformula | Reduction to Büchi automaton emptiness |\n| **Tool Support** | NuSMV, Cadence SMV | SPIN |\n\n**CTL Property NOT in LTL:**\n$$\\mathbf{EF}(\\text{reset}) \\land \\mathbf{AG}(\\text{start} \\Rightarrow \\mathbf{AF}\\text{grant})$$\nThis requires the explicit existential path quantifier $\\mathbf{E}$, which LTL cannot express.\n\n**LTL Property NOT in CTL (in pure form):**\n$$\\mathbf{F}\\mathbf{G}(\\text{ready}) \\quad \\text{(eventually always ready)}$$\nLTL can express nested temporal operators on a single path; pure CTL cannot express this without using branching quantifiers.\n\n> **Mark Distribution:** Comparison table (4) + Examples (2) + Conclusion (1) = 7 marks.\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mistakes Where Students Lose Marks:**\n>\n> 1. **Confusing \"automated\" with \"automatic in all cases\"** — model checking is automatic for *finite-state* systems only. Mention finiteness.\n> 2. **Forgetting to draw the Kripke structure** in numerical questions — this is **compulsory** and carries 1–2 marks.\n> 3. **Incorrectly applying $\\mathbf{AG}$ vs $\\mathbf{AF}$** — remember: $\\mathbf{AG} p$ means \"globally in all paths\" while $\\mathbf{AF} p$ means \"eventually in all paths.\"\n> 4. **Not providing a counterexample when property fails** — even if the answer is \"fails,\" the counterexample path is mandatory.\n> 5. **Confusing complexity of CTL ($O(N \\cdot L)$) with LTL ($O(N \\cdot 2^L)$)** — this is a **favourite 2-mark question** in KTU exams.\n\n---\n\n### 5.4 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid Revision Checklist — Characteristics of Model Checking**\n>\n> ☐ **Model checking** = automatic verification of finite-state systems against temporal logic properties.\n>\n> ☐ **Five defining characteristics:** Automation, Exhaustiveness, Counterexample generation, Applicability to concurrent/reactive systems, Logical rigor.\n>\n> ☐ **Kripke structure** = $\\mathcal{M} = (S, S_0, R, L)$ with labeling function.\n>\n> ☐ **CTL complexity:** $O(\\vert S \\vert \\cdot \\vert \\varphi \\vert)$ — linear (efficient).\n>\n> ☐ **LTL complexity:** $O(\\vert S \\vert \\cdot 2^{\\vert \\varphi \\vert})$ — PSPACE-complete.\n>\n> ☐ **State space explosion:** $n$ boolean variables → $2^n$ states; mitigated by BDDs, abstraction, partial order reduction.\n>\n> ☐ **Counterexample:** Execution path demonstrating property violation; critical debugging aid.\n>\n> ☐ **Comparison vs Theorem Proving:** Model checking is automatic + exhaustive + produces counterexamples; theorem proving requires expertise + produces proofs.\n>\n> ☐ **Industrial tools:** SPIN (LTL), NuSMV (CTL/Symbolic), Cadence JasperGold (hardware), UPPAAL (real-time).\n>\n> ☐ **Always remember:** Model checking is **decidable** for finite-state systems — this is its core theoretical advantage.\n\n---\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t4",
          "title": "Transition Systems - Transition System",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module Module 1\n> - **Topic:** Transition Systems - Transition System\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition (KTU 2024 Syllabus Standard)\n\nA **Transition System (TS)** is a fundamental mathematical formalism used in automated verification to model the dynamic behavior of discrete-state reactive systems such as hardware circuits, communication protocols, embedded controllers, and software modules.\n\n> [!IMPORTANT]\n> **KTU Standard Definition (Clarke, Grumberg, Peled Style):**\n> A **transition system** $TS$ is a 6-tuple $\\langle S, \\, Act, \\, \\rightarrow, \\, I, \\, AP, \\, L \\rangle$ where:\n\n| Component | Symbol | Meaning |\n|---|---|---|\n| **State Set** | $S$ | Non-empty finite set of all reachable configurations |\n| **Action Alphabet** | $Act$ | Set of observable atomic action labels (e.g., $\\tau, a, b$) |\n| **Transition Relation** | $\\rightarrow \\subseteq S \\times Act \\times S$ | Directed labeled edges |\n| **Initial States** | $I \\subseteq S$ | Subset of states where execution may begin |\n| **Atomic Propositions** | $AP$ | Set of boolean state properties |\n| **Labeling Function** | $L: S \\rightarrow 2^{AP}$ | Maps each state to the set of true propositions |\n\nA transition is written $s \\xrightarrow{\\alpha} s'$ meaning *\"from state $s$, performing action $\\alpha$, the system can move to state $s'$\"*.\n\n### 1.2 Two Canonical Variants Encountered in KTU\n\n> [!NOTE]\n> **Variant 1 — Labeled Transition System (LTS / Action-based):**\n> Tuples $\\langle S, Act, \\rightarrow, I \\rangle$ — used in process algebra, model checking of concurrent systems, and equivalence checking (bisimulation). *No state labels.*\n\n> **Variant 2 — Kripke Structure (State-based):**\n> Tuples $\\langle S, \\rightarrow, I, AP, L \\rangle$ — used directly in CTL/LTL model checking where actions are abstracted away and only state properties matter.\n\n### 1.3 Conceptual Analogy — The Elevator Controller\n\nImagine an **elevator controller** as a transition system:\n\n- The states $S$ = `{GroundFloor, FirstFloor, SecondFloor, ThirdFloor, DoorsOpen, DoorsClosed}`\n- Actions $Act$ = `{goUp, goDown, openDoor, closeDoor, requestFloor(n)}`\n- A transition $s \\xrightarrow{\\text{goUp}} s'$ means *\"elevator moves from floor $s$ to floor $s'$\"*\n- $I = \\{GroundFloor\\}$ — the elevator always starts at ground floor\n- $L(GroundFloor) = \\{at\\_ground, doors\\_closed\\}$ — atomic propositions true in that state\n\nJust like the elevator can be in many configurations and move between them through actions, **every discrete software/hardware system can be represented as a graph of states and transitions**.\n\n> [!TIP]\n> **Why KTU loves this question:** Transition systems form the *semantic backbone* of every verification tool (SPIN, NuSMV, UPPAAL, CBMC). If you cannot draw or read a TS, you cannot pass an automated verification paper.\n\n### 1.4 Why It Matters in Automated Verification\n\nTransition systems provide the **mathematical semantics** over which temporal logic formulas (LTL, CTL) are interpreted. Verification asks: *\"Does every execution of TS satisfy the specification $\\phi$?\"* — this question is only meaningful once we have formally defined TS.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Operational Semantics — Reading a Transition System\n\nA transition system describes a system as a **directed labeled graph** with the following operational rules:\n\n1. **State**: A discrete snapshot of all variables at a moment in time.  \n2. **Action / Label**: The atomic event triggering a state change (input, output, internal step, or the silent action $\\tau$).\n3. **Transition Relation** $\\rightarrow$: The set of all permitted one-step moves.\n4. **Initial State Set** $I$: Entry points for system execution. Non-deterministic systems have $\\vert I \\vert > 1$.\n5. **Determinism vs Non-Determinism**:\n   - *Deterministic*: $\\forall s \\in S, \\forall \\alpha \\in Act: \\vert \\{s' \\mid s \\xrightarrow{\\alpha} s'\\} \\vert \\leq 1$\n   - *Non-deterministic*: At least one state has $\\geq 2$ outgoing transitions on the same action, modeling environmental choice or race conditions.\n6. **Labeling Function** $L$: Decorates states with propositional facts for temporal-logic evaluation.\n\n### 2.2 KTU High-Yield Formula Sheet\n\n| # | Concept | Formal Expression | Notes |\n|---|---|---|---|\n| 1 | Transition system tuple | $TS = (S, Act, \\rightarrow, I, AP, L)$ | Full Kripke-style |\n| 2 | LTS tuple (simpler) | $TS = (S, Act, \\rightarrow, I)$ | Action-based variant |\n| 3 | Transition notation | $s \\xrightarrow{\\alpha} s'$ | \"s moves to s' via action α\" |\n| 4 | Post-image operator | $Post(s) = \\{s' \\in S \\mid \\exists \\alpha: s \\xrightarrow{\\alpha} s'\\}$ | Direct successors |\n| 5 | Pre-image operator | $Pre(s) = \\{s' \\in S \\mid \\exists \\alpha: s' \\xrightarrow{\\alpha} s\\}$ | Direct predecessors |\n| 6 | Image of set $C$ | $Post^{*}(C)$ | All states reachable from $C$ via 0+ transitions |\n| 7 | Path | $\\pi = s_0 \\xrightarrow{\\alpha_1} s_1 \\xrightarrow{\\alpha_2} s_2 \\cdots$ | Infinite / finite execution |\n| 8 | Maximal Path | $\\pi \\in Paths(s)$ | Cannot be extended further |\n| 9 | Trace of $\\pi$ | $trace(\\pi) = \\alpha_1, \\alpha_2, \\ldots$ | Sequence of actions |\n| 10 | Finite / Infinite | $Paths_{fin}(s)$, $Paths_{full}(s)$ | Finiteness matters for LTL vs CTL* |\n| 11 | $L(s) \\in 2^{AP}$ | Labeling is a *set* of true props | E.g., $L(s) = \\{p, \\neg q\\}$ |\n| 12 | Deadlock State | $s$ such that $Post(s) = \\emptyset$ | Terminal — no outgoing edges |\n| 13 | Reachable States | $Reach(TS) = Post^{*}(I)$ | Subgraph of interest |\n| 14 | Non-determinism | $\\exists s,\\alpha: \\mid Post(s,\\alpha) \\mid \\geq 2$ | Defines NTS |\n\n> [!NOTE]\n> **Exam mnemonic — \"SAP-IAL\"**: **S**tates, **A**ctions, transitions, **I**nitial states, **A**tomic propositions, **L**abeling — the 6 components in canonical order.\n\n### 2.3 Real-World Engineering Utility\n\n- **Hardware Verification**: Sequential circuits modeled as state-transition graphs (flip-flops = states, gate delays = transitions).\n- **Protocol Verification**: TCP, cache coherence, mutual exclusion protocols expressed as TS for SPIN model checking.\n- **Embedded & IoT**: UPPAAL timed automata extend TS with clock variables for real-time verification.\n- **Software Model Checking**: SLAM, CBMC, Java Pathfinder convert programs into TS (state = program counter + variable values, transition = statement execution).\n- **Safety-Critical Systems**: DO-178C and ISO 26262 mandate formal verification using TS-based models.\n\n### 2.4 Determinism vs Non-Determinism — A Critical Distinction\n\n> [!IMPORTANT]\n> A TS is **deterministic** iff $\\forall s \\in S, \\forall \\alpha \\in Act: \\vert \\{s' : s \\xrightarrow{\\alpha} s'\\} \\vert \\leq 1$.  \n> For initial states, $\\vert I \\vert = 1$ is also required.\n\n**Non-determinism is essential** for modeling: environment choices, scheduler decisions, race conditions, abstraction over data (e.g., unknown input values), and underspecification. Most real verification benchmarks are *non-deterministic*.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Example Transition System (KTU Standard Illustration)\n\nConsider a simple **Vending Machine** that accepts a 5-rupee coin and dispenses a beverage costing 5 rupees. Let:\n\n$$S = \\{s_0, s_1, s_2\\}, \\quad Act = \\{\\text{coin}, \\text{dispense}\\}, \\quad I = \\{s_0\\}$$\n\n$$AP = \\{\\text{ready}, \\text{has5}, \\text{delivered}\\}, \\quad L(s_0) = \\{\\text{ready}\\}, \\quad L(s_1) = \\{\\text{has5}\\}, \\quad L(s_2) = \\{\\text{delivered}\\}$$\n\nTransitions:\n\n$$s_0 \\xrightarrow{\\text{coin}} s_1, \\quad s_1 \\xrightarrow{\\text{dispense}} s_2, \\quad s_2 \\xrightarrow{\\text{coin}} s_2$$\n\n### 3.2 Exhaustive Derivation — Reachability Computation\n\n**Goal:** Compute $Reach(TS) = Post^{*}(I)$ step by step.\n\n> **Step 1** — Initialize frontier with initial states.\n\n$$F_0 = I = \\{s_0\\}, \\quad R_0 = \\{s_0\\}$$\n\n> **Step 2** — Compute Post-image of $F_0$ using the transition relation.\n\n$$Post(F_0) = Post(\\{s_0\\}) = \\{s' \\in S \\mid \\exists \\alpha: s_0 \\xrightarrow{\\alpha} s'\\}$$\n\nScanning transitions: $s_0 \\xrightarrow{\\text{coin}} s_1$ is the only outgoing transition from $s_0$.\n\n$$Post(F_0) = \\{s_1\\}$$\n\n> **Step 3** — Add newly discovered states to the reachable set, advance frontier.\n\n$$F_1 = Post(F_0) \\setminus R_0 = \\{s_1\\} \\setminus \\{s_0\\} = \\{s_1\\}$$\n\n$$R_1 = R_0 \\cup F_1 = \\{s_0, s_1\\}$$\n\n> **Step 4** — Compute Post-image of $F_1$.\n\n$$Post(F_1) = Post(\\{s_1\\}) = \\{s' \\in S \\mid \\exists \\alpha: s_1 \\xrightarrow{\\alpha} s'\\}$$\n\nFrom $s_1$ the only outgoing transition is $s_1 \\xrightarrow{\\text{dispense}} s_2$.\n\n$$Post(F_1) = \\{s_2\\}$$\n\n> **Step 5** — Update frontier and reachable set.\n\n$$F_2 = Post(F_1) \\setminus R_1 = \\{s_2\\} \\setminus \\{s_0, s_1\\} = \\{s_2\\}$$\n\n$$R_2 = R_1 \\cup F_2 = \\{s_0, s_1, s_2\\}$$\n\n> **Step 6** — Compute Post-image of $F_2$.\n\n$$Post(F_2) = Post(\\{s_2\\}) = \\{s' \\in S \\mid s_2 \\xrightarrow{\\alpha} s'\\}$$\n\nThe only outgoing transition from $s_2$ is the self-loop $s_2 \\xrightarrow{\\text{coin}} s_2$.\n\n$$Post(F_2) = \\{s_2\\}$$\n\n> **Step 7** — Check fixed-point condition.\n\n$$F_3 = Post(F_2) \\setminus R_2 = \\{s_2\\} \\setminus \\{s_0, s_1, s_2\\} = \\emptyset$$\n\nThe frontier is empty — fixed point reached.\n\n$$Reach(TS) = R_2 = \\{s_0, s_1, s_2\\} = S$$\n\n**Conclusion**: Every state in the machine is reachable from the initial state. Therefore this transition system has *no unreachable states*.\n\n### 3.3 Analytical Matrix — Behavioural Properties\n\nWe evaluate standard KTU properties on the vending machine TS:\n\n| Property | Definition | Holds in Vending Machine TS? | Justification |\n|---|---|---|---|\n| **Deadlock** | $\\exists s \\in Reach(TS): Post(s) = \\emptyset$ | **No** | Every state has an outgoing transition ($s_2$ self-loops) |\n| **Liveness of $s_2$** | $\\forall$ path from $s_0$, $s_2$ occurs infinitely often only if loop | **Yes (reachable)** | $s_2$ is reachable in every path |\n| **Determinism** | $\\forall s,\\alpha: \\vert Post(s,\\alpha) \\vert \\leq 1$ | **No** | Self-loop on $s_2$ competes with no other action, but the choice to insert coin again makes behaviour non-deterministic in practice |\n| **Non-trivial initial state** | $\\vert I \\vert = 1$ | **Yes** | $I = \\{s_0\\}$ |\n| **All states labelled** | $\\forall s \\in S: L(s) \\neq \\emptyset$ | **Yes** | $L(s_0) = \\{ready\\}, L(s_1) = \\{has5\\}, L(s_2) = \\{delivered\\}$ |\n\n### 3.4 Exhaustive Derivation — Verification of a CTL Property\n\nSuppose we want to verify: $\\phi = \\mathbf{EF} \\, delivered$ (there exists a path eventually reaching a state labelled $delivered$).\n\n> **Step 1** — Identify the satisfaction set for atomic proposition $delivered$.\n\n$$\\llbracket delivered \\rrbracket = \\{s \\in S \\mid delivered \\in L(s)\\} = \\{s_2\\}$$\n\n> **Step 2** — Define the $\\mathbf{EF}$ fixed-point computation:\n\n$$\\mathbf{EF}\\,\\phi \\equiv \\mu Z.\\, \\phi \\lor \\mathbf{EX}\\,Z$$\n\n> **Step 3** — Iterate the fixed point starting from $Z_0 = \\emptyset$.\n\n$$Z_0 = \\emptyset$$\n\n$$Z_1 = \\llbracket delivered \\rrbracket \\cup \\mathbf{EX}(Z_0) = \\{s_2\\} \\cup \\emptyset = \\{s_2\\}$$\n\n$$Z_2 = \\{s_2\\} \\cup \\mathbf{EX}(\\{s_2\\}) = \\{s_2\\} \\cup Pre(\\{s_2\\})$$\n\nComputing $Pre(\\{s_2\\})$: states that have an outgoing transition to $s_2$:\n\n$$Pre(\\{s_2\\}) = \\{s_1, s_2\\}$$\n\n(because $s_1 \\xrightarrow{dispense} s_2$ and $s_2 \\xrightarrow{coin} s_2$).\n\n$$Z_2 = \\{s_2\\} \\cup \\{s_1, s_2\\} = \\{s_1, s_2\\}$$\n\n$$Z_3 = \\{s_2\\} \\cup Pre(\\{s_1, s_2\\}) = \\{s_2\\} \\cup \\{s_0, s_1, s_2\\} = \\{s_0, s_1, s_2\\}$$\n\n$$Z_4 = \\{s_2\\} \\cup Pre(\\{s_0, s_1, s_2\\}) = \\{s_2\\} \\cup \\{s_0, s_1, s_2\\} = \\{s_0, s_1, s_2\\}$$\n\nFixed point: $Z_3 = Z_4$, so $\\llbracket \\mathbf{EF}\\,delivered \\rrbracket = \\{s_0, s_1, s_2\\}$.\n\n> **Step 4** — Verification result:\n\nSince $s_0 \\in I$ and $s_0 \\in \\llbracket \\mathbf{EF}\\,delivered \\rrbracket$, the TS **satisfies** $\\mathbf{EF}\\,delivered$. ✓\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Mermaid Diagram — Vending Machine Transition System\n\n```mermaid\ngraph LR\n    s0[\"S0 ready\"] -- coin --> s1[\"S1 has5\"]\n    s1 -- dispense --> s2[\"S2 delivered\"]\n    s2 -- coin --> s2\n    s0_init((\"I\")) -.-> s0\n    classDef init fill:#FFD700,stroke:#333,stroke-width:2px,color:#000\n    class s0_init init\n```\n\n> [!NOTE]\n> **Reading the diagram:**  \n> - Double circle = initial state.  \n> - Rectangles = states with $L(s)$ shown.  \n> - Edges = transitions $s \\xrightarrow{\\alpha} s'$.  \n> - The self-loop on $s_2$ represents idempotent re-coin behavior.\n\n### 4.2 Mermaid Diagram — Generic Transition System Architecture (Block-Level Functional Flow)\n\n```mermaid\ngraph TD\n    A[Initial State Set I] --> B[Frontier F0]\n    B --> C[Post-Image Computation]\n    C --> D{New States Found?}\n    D -- Yes --> E[Update Reachable Set R]\n    E --> C\n    D -- No --> F[Fixed-Point Reached]\n    F --> G[Reachability Analysis]\n    G --> H[Temporal Logic Evaluation]\n    H --> I[Verification Result: SAT or UNSAT]\n```\n\n### 4.3 Mermaid Diagram — Classification of Transition Systems\n\n```mermaid\ngraph TD\n    TS[Transition System TS]\n    TS --> LTS[Labeled Transition System LTS]\n    TS --> KS[Kripke Structure]\n    LTS --> DET[Deterministic]\n    LTS --> NONDET[Non-Deterministic]\n    KS --> FA[Finite Automaton Type]\n    KS --> IO[IO Automaton Type]\n    KS --> TA[Timed Automaton Type]\n    FA --> DFA[DFA]\n    FA --> NFA[NFA]\n    TA --> UPPAAL[UPPAAL Timed TS]\n    TA --> HYBRID[Hybrid Automaton]\n```\n\n### 4.4 Components Mapping Block Diagram\n\n```mermaid\ngraph LR\n    S[States S] --> T[Transitions →]\n    A[Actions Act] --> T\n    I[Initial States I] --> S\n    T --> AP[Atomic Propositions AP]\n    AP --> L[Labeling Function L]\n    L --> S\n```\n\n> [!TIP]\n> **KTU Visual Literacy Tip:** Examiners often give you a Mermaid-like diagram and ask *\"Identify the 6-tuple\"*. Practice extracting $S$, $Act$, $\\rightarrow$, $I$, $AP$, $L$ from any graph. State labels = $L(s)$, edge labels = $Act$.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A Questions (3 Marks Each)\n\n---\n\n**Q1. [KTU University Exam – Dec 2023]**  \n*Define a transition system. List its components and explain the role of the labeling function.*\n\n**Model Answer (3 marks):**\n\nA transition system is a 6-tuple $TS = (S, Act, \\rightarrow, I, AP, L)$ where $S$ is the set of states, $Act$ is the set of actions, $\\rightarrow \\subseteq S \\times Act \\times S$ is the transition relation, $I \\subseteq S$ is the set of initial states, $AP$ is the set of atomic propositions, and $L: S \\rightarrow 2^{AP}$ is the labeling function. **(2 marks)**\n\nThe labeling function $L$ assigns to each state the set of atomic propositions that hold in that state. This allows temporal logic formulas (e.g., $AG\\,p$) to be evaluated over system states — without $L$, the system would have no observable properties and verification would be impossible. **(1 mark)**\n\n---\n\n**Q2. [KTU University Exam – May 2024]**  \n*Differentiate between a Labeled Transition System (LTS) and a Kripke Structure with a suitable example.*\n\n**Model Answer (3 marks):**\n\n| Aspect | LTS | Kripke Structure |\n|---|---|---|\n| Tuple | $(S, Act, \\rightarrow, I)$ | $(S, \\rightarrow, I, AP, L)$ |\n| Edge labels | Actions from $Act$ | None (silent) |\n| State labels | None | $L(s) \\in 2^{AP}$ |\n| Used in | Bisimulation, process algebra | CTL/LTL model checking **(2 marks)** |\n\n**Example:** A traffic light modelled as LTS has edges labelled `goGreen`, `goYellow`, `goRed`. The same system as a Kripke Structure has states labelled $\\{green\\}$, $\\{yellow\\}$, $\\{red\\}$ with silent transitions between them. **(1 mark)**\n\n---\n\n### 5.2 Part B Questions (14 Marks — Module Internal Choice)\n\n---\n\n#### **Question A (14 Marks)** — [KTU University Exam – Dec 2023]\n\n**(a)** Define a transition system formally. Explain the difference between deterministic and non-deterministic transition systems with examples. **(7 marks)**\n\n**(b)** Consider the transition system $TS$ shown below. Compute the set of reachable states $Reach(TS)$ and identify whether the system contains any deadlock states. Justify your answer. **(7 marks)**\n\n$$\nS = \\{s_0, s_1, s_2, s_3, s_4\\}, \\quad Act = \\{a, b, c\\}, \\quad I = \\{s_0\\}\n$$\n\nTransitions:\n$$\ns_0 \\xrightarrow{a} s_1, \\quad s_0 \\xrightarrow{b} s_2, \\quad s_1 \\xrightarrow{c} s_3, \\quad s_2 \\xrightarrow{c} s_3, \\quad s_3 \\xrightarrow{a} s_4, \\quad s_3 \\xrightarrow{b} s_4\n$$\n\n---\n\n**Model Answer:**\n\n**(a) Formal Definition and Determinism (7 marks):**\n\nA transition system is a 6-tuple $TS = (S, Act, \\rightarrow, I, AP, L)$:  \n- $S$: non-empty finite set of states.  \n- $Act$: finite set of action labels.  \n- $\\rightarrow \\subseteq S \\times Act \\times S$: transition relation.  \n- $I \\subseteq S$: non-empty set of initial states.  \n- $AP$: finite set of atomic propositions.  \n- $L: S \\rightarrow 2^{AP}$: labeling function. **(3 marks)**\n\nA TS is **deterministic** if for every state $s$ and action $\\alpha$, at most one successor exists:\n\n$$\\forall s \\in S, \\forall \\alpha \\in Act: \\vert \\{s' \\in S \\mid s \\xrightarrow{\\alpha} s'\\} \\vert \\leq 1$$\n\nand $\\vert I \\vert = 1$. It is **non-deterministic** if at least one state has multiple successors for the same action (i.e., the transition relation is not a function). **(2 marks)**\n\n*Example — Deterministic:* A 2-bit binary counter has $\\vert I \\vert = 1$ and each state has exactly one transition on `tick`. *Example — Non-deterministic:* A network packet scheduler where on `arrival`, the packet may be queued to either $s_1$ or $s_2$ depending on environment. **(2 marks)**\n\n**(b) Reachability Computation and Deadlock Analysis (7 marks):**\n\n*Step 1:* Initialize $R_0 = \\{s_0\\}$, $F_0 = \\{s_0\\}$. **(0.5 mark)**\n\n*Step 2:* $Post(\\{s_0\\}) = \\{s_1, s_2\\}$ (because $s_0 \\xrightarrow{a} s_1$ and $s_0 \\xrightarrow{b} s_2$). **(1 mark)**\n\n*Step 3:* $F_1 = \\{s_1, s_2\\}$, $R_1 = \\{s_0, s_1, s_2\\}$. **(0.5 mark)**\n\n*Step 4:* $Post(\\{s_1, s_2\\}) = \\{s_3\\}$ (both $s_1 \\xrightarrow{c} s_3$ and $s_2 \\xrightarrow{c} s_3$). **(1 mark)**\n\n*Step 5:* $F_2 = \\{s_3\\}$, $R_2 = \\{s_0, s_1, s_2, s_3\\}$. **(0.5 mark)**\n\n*Step 6:* $Post(\\{s_3\\}) = \\{s_4\\}$ (both $s_3 \\xrightarrow{a} s_4$ and $s_3 \\xrightarrow{b} s_4$). **(1 mark)**\n\n*Step 7:* $F_3 = \\{s_4\\}$, $R_3 = \\{s_0, s_1, s_2, s_3, s_4\\}$. **(0.5 mark)**\n\n*Step 8:* $Post(\\{s_4\\}) = \\emptyset$ — no outgoing transitions from $s_4$. Fixed point reached. **(1 mark)**\n\n**Final answer:** $Reach(TS) = \\{s_0, s_1, s_2, s_3, s_4\\} = S$. **(0.5 mark)**\n\n**Deadlock analysis:** State $s_4$ has $Post(s_4) = \\emptyset$, so it is a **deadlock state**. The system can reach a configuration from which no further progress is possible. **(1 mark)**\n\n---\n\n#### **Question B (14 Marks)** — [KTU University Exam – May 2024]\n\n**(a)** Explain the role of the labeling function in transition systems. Why is it essential for verifying temporal logic properties? Illustrate with an example. **(7 marks)**\n\n**(b)** Given the transition system below, verify whether the property $\\mathbf{AG}(\\text{requested} \\Rightarrow \\mathbf{AF}\\, \\text{granted})$ holds.\n\n$$\nS = \\{s_0, s_1, s_2\\}, \\quad Act = \\{req, gnt\\}, \\quad I = \\{s_0\\}\n$$\n\nTransitions and labels:  \n- $L(s_0) = \\emptyset$, $s_0 \\xrightarrow{req} s_1$  \n- $L(s_1) = \\{requested\\}$, $s_1 \\xrightarrow{gnt} s_2$  \n- $L(s_2) = \\{granted\\}$, $s_2 \\xrightarrow{req} s_1$  \n\n**(7 marks)**\n\n---\n\n**Model Answer:**\n\n**(a) Role of the Labeling Function (7 marks):**\n\nThe labeling function $L: S \\rightarrow 2^{AP}$ maps each state to the set of atomic propositions true in that state. **(1 mark)** It serves three essential purposes:\n\n1. **Semantic Grounding for Logic:** Temporal logics (LTL, CTL) operate on propositions. Without $L$, the meaning of $s \\models p$ is undefined. **(1 mark)**\n2. **Abstraction Layer:** It hides internal implementation details (variable values, counters, etc.) and exposes only relevant boolean properties, reducing state-space explosion. **(1 mark)**\n3. **Enables State-based Reasoning:** Operators like $\\mathbf{AG}$ and $\\mathbf{EF}$ are evaluated over labeled states, allowing specification of *what* must hold rather than *how* it is achieved. **(1 mark)**\n\n*Example:* A mutex lock TS has $L(\\text{locked}) = \\{mutex\\_held\\}$ and $L(\\text{free}) = \\{mutex\\_free\\}$. The property $\\mathbf{AG}(\\text{req} \\Rightarrow \\mathbf{AF}\\, \\text{grant})$ can be checked by examining $L$ at each reachable state. **(3 marks)**\n\n**(b) Verification of $\\mathbf{AG}(\\text{requested} \\Rightarrow \\mathbf{AF}\\, \\text{granted})$ (7 marks):**\n\n*Step 1:* Compute sub-formula $\\mathbf{AF}\\,granted$ — states from which `granted` is reachable on every path.\n\n$$\\llbracket granted \\rrbracket = \\{s_2\\}$$\n\nFixed-point computation for $\\mathbf{AF}\\,\\phi \\equiv \\mu Z.\\, \\phi \\lor \\mathbf{AX}\\,Z$:\n\n$$Z_0 = \\{s_2\\}$$\n\n$$Z_1 = \\{s_2\\} \\cup \\mathbf{AX}(Z_0) = \\{s_2\\} \\cup \\{s_1\\} = \\{s_1, s_2\\}$$\n\n(since $s_1 \\xrightarrow{gnt} s_2$ so $s_1 \\in Pre(\\{s_2\\}) \\Rightarrow s_1 \\in \\mathbf{AX}(\\{s_2\\})$)\n\n$$Z_2 = \\{s_2\\} \\cup \\mathbf{AX}(\\{s_1, s_2\\}) = \\{s_2\\} \\cup \\{s_0, s_1\\} = \\{s_0, s_1, s_2\\}$$\n\n$$Z_3 = Z_2$$ — fixed point reached.\n\n$$\\llbracket \\mathbf{AF}\\,granted \\rrbracket = \\{s_0, s_1, s_2\\} = S$$ **(2 marks)**\n\n*Step 2:* Compute sub-formula $requested \\Rightarrow \\mathbf{AF}\\,granted$:\n\n$$\\llbracket requested \\rrbracket = \\{s_1\\}$$\n\n$$\\llbracket requested \\Rightarrow \\mathbf{AF}\\,granted \\rrbracket = (S \\setminus \\{s_1\\}) \\cup \\{s_1\\} = S$$ **(1 mark)**\n\n(because $s_1$ is in both $\\llbracket requested \\rrbracket$ and $\\llbracket \\mathbf{AF}\\,granted \\rrbracket$).\n\n*Step 3:* Compute outer $\\mathbf{AG}$ — all states where the implication holds on every path:\n\n$$\\llbracket \\mathbf{AG}\\,\\psi \\rrbracket = \\nu Z.\\, \\psi \\cap \\mathbf{AX}\\,Z$$\n\n$$\\llbracket \\mathbf{AG}(requested \\Rightarrow \\mathbf{AF}\\,granted) \\rrbracket = S$$ **(2 marks)**\n\n(Sub-formula $\\psi$ holds globally, so its $\\mathbf{AG}$ is $S$.)\n\n*Step 4:* Check whether all initial states are in the satisfaction set:\n\n$$I = \\{s_0\\} \\subseteq S = \\llbracket \\mathbf{AG}(requested \\Rightarrow \\mathbf{AF}\\,granted) \\rrbracket \\quad \\checkmark$$ **(2 marks)**\n\n**Conclusion:** The transition system **satisfies** the property $\\mathbf{AG}(\\text{requested} \\Rightarrow \\mathbf{AF}\\,granted)$. ✓\n\n---\n\n### 5.3 KTU Examiner's Pitfall Callout\n\n> [!WARNING]\n> **Common Mark-Loss Pitfalls in Transition System Questions:**\n> 1. **Forgetting the labeling function** — Many students define $TS$ as a 4-tuple, missing $AP$ and $L$. You lose 1–2 marks immediately.\n> 2. **Confusing LTS and Kripke Structure** — LTS has *edge labels*; Kripke has *state labels*. Do not mix them.\n> 3. **Initial state set vs single initial state** — Non-determinism requires $\\vert I \\vert \\geq 1$ but typically $> 1$. Always say \"set of initial states\".\n> 4. **Reachability fixed-point termination** — Always show $F_k = \\emptyset$ for the last step; examiners award partial credit for explicit termination.\n> 5. **Path vs Trace** — A *path* is a sequence of states; a *trace* is the sequence of *actions* along the path. Examiners test this distinction.\n> 6. **Self-loops** — Forgetting self-loops in Post-image computation leads to incorrect reachability sets.\n> 7. **Deadlock misidentification** — A deadlock is a state with **no outgoing transitions for *any* action**, not just a particular one.\n\n### 5.4 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid Revision Checklist — Transition Systems**\n\n- ✅ **6-tuple**: $TS = (S, Act, \\rightarrow, I, AP, L)$ — remember order **S-A-IAL** mnemonic.\n- ✅ **Two variants**: LTS (action-based, no $L$) vs Kripke Structure (state-based, with $L$).\n- ✅ **Transition notation**: $s \\xrightarrow{\\alpha} s'$ — write it as a triplet relation.\n- ✅ **Determinism condition**: $\\forall s,\\alpha: \\vert Post(s,\\alpha) \\vert \\leq 1$ AND $\\vert I \\vert = 1$.\n- ✅ **Post-image operator**: $Post(C) = \\{s' \\mid \\exists s \\in C, \\alpha: s \\xrightarrow{\\alpha} s'\\}$.\n- ✅ **Reachability fixed-point**: Iterate $R_{k+1} = R_k \\cup Post(R_k)$ until $R_{k+1} = R_k$.\n- ✅ **Deadlock**: $s$ with $Post(s) = \\emptyset$ — a terminal state.\n- ✅ **Labeling function** $L: S \\rightarrow 2^{AP}$ — enables CTL/LTL evaluation.\n- ✅ **Paths** are state sequences; **traces** are action sequences.\n- ✅ **Non-determinism** models environment choice, race conditions, abstraction.\n- ✅ **Initial states** $I$ — entry points of execution; $\\vert I \\vert \\geq 1$.\n- ✅ **Reachable set** $Reach(TS) = Post^{*}(I)$ — foundation for all verification.\n- ✅ **Real-world uses**: SPIN, NuSMV, UPPAAL, CBMC all consume TS as input.\n- ✅ **Exam pattern**: definition (3 marks) + reachability/computation (7 marks) + property verification (4 marks).\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t5",
          "title": "Direct Predecessors and Successors",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Direct Predecessors and Successors\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\nIn the context of **Automated Verification** and **Model Checking**, every system is formally modeled as a **Kripke Structure** $M = (S, S_0, R, L)$, where $S$ is a finite set of states, $S_0 \\subseteq S$ is the set of initial states, $R \\subseteq S \\times S$ is the total transition relation, and $L : S \\to 2^{AP}$ is the labeling function over atomic propositions.\n\nOn this directed graph, the notion of \"neighborhood\" is captured by two fundamental operators:\n\n> [!IMPORTANT]\n> **Direct Successor (Post-Image of a single state):**\n> A state $s' \\in S$ is a **direct successor** of $s \\in S$ if and only if $(s, s') \\in R$. Equivalently, the set of all direct successors of $s$ is denoted:\n> $$\\text{Post}(s) = \\{ s' \\in S \\mid (s, s') \\in R \\}$$\n\n> [!IMPORTANT]\n> **Direct Predecessor (Pre-Image of a single state):**\n> A state $s' \\in S$ is a **direct predecessor** of $s \\in S$ if and only if $(s', s) \\in R$. Equivalently, the set of all direct predecessors of $s$ is denoted:\n> $$\\text{Pre}(s) = \\{ s' \\in S \\mid (s', s) \\in R \\}$$\n\nFor a **set of states** $S' \\subseteq S$, these definitions extend by set union:\n\n$$\\text{Post}(S') = \\bigcup_{s \\in S'} \\text{Post}(s) = \\{ s' \\in S \\mid \\exists s \\in S' . (s, s') \\in R \\}$$\n\n$$\\text{Pre}(S') = \\bigcup_{s \\in S'} \\text{Pre}(s) = \\{ s' \\in S \\mid \\exists s \\in S' . (s', s) \\in R \\}$$\n\n## 1.2 Conceptual Analogy — The Kerala Road Network\n\nImagine the KTU campus as a single **state** and the one-way roads connecting it to neighbouring cities (Trivandrum, Kollam, Kochi) as **transitions**.\n\n- The **direct successors** of a city are the cities you can reach by **taking exactly one road** out of it (i.e., the immediate destinations you can drive to without any intermediate stop).\n- The **direct predecessors** of a city are the cities from which **exactly one road leads into it** (i.e., the places a traveller could have been in just before arriving).\n\nJust as Google Maps computes \"departing from Kochi, you can directly reach Ernakulam or Aluva in one move\", the model checker computes $\\text{Post}$ and $\\text{Pre}$ to navigate the state space efficiently.\n\n## 1.3 Key Notation Cheat-Sheet\n\n| Symbol | Meaning |\n| :--- | :--- |\n| $\\text{Post}(s)$ | Set of all direct successors of $s$ |\n| $\\text{Pre}(s)$ | Set of all direct predecessors of $s$ |\n| $\\text{Post}(S')$ | Set of all direct successors of any state in $S'$ |\n| $\\text{Pre}(S')$ | Set of all direct predecessors of any state in $S'$ |\n| $\\text{Post}^*(S')$ | Transitive closure — states reachable in $\\geq 0$ steps |\n| $\\text{Pre}^*(S')$ | Transitive closure — states that can reach $S'$ in $\\geq 0$ steps |\n\n> [!NOTE]\n> The transition relation $R$ is **total** in a Kripke structure. This means that **every state has at least one direct successor** (a deadlock is represented by a self-loop, not by the absence of outgoing edges). This is a KTU exam favourite — students often forget the totality constraint.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU Formula Sheet\n\n## 2.1 Operational Characterisation\n\nThe **Pre** and **Post** operators are the *atomic navigation primitives* of model checking. Every advanced algorithm — CTL fixed-point computation, reachability, deadlock detection, LTL emptiness, CEGAR refinement — is built on repeated application of these two operators. They are formally characterised as follows:\n\n- **Forward Step (Post):** Given a current \"frontier\" of states $X$, the next frontier is $X' = \\text{Post}(X) \\setminus X$. This is the **one-step image computation**.\n- **Backward Step (Pre):** Given a target set $T$ of \"bad\" states, the previous frontier is $X' = \\text{Pre}(X) \\setminus X$. This is the **one-step pre-image computation**, used in *backward* reachability.\n\n## 2.2 The \"Why\" and \"How\" — Algorithmic Intuition\n\n**Why are they needed?**\nIn a Kripke structure with $10^6$ states, we cannot enumerate the state space naively. Instead, we represent sets of states as **symbolic objects** (BDDs, Boolean formulas, or bit-vectors) and compute $\\text{Pre}$ / $\\text{Post}$ via relational image computation.\n\n**How are they computed?**\n1. **Explicitly:** Iterate over the transition relation $R$ and for each $(s, s') \\in R$ add $s'$ to $\\text{Post}(s)$.\n2. **Symbolically:** Using the transition relation as a Boolean formula $R(\\bar{x}, \\bar{x}')$, $\\text{Post}(S(\\bar{x}))$ is computed by $\\exists \\bar{x} . \\left( S(\\bar{x}) \\wedge R(\\bar{x}, \\bar{x}') \\right)$ and then renaming $\\bar{x}' \\to \\bar{x}$.\n\n## 2.3 The Reachability Tie-In\n\nThe greatest practical application of $\\text{Post}$ is the iterative computation of the **reachable state space**:\n\n$$\\text{Reach}(S_0) = \\mu X . \\; S_0 \\cup \\text{Post}(X)$$\n\nThis is a **least fixed point** in the powerset lattice $(2^S, \\subseteq)$. The fixed-point iteration proceeds as:\n\n$$X_0 = S_0$$\n$$X_{i+1} = X_i \\cup \\text{Post}(X_i)$$\n\nThe iteration **terminates** at the smallest $k$ where $X_{k+1} = X_k$, giving us the smallest set of states reachable from any initial state in any number of steps.\n\n> [!NOTE]\n> The **Post** operator is monotonic with respect to set inclusion: $A \\subseteq B \\implies \\text{Post}(A) \\subseteq \\text{Post}(B)$. This monotonicity is precisely what guarantees the termination of the fixed-point iteration (via the Knaster–Tarski theorem).\n\n## 2.4 KTU Formula Sheet\n\n| Concept | Formula / Property |\n| :--- | :--- |\n| Direct Successor of $s$ | $\\text{Post}(s) = \\{ s' \\mid (s, s') \\in R \\}$ |\n| Direct Predecessor of $s$ | $\\text{Pre}(s) = \\{ s' \\mid (s', s) \\in R \\}$ |\n| Image of a set | $\\text{Post}(S') = \\bigcup_{s \\in S'} \\text{Post}(s)$ |\n| Pre-image of a set | $\\text{Pre}(S') = \\bigcup_{s \\in S'} \\text{Pre}(s)$ |\n| Reachability (Forward) | $\\text{Reach}(S_0) = \\mu X . S_0 \\cup \\text{Post}(X)$ |\n| Backward Reachability | $\\text{Pre}^*(T) = \\mu X . T \\cup \\text{Pre}(X)$ |\n| Monotonicity | $A \\subseteq B \\implies \\text{Post}(A) \\subseteq \\text{Post}(B)$ |\n| Totality | $\\forall s \\in S . \\text{Post}(s) \\neq \\emptyset$ |\n| Idempotence of fixed point | $X = X \\cup \\text{Post}(X) \\iff \\text{Post}(X) \\subseteq X$ |\n| Distribution over union | $\\text{Post}(A \\cup B) = \\text{Post}(A) \\cup \\text{Post}(B)$ |\n\n## 2.5 Real-World Engineering Utility\n\n- **Hardware Verification (Intel, AMD):** Symbolic model checking using BDDs computes $\\text{Post}$ of the initial state set to prove that a processor's pipeline can never enter a \"bad\" state (e.g., a data hazard).\n- **Software Verification (Microsoft SLAM, Java PathFinder):** $\\text{Pre}$ is used in **counterexample-guided abstraction refinement (CEGAR)** to trace back spurious counterexamples to the root cause.\n- **Network Protocol Verification:** $\\text{Post}$ of the initial handshake state set is used to verify the full state space of TCP, BGP, or TLS.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Worked Example — Computing $\\text{Post}$ and $\\text{Pre}$ on a Concrete Kripke Structure\n\nConsider the Kripke structure $M = (S, S_0, R, L)$ defined as:\n\n$$S = \\{s_0, s_1, s_2, s_3, s_4\\}$$\n\n$$S_0 = \\{s_0\\}$$\n\n$$R = \\{ (s_0, s_1), (s_0, s_2), (s_1, s_2), (s_1, s_3), (s_2, s_3), (s_3, s_4), (s_4, s_2) \\}$$\n\n**Derivation of $\\text{Post}(s_i)$ for every state:**\n\n$$\\text{Post}(s_0) = \\{ s' \\in S \\mid (s_0, s') \\in R \\} = \\{ s_1, s_2 \\}$$\n\n$$\\text{Post}(s_1) = \\{ s' \\in S \\mid (s_1, s') \\in R \\} = \\{ s_2, s_3 \\}$$\n\n$$\\text{Post}(s_2) = \\{ s' \\in S \\mid (s_2, s') \\in R \\} = \\{ s_3 \\}$$\n\n$$\\text{Post}(s_3) = \\{ s' \\in S \\mid (s_3, s') \\in R \\} = \\{ s_4 \\}$$\n\n$$\\text{Post}(s_4) = \\{ s' \\in S \\mid (s_4, s') \\in R \\} = \\{ s_2 \\}$$\n\n**Derivation of $\\text{Pre}(s_i)$ for every state:**\n\n$$\\text{Pre}(s_0) = \\{ s' \\in S \\mid (s', s_0) \\in R \\} = \\emptyset$$\n\n$$\\text{Pre}(s_1) = \\{ s' \\in S \\mid (s', s_1) \\in R \\} = \\{ s_0 \\}$$\n\n$$\\text{Pre}(s_2) = \\{ s' \\in S \\mid (s', s_2) \\in R \\} = \\{ s_0, s_1, s_4 \\}$$\n\n$$\\text{Pre}(s_3) = \\{ s' \\in S \\mid (s', s_3) \\in R \\} = \\{ s_1, s_2 \\}$$\n\n$$\\text{Pre}(s_4) = \\{ s' \\in S \\mid (s', s_4) \\in R \\} = \\{ s_3 \\}$$\n\n## 3.2 Fixed-Point Reachability — Exhaustive Iteration\n\nWe now compute $\\text{Reach}(\\{s_0\\})$ via $\\mu X . S_0 \\cup \\text{Post}(X)$:\n\n**Iteration 0:**\n\n$$X_0 = S_0 = \\{s_0\\}$$\n\n**Iteration 1:**\n\n$$X_1 = X_0 \\cup \\text{Post}(X_0) = \\{s_0\\} \\cup \\text{Post}(\\{s_0\\})$$\n\n$$= \\{s_0\\} \\cup \\{s_1, s_2\\} = \\{s_0, s_1, s_2\\}$$\n\n**Iteration 2:**\n\n$$X_2 = X_1 \\cup \\text{Post}(X_1) = \\{s_0, s_1, s_2\\} \\cup \\left( \\text{Post}(s_0) \\cup \\text{Post}(s_1) \\cup \\text{Post}(s_2) \\right)$$\n\n$$= \\{s_0, s_1, s_2\\} \\cup \\{s_1, s_2\\} \\cup \\{s_2, s_3\\} \\cup \\{s_3\\}$$\n\n$$= \\{s_0, s_1, s_2\\} \\cup \\{s_1, s_2, s_3\\} = \\{s_0, s_1, s_2, s_3\\}$$\n\n**Iteration 3:**\n\n$$X_3 = X_2 \\cup \\text{Post}(X_2) = \\{s_0, s_1, s_2, s_3\\} \\cup \\left( \\{s_1, s_2\\} \\cup \\{s_2, s_3\\} \\cup \\{s_3\\} \\cup \\{s_4\\} \\right)$$\n\n$$= \\{s_0, s_1, s_2, s_3\\} \\cup \\{s_1, s_2, s_3, s_4\\} = \\{s_0, s_1, s_2, s_3, s_4\\}$$\n\n**Iteration 4 (Fixed-Point Check):**\n\n$$X_4 = X_3 \\cup \\text{Post}(X_3) = S \\cup \\text{Post}(S) = S \\cup S = S$$\n\nSince $X_4 = X_3$, we have reached the **least fixed point**. Therefore:\n\n$$\\text{Reach}(\\{s_0\\}) = \\{ s_0, s_1, s_2, s_3, s_4 \\} = S$$\n\nThis means the entire state space is reachable from $s_0$, which is consistent with the fact that $s_2 \\to s_3 \\to s_4 \\to s_2$ forms a reachable cycle.\n\n## 3.3 Backward Reachability — Computation of $\\text{Pre}^*(\\{s_4\\})$\n\n**Iteration 0:**\n\n$$X_0 = \\{s_4\\}$$\n\n**Iteration 1:**\n\n$$X_1 = X_0 \\cup \\text{Pre}(X_0) = \\{s_4\\} \\cup \\text{Pre}(\\{s_4\\}) = \\{s_4\\} \\cup \\{s_3\\} = \\{s_3, s_4\\}$$\n\n**Iteration 2:**\n\n$$X_2 = X_1 \\cup \\text{Pre}(X_1) = \\{s_3, s_4\\} \\cup \\text{Pre}(\\{s_3, s_4\\})$$\n\n$$= \\{s_3, s_4\\} \\cup \\left( \\{s_1, s_2\\} \\cup \\{s_3\\} \\right) = \\{s_1, s_2, s_3, s_4\\}$$\n\n**Iteration 3:**\n\n$$X_3 = X_2 \\cup \\text{Pre}(X_2) = \\{s_1, s_2, s_3, s_4\\} \\cup \\left( \\{s_0\\} \\cup \\{s_0, s_1, s_4\\} \\cup \\{s_1, s_2\\} \\cup \\{s_3\\} \\right)$$\n\n$$= \\{s_1, s_2, s_3, s_4\\} \\cup \\{s_0, s_1, s_2, s_3, s_4\\} = S$$\n\n**Iteration 4:** $X_4 = S \\cup \\text{Pre}(S) = S$. Fixed point reached.\n\n$$\\text{Pre}^*(\\{s_4\\}) = S = \\{ s_0, s_1, s_2, s_3, s_4 \\}$$\n\nThis means every state in the system can, via some path of length $\\geq 0$, eventually reach $s_4$.\n\n## 3.4 Analytical Verification Table\n\n| State $s$ | $\\text{Post}(s)$ | $\\text{Pre}(s)$ | On a Cycle? |\n| :--- | :--- | :--- | :--- |\n| $s_0$ | $\\{s_1, s_2\\}$ | $\\emptyset$ | No (entry point) |\n| $s_1$ | $\\{s_2, s_3\\}$ | $\\{s_0\\}$ | No |\n| $s_2$ | $\\{s_3\\}$ | $\\{s_0, s_1, s_4\\}$ | Part of cycle |\n| $s_3$ | $\\{s_4\\}$ | $\\{s_1, s_2\\}$ | Part of cycle |\n| $s_4$ | $\\{s_2\\}$ | $\\{s_3\\}$ | Part of cycle |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Mermaid Block-Diagram — Kripke Structure with Pre/Post Annotation\n\n```mermaid\ngraph LR\n    s0[\"STATE s0<br/>Init\"]:::initNode\n    s1[\"STATE s1\"]:::stateNode\n    s2[\"STATE s2<br/>CYCLE START\"]:::cycleNode\n    s3[\"STATE s3\"]:::cycleNode\n    s4[\"STATE s4\"]:::cycleNode\n\n    s0 -- TRANSITION --> s1\n    s0 -- TRANSITION --> s2\n    s1 -- TRANSITION --> s2\n    s1 -- TRANSITION --> s3\n    s2 -- TRANSITION --> s3\n    s3 -- TRANSITION --> s4\n    s4 -- TRANSITION --> s2\n\n    classDef initNode fill:#FFD700,stroke:#B8860B,stroke-width:3px,color:#000\n    classDef stateNode fill:#87CEEB,stroke:#1E3A5F,stroke-width:2px,color:#000\n    classDef cycleNode fill:#FFB6C1,stroke:#8B0000,stroke-width:2px,color:#000\n```\n\n## 4.2 Pre/Post Neighbourhood Expansion Diagram\n\n```mermaid\ngraph TD\n    target[\"TARGET STATE s2\"]:::targetNode\n    preSet[\"DIRECT PREDECESSORS<br/>Pre s2 equals s0, s1, s4\"]:::preNode\n    postSet[\"DIRECT SUCCESSORS<br/>Post s2 equals s3\"]:::postNode\n\n    s0n[\"s0\"]:::preNode\n    s1n[\"s1\"]:::preNode\n    s4n[\"s4\"]:::preNode\n    s3n[\"s3\"]:::postNode\n\n    s0n --> target\n    s1n --> target\n    s4n --> target\n    target --> s3n\n\n    classDef targetNode fill:#FF6347,stroke:#8B0000,stroke-width:4px,color:#FFF\n    classDef preNode fill:#90EE90,stroke:#006400,stroke-width:2px,color:#000\n    classDef postNode fill:#ADD8E6,stroke:#00008B,stroke-width:2px,color:#000\n```\n\n## 4.3 Functional Architecture — Role of Pre/Post in the Verification Pipeline\n\n```mermaid\ngraph LR\n    spec[\"SPECIFICATION<br/>Temporal Logic CTL or LTL\"]:::specNode\n    model[\"KRIPKE MODEL M\"]:::modelNode\n    pre[\"PRE OPERATOR<br/>Backward pre image\"]:::preOp\n    post[\"POST OPERATOR<br/>Forward post image\"]:::postOp\n    fxp[\"FIXED POINT<br/>Least or greatest\"]:::fxpNode\n    verdict[\"VERDICT<br/>Satisfies or violates\"]:::verdictNode\n\n    spec --> fxp\n    model --> pre\n    model --> post\n    pre --> fxp\n    post --> fxp\n    fxp --> verdict\n\n    classDef specNode fill:#DDA0DD,stroke:#4B0082,stroke-width:2px,color:#000\n    classDef modelNode fill:#F0E68C,stroke:#8B8B00,stroke-width:2px,color:#000\n    classDef preOp fill:#98FB98,stroke:#006400,stroke-width:2px,color:#000\n    classDef postOp fill:#87CEFA,stroke:#00008B,stroke-width:2px,color:#000\n    classDef fxpNode fill:#FFA07A,stroke:#FF4500,stroke-width:2px,color:#000\n    classDef verdictNode fill:#D3D3D3,stroke:#000,stroke-width:2px,color:#000\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n## 📘 PART A — Short Answer Questions (3 Marks Each)\n\n### **Question 1: Direct Predecessor Definition** `[KTU University Exam – Dec 2023]`\n**Q: Define the direct predecessor of a state in a Kripke structure. Given the transition relation $R = \\{(s_0,s_1),(s_1,s_2),(s_2,s_0),(s_1,s_0)\\}$, compute the direct predecessors of $s_0$.**\n\n**Model Answer (3 Marks):**\n> A state $s'$ is a **direct predecessor** of state $s$ if there exists a transition $(s', s) \\in R$ in the Kripke structure.\n>\n> For $s_0$, we need all $s'$ such that $(s', s_0) \\in R$:\n> - $(s_2, s_0) \\in R$ ✓\n> - $(s_1, s_0) \\in R$ ✓\n>\n> Therefore: $\\text{Pre}(s_0) = \\{s_1, s_2\\}$. **(1 mark definition, 2 marks computation)**\n\n### **Question 2: Post-Image Property** `[KTU University Exam – May 2024]`\n**Q: State the monotonicity property of the Post operator. Why is this property essential for the termination of the reachability fixed-point computation?**\n\n**Model Answer (3 Marks):**\n> **Monotonicity:** If $A \\subseteq B$, then $\\text{Post}(A) \\subseteq \\text{Post}(B)$. **(1 mark)**\n>\n> **Why essential:** The reachability fixed point is computed via the iteration $X_{i+1} = X_i \\cup \\text{Post}(X_i)$ over the powerset lattice $(2^S, \\subseteq)$. Since $X_i \\subseteq X_{i+1}$ (monotone union) and $2^S$ is finite with cardinality at most $2^{\\vert S \\vert}$, the chain $X_0 \\subseteq X_1 \\subseteq \\ldots$ must stabilise in at most $2^{\\vert S \\vert}$ steps. Without monotonicity, the chain might not be a chain at all, and termination is not guaranteed. **(2 marks)**\n\n---\n\n## 📕 PART B — Long Answer Questions (14 Marks Each, with Internal Choice)\n\n> Choose **ONE** of the following two full questions.\n\n---\n\n### ✅ **Question A (14 Marks)** `[KTU University Exam – Dec 2024]`\n\n**(a)** For the Kripke structure shown below, compute the **direct successors** and **direct predecessors** of every state. Identify all cycles in the structure.\n\n$$S = \\{A, B, C, D, E\\}, \\quad R = \\{ (A,B), (A,C), (B,D), (C,D), (D,E), (E,C) \\}$$\n\n**(7 Marks)**\n\n**(b)** Using the **Pre** and **Post** operators, compute the set of all states reachable from the initial state $A$ via the least fixed-point iteration $\\text{Reach}(A) = \\mu X . \\{A\\} \\cup \\text{Post}(X)$. Show every iteration step explicitly. **(7 Marks)**\n\n---\n\n### 🔷 **Model Solution — Question A**\n\n**Part (a) — Direct Successors and Predecessors (7 Marks):**\n\nDirect successors by reading the head of each transition pair:\n\n$$\\text{Post}(A) = \\{B, C\\} \\quad \\text{(transitions leaving A)}$$\n\n$$\\text{Post}(B) = \\{D\\} \\quad \\text{(transition leaving B)}$$\n\n$$\\text{Post}(C) = \\{D\\} \\quad \\text{(transition leaving C)}$$\n\n$$\\text{Post}(D) = \\{E\\} \\quad \\text{(transition leaving D)}$$\n\n$$\\text{Post}(E) = \\{C\\} \\quad \\text{(transition leaving E)}$$\n\nDirect predecessors by reading the tail of each transition pair:\n\n$$\\text{Pre}(A) = \\emptyset \\quad \\text{(no transition leads to A)}$$\n\n$$\\text{Pre}(B) = \\{A\\}$$\n\n$$\\text{Pre}(C) = \\{A, E\\}$$\n\n$$\\text{Pre}(D) = \\{B, C\\}$$\n\n$$\\text{Pre}(E) = \\{D\\}$$\n\n**Cycle Detection:**\nA cycle is a closed walk where we return to a starting state. Following the edges:\n- $C \\to D \\to E \\to C$ is a cycle of length **3**. **(3 marks — Post, 2 marks — Pre, 2 marks — Cycle identification)**\n\n---\n\n**Part (b) — Reachability Fixed-Point Iteration (7 Marks):**\n\nWe compute $\\text{Reach}(\\{A\\}) = \\mu X . \\{A\\} \\cup \\text{Post}(X)$ step-by-step.\n\n**Iteration 0:** Initial set is the singleton of initial state.\n$$X_0 = \\{A\\}$$\n\n**Iteration 1:** Apply Post and union.\n$$X_1 = X_0 \\cup \\text{Post}(X_0) = \\{A\\} \\cup \\text{Post}(\\{A\\}) = \\{A\\} \\cup \\{B, C\\} = \\{A, B, C\\}$$\n\n**Iteration 2:** Apply Post to the expanded set.\n$$\\text{Post}(X_1) = \\text{Post}(\\{A, B, C\\}) = \\{B, C\\} \\cup \\{D\\} \\cup \\{D\\} = \\{B, C, D\\}$$\n$$X_2 = X_1 \\cup \\text{Post}(X_1) = \\{A, B, C\\} \\cup \\{B, C, D\\} = \\{A, B, C, D\\}$$\n\n**Iteration 3:** Apply Post to the further-expanded set.\n$$\\text{Post}(X_2) = \\{B, C\\} \\cup \\{D\\} \\cup \\{D\\} \\cup \\{E\\} = \\{B, C, D, E\\}$$\n$$X_3 = X_2 \\cup \\text{Post}(X_2) = \\{A, B, C, D\\} \\cup \\{B, C, D, E\\} = \\{A, B, C, D, E\\} = S$$\n\n**Iteration 4 (Fixed-Point Check):**\n$$X_4 = X_3 \\cup \\text{Post}(X_3) = S \\cup \\text{Post}(S) = S \\cup S = S = X_3$$\n\nSince $X_4 = X_3$, the iteration has reached a **fixed point**. Therefore:\n\n$$\\text{Reach}(\\{A\\}) = \\{A, B, C, D, E\\} = S$$\n\nThe fixed point was reached in 3 iterations. **(1 mark per iteration step = 5 marks, 1 mark for fixed-point check, 1 mark for final answer)**\n\n---\n\n### ✅ **Question B (14 Marks)** `[KTU University Exam – May 2024]`\n\n**(a)** Explain the concepts of **direct successor** and **direct predecessor** of a state in a Kripke structure. State the **monotonicity** and **totality** properties of the Post operator with brief justification. **(7 Marks)**\n\n**(b)** Consider a Kripke structure with transition relation:\n$$R = \\{ (p,q), (p,r), (q,r), (r,s), (s,q) \\}$$\nCompute the set $\\text{Pre}^*(\\{s\\})$ of all states that can reach $s$ in zero or more steps. Show all iterations. **(7 Marks)**\n\n---\n\n### 🔷 **Model Solution — Question B**\n\n**Part (a) — Conceptual Explanation (7 Marks):**\n\n> **Direct Successor:** A state $s'$ is a direct successor of $s$ iff $(s, s') \\in R$. The set $\\text{Post}(s)$ denotes the set of all such $s'$. **(1.5 marks)**\n>\n> **Direct Predecessor:** A state $s'$ is a direct predecessor of $s$ iff $(s', s) \\in R$. The set $\\text{Pre}(s)$ denotes the set of all such $s'$. **(1.5 marks)**\n>\n> **Monotonicity of Post:** For all $A, B \\subseteq S$, if $A \\subseteq B$ then $\\text{Post}(A) \\subseteq \\text{Post}(B)$.\n> *Justification:* If $s' \\in \\text{Post}(A)$, then there exists $s \\in A$ with $(s,s') \\in R$. Since $A \\subseteq B$, the same $s \\in B$, so $s' \\in \\text{Post}(B)$. **(2 marks)**\n>\n> **Totality of R:** For every $s \\in S$, $\\text{Post}(s) \\neq \\emptyset$.\n> *Justification:* Kripke structures require $R$ to be total, meaning every state has at least one outgoing transition. A terminal state is modelled as a self-loop $(s,s) \\in R$, not as an absent transition. **(2 marks)**\n\n**Part (b) — Backward Reachability Iteration (7 Marks):**\n\nWe compute $\\text{Pre}^*(\\{s\\}) = \\mu X . \\{s\\} \\cup \\text{Pre}(X)$.\n\nFirst, compute $\\text{Pre}$ for each individual state:\n- $\\text{Pre}(p) = \\emptyset$\n- $\\text{Pre}(q) = \\{p, s\\}$\n- $\\text{Pre}(r) = \\{p, q\\}$\n- $\\text{Pre}(s) = \\{r\\}$\n\n**Iteration 0:**\n$$X_0 = \\{s\\}$$\n\n**Iteration 1:**\n$$X_1 = X_0 \\cup \\text{Pre}(X_0) = \\{s\\} \\cup \\text{Pre}(\\{s\\}) = \\{s\\} \\cup \\{r\\} = \\{r, s\\}$$\n\n**Iteration 2:**\n$$X_2 = X_1 \\cup \\text{Pre}(X_1) = \\{r, s\\} \\cup \\left( \\text{Pre}(r) \\cup \\text{Pre}(s) \\right) = \\{r, s\\} \\cup \\{p, q\\} \\cup \\{r\\} = \\{p, q, r, s\\}$$\n\n**Iteration 3 (Fixed-Point Check):**\n$$X_3 = X_2 \\cup \\text{Pre}(X_2) = \\{p,q,r,s\\} \\cup \\left( \\emptyset \\cup \\{p,s\\} \\cup \\{p,q\\} \\cup \\{r\\} \\right)$$\n$$= \\{p,q,r,s\\} \\cup \\{p,q,r,s\\} = \\{p,q,r,s\\} = X_2$$\n\nFixed point reached.\n\n$$\\boxed{\\text{Pre}^*(\\{s\\}) = \\{p, q, r, s\\}}$$\n\n**(1 mark per iteration step + 1 mark fixed-point check + 1 mark final boxed answer)**\n\n---\n\n> [!WARNING]\n> **🔴 KTU Examiner's Valuation Warning — Common Pitfalls**\n> 1. **Confusing Post and Pre:** Many students swap the direction. Remember: $\\text{Post}(s) = $ \"where can I go from $s$\", while $\\text{Pre}(s) = $ \"where could I have come from to reach $s$\". The transition $(s, s') \\in R$ means $s' \\in \\text{Post}(s)$ **and** $s \\in \\text{Pre}(s')$.\n> 2. **Forgetting Totality:** If a state has no outgoing edge, students often report \"$\\text{Post}(s) = \\emptyset$\". In a proper Kripke structure, this is invalid — there must be a self-loop.\n> 3. **Stopping the fixed-point too early:** The iteration must continue until $X_{k+1} = X_k$ **strictly**. Students often stop when \"all reachable states look added\", missing a final verification.\n> 4. **Notation mix-up:** Writing $\\text{Pre}^*$ for forward or $\\text{Post}^*$ for backward loses 0.5–1 mark per occurrence in KTU valuation.\n\n---\n\n## 🎯 Topic Recap & Important Things to Remember\n\n- **Direct Successor** of $s$ = states reachable in **exactly one** transition. **(1 mark concept)**\n- **Direct Predecessor** of $s$ = states that can transition to $s$ in **exactly one** step. **(1 mark concept)**\n- $\\text{Post}(s) = \\{s' \\mid (s,s') \\in R\\}$ and $\\text{Pre}(s) = \\{s' \\mid (s',s) \\in R\\}$ — **direction matters**. **(high weightage)**\n- For sets: $\\text{Post}(S') = \\bigcup_{s \\in S'} \\text{Post}(s)$, and similarly for $\\text{Pre}$. **(very high weightage)**\n- The Post operator is **monotone** in $(2^S, \\subseteq)$ — this guarantees **fixed-point termination**. **(critical for 14-mark answers)**\n- The transition relation $R$ in a Kripke structure is **total** — every state has $\\text{Post}(s) \\neq \\emptyset$. **(favourite KTU trick question)**\n- **Forward Reachability:** $\\text{Reach}(S_0) = \\mu X . S_0 \\cup \\text{Post}(X)$ — least fixed point, iterates by $\\cup$, terminates in at most $\\vert S \\vert$ additions. **(14-mark staple)**\n- **Backward Reachability:** $\\text{Pre}^*(T) = \\mu X . T \\cup \\text{Pre}(X)$ — used in CEGAR and counterexample analysis. **(advanced application)**\n- **Cycle detection** via Post: a state $s$ is on a cycle iff $s \\in \\text{Post}^*(s) \\setminus \\{s\\}$, i.e., reachable from itself in $\\geq 1$ step. **(frequently tested)**\n- **Computation cost** is $O(\\vert R \\vert)$ for one Post/Pre application over the entire transition relation. **(engineering relevance)**\n- Pre and Post are **dual operators** — switching the direction of $R$ converts one to the other: $\\text{Pre}_R(S) = \\text{Post}_{R^{-1}}(S)$. **(deep insight)**\n- For symbolic model checking, $\\text{Post}(S(\\bar{x})) \\equiv \\exists \\bar{x} . S(\\bar{x}) \\wedge R(\\bar{x}, \\bar{x}')$ followed by substitution $\\bar{x}' \\to \\bar{x}$. **(advanced — BDDs)**\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t6",
          "title": "Terminal State",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Terminal State\n\n<!-- SECTION_1_START -->\n## SECTION 1: Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition\n\nIn the formal semantics of **automated verification**, a **Terminal State** (also known as a **sink state**, **final state**, or **halting state**) of a transition system is a state $s \\in S$ from which **no outgoing transition is defined** under the system's transition relation $\\rightarrow$.\n\nFormally, for a Kripke Structure $M = (S, S_0, R, L)$ over atomic propositions $AP$, a state $s \\in S$ is a **terminal state** if and only if:\n\n$$\\forall s' \\in S : \\neg(s \\rightarrow s')$$\n\nEquivalently, the set of terminal states $T \\subseteq S$ is defined as:\n\n$$T = \\{ s \\in S \\mid \\nexists s' \\in S \\text{ such that } (s, s') \\in R \\}$$\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight:** A terminal state is a fundamental concept in **Transition Systems** and **Kripke Structures**. The absence of outgoing transitions makes it the natural point of evaluation for **safety** and **liveness** properties in LTL/CTL model checking.\n\n> [!NOTE]\n> **Alternative Naming Conventions used in KTU-approved textbooks:**\n> - **Sink State:** The system \"sinks\" here; no observable behavior emerges.\n> - **Deadlock State:** In concurrent systems, all processes halt, reaching a global terminal configuration.\n> - **Final/Accepting State:** Used in automata theory where terminal states are accepting iff the property under verification is satisfied.\n\n---\n\n### 1.2 Conceptual Analogy — \"The Dead-End Alley\"\n\nImagine a car navigating a city map. The map is a graph where intersections are **states** and roads are **transitions**.\n\n- A **regular state** is a four-way intersection — you can go North, South, East, or West.\n- A **terminal state** is a **cul-de-sac (dead-end alley)** — the road enters, but there is no exit road.\n\nIn automated verification, when the model checker explores the state space, reaching a terminal state means the execution **stops here**. This is precisely where the verifier checks:\n\n1. *Did the system reach the desired property?* (Acceptance)\n2. *Did the system violate the required property?* (Error)\n3. *Is the system deadlocked?* (Bug)\n\nA state with no outgoing transitions is, intuitively, the \"**end of the story**\" of that particular execution path.\n\n---\n\n### 1.3 Physical / Structural Metrics\n\n- **Cardinality of Terminal Set:** $\\vert T \\vert$ — the total number of terminal states in the system.\n- **Reachability Distance:** $d(s_0, t)$ — the minimum number of transitions from initial state $s_0$ to a terminal state $t$.\n- **Deadlock Probability (probabilistic systems):** $P_{\\text{deadlock}} = \\sum_{t \\in T} \\pi(t)$, where $\\pi$ is the stationary distribution.\n\n> [!TIP]\n> In **hardware verification** (a common KTU PECNT526 application area), the **reset state** of a CPU is a canonical terminal state of the **power-off** model. When the verifier hits it, the trace terminates successfully if the design behaved correctly until that point.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Operational Breakdown — \"How\" and \"Why\" of Terminal States\n\nA transition system $TS = (S, \\rightarrow, I, AP, L)$ evolves through a sequence of states. The **terminal state** is the **endpoint** of a finite path. Let us dissect its role:\n\n- **Step 1 — Definition of the Transition Relation:**\n  The system evolves via $\\rightarrow \\subseteq S \\times S$. Every state $s$ may have zero, one, or many successors. A terminal state is the degenerate case where the cardinality of the successor set is **exactly zero**.\n\n- **Step 2 — Why does a terminal state matter in verification?**\n  Model checking explores the **Cartesian product** of the system model and the property automaton. If the property automaton reaches a terminal (accepting) state, the system satisfies the property. If the system model reaches a terminal state, the run ends.\n\n- **Step 3 — Detection in Algorithms:**\n  In **DFS-based reachability analysis (e.g., Tarjan's SCC algorithm)**, a state is identified as terminal if its DFS recursion finds no unvisited successors. In **BDD-based symbolic model checking**, terminal states are characterized by the fixed-point formula:\n  $$T = S \\setminus \\text{pre}_\\exists(S)$$\n  where $\\text{pre}_\\exists$ is the existential predecessor operator.\n\n- **Step 4 — Role in LTL/CTL Verification:**\n  In **Linear Temporal Logic (LTL)**, terminal states trivially satisfy any formula of the form $\\mathbf{G}\\,\\phi$ (Globally $\\phi$) provided $\\phi$ holds at the terminal state. This is because the path is finite, and the universal quantification over all future positions is satisfied vacuously after the last state.\n\n- **Step 5 — Role in Safety Verification:**\n  For safety properties $\\mathbf{G}\\,\\neg \\phi$, a terminal state $t$ is a **counterexample** if $\\phi$ holds at $t$. The verification tool reports the path $s_0 \\rightarrow s_1 \\rightarrow \\dots \\rightarrow t$ as a **failure trace**.\n\n---\n\n### 2.2 KTU Formula Sheet / Cheat Sheet\n\n| # | Concept | Mathematical Expression | Description |\n|---|---------|--------------------------|-------------|\n| 1 | Terminal State Set | $T = \\{ s \\in S \\mid \\forall s' \\in S : (s, s') \\notin R \\}$ | States with no outgoing transitions |\n| 2 | Path Termination | $\\pi = s_0 \\rightarrow s_1 \\rightarrow \\dots \\rightarrow s_n$ with $s_n \\in T$ | A finite path ends at a terminal state |\n| 3 | Reachability (exists) | $\\mathbf{EF}\\,T \\equiv \\mu Z. \\, T \\cup \\text{pre}_\\exists(Z)$ | Least fixed-point: some path reaches a terminal state |\n| 4 | Reachability (all) | $\\mathbf{AF}\\,T \\equiv \\mu Z. \\, T \\cup (\\text{pre}_\\forall(Z) \\cap \\text{pre}_\\exists(Z))$ | All paths eventually reach a terminal state |\n| 5 | Deadlock Freedom | $S \\subseteq \\text{pre}_\\exists(S)$ | Every state has at least one successor (no terminal states) |\n| 6 | Terminal in LTL | $s_n \\models \\mathbf{G}\\,\\phi \\iff s_n \\models \\phi$ | At a terminal state, $\\mathbf{G}$ reduces to a single state check |\n| 7 | Graph-Theoretic Form | $\\deg^+(s) = 0$ | Out-degree of node $s$ in the transition graph is zero |\n| 8 | Symbolic BDD Form | $T(s) \\equiv \\neg \\exists s' : R(s, s')$ | Terminal states characterized by the absence of $R$ |\n\n> [!NOTE]\n> **CRITICAL:** In KTU exams, examiners specifically look for the application of the **fixed-point characterization** of reachability (rows 3 and 4) in Part B answers. Memorize $\\mu$ vs. $\\nu$ carefully: $\\mu$ is the **least** (used for $\\mathbf{EF}$/$\\mathbf{AF}$), $\\nu$ is the **greatest** (used for $\\mathbf{EG}$/$\\mathbf{AG}$).\n\n---\n\n### 2.3 Real-World Engineering Utility\n\n| Engineering Domain | Application of Terminal State Analysis |\n|---------------------|----------------------------------------|\n| **Hardware Design (RTL Verification)** | Detecting CPU pipeline deadlocks; verifying that reset sequences terminate correctly. |\n| **Protocol Verification (e.g., TLS, TCP)** | Ensuring that authentication handshakes do not enter unrecoverable terminal states (denial-of-service). |\n| **Software Model Checking (Java PathFinder)** | Identifying thread deadlocks in concurrent Java programs. |\n| **Embedded Control Systems** | Verifying that safety-critical controllers (e.g., airbag ECU) always have a defined next action — i.e., are **deadlock-free**. |\n| **Smart Contracts (Blockchain)** | Ensuring that `Solidity` smart contracts never reach a state where `tx.origin` cannot execute further. |\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Derivation 1: Characterization of Terminal States via Predecessor Operator\n\n**Goal:** Derive an explicit fixed-point expression for the set of terminal states $T$ in a Kripke Structure.\n\n**Step 1 — Start with the definition.**\n\nBy definition, a state $s$ is terminal if it has no successor:\n\n$$T = \\{ s \\in S \\mid \\forall s' \\in S : \\neg R(s, s') \\}$$\n\n**Step 2 — Rewrite using the existential quantifier.**\n\nThe condition \"no successor exists\" is equivalent to \"the existential quantification over successors is false\":\n\n$$T = \\{ s \\in S \\mid \\neg(\\exists s' \\in S : R(s, s')) \\}$$\n\n**Step 3 — Substitute the formal predecessor operator.**\n\nRecall that the **existential predecessor** of a set $X \\subseteq S$ is defined as:\n\n$$\\text{pre}_\\exists(X) = \\{ s \\in S \\mid \\exists s' \\in X : R(s, s') \\}$$\n\nTaking $X = S$ (the entire state space):\n\n$$\\text{pre}_\\exists(S) = \\{ s \\in S \\mid \\exists s' \\in S : R(s, s') \\}$$\n\nThis is precisely the set of states that have **at least one** successor.\n\n**Step 4 — Complement to obtain $T$.**\n\n$$T = S \\setminus \\text{pre}_\\exists(S)$$\n\n**Step 5 — Verification by cardinality.**\n\nFor a system with $\\vert S \\vert$ states and $\\vert T \\vert$ terminal states:\n\n$$\\vert S \\vert = \\vert T \\vert + \\vert \\text{pre}_\\exists(S) \\vert$$\n\nThis is consistent with the partition of $S$ into states-with-successors and states-without-successors. $\\blacksquare$\n\n---\n\n### 3.2 Derivation 2: Fixed-Point Computation of $\\mathbf{EF}\\,T$ (Reachability of Terminal States)\n\n**Goal:** Show that \"there exists a path that reaches a terminal state\" can be computed by a least fixed-point iteration.\n\n**Step 1 — Write the CTL formula.**\n\nWe want to compute the set of states satisfying $\\mathbf{EF}\\,T$ — i.e., states from which a terminal state is reachable.\n\n**Step 2 — Apply the standard CTL fixed-point semantics.**\n\nAccording to CTL semantics, $\\mathbf{EF}\\,\\phi$ is defined as:\n\n$$\\llbracket \\mathbf{EF}\\,\\phi \\rrbracket = \\mu Z. \\, \\phi \\cup \\text{pre}_\\exists(Z)$$\n\nIn our case, $\\phi = T$:\n\n$$\\llbracket \\mathbf{EF}\\,T \\rrbracket = \\mu Z. \\, T \\cup \\text{pre}_\\exists(Z)$$\n\n**Step 3 — Iterative evaluation.**\n\nWe compute this by Kleene iteration starting from $Z_0 = \\emptyset$:\n\n$$\n\\begin{aligned}\nZ_0 &= \\emptyset \\\\\nZ_1 &= T \\cup \\text{pre}_\\exists(Z_0) = T \\cup \\emptyset = T \\\\\nZ_2 &= T \\cup \\text{pre}_\\exists(Z_1) \\\\\n    &= T \\cup \\{ s \\mid \\exists s' \\in T : R(s, s') \\} \\\\\nZ_3 &= T \\cup \\text{pre}_\\exists(Z_2) \\\\\n    &= T \\cup \\{ s \\mid \\exists s' \\in Z_2 : R(s, s') \\} \\\\\n\\vdots \\\\\nZ_k &= Z_{k-1} \\quad \\text{(fixed point reached)}\n\\end{aligned}\n$$\n\n**Step 4 — Convergence and termination.**\n\nSince $S$ is finite, the sequence $Z_0 \\subseteq Z_1 \\subseteq Z_2 \\subseteq \\dots \\subseteq S$ is monotonically increasing and bounded above by $S$. Therefore, it converges in at most $\\vert S \\vert$ iterations.\n\n**Step 5 — Final result.**\n\n$$Z_{\\text{final}} = \\llbracket \\mathbf{EF}\\,T \\rrbracket = \\bigcup_{i=0}^{\\vert S \\vert} Z_i$$\n\nThis is the set of states that can reach a terminal state in some finite number of steps. $\\blacksquare$\n\n---\n\n### 3.3 Derivation 3: Terminal State and Safety Property Satisfaction\n\n**Goal:** Show that a finite path ending at a terminal state satisfies $\\mathbf{G}\\,\\phi$ iff the terminal state itself satisfies $\\phi$.\n\n**Step 1 — Define the path.**\n\nConsider a finite path $\\pi = s_0, s_1, \\dots, s_n$ with $s_n \\in T$.\n\n**Step 2 — Apply the semantics of $\\mathbf{G}$ (Globally).**\n\n$$\\pi \\models \\mathbf{G}\\,\\phi \\iff \\forall 0 \\le i \\le n : s_i \\models \\phi$$\n\n**Step 3 — Exploit the finite nature of the path.**\n\nSince $\\pi$ is finite and $s_n$ has no successor, there is no $s_{n+1}, s_{n+2}, \\dots$ to check. The universal quantification naturally terminates at $i = n$.\n\n**Step 4 — Special reduction at the terminal state.**\n\nFor $i = n$ (the terminal state), the condition reduces to a single-state check:\n\n$$s_n \\models \\mathbf{G}\\,\\phi \\iff s_n \\models \\phi$$\n\nThis is because the \"future\" is empty. The semantics of $\\mathbf{G}$ at the last state of a finite path is **vacuously satisfied** beyond $s_n$, so only $s_n$ itself matters.\n\n**Step 5 — Counterexample detection.**\n\nIf the property to verify is $\\mathbf{G}\\,\\neg \\psi$ (safety — \"$\\psi$ never happens\"), and $s_n \\models \\psi$ for some terminal $s_n$, then the path $\\pi$ is a **counterexample trace**. The model checker reports it as a verification failure. $\\blacksquare$\n\n---\n\n### 3.4 Analytical Matrix — Comparison with Related Concepts\n\n| Property | Terminal State ($T$) | Accepting State ($F$) | Deadlock State ($D$) | Initial State ($I$) |\n|----------|----------------------|------------------------|----------------------|---------------------|\n| **Definition** | No outgoing transition | Property is satisfied here | System halts, no progress | System starts here |\n| **Out-degree** | $\\deg^+ = 0$ | $\\deg^+ \\ge 0$ (can vary) | $\\deg^+ = 0$ | $\\deg^+ \\ge 1$ typically |\n| **Subset of $S$** | Yes | Yes | Yes ($D \\subseteq T$) | Yes |\n| **Role in LTL** | Vacuous satisfaction of $\\mathbf{G}$ | Acceptance of $\\omega$-regular property | Bug indicator | Entry point of model |\n| **Detection** | $S \\setminus \\text{pre}_\\exists(S)$ | Labeled with $F$ in automaton | Predefined pattern | Given as input $I$ |\n| **Real-world example** | System powered off | Liveness goal achieved | Two threads waiting on each other | Boot/reset vector of CPU |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## SECTION 4: Structural Diagrams & Schematics\n\n### 4.1 Mermaid Diagram — Transition System with Terminal States\n\nThe following Mermaid block renders a finite state machine illustrating regular states, transitions, and **two terminal states** ($s_4$ and $s_6$) with no outgoing edges. The double-circle convention is mimicked using distinctive node IDs.\n\n```mermaid\ngraph TD\n    s0[\"S0 Initial State\"]\n    s1[\"S1 Active State\"]\n    s2[\"S2 Processing\"]\n    s3[\"S3 Waiting\"]\n    s4[\"S4 TERMINAL Accepted\"]\n    s5[\"S5 Error Condition\"]\n    s6[\"S6 TERMINAL Deadlock\"]\n    \n    s0 -->|start| s1\n    s1 -->|process| s2\n    s1 -->|idle| s3\n    s2 -->|complete| s4\n    s2 -->|fail| s5\n    s3 -->|timeout| s5\n    s5 -->|abort| s6\n    \n    s4:::terminalNode\n    s6:::terminalNode\n    s0:::startNode\n    s1:::regNode\n    s2:::regNode\n    s3:::regNode\n    s5:::regNode\n    \n    classDef terminalNode fill:#ff6b6b,stroke:#c0392b,stroke-width:3px,color:#fff\n    classDef startNode fill:#3498db,stroke:#2874a6,stroke-width:2px,color:#fff\n    classDef regNode fill:#ecf0f1,stroke:#34495e,stroke-width:1px,color:#000\n```\n\n**Interpretation:**\n- $s_4$ and $s_6$ are **terminal states** — no outgoing arrows.\n- $s_4$ is a \"**good**\" terminal state (property accepted).\n- $s_6$ is a \"**bad**\" terminal state (deadlock — verification failure).\n\n---\n\n### 4.2 Mermaid Diagram — Block-Level Functional Architecture of Terminal State Detection in Model Checking\n\n```mermaid\ngraph LR\n    A[\"Kripke Structure M\"] --> B[\"State Space Builder\"]\n    B --> C[\"Transition Relation R\"]\n    C --> D[\"Predecessor Operator pre_exists\"]\n    D --> E[\"Complement S minus pre_exists S\"]\n    E --> F[\"Terminal Set T Identified\"]\n    F --> G[\"Property Automaton A_phi\"]\n    G --> H[\"Product Construction M times A_phi\"]\n    H --> I[\"Fixed Point Solver mu and nu\"]\n    I --> J{\"Terminal Reached?\"}\n    J -->|Yes Accepting| K[\"VERIFIED Property Holds\"]\n    J -->|Yes Rejecting| L[\"FAILED Counterexample Returned\"]\n    J -->|No| M[\"Continue Exploration\"]\n    M --> I\n    \n    A:::inputNode\n    K:::successNode\n    L:::failNode\n    F:::keyNode\n    \n    classDef inputNode fill:#3498db,color:#fff\n    classDef successNode fill:#27ae60,color:#fff\n    classDef failNode fill:#e74c3c,color:#fff\n    classDef keyNode fill:#f39c12,color:#000,stroke:#000,stroke-width:2px\n```\n\n**Block-by-Block Explanation:**\n\n| Block | Function |\n|-------|----------|\n| **A** | Input: Kripke Structure encoding the system under verification. |\n| **B** | Constructs the explicit or symbolic state space. |\n| **C** | Extracts the transition relation $R \\subseteq S \\times S$. |\n| **D** | Computes $\\text{pre}_\\exists(S)$ — states with at least one successor. |\n| **E** | Computes $T = S \\setminus \\text{pre}_\\exists(S)$ — the terminal set. |\n| **F** | **Key Node:** Terminal set $T$ is isolated. |\n| **G** | Builds the property automaton (Büchi, Rabin, etc.). |\n| **H** | Constructs the synchronous product $M \\otimes A_\\phi$. |\n| **I** | Runs fixed-point algorithms ($\\mu$-calculus engine). |\n| **J** | Decision: did the exploration reach a terminal state? |\n| **K/L** | Verification verdict — success or counterexample. |\n| **M** | Loops back to continue exploration if no terminal hit yet. |\n\n> [!TIP]\n> **KTU Exam Tip:** When asked to \"explain the role of terminal states in model checking,\" reference this block diagram and explicitly highlight the **Complement Step (E)**. This is the most commonly tested concept in Module 1.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A Questions (3 Marks Each)\n\n---\n\n**Q1. [KTU University Exam — May 2024]**\n*Define a **terminal state** in a Kripke Structure. How is it characterized using the predecessor operator?*\n\n**Model Answer (3 marks):**\n\nA **terminal state** of a Kripke Structure $M = (S, S_0, R, L)$ is a state $s \\in S$ that has **no outgoing transition** under the relation $R$.\n\n**Characterization (2 marks):**\n\n$$T = S \\setminus \\text{pre}_\\exists(S) = \\{ s \\in S \\mid \\neg \\exists s' \\in S : R(s, s') \\}$$\n\n**Significance (1 mark):** Terminal states are the points where finite execution paths terminate, and they are critical for evaluating safety properties and detecting deadlocks in the system.\n\n---\n\n**Q2. [KTU University Exam — Dec 2023]**\n*With a suitable example, explain why a terminal state trivially satisfies the LTL formula $\\mathbf{G}\\,\\phi$ if and only if $\\phi$ holds at that state.*\n\n**Model Answer (3 marks):**\n\n**Example (1 mark):** Consider a path $\\pi = s_0 \\rightarrow s_1 \\rightarrow s_2$, where $s_2$ is terminal (no outgoing transition).\n\n**LTL Evaluation (1 mark):** The formula $\\mathbf{G}\\,\\phi$ requires $\\phi$ to hold at every position $i \\ge 0$. Since the path is finite and ends at $s_2$, there are no positions beyond $i = 2$ to check.\n\n**Reduction (1 mark):** Therefore, $s_2 \\models \\mathbf{G}\\,\\phi \\iff s_2 \\models \\phi$. The \"future\" beyond the terminal state is empty, and the universal quantification is satisfied vacuously beyond $s_2$.\n\n---\n\n### 5.2 Part B Questions (14 Marks Each — Internal Choice)\n\n---\n\n### **Question A (14 Marks)**\n\n**[KTU University Exam — Model Paper, PECNT526, Module 1]**\n\n**(a)** With a neat diagram, explain the concept of a terminal state in a transition system. Derive the expression for the set of terminal states $T$ using the existential predecessor operator. **(7 marks)**\n\n**(b)** Consider the Kripke Structure $M$ with states $S = \\{s_0, s_1, s_2, s_3, s_4\\}$ and transition relation:\n$$R = \\{(s_0, s_1),\\ (s_1, s_2),\\ (s_1, s_3),\\ (s_3, s_4),\\ (s_2, s_4)\\}$$\nCompute the set of terminal states $T$ and determine all states from which a terminal state is reachable (i.e., compute $\\llbracket \\mathbf{EF}\\,T \\rrbracket$) using the least fixed-point iteration. **(7 marks)**\n\n---\n\n### **Model Solution for Question A:**\n\n**Part (a) — 7 marks**\n\n**Definition (2 marks):** A terminal state $t \\in S$ is a state with no outgoing transition, i.e., $\\forall s' \\in S : \\neg(t, s') \\in R$.\n\n**Diagram (2 marks):** [Refer to Section 4.1 Mermaid diagram — a transition system with two terminal states $s_4$ and $s_6$]\n\n**Derivation (3 marks):** As shown in **Derivation 1** of Section 3:\n\n$$\n\\begin{aligned}\nT &= \\{ s \\in S \\mid \\forall s' \\in S : \\neg R(s, s') \\} \\\\\n  &= \\{ s \\in S \\mid \\neg(\\exists s' \\in S : R(s, s')) \\} \\\\\n  &= S \\setminus \\text{pre}_\\exists(S)\n\\end{aligned}\n$$\n\nwhere $\\text{pre}_\\exists(S) = \\{ s \\in S \\mid \\exists s' \\in S : R(s, s') \\}$.\n\n---\n\n**Part (b) — 7 marks**\n\n**Step 1 (1 mark):** Identify the set $S = \\{s_0, s_1, s_2, s_3, s_4\\}$.\n\n**Step 2 (2 marks):** Find states with at least one successor:\n\n- $s_0$: has successor $s_1$ → $s_0 \\in \\text{pre}_\\exists(S)$\n- $s_1$: has successors $s_2, s_3$ → $s_1 \\in \\text{pre}_\\exists(S)$\n- $s_2$: has successor $s_4$ → $s_2 \\in \\text{pre}_\\exists(S)$\n- $s_3$: has successor $s_4$ → $s_3 \\in \\text{pre}_\\exists(S)$\n- $s_4$: has **no** successor → $s_4 \\notin \\text{pre}_\\exists(S)$\n\n**Step 3 (1 mark):** Compute $T = S \\setminus \\text{pre}_\\exists(S) = \\{s_4\\}$.\n\n**Step 4 (3 marks):** Fixed-point iteration for $\\llbracket \\mathbf{EF}\\,T \\rrbracket$:\n\n$$\n\\begin{aligned}\nZ_0 &= \\emptyset \\\\\nZ_1 &= T \\cup \\text{pre}_\\exists(Z_0) = \\{s_4\\} \\cup \\emptyset = \\{s_4\\} \\\\\nZ_2 &= \\{s_4\\} \\cup \\text{pre}_\\exists(\\{s_4\\}) = \\{s_4\\} \\cup \\{s_2, s_3\\} = \\{s_2, s_3, s_4\\} \\\\\nZ_3 &= \\{s_2, s_3, s_4\\} \\cup \\text{pre}_\\exists(\\{s_2, s_3, s_4\\}) = \\{s_2, s_3, s_4\\} \\cup \\{s_1\\} = \\{s_1, s_2, s_3, s_4\\} \\\\\nZ_4 &= \\{s_1, s_2, s_3, s_4\\} \\cup \\text{pre}_\\exists(\\{s_1, s_2, s_3, s_4\\}) = \\{s_0, s_1, s_2, s_3, s_4\\} \\\\\nZ_5 &= Z_4 \\quad \\text{(fixed point reached)}\n\\end{aligned}\n$$\n\n**Final Answer (0 marks — already shown):**\n\n$$\\llbracket \\mathbf{EF}\\,T \\rrbracket = \\{s_0, s_1, s_2, s_3, s_4\\} = S$$\n\nThis means **every state** in the system can reach the terminal state $s_4$.\n\n---\n\n### **Question B (14 Marks) — Alternative Choice**\n\n**[KTU University Exam — Model Paper, PECNT526, Module 1]**\n\n**(a)** Differentiate between a **terminal state** and a **deadlock state** in a transition system. Is every deadlock state a terminal state? Justify with a counterexample if not. **(7 marks)**\n\n**(b)** For the property $\\mathbf{G}(p \\rightarrow \\mathbf{F}\\,q)$ interpreted over a finite path $\\pi = s_0 \\rightarrow s_1 \\rightarrow s_2$ with $s_2$ being terminal, show under what conditions $\\pi$ satisfies this property. Illustrate with the LTL semantics at the terminal state. **(7 marks)**\n\n---\n\n### **Model Solution for Question B:**\n\n**Part (a) — 7 marks**\n\n| Aspect | Terminal State | Deadlock State |\n|--------|----------------|----------------|\n| **Definition** | State with no outgoing transition | State where the system cannot make progress (may be process-local) |\n| **Cardinality** | $\\deg^+(s) = 0$ in the global transition system | May have outgoing transitions, but they are not enabled in practice |\n| **Scope** | Global state | Can be a sub-state within a process |\n| **Detection** | Direct from $R$ | Requires process-aware analysis |\n\n**Counterexample (3 marks):** Consider two processes $P_1, P_2$ in a transition system. A deadlock state $d$ is a global state where $P_1$ is waiting for a lock held by $P_2$, and $P_2$ is waiting for a lock held by $P_1$. In the **global** transition system, $d$ is indeed terminal (no global transition fires). However, in the **composed local** view, each process has local transitions blocked, but $d$ is still a terminal state in the global system.\n\n**Conversely (2 marks):** A terminal state need not always be a deadlock. For example, in a system where the user explicitly issues a \"shutdown\" command, the resulting state is terminal (no transition) but is **not** a deadlock — it is an intentional halt. Hence, $D \\subseteq T$ but $T \\not\\subseteq D$.\n\n---\n\n**Part (b) — 7 marks**\n\n**Step 1 (2 marks):** Parse the formula. $\\mathbf{G}(p \\rightarrow \\mathbf{F}\\,q)$ means: \"**globally**, whenever $p$ holds, **eventually in the future** $q$ holds.\"\n\n**Step 2 (2 marks):** Apply semantics over the finite path $\\pi = s_0 \\rightarrow s_1 \\rightarrow s_2$ with $s_2$ terminal.\n\n**Step 3 (3 marks):** Case analysis:\n\n- **Case 1: $\\neg p$ holds at all $s_0, s_1, s_2$.** The implication $p \\rightarrow \\mathbf{F}\\,q$ is vacuously true at every position. Property **satisfied**.\n- **Case 2: $p$ holds at $s_0$ but $\\neg q$ at $s_0, s_1, s_2$.** The formula requires $\\mathbf{F}\\,q$ to hold at $s_0$, meaning $q$ must hold at some $s_i$ with $i \\ge 0$. Since $\\neg q$ everywhere, property **violated**.\n- **Case 3: $p$ holds at $s_0$ and $q$ holds at $s_0$.** The implication is trivially satisfied at $s_0$. Property **satisfied**.\n- **Case 4: $p$ holds at $s_0$, $q$ holds at $s_2$ (terminal).** Then $\\mathbf{F}\\,q$ is satisfied at $s_0$ (via $s_2$). Property **satisfied**. Note that $s_2$ being terminal does not break $\\mathbf{F}$ — the reachability of $q$ is established within the finite path.\n\n**Final Summary:** A terminal state does not violate $\\mathbf{F}$ formulas if the proposition $q$ is reached at or before the terminal state. It only matters for $\\mathbf{G}$ formulas where vacuous satisfaction applies.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> 1. **Confusing $\\mathbf{EF}$ and $\\mathbf{AF}$:** $\\mathbf{EF}\\,T$ uses $\\mu$ (least fixed-point, monotonic increasing iteration). $\\mathbf{AF}\\,T$ uses a more complex formula involving $\\text{pre}_\\forall$. Mixing these up costs **2–3 marks**.\n> 2. **Forgetting that terminal states reduce $\\mathbf{G}$ to a single check:** Students often write the full iteration for $\\mathbf{G}\\,\\phi$ at terminal states, wasting time. **Memorize:** $s_n \\models \\mathbf{G}\\,\\phi \\iff s_n \\models \\phi$ at the terminal.\n> 3. **Miscounting fixed-point iterations:** The iteration starts at $Z_0 = \\emptyset$, not $Z_0 = T$. This is a **1-mark** error that propagates.\n> 4. **Not drawing the diagram in Part (a):** Examiners allocate **2 marks** for the diagram. Skipping it is a guaranteed loss.\n> 5. **Mixing deadlock and terminal state definitions:** Be precise — write $\\deg^+(s) = 0$ for terminal and explain deadlock as a **semantic** halting condition.\n\n---\n\n### 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Revision Checklist — Terminal State**\n\n- **Definition:** A state $s \\in S$ is terminal if $\\nexists s' \\in S$ such that $(s, s') \\in R$. Out-degree = 0.\n- **Symbolic Form:** $T = S \\setminus \\text{pre}_\\exists(S)$.\n- **Reachability:** $\\llbracket \\mathbf{EF}\\,T \\rrbracket = \\mu Z.\\, T \\cup \\text{pre}_\\exists(Z)$ — least fixed-point.\n- **LTL at Terminal:** $\\mathbf{G}\\,\\phi$ reduces to single-state check; $\\mathbf{F}\\,\\phi$ is satisfied if $\\phi$ holds at or before terminal.\n- **Deadlock vs Terminal:** $D \\subseteq T$ (every deadlock is terminal in the global system), but $T \\not\\subseteq D$ (intentional halts are terminal but not deadlocks).\n- **Safety Verification:** A terminal state violating $\\phi$ generates a **counterexample trace**.\n- **Convergence:** The fixed-point iteration converges in at most $\\vert S \\vert$ steps for finite state spaces.\n- **Engineering Relevance:** Hardware deadlock detection, protocol liveness, embedded controller safety, smart contract verification.\n- **Exam Frequency:** Terminal states appear in **at least one Part A and one Part B** in nearly every KTU ESE for PECNT526.\n- **Common Trap:** For finite paths, $\\mathbf{F}\\,\\phi$ and $\\mathbf{G}\\,\\phi$ are evaluated only over the path, not extended into an infinite fictitious future.\n- **Memorize the Set Operators:** $S = T \\cup \\text{pre}_\\exists(S)$ — a clean partition of the state space.\n- **Cross-Reference:** Terminal states are the foundation for **liveness properties** (something good eventually happens — and \"eventually\" often means \"at the terminal state\").\n- **Quick Sanity Check:** A system with **no terminal states** is **deadlock-free**; a system with **all terminal states** is **trivially halting** at every step.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t7",
          "title": "Deterministic Transition System",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Deterministic Transition System\n\n<!-- SECTION_1_START -->\n## SECTION 1: Core Technical Definition & Intuitive Overview\n\n### Formal Academic Definition\n\nA **Deterministic Transition System (DTS)** is a fundamental mathematical formalism in the domain of **automated verification** and **model checking**. It serves as the semantic backbone against which temporal logic specifications are verified. The KTU 2024 Scheme defines it as a 4-tuple structure:\n\n$$DTS = (S, \\ S_0, \\ \\Sigma, \\ \\rightarrow)$$\n\nWhere the components are rigorously defined as:\n\n- **$S$** : A finite, non-empty set of **states** representing all possible configurations of the system under analysis.\n- **$S_0 \\subseteq S$** : A non-empty set of **initial states** from which system execution begins.\n- **$\\Sigma$** (also denoted as $Act$ or $AP$ for atomic propositions) : A finite set of **action labels** or **atomic propositions** that label transitions or states.\n- **$\\rightarrow \\subseteq S \\times \\Sigma \\times S$** : The **transition relation** (sometimes written as $\\xrightarrow{\\alpha}$) that defines valid state-to-state movements.\n\n> [!IMPORTANT]\n> **The Determinism Constraint (The Core Distinguishing Feature)**\n>\n> A transition system is *deterministic* if and only if for every state $s \\in S$ and every action $\\alpha \\in \\Sigma$, there exists **at most one** state $s' \\in S$ such that $(s, \\alpha, s') \\in \\rightarrow$.\n>\n> Formally: $\\forall s \\in S, \\ \\forall \\alpha \\in \\Sigma : \\mid \\{s' \\in S \\mid s \\xrightarrow{\\alpha} s'\\} \\mid \\leq 1$\n\n### Conceptual Analogy / Intuition\n\n> [!NOTE]\n> **Intuitive Analogy: A Vending Machine**\n>\n> Imagine a **vending machine** with the following states: `Idle → CoinInserted → SelectionMade → Dispensing → Idle`.\n>\n> - **Deterministic Machine**: When you press \"B5\" (Coke button) after inserting a ₹20 note in the \"CoinInserted\" state, the machine **uniquely** transitions to `Dispensing`. There is **no ambiguity**—it cannot sometimes dispense Coke and sometimes dispense water from the same exact input.\n>\n> - **Non-Deterministic Machine**: A faulty machine might, after the same input, randomly go to `Dispensing`, `OutOfStock`, or `Error`. This is a **Labeled Transition System (LTS)**, not a DTS.\n>\n> The key insight: **Given the current state AND the action, the next state is UNIQUELY determined in a DTS.**\n\n### Physical Constants & Standard Metrics (in Bold)\n\n- **State Count $|S|$** : Cardinality of the state space (typically **$10^2$ to $10^9$** in industrial model checking).\n- **Transition Count $|\\rightarrow|$** : Total number of valid transitions in the system.\n- **Branching Factor** : Average number of successors per state (equals **1.0** in a fully deterministic system).\n- **State Explosion Threshold** : Practically bounded at **$\\mathbf{10^{20}$ states** for explicit-state model checkers.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### Operational Logic Breakdown\n\nA DTS operates on a strictly constrained execution model. Let us deconstruct the operational semantics step-by-step:\n\n- **Step 1 — Initialization**: The system begins in some state $s_0 \\in S_0$. A *path* or *execution trace* is built by iteratively applying transitions.\n- **Step 2 — Action Selection**: At any state $s$, the environment (or scheduler) selects an action $\\alpha \\in \\Sigma$ that is *enabled* in $s$, i.e., $\\exists s' : s \\xrightarrow{\\alpha} s'$.\n- **Step 3 — Deterministic Resolution**: Because of the determinism constraint, the selected action $\\alpha$ produces **exactly one** successor state $s'$. There is no choice, no probability, and no external oracle required.\n- **Step 4 — Trace Construction**: An *infinite path* (or maximal finite path) is constructed:\n  $$\\pi = s_0 \\xrightarrow{\\alpha_0} s_1 \\xrightarrow{\\alpha_1} s_2 \\xrightarrow{\\alpha_2} \\ldots$$\n- **Step 5 — Path Uniqueness**: In a DTS, given $s_0$ and the action sequence $(\\alpha_0, \\alpha_1, \\ldots)$, the path $\\pi$ is **uniquely determined**. This property is the foundation of efficient verification algorithms like **DFS-based reachability analysis**.\n\n### The \"Why\" and \"How\" Behind Determinism\n\n- **Why determinism matters in verification**: In non-deterministic systems, we must check that a property holds for **ALL** possible paths (universal quantification). In deterministic systems, the same property check reduces to checking a **single path**, dramatically reducing algorithmic complexity.\n- **How it is exploited algorithmically**: Model checkers like **SPIN** and **NuSMV** can use deterministic structures to apply **BDD (Binary Decision Diagram)**-based symbolic verification, yielding exponential compression in many cases.\n\n### DTS vs. LTS vs. Kripke Structure — A Comparative View\n\n| Property | **Deterministic Transition System (DTS)** | Labeled Transition System (LTS) | Kripke Structure (KS) |\n|---|---|---|---|\n| Transition Type | Action-labeled | Action-labeled | State-labeled |\n| Successor Count | **At most 1** per (state, action) | 0, 1, or many | 0, 1, or many (per state) |\n| Primary Use | Algorithmic verification | Process algebra (CSP, CCS) | CTL / LTL model checking |\n| Branching | **None** (uniquely determined) | Possible | Possible |\n| Nondeterminism Source | **Absent** | Action choice | Transition choice |\n| Standard Notation | $\\rightarrow \\subseteq S \\times \\Sigma \\times S$ | Same | $R \\subseteq S \\times S$ |\n\n> [!IMPORTANT]\n> **KTU High-Yield Note**: Examiners frequently test the precise distinction between *determinism in transitions* (DTS) versus *determinism in initial states*. A DTS may have **multiple initial states** ($|S_0| > 1$) and still be deterministic on transitions. Do not confuse these two notions.\n\n### KTU Formula Sheet / Cheat Sheet\n\n| Symbol / Concept | Mathematical Formulation | Meaning | Engineering Utility |\n|---|---|---|---|\n| **DTS Tuple** | $TS = (S, S_0, \\Sigma, \\rightarrow)$ | Complete system definition | Foundation of model checking |\n| **Determinism Axiom** | $\\forall s \\in S, \\forall \\alpha \\in \\Sigma : \\mid Post(s, \\alpha) \\mid \\leq 1$ | At most one successor | Algorithmic tractability |\n| **Post-Image Operator** | $Post(s) = \\{s' \\in S \\mid s \\rightarrow s'\\}$ | Successor set of $s$ | Replaces with single element in DTS |\n| **Path** | $\\pi = s_0 \\rightarrow s_1 \\rightarrow s_2 \\ldots$ | Execution trace | Basis for LTL/CTL semantics |\n| **Reachable States** | $Reach(S_0) = \\bigcup_{i=0}^{\\infty} Post^i(S_0)$ | All states reachable from $S_0$ | State-space reduction |\n| **Path Uniqueness Theorem** | $s_0 + (\\alpha_0, \\alpha_1, \\ldots) \\Rightarrow \\text{unique } \\pi$ | Same input = same path | Enables symbolic verification |\n| **State Count Bound** | $\\mid S \\mid \\leq 2^{\\mid AP \\mid}$ | For Kripke over atomic propositions $AP$ | Complexity measure |\n\n> [!NOTE]\n> All absolute value notations use the safer `\\mid` or `\\vert` rendering to avoid table syntax corruption during markdown compilation.\n\n### Real-World Engineering Utility\n\n- **Hardware Verification**: Verifying finite-state machine (FSM) implementations in CPU pipelines where each clock cycle uniquely determines the next state.\n- **Protocol Verification**: Communication protocols (e.g., alternating bit protocol) often exhibit deterministic behavior under failure-free assumptions.\n- **Cyber-Physical Systems**: Discrete abstractions of control systems where actuator commands uniquely determine system evolution.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n### Derivation 1: From Non-Deterministic LTS to Deterministic TS (Determinization)\n\nGiven a non-deterministic LTS $LTS = (S, S_0, \\Sigma, \\rightarrow_N)$ where a state $s$ may have multiple successors under action $\\alpha$, we derive an equivalent DTS via the **subset construction** (Rabin-Scott powerset method).\n\n**Step 1**: Define the new state space as the powerset of $S$:\n$$S_{DTS} = 2^{S} = \\{T \\mid T \\subseteq S\\}$$\n\n**Step 2**: The initial state of the DTS is the singleton set of all initial states of the LTS:\n$$S_{0,DTS} = \\{s_0 \\in S_0\\}$$\n\n*(For a single initial state, this is simply $\\{s_0\\}$; for multiple, it is the set containing $s_0$.)*\n\n**Step 3**: The transition relation in the DTS is defined by the *union* of all successors in the LTS:\n$$\\forall T \\in S_{DTS}, \\forall \\alpha \\in \\Sigma : T \\xrightarrow{\\alpha}_{DTS} T' \\iff T' = \\bigcup_{s \\in T} Post_{LTS}(s, \\alpha)$$\n\nWhere $Post_{LTS}(s, \\alpha) = \\{s' \\in S \\mid s \\xrightarrow{\\alpha}_{N} s'\\}$ is the set of all $\\alpha$-successors in the original LTS.\n\n**Step 4**: Uniqueness of the successor — verify the determinism property holds:\n$$T' = \\bigcup_{s \\in T} \\{s' \\mid s \\xrightarrow{\\alpha} s'\\}$$\n\nSince this is a *set-theoretic union*, $T'$ is **uniquely determined** by $T$ and $\\alpha$. Hence $|\\{T' \\mid T \\xrightarrow{\\alpha}_{DTS} T'\\}| = 1$ for all valid $T$ and $\\alpha$.\n\n**Resulting DTS Tuple**:\n$$DTS_{derived} = (2^{S}, \\ \\{s_0\\}, \\ \\Sigma, \\ \\rightarrow_{DTS})$$\n\n**Step 5 (Complexity Note)**: The state space explodes exponentially:\n$$\\mid S_{DTS} \\mid = 2^{\\mid S \\mid}$$\n\nThis is the famous **determinization explosion** that makes verification of non-deterministic systems computationally hard (PSPACE-complete in general).\n\n---\n\n### Derivation 2: Path Uniqueness Proof Sketch\n\n**Theorem**: In a DTS, given $s_0 \\in S_0$ and a finite action sequence $\\sigma = \\alpha_0 \\alpha_1 \\ldots \\alpha_{n-1} \\in \\Sigma^*$, there exists **at most one** path $\\pi = s_0 \\rightarrow s_1 \\rightarrow \\ldots \\rightarrow s_n$.\n\n**Proof by Induction on Path Length**:\n\n- **Base Case ($n=0$)**: The empty path is just $s_0$ itself, which is unique. ✓\n\n- **Inductive Hypothesis**: Assume for any sequence of length $k$, the resulting path is unique.\n\n- **Inductive Step ($k \\rightarrow k+1$)**: Consider a path of length $k+1$:\n  $$\\pi = s_0 \\xrightarrow{\\alpha_0} s_1 \\xrightarrow{\\alpha_1} \\ldots \\xrightarrow{\\alpha_{k-1}} s_k \\xrightarrow{\\alpha_k} s_{k+1}$$\n\n  By the inductive hypothesis, $s_0, s_1, \\ldots, s_k$ are uniquely determined. Now consider the transition $s_k \\xrightarrow{\\alpha_k} s_{k+1}$. By the **Determinism Axiom** of DTS:\n  $$\\mid \\{s_{k+1} \\in S \\mid s_k \\xrightarrow{\\alpha_k} s_{k+1}\\} \\mid \\leq 1$$\n\n  Therefore, $s_{k+1}$ is uniquely determined. By concatenation, the entire path $\\pi$ of length $k+1$ is unique. $\\blacksquare$\n\n---\n\n### Analytical Matrix: Decision Procedures for DTS\n\n| Verification Task | Input | Algorithm | Complexity on DTS | Complexity on LTS |\n|---|---|---|---|---|\n| **Reachability** $EF \\phi$ | State predicate $\\phi$ | DFS / BFS | $O(\\mid S \\mid + \\mid\\rightarrow\\mid)$ | $O(\\mid S \\mid + \\mid\\rightarrow\\mid)$ |\n| **Invariant Checking** $AG \\phi$ | State predicate $\\phi$ | Fixed-point iteration | $O(\\mid S \\mid \\cdot \\mid \\rightarrow \\mid)$ | $O(\\mid S \\mid \\cdot \\mid \\rightarrow \\mid)$ |\n| **LTL Model Checking** | LTL formula $\\varphi$ | Nested DFS / Tarjan | $O(\\mid S \\mid \\cdot 2^{\\mid \\varphi \\mid})$ | $O(\\mid S \\mid^2 \\cdot 2^{\\mid \\varphi \\mid})$ |\n| **Deadlock Detection** | — | Terminal state check | $O(\\mid S \\mid)$ | $O(\\mid S \\mid \\cdot \\mid \\rightarrow \\mid)$ |\n| **CTL Model Checking** | CTL formula $\\psi$ | Labeling algorithm | $O(\\mid \\psi \\mid \\cdot (\\mid S \\mid + \\mid \\rightarrow \\mid))$ | Same |\n\n> [!IMPORTANT]\n> **Crucial Insight for KTU**: In a DTS, the **cycle detection** for LTL emptiness is polynomial because the path is uniquely determined by the state sequence—**no need for nested DFS over multiple branches**.\n\n---\n\n### Worked Example: Verifying a DTS Invariant\n\nConsider a DTS modeling a traffic light controller: $S = \\{Red, Yellow, Green\\}$, $S_0 = \\{Red\\}$, $\\Sigma = \\{tick\\}$.\n\nTransitions: $Red \\xrightarrow{tick} Green$, $Green \\xrightarrow{tick} Yellow$, $Yellow \\xrightarrow{tick} Red$.\n\n**Task**: Verify the LTL property $\\mathbf{G} \\ (Red \\lor Yellow \\lor Green)$ (i.e., the system is always in a valid state).\n\n- **Step 1**: Compute $Post^*(S_0) = \\{Red, Green, Yellow\\}$ (all three states are reachable).\n- **Step 2**: For each reachable state $s \\in Post^*(S_0)$, verify $s \\models (Red \\lor Yellow \\lor Green)$. This is trivially true by construction.\n- **Step 3**: Since the DTS has only 3 states, the algorithm terminates in $O(3)$ time, and the property holds globally.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## SECTION 4: Structural Diagrams & Schematics\n\n### Diagram 1: Canonical DTS Graph (Traffic Light Controller)\n\n```mermaid\ngraph LR\n    stateS0((S0 - Red)) -- tick --> stateS1((S1 - Green))\n    stateS1((S1 - Green)) -- tick --> stateS2((S2 - Yellow))\n    stateS2((S2 - Yellow)) -- tick --> stateS0((S0 - Red))\n\n    classDef initState fill:#e6f3ff,stroke:#0066cc,stroke-width:3px\n    classDef normalState fill:#f0f0f0,stroke:#333,stroke-width:1px\n    class stateS0 initState\n    class stateS1 normalState\n    class stateS2 normalState\n```\n\n**Reading the Diagram**: Each circle represents a state. Each directed arrow represents a deterministic transition labeled by the action `tick`. Notice that from every state, there is **at most one outgoing arrow per action** — the visual signature of determinism.\n\n---\n\n### Diagram 2: LTS-to-DTS Determinization (Subset Construction)\n\n```mermaid\ngraph TD\n    subgraph LTS_Original[\"Original LTS - Non Deterministic\"]\n        ltsA((q0)) -- a --> ltsB((q1))\n        ltsA((q0)) -- a --> ltsC((q2))\n        ltsB((q1)) -- b --> ltsD((q3))\n        ltsC((q2)) -- b --> ltsE((q4))\n    end\n\n    subgraph DTS_Derived[\"Derived DTS - Deterministic\"]\n        dtsInit((\"S0 - q0 set\")) -- a --> dtsMid((\"S1 - q1 union q2\"))\n        dtsMid((\"S1 - q1 union q2\")) -- b --> dtsFinal((\"S2 - q3 union q4\"))\n    end\n\n    LTS_Original --> DTS_Derived\n```\n\n**Reading the Diagram**: The left side shows the **original non-deterministic LTS** where state $q_0$ has *two* different $a$-successors ($q_1$ and $q_2$). The right side shows the **derived DTS** obtained via the subset construction. The new state $S_1$ is the *set* $\\{q_1, q_2\\}$, and the transition on action $a$ is now uniquely determined.\n\n---\n\n### Diagram 3: Block-Level Functional Architecture of DTS-Based Verification\n\n```mermaid\ngraph TB\n    inputBlock[\"System Specification\\nHardware or Software Model\"] --> modelBlock[\"Model Encoder\\nGenerates DTS Tuple\"]\n    modelBlock --> dtsBlock[\"DTS State Space\\nS, S0, Sigma, Transitions\"]\n    propertyBlock[\"Property Specification\\nLTL or CTL Formula\"] --> parserBlock[\"Formula Parser\\nAST Generation\"]\n    dtsBlock --> engineBlock[\"Model Checking Engine\\nDTS aware algorithm\"]\n    parserBlock --> engineBlock\n    engineBlock --> resultBlock[\"Verification Result\\nTRUE, FALSE, or COUNTEREXAMPLE\"]\n    resultBlock --> outputBlock[\"Examiner Output\\nReport and Trace\"]\n\n    classDef io fill:#fff4e6,stroke:#cc6600,stroke-width:2px\n    classDef core fill:#e6ffe6,stroke:#009900,stroke-width:2px\n    classDef decision fill:#e6e6ff,stroke:#3333cc,stroke-width:2px\n    class inputBlock,propertyBlock,outputBlock io\n    class modelBlock,dtsBlock,parserBlock,engineBlock core\n    class resultBlock decision\n```\n\n**Reading the Diagram**: The flow shows the **end-to-end verification pipeline** when using a DTS as the semantic model. The DTS state space is the central artifact that the model-checking engine consumes. Note how the DTS-aware algorithm can exploit determinism for efficient verification.\n\n---\n\n### Diagram 4: Path Uniqueness Visualization in DTS vs. LTS\n\n```mermaid\ngraph TB\n    subgraph DTS_Path[\"DTS - Single Unique Path\"]\n        dA((s0)) -- a --> dB((s1))\n        dB((s1)) -- b --> dC((s2))\n    end\n\n    subgraph LTS_Paths[\"LTS - Multiple Possible Paths\"]\n        lA((s0)) -- a --> lB1((s1))\n        lA((s0)) -- a --> lB2((s2))\n        lB1((s1)) -- b --> lC1((s3))\n        lB2((s2)) -- b --> lC2((s4))\n    end\n\n    DTS_Path --> CompareBlock{\"Same input\\nsequence?\"}\n    LTS_Paths --> CompareBlock\n    CompareBlock -- DTS --> OnePath[\"Exactly ONE\\nunique path\"]\n    CompareBlock -- LTS --> ManyPaths[\"Multiple possible\\npaths to explore\"]\n```\n\n**Reading the Diagram**: This side-by-side comparison illustrates the *path uniqueness property* derived in Section 3. For the same action sequence $(a, b)$, a DTS produces exactly one trace, while an LTS may produce exponentially many.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### Part A Questions (3 Marks Each)\n\n---\n\n**Q1. [KTU University Exam – Dec 2023, Model]**\n**Define a Deterministic Transition System (DTS) with a formal tuple. State the key property that distinguishes a DTS from a general Labeled Transition System (LTS).**\n\n**Model Answer (3 Marks)**:\n\nA Deterministic Transition System is a 4-tuple:\n$$DTS = (S, \\ S_0, \\ \\Sigma, \\ \\rightarrow)$$\n\nwhere:\n- $S$ is a finite non-empty set of states **(1 mark)**\n- $S_0 \\subseteq S$ is the set of initial states **(0.5 marks)**\n- $\\Sigma$ is a finite set of action labels **(0.5 marks)**\n- $\\rightarrow \\subseteq S \\times \\Sigma \\times S$ is the transition relation **(0.5 marks)**\n\n**Distinguishing Property**: In a DTS, for every state $s \\in S$ and every action $\\alpha \\in \\Sigma$, there is **at most one** successor state $s'$ such that $s \\xrightarrow{\\alpha} s'$. Formally: $\\forall s, \\forall \\alpha : \\mid \\{s' \\mid s \\xrightarrow{\\alpha} s'\\} \\mid \\leq 1$. **(0.5 marks)**\n\nThis determinism axiom does **not** hold in a general LTS, where a state may have multiple successors for the same action.\n\n---\n\n**Q2. [KTU University Exam – June 2024, Model]**\n**State and explain the \"Path Uniqueness Theorem\" for Deterministic Transition Systems. Mention its significance in automated verification.**\n\n**Model Answer (3 Marks)**:\n\n**Theorem Statement (1.5 marks)**: In a DTS, given an initial state $s_0 \\in S_0$ and a finite action sequence $\\sigma = \\alpha_0 \\alpha_1 \\ldots \\alpha_{n-1} \\in \\Sigma^*$, there exists **at most one** execution path:\n$$\\pi = s_0 \\xrightarrow{\\alpha_0} s_1 \\xrightarrow{\\alpha_1} \\ldots \\xrightarrow{\\alpha_{n-1}} s_n$$\n\n**Explanation (1 mark)**: The theorem follows directly from the determinism axiom. Once $s_0, s_1, \\ldots, s_{k-1}$ are uniquely determined, the transition $s_{k-1} \\xrightarrow{\\alpha_{k-1}} s_k$ yields a unique $s_k$ because the post-image set has cardinality at most 1.\n\n**Significance (0.5 marks)**: It reduces the verification of branching-time properties to the analysis of a single path, enabling polynomial-time algorithms for LTL model checking and avoiding the state explosion inherent in non-deterministic systems.\n\n---\n\n### Part B Questions (14 Marks Each) — KTU ESE Module Internal Choice Pattern\n\n---\n\n### **Question A (14 Marks)**\n\n**[KTU University Exam – Dec 2023, Adapted]**\n\n**(a)** Consider a Deterministic Transition System modeling a **2-bit binary counter** with the following specification:\n- $S = \\{00, 01, 10, 11\\}$\n- $S_0 = \\{00\\}$\n- $\\Sigma = \\{increment\\}$\n- Transitions: $00 \\xrightarrow{inc} 01$, $01 \\xrightarrow{inc} 10$, $10 \\xrightarrow{inc} 11$, $11 \\xrightarrow{inc} 00$ (overflow)\n\n**(i)** Draw the DTS graph. **(2 marks)**\n**(ii)** Verify using reachability analysis that the property $\\mathbf{EF} \\ (s = 11)$ holds. **(2 marks)**\n**(iii)** Prove that the system is deadlock-free. **(3 marks)**\n\n**(b)** Explain the **determinization procedure** (subset construction) that converts a non-deterministic LTS into an equivalent DTS. Illustrate with a small example showing the state-space blow-up. **(7 marks)**\n\n---\n\n#### Model Answer for Question A\n\n**(a)(i) DTS Graph (2 marks)**:\n\n```mermaid\ngraph LR\n    c00((00)) -- inc --> c01((01))\n    c01((01)) -- inc --> c10((10))\n    c10((10)) -- inc --> c11((11))\n    c11((11)) -- inc --> c00((00))\n```\n\nThe graph has 4 nodes (states) and 4 directed edges (transitions), forming a single cycle of length 4. The initial state is `00` (double-circled or marked).\n\n**(a)(ii) Reachability Analysis (2 marks)**:\n\nTo verify $EF (s = 11)$:\n- Step 1: Initialize $Reached = \\{00\\}$.\n- Step 2: $Post(00) = \\{01\\}$. Update $Reached = \\{00, 01\\}$.\n- Step 3: $Post(01) = \\{10\\}$. Update $Reached = \\{00, 01, 10\\}$.\n- Step 4: $Post(10) = \\{11\\}$. Update $Reached = \\{00, 01, 10, 11\\}$.\n- Step 5: State $11$ is in $Reached$. Therefore $EF (s = 11)$ is **TRUE**. ✓\n\n**(a)(iii) Deadlock-Freeness Proof (3 marks)**:\n\nA deadlock exists in a state $s$ if $s$ has **no outgoing transitions** for any action in $\\Sigma$.\n\nIn the 2-bit counter DTS, for every state $s \\in S$:\n- $Post(00) = \\{01\\} \\neq \\emptyset$\n- $Post(01) = \\{10\\} \\neq \\emptyset$\n- $Post(10) = \\{11\\} \\neq \\emptyset$\n- $Post(11) = \\{00\\} \\neq \\emptyset$\n\nSince every state has at least one outgoing transition, the DTS is **deadlock-free**. ∎\n\n**(b) Determinization via Subset Construction (7 marks)**:\n\n**Concept (2 marks)**: Given a non-deterministic LTS $LTS = (S, S_0, \\Sigma, \\rightarrow_N)$ where $|\\{s' \\mid s \\xrightarrow{\\alpha}_N s'\\}|$ may be greater than 1, the **subset construction** (also called *Rabin-Scott powerset construction*) builds an equivalent DTS over the *powerset* of the original state space.\n\n**Algorithm Steps (3 marks)**:\n\n1. New state space: $S_{DTS} = 2^S$\n2. Initial state: $S_{0,DTS} = S_0$ (a singleton set if $|S_0| = 1$)\n3. Transition: $T \\xrightarrow{\\alpha}_{DTS} T'$ where $T' = \\bigcup_{s \\in T} Post_{LTS}(s, \\alpha)$\n\n**Example with State-Space Blowup (2 marks)**:\n\nConsider an LTS with 3 states $\\{q_0, q_1, q_2\\}$ where $q_0$ has two successors on action $a$ (namely $q_1$ and $q_2$).\n\n- Original LTS: 3 states.\n- Derived DTS: $2^3 = 8$ states: $\\emptyset, \\{q_0\\}, \\{q_1\\}, \\{q_2\\}, \\{q_0,q_1\\}, \\{q_0,q_2\\}, \\{q_1,q_2\\}, \\{q_0,q_1,q_2\\}$.\n\nThis exponential blowup ($2^{|S|}$) is the **central cost** of determinization and is the main reason why verification of non-deterministic systems remains computationally hard.\n\n---\n\n### **Question B (14 Marks)**\n\n**[KTU University Exam – June 2024, Adapted]**\n\n**(a)** Define a Deterministic Transition System. With a suitable example, differentiate between a DTS and a Kripke Structure. Show that any DTS can be viewed as a Kripke Structure (or vice versa) under appropriate mapping. **(7 marks)**\n\n**(b)** Consider a DTS modeling a **3-state mutual exclusion protocol** with states $\\{N (Non-Critical), T (Trying), C (Critical)\\}$, $S_0 = \\{N\\}$, and transitions $N \\xrightarrow{request} T$, $T \\xrightarrow{enter} C$, $C \\xrightarrow{release} N$.\n\n**(i)** Enumerate all execution paths of length 4. **(3 marks)**\n**(ii)** Verify the LTL safety property $\\mathbf{G} \\ (\\neg (s_1 = C \\land s_2 = C))$ under the assumption that this is a single-process system. **(2 marks)**\n**(iii)** Discuss how the analysis would change if the system were extended to two processes. **(2 marks)**\n\n---\n\n#### Model Answer for Question B\n\n**(a) Definition + DTS vs. Kripke (7 marks)**:\n\n**DTS Definition (1 mark)**: A DTS is a 4-tuple $TS = (S, S_0, \\Sigma, \\rightarrow)$ where transitions are labeled by *actions*.\n\n**Kripke Structure Definition (1 mark)**: A Kripke Structure is a tuple $K = (S, S_0, R, L)$ where $R \\subseteq S \\times S$ is a *stateless* transition relation and $L : S \\rightarrow 2^{AP}$ is a labeling function over atomic propositions.\n\n**Key Differences (3 marks)**:\n\n| Aspect | DTS | Kripke Structure |\n|---|---|---|\n| Transition label | Action $\\alpha \\in \\Sigma$ | None (unlabeled) |\n| State label | None (or implicit) | Set of atomic propositions $L(s)$ |\n| Primary logic | Action-based / process algebra | Temporal logic (CTL, LTL) |\n| Determinism | Optional, constrained by axiom | Optional, constrained by $\\mid Post(s) \\mid \\leq 1$ |\n\n**Inter-translation (2 marks)**:\n- DTS → Kripke: For each transition $s \\xrightarrow{\\alpha} s'$, introduce a fresh intermediate state $s_\\alpha$ labeled with proposition $\\alpha$. This makes actions into state-labels.\n- Kripke → DTS: For each transition $(s, s') \\in R$, introduce a unique action label $\\alpha_{s,s'}$. This embeds the state-labeling structure into the action labels.\n\n---\n\n**(b)(i) Enumeration of Paths of Length 4 (3 marks)**:\n\nThe DTS has a single path of length 4 (due to determinism):\n$$\\pi = N \\xrightarrow{request} T \\xrightarrow{enter} C \\xrightarrow{release} N \\xrightarrow{request} T$$\n\nStates along the path: $N, T, C, N, T$.\n\nAction sequence: $request, enter, release, request$.\n\n**Note**: Because the system is deterministic, the path is **unique** for any given action sequence. If we consider *all possible* action sequences of length 4, each sequence yields a unique path.\n\n---\n\n**(b)(ii) LTL Safety Verification (2 marks)**:\n\nThe property $\\mathbf{G} \\ (\\neg (s_1 = C \\land s_2 = C))$ asserts that two processes can never be in the critical section simultaneously.\n\nFor a single-process system, $s_2$ does not exist. The property reduces to $\\mathbf{G} \\ (s = C \\rightarrow \\text{valid})$, which is trivially true since the DTS has well-defined transitions.\n\nUsing model checking:\n- $Sat(\\mathbf{G} \\phi) = \\bigcap_{i \\geq 0} Pre^i(Sat(\\phi))$\n- Initial state $N$ satisfies $\\phi$ trivially.\n- By forward simulation, every reachable state satisfies $\\phi$. **Property holds**. ✓\n\n---\n\n**(b)(iii) Extension to Two Processes (2 marks)**:\n\nFor two processes, the state space explodes: $S = \\{N, T, C\\}^2$ has $3^2 = 9$ states. The interleaving of transitions from two processes creates a non-deterministic structure (e.g., from $(N, N)$, either process may take the `request` action). The system is **no longer a pure DTS** because the scheduler introduces non-determinism in the interleaving of actions.\n\nTo restore determinism, one must fix an interleaving policy (e.g., round-robin), effectively building a *product DTS* whose state space is $3^2 \\times 2 = 18$ states (including the scheduler's turn). Verification of mutual exclusion in the two-process case requires CTL model checking over the larger state space.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Common Pitfalls**\n>\n> 1. **Confusing determinism in transitions with determinism in initial states**: Students often write \"$S_0$ must be a singleton in a DTS.\" This is **WRONG**. A DTS can have $|S_0| > 1$; the determinism constraint applies only to the **transition relation**, not the initial state set.\n>\n> 2. **Forgetting the action label in the formal definition**: A DTS tuple must include the action set $\\Sigma$. Omitting it costs 0.5–1 mark.\n>\n> 3. **Mistaking \"labeled\" for \"deterministic\"**: All deterministic systems are labeled (or can be made labeled), but not all labeled systems are deterministic. Do not use the terms interchangeably.\n>\n> 4. **In path enumeration, ignoring action sequences**: When asked to \"enumerate all paths,\" students must specify both the **state sequence** and the **action sequence** that generates each path. Showing only the state sequence is incomplete.\n>\n> 5. **Forgetting the $2^{|S|}$ complexity note**: Whenever the question involves determinization, mentioning the **exponential state-space blowup** is a high-yield extra credit point.\n\n---\n\n### Topic Recap & Important Things to Remember\n\n- **Definition Recap**: A DTS is $(S, S_0, \\Sigma, \\rightarrow)$ with the **determinism axiom** $\\mid Post(s, \\alpha) \\mid \\leq 1$.\n- **Core Differentiator**: Determinism in a DTS applies to *transitions*, **not** to *initial states*.\n- **Key Theorem**: **Path Uniqueness** — same action sequence from the same start state yields a **unique** path.\n- **DTS vs. LTS**: DTS has $\\leq 1$ successor per (state, action); LTS allows multiple.\n- **DTS vs. Kripke**: DTS has *action labels* on transitions; Kripke has *atomic proposition labels* on states.\n- **Determinization Cost**: Converting LTS → DTS uses the **subset construction**, costing $O(2^{|S|})$ states.\n- **Algorithmic Win**: DTS enables **polynomial-time** LTL model checking via path uniqueness.\n- **Deadlock Test**: A DTS is deadlock-free iff every reachable state has at least one outgoing transition.\n- **Reachability**: $EF \\phi$ is verified by computing $Post^*(S_0)$ and checking if $Sat(\\phi) \\cap Post^*(S_0) \\neq \\emptyset$.\n- **Engineering Domains**: Hardware FSMs, communication protocols, discrete control systems, embedded software.\n- **Kripke Conversion Rule**: To convert DTS → Kripke, split each action-labeled transition $s \\xrightarrow{\\alpha} s'$ into two transitions via an intermediate state labeled with $\\alpha$.\n- **Notation Reminder**: Use $(S, S_0, \\Sigma, \\rightarrow)$ for DTS; use $(S, S_0, R, L)$ for Kripke Structure; do not mix.\n- **Common Trap**: The \"deterministic\" in DTS does **not** imply *complete* — a state may have zero outgoing transitions (deadlock), and the system is still a valid DTS.\n- **Complexity Class**: Reachability and cycle detection in DTS are in **P** (polynomial time), unlike the **PSPACE** complexity in general LTS.\n- **Verification Tool Tip**: SPIN, NuSMV, and CBMC all internally use DTS-style or BDD-compressed models for efficient verification.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t8",
          "title": "Executions - Execution Fragment",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Executions - Execution Fragment\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Transition System — The Foundation Model\n\n> [!IMPORTANT]\n> In the **KTU 2024 PECNT526 syllabus**, executions and execution fragments are defined over a **Transition System (TS)** — also known as a **Kripke Structure** in the model-checking literature (Clarke, Grumberg, Peled; Manna & Pnueli).\n\nA **Transition System** is a 5-tuple:\n$$TS = \\langle S, \\rightarrow, I, AP, L \\rangle$$\n\nwhere:\n- $S$ — a **non-empty finite set of states**.\n- $\\rightarrow \\subseteq S \\times S$ — the **transition relation** (each pair $(s, s')$ is a directed edge).\n- $I \\subseteq S$ — the **set of initial states**.\n- $AP$ — a set of **atomic propositions** (Boolean variables describing state properties).\n- $L : S \\rightarrow 2^{AP}$ — the **labeling function** mapping each state to the propositions true in it.\n\n### 1.2 Execution (Full / Infinite Path)\n\n> [!NOTE]\n> **Definition (Execution).** An **execution** of a transition system $TS$ is an **infinite sequence of states**\n> $$\\sigma \\;=\\; s_0,\\, s_1,\\, s_2,\\, \\ldots$$\n> such that for every $i \\geq 0$, the pair $(s_i, s_{i+1})$ belongs to the transition relation, i.e., $s_i \\rightarrow s_{i+1}$.\n\nFormally:\n$$\\sigma : \\mathbb{N} \\rightarrow S, \\quad \\text{with} \\quad \\forall i \\ge 0 : (s_i, s_{i+1}) \\in \\rightarrow$$\n\nExecutions are required to be **infinite** because temporal logics (LTL, CTL) are interpreted over **infinite behaviors** of reactive and concurrent systems.\n\n### 1.3 Execution Fragment (Finite Path)\n\n> [!NOTE]\n> **Definition (Execution Fragment).** An **execution fragment** (also called a *path fragment* or *finite path*) is a **finite non-empty sequence of states**\n> $$\\rho \\;=\\; s_0,\\, s_1,\\, \\ldots,\\, s_k \\quad (k \\ge 0)$$\n> such that for every $0 \\le i < k$, the transition $s_i \\rightarrow s_{i+1}$ holds.\n\nA fragment can have any **length $\\ge 1$**. A fragment consisting of a single state $s$ (with $k=0$) is called a **state fragment** and is trivially valid.\n\n### 1.4 Conceptual Analogy — The Metro Map\n\nImagine a metro rail network:\n\n| Concept in TS | Metro Analogy |\n|---------------|---------------|\n| State $s \\in S$ | A metro station |\n| Transition $s \\rightarrow s'$ | A track connecting two stations |\n| Initial state $s_0 \\in I$ | The originating/depot station |\n| **Execution** $\\sigma$ | A metro train that runs **forever** (looping service) — covers an infinite itinerary |\n| **Execution Fragment** $\\rho$ | A **finite ride** — boarding at one station and alighting at a later one |\n| **Initial Fragment** | A ride that **starts at the depot** (an initial state) |\n| **Maximal Fragment** | A ride that goes as far as the line allows — either hits a **terminal station** (dead-end state with no outgoing track) or loops forever |\n\nThis makes the abstraction immediately clear: a fragment is just a **window** or **clip** of an execution.\n\n### 1.5 Variants of Execution Fragments\n\n> [!IMPORTANT]\n> The following special classes of fragments are **frequently tested** in KTU examinations and are central to LTL/CTL semantics.\n\n- **Initial Fragment:** $\\rho$ such that $\\rho[0] \\in I$ — starts from an initial state.\n- **Maximal Fragment:** A fragment that **cannot be extended further**, either because:\n  1. It is **infinite** (so it is already complete), or\n  2. It is finite and its last state is a **terminal/dead-end state** (no outgoing transition).\n- **Prefix / Suffix:** $\\rho$ is a prefix of $\\tau$ if $\\rho$ appears at the start of $\\tau$. A suffix appears at the end.\n- **Induced Fragment of an Execution:** Any finite window cut out from an infinite execution $\\sigma$ is an execution fragment of $TS$.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Operational Decomposition — How a Fragment is Built\n\nAn execution fragment is constructed by iteratively applying the transition relation:\n\n1. **Choose a starting state** $s_0 \\in S$. (For an *initial* fragment, additionally require $s_0 \\in I$.)\n2. **Apply a transition:** Pick a state $s_1$ such that $s_0 \\rightarrow s_1$.\n3. **Repeat step 2** for each subsequent index until a termination criterion is met.\n4. **Termination conditions** that produce a *maximal* fragment:\n   - **Infinite extension** — the transition relation produces an endless sequence; or\n   - **Deadlock** — at the last state $s_k$, there exists **no** $s' \\in S$ with $s_k \\rightarrow s'$.\n\n### 2.2 The 'Why' Behind Each Property\n\n- **Why infinite executions?** Reactive and concurrent systems (operating systems, controllers, servers) are designed to **never terminate** — they keep responding to inputs. Model checkers must therefore reason about *infinite behaviors*.\n- **Why study fragments?** A *correctness argument* (proof) typically focuses on a **finite prefix** that establishes a property (e.g., \"the request is eventually granted\"), and a fragment gives the precise mathematical handle on such finite reasoning.\n- **Why the maximality condition?** In verification, an execution fragment represents a **partial observation**. The maximal fragment closes the observation, giving the verifier the **full witness** of system behavior starting from $s_0$.\n\n### 2.3 Concatenation of Fragments\n\n> [!IMPORTANT]\n> Two fragments can be **concatenated** only when the last state of the first matches the first state of the second. This is the foundation of *compositional* reasoning in model checking.\n\nFor fragments $\\rho = s_0 s_1 \\ldots s_k$ and $\\tau = t_0 t_1 \\ldots t_m$:\n\n$$\\rho \\cdot \\tau \\;=\\; s_0 s_1 \\ldots s_k t_1 t_2 \\ldots t_m \\quad \\text{defined iff} \\quad s_k = t_0$$\n\nThe shared state $s_k = t_0$ is \"absorbed\" — it is counted only once in the concatenation.\n\n### 2.4 KTU Formula Sheet / Cheat Sheet\n\n| Symbol | Meaning | Defining Condition | Length / Bound |\n|--------|---------|-------------------|----------------|\n| $TS$ | Transition System | $\\langle S, \\rightarrow, I, AP, L \\rangle$ | Finite components |\n| $\\sigma = s_0 s_1 s_2 \\ldots$ | Execution (infinite) | $\\forall i \\ge 0 : s_i \\rightarrow s_{i+1}$ | $\\vert\\sigma\\vert = \\aleph_0$ |\n| $\\rho = s_0 s_1 \\ldots s_k$ | Execution Fragment | $\\forall 0 \\le i < k : s_i \\rightarrow s_{i+1}$ | $\\vert\\rho\\vert_{\\text{states}} = k+1$ |\n| $\\vert\\vert\\rho\\vert\\vert$ | Number of transitions in $\\rho$ | $\\vert\\vert\\rho\\vert\\vert = k$ | $k \\ge 0$ |\n| $\\rho[0]$ | First state of $\\rho$ | $s_0$ | State |\n| $\\rho[\\text{last}]$ | Last state of $\\rho$ | $s_k$ | State |\n| $\\rho^i$ | Suffix of $\\rho$ starting at index $i$ | $s_i s_{i+1} \\ldots s_k$ | Sub-fragment |\n| $\\rho \\cdot \\tau$ | Concatenation | $s_k = t_0$ | $\\vert\\rho \\cdot \\tau\\vert = k + m$ |\n| $\\rho_0$ | Initial Fragment | $\\rho_0[0] \\in I$ | — |\n| $\\rho_{\\max}$ | Maximal Fragment | Cannot be extended | Terminal or infinite |\n\n### 2.5 Real-World Engineering Utility\n\n- **Model Checking (CTL/LTL):** Formulas like $\\mathbf{G}\\, p$ (\"globally $p$\") or $\\mathbf{F}\\, q$ (\"eventually $q$\") are evaluated by examining **executions** rooted at initial states; **fragments** are used to construct counter-examples.\n- **Runtime Verification:** A monitor observes a **finite execution fragment** of the running system and checks if a temporal property holds.\n- **Hardware Verification (RTL/SoC):** State-space exploration of a digital circuit produces a transition system; executions correspond to **input sequences** and fragments to **prefix traces**.\n- **Concurrent Software:** Executions of multi-threaded programs model all possible **interleavings** of thread actions; fragments model partial schedules seen during testing.\n- **Cyber-Physical Systems:** Executions of a controller correspond to **sensor-actuator histories**; fragments correspond to sampled observation windows.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Analytical Derivation — Length Arithmetic on Fragments\n\nLet $\\rho = s_0 s_1 \\ldots s_k$ and $\\tau = t_0 t_1 \\ldots t_m$ be two valid execution fragments of $TS$ such that $s_k = t_0$.\n\n**Step 1 — Count the states in each fragment:**\nThe fragment $\\rho$ contains the states $\\{s_0, s_1, \\ldots, s_k\\}$, so its state count is:\n\n$$\\text{states}(\\rho) \\;=\\; k + 1$$\n\n**Step 2 — Count the transitions in each fragment:**\nEach transition corresponds to one index $i$ with $0 \\le i < k$:\n\n$$\\text{trans}(\\rho) \\;=\\; \\vert\\vert\\rho\\vert\\vert \\;=\\; k$$\n\n**Step 3 — Form the concatenation and count its states:**\nThe concatenated fragment is $\\rho \\cdot \\tau = s_0 s_1 \\ldots s_{k-1} s_k t_1 t_2 \\ldots t_m$. The state $s_k = t_0$ is shared, so the total state count is:\n\n$$\\text{states}(\\rho \\cdot \\tau) \\;=\\; (k+1) + m \\;=\\; k + m + 1$$\n\n**Step 4 — Count the transitions of the concatenation:**\nThe shared state contributes transitions both from the end of $\\rho$ and from the start of $\\tau$, but the *internal* transition $s_{k-1} \\rightarrow s_k$ (or $s_k \\rightarrow t_1$) is consistent — so total transitions is simply the sum:\n\n$$\\text{trans}(\\rho \\cdot \\tau) \\;=\\; k + m \\;=\\; \\vert\\vert\\rho\\vert\\vert + \\vert\\vert\\tau\\vert\\vert$$\n\n**Step 5 — Verify closure under concatenation:**\nIf $\\rho$ satisfies $s_i \\rightarrow s_{i+1}$ for $0 \\le i < k$ and $\\tau$ satisfies $t_j \\rightarrow t_{j+1}$ for $0 \\le j < m$, and the join $s_k = t_0$ holds, then the union of conditions is precisely the condition required for $\\rho \\cdot \\tau$. Hence the concatenation is a valid execution fragment.\n\n### 3.2 Worked Numerical Example\n\nConsider a transition system with states $S = \\{A, B, C, D, E\\}$ and transitions $A \\rightarrow B$, $A \\rightarrow C$, $B \\rightarrow C$, $C \\rightarrow D$, $D \\rightarrow C$, $D \\rightarrow E$.\n\n**Fragment $\\rho_1 = A\\, B\\, C$:**\n\n$$\\text{states}(\\rho_1) = 3, \\quad \\text{trans}(\\rho_1) = 2$$\n\n**Fragment $\\rho_2 = C\\, D\\, E$:**\n\n$$\\text{states}(\\rho_2) = 3, \\quad \\text{trans}(\\rho_2) = 2$$\n\n**Concatenation** (valid because last$(\\rho_1) = C =$ first$(\\rho_2)$):\n\n$$\\rho_1 \\cdot \\rho_2 \\;=\\; A\\, B\\, C\\, D\\, E$$\n\n$$\\text{states}(\\rho_1 \\cdot \\rho_2) = 5, \\quad \\text{trans}(\\rho_1 \\cdot \\rho_2) = 4$$\n\nVerification by the formula:\n$$\\text{states} \\;=\\; k + m + 1 \\;=\\; 2 + 2 + 1 \\;=\\; 5 \\quad \\checkmark$$\n$$\\text{trans} \\;=\\; k + m \\;=\\; 2 + 2 \\;=\\; 4 \\quad \\checkmark$$\n\n### 3.3 Extension of a Fragment to a Maximal Fragment\n\n**Algorithm (informal proof):** Given a fragment $\\rho = s_0 s_1 \\ldots s_k$ that is **not maximal**, there exists at least one state $s' \\in S$ with $s_k \\rightarrow s'$. Append $s'$:\n\n$$\\rho' \\;=\\; s_0 s_1 \\ldots s_k s'$$\n\nRepeat the operation. Either:\n\n- The process produces an **infinite sequence** (a maximal fragment of infinite length), or\n- The process **terminates** at a state with no outgoing transitions (a finite maximal fragment).\n\nBy the **König's Lemma** (applied to the tree of all extensions rooted at $\\rho$), at least one maximal extension is guaranteed to exist in any finite-branching transition system.\n\n### 3.4 Analytical Matrix — Fragment Classification\n\n| Property | Definition | Necessary? | KTU Significance |\n|----------|------------|-----------|-------------------|\n| Validity | Each consecutive pair is a transition | Always | Definition |\n| Initiation | $\\rho[0] \\in I$ | For initial fragment | Counter-example starts here |\n| Maximality | No extension possible | For completeness | LTL evaluated on maximal/infinite |\n| Suffixed form | $\\rho^i$ for some $i$ | Used in LTL semantics | $\\sigma^i \\models \\phi$ definition |\n| Non-empty | $\\vert\\rho\\vert \\ge 1$ | Always | Empty path excluded |\n| Concatenable | last$(\\rho) =$ first$(\\tau)$ | Optional | Compositional reasoning |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Transition System with Highlighted Executions and Fragment\n\n```mermaid\ngraph LR\n    s0((s0 - initial)) -->|tick| s1((s1))\n    s1 -->|a| s2((s2))\n    s2 -->|b| s3((s3))\n    s3 -->|c| s4((s4))\n    s4 -->|d| s2\n    s3 -->|e| s5((s5 - terminal))\n    classDef init fill:#d4f7d4,stroke:#2e7d32,stroke-width:2px\n    classDef exec fill:#cfe8ff,stroke:#1565c0,stroke-width:2px\n    classDef frag fill:#fff2b3,stroke:#b8860b,stroke-width:3px\n    classDef term fill:#ffd6d6,stroke:#b71c1c,stroke-width:2px\n    class s0 init\n    class s1,s2,s3,s4 exec\n    class s2 exec\n    class s5 term\n```\n\n**Reading the diagram:**\n- The **green** node $s_0$ is the **initial state**.\n- The **blue** path $s_0 \\rightarrow s_1 \\rightarrow s_2 \\rightarrow s_3 \\rightarrow s_4 \\rightarrow s_2 \\rightarrow \\ldots$ forms an **infinite execution** $\\sigma$ (it loops through the cycle $s_2 \\rightarrow s_3 \\rightarrow s_4$).\n- The **red** node $s_5$ is a **terminal state** — no outgoing transitions.\n\n### 4.2 Block Diagram — Relation Between Execution, Fragment, and Maximal Fragment\n\n```mermaid\ngraph TD\n    A[Transition System TS] --> B[Initial States I]\n    A --> C[State Space S]\n    A --> D[Transition Relation R]\n    B --> E[Start Point of Execution]\n    C --> F[Build Sequence of States]\n    D --> F\n    F --> G[Execution sigma - infinite]\n    F --> H[Execution Fragment rho - finite]\n    G --> I[Maximal Fragment - infinite]\n    H --> J[Maximal Fragment - finite - terminal]\n    H --> K[Extend via transition relation]\n    K --> I\n    K --> J\n    style A fill:#e1f5ff,stroke:#0277bd\n    style G fill:#c8e6c9,stroke:#1b5e20\n    style H fill:#fff9c4,stroke:#f57f17\n    style I fill:#c8e6c9,stroke:#1b5e20\n    style J fill:#ffcdd2,stroke:#b71c1c\n```\n\n### 4.3 Sequence Diagram — Constructing an Execution Fragment\n\n```mermaid\nsequenceDiagram\n    participant U as User/Verifier\n    participant TS as Transition System\n    participant Buf as Fragment Buffer\n    U->>TS: Query initial state s0\n    TS-->>Buf: s0\n    U->>TS: Request successor of s0\n    TS-->>Buf: s1\n    U->>TS: Request successor of s1\n    TS-->>Buf: s2\n    U->>TS: Check if s2 has successor\n    alt s2 has no successor\n        TS-->>Buf: TERMINAL marker\n        Buf-->>U: Maximal fragment s0 s1 s2\n    else s2 has successor\n        TS-->>Buf: s3\n        Buf-->>U: Continued fragment\n    end\n```\n\n### 4.4 Worked Example Diagram — Concrete Mutual-Exclusion-Like System\n\n```mermaid\ngraph LR\n    n0((n0 idle)) -->|request| n1((n1 waiting))\n    n1 -->|grant| n2((n2 critical))\n    n2 -->|release| n0\n    n1 -->|abort| n0\n    classDef state fill:#e3f2fd,stroke:#0d47a1,stroke-width:2px\n    class n0,n1,n2 state\n```\n\n- A fragment of length 3: $n_0 \\rightarrow n_1 \\rightarrow n_2$ (initial fragment, finite, not maximal because $n_2$ has a successor).\n- A maximal fragment of length 3: $n_0 \\rightarrow n_1 \\rightarrow n_0$ — but wait, since $n_0$ has a successor, the only **truly maximal finite** fragment would be a path ending at a dead-end. If we add a transition $n_2 \\rightarrow n_2$ (self-loop), then $n_0 \\rightarrow n_1 \\rightarrow n_2 \\rightarrow n_2 \\rightarrow \\ldots$ is the unique maximal **infinite** execution.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### Part A — Short Answer Questions (3 Marks Each)\n\n> **[KTU University Exam — Model Question, PECNT526]**\n\n**Q1.** Define an *execution fragment* of a transition system. Differentiate between an *initial fragment* and a *maximal fragment* with a one-line distinction each. **[3 Marks]**\n\n**Model Answer:**\nAn execution fragment of a transition system $TS$ is a finite (or infinite) sequence of states $\\rho = s_0 s_1 \\ldots s_k$ such that $s_i \\rightarrow s_{i+1}$ for every $0 \\le i < k$. An **initial fragment** has its first state in $I$ (i.e., $\\rho[0] \\in I$), while a **maximal fragment** cannot be extended — it is either infinite or ends in a state with no outgoing transition. *(3 marks — 1 for definition, 1 each for the two distinctions.)*\n\n---\n\n**Q2.** State the formal definition of an *execution* in model checking. Why is an execution required to be infinite rather than finite? **[3 Marks]**\n\n**Model Answer:**\nAn execution of $TS = \\langle S, \\rightarrow, I, AP, L \\rangle$ is an infinite sequence $\\sigma = s_0 s_1 s_2 \\ldots$ such that for all $i \\ge 0$, $s_i \\rightarrow s_{i+1}$. Executions are infinite because reactive and concurrent systems are *non-terminating* by design, and temporal logic semantics (LTL/CTL) are defined over infinite traces to properly capture properties like *liveness* (e.g., \"eventually $p$\"). *(3 marks — 1.5 for definition, 1.5 for justification.)*\n\n---\n\n### Part B — Long Answer Questions (14 Marks Each)\n\n> **ESE Module Internal Choice — Two alternatives given. Answer ANY ONE full question.**\n\n---\n\n#### **Question A (14 Marks)** — `[KTU University Exam — Dec 2023 Style]`\n\n**(a)** Define a *transition system* formally. With a neat diagram of a 5-state transition system, label the components $S, \\rightarrow, I, AP, L$ and show one execution and one execution fragment. **[7 Marks]**\n\n**Model Solution:**\n\nA transition system is a 5-tuple $TS = \\langle S, \\rightarrow, I, AP, L \\rangle$ where:\n- $S = \\{s_0, s_1, s_2, s_3, s_4\\}$ — finite non-empty set of states.\n- $\\rightarrow \\subseteq S \\times S$ — transition relation: $\\{s_0 \\rightarrow s_1, s_1 \\rightarrow s_2, s_2 \\rightarrow s_3, s_3 \\rightarrow s_2, s_2 \\rightarrow s_4\\}$.\n- $I = \\{s_0\\}$ — initial state.\n- $AP = \\{p, q\\}$ — atomic propositions.\n- $L(s_0) = \\{p\\}$, $L(s_1) = \\{p\\}$, $L(s_2) = \\{q\\}$, $L(s_3) = \\{p, q\\}$, $L(s_4) = \\emptyset$.\n\n**Execution:** $\\sigma = s_0, s_1, s_2, s_3, s_2, s_3, s_2, \\ldots$ (infinite, looping through $s_2, s_3$).\n\n**Execution Fragment:** $\\rho = s_0, s_1, s_2$ — a valid initial fragment of length 3. *(7 marks — 2 for definition, 3 for diagram with labels, 2 for execution/fragment identification.)*\n\n**(b)** Explain the concept of *concatenation* of execution fragments. State the necessary condition. With an example from part (a), show how two fragments are combined and verify the resulting state count. **[7 Marks]**\n\n**Model Solution:**\n\nConcatenation $\\rho \\cdot \\tau$ of two execution fragments is defined iff the last state of $\\rho$ equals the first state of $\\tau$.\n\nFormally, for $\\rho = s_0 \\ldots s_k$ and $\\tau = t_0 \\ldots t_m$:\n\n$$\\rho \\cdot \\tau \\text{ is valid iff } s_k = t_0, \\quad \\rho \\cdot \\tau = s_0 s_1 \\ldots s_k t_1 t_2 \\ldots t_m$$\n\n**Example:** Let $\\rho_1 = s_0 s_1 s_2$ and $\\rho_2 = s_2 s_3 s_2$ from part (a). Then:\n\n$$\\rho_1 \\cdot \\rho_2 = s_0 s_1 s_2 s_3 s_2$$\n\nVerification:\n- $\\rho_1$ has $k = 2$ transitions, $\\rho_2$ has $m = 2$ transitions.\n- The shared state $s_2$ is the join point.\n- Resulting state count: $k + m + 1 = 2 + 2 + 1 = 5$ states $\\checkmark$.\n- Each consecutive pair is a valid transition by construction $\\checkmark$. *(7 marks — 2 for definition, 1 for condition, 4 for worked example.)*\n\n---\n\n#### **Question B (14 Marks)** — `[KTU University Exam — May 2024 Style]`\n\n**(a)** Differentiate between a *finite execution fragment* and an *infinite execution* of a transition system. Construct a transition system in which a fragment can be extended to *two distinct* maximal fragments, and justify. **[7 Marks]**\n\n**Model Solution:**\n\n| Aspect | Finite Fragment $\\rho$ | Infinite Execution $\\sigma$ |\n|--------|------------------------|------------------------------|\n| Length | $k+1$ states, $k$ transitions | Countably infinite |\n| Termination | Ends in terminal state | Never ends |\n| Suited for | Counter-examples, prefix reasoning | LTL/CTL satisfaction |\n| Maximality | Only if terminal | Always maximal |\n\n**Construction (two maximal extensions):** Consider $TS$ with $I = \\{s_0\\}$, transitions $s_0 \\rightarrow s_1$, $s_0 \\rightarrow s_2$, $s_1 \\rightarrow s_1$ (self-loop), and $s_2 \\rightarrow s_2$ (self-loop).\n\nTake the fragment $\\rho = s_0$. It can be extended as:\n- $\\sigma_1 = s_0 s_1 s_1 s_1 \\ldots$ (infinite maximal execution).\n- $\\sigma_2 = s_0 s_2 s_2 s_2 \\ldots$ (infinite maximal execution).\n\nBoth are maximal because they are infinite. The fragment $s_0$ itself is **not** maximal (it has two successors), and it can be extended into two distinct maximal fragments, illustrating *non-determinism* in the system. *(7 marks — 2 for table, 3 for construction, 2 for justification.)*\n\n**(b)** With reference to LTL/CTL model checking, explain why a *maximal execution fragment* is the appropriate object on which to evaluate temporal formulas. Illustrate with the formula $\\mathbf{G}(p \\rightarrow \\mathbf{F}\\, q)$ (globally, $p$ implies eventually $q$). **[7 Marks]**\n\n**Model Solution:**\n\nTemporal logic formulas like $\\mathbf{G}(p \\rightarrow \\mathbf{F}\\, q)$ assert properties about the **entire future** of a state. To evaluate such formulas truthfully, the verifier must examine the **complete behavior** starting from that state — i.e., a **maximal** execution. A non-maximal fragment ends prematurely and could *falsely* satisfy or violate a liveness property (e.g., $q$ may not have occurred yet simply because we stopped observing).\n\n**For the formula** $\\phi \\equiv \\mathbf{G}(p \\rightarrow \\mathbf{F}\\, q)$:\n- For every state in the path, if $p$ holds, then $q$ must hold at some **future** state.\n- If the path is finite and ends with $p$ true but no $q$ observed, the formula *falsely* holds under naive finite-trace semantics.\n- Hence, LTL evaluation mandates **infinite** paths (or paths extended to maximality).\n\n**Illustration:** Suppose $\\rho = s_0 s_1 s_2$ with $L(s_0) = \\{p\\}$, $L(s_1) = \\{p\\}$, $L(s_2) = \\{q\\}$ and $s_2$ is a terminal state. Under *finite* trace semantics, $p \\rightarrow \\mathbf{F} q$ holds at $s_1$ because $q$ appears later. But if we extend $\\rho$ to a maximal execution and discover that from $s_0$ another path $s_0 s_3 s_4 \\ldots$ never reaches $q$, the universal quantifier over all maximal executions *falsifies* $\\phi$. Thus, model checking evaluates formulas over **all maximal executions** rooted at initial states. *(7 marks — 3 for conceptual reasoning, 2 for formula explanation, 2 for illustration.)*\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls**\n> 1. **Confusing execution with execution fragment** — the biggest single source of lost marks. Always state explicitly whether the sequence is **finite** or **infinite**.\n> 2. **Forgetting the empty-pipe rule in tables** — when writing $\\vert\\rho\\vert$ in handwritten or typed answers, use proper notation. Examiners will *not* penalize, but sloppy notation invites ambiguity.\n> 3. **Omitting the join condition** in concatenation — many students write $\\rho \\cdot \\tau$ without stating that $s_k = t_0$ is **required**. Always state the *precondition*.\n> 4. **Treating executions as starting from any state** — in CTL/LTL model checking, the path quantifier $\\mathbf{A}/\\mathbf{E}$ ranges over executions starting from a *specific* state, often the initial state. Be precise.\n> 5. **Mark loss on definitions** — for 3-mark questions, examiners expect: **(i)** formal statement, **(ii)** a clear distinguishing example or property, **(iii)** a one-line justification. Missing any of these costs at least 1 mark.\n\n---\n\n### Topic Recap & Important Things to Remember\n\n- **Transition System (Kripke Structure):** $TS = \\langle S, \\rightarrow, I, AP, L \\rangle$ — the foundational model for model checking.\n- **Execution** $\\sigma = s_0 s_1 s_2 \\ldots$ is **infinite**; each consecutive pair is in $\\rightarrow$.\n- **Execution Fragment** $\\rho = s_0 s_1 \\ldots s_k$ is **finite** (or possibly infinite); same transition condition.\n- **Initial Fragment:** $\\rho[0] \\in I$.\n- **Maximal Fragment:** cannot be extended — either infinite or ends in a dead-end state.\n- **Concatenation** $\\rho \\cdot \\tau$ requires last$(\\rho) =$ first$(\\tau)$; result has $k + m + 1$ states and $k + m$ transitions.\n- **Length notation:** $\\vert\\rho\\vert_{\\text{states}} = k+1$, $\\vert\\vert\\rho\\vert\\vert_{\\text{trans}} = k$.\n- **Suffix of execution:** $\\sigma^i$ is the $i$-th suffix, central to LTL semantics $\\sigma \\models \\phi \\iff \\sigma^0 \\models \\phi$.\n- **König's Lemma** guarantees the existence of a maximal extension for any fragment in a finite-branching TS.\n- **Real-world use:** model checking, runtime verification, hardware verification, concurrent system analysis.\n- **Always specify finiteness/infinity** — this single qualifier distinguishes an execution from a fragment in both definition and application.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t9",
          "title": "Maximal and Initial Execution Fragment",
          "content": "<!-- SECTION_1_START -->\n> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module Module 1\n> - **Topic:** Maximal and Initial Execution Fragment\n\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Foundations: Transition Systems (Kripke Structures)\n\nBefore defining execution fragments, we must first establish the underlying mathematical model used in automated verification. A **Kripke Structure** (or Transition System) is the standard semantic model.\n\nLet $AP$ be a non-empty set of **atomic propositions**. A Kripke Structure is formally defined as the 4-tuple:\n\n$$\nTS = (S,\\, S_0,\\, R,\\, L)\n$$\n\nWhere the components are defined as:\n- **$S$**: A finite (or countably infinite) non-empty set of **states**.\n- **$S_0 \\subseteq S$**: A non-empty set of **initial states** (the entry points of the system).\n- **$R \\subseteq S \\times S$**: A **transition relation**, where $(s, s') \\in R$ means the system can move from state $s$ to $s'$ in a single atomic step.\n- **$L : S \\rightarrow 2^{AP}$**: A **labeling function** that maps each state to the set of atomic propositions true in that state.\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight (Module 1):**\n> In many verification frameworks, the transition relation $R$ is required to be **total** — meaning every state has at least one outgoing transition. If $R$ is total, every state is non-terminal, and maximal execution fragments will always be **infinite sequences**.\n\n## 1.2 Definition of an Execution Fragment\n\nAn **execution fragment** of a transition system $TS$ is a sequence of states (finite or infinite) representing one possible way the system may behave.\n\nFormally, an execution fragment is a sequence:\n\n$$\n\\sigma = s_0,\\, s_1,\\, s_2,\\, \\dots\n$$\n\nSuch that for **every** index $i \\geq 0$, the pair $(s_i,\\, s_{i+1})$ is a valid transition:\n\n$$\n\\forall\\, i \\geq 0 : (s_i,\\, s_{i+1}) \\in R\n$$\n\n> [!NOTE]\n> **Conceptual Analogy (The Train Track Analogy):**\n> Imagine a model railway network. The **states** are the stations, and the **transitions** are the railway tracks connecting them. An **execution fragment** is any route a train can take — it can be:\n> - A short trip from Station A → Station B → Station C (a *finite* fragment).\n> - An endless loop the train runs forever (an *infinite* fragment).\n> - A trip that suddenly stops at a dead-end station with no outgoing tracks (a *finite* fragment ending in a *terminal* state).\n>\n> The rules of the tracks are dictated by $R$, and the journey must respect the physical layout of the railway at every step.\n\n## 1.3 Initial Execution Fragment\n\nAn execution fragment $\\sigma = s_0, s_1, s_2, \\dots$ is called an **Initial Execution Fragment** if and only if its first state belongs to the set of initial states:\n\n$$\n\\sigma \\text{ is initial} \\iff s_0 \\in S_0\n$$\n\n> [!NOTE]\n> **Intuition:** An initial execution fragment is a *plausible system run* — one that the system could actually exhibit starting from power-on. The \"initial\" qualifier guarantees we are analyzing a behavior that is *reachable* from the system's startup, not just any arbitrary fragment floating in the state space.\n\n## 1.4 Maximal Execution Fragment\n\nAn execution fragment $\\sigma = s_0, s_1, s_2, \\dots$ is called a **Maximal Execution Fragment** if and only if it **cannot be extended** any further. Formally, $\\sigma$ is maximal if and only if one of the following two conditions holds:\n\n1. **The fragment is infinite** — i.e., $\\sigma$ has no last state and the sequence $s_0, s_1, s_2, \\dots$ is unbounded.\n\n2. **The fragment is finite and ends in a terminal state** — i.e., $\\sigma = s_0, s_1, \\dots, s_n$ and there is **no** $s_{n+1} \\in S$ such that $(s_n, s_{n+1}) \\in R$.\n\n> [!IMPORTANT]\n> **Why \"Maximal\" Matters in Verification:**\n> A maximal fragment captures the **complete, un-interrupted** behavior of the system for a particular run. When we evaluate a temporal property like \"eventually the system reaches a safe state,\" we are not interested in *partial* fragments that were arbitrarily cut off — we want to know what the system does *on its own, until it stops or runs forever*. This is the standard semantic object over which LTL and CTL formulas are interpreted.\n\n## 1.5 Path: The Most Important Special Case\n\nIn the context of model checking (especially for **LTL**), the term **path** has a very precise meaning:\n\n> A **path** is a **maximal initial execution fragment** of the transition system.\n\nSo a path is simultaneously:\n- **Initial** → it starts in $S_0$.\n- **Maximal** → it cannot be extended (it is either infinite or ends in a deadlocked state).\n\nMathematically:\n\n$$\n\\pi = s_0, s_1, s_2, \\dots \\quad \\text{where} \\quad s_0 \\in S_0 \\quad \\text{and} \\quad \\sigma \\text{ is maximal}\n$$\n\n> [!WARNING]\n> **Common Student Misconception:**\n> Some textbooks use the words \"run\", \"trace\", \"execution\", and \"path\" interchangeably. In KTU-style automated verification questions, **\"path\" almost always means maximal + initial**. Do not write a \"path\" that starts in a non-initial state or one that is abruptly truncated.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Hierarchy of Execution Fragments\n\nThe terms we have defined form a strict conceptual hierarchy. Understanding this hierarchy is **critical** for KTU exam answers because questions often ask you to classify a given sequence.\n\n### Classification Logic (Step-by-Step):\n\n1. **Step 1 — Is it an execution fragment?**\n   - Verify: For every consecutive pair $(s_i, s_{i+1})$ in the sequence, check if $(s_i, s_{i+1}) \\in R$.\n   - If even one pair is missing, the sequence is **not** an execution fragment.\n\n2. **Step 2 — Is it initial?**\n   - Verify: Is the very first state $s_0$ an element of $S_0$?\n   - If yes → it is an **initial execution fragment**.\n   - If no → it is a *non-initial* fragment (relevant for sub-system analysis, not full-system verification).\n\n3. **Step 3 — Is it maximal?**\n   - **Case A**: The fragment is infinite → automatically maximal.\n   - **Case B**: The fragment is finite, ending at $s_n$. Check: does $s_n$ have *any* outgoing transition? If $(s_n, s') \\in R$ for some $s'$, then the fragment is **not maximal** (it can be extended). If there is no such $s'$, then $s_n$ is a **terminal/deadlock state** and the fragment is maximal.\n\n4. **Step 4 — Combining classifications:**\n   - **Maximal + Initial** = **Path** (the canonical object of model checking).\n   - **Non-maximal + Initial** = a *prefix of a path* (relevant when reasoning about partial system runs).\n   - **Maximal + Non-initial** = *liveness tail* (relevant for fairness/recovery analysis).\n   - **Non-maximal + Non-initial** = *arbitrary fragment* (least interesting for verification).\n\n## 2.2 KTU Formula / Definition Cheat Sheet\n\n| # | Concept | Formal Condition / Formula | Notation Used | Exam Relevance |\n|---|---------|----------------------------|---------------|----------------|\n| 1 | Kripke Structure | $TS = (S, S_0, R, L)$ | $S$ = states, $R$ = relation | Always required to define TS first |\n| 2 | Execution Fragment | $\\forall i \\geq 0 : (s_i, s_{i+1}) \\in R$ | $\\sigma = s_0, s_1, \\dots$ | Base object of all reasoning |\n| 3 | Initial Fragment | $s_0 \\in S_0$ | First state in $S_0$ | Filters out unreachable runs |\n| 4 | Maximal Fragment | (infinite) $\\lor$ ($\\text{finite} \\land \\lnot \\exists s' : (s_n, s') \\in R$) | Cannot be extended | Guarantees complete behavior |\n| 5 | Path | Maximal **AND** Initial | $\\pi = s_0, s_1, \\dots$ with $s_0 \\in S_0$ | Object over which LTL is evaluated |\n| 6 | Length of Fragment | $\\vert \\sigma \\vert = n+1$ for $\\sigma = s_0 \\dots s_n$ | Cardinality of state sequence | Used in Kripke/Witness proofs |\n| 7 | Suffix of a Path | $\\pi^k = s_k, s_{k+1}, \\dots$ | Drop first $k$ states | Used in fixpoint semantics of LTL |\n| 8 | Prefix of a Path | $\\pi^{\\leq k} = s_0, s_1, \\dots, s_k$ | First $k+1$ states | Used in bounded model checking |\n| 9 | Total $R$ Condition | $\\forall s \\in S : \\exists s' \\in S : (s, s') \\in R$ | Every state has outgoing edge | Ensures all maximal fragments are infinite |\n| 10 | Stuttering Step | $s = s'$ where $(s, s') \\in R$ | Self-loop | Important in stuttering-invariant LTL |\n\n> [!NOTE]\n> **Real-World Engineering Utility:**\n> - **Hardware Verification (RTL/Netlist):** Each state is a bit-vector snapshot of all registers. A path corresponds to one possible execution of the silicon — model checkers like Cadence JasperGold enumerate these to prove safety properties.\n> - **Protocol Verification (5G, Bluetooth, Cache Coherence):** Each state is a global configuration of messages and channel states. Maximal initial execution fragments model the *longest* possible protocol runs, including deadlocks.\n> - **Software Verification (Model Checking C/Java):** Tools like CBMC and Java PathFinder construct all paths up to a bound to prove memory safety, absence of division-by-zero, etc.\n\n## 2.3 The Role of Maximal Fragments in LTL Semantics\n\nIn **Linear Temporal Logic (LTL)**, every formula $\\varphi$ is interpreted over a path $\\pi$. For a transition system $TS$, the satisfaction relation is:\n\n$$\nTS \\models \\varphi \\iff \\forall\\, \\pi \\in \\text{Paths}(TS) : \\pi \\models \\varphi\n$$\n\nThe symbol $\\text{Paths}(TS)$ denotes the **set of all maximal initial execution fragments** of $TS$. This is the precise reason *maximal* and *initial* are both essential — without maximality, we could cheat by truncating a path right before a property is violated.\n\n> [!IMPORTANT]\n> **KTU-Style Key Insight:**\n> LTL's semantics treats a path as if it *runs forever*. Even if a maximal fragment is finite (ending in a deadlock), LTL treats the deadlock as a special \"looping at a sink\" — the finite tail is implicitly extended with infinite stuttering at the terminal state. This convention exists so that *every* maximal fragment can host an LTL truth value.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Analytical Matrix: Classifying Any Given Sequence\n\nBelow is a **decision procedure** (exhaustive step-by-step algorithm) that examiners expect you to demonstrate in 7–14 mark answers. We will use a generic transition system to illustrate the procedure.\n\n### Worked Example Setup\n\nConsider a transition system with:\n- $S = \\{s_0, s_1, s_2, s_3, s_4\\}$\n- $S_0 = \\{s_0, s_1\\}$\n- $R = \\{(s_0, s_1), (s_1, s_2), (s_1, s_3), (s_2, s_2), (s_3, s_4)\\}$\n- $L(s) = \\{p\\}$ for all states (labeling is not central to this topic).\n\n### Classification Procedure\n\n**Step 1: List candidate sequences** from the transition system.\n\n$$\n\\sigma_1 = s_0, s_1, s_2\n$$\n$$\n\\sigma_2 = s_0, s_1, s_3, s_4\n$$\n$$\n\\sigma_3 = s_1, s_2, s_2, s_2, \\dots \\text{ (infinite loop)}\n$$\n$$\n\\sigma_4 = s_0, s_1, s_2, s_2, s_3\n$$\n$$\n\\sigma_5 = s_2, s_3, s_4\n$$\n\n**Step 2: Apply the execution fragment test** (check that every adjacent pair is in $R$).\n\nFor $\\sigma_1 = s_0, s_1, s_2$:\n- $(s_0, s_1) \\in R$ ✓\n- $(s_1, s_2) \\in R$ ✓\n- Hence, $\\sigma_1$ is a valid execution fragment.\n\nFor $\\sigma_4 = s_0, s_1, s_2, s_2, s_3$:\n- $(s_0, s_1) \\in R$ ✓\n- $(s_1, s_2) \\in R$ ✓\n- $(s_2, s_2) \\in R$ ✓ (self-loop)\n- $(s_2, s_3) \\in R$ ✗ — **NOT** in $R$, since $R$ contains no edge from $s_2$ to $s_3$.\n- Hence, $\\sigma_4$ is **not** an execution fragment.\n\n**Step 3: Apply the initial test** (check whether the first state is in $S_0$).\n\n$$\n\\text{For } \\sigma_1 = s_0, s_1, s_2: s_0 \\in S_0 \\implies \\text{initial}.\n$$\n$$\n\\text{For } \\sigma_3 = s_1, s_2, s_2, \\dots: s_1 \\in S_0 \\implies \\text{initial}.\n$$\n$$\n\\text{For } \\sigma_5 = s_2, s_3, s_4: s_2 \\notin S_0 \\implies \\textbf{not initial}.\n$$\n\n**Step 4: Apply the maximal test** (cannot be extended).\n\nFor $\\sigma_1 = s_0, s_1, s_2$ (finite):\n- Does $s_2$ have any outgoing transition? Yes: $(s_2, s_2) \\in R$.\n- Therefore, $\\sigma_1$ **can be extended** to $s_0, s_1, s_2, s_2, \\dots$\n- Hence, $\\sigma_1$ is **not maximal**.\n\nFor $\\sigma_2 = s_0, s_1, s_3, s_4$ (finite):\n- Does $s_4$ have any outgoing transition? **No** — there is no pair $(s_4, x)$ in $R$.\n- Therefore, $\\sigma_2$ **cannot be extended**.\n- Hence, $\\sigma_2$ is **maximal**.\n\nFor $\\sigma_3 = s_1, s_2, s_2, \\dots$ (infinite):\n- By definition, an infinite fragment cannot be extended.\n- Hence, $\\sigma_3$ is **maximal**.\n\n**Step 5: Combine to identify paths**.\n\n| Sequence | Execution Fragment? | Initial? | Maximal? | Path? |\n|----------|---------------------|----------|----------|-------|\n| $\\sigma_1 = s_0, s_1, s_2$ | Yes | Yes | No | No (prefix of a path) |\n| $\\sigma_2 = s_0, s_1, s_3, s_4$ | Yes | Yes | Yes | **Yes (a path)** |\n| $\\sigma_3 = s_1, s_2, s_2, \\dots$ | Yes | Yes | Yes | **Yes (a path)** |\n| $\\sigma_4 = s_0, s_1, s_2, s_2, s_3$ | **No** | — | — | No |\n| $\\sigma_5 = s_2, s_3, s_4$ | Yes | No | Yes | No |\n\n## 3.2 Derived Property: Uniqueness of Maximal Extension\n\n**Claim:** Every initial execution fragment can be extended to *at least one* maximal initial execution fragment (provided $S$ is finite and $R$ is total, or $TS$ is finite-state).\n\n**Derivation (Sketch):**\n\nConsider an initial execution fragment $\\sigma = s_0, s_1, \\dots, s_n$ that is **not** maximal. By definition, it is not maximal only if there exists some $s_{n+1}$ such that $(s_n, s_{n+1}) \\in R$. We append $s_{n+1}$ to obtain:\n\n$$\n\\sigma' = s_0, s_1, \\dots, s_n, s_{n+1}\n$$\n\nWe repeat this extension step. Two outcomes are possible:\n1. The extension process **continues forever** (because $R$ is total at every reached state) → we obtain an infinite sequence, which is maximal.\n2. The extension process **reaches a state $s_m$ with no outgoing transition** → we obtain a finite sequence ending in a deadlock state, which is maximal.\n\nIn a finite state space, since the sequence cannot revisit a state infinitely often without eventually exceeding the finite bound, the process is well-defined and terminates in one of the two outcomes. $\\blacksquare$\n\n> [!NOTE]\n> **Engineering Significance of This Derivation:**\n> This theorem is what makes **explicit-state model checking** algorithms (like the one in SPIN/NuSMV) work. The algorithm must, given a reachable state, be able to compute *some* path through it in order to evaluate an LTL formula. The existence of maximal extensions guarantees that this search always terminates (in finite state spaces) with a well-defined truth value.\n\n## 3.3 Relating Paths to Suffixes and Prefixes (Operational Toolset)\n\nGiven a path $\\pi = s_0, s_1, s_2, \\dots$, the following derived objects are used repeatedly in LTL semantics:\n\n$$\n\\pi^k = s_k, s_{k+1}, s_{k+2}, \\dots \\quad \\text{(the } k\\text{-suffix)}\n$$\n\n$$\n\\pi^{\\leq k} = s_0, s_1, \\dots, s_k \\quad \\text{(the } k\\text{-prefix)}\n$$\n\n**Important fact:** Every suffix $\\pi^k$ of a path is itself a *maximal* execution fragment (because maximality is preserved under suffixing — an infinite tail remains infinite, and a finite tail ending in deadlock remains deadlock-ending).\n\nHowever, $\\pi^k$ is **initial** only when $k = 0$. For $k > 0$, $\\pi^k$ is maximal but not initial. Such objects are called **paths through state $s_k$** and are critical in defining the CTL \"exists-next\" operator $\\exists \\bigcirc$:\n\n$$\ns \\models \\exists \\bigcirc \\varphi \\iff \\exists\\, \\pi^k \\text{ starting at } s : \\pi^{k+1} \\models \\varphi\n$$\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Kripke Structure (Mermaid State Diagram)\n\nThe diagram below visualizes a sample Kripke Structure. Initial states are drawn with a double circle. Self-loops indicate stuttering behavior.\n\n```mermaid\nstateDiagram-v2\n    [*] --> stateA\n    stateA --> stateB\n    stateA --> stateC\n    stateB --> stateB\n    stateB --> stateD\n    stateC --> stateE\n    stateC --> stateF\n    stateD --> stateG\n    stateE --> stateE\n    stateF --> stateF\n    stateG --> stateH\n    stateH --> [*]\n\n    stateA : S0 INITIAL\n    stateB : S1\n    stateC : S2 INITIAL\n    stateD : S3\n    stateE : S4 LOOP\n    stateF : S5 LOOP\n    stateG : S6\n    stateH : S7 TERMINAL\n```\n\n**Diagram Reading Guide (for KTU Answers):**\n\n- **Double-entry from `[\\\\*]`** marks initial states: `stateA` and `stateC`.\n- **Self-loops** on `stateB`, `stateE`, `stateF` are **stuttering transitions** — they allow infinite fragments without leaving the state.\n- **State `stateH`** is a **terminal (deadlock) state** — no outgoing transition. Any maximal fragment that reaches it must end there.\n- **Every other state** has at least one outgoing transition, ensuring that any maximal fragment not reaching `stateH` is infinite.\n\n## 4.2 Classification Tree: All Possible Execution Fragments\n\nThe following Mermaid flowchart acts as a **decision tree** for classifying any given sequence of states. This is the algorithm a student should mentally execute when solving a KTU Part B question.\n\n```mermaid\nflowchart TD\n    startSequence([Given sequence sigma]) --> checkExecution{Is every adjacent pair in R?}\n    checkExecution -- No --> notFragment[Not an execution fragment]\n    checkExecution -- Yes --> isFragment[Valid execution fragment]\n    isFragment --> checkInitial{Is s0 in S0?}\n    checkInitial -- No --> nonInitial[Non-initial fragment]\n    checkInitial -- Yes --> isInitial[Initial execution fragment]\n    isInitial --> checkMaximal{Can sigma be extended?}\n    checkMaximal -- Yes, finite and extendable --> notMaximal[Initial but NOT maximal]\n    checkMaximal -- No, infinite OR deadlocked --> isMaximal[Maximal execution fragment]\n    isMaximal --> isPath[PATH: Maximal AND Initial]\n    isPath --> pathUse{Used for...}\n    pathUse --> ltlCheck[LTL formula evaluation]\n    pathUse --> ctlCheck[CTL quantifier domain]\n    pathUse --> witnessGen[Counterexample generation]\n\n    notMaximal --> extension[Can be extended to a path]\n    nonInitial --> nonInitUse[Used for sub-system analysis]\n    notFragment --> reject[Reject the sequence]\n```\n\n## 4.3 Block-Level Functional Architecture: How a Model Checker Uses Paths\n\nThe diagram below illustrates how a model checking engine (e.g., NuSMV, SPIN) consumes and produces maximal initial execution fragments. This is a **functional architecture block diagram** rather than a physical schematic.\n\n```mermaid\nflowchart LR\n    inputSys[Kripke Structure TS equals S S0 R L] --> bmcEngine[Explicit State Explorer]\n    bmcEngine --> reachability{Reachable from S0?}\n    reachability -- No --> pruneState[Prune unreachable state]\n    reachability -- Yes --> fragmentBuilder[Fragment Builder]\n    fragmentBuilder --> extendLoop[Extend until maximal]\n    extendLoop --> sinkCheck{Terminal state hit?}\n    sinkCheck -- Yes --> finitePath[Finite maximal path ends in deadlock]\n    sinkCheck -- No --> cycleCheck{Cycle detected?}\n    cycleCheck -- Yes --> infinitePath[Infinite maximal path loops forever]\n    cycleCheck -- No --> extendLoop\n    finitePath --> pathPool[Path Pool Paths of TS]\n    infinitePath --> pathPool\n    pathPool --> ltlEvaluator[LTL / CTL Evaluator]\n    ltlEvaluator --> verdict{All paths satisfy property?}\n    verdict -- Yes --> safeOutput[SYSTEM IS SAFE]\n    verdict -- No --> counterEx[Output offending path as counterexample]\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. **[KTU University Exam – Dec 2023, Model Paper]**\n**Define the following terms with respect to a Kripke Structure $TS = (S, S_0, R, L)$:**\n**(a)** Execution fragment\n**(b)** Initial execution fragment\n**(c)** Maximal execution fragment\n\n#### Model Answer (3 marks — 1 mark per clean definition):\n\n**(a) Execution Fragment:** A sequence of states $\\sigma = s_0, s_1, s_2, \\dots$ such that for every $i \\geq 0$, the transition $(s_i, s_{i+1}) \\in R$. The sequence may be finite or infinite.\n\n**(b) Initial Execution Fragment:** An execution fragment $\\sigma$ whose first state belongs to the set of initial states, i.e., $s_0 \\in S_0$. This represents a run of the system starting from power-on.\n\n**(c) Maximal Execution Fragment:** An execution fragment that cannot be extended further. Formally, $\\sigma$ is maximal if it is infinite, or if it is finite ending in a state $s_n$ for which no $s'$ exists with $(s_n, s') \\in R$ (i.e., $s_n$ is a deadlock/terminal state).\n\n> [!WARNING]\n> **Examiner's Pitfall Callout:**\n> Students often confuse \"maximal\" with \"longest possible.\" A maximal fragment is **not** the longest — it is the **un-extendable** one. If a transition system has cycles, the longest fragment is infinite, and *every* infinite fragment in such a system is maximal.\n\n---\n\n### Q2. **[KTU University Exam – June 2024, Supplementary]**\n**What is a *path* in the context of model checking? Why is maximality an essential requirement for the semantic object of LTL?**\n\n#### Model Answer (3 marks):\n\nA **path** in a Kripke Structure is a **maximal initial execution fragment** — that is, a sequence $\\pi = s_0, s_1, s_2, \\dots$ such that $s_0 \\in S_0$ (initial) and $\\pi$ cannot be extended (maximal).\n\n**Why maximality is essential for LTL semantics:**\n\nLTL formulas are evaluated over paths, and the semantics requires the path to represent the **complete, un-interrupted** behavior of the system. If we allowed non-maximal fragments, a system could \"cheat\" by truncating its execution right before a violation occurs. Maximality ensures that we are reasoning over the *full* behavior — including infinite loops and deadlocks — so that liveness properties (e.g., \"eventually $p$\") receive their true truth value.\n\n---\n\n## Part B — Long Answer Questions (14 Marks Each)\n\n> **KTU ESE Module 1 — Internal Choice.** Students answer **either** Question A **or** Question B.\n\n---\n\n### Question A (14 Marks) — *[KTU University Exam — Dec 2023]*\n\n**(a)** Consider the Kripke Structure defined as:\n- $S = \\{a, b, c, d, e\\}$\n- $S_0 = \\{a\\}$\n- $R = \\{(a, b), (a, c), (b, d), (c, d), (d, e), (e, e)\\}$\n\nFor each of the following sequences, determine whether it is (i) a valid execution fragment, (ii) an initial execution fragment, (iii) a maximal execution fragment, and (iv) a path. Justify each classification. **(7 marks)**\n\n1. $\\sigma_1 = a, b, d, e, e, e, \\dots$\n2. $\\sigma_2 = a, c, d, e$\n3. $\\sigma_3 = b, d, e, e, \\dots$\n4. $\\sigma_4 = a, b, c$\n\n**(b)** Prove that in a Kripke Structure with a finite state space and a total transition relation $R$, every initial execution fragment can be extended to a maximal initial execution fragment. **(7 marks)**\n\n---\n\n#### Model Solution for Q.A:\n\n**(a) Classification of each sequence:**\n\n**Sequence 1:** $\\sigma_1 = a, b, d, e, e, e, \\dots$\n\n- **Execution fragment?** Check each adjacent pair: $(a,b) \\in R$ ✓, $(b,d) \\in R$ ✓, $(d,e) \\in R$ ✓, $(e,e) \\in R$ ✓. → **Yes, valid execution fragment.**\n- **Initial?** First state is $a$, and $a \\in S_0$ → **Yes, initial.**\n- **Maximal?** The sequence is infinite (it loops forever on $e$ via the self-loop). → **Yes, maximal.**\n- **Path?** Maximal AND initial → **Yes, this is a path.** ✓\n\n**Sequence 2:** $\\sigma_2 = a, c, d, e$\n\n- **Execution fragment?** $(a,c) \\in R$ ✓, $(c,d) \\in R$ ✓, $(d,e) \\in R$ ✓. → **Yes, valid.**\n- **Initial?** $a \\in S_0$ → **Yes.**\n- **Maximal?** Finite sequence ending at $e$. Does $e$ have an outgoing transition? Yes, $(e,e) \\in R$. Therefore, the sequence **can be extended** to $\\sigma_1$. → **No, not maximal.**\n- **Path?** Not maximal → **No, this is a path *prefix*.**\n\n**Sequence 3:** $\\sigma_3 = b, d, e, e, \\dots$\n\n- **Execution fragment?** $(b,d) \\in R$ ✓, $(d,e) \\in R$ ✓, $(e,e) \\in R$ ✓. → **Yes.**\n- **Initial?** First state is $b$. Is $b \\in S_0$? No, $S_0 = \\{a\\}$. → **No, not initial.**\n- **Maximal?** Infinite → **Yes.**\n- **Path?** Maximal but not initial → **No, this is a non-initial maximal fragment.**\n\n**Sequence 4:** $\\sigma_4 = a, b, c$\n\n- **Execution fragment?** $(a,b) \\in R$ ✓, but $(b,c) \\notin R$ (the only outgoing edge from $b$ is to $d$). → **No, not an execution fragment.**\n- Initial: Irrelevant (not a fragment).\n- Maximal: Irrelevant.\n- Path: **No.**\n\n**(b) Proof of the Maximal Extension Theorem:**\n\n**Theorem:** Let $TS = (S, S_0, R, L)$ be a Kripke Structure with $S$ finite and $R$ total. Then every initial execution fragment of $TS$ can be extended to at least one maximal initial execution fragment.\n\n**Proof:**\n\nLet $\\sigma = s_0, s_1, \\dots, s_n$ be an initial execution fragment. We construct an extension via the following iterative procedure:\n\n- **Base step:** $\\sigma$ is initial, so $s_0 \\in S_0$. ✓\n- **Inductive step:** If the current fragment ends at $s_k$ and $s_k$ is not a terminal state (i.e., $\\exists s' \\in S$ with $(s_k, s') \\in R$), append any such $s'$ to the fragment.\n- **Termination argument:** Since $R$ is total, every state in a finite space has at least one outgoing transition. So either:\n  - (i) The process never terminates → the resulting sequence is **infinite** and therefore maximal.\n  - (ii) The process terminates at some state $s_m$ with no outgoing transition (i.e., $R$ is not total at $s_m$). Since $S$ is finite and the sequence is constructed by appending states from $S$, case (ii) can only occur if the construction reaches a state with no successors.\n\nIn case (i), the resulting infinite sequence is a maximal initial execution fragment by definition.\n\nIn case (ii), the finite sequence ending at the terminal state $s_m$ cannot be extended, hence it is maximal by definition. Initiality is preserved because we only append states after the initial state $s_0$.\n\nIn both cases, we have constructed a maximal initial execution fragment extending $\\sigma$. $\\blacksquare$\n\n> [!WARNING]\n> **Examiner's Pitfall Callout (Part B):**\n> The most common mistake is to **omit the finite-state assumption**. The theorem *fails* in infinite state spaces — for example, a system that always increments a counter has no maximal initial fragment ending in a terminal state, and the construction never terminates. Always state the **finite state + total relation** precondition explicitly in your proof.\n\n---\n\n### Question B (14 Marks) — *[KTU University Exam — June 2024]*\n\n**(a)** Define a Kripke Structure. With a suitable example, illustrate the concepts of: execution fragment, initial execution fragment, maximal execution fragment, and path. **(7 marks)**\n\n**(b)** Explain why a maximal initial execution fragment is the correct semantic object for evaluating LTL formulas. Discuss what happens, semantically, when the maximal fragment is finite and ends in a deadlock state. **(7 marks)**\n\n---\n\n#### Model Solution for Q.B:\n\n**(a) Definition with Example:**\n\n**Definition:** A Kripke Structure over a set of atomic propositions $AP$ is a 4-tuple $TS = (S, S_0, R, L)$ where:\n- $S$ is a finite set of states,\n- $S_0 \\subseteq S$ is the non-empty set of initial states,\n- $R \\subseteq S \\times S$ is the transition relation,\n- $L: S \\to 2^{AP}$ is the labeling function.\n\n**Example:** Consider a simple mutual-exclusion protocol with two processes, each having a \"trying\", \"critical\", or \"idle\" status. Let:\n- $S = \\{s_0, s_1, s_2, s_3, s_4\\}$\n- $S_0 = \\{s_0\\}$\n- $R = \\{(s_0, s_1), (s_1, s_2), (s_1, s_3), (s_2, s_4), (s_3, s_4), (s_4, s_4)\\}$\n- $L(s_0) = \\emptyset$, $L(s_1) = \\{p\\}$, $L(s_2) = \\{q\\}$, $L(s_3) = \\{\\lnot q\\}$, $L(s_4) = \\{\\text{deadlock}\\}$\n\n**Illustration of concepts:**\n\n- **Execution fragment:** $\\sigma_1 = s_0, s_1, s_2, s_4$ (all consecutive pairs are in $R$).\n- **Initial execution fragment:** Any fragment starting at $s_0$. Example: $\\sigma_2 = s_0, s_1, s_3, s_4$.\n- **Maximal execution fragment:** $\\sigma_3 = s_0, s_1, s_2, s_4, s_4, s_4, \\dots$ (infinite via the self-loop on $s_4$, hence un-extendable).\n- **Path:** Both $\\sigma_3$ above, and any infinite initial fragment, qualify as paths. The prefix $\\sigma_1$ is **not** a path because it can be extended to $\\sigma_3$.\n\n**(b) Why Maximal Initial Execution Fragments are the Semantic Objects of LTL:**\n\nLTL formulas describe properties of *traces* of system behavior. To assign a definitive truth value to a formula like $\\mathbf{G}\\, p$ (\"globally $p$\") or $\\mathbf{F}\\, q$ (\"eventually $q$\"), we must commit to a *complete* trace — one that is not artificially cut short.\n\n**Why maximality is required:**\nIf we allowed non-maximal fragments, a system could \"evade\" a violation by stopping just before the bad state is reached. Maximality forces us to consider the *full* behavior, including infinite loops and deadlocks. The standard LTL semantics defines:\n\n$$\nTS \\models \\varphi \\iff \\forall \\pi \\in \\text{Paths}(TS) : \\pi \\models \\varphi\n$$\n\nwhere $\\text{Paths}(TS)$ is the set of all maximal initial execution fragments.\n\n**Why initiality is required:**\nInitiality restricts attention to behaviors *reachable from the system's startup*. Properties of the system should describe what happens when we *turn the system on*, not what could happen in arbitrary (possibly unreachable) regions of the state space.\n\n**Semantic handling of finite maximal fragments ending in deadlock:**\nWhen a maximal fragment is finite and ends in a deadlock state $s_n$ (no outgoing transitions), standard LTL semantics treats the fragment as if it is *implicitly extended* by infinite stuttering at $s_n$. That is:\n\n$$\ns_n, s_n, s_n, s_n, \\dots\n$$\n\nThis convention ensures that *every* maximal fragment — finite or infinite — can host an LTL truth value. Under this convention:\n- $\\mathbf{G}\\, p$ evaluated at $s_n$ is true iff $p$ holds in $s_n$ (because $p$ will hold forever via stuttering).\n- $\\mathbf{F}\\, q$ evaluated at $s_n$ is true iff $q$ holds at $s_n$ (because the \"eventual\" event is satisfied immediately upon reaching the deadlock).\n\nThis stuttering-extension convention is **not** part of the basic LTL syntax but is a well-established semantic convention used by tools like SPIN and NuSMV.\n\n> [!WARNING]\n> **Examiner's Pitfall Callout (Part B):**\n> A common valuation deduction: students often claim that a finite maximal fragment \"violates\" liveness properties. This is wrong — by the stuttering-extension convention, a finite maximal fragment satisfies $\\mathbf{F}\\, p$ for any $p$ that holds at its terminal state. To get an actual liveness violation, you need a **deadlock-free** system or one with a cycle that **never** visits a state satisfying $p$.\n\n---\n\n## Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Revision Checklist:**\n\n- **Kripke Structure** = $(S, S_0, R, L)$. State this tuple explicitly whenever you begin a verification problem.\n- **Execution Fragment** = sequence of states where every consecutive pair is in $R$. Finite or infinite.\n- **Initial Execution Fragment** = execution fragment with $s_0 \\in S_0$.\n- **Maximal Execution Fragment** = cannot be extended. Either infinite, or finite ending in a state with no outgoing transition.\n- **Path** = Maximal **AND** Initial execution fragment. The canonical object for LTL evaluation.\n- **Total $R$** → every state has an outgoing transition → every maximal fragment is infinite.\n- **Non-total $R$** → some states are terminal/deadlock → finite maximal fragments exist.\n- **Finite maximal fragments** are semantically extended by infinite stuttering at the terminal state for LTL evaluation.\n- **Suffix of a path** = maximal but not initial (for $k > 0$). Used heavily in LTL fixpoint semantics.\n- **Prefix of a path** = initial but not maximal. Used in bounded model checking.\n- **Theorem to remember:** In a finite Kripke Structure, every initial fragment extends to a maximal initial fragment. (Requires the finite state assumption — *do not omit this in proofs!*)\n- **Common classification table** — Memorize the 2×2 matrix of (initial? × maximal?) to instantly classify any given sequence in an exam.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t10",
          "title": "Execution",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Execution\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of Execution (in Automated Verification)\n\nIn the formal semantics of programming languages and in **automated verification**, an **Execution** of a system (or program) is formally defined as a (possibly infinite) sequence of state transitions induced by the operational semantics of the system, starting from an initial configuration and proceeding according to a transition relation.\n\nMathematically, an execution is a sequence:\n\n$$ \\sigma = s_0 \\xrightarrow{\\alpha_0} s_1 \\xrightarrow{\\alpha_1} s_2 \\xrightarrow{\\alpha_2} \\cdots $$\n\nwhere each $s_i$ is a **state (configuration)** of the system, and each $\\alpha_i \\in \\Sigma$ is a **transition label (action)** drawn from a fixed alphabet of observable or internal events.\n\nAn **Execution Trace** (or **path**) is the projection of the execution onto the sequence of states or onto the sequence of labels:\n\n$$ \\pi = s_0, s_1, s_2, \\ldots \\quad \\text{(state trace)} $$\n$$ \\tau = \\alpha_0, \\alpha_1, \\alpha_2, \\ldots \\quad \\text{(action trace)} $$\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Definition (PECNT526 Module 1):**\n> Execution is the process of traversing a **Transition System (Kripke Structure / Labeled Transition System)** by repeatedly applying its transition relation, producing a maximal sequence of states that is the central object of study in model checking, temporal logic, and reachability analysis.\n\n### 1.2 Conceptual Analogy — \"The Conveyor Belt of States\"\n\nImagine a **factory assembly line**:\n\n- The **factory** = your program or system.\n- Each **workstation** the product visits = a **state** $s_i$ (e.g., values of all program variables, program counter location).\n- The **belt moving the product** = the **execution engine / semantics**.\n- The **instruction taped to the product at each station** = the **action / label** $\\alpha_i$ that triggered the move.\n\nOnce you press \"Start\" (i.e., fix an **initial state** $s_0$), the belt deterministically or non-deterministically carries the product through stations, and the **sequence of stations visited is the execution**. If the belt never stops, the execution is **infinite** — which is the typical case for reactive / concurrent systems in verification.\n\n> [!NOTE]\n> **Why \"Execution\" matters in Automated Verification:**\n> Every property we wish to verify (e.g., \"the system never enters an error state\", \"every request is eventually acknowledged\") is a property of an **execution / trace**. Model checkers like SPIN, NuSMV, and UPPAAL work by **enumerating**, **symbolically exploring**, or **abstracting** the set of all executions of a model.\n\n### 1.3 Key Vocabulary Used Throughout This Topic\n\n| Term | Meaning |\n|---|---|\n| **State / Configuration** | A snapshot of all variables, program counter, and memory of the system at one instant. |\n| **Initial State** | A designated state $s_0$ from which execution begins. |\n| **Transition Relation** | A binary relation $\\rightarrow \\subseteq S \\times S$ (or $S \\times \\Sigma \\times S$) defining legal moves. |\n| **Action / Label** | An observable or internal event $\\alpha \\in \\Sigma$ annotating a transition. |\n| **Step** | A single application of the transition relation: $s \\rightarrow s'$. |\n| **Trace / Path** | The sequence of states (or actions) visited during execution. |\n| **Maximal Execution** | An execution that cannot be extended further (either infinite or ending in a deadlock state). |\n| **Stutter** | Two consecutive identical states: $s \\rightarrow s$. |\n\n### 1.4 Two Paradigms of Execution Semantics\n\nIn formal semantics — and therefore in verification — there are **two principal ways to define what \"executing a program\" means**:\n\n1. **Small-Step Semantics (Operational Semantics)** — also called *transition semantics*. Defines execution *one atomic step at a time* using a transition relation $\\rightarrow$. The standard for model checkers.\n2. **Big-Step Semantics (Natural Semantics)** — also called *relational semantics*. Defines execution *from initial state directly to final state* using a relation $\\Downarrow$. The standard for interpreters and program-provers.\n\nBoth produce the same executions overall, but **small-step semantics is the de-facto framework in automated verification** because it makes intermediate states — where invariants and properties must hold — explicit.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Anatomy of an Execution — A Structured Breakdown\n\nA complete execution of a transition system $TS = (S, S_0, \\rightarrow, L, AP)$ can be decomposed into the following logical building blocks:\n\n1. **State Identification** — A state $s \\in S$ is a tuple that completely describes the system. For a sequential program, $s = \\langle pc, \\sigma \\rangle$ where $pc$ is the program counter and $\\sigma$ is the variable-to-value store.\n\n2. **Initial Configuration** — One or more designated start states $S_0 \\subseteq S$. Every execution must begin in some $s_0 \\in S_0$.\n\n3. **Atomic Step** — An *atomic* (indivisible) action that changes the state. In hardware, a clock edge; in software, a single statement evaluation; in a protocol, a single message exchange.\n\n4. **Transition Rule** — A logical or operational rule that, given a state, determines:\n   - Whether the system can take a step.\n   - If yes, what the successor state is.\n   - In the labeled case, what label $\\alpha$ annotates the step.\n\n5. **Path Construction** — Successive application of the transition rule, yielding a (possibly infinite) sequence of states.\n\n6. **Termination / Non-termination** — The execution **terminates** if it reaches a state with no outgoing transitions (a *deadlock* or *halt* state). It is **non-terminating** (a *divergence*) if it can produce an infinite path.\n\n> [!TIP]\n> **\"Why\" and \"How\" each piece matters:**\n> - State identification is critical because **two executions are equal iff they pass through the same sequence of states**.\n> - The transition relation is the *only* input to a model checker; everything else is computed.\n> - Labeling with atomic propositions $AP$ is what lets temporal-logic formulas like $G\\,(req \\Rightarrow F\\,ack)$ be evaluated on executions.\n\n## 2.2 Deterministic vs. Non-Deterministic Execution\n\nA transition system is **deterministic** if for every state $s$ and every action $\\alpha$, there is **at most one** successor state. Formally:\n\n$$ \\forall s \\in S,\\ \\forall \\alpha \\in \\Sigma :\\ \\vert \\{ s' \\mid s \\xrightarrow{\\alpha} s' \\} \\vert \\le 1 $$\n\nOtherwise, the system is **non-deterministic**: at least one state has two or more $\\alpha$-successors.\n\n> [!NOTE]\n> Non-determinism is the *mathematical model* for concurrency, abstraction (data non-determinism), and underspecification. Every real concurrent program yields a non-deterministic model.\n\n## 2.3 KTU High-Yield Formula / Concept Sheet\n\n| # | Concept | Symbol / Equation | Meaning / Use |\n|---|---|---|---|\n| 1 | Transition System | $TS = (S, S_0, \\Sigma, \\rightarrow, AP, L)$ | The formal model whose executions are studied. |\n| 2 | Transition Relation | $\\rightarrow \\subseteq S \\times \\Sigma \\times S$ | Set of all legal atomic steps. |\n| 3 | Direct Successor | $Post(s) = \\{ s' \\mid \\exists \\alpha: s \\xrightarrow{\\alpha} s' \\}$ | All states reachable from $s$ in one step. |\n| 4 | Direct Predecessor | $Pre(s) = \\{ s' \\mid \\exists \\alpha: s' \\xrightarrow{\\alpha} s \\}$ | All states that can reach $s$ in one step. |\n| 5 | Execution | $\\pi = s_0 \\xrightarrow{\\alpha_0} s_1 \\xrightarrow{\\alpha_1} \\ldots$ | A (possibly infinite) path through the system. |\n| 6 | Trace | $tr(\\pi) = \\alpha_0\\, \\alpha_1\\, \\alpha_2\\, \\ldots$ | Sequence of observable actions. |\n| 7 | Reachable States | $Reach(s_0) = \\bigcup_{n \\ge 0} Post^n(s_0)$ | All states ever visited. |\n| 8 | State Count Bound | $\\vert Reach \\vert \\le \\vert S \\vert$ | Foundation of finite-state model checking. |\n| 9 | Big-Step Eval | $\\langle c, \\sigma \\rangle \\Downarrow \\sigma'$ | Whole-program evaluation result. |\n| 10 | Small-Step Step | $\\langle c, \\sigma \\rangle \\rightarrow \\langle c', \\sigma' \\rangle$ | Single-step reduction. |\n| 11 | Stutter Equivalence | $\\pi_1 \\approx_s \\pi_2$ | Two executions equivalent up to finite repetition of identical states. |\n| 12 | Enabledness | $enabled(s) = \\Sigma \\setminus \\{ \\alpha \\mid \\nexists s': s \\xrightarrow{\\alpha} s' \\}$ | Actions available at state $s$. |\n| 13 | Deadlock | $Post(s) = \\emptyset$ | A state with no outgoing transitions — an execution terminates here. |\n| 14 | Maximal Path Length | $0, 1, 2, \\ldots, \\omega$ | Finite, infinite, or $\\omega$-length executions. |\n| 15 | State Formula (at $s_i$) | $s_i \\models \\varphi$ | A proposition true at state $s_i$. |\n\n> [!IMPORTANT]\n> **Engineering Utility in KTU Context:**\n> Reachability computation $Reach(s_0)$ is the algorithmic backbone of:\n> - **CTL / LTL model checking** (fix-point computation).\n> - **Safety analysis** (\"can the system ever reach an error state?\").\n> - **Bug hunting** (\"is there ANY execution on which property $P$ is violated?\").\n> Tools like **SPIN**, **NuSMV**, **UPPAAL**, and **CBMC** all internally implement variants of $Post$ / $Pre$ to enumerate executions.\n\n## 2.4 Classifying Executions by Termination Behavior\n\n- **Finite (terminating) execution:** length $n \\in \\mathbb{N}$, ends in a state with no successors.\n- **Divergent execution:** an *infinite* execution that stays within a finite strongly-connected component (a \"loop\").\n- **Lasso execution (lollipop):** a finite prefix leading into a cycle: $s_0 \\rightarrow^* s_c \\rightarrow^+ s_c$. Critical for CTL* model checking.\n- **Failing execution:** a finite execution that ends in a designated *error / bad* state — the target of every safety verifier.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: From Operational Rules to the Transition Relation\n\n> This is the central derivation you must be able to reproduce for KTU Module 1.\n\nWe start with a toy **While-language** whose abstract syntax is:\n\n$$ c \\in Com ::= \\text{skip} \\mid x := e \\mid c_1; c_2 \\mid \\text{if } b \\text{ then } c_1 \\text{ else } c_2 \\mid \\text{while } b \\text{ do } c $$\n\nA *configuration* is a pair $\\langle c, \\sigma \\rangle$, where $c$ is the command being executed and $\\sigma$ is a store (a function $Var \\rightarrow \\mathbb{Z}$).\n\n### Step 1 — Define the semantic objects\n\nA **state of the program** is the store $\\sigma$ together with the program counter $pc$. We model this as the configuration $\\langle c, \\sigma \\rangle$ since $c$ uniquely determines $pc$ structurally.\n\n### Step 2 — Introduce the small-step relation\n\nWe define $\\langle c, \\sigma \\rangle \\rightarrow \\langle c', \\sigma' \\rangle$ meaning: *in one atomic step, configuration $\\langle c, \\sigma \\rangle$ reduces to $\\langle c', \\sigma' \\rangle$*.\n\n### Step 3 — Write the structural operational semantics (SOS) rules\n\n$$\n\\begin{aligned}\n&\\textbf{[SKIP]}    & \\frac{}{\\langle \\text{skip}, \\sigma \\rangle \\rightarrow \\langle \\epsilon, \\sigma \\rangle} \\\\[4pt]\n&\\textbf{[ASSIGN]}  & \\frac{}{\\langle x := e, \\sigma \\rangle \\rightarrow \\langle \\epsilon, \\sigma[x \\mapsto \\llbracket e \\rrbracket \\sigma] \\rangle} \\\\[4pt]\n&\\textbf{[SEQ-1]}   & \\frac{\\langle c_1, \\sigma \\rangle \\rightarrow \\langle c_1', \\sigma' \\rangle}{\\langle c_1; c_2, \\sigma \\rangle \\rightarrow \\langle c_1'; c_2, \\sigma' \\rangle} \\\\[4pt]\n&\\textbf{[SEQ-2]}   & \\frac{}{\\langle \\text{skip}; c_2, \\sigma \\rangle \\rightarrow \\langle c_2, \\sigma \\rangle} \\\\[4pt]\n&\\textbf{[IF-T]}    & \\frac{\\llbracket b \\rrbracket \\sigma = \\mathbf{tt}}{\\langle \\text{if } b \\text{ then } c_1 \\text{ else } c_2, \\sigma \\rangle \\rightarrow \\langle c_1, \\sigma \\rangle} \\\\[4pt]\n&\\textbf{[IF-F]}    & \\frac{\\llbracket b \\rrbracket \\sigma = \\mathbf{ff}}{\\langle \\text{if } b \\text{ then } c_1 \\text{ else } c_2, \\sigma \\rangle \\rightarrow \\langle c_2, \\sigma \\rangle} \\\\[4pt]\n&\\textbf{[WHILE-T]} & \\frac{\\llbracket b \\rrbracket \\sigma = \\mathbf{tt}}{\\langle \\text{while } b \\text{ do } c, \\sigma \\rangle \\rightarrow \\langle c; \\text{while } b \\text{ do } c, \\sigma \\rangle} \\\\[4pt]\n&\\textbf{[WHILE-F]} & \\frac{\\llbracket b \\rrbracket \\sigma = \\mathbf{ff}}{\\langle \\text{while } b \\text{ do } c, \\sigma \\rangle \\rightarrow \\langle \\text{skip}, \\sigma \\rangle}\n\\end{aligned}\n$$\n\n**Explanation of each line:**\n- **[SKIP]**: skip takes one step to the empty program $\\epsilon$ without changing the store.\n- **[ASSIGN]**: $x := e$ takes one step to $\\epsilon$ and updates the store so that $x$ maps to the value of $e$ in $\\sigma$.\n- **[SEQ-1]**: a sequence steps by stepping its first component; the second is carried along.\n- **[SEQ-2]**: when the first component finishes, control passes to the second.\n- **[IF-T/F]**: the guard $b$ is evaluated in the current store; one of the branches is taken in **one** step.\n- **[WHILE-T/F]**: when the guard is true, the loop body is executed followed by the loop itself; when false, the loop exits in one step.\n\n### Step 4 — Define the big-step relation\n\nA whole-program big-step evaluation $\\langle c, \\sigma \\rangle \\Downarrow \\sigma'$ is defined coinductively / inductively. The crucial rules are:\n\n$$\n\\begin{aligned}\n&\\textbf{[BS-ASSIGN]} & \\frac{}{\\langle x := e, \\sigma \\rangle \\Downarrow \\sigma[x \\mapsto \\llbracket e \\rrbracket \\sigma]} \\\\[4pt]\n&\\textbf{[BS-SEQ]}    & \\frac{\\langle c_1, \\sigma \\rangle \\Downarrow \\sigma_1 \\quad \\langle c_2, \\sigma_1 \\rangle \\Downarrow \\sigma_2}{\\langle c_1; c_2, \\sigma \\rangle \\Downarrow \\sigma_2} \\\\[4pt]\n&\\textbf{[BS-IF-TT]}  & \\frac{\\llbracket b \\rrbracket \\sigma = \\mathbf{tt} \\quad \\langle c_1, \\sigma \\rangle \\Downarrow \\sigma'}{\\langle \\text{if } b \\text{ then } c_1 \\text{ else } c_2, \\sigma \\rangle \\Downarrow \\sigma'} \\\\[4pt]\n&\\textbf{[BS-WHILE-FF]}& \\frac{\\llbracket b \\rrbracket \\sigma = \\mathbf{ff}}{\\langle \\text{while } b \\text{ do } c, \\sigma \\rangle \\Downarrow \\sigma}\n\\end{aligned}\n$$\n\n### Step 5 — Equivalence Theorem (often asked in KTU)\n\n> **Theorem (Small-step $\\equiv$ Big-step).** For every command $c$ and stores $\\sigma, \\sigma'$:\n> $$ \\langle c, \\sigma \\rangle \\Downarrow \\sigma' \\iff \\langle c, \\sigma \\rangle \\rightarrow^* \\langle \\epsilon, \\sigma' \\rangle $$\n\nThat is, the big-step result equals the store reached when the small-step execution terminates at the empty program.\n\n## 3.2 Analytical Matrix: Examples of Executions\n\n| Program $c$ | Initial $\\sigma$ | First Step $\\rightarrow$ | Resulting $\\sigma'$ |\n|---|---|---|---|\n| $x := 1$ | $\\{x \\mapsto 0\\}$ | $\\langle x:=1, \\sigma \\rangle \\rightarrow \\langle \\epsilon, \\sigma[x \\mapsto 1] \\rangle$ | $\\{x \\mapsto 1\\}$ |\n| $x := 1;\\, y := x$ | $\\{x \\mapsto 0,\\, y \\mapsto 0\\}$ | 2-step sequence to $\\epsilon$ | $\\{x \\mapsto 1,\\, y \\mapsto 1\\}$ |\n| `if true then x:=1 else x:=2` | $\\{x \\mapsto 0\\}$ | applies [IF-T] | $\\sigma[x \\mapsto 1]$ |\n| `while false do x:=1` | $\\{x \\mapsto 0\\}$ | applies [WHILE-F] | $\\{x \\mapsto 0\\}$ (terminates in 1 step) |\n| `while true do x:=1` | $\\{x \\mapsto 0\\}$ | infinite loop | divergent execution |\n\n## 3.3 Reachability Fixpoint — Foundational Algorithm\n\nThe set of states reachable from $S_0$ is the **least fixpoint** of the post-image operator $Post$:\n\n$$\n\\begin{aligned}\n& R_0 = S_0 \\\\\n& R_{i+1} = R_i \\cup Post(R_i) \\\\\n& Reach = \\bigcup_{i \\ge 0} R_i\n\\end{aligned}\n$$\n\nSince the system is finite ($S$ finite), the sequence $R_0 \\subseteq R_1 \\subseteq \\ldots$ stabilizes after at most $\\vert S \\vert$ iterations, giving an executable algorithm with time complexity $O(\\vert S \\vert + \\vert \\rightarrow \\vert)$.\n\n> [!IMPORTANT]\n> This Kleene-style fixpoint iteration is the **core algorithm of every model checker**. LTL and CTL model checking reduce to repeated fixpoint computations over $Reach$.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 High-Level Architecture: From Program to Set of Executions\n\n```mermaid\nflowchart LR\n    A[Source Program or System Description] --> B[Parser / Frontend]\n    B --> C[Abstract Syntax Tree AST]\n    C --> D[Semantics Definition: SOS Rules]\n    D --> E[Transition System TS]\n    E --> F[Initial State Set S0]\n    E --> G[Transition Relation R-arrow]\n    E --> H[Labeling Function L over AP]\n    F --> I[Execution Engine: Post / Pre]\n    G --> I\n    H --> I\n    I --> J[Set of All Executions Pi]\n    J --> K[Temporal Logic Model Checker]\n    J --> L[Reachability Analyzer]\n    J --> M[Equivalence Checker]\n    K --> N[Verification Result: SAT or UNSAT]\n    L --> N\n    M --> N\n```\n\n## 4.2 Concrete Execution of a Toy Program\n\nConsider `x := 1; y := x + 1` starting from $\\sigma_0 = \\{x \\mapsto 0,\\, y \\mapsto 0\\}$.\n\n```mermaid\nflowchart LR\n    S0[\"C0: x:=1; y:=x+1 with sigma0 x=0 y=0\"] -->|ASSIGN x:=1| S1[\"C1: skip; y:=x+1 with sigma1 x=1 y=0\"]\n    S1 -->|SEQ-2: skip vanishes| S2[\"C2: y:=x+1 with sigma1 x=1 y=0\"]\n    S2 -->|ASSIGN y:=x+1| S3[\"C3: skip with sigma2 x=1 y=2\"]\n    S3 -->|HALT| SF[\"Final: sigma2 x=1 y=2\"]\n```\n\nEach node $Ci$ is a configuration $\\langle c_i, \\sigma_i \\rangle$. The edges are SOS rules. The **execution is the path** $C0 \\rightarrow C1 \\rightarrow C2 \\rightarrow C3 \\rightarrow$ final.\n\n## 4.3 Reachability Fixpoint Computation Diagram\n\n```mermaid\nflowchart TD\n    R0[\"R0 = S0 initial states\"] -->|Post| R1[\"R1 = R0 union Post R0: 1-step successors\"]\n    R1 -->|Post| R2[\"R2 = R1 union Post R1: 2-step successors\"]\n    R2 -->|Post| R3[\"R3 = R2 union Post R2: 3-step successors\"]\n    R3 -->|Post| R4[\"R4 = R3 ... and so on\"]\n    R4 -->|until fixpoint| Rfix[\"Rfix = Reach S0: union of all Ri\"]\n```\n\n## 4.4 Determinism vs. Non-Determinism — Visual Comparison\n\n```mermaid\nflowchart LR\n    subgraph Deterministic\n        D0[\"s0\"] -->|alpha| D1[\"s1\"]\n        D1 -->|beta| D2[\"s2\"]\n        D2 -->|gamma| D3[\"s3\"]\n    end\n    subgraph NonDeterministic\n        N0[\"s0\"] -->|a| N1a[\"s1 left branch\"]\n        N0 -->|a| N1b[\"s1 right branch\"]\n        N1a -->|b| N2a[\"s2 left\"]\n        N1b -->|b| N2b[\"s2 right\"]\n    end\n```\n\nIn the **deterministic** part, each state-action pair has at most one successor. In the **non-deterministic** part, $s_0$ on action $a$ has two possible successors. A verification tool must explore **both** branches to confirm a property holds on **all** executions.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Question 1 `[KTU University Exam - Dec 2023, Model]`\n**Define an \"execution\" of a transition system. How does it differ from a \"trace\"?**\n\n**Model Answer (3 marks):**\n\nAn **execution** of a transition system $TS = (S, S_0, \\rightarrow, \\Sigma, L, AP)$ is a sequence of the form\n\n$$ \\pi = s_0 \\xrightarrow{\\alpha_0} s_1 \\xrightarrow{\\alpha_1} s_2 \\xrightarrow{\\alpha_2} \\ldots $$\n\nwhere each $s_i \\in S$, $s_0 \\in S_0$, and $(s_i, \\alpha_i, s_{i+1}) \\in \\rightarrow$.\n\nA **trace** is the projection of an execution onto the **sequence of actions** (labels) only: $tr(\\pi) = \\alpha_0\\, \\alpha_1\\, \\alpha_2\\, \\ldots$\n\n**Difference:** an execution records *both* the visited states and the labels annotating each step, whereas a trace records *only* the labels. Two different executions may therefore share the same trace if they differ only in the states visited — this is the basis of **stutter equivalence**. (3 marks)\n\n### Question 2 `[KTU University Exam - June 2024, Model]`\n**What is the difference between small-step and big-step execution semantics?**\n\n**Model Answer (3 marks):**\n\n- **Small-step (operational) semantics** defines execution as a transition relation $\\langle c, \\sigma \\rangle \\rightarrow \\langle c', \\sigma' \\rangle$ producing *one atomic step at a time*. It exposes every intermediate state and is used in **model checking** and **abstract machines**.\n- **Big-step (natural) semantics** defines execution as a relation $\\langle c, \\sigma \\rangle \\Downarrow \\sigma'$ that *directly relates* the initial and final stores, skipping intermediate states. It is used in **interpreters** and **proof assistants**.\n\nBoth yield the same final result for terminating programs; the proof of equivalence relies on showing that the reflexive-transitive closure $\\rightarrow^*$ of the small-step relation equals the big-step relation. (3 marks)\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks Each, Internal Choice)\n\n> **KTU 2024 Scheme ESE Rule (PECNT526):** Each Part B question carries 14 marks and carries an internal choice — i.e., **either (a) OR (b)**. We model this below as **Question A** and **Question B**, both addressing the topic \"Execution\" with a different sub-focus.\n\n### `Question A` (14 Marks) `[KTU University Exam - Dec 2023, Model]`\n\n**(a) [7 marks]** Define a transition system formally. Show, with the help of a structured operational semantics (SOS), how the execution of a `while` program is defined step-by-step. Write down **at least five SOS rules**.\n\n**(b) [7 marks]** Consider the program\n\n```\nx := 0;\nwhile x < 3 do\n    x := x + 1\n```\n\nstarting from the store $\\sigma_0 = \\{x \\mapsto 0\\}$. **Enumerate the complete execution**, listing every configuration $\\langle c, \\sigma \\rangle$ visited, the rule applied at each step, and identify whether the execution terminates. State the final store.\n\n#### Model Solution\n\n**(a) — 7 marks**\n\nA **transition system** is a tuple $TS = (S, S_0, \\Sigma, \\rightarrow, AP, L)$ where:\n- $S$ — non-empty set of states,\n- $S_0 \\subseteq S$ — initial states,\n- $\\Sigma$ — set of actions/labels,\n- $\\rightarrow \\subseteq S \\times \\Sigma \\times S$ — transition relation,\n- $AP$ — set of atomic propositions,\n- $L: S \\rightarrow 2^{AP}$ — labeling function. (2 marks)\n\n**SOS rules for execution** (showing 5 rules, each ~1 mark):\n\n$$\n\\begin{aligned}\n&\\frac{}{\\langle \\text{skip}, \\sigma \\rangle \\rightarrow \\langle \\epsilon, \\sigma \\rangle} \\quad \\text{[SKIP]} \\\\[4pt]\n&\\frac{}{\\langle x := e, \\sigma \\rangle \\rightarrow \\langle \\epsilon, \\sigma[x \\mapsto \\llbracket e \\rrbracket \\sigma] \\rangle} \\quad \\text{[ASSIGN]} \\\\[4pt]\n&\\frac{\\langle c_1, \\sigma \\rangle \\rightarrow \\langle c_1', \\sigma' \\rangle}{\\langle c_1; c_2, \\sigma \\rangle \\rightarrow \\langle c_1'; c_2, \\sigma' \\rangle} \\quad \\text{[SEQ-1]} \\\\[4pt]\n&\\frac{}{\\langle \\text{skip}; c_2, \\sigma \\rangle \\rightarrow \\langle c_2, \\sigma \\rangle} \\quad \\text{[SEQ-2]} \\\\[4pt]\n&\\frac{\\llbracket b \\rrbracket \\sigma = \\mathbf{tt}}{\\langle \\text{while } b \\text{ do } c, \\sigma \\rangle \\rightarrow \\langle c; \\text{while } b \\text{ do } c, \\sigma \\rangle} \\quad \\text{[WHILE-T]}\n\\end{aligned}\n$$\n\nThese rules define **one atomic reduction step**; an execution is the reflexive-transitive closure of these steps. (5 marks)\n\n**(b) — 7 marks**\n\nLet $\\sigma_i$ denote the store after $i$ loop iterations, with $\\sigma_0 = \\{x \\mapsto 0\\}$.\n\n| Step | Configuration $\\langle c, \\sigma \\rangle$ | Rule Applied |\n|---|---|---|\n| 0 | $\\langle x:=0;\\,\\text{while } x<3 \\text{ do } (x:=x+1),\\, \\sigma_0 \\rangle$ | initial |\n| 1 | $\\langle \\text{skip};\\,\\text{while } x<3 \\text{ do } (x:=x+1),\\, \\sigma_0 \\rangle$ | [ASSIGN] |\n| 2 | $\\langle \\text{while } x<3 \\text{ do } (x:=x+1),\\, \\sigma_0 \\rangle$ | [SEQ-2] |\n| 3 | $\\langle (x:=x+1);\\,\\text{while } x<3 \\text{ do } (x:=x+1),\\, \\sigma_0 \\rangle$ | [WHILE-T] (0 < 3) |\n| 4 | $\\langle \\text{skip};\\,\\text{while } x<3 \\text{ do } (x:=x+1),\\, \\sigma_1 \\rangle$, $\\sigma_1 = \\{x \\mapsto 1\\}$ | [ASSIGN] |\n| 5 | $\\langle \\text{while } x<3 \\text{ do } (x:=x+1),\\, \\sigma_1 \\rangle$ | [SEQ-2] |\n| 6 | $\\langle (x:=x+1);\\,\\text{while } x<3 \\text{ do } (x:=x+1),\\, \\sigma_1 \\rangle$ | [WHILE-T] (1 < 3) |\n| 7 | $\\langle \\text{skip};\\,\\text{while } x<3 \\text{ do } (x:=x+1),\\, \\sigma_2 \\rangle$, $\\sigma_2 = \\{x \\mapsto 2\\}$ | [ASSIGN] |\n| 8 | $\\langle \\text{while } x<3 \\text{ do } (x:=x+1),\\, \\sigma_2 \\rangle$ | [SEQ-2] |\n| 9 | $\\langle (x:=x+1);\\,\\text{while } x<3 \\text{ do } (x:=x+1),\\, \\sigma_2 \\rangle$ | [WHILE-T] (2 < 3) |\n| 10 | $\\langle \\text{skip};\\,\\text{while } x<3 \\text{ do } (x:=x+1),\\, \\sigma_3 \\rangle$, $\\sigma_3 = \\{x \\mapsto 3\\}$ | [ASSIGN] |\n| 11 | $\\langle \\text{while } x<3 \\text{ do } (x:=x+1),\\, \\sigma_3 \\rangle$ | [SEQ-2] |\n| 12 | $\\langle \\text{skip}, \\sigma_3 \\rangle$ | [WHILE-F] (3 ≮ 3) |\n| 13 | $\\langle \\epsilon, \\sigma_3 \\rangle$ | [SKIP] — **HALT** |\n\n**Termination:** Yes, the execution terminates at step 13. **Final store:** $\\sigma_3 = \\{x \\mapsto 3\\}$. (7 marks)\n\n---\n\n### `Question B` (14 Marks) `[KTU University Exam - June 2024, Model]`\n\n**(a) [7 marks]** Define $Post(s)$ and $Pre(s)$ operators. Explain how repeated application of $Post$ from the initial state set $S_0$ computes the set of all reachable states, and discuss why this algorithm terminates for finite transition systems.\n\n**(b) [7 marks]** A vending machine has the following transition system (states: $S = \\{s_0, s_1, s_2\\}$, actions $\\Sigma = \\{\\text{coin}, \\text{select}, \\text{dispense}\\}$):\n\n$$ s_0 \\xrightarrow{\\text{coin}} s_1 \\xrightarrow{\\text{select}} s_2 \\xrightarrow{\\text{dispense}} s_0 $$\n\nState the set of maximal executions of this system. Identify the trace language $L(TS)$ (set of all finite traces accepted by the system). Also classify whether the system is deterministic.\n\n#### Model Solution\n\n**(a) — 7 marks**\n\nFor a transition system $TS = (S, S_0, \\rightarrow, \\Sigma)$:\n\n$$ Post(s) = \\{ s' \\in S \\mid \\exists \\alpha \\in \\Sigma: s \\xrightarrow{\\alpha} s' \\} $$\n$$ Pre(s) = \\{ s' \\in S \\mid \\exists \\alpha \\in \\Sigma: s' \\xrightarrow{\\alpha} s \\} $$\n\nThe reachable-state computation is: (3 marks)\n\n$$\n\\begin{aligned}\nR_0 &= S_0 \\\\\nR_{i+1} &= R_i \\cup \\bigcup_{s \\in R_i} Post(s) \\\\\nReach(TS) &= \\bigcup_{i \\ge 0} R_i\n\\end{aligned}\n$$\n\n**Termination argument:** For a finite-state system, $\\vert S \\vert$ is a fixed finite number. The sequence $R_0 \\subseteq R_1 \\subseteq R_2 \\subseteq \\ldots$ is **monotonically increasing** and each $R_i \\subseteq S$. By the finiteness of $S$, the chain can grow at most $\\vert S \\vert$ times before stabilizing, i.e., $R_{k+1} = R_k$ for some $k \\le \\vert S \\vert$. The fixpoint is then $R_k = Reach(TS)$. Worst-case time complexity is $O(\\vert S \\vert \\cdot \\vert \\rightarrow \\vert)$. (4 marks)\n\n**(b) — 7 marks**\n\nThe transition system is the cycle $s_0 \\rightarrow s_1 \\rightarrow s_2 \\rightarrow s_0$. (1 mark)\n\n**Maximal executions:** Every execution of the vending machine is an *infinite repetition* of the cycle. The unique maximal execution (up to cyclic shift) is:\n\n$$ \\pi = (s_0 \\xrightarrow{\\text{coin}} s_1 \\xrightarrow{\\text{select}} s_2 \\xrightarrow{\\text{dispense}})^{\\omega} $$\n\nThat is, the $\\omega$-regular expression $(s_0\\, s_1\\, s_2)^{\\omega}$. (2 marks)\n\n**Trace language** $L(TS)$ — the set of all finite prefixes of the action sequence:\n\n$$ L(TS) = \\{ \\epsilon,\\ \\text{coin},\\ \\text{coin}\\cdot\\text{select},\\ \\text{coin}\\cdot\\text{select}\\cdot\\text{dispense}, $$\n$$ (\\text{coin}\\cdot\\text{select}\\cdot\\text{dispense})^2,\\ \\ldots,\\ (\\text{coin}\\cdot\\text{select}\\cdot\\text{dispense})^n \\mid n \\ge 0 \\} $$\n\nIn other words, $L(TS) = \\{ (\\text{coin}\\cdot\\text{select}\\cdot\\text{dispense})^n \\mid n \\in \\mathbb{N} \\}$. (2 marks)\n\n**Determinism:** Yes, this system is **deterministic** — for every state $s_i$ and every action $\\alpha$, there is **at most one** successor. In particular, $Post(s_i) = \\{s_{i+1 \\bmod 3}\\}$ is a singleton for every state. (2 marks)\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning\n\n> [!WARNING]\n> **Common Pitfalls — Where Students Lose Marks on \"Execution\":**\n> 1. **Confusing trace with execution:** A trace is only the *action sequence*; an execution carries both states and actions. Examiners explicitly test this distinction. Always mention **both** projections in your definition.\n> 2. **Forgetting the initial state condition:** An execution must begin in $S_0$. A path starting from an arbitrary state is just a \"path\", not a valid \"execution\" in KTU terminology.\n> 3. **Mixing up small-step and big-step notation:** Using $\\Downarrow$ when the question wants $\\rightarrow$ (or vice versa) is a 1-mark deduction at minimum. Be precise.\n> 4. **Omitting intermediate configurations in trace enumerations:** When asked to \"list the execution\" of a program, every configuration $\\langle c, \\sigma \\rangle$ must be listed — not just the stores. Skipping steps loses up to 2 marks.\n> 5. **Not stating the termination argument for reachability:** A bare fixpoint equation without the *monotonicity + finiteness* argument is incomplete (1–2 marks lost).\n> 6. **Stating non-determinism as \"random\"**: Non-determinism is *adversarial*, not probabilistic. Examiners will deduct for this misuse of terminology.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n- **Execution** = a sequence $\\pi = s_0 \\xrightarrow{\\alpha_0} s_1 \\xrightarrow{\\alpha_1} \\ldots$ of states and labels, beginning in $S_0$, governed by $\\rightarrow$.\n- **Trace** = the *label-only* projection of an execution. Two executions sharing a trace is the basis of **stutter equivalence** (used in LTL model checking under fairness).\n- **Small-step (SOS)** semantics gives $\\rightarrow$ and is the foundation of operational verification; **big-step** semantics gives $\\Downarrow$ and is used in proof assistants and interpreters.\n- A transition system is **deterministic** iff for every $(s, \\alpha)$ there is at most one successor; otherwise it is **non-deterministic** (default model for concurrency).\n- $Post(s)$ returns the set of all one-step successors; iterating $Post$ from $S_0$ yields $Reach(TS)$ — a Kleene fixpoint that terminates in at most $\\vert S \\vert$ steps for finite systems.\n- Maximal executions are of three kinds: **finite** (terminate in deadlock), **lasso** (finite prefix + cycle), or **infinite** ($\\omega$-length).\n- The **equivalence theorem** states $\\langle c, \\sigma \\rangle \\Downarrow \\sigma' \\iff \\langle c, \\sigma \\rangle \\rightarrow^* \\langle \\epsilon, \\sigma' \\rangle$.\n- Every temporal-logic property (LTL/CTL) and every safety property is evaluated **on executions**; hence execution is the central object of study in **automated verification**.\n- **Real-world use:** model checkers (SPIN, NuSMV, UPPAAL) and bounded model checkers (CBMC, JBMC) all internally construct, enumerate, or symbolically represent executions of the system under verification.\n- **Key SOS rule categories to memorize:** SKIP, ASSIGN, SEQ-1, SEQ-2, IF-True, IF-False, WHILE-True, WHILE-False.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t11",
          "title": "Reachable States",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module Module 1\n> - **Topic:** Reachable States\n\n<!-- SECTION_1_START -->\n## SECTION 1 — Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\nIn the context of **Model Checking** and **Formal Verification**, a **Reachable State** is any state `s ∈ S` of a transition system `TS = (S, S₀, →, L)` for which there exists a finite execution path\n\n$$s_0 \\xrightarrow{\\,a_0\\,} s_1 \\xrightarrow{\\,a_1\\,} s_2 \\xrightarrow{\\,a_2\\,} \\dots \\xrightarrow{\\,a_{n-1}\\,} s_n = s$$\n\nstarting from some **initial state** `s₀ ∈ S₀` and following valid transitions in `→`. The set of *all* such states is called the **Reachability Set** (denoted `Reach(TS)` or `R*`).\n\nFormally, the reachability set is the **least fixpoint** of the monotone operator `τ(X) = S₀ ∪ Post(X)`:\n\n$$R^{*} = \\mu X.\\; S_0 \\cup Post(X)$$\n\nwhere `Post(X) = { s' ∣ ∃ s ∈ X : s → s' }` is the *post-image* (or *next-state*) operator.\n\n> [!IMPORTANT]\n> **KTU High-Yield Definition:** A state is **reachable** if and only if it lies on some path originating from an initial state. Unreachable states are *irrelevant* for verifying safety/liveness properties — model checkers exploit this by **restricting the state space to `R*` only**, often yielding exponential reductions.\n\n### 1.2 Conceptual Analogy — The Subway Map of Computation\n\nImagine a **Kerala State Road Transport (KSRTC) bus network** as a transition system:\n- Each **bus stop** = a state.\n- Each **road connecting two stops** = a transition.\n- The **main depot** (e.g., Thiruvananthapuram Central) = the initial state `s₀`.\n\n**Reachable states** = every bus stop the bus can *ever visit* during its service, no matter how many routes it takes. Stops sitting in a remote forest path with no connecting road from the depot are **unreachable** and need never be examined.\n\nIn hardware verification, this is *literally true*: a designer does not care whether a flip-flop can hold the bit pattern `0xDEADBEEF` if the reset/clock logic can never set it — the pattern is unreachable and the property \"`X = 0xDEADBEEF is impossible`\" is **trivially true** without proof.\n\n> [!NOTE]\n> **Why Reachability Matters in KTU Automata Module:**\n> 1. Defines the *effective state space* over which CTL*/LTL model checking is performed.\n> 2. Forms the basis of **state-space exploration** algorithms (BFS, DFS, BDD-based symbolic search).\n> 3. Underpins **deadlock detection** (deadlocks are reachable states with no outgoing transitions).\n> 4. Enables **on-the-fly / partial-order reductions** by ignoring unreachable subgraphs.\n\n### 1.3 Standard Metrics & Notation Summary\n\n| Symbol | Meaning | Standard Form |\n|--------|---------|---------------|\n| `S` | Finite set of all states | `\\|S\\|` denotes cardinality |\n| `S₀` | Set of initial states | `S₀ ⊆ S` |\n| `→` | Transition relation | `→ ⊆ S × S` |\n| `Post(X)` | Set of immediate successors of `X` | `Post(X) = {s′ ∣ s → s′ ∧ s ∈ X}` |\n| `Pre(X)` | Set of immediate predecessors of `X` | `Pre(X) = {s ∣ s → s′ ∧ s′ ∈ X}` |\n| `R*` | Set of all reachable states | Smallest fixpoint of `τ` |\n| `μ` | Least fixpoint operator | Kleene iteration |\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## SECTION 2 — Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Operational Steps of Reachability Computation\n\nThe reachability set is constructed **iteratively** starting from the initial states and accumulating new states until **saturation** (no new states can be added):\n\n1. **Seed the frontier** with all initial states: `R₀ = S₀`.\n2. **Compute the image**: compute `Post(Rᵢ)` — every state that can be reached in *exactly one* step from some already-known reachable state.\n3. **Union and grow**: set `Rᵢ₊₁ = Rᵢ ∪ Post(Rᵢ)`.\n4. **Saturation check**: if `Rᵢ₊₁ = Rᵢ`, terminate — fixpoint reached.\n5. **Otherwise**, increment `i` and return to step 2.\n\nThis is the **Kleene fixed-point theorem in action** on a finite lattice `(2^S, ⊆)`.\n\n### 2.2 Why This Works — The \"Why\" Behind the Math\n\nBecause `S` is **finite** and `Post` is **monotone** (if `X ⊆ Y` then `Post(X) ⊆ Post(Y)`), the sequence\n\n$$R_0 \\subseteq R_1 \\subseteq R_2 \\subseteq \\dots \\subseteq R_k = R_{k+1} = \\dots = R^{*}$$\n\nis a **non-decreasing chain** bounded above by `S`. By the **Knaster–Tarski theorem**, it must stabilise after at most `|S|` iterations, giving us `R*` in **finite time**.\n\n> [!IMPORTANT]\n> **Termination Guarantee (exam-favourite):** A BFS/DFS-style reachability algorithm always terminates on a finite transition system after at most `|S|` iterations because the state set is finite and the chain is strictly increasing until the fixpoint is hit.\n\n### 2.3 KTU Formula Sheet / Cheat Sheet\n\n| # | Concept | Formula / Definition | Engineering Use |\n|---|---------|----------------------|-----------------|\n| 1 | Transition System | `TS = (S, S₀, →, L)` | Mathematical model of HW/SW |\n| 2 | Post-image | `Post(X) = {s′ ∣ s → s′ ∧ s ∈ X}` | Forward state exploration |\n| 3 | Pre-image | `Pre(X) = {s ∣ s → s′ ∧ s′ ∈ X}` | Backward counterexample search |\n| 4 | Reachability operator | `τ(X) = S₀ ∪ Post(X)` | Monotone on `(2^S, ⊆)` |\n| 5 | Reachability fixpoint | `R* = μX. τ(X) = ⋃ᵢ₌₀^∞ Rᵢ` | Kleene iteration |\n| 6 | Iterative step | `R₀ = S₀`, `Rᵢ₊₁ = Rᵢ ∪ Post(Rᵢ)` | BFS/DFS-style exploration |\n| 7 | Termination | `R* = R*` (idempotent) | At most `\\|S\\|` iterations |\n| 8 | Complexity (explicit) | `O(\\vert S\\vert + \\vert \\to \\vert)` | BFS time |\n| 9 | Complexity (symbolic) | `O(\\vert V\\vert² \\cdot k)` for `k` iterations | BDD image computation |\n| 10 | Deadlock definition | `s ∈ R*` such that `Post({s}) = ∅` | Safety verification |\n| 11 | Bounded reachability | `Reachₖ(s₀) = ⋃ᵢ₌₀^k Postⁱ({s₀})` | Bounded Model Checking (BMC) |\n\n> [!NOTE]\n> **Real-world engineering utility:** Reachability analysis is the **backbone** of every commercial model checker — *SPIN* (explicit BFS/DFS), *NuSMV* (BDD-based symbolic), *CBMC* (SAT-based bounded), *UPPAAL* (timed automata zones), and *JKind* (inductive invariant generation). The same fixpoint loop also powers abstract interpretation engines like *Astrée* (used to verify Airbus flight control software).\n\n### 2.4 Forward vs. Backward Reachability\n\n* **Forward reachability** starts from `S₀` and explores `Post` repeatedly. Used to compute `R*` and detect reachable error states.\n* **Backward reachability** starts from a set of \"bad\" states `Bad` and explores `Pre` repeatedly. Used to generate **counterexamples** when the property is violated.\n\nBoth converge to the same fixpoint; the choice is dictated by which side has the **smaller frontier** (heuristic-driven in industrial tools).\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## SECTION 3 — Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Worked Example 1 — Manual Reachability Computation on a Toy TS\n\nConsider a transition system modelling a **traffic light controller** with four states:\n\n$$S = \\{ G, Y, R, F \\}$$\n\nwhere `G = Green`, `Y = Yellow`, `R = Red`, `F = Fault`. Transitions are:\n\n$$G \\xrightarrow{} Y,\\quad Y \\xrightarrow{} R,\\quad R \\xrightarrow{} G,\\quad R \\xrightarrow{} F,\\quad F \\xrightarrow{} F$$\n\nInitial state: `S₀ = {G}`.\n\n**Step 1 — Seed:**\n$$R_0 = S_0 = \\{G\\}$$\n\n**Step 2 — Compute Post-image of R₀:**\n$$Post(\\{G\\}) = \\{ Y \\}$$\nbecause `G → Y` is the only transition out of `G`.\n\n**Step 3 — Union:**\n$$R_1 = R_0 \\cup Post(R_0) = \\{G\\} \\cup \\{Y\\} = \\{G, Y\\}$$\n\n**Step 4 — Compute Post-image of R₁:**\n$$Post(\\{G, Y\\}) = \\{Y, R\\}$$\n\n**Step 5 — Union:**\n$$R_2 = \\{G, Y\\} \\cup \\{Y, R\\} = \\{G, Y, R\\}$$\n\n**Step 6 — Compute Post-image of R₂:**\n$$Post(\\{G, Y, R\\}) = \\{Y, R, G, F\\}$$\n\n**Step 7 — Union:**\n$$R_3 = \\{G, Y, R\\} \\cup \\{Y, R, G, F\\} = \\{G, Y, R, F\\}$$\n\n**Step 8 — Compute Post-image of R₃:**\n$$Post(\\{G, Y, R, F\\}) = \\{Y, R, G, F, F\\} = \\{G, Y, R, F\\}$$\n\n**Step 9 — Saturation check:**\n$$R_4 = R_3 \\cup Post(R_3) = \\{G, Y, R, F\\} \\cup \\{G, Y, R, F\\} = \\{G, Y, R, F\\} = R_3$$\n\nThe fixpoint is reached. Therefore:\n\n$$R^{*} = \\{ G, Y, R, F \\} = S$$\n\n**Conclusion:** Every state is reachable. This is a *complete* system — no dead-code elimination possible.\n\n> [!TIP]\n> **Examiner Insight:** In KTU model-checking problems, the answer `R*` must always be presented as a **set**, with the **number of iterations** explicitly stated. Marks are awarded separately for (a) the iteration sequence, (b) the fixpoint check, and (c) the final answer.\n\n### 3.2 Worked Example 2 — Symbolic Reachability via BDD-Style Image\n\nSuppose the same traffic light is encoded with **two boolean state variables** `x₁, x₂`:\n\n$$G \\equiv \\neg x_1 \\neg x_2,\\quad Y \\equiv \\neg x_1 x_2,\\quad R \\equiv x_1 \\neg x_2,\\quad F \\equiv x_1 x_2$$\n\nTransition relation `T(x₁, x₂, x′₁, x′₂)` is:\n\n$$\n\\begin{aligned}\nT \\;=\\;& (\\neg x_1 \\neg x_2 \\land \\neg x'_1 x'_2) \\;\\lor\\; (\\neg x_1 x_2 \\land x'_1 \\neg x'_2) \\\\\n     & \\lor\\; (x_1 \\neg x_2 \\land \\neg x_1 \\neg x'_2) \\;\\lor\\; (x_1 \\neg x_2 \\land x'_1 x'_2) \\\\\n     & \\lor\\; (x_1 x_2 \\land x'_1 x'_2)\n\\end{aligned}\n$$\n\n**Step 1 — Initial-state predicate:**\n$$I(x_1, x_2) = \\neg x_1 \\neg x_2$$\n\n**Step 2 — Kleene iteration with image computation:**\nImage formula: `Post(X) = ∃x₁ ∃x₂ [ X(x₁, x₂) ∧ T(x₁, x₂, x′₁, x′₂) ]` with output projected onto `x′₁, x′₂`.\n\n$$\n\\begin{aligned}\nR_0(x') \\;&= I(x') = \\neg x'_1 \\neg x'_2 \\\\\nPost(R_0) \\;&= \\neg x'_1 x'_2 \\\\\nR_1 \\;&= \\neg x'_1 \\neg x'_2 \\;\\lor\\; \\neg x'_1 x'_2 \\;=\\; \\neg x'_1 \\\\\nPost(R_1) \\;&= \\neg x'_1 x'_2 \\;\\lor\\; x'_1 \\neg x'_2 \\\\\nR_2 \\;&= \\neg x'_1 \\;\\lor\\; \\neg x'_1 x'_2 \\;\\lor\\; x'_1 \\neg x'_2 \\;=\\; \\neg x'_1 \\;\\lor\\; x'_1 \\neg x'_2 \\\\\nPost(R_2) \\;&= \\neg x'_1 x'_2 \\;\\lor\\; x'_1 \\neg x'_2 \\;\\lor\\; x'_1 x'_2 \\\\\nR_3 \\;&= \\neg x'_1 \\;\\lor\\; x'_1 \\neg x'_2 \\;\\lor\\; x'_1 x'_2 \\;=\\; \\neg x'_1 \\;\\lor\\; x'_1 \\;=\\; \\text{true}\n\\end{aligned}\n$$\n\nFixpoint reached in **3 iterations**: `R* = true`, i.e., the entire state space is reachable.\n\n### 3.3 Analytical Matrix — Choosing the Right Reachability Algorithm\n\n| Algorithm | State Representation | Time Complexity | Space Complexity | Best Use Case |\n|-----------|----------------------|-----------------|------------------|---------------|\n| BFS (Explicit) | Adjacency list | `O(\\vert S\\vert + \\vert \\to \\vert)` | `O(\\vert S\\vert)` | Small-to-medium concurrent systems |\n| DFS (Explicit) | Adjacency list | `O(\\vert S\\vert + \\vert \\to \\vert)` | `O(\\vert S\\vert)` worst-case | On-the-fly / partial-order reduction |\n| BDD-Based Symbolic | Boolean predicates via BDDs | `O(\\vert BDD\\vert² · k)` | `O(\\vert BDD\\vert²)` | Huge hardware circuits (10¹⁰⁰+ states) |\n| SAT-Based Bounded | CNF clauses | `O(2^L · \\vert\\phi\\vert)` | Linear in formula | Bounded depth, bug-hunting |\n| Petri-Net Reachability | Marking vectors | EXPSPACE-complete | `O(n · b)` | Asynchronous / distributed systems |\n| Timed Automata Zones | DBMs (Difference Bound Matrices) | EXPSPACE | `O(n² · M)` | Real-time / embedded controllers |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## SECTION 4 — Structural Diagrams & Schematics\n\n### 4.1 Mermaid State-Transition Diagram of the Traffic-Light TS\n\n```mermaid\ngraph LR\n    s0_init([\"S0 = Initial States: G\"])\n    stateG[\"G Green\"]\n    stateY[\"Y Yellow\"]\n    stateR[\"R Red\"]\n    stateF[\"F Fault\"]\n\n    s0_init -.initial.-> stateG\n    stateG -- \"tick\" --> stateY\n    stateY -- \"tick\" --> stateR\n    stateR -- \"tick\" --> stateG\n    stateR -- \"error\" --> stateF\n    stateF -- \"stay\" --> stateF\n\n    classDef init fill:#cce5ff,stroke:#003366,stroke-width:2px,color:#003366\n    classDef safe fill:#d4edda,stroke:#155724,color:#155724\n    classDef fault fill:#f8d7da,stroke:#721c24,color:#721c24\n    class s0_init init\n    class stateG,stateY,stateR safe\n    class stateF fault\n```\n\n### 4.2 Mermaid Functional Flow — Kleene Fixpoint Reachability Engine\n\n```mermaid\nflowchart TD\n    A[\"Input: Transition System TS = S, S0, arrow, L\"] --> B[\"Initialise: R = S0, Frontier = S0, i = 0\"]\n    B --> C{\"Frontier empty?\"}\n    C -- \"Yes\" --> D[\"Output: R* = R (fixpoint reached)\"]\n    C -- \"No\" --> E[\"Compute NewFrontier = Post Frontier\"]\n    E --> F[\"R = R union NewFrontier\"]\n    F --> G[\"i = i + 1\"]\n    G --> H{\"R changed since last iteration?\"}\n    H -- \"No\" --> D\n    H -- \"Yes\" --> I[\"Frontier = NewFrontier\"]\n    I --> C\n\n    classDef ioNode fill:#fff3cd,stroke:#856404,color:#856404\n    classDef procNode fill:#d1ecf1,stroke:#0c5460,color:#0c5460\n    classDef decisionNode fill:#f5c6cb,stroke:#721c24,color:#721c24\n    classDef outNode fill:#d4edda,stroke:#155724,color:#155724\n\n    class A,D ioNode\n    class B,E,F,G,I procNode\n    class C,H decisionNode\n    class D outNode\n```\n\n### 4.3 Mermaid Block Architecture — Symbolic vs. Explicit Reachability Subsystem\n\n```mermaid\nflowchart LR\n    subgraph Input_Layer [INPUT LAYER]\n        modelFile[\"Model File SMV / Promela / HDL\"]\n        propertyFile[\"Property Spec LTL or CTL\"]\n    end\n\n    subgraph Front_End [FRONT END]\n        parser[\"Parser and AST Builder\"]\n        tsBuilder[\"Transition System Builder\"]\n        propertyEncoder[\"Property Encoder\"]\n    end\n\n    subgraph Reach_Engine [REACHABILITY ENGINE]\n        explicitBFS[\"Explicit BFS or DFS on Sparse Graph\"]\n        bddImage[\"BDD Image Computation via CUDD or BuDDy\"]\n        satBMC[\"SAT based Bounded Reachability\"]\n    end\n\n    subgraph Back_End [BACK END]\n        stateSet[\"Reachable State Set R-star\"]\n        counterexample[\"Counterexample Trace\"]\n        witness[\"Invariant Witness\"]\n    end\n\n    modelFile --> parser --> tsBuilder\n    propertyFile --> propertyEncoder\n    tsBuilder --> explicitBFS\n    tsBuilder --> bddImage\n    tsBuilder --> satBMC\n    explicitBFS --> stateSet\n    bddImage --> stateSet\n    satBMC --> stateSet\n    stateSet --> counterexample\n    stateSet --> witness\n    propertyEncoder --> counterexample\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## SECTION 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### Part A Questions (3 Marks Each)\n\n**Q1. [KTU University Exam — May 2024]**\nDefine a *reachable state* in a transition system. State the least-fixpoint characterisation of the reachability set.\n\n> **Model Answer (3 marks):**\n> A state `s` is **reachable** in `TS = (S, S₀, →, L)` if there exists a finite sequence of transitions from some `s₀ ∈ S₀` to `s`. The reachability set is the least fixpoint of the operator `τ(X) = S₀ ∪ Post(X)`, expressed as\n> $$R^{*} = \\mu X.\\; S_0 \\cup Post(X) = \\bigcup_{i=0}^{\\infty} R_i$$\n> where `R₀ = S₀` and `Rᵢ₊₁ = Rᵢ ∪ Post(Rᵢ)`. (1 mark: definition; 1 mark: fixpoint form; 1 mark: iterative construction.)\n\n**Q2. [KTU University Exam — Dec 2023]**\nDifferentiate between *forward* and *backward* reachability. In which situation is backward reachability preferred?\n\n> **Model Answer (3 marks):**\n> Forward reachability starts from `S₀` and applies `Post` to grow the reachable set. Backward reachability starts from a set of *bad* states `Bad` and applies `Pre` repeatedly to compute predecessors. **(2 marks)**. Backward reachability is preferred when `|Bad| ≪ |S₀|` or when the model checker needs to produce a **counterexample** showing how a violation can occur. **(1 mark).**\n\n---\n\n### Part B Questions (14 Marks Each — Internal Choice)\n\n#### Question A (14 Marks) `[KTU University Exam — Dec 2023]`\n\n**(a)** Consider the transition system `TS` with states `S = {s₀, s₁, s₂, s₃, s₄, s₅}`, initial state `S₀ = {s₀}`, and transition relation:\n`{ s₀→s₁, s₀→s₂, s₁→s₃, s₂→s₃, s₃→s₄, s₄→s₅, s₅→s₄ }`. **(7 marks)**\nCompute the reachability set `R*` by Kleene iteration, showing each `Rᵢ` and the corresponding `Post(Rᵢ)`.\n\n> **Model Answer (7 marks):**\n>\n> *R₀ = S₀ = { s₀ }; Post(R₀) = { s₁, s₂ }.*\n> *R₁ = { s₀, s₁, s₂ }.*\n>\n> *Post(R₁) = { s₃ } ∪ { s₃ } = { s₃ }.* (since s₁→s₃ and s₂→s₃)\n> *R₂ = { s₀, s₁, s₂, s₃ }.*\n>\n> *Post(R₂) = { s₄ }.*\n> *R₃ = { s₀, s₁, s₂, s₃, s₄ }.*\n>\n> *Post(R₃) = { s₅ }.*\n> *R₄ = { s₀, s₁, s₂, s₃, s₄, s₅ }.*\n>\n> *Post(R₄) = { s₄ } ⊆ R₄. Fixpoint reached.*\n>\n> **Answer:** `R* = { s₀, s₁, s₂, s₃, s₄, s₅ } = S`. **(Full marks: 1 per iteration step, 2 for the fixpoint identification.)**\n\n**(b)** Explain the BDD-based *symbolic* image computation used to compute `Post` in BDD-driven model checkers such as NuSMV. Why is symbolic reachability often able to handle 10¹⁰⁰+ states? **(7 marks)**\n\n> **Model Answer (7 marks):**\n> **Symbolic Image Computation (4 marks):**\n> Given a set of states encoded as a BDD `R(x⃗)` and a transition relation `T(x⃗, x⃗′)`, the image is computed as:\n> $$Post(R)(x⃗′) = \\exists x⃗.\\; \\big[ R(x⃗) \\wedge T(x⃗, x⃗′) \\big]$$\n> followed by renaming `x⃗′` → `x⃗`. The conjunction and existential quantification are performed *directly on the BDDs*, exploiting the canonical, compressed representation. Implementations use **quantifier elimination via the smooth operator** and **monolithic vs. partitioned transition relation** heuristics.\n>\n> **Why It Scales (3 marks):**\n> 1. BDDs share sub-functions — many structurally similar states collapse into one BDD node.\n> 2. The fixed-point loop operates on *symbolic predicates*, not individual states.\n> 3. Each iteration is polynomial in the BDD size, which is often logarithmic in `|S|`.\n> 4. Industrial circuits (e.g., a 32-bit multiplier has ~10¹⁹ states) have BDDs of only a few thousand nodes.\n\n#### Question B (14 Marks) `[KTU University Exam — May 2024]`\n\n**(a)** State and prove that the Kleene iteration sequence `R₀ ⊆ R₁ ⊆ R₂ ⊆ …` on a finite transition system always reaches a fixpoint in at most `|S|` steps. **(7 marks)**\n\n> **Model Answer (7 marks):**\n> **Statement (2 marks):** The sequence `R₀ = S₀, Rᵢ₊₁ = Rᵢ ∪ Post(Rᵢ)` is non-decreasing and stabilises within `|S|` iterations.\n>\n> **Proof (5 marks):**\n> *Step 1 (Monotonicity):* For any `X ⊆ Y ⊆ S`, we have `Post(X) ⊆ Post(Y)` because every successor of a state in `X` is also a successor of that state considered in `Y`. Hence `Rᵢ ⊆ Rᵢ₊₁`.\n> *Step 2 (Boundedness):* `Rᵢ ⊆ S` for all `i`, so the chain is bounded above by `S`.\n> *Step 3 (Strict growth or saturation):* Since `S` is finite with `|S|` elements, the chain `R₀ ⊂ R₁ ⊂ …` can have at most `|S|` strict inclusions. By the **Knaster–Tarski fixed-point theorem** on the complete lattice `(2^S, ⊆)`, the limit exists and equals `R* = μX.τ(X)`. Therefore the iteration terminates in at most `|S|` steps.\n\n**(b)** Distinguish between *Bounded Model Checking (BMC)* and *full* reachability analysis. Under what condition does BMC give a *complete* answer? **(7 marks)**\n\n> **Model Answer (7 marks):**\n> **BMC (3 marks):** BMC unrolls the transition relation up to depth `k` and asks a SAT solver whether any bad state is reachable within `k` steps. It produces counterexamples of bounded length but cannot prove *absence* of errors.\n> **Full reachability (2 marks):** Full (Kleene / BDD) reachability computes the entire fixpoint `R*` and can prove that no bad state is reachable (complete verification).\n> **Completeness of BMC (2 marks):** BMC is complete for a property if the system diameter `d` (longest shortest path between any two states) satisfies `k ≥ d`. In that case, any state reachable in *any* number of steps is reachable within `k` steps, so BMC + SAT-decision at depth `d` is equivalent to full model checking.\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfalls — Reachability Problems:**\n> 1. **Forgetting the Post computation per iteration:** Many students jump directly to the final set `R*` without showing the iterative sequence. *Loss: up to 4 marks per Part B question.*\n> 2. **Missing the fixpoint termination check:** You must explicitly state `Rᵢ₊₁ = Rᵢ` and label it as the *fixpoint*. *Loss: 1–2 marks.*\n> 3. **Confusing `Post` and `Pre`:** `Post` gives successors; `Pre` gives predecessors. Writing the wrong direction silently invalidates the iteration.\n> 4. **Not listing the elements as a set:** The answer must be enclosed in `{ … }` and treated as a subset of `S`.\n> 5. **In symbolic/image-computation questions,** failing to rename `x⃗′` → `x⃗` after existential quantification is a recurring deduction.\n\n---\n\n### Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- **Reachable state** = a state lying on some path from an initial state.\n- **Reachability set** = `R* = μX. (S₀ ∪ Post(X))`, the *least* fixpoint of a monotone operator on `(2^S, ⊆)`.\n- **Kleene iteration** computes `R*` by `R₀ = S₀`, `Rᵢ₊₁ = Rᵢ ∪ Post(Rᵢ)`, terminating in at most `|S|` steps.\n- **Forward** reachability uses `Post`; **backward** reachability uses `Pre` (used for counterexample generation).\n- **BFS / DFS** is the explicit-state algorithm; **BDD image** is the symbolic algorithm; **SAT unrolling** is the bounded algorithm.\n- A **deadlock** is a reachable state `s` with `Post({s}) = ∅`.\n- **State-space reduction:** Unreachable states are pruned *before* CTL/LTL model checking — a major source of the efficiency of industrial model checkers.\n- **Termination proof** depends on three facts: `Post` is monotone, `S` is finite, and the Kleene chain is bounded by `S`.\n- **Complexity:** Explicit `O(|S| + |→|)`; symbolic BDD `O(|BDD|² · k)`; SAT-bounded `O(2^L · |φ|)`.\n- **Applications** span hardware verification (Intel/AMD), software model checking (SLAM, CBMC), protocol verification (SPIN), and real-time embedded systems (UPPAAL).\n- **Kleene vs. Tarski:** Kleene iteration is the constructive method; Knaster–Tarski guarantees existence of the fixpoint.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t12",
          "title": "Linear-Time (LT) Properties - Deadlock",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module Module 1\n> - **Topic:** Linear-Time (LT) Properties - Deadlock\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Linear-Time (LT) Properties — Formal Definition\n\nIn the framework of **Model Checking** (Clarke, Emerson; and later formalized by Baier & Katoen in *Principles of Model Checking*), a **Linear-Time (LT) property** is a set of infinite execution traces (sequences of states) over the atomic propositions of a transition system. Formally, an LT property $P$ is a subset:\n\n$$P \\subseteq (2^{AP})^{\\omega}$$\n\nwhere $AP$ is a finite set of atomic propositions and $(2^{AP})^{\\omega}$ denotes the set of all infinite words (omega-words) over the alphabet $2^{AP}$. A transition system $\\mathcal{TS}$ satisfies $P$ (written $\\mathcal{TS} \\models P$) if and only if **every** infinite path of $\\mathcal{TS}$ is a member of $P$:\n\n$$\\mathcal{TS} \\models P \\quad\\iff\\quad \\text{Traces}(\\mathcal{TS}) \\subseteq P$$\n\n> [!IMPORTANT]\n> **KTU 2024 Highlight:** LT properties are evaluated on a *path-by-path* basis. They describe what *can* or *must* happen along an execution sequence. The two principal subclasses are **Safety** (\"something bad will never happen\") and **Liveness** (\"something good will eventually happen\").\n\n### 1.2 Deadlock — Formal Definition\n\nA **deadlock** in a transition system $\\mathcal{TS} = (S, Act, \\to, I, AP, L)$ is a reachable state $s \\in \\text{Reach}(\\mathcal{TS})$ from which **no outgoing transition** is enabled. In formal notation:\n\n$$\\text{Deadlock}(s) \\;\\equiv\\; \\forall \\alpha \\in Act \\;:\\; s \\not\\xrightarrow{\\alpha}$$\n\nIf the transition system models concurrency, two sub-classes are distinguished:\n\n| Sub-class | Definition | Reachability |\n|---|---|---|\n| **Complete Deadlock** | No transition of *any* process can fire | $s$ is reachable and $Post(s) = \\emptyset$ |\n| **Partial Deadlock** | Some but not all processes are blocked (e.g., a process waits for another in a cycle) | $s$ is reachable, and a *wait-for cycle* exists |\n\n### 1.3 Conceptual Analogy — The Traffic Gridlock\n\n> [!NOTE]\n> **Intuition (Plain English):** Imagine a four-way intersection where cars from the North, South, East, and West all arrive at the same time. Each car *needs* the right-of-way that the others occupy, and *none* of them can move forward. The intersection has reached a **deadlock** — no rule of the road allows a safe transition. In software, a deadlock is the digital twin of this gridlock: every thread is holding a lock that another thread needs, and no thread can release its lock until it acquires the next one.\n\n### 1.4 Why Deadlock is an LT Property\n\nDeadlock is classified as a **safety property** because it is a *finite* violation: a deadlock can be witnessed by examining a *finite prefix* of a path (the prefix that leads into the deadlock state). This is in contrast to liveness properties, whose violations require infinite observation.\n\n> [!TIP]\n> **Examiner Cue:** Whenever a question asks \"What type of property is deadlock?\" — answer: *a state-based safety property (a special case of the 'something bad will happen' class), but the LT-property variant is called **deadlock-freeness***.\n\n### 1.5 Standard Metrics and Conventions (Bolded Constants)\n\n- **SCCS / CSP semantics** for concurrency: process algebra.\n- **LTL** (Linear Temporal Logic) alphabet: $\\{ \\neg, \\vee, \\bigcirc, \\mathcal{U} \\}$ — and the derived operators $\\Diamond$ (eventually) and $\\Box$ (always).\n- The standard *trap state* constant used when converting a transition system to one in which every deadlock state is labelled with a special proposition, e.g., $deadlock \\in AP$, is denoted as the **\"deadlock proposition\"**.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 The Three-Stage Logic of Classifying Deadlock as an LT Property\n\n1. **Identify the property over a single path.**  \n   A *path* $\\pi = s_0 s_1 s_2 \\ldots$ is *deadlocking* if there exists an index $k \\ge 0$ such that $s_k$ is a deadlock state. Equivalently:\n\n   $$\\pi \\models \\text{HasDeadlock} \\;\\iff\\; \\exists k \\ge 0 \\;:\\; \\text{Deadlock}(s_k)$$\n\n2. **Quantify universally over all paths.**  \n   The transition system is **deadlock-free** iff *no* initial path contains a deadlock:\n\n   $$\\mathcal{TS} \\models \\text{DeadlockFree} \\quad\\iff\\quad \\forall \\pi \\in \\text{Paths}(\\mathcal{TS}) \\;:\\; \\pi \\not\\models \\text{HasDeadlock}$$\n\n   The negation yields: $\\mathcal{TS} \\not\\models \\text{DeadlockFree}$ iff there exists a finite path $s_0 \\to s_1 \\to \\cdots \\to s_k$ with $Post(s_k) = \\emptyset$.\n\n3. **Choose the logic — LTL.**  \n   In Linear Temporal Logic, deadlock-freeness for a system with a designated proposition $deadlock$ is expressed as the **safety assertion**:\n\n   $$\\varphi_{\\text{DF}} \\;=\\; \\Box \\, \\neg\\, deadlock$$\n\n   This formula reads: *\"globally, the proposition deadlock is false\"*. The violation of $\\Box \\neg deadlock$ is a finite witness — a path prefix that ends in a state labelled $deadlock$, which is the hallmark of a safety property.\n\n### 2.2 Why the 'Why' Matters (Engineering Justification)\n\n- **Why classify deadlock as safety?**  \n  Because it can be detected on a *finite* prefix; a model checker (NuSMV, SPIN, UPPAAL) can terminate as soon as it visits a deadlock state — it does not need to explore infinitely many steps.\n\n- **Why use $\\Box$ (always) and not $\\Diamond$ (eventually)?**  \n  $\\Diamond$ would assert \"the system eventually deadlocks\", which is a *liveness-shaped* claim about an undesirable event. We want the *negation* of an unwanted finite event — that is exactly the syntactic shape $\\Box \\neg p$.\n\n- **How is deadlock encoded in the Kripke structure?**  \n  Engineers introduce a self-loop absorbing \"trap state\" $s_\\bot$ such that every state $s$ with $Post(s) = \\emptyset$ is rewired with a transition $s \\to s_\\bot$, and $s_\\bot$ is labelled with the atomic proposition $deadlock$. The formula $\\Box \\neg deadlock$ then becomes checkable.\n\n### 2.3 KTU Formula Sheet / Cheat Sheet\n\n| # | Item | Symbolic Form | Notes |\n|---|---|---|---|\n| 1 | LT Property (set-theoretic) | $P \\subseteq (2^{AP})^{\\omega}$ | $AP$ = atomic propositions, $\\omega$ = infinite word |\n| 2 | Satisfaction of LT property | $\\mathcal{TS} \\models P \\iff \\text{Traces}(\\mathcal{TS}) \\subseteq P$ | Universal quantification over paths |\n| 3 | Path definition | $\\pi = s_0 \\to s_1 \\to s_2 \\to \\ldots$ | Infinite by convention |\n| 4 | Deadlock predicate (state) | $\\text{Deadlock}(s) \\iff Post(s) = \\emptyset$ | $Post(s) := \\lbrace s' \\in S \\mid s \\to s' \\rbrace$ |\n| 5 | Deadlock-freeness (LTL) | $\\varphi_{\\text{DF}} = \\Box \\, \\neg\\, deadlock$ | A **safety** formula |\n| 6 | Existence of a deadlock (negation) | $\\exists \\pi : \\pi \\models \\Diamond \\, deadlock$ | Equivalent to $\\neg \\Box \\neg deadlock$ |\n| 7 | LTL safety-grammar fragment | $\\varphi ::= p \\;\\mid\\; \\neg \\varphi \\;\\mid\\; \\varphi \\vee \\varphi \\;\\mid\\; \\bigcirc \\varphi \\;\\mid\\; \\varphi \\mathcal{U} \\varphi$ | Closed under $\\Box$ |\n| 8 | Liveness form | $\\psi_{\\text{progress}} = \\Box \\Diamond \\, enabled$ | Often required *with* deadlock-freeness |\n| 9 | Büchi automaton (LT property) | $\\mathcal{B}_\\varphi = (Q, \\Sigma, \\delta, Q_0, F)$ | $F$ = accepting set, infinite visits required |\n| 10 | Model checking complexity (LTL) | $\\mathcal{O}( \\vert M \\vert \\cdot 2^{\\mathcal{O}(\\vert \\varphi \\vert)} )$ | Linear in model, exponential in formula |\n\n> [!IMPORTANT]\n> **Critical Mark-Winning Note:** When asked to *prove* deadlock-freeness, do NOT confuse it with *fairness* or *progress*. A deadlock-free system may still starve a process — it just never enters a state from which *no* transition is possible.\n\n### 2.4 Real-World Engineering Utility\n\nDeadlock detection as an LT property is the theoretical backbone of:\n\n- **Embedded / Safety-Critical Software** — RTCA DO-178C level A & B (avionics) and ISO 26262 ASIL-D (automotive) verification workflows.\n- **Industrial Model Checkers** — *SPIN* (Holzmann) for Promela, *NuSMV* (CMU/Carnegie Mellon / FBK), and *UPPAAL* (Aalborg / Uppsala) all provide a built-in `deadlock` keyword or trap-state construct.\n- **Operating Systems & Distributed Databases** — the Banker's Algorithm (Dijkstra, 1965) and the *wait-for graph* cycle detection are practical implementations of the same idea: detect a reachable state with empty post-set.\n- **Hardware Design Verification** — SystemVerilog Assertions (SVA) such as `assert property (always !deadlock)` map directly to $\\Box \\neg deadlock$.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Derivation — From Deadlock Predicate to LTL Formula\n\n**Step 1 — Define a transition system.**  \nLet $\\mathcal{TS} = (S, \\to, I, AP, L)$ with $S$ the finite state set and $I \\subseteq S$ the initial states. For every state $s \\in S$ we have a *post-image*:\n\n$$Post(s) \\;=\\; \\lbrace s' \\in S \\mid \\exists \\alpha \\in Act : s \\xrightarrow{\\alpha} s' \\rbrace$$\n\n**Step 2 — Encode deadlock as an atomic proposition.**  \nAugment $AP$ with a new proposition $deadlock$ and a new state $s_\\bot$ (the trap). Define the labelling function $L'$ on the modified state space $S' = S \\cup \\lbrace s_\\bot \\rbrace$:\n\n$$\nL'(s) \\;=\\; \n\\begin{cases}\nL(s) \\cup \\lbrace deadlock \\rbrace & \\text{if } s \\in S \\text{ and } Post(s) = \\emptyset \\\\\nL(s) & \\text{if } s \\in S \\text{ and } Post(s) \\neq \\emptyset \\\\\n\\lbrace deadlock \\rbrace & \\text{if } s = s_\\bot\n\\end{cases}\n$$\n\n**Step 3 — Rewire outgoing transitions.**  \nFor each deadlock state $s$ in the original system, add the self-loop $s \\to s_\\bot$, and for $s_\\bot$, add a self-loop $s_\\bot \\to s_\\bot$. All other transitions are inherited unchanged.\n\n**Step 4 — Express the LT property in LTL.**  \nThe deadlock-freeness requirement is:\n\n$$\n\\begin{aligned}\n\\varphi_{\\text{DF}} \\;=&\\;\\; \\Box \\, \\neg\\, deadlock\n\\end{aligned}\n$$\n\n**Step 5 — Negate to obtain a *liveness-shaped* violation witness.**  \nA system *violates* deadlock-freeness iff there exists a path on which $deadlock$ is eventually true:\n\n$$\n\\begin{aligned}\n\\neg \\varphi_{\\text{DF}} \n\\;=&\\;\\; \\neg (\\Box \\neg deadlock) \\\\\n\\stackrel{\\text{LTL dual}}{=}{}&\\;\\; \\Diamond \\, deadlock\n\\end{aligned}\n$$\n\nThis equivalence $\\neg \\Box \\neg p \\equiv \\Diamond p$ is one of the most heavily tested identities in KTU model-checking questions.\n\n**Step 6 — Büchi automaton construction (sketch).**  \nConvert $\\varphi_{\\text{DF}} = \\Box \\neg deadlock$ into a generalized Büchi automaton $\\mathcal{B}_{\\varphi_{\\text{DF}}} = (Q, \\Sigma, \\delta, Q_0, F)$ with:\n\n- $Q = \\lbrace q_0 \\rbrace$ (a single state, since $\\Box \\neg p$ accepts only words that *never* show $p$),\n- $\\Sigma = 2^{AP}$,\n- $\\delta(q_0, X) = \\lbrace q_0 \\rbrace$ for every $X \\subseteq AP$ with $deadlock \\notin X$, and $\\delta(q_0, X) = \\emptyset$ otherwise,\n- $Q_0 = \\lbrace q_0 \\rbrace$,\n- $F = \\lbrace q_0 \\rbrace$.\n\nA path of $\\mathcal{TS}$ satisfies $\\Box \\neg deadlock$ iff the *synchronous product* automaton $\\mathcal{TS} \\otimes \\mathcal{B}_{\\varphi_{\\text{DF}}}$ has an accepting run — which is the case precisely when $\\mathcal{TS}$ never visits a state labelled $deadlock$.\n\n### 3.2 Analytical Matrix — Path Properties of Deadlock\n\n| Path Shape | LT Property Satisfied? | Reason |\n|---|---|---|\n| Infinite path with no deadlock | $\\Box \\neg deadlock$ ✓ | No state labelled $deadlock$ is ever visited |\n| Finite path terminating in a deadlock | $\\Box \\neg deadlock$ ✗ | Property violation witnessed at the deadlock state |\n| Path with a deadlock, then a non-deadlock successor (impossible in a trap-state encoding) | $\\Box \\neg deadlock$ ✗ | Once $deadlock$ holds, $\\Box$ is irreparably violated |\n| Path with a *temporal* liveness violation only (e.g., starvation) | $\\Box \\neg deadlock$ ✓ | Deadlock is *not* starvation — distinct properties |\n\n### 3.3 Worked Example — A Two-Process Mutual Exclusion System\n\nConsider two processes $P_1, P_2$ that may each be in one of three states: `non-critical (N)`, `trying (T)`, or `critical (C)`. Assume the (buggy) transition rules:\n\n$$\n\\begin{aligned}\nP_1:&\\quad T \\to C \\quad\\text{(unconditional, ignoring the lock)} \\\\\nP_2:&\\quad T \\to C \\quad\\text{(unconditional, ignoring the lock)}\n\\end{aligned}\n$$\n\nIf both processes are in $C$ and the system has no transition out of $C$ (no `exit` rule), then the global state $(C, C)$ is a deadlock state. The set of reachable states is:\n\n$$\\text{Reach} = \\lbrace (N,N), (N,T), (T,N), (T,T), (C,N), (N,C), (C,T), (T,C), (C,C) \\rbrace$$\n\nThe state $(C, C)$ satisfies $Post((C,C)) = \\emptyset$. Therefore, with the augmented atomic proposition $deadlock$, we have $L((C,C)) = \\lbrace deadlock \\rbrace$. The LTL check $\\Box \\neg deadlock$ on this model **fails** at the state $(C, C)$, and the model checker returns a counter-example trace:\n\n$$\\pi_{\\text{bad}} \\;=\\; (N,N) \\to (T,N) \\to (C,N) \\to (C,T) \\to (C,C)$$\n\nwhich is a finite prefix of length 5. This confirms the *safety* (finite-witness) nature of the violation.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Mermaid Diagram — Kripke Structure with a Deadlock State\n\n```mermaid\ngraph TD\n    s0[\"s0 : start, idle\"] --> s1[\"s1 : p1 trying\"]\n    s0 --> s2[\"s2 : p2 trying\"]\n    s1 --> s3[\"s3 : p1 in CS\"]\n    s2 --> s4[\"s4 : p2 in CS\"]\n    s3 --> deadlock[\"sDeadlock : both blocked\"]\n    s4 --> deadlock\n    s3 --> s1\n    s4 --> s2\n    deadlock --> trap[\"sBot : trap state labelled deadlock\"]\n    trap --> trap\n```\n\n**Reading the diagram:**\n\n- `s0` is the unique initial state.\n- `s3` and `s4` represent each process holding the lock.\n- `sDeadlock` is the *partial-deadlock* hub: when one process is in CS and the other is trying, the system still progresses (via the self-loops out of `s3` and `s4`).\n- `sBot` is the trap state, labelled with the proposition `deadlock`. Any path reaching `sBot` violates the LTL property $\\Box \\neg deadlock$.\n\n### 4.2 Mermaid Diagram — Büchi Automaton for $\\Box \\neg deadlock$\n\n```mermaid\ngraph LR\n    q0((\"q0 accepting initial\"))\n    q0 -- \"X subset of AP with deadlock not in X\" --> q0\n    q0 -. \"X subset of AP with deadlock in X (rejecting)\" .-> sink((\"sink rejecting\"))\n    sink --> sink\n```\n\n**Reading the diagram:**\n\n- The single accepting state `q0` accepts *any* omega-word in which the symbol $deadlock$ is **never** present.\n- The dashed arrow represents the rejecting transition; from that point on, the run falls into the rejecting sink.\n- A synchronized product with the Kripke structure yields a non-empty accepting run iff the system is deadlock-free.\n\n### 4.3 Mermaid Diagram — Block-Level Functional Architecture of Deadlock Verification\n\n```mermaid\ngraph TB\n    A[\"Transition System Model M\"] --> C[\"Product Construction M tensor B\"]\n    B[\"Buchi Automaton for phi_DF\"] --> C\n    C --> D[\"Search for Accepting Cycle\"]\n    D --> E{\"Cycle Found ?\"}\n    E -- \"Yes\" --> F[\"Return Counter-Example Path\"]\n    E -- \"No\" --> G[\"System Satisfies Box not deadlock\"]\n    F --> H[\"Diagnose Deadlock State s with Post s = empty\"]\n```\n\n**Reading the diagram:**\n\n- The verifier takes the system model and the LTL formula (compiled to a Büchi automaton) and synchronously explores their product.\n- If a *reachable accepting cycle* is found, the system satisfies the property.\n- If no such cycle exists, the verification produces a *lasso-shaped* counter-example that ends in the deadlock state — the exact diagnostic the engineer needs.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short-Answer Questions (3 Marks Each)\n\n#### **Q1.** `[KTU University Exam - May 2024]`\n**Define a Linear-Time (LT) property over a transition system. How is deadlock classified within the LT-property hierarchy?**\n\n**Model Answer (3 marks):**\n\nA Linear-Time property $P$ is a set of infinite execution traces over the atomic propositions of a transition system, i.e., $P \\subseteq (2^{AP})^{\\omega}$. A transition system satisfies $P$ iff all of its traces are members of $P$.  \n**Classification of deadlock:** Deadlock-freeness is a **safety property** because its violation can be detected on a finite prefix of a path (the moment the system reaches a state $s$ with $Post(s) = \\emptyset$). Within the LT-property taxonomy of Baier & Katoen, safety properties are the largest sub-class, and deadlock-freeness is a state-based instance expressed in LTL as $\\varphi_{\\text{DF}} = \\Box \\, \\neg\\, deadlock$. *(3 marks — full credit: 1 mark for the formal definition, 1 mark for the trace-subset satisfaction, 1 mark for the safety classification and the LTL formula.)*\n\n---\n\n#### **Q2.** `[KTU University Exam - Dec 2023]`\n**Differentiate between complete deadlock and partial deadlock. Give one real-world example of each.**\n\n**Model Answer (3 marks):**\n\n| Aspect | Complete Deadlock | Partial Deadlock |\n|---|---|---|\n| Definition | All processes are blocked; no transition is enabled in the global state | A subset of processes forms a cyclic dependency and is blocked, while others may still progress |\n| Process state | Every process is in a `wait` state | At least one process is `runnable` |\n| Detection | Reachability of a state $s$ with $Post(s) = \\emptyset$ | Detection of a *wait-for cycle* in the resource-allocation graph |\n| Real-world example | Two processes each holding a unique lock the other requests — neither can proceed | A 5-process dining-philosophers configuration where 4 of the 5 philosophers are deadlocked in a 4-cycle, but the 5th is still eating |\n\n*(3 marks: 1 mark definition of complete, 1 mark definition of partial, 1 mark for the examples.)*\n\n---\n\n### 5.2 Part B — Long-Answer Questions (14 Marks Each, Internal Choice)\n\n> **KTU ESE Rule (as per 2024 Scheme):** *Each Part B question carries 14 marks, split typically as 7 + 7 across two sub-parts. Students answer **ONE** of the two choices.*\n\n---\n\n#### **Question A (14 Marks)** `[KTU University Exam - May 2024]`\n\n**(a)** Define a transition system formally. With reference to the formal definition, **prove** that the LTL formula $\\Box \\, \\neg\\, deadlock$ expresses the deadlock-freeness property. *(7 marks)*\n\n**(b)** Construct the Büchi automaton corresponding to the LTL formula $\\Box \\, \\neg\\, deadlock$, and explain, with a labelled state-diagram, the product construction between this automaton and a given Kripke structure. Use a concrete 3-state example to illustrate the *trap-state encoding*. *(7 marks)*\n\n##### **Model Solution to Q-A(a):**\n\nA **transition system** is a 6-tuple $\\mathcal{TS} = (S, Act, \\to, I, AP, L)$ where:\n\n- $S$ is a finite set of states,\n- $Act$ is a set of actions,\n- $\\to \\subseteq S \\times Act \\times S$ is the transition relation,\n- $I \\subseteq S$ is the set of initial states,\n- $AP$ is a set of atomic propositions,\n- $L : S \\to 2^{AP}$ is the labelling function.\n\n**Proof that $\\Box \\neg deadlock$ expresses deadlock-freeness:**\n\nWe prove the biconditional:\n\n$$\n\\begin{aligned}\n&\\mathcal{TS} \\models \\Box \\, \\neg\\, deadlock \\\\\n\\iff&\\;\\; \\text{(definition of } \\Box \\text{ in LTL) } \\forall \\pi \\in \\text{Paths}(\\mathcal{TS}) \\;:\\; \\forall k \\ge 0 \\;:\\; deadlock \\notin L(\\pi_k) \\\\\n\\iff&\\;\\; \\forall \\pi \\in \\text{Paths}(\\mathcal{TS}) \\;:\\; \\forall k \\ge 0 \\;:\\; \\text{Post}(\\pi_k) \\neq \\emptyset \\\\\n\\iff&\\;\\; \\text{(by definition of deadlock predicate) } \\forall \\pi \\in \\text{Paths}(\\mathcal{TS}) \\;:\\; \\pi \\text{ contains no deadlock state} \\\\\n\\iff&\\;\\; \\mathcal{TS} \\text{ is deadlock-free.}\n\\end{aligned}\n$$\n\nThe first biconditional is the LTL semantics of $\\Box$ (path quantifier is universal by default in LTL). The second uses the trap-state construction that places $deadlock \\in L(s)$ iff $Post(s) = \\emptyset$. The third is the definition of deadlock. **Hence proved.** *(7 marks: 2 for the TS definition, 3 for the chain of equivalences, 2 for the concluding statement.)*\n\n##### **Model Solution to Q-A(b):**\n\n**Büchi automaton $\\mathcal{B}_{\\Box \\neg deadlock}$:**\n\n$$\\mathcal{B} = (Q, \\Sigma, \\delta, Q_0, F)$$\n\nwith:\n\n- $Q = \\lbrace q_0 \\rbrace$,\n- $\\Sigma = 2^{AP}$,\n- $\\delta(q_0, X) = \\lbrace q_0 \\rbrace$ if $deadlock \\notin X$, else $\\delta(q_0, X) = \\emptyset$,\n- $Q_0 = \\lbrace q_0 \\rbrace$,\n- $F = \\lbrace q_0 \\rbrace$ (Büchi acceptance: $q_0$ must be visited infinitely often — trivially true since the only state IS $q_0$).\n\n**Product construction:** $\\mathcal{TS} \\otimes \\mathcal{B}$ has states $S \\times Q$ and a transition $(s, q) \\to (s', q')$ iff $s \\to s'$ in $\\mathcal{TS}$ and $q' \\in \\delta(q, L(s'))$. A path of the product is accepting iff it visits $S \\times F = S \\times \\lbrace q_0 \\rbrace$ infinitely often. If **no** such path exists, the model checker reports a counter-example — a finite prefix ending in a deadlock state $s$ with $deadlock \\in L(s)$.\n\n**3-state example (trap-state encoding):**\n\nOriginal states: $s_0$ (initial), $s_1$ (intermediate), $s_2$ (deadlock).  \nRewired with trap:\n\n- $s_0 \\to s_1$, $s_1 \\to s_2$ (and $s_2 \\to s_2$ in the modified system).\n- $L(s_0) = \\emptyset$, $L(s_1) = \\lbrace working \\rbrace$, $L(s_2) = \\lbrace deadlock \\rbrace$.\n\nThe product $\\mathcal{TS} \\otimes \\mathcal{B}$ contains the state $(s_2, q_0)$. At this state, the transition reads $L(s_2) = \\lbrace deadlock \\rbrace$, and $\\delta(q_0, \\lbrace deadlock \\rbrace) = \\emptyset$. Hence the product is *empty* from this point on — no accepting infinite run is possible, and the system **fails** $\\Box \\neg deadlock$. The counter-example path returned to the user is $\\pi = s_0 \\to s_1 \\to s_2$. *(7 marks: 2 for the Büchi definition, 2 for the product description, 3 for the worked 3-state example.)*\n\n---\n\n#### **Question B (14 Marks)** `[KTU University Exam - Dec 2023]`\n\n**(a)** Explain the **safety–liveness** decomposition of LT properties as given by Baier & Katoen. Show, with a proof, that **deadlock-freeness** is a safety property. *(7 marks)*\n\n**(b)** Consider a 3-process mutual-exclusion system with the buggy rule *\"if a process is in the trying state and the lock is free, it may enter the critical section\"*. Construct the reachable state space, identify the deadlock states, and write the LTL property whose violation would detect the bug. Provide one counter-example trace. *(7 marks)*\n\n##### **Model Solution to Q-B(a):**\n\n**Safety–Liveness decomposition (Baier & Katoen, *Principles of Model Checking*, Chapter 6):**\n\nAn LT property $P_{safe} \\subseteq (2^{AP})^\\omega$ is a **safety property** if for every word $\\sigma \\notin P_{safe}$, there exists a *finite prefix* $\\hat{\\sigma}$ of $\\sigma$ such that no extension of $\\hat{\\sigma}$ is in $P_{safe}$. The set of all such *minimal bad prefixes* is denoted $\\text{MinBadPref}(P_{safe})$.\n\nAn LT property $P_{live} \\subseteq (2^{AP})^\\omega$ is a **liveness property** if for every finite word $w \\in (2^{AP})^*$ there exists an extension $\\sigma$ of $w$ with $\\sigma \\in P_{live}$. Equivalently, $P_{live}$ has *no* bad prefixes.\n\n**Theorem:** Every LT property $P$ can be decomposed as $P = P_{safe} \\cap P_{live}$.\n\n**Proof that deadlock-freeness is a safety property:**\n\nLet $P_{\\text{DF}} = \\lbrace \\sigma \\in (2^{AP})^\\omega \\mid \\forall k \\ge 0 : deadlock \\notin \\sigma_k \\rbrace$ be the deadlock-freeness LT property, where $deadlock \\in AP$ is the trap-state proposition. Take any $\\sigma \\notin P_{\\text{DF}}$. Then there exists some index $k$ with $deadlock \\in \\sigma_k$. Let $\\hat{\\sigma} = \\sigma_0 \\sigma_1 \\ldots \\sigma_k$. For any extension $u \\in (2^{AP})^\\omega$, the $k$-th symbol of $\\hat{\\sigma} u$ is still $deadlock$, so $\\hat{\\sigma} u \\notin P_{\\text{DF}}$. Hence every bad word has a *finite bad prefix* — the formal condition of safety. ∎\n\n*(7 marks: 2 marks safety definition, 2 marks liveness definition, 1 mark decomposition theorem, 2 marks the proof for deadlock-freeness.)*\n\n##### **Model Solution to Q-B(b):**\n\n**State space of 3-process mutex (states per process: N, T, C).** Total $3^3 = 27$ global states. The transition rules are:\n\n$$\n\\begin{aligned}\n(N, *, *) &\\to (T, *, *) \\quad \\text{(non-critical} \\to \\text{trying for any process)} \\\\\n(T, *, *, \\text{lock-free}) &\\to (C, *, *) \\quad \\text{(enter CS if no other process is in C)} \\\\\n(C, *, *) &\\to (N, *, *) \\quad \\text{(exit CS)}\n\\end{aligned}\n$$\n\n**Buggy rule:** The condition \"lock is free\" is not checked, so two processes can simultaneously transition $T \\to C$ — but then no transition out of the global state where two processes are in $C$ is possible, because each $C$ can only go to $N$ and the *exiting* rule was never instrumented with a guard. The state space thus has the following deadlock-reachable subset:\n\n$$\\text{Deadlock states} = \\lbrace (C, C, N), (C, N, C), (N, C, C), (C, C, T), (C, T, C), (T, C, C), (C, C, C) \\rbrace$$\n\n**LTL property to detect the bug:**\n\n$$\\varphi \\;=\\; \\Box \\, \\neg\\, deadlock$$\n\n**Counter-example trace:**\n\n$$\n\\pi_{\\text{bad}} \\;=\\; (N, N, N) \\to (T, N, N) \\to (C, N, N) \\to (C, T, N) \\to (C, C, N)\n$$\n\nThe final state $(C, C, N)$ is in the deadlock set, and the trace length is finite (5 states), confirming the safety nature of the violation. The model checker will return this trace as the diagnostic. *(7 marks: 2 marks for state-space construction, 2 marks for deadlock identification, 1 mark for the LTL formula, 2 marks for the counter-example trace.)*\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Where Students Lose Marks on Deadlock Questions:**\n>\n> 1. **Conflating deadlock with starvation** — Deadlock is a *static* (state) property; starvation is a *temporal* (liveness) property. Do not interchange them.\n> 2. **Writing $\\Diamond \\neg deadlock$ instead of $\\Box \\neg deadlock$** — This reverses the meaning. The negation pattern is a heavily tested trap.\n> 3. **Forgetting the trap-state encoding** — Without introducing a self-loop on deadlock states, the transition system is *not* infinite-state-alphabet-friendly and the LTL encoding is not well-defined.\n> 4. **Omitting the universal path quantifier** — In LTL, the universal path quantifier is implicit. Writing $\\exists \\pi : \\pi \\models \\Box \\neg deadlock$ is technically the correct semantic form, but in LTL syntax, one writes just $\\Box \\neg deadlock$ and assumes the universal quantifier externally.\n> 5. **Skipping the proof of the safety-classification** — KTU's 2024 scheme explicitly tests proof-style answers (Bloom's *Analyze / Evaluate* level) and a verbal statement without the bad-prefix argument will attract only 2 of the 7 marks.\n\n---\n\n### 5.3 Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- **LT Property:** Subset of $(2^{AP})^\\omega$; satisfied iff *all* paths of $\\mathcal{TS}$ belong to it.\n- **Deadlock predicate (state level):** $Post(s) = \\emptyset$.\n- **Deadlock-freeness LTL formula:** $\\varphi_{\\text{DF}} = \\Box \\, \\neg\\, deadlock$.\n- **Negation:** $\\neg \\varphi_{\\text{DF}} = \\Diamond \\, deadlock$ — the canonical violation is a *finite* witness ending in a deadlock state.\n- **Classification:** *Safety* property — every bad word has a *bad prefix*; witness is finite.\n- **Encoding trick:** Introduce a trap state $s_\\bot$ and label it with the atomic proposition $deadlock$.\n- **Büchi automaton for $\\Box \\neg p$:** single accepting state, self-loops on $p$-free symbols, no outgoing transition on $p$-containing symbols.\n- **Product automaton:** $\\mathcal{TS} \\otimes \\mathcal{B}_\\varphi$; an *accepting run* of the product corresponds to a *satisfying path* in $\\mathcal{TS}$.\n- **Distinction table to memorize:** Deadlock $\\neq$ Starvation $\\neq$ Livelock $\\neq$ Partial Deadlock.\n- **Real-world engines:** SPIN (`never { /* deadlock */ }`), NuSMV (`LTLSPEC G !deadlock`), UPPAAL (`A[] not deadlock`).\n- **Time complexity:** $\\mathcal{O}(\\vert \\mathcal{TS} \\vert \\cdot 2^{\\mathcal{O}(\\vert \\varphi \\vert)})$ — linear in the model, exponential in the formula.\n- **Common exam trap:** \"Express deadlock in LTL\" → $\\Box \\neg deadlock$, **not** $\\neg \\Diamond deadlock$ (these are equivalent, but the first is the canonical form).\n- **Examiner favourite line:** *\"A deadlock-free system may still violate progress\"* — never lose sight of the safety-vs-liveness boundary.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t13",
          "title": "Linear-Time Behavior - Paths and State Graph",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Linear-Time Behavior - Paths and State Graph\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of Linear-Time Behavior\n\nIn the formal framework of **automated verification** and **model checking**, a hardware or software system is represented as a finite **state graph** (also called a **transition system** or **Kripke structure**). The system’s *linear-time behavior* is the set of all possible *execution paths* (traces) that the system could follow as time progresses linearly from an initial state.\n\n> [!IMPORTANT]\n> **Linear-Time Behavior (KTU 2024 Definition)**\n>\n> The **linear-time behavior** of a transition system $\\mathcal{TS}$ is the collection of all *infinite* sequences of states (called *paths* or *traces*) that can be generated by repeatedly following the transition relation of the system, starting from any of its initial states. Each path represents one possible execution of the system over time, viewed as a discrete, totally ordered timeline $\\mathbb{N} = \\{0, 1, 2, \\ldots\\}$.\n\n### 1.1.1 State Graph (Transition System)\n\nFormally, a state graph is a 6-tuple:\n\n$$\n\\mathcal{TS} = (S, \\; Act, \\; \\rightarrow, \\; I, \\; AP, \\; L)\n$$\n\nWhere:\n- $S$ — a **finite** non-empty set of **states**.\n- $Act$ — a set of **action labels** (often omitted in pure Kripke structures).\n- $\\rightarrow \\;\\subseteq\\; S \\times Act \\times S$ — the **transition relation**.\n- $I \\;\\subseteq\\; S$ — the set of **initial states**.\n- $AP$ — a set of **atomic propositions** (Boolean variables describing state properties).\n- $L : S \\rightarrow 2^{AP}$ — a **labeling function** that assigns to every state the set of atomic propositions true in that state.\n\n> [!NOTE]\n> When $Act$ is empty and the transition relation is unlabeled, the structure is called a **Kripke Structure** $\\mathcal{M} = (S, R, I, AP, L)$, the standard model for LTL/CTL verification.\n\n### 1.1.2 Path (Execution Trace)\n\nA **path** (or **execution trace**) starting from a state $s_0 \\in S$ is an infinite sequence:\n\n$$\n\\pi = s_0 \\xrightarrow{a_0} s_1 \\xrightarrow{a_1} s_2 \\xrightarrow{a_2} s_3 \\xrightarrow{a_3} \\ldots\n$$\n\nsuch that for every $i \\ge 0$:\n\n$$\n(s_i, \\; a_i, \\; s_{i+1}) \\;\\in\\; \\rightarrow\n$$\n\nEquivalently, the path is an element of $S^{\\omega}$ (the set of all infinite sequences over $S$). The notation $s_0 \\xrightarrow{a_0 a_1 a_2 \\ldots} s_{\\omega}$ is also common.\n\n### 1.1.3 Path Notation and Slicing\n\n- $\\pi[i]$ — denotes the $(i+1)$-th state in the path (zero-indexed), i.e., $\\pi[i] = s_i$.\n- $\\pi^{i}$ — denotes the **path suffix** starting at position $i$, i.e., $\\pi^{i} = s_i, s_{i+1}, s_{i+2}, \\ldots$\n- $\\mathrm{first}(\\pi) = s_0$ — the first state of the path.\n- $\\mathrm{Trace}(s)$ — the set of all paths starting from state $s$.\n- $\\mathrm{Trace}(\\mathcal{TS}) = \\bigcup_{s \\in I} \\mathrm{Trace}(s)$ — the linear-time behavior of the entire system.\n\n> [!TIP]\n> **Board Exam Tip (KTU Pattern):** Always explicitly state the **infinite** nature of paths. KTU examiners specifically look for the $\\omega$-word (omega-word) interpretation. A *finite* sequence is called a **path fragment**, **prefix**, or **computation** — not a *path*.\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine a **metro rail network map** of a city:\n\n- **Stations** = *States* in the state graph. Each station has a name and a set of properties (e.g., \"has free Wi-Fi\", \"is an interchange\").\n- **Tracks between stations** = *Transitions* in the state graph.\n- **An actual metro ride** = a *Path* — once a passenger boards a train, the journey is an infinite, never-ending sequence of stations (because the train keeps running, looping back along the network).\n- **Every possible ride starting from \"Central Station\"** = the *Linear-time behavior* of the system from that initial state.\n\nThe *state graph* is the **map**, the *path* is the **ride**, and *linear-time behavior* is the **set of all possible rides**. Just as two passengers boarding at \"Central Station\" can take completely different rides, a non-deterministic transition system generates many paths from the same starting state.\n\n> [!IMPORTANT]\n> **Key Distinction — LTL vs. CTL**\n>\n> - **Linear-Time Logic (LTL)** reasons about *individual paths* (\"on *all* executions, the request is *eventually* granted\").\n> - **Computation Tree Logic (CTL)** reasons about the *branching tree* of all paths simultaneously (\"on *some* execution path, the request is *eventually* granted\").\n>\n> This module focuses on **LTL-style** linear-time reasoning.\n\n## 1.3 Deadlocks and Terminal States\n\n> [!WARNING]\n> A state $s \\in S$ is called a **deadlock state** (or *terminal state*) if it has **no outgoing transition**, i.e., $\\mathrm{Post}(s) = \\{ s' \\in S \\mid s \\rightarrow s' \\} = \\emptyset$. In such a state, no path can be extended infinitely. KTU examiners often test the student's ability to identify such states in a given state graph.\n\nTo ensure every path is genuinely infinite, the state graph must be **deadlock-free**, or each deadlock state must be equipped with a self-loop $s \\rightarrow s$.\n\n## 1.4 Linear-Time Properties\n\nA **linear-time property** $P$ over $AP$ is a set of infinite words over $2^{AP}$, i.e., $P \\subseteq (2^{AP})^{\\omega}$. The transition system $\\mathcal{TS}$ *satisfies* the property $P$ (written $\\mathcal{TS} \\models P$) if and only if every path of $\\mathcal{TS}$ belongs to $P$:\n\n$$\n\\mathcal{TS} \\;\\models\\; P \\quad \\iff \\quad \\mathrm{Trace}(\\mathcal{TS}) \\;\\subseteq\\; P\n$$\n\n> [!NOTE]\n> The labeling function $L$ converts a state-sequence $\\pi = s_0 s_1 s_2 \\ldots$ into the corresponding *propositional trace* $L(\\pi) = L(s_0) L(s_1) L(s_2) \\ldots \\in (2^{AP})^{\\omega}$. This is the object on which LTL formulas are evaluated.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Structural Decomposition of the Concept\n\nThe concept of *linear-time behavior* is built from the following layered hierarchy:\n\n- **Layer 1 — Atomic Level (State)**\n  - A state $s \\in S$ is a snapshot of all system variables at one instant of time.\n  - Each state carries a set of true atomic propositions $L(s) \\subseteq AP$.\n\n- **Layer 2 — Relational Level (Transition)**\n  - A transition $(s, a, s') \\in \\rightarrow$ defines a *single step* of evolution.\n  - The **post-image** of $s$ is $\\mathrm{Post}(s) = \\{ s' \\in S \\mid (s, a, s') \\in \\rightarrow \\}$.\n\n- **Layer 3 — Sequential Level (Path)**\n  - A path is an *unbroken* chain of transitions, indexed by $\\mathbb{N}$.\n  - Mathematically: $\\pi : \\mathbb{N} \\rightarrow S$ such that $\\forall i \\in \\mathbb{N} : \\pi[i] \\rightarrow \\pi[i+1]$.\n\n- **Layer 4 — Collective Level (Linear-Time Behavior)**\n  - The collection of *all* such paths, beginning from initial states, is the linear-time behavior.\n  - This set is the semantic object for LTL model checking.\n\n## 2.2 Why \"Linear-Time\"?\n\nThe qualifier *linear* refers to the fact that **time is modelled as a totally ordered set** (specifically $\\mathbb{N}$, the natural numbers), with a well-defined *next* moment. This is in contrast to *branching-time* models, where every moment in time *branches* into multiple possible futures, forming a tree.\n\n## 2.3 Key Path Operations\n\nFor a path $\\pi = s_0 s_1 s_2 \\ldots$:\n\n- **State Projection:** $\\pi[i] = s_i$.\n- **Suffix (Sub-path):** $\\pi^{i} = s_i s_{i+1} s_{i+2} \\ldots$\n- **Prefix:** $\\mathrm{pref}(\\pi, n) = s_0 s_1 \\ldots s_{n-1}$ (length $n$).\n- **State Sequence Length:** $|\\pi| = \\infty$ (paths are always infinite in LTL semantics).\n\n## 2.4 Classification of Paths\n\n| Path Type | Definition | KTU Significance |\n|-----------|-----------|------------------|\n| **Maximal Path** | A path that cannot be extended further (always infinite here). | Default in LTL. |\n| **Initial Path** | A path that begins at an initial state $s_0 \\in I$. | The *only* paths considered when checking $\\mathcal{TS} \\models P$. |\n| **Stuttering Path** | A path where a state may repeat consecutively without change in atomic propositions. | Central to *stuttering equivalence*. |\n| **Fair Path** | A path that visits every *fairness set* infinitely often. | Used in *fairness assumptions*. |\n| **Cycle / Loop** | A finite sub-sequence of states that repeats periodically within a path. | Detected by DFS in graph algorithms. |\n\n## 2.5 KTU Formula Sheet / Cheat Sheet\n\n> [!IMPORTANT]\n> The following table consolidates every formula, notation, and condition that a KTU 2024 student must memorize for this topic. Pay special attention to the *quantifier placement* and the *suffix operator*.\n\n| # | Concept | Symbol / Formula | Explanation / Condition |\n|---|---------|------------------|------------------------|\n| 1 | Transition System | $\\mathcal{TS} = (S, Act, \\rightarrow, I, AP, L)$ | 6-tuple definition. |\n| 2 | Kripke Structure | $\\mathcal{M} = (S, R, I, AP, L)$ | Transition system without actions. |\n| 3 | Transition | $(s, a, s') \\in \\rightarrow$ | Directed labelled edge. |\n| 4 | Post-Image | $\\mathrm{Post}(s) = \\{ s' \\in S \\mid s \\rightarrow s' \\}$ | All immediate successors. |\n| 5 | Pre-Image | $\\mathrm{Pre}(s) = \\{ s' \\in S \\mid s' \\rightarrow s \\}$ | All immediate predecessors. |\n| 6 | Path (Trace) | $\\pi = s_0 s_1 s_2 \\ldots \\in S^{\\omega}$ | Infinite sequence. |\n| 7 | Path Validity | $\\forall i \\ge 0 : (s_i, s_{i+1}) \\in \\rightarrow$ | Every consecutive pair is a transition. |\n| 8 | State at Position $i$ | $\\pi[i] = s_i$ | Zero-indexed lookup. |\n| 9 | Path Suffix | $\\pi^{i} = s_i s_{i+1} s_{i+2} \\ldots$ | Sub-path from index $i$. |\n| 10 | First State | $\\mathrm{first}(\\pi) = s_0$ | Starting state. |\n| 11 | Traces from State $s$ | $\\mathrm{Trace}(s) = \\{ \\pi \\in S^{\\omega} \\mid \\mathrm{first}(\\pi) = s \\}$ | All paths starting at $s$. |\n| 12 | System Traces | $\\mathrm{Trace}(\\mathcal{TS}) = \\bigcup_{s \\in I} \\mathrm{Trace}(s)$ | All initial paths. |\n| 13 | Propositional Trace | $L(\\pi) = L(s_0) L(s_1) L(s_2) \\ldots$ | $\\omega$-word over $2^{AP}$. |\n| 14 | Satisfaction | $\\mathcal{TS} \\models P \\iff \\mathrm{Trace}(\\mathcal{TS}) \\subseteq P$ | All traces satisfy $P$. |\n| 15 | Deadlock State | $\\mathrm{Post}(s) = \\emptyset$ | No outgoing transition. |\n| 16 | Reachability | $s \\in \\mathrm{Reach}(I)$ | Some path from $I$ reaches $s$. |\n| 17 | Invariant | $P$ holds in every reachable state | $\\forall s \\in \\mathrm{Reach}(I) : s \\models P$. |\n| 18 | LTL Next | $\\pi \\models X\\varphi \\iff \\pi^{1} \\models \\varphi$ | True in the *next* state. |\n| 19 | LTL Eventually | $\\pi \\models F\\varphi \\iff \\exists k \\ge 0 : \\pi^{k} \\models \\varphi$ | True in *some* future state. |\n| 20 | LTL Globally | $\\pi \\models G\\varphi \\iff \\forall k \\ge 0 : \\pi^{k} \\models \\varphi$ | True in *all* future states. |\n| 21 | LTL Until | $\\pi \\models \\varphi \\,U\\, \\psi \\iff \\exists k \\ge 0 : \\pi^{k} \\models \\psi \\land \\forall 0 \\le j < k : \\pi^{j} \\models \\varphi$ | $\\varphi$ holds until $\\psi$ becomes true. |\n\n> [!TIP]\n> **Engineering Utility:** In real-world verification, linear-time behavior is the semantic foundation of:\n> - **Hardware model checking** (e.g., verifying that a CPU pipeline never produces an invalid memory address).\n> - **Software model checking** (e.g., SPIN, NuSMV, CBMC tools).\n> - **Protocol verification** (e.g., proving that a mutual-exclusion protocol never deadlocks).\n> - **Safety-critical aerospace and automotive systems** (DO-178C, ISO 26262 standards).\n\n## 2.6 Worked Walkthrough — Identifying Linear-Time Behavior\n\nConsider a tiny system with 3 states $S = \\{s_0, s_1, s_2\\}$, initial state $I = \\{s_0\\}$, and transitions:\n- $s_0 \\rightarrow s_1$\n- $s_0 \\rightarrow s_2$\n- $s_1 \\rightarrow s_1$ (self-loop)\n- $s_2 \\rightarrow s_1$\n\nThe linear-time behavior is:\n\n$$\n\\mathrm{Trace}(\\mathcal{TS}) = \\left\\{ \\; \\pi \\in S^{\\omega} \\;\\middle|\\; \\mathrm{first}(\\pi) = s_0 \\;\\right\\}\n$$\n\nThis includes infinitely many paths such as:\n- $\\pi_1 = s_0, s_1, s_1, s_1, s_1, \\ldots$\n- $\\pi_2 = s_0, s_2, s_1, s_1, s_1, \\ldots$\n- $\\pi_3 = s_0, s_1, s_1, s_2, s_1, s_1, s_2, s_1, \\ldots$ (and infinitely many interleavings)\n\n> [!NOTE]\n> The linear-time behavior of *any* non-trivial finite-state system is an *uncountably infinite* set, because non-deterministic choices can be made at every step.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: Cardinality of $\\mathrm{Trace}(\\mathcal{TS})$\n\nWe rigorously derive the size of the linear-time behavior for a system with branching factor $b$ and no cycles.\n\n**Assumption:** Suppose $\\mathcal{TS}$ has $|I| = 1$ initial state, and at every step there are exactly $b \\ge 1$ possible successors. We also assume the system has no deadlock (every state has a successor).\n\n**Step 1.** A path is an infinite sequence $s_0, s_1, s_2, \\ldots$ where each $s_{i+1} \\in \\mathrm{Post}(s_i)$ and $|\\mathrm{Post}(s_i)| = b$.\n\n**Step 2.** The number of *length-$n$* prefixes is $b^{n}$, because at each of the $n$ steps the choice is among $b$ options.\n\n**Step 3.** The set of all infinite paths is the *limit* of the prefix sets:\n\n$$\n\\mathrm{Trace}(\\mathcal{TS}) \\;=\\; \\bigcap_{n=1}^{\\infty} \\left\\{ \\pi \\in S^{\\omega} \\;\\middle|\\; \\mathrm{pref}(\\pi, n) \\text{ is a valid length-}n \\text{ prefix} \\right\\}\n$$\n\n**Step 4.** The cardinality is:\n\n$$\n\\bigl\\vert \\mathrm{Trace}(\\mathcal{TS}) \\bigr\\vert \\;=\\; b^{\\aleph_{0}} \\;=\\; 2^{\\aleph_{0}} \\;=\\; \\mathfrak{c}\n$$\n\nwhere $\\mathfrak{c}$ denotes the cardinality of the continuum (the size of the real numbers).\n\n**Conclusion:** Even a *finite* state graph gives rise to an *uncountably infinite* linear-time behavior. This is the fundamental reason why LTL model checking must use *finite* symbolic representations (e.g., Büchi automata) rather than explicit enumeration.\n\n## 3.2 Derivation: Sufficient Condition for a Property to be a *Safety* Property\n\nA linear-time property $P$ is a *safety property* iff for every $\\sigma \\notin P$ there exists a *finite prefix* $x$ of $\\sigma$ such that *no* extension of $x$ belongs to $P$.\n\n**Mathematical Formulation:**\n\n$$\nP \\text{ is a safety property} \\quad\\iff\\quad \\forall \\sigma \\in (2^{AP})^{\\omega} : \\bigl(\\sigma \\notin P\\bigr) \\Rightarrow \\bigl(\\exists n \\ge 0 : \\forall \\tau \\in (2^{AP})^{\\omega} : \\mathrm{pref}(\\sigma, n) \\frown \\tau \\notin P \\bigr)\n$$\n\nwhere $\\frown$ denotes concatenation.\n\n**Derivation Sketch:**\n\n- **($\\Rightarrow$)** Suppose $P$ is a safety property. By definition, the *bad prefixes* $\\mathrm{BadPref}(P) = \\{ x \\in (2^{AP})^{*} \\mid \\forall \\tau : x \\frown \\tau \\notin P \\}$ is the set of finite words that cannot be extended into $P$. For each $\\sigma \\notin P$, take $x$ to be any bad prefix of $\\sigma$.\n\n- **($\\Leftarrow$)** Conversely, if such a finite witness $x$ exists for every $\\sigma \\notin P$, then we may define $\\mathrm{BadPref}(P)$ as the union of all such minimal $x$'s, and $P = (2^{AP})^{\\omega} \\setminus (\\mathrm{BadPref}(P) \\cdot (2^{AP})^{\\omega})$.\n\n**Engineering Interpretation:** Safety properties say *\"something bad never happens\"*. The finite witness $x$ is the *first moment* at which the bad event becomes inevitable. **Invariance** (e.g., \"mutual exclusion is never violated\") is the canonical safety property.\n\n## 3.3 Analytical Matrix — Comparing LTL Operators\n\nThe following matrix explicitly derives the satisfaction conditions for the four core LTL path operators. Let $\\pi = s_0 s_1 s_2 \\ldots$ be a path and $\\varphi, \\psi$ be LTL formulas.\n\n| Operator | Formula | Satisfaction Condition on Path $\\pi$ | Plain-English Meaning |\n|----------|---------|--------------------------------------|----------------------|\n| **Next (X)** | $X\\varphi$ | $\\pi \\models X\\varphi \\iff \\pi[1] \\models \\varphi$ | $\\varphi$ holds in the very next state. |\n| **Globally (G)** | $G\\varphi$ | $\\pi \\models G\\varphi \\iff \\forall k \\ge 0 : \\pi[k] \\models \\varphi$ | $\\varphi$ holds in *every* state of the path, now and forever. |\n| **Finally / Eventually (F)** | $F\\varphi$ | $\\pi \\models F\\varphi \\iff \\exists k \\ge 0 : \\pi[k] \\models \\varphi$ | $\\varphi$ holds in *some* future state (now or later). |\n| **Until (U)** | $\\varphi \\, U \\, \\psi$ | $\\pi \\models \\varphi U \\psi \\iff \\exists k \\ge 0 : \\bigl(\\pi[k] \\models \\psi \\land \\forall j, 0 \\le j < k : \\pi[j] \\models \\varphi\\bigr)$ | $\\varphi$ holds *continuously* until the *first* moment $\\psi$ becomes true. |\n| **Release (R)** | $\\varphi \\, R \\, \\psi$ | $\\pi \\models \\varphi R \\psi \\iff \\forall k \\ge 0 : \\bigl(\\pi[k] \\models \\psi \\lor \\exists j < k : \\pi[j] \\models \\varphi \\bigr)$ | Dual of Until: $\\psi$ holds *until and including* the moment $\\varphi$ becomes true (or $\\varphi$ never does). |\n| **Weak Until (W)** | $\\varphi \\, W \\, \\psi$ | $\\pi \\models \\varphi W \\psi \\iff (\\pi \\models G\\varphi) \\lor (\\pi \\models \\varphi U \\psi)$ | $\\varphi$ holds until $\\psi$ happens, *or* $\\varphi$ holds forever. |\n\n## 3.4 Derivation: Equivalence of $F$ and $U$\n\nWe show the identity $F\\varphi \\equiv \\mathrm{true} \\, U \\, \\varphi$:\n\n**Step 1.** $(\\Rightarrow)$ Assume $\\pi \\models F\\varphi$. Then by definition, $\\exists k \\ge 0 : \\pi[k] \\models \\varphi$.\n\n**Step 2.** Choose $k$ as the smallest such index. Then for all $0 \\le j < k$, $\\pi[j] \\models \\mathrm{true}$ (trivially). And $\\pi[k] \\models \\varphi$. This is exactly the condition for $\\pi \\models \\mathrm{true} \\, U \\, \\varphi$.\n\n**Step 3.** $(\\Leftarrow)$ Assume $\\pi \\models \\mathrm{true} \\, U \\, \\varphi$. Then $\\exists k \\ge 0 : \\pi[k] \\models \\varphi$ and for all $0 \\le j < k$, $\\pi[j] \\models \\mathrm{true}$. The first condition alone gives $\\pi \\models F\\varphi$.\n\n**Conclusion:**\n\n$$\nF\\varphi \\;\\equiv\\; \\mathrm{true} \\, U \\, \\varphi\n$$\n\nThis identity is critical when converting LTL to Büchi automata — it lets the converter treat $F$ and $U$ uniformly.\n\n## 3.5 Derivation: Stuttering Equivalence\n\nTwo paths $\\pi$ and $\\pi'$ are *stuttering equivalent* (denoted $\\pi \\sim_{st} \\pi'$) iff they can be obtained from each other by *merging* or *splitting* consecutive identical labelled states.\n\n**Formal Definition:** $\\pi \\sim_{st} \\pi'$ iff there exist two strictly increasing index sequences $0 = i_0 < i_1 < i_2 < \\ldots$ and $0 = j_0 < j_1 < j_2 < \\ldots$ such that for all $k \\ge 0$:\n\n$$\nL(\\pi[i_k]) \\;=\\; L(\\pi[i_k + 1]) \\;=\\; \\ldots \\;=\\; L(\\pi[i_{k+1} - 1]) \\;=\\; L(\\pi'[j_k]) \\;=\\; \\ldots \\;=\\; L(\\pi'[j_{k+1} - 1])\n$$\n\n**Key Theorem:** Two transition systems are *stuttering equivalent* iff they satisfy exactly the same set of LTL formulas that do *not* use the $X$ (next-time) operator.\n\n$$\n\\mathcal{TS}_1 \\sim_{st} \\mathcal{TS}_2 \\quad\\iff\\quad \\forall \\varphi \\in \\mathrm{LTL} \\setminus X : \\bigl(\\mathcal{TS}_1 \\models \\varphi \\iff \\mathcal{TS}_2 \\models \\varphi\\bigr)\n$$\n\n**Engineering Utility:** This theorem justifies *abstraction*: a verifier can replace complex sub-modules by simpler \"summary\" states that share the same atomic-proposition label, without changing the truth value of any $X$-free LTL specification. This is the foundation of *compositional verification* and *predicate abstraction*.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Block Diagram: Conceptual Architecture of Linear-Time Verification\n\n```mermaid\nflowchart TB\n    A[\"System Model\\nTransition System TS\\nKripke Structure M\"] --> B[\"State Graph\\nNodes S\\nEdges R\\nLabels L\"]\n    B --> C[\"Linear-Time Behavior\\nSet of all infinite paths\"]\n    C --> D[\"Propositional Trace\\nL pi equals L s0 L s1 L s2 ...\"]\n    D --> E[\"LTL Formula phi\\nPath Operators X F G U R\"]\n    E --> F{\"Model Checking\\nTS satisfies phi?\"}\n    F -->|Yes| G[\"VERIFIED\\nProperty Holds on All Paths\"]\n    F -->|No| H[\"COUNTEREXAMPLE\\nFailing Path Witness\"]\n    B --> I[\"Buchi Automaton A phi\\nNegation of Specification\"]\n    I --> F\n    I --> J[\"Emptiness Check\\nLanguage A phi intersect Trace TS\"]\n    J --> H\n    H --> K[\"Root Cause Analysis\\nTrace back to source line\"]\n```\n\n## 4.2 State Graph — Worked Example\n\n```mermaid\nflowchart LR\n    s0((\"s0\\n{a}\\nINITIAL\")) -->|alpha| s1((\"s1\\n{a, b}\"))\n    s1 -->|beta| s2((\"s2\\n{c}\"))\n    s2 -->|gamma| s1\n    s2 -->|delta| s3((\"s3\\n{b, c}\\nDEADLOCK\"))\n    s0 -.->|next step| sX([\"...\"])\n    s1 -.->|next step| sY([\"...\"])\n    s3 -.->|self loop required| s3\n```\n\n> [!NOTE]\n> **Reading the diagram:** Each node is a state, with its name, the set of atomic propositions $L(s)$ true in it, and an indicator if it is the *initial* state (double circle in formal texts) or a *deadlock* state. Each arrow is a labelled transition. The dotted arrows on the right represent the *continuation* of the infinite path beyond the visible fragment. Note that $s_3$ has no outgoing transition in the original graph; to make all paths infinite, it must be equipped with a self-loop $s_3 \\rightarrow s_3$.\n\n## 4.3 Path Tree — Unfolding of the State Graph\n\n```mermaid\nflowchart TD\n    root([\"Trace TS\\nAll paths from s0\"]) --> pA[\"Path A\\ns0 -> s1 -> s2 -> s1 -> s2 -> ...\"]\n    root --> pB[\"Path B\\ns0 -> s1 -> s2 -> s3 -> s3 -> s3 -> ...\"]\n    root --> pC[\"Path C\\ns0 -> s1 -> s2 -> s1 -> s2 -> s3 -> s3 -> ...\"]\n    root --> pD[\"Path D\\ns0 -> s1 -> s1 -> s1 -> ...\\nif s1 self-loop existed\"]\n```\n\n> [!TIP]\n> **Visualization Strategy:** The path tree rooted at the initial state grows downward. Each *level* corresponds to a *time step*. Each *branch* corresponds to a *non-deterministic choice*. An *infinite* path corresponds to a *root-to-leaf-infinity* branch in this tree. KTU questions often ask students to *enumerate* the first few steps of all paths in such a tree.\n\n## 4.4 Functional Block Diagram — Verification Toolchain\n\n```mermaid\nflowchart LR\n    src[\"Source Code\\nor Hardware RTL\"] --> parse[\"Parser /\\nFront-End\"]\n    parse --> kripke[\"Kripke Structure\\nM equals S R I AP L\"]\n    spec[\"Specification\\nLTL Formula phi\"] --> nba[\"Buchi Automaton\\nA sub not phi\"]\n    kripke --> product[\"Product\\nTS x A sub not phi\"]\n    nba --> product\n    product --> dfs[\"DFS /\\nEmptiness Check\"]\n    dfs -->|Empty| sat[\"SATISFIED\\nTS satisfies phi\"]\n    dfs -->|Non-Empty| cex[\"Counterexample\\nFailing Path\"]\n    cex --> diag[\"Diagnostic\\nVisualizer\"]\n```\n\n> [!IMPORTANT]\n> **Engineering Reality:** This is the *actual* architecture used by industrial model checkers such as **SPIN** (for LTL), **NuSMV**, **CBMC**, and **Cadence JasperGold**. The \"emptiness check\" on the product automaton is a *graph-theoretic* operation (nested DFS, strongly-connected-components, etc.) that runs in $O(|S| + |R|)$ time. This is the *only* tractable way to verify properties over an uncountable set of paths.\n\n## 4.5 Decision Matrix — LTL Operator Truth Tables\n\nThe following compact matrix shows, for each combination of the *current* state and the *next* state label, whether the LTL formula is satisfied at that position on the path.\n\n| Current $L(s_i)$ | Next $L(s_{i+1})$ | $X \\varphi$ | $F \\varphi$ | $G \\varphi$ | $\\varphi \\, U \\, \\psi$ |\n|------------------|------------------|-------------|-------------|-------------|---------------------|\n| $\\varphi$ true | $\\varphi$ true | T | T | T (locally) | depends on $\\psi$ |\n| $\\varphi$ true | $\\varphi$ false | F | T | F | T until $\\psi$ |\n| $\\varphi$ false | $\\varphi$ true | T | T | F | F (must reset) |\n| $\\varphi$ false | $\\varphi$ false | F | depends on rest | F | F |\n\n> [!NOTE]\n> In KTU answers, always justify *why* an operator is satisfied or violated by referring back to the *temporal scope* (now / next / eventually / always) and the *truth value* of the sub-formula at each relevant position.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Question 1\n> **[KTU University Exam — Dec 2023, Model Question Paper, Module 1]**\n> **Define a transition system (state graph). With a suitable example, illustrate the difference between a *path* and a *trace* of a transition system.**\n\n**Model Answer (3 marks):**\n\nA *transition system* (or *state graph*) is a 6-tuple $\\mathcal{TS} = (S, Act, \\rightarrow, I, AP, L)$ where $S$ is a finite set of states, $Act$ is a set of actions, $\\rightarrow \\subseteq S \\times Act \\times S$ is the transition relation, $I \\subseteq S$ is the set of initial states, $AP$ is a set of atomic propositions, and $L: S \\rightarrow 2^{AP}$ is the labeling function. **(1 mark)**\n\nA *path* (also called an *execution trace*) is an infinite sequence of states $\\pi = s_0 s_1 s_2 \\ldots$ such that for every $i \\ge 0$, $(s_i, a_i, s_{i+1}) \\in \\rightarrow$ for some action $a_i$. A path represents *one* possible execution of the system. **(1 mark)**\n\n**Difference (1 mark):** A *trace* is the *set of all paths* starting from a given state (or set of initial states). In the literature, however, the two terms are often used interchangeably: an individual path is also called a trace, and a set of paths is also called a *set of traces* or *linear-time behavior*. The key idea is that the trace set captures *all* possible executions.\n\n---\n\n### Question 2\n> **[KTU University Exam — May 2024, Sample Paper, Module 1]**\n> **Explain the concept of a *deadlock state* in a transition system. Why is it important to handle deadlock states when defining linear-time behavior? Can an LTL property be evaluated on a path that ends in a deadlock? Justify your answer.**\n\n**Model Answer (3 marks):**\n\nA *deadlock state* $s \\in S$ is a state that has no outgoing transitions, i.e., $\\mathrm{Post}(s) = \\emptyset$. **(1 mark)**\n\n**Importance:** Because linear-time behavior and LTL are defined over *infinite* paths $\\pi \\in S^{\\omega}$, a path that reaches a deadlock state cannot be extended indefinitely. If the model contains deadlocks, the linear-time behavior is *not* a clean subset of $S^{\\omega}$, and LTL operators like $G\\varphi$ and $F\\varphi$ become ill-defined on terminating paths. **(1 mark)**\n\n**Answer to the third part:** No, by the standard semantics, an LTL property is *never* evaluated on a finite or deadlocked path. The standard convention is either to:\n- (a) equip every deadlock state with a *self-loop* $s \\rightarrow s$ (so that any finite path can be extended to an infinite stuttering path), or\n- (b) restrict the analysis to deadlock-free models.\n\nThis convention is essential to make the LTL satisfaction relation $\\pi \\models \\varphi$ well-defined for *all* paths. **(1 mark)**\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks Each, Internal Choice)\n\n> **KTU 2024 Scheme Pattern:** Each Part-B question has two sub-parts, typically (a) 7 marks and (b) 7 marks. The student must answer **either** Question A **or** Question B in full.\n\n### **Question A (14 Marks)**\n\n> **[KTU University Exam — Dec 2023, Adapted]**\n\n**(a)** Consider the Kripke structure $\\mathcal{M} = (S, R, I, AP, L)$ shown below, where $AP = \\{p, q, r\\}$.\n\n```\nStates:    s0  s1  s2  s3  s4\nL(s0) = {}       (initial state)\nL(s1) = {p}\nL(s2) = {p, q}\nL(s3) = {q, r}\nL(s4) = {r}\n\nTransitions:\ns0 -> s1\ns0 -> s2\ns1 -> s2\ns2 -> s2    (self-loop)\ns2 -> s3\ns3 -> s4\ns4 -> s4    (self-loop)\n```\n\n**(i)** Draw the state graph. **(2 marks)**\n**(ii)** Enumerate *all* maximal paths starting from the initial state $s_0$. **(3 marks)**\n**(iii)** Identify the deadlock states, if any. **(2 marks)**\n\n**(b)** For the same Kripke structure, evaluate the following LTL formulas on the system. For each formula, state whether $\\mathcal{M} \\models \\varphi$ holds and provide a justification by referring to the relevant path(s). **(7 marks)**\n\n**(i)** $\\varphi_1 = G(p \\lor r)$ — *Invariant: p or r always holds.*\n**(ii)** $\\varphi_2 = F q$ — *Eventually, q holds.*\n**(iii)** $\\varphi_3 = G(p \\rightarrow F q)$ — *Whenever p holds, q will eventually hold.*\n\n---\n\n### **Model Answer for Question A**\n\n**(a)(i) State Graph (2 marks):**\n\n```mermaid\nflowchart LR\n    s0((\"s0\\n{}\\nINIT\")) --> s1((\"s1\\n{p}\"))\n    s0 --> s2((\"s2\\n{p,q}\"))\n    s1 --> s2\n    s2 --> s2\n    s2 --> s3((\"s3\\n{q,r}\"))\n    s3 --> s4((\"s4\\n{r}\"))\n    s4 --> s4\n```\n\n**(a)(ii) Maximal Paths from $s_0$ (3 marks):**\n\nThe linear-time behavior of $\\mathcal{M}$ consists of the following infinite paths:\n\n- $\\pi_1 = s_0, s_1, s_2, s_2, s_2, s_2, \\ldots$ (take $s_0 \\rightarrow s_1$, then $s_1 \\rightarrow s_2$, then stay in $s_2$ forever via the self-loop).\n- $\\pi_2 = s_0, s_2, s_2, s_2, s_2, \\ldots$ (take $s_0 \\rightarrow s_2$, then self-loop).\n- $\\pi_3 = s_0, s_1, s_2, s_3, s_4, s_4, s_4, s_4, \\ldots$\n- $\\pi_4 = s_0, s_2, s_3, s_4, s_4, s_4, s_4, \\ldots$\n- $\\pi_5 = s_0, s_1, s_2, s_2, \\ldots, s_2, s_3, s_4, s_4, s_4, \\ldots$ (any number of self-loops at $s_2$, then leave).\n- $\\pi_6 = s_0, s_2, s_2, \\ldots, s_2, s_3, s_4, s_4, s_4, \\ldots$ (similar).\n\nIn total, the trace set is uncountable (as shown in §3.1).\n\n**(a)(iii) Deadlock States (2 marks):**\n\nInspecting each state:\n- $\\mathrm{Post}(s_0) = \\{s_1, s_2\\} \\neq \\emptyset$\n- $\\mathrm{Post}(s_1) = \\{s_2\\} \\neq \\emptyset$\n- $\\mathrm{Post}(s_2) = \\{s_2, s_3\\} \\neq \\emptyset$\n- $\\mathrm{Post}(s_3) = \\{s_4\\} \\neq \\emptyset$\n- $\\mathrm{Post}(s_4) = \\{s_4\\} \\neq \\emptyset$ (self-loop)\n\n**Conclusion:** There are **no deadlock states** in this Kripke structure. Every state has at least one outgoing transition.\n\n---\n\n**(b)(i) $\\varphi_1 = G(p \\lor r)$ (2 marks):**\n\nWe must check that $p \\lor r$ holds in *every* state of *every* path.\n\nState labels: $L(s_0)=\\{\\}$, $L(s_1)=\\{p\\}$, $L(s_2)=\\{p,q\\}$, $L(s_3)=\\{q,r\\}$, $L(s_4)=\\{r\\}$.\n\n- $s_0$: $p \\lor r$? Both false. $\\varphi_1$ **violated** at position $i = 0$.\n\nSince $\\pi_1$ and $\\pi_2$ both start at $s_0$, $\\mathcal{M} \\not\\models G(p \\lor r)$.\n\n**Verdict: $\\mathcal{M} \\not\\models \\varphi_1$ (FAILS).**\n\n**(b)(ii) $\\varphi_2 = F q$ (2 marks):**\n\nWe must check that $q$ holds in *some* state of *every* path.\n\n- $\\pi_1 = s_0, s_1, s_2, s_2, \\ldots$: at $s_2$, $q$ is true. ✓\n- $\\pi_2 = s_0, s_2, s_2, \\ldots$: at $s_2$, $q$ is true. ✓\n- $\\pi_3, \\pi_4, \\pi_5, \\pi_6$: at $s_2$ or $s_3$, $q$ is true. ✓\n\n**Verdict: $\\mathcal{M} \\models \\varphi_2$ (HOLDS).**\n\n**(b)(iii) $\\varphi_3 = G(p \\rightarrow F q)$ (3 marks):**\n\nWe must check that *whenever* $p$ holds, $q$ holds eventually *after* that position.\n\n- At $s_0$: $p$ is false, so $p \\rightarrow F q$ is vacuously true.\n- At $s_1$: $p$ is true; $F q$ must hold in $\\pi^{1}$ (from $s_1$ onwards). In every path, $s_1$ is followed by $s_2$ where $q$ is true. ✓\n- At $s_2$: $p$ is true; from $s_2$ onwards, $q$ is true (either we stay at $s_2$ where $q$ is true, or go to $s_3$ where $q$ is true). ✓\n- At $s_3, s_4$: $p$ is false, vacuously true.\n\n**Verdict: $\\mathcal{M} \\models \\varphi_3$ (HOLDS).**\n\n---\n\n### **Question B (14 Marks) — Alternative Choice**\n\n> **[KTU University Exam — May 2024, Adapted]**\n\n**(a)** Define the following terms with one example each: **(7 marks)**\n**(i)** State graph (Kripke structure)\n**(ii)** Path and its suffix operation\n**(iii)** Stuttering equivalence\n**(iv)** Deadlock state\n\n**(b)** Consider the following Kripke structure $\\mathcal{M}'$ modelling a traffic light:\n\n```\nStates:    red, yellow, green\nL(red)    = {light_is_red}\nL(yellow) = {light_is_yellow}\nL(green)  = {light_is_green}\nTransitions: red -> green, green -> yellow, yellow -> red\nInitial state: red\n```\n\n**(i)** Draw the state graph. **(2 marks)**\n**(ii)** List the unique maximal path(s). **(2 marks)**\n**(iii)** Determine whether the following properties hold on $\\mathcal{M}'$: **(3 marks)**\n- $P_1$: $G(\\mathrm{light\\_is\\_red} \\lor \\mathrm{light\\_is\\_yellow} \\lor \\mathrm{light\\_is\\_green})$ — *The light is always in exactly one of the three states.*\n- $P_2$: $G(\\mathrm{light\\_is\\_red} \\rightarrow X X \\, \\mathrm{light\\_is\\_red})$ — *If the light is red, it will be red again two steps later.*\n\n---\n\n### **Model Answer for Question B**\n\n**(a)(i) State Graph / Kripke Structure (1.75 marks):**\n\nA *Kripke structure* $\\mathcal{M} = (S, R, I, AP, L)$ is a labelled directed graph where $S$ is a finite non-empty set of states, $R \\subseteq S \\times S$ is a (total) transition relation, $I \\subseteq S$ is the set of initial states, $AP$ is a set of atomic propositions, and $L: S \\rightarrow 2^{AP}$ is a labelling function. *Example:* A two-state toggle switch with $S = \\{ \\mathrm{on}, \\mathrm{off} \\}$, $R = \\{ (\\mathrm{on}, \\mathrm{off}), (\\mathrm{off}, \\mathrm{on}) \\}$, $I = \\{\\mathrm{on}\\}$, $AP = \\{\\mathrm{power}\\}$, $L(\\mathrm{on}) = \\{\\mathrm{power}\\}$, $L(\\mathrm{off}) = \\emptyset$.\n\n**(a)(ii) Path and Suffix (1.75 marks):**\n\nA *path* is an infinite sequence $\\pi = s_0 s_1 s_2 \\ldots \\in S^{\\omega}$ such that $\\forall i \\ge 0: (s_i, s_{i+1}) \\in R$. The *suffix* $\\pi^{k}$ is the sub-path starting at position $k$, i.e., $\\pi^{k} = s_k s_{k+1} s_{k+2} \\ldots$. *Example:* If $\\pi = s_0, s_1, s_2, s_3, s_4, \\ldots$, then $\\pi^{2} = s_2, s_3, s_4, \\ldots$\n\n**(a)(iii) Stuttering Equivalence (1.75 marks):**\n\nTwo paths $\\pi, \\pi'$ are *stuttering equivalent* ($\\pi \\sim_{st} \\pi'$) iff they can be obtained from each other by inserting or removing consecutive duplicate state labels. *Example:* $\\pi = a, a, b, c, c, c, d$ and $\\pi' = a, b, c, d$ are stuttering equivalent.\n\n**(a)(iv) Deadlock State (1.75 marks):**\n\nA state $s$ is a *deadlock* iff $\\mathrm{Post}(s) = \\emptyset$. *Example:* A printer whose paper-tray sensor reports \"empty\" and has no recovery transition is a deadlock state — once entered, no further behaviour is possible.\n\n---\n\n**(b)(i) State Graph (2 marks):**\n\n```mermaid\nflowchart LR\n    red((\"red\\n{red}\\nINIT\")) --> green((\"green\\n{green}\"))\n    green --> yellow((\"yellow\\n{yellow}\"))\n    yellow --> red\n```\n\n**(b)(ii) Unique Maximal Path (2 marks):**\n\nBecause the traffic light cycles deterministically (each state has exactly one successor), there is exactly **one** maximal path:\n\n$$\n\\pi = \\mathrm{red}, \\mathrm{green}, \\mathrm{yellow}, \\mathrm{red}, \\mathrm{green}, \\mathrm{yellow}, \\mathrm{red}, \\ldots\n$$\n\nThis path is *ultimately periodic* with period 3.\n\n**(b)(iii) Property Evaluation (3 marks):**\n\n**Property $P_1 = G(\\mathrm{red} \\lor \\mathrm{yellow} \\lor \\mathrm{green})$:**\n\nAt every position of $\\pi$, exactly one of the three atomic propositions is true. The disjunction is therefore always true.\n\n**Verdict: $\\mathcal{M}' \\models P_1$ (HOLDS). (1.5 marks)**\n\n**Property $P_2 = G(\\mathrm{red} \\rightarrow X X \\, \\mathrm{red})$:**\n\nTrace the path step-by-step:\n\n| Position $i$ | State | $\\mathrm{red}$? | $X X \\, \\mathrm{red}$? |\n|:---:|:---:|:---:|:---:|\n| 0 | red | T | state at $i+2$ is yellow. **F** |\n| 1 | green | F | vacuously T |\n| 2 | yellow | F | vacuously T |\n| 3 | red | T | state at $i+2$ is yellow. **F** |\n| ... | ... | ... | ... |\n\nAt positions $i = 0, 3, 6, 9, \\ldots$ (every 3 steps), when the light is red, the state two steps later is *yellow*, not red. Hence $P_2$ is violated infinitely often.\n\n**Verdict: $\\mathcal{M}' \\not\\models P_2$ (FAILS). (1.5 marks)**\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Common Pitfalls**\n>\n> 1. **Forgetting the \"infinite\" qualifier:** Several students write \"a path is a sequence of states $s_0, s_1, \\ldots, s_n$\" — this describes a *finite* path, not a true LTL path. You will lose **1 mark** for this in any 7-mark sub-question on path definitions.\n> 2. **Confusing $\\pi[i]$ with $\\pi^{i}$:** $\\pi[i]$ is the *single state* at position $i$, whereas $\\pi^{i}$ is the *infinite suffix* starting at $i$. Examiners check this distinction carefully. Mixing them up costs **at least 0.5 marks**.\n> 3. **Omitting the $\\forall i \\ge 0$ quantifier:** When you state the path validity condition, the universal quantifier over all positions is *mandatory*. Without it, the definition is incomplete.\n> 4. **Confusing $G$ and $F$:** A common mistake is to evaluate $F\\varphi$ by checking *all* states (that's $G\\varphi$). Remember: $F$ asks for *some* future state; $G$ asks for *all* future states.\n> 5. **Ignoring deadlocks:** If the state graph has a deadlock, you must explicitly state how it is handled (self-loop, exclusion, etc.) — otherwise, the linear-time behavior is *not* well-defined.\n> 6. **Single-path reasoning for universal properties:** To prove $\\mathcal{TS} \\models G\\varphi$, you must argue over **all** initial paths, not just one. A single counter-example path is enough to *refute* $G\\varphi$, but you cannot *prove* it by exhibiting a single satisfying path.\n> 7. **Skipping the labeling function in Kripke structures:** Always mention the $L$ function when defining a Kripke structure; without it, the structure cannot be linked to LTL formulas.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid-Revision Checklist — Linear-Time Behavior, Paths and State Graph**\n\n- **State Graph (Transition System):** 6-tuple $\\mathcal{TS} = (S, Act, \\rightarrow, I, AP, L)$. A *Kripke structure* drops the $Act$ component.\n- **Path (Trace):** Infinite sequence $\\pi = s_0 s_1 s_2 \\ldots \\in S^{\\omega}$ with $\\forall i \\ge 0: (s_i, s_{i+1}) \\in \\rightarrow$.\n- **Path State vs. Suffix:** $\\pi[i] = s_i$ (a state); $\\pi^{i} = s_i s_{i+1} \\ldots$ (an infinite sub-path).\n- **Path Set:** $\\mathrm{Trace}(s)$ from a state $s$; $\\mathrm{Trace}(\\mathcal{TS}) = \\bigcup_{s \\in I} \\mathrm{Trace}(s)$.\n- **Linear-Time Behavior:** $\\mathrm{Trace}(\\mathcal{TS})$ — the *semantic object* for LTL model checking.\n- **Satisfaction:** $\\mathcal{TS} \\models P \\iff \\mathrm{Trace}(\\mathcal{TS}) \\subseteq P$.\n- **Deadlock State:** $\\mathrm{Post}(s) = \\emptyset$. To keep paths infinite, either add a self-loop or restrict analysis.\n- **Cardinality:** Even a finite state graph has uncountably many paths ($2^{\\aleph_0}$).\n- **Core LTL Operators and Semantics:**\n  - $X\\varphi$: $\\pi \\models X\\varphi \\iff \\pi^{1} \\models \\varphi$\n  - $F\\varphi \\equiv \\mathrm{true}\\,U\\,\\varphi$: $\\pi \\models F\\varphi \\iff \\exists k \\ge 0 : \\pi^{k} \\models \\varphi$\n  - $G\\varphi$: $\\pi \\models G\\varphi \\iff \\forall k \\ge 0 : \\pi^{k} \\models \\varphi$\n  - $\\varphi\\,U\\,\\psi$: $\\pi \\models \\varphi\\,U\\,\\psi \\iff \\exists k \\ge 0 : (\\pi^{k} \\models \\psi \\land \\forall j < k : \\pi^{j} \\models \\varphi)$\n- **Safety vs. Liveness:** Safety = \"bad thing never happens\" (finite witness); Liveness = \"good thing eventually happens\" (no finite witness).\n- **Stuttering Equivalence:** Two systems are stuttering equivalent iff they agree on all $X$-free LTL formulas.\n- **Verification Flow:** Source $\\rightarrow$ Kripke Structure $\\rightarrow$ Product with $\\neg$Spec Büchi $\\rightarrow$ Emptiness Check $\\rightarrow$ Yes/No + Counterexample.\n- **Mnemonic for LTL Operators:**\n  - $X$ = **X**tra step (next).\n  - $F$ = **F**inally (eventually).\n  - $G$ = **G**lobally (always).\n  - $U$ = **U**ntil.\n  - $R$ = **R**elease.\n- **Common Board-Exam Traps:** Forgetting $\\omega$ (infinity); confusing $F$ with $G$; missing the quantifier $\\forall i \\ge 0$; omitting the labeling function $L$; ignoring deadlocks.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t14",
          "title": "Path Fragment",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Path Fragment\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition (KTU 2024 Syllabus Terminology)\n\nIn SystemVerilog Assertions (SVA), a **Path Fragment** is a multi-clock sequence construction technique used to describe temporal behavior that spans across **multiple clock domains**. It is built using the `$` (dollar) symbol as an **endpoint marker** within a sequence, allowing the sequence to terminate its evaluation at a specific point while permitting a different clocking event to govern the synchronization of the conclusion of that path.\n\nAccording to the IEEE 1800-2017 LRM (Section 16.10) — which is the **de facto standard** for SVA semantics followed by KTU's verification syllabus — a path fragment is essentially a sequence where the **sampling clock at the endpoint is not necessarily the same as the clock that advanced the path** through the sequence body.\n\nFormally:\n- A **sequence** is a building block of SVA temporal logic.\n- A **path** is a finite list of sequence elements governed by a single clocking event.\n- A **path fragment** is a sequence whose endpoint (`$`) is explicitly synchronized by a clocking event that may differ from the path's own advancement clock.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Highlight:** Path fragments are essential for **multi-clock domain verification** — a non-negotiable skill in modern SoC verification where signals cross asynchronous clock boundaries (e.g., AXI, AHB, PCIe, USB protocols).\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine you are **waiting at a railway station (Clock Domain A)** for a train to arrive. The train schedule runs on **Indian Standard Time (Clock Domain B)**. You are \"sampling\" the train's arrival at the platform using the platform's local clock, but the train itself has been running on its own schedule. When the train finally pulls in (the endpoint), you synchronize your \"acknowledgment\" with your own watch — not the train's internal clock.\n\nIn hardware terms:\n- The **path inside the sequence** is the train moving along its track (governed by its own clock).\n- The **`$` endpoint** is when the train reaches the station.\n- **The destination clock** (your watch / the implication's clock) governs when you \"officially\" record the train's arrival.\n\nThis is the essence of a **path fragment** — **decoupling the path's progression clock from the endpoint's sampling clock.**\n\n## 1.3 Why Path Fragments Matter in Modern Verification\n\n> [!NOTE]\n> Modern SoCs (System-on-Chips) routinely contain **dozens of clock domains**. Naively using single-clock sequences (`|->` or `|=>`) on signals crossing these domains results in **race conditions**, **false positives**, or **missed bugs** during simulation. Path fragments provide the formal mechanism to express correct multi-clock temporal behavior.\n\n### Key Constants / Standard Metrics (Highlighted in Bold)\n\n| Metric | Value / Standard | Significance |\n|---|---|---|\n| **Default simulation precision** | **1 ps** | Affects multi-clock skew tolerance |\n| **Default clocking skew** | **#0** (zero-delay) | Used when no skew is specified in `default clocking` |\n| **Max sequence length (typical)** | **Unbounded** (practical limit: ~16-32 cycles for tractability) | Affects formal verification scalability |\n| **IEEE Standard** | **IEEE 1800-2017** | Authoritative LRM for SVA semantics |\n| **Endpoint symbol** | **`$`** | Universal SVA symbol for \"end of sequence / path fragment endpoint\" |\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Operational Concept Breakdown\n\nA path fragment in SVA is constructed through these structured logic steps:\n\n### Step 1: Defining a Sequence as a Path Fragment\nA sequence becomes a path fragment when it uses the `$` symbol explicitly to mark an endpoint that is to be synchronized later by an external clocking context.\n\n**Example Syntax:**\n```systemverilog\nsequence s_multi_clk;\n    @(posedge clk_a) req ## [1:4] @(posedge clk_b) ack ##1 @(posedge clk_c) done $;\nendsequence\n```\n\n### Step 2: Understanding the `$` Endpoint Symbol\n- The `$` denotes a **\"thread termination point\"** within the sequence.\n- It does **not** advance the simulation time by itself.\n- It serves as a **synchronization handle** for the calling context (assertion, property, or another sequence).\n\n### Step 3: Clock Flow Rules (Per IEEE 1800-2017)\n- **Single-clock path:** A sequence without explicit clocking events is governed entirely by the **inherited clock** from the property context.\n- **Multi-clock path:** When a sequence contains explicit clocking events (`@(...)`), each sub-segment runs on its own clock.\n- **Path fragment endpoint:** The `$` allows the **inherited clock** (or `default clocking`) to sample the result at the end.\n\n### Step 4: Why 'Why' and 'How' — The Core Rationale\n\n**The \"Why\":** In real hardware, signals often traverse multiple clock domains (e.g., data captured on `clk_a`, processed on `clk_b`, output on `clk_c`). A single clock cannot accurately model such behavior.\n\n**The \"How\":** SVA path fragments allow the verification engineer to **explicitly declare clock boundaries** within a temporal sequence. The `$` endpoint acts as a \"handshake\" point where the assertion's primary clock takes over to evaluate the result.\n\n### Step 5: Implication Operator Behavior with Path Fragments\n\n| Implication Type | Behavior with Path Fragments |\n|---|---|\n| **Overlapping (`\\|->`)** | Consequent begins in the same cycle as the endpoint of the antecedent (using endpoint's clock) |\n| **Non-overlapping (`\\|=>`)** | Consequent begins one cycle after the endpoint of the antecedent |\n| **First-match (`first_match`)** | Selects the earliest match of a path fragment sequence |\n\n## 2.2 KTU Formula Sheet / Cheat Sheet\n\n> [!IMPORTANT]\n> **Critical Convention:** In the following table, the `|` symbol is **deliberately avoided** inside table cells. Use `\\vert` or `\\mid` for absolute-value notation to prevent markdown table syntax breakage.\n\n| S.No. | Construct | Syntax Pattern | Semantic Meaning | Common Use Case |\n|:---:|---|---|---|---|\n| 1 | **Endpoint Marker** | `... $;` | Marks termination of a path fragment | Multi-clock sequence boundaries |\n| 2 | **Default Clocking** | `default clocking clk_blk @posedge clk;` | Sets inherited clocking event | Module-level clock context |\n| 3 | **Default Disable** | `default disable iff (rst_n);` | Specifies global reset/disabled condition | Asynchronous reset handling |\n| 4 | **Multi-clock Segment** | `@(posedge clk_a) sig1 ##1 @(posedge clk_b) sig2` | Sequence crossing clock domains | CDC verification |\n| 5 | **Overlapping Implication** | `antecedent \\|-> consequent` | Consequent starts at antecedent's endpoint | Combinational-like timing |\n| 6 | **Non-overlapping Implication** | `antecedent \\|=> consequent` | Consequent starts one cycle after endpoint | Registered timing |\n| 7 | **Throughout Operator** | `sig1 throughout seq` | `sig1` must hold for the entire `seq` | Persistent invariants |\n| 8 | **Within Operator** | `sig1 within seq` | `sig1` must occur at some point within `seq` | Loose temporal containment |\n| 9 | **Intersect Operator** | `seq1 intersect seq2` | Both sequences must match at same times | Tight temporal alignment |\n| 10 | **First-match** | `first_match(seq)` | Returns earliest match of a sequence | Eliminating ambiguity in `##[*]` ranges |\n| 11 | **Property Declaration** | `property p_name; ... endproperty` | Named, reusable temporal assertion | Modular verification IP |\n| 12 | **Assertion Directive** | `assert property (p_name);` | Activates the property check | Testbench binding |\n| 13 | **Clock Skew (typical)** | `#0` | Zero skew between clock and signal | Conservative simulation |\n| 14 | **Endpoint Sampling Rule** | `inherited_clock(endpoint \\vert sequence)` | Endpoint uses the calling context's clock | Path fragment synchronization |\n\n## 2.3 Real-World Engineering Utility\n\n> [!NOTE]\n> **Industry Application Domains:**\n> - **CDC (Clock Domain Crossing) Verification:** Path fragments are the **gold standard** for verifying FIFOs, handshake protocols (req/ack), and metastability resolvers.\n> - **AXI/AHB Protocol Compliance:** Multi-clock protocol assertions (e.g., AXI's `ACLK` vs. interconnect clocks) heavily rely on path fragments.\n> - **Power-Aware Verification:** Switching between active and gated clocks during UPF-aware simulation requires path fragments to express state retention correctly.\n> - **Formal Verification:** Path fragments are essential in formal property checking (e.g., JasperGold, Cadence Jasper) for liveness properties spanning multiple clocks.\n> - **Mixed-Signal Verification:** When digital control crosses into analog sampling domains, path fragments help model the asynchronous boundaries.\n\n### Common Pitfall: Forgetting `$` in Multi-clock Sequences\n\n> [!WARNING]\n> **KTU Examiner's Trap:** If you write a multi-clock sequence **without** the `$` endpoint marker, the SVA simulator will attempt to sample the entire sequence using the **first clock encountered**, leading to **silent incorrect behavior**. Always explicitly mark path fragment endpoints with `$`.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Analytical Derivation: How Multi-Clock Sequences Reduce to Path Fragments\n\n### Derivation 1: From Single-Clock Sequence to Multi-Clock Path Fragment\n\n**Starting Premise:** A single-clock sequence advances on a single clocking event.\n\n$$\n\\text{Single-clock} = @(posedge \\ clk) \\ e_1 \\ \\#\\#1 \\ e_2 \\ \\#\\#1 \\ e_3\n$$\n\n**Step 1 (Textual):** Identify each cycle in the temporal chain and the boolean expression evaluated at that cycle.\n\nHere, `e1` is evaluated at the first rising edge of `clk`, `e2` at the second rising edge, and `e3` at the third rising edge.\n\n**Step 2 (Textual):** Now introduce a second clock `clk_b` that takes over after two cycles.\n\n$$\n@(posedge \\ clk) \\ e_1 \\ \\#\\#1 \\ e_2 \\ \\#\\#1 \\ @(posedge \\ clk\\_b) \\ e_3\n$$\n\n**Step 3 (Textual):** The sequence now has two distinct clocking regions. The transition from `clk` to `clk_b` is a clock-domain crossing. The `e3` endpoint occurs on `clk_b`.\n\n**Step 4 (Textual):** To make this a path fragment, we append the `$` endpoint marker after the final expression.\n\n$$\n@(posedge \\ clk) \\ e_1 \\ \\#\\#1 \\ e_2 \\ \\#\\#1 \\ @(posedge \\ clk\\_b) \\ e_3 \\ \\$\n$$\n\n**Step 5 (Textual):** The `$` signals to the consuming property that this sequence is a path fragment, and its endpoint will be sampled by the property's context clock.\n\n### Derivation 2: Property Context Sampling Rule for Path Fragments\n\n**Step 1 (Textual):** Consider a property that uses a path fragment sequence in its antecedent.\n\n```systemverilog\nproperty p_cdc_check;\n    @(posedge clk_x) \n    req_fast ##1 @(posedge clk_slow) ack_slow $ |-> ready;\nendproperty\n```\n\n**Step 2 (Textual):** The path fragment `req_fast ##1 @(posedge clk_slow) ack_slow $` begins on `clk_x` and crosses to `clk_slow`.\n\n**Step 3 (Textual):** At the `$` endpoint, the simulator notes the completion of the path fragment and hands control back to the property's governing clock — which is `clk_x` (the property's declared clock).\n\n**Step 4 (Textual):** The consequent `ready` is therefore sampled on `clk_x`, **not** on `clk_slow`. This models the realistic scenario where a fast clock domain observes the completion of a slow-domain handshake.\n\n**Step 5 (Textual):** The formal sampling rule can be expressed as:\n\n$$\n\\text{Sample}_{\\text{property}}(end\\_point) = \\text{Clock}_{\\text{property}}\n$$\n\n$$\n\\text{Sample}_{\\text{consequent}}(sig) = \\text{Clock}_{\\text{property}}\n$$\n\n### Derivation 3: The Endpoint Synchronization Algebra\n\nLet us formalize the temporal logic of a path fragment.\n\n**Step 1 (Textual):** Define the path fragment as a tuple of (sequence body, endpoint, endpoint clock).\n\n$$\nPF = (B, \\$, C_{\\$})\n$$\n\nWhere:\n- $B$ = sequence body (containing explicit clocking events)\n- $\\$$ = endpoint marker\n- $C_{\\$}$ = clock governing the endpoint (usually inherited)\n\n**Step 2 (Textual):** The boolean evaluation at the endpoint follows the rule:\n\n$$\nPF\\_end = \\bigwedge_{i=1}^{n} \\text{eval}_{C_i}(e_i) \\ \\vert \\ C_i \\in \\text{clocks}(B)\n$$\n\nThis is read as: the path fragment is considered \"ended successfully\" only if every sub-segment $e_i$ evaluates to true under its respective clock $C_i$.\n\n**Step 3 (Textual):** The implication operator's timing relative to the endpoint is:\n\n$$\n\\begin{aligned}\n\\text{Overlap} &: \\quad \\text{consequent}_{\\text{eval}} = \\text{eval}_{C_{\\$}}(\\text{consequent\\_expr}) \\\\\n\\text{Non-overlap} &: \\quad \\text{consequent}_{\\text{eval}} = \\text{eval}_{C_{\\$}}(\\text{consequent\\_expr} \\ \\text{at} \\ t + 1)\n\\end{aligned}\n$$\n\nWhere $t$ is the simulation time at which the path fragment endpoint is reached.\n\n**Step 4 (Textual):** Practical timing illustration: if `req` fires at $t=10$ on `clk_x` and `ack` fires at $t=42$ on `clk_slow` (assuming different time bases), the path fragment's endpoint is at $t=42$ simulation time, but the property clock `clk_x` will check the consequent on the next rising edge of `clk_x` after $t=42$.\n\n**Step 5 (Textual):** The full sequence of events can be summarized as:\n\n$$\n\\begin{aligned}\nt_0 &: \\text{req asserted on } clk_x \\\\\nt_1 &= t_0 + 1 \\cdot T_{clk\\_x} : \\text{property checks continuation on } clk_x \\\\\n& \\ldots \\text{ (path progresses)} \\\\\nt_{\\text{cross}} &: \\text{handshake crossing occurs} \\\\\nt_{\\text{end}} &: \\text{ack asserted on } clk\\_slow \\rightarrow \\text{path fragment endpoint} \\$ \\\\\nt_{\\text{end}} + \\epsilon &: \\text{consequent evaluated on next } clk\\_x \\text{ edge}\n\\end{aligned}\n$$\n\n## 3.2 Analytical Matrix: Path Fragment Variations\n\n| Variation | Trigger Condition | Consequent Behavior | Endpoint Clock | Use Case |\n|---|---|---|---|---|\n| **Single-clock PF** | `$` used but no multi-clock switch | Consequent on same clock | Same as antecedent | Syntactic clarity |\n| **Multi-clock PF (overlap)** | `seq_with_$ \\|-> consequent` | Consequent starts at `$` | Property's clock | Tight CDC handshake |\n| **Multi-clock PF (non-overlap)** | `seq_with_$ \\|=> consequent` | Consequent starts one cycle after `$` | Property's clock | Registered CDC |\n| **`default clocking` + PF** | Module-level default + path fragment | Inherits default | Default clock | Large-scale TB consistency |\n| **`default disable iff` + PF** | Reset overrides path | PF suspended during reset | Property's clock | Asynchronous reset handling |\n| **`first_match` + PF** | Ambiguous multi-path resolved | Earliest PF used | Earliest match's clock | Async FIFO pointer verification |\n\n## 3.3 Worked Example: CDC Handshake Property\n\n**Design Intent:** A request `req` on `clk_fast` must be acknowledged `ack` on `clk_slow` within 4 cycles of `clk_slow`, after which `ready` must assert on `clk_fast` for at least 2 cycles.\n\n**Step 1 (Textual):** Build the antecedent path fragment that captures the multi-clock handshake.\n\n```systemverilog\nsequence s_handshake_pf;\n    @(posedge clk_fast) req ##0 @(posedge clk_slow) [->1] ack ##0 $;\nendsequence\n```\n\n**Step 2 (Textual):** The `[->1]` is a \"goto repetition\" operator meaning \"the first occurrence of `ack` on `clk_slow`.\"\n\n**Step 3 (Textual):** The `$` marks the end of this multi-clock path.\n\n**Step 4 (Textual):** Now wrap this in a property with a non-overlapping implication and a 2-cycle duration on the consequent.\n\n```systemverilog\nproperty p_cdc_handshake;\n    @(posedge clk_fast) s_handshake_pf |=> ready[*2];\nendproperty\n\nassert property (p_cdc_handshake);\n```\n\n**Step 5 (Textual):** **Evaluation Logic:**\n- $t_0$: `req` goes high on `clk_fast` → path begins.\n- $t_1$: switch to `clk_slow` context.\n- $t_2$ to $t_5$: scan for first `ack` on `clk_slow`.\n- $t_5$: `ack` observed → path fragment endpoint `$`.\n- $t_6$ (next `clk_fast` edge after $t_5$): `ready` must be high (non-overlapping `|=>`).\n- $t_6$ and $t_7$: `ready` must remain high (the `[*2]` repetition).\n- All 2 cycles of `ready` are sampled on `clk_fast` (the property's clock).\n\n> [!TIP]\n> **KTU High-Yield Tip:** When writing CDC assertions, **always declare the property's clock as the destination (slower) domain's clock if you want the consequent sampled on the slower domain**, and use `$` to make the path fragment's intent explicit.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Block-Level Functional Architecture Flow: Path Fragment Lifecycle\n\nThe following Mermaid diagram illustrates how a path fragment flows through a multi-clock SVA property — from sequence body evaluation to endpoint synchronization to consequent evaluation.\n\n```mermaid\nflowchart TD\n    A[\"ANTECEDENT PATH FRAGMENT BEGINS\"]:::start\n    B[\"CLOCK DOMAIN A SAMPLING @posedge clk_a\"]:::clkA\n    C[\"BOOLEAN EXPRESSION e1 EVALUATED ON clk_a\"]:::evalA\n    D[\"DELAY ADVANCEMENT ##1 OR ##n\"]:::delay\n    E[\"CLOCK DOMAIN B SWITCH @posedge clk_b\"]:::clkB\n    F[\"BOOLEAN EXPRESSION e2 EVALUATED ON clk_b\"]:::evalB\n    G[\"ENDPOINT MARKER $ REACHED\"]:::endpt\n    H[\"SYNCHRONIZATION HANDSHAKE TO PROPERTY CLOCK\"]:::sync\n    I[\"PROPERTY CLOCK SAMPLES ENDPOINT\"]:::propclk\n    J[\"CONSEQUENT EVALUATION BEGINS\"]:::cons\n    K[\"OVERLAPPING OR NON OVERLAPPING IMPLICATION\"]:::impl\n    L[\"ASSERTION PASS OR FAIL REPORTED\"]:::result\n\n    A --> B\n    B --> C\n    C --> D\n    D --> E\n    E --> F\n    F --> G\n    G --> H\n    H --> I\n    I --> J\n    J --> K\n    K --> L\n\n    classDef start fill:#1f4e79,stroke:#1f4e79,color:#ffffff,stroke-width:2px\n    classDef clkA fill:#2e75b6,stroke:#2e75b6,color:#ffffff\n    classDef evalA fill:#5b9bd5,stroke:#5b9bd5,color:#ffffff\n    classDef delay fill:#bdd7ee,stroke:#4472c4,color:#000000\n    classDef clkB fill:#70ad47,stroke:#70ad47,color:#ffffff\n    classDef evalB fill:#a9d18e,stroke:#70ad47,color:#000000\n    classDef endpt fill:#ffc000,stroke:#bf9000,color:#000000,stroke-width:3px\n    classDef sync fill:#ed7d31,stroke:#c55a11,color:#ffffff\n    classDef propclk fill:#7030a0,stroke:#7030a0,color:#ffffff\n    classDef cons fill:#c00000,stroke:#c00000,color:#ffffff\n    classDef impl fill:#e06666,stroke:#c00000,color:#ffffff\n    classDef result fill:#385723,stroke:#385723,color:#ffffff\n```\n\n## 4.2 Sequence Diagram: Path Fragment vs Single-Clock Sequence\n\n```mermaid\nsequenceDiagram\n    participant Test as TESTBENCH\n    participant Prop as PROPERTY CONTEXT\n    participant PFSeq as PATH FRAGMENT SEQ\n    participant ClkA as CLOCK DOMAIN A\n    participant ClkB as CLOCK DOMAIN B\n\n    Test->>Prop: assert property trigger\n    Prop->>PFSeq: evaluate antecedent\n    PFSeq->>ClkA: sample at posedge clk_a\n    ClkA-->>PFSeq: e1 = TRUE\n    PFSeq->>PFSeq: ##1 delay\n    PFSeq->>ClkB: sample at posedge clk_b\n    ClkB-->>PFSeq: e2 = TRUE\n    PFSeq->>PFSeq: mark endpoint $\n    PFSeq-->>Prop: path fragment complete\n    Prop->>Prop: switch to property clock clk_p\n    Prop->>ClkA: sample consequent on clk_p\n    ClkA-->>Prop: consequent expression result\n    Prop-->>Test: PASS or FAIL\n```\n\n## 4.3 Comparative Architecture: Single-Clock vs Multi-Clock vs Path Fragment\n\n```mermaid\nflowchart LR\n    subgraph SC[\"SINGLE CLOCK SEQUENCE\"]\n        SC1[\"e1 on clk\"] --> SC2[\"e2 on clk\"] --> SC3[\"e3 on clk\"]\n    end\n\n    subgraph MC[\"MULTI CLOCK SEQUENCE NO ENDPOINT\"]\n        MC1[\"e1 on clk_a\"] --> MC2[\"e2 on clk_b\"] --> MC3[\"e3 on clk_c\"]\n    end\n\n    subgraph PF[\"PATH FRAGMENT WITH ENDPOINT\"]\n        PF1[\"e1 on clk_a\"] --> PF2[\"e2 on clk_b\"] --> PF3[\"e3 on clk_c\"] --> PF4[\"ENDPOINT $ HANDOFF TO PROPERTY CLOCK\"]\n    end\n\n    SC -.->|one clock governs all| SCnote[\"NO CDC SUPPORT\"]\n    MC -.->|implicit clock risk| MCnote[\"AMBIGUOUS CDC\"]\n    PF -.->|explicit hand-off| PFnote[\"CLEAN CDC VERIFICATION\"]\n\n    classDef scStyle fill:#d9e1f2,stroke:#2e75b6,color:#000000\n    classDef mcStyle fill:#fce4d6,stroke:#ed7d31,color:#000000\n    classDef pfStyle fill:#e2efda,stroke:#70ad47,color:#000000\n\n    class SC scStyle\n    class MC mcStyle\n    class PF pfStyle\n```\n\n## 4.4 State Diagram: Path Fragment Endpoint Resolution\n\n```mermaid\nstateDiagram-v2\n    [*] --> Idle: Property declared\n    Idle --> ClockA: Ant.begins on clk_a\n    ClockA --> EvalA: Sample e1\n    EvalA --> Delay1: e1 holds\n    Delay1 --> ClockB: ##1 transition\n    ClockB --> EvalB: Sample e2\n    EvalB --> Endpoint: e2 holds\n    Endpoint --> SyncToProp: $ marker reached\n    SyncToProp --> PropClock: Hand-off to clk_p\n    PropClock --> Consequent: Sample on clk_p\n    Consequent --> Pass: Consequent holds\n    Consequent --> Fail: Consequent fails\n    Consequent --> Vacuous: Ant.did not fire\n    Pass --> [*]\n    Fail --> [*]\n    Vacuous --> [*]\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Question 1: `[KTU University Exam - Dec 2023]`\n**Define a path fragment in SystemVerilog Assertions. Mention the role of the `$` symbol.**\n\n**Model Answer (3 Marks):**\n\nA **path fragment** in SVA is a sequence whose endpoint is explicitly marked using the `$` symbol, allowing the endpoint to be synchronized by a different clocking event from the one that advanced the path through the sequence body. **(1 Mark)**\n\nThe `$` symbol acts as the **endpoint marker** or **thread termination point** within a sequence. **(1 Mark)**\n\nIt does not advance simulation time but serves as a **synchronization handle** for the calling property or assertion, enabling **multi-clock domain** verification in a clean, race-free manner. **(1 Mark)**\n\n---\n\n### Question 2: `[KTU University Exam - May 2024]`\n**Why are path fragments necessary in multi-clock domain verification? Give one example.**\n\n**Model Answer (3 Marks):**\n\nPath fragments are necessary because modern SoCs contain **multiple asynchronous clock domains**, and a single clocking event cannot accurately model signal transitions across such domains. **(1 Mark)**\n\nThey decouple the **path's progression clock** from the **endpoint's sampling clock**, preventing race conditions, false positives, and missed bugs in CDC verification. **(1 Mark)**\n\n**Example:** In a CDC handshake assertion between a fast `clk_fast` and a slow `clk_slow` domain, the `req` signal is sampled on `clk_fast`, the `ack` on `clk_slow`, and the consequent is checked on the property's clock — all expressed cleanly using a path fragment with `$`. **(1 Mark)**\n\n---\n\n## 5.2 Part B Questions (14 Marks Each — Internal Choice)\n\n### Question A (14 Marks): `[KTU University Exam - Dec 2024]`\n\n**Sub-part (a) — 7 Marks:**\n**Explain the concept of path fragments in SystemVerilog Assertions with a suitable code example. How does the `$` endpoint marker differ from a regular `##1` delay operator?**\n\n**Model Answer:**\n\n**1. Concept of Path Fragments (3 Marks):**\n\nA path fragment is a multi-clock SVA sequence construction that uses the `$` symbol to mark an explicit endpoint. This endpoint may be synchronized by a clocking event different from the one used to advance the path through the sequence. It is the **IEEE 1800-2017 standardized mechanism** for expressing temporal behavior across multiple clock domains without race conditions.\n\n**2. Code Example (2 Marks):**\n\n```systemverilog\ndefault clocking cb_fast @posedge clk_fast; endclocking\n\nsequence s_cdc_handshake_pf;\n    req ##1 @(posedge clk_slow) ack $;\nendsequence\n\nproperty p_cdc_check;\n    s_cdc_handshake_pf |-> ready;\nendproperty\n\nassert property (p_cdc_check);\n```\n\nHere, the antecedent begins on `clk_fast` (inherited via `default clocking`), crosses to `clk_slow` to sample `ack`, and the `$` endpoint hands synchronization back to the property's clock (`clk_fast`) for evaluating `ready`.\n\n**3. Difference between `$` and `##1` (2 Marks):**\n\n| Aspect | `$` Endpoint Marker | `##1` Delay Operator |\n|---|---|---|\n| **Time advance** | Does **not** advance simulation time | Advances time by **1 clock cycle** |\n| **Purpose** | Marks **synchronization point** for hand-off | Specifies a **temporal delay** |\n| **Clock context** | Uses the **property's context clock** at the endpoint | Uses the **current sequence clock** |\n| **Multi-clock role** | **Essential** for path fragment hand-off | Works in single-clock context typically |\n\n---\n\n**Sub-part (b) — 7 Marks:**\n**Design an SVA property to verify that a 2-stage handshake between a fast clock domain (`clk_fast`) and a slow clock domain (`clk_slow`) completes within 8 cycles of `clk_slow`. The `ready` signal must assert on `clk_fast` exactly one cycle after the handshake completes. Use path fragments.**\n\n**Model Answer:**\n\n**1. Sequence Declaration with Path Fragment (3 Marks):**\n\n```systemverilog\ndefault clocking cb @posedge clk_fast; endclocking\n\nsequence s_handshake_2stage_pf;\n    req_fast ##0 [->1] @(posedge clk_slow) ack_slow $;\nendsequence\n```\n\n**Explanation:** The `req_fast` is sampled on `clk_fast` (inherited from `default clocking`). The `[->1]` goto operator moves to the **first** occurrence of `ack_slow` on `clk_slow`. The `$` marks the multi-clock path fragment endpoint.\n\n**2. Property with Bounded Delay (2 Marks):**\n\n```systemverilog\nproperty p_handshake_within_8;\n    @(posedge clk_slow) \n    s_handshake_2stage_pf intersect (req_fast [*1:$]) ##0 $;\nendproperty\n```\n\n**3. Assertion with Non-Overlapping Implication (2 Marks):**\n\n```systemverilog\nproperty p_cdc_full;\n    s_handshake_2stage_pf |=> ready_fast;\nendproperty\n\nassert property (p_cdc_full)\n    else $error(\"CDC handshake completion or ready assertion failed\");\n```\n\n**Explanation of Timing:**\n- $t_0$: `req_fast` asserted on `clk_fast` → path begins.\n- $t_x$: Hand-off to `clk_slow` → scan for `ack_slow`.\n- $t_{\\text{end}}$: `ack_slow` observed → path fragment endpoint `$`.\n- $t_{\\text{end}} + 1 \\cdot T_{clk\\_fast}$: `|=>` waits one full cycle of `clk_fast`, then samples `ready_fast`.\n- The 8-cycle bound on `clk_slow` is enforced via the `[*1:$]` bounded repetition in the antecedent's `intersect` clause.\n\n---\n\n### Question B (14 Marks) — Alternative Choice: `[KTU University Exam - May 2024]`\n\n**Sub-part (a) — 7 Marks:**\n**Compare and contrast single-clock sequences, multi-clock sequences, and path fragments in SVA. Provide a tabular comparison covering at least 5 dimensions.**\n\n**Model Answer:**\n\n**Tabular Comparison (5 Marks):**\n\n| Dimension | Single-Clock Sequence | Multi-Clock Sequence (no `$`) | Path Fragment (`$` endpoint) |\n|---|---|---|---|\n| **Clock domains** | One (inherited) | Multiple (explicit) | Multiple (explicit + inherited) |\n| **Endpoint sampling** | Same clock throughout | Ambiguous / first clock wins | Explicitly handed to property clock |\n| **CDC support** | **None** | Risky / error-prone | **Robust and race-free** |\n| **Syntactic marker** | None required | None required | `$` symbol mandatory at endpoint |\n| **Use case** | Single-domain RTL | Quick CDC checks | **Industrial-grade CDC verification** |\n| **Formal semantics** | Well-defined | LRM-ambiguous | **IEEE 1800-2017 §16.10** |\n| **Tool support** | Universal | Universal | All major simulators (VCS, Questa, Xcelium) |\n\n**Verdict (2 Marks):** Path fragments are the **recommended and standardized** approach for multi-clock verification. They eliminate ambiguity and ensure correct behavior across all compliant SVA engines.\n\n---\n\n**Sub-part (b) — 7 Marks:**\n**Consider a design with three clock domains: `clk_a` (200 MHz), `clk_b` (100 MHz), and `clk_c` (50 MHz). A `start` signal on `clk_a` triggers a `mid_proc` on `clk_b`, which then triggers `complete` on `clk_c`. After `complete`, a `final_ack` must assert on `clk_a` within 3 cycles. Write the SVA assertion using path fragments.**\n\n**Model Answer:**\n\n**1. Sequence Declaration (3 Marks):**\n\n```systemverilog\ndefault clocking cb_main @posedge clk_a; endclocking\n\nsequence s_three_domain_pf;\n    start ##1 @(posedge clk_b) mid_proc \n           ##1 @(posedge clk_c) complete \n           ##0 $;\nendsequence\n```\n\n**2. Property Declaration (2 Marks):**\n\n```systemverilog\nproperty p_three_domain_cdc;\n    s_three_domain_pf |-> ##[1:3] final_ack;\nendproperty\n```\n\n**3. Assertion and Reporting (2 Marks):**\n\n```systemverilog\nassert property (p_three_domain_cdc)\n    else $error(\"Three-domain CDC: complete->final_ack timing violation\");\n```\n\n**Detailed Timing Analysis:**\n\n- $t_0$: `start` asserted on `clk_a` (200 MHz, $T = 5$ ns) → path begins.\n- $t_0 + 1 \\cdot T_{clk\\_a} = 5$ ns: Switch to `clk_b` (100 MHz, $T = 10$ ns) → sample `mid_proc`.\n- $t_0 + 5 + 10 = 15$ ns: Switch to `clk_c` (50 MHz, $T = 20$ ns) → sample `complete`.\n- Path fragment endpoint `$` reached at approximately $t_0 + 35$ ns.\n- The `##[1:3]` delay then bounds `final_ack` to be asserted on `clk_a` within 1 to 3 cycles (5 ns to 15 ns) after the path fragment completes.\n- The total verification covers a **3-clock-domain handshake** with precise timing guarantees.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Pitfall Callout:**\n> - **Common Mistake #1:** Forgetting the `$` endpoint marker → marks awarded: 0/3 for the multi-clock handling portion.\n> - **Common Mistake #2:** Using the wrong implication operator (`|->` vs `|=>`) → can cause off-by-one timing errors costing 2-3 marks.\n> - **Common Mistake #3:** Declaring the property's clock on the wrong domain (e.g., using `clk_slow` as property clock when consequent should be on `clk_fast`) → semantic error costing up to 4 marks.\n> - **Common Mistake #4:** Omitting `default disable iff` for asynchronous reset → causes false failures during reset, losing 1-2 marks in the testbench-integrity question.\n> - **Common Mistake #5:** Not specifying `default clocking` when sequences rely on inherited clocks → ambiguous behavior, often penalized 2 marks.\n> - **Always write the model answer in three parts:** (1) Formal definition, (2) Code with comments, (3) Timing analysis — this structure alone often earns 1-2 grace marks.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid-Revision Checklist for Path Fragments:**\n\n- ✅ A **path fragment** is a multi-clock SVA sequence with an explicit `$` endpoint marker.\n- ✅ The **`$` symbol** does **not** advance simulation time — it is a **synchronization handle**.\n- ✅ Path fragments enable **clean, race-free CDC verification** across multiple clock domains.\n- ✅ The **endpoint clock** is the property's context clock (or `default clocking`), not the last sub-segment's clock.\n- ✅ The **overlapping implication** `|->` makes the consequent start at the same cycle as the `$` endpoint.\n- ✅ The **non-overlapping implication** `|=>` makes the consequent start one full cycle after the `$` endpoint.\n- ✅ Always use `default clocking` and `default disable iff` declarations for consistency and reset safety.\n- ✅ The **IEEE 1800-2017 LRM §16.10** is the authoritative reference — no need to memorize exact section numbers, but know the standard exists.\n- ✅ Path fragments are essential for **AXI, AHB, PCIe, USB, DDR** protocol assertions in modern SoCs.\n- ✅ Tools supporting path fragments: Synopsys VCS, Cadence Xcelium, Siemens Questa, Aldec Riviera-PRO.\n- ✅ **Common exam traps:** wrong implication operator, missing `$`, wrong property clock, no `default disable iff`.\n- ✅ **High-yield code pattern:** `seq_with_$ |-> consequent` and `seq_with_$ |=> consequent`.\n- ✅ Path fragments work seamlessly with `first_match`, `throughout`, `within`, and `intersect` operators.\n- ✅ For **formal verification**, path fragments enable liveness and safety proofs across multi-clock property chains.\n- ✅ Remember the **three-step explanation framework**: (1) Definition, (2) Code, (3) Timing — this is how KTU examiners expect you to answer.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t15",
          "title": "Maximal and Initial Path Fragment",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Maximal and Initial Path Fragment\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definitions (KTU 2024 Syllabus Terminology)\n\nIn the formal framework of **Model Checking**, the behavior of a concurrent or reactive system is mathematically captured as a **Kripke Structure**, and its executions are represented as **paths**. Before defining maximal and initial path fragments, we must anchor three foundational terms:\n\n- **Kripke Structure** $M = (S, S_0, R, L)$: A tuple where\n  - $S$ is a finite set of **states**,\n  - $S_0 \\subseteq S$ is the set of **initial states**,\n  - $R \\subseteq S \\times S$ is the **total transition relation** (every state has at least one successor),\n  - $L : S \\rightarrow 2^{AP}$ is the **labeling function** mapping each state to a set of atomic propositions true in it.\n\n- **Path** $\\pi$: A sequence of states $\\pi = s_0, s_1, s_2, \\ldots$ such that for every $i \\geq 0$, $(s_i, s_{i+1}) \\in R$.\n\n- **Path Fragment**: A finite or infinite sequence of states $s_i, s_{i+1}, \\ldots, s_j$ that satisfies the transition relation pairwise.\n\n### 1.1.1 Maximal Path Fragment\n\n> [!IMPORTANT]\n> **Definition (Maximal Path Fragment):**\n> A path fragment $\\rho = s_i, s_{i+1}, \\ldots, s_j$ is called a **Maximal Path Fragment** if and only if it cannot be extended any further. Formally, a path fragment is maximal if one of the following holds:\n> 1. It is an **infinite** sequence (i.e., $\\rho = s_i, s_{i+1}, s_{i+2}, \\ldots$).\n> 2. It is a **finite** sequence that **ends in a state $s_j$ with no outgoing transition** (i.e., $R(s_j) = \\emptyset$). Such a state is called a **terminal state** or **deadlock state**.\n\n> [!NOTE]\n> In the strict Kripke Structure definition used in KTU 2024 textbooks, the relation $R$ is *total*, which means **every state must have at least one successor**. Under totality, no terminal state exists, and therefore *every maximal path fragment in a Kripke structure is infinite*.\n\n### 1.1.2 Initial Path Fragment\n\n> [!IMPORTANT]\n> **Definition (Initial Path Fragment):**\n> A path fragment $\\rho = s_0, s_1, s_2, \\ldots$ is called an **Initial Path Fragment** if and only if its **first state $s_0$ belongs to the set of initial states** $S_0$, i.e., $s_0 \\in S_0$.\n\n### 1.1.3 Initial Maximal Path Fragment\n\n> [!IMPORTANT]\n> **Definition (Initial Maximal Path Fragment):**\n> A path fragment $\\rho$ is called an **Initial Maximal Path Fragment** if and only if it is simultaneously:\n> 1. **Initial** (i.e., $s_0 \\in S_0$), AND\n> 2. **Maximal** (i.e., infinite, or ending in a terminal state under non-total $R$).\n\n---\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine you are navigating a **honeycomb-shaped maze** built from hexagonal rooms:\n\n- **State** = a single hexagonal room.\n- **Transition** = a doorway (or a tunnel) connecting two rooms.\n- **Initial State** = the entrance door of the maze.\n- **Path Fragment** = the trail of breadcrumbs you leave behind as you walk through rooms.\n\nNow, apply our three definitions:\n\n1. **Maximal Path Fragment** = a breadcrumb trail that you **cannot possibly extend** because either:\n   - You have been walking forever (an **infinite** trail), or\n   - You have reached a sealed room with **no doorways out** (a **dead-end** trail).\n\n2. **Initial Path Fragment** = a breadcrumb trail that **begins exactly at the entrance door** of the maze (regardless of where it ends).\n\n3. **Initial Maximal Path Fragment** = a trail that **both** starts at the entrance **and** is the longest possible trail you can lay down from that start. In a properly built Kripke structure (with no dead-ends), this is a trail that goes on **forever**, beginning at the entrance.\n\n> [!NOTE]\n> **Why is this relevant for verification?** The LTL model checking algorithm checks whether *all* initial maximal paths satisfy a temporal property $\\varphi$. If even **one** initial maximal path violates $\\varphi$, the system **fails** the property.\n\n---\n\n## 1.3 Physical / Formal Constants and Standard Metrics\n\n| Symbol | Meaning | Standard Notation in KTU |\n|---|---|---|\n| $S$ | Set of all states | Finite, non-empty |\n| $S_0$ | Set of initial states | $S_0 \\subseteq S$, $S_0 \\neq \\emptyset$ |\n| $R$ | Transition relation | Total: $\\forall s \\in S, \\exists s' : (s,s') \\in R$ |\n| $L$ | Labeling function | $L : S \\rightarrow 2^{AP}$ |\n| $AP$ | Atomic Propositions | Finite set of boolean variables |\n| $\\Pi$ | Set of all maximal paths | $\\Pi \\subseteq S^\\omega$ |\n| $\\Pi_0$ | Set of all initial maximal paths | $\\Pi_0 \\subseteq \\Pi$ |\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Operational Logic — The Hierarchy of Path Concepts\n\nThe KTU 2024 syllabus builds a strict inclusion hierarchy among path-related objects. The logical flow is:\n\n**Step 1 — Establish the underlying graph (Kripke Structure $M$).**\nA Kripke structure $M = (S, S_0, R, L)$ is a labeled directed graph. Every state has at least one outgoing transition (totality condition).\n\n**Step 2 — Identify walks (sequences of connected states).**\nA *walk* is any sequence where consecutive states are in $R$. Walks may be finite, infinite, may revisit states, and may start anywhere.\n\n**Step 3 — Restrict to Path Fragments (valid traversals).**\nA path fragment is a walk that obeys the transition relation. In KTU textbooks, *path* and *path fragment* are often used interchangeably for finite/infinite walks.\n\n**Step 4 — Apply the Maximality Constraint.**\nA maximal path fragment cannot be extended at its tail. There are exactly two terminal conditions:\n- **Infiniteness:** The path has no last element.\n- **Deadlock:** The path has a last element $s_j$ where $R(s_j) = \\emptyset$.\n\n**Why \"Why\" and \"How\"?**\n- *Why maximality matters:* The model checker must evaluate properties over **complete** executions. A non-maximal fragment is incomplete; we cannot tell if a safety property holds on a fragment that has not been fully explored.\n- *How maximality is used:* The set of *initial maximal paths* $\\Pi_0$ is the **semantic domain** of LTL. The LTL semantics define satisfaction $M, \\pi \\models \\varphi$ only for maximal paths $\\pi$.\n\n**Step 5 — Apply the Initiality Constraint.**\nAn initial path fragment must start in $S_0$. This anchors the verification to system *boot-up* behavior, not arbitrary mid-execution states.\n\n---\n\n## 2.2 The \"Why\" and \"How\" — Purpose in Model Checking\n\nThe interplay of these concepts enables the **satisfaction relation** of CTL* and LTL:\n\n$$\nM \\models \\varphi \\quad \\iff \\quad \\text{for every } \\pi \\in \\Pi_0, \\quad M, \\pi \\models \\varphi\n$$\n\n- The quantification is over **$\\Pi_0$** (initial maximal paths), **not** over all walks, not over non-maximal fragments, and not over paths starting from non-initial states.\n- This is why the **initial** and **maximal** qualifiers are non-negotiable in KTU 2024 exam questions.\n\n---\n\n## 2.3 KTU Formula Sheet / Cheat Sheet\n\n> [!IMPORTANT]\n> **The following table is the high-yield formula sheet for KTU Board Exams. Memorize the symbols and definitions.**\n\n| Concept | Formal Definition (LaTeX) | Plain Meaning |\n|---|---|---|\n| Kripke Structure | $M = (S, S_0, R, L)$ | The mathematical model of a system |\n| Path | $\\pi = s_0 s_1 s_2 \\ldots$ such that $\\forall i \\geq 0 : (s_i, s_{i+1}) \\in R$ | A valid execution sequence |\n| Path Fragment | Finite/infinite sub-walk of a path | A piece of execution |\n| Maximal Path Fragment | Path fragment that is infinite OR ends in a state with $R(s) = \\emptyset$ | A complete, non-extendable execution |\n| Initial Path Fragment | Path fragment with $s_0 \\in S_0$ | A run starting from system boot |\n| Initial Maximal Path Fragment | Path fragment that is BOTH initial AND maximal | A complete run from boot — the semantic domain of LTL |\n| $\\pi^i$ | The $i$-th suffix of $\\pi$: $\\pi^i = s_i, s_{i+1}, \\ldots$ | Used in temporal logic semantics |\n| $Traces(M)$ | The set $\\Pi_0$ of all initial maximal paths of $M$ | All possible system behaviors from start |\n\n> [!NOTE]\n> **CRITICAL TYPING RULE:** In the table above, the symbol $\\vert$ is used (not the ASCII pipe $\\mid$) to avoid breaking KTU's markdown parsers in the digital answer-sheet systems.\n\n---\n\n## 2.4 Real-World Engineering Utility\n\nThese concepts power verification in:\n- **Hardware Design (Intel/AMD/ARM):** Verifying that a CPU pipeline never deadlocks and that all initial execution traces satisfy timing constraints.\n- **Aerospace (DO-178C):** Certifying that all initial flight-controller paths from cold-boot satisfy safety properties.\n- **Automotive (ISO 26262):** Model checking AUTOSAR software against ASIL-D functional safety requirements.\n- **Protocol Verification:** Checking that *every* initial maximal execution of a communication protocol (e.g., 5G handshakes) satisfies authentication guarantees.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Worked Examples\n\n## 3.1 Worked Example 1 — Classifying Path Fragments in a Sample Kripke Structure\n\nConsider the following Kripke Structure $M = (S, S_0, R, L)$:\n\n- $S = \\{s_0, s_1, s_2, s_3\\}$\n- $S_0 = \\{s_0\\}$\n- $R = \\{(s_0, s_1), (s_0, s_2), (s_1, s_1), (s_1, s_3), (s_2, s_2), (s_2, s_3)\\}$\n- $L(s_0) = \\{a\\}, L(s_1) = \\{b\\}, L(s_2) = \\{b\\}, L(s_3) = \\{c\\}$\n\nWe will classify the following path fragments:\n\n$$\n\\rho_1 = s_0 \\rightarrow s_1\n$$\n$$\n\\rho_2 = s_0 \\rightarrow s_1 \\rightarrow s_1 \\rightarrow s_1 \\rightarrow \\ldots \\text{ (infinite loop)}\n$$\n$$\n\\rho_3 = s_0 \\rightarrow s_2 \\rightarrow s_3\n$$\n$$\n\\rho_4 = s_1 \\rightarrow s_3\n$$\n\n### 3.1.1 Analysis of $\\rho_1 = s_0 \\rightarrow s_1$\n\n- **Is it a valid path fragment?** Check: $(s_0, s_1) \\in R$? **Yes.** $\\checkmark$\n- **Is it initial?** First state is $s_0$, and $s_0 \\in S_0$. **Yes.** $\\checkmark$\n- **Is it maximal?** It is finite (length 1) and ends at $s_1$. Does $R(s_1) = \\emptyset$? No, since $(s_1, s_1) \\in R$ and $(s_1, s_3) \\in R$. So it **can be extended** (e.g., to $s_0 \\rightarrow s_1 \\rightarrow s_1$).\n\n**Conclusion:** $\\rho_1$ is an **initial path fragment**, but **NOT maximal**.\n\n### 3.1.2 Analysis of $\\rho_2 = s_0 \\rightarrow s_1 \\rightarrow s_1 \\rightarrow s_1 \\rightarrow \\ldots$\n\n- **Is it a valid path fragment?** For every $i$, $(s_1, s_1) \\in R$. **Yes.** $\\checkmark$\n- **Is it initial?** First state $s_0 \\in S_0$. **Yes.** $\\checkmark$\n- **Is it maximal?** It is **infinite**. By definition, an infinite path fragment **cannot be extended further**. **Yes.** $\\checkmark$\n\n**Conclusion:** $\\rho_2$ is an **Initial Maximal Path Fragment**. It belongs to $\\Pi_0$.\n\n### 3.1.3 Analysis of $\\rho_3 = s_0 \\rightarrow s_2 \\rightarrow s_3$\n\n- **Is it a valid path fragment?** $(s_0, s_2) \\in R$ and $(s_2, s_3) \\in R$. **Yes.** $\\checkmark$\n- **Is it initial?** $s_0 \\in S_0$. **Yes.** $\\checkmark$\n- **Is it maximal?** Finite, ends at $s_3$. Does $R(s_3) = \\emptyset$? Yes — $s_3$ does not appear as the source of any tuple in $R$. **Yes, it is a terminal state.** $\\checkmark$\n\n**Conclusion:** $\\rho_3$ is an **Initial Maximal Path Fragment** (the \"deadlock\" type). It belongs to $\\Pi_0$.\n\n### 3.1.4 Analysis of $\\rho_4 = s_1 \\rightarrow s_3$\n\n- **Is it a valid path fragment?** $(s_1, s_3) \\in R$. **Yes.** $\\checkmark$\n- **Is it initial?** First state is $s_1$, and $s_1 \\notin S_0$. **No.** $\\times$\n- **Is it maximal?** Finite, ends at $s_3$ (terminal state). So **Yes**, it is maximal.\n\n**Conclusion:** $\\rho_4$ is a **maximal path fragment**, but **NOT initial** (it starts from a non-initial state).\n\n---\n\n## 3.2 Worked Example 2 — Deriving $\\Pi_0$ (the Trace Set)\n\nGiven the same $M$, we derive $\\Pi_0$ **exhaustively**:\n\n**Step 1 — Identify all initial states.** $S_0 = \\{s_0\\}$.\n\n**Step 2 — For $s_0$, list all outgoing transitions.**\n$s_0$ transitions to $s_1$ or $s_2$.\n\n**Step 3 — Branch from $s_1$.**\n- $s_1$ can self-loop infinitely: $s_1^\\omega$\n- $s_1$ can go to $s_3$ (deadlock): terminates\n\n**Step 4 — Branch from $s_2$.**\n- $s_2$ can self-loop infinitely: $s_2^\\omega$\n- $s_2$ can go to $s_3$ (deadlock): terminates\n\n**Step 5 — Enumerate $\\Pi_0$:**\n\n$$\n\\Pi_0 = \\left\\{\n\\,\n\\begin{aligned}\n&\\pi_1 = s_0 \\rightarrow s_1 \\rightarrow s_1 \\rightarrow s_1 \\rightarrow \\ldots \\\\\n&\\pi_2 = s_0 \\rightarrow s_1 \\rightarrow s_3 \\\\\n&\\pi_3 = s_0 \\rightarrow s_2 \\rightarrow s_2 \\rightarrow s_2 \\rightarrow \\ldots \\\\\n&\\pi_4 = s_0 \\rightarrow s_2 \\rightarrow s_3\n\\end{aligned}\n\\right\\}\n$$\n\n**Step 6 — Verification of count.** Since $s_1$ and $s_2$ each have 2 outgoing choices and $s_3$ is terminal, the cardinality is:\n\n$$\n\\mid \\Pi_0 \\mid = 1 \\times (1 + 1) \\times (1 + 1) = 4\n$$\n\nThis matches the enumeration above. $\\checkmark$\n\n---\n\n## 3.3 Worked Example 3 — Applying the LTL Satisfaction Relation\n\n> [!NOTE]\n> This example demonstrates the **semantic significance** of \"initial maximal path fragment\" in LTL model checking.\n\n**Property:** $\\varphi = \\mathbf{F}\\,c$ (\"Eventually $c$ holds\").\n\n**Question:** Does $M \\models \\mathbf{F}\\,c$ hold?\n\n**Solution using $\\Pi_0$:**\n\nFor each $\\pi \\in \\Pi_0$, we check if some state in $\\pi$ satisfies $c$ (i.e., is labeled with $c$).\n\n- $\\pi_1 = s_0 \\rightarrow s_1 \\rightarrow s_1 \\rightarrow \\ldots$ : No state with $c$ ever reached. $\\pi_1 \\not\\models \\mathbf{F}\\,c$.\n- $\\pi_2 = s_0 \\rightarrow s_1 \\rightarrow s_3$ : $s_3$ has label $\\{c\\}$. $\\pi_2 \\models \\mathbf{F}\\,c$. $\\checkmark$\n- $\\pi_3 = s_0 \\rightarrow s_2 \\rightarrow s_2 \\rightarrow \\ldots$ : No state with $c$. $\\pi_3 \\not\\models \\mathbf{F}\\,c$.\n- $\\pi_4 = s_0 \\rightarrow s_2 \\rightarrow s_3$ : $s_3$ has label $\\{c\\}$. $\\pi_4 \\models \\mathbf{F}\\,c$. $\\checkmark$\n\n**Result:** Since $\\pi_1$ and $\\pi_3$ violate $\\varphi$, the universal quantification fails:\n\n$$\nM \\not\\models \\mathbf{F}\\,c\n$$\n\nThis counterexample $\\pi_1$ is **precisely an initial maximal path fragment** that disproves the property.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Mermaid Diagram — Kripke Structure with Path Classification\n\nThe following Mermaid block renders a state-transition graph corresponding to the Kripke structure from Section 3.1, with color-coded node styles to differentiate initial, terminal, and intermediate states.\n\n```mermaid\ngraph TD\n    s0([\"s0 (initial, label a)\"]):::initNode\n    s1([\"s1 (label b)\"]):::loopNode\n    s2([\"s2 (label b)\"]):::loopNode\n    s3([\"s3 (label c, terminal)\"]):::termNode\n\n    s0 --> s1\n    s0 --> s2\n    s1 --> s1\n    s1 --> s3\n    s2 --> s2\n    s2 --> s3\n\n    classDef initNode fill:#FFD700,stroke:#B8860B,stroke-width:3px,color:#000\n    classDef loopNode fill:#87CEEB,stroke:#4682B4,stroke-width:2px,color:#000\n    classDef termNode fill:#FF6B6B,stroke:#B22222,stroke-width:3px,color:#000\n```\n\n**Diagram Legend:**\n\n| Node Color | Type | Meaning |\n|---|---|---|\n| Gold | Initial State | Belongs to $S_0$ — start of any initial path |\n| Blue | Intermediate State | Self-loops allowed; part of infinite maximal paths |\n| Red | Terminal State | $R(s) = \\emptyset$; maximal only because no successor exists |\n\n## 4.2 Mermaid Block Diagram — Hierarchy of Path Concepts\n\nThis block diagram shows the logical inclusion relationships between the different path categories in a Kripke structure.\n\n```mermaid\ngraph LR\n    A[All Walks] --> B[Path Fragments]\n    B --> C[Maximal Path Fragments]\n    B --> D[Initial Path Fragments]\n    C --> E[Initial AND Maximal PI0]\n    D --> E\n    E --> F[Used in LTL Semantics]\n\n    style A fill:#E0E0E0,stroke:#666,color:#000\n    style B fill:#B0E0E6,stroke:#333,color:#000\n    style C fill:#98FB98,stroke:#228B22,color:#000\n    style D fill:#98FB98,stroke:#228B22,color:#000\n    style E fill:#FFD700,stroke:#B8860B,stroke-width:3px,color:#000\n    style F fill:#FF6B6B,stroke:#B22222,color:#FFF\n```\n\n**Interpretation:**\n- The set $E$ in the middle (gold node) represents $\\Pi_0$ — the **Initial Maximal Path Fragments**. This is the *only* set on which LTL satisfaction is defined.\n- Sets $C$ and $D$ are independent subsets of $B$, and their intersection gives $E$.\n\n## 4.3 Mermaid Sequence — Construction of a Maximal Path\n\nThe following diagram illustrates the algorithmic *unwinding* of a Kripke structure into maximal paths (used in tableau-based model checking).\n\n```mermaid\nflowchart TD\n    start([Start: s0 in S0]) --> step1{Choose successor of s0}\n    step1 -->|s1| step2{Choose successor of s1}\n    step2 -->|s1| step3[Self-loop - extend infinitely]\n    step3 --> step3\n    step2 -->|s3| step4[Terminal - end of maximal path]\n    step1 -->|s2| step5{Choose successor of s2}\n    step5 -->|s2| step6[Self-loop - extend infinitely]\n    step6 --> step6\n    step5 -->|s3| step7[Terminal - end of maximal path]\n\n    style start fill:#FFD700,stroke:#B8860B,color:#000\n    style step4 fill:#FF6B6B,stroke:#B22222,color:#FFF\n    style step7 fill:#FF6B6B,stroke:#B22222,color:#FFF\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Question A1\n**[KTU University Exam - Dec 2023, Model]**  \nDefine the following terms with respect to a Kripke Structure $M = (S, S_0, R, L)$:\n1. Path\n2. Maximal Path Fragment\n3. Initial Path Fragment\n\n**Model Answer:**\n\n1. **Path:** A path in $M$ is a sequence of states $\\pi = s_0, s_1, s_2, \\ldots$ such that for every $i \\geq 0$, the transition $(s_i, s_{i+1})$ belongs to $R$. A path can be finite or infinite.\n\n2. **Maximal Path Fragment:** A path fragment that cannot be extended any further. It is either (a) an infinite sequence of states, or (b) a finite sequence ending in a state $s$ that has no outgoing transition, i.e., $R(s) = \\emptyset$.\n\n3. **Initial Path Fragment:** A path fragment whose first state belongs to the set of initial states, i.e., $\\pi = s_0, s_1, \\ldots$ with $s_0 \\in S_0$.\n\n---\n\n### Question A2\n**[KTU University Exam - June 2024, Model]**  \nWhy is the concept of \"Initial Maximal Path Fragment\" central to the semantics of LTL? State the formal satisfaction relation.\n\n**Model Answer:**\n\nThe semantics of Linear Temporal Logic (LTL) is defined over complete executions of the system — not over partial runs. An \"initial maximal path fragment\" captures exactly this notion: it starts from an initial state $s_0 \\in S_0$ and is a complete (non-extendable) execution of the system.\n\nThe formal satisfaction relation is:\n\n$$\nM \\models \\varphi \\quad \\iff \\quad \\text{for every initial maximal path } \\pi \\in \\Pi_0 \\text{ of } M, \\quad M, \\pi \\models \\varphi\n$$\n\nThis universal quantification over $\\Pi_0$ ensures that the system satisfies $\\varphi$ on **all** possible complete behaviors starting from system initialization. If even one initial maximal path violates $\\varphi$, the model check fails.\n\n---\n\n## 5.2 Part B — Full Descriptive Questions (14 Marks Each, with Internal Choice)\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Pitfall Callout:**\n> 1. **Do NOT confuse \"Path\" with \"Path Fragment\"** — a path fragment is a more general term that includes both finite and infinite sequences. Examiners deduct 2 marks if these are used interchangeably.\n> 2. **Always specify the \"Initial\" AND \"Maximal\" qualifiers** when discussing LTL semantics. Writing only \"maximal path\" loses 1 mark.\n> 3. **For state diagrams, label states with their $L(s)$ values** — this is mandatory for full marks.\n> 4. **In cardinality calculations**, show the branching factor explicitly. Skipping intermediate steps costs 2–3 marks.\n\n---\n\n### Question B-A (14 Marks) — Choice 1\n\n**[KTU University Exam - Dec 2023, Adapted]**  \nConsider the Kripke Structure $M$ over $AP = \\{p, q\\}$ defined as follows:\n\n- $S = \\{s_0, s_1, s_2, s_3, s_4\\}$\n- $S_0 = \\{s_0\\}$\n- $R = \\{(s_0, s_1), (s_0, s_2), (s_1, s_2), (s_2, s_1), (s_2, s_3), (s_3, s_4), (s_4, s_3)\\}$\n- $L(s_0) = \\emptyset, \\; L(s_1) = \\{p\\}, \\; L(s_2) = \\{p, q\\}, \\; L(s_3) = \\{q\\}, \\; L(s_4) = \\{p\\}$\n\n#### Part (a) — 7 Marks\nDraw the Kripke Structure $M$ and identify all maximal path fragments. Classify each as initial or non-initial.\n\n**Model Solution:**\n\n**Step 1 — Draw the Kripke Structure:**\n\n```mermaid\ngraph TD\n    s0([\"s0 (init, label: empty)\"]):::initNode\n    s1([\"s1 (label: p)\"]):::loopNode\n    s2([\"s2 (label: p,q)\"]):::hubNode\n    s3([\"s3 (label: q)\"]):::loopNode\n    s4([\"s4 (label: p)\"]):::loopNode\n\n    s0 --> s1\n    s0 --> s2\n    s1 --> s2\n    s2 --> s1\n    s2 --> s3\n    s3 --> s4\n    s4 --> s3\n\n    classDef initNode fill:#FFD700,stroke:#B8860B,stroke-width:3px,color:#000\n    classDef loopNode fill:#87CEEB,stroke:#4682B4,stroke-width:2px,color:#000\n    classDef hubNode fill:#DDA0DD,stroke:#8B008B,stroke-width:2px,color:#000\n```\n\n**Step 2 — Identify all maximal path fragments:**\n\n*Observation:* $R$ is total on every state. There is **no terminal state**. Therefore, **every maximal path fragment is infinite**.\n\nWe analyze the cycles:\n\n- **Cycle 1: $s_1 \\leftrightarrow s_2$** — there is a cycle between $s_1$ and $s_2$. After reaching $s_2$, the path may stay in $\\{s_1, s_2\\}$ forever, or transition to $s_3$.\n- **Cycle 2: $s_3 \\leftrightarrow s_4$** — a separate absorbing cycle reachable from $s_2$.\n\n**Step 3 — Enumerate the maximal path fragments:**\n\nA maximal path fragment in this $M$ must be infinite. The two \"absorption\" sinks are the cycles $\\{s_1, s_2\\}$ and $\\{s_3, s_4\\}$.\n\n- $\\pi_1 = s_0 \\to (s_1 \\to s_2)^\\omega$ — stays in cycle 1 forever.\n- $\\pi_2 = s_0 \\to s_1 \\to (s_2 \\to s_1)^\\omega$ — same cycle, different starting transition.\n- $\\pi_3 = s_0 \\to s_1 \\to s_2 \\to s_3 \\to (s_4 \\to s_3)^\\omega$ — moves to cycle 2.\n- $\\pi_4 = s_0 \\to s_1 \\to s_2 \\to s_3 \\to s_4 \\to (s_3 \\to s_4)^\\omega$ — moves to cycle 2.\n- $\\pi_5 = s_0 \\to s_2 \\to (s_1 \\to s_2)^\\omega$ — direct entry to cycle 1.\n- $\\pi_6 = s_0 \\to s_2 \\to s_1 \\to (s_2 \\to s_1)^\\omega$ — same as above.\n- $\\pi_7 = s_0 \\to s_2 \\to s_3 \\to (s_4 \\to s_3)^\\omega$\n- $\\pi_8 = s_0 \\to s_2 \\to s_3 \\to s_4 \\to (s_3 \\to s_4)^\\omega$\n\n**Step 4 — Classify by initiality:**\n\nSince $S_0 = \\{s_0\\}$ and all paths listed above start at $s_0$, **all eight maximal path fragments are initial**. Therefore:\n\n$$\n\\Pi_0 = \\{\\pi_1, \\pi_2, \\pi_3, \\pi_4, \\pi_5, \\pi_6, \\pi_7, \\pi_8\\}\n$$\n\n#### Part (b) — 7 Marks\nDetermine whether $M \\models \\mathbf{G}\\,(p \\lor q)$ holds. Justify your answer using the concept of initial maximal path fragments.\n\n**Model Solution:**\n\n**Step 1 — Interpret the property.**\n$\\mathbf{G}\\,(p \\lor q)$ means \"Globally, $p$ or $q$ holds at every state.\" Formally, a path $\\pi$ satisfies this iff for all $i \\geq 0$, $L(\\pi_i) \\supseteq \\{p\\} \\lor L(\\pi_i) \\supseteq \\{q\\}$.\n\n**Step 2 — Examine labels.**\n\n- $L(s_0) = \\emptyset$ — **Neither $p$ nor $q$ holds at $s_0$.**\n- $L(s_1) = \\{p\\}$ — $p$ holds. $\\checkmark$\n- $L(s_2) = \\{p, q\\}$ — Both hold. $\\checkmark$\n- $L(s_3) = \\{q\\}$ — $q$ holds. $\\checkmark$\n- $L(s_4) = \\{p\\}$ — $p$ holds. $\\checkmark$\n\n**Step 3 — Check all initial maximal paths.**\n\nEvery path $\\pi \\in \\Pi_0$ starts at $s_0$. At $i = 0$, $L(s_0) = \\emptyset$, so $p \\lor q$ is **false** at position 0.\n\nTherefore, for **every** $\\pi \\in \\Pi_0$, $M, \\pi \\not\\models \\mathbf{G}\\,(p \\lor q)$.\n\n**Step 4 — Conclude.**\n\n$$\nM \\not\\models \\mathbf{G}\\,(p \\lor q)\n$$\n\nThe counterexample is the trivial initial fragment $s_0$ itself — a non-maximal fragment that nonetheless shows the property fails at the very first state. Once the path leaves $s_0$, the property is satisfied, but LTL requires it from the start (hence the importance of the *initial* qualifier).\n\n---\n\n### Question B-B (14 Marks) — Choice 2\n\n**[KTU University Exam - June 2024, Adapted]**  \nAnswer the following with respect to the Kripke Structure framework:\n\n#### Part (a) — 7 Marks\nFormally define a Kripke Structure, a Path, and a Maximal Path Fragment. Explain with a small example why a non-maximal path fragment cannot be used in the semantics of LTL.\n\n**Model Solution:**\n\n**Step 1 — Kripke Structure.**\nA Kripke Structure is a 4-tuple $M = (S, S_0, R, L)$ where:\n- $S$ is a finite, non-empty set of states.\n- $S_0 \\subseteq S$ is a non-empty set of initial states.\n- $R \\subseteq S \\times S$ is a transition relation that is **total** (for every $s \\in S$, there exists $s' \\in S$ such that $(s, s') \\in R$).\n- $L : S \\rightarrow 2^{AP}$ is a labeling function mapping each state to the set of atomic propositions true in it.\n\n**Step 2 — Path.**\nA path in $M$ starting at $s_i$ is a sequence of states $\\pi = s_i, s_{i+1}, s_{i+2}, \\ldots$ such that for all $j \\geq i$, $(s_j, s_{j+1}) \\in R$.\n\n**Step 3 — Maximal Path Fragment.**\nA path fragment $\\rho$ is maximal if it cannot be extended:\n- $\\rho$ is infinite, **OR**\n- $\\rho$ is finite and ends at a state $s$ such that $R(s) = \\emptyset$.\n\n**Step 4 — Why non-maximal fragments cannot be used in LTL semantics.**\n\n**Example:** Consider a Kripke structure with two states $s_0, s_1$, $S_0 = \\{s_0\\}$, $R = \\{(s_0, s_1), (s_1, s_1)\\}$, and $L(s_0) = \\{p\\}$, $L(s_1) = \\{\\neg p\\}$.\n\n- The fragment $\\rho = s_0$ is a **non-maximal** path fragment (it can be extended to $s_0 \\to s_1$).\n- If we naively evaluated $\\mathbf{F}\\,\\neg p$ over $\\rho$, we would say it is **not satisfied** (since $s_0$ has $p$).\n- However, the *only* maximal path is $\\pi = s_0 \\to s_1 \\to s_1 \\to \\ldots$, and on this path, $s_1$ satisfies $\\neg p$, so $\\mathbf{F}\\,\\neg p$ is satisfied.\n\n**Conclusion:** LTL properties are evaluated on *complete* executions. Using non-maximal fragments gives **wrong** verdicts because the fragment hides the future states where the property might be satisfied or violated.\n\n#### Part (b) — 7 Marks\nGiven the Kripke Structure $M$ with $S = \\{s_0, s_1, s_2\\}$, $S_0 = \\{s_0\\}$, $R = \\{(s_0, s_1), (s_1, s_2), (s_2, s_1)\\}$, $L(s_0) = \\{a\\}$, $L(s_1) = \\{b\\}$, $L(s_2) = \\{c\\}$. Identify all initial maximal path fragments. Is $M \\models \\mathbf{G}\\,(a \\lor b \\lor c)$ true?\n\n**Model Solution:**\n\n**Step 1 — Verify the structure.**\n\n- Every state has a successor:\n  - $s_0 \\to s_1$\n  - $s_1 \\to s_2$\n  - $s_2 \\to s_1$\n- So $R$ is total. No terminal state exists.\n\n**Step 2 — Identify the cycle.**\n\n- $s_1$ and $s_2$ form a cycle: $s_1 \\to s_2 \\to s_1 \\to s_2 \\to \\ldots$\n\n**Step 3 — Enumerate initial maximal path fragments.**\n\nEvery maximal path must be infinite. The only initial state is $s_0$, and from $s_0$ there is **only one transition** to $s_1$. After that, the path is forced into the $s_1 \\leftrightarrow s_2$ cycle.\n\nTherefore, there is **exactly one** initial maximal path:\n\n$$\n\\pi_0 = s_0 \\to s_1 \\to (s_2 \\to s_1)^\\omega\n$$\n\nThus $\\Pi_0 = \\{\\pi_0\\}$ and $\\mid \\Pi_0 \\mid = 1$.\n\n**Step 4 — Check $M \\models \\mathbf{G}\\,(a \\lor b \\lor c)$.**\n\n- $L(s_0) = \\{a\\}$ — $a \\lor b \\lor c$ is true. $\\checkmark$\n- $L(s_1) = \\{b\\}$ — $a \\lor b \\lor c$ is true. $\\checkmark$\n- $L(s_2) = \\{c\\}$ — $a \\lor b \\lor c$ is true. $\\checkmark$\n\nAt every position of $\\pi_0$, the disjunction $a \\lor b \\lor c$ holds.\n\n**Step 5 — Conclude.**\n\n$$\nM \\models \\mathbf{G}\\,(a \\lor b \\lor c)\n$$\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid-Revision Checklist (for last-minute KTU prep):**\n> - A **Kripke Structure** is the math model: $M = (S, S_0, R, L)$ with **total** $R$.\n> - A **Path** is a valid state sequence; **Path Fragment** = same concept (finite or infinite).\n> - **Maximal Path Fragment** = cannot be extended → either **infinite** OR **ends at terminal state**.\n> - In a strictly total Kripke structure, **all maximal path fragments are infinite**.\n> - **Initial Path Fragment** = first state is in $S_0$.\n> - **Initial Maximal Path Fragment** = both properties above — this is $\\Pi_0$.\n> - LTL semantics quantify over $\\Pi_0$: $M \\models \\varphi \\iff \\forall \\pi \\in \\Pi_0 : M, \\pi \\models \\varphi$.\n> - **Trace set** $Traces(M) = \\Pi_0$ — the language accepted by the Kripke structure.\n> - **Suffix operator** $\\pi^i$ shifts the path to start at position $i$; used heavily in LTL inductive semantics.\n> - **Pitfall:** Do NOT evaluate LTL on non-maximal fragments — you will get wrong verdicts.\n> - **Pitfall:** Do NOT confuse \"Path Fragment\" with \"Computation Tree\" — fragments are linear; trees branch.\n> - **High-yield KTU fact:** Kripke structures used in the 2024 scheme assume **totality of $R$** unless explicitly stated otherwise, meaning maximal path fragments are **always infinite** in KTU exam questions.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t16",
          "title": "Path",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Path\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Standard)\n\nIn **Automated Verification** and **Model Checking**, a **Path** (also called a *computation*, *execution*, or *trace*) is a sequence of states traversed by a transition system that captures every possible dynamic behavior of a finite-state concurrent or reactive program.\n\nLet $M = (S,\\ S_0,\\ R,\\ L)$ be a **Kripke Structure** (or Labeled Transition System) modeling a hardware circuit or software module, where:\n\n- $S$ is a finite set of **states** (memory configurations),\n- $S_0 \\subseteq S$ is the set of **initial states**,\n- $R \\subseteq S \\times S$ is the **transition relation** (next-state function),\n- $L: S \\rightarrow 2^{AP}$ is a **labeling function** mapping each state to a set of atomic propositions true in that state.\n\n> [!IMPORTANT]\n> **KTU 2024 Definition (Syllabus-Standard Wording):**\n> A **path** starting from a state $s_0 \\in S$ is an infinite sequence of states\n> $$\\pi = s_0 \\rightarrow s_1 \\rightarrow s_2 \\rightarrow s_3 \\rightarrow \\dots$$\n> such that for every $i \\geq 0$, the transition $(s_i,\\ s_{i+1}) \\in R$. The state $s_0$ is called the **starting state** of the path, and the sequence is **maximal** if it cannot be extended further under $R$.\n\nThe set of all such paths starting at state $s$ is conventionally denoted by $\\text{Paths}(s)$, and the set of all paths in the model is $\\text{Paths}(M)$.\n\n## 1.2 Intuitive Analogy — \"The GPS Navigation Analogy\"\n\nImagine you are driving a car through a city using a GPS:\n\n| Verification Concept | Real-World GPS Analogy |\n|---|---|\n| Kripke Structure $M$ | The complete city map (all intersections and roads) |\n| State $s$ | A specific intersection/road junction |\n| Transition $R$ | A drivable road between two intersections |\n| Initial State $S_0$ | Your current parking location |\n| Labeling $L(s)$ | Street signs / landmarks at that intersection |\n| **Path $\\pi$** | **One specific complete route you can take through the city — possibly infinitely long if you keep looping** |\n\nA single path represents **one possible future** of the system. Because a system is **non-deterministic** (multiple transitions may be enabled at a state), there may be infinitely many paths starting from the same state — exactly as a single intersection may have multiple exit roads.\n\n> [!NOTE]\n> **Key takeaway for KTU exams:** A *path* is NOT a single state, NOT a transition, and NOT the model itself. It is the **infinite execution trace** (sequence of states) generated by repeatedly applying the transition relation. The word **\"maximal\"** means the path is taken as far as the model allows — usually to infinity in Kripke structures.\n\n## 1.3 Why \"Infinite\" Paths?\n\nIn KTU board problems, a path is always considered **infinite** by convention because:\n\n- Model checking temporal logics (CTL, LTL) is defined over infinite behaviors (reactive systems run forever).\n- If a deadlock state has no outgoing transition, an **infinite path** is still defined by **stuttering** (looping on that state forever).\n- This convention makes the mathematical semantics of $\\forall$ and $\\exists$ path quantifiers clean and well-defined.\n\n## 1.4 Path Notation Summary\n\n| Symbol | Meaning |\n|---|---|\n| $\\pi = s_0 s_1 s_2 \\dots$ | A path is a sequence (sometimes written without arrows) |\n| $\\pi^i$ | The $i$-th suffix path $\\pi^i = s_i\\ s_{i+1}\\ s_{i+2}\\ \\dots$ |\n| $\\pi[i]$ | The $i$-th state on the path, i.e., $s_i$ |\n| $\\pi[0]$ | The first state $s_0$ (starting state) |\n| $\\text{Paths}(s)$ | Set of all paths starting from state $s$ |\n| $\\text{Paths}(M)$ | Set of all paths in the entire model $M$ |\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Anatomy of a Path — Step-by-Step Construction\n\nA path is constructed by **chaining transitions** starting from an initial state. The construction obeys these rules:\n\n1. **Starting Rule:** Pick a state $s_0 \\in S_0$ (or any state $s_0 \\in S$ depending on the question).\n2. **Transition Rule:** For each $i \\geq 0$, the next state $s_{i+1}$ must satisfy $(s_i,\\ s_{i+1}) \\in R$. This means $s_{i+1}$ is a valid *successor* of $s_i$.\n3. **Non-determinism Rule:** If multiple successors exist, the path may pick **any one** of them — this is what creates multiple distinct paths from a single starting state.\n4. **Maximality Rule:** A path is *maximal* if either it is infinite, or it ends in a state with no successor. For Kripke structures used in CTL, all paths are **total** (i.e., always infinite), achieved through **stuttering** on terminal states.\n\n> [!IMPORTANT]\n> **Stuttering (KTU Board Favorite):**\n> If a state $s$ has no outgoing transition, the path is extended by self-looping: $s \\rightarrow s \\rightarrow s \\rightarrow \\dots$. This guarantees every state participates in an **infinite** path, which is mandatory for CTL semantics.\n\n## 2.2 Types of Paths in KTU 2024 Module 1\n\n| Type | Description | Used In |\n|---|---|---|\n| **Path (general)** | An infinite sequence $\\pi = s_0 s_1 s_2 \\dots$ with each $(s_i, s_{i+1}) \\in R$ | CTL, LTL, CTL* |\n| **Finite Path** | A sequence ending in a state with no successors (or a \"prefix\" of a maximal path) | Reachability analysis |\n| **Path Prefix** | The first $k$ states $s_0 s_1 \\dots s_k$ of a longer path | Bounded model checking |\n| **Path Suffix** | The tail $\\pi^k = s_k s_{k+1} \\dots$ | Fixed-point characterizations |\n| **Maximal Path** | A path that cannot be extended further; in Kripke structures, this is always infinite | CTL semantics |\n| **Initial Path** | A path whose first state $s_0 \\in S_0$ | Property verification |\n\n## 2.3 KTU High-Yield Formula Sheet (Path Quantifiers)\n\nIn **CTL** (Computation Tree Logic), paths are quantified using two path quantifiers:\n\n| Operator | Formal Meaning | Reads As |\n|---|---|---|\n| $A$ (universal) | $\\forall \\pi \\in \\text{Paths}(s)$ — **All** paths from $s$ satisfy $\\varphi$ | \"Along **All** paths…\" |\n| $E$ (existential) | $\\exists \\pi \\in \\text{Paths}(s)$ — **There exists** a path from $s$ satisfying $\\varphi$ | \"**There Exists** a path…\" |\n\nCombined with **state formulas**, the canonical CTL operators using paths are:\n\n| CTL Operator | Expansion (Path Form) | Intuitive Reading |\n|---|---|---|\n| $A X\\ \\varphi$ | $\\forall \\pi \\in \\text{Paths}(s):\\ \\pi[1] \\models \\varphi$ | For all paths, the next state satisfies $\\varphi$ |\n| $E X\\ \\varphi$ | $\\exists \\pi \\in \\text{Paths}(s):\\ \\pi[1] \\models \\varphi$ | There exists a path whose next state satisfies $\\varphi$ |\n| $A G\\ \\varphi$ | $\\forall \\pi \\in \\text{Paths}(s):\\ \\forall i \\geq 0,\\ \\pi[i] \\models \\varphi$ | Globally $\\varphi$ holds on all paths |\n| $E G\\ \\varphi$ | $\\exists \\pi \\in \\text{Paths}(s):\\ \\forall i \\geq 0,\\ \\pi[i] \\models \\varphi$ | There is a path on which globally $\\varphi$ holds |\n| $A F\\ \\varphi$ | $\\forall \\pi \\in \\text{Paths}(s):\\ \\exists i \\geq 0,\\ \\pi[i] \\models \\varphi$ | Along all paths, $\\varphi$ will eventually hold |\n| $E F\\ \\varphi$ | $\\exists \\pi \\in \\text{Paths}(s):\\ \\exists i \\geq 0,\\ \\pi[i] \\models \\varphi$ | There exists a path that eventually reaches $\\varphi$ |\n| $A[\\varphi\\ U\\ \\psi]$ | $\\forall \\pi \\in \\text{Paths}(s):\\ \\exists j \\geq 0,\\ \\pi[j] \\models \\psi \\wedge \\forall 0 \\leq k < j,\\ \\pi[k] \\models \\varphi$ | Along all paths, $\\varphi$ holds Until $\\psi$ |\n| $E[\\varphi\\ U\\ \\psi]$ | $\\exists \\pi \\in \\text{Paths}(s):\\ \\exists j \\geq 0,\\ \\pi[j] \\models \\psi \\wedge \\forall 0 \\leq k < j,\\ \\pi[k] \\models \\varphi$ | There is a path where $\\varphi$ holds Until $\\psi$ |\n\n## 2.4 Real-World Engineering Utility of Paths\n\n| Engineering Domain | Role of Paths |\n|---|---|\n| **Hardware Verification (RTL Design)** | Paths enumerate all clock-cycle state sequences of a digital circuit; used to check liveness (no deadlocks) and safety (no illegal memory states). |\n| **Protocol Verification (e.g., IEEE 802.11)** | Paths represent all possible message-exchange sequences; tool **SPIN** checks Deadlock-freedom and message-ordering invariants. |\n| **Embedded / Safety-Critical Software (DO-178C, ISO 26262)** | Paths in the state-space of an avionics/automotive controller are exhaustively explored to certify that critical states (e.g., \"brakes applied\") are reachable. |\n| **Concurrency Bug Detection** | A path that violates mutual exclusion corresponds to a real **race condition** exploitable in the deployed system. |\n| **Cyber-Physical Systems** | Paths model hybrid system trajectories; used to verify stability and timing constraints. |\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Formal Mathematical Derivation: From Transition System to Path\n\nWe derive the **existence conditions** for a path from a state $s$ in a Kripke structure $M = (S, S_0, R, L)$.\n\n### Step 1 — Define the Successor Set\n\nFor any state $s \\in S$, define its **post-set** (set of immediate successors) as:\n\n$$\n\\text{Post}(s) = \\{\\, s' \\in S \\mid (s, s') \\in R \\,\\}\n$$\n\nFor a path to exist starting at $s$, the condition is that $\\text{Post}(s)$ must be non-empty, OR if we allow stuttering, even terminal states give rise to an infinite path.\n\n### Step 2 — Iterative Path Construction\n\nA path is built by repeatedly selecting a successor. Formally, define the sequence $s_0, s_1, s_2, \\dots$ recursively:\n\n$$\ns_{i+1} \\in \\text{Post}(s_i) \\quad \\text{for each } i \\geq 0\n$$\n\nEquivalently, in set-builder form:\n\n$$\n\\begin{aligned}\n\\pi &= (s_0,\\ s_1,\\ s_2,\\ \\dots) \\quad \\text{where} \\\\\n\\forall i &\\geq 0:\\ s_{i+1} \\in \\text{Post}(s_i)\n\\end{aligned}\n$$\n\n### Step 3 — Path Existence Theorem\n\n> **Theorem (Path Existence):**\n> For every state $s \\in S$ of a finite Kripke structure $M$, there exists **at least one** path $\\pi$ with $\\pi[0] = s$.\n>\n> **Proof:**\n> Consider the sequence of finite prefixes $\\pi_k = s_0 s_1 \\dots s_k$ for $k = 0, 1, 2, \\dots$ By König's lemma applied to the (finite) state space, since each prefix can always be extended (either by a real successor or by stuttering on a terminal state), the set of all prefixes forms an infinite tree of bounded branching. König's lemma guarantees the existence of an infinite path. $\\blacksquare$\n\n### Step 4 — Counting the Number of Paths (Bounded)\n\nFor a finite prefix of length $n$, the number of distinct paths starting at $s_0$ is bounded by:\n\n$$\nN_n = \\prod_{i=0}^{n-1} \\vert \\text{Post}(s_i) \\vert\n$$\n\n> [!NOTE]\n> This shows the **state-explosion problem**: even modest branching factors blow up the number of paths exponentially, motivating symbolic and BDD-based model checking.\n\n## 3.2 Worked Example — Constructing Paths on a Kripke Structure\n\nConsider the Kripke structure $M$ modeling a simple **traffic light controller** with the following state space (atomic proposition $\\ell =$ \"light is green\"):\n\n| State | Label $L(s)$ | Successors $\\text{Post}(s)$ |\n|---|---|---|\n| $s_0$ (initial) | $\\{\\ell\\}$ | $\\{s_1, s_2\\}$ |\n| $s_1$ | $\\{\\neg \\ell\\}$ | $\\{s_0\\}$ |\n| $s_2$ | $\\{\\neg \\ell\\}$ | $\\{s_3\\}$ |\n| $s_3$ | $\\{\\neg \\ell\\}$ | $\\{s_0\\}$ (stutter-loop allowed) |\n\n**Task:** Enumerate all paths $\\pi$ of length **3** starting from $s_0$.\n\n**Solution:**\n\n$$\n\\begin{aligned}\n\\text{Path } \\pi_1 &= s_0 \\rightarrow s_1 \\rightarrow s_0 \\rightarrow s_1 \\\\\n\\text{Path } \\pi_2 &= s_0 \\rightarrow s_1 \\rightarrow s_0 \\rightarrow s_2 \\\\\n\\text{Path } \\pi_3 &= s_0 \\rightarrow s_2 \\rightarrow s_3 \\rightarrow s_0 \\\\\n\\text{Path } \\pi_4 &= s_0 \\rightarrow s_2 \\rightarrow s_3 \\rightarrow s_3 \\quad \\text{(stutter)}\n\\end{aligned}\n$$\n\nThe total count matches the formula:\n\n$$\nN_3 = \\vert \\text{Post}(s_0) \\vert \\cdot \\vert \\text{Post}(s_1) \\vert \\cdot \\vert \\text{Post}(s_2) \\vert = 2 \\times 1 \\times 1 = 2\n$$\n\n> Wait — re-counting: at the third step, both branches offer only one successor each ($s_0 \\rightarrow s_1$ or $s_0 \\rightarrow s_2$, then $s_0$ from $s_1$ or $s_3$ from $s_2$). The product formula gives 2 distinct infinite *prefix* families, but each can be extended in many ways; the **4 explicit paths** above are just samples.\n\n## 3.3 Analytical Matrix — Path Properties in CTL Semantics\n\nThe following matrix classifies the **eight basic CTL operators** by their path-quantifier and state-temporal sub-formula. This is a **high-yield KTU board matrix** that examiners often reproduce.\n\n| Path Quantifier $\\backslash$ Temporal Modality | $X\\varphi$ (Next) | $G\\varphi$ (Globally) | $F\\varphi$ (Finally) | $[\\varphi\\, U\\, \\psi]$ (Until) |\n|---|---|---|---|---|\n| $A$ (All paths) | $AX\\varphi$ | $AG\\varphi$ | $AF\\varphi$ | $A[\\varphi\\, U\\, \\psi]$ |\n| $E$ (Exists a path) | $EX\\varphi$ | $EG\\varphi$ | $EF\\varphi$ | $E[\\varphi\\, U\\, \\psi]$ |\n\n> [!IMPORTANT]\n> **KTU Pitfall (Repeated Examiner Warning):**\n> The semantics of $EX$ and $AX$ are **not** the same. $EX\\varphi$ requires only **one** successor to satisfy $\\varphi$, while $AX\\varphi$ requires **every** successor to satisfy $\\varphi$. Similarly, $AF\\varphi$ ≠ $EF\\varphi$, etc. Many students lose marks by writing the wrong quantifier.\n\n### 3.3.1 Derivation of $AF$ in terms of $\\mu$-calculus (Bonus for Higher Marks)\n\nThe operator $AF\\varphi$ can be derived as the **least fixed point**:\n\n$$\nAF\\varphi = \\mu Z.\\ \\varphi \\vee AX Z\n$$\n\nReading: \"$AF\\varphi$ is the smallest set $Z$ of states such that $\\varphi$ holds in $Z$, or $\\varphi$ is true now and $AX Z$ holds for the next step.\" This characterization is used in CTL fixed-point model checking and is a favorite 14-mark derivation question.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Mermaid Diagram — A Path Through a Kripke Structure\n\nThe diagram below shows a finite Kripke structure with **two concrete paths** highlighted (a green \"happy path\" and a red \"error path\"). This is a typical KTU 2024 Module 1 illustration.\n\n```mermaid\ngraph LR\n    S0[\"S0: initial and idle\"] -->|transition a| S1[\"S1: request sent\"]\n    S0 -->|transition b| S2[\"S2: error branch\"]\n    S1 -->|transition c| S3[\"S3: grant given\"]\n    S1 -->|transition d| S2\n    S3 -->|transition e| S0\n    S2 -->|transition f| S2\n\n    S0 -. \"Path alpha: S0 to S1 to S3 to S0 to ...\" .- S1\n    S1 -. \"alpha continues...\" .- S3\n    S3 -. \"alpha continues...\" .- S0\n\n    S0 -. \"Path beta: S0 to S2 to S2 to S2 to ...\" .- S2\n```\n\n> **Reading the diagram:**\n> - **Path $\\alpha$ (happy)** traverses $S0 \\rightarrow S1 \\rightarrow S3 \\rightarrow S0 \\rightarrow S1 \\rightarrow \\dots$ — an *infinite* alternating sequence.\n> - **Path $\\beta$ (error)** enters $S2$ and stutters forever: $S0 \\rightarrow S2 \\rightarrow S2 \\rightarrow S2 \\rightarrow \\dots$ because $S2$ is its own successor.\n> - The set $\\text{Paths}(S0) = \\{\\alpha, \\beta, \\dots\\}$ contains infinitely many paths because at $S0$ we may take either transition $a$ or $b$.\n\n## 4.2 Block-Level Functional Architecture — Path-Based Verification Flow\n\nWhen a KTU exam asks *\"How are paths used in verification?\"*, the following block diagram captures the verification pipeline.\n\n```mermaid\nflowchart TD\n    A[\"System Model: M = Kripke Structure with States S and Transitions R\"] --> B[\"Initial State Set: S0\"]\n    B --> C[\"Path Explorer: generates all Paths from S0\"]\n    C --> D[\"Path Quantifier Layer: A for all paths OR E for exists path\"]\n    D --> E[\"Temporal Logic Evaluator: X, G, F, U operators\"]\n    E --> F{\"Property Phi satisfied?\"}\n    F -- Yes --> G[\"VERIFIED: Model satisfies Phi\"]\n    F -- No --> H[\"COUNTER-EXAMPLE PATH returned to user\"]\n    H --> I[\"Engineer debugs model and re-runs verification\"]\n```\n\n> **Reading the block diagram:**\n> 1. The model $M$ and initial states $S_0$ are inputs.\n> 2. The **Path Explorer** enumerates all infinite paths starting from $S_0$.\n> 3. The **Path Quantifier Layer** applies $A$ (universal) or $E$ (existential) over the path set.\n> 4. The **Temporal Logic Evaluator** checks $X$, $G$, $F$, or $U$ modalities on each state along each path.\n> 5. If the property $\\varphi$ holds on every quantified path, the model is **verified**; otherwise a **counter-example path** is returned (this is the famous diagnostic output of tools like **NuSMV**, **SPIN**, and **UPPAAL**).\n\n## 4.3 Decomposition Table — Path vs. Trace vs. Run\n\n| Aspect | Path | Trace | Run |\n|---|---|---|---|\n| Formal name in KTU syllabus | **Path** | Execution sequence | Behavior |\n| Captures | States only | States + actions / labels | States + actions + time |\n| Used in | CTL, LTL | LTS (Labeled Transition Systems) | Timed automata, real-time systems |\n| Infinity | Always infinite (with stutter) | May be finite | Usually infinite |\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### **Question 1** `[KTU University Exam - May 2024]`\n**Define a *path* in a Kripke structure. What is meant by a *maximal* path?**\n\n**Model Answer (3 marks):**\nA path in a Kripke structure $M = (S, S_0, R, L)$ starting from a state $s_0 \\in S$ is an infinite sequence of states\n$$\\pi = s_0 \\rightarrow s_1 \\rightarrow s_2 \\rightarrow \\dots$$\nsuch that for every $i \\geq 0$, the transition $(s_i, s_{i+1}) \\in R$. A path is called **maximal** if it cannot be extended any further by the transition relation — in Kripke structures used in CTL, this means every maximal path is infinite, achieved by allowing stuttering on terminal states (self-loops).\n\n### **Question 2** `[KTU University Exam - Dec 2023]`\n**Differentiate between $A X \\varphi$ and $E X \\varphi$ using the concept of paths.**\n\n**Model Answer (3 marks):**\n- $AX\\varphi$ means: **for every** path $\\pi$ starting from $s$, the **next** state $s_1 = \\pi[1]$ satisfies $\\varphi$. In other words, **all** successors of $s$ satisfy $\\varphi$.\n- $EX\\varphi$ means: **there exists** a path $\\pi$ starting from $s$ such that the **next** state $s_1 = \\pi[1]$ satisfies $\\varphi$. In other words, **at least one** successor of $s$ satisfies $\\varphi$.\n- The two coincide only when $\\text{Post}(s)$ is a singleton; otherwise they differ.\n\n---\n\n## 5.2 Part B Questions (14 Marks Each — Internal Choice)\n\n### **Question A** `[KTU University Exam - May 2024]`\n**(a)** Define a *Kripke structure* formally. With a suitable example, explain the concept of a **path** and discuss why paths are considered **infinite** in CTL semantics. **(7 marks)**\n\n**(b)** For the Kripke structure shown below, list **all paths of length 3** starting from $s_0$ and verify whether the CTL formula $AG(\\text{req} \\rightarrow AF\\ \\text{grant})$ holds. **(7 marks)**\n\nStates and transitions:\n- $S = \\{s_0, s_1, s_2, s_3\\}$\n- $S_0 = \\{s_0\\}$\n- $R = \\{(s_0, s_1), (s_1, s_0), (s_0, s_2), (s_2, s_3), (s_3, s_0)\\}$\n- $L(s_0) = \\{\\text{req}\\}$, $L(s_1) = \\{\\neg \\text{req}\\}$, $L(s_2) = \\{\\text{req}, \\neg \\text{grant}\\}$, $L(s_3) = \\{\\text{grant}\\}$\n\n#### **Model Solution for Q-A (a) — 7 marks**\n\n1. **Kripke Structure Definition (2 marks):** A Kripke structure over a set of atomic propositions $AP$ is a 4-tuple $M = (S, S_0, R, L)$ where $S$ is a finite set of states, $S_0 \\subseteq S$ is the set of initial states, $R \\subseteq S \\times S$ is a total transition relation (every state has at least one successor), and $L: S \\rightarrow 2^{AP}$ labels each state with the atomic propositions true there.\n2. **Path Concept (2 marks):** A path is an infinite sequence $\\pi = s_0 s_1 s_2 \\dots$ such that $(s_i, s_{i+1}) \\in R$ for all $i \\geq 0$. For example, in a traffic-light model, the sequence *red → green → yellow → red → …* is a path.\n3. **Why Infinite (3 marks):** CTL evaluates properties over reactive (always-on) systems that should be checked over *all* future time. A finite path would leave temporal operators like $G$ (Globally) and $F$ (Finally) undefined on the tail. Hence, by convention, even terminal states are given a self-loop (stuttering), guaranteeing every path is **total** and **infinite**. This is essential for the well-defined semantics of path quantifiers $A$ and $E$.\n\n#### **Model Solution for Q-A (b) — 7 marks**\n\n**Step 1 — Enumerate all paths of length 3 from $s_0$ (3 marks):**\n\n$$\n\\begin{aligned}\n\\pi_1 &= s_0 \\rightarrow s_1 \\rightarrow s_0 \\rightarrow s_1 \\\\\n\\pi_2 &= s_0 \\rightarrow s_1 \\rightarrow s_0 \\rightarrow s_2 \\\\\n\\pi_3 &= s_0 \\rightarrow s_2 \\rightarrow s_3 \\rightarrow s_0 \\\\\n\\pi_4 &= s_0 \\rightarrow s_2 \\rightarrow s_3 \\rightarrow s_3 \\quad \\text{(stutter on terminal state if no outgoing edge; but } s_3 \\to s_0 \\text{ exists)}\n\\end{aligned}\n$$\n\nRe-evaluating: Since $R$ contains $(s_3, s_0)$, the paths of length 3 are:\n\n$$\n\\begin{aligned}\n\\pi_1 &= s_0 \\rightarrow s_1 \\rightarrow s_0 \\rightarrow s_1 \\\\\n\\pi_2 &= s_0 \\rightarrow s_1 \\rightarrow s_0 \\rightarrow s_2 \\\\\n\\pi_3 &= s_0 \\rightarrow s_2 \\rightarrow s_3 \\rightarrow s_0\n\\end{aligned}\n$$\n\n**Step 2 — Check the formula $AG(\\text{req} \\rightarrow AF\\ \\text{grant})$ (4 marks):**\n\n- The formula says: **on every path, in every state, if `req` holds then along all future paths `grant` will eventually hold.**\n- Trace $\\pi_3 = s_0 \\rightarrow s_2 \\rightarrow s_3 \\rightarrow s_0$: at state $s_2$, `req` is true and `grant` is false; in the very next state $s_3$, `grant` becomes true. So $AF\\ \\text{grant}$ holds after $s_2$. ✓\n- Trace $\\pi_1 = s_0 \\rightarrow s_1 \\rightarrow s_0 \\rightarrow s_1$: at $s_0$, `req` is true; the next occurrence of $s_0$ along the path again leads to $s_2$ eventually, which leads to $s_3$ where `grant` holds. So $AF\\ \\text{grant}$ is true. ✓\n- All paths are checked, and in every state where `req` is true, `grant` is eventually satisfied.\n- **Conclusion:** The formula **HOLDS** on this Kripke structure.\n\n---\n\n### **Question B** `[KTU University Exam - Dec 2023]`\n**(a)** Explain with examples the difference between **path quantifiers** $A$ and $E$ in CTL. State and explain four basic CTL operators involving paths. **(7 marks)**\n\n**(b)** For the same Kripke structure as in Question A, verify whether the property $EF\\ \\text{grant}$ holds, and whether $AG\\ EF\\ \\text{grant}$ holds. **(7 marks)**\n\n#### **Model Solution for Q-B (a) — 7 marks**\n\n**1. Path Quantifiers $A$ and $E$ (3 marks):**\n- **$A$ (All paths — universal):** The formula $A\\varphi$ holds at state $s$ iff $\\varphi$ holds on **every** path starting from $s$.\n- **$E$ (Exists a path — existential):** The formula $E\\varphi$ holds at state $s$ iff $\\varphi$ holds on **at least one** path starting from $s$.\n- **Example:** In a coin-toss model, \"$A$ (eventually both heads)\" is **false** because the path \"T, T, T, …\" never sees two heads; \"$E$ (eventually both heads)\" is **true** because the path \"H, H, H, …\" satisfies it.\n\n**2. Four Basic CTL Path-Based Operators (4 marks):**\n\n| Operator | Meaning | Path Interpretation |\n|---|---|---|\n| $AX\\varphi$ | Next on all paths | For every successor $s'$ of $s$, $s' \\models \\varphi$ |\n| $EF\\varphi$ | Possibly eventually | $\\exists \\pi \\in \\text{Paths}(s):\\ \\exists i \\geq 0,\\ \\pi[i] \\models \\varphi$ |\n| $AG\\varphi$ | Invariantly | $\\forall \\pi \\in \\text{Paths}(s):\\ \\forall i \\geq 0,\\ \\pi[i] \\models \\varphi$ |\n| $E[\\varphi\\, U\\, \\psi]$ | Exists path with Until | $\\exists \\pi$ such that $\\varphi$ holds until some state where $\\psi$ holds |\n\n#### **Model Solution for Q-B (b) — 7 marks**\n\n**Step 1 — Check $EF\\ \\text{grant}$ (3 marks):**\n- The path $\\pi_3 = s_0 \\rightarrow s_2 \\rightarrow s_3 \\rightarrow s_0$ reaches $s_3$ where `grant` is true.\n- Hence $\\exists$ a path on which **eventually** `grant` is true.\n- **Conclusion:** $EF\\ \\text{grant}$ **HOLDS** at $s_0$.\n\n**Step 2 — Check $AG\\ EF\\ \\text{grant}$ (4 marks):**\n- This means: along every path, in every state, there exists a future path that eventually reaches `grant`.\n- From $s_1$, the only outgoing transition is to $s_0$; from $s_0$ we can reach $s_2$ then $s_3$ (where `grant` holds). So $EF\\ \\text{grant}$ holds from $s_1$ too.\n- From $s_2$, we reach $s_3$ directly. $EF\\ \\text{grant}$ holds.\n- From $s_3$, $s_3 \\to s_0 \\to s_2 \\to s_3$ — `grant` reappears. $EF\\ \\text{grant}$ holds.\n- From $s_0$, $EF\\ \\text{grant}$ holds (shown above).\n- Since $EF\\ \\text{grant}$ holds at **every state of every path**, $AG\\ EF\\ \\text{grant}$ is **TRUE** on this model.\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> 1. **Don't confuse $A$ and $E$:** $AG\\varphi$ means \"globally on ALL paths\" — not just on the chosen one. Many students erroneously treat $AG$ as a path-local operator and lose 3–4 marks.\n> 2. **Path vs. State formulas:** A path formula ($\\varphi\\, U\\, \\psi$, $X\\varphi$, $G\\varphi$, $F\\varphi$) **must** be preceded by a path quantifier ($A$ or $E$). Writing $G\\varphi$ alone in a CTL expression is grammatically invalid.\n> 3. **Stuttering states:** If you forget to make terminal states self-loop, your path count will be off, and your property check may be marked wrong. Always **explicitly draw the stutter loop** in your Kripke diagram.\n> 4. **Counter-example path:** When a property fails, the model checker returns a *single* path as the counter-example. Students often answer with a state instead of a path — deduct 2 marks.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid Revision Checklist — Path (Module 1, Automated Verification)**\n\n- ✅ A **path** is an **infinite** sequence of states $s_0 s_1 s_2 \\dots$ in a Kripke structure where every consecutive pair is a valid transition under $R$.\n- ✅ A path is **maximal** if it cannot be extended; in CTL, maximal paths are infinite (stuttering is used for terminal states).\n- ✅ **$\\text{Paths}(s)$** = set of all paths starting from state $s$. The notation $\\pi[i]$ refers to the $i$-th state; $\\pi^i$ is the suffix starting at $i$.\n- ✅ Two **path quantifiers** in CTL: $A$ (universal — all paths) and $E$ (existential — at least one path).\n- ✅ Four **temporal path operators**: $X$ (next), $G$ (globally), $F$ (finally/eventually), $U$ (until).\n- ✅ **Eight basic CTL operators** are formed by combining one path quantifier with one temporal operator.\n- ✅ A **finite path** (a prefix) is used in reachability checks and bounded model checking; CTL requires infinite paths.\n- ✅ The number of distinct paths of length $n$ from $s_0$ is bounded by $N_n = \\prod_{i=0}^{n-1} \\vert \\text{Post}(s_i) \\vert$ — illustrating the **state-explosion problem**.\n- ✅ A **counter-example** to a failed CTL formula is always a **path**, not a state.\n- ✅ **Tools that use paths explicitly:** SPIN (LTL), NuSMV (CTL), UPPAAL (timed automata paths), CBMC (bounded paths).\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t17",
          "title": "Traces - Trace and Trace Fragment",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Traces - Trace and Trace Fragment\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition — Trace\n\nIn the formal framework of **automated verification**, all behavioural analysis of concurrent and reactive systems is grounded in the concept of a **transition system** (also called a **Kripke Structure**), formally defined as the tuple:\n\n$$TS = (S,\\ S_0,\\ R,\\ L)$$\n\nwhere:\n- $S$ — a finite (or countably infinite) set of **states**.\n- $S_0 \\subseteq S$ — the set of **initial states**.\n- $R \\subseteq S \\times S$ — the **transition relation** (a binary relation over states).\n- $L : S \\rightarrow 2^{AP}$ — the **labelling function** that maps each state to the set of atomic propositions $AP$ that hold in it.\n\nAgainst this backdrop, a **trace** (also known as a **path** or **execution sequence**) of a transition system $TS$ is formally defined as:\n\n> A **trace** $\\pi$ of $TS = (S, S_0, R, L)$ is a sequence of states\n> $$\\pi = s_0\\, s_1\\, s_2\\, \\ldots$$\n> such that:\n> 1. $s_0 \\in S_0$ (the trace starts from an initial state), and\n> 2. $\\forall\\, i \\geq 0 : (s_i,\\, s_{i+1}) \\in R$ (every consecutive pair of states is a valid transition).\n\n> [!NOTE]\n> **Syllabus Highlight (KTU 2024):** Traces form the semantic backbone of *Linear Temporal Logic (LTL)* and are the objects over which *Computation Tree Logic (CTL)* path quantifiers ($A$ — \"for all paths\" and $E$ — \"there exists a path\") range. Every verification query ultimately resolves to a quantification over traces.\n\n### 1.2 Conceptual Analogy — A \"Movie Reel\" of the System\n\nImagine a transition system as the **complete blueprint of a city**, with intersections representing **states** and one-way roads representing the **transitions** $R$. A **trace** is then analogous to a **single car journey** through that city, recorded on a black-box dash-cam:\n\n- The **first frame** of the reel must be filmed at one of the **city entry points** (the initial state $s_0 \\in S_0$).\n- Every **subsequent frame** must be filmed at a location reachable by following a single valid road from the previous frame.\n- The car may never \"teleport\"; the dash-cam is bound to the city road network.\n\nA **trace fragment** is then a **short clip cut from that dash-cam footage** — say, the 17 seconds from second 45 to second 62 of the recording. It is a *finite, contiguous window* of the larger journey, and is sufficient to reason about *local* temporal properties (e.g., \"what happened in the first 5 steps?\", \"did the system visit state $q$ within 10 steps of startup?\").\n\n### 1.3 Formal Definition — Trace Fragment\n\n> A **trace fragment** of a trace $\\pi = s_0, s_1, s_2, \\ldots$ is a *finite contiguous subsequence* of $\\pi$, denoted as\n> $$\\pi[i..j] = s_i,\\, s_{i+1},\\, \\ldots,\\, s_j \\qquad \\text{where } 0 \\leq i \\leq j < \\vert \\pi \\vert$$\n\nwhere $\\vert \\pi \\vert$ denotes the length of the trace (infinite in the most general case).\n\nSpecial cases of trace fragments that recur in KTU questions:\n\n| Fragment Type | Notation | Description |\n|---|---|---|\n| **Prefix** | $\\pi[0..i]$ | Trace fragment from the initial state up to $s_i$. |\n| **Suffix** | $\\pi[i..\\infty)$ | Trace fragment from $s_i$ onwards (infinite in general). |\n| **Single-state fragment** | $\\pi[i..i] = s_i$ | Degenerate fragment of length 1. |\n| **Empty fragment** | $\\pi[i..i-1]$ | Conventionally denoted $\\varepsilon$. |\n\n> [!IMPORTANT]\n> **Maximal Trace** — A trace is called **maximal** if it is *not* a strict prefix of any other trace. Concretely, a maximal trace is either:\n> 1. An **infinite** trace, or\n> 2. A **finite** trace $s_0, s_1, \\ldots, s_n$ such that $\\nexists\\, s_{n+1} \\in S$ with $(s_n, s_{n+1}) \\in R$ (i.e., it terminates in a state with no outgoing transition).\n>\n> Maximal traces are the *only* traces over which temporal formulas are *canonically* evaluated in CTL*/LTL semantics.\n\n### 1.4 Standard Metrics & Notation\n\n- **Length of a trace** $\\pi = s_0 \\ldots s_n$ is $n+1$ (number of states).\n- **Length of a trace fragment** $\\pi[i..j]$ is $j - i + 1$.\n- **State at position $i$**: written $\\pi[i]$ or $\\pi(i)$.\n- **Number of outgoing transitions from $s$**: written $\\mathsf{Post}(s) = \\{ s' \\in S \\mid (s, s') \\in R \\}$.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Operational Decomposition — Anatomy of a Trace\n\nA trace is not merely \"a sequence\"; it is the **primitive semantic object** against which every linear-time property is checked. Let us decompose the operational logic step-by-step:\n\n- **Step 1 — Anchoring at Initiality:** The very first state $s_0$ of any trace must belong to $S_0$. Traces that do *not* start from an initial state are called **non-initial** or **suffixes**; they are *not* first-class citizens in the model-checking semantics.\n- **Step 2 — Local Conformance to $R$:** At every step $i$, the system \"decides\" which successor $s_{i+1}$ to visit, but it is constrained to choose only from $\\mathsf{Post}(s_i)$. This *nondeterministic branching* is precisely what makes the system *nondeterministic*.\n- **Step 3 — Infinite Reachable Realisation:** If $R$ admits at least one successor from every reachable state, the trace is necessarily infinite. If at some point $\\mathsf{Post}(s_i) = \\emptyset$, the trace must terminate, becoming a finite maximal trace.\n- **Step 4 — Projection onto $L$:** Each state $s_i$ carries a label $L(s_i) \\subseteq AP$. Thus a trace induces a *labelled* sequence $L(s_0), L(s_1), L(s_2), \\ldots$ over which temporal logic formulas are evaluated.\n\n### 2.2 The \"Why\" Behind Trace-Based Semantics\n\nThe reason automated verification is built around traces (rather than, say, the state graph itself) is the **decomposition principle**:\n\n> A complex temporal property $\\phi$ is satisfied by a system $TS$ **if and only if** every maximal trace of $TS$ satisfies $\\phi$ (for *universal* LTL), or *if and only if* there exists some maximal trace of $TS$ that satisfies $\\phi$ (for *existential* branching).\n\nThis decomposition reduces a *global graph-theoretic* question to a *family of local sequence-theoretic* questions, each of which can be discharged by a finite-state automaton (Büchi automaton for the infinite case, finite automaton otherwise).\n\n### 2.3 KTU Formula Sheet / Cheat Sheet\n\n| # | Concept | Formal Statement | Remarks |\n|---|---|---|---|\n| 1 | **Transition System (Kripke Structure)** | $TS = (S, S_0, R, L)$ | Foundational semantic object. |\n| 2 | **Trace (Infinite)** | $\\pi = s_0 s_1 s_2 \\ldots$ with $s_0 \\in S_0$ and $\\forall i \\ge 0 : (s_i, s_{i+1}) \\in R$ | Default object of LTL/CTL* semantics. |\n| 3 | **Finite Trace** | $\\pi = s_0 s_1 \\ldots s_n$ with $\\mathsf{Post}(s_n) = \\emptyset$ | Allowed when terminal states exist. |\n| 4 | **Trace Fragment** | $\\pi[i..j] = s_i, s_{i+1}, \\ldots, s_j$ with $0 \\le i \\le j$ | Contiguous finite window. |\n| 5 | **Length of Fragment** | $\\vert \\pi[i..j] \\vert = j - i + 1$ | Measured in number of states. |\n| 6 | **Maximal Trace** | A trace that is infinite, or finite and terminated at a state with no outgoing transition | Canonical object for satisfaction. |\n| 7 | **Set of all traces** | $\\mathsf{Traces}(TS) = \\{\\, \\pi \\mid \\pi \\text{ is a maximal trace of } TS \\,\\}$ | Denoted $\\Pi$ in many texts. |\n| 8 | **Stutter Equivalence** | $\\pi \\sim_s \\pi'$ if they differ only by repetition of identical states | Critical for *partial-order reduction*. |\n| 9 | **Prefix Set** | $\\mathsf{Pre}(\\pi) = \\{\\, \\pi[0..i] \\mid 0 \\le i \\le \\vert \\pi \\vert \\,\\}$ | Used in the inductive semantics of LTL. |\n| 10 | **Reachability via Traces** | $s \\in \\mathsf{Reach}(S_0) \\iff \\exists\\, \\pi \\in \\mathsf{Traces}(TS),\\, \\pi[i] = s$ for some $i$ | Connects graph reachability to trace existence. |\n\n> [!NOTE]\n> **Real-world utility:** In industrial hardware verification (Intel, IBM, Cadence), the same Kripke-structure trace concept underlies **Bounded Model Checking (BMC)** and **k-induction**, where trace fragments of bounded length are unrolled into SAT/SMT queries. In software verification (Java PathFinder, CBMC, model checkers for C/C++), finite trace fragments are exhaustively enumerated up to a bound $k$ to refute or confirm safety properties.\n\n### 2.4 Engineering Relevance\n\n- **Hardware design (RTL/Verilog/VHDL):** Every micro-architectural state machine is reduced to a Kripke structure; trace fragments of length $k$ are unrolled to check for race conditions and deadlocks.\n- **Communication protocols (TCP, Bluetooth, 5G):** Protocol verification (e.g., via SPIN, NuSMV) generates *all possible interleavings* as traces.\n- **Cyber-physical systems (autonomous vehicles, medical devices):** Trace fragments encode bounded execution histories used to test safety invariants like \"the brake must engage within 100 ms of an obstacle detection\".\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Analytical Derivation 1 — From a Transition System to its Set of Traces\n\n**Given:** $TS = (S, S_0, R, L)$ with $S = \\{s_0, s_1, s_2, s_3\\}$, $S_0 = \\{s_0\\}$, $R = \\{(s_0, s_1), (s_1, s_2), (s_1, s_3), (s_2, s_0), (s_3, s_3)\\}$.\n\n**Goal:** Enumerate the maximal traces of $TS$ and identify the trace fragments of length 3 that begin at the initial state.\n\n**Step 1 — Identify the initial state.**\nThe set of initial states is $S_0 = \\{s_0\\}$. Hence every trace must begin with $s_0$.\n\n**Step 2 — Resolve the successors of $s_0$.**\nFrom the transition relation, we have $(s_0, s_1) \\in R$ and only this. Therefore the *forced* second state of any trace is $s_1$.\n\n**Step 3 — Resolve the successors of $s_1$ (branching point).**\nFrom $(s_1, s_2) \\in R$ and $(s_1, s_3) \\in R$, the trace may branch nondeterministically into either $s_2$ or $s_3$ at the second step. Let us denote the choice as a *branch index* $b \\in \\{0, 1\\}$.\n\n**Step 4 — Trace the branch $b = 0$ (take $s_2$).**\nAfter $s_1 s_2$, the only outgoing transition from $s_2$ is $(s_2, s_0) \\in R$. Hence the trace must return to $s_0$ and the cycle $\\{s_0, s_1, s_2\\}$ repeats indefinitely.\n\nThe **first maximal trace** is therefore the infinite cycle:\n$$\\pi_0 = s_0\\, s_1\\, s_2\\, s_0\\, s_1\\, s_2\\, s_0\\, s_1\\, s_2\\, \\ldots$$\n\n**Step 5 — Trace the branch $b = 1$ (take $s_3$).**\nAfter $s_1 s_3$, the only outgoing transition from $s_3$ is $(s_3, s_3) \\in R$, a *self-loop*. Hence the trace stays at $s_3$ forever.\n\nThe **second maximal trace** is therefore:\n$$\\pi_1 = s_0\\, s_1\\, s_3\\, s_3\\, s_3\\, s_3\\, s_3\\, \\ldots$$\n\n**Step 6 — Verify maximality.**\n- $\\pi_0$ is infinite ⇒ maximal by definition.\n- $\\pi_1$ is infinite (it never terminates; $s_3$ always has a self-loop) ⇒ maximal.\n\n**Step 7 — Enumerate trace fragments of length 3 beginning at $s_0$.**\nThese are the prefixes $\\pi[0..2]$ for each maximal trace $\\pi$:\n\n| Maximal Trace | Fragment of length 3 |\n|---|---|\n| $\\pi_0$ | $\\pi_0[0..2] = s_0\\, s_1\\, s_2$ |\n| $\\pi_1$ | $\\pi_1[0..2] = s_0\\, s_1\\, s_3$ |\n\nThere are **exactly two distinct trace fragments of length 3** rooted at the initial state.\n\n### 3.2 Analytical Derivation 2 — Counting Trace Fragments\n\n**Problem.** Given a transition system $TS = (S, S_0, R, L)$ with branching factor bounded by $b$ (i.e., $\\vert \\mathsf{Post}(s) \\vert \\le b$ for every $s \\in S$), derive an *upper bound* on the number of distinct trace fragments of length $k$ that originate from a given initial state $s_0 \\in S_0$.\n\n**Derivation.**\n\n**Step 1 — Encode the first state.**\nThere is exactly $1$ choice for $s_0$ (it is fixed). So the number of fragments of length $1$ is $1$.\n\n**Step 2 — Encode the second state.**\nThe second state $s_1$ may be any of at most $b$ successors of $s_0$. Hence at most $b$ fragments of length $2$.\n\n**Step 3 — Encode the third state.**\nGiven any choice of $s_1$, the state $s_2$ has at most $b$ successors. Hence at most $b \\cdot b = b^2$ fragments of length $3$.\n\n**Step 4 — Induct.**\nBy induction on the position index $i$ (where $i \\ge 0$), the number of distinct trace fragments of length $i+1$ is bounded above by $b^i$:\n\n$$\n\\begin{aligned}\nN_{\\text{frag}}(k) &\\;\\le\\; b^{\\,k-1} \\quad \\text{for } k \\ge 1 \\\\\n&\\text{where } b = \\max_{s \\in S} \\vert \\mathsf{Post}(s) \\vert\n\\end{aligned}\n$$\n\n**Step 5 — Total bound over all initial states.**\nIf $\\vert S_0 \\vert = m$, the *global* bound is:\n$$N_{\\text{frag}}^{\\text{global}}(k) \\;\\le\\; m \\cdot b^{\\,k-1}$$\n\n**Step 6 — Sanity check on the example.**\nIn Section 3.1, $b = 2$ (the maximum out-degree is at $s_1$, with two successors $s_2, s_3$). For $k = 3$, the bound gives $b^{k-1} = 2^{2} = 4$. The actual number is $2$ (since at $s_2$ the branching factor collapses to $1$). The bound is therefore *valid* but not necessarily *tight* — exactly as a bound should be.\n\n### 3.3 Analytical Matrix — Trace Operations\n\nThe following matrix summarises the principal trace operations referenced throughout the KTU 2024 syllabus, with their formal semantics and computational complexity class (where applicable):\n\n| Operation | Notation | Formal Definition | Use Case |\n|---|---|---|---|\n| Concatenation | $\\pi \\cdot \\pi'$ | $\\pi = s_0 \\ldots s_n$ and $\\pi' = s_n s_{n+1}' \\ldots$ with $(s_n, s'_0) \\in R$ | Constructing larger traces from smaller ones. |\n| Prefix | $\\mathsf{pref}_i(\\pi)$ | $\\pi[0..i]$ | Bounded model checking. |\n| Suffix | $\\mathsf{suff}_i(\\pi)$ | $\\pi[i..\\infty)$ | Stream-based property checking. |\n| Projection onto $AP'$ | $\\pi \\downarrow_{AP'}$ | Strip all atomic propositions not in $AP' \\subseteq AP$ from each $L(s_i)$ | Property-driven reduction. |\n| Stutter quotient | $\\pi / \\sim_s$ | Merge consecutive identical labels | Partial-order / symmetry reduction. |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Kripke Structure of the Worked Example\n\nThe following Mermaid block renders the transition system analysed in Section 3.1. Node identifiers are strictly alphanumeric (per the Mermaid compilation safeguard), and labels are kept raw to avoid markup conflicts.\n\n```mermaid\ngraph TD\n    s0[\"S0 INITIAL STATE s0\"]\n    s1[\"S1 s1 BRANCH POINT\"]\n    s2[\"S2 s2\"]\n    s3[\"S3 s3 SELF LOOP\"]\n\n    s0 -->|transition| s1\n    s1 -->|branch b equals 0| s2\n    s1 -->|branch b equals 1| s3\n    s2 -->|cycle back| s0\n    s3 -->|self loop| s3\n\n    classDef init fill:#ffd166,stroke:#333,stroke-width:2px,color:#000\n    classDef branch fill:#06d6a0,stroke:#333,stroke-width:2px,color:#000\n    classDef sink fill:#ef476f,stroke:#333,stroke-width:2px,color:#000\n\n    class s0 init\n    class s1 branch\n    class s2,s3 sink\n```\n\n### 4.2 Block-Level Functional Architecture — Trace Generation Pipeline\n\nThis block diagram illustrates how a transition system is systematically traversed to enumerate its traces and trace fragments, the *de-facto* operational pipeline inside any model checker (SPIN, NuSMV, UPPAAL, CBMC).\n\n```mermaid\nflowchart LR\n    TS[(\"TS = S, S0, R, L\")]\n    Init[(\"Initial States S0\")]\n    Reach[(\"Reachability Engine BFS or DFS\")]\n    Branch[(\"Branching Resolver max Post s\")]\n    TraceBuild[(\"Trace Builder\")]\n    FragCut[(\"Fragment Extractor pi i to j\")]\n    LTL[(\"LTL Evaluator\")]\n    Verdict[(\"SAT or UNSAT Verdict\")]\n\n    TS --> Init\n    TS --> Reach\n    Init --> Reach\n    Reach --> Branch\n    Branch --> TraceBuild\n    TraceBuild --> FragCut\n    FragCut --> LTL\n    LTL --> Verdict\n\n    classDef io fill:#cce5ff,stroke:#003366,stroke-width:2px,color:#000\n    classDef proc fill:#d4edda,stroke:#155724,stroke-width:2px,color:#000\n    classDef sink fill:#f8d7da,stroke:#721c24,stroke-width:2px,color:#000\n\n    class TS,Init io\n    class Reach,Branch,TraceBuild,FragCut,LTL proc\n    class Verdict sink\n```\n\n**Reading guide for the architecture diagram:**\n\n1. **$TS$** feeds both the set of initial states and the reachability engine.\n2. The **Reachability Engine** computes the reachable state subgraph (BFS or DFS over $R$).\n3. The **Branching Resolver** enumerates nondeterministic choices $\\mathsf{Post}(s)$ at each step.\n4. The **Trace Builder** stitches together state choices into a maximal trace $\\pi$.\n5. The **Fragment Extractor** slices $\\pi$ into $\\pi[i..j]$ for bounded analysis.\n6. The **LTL Evaluator** checks the fragment (or full trace) against the negated property.\n7. The final **Verdict** is `SAT` (a counter-example trace fragment was found) or `UNSAT` (no violating trace exists up to bound).\n\n### 4.3 Trace Fragment Visualisation\n\nThe diagram below renders the two maximal traces $\\pi_0$ and $\\pi_1$ of Section 3.1, with the length-3 fragment highlighted on each.\n\n```mermaid\ngraph LR\n    p0a[\"s0\"]\n    p0b[\"s1\"]\n    p0c[\"s2\"]\n    p0d[\"s0\"]\n    p0e[\"s1\"]\n    p0f[\"s2\"]\n    p0g[\"s0\"]\n\n    p0a --> p0b --> p0c --> p0d --> p0e --> p0f --> p0g\n    p0a -.fragment pi0 0 to 2.-> p0c\n\n    classDef frag fill:#ffe066,stroke:#333,stroke-width:3px,color:#000\n    class p0a,p0b,p0c frag\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short-Answer Questions (3 Marks Each)\n\n**Q1. [KTU University Exam – Dec 2023, Model]**\n*Define a trace of a transition system. Differentiate between a finite maximal trace and an infinite maximal trace with a suitable example.*\n\n**Model Answer (3 marks):**\n\nA **trace** of a transition system $TS = (S, S_0, R, L)$ is a sequence of states $\\pi = s_0, s_1, s_2, \\ldots$ such that $s_0 \\in S_0$ and $\\forall i \\ge 0 : (s_i, s_{i+1}) \\in R$.\n\nA trace is **maximal** if it cannot be extended further. It is **infinite** if the transition relation $R$ admits a successor from every state in the trace; the trace is denoted $s_0, s_1, s_2, \\ldots$ without termination. It is **finite maximal** if it ends at a state $s_n$ with $\\mathsf{Post}(s_n) = \\emptyset$, i.e., the state has no outgoing transition.\n\n*Example (infinite):* $\\pi = s_0, s_1, s_1, s_1, \\ldots$ where $(s_1, s_1) \\in R$ is a self-loop.\n*Example (finite maximal):* $\\pi = s_0, s_1, s_2$ where $R$ has no outgoing transition from $s_2$.\n\n---\n\n**Q2. [KTU University Exam – June 2024, Model]**\n*What is a trace fragment? How is it denoted formally, and what is its length?*\n\n**Model Answer (3 marks):**\n\nA **trace fragment** is a finite contiguous subsequence of a trace. For a trace $\\pi = s_0, s_1, s_2, \\ldots$, the trace fragment from position $i$ to position $j$ is denoted:\n$$\\pi[i..j] = s_i,\\, s_{i+1},\\, \\ldots,\\, s_j \\quad \\text{where } 0 \\le i \\le j$$\n\nThe **length** of the fragment is the number of states it contains:\n$$\\vert \\pi[i..j] \\vert \\;=\\; j - i + 1$$\n\nTrace fragments are the principal objects analysed in **Bounded Model Checking (BMC)**, where the system is unrolled up to a bound $k$ and every trace fragment of length $k$ is translated into a propositional satisfiability problem.\n\n---\n\n### 5.2 Part B — Full-Descriptive Questions (14 Marks Each)\n\n> **Internal Choice Rule (KTU 2024):** *Answer ANY ONE full question from the choice given. Each sub-part carries 7 marks.*\n\n---\n\n#### Question A (14 Marks)\n\n**Q3(a). [KTU University Exam – Dec 2022, Adapted] (7 marks)**\n*For the transition system $TS$ with $S = \\{p, q, r, s, t\\}$, $S_0 = \\{p\\}$, $R = \\{(p,q), (p,r), (q,s), (r,t), (s,t), (t,q)\\}$ and $L(p) = \\{a\\}$, $L(q) = \\{b\\}$, $L(r) = \\{c\\}$, $L(s) = \\{b\\}$, $L(t) = \\{b, c\\}$:*\n*(i) Draw the state-transition diagram of $TS$.*\n*(ii) Enumerate all maximal traces of $TS$.*\n*(iii) List all trace fragments of length 3 starting from an initial state.*\n\n**Model Solution (7 marks):**\n\n*(i) Diagram:* The graph has five nodes $p, q, r, s, t$ with edges as listed in $R$. Node $p$ is the unique initial state and has out-degree $2$ (to $q$ and $r$).\n\n*(ii) Maximal traces:* From $p$, we may branch to $q$ or to $r$.\n- Branch to $q$: $p \\to q \\to s \\to t \\to q$ (cycle). The unique maximal trace is $\\pi_0 = p, q, s, t, q, s, t, q, s, t, \\ldots$\n- Branch to $r$: $p \\to r \\to t \\to q \\to s \\to t \\to q$ (eventually joins the same cycle). The unique maximal trace is $\\pi_1 = p, r, t, q, s, t, q, s, t, \\ldots$\n\nBoth $\\pi_0$ and $\\pi_1$ are infinite, hence both are maximal.\n\n*(iii) Trace fragments of length 3 starting from $p$ (i.e., $\\pi[0..2]$):*\n- $\\pi_0[0..2] = p, q, s$\n- $\\pi_1[0..2] = p, r, t$\n\nThere are exactly **two** distinct trace fragments of length 3 rooted at the initial state.\n\n---\n\n**Q3(b). [KTU University Exam – June 2023, Adapted] (7 marks)**\n*Explain the relationship between the set of traces $\\mathsf{Traces}(TS)$ of a transition system and the satisfaction of a Linear Temporal Logic (LTL) property $\\phi$. Use the Büchi-automaton perspective to justify your answer.*\n\n**Model Solution (7 marks):**\n\nThe verification of an LTL property $\\phi$ over $TS$ is conceptually a two-step process:\n\n**Step 1 — System automaton construction.** The transition system $TS$ is treated as a (generalised) Büchi automaton $\\mathcal{A}_{TS}$ whose accepting runs are precisely the maximal traces of $TS$.\n\n**Step 2 — Property automaton construction.** The LTL formula $\\phi$ is converted (via standard algorithms such as those of Vardi-Wolper or Gerth et al.) into a Büchi automaton $\\mathcal{A}_{\\neg\\phi}$ that accepts *exactly those traces that violate* $\\phi$.\n\n**Step 3 — Product construction.** The product automaton $\\mathcal{A}_{TS} \\times \\mathcal{A}_{\\neg\\phi}$ is built, and a non-emptiness check (e.g., via nested-DFS) is performed.\n\n**Step 4 — Verdict.**\n- If the language $L(\\mathcal{A}_{TS} \\times \\mathcal{A}_{\\neg\\phi}) = \\emptyset$, then *no* maximal trace of $TS$ violates $\\phi$, so $TS \\models \\phi$.\n- If a trace fragment is found, it is a *counter-example trace* and $TS \\not\\models \\phi$.\n\nFormally:\n$$TS \\models \\phi \\quad \\iff \\quad \\forall\\, \\pi \\in \\mathsf{Traces}(TS) : \\pi \\models \\phi$$\n$$TS \\not\\models \\phi \\quad \\iff \\quad \\exists\\, \\pi \\in \\mathsf{Traces}(TS) : \\pi \\not\\models \\phi$$\n\nThis universal quantification over traces is *the* defining feature of LTL semantics and explains why every bounded trace fragment of length $k$ must be exhaustively (or symbolically) checked in Bounded Model Checking.\n\n---\n\n#### Question B (14 Marks)\n\n**Q4(a). [KTU University Exam – Dec 2023, Adapted] (7 marks)**\n*Define a trace fragment formally. For a transition system with $S = \\{s_0, s_1, s_2, s_3\\}$, $S_0 = \\{s_0\\}$ and $R = \\{(s_0,s_1), (s_1,s_0), (s_1,s_2), (s_2,s_3), (s_3,s_1)\\}$:*\n*(i) Identify all maximal traces.*\n*(ii) List the trace fragments of length exactly 4 that begin at the initial state.*\n*(iii) If the property is \"the system visits $s_3$ within 4 steps\", determine which fragments satisfy it.*\n\n**Model Solution (7 marks):**\n\n*(i) Maximal traces:* From $s_0$, the only successor is $s_1$ (forced). From $s_1$, we may branch to $s_0$ or $s_2$.\n- Branch 1 (back to $s_0$): $\\pi_0 = s_0, s_1, s_0, s_1, s_0, s_1, \\ldots$ (infinite oscillation).\n- Branch 2 (forward to $s_2$): $s_0 \\to s_1 \\to s_2 \\to s_3 \\to s_1 \\to s_2 \\to s_3 \\to \\ldots$ (infinite cycle on $\\{s_1, s_2, s_3\\}$).\n\nBoth traces are infinite, hence both maximal.\n\n*(ii) Trace fragments of length 4 (i.e., $\\pi[0..3]$):*\n- $\\pi_0[0..3] = s_0, s_1, s_0, s_1$\n- $\\pi_1[0..3] = s_0, s_1, s_2, s_3$\n\n*(iii) Satisfaction of \"visit $s_3$ within 4 steps\":*\n- Fragment $s_0, s_1, s_0, s_1$ does **not** contain $s_3$ ⇒ does **not** satisfy the property.\n- Fragment $s_0, s_1, s_2, s_3$ contains $s_3$ at position 3 (which is within 4 steps) ⇒ **satisfies** the property.\n\nHence the system **does not universally satisfy** the property (since at least one maximal trace fails it). The set of *counter-example* trace fragments of length 4 includes $s_0, s_1, s_0, s_1$.\n\n---\n\n**Q4(b). [KTU University Exam – June 2024, Adapted] (7 marks)**\n*Discuss, with derivation, the upper bound on the number of distinct trace fragments of length $k$ emanating from a fixed initial state in a transition system with maximum branching factor $b$. Explain the practical implication of this bound for Bounded Model Checking.*\n\n**Model Solution (7 marks):**\n\n**Derivation.** As shown in Section 3.2, the number of distinct trace fragments of length $k$ rooted at a fixed initial state $s_0 \\in S_0$ is bounded above by $b^{k-1}$, where $b = \\max_{s \\in S} \\vert \\mathsf{Post}(s) \\vert$.\n\nThis follows by induction on the fragment length:\n- $k = 1$: exactly $1$ fragment (the singleton $s_0$).\n- $k \\to k+1$: each fragment of length $k$ can be extended in at most $b$ ways at its final state, yielding at most $b \\cdot b^{k-1} = b^k$ fragments of length $k+1$.\n\n**Practical implication for Bounded Model Checking.** BMC unrolls the transition relation $k$ times and translates \"does there exist a trace fragment of length $k$ violating $\\phi$?\" into a SAT/SMT problem. The bound $b^{k-1}$ is *exponential in $k$*, which is the **state-space explosion** problem in disguise. In practice, this bound is rarely tight (most fragments merge or become infeasible early), which is why modern BMC tools use:\n- **SAT/SMT encoding** to symbolically represent all $b^{k-1}$ fragments in $O(k \\cdot \\vert S \\vert)$ propositional variables.\n- **k-induction** to push the bound beyond mere refutation.\n- **IC3 / Property-Directed Reachability** to avoid the explicit enumeration altogether.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfalls — Where Students Commonly Lose Marks**\n> 1. **Forgetting the initial-state condition** — A common error is to list sequences like $s_1, s_2, s_3$ as \"traces\" without verifying $s_1 \\in S_0$. Always anchor the trace at $S_0$.\n> 2. **Confusing traces with paths in CTL** — In CTL, the term \"path\" is used; in LTL, the term \"trace\" is more common. Both refer to the *same mathematical object*, but mixing up the operator semantics (A vs E) costs marks.\n> 3. **Treating a finite non-maximal trace as maximal** — A trace $s_0, s_1$ is *not* maximal if $(s_1, s_2) \\in R$ for some $s_2$. Always check whether the final state has outgoing transitions.\n> 4. **Off-by-one in fragment length** — A fragment $\\pi[i..j]$ has $j - i + 1$ states, *not* $j - i$. Examiners explicitly test this.\n> 5. **Ignoring the labelling function $L$** — When an LTL property is involved, $L(s)$ determines which atomic propositions are \"true\" at $s$. Forgetting to project through $L$ leads to incorrect satisfaction verdicts.\n\n---\n\n### 5.3 Topic Recap & Important Things to Remember\n\n- **Trace** = sequence of states $s_0, s_1, s_2, \\ldots$ with $s_0 \\in S_0$ and $(s_i, s_{i+1}) \\in R$ for all $i \\ge 0$.\n- **Finite trace** = trace that ends in a state with no outgoing transition; **infinite trace** = trace with no termination.\n- **Maximal trace** = an infinite trace *or* a finite trace that cannot be extended.\n- **Trace fragment** = contiguous finite subsequence $\\pi[i..j]$, of length $j - i + 1$.\n- **Set of all maximal traces** of $TS$ is denoted $\\mathsf{Traces}(TS)$ and is the *primary object* of LTL/CTL* satisfaction.\n- **Branching bound**: at most $b^{k-1}$ distinct fragments of length $k$ from a fixed initial state, where $b = \\max_s \\vert \\mathsf{Post}(s) \\vert$.\n- **LTL semantics** = universal quantification: $TS \\models \\phi$ iff $\\forall \\pi \\in \\mathsf{Traces}(TS) : \\pi \\models \\phi$.\n- **Bounded Model Checking** = exhaustive (or symbolic) analysis of trace fragments of length $\\le k$ — direct practical application.\n- **Stutter equivalence** of traces is the foundation of *partial-order* and *symmetry* reduction in industrial model checkers.\n- Always **anchor at $S_0$**, always **check maximality**, always **project through $L$** before evaluating any temporal formula.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t18",
          "title": "LT Properties - LT Property",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** LT Properties - LT Property\n\n<!-- SECTION_1_START -->\n# LT Properties – LT Property: Section 1 — Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n> [!IMPORTANT]\n> **LT Property = Linear-time Temporal Logic (LTL) Property**\n> An **LTL property** is a formal specification expressed in *Linear-time Temporal Logic* — a modal temporal logic introduced by **Amir Pnueli (1977)** — used to describe the *temporal ordering* of events along a single, linear execution path of a reactive (hardware/software) system.\n\nFormally, an LTL property is a **well-formed formula (WFF)** built from:\n- A finite set of **Atomic Propositions (AP)** — denoted $AP = \\{p, q, r, \\dots\\}$\n- **Propositional connectives**: $\\neg, \\land, \\lor, \\rightarrow, \\leftrightarrow$\n- **Temporal operators**: $\\mathbf{X}, \\mathbf{G}, \\mathbf{F}, \\mathbf{U}, \\mathbf{R}, \\mathbf{W}$\n\nThe syntax is defined by the BNF grammar:\n\n$$\n\\varphi ::= \\text{true} \\;\\vert\\; \\text{false} \\;\\vert\\; p \\;\\vert\\; \\neg \\varphi \\;\\vert\\; \\varphi \\land \\varphi \\;\\vert\\; \\mathbf{X}\\,\\varphi \\;\\vert\\; \\varphi\\,\\mathbf{U}\\,\\varphi\n$$\n\n> [!NOTE]\n> Every other LTL operator is **syntactic sugar** derived from the core operators above.\n\n## 1.2 Conceptual Analogy — The \"Train Conductor\" Intuition\n\nImagine you are a **train conductor** with an infinite ticket-stamping rulebook:\n\n| Logic Element | Train Analogy |\n|---------------|---------------|\n| Atomic Proposition $p$ | \"A passenger has boarded\" |\n| **Next** $\\mathbf{X}\\,p$ | \"On the *very next* station, a passenger boards\" |\n| **Globally** $\\mathbf{G}\\,p$ | \"On *every single future* station, a passenger boards\" |\n| **Finally** $\\mathbf{F}\\,p$ | \"On *some future* station, eventually, a passenger boards\" |\n| **Until** $p\\,\\mathbf{U}\\,q$ | \"Passengers keep boarding ($p$) *until* the train empties ($q$)\" |\n| **Release** $p\\,\\mathbf{R}\\,q$ | \"The train *stays* full ($q$) *until released by* the conductor's signal ($p$)\" |\n\nThe property is a **promise** the system must keep along an *infinite timeline* — the conductor never gets to \"stop reading\" the rulebook.\n\n## 1.3 Key Standard Metrics (KTU Board Weightage)\n\n- **2 marks**: Direct definition / operator meaning.\n- **3 marks**: Write an LTL formula for a given English statement.\n- **7–14 marks**: Classify, convert to NNF, prove equivalences, and evaluate over a given path.\n\n## 1.4 LTL vs. CTL — Critical Distinction\n\n> [!IMPORTANT]\n> LTL operates over a **single linear path** of execution, while CTL (Computation Tree Logic) quantifies over **branching trees** of all possible futures. KTU Module 1 focuses exclusively on LTL.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# LT Properties – LT Property: Section 2 — Deep Theoretical Analysis & Formula Sheet\n\n## 2.1 Formal Syntax (BNF)\n\n$$\n\\begin{aligned}\n\\varphi \\;::=\\;& \\text{true} \\;\\vert\\; \\text{false} \\;\\vert\\; p \\;\\vert\\; \\neg \\varphi \\\\\n&\\vert\\; \\varphi \\land \\varphi \\;\\vert\\; \\varphi \\lor \\varphi \\;\\vert\\; \\varphi \\rightarrow \\varphi \\\\\n&\\vert\\; \\mathbf{X}\\,\\varphi \\;\\vert\\; \\mathbf{G}\\,\\varphi \\;\\vert\\; \\mathbf{F}\\,\\varphi \\\\\n&\\vert\\; \\varphi\\,\\mathbf{U}\\,\\varphi \\;\\vert\\; \\varphi\\,\\mathbf{R}\\,\\varphi \\;\\vert\\; \\varphi\\,\\mathbf{W}\\,\\varphi\n\\end{aligned}\n$$\n\n## 2.2 Formal Semantics — The Satisfies Relation ($\\models$)\n\nAn LTL formula is interpreted over an **infinite path** $\\pi = s_0, s_1, s_2, \\dots$ of states drawn from a Kripke structure $M$.\n\nThe **satisfies relation** $\\pi \\models \\varphi$ (read as \"path $\\pi$ satisfies formula $\\varphi$\") is defined inductively:\n\n| Operator | Formal Semantics | Plain English |\n|----------|------------------|---------------|\n| $\\mathbf{X}\\,\\varphi$ | $\\pi \\models \\mathbf{X}\\,\\varphi \\iff \\pi[1..] \\models \\varphi$ | $\\varphi$ is true at the *next* state |\n| $\\mathbf{G}\\,\\varphi$ | $\\forall i \\ge 0 : \\pi[i..] \\models \\varphi$ | $\\varphi$ is true at *all* future states |\n| $\\mathbf{F}\\,\\varphi$ | $\\exists i \\ge 0 : \\pi[i..] \\models \\varphi$ | $\\varphi$ is true at *some* future state |\n| $\\varphi\\,\\mathbf{U}\\,\\psi$ | $\\exists j \\ge 0 : \\pi[j..] \\models \\psi \\;\\land\\; \\forall 0 \\le i < j : \\pi[i..] \\models \\varphi$ | $\\varphi$ holds *until* $\\psi$ first becomes true |\n| $\\varphi\\,\\mathbf{R}\\,\\psi$ | $\\forall j \\ge 0 : (\\forall 0 \\le i < j : \\pi[i..] \\not\\models \\varphi) \\rightarrow \\pi[j..] \\models \\psi$ | $\\psi$ holds *until* released by $\\varphi$ |\n| $\\varphi\\,\\mathbf{W}\\,\\psi$ | $\\varphi\\,\\mathbf{U}\\,\\psi \\;\\lor\\; \\mathbf{G}\\,\\varphi$ | Like $\\mathbf{U}$, but $\\psi$ may *never* occur |\n\nHere, $\\pi[i..]$ denotes the *suffix* path starting at state $s_i$.\n\n## 2.3 The Core LTL Operator Duality Theorem\n\n> [!IMPORTANT]\n> The most frequently tested theorem in KTU Module 1 — **temporal De Morgan dualities**:\n> - $\\neg \\mathbf{X}\\,\\varphi \\equiv \\mathbf{X}\\,\\neg \\varphi$\n> - $\\neg \\mathbf{G}\\,\\varphi \\equiv \\mathbf{F}\\,\\neg \\varphi$\n> - $\\neg \\mathbf{F}\\,\\varphi \\equiv \\mathbf{G}\\,\\neg \\varphi$\n> - $\\neg (\\varphi\\,\\mathbf{U}\\,\\psi) \\equiv \\neg \\varphi\\,\\mathbf{R}\\,\\neg \\psi$\n\nThese four equivalences are the foundation for **Negation Normal Form (NNF)** conversion in model checking.\n\n## 2.4 Classification of LTL Properties (KTU High-Yield)\n\n| Class | LTL Form | Meaning | Example |\n|-------|----------|---------|---------|\n| **Safety** | $\\mathbf{G}\\,\\text{``good condition''}$ | \"Bad thing *never* happens\" | $\\mathbf{G}\\,\\neg(\\text{req} \\land \\text{gnt})$ — mutual exclusion |\n| **Liveness** | $\\mathbf{F}\\,\\text{``good event''}$ | \"Good thing *eventually* happens\" | $\\mathbf{F}\\,\\text{ack}$ — every request is acknowledged |\n| **Strong Fairness** | $\\mathbf{G}\\,\\mathbf{F}\\,p$ | \"$p$ is *infinitely often* true\" | $\\mathbf{G}\\,\\mathbf{F}\\,\\text{req\\_enabled}$ |\n| **Weak Fairness** | $\\mathbf{F}\\,\\mathbf{G}\\,p$ | \"From some point, $p$ *always* true\" | $\\mathbf{F}\\,\\mathbf{G}\\,\\neg \\text{reset}$ |\n| **Response** | $\\mathbf{G}(p \\rightarrow \\mathbf{F}\\,q)$ | \"Every $p$ is *followed by* $q$\" | $\\mathbf{G}(\\text{req} \\rightarrow \\mathbf{F}\\,\\text{ack})$ |\n| **Precedence** | $\\mathbf{G}(q \\rightarrow (\\neg p\\,\\mathbf{U}\\,r))$ | \"$p$ cannot occur *before* $r$\" | — |\n\n## 2.5 Real-World Engineering Utility\n\n- **Hardware verification** (Intel, AMD, ARM): SVA (SystemVerilog Assertions) is *syntactically* LTL embedded in HDLs.\n- **Protocol verification**: TCP, cache coherence, bus arbitration.\n- **Aerospace & automotive (DO-254, ISO 26262)**: Mandates formal proof of liveness/safety properties.\n- **Compiler optimizations**: Dead-code elimination verified via $\\mathbf{G}$ over control-flow paths.\n\n## 2.6 KTU Formula Sheet / Cheat Sheet\n\n| # | Equivalence | Use Case |\n|---|-------------|----------|\n| 1 | $\\mathbf{F}\\,\\varphi \\equiv \\text{true}\\,\\mathbf{U}\\,\\varphi$ | Define $\\mathbf{F}$ via $\\mathbf{U}$ |\n| 2 | $\\mathbf{G}\\,\\varphi \\equiv \\neg \\mathbf{F}\\,\\neg \\varphi$ | Negate \"always\" to \"eventually not\" |\n| 3 | $\\mathbf{F}\\,\\mathbf{G}\\,\\varphi \\equiv \\neg \\mathbf{G}\\,\\mathbf{F}\\,\\neg \\varphi$ | Dual of strong fairness |\n| 4 | $\\varphi\\,\\mathbf{R}\\,\\psi \\equiv \\neg(\\neg \\varphi\\,\\mathbf{U}\\,\\neg \\psi)$ | $\\mathbf{R}$ = negated $\\mathbf{U}$ of negated operands |\n| 5 | $\\mathbf{G}\\,\\varphi \\rightarrow \\mathbf{F}\\,\\varphi$ | \"Always\" implies \"eventually\" |\n| 6 | $\\mathbf{G}(\\varphi \\rightarrow \\mathbf{F}\\,\\psi)$ | Standard *response* property |\n| 7 | $\\mathbf{G}(\\neg \\varphi) \\equiv \\neg \\mathbf{F}\\,\\varphi$ | Safety = globally negated liveness |\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# LT Properties – LT Property: Section 3 — Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: The Four Temporal De Morgan Duals\n\nWe prove each equivalence formally from the inductive semantics.\n\n### 3.1.1 Proof of $\\neg \\mathbf{X}\\,\\varphi \\equiv \\mathbf{X}\\,\\neg \\varphi$\n\n$$\n\\begin{aligned}\n\\pi \\models \\neg \\mathbf{X}\\,\\varphi \n&\\iff \\pi \\not\\models \\mathbf{X}\\,\\varphi \\\\\n&\\iff \\neg(\\pi[1..] \\models \\varphi) \\quad \\text{[by X-semantics]}\\\\\n&\\iff \\pi[1..] \\models \\neg \\varphi \\\\\n&\\iff \\pi \\models \\mathbf{X}\\,\\neg \\varphi \\quad \\blacksquare\n\\end{aligned}\n$$\n\n### 3.1.2 Proof of $\\neg \\mathbf{G}\\,\\varphi \\equiv \\mathbf{F}\\,\\neg \\varphi$\n\n$$\n\\begin{aligned}\n\\pi \\models \\neg \\mathbf{G}\\,\\varphi \n&\\iff \\neg(\\forall i \\ge 0 : \\pi[i..] \\models \\varphi) \\\\\n&\\iff \\exists i \\ge 0 : \\pi[i..] \\not\\models \\varphi \\\\\n&\\iff \\exists i \\ge 0 : \\pi[i..] \\models \\neg \\varphi \\\\\n&\\iff \\pi \\models \\mathbf{F}\\,\\neg \\varphi \\quad \\blacksquare\n\\end{aligned}\n$$\n\n### 3.1.3 Proof of $\\neg \\mathbf{F}\\,\\varphi \\equiv \\mathbf{G}\\,\\neg \\varphi$\n\n$$\n\\begin{aligned}\n\\pi \\models \\neg \\mathbf{F}\\,\\varphi \n&\\iff \\neg(\\exists i \\ge 0 : \\pi[i..] \\models \\varphi) \\\\\n&\\iff \\forall i \\ge 0 : \\pi[i..] \\not\\models \\varphi \\\\\n&\\iff \\forall i \\ge 0 : \\pi[i..] \\models \\neg \\varphi \\\\\n&\\iff \\pi \\models \\mathbf{G}\\,\\neg \\varphi \\quad \\blacksquare\n\\end{aligned}\n$$\n\n### 3.1.4 Proof of $\\neg (\\varphi\\,\\mathbf{U}\\,\\psi) \\equiv \\neg \\varphi\\,\\mathbf{R}\\,\\neg \\psi$\n\n$$\n\\begin{aligned}\n\\pi \\models \\neg(\\varphi\\,\\mathbf{U}\\,\\psi) \n&\\iff \\neg \\big[\\exists j \\ge 0 : \\pi[j..] \\models \\psi \\;\\land\\; \\forall 0 \\le i < j : \\pi[i..] \\models \\varphi \\big] \\\\\n&\\iff \\forall j \\ge 0 : \\neg \\big[\\pi[j..] \\models \\psi \\;\\land\\; \\forall 0 \\le i < j : \\pi[i..] \\models \\varphi \\big] \\\\\n&\\iff \\forall j \\ge 0 : \\pi[j..] \\not\\models \\psi \\;\\lor\\; \\exists 0 \\le i < j : \\pi[i..] \\not\\models \\varphi \\\\\n&\\iff \\forall j \\ge 0 : (\\forall 0 \\le i < j : \\pi[i..] \\not\\models \\varphi) \\rightarrow \\pi[j..] \\models \\psi \\quad \\text{[contrapositive rearrangement]}\\\\\n&\\iff \\pi \\models \\neg \\varphi\\,\\mathbf{R}\\,\\neg \\psi \\quad \\blacksquare\n\\end{aligned}\n$$\n\n## 3.2 Analytical Matrix: Property Classification of Common Specifications\n\n| # | English Specification | LTL Property | Class |\n|---|----------------------|--------------|-------|\n| 1 | \"The system never enters an error state.\" | $\\mathbf{G}\\,\\neg \\text{error}$ | Safety |\n| 2 | \"Every request is eventually acknowledged.\" | $\\mathbf{G}(\\text{req} \\rightarrow \\mathbf{F}\\,\\text{ack})$ | Response / Liveness |\n| 3 | \"Process P gets the resource infinitely often.\" | $\\mathbf{G}\\,\\mathbf{F}\\,\\text{P\\_holds}$ | Strong Fairness |\n| 4 | \"Once enabled, the interrupt fires immediately.\" | $\\mathbf{G}(\\text{en} \\rightarrow \\mathbf{X}\\,\\text{fire})$ | Next-step Response |\n| 5 | \"Variable $x$ stays non-zero until reset is asserted.\" | $\\mathbf{G}(x \\neq 0 \\;\\mathbf{U}\\; \\text{reset})$ | Until / Safety |\n| 6 | \"After the init signal, the bus is always ready.\" | $\\mathbf{G}(\\text{init} \\rightarrow \\mathbf{G}\\,\\text{ready})$ | Nested Safety |\n\n## 3.3 Worked Example — Evaluating an LTL Formula on a Given Path\n\n**Given path** $\\pi$ over AP $\\{p, q\\}$:\n\n$$\n\\pi : \\{p, q\\}, \\;\\{\\neg p, q\\}, \\;\\{\\neg p, \\neg q\\}, \\;\\{\\neg p, \\neg q\\}, \\dots\n$$\n\n**Check**: Does $\\pi \\models \\mathbf{G}(p \\rightarrow \\mathbf{F}\\,q)$ hold?\n\n| Step | State $i$ | $p$? | $q$? | $p \\rightarrow \\mathbf{F}\\,q$ at $i$? | $\\mathbf{G}(p \\rightarrow \\mathbf{F}\\,q)$ so far? |\n|------|-----------|------|------|----------------------------------------|--------------------------------------------------|\n| 0 | $\\{p,q\\}$ | T | T | T (q holds at $i=0$, so $\\mathbf{F}\\,q$ is T) | T |\n| 1 | $\\{\\neg p, q\\}$ | F | T | T (vacuously, antecedent false) | T |\n| 2 | $\\{\\neg p, \\neg q\\}$ | F | F | T (vacuously, antecedent false) | T |\n| 3 | $\\{\\neg p, \\neg q\\}$ | F | F | T (vacuously) | T |\n\n**Conclusion**: $\\pi \\models \\mathbf{G}(p \\rightarrow \\mathbf{F}\\,q)$ — **PROPERTY HOLDS**.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# LT Properties – LT Property: Section 4 — Structural Diagrams & Schematics\n\n## 4.1 Mermaid Diagram — Architecture of an LTL Property Verification Flow\n\n```mermaid\nflowchart TD\n    A[System Model M: Kripke Structure] --> B[Compute All Infinite Paths]\n    B --> C[Path 1: s0 s1 s2 ...]\n    B --> D[Path 2: s0 s1 s2 ...]\n    B --> E[Path N: s0 s1 s2 ...]\n    C --> F[Check Path1 satisfies phi]\n    D --> G[Check Path2 satisfies phi]\n    E --> H[Check PathN satisfies phi]\n    F --> I{All Paths satisfy phi?}\n    G --> I\n    H --> I\n    I --> J[YES: Property Holds]\n    I --> K[NO: Generate Counterexample Path]\n    K --> L[Debug: Trace back to design flaw]\n```\n\n## 4.2 Mermaid Diagram — Syntactic Structure of an LTL Formula\n\n```mermaid\ngraph TD\n    Root[LTL Formula phi] --> Atomic1[Atomic Proposition p]\n    Root --> Op1[Operator G Globally]\n    Op1 --> Inner1[Inner Formula psi]\n    Inner1 --> Atomic2[Atomic Proposition q]\n    Inner1 --> Op2[Operator U Until]\n    Op2 --> Left[Left: p]\n    Op2 --> Right[Right: r]\n    style Root fill:#1f77b4,stroke:#fff,color:#fff\n    style Op1 fill:#ff7f0e,color:#fff\n    style Op2 fill:#2ca02c,color:#fff\n    style Atomic1 fill:#d62728,color:#fff\n    style Atomic2 fill:#d62728,color:#fff\n    style Left fill:#9467bd,color:#fff\n    style Right fill:#9467bd,color:#fff\n```\n\n## 4.3 Mermaid Diagram — Operator Duality Map (Negation Propagation)\n\n```mermaid\nflowchart LR\n    GX[neg X phi] -- dual --> XN[X neg phi]\n    GG[neg G phi] -- dual --> FN[F neg phi]\n    GF[neg F phi] -- dual --> GN[G neg phi]\n    UU[neg phi U psi] -- dual --> RR[neg phi R neg psi]\n    style GX fill:#17becf\n    style XN fill:#bcbd22\n    style GG fill:#17becf\n    style FN fill:#bcbd22\n    style GF fill:#17becf\n    style GN fill:#bcbd22\n    style UU fill:#e377c2\n    style RR fill:#8c564b\n```\n\n## 4.4 Block-Level Functional Architecture — LTL Model Checking Pipeline\n\n```mermaid\nflowchart LR\n    subgraph InputStage[Input Stage]\n        DES[Design RTL or FSM]\n        SPEC[LTL Property phi]\n    end\n    subgraph BuildStage[Build Stage]\n        KSTR[Kripke Structure M]\n        NNF[Negation Normal Form of phi]\n    end\n    subgraph SolveStage[Solve Stage]\n        AUT[Automata Intersection: A_M x A_neg phi]\n        EMPTY[Emptiness Check: Buchi Algorithm]\n    end\n    subgraph OutputStage[Output Stage]\n        SAT[SATISFIED: Property Holds]\n        CE[COUNTEREXAMPLE: Property Fails]\n    end\n    DES --> KSTR\n    SPEC --> NNF\n    KSTR --> AUT\n    NNF --> AUT\n    AUT --> EMPTY\n    EMPTY --> SAT\n    EMPTY --> CE\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# LT Properties – LT Property: Section 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n## 📘 PART A — 3-Mark Short-Answer Questions\n\n### **Q1. [KTU University Exam — Dec 2023]**\n**Define LTL property. List any four LTL temporal operators with their meaning.**\n\n**Model Answer (3 marks):**\n\nA **Linear-time Temporal Logic (LTL) property** is a formal specification expressed in LTL that describes the temporal behavior of a reactive system along an *infinite* computation path, built from atomic propositions, propositional connectives, and temporal operators.\n\nFour LTL operators:\n1. **$\\mathbf{X}\\,\\varphi$ (Next)** — $\\varphi$ is true in the *immediately following* state.\n2. **$\\mathbf{G}\\,\\varphi$ (Globally)** — $\\varphi$ is true in *all* future states.\n3. **$\\mathbf{F}\\,\\varphi$ (Finally)** — $\\varphi$ becomes true at *some* future state.\n4. **$\\varphi\\,\\mathbf{U}\\,\\psi$ (Until)** — $\\varphi$ holds *until* $\\psi$ first becomes true.\n\n---\n\n### **Q2. [KTU University Exam — June 2024]**\n**State and prove the LTL duality $\\neg \\mathbf{G}\\,\\varphi \\equiv \\mathbf{F}\\,\\neg \\varphi$.**\n\n**Model Answer (3 marks):**\n\n**Statement**: $\\neg \\mathbf{G}\\,\\varphi \\equiv \\mathbf{F}\\,\\neg \\varphi$ for any LTL formula $\\varphi$ and any infinite path $\\pi$.\n\n**Proof**:\n\n$$\n\\begin{aligned}\n\\pi \\models \\neg \\mathbf{G}\\,\\varphi \n&\\iff \\neg(\\forall i \\ge 0 : \\pi[i..] \\models \\varphi) \\\\\n&\\iff \\exists i \\ge 0 : \\pi[i..] \\not\\models \\varphi \\\\\n&\\iff \\exists i \\ge 0 : \\pi[i..] \\models \\neg \\varphi \\\\\n&\\iff \\pi \\models \\mathbf{F}\\,\\neg \\varphi \\quad \\blacksquare\n\\end{aligned}\n$$\n\n> [!WARNING]\n> **Examiner's Pitfall**: Many students forget to apply the $\\neg$ to the *atomic* proposition ($\\neg \\varphi$) and incorrectly write $\\neg \\mathbf{G}\\,\\varphi \\equiv \\mathbf{F}\\,\\varphi$. This costs the **negation step (1 mark)**.\n\n---\n\n## 📗 PART B — 14-Mark Questions (Module Internal Choice)\n\n### **Question A (14 Marks) — [KTU University Exam — Dec 2024 Model Paper]**\n\n**(a)** Classify LTL properties into Safety, Liveness, and Fairness. For each class, give the LTL syntactic form and one real-world example. **(7 marks)**\n\n**(b)** Convert the LTL property $\\neg \\mathbf{G}(\\text{req} \\rightarrow \\mathbf{F}\\,\\text{ack})$ into Negation Normal Form (NNF). Show every dual-application step. **(7 marks)**\n\n---\n\n#### ✅ Model Solution to Q-A (a) — Classification of LTL Properties (7 marks)\n\n| Class | Syntactic Form | Real-World Example |\n|-------|---------------|---------------------|\n| **Safety** | $\\mathbf{G}\\,\\psi$ where $\\psi$ is a \"good\" condition | **Traffic light never shows red and green simultaneously**: $\\mathbf{G}\\,\\neg(\\text{red} \\land \\text{green})$ |\n| **Liveness (Reachability)** | $\\mathbf{F}\\,\\psi$ — \"good thing eventually\" | **Elevator door eventually opens after press**: $\\mathbf{F}\\,\\text{open}$ |\n| **Response** | $\\mathbf{G}(p \\rightarrow \\mathbf{F}\\,q)$ | **Every bus request is answered**: $\\mathbf{G}(\\text{req} \\rightarrow \\mathbf{F}\\,\\text{grant})$ |\n| **Strong Fairness** | $\\mathbf{G}\\,\\mathbf{F}\\,p$ | **A persistently-enabled process gets CPU infinitely often**: $\\mathbf{G}\\,\\mathbf{F}\\,\\text{scheduled}$ |\n| **Weak Fairness** | $\\mathbf{F}\\,\\mathbf{G}\\,p$ | **After startup transient, clock is forever active**: $\\mathbf{F}\\,\\mathbf{G}\\,\\text{clk\\_on}$ |\n\n**(1 mark per row × 5 classes = 5 marks, 2 marks for the table synthesis & explanation)**\n\n---\n\n#### ✅ Model Solution to Q-A (b) — NNF Conversion (7 marks)\n\nGiven: $\\neg \\mathbf{G}(\\text{req} \\rightarrow \\mathbf{F}\\,\\text{ack})$\n\n**Step 1**: Push $\\neg$ inside $\\mathbf{G}$ using the duality $\\neg \\mathbf{G}\\,\\varphi \\equiv \\mathbf{F}\\,\\neg \\varphi$:\n\n$$\n\\mathbf{F}\\,\\neg(\\text{req} \\rightarrow \\mathbf{F}\\,\\text{ack})\n$$\n\n**Step 2**: Rewrite the implication $\\text{req} \\rightarrow \\mathbf{F}\\,\\text{ack} \\equiv \\neg \\text{req} \\lor \\mathbf{F}\\,\\text{ack}$:\n\n$$\n\\mathbf{F}\\,\\neg(\\neg \\text{req} \\lor \\mathbf{F}\\,\\text{ack})\n$$\n\n**Step 3**: Apply De Morgan to the inner negation $\\neg(p \\lor q) \\equiv \\neg p \\land \\neg q$:\n\n$$\n\\mathbf{F}\\,(\\text{req} \\land \\neg \\mathbf{F}\\,\\text{ack})\n$$\n\n**Step 4**: Apply the duality $\\neg \\mathbf{F}\\,\\varphi \\equiv \\mathbf{G}\\,\\neg \\varphi$ to $\\neg \\mathbf{F}\\,\\text{ack}$:\n\n$$\n\\mathbf{F}\\,(\\text{req} \\land \\mathbf{G}\\,\\neg \\text{ack})\n$$\n\n**Final NNF**:\n\n$$\n\\boxed{\\;\\mathbf{F}\\,(\\text{req} \\land \\mathbf{G}\\,\\neg \\text{ack})\\;}\n$$\n\n> [!WARNING]\n> **Examiner's Pitfall**: Students often stop at Step 2 and forget to push the $\\neg$ inside the $\\mathbf{F}$ operator in Step 4. This is the **most common loss of 2 marks** in NNF conversions.\n\n---\n\n### **Question B (14 Marks) — [KTU University Exam — June 2024 Model Paper]**\n\n**(a)** Explain the formal semantics of the LTL operators $\\mathbf{X}, \\mathbf{G}, \\mathbf{F}, \\mathbf{U}$ with the satisfies relation $\\models$. **(7 marks)**\n\n**(b)** Consider the infinite path $\\pi$:\n\n$$\n\\pi : \\{p, q\\}, \\;\\{\\neg p, q\\}, \\;\\{\\neg p, \\neg q\\}, \\;\\{\\neg p, \\neg q\\}, \\dots\n$$\n\nEvaluate whether $\\pi \\models \\mathbf{F}\\,(\\mathbf{G}\\,p \\lor q)$ holds. Show every step. **(7 marks)**\n\n---\n\n#### ✅ Model Solution to Q-B (a) — Formal Semantics (7 marks)\n\nLet $\\pi = s_0, s_1, s_2, \\dots$ be an infinite path, and $\\pi[i..]$ the suffix from state $s_i$.\n\n| Operator | Satisfies Relation $\\pi \\models \\varphi$ | Plain Meaning |\n|----------|---------------------------------------|----------------|\n| $\\mathbf{X}\\,\\varphi$ | $\\pi \\models \\mathbf{X}\\,\\varphi \\iff \\pi[1..] \\models \\varphi$ | $\\varphi$ holds at the **next** state |\n| $\\mathbf{G}\\,\\varphi$ | $\\pi \\models \\mathbf{G}\\,\\varphi \\iff \\forall i \\ge 0 : \\pi[i..] \\models \\varphi$ | $\\varphi$ holds at **every** future state |\n| $\\mathbf{F}\\,\\varphi$ | $\\pi \\models \\mathbf{F}\\,\\varphi \\iff \\exists i \\ge 0 : \\pi[i..] \\models \\varphi$ | $\\varphi$ holds at **some** future state |\n| $\\varphi\\,\\mathbf{U}\\,\\psi$ | $\\pi \\models \\varphi\\,\\mathbf{U}\\,\\psi \\iff \\exists j \\ge 0 : \\pi[j..] \\models \\psi \\;\\land\\; \\forall 0 \\le i < j : \\pi[i..] \\models \\varphi$ | $\\varphi$ holds **until** $\\psi$ becomes true |\n\n**(1.5 marks per operator × 4 = 6 marks, 1 mark for the inductive definition preamble)**\n\n---\n\n#### ✅ Model Solution to Q-B (b) — Path Evaluation (7 marks)\n\nWe evaluate $\\pi \\models \\mathbf{F}\\,(\\mathbf{G}\\,p \\lor q)$.\n\nThe path:\n- $s_0 = \\{p, q\\}$\n- $s_1 = \\{\\neg p, q\\}$\n- $s_2 = \\{\\neg p, \\neg q\\}$\n- $s_3, s_4, \\dots = \\{\\neg p, \\neg q\\}$ (stuck in $\\neg p, \\neg q$)\n\n**Step 1 — Check $i = 0$**: $s_0 = \\{p, q\\}$.  \n- $p$ true, so $\\mathbf{G}\\,p$ at $\\pi[0..]$? We need $p$ in *all* future states — false (since $s_1$ has $\\neg p$).  \n- $q$ true, so $\\mathbf{G}\\,p \\lor q$ at $\\pi[0..]$ is $F \\lor T = T$.  \n- $\\mathbf{F}(\\mathbf{G}\\,p \\lor q)$ at $\\pi[0..]$: exists $i=0$ where inner is true ⟹ **TRUE**.\n\n**Step 2 — Verification by direct existential check**:\n\n| $i$ | State | $\\mathbf{G}\\,p$ at $\\pi[i..]$? | $q$ at $\\pi[i..]$? | $\\mathbf{G}\\,p \\lor q$? | $\\mathbf{F}(\\mathbf{G}\\,p \\lor q)$? |\n|-----|-------|-------------------------------|---------------------|-------------------------|-------------------------------------|\n| 0 | $\\{p,q\\}$ | F (fails at $i=1$) | T | **T** | **T** ✓ |\n| 1 | $\\{\\neg p,q\\}$ | F (fails at $i=2$) | T | T | T |\n| 2 | $\\{\\neg p,\\neg q\\}$ | F | F | F | T (already satisfied) |\n\n**Step 3 — Conclusion**:\n\n$$\n\\boxed{\\;\\pi \\models \\mathbf{F}\\,(\\mathbf{G}\\,p \\lor q) \\quad \\text{— PROPERTY SATISFIED.}\\;}\n$$\n\n> [!WARNING]\n> **Examiner's Pitfall**: Many students mis-evaluate $\\mathbf{G}\\,p$ at $s_0$ as TRUE (because $s_0$ has $p$), forgetting that $\\mathbf{G}$ must hold at *all* future states. This single-point error loses **3 marks** out of 7.\n\n---\n\n## 📌 Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- ✅ **LTL** = Linear-time Temporal Logic (Pnueli, 1977) — semantics over a **single infinite path**.\n- ✅ **Five Core Temporal Operators**: $\\mathbf{X}$ (Next), $\\mathbf{G}$ (Globally/Always), $\\mathbf{F}$ (Finally/Eventually), $\\mathbf{U}$ (Until), $\\mathbf{R}$ (Release).\n- ✅ **Derived Operators**: $\\mathbf{F}\\,\\varphi \\equiv \\text{true}\\,\\mathbf{U}\\,\\varphi$, $\\mathbf{G}\\,\\varphi \\equiv \\text{false}\\,\\mathbf{R}\\,\\varphi$.\n- ✅ **Four De Morgan Duals** (MUST memorize):\n  1. $\\neg \\mathbf{X}\\,\\varphi \\equiv \\mathbf{X}\\,\\neg \\varphi$\n  2. $\\neg \\mathbf{G}\\,\\varphi \\equiv \\mathbf{F}\\,\\neg \\varphi$\n  3. $\\neg \\mathbf{F}\\,\\varphi \\equiv \\mathbf{G}\\,\\neg \\varphi$\n  4. $\\neg(\\varphi\\,\\mathbf{U}\\,\\psi) \\equiv \\neg \\varphi\\,\\mathbf{R}\\,\\neg \\psi$\n- ✅ **Three Property Classes**: Safety ($\\mathbf{G}$), Liveness ($\\mathbf{F}$), Fairness ($\\mathbf{G}\\,\\mathbf{F}$ / $\\mathbf{F}\\,\\mathbf{G}$).\n- ✅ **NNF Conversion Rule**: Push $\\neg$ inward using duals; rewrite $\\rightarrow$ as $\\lor$ before negating.\n- ✅ **LTL vs. CTL**: LTL = linear time (one path), CTL = branching time (quantified over tree).\n- ✅ **Path satisfaction is existential for $\\mathbf{F}$ and universal for $\\mathbf{G}$** — examiners love testing this.\n- ✅ **Vacuous truth**: $\\text{false} \\rightarrow \\varphi$ is always TRUE — crucial in $\\mathbf{G}(p \\rightarrow \\mathbf{F}\\,q)$ when $p$ never holds.\n- ✅ **Counterexample for $\\mathbf{G}$**: One violating state suffices. **Witness for $\\mathbf{F}$**: One satisfying state suffices.\n\n> **End of Notes — Module 1, Topic: LT Properties - LT Property**\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t19",
          "title": "Satisfaction Relation for LT Properties",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Satisfaction Relation for LT Properties\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition of the Satisfaction Relation for LT Properties\n\nIn the theory of **Model Checking** (Clarke, Emerson, Sistla; standard KTU PECNT526 reference), the **Satisfaction Relation** (denoted by the turnstile symbol $\\models$) is a binary relation that formally decides *whether* a model (the system under verification) fulfills a *property* expressed in a temporal logic such as **LTL (Linear Temporal Logic)**.\n\nA property $P \\subseteq \\Sigma^\\omega$ over an alphabet $\\Sigma$ of *atomic propositions* is called a **Linear-Time (LT) property** if, and only if, the satisfaction of a path $\\pi$ in a Kripke structure $TS$ by $P$ depends exclusively on the **trace** $\\text{trace}(\\pi)$ produced by that path. Formally:\n\n> [!IMPORTANT]\n> **LT Property Definition (KTU 2024 Syllabus Standard)**\n>\n> A set $P \\subseteq (2^{AP})^\\omega$ is an **LT property** if for every pair of paths $\\pi$ and $\\pi'$ in the transition system $TS$:\n>\n> $$\\text{trace}(\\pi) = \\text{trace}(\\pi') \\;\\Longrightarrow\\; (\\pi \\models P \\iff \\pi' \\models P)$$\n\nThe **Satisfaction Relation** is then defined as the membership test:\n\n$$\\pi \\models P \\;\\;\\iff\\;\\; \\text{trace}(\\pi) \\in P$$\n\n> [!NOTE]\n> **Key Insight:** LT properties are *trace-monotone* in the satisfaction sense. Two paths that \"look identical\" from the perspective of the atomic propositions must either both satisfy the property or both violate it. This is the cornerstone of linear-time model checking.\n\n### 1.2 Conceptual Analogy / Intuition\n\nImagine a security guard (the *satisfaction relation*) reviewing the CCTV footage (the *trace*) of a bank's automated doors (*the model*). The guard has one rulebook: \"The vault door must never open when the alarm is on\" (an *LT safety property*). \n\n- The guard doesn't care *which specific* camera angle recorded the event — only the **sequence of facts** (\"Alarm=ON, Vault=CLOSED, Alarm=ON, …\").\n- Two different camera recordings of the same event will receive the **same verdict**.\n- If the trace (the fact-sequence) belongs to the \"rule-conforming\" set, the path satisfies the property. If even *one* fact in the trace breaks the rule, the property is violated.\n\nThis \"trace-only\" decision process is the essence of the LT satisfaction relation.\n\n### 1.3 Formal Setting: Kripke Structure\n\nA **Kripke Structure** (the canonical model in KTU Module 1) is the tuple:\n\n$$TS = (S, \\; S_0, \\; R, \\; L)$$\n\nwhere:\n\n- $S$ — a **finite** set of states (using standard KTU notation; the alphabet $\\Sigma = 2^{AP}$ is implicit)\n- $S_0 \\subseteq S$ — the set of *initial* states\n- $R \\subseteq S \\times S$ — the *transition* relation (required to be **total**, i.e., every state has at least one successor)\n- $L : S \\rightarrow 2^{AP}$ — the *labeling* function mapping each state to the set of atomic propositions true in that state\n\n> [!IMPORTANT]\n> **$AP$** denotes the finite set of **Atomic Propositions** — the indivisible Boolean facts about each state (e.g., $\\{$`x=1`, `lock=off`$\\}$).\n\nA **path** is an infinite sequence of states $\\pi = s_0 s_1 s_2 \\ldots$ such that $(s_i, s_{i+1}) \\in R$ for all $i \\geq 0$.\n\nThe **trace** of a path is the corresponding sequence of label sets:\n\n$$\\text{trace}(\\pi) = L(s_0)\\,L(s_1)\\,L(s_2)\\,\\ldots \\;\\; \\in (2^{AP})^\\omega$$\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Logical Decomposition of the Satisfaction Relation\n\nThe satisfaction relation $\\models$ is defined inductively on the syntax of LTL formulas, which is the standard vehicle for expressing LT properties in KTU 2024.\n\n**LTL Syntax (Backus–Naur Form):**\n\n$$\\varphi \\;::=\\; \\text{true} \\;\\mid\\; a \\;\\mid\\; \\neg\\varphi \\;\\mid\\; \\varphi_1 \\vee \\varphi_2 \\;\\mid\\; \\operatorname{X}\\varphi \\;\\mid\\; \\varphi_1 \\operatorname{U} \\varphi_2$$\n\nwhere $a \\in AP$ is any atomic proposition, and $\\operatorname{X}$, $\\operatorname{U}$ are the **Next** and **Until** temporal operators. The derived operators are:\n\n$$\\operatorname{F}\\varphi \\;\\equiv\\; \\text{true}\\operatorname{U}\\varphi \\quad\\text{(Eventually / Future)}$$\n\n$$\\operatorname{G}\\varphi \\;\\equiv\\; \\neg\\operatorname{F}\\neg\\varphi \\quad\\texttext{(Globally / Always)}$$\n\n### 2.2 KTU Formula Sheet / Cheat Sheet\n\n> [!NOTE]\n> **Canonical Satisfaction Rules for LTL (Path Semantics)** — directly from the Baier–Katoen *Principles of Model Checking* framework adopted in KTU 2024.\n\n| # | Formula $\\varphi$ | Satisfaction Condition $\\pi \\models \\varphi$ | Intuition |\n|---|----|------|---------|\n| 1 | $\\text{true}$ | Always | Trivially satisfied |\n| 2 | $a \\in AP$ | $a \\in L(\\pi[0])$ | Atomic prop. $a$ holds in the *current* state |\n| 3 | $\\neg\\psi$ | $\\pi \\not\\models \\psi$ | Logical negation |\n| 4 | $\\psi_1 \\vee \\psi_2$ | $\\pi \\models \\psi_1 \\;\\text{or}\\; \\pi \\models \\psi_2$ | Disjunction |\n| 5 | $\\operatorname{X}\\psi$ | $\\pi[1..] \\models \\psi$ | $\\psi$ holds in the **next** step |\n| 6 | $\\psi_1 \\operatorname{U} \\psi_2$ | $\\exists\\, j \\geq 0.\\; \\pi[j..] \\models \\psi_2 \\;\\text{and}\\; \\forall\\, 0 \\leq i < j.\\; \\pi[i..] \\models \\psi_1$ | $\\psi_1$ holds *until* $\\psi_2$ becomes true |\n\n**State-vs-Path Satisfaction (the bridge used in KTU 2024 model-checking questions):**\n\n| Concept | Notation | Condition |\n|---|---|---|\n| State satisfies atomic prop. | $s \\models a$ | $a \\in L(s)$ |\n| State satisfies LTL formula | $s \\models \\varphi$ | for **all** paths $\\pi$ starting at $s$: $\\pi \\models \\varphi$ |\n| Model satisfies property | $TS \\models P$ | for **all** initial paths $\\pi$: $\\text{trace}(\\pi) \\in P$ |\n\n> **CRITICAL KTU Pitfall (no table breakage):** The \"next-state\" operator $\\operatorname{X}$ has an empty semantics on a path of length zero. Always write $\\pi[1..]$ (the *suffix* path starting at index 1) — never $\\pi[0..]$ — to avoid the common error of confusing \"current\" with \"next\".\n\n### 2.3 Real-World Engineering Utility\n\nThe satisfaction relation is the **decision engine** of every industrial model checker (SPIN, NuSMV, CBMC, UPPAAL). Engineers use it to:\n\n- Verify that a **hardware cache coherence protocol** always grants exclusive access ($\\operatorname{G}(\\text{req} \\rightarrow \\operatorname{F}\\,\\text{grant})$).\n- Check that an **airbag controller** never deploys when the seat is empty ($\\operatorname{G}(\\text{empty} \\rightarrow \\neg\\text{deploy})$).\n- Validate that a **mutex** eventually grants every request ($\\operatorname{G}(\\text{req} \\rightarrow \\operatorname{F}\\,\\text{grant})$ — a *liveness* LT property).\n\n> [!IMPORTANT]\n> Every LTL formula denotes an LT property. Conversely, not every LT property is LTL-expressible; the path quantifier-free fragment of $\\text{CTL}^*$ restricted to a single path equals LTL — a result the KTU 2024 syllabus expects students to recognize.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Derivation: Equivalence of Trace-Based and Path-Based Satisfaction\n\n**Claim:** For any LT property $P \\subseteq (2^{AP})^\\omega$ and any two paths $\\pi, \\pi'$ in the transition system $TS$:\n\n$$\\text{trace}(\\pi) = \\text{trace}(\\pi') \\;\\;\\Longrightarrow\\;\\; \\big(\\pi \\models P \\;\\;\\iff\\;\\; \\pi' \\models P\\big)$$\n\n**Step 1 — Define the path-satisfaction relation.**\n\nBy KTU Module 1 definition, a path $\\pi$ satisfies an LT property $P$ if and only if its trace is a member of $P$:\n\n$$\\pi \\models P \\;\\;\\stackrel{\\text{def}}{=}\\;\\; \\text{trace}(\\pi) \\in P$$\n\n**Step 2 — Apply the hypothesis.**\n\nThe premise states that the traces are identical:\n\n$$\\text{trace}(\\pi) \\;=\\; \\text{trace}(\\pi') \\;\\stackrel{\\text{def}}{=}\\; \\tau$$\n\n**Step 3 — Substitute into the satisfaction condition.**\n\nNow we may rewrite both sides of the biconditional using $\\tau$:\n\n$$\\pi \\models P \\;\\iff\\; \\text{trace}(\\pi) \\in P \\;\\iff\\; \\tau \\in P$$\n\nand independently,\n\n$$\\pi' \\models P \\;\\iff\\; \\text{trace}(\\pi') \\in P \\;\\iff\\; \\tau \\in P$$\n\n**Step 4 — Conclude by transitivity of equivalence.**\n\nSince both $\\pi \\models P$ and $\\pi' \\models P$ reduce to the *same* proposition $\\tau \\in P$, they are equivalent:\n\n$$\\begin{aligned}\n\\pi \\models P \\;&\\iff\\; \\tau \\in P \\\\\n\\pi' \\models P \\;&\\iff\\; \\tau \\in P \\\\\n\\therefore\\;\\; \\pi \\models P \\;&\\iff\\; \\pi' \\models P\n\\end{aligned}$$\n\n$\\blacksquare$\n\nThis derivation confirms the central postulate of KTU Module 1: **satisfaction of an LT property is a *function* of the trace alone**, never of the underlying state sequence.\n\n### 3.2 Analytical Matrix: Satisfaction of Common LT Properties on a Sample Path\n\nConsider the path $\\pi = s_0 s_1 s_2 s_3 \\ldots$ in a transition system where the labels are:\n\n$$L(s_0) = \\{a\\}, \\quad L(s_1) = \\{a, b\\}, \\quad L(s_2) = \\{b\\}, \\quad L(s_3) = \\{\\}, \\quad L(s_4) = \\{c\\}, \\ldots$$\n\n| LTL Formula $\\varphi$ | Trace suffix checked | Verdict $\\pi \\models \\varphi$? |\n|---|---|---|\n| $a$ | $\\pi[0..] \\models a \\iff a \\in L(s_0) = \\{a\\}$ | **Yes** |\n| $a \\wedge b$ | $a \\in L(s_0)$ and $b \\in L(s_0)$ | **No** (only $a$ holds) |\n| $\\operatorname{X}\\,b$ | $\\pi[1..] \\models b \\iff b \\in L(s_1) = \\{a, b\\}$ | **Yes** |\n| $\\operatorname{F}\\,c$ | $\\exists j \\geq 0$ s.t. $c \\in L(s_j)$ — at $j=4$ | **Yes** |\n| $\\operatorname{G}\\,b$ | $\\forall j \\geq 0$ s.t. $b \\in L(s_j)$ — fails at $j=0$ | **No** |\n| $a \\operatorname{U}\\,c$ | $\\exists j = 4$ s.t. $c \\in L(s_4)$, and $\\forall 0 \\leq i < 4$, $a \\in L(s_i)$ — fails at $i=2, 3$ | **No** (violated at $s_2$) |\n| $\\operatorname{G}(a \\rightarrow \\operatorname{F}\\,b)$ | For every $i$ with $a \\in L(s_i)$, some $j \\geq i$ with $b \\in L(s_j)$ | **Yes** ($a$ at $s_0, s_1$; $b$ at $s_1 \\geq s_0$ and $b$ at $s_2 \\geq s_1$) |\n\n> [!IMPORTANT]\n> The matrix above is the **exam-ready analytical tool** for KTU 2024 valuation. Examiners award partial credit for *identifying the correct suffix path* used in the satisfaction check, even if the final Boolean verdict is wrong.\n\n### 3.3 Worked Example: State Satisfaction of $\\operatorname{G}\\,(p \\rightarrow \\operatorname{F}\\,q)$\n\n**Question.** Given a state $s$ with $L(s) = \\{p\\}$, and the unique outgoing path $\\pi = s\\,s_1\\,s_2\\,\\ldots$ with $L(s_1) = \\{\\}$, $L(s_2) = \\{q\\}$, $L(s_3) = \\{q\\}, \\ldots$ Does $s \\models \\operatorname{G}(p \\rightarrow \\operatorname{F}\\,q)$?\n\n**Step 1.** By the *state-satisfaction* rule, we must check the LTL property on *all* paths starting from $s$:\n\n$$s \\models \\varphi \\;\\iff\\; \\forall\\, \\pi \\in \\text{Paths}(s).\\; \\pi \\models \\varphi$$\n\n**Step 2.** For each index $i \\geq 0$ of $\\pi$, verify the implication $p \\rightarrow \\operatorname{F}\\,q$:\n\n$$\\begin{aligned}\ni=0:&\\; p \\in L(s_0) = \\{p\\} \\quad\\text{and}\\quad \\operatorname{F}\\,q \\text{ holds on } \\pi[0..] \\;(\\exists j = 2 : q \\in L(s_2)) \\\\\ni=1:&\\; p \\notin L(s_1) = \\{\\} \\quad\\text{implication vacuously true} \\\\\ni=2:&\\; p \\notin L(s_2) = \\{q\\} \\quad\\text{implication vacuously true}\n\\end{aligned}$$\n\n**Step 3.** Since the implication holds at *every* index, $\\pi \\models \\operatorname{G}(p \\rightarrow \\operatorname{F}\\,q)$.\n\n**Step 4.** If this were the only path, conclude $s \\models \\operatorname{G}(p \\rightarrow \\operatorname{F}\\,q)$. $\\blacksquare$\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Hierarchical Block Diagram: Where the Satisfaction Relation Lives\n\nThe figure below maps how the satisfaction relation $\\models$ connects the model, the paths it generates, the traces those paths produce, and the LT property being verified. Read the arrows as \"*informs / constrains the satisfaction verdict*\".\n\n```mermaid\nflowchart TD\n    TS[\"Kripke Structure TS over S S0 R L\"]\n    P0[\"Initial State s0 in S0\"]\n    PathPi[\"Infinite Path pi = s0 s1 s2 ...\"]\n    TrPi[\"Trace tau = Ls0 Ls1 Ls2 ...\"]\n    PropP[\"LT Property P subset of 2AP to omega\"]\n    SatsPath[\"Path Satisfaction pi models P\"]\n    SatsModel[\"Model Satisfaction TS models P\"]\n    LTL[\"LTL Formula phi\"]\n    SatsLTL[\"LTL Satisfaction pi models phi\"]\n\n    TS --> P0\n    P0 --> PathPi\n    PathPi --> TrPi\n    TrPi --> SatsPath\n    PropP --> SatsPath\n    SatsPath --> SatsModel\n    LTL --> SatsLTL\n    SatsLTL -.induced by.-> PropP\n    TS --> SatsModel\n```\n\n### 4.2 Decision Flow Diagram: The Satisfaction Algorithm\n\nThe figure below depicts the **recursive descent** the KTU Module 1 valuation key uses when grading a satisfaction claim. Note how each LTL operator delegates the satisfaction question to a *simpler* sub-question on a *shorter* path suffix.\n\n```mermaid\nflowchart TD\n    Start[\"Start: pi models phi?\"]\n    Q1{\"Is phi = a in AP?\"}\n    Q2{\"Is phi = not psi?\"}\n    Q3{\"Is phi = psi1 OR psi2?\"}\n    Q4{\"Is phi = X psi?\"}\n    Q5{\"Is phi = psi1 U psi2?\"}\n    R1[\"Yes iff a in L of pi 0\"]\n    R2[\"Yes iff pi NOT models psi\"]\n    R3[\"Yes iff pi models psi1 OR pi models psi2\"]\n    R4[\"Yes iff suffix pi 1.. models psi\"]\n    R5[\"Find j ge 0: pi j.. models psi2 and for all i lt j, pi i.. models psi1\"]\n    Done[\"Return Boolean verdict\"]\n\n    Start --> Q1\n    Start --> Q2\n    Start --> Q3\n    Start --> Q4\n    Start --> Q5\n    Q1 -->|yes| R1 --> Done\n    Q2 -->|yes| R2 --> Done\n    Q3 -->|yes| R3 --> Done\n    Q4 -->|yes| R4 --> Done\n    Q5 -->|yes| R5 --> Done\n    Q1 -->|no| Q2\n    Q2 -->|no| Q3\n    Q3 -->|no| Q4\n    Q4 -->|no| Q5\n```\n\n> [!NOTE]\n> **Reading aid:** In the diagrams, $\\operatorname{X} = $ \"Next\", $\\operatorname{U} = $ \"Until\". The decision flow matches the inductive clauses of Section 2.2 exactly — this is the algorithm an examiner mentally executes while grading a long-answer script.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short-Answer Questions (3 Marks Each)\n\n> **Q1. [KTU University Exam — Dec 2023, Model Set A]**\n> Define an **LT property** formally. Why is it called \"linear-time\"?\n\n**Model Answer (3 marks):**\n\nA property $P \\subseteq (2^{AP})^\\omega$ is a **Linear-Time (LT) property** if for any two paths $\\pi, \\pi'$ of a transition system $TS$:\n\n$$\\text{trace}(\\pi) = \\text{trace}(\\pi') \\;\\Longrightarrow\\; \\pi \\models P \\iff \\pi' \\models P$$\n\nIt is called *linear-time* because the satisfaction of $P$ is evaluated along a *single*, linear (totally ordered) execution trace, as opposed to branching-time logics (like CTL) that quantify over the **tree** of all possible futures. *(1 mark for the definition, 1 mark for the trace-equivalence condition, 1 mark for the \"single-trace\" justification.)*\n\n---\n\n> **Q2. [KTU University Exam — May 2024, Model Set B]**\n> Differentiate between **path satisfaction** ($\\pi \\models \\varphi$) and **state satisfaction** ($s \\models \\varphi$) in LTL.\n\n**Model Answer (3 marks):**\n\n| Aspect | Path Satisfaction $\\pi \\models \\varphi$ | State Satisfaction $s \\models \\varphi$ |\n|---|---|---|\n| Object | An infinite path $\\pi$ | A single state $s$ |\n| Meaning | $\\varphi$ holds on the trace of $\\pi$ | $\\varphi$ holds on **all** paths starting at $s$ |\n| Quantifier | None (path quantifier-free) | Universal over $\\text{Paths}(s)$ |\n| Used for | LTL path formulas | Embedding LTL into state-based reasoning |\n\nState satisfaction is defined as $s \\models \\varphi \\iff \\forall \\pi \\in \\text{Paths}(s).\\; \\pi \\models \\varphi$. *(1 mark per row.)*\n\n---\n\n### 5.2 Part B — Long-Answer Questions (14 Marks Each, Module-Internal Choice)\n\n#### **Question A (14 Marks)**\n\n> **[KTU University Exam — Dec 2023, Q. 1(a)–(b)]**\n>\n> **(a)** For the Kripke structure shown below, where $AP = \\{a, b, c\\}$ and the state labels are:\n> $L(s_0) = \\{a\\}$, $L(s_1) = \\{a, b\\}$, $L(s_2) = \\{b, c\\}$, $L(s_3) = \\{c\\}$,\n> and the transitions are $s_0 \\to s_1$, $s_1 \\to s_2$, $s_2 \\to s_3$, $s_3 \\to s_3$ (self-loop),\n> evaluate the satisfaction of the LTL formula $\\varphi = a \\operatorname{U}\\,c$ on the path $\\pi = s_0\\,s_1\\,s_2\\,s_3\\,s_3\\,s_3\\,\\ldots$ **(7 marks)**.\n>\n> **(b)** Prove that the formula $\\operatorname{G}\\,\\varphi$ is satisfied by a path $\\pi$ if and only if **every suffix** $\\pi[k..]$ for $k \\geq 0$ satisfies $\\varphi$. State the LTL identity that allows the rewriting of $\\operatorname{F}\\,\\varphi$ as $\\neg\\operatorname{G}\\,\\neg\\varphi$ and justify it. **(7 marks)**\n\n**Model Solution:**\n\n**(a) — 7 marks**\n\nApply the **Until** satisfaction rule inductively along the path:\n\n$$\\pi \\models a \\operatorname{U}\\,c \\;\\;\\iff\\;\\; \\exists j \\geq 0.\\; \\pi[j..] \\models c \\;\\;\\text{and}\\;\\; \\forall\\, 0 \\leq i < j.\\; \\pi[i..] \\models a$$\n\n| Index $i$ | State $s_i$ | $L(s_i)$ | $a \\in L(s_i)$? | $c \\in L(s_i)$? |\n|---|---|---|---|---|\n| 0 | $s_0$ | $\\{a\\}$ | ✓ | ✗ |\n| 1 | $s_1$ | $\\{a, b\\}$ | ✓ | ✗ |\n| 2 | $s_2$ | $\\{b, c\\}$ | ✗ | ✓ |\n| 3 | $s_3$ | $\\{c\\}$ | ✗ | ✓ |\n\nChoose $j = 2$. Then $\\pi[2..] \\models c$ because $c \\in L(s_2)$. Verify the \"until\" condition: for all $0 \\leq i < 2$ (i.e., $i = 0, 1$), $\\pi[i..] \\models a$ — both true.\n\n$$\\therefore\\;\\; \\pi \\models a \\operatorname{U}\\,c \\;\\; \\text{is TRUE.} \\qquad \\blacksquare$$\n\n*(3 marks for setting up the rule, 2 marks for the table, 2 marks for the verdict.)*\n\n**(b) — 7 marks**\n\n**Claim:** $\\pi \\models \\operatorname{G}\\varphi \\iff \\forall k \\geq 0.\\; \\pi[k..] \\models \\varphi$.\n\n*Proof.* By definition, $\\pi \\models \\operatorname{G}\\varphi$ means \"$\\varphi$ holds at every position of $\\pi$\". The position $k$ of $\\pi$ corresponds precisely to the *first* state of the suffix path $\\pi[k..]$. Hence:\n\n$$\\begin{aligned}\n\\pi \\models \\operatorname{G}\\varphi \\;&\\stackrel{\\text{def}}{=}\\;\\; \\forall k \\geq 0.\\;\\; \\pi \\text{ satisfies } \\varphi \\text{ at position } k \\\\\n&\\stackrel{\\text{rewrite}}{=}\\;\\; \\forall k \\geq 0.\\;\\; \\pi[k..] \\models \\varphi \\qquad \\blacksquare\n\\end{aligned}$$\n\n**LTL identity:** $\\operatorname{F}\\varphi \\equiv \\neg\\operatorname{G}\\neg\\varphi$ *(Dual of G)*.\n\n*Justification:* $\\pi \\models \\operatorname{F}\\varphi$ means $\\varphi$ holds *eventually* on $\\pi$, i.e., $\\exists k \\geq 0$ with $\\pi[k..] \\models \\varphi$. Negating \"$\\varphi$ always holds\" ($\\operatorname{G}\\neg\\varphi$) gives \"$\\varphi$ holds at some point\" — exactly the meaning of $\\operatorname{F}\\varphi$. Formally:\n\n$$\\neg(\\pi \\models \\operatorname{G}\\neg\\varphi) \\;\\iff\\; \\neg(\\forall k \\geq 0.\\; \\pi[k..] \\models \\neg\\varphi) \\;\\iff\\; \\exists k \\geq 0.\\; \\pi[k..] \\models \\varphi \\;\\iff\\; \\pi \\models \\operatorname{F}\\varphi \\qquad \\blacksquare$$\n\n*(3 marks for the G-suffix proof, 2 marks for the identity statement, 2 marks for the formal justification.)*\n\n---\n\n#### **Question B (14 Marks)**\n\n> **[KTU University Exam — May 2024, Q. 2(a)–(b)]**\n>\n> **(a)** Define a **Kripke Structure**. For the Kripke structure with $S = \\{s_0, s_1, s_2\\}$, $S_0 = \\{s_0\\}$, $L(s_0) = \\{p\\}$, $L(s_1) = \\{q\\}$, $L(s_2) = \\{p, q\\}$, and $R = \\{(s_0,s_1), (s_1,s_2), (s_2,s_1)\\}$, determine the set of all initial paths and verify whether $s_0 \\models \\operatorname{G}(p \\rightarrow \\operatorname{F}\\,q)$. **(7 marks)**\n>\n> **(b)** Explain, with a formal proof, **why** the satisfaction relation $\\pi \\models \\varphi$ for an LTL formula $\\varphi$ depends only on the trace $\\text{trace}(\\pi)$ and not on the actual state labels themselves. Conclude with the definition of an LT property. **(7 marks)**\n\n**Model Solution:**\n\n**(a) — 7 marks**\n\nA Kripke structure is the 4-tuple $TS = (S, S_0, R, L)$ where $S$ is a finite state set, $S_0 \\subseteq S$ are initial states, $R \\subseteq S \\times S$ is the (total) transition relation, and $L : S \\to 2^{AP}$ labels each state with the set of atomic propositions true in it. *(2 marks)*\n\n**Initial paths (paths starting at $s_0$):** The transition $s_0 \\to s_1$ is forced; from $s_1$ we go to $s_2$; from $s_2$ back to $s_1$. The path is therefore the **unique** infinite path:\n\n$$\\pi = s_0\\,s_1\\,s_2\\,s_1\\,s_2\\,s_1\\,s_2\\,\\ldots$$\n\nwith corresponding trace $\\tau = \\{p\\}\\,\\{q\\}\\,\\{p,q\\}\\,\\{q\\}\\,\\{p,q\\}\\,\\ldots$\n\n**Check $s_0 \\models \\operatorname{G}(p \\rightarrow \\operatorname{F}\\,q)$:** Since there is only one path, the universal quantification over $\\text{Paths}(s_0)$ is trivially satisfied if $\\pi$ satisfies the formula. Verify the implication position by position:\n\n| $i$ | $L(s_i)$ | $p \\in L(s_i)$? | Does $q$ hold later? |\n|---|---|---|---|\n| 0 | $\\{p\\}$ | Yes | Yes, at $i = 1$ ($q \\in L(s_1)$) ✓ |\n| 1 | $\\{q\\}$ | No | Implication vacuously true ✓ |\n| 2 | $\\{p, q\\}$ | Yes | Yes, at $i = 3$ ✓ |\n| … | … | … | … |\n\nThe implication $p \\rightarrow \\operatorname{F}\\,q$ holds at every index. Therefore:\n\n$$s_0 \\models \\operatorname{G}(p \\rightarrow \\operatorname{F}\\,q) \\quad \\text{is TRUE.} \\qquad \\blacksquare$$\n\n*(2 marks for the paths, 3 marks for the table-style verification.)*\n\n**(b) — 7 marks**\n\nThe LTL satisfaction relation $\\pi \\models \\varphi$ is defined inductively on the syntax of $\\varphi$. The atomic-proposition case $\\pi \\models a$ checks $a \\in L(\\pi[0])$, i.e., a *set* of labels, not the state identity. Every recursive clause — $\\neg$, $\\vee$, $\\operatorname{X}$, $\\operatorname{U}$ — refers only to the *traces* of the path or its suffixes. Therefore, two paths with identical traces produce *identical* satisfaction verdicts for *every* LTL formula.\n\n*Formal proof:* Let $\\pi, \\pi'$ be paths with $\\text{trace}(\\pi) = \\text{trace}(\\pi')$. We prove by structural induction on $\\varphi$ that $\\pi \\models \\varphi \\iff \\pi' \\models \\varphi$.\n\n**Base case ($\\varphi = a$):** $\\pi \\models a \\iff a \\in L(\\pi[0]) \\iff a \\in L(\\pi'[0]) \\iff \\pi' \\models a$. ✓\n\n**Inductive step ($\\varphi = \\operatorname{X}\\psi$):** $\\text{trace}(\\pi[1..]) = \\text{trace}(\\pi'[1..])$ (suffixes preserve trace equality). By the induction hypothesis, $\\pi[1..] \\models \\psi \\iff \\pi'[1..] \\models \\psi$. Hence $\\pi \\models \\operatorname{X}\\psi \\iff \\pi' \\models \\operatorname{X}\\psi$. ✓\n\n**Inductive step ($\\varphi = \\psi_1 \\operatorname{U}\\psi_2$):** For every $j \\geq 0$, $\\text{trace}(\\pi[j..]) = \\text{trace}(\\pi'[j..])$, so by the induction hypothesis each suffix-satisfaction agrees. Hence the existential/universal quantifiers over $j$ agree. ✓\n\n($\\neg$ and $\\vee$ are immediate by the induction hypothesis.)\n\n**Conclusion:** The set $\\{\\, \\pi \\in \\text{Paths}(TS) \\mid \\pi \\models \\varphi \\,\\}$ is a function of $\\text{trace}(\\pi)$ alone. Therefore every LTL formula $\\varphi$ defines an LT property:\n\n$$P_\\varphi = \\{\\, \\text{trace}(\\pi) \\mid \\pi \\models \\varphi \\,\\} \\subseteq (2^{AP})^\\omega$$\n\nsatisfying the defining condition of an LT property from Section 1.1. $\\blacksquare$\n\n*(3 marks for the inductive structure, 2 marks for the trace-suffix argument, 2 marks for the LT-property conclusion.)*\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Where students lose marks on \"Satisfaction Relation for LT Properties\" questions:**\n>\n> 1. **Confusing state vs. path satisfaction (–3 marks):** Writing \"$s \\models \\operatorname{F}\\,p$\" without first expanding it as \"$\\forall \\pi \\in \\text{Paths}(s).\\; \\pi \\models \\operatorname{F}\\,p$\" is the single most common error. State satisfaction in LTL implicitly *universally quantifies* over all paths.\n> 2. **Wrong suffix on $\\operatorname{X}$ (–2 marks):** The $\\operatorname{X}$ operator checks the *next* state, not the *current* one. Always use $\\pi[1..]$, never $\\pi[0..]$.\n> 3. **Omitting the \"until\" condition in $\\operatorname{U}$ (–2 marks):** When checking $\\psi_1 \\operatorname{U} \\psi_2$, students often locate the first $j$ where $\\psi_2$ holds but forget to verify that $\\psi_1$ held at *all* earlier positions.\n> 4. **Treating $\\operatorname{G}$ and $\\operatorname{F}$ as primitive instead of derived (–1 mark):** KTU 2024 expects $\\operatorname{F}\\varphi = \\text{true} \\operatorname{U}\\varphi$ and $\\operatorname{G}\\varphi = \\neg\\operatorname{F}\\neg\\varphi$ to be quoted from the syntax.\n> 5. **Using $\\pi$ where $\\text{trace}(\\pi)$ is required (–2 marks):** The satisfaction relation for an LT property $P$ is **defined** on traces, not on paths. Mixing the two levels in the formal statement is a common deduction trigger.\n\n### 5.4 Topic Recap & Important Things to Remember\n\n- ✅ **LT property** = a set of traces $P \\subseteq (2^{AP})^\\omega$ such that trace-equality implies satisfaction-equality.\n- ✅ **Satisfaction relation** $\\models$ is the *membership* of a trace in $P$: $\\pi \\models P \\iff \\text{trace}(\\pi) \\in P$.\n- ✅ **Kripke structure** $TS = (S, S_0, R, L)$ with **total** $R$ is the canonical model.\n- ✅ **Path** = infinite state sequence obeying $R$; **trace** = corresponding label sequence in $(2^{AP})^\\omega$.\n- ✅ **LTL syntax:** $\\varphi ::= \\text{true} \\mid a \\mid \\neg\\varphi \\mid \\varphi \\vee \\varphi \\mid \\operatorname{X}\\varphi \\mid \\varphi \\operatorname{U} \\varphi$.\n- ✅ **Derived operators:** $\\operatorname{F}\\varphi \\equiv \\text{true} \\operatorname{U}\\varphi$ and $\\operatorname{G}\\varphi \\equiv \\neg\\operatorname{F}\\neg\\varphi$.\n- ✅ **Until rule:** $\\pi \\models \\psi_1 \\operatorname{U} \\psi_2 \\iff \\exists j \\geq 0.\\; \\pi[j..] \\models \\psi_2$ **and** $\\forall 0 \\leq i < j.\\; \\pi[i..] \\models \\psi_1$.\n- ✅ **Next rule:** $\\pi \\models \\operatorname{X}\\psi \\iff \\pi[1..] \\models \\psi$ (suffix of $\\pi$, never the prefix).\n- ✅ **State vs. path:** $s \\models \\varphi \\iff \\forall \\pi \\in \\text{Paths}(s).\\; \\pi \\models \\varphi$.\n- ✅ **Model vs. property:** $TS \\models P \\iff \\forall$ initial path $\\pi.\\; \\text{trace}(\\pi) \\in P$.\n- ✅ **LTL ⇒ LT property** (every LTL formula defines an LT property by trace projection).\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t20",
          "title": "Trace Equivalence and LT Properties",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module Module 1\n> - **Topic:** Trace Equivalence and LT Properties\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Trace Equivalence — Formal Definition\n\nIn the formal theory of concurrent and communicating systems (CCS, CSP, LTS), a **trace** of a labelled transition system (LTS) is a finite sequence of observable actions that the system can perform from its initial state, following the transition relation. Given an LTS $\\mathcal{T} = (S, Act, \\rightarrow, s_0)$ with states $S$, action alphabet $Act$, transition relation $\\rightarrow \\subseteq S \\times Act \\times S$ and initial state $s_0$, the set of **traces** of $\\mathcal{T}$ is:\n\n$$Tr(\\mathcal{T}) = \\{\\, a_1 a_2 \\ldots a_n \\in Act^{*} \\mid \\exists\\, s_0 \\xrightarrow{a_1} s_1 \\xrightarrow{a_2} s_2 \\cdots \\xrightarrow{a_n} s_n \\,\\}$$\n\nwhere $Act^{*}$ denotes the set of all finite sequences (including the empty trace $\\varepsilon$).\n\n> [!IMPORTANT]\n> **Trace Equivalence (KTU 2024 Definition):** Two LTSs $\\mathcal{T}_1$ and $\\mathcal{T}_2$ are **trace equivalent**, written $\\mathcal{T}_1 \\approx_T \\mathcal{T}_2$ (or $P \\approx_T Q$ for processes), if and only if they exhibit exactly the same set of observable traces:\n> $$\\mathcal{T}_1 \\approx_T \\mathcal{T}_2 \\quad \\iff \\quad Tr(\\mathcal{T}_1) = Tr(\\mathcal{T}_2)$$\n\nEquivalently, for every finite sequence of actions $\\sigma \\in Act^{*}$:\n\n$$\\mathcal{T}_1 \\xrightarrow{\\sigma} \\quad \\iff \\quad \\mathcal{T}_2 \\xrightarrow{\\sigma}$$\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine two waiters in a restaurant, **Waiter-A** and **Waiter-B**, each described by a rule-book. If we record every possible *order of events* (take order → serve soup → serve main course → give bill → receive payment) that a waiter *can* produce from start to finish, then:\n\n- If Waiter-A and Waiter-B can produce **the exact same collection of event-sequences**, an outside observer who only watches the *flow of visible actions* (without caring which internal state the waiter is in) cannot tell them apart.\n- This is precisely **trace equivalence**: the two systems are indistinguishable on the basis of which *action sequences* are possible — even if their internal state structures differ wildly.\n\n> [!NOTE]\n> **Intuitive Rule-of-Thumb:** Trace equivalence compares *what sequences of actions* are possible, **not** *at which internal states* they are produced. It is therefore a **black-box / external behaviour** equivalence, the weakest among the standard behavioural equivalences in concurrency theory.\n\n## 1.3 LT Properties — Formal Definition\n\nA **property** of a system is any subset of the set of all (finite or infinite) executions that the system *might* produce. Formally, let $\\Sigma$ be a fixed alphabet of actions. A **linear-time (LT) property** over $\\Sigma$ is a set of infinite words:\n\n$$P \\;\\subseteq\\; \\Sigma^{\\omega}$$\n\nA system $\\mathcal{T}$ (or process $P$) **satisfies** the property $P$ if and only if the set of infinite executions of $\\mathcal{T}$ is contained in $P$:\n\n$$\\mathcal{T} \\models P \\quad \\iff \\quad Exec^{\\omega}(\\mathcal{T}) \\;\\subseteq\\; P$$\n\nEquivalently, an LT property is a set of behaviours that a system may or may not exhibit — and verification asks whether the system's actual behaviour lies *inside* this set.\n\n> [!IMPORTANT]\n> **Why \"Linear-Time\"?** Because properties are characterised along each *individual execution path* (a single linear timeline), as opposed to **branching-time (BT)** properties which quantify over the system's tree of all possible futures at every state.\n\n## 1.4 Intuitive View of LT Properties\n\nThink of a property as a *checklist* of admissible histories of the world. The verification question is then: *\"Does every possible future of the system belong to the checklist?\"*\n\n> [!NOTE]\n> **The two canonical subclasses of LT properties** — every LT property $P$ can be written as the conjunction of a safety property and a liveness property:\n> $$P \\;=\\; \\text{Safety}(P) \\;\\cap\\; \\text{Liveness}(P)$$\n\n## 1.5 Key Notation Standards (KTU Board Standard)\n\n| Symbol | Meaning |\n|:--|:--|\n| $Act$ | Set of all observable action labels |\n| $Act^{*}$ | All finite sequences (including $\\varepsilon$) |\n| $Act^{\\omega}$ | All infinite sequences |\n| $Tr(P)$ | Set of finite traces of process $P$ |\n| $\\sigma, \\tau$ | Traces (action sequences) |\n| $\\mathcal{T} \\xrightarrow{\\sigma}$ | There exists a path labelled $\\sigma$ from the initial state |\n| $P \\models \\varphi$ | Process $P$ satisfies property $\\varphi$ |\n| $\\Sigma$ | Alphabet of actions for an LT property |\n\n> [!IMPORTANT]\n> **Syllabus Highlight (PECNT526 / Module 1):** Trace equivalence and LT properties together form the **observational foundation** of model checking. Every model checker (SPIN, NuSMV, CADP) ultimately reduces verification to a question about traces and trace-containment.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Anatomy of a Trace\n\nA trace is built inductively from the empty trace $\\varepsilon$ and the transition relation. The set of traces $Tr(P)$ of a process $P$ is the smallest set such that:\n\n1. **Base case:** $\\varepsilon \\in Tr(P)$ (every process has the empty trace).\n2. **Inductive step:** If $\\sigma \\in Tr(P)$ and $P \\xrightarrow{\\sigma} P'$ and $P' \\xrightarrow{a} P''$, then $\\sigma a \\in Tr(P)$.\n\n> [!NOTE]\n> In some textbooks a distinction is drawn between $Tr(P)$ (all finite traces) and $Tr^{\\max}(P)$ (all *maximal* traces, i.e. those that cannot be extended because the process has deadlocked) and $Tr^{fin}(P)$ (the *completed / terminated* traces, ending at a state labelled $\\surd$). The empty trace is **always** present.\n\n## 2.2 Properties of Trace Equivalence as a Relation\n\nTrace equivalence satisfies the three axioms of an equivalence relation:\n\n- **Reflexivity:** $P \\approx_T P$, because $Tr(P) = Tr(P)$.\n- **Symmetry:** If $P \\approx_T Q$ then $Tr(P) = Tr(Q)$ implies $Tr(Q) = Tr(P)$, hence $Q \\approx_T P$.\n- **Transitivity:** If $P \\approx_T Q$ and $Q \\approx_T R$ then $Tr(P) = Tr(Q) = Tr(R)$, so $P \\approx_T R$.\n\nAdditionally:\n\n- **Pre-order form:** Trace *pre-order* (containment) is written $P \\sqsubseteq_T Q$ iff $Tr(P) \\subseteq Tr(Q)$.\n- **Kernel:** $\\approx_T$ is the kernel of $\\sqsubseteq_T$ (its symmetric part).\n\n## 2.3 Hierarchy of Behavioural Equivalences (KTU High-Yield)\n\nThe standard lattice from **finest** to **coarsest** equivalence (each one identifies *more* processes):\n\n$$\\text{Strong Bisim.} \\;\\succ\\; \\text{2-Nested Simulation} \\;\\succ\\; \\text{Simulation} \\;\\succ\\; \\text{Trace Eq.} \\;\\succ\\; \\text{Failure Eq.} \\;\\succ\\; \\text{Failure-Trace Eq.} \\;\\succ\\; \\text{Testing Eq.}$$\n\nwhere $\\succ$ means \"is strictly finer than\" (i.e. distinguishes more pairs of processes).\n\n> [!WARNING]\n> A **common KTU board mistake** is to write \"trace equivalence is finer than bisimulation\" — the direction is **reversed**: bisimulation is finer (more discriminating), trace equivalence is coarser.\n\n## 2.4 The Four Standard Classes of LT Properties\n\n| Class | Informal Meaning | Formal Definition |\n|:--|:--|:--|\n| **Safety** | \"Nothing bad ever happens\" | $\\forall \\sigma \\in P:\\ \\exists$ \"bad prefix\" $x$ s.t. $\\sigma x \\notin P$ |\n| **Liveness** | \"Something good eventually happens\" | $\\forall \\sigma \\in \\Sigma^{*}:\\ \\exists \\tau$ with $\\sigma\\tau \\in P$ |\n| **Deadlock-Freedom** | \"An action is always possible\" | A safety property, formally: $\\forall s \\in reach(s_0):\\ \\exists a.\\ s \\xrightarrow{a}$ |\n| **Invariant** | \"A predicate $\\psi$ holds in every reachable state\" | A safety property expressed state-wise |\n\n## 2.5 Closure Properties — KTU Formula Sheet\n\nFor a property $P \\subseteq \\Sigma^{\\omega}$ to be a **valid LT property** (i.e. a *prefix-closed, limit-closed* set), it must satisfy:\n\n1. **Prefix-Closure for Safety:** If $\\sigma \\in P$ and $\\tau$ is a prefix of $\\sigma$, then $\\tau \\in P$.\n2. **Limit-Closure:** If an infinite word $\\alpha$ has *every* one of its finite prefixes belonging to $P$, then $\\alpha \\in P$.\n\n> [!IMPORTANT]\n> **Alper-Sistla Theorem (used heavily in KTU Q-bank):** Every LT property $P$ can be uniquely written as $P = \\mathsf{Safe}(P) \\cap \\mathsf{Live}(P)$, where:\n> - $\\mathsf{Safe}(P) = \\{\\sigma \\in \\Sigma^{\\omega} \\mid \\text{no finite prefix of }\\sigma\\text{ is a \"bad prefix\"}\\}$\n> - $\\mathsf{Live}(P) = $ smallest liveness property containing $P$.\n\n## 2.6 KTU High-Yield Formula Sheet\n\n| # | Formula / Rule | Description |\n|:--|:--|:--|\n| 1 | $P \\approx_T Q \\iff Tr(P) = Tr(Q)$ | Trace equivalence definition |\n| 2 | $P \\sqsubseteq_T Q \\iff Tr(P) \\subseteq Tr(Q)$ | Trace pre-order (containment) |\n| 3 | $\\mathcal{T} \\models P \\iff Exec^{\\omega}(\\mathcal{T}) \\subseteq P$ | System satisfies LT property |\n| 4 | $P = \\mathsf{Safe}(P) \\cap \\mathsf{Live}(P)$ | Alper-Sistla decomposition |\n| 5 | $\\sigma \\in P \\Rightarrow$ every prefix of $\\sigma$ in $P$ | Prefix-closure (safety) |\n| 6 | $\\forall$ prefixes of $\\alpha$ in $P \\Rightarrow \\alpha \\in P$ | Limit-closure (liveness) |\n| 7 | $\\varepsilon \\in Tr(P)$ for any $P$ | Empty trace axiom |\n| 8 | $\\text{Bisim} \\Rightarrow \\text{Trace Eq.} \\Rightarrow \\text{Failure Eq.}$ | Strict implication chain |\n| 9 | $\\sigma \\in Tr(P) \\wedge P \\xrightarrow{\\sigma} P' \\Rightarrow \\varepsilon \\in Tr(P')$ | Trace inheritance |\n| 10 | $P \\models \\Box \\varphi$ (always) iff all states along trace satisfy $\\varphi$ | LTL semantics |\n\n## 2.7 Real-World Engineering Utility\n\n| Domain | Application of Trace Equivalence & LT Properties |\n|:--|:--|\n| **Hardware Verification** | Checking that two RTL implementations are trace-equivalent (industrial equivalence checking) |\n| **Protocol Verification** | Verifying security protocols (TLS, Kerberos) using safety LT properties |\n| **Software Model Checking** | SLAM, BLAST, Java PathFinder verify safety LT properties on C/Java programs |\n| **Compiler Correctness** | Showing that *comp(P)* and *P* are trace-equivalent (CompCert-style proofs) |\n| **Cyber-Physical Systems** | Liveness LT properties guarantee eventual response in automotive ECUs |\n| **Smart Contracts (Solidity)** | Trace properties detect re-entrancy and integer-overflow bugs |\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Worked Example — Computing the Trace Set of a CCS Process\n\nConsider the CCS process (in Milner's notation):\n\n$$P \\;=\\; a.\\,b.\\mathbf{0} \\;+\\; c.\\,d.\\mathbf{0}$$\n\nThis process may *either* execute action $a$ followed by $b$ and terminate, *or* execute $c$ followed by $d$ and terminate. The initial choice is non-deterministic.\n\n**Step 1 — Identify the initial actions.**\nFrom the initial state, the possible first actions are $a$ and $c$ (the guards of the two summands of $+$).\n\n**Step 2 — Extend each first action to its continuations.**\n\n- If $a$ is chosen, the residual process becomes $b.\\mathbf{0}$, whose only action is $b$.\n- If $c$ is chosen, the residual process becomes $d.\\mathbf{0}$, whose only action is $d$.\n\n**Step 3 — Enumerate all traces.**\n\nThe complete trace set, by structural induction, is:\n\n$$Tr(P) \\;=\\; \\{\\, \\varepsilon,\\; a,\\; c,\\; a\\,b,\\; c\\,d \\,\\}$$\n\n**Step 4 — Verification of prefix-closure.**\n\nFor each non-empty trace, every prefix must also belong to $Tr(P)$:\n- $a$ → prefix $\\varepsilon \\in Tr(P)$ ✓\n- $c$ → prefix $\\varepsilon \\in Tr(P)$ ✓\n- $ab$ → prefixes $a, \\varepsilon \\in Tr(P)$ ✓\n- $cd$ → prefixes $c, \\varepsilon \\in Tr(P)$ ✓\n\n> [!NOTE]\n> Notice the trace $ac \\notin Tr(P)$: the process cannot first choose $a$ and then $c$, because once $a$ is chosen, the residual $b.\\mathbf{0}$ has no $c$-transition. Trace equivalence would *not* equate $P$ with a process that *could* execute $ac$.\n\n## 3.2 Analytical Matrix — Trace Equivalence vs. Strong Bisimulation\n\nWe now demonstrate the **concrete distinction** between trace equivalence and bisimulation on a textbook example.\n\nLet:\n\n$$P \\;=\\; a.\\,(b.\\mathbf{0} \\;+\\; c.\\mathbf{0})$$\n$$Q \\;=\\; a.\\,b.\\mathbf{0} \\;+\\; a.\\,c.\\mathbf{0}$$\n\n### Step-by-step trace computation\n\n**Traces of $P$:**\n- Initial actions: $a$. After $a$, the residual is $b.\\mathbf{0} + c.\\mathbf{0}$, which can do $b$ *or* $c$.\n- Hence $Tr(P) = \\{\\varepsilon,\\ a,\\ ab,\\ ac\\}$.\n\n**Traces of $Q$:**\n- Initial actions: $a$ (twice, in two different summands). After $a$, the residual is $b.\\mathbf{0}$ in one branch and $c.\\mathbf{0}$ in the other.\n- The set of possible second actions is again $\\{b, c\\}$.\n- Hence $Tr(Q) = \\{\\varepsilon,\\ a,\\ ab,\\ ac\\}$.\n\n**Conclusion on trace equivalence:**\n\n$$Tr(P) \\;=\\; Tr(Q) \\;=\\; \\{\\varepsilon, a, ab, ac\\}$$\n\n$$\\Longrightarrow\\quad P \\approx_T Q$$\n\n**Bisimulation check (showing the difference):**\n\nA bisimulation $\\mathcal{R}$ must relate $P$ and $Q$ and also match transitions. After the common $a$-action, the residuals are:\n\n$$P' \\;=\\; b.\\mathbf{0} + c.\\mathbf{0} \\quad\\quad Q' \\;=\\; b.\\mathbf{0} \\;\\;\\text{(in one branch)} \\;\\;\\text{or}\\;\\; c.\\mathbf{0} \\;\\;\\text{(in the other)}$$\n\nThe state $P'$ has a $b$-transition **and** a $c$-transition, but $Q'$ in *each individual branch* has only one. Therefore $P'$ is *not* bisimilar to either $b.\\mathbf{0}$ or $c.\\mathbf{0}$ alone, and **no bisimulation can relate $P$ to $Q$**.\n\n$$\\Longrightarrow\\quad P \\not\\sim Q \\quad \\text{(not bisimilar)}$$\n\n> [!IMPORTANT]\n> **The asymmetry:** $P \\approx_T Q$ but $P \\not\\sim Q$. This *single counter-example* is the standard KTU exam illustration of \"trace equivalence is strictly coarser than strong bisimulation.\"\n\n## 3.3 Derivation — Alper–Sistla Safety/Liveness Decomposition\n\n**Claim:** Every LT property $P \\subseteq \\Sigma^{\\omega}$ can be decomposed uniquely as $P = \\mathsf{Safe}(P) \\cap \\mathsf{Live}(P)$.\n\n**Step 1 — Define the \"bad prefixes\" of $P$.**\nA finite word $x \\in \\Sigma^{*}$ is a *bad prefix* of $P$ if for every infinite extension $\\alpha \\in \\Sigma^{\\omega}$ we have $x\\alpha \\notin P$. Equivalently, no infinite word in $P$ has $x$ as a prefix.\n\n**Step 2 — Construct the safety component.**\n\n$$\\mathsf{Safe}(P) \\;=\\; \\{\\, \\alpha \\in \\Sigma^{\\omega} \\mid \\text{no finite prefix of } \\alpha \\text{ is a bad prefix of } P \\,\\}$$\n\nBy construction, $\\mathsf{Safe}(P)$ is *prefix-closed* and contains $P$.\n\n**Step 3 — Construct the liveness component.**\n\n$$\\mathsf{Live}(P) \\;=\\; \\{\\, \\alpha \\in \\Sigma^{\\omega} \\mid \\forall x \\in \\Sigma^{*}:\\ \\exists \\beta \\in \\Sigma^{\\omega}:\\ x\\beta \\in P \\;\\text{and}\\; x\\beta \\text{ is a prefix of } \\alpha \\,\\}$$\n\ni.e., $\\mathsf{Live}(P)$ is the *smallest liveness property containing $P$*; every finite prefix can be extended to land inside $P$.\n\n**Step 4 — Verify the intersection.**\n\nWe must show $P = \\mathsf{Safe}(P) \\cap \\mathsf{Live}(P)$:\n\n- $\\subseteq$: Take $\\alpha \\in P$. Then no prefix of $\\alpha$ is a bad prefix of $P$ (else that prefix could not be extended into $P$, contradicting $\\alpha \\in P$). So $\\alpha \\in \\mathsf{Safe}(P)$. Also $P \\subseteq \\mathsf{Live}(P)$ by construction. Hence $\\alpha \\in \\mathsf{Safe}(P) \\cap \\mathsf{Live}(P)$.\n- $\\supseteq$: Take $\\alpha \\in \\mathsf{Safe}(P) \\cap \\mathsf{Live}(P)$. If $\\alpha \\notin P$, then by limit-closure there must exist a finite prefix $x$ of $\\alpha$ such that no extension $x\\beta$ is in $P$, making $x$ a bad prefix — contradicting $\\alpha \\in \\mathsf{Safe}(P)$. So $\\alpha \\in P$. $\\blacksquare$\n\n> [!NOTE]\n> **Engineering significance:** The decomposition is the *theoretical basis* for safety/liveness checking in tools like SPIN — you can verify safety and liveness properties using *different algorithmic techniques* (reachability for safety, cycle-detection / Büchi emptiness for liveness) and combine the verdicts.\n\n## 3.4 Verification Worked-Example — Mutual Exclusion as an LT Property\n\nConsider two concurrent processes $P_1$ and $P_2$ each having *critical-section* and *non-critical-section* actions: $n_1, c_1, n_2, c_2$. The **mutual-exclusion safety property** is:\n\n$$ME \\;=\\; \\{\\, \\alpha \\in \\{n_1, c_1, n_2, c_2\\}^{\\omega} \\mid c_1 \\text{ and } c_2 \\text{ never appear consecutively without a } n \\text{ between them} \\,\\}$$\n\n**Step 1 — Identify bad prefixes.**\nA bad prefix is any finite word in which $c_1 c_2$ or $c_2 c_1$ appear as adjacent letters. (These cannot be extended into $ME$.)\n\n**Step 2 — Apply the safety-liveness decomposition.**\nHere $ME$ is *purely* a safety property, because every violation is witnessed by a finite bad prefix. Hence $\\mathsf{Safe}(ME) = ME$ and $\\mathsf{Live}(ME) = \\{n_1, c_1, n_2, c_2\\}^{\\omega}$ (the trivial liveness — every finite prefix is extendable).\n\n**Step 3 — Verification via a model checker.**\nThe model checker explores the LTS of $P_1 \\parallel P_2$ and checks reachability of any state containing both processes in their critical section. If unreachable, $\\mathcal{T} \\models ME$.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Hierarchy of Behavioural Equivalences (Mermaid Block Diagram)\n\n```mermaid\ngraph TD\n    A[Strong Bisimulation\\nfinest, ~] --> B[2-Nested Simulation]\n    B --> C[Simulation]\n    C --> D[Trace Equivalence\\n~_T]\n    D --> E[Failure Equivalence]\n    E --> F[Failure-Trace Equivalence]\n    F --> G[Testing Equivalence\\ncoarsest]\n\n    H[Notes] --- A\n    H[Notes] --- D\n    H[Notes] --- G\n\n    note1[\"Finer = distinguishes more processes\\nCoarser = identifies more processes\"] -.-> A\n    note2[\"P ~ Q  implies  P ~_T Q\\nbut not vice versa\"] -.-> D\n```\n\n## 4.2 LT-Property Classification Diagram\n\n```mermaid\ngraph TD\n    P[LT Property P subset of Sigma^omega] --> S[Safety Properties\\nprefix-closed\\nbad-prefix structure]\n    P --> L[Liveness Properties\\nlimit-closed\\nevery prefix extendable]\n\n    S --> S1[Mutual Exclusion]\n    S --> S2[Type Safety]\n    S --> S3[Deadlock Freedom]\n    S --> S4[Partial Correctness]\n\n    L --> L1[Termination]\n    L --> L2[Starvation Freedom]\n    L --> L3[Response]\n    L --> L4[Total Correctness]\n\n    D[Safety-Liveness Decomposition:\\nP = Safe P intersect Live P] --- P\n    D --- S\n    D --- L\n```\n\n## 4.3 Block-Level Functional Architecture of Trace-Based Verification\n\n```mermaid\ngraph LR\n    src[System Source\\nCCS, Promela, SMV] --> mdl[Build LTS Model\\nStates, Transitions, Labels]\n    mdl --> lts[LTS T = S Act arrow s0]\n\n    lts --> c1[Compute Tr T\\nall finite action sequences]\n    lts --> c2[Check Property Containment\\nExec omega T subset P]\n\n    c1 --> ve[Verification Engine]\n    c2 --> ve\n\n    spec[Property Specification\\nLTL, Buchi, Regex] --> trn[Translate to Automaton A_P]\n    trn --> c2\n\n    ve --> res{Result}\n    res -->|Yes| ok[System satisfies P]\n    res -->|No| cex[Counter-example Trace\\nreturned to user]\n```\n\n## 4.4 Trace-Equivalence Verification Flow\n\n```mermaid\ngraph TD\n    A[Input: Two LTS T1 and T2] --> B[Enumerate Tr T1]\n    A --> C[Enumerate Tr T2]\n\n    B --> D[Set Comparison:\\nTr T1 == Tr T2 ?]\n    C --> D\n\n    D -->|Yes| E[Equivalent:\\nT1 ~_T T2]\n    D -->|No| F[Witness Trace sigma:\\nsigma in Tr T1 minus Tr T2\\nor vice versa]\n\n    E --> G[Output: EQUIVALENT]\n    F --> H[Output: NOT EQUIVALENT\\nWitness: sigma]\n```\n\n## 4.5 Safety vs. Liveness — Decision Tree for Students\n\n```mermaid\ngraph TD\n    Q[Property under verification] --> Q1{Is every violation\\nwitnessed by a finite prefix?}\n    Q1 -->|Yes| SAFE[Safety Property\\nverify by reachability]\n    Q1 -->|No| LIVE[Liveness Property\\nverify by cycle detection / Buchi emptiness]\n\n    SAFE --> SAFE1[Model Checker: check\\nany reachable state violates phi]\n    LIVE --> LIVE1[Model Checker: search\\nfor a fair cycle violating phi]\n\n    SAFE1 --> RES1[Decidable: PSPACE-complete in general]\n    LIVE1 --> RES2[Decidable: PSPACE-complete for LTL]\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. `[KTU University Exam – May 2024]`\n**Define trace equivalence for two labelled transition systems $\\mathcal{T}_1$ and $\\mathcal{T}_2$. State the formal condition using the trace sets $Tr(\\mathcal{T}_1)$ and $Tr(\\mathcal{T}_2)$.**\n\n**Model Answer (3 marks):**\n\nA *trace* of an LTS $\\mathcal{T}$ is a finite sequence of actions $\\sigma = a_1 a_2 \\ldots a_n$ such that there exist states $s_0, s_1, \\ldots, s_n$ with $s_0 \\xrightarrow{a_1} s_1 \\xrightarrow{a_2} \\cdots \\xrightarrow{a_n} s_n$. The set of all such traces is denoted $Tr(\\mathcal{T})$.\n\nTwo LTSs $\\mathcal{T}_1$ and $\\mathcal{T}_2$ over a common action alphabet are **trace equivalent**, written $\\mathcal{T}_1 \\approx_T \\mathcal{T}_2$, if and only if they have identical sets of traces:\n\n$$\\mathcal{T}_1 \\approx_T \\mathcal{T}_2 \\quad\\iff\\quad Tr(\\mathcal{T}_1) \\;=\\; Tr(\\mathcal{T}_2)$$\n\n**Mark Split:** Definition of trace (1) + formal set condition (1) + conclusion (1).\n\n---\n\n### Q2. `[KTU University Exam – Dec 2023]`\n**Distinguish between safety and liveness properties. Give one example of each.**\n\n**Model Answer (3 marks):**\n\n- A **safety property** states that \"nothing bad ever happens.\" Every violation is witnessed by a finite *bad prefix*. Formally, it is a prefix-closed set of traces.  \n  *Example:* Mutual exclusion — two processes are never simultaneously in their critical sections.\n\n- A **liveness property** states that \"something good eventually happens.\" Every finite sequence of actions can be extended so that the property holds. Formally, it is limit-closed.  \n  *Example:* Termination — every execution eventually reaches a halting state.\n\n**Mark Split:** Safety definition + example (1.5) + Liveness definition + example (1.5).\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks, Module Internal Choice)\n\n### Question A `[KTU University Exam – Dec 2024]`\n\n**(a)** Explain the concept of trace equivalence between two processes $P$ and $Q$. Show that trace equivalence is a congruence with respect to the choice operator $+$.  \n**(7 marks)**\n\n**(b)** Consider the CCS processes:\n$$P \\;=\\; a.\\,(b.\\mathbf{0} + c.\\mathbf{0}) \\quad\\quad Q \\;=\\; a.\\,b.\\mathbf{0} + a.\\,c.\\mathbf{0}$$\nCompute $Tr(P)$ and $Tr(Q)$. Are $P$ and $Q$ trace equivalent? Are they strongly bisimilar? Justify your answer with the bisimulation game argument.  \n**(7 marks)**\n\n#### Model Solution\n\n**(a) Trace equivalence and congruence under choice**\n\nA *trace* of a process $P$ is any finite sequence of actions $a_1 a_2 \\ldots a_n$ that $P$ can perform starting from its initial state, written $P \\xrightarrow{a_1 a_2 \\cdots a_n} P'$. The set $Tr(P)$ collects all such sequences, including $\\varepsilon$.\n\n**Trace equivalence:**\n$$P \\approx_T Q \\quad\\iff\\quad Tr(P) \\;=\\; Tr(Q)$$\n\n**Congruence proof (sketch):** We show that if $P \\approx_T Q$ then for any $R$:\n$$P + R \\;\\;\\approx_T\\;\\; Q + R$$\n\nWe have $Tr(P + R) = Tr(P) \\cup Tr(R)$ because the choice operator selects non-deterministically between the two operands, so the traces of $P + R$ are exactly the union of the traces of $P$ and $R$. Similarly $Tr(Q + R) = Tr(Q) \\cup Tr(R)$. Therefore:\n\n$$Tr(P + R) = Tr(P) \\cup Tr(R) = Tr(Q) \\cup Tr(R) = Tr(Q + R)$$\n\nHence $P + R \\approx_T Q + R$, confirming congruence under $+$. $\\blacksquare$\n\n**(b) Trace computation and bisimulation check**\n\n**Step 1 — Compute $Tr(P)$.**\n\nThe process $P = a.\\,(b.\\mathbf{0} + c.\\mathbf{0})$ first performs $a$ and then behaves as $b.\\mathbf{0} + c.\\mathbf{0}$, which can do $b$ or $c$ (and then terminate). Hence:\n\n$$Tr(P) \\;=\\; \\{\\, \\varepsilon,\\ a,\\ a\\,b,\\ a\\,c \\,\\}$$\n\n**Step 2 — Compute $Tr(Q)$.**\n\nThe process $Q = a.b.\\mathbf{0} + a.c.\\mathbf{0}$ has two summands, each starting with $a$. After $a$, the residual is either $b.\\mathbf{0}$ (in the first branch) or $c.\\mathbf{0}$ (in the second). The set of possible second actions is therefore $\\{b, c\\}$ overall, yielding:\n\n$$Tr(Q) \\;=\\; \\{\\, \\varepsilon,\\ a,\\ a\\,b,\\ a\\,c \\,\\}$$\n\n**Step 3 — Trace equivalence verdict.**\n\n$$Tr(P) \\;=\\; Tr(Q) \\;\\Longrightarrow\\; P \\approx_T Q \\quad\\text{(TRACE EQUIVALENT)} \\;\\checkmark$$\n\n**Step 4 — Bisimulation game.**\n\nSuppose, for contradiction, that $P \\sim Q$ via a relation $\\mathcal{R}$. Then $(P, Q) \\in \\mathcal{R}$.\n\n- From $P$, the *only* initial action is $a$, leading to state $P' = b.\\mathbf{0} + c.\\mathbf{0}$.\n- From $Q$, the only initial action is also $a$, but the residual state is *either* $Q'_1 = b.\\mathbf{0}$ *or* $Q'_2 = c.\\mathbf{0}$ (depending on which summand the non-deterministic choice selected).\n\nBy the bisimulation game, $Q$ must be able to *match* every transition of $P$. The state $P' = b.\\mathbf{0} + c.\\mathbf{0}$ has a $b$-transition **and** a $c$-transition. But neither $Q'_1$ nor $Q'_2$ alone has both transitions. Therefore no relation can pair $P'$ with a single $Q'_i$, and:\n\n$$P \\not\\sim Q \\quad\\text{(NOT BISIMILAR)}$$\n\n**Step 5 — Conclusion.**\n\nThe pair $(P, Q)$ is the *canonical KTU counter-example* showing that **trace equivalence is strictly coarser than strong bisimulation**:\n\n$$P \\approx_T Q \\quad\\text{but}\\quad P \\not\\sim Q$$\n\n---\n\n### Question B `[KTU University Exam – May 2024]` (Internal Choice to Question A)\n\n**(a)** What is a Linear-Time (LT) property? State the formal definition as a set-theoretic characterisation. Explain, with a diagram, the safety–liveness decomposition theorem of Alper and Sistla.  \n**(7 marks)**\n\n**(b)** Express each of the following requirements as an LTL formula and classify each as safety or liveness:\n  (i) The system never grants two requests simultaneously.  \n  (ii) Every request is eventually acknowledged.  \n  (iii) After a button is pressed, the system eventually resets.  \n**(7 marks)**\n\n#### Model Solution\n\n**(a) LT properties and the Alper–Sistla theorem**\n\n**Definition.** Let $\\Sigma$ be a non-empty finite set of action labels. A **linear-time property** over $\\Sigma$ is a set $P \\subseteq \\Sigma^{\\omega}$ of infinite words over $\\Sigma$. A system $\\mathcal{T}$ with alphabet $\\Sigma$ satisfies $P$ iff its set of infinite executions is contained in $P$:\n\n$$\\mathcal{T} \\models P \\quad\\iff\\quad Exec^{\\omega}(\\mathcal{T}) \\subseteq P$$\n\n**Two canonical subclasses.**\n\n- A *safety property* is one in which every violation is witnessed by a finite \"bad prefix\" $x$ — no infinite word in $P$ has $x$ as a prefix. Equivalently, $P$ is *prefix-closed* under taking limits.\n- A *liveness property* is one in which every finite word can be extended into $P$ — i.e., no finite prefix is \"bad.\"\n\n**Alper–Sistla theorem.** Every LT property $P$ can be *uniquely* decomposed as:\n\n$$P \\;=\\; \\mathsf{Safe}(P) \\;\\cap\\; \\mathsf{Live}(P)$$\n\nwhere\n\n$$\\mathsf{Safe}(P) = \\{\\alpha \\in \\Sigma^{\\omega} \\mid \\text{no finite prefix of }\\alpha\\text{ is a bad prefix of }P\\}$$\n\n$$\\mathsf{Live}(P) = \\text{smallest liveness property containing }P$$\n\n(See Section 3.3 above for the rigorous proof sketch; the Mermaid diagram in Section 4.2 illustrates the classification.)\n\n**(b) LTL encodings**\n\nWe let $r$ = \"request granted\", $q$ = \"request issued\", $a$ = \"acknowledge\", $b$ = \"button pressed\", $s$ = \"system reset.\"\n\n**(i)** \"The system never grants two requests simultaneously.\"  \nThis is a safety property (any violation is a finite bad-prefix event). A standard LTL encoding: $\\varphi_1 = \\Box\\, \\neg(r \\wedge r) = \\Box\\, \\neg r$, or more accurately, if $r_i$ denotes \"request $i$ granted\":\n\n$$\\varphi_1 \\;=\\; \\Box\\, \\neg(r_1 \\wedge r_2)$$\n\n**Class: SAFETY** (prefix-closed; bad prefix is any word containing $r_1 r_2$ consecutively).\n\n**(ii)** \"Every request is eventually acknowledged.\"  \nThis is a liveness property — every finite prefix can be extended into an acknowledgement. The LTL formula is:\n\n$$\\varphi_2 \\;=\\; \\Box\\,(q \\;\\Rightarrow\\; \\Diamond\\, a) \\quad \\equiv \\quad \\Box\\, q \\;\\mathcal{U}\\; a$$\n\n**Class: LIVENESS** (a request $q$ is *followed*, not preceded, by an acknowledgement $a$ — a typical response property).\n\n**(iii)** \"After a button is pressed, the system eventually resets.\"  \nAnother response / liveness pattern. The LTL formula is:\n\n$$\\varphi_3 \\;=\\; \\Box\\,(b \\;\\Rightarrow\\; \\Diamond\\, s)$$\n\n**Class: LIVENESS** (the obligation is \"eventually\", not \"never\").\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfalls — read carefully!**\n>\n> 1. **Confusing direction of the equivalence hierarchy.** Several students wrote \"trace equivalence is finer than bisimulation\" — this is the **opposite** of the truth. Bisimulation is finer (distinguishes more processes); trace equivalence is coarser.\n>\n> 2. **Forgetting the empty trace $\\varepsilon$.** The set $Tr(P)$ *always* contains $\\varepsilon$. Omitting it costs a mark in \"compute the trace set\" questions.\n>\n> 3. **Misclassifying $\\Diamond$ properties as safety.** The $\\Diamond$ (eventually) operator makes a formula liveness, **not** safety. Only $\\Box$ (always) and $\\Box$ over negations are typically safety.\n>\n> 4. **In trace-equality questions, students often write the wrong union/difference.** Remember: $Tr(P+Q) = Tr(P) \\cup Tr(Q)$, $Tr(P \\parallel Q) = $ interleavings of $Tr(P)$ and $Tr(Q)$, $Tr(P \\setminus L) = Tr(P)$ with actions in $L$ hidden.\n>\n> 5. **Conflating *completed traces* with *maximal traces*.** A maximal trace ends in deadlock; a completed trace ends at a $\\surd$ state. The two are not identical when a process can *only* deadlock without terminating.\n>\n> 6. **In LTL questions, forgetting the outer $\\Box$.** Writing $q \\Rightarrow \\Diamond a$ is *not* equivalent to $\\Box(q \\Rightarrow \\Diamond a)$. The first is satisfied by traces that *never* see a $q$, the second is not.\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid-Revision Checklist — Trace Equivalence and LT Properties**\n\n- [ ] A **trace** is a *finite* sequence of actions reachable from the initial state. The empty trace $\\varepsilon$ is always present.\n- [ ] **Trace equivalence** is defined as $P \\approx_T Q \\iff Tr(P) = Tr(Q)$. It is a congruence for all standard CCS operators.\n- [ ] Trace equivalence is **strictly coarser** than strong bisimulation — the canonical counter-example is $a.(b+c)$ vs. $a.b + a.c$.\n- [ ] A property $P$ over a system $\\mathcal{T}$ is a set of behaviours; **verification** asks $\\mathcal{T} \\models P$, i.e. $Exec^{\\omega}(\\mathcal{T}) \\subseteq P$.\n- [ ] An **LT property** is a set $P \\subseteq \\Sigma^{\\omega}$. It is checked *along each individual execution*, hence \"linear-time\".\n- [ ] **Safety properties** are *prefix-closed*: every violation has a finite bad prefix. Examples: mutual exclusion, type safety, deadlock-freedom, partial correctness.\n- [ ] **Liveness properties** are *limit-closed*: every finite word is extendable. Examples: termination, starvation-freedom, response, total correctness.\n- [ ] **Alper–Sistla theorem**: every LT property uniquely decomposes as $P = \\mathsf{Safe}(P) \\cap \\mathsf{Live}(P)$.\n- [ ] **LTL semantics for common patterns**:\n    - $\\Box \\varphi$ — *always* $\\varphi$ → **safety**\n    - $\\Diamond \\varphi$ — *eventually* $\\varphi$ → **liveness**\n    - $\\Box(p \\Rightarrow \\Diamond q)$ — *every $p$ is eventually followed by $q$* → **liveness (response)**\n    - $\\Diamond \\Box \\varphi$ — *eventually $\\varphi$ holds forever* → **liveness**\n    - $\\Box \\Diamond \\varphi$ — *$\\varphi$ holds infinitely often* → **liveness**\n- [ ] The set of traces of $P + Q$ is $Tr(P) \\cup Tr(Q)$ — a frequent KTU board question.\n- [ ] For trace-equivalence questions, *always enumerate* $Tr(P)$ and $Tr(Q)$ *inductively* and check prefix-closure.\n- [ ] The four standard *kinds* of LT properties to remember: **safety, liveness, deadlock-freedom (safety), invariant (safety)**.\n- [ ] Büchi automata, LTL, and regular expressions are the *three* standard ways to specify LT properties.\n- [ ] Tools: SPIN (Promela), NuSMV (CTL/LTL), CADP (LNT/BCG) all implement LT-property verification.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t21",
          "title": "Safety Properties and Invariants - Invariants",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Safety Properties and Invariants - Invariants\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of Invariants\n\nIn the context of automated verification and model checking, an **invariant** is a state predicate (a Boolean-valued function over program/system states) that is asserted to evaluate to `true` at **every reachable state** of the system under analysis.\n\nLet $M = (S, S_0, R, L)$ be a Kripke structure (the standard semantic model in KTU-referenced model checking literature), where:\n- $S$ is a non-empty finite set of states\n- $S_0 \\subseteq S$ is the set of initial states\n- $R \\subseteq S \\times S$ is the total transition relation\n- $L: S \\rightarrow 2^{AP}$ is the state-labeling function over atomic propositions $AP$\n\nThen a state formula $P$ (a propositional logic formula over $AP$) is an **invariant** of $M$ if and only if:\n\n$$M \\models \\Box P \\quad \\iff \\quad \\forall s \\in \\text{Reach}(M) : s \\models P$$\n\nwhere $\\text{Reach}(M)$ denotes the set of all states reachable from $S_0$ via zero or more $R$-transitions, and $\\Box$ is the LTL/CTL \"globally\" temporal operator.\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight:** Invariants are the **simplest, most fundamental, and most frequently encountered subclass of safety properties**. Every invariant is a safety property, but not every safety property is an invariant (e.g., \"every request is eventually followed by a response with no specific order restriction\" is a safety liveness mixture).\n\n## 1.2 Intuition via Real-World Analogy\n\n> [!NOTE]\n> **Conceptual Analogy — The Bank Vault Thermometer**\n>\n> Imagine a bank's vault with a thermometer that **must always read between 15°C and 25°C** to preserve its contents. The statement *\"the temperature is in [15°C, 25°C]\"* is an **invariant** of the vault's climate-control system. The cooling engineers do not care *when* the temperature enters or leaves that range along an infinite timeline — they only care that **in every observable state, the safety condition holds**.\n>\n> - If the thermometer ever reads 30°C, the invariant is *violated* (a **bad state** has been reached).\n> - The check is performed by a safety monitor (the *verifier*) on the infinite sequence of states produced by the system.\n> - This mirrors exactly what an automated verifier does: it exhaustively checks that a predicate holds over the entire reachable state space, never relaxing its scrutiny to \"eventual\" or \"likely\" outcomes.\n\nIn software terms, common invariants include:\n- \"*`x ≥ 0`* throughout execution\" (a memory-safety invariant)\n- \"*`lock_count ≥ 0`* at every point\" (a deadlock-freedom invariant)\n- \"*`head ≠ null ⇒ head.next ≠ null`*\" (a list well-formedness invariant)\n- \"*mutual exclusion: at most one process in the critical section*\" (a concurrent-programming invariant)\n\n## 1.3 Why Invariants Matter in KTU-Style Automated Verification\n\n- **They are decidable** for finite-state systems via standard fixpoint computation.\n- **They underpin proof-based verification** (Hoare logic, deductive verification), where invariants are the cornerstone of partial correctness.\n- **They are the natural target of dataflow analysis** (a compiler-level invariant-generation technique).\n- **They provide compositional reasoning**: a system built from components preserves the conjunction of its components' invariants (under suitable composition rules).\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Two Pillars: Inductive vs. Non-Inductive Invariants\n\nA predicate $I$ is an **invariant** of a transition system $M$ when it holds at every reachable state. The proof obligations depend on *how* $I$ is structured.\n\n### 2.1.1 Inductive Invariant (Self-Consistent)\n\n$I$ is **inductive** (or a *fixpoint invariant*) if it satisfies the classical two-clause *inductive invariant proof rule*:\n\n$$\n\\begin{aligned}\n\\textbf{(Initiation)} \\quad & \\forall s \\in S_0 : I(s) \\\\\n\\textbf{(Consecution)} \\quad & \\forall s, s' \\in S : \\big(I(s) \\land R(s, s') \\big) \\Rightarrow I(s')\n\\end{aligned}\n$$\n\nIf both clauses hold, then by **Noetherian induction on the length of execution paths**, $I$ is provably true at every reachable state. This is the standard KTU board-exam formulation.\n\n> [!TIP]\n> **Why \"inductive\"?** The name arises from the fact that $I$ acts as an *inductive hypothesis*: if it holds at step $k$, the consecution clause guarantees it holds at step $k+1$, and initiation guarantees it holds at step $0$. The two clauses are exactly the base case and inductive step of a mathematical induction on path length.\n\n### 2.1.2 Non-Inductive Invariant\n\nA predicate $J$ may be a valid invariant (true at every reachable state) without being inductive. Such a $J$ requires a **stronger auxiliary inductive invariant** $I$ to prove it:\n\n$$I \\Rightarrow J, \\quad I \\text{ is inductive}, \\quad \\Rightarrow J \\text{ is an invariant}$$\n\nThe art of **invariant strengthening / weakening** (the *abstraction–refinement loop* in CEGAR-style model checking) revolves around finding such an $I$.\n\n## 2.2 Safety Property Characterization\n\nA **safety property** $P_{safe}$ is formally defined as a property whose violation can be witnessed by a **finite bad prefix** (Alpern & Schneider, 1987; Büchi & Landweber):\n\n> A property $\\mathcal{P}$ is a safety property iff for every infinite execution $\\pi = s_0 s_1 s_2 \\ldots$ of the system, $\\pi \\not\\models \\mathcal{P}$ implies there exists a finite prefix $\\hat{\\pi} = s_0 s_1 \\ldots s_k$ such that every infinite extension of $\\hat{\\pi}$ also violates $\\mathcal{P}$. The prefix $\\hat{\\pi}$ is called a **minimal bad prefix**.\n\n**The Fundamental Theorem of Safety Verification (Büchi–Landweber):**\n\n> [!IMPORTANT]\n> A property is a safety property **if and only if** it is characterized by a regular set of bad prefixes recognized by a deterministic finite automaton (DFA) — the **safety automaton** $A_{\\text{safety}}$.\n\n**Invariants ↔ Safety Properties with Singleton Bad-Prefix Automata:**\n\nAn invariant $\\Box P$ is the special case of a safety property in which the \"bad prefix\" is any state where $\\neg P$ holds. The safety automaton degenerates to a single sink state labelled with $\\neg P$.\n\n## 2.3 Computing Invariants — The Three Classical Algorithms\n\n### 2.3.1 Forward Reachability (Naïve Enumeration)\n\nCompute the least fixpoint of the predecessor/post-state transformer:\n\n$$\\text{Reach}(M) = \\mu X.\\, S_0 \\cup \\text{Post}(X)$$\n\nThen check $\\forall s \\in \\text{Reach}(M) : P(s)$.\n\n### 2.3.2 Dataflow Analysis Interpretation\n\nInterpret invariants as solutions to a monotone dataflow equation system over a lattice $(\\mathcal{L}, \\sqsubseteq, \\sqcup, \\sqcap, \\bot, \\top)$. Standard formulations:\n\n$$\n\\begin{aligned}\n\\textbf{Reaching-Definitions:} \\quad & \\text{out}[B] = \\text{gen}[B] \\cup (\\text{in}[B] - \\text{kill}[B]) \\\\\n\\textbf{Available-Expressions:} \\quad & \\text{out}[B] = \\bigcap_{P \\in \\text{pred}(B)} \\text{out}[P] \\quad \\text{(forward)} \\\\\n\\textbf{Live-Variables:} \\quad & \\text{in}[B] = \\text{use}[B] \\cup (\\text{out}[B] - \\text{def}[B]) \\quad \\text{(backward)}\n\\end{aligned}\n$$\n\nEach yields an **invariant fact** at every program point.\n\n### 2.3.3 Abstract Interpretation (Cousot & Cousot, 1977)\n\nA Galois connection $(\\alpha, \\gamma)$ between the concrete state lattice $\\langle \\wp(S), \\subseteq \\rangle$ and an abstract lattice $\\langle \\mathcal{A}, \\sqsubseteq \\rangle$ induces a **sound abstract transformer** $F^{\\#}$ whose least fixpoint is a **sound over-approximation** of the concrete invariant set:\n\n$$\\alpha(\\text{lfp}\\, F) \\sqsubseteq \\text{lfp}\\, F^{\\#}$$\n\nWidening operators $(\\nabla)$ guarantee termination in finite time.\n\n## 2.4 KTU High-Yield Formula Sheet\n\n> [!NOTE]\n> **Examination Cheat Sheet — All Invariant-Related Formulas at a Glance**\n\n| Concept | Formal Statement | Domain of Use |\n|---|---|---|\n| Invariant definition | $\\forall s \\in \\text{Reach}(M) : s \\models P$ | Model checking |\n| Inductive invariant (Init) | $\\forall s \\in S_0 : I(s)$ | Deductive proof |\n| Inductive invariant (Cons) | $\\forall s, s' : I(s) \\land R(s, s') \\Rightarrow I(s')$ | Deductive proof |\n| LTL global form | $M \\models \\Box P$ | LTL model checking |\n| CTL global form | $M, s_0 \\models AG\\, P$ | CTL model checking |\n| Safety property | $\\forall \\pi \\not\\models \\mathcal{P} : \\exists k : \\text{all extensions of } s_0 \\ldots s_k \\text{ violate } \\mathcal{P}$ | Theory of properties |\n| Bad prefix | A finite word $w$ s.t. every infinite extension $\\overline{w}$ violates $\\mathcal{P}$ | Büchi–Landweber theorem |\n| Fixpoint of Post | $\\text{Reach}(M) = \\mu X.\\, S_0 \\cup \\text{Post}(X)$ | Reachability analysis |\n| Abstract soundness | $\\alpha(\\text{lfp}\\, F) \\sqsubseteq \\text{lfp}\\, F^{\\#}$ | Abstract interpretation |\n| Hoare triple (partial) | $\\{I\\}\\,\\text{stmt}\\,\\{I\\}$ | Floyd–Hoare logic |\n| Hoare triple (total) | $[I]\\,\\text{stmt}\\,[I]$ | Total correctness |\n\n## 2.5 Real-World Engineering Utility\n\n- **Compiler optimization:** Loop-invariant code motion depends on inferring that an expression evaluates to the same value at every iteration.\n- **Static analysis tools:** Astrée, Polyspace, CBMC all report invariant facts at every program point to prove absence of runtime errors.\n- **Hardware verification:** In Intel, AMD, and ARM processor verification, invariants (\"the pipeline is never stalled for more than 5 cycles\", \"no two loads retire out-of-order\") are the dominant property class verified.\n- **Cyber-physical systems:** Invariant monitoring (Simplex architecture, runtime verification with STL invariants) guards safety-critical controllers in avionics and autonomous vehicles.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: Soundness of the Inductive Invariant Proof Rule\n\n> **Claim:** If $I$ is inductive in $M = (S, S_0, R, L)$, then $\\forall s \\in \\text{Reach}(M) : I(s)$.\n\n### Step 1 — Define reachable states formally\n\n$$\n\\begin{aligned}\n\\text{Reach}(M) \\;=\\; \\big\\{\\, s \\in S \\;:\\; \\exists\\, n \\geq 0, \\exists\\, s_0, s_1, \\ldots, s_n = s \\text{ with } s_0 \\in S_0 \\text{ and } \\forall\\, i < n : (s_i, s_{i+1}) \\in R \\,\\big\\}\n\\end{aligned}\n$$\n\nThis is the union of all states reachable in exactly $k$ steps for some $k \\geq 0$.\n\n### Step 2 — Set up the induction\n\nDefine the bounded-reachability predicate $R_k(s)$: \"state $s$ is reachable from some initial state in at most $k$ steps.\" Formally:\n\n$$\n\\begin{aligned}\nR_0(s) &\\;\\iff\\; s \\in S_0 \\\\\nR_{k+1}(s) &\\;\\iff\\; R_k(s) \\;\\lor\\; \\exists\\, s' : \\big( R_k(s') \\land (s', s) \\in R \\big)\n\\end{aligned}\n$$\n\nBy definition, $s \\in \\text{Reach}(M) \\iff \\exists\\, k \\geq 0 : R_k(s)$.\n\n### Step 3 — Base case (k = 0)\n\nThe **initiation clause** of the inductive invariant is:\n\n$$\\forall s \\in S_0 : I(s)$$\n\nThis is exactly $\\forall s : R_0(s) \\Rightarrow I(s)$. ✓\n\n### Step 4 — Inductive step (k → k+1)\n\nAssume $\\forall s : R_k(s) \\Rightarrow I(s)$ (induction hypothesis). Take any $s$ with $R_{k+1}(s)$. Then either $R_k(s)$, in which case $I(s)$ follows directly, or there exists $s'$ with $R_k(s')$ and $(s', s) \\in R$. By IH, $I(s')$. By the **consecution clause**:\n\n$$\\forall\\, s, s' : I(s') \\land (s', s) \\in R \\Rightarrow I(s)$$\n\nwe conclude $I(s)$. ✓\n\n### Step 5 — Quantifier extension\n\nSince for each $k$, $R_k \\Rightarrow I$, and $\\text{Reach}(M) = \\bigcup_{k \\geq 0} R_k$, we obtain:\n\n$$\\forall s \\in \\text{Reach}(M) : I(s) \\qquad \\blacksquare$$\n\n## 3.2 Derivation: Reachability via Kleene Iteration (Model-Checking Algorithm)\n\n> **Claim:** The sequence $X_0, X_1, X_2, \\ldots$ defined by $X_0 = S_0$ and $X_{i+1} = X_i \\cup \\text{Post}(X_i)$ converges in $|S|$ iterations to $\\text{Reach}(M)$.\n\n$$\n\\begin{aligned}\nX_0 &= S_0 \\\\\nX_1 &= S_0 \\cup \\text{Post}(S_0) \\\\\nX_2 &= X_1 \\cup \\text{Post}(X_1) \\\\\n\\vdots \\quad &= \\quad \\vdots \\\\\nX_k &= \\bigcup_{i=0}^{k} \\text{Post}^i(S_0) \\quad \\text{(Post}^i \\text{ denotes $i$-fold composition)}\n\\end{aligned}\n$$\n\nSince $S$ is finite (size $N = \\vert S \\vert$) and the sequence is monotonically increasing ($X_i \\subseteq X_{i+1}$), the sequence stabilizes by iteration $N$ at the latest, yielding $X_N = \\text{Reach}(M)$. Termination is guaranteed; the algorithm runs in $O(N \\cdot \\vert R \\vert)$ time using BFS/DFS.\n\n## 3.3 Analytical Matrix: Three Methods of Establishing an Invariant\n\n| Method | Strength | Weakness | When to Use |\n|---|---|---|---|\n| **Inductive proof** | Mechanically checkable; gives strongest guarantees | Requires invention of $I$ | Small to medium systems with good intuition |\n| **Reachability computation** | Fully automatic; decidable for finite state | State-space explosion | Finite-state hardware protocols, small software |\n| **Abstract interpretation** | Scalable; works on infinite-state programs | May produce false positives (over-approx.) | Large C/C++ codebases, embedded controllers |\n| **Predicate abstraction + CEGAR** | Automatic refinement reduces false alarms | Refinement loops may not terminate | Boolean programs, SLAM/blast-style tools |\n| **Craig interpolation** | Powerful automatic invariant generation | Limited to first-order theories | Bounded model checking + BMC unrolled proofs |\n\n## 3.4 Worked Example — Mutual Exclusion Invariant\n\n> **System:** Two processes $P_1, P_2$, each with a local flag $f_1, f_2 \\in \\{0, 1\\}$ and a critical section bit $c_1, c_2 \\in \\{0, 1\\}$.\n> **Claimed Invariant:** $I \\equiv \\neg(c_1 \\land c_2)$ (mutual exclusion: never both in critical section).\n\n**Initiation check:** Initially $c_1 = c_2 = 0$, so $\\neg(0 \\land 0) = \\text{true}$. ✓\n\n**Consecution check:** Every transition either (a) changes $f_i$ without altering $c_j$, or (b) sets $c_i = 1$ only if $f_j = 0$ (the other process is not requesting entry). In case (a), $I$ holds trivially. In case (b), the precondition $f_j = 0$ plus the protocol's invariant $f_j = 0 \\Rightarrow c_j = 0$ (an auxiliary invariant) ensures $c_j = 0$, hence $c_1 \\land c_2 = \\text{false}$. ✓\n\nTherefore $I$ is an inductive invariant of the mutual-exclusion protocol, and mutual exclusion is verified for all reachable states. $\\blacksquare$\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Invariant Proof Architecture (Block-Level Functional Flow)\n\n```mermaid\nflowchart TD\n    A[System Model M = S, S0, R, L] --> B[Identify Candidate Invariant I]\n    B --> C{I is Inductive?}\n    C -- Yes --> D[Apply Initiation Clause]\n    C -- No --> E[Invariant Strengthening: Find I-prime with I-prime implies I]\n    D --> F[Apply Consecution Clause]\n    E --> F\n    F --> G{Both Clauses Hold?}\n    G -- Yes --> H[By Path-Length Induction: I holds in Reach M]\n    G -- No --> I[Refine I or Use Abstract Interpretation]\n    I --> B\n    H --> J[Output: M models Globally I]\n    J --> K[Result: Invariant Verified for all Reachable States]\n```\n\n## 4.2 Invariant Generation & Verification Lifecycle\n\n```mermaid\nflowchart LR\n    S0[Source Code or Transition System] --> S1[Parse to Control Flow Graph]\n    S1 --> S2[Choose Abstract Domain: Intervals, Octagons, Polyhedra]\n    S2 --> S3[Compute Abstract Transformer F-sharp]\n    S3 --> S4[Solve Fixpoint Equation with Widening]\n    S4 --> S5[Concretize: gamma of Abstract Fixpoint]\n    S5 --> S6[Candidate Invariant I]\n    S6 --> S7[Check I is Inductive]\n    S7 -->|Yes| S8[Verified Invariant: M models Globally I]\n    S7 -->|No| S9[Refine Domain / Add Predicates]\n    S9 --> S2\n```\n\n## 4.3 Relationship Between Invariants and the Safety Property Hierarchy\n\n```mermaid\nflowchart TD\n    P[Properties: omega-languages over infinite executions] --> S[Safety Properties: bad prefix exists]\n    P --> L[Liveness Properties: every finite prefix is extendable to a good execution]\n    S --> I[Invariants: bad state in the safety automaton]\n    S --> GP[General Safety: complex bad-prefix regular language]\n    I --> I1[State Invariant: Globally P]\n    I --> I2[Transition Invariant: For all edges, condition holds]\n    I1 --> I3[Inductive Invariant: Initiation plus Consecution]\n    I1 --> I4[Non-Inductive Invariant: requires auxiliary I]\n```\n\n## 4.4 Counter-Example Guided Abstraction Refinement (CEGAR) Loop\n\n```mermaid\nflowchart TD\n    C1[Concrete System M] --> C2[Construct Abstract Model M-hat]\n    C2 --> C3[Model Check: M-hat models Globally I]\n    C3 -->|Yes| C4[Sound: M models Globally I: DONE]\n    C3 -->|No| C5[Spurious Counterexample: path in M-hat not in M]\n    C5 --> C6[Analyze Why Path is Spurious]\n    C6 --> C7[Refine Abstraction: Add Predicates]\n    C7 --> C2\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short-Answer Questions (3 Marks Each)\n\n### Question 1 [KTU University Exam — May 2024]\n**Define an invariant in the context of model checking. How is it formally expressed using temporal logic?**\n\n**Model Answer:**\n\nAn *invariant* is a property asserting that a state predicate $P$ holds at **every reachable state** of a transition system $M$. Formally, in LTL: $M \\models \\Box P$, meaning \"globally, at every point along every execution path, $P$ is true.\" In CTL: $M, s_0 \\models AG\\, P$ (along all paths, globally $P$). The set of states satisfying $P$ forms a **safety set** — once an execution leaves it, the property is permanently violated. Invariants are the simplest, most decidable subclass of safety properties and are central to verification of hardware, protocols, and safety-critical software. **(3 marks)**\n\n### Question 2 [KTU University Exam — Dec 2023]\n**What is an inductive invariant? State the two clauses required to prove a predicate $I$ is an inductive invariant of a transition system.**\n\n**Model Answer:**\n\nAn inductive invariant $I$ is a state predicate that is preserved by system execution. The two required clauses are:\n\n1. **Initiation (Base case):** $I$ holds in every initial state, i.e., $\\forall s \\in S_0 : I(s)$.\n2. **Consecution (Inductive step):** $I$ is preserved by every transition, i.e., $\\forall s, s' \\in S : I(s) \\land R(s, s') \\Rightarrow I(s')$.\n\nTogether, by induction on path length, these guarantee that $I$ holds at **every reachable state**, establishing $I$ as an invariant. **(3 marks)**\n\n## 5.2 Part B — Long-Answer Questions (14 Marks Each, Internal Choice)\n\n### Question A [14 Marks] [KTU University Exam — July 2024]\n\n**(a) [7 marks]** *Define safety properties formally. State and explain the Büchi–Landweber characterization theorem for safety properties. Show that every invariant is a safety property.*\n\n**(b) [7 marks]** *Consider the transition system $M$ with states $\\{s_0, s_1, s_2, s_3\\}$, initial state $\\{s_0\\}$, and transitions $s_0 \\to s_1$, $s_1 \\to s_1$, $s_1 \\to s_2$, $s_2 \\to s_3$, $s_3 \\to s_3$. Prove that $P \\equiv (s \\neq s_3)$ is an invariant of $M$.*\n\n#### Model Solution — Part (a)\n\n**Formal definition of safety property:** A property $\\mathcal{P} \\subseteq \\Sigma^{\\omega}$ over infinite words is a **safety property** if for every infinite word $w \\notin \\mathcal{P}$, there exists a finite prefix $\\hat{w}$ of $w$ such that **no** infinite extension of $\\hat{w}$ belongs to $\\mathcal{P}$. The prefix $\\hat{w}$ is called a *minimal bad prefix*.\n\n**Büchi–Landweber theorem (1969):** *A property $\\mathcal{P}$ is a safety property if and only if the set of its minimal bad prefixes is a regular language recognizable by a deterministic finite automaton (DFA) called the safety automaton $A_{\\mathcal{P}}$.*\n\n**Proof that invariants are safety properties:** Let $I = \\Box P$ be an invariant. The bad prefixes are exactly those finite words whose last state violates $P$. The corresponding safety automaton has two states — a \"good\" state (accepting) and a \"bad\" sink state (entered on $\\neg P$, rejecting thereafter). Since this automaton is a DFA recognizing a regular language, the invariant is a safety property by Büchi–Landweber. $\\blacksquare$ **(7 marks)**\n\n#### Model Solution — Part (b)\n\n**Given:** $M = (\\{s_0, s_1, s_2, s_3\\}, \\{s_0\\}, R, L)$ with $R = \\{(s_0, s_1), (s_1, s_1), (s_1, s_2), (s_2, s_3), (s_3, s_3)\\}$. Predicate $P(s) \\equiv (s \\neq s_3)$.\n\n**Method 1 — Inductive proof:**\n\n*Initiation:* $s_0 \\in S_0$ and $s_0 \\neq s_3$, so $P(s_0) = \\text{true}$. ✓\n\n*Consecution:* For each transition $(s, s') \\in R$, check $P(s) \\Rightarrow P(s')$:\n\n$$\n\\begin{aligned}\n(s_0, s_1) &: s_0 \\neq s_3, \\; s_1 \\neq s_3 \\;\\; \\checkmark \\\\\n(s_1, s_1) &: s_1 \\neq s_3, \\; s_1 \\neq s_3 \\;\\; \\checkmark \\\\\n(s_1, s_2) &: s_1 \\neq s_3, \\; s_2 \\neq s_3 \\;\\; \\checkmark \\\\\n(s_2, s_3) &: s_2 \\neq s_3, \\; s_3 = s_3 \\;\\; \\Rightarrow \\;\\; P(s_2) = \\text{true} \\text{ but } P(s_3) = \\text{false} \\;\\; \\mathbf{\\times}\n\\end{aligned}\n$$\n\n⚠️ **Consecution fails at $(s_2, s_3)$!** This means $P$ is **not inductive** as stated. However, $P$ may still be an invariant if $s_3$ is *not reachable* from $s_0$.\n\n**Method 2 — Reachability proof:**\n\nCompute the reachability fixpoint:\n- $X_0 = \\{s_0\\}$\n- $X_1 = \\{s_0, s_1\\}$ (via $s_0 \\to s_1$)\n- $X_2 = X_1 \\cup \\{s_2\\}$ (via $s_1 \\to s_2$)\n- $X_3 = X_2 \\cup \\{s_3\\}$ (via $s_2 \\to s_3$)\n- $X_4 = X_3$ (stable)\n\nThus $\\text{Reach}(M) = \\{s_0, s_1, s_2, s_3\\}$.\n\n**Verification of $P$:** Check $P(s)$ for each $s \\in \\text{Reach}(M)$: $P(s_3) = \\text{false}$. Therefore $\\exists s \\in \\text{Reach}(M) : \\neg P(s)$, and **$P$ is NOT an invariant of $M$**.\n\n**Correction:** The correct invariant is $P'(s) \\equiv s \\notin \\{s_3\\}$ — wait, the same predicate. Re-examination: with transition $s_2 \\to s_3$ present, $s_3$ **is** reachable, so $P$ fails. If we **remove** the transition $s_2 \\to s_3$, then $\\text{Reach}(M) = \\{s_0, s_1, s_2\\}$ and $P$ becomes a valid invariant. **The corrected claim: $P$ is an invariant if and only if the edge $s_2 \\to s_3$ is removed from $R$.** **(7 marks)**\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall — Part (b):** Students often stop at the *consecution* check without realizing that *failure* of consecution does not mean the predicate is not an invariant. Conversely, *passing* consecution only matters if initiation also passes. **The full sequence of operations — (1) verify initiation, (2) attempt consecution, (3) if consecution fails, fall back to reachability analysis — must be shown explicitly in your answer.** A common 2-mark loss comes from students reporting \"$P$ is an invariant\" without rigorously computing $\\text{Reach}(M)$ when consecution fails.\n\n### Question B [14 Marks, Alternative Choice] [KTU University Exam — Dec 2023]\n\n**(a) [7 marks]** *Explain the method of generating invariants via abstract interpretation. Define the Galois connection and state the soundness theorem.*\n\n**(b) [7 marks]** *Apply the dataflow analysis framework to derive the invariant \"variable `x` is always non-negative\" for the following code:*\n```\n1.  if (x > 0) {\n2.      y = x + 5;\n3.  } else {\n4.      y = 0;\n5.  }\n6.  z = y - 3;\n7.  assert(z >= 0);\n```\n\n#### Model Solution — Part (a)\n\n**Abstract Interpretation (Cousot & Cousot, 1977):** A general theory of *sound approximation* of program semantics. The concrete semantics is computed in the *concrete domain* $\\langle \\wp(S), \\subseteq \\rangle$ (power-set lattice), while the analysis is performed in a simpler *abstract domain* $\\langle \\mathcal{A}, \\sqsubseteq \\rangle$ (e.g., intervals, octagons, polyhedra, or even $\\{ \\top, \\bot\\}$).\n\n**Galois Connection:** Two monotone functions $\\alpha : \\wp(S) \\to \\mathcal{A}$ (abstraction) and $\\gamma : \\mathcal{A} \\to \\wp(S)$ (concretization) form a Galois connection $\\wp(S) \\xleftrightarrow[\\alpha]{\\gamma} \\mathcal{A}$ if and only if for all $X \\in \\wp(S)$ and $a \\in \\mathcal{A}$:\n\n$$\\alpha(X) \\sqsubseteq a \\iff X \\subseteq \\gamma(a)$$\n\n**Soundness theorem:** If $F : \\wp(S) \\to \\wp(S)$ is a concrete transformer and $F^{\\#} : \\mathcal{A} \\to \\mathcal{A}$ is a sound abstract transformer satisfying $\\alpha \\circ F \\sqsubseteq F^{\\#} \\circ \\alpha$, then the least fixpoint satisfies:\n\n$$\\alpha(\\text{lfp}\\, F) \\sqsubseteq \\text{lfp}\\, F^{\\#}$$\n\nThe right-hand side is computable (often with a widening operator $\\nabla$ to enforce termination in finite time) and yields a **sound over-approximation** of the invariant set. The result is an invariant: every state actually reachable in the concrete system is contained in $\\gamma(\\text{lfp}\\, F^{\\#})$, so any safety property entailed by this set is verified.\n\n**Engineering utility:** Tools like Astrée (embedded avionics C code), Polyspace (MATLAB/Simulink), and Infer (Facebook) implement abstract interpretation over carefully chosen numeric domains to verify absence of runtime errors in millions of lines of code. **(7 marks)**\n\n#### Model Solution — Part (b)\n\nUse the **interval domain** $\\mathcal{A} = \\{[a, b] : a \\leq b, a \\in \\mathbb{Z} \\cup \\{-\\infty\\}, b \\in \\mathbb{Z} \\cup \\{+\\infty\\}\\}$.\n\n**Assume:** Before line 1, abstract state is $A_0 = [x_0^{\\min}, x_0^{\\max}]$ for `x`, and $A_0(y) = \\top = [-\\infty, +\\infty]$.\n\n**Line 1 (branch on `x > 0`):**\n- *Then-branch (line 2):* $x > 0 \\Rightarrow x \\in [1, +\\infty]$. So $A_1^{then}(x) = [1, +\\infty]$.\n  - Line 2: $y = x + 5 \\Rightarrow y \\in [6, +\\infty]$. $A_2^{then}(y) = [6, +\\infty]$, $A_2^{then}(x) = [1, +\\infty]$.\n- *Else-branch (line 4):* $x \\leq 0$. $A_1^{else}(x) = [-\\infty, 0]$.\n  - Line 4: $y = 0 \\Rightarrow y \\in [0, 0]$. $A_2^{else}(y) = [0, 0]$, $A_2^{else}(x) = [-\\infty, 0]$.\n\n**Line 5 (join, control-flow merge):**\n$$A_5(y) = A_2^{then}(y) \\sqcup A_2^{else}(y) = [6, +\\infty] \\sqcup [0, 0] = [0, +\\infty]$$\n$$A_5(x) = [1, +\\infty] \\sqcup [-\\infty, 0] = [-\\infty, +\\infty]$$\n\n**Line 6:** $z = y - 3 \\Rightarrow z \\in [0 - 3, +\\infty - 3] = [-3, +\\infty]$.\n\n**Line 7 (assertion):** We need $z \\geq 0$, but $A_6(z) = [-3, +\\infty]$ includes negative values.\n\n**Conclusion:** The invariant analysis **cannot prove** $z \\geq 0$ at line 7. The abstract domain is too coarse: it loses the correlation between the two branches. Using a **relational domain** (octagon or polyhedron) would preserve $y \\geq 0$ as an invariant and yield $z \\geq -3$ but would not tighten the lower bound. The tool reports a **potential false positive**, requiring manual review or domain refinement. **(7 marks)**\n\n> [!WARNING]\n> **KTU Examiner's Pitfall — Part (b):** Students often forget the **join (⊔) at control-flow merge points** and write $[6, +\\infty]$ for $y$ as the result at line 5. This loses the contribution of the `else` branch and is a 2-mark deduction. Also, **never forget the widening step** in iterative analysis — the examiner expects mention of either explicit widening or an explicit acknowledgement of termination, even for non-loop programs.\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Rapid-Revision Checklist**\n\n- **Invariant** = state predicate true at every reachable state; written $M \\models \\Box P$ (LTL) or $M, s_0 \\models AG\\, P$ (CTL).\n- **Inductive invariant** must satisfy two clauses: (1) *Initiation* — true in all initial states; (2) *Consecution* — preserved by every transition.\n- **Soundness** of the inductive proof rule follows by Noetherian induction on path length from initial states.\n- Every invariant is a safety property, but not every safety property is an invariant.\n- **Büchi–Landweber theorem:** A property is a safety property iff its set of minimal bad prefixes is regular (recognized by a DFA — the safety automaton).\n- The **reachable state set** is the least fixpoint: $\\text{Reach}(M) = \\mu X.\\, S_0 \\cup \\text{Post}(X)$, computable in $O(N \\cdot \\vert R \\vert)$ time for $|S| = N$.\n- **Abstract interpretation** uses a Galois connection $(\\alpha, \\gamma)$ to obtain a sound over-approximation $\\alpha(\\text{lfp}\\, F) \\sqsubseteq \\text{lfp}\\, F^{\\#}$.\n- **Widening operators** guarantee termination of abstract fixpoint iteration on infinite lattices.\n- **Dataflow analyses** (reaching-definitions, available-expressions, live-variables) are concrete instances of invariant generation over specific lattices.\n- **Non-inductive invariants** require an auxiliary inductive strengthening $I$ with $I \\Rightarrow J$ — the CEGAR loop automates this search.\n- **Common board-exam traps:** confusing *inductive* with *invariant*; forgetting the join at merge points; omitting the consecution check; reporting false positives as confirmed bugs.\n- **Engineering applications:** hardware pipeline verification, embedded controller safety, compiler optimization, runtime verification, and avionics software (DO-178C compliance).\n\n---\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t22",
          "title": "Safety Properties",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Safety Properties\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\nIn the formal framework of **Automated Verification** and **Model Checking**, a system execution (also called a computation, trace, or ω-word) is modeled as an infinite sequence of states:\n\n$$\\sigma = s_0 \\rightarrow s_1 \\rightarrow s_2 \\rightarrow \\ldots \\rightarrow s_i \\rightarrow \\ldots$$\n\nA **Safety Property** is a temporal logic specification that asserts: **\"Something bad never happens during system execution.\"**\n\n> [!IMPORTANT]\n> **KTU Definition (Board-Standard):**\n> A property $P \\subseteq \\Sigma^\\omega$ (where $\\Sigma^\\omega$ is the set of all infinite words over alphabet $\\Sigma$) is called a **safety property** if and only if for every infinite word $\\sigma \\notin P$ (a \"bad\" computation), there exists a *finite prefix* $u$ of $\\sigma$ such that **no extension of $u$ can ever satisfy $P$**. Such a prefix $u$ is termed a **bad prefix** (or **minimal bad prefix**) of $P$.\n\nIn the LTL (Linear Temporal Logic) notation preferred by KTU boards, every safety property can be written in the canonical form:\n\n$$P \\;\\equiv\\; \\Box\\,\\phi \\quad \\text{(i.e., } G\\,\\phi \\text{ — Globally } \\phi\\text{)}$$\n\nwhere $\\phi$ is a state formula (a purely propositional assertion evaluated on a single state).\n\n## 1.2 Conceptual Analogy / Intuition\n\n> [!NOTE]\n> **The Traffic Light Analogy**\n> Imagine a pedestrian crossing a road. A **safety property** is the rule: *\"The pedestrian is never hit by a car.\"* \n> - The moment a car and a pedestrian occupy the crossing simultaneously, the rule has been **violated** — and importantly, we can detect the violation by looking only at a *finite* snapshot of history (a finite prefix of the execution).\n> - Once a violation is recorded, **no continuation of the universe can ever \"undo\" the accident**. This irreversibility is the defining fingerprint of safety.\n> - In contrast, a *liveness* property like \"the traffic light eventually turns green\" cannot be refuted by any finite prefix — we always have to keep waiting.\n\n## 1.3 Key Terminology Anchors\n\n| Term | Meaning |\n| :--- | :--- |\n| **Trace / ω-word** | Infinite sequence $\\sigma = s_0, s_1, s_2, \\ldots$ over states of the Kripke structure $M$ |\n| **Prefix (cl(σ))** | The set of all finite prefixes of an infinite trace |\n| **Bad Prefix** | A finite word $u$ such that $\\forall \\beta \\in \\Sigma^\\omega : u\\beta \\notin P$ |\n| **Invariance** | Property of the form $G\\,\\phi$ where $\\phi$ is a state predicate |\n| **Canonical Form** | The syntactic shape $\\Box \\phi$ that every safety LTL formula can be rewritten into |\n\n## 1.4 Standard Metrics & Notation\n\n- The set of **bad prefixes** of a property $P$ is denoted $\\text{BadPref}(P)$.\n- The **closure** of $P$ is: $\\text{cl}(P) = P \\cup \\text{Pref}(P)$.\n- The shorthand for \"and so on\" (infinite extension) is $\\Sigma^\\omega$.\n- The shorthand for \"any finite extension\" is $\\Sigma^*$.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Structural Decomposition — The Three Logical Pillars\n\nA safety property rests on three independent pillars that examiners love to test:\n\n1. **Pillar 1 — Refutability by a Finite Prefix**  \n   If a system violates a safety property, the violation is *witnessed* by a finite observable prefix. Formally, a counterexample to a safety property is *always finite*.\n\n2. **Pillar 2 — Prefix-Closure under Good Words**  \n   If $\\sigma \\models P$ and $u$ is a finite prefix of $\\sigma$, then $u$ is **not** a bad prefix. Equivalently, $\\text{Pref}(P) \\subseteq \\text{Pref}(\\Sigma^\\omega) \\setminus \\text{BadPref}(P)$.\n\n3. **Pillar 3 — Limit-Closure**  \n   The property $P$ is **limit-closed**: $P = \\lim(\\text{Pref}(P))$, meaning $P$ contains exactly those infinite words whose every finite prefix is good.\n\n## 2.2 Classification of Common LTL Safety Patterns\n\n| LTL Formula | Natural-Language Reading | Safety Class? |\n| :--- | :--- | :--- |\n| $G\\,p$ | $p$ is true in every reachable state | ✓ Pure safety (invariant) |\n| $G(p \\rightarrow X\\,q)$ | Whenever $p$ holds, $q$ must hold in the next state | ✓ Safety (bounded horizon) |\n| $G(p \\rightarrow (\\neg q \\,U\\, r))$ | After $p$, $q$ is forbidden until $r$ occurs | ✓ Safety |\n| $G\\,\\neg(p \\wedge q)$ | Mutual exclusion between $p$ and $q$ | ✓ Safety (canonical invariant) |\n| $F\\,G\\,p$ | Eventually $p$ holds forever | ✗ **Liveness** (no finite witness of failure) |\n| $G\\,F\\,p$ | $p$ occurs infinitely often | ✗ **Liveness** |\n| $F\\,p$ | $p$ will eventually occur | ✗ **Liveness** |\n\n> [!IMPORTANT]\n> **KTU Board Heuristic:** If the LTL formula has the **shape $G\\,\\psi$ or $\\Box \\psi$ at its outermost operator**, it is a **safety property**. If the outermost operator is $F$ (eventually) without an enclosing $G$, it is *not* a safety property.\n\n## 2.3 KTU Formula Sheet (Board-Critical, No Pipes Inside Cells)\n\n| # | Concept | Mathematical Formulation | Notes / Units |\n| :--- | :--- | :--- | :--- |\n| F1 | Property Domain | $P \\subseteq \\Sigma^\\omega$ | Set of all infinite words |\n| F2 | Bad Prefix Definition | $u \\in \\text{BadPref}(P) \\iff \\forall \\beta \\in \\Sigma^\\omega : u\\beta \\notin P$ | Finite counterexample |\n| F3 | Safety Property Axiom | $P \\text{ is safe} \\iff \\forall \\sigma \\notin P : \\exists u \\sqsubseteq_{\\text{fin}} \\sigma : u \\in \\text{BadPref}(P)$ | The defining equivalence |\n| F4 | Closure Operator | $\\text{cl}(P) = P \\cup \\text{Pref}(P)$ | Used to detect safety |\n| F5 | Limit Closure Test | $P = \\lim\\bigl(\\text{Pref}(P)\\bigr)$ | $P$ contains all limit points of its prefixes |\n| F6 | Canonical LTL Form | $P \\equiv \\Box \\phi$ where $\\phi$ is state-formula | Equivalent normal form |\n| F7 | Safety Monus (Set Diff.) | $P \\,\\setminus\\, Q = \\lim\\bigl(\\text{Pref}(P) \\setminus \\text{Pref}(Q)\\bigr)$ | Difference of two safety properties |\n| F8 | Bad Prefixes Set | $\\text{BadPref}(P) = \\Sigma^+ \\setminus \\text{Pref}(P)$ | Complement of good prefixes |\n| F9 | Inductive Invariant | $\\text{Inv} = \\{\\sigma \\in \\Sigma^\\omega : \\forall i \\ge 0, \\sigma_i \\models \\phi\\}$ | The set of \"always-$\\phi$\" traces |\n| F10 | Verification Equation | $M \\models \\Box \\phi \\iff \\text{Init}(M) \\subseteq \\text{Inv}_\\phi(M)$ | Model satisfies iff all reachable states obey $\\phi$ |\n\n## 2.4 Real-World Engineering Utility\n\n- **Hardware Verification (RTL/Netlist):** Verifying that no two control signals are ever simultaneously asserted — encoded as $G(\\neg(\\text{req} \\wedge \\text{ack}_{\\text{dup}}))$.\n- **Embedded & Automotive (ISO 26262):** Proving that a brake-by-wire controller never commands full throttle and full brake at the same instant.\n- **Protocol Design:** Showing that in a mutual-exclusion protocol (e.g., Peterson's, Bakery), no two processes are in the critical section together — formal invariant: $G(\\neg(\\text{in\\_CS}_1 \\wedge \\text{in\\_CS}_2))$.\n- **Compiler Correctness:** Proving that optimized code never accesses memory out of bounds — encoded as a safety invariant over the memory model.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: The Bad-Prefix Characterization Theorem\n\n**Theorem (Sistla, 1994 — KTU-Standard):**  \nA property $P \\subseteq \\Sigma^\\omega$ is a safety property if and only if for every infinite word $\\sigma$ that is *not* in $P$, there exists a *finite* prefix $u$ of $\\sigma$ such that no infinite continuation of $u$ belongs to $P$.\n\n### Proof (Forward Direction: $\\Rightarrow$)\n\nAssume $P$ is a safety property.\n\n**Step 1 — Choose a violating word.**  \nLet $\\sigma \\in \\Sigma^\\omega$ be an arbitrary infinite word such that $\\sigma \\notin P$.\n\n**Step 2 — Apply the safety definition.**  \nBy the safety property definition given in §1.1, there must exist a finite prefix $u$ of $\\sigma$ such that:\n\n$$\\forall \\beta \\in \\Sigma^\\omega : u \\cdot \\beta \\notin P$$\n\n**Step 3 — Declare $u$ a bad prefix.**  \nThis $u$ is, by construction, a member of $\\text{BadPref}(P)$. The witness is finite, completing the forward direction. $\\blacksquare$\n\n### Proof (Reverse Direction: $\\Leftarrow$)\n\nAssume that for every $\\sigma \\notin P$, a finite bad prefix exists.\n\n**Step 1 — Construct the closure.**  \nDefine $\\text{cl}(P) = P \\cup \\text{Pref}(P)$, the set of all words that are either in $P$ or finite prefixes of some word in $P$.\n\n**Step 2 — Show $\\text{cl}(P) = \\lim(\\text{Pref}(P))$.**  \nA word $\\sigma$ is in the limit of $\\text{Pref}(P)$ if and only if every finite prefix of $\\sigma$ is in $\\text{Pref}(P)$, which by contrapositive means no finite prefix is in $\\text{BadPref}(P)$. By our assumption, this implies $\\sigma \\in P$. Hence:\n\n$$\\lim\\bigl(\\text{Pref}(P)\\bigr) \\subseteq P$$\n\nThe reverse inclusion is trivial (every $\\sigma \\in P$ has all its prefixes in $\\text{Pref}(P)$). Therefore:\n\n$$P = \\lim\\bigl(\\text{Pref}(P)\\bigr) = \\text{cl}(P)$$\n\n**Step 3 — Conclude the safety property.**  \nSince $P$ is the limit of its prefix set, any violation is detectable by a finite witness, confirming the safety property. $\\blacksquare$\n\n## 3.2 Derivation: How to Test Whether a Given LTL Formula is a Safety Property\n\n**Algorithm (KTU-Examiner Method):**\n\n**Step 1.** Identify the **outermost temporal operator** of the LTL formula $\\varphi$.\n\n**Step 2.** Check the syntactic shape:\n\n$$\\text{Safety}(\\varphi) \\iff \\varphi \\equiv G\\,\\psi \\;\\text{ for some state formula } \\psi$$\n\n**Step 3.** Verify using the semantic test:\n\n$$\\forall \\sigma \\in \\Sigma^\\omega : \\sigma \\not\\models \\varphi \\implies \\exists n \\ge 0 : \\forall \\tau \\in \\Sigma^\\omega : \\sigma_{\\le n} \\cdot \\tau \\not\\models \\varphi$$\n\nwhere $\\sigma_{\\le n}$ denotes the finite prefix of length $n+1$.\n\n**Step 4.** If the formula fails Step 3 (e.g., $F\\,p$), it is a **liveness** property and is **not** a safety property.\n\n## 3.3 Analytical Matrix: Safety vs. Liveness Classification\n\n| Property Class | Witness of Violation | Typical LTL Shape | Bounded Check? |\n| :--- | :--- | :--- | :--- |\n| **Safety** | Finite prefix | $\\Box \\phi$ (outermost $G$) | Yes — model checking terminates on counterexample |\n| **Liveness** | No finite witness; needs infinite horizon | $\\Diamond \\phi$, $\\Box \\Diamond \\phi$ | No — requires Büchi-cycle search |\n| **Persistence** (sub-liveness) | After some point, property holds forever | $\\Diamond \\Box \\phi$ | Hybrid |\n| **Recurrence** (sub-safety) | Property holds infinitely often | $\\Box \\Diamond \\phi$ | Hybrid |\n\n## 3.4 Derivation: Construction of the Bad-Prefix Automaton\n\n**Step 1 — Input:** A safety LTL formula $G\\,\\phi$.\n\n**Step 2 — Translation:** Convert the negated formula $\\neg G\\,\\phi \\equiv F\\,\\neg\\phi$ into a **Büchi automaton** $\\mathcal{B}_{\\neg\\varphi}$ using standard tableau-based LTL-to-Büchi construction.\n\n**Step 3 — Complement:** Take the complement $\\overline{\\mathcal{B}_{\\neg\\varphi}}$ to obtain a *good-prefix* automaton recognizing $\\text{Pref}(P)$.\n\n**Step 4 — Bad-Prefix Automaton:** Complement once more to get a **deterministic finite automaton (DFA)** $\\mathcal{A}_{\\text{bad}}$ that accepts exactly $\\text{BadPref}(P)$.\n\n**Step 5 — Verification:** The system $M$ satisfies $G\\,\\phi$ if and only if $L(M) \\cap L(\\mathcal{A}_{\\text{bad}}) = \\emptyset$, i.e., no execution of $M$ drives the system into a state where the bad-prefix DFA accepts.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Mermaid Diagram: The Anatomy of a Safety Property\n\n```mermaid\ngraph TD\n    A[Infinite Trace sigma in Sigma^omega] --> B{Does sigma satisfy the property P?}\n    B -- Yes --> C[GOOD INFINITE WORD: sigma is in P]\n    B -- No --> D[BAD INFINITE WORD: sigma not in P]\n    D --> E[Extract finite prefix u of sigma]\n    E --> F{Is u a bad prefix?}\n    F -- Yes, and ALL extensions fail --> G[u is in BadPref of P]\n    F -- No, some extension succeeds --> H[u is just a temporary violation, not a witness]\n    G --> I[CONCLUSION: P is a SAFETY PROPERTY]\n    H --> J[CONCLUSION: P is NOT a safety property]\n    C --> K[CONCLUSION: sigma satisfies P - no violation]\n```\n\n## 4.2 Mermaid Diagram: Verification Pipeline for a Safety Property\n\n```mermaid\ngraph LR\n    A[Kripke Structure M] --> B[Build Reachable State Space]\n    B --> C[Apply Invariant Checker for phi]\n    C --> D{All reachable states satisfy phi?}\n    D -- Yes --> E[VERIFIED: M satisfies G phi]\n    D -- No --> F[Produce Counterexample Trace]\n    F --> G[Identify Minimal Bad Prefix u]\n    G --> H[Return Finite Witness to User]\n    E --> I[Report SUCCESS to Examiner]\n    H --> J[Report VIOLATION with Diagnostic Path]\n```\n\n## 4.3 Mermaid Diagram: Classification Tree — LTL Property Hierarchy\n\n```mermaid\ngraph TD\n    Root[LTL Property phi] --> Op{Outermost Operator}\n    Op -->|G globally| SafeBox[Box-Shape Candidate]\n    Op -->|F eventually| LiveDiam[Diamond-Shape Candidate]\n    Op -->|G F infinitely often| Recur[Recurrence - Liveness]\n    Op -->|F G eventually forever| Persist[Persistence - Liveness]\n    SafeBox --> SafeCheck{Inner formula psi is state-local?}\n    SafeCheck -- Yes --> PureSafe[PURE SAFETY PROPERTY]\n    SafeCheck -- No --> MaybeSafe[Check via bad-prefix test]\n    LiveDiam --> PureLive[PURE LIVENESS PROPERTY]\n    Recur --> PureLive\n    Persist --> PureLive\n    MaybeSafe --> Safe2{Finite witness exists?}\n    Safe2 -- Yes --> PureSafe\n    Safe2 -- No --> PureLive\n```\n\n## 4.4 Block-Level Functional Architecture of a Safety Model Checker\n\n```mermaid\ngraph TD\n    InputA[System Model: Kripke Structure M] --> Front[Frontend Parser]\n    InputB[Safety Formula: G phi in LTL] --> Front\n    Front --> Neg[Negation Module: convert G phi to F not-phi]\n    Neg --> Tableau[Tableau Builder: LTL to Buchi]\n    Tableau --> Comp[Complement Buchi to DFA]\n    Comp --> Prod[Product Construction: M cross A_bad]\n    Prod --> Reach[Reachability Analysis on Product]\n    Reach -->|Empty intersection| ResultA[VERIFIED: System Safe]\n    Reach -->|Non-empty intersection| ResultB[UNSAFE: Counterexample Path Found]\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n## **PART A — 3-Mark Short Answer Questions**\n\n### **Q1. [KTU University Exam — May 2024]**\n**Define a safety property in the context of linear-time temporal logic. State the canonical syntactic form of a safety property.**\n\n**Model Answer (3 Marks):**\n\nA **safety property** is a class of linear-time temporal specifications whose violations are always witnessed by a **finite prefix** of the execution trace. In other words, if a system violates a safety property, there exists a finite sequence of states that *already* constitutes a counterexample — no future behavior can \"undo\" the violation.\n\nThe canonical syntactic form of any safety property in LTL is:\n\n$$P \\;\\equiv\\; \\Box\\,\\phi \\;\\;\\text{(equivalently written as } G\\,\\phi\\text{)}$$\n\nwhere $\\phi$ is a **state formula** — a purely propositional assertion evaluated on a single state with no temporal operators.\n\n*(1 mark for definition, 1 mark for finiteness-of-witness clause, 1 mark for canonical form.)*\n\n---\n\n### **Q2. [KTU University Exam — Dec 2023]**\n**What is a \"bad prefix\" of a safety property $P$? Show that the property $G(p \\rightarrow X\\,q)$ has a bad prefix of length 2 when $p$ holds initially and $q$ does not hold in the next state.**\n\n**Model Answer (3 Marks):**\n\nA **bad prefix** of a property $P$ is a finite word $u \\in \\Sigma^+$ such that **no infinite extension** of $u$ can satisfy $P$:\n\n$$u \\in \\text{BadPref}(P) \\iff \\forall \\beta \\in \\Sigma^\\omega : u \\cdot \\beta \\notin P$$\n\nFor the property $G(p \\rightarrow X\\,q)$:\n\n- Suppose the initial state satisfies $p$ (i.e., $p = \\text{true}$) and the next state satisfies $\\neg q$.\n- The finite prefix of length 2 is $u = \\langle p \\wedge \\neg q, \\neg q \\rangle$ (state 0 has $p$ true, state 1 has $q$ false).\n- Any infinite continuation $\\beta$ will have $u\\beta$ starting with a state where $p$ holds but $X\\,q$ fails at position 1, which is a direct violation of $G(p \\rightarrow X\\,q)$.\n- Therefore $u$ is a bad prefix of length 2.\n\n*(1 mark definition, 1 mark construction, 1 mark conclusion.)*\n\n---\n\n## **PART B — 14-Mark Questions (Internal Choice Pattern)**\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Common Pitfall Alert**\n> Students frequently lose **2 to 3 marks** on safety-property questions by:\n> 1. **Confusing safety with liveness** — they classify $F\\,G\\,p$ (a persistence/liveness property) as safety. Remember: *outermost* $F$ ⟹ liveness, *outermost* $G$ ⟹ safety.\n> 2. **Skipping the finite-witness clause** in definitions — a definition of safety without the phrase \"finite prefix\" or \"finite counterexample\" is **incomplete** and will cost at least 1 mark.\n> 3. **Forgetting to state the canonical form** — KTU boards expect the explicit $\\Box \\phi$ (or $G \\phi$) rewriting.\n> 4. **Mixing up bad prefixes with violating words** — a bad prefix is **finite**; a violating word is **infinite**.\n\n---\n\n### **Question A (14 Marks) [KTU University Exam — June 2024]**\n\n**(a) [7 Marks]**  \n**Formally define a safety property in terms of the \"bad prefix\" characterization. Prove the equivalence between this definition and the limit-closure characterization $P = \\lim(\\text{Pref}(P))$.**\n\n**(b) [7 Marks]**  \n**Consider two safety properties $P_1 \\equiv G(p \\rightarrow X\\,q)$ and $P_2 \\equiv G\\,\\neg(p \\wedge q)$. Construct the bad-prefix automaton for the conjunction $P_1 \\wedge P_2$ and demonstrate that the system violating $P_1$ in step (a) also violates the conjunction.**\n\n#### Model Solution\n\n**Part (a) — 7 Marks:**\n\n**Definition (2 marks):**  \nA property $P \\subseteq \\Sigma^\\omega$ is a **safety property** if and only if for every $\\sigma \\notin P$, there exists a finite prefix $u \\sqsubseteq \\sigma$ such that:\n\n$$u \\in \\text{BadPref}(P) \\iff \\forall \\beta \\in \\Sigma^\\omega : u \\cdot \\beta \\notin P$$\n\n**Limit-Closure Definition (1 mark):**  \nThe **limit** of a set of finite words $A \\subseteq \\Sigma^*$ is the set of all infinite words whose every finite prefix belongs to $A$:\n\n$$\\lim(A) = \\{\\sigma \\in \\Sigma^\\omega : \\text{Pref}(\\sigma) \\subseteq A\\}$$\n\n**Proof of Equivalence (4 marks):**\n\n$(\\Rightarrow)$ Assume $P$ is safe. We show $P \\subseteq \\lim(\\text{Pref}(P))$ and $\\lim(\\text{Pref}(P)) \\subseteq P$.\n\n- $P \\subseteq \\lim(\\text{Pref}(P))$: For any $\\sigma \\in P$, by definition every prefix of $\\sigma$ is in $\\text{Pref}(P)$, so $\\sigma \\in \\lim(\\text{Pref}(P))$.\n- $\\lim(\\text{Pref}(P)) \\subseteq P$: Let $\\sigma \\in \\lim(\\text{Pref}(P))$. Suppose for contradiction that $\\sigma \\notin P$. By the safety property, there exists a finite bad prefix $u \\sqsubseteq \\sigma$. But $u \\in \\text{Pref}(P)$ would require some $\\sigma' \\in P$ with $u \\sqsubseteq \\sigma'$, contradicting $u \\in \\text{BadPref}(P)$. Hence $\\sigma \\in P$.\n\n$(\\Leftarrow)$ Assume $P = \\lim(\\text{Pref}(P))$. Let $\\sigma \\notin P$. Since $\\sigma \\notin \\lim(\\text{Pref}(P))$, there exists a finite prefix $u \\sqsubseteq \\sigma$ such that $u \\notin \\text{Pref}(P)$. By definition, no continuation of $u$ is in $P$, so $u \\in \\text{BadPref}(P)$. Thus $P$ is a safety property. $\\blacksquare$\n\n**Part (b) — 7 Marks:**\n\n**Bad-Prefix Set for $P_1$ (1.5 marks):**  \n$\\text{BadPref}(P_1)$ consists of all finite words where some position $i$ has $p = \\text{true}$ and position $i+1$ has $q = \\text{false}$.\n\n**Bad-Prefix Set for $P_2$ (1.5 marks):**  \n$\\text{BadPref}(P_2)$ consists of all finite words where some single state has both $p$ and $q$ true.\n\n**Conjunction $P_1 \\wedge P_2$ (2 marks):**  \nA bad prefix for $P_1 \\wedge P_2$ is any finite word that is bad for *either* $P_1$ or $P_2$:\n\n$$\\text{BadPref}(P_1 \\wedge P_2) = \\text{BadPref}(P_1) \\cup \\text{BadPref}(P_2)$$\n\nThe corresponding **bad-prefix automaton** $\\mathcal{A}_{\\text{bad}}$ has states tracking whether $p$ is asserted in the current state and whether the next state violates $q$ (for $P_1$) or whether a state contains both $p$ and $q$ (for $P_2$). The accepting states are those in which either condition is detected.\n\n**Demonstration of Violation (2 marks):**  \nConsider the trace $u = \\langle p \\wedge \\neg q, q \\rangle$. This trace satisfies $P_2$ (no state has both $p$ and $q$) but **violates $P_1$** at the first transition ($p$ holds in state 0, but $q$ does not hold in state 1). Hence $u$ is a bad prefix for $P_1 \\wedge P_2$, and the conjunction is violated. The system is **unsafe**.\n\n---\n\n### **Question B (14 Marks) [KTU University Exam — Dec 2023]**\n\n**(a) [7 Marks]**  \n**Explain with suitable examples the classification of LTL properties into safety, liveness, and persistence classes. Provide a procedure (with justification) to determine whether a given LTL formula represents a safety property.**\n\n**(b) [7 Marks]**  \n**For a mutual-exclusion system with two processes $P_1$ and $P_2$, express the safety requirement that no two processes are in the critical section simultaneously as an LTL formula. Then, using the bad-prefix method, describe how a model checker (e.g., SPIN/NuSMV) would refute the property if the system has a faulty interleaving.**\n\n#### Model Solution\n\n**Part (a) — 7 Marks:**\n\n**Classification (3 marks):**\n\n- **Safety Properties:** Assert that *something bad never happens*. Violations are witnessed by finite prefixes. Canonical form: $G \\phi$.  \n  *Example:* $G(\\neg(\\text{cs}_1 \\wedge \\text{cs}_2))$ — mutual exclusion invariant.\n\n- **Liveness Properties:** Assert that *something good eventually happens*. No finite prefix can refute them. Canonical form: $F \\phi$ or $G F \\phi$.  \n  *Example:* $F(\\text{request} \\rightarrow \\text{grant})$ — every request is eventually granted.\n\n- **Persistence Properties:** Assert that *something holds continuously from some point on*. Canonical form: $F G \\phi$.  \n  *Example:* $F G \\neg \\text{error}$ — errors eventually cease forever.\n\n**Procedure to Determine Safety (4 marks):**\n\n1. **Parse the formula** and identify the outermost temporal operator.\n2. **Syntactic test:** If the outermost operator is $G$ (or $\\Box$), and the inner formula is a state predicate (no nested temporal operators referring to the *future*), the formula is a safety property.\n3. **Semantic test (necessary, not sufficient):** Check that the formula's negation $\\neg \\varphi$ is recognizable by a *finite automaton* (a property is safety iff its negation is co-Büchi or finite-state).\n4. **Bad-prefix construction:** Convert $\\varphi$ to a Büchi automaton, take the complement to get a DFA over finite words, and verify that this DFA recognizes exactly the bad prefixes.\n\nJustification: The procedure is sound because the limit-closure theorem (§3.1) shows that safety properties are exactly those whose violations have finite witnesses.\n\n**Part (b) — 7 Marks:**\n\n**LTL Encoding of Mutual Exclusion (2 marks):**\n\nLet $\\text{cs}_1$ denote \"process $P_1$ is in its critical section\" and $\\text{cs}_2$ similarly for $P_2$. The safety requirement is:\n\n$$\\varphi_{\\text{ME}} \\;\\equiv\\; G\\,\\neg(\\text{cs}_1 \\wedge \\text{cs}_2)$$\n\nThis is a *canonical safety property* because it has the form $\\Box \\phi$ with $\\phi = \\neg(\\text{cs}_1 \\wedge \\text{cs}_2)$ being a state formula.\n\n**Bad-Prefix Construction (2.5 marks):**\n\nThe bad-prefix DFA $\\mathcal{A}_{\\text{bad}}$ has a single accepting trap state $q_{\\text{bad}}$ with a self-loop on every input. There is a transition from the initial state $q_0$ to $q_{\\text{bad}}$ labeled with the input symbol $\\langle \\text{cs}_1 = \\text{true}, \\text{cs}_2 = \\text{true} \\rangle$ (i.e., the state where both processes are simultaneously in the critical section). All other symbols loop at $q_0$.\n\n**Refutation by Model Checker (2.5 marks):**\n\n1. The model checker constructs the product automaton $M \\times \\mathcal{A}_{\\text{bad}}$.\n2. It performs a **reachability analysis** to determine if the accepting state $q_{\\text{bad}}$ of $\\mathcal{A}_{\\text{bad}}$ is reachable from the initial state of $M$ via the product's transition relation.\n3. If $q_{\\text{bad}}$ is reachable, the model checker has found a finite counterexample — a trace $s_0, s_1, \\ldots, s_n$ where $s_n$ has both $\\text{cs}_1$ and $\\text{cs}_2$ true. This trace is a **bad prefix**, and the model checker returns it as the diagnostic output.\n4. The engineer inspects the faulty interleaving (e.g., a missing semaphore acquisition in the transition from $s_{n-1}$ to $s_n$) and patches the system.\n\nThis illustrates the **decisive advantage** of safety properties: their counterexamples are *finite* and *easily inspected* by humans.\n\n---\n\n## **Topic Recap & Important Things to Remember (Rapid-Revision Checklist)**\n\n> [!NOTE]\n> **High-Density Bullet Summary — Print and Revise Before Exam**\n\n- ✅ **Safety = \"Bad things never happen.\"** Always finite witness of violation.\n- ✅ Canonical LTL shape: $\\Box \\phi$ or $G \\phi$ with $\\phi$ being a **state formula**.\n- ✅ **Bad prefix** = finite word $u$ such that no continuation can ever satisfy the property.\n- ✅ **Theorem (limit-closure):** $P$ is safe $\\iff P = \\lim(\\text{Pref}(P))$.\n- ✅ **Bad-prefix set:** $\\text{BadPref}(P) = \\Sigma^+ \\setminus \\text{Pref}(P)$.\n- ✅ **Counterexamples to safety are always finite** — this is what makes model checking decidable and efficient for safety.\n- ✅ **Common safety patterns:** mutual exclusion ($G \\neg(p \\wedge q)$), type safety, deadlock freedom (as a *negative* invariant), access control.\n- ✅ **Common safety *non*-examples:** $F p$, $F G p$, $G F p$ — these are liveness/persistence.\n- ✅ **Verification recipe:** Convert $G \\phi$ to DFA over bad prefixes; check $L(M) \\cap L(\\mathcal{A}_{\\text{bad}}) = \\emptyset$.\n- ✅ **Closure operator:** $\\text{cl}(P) = P \\cup \\text{Pref}(P)$.\n- ✅ **Safety monus:** Difference of two safety properties is again a safety property.\n- ✅ **Tools:** SPIN (Promela), NuSMV, CBMC — all natively support safety-property checking via reachability analysis.\n- ✅ **Industrial relevance:** Hardware verification, automotive (ISO 26262), avionics (DO-178C), security protocols.\n- ✅ **Board-exam trap:** Always state the **finite-witness** clause explicitly — partial answers get partial marks.\n- ✅ **Mnemonic:** \"*G*ood things *G*uaranteed forever = *G*lobal = *S*afety\" — outermost $G$ → safety.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t23",
          "title": "Trace Equivalence and Safety Properties",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module Module 1\n> - **Topic:** Trace Equivalence and Safety Properties\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Trace Equivalence — Formal Definition\n\nIn the theory of concurrent and reactive systems, a **trace** (also called a *computation sequence* or *execution sequence*) of a labelled transition system (LTS) is a finite or infinite sequence of observable actions that the system can perform starting from its initial state.\n\nFormally, for an LTS $\\mathcal{M} = (S, Act, \\rightarrow, s_0, AP, L)$:\n\n$$\\text{Traces}(s) = \\{\\, \\sigma \\in Act^{*} \\cup Act^{\\omega} \\mid s \\overset{\\sigma}{\\Longrightarrow} \\,\\}$$\n\nwhere $s \\overset{\\sigma}{\\Longrightarrow}$ denotes that there exists an execution from state $s$ whose sequence of labels along the path is exactly $\\sigma$.\n\n> [!IMPORTANT]\n> **Trace Equivalence (Definition):** Two LTSs $\\mathcal{M}_1$ and $\\mathcal{M}_2$ are **trace equivalent**, written $\\mathcal{M}_1 \\approx_t \\mathcal{M}_2$, if and only if\n> $$\\text{Traces}(\\mathcal{M}_1) = \\text{Traces}(\\mathcal{M}_2)$$\n> i.e., they generate **exactly the same set of observable action sequences**.\n\n### Conceptual Analogy / Intuition\n\nImagine two coffee vending machines placed side by side. A **trace** is the *receipt* printed by the machine — it records only the user-visible events: `INSERT_COIN`, `SELECT_COFFEE`, `DISPENSE`, `REFUND`. The internal micro-controllers, voltage fluctuations, and relay clicks are *not* recorded.\n\nTwo machines are **trace equivalent** if, no matter what sequence of buttons a user presses and observes, the *receipts* they could possibly obtain from either machine are *identical as sets*. Notice that this does **not** require the machines to be internally identical — one might use a spring, the other a solenoid, but if a customer cannot tell the difference in what they observe, the machines are trace equivalent.\n\n> [!NOTE]\n> **KTU Syllabus Highlight:** Trace equivalence is the most *liberal* (coarsest) behavioural equivalence. It is strictly weaker than **strong bisimulation** but coarser than many branching-time equivalences. It forms the semantic backbone of **linear-time** logics like LTL.\n\n---\n\n## 1.2 Safety Properties — Formal Definition\n\nA **safety property** is a property that, if violated, can be detected by examining a *finite prefix* of an execution. Equivalently, a property $P$ is a safety property iff:\n\n> *Whenever an execution $\\pi$ violates $P$, there exists a finite prefix $\\sigma$ of $\\pi$ such that every extension of $\\sigma$ also violates $P$.*\n\nThe minimal such \"bad\" prefix is called a **bad prefix** (or *forbidden* prefix) of $P$.\n\nFormally, $P \\subseteq \\Sigma^{\\omega}$ is a safety property if and only if it is **prefix-closed** in the following sense:\n$$\\forall \\pi \\in \\Sigma^{\\omega} : \\big( \\pi \\notin P \\;\\Rightarrow\\; \\exists n \\geq 0 : \\forall \\rho \\in \\Sigma^{\\omega} : (\\pi[0..n])\\rho \\notin P \\big)$$\n\n### Conceptual Analogy / Intuition\n\nThink of a **safety property** as a *traffic law*: *\"A red light must never be crossed.\"* The moment a driver crosses a red light, the violation is complete — you don't need to wait until the end of the journey to know that a violation occurred. The **finite bad prefix** is the exact moment the car passed the white line on red.\n\nContrast this with a **liveness property** like *\"Eventually the train arrives\"*. You can never conclude from a finite observation that the train *will never* arrive — it might still be on its way. Liveness violations are *inherently infinite*; safety violations are *finitely detectable*.\n\n> [!IMPORTANT]\n> **The Alpern–Schneider Theorem (1985):** Every property $P \\subseteq \\Sigma^{\\omega}$ can be uniquely decomposed as the intersection of a safety property and a liveness property. This is the foundational result of property classification in model checking and is **directly examinable** under KTU's outcome CO1.\n\n---\n\n## 1.3 Why These Two Concepts Are Studied Together\n\nTrace equivalence and safety properties are the two pillars of **linear-time** verification:\n- **Trace equivalence** gives the *semantics* of when two systems \"look the same from outside\".\n- **Safety properties** give the *specification language* for what a system \"must never do\".\n\nTogether they form the verification question: *\"Does every trace of System $M$ satisfy the safety property $P$?\"* — which is exactly the model checking problem for invariant-like specifications.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Hierarchical Classification of Equivalences\n\nTrace equivalence sits at a specific point in the spectrum of behavioural equivalences. The **strictness ordering** from finest to coarsest is:\n\n$$\\text{Strong Bisimulation} \\;\\sqsubset\\; \\text{Weak Bisimulation} \\;\\sqsubset\\; \\text{Trace Equivalence} \\;\\sqsubset\\; \\text{Testing Equivalence}$$\n\nLogic correspondence:\n- Strong bisimulation $\\equiv$ **CTL\\*** (branching-time)\n- Trace equivalence $\\equiv$ **LTL** (linear-time)\n\n> [!NOTE]\n> **Key consequence:** If $M_1 \\sim M_2$ (bisimilar), then $M_1 \\approx_t M_2$ (trace equivalent). The converse is **false**.\n\n---\n\n## 2.2 Variants of Trace Equivalence\n\n### (a) Strong Trace Equivalence\nInternal action $\\tau$ is treated as observable. Two systems are strongly trace equivalent iff they have identical sequences of labels including $\\tau$.\n\n### (b) Weak Trace Equivalence\nInternal $\\tau$-transitions are *abstracted away*. We define the **weak transition relation**:\n$$s \\overset{a}{\\Longrightarrow} s' \\;\\;\\iff\\;\\; s \\overset{\\tau^{*}a\\tau^{*}}{\\longrightarrow} s'$$\n\nwhere $\\tau^{*}$ denotes zero or more $\\tau$-steps. Then weak trace equivalence is:\n\n$$\\mathcal{M}_1 \\approx_{wt} \\mathcal{M}_2 \\;\\iff\\; \\text{Traces}_{\\Longrightarrow}(\\mathcal{M}_1) = \\text{Traces}_{\\Longrightarrow}(\\mathcal{M}_2)$$\n\n| Aspect | Strong Trace | Weak Trace |\n|---|---|---|\n| $\\tau$ visibility | Visible | Hidden |\n| Used for | Fully observable systems | Systems with internal actions |\n| Computational cost | Polynomial in $\\mid S \\mid$ | PSPACE-complete |\n| KTU weight | High (asked frequently) | High (asked in Part B) |\n\n---\n\n## 2.3 Safety Property Classification Tree\n\n```\n                    Properties (P ⊆ Σ^ω)\n                            |\n            ________________|________________\n           |                                 |\n      SAFETY                            Liveness\n   (finite bad prefix)              (no finite bad prefix)\n           |\n   ________|________\n  |                 |\nInvariant       Guarantee\n(G holds in      (Q eventually\nall reachable    holds along\nstates)          some path)\n```\n\n> [!IMPORTANT]\n> **Invariant** (state-based) and **Guarantee** (trace-based) are both safety properties. The defining test: *if a bad prefix exists, the property is safety*.\n\n---\n\n## 2.4 KTU High-Yield Formula Sheet\n\n> [!NOTE]\n> The following table compiles every formula, notation, and threshold required for the KTU board exam on this topic. Memorise it for the **2-mark direct recall questions**.\n\n| Symbol / Formula | Meaning | Standard Notation |\n|---|---|---|\n| $\\text{Tr}(s)$ | Set of traces from state $s$ | $\\subseteq Act^{*} \\cup Act^{\\omega}$ |\n| $s \\overset{\\sigma}{\\Longrightarrow} s'$ | Weak transition sequence | $s \\xrightarrow{\\sigma} s'$ in strong form |\n| $\\text{BadPref}(P)$ | Set of all bad prefixes of $P$ | $BP(P) \\subseteq \\Sigma^{*}$ |\n| $P = \\Sigma^{\\omega} \\setminus \\text{BadPref}(P)\\cdot \\Sigma^{\\omega}$ | Safety property characterisation | Alpern–Schneider |\n| $M \\models \\mathbf{G}\\, p$ | Invariant (always $p$) | CTL/LTL safety fragment |\n| $M_1 \\approx_t M_2$ | Trace equivalence | $\\text{Tr}(M_1) = \\text{Tr}(M_2)$ |\n| $s_1 \\sim s_2$ | Bisimulation equivalence | Branching-time |\n| $\\square \\varphi$ | \"Always $\\varphi$\" (LTL) | Linear safety operator |\n| $\\lozenge \\varphi$ | \"Eventually $\\varphi$\" (LTL) | Liveness operator |\n| $Tr_{fin}(M)$ | Finite traces of $M$ | Subset of $Act^{*}$ |\n| $\\vert BadPref(P) \\vert$ | Cardinality of bad-prefix set | Used in bounded verification |\n\n> Real-world engineering utility: Trace equivalence is the formal basis of **interface contract checking** in embedded systems (e.g., AUTOSAR), and safety properties underpin **DO-178C** avionics certification, **ISO 26262** automotive functional safety, and **IEC 61508** industrial safety standards.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: Equivalence of Two Safety Property Characterisations\n\nWe prove the foundational identity used in every KTU Part-B question on safety.\n\n> **Claim:** A property $P \\subseteq \\Sigma^{\\omega}$ is a safety property **iff**\n> $$P \\;=\\; \\Sigma^{\\omega} \\setminus BP(P) \\cdot \\Sigma^{\\omega}$$\n\n**Proof (Standard):**\n\n*Step 1 — Direction $(\\Rightarrow)$:*\nAssume $P$ is a safety property. By definition, for every $\\pi \\notin P$, there is a finite bad prefix $\\hat{\\pi} \\in BP(P)$. Therefore $\\pi \\in BP(P) \\cdot \\Sigma^{\\omega}$.\n\n*Step 2 — Direction $(\\Leftarrow)$:*\nAssume $P = \\Sigma^{\\omega} \\setminus BP(P) \\cdot \\Sigma^{\\omega}$. Take any $\\pi \\notin P$. Then $\\pi \\in BP(P) \\cdot \\Sigma^{\\omega}$, meaning there exists a finite prefix $\\pi[0..n] \\in BP(P)$. By definition of bad prefix, **every** extension $\\pi[0..n] \\cdot \\rho$ violates $P$. Hence $P$ has a finite bad prefix for every violation, so $P$ is a safety property. $\\blacksquare$\n\n---\n\n## 3.2 Analytical Derivation: Trace Set of an LTS\n\nGiven a 3-state LTS $M$ with states $\\{s_0, s_1, s_2\\}$, transitions and labels as in the diagram of SECTION 4, the trace set is computed as:\n\n$$\n\\begin{aligned}\n\\text{Tr}(s_0) &= \\{a\\} \\cdot \\text{Tr}(s_1) \\;\\cup\\; \\{a\\} \\cdot \\text{Tr}(s_2) \\\\[4pt]\n\\text{Tr}(s_1) &= \\{\\varepsilon\\} \\;\\cup\\; \\{b\\} \\cdot \\text{Tr}(s_1) \\;\\cup\\; \\{b\\} \\cdot \\text{Tr}(s_2) \\\\[4pt]\n\\text{Tr}(s_2) &= \\{\\varepsilon\\} \\;\\cup\\; \\{c\\} \\cdot \\text{Tr}(s_2)\n\\end{aligned}\n$$\n\nSolving the recursive equations by the **Kleene fixed-point theorem** applied to finite automata:\n\n$$\n\\begin{aligned}\n\\text{Tr}(s_2) &= c^{*} \\\\\n\\text{Tr}(s_1) &= b^{*} \\;\\cup\\; b^{+} \\cdot c^{*} \\;=\\; b^{*} \\cdot (c^{*}) \\\\\n\\text{Tr}(s_0) &= a \\cdot b^{*} \\cdot c^{*}\n\\end{aligned}\n$$\n\n> **Final closed form:** $\\text{Tr}(M) = a \\cdot b^{*} \\cdot c^{*}$\n\n---\n\n## 3.3 Decision Procedure for Trace Equivalence\n\n> **Theorem (Trace Equivalence is Decidable for Finite LTS).**\n\nGiven finite LTSs $M_1, M_2$, the following steps yield a decision:\n\n$$\n\\begin{aligned}\n\\text{Step 1:} \\quad & \\text{Compute deterministic product } M_1 \\times M_2. \\\\[2pt]\n\\text{Step 2:} \\quad & \\text{Project to the synchronised alphabet } Act. \\\\[2pt]\n\\text{Step 3:} \\quad & \\text{Build the canonical DFA } A_{\\cap} = \\text{Det}(\\text{LTL}(M_1) \\cup \\text{LTL}(M_2)). \\\\[2pt]\n\\text{Step 4:} \\quad & \\text{Check emptiness: } A_{\\cap} = \\emptyset \\;\\Leftrightarrow\\; M_1 \\approx_t M_2.\n\\end{aligned}\n$$\n\nThe complexity is polynomial in $\\vert S_1 \\vert \\cdot \\vert S_2 \\vert$ for finite-state systems.\n\n---\n\n## 3.4 Verification of a Safety Property — Worked Example\n\n> **Problem:** Let $M$ be a model and $P = \\mathbf{G}\\,\\neg q$ (invariant: $q$ never holds). Verify that $M \\models P$.\n\n$$\n\\begin{aligned}\n\\text{Compute } Reach(M) &= \\text{set of all states reachable from } s_0. \\\\[4pt]\n\\text{Compute } \\{s \\in S \\mid q \\in L(s)\\} &= \\text{set of states violating } \\neg q. \\\\[4pt]\n\\text{If } Reach(M) \\cap \\{s \\mid q \\in L(s)\\} &= \\emptyset, \\text{ then } M \\models \\mathbf{G}\\,\\neg q.\n\\end{aligned}\n$$\n\nThis is the standard **invariant-checking** reduction, which runs in $O(\\vert S \\vert + \\vert \\rightarrow \\vert)$.\n\n---\n\n## 3.5 Distinguishing Bisimulation vs Trace Equivalence — Counterexample\n\nConsider the LTSs:\n\n- $M_1$: $s_0 \\xrightarrow{a} s_1$, $s_0 \\xrightarrow{a} s_2$\n- $M_2$: $s_0' \\xrightarrow{a} s_1'$, $s_0' \\xrightarrow{a} s_2'$, plus $s_1' \\xrightarrow{a} s_3'$ and $s_2' \\xrightarrow{a} s_3'$\n\n**Trace sets:**\n$$\n\\begin{aligned}\n\\text{Tr}(M_1) &= \\{a,\\; a\\cdot a\\} \\\\\n\\text{Tr}(M_2) &= \\{a,\\; a\\cdot a\\}\n\\end{aligned}\n$$\n\nTherefore $M_1 \\approx_t M_2$. However, the *branching structure* differs, so $M_1 \\not\\sim M_2$ in general (depends on the precise transitions). This proves the strictness of the equivalence hierarchy.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Equivalence Hierarchy (Mermaid)\n\n```mermaid\ngraph TD\n    A[Isomorphism] --> B[Strong Bisimulation]\n    B --> C[Weak Bisimulation]\n    C --> D[Trace Equivalence]\n    D --> E[Testing Equivalence]\n    E --> F[Failure Trace Equivalence]\n    B -. \"preserves branching\" .- G[CTL Star]\n    D -. \"linear time\" .- H[LTL]\n    C -. \"abstracts tau\" .- I[Internal Actions]\n    style A fill:#FFD700,stroke:#000\n    style B fill:#90EE90,stroke:#000\n    style C fill:#87CEEB,stroke:#000\n    style D fill:#FFA07A,stroke:#000\n    style E fill:#DDA0DD,stroke:#000\n    style F fill:#F0E68C,stroke:#000\n```\n\n## 4.2 Trace Computation Flow (Mermaid)\n\n```mermaid\ngraph LR\n    A[LTS M with states S] --> B[Identify initial state s0]\n    B --> C[DFS or BFS over transitions]\n    C --> D[Record label sequence per path]\n    D --> E[Union over all paths = Tr M]\n    E --> F{Compare with Tr M2}\n    F -- \"Tr M1 equals Tr M2\" --> G[M1 is trace equivalent to M2]\n    F -- \"Tr M1 differs Tr M2\" --> H[Distinguishing trace exists]\n    style G fill:#90EE90,stroke:#000\n    style H fill:#FF6347,stroke:#000\n```\n\n## 4.3 Property Decomposition: Alpern–Schneider Theorem (Mermaid)\n\n```mermaid\ngraph TD\n    P[Property P over Sigma omega] --> Q{Does a finite<br/>bad prefix exist?}\n    Q -- YES --> S[SAFETY PROPERTY]\n    Q -- NO  --> L[LIVENESS PROPERTY]\n    S --> I[Invariant: G p]\n    S --> G2[Guarantee: F q]\n    L --> EV[Eventuality: F r]\n    L --> PR[Persistence: G F r]\n    I --> BAD1[BadPrefix = states where not p]\n    G2 --> BAD2[BadPrefix = paths ending without q]\n    style S fill:#90EE90,stroke:#000\n    style L fill:#FFB6C1,stroke:#000\n    style I fill:#ADD8E6,stroke:#000\n    style G2 fill:#ADD8E6,stroke:#000\n```\n\n## 4.4 Safety Property Verification Architecture (Mermaid)\n\n```mermaid\ngraph TB\n    A[Model M] --> B[Graph Reachability Engine]\n    C[Safety Property P] --> D[Bad Prefix Automaton B]\n    D --> E[Product Construction]\n    B --> E\n    E --> F{Is accepting<br/>state reachable?}\n    F -- NO  --> G[P HOLDS in M]\n    F -- YES --> H[Counterexample trace<br/>bad prefix returned]\n    G --> I[Verification PASS]\n    H --> J[Verification FAIL]\n    style I fill:#90EE90,stroke:#000\n    style J fill:#FF6347,stroke:#000\n```\n\n## 4.5 Example LTS for Trace Computation (Mermaid)\n\n```mermaid\ngraph LR\n    s0((s0)) -- a --> s1((s1))\n    s0 -- a --> s2((s2))\n    s1 -- b --> s1\n    s1 -- b --> s2\n    s2 -- c --> s2\n    style s0 fill:#FFD700,stroke:#000\n    style s1 fill:#87CEEB,stroke:#000\n    style s2 fill:#FFA07A,stroke:#000\n```\n\n> **Reading the diagram:** The LTS has initial state $s_0$, two outgoing $a$-transitions to $s_1$ and $s_2$, looping $b$-transitions at $s_1$, and a self-loop $c$ at $s_2$. This is the same LTS used in SECTION 3.2 for the worked derivation.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **Q1. [KTU University Exam – Dec 2023, Model 1]**\n> Define *trace equivalence* between two labelled transition systems. How does it differ from bisimulation equivalence?\n\n**Model Answer (3 marks):**\n- **(1 mark)** Two LTSs $M_1, M_2$ are trace equivalent, $M_1 \\approx_t M_2$, if they have the same set of observable execution traces: $\\text{Tr}(M_1) = \\text{Tr}(M_2)$.\n- **(1 mark)** A trace is a sequence of actions (labels) along an execution, not the sequence of states.\n- **(1 mark)** Bisimulation additionally requires *branching structure* preservation (matching of outgoing transitions at every step), so bisimulation is strictly finer; bisimilar $\\Rightarrow$ trace equivalent, but not conversely.\n\n---\n\n> **Q2. [KTU University Exam – May 2024, Model 1]**\n> What is a *safety property*? Give one example from a real-time or embedded system.\n\n**Model Answer (3 marks):**\n- **(1 mark)** A safety property states that \"something bad never happens\" — every violation has a finite bad prefix that is itself a witness.\n- **(1 mark)** Formal definition: $P$ is safety iff $\\forall \\pi \\notin P : \\exists n : \\forall \\rho : \\pi[0..n]\\rho \\notin P$.\n- **(1 mark)** Example: *\"The aircraft autopilot must never command full throttle when altitude < 100 ft\"* (avionics DO-178C), or in LTL: $\\mathbf{G}(\\text{altitude} < 100 \\rightarrow \\neg \\text{fullThrottle})$.\n\n---\n\n## 5.2 Part B — Module Internal Choice (14 Marks Each)\n\n> ### Question A (14 Marks)\n> **[KTU University Exam – June 2024, Adapted]**\n>\n> **(a)** [7 Marks] Define *trace equivalence* and *weak trace equivalence* for LTSs. Prove or disprove: *\"If $M_1$ is weakly bisimilar to $M_2$, then $M_1$ is trace equivalent to $M_2$.\"*\n>\n> **(b)** [7 Marks] Consider the LTSs $M_1$ and $M_2$ below. Compute $\\text{Tr}(M_1)$ and $\\text{Tr}(M_2)$ in closed-form regular expressions and determine whether $M_1 \\approx_t M_2$.\n\n**Model Solution:**\n\n**(a) [7 marks]**\n- **Definition of trace equivalence (1 mark):** $M_1 \\approx_t M_2 \\iff \\text{Tr}(M_1) = \\text{Tr}(M_2)$ where $\\text{Tr}(M)$ is the set of all finite/infinite label sequences from initial state.\n- **Definition of weak trace equivalence (2 marks):** $M_1 \\approx_{wt} M_2 \\iff \\text{Tr}_{\\Rightarrow}(M_1) = \\text{Tr}_{\\Rightarrow}(M_2)$ where $s \\overset{a}{\\Rightarrow} s' \\iff s \\xrightarrow{\\tau^{*} a \\tau^{*}} s'$. The internal $\\tau$ is abstracted.\n- **Proof of the claim (4 marks):** *Yes, the claim is true.* By the equivalence hierarchy theorem, weak bisimulation $\\sqsubseteq$ trace equivalence. Proof sketch: if $M_1 \\approx_w M_2$, then for every $s_1$ reachable in $M_1$ there exists $s_2$ in $M_2$ such that $s_1 \\approx_w s_2$. Hence every weakly labelled sequence in $M_1$ has a corresponding one in $M_2$ (and vice versa by symmetry of bisimulation). Therefore the set of weak traces is identical: $M_1 \\approx_{wt} M_2$.\n\n**(b) [7 marks]**\nLet the two LTSs share the alphabet $\\{a,b,c\\}$ and have transitions:\n- $M_1$: $s_0 \\xrightarrow{a} s_1$, $s_1 \\xrightarrow{b} s_1$, $s_1 \\xrightarrow{b} s_2$, $s_2 \\xrightarrow{c} s_2$\n- $M_2$: $t_0 \\xrightarrow{a} t_1$, $t_0 \\xrightarrow{a} t_2$, $t_1 \\xrightarrow{b} t_1$, $t_2 \\xrightarrow{b} t_2$, $t_1 \\xrightarrow{c} t_1$, $t_2 \\xrightarrow{c} t_2$\n\n**Step 1 — Trace of $M_1$ (3 marks):** Following SECTION 3.2's derivation:\n$$\\text{Tr}(M_1) = a \\cdot b^{*} \\cdot c^{*}$$\n\n**Step 2 — Trace of $M_2$ (2 marks):** Two non-deterministic branches:\n$$\\text{Tr}(M_2) = a \\cdot b^{*} \\;\\cup\\; a \\cdot c^{*} \\;=\\; a \\cdot (b^{*} \\cup c^{*})$$\n\n**Step 3 — Comparison (2 marks):** $a\\cdot b^{*} \\cdot c^{*} \\neq a \\cdot (b^{*} \\cup c^{*})$ because trace $a \\cdot b \\cdot b \\notin \\text{Tr}(M_1)$ but $a \\cdot b \\cdot b \\in \\text{Tr}(M_2)$ (if $b$ is followed by $b$ alone, the $c^{*}$-path of $M_1$ does not match). **Conclusion:** $M_1 \\not\\approx_t M_2$.\n\n---\n\n> ### Question B (14 Marks)\n> **[KTU University Exam – Dec 2023, Adapted]**\n>\n> **(a)** [7 Marks] State and prove the Alpern–Schneider characterisation of safety properties.\n>\n> **(b)** [7 Marks] Show how safety properties are verified via *bad-prefix Büchi automata* construction. Use $\\mathbf{G}\\,\\neg q$ as a running example.\n\n**Model Solution:**\n\n**(a) [7 marks]**\n- **Statement (2 marks):** $P \\subseteq \\Sigma^{\\omega}$ is a safety property iff $P = \\Sigma^{\\omega} \\setminus BP(P) \\cdot \\Sigma^{\\omega}$, where $BP(P)$ is the set of all finite bad prefixes of $P$.\n- **Definition of bad prefix (1 mark):** A finite word $\\sigma$ is a bad prefix of $P$ if every infinite extension of $\\sigma$ violates $P$.\n- **Proof $(\\Rightarrow)$ (2 marks):** Suppose $P$ is a safety property and $\\pi \\notin P$. By definition there exists a finite $n$ such that for all $\\rho$, $\\pi[0..n]\\rho \\notin P$. Hence $\\pi[0..n] \\in BP(P)$ and $\\pi \\in BP(P) \\cdot \\Sigma^{\\omega}$. Thus $P^{c} \\subseteq BP(P)\\cdot \\Sigma^{\\omega}$. The reverse inclusion holds by definition of bad prefix. Hence $P = \\Sigma^{\\omega} \\setminus BP(P)\\cdot \\Sigma^{\\omega}$.\n- **Proof $(\\Leftarrow)$ (2 marks):** Conversely, suppose $P = \\Sigma^{\\omega} \\setminus BP(P)\\cdot\\Sigma^{\\omega}$. For any $\\pi \\notin P$, $\\pi$ belongs to $BP(P)\\cdot\\Sigma^{\\omega}$, so a finite prefix $\\hat{\\pi} \\in BP(P)$ exists. Since $\\hat{\\pi}$ is a bad prefix, all extensions of $\\hat{\\pi}$ also lie outside $P$. Hence $P$ satisfies the safety definition.\n\n**(b) [7 marks]**\n- **Büchi acceptance for safety (1 mark):** A safety property can be accepted by a *deterministic* Büchi automaton whose accepting states are those that have *not* yet encountered a bad prefix.\n- **Construction for $\\mathbf{G}\\,\\neg q$ (2 marks):** The automaton $A_{\\neg \\mathbf{G} q} = A_{\\mathbf{F} q}$ has states $\\{q, \\neg q\\}$, initial state $\\neg q$, transitions: on action $q$ go to state $q$ (and stay); on $\\neg q$ stay in $\\neg q$. State $q$ is the *only* accepting state — a *bad prefix* is any word that first reaches $q$.\n- **Product construction (2 marks):** The model checker computes $M \\otimes A_{\\neg \\mathbf{G} q}$. The synchronous product's Büchi emptiness check decides $M \\models \\mathbf{G}\\,\\neg q$ iff the product has no accepting cycle.\n- **Algorithmic outcome (2 marks):** Reachability of the accepting state $\\Rightarrow$ counterexample trace (a bad prefix, e.g., the first time $q$ becomes true). If unreachable, the safety property holds. This reduction to graph reachability is what makes safety checking *linear* in the size of $M$ and polynomial overall.\n\n> [!WARNING]\n> **KTU Examiner's Pitfall Callout:** Students commonly confuse the direction of the Alpern–Schneider proof. The **$(\\Leftarrow)$ direction** (showing that the closed-form equation implies safety) is *more often* incorrectly argued. Marks are lost for: (i) using \"$\\pi$\" to mean both a trace and its length simultaneously, (ii) forgetting that the bad-prefix set must be **prefix-closed** (if $\\sigma$ is bad, every prefix is also bad), and (iii) failing to mention that the automaton is **deterministic** for safety properties — a crucial fact for verification complexity analysis.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!NOTE]\n> **Rapid Revision Checklist (Print and Carry to Exam Hall)**\n\n- ✅ **Trace** = sequence of *labels* (actions) along an execution; not the sequence of states.\n- ✅ **Trace equivalence** = equality of trace *sets*; coarsest of the standard equivalences.\n- ✅ **Strong trace equivalence** treats $\\tau$ as observable; **weak trace equivalence** hides $\\tau$.\n- ✅ **Bisimulation** is strictly finer than trace equivalence — preserves branching structure, not just linear behaviour.\n- ✅ **Safety property** = \"something bad never happens\"; every violation has a *finite* bad prefix.\n- ✅ **Liveness property** = \"something good eventually happens\"; no finite bad prefix exists.\n- ✅ **Alpern–Schneider Theorem**: every property is the intersection of one safety and one liveness property.\n- ✅ Invariants $\\mathbf{G}\\,p$ and guarantees $\\mathbf{F}\\,q$ are *safety*; eventualities $\\mathbf{F}\\,r$ and persistence $\\mathbf{G}\\mathbf{F}\\,r$ are *liveness*.\n- ✅ Safety property verification reduces to **reachability** of a bad-prefix state — polynomial-time for finite models.\n- ✅ The standard KTU 14-mark question pattern: (a) definition + theorem, (b) worked example or counterexample distinguishing trace equivalence from bisimulation.\n- ✅ Common exam trap: stating that bisimulation and trace equivalence coincide — they do **not**; a small non-deterministic choice without differing traces is a counterexample.\n- ✅ Linear-time logics (LTL) cannot distinguish bisimilar systems that differ in trace sets, but bisimulation logics (CTL*) can.\n- ✅ Memorise: $M_1 \\sim M_2 \\;\\Rightarrow\\; M_1 \\approx_t M_2 \\;\\Rightarrow\\; M_1 \\approx_{test} M_2$ (strict chain).\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t24",
          "title": "Liveness Properties - Liveness Property",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Liveness Properties - Liveness Property\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\nIn **Model Checking** and **Temporal Logic**, a **Liveness Property** is a class of linear-time properties that guarantees that \"**something good will eventually happen**\" along every execution path of a system. Formally, in the context of **Linear Temporal Logic (LTL)** and **Kripke Structures**, a property $P \\subseteq \\Sigma^\\omega$ (over infinite words/sequences) is a **liveness property** if and only if for every finite prefix $u \\in \\Sigma^*$, there exists a continuation $v \\in \\Sigma^\\omega$ such that the concatenation $u \\cdot v \\in P$.\n\nMathematically, the condition is:\n\n$$\\forall u \\in \\Sigma^*, \\quad \\exists v \\in \\Sigma^\\omega \\mid u \\cdot v \\in P$$\n\nThis captures the essential intuition that **no finite observation** of a system's behavior is sufficient to conclude that a liveness property has been **violated** — because one can always extend the prefix in some manner that eventually satisfies the property.\n\n> [!IMPORTANT]\n> **KTU 2024 Highlight — Liveness vs. Safety**\n> The **Safety-Liveness Classification** (proposed by **Lamport**, formalized by **Alpern & Schneider**) is a *canonical* classification of all linear-time properties. **Every** linear-time property $P$ can be expressed as the intersection of a safety property and a liveness property:\n> $$P = \\text{Safety}(P) \\cap \\text{Liveness}(P)$$\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine a **busy post office counter**:\n\n- **Safety Property Analogy**: \"The counter will *never* dispense money without receiving a payment first.\" — This is a **bad thing that must never happen**; we can verify it by examining a finite prefix of behavior. If the violation occurs, a finite trace is enough to witness it.\n\n- **Liveness Property Analogy**: \"Every customer who joins the queue will *eventually* be served.\" — This is a **good thing that must eventually happen**. Even if you observe a customer waiting for 10 minutes, you cannot conclude from any *finite* observation that the liveness property has failed — perhaps they will be served at minute 11, or minute 1000.\n\n> [!NOTE]\n> **Intuitive Distinction (KTU Board Exam Favorite)**:\n> - **Safety** = \"**Nothing bad ever happens**\" → Verifiable on **finite** traces\n> - **Liveness** = \"**Something good eventually happens**\" → Requires **infinite** traces for verification\n\n## 1.3 Key Engineering Metrics and Constants\n\n| Metric / Constant | Symbol | Value / Meaning |\n|---|---|---|\n| Time Domain | $\\mathbb{N}$ or $\\mathbb{R}_{\\geq 0}$ | Discrete / continuous time for temporal logic |\n| Trace Alphabet | $\\Sigma$ | Set of atomic propositions / states |\n| Finite Prefixes | $\\Sigma^*$ | Kleene star of the alphabet |\n| Infinite Traces | $\\Sigma^\\omega$ | $\\omega$-word set (infinite behaviors) |\n| **Büchi Acceptance** | $\\mathcal{F}$ | Set of accepting states for liveness |\n\n> [!NOTE]\n> **Liveness in Büchi Automata**: Liveness properties are precisely those that can be recognized by **Büchi Automata** (a finite automaton over infinite words). A Büchi automaton accepts an infinite word if it visits its accepting set $F$ **infinitely often**.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Structured Theoretical Breakdown\n\n### The Five Pillars of Liveness Property Theory\n\n**Pillar 1: Topological Characterization (Alpern-Schneider)**\n- The set of all infinite traces $\\Sigma^\\omega$ is endowed with the **Cantor topology**.\n- A property $P$ is **liveness** if and only if $P$ is a **dense** subset of $\\Sigma^\\omega$ in this topology.\n- **Why?** Density captures the intuition that every finite prefix can be \"completed\" to satisfy $P$ — there is no \"forbidden\" finite prefix.\n\n**Pillar 2: Closure Properties**\n- Liveness properties are **closed under finite and infinite intersections** (i.e., under conjunction $\\land$).\n- Liveness properties are **closed under finite and infinite unions** (i.e., under disjunction $\\lor$).\n- **Engineering utility**: This allows composition — combining multiple liveness requirements yields another liveness requirement.\n\n**Pillar 3: Density Decomposition Theorem**\n- Every property $P$ can be uniquely decomposed (with respect to a fixed \"natural\" property used as a basis) as:\n$$P = S \\cap L$$\nwhere $S$ is a **safety property** (closed in the Cantor topology) and $L$ is a **liveness property** (dense in the Cantor topology).\n\n**Pillar 4: Liveness as a Limit of Stutter**\n- A liveness property $P$ is one such that for any finite word $u$, the set of extensions $u \\cdot \\Sigma^\\omega$ is a **\"liveness subset\"** of $P$.\n- Equivalently: $P$ cannot be refuted by any finite trace.\n\n**Pillar 5: Liveness Automata (Büchi Acceptance)**\n- A **Büchi Automaton** $\\mathcal{B} = (Q, \\Sigma, \\delta, Q_0, F)$ accepts an infinite word $w = a_0 a_1 a_2 \\dots$ if there exists a run $\\rho$ such that:\n$$\\text{Inf}(\\rho) \\cap F \\neq \\emptyset$$\n- $F$ is the set of accepting states; $\\text{Inf}(\\rho)$ is the set of states visited infinitely often.\n\n## 2.2 Why and How — Pedagogical Deep Dive\n\n| Step | Why (Motivation) | How (Mechanism) |\n|------|------------------|------------------|\n| Define liveness | Need to verify *\"eventually\"* properties | Use infinite traces in $\\Sigma^\\omega$ |\n| Topological view | Reframe verification geometrically | Cantor topology $\\to$ open/closed/dense sets |\n| Büchi acceptance | Finite representation of $\\omega$-languages | Automaton visits $F$ infinitely often |\n| Safety-Liveness split | Decompose verification problem | $P = S \\cap L$ unique decomposition |\n| Stuttering closure | Hardware-level liveness analysis | Equivalence under stutter-equivalent traces |\n\n## 2.3 KTU Formula Sheet / Cheat Sheet\n\n| # | Formula / Theorem | Statement | Application |\n|---|---|---|---|\n| 1 | Liveness Definition | $\\forall u \\in \\Sigma^*, \\exists v \\in \\Sigma^\\omega \\mid u \\cdot v \\in P$ | Verifying eventual properties |\n| 2 | Density Characterization | $P$ is liveness $\\iff \\overline{P} = \\Sigma^\\omega$ (closure = whole space) | Topological test |\n| 3 | Safety-Liveness Decomposition | $P = \\text{Safety}(P) \\cap \\text{Liveness}(P)$ | Modular verification |\n| 4 | Büchi Acceptance | $\\text{Inf}(\\rho) \\cap F \\neq \\emptyset$ | LTL to automata translation |\n| 5 | LTL Liveness Pattern | $\\mathbf{G}(p \\rightarrow \\mathbf{F} q)$ | \"Every $p$ is eventually followed by $q$\" |\n| 6 | Liveness $\\subseteq$ Topological Dense Sets | $P \\text{ is liveness} \\iff P \\text{ is dense}$ | Theoretical check |\n| 7 | Liveness is limit-closed | $\\bigcap_{i} P_i$ of liveness sets is liveness | Infinite conjunction closure |\n| 8 | Reachability Liveness | $\\mathbf{F} p$ (eventually $p$) | Basic liveness formula |\n| 9 | Recurrence Liveness | $\\mathbf{G} \\mathbf{F} p$ ($p$ holds infinitely often) | Strong liveness |\n| 10 | Persistence Liveness | $\\mathbf{F} \\mathbf{G} p$ (eventually $p$ always) | Stability property |\n\n> [!IMPORTANT]\n> **Caveat for Markdown Rendering**: All set-membership / divisibility bars use $\\vert$ or $\\mid$ to avoid breaking table syntax. For example, \"$\\vert x \\vert$\" is written as $\\lvert x \\rvert$ inside the LaTeX math mode.\n\n## 2.4 Real-World Engineering Utility\n\nLiveness properties are foundational in:\n\n- **Hardware Verification**: Proving that a circuit's request line is *eventually* acknowledged (used in **Intel's** and **IBM's** formal verification flows for cache coherence protocols).\n- **Protocol Verification**: Mutual exclusion (no two processes in critical section — **safety**); every process *eventually* enters the critical section — **liveness**).\n- **Operating Systems**: Deadlock-freedom (liveness) and no partial deadlock (liveness combined with safety).\n- **Embedded / Cyber-Physical Systems**: Sensor data is *eventually* delivered to the controller.\n- **Distributed Systems**: Consensus protocols (e.g., Paxos, Raft) must guarantee **eventual agreement** — a liveness property under the FLP impossibility result.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation 1: Equivalence of Liveness Definitions\n\n**Claim**: A property $P \\subseteq \\Sigma^\\omega$ is a liveness property **if and only if** for every finite word $u \\in \\Sigma^*$, there exists an infinite continuation $v \\in \\Sigma^\\omega$ such that $u \\cdot v \\in P$.\n\nWe prove the equivalence by establishing the topological correspondence.\n\n### Step 1 — Setup of Cantor Topology\n\nDefine the metric $d$ on $\\Sigma^\\omega$ for two infinite words $w_1, w_2$:\n\n$$\n\\begin{aligned}\nd(w_1, w_2) &= 2^{-n}, \\quad \\text{where } n = \\max\\{ k \\mid w_1[0..k] = w_2[0..k] \\} \\\\\n&\\text{with convention } n = \\infty \\implies d = 0\n\\end{aligned}\n$$\n\nThis metric induces the **Cantor topology** on $\\Sigma^\\omega$. The open balls in this topology are precisely the cylinder sets of the form:\n\n$$\nu \\cdot \\Sigma^\\omega = \\{ w \\in \\Sigma^\\omega \\mid w \\text{ has prefix } u \\}\n$$\n\nwhere $u \\in \\Sigma^*$.\n\n### Step 2 — Density Characterization\n\n**Theorem**: $P$ is liveness $\\iff$ $P$ is dense in $\\Sigma^\\omega$.\n\n**($\\Rightarrow$ Direction)**: Suppose $P$ is liveness, i.e., $\\forall u \\in \\Sigma^*, \\exists v \\in \\Sigma^\\omega \\mid u \\cdot v \\in P$. For any non-empty open set $u \\cdot \\Sigma^\\omega$ in the topology, there exists some element of $P$ in it (namely $u \\cdot v$). Hence $P$ intersects every non-empty open set, which is the definition of a **dense set**.\n\n**($\\Leftarrow$ Direction)**: Suppose $P$ is dense. For any $u \\in \\Sigma^*$, the cylinder set $u \\cdot \\Sigma^\\omega$ is non-empty and open. By density of $P$, there exists $w \\in P \\cap (u \\cdot \\Sigma^\\omega)$. Writing $w = u \\cdot v$ for some $v$, we conclude $\\exists v \\mid u \\cdot v \\in P$. Hence $P$ is liveness.\n\n### Step 3 — Closure of Liveness\n\n**Claim**: The class $\\mathcal{L}$ of all liveness properties is closed under arbitrary intersections.\n\n**Proof**: Let $\\{ P_i \\}_{i \\in I}$ be a family of liveness properties. Let $u \\in \\Sigma^*$ be arbitrary. For each $i$, since $P_i$ is liveness, $\\exists v_i \\mid u \\cdot v_i \\in P_i$. By the axiom of choice, select one such $v_i$ for each $i$. Now define:\n\n$$\n\\begin{aligned}\nP &= \\bigcap_{i \\in I} P_i \\\\\nu \\cdot \\Sigma^\\omega \\cap P &= u \\cdot \\Sigma^\\omega \\cap \\bigcap_i P_i \\\\\n&= \\bigcap_i (u \\cdot \\Sigma^\\omega \\cap P_i) \\\\\n&\\text{(each set in the intersection is non-empty)} \\\\\n&\\neq \\emptyset\n\\end{aligned}\n$$\n\nHence $P$ is liveness. $\\blacksquare$\n\n## 3.2 Derivation 2: Safety-Liveness Decomposition (Alpern-Schneider)\n\n**Theorem**: For any property $P \\subseteq \\Sigma^\\omega$, there exist a safety property $S$ and a liveness property $L$ such that $P = S \\cap L$.\n\n### Step 1 — Construct the Safety Part\n\nThe **safety closure** of $P$ is:\n\n$$\n\\text{Safety}(P) = \\bigcup \\{ u \\cdot \\Sigma^\\omega \\mid \\forall v, u \\cdot v \\in P \\}\n$$\n\nIn words, $\\text{Safety}(P)$ contains all infinite words $w$ such that *every* finite prefix of $w$ can be extended to an element of $P$.\n\n### Step 2 — Construct the Liveness Part\n\nThe **liveness part** is:\n\n$$\n\\text{Liveness}(P) = P\n$$\n\n### Step 3 — Show the Decomposition\n\nWe claim $P = \\text{Safety}(P) \\cap P$. The key observation is that $\\text{Safety}(P) \\supseteq P$ trivially (every $w \\in P$ has all its prefixes extendable to $w$ itself). So:\n\n$$\n\\begin{aligned}\nP &\\subseteq \\text{Safety}(P) \\cap P \\\\\n\\text{Conversely, } w \\in \\text{Safety}(P) \\cap P &\\Rightarrow w \\in P\n\\end{aligned}\n$$\n\n### Step 4 — Verify Liveness of the Liveness Part\n\nThe set $P$ itself is liveness **relative to the trace set**, but for the *absolute* decomposition, we use:\n\n$$\n\\text{Liveness}(P) = \\{ w \\in \\Sigma^\\omega \\mid \\text{no finite prefix of } w \\text{ \"forces\" violation} \\}\n$$\n\nA canonical construction is: $L = P \\cup (\\Sigma^\\omega \\setminus \\text{Safety}(P))$.\n\nThis is dense, hence liveness, and $P = S \\cap L$ holds. $\\blacksquare$\n\n## 3.3 Analytical Matrix — Common Liveness LTL Patterns\n\n| LTL Formula | Liveness Type | Meaning | Example Application |\n|---|---|---|---|\n| $\\mathbf{F}\\,p$ | Reachability | $p$ eventually holds | \"Message is eventually delivered\" |\n| $\\mathbf{G}(p \\rightarrow \\mathbf{F}\\,q)$ | Response | Every $p$ is followed by $q$ | \"Every request is acknowledged\" |\n| $\\mathbf{G}\\mathbf{F}\\,p$ | Recurrence | $p$ occurs infinitely often | \"Process enters CS infinitely often\" |\n| $\\mathbf{F}\\mathbf{G}\\,p$ | Persistence | Eventually $p$ holds forever | \"System eventually stabilizes\" |\n| $\\mathbf{G}\\mathbf{F}\\,p \\rightarrow \\mathbf{G}\\mathbf{F}\\,q$ | Conditional recurrence | Recurrence implication | \"If starvation occurs, recovery also recurs\" |\n\n## 3.4 Step-by-Step Worked Example: Verifying a Simple Liveness Property\n\n**Problem**: Consider a Kripke structure with two states $s_0, s_1$, both atomic propositions $p, q$ initially false, with transitions $s_0 \\to s_1$ and $s_1 \\to s_1$, and $p$ becomes true at $s_1$. Verify the liveness property $\\mathbf{F}\\,p$.\n\n**Solution Path**:\n\n$$\n\\begin{aligned}\n\\text{Step 1:} &\\quad \\text{List all infinite paths from initial state } s_0 \\\\\n& \\quad \\pi_1 = s_0 s_1 s_1 s_1 \\dots \\\\\n& \\quad \\pi_2 = s_0 s_0 s_1 s_1 \\dots \\quad \\text{(if self-loop on } s_0 \\text{ exists)} \\\\\n\\text{Step 2:} &\\quad \\text{Check } \\mathbf{F}\\,p \\text{ on each path} \\\\\n& \\quad \\pi_1 \\models \\mathbf{F}\\,p \\text{ since } s_1 \\text{ (at time 1) has } p = \\text{true} \\\\\n& \\quad \\pi_2 \\models \\mathbf{F}\\,p \\text{ since } s_1 \\text{ (at time 2) has } p = \\text{true} \\\\\n\\text{Step 3:} &\\quad \\text{Since all paths satisfy } \\mathbf{F}\\,p, \\text{ the system satisfies the liveness property} \\\\\n\\text{Step 4:} &\\quad \\text{Büchi automaton for } \\mathbf{F}\\,p \\text{ has accepting state at } p = \\text{true}\n\\end{aligned}\n$$\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Mermaid Diagram: Liveness Property Verification Flow\n\n```mermaid\nflowchart TD\n    A[System Model Kripke Structure M] --> B[Specify Liveness Property P in LTL]\n    B --> C{Property Type?}\n    C -->|Reachability| D[Construct Buchi Automaton for F p]\n    C -->|Response| E[Construct Buchi Automaton for G p implies F q]\n    C -->|Recurrence| F[Construct Buchi Automaton for GF p]\n    D --> G[Product Automaton M times B]\n    E --> G\n    F --> G\n    G --> H[Emptiness Check via Nested DFS]\n    H --> I{Is Product Empty?}\n    I -->|Yes| J[PROPERTY SATISFIED System is Live]\n    I -->|No| K[PROPERTY VIOLATED Generate Counterexample Trace]\n    K --> L[Witness Finite Prefix Violating Liveness]\n    L --> M[Refine Model or Property]\n    M --> A\n    J --> N[Verification Complete]\n```\n\n## 4.2 Mermaid Diagram: Safety-Liveness Classification Hierarchy\n\n```mermaid\nflowchart TD\n    A[All Linear Time Properties P subset of Sigma omega] --> B{Finite Trace Witness for Violation?}\n    B -->|Yes| C[Safety Property]\n    B -->|No| D[Liveness Property]\n    C --> E[Invariant G p]\n    C --> F[Mutual Exclusion]\n    C --> G[Type Checking]\n    D --> H[Reachability F p]\n    D --> I[Response G p implies F q]\n    D --> J[Recurrence GF p]\n    A --> K[Decomposition Theorem P equals S intersection L]\n    C --> K\n    D --> K\n    K --> L[Modular Verification Framework]\n```\n\n## 4.3 Mermaid Diagram: Liveness in a Distributed Protocol (Mutual Exclusion)\n\n``` 3 Nodes want Critical Section\nflowchart LR\n    N1[Node 1 Non CS] -->|request| Q1[Node 1 Waiting]\n    Q1 -->|grant from arbiter| CS1[Node 1 in CS]\n    CS1 -->|release| N1\n    N2[Node 2 Non CS] -->|request| Q2[Node 2 Waiting]\n    Q2 -->|grant| CS2[Node 2 in CS]\n    CS2 -->|release| N2\n    N3[Node 3 Non CS] -->|request| Q3[Node 3 Waiting]\n    Q3 -->|grant| CS3[Node 3 in CS]\n    CS3 -->|release| N3\n    Q1 -.->|Safety at most one in CS| SAF{{SAFETY at most 1 in CS}}\n    Q2 -.->|Safety at most one in CS| SAF\n    Q3 -.->|Safety at most one in CS| SAF\n    Q1 -.->|Liveness eventually served| LIVE{{LIVENESS GF enter CS}}\n    Q2 -.->|Liveness eventually served| LIVE\n    Q3 -.->|Liveness eventually served| LIVE\n```\n\n## 4.4 Mermaid Diagram: Büchi Automaton for Liveness $\\mathbf{F}\\,p$\n\n```mermaid\nflowchart LR\n    S0[State q0 initial not p] -->|not p| S0\n    S0 -->|p| S1[State q1 accepting p holds]\n    S1 -->|p| S1\n    S1 -->|not p| S1\n```\n\n> [!NOTE]\n> **Diagram Reading Note**: The double-circle / labeled *accepting* state $q_1$ in the Büchi automaton must be visited **infinitely often** for an infinite trace to be accepted — this is the formal meaning of the liveness property $\\mathbf{F}\\,p$ being satisfied.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### **Q1. Define a liveness property. Give one example.**\n`[KTU University Exam – Dec 2023]`\n\n**Model Answer** (3 marks):\n\nA liveness property is a linear-time property $P \\subseteq \\Sigma^\\omega$ over infinite traces such that for every finite prefix $u \\in \\Sigma^*$, there exists an infinite continuation $v \\in \\Sigma^\\omega$ with $u \\cdot v \\in P$. This means no finite observation of a system can determine the violation of a liveness property. **Example**: \"Every request is eventually acknowledged\", expressed in LTL as $\\mathbf{G}(req \\rightarrow \\mathbf{F}\\,ack)$.\n\n---\n\n### **Q2. State the Safety–Liveness decomposition theorem.**\n`[KTU University Exam – June 2024]`\n\n**Model Answer** (3 marks):\n\nThe Alpern–Schneider theorem states that **every linear-time property $P$ can be expressed as the intersection of a safety property $S$ and a liveness property $L$**, i.e., $P = S \\cap L$. The safety part captures the \"nothing bad happens\" requirement, while the liveness part captures the \"something good eventually happens\" requirement. This decomposition is **unique** and is foundational for modular verification.\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks Each, Module Internal Choice)\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Pitfall Callout**\n> - **Common Mistake 1**: Students confuse *reachability* ($\\mathbf{F}\\,p$, a liveness property) with *safety invariant* ($\\mathbf{G}\\,p$). An examiner will award **zero** marks if these are interchanged.\n> - **Common Mistake 2**: When asked to \"give an example\", students give a *safety* example for a *liveness* question. Always double-check: liveness = \"eventually\".\n> - **Common Mistake 3**: Forgetting the prefix condition in the formal definition: many students write \"a property is liveness if some good thing happens\" — this is incomplete. The **dense prefix** condition is mandatory for full marks.\n> - **Common Mistake 4**: Drawing a Büchi automaton without marking the *accepting* states with double circles or explicit labels — this loses up to 2 marks.\n\n---\n\n### **Question A (14 Marks) — Option 1**\n\n#### (a) Explain the formal definition and topological characterization of a liveness property. (7 marks)\n\n**Model Solution**:\n\nA **liveness property** $P \\subseteq \\Sigma^\\omega$ satisfies:\n\n$$\n\\forall u \\in \\Sigma^*, \\quad \\exists v \\in \\Sigma^\\omega \\mid u \\cdot v \\in P\n$$\n\nThis means that for any finite prefix, there always exists an extension that satisfies the property. In the **Cantor topology** on $\\Sigma^\\omega$, a property is liveness **if and only if it is a dense subset**. Density is captured as: for every non-empty open set $U$ (i.e., every cylinder set $u \\cdot \\Sigma^\\omega$), we have $U \\cap P \\neq \\emptyset$.\n\n**Engineering significance**:\n- Liveness corresponds to **eventual** requirements (e.g., termination, response).\n- It is **closed under conjunction and disjunction**, enabling modular specification.\n\n**Liveness in LTL**: Common patterns include $\\mathbf{F}\\,p$, $\\mathbf{G}(p \\rightarrow \\mathbf{F}\\,q)$, $\\mathbf{G}\\mathbf{F}\\,p$. These formulas are recognized by **Büchi automata**, which accept infinite words visiting an accepting set infinitely often.\n\n#### (b) Differentiate safety and liveness properties. State the Alpern–Schneider decomposition theorem and explain its significance. (7 marks)\n\n**Model Solution**:\n\n| Aspect | Safety Property | Liveness Property |\n|---|---|---|\n| Intuition | \"Nothing bad happens\" | \"Something good eventually happens\" |\n| Formal Test | Closed in Cantor topology | Dense in Cantor topology |\n| Violation Witness | Finite trace | Requires infinite trace |\n| Büchi Class | Safety automaton | Büchi automaton |\n| LTL Example | $\\mathbf{G}\\,p$ (invariant) | $\\mathbf{F}\\,p$ (reachability) |\n\n**Decomposition Theorem (Alpern–Schneider)**: Every linear-time property $P$ can be uniquely written as $P = S \\cap L$ where $S$ is safety and $L$ is liveness.\n\n$$\nP = \\underbrace{\\text{Safety}(P)}_{\\text{closed set}} \\cap \\underbrace{\\text{Liveness}(P)}_{\\text{dense set}}\n$$\n\n**Significance**: This decomposition allows verification to be **modularized**:\n1. Check safety first (often cheaper, decidable via reachability).\n2. Check liveness separately (often via cycle detection / fairness).\n\nThis is the foundation of modern **explicit-state model checkers** like **SPIN** and **nuSMV**.\n\n---\n\n### **Question B (14 Marks) — Option 2**\n\n#### (a) Discuss the role of Büchi automata in recognizing liveness properties. Construct the Büchi automaton for the LTL property $\\mathbf{G}(p \\rightarrow \\mathbf{F}\\,q)$. (7 marks)\n\n**Model Solution**:\n\nA **Büchi automaton** $\\mathcal{B} = (Q, \\Sigma, \\delta, Q_0, F)$ accepts an infinite word $w$ if there exists a run $\\rho$ such that $\\text{Inf}(\\rho) \\cap F \\neq \\emptyset$, i.e., an accepting state is visited **infinitely often**. Büchi acceptance is the canonical recognition mechanism for liveness (and indeed all $\\omega$-regular) properties.\n\n**Construction for $\\mathbf{G}(p \\rightarrow \\mathbf{F}\\,q)$** (the \"response\" pattern):\n\nThe Büchi automaton has three states: $q_0$ (initial, $p$ false), $q_1$ (waiting for $q$ after $p$), $q_2$ (accepting — $q$ has been seen).\n\nStates and transitions:\n- $q_0 \\xrightarrow{\\neg p} q_0$\n- $q_0 \\xrightarrow{p} q_1$\n- $q_1 \\xrightarrow{\\neg p} q_0$ (if $p$ not sustained, restart)\n- $q_1 \\xrightarrow{p} q_1$ (still waiting for $q$)\n- $q_1 \\xrightarrow{q} q_2$ (acceptance triggered)\n- $q_2 \\xrightarrow{*} q_2$ (loop, stays accepting)\n\n**Accepting set**: $F = \\{ q_2 \\}$.\n\nFor an infinite trace to satisfy the liveness property, the run must visit $q_2$ infinitely often, meaning **every $p$ is eventually followed by a $q$**.\n\n#### (b) Explain with examples the liveness LTL patterns: reachability, response, recurrence, and persistence. (7 marks)\n\n**Model Solution**:\n\n| Pattern | LTL Formula | Meaning | Real-world Example |\n|---|---|---|---|\n| **Reachability** | $\\mathbf{F}\\,p$ | $p$ eventually holds | \"A message is eventually delivered\" |\n| **Response** | $\\mathbf{G}(p \\rightarrow \\mathbf{F}\\,q)$ | Every $p$ triggers eventual $q$ | \"Every request is acknowledged\" |\n| **Recurrence** | $\\mathbf{G}\\mathbf{F}\\,p$ | $p$ holds infinitely often | \"A process enters CS infinitely often\" |\n| **Persistence** | $\\mathbf{F}\\mathbf{G}\\,p$ | Eventually $p$ holds forever | \"The system eventually stabilizes\" |\n\n**Distinction via nesting depth**:\n- $\\mathbf{F}$ outer: **reachability** (eventual satisfaction, may not recur)\n- $\\mathbf{G}\\mathbf{F}$: **recurrence** (must happen again and again)\n- $\\mathbf{F}\\mathbf{G}$: **persistence** (eventually becomes invariant)\n\n**Worked example**: In a **token-ring protocol**:\n- *Liveness 1 (Reachability)*: $\\mathbf{F}\\,(\\text{token at node}_1)$ — the token eventually reaches node 1.\n- *Liveness 2 (Recurrence)*: $\\mathbf{G}\\mathbf{F}\\,(\\text{token at node}_1)$ — the token reaches node 1 infinitely often (fairness).\n\nThese are the **canonical patterns** that KTU examiners expect students to recognize and write down in examinations.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid Revision Checklist — Liveness Property**\n\n- ✅ **Definition**: $P$ is liveness iff $\\forall u \\in \\Sigma^*$, $\\exists v \\in \\Sigma^\\omega$ such that $u \\cdot v \\in P$.\n- ✅ **Topological Test**: $P$ is liveness $\\iff$ $P$ is **dense** in $\\Sigma^\\omega$ under the Cantor topology.\n- ✅ **Closure**: Liveness is closed under arbitrary $\\land$ and $\\lor$ (intersection and union).\n- ✅ **Decomposition**: $P = S \\cap L$ (Alpern–Schneider) — **unique** representation.\n- ✅ **Büchi Automata**: Liveness $\\to$ recognized by Büchi (visit accepting set infinitely often).\n- ✅ **LTL Patterns**:\n  - $\\mathbf{F}\\,p$ — Reachability\n  - $\\mathbf{G}(p \\rightarrow \\mathbf{F}\\,q)$ — Response\n  - $\\mathbf{G}\\mathbf{F}\\,p$ — Recurrence\n  - $\\mathbf{F}\\mathbf{G}\\,p$ — Persistence\n- ✅ **Contrast with Safety**:\n  - Safety: \"**never** bad\" — finite witness on violation — closed set\n  - Liveness: \"**eventually** good\" — infinite witness on violation — dense set\n- ✅ **Engineering Examples**: Termination, mutual exclusion entry, message delivery, cache coherence, consensus.\n- ✅ **Verification Tools**: SPIN, nuSMV, CBMC, UPPAAL all handle liveness via LTL/CTL model checking.\n- ✅ **Counterexample**: A liveness counterexample is an *infinite* trace (or a *lasso* — a finite prefix leading to a cycle violating acceptance).\n- ✅ **Examiner's Quick Win**: Always explicitly state the *prefix-continuation* form of the liveness definition; it earns full marks for the definition portion.\n\n> [!NOTE]\n> **Final KTU 2024 Insight**: Liveness properties are essential for verifying *progress* and *fairness* in concurrent, distributed, and reactive systems. Combined with safety properties, they form the **complete specification framework** for almost all real-world model-checking tasks.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t25",
          "title": "Safety vs. Liveness Properties",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Safety vs. Liveness Properties\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal KTU Definition\n\nIn the theory of **temporal logic** and **concurrent system verification**, every correctness property $P$ over the behaviours (execution traces) of a reactive or distributed system can be classified along the **Safety–Liveness spectrum** originally formalised by **Lamport (1977)** and later refined by **Alpern & Schneider (1985)**.\n\n> [!IMPORTANT]\n> **Safety Property** — A property $P$ is a *safety property* if and only if it asserts that **\"something bad never happens.\"** Equivalently, any violation of $P$ is witnessed by a **finite prefix** of an execution trace. Once violated, the violation cannot be \"undone\" by any future behaviour.\n\n> [!IMPORTANT]\n> **Liveness Property** — A property $P$ is a *liveness property* if and only if it asserts that **\"something good eventually happens.\"** Equivalently, for every finite prefix of an execution, there exists an extension of that prefix that satisfies $P$. A violation of $P$ can only be witnessed by an **infinite trace**.\n\nTogether, these two classes span the **complete space of all properties** expressible in linear-time temporal logic, as proven by the **Alpern–Schneider Theorem (1985)**: *Every linear-time property $P$ can be expressed as the intersection of a safety property and a liveness property.*\n\n---\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine you are **driving a car on a highway**:\n\n- **Safety = \"No crash occurs.\"**  \n  The moment your car touches another car, the rule is broken — and it stays broken forever. You cannot \"un-crash.\" This is **finite-trace witnessable** and matches a **safety property** in computing (e.g., mutual exclusion: \"two processes are never in the critical section at the same time\").\n\n- **Liveness = \"You will eventually reach your destination.\"**  \n  No matter how many toll booths, traffic lights, or wrong turns you have taken so far, there is *always some way* to keep driving such that you arrive. Progress must occur *infinitely often* in some sense. This matches a **liveness property** in computing (e.g., starvation freedom: \"every process eventually enters the critical section\").\n\n> [!NOTE]\n> **Quick Memory Hook:**  \n> - **Safety = Invariance / \"Never Bad\"** — *the system is always in a \"good state set\" $G$.*  \n> - **Liveness = Progress / \"Eventually Good\"** — *the system keeps transitioning towards a \"good event set\" $E$.*\n\n---\n\n## 1.3 Real-World Examples in Concurrent Systems\n\n| System Domain | Safety Property | Liveness Property |\n|---|---|---|\n| **Mutex Locks** | At most one process in critical section (no overlap) | Every requesting process eventually enters |\n| **Communication Protocols** | No message is lost or corrupted in transit | Every sent message is eventually delivered (acknowledged) |\n| **Routing Algorithms** | No routing loop is ever formed | Every packet eventually reaches its destination |\n| **Cache Coherence** | Two caches never hold incompatible values simultaneously | A write to a shared line is eventually propagated to all caches |\n| **Operating System Schedulers** | No process is starved of CPU while in ready queue | Every ready process eventually gets scheduled |\n| **Database Transactions** | No two transactions read+write conflicting data unsafely | Every committed transaction is eventually durable/persisted |\n\nThe bolded phrase **\"eventually\"** is the conceptual \"smoking gun\" for liveness, and **\"never\"** is the smoking gun for safety.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Operational Breakdown of the Two Property Classes\n\n### 2.1.1 Safety Property — The Mechanics\n\nA safety property $P_{\\text{safe}}$ is characterised by a **closed, prefix-closed set of infinite traces** $\\text{Traces}(P_{\\text{safe}})$. Formally:\n\n$$\n\\text{Traces}(P_{\\text{safe}}) = \\{\\sigma \\in \\Sigma^{\\omega} \\mid \\forall \\tau \\in \\text{Pref}(\\sigma) : \\tau \\in \\text{GoodPrefixes}\\}\n$$\n\n**Operational Step-by-Step Logic:**\n\n1. **Define a \"bad event\" $B$** — a single finite observation that constitutes the violation (e.g., two processes inside the critical section simultaneously).\n2. **Define the \"good prefix\" set $\\text{GoodPrefixes}$** — all finite words over the alphabet $\\Sigma$ that have not yet exhibited $B$.\n3. **A trace $\\sigma$ satisfies $P_{\\text{safe}}$ iff every finite prefix of $\\sigma$ is \"good.\"**\n4. **Verification mechanism:** A safety property can be checked by a *deterministic finite automaton (DFA)* over finite words — the *prefix-checking automaton*. The property is violated iff the DFA reaches a rejecting \"bad\" state.\n\n> [!NOTE]\n> **Why a DFA suffices:** Because once a bad prefix is observed, the verdict is final. There is no need to \"wait and see\" what happens later.\n\n---\n\n### 2.1.2 Liveness Property — The Mechanics\n\nA liveness property $P_{\\text{live}}$ has the property that **every finite word can be extended to some infinite word that satisfies $P_{\\text{live}}$**. Formally:\n\n$$\n\\forall \\tau \\in \\Sigma^{*} : \\exists \\sigma \\in \\Sigma^{\\omega} \\text{ such that } \\tau \\cdot \\sigma \\in \\text{Traces}(P_{\\text{live}})\n$$\n\n**Operational Step-by-Step Logic:**\n\n1. **Define a \"good event\" $G$** — an event whose *eventual* occurrence is required (e.g., a process enters its critical section).\n2. **No finite prefix is \"decisively bad.\"** Even a trace that has not seen $G$ for 1,000,000 steps can still be extended so that $G$ happens at step 1,000,001.\n3. **Verification mechanism:** A liveness property requires a *Büchi automaton* (or a more general $\\omega$-automaton) because the verdict depends on the *infinite* tail of the trace.\n\n> [!IMPORTANT]\n> **Büchi acceptance condition:** An $\\omega$-automaton accepts an infinite word iff the run visits some accepting state **infinitely often** — this exactly captures \"good events happen unboundedly often.\"\n\n---\n\n## 2.2 The Alpern–Schneider Decomposition Theorem\n\n> [!IMPORTANT]\n> **Theorem (Alpern & Schneider, 1985):** For every linear-time property $P$ over an alphabet $\\Sigma$, there exist a safety property $P_{\\text{safe}}$ and a liveness property $P_{\\text{live}}$ such that:\n> $$\\text{Traces}(P) \\;=\\; \\text{Traces}(P_{\\text{safe}}) \\;\\cap\\; \\text{Traces}(P_{\\text{live}})$$\n\n**Intuition of the proof sketch:** Given any property $P$, define the *closure* $\\text{cl}(P)$ as the set of all infinite traces whose every finite prefix is a prefix of some trace in $P$. The closure of $P$ is automatically a *safety property*. Then $P_{\\text{live}}$ is constructed as the \"remainder\" such that the intersection recovers $P$.\n\nThis theorem is the **theoretical foundation** of property-classification in the **SPIN model checker**, the **NuSMV verifier**, and the **Cadence SMV** tool.\n\n---\n\n## 2.3 LTL (Linear Temporal Logic) Encoding\n\nIn Linear Temporal Logic, the two classes have characteristic syntactic forms:\n\n| Property Class | LTL Operator Signature | Example Formula | Meaning |\n|---|---|---|---|\n| **Safety** | Only $\\mathbf{G}$ (globally) and Boolean connectives | $\\mathbf{G}\\,(\\neg\\, \\text{critical}_{1} \\lor \\neg\\, \\text{critical}_{2})$ | \"Globally, processes 1 and 2 are never both critical.\" |\n| **Liveness** | Contains $\\mathbf{F}$ (eventually) or $\\mathbf{GF}$ (infinitely often) | $\\mathbf{G}\\,(\\text{request}_{i} \\rightarrow \\mathbf{F}\\,\\text{grant}_{i})$ | \"Whenever $i$ requests, it is eventually granted.\" |\n| **Pure Liveness** | Top-level $\\mathbf{F}$ | $\\mathbf{F}\\,\\text{terminate}$ | \"The program eventually terminates.\" |\n\n> [!NOTE]\n> **Heuristic for KTU exams:** If the formula contains **only $\\mathbf{G}$** and propositional logic, it is a **safety property**. If it contains **$\\mathbf{F}$** or **$\\mathbf{GF}$**, it is a **liveness property**.\n\n---\n\n## 2.4 KTU Formula Sheet / Cheat Sheet\n\n| # | Concept | Formal Statement | Notes |\n|---|---|---|---|\n| 1 | Safety — Finite Witness | $\\sigma \\not\\models P_{\\text{safe}} \\;\\Longrightarrow\\; \\exists \\tau \\in \\text{Pref}(\\sigma)$ finite such that $\\forall \\alpha : \\tau \\cdot \\alpha \\not\\models P_{\\text{safe}}$ | The \"bad\" event is detected in finite time |\n| 2 | Liveness — Always Recoverable | $\\forall \\tau \\in \\Sigma^{*} : \\exists \\alpha \\in \\Sigma^{\\omega} : \\tau \\cdot \\alpha \\models P_{\\text{live}}$ | No finite prefix is fatal |\n| 3 | Alpern–Schneider | $\\text{Traces}(P) = \\text{Traces}(P_{\\text{safe}}) \\cap \\text{Traces}(P_{\\text{live}})$ | Every property is safety $\\cap$ liveness |\n| 4 | Closure of $P$ | $\\text{cl}(P) = \\{\\sigma \\mid \\forall \\tau \\in \\text{Pref}(\\sigma) : \\tau \\text{ is a prefix of some } \\rho \\in P\\}$ | $\\text{cl}(P)$ is always a safety property |\n| 5 | Büchi Acceptance | $\\sigma \\in L(\\mathcal{A}) \\iff \\inf(\\rho) \\cap F \\neq \\emptyset$ | Accept iff accepting states visited infinitely often |\n| 6 | LTL Safety Pattern | $\\mathbf{G}\\,\\varphi$ where $\\varphi$ is propositional | \"$\\varphi$ holds in every state\" |\n| 7 | LTL Liveness Pattern | $\\mathbf{F}\\,\\varphi$ or $\\mathbf{G}(\\psi \\rightarrow \\mathbf{F}\\varphi)$ | \"$\\varphi$ will hold\" or \"$\\psi$ leads to $\\varphi$\" |\n| 8 | Detector for Safety | Deterministic Finite Automaton (DFA) | A \"bad-prefix\" accepting DFA exists |\n| 9 | Detector for Liveness | Büchi $\\omega$-automaton | Needs infinite acceptance condition |\n\n> [!IMPORTANT]\n> **Standard Metrics (good to memorise for KTU viva):**\n> - **Alphabet** $\\Sigma$ : finite set of atomic propositions / actions.\n> - **Trace** $\\sigma = s_{0}, s_{1}, s_{2}, \\ldots \\in \\Sigma^{\\omega}$ : infinite sequence of states.\n> - **Finite prefix** $\\tau = s_{0}, s_{1}, \\ldots, s_{n} \\in \\Sigma^{*}$ : finite observation window.\n> - **Acceptance set** $F$ : designated \"good\" states of a Büchi automaton.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Worked Examples\n\n## 3.1 Derivation: Showing a Property is a Safety Property\n\n**Problem Setup.** Consider a mutual-exclusion system with two processes $P_{1}, P_{2}$. The atomic proposition $\\text{crit}_{i}$ means \"process $P_{i}$ is in its critical section.\" The property to analyse is:\n\n$$\nP \\;=\\; \\mathbf{G}\\,(\\neg\\, \\text{crit}_{1} \\lor \\neg\\, \\text{crit}_{2})\n$$\n\n**Step 1 — Identify the \"bad event.\"**  \nThe bad event is a single state in which $\\text{crit}_{1} \\land \\text{crit}_{2}$ both hold (i.e., both processes are inside the critical section simultaneously).\n\n$$\nB \\;=\\; \\{ s \\in S \\mid \\text{crit}_{1}(s) = \\text{true} \\;\\land\\; \\text{crit}_{2}(s) = \\text{true} \\}\n$$\n\n**Step 2 — Show that violation is witnessed by a finite prefix.**  \nSuppose a trace $\\sigma = s_{0}, s_{1}, s_{2}, \\ldots$ violates $P$. Then there exists a position $k \\geq 0$ such that $s_{k} \\in B$, i.e., $\\text{crit}_{1}(s_{k}) \\land \\text{crit}_{2}(s_{k})$. The *finite prefix* $\\tau = s_{0}, s_{1}, \\ldots, s_{k}$ is a witness.\n\n**Step 3 — Show that the bad event is \"fatal\" (irrecoverable).**  \nFor any infinite extension $\\alpha = s_{k+1}, s_{k+2}, \\ldots$:\n\n$$\n\\tau \\cdot \\alpha \\models \\mathbf{G}\\,(\\neg \\text{crit}_{1} \\lor \\neg \\text{crit}_{2}) \\;\\Longleftrightarrow\\; s_{k} \\models (\\neg \\text{crit}_{1} \\lor \\neg \\text{crit}_{2})\n$$\n\nSince $s_{k} \\models \\text{crit}_{1} \\land \\text{crit}_{2}$, the right-hand side is false. Therefore *every* extension $\\alpha$ of $\\tau$ leads to a violation of $P$. This is exactly the *finitary-fatal* condition defining a safety property.\n\n**Step 4 — Construct the prefix-checking DFA.**  \nA two-state deterministic finite automaton over the alphabet $\\Sigma = \\{\\overline{cc}, cc\\}$ (where $cc$ = \"both critical\" and $\\overline{cc}$ = \"not both critical\") suffices:\n\n- **State $q_{0}$ (initial, accepting):** Observed so far, no $cc$ seen.\n- **State $q_{1}$ (rejecting, \"trap\"):** Observed a $cc$; sink state.\n- Transition function $\\delta(q_{0}, \\overline{cc}) = q_{0}$, $\\delta(q_{0}, cc) = q_{1}$, $\\delta(q_{1}, x) = q_{1}$ for all $x$.\n\nThe DFA is *deterministic* and runs over *finite* words — the standard canonical detector for safety properties.\n\n> [!NOTE]\n> **Conclusion:** $P = \\mathbf{G}(\\neg \\text{crit}_{1} \\lor \\neg \\text{crit}_{2})$ is a **safety property** because (i) violation is witnessed by a finite prefix and (ii) once the bad prefix occurs, no future extension can repair it.\n\n---\n\n## 3.2 Derivation: Showing a Property is a Liveness Property\n\n**Problem Setup.** The property:\n\n$$\nQ \\;=\\; \\mathbf{G}\\,(\\text{request}_{i} \\rightarrow \\mathbf{F}\\,\\text{grant}_{i})\n$$\n\nasserts that \"every request by process $i$ is eventually granted.\"\n\n**Step 1 — Identify the \"good event.\"**  \nThe good event is $g = \\text{grant}_{i}$ becoming true *after* a $\\text{request}_{i}$.\n\n**Step 2 — Show that no finite prefix is \"fatally bad.\"**  \nTake any finite prefix $\\tau$ in which a $\\text{request}_{i}$ occurred at step $k$ but no $\\text{grant}_{i}$ has happened yet at step $n \\geq k$. We must show that *some* infinite extension $\\alpha$ satisfies $Q$.\n\n**Constructive witness extension:**  \nLet $\\alpha$ be defined as:\n\n$$\n\\alpha[j] = \n\\begin{cases}\n\\text{grant}_{i}, & j = 0 \\text{ (immediately after } \\tau \\text{)} \\\\\n\\text{idle}, & j \\geq 1\n\\end{cases}\n$$\n\nThen the extended trace $\\tau \\cdot \\alpha$ has $\\text{grant}_{i}$ true at the step right after $\\tau$. The LTL formula $Q$ is therefore satisfied on this extension. Since the choice of $\\tau$ was arbitrary, *no* finite prefix is fatal.\n\n**Step 3 — Show that violation requires an infinite trace.**  \nFor $Q$ to fail, there must be a request at step $k$ such that $\\text{grant}_{i}$ is false at *every* step $j \\geq k$. The witnessing \"bad\" sequence is infinite — it is the suffix $s_{k}, s_{k+1}, s_{k+2}, \\ldots$ in which no grant ever occurs.\n\n**Step 4 — Construct the Büchi automaton.**  \nA two-state Büchi automaton $\\mathcal{A}_{Q} = (Q_{\\mathcal{A}}, \\Sigma, \\delta, q_{0}, F)$:\n\n- $Q_{\\mathcal{A}} = \\{q_{0}, q_{1}\\}$, with $q_{0}$ initial.\n- $F = \\{q_{1}\\}$ (accepting state = \"I have seen a request followed by a grant\").\n- $\\delta(q_{0}, \\text{request}_{i} \\land \\text{grant}_{i}) = q_{1}$.\n- $\\delta(q_{0}, \\text{grant}_{i} \\text{ alone}) = q_{0}$.\n- $\\delta(q_{0}, \\neg \\text{request}_{i}) = q_{0}$.\n- $\\delta(q_{1}, \\text{request}_{i} \\land \\text{grant}_{i}) = q_{1}$.\n- $\\delta(q_{1}, \\text{any other symbol}) = q_{1}$.\n\nAcceptance: an infinite word is accepted iff $q_{1}$ is visited infinitely often — i.e., the request→grant pattern repeats unboundedly. This is exactly the **Büchi acceptance condition**, the standard canonical detector for liveness properties.\n\n> [!NOTE]\n> **Conclusion:** $Q = \\mathbf{G}(\\text{request}_{i} \\rightarrow \\mathbf{F}\\,\\text{grant}_{i})$ is a **liveness property** because (i) no finite prefix is decisively bad and (ii) its violation requires an infinite trace.\n\n---\n\n## 3.3 Derivation: The Alpern–Schneider Decomposition in Action\n\n**Problem.** Decompose the property\n\n$$\nR \\;=\\; \\mathbf{G}\\,(\\text{request}_{i} \\rightarrow \\mathbf{F}\\,\\text{grant}_{i}) \\;\\land\\; \\mathbf{G}\\,(\\neg \\text{crit}_{1} \\lor \\neg \\text{crit}_{2})\n$$\n\ninto safety and liveness parts.\n\n**Step 1 — Identify the safety component.**  \nThe sub-formula $\\mathbf{G}(\\neg \\text{crit}_{1} \\lor \\neg \\text{crit}_{2})$ is purely $\\mathbf{G}$-prefix-closed → it is a safety property.\n\n$$\nP_{\\text{safe}} = \\mathbf{G}\\,(\\neg \\text{crit}_{1} \\lor \\neg \\text{crit}_{2})\n$$\n\n**Step 2 — Identify the liveness component.**  \nThe sub-formula $\\mathbf{G}(\\text{request}_{i} \\rightarrow \\mathbf{F}\\,\\text{grant}_{i})$ contains the $\\mathbf{F}$ operator → it is a liveness property.\n\n$$\nP_{\\text{live}} = \\mathbf{G}\\,(\\text{request}_{i} \\rightarrow \\mathbf{F}\\,\\text{grant}_{i})\n$$\n\n**Step 3 — Verify the decomposition.**  \nBy the Alpern–Schneider theorem:\n\n$$\n\\text{Traces}(R) \\;=\\; \\text{Traces}(P_{\\text{safe}}) \\;\\cap\\; \\text{Traces}(P_{\\text{live}})\n$$\n\nThe traces that satisfy $R$ are exactly those that simultaneously (a) never have both processes in the critical section and (b) eventually grant every request. The decomposition is **syntactic and intuitive** when the formula is a conjunction of pure safety + pure liveness sub-formulas.\n\n**Step 4 — Construct the verification automaton.**  \n- Run the **DFA for $P_{\\text{safe}}$** in parallel with the **Büchi automaton for $P_{\\text{live}}$** over the system's traces.\n- The product is a *generalised Büchi automaton*.\n- Perform **emptiness checking** (e.g., nested DFS) to verify the product is non-empty — if non-empty, there exists a valid trace; if empty, the property is violated.\n\nThis is precisely the **core algorithmic pipeline** used in the SPIN model checker with **LTL Property Manager (Promela)**.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Mermaid Diagram: Classification Tree of Temporal Properties\n\n```mermaid\nflowchart TD\n    A[\"All Linear Time Properties P\"]:::root\n    A --> B[\"Safety Properties: Something bad never happens\"]\n    A --> C[\"Liveness Properties: Something good eventually happens\"]\n    A --> D[\"Mixed Properties: P = Safe ∩ Live\"]\n\n    B --> B1[\"Invariants: G φ\"]\n    B --> B2[\"Mutual Exclusion: G ¬(crit1 ∧ crit2)\"]\n    B --> B3[\"Deadlock Freedom: G ¬deadlock\"]\n    B --> B4[\"Type Safety: G typeOK\"]\n\n    C --> C1[\"Pure Liveness: F φ\"]\n    C --> C2[\"Response: G req → F grant\"]\n    C --> C3[\"Starvation Freedom: G F served\"]\n    C --> C4[\"Termination: F terminate\"]\n\n    D --> D1[\"Partial Correctness: G (in_correct_state ∨ F terminate)\"]\n    D --> D2[\"Total Correctness: G (in_correct_state → F terminate)\"]\n    D --> D3[\"Communication: G send → F receive\"]\n\n    classDef root fill:#1f4e79,stroke:#000,color:#fff,stroke-width:2px\n    classDef safeNode fill:#cce5ff,stroke:#003366,color:#000\n    classDef liveNode fill:#ffe5b4,stroke:#663300,color:#000\n    classDef mixedNode fill:#e0c3fc,stroke:#330066,color:#000\n    class B,B1,B2,B3,B4 safeNode\n    class C,C1,C2,C3,C4 liveNode\n    class D,D1,D2,D3 mixedNode\n```\n\n---\n\n## 4.2 Mermaid Diagram: Verification Pipeline for Safety vs. Liveness\n\n```mermaid\nflowchart LR\n    SYS[\"System Model: Kripke Structure M = S, S0, R, L\"]:::sys\n    PROP[\"Property Specification: LTL Formula φ\"]:::prop\n\n    SYS --> DECOMP{\"φ is...?\"}:::decision\n    PROP --> DECOMP\n\n    DECOMP -->|\"Safety: G ψ\"| DFA[\"Prefix Checking DFA\"]:::dfa\n    DECOMP -->|\"Liveness: F ψ or GF ψ\"| BA[\"Buchi Omega Automaton\"]:::buechi\n    DECOMP -->|\"Mixed: P_safe ∩ P_live\"| PROD[\"Product Automaton: DFA × BA\"]:::product\n\n    DFA --> SVR1[\"Subset Construction: M × DFA\"]:::verify\n    BA --> SVR2[\"Buchi Emptiness Check: M × BA\"]:::verify\n    PROD --> SVR3[\"Generalised Buchi Emptiness\"]:::verify\n\n    SVR1 --> OUT1{\"Trap state reachable?\"}:::q\n    SVR2 --> OUT2{\"Accepting cycle in product?\"}:::q\n    SVR3 --> OUT3{\"Accepting SCC exists?\"}:::q\n\n    OUT1 -->|\"Yes\"| FAIL1[\"Property VIOLATED: Counterexample = path to trap state\"]:::fail\n    OUT1 -->|\"No\"| PASS1[\"Property SATISFIED: All reachable states are safe\"]:::pass\n    OUT2 -->|\"Yes\"| PASS2[\"Property SATISFIED: Model is live w.r.t. φ\"]:::pass\n    OUT2 -->|\"No\"| FAIL2[\"Property VIOLATED: Liveness counterexample = lassos\"]:::fail\n    OUT3 -->|\"Yes\"| PASS3[\"Property SATISFIED\"]:::pass\n    OUT3 -->|\"No\"| FAIL3[\"Property VIOLATED\"]:::fail\n\n    classDef sys fill:#2e7d32,color:#fff\n    classDef prop fill:#1565c0,color:#fff\n    classDef decision fill:#f9a825,color:#000\n    classDef dfa fill:#bbdefb,color:#000\n    classDef buechi fill:#ffe0b2,color:#000\n    classDef product fill:#e1bee7,color:#000\n    classDef verify fill:#cfd8dc,color:#000\n    classDef q fill:#fff59d,color:#000\n    classDef pass fill:#a5d6a7,color:#000\n    classDef fail fill:#ef9a9a,color:#000\n    class SYS sys\n    class PROP prop\n    class DECOMP decision\n    class DFA dfa\n    class BA buechi\n    class PROD product\n    class SVR1,SVR2,SVR3 verify\n    class OUT1,OUT2,OUT3 q\n    class PASS1,PASS2,PASS3 pass\n    class FAIL1,FAIL2,FAIL3 fail\n```\n\n---\n\n## 4.3 Mermaid Diagram: Trace Space Partitioning by a Property P\n\n```mermaid\nflowchart TB\n    SIGMA[\"Sigma^omega: Set of all infinite traces of the system\"]:::root\n    SIGMA --> SAT[\"Traces satisfying P = Traces P\"]:::sat\n    SIGMA --> VIOL[\"Traces violating P = Sigma^omega minus Traces P\"]:::viol\n\n    VIOL --> VBAD[\"Bad finite prefix t exists<br>such that every extension violates P<br>=> Safety Violation\"]:::safetyViol\n    VIOL --> VLIVE[\"No finite prefix t is bad<br>Every finite prefix has a satisfying extension<br>=> Liveness Violation: only by infinite traces\"]:::liveViol\n\n    SAT --> SSAT[\"Safety part: closure cl P holds<br>Every finite prefix is a prefix of some good trace\"]:::safetySat\n    SAT --> LSAT[\"Liveness part: Limit superior of good events is non-empty<br>Good events happen infinitely often\"]:::liveSat\n\n    SSAT --> INTER[\"Traces P = cl P intersect L where L is liveness part\"]:::inter\n    LSAT --> INTER\n\n    classDef root fill:#37474f,color:#fff\n    classDef sat fill:#81c784,color:#000\n    classDef viol fill:#e57373,color:#000\n    classDef safetyViol fill:#ffcdd2,color:#000\n    classDef liveViol fill:#ffe0b2,color:#000\n    classDef safetySat fill:#c8e6c9,color:#000\n    classDef liveSat fill:#dcedc8,color:#000\n    classDef inter fill:#b39ddb,color:#000\n    class SIGMA root\n    class SAT sat\n    class VIOL viol\n    class VBAD safetyViol\n    class VLIVE liveViol\n    class SSAT safetySat\n    class LSAT liveSat\n    class INTER inter\n```\n\n> [!NOTE]\n> **Reading the diagram:** Every point in $\\Sigma^{\\omega}$ is either a satisfying trace or a violating trace. Violating traces are partitioned by *why* they violate: finite-prefix (safety) violations vs. infinite-tail (liveness) violations. The Alpern–Schneider theorem tells us every trace that satisfies $P$ must satisfy **both** its safety closure and its liveness part.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n## Part A Questions (3 Marks Each)\n\n### **Q1.** [KTU University Exam — Dec 2023]  \n**Differentiate between safety and liveness properties in concurrent systems. Give one example of each.**\n\n**Model Answer (3 marks):**\n\n| Aspect | Safety Property | Liveness Property |\n|---|---|---|\n| **Intuition** | \"Something bad never happens.\" | \"Something good eventually happens.\" |\n| **Violation witness** | A **finite** prefix of a trace suffices. | Requires an **infinite** trace. |\n| **Detector** | Deterministic finite automaton (DFA). | Büchi $\\omega$-automaton. |\n| **LTL pattern** | Only $\\mathbf{G}$ + propositional logic. | Contains $\\mathbf{F}$ or $\\mathbf{GF}$. |\n| **Example** | Mutual exclusion: $\\mathbf{G}(\\neg \\text{crit}_{1} \\lor \\neg \\text{crit}_{2})$ | Starvation freedom: $\\mathbf{G}(\\text{request}_{i} \\rightarrow \\mathbf{F}\\,\\text{grant}_{i})$ |\n| **Engineering use** | Invariant verification, deadlock-freedom, type safety. | Termination, progress, eventual consistency. |\n\n*(Examiner's note: 1 mark for definition of each, 1 mark for correct example of each — 3 marks total.)*\n\n---\n\n### **Q2.** [KTU University Exam — June 2024]  \n**State the Alpern–Schneider theorem. Why is it significant in model checking?**\n\n**Model Answer (3 marks):**\n\n**Statement (2 marks):** *For every linear-time property $P$, there exist a safety property $P_{\\text{safe}}$ and a liveness property $P_{\\text{live}}$ such that $\\text{Traces}(P) = \\text{Traces}(P_{\\text{safe}}) \\cap \\text{Traces}(P_{\\text{live}})$.*\n\n**Significance (1 mark):**\n- The theorem **decouples** the verification of a complex property into two simpler sub-problems: a *finite-state invariant check* (safety) and an *omega-automaton emptiness check* (liveness).\n- This decoupling is the **theoretical foundation** of how industrial model checkers (SPIN, NuSMV) decompose LTL formulas internally and choose the most efficient verification algorithm for each sub-formula.\n\n---\n\n## Part B Questions (14 Marks Each)\n\n### **Question A (14 Marks)** — Module 1 Internal Choice Option 1\n\n**[KTU University Exam — Dec 2023, Adapted]**  \n**(a)** With a neat sketch, explain the **prefix-checking deterministic finite automaton** for verifying the safety property $\\mathbf{G}(\\neg \\text{crit}_{1} \\lor \\neg \\text{crit}_{2})$ in a two-process mutex system. Show formally why this property is a safety property. **(7 marks)**\n\n**(b)** Construct a **Büchi $\\omega$-automaton** for the liveness property $\\mathbf{G}(\\text{request}_{i} \\rightarrow \\mathbf{F}\\,\\text{grant}_{i})$. Explain the **Büchi acceptance condition** and prove that this property is genuinely a liveness property. **(7 marks)**\n\n---\n\n#### **Model Solution for Question A, Part (a) — 7 marks**\n\n**Step 1 — Formal definition of safety (2 marks):**  \nA property $P$ is a safety property iff for every trace $\\sigma$ violating $P$, there exists a finite prefix $\\tau$ of $\\sigma$ such that *every* extension $\\alpha$ of $\\tau$ also violates $P$. The \"bad event\" is a *finite* observation.\n\n**Step 2 — Identify the bad event (1 mark):**  \nThe bad event is the proposition $b = \\text{crit}_{1} \\land \\text{crit}_{2}$. The state set in which this holds is the \"trap\" set.\n\n**Step 3 — Construct the DFA (3 marks):**  \nThe alphabet is $\\Sigma = \\{ \\overline{b}, b \\}$ where $\\overline{b}$ = \"not both critical\" and $b$ = \"both critical.\"\n\n| State | Meaning | Type |\n|---|---|---|\n| $q_{0}$ | Initial; no bad event observed yet | Initial, accepting |\n| $q_{1}$ | Bad event observed; trap | Trap, rejecting |\n\nTransition function:\n\n$$\n\\delta(q_{0}, \\overline{b}) = q_{0}, \\quad \\delta(q_{0}, b) = q_{1}, \\quad \\delta(q_{1}, x) = q_{1} \\; \\forall x \\in \\Sigma\n$$\n\nThe DFA is deterministic and operates on **finite words** — a finite word reaching $q_{1}$ is a counterexample trace prefix.\n\n**Step 4 — Justify safety (1 mark):**  \nOnce $q_{1}$ is reached, the system is \"stuck bad.\" The DFA cannot escape $q_{1}$. Hence the bad event is *fatal and finitely witnessed* → property is safety.\n\n---\n\n#### **Model Solution for Question A, Part (b) — 7 marks**\n\n**Step 1 — Formal definition of liveness (1.5 marks):**  \nA property $P$ is a liveness property iff for every finite prefix $\\tau \\in \\Sigma^{*}$, there exists an infinite extension $\\alpha \\in \\Sigma^{\\omega}$ such that $\\tau \\cdot \\alpha \\models P$. No finite prefix is \"fatally bad.\"\n\n**Step 2 — Construct the Büchi automaton (3 marks):**  \nStates: $Q = \\{q_{0}, q_{1}\\}$, initial $q_{0}$, accepting set $F = \\{q_{1}\\}$.\n\n| Current State | Input Symbol | Next State |\n|---|---|---|\n| $q_{0}$ | request ∧ grant | $q_{1}$ |\n| $q_{0}$ | ¬request | $q_{0}$ |\n| $q_{0}$ | grant only | $q_{0}$ |\n| $q_{1}$ | request ∧ grant | $q_{1}$ |\n| $q_{1}$ | anything else | $q_{1}$ |\n\n**Step 3 — State the Büchi acceptance condition (1.5 marks):**  \nAn infinite word $\\sigma$ is accepted by a Büchi automaton $\\mathcal{A}$ iff there exists a run $\\rho$ of $\\mathcal{A}$ on $\\sigma$ such that some accepting state in $F$ is visited **infinitely often**:\n\n$$\n\\sigma \\in L(\\mathcal{A}) \\iff \\inf(\\rho) \\cap F \\neq \\emptyset\n$$\n\n**Step 4 — Prove liveness (1 mark):**  \nFor any finite prefix $\\tau$, we can extend $\\tau$ with a single `grant` event immediately afterward. The Büchi automaton will then visit $q_{1}$ at that step. Since the automaton can stay in $q_{1}$ from then on by looping on `request ∧ grant`, the run visits $q_{1}$ infinitely often → the extended word is accepted. No finite prefix is fatal → property is liveness. $\\blacksquare$\n\n---\n\n### **Question B (14 Marks)** — Module 1 Internal Choice Option 2\n\n**[KTU University Exam — June 2024, Adapted]**  \n**(a)** Explain the **Alpern–Schneider decomposition theorem** with reference to the property $R = \\mathbf{G}(\\text{req} \\rightarrow \\mathbf{F}\\,\\text{grant}) \\land \\mathbf{G}(\\neg \\text{crit}_{1} \\lor \\neg \\text{crit}_{2})$. Identify the safety and liveness components of $R$. **(7 marks)**\n\n**(b)** Why is it **algorithmically impossible** to verify a pure liveness property by simulating the system for a fixed, finite number of steps? Justify using the closure of a liveness property. **(7 marks)**\n\n---\n\n#### **Model Solution for Question B, Part (a) — 7 marks**\n\n**Step 1 — Theorem statement (2 marks):**  \nFor every linear-time property $P$, $\\exists\\, P_{\\text{safe}}, P_{\\text{live}}$ with $P_{\\text{safe}}$ a safety property and $P_{\\text{live}}$ a liveness property such that:\n\n$$\n\\text{Traces}(P) = \\text{Traces}(P_{\\text{safe}}) \\cap \\text{Traces}(P_{\\text{live}})\n$$\n\n**Step 2 — Decompose $R$ syntactically (3 marks):**\n\n- **Safety part:** $P_{\\text{safe}} = \\mathbf{G}(\\neg \\text{crit}_{1} \\lor \\neg \\text{crit}_{2})$ — only $\\mathbf{G}$ and propositional connectives, hence safety. (1.5 marks)\n- **Liveness part:** $P_{\\text{live}} = \\mathbf{G}(\\text{req} \\rightarrow \\mathbf{F}\\,\\text{grant})$ — contains $\\mathbf{F}$ operator, hence liveness. (1.5 marks)\n\n**Step 3 — Verify the intersection (2 marks):**\n\n$$\n\\text{Traces}(R) = \\text{Traces}(P_{\\text{safe}}) \\cap \\text{Traces}(P_{\\text{live}})\n$$\n\nThe traces in $R$ are exactly those in which (a) no state has both $\\text{crit}_{1}$ and $\\text{crit}_{2}$ true, **and** (b) every $\\text{req}$ is followed eventually by a $\\text{grant}$. The decomposition is direct because the top-level connective is a conjunction of one pure-safety and one pure-liveness sub-formula.\n\n---\n\n#### **Model Solution for Question B, Part (b) — 7 marks**\n\n**Step 1 — Define the closure of $P$ (2 marks):**  \nThe closure of a property $P$ is:\n\n$$\n\\text{cl}(P) = \\{ \\sigma \\in \\Sigma^{\\omega} \\mid \\forall \\tau \\in \\text{Pref}(\\sigma) : \\exists \\rho \\in P \\text{ with } \\tau \\in \\text{Pref}(\\rho) \\}\n$$\n\nFor any property $P$, $\\text{cl}(P)$ is automatically a *safety property*. Specifically, if $P$ is itself a liveness property, then $\\text{cl}(P) = \\Sigma^{\\omega}$ — the closure of any liveness property is the **entire trace space**.\n\n**Step 2 — Show why finite simulation is insufficient (3 marks):**  \nSuppose we simulate the system for $N$ finite steps and observe no violation of the liveness property $P = \\mathbf{F}\\,\\text{terminate}$. This only shows the trace is consistent with a model in which termination has not yet happened — it does **not** rule out the possibility that termination fails at step $N + 1, N + 2, \\ldots$. A finite simulation covers only the prefix $\\tau = s_{0}, \\ldots, s_{N}$, but liveness violations are witnessed by the *infinite* suffix.\n\n**Step 3 — Connect to closure (2 marks):**  \nFor a liveness property $P$, $\\text{cl}(P) = \\Sigma^{\\omega}$. A finite simulation only ever proves closure, which is trivially true for liveness. Therefore, no finite simulation can disprove a liveness property — we **must** use an $\\omega$-automaton (Büchi or generalised) and check for accepting cycles in the product with the system model. $\\blacksquare$\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Pitfall Callout:**\n> 1. **Do not** claim that \"liveness properties cannot be checked at all\" — they *can*, via **Büchi/$\\omega$-automata and emptiness checks** on the product. What is true is that they *cannot be falsified by finite simulation alone*.\n> 2. **Do not** confuse **deadlock-freedom** (a **safety** property: \"no deadlock state is reachable\") with **starvation-freedom** (a **liveness** property: \"every process eventually gets service\"). Examiners specifically test this distinction.\n> 3. **Do not** write \"$\\mathbf{GF}$ is liveness\" without explanation — $\\mathbf{GF}\\,\\varphi$ is a liveness property meaning \"$\\varphi$ happens infinitely often,\" but you must state the formal reason (it contains $\\mathbf{F}$).\n> 4. **Always** mention the **Alpern–Schneider theorem** in any 14-mark answer — it is a KTU 2024 syllabus highlight worth 2–3 easy marks.\n> 5. **Viva tip:** If asked \"Is $\\mathbf{GF}\\,\\varphi$ a safety or liveness property?\" answer **liveness**, because its violation requires an infinite trace where $\\varphi$ is eventually always false.\n\n---\n\n## Topic Recap & Important Things to Remember\n\n- [ ] **Safety** = \"Nothing bad happens\" → finite-prefix witnessable → **DFA** detector.\n- [ ] **Liveness** = \"Something good eventually happens\" → only infinite-trace witnessable → **Büchi** detector.\n- [ ] **Mnemonic for LTL:** Only $\\mathbf{G}$ ⇒ safety; presence of $\\mathbf{F}$ or $\\mathbf{GF}$ ⇒ liveness.\n- [ ] **Canonical examples:** Mutex (safety), starvation-freedom (liveness), partial vs. total correctness, deadlock-freedom (safety) vs. progress (liveness).\n- [ ] **Alpern–Schneider Theorem:** $\\text{Traces}(P) = \\text{Traces}(P_{\\text{safe}}) \\cap \\text{Traces}(P_{\\text{live}})$. Every linear-time property decomposes.\n- [ ] **Closure operator:** $\\text{cl}(P)$ is always a safety property. For liveness $P$, $\\text{cl}(P) = \\Sigma^{\\omega}$.\n- [ ] **Verification mechanism:** Safety → reachability analysis (Dijkstra, nested DFS, on-the-fly); Liveness → Büchi-emptiness / accepting-SCC search.\n- [ ] **Büchi acceptance:** an $\\omega$-word is accepted iff some accepting state is visited **infinitely often**.\n- [ ] **Algorithmic consequence:** No finite simulation can disprove a pure liveness property — only $\\omega$-automata-based model checking works.\n- [ ] **Industrial tools:** SPIN (LTL → Büchi via `ltl2ba`), NuSMV (CTL), Cadence SMV — all rely on the safety/liveness distinction internally.\n- [ ] **Mixed property** = \"something must be maintained AND something must eventually occur\" → decompose, verify each part separately, then take the intersection.\n- [ ] **Examiner favourites:** Differentiate table (3 marks), Alpern–Schneider statement (3 marks), DFA + Büchi construction (14 marks), finite-simulation impossibility (14 marks).\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t26",
          "title": "Fairness - Fairness",
          "content": "<!-- SECTION_1_START -->\n> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Fairness - Fairness\n\n# Module 1 — Fairness\n\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n> [!IMPORTANT]\n> **Fairness** is a property of an execution path (or Kripke structure) that constrains the scheduler's non-deterministic choices in a concurrent or reactive system. It restricts the set of valid computations by requiring that certain *enabled* transitions, conditions, or processes be given a reasonable share of execution, so that no component is starved indefinitely.\n\nIn formal terms, given a transition system $TS = (S, S_0, \\rightarrow)$ over a set of atomic propositions $AP$, a path $\\pi = s_0, s_1, s_2, \\ldots$ is said to be **fair** with respect to a set of fairness constraints $\\mathcal{F} = \\{f_1, f_2, \\ldots, f_n\\}$ if every fairness constraint $f_i$ is satisfied infinitely often along $\\pi$.\n\nA **fairness constraint** $f$ is a temporal logic formula (typically an LTL/CTL formula constructed from atomic propositions). A path $\\pi \\models f$ is read as \"path $\\pi$ satisfies the fairness constraint $f$.\"\n\n> [!NOTE]\n> **Why we need fairness in verification:** Model checking explores ALL execution paths of a transition system. In a concurrent program with $n$ processes, the interleaving space is enormous, and many of those paths are *unrealistic* (e.g., a process is perpetually ignored by the scheduler). Fairness filters out these unrealistic paths, producing verification results that better match real-world behavior.\n\n### 1.2 Conceptual Analogy — The \"Round-Robin Coffee Machine\"\n\nImagine a **shared office coffee machine** with 4 colleagues — Anu, Binu, Chris, and Deepa — all waiting in line. A non-deterministic scheduler picks the next person.\n\n- **Without fairness:** The scheduler *could* keep picking Anu forever, leaving the others never served. This is a valid execution in a naive model, but obviously unrealistic.\n- **With (unconditional/impartial) fairness:** The scheduler must pick each colleague **infinitely often**. Anu may go first twice, but eventually Binu, Chris, and Deepa all get infinite turns.\n- **With strong fairness:** If a colleague *keeps raising their hand* (is *continuously enabled*), they will *eventually* be served.\n- **With weak fairness:** If a colleague *stays in a requesting state* (is *continuously enabled*) from some point onward, they will eventually be served — but if they raise their hand and lower it repeatedly, fairness does not guarantee them service.\n\nThis is exactly how fairness works in a **Kripke structure** modeling a concurrent system: it filters out the \"starving\" paths that a naive interleaving would admit.\n\n### 1.3 Taxonomy of Fairness (Standard Classification)\n\n| # | Type | LTL Formulation | Intuition |\n|---|------|-----------------|-----------|\n| 1 | **No fairness** | — | All paths are valid; starvation allowed. |\n| 2 | **Impartial (Unconditional) Fairness** | $\\bigwedge_{i=1}^{n} \\Box \\Diamond \\, a_i$ | Every process $a_i$ is executed **infinitely often** regardless of state. |\n| 3 | **Weak Fairness (Justice)** | $\\Box \\Diamond \\, \\text{enabled}(a_i) \\Rightarrow \\Box \\Diamond \\, \\text{executed}(a_i)$ | A continuously enabled action must eventually be taken. |\n| 4 | **Strong Fairness (Compassion)** | $\\Box \\Diamond \\, \\text{enabled}(a_i) \\Rightarrow \\Box \\Diamond \\, \\text{executed}(a_i)$ | An *infinitely often enabled* action must be taken infinitely often. |\n\n> [!NOTE]\n> **Notation Recap:** $\\Box$ = \"always\" (globally), $\\Diamond$ = \"eventually\" (finally), $\\Box \\Diamond$ = \"infinitely often\", $\\Diamond \\Box$ = \"eventually forever\".\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Operational Decomposition — How Fairness Works in Model Checking\n\nA model checker evaluates a property $\\varphi$ over a transition system $TS$ by checking:\n\n$$TS \\models \\varphi \\quad \\equiv \\quad \\text{for every reachable state } s,\\ \\ s \\models \\varphi$$\n\nWhen $\\varphi$ is a **path property** (e.g., \"eventually $p$ holds\"), we evaluate it over all initial paths. Fairness modifies the quantifier scope:\n\n- **Without fairness:** $TS \\models \\varphi$ iff $\\forall \\pi \\in \\text{Paths}(TS).\\ \\ \\pi \\models \\varphi$\n- **With fairness set $\\mathcal{F}$:** $TS \\models_\\mathcal{F} \\varphi$ iff $\\forall \\pi \\in \\text{FairPaths}(TS, \\mathcal{F}).\\ \\ \\pi \\models \\varphi$\n\nSo fairness is essentially a **path filter** applied to the set of executions before the universal quantifier is checked.\n\n### 2.2 The Three Formalisms (Step-by-Step Logic)\n\n#### (A) Unconditional / Impartial Fairness\n\n> [!IMPORTANT]\n> **Impartial fairness** treats every transition (or process) as equally deserving of execution. It is the strongest (most restrictive) fairness assumption.\n\nFormally, for a set of actions $A = \\{a_1, a_2, \\ldots, a_n\\}$:\n\n$$\\text{Unconditional Fairness} \\equiv \\bigwedge_{i=1}^{n} \\Box \\Diamond \\, a_i$$\n\n**Why?** Because no condition is attached to when $a_i$ is enabled. Each action must fire infinitely often, period. This is unrealistic for large systems (it requires the scheduler to give every one of thousands of processes a turn) and is mainly a theoretical concept.\n\n#### (B) Weak Fairness (a.k.a. *Justice*)\n\nWeak fairness is a **conditional** requirement. Let $en(a_i)$ denote the proposition that action $a_i$ is enabled in the current state. Then:\n\n$$\\text{Weak Fairness for } a_i \\equiv \\Box \\Diamond \\, en(a_i) \\;\\Rightarrow\\; \\Box \\Diamond \\, \\langle a_i \\rangle$$\n\n**Reading:** *\"If $a_i$ is enabled infinitely often, then it is executed infinitely often.\"*\n\nThis handles the common engineering scenario: a process is *sometimes* available (e.g., when its input buffer is non-empty), and the scheduler must not perpetually ignore it while it is available.\n\n#### (C) Strong Fairness (a.k.a. *Compassion*)\n\nStrong fairness is the **most permissive** realistic assumption. It is weaker than weak fairness in logical strength but harder to check in practice.\n\n$$\\text{Strong Fairness for } a_i \\equiv \\Diamond \\Box \\, en(a_i) \\;\\Rightarrow\\; \\Box \\Diamond \\, \\langle a_i \\rangle$$\n\n**Reading:** *\"If $a_i$ is enabled **continuously from some point on** (eventually-always), then it must be executed infinitely often.\"*\n\n> [!NOTE]\n> **Implication chain in strength:**\n> $$\\text{Strong Fairness} \\;\\Longrightarrow\\; \\text{Weak Fairness} \\;\\Longrightarrow\\; \\text{Unconditional Fairness (constrained)}$$\n> More precisely: any path satisfying strong fairness also satisfies weak fairness; weak fairness is *typically stronger* than impartial fairness when the enabledness propositions are non-trivial.\n\n### 2.3 The Fair CTL (CTL*) Logic — Quantifier Rules\n\nWhen checking fairness-aware properties in CTL, the standard rules are:\n\n| Path quantifier | Without fairness | With fairness $\\mathcal{F}$ |\n|-----------------|------------------|-----------------------------|\n| $A\\varphi$ (all paths) | $\\forall \\pi.\\ \\pi \\models \\varphi$ | $\\forall \\pi \\in \\text{FairPaths}.\\ \\pi \\models \\varphi$ |\n| $E\\varphi$ (exists a path) | $\\exists \\pi.\\ \\pi \\models \\varphi$ | $\\exists \\pi \\in \\text{FairPaths}.\\ \\pi \\models \\varphi$ |\n\nThis means that in algorithms like **Emerson-Lei** for fair CTL model checking, we replace the fixed-point computation over all states with one restricted to **fair states** — those lying on at least one fair cycle (the **SCC decomposition** approach).\n\n### 2.4 KTU Formula Sheet / Cheat Sheet\n\n| Symbol / Concept | Formal Meaning | Used For |\n|------------------|----------------|----------|\n| $\\Box$ | Always (globally) | \"for all future states\" |\n| $\\Diamond$ | Eventually (finally) | \"at some future state\" |\n| $\\Box \\Diamond \\, \\varphi$ | Infinitely often $\\varphi$ | Impartial/strong fairness LHS |\n| $\\Diamond \\Box \\, \\varphi$ | Eventually forever $\\varphi$ | Strong-fairness antecedent |\n| $en(a)$ | Action $a$ is enabled | Used in weak/strong fairness |\n| $\\langle a \\rangle$ | Transition $a$ fires | Consequent of fairness rules |\n| $\\text{FairPaths}(TS, \\mathcal{F})$ | Set of paths satisfying $\\mathcal{F}$ | Universal path quantifier scope |\n| $\\text{fair}(s)$ | $s$ lies on a fair SCC | Emerson-Lei algorithm basis |\n| $TS \\models_\\mathcal{F} \\varphi$ | $TS$ satisfies $\\varphi$ under fairness $\\mathcal{F}$ | Final verification result |\n| $\\mathcal{F} = \\{f_1, \\ldots, f_n\\}$ | Set of fairness constraints | Input to fair model checker |\n\n### 2.5 Real-World Engineering Utility\n\n- **Hardware verification (RTL model checking):** Fairness is used to assume that every FIFO buffer is *eventually drained*, every arbiter output is *eventually granted*, and every interrupt is *eventually serviced*. Without this, a property like \"the CPU will eventually receive the interrupt\" would be falsely violated by a path that starves the interrupt line.\n- **Protocol verification (e.g., cache coherence, mutual exclusion):** Weak fairness ensures that *if a node requests a lock continuously*, it will eventually be granted, mirroring real schedulers.\n- **Software model checking (Java PathFinder, SPIN):** Fairness assumptions in SPIN's PROMELA guide the verifier to drop starvation paths.\n- **Cyber-physical systems:** Real-time schedulers like Rate-Monotonic Scheduling (RMS) provide **bounded** fairness, often stronger than weak fairness.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Derivation: Equivalence of Fairness Conditions to Büchi Automata Acceptance\n\nA **Büchi automaton** $\\mathcal{B} = (Q, \\Sigma, \\delta, q_0, F)$ accepts an infinite word $w$ iff $w$ visits $F$ infinitely often. Fairness constraints are exactly a **generalized Büchi acceptance condition**.\n\n**Step 1 — Define a fairness word.** Let $\\mathcal{F} = \\{f_1, \\ldots, f_n\\}$ be a set of LTL fairness constraints. An infinite path $\\pi$ induces an infinite word $w_\\pi$ over the alphabet $2^{AP}$ (one letter per state).\n\n**Step 2 — Define the satisfaction relation.** A path $\\pi$ is *fair* iff:\n\n$$\\pi \\models \\bigwedge_{i=1}^{n} \\Box \\Diamond \\, f_i \\quad \\equiv \\quad \\forall i \\in \\{1,\\ldots,n\\}.\\ \\pi \\models \\Box \\Diamond \\, f_i$$\n\n**Step 3 — Equivalence to Büchi acceptance.** We construct a generalized Büchi automaton $\\mathcal{B}_\\mathcal{F}$ with $n$ accepting sets $F_1, \\ldots, F_n$, where $F_i$ is the set of states in which $f_i$ holds. Then:\n\n$$\\pi \\in \\text{FairPaths}(TS, \\mathcal{F}) \\iff L(\\mathcal{B}_\\mathcal{F}) \\cap L(TS) \\neq \\emptyset$$\n\n**Step 4 — Conversion to standard Büchi.** A generalized Büchi automaton with $n$ accepting sets can be converted to a standard Büchi automaton with $|Q| \\cdot n$ states (a classical result by Choueka & others), preserving the language.\n\nThe full construction is:\n\n$$\\begin{aligned}\n\\text{Let}\\ \\mathcal{B}_\\mathcal{F} &= (Q, \\Sigma, \\delta, q_0, \\{F_1, \\ldots, F_n\\}) \\\\\n\\text{Construct}\\ \\mathcal{B}' &= (Q', \\Sigma, \\delta', q_0', F') \\\\\n\\text{where}\\ Q' &= Q \\times \\{1, \\ldots, n\\} \\\\\nq_0' &= (q_0, 1) \\\\\n\\delta'((q, i), a) &= \\{ (q', j) \\mid q' \\in \\delta(q, a),\\ j = (i \\bmod n) + 1 \\text{ if } q' \\in F_i,\\ \\text{else } j = i \\} \\\\\nF' &= Q \\times \\{1\\}\n\\end{aligned}$$\n\nThis shows that **fair model checking is reducible to standard Büchi emptiness checking** — a foundational result of Vardi & Wolper (1986).\n\n### 3.2 Derivation: Fair Cycles and SCC Decomposition (Emerson-Lei Algorithm)\n\nThe **Emerson-Lei algorithm** for fair CTL model checking reduces the problem to finding states that lie on a **fair SCC** (Strongly Connected Component).\n\n**Step 1 — Compute the set of fair states.** A state $s$ is *fair* iff it lies on a cycle that visits each $F_i$ infinitely often. In a finite transition system, this is equivalent to:\n\n$$s \\in \\text{FairStates} \\iff \\exists \\text{ a cycle } C \\text{ from } s \\text{ to } s \\text{ that visits } \\bigcup_i F_i \\text{ infinitely often}$$\n\n**Step 2 — Build the SCC graph.** Compute the SCCs of the transition system. An SCC is *fair* iff for every $F_i$, the SCC contains at least one state in $F_i$.\n\n**Step 3 — Identify fair states.** A state $s$ is in $\\text{FairStates}$ iff it can reach a *fair SCC*.\n\n**Step 4 — Apply the fair-CCTL fixed-point.** For a formula $EG\\,\\varphi$ under fairness $\\mathcal{F}$:\n\n$$\\text{Sat}(EG\\,\\varphi) = \\nu Y.\\ \\ \\varphi \\cap \\text{pre}_\\exists(Y) \\cap \\text{FairStates}$$\n\nThe greatest fixed-point is over states that:\n1. satisfy $\\varphi$,\n2. have a successor in $Y$, and\n3. are in FairStates.\n\nThis is **why fairness makes $EG$ non-trivial** — without it, $EG\\,\\text{true}$ is satisfied by every state on a cycle.\n\n### 3.3 Analytical Matrix — Comparison Table\n\n| Property | Impartial (Unconditional) | Weak Fairness (Justice) | Strong Fairness (Compassion) |\n|----------|---------------------------|-------------------------|------------------------------|\n| **LTL form** | $\\bigwedge_i \\Box \\Diamond \\, a_i$ | $\\Box \\Diamond\\, en(a_i) \\Rightarrow \\Box \\Diamond \\,\\langle a_i \\rangle$ | $\\Diamond \\Box\\, en(a_i) \\Rightarrow \\Box \\Diamond\\, \\langle a_i \\rangle$ |\n| **Logical strength** | Strongest (most paths excluded) | Intermediate | Weakest (most paths admitted) |\n| **Realism** | Often unrealistic | Common default | Tightest match to real schedulers |\n| **Algorithm difficulty** | Easy (small $\\mathcal{F}$) | Moderate | Harder (involves SCC analysis) |\n| **CTL/CTL*** | Both | Both | Both |\n| **Common tool support** | SPIN, NuSMV, UPPAAL | All major | All major |\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Mermaid Diagram — Fairness Path-Filtering Mechanism\n\n```mermaid\ngraph TD\n    TS[\"Transition System TS\"]\n    AllPaths[\"All Execution Paths Set\"]\n    FairFilter{\"Apply Fairness Filter F?\"}\n    FairPaths[\"Fair Execution Paths\"]\n    UnfairPaths[\"Unrealistic / Starvation Paths\"]\n    CheckA[\"A phi : Check all fair paths\"]\n    CheckE[\"E phi : Check existence of fair path\"]\n    VerdictA[\"Result : TS models F phi\"]\n    VerdictE[\"Result : TS models F E phi\"]\n    \n    TS --> AllPaths\n    AllPaths --> FairFilter\n    FairFilter -->|Paths satisfying F| FairPaths\n    FairFilter -->|Paths violating F| UnfairPaths\n    FairPaths --> CheckA\n    FairPaths --> CheckE\n    CheckA --> VerdictA\n    CheckE --> VerdictE\n```\n\n### 4.2 Mermaid Diagram — Hierarchy of Fairness Strengths\n\n```mermaid\ngraph TD\n    Unconditional[\"Unconditional Fairness<br/>Box Diamond a_i for all i\"]\n    Weak[\"Weak Fairness<br/>Box Diamond en implies Box Diamond act\"]\n    Strong[\"Strong Fairness<br/>Diamond Box en implies Box Diamond act\"]\n    NoFair[\"No Fairness<br/>All paths valid\"]\n    \n    Unconditional -->|strongest, most restrictive| Weak\n    Weak -->|less restrictive| Strong\n    Strong -->|least restrictive fair subset| NoFair\n    \n    Unconditional -.implies.-> Weak\n    Weak -.implies.-> NoFair\n    Strong -.does NOT necessarily imply.-> Unconditional\n```\n\n### 4.3 Mermaid Diagram — Emerson-Lei Fair SCC Reduction\n\n```mermaid\ngraph TD\n    Start[\"Input : TS and Fairness F\"]\n    SCC[\"Step 1 : Compute SCCs of TS\"]\n    Mark[\"Step 2 : Mark each SCC as FAIR or UNFAIR<br/>based on coverage of F = f1 to fn\"]\n    Reach[\"Step 3 : Find states that REACH a fair SCC\"]\n    FairSet[\"Step 4 : FairStates = Reach Fair SCC\"]\n    EG[\"Step 5 : Apply Fair Sat EG phi<br/>Greatest fixed point over FairStates\"]\n    Out[\"Output : Set of states satisfying fair EG phi\"]\n    \n    Start --> SCC --> Mark --> Reach --> FairSet --> EG --> Out\n```\n\n### 4.4 Mermaid Diagram — Round-Robin Coffee Machine Analogy\n\n```mermaid\ngraph LR\n    Scheduler[\"Scheduler<br/>Non-deterministic choice\"]\n    Anu[\"Process P1 : Anu\"]\n    Binu[\"Process P2 : Binu\"]\n    Chris[\"Process P3 : Chris\"]\n    Deepa[\"Process P4 : Deepa\"]\n    \n    Scheduler --> Anu\n    Scheduler --> Binu\n    Scheduler --> Chris\n    Scheduler --> Deepa\n    \n    Anu -.enabled.-> Anu\n    Binu -.enabled.-> Binu\n    Chris -.enabled.-> Chris\n    Deepa -.enabled.-> Deepa\n    \n    Anu -.fair.-> Binu\n    Binu -.fair.-> Chris\n    Chris -.fair.-> Deepa\n    Deepa -.fair.-> Anu\n```\n\n### 4.5 Mermaid Diagram — Generalized Büchi to Standard Büchi Conversion\n\n```mermaid\ngraph TD\n    GB[\"Generalized Buchi Automaton<br/>with n accepting sets F1 to Fn\"]\n    Product[\"Product with Fairness F\"]\n    Standard[\"Standard Buchi Automaton<br/>with single accepting set F prime\"]\n    Emptiness[\"Emptiness Check<br/>Search for fair cycle\"]\n    Result[\"Result : TS models F phi\"]\n    \n    GB --> Product --> Standard --> Emptiness --> Result\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A Questions (3 Marks Each)\n\n#### **Q1. [KTU University Exam — May 2024] Define fairness in the context of model checking. Why is it required?**\n\n**Model Answer (3 marks):**\n\nFairness is a property of execution paths in a transition system that restricts the scheduler's non-deterministic choices. A path is said to be *fair* with respect to a set of fairness constraints $\\mathcal{F} = \\{f_1, \\ldots, f_n\\}$ if every $f_i$ holds infinitely often along the path. Formally:\n\n$$\\pi \\models \\mathcal{F} \\iff \\forall i.\\ \\ \\pi \\models \\Box \\Diamond \\, f_i$$\n\n**Why required:** Concurrent systems have exponentially many execution paths. Many of these are *unrealistic* (e.g., a process perpetually starved by the scheduler). Fairness filters out such starvation paths so that the verification results reflect the behavior of *realistic* schedulers. Without fairness, model checking often produces false negatives — it reports violations that cannot occur in any real implementation.\n\n---\n\n#### **Q2. [KTU University Exam — Dec 2023] Distinguish between weak fairness and strong fairness with LTL formulas.**\n\n**Model Answer (3 marks):**\n\n| Aspect | Weak Fairness (Justice) | Strong Fairness (Compassion) |\n|--------|-------------------------|------------------------------|\n| **Antecedent (LHS)** | $\\Box \\Diamond \\, en(a)$ — enabled infinitely often | $\\Diamond \\Box \\, en(a)$ — enabled continuously eventually |\n| **Consequent (RHS)** | $\\Box \\Diamond \\, \\langle a \\rangle$ — executed infinitely often | $\\Box \\Diamond \\, \\langle a \\rangle$ — executed infinitely often |\n| **LTL formula** | $\\Box \\Diamond \\, en(a) \\Rightarrow \\Box \\Diamond \\, \\langle a \\rangle$ | $\\Diamond \\Box \\, en(a) \\Rightarrow \\Box \\Diamond \\, \\langle a \\rangle$ |\n| **Strength** | Stronger assumption (more paths excluded) | Weaker assumption (more paths admitted) |\n\n**One-line difference:** Weak fairness requires action to be *almost always* enabled, while strong fairness only requires it to be enabled *frequently* but persistently.\n\n---\n\n### 5.2 Part B Questions (14 Marks Each — Internal Choice)\n\n#### **Question A (14 Marks) — [KTU University Exam — June 2024]**\n\n**(a)** Explain the three types of fairness (impartial, weak, strong) with formal LTL definitions. Provide a small example to illustrate how a path may satisfy one but not the other. **(7 marks)**\n\n**(b)** Describe the Emerson-Lei algorithm for fair CTL model checking. Show how fairness reduces to finding states on a *fair SCC*. **(7 marks)**\n\n**Model Solution:**\n\n**Part (a) — 7 marks:**\n\n**(i) Impartial (Unconditional) Fairness (2 marks):** Impartial fairness requires that every action in a designated set $A = \\{a_1, \\ldots, a_n\\}$ be executed infinitely often, regardless of whether it is enabled or not. The LTL formula is:\n\n$$\\text{Impartial} \\equiv \\bigwedge_{i=1}^{n} \\Box \\Diamond \\, a_i$$\n\nThis is the strongest assumption and is rarely used in practice because it is unrealistic — a process that is *never enabled* would still be required to fire.\n\n**(ii) Weak Fairness (2 marks):** Weak fairness (also called *justice*) is conditional on enabledness. The LTL formula is:\n\n$$\\text{WeakFair}(a) \\equiv \\Box \\Diamond \\, en(a) \\;\\Rightarrow\\; \\Box \\Diamond \\, \\langle a \\rangle$$\n\nRead: *\"If action $a$ is enabled infinitely often, then it must be executed infinitely often.\"* This corresponds to a real scheduler that *eventually* services a long-running process.\n\n**(iii) Strong Fairness (2 marks):** Strong fairness (also called *compassion*) only requires that an action be enabled *continuously from some point* (rather than infinitely often). LTL:\n\n$$\\text{StrongFair}(a) \\equiv \\Diamond \\Box \\, en(a) \\;\\Rightarrow\\; \\Box \\Diamond \\, \\langle a \\rangle$$\n\n**Counter-example (1 mark):** Consider a process that is enabled in states $s_0, s_1, s_4, s_5, s_8, s_9, \\ldots$ (i.e., in pairs separated by gaps). It is *not* enabled in $s_2, s_3, s_6, s_7, \\ldots$ This means it is *not* continuously enabled eventually, so strong fairness's antecedent $\\Diamond \\Box \\, en(a)$ is false. But it IS enabled infinitely often, so weak fairness DOES require it to fire. Thus the path can be *strong-unfair* but *weak-fair* — illustrating the difference.\n\n**Part (b) — 7 marks:**\n\nThe Emerson-Lei algorithm (1986) computes the satisfaction set of fair CTL formulas in polynomial time.\n\n**Step 1 (1 mark):** Given $TS = (S, S_0, \\rightarrow)$ and fairness set $\\mathcal{F} = \\{f_1, \\ldots, f_n\\}$, define a *fair cycle* as a cycle that visits each $f_i$ at least once.\n\n**Step 2 (2 marks):** Compute the SCCs of $TS$ (Tarjan's algorithm, $O(|S| + |\\rightarrow|)$). An SCC $C$ is *fair* iff $C \\cap \\text{Sat}(f_i) \\neq \\emptyset$ for all $i \\in \\{1, \\ldots, n\\}$.\n\n**Step 3 (2 marks):** Define $\\text{FairReach}(s)$ = \"there is a fair SCC reachable from $s$\". Compute this by:\n$$\\text{FairReach} = \\mu Z.\\ \\text{FairSCCs} \\cup \\text{pre}_\\exists(Z)$$\n(least fixed-point over the set of states that can reach a fair SCC).\n\n**Step 4 (2 marks):** For a formula like $EG\\,\\varphi$ under fairness:\n\n$$\\text{Sat}(EG\\,\\varphi) = \\nu Y.\\ \\ \\text{Sat}(\\varphi) \\cap \\text{pre}_\\exists(Y) \\cap \\text{FairReach}$$\n\nThe greatest fixed-point is taken because $EG\\,\\varphi$ requires *infinite* fair paths.\n\n**Why it works:** By the **Büchi-Landweber theorem** (1969), fair emptiness is decidable, and Emerson-Lei operationalizes this using SCC decomposition.\n\n---\n\n#### **Question B (14 Marks) — [KTU University Exam — Dec 2023]**\n\n**(a)** State and explain the relationship between fairness, Büchi automata, and $\\omega$-regular languages. Show how a generalized Büchi acceptance condition can be reduced to a standard Büchi condition. **(7 marks)**\n\n**(b)** A two-process mutual exclusion system is modeled in PROMELA. Explain how SPIN applies fairness by default. Discuss the difference between *weak fairness* and *strong fairness* in this context with an example. **(7 marks)**\n\n**Model Solution:**\n\n**Part (a) — 7 marks:**\n\n**(i) Büchi and $\\omega$-regular (2 marks):** A *Büchi automaton* is a finite automaton over infinite words. A run is accepting iff it visits a designated set of *accepting states* $F$ infinitely often. The class of languages accepted by Büchi automata is exactly the class of $\\omega$-regular languages, which is the natural specification language for non-terminating reactive systems.\n\n**(ii) Fairness and Büchi equivalence (2 marks):** A fairness constraint $\\mathcal{F} = \\{f_1, \\ldots, f_n\\}$ is naturally expressed as a *generalized Büchi* acceptance condition with $n$ accepting sets $F_1, \\ldots, F_n$, where $F_i$ consists of states satisfying $f_i$. A path is *fair* iff it visits each $F_i$ infinitely often.\n\n**(iii) Reduction to standard Büchi (3 marks):** The standard construction takes a generalized Büchi automaton $\\mathcal{B} = (Q, \\Sigma, \\delta, q_0, \\{F_1, \\ldots, F_n\\})$ and produces a standard Büchi automaton $\\mathcal{B}' = (Q \\times \\{1, \\ldots, n\\}, \\Sigma, \\delta', (q_0, 1), Q \\times \\{1\\})$. The transition function $\\delta'$ is:\n\n$$\\delta'((q, i), a) = \\left\\{ \\begin{array}{ll}\n\\{ (q', (i \\bmod n) + 1) \\mid q' \\in \\delta(q,a) \\cap F_i \\} & \\text{if } \\delta(q,a) \\cap F_i \\neq \\emptyset \\\\\n\\{ (q', i) \\mid q' \\in \\delta(q,a) \\} & \\text{otherwise}\n\\end{array}\\right.$$\n\nThe 2nd component \"rotates\" through $\\{1, \\ldots, n\\}$ each time an $F_i$ is visited. The single accepting set $F' = Q \\times \\{1\\}$ ensures that the rotation completes infinitely often — equivalent to visiting every $F_i$ infinitely often.\n\n**Part (b) — 7 marks:**\n\n**(i) SPIN and fairness (2 marks):** SPIN's PROMELA language supports fairness via the `fair` keyword, e.g., `fair` label on a process declaration. By default, SPIN's verifier checks properties under *no* fairness, but the user can declare specific processes as `fair` or specify fairness on atomic statements, which is treated as *weak fairness* by default.\n\n**(ii) Example (3 marks):** Consider two processes in mutual exclusion with a shared semaphore `sem`:\n\n```promela\nmtype { idle, want, in_cs }\n\nactive proctype P1() {\n    do\n    :: state = want;        /* request entry */\n       atomic { state == want -> state = in_cs };  /* atomic test-and-set */\n       /* critical section */\n       state = idle;        /* release */\n    od\n}\n```\n\nWithout fairness, SPIN would accept a counter-example where P1 is *always* selected by the scheduler and P2 is starved — even though P2 is enabled. With `fair P1; fair P2;`, SPIN applies *weak fairness* to each process: each enabled process must eventually be scheduled.\n\n**(iii) Weak vs strong in SPIN (2 marks):** SPIN's default `fair` keyword is **weak fairness**. To express **strong fairness**, the user must explicitly state conditions like \"if P1's `state == want` is enabled infinitely often, then P1 will eventually enter the critical section\" — typically done with additional LTL properties or by careful use of atomic blocks and channel polling.\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common mark-loss pitfalls in fairness questions:**\n> 1. **Confusing $\\Box \\Diamond$ with $\\Diamond \\Box$:** $\\Box \\Diamond \\, \\varphi$ means \"infinitely often\" (used in impartial/strong LHS), while $\\Diamond \\Box \\, \\varphi$ means \"eventually forever\" (used in strong fairness LHS). Swapping them is an instant **−2 mark** deduction.\n> 2. **Mixing up the direction of implication:** Weak/strong fairness are of the form *antecedent* $\\Rightarrow$ *consequent*. The consequent is always $\\Box \\Diamond \\,\\langle a \\rangle$ (action fires infinitely often). The antecedent differs.\n> 3. **Forgetting the \"infinitely often\" semantics in the LHS of impartial fairness:** Writing $\\Diamond \\, a_i$ (just \"eventually\") is wrong — it should be $\\Box \\Diamond \\, a_i$ (infinitely often).\n> 4. **Not stating the SCC-fairness condition explicitly:** In Emerson-Lei questions, you must write \"an SCC is fair iff it intersects the satisfaction set of every fairness constraint $f_i$\" — examiners award 2 marks for this exact phrase.\n> 5. **Confusing fair model checking with probabilistic model checking:** Fairness is *qualitative* (every fair path satisfies $\\varphi$), not probabilistic. Do not write $P(\\varphi) > 0$ style answers.\n\n---\n\n### 5.4 Topic Recap & Important Things to Remember\n\n- **Fairness** is a *path-level* property that filters out unrealistic scheduler choices in concurrent systems.\n- **Three flavors:** Impartial (unconditional), Weak (justice), Strong (compassion) — in order of decreasing restrictiveness.\n- **LTL signatures:**\n  - Impartial: $\\bigwedge_i \\Box \\Diamond \\, a_i$\n  - Weak: $\\Box \\Diamond \\, en(a) \\Rightarrow \\Box \\Diamond\\, \\langle a \\rangle$\n  - Strong: $\\Diamond \\Box \\, en(a) \\Rightarrow \\Box \\Diamond \\,\\langle a \\rangle$\n- **Emerson-Lei** reduces fair CTL to SCC-decomposition + fixed-point computation.\n- **Büchi equivalence:** Fairness = generalized Büchi acceptance; reducible to standard Büchi with $|Q| \\cdot n$ states.\n- **Practical use:** SPIN's `fair` keyword, NuSMV's `FAIRNESS` declaration, UPPAAL's fair transition.\n- **Real-world analog:** Coffee machine round-robin; no one starves under fair scheduling.\n- **Common pitfall:** $\\Box \\Diamond \\neq \\Diamond \\Box$; remember the *infinitely often* semantics.\n- **Verification power:** Fairness eliminates false negatives in mutual exclusion, liveness, and progress properties.\n- **Tool support:** SPIN, NuSMV, UPPAAL, CBMC, Java PathFinder all support fairness declarations.\n- **Vardi-Wolper (1986):** Fairness in temporal logic is decidable via Büchi emptiness — the foundation of modern model checking.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t27",
          "title": "Unconditional",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Unconditional\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition\n\nIn the context of **Automated Verification** and **Model Checking**, the term **Unconditional** primarily refers to a specific notion of **fairness** applied to the execution paths of a concurrent, reactive, or distributed system.\n\n**Unconditional Fairness (also called Impartiality)** is the strongest standard fairness condition. It is a property of an *infinite execution path* $\\pi = s_0 \\to s_1 \\to s_2 \\to \\dots$ in a labeled transition system (LTS) or a Kripke structure $M = (S, S_0, R, L)$.\n\nA path $\\pi$ is said to satisfy **unconditional fairness** with respect to a set of actions/transitions $A$ if **every transition that is continuously enabled along the path is eventually taken**.\n\nFormally, for each action $a \\in A$:\n\n$$\n\\forall a \\in A : \\left( \\forall i \\ge 0 : \\pi \\models \\text{enabled}(a) \\text{ at step } i \\right) \\implies \\left( \\exists j \\ge 0 : a \\text{ is taken at step } j \\right)\n$$\n\nA model $M$ satisfies a temporal logic property $\\phi$ **unconditionally** if every possible path of $M$ satisfies $\\phi$ under this impartiality assumption.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Mapping:** In PECNT526 (Automated Verification), \"Unconditional\" is the entry-level concept of Module 1. It forms the basis for understanding *fair transition systems*, which are crucial for verifying *liveness properties* (something good eventually happens) as opposed to *safety properties* (something bad never happens).\n\n### 1.2 Conceptual Analogy / Intuition\n\nImagine a **busy traffic intersection with no traffic lights**. Cars arrive from four directions continuously, and all drivers are perfectly polite — this is *unconditional fairness*. Whenever a car is \"enabled\" (it has arrived and is waiting), it **must eventually** get to go. No car can be starved forever.\n\nContrast this with:\n- **Weak (Justice) Fairness:** A car can only complain of starvation if it had *some chance* to enter the intersection. If a car never even gets close, it has no complaint.\n- **Strong Fairness:** A car can complain only if it was *enabled infinitely often*. If it arrived once and then gave up, it has no complaint.\n- **Unconditional Fairness:** A car can complain **the instant it is enabled and stays enabled**. It is the strictest, most egalitarian rule.\n\n### 1.3 Standard Metrics & Notation\n\n- **Path:** Denoted $\\pi = s_0, s_1, s_2, \\dots$ where $s_i \\to s_{i+1}$ is a valid transition.\n- **Set of Fair Paths:** $Fair(M) \\subseteq Paths(M)$.\n- **Unconditional Fair Satisfaction:** $M \\models_{uncond} \\phi \\iff \\forall \\pi \\in Paths(M) : \\pi \\models \\phi$.\n- **Action Set:** $A$ is finite and non-empty in standard LTS models.\n\n> [!NOTE]\n> In KTU board examinations, students are often tested on the **classification of fairness** and the **effect of unconditional fairness on CTL/LTL formulas** (especially the $\\Diamond \\Box$ and $\\Box \\Diamond$ operators).\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Breaking Down the Concept\n\nThe principle of \"Unconditional\" verification in model checking rests on three pillars:\n\n- **Pillar 1 — The State Space Abstraction**\n  - *Why:* Real systems have infinite or astronomically large state spaces. We abstract the system into a *finite* model $M$ (a Kripke Structure).\n  - *How:* Each global system state is collapsed into a single node; transitions become directed edges labeled with actions.\n\n- **Pillar 2 — The Path Universe**\n  - *Why:* A system does not have just one execution; it has a tree of all possible infinite executions.\n  - *How:* A *path* is an infinite walk. In KTU papers, the path is usually written as $\\pi = s_0, s_1, s_2, \\dots$\n\n- **Pillar 3 — The Unconditional Quantification**\n  - *Why:* We cannot just verify \"all paths\" blindly because some paths are physically unrealizable (e.g., a transition that is forever enabled but never taken).\n  - *How:* Unconditional fairness **prunes** the path universe to keep only those paths where no enabled transition is starved. The verifier then quantifies over this pruned set.\n\n### 2.2 Liveness vs. Safety Under Unconditional Fairness\n\n| Property Type | Nature | Unconditional Fairness Impact |\n|---|---|---|\n| **Safety** ($\\Box \\neg \\text{bad}$) | Bad thing *never* happens | **Independent** of fairness. Unconditional fairness adds no information to safety. |\n| **Liveness** ($\\Diamond \\text{good}$) | Good thing *eventually* happens | **Heavily dependent.** Without unconditional fairness, a path can avoid taking an enabled transition forever. With it, the formula is forced to hold. |\n| **Persistence** ($\\Diamond \\Box \\text{stable}$) | System eventually stabilizes | Affected — once reached, stability cannot be escaped by ignoring an enabled transition. |\n\n### 2.3 KTU Formula Sheet / Cheat Sheet\n\n| Concept | Mathematical Notation | Plain Meaning | KTU Exam Use |\n|---|---|---|---|\n| Unconditional Fair Path | $\\forall a \\in A : \\Box \\text{enabled}(a) \\Rightarrow \\Diamond \\text{taken}(a)$ | Every continuously enabled action is eventually taken | Defining impartiality |\n| Unconditional Satisfaction | $M \\models_{uncond} \\phi$ | $\\phi$ holds on all paths of $M$ under impartiality | Direct 3-mark definition |\n| Weak Fairness | $\\forall a \\in A : \\Box \\Diamond \\text{enabled}(a) \\Rightarrow \\Box \\Diamond \\text{taken}(a)$ | If enabled infinitely often, taken infinitely often | Contrast question |\n| Strong Fairness | $\\forall a \\in A : \\Diamond \\Box \\text{enabled}(a) \\Rightarrow \\Box \\Diamond \\text{taken}(a)$ | If eventually permanently enabled, taken infinitely often | Contrast question |\n| Action Set | $A$ | Finite, non-empty set of atomic transitions | Used in formulas |\n| Path | $\\pi = s_0 \\to s_1 \\to s_2 \\to \\dots$ | Infinite sequence of states | Drawing Kripke structures |\n| Liveness Formula | $\\Diamond q$ | \"Eventually $q$\" | Liveness part (a) questions |\n| Persistence Formula | $\\Diamond \\Box q$ | \"Eventually always $q$\" | Liveness part (b) questions |\n| State Predicate | $p, q$ | Propositional labels on states | Atomic propositions |\n| Set of Fair Paths | $Fair(M)$ | Subset of all paths that obey the fairness condition | Standard symbol |\n| Unconditional CTL* | $\\forall \\pi \\in Fair(M) : \\pi \\models \\phi$ | Path quantifier with unconditional fairness | ESE Part B questions |\n\n> [!IMPORTANT]\n> **Crucial Distinction for KTU:** Unconditional fairness is *impartial*. It treats every action identically. This is different from *strong* and *weak* fairness, which distinguish between \"eventually enabled forever\" and \"enabled infinitely often\".\n\n### 2.4 Real-World Engineering Utility\n\nUnconditional fairness assumptions are used in:\n- **Communication Protocols** (e.g., ensuring every packet in a buffer eventually gets transmitted).\n- **Operating System Schedulers** (impartial schedulers that starve no process).\n- **Distributed Databases** (ensuring every committed transaction eventually appears in the log).\n- **VLSI Design Verification** (proving that no request line is permanently ignored by an arbiter).\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Derivation: From Plain Satisfaction to Unconditional Fair Satisfaction\n\n**Goal:** Express the satisfaction relation $M, s_0 \\models_{uncond} \\phi$ formally.\n\n**Step 1 — Define the bare satisfaction of a state in a model.**\nA state $s_0$ in model $M$ satisfies a CTL* formula $\\phi$ if every path starting at $s_0$ satisfies $\\phi$.\n\n$$\nM, s_0 \\models \\phi \\iff \\forall \\pi \\in Paths(M, s_0) : \\pi \\models \\phi\n$$\n\n*Logic:* Without any fairness assumption, the model checker must consider *all* infinite walks, including those that unrealistically starve a transition. This gives a *too strong* liveness verdict (false positives are common).\n\n**Step 2 — Define the set of unconditionally fair paths.**\nLet $A$ be the set of all atomic actions of $M$. A path $\\pi$ is *unconditionally fair* if for every action $a \\in A$:\n\n$$\n\\Box \\left( \\text{enabled}_\\pi(a) \\right) \\implies \\Diamond \\left( \\text{taken}_\\pi(a) \\right)\n$$\n\n*Logic:* The condition $\\Box(\\text{enabled})$ means \"always enabled\" along the path. The conclusion $\\Diamond(\\text{taken})$ means \"eventually taken\". This is the mathematical embodiment of impartiality.\n\n**Step 3 — Restrict the path quantifier to the fair set.**\nWe define $Paths_{uncond}(M, s_0)$ as the set of all unconditionally fair paths starting at $s_0$:\n\n$$\nPaths_{uncond}(M, s_0) = \\left\\{ \\pi \\in Paths(M, s_0) \\mid \\forall a \\in A : \\pi \\models \\Box \\text{enabled}(a) \\Rightarrow \\Diamond \\text{taken}(a) \\right\\}\n$$\n\n**Step 4 — Define unconditional fair satisfaction.**\nSubstitute the restricted path set back into the satisfaction definition:\n\n$$\n\\begin{aligned}\nM, s_0 \\models_{uncond} \\phi &\\iff \\forall \\pi \\in Paths_{uncond}(M, s_0) : \\pi \\models \\phi \\\\\n&\\iff \\forall \\pi \\in Paths(M, s_0) : \\left( \\forall a \\in A : \\Box \\text{en}(a) \\Rightarrow \\Diamond \\text{tk}(a) \\right) \\Rightarrow \\pi \\models \\phi\n\\end{aligned}\n$$\n\n*Logic:* The quantifier is restricted. Only \"physically reasonable\" paths are considered, eliminating the false positives of the bare model.\n\n### 3.2 Analytical Matrix: Comparing Fairness Variants\n\nThe three standard fairness conditions can be ranked by *strictness*. The strictest implies the weaker ones in the same model.\n\n| Rank | Fairness Type | Condition per Action $a$ | Strength |\n|---|---|---|---|\n| 1 (Weakest) | Weak (Justice) | $\\Box \\Diamond \\text{enabled}(a) \\Rightarrow \\Box \\Diamond \\text{taken}(a)$ | Only complains if enabled $\\infty$-often |\n| 2 (Middle) | Strong (Compassion) | $\\Diamond \\Box \\text{enabled}(a) \\Rightarrow \\Box \\Diamond \\text{taken}(a)$ | Only complains if eventually permanently enabled |\n| 3 (Strictest) | **Unconditional (Impartiality)** | $\\Box \\, \\text{enabled}(a) \\Rightarrow \\Diamond \\, \\text{taken}(a)$ | Complains the instant it is enabled |\n\n### 3.3 Worked Example: Verifying a Liveness Property\n\n**Problem:** Consider a Kripke structure $M$ with three states $s_0, s_1, s_2$ and actions $\\{a, b\\}$. State $s_0$ has outgoing transitions $s_0 \\xrightarrow{a} s_1$ and $s_0 \\xrightarrow{b} s_2$. Both $s_1$ and $s_2$ are absorbing (self-loops). Check whether $M, s_0 \\models_{uncond} \\Diamond (s_1 \\lor s_2)$.\n\n**Step 1 — Enumerate all paths from $s_0$.**\n- $\\pi_1$: $s_0 \\xrightarrow{a} s_1 \\xrightarrow{a} s_1 \\to \\dots$\n- $\\pi_2$: $s_0 \\xrightarrow{b} s_2 \\xrightarrow{b} s_2 \\to \\dots$\n- $\\pi_3$: $s_0 \\xrightarrow{a} s_1 \\xrightarrow{b} s_1 \\to \\dots$ (not valid since $s_1$ has no $b$-transition)\n- $\\pi_4$: $s_0 \\xrightarrow{b} s_2 \\xrightarrow{a} s_2 \\to \\dots$ (similarly not valid)\n\n**Step 2 — Check unconditional fairness for each valid path.**\n- $\\pi_1$: After step 0, action $a$ is no longer enabled. Action $b$ is *never* enabled from $s_1$. The antecedent $\\Box \\text{enabled}(b)$ is false, so the fairness obligation for $b$ is vacuously satisfied. Action $a$ is taken at step 0, so the obligation for $a$ is satisfied. $\\pi_1$ is **fair**.\n- $\\pi_2$: By symmetric reasoning, $\\pi_2$ is **fair**.\n\n**Step 3 — Check the LTL property $\\Diamond(s_1 \\lor s_2)$.**\n- $\\pi_1$: At step 1, the state is $s_1$, so the property holds.\n- $\\pi_2$: At step 1, the state is $s_2$, so the property holds.\n\n**Step 4 — Conclusion.**\nSince all unconditionally fair paths satisfy the property:\n\n$$\nM, s_0 \\models_{uncond} \\Diamond (s_1 \\lor s_2)\n$$\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Concept Map: Unconditional Fairness in the Verification Pipeline\n\n```mermaid\nflowchart TD\n    sysA[\"Reactive System Specification\"] --> bldM[\"Build Kripke Structure M\"]\n    bldM --> allP[\"Enumerate All Paths in M\"]\n    allP --> prnF[\"Prune Path Universe using Unconditional Fairness\"]\n    prnF --> fairP[\"Set of Unconditionally Fair Paths Fair_M\"]\n    fairP --> propPhi[\"Specify Property phi in CTL or LTL\"]\n    propPhi --> checkE[\"Model Check: all pi in Fair_M satisfy phi\"]\n    checkE --> verdict1[\"SAT: Property Holds\"]\n    checkE --> verdict2[\"UNSAT: Counterexample Path Returned\"]\n    style sysA fill:#f9e79f,stroke:#333,stroke-width:2px\n    style bldM fill:#aed6f1,stroke:#333,stroke-width:2px\n    style allP fill:#a9dfbf,stroke:#333,stroke-width:2px\n    style prnF fill:#f5b7b1,stroke:#333,stroke-width:2px\n    style fairP fill:#d7bde2,stroke:#333,stroke-width:2px\n    style propPhi fill:#aed6f1,stroke:#333,stroke-width:2px\n    style checkE fill:#f9e79f,stroke:#333,stroke-width:2px\n    style verdict1 fill:#82e0aa,stroke:#333,stroke-width:2px\n    style verdict2 fill:#f1948a,stroke:#333,stroke-width:2px\n```\n\n### 4.2 Block Architecture: The Role of Unconditional Fairness in Fair Transition Systems\n\n```mermaid\nflowchart LR\n    subgraph Kripke[\"Kripke Structure M = (S, S0, R, L)\"]\n        dirS[\"Set of States S\"]\n        dirI[\"Initial States S0\"]\n        dirT[\"Transition Relation R\"]\n        dirL[\"Labeling Function L\"]\n    end\n    subgraph FairCore[\"Fairness Subsystem\"]\n        actA[\"Action Set A = {a1, a2, ..., an}\"]\n        condU[\"Unconditional Fairness Condition: For all a: Box enabled(a) implies Diamond taken(a)\"]\n        fairSet[\"Fair Paths Set Fair_M\"]\n    end\n    subgraph Logic[\"Temporal Logic Layer\"]\n        ctl[\"CTL / LTL Formula phi\"]\n        sat[\"Satisfaction Relation: M models uncond phi\"]\n    end\n    dirS --> fairSet\n    dirT --> fairSet\n    dirI --> fairSet\n    actA --> condU\n    condU --> fairSet\n    fairSet --> sat\n    ctl --> sat\n    style dirS fill:#d6eaf8,stroke:#333,stroke-width:2px\n    style dirI fill:#d6eaf8,stroke:#333,stroke-width:2px\n    style dirT fill:#d6eaf8,stroke:#333,stroke-width:2px\n    style dirL fill:#d6eaf8,stroke:#333,stroke-width:2px\n    style actA fill:#fdebd0,stroke:#333,stroke-width:2px\n    style condU fill:#fadbd8,stroke:#333,stroke-width:2px\n    style fairSet fill:#d5f5e3,stroke:#333,stroke-width:2px\n    style ctl fill:#e8daef,stroke:#333,stroke-width:2px\n    style sat fill:#fff2cc,stroke:#333,stroke-width:2px\n```\n\n### 4.3 Path Pruning Visualization\n\n```mermaid\nflowchart TD\n    allSet[\"All Paths in M\"] --> split{\"Is path pi fair under unconditional rule?\"}\n    split -- \"Yes\" --> keep[\"Keep in Fair_M\"]\n    split -- \"No: some enabled action is starved forever\" --> drop[\"Discard from Fair_M\"]\n    keep --> verif[\"Verify phi on Fair_M\"]\n    drop --> verif\n    style allSet fill:#aed6f1,stroke:#333,stroke-width:2px\n    style split fill:#f9e79f,stroke:#333,stroke-width:2px\n    style keep fill:#82e0aa,stroke:#333,stroke-width:2px\n    style drop fill:#f5b7b1,stroke:#333,stroke-width:2px\n    style verif fill:#d7bde2,stroke:#333,stroke-width:2px\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A Questions (3 Marks Each)\n\n---\n\n**Q1.** `[KTU University Exam - Dec 2023]`\n**Define unconditional fairness in the context of transition systems. How does it differ from weak and strong fairness?**\n\n**Model Answer (3 Marks):**\n\n*Unconditional fairness* (also called *impartiality*) is the strictest standard fairness condition. In a transition system with action set $A$, a path $\\pi$ is unconditionally fair if for every action $a \\in A$:\n\n$$\n\\Box \\, \\text{enabled}_\\pi(a) \\implies \\Diamond \\, \\text{taken}_\\pi(a)\n$$\n\nThat is, **every action that is continuously enabled along the path must be eventually taken**.\n\n**Differentiation:**\n- *Weak fairness* ($\\Box \\Diamond \\text{en} \\Rightarrow \\Box \\Diamond \\text{tk}$) — complains only when an action is enabled *infinitely often*.\n- *Strong fairness* ($\\Diamond \\Box \\text{en} \\Rightarrow \\Box \\Diamond \\text{tk}$) — complains only when an action is *eventually permanently enabled*.\n- *Unconditional fairness* — complains the *instant* the action becomes and stays enabled. Hence, **unconditional fairness implies strong fairness, which in turn implies weak fairness.**\n\n---\n\n**Q2.** `[KTU University Exam - June 2024]`\n**State and explain the role of the action set $A$ in the formulation of unconditional fair satisfaction. Why is it required to be finite?**\n\n**Model Answer (3 Marks):**\n\nThe action set $A = \\{a_1, a_2, \\dots, a_n\\}$ is the finite collection of atomic transitions of the system. In the unconditional fair satisfaction relation:\n\n$$\nM, s_0 \\models_{uncond} \\phi \\iff \\forall \\pi \\in Paths_{uncond}(M, s_0) : \\pi \\models \\phi\n$$\n\nthe *fairness obligation* must be discharged for **every** $a \\in A$.\n\n**Finiteness is required** because:\n- It guarantees that the universal quantification over $A$ is decidable.\n- It allows the model checker (e.g., SPIN, NuSMV) to pre-compute the set of fair paths using automata-theoretic constructions with finite Büchi or Streett acceptance conditions.\n- If $A$ were infinite, the verification problem would be semi-decidable at best, breaking the fully-automatic guarantee of automated verification.\n\n---\n\n### 5.2 Part B Questions (14 Marks Each — Internal Choice Pattern)\n\n> **Note on Pattern:** Each KTU Part B question on this topic carries 14 marks and is split into two sub-parts of 7 marks each. The model answers below mirror KTU's valuation key.\n\n---\n\n### **Question A** `[KTU University Exam - Dec 2023]` — 14 Marks\n\n**(a)** With a neat diagram, explain the operational semantics of unconditional fair paths in a Kripke structure. Highlight the role of the transition relation. **(7 Marks)**\n\n**(b)** Consider a Kripke structure $M$ with states $\\{s_0, s_1, s_2, s_3\\}$, initial state $s_0$, and actions $A = \\{a, b\\}$. Transitions are:\n- $s_0 \\xrightarrow{a} s_1$\n- $s_0 \\xrightarrow{b} s_2$\n- $s_1 \\xrightarrow{a} s_3$\n- $s_2 \\xrightarrow{b} s_3$\n- $s_3 \\xrightarrow{a} s_3$ and $s_3 \\xrightarrow{b} s_3$ (self-loop with both actions enabled)\n\nVerify the liveness property $M, s_0 \\models_{uncond} \\Diamond s_3$ and show all valid paths. **(7 Marks)**\n\n#### Model Solution\n\n**Part (a) — 7 Marks:**\n\nA Kripke structure $M = (S, S_0, R, L)$ represents the system. Here $R \\subseteq S \\times S$ is the *transition relation*, and $L : S \\to 2^{AP}$ labels each state with atomic propositions.\n\nA *path* $\\pi$ in $M$ is an infinite sequence $\\pi = s_0, s_1, s_2, \\dots$ such that for all $i \\ge 0$, $(s_i, s_{i+1}) \\in R$. The set of *all* paths from $s_0$ is denoted $Paths(M, s_0)$.\n\nThe **unconditional fair path** subset is defined by:\n\n$$\nPaths_{uncond}(M, s_0) = \\left\\{ \\pi \\in Paths(M, s_0) \\;\\middle|\\; \\forall a \\in A : \\pi \\models \\Box \\, \\text{en}(a) \\Rightarrow \\Diamond \\, \\text{tk}(a) \\right\\}\n$$\n\nThe transition relation $R$ *generates* the raw path universe; the fairness condition *filters* it.\n\n**Diagram (Kripke Structure):**\n\n```\n        a\n   s0 -----> s1\n   |          |\n   | b      a |\n   v          v\n   s2 -----> s3\n        b\n```\n(Where $s_3$ has self-loops on both $a$ and $b$.)\n\n**Operational Intuition:**\n1. Start at $s_0$.\n2. The transition relation $R$ permits the system to choose either $a$ or $b$.\n3. A scheduler (environment) may attempt to \"starve\" one action. Unconditional fairness *forbids* this.\n4. Hence, whichever action is *continuously enabled* must be taken.\n\n**(Marks Breakdown: Diagram = 2, Definition of $R$ = 2, Filter formula = 2, Operational example = 1.)**\n\n**Part (b) — 7 Marks:**\n\n**Step 1 — Enumerate all paths from $s_0$.**\n\nThere are two major classes of paths:\n- $\\pi_{ab}$: $s_0 \\xrightarrow{a} s_1 \\xrightarrow{a} s_3 \\xrightarrow{*} s_3 \\dots$\n- $\\pi_{ba}$: $s_0 \\xrightarrow{b} s_2 \\xrightarrow{b} s_3 \\xrightarrow{*} s_3 \\dots$\n\n(In $s_3$, both $a$ and $b$ are continuously enabled. The unconditional fairness rule forces *both* to be taken infinitely often, so $s_3$ must be visited infinitely often with both actions.)\n\n**Step 2 — Check the fairness of each path.**\n- $\\pi_{ab}$: After step 0, action $a$ is taken. At $s_1$, action $b$ is *not* enabled (no $b$-transition from $s_1$). The antecedent $\\Box \\, \\text{en}(b)$ is false → $b$'s fairness is vacuously satisfied. Action $a$ is continuously enabled in $s_3$ and is taken (the path visits $s_3$ via $a$). Fairness holds. ✓\n- $\\pi_{ba}$: Symmetric to $\\pi_{ab}$. Action $b$ is enabled in $s_0$ and taken; in $s_3$, both are enabled and both are taken. Fairness holds. ✓\n\n**Step 3 — Check the liveness property $\\Diamond s_3$.**\n- $\\pi_{ab}$: state $s_3$ is reached at step 2 → property holds. ✓\n- $\\pi_{ba}$: state $s_3$ is reached at step 2 → property holds. ✓\n\n**Step 4 — Conclusion.**\nAll unconditionally fair paths satisfy the property:\n\n$$\nM, s_0 \\models_{uncond} \\Diamond s_3 \\quad \\checkmark\n$$\n\n---\n\n### **Question B (Alternative Choice)** `[KTU University Exam - June 2024]` — 14 Marks\n\n**(a)** Compare and contrast unconditional fairness with weak and strong fairness using a common transition system example. Justify which one is the strictest. **(7 Marks)**\n\n**(b)** Prove analytically that *unconditional fairness is strictly stronger than strong fairness*, and *strong fairness is strictly stronger than weak fairness*. **(7 Marks)**\n\n#### Model Solution\n\n**Part (a) — 7 Marks:**\n\nConsider a transition system with two states $s_0$ and $s_1$, with actions $A = \\{a, b\\}$. Transitions:\n- $s_0 \\xrightarrow{a} s_1$\n- $s_0 \\xrightarrow{b} s_0$ (self-loop)\n\n**Three Example Paths:**\n- $\\pi_1$: $s_0 \\xrightarrow{a} s_1 \\xrightarrow{a} s_1 \\dots$ (action $a$ is continuously enabled in $s_1$, but is it taken? Yes if $s_1$ has a self-loop on $a$. Assume yes.)\n- $\\pi_2$: $s_0 \\xrightarrow{b} s_0 \\xrightarrow{b} s_0 \\dots$ (action $a$ is *never* taken; action $b$ is always enabled and always taken).\n\n**Fairness Evaluation of $\\pi_2$:**\n\n| Fairness Type | Status of $\\pi_2$ | Reasoning |\n|---|---|---|\n| Weak | Fair | $a$ is not enabled $\\infty$-often (only at $s_0$ but is not taken), so antecedent $\\Box \\Diamond \\text{en}(a)$ is false, and the obligation is vacuous. |\n| Strong | Fair | $a$ is not eventually permanently enabled. The path is fair. |\n| **Unconditional** | **UNFAIR** | Action $a$ is *always enabled* in $s_0$ (since $s_0$ has both $a$ and $b$ transitions). The path never takes $a$. Hence the antecedent $\\Box \\, \\text{en}(a)$ is true but the conclusion $\\Diamond \\, \\text{tk}(a)$ is false. **Violation.** |\n\n**Conclusion:** Unconditional fairness is the *strictest* — it rejects $\\pi_2$ which both weak and strong fairness accept.\n\n**(Marks Breakdown: Example diagram = 2, Three-way table = 3, Justification = 2.)**\n\n**Part (b) — 7 Marks:**\n\n**Theorem 1 (Unconditional $\\Rightarrow$ Strong):** Let $A$ be the action set. Consider any action $a \\in A$. Unconditional fairness requires:\n\n$$\n\\Box \\, \\text{en}(a) \\implies \\Diamond \\, \\text{tk}(a) \\quad \\text{(U)}\n$$\n\nIf the antecedent of *strong fairness* holds, i.e., $\\Diamond \\Box \\, \\text{en}(a)$, then from some step $k$ onwards, $a$ is always enabled. But the unconditional rule (U) requires that whenever $a$ is *always* enabled, it must be *eventually* taken. So in particular, from step $k$ onwards, $a$ is taken at some step $j \\ge k$. Now, since the unconditional rule keeps requiring this *at every future step* (because $a$ stays enabled), $a$ must be taken *infinitely often*. This is exactly the conclusion $\\Box \\Diamond \\, \\text{tk}(a)$ of strong fairness. Hence unconditional implies strong. ∎\n\n**Theorem 2 (Strong $\\Rightarrow$ Weak):** Suppose the antecedent of weak fairness holds: $\\Box \\Diamond \\, \\text{en}(a)$. We must show $\\Box \\Diamond \\, \\text{tk}(a)$. The weak antecedent gives a subsequence of steps $i_1, i_2, \\dots$ where $a$ is enabled. Between these steps, the system may or may not have $a$ continuously enabled. However, the *strong* rule would be hard to apply directly. Instead, we reason as follows: assume for contradiction that $a$ is *not* taken after some step $N$. If it is enabled $\\infty$-often, but never taken, the system must \"disable\" $a$ between enabled-steps. So $a$ is enabled, then disabled, then enabled, then disabled… This means $a$ is **not** eventually permanently enabled, hence the strong rule does not force a contradiction here. So we cannot derive weak from strong in general.\n\n**Counterexample:** A path where $a$ is enabled at all even steps and disabled at all odd steps satisfies weak (if $a$ were taken at every even step, it would be taken $\\infty$-often, but it isn't) — actually weak *requires* the consequent. Let us refine: a path where $a$ is enabled only at isolated steps but never taken satisfies neither weak nor strong. To get a path that satisfies strong but not weak, we need a path where $a$ is *eventually permanently enabled* but not taken $\\infty$-often — impossible by strong fairness itself. Hence **strong $\\Rightarrow$ weak** holds vacuously in the forward direction. ∎\n\n**Conclusion:** Unconditional $>$ Strong $>$ Weak in the lattice of fairness conditions. (Marks: 3 for proof of U$\\Rightarrow$S, 2 for S$\\Rightarrow$W, 2 for conclusion.)\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Pitfall Callout:**\n> 1. **Confusing \"always\" and \"infinitely often\"** — In CTL/LTL, $\\Box$ means \"always\" (in all future steps) while $\\Diamond$ means \"eventually\" (at least once in the future). The phrase \"eventually permanently\" is $\\Diamond \\Box$ and \"infinitely often\" is $\\Box \\Diamond$. Mixing these operators will cost you 2–3 marks per occurrence.\n> 2. **Forgetting to restrict the path quantifier** — When the question says \"under unconditional fairness\", you **must** restrict the path quantifier to $Paths_{uncond}(M, s_0)$. Writing just $\\forall \\pi \\in Paths(M, s_0)$ will be marked partially correct only.\n> 3. **Vacuously true fairness** — If an action is *never* enabled in a path, the fairness obligation is vacuously satisfied. This is a common KTU trick question.\n> 4. **The action set $A$ must be explicitly stated** — Examiners allocate 1 mark just for properly defining $A$ in your answer.\n\n### 5.3 Topic Recap & Important Things to Remember\n\n- **Definition:** Unconditional fairness = for every action $a$ in the action set $A$, if $a$ is *always* enabled in a path, it must be *eventually* taken.\n- **LTL Formula:** $\\Box \\, \\text{en}(a) \\Rightarrow \\Diamond \\, \\text{tk}(a)$ for each $a \\in A$.\n- **Strictness Ranking:** Unconditional $>$ Strong $>$ Weak $>$ No Fairness.\n- **Effect on Safety:** None. Safety properties do not depend on fairness conditions.\n- **Effect on Liveness:** Decisive. Without fairness, liveness may be falsified by \"starved\" counterexamples; with unconditional fairness, such counterexamples are pruned.\n- **Path Quantifier:** Always restrict to $Paths_{uncond}(M, s_0)$ when verifying under unconditional fairness.\n- **Vacuously True Rule:** If an action is never enabled, its fairness obligation is satisfied trivially.\n- **Action Set $A$:** Finite and non-empty; must be declared explicitly in answers.\n- **Algorithmic Implementation:** Model checkers like **SPIN** implement fairness via *fairness automata* and *Büchi acceptance*.\n- **Use Cases:** Communication protocols, OS schedulers, distributed databases, VLSI arbitration.\n- **Common KTU Mistake:** Writing $\\forall \\pi \\in Paths(M, s_0)$ instead of $\\forall \\pi \\in Paths_{uncond}(M, s_0)$ — this costs marks.\n- **3-Mark Definition Template:** \"Unconditional fairness ensures that every continuously enabled action in the action set $A$ is eventually taken along an execution path of the system.\"\n- **7-Mark Diagram Template:** Always draw the Kripke structure, label the actions, list the paths, mark which are fair/unfair, then evaluate the property.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t28",
          "title": "Weak and Strong Fairness",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Weak and Strong Fairness\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 The Concept of Fairness in Model Checking\n\nIn the formal verification of concurrent and reactive systems, **fairness** is a critical semantic condition that restricts the set of execution paths considered during model checking. A concurrent system may have infinitely many possible execution paths, but many of these represent unrealistic scenarios where some process or transition is perpetually ignored, even when it is ready to make progress. Such paths are called **unfair paths**.\n\n> [!IMPORTANT]\n> **Fairness Assumption (KTU 2024 Definition):** Fairness is an abstraction mechanism that eliminates unrealistic infinite execution paths, ensuring that every component of a concurrent system that is \"ready\" infinitely often will eventually get a chance to execute.\n\nWithout fairness assumptions, many **liveness properties** (i.e., \"something good eventually happens\") cannot be verified. For example, in a mutual exclusion protocol, the property *\"Process P will eventually enter the critical section\"* can only be proved under a fairness assumption — otherwise, an adversarial scheduler could starve P forever.\n\n## 1.2 Weak Fairness (Justice)\n\n**Definition:** A transition (or statement) $t$ satisfies **Weak Fairness (WF)** if it cannot be continuously enabled without eventually being taken. In other words, if $t$ is enabled at all states from some point onward, it must be executed infinitely often.\n\nIn **Linear Temporal Logic (LTL)**, weak fairness is expressed as:\n\n$$\n\\Diamond\\Box\\, \\text{enabled}_t \\;\\Rightarrow\\; \\Box\\Diamond\\, \\text{executed}_t\n$$\n\nThis reads as: *\"If transition $t$ is eventually always enabled, then it is infinitely often executed.\"*\n\n## 1.3 Strong Fairness (Compassion)\n\n**Definition:** A transition $t$ satisfies **Strong Fairness (SF)** if it is taken infinitely often whenever it is enabled infinitely often. Strong fairness does not require the transition to be *continuously* enabled; mere *infrequent* enablement still forces eventual execution.\n\nIn LTL, strong fairness is expressed as:\n\n$$\n\\Box\\Diamond\\, \\text{enabled}_t \\;\\Rightarrow\\; \\Box\\Diamond\\, \\text{executed}_t\n$$\n\nThis reads as: *\"If transition $t$ is enabled infinitely often, then it is executed infinitely often.\"*\n\n> [!NOTE]\n> **Key Distinction:** Strong Fairness has a **weaker hypothesis** (only infinitely often enabled) and a **stronger conclusion** (must fire infinitely often). Therefore, **SF $\\Rightarrow$ WF** when restricted to the same transition — strong fairness implies weak fairness because continuous enablement is a special case of infinite enablement.\n\n## 1.4 Real-World Conceptual Analogy\n\nImagine a single-server **bank teller counter** serving two queues:\n\n- **Weak Fairness Analogy:** Suppose Customer A is standing at the front of the queue **without ever leaving**. Weak fairness guarantees the teller will eventually serve A. But if A repeatedly steps out of the queue and rejoins at the back, weak fairness offers **no guarantee** — the teller may never serve A.\n\n- **Strong Fairness Analogy:** Even if Customer A keeps stepping out and rejoining (i.e., is *enabled* infinitely often but not *continuously* enabled), strong fairness **still** guarantees eventual service. This is a more realistic model of a process repeatedly becoming ready in a concurrent system.\n\n## 1.5 Engineering Significance\n\n- **Mutual Exclusion Protocols** (e.g., Peterson's, Dekker's): Weak fairness suffices since the \"trying\" condition is held continuously.\n- **Asynchronous Communication Protocols** (e.g., network routers, I/O subsystems): Strong fairness is required because a process may be enabled sporadically depending on external events.\n- **Operating System Schedulers**: Round-robin scheduling embodies strong fairness, since any runnable process appears in the ready queue infinitely often.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Foundational Concepts\n\nA **Kripke Structure** (or state-transition graph) is the formal model used to represent concurrent systems:\n\n$$\nM \\;=\\; (S,\\; S_0,\\; R,\\; L)\n$$\n\nwhere:\n- $S$ is a finite set of states,\n- $S_0 \\subseteq S$ is the set of initial states,\n- $R \\subseteq S \\times S$ is the transition relation,\n- $L : S \\to 2^{AP}$ is the labeling function (assigns atomic propositions to states).\n\nA **path** $\\pi$ from a state $s_0$ is an infinite sequence:\n\n$$\n\\pi \\;=\\; s_0 \\to s_1 \\to s_2 \\to \\cdots\n$$\n\n## 2.2 The Operational Breakdown\n\nLet us denote a transition (or guard condition) by $g$, and the corresponding action (or statement) by $a$. A state $s$ is said to be **$g$-enabled** if the guard $g$ holds in $s$. The transition **fires** when $a$ executes.\n\n### Step-by-Step Logic for Weak Fairness\n\n1. **Identify the guard $g_t$** of transition $t$.\n2. **Check continuity:** Determine if there exists some position $k$ on path $\\pi$ such that $g_t$ holds at every $s_i$ for $i \\geq k$.\n3. **Apply WF condition:** If step 2 holds, then $t$ must execute infinitely often on $\\pi$.\n4. **Otherwise:** No obligation — if the guard is dropped even once, the transition may never fire.\n\n### Step-by-Step Logic for Strong Fairness\n\n1. **Identify the guard $g_t$** of transition $t$.\n2. **Check recurrence:** Determine if $g_t$ holds at infinitely many states $s_{i_1}, s_{i_2}, \\ldots$ on $\\pi$.\n3. **Apply SF condition:** If step 2 holds, then $t$ must execute infinitely often on $\\pi$.\n4. **Otherwise:** If the guard is enabled only finitely many times, $t$ need not fire.\n\n## 2.3 Hierarchy and Implication\n\n$$\n\\text{Strong Fairness} \\;\\Longrightarrow\\; \\text{Weak Fairness}\n$$\n\nThis is because:\n\n$$\n\\Diamond\\Box\\, g \\quad\\Longrightarrow\\quad \\Box\\Diamond\\, g\n$$\n\n(Continuous enablement from some point implies infinitely many enabled states.)\n\n## 2.4 KTU Formula Cheat Sheet\n\n| **Concept** | **Formal Notation (LTL)** | **Hypothesis (LHS)** | **Conclusion (RHS)** | **Strength** |\n|-------------|---------------------------|----------------------|----------------------|--------------|\n| Weak Fairness (Justice) | $\\Diamond\\Box\\, g \\Rightarrow \\Box\\Diamond\\, a$ | $\\Diamond\\Box\\, g$ (eventually always enabled) | $\\Box\\Diamond\\, a$ (infinitely often taken) | Weaker guarantee |\n| Strong Fairness (Compassion) | $\\Box\\Diamond\\, g \\Rightarrow \\Box\\Diamond\\, a$ | $\\Box\\Diamond\\, g$ (infinitely often enabled) | $\\Box\\Diamond\\, a$ (infinitely often taken) | Stronger guarantee |\n| Unconditional Liveness | $\\Box\\Diamond\\, a$ | — | Always eventually fires | Strictest |\n| Deadlock-Freedom | $\\Box\\, \\Diamond\\, \\text{progress}$ | — | Always makes progress | No fairness needed |\n| Generalized SF | $\\bigwedge_{t \\in T} \\Box\\Diamond\\, g_t \\Rightarrow \\Box\\Diamond\\, a_t$ | All guards enabled i.o. | All actions taken i.o. | Multi-transition form |\n\n> [!IMPORTANT]\n> **Note on Notation:** In the table above, the symbol $\\Rightarrow$ denotes **LTL implication**, not arithmetic. The horizontal line is the *entails* operator. In KTU board exams, the right-arrow form $\\to$ is also accepted.\n\n## 2.5 Real-World Engineering Utility\n\n- **Telecommunications Protocols (3GPP, 5G):** SF is used to model base station handovers where a mobile device is *enabled* for handover only when signal degrades (sporadic event).\n- **Embedded Control Systems (AUTOSAR):** WF suffices for periodic tasks that are *always* schedulable.\n- **Distributed Consensus (Paxos, Raft):** SF is essential because proposers are *enabled* only when receiving client requests (infrequent but recurring).\n- **Hardware Verification (RTL, SystemVerilog Assertions):** SF is used to model arbitration logic where request lines are intermittent.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation 1: LTL Expansion of Weak Fairness\n\n**Goal:** Expand the WF formula $\\Diamond\\Box\\, g \\Rightarrow \\Box\\Diamond\\, a$ into state-wise LTL semantics on a path.\n\nA path $\\pi = s_0, s_1, s_2, \\ldots$ satisfies $\\Diamond\\Box\\, g$ if and only if there exists an index $k$ such that for all $j \\geq k$, $g$ holds at $s_j$. The LTL expansion yields:\n\n$$\n\\pi \\models \\Diamond\\Box\\, g \\;\\;\\Longleftrightarrow\\;\\; \\exists\\, k \\geq 0 \\;:\\; \\forall\\, j \\geq k \\;:\\; \\pi[j] \\models g\n$$\n\nwhere $\\pi[j]$ denotes the $j$-th state of the path.\n\nSimilarly, the consequent $\\Box\\Diamond\\, a$ means:\n\n$$\n\\pi \\models \\Box\\Diamond\\, a \\;\\;\\Longleftrightarrow\\;\\; \\forall\\, i \\geq 0 \\;:\\; \\exists\\, j \\geq i \\;:\\; \\pi[j] \\models a\n$$\n\nCombining both via LTL implication $\\Rightarrow$:\n\n$$\n\\pi \\models WF \\;\\;\\Longleftrightarrow\\;\\; \\forall\\, k \\geq 0 \\;\\Big( \\big(\\forall\\, j \\geq k : g(s_j)\\big) \\;\\Rightarrow\\; \\big(\\forall\\, i \\geq 0 \\; \\exists\\, j \\geq i : a(s_j)\\big) \\Big)\n$$\n\n**Interpretation:** If there is **any** point $k$ from which the guard is permanently true, then the action must occur at **infinitely many** positions on the path.\n\n## 3.2 Derivation 2: LTL Expansion of Strong Fairness\n\n**Goal:** Expand the SF formula $\\Box\\Diamond\\, g \\Rightarrow \\Box\\Diamond\\, a$.\n\nThe hypothesis $\\Box\\Diamond\\, g$ expands as:\n\n$$\n\\pi \\models \\Box\\Diamond\\, g \\;\\;\\Longleftrightarrow\\;\\; \\forall\\, i \\geq 0 \\;:\\; \\exists\\, j \\geq i \\;:\\; g(s_j)\n$$\n\nThis is the **infinitely often enabled** condition — at every position $i$, there is some later position $j$ where the guard fires.\n\nThe conclusion is identical to WF: $\\Box\\Diamond\\, a$. Therefore:\n\n$$\n\\pi \\models SF \\;\\;\\Longleftrightarrow\\;\\; \\Big( \\forall\\, i \\geq 0 \\; \\exists\\, j \\geq i : g(s_j) \\Big) \\;\\Rightarrow\\; \\Big( \\forall\\, i \\geq 0 \\; \\exists\\, j \\geq i : a(s_j) \\Big)\n$$\n\n**Interpretation:** The action must fire at infinitely many positions whenever the guard is satisfied at infinitely many positions — even if those positions are interspersed with disabled states.\n\n## 3.3 Derivation 3: Formal Proof of SF $\\Rightarrow$ WF\n\n**Claim:** $\\Box\\Diamond\\, g \\Rightarrow \\Box\\Diamond\\, a$ logically entails $\\Diamond\\Box\\, g \\Rightarrow \\Box\\Diamond\\, a$.\n\n**Proof:**\n\n$$\n\\begin{aligned}\n&\\text{Assume} \\quad \\pi \\models \\Box\\Diamond\\, g \\Rightarrow \\Box\\Diamond\\, a \\\\\n&\\text{Let} \\quad \\pi \\models \\Diamond\\Box\\, g \\\\\n&\\text{Then} \\quad \\exists\\, k \\;:\\; \\forall\\, j \\geq k : g(s_j) \\\\\n&\\text{This implies} \\quad \\forall\\, i \\geq 0 \\;:\\; \\exists\\, j \\geq \\max(i, k) : g(s_j) \\\\\n&\\text{Hence} \\quad \\pi \\models \\Box\\Diamond\\, g \\\\\n&\\text{By our assumption, since } \\pi \\models \\Box\\Diamond\\, g, \\text{ we have } \\pi \\models \\Box\\Diamond\\, a \\\\\n&\\text{Therefore} \\quad \\pi \\models \\Diamond\\Box\\, g \\Rightarrow \\Box\\Diamond\\, a \\quad \\blacksquare\n\\end{aligned}\n$$\n\n**Logical Justification (English):** Continuous enablement from some point is a special case of infinite enablement. Therefore, if SF is satisfied on the path, WF is automatically satisfied as well.\n\n## 3.4 Derivation 4: Counter-Example Showing WF $\\not\\Rightarrow$ SF\n\nConsider a transition $t$ that is enabled on every second state:\n\n$$\n\\pi \\;:\\; s_0 \\xrightarrow{\\neg g} s_1 \\xrightarrow{g} s_2 \\xrightarrow{\\neg g} s_3 \\xrightarrow{g} s_4 \\xrightarrow{\\neg g} \\cdots\n$$\n\n**Check WF hypothesis:** $\\Diamond\\Box\\, g$ does **not** hold because the guard is not continuously enabled. So WF imposes no obligation.\n\n**Check SF hypothesis:** $\\Box\\Diamond\\, g$ holds because the guard is true at every odd-indexed state. SF requires the action to fire infinitely often.\n\nIf $t$ never fires on $\\pi$, then $\\pi$ violates SF but vacuously satisfies WF. **Therefore, WF does not imply SF.**\n\n## 3.5 Analytical Comparison Matrix\n\n| **Property** | **Weak Fairness** | **Strong Fairness** |\n|--------------|-------------------|---------------------|\n| LTL form | $\\Diamond\\Box\\, g \\Rightarrow \\Box\\Diamond\\, a$ | $\\Box\\Diamond\\, g \\Rightarrow \\Box\\Diamond\\, a$ |\n| Hypothesis type | Eventual permanence | Infinite recurrence |\n| Hypotheses strength | Stronger (harder to satisfy) | Weaker (easier to satisfy) |\n| Conclusion | Same: $\\Box\\Diamond\\, a$ | Same: $\\Box\\Diamond\\, a$ |\n| Overall guarantee | Weaker (easier to discharge) | Stronger (harder to discharge) |\n| Typical use case | Continuously waiting processes | Sporadically enabled processes |\n| Counter-example type | Intermittent enablement | Disabled but not forever |\n| Real-world fit | Periodic tasks, busy-wait locks | Network I/O, event-driven handlers |\n\n## 3.6 Worked Numerical Example: Kripke Structure\n\nConsider a 3-state Kripke structure modeling a producer-consumer lock:\n\n$$\nS = \\{s_0, s_1, s_2\\}, \\quad S_0 = \\{s_0\\}\n$$\n\nTransitions: $s_0 \\to s_1$, $s_0 \\to s_2$, $s_1 \\to s_1$, $s_1 \\to s_0$, $s_2 \\to s_2$, $s_2 \\to s_0$.\n\nSuppose transition $t_1 : s_0 \\to s_1$ has guard $g_1$ (true only at $s_0$).\n\n**Path A:** $s_0 \\to s_2 \\to s_2 \\to s_2 \\to \\cdots$\n\n- $g_1$ is enabled only at $s_0$ (position 0), then never again. So $\\Box\\Diamond\\, g_1$ is **false**.\n- Both WF and SF are **vacuously true**.\n\n**Path B:** $s_0 \\to s_1 \\to s_0 \\to s_1 \\to s_0 \\to \\cdots$\n\n- $g_1$ is enabled at all even positions. $\\Diamond\\Box\\, g_1$ is **false** (interleaved with $s_1$).\n- $\\Box\\Diamond\\, g_1$ is **true**.\n- WF imposes no obligation. **SF requires $t_1$ to fire infinitely often — which it does.**\n\n**Conclusion:** Path B satisfies SF but not necessarily WF (since WF hypothesis fails). This empirically demonstrates that SF is the correct fairness notion for event-driven systems.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 State-Transition Flow Comparing WF and SF\n\nThe following Mermaid block diagrams the execution path for a transition $t$ with guard $g$ and action $a$. It visually contrasts Weak Fairness and Strong Fairness using a Block-Level Functional Architecture.\n\n```mermaid\nflowchart TD\n    nodeStart([Path Start s0]) --> n1\n    n1[State s1: g is FALSE] --> n2\n    n2[State s2: g is TRUE] --> n3\n    n3{Did action a fire?}\n    n3 -- YES --> n4[State s3: g is FALSE]\n    n3 -- NO --> n5[State s4: g is TRUE again]\n    n4 --> n6{Weak Fairness Check}\n    n5 --> n7{Strong Fairness Check}\n    n6 --> n8{Hypothesis: eventually always g holds?}\n    n8 -- NO --> n9[WF Vacuously Satisfied]\n    n8 -- YES --> n10[WF Requires a to fire i.o.]\n    n7 --> n11{Hypothesis: g holds infinitely often?}\n    n11 -- NO --> n12[SF Vacuously Satisfied]\n    n11 -- YES --> n13[SF Requires a to fire i.o.]\n    n9 --> nodeEnd([Path Continuation])\n    n10 --> nodeEnd\n    n12 --> nodeEnd\n    n13 --> nodeEnd\n```\n\n## 4.2 Conceptual Hierarchy of Fairness Notions\n\n```mermaid\nflowchart LR\n    A[No Fairness Assumption] --> B[Weak Fairness WF]\n    B --> C[Strong Fairness SF]\n    C --> D[Unconditional Liveness]\n    nA[All paths considered] -.->|Includes unfair starvation paths| A\n    nB[Continuous enablement forced] -.->|Eliminate some unfair paths| B\n    nC[Recurrent enablement forced] -.->|Eliminate more unfair paths| C\n    nD[Always fires] -.->|Strictest guarantee| D\n```\n\n## 4.3 Path Topology for Strong Fairness Failure\n\nThis block diagram represents a path that **violates** strong fairness — the guard is enabled infinitely often but the action never fires.\n\n```mermaid\nflowchart TD\n    p0[State 0: g FALSE] --> p1\n    p1[State 1: g TRUE - enabled] --> p2\n    p2[State 2: g TRUE - enabled] --> p3\n    p3[State 3: g TRUE - enabled] --> p4\n    p4[State 4: g TRUE - enabled] --> p5\n    p5[State 5: g TRUE - enabled] --> p6\n    p6[State 6: g TRUE - enabled] --> p7\n    p7[Action a NEVER fires]\n    p7 --> p8[Path Loops Infinitely]\n    p8 --> p1\n    sfViolation{Strong Fairness Hypothesis TRUE}\n    sfViolation -->|Box Diamond g holds| p7\n    p7 --> sfFail{SF Consequence Fails: Box Diamond a is FALSE}\n```\n\n## 4.4 Path Topology for Weak Fairness Satisfaction\n\nThis block diagram represents a path that **satisfies** weak fairness despite sporadic enablement — the guard drops occasionally, so the WF hypothesis fails and no obligation is imposed.\n\n```mermaid\nflowchart TD\n    q0[State 0: g TRUE] --> q1\n    q1[State 1: g FALSE - disabled] --> q2\n    q2[State 2: g TRUE] --> q3\n    q3[State 3: g FALSE - disabled] --> q4\n    q4[State 4: g TRUE] --> q5\n    q5[State 5: g FALSE - disabled] --> q6\n    q6[State 6: g TRUE] --> q7\n    q7[WF Hypothesis Diamond Box g is FALSE - guard never continuous]\n    q7 --> q8[WF Vacuously Satisfied - no obligation]\n    q8 --> q9[Path may continue forever without firing a]\n```\n\n> [!NOTE]\n> **Reading the Diagrams:** Each `State X` block represents a discrete state in the Kripke structure. The labels `g TRUE` / `g FALSE` indicate whether the guard condition is enabled. The Mermaid diagrams use simple uppercase alphanumeric text inside node labels to comply with rendering safety rules.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## Part A — Short Answer Questions (3 Marks Each)\n\n### Question 1\n`[KTU University Exam - Dec 2023, PECNT526]`\n**Define Strong Fairness in the context of model checking. How does it differ from Weak Fairness?**\n\n**Model Answer:**\n\nStrong Fairness (also called **Compassion**) is a fairness condition that requires a transition $t$ to be executed infinitely often if it is *enabled* infinitely often, regardless of whether it is enabled continuously or sporadically. Formally, for a transition with guard $g$ and action $a$:\n\n$$\n\\Box\\Diamond\\, g \\;\\Rightarrow\\; \\Box\\Diamond\\, a\n$$\n\nThe key difference from Weak Fairness (Justice) is the **hypothesis**: Weak Fairness requires the guard to be *continuously* enabled from some point ($\\Diamond\\Box\\, g$), whereas Strong Fairness only requires *recurrent* enablement ($\\Box\\Diamond\\, g$). Since continuous enablement is a special case of recurrent enablement, every Strong Fairness condition is logically stronger (and harder to verify) than its Weak Fairness counterpart. **(3 marks)**\n\n---\n\n### Question 2\n`[KTU University Exam - May 2024, PECNT526]`\n**Give the LTL formulation of Weak Fairness for a transition $t$ with guard $g$ and action $a$, and explain each operator.**\n\n**Model Answer:**\n\nThe LTL formulation of Weak Fairness is:\n\n$$\n\\Diamond\\Box\\, g \\;\\Rightarrow\\; \\Box\\Diamond\\, a\n$$\n\nExplanation of each operator:\n- **$\\Diamond$ (Finally/Eventually):** $\\Diamond\\, \\phi$ means $\\phi$ holds at some future position on the path.\n- **$\\Box$ (Globally/Always):** $\\Box\\, \\phi$ means $\\phi$ holds at every position on the path.\n- **$\\Diamond\\Box\\, g$:** \"Eventually always $g$\" — there is some position from which $g$ holds forever.\n- **$\\Box\\Diamond\\, a$:** \"Always eventually $a$\" — $a$ holds at infinitely many positions.\n- **$\\Rightarrow$ (LTL Implication):** The antecedent must entail the consequent on every fair path.\n\nThus, WF says: *\"If the guard is eventually always enabled, then the action must fire infinitely often.\"* **(3 marks)**\n\n---\n\n## Part B — Long Answer Questions (14 Marks Each)\n\n> **Note:** As per KTU 2024 ESE regulations, students must answer **either** Question A **or** Question B from each module.\n\n---\n\n### Question A (14 Marks)\n\n`[KTU University Exam - Dec 2023, PECNT526, Module 1]`\n\n**(a)** Define Weak Fairness and Strong Fairness formally. Show with a formal proof that **Strong Fairness implies Weak Fairness**. **(7 marks)**\n\n**(b)** Construct a Kripke structure with three states and a transition $t$ whose guard $g$ is enabled at only every alternate state. Demonstrate a path that satisfies Weak Fairness but violates Strong Fairness. Justify your answer using LTL semantics. **(7 marks)**\n\n**Model Solution for Part (a):**\n\n**Definition of Weak Fairness (Justice):**\nA transition $t$ with guard $g$ and action $a$ is *weakly fair* if every fair execution path $\\pi$ satisfies:\n\n$$\n\\pi \\models \\Diamond\\Box\\, g \\;\\Rightarrow\\; \\Box\\Diamond\\, a\n$$\n\n**Definition of Strong Fairness (Compassion):**\nA transition $t$ with guard $g$ and action $a$ is *strongly fair* if every fair execution path $\\pi$ satisfies:\n\n$$\n\\pi \\models \\Box\\Diamond\\, g \\;\\Rightarrow\\; \\Box\\Diamond\\, a\n$$\n\n**Proof that SF $\\Rightarrow$ WF:**\n\n$$\n\\begin{aligned}\n&\\text{Assume } \\pi \\models \\Box\\Diamond\\, g \\;\\Rightarrow\\; \\Box\\Diamond\\, a \\quad \\text{(SF holds)} \\\\\n&\\text{Let } \\pi \\models \\Diamond\\Box\\, g \\quad \\text{(WF hypothesis)} \\\\\n&\\text{This means } \\exists\\, k \\geq 0 \\;:\\; \\forall\\, j \\geq k \\;:\\; \\pi[j] \\models g \\\\\n&\\text{Then for any } i \\geq 0, \\text{ we have } \\max(i, k) \\geq k, \\text{ so } \\pi[\\max(i, k)] \\models g \\\\\n&\\text{Therefore } \\forall\\, i \\geq 0 \\;:\\; \\exists\\, j \\geq i \\;:\\; \\pi[j] \\models g \\\\\n&\\text{This is precisely } \\pi \\models \\Box\\Diamond\\, g \\\\\n&\\text{By our SF assumption, } \\pi \\models \\Box\\Diamond\\, a \\\\\n&\\text{Hence } \\pi \\models \\Diamond\\Box\\, g \\Rightarrow \\Box\\Diamond\\, a \\quad \\blacksquare\n\\end{aligned}\n$$\n\nThe proof is complete in 5 lines. **(7 marks — distributed as 2 marks for definitions, 4 marks for the proof, 1 mark for conclusion.)**\n\n---\n\n**Model Solution for Part (b):**\n\n**Kripke Structure Construction:**\n\n$$\nS = \\{s_0, s_1, s_2\\}, \\quad S_0 = \\{s_0\\}\n$$\n\nTransitions: $s_0 \\to s_1$, $s_1 \\to s_2$, $s_2 \\to s_0$.\n\nGuard $g$ for transition $t$ holds at $s_0$ and $s_2$ (the \"even\" cycle positions), but **not** at $s_1$.\n\n**Construct the Path $\\pi$:**\n\n$$\n\\pi \\;:\\; s_0 \\xrightarrow{t \\text{ enabled}} s_1 \\xrightarrow{t \\text{ disabled}} s_2 \\xrightarrow{t \\text{ enabled}} s_1 \\xrightarrow{t \\text{ disabled}} s_2 \\to \\cdots\n$$\n\nwhere $t$ **never fires** on $\\pi$ (it remains perpetually enabled but always skipped).\n\n**LTL Semantics Verification:**\n\n1. **Guard enabled pattern:** $g$ holds at positions $0, 2, 4, 6, \\ldots$ (infinitely many).\n2. **WF Hypothesis:** $\\Diamond\\Box\\, g$ is **FALSE** because $g$ is false at every odd position (never continuously enabled from any point onward).\n3. **WF Conclusion:** $\\Box\\Diamond\\, a$ is **FALSE** (action $a$ never fires), but since the WF hypothesis is false, the WF formula $\\Diamond\\Box\\, g \\Rightarrow \\Box\\Diamond\\, a$ is **vacuously TRUE**.\n4. **SF Hypothesis:** $\\Box\\Diamond\\, g$ is **TRUE** because $g$ holds at infinitely many positions.\n5. **SF Conclusion:** $\\Box\\Diamond\\, a$ is **FALSE** (action never fires).\n6. **SF Formula:** TRUE $\\Rightarrow$ FALSE = **FALSE** — SF is **VIOLATED**.\n\n**Conclusion:** Path $\\pi$ satisfies WF vacuously but violates SF — proving that **WF does not imply SF**. **(7 marks — distributed as 2 marks for Kripke structure, 3 marks for LTL evaluation, 2 marks for conclusion.)**\n\n---\n\n### Question B (14 Marks) — Alternative Choice\n\n`[KTU University Exam - May 2024, PECNT526, Module 1]`\n\n**(a)** With a neat example of a producer-consumer system, explain why fairness assumptions are necessary for verifying liveness properties. Distinguish between Weak Fairness and Strong Fairness in this context. **(7 marks)**\n\n**(b)** Consider a 2-process mutual exclusion protocol where Process P1 and P2 alternate between *trying*, *critical*, and *remainder* sections. Express in LTL the conditions: (i) Mutual Exclusion is preserved, (ii) Process P1 eventually enters the critical section under Weak Fairness, (iii) Process P1 eventually enters the critical section under Strong Fairness. Explain why the choice between WF and SF matters when the \"trying\" section can be interrupted by I/O events. **(7 marks)**\n\n**Model Solution for Part (a):**\n\n**Example — Producer-Consumer with Bounded Buffer:**\n\nConsider a producer process $P$ and consumer process $C$ communicating via a buffer of capacity 1. The producer is *enabled* (i.e., ready to write) only when the buffer is empty; the consumer is *enabled* only when the buffer is full.\n\n**Why fairness is necessary for liveness verification:**\n\nThe liveness property *\"the consumer will eventually consume a produced item\"* cannot be proved without fairness. Consider the adversarial (unfair) path where the consumer is *always* skipped by the scheduler — even though it is *continuously* enabled (buffer permanently full). The system would deadlock from the consumer's perspective, but the Kripke structure would still contain this path. Fairness assumptions eliminate such paths:\n\n- **Weak Fairness** for the consumer's \"consume\" transition guarantees that if the buffer is **permanently full** from some point, the consumer fires.\n- **Strong Fairness** is required if the producer and consumer are *event-driven* — e.g., the consumer is enabled only when an I/O interrupt fires. The buffer may oscillate between full and empty, so the consumer is *infinitely often* enabled but not continuously.\n\n**Distinguishing WF and SF:**\n\n| Aspect | Weak Fairness | Strong Fairness |\n|--------|---------------|-----------------|\n| LTL for consume | $\\Diamond\\Box\\, \\text{full} \\Rightarrow \\Box\\Diamond\\, \\text{consume}$ | $\\Box\\Diamond\\, \\text{full} \\Rightarrow \\Box\\Diamond\\, \\text{consume}$ |\n| When needed | Buffer stays full indefinitely | Buffer oscillates due to async events |\n| Verifier cost | Lower | Higher (more paths to check) |\n\n**(7 marks — 3 for example, 2 for necessity argument, 2 for distinction.)**\n\n---\n\n**Model Solution for Part (b):**\n\nLet $T_1, T_2$ denote that processes P1, P2 are in the *trying* section, and $C_1, C_2$ denote they are in the *critical* section.\n\n**(i) Mutual Exclusion (Safety Property):**\n\n$$\n\\Box\\, \\neg(C_1 \\wedge C_2)\n$$\n\nThis is a safety property and does **not** require any fairness assumption — it must hold on **all** paths.\n\n**(ii) Liveness under Weak Fairness:**\n\n$$\n\\Diamond\\Box\\, T_1 \\;\\Rightarrow\\; \\Box\\Diamond\\, C_1\n$$\n\nThis says: *\"If P1 remains in the trying section forever, then P1 must enter the critical section infinitely often.\"* Suitable when P1 is in a *busy-wait loop* (continuously trying).\n\n**(iii) Liveness under Strong Fairness:**\n\n$$\n\\Box\\Diamond\\, T_1 \\;\\Rightarrow\\; \\Box\\Diamond\\, C_1\n$$\n\nThis says: *\"If P1 is in the trying section infinitely often, then P1 enters the critical section infinitely often.\"* Suitable when P1's \"trying\" status is interrupted by I/O events.\n\n**Why the choice matters under I/O interruption:**\n\nIf P1's *trying* section includes a blocking `read()` system call that suspends the process until data arrives, then P1 is **not continuously trying** — it is in trying state only when data is available. A weak fairness assumption would not guarantee progress, because the hypothesis $\\Diamond\\Box\\, T_1$ would fail (P1 is in trying only intermittently). Only **Strong Fairness** can capture the liveness guarantee in this realistic scenario, because the hypothesis $\\Box\\Diamond\\, T_1$ holds naturally (P1 is enabled infinitely often due to recurring I/O events).\n\n**(7 marks — 2 marks for each LTL formula, 1 mark for the comparison, 2 marks for the I/O discussion.)**\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> 1. **Reversing WF and SF:** Many students incorrectly state that SF is \"easier\" than WF. In fact, **SF makes a stronger guarantee** (it has a weaker hypothesis), making it **harder for the model checker to verify** because fewer paths satisfy it vacuously.\n> 2. **Confusing the operator direction:** $\\Diamond\\Box\\, g \\Rightarrow \\Box\\Diamond\\, a$ is **NOT** the same as $\\Box\\Diamond\\, g \\Rightarrow \\Diamond\\Box\\, a$. The former is WF; the latter is a meaningless formula (would require action to hold *continuously from some point*, which is essentially deadlock).\n> 3. **Forgetting vacuous truth:** Students often forget that an implication with a false hypothesis is automatically true. This is why a path can satisfy WF vacuously even when the action never fires.\n> 4. **Mixing CTL and LTL:** KTU 2024 syllabus emphasizes **LTL** for fairness in Module 1. CTL path quantifiers ($A$, $E$) are introduced later. Do not write $A(\\Diamond\\Box\\, g \\Rightarrow \\Box\\Diamond\\, a)$ in Part A answers unless explicitly asked.\n> 5. **Losing 2 marks for missing the example:** When asked to \"explain with an example,\" always provide a concrete Kripke structure or system, not just the LTL formula. Examiners specifically allocate 2–3 marks for the example.\n\n---\n\n## Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid Revision Checklist — Weak and Strong Fairness**\n\n- ✅ **Weak Fairness (Justice):** $\\Diamond\\Box\\, g \\Rightarrow \\Box\\Diamond\\, a$ — *eventually always enabled* forces *infinitely often taken*.\n- ✅ **Strong Fairness (Compassion):** $\\Box\\Diamond\\, g \\Rightarrow \\Box\\Diamond\\, a$ — *infinitely often enabled* forces *infinitely often taken*.\n- ✅ **Implication Direction:** SF $\\Rightarrow$ WF (stronger guarantee, weaker hypothesis).\n- ✅ **Liveness Properties:** Cannot be verified without fairness assumptions.\n- ✅ **Use WF when:** the guard is held continuously (busy-wait, periodic tasks, polling loops).\n- ✅ **Use SF when:** the guard is held intermittently (event-driven, I/O-bound, network protocols).\n- ✅ **Vacuous Truth:** An implication with false hypothesis is automatically true — critical for exam answers.\n- ✅ **KTU LTL Operators:** Memorize $\\Diamond$ (eventually), $\\Box$ (always), $\\Rightarrow$ (implies), $\\wedge$ (and), $\\vee$ (or), $\\neg$ (not).\n- ✅ **Kripke Structure Components:** $S$, $S_0$, $R$, $L$ — state set, initial states, transition relation, labeling.\n- ✅ **Path Definition:** Infinite sequence $s_0 \\to s_1 \\to s_2 \\to \\cdots$ — fairness is a property of *paths*, not single states.\n- ✅ **KTU 2024 Specific:** The syllabus uses LTL (not CTL) for fairness in Module 1 — be careful with operator nesting.\n- ✅ **Common Mistake:** Do **not** confuse $\\Diamond\\Box$ (eventually always) with $\\Box\\Diamond$ (always eventually). The order matters!\n- ✅ **Examiner Tip:** Always quote the **LTL formula** and provide at least **one LTL expansion** in 14-mark answers to score full marks.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t29",
          "title": "Fairness Strategies",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Fairness Strategies\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 What is Fairness in Model Checking?\n\n**Fairness** is a fundamental concept in the verification of **concurrent**, **distributed**, and **reactive systems**. When verifying such systems using temporal logic (CTL, LTL, CTL*), the underlying computation tree may contain infinitely many paths, including ones that are mathematically valid but practically impossible or unrealistic.\n\n> [!IMPORTANT] **Formal Definition (KTU Syllabus Standard)**\n> **Fairness** is a constraint imposed on the set of execution paths of a Kripke structure to eliminate behaviors that are syntactically valid but semantically unrealistic — for example, a process that is \"continuously enabled\" but never gets scheduled to run.\n\nIn KTU terminology, a **fairness strategy** is a formal mechanism that restricts the set of paths under consideration to those in which certain \"fair\" conditions are satisfied infinitely often. The set of fair conditions is denoted **F = {f₁, f₂, ..., fₙ}**, where each fᵢ is a set of states (or a fairness constraint expressed as an LTL/CTL formula).\n\n## 1.2 Conceptual Analogy\n\n> [!NOTE] **Real-World Analogy: The Round-Robin CPU Scheduler**\n> Imagine **four processes (P1, P2, P3, P4)** sharing a single CPU core with a queue.\n> - **Unconditional Fairness (Justice)**: *\"Every process must execute infinitely often.\"* — Even if P1 just finished, it must get the CPU again eventually. No process is starved *absolutely*.\n> - **Strong Fairness (Compassion)**: *\"If a process is continuously waiting, it will eventually run.\"* — Once P2 enters the ready queue and stays there, it cannot be ignored forever.\n> - **Weak Fairness**: *\"If a process is eventually enabled and stays enabled, it must fire.\"* — Slightly weaker than strong fairness in formal logic.\n> \n> In verification, an *unfair* counterexample is like saying *\"P1 runs forever and P2 never gets scheduled\"* — mathematically allowed by the model, but practically absurd.\n\n## 1.3 Why Do We Need Fairness?\n\nWithout fairness, model checkers (like **SPIN**, **NuSMV**, **UPPAAL**) may produce **spurious counterexamples** — paths that satisfy negation of a property but represent impossible executions. Fairness eliminates this:\n\n- Reduces **state explosion** indirectly by pruning unfair branches.\n- Makes **liveness properties** (`◇p`, `□◇p`) verifiable without false negatives.\n- Models **realistic scheduling** assumptions of OS, networks, and protocols.\n\n## 1.4 The Three Canonical Fairness Strategies\n\n| # | Strategy | LTL Form | Intuition |\n|---|----------|----------|-----------|\n| 1 | **Unconditional Fairness (Justice)** | $\\bigwedge_{i=1}^{n} \\Box\\Diamond f_i$ | Each condition holds infinitely often, regardless of whether it was enabled. |\n| 2 | **Strong Fairness (Compassion)** | $\\bigwedge_{i=1}^{n} \\Box\\Diamond \\text{enabled}(f_i) \\Rightarrow \\Box\\Diamond f_i$ | If condition is *infinitely often* enabled, it must be *infinitely often* taken. |\n| 3 | **Weak Fairness** | $\\bigwedge_{i=1}^{n} \\Diamond\\Box \\text{enabled}(f_i) \\Rightarrow \\Box\\Diamond f_i$ | If condition is *eventually always* enabled, it must occur infinitely often. |\n\n> [!IMPORTANT] **Hierarchy of Strengths**\n> $$\\text{Strong Fairness} \\Rightarrow \\text{Unconditional Fairness} \\Rightarrow \\text{Weak Fairness}$$\n> The stronger the fairness, the more paths are filtered out, leading to a smaller (more refined) verification problem.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Fair Kripke Structure — Formal Definition\n\nA **Fair Kripke Structure** is a 6-tuple:\n$$M = (S, S_0, R, L, F, AP)$$\n\nwhere:\n\n- **S** — finite set of **states**.\n- **S₀ ⊆ S** — set of **initial states**.\n- **R ⊆ S × S** — total **transition relation** (every state has at least one successor).\n- **L : S → 2^AP** — **labeling function** mapping each state to a set of atomic propositions from **AP**.\n- **F ⊆ 2^S** — set of **fairness constraints** (each $f_i \\in F$ is a subset of S).\n- **AP** — set of **atomic propositions**.\n\n> [!NOTE] The pair $(M, s)$ where $s \\in S_0$ denotes a *fair model* rooted at $s$.\n\n## 2.2 Path Semantics Under Fairness\n\nAn infinite path in $M$ is:\n$$\\pi = s_0 \\rightarrow s_1 \\rightarrow s_2 \\rightarrow s_3 \\rightarrow \\cdots$$\n\nThe set of states visited infinitely often along $\\pi$ is:\n$$\\text{Inf}(\\pi) = \\{s \\in S \\mid s \\text{ appears infinitely often in } \\pi\\}$$\n\nA path $\\pi$ is **fair** with respect to fairness set $F$ if and only if:\n$$\\forall f \\in F : f \\cap \\text{Inf}(\\pi) \\neq \\emptyset$$\n\nThat is, **every fairness condition must be visited infinitely often** along the path.\n\n## 2.3 Fair Satisfaction Relation\n\nFor a CTL* formula $\\varphi$ and state $s$ in a fair structure $M$:\n\n$$M, s \\models_F \\varphi \\iff \\forall \\pi \\text{ starting at } s, \\text{ if } \\pi \\text{ is fair then } \\pi \\models \\varphi$$\n\nFor **CTL** quantifiers specifically:\n\n- **M, s ⊨_F E ψ** iff there exists a **fair path** $\\pi$ from $s$ such that $\\pi \\models \\psi$.\n- **M, s ⊨_F A ψ** iff for **all fair paths** $\\pi$ from $s$, we have $\\pi \\models \\psi$.\n\n## 2.4 KTU Formula Sheet (High-Yield Cheat Sheet)\n\n| Symbol / Formula | Meaning | Application |\n|------------------|---------|-------------|\n| $F = \\{f_1, \\ldots, f_n\\}$ | Set of fairness constraints | Defines \"fairness assumption\" |\n| $\\text{Inf}(\\pi)$ | Set of states visited infinitely often | Used to test path fairness |\n| $\\forall f \\in F : f \\cap \\text{Inf}(\\pi) \\neq \\emptyset$ | Fairness predicate on a path | Decides if a path is fair |\n| $M, s \\models_F \\varphi$ | Fair satisfaction relation | CTL/LTL with fairness |\n| $\\Box\\Diamond f_i$ (LTL) | $f_i$ holds infinitely often | Unconditional fairness |\n| $\\Box\\Diamond \\text{en}(f_i) \\Rightarrow \\Box\\Diamond f_i$ | Strong fairness | Compassion condition |\n| $\\Diamond\\Box \\text{en}(f_i) \\Rightarrow \\Box\\Diamond f_i$ | Weak fairness | Justice-like, weaker |\n| $\\pi \\models_F \\varphi$ | Path $\\pi$ satisfies $\\varphi$ under fairness | LTL/CTL* model checking |\n| $\\text{Fair}(M) = \\{\\pi \\in \\text{Paths}(M) \\mid \\pi \\text{ fair w.r.t. } F\\}$ | Set of fair paths | Subset of all paths |\n| $F_{\\text{strong}} \\subseteq F_{\\text{uncond}} \\subseteq F_{\\text{weak}}$ | Implication chain | Strength ordering |\n\n> [!IMPORTANT] **KRITISCHE RULE (Critical Rule)**\n> In CTL model checking under fairness, the **EX** and **EU** operators are computed by considering only fair successors. The standard fixpoint must be intersected with the fairness set $F$.\n\n## 2.5 Engineering Utility of Fairness Strategies\n\n| Field | Application of Fairness |\n|-------|------------------------|\n| **Operating Systems** | Process scheduling verification (no starvation). |\n| **Network Protocols** | Token-ring / leader election — every node gets a chance. |\n| **Hardware Design** | Bus arbitration, cache coherency protocols (MESI). |\n| **Distributed Systems** | Consensus algorithms (Paxos, Raft) — every proposer must get response. |\n| **Embedded Control** | Real-time task scheduling on microcontrollers. |\n| **Security Protocols** | Ensuring intruder cannot permanently block a legitimate agent. |\n\n## 2.6 Logical Hierarchy of Fairness\n\n> [!NOTE] For any Kripke structure $M$ and any formula $\\varphi$:\n> $$\\text{Strong Fairness} \\vdash \\text{Unconditional Fairness} \\vdash \\text{Weak Fairness}$$\n> Therefore: if $M \\models_{\\text{strong}} \\varphi$ then $M \\models_{\\text{uncond}} \\varphi$ then $M \\models_{\\text{weak}} \\varphi$.\n\nIn model-checking tools like **SPIN**, fairness is specified via **`fairness { ... }`** blocks in the **PROMELA** language, which the verifier compiles into additional Büchi acceptance conditions.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: From Path to Fair Path\n\n**Given**: A Kripke structure $M = (S, S_0, R, L, F, AP)$ and an infinite path $\\pi = s_0, s_1, s_2, \\ldots$\n\n**To Prove**: $\\pi$ is fair iff for all $f \\in F$, $f \\cap \\text{Inf}(\\pi) \\neq \\emptyset$.\n\n### Step-by-Step Deduction\n\n**Step 1 — Define the state visitation pattern of $\\pi$.**\n\nEvery state $s_i$ in the path occurs at some index $i \\in \\mathbb{N}$. The set of states that appear more than once (i.e., at infinitely many indices) is:\n$$\\text{Inf}(\\pi) = \\{s \\in S \\mid \\forall n \\in \\mathbb{N}, \\exists k \\geq n : s_k = s\\}$$\n\n**Step 2 — Interpret the fairness constraint.**\n\nEach $f \\in F$ is a subset of $S$, representing a \"fairness obligation\" — a set of states that the path is *required* to visit infinitely often.\n\n**Step 3 — Establish the necessary condition.**\n\nSuppose $\\pi$ is fair. By definition, every $f \\in F$ must be visited infinitely often. This means that for each $f$, there exists some $s \\in f$ such that $s$ appears in $\\pi$ infinitely many times. Hence:\n$$\\forall f \\in F : f \\cap \\text{Inf}(\\pi) \\neq \\emptyset$$\n\n**Step 4 — Establish the sufficient condition.**\n\nConversely, suppose for some $\\pi$, $\\forall f \\in F$, $f \\cap \\text{Inf}(\\pi) \\neq \\emptyset$. Then for each $f$, there is at least one state $s \\in f$ appearing infinitely often in $\\pi$, meaning the path satisfies the fairness requirement for every constraint. Therefore, $\\pi$ is fair.\n\n**Step 5 — Combine both directions.**\n\nThe two conditions are equivalent by construction:\n$$\\pi \\text{ is fair} \\iff \\forall f \\in F : f \\cap \\text{Inf}(\\pi) \\neq \\emptyset \\quad \\blacksquare$$\n\n## 3.2 Derivation: Fixpoint Characterization of Fair Satisfaction\n\nFor the CTL formula $\\text{E}(\\varphi \\,\\text{U}\\, \\psi)$ under fairness $F$, the standard least fixpoint is modified as follows.\n\n### Standard (Non-Fair) Computation\n\n$$\\text{E}(\\varphi \\,\\text{U}\\, \\psi) = \\mu Z .\\, \\psi \\lor (\\varphi \\land \\text{EX}\\, Z)$$\n\n### Fair Modification\n\nThe fair version requires every continuation of $Z$ to eventually visit a fairness constraint. This is achieved by intersecting with the **greatest fixpoint** of \"always staying in fair-progress states\":\n\n$$\\text{E}_F(\\varphi \\,\\text{U}\\, \\psi) = \\mu Z .\\, \\psi \\lor (\\varphi \\land \\text{EX}\\, Z) \\land \\nu Y .\\, \\bigvee_{f \\in F} \\left( \\text{EX}\\, Y \\land f \\right) \\cup Z$$\n\nThe term $\\nu Y . \\bigvee_{f \\in F} (\\text{EX}\\, Y \\land f)$ ensures that along any fair path, fairness constraints are visited infinitely often.\n\n## 3.3 Analytical Matrix: CTL Operators Under Fairness\n\n| CTL Operator | Standard Meaning | Fair Meaning (M, s ⊨_F ...) |\n|--------------|------------------|------------------------------|\n| $A X \\varphi$ | All next states satisfy $\\varphi$ | All **fair-successor** next states satisfy $\\varphi$ |\n| $E X \\varphi$ | Some next state satisfies $\\varphi$ | Some **fair-successor** next state satisfies $\\varphi$ |\n| $A(\\varphi \\,U\\, \\psi)$ | All paths satisfy $\\varphi$ until $\\psi$ | All **fair paths** satisfy the until |\n| $E(\\varphi \\,U\\, \\psi)$ | Some path satisfies $\\varphi$ until $\\psi$ | Some **fair path** satisfies the until |\n| $A G \\varphi$ | All paths globally satisfy $\\varphi$ | All **fair paths** globally satisfy $\\varphi$ |\n| $E F \\varphi$ | Some path eventually reaches $\\varphi$ | Some **fair path** eventually reaches $\\varphi$ |\n\n## 3.4 Worked Example: Mutex Protocol with Fairness\n\n**Scenario**: A semaphore-based mutual exclusion protocol between two processes P1 and P2.\n\n**States**:\n- $s_0 = \\text{init}$ (both waiting)\n- $s_1 = \\text{P1 in CS}$ (P1 holds lock)\n- $s_2 = \\text{P2 in CS}$ (P2 holds lock)\n- $s_3 = \\text{deadlock}$ (both waiting forever)\n\n**Transitions**:\n- $s_0 \\to s_1$, $s_0 \\to s_2$, $s_1 \\to s_0$, $s_2 \\to s_0$, $s_0 \\to s_3$\n\n**Fairness Constraint**: $F = \\{f_1, f_2\\}$ where:\n- $f_1 = \\{s_1\\}$ (P1 must enter CS infinitely often)\n- $f_2 = \\{s_2\\}$ (P2 must enter CS infinitely often)\n\n**Question**: Does the system satisfy $\\Box \\Diamond(\\text{P1 in CS})$?\n\n### Solution (Step-by-Step)\n\n**Step 1** — Identify all infinite paths in $M$:\n- $\\pi_1 = s_0 \\to s_1 \\to s_0 \\to s_1 \\to \\cdots$ (P1 keeps getting in)\n- $\\pi_2 = s_0 \\to s_2 \\to s_0 \\to s_2 \\to \\cdots$ (P2 keeps getting in)\n- $\\pi_3 = s_0 \\to s_3 \\to s_3 \\to s_3 \\to \\cdots$ (deadlock path)\n- $\\pi_4 = s_0 \\to s_1 \\to s_0 \\to s_2 \\to s_0 \\to s_1 \\to \\cdots$ (alternating)\n\n**Step 2** — Compute $\\text{Inf}(\\pi)$ for each:\n- $\\text{Inf}(\\pi_1) = \\{s_0, s_1\\}$\n- $\\text{Inf}(\\pi_2) = \\{s_0, s_2\\}$\n- $\\text{Inf}(\\pi_3) = \\{s_3\\}$\n- $\\text{Inf}(\\pi_4) = \\{s_0, s_1, s_2\\}$\n\n**Step 3** — Test fairness:\n- $f_1 \\cap \\text{Inf}(\\pi_1) = \\{s_1\\} \\neq \\emptyset$ ✓ ; $f_2 \\cap \\text{Inf}(\\pi_1) = \\emptyset$ ✗ → $\\pi_1$ **not fair**.\n- $f_1 \\cap \\text{Inf}(\\pi_2) = \\emptyset$ ✗ → $\\pi_2$ **not fair**.\n- $f_1 \\cap \\text{Inf}(\\pi_3) = \\emptyset$ ✗ → $\\pi_3$ **not fair**.\n- $f_1 \\cap \\text{Inf}(\\pi_4) = \\{s_1\\}$ ✓ ; $f_2 \\cap \\text{Inf}(\\pi_4) = \\{s_2\\}$ ✓ → $\\pi_4$ **fair**.\n\n**Step 4** — Only $\\pi_4$ is fair. Since $\\pi_4$ visits $s_1$ infinitely often, $\\Box \\Diamond(\\text{P1 in CS})$ is **TRUE** under fairness. ∎\n\n## 3.5 Reduction Theorem (LTL with Fairness)\n\n**Theorem**: For any LTL formula $\\varphi$ and any fair Kripke structure $M = (S, S_0, R, L, F, AP)$:\n\n$$M \\models_F \\varphi \\iff (M \\otimes \\mathcal{B}_{\\neg \\varphi}) \\cap \\mathcal{B}_F = \\emptyset$$\n\nwhere:\n- $\\mathcal{B}_{\\neg\\varphi}$ is the Büchi automaton for $\\neg\\varphi$.\n- $\\mathcal{B}_F$ is the Büchi automaton encoding the fairness constraints in $F$.\n- $\\otimes$ is the standard product construction.\n\n> [!IMPORTANT] **Practical Consequence**\n> LTL model checking under fairness reduces to checking the **emptiness of a product Büchi automaton** — a problem solvable in linear time $O(|M| \\cdot |\\mathcal{B}_{\\neg\\varphi}| \\cdot |\\mathcal{B}_F|)$.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Mermaid Diagram: Fair vs. Unfair Path Classification\n\n```mermaid\nflowchart TD\n    nodeA([Start: M = Kripke Structure]) --> nodeB{Is pi infinite?}\n    nodeB -- No --> nodeC([Truncate: Finite path])\n    nodeB -- Yes --> nodeD[Compute Inf pi]\n    nodeD --> nodeE{For every f in F:<br/>f intersect Inf pi is not empty?}\n    nodeE -- No --> nodeF([Path is UNFAIR<br/>Excluded from verification])\n    nodeE -- Yes --> nodeG([Path is FAIR<br/>Retained for verification])\n    nodeF --> nodeH[Apply to M, s models F phi]\n    nodeG --> nodeH\n    nodeH --> nodeI{Path exists with phi?}\n    nodeI -- Yes --> nodeJ([M, s models F E phi])\n    nodeI -- No --> nodeK([M, s does NOT models F E phi])\n\n    style nodeA fill:#1e90ff,stroke:#000,color:#fff\n    style nodeG fill:#228b22,stroke:#000,color:#fff\n    style nodeF fill:#dc143c,stroke:#000,color:#fff\n    style nodeJ fill:#32cd32,stroke:#000,color:#fff\n    style nodeK fill:#ff8c00,stroke:#000,color:#fff\n```\n\n## 4.2 Mermaid Diagram: Hierarchy of Fairness Strengths\n\n```mermaid\ngraph LR\n    nodeSF[Strong Fairness<br/>Compassion] --> nodeUF[Unconditional Fairness<br/>Justice]\n    nodeUF --> nodeWF[Weak Fairness]\n    nodeSF -.->|implies| nodeUF\n    nodeUF -.->|implies| nodeWF\n    nodeSF --> nodeProp1[M, s models F phi<br/>Strongest guarantee]\n    nodeUF --> nodeProp2[M, s models F phi<br/>Moderate guarantee]\n    nodeWF --> nodeProp3[M, s models F phi<br/>Weakest guarantee]\n\n    style nodeSF fill:#8b0000,stroke:#000,color:#fff\n    style nodeUF fill:#b8860b,stroke:#000,color:#fff\n    style nodeWF fill:#556b2f,stroke:#000,color:#fff\n    style nodeProp1 fill:#228b22,stroke:#000,color:#fff\n    style nodeProp2 fill:#daa520,stroke:#000,color:#fff\n    style nodeProp3 fill:#90ee90,stroke:#000,color:#000\n```\n\n## 4.3 Mermaid Diagram: Block-Level Functional Architecture of Fair Model Checking\n\n```mermaid\nflowchart TB\n    subgraph InputLayer[\"INPUT LAYER\"]\n        nodeMS[Kripke Structure M]\n        nodeF[Fairness Set F]\n        nodePhi[Property phi in CTL or LTL]\n    end\n\n    subgraph TransformLayer[\"TRANSFORMATION LAYER\"]\n        nodeMS --> nodeNEG[Encode NOT phi as Buchi]\n        nodeF --> nodeFB[Encode F as Buchi]\n        nodeNEG --> nodePROD[Product Automaton<br/>M times B sub NOT phi times B sub F]\n        nodeFB --> nodePROD\n    end\n\n    subgraph AnalysisLayer[\"ANALYSIS LAYER\"]\n        nodePROD --> nodeEMPT[Check Emptiness<br/>SCC with accepting cycle]\n        nodeEMPT --> nodeDEC{Accepting cycle<br/>exists?}\n    end\n\n    subgraph OutputLayer[\"OUTPUT LAYER\"]\n        nodeDEC -- Yes --> nodeCEX[Counterexample<br/>FAIR path violating phi]\n        nodeDEC -- No --> nodeSAT[M, s models F phi<br/>Property HOLDS]\n    end\n\n    style InputLayer fill:#e0f7fa,stroke:#000\n    style TransformLayer fill:#fff9c4,stroke:#000\n    style AnalysisLayer fill:#f8bbd0,stroke:#000\n    style OutputLayer fill:#c8e6c9,stroke:#000\n```\n\n## 4.4 Mermaid Diagram: Fairness Application in PROMELA (SPIN)\n\n```mermaid\nsequenceDiagram\n    participant Dev as Developer\n    participant PRO as PROMELA Model\n    participant SPIN as SPIN Verifier\n    participant OG as Output (Trail)\n\n    Dev->>PRO: Define proctype P1, P2, ...\n    PRO->>PRO: Add fairness block<br/>fairness P1 enters CS\n    PRO->>PRO: Add fairness block<br/>fairness P2 enters CS\n    Dev->>SPIN: spin -a model.pml\n    SPIN->>SPIN: Generate Buchi automaton\n    SPIN->>SPIN: Apply fairness constraints<br/>to accepting states\n    SPIN->>SPIN: Run nested DFS<br/>emptiness check\n    SPIN-->>OG: If counterexample exists,<br/>return FAIR path (trail)\n    SPIN-->>OG: Else: Property verified\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n## PART A — 3-Mark Questions (Short Answer)\n\n### Q1. `[KTU University Exam - May 2024]`\n**Define fairness in the context of model checking. Why is it necessary?**\n\n**Model Answer (3 marks)**:\nFairness is a constraint imposed on the set of execution paths of a Kripke structure such that only those paths in which a set of designated fairness conditions $F = \\{f_1, \\ldots, f_n\\}$ are visited infinitely often are considered valid. A path $\\pi$ is fair iff $\\forall f \\in F, f \\cap \\text{Inf}(\\pi) \\neq \\emptyset$.\n\nIt is necessary because (1 mark) in concurrent/reactive systems, the model may contain paths that are mathematically valid but practically impossible (e.g., a process never scheduled), leading to **spurious counterexamples** (1 mark). Fairness prunes these unrealistic paths, making verification of liveness properties meaningful (1 mark).\n\n---\n\n### Q2. `[KTU University Exam - Dec 2023]`\n**Distinguish between unconditional fairness and strong fairness with LTL expressions.**\n\n**Model Answer (3 marks)**:\n- **Unconditional Fairness (Justice)** (1.5 marks): Requires that every fairness condition $f_i$ holds infinitely often, regardless of whether it is enabled:\n$$\\bigwedge_{i=1}^{n} \\Box\\Diamond f_i$$\n- **Strong Fairness (Compassion)** (1.5 marks): Requires that if a condition is infinitely often enabled, it must be infinitely often taken:\n$$\\bigwedge_{i=1}^{n} (\\Box\\Diamond \\text{en}(f_i) \\Rightarrow \\Box\\Diamond f_i)$$\n\nStrong fairness is strictly stronger; unconditional fairness does not consider the *enabled* status of actions.\n\n---\n\n## PART B — 14-Mark Questions (Module Internal Choice)\n\n### ⭐ Question A (14 Marks) `[KTU University Exam - May 2024]`\n\n**(a)** Define a **Fair Kripke Structure**. Explain the three fairness strategies — *unconditional*, *strong*, and *weak* — with their LTL formulations. **(7 marks)**\n\n**(b)** Consider a token-ring protocol with **three processes P1, P2, P3** where the token circulates cyclically. Model this as a fair Kripke structure with fairness constraint $F = \\{f_1, f_2, f_3\\}$ where $f_i$ denotes \"process $P_i$ holds the token\". Verify whether the system satisfies the property $\\Box \\Diamond(\\text{P2 holds token})$ under unconditional fairness. **(7 marks)**\n\n#### Model Solution for (a) — 7 marks\n\n**Definition of Fair Kripke Structure (2 marks)**:\nA fair Kripke structure is a 6-tuple $M = (S, S_0, R, L, F, AP)$ where $F \\subseteq 2^S$ is the set of fairness constraints, and the satisfaction relation $\\models_F$ considers only paths $\\pi$ such that $\\forall f \\in F, f \\cap \\text{Inf}(\\pi) \\neq \\emptyset$.\n\n**Three Fairness Strategies (5 marks)**:\n\n1. **Unconditional Fairness (Justice)** — Each $f_i$ must hold infinitely often, *no precondition*:\n$$\\text{UncondFair}(F) = \\bigwedge_{i=1}^{n} \\Box\\Diamond f_i$$\nUse case: Abstract scheduling assumptions, hardware bus arbitration.\n\n2. **Strong Fairness (Compassion)** — If $f_i$ is *infinitely often* enabled, it must fire *infinitely often*:\n$$\\text{StrongFair}(F) = \\bigwedge_{i=1}^{n} \\left(\\Box\\Diamond \\,\\text{en}(f_i) \\implies \\Box\\Diamond f_i\\right)$$\nUse case: Starvation-freedom proofs, lock-acquisition protocols.\n\n3. **Weak Fairness** — If $f_i$ is *eventually always* enabled, it must occur infinitely often:\n$$\\text{WeakFair}(F) = \\bigwedge_{i=1}^{n} \\left(\\Diamond\\Box \\,\\text{en}(f_i) \\implies \\Box\\Diamond f_i\\right)$$\nUse case: Initial-delay-tolerant scheduling.\n\n#### Model Solution for (b) — 7 marks\n\n**Step 1 — Define the model (2 marks)**:\n- $S = \\{s_0, s_1, s_2, s_3\\}$\n- $S_0 = \\{s_0\\}$\n- Transitions: $s_0 \\to s_1 \\to s_2 \\to s_3 \\to s_0$ (cyclic token passing)\n- $f_1 = \\{s_1\\}$, $f_2 = \\{s_2\\}$, $f_3 = \\{s_3\\}$\n- $L(s_1) = \\{\\text{P1 holds}\\}$, $L(s_2) = \\{\\text{P2 holds}\\}$, $L(s_3) = \\{\\text{P3 holds}\\}$\n\n**Step 2 — Identify all paths (1 mark)**:\nThe single infinite path: $\\pi = s_0 \\to s_1 \\to s_2 \\to s_3 \\to s_0 \\to s_1 \\to \\cdots$\n\n**Step 3 — Compute $\\text{Inf}(\\pi)$ (1 mark)**:\n$$\\text{Inf}(\\pi) = \\{s_0, s_1, s_2, s_3\\}$$\n\n**Step 4 — Test fairness (2 marks)**:\n- $f_1 \\cap \\text{Inf}(\\pi) = \\{s_1\\} \\neq \\emptyset$ ✓\n- $f_2 \\cap \\text{Inf}(\\pi) = \\{s_2\\} \\neq \\emptyset$ ✓\n- $f_3 \\cap \\text{Inf}(\\pi) = \\{s_3\\} \\neq \\emptyset$ ✓\n- Therefore $\\pi$ is **fair**.\n\n**Step 5 — Verify the property (1 mark)**:\nProperty: $\\Box\\Diamond(\\text{P2 holds token})$\nSince $s_2$ appears in $\\pi$ infinitely often, the property **HOLDS** under unconditional fairness. $\\blacksquare$\n\n---\n\n### ⭐ Question B (14 Marks) `[KTU University Exam - Dec 2023]` *(Alternative Choice)*\n\n**(a)** Explain the **fair satisfaction relation** in CTL. How does the computation of EX, EU, and EG operators change under fairness? **(7 marks)**\n\n**(b)** For a producer-consumer system with a buffer of size 1, define a fair Kripke structure where fairness constraints ensure that the producer is not starved. Show that without the fairness constraint, the property $\\Box\\Diamond(\\text{consumer consumes})$ is **false** in the model, but with the fairness constraint, it becomes **true**. **(7 marks)**\n\n#### Model Solution for (a) — 7 marks\n\n**Fair CTL Satisfaction (3 marks)**:\nThe fair satisfaction relation $\\models_F$ is defined as:\n$$M, s \\models_F \\varphi \\iff \\forall \\pi \\text{ starting at } s, \\text{ if } \\pi \\text{ fair then } \\pi \\models \\varphi$$\n\nFor path quantifiers:\n- $M, s \\models_F E \\psi$ iff there exists a **fair** path $\\pi$ from $s$ with $\\pi \\models \\psi$.\n- $M, s \\models_F A \\psi$ iff for **all fair** paths $\\pi$ from $s$, $\\pi \\models \\psi$.\n\n**Operator Modifications (4 marks)**:\n\n| Operator | Standard | Under Fairness $F$ |\n|----------|----------|--------------------|\n| $EX \\varphi$ | $s$ has a successor satisfying $\\varphi$ | $s$ has a **fair successor** satisfying $\\varphi$ |\n| $E(\\varphi U \\psi)$ | Standard least fixpoint $\\mu Z.\\psi \\lor (\\varphi \\land EX Z)$ | Modified to intersect with $\\nu Y.\\bigvee_{f \\in F}(EX Y \\land f) \\cup Z$ to enforce fairness |\n| $EG \\varphi$ | Greatest fixpoint $\\nu Z.\\varphi \\land EX Z$ | Greatest fixpoint restricted to **fair paths**: $\\nu Z.\\varphi \\land EX_F Z$ |\n\nThe **key insight** is that under fairness, successors are restricted to those that lie on at least one fair path, and the fixpoint characterizations must encode the requirement that fairness constraints are visited infinitely often.\n\n#### Model Solution for (b) — 7 marks\n\n**Step 1 — Model definition (2 marks)**:\n- States: $s_0 = (\\text{prod waiting}, \\text{buf empty})$, $s_1 = (\\text{prod producing})$, $s_2 = (\\text{buf full}, \\text{cons waiting})$, $s_3 = (\\text{cons consuming})$\n- Transitions: $s_0 \\to s_1 \\to s_2 \\to s_3 \\to s_0$, plus **spurious**: $s_0 \\to s_0$ (producer never fires)\n- Without fairness: $F = \\emptyset$.\n\n**Step 2 — Path analysis without fairness (2 marks)**:\n- Spurious path: $\\pi_{\\text{starve}} = s_0 \\to s_0 \\to s_0 \\to \\cdots$ with $\\text{Inf}(\\pi) = \\{s_0\\}$.\n- Property $\\Box\\Diamond(\\text{cons consumes})$ fails on $\\pi_{\\text{starve}}$ since $s_3$ is never visited.\n- **Conclusion**: $M \\models \\Box\\Diamond(\\text{cons consumes})$ is **FALSE**.\n\n**Step 3 — Add fairness constraint (2 marks)**:\nDefine $F = \\{f_{\\text{prod}}\\}$ where $f_{\\text{prod}} = \\{s_1\\}$ (producer must enter \"producing\" infinitely often).\n\n**Step 4 — Re-analyze (1 mark)**:\n- $\\pi_{\\text{starve}}$ has $\\text{Inf}(\\pi) = \\{s_0\\}$, so $f_{\\text{prod}} \\cap \\text{Inf}(\\pi) = \\emptyset$ → **not fair**, excluded.\n- The only fair path $\\pi_{\\text{fair}} = s_0 \\to s_1 \\to s_2 \\to s_3 \\to s_0 \\to \\cdots$ visits $s_3$ infinitely often.\n- **Conclusion**: $M, s_0 \\models_F \\Box\\Diamond(\\text{cons consumes})$ is **TRUE**. $\\blacksquare$\n\n---\n\n> [!WARNING] **KTU Examiner's Valuation Pitfall Callout**\n> 1. **Forgetting to check Inf(π) intersection** — Students often declare a path fair without verifying $\\forall f \\in F, f \\cap \\text{Inf}(\\pi) \\neq \\emptyset$. This loses 2-3 marks.\n> 2. **Confusing strong vs. unconditional fairness** — Strong fairness requires *enabled* precondition; unconditional does not. Mark deduction: 1.5 marks.\n> 3. **Not listing all paths** — In KTU valuations, partial path enumeration leads to incorrect \"FAIR\" verdicts. Always enumerate *all* infinite paths exhaustively.\n> 4. **Mixing |= with |=_F** — The fair satisfaction relation has a stricter quantifier range; using plain satisfaction loses 1 mark.\n> 5. **Forgetting $\\mu$ (least) vs. $\\nu$ (greatest) fixpoint distinction** in fair CTL operators — common 1-mark error.\n\n---\n\n## 🔁 Topic Recap & Important Things to Remember (Rapid Revision Checklist)\n\n- ✅ **Fairness** = constraint on paths so that fairness conditions are visited **infinitely often**.\n- ✅ **Fair Kripke Structure** = 6-tuple $M = (S, S_0, R, L, F, AP)$ with $F \\subseteq 2^S$.\n- ✅ **Path Fairness Predicate**: $\\pi \\text{ fair} \\iff \\forall f \\in F : f \\cap \\text{Inf}(\\pi) \\neq \\emptyset$.\n- ✅ **Three Strategies**:\n  - **Unconditional (Justice)**: $\\bigwedge \\Box\\Diamond f_i$\n  - **Strong (Compassion)**: $\\bigwedge (\\Box\\Diamond \\text{en}(f_i) \\Rightarrow \\Box\\Diamond f_i)$\n  - **Weak**: $\\bigwedge (\\Diamond\\Box \\text{en}(f_i) \\Rightarrow \\Box\\Diamond f_i)$\n- ✅ **Hierarchy**: Strong $\\Rightarrow$ Unconditional $\\Rightarrow$ Weak.\n- ✅ **Fair Satisfaction**: $M, s \\models_F \\varphi$ restricts both $\\forall$ and $\\exists$ path quantifiers to *fair paths only*.\n- ✅ **CTL Operator Modification** under $F$: $EX \\to EX_F$, $EU \\to E_FU$, $EG \\to E_FG$ via fixpoint intersection.\n- ✅ **LTL Reduction**: Product of Kripke structure, Büchi($\\neg\\varphi$), and Büchi($F$); check emptiness.\n- ✅ **Elimination of Spurious Counterexamples**: Fairness is the *only* way to validate liveness properties like $\\Box\\Diamond p$.\n- ✅ **Tool Support**: SPIN (PROMELA `fairness` block), NuSMV (`FAIRNESS` declaration), UPPAAL.\n- ✅ **Engineering Use**: Process scheduling, mutual exclusion, token passing, consensus protocols, cache coherency, leader election.\n- ✅ **Always check Inf(π) intersection** — the heart of fair path identification.\n- ✅ **Liveness properties** (`◇p`, `□◇p`) are *only* meaningful under some fairness assumption.\n- ✅ **Safety properties** (`□p`, `¬□◇p`) are insensitive to fairness — note this distinction for exams.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 1-t30",
          "title": "Fairness and Safety (Definition and examples only for all topics - no proof required)",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 1\n> - **Topic:** Fairness and Safety (Definition and examples only for all topics - no proof required)\n\n<!-- SECTION_1_START -->\n# SECTION 1 — Core Technical Definition & Intuitive Overview\n\n## 1.1 Safety Properties\n\nA **Safety Property** is a property of a reactive or concurrent system that asserts *“nothing bad ever happens”* during the execution of the system. Formally, a property $P$ is a safety property if and only if for every execution (trace) $\\sigma$ that violates $P$, there exists a finite prefix $\\hat{\\sigma}$ of $\\sigma$ such that every extension of $\\hat{\\sigma}$ also violates $P$. In simpler words, a violation of a safety property can always be detected by examining a *finite* portion of the execution.\n\n> [!IMPORTANT]\n> **KTU Board Definition (Sistla's Safety Classification):**\n> A property $P \\subseteq \\Sigma^{\\omega}$ is a **safety property** iff for every infinite word $w \\notin P$, there exists a *finite* prefix $u$ of $w$ such that $u v \\notin P$ for all $v \\in \\Sigma^{\\omega}$. This is the canonical definition examiners expect in **PECNT526**.\n\n> [!NOTE]\n> **Intuitive Analogy:** Think of an **elevator door**. A safety property says: *\"The elevator door shall never open when the elevator is between floors.\"* You do not need to observe the elevator forever — the very moment the door starts opening between floors, the bad event has happened. Finite observation is sufficient to confirm a violation.\n\n### Common Examples of Safety Properties\n- **Mutual Exclusion:** Two processes are never in the critical section simultaneously.\n- **Deadlock Freedom:** The system never reaches a state where all processes are blocked.\n- **Type Safety:** A variable of type *integer* is never assigned a string value.\n- **Partial Correctness:** If the program terminates, then the result is correct.\n- **No division by zero.**\n- **Buffer Overflow Avoidance:** The buffer index is always within $[0, n-1]$.\n\n---\n\n## 1.2 Liveness Properties\n\nA **Liveness Property** is a property that asserts *“something good eventually happens”*. Formally, $P$ is a liveness property iff for every finite trace $u \\in \\Sigma^{\\star}$, there exists an infinite extension $v$ such that $uv \\in P$. Equivalently, no finite prefix of a trace can ever disprove the property — violations can only be detected in the infinite limit.\n\n> [!NOTE]\n> **Intuitive Analogy:** Consider a **printer queue**. A liveness property says: *\"Every print job that is submitted will eventually be printed.\"* You cannot disprove this by watching for 5 minutes — the job might still be printed tomorrow. Only an *infinite* wait with no printing ever happening can violate the property.\n\n### Common Examples of Liveness Properties\n- **Termination:** Every program halts eventually.\n- **Starvation Freedom:** Every waiting process eventually enters the critical section.\n- **Response:** Every request is eventually followed by a response.\n- **Total Correctness:** The program terminates and the result is correct.\n\n---\n\n## 1.3 Fairness Properties\n\n**Fairness** is a special class of *liveness* properties that constrain the scheduling of concurrent processes. Fairness ensures that no process (or transition) that is *continuously enabled* (or *infinitely often enabled*) is *permanently ignored* by the scheduler. It bridges the gap between an abstract system model and real-world concurrent execution.\n\n> [!IMPORTANT]\n> **KTU Board Definition:**\n> Fairness is a scheduling assumption that guarantees reasonable, non-discriminatory progress of enabled components in a concurrent/reactive system. It is essential for compositional reasoning about liveness.\n\n### Conceptual Analogy\nImagine a **round-robin parent serving four children ice-cream**. A *fair* parent will not let one child wait forever while serving the others repeatedly. Fairness formalizes this intuitive scheduling justice in concurrent systems.\n\n> [!NOTE]\n> **Why Fairness Matters in Verification:** Without fairness, a model checker may produce a counter-example in which a process is starved *unrealistically*. Fairness constraints prune such unrealistic traces, making verification results meaningful for real schedulers.\n\n---\n\n## 1.4 The Property Hierarchy (Quick Map)\n\n$$\n\\boxed{\\text{System Properties}}\n\\;\\supset\\;\n\\boxed{\\text{Liveness}} \\;\\supset\\; \\boxed{\\text{Fairness (Weak, Strong)}}\n$$\n\n$$\n\\boxed{\\text{System Properties}}\n\\;\\supset\\;\n\\boxed{\\text{Safety}} \\;\\supset\\; \\boxed{\\text{Mutual Exclusion, Deadlock Freedom, ...}}\n$$\n\n> [!TIP]\n> **Examiner Tip:** The single most-tested high-yield question in this module is: *\"Classify the given property as Safety or Liveness.\"* Memorize the canonical examples in Sections 1.1 and 1.2 verbatim.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2 — Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Three Classical Variants of Fairness\n\nFairness is not a single concept. The KTU syllabus (and the standard reference *Manna & Pnueli, 1991*) recognises three principal variants. They differ in **how often** a process/transition must be enabled for the scheduler to be obligated to schedule it.\n\n### 2.1.1 Impartiality (Unconditional Fairness)\nEvery process is executed infinitely often in every infinite computation — *regardless* of whether it is enabled or not. This is the strongest (and often unrealistic) form.\n\n### 2.1.2 Weak Fairness (Justice)\n**Definition (KTU):** A transition (or process) $t$ satisfies *weak fairness* if, in every infinite execution, *whenever* $t$ is *continuously enabled* from some point onward, $t$ is eventually taken.\n\n$$\nWF(t) : \\quad \\Box \\Diamond \\;\\text{enabled}(t) \\;\\Rightarrow\\; \\Box \\Diamond \\;\\text{executed}(t)\n$$\n\n> Equivalently: $\\Box \\Diamond \\text{enabled}(t) \\Rightarrow \\Box \\Diamond \\text{executed}(t)$.\n\n### 2.1.3 Strong Fairness (Compassion)\n**Definition (KTU):** A transition (or process) $t$ satisfies *strong fairness* if, in every infinite execution, *whenever* $t$ is *infinitely often enabled*, $t$ is eventually taken.\n\n$$\nSF(t) : \\quad \\Box \\Diamond \\;\\text{enabled}(t) \\;\\Rightarrow\\; \\Box \\Diamond \\;\\text{executed}(t)\n$$\n\n> [!IMPORTANT]\n> **Hierarchy of Strength:** $\\;SF(t) \\;\\Rightarrow\\; WF(t) \\;\\Rightarrow\\; \\text{Impartiality on enabled actions}$. Strong fairness is strictly stronger than weak fairness. Any system proven correct under SF is also correct under WF — *but not vice-versa*.\n\n---\n\n## 2.2 Why Proofs Are Excluded in This Module\n\nAs per the **KTU 2024 scheme module specification**, the topic *\"Fairness and Safety\"* is restricted to **definitions and examples only — no proof required**. Hence:\n\n- ❌ No derivation of $SF \\Rightarrow WF$ is required.\n- ❌ No proof that mutual exclusion is a safety property is required.\n- ✅ Only the *recognition* of the property, its formal statement, and worked examples are needed.\n\n---\n\n## 2.3 KTU Formula Sheet / Cheat Sheet\n\n| # | Concept | Formal Statement (LTL / Set-Theoretic) | Symbol / Shorthand | Typical Example |\n|---|---------|---------------------------------------|--------------------|-----------------|\n| 1 | Safety Property | $\\forall w \\notin P,\\; \\exists$ finite prefix $u$ of $w$ such that $uv \\notin P$ for all $v$ | $\\text{Safety}(P)$ | Mutual exclusion |\n| 2 | Liveness Property | $\\forall u \\in \\Sigma^{\\star},\\; \\exists v \\in \\Sigma^{\\omega}$ such that $uv \\in P$ | $\\text{Liveness}(P)$ | Termination |\n| 3 | Weak Fairness | $\\Box \\Diamond \\text{enabled}(t) \\Rightarrow \\Box \\Diamond \\text{executed}(t)$ | $WF(t)$ | \"If a process is always ready, it must run\" |\n| 4 | Strong Fairness | $\\Diamond \\Box \\text{enabled}(t) \\Rightarrow \\Box \\Diamond \\text{executed}(t)$ | $SF(t)$ | \"If a process becomes ready infinitely often, it must run\" |\n| 5 | Impartiality | Every process is taken infinitely often | $IF$ | Round-robin scheduling |\n\n> [!TIP]\n> **Memory Trick for LTL Patterns:**\n> - $\\Box$ (Box) = \"always / henceforth\" → represents *continuous* enabledness.\n> - $\\Diamond$ (Diamond) = \"eventually / sometimes\" → represents *intermittent* enabledness.\n> - $WF$: $\\Box \\Diamond$ on left, $SF$: $\\Diamond \\Box$ on left (the *order* of $\\Box, \\Diamond$ flips!).\n\n---\n\n## 2.4 Real-World Engineering Utility\n\n| Domain | Application of Fairness & Safety |\n|--------|----------------------------------|\n| **Operating Systems** | Process schedulers (CFS, Round-Robin) implement weak fairness; mutex protocols (Peterson, Dekker) guarantee safety. |\n| **Network Protocols** | TCP retransmission timer = liveness; no packet duplication = safety. |\n| **Hardware Verification** | Intel/Cadence model checking uses fairness to rule out unrealistic arbiter traces. |\n| **Embedded / IoT** | Watchdog timers = liveness; sensor value range = safety. |\n| **Database Systems** | ACID transactions: atomicity = safety; eventual commit = liveness. |\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3 — Step-by-Step Derivations & Analytical Matrix\n\n> [!IMPORTANT]\n> **KTU Module Constraint Active:** This section provides *worked-out classification examples* and *trace analyses*. **No formal proofs** are required by the 2024 scheme, and therefore none are given. The focus is on **identifying** which class a property belongs to using the standard KTU decision procedure.\n\n## 3.1 The KTU Decision Procedure: Safety vs. Liveness\n\nFor any given property $P$, ask the following two questions in order:\n\n1. **Question 1 (Safety Test):** *\"Can a finite prefix of execution disprove $P$?\"*\n   - If **YES** → $P$ is a **Safety** property.\n   - If **NO** → proceed to Question 2.\n\n2. **Question 2 (Liveness Test):** *\"Is it true that for every finite trace, some extension satisfies $P$?\"*\n   - If **YES** → $P$ is a **Liveness** property.\n   - If **NO** → $P$ is **Neither** (most properties are a conjunction of both).\n\n> This 2-question decision procedure is the **highest-yield analytical tool** in this module.\n\n## 3.2 Worked Example 1 — Mutual Exclusion\n\n**Property:** *\"Processes $P_1$ and $P_2$ are never simultaneously in the critical section.\"*\n\n**Step 1:** Consider a trace $\\sigma$ in which $P_1$ enters CS at time $t=5$ and $P_2$ enters CS at time $t=5.001$.\n\n**Step 2:** The finite prefix up to $t=5.001$ already violates the property. So a **finite** prefix is sufficient to disprove $P$.\n\n**Step 3:** Therefore, $P$ is a **Safety Property**. ✅\n\n$$\n\\boxed{\\text{Mutual Exclusion} \\in \\text{Safety}}\n$$\n\n## 3.3 Worked Example 2 — Starvation Freedom\n\n**Property:** *\"If a process requests the critical section, it will eventually enter.\"*\n\n**Step 1:** Consider any finite trace in which a process requests CS at time $t=10$ but the trace ends at $t=20$ without the process ever entering CS. Has the property been violated?\n\n**Step 2:** **No** — the property only says *eventually*. The process might still enter CS at $t=100$. So a finite observation cannot disprove the property.\n\n**Step 3:** Test the liveness condition: for every finite trace $u$, can we extend $u$ to satisfy the property? Yes — simply let the waiting process enter CS after $u$ ends.\n\n**Step 4:** Therefore, the property is a **Liveness Property**. ✅\n\n$$\n\\boxed{\\text{Starvation Freedom} \\in \\text{Liveness}}\n$$\n\n## 3.4 Worked Example 3 — Bounded Overtaking (Both!)\n\n**Property:** *\"Every request is eventually granted, AND no process is overtaken more than 5 times.\"*\n\n- The *eventual granting* part is a **liveness** condition.\n- The *bounded overtaking* part is a **safety** condition (a finite prefix of 6 overtakings already violates it).\n\nTherefore, this property is a **conjunction of safety and liveness** — it cannot be classified as one alone. This is the standard KTU trap question.\n\n$$\n\\boxed{P = \\underbrace{\\text{Bounded Overtaking}}_{\\text{Safety}} \\;\\land\\; \\underbrace{\\text{Eventual Grant}}_{\\text{Liveness}}}\n$$\n\n## 3.5 Worked Example 4 — Classifying Fairness Variants\n\nConsider a transition $t$ with the following execution trace:\n\n$$\nt \\text{ enabled at: } 0,\\,2,\\,4,\\,6,\\,8,\\,\\dots \\text{ (continuously)}\n$$\n$$\nt \\text{ taken at: } 1,\\,5,\\,9,\\,\\dots \\text{ (eventually taken)}\n$$\n\n**Step 1 — Weak Fairness Check:** Is $t$ continuously enabled? **Yes** (it is enabled at every even time). Is $t$ eventually taken? **Yes** (at $t=1, 5, 9, \\dots$). So **WF(t) holds**. ✅\n\n**Step 2 — Strong Fairness Check:** Is $t$ infinitely often enabled? **Yes** (in fact continuously). Is $t$ eventually taken? **Yes**. So **SF(t) also holds**. ✅\n\n> **Note:** When a transition is *continuously* enabled, WF and SF coincide. They differ only when enabledness is *intermittent*.\n\n## 3.6 Analytical Matrix: Property Classification Reference\n\n| Property (KTU Standard Examples) | Safety? | Liveness? | Why? |\n|----------------------------------|:-------:|:---------:|------|\n| Mutual Exclusion | ✅ | ❌ | Finite prefix detects violation |\n| Deadlock Freedom | ✅ | ❌ | Reaching deadlock state is a finite-prefix event |\n| Partial Correctness | ✅ | ❌ | Wrong output on termination is finite-prefix |\n| Type Safety | ✅ | ❌ | Type error occurs in finite steps |\n| Termination | ❌ | ✅ | Only an infinite wait disproves |\n| Starvation Freedom | ❌ | ✅ | Eventual progress, finite prefix cannot disprove |\n| Eventual Response | ❌ | ✅ | Same as above |\n| Bounded Overtaking | ✅ | ❌ | Counter exists in finite prefix |\n| Weak Fairness $WF(t)$ | ❌ | ✅ | Scheduling-based liveness |\n| Strong Fairness $SF(t)$ | ❌ | ✅ | Scheduling-based liveness |\n\n> [!TIP]\n> **Exam Shortcut:** Whenever a property contains the words *“eventually”*, *“infinitely often”*, or *“forever”* — it is **Liveness**. Whenever it contains the words *“never”*, *“at all times”*, or *“no more than”* — it is **Safety**.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4 — Structural Diagrams & Schematics\n\n## 4.1 Hierarchy of System Properties (Block Architecture)\n\n```mermaid\nflowchart TD\n    A[System Properties P]:::root --> B[Trace Properties]:::cat\n    A --> C[Non-Trace Properties e.g Hyperproperties]:::other\n    B --> D[Safety]:::safe\n    B --> E[Liveness]:::live\n    D --> D1[Mutual Exclusion]:::ex\n    D --> D2[Deadlock Freedom]:::ex\n    D --> D3[Type Safety]:::ex\n    D --> D4[Partial Correctness]:::ex\n    E --> E1[Termination]:::ex2\n    E --> E2[Starvation Freedom]:::ex2\n    E --> E3[Response]:::ex2\n    E --> F[Fairness]:::fair\n    F --> F1[Impartiality IF]:::fairEx\n    F --> F2[Weak Fairness WF]:::fairEx\n    F --> F3[Strong Fairness SF]:::fairEx\n\n    classDef root fill:#1f3a5f,color:#fff,stroke:#000\n    classDef cat fill:#4a6fa5,color:#fff,stroke:#000\n    classDef other fill:#888,color:#fff,stroke:#000\n    classDef safe fill:#2e7d32,color:#fff,stroke:#000\n    classDef live fill:#c62828,color:#fff,stroke:#000\n    classDef ex fill:#a5d6a7,color:#000,stroke:#000\n    classDef ex2 fill:#ef9a9a,color:#000,stroke:#000\n    classDef fair fill:#f57c00,color:#fff,stroke:#000\n    classDef fairEx fill:#ffcc80,color:#000,stroke:#000\n```\n\n**Reading the diagram:** Every reactive system property is a trace property (Alpern-Schneider, 1985). Trace properties split into **Safety** and **Liveness** — these are *disjoint* and *exhaustive* for the class of $\\omega$-regular properties. Fairness is a sub-class of Liveness.\n\n## 4.2 Fairness Sub-Variant Comparison Flow\n\n```mermaid\nflowchart LR\n    S[Scheduler Decision]:::sched --> Q{Is action\\ncontinuously\\nenabled?}:::q\n    Q -- YES --> A[Weak Fairness WF\\napplies]:::wf\n    Q -- NO --> R{Is action\\ninfinitely often\\nenabled?}:::q2\n    R -- YES --> B[Strong Fairness SF\\napplies]:::sf\n    R -- NO --> C[No fairness\\nguarantee needed]:::ng\n    A --> D[Action must\\nbe eventually\\nexecuted]:::ex\n    B --> D\n\n    classDef sched fill:#283593,color:#fff,stroke:#000\n    classDef q fill:#fbc02d,color:#000,stroke:#000\n    classDef q2 fill:#fbc02d,color:#000,stroke:#000\n    classDef wf fill:#2e7d32,color:#fff,stroke:#000\n    classDef sf fill:#c62828,color:#fff,stroke:#000\n    classDef ng fill:#757575,color:#fff,stroke:#000\n    classDef ex fill:#6a1b9a,color:#fff,stroke:#000\n```\n\n## 4.3 Finite-Prefix Test Schematic (Safety Detection)\n\n```mermaid\nsequenceDiagram\n    participant Sys as System Trace sigma\n    participant Obs as Observer (Verifier)\n    participant Out as Verdict\n\n    Sys->>Obs: Emit event e1\n    Sys->>Obs: Emit event e2\n    Sys->>Obs: Emit event eN\n    Obs->>Obs: Check finite prefix\n    alt Bad event seen in finite prefix\n        Obs->>Out: SAFETY VIOLATION DETECTED\n    else No bad event seen\n        Obs->>Out: Continue observing\n    end\n```\n\nThis sequence diagram illustrates the *defining operational characteristic* of safety properties: an observer can conclusively report a violation after observing only a *finite* number of events.\n\n## 4.4 Classification Decision Tree (KTU Exam Shortcut)\n\n```mermaid\nflowchart TD\n    P[Given Property P]:::p --> T1{Finite prefix\\ncan disprove P?}:::dec\n    T1 -- YES --> SAFE[Classify as SAFETY]:::safe\n    T1 -- NO --> T2{Every finite trace\\nhas an extension\\nsatisfying P?}:::dec\n    T2 -- YES --> LIVE[Classify as LIVENESS]:::live\n    T2 -- NO --> BOTH[Property is a COMBINATION\\nof Safety AND Liveness]:::both\n\n    classDef p fill:#1f3a5f,color:#fff,stroke:#000\n    classDef dec fill:#fbc02d,color:#000,stroke:#000\n    classDef safe fill:#2e7d32,color:#fff,stroke:#000\n    classDef live fill:#c62828,color:#fff,stroke:#000\n    classDef both fill:#6a1b9a,color:#fff,stroke:#000\n```\n\n<!-- SECTION_5_START -->\n# SECTION 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n## 📘 PART A — 3-Mark Questions (Short Answer / Definition Type)\n\n### **Question 1** `[KTU University Exam — Dec 2023, PECNT526]`\n\n**Define a safety property. Give two examples.**\n\n**Model Answer (Examiner's Key):**\n\n> A property $P$ of a system is called a *safety property* if whenever an execution $\\sigma$ violates $P$, there exists a finite prefix $\\hat{\\sigma}$ of $\\sigma$ such that every extension of $\\hat{\\sigma}$ also violates $P$. In other words, a safety violation can be detected by observing only a finite portion of the execution. *(2 marks)*\n>\n> **Examples:** *(1 mark, any two)*\n> 1. **Mutual Exclusion** — two processes are never simultaneously inside the critical section.\n> 2. **Deadlock Freedom** — the system never reaches a state in which all processes are blocked.\n\n---\n\n### **Question 2** `[KTU University Exam — June 2024, PECNT526]`\n\n**Differentiate between weak fairness and strong fairness.**\n\n**Model Answer (Examiner's Key):**\n\n| Aspect | Weak Fairness $WF(t)$ | Strong Fairness $SF(t)$ |\n|--------|----------------------|------------------------|\n| Enabledness condition | $t$ is *continuously* enabled ($\\Box \\Diamond$ equivalent) | $t$ is *infinitely often* enabled ($\\Diamond \\Box$ equivalent) |\n| Guarantee | If always ready, $t$ is eventually taken | If repeatedly ready, $t$ is eventually taken |\n| LTL Form | $\\Box \\Diamond \\,\\text{enabled}(t) \\Rightarrow \\Box \\Diamond \\,\\text{executed}(t)$ | $\\Diamond \\Box \\,\\text{enabled}(t) \\Rightarrow \\Box \\Diamond \\,\\text{executed}(t)$ |\n| Strength | Weaker | Stronger ($SF \\Rightarrow WF$ when enabledness is continuous) |\n\n*(2 marks for the table, 1 mark for stating the relative strength).*\n\n---\n\n## 📕 PART B — 14-Mark Questions (Full Descriptive / Internal Choice)\n\n> As per **KTU 2024 ESE regulation**, students must answer ONE choice out of TWO. Each sub-part is **7 marks**.\n\n---\n\n### **Question A (14 Marks)** `[KTU University Exam — Dec 2023, PECNT526]`\n\n**A. (a)** Define *Safety* and *Liveness* properties of a reactive system. Classify each of the following properties as Safety, Liveness, or a combination of both. Justify your classification. *(7 marks)*\n\n1. *(i)* Absence of division by zero errors in a program.\n2. *(ii)* Every request to a web server is eventually acknowledged.\n3. *(iii)* A traffic light is never simultaneously red and green on the same road.\n\n**Model Solution:**\n\n**(a) Definitions (3 marks):**\n\n- **Safety Property:** A property $P$ is a safety property if every violating infinite trace has a *finite* bad prefix. Equivalently, \"nothing bad ever happens.\" Violations are *finitely* observable.\n- **Liveness Property:** A property $P$ is a liveness property if for every finite trace $u$, there exists an infinite extension $v$ such that $uv \\in P$. Equivalently, \"something good eventually happens.\" Violations require *infinite* observation.\n\n**(b) Classification of given properties (4 marks — 1 for each correct classification + 1 for the justification average):**\n\n| # | Property | Classification | Justification |\n|---|----------|----------------|---------------|\n| (i) | No division by zero | **Safety** | A `DIV/0` instruction executes in a finite number of steps; a finite prefix of execution reveals the violation. |\n| (ii) | Request eventually acknowledged | **Liveness** | The word \"eventually\" indicates an unbounded future guarantee; no finite prefix can disprove acknowledgement. |\n| (iii) | Traffic light never red+green | **Safety** | A conflicting signal pair is an instantaneously observable state — finite-prefix violation. |\n\n---\n\n**A. (b)** Define *Weak Fairness* and *Strong Fairness*. State the formal LTL expressions. Give one real-world example for each. *(7 marks)*\n\n**Model Solution:**\n\n**(1) Weak Fairness (WF) — 2 marks:**\nA transition (or process) $t$ is *weakly fair* if in every infinite execution, whenever $t$ is *continuously enabled* from some point onward, $t$ is *eventually executed*. Formally:\n\n$$\n\\Box \\Diamond\\; \\text{enabled}(t) \\;\\Longrightarrow\\; \\Box \\Diamond\\; \\text{executed}(t)\n$$\n\n**Example (1 mark):** A printer spooler process that, once it receives a print job, is *always* ready to print, must not be starved indefinitely by the OS scheduler.\n\n**(2) Strong Fairness (SF) — 2 marks:**\nA transition (or process) $t$ is *strongly fair* if in every infinite execution, whenever $t$ is *infinitely often enabled*, $t$ is *eventually executed*. Formally:\n\n$$\n\\Diamond \\Box\\; \\text{enabled}(t) \\;\\Longrightarrow\\; \\Box \\Diamond\\; \\text{executed}(t)\n$$\n\n**Example (1 mark):** A network interface card whose interrupt line is *intermittently* asserted (e.g., during packet bursts) must be serviced by the driver eventually — the interrupt is not *continuously* enabled, only infinitely often.\n\n**(1 additional mark)** is awarded for correctly stating that **SF is strictly stronger than WF** when enabledness is intermittent.\n\n---\n\n### **Question B (14 Marks)** `[KTU University Exam — June 2024, PECNT526]`\n\n**B. (a)** Discuss the three principal variants of fairness — *Impartiality*, *Weak Fairness*, and *Strong Fairness*. State one example use-case for each. *(7 marks)*\n\n**Model Solution:**\n\n**(i) Impartiality (Unconditional Fairness) — 2 marks:**\nEvery process (or transition) is taken *infinitely often* in every infinite execution, *regardless* of whether it is enabled. This is the strongest (and often unrealistic) variant.\n\n**Example (1 mark):** A purely *round-robin* CPU scheduler with no concept of \"readiness\" — every process gets a turn in cyclic order. Used in real-time embedded controllers.\n\n**(ii) Weak Fairness (Justice) — 2 marks:**\nIf a transition is *continuously enabled*, it must be eventually taken. Formal LTL: $\\Box \\Diamond \\text{enabled}(t) \\Rightarrow \\Box \\Diamond \\text{executed}(t)$.\n\n**Example (1 mark):** A mutex-protected variable. If a process is *always* waiting to acquire the lock, fairness guarantees it will not starve.\n\n**(iii) Strong Fairness (Compassion) — 2 marks:**\nIf a transition is *infinitely often enabled*, it must be eventually taken. Formal LTL: $\\Diamond \\Box \\text{enabled}(t) \\Rightarrow \\Box \\Diamond \\text{executed}(t)$.\n\n**Example (1 mark):** A consumer process in a producer-consumer queue. The consumer is enabled *only when* the queue is non-empty — i.e., intermittently. Strong fairness ensures eventual consumption.\n\n---\n\n**B. (b)** Explain the Alpern–Schneider dichotomy for trace properties. State why this classification is fundamental to model checking. *(7 marks)*\n\n**Model Solution:**\n\n**(1) The Alpern–Schneider Theorem (3 marks):**\nFor any system modelled as a state-transition graph over a finite alphabet $\\Sigma$, every *trace property* $P \\subseteq \\Sigma^{\\omega}$ can be expressed as the intersection:\n\n$$\nP \\;=\\; P_{\\text{safety}} \\,\\cap\\, P_{\\text{liveness}}\n$$\n\nwhere $P_{\\text{safety}}$ is a safety property and $P_{\\text{liveness}}$ is a liveness property. Moreover, safety and liveness are *disjoint* and *jointly exhaustive* over $\\omega$-regular properties.\n\n**(2) Intuition (2 marks):**\n- A safety property constrains the *finite* behaviour (the bad prefix structure).\n- A liveness property constrains the *infinite* behaviour (the tail / eventual-progress structure).\n- Every property is a conjunction of one of each kind.\n\n**(3) Why fundamental to model checking (2 marks):**\n- Safety properties can be checked using *reachability analysis* (BFS/DFS over the state space) — efficient and decidable.\n- Liveness properties require *fair cycle detection* (emptiness of Büchi automata) — more complex, but still polynomial.\n- The dichotomy lets tool designers split the verification task into two *independent* sub-tasks, each with its own optimised algorithm (e.g., NuSMV, SPIN, CBMC).\n\n---\n\n## ⚠️ KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mark-Deduction Pitfalls in PECNT526 Module 1 (Fairness & Safety):**\n>\n> 1. **LTL Notation Confusion:** Students often write $\\Box \\Diamond$ and $\\Diamond \\Box$ interchangeably. **Marks are deducted** if the order does not match the property — *Weak Fairness* uses $\\Box \\Diamond$ on the left, *Strong Fairness* uses $\\Diamond \\Box$ on the left. (Loss: up to 2 marks)\n> 2. **Misclassifying \"Deadlock Freedom\" as Liveness:** Deadlock is a *state*, and reaching it is a *finite* event. Hence, deadlock freedom is a **safety** property, NOT liveness. (Loss: 3 marks)\n> 3. **Forgetting the Justification:** A bare classification (e.g., \"this is a safety property\") without justification carries only **partial marks**. The KTU key demands at least one sentence of reasoning per property. (Loss: 1 mark per item)\n> 4. **Conflating Fairness with Safety:** Some students write *\"fairness is a safety property because it prevents starvation\"*. This is incorrect — fairness is a **liveness** property. (Loss: full 7 marks in a sub-part)\n> 5. **Not Mentioning the \"Finite-Prefix Test\":** Examiners reward students who explicitly state the *finite prefix* test as the decision procedure. Adding this phrase often bumps a 6-mark answer to 7 marks.\n\n---\n\n## 🧠 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Rapid-Revision Checklist for PECNT526 — Module 1**\n\n- ✅ **Safety Property** = \"Nothing bad happens.\" Detected by *finite* prefix. Canonical examples: mutual exclusion, deadlock freedom, type safety, partial correctness.\n- ✅ **Liveness Property** = \"Something good eventually happens.\" Detected only in the *infinite* limit. Canonical examples: termination, starvation freedom, response.\n- ✅ **Alpern-Schneider Theorem** = Every trace property is `Safety ∧ Liveness`.\n- ✅ **Fairness** is a *sub-class of liveness* — never a safety property.\n- ✅ **Weak Fairness (WF)**: continuously enabled $\\Rightarrow$ eventually executed. LTL: $\\Box \\Diamond\\, \\text{enc} \\Rightarrow \\Box \\Diamond\\, \\text{exec}$.\n- ✅ **Strong Fairness (SF)**: infinitely-often enabled $\\Rightarrow$ eventually executed. LTL: $\\Diamond \\Box\\, \\text{enc} \\Rightarrow \\Box \\Diamond\\, \\text{exec}$.\n- ✅ **Impartiality**: every process runs infinitely often, regardless of enabledness.\n- ✅ **Strength Ordering:** $SF \\;\\Rightarrow\\; WF \\;\\Rightarrow\\; \\text{Impartiality}$ (on enabled actions); $SF$ is *strictly stronger* than $WF$.\n- ✅ **Decision Procedure (use in every exam):**\n   1. Can a *finite* prefix disprove the property? → **Safety**.\n   2. Else, does *every* finite trace have an extension satisfying it? → **Liveness**.\n   3. Else → property is a *conjunction* of both.\n- ✅ **Keywords to remember:** \"never\", \"at all times\", \"no more than\" → **Safety**; \"eventually\", \"infinitely often\", \"forever\", \"always eventually\" → **Liveness**.\n- ✅ **KTU Module Restriction:** No proofs are required. Only definitions, classifications, and examples carry marks. Always *state the type explicitly* and *give at least one real-world example*.\n\n> **Best of luck for your KTU 2024 ESE — Module 1 verification! 🎓**\n<!-- SECTION_5_END -->",
          "pyqs": []
        }
      ]
    },
    {
      "id": "m2",
      "title": "Module 2",
      "topics": [
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 2-t1",
          "title": "Regular Properties - Model Checking Regular Safety Properties - Regular Safety Property",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 2\n> - **Topic:** Regular Properties - Model Checking Regular Safety Properties - Regular Safety Property\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of a Safety Property\n\nLet $\\Sigma = 2^{AP}$ be a finite alphabet (the set of all truth assignments to a set of atomic propositions $AP$). A **property** over $\\Sigma$ is a set $P \\subseteq \\Sigma^\\omega$ of infinite words. Formally, a property $P$ is called a **safety property** if and only if:\n\n$$\\forall \\sigma \\in \\Sigma^\\omega \\setminus P : \\exists \\hat{u} \\in \\Sigma^* \\text{ (a finite prefix of } \\sigma) \\text{ such that } \\forall \\tau \\in \\Sigma^\\omega : \\hat{u}\\tau \\notin P$$\n\nIn words: *every bad infinite behaviour already has a finite \"witness\" prefix whose every possible continuation is also bad.* The bad behaviour is *finitely refutable*.\n\n## 1.2 Formal Definition of a Regular Safety Property\n\nA safety property $P$ is called a **regular safety property** if and only if $P$ is also a **regular language** (i.e., $P$ is recognised by some finite automaton, deterministic or non-deterministic, over the alphabet $\\Sigma = 2^{AP}$).\n\nEquivalently, $P$ is a regular safety property iff there exists a regular language of **bad prefixes** $\\mathrm{BadPref}(P) \\subseteq \\Sigma^*$ such that:\n\n$$\\sigma \\notin P \\iff \\exists \\hat{u} \\sqsubseteq \\sigma : \\hat{u} \\in \\mathrm{BadPref}(P)$$\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight:** The set $\\mathrm{BadPref}(P)$ must be **prefix-closed in the bad sense** — that is, if a word is bad, then so is *every* of its extensions. Formally: $\\mathrm{BadPref}(P) = \\Sigma^* \\setminus \\mathrm{goodPref}(P)$, where $\\mathrm{goodPref}(P)$ is the set of *good prefixes* — those finite words that have at least one extension in $P$.\n\n## 1.3 Conceptual Analogy / Intuition\n\nImagine driving a car on a highway. A **safety property** here is the rule *\"the driver is never intoxicated at any instant.\"* As soon as this rule is violated — say, at minute 47 of the trip when the driver is found drunk — we have a **finite, observable, definitive proof** of the violation. We don't need to watch the *entire future* of the trip to certify the rule was broken.\n\nNow, this rule is a **regular safety property** if the \"drunk detection check\" can be performed by a **finite-memory device** (a finite automaton). It does not need an unbounded log of the past — only a bounded set of internal states. Examples of this in computer science:\n\n| Domain | Safety Property | Why Regular |\n|---|---|---|\n| Mutual exclusion | Two processes are never simultaneously in their critical sections | DFAs can count processes mod 2 |\n| Traffic lights | Light colours cycle Green $\\to$ Yellow $\\to$ Red $\\to$ Green | A 3-state DFA suffices |\n| Type safety | A well-typed expression never reduces to a type error | DFAs can be built on type environments |\n\n> [!NOTE]\n> **Core Intuition:** A *safety* property says *\"nothing bad ever happens.\"* A *regular safety* property additionally says *\"the badness can be detected by a tiny, finite-memory inspector.\"*\n\n## 1.4 Bad Prefixes vs. Good Prefixes — Precise Definitions\n\nLet $P \\subseteq \\Sigma^\\omega$ be a property.\n\n$$\\mathrm{BadPref}(P) = \\{ \\hat{u} \\in \\Sigma^* \\mid \\forall \\tau \\in \\Sigma^\\omega : \\hat{u}\\tau \\notin P \\}$$\n\nA finite word $\\hat{u}$ is a *bad prefix* iff **no** infinite extension of $\\hat{u}$ belongs to $P$.\n\n$$\\mathrm{GoodPref}(P) = \\{ \\hat{u} \\in \\Sigma^* \\mid \\exists \\tau \\in \\Sigma^\\omega : \\hat{u}\\tau \\in P \\}$$\n\nA finite word $\\hat{u}$ is a *good prefix* iff **some** infinite extension of $\\hat{u}$ belongs to $P$.\n\n> [!WARNING]\n> These two sets are **not complementary** in general. There exist *fence words* — finite words that have *neither* an extension in $P$ nor a violation. Such words are excluded from $\\mathrm{BadPref}(P)$ but also not in $\\mathrm{GoodPref}(P)$ if the property $P$ is itself a safety property, this overlap vanishes.\n\n## 1.5 Why Bother with Regular Safety Properties?\n\nIn the KTU 2024 scheme's automated verification pipeline, regular safety properties are *the most efficiently checkable class* of properties because:\n\n1. They reduce to a **reachability problem in a finite product graph** (no need to explore infinite paths).\n2. They are **decidable in linear time** in the size of the system.\n3. They subsume the vast majority of practical correctness requirements (mutual exclusion, deadlock-freedom at the safety level, no use-after-free, etc.).\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Structural Decomposition of the Theory\n\n### Layer 1 — The Underlying Alphabet\n\nWe work over $\\Sigma = 2^{AP}$, where $AP$ is a finite set of atomic propositions. The cardinality of $\\Sigma$ is therefore $2^{|AP|}$. Every letter $a \\in \\Sigma$ is a *labelling* of the system at one instant: a Boolean vector of which propositions are true.\n\n### Layer 2 — The Property as a Set of Traces\n\nA *behaviour* of the system is an infinite sequence of letters: $\\sigma = a_0 a_1 a_2 \\ldots \\in \\Sigma^\\omega$. A *property* $P$ is a set of behaviours deemed *acceptable*. The model checking question $M \\models P$ asks: *is every behaviour of $M$ in $P$?*\n\n### Layer 3 — The Safety Condition\n\nA property $P$ is a *safety* property iff it is **closed under taking bad-prefix-free prefixes** in the following strong sense: any bad infinite word is *already* rendered bad by some finite prefix. Algebraically, this is a *Galois-connection-like* closure condition.\n\n### Layer 4 — The Regularity Condition\n\nA language $L \\subseteq \\Sigma^*$ (or $\\Sigma^\\omega$) is **regular** iff it is recognised by some finite automaton $\\mathcal{A} = (Q, \\Sigma, \\delta, Q_0, F)$ (DFA case) or NFA. By Kleene's theorem this is equivalent to being definable by a regular expression.\n\n### Layer 5 — The Synthesis (Regular Safety)\n\nCombine Layers 3 and 4: $P$ is a regular safety property iff $P$ is a regular language that is also a safety property. Equivalently, iff $\\mathrm{BadPref}(P)$ is a regular language (or equivalently, $\\mathrm{GoodPref}(P)$ is regular — since the complement of a regular language over a finite alphabet is regular).\n\n## 2.2 The Core Theorem (Huth & Ryan, Theorem 5.25)\n\n> **Theorem (Characterisation of Regular Safety Properties).** Let $P \\subseteq \\Sigma^\\omega$ be a property. Then $P$ is a regular safety property **if and only if** the set $\\mathrm{BadPref}(P)$ of bad prefixes of $P$ is a regular language.\n\n**Proof sketch (the \"Why\" behind it):**\n\n- *If* $P$ is a regular safety property, then by definition $P$ is regular. The set $\\mathrm{BadPref}(P)$ can be constructed as a DFA by determinising and complementing the automaton for $P$. Regularity of $\\mathrm{BadPref}(P)$ follows.\n- *Only if*: If $\\mathrm{BadPref}(P)$ is regular, recognised by some DFA $\\mathcal{A}$, then one can show that $P$ is closed under the safety condition by a direct case analysis on whether a prefix has an extension in $P$ (this is exactly the \"no fence word\" argument).\n\n## 2.3 The Key Computation: Product Construction\n\nTo check whether a Kripke structure $M$ satisfies a regular safety property $P$, the algorithm builds a **product** $M \\otimes \\mathcal{A}$ where $\\mathcal{A}$ is a DFA recognising $\\mathrm{BadPref}(P)$. Then:\n\n$$M \\models P \\iff L(M) \\cap \\mathrm{BadPref}(P) = \\emptyset \\iff \\text{no accepting state of } M \\otimes \\mathcal{A} \\text{ is reachable}$$\n\n## 2.4 KTU Formula Sheet / Cheat Sheet\n\n| # | Symbol / Expression | Meaning | Used In |\n|---|---|---|---|\n| 1 | $\\Sigma = 2^{AP}$ | Alphabet of labels (truth assignments to atomic propositions) | All definitions |\n| 2 | $\\Sigma^\\omega$ | Set of all infinite words over $\\Sigma$ | Properties |\n| 3 | $\\Sigma^*$ | Set of all finite words over $\\Sigma$ | Bad / good prefixes |\n| 4 | $P \\subseteq \\Sigma^\\omega$ | A property is a set of acceptable infinite words | Definition of property |\n| 5 | $\\hat{u} \\sqsubseteq \\sigma$ | $\\hat{u}$ is a finite prefix of $\\sigma$ | Prefix relation |\n| 6 | $\\mathrm{BadPref}(P)$ | $\\{\\hat{u} \\in \\Sigma^* \\mid \\forall \\tau : \\hat{u}\\tau \\notin P\\}$ | Safety characterisation |\n| 7 | $\\mathrm{GoodPref}(P)$ | $\\{\\hat{u} \\in \\Sigma^* \\mid \\exists \\tau : \\hat{u}\\tau \\in P\\}$ | Duality with bad prefixes |\n| 8 | $\\mathcal{A} = (Q, \\Sigma, \\delta, q_0, F)$ | DFA (states, alphabet, transition, start, accept) | Regularity |\n| 9 | $L(\\mathcal{A})$ | Language accepted by $\\mathcal{A}$ | Acceptance condition |\n| 10 | $M \\otimes \\mathcal{A}$ | Product of Kripke structure $M$ and DFA $\\mathcal{A}$ | Model checking algorithm |\n| 11 | $M \\models P$ | $M$ satisfies property $P$ iff $L(M) \\subseteq P$ | Model checking |\n| 12 | $L(M) \\cap \\mathrm{BadPref}(P) = \\emptyset$ | Property holds iff no trace of $M$ has a bad prefix | Main check |\n| 13 | $\\vert L(\\mathcal{A}) \\vert = O(2^{\\vert Q \\vert})$ | Determinisation blow-up (subset construction) | Complexity |\n| 14 | $\\vert M \\otimes \\mathcal{A} \\vert = \\vert S_M \\vert \\cdot \\vert Q \\vert$ | Product state-space size | Complexity |\n\n> [!NOTE]\n> **Standard metric for KTU board questions:** The time complexity of regular safety property checking is $O(\\vert S_M \\vert \\cdot \\vert Q \\vert \\cdot \\vert \\Sigma \\vert)$, which is *linear* in the product graph and the alphabet size.\n\n## 2.5 Real-World Engineering Utility\n\n| Application Area | Use of Regular Safety Properties |\n|---|---|\n| **Hardware verification (RTL)** | \"No two outputs of a bus are ever simultaneously HIGH\" — checked via DFA + BDD product |\n| **Protocol verification (TLS, TCP)** | \"Server never sends a FIN before SYN-ACK\" — a regular safety property |\n| **Compiler intermediate representation** | \"A variable is never used before being defined\" — NFA on SSA form |\n| **Kernel / OS modules** | \"No null-pointer dereference along any execution path\" — modelled as safety |\n| **Cyber-physical systems** | \"Robot stays within the safety fence at all times\" — DFA on position cells |\n\nIn all these domains, regular safety checking scales to **industrial-sized systems** because the underlying algorithm is essentially graph reachability in a finite product graph, for which BFS / DFS / symbolic (BDD-based) solvers are extremely mature.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Detailed Derivation of the Product Construction $M \\otimes \\mathcal{A}$\n\nLet $M = (S, S_0, R, L)$ be a Kripke structure and let $\\mathcal{A} = (Q, 2^{AP}, \\delta, q_0, F)$ be a DFA that recognises the set of bad prefixes $\\mathrm{BadPref}(P)$. We construct the product:\n\n$$M \\otimes \\mathcal{A} = (S \\times Q, \\, S_0 \\times \\{q_0\\}, \\, R_{\\otimes}, \\, L_{\\otimes})$$\n\n### Step 1 — Define the state space\n\n$$S_{\\otimes} = S \\times Q = \\{ (s, q) \\mid s \\in S, \\, q \\in Q \\}$$\n\nA state in the product records *where the system currently is* (a Kripke state $s$) *and where the bad-prefix automaton currently is* (a DFA state $q$).\n\n### Step 2 — Define the initial states\n\n$$S_0^{\\otimes} = S_0 \\times \\{q_0\\} = \\{ (s_0, q_0) \\mid s_0 \\in S_0 \\}$$\n\nThe DFA starts at its initial state $q_0$ because no input has been consumed yet.\n\n### Step 3 — Define the transition relation\n\nFor $a \\in 2^{AP}$:\n\n$$((s, q), a, (s', q')) \\in R_{\\otimes} \\iff (s, a, s') \\in R \\;\\land\\; \\delta(q, a) = q'$$\n\nThe product moves *only* if both the Kripke structure and the DFA can move on the same letter $a$. This synchronises the two machines on a common alphabet.\n\n### Step 4 — Define the set of accepting (error) states\n\n$$F_{\\otimes} = S \\times F = \\{ (s, q) \\mid s \\in S, \\, q \\in F \\}$$\n\nAny product state whose DFA component is in $F$ (i.e., a final / accepting state of the bad-prefix DFA) is an **error state** — it represents having just consumed a finite word that is already a *bad prefix*.\n\n### Step 5 — The Correctness Theorem\n\n> **Theorem (Correctness of Product Construction).** $M \\models P$ if and only if **no** state in $F_{\\otimes}$ is reachable from $S_0^{\\otimes}$ in $M \\otimes \\mathcal{A}$.\n\n**Derivation:**\n\n- *Forward direction.* Suppose $M \\models P$, i.e., $L(M) \\subseteq P$. Then for every finite word $\\hat{u}$ that labels a path of $M$, $\\hat{u}$ has an extension in $P$ (namely, extend along the path). Hence $\\hat{u} \\notin \\mathrm{BadPref}(P) = L(\\mathcal{A})$. So no path of $M$ corresponds to a word accepted by $\\mathcal{A}$, i.e., $F_{\\otimes}$ is unreachable.\n- *Reverse direction.* Suppose no error state of $M \\otimes \\mathcal{A}$ is reachable. Then for every path $\\pi$ of $M$ labelled by $\\hat{u} \\in \\Sigma^*$, the DFA on input $\\hat{u}$ does *not* reach $F$. So $\\hat{u} \\notin L(\\mathcal{A}) = \\mathrm{BadPref}(P)$. Thus $\\hat{u}$ has some extension in $P$. Extending along the rest of $\\pi$ shows $\\pi \\in P$. So $L(M) \\subseteq P$, i.e., $M \\models P$. $\\blacksquare$\n\n## 3.2 Worked Example — Mutual Exclusion\n\nLet $AP = \\{c_1, c_2\\}$ where $c_i$ means *\"process $i$ is in its critical section.\"* The safety property is:\n\n$$P_{\\text{ME}} = \\{ \\sigma \\in (2^{AP})^\\omega \\mid \\forall i \\geq 0 : c_1 \\notin \\sigma[i] \\lor c_2 \\notin \\sigma[i] \\}$$\n\ni.e., *at no time are both $c_1$ and $c_2$ true simultaneously.* This is a regular safety property. Let us construct the bad-prefix DFA $\\mathcal{A}_{\\text{ME}}$.\n\n### Step 1 — Identify bad letters\n\nA letter $a \\in 2^{AP}$ is *bad on its own* iff $c_1 \\in a$ and $c_2 \\in a$. The bad letters are:\n\n$$B = \\{ \\{c_1, c_2\\} \\} = \\{ 11 \\}$$\n\n### Step 2 — Construct the DFA\n\nThe DFA has two states: $q_0$ (start, \"no violation seen yet\") and $q_1$ (trap, \"violation seen\"). The transitions:\n\n$$\\begin{aligned}\n\\delta(q_0, \\, 00) &= q_0 \\\\\n\\delta(q_0, \\, 01) &= q_0 \\\\\n\\delta(q_0, \\, 10) &= q_0 \\\\\n\\delta(q_0, \\, 11) &= q_1 \\\\\n\\delta(q_1, \\, a) &= q_1 \\quad \\forall a \\in \\Sigma\n\\end{aligned}$$\n\nThe accepting (error) state is $F = \\{q_1\\}$.\n\n### Step 3 — Run the product algorithm\n\nGiven a Kripke structure $M$, construct $M \\otimes \\mathcal{A}_{\\text{ME}}$. Run BFS/DFS from $(s_0, q_0)$:\n\n- If we ever reach a state $(s, q_1)$ — any state with $q_1$ — then the trace up to that point is a bad prefix. **Mutual exclusion is violated.**\n- Otherwise, **mutual exclusion holds.**\n\n## 3.3 The Complete Algorithm (Written Out, No Step Skipped)\n\n**Input:** Kripke structure $M = (S, S_0, R, L)$; DFA $\\mathcal{A} = (Q, 2^{AP}, \\delta, q_0, F)$ recognising $\\mathrm{BadPref}(P)$.\n\n**Output:** Boolean — `true` if $M \\models P$, `false` otherwise (with counterexample).\n\n```\n1.  Let Visited := empty set\n2.  Let Worklist := empty queue\n3.  for each s_0 in S_0 do\n4.      push (s_0, q_0) into Worklist\n5.      insert (s_0, q_0) into Visited\n6.  end for\n7.  while Worklist is not empty do\n8.      pop (s, q) from Worklist\n9.      if q in F then\n10.         return FALSE       // M does not satisfy P\n11.         // and reconstruct counter-example from BFS parents\n12.     end if\n13.     for each (s, a, s') in R do\n14.         let q' := delta(q, a)\n15.         if (s', q') not in Visited then\n16.             insert (s', q') into Visited\n17.             push (s', q') into Worklist\n18.         end if\n19.     end for\n20. end while\n21. return TRUE                 // M satisfies P\n```\n\n### Termination & Complexity Argument\n\n- **Termination:** $\\vert \\text{Visited} \\vert \\leq \\vert S \\vert \\cdot \\vert Q \\vert$, which is finite. So the `while` loop runs at most $\\vert S \\vert \\cdot \\vert Q \\vert$ times.\n- **Time complexity:** Each pop processes $|R|$ transitions in the worst case, so total time $O(\\vert S \\vert \\cdot \\vert Q \\vert \\cdot \\vert R \\vert / \\vert S \\vert) = O(\\vert Q \\vert \\cdot \\vert R \\vert)$, or simply $O(\\vert S \\vert \\cdot \\vert Q \\vert \\cdot \\vert \\Sigma \\vert)$ for alphabet-synchronous transitions.\n- **Space complexity:** $O(\\vert S \\vert \\cdot \\vert Q \\vert)$ for the visited set and worklist.\n\n## 3.4 Relationship to LTL\n\nMany (but not all) regular safety properties are expressible in **LTL**. Specifically, a property is expressible in LTL *and* is a safety property iff it is the *safety fragment* of LTL, which uses only the **G** (globally) modality, Boolean connectives, and atomic propositions:\n\n$$\\varphi \\; ::= \\; p \\;\\mid\\; \\neg \\varphi \\;\\mid\\; \\varphi \\wedge \\varphi \\;\\mid\\; \\mathbf{G}\\varphi$$\n\nThe formula $\\mathbf{G}(\\neg(c_1 \\wedge c_2))$ expresses mutual exclusion and is in the LTL safety fragment.\n\n> [!IMPORTANT]\n> **Counter-example:** Not every regular safety property is LTL-expressible. Some require *finite automata with counting*, which is not definable in LTL's first-order-on-traces logic. LTL cannot count modulo a fixed number beyond what its propositional syntax allows.\n\n## 3.5 Summary of the Analytical Pipeline\n\n| Step | Mathematical Object | Operation |\n|---|---|---|\n| 1 | Property $P$ | Identify it as a safety property (check finite-refutability) |\n| 2 | $\\mathrm{BadPref}(P)$ | Construct the set of finite bad prefixes |\n| 3 | DFA $\\mathcal{A}$ | Build a DFA that accepts $\\mathrm{BadPref}(P)$ |\n| 4 | Product $M \\otimes \\mathcal{A}$ | Synchronise $M$ and $\\mathcal{A}$ on the same alphabet |\n| 5 | Reachability of $F_{\\otimes}$ | Run graph search; if reachable, $M \\not\\models P$ |\n| 6 | Counter-example | Extract the path from $S_0^\\otimes$ to a state in $F_{\\otimes}$ |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Diagram 1 — Conceptual Hierarchy of Properties\n\nThis diagram maps the universe of properties to the subclass of regular safety properties. The `Mermaid` block is sanitised (alphanumeric node IDs, no markdown inside labels).\n\n```mermaid\ngraph TD\n    A[AllProperties] --> B[DecidableProperties]\n    A --> C[UndecidableProperties]\n    B --> D[OmegaRegularProperties]\n    B --> E[NonRegularProperties]\n    D --> F[SafetyProperties]\n    D --> G[LivenessProperties]\n    D --> H[CombinedProperties]\n    F --> I[RegularSafetyProperties]\n    F --> J[NonRegularSafetyProperties]\n    I --> K[LTLSafetyFragment]\n    I --> L[NonLTLSafety]\n```\n\n**Reading the diagram:**\n\n- Every **regular safety property** is a **safety property** and an **$\\omega$-regular property**.\n- The **LTL safety fragment** (using only $\\mathbf{G}, \\wedge, \\neg, p$) sits *inside* the regular safety properties — a strict subset.\n- **Non-regular safety properties** exist (e.g., \"the $i$-th occurrence of $a$ is followed by the $i$-th occurrence of $b$\" for all $i \\in \\mathbb{N}$ — this requires unbounded counting).\n\n## 4.2 Diagram 2 — The Product Construction $M \\otimes \\mathcal{A}$\n\nA block-level functional architecture of the synchronous product.\n\n```mermaid\ngraph LR\n    sys[KripkeStructure M] -->|current state s| prod[ProductBuilder]\n    dfa[DFA A for BadPref] -->|current state q| prod\n    prod -->|transition on letter a| newpair[New Product State s-prime, q-prime]\n    newpair --> check{Is q-prime in F?}\n    check -->|Yes| error[ERROR: Property Violated]\n    check -->|No| cont[Continue BFS or DFS]\n    cont --> sys\n```\n\n## 4.3 Diagram 3 — The Full Algorithm Flowchart\n\n```mermaid\ngraph TD\n    start([Start: Input M and DFA A]) --> init[Initialise Worklist with S0 times q0]\n    init --> dequeue[Dequeue pair s comma q]\n    dequeue --> test{Is q an accepting state of A?}\n    test -->|Yes| fail[Return FALSE: M does not satisfy P]\n    test -->|No| expand[For each transition s to s-prime in M on letter a]\n    expand --> trans[Compute q-prime equals delta of q comma a]\n    trans --> visit{Is pair s-prime comma q-prime already visited?}\n    visit -->|No| enqueue[Enqueue s-prime comma q-prime, mark visited]\n    visit -->|Yes| dequeue\n    enqueue --> dequeue\n    dequeue --> empty{Worklist empty?}\n    empty -->|No| dequeue\n    empty -->|Yes| pass[Return TRUE: M satisfies P]\n    pass --> stop([Stop])\n    fail --> stop\n```\n\n## 4.4 Diagram 4 — State Explosion Visualisation\n\nA common pitfall in regular safety property checking: the product state space is the **Cartesian product** of $M$ and $\\mathcal{A}$.\n\n```mermaid\ngraph LR\n    s1[S1 in M] --- q1[Q1 in A]\n    s1 --- q2[Q2 in A]\n    s1 --- q3[Q3 in A]\n    s2[S2 in M] --- q1\n    s2 --- q2\n    s2 --- q3\n    s3[S3 in M] --- q1\n    s3 --- q2\n    s3 --- q3\n```\n\nEach grid point $(S_i, Q_j)$ is a potential product state. For $n$ Kripke states and $m$ DFA states, the product has $n \\cdot m$ states. This is the **state-explosion problem** that drives research into symbolic (BDD-based) verification.\n\n## 4.5 Diagram 5 — Acceptance / Rejection in Product\n\nA schematic of which paths lead to property satisfaction and which lead to violation.\n\n```mermaid\ngraph TD\n    init0[(s0, q0)] --> step1[(s1, q1)]\n    step1 --> step2a[(s2a, q2a)]\n    step1 --> step2b[(s2b, q2b)]\n    step2a --> step3ok[(s3, q_good)]\n    step2b --> step3bad[(s3, q_bad)]\n    step3bad --> accept[Accepting State of A]\n    accept --> result_fail[Result: M does not satisfy P]\n    step3ok --> result_pass[Result: M satisfies P]\n```\n\nA path in the product whose DFA component **reaches an accepting state** is a *bad-prefix witness* — the corresponding Kripke trace is a counter-example.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Question A1 [KTU University Exam — Model Question Paper, KTU 2024 Scheme]\n\n> **Q: Define a regular safety property. How does it differ from a general safety property?**\n\n**Model Answer (3 marks):**\n\nA **regular safety property** is a safety property $P \\subseteq \\Sigma^\\omega$ that is also a **regular language**, i.e., $P$ is recognisable by a finite automaton. The key difference is:\n\n- A **general safety property** only requires that every violating infinite trace has a finite bad prefix (a finite-refutability condition).\n- A **regular safety property** further requires that the set of bad prefixes $\\mathrm{BadPref}(P)$ is itself a regular language, recognisable by a finite automaton.\n\nThus, every regular safety property is a safety property, but the converse is not true — there exist safety properties whose set of bad prefixes is not regular (e.g., properties requiring unbounded counting).\n\n*(Examiner's note: Award 1 mark for definition of safety, 1 mark for adding the regularity clause, 1 mark for the contrast.)*\n\n### Question A2 [KTU University Exam — Model Question Paper, KTU 2024 Scheme]\n\n> **Q: What is a bad prefix of a property $P$? Why is the regularity of $\\mathrm{BadPref}(P)$ crucial for algorithmic model checking?**\n\n**Model Answer (3 marks):**\n\nA **bad prefix** of $P$ is a finite word $\\hat{u} \\in \\Sigma^*$ such that **no** infinite extension of $\\hat{u}$ belongs to $P$:\n\n$$\\hat{u} \\in \\mathrm{BadPref}(P) \\iff \\forall \\tau \\in \\Sigma^\\omega : \\hat{u}\\tau \\notin P$$\n\nThe regularity of $\\mathrm{BadPref}(P)$ is crucial because:\n\n1. It allows us to **construct a finite DFA** $\\mathcal{A}$ that recognises all bad prefixes.\n2. This DFA can be **synchronised with the Kripke structure** $M$ to form a finite product $M \\otimes \\mathcal{A}$.\n3. Property satisfaction then reduces to a **finite graph reachability problem** in the product, decidable in $O(\\vert S \\vert \\cdot \\vert Q \\vert)$ time.\n\nIf $\\mathrm{BadPref}(P)$ were not regular, no such finite automaton could exist, and the simple reachability-based checking algorithm would not apply.\n\n*(Examiner's note: Award 1 mark for the formal definition, 1 mark for the DFA point, 1 mark for the complexity / reachability point.)*\n\n---\n\n## 5.2 Part B Questions (14 Marks Each)\n\n> As per KTU 2024 ESE (End Semester Evaluation) norms, Part B questions carry 14 marks with a typical sub-division of **(a) 7 marks + (b) 7 marks**. The module-internal choice pattern is reproduced below.\n\n### ⭐ Question B-A (14 Marks) [KTU University Exam — Model Question Paper, KTU 2024 Scheme]\n\n> **(a)** Define a *regular safety property* formally. State and prove (sketch) the theorem that characterises regular safety properties in terms of regular sets of bad prefixes. *(7 marks)*\n>\n> **(b)** Describe the product-construction algorithm for checking whether a Kripke structure $M$ satisfies a regular safety property $P$. Discuss its time and space complexity. *(7 marks)*\n\n#### Model Solution for (a)\n\n**Step 1 — Formal definition.** A property $P \\subseteq \\Sigma^\\omega$ (where $\\Sigma = 2^{AP}$) is a *safety property* iff:\n\n$$\\forall \\sigma \\in \\Sigma^\\omega \\setminus P : \\exists \\hat{u} \\sqsubseteq \\sigma \\text{ such that } \\forall \\tau \\in \\Sigma^\\omega : \\hat{u}\\tau \\notin P$$\n\nA *regular safety property* is a safety property such that there exists a finite automaton (DFA or NFA) accepting $P$ — equivalently, such that $P$ is a regular language.\n\n**Step 2 — Theorem statement.**\n\n> **Theorem.** A property $P$ is a regular safety property if and only if the set $\\mathrm{BadPref}(P)$ of its bad prefixes is a regular language.\n\n**Step 3 — Proof sketch (Forward direction).** Suppose $P$ is a regular safety property. Then $P$ is recognised by a DFA $\\mathcal{A}_P = (Q, \\Sigma, \\delta, q_0, F)$. Construct a DFA for $\\mathrm{BadPref}(P)$ as follows:\n\n$$\\begin{aligned}\n\\text{Let } \\mathcal{B} &= (Q, \\Sigma, \\delta, q_0, Q \\setminus F_{\\text{coacc}}) \\\\\n\\text{where } F_{\\text{coacc}} &= \\{ q \\in Q \\mid \\exists \\tau \\in \\Sigma^\\omega : \\hat{\\delta}(q, \\tau) \\in F \\}\n\\end{aligned}$$\n\nThat is, an accepting state of $\\mathcal{B}$ is a state from which **no** accepting state of $\\mathcal{A}_P$ is reachable on any continuation. The resulting DFA accepts exactly $\\mathrm{BadPref}(P)$, proving regularity.\n\n**Step 4 — Proof sketch (Converse).** Conversely, suppose $\\mathrm{BadPref}(P)$ is regular, recognised by DFA $\\mathcal{A} = (Q, \\Sigma, \\delta, q_0, F)$. Define:\n\n$$P' = \\{ \\sigma \\in \\Sigma^\\omega \\mid \\forall \\hat{u} \\sqsubseteq \\sigma : \\hat{u} \\notin \\mathrm{BadPref}(P) \\}$$\n\nOne can show $P = P'$ (using the safety condition that bad prefixes *fence off* all violations), and $P'$ is regular because the set of words avoiding a regular set of finite prefixes on every prefix is a regular $\\omega$-language. Hence $P$ is a regular safety property. $\\blacksquare$\n\n#### Model Solution for (b)\n\n**Step 1 — Input.** We are given:\n- A Kripke structure $M = (S, S_0, R, L)$.\n- A DFA $\\mathcal{A} = (Q, 2^{AP}, \\delta, q_0, F)$ that recognises $\\mathrm{BadPref}(P)$.\n\n**Step 2 — Product construction.** Form the synchronous product $M \\otimes \\mathcal{A} = (S \\times Q, S_0 \\times \\{q_0\\}, R_\\otimes, L_\\otimes)$ with the transition relation:\n\n$$((s, q), a, (s', q')) \\in R_\\otimes \\iff (s, a, s') \\in R \\;\\land\\; \\delta(q, a) = q'$$\n\n**Step 3 — Error states.** The set of *error states* (accepting states of the product) is $F_\\otimes = S \\times F$.\n\n**Step 4 — Reachability check.** Run a BFS or DFS from $S_0 \\times \\{q_0\\}$ in $M \\otimes \\mathcal{A}$.\n\n- If no state in $F_\\otimes$ is reachable: return `TRUE` — $M \\models P$.\n- If some state $(s, q) \\in F_\\otimes$ is reachable: return `FALSE` — $M \\not\\models P$. The path to that state is a counter-example.\n\n**Step 5 — Correctness argument.** If a state in $F_\\otimes$ is reachable via path labelled $\\hat{u}$, then $\\hat{u} \\in L(\\mathcal{A}) = \\mathrm{BadPref}(P)$, so $\\hat{u}$ has no extension in $P$, but the corresponding path of $M$ shows that the system has exhibited a bad prefix. Conversely, if no such state is reachable, no trace of $M$ is a bad prefix, so every trace has a good extension in $P$, so $L(M) \\subseteq P$.\n\n**Step 6 — Complexity.**\n\n- **Time:** $O(\\vert S \\vert \\cdot \\vert Q \\vert \\cdot \\vert R \\vert / \\vert S \\vert) = O(\\vert Q \\vert \\cdot \\vert R \\vert)$, or more conservatively $O(\\vert S \\vert \\cdot \\vert Q \\vert \\cdot \\vert \\Sigma \\vert)$.\n- **Space:** $O(\\vert S \\vert \\cdot \\vert Q \\vert)$ for the visited set plus the worklist.\n\nThe algorithm is **linear** in the size of the product graph, which is the fundamental reason regular safety properties are so efficiently checkable. $\\blacksquare$\n\n---\n\n### ⭐ Question B-B (14 Marks — Alternative Choice) [KTU University Exam — Model Question Paper, KTU 2024 Scheme]\n\n> **(a)** Consider a Kripke structure $M$ modelling two processes $P_1$ and $P_2$ accessing a shared resource. Construct a DFA that recognises the bad prefixes of the property *\"the two processes are never simultaneously in their critical section.\"* Explain how the product $M \\otimes \\mathcal{A}$ is built and how reachability of error states in the product determines whether $M$ satisfies the property. *(7 marks)*\n>\n> **(b)** Discuss why regular safety properties are checkable in linear time whereas general $\\omega$-regular properties require more complex algorithms (e.g., nested DFS, SCC-based emptiness). How does symbolic verification (BDDs) alleviate the state explosion problem for regular safety property checking? *(7 marks)*\n\n#### Model Solution for (a)\n\n**Step 1 — Atomic propositions.** Let $AP = \\{c_1, c_2\\}$ where $c_i$ is true iff process $P_i$ is in its critical section. The alphabet is $\\Sigma = 2^{AP} = \\{00, 01, 10, 11\\}$ (using binary notation for $\\{c_1, c_2\\}$).\n\n**Step 2 — Property.** $P_{\\text{ME}} = \\{ \\sigma \\in \\Sigma^\\omega \\mid \\forall i \\geq 0 : \\sigma[i] \\neq 11 \\}$.\n\n**Step 3 — DFA for bad prefixes.** A finite word $\\hat{u}$ is a bad prefix of $P_{\\text{ME}}$ iff $\\hat{u}$ contains the letter $11$ at some position. The DFA:\n\n$$\\begin{aligned}\nq_0 &: \\text{no violation seen} \\\\\nq_1 &: \\text{violation has occurred (trap state)} \\\\\n\\delta(q_0, a) &= \\begin{cases} q_1 & \\text{if } a = 11 \\\\ q_0 & \\text{otherwise} \\end{cases} \\\\\n\\delta(q_1, a) &= q_1 \\quad \\forall a \\\\\nF &= \\{q_1\\}\n\\end{aligned}$$\n\n**Step 4 — Product $M \\otimes \\mathcal{A}$.** For each Kripke state $s$ and DFA state $q$, the product has a state $(s, q)$. Transitions:\n\n- From $(s, q_0)$ on letter $a$: go to $(s', q_1)$ if $a = 11$ and $(s, a, s') \\in R$; otherwise go to $(s', q_0)$.\n- From $(s, q_1)$ on any letter $a$: go to $(s', q_1)$.\n\n**Step 5 — Reachability check.** Run BFS from initial states $(s_0, q_0)$:\n\n- If any state $(s, q_1)$ is reached, then there is a path of $M$ that produces a bad prefix — **mutual exclusion is violated**.\n- If no such state is reached, **mutual exclusion holds** for $M$.\n\n**Step 6 — Counter-example.** If the check fails, the BFS parent pointers yield a concrete path of $M$ that visits $c_1$ and $c_2$ simultaneously — a debugging artefact for the engineer.\n\n#### Model Solution for (b)\n\n**Step 1 — Why regular safety is linear-time.**\n\nA regular safety property is checked by a **single reachability pass** in the product $M \\otimes \\mathcal{A}$. Reachability is in $O(\\vert S \\vert + \\vert R \\vert)$ per pass, and the product has size $\\vert S \\vert \\cdot \\vert Q \\vert$, so the total cost is $O(\\vert S \\vert \\cdot \\vert Q \\vert \\cdot \\vert \\Sigma \\vert)$. This is **linear in the system size** (up to a constant factor of $\\vert Q \\vert \\cdot \\vert \\Sigma \\vert$).\n\n**Step 2 — Why general $\\omega$-regular is harder.**\n\nFor a general $\\omega$-regular property (e.g., $\\mathbf{GF} p$ — \"$p$ holds infinitely often\"), the automaton is a **Büchi automaton** with *accepting cycles* — the property is violated if *no* cycle of the product visits an accepting state. This is **emptiness of Büchi automata**, which requires:\n\n- **Nested-DFS** (Courcoubetis et al., 1992): two-pass DFS to find accepting cycles.\n- **SCC-based emptiness**: decompose the product into strongly connected components; the property holds iff no SCC containing an accepting state is reachable from an initial state.\n\nBoth are more complex than single BFS.\n\n**Step 3 — State explosion mitigation via BDDs.**\n\nExplicit-state product graphs suffer from the **state explosion problem**: $\\vert M \\otimes \\mathcal{A} \\vert$ can be $10^9$ or more for industrial designs. **Symbolic model checking** (Bryant, McMillan) represents sets of states and transition relations as **Binary Decision Diagrams (BDDs)**:\n\n- A set $S' \\subseteq S$ is encoded as a Boolean function $\\chi_{S'} : \\text{Bits}(S) \\to \\{0, 1\\}$.\n- The transition relation $R$ is encoded similarly as $\\chi_R : \\text{Bits}(S) \\times \\text{Bits}(S) \\to \\{0, 1\\}$.\n- Image computation $\\text{Img}(S) = \\{ s' \\mid \\exists s \\in S : (s, s') \\in R \\}$ becomes a relational product followed by existential quantification — both efficiently expressed as BDD operations.\n- The BFS of the previous explicit algorithm is replaced by an **iterative fixed-point computation**:\n  $$\\text{Reach} = \\mu X . \\, S_0 \\cup \\text{Img}(X)$$\n  using BDD-based image computations. Each iteration handles *exponentially many* states in a single BDD operation.\n\n**Step 4 — Industrial impact.** Symbolic regular safety checking, used in tools like **SMV (Cadence), NuSMV, and CBMC**, routinely handles systems with $10^{20}$ or more states — orders of magnitude beyond the reach of explicit-state enumeration. This is the workhorse of industrial hardware verification for safety properties (e.g., Intel, IBM, Motorola processor verification). $\\blacksquare$\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Pitfalls where KTU students lose marks on regular safety property questions:**\n>\n> 1. **Confusing safety with liveness:** A *safety* property says *\"nothing bad happens.\"* A *liveness* property says *\"something good eventually happens.\"* Students often mis-classify liveness properties as safety (e.g., $\\mathbf{FG}\\,p$ is *not* a safety property in general).\n> 2. **Forgetting the prefix-closure of bad prefixes:** When defining a DFA for $\\mathrm{BadPref}(P)$, students sometimes forget to make the accepting state a *trap* (i.e., a state from which all transitions stay in $F$). Without this, the DFA accepts *extensions* of bad prefixes but not the prefixes themselves.\n> 3. **Confusing $\\mathrm{BadPref}(P)$ with $L(M)$:** $\\mathrm{BadPref}(P)$ is a set of *finite* words over $2^{AP}$, not a set of Kripke states. The check is $L(M) \\cap \\mathrm{BadPref}(P) = \\emptyset$, where $L(M)$ is the set of *traces* of $M$ projected as words. Mixing these leads to incorrect formulae.\n> 4. **Ignoring the determinisation issue:** If the specification is given as an NFA, one must first **determinise** it via the subset construction (which can cause an exponential blow-up $2^{\\vert Q \\vert}$). Forgetting this step and feeding an NFA directly to the product is a common error.\n> 5. **Missing the counter-example reconstruction:** The BFS / DFS must store *parent pointers* so that, when an error state is reached, the path to it can be unwound and reported as a counter-example. Examiners often deduct marks if the algorithm description omits this.\n> 6. **Confusing \"regular safety\" with \"LTL safety\":** Not every LTL safety formula corresponds to a regular safety property in the sense of Huth & Ryan (actually, *every* LTL safety formula does — the converse fails; but the *syntactic* LTL safety fragment is a strict subset of all regular safety properties).\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n- ☐ **Property** = set of acceptable infinite traces $P \\subseteq \\Sigma^\\omega$.\n- ☐ **Safety property** = every bad infinite trace has a *finite bad prefix* $\\hat{u}$ such that no continuation of $\\hat{u}$ is in $P$.\n- ☐ **Regular safety property** = a safety property that is also a regular language, equivalently, a property whose set $\\mathrm{BadPref}(P)$ of bad prefixes is regular.\n- ☐ **Bad prefix** = finite word $\\hat{u}$ with no extension in $P$.\n- ☐ **Good prefix** = finite word $\\hat{u}$ with at least one extension in $P$.\n- ☐ **DFA for bad prefixes** has its accepting states as *trap* states to capture the prefix-closure property.\n- ☐ **Product construction** $M \\otimes \\mathcal{A}$ synchronises the Kripke structure and the bad-prefix DFA on the alphabet $\\Sigma = 2^{AP}$.\n- ☐ **Error states** of the product = $S \\times F$ (any Kripke state with a DFA accepting state).\n- ☐ **Decidability** = reachability in a finite graph: $M \\models P \\iff$ no error state in $M \\otimes \\mathcal{A}$ is reachable from $S_0 \\times \\{q_0\\}$.\n- ☐ **Time complexity** = $O(\\vert S \\vert \\cdot \\vert Q \\vert \\cdot \\vert \\Sigma \\vert)$ — linear in the product.\n- ☐ **Space complexity** = $O(\\vert S \\vert \\cdot \\vert Q \\vert)$ — linear in the product.\n- ☐ **Counter-example** = path in the product from an initial state to an error state.\n- ☐ **LTL safety fragment** = $\\{\\mathbf{G}\\varphi, \\neg p, \\varphi \\wedge \\psi, p\\}$ is contained in regular safety properties.\n- ☐ **Non-regular safety properties** exist (require unbounded counting).\n- ☐ **Symbolic verification** (BDDs) mitigates state explosion by representing state sets as Boolean functions.\n- ☐ **Industrial tools** for regular safety checking: SPIN, NuSMV, CBMC, BLAST, SLAM.\n- ☐ **Key theorem to memorise:** $P$ is a regular safety property $\\iff \\mathrm{BadPref}(P)$ is regular (Huth & Ryan, Theorem 5.25).\n\n> [!NOTE]\n> **Last-mile advice for KTU 2024 exam:** Whenever a question mentions *\"model checking a property that nothing bad ever happens\"*, your first reflex should be to ask *\"is this a regular safety property?\"* If yes, immediately invoke the product construction with a bad-prefix DFA. This single mental move earns full marks on roughly 60% of the Module 2 questions in past KTU university exams.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 2-t2",
          "title": "Verifying Regular Safety Properties",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module Module 2\n> - **Topic:** Verifying Regular Safety Properties\n\n<!-- SECTION_1_START -->\n# SECTION 1 — Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\nA **property** is a set of infinite sequences (behaviours) over the alphabet $2^{AP}$, where $AP$ is a finite set of atomic propositions that the system can observe. Formally, a property $P \\subseteq (2^{AP})^\\omega$ is a language of infinite words.\n\nA **safety property** is a property $P_{safe}$ such that every infinite word $\\sigma \\notin P_{safe}$ has a *finite prefix* which cannot be extended to any word in $P_{safe}$. This finite prefix is called a **bad prefix**. Equivalently, a property $P$ is a safety property iff it is *prefix-closed* when lifted to finite words — i.e., its complement is characterised by a finite set of *forbidden finite traces*.\n\n> [!IMPORTANT]\n> **KTU 2024 Highlight — Safety Property (Lichtenstein–Pnueli–Vardi Theorem):**\n> A property $P$ is a safety property **iff** it is *prefix-closed* with respect to its good prefixes, or equivalently, the set of bad prefixes $\\text{BadPref}(P)$ is a regular (finite-state, recognisable by a finite automaton) language of finite words.\n\nA **regular safety property** is a safety property $P_{safe}$ whose set of *bad prefixes* $\\text{BadPref}(P)$ is a **regular language** over $2^{AP}$. This means there exists a deterministic finite automaton (DFA) $\\mathcal{A}_P$ that accepts exactly the bad prefixes.\n\n> [!NOTE]\n> **Why \"Regular\"?** Regular languages are precisely those recognised by finite automata. Since the verification procedure needs to construct an automaton for the property and intersect it with the system's transition structure, requiring the property to be regular keeps the product finite-state and the algorithm decidable.\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine a **subway train network** running on automated tracks:\n\n- **Atomic Propositions ($AP$)** = Sensors on the track (e.g., \"Signal Green\", \"Door Closed\", \"Brake Engaged\", \"Speed = 0\").\n- **System Behaviour** = The infinite sequence of sensor readings generated by the train's operation.\n- **Safety Property** = \"The train **never** departs a station with its doors open.\"\n  - A *bad prefix* = any finite sensor log that already shows \"Doors Opened\" immediately followed by \"Train Departed\". Once this happens, *no* future continuation can fix the violation.\n- **Regular Safety Property** = The set of all such bad prefixes is finite and pattern-based, recognisable by a simple finite-state gadget (e.g., a counter or a small DFA).\n\n> [!TIP]\n> **Intuitive Rule of Thumb:** Safety = \"Something *bad* never happens.\" Liveness = \"Something *good* eventually happens.\" The verification of regular safety properties reduces to a **reachability problem on the product** of the system and the property's bad-prefix automaton — and reachability is one of the easiest problems in model checking (linear in the product size using BFS/DFS).\n\n## 1.3 Standard Metrics & Conventions Used in KTU Valuation\n\n| Symbol | Meaning |\n|---|---|\n| $M$ | Kripke structure (system model) with $|S|$ states and $|R|$ transitions |\n| $\\mathcal{A}_P$ | Property automaton (DFA) for bad prefixes with $n$ states |\n| $M \\otimes \\mathcal{A}_P$ | Synchronous product of $M$ and $\\mathcal{A}_P$ |\n| $|S_M \\otimes S_P|$ | Number of product states = $|S| \\cdot n$ |\n| $\\text{time complexity}$ | $O(|S| \\cdot n \\cdot \\log(\\text{max out-degree}))$ for BFS reachability |\n| $\\text{space complexity}$ | $O(|S| \\cdot n)$ |\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2 — Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Automata-Theoretic Verification Framework\n\nThe verification of regular safety properties proceeds in three canonical stages:\n\n### Stage 1 — Model the System\nThe system under verification is formalised as a **Kripke structure** $M = (S, S_0, R, L)$ where:\n- $S$ is a finite set of states.\n- $S_0 \\subseteq S$ is the set of initial states.\n- $R \\subseteq S \\times S$ is the total transition relation.\n- $L : S \\to 2^{AP}$ is the labelling function mapping each state to the set of atomic propositions true at it.\n\n### Stage 2 — Encode the Property as a Bad-Prefix Automaton\nThe regular safety property $P$ is encoded as a DFA $\\mathcal{A}_P = (Q, \\Sigma, \\delta, q_0, F)$ over the alphabet $\\Sigma = 2^{AP}$, where:\n- $Q$ is a finite set of automaton states.\n- $q_0 \\in Q$ is the initial state.\n- $\\delta : Q \\times \\Sigma \\to Q$ is the transition function.\n- $F \\subseteq Q$ is the set of *accepting* (i.e., \"bad-prefix-reached\") states.\n\nThe language accepted is $L(\\mathcal{A}_P) = \\text{BadPref}(P)$.\n\n### Stage 3 — Build the Synchronous Product and Check Reachability\nThe product $M \\otimes \\mathcal{A}_P$ is a finite directed graph $G = (V, E)$ where:\n- $V = S \\times Q$.\n- An edge $((s, q), (s', q'))$ exists iff $R(s, s')$ and $q' = \\delta(q, L(s'))$.\n\n> [!IMPORTANT]\n> **Core Theorem (Correctness of Product-Based Safety Verification):**\n> $M \\models P_{safe}$ if and only if **no accepting state** of $\\mathcal{A}_P$ is reachable in $M \\otimes \\mathcal{A}_P$ from any initial state $(s_0, q_0)$ where $s_0 \\in S_0$.\n\nIf an accepting state is reached, the path to it is a *counterexample* — a finite execution of $M$ that is a bad prefix, violating the safety property.\n\n## 2.2 Why Finite Automata Suffice (and Why Not Pushdown Automata)\n\nThe restriction to *regular* safety properties is essential. If we allowed, say, context-free bad-prefix languages, the product construction would have to use a **pushdown automaton**, and reachability in pushdown systems is more expensive (though still decidable, via saturation). Regular safety properties are the sweet spot:\n- Decidable in linear time in the product.\n- Closed under union, intersection, and complement (within regular languages).\n- Expressive enough for nearly all practical \"bad-thing-never-happens\" specifications (mutual exclusion, absence of deadlock, no buffer overflow, no use-after-free, etc.).\n\n## 2.3 The Verification Algorithm (Pseudo-Conceptual)\n\n> [!NOTE]\n> The following is the *conceptual* structure of the algorithm. No programming language is used since the course is a formal/theoretical elective and KTU valuation rewards algorithmic understanding, not coding syntax.\n\n**Algorithm** $\\text{VerifyRegularSafety}(M, \\mathcal{A}_P)$:\n1. Compute the synchronous product graph $G = M \\otimes \\mathcal{A}_P$.\n2. Initialise a worklist $W$ with $\\{(s_0, q_0) \\mid s_0 \\in S_0\\}$.\n3. Mark all such initial product states as visited.\n4. While $W$ is not empty:\n   1. Pop a state $(s, q)$ from $W$.\n   2. If $q \\in F$, return $\\text{VIOLATED}$ and emit the path to $(s, q)$ as counterexample.\n   3. For each successor $(s', q')$ via $R(s, s')$ and $\\delta(q, L(s'))$:\n      - If $(s', q')$ is unvisited, mark visited and push to $W$.\n5. Return $\\text{VERIFIED}$.\n\nThe counterexample extraction in step 4.ii uses parent pointers (or a nested DFS over a spanning forest) to recover the shortest bad prefix.\n\n## 2.4 KTU Formula Sheet / Cheat Sheet\n\n| Concept | Formula / Definition | Units / Type |\n|---|---|---|\n| Product state space size | $\\vert S \\otimes Q \\vert = \\vert S \\vert \\cdot \\vert Q \\vert$ | dimensionless (count) |\n| Product transition count | $\\vert R \\vert \\cdot \\vert \\delta \\vert$ upper bound | dimensionless (count) |\n| Verification time | $O(\\vert S \\vert \\cdot \\vert Q \\vert + \\vert R \\vert \\cdot \\vert Q \\vert)$ | time steps |\n| Verification space | $O(\\vert S \\vert \\cdot \\vert Q \\vert)$ | states |\n| Bad prefix length bound | $\\le \\vert S \\vert \\cdot \\vert Q \\vert$ (product diameter) | transitions |\n| $\\models$ relation | $M \\models P \\iff \\text{Traces}(M) \\subseteq P$ | logical entailment |\n| $M \\not\\models P$ witness | A path to $F$ in $M \\otimes \\mathcal{A}_P$ | finite path |\n| Property automaton type | DFA, NFA, or NFA-$\\varepsilon$ for $\\text{BadPref}(P)$ | finite automaton |\n| Alphabets | $\\Sigma = 2^{AP}$ for both $M$ and $\\mathcal{A}_P$ | power set |\n\n> [!IMPORTANT]\n> **Engineering Utility:** The same machinery is the engine inside **SPIN** (for the `never` claim automata), **NuSMV** (for CTL safety fragments), **CBMC**, and **JPF**. Modern hardware verification at Intel/AMD uses regular safety property verification (via \"assertion-based verification\") to catch silicon bugs before fabrication — each bug saved represents millions of dollars in re-spin cost.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3 — Step-by-Step Derivations, Worked Example & Analytical Matrix\n\n## 3.1 Canonical Worked Example — Mutual Exclusion on a Two-Process Semaphore\n\n### 3.1.1 System Model\n\nConsider two processes $P_1$ and $P_2$ competing for a critical section. We define:\n- $AP = \\{c_1, c_2\\}$ where $c_i$ means \"Process $i$ is in its critical section.\"\n- The Kripke structure $M$ has four reachable states tracking combinations of $(c_1, c_2)$:\n  - $s_0$: $(\\text{false}, \\text{false})$ — both in non-critical section (initial).\n  - $s_1$: $(\\text{true}, \\text{false})$ — only $P_1$ in critical section.\n  - $s_2$: $(\\text{false}, \\text{true})$ — only $P_2$ in critical section.\n  - $s_3$: $(\\text{true}, \\text{true})$ — **both** in critical section (must never be reached).\n\nTransitions: $s_0 \\to s_1$, $s_0 \\to s_2$, $s_1 \\to s_0$, $s_2 \\to s_0$, plus the *forbidden* transition $s_1 \\to s_3$ and $s_2 \\to s_3$ (to test violation).\n\n### 3.1.2 Property as a Bad-Prefix Automaton\n\nThe mutual-exclusion property is:\n$$P_{mutex} = \\{\\sigma \\in (2^{AP})^\\omega \\mid \\forall i \\ge 0, \\neg(c_1 \\in L(s_i) \\land c_2 \\in L(s_i))\\}$$\n\nA bad prefix is any finite trace that already contains a state where both $c_1$ and $c_2$ are true simultaneously. The automaton $\\mathcal{A}_P$ for $\\text{BadPref}(P_{mutex})$ has two states:\n\n- $q_0$ (safe, initial): Neither violation seen yet.\n- $q_1$ (violating, accepting): Violation has occurred.\n\nTransitions of $\\mathcal{A}_P$:\n- $\\delta(q_0, A) = q_0$ for all $A \\subseteq AP$ such that $\\neg(c_1 \\in A \\land c_2 \\in A)$.\n- $\\delta(q_0, \\{c_1, c_2\\}) = q_1$ (a state with both propositions true is the bad event).\n- $\\delta(q_1, A) = q_1$ for all $A$ (once violated, stays violated — \"sticky\" trap).\n\nAccepting set $F = \\{q_1\\}$.\n\n### 3.1.3 Building the Synchronous Product\n\nThe product $M \\otimes \\mathcal{A}_P$ has $|S| \\cdot |Q| = 4 \\times 2 = 8$ product states. We enumerate them and compute the product transition relation step by step.\n\n**Step 1 — Identify the 8 product states** $\\{s_i, q_j\\}$ for $i \\in \\{0,1,2,3\\}$ and $j \\in \\{0,1\\}$:\n$(s_0, q_0), (s_0, q_1), (s_1, q_0), (s_1, q_1), (s_2, q_0), (s_2, q_1), (s_3, q_0), (s_3, q_1)$.\n\n**Step 2 — Compute the product transitions** using the rule: an edge $((s, q) \\to (s', q'))$ exists iff $R(s, s')$ and $q' = \\delta(q, L(s'))$.\n\nRecall the labelling function $L$:\n- $L(s_0) = \\emptyset$ (neither proposition holds).\n- $L(s_1) = \\{c_1\\}$.\n- $L(s_2) = \\{c_2\\}$.\n- $L(s_3) = \\{c_1, c_2\\}$.\n\nWe tabulate transitions from each source state, applying $\\delta$:\n\nFrom $(s_0, q_0)$ with $L(s_0) = \\emptyset$:\n$$\\delta(q_0, \\emptyset) = q_0 \\text{ (safe input)}$$\nSo successors in $M$ from $s_0$ are $s_1$ and $s_2$. Product successors:\n- $((s_0, q_0) \\to (s_1, q_0))$.\n- $((s_0, q_0) \\to (s_2, q_0))$.\n\nFrom $(s_1, q_0)$ with $L(s_1) = \\{c_1\\}$:\n$$\\delta(q_0, \\{c_1\\}) = q_0 \\text{ (no violation yet, only one in CS)}$$\nSuccessors in $M$ from $s_1$ are $s_0$ and $s_3$. Product successors:\n- $((s_1, q_0) \\to (s_0, q_0))$.\n- $((s_1, q_0) \\to (s_3, q_0))$.\n\nFrom $(s_2, q_0)$ with $L(s_2) = \\{c_2\\}$:\n$$\\delta(q_0, \\{c_2\\}) = q_0$$\nSuccessors in $M$ from $s_2$ are $s_0$ and $s_3$. Product successors:\n- $((s_2, q_0) \\to (s_0, q_0))$.\n- $((s_2, q_0) \\to (s_3, q_0))$.\n\nFrom $(s_3, q_0)$ with $L(s_3) = \\{c_1, c_2\\}$:\n$$\\delta(q_0, \\{c_1, c_2\\}) = q_1 \\text{ (VIOLATION)}$$\nSuccessors in $M$ from $s_3$ are $s_0$ (assume it can return to safe region). Product successors:\n- $((s_3, q_0) \\to (s_0, q_1))$.\n\nFrom $(s_0, q_1)$, $(s_1, q_1)$, $(s_2, q_1)$, $(s_3, q_1)$:\n$$\\delta(q_1, A) = q_1 \\text{ for all } A \\subseteq AP \\text{ (sticky trap)}$$\nThese states are \"post-violation\" — every product transition lands in another $q_1$ state.\n\n**Step 3 — Initial states.** Initial product states are $(s_0, q_0)$ only.\n\n**Step 4 — Reachability search.** Start BFS/DFS from $(s_0, q_0)$. Trace the frontier:\n1. Visit $(s_0, q_0)$. Enqueue $(s_1, q_0)$ and $(s_2, q_0)$.\n2. Visit $(s_1, q_0)$. Enqueue $(s_0, q_0)$ [already visited] and $(s_3, q_0)$.\n3. Visit $(s_2, q_0)$. Enqueue $(s_0, q_0)$ [visited] and $(s_3, q_0)$ [will be visited next].\n4. Visit $(s_3, q_0)$. This is **critical**: $\\delta(q_0, \\{c_1, c_2\\}) = q_1$, so the product transition from $s_3$ yields $(s_0, q_1)$.\n5. Visit $(s_0, q_1)$. The automaton component is $q_1 \\in F$ — **VIOLATION DETECTED**.\n\n**Step 5 — Counterexample extraction.** The shortest bad prefix recovered is:\n$$s_0 \\to s_1 \\to s_3 \\to s_0$$\nwith corresponding labels $\\emptyset, \\{c_1\\}, \\{c_1, c_2\\}, \\emptyset$. The third state $s_3$ carries the label $\\{c_1, c_2\\}$ which triggers $\\mathcal{A}_P$ to enter $q_1$, the accepting \"violated\" state.\n\n### 3.1.4 Conclusion of the Example\n\nThe verification reports $\\text{VIOLATED}$ with the counterexample \"Process $P_1$ enters its critical section, then both processes are simultaneously in their critical sections.\" If we remove the $s_1 \\to s_3$ and $s_2 \\to s_3$ transitions, no product state with $q_1$ is reachable, and the algorithm reports $\\text{VERIFIED}$ — proving mutual exclusion.\n\n## 3.2 Analytical Matrix — Decision Table for Verifier\n\n| Property Type | Automaton | Product Construction | Decision Problem | Complexity Class |\n|---|---|---|---|---|\n| Regular safety (bad prefix) | DFA on $\\Sigma^*$ | Finite graph $S \\times Q$ | Reachability of $F$ | $NLOGSPACE$-complete, linear time on product |\n| Context-free safety | Pushdown automaton | Pushdown system | Reachability | Polynomial (P-complete via saturation) |\n| Recursive safety (Turing-recognisable) | Turing machine | Undecidable in general | Reachability | Undecidable |\n| Liveness (Büchi acceptance) | Büchi automaton | Infinite-state exploration | Cycle detection with accepting state revisit | $P$-complete, polynomial |\n| Fair liveness | Streett / Rabin automaton | Fair graph | Non-emptiness with fairness | $P$-complete |\n\n> [!TIP]\n> **KTU Mnemonic:** \"**R**egular **S**afety is **R**eachability\" — and reachability is solvable in **linear time** using graph search. This is why regular safety is the *easiest* fragment of verification and appears so frequently in board questions.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4 — Structural Diagrams & Schematics\n\n## 4.1 Block-Level Architecture of the Verification Pipeline\n\nThe following Mermaid block diagram shows the complete flow of the automata-theoretic verification of a regular safety property.\n\n```mermaid\nflowchart TB\n    startA[System Specification in Natural Language] --> sysModel[Kripke Structure M over states S]\n    startB[Safety Property P in Plain Logic] --> badPref[Identify Bad Prefixes of P]\n    badPref --> dfaBuild[Construct DFA AutoP for BadPref P]\n    sysModel --> product[Synchronous Product M tensor AutoP]\n    dfaBuild --> product\n    product --> reachCheck[Reachability Search on Product Graph]\n    reachCheck --> decision{Is an accepting product state in F reachable?}\n    decision -- yes --> violate[Report VIOLATION plus counterexample trace]\n    decision -- no --> verified[Report VERIFIED M satisfies P]\n    violate --> fix[Engineer refines design]\n    verified --> ship[Ship / fabricate / deploy]\n    fix --> sysModel\n```\n\n> [!NOTE]\n> **Reading the diagram:** The pipeline is iterative. If verification reports a violation, the engineer fixes the design (or the property) and re-runs the model checker. This cycle is the everyday reality of formal methods in industry — verification is *not* a one-shot ceremony but a continuous design companion.\n\n## 4.2 State Diagram — The Bad-Prefix Automaton Pattern\n\nA canonical regular safety property automaton has the *safe → trap-accepting* shape. The diagram below shows the two-state pattern for \"Never both $c_1$ and $c_2$ true\":\n\n```mermaid\nstateDiagram-v2\n    direction LR\n    [*] --> q0safe\n    q0safe : q0 safe initial state\n    q1bad : q1 bad accepting trap state\n    q0safe --> q0safe : input not c1 and c2\n    q0safe --> q1bad : input is c1 and c2 VIOLATION\n    q1bad --> q1bad : any input sticky trap\n    q1bad --> [*]\n```\n\n## 4.3 Product Construction Schematic — Mutual Exclusion\n\nThe following graph shows the synchronous product $M \\otimes \\mathcal{A}_P$ for the worked example. Solid edges are *safe* product transitions, dashed edges lead to the *accepting* $q_1$ states.\n\n```mermaid\nflowchart LR\n    n00[(s0, q0) initial]\n    n10[(s1, q0)]\n    n20[(s2, q0)]\n    n30[(s3, q0)]\n    n01[(s0, q1) accepting bad]\n    n11[(s1, q1) accepting bad]\n    n21[(s2, q1) accepting bad]\n    n31[(s3, q1) accepting bad]\n    n00 --> n10\n    n00 --> n20\n    n10 --> n00\n    n10 --> n30\n    n20 --> n00\n    n20 --> n30\n    n30 -.-> n01\n    n01 --> n11\n    n01 --> n21\n    n11 --> n01\n    n11 --> n31\n    n21 --> n01\n    n21 --> n31\n    n31 --> n01\n```\n\n> [!IMPORTANT]\n> **Reading the product graph:** The dashed edge from $(s_3, q_0)$ to $(s_0, q_1)$ is the *only* transition that crosses from the $q_0$ half of the product to the $q_1$ half. This crossing is the precise \"moment of violation\" — when the system observes a label that the property automaton classifies as a bad prefix. The reachability search finds this edge in time linear in the product.\n\n## 4.4 Hierarchy of Property Classes (Context Diagram)\n\n```mermaid\nflowchart TB\n    classDef sat fill:#fde2e4,stroke:#b8323c,color:#000\n    classDef live fill:#cdeac0,stroke:#3a7d44,color:#000\n    classDef core fill:#bee1f5,stroke:#1e6091,color:#000\n    properties[All omega-regular properties]:::core\n    safetyBlock[Safety properties]:::core\n    livenessBlock[Liveness properties]:::core\n    regSafe[Regular safety properties]:::sat\n    regLive[Regular liveness properties]:::live\n    allomega[omega-regular safety plus liveness]:::core\n    properties --> safetyBlock\n    properties --> livenessBlock\n    safetyBlock --> regSafe\n    livenessBlock --> regLive\n    regSafe --> allomega\n    regLive --> allomega\n```\n\n> [!TIP]\n> **KTU Insight:** In the 2024 scheme, \"regular safety\" is a *strict subset* of \"safety,\" which is a *strict subset* of \"$\\omega$-regular\" properties. Each extra inclusion buys you *expressivity* but costs you *algorithmic complexity*. Reachability (for regular safety) is the cheapest; emptiness-of-Büchi (for regular liveness) is the next step; full LTL is the hardest of the three.\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **`[KTU University Exam — Apr 2024]`** **Q1.** *Define a safety property in the automata-theoretic framework. What is a \"bad prefix\"?*\n\n**Model Answer (3 marks):**\nA property $P \\subseteq (2^{AP})^\\omega$ is a **safety property** if every infinite word $\\sigma \\notin P$ has a finite prefix that cannot be extended to any word in $P$. Such a finite prefix is called a **bad prefix** of $P$. Intuitively, a safety property says \"something bad never happens\" — once a bad prefix occurs, the violation is irrevocable, no matter how the system continues. A property is a *regular* safety property if its set of bad prefixes forms a regular language recognisable by a finite automaton. (1 mark for definition, 1 mark for bad prefix, 1 mark for the \"irrevocability\" intuition.)\n\n> **`[KTU University Exam — Dec 2023]`** **Q2.** *Why is the restriction to regular safety properties important for the decidability and complexity of verification?*\n\n**Model Answer (3 marks):**\nRestricting to regular safety properties ensures that (1 mark) the property can be encoded as a **finite automaton** over the alphabet $2^{AP}$, making the synchronous product $M \\otimes \\mathcal{A}_P$ a *finite* graph. Consequently, (1 mark) the verification problem reduces to a **reachability search** on a finite graph, which is decidable and solvable in time **linear in the product state space** $O(\\vert S \\vert \\cdot \\vert Q \\vert)$. Without the regularity restriction, the property automaton could be a pushdown system or a Turing machine, and reachability would be either polynomial-but-more-expensive or (1 mark) outright **undecidable** in the general case. Thus, regularity balances expressivity with algorithmic tractability.\n\n## 5.2 Part B — Long Answer Questions (14 Marks Each, Internal Choice)\n\n> **`[KTU University Exam — May 2024, Module 2]`**\n\n### Question A (14 Marks)\n\n**(a) [7 Marks]** With a neat diagram, describe the automata-theoretic framework for verifying that a Kripke structure $M$ satisfies a regular safety property $P$. Clearly state the role of the synchronous product and the acceptance condition.\n\n**(b) [7 Marks]** Consider a single-lane one-way bridge with cars arriving from the East and West. The bridge can hold at most one car at a time. Model the system as a Kripke structure and write a regular safety property automaton that forbids two cars being on the bridge simultaneously. Explain the product construction in 3–4 steps.\n\n---\n\n### Model Solution for Question A\n\n#### Part (a) — 7 Marks\n\nThe automata-theoretic verification framework has three stages (5 marks for the stages, 1 mark for diagram, 1 mark for acceptance discussion):\n\n1. **Model the System.** Construct a Kripke structure $M = (S, S_0, R, L)$ capturing all reachable states, initial states, transitions, and the labelling with atomic propositions from $AP$.\n2. **Encode the Property as a DFA.** For the regular safety property $P$, identify the language of bad prefixes $\\text{BadPref}(P)$ and construct a deterministic finite automaton $\\mathcal{A}_P = (Q, \\Sigma, \\delta, q_0, F)$ that accepts exactly $\\text{BadPref}(P)$.\n3. **Build the Synchronous Product and Search.** Form the product graph $G = M \\otimes \\mathcal{A}_P$ with states $S \\times Q$ and the product transition relation. Perform a BFS/DFS reachability search from the initial product states. If any state of the form $(s, q)$ with $q \\in F$ is reachable, report $\\text{VIOLATION}$ with the recovered bad-prefix counterexample. Otherwise, report $\\text{VERIFIED}$.\n\nThe **acceptance condition** is reachability of an accepting product state — not cycle detection (that would be liveness). Reachability is decidable in linear time using standard graph search.\n\n**Neat Diagram (reproduced from Section 4.1):**\nThe flow is: System Spec → Kripke $M$ → Product $M \\otimes \\mathcal{A}_P$ → BFS → Decision (Verified or Violation + counterexample).\n\n#### Part (b) — 7 Marks\n\n**Kripke Model (3 marks).** Let $AP = \\{E, W\\}$ where $E$ means \"a car from the East is on the bridge\" and $W$ means \"a car from the West is on the bridge.\" States:\n- $s_0$: No car on the bridge. Initial state. $L(s_0) = \\emptyset$.\n- $s_E$: East car on bridge. $L(s_E) = \\{E\\}$.\n- $s_W$: West car on bridge. $L(s_W) = \\{W\\}$.\n- $s_{bad}$: Both on bridge. $L(s_{bad}) = \\{E, W\\}$. **Forbidden.**\n\nTransitions reflect the physical rule that only one car can be on the bridge at a time, but we *include* $s_{bad}$ transitions to test the property.\n\n**Property Automaton (2 marks).** The DFA $\\mathcal{A}_P$ has:\n- $Q = \\{q_0, q_1\\}$, $q_0$ safe, $q_1$ bad (accepting).\n- $F = \\{q_1\\}$.\n- $\\delta(q_0, A) = q_1$ iff $E \\in A \\land W \\in A$; else $q_0$.\n- $\\delta(q_1, A) = q_1$ for all $A$ (sticky trap).\n\n**Product Construction in Steps (2 marks).**\n1. Compute $V = S \\times Q = \\{s_0, s_E, s_W, s_{bad}\\} \\times \\{q_0, q_1\\}$ — **8 product states**.\n2. For each system transition $s \\to s'$ and each product state $(s, q)$, add a product edge to $(s', \\delta(q, L(s')))$.\n3. Run BFS from initial $(s_0, q_0)$. If $q_1$ is reached (specifically, via the transition from $s_{bad}$ which is labelled $\\{E, W\\}$), the property is violated; otherwise it holds.\n4. Counterexample path: $s_0 \\to s_E \\to s_{bad}$ (if such a transition is present) yields the bad prefix $\\emptyset, \\{E\\}, \\{E, W\\}$.\n\n---\n\n### Question B (14 Marks — Alternative Choice)\n\n**(a) [7 Marks]** Distinguish between safety and liveness properties. Show, with a one-sentence justification for each, whether the following are safety or liveness: (i) \"Every request is eventually granted.\" (ii) \"The system never enters a deadlock state.\" (iii) \"The temperature eventually falls below 100 °C.\"\n\n**(b) [7 Marks]** Construct a regular safety property automaton for the property: \"The variable $x$ is never assigned the value 0 immediately after being assigned the value 5.\" Take $AP = \\{x=0, x=5\\}$. Show the product construction assuming a 3-state system $M$ that can transition through $x=5$ then $x=0$.\n\n---\n\n### Model Solution for Question B\n\n#### Part (a) — 7 Marks\n\n**Distinction (2 marks):** A **safety property** states that \"something bad *never* happens\" — violations are detectable in finite time and are *irrevocable* (a bad prefix cannot be \"fixed\" by future behaviour). A **liveness property** states that \"something good *eventually* happens\" — no finite prefix can rule out satisfaction; violations can only be confirmed by *infinite* non-occurrence.\n\n**Classifications (5 marks: ~1.67 each):**\n- (i) \"Every request is eventually granted\" — **Liveness**. Any finite prefix where a request has occurred but not yet been granted does *not* violate the property, because the grant may still happen later. There is no bad prefix; satisfaction is determined only by infinite behaviour.\n- (ii) \"The system never enters a deadlock state\" — **Safety**. A deadlock state is a \"bad\" state. As soon as the system reaches a deadlock, the property is irrevocably violated, and the offending finite prefix is a bad prefix.\n- (iii) \"The temperature eventually falls below 100 °C\" — **Liveness**. No finite observation can prove that the temperature will *never* fall below 100 °C, because future behaviour may still satisfy the property.\n\n#### Part (b) — 7 Marks\n\n**Property Formulation (1 mark).** Bad prefixes are finite traces that contain the pattern \"$\\dots, x=5, x=0, \\dots$\" — i.e., a state labelled $\\{x=5\\}$ immediately followed by a state labelled $\\{x=0\\}$. The bad prefix is detected the moment $x=0$ is observed after $x=5$.\n\n**Automaton $\\mathcal{A}_P$ (3 marks).** $Q = \\{q_0, q_1, q_2\\}$:\n- $q_0$: Initial safe state — neither bad event has been seen.\n- $q_1$: Have just seen $x=5$ but not yet the bad transition.\n- $q_2$: Bad event observed — accepting trap state. $F = \\{q_2\\}$.\n\nTransitions (alphabet $\\Sigma = 2^{AP}$):\n- $\\delta(q_0, \\{x=5\\}) = q_1$.\n- $\\delta(q_0, A) = q_0$ for $A \\neq \\{x=5\\}$.\n- $\\delta(q_1, \\{x=0\\}) = q_2$ (the forbidden pattern: 5 followed by 0).\n- $\\delta(q_1, \\{x=5\\}) = q_1$ (another 5 — restart the watch).\n- $\\delta(q_1, A) = q_0$ for $A \\notin \\{\\{x=0\\}, \\{x=5\\}\\}$.\n- $\\delta(q_2, A) = q_2$ for all $A$ (sticky trap).\n\n**Product with 3-state $M$ (3 marks).** Let $M$ have states $s_0$ (no relevant value, $L = \\emptyset$), $s_5$ ($L = \\{x=5\\}$), $s_0'$ ($L = \\{x=0\\}$), with the suspicious path $s_0 \\to s_5 \\to s_0'$. The product has $3 \\times 3 = 9$ states. Tracing the bad path:\n- Start at $(s_0, q_0)$.\n- Move to $(s_5, q_1)$ via $\\delta(q_0, \\{x=5\\}) = q_1$.\n- Move to $(s_0', q_2)$ via $\\delta(q_1, \\{x=0\\}) = q_2$.\n- Since $q_2 \\in F$, the property is **VIOLATED**; the counterexample is the bad prefix $s_0 \\to s_5 \\to s_0'$ with label sequence $\\emptyset, \\{x=5\\}, \\{x=0\\}$.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls (Lose Marks Here!):**\n> 1. **Confusing safety and liveness.** A property is *safety* if a finite trace can prove violation; it is *liveness* if only an infinite trace can prove violation. Many students mark \"eventually\" statements as safety — they are not.\n> 2. **Forgetting the sticky trap.** When the bad event is observed, the property automaton must transition to a *trap accepting* state so that *any* subsequent continuation stays \"bad.\" Students often forget the loop on $q_1$ (or equivalent), and the counterexample is then incorrectly curtailed.\n> 3. **Forgetting the initial state of the property automaton.** The product's initial state is $(s_0, q_0)$ where $s_0$ is from the system and $q_0$ is the property's own initial state. Mixing them up gives a wrong reachability search.\n> 4. **Assuming Büchi acceptance for safety properties.** Safety verification uses *reachability* of an accepting state, not *Büchi cycle* acceptance. Using a Büchi condition is a tell-tale sign of a confused answer.\n> 5. **Ignoring counterexample extraction.** KTU 2024 valuation explicitly awards 2–3 marks for the counterexample trace, not just the yes/no verdict. Always extract the bad prefix.\n> 6. **Not checking the property automaton for determinism.** The construction works for both DFA and NFA, but for KTU board questions, always present a DFA — it makes the product construction deterministic and easier to grade.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- **Safety Property** = \"Bad thing never happens.\" Every violation has a finite bad prefix that makes the violation irrevocable. (Slogan: \"Finite witness of failure.\")\n- **Liveness Property** = \"Good thing eventually happens.\" No finite prefix can prove violation. (Slogan: \"Only infinite witness of failure.\")\n- **Regular Safety Property** = A safety property whose set of bad prefixes is a **regular language**, recognisable by a DFA over $\\Sigma = 2^{AP}$.\n- **Verification Recipe**: (i) Build $M$ as a Kripke structure. (ii) Build $\\mathcal{A}_P$ as a DFA for $\\text{BadPref}(P)$. (iii) Form $M \\otimes \\mathcal{A}_P$. (iv) BFS/DFS for an accepting product state.\n- **Correctness Theorem**: $M \\models P_{safe} \\iff$ no accepting state of $\\mathcal{A}_P$ is reachable in $M \\otimes \\mathcal{A}_P$ from initial states.\n- **Time Complexity**: $O(\\vert S \\vert \\cdot \\vert Q \\vert + \\vert R \\vert \\cdot \\vert Q \\vert)$ — linear in the product.\n- **Space Complexity**: $O(\\vert S \\vert \\cdot \\vert Q \\vert)$.\n- **Counterexample**: A finite path from an initial product state to a state in $F$, of length at most $\\vert S \\vert \\cdot \\vert Q \\vert$.\n- **Sticky Trap Convention**: The accepting state of $\\mathcal{A}_P$ has a self-loop on every input, making the automaton \"monotone bad\" once triggered.\n- **Property Automaton Type**: DFA (or NFA — equivalent) for regular safety; pushdown for context-free safety; Büchi for liveness.\n- **Industry Tools that Use This**: SPIN, NuSMV, CBMC, SLAM/SDV, Jasper, and the formal verification engines inside Intel, AMD, Cadence, and Synopsys.\n- **Common Exam Traps**: Confusing safety with liveness, forgetting the sticky trap, using Büchi acceptance for safety, omitting the counterexample, and mixing up initial states of $M$ and $\\mathcal{A}_P$ in the product.\n\n> [!TIP]\n> **One-line KTU 2024 takeaway:** *\"To verify a regular safety property, build a DFA for its bad prefixes, take the synchronous product with the system, and run a reachability search — if any accepting state is hit, you have a finite counterexample.\"*\n\n---\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 2-t3",
          "title": "Automata on Infinite Words - ω-Regular Languages and Properties",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 2\n> - **Topic:** Automata on Infinite Words - ω-Regular Languages and Properties\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Infinite Words (ω-Words): The Never-Ending Tape\n\nIn classical automata theory, a *word* (or *string*) is a finite sequence of symbols drawn from an alphabet $\\Sigma$. A word $w = a_1 a_2 \\ldots a_n$ has a definite end. However, many computational scenarios — such as **operating system schedulers, reactive hardware controllers, communication protocols, and software that runs indefinitely** — produce sequences of events that never terminate.\n\nAn **$\\omega$-word** (read as \"omega-word\") is an *infinite* sequence of symbols:\n\n$$w = a_1 a_2 a_3 \\ldots \\quad \\text{where } a_i \\in \\Sigma \\text{ and } \\vert w \\vert = \\infty$$\n\nThe set of all infinite words over $\\Sigma$ is denoted $\\Sigma^{\\omega}$. Note that $\\Sigma^* \\cap \\Sigma^{\\omega} = \\emptyset$, so finite and infinite words form disjoint worlds.\n\n> [!IMPORTANT]\n> **KTU Definition:** An $\\omega$-word over an alphabet $\\Sigma$ is a function $w : \\mathbb{N} \\to \\Sigma$. The collection of all such infinite words is $\\Sigma^{\\omega} = \\{w : \\mathbb{N} \\to \\Sigma\\}$.\n\n## 1.2 ω-Languages: Sets of Never-Ending Conversations\n\nA **language of infinite words** (or **$\\omega$-language**) is any subset $L \\subseteq \\Sigma^{\\omega}$. The study of $\\omega$-languages is the heart of *automated verification* because:\n\n- A program's execution trace is potentially infinite.\n- A specification of \"what should always happen\" or \"what must eventually occur\" is naturally expressed as a set of acceptable infinite traces.\n\n## 1.3 Büchi Automata: Recognizers of the Infinite\n\nA **Büchi automaton** (BA) is a finite-state machine that *accepts* sets of infinite words. Formally, it is a 5-tuple:\n\n$$\\mathcal{B} = (Q, \\Sigma, \\delta, Q_0, F)$$\n\nwhere:\n- $Q$ is a finite set of states\n- $\\Sigma$ is a finite input alphabet\n- $\\delta : Q \\times \\Sigma \\to 2^{Q}$ is the non-deterministic transition function\n- $Q_0 \\subseteq Q$ is the set of initial states\n- $F \\subseteq Q$ is the set of accepting states\n\n### Acceptance Condition (Büchi Acceptance)\n\nFor an infinite run $\\rho = q_0 a_1 q_1 a_2 q_2 \\ldots$ on input $w$, the run is **accepting** if and only if some accepting state $f \\in F$ appears *infinitely often* in $\\rho$. Formally:\n\n$$\\text{Inf}(\\rho) \\cap F \\neq \\emptyset$$\n\nThe language accepted by $\\mathcal{B}$ is:\n\n$$L_{\\omega}(\\mathcal{B}) = \\{w \\in \\Sigma^{\\omega} \\mid \\text{there exists an accepting run of } \\mathcal{B} \\text{ on } w\\}$$\n\n## 1.4 Intuitive Analogy: The Vigilant Security Guard\n\n> [!NOTE]\n> **Real-World Analogy:** Imagine a security guard stationed at a bank lobby watching cameras forever (never ending their shift). They mark their logbook each time they see a \"suspicious event\" (this is like visiting an accepting state $F$). The infinite tape of camera footage is the $\\omega$-word. The guard \"accepts\" the night's recording if and only if they marked their logbook *infinitely many times*. A single suspicious event is not enough — the guard must keep seeing something interesting recurring forever. This is the **Büchi acceptance condition**.\n\nContrast this with standard DFA acceptance, where the guard only needs to see something interesting *once* at the end of the finite tape.\n\n## 1.5 ω-Regular Languages\n\nAn **$\\omega$-regular language** is a language $L \\subseteq \\Sigma^{\\omega}$ that can be expressed by an **$\\omega$-regular expression**, which is built from:\n\n- Atomic $\\omega$-regular expressions: $A^{\\omega}$ where $A \\subseteq \\Sigma^*$ is a *regular language* of finite words\n- Boolean operations: union $L_1 \\cup L_2$, complement $L^c = \\Sigma^{\\omega} \\setminus L$\n- Concatenation: $L_1 \\cdot L_2 = \\{u \\cdot v \\mid u \\in L_1, v \\in L_2\\}$ (where $L_1 \\subseteq \\Sigma^*$, $L_2 \\subseteq \\Sigma^{\\omega}$)\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight:** The class of $\\omega$-regular languages is **exactly** the class of languages accepted by non-deterministic Büchi automata. This is the Büchi–McNaughton–Rabin theorem and is fundamental to KTU exam questions.\n\n## 1.6 Why This Matters in Automated Verification\n\n| Domain | Infinite Behaviour Modelled |\n|---|---|\n| Operating Systems | Non-terminating scheduler loops |\n| Hardware | Reactive circuits with clock cycles |\n| Communication | Continuous network protocols |\n| Concurrent Programs | Reactive threads with infinite traces |\n| Temporal Logic | LTL/CTL formulas as $\\omega$-languages |\n\nThe model-checking question \"Does the system $M$ satisfy the property $\\varphi$?\" is reduced to the **emptiness problem** for Büchi automata: $L(M) \\cap L(\\neg\\varphi) = \\emptyset$?\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Anatomy of a Büchi Automaton: Step-by-Step Logic\n\n1. **Finite Control**: A Büchi automaton has only finitely many states $Q$ — bounded memory regardless of how long the input runs.\n2. **Non-Deterministic Transitions**: From state $q$ on symbol $a$, the automaton may jump to *any* state in $\\delta(q, a)$. This non-determinism is essential and cannot always be removed (unlike DFAs).\n3. **Infinite Reading**: The input tape has no end-marker; the automaton reads symbols forever.\n4. **Büchi Acceptance**: Acceptance requires that *at least one* accepting state be visited *infinitely often*. Equivalently, $\\text{Inf}(\\rho) \\cap F \\neq \\emptyset$.\n\n> [!NOTE]\n> **Why \"Infinitely Often\" and not \"Eventually\"?** Because in non-terminating systems, \"eventually\" never comes — the run goes on forever. The Büchi condition elegantly captures \"the property must hold *recurrently*\" — a perfect match for fairness and liveness.\n\n## 2.2 Deterministic vs Non-Deterministic Büchi Automata\n\n| Property | Non-Deterministic BA (NBA) | Deterministic BA (DBA) |\n|---|---|---|\n| Power | Full $\\omega$-regular languages | Strictly weaker (e.g., cannot accept $(a^*b)^{\\omega}$) |\n| Closure under complement | **Yes** (via determinization + Safra) | Yes (trivial) |\n| Emptiness check | Polynomial (nested DFS) | Polynomial |\n| KTU relevance | Used in LTL model checking | Used in complementation theory |\n\n## 2.3 ω-Regular Expressions: The Algebra of the Infinite\n\nAn $\\omega$-regular expression over $\\Sigma$ has the syntax:\n\n$$\\alpha ::= \\emptyset \\mid a \\in \\Sigma \\mid \\alpha + \\alpha \\mid \\alpha \\cdot \\alpha \\mid \\alpha^* \\mid \\alpha^{\\omega}$$\n\nThe semantics for the $\\omega$-power operator are:\n\n$$A^{\\omega} = \\{a_1 a_2 a_3 \\ldots \\mid \\forall i \\geq 1, \\, a_1 \\ldots a_i \\in A \\text{ and } a_i \\neq \\epsilon\\}$$\n\n> [!IMPORTANT]\n> **KTU Pitfall:** $A^{\\omega}$ requires $A$ to be a *language of non-empty words* (i.e., $\\epsilon \\notin A$). Otherwise, $\\epsilon^{\\omega} = \\epsilon$, which is finite and not a valid $\\omega$-word.\n\n## 2.4 KTU High-Yield Formula Sheet\n\n| Concept | Symbol / Expression | Meaning |\n|---|---|---|\n| Infinite word | $w = a_1 a_2 \\ldots$ | $\\vert w \\vert = \\infty$, $w \\in \\Sigma^{\\omega}$ |\n| Büchi acceptance | $\\text{Inf}(\\rho) \\cap F \\neq \\emptyset$ | Some accepting state recurs infinitely |\n| $\\omega$-regular expression | $\\alpha = A_1 \\cdot B_1^{\\omega} + A_2 \\cdot B_2^{\\omega} + \\ldots$ | Finite prefix from $A_i$ then loop in $B_i^{\\omega}$ |\n| Concatenation (mixed) | $L_1 \\cdot L_2$ | $L_1 \\subseteq \\Sigma^*$, $L_2 \\subseteq \\Sigma^{\\omega}$ |\n| Complement | $\\overline{L_{\\omega}(\\mathcal{B})}$ | $\\Sigma^{\\omega} \\setminus L_{\\omega}(\\mathcal{B})$ |\n| Union closure | $L(\\mathcal{B}_1) \\cup L(\\mathcal{B}_2) = L(\\mathcal{B}_1 \\oplus \\mathcal{B}_2)$ | Direct product with merged accepting sets |\n| Intersection closure | $L(\\mathcal{B}_1) \\cap L(\\mathcal{B}_2) = L(\\mathcal{B}_1 \\otimes \\mathcal{B}_2)$ | Product construction with *pair-of-states* acceptance |\n| Emptiness condition | $L(\\mathcal{B}) = \\emptyset \\iff \\text{no accepting SCC reachable from } Q_0$ | Decidable in $O(\\vert Q \\vert \\cdot \\vert \\Sigma \\vert)$ |\n| Myhill–Nerode for $\\omega$ | $u \\sim_{\\omega} v$ iff $\\forall \\alpha \\in \\Sigma^{\\omega}: u\\alpha \\in L \\iff v\\alpha \\in L$ | Right congruence giving canonical NBA |\n| Rabin acceptance | $\\bigvee_{i=1}^{k} ( \\text{Inf}(\\rho) \\cap F_i \\neq \\emptyset \\wedge \\text{Inf}(\\rho) \\cap G_i = \\emptyset )$ | Generalisation via $(F_i, G_i)$ pairs |\n| Streett acceptance | $\\bigwedge_{i=1}^{k} ( \\text{Inf}(\\rho) \\cap F_i \\neq \\emptyset \\implies \\text{Inf}(\\rho) \\cap G_i \\neq \\emptyset )$ | Duality of Rabin |\n| Büchi $\\equiv$ LTL | $L(\\varphi) = L(\\mathcal{B}_{\\varphi})$ | Every LTL formula is an $\\omega$-regular language |\n| Model checking check | $M \\models \\varphi \\iff L(M) \\cap L(\\neg\\varphi) = \\emptyset$ | Reduce to Büchi emptiness |\n\n## 2.5 Real-World Utility in Engineering\n\n- **Hardware Verification (Intel, AMD, ARM)**: Pre-silicon verification of CPU pipelines using $\\omega$-automata; the property \"no starvation\" or \"fair request acknowledgment\" is an $\\omega$-regular property.\n- **Software Model Checkers (SPIN, NuSMV)**: LTL formulas are translated to Büchi automata, then intersected with the system's transition system.\n- **Communication Protocols (TCP, Bluetooth)**: Liveness \"every request is eventually answered\" is a Büchi-acceptable property.\n- **Cyber-Physical Systems**: Continuous-time control loops are discretised and verified for $\\omega$-regular safety properties.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Theorem: Closure of ω-Regular Languages Under Union\n\n**Statement:** If $L_1$ and $L_2$ are $\\omega$-regular languages over $\\Sigma$, then $L_1 \\cup L_2$ is $\\omega$-regular.\n\n**Proof by Büchi Automaton Construction:**\n\nLet $\\mathcal{B}_1 = (Q_1, \\Sigma, \\delta_1, Q_0^1, F_1)$ and $\\mathcal{B}_2 = (Q_2, \\Sigma, \\delta_2, Q_0^2, F_2)$ accept $L_1$ and $L_2$ respectively.\n\nConstruct the *union* Büchi automaton $\\mathcal{B}_{\\cup}$:\n\n$$\\mathcal{B}_{\\cup} = (Q_1 \\cup Q_2, \\, \\Sigma, \\, \\delta_{\\cup}, \\, Q_0^1 \\cup Q_0^2, \\, F_1 \\cup F_2)$$\n\nwhere for any state $q \\in Q_1 \\cup Q_2$ and symbol $a \\in \\Sigma$:\n\n$$\\delta_{\\cup}(q, a) = \\begin{cases} \\delta_1(q, a) & \\text{if } q \\in Q_1 \\\\ \\delta_2(q, a) & \\text{if } q \\in Q_2 \\end{cases}$$\n\n**Justification (textual, line-by-line):**\n\n- *Line 1:* We take the disjoint union of the state spaces. The states are simply pooled together.\n- *Line 2:* Initial states of $\\mathcal{B}_{\\cup}$ are the union of initial states — non-determinism lets us \"guess\" which sub-automaton to follow.\n- *Line 3:* Accepting states are merged. The Büchi condition is satisfied if *any* accepting state (from either sub-automaton) recurs infinitely often.\n- *Line 4:* Any infinite word $w \\in L_1$ has an accepting run in $\\mathcal{B}_1$, which is also a run in $\\mathcal{B}_{\\cup}$ whose accepting set $F_1 \\subseteq F_1 \\cup F_2$ is hit infinitely often. Symmetric for $L_2$. Hence $L_1 \\cup L_2 \\subseteq L(\\mathcal{B}_{\\cup})$.\n- *Line 5:* Conversely, any accepting run in $\\mathcal{B}_{\\cup}$ must eventually live entirely within $Q_1$ or entirely within $Q_2$ (no cross transitions defined), so $w \\in L_1 \\cup L_2$.\n\nTherefore $L(\\mathcal{B}_{\\cup}) = L_1 \\cup L_2$. $\\blacksquare$\n\n## 3.2 Theorem: Closure Under Intersection (Product Construction)\n\n**Statement:** If $L_1$ and $L_2$ are $\\omega$-regular, then $L_1 \\cap L_2$ is $\\omega$-regular.\n\n**Proof:**\n\nDefine the *product* Büchi automaton $\\mathcal{B}_{\\cap}$:\n\n$$\\mathcal{B}_{\\cap} = (Q_1 \\times Q_2, \\, \\Sigma, \\, \\delta_{\\cap}, \\, Q_0^1 \\times Q_0^2, \\, F_{\\cap})$$\n\nwhere:\n\n$$\\delta_{\\cap}((q_1, q_2), a) = \\delta_1(q_1, a) \\times \\delta_2(q_2, a)$$\n\n$$F_{\\cap} = \\{(q_1, q_2) \\in Q_1 \\times Q_2 \\mid q_1 \\in F_1\\}$$\n\n**Why this works (step-by-step):**\n\n- The product tracks the run of *both* automata simultaneously.\n- An accepting state of $\\mathcal{B}_{\\cap}$ is one where the *first* component is accepting.\n- For a run to be accepting, some state $(q_1, q_2)$ with $q_1 \\in F_1$ must recur infinitely often.\n- But this is **not sufficient**! It guarantees $L_1$ is accepted, but the second component may not visit $F_2$ infinitely often.\n\n**Refined Construction (Büchi on Both):**\n\nWe must use a *generalised* Büchi condition with two acceptance sets $F_a = F_1 \\times Q_2$ and $F_b = Q_1 \\times F_2$, and require $\\text{Inf} \\cap F_a \\neq \\emptyset$ **and** $\\text{Inf} \\cap F_b \\neq \\emptyset$.\n\nThis can be converted back to standard Büchi acceptance by a state-doubling trick:\n\n$$\\mathcal{B}_{\\cap}' = (Q_1 \\times Q_2 \\times \\{1, 2\\}, \\, \\Sigma, \\, \\delta', \\, Q_0^1 \\times Q_0^2 \\times \\{1\\}, \\, F' = Q_1 \\times Q_2 \\times \\{2\\})$$\n\nwhere state $(q_1, q_2, i)$ transitions to states in $Q_1 \\times Q_2 \\times \\{3 - i\\}$ and acceptance is tracked by alternating phases. $\\blacksquare$\n\n## 3.3 Theorem: Closure Under Complementation\n\n**Statement:** If $L$ is $\\omega$-regular, then $\\overline{L} = \\Sigma^{\\omega} \\setminus L$ is $\\omega$-regular.\n\n**Proof Sketch (using determinisation):**\n\nThis is the historically hard direction. The construction proceeds in three stages:\n\n1. **Determinise the NBA to a deterministic Rabin automaton** (using Safra's construction, 1988) — exponential blow-up.\n2. **Dualise the Rabin condition to a Streett condition** (swap universal and existential quantifiers over the pairs).\n3. **Convert Streett to NBA** (polynomial in the original NBA size).\n\nThe final result is an NBA $\\overline{\\mathcal{B}}$ with $L_{\\omega}(\\overline{\\mathcal{B}}) = \\Sigma^{\\omega} \\setminus L_{\\omega}(\\mathcal{B})$. $\\blacksquare$\n\n> [!IMPORTANT]\n> **KTU Exam Tip:** This theorem is often asked as a 7-mark sub-part. Students must mention the three-step pipeline: NBA → DRA (Safra) → DSA (dual) → NBA-complement.\n\n## 3.4 Worked Example: ω-Regular Expression to Büchi Automaton\n\n**Problem:** Construct a Büchi automaton for the $\\omega$-regular language $(ab)^{\\omega} = \\{ababab\\ldots\\}$ over $\\Sigma = \\{a, b\\}$.\n\n**Solution:**\n\nWe construct a NBA with two states to track the alternation.\n\n$$\\mathcal{B} = (Q, \\Sigma, \\delta, Q_0, F)$$\n\n- $Q = \\{q_0, q_1\\}$\n- $Q_0 = \\{q_0\\}$\n- $F = \\{q_0\\}$ (we accept by returning to $q_0$ infinitely often)\n- $\\delta$:\n\n$$\\begin{aligned}\n\\delta(q_0, a) &= \\{q_1\\} \\\\\n\\delta(q_0, b) &= \\emptyset \\\\\n\\delta(q_1, a) &= \\emptyset \\\\\n\\delta(q_1, b) &= \\{q_0\\}\n\\end{aligned}$$\n\n**Acceptance analysis (textual):**\n\n- A run reads $a$ (goes $q_0 \\to q_1$), reads $b$ (goes $q_1 \\to q_0$), reads $a$ ($q_0 \\to q_1$), reads $b$ ($q_1 \\to q_0$), ...\n- The accepting state $q_0$ is visited at positions $0, 2, 4, \\ldots$ — infinitely often.\n- Hence any word in $(ab)^{\\omega}$ is accepted.\n- A word *not* in $(ab)^{\\omega}$ either has length mismatch (rejected by no valid transition) or has $\\text{Inf}(\\rho) \\cap F = \\emptyset$.\n\n## 3.5 Worked Example: Emptiness Check\n\n**Problem:** Given the NBA below, check if its language is empty.\n\nNBA: $Q = \\{q_0, q_1, q_2\\}$, $Q_0 = \\{q_0\\}$, $F = \\{q_2\\}$, transitions: $q_0 \\xrightarrow{a} q_1$, $q_1 \\xrightarrow{b} q_2$, $q_2 \\xrightarrow{c} q_2$.\n\n**Solution via SCC Analysis:**\n\n- Build the transition graph: $q_0 \\to q_1 \\to q_2 \\to q_2$ (self-loop).\n- Find *Strongly Connected Components* (SCCs): $\\{q_0\\}$, $\\{q_1\\}$, $\\{q_2\\}$.\n- An SCC is *accepting* if it contains some $f \\in F$ and is reachable from $Q_0$.\n- $\\{q_2\\}$ is accepting (contains $q_2 \\in F$) and reachable from $Q_0$.\n- $L(\\mathcal{B}) \\neq \\emptyset$. Indeed, the word $abc^{\\omega} = a\\,b\\,c\\,c\\,c\\ldots$ is accepted (run visits $q_2$ infinitely often via the self-loop).\n\n**Conclusion:** $L(\\mathcal{B}) = \\{a b c^{\\omega}\\}$.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Büchi Automaton for $(ab)^{\\omega}$ — Mermaid State Diagram\n\nThe following Mermaid diagram visualises the NBA constructed in §3.4. Every node ID is purely alphanumeric and labels are raw uppercase text without any markdown formatting, per the Mermaid compilation safeguard.\n\n```mermaid\nstateDiagram-v2\n    direction LR\n    [*] --> nodeQ0\n    nodeQ0 --> nodeQ1 : a\n    nodeQ0 --> nodeQ0_loop : a UNDEFINED PATH\n    nodeQ1 --> nodeQ0 : b\n    nodeQ0 --> nodeQ0 : a\n    nodeQ1 --> nodeQ1 : b\n    nodeQ0 --> nodeQ2_reject : b\n    nodeQ1 --> nodeQ2_reject : a\n    nodeQ0 --> nodeQ0_accept : ALL OTHER\n    nodeQ1 --> nodeQ1_accept : ALL OTHER\n\n    state nodeQ0 {\n        description: Q0 INITIAL AND ACCEPTING\n    }\n    state nodeQ1 {\n        description: Q1 INTERMEDIATE\n    }\n    state nodeQ0_accept {\n        description: ACCEPTING TRAP\n    }\n    state nodeQ1_accept {\n        description: NON ACCEPTING TRAP\n    }\n    state nodeQ0_loop {\n        description: SELF LOOP ON Q0\n    }\n    state nodeQ2_reject {\n        description: REJECTING SINK\n    }\n\n    classDef acceptState fill:#c8e6c9,stroke:#1b5e20,stroke-width:2px\n    classDef rejectState fill:#ffcdd2,stroke:#b71c1c,stroke-width:1px\n    class nodeQ0,nodeQ0_accept acceptState\n    class nodeQ2_reject,nodeQ1_accept rejectState\n```\n\n## 4.2 Model Checking Architecture — Block-Level Functional Flow\n\nThis block diagram depicts how $\\omega$-regular languages integrate into a complete model checking pipeline. Each block represents a processing stage, and the arrows show information flow.\n\n```mermaid\nflowchart TD\n    classDef inputBlock fill:#bbdefb,stroke:#0d47a1,stroke-width:2px\n    classDef processBlock fill:#fff9c4,stroke:#f57f17,stroke-width:2px\n    classDef automatonBlock fill:#c8e6c9,stroke:#1b5e20,stroke-width:2px\n    classDef outputBlock fill:#f8bbd0,stroke:#880e4f,stroke-width:2px\n    classDef errorBlock fill:#ffcdd2,stroke:#b71c1c,stroke-width:2px\n\n    A[SYSTEM MODEL M Kripke Structure]:::inputBlock\n    B[PROPERTY NEGATION NOT PHI LTL FORMULA]:::inputBlock\n    C[LTL TO BUCHI TRANSLATOR]:::processBlock\n    D[BUCHI AUTOMATON B NOT PHI]:::automatonBlock\n    E[PRODUCT CONSTRUCTOR M AND B NOT PHI]:::processBlock\n    F[PRODUCT AUTOMATON P]:::automatonBlock\n    G[SCC ANALYSIS AND NESTED DFS]:::processBlock\n    H{EMPTINESS CHECK}:::outputBlock\n    I[COUNTEREXAMPLE TRACE]:::errorBlock\n    J[VERIFICATION PASSED M SATISFIES PHI]:::outputBlock\n\n    A --> E\n    B --> C\n    C --> D\n    D --> E\n    E --> F\n    F --> G\n    G --> H\n    H -->|NON EMPTY| I\n    H -->|EMPTY| J\n\n    K[OMEGA REGULAR LANGUAGE LIBRARY]:::processBlock\n    K --> C\n    K --> E\n\n    L[CLOSURE PROPERTIES UNION INTERSECTION COMPLEMENT]:::processBlock\n    K --> L\n    L --> C\n```\n\n**Reading the diagram:**\n\n1. The **System Model** (a Kripke structure) and the **Negated Property** (an LTL formula) are the two inputs.\n2. The LTL formula is converted to a **Büchi Automaton** using known translation algorithms.\n3. The **Product Construction** merges the system and the property automaton into a single NBA whose language is $L(M) \\cap L(\\neg\\varphi)$.\n4. **SCC Analysis** (or nested DFS) checks whether an accepting SCC exists.\n5. If the product language is **empty**, the system satisfies the property (verification passed). If not, a **counterexample trace** is returned.\n\n## 4.3 Classification of ω-Regular Properties\n\nThe Mermaid diagram below classifies the two principal classes of properties in automated verification, each expressible as an $\\omega$-regular language.\n\n```mermaid\nflowchart LR\n    classDef safetyStyle fill:#c8e6c9,stroke:#1b5e20,stroke-width:2px\n    classDef livenessStyle fill:#ffe0b2,stroke:#e65100,stroke-width:2px\n    classDef rootStyle fill:#e1bee7,stroke:#4a148c,stroke-width:2px\n\n    P[PROPERTIES OF INFINITE TRACES]:::rootStyle\n    P --> S[SAFETY NOTHING BAD EVER HAPPENS]:::safetyStyle\n    P --> LI[LIVENESS SOMETHING GOOD EVENTUALLY HAPPENS]:::livenessStyle\n    P --> F[FAIRNESS INFINITELY OFTEN]:::livenessStyle\n\n    S --> S1[ASSERTION VIOLATION]:::safetyStyle\n    S --> S2[DEADLOCK FREEDOM]:::safetyStyle\n    S --> S3[BOUND VIOLATION]:::safetyStyle\n\n    LI --> L1[EVENTUAL RESPONSE]:::livenessStyle\n    LI --> L2[PROGRESS]:::livenessStyle\n    LI --> L3[TERMINATION GENERALIZED]:::livenessStyle\n\n    F --> F1[STRONG FAIRNESS]:::livenessStyle\n    F --> F2[WEAK FAIRNESS]:::livenessStyle\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Question 1 `[KTU University Exam - Dec 2023]`\n**Define a Büchi automaton. Explain its acceptance condition with an example.**\n\n**Model Answer:**\n\nA **Büchi automaton** is a 5-tuple $\\mathcal{B} = (Q, \\Sigma, \\delta, Q_0, F)$ where $Q$ is a finite set of states, $\\Sigma$ is a finite alphabet, $\\delta : Q \\times \\Sigma \\to 2^Q$ is the non-deterministic transition function, $Q_0 \\subseteq Q$ is the set of initial states, and $F \\subseteq Q$ is the set of accepting states.\n\n**Acceptance Condition:** An infinite input word $w = a_1 a_2 a_3 \\ldots$ is accepted by $\\mathcal{B}$ if there exists a run $\\rho = q_0 a_1 q_1 a_2 q_2 \\ldots$ starting from some $q_0 \\in Q_0$ such that at least one accepting state $f \\in F$ occurs *infinitely often* in $\\rho$, i.e., $\\text{Inf}(\\rho) \\cap F \\neq \\emptyset$.\n\n**Example:** To accept $(ab)^{\\omega}$, take $Q = \\{q_0, q_1\\}$, $Q_0 = \\{q_0\\}$, $F = \\{q_0\\}$, $\\delta(q_0, a) = q_1$, $\\delta(q_1, b) = q_0$. The word $ababab\\ldots$ produces run $q_0 q_1 q_0 q_1 q_0 \\ldots$ which visits $q_0$ infinitely often. ✓\n\n---\n\n### Question 2 `[KTU University Exam - May 2024]`\n**What are $\\omega$-regular languages? Give two examples.**\n\n**Model Answer:**\n\nAn **$\\omega$-regular language** is a subset $L \\subseteq \\Sigma^{\\omega}$ that can be expressed by an $\\omega$-regular expression built using finite regular languages, the $\\omega$-operator, union, and concatenation. Equivalently, $L$ is accepted by some non-deterministic Büchi automaton.\n\n**Examples:**\n\n1. $L_1 = (ab)^{\\omega} = \\{ababab\\ldots\\}$ — the set of all infinite alternating $a, b$ words.\n2. $L_2 = (a + b)^* b^{\\omega}$ — all infinite words that eventually stay in $b$ forever (i.e., have only finitely many $a$'s).\n3. $L_3 = \\Sigma^{\\omega}$ where $\\Sigma = \\{a, b\\}$ — the universal $\\omega$-language (accepted by a single accepting state with self-loops on $a$ and $b$).\n\n---\n\n## 5.2 Part B Questions (14 Marks Each — Internal Choice)\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Pitfall Callout**\n> - **Forgetting infiniteness in the definition of $\\omega$-words:** Many students write $w = a_1 a_2 \\ldots a_n$ with a finite $n$. This is wrong — $\\omega$-words are infinite. **Penalty: -2 marks.**\n> - **Confusing Büchi with Muller acceptance:** Büchi is \"infinitely often in $F$\"; Muller is \"Inf($\\rho$) $\\in \\mathcal{F}$ for some set family $\\mathcal{F}$\". Mixing them up costs full sub-part marks.\n> - **Skipping the non-determinism justification in the union closure proof:** You must mention the merge of $Q_0^1 \\cup Q_0^2$ and $F_1 \\cup F_2$ explicitly. A vague proof loses 3–4 marks.\n> - **Forgetting to convert generalised Büchi to standard Büchi** in the intersection proof: This is the most common mistake. Examiners expect the state-doubling construction.\n> - **Writing `L = { w | condition }` in tables or proof sketches** can break LaTeX. Use $L = \\{ w \\mid \\text{condition} \\}$ with `\\mid` (or `\\vert`) instead of `|`.\n\n---\n\n### OPTION A (14 Marks) `[KTU University Exam - Dec 2022]`\n\n**Part (a) [7 Marks]:** *Explain the construction of a Büchi automaton. Show that the class of $\\omega$-regular languages is closed under union by constructing a Büchi automaton for $L_1 \\cup L_2$ given two NBAs.*\n\n**Model Solution:**\n\n**Step 1 — Definition of Büchi Automaton [2 marks]:**\nA Büchi automaton $\\mathcal{B} = (Q, \\Sigma, \\delta, Q_0, F)$ accepts infinite words $w \\in \\Sigma^{\\omega}$. A run $\\rho$ is a sequence $\\rho = q_0 \\xrightarrow{a_1} q_1 \\xrightarrow{a_2} q_2 \\ldots$ with $q_0 \\in Q_0$ and $q_{i+1} \\in \\delta(q_i, a_i)$. The run is *accepting* if $\\text{Inf}(\\rho) \\cap F \\neq \\emptyset$, where $\\text{Inf}(\\rho) = \\{q \\in Q \\mid q = q_i \\text{ for infinitely many } i\\}$.\n\n**Step 2 — Closure Under Union [5 marks]:**\n\nLet $\\mathcal{B}_1 = (Q_1, \\Sigma, \\delta_1, Q_0^1, F_1)$ with $L(\\mathcal{B}_1) = L_1$ and $\\mathcal{B}_2 = (Q_2, \\Sigma, \\delta_2, Q_0^2, F_2)$ with $L(\\mathcal{B}_2) = L_2$.\n\n**Construction:** Define $\\mathcal{B}_{\\cup} = (Q, \\Sigma, \\delta, Q_0, F)$ where:\n- $Q = Q_1 \\cup Q_2$ (assume disjoint by renaming)\n- $Q_0 = Q_0^1 \\cup Q_0^2$\n- $F = F_1 \\cup F_2$\n- $\\delta(q, a) = \\delta_1(q, a)$ if $q \\in Q_1$, else $\\delta_2(q, a)$ if $q \\in Q_2$\n\n**Proof of Correctness:**\n\n*($\\supseteq$ direction):* Take $w \\in L_1$. There exists an accepting run $\\rho_1$ in $\\mathcal{B}_1$ with $q_0 \\in Q_0^1$ and $\\text{Inf}(\\rho_1) \\cap F_1 \\neq \\emptyset$. The same sequence is a valid run in $\\mathcal{B}_{\\cup}$ because $\\delta$ restricted to $Q_1$ equals $\\delta_1$. Also $F_1 \\subseteq F$, so $\\text{Inf}(\\rho) \\cap F \\neq \\emptyset$. Hence $w \\in L(\\mathcal{B}_{\\cup})$. Symmetric for $L_2$.\n\n*($\\subseteq$ direction):* Take $w \\in L(\\mathcal{B}_{\\cup})$ with accepting run $\\rho$. Since $Q_1 \\cap Q_2 = \\emptyset$ and no transition crosses between $Q_1$ and $Q_2$, after the first step the run stays either in $Q_1$ or in $Q_2$. Hence $w \\in L_1$ or $w \\in L_2$.\n\nTherefore $L(\\mathcal{B}_{\\cup}) = L_1 \\cup L_2$, proving closure under union. $\\blacksquare$\n\n---\n\n**Part (b) [7 Marks]:** *Discuss the closure properties of $\\omega$-regular languages under complementation. Outline the Safra construction for complementing a non-deterministic Büchi automaton.*\n\n**Model Solution:**\n\n**Step 1 — Statement of Closure [1 mark]:**\nIf $L \\subseteq \\Sigma^{\\omega}$ is $\\omega$-regular, then $\\overline{L} = \\Sigma^{\\omega} \\setminus L$ is also $\\omega$-regular.\n\n**Step 2 — Why Direct Determinisation Is Hard [1 mark]:**\nNon-deterministic Büchi automata cannot be determinised to deterministic Büchi automata while preserving language. The class DBA is strictly weaker than NBA. For example, $(a + b)^* b^{\\omega}$ is accepted by an NBA but not by any DBA.\n\n**Step 3 — Safra's Three-Stage Pipeline [4 marks]:**\n\n*Stage 1 — NBA to Deterministic Rabin Automaton (DRA):*\nGiven NBA $\\mathcal{B} = (Q, \\Sigma, \\delta, Q_0, F)$, construct a DRA $\\mathcal{D}$ using *Safra trees* (1988). Each state of $\\mathcal{D}$ is a labelled tree over $2^Q$. The number of states is bounded by $(2 \\vert Q \\vert)^{O(\\vert Q \\vert)}$ — exponential but not double-exponential.\n\n*Stage 2 — DRA to Deterministic Streett Automaton (DSA):*\nA Rabin condition is $\\bigvee_{i=1}^{k} ( \\text{Inf} \\cap F_i \\neq \\emptyset \\wedge \\text{Inf} \\cap G_i = \\emptyset )$. Dualise to Streett: $\\bigwedge_{i=1}^{k} ( \\text{Inf} \\cap F_i \\neq \\emptyset \\to \\text{Inf} \\cap G_i \\neq \\emptyset )$. Same states, swapped acceptance interpretation.\n\n*Stage 3 — DSA to NBA:*\nConvert DSA back to NBA by tracking the satisfaction of each Streett pair. This step is polynomial in the DRA size.\n\nThe composed NBA $\\overline{\\mathcal{B}}$ accepts exactly $\\Sigma^{\\omega} \\setminus L(\\mathcal{B})$. The construction is non-trivial but conceptually clean.\n\n**Step 4 — KTU Exam Concluding Remark [1 mark]:**\nThe complement of an $\\omega$-regular language is itself $\\omega$-regular, and this is the cornerstone of model checking — the *negation* of a property is also a property.\n\n---\n\n### OPTION B (14 Marks) `[KTU University Exam - May 2023]`\n\n**Part (a) [7 Marks]:** *Convert the $\\omega$-regular expression $\\alpha = (a + b)^* a^{\\omega}$ into an equivalent non-deterministic Büchi automaton. Show step-by-step construction.*\n\n**Model Solution:**\n\n**Step 1 — Understanding the Language [1 mark]:**\n$\\alpha = (a + b)^* a^{\\omega}$ denotes the set of infinite words that *eventually* consist entirely of $a$'s. In other words, words with only finitely many $b$'s.\n\n**Step 2 — High-Level Construction Idea [1 mark]:**\nWe need a Büchi automaton that \"guesses\" the point at which the word becomes all $a$'s, then stays in an accepting state reading only $a$'s.\n\n**Step 3 — Formal Construction [4 marks]:**\n\nDefine $\\mathcal{B} = (Q, \\Sigma, \\delta, Q_0, F)$ where:\n- $Q = \\{q_0, q_1\\}$\n- $\\Sigma = \\{a, b\\}$\n- $Q_0 = \\{q_0\\}$\n- $F = \\{q_1\\}$\n- $\\delta$:\n\n$$\\begin{aligned}\n\\delta(q_0, a) &= \\{q_0, q_1\\} \\\\\n\\delta(q_0, b) &= \\{q_0\\} \\\\\n\\delta(q_1, a) &= \\{q_1\\} \\\\\n\\delta(q_1, b) &= \\emptyset\n\\end{aligned}$$\n\n**Step 4 — Acceptance Verification [1 mark]:**\n\nFor any word $w$ with finitely many $b$'s: the automaton non-deterministically transitions from $q_0$ to $q_1$ at some point after the last $b$. Once in $q_1$, the remaining $a$'s keep it in $q_1$, and $q_1$ is visited infinitely often. ✓\n\nFor a word with infinitely many $b$'s: any run that leaves $q_0$ for $q_1$ must return to $q_0$ on a $b$ (or deadlocks). Since $b$'s occur infinitely often, the run cannot stay in $q_1$ forever — so $q_1$ is visited only finitely many times. ✗\n\nHence $L(\\mathcal{B}) = (a + b)^* a^{\\omega}$. $\\blacksquare$\n\n---\n\n**Part (b) [7 Marks]:** *Explain the role of $\\omega$-regular languages in model checking. Distinguish between safety and liveness properties with examples.*\n\n**Model Solution:**\n\n**Step 1 — Model Checking Setup [1 mark]:**\nGiven a system model $M$ (a Kripke structure or labelled transition system) and a property $\\varphi$ (often in LTL), the verification question is:\n\n$$M \\models \\varphi \\quad \\text{(Does } M \\text{ satisfy } \\varphi\\text{?)}$$\n\n**Step 2 — Reduction to Büchi Emptiness [2 marks]:**\nUsing the equivalence $L(\\varphi)$ is $\\omega$-regular, the question $M \\models \\varphi$ reduces to:\n\n$$L(M) \\subseteq L(\\varphi) \\iff L(M) \\cap \\overline{L(\\varphi)} = \\emptyset \\iff L(M) \\cap L(\\neg\\varphi) = \\emptyset$$\n\nThe product $L(M) \\cap L(\\neg\\varphi)$ is again $\\omega$-regular (closure under intersection). The emptiness check is decidable in polynomial time via SCC analysis or nested DFS.\n\n**Step 3 — Safety Properties [2 marks]:**\n\nA **safety property** states that \"nothing bad ever happens.\" Formally, a property $P_{safe} \\subseteq \\Sigma^{\\omega}$ is a safety property if for every $w \\notin P_{safe}$, there exists a finite prefix $u$ of $w$ such that no extension $u \\cdot v$ is in $P_{safe}$. The \"bad\" event is detectable from a finite prefix.\n\n**Examples:**\n- \"The variable $x$ never exceeds 100.\"\n- \"Mutual exclusion is never violated.\"\n- \"The system never enters an error state.\"\n\nThese are accepted by *safety automata* (no accepting state can be a trap).\n\n**Step 4 — Liveness Properties [2 marks]:**\n\nA **liveness property** states that \"something good eventually happens repeatedly.\" A property $P_{live} \\subseteq \\Sigma^{\\omega}$ is a liveness property if for every finite word $u \\in \\Sigma^*$, there exists an extension $v$ such that $u \\cdot v \\in P_{live}$. The \"good\" event cannot be ruled out by any finite prefix.\n\n**Examples:**\n- \"Every request is eventually acknowledged.\" (Büchi: infinitely many grants if infinitely many requests.)\n- \"The system makes progress infinitely often.\" (Büchi: infinitely many progress states.)\n- \"If a process is enabled infinitely often, it executes infinitely often.\" (Strong fairness.)\n\n**Conclusion:** Every LTL property can be decomposed into a safety part and a liveness part (the *Safety-Liveness Decomposition Theorem*). The $\\omega$-regular framework unifies both and provides a uniform verification algorithm. $\\blacksquare$\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n- **$\\omega$-word**: An infinite sequence $w = a_1 a_2 a_3 \\ldots$ with $a_i \\in \\Sigma$. The set of all such words is $\\Sigma^{\\omega}$.\n- **Büchi automaton**: 5-tuple $(Q, \\Sigma, \\delta, Q_0, F)$ with $\\delta : Q \\times \\Sigma \\to 2^Q$. Accepts $w$ iff some $f \\in F$ occurs *infinitely often* in some valid run.\n- **Büchi acceptance** is a *recurrence* condition: $\\text{Inf}(\\rho) \\cap F \\neq \\emptyset$. It is *not* a final-state condition.\n- **$\\omega$-regular language**: A language $L \\subseteq \\Sigma^{\\omega}$ accepted by some NBA (Büchi–McNaughton–Rabin theorem).\n- **$\\omega$-regular expression**: Built from $\\emptyset, a, +, \\cdot, {}^*, {}^{\\omega}$. Requires non-empty base for the $\\omega$-operator.\n- **NBA $\\equiv$ $\\omega$-regular expressions** in expressive power.\n- **DBA $\\subsetneq$ NBA**: Deterministic Büchi automata are strictly less powerful. Example: $(a + b)^* b^{\\omega}$ has no DBA.\n- **Closure under union**: Disjoint union of states, merge $Q_0$ and $F$.\n- **Closure under intersection**: Product construction with generalised Büchi acceptance, then state-doubling for standard Büchi.\n- **Closure under complement**: Safra's construction (NBA $\\to$ DRA $\\to$ DSA $\\to$ NBA) — exponential worst-case but conceptually clean.\n- **Emptiness problem**: $L(\\mathcal{B}) = \\emptyset \\iff$ no accepting SCC is reachable from $Q_0$. Decidable in $O(\\vert Q \\vert \\cdot \\vert \\Sigma \\vert)$.\n- **Model checking reduction**: $M \\models \\varphi \\iff L(M) \\cap L(\\neg\\varphi) = \\emptyset$. This is *the* central equation of automated verification.\n- **Safety vs Liveness**: Safety = \"bad thing never happens\" (finite-prefix checkable). Liveness = \"good thing happens repeatedly\" (no finite prefix can rule it out).\n- **LTL is $\\omega$-regular**: Every LTL formula can be translated to an NBA — the standard translation uses tableau construction with state-doubling for the \"until\" operator.\n- **Latex pitfall in tables**: Always use `\\mid` or `\\vert` instead of bare `|` inside table cells.\n- **Infinite words cannot be empty**: $\\Sigma^{\\omega}$ contains no finite word, so the empty string $\\epsilon$ is in $\\Sigma^*$ but not in $\\Sigma^{\\omega}$.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 2-t4",
          "title": "Nondeterministic Büchi Automata (NBA)",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 2\n> - **Topic:** Nondeterministic Büchi Automata (NBA)\n\n<!-- SECTION_1_START -->\n# Module 2 — Nondeterministic Büchi Automata (NBA)\n\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition of Nondeterministic Büchi Automaton (NBA)\n\nA **Nondeterministic Büchi Automaton (NBA)** is a finite-state machine designed to accept **infinite words** (ω-words) over a finite alphabet. It is the foundational recognition device for **ω-regular languages** and serves as the canonical model used in **Linear Temporal Logic (LTL) model checking** and the automated verification of reactive systems.\n\nFormally, an NBA is defined as a **5-tuple**:\n\n$$\\mathcal{A} = (Q,\\ \\Sigma,\\ \\delta,\\ Q_0,\\ F)$$\n\nwhere each component has the following precise meaning:\n\n| Symbol | Name | Mathematical Type | Description |\n| :--- | :--- | :--- | :--- |\n| $Q$ | State set | Finite, non-empty set | The complete set of configurations the automaton can occupy |\n| $\\Sigma$ | Alphabet | Finite, non-empty set | The set of input symbols (e.g., propositions, events) |\n| $\\delta$ | Transition relation | $Q \\times \\Sigma \\rightarrow 2^{Q}$ | Nondeterministic mapping from state-symbol pairs to sets of successor states |\n| $Q_0$ | Initial states | $Q_0 \\subseteq Q$ | The (possibly multiple) starting configurations |\n| $F$ | Accepting states | $F \\subseteq Q$ | The designated **Büchi** or **final** states |\n\n> [!IMPORTANT]\n> **Büchi Acceptance Condition (KTU Syllabus Highlight):**\n> An infinite word $w = a_1 a_2 a_3 \\ldots \\in \\Sigma^{\\omega}$ is **accepted** by $\\mathcal{A}$ if and only if there exists a run $\\rho = q_0 q_1 q_2 \\ldots$ such that:\n> 1. $q_0 \\in Q_0$ (the run starts at an initial state)\n> 2. $\\forall i \\geq 0: q_{i+1} \\in \\delta(q_i, a_{i+1})$ (every transition is valid)\n> 3. $\\inf(\\rho) \\cap F \\neq \\emptyset$ (some accepting state appears **infinitely often** in the run)\n>\n> where $\\inf(\\rho)$ denotes the set of states that occur infinitely many times along $\\rho$.\n\nThe language recognized by $\\mathcal{A}$ is therefore:\n\n$$L(\\mathcal{A}) = \\left\\{\\, w \\in \\Sigma^{\\omega} \\;\\middle|\\; \\exists \\rho.\\ \\rho_0 \\in Q_0 \\ \\land\\ \\forall i \\geq 0.\\ \\rho_{i+1} \\in \\delta(\\rho_i, w_i) \\ \\land\\ \\inf(\\rho) \\cap F \\neq \\emptyset \\,\\right\\}$$\n\n### 1.2 Intuitive Overview — The \"Endless Library\" Analogy\n\n> [!NOTE]\n> **Conceptual Analogy: The Never-Ending Library Scanner**\n> Imagine a librarian (the NBA) walking through an **infinite library** (an ω-word $w = a_1 a_2 a_3 \\ldots$) whose shelves contain symbols from a fixed alphabet $\\Sigma$. The librarian carries a **flashlight** and follows nondeterministic movement rules: at each shelf, the flashlight may illuminate several different rooms (states) to walk into next. The library never ends.\n>\n> - **Normal rooms** are like non-accepting states.\n> - **Golden rooms** are the **accepting states** $F$.\n> - The librarian is said to \"accept\" the library **only if they visit a Golden room infinitely many times** — i.e., the Golden rooms must keep reappearing forever, not just once.\n>\n> This captures the core idea of Büchi acceptance: **persistent recurrence** of acceptance, not mere single-time occurrence.\n\nThis persistent-recurrence condition is what makes NBA fundamentally different from classical DFAs (which require a final state at the *end* of a *finite* string). Since ω-words have **no end**, the acceptance criterion must be reformulated in terms of **limiting behavior** — exactly the role played by $\\inf(\\rho) \\cap F \\neq \\emptyset$.\n\n### 1.3 Why NBA Exists — The Inadequacy of DFA on ω-Words\n\nClassical Deterministic Finite Automata (DFAs) are designed to classify **finite** strings, so they fundamentally cannot recognize many natural ω-regular languages. For example, consider:\n\n$$L = \\{\\, a_1 a_2 a_3 \\ldots \\in \\{0,1\\}^{\\omega} \\;\\middle|\\; a_i = 1 \\text{ for infinitely many } i \\,\\}$$\n\nNo DFA can recognize $L$, because once a DFA reads the entire (infinite) input it never reaches a halting \"accept\" state. The NBA solves this by replacing \"halting in an accept state\" with \"visiting an accept state infinitely often\" — a strictly more expressive formalism for ω-words.\n\n### 1.4 Physical Constants / Standard Metrics\n\n> [!IMPORTANT]\n> The following numerical conventions are standard throughout the KTU syllabus and reference literature on ω-automata:\n> - **State-space size** of an NBA is denoted $\\vert Q \\vert$ (we use $\\lvert Q \\rvert$ in $\\LaTeX$ to avoid breaking tables).\n> - **Alphabet size** is denoted $\\lvert \\Sigma \\rvert$.\n> - **Number of accepting states** is $\\lvert F \\rvert$.\n> - **Determinization blow-up (Safra's bound):** converting an NBA with $n$ states into an equivalent **Deterministic Rabin Automaton (DRA)** yields at most $2^{O(n \\log n)}$ states, and at least $2^{\\Omega(n \\log n)}$ are needed in the worst case.\n> - **Synchronized product size** for two NBAs with $n_1$ and $n_2$ states is $n_1 \\cdot n_2$.\n\nThese constants matter when reasoning about the **complexity of verification algorithms** built on top of NBAs.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Operational Semantics — A Structured Step-by-Step Walkthrough\n\nThe execution of an NBA on an ω-word $w = a_1 a_2 a_3 \\ldots$ proceeds as follows:\n\n- **Step 1 — Initialization:** Select a starting state $q_0$ from the set of initial states $Q_0$. This selection is **nondeterministic** — multiple valid choices may exist.\n- **Step 2 — Iterative Reading & Transition:** For each input symbol $a_{i+1}$ at time step $i+1$, consult the transition relation $\\delta(q_i, a_{i+1}) \\subseteq Q$ to obtain the set of all permissible next states. The automaton then **nondeterministically** chooses one of them as $q_{i+1}$.\n- **Step 3 — Path Formation:** Continue indefinitely to construct an infinite path $\\rho = q_0 q_1 q_2 q_3 \\ldots$ in the state space. Each such path is called a **run** of $\\mathcal{A}$ on $w$.\n- **Step 4 — Limiting Behavior Analysis:** Examine the set $\\inf(\\rho)$, which contains every state that appears **infinitely many times** along $\\rho$ (a consequence of the pigeonhole principle applied to the finite set $Q$).\n- **Step 5 — Acceptance Decision:** The word $w$ is accepted **iff** at least one state in $\\inf(\\rho)$ is a member of the accepting set $F$.\n\n> [!NOTE]\n> **Why the \"infinitely often\" condition?** Because $Q$ is finite, every infinite run $\\rho$ necessarily revisits some states. The acceptance condition asks whether **some accepting state is among those revisited states**. This is the natural ω-analogue of the DFA's \"halt in a final state\" rule.\n\n## 2.2 NBA vs. Other ω-Automata — The Comparative Landscape\n\n| Automaton Type | Acceptance Condition | Closure under Complement? | Determ. Equivalent Size |\n| :--- | :--- | :--- | :--- |\n| **NBA** (Nondeterministic Büchi) | $\\inf(\\rho) \\cap F \\neq \\emptyset$ | **No** (requires Safra) | DRA: $2^{O(n \\log n)}$ |\n| **NGA** (Nondeterministic Generalized Büchi) | $\\bigwedge_j \\inf(\\rho) \\cap F_j \\neq \\emptyset$ | **No** | DRA: $2^{O(n \\log n)}$ |\n| **DRA** (Deterministic Rabin) | $\\exists i.\\ \\inf(\\rho) \\cap F_i \\neq \\emptyset \\ \\land\\ \\inf(\\rho) \\cap G_i = \\emptyset$ | **Yes** | — (already deterministic) |\n| **DPA** (Deterministic Parity) | $\\max\\{\\, c(q) \\mid q \\in \\inf(\\rho) \\,\\}$ is even | **Yes** | DRA: $2^{O(n \\log n)}$ |\n| **NPA** (Nondeterministic Parity) | $\\max\\{\\, c(q) \\mid q \\in \\inf(\\rho) \\,\\}$ is even | **No** | DPA: $2^{O(n \\log n)}$ |\n\n## 2.3 KTU Formula Sheet / Cheat Sheet\n\n> [!IMPORTANT]\n> The following table compiles **every** equation, definition, and bound a KTU 2024 student must memorize for NBA-related problems.\n\n| # | Concept | Formula / Definition | Notes / Units |\n| :--- | :--- | :--- | :--- |\n| 1 | **NBA tuple** | $\\mathcal{A} = (Q, \\Sigma, \\delta, Q_0, F)$ | 5-tuple; $\\delta$ is a relation |\n| 2 | **Transition type** | $\\delta: Q \\times \\Sigma \\rightarrow 2^{Q}$ | Set-valued → nondeterminism |\n| 3 | **Büchi acceptance** | $w \\in L(\\mathcal{A}) \\iff \\exists \\rho.\\ \\rho_0 \\in Q_0 \\ \\land\\ \\forall i.\\ \\rho_{i+1} \\in \\delta(\\rho_i, w_i) \\ \\land\\ \\inf(\\rho) \\cap F \\neq \\emptyset$ | \"Some $F$ infinitely often\" |\n| 4 | **$\\inf$ set** | $\\inf(\\rho) = \\{\\, q \\in Q \\mid \\forall n.\\ \\exists m \\geq n.\\ \\rho_m = q \\,\\}$ | States recurring infinitely often |\n| 5 | **Union closure** | $L(\\mathcal{A}_1) \\cup L(\\mathcal{A}_2) = L(\\mathcal{A}_{\\cup})$ | Product construction |\n| 6 | **Union product state set** | $Q_{\\cup} = Q_1 \\times Q_2$ | Size $\\lvert Q_1 \\rvert \\cdot \\lvert Q_2 \\rvert$ |\n| 7 | **Union product initial set** | $Q_{0,\\cup} = Q_{0,1} \\times Q_{0,2}$ | — |\n| 8 | **Union product accepting set** | $F_{\\cup} = (F_1 \\times Q_2) \\cup (Q_1 \\times F_2)$ | Either component \"wins\" |\n| 9 | **Union product transition** | $\\delta_{\\cup}((p,q),a) = \\delta_1(p,a) \\times \\delta_2(q,a)$ | Pointwise product |\n| 10 | **Intersection closure** | $L(\\mathcal{A}_1) \\cap L(\\mathcal{A}_2) = L(\\mathcal{A}_{\\cap})$ | NGA then NBA reduction |\n| 11 | **Intersection → NGA** | $F_{\\cap}^{(1)} = F_1 \\times Q_2,\\ F_{\\cap}^{(2)} = Q_1 \\times F_2$ | Generalized Büchi |\n| 12 | **NGA → NBA conversion** | $\\lvert Q_{\\text{NBA}} \\rvert = \\lvert Q \\rvert \\cdot k$ where $k = $ number of Büchi sets | Accepting set cycles |\n| 13 | **Safra bound (NBA → DRA)** | $\\lvert Q_{\\text{DRA}} \\rvert \\leq 2^{O(n \\log n)}$ | $n = \\lvert Q_{\\text{NBA}} \\rvert$ |\n| 14 | **NBA complementation cost** | $\\Sigma^{\\omega} \\setminus L(\\mathcal{A})$ requires DRA then DRA complement | $2^{O(n \\log n)}$ states |\n| 15 | **Run count** | A run is a function $\\rho: \\mathbb{N} \\rightarrow Q$ | Infinite sequence |\n| 16 | **LTL → NBA size** | $\\lvert Q \\rvert \\leq 2^{\\mathcal{O}(\\lvert \\varphi \\rvert)}$ | Exponential in formula length |\n\n## 2.4 Why These Formulas Matter — Real-World Engineering Utility\n\n> [!NOTE]\n> The NBA framework is the **backbone of industrial model checkers** like SPIN, NuSMV, and CBMC. Every time a reactive system (an embedded controller, a network protocol, an operating-system scheduler) is verified against temporal specifications, the system and the negation of the specification are translated into two NBAs whose **intersection emptiness** is checked. The intersection emptiness test reduces to finding a reachable accepting **lasso** (a cycle through a final state), which is solved in $O(\\lvert Q_1 \\rvert \\cdot \\lvert Q_2 \\rvert)$ time. Without NBAs and their efficient product constructions, modern hardware/software verification would be intractable.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Worked Example 1 — Constructing an NBA for \"Infinitely Many a's\"\n\n**Problem:** Build an NBA $\\mathcal{A}$ over the alphabet $\\Sigma = \\{a, b\\}$ that accepts precisely those ω-words $w$ containing the symbol $a$ **infinitely many times**.\n\n### 3.1.1 Step-by-Step Construction\n\nWe need two states:\n- $q_0$ — the \"still-hoping\" state (non-accepting, will be initial).\n- $q_1$ — the \"a-was-just-seen\" state (accepting, used to count a's).\n\nThe transition relation is constructed as follows:\n\n$$\\delta(q_0, a) = \\{q_1\\}$$\n$$\\delta(q_0, b) = \\{q_0\\}$$\n$$\\delta(q_1, a) = \\{q_1\\}$$\n$$\\delta(q_1, b) = \\{q_1\\}$$\n\nThe remaining components of the tuple are:\n\n$$Q = \\{q_0, q_1\\}, \\quad \\Sigma = \\{a, b\\}, \\quad Q_0 = \\{q_0\\}, \\quad F = \\{q_1\\}$$\n\n### 3.1.2 Acceptance Proof (Exhaustive)\n\n**Forward direction ($\\Rightarrow$):** Suppose $w = a_1 a_2 a_3 \\ldots$ is accepted by $\\mathcal{A}$. Then there exists a run $\\rho = q_0 q_1 q_2 \\ldots$ with $\\inf(\\rho) \\cap \\{q_1\\} \\neq \\emptyset$. This means $q_1$ appears infinitely often along $\\rho$. By construction, $q_1$ is entered **only** when the symbol $a$ is read (because the only transition into $q_1$ is $\\delta(q_0, a) = \\{q_1\\}$ and $\\delta(q_1, a) = \\{q_1\\}$, both triggered by $a$). Hence $a$ must appear infinitely often in $w$.\n\n**Backward direction ($\\Leftarrow$):** Suppose $a$ appears infinitely often in $w$. Construct the canonical run: start at $q_0$, stay in $q_0$ on every $b$, and move to $q_1$ on every $a$. After the first $a$, the run is permanently in $q_1$ (since $q_1$ is a sink on both $a$ and $b$). Because $a$ occurs infinitely often, we are guaranteed that **every** symbol after the first $a$ is processed in state $q_1$, so $q_1$ occurs infinitely often. Thus $\\inf(\\rho) = \\{q_1\\} \\subseteq F$, and $w$ is accepted.\n\n$$\\blacksquare$$\n\n## 3.2 Worked Example 2 — Union Closure via Product Construction (Full Derivation)\n\n**Theorem (Union Closure):** If $L_1$ and $L_2$ are Büchi-recognizable, then $L_1 \\cup L_2$ is also Büchi-recognizable.\n\n**Given:** $\\mathcal{A}_1 = (Q_1, \\Sigma, \\delta_1, Q_{0,1}, F_1)$ and $\\mathcal{A}_2 = (Q_2, \\Sigma, \\delta_2, Q_{0,2}, F_2)$.\n\n**Construct:** $\\mathcal{A}_{\\cup} = (Q_{\\cup}, \\Sigma, \\delta_{\\cup}, Q_{0,\\cup}, F_{\\cup})$.\n\n**Step 1 — State set:** The product state set is the Cartesian product.\n\n$$Q_{\\cup} = Q_1 \\times Q_2 = \\{\\, (p, q) \\mid p \\in Q_1 \\ \\land\\ q \\in Q_2 \\,\\}$$\n\n**Step 2 — Initial set:**\n\n$$Q_{0,\\cup} = Q_{0,1} \\times Q_{0,2}$$\n\n**Step 3 — Transition relation (synchronized product):**\n\n$$\\delta_{\\cup}((p, q), a) = \\delta_1(p, a) \\times \\delta_2(q, a)$$\n\n**Step 4 — Accepting set (the key design choice):**\n\n$$F_{\\cup} = (F_1 \\times Q_2) \\cup (Q_1 \\times F_2)$$\n\nThat is, a product state $(p, q)$ is accepting iff **at least one** of its components is accepting in its own automaton.\n\n### 3.2.1 Correctness Proof\n\nLet $w \\in L(\\mathcal{A}_{\\cup})$. Then there is a run $\\rho = (p_0, q_0)(p_1, q_1)(p_2, q_2)\\ldots$ with $\\inf(\\rho) \\cap F_{\\cup} \\neq \\emptyset$. The projections $\\pi_1(\\rho) = p_0 p_1 p_2 \\ldots$ and $\\pi_2(\\rho) = q_0 q_1 q_2 \\ldots$ are valid runs of $\\mathcal{A}_1$ and $\\mathcal{A}_2$ on $w$, respectively. Since some product state in $F_{\\cup}$ appears infinitely often, infinitely many of these product states lie in $F_1 \\times Q_2$ **or** in $Q_1 \\times F_2$. In either sub-case, the corresponding projection has an accepting state recurring infinitely often. Hence $w \\in L(\\mathcal{A}_1) \\cup L(\\mathcal{A}_2)$.\n\nThe reverse direction follows symmetrically: if $w \\in L(\\mathcal{A}_1)$, the run $\\rho$ of $\\mathcal{A}_1$ that witnesses acceptance can be paired with any run of $\\mathcal{A}_2$ on $w$ to produce a product run; the projection onto the first component is accepting, so infinitely many product states are in $F_1 \\times Q_2$, ensuring acceptance by $\\mathcal{A}_{\\cup}$.\n\n### 3.2.2 Numerical Verification\n\nSuppose $L_1$ requires $a$ infinitely often and $L_2$ requires $b$ infinitely often. Take $w = a b a b a b \\ldots$ Both $a$ and $b$ appear infinitely often, so $w \\in L_1 \\cap L_2 \\subseteq L_1 \\cup L_2$. The product automaton has the property that infinitely many of its states lie in $F_1 \\times Q_2$ (whenever $a$ is read) **and** infinitely many lie in $Q_1 \\times F_2$ (whenever $b$ is read), so $F_{\\cup}$ is hit infinitely often. Acceptance is confirmed.\n\n## 3.3 Worked Example 3 — NBA Closure under Intersection (via NGA Reduction)\n\n**Theorem (Intersection Closure):** $L_1 \\cap L_2$ is Büchi-recognizable whenever $L_1$ and $L_2$ are.\n\n**Step 1 — Build the product NGA:**\n\n$$\\mathcal{A}_{\\cap} = (Q_1 \\times Q_2,\\ \\Sigma,\\ \\delta_{\\cap},\\ Q_{0,1} \\times Q_{0,2},\\ \\{F_1 \\times Q_2,\\ Q_1 \\times F_2\\})$$\n\nThis is a **Nondeterministic Generalized Büchi Automaton (NGA)** with **two** acceptance sets $F^{(1)} = F_1 \\times Q_2$ and $F^{(2)} = Q_1 \\times F_2$.\n\n**Step 2 — NGA to NBA conversion:** The standard trick is to introduce $k$ \"phases\" (one per acceptance set, here $k = 2$) and cycle through them. Define:\n\n$$Q' = Q_1 \\times Q_2 \\times \\{1, 2\\}$$\n\nwith the modified initial set $Q_0' = Q_{0,1} \\times Q_{0,2} \\times \\{1\\}$ and the unique accepting set:\n\n$$F' = F_1 \\times Q_2 \\times \\{1\\} \\cup Q_1 \\times F_2 \\times \\{2\\}$$\n\nThe transition simulates one step of the NGA and increments the phase modulo $k$. A state with phase $j$ transitions to phase $j+1$ **only if** it is in $F^{(j)}$; otherwise it remains in phase $j$. Because the phase index cycles through $\\{1, \\ldots, k\\}$ and the run must visit each $F^{(j)}$ infinitely often to \"advance\" the phase, the resulting NBA accepts precisely $L_1 \\cap L_2$.\n\n**State count:** $\\lvert Q' \\rvert = \\lvert Q_1 \\rvert \\cdot \\lvert Q_2 \\rvert \\cdot k$.\n\n## 3.4 Analytical Matrix — Quick Decision Table for NBA Operations\n\n| Operation | Result Type | State Count | Closed? |\n| :--- | :--- | :--- | :--- |\n| Union $\\mathcal{A}_1 \\cup \\mathcal{A}_2$ | NBA | $\\lvert Q_1 \\rvert \\cdot \\lvert Q_2 \\rvert$ | **Yes** |\n| Intersection $\\mathcal{A}_1 \\cap \\mathcal{A}_2$ | NBA | $\\lvert Q_1 \\rvert \\cdot \\lvert Q_2 \\rvert \\cdot k$ | **Yes** |\n| Complement $\\overline{L(\\mathcal{A})}$ | DRA (not NBA in general) | $2^{O(n \\log n)}$ | **No** (NBA not closed) |\n| Projection (existential alphabet hiding) | NBA | $\\leq \\lvert Q \\rvert$ | **Yes** |\n| Quotient / language substitution | NBA | exponential | **Yes** |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 NBA State Diagram for \"Infinitely Many a's\"\n\nThe following Mermaid diagram renders the canonical NBA constructed in §3.1.1. The **accepting** state $q_1$ is rendered as a double circle (per the convention `circle` shape with a thick border). $q_0$ is the unique initial state.\n\n```mermaid\nstateDiagram-v2\n    [*] --> nodeQ0\n    nodeQ0: q0 (initial, non-accepting)\n    nodeQ1: q1 (accepting, BUCHI)\n    nodeQ0 --> nodeQ0 : b\n    nodeQ0 --> nodeQ1 : a\n    nodeQ1 --> nodeQ1 : a\n    nodeQ1 --> nodeQ1 : b\n```\n\n**Reading the diagram:** From $q_0$, reading $a$ sends the run to $q_1$ (which is \"sticky\" — every subsequent symbol keeps the run in $q_1$). Reading $b$ from $q_0$ self-loops. Acceptance requires that the run land in $q_1$ at least once, after which it stays there forever — guaranteeing $q_1 \\in \\inf(\\rho)$ and hence $w \\in L(\\mathcal{A})$.\n\n## 4.2 NBA Product (Union) Construction — Block-Level Architecture\n\nThe following block diagram shows the **architectural** view of the union product. The two component NBAs execute in lock-step, and the Büchi-acceptance is a logical disjunction of the two component acceptances.\n\n```mermaid\nflowchart LR\n    subgraph INP[\"INPUT OMEGA-WORD w\"]\n        symA[\"a1\"]\n        symB[\"a2\"]\n        symC[\"a3\"]\n        symD[\"...\"]\n    end\n\n    subgraph NBA1[\"AUTOMATON A1 (Q1, Sigma, delta1, Q01, F1)\"]\n        stA1[\"STATE MACHINE 1\"]\n        f1A[\"F1 ACCEPTING\"]\n    end\n\n    subgraph NBA2[\"AUTOMATON A2 (Q2, Sigma, delta2, Q02, F2)\"]\n        stA2[\"STATE MACHINE 2\"]\n        f2A[\"F2 ACCEPTING\"]\n    end\n\n    subgraph PROD[\"PRODUCT AUTOMATON A_union\"]\n        prodS[\"STATE SET Q1 x Q2\"]\n        fUnion[\"F_union = (F1 x Q2) U (Q1 x F2)\"]\n        acceptDec{\"inf(rho) intersects F_union?\"}\n    end\n\n    symA --> stA1\n    symA --> stA2\n    symB --> stA1\n    symB --> stA2\n    symC --> stA1\n    symC --> stA2\n    symD --> stA1\n    symD --> stA2\n\n    stA1 --> prodS\n    stA2 --> prodS\n    f1A --> fUnion\n    f2A --> fUnion\n    prodS --> acceptDec\n    fUnion --> acceptDec\n    acceptDec -- YES --> acceptOut[\"ACCEPT w\"]\n    acceptDec -- NO  --> rejectOut[\"REJECT w\"]\n\n    style NBA1 fill:#E8F0FE,stroke:#1A73E8\n    style NBA2 fill:#FCE8E6,stroke:#D93025\n    style PROD fill:#FFF3CD,stroke:#F9AB00\n    style acceptDec fill:#E6F4EA,stroke:#34A853\n    style acceptOut fill:#34A853,color:#FFFFFF\n    style rejectOut fill:#D93025,color:#FFFFFF\n```\n\n**Functional interpretation:** The product automaton is a *synchronised* cross-product. Each of its states encodes the joint configuration $(q_1, q_2)$ of the two components. A state is **accepting** (in $F_{\\cup}$) iff either component is in its own accepting set. Acceptance of the input ω-word requires that this union set be hit infinitely often along the run.\n\n## 4.3 NBA → DRA Determinization (Safra's Construction) — Functional Block Flow\n\n```mermaid\nflowchart TD\n    nbaIn[\"INPUT: NBA A = (Q, Sigma, delta, Q0, F)\"]\n    nbaIn --> nbaTree[\"STEP 1: Build Safra Tree \\n (Ordered tree of Q-subsets,\\n levels labeled by states)\"]\n    nbaTree --> treeUpdate[\"STEP 2: On reading symbol a,\\n for each node S in the tree:\\n - Compute delta*(S, a)\\n - If S is accepting, create child\\n   in NEXT tree containing delta*(S, a)\"]\n    treeUpdate --> merge[\"STEP 3: Merge common subsets\\n across siblings (Safra's key trick)\\n to bound tree size\"]\n    merge --> pairExtract[\"STEP 4: Extract Rabin pairs\\n from horizontal/vertical splits:\\n (L_i, U_i) for each node i\"]\n    pairExtract --> draOut[\"OUTPUT: DRA = (S, Sigma, delta_S, s_0, {(L_i, U_i)})\\n with at most 2^{O(n log n)} states\"]\n    draOut --> acceptCheck[\"STEP 5: Run DRA on w.\\n Accept iff exists pair (L_i, U_i) such that\\n inf intersects L_i but not U_i\"]\n\n    style nbaIn fill:#E8F0FE,stroke:#1A73E8\n    style draOut fill:#FFF3CD,stroke:#F9AB00\n    style acceptCheck fill:#E6F4EA,stroke:#34A853\n```\n\n**Why Safra's construction is the canonical method:** The naive subset-construction fails for NBAs because we must track which accepting states have been visited (Büchi condition), not just the reachable set. Safra's **ordered tree of node subsets** elegantly captures both *which states are currently reachable* (horizontal information) and *which accepting states have been recently visited* (vertical information via the tree hierarchy). The $2^{O(n \\log n)}$ bound is tight — no asymptotically better construction is known.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short-Answer Questions (3 Marks Each)\n\n---\n\n### **Question A1** `[KTU University Exam — May 2024]`\n\n**Q: Define a Nondeterministic Büchi Automaton (NBA). State and explain the Büchi acceptance condition with a suitable example.**\n\n**Model Answer (3 marks):**\n\nA Nondeterministic Büchi Automaton is a 5-tuple $\\mathcal{A} = (Q, \\Sigma, \\delta, Q_0, F)$ where:\n- $Q$ is a finite set of states,\n- $\\Sigma$ is a finite input alphabet,\n- $\\delta: Q \\times \\Sigma \\rightarrow 2^{Q}$ is the nondeterministic transition relation,\n- $Q_0 \\subseteq Q$ is the set of initial states, and\n- $F \\subseteq Q$ is the set of accepting (Büchi) states. **(1 mark)**\n\nAn infinite word $w = a_1 a_2 a_3 \\ldots \\in \\Sigma^{\\omega}$ is accepted by $\\mathcal{A}$ if there exists a run $\\rho = q_0 q_1 q_2 \\ldots$ starting in $Q_0$ and following $\\delta$, such that the set of states appearing infinitely often in $\\rho$ intersects $F$:\n\n$$\\inf(\\rho) \\cap F \\neq \\emptyset \\quad \\textbf{(1 mark)}$$\n\n**Example:** The NBA over $\\Sigma = \\{a,b\\}$ with $Q = \\{q_0, q_1\\}$, $Q_0 = \\{q_0\\}$, $F = \\{q_1\\}$, $\\delta(q_0, a) = \\{q_1\\}$, $\\delta(q_0, b) = \\{q_0\\}$, $\\delta(q_1, a) = \\delta(q_1, b) = \\{q_1\\}$ accepts all ω-words containing $a$ infinitely often. Once the run enters $q_1$ (triggered by the first $a$), it stays in $q_1$ forever, so $q_1 \\in \\inf(\\rho)$. **(1 mark)**\n\n---\n\n### **Question A2** `[KTU University Exam — Dec 2023]`\n\n**Q: Why is the class of Büchi-recognizable languages not closed under complementation by simple subset construction? Explain briefly.**\n\n**Model Answer (3 marks):**\n\nThe classical subset construction determinizes a DFA, but for NBAs the situation is fundamentally harder because of the **Büchi acceptance condition** $\\inf(\\rho) \\cap F \\neq \\emptyset$. **(1 mark)** A deterministic automaton must decide acceptance of an ω-word based on the **single** run it produces, but an NBA may have many runs of which only one is accepting. To complement, we must build a deterministic automaton that accepts a word $w$ **iff none of the NBA's runs on $w$ are accepting**. The classical Rabin-Scott determinization produces a DFA (accepting finite words), not a DRA (accepting ω-words). The fix requires **Safra's construction**, which builds a Deterministic Rabin Automaton (DRA) of size $2^{O(n \\log n)}$. **(1 mark)** This is exponentially more expensive than the linear blow-up of standard subset construction, and it is the reason why NBA complementation is theoretically and practically non-trivial — and is the bottleneck of LTL model checking for the universal fragment (∀LTL). **(1 mark)**\n\n---\n\n## 5.2 Part B — Full 14-Mark Questions (Module Internal Choice)\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall — Büchi Acceptance vs. Reachability:**\n> A very common mistake is to write \"the word is accepted if some accepting state is reached\" — but this is the reachability condition, **not** the Büchi condition. The Büchi condition requires the accepting state to be visited **infinitely often**, not merely once. Students who commit this error typically lose **3 to 4 marks** out of 14. State explicitly: \"$\\inf(\\rho) \\cap F \\neq \\emptyset$\" — the phrase **\"infinitely often\"** must appear in every model answer.\n\n---\n\n### **Part B — Question A (14 Marks)** `[KTU University Exam — May 2024]`\n\n**(a) Construct an NBA over $\\Sigma = \\{a, b\\}$ that accepts exactly those ω-words in which every $a$ is followed eventually by a $b$ (i.e., between any two consecutive $a$'s, at least one $b$ must occur). Prove that your construction is correct. (7 marks)**\n\n**Model Solution:**\n\n**Construction:** Define $\\mathcal{A} = (Q, \\Sigma, \\delta, Q_0, F)$ with:\n- $Q = \\{q_0, q_1, q_2\\}$ where:\n  - $q_0$ = \"no pending $a$\" (initial, accepting),\n  - $q_1$ = \"an $a$ has been seen and we are waiting for a $b$\" (non-accepting),\n  - $q_2$ = \"trap — two $a$'s without an intervening $b$\" (non-accepting, sink).\n- $Q_0 = \\{q_0\\}$\n- $F = \\{q_0\\}$\n- $\\delta$ defined by:\n  - $\\delta(q_0, a) = \\{q_1\\}$\n  - $\\delta(q_0, b) = \\{q_0\\}$\n  - $\\delta(q_1, a) = \\{q_2\\}$\n  - $\\delta(q_1, b) = \\{q_0\\}$\n  - $\\delta(q_2, a) = \\{q_2\\}$\n  - $\\delta(q_2, b) = \\{q_2\\}$ **(2 marks for full tuple)**\n\n**Correctness — Forward direction ($\\Rightarrow$):** Suppose $w \\in L(\\mathcal{A})$. Then there exists a run $\\rho$ with $\\inf(\\rho) \\cap \\{q_0\\} \\neq \\emptyset$, so $q_0$ appears infinitely often. Each time the run enters $q_1$ (which happens only on reading $a$ from $q_0$), it must return to $q_0$ before the next $a$ (because the only way to leave $q_1$ to a non-trap state is on $b$, leading back to $q_0$). Thus every $a$ is followed (eventually, in the same step or later) by a $b$ that returns the run to $q_0$. **(2 marks)**\n\n**Correctness — Backward direction ($\\Leftarrow$):** Suppose every $a$ in $w$ is eventually followed by a $b$. Construct the canonical run: start at $q_0$, transition to $q_1$ on $a$, return to $q_0$ on the first $b$ after that $a$, and stay in $q_0$ on every $b$ that doesn't follow an $a$. Because $a$ is always followed by some $b$, the run never reaches $q_2$. Since $q_0$ is reached on every $b$ and on every $a$ immediately following such a $b$, $q_0$ appears infinitely often (as $w$ is infinite and contains infinitely many symbols), so $\\inf(\\rho) \\ni q_0 \\in F$. Hence $w$ is accepted. **(2 marks)**\n\n**State-diagram (for full credit):**\n```\nq0 --a--> q1 --b--> q0\nq0 --b--> q0\nq1 --a--> q2 (trap, self-loops on both a and b)\n```\n**(1 mark for diagram)**\n\n---\n\n**(b) Prove that the class of NBA-recognizable languages is closed under union. Illustrate the product construction on the automata from part (a) and a similar NBA that requires $b$ to appear infinitely often. (7 marks)**\n\n**Model Solution:**\n\n**Theorem:** If $L_1, L_2$ are Büchi-recognizable, so is $L_1 \\cup L_2$.\n\n**Construction:** Given $\\mathcal{A}_1 = (Q_1, \\Sigma, \\delta_1, Q_{0,1}, F_1)$ and $\\mathcal{A}_2 = (Q_2, \\Sigma, \\delta_2, Q_{0,2}, F_2)$, build:\n\n$$\\mathcal{A}_{\\cup} = (Q_1 \\times Q_2,\\ \\Sigma,\\ \\delta_{\\cup},\\ Q_{0,1} \\times Q_{0,2},\\ F_{\\cup})$$\n\nwhere $\\delta_{\\cup}((p,q), a) = \\delta_1(p, a) \\times \\delta_2(q, a)$ and $F_{\\cup} = (F_1 \\times Q_2) \\cup (Q_1 \\times F_2)$. **(2 marks)**\n\n**Proof of correctness:** Let $w \\in L(\\mathcal{A}_{\\cup})$, so there is a run $\\rho$ of $\\mathcal{A}_{\\cup}$ on $w$ with $\\inf(\\rho) \\cap F_{\\cup} \\neq \\emptyset$. The two projections $\\pi_1(\\rho)$ and $\\pi_2(\\rho)$ are valid runs of $\\mathcal{A}_1$ and $\\mathcal{A}_2$ on $w$. Since infinitely many product states are in $F_{\\cup}$, infinitely many of them are in $F_1 \\times Q_2$ or $Q_1 \\times F_2$. In the first case, $F_1$ is hit infinitely often by $\\pi_1(\\rho)$, so $w \\in L(\\mathcal{A}_1) \\subseteq L(\\mathcal{A}_1) \\cup L(\\mathcal{A}_2)$. The second case is symmetric. The reverse direction is by the standard \"witness extension\" argument. **(3 marks)**\n\n**Illustration:** Take $\\mathcal{A}_1$ from part (a) (i.e., every $a$ is followed by a $b$) with $Q_1 = \\{p_0, p_1, p_2\\}$, $F_1 = \\{p_0\\}$, and a second NBA $\\mathcal{A}_2$ requiring $b$ infinitely often: $Q_2 = \\{r_0, r_1\\}$, $Q_{0,2} = \\{r_0\\}$, $F_2 = \\{r_1\\}$, $\\delta_2(r_0, a) = \\{r_0\\}$, $\\delta_2(r_0, b) = \\{r_1\\}$, $\\delta_2(r_1, a) = \\delta_2(r_1, b) = \\{r_1\\}$.\n\nThe product $\\mathcal{A}_{\\cup}$ has $3 \\times 2 = 6$ states: $(p_0, r_0), (p_0, r_1), (p_1, r_0), (p_1, r_1), (p_2, r_0), (p_2, r_1)$. The accepting set is $F_{\\cup} = \\{(p_0, r_0), (p_0, r_1), (p_1, r_0), (p_1, r_1)\\} \\cup \\{(p_0, r_1), (p_1, r_1), (p_2, r_1)\\}$. A sample accepted word is $a b a b a b \\ldots$ **(2 marks)**\n\n---\n\n### **Part B — Question B (14 Marks)** `[KTU University Exam — Dec 2023]`\n\n**(a) State and prove the closure of NBA under intersection. Explain why a direct product construction yields a Generalized Büchi Automaton (NGA) rather than a Büchi automaton. (7 marks)**\n\n**Model Solution:**\n\n**Theorem:** If $L_1, L_2$ are Büchi-recognizable, so is $L_1 \\cap L_2$.\n\n**Step 1 — Direct product yields an NGA:** Define\n\n$$\\mathcal{A}_{\\cap} = (Q_1 \\times Q_2,\\ \\Sigma,\\ \\delta_{\\cap},\\ Q_{0,1} \\times Q_{0,2},\\ \\{F^{(1)}, F^{(2)}\\})$$\n\nwith $\\delta_{\\cap}((p,q), a) = \\delta_1(p, a) \\times \\delta_2(q, a)$, $F^{(1)} = F_1 \\times Q_2$, $F^{(2)} = Q_1 \\times F_2$. **(2 marks)**\n\nThis is an **NGA** (not NBA) because it has **two** acceptance sets; the NGA acceptance condition requires $\\inf(\\rho) \\cap F^{(1)} \\neq \\emptyset$ **and** $\\inf(\\rho) \\cap F^{(2)} \\neq \\emptyset$, i.e., a conjunction rather than a single disjunction. **Why a direct NBA construction fails:** A naive attempt to set $F_{\\cap} = F_1 \\times F_2$ is incorrect. Consider $w$ where the run of $\\mathcal{A}_{\\cap}$ alternates between visiting $F^{(1)}$ and $F^{(2)}$ at different times: $F_1 \\times F_2$ might be hit only finitely often, but both $F^{(1)}$ and $F^{(2)}$ are hit infinitely often — so the word *should* be accepted but would be rejected. **(2 marks)**\n\n**Step 2 — NGA to NBA reduction:** Introduce $k = 2$ \"phases\" (one per acceptance set):\n\n$$Q' = Q_1 \\times Q_2 \\times \\{1, 2\\}$$\n\nInitial states: $Q_{0,1} \\times Q_{0,2} \\times \\{1\\}$. The accepting set is:\n\n$$F' = (F_1 \\times Q_2 \\times \\{1\\}) \\cup (Q_1 \\times F_2 \\times \\{2\\})$$\n\nThe transition advances the phase from $j$ to $j+1 \\pmod{k}$ **only** if the current state is in $F^{(j)}$. Because the phase index must cycle through all $k$ values infinitely often, and each cycle through a phase requires a visit to $F^{(j)}$, every $F^{(j)}$ is hit infinitely often — exactly the NGA condition. **(2 marks)**\n\n**Step 3 — Correctness:** Any run of $\\mathcal{A}_{\\cap}$ satisfying the NGA condition can be \"lifted\" to a run of $\\mathcal{A}'$ that visits $F'$ infinitely often (by tracking the phase), and conversely. So $L(\\mathcal{A}_{\\cap}) = L(\\mathcal{A}')$ and the latter is a genuine NBA. **(1 mark)**\n\n---\n\n**(b) Convert the LTL formula $\\varphi = \\mathbf{G}(a \\rightarrow \\mathbf{F} b)$ into an equivalent NBA. Explain each construction step. (7 marks)**\n\n**Model Solution:**\n\n**LTL semantics:** $\\varphi = \\mathbf{G}(a \\rightarrow \\mathbf{F} b)$ means \"globally, whenever $a$ holds, $b$ will hold at some future moment\". The negation $\\neg \\varphi = \\mathbf{F}(a \\land \\mathbf{G} \\neg b)$ says \"there exists a position where $a$ holds and from then on $b$ never holds\". An NBA for $\\neg \\varphi$ is constructed and then complemented (via Safra) — but for this problem, we construct the NBA for $\\varphi$ directly by tableau-style construction. **(1 mark)**\n\n**Step 1 — Closure of subformulas:** $\\mathrm{cl}(\\varphi) = \\{a, b, a \\rightarrow \\mathbf{F} b, \\mathbf{F} b, \\neg a, \\neg b, \\neg(a \\rightarrow \\mathbf{F} b)\\}$. **(1 mark)**\n\n**Step 2 — States as consistent subsets:** Each state of the NBA is a maximally consistent subset of $\\mathrm{cl}(\\varphi)$ — a set containing either $\\psi$ or $\\neg \\psi$ for every $\\psi \\in \\mathrm{cl}(\\varphi)$, satisfying local logical consistency. **(1 mark)**\n\n**Step 3 — Transition condition:** A state $s$ can transition to $s'$ on symbol $a$ (an interpretation of atomic propositions) iff:\n- $s$ and $s'$ agree on the **current values** of atomic propositions (i.e., $a \\in s$ iff $a$ is true at the current step),\n- For every \"next-time\" obligation $\\mathbf{X}\\psi$: $\\mathbf{X}\\psi \\in s$ iff $\\psi \\in s'$,\n- For every \"until/future\" obligation: standard tableau expansion rules apply. **(2 marks)**\n\n**Step 4 — Accepting set $F$:** The Büchi condition requires that every \"promise\" $\\mathbf{F} \\psi$ that was issued in $s$ must eventually be **fulfilled**: any state where $\\mathbf{F} b$ is pending but $b$ is not in scope must mark a \"fulfillment\" obligation. The standard construction partitions states into those that contain $\\mathbf{F} b$ and require a future visit, and marks accepting states as those that have **just fulfilled** this obligation. For the formula $\\mathbf{G}(a \\rightarrow \\mathbf{F} b)$, the set $F$ consists of states that contain $b$ (because each such state \"fulfills\" any pending $\\mathbf{F} b$ promise). **(1 mark)**\n\n**Step 5 — Resulting NBA:** The resulting automaton has at most $2^{|\\mathrm{cl}(\\varphi)|} = 2^{7} = 128$ states (in the worst case, fewer after simplification). It accepts an ω-word $w$ iff $w \\models \\varphi$. **(1 mark)**\n\n**Verification trace:** On the input $w = a, b, a, b, a, b, \\ldots$ (where $a$ and $b$ alternate), the run visits a state in $F$ at every step (because $b$ is true at every even step), so $\\inf(\\rho) \\cap F \\neq \\emptyset$ and the word is accepted — which matches the LTL semantics. On the input $w = a, \\neg b, \\neg b, \\neg b, \\ldots$ the run enters a non-accepting cycle, and acceptance fails — also matching LTL. **(Included for full credit.)**\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!NOTE]\n> **Rapid Revision Checklist — Nondeterministic Büchi Automata (NBA)**\n\n- **Definition:** $\\mathcal{A} = (Q, \\Sigma, \\delta, Q_0, F)$ — a 5-tuple. Memorize the role of each component. **(high-weight)**\n- **Acceptance condition:** $\\inf(\\rho) \\cap F \\neq \\emptyset$ — **infinitely often**, not \"once\". **(most-missed concept)**\n- **$\\inf(\\rho)$** = set of states occurring infinitely often in run $\\rho$. By the pigeonhole principle, $\\inf(\\rho) \\neq \\emptyset$ for any infinite run on a finite-state automaton.\n- **Nondeterminism** appears in $\\delta: Q \\times \\Sigma \\rightarrow 2^{Q}$ — set-valued, not single-valued.\n- **NBA closure properties (memorize this exact list):**\n  - ✅ Closed under **union** (direct product, $F_{\\cup} = (F_1 \\times Q_2) \\cup (Q_1 \\times F_2)$).\n  - ✅ Closed under **intersection** (product gives NGA, then reduce to NBA via $k$-phase construction).\n  - ❌ **Not closed under complementation** — requires Safra's $2^{O(n \\log n)}$ construction.\n  - ✅ Closed under projection (existential quantification) onto a sub-alphabet.\n- **NBA → DRA determinization (Safra):** produces $2^{O(n \\log n)}$ states; tight bound (no better general construction).\n- **LTL → NBA:** exponential in formula length ($2^{\\mathcal{O}(|\\varphi|)}$); the standard tableau construction.\n- **Model checking use:** system $\\mathcal{M}$ satisfies $\\varphi$ iff $L(\\mathcal{A}_{\\mathcal{M}}) \\cap L(\\mathcal{A}_{\\neg\\varphi}) = \\emptyset$ (language emptiness of the synchronized product).\n- **Deterministic vs. Nondeterministic distinction:** every DFA is an NBA (with $F$ visited exactly once at the end is equivalent, since runs are unique). The converse is false.\n- **Trap states / sinks:** non-accepting states with self-loops on every symbol; often used in constructions to enforce violation of properties.\n- **Common exam pitfall:** confusing Büchi acceptance ($\\inf$) with reachability (single visit), and confusing NGA acceptance (conjunction over acceptance sets) with NBA acceptance (disjunction over accepting states).\n- **Sanity check for exam answers:** always specify **why** the accepting states are visited infinitely often — not just that they are visited at all.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 2-t5",
          "title": "Deterministic Büchi Automata (DBA)",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 2\n> - **Topic:** Deterministic Büchi Automata (DBA)\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\nA **Deterministic Büchi Automaton (DBA)** is a quintuple\n\n$$\n\\mathcal{A} = (Q,\\ \\Sigma,\\ \\delta,\\ q_0,\\ F)\n$$\n\nwhere each component is defined as follows:\n\n- $Q$ is a **finite, non-empty** set of states.\n- $\\Sigma$ is a **finite alphabet** of input symbols.\n- $\\delta : Q \\times \\Sigma \\rightarrow Q$ is a **total deterministic transition function** (exactly one successor for every state-symbol pair).\n- $q_0 \\in Q$ is the unique **initial state**.\n- $F \\subseteq Q$ is the set of **accepting (final) states**.\n\nA DBA is structurally identical to a DFA, but it consumes **infinite words** $w = a_0 a_1 a_2 \\dots \\in \\Sigma^{\\omega}$ and uses a **Büchi acceptance condition**: a run is accepting iff it visits $F$ **infinitely often**.\n\n> [!IMPORTANT]\n> **Büchi Acceptance Rule:** A run $\\rho = q_0\\, q_1\\, q_2\\, \\dots$ is *accepting* if and only if\n> $$\\inf(\\rho) \\cap F \\neq \\varnothing$$\n> i.e., at least one accepting state appears infinitely many times in the run.\n\n## 1.2 Conceptual Analogy — A Night Security Guard\n\nImagine a **night security guard** patrolling a building with several rooms. He follows a strict schedule:\n\n1. He is given an **infinite list** of instructions (the input word), one instruction per second.\n2. From **any room, for any instruction, there is exactly one next room** he must walk to — no choice, no ambiguity (determinism).\n3. A small set of rooms are marked \"**VIP Rooms**\" (the accepting set $F$).\n4. The patrol is considered *successful* if the guard **keeps entering VIP rooms forever** — at least once every now and then, infinitely often.\n\nIf at some point he walks past VIP rooms only finitely many times, the patrol **fails**, even if he keeps walking. The DBA formalizes this exact idea: an input infinite word is accepted iff the deterministic walk revisits $F$ infinitely often.\n\n> [!NOTE]\n> **KTU 2024 Scheme Highlight:** A DBA processes *infinite* traces because reactive systems (operating systems, hardware circuits, communication protocols) are non-terminating. The Büchi condition elegantly captures \"good behavior occurs repeatedly forever\".\n\n## 1.3 Physical / Engineering Constants\n\n- **Alphabet size** $|\\Sigma|$ — typically $2$ for boolean signals, up to $2^k$ for $k$-bit data words.\n- **State count** $|Q|$ — usually $10^1$ to $10^6$ in industrial model checkers.\n- **Emptiness-check complexity** $\\mathcal{O}(|Q|)$ — linear in the number of states (using Tarjan's SCC algorithm).\n- **Safra's construction blowup** from NBA to DBA: at most $2^{n} \\cdot n!$ states, where $n = |Q_{\\text{NBA}}|$.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Determinism vs. Nondeterminism — The Central Distinction\n\nThe transition function behaves fundamentally differently:\n\n| Property | Nondeterministic Büchi Automaton (NBA) | Deterministic Büchi Automaton (DBA) |\n|---|---|---|\n| Transition type | $\\delta : Q \\times \\Sigma \\rightarrow 2^{Q}$ | $\\delta : Q \\times \\Sigma \\rightarrow Q$ |\n| Successor count | Any number (incl. $0$, $>1$) | **Exactly one** |\n| Run count per word | Many (tree of runs) | **Exactly one** |\n| Expressive power | **Full** $\\omega$-regular | **Strict subset** of $\\omega$-regular |\n\n> [!IMPORTANT]\n> **Expressivity Gap:** There exist $\\omega$-regular languages accepted by an NBA but **by no DBA** whatsoever. The classic counter-example is\n> $$L = (a+b)^{*}a^{\\omega} \\cup b^{\\omega}$$\n> which is NBA-recognizable but **not** DBA-recognizable.\n\n## 2.2 Operational Semantics (Step-by-Step)\n\nGiven input $\\omega$-word $w = a_0\\, a_1\\, a_2\\, \\dots$:\n\n1. **Initialize:** Set current state $q := q_0$, set $i := 0$.\n2. **Loop forever:**\n   - Read the next symbol $a_i$.\n   - Update $q := \\delta(q, a_i)$.\n   - Record whether $q \\in F$.\n   - Increment $i := i+1$.\n3. **Decide:** If accepting states have been visited *infinitely often*, **ACCEPT**; else **REJECT**.\n\n## 2.3 KTU Formula Sheet\n\n$$\n\\begin{array}{|l|c|c|}\n\\hline\n\\textbf{Quantity} & \\textbf{Formula} & \\textbf{Notes / Units} \\\\\n\\hline\n\\text{State-space size} & \\vert Q \\vert & \\text{dimensionless integer} \\\\\n\\hline\n\\text{Transition function} & \\delta : Q \\times \\Sigma \\rightarrow Q & \\text{total function} \\\\\n\\hline\n\\text{Acceptance (Büchi)} & \\inf(\\rho) \\cap F \\neq \\varnothing & \\text{set intersection} \\\\\n\\hline\n\\text{Emptiness check} & \\mathcal{O}(\\vert Q \\vert + \\vert\\delta\\vert) & \\text{Tarjan SCC} \\\\\n\\hline\n\\text{NBA} \\rightarrow \\text{DBA (Safra)} & 2^{n} \\cdot n! & n = \\vert Q_{\\text{NBA}}\\vert \\\\\n\\hline\n\\text{Complement of DBA} & \\mathcal{O}(2^{n}) & \\text{standard determinization} \\\\\n\\hline\n\\text{Product DBA} & \\vert Q_1\\vert \\cdot \\vert Q_2\\vert & \\text{component-wise product} \\\\\n\\hline\n\\end{array}\n$$\n\n> [!NOTE]\n> **Engineering Utility:** DBAs underpin **SPIN's** LTL-to-Büchi translation, **Cadence SMV's** synchronous model-checking engine, and **hardware fair-arbitration verification** where every cycle must see every requester infinitely often.\n\n## 2.4 Why DBAs Matter in Verification\n\n- **Reactive system specs** (LTL formulas of the form $\\Box \\Diamond p$) reduce naturally to Büchi acceptance.\n- **Determinism enables complementation cheaply** — a DBA for $\\Sigma^{\\omega} \\setminus L$ is obtained by swapping $F \\leftrightarrow Q \\setminus F$ (incorrect for NBA, correct for DBA).\n- **Canonical construction** for synchronous products in compositional verification.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: Emptiness-Check Algorithm for a DBA\n\n**Theorem.** $L(\\mathcal{A}) \\neq \\varnothing$ iff there exists a state $q \\in F$ that lies on a cycle **reachable from $q_0$**.\n\n**Proof.** We prove both directions explicitly.\n\n### $(\\Rightarrow)$ Assume $L(\\mathcal{A}) \\neq \\varnothing$.\n\nThen there exists an accepted infinite word $w = a_0 a_1 a_2 \\dots \\in \\Sigma^{\\omega}$.\nLet $\\rho = q_0\\, q_1\\, q_2\\, \\dots$ be the unique run on $w$.\n\nBecause $w$ is accepted, $\\rho$ visits $F$ infinitely often. Pick two distinct positions $i < j$ with $q_i \\in F$ and $q_j \\in F$. Since the run is deterministic, the segment\n\n$$\nq_i \\xrightarrow{a_i\\, a_{i+1}\\, \\dots\\, a_{j-1}} q_j\n$$\n\nis a cycle (state $q_i$ is revisited at position $j$), and $q_i \\in F$. Hence an accepting state on a reachable cycle exists.\n\n### $(\\Leftarrow)$ Assume such $q \\in F$ exists on a reachable cycle.\n\nReachability gives a finite path $\\pi : q_0 \\to q$. The cycle gives a non-empty finite loop $\\gamma : q \\to q$ reading some word $u \\in \\Sigma^{+}$.\n\nDefine the $\\omega$-word\n\n$$\nw = \\pi\\ \\gamma\\ \\gamma\\ \\gamma\\ \\dots\n$$\n\nwhich is the path to $q$ followed by infinitely many repetitions of the cycle. The corresponding run stays forever inside the cycle and visits $q \\in F$ infinitely often. Hence $w \\in L(\\mathcal{A})$, so $L(\\mathcal{A}) \\neq \\varnothing$. $\\blacksquare$\n\n## 3.2 Analytical Derivation: Subset Construction for NBA $\\rightarrow$ DBA\n\nTo convert an NBA into an equivalent DBA, the standard approach (Safra) replaces the power-set construction with a *tree-of-subsets* construction. A simplified derivation follows.\n\n**Step 1 — Initial state of DBA.**\n\n$$\nq_0^{\\text{DBA}} = \\text{wrap}(\\{q_0^{\\text{NBA}}\\}, \\text{level}=0)\n$$\n\nEach DBA state is a *node* holding a subset of NBA states.\n\n**Step 2 — Transition on symbol $a$.**\n\n$$\nS' = \\bigcup_{q \\in S} \\delta_{\\text{NBA}}(q, a)\n$$\n\n**Step 3 — Accepting-state tracking.**\n\nFor each $S$ that contains at least one $q \\in F_{\\text{NBA}}$, we record a \"fresh track\" so that we know when the Büchi condition has been \"freshly\" satisfied since the last visit to $F$.\n\n**Step 4 — State size bound.**\n\nIf the NBA has $n$ states, the number of distinct subsets is $2^{n}$. Tracks are subsets of the active subsets, giving at most $n$ ordered tracks. The total bound is therefore\n\n$$\n\\begin{aligned}\n|Q_{\\text{DBA}}| &\\leq 2^{n} \\cdot n! \\\\\n\\log_2 |Q_{\\text{DBA}}| &\\leq n + \\log_2(n!) \\\\\n&\\leq n + n \\log_2 n \\quad \\text{(using Stirling's approximation)}.\n\\end{aligned}\n$$\n\n## 3.3 Analytical Matrix — Acceptance of $L = (ab)^{\\omega}$\n\nConsider the DBA $\\mathcal{A}_1$ for the language $(ab)^{\\omega}$ over $\\Sigma = \\{a,b\\}$.\n\n$$\n\\begin{array}{|c|c|c|c|}\n\\hline\n\\textbf{Current state} & \\textbf{On } a & \\textbf{On } b & \\textbf{Accepting?} \\\\\n\\hline\nq_0 \\text{ (start)} & q_1 & q_3 & \\text{No} \\\\\n\\hline\nq_1 & q_3 & q_2 & \\text{No} \\\\\n\\hline\nq_2 & q_1 & q_3 & \\textbf{Yes} \\\\\n\\hline\nq_3 \\text{ (trap)} & q_3 & q_3 & \\text{No} \\\\\n\\hline\n\\end{array}\n$$\n\n**Run on $w = ababab\\dots$:**\n\n$$\nq_0 \\xrightarrow{a} q_1 \\xrightarrow{b} q_2 \\xrightarrow{a} q_1 \\xrightarrow{b} q_2 \\xrightarrow{a} q_1 \\xrightarrow{b} \\dots\n$$\n\nState $q_2 \\in F$ is visited at positions $2, 4, 6, \\dots$ — **infinitely often**. Hence $w \\in L(\\mathcal{A}_1)$. $\\checkmark$\n\nRun on $w = aabab\\dots$ hits $q_3$ and stays there; $q_3 \\notin F$, so the run never revisits $F$ after that. **Rejected.** $\\checkmark$\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 State Diagram of the Running DBA $\\mathcal{A}_1$\n\n```mermaid\nflowchart LR\n    nodeQ0((q0\\nstart))\n    nodeQ1((q1))\n    nodeQ2((q2\\nACCEPT))\n    nodeQ3((q3\\ntrap))\n\n    nodeQ0 -- a --> nodeQ1\n    nodeQ0 -- b --> nodeQ3\n    nodeQ1 -- a --> nodeQ3\n    nodeQ1 -- b --> nodeQ2\n    nodeQ2 -- a --> nodeQ1\n    nodeQ2 -- b --> nodeQ3\n    nodeQ3 -- a --> nodeQ3\n    nodeQ3 -- b --> nodeQ3\n```\n\n## 4.2 NBA-to-DBA Translation Architecture (Block Flow)\n\n```mermaid\nflowchart TB\n    subgraph INPUT[Input Layer]\n        NBA[\"NBA\\n(Q, Sigma, delta_N, q0, F)\"]\n        InputWord[\"Infinite word w = a0 a1 a2 ...\"]\n    end\n\n    subgraph ENGINE[Determinization Engine]\n        Subset[\"Subset Builder\\nS' = union of delta_N(q, a)\"]\n        Tracker[\"Acceptance Tracker\\nMark when F is freshly hit\"]\n        SafraTree[\"Safra Tree Builder\\nTrack ordering + pruning\"]\n    end\n\n    subgraph OUTPUT[DBA Layer]\n        DBAState[\"DBA State\\n(T, marking)\"]\n        AcceptLoop[\"Loop in Q_DBA\\ninfinite visit to F\"]\n    end\n\n    NBA --> Subset\n    Subset --> Tracker\n    Tracker --> SafraTree\n    SafraTree --> DBAState\n    InputWord --> DBAState\n    DBAState --> AcceptLoop\n```\n\n## 4.3 Functional Architecture: Model-Checking Pipeline\n\n```mermaid\nflowchart LR\n    Spec[\"LTL Spec phi\"] --> Neg[\"Negate phi\"]\n    Neg --> NBA[\"NBA for not phi\"]\n    NBA -->|\"Safra\\n2^n * n!\"| DBA[\"Equivalent DBA\"]\n    DBA --> Product[\"Product with system K\"]\n    Product --> Emptiness[\"Emptiness Check\\nO(|Q|)\"]\n    Emptiness --> Verdict[\"Counterexample\\nOR Verified\"]\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. `[KTU University Exam – May 2024]`\n**Define a Deterministic Büchi Automaton (DBA). State the Büchi acceptance condition precisely.**\n\n**Model Answer (3 marks):**\n\nA DBA is a 5-tuple $\\mathcal{A} = (Q, \\Sigma, \\delta, q_0, F)$ where\n$Q$ is a finite set of states, $\\Sigma$ a finite alphabet, $q_0 \\in Q$ the initial state, $F \\subseteq Q$ the accepting set, and $\\delta : Q \\times \\Sigma \\rightarrow Q$ a **total** deterministic transition function. **(1 mark)**\n\nFor an infinite word $w = a_0 a_1 a_2 \\dots \\in \\Sigma^{\\omega}$ there is a unique run $\\rho = q_0\\, q_1\\, q_2\\, \\dots$ with $q_{i+1} = \\delta(q_i, a_i)$. **(1 mark)**\n\nThe word is *accepted* iff $\\inf(\\rho) \\cap F \\neq \\varnothing$, i.e., some accepting state is visited **infinitely often**. **(1 mark)**\n\n---\n\n### Q2. `[KTU University Exam – Dec 2023]`\n**Give one $\\omega$-regular language accepted by a DBA and one that is NBA-recognizable but NOT DBA-recognizable. Justify the second claim.**\n\n**Model Answer (3 marks):**\n\n- DBA-acceptable: $L_1 = (ab)^{\\omega}$ — accepted by a 4-state DBA (see §3.3). **(1 mark)**\n- Not DBA-acceptable: $L_2 = (a+b)^{*}a^{\\omega} \\cup b^{\\omega}$ — recognizable by a 3-state NBA. **(1 mark)**\n- **Justification:** Any DBA must commit, on seeing its first $a$, to entering a cycle of $a$'s. But if $b$ is seen later, the run cannot recover and stay in $L_2$. The language violates the \"deterministic prefix-class\" property. **(1 mark)**\n\n---\n\n## 5.2 Part B — 14-Mark Questions (Module Internal Choice)\n\n### Question A — 14 Marks `[KTU University Exam – May 2024]`\n\n**(a)** *Construct a DBA for the language*\n$$L = (a b^{*} a)^{\\omega}.$$\n*Clearly specify $Q, \\Sigma, \\delta, q_0, F$, and show that your construction is deterministic. **(7 marks)***\n\n**Model Solution (a):**\n\n- $\\Sigma = \\{a, b\\}$.\n- States: $Q = \\{q_0, q_1, q_2, q_3\\}$.\n  - $q_0$ = \"start, expect first $a$\".\n  - $q_1$ = \"inside block, reading $b^{*}$\".\n  - $q_2$ = \"block completed, expect next $a$\".\n  - $q_3$ = \"trap / dead\".\n- $q_0 = q_0$, $F = \\{q_2\\}$.\n- Transition function:\n\n$$\n\\begin{aligned}\n\\delta(q_0, a) &= q_1, & \\delta(q_0, b) &= q_3 \\\\\n\\delta(q_1, a) &= q_2, & \\delta(q_1, b) &= q_1 \\\\\n\\delta(q_2, a) &= q_1, & \\delta(q_2, b) &= q_3 \\\\\n\\delta(q_3, a) &= q_3, & \\delta(q_3, b) &= q_3\n\\end{aligned}\n$$\n\nEvery row has **exactly one entry per column**, so the function is total and deterministic. **(7 marks breakdown: 1 for $Q,\\Sigma$, 2 for $\\delta$, 1 for $q_0,F$, 3 for determinism + run demonstration)**\n\n**(b)** *Explain the Büchi acceptance condition. Prove that the emptiness problem for DBA is decidable in $\\mathcal{O}(|Q|)$ time. **(7 marks)***\n\n**Model Solution (b):**\n\n**Büchi acceptance (2 marks):** As in §2.2 — a run is accepting iff $\\inf(\\rho) \\cap F \\neq \\varnothing$.\n\n**Emptiness is $\\mathcal{O}(|Q|)$ (5 marks):**\n- Build the directed graph of $\\mathcal{A}$ (vertices $Q$, edges from $\\delta$).\n- Run **Tarjan's SCC algorithm**, which is linear in the number of edges.\n- Check whether any SCC $C$ satisfies $C \\cap F \\neq \\varnothing$ **and** $|C| \\geq 1$ cycle (i.e., non-trivial SCC).\n- If yes, the cycle yields an accepted $\\omega$-word by repeating it forever. If no, $L(\\mathcal{A}) = \\varnothing$.\n\nTarjan's algorithm processes each vertex once: $\\mathcal{O}(|Q| + |\\delta|) = \\mathcal{O}(|Q|)$ when the alphabet is fixed. $\\blacksquare$\n\n---\n\n### Question B — 14 Marks `[KTU University Exam – Dec 2023]`\n\n**(a)** *Convert the following NBA over $\\Sigma = \\{a,b\\}$ to an equivalent DBA via the subset construction, and prove equivalence. **(7 marks)***\n- $Q = \\{p_0, p_1\\}$, $q_0 = p_0$, $F = \\{p_1\\}$.\n- $\\delta(p_0, a) = \\{p_0, p_1\\}$, $\\delta(p_0, b) = \\{p_0\\}$,\n- $\\delta(p_1, a) = \\varnothing$, $\\delta(p_1, b) = \\{p_1\\}$.\n\n**Model Solution (a):**\n\nSubset states of DBA: $2^{\\{p_0, p_1\\}} = \\{\\varnothing, \\{p_0\\}, \\{p_1\\}, \\{p_0, p_1\\}\\}$.\n- Initial: $S_0 = \\{p_0\\}$.\n- $F_{\\text{DBA}}$ = subsets containing some $p_1$ → $\\{\\{p_1\\}, \\{p_0, p_1\\}\\}$.\n- Transitions computed via $\\delta_{\\text{DBA}}(S, x) = \\bigcup_{q \\in S} \\delta_{\\text{NBA}}(q, x)$:\n\n$$\n\\begin{aligned}\n\\delta_{\\text{DBA}}(\\{p_0\\}, a) &= \\{p_0, p_1\\}, & \\delta_{\\text{DBA}}(\\{p_0\\}, b) &= \\{p_0\\} \\\\\n\\delta_{\\text{DBA}}(\\{p_0, p_1\\}, a) &= \\{p_0, p_1\\}, & \\delta_{\\text{DBA}}(\\{p_0, p_1\\}, b) &= \\{p_0, p_1\\}\n\\end{aligned}\n$$\n\nReachable states reduce to $\\{\\{p_0\\}, \\{p_0,p_1\\}\\}$. **Equivalence:** every NBA run has a leaf corresponding to the unique deterministic path of the DBA. Büchi condition preserved because the accepting subset is reached whenever NBA could be. **(7 marks)**\n\n**(b)** *Discuss the expressivity gap between NBA and DBA. Show that $L = (a+b)^{*}a^{\\omega}$ has no DBA. **(7 marks)***\n\n**Model Solution (b):**\n\n**Expressivity gap (2 marks):** NBA accept **all** $\\omega$-regular languages; DBA accept only those that are *deterministic*, which form a strict subset. Safra's construction converts NBA → DBA with at most $2^{n} \\cdot n!$ states, and the bound is essentially tight.\n\n**No DBA for $L$ (5 marks):**\n- Suppose, for contradiction, some DBA $\\mathcal{A} = (Q, \\Sigma, \\delta, q_0, F)$ accepts $L$.\n- Consider the run on $a^{\\omega}$: since $a^{\\omega} \\in L$, the run must visit $F$ infinitely often. So the run $\\rho_a$ is eventually periodic, say entering a cycle $C_a \\subseteq F$ consisting only of $a$-transitions.\n- Now consider the run on $a^k b\\, a^{\\omega}$ for any $k \\geq 0$. Because this word is in $L$ (it ends in $a^{\\omega}$), the run must also be accepting.\n- Because the DBA is deterministic, the prefix $a^k b$ forces a unique state $q_k$. From $q_k$, the continuation $a^{\\omega}$ must drive the run to visit $F$ infinitely often — i.e., $q_k$ must be in a cycle of $a$-transitions intersecting $F$.\n- But there are infinitely many $k$, while $Q$ is finite. By pigeonhole, two prefixes $a^{k_1} b$ and $a^{k_2} b$ end in the same state. Both lead to the same $a$-cycle, hence the word $a^{k_1} b a^{k_2} b a^{\\omega}$ is accepted, yet it does **not** lie in $L$ (it has $b$'s after the first $b$). **Contradiction.** $\\blacksquare$\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Pitfall Callout — Mark-Loss Hot Spots**\n> 1. Forgetting to declare $\\delta$ as **total** (DBA requires every (state, symbol) to have a transition). -1 mark.\n> 2. Confusing \"visited once\" with \"visited infinitely often\" in the Büchi condition. -2 marks.\n> 3. For 14-mark questions, omitting the **proof of equivalence** when asked to convert NBA → DBA. -3 marks.\n> 4. Using the DFA-style \"final state\" condition instead of Büchi. -3 marks.\n> 5. Claiming \"all $\\omega$-regular languages are DBA-recognizable\" — the expressivity gap is a **favorite examiner trap**.\n\n---\n\n## 5.3 Topic Recap & Rapid-Revision Checklist\n\n- A **DBA** is a DFA over infinite words with Büchi acceptance.\n- Components: $Q, \\Sigma, \\delta : Q \\times \\Sigma \\rightarrow Q$ (total!), $q_0, F$.\n- **Acceptance:** $\\inf(\\rho) \\cap F \\neq \\varnothing$ — visit $F$ **infinitely often**.\n- **Expressivity:** strictly weaker than NBA; Safra's construction gives $2^{n} \\cdot n!$ blowup.\n- **Emptiness check:** $\\mathcal{O}(|Q|)$ via SCCs — fast, deterministic.\n- **Complementation:** trivial swap $F \\leftrightarrow Q \\setminus F$ (unlike NBA).\n- **Counter-example:** $L = (a+b)^{*}a^{\\omega} \\cup b^{\\omega}$ is NBA-only.\n- **Engineering use:** LTL model checking, fair-termination verification, hardware liveness proofs.\n- **Pitfalls:** total transition function, infinite vs. finite visits, DBA $\\neq$ NBA power.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 2-t6",
          "title": "Generalised Büchi Automata (Definitions only)",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 2\n> - **Topic:** Generalised Büchi Automata (Definitions only)\n\n<!-- SECTION_1_START -->\n# Section 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of Generalised Büchi Automaton (GBA)\n\nA **Generalised Büchi Automaton (GBA)** is a non-deterministic finite automaton over infinite words whose acceptance condition requires that the automaton visits **multiple (a finite set of) accepting states infinitely often**, rather than just a single set of accepting states as in a classical Büchi automaton.\n\nFormally, a GBA is defined as a 5-tuple:\n\n$$\n\\mathcal{A} = (\\Sigma,\\ Q,\\ Q_0,\\ \\delta,\\ \\mathcal{F})\n$$\n\nwhere:\n\n| Component | Meaning |\n|---|---|\n| $\\Sigma$ | A finite, non-empty alphabet of input symbols |\n| $Q$ | A finite, non-empty set of states |\n| $Q_0 \\subseteq Q$ | The set of initial states |\n| $\\delta : Q \\times \\Sigma \\rightarrow 2^{Q}$ | The non-deterministic transition relation |\n| $\\mathcal{F} = \\{F_1, F_2, \\ldots, F_k\\}$ | A **finite non-empty family of accepting sets**, with each $F_i \\subseteq Q$ |\n\nThe accepting condition is:\n\n> A run $\\rho = q_0 q_1 q_2 \\ldots$ over an infinite word $w = a_0 a_1 a_2 \\ldots \\in \\Sigma^{\\omega}$ is **accepting** if and only if for **every** set $F_i \\in \\mathcal{F}$, the run visits $F_i$ **infinitely often**.\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight (Definitions Only)**\n> For Module 2 of PECNT526, students are expected to know only the **formal tuple definition**, the **acceptance condition**, and the **relationship with classical Büchi Automata**. No construction or conversion algorithms are required at this stage.\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine you are a **quality inspector in a factory** with **multiple checkpoints** (say 3 checkpoints: A, B, C) along a conveyor belt carrying an infinite stream of products.\n\n- A **classical Büchi automaton** says: \"Every product that comes by must **pass through checkpoint A infinitely often**.\"\n- A **Generalised Büchi automaton** says: \"Every product that comes by must **pass through checkpoint A infinitely often AND checkpoint B infinitely often AND checkpoint C infinitely often**.\"\n\nEach checkpoint corresponds to one accepting set $F_i \\subseteq Q$. The product (word) is accepted only if it keeps coming back to **all** checkpoints, again and again, without end.\n\nIn simpler terms:\n\n> [!NOTE]\n> Think of a GBA as a Büchi automaton that has been given **a \"to-do list\" of multiple repeating tasks**, instead of just one. To be accepted, the word must complete **every task on the list infinitely often**, not just some of them.\n\nThis generalisation is critical in **model checking of Linear Temporal Logic (LTL)** because LTL formulas often contain multiple fairness constraints, and a single Büchi condition is not expressive enough — but a GBA can encode all of them natively.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# Section 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Anatomy of the Tuple — Step-by-Step Meaning\n\n- **Alphabet ($\\Sigma$)**: The vocabulary of the input. For hardware verification, $\\Sigma = 2^{AP}$ where $AP$ is the set of atomic propositions.\n- **States ($Q$)**: Nodes of the automaton. Each state encodes a piece of memory about \"what has been seen so far\" along the run.\n- **Initial States ($Q_0$)**: The starting configuration. Non-determinism allows multiple entry points.\n- **Transition Relation ($\\delta$)**: For each state $q$ and each input symbol $a \\in \\Sigma$, $\\delta(q,a)$ gives the set of possible next states. Non-determinism means $| \\delta(q,a) | \\geq 0$.\n- **Acceptance Family ($\\mathcal{F}$)**: This is the **distinguishing feature** of a GBA. Instead of one subset $F \\subseteq Q$, there is a **set of subsets** $\\{F_1, F_2, \\ldots, F_k\\}$.\n\n## 2.2 The Acceptance Condition — Unpacked\n\nLet $\\rho = q_0, q_1, q_2, \\ldots$ be an infinite run over word $w \\in \\Sigma^{\\omega}$. Define:\n$$\n\\inf(\\rho) = \\{ q \\in Q \\mid q \\text{ appears infinitely often in } \\rho \\}\n$$\n\nThen $\\rho$ is **accepting** iff:\n\n$$\n\\forall F_i \\in \\mathcal{F} \\;:\\; \\inf(\\rho) \\cap F_i \\neq \\emptyset\n$$\n\nEquivalently: every set $F_i$ has at least one state that appears **infinitely often** in the run.\n\n## 2.3 Why \"Generalised\"?\n\n- A **classical Büchi Automaton (BA)** has $\\mathcal{F} = \\{ F \\}$ — a singleton family with **one** accepting set.\n- A **Generalised Büchi Automaton (GBA)** has $\\mathcal{F} = \\{ F_1, F_2, \\ldots, F_k \\}$ with $k \\geq 1$ accepting sets.\n- Therefore: **every BA is a GBA** (with $k=1$), but not every GBA is a BA.\n\n## 2.4 KTU Formula Sheet / Cheat Sheet\n\n| Symbol / Notation | Description |\n|---|---|\n| $\\mathcal{A} = (\\Sigma, Q, Q_0, \\delta, \\mathcal{F})$ | Formal 5-tuple of a GBA |\n| $\\Sigma$ | Finite input alphabet |\n| $Q$ | Finite set of states |\n| $Q_0 \\subseteq Q$ | Initial states |\n| $\\delta : Q \\times \\Sigma \\rightarrow 2^{Q}$ | Transition function |\n| $\\mathcal{F} = \\{ F_1, F_2, \\ldots, F_k \\}$ | Family of accepting sets |\n| $\\rho = q_0 q_1 q_2 \\ldots$ | An infinite run |\n| $\\inf(\\rho)$ | States visited infinitely often in $\\rho$ |\n| $\\forall F_i \\in \\mathcal{F} \\;:\\; \\inf(\\rho) \\cap F_i \\neq \\emptyset$ | GBA acceptance condition |\n| $L(\\mathcal{A})$ | Language accepted by $\\mathcal{A}$ |\n| $L_{\\omega}(\\mathcal{A}) \\subseteq \\Sigma^{\\omega}$ | $\\omega$-regular language of infinite words |\n\n## 2.5 Real-World Utility\n\nGBAs are the **canonical automaton model used in LTL model checking**:\n\n- Every LTL formula $\\varphi$ can be translated into a GBA $\\mathcal{A}_{\\varphi}$ of size $2^{O(\\vert \\varphi \\vert)}$.\n- The product $\\mathcal{M} \\times \\mathcal{A}_{\\varphi}$ is then checked for emptiness to decide whether the system $\\mathcal{M}$ satisfies $\\varphi$.\n- Industrial model checkers (SPIN, NuSMV, VIS) exploit GBAs because they preserve the structure of the LTL formula more compactly than classical BAs.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# Section 3: Step-by-Step Analysis & Analytical Matrix\n\n## 3.1 Comparison Matrix: Büchi vs. Generalised Büchi Automaton\n\n| Property | Büchi Automaton (BA) | Generalised Büchi Automaton (GBA) |\n|---|---|---|\n| Tuple | $(\\Sigma, Q, Q_0, \\delta, F)$ | $(\\Sigma, Q, Q_0, \\delta, \\mathcal{F})$ |\n| Accepting component | Single set $F \\subseteq Q$ | Family $\\mathcal{F} = \\{F_1, \\ldots, F_k\\}$, $k \\geq 1$ |\n| Acceptance rule | $\\inf(\\rho) \\cap F \\neq \\emptyset$ | $\\forall F_i \\in \\mathcal{F} : \\inf(\\rho) \\cap F_i \\neq \\emptyset$ |\n| Strength of condition | Weaker (one obligation) | Stronger (multiple obligations) |\n| Expressive power over $\\Sigma^{\\omega}$ | Same as GBA (they are equivalent) | Same as BA |\n| Compactness for LTL | Less compact (after translation) | More compact (preserves formula structure) |\n| Used in | Theoretical proofs, automata theory | LTL model checking in practice |\n\n## 3.2 Worked Logical Walkthrough\n\n**Given:** A GBA $\\mathcal{A} = (\\Sigma, Q, Q_0, \\delta, \\mathcal{F})$ where $\\mathcal{F} = \\{F_1, F_2\\}$.\n\n**Question:** When is a run $\\rho$ accepted?\n\n**Step 1.** Identify all states that appear in $\\rho$ infinitely often. Call this set $\\inf(\\rho)$.\n\n**Step 2.** Check the intersection with $F_1$:\n\n$$\n\\inf(\\rho) \\cap F_1 \\stackrel{?}{\\neq} \\emptyset\n$$\n\nIf empty → reject.\n\n**Step 3.** Check the intersection with $F_2$:\n\n$$\n\\inf(\\rho) \\cap F_2 \\stackrel{?}{\\neq} \\emptyset\n$$\n\nIf empty → reject.\n\n**Step 4.** Only if **both** checks succeed, the run is **accepting**.\n\n**Step 5.** A word $w \\in \\Sigma^{\\omega}$ is in $L_{\\omega}(\\mathcal{A})$ if **at least one** accepting run $\\rho$ over $w$ exists.\n\n## 3.3 Formal Property: Closure Under Union\n\nGBAs are **closed under union**. Given two GBAs $\\mathcal{A}_1$ and $\\mathcal{A}_2$:\n\n$$\nL_{\\omega}(\\mathcal{A}_1) \\cup L_{\\omega}(\\mathcal{A}_2) = L_{\\omega}(\\mathcal{A}_1 \\oplus \\mathcal{A}_2)\n$$\n\nwhere $\\oplus$ denotes the standard product construction for automata. This property does **not hold** for intersection directly — intersection of GBAs is generally expressible but requires non-trivial construction.\n\n## 3.4 Equivalence with Büchi Automata\n\n> [!IMPORTANT]\n> **Key Theorem (without proof, definitions-only module):**\n> For every GBA $\\mathcal{A}_G$, there exists a Büchi automaton $\\mathcal{A}_B$ such that $L_{\\omega}(\\mathcal{A}_G) = L_{\\omega}(\\mathcal{A}_B)$.\n\nThis means GBA is **not more expressive** than BA, but it is **more convenient** as an intermediate representation when translating LTL formulas.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# Section 4: Structural Diagrams & Schematics\n\n## 4.1 Generalised Büchi Automaton — Functional Architecture\n\nThe diagram below illustrates a GBA with two accepting sets $\\mathcal{F} = \\{F_1, F_2\\}$. The blue states belong to $F_1$, the orange states belong to $F_2$. A valid accepting run must visit **both** sets infinitely often.\n\n```mermaid\ngraph LR\n    q0[\"q0 initial\"] -->|a| q1[\"q1 in F1\"]\n    q1 -->|b| q2[\"q2 normal\"]\n    q2 -->|c| q3[\"q3 in F2\"]\n    q3 -->|d| q1\n    q2 -->|e| q2\n    q3 -->|f| q3\n\n    style q1 fill:#cce5ff,stroke:#003366,color:#000000\n    style q3 fill:#ffd9b3,stroke:#663300,color:#000000\n```\n\n**Interpretation:** Any accepted word must drive the automaton such that states $q_1 \\in F_1$ and $q_3 \\in F_2$ are revisited infinitely often — i.e., the cycle $\\rho$ must contain both infinitely often.\n\n## 4.2 Component-Level Architecture of GBA Definition\n\n```mermaid\ngraph TB\n    A[\"Sigma: Alphabet\"] --> B[\"Q: State Set\"]\n    C[\"Q0: Initial States\"] --> B\n    B --> D[\"delta: Transition Function\"]\n    D --> E[\"Q x Sigma -> 2 exp Q\"]\n    B --> F[\"Family F = F1, F2, ..., Fk\"]\n    F1[\"F1 subset Q\"] --> F\n    F2[\"F2 subset Q\"] --> F\n    F3[\"Fk subset Q\"] --> F\n    F --> G[\"Acceptance: inf rho intersect Fi non-empty for all i\"]\n\n    style G fill:#d5e8d4,stroke:#82b366,color:#000000\n    style F fill:#fff2cc,stroke:#d6b656,color:#000000\n```\n\n## 4.3 Comparison Flow: BA vs GBA Acceptance Check\n\n```mermaid\nflowchart TD\n    Start[\"Infinite Run rho\"] --> I[\"Compute inf rho\"]\n    I --> J{\"Check each F_i in F\"}\n    J -->|All Fi visited infinitely often| K[\"ACCEPT rho\"]\n    J -->|Some Fi visited finitely often| L[\"REJECT rho\"]\n    K --> M[\"Word w is in L omega of A\"]\n    L --> N[\"Word w is NOT in L omega of A\"]\n\n    style K fill:#d5e8d4,stroke:#82b366,color:#000000\n    style L fill:#f8cecc,stroke:#b85450,color:#000000\n    style M fill:#dae8fc,stroke:#6c8ebf,color:#000000\n    style N fill:#f8cecc,stroke:#b85450,color:#000000\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# Section 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Question 1 `[KTU University Exam – Model Question]`\n\n**Define a Generalised Büchi Automaton. State the acceptance condition clearly.**\n\n**Model Answer (3 marks):**\n\nA Generalised Büchi Automaton is a 5-tuple\n$$\n\\mathcal{A} = (\\Sigma,\\ Q,\\ Q_0,\\ \\delta,\\ \\mathcal{F})\n$$\nwhere $\\Sigma$ is a finite alphabet, $Q$ is a finite set of states, $Q_0 \\subseteq Q$ is the set of initial states, $\\delta : Q \\times \\Sigma \\rightarrow 2^{Q}$ is the transition function, and $\\mathcal{F} = \\{F_1, F_2, \\ldots, F_k\\}$ is a finite family of accepting subsets of $Q$. An infinite run $\\rho$ is accepting if and only if for every $F_i \\in \\mathcal{F}$, $\\inf(\\rho) \\cap F_i \\neq \\emptyset$, i.e., each accepting set is visited infinitely often by the run. (3 marks)\n\n### Question 2 `[KTU University Exam – Model Question]`\n\n**How does a Generalised Büchi Automaton differ from a classical Büchi Automaton?**\n\n**Model Answer (3 marks):**\n\nA classical Büchi Automaton has a **single** accepting set $F \\subseteq Q$, and an infinite run is accepted if it visits some state in $F$ infinitely often. In contrast, a Generalised Büchi Automaton has a **family** of accepting sets $\\mathcal{F} = \\{F_1, \\ldots, F_k\\}$ (with $k \\geq 1$), and acceptance requires the run to visit **every** $F_i$ infinitely often. The expressive power of both models over $\\Sigma^{\\omega}$ is the same, but GBA preserves the modular structure of LTL formulas more naturally during translation. (3 marks)\n\n## 5.2 Part B — Full 14-Mark Questions (Module Internal Choice)\n\n### Question A (14 Marks) `[KTU University Exam – Model Question]`\n\n**(a)** Define a Generalised Büchi Automaton (GBA) formally. List all components of its tuple with one-line explanations. **(7 marks)**\n\n**(b)** State and explain the GBA acceptance condition. How does it differ from the acceptance condition of a classical Büchi Automaton? **(7 marks)**\n\n**Model Solution:**\n\n**(a) Formal Definition (7 marks):**\n\nA Generalised Büchi Automaton over an infinite alphabet is a 5-tuple\n$$\n\\mathcal{A} = (\\Sigma,\\ Q,\\ Q_0,\\ \\delta,\\ \\mathcal{F})\n$$\n\nComponents:\n\n- $\\Sigma$: a finite, non-empty alphabet.\n- $Q$: a finite, non-empty set of states.\n- $Q_0 \\subseteq Q$: the set of initial states, where the run begins.\n- $\\delta : Q \\times \\Sigma \\rightarrow 2^{Q}$: the non-deterministic transition relation, giving the set of possible successors for each state-input pair.\n- $\\mathcal{F} = \\{F_1, F_2, \\ldots, F_k\\}$: a finite non-empty family of subsets of $Q$, each called an *accepting set*.\n\n**(b) Acceptance Condition (7 marks):**\n\nLet $\\rho = q_0 q_1 q_2 \\ldots$ be an infinite run of $\\mathcal{A}$ over an infinite word $w \\in \\Sigma^{\\omega}$. Define\n$$\n\\inf(\\rho) = \\{ q \\in Q \\mid q \\text{ occurs infinitely often in } \\rho \\}.\n$$\n\nThe run $\\rho$ is **accepting** iff for every $F_i \\in \\mathcal{F}$:\n$$\n\\inf(\\rho) \\cap F_i \\neq \\emptyset.\n$$\n\nA word $w$ is accepted by $\\mathcal{A}$ if **some** run over $w$ is accepting. The accepted language is $L_{\\omega}(\\mathcal{A}) \\subseteq \\Sigma^{\\omega}$.\n\n**Difference from BA:** For a classical BA, $|\\mathcal{F}| = 1$; for a GBA, $|\\mathcal{F}| \\geq 1$ and **all** sets must be visited infinitely often. Hence GBA imposes a stronger conjunction of obligations.\n\n---\n\n### Question B (14 Marks) `[KTU University Exam – Model Question – Alternative Choice]`\n\n**(a)** Define the components of a Generalised Büchi Automaton formally. Explain why the component $\\mathcal{F}$ is called a \"family\" of accepting sets rather than just a single set. **(7 marks)**\n\n**(b)** Write the acceptance condition of a GBA using $\\inf(\\rho)$. Explain with a small example (in words) how a word would fail to be accepted when even one accepting set $F_i$ is visited only finitely often. **(7 marks)**\n\n**Model Solution:**\n\n**(a) Tuple & Meaning of \"Family\" (7 marks):**\n\nA GBA is\n$$\n\\mathcal{A} = (\\Sigma,\\ Q,\\ Q_0,\\ \\delta,\\ \\mathcal{F})\n$$\nwith components as defined above. The collection $\\mathcal{F}$ is called a *family* because it is a **set of sets** — specifically,\n$$\n\\mathcal{F} \\subseteq 2^{Q} \\quad \\text{and} \\quad \\mathcal{F} = \\{ F_1, F_2, \\ldots, F_k \\}.\n$$\n\nIt is not a single set because acceptance in a GBA requires the run to satisfy **multiple, simultaneous recurrence obligations** — one per $F_i$. A single set cannot capture more than one independent obligation, so we need a family (i.e., a set whose elements are themselves sets of states). When $k=1$, the family reduces to a single set and $\\mathcal{A}$ becomes a classical Büchi Automaton.\n\n**(b) Acceptance via $\\inf(\\rho)$ and Failure Example (7 marks):**\n\nFor a run $\\rho$, the acceptance condition is\n$$\n\\forall F_i \\in \\mathcal{F} \\;:\\; \\inf(\\rho) \\cap F_i \\neq \\emptyset.\n$$\n\n**Failure Example:** Suppose $\\mathcal{F} = \\{F_1, F_2\\}$ with $F_1 = \\{q_a\\}$, $F_2 = \\{q_b\\}$. Consider a run $\\rho$ that visits $q_a$ infinitely often but visits $q_b$ only twice and then never again. Then $\\inf(\\rho) \\cap F_1 \\neq \\emptyset$ (good) but $\\inf(\\rho) \\cap F_2 = \\emptyset$ (fails). Since the universal quantifier requires **all** $F_i$ to be satisfied, the run is **rejected**. This illustrates the conjunctive nature of GBA acceptance.\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> - **Do NOT** write a single accepting set $F$ in the GBA tuple. Examiners specifically look for the family notation $\\mathcal{F} = \\{F_1, \\ldots, F_k\\}$. Writing $F$ instead of $\\mathcal{F}$ is a 2-mark deduction.\n> - **Do NOT** confuse \"all $F_i$ visited at least once\" with \"visited infinitely often.\" The acceptance condition is **infinitely often**, not \"at least once.\" This is the most common conceptual error in Module 2 answers.\n> - **Do NOT** state that GBA is strictly more expressive than BA — they accept **the same class of $\\omega$-regular languages**, but GBAs are structurally more convenient.\n> - **Do NOT** omit the universal quantifier in the acceptance condition. The condition is $\\forall F_i \\in \\mathcal{F}$, not $\\exists F_i \\in \\mathcal{F}$.\n\n## 5.3 Topic Recap & Important Things to Remember\n\n- A **Generalised Büchi Automaton (GBA)** is a 5-tuple $(\\Sigma, Q, Q_0, \\delta, \\mathcal{F})$.\n- $\\mathcal{F} = \\{F_1, F_2, \\ldots, F_k\\}$ is a **family (set of sets)** of accepting subsets of $Q$.\n- Acceptance requires **every** $F_i \\in \\mathcal{F}$ to be visited **infinitely often**.\n- Equivalent compact form: $\\forall F_i \\in \\mathcal{F} \\;:\\; \\inf(\\rho) \\cap F_i \\neq \\emptyset$.\n- Every Büchi Automaton is a GBA with $k = 1$, but not vice versa in *form* — though both accept identical $\\omega$-regular languages.\n- GBAs are the **standard intermediate representation for LTL formulas** in model checking.\n- $\\inf(\\rho)$ denotes the set of states appearing **infinitely often** in the run $\\rho$.\n- A word is accepted iff **at least one** run over it is accepting (existential over runs, universal over $F_i$).\n- GBA acceptance is **conjunctive** across the family — weakest link rejects the entire run.\n- Industrial relevance: SPIN, NuSMV, and other model checkers internally represent LTL properties as GBAs.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 2-t7",
          "title": "Model Checking ω-Regular Properties - Persistence Properties and Product",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 2\n> - **Topic:** Model Checking ω-Regular Properties - Persistence Properties and Product\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Persistence Properties — Formal Definition\n\nIn the context of Linear Temporal Logic (LTL) and ω-regular model checking, a **persistence property** is a special class of liveness property whose canonical syntactic form is:\n\n$$\\varphi \\;\\equiv\\; \\mathbf{F}\\,\\mathbf{G}\\, p \\quad \\text{(read as: \"eventually globally } p\\text{\")}$$\n\nwhere $p \\in AP$ is an atomic proposition drawn from the set $AP$ of atomic propositions over which the Kripke structure is labelled. A run $\\pi = s_0\\, s_1\\, s_2\\, \\dots$ of a Kripke structure $M$ satisfies $\\mathbf{F}\\mathbf{G}\\,p$, written $\\pi \\models \\mathbf{F}\\mathbf{G}\\,p$, if and only if:\n\n$$\\exists\\, i \\;\\geq\\; 0 \\;:\\; \\forall\\, j \\;\\geq\\; i \\;:\\; p \\in L(\\pi[j])$$\n\nThat is, **there exists a finite prefix after which $p$ is true at every step on the remaining infinite suffix of the run**.\n\n> [!IMPORTANT]\n> **Syllabus Highlight (KTU 2024 Scheme):** Persistence properties form one of the three canonical subclasses of ω-regular properties studied in Module 2, the other two being **safety** ($\\mathbf{G}\\,p$) and **recurrence** ($\\mathbf{G}\\mathbf{F}\\,p$). Together, these exhaust the canonical forms of LTL formulas used as benchmarks in industrial model checkers such as **SPIN**, **NuSMV**, and **CADP**.\n\n> [!NOTE]\n> **Logical Duality Note:** Persistence and recurrence are duals of one another under negation:\n> $$\\mathbf{F}\\mathbf{G}\\,p \\;\\equiv\\; \\neg\\,\\mathbf{G}\\mathbf{F}\\,\\neg p$$\n> This duality is the key algebraic identity that enables every persistence property to be checked via a *recurrence monitor* for the negated proposition.\n\n## 1.2 The Persistence-Property Acceptance Game — Intuition\n\nPicture a **traffic-light controller** for a railway crossing. The property *\"eventually the gate stays closed forever after a train has fully passed\"* is a persistence property:\n\n- **Until** some instant $i$, the gate may be opening, closing, or anything (transient behaviour).\n- **From instant $i$ onwards**, the gate is *guaranteed* to remain closed.\n\nThe \"eventually\" quantifier absorbs the finite warm-up of the controller, and the \"globally\" quantifier certifies the steady-state lock-in. This is exactly the shape of $\\mathbf{F}\\mathbf{G}\\,p$.\n\n> [!NOTE]\n> **Plain-English Analogy (Light Switch):** A persistence property is like a light switch that is allowed to flicker for a while, but **must eventually settle in the ON position and stay there forever**. In contrast:\n> - A *recurrence* property $\\mathbf{G}\\mathbf{F}\\,p$ would be a switch that **flickers ON infinitely often** but is allowed to turn OFF in between.\n> - A *safety* property $\\mathbf{G}\\,p$ would be a switch that **must be ON from the very first flip** with no exceptions.\n\n## 1.3 The Product Construction — Why It Is Needed\n\nA Kripke structure $M$ is a *finite-state* object with branching semantics, while an LTL formula $\\varphi$ is a *linear-time* specification over infinite runs. The **product construction** bridges these two worlds by:\n\n1. Translating $\\varphi$ into a Büchi (or generalised Büchi / Streett) automaton $\\mathcal{A}_{\\neg\\varphi}$.\n2. Synchronising the Kripke structure with the automaton state-by-state, producing a new graph $M \\times \\mathcal{A}_{\\neg\\varphi}$ whose accepting cycles correspond exactly to behaviours of $M$ that *violate* $\\varphi$.\n\nThe fundamental decision problem then reduces to **emptiness of an ω-automaton**, decidable in time $O(|M| \\cdot |\\mathcal{A}_{\\neg\\varphi}|)$ for Büchi automata.\n\n> [!IMPORTANT]\n> **Standard Complexity Metric:** For a system with $n$ states and a Büchi automaton with $m$ states, the product $M \\times \\mathcal{A}$ has at most $n \\cdot m$ states. Nested DFS emptiness runs in time $O(n \\cdot m)$. This **polynomial complexity** is what makes ω-regular model checking tractable for finite-state systems — a central selling point emphasised in the KTU 2024 PECNT526 syllabus.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Syntactic Normal Form of a Persistence Property\n\nA **persistence property** is canonically a *single* $\\mathbf{F}\\mathbf{G}\\,p$ formula or a Boolean combination thereof. The most general form used in industrial tools is:\n\n$$\\Phi \\;=\\; \\bigwedge_{i=1}^{k}\\; \\mathbf{F}\\mathbf{G}\\, p_i$$\n\nmeaning *\"eventually, all the $p_i$ hold forever and simultaneously\"*. This is the **generalised persistence** form, and it is accepted by **Streett automata with $k$ Streett pairs**.\n\n> [!NOTE]\n> **Why a Boolean Normal Form Matters:** When model checking, the negation of a generalised persistence property becomes:\n> $$\\neg \\Phi \\;\\equiv\\; \\bigvee_{i=1}^{k}\\; \\mathbf{G}\\mathbf{F}\\, \\neg p_i$$\n> The disjunction of $\\mathbf{G}\\mathbf{F}\\, \\neg p_i$ is a *recurrence* (fairness) property — perfect for monitor-based verification.\n\n## 2.2 From $\\mathbf{F}\\mathbf{G}\\,p$ to a Büchi Automaton\n\nThe atomic persistence $\\mathbf{F}\\mathbf{G}\\,p$ is recognised by a **2-state Büchi automaton** $\\mathcal{A}_{\\mathbf{F}\\mathbf{G}\\,p}$:\n\n| Component | Symbol | Definition |\n|---|---|---|\n| States | $Q$ | $\\{q_0, q_1\\}$ |\n| Initial states | $Q_0$ | $\\{q_0\\}$ |\n| Alphabet | $\\Sigma$ | $2^{AP}$ |\n| Transition relation | $\\delta$ | $\\delta(q_0, \\sigma) = q_1$ if $p \\in \\sigma$, else $q_0$ ; $\\delta(q_1, \\sigma) = q_1$ |\n| Accepting set | $F$ | $\\{q_1\\}$ |\n\n> [!IMPORTANT]\n> **Reading the Automaton:** State $q_0$ is the \"still searching\" state. State $q_1$ is the \"**have found a point where $p$ is true and must keep being true**\" state. Büchi acceptance requires $q_1$ to be visited **infinitely often** — and because $q_1$ is a *sink* (self-loop on $p$), once the run reaches $q_1$ and $p$ continues to hold, the run is accepted.\n\nFor verification we use the complement **monitor** for $\\neg \\mathbf{F}\\mathbf{G}\\,p \\equiv \\mathbf{G}\\mathbf{F}\\,\\neg p$, a 2-state Büchi automaton $\\mathcal{A}_{\\mathbf{G}\\mathbf{F}\\,\\neg p}$:\n\n| Component | Definition |\n|---|---|\n| States | $\\{q_0', q_1'\\}$ |\n| Initial | $\\{q_0'\\}$ |\n| $\\delta$ | $q_0' \\xrightarrow{\\neg p} q_1'$; $q_0' \\xrightarrow{p} q_0'$; $q_1' \\xrightarrow{\\neg p} q_1'$; $q_1' \\xrightarrow{p} q_0'$ |\n| Accepting $F$ | $\\{q_1'\\}$ |\n\nThis is the **standard counter-example generator** in SPIN's `never` claim construction.\n\n## 2.3 KTU High-Yield Formula Sheet\n\n| # | Symbol / Expression | Meaning | Engineering Use |\n|---|---|---|---|\n| 1 | $\\mathbf{F}\\mathbf{G}\\,p$ | Canonical persistence property | \"Stability\" specs (e.g., deadlock-free steady state) |\n| 2 | $\\mathbf{F}\\mathbf{G}\\,p \\;\\Leftrightarrow\\; \\neg \\mathbf{G}\\mathbf{F}\\,\\neg p$ | Logical duality | Automata-based model reduction |\n| 3 | $\\pi \\models \\mathbf{F}\\mathbf{G}\\,p \\;\\Leftrightarrow\\; \\exists i \\, \\forall j \\geq i : p \\in L(\\pi[j])$ | Satisfaction on a path | Run-based semantics |\n| 4 | $M \\models \\mathbf{F}\\mathbf{G}\\,p \\;\\Leftrightarrow\\; \\forall$ paths from every reachable state, $\\mathbf{F}\\mathbf{G}\\,p$ holds | Universal satisfaction | \"All executions\" spec (Kripke semantics) |\n| 5 | $\\lvert S \\times Q \\rvert \\;=\\; \\lvert S \\rvert \\cdot \\lvert Q \\rvert$ | Product state-space size | Worst-case memory bound |\n| 6 | $\\mathcal{L}(\\mathcal{A}) = \\emptyset \\;\\Leftrightarrow\\;$ no accepting cycle in $M \\times \\mathcal{A}$ | Emptiness test for counter-examples | Decision procedure |\n| 7 | $\\bigwedge_{i=1}^{k} \\mathbf{F}\\mathbf{G}\\,p_i$ | Generalised persistence | Streett acceptance |\n| 8 | Time complexity $O(\\lvert S \\rvert \\cdot \\lvert Q \\rvert)$ | Nested-DFS emptiness | Industrial-tool benchmark |\n\n> [!IMPORTANT]\n> **Boundary / Edge Case:** If $p$ is satisfied at $s_0$ (the initial state) of every run, then $\\mathbf{F}\\mathbf{G}\\,p$ collapses to the *safety* property $\\mathbf{G}\\,p$. Conversely, if no $p$ ever appears, the formula is unsatisfiable on that structure. Always check the *feasibility* of $p$ in the Kripke structure first.\n\n## 2.4 Why Persistence Is Harder Than Safety — and Easier Than Recurrence\n\n- **Safety** $\\mathbf{G}\\,p$: a *bad prefix* is finite. Trivial — search for one violating state.\n- **Persistence** $\\mathbf{F}\\mathbf{G}\\,p$: requires proving that *every infinite run* has a *stable suffix*. Needs cycle analysis.\n- **Recurrence** $\\mathbf{G}\\mathbf{F}\\,p$: needs *infinite* witnesses of $p$. Needs **fairness** (Streett) analysis.\n\n> [!NOTE]\n> **Engineering Utility:** Persistence properties are pervasive in:\n> - **Embedded controllers** (mode-locking, \"controller enters LOCKED state and stays\").\n> - **Cache-coherence protocols** (MESI: \"every line eventually settles in SHARED or EXCLUSIVE\").\n> - **Distributed consensus** (\"eventually the cluster converges to a single leader\").\n> - **Safety-critical shutdown sequences** (\"after the abort signal, the thrusters are disabled forever\").\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation of the Büchi Automaton for $\\mathbf{F}\\mathbf{G}\\,p$\n\nWe construct the *monitor* $\\mathcal{A}_{\\mathbf{G}\\mathbf{F}\\,\\neg p}$ explicitly because it is the automaton whose **accepting runs are exactly the counter-examples** to $\\mathbf{F}\\mathbf{G}\\,p$.\n\n**Goal:** $\\mathcal{A}$ accepts an infinite word $\\sigma = \\sigma_0 \\sigma_1 \\sigma_2 \\cdots \\in (2^{AP})^{\\omega}$ iff $\\sigma \\models \\mathbf{G}\\mathbf{F}\\,\\neg p$.\n\n**Step 1.** Identify the **two logical phases** of a run satisfying $\\mathbf{G}\\mathbf{F}\\,\\neg p$:\n\n- \"No $\\neg p$ has been observed yet\" — let this be state $q_0'$.\n- \"$\\neg p$ has been observed, await the next one\" — let this be state $q_1'$.\n\n**Step 2.** Define $\\delta$ such that $q_1'$ is entered precisely on a $\\neg p$ letter, and is exited precisely on a $p$ letter:\n\n$$\\delta(q_0', \\sigma) = \\begin{cases} q_1' & \\text{if } \\neg p \\in \\sigma \\\\ q_0' & \\text{if } p \\in \\sigma \\end{cases}$$\n\n$$\\delta(q_1', \\sigma) = \\begin{cases} q_1' & \\text{if } \\neg p \\in \\sigma \\\\ q_0' & \\text{if } p \\in \\sigma \\end{cases}$$\n\n**Step 3.** Set the accepting set $F = \\{q_1'\\}$, so Büchi acceptance demands infinitely many visits to $q_1'$ — i.e. $\\neg p$ must occur infinitely often.\n\n**Step 4.** Verify by induction on run length that an accepting run exists iff $\\mathbf{G}\\mathbf{F}\\,\\neg p$ holds:\n\n$$\\text{Run visits } q_1' \\text{ at step } i \\;\\Longleftrightarrow\\; \\neg p \\in \\sigma_i$$\n$$\\text{Büchi acceptance} \\;\\Longleftrightarrow\\; q_1' \\text{ visited i.o.} \\;\\Longleftrightarrow\\; \\mathbf{G}\\mathbf{F}\\,\\neg p \\text{ holds on } \\sigma$$\n\n**Conclusion:** $\\mathcal{L}(\\mathcal{A}_{\\mathbf{G}\\mathbf{F}\\,\\neg p}) = \\{\\sigma \\in (2^{AP})^{\\omega} \\mid \\sigma \\models \\mathbf{G}\\mathbf{F}\\,\\neg p\\}$.\n\n## 3.2 The Product Automaton — Full Definition\n\nGiven a Kripke structure $M = (S, S_0, R, L)$ over $AP$, and a Büchi automaton $\\mathcal{A}_{\\neg\\varphi} = (Q, Q_0, \\Sigma, \\delta, F)$, the **synchronised product** is:\n\n$$M \\otimes \\mathcal{A}_{\\neg\\varphi} \\;=\\; (S \\times Q,\\; S_0 \\times Q_0,\\; R_{\\otimes},\\; L_{\\otimes},\\; F_{\\otimes})$$\n\nwith:\n\n$$((s, q), (s', q')) \\in R_{\\otimes} \\;\\Longleftrightarrow\\; (s, s') \\in R \\;\\wedge\\; q' \\in \\delta(q, L(s'))$$\n\n$$L_{\\otimes}((s, q)) = L(s)$$\n\n$$F_{\\otimes} \\;=\\; S \\times F$$\n\nThe key theorem:\n\n> [!IMPORTANT]\n> **Theorem (Product Emptiness):**\n> $$M \\models \\varphi \\;\\Longleftrightarrow\\; \\mathcal{L}(M \\otimes \\mathcal{A}_{\\neg\\varphi}) = \\emptyset \\;\\Longleftrightarrow\\; \\text{no accepting cycle in } M \\otimes \\mathcal{A}_{\\neg\\varphi}$$\n\n## 3.3 Detailed Worked Example — Model Checking $\\mathbf{F}\\mathbf{G}\\,p$\n\nConsider the Kripke structure $M$ with $S = \\{s_0, s_1, s_2, s_3\\}$, $S_0 = \\{s_0\\}$, $R$ as drawn in the diagram of Section 4, and $L(s_0) = L(s_1) = \\{q\\}$ (i.e. $p$ is *false*), $L(s_2) = L(s_3) = \\{p\\}$.\n\n**Step 1.** Build the monitor for $\\mathbf{G}\\mathbf{F}\\,\\neg p$: $\\mathcal{A} = (\\{q_0', q_1'\\}, \\{q_0'\\}, \\delta, \\{q_1'\\})$ as derived above.\n\n**Step 2.** Build the product $M \\otimes \\mathcal{A}$ — state space $S \\times Q$ has $4 \\times 2 = 8$ states.\n\n**Step 3.** Compute reachable product states from $S_0 \\times Q_0 = \\{(s_0, q_0')\\}$:\n\n| Product state | $L_{\\otimes}$ | In $F_{\\otimes}$? |\n|---|---|---|\n| $(s_0, q_0')$ | $\\{q\\}$ | No (state $q_0' \\notin F$) |\n| $(s_1, q_0')$ | $\\{q\\}$ | No |\n| $(s_1, q_1')$ | $\\{q\\}$ | **Yes** |\n| $(s_2, q_1')$ | $\\{p\\}$ | **Yes** |\n| $(s_2, q_0')$ | $\\{p\\}$ | No |\n| $(s_3, q_1')$ | $\\{p\\}$ | **Yes** |\n| $(s_3, q_0')$ | $\\{p\\}$ | No |\n\n**Step 4.** Search for an **accepting cycle** — i.e. an SCC containing at least one state from $F_{\\otimes} = S \\times \\{q_1'\\}$.\n\nSuppose $R$ contains a cycle $s_2 \\to s_3 \\to s_2$. In the product, this lifts to a cycle $(s_2, q_1') \\to (s_3, q_1') \\to (s_2, q_1')$, and every state in this cycle is in $F_{\\otimes}$. Therefore the product contains an **accepting cycle**.\n\n**Step 5.** Decision:\n\n$$\\exists \\text{ accepting cycle in } M \\otimes \\mathcal{A} \\;\\Longrightarrow\\; M \\;\\not\\models\\; \\mathbf{F}\\mathbf{G}\\,p$$\n\n> [!WARNING]\n> **Common Valuation Pitfall:** The *single* existence of an accepting state in the product is *not* enough to declare a counter-example. The accepting state must lie on a **cycle**, otherwise the run is finite and Büchi acceptance is not met. Examiners specifically deduct 2 marks for confusing \"reachable accepting state\" with \"accepting cycle\".\n\n## 3.4 Set-Theoretic Characterisation of States Satisfying $\\mathbf{F}\\mathbf{G}\\,p$\n\nFor completeness, we derive the set of states that *do* satisfy $\\mathbf{F}\\mathbf{G}\\,p$ via fixed-point computation. Define:\n\n$$E \\;=\\; \\{ s \\in S \\mid s \\models \\mathbf{F}\\mathbf{G}\\,p \\}$$\n\n$E$ can be computed as the greatest fixed point of the following monotone operator on subsets of $S$:\n\n$$\\tau(X) \\;=\\; \\{ s \\in S \\mid p \\in L(s) \\;\\wedge\\; \\forall s' \\in R(s) : s' \\in X \\}$$\n\nIn words: $s$ is in $\\tau(X)$ iff $p$ holds at $s$ **and** every successor of $s$ is again in $X$. Iterating $\\tau$ from the top of the lattice yields $E$ in $O(|S|^2)$ time.\n\n> [!NOTE]\n> **Engineering Connection:** This greatest fixed-point corresponds to the *stable set* of a Petri net, and the algorithm is identical to the one used in the **TAPAAL** and **ITS-tools** model checkers for Petri-net reachability.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 System Kripke Structure $M$ (Block-Level Functional Architecture Flow)\n\nThe diagram below shows a small Kripke structure $M$ with **four states** $s_0, s_1, s_2, s_3$. States shaded green satisfy the proposition $p$ (here $p \\in L$); states shaded red do not.\n\n```mermaid\ngraph LR\n    s0[STATE s0: not p]:::redState\n    s1[STATE s1: not p]:::redState\n    s2[STATE s2: p]:::greenState\n    s3[STATE s3: p]:::greenState\n\n    s0 -- R --> s1\n    s1 -- R --> s2\n    s2 -- R --> s3\n    s3 -- R --> s2\n    s1 -- R --> s1\n\n    classDef redState fill:#ffd6d6,stroke:#c0392b,stroke-width:2px,color:#000\n    classDef greenState fill:#d6ffd6,stroke:#27ae60,stroke-width:2px,color:#000\n```\n\n## 4.2 Büchi Monitor $\\mathcal{A}_{\\mathbf{G}\\mathbf{F}\\,\\neg p}$\n\nThe following 2-state automaton is the **counter-example monitor**. State $q_1'$ is the *accepting* state (drawn with double circle).\n\n```mermaid\ngraph LR\n    q0p[STATE q0 prime: hunting]:::normal\n    q1p[STATE q1 prime: accepting]:::accepting\n    q0p -- label p --> q0p\n    q0p -- label not p --> q1p\n    q1p -- label p --> q0p\n    q1p -- label not p --> q1p\n\n    classDef normal fill:#e8e8ff,stroke:#2c3e50,stroke-width:1.5px,color:#000\n    classDef accepting fill:#fff3b0,stroke:#b8860b,stroke-width:3px,color:#000\n```\n\n## 4.3 Synchronised Product $M \\otimes \\mathcal{A}_{\\mathbf{G}\\mathbf{F}\\,\\neg p}$\n\nThe product combines the two graphs above into a single 8-state graph. Nodes whose automaton-component is $q_1'$ are highlighted as **accepting** (yellow). Note the cycle at the bottom — it is the **accepting cycle** responsible for the counter-example.\n\n```mermaid\ngraph LR\n    A0[s0 q0 prime: not p]:::redState\n    A1[s1 q0 prime: not p]:::redState\n    B1[s1 q1 prime: not p ACCEPT]:::accepting\n    C0[s2 q0 prime: p]:::greenState\n    C1[s2 q1 prime: p ACCEPT]:::acceptingGreen\n    D0[s3 q0 prime: p]:::greenState\n    D1[s3 q1 prime: p ACCEPT]:::acceptingGreen\n    s1self[s1 q0 prime self loop: not p]:::redState\n\n    A0 -- R delta --> A1\n    A1 -- R delta --> s1self\n    A1 -- R delta --> B1\n    s1self -- R delta --> s1self\n    s1self -- R delta --> B1\n    B1 -- R delta --> C0\n    B1 -- R delta --> C1\n    C0 -- R delta --> C1\n    C0 -- R delta --> D0\n    C1 -- R delta --> D0\n    C1 -- R delta --> C1\n    D0 -- R delta --> C0\n    D0 -- R delta --> D1\n    D1 -- R delta --> C0\n    D1 -- R delta --> C1\n\n    classDef redState fill:#ffd6d6,stroke:#c0392b,stroke-width:1.5px,color:#000\n    classDef greenState fill:#d6ffd6,stroke:#27ae60,stroke-width:1.5px,color:#000\n    classDef accepting fill:#fff3b0,stroke:#b8860b,stroke-width:3px,color:#000\n    classDef acceptingGreen fill:#d6ffd6,stroke:#b8860b,stroke-width:3px,color:#000\n```\n\n## 4.4 Algorithm Block Diagram — Persistence Model Checking Pipeline\n\n```mermaid\ngraph TD\n    INPUT[INPUT Kripke M and LTL F G p]:::io\n    NEG[STEP 1 Negate: F G p becomes G F not p]:::process\n    AUTO[STEP 2 Translate G F not p to Buchi automaton A]:::process\n    PROD[STEP 3 Build synchronised product M tensor A]:::process\n    REACH[STEP 4 Compute reachable product states]:::process\n    SCC[STEP 5 Find SCCs and detect accepting cycle]:::process\n    DECIDE[STEP 6 Decision: accepting cycle exists implies M does not satisfy F G p]:::decision\n    OUT_SAT[OUTPUT M satisfies F G p]:::io\n    OUT_VIO[OUTPUT Counter example trace returned]:::io\n\n    INPUT --> NEG --> AUTO --> PROD --> REACH --> SCC --> DECIDE\n    DECIDE -- YES --> OUT_VIO\n    DECIDE -- NO --> OUT_SAT\n\n    classDef io fill:#d6e8ff,stroke:#1f4e79,stroke-width:2px,color:#000\n    classDef process fill:#e8e8e8,stroke:#2c3e50,stroke-width:1.5px,color:#000\n    classDef decision fill:#fff3b0,stroke:#b8860b,stroke-width:2px,color:#000\n```\n\n> [!NOTE]\n> **Why the Synchronised Product Works:** The product enforces a *lock-step* execution: at every step, $M$ chooses a successor $s'$ and the Büchi automaton $\\mathcal{A}$ simultaneously reads $L(s')$ and updates its own state. Accepting cycles in the product are exactly the infinite runs of $M$ that drive $\\mathcal{A}$ to its accepting state infinitely often — i.e. counter-examples to $\\varphi$.\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer (3 Marks Each)\n\n### Question 1 (3 Marks)\n**[KTU University Exam — Model Exam 2024]**\n*Define a persistence property in LTL. Give one real-world example from an embedded-control domain.*\n\n**Model Answer (3 Marks):**\nA *persistence property* is an LTL formula of the canonical form $\\mathbf{F}\\mathbf{G}\\,p$ (or a Boolean combination thereof), read *\"eventually $p$ holds forever\"*. Formally, a run $\\pi$ satisfies $\\mathbf{F}\\mathbf{G}\\,p$ iff $\\exists i \\geq 0$ such that $\\forall j \\geq i$, $p \\in L(\\pi[j])$.\n**Example:** In a railway-crossing controller, the requirement *\"after the train has fully cleared, the gate remains closed forever\"* is a persistence property with $p \\equiv \\text{gate\\_closed}$.\n*(1 mark each for definition, formal satisfaction, and example.)*\n\n### Question 2 (3 Marks)\n**[KTU University Exam — June 2024]**\n*State and justify the logical duality between persistence and recurrence. Why is this duality important for model checking?*\n\n**Model Answer (3 Marks):**\n**Duality:** $\\mathbf{F}\\mathbf{G}\\,p \\equiv \\neg\\, \\mathbf{G}\\mathbf{F}\\,\\neg p$ — proved by De Morgan-style reasoning on the path quantifiers.\n**Importance:** To check whether $M \\models \\mathbf{F}\\mathbf{G}\\,p$, it suffices to check whether the *monitor* for $\\mathbf{G}\\mathbf{F}\\,\\neg p$ has an accepting run in the product $M \\otimes \\mathcal{A}_{\\mathbf{G}\\mathbf{F}\\,\\neg p}$. This converts a *universal* \"all paths\" persistence question into an *existential* \"is there a counter-example?\" recurrence question — much easier algorithmically (1.5 marks for duality statement, 1.5 marks for the algorithmic significance).\n\n---\n\n## 5.2 Part B — Module-Internal Choice (14 Marks Each)\n\n### Question A (14 Marks)\n**[KTU University Exam — Dec 2024, Adapted]**\n\n**(a) (7 Marks)** Construct a Büchi automaton $\\mathcal{A}$ over $AP = \\{p\\}$ that recognises exactly the language $\\{\\sigma \\in (2^{AP})^{\\omega} \\mid \\sigma \\models \\mathbf{G}\\mathbf{F}\\,\\neg p\\}$. Tabulate the transition function and clearly identify the accepting state(s).\n\n**(b) (7 Marks)** Given the Kripke structure $M$ with states $S = \\{s_0, s_1, s_2\\}$, $S_0 = \\{s_0\\}$, $L(s_0) = L(s_1) = \\emptyset$, $L(s_2) = \\{p\\}$, and $R = \\{(s_0, s_1), (s_1, s_1), (s_1, s_2), (s_2, s_2)\\}$, determine whether $M \\models \\mathbf{F}\\mathbf{G}\\,p$. Use the product construction with $\\mathcal{A}$ from part (a) and report whether an accepting cycle exists.\n\n#### Model Solution\n\n**Part (a) — Büchi Automaton Construction (7 Marks):**\n\nThe Büchi automaton is $\\mathcal{A} = (Q, Q_0, \\Sigma, \\delta, F)$ with:\n\n$$Q = \\{q_0, q_1\\}, \\quad Q_0 = \\{q_0\\}, \\quad \\Sigma = 2^{\\{p\\}}, \\quad F = \\{q_1\\}$$\n\n| Current state | Letter | Next state |\n|---|---|---|\n| $q_0$ | $\\{p\\}$ (i.e. $p$ true) | $q_0$ |\n| $q_0$ | $\\emptyset$ (i.e. $\\neg p$ true) | $q_1$ |\n| $q_1$ | $\\{p\\}$ | $q_0$ |\n| $q_1$ | $\\emptyset$ | $q_1$ |\n\n*(2 marks for state/accepting set definition, 3 marks for the transition table, 2 marks for justifying that this accepts exactly $\\mathbf{G}\\mathbf{F}\\,\\neg p$.)*\n\n**Part (b) — Product Construction & Decision (7 Marks):**\n\n**Step 1.** Build the product $M \\otimes \\mathcal{A}$. State space: $S \\times Q$ with $3 \\times 2 = 6$ states.\n\n**Step 2.** Enumerate reachable product states from $\\{(s_0, q_0)\\}$:\n\n| Product state | Letter at $L(s)$ | In $F_{\\otimes}$? |\n|---|---|---|\n| $(s_0, q_0)$ | $\\emptyset$ | No |\n| $(s_1, q_0)$ | $\\emptyset$ | No |\n| $(s_1, q_1)$ | $\\emptyset$ | **Yes** |\n| $(s_2, q_1)$ | $\\{p\\}$ | **Yes** |\n| $(s_2, q_0)$ | $\\{p\\}$ | No |\n\n**Step 3.** List product transitions using $\\delta$ and $R$:\n\n$$(s_0, q_0) \\to (s_1, q_0)$$\n\n$$(s_1, q_0) \\to (s_1, q_0) \\text{ (via self-loop on } s_1 \\text{, letter } \\emptyset \\Rightarrow q_0 \\xrightarrow{\\emptyset} q_0)$$\n\n$$(s_1, q_0) \\to (s_1, q_1) \\text{ (via self-loop on } s_1 \\text{, letter } \\emptyset \\Rightarrow q_0 \\xrightarrow{\\emptyset} q_1 \\text{ allowed by } \\delta \\text{ non-determinism? NO)}$$\n\n> [!NOTE]\n> **Refinement of the monitor:** The standard construction for $\\mathbf{G}\\mathbf{F}\\,\\neg p$ is *deterministic* in some presentations and non-deterministic in others. The KTU 2024 scheme accepts either; the cleanest version has $q_0 \\xrightarrow{\\neg p} q_1$ and $q_1 \\xrightarrow{p} q_0$ and $q_1 \\xrightarrow{\\neg p} q_1$, $q_0 \\xrightarrow{p} q_0$. With that version:\n>\n> $(s_1, q_0) \\to (s_1, q_1)$ via the $s_1$-self-loop because $\\emptyset \\models \\neg p$, so $q_0 \\to q_1$.\n>\n> Continuing: $(s_1, q_1) \\to (s_2, q_0)$ because $\\delta(q_1, \\{p\\}) = q_0$. And $(s_2, q_0) \\to (s_2, q_0)$ because $q_0 \\xrightarrow{\\{p\\}} q_0$.\n\n**Step 4.** Find SCCs:\n- $\\{(s_2, q_0)\\}$ — trivial SCC, but its automaton component is $q_0 \\notin F$, so it is **not accepting**.\n- $\\{(s_1, q_0), (s_1, q_1)\\}$ — contains $(s_1, q_1) \\in F_{\\otimes}$, so it is an **accepting SCC**.\n\n**Step 5.** Decision: an accepting cycle exists $\\Rightarrow$ the monitor accepts a run of $M$ $\\Rightarrow$ there is a run satisfying $\\mathbf{G}\\mathbf{F}\\,\\neg p$ $\\Rightarrow$ $M \\not\\models \\mathbf{F}\\mathbf{G}\\,p$.\n\n*(2 marks for product states list, 2 marks for cycle detection, 1 mark for accepting-cycle identification, 2 marks for the final decision.)*\n\n---\n\n### Question B (14 Marks) — *Internal Choice Alternative*\n**[KTU University Exam — June 2024, Adapted]**\n\n**(a) (7 Marks)** With the help of the greatest fixed-point equation\n$$E = \\{s \\in S \\mid p \\in L(s) \\wedge \\forall s' \\in R(s) : s' \\in E\\},$$\nprove that the set $E$ of states satisfying $\\mathbf{F}\\mathbf{G}\\,p$ can be computed by iterating the operator $\\tau(X) = \\{s \\mid p \\in L(s) \\wedge R(s) \\subseteq X\\}$ from $X_0 = S$ down to the greatest fixed point.\n\n**(b) (7 Marks)** Apply this fixed-point algorithm to the Kripke structure with $S = \\{s_1, s_2, s_3, s_4\\}$, $R$ the complete graph on $\\{s_2, s_3, s_4\\}$, $R(s_1) = \\{s_2\\}$, and $L(s_1) = \\emptyset$, $L(s_2) = L(s_3) = L(s_4) = \\{p\\}$. Show every iteration step explicitly. Does $M \\models \\mathbf{F}\\mathbf{G}\\,p$?\n\n#### Model Solution\n\n**Part (a) — Theoretical Proof (7 Marks):**\n\nWe show that $\\tau$ is *monotone* on the lattice $(2^S, \\subseteq)$: if $X \\subseteq Y$ then $\\tau(X) \\subseteq \\tau(Y)$, because the condition $R(s) \\subseteq X$ becomes easier to satisfy when $X$ grows. By the Knaster–Tarski theorem, the greatest fixed point $\\mathrm{gfp}(\\tau)$ exists and equals $\\bigcap \\{ X \\mid \\tau(X) \\subseteq X \\}$.\n\nTo see that $\\mathrm{gfp}(\\tau) = \\{s \\mid M, s \\models \\mathbf{F}\\mathbf{G}\\,p\\}$:\n\n- ($\\subseteq$) Suppose $s \\in \\mathrm{gfp}(\\tau)$. By definition $p \\in L(s)$ and $R(s) \\subseteq \\mathrm{gfp}(\\tau)$. By induction on iteration index $k$ of $\\tau^k(S)$, every successor of $s$ is in $\\mathrm{gfp}(\\tau)$, so $p$ holds along every path from $s$ once $s$ is reached — i.e. $M, s \\models \\mathbf{F}\\mathbf{G}\\,p$.\n- ($\\supseteq$) Conversely, suppose $M, s \\models \\mathbf{F}\\mathbf{G}\\,p$. Then $p$ holds at $s$ and every path from $s$ eventually stays in $\\{s' \\mid p \\in L(s')\\}$. So every successor of $s$ also satisfies $\\mathbf{F}\\mathbf{G}\\,p$. Thus $s \\in \\tau(\\{s' \\mid M, s' \\models \\mathbf{F}\\mathbf{G}\\,p\\})$. Since $\\{s' \\mid M, s' \\models \\mathbf{F}\\mathbf{G}\\,p\\}$ is a fixed point of $\\tau$, it contains $\\mathrm{gfp}(\\tau)$ by definition.\n\n*(2 marks for monotonicity argument, 2 marks for greatest fixed-point existence, 3 marks for the two inclusion proofs.)*\n\n**Part (b) — Algorithm Application (7 Marks):**\n\nIteration table, with $X_0 = S = \\{s_1, s_2, s_3, s_4\\}$:\n\n| Iter $k$ | $X_k$ | Computation |\n|---|---|---|\n| 0 | $\\{s_1, s_2, s_3, s_4\\}$ | $X_0 = S$ |\n| 1 | $\\{s_2, s_3, s_4\\}$ | Drop $s_1$: $p \\notin L(s_1)$ |\n| 2 | $\\{s_2, s_3, s_4\\}$ | All remaining states have $p$, all successors are in $X_1$. $\\tau(X_1) = X_1$ — fixed point reached. |\n\nConclusion: $E = \\{s_2, s_3, s_4\\}$.\n\nSince $s_1 \\notin E$, the property $\\mathbf{F}\\mathbf{G}\\,p$ does **not** hold globally on $M$. However, from any state in $\\{s_2, s_3, s_4\\}$ the property is satisfied (it holds trivially, since $p$ is true at every state in the reachable sub-graph).\n\nTherefore: $M \\not\\models \\mathbf{F}\\mathbf{G}\\,p$ at $s_1$, but $M, s_2 \\models \\mathbf{F}\\mathbf{G}\\,p$, $M, s_3 \\models \\mathbf{F}\\mathbf{G}\\,p$, $M, s_4 \\models \\mathbf{F}\\mathbf{G}\\,p$.\n\n*(1 mark per iteration row, 1 mark for fixed-point detection, 1 mark for the global $M$-satisfaction decision, 1 mark for per-state satisfaction.)*\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Pitfall Callout (Common Mark-Loss Zones):**\n> 1. **Forgetting the \"eventually\" quantifier:** Many students write $\\mathbf{G}\\mathbf{F}\\,p$ (recurrence) instead of $\\mathbf{F}\\mathbf{G}\\,p$ (persistence). These are **not equivalent** — $\\mathbf{G}\\mathbf{F}\\,p$ allows infinitely many violations, but $\\mathbf{F}\\mathbf{G}\\,p$ forbids any violation after the stable point. *2-mark penalty if the formula is wrong.*\n> 2. **Reachable accepting state vs. accepting cycle:** Reaching an $F_{\\otimes}$ state once is *not* a counter-example. The run must be **infinite** and revisit $F_{\\otimes}$ i.o. — i.e. lie on an accepting cycle. Examiners deduct 1–2 marks for confusing these.\n> 3. **Wrong direction of duality:** Some students write $\\mathbf{F}\\mathbf{G}\\,p \\equiv \\mathbf{G}\\mathbf{F}\\,\\neg p$. This is the **negation**, not the duality. Correct: $\\mathbf{F}\\mathbf{G}\\,p \\equiv \\neg\\, \\mathbf{G}\\mathbf{F}\\,\\neg p$. *1-mark penalty.*\n> 4. **Product state-space arithmetic:** Forgetting to multiply $|S| \\times |Q|$ in the complexity bound loses 1 mark. Always quote $O(|S| \\cdot |Q|)$.\n> 5. **Forgetting the $\\bigwedge$ over multiple $p_i$:** The generalised form $\\bigwedge_i \\mathbf{F}\\mathbf{G}\\,p_i$ is a **Streett** property, not a single Büchi. Worth 2 marks in any part-(a) theoretical question.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n- **Persistence property** = canonical LTL form $\\mathbf{F}\\mathbf{G}\\,p$, meaning \"eventually $p$ holds forever\". *(Must-memorise the formal path-quantifier form.)*\n- **Duality identity** $\\mathbf{F}\\mathbf{G}\\,p \\equiv \\neg \\mathbf{G}\\mathbf{F}\\,\\neg p$ — drives every Büchi-automaton-based decision procedure for persistence.\n- **Generalised persistence** $\\bigwedge_{i=1}^{k} \\mathbf{F}\\mathbf{G}\\,p_i$ requires a **Streett** automaton with $k$ Streett pairs.\n- The **2-state Büchi monitor** for $\\mathbf{G}\\mathbf{F}\\,\\neg p$ is: $q_0 \\xleftrightarrow{p} q_0 \\to^{\\neg p} q_1 \\xleftrightarrow{\\neg p} q_1 \\to^{p} q_0$, with $F = \\{q_1\\}$.\n- The **synchronised product** $M \\otimes \\mathcal{A}_{\\neg\\varphi}$ has at most $|S| \\cdot |Q|$ states, transitions synchronised with $L(s')$.\n- **Decision rule:** $M \\models \\mathbf{F}\\mathbf{G}\\,p$ iff $M \\otimes \\mathcal{A}_{\\mathbf{G}\\mathbf{F}\\,\\neg p}$ has **no accepting cycle**.\n- **Fixed-point characterisation:** $E = \\mathrm{gfp}(\\tau)$ where $\\tau(X) = \\{s \\mid p \\in L(s) \\wedge R(s) \\subseteq X\\}$ — runs in $O(|S|^2)$.\n- **Time complexity** of nested-DFS emptiness on the product: $O(|S| \\cdot |Q|)$.\n- **Edge case:** If $p$ is true at $s_0$ and along all successors, $\\mathbf{F}\\mathbf{G}\\,p$ collapses to safety $\\mathbf{G}\\,p$.\n- **Engineering uses:** mode-locking, cache-coherence settling, distributed-consensus convergence, safety-critical shutdowns.\n- **Exam mantra:** *\"Reach the accepting state, then verify the cycle.\"* Partial counter-examples are worth zero.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 2-t8",
          "title": "Nested Depth-First Search (Only algorithms required)",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 2\n> - **Topic:** Nested Depth-First Search (Only algorithms required)\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\n**Nested Depth-First Search (NDFS)** is a two-phase, stack-based graph-traversal algorithm used in *automated verification*—specifically in **Linear Temporal Logic (LTL) model checking**—to detect **accepting cycles** in the synchronized product of a Kripke structure $\\mathcal{M}$ and a Büchi automaton $\\mathcal{B}_{\\neg \\phi}$.\n\n> [!NOTE]\n> **Syllabus Highlight (PECNT526 — Module 2):**\n> Nested Depth-First Search is the canonical on-the-fly algorithm for **emptiness checking** of Büchi automata. It determines whether $\\mathcal{L}(\\mathcal{M}) \\cap \\mathcal{L}(\\mathcal{B}_{\\neg \\phi}) \\neq \\emptyset$ by searching for a *lasso-shaped* counterexample path that loops through at least one accepting state.\n\nMathematically, NDFS is invoked on a product graph $G = (S, T, s_0, F)$ where:\n- $S$ = finite set of product states, with $\\vert S \\vert \\le \\vert S_{\\mathcal{M}} \\vert \\cdot \\vert S_{\\mathcal{B}} \\vert$\n- $T \\subseteq S \\times S$ = transition relation, with $\\vert T \\vert \\le \\vert T_{\\mathcal{M}} \\vert \\cdot \\vert T_{\\mathcal{B}} \\vert$\n- $s_0$ = initial state\n- $F \\subseteq S$ = set of **accepting states** (inherited from the Büchi automaton)\n\nThe algorithm answers the question: *Does there exist a reachable state $s \\in F$ that lies on a cycle?*\n\n## 1.2 Conceptual Analogy — The \"Treasure Hunter on an Island\"\n\n> [!IMPORTANT]\n> **The Treasure-Hunter Analogy**\n> Imagine a treasure hunter dropped on a vast island network connected by one-way roads. The hunter carries a piece of chalk with **three colors**: **white** (never visited), **cyan** (currently being explored — paint a stripe on the road), and **blue** (fully explored, dead end).\n>\n> - **Outer DFS (dfs1)** is the *first journey* — the hunter walks every road reachable from the landing zone, painting roads cyan. Each time the hunter steps on a road that already has a cyan stripe (a *back edge*), a **Cycle Alert** is raised.\n> - **Inner DFS (dfs2)** is the *verification trek* — starting from the suspicious cyan state, the hunter confirms whether a **treasure (accepting state)** lies somewhere on the cycle. If found, the cycle is a valid counterexample.\n> - The reason it is **nested** is that dfs2 may be invoked many times during dfs1, and each invocation is itself a complete DFS that respects the cyan/blue markings of the outer search.\n\n## 1.3 Why Two DFS Searches Are Necessary\n\nA *single* DFS cannot detect Büchi acceptance, because acceptance requires the conjunction of two reachability conditions:\n1. Reach an **accepting state** $f \\in F$ from $s_0$.\n2. Reach **back to $f$** (or any state already on the current DFS stack) starting from $f$ — i.e., find a cycle through $f$.\n\nNDFS solves both in one structured traversal by using the outer search's stack (the cyan set) to encode the second condition implicitly.\n\n## 1.4 Color Marking — The State Classification\n\n| Color | Symbol | Meaning | Set Notation |\n| :---: | :---: | :--- | :---: |\n| **White** | $\\perp$ | Unvisited, never seen | $W = \\{s \\in S \\mid \\text{color}(s)=\\perp\\}$ |\n| **Cyan** | $C$ | On the current outer DFS stack | $C = \\{s \\in S \\mid \\text{color}(s)=C\\}$ |\n| **Blue** | $B$ | Fully explored, no longer on the stack | $B = \\{s \\in S \\mid \\text{color}(s)=B\\}$ |\n| **Red** (optimized) | $R$ | On inner DFS stack / has an accepting successor on the current stack | $R \\subseteq C$ |\n\n> [!NOTE]\n> Invariant maintained at all times: $S = W \\sqcup C \\sqcup B$ (a partition — every state has exactly one color).\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Underlying Logical Structure\n\nThe NDFS family decomposes the **emptiness check** $\\mathcal{L}(G) \\stackrel{?}{=} \\emptyset$ into three logically independent observations:\n\n- **Observation A — Reachable Cycle Test.** During outer DFS, every back edge $(s, s')$ where $s' \\in C$ certifies the existence of a cycle. The cycle consists of the suffix of the cyan stack from $s'$ down to $s$, plus the transition $s \\to s'$.\n- **Observation B — Acceptance Test.** A cycle is *accepting* iff it contains at least one state $f \\in F$.\n- **Observation C — On-the-Fly Property.** Because the product graph can be constructed lazily, NDFS can terminate as soon as one accepting cycle is found — no full graph needs to be materialized.\n\n> [!IMPORTANT]\n> **Why NDFS Beats Plain DFS + SCC**\n> Tarjan's SCC algorithm also finds cycles, but it must complete the entire graph traversal to report the strongly connected component containing an accepting state. NDFS can short-circuit the moment an accepting cycle is witnessed, giving vastly better average-case performance in industrial model checkers such as **SPIN**.\n\n## 2.2 KTU Formula Sheet / Cheat Sheet\n\n| Symbol / Term | Definition | Typical Magnitude |\n| :--- | :--- | :--- |\n| $S$ | State space of product $G$ | $10^{4}$ – $10^{9}$ |\n| $T$ | Transition relation of product $G$ | $10^{5}$ – $10^{10}$ |\n| $F$ | Accepting states set, $F \\subseteq S$ | $\\mathcal{O}(\\vert S \\vert)$ |\n| $C$ | Cyan set (outer DFS stack) | $\\le \\vert S \\vert$ |\n| $B$ | Blue set (closed states) | $\\le \\vert S \\vert$ |\n| $\\mathcal{O}(\\vert S \\vert \\cdot \\vert T \\vert)$ | Worst-case time of basic NDFS | — |\n| $\\mathcal{O}(\\vert S \\vert + \\vert T \\vert)$ | Worst-case time of optimized NDFS (Schwoon-Esparza) | — |\n| $\\mathcal{O}(\\vert S \\vert)$ | Memory — single DFS stack + one extra stack | — |\n\n> [!CAUTION]\n> The basic NDFS has worst-case **$\\mathcal{O}(\\vert S \\vert \\cdot \\vert T \\vert)$** because the inner DFS may be invoked up to $\\vert S \\vert$ times, each costing up to $\\vert T \\vert$ in the worst case. The *optimized* variant by Schwoon & Esparza reduces this to **$\\mathcal{O}(\\vert S \\vert + \\vert T \\vert)$** by using a fourth color (red) to cache the result of inner DFS.\n\n## 2.3 Real-World Engineering Utility\n\n- **Software Verification (SPIN / Promela):** NDFS is the heart of SPIN's LTL model checker; it is invoked billions of times during a single verification run.\n- **Hardware Model Checking (NuSMV, Cadence):** NDFS underlies the emptiness check for LTL specifications on RTL designs.\n- **Concurrent Protocol Verification:** Detecting deadlocks, livelocks, and violated mutual-exclusion invariants in distributed systems.\n- **Cyber-Physical System Validation:** Used in tools such as **UPPAAL** and **ThETA** for verifying timed automata against LTL properties.\n\n## 2.4 High-Yield Variants You Must Know for KTU Exams\n\n| # | Variant Name | Year | Optimisation Gained |\n| :---: | :--- | :---: | :--- |\n| 1 | **Basic NDFS** (Courcoubetis–Vardi–Wolper–Yannakakis) | 1992 | First on-the-fly LTL check |\n| 2 | **NDFS with Re-initialisation** (Godefroid–Holzmann) | 1994 | Caches inner-DFS results via RED |\n| 3 | **SW-NDFS** (Schwoon–Esparza) | 2005 | Worst-case optimal: $\\mathcal{O}(\\vert S \\vert + \\vert T \\vert)$ |\n| 4 | **Parallel NDFS** (Barnat–Brim) | 2001 | Distributed-memory parallelisation |\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations, Algorithms & Analytical Matrix\n\n## 3.1 Algorithm 1 — Basic Nested DFS (Courcoubetis et al., 1992)\n\nThis is the **foundational** version you will be expected to reproduce in the KTU ESE.\n\n### 3.1.1 The Pseudocode (Production-Quality Form)\n\n```\nALGORITHM 1: BASIC NDFS  (Courcoubetis-Vardi-Wolper-Yannakakis, 1992)\n─────────────────────────────────────────────────────────────────────\nINPUT : Product graph G = (S, T, s0, F)\nOUTPUT: \"COUNTEREXAMPLE_FOUND\"  or  \"NO_COUNTEREXAMPLE\"\n\n─────────────────────────────────────────────────────────────────────\nPROCEDURE  NDFS_main(s0):\n1.  for every s in S do\n2.       color(s) ← WHITE\n3.  end for\n4.  report ← dfs_outer(s0)\n5.  return report\n─────────────────────────────────────────────────────────────────────\n\nPROCEDURE  dfs_outer(u):\n6.  color(u) ← CYAN                            // push u onto outer stack\n7.  for every (u, v) in T do                   // iterate over successors\n8.       if color(v) = CYAN then                // BACK-EDGE detected\n9.            if dfs_inner(v) = FOUND then\n10.                return COUNTEREXAMPLE_FOUND\n11.            end if\n12.       else if color(v) = WHITE then\n13.            if dfs_outer(v) = COUNTEREXAMPLE_FOUND then\n14.                return COUNTEREXAMPLE_FOUND\n15.            end if\n16.       end if\n17.       // BLUE successors are skipped: their subtree cannot contain\n18.       // an accepting cycle reachable from u via this edge.\n19.  end for\n20.  color(u) ← BLUE                            // pop u from outer stack\n21.  return NO_COUNTEREXAMPLE\n─────────────────────────────────────────────────────────────────────\n\nPROCEDURE  dfs_inner(u):\n22.  if u ∈ F then                              // u is accepting\n23.       return COUNTEREXAMPLE_FOUND\n24.  end if\n25.  for every (u, v) in T do\n26.       if color(v) = CYAN then                // successor on outer stack\n27.            if dfs_inner(v) = COUNTEREXAMPLE_FOUND then\n28.                return COUNTEREXAMPLE_FOUND\n29.            end if\n30.       end if\n31.       // WHITE and BLUE successors are SKIPPED entirely.\n32.       // Reason: we only need to know if an outer-stack state\n33.       // is reachable, and outer-stack states are CYAN.\n34.  end for\n35.  return NOT_FOUND\n─────────────────────────────────────────────────────────────────────\n```\n\n### 3.1.2 Step-by-Step Walk-through on a Worked Example\n\nConsider the product graph with **6 states** and the following structure (used in the figure below):\n\n$$\ns_0 \\to s_1 \\to s_2 \\to s_3 \\to s_1 \\quad (\\text{back edge}), \\qquad\ns_3 \\to s_4 \\to s_5 \\to s_4 \\quad (\\text{back edge})\n$$\n\nwith **accepting set** $F = \\{s_2, s_4\\}$.\n\n| Step | Call | Action | Cyan Stack | Blue Set | Observation |\n| :---: | :--- | :--- | :---: | :---: | :--- |\n| 1 | `dfs_outer(s0)` | Paint $s_0$ cyan | $\\{s_0\\}$ | $\\emptyset$ | Outer DFS begins |\n| 2 | recurse on $s_1$ | Paint $s_1$ cyan | $\\{s_0, s_1\\}$ | $\\emptyset$ | — |\n| 3 | recurse on $s_2$ | Paint $s_2$ cyan | $\\{s_0, s_1, s_2\\}$ | $\\emptyset$ | $s_2 \\in F$ but no successor yet |\n| 4 | iterate $T(s_2) = \\{s_3\\}$ | $s_3$ is WHITE → recurse | $\\{s_0, s_1, s_2, s_3\\}$ | $\\emptyset$ | — |\n| 5 | iterate $T(s_3) = \\{s_1, s_4\\}$ | First $s_1$ is CYAN → back edge! | $\\{s_0, s_1, s_2, s_3\\}$ | $\\emptyset$ | Trigger `dfs_inner(s_1)` |\n| 6 | `dfs_inner(s_1)` | $s_1 \\notin F$, iterate $T(s_1) = \\{s_2\\}$ | same | same | $s_2$ is CYAN → recurse `dfs_inner(s_2)` |\n| 7 | `dfs_inner(s_2)` | $s_2 \\in F$ → **return FOUND** | same | same | Cycle $s_1 \\to s_2 \\to s_3 \\to s_1$ contains an accepting state |\n| 8 | unwind | Return **COUNTEREXAMPLE_FOUND** | — | — | Algorithm terminates |\n\nThe cycle returned as counterexample is the cyan-stack path $s_1 \\to s_2 \\to s_3$ plus the back edge $s_3 \\to s_1$.\n\n### 3.1.3 Derivation of the Worst-Case Time Complexity\n\n$$\n\\begin{aligned}\nT_{\\text{best}} &= \\mathcal{O}(\\vert S \\vert + \\vert T \\vert) \\quad &\\text{(single accepting cycle found immediately)} \\\\[4pt]\nT_{\\text{basic}} &= \\sum_{s \\in S} (\\text{cost of } dfs_{\\text{inner}} \\text{ invoked at } s) \\\\[4pt]\n                &\\le \\sum_{s \\in S} \\mathcal{O}(\\vert T \\vert) \\quad &\\text{(inner DFS traverses } \\le \\vert T \\vert \\text{ edges per call)} \\\\[4pt]\n                &= \\mathcal{O}(\\vert S \\vert \\cdot \\vert T \\vert) \\quad &\\blacksquare\n\\end{aligned}\n$$\n\n## 3.2 Algorithm 2 — Optimised NDFS (Schwoon-Esparza, 2005)\n\n### 3.2.1 The Pseudocode\n\n```\nALGORITHM 2: OPTIMISED NDFS  (Schwoon & Esparza, 2005)\n─────────────────────────────────────────────────────────────────────\nUses an additional colour RED.  RED(s) means \"s is reachable from\nsome accepting state s' such that s' lies on the current cyan stack\".\n\nPROCEDURE  optimised_NDFS(s0):\n1.  for every s in S do\n2.       color(s) ← WHITE\n3.       seed(s)  ← ∅\n4.  end for\n5.  return dfs_blue(s0)\n─────────────────────────────────────────────────────────────────────\n\nPROCEDURE  dfs_blue(u):\n6.  color(u) ← CYAN\n7.  if u ∈ F then\n8.       dfs_red(u)\n9.  end if\n10. for every (u, v) in T do\n11.      if color(v) = CYAN then\n12.           if accept_found = TRUE then\n13.                return COUNTEREXAMPLE_FOUND\n14.           end if\n15.      else if color(v) = WHITE then\n16.           if dfs_blue(v) = COUNTEREXAMPLE_FOUND then\n17.                return COUNTEREXAMPLE_FOUND\n18.           end if\n19.      end if\n20. end for\n21. color(u) ← BLUE\n22. return NO_COUNTEREXAMPLE\n─────────────────────────────────────────────────────────────────────\n\nPROCEDURE  dfs_red(u):\n23. color(u) ← RED\n24. for every (u, v) in T do\n25.      if color(v) = CYAN then\n26.           return COUNTEREXAMPLE_FOUND          // accepting cycle!\n27.      else if color(v) = WHITE then\n28.           dfs_red(v)\n29.      else if color(v) = RED and v ∉ seed(u) then\n30.           // reuse cached information\n31.           ...\n32.      end if\n33. end for\n34. color(u) ← BLUE\n─────────────────────────────────────────────────────────────────────\n```\n\n### 3.2.2 Complexity Derivation of the Optimised Variant\n\n$$\n\\begin{aligned}\nT_{\\text{opt}} &= T_{\\text{dfs\\_blue}} + T_{\\text{dfs\\_red}} \\\\[4pt]\nT_{\\text{dfs\\_blue}} &= \\mathcal{O}(\\vert S \\vert + \\vert T \\vert) \\quad &\\text{(standard DFS)} \\\\[4pt]\nT_{\\text{dfs\\_red}} &\\le \\mathcal{O}(\\vert S \\vert + \\vert T \\vert) \\quad &\\text{(each state coloured RED at most once)} \\\\[4pt]\n\\therefore\\; T_{\\text{opt}} &= \\mathcal{O}(\\vert S \\vert + \\vert T \\vert) \\quad &\\blacksquare\n\\end{aligned}\n$$\n\nThe key savings come from the fact that each state is recoloured to **RED at most once**, and the cached RED information is reused via the *seed* propagation.\n\n## 3.3 Correctness Argument (Soundness + Completeness)\n\n| Property | Statement | Justification |\n| :--- | :--- | :--- |\n| **Soundness** | If NDFS reports `COUNTEREXAMPLE_FOUND`, then $\\mathcal{L}(G) \\neq \\emptyset$. | `dfs_inner` (Alg. 1) is invoked only on **back edges** $(s, s')$ with $s' \\in C$; the cyan stack gives a path $s_0 \\leadsto s'$ and the back edge closes the cycle. The inner DFS additionally confirms that some $f \\in F$ is reachable from $s'$ staying inside $C \\cup \\{s'\\}$. |\n| **Completeness** | If $\\mathcal{L}(G) \\neq \\emptyset$, NDFS will report `COUNTEREXAMPLE_FOUND`. | Every accepting cycle contains at least one back edge w.r.t. the outer DFS. When that back edge is processed, the inner DFS is invoked from the target of the back edge, and the cycle is detected. |\n| **Termination** | NDFS halts in finite time. | Outer DFS colours each state CYAN at most once and BLUE at most once; inner DFS visits only cyan successors, which are bounded. No infinite recursion is possible on a finite graph. |\n\n## 3.4 Comparative Analytical Matrix\n\n| Criterion | Algorithm 1 (Basic) | Algorithm 2 (Optimised) |\n| :--- | :---: | :---: |\n| Year of publication | 1992 | 2005 |\n| Number of colour states | 3 (W, C, B) | 4 (W, C, B, R) |\n| Worst-case time | $\\mathcal{O}(\\vert S \\vert \\cdot \\vert T \\vert)$ | $\\mathcal{O}(\\vert S \\vert + \\vert T \\vert)$ |\n| Worst-case memory | $\\mathcal{O}(\\vert S \\vert)$ | $\\mathcal{O}(\\vert S \\vert)$ |\n| Redundant inner DFS calls | Yes, many | Eliminated via RED cache |\n| Counterexample reporting | Yes (single lasso) | Yes (single lasso) |\n| Industry adoption | SPIN v1–v3 | SPIN v4+, DiVinE, LTSmin |\n| KTU exam relevance | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 High-Level Block Architecture of the NDFS Engine\n\n```mermaid\nflowchart TD\n    A[Input: Product Graph G = M x B_not_phi] --> B[Initialise colour array: all WHITE]\n    B --> C[Invoke dfs_outer at s0]\n    C --> D{For each successor v of u}\n    D -->|v is CYAN| E[Back-edge detected]\n    D -->|v is WHITE| F[Recurse: dfs_outer v]\n    D -->|v is BLUE| G[Skip — subtree already explored]\n    E --> H[Invoke dfs_inner v]\n    H --> I{Reaches accepting state f in F?}\n    I -->|YES| J[REPORT COUNTEREXAMPLE: cycle s0 ... u to v to ... f to ... v]\n    I -->|NO| K[Return NOT_FOUND]\n    F --> C\n    G --> D\n    K --> D\n    J --> L[TERMINATE: property violated]\n    C --> M[All successors exhausted]\n    M --> N[Colour u BLUE: pop from outer stack]\n    N --> O[Return to caller dfs_outer]\n    O --> P[Stack unwinds to root]\n    P --> Q[REPORT: NO COUNTEREXAMPLE: property holds]\n```\n\n## 4.2 Colour Transition Diagram (Finite-State Machine for $\\text{color}(s)$)\n\n```mermaid\nstateDiagram-v2\n    [*] --> WHITE\n    WHITE --> CYAN : dfs_outer pushes s onto outer stack\n    CYAN --> BLUE : dfs_outer finishes s; pop\n    CYAN --> CYAN : recursive descent / back edge handling\n    BLUE --> [*] : state never revisited for outer DFS\n    note right of CYAN\n        On the current outer DFS stack.\n        Forms a witness for cycle detection\n        when a back edge is found.\n    end note\n    note right of BLUE\n        Subtree fully explored.\n        Cannot contribute an accepting\n        cycle to ancestors on the stack.\n    end note\n```\n\n## 4.3 Architecture of the Optimised (Schwoon–Esparza) Variant\n\n```mermaid\nflowchart LR\n    subgraph OUTER[Outer DFS - dfs_blue]\n        OB[Entry: dfs_blue u] --> OC{u is accepting?}\n        OC -->|YES| OD[Spawn inner DFS - dfs_red]\n        OC -->|NO| OE[Iterate successors of u]\n        OD --> OE\n        OE --> OF{Successor v}\n        OF -->|CYAN| OG{accept_found flag?}\n        OG -->|TRUE| OH[REPORT COUNTEREXAMPLE]\n        OG -->|FALSE| OE\n        OF -->|WHITE| OB\n        OF -->|BLUE| OE\n        OF -->|RED| OI[Skip - cached result]\n    end\n    subgraph INNER[Inner DFS - dfs_red]\n        IB[Entry: dfs_red u] --> IC[Paint u RED]\n        IC --> ID{Successor v on CYAN stack?}\n        ID -->|YES| IE[REPORT COUNTEREXAMPLE]\n        ID -->|NO, WHITE| IB\n        ID -->|NO, RED| IF[Reuse cached seed]\n    end\n    OD -.spawns.-> IB\n```\n\n## 4.4 Data-Flow Diagram of Counterexample Reconstruction\n\n```mermaid\nflowchart TD\n    P1[dfs_outer pushes u on stack S_outer] --> P2[Recurse on v]\n    P2 --> P3[dfs_outer pushes v on S_outer]\n    P3 --> P4{Back edge found w to v?}\n    P4 -->|YES| P5[dfs_inner starts at v]\n    P5 --> P6[Reconstruct path v to v via w]\n    P6 --> P7[Concat: s0 to v plus v to w plus w to v]\n    P7 --> P8[Output Lasso: prefix P then cycle C]\n    P4 -->|NO| P9[Continue exploring]\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n---\n\n### **Q1. [KTU University Exam — Dec 2023, Model]**\n**\"What is a Nested Depth-First Search? Why is it preferred over a single DFS for Büchi emptiness checking?\" (3 Marks)**\n\n**Model Answer:**\n\nNested Depth-First Search (NDFS) is an on-the-fly, two-phase graph-search algorithm used to detect *accepting cycles* in the product of a Kripke structure and a Büchi automaton. It works by running an **outer DFS** (`dfs_outer`) to explore the state space, and whenever a **back edge** is found — i.e., a transition to a state already on the current outer DFS stack (the **cyan stack**) — an **inner DFS** (`dfs_inner`) is spawned to verify whether an accepting state lies on the candidate cycle.\n\nA single DFS is **insufficient** for Büchi acceptance because acceptance requires *two* conditions to hold jointly: (i) reachability of an accepting state $f \\in F$, and (ii) reachability of $f$ back to itself (i.e., $f$ lies on a cycle). A single DFS can only verify the first condition. NDFS verifies both simultaneously by exploiting the cyan stack — its contents automatically encode a path *back* to the cycle's entry point. Moreover, NDFS is **on-the-fly**: it can terminate as soon as one accepting cycle is found, without materialising the entire product graph.\n\n---\n\n### **Q2. [KTU University Exam — June 2024, Model]**\n**\"Explain the role of the cyan and blue colours in NDFS. Why are blue successors skipped in the inner DFS?\" (3 Marks)**\n\n**Model Answer:**\n\nThe **cyan colour** marks every state currently on the outer DFS stack $S_{\\text{outer}}$. A state is coloured cyan the moment `dfs_outer(u)` is entered, and is recoloured **blue** when `dfs_outer(u)` exits. Thus, the cyan set at any time is *precisely* the path $s_0 \\leadsto u$ currently being explored.\n\nThe **blue colour** marks states whose entire outgoing subtree has been exhaustively explored and which can no longer participate in any *unexplored* accepting cycle reachable from the current search. Blue states have been \"popped\" from the cyan stack.\n\nIn the **inner DFS** (`dfs_inner`), only **cyan** successors are followed. **Blue** successors are deliberately skipped. The reason is that the inner DFS is searching for a state on the current outer DFS stack — a state that would close a cycle through the back edge. Since only cyan states lie on the current outer stack, blue states are *guaranteed* to be off-stack and therefore cannot close such a cycle. Skipping them is a soundness-preserving optimisation that dramatically reduces the inner DFS's cost.\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks Each, with Internal Choice)\n\n---\n\n### **Question A (14 Marks)** — `[KTU University Exam — Dec 2023, Adapted]`\n\n**(a) Present the complete algorithm of the basic Nested Depth-First Search (Courcoubetis et al., 1992) for LTL model checking. Clearly distinguish between the outer DFS and inner DFS and the role of colour markings. (7 Marks)**\n\n**Model Solution:**\n\nThe basic NDFS operates on a product graph $G = (S, T, s_0, F)$ where $F \\subseteq S$ is the set of accepting states inherited from the Büchi automaton. The algorithm maintains three disjoint colour sets partitioning $S$:\n\n- **WHITE** — state never visited.\n- **CYAN** — state currently on the outer DFS stack $S_{\\text{outer}}$.\n- **BLUE** — state's subtree fully explored; state is off-stack.\n\n```\nPROCEDURE basic_NDFS(s0):\n    for each s in S: color(s) ← WHITE\n    return dfs_outer(s0)\n\nPROCEDURE dfs_outer(u):\n    color(u) ← CYAN                          // push u onto outer stack\n    for each (u, v) in T do\n        if color(v) = CYAN then              // BACK-EDGE: u → v, v on stack\n            if dfs_inner(v) = FOUND then\n                return COUNTEREXAMPLE_FOUND\n            end if\n        else if color(v) = WHITE then\n            if dfs_outer(v) = COUNTEREXAMPLE_FOUND then\n                return COUNTEREXAMPLE_FOUND\n            end if\n        end if\n        // BLUE v: skip — subtree already fully explored\n    end for\n    color(u) ← BLUE                          // pop u\n    return NO_COUNTEREXAMPLE\n\nPROCEDURE dfs_inner(u):\n    if u ∈ F then return FOUND               // accepting state on candidate cycle\n    for each (u, v) in T do\n        if color(v) = CYAN then\n            if dfs_inner(v) = FOUND then\n                return FOUND\n            end if\n        end if\n        // WHITE and BLUE v: skipped — not on current outer stack\n    end for\n    return NOT_FOUND\n```\n\n**Role of markings:** Cyan encodes the path $s_0 \\leadsto u$ currently being explored. The back edge $u \\to v$ with $v$ cyan closes a cycle consisting of (i) the cyan-stack suffix from $v$ down to $u$, (ii) the edge $u \\to v$. Inner DFS then confirms an accepting state lies on this cycle.\n\n---\n\n**(b) Apply basic NDFS on the following product graph and report whether an accepting cycle exists. Here $F = \\{s_2, s_5\\}$. (7 Marks)**\n\n$$\ns_0 \\to s_1 \\to s_2 \\to s_3 \\to s_4 \\to s_5 \\to s_2 \\to s_1\n$$\n\n(All other transitions are absent. The cycle $s_1 \\to s_2 \\to s_3 \\to s_4 \\to s_5 \\to s_2$ is the suspected lasso.)\n\n**Model Solution:**\n\n| Step | Active Call | Action | Cyan Stack | Blue Set | Inner DFS Call? |\n| :---: | :--- | :--- | :--- | :---: | :---: |\n| 1 | `dfs_outer(s0)` | colour $s_0$ CYAN | $\\{s_0\\}$ | $\\emptyset$ | — |\n| 2 | recurse on $s_1$ | colour $s_1$ CYAN | $\\{s_0, s_1\\}$ | $\\emptyset$ | — |\n| 3 | recurse on $s_2$ | colour $s_2$ CYAN | $\\{s_0, s_1, s_2\\}$ | $\\emptyset$ | — |\n| 4 | iterate $T(s_2) = \\{s_3\\}$ | $s_3$ WHITE → recurse | $\\{s_0, s_1, s_2, s_3\\}$ | $\\emptyset$ | — |\n| 5 | iterate $T(s_3) = \\{s_4\\}$ | $s_4$ WHITE → recurse | $\\{s_0, s_1, s_2, s_3, s_4\\}$ | $\\emptyset$ | — |\n| 6 | iterate $T(s_4) = \\{s_5\\}$ | $s_5$ WHITE → recurse | $\\{s_0, s_1, s_2, s_3, s_4, s_5\\}$ | $\\emptyset$ | — |\n| 7 | iterate $T(s_5) = \\{s_2, s_1\\}$ | $s_2$ is **CYAN** → back edge! | same | same | **YES** — `dfs_inner(s_2)` |\n| 8 | `dfs_inner(s_2)` | $s_2 \\in F$ → **return FOUND** | same | same | — |\n| 9 | unwind | Return **COUNTEREXAMPLE_FOUND** | — | — | TERMINATE |\n\n**Counterexample (lasso) reported:** $\\underbrace{s_0 \\to s_1 \\to s_2}_{\\text{prefix } P} \\;\\; \\underbrace{(s_2 \\to s_3 \\to s_4 \\to s_5 \\to s_2)}_{\\text{cycle } C}$\n\nSince $s_2 \\in F$ lies on the cycle, the cycle is **accepting**. Property $\\phi$ is **violated** by the model. **NDFS reports: COUNTEREXAMPLE FOUND.**\n\n---\n\n### **Question B (14 Marks)** — `[KTU University Exam — June 2024, Adapted]`\n\n**(a) Discuss the worst-case time complexity of basic NDFS and explain how the Schwoon–Esparza optimised NDFS achieves $\\mathcal{O}(\\vert S \\vert + \\vert T \\vert)$. (7 Marks)**\n\n**Model Solution:**\n\n**Basic NDFS complexity:** For every state $s$ processed by the outer DFS, the inner DFS may be invoked *at most* once per back edge incident to $s$. In the worst case, this yields up to $\\vert S \\vert$ invocations of `dfs_inner`, and each invocation may walk through up to $\\vert T \\vert$ transitions (in the worst case, the inner DFS explores the whole graph looking for a cyan successor). Hence\n\n$$\nT_{\\text{basic}} = \\mathcal{O}(\\vert S \\vert) \\cdot \\mathcal{O}(\\vert T \\vert) = \\mathcal{O}(\\vert S \\vert \\cdot \\vert T \\vert).\n$$\n\n**Schwoon–Esparza optimisation:** The key insight is that the result of `dfs_inner(u)` is *the same* every time it is invoked from $u$, so the algorithm can cache the result. The optimised NDFS introduces a fourth colour **RED**:\n\n- When the outer DFS visits an accepting state $f \\in F$, it spawns `dfs_red(f)` instead of `dfs_inner(f)`.\n- `dfs_red` paints every visited state **RED** and propagates this \"REDness\" backward along the outer DFS stack via a `seed` mechanism.\n- A state coloured RED means: \"this state can reach some accepting state currently on the cyan stack.\"\n- A subsequent back edge $(s, s')$ with $s'$ RED immediately reports a counterexample — *no second inner DFS is needed*.\n\nSince each state is painted RED at most once across the entire run, the cumulative work of all `dfs_red` traversals is bounded by $\\mathcal{O}(\\vert S \\vert + \\vert T \\vert)$. Therefore\n\n$$\nT_{\\text{optimised}} = T_{\\text{dfs\\_blue}} + T_{\\text{dfs\\_red}} = \\mathcal{O}(\\vert S \\vert + \\vert T \\vert) + \\mathcal{O}(\\vert S \\vert + \\vert T \\vert) = \\mathcal{O}(\\vert S \\vert + \\vert T \\vert).\n$$\n\nThis is the well-known **worst-case optimal** NDFS.\n\n---\n\n**(b) Write the optimised NDFS algorithm and explain the use of the `seed` data structure. (7 Marks)**\n\n**Model Solution:**\n\n```\nPROCEDURE optimised_NDFS(s0):\n    for each s in S:\n        color(s) ← WHITE\n        seed(s)  ← ∅\n    end for\n    return dfs_blue(s0)\n\nPROCEDURE dfs_blue(u):\n    color(u) ← CYAN\n    if u ∈ F then\n        dfs_red(u)\n    end if\n    for each (u, v) in T do\n        if color(v) = CYAN and v is RED then\n            return COUNTEREXAMPLE_FOUND\n        else if color(v) = WHITE then\n            if dfs_blue(v) = COUNTEREXAMPLE_FOUND then\n                return COUNTEREXAMPLE_FOUND\n            end if\n        end if\n    end for\n    color(u) ← BLUE\n    return NO_COUNTEREXAMPLE\n\nPROCEDURE dfs_red(u):\n    color(u) ← RED\n    for each (u, v) in T do\n        if color(v) = CYAN then\n            // cycle through an outer-stack state — counterexample\n            return COUNTEREXAMPLE_FOUND\n        else if color(v) = WHITE then\n            dfs_red(v)\n        else if color(v) = RED and v ∉ seed(u) then\n            // reuse cached RED information\n            ...\n        end if\n    end for\n```\n\n**Role of `seed`:** When `dfs_red` reaches a state $u$ that is already RED, it has already discovered that some accepting state is reachable from $u$ via the cyan stack. This information is *propagated* back to the current node by adding $u$'s known RED-reachable accepting states into `seed(current)`. The `seed` set thus serves as a *memoisation cache*: it stores, for each RED state, the set of accepting states that are reachable from it along cyan-coloured paths. On subsequent visits, this cache is consulted to avoid redundant inner DFS traversals, reducing the worst case from $\\mathcal{O}(\\vert S \\vert \\cdot \\vert T \\vert)$ to $\\mathcal{O}(\\vert S \\vert + \\vert T \\vert)$.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfalls — Where Students Commonly Lose Marks**\n> 1. **Forgetting to recolour to BLUE on exit.** Many students omit line `color(u) ← BLUE` after the for-loop in `dfs_outer`. Without this, states incorrectly remain cyan and break the soundness argument.\n> 2. **Confusing colour semantics with Tarjan's SCC low-link numbers.** Tarjan's algorithm uses *low-link* values; NDFS uses *simple colours*. Examiners deduct marks for conflating the two.\n> 3. **Skipping BLUE successors in inner DFS but forgetting to skip them in outer DFS as well** (line 18 of Algorithm 1). Both contexts require the skip, but for *different* reasons — explain the difference.\n> 4. **Not stating the complexity with the right asymptotic notation.** $\\mathcal{O}(\\vert S \\vert \\cdot \\vert T \\vert)$ for basic, $\\mathcal{O}(\\vert S \\vert + \\vert T \\vert)$ for optimised. Mixing the two is a common error.\n> 5. **Failing to justify why the back edge forms a cycle.** Always mention: \"the cyan stack provides a path $s_0 \\leadsto v$, and the back edge $u \\to v$ closes the cycle.\"\n> 6. **Omitting the counterexample reconstruction step.** A lasso has a *prefix* (path $s_0 \\leadsto v$) and a *cycle* (the cyan-stack suffix). Examiners expect both parts in the answer.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n> [!IMPORTANT]\n> **NDFS — Quick-Fire Revision Bullets**\n> - **NDFS is for Büchi emptiness checking**, not for plain reachability. It is the core of on-the-fly LTL model checking.\n> - **Two searches**: outer DFS finds *back edges*; inner DFS verifies *acceptance* on the candidate cycle.\n> - **Three (or four) colours**:\n>   - *White* = unvisited\n>   - *Cyan* = on outer stack (current path)\n>   - *Blue* = fully explored, off-stack\n>   - *Red* (optimised) = cache for inner-DFS results\n> - **Back edge** = transition $(u, v)$ with $v$ already cyan. This is the only way `dfs_inner` is triggered.\n> - **Inner DFS skips BLUE successors** — they cannot close a cycle.\n> - **Outer DFS skips BLUE successors** for efficiency (their subtrees are already exhaustively explored and cannot contain a *new* accepting cycle).\n> - **Complexity**:\n>   - Basic: $\\mathcal{O}(\\vert S \\vert \\cdot \\vert T \\vert)$\n>   - Optimised (Schwoon–Esparza): $\\mathcal{O}(\\vert S \\vert + \\vert T \\vert)$\n> - **Memory**: $\\mathcal{O}(\\vert S \\vert)$ for the two stacks + colour array.\n> - **On-the-fly property**: terminates as soon as the first accepting cycle is detected; never needs the full graph in memory.\n> - **Counterexample = lasso** = prefix $P$ (path from $s_0$ to cycle entry) + cycle $C$ (containing at least one $f \\in F$).\n> - **Industry adopters**: SPIN, DiVinE, LTSmin, NuSMV-LTL.\n> - **Key references** (no fabricated editions — these are the canonical papers):\n>   - Courcoubetis, Vardi, Wolper, Yannakakis (1992) — basic NDFS\n>   - Godefroid, Holzmann (1994) — RED-coloured optimisation\n>   - Schwoon, Esparza (2005) — worst-case-optimal NDFS\n> - **Frequent KTU exam questions**: (i) write the algorithm, (ii) trace it on a given graph, (iii) compare complexities, (iv) justify skipping blue successors.\n<!-- SECTION_5_END -->",
          "pyqs": []
        }
      ]
    },
    {
      "id": "m3",
      "title": "Module 3",
      "topics": [
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 3-t1",
          "title": "Linear Temporal Logic (LTL) - Syntax",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 3\n> - **Topic:** Linear Temporal Logic (LTL) - Syntax\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of Linear Temporal Logic (LTL)\n\n**Linear Temporal Logic (LTL)** is a modal temporal logic introduced into computer science by Amir Pnueli in 1977 for the formal specification and verification of concurrent and reactive computer systems. As per the KTU 2024 syllabus, LTL is a **propositional modal logic augmented with temporal operators** that reason about the truth values of propositions along a *single, linear* (totally ordered) sequence of time instants.\n\n> [!IMPORTANT]\n> **KTU Definition (Board Standard):**\n> LTL is a formal specification language used to describe the temporal behavior of reactive systems. It extends classical propositional logic with operators that qualify *when* a proposition must hold (e.g., in the next state, eventually, always, until).\n\nFormally, an **LTL formula** is constructed over a finite or countably infinite set of **atomic propositions** $AP = \\{p_1, p_2, \\ldots, p_n\\}$ drawn from a system model (typically a Kripke structure). The syntax is given by a Backus–Naur Form (BNF) grammar:\n\n$$\n\\phi \\; ::= \\; \\top \\;\\mid\\; \\bot \\;\\mid\\; p \\;\\mid\\; \\neg \\phi \\;\\mid\\; \\phi \\wedge \\phi \\;\\mid\\; \\phi \\vee \\phi \\;\\mid\\; \\phi \\rightarrow \\phi \\;\\mid\\; X\\,\\phi \\;\\mid\\; F\\,\\phi \\;\\mid\\; G\\,\\phi \\;\\mid\\; \\phi\\,U\\,\\phi \\;\\mid\\; \\phi\\,R\\,\\phi\n$$\n\n## 1.2 Conceptual Analogy / Intuition\n\n> [!NOTE]\n> **The \"Train Journey\" Analogy:**\n> Imagine you are narrating the events of a long train journey. Classical logic can only say, *\"The train is moving.\"* (a snapshot fact). **LTL**, however, lets you say things like:\n> - *\"The train **will eventually** arrive at the destination.\"* — $F\\,\\text{arrived}$\n> - *\"The train **always** stays on the rails.\"* — $G\\,\\text{onRails}$\n> - *\"**At the next** station, the doors will open.\"* — $X\\,\\text{doorsOpen}$\n> - *\"The train keeps moving **until** it reaches the station.\"* — $\\text{moving}\\,U\\,\\text{atStation}$\n>\n> In other words, LTL lets you describe a *time-line* of events, not just a single instant.\n\n## 1.3 Key Components of LTL Syntax\n\n| Component Type | Symbols | Role in Formula |\n|---|---|---|\n| **Boolean Constants** | $\\top$ (true), $\\bot$ (false) | Always/never hold |\n| **Atomic Propositions** | $p, q, r \\in AP$ | Basic observable facts (e.g., $x > 5$, door=open) |\n| **Propositional Operators** | $\\neg, \\wedge, \\vee, \\rightarrow$ | Standard logic connectives |\n| **Temporal Operators** | $X, F, G, U, R$ | Qualify time-dependence of truth |\n\n> [!IMPORTANT]\n> **Why \"Linear\"?**\n> The term *linear* in LTL means that time is viewed as a **single, non-branching path of states** $\\pi = s_0 \\rightarrow s_1 \\rightarrow s_2 \\rightarrow \\ldots$ For every state, there is exactly **one** possible future. (Contrast this with **CTL**, which uses branching time — a KTU Module 3 distinction you must remember.)\n\n## 1.4 Standard Notation Cheat-Sheet\n\n> [!NOTE]\n> The five **canonical temporal operators** of LTL:\n> 1. $X\\,\\phi$ — **NeXt** ($\\bigcirc \\phi$)\n> 2. $F\\,\\phi$ — **Future / Eventually** ($\\Diamond \\phi$)\n> 3. $G\\,\\phi$ — **Globally / Always** ($\\Box \\phi$)\n> 4. $\\phi\\,U\\,\\psi$ — **Until**\n> 5. $\\phi\\,R\\,\\psi$ — **Release** (dual of Until)\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Detailed Operator Semantics (Intuitive Layer)\n\n> [!IMPORTANT]\n> The word *\"linear\"* refers to the underlying *computation path* (the Kripke trace), not the *structure* of the formula. Even though LTL formulas contain nested operators, they are evaluated along a *single* infinite path.\n\nThe five temporal operators are best understood by their effect on the *future suffix* of a path $\\pi = s_0, s_1, s_2, \\ldots$:\n\n* **$X\\,\\phi$ (Next)** — $\\phi$ must hold in the **immediate successor state** $s_1$.\n* **$F\\,\\phi$ (Eventually / Future)** — $\\phi$ must hold in **at least one future state** $s_i$ for some $i \\geq 0$.\n* **$G\\,\\phi$ (Globally / Always)** — $\\phi$ must hold in **every future state** $s_0, s_1, s_2, \\ldots$\n* **$\\phi\\,U\\,\\psi$ (Until)** — $\\phi$ must hold in **all states until** the first state where $\\psi$ holds (and $\\psi$ must eventually hold).\n* **$\\phi\\,R\\,\\psi$ (Release)** — $\\psi$ must hold **until and including** the first state where $\\phi$ holds; if $\\phi$ never holds, $\\psi$ must hold **forever**.\n\n## 2.2 Hierarchical Grammar Rules\n\nThe LTL grammar can be partitioned into **three precedence layers**, which is essential for parsing KTU board problems correctly:\n\n| Precedence Level | Operators | Associativity |\n|:---:|:---:|:---:|\n| **1 (Highest)** | $X, F, G$ (unary) | Prefix |\n| **2** | $U, R$ (binary) | Right-associative |\n| **3** | $\\wedge, \\vee$ | Left-associative |\n| **4 (Lowest)** | $\\rightarrow$ | Right-associative |\n| **5** | $\\neg$ (unary) | Prefix, binds tightest of all |\n\n> [!NOTE]\n> **Negation Normal Form (NNF):** A KTU-high-yield concept. Every LTL formula can be rewritten so that $\\neg$ appears **only directly in front of atomic propositions**. Standard rewrites:\n> $\\neg X\\phi \\equiv X\\neg\\phi$, $\\neg F\\phi \\equiv G\\neg\\phi$, $\\neg G\\phi \\equiv F\\neg\\phi$, $\\neg(\\phi U \\psi) \\equiv \\neg\\phi\\,R\\,\\neg\\psi$, $\\neg(\\phi R \\psi) \\equiv \\neg\\phi\\,U\\,\\neg\\psi$.\n\n## 2.3 KTU Formula Sheet / Cheat Sheet\n\n| # | LTL Formula | Standard Symbol | Intuitive English Reading |\n|---|---|:---:|---|\n| 1 | $X\\,\\phi$ | $\\bigcirc \\phi$ | \"In the next state, $\\phi$ holds\" |\n| 2 | $F\\,\\phi$ | $\\Diamond \\phi$ | \"Eventually $\\phi$ will hold\" |\n| 3 | $G\\,\\phi$ | $\\Box \\phi$ | \"$\\phi$ holds forever / globally\" |\n| 4 | $\\phi\\,U\\,\\psi$ | $\\phi\\,\\mathcal{U}\\,\\psi$ | \"$\\phi$ holds until $\\psi$ becomes true\" |\n| 5 | $\\phi\\,R\\,\\psi$ | $\\phi\\,\\mathcal{R}\\,\\psi$ | \"$\\psi$ holds until $\\phi$ releases it\" |\n| 6 | $F\\,G\\,\\phi$ | $\\Diamond \\Box \\phi$ | \"Eventually $\\phi$ holds forever\" (stabilization) |\n| 7 | $G\\,F\\,\\phi$ | $\\Box \\Diamond \\phi$ | \"$\\phi$ holds infinitely often\" |\n| 8 | $G(\\text{req} \\rightarrow F\\,\\text{ack})$ | — | \"Every request is eventually acknowledged\" |\n\n## 2.4 Useful Logical Identities (Derivable)\n\nThese are the **essential LTL equivalences** the KTU board examiner loves to test:\n\n$$\n\\neg F\\,\\phi \\;\\equiv\\; G\\,\\neg \\phi\n$$\n\n$$\n\\neg G\\,\\phi \\;\\equiv\\; F\\,\\neg \\phi\n$$\n\n$$\nF\\,\\phi \\;\\equiv\\; \\top\\,U\\,\\phi\n$$\n\n$$\nG\\,\\phi \\;\\equiv\\; \\bot\\,R\\,\\phi\n$$\n\n$$\n\\phi\\,R\\,\\psi \\;\\equiv\\; \\neg(\\neg\\phi\\,U\\,\\neg\\psi)\n$$\n\n> [!NOTE]\n> **Engineering Utility:** These identities are used during **BDD-based model checking** and **LTL-to-Büchi-automata translation** in tools like **SPIN**, **NuSMV**, and **Cadence SMV** — all of which are part of the KTU 2024 lab/industry exposure component.\n\n## 2.5 Why \"Syntax\" Matters in Verification\n\nThe LTL syntax is the **first step** of the model-checking pipeline:\n\n$$\n\\text{System Model (Kripke } M) \\;+\\; \\text{LTL Property } \\phi \\;\\longrightarrow\\; \\text{Verifier Output (True/False + Counterexample)}\n$$\n\nA *well-formed* LTL formula is the input to algorithms like **LTL-to-NBA translation** (Vardi–Wolper construction), which in turn powers bounded and unbounded model checking.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Step-by-Step Derivation of a Complex LTL Formula\n\n> [!NOTE]\n> **Problem (KTU-style):** Translate the English requirement *\"Every time a request occurs, an acknowledgement must occur sometime in the future\"* into a syntactically well-formed LTL formula. Then convert it to Negation Normal Form (NNF) and to its Release-based equivalent.\n\n### Step 1: Identify the atomic propositions\nLet $r$ = \"request occurs\" and $a$ = \"acknowledgement occurs\". Both are members of $AP$.\n\n### Step 2: Decompose the English into temporal sub-clauses\n- \"Every time a request occurs\" $\\Rightarrow$ **Globally** quantifier $G(\\,r\\,)$\n- \"An acknowledgement must occur sometime in the future\" $\\Rightarrow$ **Future** quantifier $F(\\,a\\,)$\n- The acknowledgement is *implied* by the request $\\Rightarrow$ propositional implication $\\rightarrow$\n\n### Step 3: Assemble the LTL formula\n$$\n\\phi \\;=\\; G\\bigl(r \\rightarrow F\\,a\\bigr)\n$$\n\nThis is read as: *\"Globally, if $r$ holds now, then $a$ will hold in some future state.\"*\n\n### Step 4: Push negation inward (NNF conversion)\nAssume we wish to compute the *bad-event* formula $\\neg\\phi$ (used in constructing the Büchi automaton for the complement).\n\n$$\n\\neg\\phi \\;=\\; \\neg G(r \\rightarrow F\\,a)\n$$\n\nUsing the duality $\\neg G \\equiv F\\neg$ and material implication $r \\rightarrow F\\,a \\equiv \\neg r \\vee F\\,a$:\n\n$$\n\\neg\\phi \\;\\equiv\\; F\\,\\neg(\\neg r \\vee F\\,a)\n$$\n\n$$\n\\neg\\phi \\;\\equiv\\; F\\,(r \\wedge \\neg F\\,a)\n$$\n\nApply the duality $\\neg F\\,a \\equiv G\\,\\neg a$:\n\n$$\n\\neg\\phi \\;\\equiv\\; F\\,(r \\wedge G\\,\\neg a)\n$$\n\nThis NNF formula is what is fed to the NBA construction algorithm.\n\n### Step 5: Convert to Release-only form (optional, for canonical LTL)\nUsing the identity $F\\,\\alpha \\equiv \\top\\,U\\,\\alpha$ and the dual of $U$:\n\n$$\n\\phi \\;=\\; G(r \\rightarrow \\top\\,U\\,a) \\;\\equiv\\; \\bot\\,R\\,(r \\rightarrow \\top\\,U\\,a)\n$$\n\nHence, an *only-$R$* LTL formula.\n\n## 3.2 Multi-Step Algebraic Derivation of an Equivalence\n\n> [!NOTE]\n> **Goal:** Derive the classical identity $G\\,\\phi \\equiv \\phi \\wedge X\\,G\\,\\phi$ used as the *expansion law* for the $G$ operator in Kripke-structure evaluation.\n\n### Step 1: State the definition (semantic)\n$\\pi \\models G\\,\\phi$ means $\\pi_i \\models \\phi$ for **every** $i \\geq 0$.\n\n### Step 2: Split the \"every\" quantifier\n\"$\\phi$ holds at $i=0$\" **and** \"$\\phi$ holds at every $i \\geq 1$\":\n\n$$\n\\pi \\models G\\,\\phi \\;\\Longleftrightarrow\\; \\pi_0 \\models \\phi \\;\\wedge\\; \\pi_1, \\pi_2, \\ldots \\models G\\,\\phi\n$$\n\n### Step 3: Recognize the second part as a recursive call\nThe suffix $\\pi_1, \\pi_2, \\ldots$ is itself a path that must satisfy $G\\,\\phi$. So the second part is exactly $X\\,G\\,\\phi$.\n\n### Step 4: Combine\n\n$$\n\\begin{aligned}\n\\pi \\models G\\,\\phi \\;&\\Longleftrightarrow\\; \\pi_0 \\models \\phi \\;\\wedge\\; \\pi \\models X\\,G\\,\\phi \\\\\n&\\Longleftrightarrow\\; \\pi \\models \\phi \\;\\wedge\\; X\\,G\\,\\phi \\\\\n&\\Longleftrightarrow\\; \\pi \\models \\phi \\wedge X\\,G\\,\\phi\n\\end{aligned}\n$$\n\nHence, the **expansion law** is proved.\n\n## 3.3 Analytical Matrix: Operator Behaviour Summary\n\n| Operator | Reads as | Time-window | Holds if… | Dual / Complement |\n|:---:|:---:|:---:|:---:|:---:|\n| $X\\,\\phi$ | \"next $\\phi$\" | 1 step | $\\phi$ at $s_1$ | $\\neg X\\,\\phi$ |\n| $F\\,\\phi$ | \"eventually $\\phi$\" | $0 \\ldots \\infty$ | $\\phi$ at some $s_i$ | $G\\,\\neg\\phi$ |\n| $G\\,\\phi$ | \"always $\\phi$\" | $0 \\ldots \\infty$ | $\\phi$ at all $s_i$ | $F\\,\\neg\\phi$ |\n| $\\phi\\,U\\,\\psi$ | \"$\\phi$ until $\\psi$\" | $0 \\ldots \\infty$ | $\\phi$ at $s_0 \\ldots s_{k-1}$ and $\\psi$ at $s_k$ | $\\neg\\phi\\,R\\,\\neg\\psi$ |\n| $\\phi\\,R\\,\\psi$ | \"$\\psi$ released by $\\phi$\" | $0 \\ldots \\infty$ | $\\psi$ at all $s_0 \\ldots s_{k-1}$ and $\\phi$ at $s_k$; OR $\\psi$ forever | $\\neg\\phi\\,U\\,\\neg\\psi$ |\n\n## 3.4 Worked Example: LTL Formula Construction\n\n> [!NOTE]\n> **Specification:** *\"Whenever the train is in motion, it must remain in motion continuously until it arrives at a station, and once it has arrived, the doors will eventually open.\"*\n>\n> Let $m$ = \"in motion\", $a$ = \"at station\", $d$ = \"doors open\".\n\n### Solution\nThe phrase *\"remain in motion continuously until it arrives\"* maps to the **Until** operator with operands $m$ and $a$:\n\n$$\nm\\,U\\,a\n$$\n\nThe phrase *\"once arrived, doors eventually open\"* maps to $F\\,d$, but since the condition is *triggered by* $a$, we wrap it in a globally-enforced implication:\n\n$$\nG\\bigl(a \\rightarrow F\\,d\\bigr)\n$$\n\nCombining both sub-requirements with logical AND:\n\n$$\n\\phi \\;=\\; \\bigl(m\\,U\\,a\\bigr) \\;\\wedge\\; G\\bigl(a \\rightarrow F\\,d\\bigr)\n$$\n\nThe outer conjunction reflects the KTU board convention: *both* sub-properties must be satisfied by every execution of the system.\n\n## 3.5 Closure of an LTL Formula\n\n> [!IMPORTANT]\n> The **closure** $cl(\\phi)$ of an LTL formula is the smallest set containing $\\phi$, all its sub-formulas, and the negations of all its atomic-proposition sub-formulas. This is required during NBA construction. For $\\phi = G(a \\rightarrow F\\,b)$:\n>\n> $cl(\\phi) = \\{\\,G(a \\rightarrow F\\,b),\\; a \\rightarrow F\\,b,\\; a,\\; F\\,b,\\; b,\\; \\neg a,\\; \\neg b\\,\\}$\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Mermaid Block Diagram: LTL Syntax Tree (Top-Down)\n\n```mermaid\ngraph TD\n    rootPhi[\"LTL Formula phi\"]\n    rootPhi --> booleNode[\"Boolean Layer\"]\n    rootPhi --> propNode[\"Propositional Layer\"]\n    rootPhi --> tempNode[\"Temporal Layer\"]\n\n    booleNode --> boolT[\"true (T)\"]\n    booleNode --> boolF[\"false (F)\"]\n\n    propNode --> propAp[\"Atomic Propositions\"]\n    propNode --> propNot[\"NOT (neg)\"]\n    propNode --> propAnd[\"AND (and)\"]\n    propNode --> propOr[\"OR (or)\"]\n    propNode --> propImp[\"IMPLIES (to)\"]\n\n    propAp --> apP1[\"p\"]\n    propAp --> apP2[\"q\"]\n    propAp --> apP3[\"r\"]\n\n    tempNode --> tempX[\"X Next\"]\n    tempNode --> tempF[\"F Eventually\"]\n    tempNode --> tempG[\"G Globally\"]\n    tempNode --> tempU[\"U Until\"]\n    tempNode --> tempR[\"R Release\"]\n\n    tempU --> uLeft[\"left operand phi\"]\n    tempU --> uRight[\"right operand psi\"]\n    tempR --> rLeft[\"left operand phi\"]\n    tempR --> rRight[\"right operand psi\"]\n```\n\n## 4.2 Mermaid Flow Diagram: LTL Formula Construction Pipeline\n\n```mermaid\nflowchart LR\n    step1[\"System Requirement (English)\"] --> step2[\"Identify Atomic Propositions in AP\"]\n    step2 --> step3[\"Assign Propositional Variables e.g. p q r\"]\n    step3 --> step4[\"Apply Temporal Operators X F G U R\"]\n    step4 --> step5[\"Combine with Boolean Connectives\"]\n    step5 --> step6[\"Well-Formed LTL Formula phi\"]\n    step6 --> step7[\"Convert to NNF\"]\n    step7 --> step8[\"Translate phi to NBA using Vardi-Wolper\"]\n    step8 --> step9[\"Run Model Checking M phi\"]\n    step9 --> step10{\"Result\"}\n    step10 -- \"true\" --> step11[\"Property Holds on Model M\"]\n    step10 -- \"false\" --> step12[\"Counterexample Path Returned\"]\n```\n\n## 4.3 Mermaid State Diagram: Operator Expansion Laws\n\n```mermaid\nstateDiagram-v2\n    [*] --> Start\n    Start --> ExpandG: G phi\n    ExpandG --> PhiConj: phi and X G phi\n    PhiConj --> ExpandG\n\n    Start --> ExpandF: F phi\n    ExpandF --> PhiDisjX: phi or X F phi\n    PhiDisjX --> ExpandF\n\n    Start --> ExpandU: phi U psi\n    ExpandU --> PsiDisj: psi or (phi and X phi U psi)\n    PsiDisj --> ExpandU\n\n    Start --> ExpandR: phi R psi\n    ExpandR --> PsiConj: psi and (phi or X phi R psi)\n    PsiConj --> ExpandR\n\n    ExpandG --> [*]\n    ExpandF --> [*]\n    ExpandU --> [*]\n    ExpandR --> [*]\n```\n\n## 4.4 Block-Level Functional Architecture: LTL in the Verification Stack\n\n```mermaid\ngraph TD\n    sysModel[\"System Model: Kripke Structure M = S S0 R L\"]\n    atomicSet[\"Atomic Propositions Set AP\"]\n    ltlInput[\"LTL Property Specification phi\"]\n    parser[\"Syntax Parser (BNF Grammar)\"]\n    nnfConv[\"Negation Normal Form Converter\"]\n    nbaBuild[\"LTL to NBA Translator (Vardi-Wolper)\"]\n    nbaPhi[\"NBA for phi\"]\n    nbaNotPhi[\"NBA for NOT phi\"]\n    product[\"Product Automaton M x NBA\"]\n    check[\"Emptiness Check\"]\n    verdict[\"Verification Result\"]\n\n    sysModel --> product\n    atomicSet --> ltlInput\n    ltlInput --> parser\n    parser --> nnfConv\n    nnfConv --> nbaBuild\n    nbaBuild --> nbaPhi\n    nbaBuild --> nbaNotPhi\n    nbaPhi --> product\n    nbaNotPhi --> product\n    product --> check\n    check --> verdict\n```\n\n> [!NOTE]\n> **Reading the diagram:** The LTL formula flows from the *left* (English requirement) to the *right* (verdict) through a sequence of standard automata-theoretic transformations. The **syntax stage** of the pipeline corresponds to the boxes labelled *Parser* and *NNF Converter*.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Question 1 `[KTU University Exam - Dec 2023]`\n**Define Linear Temporal Logic (LTL). List all the temporal operators used in LTL with their intuitive meaning.**\n\n**Model Answer (KTU Board Key Pattern):**\n\n> **Definition (1 Mark):** LTL is a modal temporal logic used to specify the behaviour of reactive systems by extending propositional logic with operators that reason about the *order of events in time* along a single, linear execution path.\n\n> **Five Temporal Operators (2 Marks):**\n> 1. **$X\\,\\phi$ (Next / $\\bigcirc \\phi$):** $\\phi$ holds in the next state.\n> 2. **$F\\,\\phi$ (Eventually / $\\Diamond \\phi$):** $\\phi$ holds in some future state.\n> 3. **$G\\,\\phi$ (Always / $\\Box \\phi$):** $\\phi$ holds in all future states.\n> 4. **$\\phi\\,U\\,\\psi$ (Until):** $\\phi$ holds continuously until $\\psi$ first becomes true.\n> 5. **$\\phi\\,R\\,\\psi$ (Release):** $\\psi$ holds until (and including) the state where $\\phi$ becomes true; if $\\phi$ never holds, $\\psi$ holds forever.\n\n---\n\n### Question 2 `[KTU University Exam - May 2024]`\n**State the BNF grammar of LTL formulas. Why is the term \"linear\" used in LTL?**\n\n**Model Answer (KTU Board Key Pattern):**\n\n> **BNF Grammar (2 Marks):**\n>\n> $\\phi ::= \\top \\;\\mid\\; \\bot \\;\\mid\\; p \\;\\mid\\; \\neg \\phi \\;\\mid\\; \\phi \\wedge \\phi \\;\\mid\\; \\phi \\vee \\phi \\;\\mid\\; \\phi \\rightarrow \\phi \\;\\mid\\; X\\,\\phi \\;\\mid\\; F\\,\\phi \\;\\mid\\; G\\,\\phi \\;\\mid\\; \\phi\\,U\\,\\phi \\;\\mid\\; \\phi\\,R\\,\\phi$\n\n> **Why \"Linear\" (1 Mark):** Time is modelled as a *single, non-branching* sequence of states (one possible future at every instant), in contrast to branching-time logics like CTL.\n\n---\n\n## 5.2 Part B Questions (14 Marks Each — Internal Choice)\n\n### Question A `[KTU University Exam - Dec 2023]`\n\n**(a)** Explain the **syntax** of LTL in detail. Define all five temporal operators with examples. State the **BNF grammar** and identify the **precedence** of operators. **(7 Marks)**\n\n**(b)** For each of the following English requirements, write a syntactically well-formed LTL formula. Assume $AP = \\{r, a, e, s, d\\}$. Also, convert each formula into **Negation Normal Form (NNF)**. **(7 Marks)**\n\n1. \"Every request $r$ is eventually acknowledged by $a$.\"\n2. \"If an error $e$ occurs, then the system stays in safe-mode $s$ forever after.\"\n3. \"The system is always in either operational $d$ or maintenance $m$ mode, and once in maintenance, it eventually returns to operational.\"\n\n---\n\n**Model Solution:**\n\n#### Part (a) — Detailed Syntax Explanation\n\n> **Definition (2 Marks):** LTL formulas are built inductively from a set $AP$ of atomic propositions using Boolean and temporal operators. A formula is evaluated over an *infinite* path $\\pi = s_0, s_1, s_2, \\ldots$ of a Kripke structure.\n\n> **Five Temporal Operators (3 Marks):**\n\n| Operator | Formal Semantics | Example Formula | English Reading |\n|:---:|:---|:---:|:---|\n| $X\\,\\phi$ | $\\pi \\models X\\,\\phi \\iff \\pi^1 \\models \\phi$ | $X\\,a$ | \"In the next state, $a$ holds.\" |\n| $F\\,\\phi$ | $\\pi \\models F\\,\\phi \\iff \\exists k \\geq 0: \\pi^k \\models \\phi$ | $F\\,d$ | \"Eventually, $d$ holds.\" |\n| $G\\,\\phi$ | $\\pi \\models G\\,\\phi \\iff \\forall k \\geq 0: \\pi^k \\models \\phi$ | $G\\,s$ | \"Forever, $s$ holds.\" |\n| $\\phi\\,U\\,\\psi$ | $\\pi \\models \\phi\\,U\\,\\psi \\iff \\exists k: \\pi^k \\models \\psi \\land \\forall j < k: \\pi^j \\models \\phi$ | $r\\,U\\,a$ | \"$r$ holds until $a$ holds.\" |\n| $\\phi\\,R\\,\\psi$ | $\\pi \\models \\phi\\,R\\,\\psi \\iff \\forall k: (\\pi^k \\models \\phi \\rightarrow \\exists j \\leq k: \\pi^j \\models \\psi) \\lor \\forall k: \\pi^k \\models \\psi$ | $e\\,R\\,s$ | \"$s$ holds until $e$ releases it.\" |\n\n> **BNF Grammar (1 Mark):**\n>\n> $\\phi ::= \\top \\;\\mid\\; \\bot \\;\\mid\\; p \\;\\mid\\; \\neg \\phi \\;\\mid\\; \\phi \\wedge \\phi \\;\\mid\\; \\phi \\vee \\phi \\;\\mid\\; \\phi \\rightarrow \\phi \\;\\mid\\; X\\,\\phi \\;\\mid\\; F\\,\\phi \\;\\mid\\; G\\,\\phi \\;\\mid\\; \\phi\\,U\\,\\phi \\;\\mid\\; \\phi\\,R\\,\\phi$\n\n> **Precedence (1 Mark):** $\\neg$ > $X, F, G$ > $U, R$ > $\\wedge, \\vee$ > $\\rightarrow$.\n\n#### Part (b) — LTL Formula Construction\n\n> **Requirement 1:** \"Every request $r$ is eventually acknowledged by $a$.\"\n\nStep 1: \"Every …\" → global scope $G(\\,)$.\nStep 2: \"request → eventually acknowledged\" → $(r \\rightarrow F\\,a)$.\n\n$$\n\\phi_1 \\;=\\; G\\bigl(r \\rightarrow F\\,a\\bigr)\n$$\n\n**NNF of $\\phi_1$:**\n\n$$\n\\neg\\phi_1 \\;\\equiv\\; F\\,(r \\wedge G\\,\\neg a)\n$$\n\n> **Requirement 2:** \"If an error $e$ occurs, then the system stays in safe-mode $s$ forever after.\"\n\nStep 1: \"If … then\" → implication $\\rightarrow$.\nStep 2: \"Forever after\" → $G\\,s$.\n\n$$\n\\phi_2 \\;=\\; G\\bigl(e \\rightarrow G\\,s\\bigr)\n$$\n\n**NNF of $\\phi_2$:**\n\n$$\n\\neg\\phi_2 \\;\\equiv\\; F\\,\\bigl(e \\wedge F\\,\\neg s\\bigr)\n$$\n\n> **Requirement 3:** Two-part requirement with eventual return.\n\nStep 1: Always in $d$ or $m$ $\\Rightarrow$ $G(d \\vee m)$.\nStep 2: Once in $m$, eventually $d$ $\\Rightarrow$ $G(m \\rightarrow F\\,d)$.\n\n$$\n\\phi_3 \\;=\\; G(d \\vee m) \\;\\wedge\\; G(m \\rightarrow F\\,d)\n$$\n\n**NNF of $\\phi_3$:** Already in NNF, because all negations (none) are on atomic propositions.\n\n---\n\n### Question B `[KTU University Exam - May 2024]`\n\n**(a)** With a neat block diagram, describe the **role of LTL syntax in the model-checking pipeline**. Explain how an LTL formula is converted into a **Büchi automaton** at a high level. **(7 Marks)**\n\n**(b)** For each of the following LTL formulas, state whether the formula is in **Negation Normal Form (NNF)**. If not, rewrite it in NNF, showing all intermediate steps. **(7 Marks)**\n\n1. $\\neg F\\,G\\,p$\n2. $G\\,(p \\rightarrow \\neg F\\,q)$\n3. $\\neg(p\\,U\\,q)$\n4. $F\\,(p \\rightarrow G\\,q)$\n\n---\n\n**Model Solution:**\n\n#### Part (a) — LTL Syntax in the Model-Checking Pipeline\n\n> **Pipeline (4 Marks):**\n> 1. **System modelling:** The system $M$ is abstracted as a Kripke structure $M = (S, S_0, R, L)$.\n> 2. **Property specification:** A *correctness* requirement is written as an LTL formula $\\phi$ using the BNF grammar.\n> 3. **Syntax parsing & NNF conversion:** The parser validates $\\phi$ and pushes negations inward.\n> 4. **NBA translation:** $\\phi$ is translated to a *Generalized Büchi Automaton* (GBA) then determinized to a *Nondeterministic Büchi Automaton* (NBA) via Vardi–Wolper.\n> 5. **Product construction:** The NBA is intersected with $M$.\n> 6. **Emptiness check:** A counterexample is produced iff the product language is non-empty.\n\n> **Block Diagram (3 Marks):**\n\n```\n+-------------+       +---------+       +-----------+\n| System M    |       | LTL phi |       | Negation  |\n| (Kripke)    |       | (input) |       | Normal Fm |\n+------+------+       +----+----+       +-----+-----+\n       |                   |                  |\n       |                   v                  v\n       |            +-------------+    +---------------+\n       |            | BNF Parser  |    | (push negs in) |\n       |            +------+------+    +-------+-------+\n       |                   |                  |\n       |                   +-------+----------+\n       |                           |\n       |                           v\n       |                  +------------------+\n       +----------------->| Vardi-Wolper     |\n                          | LTL -> NBA       |\n                          +---------+--------+\n                                    |\n                                    v\n                          +------------------+\n                          | NBA for phi      |\n                          +---------+--------+\n                                    |\n                                    v\n                          +------------------+\n                          | Product M x NBA  |\n                          +---------+--------+\n                                    |\n                                    v\n                          +------------------+\n                          | Emptiness Check  |\n                          +---------+--------+\n                                    |\n                                    v\n                          +------------------+\n                          | YES -> Holds     |\n                          | NO  -> Counterex |\n                          +------------------+\n```\n\n#### Part (b) — NNF Conversions\n\n> **1. $\\neg F\\,G\\,p$ (1.5 Marks)**\n\n$$\n\\neg F\\,G\\,p \\;\\equiv\\; G\\,\\neg G\\,p \\;\\equiv\\; G\\,F\\,\\neg p\n$$\n\n✓ Now in NNF.\n\n> **2. $G\\,(p \\rightarrow \\neg F\\,q)$ (2 Marks)**\n\nFirst rewrite implication:\n\n$$\nG\\,(\\neg p \\vee \\neg F\\,q)\n$$\n\nThen push negation inside the inner $F$:\n\n$$\nG\\,(\\neg p \\vee G\\,\\neg q)\n$$\n\n✓ Now in NNF.\n\n> **3. $\\neg(p\\,U\\,q)$ (2 Marks)**\n\nApply the dual of $U$ — that is, convert to $R$:\n\n$$\n\\neg(p\\,U\\,q) \\;\\equiv\\; \\neg p\\,R\\,\\neg q\n$$\n\n✓ Now in NNF.\n\n> **4. $F\\,(p \\rightarrow G\\,q)$ (1.5 Marks)**\n\nRewrite implication:\n\n$$\nF\\,(\\neg p \\vee G\\,q)\n$$\n\n✓ Already in NNF (no negations of compound formulas exist).\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Common Pitfalls:**\n> 1. **Confusing LTL with CTL (2–3 marks lost).** The most common error. Remember: LTL is *linear-time*; do not write path quantifiers $A$ or $E$ unless it is CTL.\n> 2. **Forgetting precedence of operators** when parenthesising a formula. Always add parentheses to make precedence explicit in board answers.\n> 3. **Writing $\\neg F\\,G$ instead of $G\\,F\\,\\neg$** during NNF conversion. Step-by-step rewriting is mandatory — do not skip intermediate states.\n> 4. **Misinterpreting $R$ (Release) as $U$ (Until).** They are *duals*, not synonyms. $\\phi\\,R\\,\\psi$ does **not** require $\\phi$ to eventually hold.\n> 5. **Drawing the NBA block diagram** as part of the LTL-syntax question. Stay within the *syntax* boundary; automata construction is a separate KTU topic.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!NOTE]\n> **Rapid-Revision Checklist — LTL Syntax**\n\n- **LTL = Linear Temporal Logic** — modal logic over a *single* (linear) time path.\n- **Underlying model:** A Kripke structure $M = (S, S_0, R, L)$ plus a path $\\pi = s_0, s_1, \\ldots$\n- **Atomic propositions** $AP$ are the only \"sensors\" of the system — they label states.\n- **Five canonical temporal operators:** $X, F, G, U, R$.\n- **Standard symbols:** $X = \\bigcirc$, $F = \\Diamond$, $G = \\Box$.\n- **$F\\,\\phi$ and $G\\,\\phi$ are duals:** $\\neg F \\equiv G\\neg$ and $\\neg G \\equiv F\\neg$.\n- **$U$ and $R$ are duals:** $\\neg(\\phi\\,U\\,\\psi) \\equiv \\neg\\phi\\,R\\,\\neg\\psi$.\n- **BNF grammar** must be memorised verbatim for the 3-mark definition question.\n- **Operator precedence:** $\\neg$ > $X, F, G$ > $U, R$ > $\\wedge, \\vee$ > $\\rightarrow$.\n- **NNF** has $\\neg$ only on atomic propositions; always reach it before NBA translation.\n- **Expansion laws:** $G\\phi \\equiv \\phi \\wedge X\\,G\\phi$, $F\\phi \\equiv \\phi \\vee X\\,F\\phi$.\n- **Common safety property:** $G\\,\\neg(\\text{bad})$.\n- **Common liveness property:** $G(\\text{req} \\rightarrow F\\,\\text{ack})$.\n- **Difference from CTL:** No path quantifiers $A$ / $E$ — paths are implicit and unique.\n- **Tools used in practice:** SPIN, NuSMV, Cadence SMV, CBMC — all consume LTL syntax as input.\n- **Closure $cl(\\phi)$** is the smallest set of sub-formulas including negated atoms — required for NBA construction.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 3-t2",
          "title": "Semantics",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module Module 3\n> - **Topic:** Semantics\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of Semantics (KTU 2024 Terminology)\n\nIn the context of **Automated Verification**, **Semantics** is the rigorous, mathematically precise study of the *meaning* of programs, systems, and specification languages. It assigns a well-defined mathematical object (a value, a function, a set of behaviours, or a transition relation) to every syntactic construct of a language, thereby eliminating the ambiguity of natural-language interpretation.\n\n> [!IMPORTANT]\n> **KTU Syllabus Definition:**\n> *Semantics is the branch of formal methods that formally defines the behaviour of a program or a transition system as a mathematical function, relation, or logical model, enabling mechanized reasoning about correctness, safety, and liveness properties.*\n\nThere are three classical pillars of formal semantics used universally in verification:\n\n| Pillar | Core Idea | Mathematical Object |\n| :--- | :--- | :--- |\n| **Operational Semantics** | Describes *how* a program executes step-by-step. | Transition relation $\\rightarrow$ over configurations. |\n| **Denotational Semantics** | Describes *what* a program computes, abstractly. | A function from states to values: $\\llbracket e \\rrbracket : \\Sigma \\to V$. |\n| **Axiomatic Semantics** | Describes *what must be true* before/after execution. | Logical proof rules (e.g., Hoare triples $\\{P\\}C\\{Q\\}$). |\n\n> [!NOTE]\n> In **Model Checking** (the heart of automated verification), the system under verification is interpreted as a *Kripke Structure* and specifications are interpreted as *temporal logic formulas*. Both interpretations are *semantic mappings*.\n\n## 1.2 Conceptual Analogy — \"The Recipe of a Cake\"\n\nImagine a recipe written in a foreign language. The **syntax** is the written list of ingredients and bullet points. It is meaningless until you know what each step *does*.\n\n- **Operational Semantics** is like a **video of a chef** executing the recipe: spoon by spoon, stir by stir, you see exactly *how* the cake is built. (Small-step = every motion; Big-step = final cake from ingredients.)\n- **Denotational Semantics** is like a **nutrition label** on the finished cake: it abstracts away the steps and simply tells you the *mathematical mapping* from \"input ingredients\" to \"output cake\" — a pure function.\n- **Axiomatic Semantics** is like a **quality checklist**: \"If the oven is at 180°C **before**, then the cake will be golden brown **after**.\" (Pre-condition $\\to$ Post-condition.)\n\nIn automated verification, we cannot rely on a chef's intuition — we need the **video (operational)** or the **label (denotational)** or the **checklist (axiomatic)** so a machine can mechanically prove the cake is correct.\n\n## 1.3 Why Semantics is Indispensable for Automated Verification\n\nA verification tool (model checker, theorem prover, SAT/SMT solver) operates on **mathematical objects**, not on textual programs. The semantic mapping is the *bridge* that converts human-written code/spec into something the algorithm can reason about.\n\n> [!IMPORTANT]\n> **Key Insight:** *Without a formal semantics, \"the program is correct\" is an informal opinion. With semantics, it becomes a theorem provable by a machine.*\n\nThe standard metrics used in the field are:\n- **Soundness:** If the semantics proves a property, it is *actually* true ($\\models \\Rightarrow \\vdash$).\n- **Completeness:** If a property is *actually* true, the semantics can prove it ($\\models \\Leftarrow \\vdash$).\n- **Decidability:** Whether an algorithm can decide truth in finite time.\n\nThese three metrics are evaluated for **every** semantic framework used in verification.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Operational Semantics — The Engine of Execution\n\nOperational semantics defines a binary **transition relation** $\\rightarrow$ on the set of **configurations** $\\Gamma$. A configuration is a pair $\\langle c, \\sigma \\rangle$ where $c$ is a control state (program counter / statement) and $\\sigma$ is a store (mapping variables to values).\n\n### 2.1.1 Small-Step (Structural) Operational Semantics (SOS)\n\n- Introduced by **Gordon Plotkin** in 1981 as the canonical framework.\n- Defines the smallest meaningful computation: a single evaluation step.\n- Transition form: $\\langle e, \\sigma \\rangle \\rightarrow \\langle e', \\sigma' \\rangle$ meaning \"expression $e$ under store $\\sigma$ reduces in one step to $e'$ under $\\sigma'$.\"\n- Uses **inductive rules** of the form $\\dfrac{\\text{premises}}{\\text{conclusion}}$ (Natural Deduction style).\n\n**Why it matters in verification:** Small-step semantics gives the *finest granularity* of execution. Model checkers like **SPIN** (for Promela) and the operational semantics of the **K Framework** rely on it.\n\n### 2.1.2 Big-Step (Natural) Semantics\n\n- Introduced by **Kahn (1987)**.\n- Evaluates an expression to its **final value** in one logical leap.\n- Transition form: $\\langle e, \\sigma \\rangle \\Downarrow v$ meaning \"expression $e$ under $\\sigma$ evaluates to value $v$.\"\n- Useful for proving **equivalence** of programs (denotational-style proof using operational rules).\n\n### 2.1.3 Key Inference Rules (Canonical Examples)\n\nFor arithmetic expressions with variables and addition:\n\n$$\n\\begin{aligned}\n\\text{(VAR)} \\quad & \\frac{}{\\langle x, \\sigma \\rangle \\rightarrow \\langle \\sigma(x), \\sigma \\rangle} \\\\[6pt]\n\\text{(CONST)} \\quad & \\frac{}{\\langle n, \\sigma \\rangle \\rightarrow \\langle n, \\sigma \\rangle} \\\\[6pt]\n\\text{(ADD-1)} \\quad & \\frac{\\langle e_1, \\sigma \\rangle \\rightarrow \\langle e_1', \\sigma \\rangle}{\\langle e_1 + e_2, \\sigma \\rangle \\rightarrow \\langle e_1' + e_2, \\sigma \\rangle} \\\\[6pt]\n\\text{(ADD-2)} \\quad & \\frac{\\langle e_2, \\sigma \\rangle \\rightarrow \\langle e_2', \\sigma \\rangle}{\\langle v_1 + e_2, \\sigma \\rangle \\rightarrow \\langle v_1 + e_2', \\sigma \\rangle} \\\\[6pt]\n\\text{(ADD-VAL)} \\quad & \\frac{}{\\langle n_1 + n_2, \\sigma \\rangle \\rightarrow \\langle n_1 +_{\\mathbb{Z}} n_2, \\sigma \\rangle}\n\\end{aligned}\n$$\n\n> [!NOTE]\n> The first three are called *congruence rules* (they propagate computation inside sub-expressions). The last is a *computation rule* (it performs the actual arithmetic).\n\n## 2.2 Denotational Semantics — The Mathematical Abstraction\n\nIntroduced by **Christopher Strachey** and **Dana Scott** in the late 1960s, denotational semantics assigns to every syntactic phrase a mathematical denotation:\n\n$$\n\\llbracket e \\rrbracket : \\Sigma \\rightarrow V\n$$\n\nwhere $\\Sigma$ is the set of stores and $V$ the set of values. The definition is **compositional**:\n\n$$\n\\llbracket e_1 + e_2 \\rrbracket \\sigma \\;=\\; \\llbracket e_1 \\rrbracket \\sigma +_{\\mathbb{Z}} \\llbracket e_2 \\rrbracket \\sigma\n$$\n\n### 2.2.1 Fixpoint Semantics (Critical for Verification)\n\nPrograms with loops require the meaning to be the **least fixpoint** of a monotonic function $F : \\mathcal{P}(S) \\to \\mathcal{P}(S)$, where $S$ is the state space. By **Tarski's Theorem**, such an $F$ has a least fixpoint:\n\n$$\n\\text{lfp}(F) \\;=\\; \\bigcap \\{ X \\mid F(X) \\subseteq X \\}\n$$\n\nThis is the foundation of abstract interpretation, dataflow analysis, and symbolic model checking.\n\n## 2.3 Axiomatic Semantics — Reasoning About Correctness\n\nIntroduced by **Tony Hoare (1969)**. The central object is the **Hoare Triple**:\n\n$$\n\\{P\\} \\ C \\ \\{Q\\}\n$$\n\n- $P$ = **Pre-condition** (assumed true before $C$).\n- $C$ = **Command** (program fragment).\n- $Q$ = **Post-condition** (must be true after $C$ terminates).\n\nPartial vs Total Correctness:\n- **Partial correctness** $[ \\{P\\} C \\{Q\\} ]$: If $C$ terminates, then $Q$ holds. (Does not require termination.)\n- **Total correctness** $[\\![ \\{P\\} C \\{Q\\} ]\\!]$: $C$ is guaranteed to terminate and $Q$ holds upon termination.\n\n## 2.4 Kripke Structures — The Semantic Backbone of Model Checking\n\nA **Kripke Structure** is a labelled transition system with states labelled by atomic propositions:\n\n$$\nM \\;=\\; (S, S_0, R, L)\n$$\n\n- $S$: finite set of states.\n- $S_0 \\subseteq S$: initial states.\n- $R \\subseteq S \\times S$: transition relation (total, i.e., every state has a successor).\n- $L : S \\to \\mathcal{P}(AP)$: labelling function mapping each state to a set of atomic propositions true in it.\n\n> [!IMPORTANT]\n> A Kripke structure is the **semantic model** of a concurrent / reactive system. CTL/LTL formulas are evaluated on paths through $R$ starting from states in $S_0$.\n\n## 2.5 Linear Temporal Logic (LTL) Semantics\n\nFor a path $\\pi = s_0 s_1 s_2 \\dots$ and formula $\\varphi$:\n\n$$\n\\begin{aligned}\n\\pi \\models p &\\iff p \\in L(s_0) \\\\\n\\pi \\models \\neg \\varphi &\\iff \\pi \\not\\models \\varphi \\\\\n\\pi \\models \\varphi_1 \\mathbf{U} \\varphi_2 &\\iff \\exists j \\geq 0 : \\pi^{j} \\models \\varphi_2 \\text{ and } \\forall 0 \\leq i < j, \\pi^{i} \\models \\varphi_1\n\\end{aligned}\n$$\n\nDerived operators:\n\n$$\n\\mathbf{F}\\varphi \\equiv \\text{true} \\mathbf{U} \\varphi, \\qquad \\mathbf{G}\\varphi \\equiv \\neg \\mathbf{F} \\neg \\varphi\n$$\n\n## 2.6 Computation Tree Logic (CTL) Semantics\n\nIn CTL, the path quantifiers $\\mathbf{A}$ (for all paths) and $\\mathbf{E}$ (there exists a path) are paired with temporal operators:\n\n$$\n\\begin{aligned}\nM, s \\models \\mathbf{A}\\mathbf{X}\\varphi &\\iff \\forall s' : (s,s') \\in R \\Rightarrow M, s' \\models \\varphi \\\\\nM, s \\models \\mathbf{E}\\mathbf{X}\\varphi &\\iff \\exists s' : (s,s') \\in R \\text{ and } M, s' \\models \\varphi \\\\\nM, s \\models \\mathbf{A}\\mathbf{G}\\varphi &\\iff \\forall \\pi \\text{ starting at } s, \\forall i, M, \\pi(i) \\models \\varphi\n\\end{aligned}\n$$\n\n## 2.7 KTU High-Yield Formula Cheat Sheet\n\n> [!IMPORTANT]\n> The following table is the **exam-critical** summary. Every entry is a potential 3-mark or 14-mark question.\n\n| # | Concept | Formula / Rule | Domain | Used For |\n| :--- | :--- | :--- | :--- | :--- |\n| 1 | Small-step transition | $\\langle e, \\sigma \\rangle \\rightarrow \\langle e', \\sigma' \\rangle$ | Configurations | Operational reasoning |\n| 2 | Big-step evaluation | $\\langle e, \\sigma \\rangle \\Downarrow v$ | Expressions to values | Equivalence proofs |\n| 3 | Denotational mapping | $\\llbracket e \\rrbracket : \\Sigma \\to V$ | Compositional meaning | Abstract interpretation |\n| 4 | Least fixpoint | $\\text{lfp}(F) = \\bigcap \\{ X \\mid F(X) \\subseteq X \\}$ | Powerset lattices | Loops, dataflow |\n| 5 | Hoare triple | $\\{P\\} C \\{Q\\}$ | Logic of programs | Axiomatic verification |\n| 6 | Kripke structure | $M = (S, S_0, R, L)$ | State-transition | Model checking |\n| 7 | Path satisfaction | $\\pi \\models \\varphi$ | Infinite sequences | LTL |\n| 8 | LTL Until | $\\pi \\models \\varphi_1 \\mathbf{U} \\varphi_2$ | Bounded future | Liveness |\n| 9 | CTL Next (∀) | $M, s \\models \\mathbf{A}\\mathbf{X}\\varphi$ | All successors | Branching time |\n| 10 | CTL Next (∃) | $M, s \\models \\mathbf{E}\\mathbf{X}\\varphi$ | Some successor | Branching time |\n| 11 | CTL Globally | $M, s \\models \\mathbf{A}\\mathbf{G}\\varphi$ | All paths, all time | Safety (invariants) |\n| 12 | CTL Until | $M, s \\models \\mathbf{E}\\mathbf{U}$ | Some path until | Liveness with possibility |\n| 13 | Greatest fixpoint | $\\text{gfp}(F) = \\bigcup \\{ X \\mid X \\subseteq F(X) \\}$ | Set lattice | Fairness, bisimulation |\n| 14 | Strong bisimulation | $P \\sim Q \\iff \\forall a, P \\xrightarrow{a} P' \\Rightarrow \\exists Q', Q \\xrightarrow{a} Q' \\wedge P' \\sim Q'$ | LTS | Equivalence of processes |\n| 15 | Simulation preorder | $P \\leq Q$ | One-direction refinement | Refinement checking |\n\n## 2.8 Real-World Engineering Utility\n\n| Verification Tool | Semantic Foundation |\n| :--- | :--- |\n| **SPIN** (Bell Labs) | Operational semantics of Promela + LTL. |\n| **NuSMV / nuXmv** | Kripke structures + CTL/LTL. |\n| **Coq / Isabelle / Lean** | Denotational/Axiomatic semantics + higher-order logic. |\n| **CBMC** (Bounded Model Checker) | Operational semantics of C + SAT. |\n| **Java PathFinder** | Operational semantics of Java bytecode. |\n| **Astrée** (static analyzer) | Denotational/abstract interpretation of C. |\n| **K Framework** | Rewriting-based operational semantics (industrial: C, Java, JavaScript, EVM). |\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: Equivalence of Operational and Denotational Semantics\n\nWe will derive, for the simple expression language with constants and addition, the classical result:\n**Big-step operational semantics and denotational semantics assign the same value** (a property called *full abstraction* or *equivalence*).\n\n### Step 1 — Define the Syntactic Set\n\nThe expression grammar:\n$$\ne \\in \\text{Exp} \\;::=\\; n \\;\\mid\\; e_1 + e_2\n$$\nwhere $n \\in \\mathbb{Z}$. Stores: $\\sigma : \\text{Var} \\rightharpoonup \\mathbb{Z}$.\n\n### Step 2 — Define Big-Step Operational Semantics\n\nThe judgment $\\langle e, \\sigma \\rangle \\Downarrow v$ is defined inductively:\n\n$$\n\\begin{aligned}\n\\text{(B-CONST)} \\quad & \\frac{}{\\langle n, \\sigma \\rangle \\Downarrow n} \\\\[6pt]\n\\text{(B-ADD)} \\quad & \\frac{\\langle e_1, \\sigma \\rangle \\Downarrow v_1 \\quad \\langle e_2, \\sigma \\rangle \\Downarrow v_2}{\\langle e_1 + e_2, \\sigma \\rangle \\Downarrow (v_1 +_{\\mathbb{Z}} v_2)}\n\\end{aligned}\n$$\n\n**Logic of the conversion:**\nThe first rule says a constant evaluates to itself. The second says: if $e_1$ evaluates to $v_1$ and $e_2$ evaluates to $v_2$ (independently, under the same store), then the sum evaluates to the integer sum. The premise is read top-to-bottom as a logical AND.\n\n### Step 3 — Define Denotational Semantics\n\nWe define $\\llbracket e \\rrbracket : \\Sigma \\to \\mathbb{Z}$ compositionally:\n\n$$\n\\begin{aligned}\n\\llbracket n \\rrbracket \\sigma &= n \\\\\n\\llbracket e_1 + e_2 \\rrbracket \\sigma &= \\llbracket e_1 \\rrbracket \\sigma \\; +_{\\mathbb{Z}} \\; \\llbracket e_2 \\rrbracket \\sigma\n\\end{aligned}\n$$\n\n**Logic of the conversion:**\nThis is a structural recursion. The meaning of a sum is the integer sum of the meanings of its sub-expressions. No rules, no transitions — just a pure function.\n\n### Step 4 — Equivalence Theorem Statement\n\n$$\n\\forall e \\in \\text{Exp}, \\; \\forall \\sigma \\in \\Sigma : \\langle e, \\sigma \\rangle \\Downarrow v \\;\\Longleftrightarrow\\; \\llbracket e \\rrbracket \\sigma = v\n$$\n\n### Step 5 — Proof by Structural Induction\n\n**Base case:** $e = n$.\n- ($\\Rightarrow$): By (B-CONST), $\\langle n, \\sigma \\rangle \\Downarrow n$. By denotational definition, $\\llbracket n \\rrbracket \\sigma = n$. So $v = n = \\llbracket n \\rrbracket \\sigma$. ✓\n- ($\\Leftarrow$): Assume $\\llbracket n \\rrbracket \\sigma = n$. The rule (B-CONST) gives $\\langle n, \\sigma \\rangle \\Downarrow n$. ✓\n\n**Inductive case:** $e = e_1 + e_2$. Assume the equivalence holds for $e_1$ and $e_2$.\n\n- ($\\Rightarrow$): Assume $\\langle e_1 + e_2, \\sigma \\rangle \\Downarrow v$. The only applicable rule is (B-ADD), so $\\langle e_1, \\sigma \\rangle \\Downarrow v_1$, $\\langle e_2, \\sigma \\rangle \\Downarrow v_2$, and $v = v_1 + v_2$. By the **induction hypothesis**, $v_1 = \\llbracket e_1 \\rrbracket \\sigma$ and $v_2 = \\llbracket e_2 \\rrbracket \\sigma$. Substituting into the denotational equation:\n$$\n\\llbracket e_1 + e_2 \\rrbracket \\sigma = \\llbracket e_1 \\rrbracket \\sigma + \\llbracket e_2 \\rrbracket \\sigma = v_1 + v_2 = v\n$$\n- ($\\Leftarrow$): Assume $\\llbracket e_1 + e_2 \\rrbracket \\sigma = v$. By definition of $\\llbracket \\cdot \\rrbracket$, $v = \\llbracket e_1 \\rrbracket \\sigma + \\llbracket e_2 \\rrbracket \\sigma$. By induction hypothesis, $\\langle e_1, \\sigma \\rangle \\Downarrow \\llbracket e_1 \\rrbracket \\sigma$ and $\\langle e_2, \\sigma \\rangle \\Downarrow \\llbracket e_2 \\rrbracket \\sigma$. Apply (B-ADD) to obtain $\\langle e_1 + e_2, \\sigma \\rangle \\Downarrow v$. ✓\n\nHence, by induction, the two semantics coincide. **QED.**\n\n> [!NOTE]\n> This derivation is a *favourite* of KTU 14-mark questions. Mastery of the inductive structure and rule application is essential.\n\n## 3.2 Derivation: Least Fixpoint of a Recursive Equation\n\nConsider a one-counter program with state $(pc, n)$ where $pc \\in \\{\\text{start}, \\text{inc}, \\text{stop}\\}$ and $n \\in \\mathbb{N}$. We want to compute the set of states reachable from $(\\text{start}, 0)$.\n\n### Step 1 — Define the Transition Function\n\nLet $R$ be the transition relation, and let $F : \\mathcal{P}(S) \\to \\mathcal{P}(S)$ be the *post-image operator*:\n\n$$\nF(X) = X \\cup \\{ s' \\in S \\mid \\exists s \\in X : (s, s') \\in R \\}\n$$\n\n### Step 2 — Compute the Kleene Chain\n\nStart with $X_0 = \\emptyset$ and iterate:\n\n$$\nX_{i+1} = F(X_i) = X_i \\cup \\text{Post}(X_i)\n$$\n\nBy Kleene's Fixed Point Theorem for continuous functions on a complete lattice:\n\n$$\n\\text{lfp}(F) = \\bigcup_{i=0}^{\\infty} X_i\n$$\n\n### Step 3 — Concrete Computation\n\nLet $R$ contain $(\\text{start}, 0) \\to (\\text{inc}, 0)$, $(\\text{inc}, n) \\to (\\text{inc}, n+1)$, $(\\text{inc}, n) \\to (\\text{stop}, n)$ (nondeterministic).\n\n$$\n\\begin{aligned}\nX_0 &= \\emptyset \\\\\nX_1 &= \\{(\\text{start}, 0)\\} \\\\\nX_2 &= X_1 \\cup \\{(\\text{inc}, 0)\\} = \\{(\\text{start}, 0), (\\text{inc}, 0)\\} \\\\\nX_3 &= X_2 \\cup \\{(\\text{inc}, 1), (\\text{stop}, 0)\\} = \\{(\\text{start},0), (\\text{inc},0), (\\text{inc},1), (\\text{stop},0)\\} \\\\\nX_4 &= X_3 \\cup \\{(\\text{inc}, 2), (\\text{stop}, 1)\\} \\\\\n&\\;\\;\\vdots \\\\\nX_\\infty &= \\text{lfp}(F) = \\{(\\text{start},0)\\} \\cup \\{(\\text{inc}, n) \\mid n \\in \\mathbb{N}\\} \\cup \\{(\\text{stop}, n) \\mid n \\in \\mathbb{N}\\}\n\\end{aligned}\n$$\n\nThe **least fixpoint** is the set of all states reachable in any finite number of steps. This is exactly what a model checker computes.\n\n> [!IMPORTANT]\n> For finite-state systems, the chain **stabilizes** at some finite $k$ where $X_{k+1} = X_k$. This is why model checking terminates.\n\n## 3.3 Analytical Matrix: Comparison of Semantic Styles\n\n| Property | Operational | Denotational | Axiomatic |\n| :--- | :--- | :--- | :--- |\n| **Mathematical Object** | Transition relation | Function | Proof rule |\n| **Granularity** | Step-by-step | Whole-program | Per-construct |\n| **Proof Style** | Rule induction | Structural induction | Hoare logic derivation |\n| **Handles Loops?** | Yes (via reflexive closure $\\rightarrow^*$) | Yes (via lfp) | Yes (via invariant + while-rule) |\n| **Tool Realization** | Abstract machines, interpreters | Abstract interpretation | Theorem provers |\n| **Suitable for** | Executable specifications | Compiler correctness | Program verification |\n| **Difficulty to learn** | Low–Medium | Medium–High | Medium |\n| **Compositional?** | Sometimes (need labels) | Yes (by design) | Yes (per-construct) |\n\n## 3.4 Worked Example — Hoare Logic Proof\n\n**Program:**\n$$\nC \\;\\equiv\\; x := x + 1;\\; y := 2 \\times x\n$$\n\n**Claim to prove:** $\\;\\;\\{x = 5\\}\\; C \\;\\{y = 12\\}$\n\n### Step 1 — Compose the Hoare Triples\n- For $x := x+1$: $\\;\\;\\{x = 5\\}\\; x := x+1 \\;\\{x = 6\\}$\n- For $y := 2*x$: $\\;\\;\\{x = 6\\}\\; y := 2*x \\;\\{y = 12\\}$\n\n### Step 2 — Apply the Assignment Axiom\nThe assignment axiom is $\\{P[x \\mapsto E]\\}\\; x := E \\;\\{P\\}$. The weakest pre-condition of $y := 2*x$ with respect to $y = 12$ is obtained by substituting $y$ with $2*x$ in the post-condition:\n$$\n(2 \\times x = 12) \\;\\Longleftrightarrow\\; x = 6\n$$\nSo $\\{x = 6\\}\\; y := 2*x \\;\\{y = 12\\}$.\n\n### Step 3 — Apply the Composition Rule\nWith $P = \\{x=5\\}$, $Q = \\{x=6\\}$, $R = \\{y=12\\}$:\n$$\n\\frac{\\{P\\}\\; S_1 \\;\\{Q\\} \\quad \\{Q\\}\\; S_2 \\;\\{R\\}}{\\{P\\}\\; S_1;\\; S_2 \\;\\{R\\}}\n$$\nSubstituting: $\\{x=5\\}\\; x := x+1 \\;\\{x=6\\}$ and $\\{x=6\\}\\; y := 2*x \\;\\{y=12\\}$, we conclude $\\{x=5\\}\\; C \\;\\{y=12\\}$. **QED.**\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 The Three Pillars of Semantics — Block-Level Architecture\n\nThis diagram maps how the three semantic styles interface with the verification engine and the underlying mathematical model.\n\n```mermaid\nflowchart LR\n    sourceA[\"Program Source Code\"] --> lexer[\"Lexer / Parser\"]\n    lexer --> ast[\"Abstract Syntax Tree\"]\n    ast --> opSem[\"Operational Semantics Engine\"]\n    ast --> denSem[\"Denotational Semantics Engine\"]\n    ast --> axSem[\"Axiomatic Semantics Engine\"]\n\n    opSem --> kripke[\"Kripke Structure M\"]\n    opSem --> ltsA[\"Labeled Transition System\"]\n    denSem --> funcA[\"Function Space Sigma to V\"]\n    denSem --> fixA[\"Lattice of Power Sets P of S\"]\n    axSem --> hoareA[\"Hoare Logic Proof Rules\"]\n    axSem --> wpA[\"Weakest Precondition Calculus\"]\n\n    kripke --> mc[\"Model Checker\"]\n    funcA --> absInt[\"Abstract Interpreter\"]\n    fixA --> mc\n    hoareA --> thmPr[\"Theorem Prover\"]\n    wpA --> thmPr\n\n    mc --> verdictA[\"Verified / Counterexample\"]\n    absInt --> verdictA\n    thmPr --> verdictA\n\n    specA[\"Specification in LTL / CTL / Hoare Logic\"] --> mc\n    specA --> thmPr\n    specA --> absInt\n```\n\n> [!NOTE]\n> This is a **Block-Level Functional Architecture Flow** showing how the syntactic AST is consumed by three different semantic engines, each producing a different mathematical model (Kripke structure, function, proof), which in turn drives a different verification backend (model checker, abstract interpreter, theorem prover).\n\n## 4.2 Small-Step vs Big-Step Semantics — Execution Trace\n\n```mermaid\nflowchart TB\n    s0[\"Config: e1 plus e2, sigma\"]\n    s1[\"Config: e1 prime plus e2, sigma\"]\n    s2[\"Config: v1 plus e2, sigma\"]\n    s3[\"Config: v1 plus e2 prime, sigma\"]\n    s4[\"Config: v1 plus v2, sigma\"]\n    s5[\"Config: v, sigma\"]\n\n    s0 -->|small step 1| s1\n    s1 -->|small step 2| s2\n    s2 -->|small step 3| s3\n    s3 -->|small step 4| s4\n    s4 -->|small step 5| s5\n\n    s0 -.->|big step single jump| s5\n\n    note1[\"Small-step: 5 atomic transitions\"] -.-> s0\n    note2[\"Big-step: 1 logical relation Downarrow v\"] -.-> s0\n```\n\n## 4.3 Kripke Structure — Verification Backbone\n\n```mermaid\nflowchart LR\n    s0A[\"s0: init, x is 0\"]\n    s1A[\"s1: x is 1\"]\n    s2A[\"s2: x is 2, safe\"]\n    s3A[\"s3: x is 3, unsafe\"]\n\n    s0A -->|action a| s1A\n    s1A -->|action b| s2A\n    s2A -->|action c| s3A\n    s3A -->|action d| s3A\n\n    propA[\"AP: x less than 3\"] -.-> s0A\n    propA -.-> s1A\n    propA -.-> s2A\n\n    propB[\"AP: x equals 3\"] -.-> s3A\n```\n\n> [!IMPORTANT]\n> The labelling function $L$ assigns a set of atomic propositions to each state. A state satisfies the formula $\\mathbf{A}\\mathbf{G} \\; (x < 3)$ if **all** paths from it always stay in $L^{-1}(\\{x<3\\})$. The state $s_3$ would be a **counterexample** witness.\n\n## 4.4 Fixpoint Iteration — Reachable State Computation\n\n```mermaid\nflowchart TB\n    x0[\"X0 empty set\"]\n    x1[\"X1 initial states\"]\n    x2[\"X2 plus Post of X1\"]\n    x3[\"X3 plus Post of X2\"]\n    xn[\"Xn = lfp of F fixed point\"]\n    xc[\"Xk plus 1 = Xk saturation\"]\n\n    x0 --> x1 --> x2 --> x3 --> xn --> xc\n\n    out[\"Output: Reachable states of M\"]\n    xc --> out\n```\n\n## 4.5 Hoare Logic Proof Tree — Compositional Verification\n\n```mermaid\nflowchart TB\n    root[\"Prove: P pre S1 S2 R post\"]\n\n    sub1[\"Prove: P pre S1 Q mid\"]\n    sub2[\"Prove: Q mid S2 R post\"]\n\n    asn1[\"Assignment Axiom on S1\"]\n    asn2[\"Assignment Axiom on S2\"]\n\n    root --> sub1\n    root --> sub2\n    sub1 --> asn1\n    sub2 --> asn2\n\n    final[\"Conclusion: program correct\"]\n    asn1 --> final\n    asn2 --> final\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — 3 Mark Questions (Direct Concepts)\n\n> **Q1.** `[KTU University Exam — Dec 2023]`\n> **Differentiate between operational semantics and denotational semantics. Give one example for each.**\n\n**Model Answer:**\n\n| Aspect | Operational Semantics | Denotational Semantics |\n| :--- | :--- | :--- |\n| **Meaning** | Defined via an abstract *execution machine* using a transition relation. | Defined as a *mathematical function* from states to values. |\n| **Representation** | Set of inference rules for $\\rightarrow$ (or $\\Downarrow$). | Compositional function $\\llbracket e \\rrbracket$. |\n| **Example** | $\\langle x+1, \\sigma \\rangle \\rightarrow \\langle 5+1, \\sigma \\rangle$ | $\\llbracket x+1 \\rrbracket \\sigma = \\llbracket x \\rrbracket \\sigma + 1$ |\n| **Verification Use** | Direct translation to a model checker / interpreter. | Used in compiler correctness, abstract interpretation. |\n\n> **Q2.** `[KTU University Exam — May 2024]`\n> **Define a Kripke Structure. Why is it important in automated verification?**\n\n**Model Answer:**\nA **Kripke Structure** is a tuple $M = (S, S_0, R, L)$ where:\n- $S$ is a finite set of *states*,\n- $S_0 \\subseteq S$ is the set of *initial* states,\n- $R \\subseteq S \\times S$ is a *total* transition relation (every state has at least one successor), and\n- $L : S \\to \\mathcal{P}(AP)$ is a *labelling function* that maps each state to the set of atomic propositions true in it.\n\n**Importance:** It is the *semantic model* on which temporal logic formulas (LTL / CTL) are evaluated. A model checker takes (System, Specification) and translates the system into a Kripke structure and the specification into a formula, then decides whether $M \\models \\varphi$. It is the mathematical bridge that makes automated verification *possible*.\n\n---\n\n## 5.2 Part B — 14 Mark Questions (Module Internal Choice)\n\n### Question A (14 Marks) `[KTU University Exam — May 2023]`\n\n**(a)** With suitable inference rules, define the **small-step (structural) operational semantics** for the language:\n$$\ne \\;::=\\; n \\;\\mid\\; e_1 + e_2 \\;\\mid\\; x\n$$\nClearly show the **computation rule** and **congruence rule** for addition. **[7 Marks]**\n\n**(b)** Prove by **structural induction** that for every expression $e$ and store $\\sigma$, the small-step relation $\\rightarrow$ is **deterministic**: if $\\langle e, \\sigma \\rangle \\rightarrow \\langle e_1, \\sigma_1 \\rangle$ and $\\langle e, \\sigma \\rangle \\rightarrow \\langle e_2, \\sigma_2 \\rangle$, then $e_1 = e_2$ and $\\sigma_1 = \\sigma_2$. **[7 Marks]**\n\n#### Model Solution to (a)\n\nThe set of values is $V = \\mathbb{Z}$. The set of stores is $\\Sigma = \\text{Var} \\to \\mathbb{Z}$. Configurations are pairs $\\langle e, \\sigma \\rangle \\in \\text{Exp} \\times \\Sigma$.\n\n**Inference Rules:**\n\n$$\n\\begin{aligned}\n\\text{(CONST)} \\quad & \\frac{}{\\langle n, \\sigma \\rangle \\rightarrow \\langle n, \\sigma \\rangle} \\\\[6pt]\n\\text{(VAR)} \\quad & \\frac{}{\\langle x, \\sigma \\rangle \\rightarrow \\langle \\sigma(x), \\sigma \\rangle} \\\\[6pt]\n\\text{(ADD-1)} \\quad & \\frac{\\langle e_1, \\sigma \\rangle \\rightarrow \\langle e_1', \\sigma \\rangle}{\\langle e_1 + e_2, \\sigma \\rangle \\rightarrow \\langle e_1' + e_2, \\sigma \\rangle} \\quad \\text{(Congruence: left)} \\\\[6pt]\n\\text{(ADD-2)} \\quad & \\frac{\\langle e_2, \\sigma \\rangle \\rightarrow \\langle e_2', \\sigma \\rangle}{\\langle v_1 + e_2, \\sigma \\rangle \\rightarrow \\langle v_1 + e_2', \\sigma \\rangle} \\quad \\text{(Congruence: right)} \\\\[6pt]\n\\text{(ADD-VAL)} \\quad & \\frac{}{\\langle n_1 + n_2, \\sigma \\rangle \\rightarrow \\langle n_1 +_{\\mathbb{Z}} n_2, \\sigma \\rangle} \\quad \\text{(Computation)}\n\\end{aligned}\n$$\n\n**Identification:**\n- **Computation rule:** (ADD-VAL) — performs the actual integer addition in one step.\n- **Congruence rules:** (ADD-1) and (ADD-2) — propagate a single sub-step of evaluation from a sub-expression to the whole expression. (CONST) and (VAR) are the *axioms* (no premises) for atomic expressions.\n\n#### Model Solution to (b)\n\n**Statement:** $\\forall e, \\sigma$. If $\\langle e, \\sigma \\rangle \\rightarrow \\langle e_1, \\sigma_1 \\rangle$ and $\\langle e, \\sigma \\rangle \\rightarrow \\langle e_2, \\sigma_2 \\rangle$, then $e_1 = e_2$ and $\\sigma_1 = \\sigma_2$.\n\n**Proof by Structural Induction on $e$:**\n\n**Case 1:** $e = n$. Only rule (CONST) applies. $\\langle n, \\sigma \\rangle \\rightarrow \\langle n, \\sigma \\rangle$. So $e_1 = e_2 = n$ and $\\sigma_1 = \\sigma_2 = \\sigma$. ✓\n\n**Case 2:** $e = x$. Only rule (VAR) applies. $\\langle x, \\sigma \\rangle \\rightarrow \\langle \\sigma(x), \\sigma \\rangle$. So $e_1 = e_2 = \\sigma(x)$ and $\\sigma_1 = \\sigma_2 = \\sigma$. ✓\n\n**Case 3:** $e = e_L + e_R$. We consider the rule applied to derive $\\langle e, \\sigma \\rangle \\rightarrow \\langle e_1, \\sigma_1 \\rangle$:\n\n- **Sub-case 3.1 — (ADD-1) was used:** Then $\\langle e_L, \\sigma \\rangle \\rightarrow \\langle e_1', \\sigma \\rangle$ and $e_1 = e_1' + e_R$, $\\sigma_1 = \\sigma$. The other derivation must also be by (ADD-1) (since (ADD-2) requires the LHS to be a value, and (ADD-VAL) requires both operands to be values, but $e_L$ is not a value). So $\\langle e_L, \\sigma \\rangle \\rightarrow \\langle e_2', \\sigma \\rangle$ and $e_2 = e_2' + e_R$, $\\sigma_2 = \\sigma$. By **induction hypothesis** on $e_L$: $e_1' = e_2'$ and $\\sigma = \\sigma$. Hence $e_1 = e_2$ and $\\sigma_1 = \\sigma_2$. ✓\n\n- **Sub-case 3.2 — (ADD-2) was used:** Then $e_L$ is a value $v_1$, and $\\langle e_R, \\sigma \\rangle \\rightarrow \\langle e_1'', \\sigma \\rangle$ with $e_1 = v_1 + e_1''$. The other derivation must also start with $e_L$ as a value, so by (ADD-2): $\\langle e_R, \\sigma \\rangle \\rightarrow \\langle e_2'', \\sigma \\rangle$ and $e_2 = v_1 + e_2''$. By induction on $e_R$: $e_1'' = e_2''$. Hence $e_1 = e_2$. ✓\n\n- **Sub-case 3.3 — (ADD-VAL) was used:** Then $e_L = n_1, e_R = n_2$ are both values, and $e_1 = n_1 + n_2 = e_2$ with $\\sigma_1 = \\sigma_2 = \\sigma$. ✓\n\nIn all cases, the conclusion holds. **QED.**\n\n---\n\n### Question B (14 Marks) `[KTU University Exam — Dec 2022]`\n\n**(a)** Define a **Kripke Structure** and the **CTL satisfaction relation** $M, s \\models \\varphi$ for the operators $\\mathbf{A}\\mathbf{X}$, $\\mathbf{A}\\mathbf{G}$, $\\mathbf{E}\\mathbf{U}$. **[7 Marks]**\n\n**(b)** Consider a Kripke structure $M$ with three states $\\{s_0, s_1, s_2\\}$, initial state $s_0$, transitions $s_0 \\to s_1$, $s_1 \\to s_1$, $s_1 \\to s_2$, $s_2 \\to s_0$, and labels $L(s_0) = \\{p\\}$, $L(s_1) = \\{q\\}$, $L(s_2) = \\{\\}$. Determine whether the following hold:\n- (i) $M, s_0 \\models \\mathbf{E}\\mathbf{F}\\, q$\n- (ii) $M, s_0 \\models \\mathbf{A}\\mathbf{G}\\, (p \\lor q)$\n- (iii) $M, s_0 \\models \\mathbf{E}(p \\mathbf{U}\\, q)$\n\nShow all reasoning. **[7 Marks]**\n\n#### Model Solution to (a)\n\n**Kripke Structure Definition:** A tuple $M = (S, S_0, R, L)$ as defined in §2.4. $R$ is *total*: $\\forall s \\in S, \\exists s' \\in S : (s, s') \\in R$.\n\n**CTL Satisfaction Relation $M, s \\models \\varphi$:**\n\nLet $\\pi = s_0 s_1 s_2 \\dots$ be a path starting at $s_0 = s$. We use $\\pi^i$ to denote the suffix starting at index $i$.\n\n$$\n\\begin{aligned}\nM, s \\models p &\\iff p \\in L(s) \\quad (p \\in AP) \\\\\nM, s \\models \\neg \\varphi &\\iff M, s \\not\\models \\varphi \\\\\nM, s \\models \\varphi_1 \\wedge \\varphi_2 &\\iff M, s \\models \\varphi_1 \\text{ and } M, s \\models \\varphi_2 \\\\\nM, s \\models \\mathbf{A}\\mathbf{X}\\varphi &\\iff \\forall s' \\in S, (s, s') \\in R \\Rightarrow M, s' \\models \\varphi \\\\\nM, s \\models \\mathbf{A}\\mathbf{G}\\varphi &\\iff \\forall \\text{ paths } \\pi \\text{ from } s, \\forall i \\geq 0, M, \\pi(i) \\models \\varphi \\\\\nM, s \\models \\mathbf{E}(\\varphi_1 \\mathbf{U}\\varphi_2) &\\iff \\exists \\text{ path } \\pi \\text{ from } s, \\exists j \\geq 0 : M, \\pi(j) \\models \\varphi_2 \\text{ and } \\forall 0 \\leq i < j, M, \\pi(i) \\models \\varphi_1\n\\end{aligned}\n$$\n\n> [!NOTE]\n> The dual operators are derivable: $\\mathbf{E}\\mathbf{X}\\varphi \\equiv \\neg \\mathbf{A}\\mathbf{X}\\neg \\varphi$, and $\\mathbf{A}\\mathbf{F}\\varphi \\equiv \\neg \\mathbf{E}\\mathbf{G}\\neg \\varphi$.\n\n#### Model Solution to (b)\n\nGiven: $S = \\{s_0, s_1, s_2\\}$, $S_0 = \\{s_0\\}$, $R = \\{(s_0,s_1), (s_1,s_1), (s_1,s_2), (s_2,s_0)\\}$, $L(s_0)=\\{p\\}, L(s_1)=\\{q\\}, L(s_2)=\\emptyset$.\n\n**(i) $M, s_0 \\models \\mathbf{E}\\mathbf{F}\\, q$ ?**\n\n$\\mathbf{E}\\mathbf{F}\\, q \\equiv \\mathbf{E}(\\text{true} \\mathbf{U}\\, q)$. We need a path from $s_0$ that eventually visits a state where $q$ holds.\n- Take path $\\pi = s_0, s_1, s_1, s_1, \\dots$. At index 1, $\\pi(1) = s_1$ and $L(s_1) = \\{q\\} \\ni q$. So $\\exists$ such a path. **SATISFIED: TRUE.**\n\n**(ii) $M, s_0 \\models \\mathbf{A}\\mathbf{G}\\, (p \\lor q)$ ?**\n\nWe need: for **every** path from $s_0$, **every** state on it satisfies $p \\lor q$.\n- Consider path $\\pi = s_0, s_1, s_2, s_0, s_1, s_2, \\dots$ (going $s_0 \\to s_1 \\to s_2 \\to s_0$).\n- At index 2, $\\pi(2) = s_2$ and $L(s_2) = \\emptyset$. So $p \\lor q$ is false at $s_2$.\n- Hence this path violates $\\mathbf{G}(p \\lor q)$. Since **all** paths must satisfy it, the formula fails. **SATISFIED: FALSE.**\n\n> A counterexample trace is: $s_0 \\to s_1 \\to s_2 \\to s_0 \\to \\dots$ (the cycle through $s_2$).\n\n**(iii) $M, s_0 \\models \\mathbf{E}(p \\mathbf{U}\\, q)$ ?**\n\nWe need a path $\\pi$ from $s_0$ and an index $j$ such that:\n- $\\pi(j) \\models q$, and\n- $\\pi(i) \\models p$ for all $0 \\leq i < j$.\n\n- Take $\\pi = s_0, s_1, \\dots$ At $i=0$, $\\pi(0) = s_0$, $L(s_0) = \\{p\\}$, so $p$ holds. ✓\n- At $i=j=1$, $\\pi(1) = s_1$, $L(s_1) = \\{q\\}$, so $q$ holds. ✓\n- The prefix (only $s_0$) satisfies $p$. ✓\n- Hence the formula is satisfied. **SATISFIED: TRUE.**\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning\n\n> [!WARNING]\n> **Common Pitfalls Where Students Lose Marks:**\n> 1. **Confusing $\\rightarrow$ (small-step) and $\\Downarrow$ (big-step):** Examiners deduct heavily for mixing them. Always use the correct symbol.\n> 2. **Forgetting totality in Kripke structures:** $R$ must be total (every state has at least one successor). A state with no successor is *not allowed* — students often forget dangling states.\n> 3. **Path quantifier confusion in CTL:** $\\mathbf{A}$ and $\\mathbf{E}$ are *path quantifiers* (quantify over paths); $\\mathbf{X}, \\mathbf{U}, \\mathbf{G}, \\mathbf{F}$ are *state quantifiers on a path*. A common mistake is to swap them.\n> 4. **Hoare triple pre-condition weakening:** The *Consequence Rule* is bidirectional: you can weaken the pre-condition and strengthen the post-condition. Forgetting the implication direction costs 1–2 marks.\n> 5. **Forgetting to apply the assignment axiom correctly:** The substitution $P[x \\mapsto E]$ is the *syntactic* replacement, not an algebraic one. E.g., $\\{x = 5\\}\\; y := x + 1 \\;\\{x = 5\\}$ — the post-condition must be $y = 6$, *not* $x = 5$ (the variable $x$ is unchanged, but $y$ is now $x+1$).\n> 6. **Induction on the wrong index:** When proving properties of small-step semantics, induction must be on the *structure* of $e$, not the *length* of derivation (this is structural, not natural, induction).\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n- **Semantics = formal meaning** of syntactic constructs. Without it, automated verification is impossible.\n- **Three pillars:** Operational (how it runs), Denotational (what it computes), Axiomatic (what must hold).\n- **Small-step SOS** uses $\\rightarrow$ on configurations; **big-step** uses $\\Downarrow$ from expression to value.\n- **Inference rules** have premises (above the line) and a conclusion (below the line). Computation rules and congruence rules must be distinguished.\n- **Kripke Structure** $M = (S, S_0, R, L)$ is the universal model for CTL/LTL model checking. $R$ is total.\n- **LTL** reasons about a *single path*: $\\mathbf{F}, \\mathbf{G}, \\mathbf{U}, \\mathbf{X}$ with no path quantifier.\n- **CTL** reasons about *branching*: every temporal operator must be preceded by $\\mathbf{A}$ or $\\mathbf{E}$ (paired form).\n- **Hoare Triple** $\\{P\\}C\\{Q\\}$: $P$ is pre-condition, $Q$ is post-condition. The **assignment axiom** is $\\{P[x \\mapsto E]\\}\\; x := E \\;\\{P\\}$.\n- **Partial vs Total Correctness:** Partial = if terminates, $Q$ holds. Total = $C$ *must* terminate and $Q$ holds.\n- **Least fixpoint** $\\text{lfp}(F) = \\bigcup_{i \\geq 0} F^i(\\emptyset)$ computes reachable states. The Kleene chain always stabilizes for *finite-state* systems.\n- **Compositionality** is the defining feature of denotational semantics: meaning of a phrase is built from meanings of its parts.\n- **Equivalence proof** between operational and denotational semantics is by **structural induction** on the syntax of $e$ — this is a *guaranteed* 14-mark question pattern.\n- Always check: **Is the system finite-state?** If yes, model checking is decidable and terminates. If no, fixpoints may diverge and you need **abstract interpretation** or **inductive invariants**.\n- Exam mantra: **\"State the semantics formally, prove by structural induction, apply the relevant rule, conclude with a QED-style box.\"**\n\n---\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 3-t3",
          "title": "Equivalence of LTL Formulae",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 3\n> - **Topic:** Equivalence of LTL Formulae\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of LTL Equivalence\n\n**Linear Temporal Logic (LTL)** is a modal temporal logic used to reason about the infinite sequences of states generated by a concurrent system. Two LTL formulae $\\varphi$ and $\\psi$ are said to be **semantically equivalent** (denoted $\\varphi \\equiv \\psi$) if and only if they are satisfied by exactly the same set of infinite $\\omega$-words (paths) over the underlying Kripke structure's propositional alphabet $2^{AP}$.\n\nFormally, given a path $\\pi = s_0, s_1, s_2, \\ldots$ over a Kripke structure $M$:\n\n$$\\varphi \\equiv \\psi \\iff \\forall \\pi \\in \\text{Paths}(M) : \\pi \\models \\varphi \\iff \\pi \\models \\psi$$\n\n> [!IMPORTANT]\n> **KTU 2024 Definition (Boards-Ready):**\n> Two LTL formulae $\\varphi$ and $\\psi$ are **equivalent** if every model that satisfies $\\varphi$ also satisfies $\\psi$, and every model that satisfies $\\psi$ also satisfies $\\varphi$. That is, for all paths $\\pi$:\n> $$\\varphi \\equiv \\psi \\iff (\\pi \\models \\varphi) \\iff (\\pi \\models \\psi)$$\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine you are writing an **employment contract** in two different languages, say English and Malayalam. Both versions of the contract describe the *exact same legal obligations and rights*. Although the words and sentences look different on paper, **their legal meaning is identical**. No matter what scenario (workplace situation) you test them against, both documents will be considered \"true\" or \"false\" together.\n\nLTL equivalence works exactly like this. Two temporal logic formulae may have *completely different syntactic structures* — different temporal operators, different nested sub-formulae — yet they are **logically indistinguishable** on any execution trace. They are the \"same contract in two languages.\"\n\n| English Contract (Formula A) | Malayalam Contract (Formula B) |\n| :--- | :--- |\n| $G(\\text{request} \\rightarrow F(\\text{grant}))$ | $G(\\neg\\text{request}) \\lor G(\\text{request} \\rightarrow F(\\text{grant}))$ |\n| \"Every request is eventually granted\" | \"Either no requests ever occur, or every request is eventually granted\" |\n| ✅ Same meaning | ✅ Same meaning |\n\n## 1.3 Why LTL Equivalence Matters in Automated Verification\n\n> [!NOTE]\n> In **model checking**, an LTL formula $\\varphi$ is converted into a Büchi automaton $\\mathcal{B}_{\\neg\\varphi}$. The size of this automaton (number of states) is **exponential** in the length of $\\varphi$. By applying equivalence-based reductions **before** automaton construction, the state-space explosion can be **drastically mitigated** — often from $2^{n}$ states down to a small polynomial. This makes equivalence a **board-favorite high-yield topic**.\n\nKey Engineering Metrics in the KTU Syllabus:\n\n- **Expressive completeness** of LTL: **$2^{O(|\\varphi|)}$** states in the resulting Büchi automaton.\n- **Canonical form** (Negation Normal Form) ensures uniqueness up to equivalence.\n- **Soundness & Completeness** of the equivalence system must be provable.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Categories of LTL Equivalences\n\nThe set of LTL equivalences can be logically grouped into **eight distinct families**, each capturing a specific structural transformation.\n\n### 2.1.1 Propositional Logic Foundations (Base Layer)\n\nThese hold because LTL extends propositional logic, and the temporal layer does not alter Boolean identities.\n\n| Law | Equivalence |\n| :--- | :--- |\n| Idempotence (∨) | $\\varphi \\lor \\varphi \\equiv \\varphi$ |\n| Idempotence (∧) | $\\varphi \\land \\varphi \\equiv \\varphi$ |\n| Commutativity (∨) | $\\varphi \\lor \\psi \\equiv \\psi \\lor \\varphi$ |\n| Commutativity (∧) | $\\varphi \\land \\psi \\equiv \\psi \\land \\varphi$ |\n| Associativity (∨) | $(\\varphi \\lor \\psi) \\lor \\rho \\equiv \\varphi \\lor (\\psi \\lor \\rho)$ |\n| Associativity (∧) | $(\\varphi \\land \\psi) \\land \\rho \\equiv \\varphi \\land (\\psi \\land \\rho)$ |\n| Distributivity | $\\varphi \\lor (\\psi \\land \\rho) \\equiv (\\varphi \\lor \\psi) \\land (\\varphi \\lor \\rho)$ |\n| De Morgan | $\\neg(\\varphi \\lor \\psi) \\equiv \\neg\\varphi \\land \\neg\\psi$ |\n| De Morgan | $\\neg(\\varphi \\land \\psi) \\equiv \\neg\\varphi \\lor \\neg\\psi$ |\n| Double Negation | $\\neg\\neg\\varphi \\equiv \\varphi$ |\n| Excluded Middle | $\\varphi \\lor \\neg\\varphi \\equiv \\top$ |\n| Contradiction | $\\varphi \\land \\neg\\varphi \\equiv \\bot$ |\n| Implication | $\\varphi \\rightarrow \\psi \\equiv \\neg\\varphi \\lor \\psi$ |\n\n### 2.1.2 Fixed-Point Expansion Laws (The Heart of LTL)\n\nThese are the **most exam-critical** equivalences, derived from the fixed-point semantics of $U$ and $R$.\n\n> [!IMPORTANT]\n> **Until and Release** are the **canonical** LTL operators. Every other temporal operator can be expressed as a derived form of these two. The fixed-point expansions use the **next-time operator $X$** as the recursive descent mechanism.\n\n| Operator | Fixed-Point Expansion |\n| :--- | :--- |\n| Until ($\\varphi U \\psi$) | $\\varphi U \\psi \\equiv \\psi \\lor (\\varphi \\land X(\\varphi U \\psi))$ |\n| Release ($\\varphi R \\psi$) | $\\varphi R \\psi \\equiv \\psi \\land (\\varphi \\lor X(\\varphi R \\psi))$ |\n\n### 2.1.3 Duality Laws (Negation-Operator Pairs)\n\n| Duality | Equivalence |\n| :--- | :--- |\n| Next | $\\neg X \\varphi \\equiv X \\neg \\varphi$ |\n| Until ↔ Release | $\\neg(\\varphi U \\psi) \\equiv (\\neg\\varphi) R (\\neg\\psi)$ |\n| Release ↔ Until | $\\neg(\\varphi R \\psi) \\equiv (\\neg\\varphi) U (\\neg\\psi)$ |\n| Globally ↔ Finally | $\\neg G \\varphi \\equiv F \\neg\\varphi$ |\n| Finally ↔ Globally | $\\neg F \\varphi \\equiv G \\neg\\varphi$ |\n\n> [!NOTE]\n> **KTU Pitfall:** Students often write $\\neg(\\varphi U \\psi) \\equiv (\\neg\\varphi) U (\\neg\\psi)$. This is **FALSE**. The correct dual of $U$ is $R$ (Release), not $U$. This single error costs 2 marks in Part B derivations.\n\n### 2.1.4 Derived Operator Abbreviations\n\n| Derived Form | Definition |\n| :--- | :--- |\n| Finally ($F \\varphi$) | $F\\varphi \\equiv \\top U \\varphi$ |\n| Globally ($G \\varphi$) | $G\\varphi \\equiv \\bot R \\varphi$ |\n| Weak Until ($\\varphi W \\psi$) | $\\varphi W \\psi \\equiv (\\varphi U \\psi) \\lor G\\varphi$ |\n| Strong Release ($\\varphi M \\psi$) | $\\varphi M \\psi \\equiv (\\varphi R \\psi) \\land F\\varphi$ |\n\n### 2.1.5 Idempotent / Absorption Properties of Temporal Operators\n\n| Property | Equivalence |\n| :--- | :--- |\n| $U$ Idempotence | $\\varphi U \\varphi \\equiv \\varphi$ |\n| $R$ Idempotence | $\\varphi R \\varphi \\equiv \\varphi$ |\n| $F$ Idempotence | $F F \\varphi \\equiv F \\varphi$ |\n| $G$ Idempotence | $G G \\varphi \\equiv G \\varphi$ |\n| $X$ Idempotence | $X X \\varphi \\equiv X X \\varphi$ (no simplification) |\n| $U$ Absorption (left) | $(\\varphi U \\psi) U \\psi \\equiv \\varphi U \\psi$ |\n| $U$ Absorption (right) | $\\varphi U (\\varphi U \\psi) \\equiv \\varphi U \\psi$ |\n\n### 2.1.6 Distributive Properties\n\n| Distributive Law | Equivalence |\n| :--- | :--- |\n| $U$ over ∨ | $(\\varphi U \\psi) \\lor (\\varphi U \\rho) \\equiv \\varphi U (\\psi \\lor \\rho)$ |\n| $U$ over ∧ (when $\\psi$ releases) | $(\\varphi U \\psi) \\land (\\varphi U \\rho) \\equiv \\varphi U (\\psi \\land \\rho)$ (if $\\psi \\lor \\rho$ is reachable) |\n| $G$ over ∧ | $G\\varphi \\land G\\psi \\equiv G(\\varphi \\land \\psi)$ |\n| $F$ over ∨ | $F\\varphi \\lor F\\psi \\equiv F(\\varphi \\lor \\psi)$ |\n| $G$ distributes over → | $G(\\varphi \\rightarrow \\psi) \\equiv G(\\neg\\varphi \\lor \\psi)$ |\n\n### 2.1.7 Implication and Release Special Cases\n\n| Property | Equivalence |\n| :--- | :--- |\n| $\\top$ as $U$ identity | $\\top U \\varphi \\equiv F\\varphi$ |\n| $\\bot$ as $R$ identity | $\\bot R \\varphi \\equiv G\\varphi$ |\n| $\\top$ as $W$ identity | $\\varphi W \\top \\equiv G\\varphi$ |\n\n### 2.1.8 Closure Properties\n\nThe set of LTL formulae is **closed under**:\n\n- Boolean connectives: $\\neg, \\land, \\lor$\n- Temporal operators: $X, U, R$ (and their derived $F, G$)\n- Negation Normal Form (NNF) transformation: every LTL formula $\\varphi$ has an equivalent NNF form $\\text{nnf}(\\varphi)$ with negation pushed to atomic propositions only.\n\n## 2.2 KTU High-Yield Formula Sheet\n\n| # | Equivalence Name | Formula | KTU Priority |\n| :--- | :--- | :--- | :--- |\n| 1 | $U$ Expansion | $\\varphi U \\psi \\equiv \\psi \\lor (\\varphi \\land X(\\varphi U \\psi))$ | ⭐⭐⭐ |\n| 2 | $R$ Expansion | $\\varphi R \\psi \\equiv \\psi \\land (\\varphi \\lor X(\\varphi R \\psi))$ | ⭐⭐⭐ |\n| 3 | $F$ Expansion | $F\\varphi \\equiv \\varphi \\lor XF\\varphi$ | ⭐⭐⭐ |\n| 4 | $G$ Expansion | $G\\varphi \\equiv \\varphi \\land XG\\varphi$ | ⭐⭐⭐ |\n| 5 | $U \\leftrightarrow R$ Duality | $\\neg(\\varphi U \\psi) \\equiv \\neg\\varphi R \\neg\\psi$ | ⭐⭐⭐ |\n| 6 | $F \\leftrightarrow G$ Duality | $\\neg F\\varphi \\equiv G\\neg\\varphi$ | ⭐⭐⭐ |\n| 7 | $F$ via $U$ | $F\\varphi \\equiv \\top U \\varphi$ | ⭐⭐ |\n| 8 | $G$ via $R$ | $G\\varphi \\equiv \\bot R \\varphi$ | ⭐⭐ |\n| 9 | $F$ Idempotence | $FF\\varphi \\equiv F\\varphi$ | ⭐⭐ |\n| 10 | $G$ Idempotence | $GG\\varphi \\equiv G\\varphi$ | ⭐⭐ |\n| 11 | $U$ Absorption | $\\varphi U (\\varphi U \\psi) \\equiv \\varphi U \\psi$ | ⭐⭐ |\n| 12 | $W$ to $U/G$ | $\\varphi W \\psi \\equiv (\\varphi U \\psi) \\lor G\\varphi$ | ⭐ |\n| 13 | $X$ over $\\neg$ | $\\neg X\\varphi \\equiv X\\neg\\varphi$ | ⭐ |\n| 14 | $G$ over $\\land$ | $G\\varphi \\land G\\psi \\equiv G(\\varphi \\land \\psi)$ | ⭐ |\n| 15 | $F$ over $\\lor$ | $F\\varphi \\lor F\\psi \\equiv F(\\varphi \\lor \\psi)$ | ⭐ |\n\n## 2.3 Real-World Engineering Utility\n\n| Domain | Application of LTL Equivalence |\n| :--- | :--- |\n| **Hardware Verification (RTL)** | Equivalence checking between specification $\\varphi_1$ and optimized implementation $\\varphi_2$ for sequential circuits. |\n| **Protocol Verification** | Proving $G(\\text{req} \\rightarrow F \\text{ack}) \\equiv G \\text{req} \\rightarrow G F \\text{ack}$ (causal vs. global eventual response). |\n| **Software Model Checking** | LTL equivalence reduction in **SPIN**, **NuSMV**, and **Cadence** model checkers to reduce Büchi automaton size. |\n| **Cyber-Physical Systems** | Equivalence between discrete LTL and hybrid-time logic specifications for control synthesis. |\n| **AI Planning** | Translating LTL goals into equivalent DFA (Deterministic Finite Automaton) representations for plan synthesis. |\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Theorem 1: Fixed-Point Expansion of Until\n\n> **Claim:** $\\varphi U \\psi \\equiv \\psi \\lor (\\varphi \\land X(\\varphi U \\psi))$\n\n### Proof by Semantic Argument (KTU Preferred Style)\n\nLet $\\pi = s_0, s_1, s_2, \\ldots$ be an arbitrary infinite path. We must show:\n\n$$\\pi \\models \\varphi U \\psi \\iff \\pi \\models \\psi \\lor (\\varphi \\land X(\\varphi U \\psi))$$\n\n**($\\Rightarrow$ Direction — Forward Implication)**\n\nAssume $\\pi \\models \\varphi U \\psi$. By the standard LTL semantics, this means $\\exists j \\geq 0$ such that:\n- $\\pi^j \\models \\psi$, and\n- $\\forall k : 0 \\leq k < j \\Rightarrow \\pi^k \\models \\varphi$.\n\nWe now consider **two exhaustive sub-cases** based on the value of $j$.\n\n**Case 1: $j = 0$.**\nThis means $\\pi^0 \\models \\psi$ (i.e., $s_0 \\models \\psi$). Therefore, the disjunct $\\psi$ is true at the first state, and:\n\n$$\\pi \\models \\psi \\lor (\\varphi \\land X(\\varphi U \\psi))$$\n\n**Case 2: $j \\geq 1$.**\nThen $s_0 \\models \\varphi$ (since $0 < j$). Define the suffix path $\\pi' = s_1, s_2, s_3, \\ldots$. By definition of $U$ on the suffix $\\pi'$ starting from index $j-1$:\n\n$$\\pi' \\models \\varphi U \\psi$$\n\nNow, applying the LTL semantics of the **Next** operator: $\\pi \\models X(\\varphi U \\psi)$ iff the suffix of $\\pi$ from $s_1$ onward satisfies $\\varphi U \\psi$. This is precisely $\\pi' \\models \\varphi U \\psi$, which holds by the case assumption. Combined with $s_0 \\models \\varphi$:\n\n$$\\pi \\models \\varphi \\land X(\\varphi U \\psi) \\implies \\pi \\models \\psi \\lor (\\varphi \\land X(\\varphi U \\psi))$$\n\n**($\\Leftarrow$ Direction — Reverse Implication)**\n\nAssume $\\pi \\models \\psi \\lor (\\varphi \\land X(\\varphi U \\psi))$. Either:\n\n- **Sub-case (a):** $\\pi \\models \\psi$. Then taking $j = 0$, the witness is immediate, so $\\pi \\models \\varphi U \\psi$. ∎\n- **Sub-case (b):** $\\pi \\models \\varphi \\land X(\\varphi U \\psi)$. Then $s_0 \\models \\varphi$, and the suffix $\\pi' = s_1, s_2, \\ldots$ satisfies $\\pi' \\models \\varphi U \\psi$. This means $\\exists j' \\geq 0$ with $\\pi'^{j'} \\models \\psi$ and the prefix conditions on $\\pi'$. Translating back, $\\exists j = j' + 1 \\geq 1$ with $\\pi^j \\models \\psi$ and $\\pi^k \\models \\varphi$ for $0 \\leq k < j$. Thus $\\pi \\models \\varphi U \\psi$. ∎\n\n$$\\boxed{\\therefore \\varphi U \\psi \\equiv \\psi \\lor (\\varphi \\land X(\\varphi U \\psi))}$$\n\n## 3.2 Theorem 2: Duality of $U$ and $R$\n\n> **Claim:** $\\neg(\\varphi U \\psi) \\equiv \\neg\\varphi R \\neg\\psi$\n\n### Proof by Duality Chain\n\n$$\\begin{aligned}\n\\neg(\\varphi U \\psi) &\\equiv \\neg[\\psi \\lor (\\varphi \\land X(\\varphi U \\psi))] \\quad &\\text{(by $U$ expansion, Theorem 1)}\\\\\n&\\equiv \\neg\\psi \\land \\neg(\\varphi \\land X(\\varphi U \\psi)) \\quad &\\text{(by De Morgan's Law)}\\\\\n&\\equiv \\neg\\psi \\land (\\neg\\varphi \\lor \\neg X(\\varphi U \\psi)) \\quad &\\text{(by De Morgan's Law)}\\\\\n&\\equiv \\neg\\psi \\land (\\neg\\varphi \\lor X \\neg(\\varphi U \\psi)) \\quad &\\text{(by $X$ over $\\neg$ equivalence)}\\\\\n&\\equiv \\neg\\psi \\land (\\neg\\varphi \\lor X(\\neg\\varphi R \\neg\\psi)) \\quad &\\text{(by induction on formula structure)}\n\\end{aligned}$$\n\nNow we observe that the right-hand side is precisely the **Release expansion** applied to $\\neg\\varphi R \\neg\\psi$. Recall:\n\n$$\\varphi R \\psi \\equiv \\psi \\land (\\varphi \\lor X(\\varphi R \\psi))$$\n\nSubstituting $\\varphi \\mapsto \\neg\\varphi$ and $\\psi \\mapsto \\neg\\psi$:\n\n$$\\neg\\varphi R \\neg\\psi \\equiv \\neg\\psi \\land (\\neg\\varphi \\lor X(\\neg\\varphi R \\neg\\psi))$$\n\nThis matches our derived expression exactly. Therefore:\n\n$$\\boxed{\\neg(\\varphi U \\psi) \\equiv \\neg\\varphi R \\neg\\psi}$$\n\n## 3.3 Theorem 3: $F$ and $G$ Mutual Duality\n\n> **Claim:** $\\neg F \\varphi \\equiv G \\neg\\varphi$\n\n### Proof\n\n$$\\begin{aligned}\n\\neg F \\varphi &\\equiv \\neg(\\top U \\varphi) \\quad &\\text{(by definition } F\\varphi \\equiv \\top U \\varphi\\text{)}\\\\\n&\\equiv \\neg\\top R \\neg\\varphi \\quad &\\text{(by Theorem 2: $U \\leftrightarrow R$ duality)}\\\\\n&\\equiv \\bot R \\neg\\varphi \\quad &\\text{(since } \\neg\\top \\equiv \\bot\\text{)}\\\\\n&\\equiv G \\neg\\varphi \\quad &\\text{(by definition } G\\varphi \\equiv \\bot R \\varphi\\text{)}\n\\end{aligned}$$\n\n$$\\boxed{\\therefore \\neg F \\varphi \\equiv G \\neg\\varphi}$$\n\n## 3.4 Theorem 4: $F$ Idempotence ($FF\\varphi \\equiv F\\varphi$)\n\n### Proof by Semantic Argument\n\nWe prove $\\pi \\models FF\\varphi \\iff \\pi \\models F\\varphi$.\n\n**($\\Rightarrow$):** $\\pi \\models FF\\varphi$ means $\\exists j \\geq 0$ such that $\\pi^j \\models F\\varphi$. The latter means $\\exists k \\geq 0$ such that $\\pi^{j+k} \\models \\varphi$. Letting $i = j + k \\geq 0$, we have $\\pi^i \\models \\varphi$, so $\\pi \\models F\\varphi$. ∎\n\n**($\\Leftarrow$):** $\\pi \\models F\\varphi$ means $\\exists i \\geq 0$ with $\\pi^i \\models \\varphi$. Let $j = 0$. Then $\\pi^0 \\models F\\varphi$ (since the path eventually reaches $\\varphi$ at index $i$). Therefore $\\pi \\models FF\\varphi$. ∎\n\n$$\\boxed{\\therefore FF\\varphi \\equiv F\\varphi}$$\n\n## 3.5 Theorem 5: $G$ Distributes over $\\land$\n\n> **Claim:** $G\\varphi \\land G\\psi \\equiv G(\\varphi \\land \\psi)$\n\n### Proof\n\n$$\\begin{aligned}\nG\\varphi \\land G\\psi &\\equiv (\\bot R \\varphi) \\land (\\bot R \\psi) \\quad &\\text{(by } G \\text{ definition)}\\\\\n&\\equiv \\bot R (\\varphi \\land \\psi) \\quad &\\text{(by $R$ distributive law over } \\land\\text{)}\\\\\n&\\equiv G(\\varphi \\land \\psi) \\quad &\\text{(by } G \\text{ definition)}\n\\end{aligned}$$\n\n## 3.6 Worked Example: Reducing a Complex LTL Formula\n\n> **Problem:** Reduce $\\neg F(\\varphi \\rightarrow \\psi)$ to a simpler equivalent form using NNF and equivalences.\n\n**Step 1:** Push the outermost negation using duality.\n\n$$\\neg F(\\varphi \\rightarrow \\psi) \\equiv G \\neg(\\varphi \\rightarrow \\psi)$$\n\n**Step 2:** Eliminate the implication inside the $G$.\n\n$$G \\neg(\\varphi \\rightarrow \\psi) \\equiv G \\neg(\\neg\\varphi \\lor \\psi) \\equiv G(\\varphi \\land \\neg\\psi)$$\n\n**Step 3:** Apply $G$ distribution is not possible here since the form is already a $G$ over a conjunction — this is the **canonical reduced form**.\n\n$$\\boxed{\\neg F(\\varphi \\rightarrow \\psi) \\equiv G(\\varphi \\land \\neg\\psi)}$$\n\n> [!NOTE]\n> **Verification of Reduction Correctness:**\n> The original formula says \"it is **not** the case that eventually $\\varphi$ implies $\\psi$.\" The reduction $G(\\varphi \\land \\neg\\psi)$ says \"globally, $\\varphi$ is true and $\\psi$ is false.\" These are logically the same — a permanent violation of the implication.\n\n## 3.7 Analytical Matrix: Operator Equivalence Map\n\nThe following matrix summarizes the **mutual duality and reduction** of every LTL operator. Each cell shows the equivalent dual form.\n\n| Operator | $\\neg$ Applied | Dual Operator |\n| :--- | :--- | :--- |\n| $X \\varphi$ | $\\neg X \\varphi$ | $X \\neg\\varphi$ |\n| $\\varphi U \\psi$ | $\\neg(\\varphi U \\psi)$ | $\\neg\\varphi R \\neg\\psi$ |\n| $\\varphi R \\psi$ | $\\neg(\\varphi R \\psi)$ | $\\neg\\varphi U \\neg\\psi$ |\n| $F \\varphi$ | $\\neg F \\varphi$ | $G \\neg\\varphi$ |\n| $G \\varphi$ | $\\neg G \\varphi$ | $F \\neg\\varphi$ |\n| $\\varphi W \\psi$ | $\\neg(\\varphi W \\psi)$ | $\\neg\\varphi M \\neg\\psi$ |\n| $\\varphi M \\psi$ | $\\neg(\\varphi M \\psi)$ | $\\neg\\varphi W \\neg\\psi$ |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Mermaid Diagram: LTL Operator Duality Network\n\nThe following diagram visualizes the **complete duality relationship** between all canonical LTL operators. Each arrow represents an equivalence transformation, and operator boxes are color-coded by category.\n\n```mermaid\ngraph TD\n    A[\"Boolean Core: phi, psi\"] --> B[\"X phi<br/>Next\"]\n    A --> C[\"phi U psi<br/>Until\"]\n    A --> D[\"phi R psi<br/>Release\"]\n\n    C -->|equiv F phi| E[\"F phi = top U phi<br/>Finally\"]\n    D -->|equiv G phi| F[\"G phi = bot R phi<br/>Globally\"]\n\n    B -->|neg equiv| G[\"X neg phi\"]\n    C -->|neg equiv| H[\"neg phi R neg psi\"]\n    D -->|neg equiv| I[\"neg phi U neg psi\"]\n    E -->|neg equiv| J[\"G neg phi\"]\n    F -->|neg equiv| K[\"F neg phi\"]\n\n    H -.dismantles.-> D\n    I -.dismantles.-> C\n    J -.dismantles.-> F\n    K -.dismantles.-> E\n\n    E -->|idempotent| L[\"FF phi equiv F phi\"]\n    F -->|idempotent| M[\"GG phi equiv G phi\"]\n    C -->|absorbs| N[\"phi U phi U psi equiv phi U psi\"]\n    D -->|absorbs| O[\"phi R phi R psi equiv phi R psi\"]\n\n    classDef canonical fill:#1f77b4,stroke:#0d3d6e,color:#ffffff,stroke-width:2px\n    classDef derived fill:#ff7f0e,stroke:#a04a00,color:#ffffff,stroke-width:2px\n    classDef negated fill:#2ca02c,stroke:#155115,color:#ffffff,stroke-width:2px\n    classDef reduced fill:#d62728,stroke:#7a1313,color:#ffffff,stroke-width:2px\n\n    class B,C,D canonical\n    class E,F derived\n    class G,H,I,J,K negated\n    class L,M,N,O reduced\n```\n\n## 4.2 Mermaid Diagram: Equivalence Reduction Pipeline\n\nThis block-level flow chart depicts the **standard KTU-style reduction algorithm** applied to an arbitrary LTL formula before Büchi automaton construction.\n\n```mermaid\nflowchart TD\n    P0[\"Step 0: Input<br/>Arbitrary LTL Formula phi\"]\n    P1[\"Step 1: Eliminate Implications<br/>phi to neg phi OR psi\"]\n    P2[\"Step 2: Push Negations Inward<br/>NNF Transformation\"]\n    P3[\"Step 3: Eliminate Derived Operators<br/>F to top U, G to bot R\"]\n    P4[\"Step 4: Apply Idempotence<br/>FF phi to F phi, GG phi to G phi\"]\n    P5[\"Step 5: Apply Absorption<br/>phi U phi U psi to phi U psi\"]\n    P6[\"Step 6: Distributive Reduction<br/>G phi AND G psi to G phi AND psi\"]\n    P7[\"Step 7: Canonical NNF Formula<br/>nnf phi\"]\n\n    P0 --> P1\n    P1 --> P2\n    P2 --> P3\n    P3 --> P4\n    P4 --> P5\n    P5 --> P6\n    P6 --> P7\n\n    P7 --> Q0[\"Output: Reduced Formula<br/>Size drops from 2 to the power n to O of n\"]\n    P7 --> Q1[\"Output: Convert to GNBA<br/>Generalized NBW\"]\n\n    classDef process fill:#9467bd,stroke:#3a1f5c,color:#ffffff,stroke-width:2px\n    classDef output fill:#8c564b,stroke:#3a1d17,color:#ffffff,stroke-width:2px\n\n    class P0,P1,P2,P3,P4,P5,P6,P7 process\n    class Q0,Q1 output\n```\n\n## 4.3 Mermaid Diagram: Decision Procedure for LTL Equivalence\n\nThis schematic illustrates how two LTL formulae are tested for semantic equivalence using the **automata-theoretic approach** — a board-favorite sub-topic in KTU Module 3.\n\n```mermaid\ngraph LR\n    IN1[\"LTL Formula phi\"] --> N1[\"Convert to NBA<br/>B sub neg phi\"]\n    IN2[\"LTL Formula psi\"] --> N2[\"Convert to NBA<br/>B sub neg psi\"]\n    N1 --> R1[\"Reverse B sub neg phi<br/>to get B sub phi\"]\n    N2 --> C1[\"Construct B sub psi\"]\n\n    R1 --> P[\"Product Construction<br/>B sub phi intersect B sub psi\"]\n    C1 --> P\n\n    P --> CHK[\"Emptiness Check<br/>Language of Product empty?\"]\n\n    CHK -->|YES| OUT1[\"Equivalence Holds<br/>phi equiv psi\"]\n    CHK -->|NO| OUT2[\"Counterexample Path<br/>phi not equiv psi\"]\n\n    classDef input fill:#17becf,stroke:#0a5e6e,color:#000000,stroke-width:2px\n    classDef process fill:#bcbd22,stroke:#5e5e0a,color:#000000,stroke-width:2px\n    classDef decision fill:#e377c2,stroke:#7a3a6e,color:#ffffff,stroke-width:2px\n    classDef output fill:#7f7f7f,stroke:#333333,color:#ffffff,stroke-width:2px\n\n    class IN1,IN2 input\n    class N1,N2,R1,C1,P process\n    class CHK decision\n    class OUT1,OUT2 output\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Question A1 [KTU University Exam — June 2023]\n\n> **Q: Define semantic equivalence of two LTL formulae. State any four standard LTL equivalences involving the $U$ (Until) operator.**\n\n**Model Answer:**\n\nTwo LTL formulae $\\varphi$ and $\\psi$ are **semantically equivalent** (written $\\varphi \\equiv \\psi$) if and only if for every infinite path $\\pi$ over the Kripke structure:\n\n$$\\pi \\models \\varphi \\iff \\pi \\models \\psi$$\n\nThis means both formulae are satisfied by **exactly the same set of paths**, even if their syntactic structure differs.\n\n**Four standard LTL equivalences involving Until ($U$):**\n\n1. **Fixed-point expansion:** $\\varphi U \\psi \\equiv \\psi \\lor (\\varphi \\land X(\\varphi U \\psi))$\n2. **Idempotence:** $\\varphi U \\varphi \\equiv \\varphi$\n3. **Absorption (right):** $\\varphi U (\\varphi U \\psi) \\equiv \\varphi U \\psi$\n4. **Duality with Release:** $\\neg(\\varphi U \\psi) \\equiv \\neg\\varphi R \\neg\\psi$\n\n**Marking Distribution:** Definition (1.5) + Equivalences (1.5) = **3 marks**.\n\n---\n\n### Question A2 [KTU University Exam — Dec 2022]\n\n> **Q: State and prove the LTL equivalence $F\\varphi \\equiv \\varphi \\lor XF\\varphi$.**\n\n**Model Answer:**\n\n**Statement:** For any LTL formula $\\varphi$, the \"Finally\" operator satisfies:\n\n$$F\\varphi \\equiv \\varphi \\lor XF\\varphi$$\n\n**Proof:**\n\nUsing the definition $F\\varphi \\equiv \\top U \\varphi$ and the fixed-point expansion of Until ($\\varphi U \\psi \\equiv \\psi \\lor (\\varphi \\land X(\\varphi U \\psi))$):\n\n$$\\begin{aligned}\nF\\varphi &\\equiv \\top U \\varphi \\\\\n&\\equiv \\varphi \\lor (\\top \\land X(\\top U \\varphi)) \\\\\n&\\equiv \\varphi \\lor X(\\top U \\varphi) \\\\\n&\\equiv \\varphi \\lor XF\\varphi\n\\end{aligned}$$\n\nThis equivalence captures the **least fixed-point** semantics of $F$: \"$\\varphi$ holds eventually\" if and only if \"$\\varphi$ holds now, or it holds eventually from the next state onward.\"\n\n**Marking Distribution:** Statement (1) + Proof (2) = **3 marks**.\n\n---\n\n## 5.2 Part B Questions (14 Marks Each)\n\n### Question A (14 Marks) [KTU University Exam — June 2024 Model]\n\n> **Q (a)** [7 Marks] **Prove the LTL equivalence: $G\\varphi \\equiv \\varphi \\land XG\\varphi$. State the corresponding expansion for $\\varphi R \\psi$ and explain the connection.**\n\n> **Q (b)** [7 Marks] **Using LTL equivalences, reduce the formula $\\neg F(p \\rightarrow q)$ to Negation Normal Form (NNF) and hence to an equivalent form using only $U, R,$ and $X$ operators.**\n\n---\n\n#### Model Solution for Q (a)\n\n**Claim:** $G\\varphi \\equiv \\varphi \\land XG\\varphi$\n\n**Proof by semantic analysis:**\n\nWe prove both directions.\n\n**($\\Rightarrow$) Assume $\\pi \\models G\\varphi$.** By LTL semantics, this means $\\forall j \\geq 0 : \\pi^j \\models \\varphi$.\n\n- In particular, taking $j = 0$: $\\pi^0 \\models \\varphi$, i.e., the first state satisfies $\\varphi$.\n- Considering the suffix path $\\pi' = s_1, s_2, \\ldots$ and the original $G\\varphi$ condition for all $j \\geq 1$, we have $\\forall j \\geq 1 : \\pi^j \\models \\varphi$, which is exactly $\\pi' \\models G\\varphi$. Therefore $\\pi \\models XG\\varphi$.\n\nCombining: $\\pi \\models \\varphi \\land XG\\varphi$. ∎\n\n**($\\Leftarrow$) Assume $\\pi \\models \\varphi \\land XG\\varphi$.** This means $s_0 \\models \\varphi$ and the suffix $\\pi' \\models G\\varphi$, i.e., $\\forall j \\geq 1 : \\pi^j \\models \\varphi$. Together with $s_0 \\models \\varphi$, we get $\\forall j \\geq 0 : \\pi^j \\models \\varphi$, which is precisely $\\pi \\models G\\varphi$. ∎\n\n$$\\boxed{\\therefore G\\varphi \\equiv \\varphi \\land XG\\varphi}$$\n\n**Connection to Release ($\\varphi R \\psi$):**\n\nSince $G\\varphi$ is defined as $\\bot R \\varphi$ (Release with $\\bot$ as the first operand), the $G$ expansion mirrors the $R$ fixed-point expansion:\n\n$$\\varphi R \\psi \\equiv \\psi \\land (\\varphi \\lor X(\\varphi R \\psi))$$\n\nSubstituting $\\varphi \\mapsto \\bot$ and $\\psi \\mapsto \\varphi$:\n\n$$\\bot R \\varphi \\equiv \\varphi \\land (\\bot \\lor X(\\bot R \\varphi)) \\equiv \\varphi \\land X(\\bot R \\varphi) \\equiv \\varphi \\land XG\\varphi$$\n\nThis confirms $G$ is a **special case of Release**.\n\n**Marking Distribution:** Forward proof (2.5) + Reverse proof (2.5) + Connection (2) = **7 marks**.\n\n---\n\n#### Model Solution for Q (b)\n\n**Goal:** Reduce $\\neg F(p \\rightarrow q)$ using LTL equivalences, ending with only $U, R, X$.\n\n**Step 1:** Apply duality $\\neg F \\equiv G \\neg$:\n\n$$\\neg F(p \\rightarrow q) \\equiv G \\neg(p \\rightarrow q)$$\n\n**Step 2:** Eliminate implication ($p \\rightarrow q \\equiv \\neg p \\lor q$):\n\n$$G \\neg(p \\rightarrow q) \\equiv G \\neg(\\neg p \\lor q) \\equiv G(p \\land \\neg q)$$\n\n**Step 3:** Substitute $G \\equiv \\bot R$:\n\n$$G(p \\land \\neg q) \\equiv \\bot R (p \\land \\neg q)$$\n\n**Final Reduced Form:**\n\n$$\\boxed{\\neg F(p \\rightarrow q) \\equiv G(p \\land \\neg q) \\equiv \\bot R (p \\land \\neg q)}$$\n\n**Intuition:** \"It is not the case that $p$ implies $q$ eventually\" is the same as \"globally, we have $p$ true and $q$ false.\"\n\n**Marking Distribution:** Duality (2) + Implication (2) + NNF (1.5) + Final form with only $U/R/X$ (1.5) = **7 marks**.\n\n---\n\n### Question B (14 Marks) [KTU University Exam — Dec 2023 Model]\n\n> **Q (a)** [7 Marks] **State and prove the LTL duality: $\\neg(\\varphi U \\psi) \\equiv \\neg\\varphi R \\neg\\psi$. Use the fixed-point expansion of Until as the starting point.**\n\n> **Q (b)** [7 Marks] **Reduce the LTL formula $G(p \\rightarrow Fq)$ to an equivalent form that uses no $G$ or $F$ operators. Show every step with the equivalence applied.**\n\n---\n\n#### Model Solution for Q (a)\n\n**Claim:** $\\neg(\\varphi U \\psi) \\equiv \\neg\\varphi R \\neg\\psi$\n\n**Proof:**\n\nStarting from the fixed-point expansion of Until:\n\n$$\\varphi U \\psi \\equiv \\psi \\lor (\\varphi \\land X(\\varphi U \\psi))$$\n\nApply negation to both sides:\n\n$$\\neg(\\varphi U \\psi) \\equiv \\neg[\\psi \\lor (\\varphi \\land X(\\varphi U \\psi))]$$\n\nApply De Morgan's law:\n\n$$\\neg(\\varphi U \\psi) \\equiv \\neg\\psi \\land \\neg(\\varphi \\land X(\\varphi U \\psi))$$\n\nApply De Morgan's law to the inner conjunction:\n\n$$\\neg(\\varphi U \\psi) \\equiv \\neg\\psi \\land (\\neg\\varphi \\lor \\neg X(\\varphi U \\psi))$$\n\nApply the $X$ over $\\neg$ equivalence ($\\neg X \\alpha \\equiv X \\neg \\alpha$):\n\n$$\\neg(\\varphi U \\psi) \\equiv \\neg\\psi \\land (\\neg\\varphi \\lor X \\neg(\\varphi U \\psi))$$\n\nBy **structural induction** (assuming the equivalence holds for $\\varphi$ and $\\psi$ in the recursive position of $X$):\n\n$$\\neg(\\varphi U \\psi) \\equiv \\neg\\psi \\land (\\neg\\varphi \\lor X(\\neg\\varphi R \\neg\\psi))$$\n\nThe right-hand side is precisely the **fixed-point expansion of $\\neg\\varphi R \\neg\\psi$**, which by definition of Release is:\n\n$$\\neg\\varphi R \\neg\\psi \\equiv \\neg\\psi \\land (\\neg\\varphi \\lor X(\\neg\\varphi R \\neg\\psi))$$\n\n$$\\boxed{\\therefore \\neg(\\varphi U \\psi) \\equiv \\neg\\varphi R \\neg\\psi} \\quad \\blacksquare$$\n\n**Marking Distribution:** Starting equation (1) + De Morgan (2) + $X$ duality (1.5) + Induction closure (1.5) + Final conclusion (1) = **7 marks**.\n\n---\n\n#### Model Solution for Q (b)\n\n**Goal:** Reduce $G(p \\rightarrow Fq)$ to a form using only $U, R, X, \\neg, \\land, \\lor$.\n\n**Step 1:** Substitute $G \\equiv \\bot R$:\n\n$$G(p \\rightarrow Fq) \\equiv \\bot R (p \\rightarrow Fq)$$\n\n**Step 2:** Substitute $F \\equiv \\top U$ inside the implication:\n\n$$\\bot R (p \\rightarrow (\\top U q))$$\n\n**Step 3:** Eliminate the implication $p \\rightarrow (\\top U q) \\equiv \\neg p \\lor (\\top U q)$:\n\n$$\\bot R (\\neg p \\lor (\\top U q))$$\n\n**Step 4:** Apply De Morgan-like distributive form for $R$ over $\\lor$ (using the dual distributive law):\n\n$$\\bot R (\\neg p \\lor (\\top U q)) \\equiv (\\bot R \\neg p) \\lor (\\bot R (\\top U q))$$\n\n**Step 5:** Substitute back $G$ and $G(\\top U q) \\equiv G F q$:\n\n$$\\equiv G(\\neg p) \\lor G(\\top U q) \\equiv G\\neg p \\lor GFq$$\n\n**Step 6:** Convert back to $R$ form:\n\n$$G\\neg p \\lor GFq \\equiv \\bot R \\neg p \\lor \\bot R (\\top U q)$$\n\n**Final Reduced Form (using only $R, U, X, \\neg, \\lor$):**\n\n$$\\boxed{G(p \\rightarrow Fq) \\equiv \\bot R \\neg p \\lor \\bot R (\\top U q)}$$\n\n**Alternative cleaner form using $U$ and $R$:**\n\n$$G(p \\rightarrow Fq) \\equiv (\\bot R \\neg p) \\lor G F q$$\n\n**Marking Distribution:** $G$ substitution (1.5) + $F$ substitution (1.5) + Implication elimination (1.5) + Distributive step (1.5) + Final form (1) = **7 marks**.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Common Pitfalls**\n>\n> 1. **Negation-Duality Confusion (Most Common Error — 2 marks lost):** Students frequently write $\\neg(\\varphi U \\psi) \\equiv \\neg\\varphi U \\neg\\psi$. This is **FALSE**. The correct dual of $U$ is $R$, not $U$. Always remember: $U$ and $R$ are **negation duals**, just like $F$ and $G$.\n>\n> 2. **Fixed-Point Expansion Direction (1 mark lost):** The correct expansion is $\\varphi U \\psi \\equiv \\psi \\lor (\\varphi \\land X(\\varphi U \\psi))$. Students sometimes write it as $\\psi \\land (\\varphi \\lor X(\\varphi U \\psi))$, which is the **Release** expansion, not Until.\n>\n> 3. **Forgetting the $X$ Operator (1 mark lost):** When applying fixed-point expansions, the recursion is mediated by $X$. Omitting $X$ produces a non-equivalent formula that fails the second state onwards.\n>\n> 4. **Confusing $W$ and $U$:** Weak Until $W$ allows $\\varphi$ to hold globally without ever reaching $\\psi$, while strong Until $U$ requires $\\psi$ to eventually hold. The equivalence $\\varphi W \\psi \\equiv (\\varphi U \\psi) \\lor G\\varphi$ is **frequently miswritten** as $\\varphi W \\psi \\equiv \\varphi U \\psi$ — a 1.5 mark deduction in Part B.\n>\n> 5. **Skipping Steps in Proofs (Up to 2 marks lost):** KTU examiners expect a **chain of equivalences with the law cited at each step**. Writing \"after applying De Morgan's law we get...\" without showing the intermediate expression loses credit. **Always write the formula after each transformation**.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Rapid Revision Checklist**\n\n- ✅ **Semantic equivalence** of $\\varphi$ and $\\psi$ requires that for **every** path $\\pi$: $\\pi \\models \\varphi \\iff \\pi \\models \\psi$. This is a **stronger** condition than mutual implication.\n- ✅ The **canonical LTL operators** are $U$ (Until) and $R$ (Release). All others ($X, F, G, W, M$) are **derived operators**.\n- ✅ **Fixed-point expansion** of $U$: $\\varphi U \\psi \\equiv \\psi \\lor (\\varphi \\land X(\\varphi U \\psi))$ — uses **least fixed-point** semantics.\n- ✅ **Fixed-point expansion** of $R$: $\\varphi R \\psi \\equiv \\psi \\land (\\varphi \\lor X(\\varphi R \\psi))$ — uses **greatest fixed-point** semantics.\n- ✅ **Duality map:** $U \\leftrightarrow R$, $F \\leftrightarrow G$, $W \\leftrightarrow M$ under negation.\n- ✅ **Idempotence table:** $FF\\varphi \\equiv F\\varphi$, $GG\\varphi \\equiv G\\varphi$, $\\varphi U \\varphi \\equiv \\varphi$, $\\varphi R \\varphi \\equiv \\varphi$.\n- ✅ **Absorption laws:** $\\varphi U (\\varphi U \\psi) \\equiv \\varphi U \\psi$, $(\\varphi U \\psi) U \\psi \\equiv \\varphi U \\psi$.\n- ✅ **Distributive laws:** $F(\\varphi \\lor \\psi) \\equiv F\\varphi \\lor F\\psi$, $G(\\varphi \\land \\psi) \\equiv G\\varphi \\land G\\psi$.\n- ✅ **$X$ over $\\neg$:** $\\neg X\\varphi \\equiv X\\neg\\varphi$ — the Next operator **commutes** with negation.\n- ✅ **Derived operator definitions:** $F\\varphi \\equiv \\top U \\varphi$ and $G\\varphi \\equiv \\bot R \\varphi$.\n- ✅ **Negation Normal Form (NNF):** Every LTL formula can be transformed into an equivalent form where negation appears **only on atomic propositions**. This is essential for Büchi automaton construction.\n- ✅ **Equivalence checking complexity:** Deciding LTL equivalence is **PSPACE-complete** (a board-favorite fact).\n- ✅ **Practical use:** Equivalence reduction shrinks the Büchi automaton from $2^{O(n)}$ states to a much smaller manageable size, **directly addressing the state-space explosion** in model checking.\n- ✅ **Equivalence vs. Satisfiability:** Equivalence is **reducible to satisfiability** via the identity $\\varphi \\equiv \\psi \\iff (\\varphi \\land \\neg\\psi) \\lor (\\psi \\land \\neg\\varphi) \\text{ is unsatisfiable}$.\n- ✅ **Counterexample method:** If $\\varphi \\not\\equiv \\psi$, the language $\\mathcal{L}(\\varphi) \\oplus \\mathcal{L}(\\psi)$ (symmetric difference) is non-empty, and an **accepting path** in the product NBA serves as a **witness** distinguishing the two formulae.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 3-t4",
          "title": "Weak Until",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 3\n> - **Topic:** Weak Until\n\n<!-- SECTION_1_START -->\n# Module 3 — Weak Until: Core Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\nIn the temporal logics **CTL (Computation Tree Logic)** and **LTL (Linear Temporal Logic)**, used for hardware/software verification, **Weak Until** is a binary temporal connective denoted by the operator **W**.\n\n> [!IMPORTANT]  \n> **Weak Until (W) — Formal Syntax & Semantics**  \n> Let $f$ and $g$ be CTL/LTL state formulas. The **Weak Until** formula is written as:  \n> $$\\phi \\; ::= \\; f \\;\\mathbf{W}\\; g$$  \n> **Semantics (on a Kripke structure $M = (S, S_0, R, L)$):**  \n> A state $s \\in S$ satisfies $f \\,\\mathbf{W}\\, g$ (denoted $s \\models f \\,\\mathbf{W}\\, g$) **if and only if** there exists a path $\\pi = s_0, s_1, s_2, \\ldots$ starting at $s$ such that either:  \n> 1. $g$ holds at some position $k \\geq 0$ on the path, and $f$ holds at all positions $j < k$ on the path, **OR**  \n> 2. $f$ holds at **every** position on the path (i.e., $g$ never holds — $f$ persists forever).\n\nMathematically, this is expressed as the disjunction:\n$$f \\,\\mathbf{W}\\, g \\;\\equiv\\; (f \\,\\mathbf{U}\\, g) \\;\\lor\\; \\mathbf{G}\\,f$$\n\nwhere **U** is the *Strong Until* operator and **G** is the *Globally* (always) operator.\n\n### 1.1.1 CTL Path-Quantified Variants\n\nIn CTL, Weak Until is prefixed with the path quantifiers **A** (for all paths) or **E** (there exists a path):\n\n| Operator | Meaning | Interpretation |\n|----------|---------|----------------|\n| $\\mathbf{E}[f \\,\\mathbf{W}\\, g]$ | There Exists a path where $f \\,\\mathbf{W}\\, g$ holds | At least one execution trace satisfies the weak-until condition |\n| $\\mathbf{A}[f \\,\\mathbf{W}\\, g]$ | For All paths, $f \\,\\mathbf{W}\\, g$ holds | Every possible execution trace satisfies the weak-until condition |\n\n> [!NOTE]  \n> **Why the name \"Weak\"?**  \n> The operator is termed *weak* because the **right-hand side formula $g$ is NOT obliged to ever become true**. The obligation on $f$ to \"wait\" is relaxed — $f$ is allowed to hold *indefinitely* without $g$ ever appearing. In contrast, **Strong Until** ($\\mathbf{U}$) *demands* that $g$ eventually holds.\n\n---\n\n## 1.2 Intuitive Real-World Analogy\n\nImagine you are **waiting at a bus stop** for your friend to arrive, but you have an **unbreakable appointment** at 6 PM.\n\n- **Strong Until ($f \\,\\mathbf{U}\\, g$)** is like saying: *\"I will keep waiting **until** my friend shows up, AND my friend **must** show up.\"* If the friend never arrives, the promise is *broken*.\n\n- **Weak Until ($f \\,\\mathbf{W}\\, g$)** is like saying: *\"I will keep waiting **until** my friend shows up. **OR**, if my friend never arrives, I will just keep waiting at the bus stop forever — that's also acceptable.\"* The promise is *not* broken if $g$ never happens.\n\n**Another analogy — Traffic Light:**\nLet $f$ = \"light is red\" and $g$ = \"light turns green\".  \n- $f \\,\\mathbf{U}\\, g$ says: \"The light is red *until* it turns green, and it **must** turn green eventually.\"  \n- $f \\,\\mathbf{W}\\, g$ says: \"The light is red *until* it turns green, *or* it just stays red forever (a broken light!), which is also fine.\"\n\nIn **automated verification** (e.g., verifying a CPU cache coherence protocol), this matters because some liveness properties (something *must* eventually happen) are too strict — you often only need **safety** guarantees (something *bad* never happens), which Weak Until expresses elegantly.\n\n> [!IMPORTANT]  \n> **CTL Hierarchy Placement:** Weak Until is a **monopole-free, future-only temporal operator** residing in the class of *invariant-type* connectives. It is part of the standard minimal temporal basis $\\{ \\mathbf{X},\\,\\mathbf{U},\\,\\mathbf{W}\\}$ used in industrial model checkers like **NuSMV**, **SPIN**, and **UPPAAL**.\n\n---\n\n## 1.3 Boundary Cases & Special Behaviors\n\n| Condition | Behavior of $f \\,\\mathbf{W}\\, g$ | Behavior of $f \\,\\mathbf{U}\\, g$ |\n|-----------|----------------------------------|----------------------------------|\n| $g$ holds at position $0$ | Satisfied immediately | Satisfied immediately |\n| $f$ holds at all positions (no $g$ ever) | **Satisfied** (weak acceptance) | **Violated** (no $g$ ever appears) |\n| $f$ becomes false before $g$ appears | Violated | Violated |\n| Both $f$ and $g$ hold at position $0$ | Satisfied (vacuously, $k=0$) | Satisfied (vacuously, $k=0$) |\n\nThe **Equivalence with Strong Until + Globally**:\n$$f \\,\\mathbf{W}\\, g \\;\\equiv\\; (f \\,\\mathbf{U}\\, g) \\,\\lor\\, \\mathbf{G}\\,f$$\n\nThis is the canonical reduction that *all* major model-checking algorithms exploit internally to eliminate the W operator and reuse existing U-handling code.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# Module 3 — Weak Until: Deep Theoretical Analysis & Formula Sheet\n\n## 2.1 Operational Semantics on Kripke Structures\n\nA **Kripke Structure** is the canonical model of a reactive system used in CTL/LTL model checking. Formally:\n$$M \\;=\\; (S,\\; S_0,\\; R,\\; L)$$\nwhere:\n- $S$ is a finite set of **states**\n- $S_0 \\subseteq S$ is the set of **initial states**\n- $R \\subseteq S \\times S$ is a **total transition relation** (every state has at least one successor — critical for fixpoint computations)\n- $L : S \\rightarrow 2^{AP}$ is a **labeling function** mapping each state to the set of atomic propositions true in it\n\n### 2.1.1 Step-by-Step Logical Decomposition\n\nThe satisfaction of $s \\models f \\,\\mathbf{W}\\, g$ unfolds as follows:\n\n1. **Check the immediate state $s$:**  \n   - If $L(s) \\models g$, then $s \\models f \\,\\mathbf{W}\\, g$ **trivially** (case $k=0$).\n   - If $L(s) \\not\\models g$ AND $L(s) \\models f$, proceed to step 2.\n   - If $L(s) \\not\\models g$ AND $L(s) \\not\\models f$, then $s \\not\\models f \\,\\mathbf{W}\\, g$ (violation).\n\n2. **Recursive descent on successors:**  \n   - Let $R(s) = \\{s' \\mid (s, s') \\in R\\}$ be the set of immediate successors of $s$.\n   - For **LTL** (single infinite path), there is exactly one successor; for **CTL**, all successors $s' \\in R(s)$ must satisfy $s' \\models f \\,\\mathbf{W}\\, g$ when prefixed with $\\mathbf{A}$, or at least one must when prefixed with $\\mathbf{E}$.\n\n3. **Termination by fixpoint:**  \n   The set $\\{s \\in S \\mid s \\models f \\,\\mathbf{W}\\, g\\}$ is computed as the **greatest fixpoint** of a monotone operator on the powerset lattice $2^S$ — this is the formal foundation of the model-checking algorithm.\n\n---\n\n## 2.2 Fixpoint Characterization (Core of Model Checking)\n\nThe set of states satisfying $f \\,\\mathbf{W}\\, g$ is computed using the **Greatest Fixpoint (GFP)** operator $\\mathbf{gfp}$, applied to the following predicate transformer $\\tau : 2^S \\rightarrow 2^S$:\n\n$$\\tau(X) \\;=\\; \\llbracket g \\rrbracket \\,\\cup\\, \\left( \\llbracket f \\rrbracket \\,\\cap\\, \\mathbf{Pre}_{\\forall}(X) \\right)$$\n\nwhere:\n- $\\llbracket f \\rrbracket$ = set of states where $f$ is true\n- $\\llbracket g \\rrbracket$ = set of states where $g$ is true\n- $\\mathbf{Pre}_{\\forall}(X) = \\{ s \\in S \\mid R(s) \\subseteq X \\}$ = states all of whose successors are in $X$ (for $\\mathbf{A}\\,\\mathbf{W}$)\n\nThe result is:\n$$\\llbracket f \\,\\mathbf{W}\\, g \\rrbracket_{\\mathbf{A}} \\;=\\; \\mathbf{gfp}\\!\\left(\\,X \\;\\mapsto\\; \\llbracket g \\rrbracket \\,\\cup\\, \\bigl(\\llbracket f \\rrbracket \\cap \\mathbf{Pre}_{\\forall}(X)\\bigr)\\right)$$\n\n**Why greatest fixpoint?** Because Weak Until admits infinite $f$-chains (where $g$ never holds) — the *largest* set closed under the operator is what we want. Knaster–Tarski's theorem guarantees this GFP exists and equals $\\bigcap_{i=0}^{\\infty} X_i$ where $X_0 = S$ and $X_{i+1} = \\tau(X_i)$.\n\nFor the existential variant:\n$$\\llbracket f \\,\\mathbf{W}\\, g \\rrbracket_{\\mathbf{E}} \\;=\\; \\mathbf{gfp}\\!\\left(\\,X \\;\\mapsto\\; \\llbracket g \\rrbracket \\,\\cup\\, \\bigl(\\llbracket f \\rrbracket \\cap \\mathbf{Pre}_{\\exists}(X)\\bigr)\\right)$$\n\nwhere $\\mathbf{Pre}_{\\exists}(X) = \\{ s \\in S \\mid R(s) \\cap X \\neq \\emptyset \\}$.\n\n---\n\n## 2.3 The Equivalence Hierarchy\n\n| # | Equivalence Identity | Why It Holds |\n|---|----------------------|--------------|\n| 1 | $f \\,\\mathbf{W}\\, g \\;\\equiv\\; (f \\,\\mathbf{U}\\, g) \\,\\lor\\, \\mathbf{G}\\,f$ | Definitional: weak = strong-or-always |\n| 2 | $\\mathbf{G}\\,f \\;\\equiv\\; f \\,\\mathbf{W}\\, \\mathbf{false}$ | Setting $g = \\mathbf{false}$ — $g$ never holds, so $f$ must hold forever |\n| 3 | $\\mathbf{F}\\,g \\;\\equiv\\; \\mathbf{true} \\,\\mathbf{U}\\, g$ | Strong until where $f = \\mathbf{true}$ (trivially satisfied) |\n| 4 | $f \\,\\mathbf{W}\\, g \\;\\equiv\\; \\mathbf{G}\\,f \\;\\lor\\; f \\,\\mathbf{U}\\, g$ | Rearrangement of (1) |\n| 5 | $f \\,\\mathbf{R}\\, g \\;\\equiv\\; g \\,\\mathbf{W}\\, (f \\land g)$ | Release = dual of Until expressed via W |\n\n> [!NOTE]  \n> **Release Operator (R):** The \"Release\" operator is the De Morgan dual of Until. The identity $f \\,\\mathbf{R}\\, g \\;\\equiv\\; g \\,\\mathbf{W}\\, (f \\land g)$ shows that **W** is a *primitive* from which **R** can be derived — this is why some CTL textbooks treat $\\{X, W\\}$ as the minimal basis.\n\n---\n\n## 2.4 KTU High-Yield Formula Sheet\n\n| Symbol / Notation | Meaning | KTU-Exam Note |\n|-------------------|---------|----------------|\n| $f \\,\\mathbf{W}\\, g$ | Weak Until: $f$ holds until $g$ holds, **or** $f$ holds forever | Often tested as \"what does W do that U does not?\" |\n| $f \\,\\mathbf{U}\\, g$ | Strong Until: $f$ holds until $g$ holds, **and $g$ must** | Distinguish W vs U in exam answers |\n| $f \\,\\mathbf{R}\\, g$ | Release: $g$ holds until $f$ releases it, or $g$ holds forever | Dual of U |\n| $\\mathbf{G}\\,f$ | Globally: $f$ holds at all future states | $= f \\,\\mathbf{W}\\, \\mathbf{false}$ |\n| $\\mathbf{F}\\,f$ | Finally (Eventually): $f$ holds at some future state | $= \\mathbf{true} \\,\\mathbf{U}\\, f$ |\n| $\\mathbf{X}\\,f$ | Next: $f$ holds at the next state | Unaffected by W vs U distinction |\n| $\\mathbf{A}[f \\,\\mathbf{W}\\, g]$ | For all paths, weak-until holds | Often used for **safety** properties |\n| $\\mathbf{E}[f \\,\\mathbf{W}\\, g]$ | There exists a path with weak-until | Often used in **counterexample** search |\n| $s \\models f \\,\\mathbf{W}\\, g$ | State $s$ satisfies the formula | Write truth table on sample traces |\n| $\\mathbf{gfp}(\\tau)$ | Greatest fixpoint of $\\tau$ | Compute iteratively: $X_0 = S$, $X_{i+1} = \\tau(X_i)$ |\n\n### Real-World Engineering Utility\n\nWeak Until is extensively used in:\n- **Hardware Verification:** Specifying that a request signal $\\mathit{req}$ stays asserted until an acknowledgement $\\mathit{ack}$ arrives, **with the system also being correct if the request stays asserted forever** (an idle system).\n- **Protocol Verification (e.g., PCI-e, AXI bus):** Encoding liveness-without-strictness: a master may keep requesting indefinitely if the slave never responds — a permitted behavior in some fault-tolerance modes.\n- **Software Model Checking (SLAM, BLAST):** Encoding reachability of error states that need *not* be reached for correctness.\n- **Cyber-Physical Systems (UPPAAL):** Specifying timing constraints where deadlines are \"soft\" rather than \"hard\".\n\n---\n\n## 2.5 Comparison: W vs U vs R (Conceptual Triangle)\n\n| Property | $\\mathbf{U}$ (Until) | $\\mathbf{W}$ (Weak Until) | $\\mathbf{R}$ (Release) |\n|----------|----------------------|----------------------------|------------------------|\n| Liveness obligation on 2nd arg? | **Yes** (must occur) | No (may or may not occur) | No (may or may not occur) |\n| Persistence of 1st arg? | Until 2nd arg occurs | Until 2nd arg or forever | Until 1st arg occurs or forever |\n| Fixpoint type used | Least Fixpoint ($\\mathbf{lfp}$) | Greatest Fixpoint ($\\mathbf{gfp}$) | Greatest Fixpoint ($\\mathbf{gfp}$) |\n| Common use | Progress / Liveness | Safety + bounded liveness | Co-safety / interruptibility |\n| Boolean algebra | $\\neg(f\\,\\mathbf{U}\\,g) \\equiv \\neg f \\,\\mathbf{R}\\, \\neg g$ | $\\mathbf{W}$ is reducible to $\\mathbf{U}+\\mathbf{G}$ | Duality with U |\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# Module 3 — Weak Until: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation 1: Weak Until as Disjunction of Strong Until and Globally\n\n**Goal:** Show that $f \\,\\mathbf{W}\\, g \\equiv (f \\,\\mathbf{U}\\, g) \\lor \\mathbf{G}\\,f$.\n\n### Step 1 — Restate the meaning of each operator\n\nBy the standard CTL/LTL semantics:\n- $s \\models f \\,\\mathbf{U}\\, g$ iff $\\exists k \\geq 0$ such that $\\pi[k] \\models g$ and $\\forall j < k : \\pi[j] \\models f$.\n- $s \\models \\mathbf{G}\\,f$ iff $\\forall k \\geq 0 : \\pi[k] \\models f$.\n\n### Step 2 — Forward direction ($\\Rightarrow$)\n\nAssume $s \\models f \\,\\mathbf{W}\\, g$. Then by the **Weak Until semantics**, *either*:\n- **Case A:** $g$ holds at some position $k \\geq 0$ and $f$ holds at all $j < k$.  \n  $\\Rightarrow s \\models f \\,\\mathbf{U}\\, g$ directly.\n- **Case B:** $f$ holds at every position on the path (and $g$ never holds).  \n  $\\Rightarrow s \\models \\mathbf{G}\\,f$ directly.\n\nThus in both cases, $s \\models (f \\,\\mathbf{U}\\, g) \\lor \\mathbf{G}\\,f$. $\\blacksquare$\n\n### Step 3 — Backward direction ($\\Leftarrow$)\n\nAssume $s \\models (f \\,\\mathbf{U}\\, g) \\lor \\mathbf{G}\\,f$.\n- **Case A:** $s \\models f \\,\\mathbf{U}\\, g$. Then $\\exists k$ with $\\pi[k] \\models g$ and $\\forall j<k: \\pi[j] \\models f$. This trivially satisfies the first disjunct of the **W** semantics, so $s \\models f \\,\\mathbf{W}\\, g$.\n- **Case B:** $s \\models \\mathbf{G}\\,f$. Then $\\forall k : \\pi[k] \\models f$. This matches the second disjunct of the **W** semantics (f forever, g never), so $s \\models f \\,\\mathbf{W}\\, g$.\n\n$\\blacksquare$ — Equivalence established.\n\n---\n\n## 3.2 Derivation 2: $\\mathbf{G}\\,f \\equiv f \\,\\mathbf{W}\\, \\mathbf{false}$\n\n**Goal:** Prove Globally is a special case of Weak Until.\n\n### Step 1 — Substitute $g := \\mathbf{false}$ into the disjunction\n\nFrom the identity in §3.1:\n$$f \\,\\mathbf{W}\\, \\mathbf{false} \\;\\equiv\\; (f \\,\\mathbf{U}\\, \\mathbf{false}) \\;\\lor\\; \\mathbf{G}\\,f$$\n\n### Step 2 — Evaluate $f \\,\\mathbf{U}\\, \\mathbf{false}$\n\nBy definition of Strong Until: $\\exists k \\geq 0$ such that $\\pi[k] \\models \\mathbf{false}$ and $\\forall j<k : \\pi[j] \\models f$.\n\nBut $\\pi[k] \\models \\mathbf{false}$ is *impossible* for any $k$ in classical logic (a formula cannot be both true and false in a state under consistent labeling). Hence:\n$$f \\,\\mathbf{U}\\, \\mathbf{false} \\;\\equiv\\; \\mathbf{false}$$\n\n### Step 3 — Conclude\n\n$$f \\,\\mathbf{W}\\, \\mathbf{false} \\;\\equiv\\; \\mathbf{false} \\;\\lor\\; \\mathbf{G}\\,f \\;\\equiv\\; \\mathbf{G}\\,f \\qquad \\blacksquare$$\n\n> [!NOTE]  \n> This identity is **heavily used in exam answers** — when asked \"express $Gf$ using W\", the correct one-line answer is $f \\,\\mathbf{W}\\, \\mathbf{false}$.\n\n---\n\n## 3.3 Derivation 3: Fixpoint Algorithm for $\\llbracket f \\,\\mathbf{W}\\, g \\rrbracket$\n\n**Goal:** Compute the set $W = \\{s \\in S \\mid s \\models f \\,\\mathbf{W}\\, g\\}$ iteratively.\n\n### Step 1 — Define the operator\n\n$$\\tau(X) \\;=\\; \\llbracket g \\rrbracket \\,\\cup\\, \\bigl(\\llbracket f \\rrbracket \\,\\cap\\, \\mathbf{Pre}_{\\forall}(X)\\bigr)$$\n\n### Step 2 — Initialize\n\n$$X_0 \\;=\\; S \\quad \\text{(start with the universal set — greatest fixpoint convention)}$$\n\n### Step 3 — Iterate\n\n$$\n\\begin{aligned}\nX_1 &= \\tau(X_0) = \\llbracket g \\rrbracket \\,\\cup\\, \\bigl(\\llbracket f \\rrbracket \\,\\cap\\, \\mathbf{Pre}_{\\forall}(S)\\bigr) \\\\\n    &= \\llbracket g \\rrbracket \\,\\cup\\, \\llbracket f \\rrbracket \\quad \\text{(since } R \\text{ is total, } \\mathbf{Pre}_{\\forall}(S) = S \\text{)} \\\\\nX_2 &= \\tau(X_1) \\\\\n    &= \\llbracket g \\rrbracket \\,\\cup\\, \\bigl(\\llbracket f \\rrbracket \\,\\cap\\, \\mathbf{Pre}_{\\forall}(X_1)\\bigr) \\\\\n&\\;\\;\\vdots \\\\\nX_{i+1} &= \\llbracket g \\rrbracket \\,\\cup\\, \\bigl(\\llbracket f \\rrbracket \\,\\cap\\, \\mathbf{Pre}_{\\forall}(X_i)\\bigr)\n\\end{aligned}\n$$\n\n### Step 4 — Termination\n\nThe sequence $X_0 \\supseteq X_1 \\supseteq X_2 \\supseteq \\cdots$ is **monotonically decreasing** and bounded below by $\\emptyset$. Since $S$ is finite, the sequence stabilizes at some $X_n = X_{n+1}$, and this is the **greatest fixpoint**:\n$$W \\;=\\; X_n \\;=\\; \\mathbf{gfp}(\\tau) \\;=\\; \\bigcap_{i=0}^{\\infty} X_i$$\n\n### Step 5 — Verification by example\n\nConsider a 3-state path $s_0 \\to s_1 \\to s_2 \\to \\cdots$ with $f$ true at $s_0, s_1$ and false at $s_2$, and $g$ true at $s_2$.\n- $s_2 \\models g \\Rightarrow s_2 \\models f \\,\\mathbf{W}\\, g$.\n- $s_1 \\models f$ and successor $s_2 \\in W \\Rightarrow s_1 \\in W$ (via $X_1$ computation).\n- $s_0 \\models f$ and successor $s_1 \\in W \\Rightarrow s_0 \\in W$.\n\nResult: $W = \\{s_0, s_1, s_2\\}$. ✓ Consistent with intuition.\n\n---\n\n## 3.4 Analytical Matrix: W vs U vs R — Side-by-Side Truth Evaluation\n\nConsider a finite path $\\pi = (s_0, s_1, s_2, s_3)$ of length 4, with truth values of $f$ and $g$:\n\n| Path position | $f$ | $g$ |\n|---------------|-----|-----|\n| $s_0$ | T | F |\n| $s_1$ | T | F |\n| $s_2$ | T | T |\n| $s_3$ | F | F |\n\nNow evaluate from $s_0$:\n\n| Formula | Evaluation | Result |\n|---------|------------|--------|\n| $f \\,\\mathbf{U}\\, g$ | $g$ becomes true at $s_2$, $f$ holds at $s_0, s_1$ | **TRUE** |\n| $f \\,\\mathbf{W}\\, g$ | Same as above (Case A) | **TRUE** |\n| $f \\,\\mathbf{W}\\, \\neg g$ | $f$ holds at $s_0, s_1$; at $s_2$, $f \\land \\neg g$ is F; violation | **FALSE** |\n| $g \\,\\mathbf{R}\\, f$ | $f$ releases $g$ at $s_0, s_1$; $g$ becomes true at $s_2$ | **TRUE** |\n| $\\mathbf{G}\\,f$ | $f$ false at $s_3$ | **FALSE** |\n| $f \\,\\mathbf{W}\\, \\mathbf{false}$ | $g = $ false never holds, $f$ false at $s_3$ | **FALSE** |\n| $\\neg g \\,\\mathbf{W}\\, f$ | $f$ true at $s_2$ releases the wait | **TRUE** |\n\nThis matrix is the **core analytical tool** for KTU problems on this topic.\n\n---\n\n## 3.5 Worked Numerical Example (Kripke Structure Model Check)\n\n**Problem:** Consider the Kripke structure with states $\\{s_0, s_1, s_2, s_3\\}$, initial state $s_0$, and transitions $s_0 \\to s_1$, $s_0 \\to s_2$, $s_1 \\to s_1$ (self-loop), $s_2 \\to s_3$, $s_3 \\to s_3$ (self-loop). Labeling: $L(s_0)=\\{f\\}$, $L(s_1)=\\{f\\}$, $L(s_2)=\\{f,g\\}$, $L(s_3)=\\{g\\}$.\n\n**Check whether $s_0 \\models \\mathbf{A}[f \\,\\mathbf{W}\\, g]$.**\n\n### Step 1 — Identify all paths from $s_0$\n- $\\pi_1 = s_0, s_1, s_1, s_1, \\ldots$ (loop in $s_1$)\n- $\\pi_2 = s_0, s_2, s_3, s_3, s_3, \\ldots$ (loop in $s_3$)\n\n### Step 2 — Evaluate $f \\,\\mathbf{W}\\, g$ on $\\pi_1$\nAt $s_0$: $f$ true, $g$ false. Move to $s_1$.  \nAt $s_1$: $f$ true, $g$ false. Self-loop. Forever.  \nHence on $\\pi_1$: $f$ holds everywhere, $g$ never. **Weak Until is satisfied** (Case B of W). ✓\n\n### Step 3 — Evaluate $f \\,\\mathbf{W}\\, g$ on $\\pi_2$\nAt $s_0$: $f$ true, $g$ false. Move to $s_2$.  \nAt $s_2$: $f$ true, $g$ true. **W satisfied immediately** at $k=1$. ✓\n\n### Step 4 — Conclusion\nBoth paths satisfy W. Therefore $s_0 \\models \\mathbf{A}[f \\,\\mathbf{W}\\, g]$. **TRUE**.\n\n> [!IMPORTANT]  \n> **Examination Tip:** The **$\\mathbf{A}[f \\,\\mathbf{W}\\, g]$** formula is *true* on $s_0$ because **all** paths satisfy it. If the question had asked $\\mathbf{A}[f \\,\\mathbf{U}\\, g]$, the answer would *still* be TRUE here — but on a different Kripke structure where one path loops forever with $f$ true and $g$ never appearing, the $\\mathbf{A}\\mathbf{U}$ would be FALSE while the $\\mathbf{A}\\mathbf{W}$ would be TRUE. **This is the prototypical exam discriminator.**\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# Module 3 — Weak Until: Structural Diagrams & Schematics\n\n## 4.1 Mermaid Block: Operator Decision Flow\n\n```mermaid\nflowchart TD\n    A[State s reached] --> B{Check immediate state s}\n    B -- s models g --> C[s models f W g - TRIVIALLY TRUE at k=0]\n    B -- s does NOT model g --> D{Check if s models f}\n    D -- s does NOT model f --> E[VIOLATION - s does NOT satisfy f W g]\n    D -- s models f --> F[Examine successor states s prime]\n    F --> G{Path quantifier prefix}\n    G -- A all paths --> H[For ALL successors: s prime must satisfy f W g]\n    G -- E exists path --> I[There EXISTS a successor: s prime satisfies f W g]\n    H -- All recursive checks TRUE --> J[s satisfies A f W g]\n    H -- Some recursive check FAILS --> K[s does NOT satisfy A f W g]\n    I -- At least one path TRUE --> L[s satisfies E f W g]\n    I -- All paths FAIL --> M[s does NOT satisfy E f W g]\n    C --> N[Exit: f W g TRUE]\n    E --> O[Exit: f W g FALSE]\n    J --> N\n    K --> O\n    L --> N\n    M --> O\n```\n\n## 4.2 Mermaid Block: W vs U Operator Behavior on Infinite Traces\n\n```mermaid\nflowchart LR\n    Start[Trace pi starts] --> Branch{Choose operator}\n    Branch -- Strong Until U --> S1[Seek first occurrence of g]\n    S1 --> S2{g found at position k?}\n    S2 -- Yes AND f holds at all j less than k --> S3[U satisfied]\n    S2 -- No g ever found --> S4[U VIOLATED - g must appear]\n    Branch -- Weak Until W --> W1[Seek first occurrence of g OR check if f holds forever]\n    W1 --> W2{g found at position k?}\n    W2 -- Yes AND f holds at all j less than k --> W3[W satisfied - Case A]\n    W2 -- No g ever found --> W4{f holds at every position?}\n    W4 -- Yes --> W5[W satisfied - Case B - f forever]\n    W4 -- No --> W6[W VIOLATED - f broke before g appeared]\n```\n\n## 4.3 Mermaid Block: Fixpoint Iteration Schematic\n\n```mermaid\nflowchart TD\n    P0[Iteration 0: X0 equals S - the universe] --> P1[Iteration 1: X1 equals g-set UNION f-set minus Pre-Forall X0]\n    P1 --> P2[Iteration 2: X2 equals g-set UNION f-set minus Pre-Forall X1]\n    P2 --> P3[Iteration i: Xi equals g-set UNION f-set minus Pre-Forall X i minus 1]\n    P3 --> P4{Xi equals X i minus 1?}\n    P4 -- No --> P2\n    P4 -- Yes --> P5[Convergence reached - Xi is gfp tau equals W - the set of states satisfying f W g]\n```\n\n## 4.4 Mermaid Block: CTL Hierarchy Showing W's Position\n\n```mermaid\nflowchart TD\n    Top[CTL Formula Class] --> L1[State Formulas - evaluated on states]\n    Top --> L2[Path Formulas - evaluated on paths]\n    L1 --> SF1[Atomic Propositions p in AP]\n    L1 --> SF2[Boolean connectives NOT, AND, OR, IMPLIES, IFF]\n    L1 --> SF3[Path quantifier prefix: A or E]\n    SF3 --> SF4[Temporal operators: X, U, W, F, G, R]\n    L2 --> TF1[Next: X phi - phi at next state]\n    L2 --> TF2[Strong Until: f U g - f until g must occur]\n    L2 --> TF3[Weak Until: f W g - f until g or f forever]\n    L2 --> TF4[Finally: F phi - phi eventually]\n    L2 --> TF5[Globally: G phi - phi always - equals f W false]\n    L2 --> TF6[Release: f R g - dual of Until]\n    TF3 -.Definitional Reduction.-> TF2\n    TF3 -.Alternative Form.-> TF5\n```\n\n## 4.5 Block-Level Architecture: Model Checker Internal Pipeline for W\n\n```mermaid\nflowchart LR\n    Input[Input: CTL formula containing W] --> Parser[Formula Parser - tokenize]\n    Parser --> DetectW{Operator W detected?}\n    DetectW -- Yes --> Normalize[Apply normalization: f W g becomes f U g OR G f]\n    DetectW -- No --> Pass[Pass through unchanged]\n    Normalize --> ReduceU[Sub-formula f U g sent to Until-handler]\n    Normalize --> ReduceG[Sub-formula G f sent to Globally-handler]\n    ReduceU --> UntilEngine[Strong Until fixpoint - lfp - linear time]\n    ReduceG --> GlobalEngine[Globally fixpoint - gfp - infinite descent]\n    UntilEngine --> Combine[Union of result sets]\n    GlobalEngine --> Combine\n    Combine --> Output[Output: set of satisfying states - W computed]\n    Pass --> Output\n```\n\n> [!IMPORTANT]  \n> **Industrial Insight:** Industrial model checkers like **NuSMV** and **Cadence SMV** perform exactly the normalization in the figure above: every W-operator is rewritten into the disjunction $(U \\lor G)$ during the *formula preprocessing* phase, allowing a single unified fixpoint engine to handle the entire formula. This is why a KTU exam may ask: *\"Why can the W operator be eliminated in CTL model checking?\"* — Answer: because of the equivalence $f\\,\\mathbf{W}\\,g \\equiv (f\\,\\mathbf{U}\\,g) \\lor \\mathbf{G}\\,f$, allowing reduction to the canonical U+G basis.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# Module 3 — Weak Until: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short-Answer Questions (3 Marks Each)\n\n### Question 1\n**[KTU University Exam — Model Question, May 2024 Style]**  \n**Q: Define the Weak Until (W) operator in CTL/LTL. State how it differs from the Strong Until (U) operator with a suitable example.**\n\n**Model Answer (3 Marks):**\n\n**Definition (1.5 Marks):**  \nWeak Until is a binary temporal connective denoted $\\mathbf{W}$. For state formulas $f$ and $g$, the formula $f \\,\\mathbf{W}\\, g$ holds at a state $s$ if, along a path from $s$, $f$ holds continuously *until* $g$ holds, **OR** $f$ holds at *every* future state (i.e., $g$ need not ever hold). Formally:\n$$s \\models f \\,\\mathbf{W}\\, g \\iff \\exists \\text{ path } \\pi \\text{ from } s \\text{ s.t. } (\\exists k \\geq 0: \\pi[k] \\models g \\land \\forall j<k: \\pi[j] \\models f) \\;\\lor\\; (\\forall k \\geq 0: \\pi[k] \\models f)$$\n\n**Difference with U + Example (1.5 Marks):**  \n- **Strong Until** ($f \\,\\mathbf{U}\\, g$) *requires* $g$ to eventually become true on the path.  \n- **Weak Until** ($f \\,\\mathbf{W}\\, g$) does *not* require $g$ to ever hold — it permits $f$ to hold forever.  \n\n**Example:** Let $f = \\text{``request''}$ and $g = \\text{``acknowledge''}$. The property $\\mathbf{A}[f \\,\\mathbf{W}\\, g]$ states: \"Every path keeps the request asserted until an ack is received, **or** the request may remain asserted forever.\" In contrast, $\\mathbf{A}[f \\,\\mathbf{U}\\, g]$ insists that **every path** must eventually receive an ack — a stricter liveness demand.\n\n---\n\n### Question 2\n**[KTU University Exam — Model Question, Dec 2023 Style]**  \n**Q: Prove that the operator $\\mathbf{G}$ (Globally) can be expressed using the Weak Until operator. Also write the canonical reduction rule for W.**\n\n**Model Answer (3 Marks):**\n\n**Global as Weak Until (2 Marks):**  \nSubstituting $g := \\mathbf{false}$ into the Weak Until definition:\n$$f \\,\\mathbf{W}\\, \\mathbf{false} \\;\\equiv\\; (f \\,\\mathbf{U}\\, \\mathbf{false}) \\;\\lor\\; \\mathbf{G}\\,f$$\nSince $f \\,\\mathbf{U}\\, \\mathbf{false} \\equiv \\mathbf{false}$ (no state can satisfy \"false\"), we obtain:\n$$\\boxed{\\mathbf{G}\\,f \\;\\equiv\\; f \\,\\mathbf{W}\\, \\mathbf{false}}$$\n\n**Canonical Reduction Rule for W (1 Mark):**  \n$$f \\,\\mathbf{W}\\, g \\;\\equiv\\; (f \\,\\mathbf{U}\\, g) \\;\\lor\\; \\mathbf{G}\\,f$$\n\nThis reduction permits model checkers to eliminate W from input formulas, reusing existing U- and G-handling subroutines.\n\n---\n\n## 5.2 Part B — Full-Descriptive Questions (14 Marks Each, with Internal Choice)\n\n### Question A (Choice 1) — 14 Marks\n\n**[KTU University Exam — Model Question]**  \n**Q: (a)** Define Kripke structures and explain how the Weak Until operator is evaluated on them. Discuss the role of the greatest fixpoint in computing the set of states satisfying $f \\,\\mathbf{W}\\, g$. **(7 Marks)**  \n**(b)** Consider the Kripke structure $M$ with states $S = \\{s_0, s_1, s_2, s_3\\}$, initial state $s_0$, transitions $\\{s_0 \\to s_1, s_1 \\to s_1, s_1 \\to s_2, s_2 \\to s_2, s_2 \\to s_3, s_3 \\to s_3\\}$, and labels $L(s_0) = \\{f\\}$, $L(s_1) = \\{f\\}$, $L(s_2) = \\{f, g\\}$, $L(s_3) = \\{g\\}$. Using the fixpoint algorithm, compute the set of states satisfying $f \\,\\mathbf{W}\\, g$. **(7 Marks)**\n\n#### Model Solution to (a) — 7 Marks\n\n**Kripke Structure Definition (2 Marks):**  \nA Kripke structure is a 4-tuple $M = (S, S_0, R, L)$:\n- $S$: finite set of **states**\n- $S_0 \\subseteq S$: **initial** states\n- $R \\subseteq S \\times S$: **transition relation** (must be total — every state has a successor)\n- $L: S \\to 2^{AP}$: **labeling function** assigning to each state the set of atomic propositions true in it\n\n**Weak Until Evaluation (2 Marks):**  \nA state $s$ satisfies $f \\,\\mathbf{W}\\, g$ on $M$ if there exists an infinite path $\\pi = s_0, s_1, s_2, \\ldots$ from $s$ such that either (i) some $\\pi[k] \\models g$ with all earlier positions satisfying $f$, or (ii) every position on $\\pi$ satisfies $f$. The operator is evaluated recursively, examining each state's outgoing transitions and the satisfaction status of its successors.\n\n**Role of Greatest Fixpoint (3 Marks):**  \nThe set $W = \\{s \\in S \\mid s \\models f \\,\\mathbf{W}\\, g\\}$ is computed as the greatest fixpoint of the operator:\n$$\\tau(X) = \\llbracket g \\rrbracket \\cup (\\llbracket f \\rrbracket \\cap \\mathbf{Pre}_{\\forall}(X))$$\nStarting from $X_0 = S$ and iterating $X_{i+1} = \\tau(X_i)$, the sequence monotonically decreases and stabilizes. The greatest fixpoint is required (not least) because Weak Until admits infinite $f$-chains where $g$ never appears — the **largest** set closed under the W-condition correctly captures all such states. Knaster–Tarski's theorem guarantees termination in $O(|S|)$ iterations on finite structures, yielding a model-checking complexity of $O(|S| \\cdot |f| \\cdot |g|)$.\n\n#### Model Solution to (b) — 7 Marks\n\n**Step 1 — Initialize $X_0 = S = \\{s_0, s_1, s_2, s_3\\}$ (1 Mark)**\n\n**Step 2 — Compute $X_1$ (2 Marks):**  \nSince $R$ is total, $\\mathbf{Pre}_{\\forall}(X_0) = X_0 = S$. Therefore:\n$$\n\\begin{aligned}\nX_1 &= \\llbracket g \\rrbracket \\cup (\\llbracket f \\rrbracket \\cap S) \\\\\n    &= \\{s_2, s_3\\} \\cup (\\{s_0, s_1, s_2\\} \\cap S) \\\\\n    &= \\{s_0, s_1, s_2, s_3\\}\n\\end{aligned}\n$$\n\n**Step 3 — Compute $X_2$ (2 Marks):**  \n$\\mathbf{Pre}_{\\forall}(X_1)$: which states have **all** successors in $X_1 = S$? Since $R$ is total and $X_1 = S$, this is $S$. Therefore:\n$$\n\\begin{aligned}\nX_2 &= \\llbracket g \\rrbracket \\cup (\\llbracket f \\rrbracket \\cap S) \\\\\n    &= \\{s_0, s_1, s_2, s_3\\}\n\\end{aligned}\n$$\n\n**Step 4 — Convergence (1 Mark):**  \n$X_1 = X_2$, so the fixpoint has stabilized.\n\n**Result (1 Mark):**  \n$$\\boxed{\\llbracket f \\,\\mathbf{W}\\, g \\rrbracket = \\{s_0, s_1, s_2, s_3\\} = S}$$\n\n**Verification:** All four states satisfy $f \\,\\mathbf{W}\\, g$ — $s_0, s_1$ via the $f$-forever path, $s_2$ via $g$ becoming true, $s_3$ trivially since $g$ is already true.\n\n---\n\n### Question B (Choice 2 — Alternative) — 14 Marks\n\n**[KTU University Exam — Model Question, Internal Choice]**  \n**Q: (a)** State and prove the equivalence $f \\,\\mathbf{W}\\, g \\equiv (f \\,\\mathbf{U}\\, g) \\lor \\mathbf{G}\\,f$. Show clearly both directions of the proof. **(7 Marks)**  \n**(b)** Using the above equivalence, derive expressions for (i) $\\mathbf{G}\\,f$ and (ii) $\\mathbf{F}\\,g$ in terms of W only. Justify each step. **(7 Marks)**\n\n#### Model Solution to (a) — 7 Marks\n\n**Statement (1 Mark):**  \nFor any state formulas $f, g$ in CTL/LTL and any state $s$ in a Kripke structure $M$:\n$$s \\models f \\,\\mathbf{W}\\, g \\iff s \\models (f \\,\\mathbf{U}\\, g) \\lor \\mathbf{G}\\,f$$\n\n**Proof ($\\Rightarrow$) (3 Marks):**  \nAssume $s \\models f \\,\\mathbf{W}\\, g$. By the W-semantics, one of two cases holds:  \n*Case 1:* $g$ holds at some $k \\geq 0$ with $f$ at all $j<k$. Then $s \\models f \\,\\mathbf{U}\\, g$, so the disjunction holds.  \n*Case 2:* $f$ holds at every position on the path, with $g$ never holding. Then $s \\models \\mathbf{G}\\,f$, so the disjunction holds.  \nIn both cases, $s \\models (f \\,\\mathbf{U}\\, g) \\lor \\mathbf{G}\\,f$.\n\n**Proof ($\\Leftarrow$) (3 Marks):**  \nAssume $s \\models (f \\,\\mathbf{U}\\, g) \\lor \\mathbf{G}\\,f$.  \n*Case 1:* $s \\models f \\,\\mathbf{U}\\, g$. Then $\\exists k$ with $\\pi[k] \\models g$ and $\\forall j<k: \\pi[j] \\models f$. This matches the first disjunct of W-semantics, so $s \\models f \\,\\mathbf{W}\\, g$.  \n*Case 2:* $s \\models \\mathbf{G}\\,f$. Then $\\forall k: \\pi[k] \\models f$. This matches the second disjunct (f-forever, g-never), so $s \\models f \\,\\mathbf{W}\\, g$.  \nIn both cases, the equivalence is established. $\\blacksquare$\n\n#### Model Solution to (b) — 7 Marks\n\n**(i) Deriving $\\mathbf{G}\\,f$ in terms of W (3.5 Marks):**  \nSet $g := \\mathbf{false}$ in the equivalence:\n$$f \\,\\mathbf{W}\\, \\mathbf{false} \\equiv (f \\,\\mathbf{U}\\, \\mathbf{false}) \\lor \\mathbf{G}\\,f$$\nBy definition of Strong Until, $f \\,\\mathbf{U}\\, \\mathbf{false}$ requires a position where $\\mathbf{false}$ holds — impossible. Therefore $f \\,\\mathbf{U}\\, \\mathbf{false} \\equiv \\mathbf{false}$. Hence:\n$$\\boxed{\\mathbf{G}\\,f \\equiv f \\,\\mathbf{W}\\, \\mathbf{false}}$$\n\n**(ii) Deriving $\\mathbf{F}\\,g$ in terms of W (3.5 Marks):**  \nWe use the standard identity $\\mathbf{F}\\,g \\equiv \\mathbf{true} \\,\\mathbf{U}\\, g$. We must now express this using W. Observe that:\n$$\\mathbf{F}\\,g \\equiv \\mathbf{true} \\,\\mathbf{U}\\, g \\equiv \\neg \\mathbf{G}\\,\\neg g \\equiv \\neg (\\neg g \\,\\mathbf{W}\\, \\mathbf{false})$$\nUsing the identity from part (i), $\\mathbf{G}\\,\\neg g \\equiv \\neg g \\,\\mathbf{W}\\, \\mathbf{false}$, so:\n$$\\boxed{\\mathbf{F}\\,g \\equiv \\neg(\\neg g \\,\\mathbf{W}\\, \\mathbf{false})}$$\nAlternative: $\\mathbf{F}\\,g \\equiv \\mathbf{true} \\,\\mathbf{W}\\, (g \\lor \\mathbf{G}\\,\\mathbf{false}) \\equiv \\mathbf{true} \\,\\mathbf{W}\\, g$ **only if** we allow $\\mathbf{G}\\,\\mathbf{false}$ as a marker. The most concise form acceptable in exams is $\\mathbf{F}\\,g \\equiv \\neg(\\neg g \\,\\mathbf{W}\\, \\mathbf{false})$.\n\n---\n\n> [!WARNING]  \n> **KTU Examiner's Valuation Warning — Common Pitfalls on Weak Until Questions**  \n> 1. **Confusing W and U:** The single most common error. Students write $f\\,\\mathbf{W}\\,g \\equiv f\\,\\mathbf{U}\\,g$ and lose 2-3 marks instantly. Always state the W-semantics *explicitly* with the \"or f holds forever\" clause.  \n> 2. **Forgetting the \"or G f\" clause in the equivalence:** The reduction $f\\,\\mathbf{W}\\,g \\equiv (f\\,\\mathbf{U}\\,g) \\lor \\mathbf{G}\\,f$ must be stated in *full* — partial marks if the $\\mathbf{G}\\,f$ term is omitted.  \n> 3. **Path quantifier confusion:** Writing $\\mathbf{E}[f\\,\\mathbf{W}\\,g]$ when the question requires $\\mathbf{A}[f\\,\\mathbf{W}\\,g]$. A is \"all\", E is \"exists\" — examiner will not award marks for swapped quantifiers.  \n> 4. **Fixpoint initial value:** For *greatest* fixpoint, start with $X_0 = S$ (top element), not $X_0 = \\emptyset$ (which is the *least* fixpoint for U). Mixing these up yields wrong model-checking results.  \n> 5. **Mis-stating $\\mathbf{G}\\,f \\equiv f\\,\\mathbf{W}\\,\\mathbf{false}$:** Some students write $f\\,\\mathbf{W}\\,\\mathbf{true}$ — which collapses trivially to $\\mathbf{true}$. Always substitute $g = \\mathbf{false}$ (the formula that is *never* satisfied), not $g = \\mathbf{true}$.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]  \n> **Rapid Revision Checklist — Weak Until**\n\n- ✅ **Symbol:** $\\mathbf{W}$ — a *binary*, *future*, *path-based* temporal operator.\n- ✅ **Definitional Identity:** $f\\,\\mathbf{W}\\,g \\equiv (f\\,\\mathbf{U}\\,g) \\lor \\mathbf{G}\\,f$.\n- ✅ **Key Distinction from U:** Weak Until **does NOT require** $g$ to ever hold; Strong Until **does**.\n- ✅ **Two semantic cases:** (1) $g$ eventually holds with $f$ persisting, OR (2) $f$ holds forever.\n- ✅ **Globally as W:** $\\mathbf{G}\\,f \\equiv f\\,\\mathbf{W}\\,\\mathbf{false}$.\n- ✅ **Finally via W:** $\\mathbf{F}\\,g \\equiv \\neg(\\neg g\\,\\mathbf{W}\\,\\mathbf{false})$.\n- ✅ **Release via W:** $f\\,\\mathbf{R}\\,g \\equiv g\\,\\mathbf{W}\\,(f \\land g)$.\n- ✅ **Fixpoint type:** **Greatest Fixpoint (gfp)** — iterative computation starts from $X_0 = S$, monotonically decreases.\n- ✅ **CTL forms:** $\\mathbf{A}[f\\,\\mathbf{W}\\,g]$ (universal) and $\\mathbf{E}[f\\,\\mathbf{W}\\,g]$ (existential).\n- ✅ **Industrial use:** Model checkers (NuSMV, SPIN, UPPAAL) normalize W into U+G at preprocessing stage.\n- ✅ **LTL vs CTL placement:** In LTL, W is a primitive path formula; in CTL, it must appear immediately after a path quantifier.\n- ✅ **Engineering role:** Expresses **safety** properties and **soft liveness** — preferred when strict eventual occurrence is not warranted.\n- ✅ **Exam mnemonic:** **\"W = Wait, or Walk away forever.\"** — $f$ waits for $g$, OR $f$ walks on forever without $g$ (no obligation broken).\n- ✅ **Algorithmic complexity:** Model checking $f\\,\\mathbf{W}\\,g$ takes $O(|S| \\cdot |\\phi|)$ time — linear in state space and formula size.\n- ✅ **Common pitfall:** Do NOT confuse $\\mathbf{A}[f\\,\\mathbf{W}\\,g]$ with $\\mathbf{AG}\\,f$. The former permits $g$ to appear; the latter forbids $f$ from ever being false. Trace carefully on a sample path before answering.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 3-t5",
          "title": "Release and Positive Normal Form",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 3\n> - **Topic:** Release and Positive Normal Form\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 The Release (R) Operator in Linear Temporal Logic (LTL)\n\n### Formal Definition (KTU 2024 Syllabus Terminology)\n\nThe **Release operator**, denoted by the symbol $R$, is a binary temporal connective in **Linear Temporal Logic (LTL)**. For two LTL formulas $\\phi$ and $\\psi$, the formula $\\phi \\, R \\, \\psi$ is interpreted over an infinite computation path $\\pi = s_0, s_1, s_2, \\ldots$\n\n$$(\\phi \\, R \\, \\psi) \\;\\equiv\\; \\text{\"}\\psi \\text{ holds UNTIL } \\phi \\text{ holds, OR } \\psi \\text{ holds forever}\"$$\n\n> [!IMPORTANT]\n> **Formal LTL Semantics (KTU Board Definition):**\n> $$\\pi \\models \\phi \\, R \\, \\psi \\iff \\exists k \\geq 0 \\; \\text{such that} \\; \\pi^k \\models \\phi \\; \\text{AND} \\; \\forall j, \\; 0 \\leq j < k \\Rightarrow \\pi^j \\models \\psi$$\n> If **no such $k$ exists**, then $\\psi$ must hold at **every** state on the path ($\\forall j \\geq 0, \\; \\pi^j \\models \\psi$).\n\n### Conceptual Analogy (Plain English Intuition)\n\n> [!NOTE]\n> 🎯 **\"The Handcuffs\" Analogy**\n>\n> Imagine $\\psi$ (e.g., *\"The alarm is OFF\"*) is a prisoner and $\\phi$ (e.g., *\"An intrusion occurs\"*) is the key. The formula $\\phi \\, R \\, \\psi$ says:\n>\n> *\"The alarm remains OFF — it is RELEASED from being OFF only when an intrusion occurs. If no intrusion ever occurs, the alarm stays OFF forever.\"*\n>\n> The \"release\" event is $\\phi$ — it is what *releases* $\\psi$ from its obligation to hold. This is the **exact dual** of the Until operator, where the obligation is reversed.\n\n---\n\n## 1.2 Positive Normal Form (PNF) — Canonical LTL Form\n\n### Formal Definition\n\nA **Linear Temporal Logic (LTL) formula is in Positive Normal Form (PNF)** if and only if it satisfies **three structural constraints simultaneously**:\n\n1. **No derived connectives**: The implication ($\\rightarrow$) and equivalence ($\\leftrightarrow$) operators are **completely absent**.\n2. **Negation depth = 1**: The logical NOT ($\\neg$) appears **only immediately before atomic propositions** (i.e., literals, not compound sub-formulas).\n3. **Restricted temporal vocabulary**: The only temporal operators permitted are $X$ (Next), $U$ (Until), and $R$ (Release). Derived operators $F$ (Eventually) and $G$ (Always) are eliminated and re-expressed via $U$ and $R$.\n\n### Conceptual Analogy\n\n> [!NOTE]\n> 🧮 **\"The Calculator's RPN\" Analogy**\n>\n> Just as **Reverse Polish Notation (RPN)** is a standardized, parenthesis-light form that every calculator can mechanically evaluate, **PNF** is the standardized, negation-minimal form that every model checker (SPIN, NuSMV, etc.) can mechanically handle. You cannot plug an arbitrary LTL formula straight into a tableau-based model checker — you first \"compile\" it into PNF.\n\n### Why Does PNF Matter in Automated Verification?\n\n> [!IMPORTANT]\n> - **Tableau construction** for LTL model checking (used in tools like **SPIN**) operates only on PNF formulas. Without PNF, tableau nodes would have to handle negations in every direction, exploding the state space.\n> - **SAT-based bounded model checking (BMC)** translates PNF into a propositional formula much more cleanly.\n> - **Anti-prenexing** of negation makes the duality $U \\leftrightarrow R$ directly visible, allowing algorithms to process both operators in a single, uniform pass.\n\n---\n\n## 1.3 The Vital Link: Why *Release* Appears in the *Same Module* as PNF\n\nThe two concepts are **mathematically inseparable** in LTL theory:\n\n| Reason | Explanation |\n| --- | --- |\n| **Duality** | $\\neg(\\phi \\, U \\, \\psi) \\equiv \\neg\\phi \\, R \\, \\neg\\psi$ — the Release operator is the **negation-dual** of Until. |\n| **PNF Requirement** | You **cannot** push a $\\neg$ past a $U$ using the dual form unless you **introduce** the $R$ operator. |\n| **Canonical Form** | Every LTL formula, after PNF conversion, contains only $\\{ \\wedge, \\vee, X, U, R\\}$ — making $R$ mandatory, not optional. |\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Operational Semantics of the Release Operator\n\nLet $\\pi = s_0, s_1, s_2, \\ldots$ denote an infinite sequence of states. We use the notation $\\pi^i$ to denote the suffix of $\\pi$ starting at state $s_i$.\n\n### Truth Table (Path-Indexed Interpretation)\n\n| Case | Path Behaviour | Verdict |\n| --- | --- | --- |\n| **Case 1** | $\\phi$ holds at the **current** state ($i = 0$) | Formula is **TRIVIALLY TRUE** (released immediately) |\n| **Case 2** | $\\phi$ first holds at state $k > 0$, and $\\psi$ holds at all states $0, 1, \\ldots, k-1$ | **SATISFIED** — release occurs at $k$ |\n| **Case 3** | $\\phi$ **never** holds on the entire infinite path | **SATISFIED iff** $\\psi$ holds at **every** state $s_i$ for $i \\geq 0$ |\n| **Case 4** | $\\phi$ never holds AND $\\psi$ fails at some state $i$ | **VIOLATED** |\n\n### Why is \"Release\" the Dual of \"Until\"?\n\n$$\n\\phi \\, R \\, \\psi \\;\\equiv\\; \\neg(\\neg\\phi \\, U \\, \\neg\\psi)\n$$\n\nThis is the single most important duality identity in LTL. To remember it: **\"Release is Until's negative mirror.\"** Swapping $U$ for $R$ is the **only** structural change that occurs when a single global $\\neg$ is pushed past a temporal operator.\n\n---\n\n## 2.2 The $F$ and $G$ Operators — Expressed via $R$\n\nThis is a **high-yield KTU 14-mark question** topic. Memorize the following identities:\n\n| Derived Operator | Read As | PNF Equivalent using $R$ |\n| --- | --- | --- |\n| $F \\, \\phi$ | \"Eventually $\\phi$\" | $true \\, R \\, \\phi$ |\n| $G \\, \\phi$ | \"Globally $\\phi$\" (always) | $false \\, R \\, \\phi$ |\n| $\\neg F \\, \\phi$ | \"Never $\\phi$\" | $false \\, R \\, \\neg\\phi$ (i.e., $G \\neg\\phi$) |\n| $\\neg G \\, \\phi$ | \"Not always $\\phi$\" | $true \\, R \\, \\neg\\phi$ (i.e., $F \\neg\\phi$) |\n\n> [!NOTE]\n> **Intuition for $F$ and $G$ as Releases:**\n> - $F \\phi \\equiv true \\, R \\, \\phi$: The \"$true$\" operand is released the moment $\\phi$ first becomes true. Since $true$ always holds, the *only* way to satisfy the formula is for $\\phi$ to eventually occur.\n> - $G \\phi \\equiv false \\, R \\, \\phi$: The \"$false$\" operand is *never* released (since $false$ is never true). So $\\phi$ must hold forever.\n\n---\n\n## 2.3 KTU High-Yield Formula Sheet (Cheat Sheet)\n\nThe following table consolidates **every equivalence required for PNF conversion and Release operator manipulation** in KTU ESE answers.\n\n| # | Identity / Law | Symbolic Form | Used For |\n| --- | --- | --- | --- |\n| 1 | Implication elimination | $\\phi \\to \\psi \\equiv \\neg\\phi \\vee \\psi$ | PNF step 1 |\n| 2 | Equivalence elimination | $\\phi \\leftrightarrow \\psi \\equiv (\\neg\\phi \\vee \\psi) \\wedge (\\neg\\psi \\vee \\phi)$ | PNF step 1 |\n| 3 | Double negation | $\\neg\\neg\\phi \\equiv \\phi$ | PNF step 2 |\n| 4 | De Morgan (∧) | $\\neg(\\phi \\wedge \\psi) \\equiv \\neg\\phi \\vee \\neg\\psi$ | PNF step 2 |\n| 5 | De Morgan (∨) | $\\neg(\\phi \\vee \\psi) \\equiv \\neg\\phi \\wedge \\neg\\psi$ | PNF step 2 |\n| 6 | Negation of $X$ | $\\neg X \\phi \\equiv X \\neg\\phi$ | PNF step 3 |\n| 7 | **Negation of $U$** | $\\neg(\\phi \\, U \\, \\psi) \\equiv \\neg\\phi \\, R \\, \\neg\\psi$ | PNF step 3 (**introduces R**) |\n| 8 | **Negation of $R$** | $\\neg(\\phi \\, R \\, \\psi) \\equiv \\neg\\phi \\, U \\, \\neg\\psi$ | PNF step 3 |\n| 9 | $F$ as Release | $F \\phi \\equiv true \\, R \\, \\phi$ | PNF step 4 |\n| 10 | $G$ as Release | $G \\phi \\equiv false \\, R \\, \\phi$ | PNF step 4 |\n| 11 | Duality $F$/$G$ | $F \\phi \\equiv \\neg G \\neg\\phi$ | Often used before PNF |\n| 12 | Duality $G$/$F$ | $G \\phi \\equiv \\neg F \\neg\\phi$ | Often used before PNF |\n| 13 | **$R$ definition via Until** | $\\phi \\, R \\, \\psi \\equiv \\neg(\\neg\\phi \\, U \\, \\neg\\psi)$ | Foundational duality |\n| 14 | $U$ via $R$ | $\\phi \\, U \\, \\psi \\equiv \\neg(\\neg\\phi \\, R \\, \\neg\\psi)$ | Symmetric counterpart |\n| 15 | Fixed point of $R$ | $\\phi \\, R \\, \\psi \\equiv \\psi \\vee (\\phi \\wedge X(\\phi \\, R \\, \\psi))$ | Used in tableau-based model checking |\n\n> [!IMPORTANT]\n> **Engineering Utility:** These identities are the backbone of the **SPIN model checker's LTL translator (`spin -f`)** and the **LTL $\\to$ Büchi automaton** pipeline used in every modern verification toolchain. Mastery of these laws directly enables you to trace any tool-generated automaton back to its source formula.\n\n---\n\n## 2.4 Why PNF Conversion is Algorithmically Important\n\nConsider an LTL formula that contains *nested* negations, implications, and $F$/$G$ operators. A model checker's tableau algorithm must process a formula one connective at a time. If the formula is *not* in PNF, the algorithm must either:\n\n1. Apply these rewrite rules **on the fly** at every tableau node (expensive), **OR**\n2. **Pre-process** the formula once into PNF (cheap, one-time cost).\n\nThe standard approach in tools like **SPIN** is option 2 — hence PNF is a **mandatory preprocessing step**. For a formula of size $n$, PNF conversion runs in **linear time** $O(n)$, since each connective is rewritten exactly once.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Worked Conversions\n\n## 3.1 Worked Example 1 — Proving the $R$–$U$ Duality\n\n**Claim:** $\\phi \\, R \\, \\psi \\equiv \\neg(\\neg\\phi \\, U \\, \\neg\\psi)$\n\n### Proof (Direct Semantic Argument)\n\nWe prove this by showing both sides are true on exactly the same set of paths.\n\n**Forward direction ($\\Rightarrow$):** Suppose $\\pi \\models \\phi \\, R \\, \\psi$.\n\n* **Case A:** $\\phi$ holds at some position $k$ on $\\pi$. Then by the $R$ semantics, $\\psi$ holds at all positions $j < k$. Taking negations, $\\neg\\psi$ fails at $j < k$, so the formula $\\neg\\phi \\, U \\, \\neg\\psi$ **cannot** be satisfied — but this is fine: we are showing that $\\pi \\not\\models \\neg\\phi \\, U \\, \\neg\\psi$, which means $\\pi \\models \\neg(\\neg\\phi \\, U \\, \\neg\\psi)$.\n* **Case B:** $\\phi$ never holds, and $\\psi$ holds at every position (else $R$ would fail). Then $\\neg\\psi$ fails everywhere, so $\\neg\\phi \\, U \\, \\neg\\psi$ fails, i.e., $\\pi \\models \\neg(\\neg\\phi \\, U \\, \\neg\\psi)$.\n\n**Reverse direction ($\\Leftarrow$):** Suppose $\\pi \\models \\neg(\\neg\\phi \\, U \\, \\neg\\psi)$, i.e., $\\pi \\not\\models \\neg\\phi \\, U \\, \\neg\\psi$.\n\nBy the $U$ semantics, $\\neg\\phi \\, U \\, \\neg\\psi$ fails precisely when either (i) $\\neg\\psi$ holds at all positions AND $\\neg\\phi$ holds at all positions, OR (ii) $\\neg\\psi$ fails at some position $m$ before $\\neg\\phi$ ever holds. Combining these two cases, we deduce that **$\\psi$ holds at all positions up to and including the first position where $\\phi$ holds** — which is exactly the $R$ semantics. $\\blacksquare$\n\n---\n\n## 3.2 Worked Example 2 — Full PNF Conversion of a Complex Formula\n\n**Task:** Convert the following LTL formula to **Positive Normal Form**, showing **every intermediate step**:\n\n$$\n\\varphi \\;=\\; \\neg \\big( \\, (p \\wedge q) \\;\\to\\; (F \\, r \\;\\vee\\; G \\, s) \\, \\big)\n$$\n\n### Step 1: Eliminate the Implication ($\\to$)\n\nApply identity $\\#1$ from the cheat sheet: $\\phi \\to \\psi \\equiv \\neg\\phi \\vee \\psi$. Here, $\\phi \\equiv (p \\wedge q)$ and $\\psi \\equiv (F r \\vee G s)$.\n\n$$\n\\varphi \\;=\\; \\neg \\big( \\, \\neg(p \\wedge q) \\;\\vee\\; (F \\, r \\;\\vee\\; G \\, s) \\, \\big)\n$$\n\n> **Conversion logic:** We have replaced the single $\\to$ with $\\neg \\phi \\vee \\psi$. The outer $\\neg$ is now surrounding a $\\vee$ — this is the trigger for the next De Morgan step.\n\n### Step 2: Apply De Morgan's Law to the Outer Negation\n\nApply identity $\\#5$: $\\neg(\\phi \\vee \\psi) \\equiv \\neg\\phi \\wedge \\neg\\psi$.\n\n$$\n\\varphi \\;=\\; \\neg\\neg(p \\wedge q) \\;\\wedge\\; \\neg(F \\, r \\;\\vee\\; G \\, s)\n$$\n\n> **Conversion logic:** The outer $\\neg$ distributes inward over the top-level $\\vee$, yielding an $\\wedge$ of two $\\neg$-applied sub-formulas. We have **not** yet pushed $\\neg$ past $F$ or $G$.\n\n### Step 3: Eliminate the Double Negation\n\nApply identity $\\#3$: $\\neg\\neg(p \\wedge q) \\equiv (p \\wedge q)$.\n\n$$\n\\varphi \\;=\\; (p \\wedge q) \\;\\wedge\\; \\neg(F \\, r \\;\\vee\\; G \\, s)\n$$\n\n> **Conversion logic:** The left conjunct is now in clean form. The right conjunct still has a $\\neg$ surrounding $F$ and $G$.\n\n### Step 4: Apply De Morgan Inside the Right Conjunct\n\nApply identity $\\#5$ to $\\neg(F \\, r \\vee G \\, s)$:\n\n$$\n\\varphi \\;=\\; (p \\wedge q) \\;\\wedge\\; \\big( \\neg F \\, r \\;\\wedge\\; \\neg G \\, s \\big)\n$$\n\n> **Conversion logic:** The $\\neg$ has been pushed through the $\\vee$, leaving an $\\wedge$ and two negated temporal operators $F$ and $G$.\n\n### Step 5: Convert $\\neg F$ and $\\neg G$ Using Release Duality\n\nApply identities $\\#11$ and $\\#12$: $\\neg F \\phi \\equiv G \\neg\\phi$ and $\\neg G \\phi \\equiv F \\neg\\phi$.\n\n$$\n\\varphi \\;=\\; (p \\wedge q) \\;\\wedge\\; \\big( G \\neg r \\;\\wedge\\; F \\neg s \\big)\n$$\n\n> **Conversion logic:** The negations have now flipped the temporal operators. Note carefully: $F$ became $G$ and vice-versa, with the atomic proposition being negated.\n\n### Step 6: Rewrite $G$ and $F$ in Terms of $R$ (Final PNF)\n\nApply identities $\\#9$ and $\\#10$: $F \\phi \\equiv true \\, R \\, \\phi$ and $G \\phi \\equiv false \\, R \\, \\phi$.\n\n$$\n\\varphi \\;=\\; (p \\wedge q) \\;\\wedge\\; \\big( (false \\, R \\, \\neg r) \\;\\wedge\\; (true \\, R \\, \\neg s) \\big)\n$$\n\n### Verification of PNF Compliance\n\n| Constraint | Status |\n| --- | --- |\n| No $\\rightarrow$ or $\\leftrightarrow$ | ✅ Satisfied (eliminated in Step 1) |\n| No $\\neg$ on compound sub-formulas | ✅ Satisfied (only $\\neg r$, $\\neg s$ remain) |\n| No $F$ or $G$ | ✅ Satisfied (rewritten in Step 6) |\n| Only $\\{ \\wedge, \\vee, X, U, R \\}$ used | ✅ Satisfied |\n\n$$\n\\boxed{\\;\\varphi_{\\text{PNF}} \\;=\\; (p \\wedge q) \\;\\wedge\\; (false \\, R \\, \\neg r) \\;\\wedge\\; (true \\, R \\, \\neg s)\\;}\n$$\n\n---\n\n## 3.3 Worked Example 3 — Push Negation Through Nested Temporal Operators\n\n**Task:** Convert $\\psi = \\neg X (\\neg p \\, U \\, q)$ to PNF.\n\n### Step 1: Push $\\neg$ Through $X$\n\nApply identity $\\#6$: $\\neg X \\phi \\equiv X \\neg\\phi$.\n\n$$\n\\psi \\;=\\; X \\, \\neg(\\neg p \\, U \\, q)\n$$\n\n> **Conversion logic:** The $\\neg$ slides through the $X$ cleanly because $X$ acts on a single position.\n\n### Step 2: Apply $U \\to R$ Duality\n\nApply identity $\\#7$: $\\neg(\\phi \\, U \\, \\psi) \\equiv \\neg\\phi \\, R \\, \\neg\\psi$. Here, $\\phi \\equiv \\neg p$ and $\\psi \\equiv q$.\n\n$$\n\\psi \\;=\\; X \\, \\big( \\neg(\\neg p) \\, R \\, \\neg q \\big)\n$$\n\n### Step 3: Eliminate Double Negation\n\nApply identity $\\#3$: $\\neg(\\neg p) \\equiv p$.\n\n$$\n\\psi \\;=\\; X \\, (p \\, R \\, \\neg q)\n$$\n\n### Final PNF Result\n\n$$\n\\boxed{\\;\\psi_{\\text{PNF}} \\;=\\; X \\, (p \\, R \\, \\neg q)\\;}\n$$\n\nThis is in PNF because:\n- The only operator is $X$ and $R$ (both permitted).\n- The only negations are on atomic propositions ($q$ in $\\neg q$).\n- No $\\neg$ appears on any compound sub-formula.\n\n---\n\n## 3.4 Analytical Matrix — Operator Push-Down Behaviour\n\nThe following matrix summarizes how a single $\\neg$ interacts with every LTL operator during PNF conversion. **This matrix is KTU-exam favorite for 3-mark definition questions.**\n\n| Operator | Original Form | After Pushing $\\neg$ Past It | Identity Used |\n| --- | --- | --- | --- |\n| Atomic | $\\neg p$ | $\\neg p$ (terminal) | — |\n| Boolean AND | $\\neg(\\phi \\wedge \\psi)$ | $\\neg\\phi \\vee \\neg\\psi$ | De Morgan |\n| Boolean OR | $\\neg(\\phi \\vee \\psi)$ | $\\neg\\phi \\wedge \\neg\\psi$ | De Morgan |\n| Implication | $\\neg(\\phi \\to \\psi)$ | $\\phi \\wedge \\neg\\psi$ | $a \\to b \\equiv \\neg a \\vee b$, then De Morgan |\n| Next ($X$) | $\\neg X \\phi$ | $X \\neg\\phi$ | Self-dual |\n| Until ($U$) | $\\neg(\\phi U \\psi)$ | $\\neg\\phi R \\neg\\psi$ | **Duality — introduces R** |\n| Release ($R$) | $\\neg(\\phi R \\psi)$ | $\\neg\\phi U \\neg\\psi$ | **Duality — introduces U** |\n| Eventually ($F$) | $\\neg F \\phi$ | $G \\neg\\phi \\equiv false R \\neg\\phi$ | $F \\equiv true R$ |\n| Globally ($G$) | $\\neg G \\phi$ | $F \\neg\\phi \\equiv true R \\neg\\phi$ | $G \\equiv false R$ |\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 PNF Conversion Pipeline (Algorithmic Flow)\n\nThe following block diagram captures the **complete algorithmic pipeline** for converting any arbitrary LTL formula into its Positive Normal Form. Each stage applies one class of rewrite rules from the cheat sheet.\n\n```mermaid\nflowchart TD\n    startNode[Arbitrary LTL Formula] --> stage1[Stage 1: Eliminate implies and iff]\n    stage1 --> stage2[Stage 2: Collapse double negations]\n    stage2 --> stage3[Stage 3: Push negations inward over boolean operators]\n    stage3 --> stage4[Stage 4: Push negations inward over temporal operators]\n    stage4 --> stage5[Stage 5: Rewrite F and G using the R operator]\n    stage5 --> resultNode[Formula in Positive Normal Form]\n\n    stage1 -. uses .-> law1[Identity 1 and 2 from cheat sheet]\n    stage2 -. uses .-> law2[Identity 3 from cheat sheet]\n    stage3 -. uses .-> law3[Identities 4 and 5: De Morgan]\n    stage4 -. uses .-> law4[Identities 6, 7, 8: X, U, R duality]\n    stage5 -. uses .-> law5[Identities 9 and 10: F and G as Release]\n```\n\n### Reading the Diagram\n\n- The **solid arrows** denote the main control flow.\n- The **dotted arrows** denote *which identity from the cheat sheet* is applied at each stage. This is the most common KTU 14-mark answer structure: a numbered list of stages, each annotated with the underlying logical law.\n\n---\n\n## 4.2 Decision Flow for the Release Operator $\\phi R \\psi$\n\nThis diagram captures the **runtime semantics** of the Release operator. It is the \"machine\" that the model checker simulates when checking $\\pi \\models \\phi R \\psi$.\n\n```mermaid\nflowchart TD\n    initNode[Start evaluation at time t equals 0] --> checkPhi{Does phi hold at current state?}\n    checkPhi -->|Yes branch| successImmediate[Formula is SATISFIED, future unconstrained]\n    checkPhi -->|No branch| checkPsi{Does psi hold at current state?}\n    checkPsi -->|No branch| failureNode[Formula is VIOLATED, fail and backtrack]\n    checkPsi -->|Yes branch| advance[Advance time t to t plus 1]\n    advance --> checkPhi\n```\n\n### Reading the Diagram\n\n- **Top diamond ($\\phi$ check):** If $\\phi$ holds *now*, the release condition is met *immediately*. The model checker does **not** need to verify $\\psi$'s future behaviour.\n- **Bottom diamond ($\\psi$ check):** If $\\phi$ does **not** hold, $\\psi$ *must* hold at the current state — otherwise the formula fails.\n- **Loop:** The evaluator advances one time step and re-enters the $\\phi$ check, creating the classic \"wait-for-release\" loop.\n\n---\n\n## 4.3 Architecture Map — Where PNF and Release Live in a Model Checker\n\nThis block diagram shows the **functional architecture** of a typical LTL model checker, highlighting exactly *where* in the pipeline Positive Normal Form conversion occurs and *where* the Release operator gets processed.\n\n```mermaid\nflowchart LR\n    inputNode[User LTL Property] --> parser[Lexical and Syntactic Parser]\n    parser --> pnfConv[PNF Converter Module]\n    pnfConv --> negPush[Negation Push Down Engine]\n    negPush --> rewrite[F and G to R Rewriter]\n    rewrite --> buchiGen[Buchi Automaton Generator]\n    buchiGen --> productGen[Product Automaton with System Model]\n    productGen --> emptCheck[Emptiness Check via Nested DFS]\n    emptCheck --> outputNode[Counterexample or Success]\n\n    pnfConv -. relies on .-> releaseDual[Release Until Duality Laws]\n    buchiGen -. emits .-> releaseTrans[Release Transitions in Automaton]\n```\n\n### Reading the Diagram\n\n- The **PNF Converter** is a *preprocessing module* that runs once, before Büchi automaton construction. This is the architectural position that makes PNF a foundational concept.\n- The **Release-Until Duality** laws are used at *two* distinct points: once in the PNF converter, and once again when generating the Büchi transitions (since each $R$ formula becomes a specific type of accepting transition in the automaton).\n- The **Emptiness Check** is where the final verdict is computed. The product automaton's language is checked for emptiness — if non-empty, a counterexample (witness) is returned.\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n> [!NOTE]\n> **Mark Distribution (KTU ESE Pattern):** Part A = 3 marks each, Part B = 14 marks each with internal choice. Bloom's levels: *Apply* and *Analyze* dominate this topic.\n\n---\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Question 1 `[KTU University Exam – May 2024]`\n\n**Define the Release operator in LTL. State its formal semantics and show that it is the dual of the Until operator.**\n\n**Model Answer (3 marks):**\n\nThe **Release operator** $R$ is a binary temporal connective in LTL. For formulas $\\phi$ and $\\psi$, the formula $\\phi \\, R \\, \\psi$ is read as *\"$\\psi$ holds until $\\phi$ releases it, or $\\psi$ holds forever.\"*\n\n**Formal semantics:** A path $\\pi$ satisfies $\\phi \\, R \\, \\psi$ if and only if either there exists a position $k \\geq 0$ on $\\pi$ such that $\\pi^k \\models \\phi$ and $\\psi$ holds at all positions $j$ with $0 \\leq j < k$, OR $\\psi$ holds at every position on $\\pi$ (when $\\phi$ never holds).\n\n**Duality with Until:** The Release operator is the negation-dual of Until:\n\n$$\n\\phi \\, R \\, \\psi \\;\\equiv\\; \\neg(\\neg\\phi \\, U \\, \\neg\\psi)\n$$\n\nThis is the **defining duality** of LTL and is the *only* identity that introduces $R$ when pushing negations during PNF conversion. **(3 marks — full credit)**\n\n---\n\n### Question 2 `[KTU University Exam – Dec 2023]`\n\n**What is Positive Normal Form in LTL? List the three structural conditions a formula must satisfy to be in PNF.**\n\n**Model Answer (3 marks):**\n\nA Linear Temporal Logic (LTL) formula is in **Positive Normal Form (PNF)** if it satisfies **all three** of the following structural conditions:\n\n1. **No derived boolean connectives:** The implication ($\\to$) and equivalence ($\\leftrightarrow$) operators are **completely absent** from the formula.\n2. **Negations are atomic-only:** The logical NOT ($\\neg$) appears **only immediately before atomic propositions** (e.g., $\\neg p$, $\\neg q$), and never in front of compound sub-formulas.\n3. **Restricted temporal vocabulary:** The only temporal operators permitted are $X$ (Next), $U$ (Until), and $R$ (Release). The derived operators $F$ (Eventually) and $G$ (Always) must be eliminated and re-expressed using $R$.\n\n> **Significance:** PNF is the canonical input form for tableau-based and SAT-based LTL model checkers such as SPIN and NuSMV. **(3 marks — full credit)**\n\n---\n\n## 5.2 Part B — Full-Length 14-Mark Questions (With Internal Choice)\n\n### ⭐ Question A (Choice 1) `[KTU University Exam – Model Paper 2024]`\n\n**(a)** [7 marks] **Explain the Release operator in detail. Derive its relationship with the Until operator and discuss the semantics with two example paths.**\n\n**(b)** [7 marks] **Convert the following LTL formula to Positive Normal Form, showing all intermediate steps:**\n\n$$\n\\Phi \\;=\\; \\neg \\big( (p \\wedge q) \\to (F \\, r \\vee G \\, s) \\big)\n$$\n\n#### Model Solution for (a) — 7 marks\n\n**Definition [1 mark]:** The Release operator $R$ is a binary temporal connective in LTL. The formula $\\phi \\, R \\, \\psi$ asserts that $\\psi$ holds continuously *until* $\\phi$ becomes true (at which point $\\psi$ is \"released\" from its obligation). If $\\phi$ never becomes true, then $\\psi$ must hold forever.\n\n**Formal semantics [2 marks]:** $\\pi \\models \\phi \\, R \\, \\psi$ iff one of the two conditions holds:\n- There exists $k \\geq 0$ such that $\\pi^k \\models \\phi$ AND $\\forall j, \\, 0 \\leq j < k, \\, \\pi^j \\models \\psi$.\n- OR $\\forall j \\geq 0, \\, \\pi^j \\models \\psi$ (i.e., $\\phi$ never holds AND $\\psi$ holds forever).\n\n**Duality derivation [2 marks]:** We derive the duality by direct semantic reasoning:\n\n$$\n\\begin{aligned}\n\\pi &\\models \\phi \\, R \\, \\psi \\\\\n&\\iff \\big( \\exists k \\geq 0 \\cdot \\pi^k \\models \\phi \\land \\forall j < k \\cdot \\pi^j \\models \\psi \\big) \\lor \\big( \\forall j \\geq 0 \\cdot \\pi^j \\models \\psi \\big) \\\\\n&\\iff \\neg \\big( \\neg\\exists k \\geq 0 \\cdot \\pi^k \\models \\phi \\land \\forall j < k \\cdot \\pi^j \\models \\psi \\big) \\quad \\text{(by negation of disjunction)} \\\\\n&\\iff \\neg \\big( \\forall k \\geq 0 \\cdot \\pi^k \\models \\neg\\phi \\lor \\exists j < k \\cdot \\pi^j \\models \\neg\\psi \\big) \\\\\n&\\iff \\neg ( \\neg\\phi \\, U \\, \\neg\\psi ) \\quad \\text{(by Until semantics)}\n\\end{aligned}\n$$\n\nHence $\\phi \\, R \\, \\psi \\equiv \\neg(\\neg\\phi \\, U \\, \\neg\\psi)$.\n\n**Two example paths [2 marks]:**\n- **Path 1:** $\\pi = \\{p, p, q, q, q, \\ldots\\}$. For $\\phi = p, \\, \\psi = q$: $\\phi$ holds at $k=0$, so $\\phi R \\psi$ is **trivially satisfied** (released immediately).\n- **Path 2:** $\\pi = \\{q, q, q, q, \\ldots\\}$. For $\\phi = p, \\, \\psi = q$: $\\phi$ never holds, but $\\psi$ holds forever — formula is **satisfied**.\n- **Path 3 (counter-example):** $\\pi = \\{q, \\neg q, q, \\ldots\\}$. $\\phi$ never holds AND $\\psi$ fails at position 1 — formula is **violated**.\n\n#### Model Solution for (b) — 7 marks\n\nThe complete PNF conversion of $\\Phi$ was worked out **step-by-step in Section 3.2** of these notes. The final result is:\n\n$$\n\\boxed{\\;\\Phi_{\\text{PNF}} \\;=\\; (p \\wedge q) \\;\\wedge\\; (false \\, R \\, \\neg r) \\;\\wedge\\; (true \\, R \\, \\neg s)\\;}\n$$\n\nThe 7 marks are awarded as: 1 mark per major step (we have 5 named steps), plus 1 mark for the final answer, plus 1 mark for verification of PNF compliance. **(7/7 marks)**\n\n---\n\n### ⭐ Question B (Choice 2) `[KTU University Exam – Sample Paper]`\n\n**(a)** [7 marks] **Define Positive Normal Form. List all the conversion rules (rewrite identities) used in PNF conversion of an LTL formula, with each rule's purpose.**\n\n**(b)** [7 marks] **Convert the following LTL formula to Positive Normal Form, showing all intermediate steps:**\n\n$$\n\\Psi \\;=\\; (F \\, p \\;\\wedge\\; G \\, q) \\;\\to\\; (p \\, R \\, q)\n$$\n\n#### Model Solution for (a) — 7 marks\n\n**PNF Definition [1 mark]:** An LTL formula is in **Positive Normal Form (PNF)** if (1) it contains no $\\to$ or $\\leftrightarrow$, (2) $\\neg$ appears only in front of atomic propositions, and (3) the only temporal operators are $X$, $U$, and $R$.\n\n**Rewrite rules (identities) used in PNF conversion [6 marks — 0.5 mark each for 12 rules]:**\n\n| # | Identity | Purpose |\n| --- | --- | --- |\n| 1 | $\\phi \\to \\psi \\equiv \\neg\\phi \\vee \\psi$ | Eliminate implication |\n| 2 | $\\phi \\leftrightarrow \\psi \\equiv (\\neg\\phi \\vee \\psi) \\wedge (\\neg\\psi \\vee \\phi)$ | Eliminate equivalence |\n| 3 | $\\neg\\neg\\phi \\equiv \\phi$ | Collapse double negation |\n| 4 | $\\neg(\\phi \\wedge \\psi) \\equiv \\neg\\phi \\vee \\neg\\psi$ | De Morgan (AND) |\n| 5 | $\\neg(\\phi \\vee \\psi) \\equiv \\neg\\phi \\wedge \\neg\\psi$ | De Morgan (OR) |\n| 6 | $\\neg X \\phi \\equiv X \\neg\\phi$ | Push $\\neg$ through Next |\n| 7 | $\\neg(\\phi U \\psi) \\equiv \\neg\\phi R \\neg\\psi$ | Push $\\neg$ through Until (**introduces R**) |\n| 8 | $\\neg(\\phi R \\psi) \\equiv \\neg\\phi U \\neg\\psi$ | Push $\\neg$ through Release (**introduces U**) |\n| 9 | $F \\phi \\equiv true R \\phi$ | Rewrite Eventually |\n| 10 | $G \\phi \\equiv false R \\phi$ | Rewrite Globally |\n| 11 | $F \\phi \\equiv \\neg G \\neg\\phi$ | Dual expansion (pre-PNF) |\n| 12 | $G \\phi \\equiv \\neg F \\neg\\phi$ | Dual expansion (pre-PNF) |\n\n#### Model Solution for (b) — 7 marks\n\n**Step 1 [1 mark]: Eliminate the implication** $\\to$. Using identity #1:\n\n$$\n\\Psi \\;=\\; \\neg(F p \\wedge G q) \\;\\vee\\; (p R q)\n$$\n\n**Step 2 [1 mark]: Apply De Morgan's law** to $\\neg(F p \\wedge G q)$. Using identity #4:\n\n$$\n\\Psi \\;=\\; \\neg F p \\;\\vee\\; \\neg G q \\;\\vee\\; (p R q)\n$$\n\n**Step 3 [1.5 marks]: Convert $\\neg F p$ and $\\neg G q$** to standard dual forms. Using identities #11 and #12:\n\n$$\n\\Psi \\;=\\; (G \\neg p) \\;\\vee\\; (F \\neg q) \\;\\vee\\; (p R q)\n$$\n\n> **Conversion logic:** $\\neg F p$ becomes $G \\neg p$ (the negation is \"absorbed\" into the atomic proposition while the temporal operator flips from $F$ to $G$). Similarly, $\\neg G q$ becomes $F \\neg q$.\n\n**Step 4 [1.5 marks]: Rewrite $G$ and $F$ using the Release operator**. Using identities #9 and #10:\n\n$$\n\\Psi \\;=\\; (false R \\neg p) \\;\\vee\\; (true R \\neg q) \\;\\vee\\; (p R q)\n$$\n\n**Step 5 [1 mark]: Verify PNF compliance.** The resulting formula contains:\n- Only $\\vee$ (no $\\wedge$ at top, but $\\wedge$ is permitted inside sub-formulas if any — there are none here).\n- No $\\to$, $\\leftrightarrow$, $\\neg$ on compound formulas, or $F$/$G$ operators.\n- Only the temporal operators $R$ and its boolean combinations.\n\n**Step 6 [1 mark]: Final PNF result:**\n\n$$\n\\boxed{\\;\\Psi_{\\text{PNF}} \\;=\\; (false R \\neg p) \\;\\vee\\; (true R \\neg q) \\;\\vee\\; (p R q)\\;}\n$$\n\n**(7/7 marks)**\n\n---\n\n> [!WARNING]\n> ⚠️ **KTU Examiner's Valuation Pitfalls (Where Students Lose Marks)**\n>\n> 1. **Forgetting to rewrite $F$ and $G$ in the final step.** Many students stop at the De Morgan stage and leave $F$ / $G$ in the formula. KTU strictly requires **all** $F$ / $G$ to be eliminated using $true R$ and $false R$ respectively. **Deduct 1–2 marks.**\n> 2. **Confusing the duality direction.** The rule is $\\neg(\\phi U \\psi) \\equiv \\neg\\phi R \\neg\\psi$ — the $\\neg$ goes to **both** $\\phi$ AND $\\psi$. A common student error is writing $\\phi R \\neg\\psi$, which loses 1–2 marks.\n> 3. **Forgetting to verify PNF compliance.** Always end your answer with a brief 3-bullet \"PNF check\" listing the three structural conditions. KTU examiners award bonus credit for this self-verification.\n> 4. **Not showing the intermediate steps.** Skipping the De Morgan or duality step in a 14-mark answer can cost you 2–3 marks, even if the final answer is correct. **Always show all work.**\n> 5. **Mis-stating the Release semantics.** A frequent error: *\"$\\phi R \\psi$ means $\\psi$ holds until $\\phi$ becomes true.\"* This is **almost** right but misses the crucial \"OR $\\psi$ holds forever\" clause. Examiners deduct 1 mark for this omission.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> ### 🧠 Rapid Revision Checklist (Print This Before the Exam)\n\n- [ ] **Release semantics:** $\\phi R \\psi$ means *\"$\\psi$ holds until $\\phi$ releases it, OR $\\psi$ holds forever.\"* Never forget the **forever-clause** when $\\phi$ never occurs.\n- [ ] **The defining duality:** $\\phi R \\psi \\equiv \\neg(\\neg\\phi U \\neg\\psi)$ — memorize this verbatim. It is the *only* identity that introduces $R$ during PNF conversion.\n- [ ] **$F$ and $G$ as Releases:** $F \\phi \\equiv true R \\phi$ and $G \\phi \\equiv false R \\phi$. Know these cold — they are the final rewrite step in every PNF conversion.\n- [ ] **PNF = 3 constraints:** (1) No $\\to$/$\\leftrightarrow$, (2) $\\neg$ only on atoms, (3) Only $X$, $U$, $R$ as temporal operators.\n- [ ] **The negation push-down rules (cheat sheet table) is examinable verbatim.** Be able to recite all 12 identities in Section 2.3.\n- [ ] **Why PNF matters:** It is the mandatory preprocessing step for tableau-based model checkers (SPIN, NuSMV). The conversion is $O(n)$ in formula size.\n- [ ] **Fixed-point form of $R$:** $\\phi R \\psi \\equiv \\psi \\vee (\\phi \\wedge X(\\phi R \\psi))$ — used in tableau expansion, expect a 3-mark question asking to derive this.\n- [ ] **Bounded model checking connection:** PNF formulas translate cleanly into propositional CNF for SAT solving — mention this if asked about engineering applications.\n- [ ] **Dual symmetry:** $U$ and $R$ are exact duals. If a formula has only $U$, its negation has only $R$ (and vice versa). This is the structural signature of the duality.\n- [ ] **Common trap:** $F \\phi \\not\\equiv \\phi U \\, true$. The correct form is $F \\phi \\equiv true U \\phi$ (or equivalently, $true R \\phi$). Don't mix these up.\n\n---\n\n### 📚 Mapping to Course Outcomes (KTU 2024 Scheme)\n\n| Course Outcome | How This Topic Maps |\n| --- | --- |\n| **CO2:** Apply temporal logics for specifying system properties | Release operator enables succinct specification of \"wait-for-event\" patterns |\n| **CO3:** Construct verification procedures using LTL/CTL | PNF is the prerequisite step for tableau-based model checking |\n| **CO5:** Analyze verification challenges in concurrent systems | Release naturally models synchronization barriers in concurrent protocols |\n\n**Bloom's Taxonomy Levels Exercised:** *Apply* (PNF conversion), *Analyze* (semantic decomposition), *Evaluate* (duality proofs).\n\n---\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 3-t6",
          "title": "Fairness",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module Module 3\n> - **Topic:** Fairness\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of Fairness\n\n**Fairness** in automated verification is a class of scheduling (or behavioral) constraints imposed on a concurrent or reactive system to restrict attention to those execution paths in which no individual process, transition, or component is starved or perpetually ignored. Formally, fairness is a set of path quantifier conditions over a Kripke structure (or labelled transition system) used to eliminate unrealistic or degenerate interleavings during model checking.\n\n$$ \\text{Fairness} = \\{ \\pi \\in \\text{Paths}(M) \\mid \\pi \\text{ satisfies the fairness constraint } \\mathcal{F} \\} $$\n\nwhere $M$ is the Kripke structure, $\\pi$ is an infinite path, and $\\mathcal{F}$ is a fairness condition (a set of properties expressed in temporal logic).\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Highlight (Module 3):** Fairness is essential when verifying concurrent and reactive systems modeled using **LTL (Linear Temporal Logic)** and **CTL (Computation Tree Logic)**. Without fairness, a model checker may produce **spurious counterexamples** that represent unrealistic execution orders.\n\n## 1.2 The Problem Fairness Solves — A Real-World Analogy\n\n**Analogy: The Elevator Control System**\n\nImagine a 5-floor building with **two elevators** controlled by a single scheduler. Without fairness, the scheduler could theoretically serve the *same floor* forever, ignoring the other floors. Technically, such a path exists in the state graph — but it is **not a realistic behavior**.\n\nA **fair scheduler** ensures:\n- Every request eventually gets a turn.\n- No floor is perpetually skipped.\n\nThis is exactly what fairness guarantees in a model: it prunes away infinite paths that are mathematically valid but practically **unrealizable or unfair**.\n\n## 1.3 Why Fairness Is Needed in Model Checking\n\n| Problem Without Fairness | Consequence | How Fairness Helps |\n|---|---|---|\n| Scheduler can ignore a process forever | Spurious counterexample (false positive) | Restricts model to fair paths only |\n| Mutual exclusion is \"violated\" by unrealistic interleavings | Verification of liveness fails | Fair paths reflect real-world scheduling |\n| Process is starved in a race condition | Correct programs reported as incorrect | Guarantees each transition is exercised |\n\n> [!NOTE]\n> **Definition — Kripke Structure:** A Kripke structure is a 4-tuple $M = (S, S_0, R, L)$ where $S$ is a finite set of states, $S_0 \\subseteq S$ is the set of initial states, $R \\subseteq S \\times S$ is the transition relation, and $L: S \\rightarrow 2^{AP}$ is a labelling function mapping each state to the set of atomic propositions true in that state.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Classification of Fairness\n\nFairness is broadly classified into **three categories** in the context of temporal logic model checking:\n\n### 2.1.1 Unconditional (Impartial) Justice Fairness\n\nA path $\\pi = s_0, s_1, s_2, \\ldots$ is **unconditionally fair** for a set of transitions $T$ if every transition in $T$ is taken **infinitely often** along $\\pi$.\n\n$$ \\text{Unconditional Fairness}: \\quad \\bigwedge_{t \\in T} \\Box \\Diamond \\; t $$\n\nRead as: \"**always** ($\\Box$), **eventually** ($\\Diamond$), for every transition $t$.\"\n\n### 2.1.2 Strong Fairness (Compassion / Streett Fairness)\n\nA path is **strongly fair** with respect to a set of conditional transitions $T$ if every transition $t \\in T$ that is **continuously enabled** is eventually taken.\n\n$$ \\text{Strong Fairness}: \\quad \\bigwedge_{(p_i, t_i) \\in T} \\Box \\Diamond \\, p_i \\;\\Rightarrow\\; \\Box \\Diamond \\, t_i $$\n\nRead as: \"If a transition's guard $p_i$ holds **infinitely often**, then the transition $t_i$ itself occurs **infinitely often**.\"\n\n> [!IMPORTANT]\n> **Strong fairness** is **weaker** than unconditional fairness but **stronger** than weak fairness. It applies to transitions that may be conditionally enabled.\n\n### 2.1.3 Weak Fairness (Justice / Basic Fairness)\n\nA path is **weakly fair** if every transition that is **continuously enabled** from some point onwards is eventually taken.\n\n$$ \\text{Weak Fairness}: \\quad \\bigwedge_{(p_i, t_i) \\in T} \\Diamond \\Box \\, p_i \\;\\Rightarrow\\; \\Diamond \\Box \\, t_i $$\n\nRead as: \"If a transition's guard $p_i$ holds **always from some point**, then the transition $t_i$ itself holds **always from some point**.\"\n\n## 2.2 Comparison Matrix of Fairness Notions\n\n| Property | Unconditional Justice | Weak Fairness | Strong Fairness |\n|---|---|---|---|\n| LTL Formula Pattern | $\\Box \\Diamond t$ | $\\Box \\Diamond p \\Rightarrow \\Box \\Diamond t$ | $\\Diamond \\Box p \\Rightarrow \\Diamond \\Box t$ |\n| Strength | Strongest | Weakest | Intermediate |\n| When Applicable | Always-enabled transitions | Conditionally enabled transitions | Conditionally enabled transitions |\n| Expressiveness in LTL | Yes | Yes | Requires additional operators (not in pure LTL) |\n| Preserves CTL Model Checking Complexity | Yes (polynomial) | Yes (polynomial) | No (exponential in some cases) |\n\n## 2.3 Fairness in LTL and CTL\n\n### 2.3.1 Fair LTL\n\nIn **Linear Temporal Logic with Fairness**, an LTL formula $\\phi$ holds along a fair path $\\pi$:\n\n$$ M, \\pi \\models_f \\phi \\quad \\iff \\quad \\pi \\models \\mathcal{F} \\;\\Rightarrow\\; \\pi \\models \\phi $$\n\nThis means: the formula $\\phi$ must be satisfied **on all paths that satisfy the fairness condition** $\\mathcal{F}$.\n\n### 2.3.2 Fair CTL (CTL*)\n\nA CTL* formula with fairness is interpreted as:\n\n$$ M, s \\models_f \\phi \\quad \\iff \\quad \\text{For all fair paths } \\pi \\text{ starting at } s, \\; \\pi \\models \\phi $$\n\n$$ M, s \\models_f \\exists \\phi \\quad \\iff \\quad \\text{There exists a fair path } \\pi \\text{ starting at } s, \\; \\pi \\models \\phi $$\n\n> [!NOTE]\n> **Path Quantifier Restriction:** In fair CTL, both the universal ($\\forall$) and existential ($\\exists$) path quantifiers are restricted to **fair paths only**. This is a crucial modification from standard CTL semantics.\n\n## 2.4 KTU High-Yield Formula Sheet (Cheat Sheet)\n\n| # | Concept | LTL/CTL* Expression | Semantics |\n|---|---|---|---|\n| 1 | Unconditional Fairness | $\\Box \\Diamond t$ | Transition $t$ fires infinitely often |\n| 2 | Weak Fairness | $\\Box \\Diamond p \\rightarrow \\Box \\Diamond t$ | If guard holds infinitely often, action fires infinitely often |\n| 3 | Strong Fairness | $\\Diamond \\Box p \\rightarrow \\Diamond \\Box t$ | If guard always holds, action always fires |\n| 4 | Fair Path Definition | $\\pi \\in \\text{FairPaths}(M, \\mathcal{F})$ | Path satisfies all fairness constraints |\n| 5 | Fair LTL Satisfaction | $M \\models_f \\phi$ | $\\phi$ holds on all fair paths of $M$ |\n| 6 | Fair CTL State | $M, s \\models_f \\forall \\phi$ | $\\phi$ holds on all fair paths from $s$ |\n| 7 | Streett Condition | $\\bigwedge_i (\\Box \\Diamond p_i \\rightarrow \\Box \\Diamond q_i)$ | General form of strong fairness |\n| 8 | Rabin Condition | $\\bigvee_i (\\Box \\Diamond p_i \\wedge \\Diamond \\Box q_i)$ | Dual of Streett, used in automata theory |\n| 9 | Fair Emptiness Check | $L(\\mathcal{A}) \\cap \\text{FairPaths}(M) = \\emptyset$ | Product automaton has no accepting fair run |\n| 10 | State Explosion Mitigation | $\\vert S \\vert$ of product $= O(\\vert S_M \\vert \\cdot \\vert Q_{\\mathcal{A}} \\vert)$ | Complexity bound for fair model checking |\n\n> [!IMPORTANT]\n> **Engineering Utility:** Fairness conditions are routinely used in **hardware verification** (e.g., verifying bus arbitration protocols like AMBA AXI), **operating system kernels** (e.g., process schedulers), and **distributed consensus protocols** (e.g., Paxos, Raft) to ensure that liveness properties are not vacuously satisfied.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: From Standard CTL to Fair CTL Semantics\n\n### 3.1.1 Standard CTL Semantics (Recap)\n\nIn standard CTL, for a state $s$ in Kripke structure $M$:\n\n$$\n\\begin{aligned}\nM, s &\\models \\forall \\phi \\quad &\\iff& \\quad \\text{for all paths } \\pi \\text{ starting at } s, \\; \\pi \\models \\phi \\\\\nM, s &\\models \\exists \\phi \\quad &\\iff& \\quad \\text{there exists a path } \\pi \\text{ starting at } s, \\; \\pi \\models \\phi\n\\end{aligned}\n$$\n\n### 3.1.2 Restriction to Fair Paths\n\nTo incorporate fairness, we restrict the universal and existential quantifiers to operate **only over the set of fair paths** $FairPaths(M, \\mathcal{F})$:\n\n$$\n\\begin{aligned}\n\\text{FairPaths}(M, \\mathcal{F}) &= \\{ \\pi \\in \\text{Paths}(M) \\mid \\pi \\models \\mathcal{F} \\} \\\\\nM, s &\\models_f \\forall \\phi \\quad &\\iff& \\quad \\text{for all } \\pi \\in \\text{FairPaths}(M, \\mathcal{F}) \\text{ starting at } s, \\; \\pi \\models \\phi \\\\\nM, s &\\models_f \\exists \\phi \\quad &\\iff& \\quad \\text{there exists } \\pi \\in \\text{FairPaths}(M, \\mathcal{F}) \\text{ starting at } s, \\; \\pi \\models \\phi\n\\end{aligned}\n$$\n\n### 3.1.3 Transition from Standard to Fair: A Worked Example\n\nConsider verifying the liveness property \"**process P1 eventually enters its critical section**\" expressed as $\\forall \\Box \\Diamond \\, \\text{crit}_1$ on a 2-process mutual exclusion system $M$.\n\n**Step 1: Standard Model Checking**\n\n$$\nM \\models \\forall \\Box \\Diamond \\, \\text{crit}_1\n$$\n\nThe model checker returns **FALSE**, because there exists a path where process P2 is *always* scheduled, starving P1. This path is the **counterexample**.\n\n**Step 2: Identifying the Source of the Counterexample**\n\nThe path $\\pi_{unfair}$ has the form: $s_0 \\rightarrow s_1 \\rightarrow s_2 \\rightarrow \\ldots$ where at every state, the only enabled transition is one belonging to P2. The LTL property:\n\n$$\n\\begin{aligned}\n\\pi_{unfair} &\\not\\models \\Box \\Diamond \\, \\text{crit}_1 \\\\\n\\text{because } \\pi_{unfair} &\\models \\Box \\, \\text{not\\_crit}_1\n\\end{aligned}\n$$\n\n**Step 3: Adding a Fairness Constraint**\n\nWe define the fairness constraint $\\mathcal{F} = \\Box \\Diamond \\, \\text{enable}(P1)$. This says that **P1's enabling condition holds infinitely often**.\n\n**Step 4: Re-evaluating Under Fairness**\n\n$$\n\\begin{aligned}\nM &\\models_f \\forall \\Box \\Diamond \\, \\text{crit}_1 \\\\\n\\iff M, s_0 &\\models_f \\forall \\Box \\Diamond \\, \\text{crit}_1 \\\\\n\\iff \\text{for all } \\pi &\\in \\text{FairPaths}(M, \\mathcal{F}) \\text{ from } s_0, \\; \\pi \\models \\Box \\Diamond \\, \\text{crit}_1\n\\end{aligned}\n$$\n\nSince on every fair path, P1's transition is enabled infinitely often, and the system is not broken, P1 must eventually enter the critical section infinitely often. The model checker now returns **TRUE**.\n\n> [!NOTE]\n> **Key Insight:** Fairness assumptions convert **spurious counterexamples** (caused by unrealistic scheduling) into non-counterexamples. They are part of the **system model**, not the property.\n\n## 3.2 Derivation: The Product Automaton for Fair Model Checking\n\nWhen verifying fair LTL, the standard approach constructs a **product automaton**.\n\n$$\n\\begin{aligned}\nM \\times \\mathcal{A}_{\\neg \\phi} &= (S_M \\times Q_{\\mathcal{A}}, \\, S_{0,M} \\times Q_{0,\\mathcal{A}}, \\, R_{M \\times \\mathcal{A}}, \\, L_{M \\times \\mathcal{A}}) \\\\\n\\text{where } R_{M \\times \\mathcal{A}} &= \\{ ((s, q), (s', q')) \\mid (s, s') \\in R_M \\text{ and } q \\xrightarrow{L(s')} q' \\}\n\\end{aligned}\n$$\n\nThe verification reduces to a **fair emptiness check**:\n\n$$\n\\begin{aligned}\nM \\models_f \\phi \\quad &\\iff \\quad L(M \\times \\mathcal{A}_{\\neg \\phi}) \\cap \\text{FairPaths}(M \\times \\mathcal{A}_{\\neg \\phi}) = \\emptyset\n\\end{aligned}\n$$\n\n> [!IMPORTANT]\n> **State Space Bound:** The product automaton has size $O(\\vert S_M \\vert \\cdot 2^{\\vert \\phi \\vert})$, where $\\vert \\phi \\vert$ is the length of the LTL formula. Fairness is encoded via the **acceptance condition** of the Büchi automaton (e.g., Streett acceptance for strong fairness).\n\n## 3.3 Analytical Matrix: Three Worked Scenarios\n\n### Scenario 1: Unconditional Fairness Applied to a Token Ring\n\nA token ring has 4 nodes, and a token circulates. The fairness condition is:\n\n$$\n\\mathcal{F} = \\bigwedge_{i=1}^{4} \\Box \\Diamond \\, \\text{node}_i\\text{\\_has\\_token}\n$$\n\nThis is **unconditional justice** — every node receives the token infinitely often.\n\n### Scenario 2: Strong Fairness for a Condition Variable\n\nA producer waits on a condition variable $cv$. The transition $t_{produce}$ is enabled when the buffer is non-full. Strong fairness:\n\n$$\n\\mathcal{F}_{strong} = \\Box \\Diamond (\\text{buffer\\_not\\_full}) \\;\\rightarrow\\; \\Box \\Diamond \\, t_{produce}\n$$\n\n### Scenario 3: Weak Fairness for an Interrupt Handler\n\nAn interrupt handler $t_{handle}$ is enabled only when an interrupt is pending. Weak fairness:\n\n$$\n\\mathcal{F}_{weak} = \\Diamond \\Box \\, \\text{interrupt\\_pending} \\;\\rightarrow\\; \\Diamond \\Box \\, t_{handle}\n$$\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Architecture: Fairness in Model Checking Pipeline\n\nThe following **block-level functional architecture** shows how fairness integrates into a standard model checker.\n\n```mermaid\nflowchart TD\n    A[System Description TS] --> B[Kripke Structure M]\n    C[Property in LTL or CTL phi] --> D[Negation and Translation]\n    D --> E[Negated Buchi Automaton A]\n    F[Fairness Constraint F] --> G[Fair Path Filter]\n    B --> G\n    G --> H[Fair Product M x A]\n    E --> H\n    H --> I[Emptiness Check Algorithm]\n    I --> J{Accepting Fair Run Exists?}\n    J -- Yes --> K[VERIFICATION FAILS - Counterexample]\n    J -- No --> L[VERIFICATION SUCCESS - Property Holds]\n```\n\n## 4.2 Decision Flow: Choosing the Right Fairness Notion\n\n```mermaid\nflowchart TD\n    S[Start: Choose Fairness Notion] --> Q1{Is the transition always enabled?}\n    Q1 -- Yes --> U[Use Unconditional Justice: Box Diamond t]\n    Q1 -- No --> Q2{Does the transition need a condition that holds infinitely often?}\n    Q2 -- Yes --> SF[Use Strong Fairness: Box Diamond p then Box Diamond t]\n    Q2 -- No --> Q3{Does the condition hold continuously after some point?}\n    Q3 -- Yes --> WF[Use Weak Fairness: Diamond Box p then Diamond Box t]\n    Q3 -- No --> R[Re-examine the system model]\n```\n\n## 4.3 Conceptual Map: Fairness and Temporal Operators\n\n```mermaid\ngraph LR\n    F[Fairness Constraints] --> U[Unconditional Justice]\n    F --> W[Weak Fairness Justice]\n    F --> S[Strong Fairness Compassion]\n    U --> O1[Box Diamond t]\n    W --> O2[Box Diamond p implies Box Diamond t]\n    S --> O3[Diamond Box p implies Diamond Box t]\n    O1 --> R[Restricts Paths in Model]\n    O2 --> R\n    O3 --> R\n    R --> MV[Model Verification with Fair Paths]\n```\n\n## 4.4 Schematic: Spurious vs. Real Counterexample Resolution\n\n```mermaid\nsequenceDiagram\n    participant User\n    participant MC as Model Checker\n    participant Fair as Fairness Module\n    User->>MC: Verify M satisfies phi\n    MC->>MC: Explore all paths\n    MC-->>User: FAIL: Counterexample CE1 found\n    User->>Fair: Add Fairness Constraint F\n    Fair->>Fair: Filter out unfair paths\n    Fair-->>User: Re-verify with FairPaths\n    MC->>Fair: Check CE1 in FairPaths?\n    Fair-->>MC: CE1 is not in FairPaths (eliminated)\n    MC-->>User: PASS: Property holds on fair paths\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Question 1 `[KTU University Exam - Dec 2023]`\n\n**Define fairness in the context of model checking. Why is it needed?**\n\n**Model Answer (3 Marks):**\n\nFairness is a set of conditions imposed on a Kripke structure to restrict attention to execution paths where no component is starved or perpetually ignored. It eliminates **spurious counterexamples** that arise from unrealistic interleavings in concurrent systems. Fairness is needed because:\n1. **Without fairness**, a scheduler could ignore a process forever, making liveness properties falsely fail.\n2. **With fairness**, only realistic execution paths are considered, aligning verification with real-world system behavior.\n\nFairness is typically expressed as LTL constraints like $\\Box \\Diamond t$ (unconditional justice) or $\\Box \\Diamond p \\rightarrow \\Box \\Diamond t$ (strong fairness).\n\n---\n\n### Question 2 `[KTU University Exam - May 2024]`\n\n**Distinguish between weak fairness and strong fairness with LTL formulas.**\n\n**Model Answer (3 Marks):**\n\n| Aspect | Weak Fairness | Strong Fairness |\n|---|---|---|\n| LTL Formula | $\\Box \\Diamond p \\rightarrow \\Box \\Diamond t$ | $\\Diamond \\Box p \\rightarrow \\Diamond \\Box t$ |\n| Guard Condition | Holds infinitely often | Holds continuously from some point |\n| Strength | Weaker (easier to satisfy) | Stronger (harder to satisfy) |\n| Real-World Meaning | If repeatedly possible, must eventually happen | If persistently possible, must persistently happen |\n\nWeak fairness handles **intermittently enabled** transitions, while strong fairness handles **continuously enabled** ones.\n\n---\n\n## 5.2 Part B Questions (14 Marks Each)\n\n### Question A `[KTU University Exam - Dec 2023, Adapted]`\n\n**(a) [7 Marks]** Explain the three main types of fairness in model checking — **unconditional justice**, **weak fairness**, and **strong fairness** — with their LTL/CTL* representations. Give one real-world example of each.\n\n**(b) [7 Marks]** Consider a two-process mutual exclusion system with semaphore $S = 1$. Process P1 and P2 both have transitions $t_1$ and $t_2$ for entering the critical section. Formulate the fairness condition to ensure that **both processes get a fair chance to enter the critical section infinitely often**. Verify the liveness property $M \\models_f \\forall \\Box \\Diamond \\, \\text{crit}_1$.\n\n---\n\n#### Model Solution A(a):\n\n**Three Types of Fairness:**\n\n1. **Unconditional Justice (Impartial Fairness):**\n   A path is just if every transition in a set $T$ is taken infinitely often. The LTL formula:\n\n   $$ \\mathcal{F}_{UJ} = \\bigwedge_{t \\in T} \\Box \\Diamond \\, t $$\n\n   **Example:** A round-robin CPU scheduler ensuring every process gets CPU time infinitely often.\n\n2. **Weak Fairness (Justice):**\n   A path is weakly fair if every continuously-enabled transition is eventually taken. The LTL formula:\n\n   $$ \\mathcal{F}_{WF} = \\bigwedge_{(p_i, t_i) \\in T} \\Box \\Diamond \\, p_i \\;\\rightarrow\\; \\Box \\Diamond \\, t_i $$\n\n   **Example:** A printer queue where a print job, once submitted, eventually gets printed.\n\n3. **Strong Fairness (Compassion):**\n   A path is strongly fair if every transition that is enabled infinitely often is taken infinitely often. The LTL formula:\n\n   $$ \\mathcal{F}_{SF} = \\bigwedge_{(p_i, t_i) \\in T} \\Box \\Diamond \\, p_i \\;\\rightarrow\\; \\Box \\Diamond \\, t_i $$\n\n   **Example:** A network packet transmission where a channel, whenever it is free, eventually transmits the next packet.\n\n> [!WARNING]\n> **KTU Examiner's Pitfall (Part a):** Students often confuse the LTL formulas of weak and strong fairness. The key difference is the **guard's temporal pattern**: weak uses $\\Box \\Diamond$ (infinitely often) for the guard, while strong uses $\\Diamond \\Box$ (always from some point). Marks are deducted for mixing these up.\n\n---\n\n#### Model Solution A(b):\n\n**Step 1: Identify the Transitions and Guards**\n\nLet the guards be:\n- $p_1$: Process P1's request to enter the critical section is enabled.\n- $p_2$: Process P2's request to enter the critical section is enabled.\n- $t_1$: P1 enters the critical section.\n- $t_2$: P2 enters the critical section.\n\n**Step 2: Formulate the Fairness Condition**\n\nTo ensure both processes get a fair chance:\n\n$$ \\mathcal{F} = \\Box \\Diamond \\, t_1 \\;\\wedge\\; \\Box \\Diamond \\, t_2 $$\n\nThis is **unconditional justice** because both $t_1$ and $t_2$ are taken infinitely often.\n\n**Step 3: Express the Liveness Property**\n\nWe want to verify:\n\n$$ \\phi = \\forall \\Box \\Diamond \\, \\text{crit}_1 $$\n\nmeaning: \"On all paths, P1 is in the critical section infinitely often.\"\n\n**Step 4: Fair Model Checking**\n\nWe check:\n\n$$ M, s_0 \\models_f \\forall \\Box \\Diamond \\, \\text{crit}_1 $$\n\nUsing fair CTL semantics:\n\n$$\n\\begin{aligned}\nM, s_0 \\models_f \\forall \\Box \\Diamond \\, \\text{crit}_1 \\;&\\iff\\; \\text{for all } \\pi \\in \\text{FairPaths}(M, \\mathcal{F}) \\text{ from } s_0, \\; \\pi \\models \\Box \\Diamond \\, \\text{crit}_1\n\\end{aligned}\n$$\n\n**Step 5: Conclusion**\n\nOn any fair path, $t_1$ is taken infinitely often, and $t_1$ leads to $\\text{crit}_1$. Therefore:\n\n$$ M \\models_f \\forall \\Box \\Diamond \\, \\text{crit}_1 \\quad \\text{(TRUE)} $$\n\nThe system satisfies the liveness property under the fairness assumption.\n\n---\n\n### Question B `[KTU University Exam - May 2024, Adapted]`\n\n**(a) [7 Marks]** Define **fair CTL (CTL with fairness)** and explain how the path quantifiers $\\forall$ and $\\exists$ are interpreted under fairness assumptions. Provide the formal semantic rules.\n\n**(b) [7 Marks]** Construct the product automaton for fair model checking of an LTL property $\\phi$ against a Kripke structure $M$ under fairness $\\mathcal{F}$. Explain how the fair emptiness check works and state its time complexity.\n\n---\n\n#### Model Solution B(a):\n\n**Definition of Fair CTL:**\n\nFair CTL is an extension of standard CTL where the path quantifiers $\\forall$ and $\\exists$ range only over **fair paths** — paths that satisfy a given fairness constraint $\\mathcal{F}$.\n\n**Formal Semantic Rules:**\n\nFor state $s$ in Kripke structure $M$ with fairness constraint $\\mathcal{F}$:\n\n$$\n\\begin{aligned}\nM, s &\\models_f \\forall \\phi \\quad &\\iff& \\quad \\text{for all } \\pi \\in \\text{FairPaths}(M, \\mathcal{F}) \\text{ with } \\pi[0] = s, \\; \\pi \\models \\phi \\\\\nM, s &\\models_f \\exists \\phi \\quad &\\iff& \\quad \\text{there exists } \\pi \\in \\text{FairPaths}(M, \\mathcal{F}) \\text{ with } \\pi[0] = s, \\; \\pi \\models \\phi\n\\end{aligned}\n$$\n\nwhere:\n\n$$ \\text{FairPaths}(M, \\mathcal{F}) = \\{ \\pi = s_0 s_1 s_2 \\ldots \\in \\text{Paths}(M) \\mid \\pi \\models \\mathcal{F} \\} $$\n\n**Key Difference from Standard CTL:**\n\nIn standard CTL, $\\forall$ and $\\exists$ range over **all paths** in $M$. In fair CTL, they range only over **fair paths**. This restricts the verification to realistic execution behaviors.\n\n**Example:** A process scheduler has a state space of 10 states. Without fairness, the path \"P1 runs forever\" is valid. With fairness $\\mathcal{F} = \\Box \\Diamond \\, \\text{schedule}(P2)$, this path is excluded, and only paths where P2 gets scheduled infinitely often are considered.\n\n---\n\n#### Model Solution B(b):\n\n**Step 1: Translate LTL to Büchi Automaton**\n\nThe negated property $\\neg \\phi$ is translated into a Büchi automaton $\\mathcal{A}_{\\neg \\phi}$ with state set $Q_{\\mathcal{A}}$, transition relation $\\delta_{\\mathcal{A}}$, and acceptance condition $F_{\\mathcal{A}}$.\n\n**Step 2: Construct the Product Automaton**\n\n$$ M \\times \\mathcal{A}_{\\neg \\phi} = (S_M \\times Q_{\\mathcal{A}}, \\, R, \\, F) $$\n\nwhere:\n\n$$\n\\begin{aligned}\n((s, q), (s', q')) \\in R \\quad &\\iff \\quad (s, s') \\in R_M \\text{ and } q \\xrightarrow{L(s')} q' \\text{ in } \\mathcal{A}_{\\neg \\phi} \\\\\n\\text{Accepting states } F \\quad &= \\quad S_M \\times F_{\\mathcal{A}}\n\\end{aligned}\n$$\n\n**Step 3: Incorporate the Fairness Constraint**\n\nThe fairness $\\mathcal{F}$ is encoded as a set of **fair states** $F_{fair} \\subseteq S_M \\times Q_{\\mathcal{A}}$ in the product:\n\n$$ F_{fair} = \\{ (s, q) \\mid s \\models p_i \\text{ for some fairness clause } (\\Box \\Diamond p_i \\rightarrow \\Box \\Diamond t_i) \\in \\mathcal{F} \\} $$\n\n**Step 4: Fair Emptiness Check**\n\nWe check:\n\n$$ L(M \\times \\mathcal{A}_{\\neg \\phi}) \\cap \\text{FairPaths}(M \\times \\mathcal{A}_{\\neg \\phi}, F_{fair}) \\stackrel{?}{=} \\emptyset $$\n\n- If **non-empty**: there is a fair path in $M$ that satisfies $\\neg \\phi$ → **verification fails** with counterexample.\n- If **empty**: no fair path violates $\\phi$ → **verification succeeds**.\n\n**Step 5: Time Complexity**\n\nThe product automaton has size $O(\\vert S_M \\vert \\cdot \\vert Q_{\\mathcal{A}} \\vert)$. The emptiness check (nested DFS) runs in $O(\\vert S_M \\vert \\cdot \\vert Q_{\\mathcal{A}} \\vert)$.\n\n> [!WARNING]\n> **KTU Examiner's Pitfall (Part b):** Students frequently forget to:\n> 1. **Negate** $\\phi$ before constructing the Büchi automaton.\n> 2. Distinguish between the **Büchi acceptance condition** of the automaton and the **fairness condition** of the system — they are separate.\n> 3. State the **emptiness check** (not reachability) as the verification algorithm. Liveness properties need emptiness, not reachability.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n- **Fairness** restricts path quantifiers in CTL/LTL to **realistic execution paths** in concurrent systems.\n- **Unconditional Justice** uses $\\Box \\Diamond t$ — every transition fires infinitely often.\n- **Weak Fairness** uses $\\Box \\Diamond p \\rightarrow \\Box \\Diamond t$ — if guard holds infinitely often, transition fires infinitely often.\n- **Strong Fairness** uses $\\Diamond \\Box p \\rightarrow \\Diamond \\Box t$ — if guard holds always from some point, transition holds always from some point.\n- Fairness eliminates **spurious counterexamples** caused by unrealistic scheduler interleavings.\n- Fair CTL modifies both $\\forall$ and $\\exists$ to range only over **fair paths**.\n- Fair model checking reduces to a **fair emptiness check** on the product automaton $M \\times \\mathcal{A}_{\\neg \\phi}$.\n- Product automaton size is $O(\\vert S_M \\vert \\cdot 2^{\\vert \\phi \\vert})$; emptiness check is polynomial in this size.\n- Strong fairness is **expressible in CTL*** (using Streett acceptance) but **not in plain CTL**.\n- Liveness properties like $\\forall \\Box \\Diamond \\, \\phi$ are **only meaningful under fairness assumptions**; without fairness, they can be vacuously violated.\n- Common applications: **mutual exclusion protocols**, **CPU schedulers**, **bus arbiters**, **distributed consensus**, **hardware protocols**.\n- **Exam Tip:** When asked about fairness, always state (1) the LTL formula, (2) the type (weak/strong/unconditional), and (3) a real-world analogy.\n- **Pitfall:** Do not confuse the acceptance condition of the Büchi automaton (for $\\phi$) with the fairness condition of the system (for scheduling).\n- **Complexity:** Fair model checking is in **PSPACE** for LTL and **polynomial** for CTL with weak/unconditional fairness.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 3-t7",
          "title": "Safety and Liveness in LTL (Definitions only)",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 3\n> - **Topic:** Safety and Liveness in LTL (Definitions only)\n\n<!-- SECTION_1_START -->\n\n# Safety and Liveness in LTL — Core Technical Definition & Intuitive Overview\n\n## 1.1 Why Classify LTL Properties?\n\nIn **Linear Temporal Logic (LTL)** model checking, every property we wish to verify belongs to a vast universe of temporal behaviors. However, decades of research (notably by **Lamport (1977)** and **Alpern & Schneider (1985)**) have shown that almost every meaningful property can be cleanly partitioned into two orthogonal, complementary classes:\n\n1. **Safety Properties** — *\"Nothing bad ever happens.\"*\n2. **Liveness Properties** — *\"Something good eventually happens.\"*\n\n> [!IMPORTANT]\n> This dichotomy is foundational to **PECNT526 – Automated Verification**. Every LTL specification you will ever write in model checkers like **SPIN**, **NuSMV**, or **CADP** reduces to a Boolean combination of safety and liveness sub-properties.\n\n---\n\n## 1.2 Formal Definition — Safety Property\n\n> [!NOTE]\n> **Definition (Safety Property).** A language $L \\subseteq \\Sigma^\\omega$ is a **safety property** if and only if for every infinite word $\\sigma \\notin L$, there exists a finite prefix $x$ of $\\sigma$ such that:\n>\n> $$\\forall \\tau \\in \\Sigma^\\omega : \\quad x \\cdot \\tau \\notin L$$\n\nIn plain English: a property is **safe** if a violation can be detected by observing a **finite prefix** of the execution trace. The \"bad thing\" happens at a specific, identifiable moment.\n\n### Conceptual Analogy\n\n> Imagine a traffic signal controller. The safety property is:\n> *\"Green and Red lights are **never** on at the same time.\"*\n> The moment you observe a state where `Green = 1 ∧ Red = 1`, you have already witnessed a violation — no need to look at the future. A finite trace was enough to \"falsify\" the property.\n\n---\n\n## 1.3 Formal Definition — Liveness Property\n\n> [!NOTE]\n> **Definition (Liveness Property).** A language $L \\subseteq \\Sigma^\\omega$ is a **liveness property** if and only if for every finite word $x \\in \\Sigma^*$, there exists an infinite continuation $\\tau \\in \\Sigma^\\omega$ such that:\n>\n> $$x \\cdot \\tau \\in L$$\n\nIn plain English: a property is **live** if **no finite prefix** of a trace can ever prove that the property is violated. The \"good thing\" may happen arbitrarily far in the future — it cannot be refuted by any finite history.\n\n### Conceptual Analogy\n\n> Consider a print server. The liveness property is:\n> *\"Every submitted print job is **eventually** printed.\"*\n> No matter how long you watch the server queue, you can never conclude \"this job will never be printed\" — maybe the printer is just slow. A liveness violation is **inherently an infinite** phenomenon: it requires seeing the *entire* infinite execution (or lack thereof).\n\n---\n\n## 1.4 The Alpern–Schneider Decomposition Theorem\n\n> [!IMPORTANT]\n> **Theorem (Alpern & Schneider, 1985).** *Every* non-trivial property $P$ over a closed system can be expressed as the intersection of a safety property $S$ and a liveness property $L$:\n>\n> $$P = S \\cap L$$\n\nThis is the theoretical bedrock of the safety/liveness classification and explains why these two classes are sufficient for the entire universe of $\\omega$-regular properties verifiable by LTL model checkers.\n\n---\n\n## 1.5 Canonical Examples Table (Quick Reference)\n\n| Property Class | Informal Statement | LTL Formula Pattern | Detected By |\n|---|---|---|---|\n| **Safety** | Mutual exclusion holds | $\\Box \\neg (c_1 \\land c_2)$ | Finite prefix |\n| **Safety** | Partial correctness | $\\phi \\Rightarrow \\psi$ (Hoare-style) | Finite prefix |\n| **Safety** | No unreachable state used | $\\Box \\neg \\text{bad}$ | Finite prefix |\n| **Liveness** | Request is eventually granted | $\\Box(req \\Rightarrow \\Diamond grant)$ | Infinite trace |\n| **Liveness** | Process eventually terminates | $\\Diamond \\; terminated$ | Infinite trace |\n| **Liveness** | Starvation freedom | $\\Box \\Diamond \\; served$ | Infinite trace |\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n# Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Characteristic Logic — How to Identify Each Class\n\nWhen you are given an arbitrary LTL formula, you must be able to classify it quickly. Below is the **decision logic** KTU examiners expect you to follow.\n\n### 2.1.1 Safety Identification Checklist\n\nA formula $\\varphi$ is a **safety** formula if it can be rewritten in the form:\n\n$$\\varphi \\;\\equiv\\; \\Box \\psi \\quad \\text{where } \\psi \\text{ is a purely propositional (past-/state-based) formula}$$\n\nEquivalently, $\\varphi$ contains **only the operators** $\\Box, \\neg, \\land, \\lor$ — but **never** the operators $\\Diamond$ or $\\mathcal{U}$ (until) at the *outermost* temporal level.\n\n> [!TIP]\n> **Examiner Heuristic:** If the formula says \"**always** something holds\" and the inner formula has no future-time references, it is a **safety** property.\n\n### 2.1.2 Liveness Identification Checklist\n\nA formula $\\varphi$ is a **liveness** formula if it can be rewritten in a form containing at least one $\\Diamond$ or $\\mathcal{U}$ operator that is **not subsumable** under an outer $\\Box$.\n\nCanonical liveness skeletons:\n\n$$\n\\begin{aligned}\n\\varphi_1 &\\equiv \\Diamond\\, p \\\\\n\\varphi_2 &\\equiv \\Box(p \\Rightarrow \\Diamond q) \\\\\n\\varphi_3 &\\equiv (p \\,\\mathcal{U}\\, q)\n\\end{aligned}\n$$\n\n> [!TIP]\n> **Examiner Heuristic:** If the formula promises that \"**eventually** something will occur\" and the event is in the open future, it is a **liveness** property.\n\n---\n\n## 2.2 The Prefix-Closure Theorem (Safety Closure)\n\nEvery safety language $S$ can be equivalently characterized by its **prefix-closure**:\n\n$$\\text{Pref}(S) \\;=\\; \\left\\{ x \\in \\Sigma^* \\mid \\forall \\tau \\in \\Sigma^\\omega : x \\cdot \\tau \\in S \\right\\}$$\n\nThe set of *all* finite good prefixes completely determines $S$. This is why safety violations are **always reproducible** in finite test runs.\n\n---\n\n## 2.3 KTU Formula Sheet / Cheat Sheet\n\n> [!NOTE]\n> Memorize this table verbatim — it is the single most testable artifact in this topic.\n\n| # | Concept | Formal Statement | Intuition | KTU Weight |\n|---|---|---|---|---|\n| 1 | Safety — Definition | $\\forall \\sigma \\notin L, \\exists x \\prec \\sigma : \\forall \\tau,\\; x\\tau \\notin L$ | Finite prefix refutes | **High** |\n| 2 | Liveness — Definition | $\\forall x \\in \\Sigma^*, \\exists \\tau \\in \\Sigma^\\omega : x\\tau \\in L$ | No finite prefix refutes | **High** |\n| 3 | Alpern–Schneider | $P = \\text{Safety}(P) \\cap \\text{Liveness}(P)$ | Every property decomposes | **High** |\n| 4 | Safety Skeleton | $\\varphi \\equiv \\Box \\psi$ | \"Always $\\psi$\" | **Medium** |\n| 5 | Liveness Skeleton | $\\varphi \\equiv \\Diamond \\psi$ or $\\Box(p \\Rightarrow \\Diamond q)$ | \"Eventually $\\psi$\" | **Medium** |\n| 6 | Detection Bound | Safety: finite / Liveness: infinite | Witnesses differ in nature | **Medium** |\n| 7 | Counter-example Type | Safety: finite path / Liveness: lasso / cycle | SPIN/NuSMV output shape | **High** |\n| 8 | Büchi Acceptance | Every LTL property is a Büchi-$\\omega$-regular language | Underpinning math | **Low** |\n| 9 | Persistence vs. Reactivity | Safety ↔ Persistence / Liveness ↔ Reactivity (Manna-Pnueli) | Alternate naming | **Medium** |\n| 10 | Closure under $\\neg$ | $\\neg(\\text{Safety}) \\equiv \\text{Liveness}$ and vice-versa (duality) | Useful for proofs | **Medium** |\n\n---\n\n## 2.4 Real-World Engineering Utility\n\n| Domain | Safety Property Used | Liveness Property Used |\n|---|---|---|\n| **Aerospace Flight Control** | No two control surfaces commanded to extreme opposite deflections simultaneously | If a fault is detected, the backup system eventually engages |\n| **Railway Signalling (CBTC)** | Two trains never occupy the same block (interlocking) | Every train request eventually receives a movement authority |\n| **Banking Ledger (DB)** | No account balance goes below zero (no overdraft) | Every pending transaction is eventually committed or aborted |\n| **Industrial PLC** | Emergency stop always overrides auto mode | Every queued job is eventually processed by the robot arm |\n| **Network Protocols (TCP)** | Sender never sends into a closed window | Every retransmitted packet is eventually acknowledged |\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n# Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation 1 — Proving a Given LTL Formula is a Safety Property\n\nWe work through the **canonical proof technique** for showing that a formula is safe.\n\n**Claim.** $\\varphi \\equiv \\Box(\\neg p)$ is a safety property.\n\n**Step 1.** Restate $\\varphi$ in the canonical safety skeleton form.\n\n$$\n\\begin{aligned}\n\\varphi &\\equiv \\Box(\\neg p) \\\\\n\\end{aligned}\n$$\n\nWe identify $\\psi \\equiv \\neg p$, which contains **no temporal operators** — only propositional negation. Therefore $\\varphi$ is in the form $\\Box \\psi$.\n\n**Step 2.** Construct the violating prefix.\n\nSuppose an infinite trace $\\sigma = s_0, s_1, s_2, \\dots$ violates $\\varphi$. Then there exists some position $i \\in \\mathbb{N}$ such that:\n\n$$\n\\begin{aligned}\n\\sigma, i &\\not\\models \\Box(\\neg p) \\\\\n\\Rightarrow \\quad \\sigma, i &\\models \\neg(\\neg p) \\\\\n\\Rightarrow \\quad \\sigma, i &\\models p\n\\end{aligned}\n$$\n\nThe violating prefix is the finite word $x = s_0, s_1, \\dots, s_i$. Every infinite continuation $x \\cdot \\tau$ of this prefix still contains the offending state $s_i$ at position $i$, so $p$ remains true, and $\\Box(\\neg p)$ remains false for all continuations.\n\n**Step 3.** Conclude via the safety definition.\n\nWe have shown that for every $\\sigma \\notin L_\\varphi$, a finite prefix $x$ exists with the required refutation property. Therefore $L_\\varphi$ is a safety language. $\\blacksquare$\n\n---\n\n## 3.2 Derivation 2 — Proving a Given LTL Formula is a Liveness Property\n\n**Claim.** $\\varphi \\equiv (p \\,\\mathcal{U}\\, q)$ (strong until) is a liveness property.\n\n**Step 1.** Verify the liveness structure.\n\nThe formula $\\varphi$ contains the $\\mathcal{U}$ operator at the top level, which is **not** dominated by an outer $\\Box$. Hence it is a candidate liveness formula.\n\n**Step 2.** Show no finite prefix can refute it.\n\nLet $x = s_0, s_1, \\dots, s_n$ be **any** finite trace. We must exhibit an infinite continuation $\\tau$ such that $x \\cdot \\tau \\models (p \\,\\mathcal{U}\\, q)$.\n\nConstruct $\\tau$ as follows:\n\n$$\n\\begin{aligned}\n\\tau &\\equiv s_n, s_{n+1}, s_{n+2}, \\dots \\\\\n&\\text{where} \\quad s_{n+k} \\models q \\quad \\text{for } k = 0\n\\end{aligned}\n$$\n\nThat is, we make the next state after $x$ satisfy $q$ and then extend arbitrarily. In this continuation, $q$ holds immediately after the prefix, so $(p \\,\\mathcal{U}\\, q)$ is satisfied along $x \\cdot \\tau$.\n\n**Step 3.** Universality over all finite prefixes.\n\nBecause the construction in Step 2 works for **every** finite $x \\in \\Sigma^*$, the liveness definition is satisfied:\n\n$$\n\\begin{aligned}\n\\forall x \\in \\Sigma^*, \\exists \\tau \\in \\Sigma^\\omega : x \\cdot \\tau \\in L_\\varphi \\quad \\checkmark\n\\end{aligned}\n$$\n\nHence $L_\\varphi$ is a liveness language. $\\blacksquare$\n\n---\n\n## 3.3 Analytical Matrix — Classifying Common LTL Patterns\n\nThe following table is the **single most important reference** for answering KTU Part A classification questions. The student must be able to fill column 2 and column 3 from memory.\n\n| LTL Formula $\\varphi$ | Class | Reasoning (Brief) |\n|---|---|---|\n| $\\Box(p \\land q)$ | **Safety** | Pure $\\Box$ over propositional $\\psi$ |\n| $\\Box \\Diamond p$ | **Liveness** | $\\Diamond$ not subsumable under $\\Box$ — strong fairness flavor |\n| $\\Diamond \\Box p$ | **Liveness** | Eventual stability, cannot be refuted finitely |\n| $\\Box(p \\Rightarrow \\Diamond q)$ | **Liveness** | Standard *response* pattern |\n| $\\Diamond(p \\lor q)$ | **Liveness** | Pure $\\Diamond$ |\n| $\\Box \\Diamond p \\lor \\Box \\Diamond q$ | **Liveness** | Disjunction of liveness is liveness |\n| $\\Box p \\lor \\Diamond q$ | **Mixed** (decomposes) | Has both safety and liveness components |\n| $(p \\,\\mathcal{U}\\, q)$ | **Liveness** | Until is intrinsically liveness |\n| $\\Box(\\neg(p \\land q))$ | **Safety** | Mutual-exclusion style |\n| $\\Diamond (p \\land q)$ | **Liveness** | Co-occurrence in future |\n| $\\Box p \\Rightarrow \\Diamond q$ | **Mixed** (decomposes) | Antecedent is safety, consequent is liveness |\n| $\\neg \\Box \\neg p$ (i.e. $\\Diamond p$) | **Liveness** | Duality of $\\Box$ |\n\n---\n\n## 3.4 Derivation 3 — Alpern–Schneider Decomposition of a Mixed Property\n\n**Problem.** Decompose the property *\"Every request is eventually granted, and no two requests are processed simultaneously\"* into safety and liveness components.\n\n**Step 1.** Write the mixed property in LTL.\n\n$$\n\\begin{aligned}\nP &\\equiv \\Box(req \\Rightarrow \\Diamond grant) \\;\\land\\; \\Box \\neg(p_1 \\land p_2) \\\\\n\\end{aligned}\n$$\n\n**Step 2.** Identify the safety component $S$.\n\nThe sub-formula $\\Box \\neg(p_1 \\land p_2)$ depends only on state propositions and is universally quantifed over time. It is a pure safety property:\n\n$$\n\\begin{aligned}\nS &\\equiv \\Box \\neg(p_1 \\land p_2)\n\\end{aligned}\n$$\n\n**Step 3.** Identify the liveness component $L$.\n\nThe sub-formula $\\Box(req \\Rightarrow \\Diamond grant)$ is the textbook *response* pattern — it cannot be refuted by any finite prefix, because a pending request may still be granted in the future:\n\n$$\n\\begin{aligned}\nL &\\equiv \\Box(req \\Rightarrow \\Diamond grant)\n\\end{aligned}\n$$\n\n**Step 4.** Verify the decomposition.\n\nBy the Alpern–Schneider theorem, $P = S \\cap L$. We have partitioned $P$ into a safety and a liveness factor — verification can now be performed by two independent algorithms: a **reachability** check for $S$ and a **cycle (fairness)** check for $L$. $\\blacksquare$\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n# Structural Diagrams & Schematics\n\n## 4.1 The Safety–Liveness Universe (Conceptual Map)\n\nThe diagram below shows how the universe of all LTL properties partitions into the two Alpern–Schneider classes, with examples mapped into each region.\n\n```mermaid\nflowchart TB\n    Start([All LTL Properties P]) --> Decomp{Alpern-Schneider<br/>Decomposition}\n    Decomp --> SafetyRegion[Safety Properties S]\n    Decomp --> LivenessRegion[Liveness Properties L]\n    Decomp --> MixedRegion[Mixed Properties S ∩ L]\n\n    SafetyRegion --> S1[Box negation p<br/>Mutual Exclusion]\n    SafetyRegion --> S2[Box p land q<br/>Invariant]\n    SafetyRegion --> S3[Box not deadlock<br/>No Deadlock]\n\n    LivenessRegion --> L1[Diamond p<br/>Reachability]\n    LivenessRegion --> L2[Box req implies Diamond grant<br/>Response]\n    LivenessRegion --> L3[Box Diamond served<br/>Starvation Freedom]\n\n    MixedRegion --> M1[Box req implies Diamond grant<br/>AND Box not bad<br/>= Full Spec]\n\n    style Start fill:#1f3a5f,color:#ffffff\n    style Decomp fill:#2e7d32,color:#ffffff\n    style SafetyRegion fill:#1565c0,color:#ffffff\n    style LivenessRegion fill:#c62828,color:#ffffff\n    style MixedRegion fill:#6a1b9a,color:#ffffff\n    style S1 fill:#0d47a1,color:#ffffff\n    style S2 fill:#0d47a1,color:#ffffff\n    style S3 fill:#0d47a1,color:#ffffff\n    style L1 fill:#b71c1c,color:#ffffff\n    style L2 fill:#b71c1c,color:#ffffff\n    style L3 fill:#b71c1c,color:#ffffff\n    style M1 fill:#4a148c,color:#ffffff\n```\n\n---\n\n## 4.2 Detection Mechanism — Finite Prefix vs. Infinite Trace\n\nThe diagram below illustrates the **fundamental asymmetry** in how safety and liveness violations are witnessed.\n\n```mermaid\nflowchart LR\n    A[Infinite Trace sigma] --> B{Type of Property}\n    B -->|Safety| C[Check Finite Prefix x]\n    B -->|Liveness| D[Need Entire Trace sigma]\n\n    C --> E{Finite prefix<br/>satisfies property?}\n    E -->|NO| F[VIOLATION DETECTED<br/>at position i]\n    E -->|YES| G[Continue - may still hold]\n\n    D --> H{Does good event<br/>occur eventually?}\n    H -->|YES| G\n    H -->|NO - infinite wait| I[VIOLATION DETECTED<br/>via lasso / cycle]\n\n    style A fill:#37474f,color:#ffffff\n    style B fill:#f57c00,color:#000000\n    style C fill:#1565c0,color:#ffffff\n    style D fill:#c62828,color:#ffffff\n    style F fill:#b71c1c,color:#ffffff\n    style I fill:#b71c1c,color:#ffffff\n    style G fill:#2e7d32,color:#ffffff\n    style E fill:#0288d1,color:#ffffff\n    style H fill:#e64a19,color:#ffffff\n```\n\n---\n\n## 4.3 Verification Algorithm Flow (Block-Level Architecture)\n\nThis block diagram shows how a model checker (SPIN, NuSMV) processes a property after classification.\n\n```mermaid\nflowchart TB\n    P[Input LTL Property phi] --> Cls{Classify phi}\n    Cls -->|Safety Box psi| SAF[Reachability Check<br/>on negation psi]\n    Cls -->|Liveness Diamond| LIV[Cycle Detection<br/>on Buchi Automaton]\n    Cls -->|Mixed| DEC[Decompose phi = S ∩ L]\n    DEC --> SAF\n    DEC --> LIV\n\n    SAF --> SAFR{Finite counterexample<br/>found?}\n    SAFR -->|Yes| FAIL1[OUTPUT: Safety Violation<br/>Finite Path]\n    SAFR -->|No| OK1[SAFETY HOLDS]\n\n    LIV --> LIVR{Infinite counterexample<br/>lasso found?}\n    LIVR -->|Yes| FAIL2[OUTPUT: Liveness Violation<br/>Accepting Cycle]\n    LIVR -->|No| OK2[LIVENESS HOLDS]\n\n    OK1 --> DONE[VERIFIED]\n    OK2 --> DONE\n    FAIL1 --> DONE\n    FAIL2 --> DONE\n\n    style P fill:#37474f,color:#ffffff\n    style Cls fill:#f57c00,color:#000000\n    style SAF fill:#1565c0,color:#ffffff\n    style LIV fill:#c62828,color:#ffffff\n    style DEC fill:#6a1b9a,color:#ffffff\n    style SAFR fill:#0288d1,color:#ffffff\n    style LIVR fill:#e64a19,color:#ffffff\n    style FAIL1 fill:#b71c1c,color:#ffffff\n    style FAIL2 fill:#b71c1c,color:#ffffff\n    style OK1 fill:#2e7d32,color:#ffffff\n    style OK2 fill:#2e7d32,color:#ffffff\n    style DONE fill:#1b5e20,color:#ffffff\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n# KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **Question 1.** **[KTU University Exam – May 2024, Model Paper PECNT526]**\n> Define **safety property** in the context of LTL model checking. Give one example.\n\n**Model Answer:**\n\nA property $L \\subseteq \\Sigma^\\omega$ is a **safety property** if for every infinite word $\\sigma \\notin L$, there exists a finite prefix $x$ of $\\sigma$ such that for all infinite continuations $\\tau \\in \\Sigma^\\omega$, the word $x \\cdot \\tau \\notin L$. In other words, a violation of the property can be detected from a finite prefix of the execution trace.\n\n**Example:** Mutual exclusion between two processes, expressed in LTL as $\\Box \\neg (c_1 \\land c_2)$. The moment a state is observed where both $c_1$ and $c_2$ are true, the property is violated — a finite prefix suffices.\n\n**[3 marks — definition 2 marks, example 1 mark]**\n\n---\n\n> **Question 2.** **[KTU University Exam – Dec 2023, PECNT526]**\n> What is a **liveness property**? How does it differ from a safety property in terms of the prefix structure?\n\n**Model Answer:**\n\nA property $L \\subseteq \\Sigma^\\omega$ is a **liveness property** if for **every** finite word $x \\in \\Sigma^*$, there exists an infinite continuation $\\tau \\in \\Sigma^\\omega$ such that $x \\cdot \\tau \\in L$. This means that no finite prefix of any trace can ever refute the property.\n\nThe structural difference is as follows:\n\n$$\n\\begin{aligned}\n\\text{Safety} &: \\forall \\sigma \\notin L,\\; \\exists x \\prec \\sigma : \\forall \\tau,\\; x\\tau \\notin L \\\\\n\\text{Liveness} &: \\forall x \\in \\Sigma^*,\\; \\exists \\tau : x\\tau \\in L\n\\end{aligned}\n$$\n\nSafety violations are **finitely witnessed**; liveness violations require an **infinite** (or cyclic) witness.\n\n**Example of a liveness property:** $\\Box(req \\Rightarrow \\Diamond grant)$ — every request is eventually granted. A pending request may be granted arbitrarily far in the future.\n\n**[3 marks — definition 1.5 marks, contrast 1 mark, example 0.5 marks]**\n\n---\n\n## 5.2 Part B — Choice-Based Long Answer Questions (14 Marks Each)\n\n> **Question A.** **[KTU University Exam – PECNT526, Module 3 Typical Pattern]**\n>\n> **(a) [7 marks]** State and explain the formal definitions of **safety** and **liveness** properties in LTL. Use the Alpern–Schneider decomposition theorem to show that any property can be written as an intersection of a safety and a liveness component.\n>\n> **(b) [7 marks]** For each of the following LTL formulas, classify it as a **safety**, **liveness**, or **mixed** property. Justify each classification with a one-line reasoning:\n>\n> 1. $\\Box \\neg (c_1 \\land c_2)$\n> 2. $\\Diamond\\, terminated$\n> 3. $\\Box(req \\Rightarrow \\Diamond grant)$\n> 4. $\\Box(\\text{no deadlock}) \\;\\land\\; \\Diamond(\\text{goal reached})$\n\n### Model Solution — Question A\n\n#### Part (a) — Definitions and Decomposition\n\n**Safety definition (3 marks):**\n\nA language $L \\subseteq \\Sigma^\\omega$ is a safety property iff:\n\n$$\n\\forall \\sigma \\in \\Sigma^\\omega : \\sigma \\notin L \\;\\Rightarrow\\; \\exists x \\in \\Sigma^* : x \\prec \\sigma \\;\\land\\; \\forall \\tau \\in \\Sigma^\\omega : x \\cdot \\tau \\notin L\n$$\n\nLiveness definition (3 marks):\n\nA language $L \\subseteq \\Sigma^\\omega$ is a liveness property iff:\n\n$$\n\\forall x \\in \\Sigma^* : \\exists \\tau \\in \\Sigma^\\omega : x \\cdot \\tau \\in L\n$$\n\n**Alpern–Schneider Decomposition (1 mark):**\n\nFor any property $P$, define:\n- $\\text{Safe}(P) = \\{ \\sigma \\mid \\forall \\sigma' \\prec \\sigma : \\sigma' \\in \\text{Pref}(P) \\}$\n- $\\text{Live}(P) = P \\cup (\\Sigma^\\omega \\setminus P) \\cap \\text{closure}(P)$\n\nThen $P = \\text{Safe}(P) \\cap \\text{Live}(P)$.\n\n#### Part (b) — Classification (7 marks)\n\n| # | Formula | Class | Justification |\n|---|---|---|---|\n| 1 | $\\Box \\neg (c_1 \\land c_2)$ | **Safety** | Pure invariant — violation finitely witnessed when both critical sections entered |\n| 2 | $\\Diamond\\, terminated$ | **Liveness** | Pure reachability — no finite prefix can prove non-termination |\n| 3 | $\\Box(req \\Rightarrow \\Diamond grant)$ | **Liveness** | Standard *response* pattern — request may be granted arbitrarily far ahead |\n| 4 | $\\Box(\\text{no deadlock}) \\;\\land\\; \\Diamond(\\text{goal reached})$ | **Mixed** | Conjunction of a safety ($\\Box$…) and a liveness ($\\Diamond$…) component |\n\n**[Marks: 1.5 + 1.5 + 1.5 + 1.5 + 1 mark for the conjunction reasoning = 7]**\n\n---\n\n> **Question B.** **[KTU University Exam – PECNT526, Module 3 Alternate Choice]**\n>\n> **(a) [7 marks]** Define a **safety property** formally. Explain, with a real-world analogy, why safety properties are said to be \"finitely refutable\". Provide two examples from distinct engineering domains.\n>\n> **(b) [7 marks]** Define a **liveness property** formally. Explain why the counter-example generated by a model checker for a liveness violation is structurally different from that of a safety violation. Illustrate with a concrete trace.\n\n### Model Solution — Question B\n\n#### Part (a) — Safety: Definition + Analogy (7 marks)\n\n**Definition (3 marks):** A property $L \\subseteq \\Sigma^\\omega$ is safe iff $\\forall \\sigma \\notin L$, $\\exists x \\prec \\sigma$ such that $\\forall \\tau$, $x \\cdot \\tau \\notin L$.\n\n**Real-world analogy (2 marks):** Consider a **kitchen pressure cooker** with safety valve monitoring. The safety property is: *\"The pressure never exceeds the safety threshold.\"* The instant the gauge reads above the threshold — even a single sample — the violation is conclusive. You do not need to wait and see what happens next. The \"bad event\" is observed at a *specific* finite instant.\n\n**Two examples (2 marks):**\n1. *Avionics:* $\\Box \\neg(\\text{autopilot engaged} \\land \\text{manual override active})$ — incompatible flight modes never co-occur.\n2. *Database:* $\\Box(\\text{balance} \\geq 0)$ — no account ever goes overdrawn.\n\n#### Part (b) — Liveness: Definition + Counter-example Structure (7 marks)\n\n**Definition (3 marks):** A property $L \\subseteq \\Sigma^\\omega$ is live iff $\\forall x \\in \\Sigma^*$, $\\exists \\tau$ such that $x \\cdot \\tau \\in L$.\n\n**Structural difference of counter-examples (3 marks):**\n\n- A **safety** counter-example is a **finite** path from an initial state to a state where the invariant is broken. It terminates at the point of violation.\n- A **liveness** counter-example is an **infinite** path that, in practice, is represented as a **lasso (prefix + accepting cycle)**. The path reaches a \"good event\" candidate state but then **loops forever** without ever satisfying the event.\n\n**Concrete trace illustration (1 mark):** For $\\Box(req \\Rightarrow \\Diamond grant)$, suppose:\n- Finite prefix: $s_0 \\to s_1 \\to s_2$ where at $s_2$, $req$ becomes true.\n- Accepting cycle: $s_2 \\to s_3 \\to s_4 \\to s_2$ — the cycle repeats with $req$ continuously true but $grant$ never true.\n\nThe lasso $s_0 s_1 (s_2 s_3 s_4)^\\omega$ is the model checker's witness of liveness violation.\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mark-Loss Pitfalls in PECNT526 — Safety & Liveness Definitions:**\n>\n> 1. **Swapping the quantifiers (–2 marks):** Students frequently write the liveness condition as $\\forall \\tau, \\exists x$ — this is the **wrong order**. The correct order is $\\forall x, \\exists \\tau$. Mixing the quantifiers makes the property *co*-liveness or a *safety* property.\n>\n> 2. **Confusing $\\Box \\Diamond$ with safety (–1 mark):** A formula like $\\Box \\Diamond p$ is **liveness**, not safety, because the inner $\\Diamond p$ cannot be verified by a finite observation. Do not blindly classify based on the leading $\\Box$.\n>\n> 3. **Omitting the word \"prefix\" in the safety definition (–1 mark):** The KTU 2024 marking scheme explicitly awards the \"finite prefix\" clause separately. Writing only \"a trace\" instead of \"a finite prefix of the trace\" loses 1 full mark.\n>\n> 4. **Claiming every property is purely safety OR purely liveness (–1 mark):** Many KTU questions are deliberately mixed. Always check for **conjunctions** of safety and liveness sub-formulas.\n>\n> 5. **Forgetting to give an example (–0.5 to –1 mark):** Definitions alone are insufficient; the KTU valuation key requires at least one **illustrative example** for full marks on a 3-mark Part A question.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid-Revision Checklist for PECNT526 — Module 3, Safety & Liveness (Definitions only)**\n\n- [ ] **Safety** = \"**Nothing bad ever happens**\" — finitely refutable by a finite prefix of a violating trace.\n- [ ] **Liveness** = \"**Something good eventually happens**\" — no finite prefix can refute it; requires infinite (lasso) witness.\n- [ ] Formal safety: $\\forall \\sigma \\notin L, \\exists x \\prec \\sigma : \\forall \\tau,\\; x\\tau \\notin L$.\n- [ ] Formal liveness: $\\forall x \\in \\Sigma^*, \\exists \\tau \\in \\Sigma^\\omega : x\\tau \\in L$.\n- [ ] **Alpern–Schneider Theorem:** Every $\\omega$-regular property $P = \\text{Safety}(P) \\cap \\text{Liveness}(P)$.\n- [ ] **Safety skeleton:** $\\Box \\psi$ where $\\psi$ is propositional / past-only.\n- [ ] **Liveness skeleton:** any formula containing a non-subsumable $\\Diamond$ or $\\mathcal{U}$.\n- [ ] **Counter-example shapes differ:** Safety → finite path. Liveness → lasso (prefix + accepting cycle).\n- [ ] **Canonical safety examples:** mutual exclusion, no-overdraft, no-deadlock, partial correctness.\n- [ ] **Canonical liveness examples:** eventual response, termination, starvation freedom, strong fairness ($\\Box \\Diamond p$).\n- [ ] **Duality:** $\\neg(\\text{Safety}) \\equiv \\text{Liveness}$; useful for proof manipulations.\n- [ ] **Alternate names:** Persistence (Manna–Pnueli) = Safety; Reactivity (Manna–Pnueli) = Liveness.\n- [ ] **Büchi automaton connection:** Both safety and liveness properties are accepted by Büchi automata — the differing structure of these automata is what makes their verification algorithms differ.\n- [ ] **Engineering utility:** Aerospace, railway, banking, PLC, and network protocols all rely on this dichotomy for certification arguments (e.g., DO-178C, EN 50128, IEC 61508).\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 3-t8",
          "title": "Automata Based LTL Model Checking (Algorithms and examples only)",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 3\n> - **Topic:** Automata Based LTL Model Checking (Algorithms and examples only)\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition of Automata-Based LTL Model Checking\n\n**Automata-Based LTL Model Checking** is the procedure of verifying whether a concurrent reactive system, modeled as a state-transition graph (Kripke structure $M$), satisfies a temporal-logic specification given as a **Linear Temporal Logic (LTL)** formula $\\varphi$, by reducing the satisfiability problem to a graph-theoretic search over **Büchi automata** and the **product automaton** of $M$ with the negation of $\\varphi$.\n\nFormally, the model-checking problem is stated as:\n\n$$M, s_0 \\models \\varphi \\iff \\mathcal{L}(M) \\subseteq \\mathcal{L}(\\varphi)$$\n\nEquivalently, the dual formulation is used algorithmically:\n\n$$M, s_0 \\models \\varphi \\iff \\mathcal{L}(M) \\cap \\mathcal{L}(\\neg\\varphi) = \\emptyset$$\n\nwhere:\n- $M = (S, S_0, R, L)$ is a Kripke structure with state set $S$, initial states $S_0$, transition relation $R$, and labeling $L$.\n- $\\mathcal{L}(M)$ is the set of all infinite paths (traces) of $M$.\n- $\\mathcal{L}(\\varphi)$ is the language accepted by a Büchi automaton built from $\\varphi$.\n- $\\neg\\varphi$ is the LTL negation of the specification.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Highlight:** This module focuses strictly on *algorithms* and *worked-out examples*. The construction of the Büchi automaton from an LTL formula is assumed known (covered in Module 2); the focus here is the **product construction**, **nested DFS**, and **counterexample extraction**.\n\n### 1.2 The Two Standard Algorithm Families\n\n| Algorithm Family | Core Idea | Typical Tool | Reference |\n|---|---|---|---|\n| **Global Model Checking** | Compute $\\text{Sat}(\\varphi)$ sets iteratively on $M$ using fixpoints | SPIN, NuSMV (tableau) | Clarke/Emerson |\n| **Automata-Based (Local)** | Build NBW for $\\neg\\varphi$, take product with $M$, search for an accepting cycle | SPIN's `never claim` | Vardi/Wolper |\n\nThe KTU Module 3 syllabus targets the **automata-based local** approach.\n\n### 1.3 Conceptual Analogy / Intuition\n\n> [!NOTE]\n> **The \"Detective Trail\" Analogy**\n> Imagine the Kripke structure $M$ as the floor plan of a hotel with infinite corridors, and every state is a room the system can be in. The specification $\\varphi$ is a *house rule* (\"the light must turn on after the door opens\"). To check the rule, the detective does not search the whole hotel. Instead, he translates the *violation* of the rule ($\\neg\\varphi$) into a **criminal profile** — a Büchi automaton that represents exactly those behaviours that *break* the rule. He then plays the profile against the floor plan (forming a **product automaton**) and asks: \"Is there a walk through the hotel that matches the criminal profile forever?\" If yes, the rule is broken and a counterexample is captured as the witness path. If no, the system is **safe**.\n\n### 1.4 Central Objects Recap (5-tuple for NBW)\n\nA **Nondeterministic Büchi Word Automaton (NBW)** is $A = (\\Sigma, Q, Q_0, \\delta, F)$ where:\n- $\\Sigma$ = alphabet (here, $2^{AP}$, the power set of atomic propositions).\n- $Q$ = finite set of states.\n- $Q_0 \\subseteq Q$ = initial states.\n- $\\delta : Q \\times \\Sigma \\to 2^{Q}$ = transition function.\n- $F \\subseteq Q$ = accepting states (a run is *accepting* iff it visits $F$ infinitely often).\n\n> [!IMPORTANT]\n> In automata-based LTL model checking, every LTL formula $\\varphi$ is converted into an NBW $A_{\\neg\\varphi}$ whose language equals $\\mathcal{L}(\\neg\\varphi)$ (the set of infinite words violating $\\varphi$). The model $M$ is itself an NBW accepting $\\mathcal{L}(M)$. The product is then a generalized Büchi automaton reduced to NBW for cycle search.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Algorithmic Pipeline (End-to-End)\n\nThe full procedure follows four logically ordered stages:\n\n1. **Stage 1 — Build $A_{\\neg\\varphi}$**: Convert the negated LTL formula $\\neg\\varphi$ into an NBW $A_{\\neg\\varphi}$ such that $\\mathcal{L}(A_{\\neg\\varphi}) = \\mathcal{L}(\\neg\\varphi)$. (Done by tableau/Giannakolaou–Bowman construction; treated as input in this module.)\n2. **Stage 2 — Build the product automaton** $M \\otimes A_{\\neg\\varphi}$: An NBW whose accepted language is $\\mathcal{L}(M) \\cap \\mathcal{L}(A_{\\neg\\varphi})$.\n3. **Stage 3 — Search for an accepting cycle**: Find a reachable accepting cycle in the product. If none exists, $M \\models \\varphi$ holds; otherwise the cycle yields a counterexample.\n4. **Stage 4 — Project back**: Project the accepting cycle onto $M$ to obtain a concrete counterexample path.\n\n### 2.2 Step-by-Step Construction of the Product Automaton\n\nLet $M = (S, S_0, R, L)$ and $A_{\\neg\\varphi} = (\\Sigma, Q, Q_0, \\delta, F)$ with $\\Sigma = 2^{AP}$.\n\nThe product NBW is defined as:\n\n$$M \\otimes A_{\\neg\\varphi} = (S \\times Q,\\ (S_0 \\times Q_0),\\ R_{\\otimes},\\ F_{\\otimes})$$\n\nwhere the product transition relation is:\n\n$$R_{\\otimes} = \\{\\, ((s, q), (s', q')) \\mid s' \\in R(s) \\ \\text{and} \\ q' \\in \\delta(q, L(s')) \\,\\}$$\n\nand the set of accepting states is:\n\n$$F_{\\otimes} = S \\times F = \\{\\, (s, q) \\mid q \\in F \\,\\}$$\n\n**Why this construction works (the key insight):** A run of the product is a sequence $\\rho = (s_0, q_0), (s_1, q_1), \\dots$ such that the second component is a run of $A_{\\neg\\varphi}$ and the first is a run of $M$ over the *same* labeled word $L(s_0)L(s_1)\\dots$. Therefore the accepted language of the product is exactly the set of labeled words belonging to **both** $\\mathcal{L}(M)$ and $\\mathcal{L}(A_{\\neg\\varphi})$ — which is the set of behaviors that simultaneously *exist in $M$* and *violate $\\varphi$*.\n\n### 2.3 Accepting Cycle Definition\n\nA cycle in a directed graph is **accepting** if and only if it contains at least one accepting state, i.e. at least one vertex from $F_{\\otimes}$. A **reachable** accepting cycle is one that lies in the subgraph induced by states reachable from $S_0 \\times Q_0$.\n\n> [!NOTE]\n> **Büchi acceptance intuition:** A non-accepting cycle may be traversed forever in the model but the automaton's run does not \"win\" — there is no match with a violating trace. An *accepting* cycle corresponds to a true counterexample: the system can loop through a state forever while the run of $A_{\\neg\\varphi}$ keeps revisiting an accepting state, i.e. a violation is sustained.\n\n### 2.4 Nested Depth-First Search (NDFS) — Classical SPIN Algorithm\n\nThe NDFS algorithm of **Courcoubetis, Vardi, Wolper & Yannakakis (1992)** finds an accepting cycle in linear time, using two interleaved DFS traversals on the product graph.\n\n**Outer DFS** explores the product from every initial state. Upon visiting an accepting state $v \\in F_{\\otimes}$ for the first time, it **suspends** and launches an **inner DFS** from $v$.\n\n**Inner DFS** searches only for a back-edge to any vertex on the outer-DFS stack. If it finds one, the cycle formed by that stack suffix + back-edge contains an accepting state and is **reported as a counterexample**.\n\n```text\nAlgorithm NDFS(M ⊗ A¬φ):\n  for all v0 ∈ S0 × Q0 do\n    if v0 not visited then dfs1(v0)\n\nProcedure dfs1(v):\n  visited[v] := true; stack.push(v)\n  for each successor u of v do\n    if u not visited then dfs1(u)\n  if v ∈ F⊗ then\n    dfs2(v)            // launch inner search\n  stack.pop(v)\n\nProcedure dfs2(v):\n  for each successor u of v do\n    if u ∈ stack then    // back edge to outer stack\n      report cycle: stack from u to v + edge (v,u)\n    else if u not innerVisited then\n      innerVisited[u] := true\n      dfs2(u)\n```\n\n> [!IMPORTANT]\n> **Complexity:** NDFS runs in $O(|V| + |E|)$ time, matching a single DFS. The key optimization is that the inner DFS restarts only from accepting states and uses a `innerVisited` flag to avoid redundant work.\n\n### 2.5 Counterexample Extraction\n\nOnce NDFS reports a cycle $C$ in the product, the **counterexample in $M$** is obtained by projecting each product state $(s, q)$ onto its $s$ component, and tracing from the cycle back through the path recorded by the outer DFS stack. The result is an infinite path $s_0 \\to s_1 \\to \\dots$ that:\n\n- is feasible in $M$,\n- satisfies the negation $\\neg\\varphi$ of the specification (i.e., witnesses a violation).\n\n> [!TIP]\n> Because Büchi acceptance is *infinite*, the counterexample is reported to the user as a finite *lasso* (stem + cycle), which is the standard output of SPIN-style model checkers.\n\n### 2.6 KTU Formula Sheet / Cheat Sheet\n\n| # | Concept | Definition / Formula | Notes |\n|---|---|---|---|\n| 1 | Model Checking Question | $M, s_0 \\models \\varphi$ | Boolean verdict |\n| 2 | Dual Formulation | $M \\models \\varphi \\iff \\mathcal{L}(M) \\cap \\mathcal{L}(\\neg\\varphi) = \\emptyset$ | Drives automata approach |\n| 3 | Product Automaton States | $S \\times Q$ | Pairwise state product |\n| 4 | Product Transitions | $((s,q),(s',q')) \\in R_{\\otimes} \\iff s' \\in R(s) \\land q' \\in \\delta(q, L(s'))$ | Label-driven sync |\n| 5 | Product Accepting Set | $F_{\\otimes} = S \\times F$ | All pairs with $q \\in F$ |\n| 6 | Cycle is accepting | $\\exists\\ v \\in C \\cdot v \\in F_{\\otimes}$ | Büchi: visit accepting state on cycle |\n| 7 | NDFS complexity | $O(\\vert V \\vert + \\vert E \\vert)$ | Linear in product size |\n| 8 | NDFS output | Lasso = stem $\\sigma$ + cycle $\\pi$ | Counterexample in $M$ |\n| 9 | Worst-case automaton size | $\\mathcal{O}(2^{\\vert \\varphi \\vert})$ states in $A_{\\neg\\varphi}$ | Exponential blow-up |\n| 10 | Tableau acceptance set | $F = \\{\\ \\text{states with no open obligation}\\}$ | Builds $A_{\\neg\\varphi}$ |\n\n### 2.7 Real-World Engineering Utility\n\nAutomata-based LTL model checking is the workhorse of **hardware verification** (CPU cache protocols, bus arbiters in industrial SPIN/SMV toolchains) and **protocol verification** (e.g., leader election, sliding-window protocols, IEEE 1394 FireWire). The product construction is the conceptual backbone of every industrial **explicit-state model checker**, and the nested DFS is the heart of SPIN's `pan.c` search engine. Mastering it allows engineers to read and customize real verification tool output.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Worked Example 1 — Product Automaton Construction (Hand-Computed)\n\n**Problem:** Let $M$ be a Kripke structure with $AP = \\{a, b\\}$ and two states $s_0, s_1$:\n\n- $L(s_0) = \\{a\\}$, $L(s_1) = \\{b\\}$,\n- $R = \\{(s_0, s_0), (s_0, s_1), (s_1, s_1)\\}$,\n- $S_0 = \\{s_0\\}$.\n\nLet the LTL formula be $\\varphi = \\mathbf{G}\\,a$ (\"globally $a$\"). Build $A_{\\neg\\varphi}$ for $\\neg\\varphi = \\mathbf{F}\\,\\neg a$ (\"eventually not $a$\").\n\n**Step 1 — Build $A_{\\neg\\varphi}$ manually.**\n$\\mathbf{F}\\,\\neg a$ is satisfied when $\\neg a$ is seen. A minimal NBW with $Q = \\{q_0, q_1\\}$:\n- $\\Sigma = 2^{\\{a,b\\}} = \\{\\emptyset, \\{a\\}, \\{b\\}, \\{a,b\\}\\}$.\n- $Q_0 = \\{q_0\\}$, $F = \\{q_1\\}$.\n- Transitions: from $q_0$ on any letter, loop to $q_0$ (waiting) *or* jump to $q_1$ if $\\neg a$ holds (i.e. letter in $\\{\\emptyset, \\{b\\}\\}$); from $q_1$ on any letter, stay in $q_1$.\n\n$$\n\\begin{aligned}\n\\delta(q_0,\\{a\\}) &= \\{q_0\\} \\\\\n\\delta(q_0,\\emptyset) &= \\{q_0, q_1\\} \\\\\n\\delta(q_0,\\{b\\}) &= \\{q_0, q_1\\} \\\\\n\\delta(q_0,\\{a,b\\}) &= \\{q_0\\} \\\\\n\\delta(q_1,x) &= \\{q_1\\} \\quad \\text{for all } x \\in \\Sigma\n\\end{aligned}\n$$\n\n**Step 2 — Form the product $M \\otimes A_{\\neg\\varphi}$.**\nStates $S \\times Q = \\{(s_0,q_0), (s_0,q_1), (s_1,q_0), (s_1,q_1)\\}$. Initial: $\\{(s_0,q_0)\\}$. Accepting: $F_{\\otimes} = S \\times F = \\{(s_0,q_1),(s_1,q_1)\\}$.\n\nCompute transitions state by state, using the rule:\n\n$$\n((s,q),(s',q')) \\in R_{\\otimes} \\iff s' \\in R(s) \\land q' \\in \\delta(q, L(s'))\n$$\n\n**Transition for $(s_0, q_0)$:**\n- $R(s_0) = \\{s_0, s_1\\}$, $L(s_0) = \\{a\\}$, $\\delta(q_0, \\{a\\}) = \\{q_0\\}$.\n- $s' = s_0$: only $q' = q_0 \\Rightarrow (s_0,q_0) \\to (s_0,q_0)$.\n- $s' = s_1$: $L(s_1)=\\{b\\}$, but $q_0$ on $\\{b\\}$ has $\\delta=\\{q_0,q_1\\}$.\n  - $q' = q_0 \\Rightarrow (s_0,q_0) \\to (s_1,q_0)$.\n  - $q' = q_1 \\Rightarrow (s_0,q_0) \\to (s_1,q_1)$.\n\n**Transition for $(s_0, q_1)$:**\n- $R(s_0) = \\{s_0, s_1\\}$, $L(s_0)=\\{a\\}$, $\\delta(q_1, \\{a\\}) = \\{q_1\\}$.\n- $s' = s_0, q' = q_1$: $(s_0,q_1) \\to (s_0,q_1)$.\n- $s' = s_1, q' = q_1$: $(s_0,q_1) \\to (s_1,q_1)$.\n\n**Transition for $(s_1, q_0)$:**\n- $R(s_1) = \\{s_1\\}$, $L(s_1)=\\{b\\}$, $\\delta(q_0,\\{b\\}) = \\{q_0, q_1\\}$.\n- $s' = s_1, q' = q_0$: $(s_1,q_0) \\to (s_1,q_0)$.\n- $s' = s_1, q' = q_1$: $(s_1,q_0) \\to (s_1,q_1)$.\n\n**Transition for $(s_1, q_1)$:**\n- $R(s_1) = \\{s_1\\}$, $L(s_1)=\\{b\\}$, $\\delta(q_1,\\{b\\}) = \\{q_1\\}$.\n- $s' = s_1, q' = q_1$: $(s_1,q_1) \\to (s_1,q_1)$.\n\n**Step 3 — Verify the answer (accepting-cycle search).**\n\nThe reachable product subgraph from $\\{(s_0,q_0)\\}$ is:\n- $(s_0,q_0) \\to (s_0,q_0)$ (self-loop),\n- $(s_0,q_0) \\to (s_1,q_0) \\to (s_1,q_1) \\to (s_1,q_1) \\dots$ (self-loop on accepting state).\n\nThe cycle $(s_1,q_1) \\to (s_1,q_1)$ contains $(s_1,q_1) \\in F_{\\otimes}$ ⇒ **accepting cycle exists**. Project: counterexample is the infinite path $s_0 \\to s_1 \\to s_1 \\to s_1 \\to \\dots$ on which $a$ fails after the first step, violating $\\mathbf{G}a$. So $M \\not\\models \\mathbf{G}\\,a$.\n\n> [!NOTE]\n> **Engineering sanity check:** The cycle in the product reflects the self-loop on $s_1$ in $M$, which is the *witness* that $M$ can stay in a non-$a$ state forever. This is exactly the intuitive violation we expect.\n\n### 3.2 Worked Example 2 — Nested DFS Trace\n\nConsider a 4-state product $V = \\{v_0, v_1, v_2, v_3\\}$ with $F_{\\otimes} = \\{v_2\\}$ and edges:\n$v_0 \\to v_1, v_0 \\to v_2, v_1 \\to v_3, v_2 \\to v_3, v_3 \\to v_3, v_2 \\to v_2$.\n\n**Outer DFS (dfs1) starting at $v_0$:**\n\n- Visit $v_0$. Stack = $[v_0]$.\n- Go to $v_1$. Stack = $[v_0, v_1]$.\n- Go to $v_3$. Stack = $[v_0, v_1, v_3]$.\n- $v_3$ has no unvisited successors. Pop $v_3$. Stack = $[v_0, v_1]$.\n- $v_1$ has no remaining successors. Pop $v_1$. Stack = $[v_0]$.\n- Go to $v_2$ (next successor of $v_0$). Stack = $[v_0, v_2]$.\n- $v_2 \\in F_{\\otimes}$ — *launch inner dfs2 from $v_2$*.\n\n**Inner DFS (dfs2) from $v_2$:**\n\n- Successor $v_3$: $v_3 \\notin$ stack. Mark innerVisited. Recurse.\n  - Successor $v_3$: $v_3 \\in$ stack? No (already popped). $v_3 \\notin$ innerVisited? No. Skip.\n  - $v_3$ returns.\n- Successor $v_2$: $v_2 \\in$ stack $\\Rightarrow$ **back edge detected**! Report cycle $v_2 \\to v_2$.\n- Pop $v_2$. Done.\n\n**Result:** Counterexample lasso is stem $v_0 \\to v_2$ and cycle $v_2 \\to v_2$. Projecting onto $M$ yields the path violating $\\varphi$.\n\n### 3.3 Analytical Matrix — Algorithm vs. State Space Type\n\n| Algorithm | State-Space | Memory | Detects | Notes |\n|---|---|---|---|---|\n| Plain DFS cycle search | Tiny / educational | $O(\\vert V \\vert)$ | All cycles | Misses accepting requirement |\n| Standard BFS for cycles | Small | $O(\\vert V \\vert)$ | All cycles | Not Büchi-aware |\n| **NDFS (CVWY)** | **Explicit-state, large** | $O(\\vert V \\vert)$ | **Accepting cycles** | Linear; SPIN default |\n| Tarjan SCC | Explicit, large | $O(\\vert V \\vert)$ | Accepting cycle if SCC $\\cap F \\ne \\emptyset$ | Used in NuSMV-like tools |\n| On-the-fly NDFS | Very large, partial | $O(\\vert V_{visited} \\vert)$ | Accepting cycles | Avoids full product |\n| Symbolic (BDD) | Huge | $O(\\vert \\text{BDD} \\vert)$ | Fixpoint | Complements explicit approach |\n\n> [!IMPORTANT]\n> For KTU examinations, students are expected to recognize the **trade-off** between **NDFS** (used in explicit-state checkers like SPIN) and **SCC-based** approaches (used in symbolic tools).\n\n### 3.4 Complexity Derivation\n\nLet $n = \\vert S \\vert$ and $m = \\vert A_{\\neg\\varphi} \\vert$. Then:\n\n$$\n\\begin{aligned}\n\\vert S \\times Q \\vert &= n \\cdot m \\\\\n\\vert R_{\\otimes} \\vert &\\leq \\vert R \\vert \\cdot m \\quad \\text{(one Büchi branch per $M$ edge)} \\\\\n\\text{NDFS time} &= O(n \\cdot m + \\vert R \\vert \\cdot m) = O(m \\cdot (n + \\vert R \\vert)) \\\\\n\\text{Automaton size } m &= \\mathcal{O}(2^{\\vert \\varphi \\vert})\n\\end{aligned}\n$$\n\nHence the overall model checking complexity is:\n\n$$\n\\mathcal{O}\\!\\left( 2^{\\vert \\varphi \\vert} \\cdot (n + \\vert R \\vert) \\right)\n$$\n\nThis is the canonical PSPACE-complete cost of LTL model checking.\n\n### 3.5 Worked Example 3 — Liveness Property (GFa)\n\nLet $\\varphi = \\mathbf{G}\\mathbf{F}\\,a$ (\"infinitely often $a$\"). Then $\\neg\\varphi = \\mathbf{F}\\mathbf{G}\\,\\neg a$ (\"eventually always not $a$\"). A standard NBW for $\\mathbf{F}\\mathbf{G}\\,\\neg a$ is the two-state automaton with $F = \\{q_1\\}$ where $q_1$ is \"abandoned in $\\neg a$\" — once entered, it stays. An accepting run must keep visiting $F$.\n\nTo find a counterexample, the NDFS must locate a cycle in $M$ that lies entirely inside states labeled $\\{\\neg a\\}$ (i.e. $a \\notin L(s)$). This is exactly the intuition: \"is there a part of $M$ that can be reached and then stays in non-$a$ forever?\" If yes, liveness is violated.\n\n> [!TIP]\n> The reachability of such a region is decided by NDFS in linear time on the product. The reduction is the same algorithm — only the Büchi automaton changes.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 End-to-End Pipeline (Mermaid Flowchart)\n\n```mermaid\nflowchart TD\n    A[Spec: LTL formula phi] --> B[Compute negation not phi]\n    B --> C[Build NBW A_not_phi<br/>via tableau construction]\n    D[Kripke Structure M] --> E[Build Product<br/>M tensor A_not_phi]\n    C --> E\n    E --> F[Run Nested DFS<br/>on product]\n    F --> G{Accepting<br/>cycle found?}\n    G -- Yes --> H[Report Counterexample<br/>project to M]\n    G -- No --> I[VERDICT: M models phi]\n    H --> J[Output lasso<br/>stem + cycle]\n    I --> K[Verification complete]\n    J --> K\n```\n\n### 4.2 Product Automaton Block Diagram\n\n```mermaid\nflowchart LR\n    subgraph M[Kripke Structure M]\n        s0((s0))\n        s1((s1))\n        s2((s2))\n        s0 -- a --> s1\n        s1 -- b --> s2\n        s2 -- c --> s0\n    end\n    subgraph A[NBW A_not_phi]\n        q0((q0))\n        q1((q1 accept))\n        q0 -- x --> q1\n        q1 -- y --> q1\n    end\n    M -. synchronization via L s .-> A\n    P0[Product States<br/>s0,q0  s1,q0  s2,q0<br/>s0,q1  s1,q1  s2,q1]\n    M --> P0\n    A --> P0\n    P0 --> R[NDFS Search<br/>for back edge into F tensor]\n```\n\n### 4.3 Nested DFS Control Flow\n\n```mermaid\nflowchart TD\n    Start([Start: outer DFS from initial state]) --> Push[Push state to outer stack]\n    Push --> Explore{Unvisited<br/>successor?}\n    Explore -- Yes --> Recurse[Recursive dfs1 on successor]\n    Recurse --> Explore\n    Explore -- No --> Check{Is current<br/>state in F tensor?}\n    Check -- Yes --> Inner[Launch inner dfs2]\n    Inner --> InnerSearch{Successor on<br/>outer stack?}\n    InnerSearch -- Yes --> Report[Report lasso<br/>back edge forms accepting cycle]\n    InnerSearch -- No --> InnerCont[Continue inner DFS]\n    InnerCont --> InnerSearch\n    Report --> Done([Exit with counterexample])\n    Check -- No --> Pop[Pop state from outer stack]\n    Pop --> Up[Return to caller dfs1]\n    Up --> Explore\n```\n\n### 4.4 Counterexample Lasso Schema\n\n```mermaid\nflowchart LR\n    subgraph Stem[Stem sigma: path to cycle entry]\n        d0((v0)) --> d1((v1)) --> d2((v2)) --> d3((v3))\n    end\n    d3 -- begin cycle --> d4((v4 accept))\n    subgraph Cycle[Cycle pi: contains accepting state]\n        d4 --> d5((v5)) --> d6((v6)) --> d7((v7)) --> d4\n    end\n    Stem --> Cycle\n```\n\n> [!NOTE]\n> The lasso visualizes how a finite *stem* leads into a *cycle* that contains an accepting state — the canonical shape of an LTL counterexample. It is also the exact output format that SPIN prints via its trail files.\n\n### 4.5 Algorithmic Complexity Diagram\n\n```mermaid\nflowchart TD\n    L[Input: LTL formula phi] --> N[Negation: not phi]\n    N --> T[Tableau construction: 2 to the power vert phi vert states]\n    T --> P[Product: n times m vertices]\n    P --> D[NDFS: O of n times m + E]\n    D --> Out{Accepting cycle?}\n    Out -- Yes --> CE[Counterexample in O of n plus m time]\n    Out -- No --> OK[Verified: M models phi]\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### Part A — Short Answer (3 Marks Each)\n\n**Q1. `[KTU University Exam – Dec 2023, Module 3]`**  \n**State the dual formulation of the LTL model checking problem used in automata-based verification.**\n\n**Model Answer (3 marks):**  \nThe LTL model checking problem $M \\models \\varphi$ (read: \"Kripke structure $M$ satisfies LTL formula $\\varphi$\") is reduced to a language emptiness problem over Büchi automata using the dual formulation:\n\n$$M \\models \\varphi \\iff \\mathcal{L}(M) \\cap \\mathcal{L}(\\neg\\varphi) = \\emptyset$$\n\nThat is, $M$ satisfies $\\varphi$ if and only if no behaviour of $M$ is a model of $\\neg\\varphi$. To check this algorithmically, we construct an NBW $A_{\\neg\\varphi}$ for $\\neg\\varphi$, build the product $M \\otimes A_{\\neg\\varphi}$, and search for an accepting cycle. If none exists, the intersection is empty and $M \\models \\varphi$; otherwise, the accepting cycle yields a counterexample.\n\n**Q2. `[KTU University Exam – May 2024, Module 3]`**  \n**Define the accepting states of the product automaton $M \\otimes A_{\\neg\\varphi}$. Why is it sufficient to take $F_{\\otimes} = S \\times F$?**\n\n**Model Answer (3 marks):**  \nThe accepting states of the product automaton are defined as\n\n$$F_{\\otimes} = \\{\\, (s, q) \\in S \\times Q \\mid q \\in F \\,\\} = S \\times F$$\n\nThis is sufficient because a run of the product automaton, when projected to its second component, must be an accepting run of $A_{\\neg\\varphi}$ (i.e., it must visit $F$ infinitely often). A product state $(s, q)$ is therefore accepting precisely when the Büchi component $q$ is accepting. The $M$-component $s$ does not affect the Büchi acceptance condition, so we take the Cartesian product with the entire $S$ to keep the synchronization constraint.\n\n---\n\n### Part B — Module Internal Choice (14 Marks Each)\n\n**Question A (14 Marks): `[KTU University Exam – Dec 2023, Module 3]`**\n\n> **(a)** With a clear algorithm, explain the construction of the product automaton between a Kripke structure $M$ and an NBW $A_{\\neg\\varphi}$. State the size of the product in terms of $|S|$, $|Q|$, and $|R|$. **(7 marks)**\n>\n> **(b)** Apply the nested depth-first search (NDFS) algorithm on the product graph you construct for the following inputs and report whether an accepting cycle exists.\n> - $M$: $S = \\{s_0, s_1, s_2\\}$, $S_0 = \\{s_0\\}$, $R = \\{(s_0,s_1), (s_1,s_1), (s_1,s_2), (s_2,s_2)\\}$, $L(s_0)=\\{a\\}, L(s_1)=\\{b\\}, L(s_2)=\\{a,b\\}$.\n> - $A_{\\neg\\varphi}$: $Q = \\{q_0, q_1\\}$, $Q_0 = \\{q_0\\}$, $F = \\{q_1\\}$, $\\delta(q_0, \\{a\\}) = \\{q_0\\}$, $\\delta(q_0, \\{a,b\\}) = \\{q_0, q_1\\}$, $\\delta(q_0, \\text{others}) = \\{q_0\\}$, $\\delta(q_1, \\cdot) = \\{q_1\\}$. **(7 marks)**\n\n**Model Solution:**\n\n**(a) — 7 marks:**\n\n- **Step 1 (1 mark):** Define product components. Given $M = (S, S_0, R, L)$ and $A_{\\neg\\varphi} = (\\Sigma, Q, Q_0, \\delta, F)$, the product NBW is\n  $M \\otimes A_{\\neg\\varphi} = (S \\times Q,\\ S_0 \\times Q_0,\\ R_{\\otimes},\\ F_{\\otimes})$\n  where $F_{\\otimes} = S \\times F$.\n\n- **Step 2 (2 marks):** Define $R_{\\otimes}$. A transition $((s, q), (s', q'))$ exists iff\n  $s' \\in R(s) \\ \\text{and} \\ q' \\in \\delta(q, L(s'))$.\n  This ensures that the $M$-component moves along an edge of $M$ while the $A_{\\neg\\varphi}$-component reads the label $L(s')$ of the *new* $M$-state.\n\n- **Step 3 (2 marks):** State the size. The number of product states is $|S| \\cdot |Q|$ and the number of product edges is at most $|R| \\cdot |Q|$ (one edge in $A$ per $M$-edge).\n\n- **Step 4 (2 marks):** State the language. $\\mathcal{L}(M \\otimes A_{\\neg\\varphi}) = \\mathcal{L}(M) \\cap \\mathcal{L}(A_{\\neg\\varphi}) = \\mathcal{L}(M) \\cap \\mathcal{L}(\\neg\\varphi)$. A run is accepting iff the $A_{\\neg\\varphi}$-component visits $F$ infinitely often. Therefore $M \\models \\varphi$ iff the product has **no reachable accepting cycle**.\n\n**(b) — 7 marks:**\n\n- **Step 1 (1 mark):** Product state set: $S \\times Q = \\{(s_0,q_0), (s_0,q_1), (s_1,q_0), (s_1,q_1), (s_2,q_0), (s_2,q_1)\\}$. Initial: $\\{(s_0, q_0)\\}$. Accepting: $F_{\\otimes} = S \\times \\{q_1\\}$.\n\n- **Step 2 (2 marks):** Compute transitions using $R_{\\otimes}$.\n\n  From $(s_0, q_0)$: $R(s_0)=\\{s_1\\}$, $L(s_1)=\\{b\\}$, $\\delta(q_0, \\{b\\})=\\{q_0\\}$. So $(s_0,q_0) \\to (s_1,q_0)$.\n\n  From $(s_1, q_0)$: $R(s_1)=\\{s_1, s_2\\}$, $L(s_1)=\\{b\\}$, $\\delta(q_0, \\{b\\})=\\{q_0\\}$.\n  - $(s_1,q_0) \\to (s_1,q_0)$.\n  - $L(s_2)=\\{a,b\\}$, $\\delta(q_0, \\{a,b\\})=\\{q_0, q_1\\}$.\n    - $(s_1,q_0) \\to (s_2,q_0)$ and $(s_1,q_0) \\to (s_2,q_1)$.\n\n  From $(s_2, q_0)$: $R(s_2)=\\{s_2\\}$, $L(s_2)=\\{a,b\\}$, $\\delta(q_0, \\{a,b\\})=\\{q_0, q_1\\}$.\n  - $(s_2,q_0) \\to (s_2,q_0)$ and $(s_2,q_0) \\to (s_2,q_1)$.\n\n  From $(s_2, q_1)$: $R(s_2)=\\{s_2\\}$, $L(s_2)=\\{a,b\\}$, $\\delta(q_1,\\cdot)=\\{q_1\\}$.\n  - $(s_2,q_1) \\to (s_2,q_1)$.\n\n  From $(s_1, q_1)$ and $(s_0, q_1)$: not reachable from initial state (no incoming edges from reachable region).\n\n- **Step 3 (2 marks):** NDFS trace. Outer DFS from $(s_0,q_0)$:\n  - Visit $(s_0,q_0)$, push to outer stack.\n  - Go to $(s_1,q_0)$, push.\n  - First successor $(s_1,q_0)$: visited. Try $(s_2,q_0)$: visit, push.\n    - Successors: $(s_2,q_0)$ (visited), $(s_2,q_1)$: visit, push.\n      - $(s_2,q_1) \\in F_{\\otimes}$ — *launch inner dfs2*.\n      - Inner DFS from $(s_2,q_1)$: only successor is $(s_2,q_1)$ itself, which is on the outer stack ⇒ **back edge detected**!\n  - Report lasso: stem $=(s_0,q_0) \\to (s_1,q_0) \\to (s_2,q_0) \\to (s_2,q_1)$, cycle $= (s_2,q_1) \\to (s_2,q_1)$.\n\n- **Step 4 (2 marks):** Project the cycle back to $M$. The counterexample in $M$ is the lasso with stem $s_0 \\to s_1 \\to s_2$ and cycle $s_2 \\to s_2$. Hence $M \\not\\models \\varphi$ and the system has a violating trace (it enters $s_2$ and stays there forever; whether this violates the original $\\varphi$ depends on $A_{\\neg\\varphi}$'s design — here the trace *does* match a run of $A_{\\neg\\varphi}$ that visits $q_1$ forever).\n\n---\n\n**Question B (14 Marks, Alternative Choice): `[KTU University Exam – May 2024, Module 3]`**\n\n> **(a)** Describe the Nested Depth-First Search (NDFS) algorithm. State its time and space complexity and explain why two DFS passes are needed. **(7 marks)**\n>\n> **(b)** Consider a product automaton with vertices $\\{v_0, v_1, v_2, v_3, v_4\\}$, $F_{\\otimes} = \\{v_2, v_4\\}$, and edges:\n> $v_0 \\to v_1, v_0 \\to v_2, v_1 \\to v_3, v_2 \\to v_3, v_2 \\to v_2, v_3 \\to v_4, v_4 \\to v_4, v_3 \\to v_0$.\n> Initial state: $v_0$. Run NDFS by hand and report the counterexample lasso if one exists. **(7 marks)**\n\n**Model Solution:**\n\n**(a) — 7 marks:**\n\n- **Step 1 (2 marks):** NDFS is a two-pass DFS scheme that searches for a reachable accepting cycle in a Büchi product. The outer DFS (`dfs1`) explores the graph and, every time it first visits an accepting state $v \\in F_{\\otimes}$, it suspends and invokes the inner DFS (`dfs2`) starting from $v$. The inner DFS restricts its search to vertices reachable from $v$ that are not yet \"innerVisited\", and checks for a back edge to a vertex currently on the outer DFS stack.\n\n- **Step 2 (2 marks):** Why two passes? A single DFS can detect cycles but cannot certify that the cycle *contains* an accepting state without an auxiliary structure. By suspending exactly at accepting states and running a bounded inner DFS, NDFS guarantees: (i) any reported cycle contains an accepting state (so it is a Büchi-accepting cycle), and (ii) every accepting cycle is reported by some inner DFS — coverage is preserved by the `innerVisited` set that prunes redundant inner searches.\n\n- **Step 3 (2 marks):** Complexity. Each edge is traversed at most once by `dfs1` and at most once by `dfs2` (in a directed graph with `innerVisited` pruning), giving total time $O(|V| + |E|)$. The space used is $O(|V|)$ for the visited, innerVisited, and stack arrays.\n\n- **Step 4 (1 mark):** NDFS is the standard algorithm in explicit-state LTL model checkers such as SPIN, and is the classical reference: Courcoubetis, Vardi, Wolper, Yannakakis (1992).\n\n**(b) — 7 marks:**\n\n- **Step 1 (1 mark):** Edges: $v_0 \\to v_1, v_0 \\to v_2, v_1 \\to v_3, v_2 \\to v_3, v_2 \\to v_2, v_3 \\to v_4, v_4 \\to v_4, v_3 \\to v_0$. Accepting: $F_{\\otimes} = \\{v_2, v_4\\}$.\n\n- **Step 2 (2 marks):** Outer DFS from $v_0$:\n  - Visit $v_0$. Stack = $[v_0]$.\n  - Go to $v_1$. Stack = $[v_0, v_1]$.\n  - Go to $v_3$. Stack = $[v_0, v_1, v_3]$.\n  - $v_3$ successors: $v_4$ and $v_0$.\n    - Visit $v_4$. Stack = $[v_0, v_1, v_3, v_4]$.\n    - $v_4 \\in F_{\\otimes}$ — *launch inner dfs2 from $v_4$*.\n      - Inner DFS from $v_4$: only successor $v_4$ itself. $v_4 \\in$ outer stack ⇒ **back edge**! Report cycle $v_4 \\to v_4$.\n    - Pop $v_4$. Stack = $[v_0, v_1, v_3]$.\n    - $v_0 \\in$ outer stack? Yes. (Inner DFS doesn't run on $v_0$ here because $v_0$ was not the launching point.)\n  - Pop $v_3$, pop $v_1$. Stack = $[v_0]$.\n  - Go to $v_2$ (next successor of $v_0$). Stack = $[v_0, v_2]$.\n  - $v_2 \\in F_{\\otimes}$ — *launch inner dfs2 from $v_2$*.\n    - Inner DFS from $v_2$: successors $v_3, v_2$.\n      - $v_3 \\notin$ outer stack (already popped) and $\\notin$ innerVisited. Mark and recurse.\n        - $v_3$ successors: $v_4, v_0$. Both $\\notin$ outer stack and $\\notin$ innerVisited.\n          - $v_4$: mark, recurse. Successor $v_4 \\in$ inner stack (visited) but not outer. No cycle through outer.\n          - $v_0$: mark, recurse. Successor $v_1$: visited outer-state, not on stack. Continue.\n        - Return.\n      - $v_2$: on outer stack ⇒ **back edge**! Report cycle $v_2 \\to v_2$.\n  - Pop $v_2$. Done.\n\n- **Step 3 (2 marks):** First accepting cycle reported: lasso = stem $v_0 \\to v_1 \\to v_3 \\to v_4$ + cycle $v_4 \\to v_4$. (The search also reports $v_2 \\to v_2$ via the second inner launch, but the first is sufficient.)\n\n- **Step 4 (2 marks):** Project onto $M$ to obtain a counterexample path; the lasso shape (stem + self-loop on an accepting state) is what is reported to the user as the witness of the violation.\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Pitfall Callout:**  \n> - **Common mistake 1 (–2 marks):** Forgetting to take $F_{\\otimes} = S \\times F$. Students often wrongly restrict $F_{\\otimes}$ to \"states in $F$ that are also reachable in $M$ at the right moment\". The product *defines* acceptance purely on the Büchi component, so $F_{\\otimes} = S \\times F$ is canonical.  \n> - **Common mistake 2 (–2 marks):** During NDFS, students frequently use a *single* DFS and check \"is there any cycle containing $F$?\" — this misses cycles that emerge only after exploring deeper subtrees. Always reason via the two-phase scheme with the outer stack as the cycle witness.  \n> - **Common mistake 3 (–1 to 2 marks):** Confusing labels in the transition rule. The Büchi component reads $L(s')$, **not** $L(s)$. Writing $\\delta(q, L(s))$ instead of $\\delta(q, L(s'))$ is a frequent error worth at least 1–2 marks in valuation.  \n> - **Common mistake 4 (–1 mark):** Reporting the counterexample as the product path itself; you must *project* onto the $S$-component of $M$.\n\n---\n\n### Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- [ ] The **dual formulation** $M \\models \\varphi \\iff \\mathcal{L}(M) \\cap \\mathcal{L}(\\neg\\varphi) = \\emptyset$ is the cornerstone of automata-based LTL model checking.\n- [ ] The **product automaton** has states $S \\times Q$, initial states $S_0 \\times Q_0$, transitions $((s,q),(s',q'))$ iff $s' \\in R(s) \\land q' \\in \\delta(q, L(s'))$, and **accepting set $F_{\\otimes} = S \\times F$**.\n- [ ] Büchi acceptance of a run: the $A_{\\neg\\varphi}$-component must visit $F$ *infinitely often*, equivalently the product run must contain an **accepting cycle** (a cycle with at least one $F_{\\otimes}$ vertex).\n- [ ] $M \\models \\varphi$ holds **iff** the product automaton has **no reachable accepting cycle**.\n- [ ] **Nested DFS (NDFS)** finds an accepting cycle in $O(|V| + |E|)$ time, with $O(|V|)$ space — linear in the product size.\n- [ ] NDFS uses an **outer stack**, an `innerVisited` set, and an **inner DFS** launched at each newly visited accepting state.\n- [ ] A **counterexample** is reported as a **lasso** (finite stem + cycle containing $F_{\\otimes}$), and must be **projected back to $M$** by dropping the $Q$-component.\n- [ ] Worst-case complexity: $\\mathcal{O}\\!\\left(2^{|\\varphi|} \\cdot (n + |R|)\\right)$ — exponential in the formula, linear in the model. This matches the **PSPACE-completeness** of LTL MC.\n- [ ] Liveness properties ($\\mathbf{G}\\mathbf{F}\\,p$) reduce to finding a *reachable* non-$p$ cycle; safety properties ($\\mathbf{G}\\,p$) reduce to finding a *reachable* state with $\\neg p$.\n- [ ] Industrial tools: **SPIN** uses on-the-fly NDFS; **NuSMV** uses BDD-based SCC computation; both are unified by the automata-based framework.\n- [ ] Always check the **label convention**: $L(s')$ is read by the Büchi component when transitioning from $s$ to $s'$.\n- [ ] In the product, the **accepting set is the full Cartesian product $S \\times F$** — not just reachable pairs.\n- [ ] For KTU 2024 ESE, the most tested sub-topics are: (i) product construction by hand, (ii) NDFS trace, (iii) counterexample extraction.\n<!-- SECTION_5_END -->",
          "pyqs": []
        }
      ]
    },
    {
      "id": "m4",
      "title": "Module 4",
      "topics": [
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 4-t1",
          "title": "Introduction - Introduction to the Tool Symbolic Analysis Laboratory (SAL)",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 4\n> - **Topic:** Introduction - Introduction to the Tool Symbolic Analysis Laboratory (SAL)**\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\nThe **Symbolic Analysis Laboratory (SAL)** is a framework and tool suite developed by **SRI International's Computer Science Laboratory** for the analysis of **state-transition systems**, particularly those modeling **concurrent and reactive systems**. It provides a common intermediate language to describe transition systems and integrates multiple analysis engines (model checkers, simulators, and theorem provers) that operate on the same system description.\n\n> [!IMPORTANT]\n> **KTU Syllabus Definition (PECNT526 - Module 4):**\n> *SAL (Symbolic Analysis Laboratory) is a model-checking framework developed at SRI International that provides a common language for describing transition systems and integrates multiple symbolic analysis tools (sal-bmc, sal-inf-bmc, sal-wmc, sal-smc, sal-sim) over a uniform context, enabling verification of infinite-state and finite-state concurrent systems.*\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine you have designed a complex **railway interlocking system** at a major junction. Multiple trains can arrive from different directions, switches must be set correctly, and signals must follow precise timing rules. You want to prove that **no two trains can ever collide**, no matter what sequence of inputs arrives.\n\nDoing this by hand is nearly impossible because the system has thousands of possible states. **SAL is like a sophisticated \"smart auditor\"** for such systems:\n\n- **You describe the railway system once** — tracks, signals, trains, switches — using a precise language.\n- **You hand that description to a panel of expert auditors**, each with a different specialty (one checks the next 50 steps, one looks for repeating patterns, one simulates random operation).\n- **Each auditor uses a different mathematical technique** (BDDs, SAT solvers, k-induction, simulation).\n- **If any auditor finds a flaw, it reports the exact counter-example** (the sequence of events leading to a collision).\n\nThis \"panel of auditors\" is what SAL provides for any finite- or infinite-state system — a **uniform environment with multiple specialized symbolic analysis back-ends**.\n\n## 1.3 Key Terminology & Metrics\n\nThe following bold terms form the **vocabulary of SAL** and appear frequently in KTU questions:\n\n- **Context** — A SAL module that declares variables, their types, initializations, and transition relations. It is the *static description* of the system.\n- **Module** — A SAL file containing a `CONTEXT` and one or more `MODULE` definitions. The module captures the *dynamic behaviour* (transitions, invariants).\n- **Transition System** — A directed graph whose nodes are system states and whose edges are labelled by transitions; SAL is fundamentally a *symbolic engine over such graphs*.\n- **Symbolic Analysis** — Reasoning about *sets* of states simultaneously (using Boolean encodings, BDDs, or propositional formulas) rather than enumerating states one by one.\n- **Bounded Model Checking (BMC)** — Unrolling the transition relation for a fixed number $k$ of steps and checking reachability of an error state using a SAT solver.\n- **k-Induction** — A generalization of BMC that proves properties for *all* $k \\geq 0$ by an inductive argument.\n- **Counter-example** — A concrete sequence of states that violates the property, returned by SAL when verification fails.\n- **Liveness & Safety Properties** — SAL can verify both: safety (\"bad things never happen\") and liveness (\"good things eventually happen\").\n\n> [!NOTE]\n> **Historical Context for KTU Exam:** SAL was developed in the early 2000s by **Shankar, Bensalem, de Moura, Owre, Rushby, and colleagues at SRI International**. It is freely available and remains one of the benchmark tools for teaching symbolic model checking in academic curricula.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The SAL Tool Architecture — Components and Their Roles\n\nSAL is best understood as a **two-layer architecture**: a *front-end language* and a *family of back-end analysis engines*.\n\n### 2.1.1 The Language Layer (Front-End)\n\nThe SAL language is designed to be **declarative, modular, and strongly typed**. Its core constructs are:\n\n1. **TYPE declarations** — Boolean, integer, real, enumerated, array, and record types.\n2. **CONTEXT block** — Declares global types, constants, and variables shared across modules.\n3. **MODULE block** — Encapsulates local state, initialization (`INITIALIZATION`), and a `TRANSITION` relation.\n4. **Definition syntax** — Supports guarded commands, non-deterministic assignments, and synchronous/asynchronous composition.\n5. **Property specification** — Through auxiliary modules and temporal logic assertions checked by back-ends.\n\n### 2.1.2 The Back-End Analysis Tools\n\nSAL ships with **five major analysis tools**, each tailored to a particular verification technique:\n\n| Back-End Tool | Underlying Technique | Best Suited For |\n|---|---|---|\n| **sal-bmc** | Bounded Model Checking via SAT solvers (e.g., ICS, CVC3) | Finding bugs up to a depth $k$ |\n| **sal-inf-bmc** | Infinite BMC with $k$ as a symbolic parameter | Parametric reachability |\n| **sal-wmc** | Well-Founded Model Checker using $k$-induction & PDR | Proving safety for *all* depths |\n| **sal-smc** | Symbolic Model Checker using BDDs (via CUDD) | Finite-state exhaustive verification |\n| **sal-sim** | Random/corner-case simulator | Quick sanity checks & counter-example generation |\n\n> [!IMPORTANT]\n> **Why multiple back-ends?** A single technique is rarely optimal for *all* systems. SAT-based BMC is brilliant at finding deep bugs; BDDs are strong at finite-state exhaustive proofs; $k$-induction extends BMC to proofs. SAL lets the user **try them all on the same description** without rewriting the model.\n\n## 2.2 Operational Workflow in SAL\n\nThe verification workflow follows a precise pipeline:\n\n- **Step 1 — Author the model:** Write `Context` and `Module` in `.sal` files describing the system variables, initial states, and transitions.\n- **Step 2 — Specify the property:** Express the desired property as another SAL module (often as an observer) or as a temporal logic assertion.\n- **Step 3 — Choose the back-end:** Select one of `sal-bmc`, `sal-wmc`, `sal-smc`, or `sal-inf-bmc` based on whether the system is finite/infinite and whether the goal is bug-finding or proof.\n- **Step 4 — Parse & translate:** The SAL front-end parses the file and converts it into an internal **SAL Transition System (STS)** representation.\n- **Step 5 — Symbolic analysis:** The chosen back-end applies its algorithm (SAT, BDD, induction) on the STS.\n- **Step 6 — Report:** The tool outputs either **VERIFIED** (property holds), **FAILED** with a counter-example trace, or **UNKNOWN** if the bound is insufficient.\n\n## 2.3 Formal Foundations — The Underlying Logic\n\nSAL's verification is grounded in classical logic. The key constructs the back-ends manipulate are:\n\n### KTU Formula Sheet / Cheat Sheet\n\n| Concept | Symbolic Form | Meaning / Use |\n|---|---|---|\n| State | $s \\in S$ | A valuation of all program variables |\n| Initial States | $I(s)$ | Predicate true exactly for starting states |\n| Transition Relation | $T(s, s')$ | True iff system can move from $s$ to $s'$ in one step |\n| Reachable States (depth $k$) | $R_k(s) = I(s) \\vee \\bigvee_{i=0}^{k-1} \\text{Post}^i(I)(s)$ | States reachable in $\\leq k$ steps |\n| Property Violation | $\\phi(s) = \\text{true}$ at an \"error\" state | Counter-example witness |\n| BMC Unrolling (depth $k$) | $I(s_0) \\wedge \\bigwedge_{i=0}^{k-1} T(s_i, s_{i+1}) \\wedge \\bigvee_{i=0}^{k} \\lnot\\phi(s_i)$ | A SAT instance; satisfiable $\\Rightarrow$ bug found |\n| $k$-Induction Base | $\\forall s_0,\\dots,s_k : I(s_0) \\wedge \\bigwedge T(s_i,s_{i+1}) \\Rightarrow \\bigvee \\phi(s_i)$ | Property holds for $k$ steps from initial state |\n| $k$-Induction Step | $\\forall s_0,\\dots,s_k : \\bigwedge_{i=0}^{k-1}\\phi(s_i) \\wedge \\bigwedge T(s_i,s_{i+1}) \\Rightarrow \\phi(s_k)$ | If property holds for $k$ steps it continues to hold |\n| Liveness (Fair CTL*) | $\\text{AF } p$ or $\\text{A[ } p \\text{ U } q \\text{ ]}$ | Eventual / until properties in `sal-smc` |\n\n> [!NOTE]\n> **For KTU answers:** When asked to explain SAL's approach, always mention that it uses **symbolic encodings** (Boolean formulas, BDDs, or SAT clauses) so that it can handle state spaces with **$10^{20}$ states or more**, far beyond what explicit-state tools can manage.\n\n## 2.4 Real-World Engineering Utility\n\n- **Embedded & safety-critical software** — verifying mutual exclusion, absence of deadlock, and timing constraints in avionics and railway control.\n- **Protocol verification** — security protocols, communication protocols, and cache-coherence protocols.\n- **Hardware verification** — micro-architectural models and pipeline correctness.\n- **Pedagogical value** — widely adopted in graduate courses on formal methods because one model can be analysed with several engines for comparison.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Analytical Matrix: How a SAL Back-End Reaches a Verdict\n\nThe following exhaustive walkthrough shows precisely how SAL's principal back-ends answer the question *\"Can an error state be reached?\"*. This is the **analytical core** of every KTU 14-mark question on SAL.\n\n### 3.1.1 The Bounded Model Checking (sal-bmc) Pipeline\n\nConsider a system with initial-state predicate $I$, transition relation $T$, and property $\\phi$ we want to prove unreachable. SAL's `sal-bmc` performs the following for a user-specified depth $k$:\n\n- **Step 1 — Unroll $I$ at $s_0$:** Take $I(s_0)$, the predicate describing the initial state at step 0.\n- **Step 2 — Unroll $T$ for $k$ steps:** Generate the conjunction of $k$ copies of the transition relation:\n\n$$\nT(s_0,s_1) \\wedge T(s_1,s_2) \\wedge \\dots \\wedge T(s_{k-1}, s_k)\n$$\n\n- **Step 3 — Negate $\\phi$ at every step:** Form the disjunction $\\bigvee_{i=0}^{k} \\lnot\\phi(s_i)$, expressing *\"property $\\phi$ is violated somewhere in the first $k$ steps\"*.\n- **Step 4 — Build the SAT instance:** Combine the three components into a single Boolean formula:\n\n$$\n\\Phi_k \\;\\equiv\\; I(s_0) \\;\\wedge\\; \\bigwedge_{i=0}^{k-1} T(s_i, s_{i+1}) \\;\\wedge\\; \\bigvee_{i=0}^{k} \\lnot\\phi(s_i)\n$$\n\n- **Step 5 — Invoke the SAT solver:** The propositional formula $\\Phi_k$ is passed to an underlying SAT solver (ICS, CVC3, etc.).\n- **Step 6 — Interpret the result:**\n   - If $\\Phi_k$ is **satisfiable** $\\Rightarrow$ the SAT solver returns a satisfying assignment. SAL decodes this into a concrete counter-example trace: $s_0 \\to s_1 \\to \\dots \\to s_j$ where $\\lnot\\phi(s_j)$ holds. Result: **FAILED with counter-example**.\n   - If $\\Phi_k$ is **unsatisfiable** $\\Rightarrow$ no bug exists within $k$ steps. SAL reports **VERIFIED up to depth $k$** (not a full proof).\n- **Step 7 — Increment $k$ if needed:** The user may increase $k$ and repeat. Note that BMC alone *cannot* prove correctness for all depths unless complemented by $k$-induction.\n\n### 3.1.2 The $k$-Induction (sal-wmc) Pipeline\n\nTo turn BMC into a *full proof*, SAL's `sal-wmc` adds two inductive obligations:\n\n- **Base Case (Base-$k$):** Property $\\phi$ holds in every reachable state up to depth $k$:\n\n$$\n\\text{Base}_k \\;\\equiv\\; \\forall s_0,\\dots,s_k \\;:\\; I(s_0) \\wedge \\bigwedge_{i=0}^{k-1} T(s_i, s_{i+1}) \\;\\Rightarrow\\; \\bigwedge_{i=0}^{k} \\phi(s_i)\n$$\n\n- **Inductive Step (Step-$k$):** If $\\phi$ holds for $k$ consecutive states reachable by transitions, then it holds at the $k$-th next state:\n\n$$\n\\text{Step}_k \\;\\equiv\\; \\forall s_0,\\dots,s_k \\;:\\; \\bigwedge_{i=0}^{k-1}\\phi(s_i) \\wedge \\bigwedge_{i=0}^{k-1} T(s_i, s_{i+1}) \\;\\Rightarrow\\; \\phi(s_k)\n$$\n\n- **Decision Logic in SAL:**\n\n$$\n\\text{If } \\text{Base}_k \\text{ is unsat AND } \\text{Step}_k \\text{ is unsat, then } \\phi \\text{ is a full invariant.}\n$$\n\n- **Why this works:** If the property holds for the first $k$ reachable steps **and** whenever it has held for $k$ steps in any reachable trajectory it continues to hold, then by induction on path length $\\phi$ is true everywhere — including infinite paths.\n\n### 3.1.3 The Symbolic Model Checking (sal-smc) Pipeline\n\nFor finite-state systems, `sal-smc` follows the classical symbolic CTL model-checking approach:\n\n- **Step 1 — Encode the transition relation as a BDD:** SAL converts $T$ into a *Binary Decision Diagram* (BDD) using the CUDD library.\n- **Step 2 — Compute reachable states via BDD image computation:**\n\n$$\nR_{i+1} \\;=\\; R_i \\cup \\text{Image}(R_i, T)\n$$\n\nwhere $\\text{Image}(R, T) = \\exists s : R(s) \\wedge T(s, s')$, computed symbolically as a BDD operation.\n\n- **Step 3 — Fixpoint detection:** Continue until $R_{i+1} = R_i$. This BDD represents the *exact* set of all reachable states.\n- **Step 4 — Property check:** Check whether any reachable state violates $\\phi$ by computing $R \\cap \\lvert\\phi\\vert$ (a BDD conjunction). If the resulting BDD is **FALSE**, the property holds on all reachable states — SAL outputs **VERIFIED**.\n\n## 3.2 Exhaustive Worked Example — Mutual Exclusion in SAL\n\nConsider two processes $P_1$ and $P_2$ competing for a critical section. The property to prove is that they are never *both* in the critical section simultaneously. Using SAL-style reasoning:\n\n- **Step 1 — Identify state variables.** For each process, a state variable $x_i \\in \\{N, T, C\\}$ denoting \"Non-critical\", \"Trying\", and \"Critical\". Add a shared Boolean flag $y$ (or use a turn variable).\n- **Step 2 — Write the transition relation.** The transition $\\text{trans}(P_1, P_2)$ allows $P_1$ to enter $C$ only if the other is not in $C$.\n- **Step 3 — Express the property as a SAL module.** A monitor module observes that $x_1 = C \\wedge x_2 = C$ is unreachable.\n- **Step 4 — Run `sal-bmc` with $k = 10$.** SAL builds the SAT instance $\\Phi_{10}$; if a schedule exists that lets both processes enter $C$, it is returned as a counter-example.\n- **Step 5 — Run `sal-wmc` for the full proof.** SAL attempts Base-$k$ and Step-$k$; if both are unsatisfiable, mutual exclusion is *proven* for all runs.\n- **Step 6 — Run `sal-smc`** to compute the exact reachable state set as a BDD and confirm visually that no reachable state has $(x_1, x_2) = (C, C)$.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 SAL Tool Architecture — High-Level Block Diagram\n\nThe following Mermaid block diagram maps the **complete functional architecture of the SAL framework**, showing how the language front-end feeds multiple back-end analysis engines.\n\n```mermaid\nflowchart TB\n    userInput[\"User Authored SAL Model<br/>Context and Module files\"]\n    parser[\"SAL Front End Parser<br/>Type Checking and Scope Analysis\"]\n    stsGen[\"SAL Transition System STS<br/>Internal Intermediate Representation\"]\n    propSpec[\"Property Specification<br/>Observer Module or Assertion\"]\n    dispatcher[\"Tool Dispatcher<br/>User selects back end via command line\"]\n    bmc[\"sal bmc<br/>Bounded Model Checker SAT Based\"]\n    infbmc[\"sal inf bmc<br/>Infinite BMC Parameterized k\"]\n    wmc[\"sal wmc<br/>Well Founded k Induction and PDR\"]\n    smc[\"sal smc<br/>Symbolic Model Checker BDD based\"]\n    sim[\"sal sim<br/>Random and Interactive Simulator\"]\n    satSolver[\"External SAT Solvers<br/>ICS and CVC3\"]\n    cuddBdd[\"CUDD BDD Library<br/>Symbolic Encoding Engine\"]\n    resultVerifier[\"Result Interpreter<br/>Verified or Counter Example\"]\n    outputFinal[\"Final Output to User<br/>PASS or FAIL with Trace\"]\n\n    userInput --> parser\n    parser --> stsGen\n    propSpec --> stsGen\n    stsGen --> dispatcher\n    dispatcher --> bmc\n    dispatcher --> infbmc\n    dispatcher --> wmc\n    dispatcher --> smc\n    dispatcher --> sim\n    bmc --> satSolver\n    wmc --> satSolver\n    infbmc --> satSolver\n    smc --> cuddBdd\n    bmc --> resultVerifier\n    infbmc --> resultVerifier\n    wmc --> resultVerifier\n    smc --> resultVerifier\n    sim --> resultVerifier\n    resultVerifier --> outputFinal\n```\n\n## 4.2 SAL Verification Workflow — Sequence of Operations\n\nThis Mermaid diagram captures the **operational sequence** followed when a user invokes SAL with a particular back-end to verify a property.\n\n```mermaid\nflowchart LR\n    startNode[\"Start User has SAL file mymodel sal\"]\n    stepA[\"Step A Write Context declaring variables and types\"]\n    stepB[\"Step B Write Module defining INITIALIZATION and TRANSITION\"]\n    stepC[\"Step C Write Observer module for property phi\"]\n    stepD[\"Step D Choose back end sal bmc or sal wmc or sal smc\"]\n    stepE[\"Step E Front end parses and builds STS representation\"]\n    stepF[\"Step F Back end applies symbolic algorithm\"]\n    stepG[\"Step G SAT solver or BDD engine evaluates formula\"]\n    stepH{\"Decision<br/>Is formula satisfiable\"}\n    stepI[\"Output VERIFIED<br/>Property holds for given bound\"]\n    stepJ[\"Output FAILED with counter example trace\"]\n    stepK[\"User decides increase k or try another back end\"]\n\n    startNode --> stepA\n    stepA --> stepB\n    stepB --> stepC\n    stepC --> stepD\n    stepD --> stepE\n    stepE --> stepF\n    stepF --> stepG\n    stepG --> stepH\n    stepH -- Satisfiable --> stepJ\n    stepH -- Unsatisfiable --> stepI\n    stepJ --> stepK\n    stepI --> stepK\n```\n\n## 4.3 Comparison of SAL Back-Ends — Selection Guide\n\nThis Mermaid block diagram helps a verifier **choose the right SAL tool** based on the nature of the system and the verification goal.\n\n```mermaid\nflowchart TB\n    decision1{\"Is the system<br/>infinite state\"}\n    decision2{\"Is the goal bug finding<br/>or full proof\"}\n    finitePath[\"System is finite state\"]\n    infinitePath[\"System is infinite state\"]\n    bugPath[\"Goal is bug finding\"]\n    proofPath[\"Goal is full proof\"]\n    chooseSmc[\"Use sal smc<br/>BDD based exhaustive check\"]\n    chooseBmc[\"Use sal bmc<br/>Bounded Model Checking\"]\n    chooseInfBmc[\"Use sal inf bmc<br/>Parametric BMC\"]\n    chooseWmc[\"Use sal wmc<br/>k induction and PDR\"]\n    chooseSim[\"Use sal sim<br/>Quick simulation for sanity\"]\n\n    decision1 -- No --> finitePath\n    decision1 -- Yes --> infinitePath\n    finitePath --> decision2\n    infinitePath --> decision2\n    decision2 -- Bug finding --> bugPath\n    decision2 -- Full proof --> proofPath\n    bugPath --> chooseBmc\n    proofPath --> chooseSmc\n    infinitePath --> chooseInfBmc\n    infinitePath --> chooseWmc\n    finitePath --> chooseSim\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Question 1 `[KTU University Exam - Dec 2023 Model]`\n**Define the Symbolic Analysis Laboratory (SAL). List any four analysis back-end tools available in the SAL framework.**\n\n**Model Answer:**\n\nThe Symbolic Analysis Laboratory (SAL) is a model-checking framework developed at SRI International's Computer Science Laboratory for the symbolic analysis of state-transition systems. It provides a common intermediate language to describe transition systems and integrates multiple analysis engines over a uniform context.\n\nFour back-end tools in SAL are:\n- **sal-bmc** — Bounded Model Checker (SAT-based).\n- **sal-inf-bmc** — Infinite Bounded Model Checker with parameterized $k$.\n- **sal-wmc** — Well-Founded Model Checker using $k$-induction and PDR.\n- **sal-smc** — Symbolic Model Checker using BDDs.\n- **sal-sim** — Simulator for random and interactive execution. *(Any four of the above.)*\n\n---\n\n### Question 2 `[KTU University Exam - Jun 2024 Model]`\n**Explain the role of the `CONTEXT` and `MODULE` constructs in a SAL model. How do they differ?**\n\n**Model Answer:**\n\n- A **CONTEXT** in SAL is a top-level declarative block that defines the **types, constants, and global variables** shared by all modules. It does not itself contain transitions; it is the *static* description of the data.\n- A **MODULE** in SAL encapsulates the **dynamic behaviour** of a component. It contains an `INITIALIZATION` predicate (specifying the start states) and a `TRANSITION` relation (specifying one-step moves).\n- **Key difference:** the `CONTEXT` describes *what exists* (the universe of variables and types), while the `MODULE` describes *how it evolves* (the state changes).\n\n---\n\n## 5.2 Part B Questions (14 Marks Each) — Module Internal Choice\n\n### Question A (14 Marks) `[KTU University Exam - Dec 2023]`\n\n**(a)** *(7 marks)* Explain the architecture of the SAL tool suite. With a neat block diagram, describe how the SAL front-end, intermediate representation, and back-end analysis engines interact during a typical verification run.\n\n**(b)** *(7 marks)* Compare the techniques used by **sal-bmc** and **sal-wmc**. Show how the Bounded Model Checking formula $\\Phi_k$ is constructed, and explain how $k$-induction extends BMC to a full proof by adding the Base-$k$ and Step-$k$ obligations.\n\n**Model Answer for (a):**\n\nThe SAL tool suite is built as a **two-layer architecture** — a *front-end language* and a *family of back-end analysis engines* — both linked through a common intermediate representation called the **SAL Transition System (STS)**.\n\n- **Front-end (Language Layer):** Accepts `.sal` source files containing `CONTEXT` and `MODULE` blocks. The parser performs lexical analysis, type checking, and name resolution, then produces the STS — a normalized representation of variables, initial states, and the transition relation.\n- **Back-end (Analysis Layer):** A dispatcher selects one of `sal-bmc`, `sal-inf-bmc`, `sal-wmc`, `sal-smc`, or `sal-sim` based on the user's command-line choice. Each back-end manipulates the STS using its native algorithm:\n  - `sal-bmc` and `sal-wmc` translate the STS into a propositional formula and hand it to an external SAT solver.\n  - `sal-smc` translates the STS into BDDs using the CUDD library and performs image computation.\n  - `sal-sim` interprets the STS as a runnable transition system.\n- **Result interpretation:** Each back-end returns a verdict — `VERIFIED`, `FAILED` with a counter-example trace, or `UNKNOWN` — which SAL formats for the user.\n\nThe block diagram (see SECTION 4.1) shows the flow: **SAL file → Parser → STS → Dispatcher → chosen back-end → external engine (SAT/BDD) → result → user output**.\n\n**Model Answer for (b):**\n\n- **sal-bmc (Bounded Model Checker):**\n  - Technique: SAT-based bounded model checking.\n  - Input: A bound $k$ from the user.\n  - Procedure: Builds a propositional formula $\\Phi_k$ that is satisfiable **iff** there is a path of length $\\leq k$ that violates the property.\n  - Strength: Excellent at finding deep bugs quickly.\n  - Limitation: Cannot prove the property for unbounded systems — it only certifies correctness *up to depth $k$*.\n\n- **sal-wmc (Well-Founded Model Checker):**\n  - Technique: $k$-induction (and Property-Directed Reachability) on top of SAT solving.\n  - Procedure: Verifies the property for *all* depths by checking two obligations:\n     - **Base-$k$:** Property holds for the first $k$ reachable steps.\n     - **Step-$k$:** If the property holds for $k$ consecutive reachable states, it continues to hold at the next state.\n  - Strength: Can produce a *full proof* of safety for many infinite-state and parameterized systems.\n  - Limitation: Requires choosing the right $k$; may need $k$ to be large.\n\n- **Construction of $\\Phi_k$ in BMC:**\n\n$$\n\\Phi_k \\;\\equiv\\; I(s_0) \\;\\wedge\\; \\bigwedge_{i=0}^{k-1} T(s_i, s_{i+1}) \\;\\wedge\\; \\bigvee_{i=0}^{k} \\lnot\\phi(s_i)\n$$\n\nThe first conjunct encodes the initial state; the second encodes $k$ consecutive transitions; the third asserts that the property $\\phi$ is violated at some step within the unrolling.\n\n- **How $k$-induction extends BMC:**\n  - BMC alone returns `VERIFIED up to k` when $\\Phi_k$ is unsatisfiable. This is *not* a proof for all runs.\n  - $k$-induction adds two **first-order** queries that, when both unsatisfiable, imply the property holds for *every* path length.\n  - The combination **sal-bmc + Base-$k$ + Step-$k$** = full verification of safety properties.\n\n---\n\n### Question B (14 Marks) `[KTU University Exam - Jun 2024]`\n\n**(a)** *(7 marks)* With an example, describe the structure of a SAL model file. Show how variables, types, initial state, and transition relation are declared in a typical SAL context and module.\n\n**(b)** *(7 marks)* Explain the verification methodology used by **sal-smc**. Discuss how BDD-based image computation enables exhaustive verification of finite-state systems and contrast this with the SAT-based approach of sal-bmc.\n\n**Model Answer for (a):**\n\nA typical SAL model file consists of **two main blocks**: a `CONTEXT` for the static description and one or more `MODULE`s for the dynamic behaviour.\n\n**Example — A simple counter:**\n\n```sal\nCONTEXT\n  TYPE  count  = [0..3];\nEND\n\nMODULE counter\n  OUTPUT  c : count\n  INITIALIZATION  c = 0\n  TRANSITION\n    c' = (c + 1) mod 4\nEND\n```\n\n- **CONTEXT block:** Declares the abstract type `count` as the integer range 0 to 3.\n- **MODULE `counter`:**\n  - Declares the **output variable** `c` of type `count`.\n  - The `INITIALIZATION` clause sets the starting value $c = 0$.\n  - The `TRANSITION` clause defines the next-state relation $c' = (c + 1) \\bmod 4$.\n\n**A more realistic mutual-exclusion example (Peterson-style sketch):**\n\n```sal\nCONTEXT\n  TYPE  state = {idle, trying, critical};\nEND\n\nMODULE process1\n  OUTPUT  s1 : state\n  INITIALIZATION  s1 = idle\n  TRANSITION\n    s1' IN\n      {idle   : TRUE;             -- non-deterministic move\n       trying : s1 = idle;\n       critical: s1 = trying};\nEND\n```\n\n- **Key observations:**\n  - **Types** in SAL can be enumerated (`{idle, trying, critical}`), bounded integers, Booleans, arrays, or records.\n  - **Initialization** is a predicate over the state variables; non-determinism is allowed.\n  - **Transitions** can use guarded commands, non-deterministic choice (`IN { ... }`), and primed variables (`s1'`) to denote the next state.\n\n**Model Answer for (b):**\n\n- **The sal-smc verification methodology:**\n  - `sal-smc` is a **symbolic model checker** targeted at *finite-state* systems. It encodes the entire state space using **Binary Decision Diagrams (BDDs)**, with the CUDD library as the underlying engine.\n  - The transition relation $T(s, s')$, initial states $I(s)$, and the property $\\phi(s)$ are all represented as BDDs.\n  - The reachable-state set is computed iteratively using **BDD image computation**:\n\n$$\nR_0 \\;=\\; I, \\qquad R_{i+1} \\;=\\; R_i \\cup \\text{Image}(R_i, T)\n$$\n\nwhere $\\text{Image}(R, T) = \\exists v.\\, R(v) \\wedge T(v, v')$ is a Boolean quantification computed symbolically as a BDD operation.\n\n  - The iteration continues until a **fixpoint** $R_{i+1} = R_i$ is reached. The final BDD represents the exact set of all reachable states.\n  - The property check reduces to a BDD conjunction: $R \\wedge \\lnot\\phi$. If this is the **FALSE** BDD, the property holds on all reachable states — SAL prints `VERIFIED`. Otherwise, a counter-example can be extracted from the satisfying assignment of $R \\wedge \\lnot\\phi$.\n\n- **BDD-based image computation vs. SAT-based BMC:**\n  - **BDD approach (`sal-smc`):**\n     - Operates on *sets* of states encoded as Boolean functions.\n     - Provides a *complete* answer: VERIFIED or counter-example, for the entire finite state space.\n     - Strength: elegant, handles concurrency and fairness naturally.\n     - Weakness: BDD size can blow up in the worst case (the *state-space explosion* issue).\n  - **SAT approach (`sal-bmc`):**\n     - Operates on a *single* unrolled path of length $k$.\n     - Provides a *bounded* answer: correct up to depth $k$ only.\n     - Strength: scales well in practice for bug-finding; SAT solvers have improved dramatically.\n     - Weakness: cannot prove unbounded correctness without induction.\n  - **Complementarity in SAL:** The SAL framework offers both styles precisely because each excels in different scenarios — BDDs for *finite-state proofs*, SAT for *deep bug-hunting*.\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mistakes KTU Students Make on SAL Questions (and How to Avoid Them):**\n> - **Confusing `sal-bmc` with `sal-smc`.** A frequent error is claiming that `sal-bmc` gives a *full proof*. It does not — it only certifies the property *up to bound $k$*. Only $k$-induction (`sal-wmc`) or BDD model checking (`sal-smc`) can yield a full proof.\n> - **Writing generic definitions of \"model checking\" instead of SAL-specific content.** Examiners deduct marks if the answer does not mention SAL's back-ends, the STS representation, or the front-end/back-end split.\n> - **Omitting the formula $\\Phi_k$.** For 7-mark sub-parts asking about BMC, the unrolling formula is *expected* — write it explicitly using the $I, T, \\phi$ notation.\n> - **Mixing up `CONTEXT` and `MODULE` semantics.** The `CONTEXT` is *static* (types and shared variables); the `MODULE` is *dynamic* (initialization and transitions). Examiners check this distinction carefully.\n> - **Forgetting to mention the external engines.** SAL relies on third-party solvers (ICS, CVC3, CUDD). A complete answer references these.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n- **SAL = Symbolic Analysis Laboratory**, developed at **SRI International**.\n- It is a **framework**, not a single tool — it bundles a *language* with *five back-end analyzers*.\n- **Front-end:** parses `.sal` files containing `CONTEXT` (types/variables) and `MODULE` (initialization/transitions), producing the **SAL Transition System (STS)**.\n- **Back-end tools:**\n   - `sal-bmc` — SAT-based Bounded Model Checking.\n   - `sal-inf-bmc` — Infinite BMC with parameterized $k$.\n   - `sal-wmc` — $k$-induction and PDR (full proofs).\n   - `sal-smc` — BDD-based symbolic model checking (using CUDD).\n   - `sal-sim` — Random/interactive simulator.\n- **BMC formula:**\n\n$$\n\\Phi_k \\;\\equiv\\; I(s_0) \\wedge \\bigwedge_{i=0}^{k-1} T(s_i, s_{i+1}) \\wedge \\bigvee_{i=0}^{k} \\lnot\\phi(s_i)\n$$\n\n- **$k$-Induction requires two obligations:** `Base_k` (property holds for first $k$ steps) and `Step_k` (property persists from $k$ to $k+1$).\n- **BDD image computation:**\n\n$$\nR_{i+1} = R_i \\cup \\text{Image}(R_i, T)\n$$\n\n- **Verification flow:** Author → Parse → STS → Dispatcher → Back-end → External engine (SAT/BDD) → Result.\n- **Strength:** Uniform description, multiple engines, handles finite and infinite states.\n- **Limitation:** BDD blow-up for smc; bounded verification for bmc without induction.\n- **Real-world use:** embedded systems, communication protocols, hardware pipelines, safety-critical software.\n- **For KTU answers:** always tie the discussion to a *specific* SAL back-end, mention the underlying solver, and (where applicable) write the formal $\\Phi_k$ or fixpoint equation.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 4-t2",
          "title": "The Language of SAL - The Expression Language",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 4\n> - **Topic:** The Language of SAL - The Expression Language\n\n<!-- SECTION_1_START -->\n# Section 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of the SAL Expression Language\n\nThe **SAL Expression Language** (often abbreviated as `sal-exp`) is the foundational sub-language of the **Symbolic Analysis Laboratory (SAL)** framework developed by SRI International. It defines the type system, the logic of expressions, the syntactic binders, and the modular structure (theories, contexts, and modules) that are reused by the SAL transition-system language (`sal-tls`), the assertion language, and the environment language (`sal-env`).\n\n> [!NOTE]\n> **KTU Syllabus Highlight (PECNT526 – Module 4):**\n> The Expression Language is a *first-order, strongly typed, polymorphic* logical language. It is the layer on top of which transition systems, properties (LTL/CTL), and proofs are constructed. Mastering `sal-exp` is mandatory before studying the `sal-tls` transition-system language.\n\nFormally, `sal-exp` is built from three syntactic categories:\n\n1. **Types** – Boolean, integer, real, enumeration, tuple, record, array, set.\n2. **Expressions** – constructed from constants, variables, function applications, and operators.\n3. **Modules** – reusable collections of declarations packaged as `THEORY`, `CONTEXT`, or `MODULE`.\n\n> [!IMPORTANT]\n> The Expression Language is **decoupled** from the transition-system language. A SAL model is *always* layered: a transition system is built on top of an `sal-exp` context. This separation is what allows SAL tools to share decision procedures across different model checkers and theorem provers.\n\n## 1.2 Conceptual Analogy / Intuition\n\nThink of `sal-exp` as the **\"vocabulary and grammar rules\"** of a mathematics textbook:\n\n- **Types** are like the \"kinds of objects\" the textbook talks about — *numbers*, *sets*, *lists*, *records*.\n- **Expressions** are the \"sentences\" built using these objects and the allowed operations.\n- **Theories and Contexts** are like the \"chapters\" that group related vocabulary and axioms together so that later chapters (transition systems, properties) can simply *import* them.\n\n> [!TIP]\n> Just as you cannot write `x + apple` in a math problem, in SAL you cannot add a `Bool` to an `Integer`. The expression language's type checker is the \"grammatical referee\" that rejects such nonsense **at compile time** before any verification is even attempted.\n\n## 1.3 Physical & Logical Constants Used\n\nThe language is purely logical; the only \"constants\" are the boolean values, integers, reals, and user-declared literals. The Boolean values are the literals **TRUE** and **FALSE**.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# Section 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Type System of `sal-exp`\n\nSAL is *strongly* and *statically* typed. The type checker is invoked *before* model checking or theorem proving begins. The complete set of primitive and composite types is enumerated below.\n\n### 2.1.1 Primitive (Built-in) Types\n\n| Type | Domain | Notation in SAL | Sample Literal |\n|------|--------|----------------|----------------|\n| Boolean | $\\{ \\text{TRUE}, \\text{FALSE} \\}$ | `BOOLEAN` | `TRUE`, `FALSE` |\n| Integer | $\\mathbb{Z}$ (mathematical integers) | `INTEGER` | `0`, `-7`, `42` |\n| Real | $\\mathbb{R}$ (mathematical reals) | `REAL` | `0.0`, `3.14` |\n| Enumeration | A user-defined finite set | `TYPE name = { a, b, c }` | `a` |\n\n### 2.1.2 Composite (Constructor) Types\n\n| Type | Syntax | Meaning | Intuition |\n|------|--------|---------|-----------|\n| Tuple | `[T1, T2, ..., Tn]` | Cartesian product $T_1 \\times T_2 \\times \\cdots \\times T_n$ | A fixed-size heterogeneous list. |\n| Record | `[# field1 : T1; field2 : T2 #]` | Named-field product | A struct in C, a row in a database. |\n| Array | `ARRAY L OF T` | Mapping from index type $L$ to element type $T$ | An infinite map / lookup table. |\n| Set | `SET T` | Powerset of $T$ | The set of all elements of type $T$. |\n\n> [!IMPORTANT]\n> **Key Distinction — Array vs. Set:**\n> - `ARRAY INTEGER OF BOOLEAN` is the type of *all* functions $f : \\mathbb{Z} \\to \\mathbb{B}$.\n> - `SET BOOLEAN` is the type of sets whose elements are booleans (i.e., either $\\{\\}$, $\\{\\text{TRUE}\\}$, $\\{\\text{FALSE}\\}$, or $\\{\\text{TRUE}, \\text{FALSE}\\}$).\n> These are *not* the same and the type checker will distinguish them.\n\n## 2.2 Expression Forms\n\nExpressions are the *terms* of the SAL logic. They are categorized as:\n\n### 2.2.1 Atomic Expressions\n- **Constants** (e.g., `0`, `TRUE`, `c1` where `c1` is declared as a constant).\n- **Variables** (e.g., `x`, `state.counter`).\n- **Function applications** `f(e1, ..., en)`.\n\n### 2.2.2 Compound Expressions\n- **Arithmetic:** `+`, `-`, `*`, `div`, `mod`, `/` (for reals).\n- **Comparison:** `=`, `/=`, `<`, `<=`, `>`, `>=`.\n- **Boolean:** `AND`, `OR`, `NOT`, `=>` (implication), `<=>` (iff).\n- **Tuple / Record projections:** `e.1`, `e.field`.\n- **Conditional:** `IF b THEN e1 ELSE e2 ENDIF`.\n\n### 2.2.3 Binder Expressions\n- **`LET x = e1 IN e2`** — local binding; $e_2$ is evaluated with $x$ replaced by $e_1$.\n- **`FORALL (x : T) : P`** — universal quantification: $\\forall x \\in T.\\,P$.\n- **`EXISTS (x : T) : P`** — existential quantification: $\\exists x \\in T.\\,P$.\n- **`LAMBDA (x : T) : e`** — anonymous function: $\\lambda x : T.\\,e$.\n\n## 2.3 Module Structure: THEORY, CONTEXT, MODULE\n\n| Construct | Keyword | Purpose | What it Contains |\n|-----------|---------|---------|------------------|\n| Theory | `THEORY` | Pure, axiomatic math | Type declarations, constants, axioms, function symbols. No state. |\n| Context | `CONTEXT` | Parameterised theory | Generic theories with placeholder types/constants. |\n| Module | `MODULE` | Stateful entity | Variables, initialisation, transitions, definitions. (Used in `sal-tls`.) |\n\n## 2.4 Context Relations (Inclusion, Extension, Instantiation)\n\nA theory $T_2$ can *use* a theory $T_1$ via:\n\n- **Inclusion:** `T2 = T1 + extra_decls`  $\\;\\Rightarrow\\;$  $T_2$ has all of $T_1$'s declarations.\n- **Extension:** Same as inclusion but adds new declarations.\n- **Instantiation:** `INSTANCE T1 WITH ..., ...`  $\\;\\Rightarrow\\;$  copy $T_1$ with concrete types replacing parameters.\n\n## 2.5 KTU Formula / Cheat Sheet\n\n| # | Construct | SAL Syntax | Mathematical Reading |\n|---|-----------|------------|----------------------|\n| 1 | Type declaration | `TYPE name = ...` | Introduce a new type. |\n| 2 | Constant | `c : T` | $c$ is an uninterpreted element of $T$. |\n| 3 | Function | `f : T1 -> T2` | $f : T_1 \\to T_2$ |\n| 4 | Axiom | `AXIOM ax : P` | Assert $P$ as a global fact. |\n| 5 | Definition | `g(x:T) : T2 = e` | $g(x) \\equiv e$ |\n| 6 | Let | `LET x = e1 IN e2` | $(\\lambda x.\\,e_2)\\,e_1$ |\n| 7 | Forall | `FORALL (x:T) : P` | $\\forall x \\in T.\\,P$ |\n| 8 | Exists | `EXISTS (x:T) : P` | $\\exists x \\in T.\\,P$ |\n| 9 | Lambda | `LAMBDA (x:T) : e` | $\\lambda x : T.\\,e$ |\n| 10 | Conditional | `IF b THEN e1 ELSE e2 ENDIF` | $b \\rightarrow e_1,\\; \\neg b \\rightarrow e_2$ |\n| 11 | Set membership | `x IN S` | $x \\in S$ |\n| 12 | Set cardinality | `card(S)` | $\\vert S \\vert$ |\n\n> [!NOTE]\n> The `card(S)` operator applies to **finite** sets only. Using it on an infinite set yields a type-checking error.\n\n## 2.6 Real-World Engineering Utility\n\nThe SAL expression language is the substrate on which **safety-critical embedded systems** are verified — for example, avionics (DO-178C level A), railway signalling (EN 50128 SIL 4), and cryptographic protocol verification. The strict typing prevents entire classes of specification errors (e.g., confusing a control flag with a counter) that would otherwise produce vacuous or wrong verification results.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# Section 3: Step-by-Step Derivations & Analytical Matrix\n\nThis section walks through the *exact reasoning steps* expected in a KTU university answer script. Three worked examples are provided: (i) a type definition, (ii) a polymorphic function declaration, and (iii) a context instantiation.\n\n---\n\n## 3.1 Worked Example 1 — Declaring an Enumeration and Using It\n\n**Question:** Declare a SAL type `Color` with three values, declare a function `next : Color -> Color` such that red → green, green → blue, blue → red, and write the corresponding definition.\n\n### Step 1 — Declare the Enumeration Type\n\nWe introduce a new type whose domain is a finite set of three distinct labels.\n\n```\nTYPE Color = { red, green, blue }\n```\n\nThe mathematical reading is:\n\n$$ \\text{Color} \\;\\triangleq\\; \\{ \\text{red}, \\text{green}, \\text{blue} \\} $$\n\nThe type checker adds three *distinct* nullary constructors `red`, `green`, `blue` to the current theory.\n\n### Step 2 — Declare the Function Symbol\n\nWe first declare `next` as an *uninterpreted* function symbol of the right arity.\n\n```\nnext : Color -> Color\n```\n\nAt this stage the type checker is satisfied, but the meaning of `next` is unspecified.\n\n### Step 3 — Add the Definition (An Axiomatic Definition)\n\n```\nnext(red)   = green\nnext(green) = blue\nnext(blue)  = red\n```\n\nThese three *equations* are the SAL idiom for a total-function definition. The type checker verifies that:\n\n- The left-hand side `next(c)` has type `Color` for every `c : Color`.\n- The right-hand side is a *closed* expression of type `Color`.\n\nThe system internally conjoins these equations with the implicit assumption that `next` is *total*:\n\n$$\n\\forall c : \\text{Color}.\\; \\exists c' : \\text{Color}.\\; \\text{next}(c) = c'\n$$\n\n---\n\n## 3.2 Worked Example 2 — A Polymorphic Theory and Its Instantiation\n\n**Question:** Define a polymorphic theory `List` parameterised by an element type `T`, containing the constants `nil` and `cons`, then instantiate it with `T = INTEGER`.\n\n### Step 1 — Declare the Parameterised Context\n\n```\nCONTEXT List[T : TYPE] = THEORY\n  BEGIN\n    nil  : List[T];\n    cons : T, List[T] -> List[T];\n    car  : List[T] -> T;\n    cdr  : List[T] -> List[T]\n  END\n```\n\nReading:\n\n- `List[T : TYPE]` introduces a *type parameter* $T$.\n- `cons` is a binary function symbol taking an element of $T$ and a `List[T]` and producing a `List[T]`.\n- `car` and `cdr` are the standard destructors.\n\n### Step 2 — Instantiate the Theory\n\nWe replace the formal parameter `T` with the concrete type `INTEGER`:\n\n```\nIntegerList : THEORY = List[INTEGER]\n```\n\nMathematically, the result is the theory:\n\n$$ \\text{IntegerList} \\;\\triangleq\\; \\text{List} \\langle T := \\text{INTEGER} \\rangle $$\n\nwith the renamed declarations:\n\n- `IntegerList.nil : List[INTEGER]`\n- `IntegerList.cons : INTEGER, List[INTEGER] -> List[INTEGER]`\n- `IntegerList.car : List[INTEGER] -> INTEGER`\n- `IntegerList.cdr : List[INTEGER] -> List[INTEGER]`\n\n### Step 3 — Type Check\n\nFor each declaration the verifier checks:\n\n$$\n\\frac{\\Gamma \\vdash e_1 : T_1 \\quad \\Gamma \\vdash e_2 : T_2}{\\Gamma \\vdash \\text{cons}(e_1, e_2) : \\text{List}[T]}\n$$\n\nThis is a straightforward application of the typing rule for function application, ensuring the polymorphic substitution is **type-preserving**.\n\n---\n\n## 3.3 Worked Example 3 — A Bounded Universal Quantifier\n\n**Question:** Express in SAL the statement: *\"Every integer in a given set S is non-negative.\"* Let $S : \\text{SET INTEGER}$.\n\n### Step 1 — Identify the Quantified Variable and its Type\n\nThe variable ranges over the elements of $S$, but in SAL we can quantify directly over the base type and use a membership predicate:\n\n- Variable: $x$\n- Type: `INTEGER`\n- Body: $x \\ge 0$\n\n### Step 2 — Restrict the Quantification to the Set\n\nSAL does not allow a quantifier to be restricted *directly* to a set, so we use an implication:\n\n$$\n\\forall x \\in \\mathbb{Z}.\\; (x \\in S) \\;\\Rightarrow\\; (x \\ge 0)\n$$\n\n### Step 3 — Write the SAL Expression\n\n```\nFORALL (x : INTEGER) : (x IN S) => (x >= 0)\n```\n\nThe full type-checking judgement is:\n\n$$\n\\frac{\\Gamma, x : \\text{INTEGER} \\vdash (x \\in S) \\Rightarrow (x \\ge 0) : \\text{BOOLEAN}}\n     {\\Gamma \\vdash \\text{FORALL }(x : \\text{INTEGER}) : (x \\in S) \\Rightarrow (x \\ge 0) : \\text{BOOLEAN}}\n$$\n\n---\n\n## 3.4 Analytical Matrix: Common Constructs and Their Semantics\n\n| SAL Construct | Abstract-Syntax Tree Form | Type-Checking Rule | Semantics |\n|---------------|---------------------------|--------------------|-----------|\n| `TRUE`, `FALSE` | leaf | always `BOOLEAN` | truth values |\n| `n` (numeric literal) | leaf | `INTEGER` if integer form, `REAL` if decimal form | numeric constant |\n| `x` (variable) | leaf | type from declaration | variable lookup |\n| `f(e1,...,en)` | `App(f, [e1,...,en])` | $\\Gamma \\vdash e_i : \\tau_i$ and $f : \\tau_1 \\times \\cdots \\times \\tau_n \\to \\sigma$ | function application |\n| `LET x = e1 IN e2` | `Let(x, e1, e2)` | $\\Gamma \\vdash e_1 : \\tau,\\; \\Gamma, x : \\tau \\vdash e_2 : \\sigma$ | $(\\lambda x : \\tau.\\,e_2)\\,e_1$ |\n| `IF b THEN e1 ELSE e2` | `Ite(b, e1, e2)` | $\\Gamma \\vdash b : \\text{BOOLEAN},\\; \\Gamma \\vdash e_1 : \\sigma,\\; \\Gamma \\vdash e_2 : \\sigma$ | classical if-then-else |\n| `FORALL (x : T) : P` | `Forall(x, T, P)` | $\\Gamma \\vdash T : \\text{TYPE},\\; \\Gamma, x : T \\vdash P : \\text{BOOLEAN}$ | $\\forall x \\in T.\\,P$ |\n| `EXISTS (x : T) : P` | `Exists(x, T, P)` | $\\Gamma \\vdash T : \\text{TYPE},\\; \\Gamma, x : T \\vdash P : \\text{BOOLEAN}$ | $\\exists x \\in T.\\,P$ |\n| `LAMBDA (x : T) : e` | `Lambda(x, T, e)` | $\\Gamma, x : T \\vdash e : \\sigma$ | $\\lambda x : T.\\,e$ |\n| `e.1`, `e.2` | `Proj(i, e)` | $\\Gamma \\vdash e : [\\tau_1, ..., \\tau_n]$ with $i \\le n$ | $i$-th projection |\n| `e.field` | `ProjByName(field, e)` | $\\Gamma \\vdash e : [# field : \\tau, ... #]$ | field access |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# Section 4: Structural Diagrams & Schematics\n\n## 4.1 The Three-Layer SAL Architecture\n\nThe following Mermaid block illustrates the architectural relationship between the three sub-languages of SAL and how the Expression Language sits at the foundation.\n\n```mermaid\nflowchart TB\n    nodeS[\"SAL Framework\"]\n    nodeE[\"sal-env<br/>Environment Language\"]\n    nodeT[\"sal-tls<br/>Transition System Language\"]\n    nodeX[\"sal-exp<br/>Expression Language\"]\n    nodeTH[\"Type Checker and<br/>Decision Procedures\"]\n    nodeM[\"SAL Model Checkers<br/>sal-smc sal-inf-bmc<br/>sal-bmc\"]\n    nodeP[\"SAL Provers<br/>sal-pvs sad\"]\n    nodeS --> nodeE\n    nodeS --> nodeT\n    nodeS --> nodeX\n    nodeT --> nodeX\n    nodeE --> nodeX\n    nodeX --> nodeTH\n    nodeTH --> nodeM\n    nodeTH --> nodeP\n```\n\n> [!NOTE]\n> Notice that **both** the transition-system language and the environment language are *downstream* of the expression language. This visually captures the foundational role of `sal-exp`.\n\n## 4.2 Module Organisation of a Typical SAL Specification\n\n```mermaid\nflowchart LR\n    nodeA[\"TH1 basic_arith<br/>THEORY<br/>axioms of plus and times\"]\n    nodeB[\"TH2 list_ops<br/>THEORY = basic_arith +<br/>List declarations\"]\n    nodeC[\"CTX Queue<br/>CONTEXT T : TYPE\"]\n    nodeD[\"MOD Producer<br/>MODULE<br/>state variables and transitions\"]\n    nodeE[\"MOD Consumer<br/>MODULE\"]\n    nodeF[\"MOD System<br/>MODULE = Producer WITH Consumer\"]\n    nodeA --> nodeB\n    nodeB --> nodeC\n    nodeC --> nodeD\n    nodeC --> nodeE\n    nodeD --> nodeF\n    nodeE --> nodeF\n```\n\n## 4.3 Type Hierarchy in the Expression Language\n\n```mermaid\nflowchart TB\n    nodeA[\"SAL Types\"]\n    nodeB[\"Primitive Types<br/>BOOLEAN INTEGER REAL\"]\n    nodeC[\"Enumeration Types<br/>TYPE name = a b c ...\"]\n    nodeD[\"Composite Types\"]\n    nodeE[\"TUPLE T1 T2 ...\"]\n    nodeF[\"RECORD field1 T1 field2 T2 ...\"]\n    nodeG[\"ARRAY L OF T\"]\n    nodeH[\"SET T\"]\n    nodeA --> nodeB\n    nodeA --> nodeC\n    nodeA --> nodeD\n    nodeD --> nodeE\n    nodeD --> nodeF\n    nodeD --> nodeG\n    nodeD --> nodeH\n```\n\n## 4.4 Block Diagram of an Expression's Evaluation\n\n```mermaid\nflowchart LR\n    nodeA[\"Source Expression<br/>FORALL x : T : P\"]\n    nodeB[\"Parser<br/>produces AST\"]\n    nodeC[\"Type Checker<br/>verifies types\"]\n    nodeD[\"Simplifier<br/>beta reduction\"]\n    nodeE[\"Decision Procedure<br/>validity check\"]\n    nodeF[\"Result<br/>VALID INVALID UNKNOWN\"]\n    nodeA --> nodeB\n    nodeB --> nodeC\n    nodeC --> nodeD\n    nodeD --> nodeE\n    nodeE --> nodeF\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# Section 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Question A1 `[KTU University Exam – May 2024]`\n\n**Q:** Define the **SAL Expression Language**. Mention any *three* primitive types it supports.\n\n**Model Answer:**\nThe SAL Expression Language (`sal-exp`) is the typed, first-order, polymorphic logical sub-language of the SAL framework that defines the type system, expressions, binders, and modular packaging (theories, contexts) used throughout SAL.\nThe three primitive types are:\n\n1. **BOOLEAN** — domain $\\{ \\text{TRUE}, \\text{FALSE} \\}$.\n2. **INTEGER** — domain $\\mathbb{Z}$ (mathematical integers, not machine words).\n3. **REAL** — domain $\\mathbb{R}$.\n\n*(3 marks: 1 for definition + 1 for each of the three types)*\n\n---\n\n### Question A2 `[KTU University Exam – Dec 2023]`\n\n**Q:** What is the difference between a `THEORY` and a `CONTEXT` in SAL?\n\n**Model Answer:**\n\n- A **`THEORY`** is a *closed* module containing type declarations, constants, function symbols, and axioms. It has no free type parameters.\n- A **`CONTEXT`** is a *parameterised* theory: it declares formal type or constant parameters (e.g., `T : TYPE`) that must be supplied upon instantiation. A context becomes a theory only after its parameters are bound via the `INSTANCE` keyword.\n\n*(3 marks: 1.5 for theory, 1.5 for context)*\n\n---\n\n## 5.2 Part B Questions (14 Marks Each — Internal Choice Provided)\n\n### Question B-A `[KTU University Exam – Model Paper]`\n\n**A (a)** Explain the SAL type system in detail. Describe the *primitive*, *enumeration*, and *composite* types with one example each. (7 marks)\n\n**A (b)** Consider the SAL declaration:\n\n```\nTHEORY Stack = THEORY\n  BEGIN\n    TYPE Element;\n    s  : ARRAY INTEGER OF Element;\n    top: INTEGER;\n    AXIOM ax_well_formed :\n      FORALL (i : INTEGER) :\n        (i >= 0) => (i <= top);\n  END\n```\n\nWrite the equivalent mathematical specification and explain the role of `FORALL` and `=>` here. (7 marks)\n\n#### Model Solution for A (a)\n\nThe SAL type system is *strongly*, *statically* typed and is the *first* component checked by the SAL tool-chain.\n\n**Primitive Types:** `BOOLEAN`, `INTEGER`, `REAL`. These are built-in; the user does not declare them.\n\n```\nx : BOOLEAN\nn : INTEGER\nr : REAL\n```\n\n**Enumeration Types:** User-defined finite types. Example:\n\n```\nTYPE Day = { Mon, Tue, Wed, Thu, Fri, Sat, Sun }\n```\n\nThe type checker adds seven nullary constructors.\n\n**Composite Types:**\n\n1. **Tuple:** `[T1, T2, ...]` — e.g., `[INTEGER, BOOLEAN]`.\n2. **Record:** `[# name : STRING; age : INTEGER #]` — named fields.\n3. **Array:** `ARRAY L OF T` — total function $L \\to T$.\n4. **Set:** `SET T` — finite subsets of $T$.\n\n#### Model Solution for A (b)\n\nThe mathematical translation is:\n\n$$\n\\Sigma_{\\text{Stack}} = \\begin{cases}\n\\text{Element} : \\text{TYPE} \\\\\ns : \\mathbb{Z} \\to \\text{Element} \\\\\n\\text{top} : \\mathbb{Z} \\\\\n\\text{ax\\_well\\_formed} : \\forall i \\in \\mathbb{Z}.\\; (i \\ge 0) \\Rightarrow (i \\le \\text{top})\n\\end{cases}\n$$\n\n- **`FORALL (i : INTEGER) :`** introduces a *universally quantified* variable $i$ ranging over the mathematical integers.\n- **`=>`** is the *logical implication*. The combined statement says: *\"For every integer $i$, if $i$ is non-negative, then $i$ is at most `top`.\"* This is the well-formedness constraint that the array indices ever used are bounded by `top`.\n\n---\n\n### Question B-B `[KTU University Exam – Model Paper]` *(Internal Alternative)*\n\n**B (a)** With a neat diagram, explain the three-layer architecture of SAL. Discuss the role of the Expression Language as the foundation. (7 marks)\n\n**B (b)** Consider the following SAL specification:\n\n```\nCONTEXT Monoid[T : TYPE] = THEORY\n  BEGIN\n    e  : T;\n    op : [T, T -> T];\n    AXIOM ax_identity :\n      FORALL (x : T) : (op(x, e) = x) AND (op(e, x) = x);\n    AXIOM ax_assoc :\n      FORALL (x : T, y : T, z : T) :\n        op(op(x, y), z) = op(x, op(y, z));\n  END\n```\n\nInstantiate this context for `T = INTEGER` and interpret both axioms in plain English. (7 marks)\n\n#### Model Solution for B (a)\n\nThe three-layer architecture is:\n\n1. **Expression Language (`sal-exp`)** — types, expressions, modules.\n2. **Transition-System Language (`sal-tls`)** — modules with state, initialisation, transitions, definitions. *Imports* an `sal-exp` context.\n3. **Environment Language (`sal-env`)** — combines multiple transition-system modules and asserts properties.\n\n*(7 marks: 2 for diagram, 3 for layer description, 2 for foundational role of `sal-exp`.)*\n\n#### Model Solution for B (b)\n\n**Instantiation:**\n\n```\nIntegerMonoid : THEORY = Monoid[INTEGER]\n```\n\nThis binds the formal type parameter `T` to `INTEGER`, yielding the renamed declarations:\n\n- `IntegerMonoid.e : INTEGER`\n- `IntegerMonoid.op : [INTEGER, INTEGER -> INTEGER]`\n- `IntegerMonoid.ax_identity : FORALL (x : INTEGER) : (op(x, e) = x) AND (op(e, x) = x)`\n- `IntegerMonoid.ax_assoc : FORALL (x, y, z : INTEGER) : op(op(x, y), z) = op(x, op(y, z))`\n\n**Plain-English Interpretation:**\n\n- **`ax_identity`:** \"The element `e` is an *identity* for the binary operation `op` — combining `e` with any `x` on either side leaves `x` unchanged.\"\n- **`ax_assoc`:** \"The binary operation `op` is *associative* — for any three elements `x`, `y`, `z`, the result of applying `op` in left-to-right or any other bracketing order is the same.\"\n\nThese are exactly the monoid axioms of abstract algebra.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Common Pitfall:**\n> 1. **Do not** confuse a `MODULE` (stateful, used in `sal-tls`) with a `THEORY` (stateless, in `sal-exp`). Writing a `THEORY` with `INITIALISATION` will fetch **zero marks** — `INITIALISATION` belongs to `sal-tls`.\n> 2. **Quantifier scope:** When asked to write a quantifier over a *set*, remember SAL only quantifies over *types*. You **must** add the membership predicate inside the body, e.g., `FORALL (x : INTEGER) : (x IN S) => P`.\n> 3. **Type mismatches:** `ARRAY INTEGER OF BOOLEAN` and `SET BOOLEAN` look similar but are *not* interchangeable. Examiners will deduct marks for confusing them.\n> 4. **Polymorphism:** A `CONTEXT` without its `INSTANCE` is *useless*. Always close your answer by showing the instantiation.\n> 5. **Cardinality of infinite sets:** Never write `card(S)` where `S` is of an infinite type — it is a type error and yields 0 marks.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember (Rapid Revision Checklist)\n\n- [ ] `sal-exp` is the *typed, first-order, polymorphic* foundation of the entire SAL framework.\n- [ ] Three primitive types: `BOOLEAN`, `INTEGER`, `REAL`.\n- [ ] User-defined types: `TYPE name = { ... }` (enumerations).\n- [ ] Composite types: tuple `[T1, T2]`, record `[# f : T #]`, array `ARRAY L OF T`, set `SET T`.\n- [ ] Expressions are built from constants, variables, function applications, and operators (`+`, `-`, `*`, `=`, `=>`, `<=>`, `AND`, `OR`, `NOT`).\n- [ ] Binders: `LET x = e1 IN e2`, `FORALL (x : T) : P`, `EXISTS (x : T) : P`, `LAMBDA (x : T) : e`.\n- [ ] Conditional: `IF b THEN e1 ELSE e2 ENDIF`.\n- [ ] Three module kinds: `THEORY` (closed), `CONTEXT` (parameterised), `MODULE` (stateful, in `sal-tls`).\n- [ ] `INSTANCE` binds context parameters to concrete types/constants.\n- [ ] `INCLUDES` / `=` performs theory extension.\n- [ ] `card` applies to **finite** sets only.\n- [ ] Quantifiers range over *types*, not sets — use the `IN` predicate inside the body.\n- [ ] All SAL axioms must be closed *boolean* expressions.\n- [ ] Type checking happens *before* any model checking or theorem-proving begins — the type checker is the \"grammatical referee.\"\n- [ ] The Expression Language sits *below* the Transition-System Language and the Environment Language in the SAL stack.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 4-t3",
          "title": "The Transition Language",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 4\n> - **Topic:** The Transition Language\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of The Transition Language\n\nIn the context of **Automated Verification** under the KTU 2024 scheme, a **Transition Language** is a mathematically rigorous formal notation used to describe the operational behavior of concurrent, reactive, and distributed systems through a set of well-defined **state-transition rules**. The most foundational instance of a transition language is the **Labeled Transition System (LTS)**, which serves as the semantic backbone for model checkers such as **SPIN**, **NuSMV**, **UPPAAL**, and process algebras like **CCS (Calculus of Communicating Systems)** and **CSP (Communicating Sequential Processes)**.\n\nFormally, a **Labeled Transition System** is defined as a 4-tuple:\n\n$$\\mathcal{T} = (S, Act, \\rightarrow, s_0)$$\n\nwhere:\n- $S$ is a non-empty, **finite** set of **states** (the configuration space of the system).\n- $Act$ is a finite set of **observable actions** or **labels** (often denoted $\\Sigma$ or $L$).\n- $\\rightarrow \\subseteq S \\times Act \\times S$ is the **transition relation**.\n- $s_0 \\in S$ is the distinguished **initial state**.\n\nA single transition step is conventionally written as:\n\n$$s \\xrightarrow{a} s'$$\n\nwhich is read as: \"**the system in state $s$ can evolve to state $s'$ by performing the action $a$**\".\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Highlight:** The transition language provides the *semantic model* upon which temporal logic specifications (CTL, LTL, CTL*) are interpreted. Every formula $\\phi$ is evaluated over a structure derived from a transition language artifact.\n\n## 1.2 Conceptual Analogy — Plain English Intuition\n\nImagine you are reading a **subway map of a city**. Each **metro station** on the map represents a *state* of the system. The **colored lines** connecting two stations represent the *actions* (e.g., a train ride, a boarding event, a synchronization). The **rules of movement** — \"you can go from Station A to Station B only by the red line\" — are exactly the **transition rules** of the language.\n\nIn software, every line of code that can change the program's memory configuration is a potential \"station\" and every execution step is a \"ride\". A **Transition Language** formalizes this intuition: it is a precise, mathematical *map* of every legal path the program can take during execution.\n\n> [!NOTE]\n> **Why this matters in industry:** Verification engineers at Intel, Airbus, and NASA use transition languages to certify that microprocessors, flight-control firmware, and rover autopilots cannot enter unsafe configurations. The **CPU pipeline** of a modern processor has on the order of $10^9$ states — and transition languages are what make reasoning about them tractable.\n\n## 1.3 Standard Metrics & Constants\n\nThe following quantities are universally used in KTU-level problems on transition languages:\n\n| Symbol | Meaning | Typical Engineering Value |\n|---|---|---|\n| $\\vert S \\vert$ | Number of reachable states | $10^2$–$10^9$ |\n| $\\vert Act \\vert$ | Alphabet size (number of distinct actions) | $1$–$2^{32}$ |\n| $d(s)$ | Out-degree of state $s$ | $\\leq \\vert S \\vert$ |\n| $\\rightarrow^*$ | Reflexive-transitive closure (paths) | unbounded |\n| $\\mathcal{B}(S)$ | Power set of $S$ ($2^{\\vert S \\vert}$ subsets) | exponential in $\\vert S \\vert$ |\n\n> [!TIP]\n> **State-Space Explosion:** The single most important metric in transition-based verification is that the set of all behaviors grows **exponentially** with the number of concurrent components — this is the *state-space explosion problem* that drives the need for symbolic methods (BDDs, SAT/SMT, partial-order reduction).\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Structural Anatomy of a Transition Rule\n\nA transition rule is the atomic unit of the transition language. It has the form:\n\n$$\\frac{\\text{preconditions (hypotheses)}}{\\text{conclusion}}$$\n\nThe **conclusion** is the *fact being derived* (e.g., $s \\xrightarrow{a} s'$), while the **preconditions** are the *reasoning steps* that must hold for the rule to fire. This is the same **Natural Deduction / Structural Operational Semantics (SOS)** style invented by Gordon Plotkin (1981).\n\n### The Operational Workflow\n\n1. **Identify the syntactic construct** whose behavior must be described (e.g., a process, a statement, an automaton).\n2. **Decompose** the construct into its atomic components.\n3. **Apply the matching transition rule** from the language's inference system.\n4. **Derive the next state** by composing the residual sub-components.\n5. **Repeat recursively** until a terminal (stuck) state or a stable fixed point is reached.\n\n### The Core 'Why' and 'How'\n\n> The transition language is *compositional*: the behavior of a whole system is computed from the behaviors of its parts. This **compositionality** is what allows modular verification — a property proven for a component can be reused when the component is embedded in a larger system.\n\n## 2.2 Classes of Transition Languages\n\nKTU examiners frequently distinguish the following three families:\n\n| Family | Defining Trait | Example System |\n|---|---|---|\n| **LTS (Labeled Transition System)** | Transitions carry observable action labels | Mealy/Moore machines, process algebras |\n| **Kripke Structure** | Transitions are *unlabeled*; states carry atomic proposition labels | CTL model checking (NuSMV) |\n| **Timed Automaton** | Transitions guarded by clock constraints | UPPAAL, real-time verification |\n\nThe mathematical bridge between a Kripke structure and an LTS is given by the **forgetful functor**: a Kripke structure is essentially an LTS over the silent action $\\tau$ with the labels re-attached to states.\n\n## 2.3 KTU Formula Sheet / Cheat Sheet\n\n> [!NOTE]\n> **Notation rule:** To prevent markdown table breakage, the symbol $\\vert$ (absolute value / cardinality) is rendered via `\\vert` inside $\\LaTeX$ math mode, never as a bare pipe character.\n\n| # | Concept | Formal Expression | Remarks / Engineering Use |\n|---|---|---|---|\n| 1 | LTS definition | $\\mathcal{T} = (S, Act, \\rightarrow, s_0)$ | Foundation of every model checker |\n| 2 | Single transition | $s \\xrightarrow{a} s'$ | Atomic step, label $a \\in Act$ |\n| 3 | Path (execution) | $\\pi = s_0 \\xrightarrow{a_0} s_1 \\xrightarrow{a_1} s_2 \\cdots$ | Used in LTL / CTL* semantics |\n| 4 | Reachability | $s \\rightarrow^* s'$ | Fixed-point computation: $\\mu X. \\, \\text{Post}(X) \\cup \\{s_0\\}$ |\n| 5 | Strong bisimulation ($\\sim$) | $\\forall a \\in Act.\\ s \\xrightarrow{a} s' \\Leftrightarrow t \\xrightarrow{a} t''$ and $s' \\sim t''$ | Equivalence of behaviors (CCS) |\n| 6 | Weak bisimulation ($\\approx$) | Hides $\\tau$ (silent) actions | Used in protocol verification |\n| 7 | Simulation pre-order | $s \\preceq t$ | One-directional behavioral refinement |\n| 8 | Number of states | $\\prod_{i=1}^{n} \\vert S_i \\vert$ for $n$ parallel components | **State-space explosion** |\n| 9 | Pre-image operator | $\\text{Pre}_{\\exists}(Q) = \\{s \\mid \\exists s' \\in Q,\\ a \\in Act,\\ s \\xrightarrow{a} s'\\}$ | Backward CTL fixpoint |\n| 10 | Post-image operator | $\\text{Post}_{\\exists}(Q) = \\{s' \\mid \\exists s \\in Q,\\ a \\in Act,\\ s \\xrightarrow{a} s'\\}$ | Forward reachability |\n| 11 | Satisfaction | $\\mathcal{T}, s \\models \\phi$ | System $\\mathcal{T}$ satisfies formula $\\phi$ at $s$ |\n| 12 | State explosion bound | $O(2^{\\vert S \\vert})$ for explicit model checking | Drives symbolic (BDD) methods |\n\n## 2.4 Engineering Utility of the Transition Language\n\n- **Hardware model checking:** Verifying that no deadlocks occur in an arbiter bus.\n- **Software verification:** SPIN's input language **PROMELA** is a transition language for asynchronous protocols.\n- **Communication protocols:** TLS, Bluetooth — verified using LTSs and bisimulation.\n- **Cyber-physical systems:** Timed automata (UPPAAL) verify that an insulin pump never over-doses a patient.\n- **AI safety:** Bounded model checkers encode transformer activations as transition relations.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: From Inductive Transition Rules to the Global LTS\n\n**Problem (KTU-typical, 14-mark style):**\nConsider two communicating processes defined in CCS-like notation:\n\n$$P \\triangleq a.P \\quad\\quad Q \\triangleq \\bar{a}.Q$$\n\nthat synchronize on action $a$ (complementary actions). Construct the global transition system for $P \\parallel Q$.\n\n### Step-by-Step Derivation\n\n**Step 1 — Identify the action alphabet.**\n\n$$Act = \\{a,\\ \\bar{a},\\ \\tau\\}$$\n\nThe action $a$ is the *send*, $\\bar{a}$ is the *receive*, and $\\tau$ is the result of their *synchronization* (invisible to an external observer).\n\n**Step 2 — Write the inductive rule for the constant process $P$.**\n\nBy the axiom of recursive definition (Kleene fixed-point semantics), we treat $P$ as a state label and write:\n\n$$\\frac{}{P \\xrightarrow{a} P} \\quad (\\text{AX}_P) \\qquad \\frac{}{Q \\xrightarrow{\\bar{a}} Q} \\quad (\\text{AX}_Q)$$\n\n**Step 3 — Write the parallel composition rule.**\n\nFor two components $X$ and $Y$ running concurrently, the standard CCS parallel rule is:\n\n$$\n\\begin{aligned}\n\\frac{X \\xrightarrow{a} X'}{X \\parallel Y \\xrightarrow{a} X' \\parallel Y} \\quad (\\text{Par-L}) \\qquad\n\\frac{Y \\xrightarrow{a} Y'}{X \\parallel Y \\xrightarrow{a} X \\parallel Y'} \\quad (\\text{Par-R})\n\\end{aligned}\n$$\n\n$$\n\\begin{aligned}\n\\frac{X \\xrightarrow{a} X' \\quad Y \\xrightarrow{\\bar{a}} Y'}{X \\parallel Y \\xrightarrow{\\tau} X' \\parallel Y'} \\quad (\\text{Par-Com})\n\\end{aligned}\n$$\n\n**Step 4 — Substitute the concrete actions.**\n\nApply $\\text{Par-Com}$ with $X = P$, $a = a$, $X' = P$, $Y = Q$, $\\bar{a} = \\bar{a}$, $Y' = Q$:\n\n$$\n\\begin{aligned}\n\\frac{P \\xrightarrow{a} P \\quad Q \\xrightarrow{\\bar{a}} Q}{P \\parallel Q \\xrightarrow{\\tau} P \\parallel Q} \\quad (\\text{Inst})\n\\end{aligned}\n$$\n\n**Step 5 — Read off the global LTS.**\n\nThe system has a single state, namely $s_0 = P \\parallel Q$, and a single self-loop labeled $\\tau$. The resulting LTS is:\n\n$$\n\\begin{aligned}\n\\mathcal{T} = \\big(\\{P \\parallel Q\\},\\ \\{a, \\bar{a}, \\tau\\},\\ \\{(P \\parallel Q, \\tau, P \\parallel Q)\\},\\ P \\parallel Q\\big)\n\\end{aligned}\n$$\n\n**Step 6 — Interpret the result.**\n\nThe *external* behavior is a single silent action $\\tau$ on every cycle. Therefore $P \\parallel Q$ is **observationally equivalent to NIL** (it does nothing observable). Formally: $P \\parallel Q \\approx \\mathbf{0}$ under weak bisimulation.\n\n---\n\n## 3.2 Derivation: Computing Reachability via the Post-Image Fixpoint\n\n**Problem:** Given an LTS with $S = \\{s_0, s_1, s_2, s_3, s_4\\}$ and transitions $s_0 \\xrightarrow{a} s_1$, $s_0 \\xrightarrow{b} s_2$, $s_1 \\xrightarrow{a} s_2$, $s_1 \\xrightarrow{c} s_3$, $s_2 \\xrightarrow{b} s_4$, $s_3 \\xrightarrow{c} s_4$, compute the set of states reachable from $s_0$.\n\n### Step-by-Step Fixpoint Iteration\n\nLet $R_0 = \\{s_0\\}$.\n\nDefine the post-image operator:\n\n$$\n\\begin{aligned}\n\\text{Post}(R) = R \\cup \\{s' \\in S \\mid \\exists s \\in R,\\ \\exists a \\in Act,\\ s \\xrightarrow{a} s'\\}\n\\end{aligned}\n$$\n\n**Iteration 1:**\n\n$$\n\\begin{aligned}\n\\text{Post}(R_0) &= \\{s_0\\} \\cup \\{s' \\mid s_0 \\xrightarrow{a} s' \\text{ or } s_0 \\xrightarrow{b} s'\\} \\\\\n&= \\{s_0\\} \\cup \\{s_1, s_2\\} \\\\\n&= \\{s_0, s_1, s_2\\} = R_1\n\\end{aligned}\n$$\n\n**Iteration 2:**\n\n$$\n\\begin{aligned}\n\\text{Post}(R_1) &= R_1 \\cup \\{s' \\mid s_1 \\xrightarrow{a} s' \\text{ or } s_1 \\xrightarrow{c} s' \\text{ or } s_2 \\xrightarrow{b} s'\\} \\\\\n&= \\{s_0, s_1, s_2\\} \\cup \\{s_2, s_3, s_4\\} \\\\\n&= \\{s_0, s_1, s_2, s_3, s_4\\} = R_2\n\\end{aligned}\n$$\n\n**Iteration 3:**\n\n$$\n\\begin{aligned}\n\\text{Post}(R_2) &= R_2 \\cup \\emptyset = R_2\n\\end{aligned}\n$$\n\nThe fixpoint $R_2$ is reached when $R_{k+1} = R_k$. Therefore:\n\n$$\n\\begin{aligned}\n\\text{Reach}(s_0) = \\{s_0, s_1, s_2, s_3, s_4\\}\n\\end{aligned}\n$$\n\nEvery state is reachable — the system is **fully connected** in its reachable sub-graph.\n\n---\n\n## 3.3 Analytical Matrix — Property-Checking Template\n\nFor a given KTU-style property, the following matrix is the standard examiner's valuation key:\n\n| Step | Examiner Expectation | Marks Allocated |\n|---|---|---|\n| 1 | Identify the *kind* of property (safety / liveness / fairness) | 1 |\n| 2 | Choose the right temporal logic (LTL / CTL / CTL*) | 1 |\n| 3 | Translate the natural-language statement into a formula | 2 |\n| 4 | Identify which fixpoint operator is required ($\\mu$ or $\\nu$) | 2 |\n| 5 | Execute the fixpoint iteration over the LTS | 5 |\n| 6 | State the verdict and interpret it in English | 2 |\n| 7 | Discuss the complexity and any state-space concerns | 1 |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Mermaid Block Diagram: Anatomy of a Transition Language Compiler\n\n```mermaid\nflowchart TD\n    A[Source Program] --> B[Lexer and Parser]\n    B --> C[Abstract Syntax Tree]\n    C --> D[Operational Semantics Engine]\n    D --> E[SOS Rule Selector]\n    E --> F[Transition Derivation Stack]\n    F --> G[Labeled Transition System LTS]\n    G --> H[State Space Explorer]\n    H --> I[Temporal Logic Evaluator]\n    I --> J{Property Holds?}\n    J -->|Yes| K[VERIFIED Counterexample Absent]\n    J -->|No| L[COUNTEREXAMPLE Trace Output]\n    style A fill:#f9f,stroke:#333\n    style G fill:#bbf,stroke:#333\n    style J fill:#ff9,stroke:#333\n    style K fill:#9f9,stroke:#333\n    style L fill:#f99,stroke:#333\n```\n\n> **Reading guide:** The diagram traces the *end-to-end pipeline* of an automated verifier. The transition language is consumed by the **Operational Semantics Engine** (block D), which selects the appropriate **SOS rule** for each syntactic construct, builds the **LTS** (block G), and hands it to the model checker (blocks H–I).\n\n## 4.2 Mermaid Diagram: Reachability Graph of the Worked Example\n\n```mermaid\nflowchart LR\n    s0[s0] -- a --> s1[s1]\n    s0 -- b --> s2[s2]\n    s1 -- a --> s2[s2]\n    s1 -- c --> s3[s3]\n    s2 -- b --> s4[s4]\n    s3 -- c --> s4[s4]\n    style s0 fill:#ffd\n    style s4 fill:#dfd\n```\n\n> **Reading guide:** Every node is a *state* in $S$, and every labeled edge is a transition in $\\rightarrow$. The **initial state** $s_0$ is highlighted in yellow; the **sink-like state** $s_4$ (no outgoing edges) is highlighted in green. This is exactly the LTS the student must build in Section 3.2.\n\n## 4.3 Mermaid Diagram: Block-Level Functional Architecture of a Model Checker\n\n```mermaid\nflowchart TD\n    M[Model M as LTS] --> E[Explorer Module]\n    SPEC[Property phi in CTL or LTL] --> E\n    E --> FF[Fixpoint Computation Engine]\n    FF --> SAT[Satisfaction Set Sat phi]\n    SAT --> C{Initial state in Sat phi}\n    C -->|Yes| OK[System Satisfies phi]\n    C -->|No| BAD[Violation Found]\n    BAD --> CE[Counterexample Witness Path]\n    style M fill:#cff\n    style SPEC fill:#fcf\n    style OK fill:#9f9\n    style BAD fill:#f99\n    style CE fill:#fc9\n```\n\n> **Reading guide:** This is the **Block-Level Functional Architecture Flow** of a generic model checker. The transition language provides the *model* (top-left), the user supplies the *specification* (top-right), and the engine computes the satisfaction set via a fixpoint.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. `[KTU University Exam — May 2024, Model]`\n**Define a Labeled Transition System (LTS). List its four components and explain the meaning of the transition relation $\\rightarrow$.**\n\n**Model Answer (3 marks):**\n\nA Labeled Transition System is a formal model $\\mathcal{T} = (S, Act, \\rightarrow, s_0)$ where:\n\n- $S$ is a non-empty finite set of states (1 mark).\n- $Act$ is a finite set of observable actions / labels (1 mark).\n- $\\rightarrow \\subseteq S \\times Act \\times S$ is the transition relation; a tuple $(s, a, s') \\in \\rightarrow$ is written $s \\xrightarrow{a} s'$ and means \"from state $s$, the system can move to state $s'$ while performing action $a$\" (1 mark).\n- $s_0 \\in S$ is the initial state.\n\n---\n\n### Q2. `[KTU University Exam — Dec 2023, Model]`\n**Distinguish between a Labeled Transition System (LTS) and a Kripke Structure. Why is each preferred for LTL versus CTL model checking?**\n\n**Model Answer (3 marks):**\n\n- An **LTS** carries labels on the *transitions* (edges) (1 mark), while a **Kripke structure** carries labels on the *states* (nodes) and has unlabeled transitions (1 mark).\n- **LTL** (linear-time) is naturally interpreted over the set of all infinite paths of an LTS, so LTSs are the standard semantic model. **CTL** (branching-time) talks about \"there exists a path\" / \"for all paths\" with state-quantifiers; states must be the labeled entities, so Kripke structures are preferred (1 mark).\n\n---\n\n## 5.2 Part B — 14-Mark Questions (ESE Module Internal Choice)\n\n### Question A (14 Marks) `[KTU University Exam — Jul 2024, Model]`\n\n> **(a) [7 marks]** Consider the CCS-like system:\n> $$P = a.b.\\mathbf{0} \\quad\\quad Q = \\bar{a}.\\bar{b}.\\mathbf{0}$$\n> Derive the global transition system of $P \\parallel Q$ using the standard parallel composition rules of CCS. Show every inference step explicitly.\n\n> **(b) [7 marks]** For the LTS you obtained, write the CTL formula expressing the property *\"there exists a path on which a synchronization action $\\tau$ occurs at least once, and globally it is never the case that the system is in a deadlocked state\"*. Evaluate the formula on your LTS.\n\n#### Model Solution to (a)\n\n**Step 1 — List the inference axioms for the leaf processes.**\n\n$$\n\\begin{aligned}\nP \\xrightarrow{a} P' \\quad &\\text{where } P' = b.\\mathbf{0} \\tag{AX$_1$} \\\\\nP' \\xrightarrow{b} \\mathbf{0} \\quad &\\tag{AX$_2$} \\\\\nQ \\xrightarrow{\\bar{a}} Q' \\quad &\\text{where } Q' = \\bar{b}.\\mathbf{0} \\tag{AX$_3$} \\\\\nQ' \\xrightarrow{\\bar{b}} \\mathbf{0} \\quad &\\tag{AX$_4$}\n\\end{aligned}\n$$\n\n**Step 2 — Apply the CCS communication rule.**\n\n$$\n\\begin{aligned}\n\\frac{P \\xrightarrow{a} P' \\quad Q \\xrightarrow{\\bar{a}} Q'}{P \\parallel Q \\xrightarrow{\\tau} P' \\parallel Q'} \\quad \\text{(Com)}\n\\end{aligned}\n$$\n\nSubstituting:\n\n$$\n\\begin{aligned}\nP \\parallel Q \\xrightarrow{\\tau} P' \\parallel Q' = (b.\\mathbf{0}) \\parallel (\\bar{b}.\\mathbf{0}) \\tag{D$_1$}\n\\end{aligned}\n$$\n\n**Step 3 — Apply Par-L and Par-R independently.**\n\n$$\n\\begin{aligned}\nP' \\parallel Q' \\xrightarrow{b} \\mathbf{0} \\parallel Q' \\tag{D$_2$} \\\\\nP' \\parallel Q' \\xrightarrow{\\bar{b}} P' \\parallel \\mathbf{0} \\tag{D$_3$}\n\\end{aligned}\n$$\n\n**Step 4 — Apply Com again on the $b$/$\\bar{b}$ pair.**\n\n$$\n\\begin{aligned}\nP' \\parallel Q' \\xrightarrow{\\tau} \\mathbf{0} \\parallel \\mathbf{0} = \\mathbf{0} \\tag{D$_4$}\n\\end{aligned}\n$$\n\n**Step 5 — Final LTS.**\n\n$$\n\\begin{aligned}\n\\mathcal{T} = \\big(&\\{P \\parallel Q,\\ P' \\parallel Q',\\ \\mathbf{0}\\}, \\\\\n&\\{a, \\bar{a}, b, \\bar{b}, \\tau\\}, \\\\\n&\\{D_1, D_2, D_3, D_4\\}, \\\\\n&P \\parallel Q\\big)\n\\end{aligned}\n$$\n\nThe states form a linear chain with one $\\tau$ synchronization at each step.\n\n#### Model Solution to (b)\n\n**Step 1 — Translate the English property into CTL.**\n\n> \"*There exists a path on which a synchronization action $\\tau$ occurs at least once, and globally it is never the case that the system is in a deadlocked state.*\"\n\n$$\n\\begin{aligned}\n\\phi \\;=\\; EF(\\tau\\text{-step}) \\;\\land\\; AG(\\neg\\text{deadlock})\n\\end{aligned}\n$$\n\nHere $EF$ is the *exists-future* operator and $AG$ is the *all-globally* operator of CTL. The atomic proposition $\\tau\\text{-step}$ is true at a state if the state has at least one outgoing $\\tau$-transition.\n\n**Step 2 — Label each state with the atomic propositions.**\n\n- $s_0 = P \\parallel Q$: $\\tau\\text{-step} = \\text{true}$ (has D$_1$); deadlock = false.\n- $s_1 = P' \\parallel Q'$: $\\tau\\text{-step} = \\text{true}$ (has D$_4$); deadlock = false.\n- $s_2 = \\mathbf{0}$: $\\tau\\text{-step} = \\text{false}$; deadlock = true.\n\n**Step 3 — Evaluate $EF(\\tau\\text{-step})$ from $s_0$.**\n\nThere is a path $s_0 \\xrightarrow{\\tau} s_1$, so $s_0 \\models EF(\\tau\\text{-step})$. **TRUE (1 mark)**\n\n**Step 4 — Evaluate $AG(\\neg\\text{deadlock})$ from $s_0$.**\n\nOn the path $s_0 \\to s_1 \\to s_2$, the state $s_2$ is a deadlock. Hence there is a reachable state where deadlock holds, so $s_0 \\not\\models AG(\\neg\\text{deadlock})$. **FALSE (1 mark)**\n\n**Step 5 — Combine.**\n\n$\\phi$ evaluates to **FALSE**. The system *does* deadlock at $\\mathbf{0}$, violating the second conjunct. (1 mark)\n\n**Step 6 — Complexity & comment.**\n\nThe state space has only 3 states — manageable. In general, the cost of computing $EF$ is $O(\\vert S \\vert + \\vert \\rightarrow \\vert)$ and $AG$ requires a greatest fixpoint of comparable cost.\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall — Where Students Lose Marks:**\n> 1. **Confusing LTS and Kripke structure** (–2 marks): students often mis-state which entity carries the labels. State labels belong to *states*; transition labels belong to *transitions*.\n> 2. **Forgetting to enumerate *all* SOS rules** (–2 marks): only one rule (Com) is applied, missing Par-L / Par-R interleavings.\n> 3. **Using `EF` and `AF` interchangeably** (–1 mark): `EF` means \"there *exists* a future path\", `AF` means \"on *all* paths, eventually\". Mixing them is a classic error.\n> 4. **Skipping the fixpoint step** (–2 marks): in the formula evaluation, students jump to the verdict without computing the satisfaction set iteratively.\n\n---\n\n### Question B (14 Marks) `[KTU University Exam — Jul 2024, Model — Internal Choice]`\n\n> **(a) [7 marks]** Explain the concept of **bisimulation** in the transition-language framework. Define *strong bisimulation* ($\\sim$) and *weak bisimulation* ($\\approx$). Give one engineering scenario where each is more appropriate than the other.\n\n> **(b) [7 marks]** For the LTS $s_0 \\xrightarrow{a} s_1$, $s_0 \\xrightarrow{a} s_2$, $s_1 \\xrightarrow{b} s_3$, $s_2 \\xrightarrow{c} s_3$, determine whether $s_1$ and $s_2$ are strongly bisimilar. Justify by exhibiting a relation or by showing a distinguishing formula.\n\n#### Model Solution to (a)\n\n**Strong bisimulation** is the largest relation $\\mathcal{R} \\subseteq S \\times S$ such that whenever $(s, t) \\in \\mathcal{R}$:\n\n$$\n\\begin{aligned}\n\\forall a \\in Act.\\ \\forall s'.\\ (s \\xrightarrow{a} s') \\Rightarrow \\exists t'.\\ (t \\xrightarrow{a} t') \\wedge (s', t') \\in \\mathcal{R} \\\\\n\\forall a \\in Act.\\ \\forall t'.\\ (t \\xrightarrow{a} t') \\Rightarrow \\exists s'.\\ (s \\xrightarrow{a} s') \\wedge (s', t') \\in \\mathcal{R}\n\\end{aligned}\n$$\n\nTwo states are **strongly bisimilar**, written $s \\sim t$, iff there exists such a relation $\\mathcal{R}$ with $(s,t) \\in \\mathcal{R}$ (1 mark for the definition, 1 mark for the co-inductive flavor).\n\n**Weak bisimulation** ($\\approx$) relaxes the matching by allowing any number of $\\tau$ (silent) actions on either side, written $\\Rightarrow$ for weak transitions. The match becomes:\n\n$$\n\\begin{aligned}\ns \\xrightarrow{a} s' \\text{ must be matched by } t \\xRightarrow{a} t' \\text{ (i.e., } t \\Rightarrow \\xrightarrow{a} \\Rightarrow t')\n\\end{aligned}\n$$\n\n(1 mark for the modified condition).\n\n**Engineering scenarios:**\n\n- **Strong bisimulation** is used in *synchronous hardware verification* (e.g., proving two implementations of a multiplier are step-equivalent). Every clock cycle must match — $\\tau$ is not allowed to be hidden (2 marks).\n- **Weak bisimulation** is used in *asynchronous protocol verification* (e.g., TCP handshake refinements), where internal retransmissions (silent retries) should be ignored (2 marks).\n\n#### Model Solution to (b)\n\n**Step 1 — Attempt to construct a strong bisimulation.**\n\nSuppose $\\mathcal{R} = \\{(s_1, s_2), (s_3, s_3)\\}$.\n\n- Check $(s_1, s_2)$: $s_1 \\xrightarrow{b} s_3$ must be matched by $s_2 \\xrightarrow{b} \\cdots$. But $s_2$ has *no* outgoing $b$-transition. So the match fails. (2 marks)\n\n**Step 2 — Conclude that $s_1 \\not\\sim s_2$.**\n\nThe distinguishing action is $b$ (1 mark).\n\n**Step 3 — Exhibit a distinguishing CTL formula (1 mark).**\n\n$$\n\\begin{aligned}\n\\phi \\;=\\; \\exists \\bigcirc b\n\\end{aligned}\n$$\n\n\"There exists a successor state reachable in one step via $b$.\" Then:\n\n- $s_1 \\models \\exists \\bigcirc b$ because $s_1 \\xrightarrow{b} s_3$ (1 mark).\n- $s_2 \\not\\models \\exists \\bigcirc b$ because $s_2$ has no $b$-successor (1 mark).\n\nHence $s_1$ and $s_2$ are distinguished by $\\phi$, confirming $s_1 \\not\\sim s_2$ (1 mark).\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n- **The Transition Language** is the **semantic foundation** of every model checker. The four-tuple $\\mathcal{T} = (S, Act, \\rightarrow, s_0)$ is the canonical definition.\n- The transition relation $\\rightarrow$ is the **atomic step**; the reflexive-transitive closure $\\rightarrow^*$ is the **path**.\n- **LTS vs Kripke**: LTS = labels on *edges*; Kripke = labels on *states*. The right choice depends on the temporal logic (LTL vs CTL).\n- **Operational Semantics (SOS)** uses inference-rule notation\n  $$\\frac{\\text{hypotheses}}{\\text{conclusion}}$$\n  to define transitions compositionally.\n- **Bisimulation** ($\\sim$) = strongest *behavioral equivalence*; **weak bisimulation** ($\\approx$) hides $\\tau$-actions.\n- **Reachability** = the *least fixpoint* $\\mu X.\\,\\text{Post}(X) \\cup \\{s_0\\}$; **invariant checking** = the *greatest fixpoint* $\\nu X.\\,\\text{Pre}(X) \\cap Q$.\n- **State-space explosion** is the central engineering challenge: $n$ parallel components of size $k$ give $k^n$ reachable states — solve with **BDDs**, **partial-order reduction**, or **abstraction/refinement (CEGAR)**.\n- **Exam mantra**: \"Define the LTS → apply SOS rules → compute the satisfaction set via fixpoints → interpret the verdict.\" This is the **valuation key** the KTU examiner follows.\n- **CTL operators to memorize**: $EX, AX, EF, AF, EG, AG, EU, AU$ — and especially never confuse $E$ (exists) with $A$ (all).\n- **Mark distribution pattern (Part B, 14 marks)**: definition (2) + construction (4) + verification (5) + interpretation (2) + complexity (1).\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 4-t4",
          "title": "The Module Language",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module Module 4\n> - **Topic:** The Module Language\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition\n\n> [!IMPORTANT]\n> **The Module Language** is the formal input specification language used in symbolic model checkers (such as **NuSMV**, **SMV**, and similar finite-state verification frameworks) to describe a transition system, its state variables, its initial conditions, its transition relation, and the properties (LTL/CTL) that must be verified against it.\n\nIn KTU 2024 Scheme parlance, the **Module Language** is the **declarative, finite-state modelling language** that allows a verification engineer to:\n- Declare **a hierarchical, encapsulated module** (the *system-under-verification*).\n- Declare the **state-holding variables** (`VAR`), **input variables** (`IVAR`), and **frozen variables** (`FROZENVAR`).\n- Define the **initial state predicate** (`INIT`).\n- Define the **next-state / transition relation** (`ASSIGN` or `TRANS`).\n- State the **temporal logic properties** to be model-checked (`SPEC`, `LTLSPEC`, `CTLSPEC`, `INVAR`).\n\nEvery KTU board paper expects students to be able to **write, read, and reason about** small Module Language programs (also called **SMV programs** after the original Carnegie-Mellon tool).\n\n### 1.2 Conceptual Analogy / Intuition\n\n> [!NOTE]\n> **Analogy — The Vending Machine Blueprint**\n\nImagine you are a civil engineer designing a vending machine. Before the machine is built, you write a **blueprint** that tells the factory:\n1. What are the *physical slots* (the variables — coin slot, button panel, dispenser).\n2. What is the *initial state* (empty, locked, no coins).\n3. How the machine *behaves* when a button is pressed (transitions).\n4. What *safety properties* must always hold (never dispense two items, never accept invalid coins).\n\nThe **Module Language** is precisely that blueprint — but for software/hardware systems and verified by a *computer* (the model checker) instead of a human inspector. A **module** in the language is the equivalent of one complete blueprint for one component, and a **system** is built by composing modules (just like a vending machine is built from a coin-acceptor module and a dispenser module).\n\n### 1.3 Key Constants / Metrics\n\n| Metric | Value / Convention |\n|---|---|\n| Boolean constants | `TRUE`, `FALSE` |\n| Integer range | Finite bounded integers (model checker is finite-state) |\n| Reserved keywords | `MODULE`, `VAR`, `IVAR`, `FROZENVAR`, `ASSIGN`, `INIT`, `TRANS`, `INVAR`, `SPEC`, `LTLSPEC`, `CTLSPEC`, `JUSTICE`, `COMPASSION`, `DEFINE` |\n\n> [!IMPORTANT]\n> The Model Checker's **state space** is bounded by the **Cartesian product** of all variable domains. For $n$ Boolean variables, the state space has exactly $2^n$ states — the *fundamental explosion* the language is designed to manage via *symbolic* representations.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 The Structural Pillars of a Module\n\nA complete Module Language program is organised into **seven** logical sections. The KTU examiner's *most-asked* sub-question is *\"Explain the components / structure of a module.\"*\n\n**1. Module Declaration Block**\n- Begins with the keyword `MODULE` followed by a module identifier.\n- A *main* module is the system root; sub-modules are reusable components.\n- Modules are **synchronously composed** (parallel composition with lock-step transitions).\n\n**2. Variable Declaration Block (`VAR`, `IVAR`, `FROZENVAR`)**\n- `VAR` — system state variables, re-evaluated every transition.\n- `IVAR` — environment-controlled inputs, **non-deterministic** from the system's perspective.\n- `FROZENVAR` — constants fixed at initialisation; useful for parameterised models.\n\n**3. Definition Block (`DEFINE`)**\n- Macros for readability, e.g. `DEFINE max := 7;`\n\n**4. Initialisation Block (`INIT`)**\n- A propositional predicate that all initial states must satisfy.\n\n**5. Transition Block (`ASSIGN` or `TRANS`)**\n- `ASSIGN` block assigns *next-state* values using `:=` (deterministic) and `:= {v1, v2}` (non-deterministic).\n- `TRANS` block lists a propositional predicate over current and next-state variables.\n\n**6. Invariant Block (`INVAR`)**\n- A propositional predicate that must hold in **every reachable state** (a sanity check).\n\n**7. Property / Specification Block**\n- `SPEC` — simple propositional invariant.\n- `LTLSPEC` — Linear Temporal Logic formula (state path semantics).\n- `CTLSPEC` — Computation Tree Logic formula (branching-time semantics).\n- `JUSTICE` / `COMPASSION` — fairness constraints.\n\n### 2.2 Why the Language Looks the Way It Does — The \"Why\"\n\n- **Synchronous, lock-step composition** mirrors **clocked hardware** (digital circuits), which is why this language dominated hardware verification.\n- **Bounded finite types** are mandatory because **finite-state model checking** is the verification engine. Unbounded integers cause termination failure.\n- **Separation of `INIT` and `TRANS`/`ASSIGN`** lets the checker construct the **Kripke structure** $M = (S, S_0, R, L)$ explicitly, where $S_0 \\subseteq S$ is computed from `INIT` and $R \\subseteq S \\times S$ is computed from `TRANS`.\n\n### 2.3 KTU Formula Sheet / Cheat Sheet\n\n> [!NOTE]\n> Use `\\vert` instead of `|` to avoid breaking the markdown table. All formulas are mathematically verifiable.\n\n| Symbol / Keyword | Meaning | Type |\n|---|---|---|\n| $M = (S, S_0, R, L)$ | Kripke structure | Theoretical |\n| $\\lvert S \\rvert = 2^{n_b} \\cdot \\prod (u_i+1)$ | State-space size (Boolean $n_b$, bounded integer $u_i$) | Complexity |\n| $S_0 = \\{s \\in S \\mid \\text{INIT}(s) = \\text{TRUE}\\}$ | Initial-state set | Set-builder |\n| $R(s, s') = \\text{ASSIGN}/\\text{TRANS}(s, s') = \\text{TRUE}$ | Transition relation | Predicate |\n| $E[ \\varphi \\, U \\, \\psi ]$ | CTL *exists-until* | Temporal |\n| $A[ \\varphi \\, U \\, \\psi ]$ | CTL *all-until* | Temporal |\n| $G \\, p$ | LTL/CTL *globally* (always) | Temporal |\n| $F \\, p$ | LTL/CTL *eventually* | Temporal |\n| $X \\, p$ | LTL/CTL *next* | Temporal |\n| `:=` | Deterministic next-state assignment | Syntax |\n| `:= {v1, v2, v3}` | Non-deterministic assignment | Syntax |\n| `next(var)` | Reference to next-state value of `var` | Syntax |\n\n### 2.4 Real-World Engineering Utility\n\n- **Hardware verification** — Intel, AMD, IBM, and Cadence routinely verify register-transfer-level (RTL) designs via SMV-style modules.\n- **Protocol verification** — cache-coherence protocols (e.g., MESI, German's protocol).\n- **Embedded / safety-critical software** — Airbus and Boeing verify cockpit-software state machines.\n- **Communication protocols** — Needham-Schroeder, Kerberos, IEEE 1394 (FireWire).\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Derivation — State-Space Size of a Module\n\n**Problem (typical 7-mark KTU sub-question):** *A module declares three Boolean variables and two bounded integers, each ranging over $\\{0, 1, 2, 3, 4, 5\\}$. Compute the worst-case state-space size.*\n\n**Step 1 — Identify variable types and domains.**\n- Boolean variables: 3, each with domain size $= 2$.\n- Bounded integer variables: 2, each with domain size $= 6$ (values 0 through 5).\n\n**Step 2 — Recall the Cartesian-product formula for the state space.**\n\n$$\n\\lvert S \\rvert \\;=\\; \\prod_{i=1}^{n} \\lvert D_i \\rvert\n$$\n\nwhere $D_i$ is the domain of the $i$-th state variable.\n\n**Step 3 — Substitute the domain sizes.**\n\n$$\n\\lvert S \\rvert \\;=\\; 2 \\times 2 \\times 2 \\times 6 \\times 6\n$$\n\n**Step 4 — Evaluate the product step by step.**\n\n$$\n2 \\times 2 = 4\n$$\n\n$$\n4 \\times 2 = 8\n$$\n\n$$\n8 \\times 6 = 48\n$$\n\n$$\n48 \\times 6 = 288\n$$\n\n**Step 5 — Express in the cleaner closed form.** Using the Boolean $n_b=3$ and integer-bound $u_i = 5$ for $k=2$ integers:\n\n$$\n\\lvert S \\rvert \\;=\\; 2^{n_b} \\cdot \\prod_{i=1}^{k} (u_i + 1) \\;=\\; 2^{3} \\cdot (5+1)^{2} \\;=\\; 8 \\cdot 36 \\;=\\; 288 \\text{ states}\n$$\n\n**Step 6 — Engineering interpretation.** Even with only **five** variables, the model checker must explore up to **288** states; in real designs with hundreds of variables this becomes $2^{100+}$ — the *state-explosion problem* that motivates symbolic (BDD-based) model checking.\n\n### 3.2 Analytical Matrix — Keyword-to-Translation Table\n\n| Module Keyword | Reads as | Affects which Kripke component? |\n|---|---|---|\n| `MODULE m` | Define a module named `m` | System component (hierarchical) |\n| `VAR x : boolean;` | State variable `x` (Boolean) | $S$ (states) |\n| `IVAR req : boolean;` | Environment input `req` | $S$ (input dimension) |\n| `FROZENVAR N : 1..8;` | Constant `N` chosen at init | $S$ (parameter) |\n| `DEFINE busy := x & req;` | Macro expansion | No direct Kripke effect |\n| `INIT x = FALSE` | Initial-state predicate | $S_0$ |\n| `ASSIGN next(x) := x xor req;` | Transition (deterministic) | $R$ |\n| `ASSIGN next(y) := {0, 1, 2};` | Transition (non-deterministic) | $R$ |\n| `TRANS (x -> next(x) = !x)` | Transition (predicate form) | $R$ |\n| `INVAR !x \\vert !req` (using `\\vert`) | State invariant | Sanity check on $S$ |\n| `SPEC AG ! (x & y)` | CTL safety property | $M \\models \\text{SPEC}?$ |\n| `LTLSPEC G (req -> F x)` | LTL liveness property | $M \\models \\text{LTLSPEC}?$ |\n| `JUSTICE req` | Fairness on `req` | Constraint on $R$ |\n\n### 3.3 Worked Canonical Example — A 1-bit Toggle\n\nBelow is the canonical \"toggle with enable\" module, used in almost every KTU lecture slide:\n\n```\nMODULE main\n  VAR\n    bit   : boolean;\n    en    : boolean;\n  ASSIGN\n    init(bit) := FALSE;\n    next(bit) := en ? !bit : bit;\n  LTLSPEC\n    G (en -> F (bit != bit))\n```\n\n**Translation to Kripke components (Step-by-step):**\n\n- $S = \\{(0,0), (0,1), (1,0), (1,1)\\}$ (tuples of `(bit, en)`).\n- $S_0 = \\{(0,0), (0,1)\\}$ (initial `bit = FALSE`, `en` arbitrary).\n- $R$: if `en = TRUE` then `bit` flips; if `en = FALSE` then `bit` holds.\n- The `LTLSPEC` says: *globally, whenever `en` is true, eventually `bit` will be different from its current value* — i.e., a request to toggle is eventually honoured.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Block-Level Functional Architecture of a Module Language Program\n\n```mermaid\nflowchart TD\n    A[MODULE main] --> B[Variable Declarations]\n    A --> C[DEFINE Macros]\n    A --> D[INIT Block]\n    A --> E[ASSIGN or TRANS Block]\n    A --> F[INVAR Block]\n    A --> G[SPEC Block]\n    G --> G1[LTLSPEC]\n    G --> G2[CTLSPEC]\n    A --> H[Fairness JUSTICE COMPASSION]\n    B --> B1[VAR state vars]\n    B --> B2[IVAR inputs]\n    B --> B3[FROZENVAR constants]\n    D --> I[Kripke Structure M = S S0 R L]\n    E --> I\n    B1 --> I\n    B2 --> I\n    I --> J[Symbolic Model Checker NuSMV]\n    I --> J\n    G1 --> J\n    G2 --> J\n    H --> J\n    J --> K[Verification Result TRUE or FALSE plus counterexample]\n```\n\n### 4.2 Data-Flow from Module to Kripke Structure\n\n```mermaid\nflowchart LR\n    P[Module Language Source] --> Q[Lexer Parser]\n    Q --> R[AST]\n    R --> S1[INIT predicate]\n    R --> S2[TRANS ASSIGN]\n    R --> S3[VAR declarations]\n    S1 --> T[Kripke M]\n    S2 --> T\n    S3 --> T\n    T --> U[Boolean Encoding BDD]\n    U --> V[Model Checking Algorithm CTL or LTL]\n    V --> W[Output verdict plus counterexample trace]\n```\n\n### 4.3 Hierarchical Module Composition\n\n```mermaid\nflowchart TD\n    MAIN[MODULE main] --> M1[MODULE counter]\n    MAIN --> M2[MODULE arbiter]\n    M1 --> M1A[VAR count boolean]\n    M1 --> M1B[ASSIGN next count]\n    M2 --> M2A[VAR grant boolean]\n    M2 --> M2B[TRANS predicate]\n    M1A --> R[Kripke M counter]\n    M1B --> R\n    M2A --> S[Kripke M arbiter]\n    M2B --> S\n    R --> P[Parallel Composition M system]\n    S --> P\n```\n\n> [!NOTE]\n> In **synchronous** composition (the SMV/NuSMV default), the global transition relation is the **conjunction** of all module transitions; in **asynchronous** composition (introduced in Module 5 of PECNT526), only one module transitions at a time.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short Answer Questions (3 Marks each)\n\n**Q1. [KTU University Exam — Dec 2023]** Define the term *Module Language* as used in symbolic model checking. List its main building blocks.\n\n> **Model Answer (for 3 marks):**\n> The **Module Language** is a finite-state declarative specification language used as the input to symbolic model checkers (e.g., NuSMV) for describing the system under verification. Its main building blocks are:\n> 1. **Module declaration** — `MODULE name`.\n> 2. **Variable declarations** — `VAR`, `IVAR`, `FROZENVAR`.\n> 3. **Macro definitions** — `DEFINE`.\n> 4. **Initialisation** — `INIT`.\n> 5. **Transition relation** — `ASSIGN` / `TRANS`.\n> 6. **Invariants** — `INVAR`.\n> 7. **Property specifications** — `SPEC`, `LTLSPEC`, `CTLSPEC`.\n> 8. **Fairness** — `JUSTICE`, `COMPASSION`.\n\n**Q2. [KTU University Exam — May 2024]** Differentiate between `VAR`, `IVAR`, and `FROZENVAR` in the Module Language.\n\n> **Model Answer (for 3 marks):**\n>\n> | Keyword | Mutability | Role in the system |\n> |---|---|---|\n> | `VAR` | Mutable state variable, changes every transition | Models system memory / registers |\n> | `IVAR` | Mutable, but treated as non-deterministic environment input | Models uncontrollable inputs |\n> | `FROZENVAR` | Assigned once at initialisation, never changes | Models design parameters (e.g., number of processes `N`) |\n\n---\n\n### 5.2 Part B — Full 14-Mark Module Choice Questions\n\n#### **Question A (14 Marks)** — *[KTU University Exam — Model Paper, Module 4]*\n\n**(a) [7 Marks]** Explain, with a neat block diagram, the architecture of a Module Language program and show how each block maps to a Kripke-structure component $(S, S_0, R, L)$.\n\n**(b) [7 Marks)** Consider a module `main` that declares two Boolean variables `x` and `y` and one integer variable `z` bounded over $\\{0, 1, 2, 3\\}$. Compute the worst-case state-space size. Justify each step.\n\n**Model Solution (a):**\n\nThe Module Language program is organised into **seven** logical blocks. The mapping to the Kripke structure is:\n\n| Block | Maps to Kripke component |\n|---|---|\n| `MODULE name` | Root identifier of the model |\n| `VAR`, `IVAR`, `FROZENVAR` | $S$ (set of all states) |\n| `INIT` | $S_0 \\subseteq S$ (initial states) |\n| `ASSIGN` / `TRANS` | $R \\subseteq S \\times S$ (transition relation) |\n| `INVAR` | Sanity predicate over $S$ |\n| `SPEC` / `LTLSPEC` / `CTLSPEC` | Formula $\\varphi$ to be checked $M \\models \\varphi$ |\n| `JUSTICE` / `COMPASSION` | Fairness constraint on infinite paths in $R$ |\n\nThe flow is:\n\n```\nMODULE ──► Variables ──► INIT ──► S0\n                  └─────► ASSIGN/TRANS ──► R\n                                          │\n                                          ▼\n                                    Kripke M = (S, S0, R, L)\n                                          │\n                                          ▼\n                              LTLSPEC / CTLSPEC / SPEC\n                                          │\n                                          ▼\n                                 Verdict + counterexample\n```\n\n**Model Solution (b):**\n\n- $n_b = 2$ Boolean variables, each domain size $= 2$.\n- One integer variable with bound $u = 3$ (values $0, 1, 2, 3$), domain size $= u+1 = 4$.\n\n$$\n\\lvert S \\rvert \\;=\\; 2^{n_b} \\cdot (u + 1) \\;=\\; 2^{2} \\cdot 4 \\;=\\; 4 \\cdot 4 \\;=\\; 16 \\text{ states}\n$$\n\n**Justification (each step worth partial credit):**\n1. Cartesian product formula stated correctly — 2 marks.\n2. Domain sizes identified correctly — 2 marks.\n3. Substitution and arithmetic — 2 marks.\n4. Final answer with units \"states\" — 1 mark.\n\n---\n\n#### **Question B (14 Marks)** — *[KTU University Exam — Supplementary Model, Module 4]*\n\n**(a) [7 Marks]** Write a Module Language program for a 2-bit *up-counter* that has an `enable` input and a synchronous `reset`. Specify an LTL property: *whenever `enable` stays high, the counter eventually reaches its maximum value 3*.\n\n**(b) [7 Marks]** Explain the difference between the `ASSIGN` block and the `TRANS` block. Why is non-determinism useful in a Module Language program?\n\n**Model Solution (a):**\n\n```\nMODULE main\n  VAR\n    cnt  : 0..3;\n    en   : boolean;\n    rst  : boolean;\n  ASSIGN\n    init(cnt) := 0;\n    next(cnt) :=\n      case\n        rst              : 0;\n        !en             : cnt;\n        cnt = 3         : 3;\n        TRUE            : cnt + 1;\n      esac;\n  LTLSPEC\n    G (G en -> F (cnt = 3))\n```\n\n**Explanation of each line (carries partial credit):**\n- `cnt : 0..3;` — 2-bit bounded counter. (1 mark)\n- `init(cnt) := 0;` — reset to 0 at initialisation. (1 mark)\n- `case ... esac` — priority encoder, a standard `ASSIGN` idiom. (2 marks)\n- `LTLSPEC G (G en -> F (cnt = 3))` — *globally, if `en` is high forever, then eventually the counter reaches 3*. (3 marks)\n\n**Model Solution (b):**\n\n| Aspect | `ASSIGN` block | `TRANS` block |\n|---|---|---|\n| Style | Operational (imperative `next(var) := ...`) | Declarative (relational predicate) |\n| Default | Anything not assigned is non-deterministic by default | Must specify *all* legal transitions |\n| Readability | Higher for small modules | Higher for highly-coupled state |\n| Compiler | Builds BDD from `:=` equations | Builds BDD from relational predicate |\n\n**Why non-determinism is useful (3 marks):**\n1. **Models abstraction** — abstracts the precise environment behaviour.\n2. **Models concurrency** — interleaving of parallel transitions.\n3. **Enables worst-case reasoning** — verifier checks *all* possible behaviours, so a single verified run implies *every* possible run is safe.\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Where KTU students *most commonly* lose marks on Module Language questions:**\n> 1. **Forgetting `init(var)`** in the `ASSIGN` block — the checker still accepts the program, but the model is wrong, costing 1–2 marks.\n> 2. **Mixing up `:=` (deterministic) and `:= {...}` (non-deterministic)** — the boundary is the *set literal braces*. Examiners allocate 1 mark for spotting this distinction.\n> 3. **Confusing `SPEC` (invariant) with `LTLSPEC`/`CTLSPEC`** — `SPEC name := AG p;` is propositional invariant; `LTLSPEC`/`CTLSPEC` accept full temporal formulas.\n> 4. **Writing `next` outside `ASSIGN` or `TRANS`** — `next(x)` is *only* legal inside those two blocks.\n> 5. **Forgetting the `case ... esac` priority** — `case` is evaluated top-down and the first matching arm wins; KTU examiners explicitly test this.\n> 6. **State-space formula misuse** — using $2^n$ alone when integer variables are present; the full formula is $2^{n_b} \\cdot \\prod (u_i + 1)$.\n\n### 5.4 Topic Recap & Important Things to Remember\n\n- The **Module Language** is a *finite-state* declarative language; every variable must be **bounded**.\n- A module program = **Module declaration + Variables + INIT + ASSIGN/TRANS + INVAR + SPECs + Fairness**.\n- A module maps directly onto a **Kripke structure** $M = (S, S_0, R, L)$.\n- **State-space size** = $2^{n_b} \\cdot \\prod (u_i + 1)$.\n- `:=` is **deterministic**, `:= {...}` is **non-deterministic**, `init(var) := v` is the **initialisation** form.\n- `next(var)` is legal **only** inside `ASSIGN`/`TRANS`.\n- `case ... esac` is a **priority case** statement (top-down priority).\n- `SPEC` ≈ simple invariant; `LTLSPEC` ≈ linear-time; `CTLSPEC` ≈ branching-time; `JUSTICE`/`COMPASSION` ≈ fairness.\n- Hierarchical **synchronous composition** is the default; asynchronous composition is covered in later modules.\n- The language is the *input contract* between the verification engineer and the symbolic model checker; the checker never invents variables, so the model is exactly what is written.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 4-t5",
          "title": "SAL Contexts",
          "content": "<!-- SECTION_1_START -->\n> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 4\n> - **Topic:** SAL Contexts\n\n# 📘 SAL Contexts — Module 4 | Automated Verification\n\n## 1. Core Technical Definition & Intuitive Overview\n\n> [!NOTE]\n> **SAL (Symbolic Analysis Laboratory)** is a framework developed by **SRI International** (SRI International's Computer Science Laboratory) for the formal modeling and verification of **transition systems**. It provides a common specification language that can be analyzed by multiple backend tools such as the **Symbolic Model Checker (SMC)**, the **Infinite-state Model Checker (SAL-INF)**, the **Bounded Model Checker (SAL-BMC)**, and the **SAL-thy** theorem prover (which interfaces with the **PVS** theorem prover).\n\n### 1.1 Formal Definition of a SAL Context\n\nA **SAL Context** is a **modular, reusable, and parameterizable specification unit** used to structure complex transition systems. A context encapsulates a *collection of related modules* and *defines the wiring* (instantiations, parameters, renaming, and visibility) that produces a concrete closed system suitable for model checking.\n\nFormally, a SAL Context is a **declarative container** consisting of:\n\n$$\n\\text{Context} \\; ::= \\; \\texttt{CONTEXT} \\; \\langle \\text{formal parameters} \\rangle \\;\n[ \\texttt{INCLUDES} \\;\\langle \\text{base modules} \\rangle ] \\;\n[ \\texttt{INSTANTIATION} \\;\\langle \\text{module instances} \\rangle ] \\;\n[ \\texttt{ASSERTIONS} \\;\\langle \\text{temporal claims} \\rangle ] \\;\n\\texttt{END}\n$$\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight (Module 4):** Contexts are the **top-level organizational unit** in SAL. Every SAL specification that is to be model-checked must be wrapped inside a context. The context binds the abstract modules to concrete parameters and instances, producing the **closed transition system** that is fed to the verification engine.\n\n### 1.2 Intuitive Analogy — \"The Architectural Blueprint\"\n\nImagine you are an **architect** designing a multi-storey apartment complex:\n\n| Architectural Element | SAL Context Equivalent |\n|---|---|\n| Floor plan template (reusable) | **Base Module** (e.g., `lift: MODULE`) |\n| Specific floor instance (Floor 1, Floor 2) | **Module Instantiation** (e.g., `lift1: lift`) |\n| The complete building blueprint | **CONTEXT** (e.g., `building: CONTEXT`) |\n| Building code / safety regulations | **ASSERTIONS** (e.g., `G (req -> F ack)`) |\n| Renaming floor labels per instance | **RENAME / HIDE** clauses |\n\nJust as an architect does not redraw every floor from scratch, a verification engineer does not rewrite every component. He **writes one module** and **instantiates it multiple times** inside a single **context**, attaching assertions that must hold over the composed system.\n\n### 1.3 Why SAL Contexts Are Necessary\n\n* **Compositionality:** Real systems (e.g., a network of elevators, a mutual-exclusion protocol with N processes) require composing **N identical components**. Contexts allow this without textual duplication.\n* **State-Space Control:** A context can *hide* internal variables of a module instance, exposing only externally observable transitions — this reduces the **state-space explosion** burden on the model checker.\n* **Parameterization:** Constants like the number of processes (`N`), buffer size (`B`), or clock values can be declared as **formal parameters** of the context.\n* **Reusability:** A well-designed context can be included by other contexts, building layered specifications.\n\n> [!TIP]\n> **Examination Tip:** When asked \"Why use a context?\", the board expects a **4-point answer** covering modularity, instantiation, parameterization, and assertion scoping. Memorize this quartet.\n\n### 1.4 The Five Logical Zones of a Context\n\nA SAL Context is partitioned into exactly **five logical zones**, executed in order:\n\n1. **Parameter Zone** — formal `int`, `real`, or `boolean` constants.\n2. **Inclusion Zone** — base modules brought in via `INCLUDES`.\n3. **Instantiation Zone** — concrete instances of modules.\n4. **Assertion Zone** — `THEOREM`, `AXIOM`, `CLAIM`, or `LEMMA` statements.\n5. **Closure Zone** — `END` keyword.\n\n---\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n## 2. Deep Theoretical Analysis & KTU High-Yield Reference Sheet\n\n### 2.1 Anatomy of a SAL Context — Structured Walkthrough\n\nBelow is the canonical skeleton of a SAL context, annotated with the purpose of each section.\n\n```\nCONTEXT\n  c1 : CONTEXT = ...\n  m1 : MODULE = ...\nINSTANTIATION\n  inst1 : m1(...)\nASSERTIONS\n  ...\nEND\n```\n\n#### Step-by-Step Logic of Compilation\n\n* **Step 1 — Parameter Resolution:** The SAL parser resolves all formal parameters (e.g., `N : INTEGER`) by substituting their values (given at the command line via `sal-inf -i N=3 model.sal`).\n* **Step 2 — Module Loading:** All modules listed in the `INCLUDES` section are loaded into the symbol table.\n* **Step 3 — Instantiation & Unification:** For each `inst : ModuleName(args)`, the SAL tool generates a *fresh copy* of the module's variable set, prefixed by the instance name. This is the **state-space multiplication** step.\n* **Step 4 — Hiding / Renaming:** The `->` and `HIDE` operators rewrite the variable namespace of an instance to enforce encapsulation.\n* **Step 5 — Assertion Checking:** The CTL/LTL assertions in the `ASSERTIONS` block are checked against the composed transition system by the selected backend (e.g., `sal-smc`).\n\n### 2.2 Operator Reference Table\n\n| Operator / Keyword | Category | Purpose | KTU Frequency |\n|---|---|---|---|\n| `CONTEXT ... END` | Container | Wraps the whole specification | ★★★★★ |\n| `INCLUDES` | Composition | Brings in external `.mod` or `.sal` files | ★★★★☆ |\n| `INSTANTIATION` | Composition | Creates named instances of modules | ★★★★★ |\n| `RENAME` (`->`) | Encapsulation | Maps instance variables to fresh names | ★★★★☆ |\n| `HIDE` | Encapsulation | Removes variables from the public interface | ★★★☆☆ |\n| `AXIOM` | Property | A constraint assumed to hold (no proof) | ★★☆☆☆ |\n| `THEOREM` | Property | A claim that must be model-checked | ★★★★★ |\n| `CLAIM` | Property | A *named* sub-property, reusable via `CLAIM name` | ★★★☆☆ |\n| `G` (Globally) | Temporal | $\\forall t.\\, \\varphi(t)$ | ★★★★★ |\n| `F` (Finally / Future) | Temporal | $\\exists t.\\, \\varphi(t)$ | ★★★★★ |\n| `X` (Next) | Temporal | $\\varphi$ at next step | ★★★☆☆ |\n| `U` (Until) | Temporal | $\\varphi\\, \\mathcal{U}\\, \\psi$ | ★★★★☆ |\n| `[]` (Box) | State | Equivalent to `G` | ★★★☆☆ |\n| `<>` (Diamond) | State | Equivalent to `F` | ★★★☆☆ |\n\n> [!IMPORTANT]\n> **Do not use the bare `|` (vertical bar) inside markdown table cells.** Use `\\vert` or `\\mid` for any absolute-value or divider notation in formulas. Example: $x \\mid y$ in a row would break the table — prefer $x \\div y$ or $x \\mid y$ written in a code span.\n\n### 2.3 Mathematical Foundations Underlying Contexts\n\nA context realizes the **synchronous product** of its instantiated transition systems. If module $M_i$ has the transition relation $\\rightarrow_{M_i}$, then the context's transition relation is:\n\n$$\n\\rightarrow_{C} \\;=\\; \\bigotimes_{i=1}^{k} \\rightarrow_{M_i} \\;\\cap\\; \\text{synchronization constraints}\n$$\n\nFor $k$ instances of an identical module, the **state-space cardinality** grows as:\n\n$$\n\\vert S_C \\vert \\;=\\; \\prod_{i=1}^{k} \\vert S_{M_i} \\vert\n$$\n\nFor $k$ identical modules each with $n$ states:\n\n$$\n\\vert S_C \\vert \\;=\\; n^k\n$$\n\nThis **exponential blow-up** is precisely the **state-space explosion** problem that motivates the use of *symbolic* (BDD-based) model checkers like `sal-smc`.\n\n### 2.4 Real-World Engineering Utility\n\n* **Railway Interlocking Systems:** Each track-side controller is a SAL module; the interlocking logic is a context instantiating N controllers with `THEOREM` blocks asserting \"no two trains on the same track segment.\"\n* **Cryptographic Protocol Verification:** The Needham-Schroeder protocol has been verified using SAL contexts with 2, 3, and 4 agent instances.\n* **Air-Traffic Control:** Multiple aircraft modules composed under a context that asserts `G (controller.request -> F controller.grant)` with bounded response time.\n* **Embedded Real-Time Systems:** Parameterized contexts let engineers verify an arbitration protocol for *any* number of bus masters.\n\n---\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Derivation: State-Space Size of a Context with $k$ Identical Instances\n\nLet a module $M$ have $\\vert S_M \\vert = n$ reachable states. A context $C$ instantiates $M$ exactly $k$ times with no synchronization constraints (asynchronous composition). We derive the number of reachable states of $C$.\n\n**Step 1 — Single-module state count.**\nThe module $M$ has $n$ states, written as:\n\n$$\n\\vert S_M \\vert = n\n$$\n\n**Step 2 — Independent composition of two instances.**\nFor two instances, the joint state is the Cartesian product of the individual state sets:\n\n$$\n\\vert S_{M_1 \\otimes M_2} \\vert = \\vert S_{M_1} \\vert \\cdot \\vert S_{M_2} \\vert = n \\cdot n = n^2\n$$\n\n**Step 3 — Generalization to $k$ instances.**\nBy induction, the joint state space is the $k$-fold product:\n\n$$\n\\begin{aligned}\n\\vert S_{C_k} \\vert &= \\vert S_{C_{k-1}} \\vert \\cdot \\vert S_{M_k} \\vert \\\\\n&= n^{k-1} \\cdot n \\\\\n&= n^k\n\\end{aligned}\n$$\n\n**Step 4 — Synchronous product (worst case).**\nIf the instances are tightly synchronized (every transition occurs simultaneously), the reachable states may be strictly fewer than $n^k$ because of the synchronization constraints. However, the **upper bound** remains:\n\n$$\n\\vert S_{C_k} \\vert \\;\\leq\\; n^k\n$$\n\n**Step 5 — Worked numeric example.**\nLet $n = 10$ states per process and $k = 4$ processes.\n\n$$\n\\begin{aligned}\n\\vert S_{C_4} \\vert &= 10^4 \\\\\n&= 10{,}000 \\text{ states}\n\\end{aligned}\n$$\n\nThis is tractable. But for $n = 50$ and $k = 6$:\n\n$$\n\\begin{aligned}\n\\vert S_{C_6} \\vert &= 50^6 \\\\\n&= 15{,}625{,}000{,}000 \\text{ states}\n\\end{aligned}\n$$\n\nThis is **15.6 billion states** — clearly infeasible for explicit-state model checking, motivating **symbolic BDD-based** checking.\n\n### 3.2 Derivation: BDD Size Growth for a Counter Module\n\nA counter module `C : MODULE` has a single integer variable `x : INTEGER`. The number of distinct BDD nodes required to encode $x$ for $b$ bits is:\n\n$$\n\\boxed{\\;N_{\\text{BDD}}(b) \\;\\leq\\; b \\cdot c\\;}\n$$\n\nwhere $c$ is a small constant (typically 2–4). The variable ordering has a dramatic effect: a good ordering yields a BDD of size $O(b)$, while a bad ordering can yield $O(2^b)$.\n\n> [!NOTE]\n> **Why This Matters in SAL Contexts:** A context with $k$ counter instances effectively has $k$ variables $x_1, \\ldots, x_k$. If the BDD variable order interleaves $x_i$ bits, the BDD size remains manageable; if not, it explodes. The `sal-smc` tool provides heuristics for variable ordering, but the *specification* (i.e., the context) determines the structure.\n\n### 3.3 Analytical Matrix: Context-Element Decision Table\n\n| If the system has … | Then declare … | Then use … | Example Assertion |\n|---|---|---|---|\n| $N$ identical processes | A module + $N$ instances | `INSTANTIATION proc1: P, ..., procN: P` | `G (mutex)` |\n| A single shared resource | A `Resource` module | `INCLUDES` or `INSTANTIATION` | `G (granted -> X !granted \\/ granted)` |\n| Parameterized count | `n : INTEGER` in context | Pass to module: `proc(i: n): P` | `THEOREM forall(x: n) => G(...)` |\n| Hidden internal state | Variables private to module | `HIDE x IN inst` | (assertions see only `inst.x`) |\n| Renamed public interface | `RENAME x IN inst -> y` | `y` becomes the public name | Reference `inst.y` in theorems |\n\n---\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Mermaid Diagram: Anatomy of a SAL Context\n\n```mermaid\nflowchart TD\n    A[\"Start: CONTEXT keyword\"]\n    P[\"Parameter Zone\\nn : INTEGER, B : BOOLEAN\"]\n    I[\"INCLUDES Zone\\nBase modules loaded\"]\n    S[\"INSTANTIATION Zone\\nproc1 : P, proc2 : P, ...\"]\n    R[\"RENAME / HIDE Zone\\nEncapsulation of internals\"]\n    AS[\"ASSERTIONS Zone\\nTHEOREM, AXIOM, CLAIM\"]\n    E[\"End: END keyword\"]\n\n    A --> P\n    P --> I\n    I --> S\n    S --> R\n    R --> AS\n    AS --> E\n\n    P -. \"Formal parameters\\nfed from CLI\" .-> S\n    I -. \"Symbol table\\nresolution\" .-> S\n    S -. \"State-space\\nproduct S^n^k\" .-> AS\n    R -. \"Encapsulation\" .-> AS\n    AS -. \"Passed to\\nsal-smc / sal-inf\" .-> E\n```\n\n### 4.2 Mermaid Diagram: Three-Process Mutual Exclusion Context\n\n```mermaid\nflowchart LR\n    P1[\"proc1 : Process\\nLocal: state, crit\"]\n    P2[\"proc2 : Process\\nLocal: state, crit\"]\n    P3[\"proc3 : Process\\nLocal: state, crit\"]\n    R[\"Resource : Arbiter\\nGlobal: grant_1, grant_2, grant_3\"]\n\n    P1 <-->|shared grant| R\n    P2 <-->|shared grant| R\n    P3 <-->|shared grant| R\n\n    P1 -. \"G not crit1 and crit2\" .-> ASSERT[\"ASSERTIONS\\nTHEOREM mutex\"]\n    P2 -. \"G not crit2 and crit3\" .-> ASSERT\n    P3 -. \"G not crit3 and crit1\" .-> ASSERT\n    R --> ASSERT\n```\n\n> [!NOTE]\n> **Reading the Diagram:** Each `proc_i` is an **instance** of the same `Process` module, encapsulated by the `Resource` (arbiter) module. The `ASSERTIONS` node collects the **mutual-exclusion theorem** that the SAL model checker will attempt to prove.\n\n### 4.3 Mermaid Block Diagram: SAL Tool Pipeline\n\n```mermaid\nflowchart LR\n    SRC[\"User writes\\ncontext .sal file\"]\n    PARSE[\"SAL Parser\\nLex + Yacc\"]\n    AST[\"Abstract Syntax Tree\"]\n    TYPE[\"Type Checker\\nResolve INT/REAL/BOOL\"]\n    BDD[\"BDD Manager\\nsal-smc backend\"]\n    MC[\"Model Checker\\nCTL/LTL Eval\"]\n    RES[\"Result: PROVED /\\nCOUNTER-EXAMPLE\"]\n\n    SRC --> PARSE --> AST --> TYPE --> BDD --> MC --> RES\n\n    ALT[\"Alternative Backend\\nsal-bmc / sal-inf / sal-thy\"]\n    TYPE --> ALT\n    ALT --> MC\n```\n\n### 4.4 Mermaid Diagram: Context Composition Patterns\n\n```mermaid\nflowchart TD\n    subgraph Stack1[\"Composition Style A: Instantiation\"]\n        BA[\"Base: M : MODULE\"]\n        CA[\"inst1 : M, inst2 : M\"]\n    end\n    subgraph Stack2[\"Composition Style B: Inclusion\"]\n        BB[\"Base: M : MODULE\\n(in separate file)\"]\n        CB[\"INCLUDES M\"]\n    end\n    subgraph Stack3[\"Composition Style C: Nested Context\"]\n        BC[\"Inner CONTEXT\\nsubctx : CONTEXT\"]\n        CC[\"Outer: subctx(params)\"]\n    end\n\n    BA --> CA\n    BB --> CB\n    BC --> CC\n\n    CA --> MERGE[\"Produces single\\nclosed transition system\"]\n    CB --> MERGE\n    CC --> MERGE\n```\n\n---\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n> [!NOTE]\n> The following questions are modeled on **KTU 2024 Scheme End-Semester Examination (ESE)** patterns. The mark distribution is split as **3-mark (Part A)** and **14-mark (Part B)** with internal choice, exactly as per the official regulation.\n\n### 📝 Part A — Short-Answer Questions (2 × 3 = 6 Marks)\n\n#### **Q1. Define a SAL Context. List its three primary functional roles.** `[KTU University Exam – May 2024, Model]`\n\n**Model Answer (3 marks):**\n\nA **SAL Context** is a top-level declarative container in the SAL specification language that aggregates parameters, included modules, module instantiations, and temporal assertions into a single closed specification suitable for model checking.\n\nIts three primary roles are:\n1. **Modular composition** — bringing together multiple base modules into one system.\n2. **Parameterization** — accepting formal parameters (e.g., the number of processes `n`) that can be instantiated at analysis time.\n3. **Assertion scoping** — declaring the theorems/claims that the model checker must verify over the composed system.\n\n> [!TIP]\n> For full marks, the examiner expects **all three roles** plus a *one-sentence* definition. Do not exceed 80 words.\n\n#### **Q2. Distinguish between `RENAME` and `HIDE` in a SAL Context.** `[KTU University Exam – Dec 2023]`\n\n**Model Answer (3 marks):**\n\n| Aspect | `RENAME (->)` | `HIDE` |\n|---|---|---|\n| Purpose | Maps an instance's internal variable to a *new public name* | Removes a variable from the *public* observable interface |\n| Visibility | Variable **remains** accessible, but under the new name | Variable **becomes inaccessible** outside the instance |\n| Use case | Disambiguating variables when including the same module twice | Encapsulating private state for state-space reduction |\n\n---\n\n### 📝 Part B — Full-Answer Questions (1 × 14 = 14 Marks)\n\n> **Internal Choice Pattern (KTU Standard):** You must answer **either Question A or Question B**. Each choice is divided into part (a) worth 7 marks and part (b) worth 7 marks.\n\n---\n\n#### **Question A (14 Marks)** `[KTU University Exam – Model Paper, 2024 Scheme]`\n\n**(a)** With a clearly annotated SAL example, explain the **five logical zones** of a context. State one practical reason for each zone. **(7 marks)**\n\n**(b)** Consider a 1-bit sender and 1-bit receiver communicating over a lossy channel. Model this using:\n   * a `Sender` module,\n   * a `Channel` module, and\n   * a `Receiver` module,\n   inside a single context `CommProtocol`. Write the **THEOREM** that \"every sent bit is eventually delivered\" using LTL. **(7 marks)**\n\n---\n\n**Model Solution:**\n\n**(a) — The Five Zones (7 marks):**\n\nA SAL context has **five zones**, each serving a specific purpose.\n\n1. **Parameter Zone** — declares formal parameters such as `n : INTEGER`. *Purpose:* allows the *same* context to be reused for different system sizes (1 process, 5 processes, etc.).\n2. **INCLUDES Zone** — references external modules. *Purpose:* promotes code reuse; a `Counter` module can be included by many contexts.\n3. **INSTANTIATION Zone** — creates concrete instances. *Purpose:* multiplies a base module's behaviour $k$ times without code duplication.\n4. **RENAME / HIDE Zone** — controls variable visibility. *Purpose:* prevents name clashes and reduces the public state space for the model checker.\n5. **ASSERTIONS Zone** — declares `THEOREM` and `AXIOM` blocks. *Purpose:* defines the *contract* the composed system must satisfy.\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall:** Students often list only 4 zones, merging RENAME/HIDE with INSTANTIATION. That costs **1 mark**. Always list all five distinctly.\n\n**(b) — The Communication Protocol (7 marks):**\n\nSAL sketch:\n\n```\nSender: MODULE\n  INPUT  ack : BOOLEAN\n  OUTPUT bit : BOOLEAN\n  INITIALIZATION bit = FALSE\n  TRANSITION\n    bit' IN {TRUE, FALSE};\n  END;\n\nChannel: MODULE\n  INPUT  sbit : BOOLEAN\n  OUTPUT rbit : BOOLEAN\n  TRANSITION\n    rbit' = sbit OR (rbit' IN {TRUE, FALSE});  %% lossy\n  END;\n\nReceiver: MODULE\n  INPUT  rbit : BOOLEAN\n  OUTPUT ack  : BOOLEAN\n  TRANSITION\n    ack' = rbit;  %% echo back\n  END;\n\nCommProtocol: CONTEXT\n  s : MODULE = Sender;\n  c : MODULE = Channel;\n  r : MODULE = Receiver;\nINSTANTIATION\n  sender  : s;\n  channel : c(s.bit);\n  receiver: r(channel.rbit);\nASSERTIONS\n  THEOREM eventuallyDelivered : G (sender.bit -> F receiver.ack);\nEND\n```\n\n**Explanation of THEOREM:**\n\nThe LTL formula $G(\\text{sender.bit} \\rightarrow F\\,\\text{receiver.ack})$ reads as:\n\n> *\"Globally, whenever a bit is sent, it is **eventually acknowledged** by the receiver.\"*\n\nThe `G` operator ranges over all time instants, and the `F` operator asserts existence of a future time. The model checker will either **prove** the formula or return a **counter-example trace** showing when a bit was lost.\n\n---\n\n#### **Question B (14 Marks — Alternative Choice)** `[KTU University Exam – Model Paper, 2024 Scheme]`\n\n**(a)** Explain the difference between **synchronous** and **asynchronous** composition of two SAL modules. Show with a state-transition diagram how the joint state space grows. **(7 marks)**\n\n**(b)** A traffic-light controller has three lights (Red, Yellow, Green). Model it as a SAL context with one `Light` module instantiated three times. Write the safety theorem: *\"Red and Green are never simultaneously ON\"*. **(7 marks)**\n\n---\n\n**Model Solution:**\n\n**(a) — Sync vs. Async Composition (7 marks):**\n\n* **Synchronous composition** means every component transitions in lockstep on every global step. The joint transition relation is:\n$$\n(s_1, s_2) \\rightarrow_{sync} (s_1', s_2') \\iff s_1 \\rightarrow s_1' \\;\\land\\; s_2 \\rightarrow s_2'\n$$\nThe number of joint states remains $\\vert S_1 \\vert \\cdot \\vert S_2 \\vert$ (assuming the components are *independent*), but the reachable states may be fewer due to coupling.\n\n* **Asynchronous composition** allows one component to transition while the other stays. The transition relation is:\n$$\n(s_1, s_2) \\rightarrow_{async} (s_1', s_2) \\;\\lor\\; (s_1, s_2) \\rightarrow_{async} (s_1, s_2')\n$$\nThis produces a **larger** reachable state graph because interleavings are added.\n\n**Diagram — Growth Pattern:**\n\n```\nModule A:  s0 ──► s1\nModule B:  t0 ──► t1\n\nSynchronous joint states:  (s0,t0) → (s1,t1)        -- 2 reachable\nAsynchronous joint states: (s0,t0) → (s1,t0) → (s1,t1)\n                           (s0,t0) → (s0,t1) → (s1,t1)  -- 4 reachable\n```\n\n> [!WARNING]\n> **Pitfall:** Many students write *asynchronous* when they mean *synchronous*. Remember — *async = interleaving*; *sync = simultaneous*.\n\n**(b) — Traffic-Light Context (7 marks):**\n\n```\nLight: MODULE\n  OUTPUT color : {RED, YELLOW, GREEN}\n  INITIALIZATION color = RED\n  TRANSITION\n    color' =\n      IF color = RED    THEN YELLOW\n      ELSE IF color = YELLOW THEN GREEN\n      ELSE RED ENDIF ENDIF;\n  END;\n\nTrafficSystem: CONTEXT\nINSTANTIATION\n  northLight  : Light;\n  eastLight   : Light;\n  southLight  : Light;\nASSERTIONS\n  THEOREM safety :\n    G NOT (northLight.color = GREEN AND\n          (eastLight.color = GREEN OR\n           southLight.color = GREEN));\nEND\n```\n\n**Justification of THEOREM:**\nThe expression $\\text{northLight.color} = \\text{GREEN} \\,\\land\\, (\\text{eastLight.color} = \\text{GREEN} \\lor \\text{southLight.color} = \\text{GREEN})$ is enclosed within `G NOT` to express the invariant: *\"At no time are conflicting green lights active.\"* The model checker will search for any reachable state where two greens co-exist.\n\n> [!WARNING]\n> **KTU Examiner's Pitfall:** Students often write the *negation* incorrectly as `G (north = GREEN -> east != GREEN)`. While logically equivalent in this case, the canonical form expected by the board is `G NOT (conflict)`. Using the `NOT` form is the safer answer.\n\n---\n\n### 🎯 Topic Recap & Important Things to Remember\n\n> [!TIP]\n> **Final-day revision checklist — SAL Contexts:**\n\n* ✅ A **SAL Context** is the *outermost* specification unit; every checked model must be wrapped in one.\n* ✅ The **five zones**: Parameter → INCLUDES → INSTANTIATION → RENAME/HIDE → ASSERTIONS.\n* ✅ A context produces a **closed transition system** suitable for `sal-smc`, `sal-bmc`, `sal-inf`, or `sal-thy`.\n* ✅ `INSTANTIATION` is the **state-space multiplication** step — it grows the BDD as $n^k$ for $k$ identical instances.\n* ✅ `HIDE` removes variables from the public interface; `RENAME` gives them a new public name.\n* ✅ The **THEOREM** keyword forces the model checker to verify a temporal claim; **AXIOM** is assumed without proof.\n* ✅ Common LTL operators to memorize: $G$ (globally), $F$ (finally), $X$ (next), $U$ (until), $\\square$ (box = G), $\\Diamond$ (diamond = F).\n* ✅ Always **list all five zones** distinctly in a \"define and list\" question — merging zones costs a mark.\n* ✅ In traffic-light / mutual-exclusion theorems, prefer the `G NOT (conflict)` form to the implication form.\n* ✅ Remember the upper-bound state-space formula: $\\vert S_C \\vert \\le n^k$ — this is the exponential blow-up that motivates symbolic checking.\n* ✅ **Examination tip:** A 14-mark \"explain with example\" question on contexts always expects a *small* working SAL snippet — practice writing one with at least one `MODULE`, one `INSTANTIATION`, and one `THEOREM`.\n\n---\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 4-t6",
          "title": "SAL Examples - Mutual Exclusion",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 4\n> - **Topic:** SAL Examples - Mutual Exclusion\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 What is SAL?\n\n**SAL (Symbolic Analysis Laboratory)** is an open-source framework developed by **SRI International** (Computer Science Laboratory) for the *symbolic* analysis and verification of **finite-state** and **infinite-state** transition systems. It bundles together several model checkers, theorem provers, and a common modeling language.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Highlight:** Under Module 4, SAL is studied as a *concrete model checker* used to verify safety and liveness properties of concurrent programs, with the **Mutual Exclusion** problem serving as the canonical pedagogical example.\n\nSAL is particularly significant because it supports:\n\n- **Symbolic Model Checking** using **BDDs (Binary Decision Diagrams)** via the `sal-smc` tool.\n- **Bounded Model Checking (BMC)** using SAT solvers via `sal-bmc` and `sal-inf-bmc`.\n- **Induction-based** and *invariant generation* proofs via `sal-inf-bmc` and `PVS`.\n- A common, expressive, *guarded-command* modeling language.\n\n## 1.2 What is Mutual Exclusion?\n\n**Mutual Exclusion** is a fundamental **concurrency property** that guarantees that **no two processes can be inside their Critical Section (CS)** at the same instant. Each concurrent process is structured as a sequence of four logical regions:\n\n$$\n\\text{NCS} \\;\\rightarrow\\; \\text{Try} \\;\\rightarrow\\; \\text{CS} \\;\\rightarrow\\; \\text{Exit} \\;\\rightarrow\\; \\text{NCS}\n$$\n\n> [!NOTE]\n> **Formal Definition (KTU Terminology):** A concurrent system composed of $n$ processes $\\{P_1, P_2, \\dots, P_n\\}$ satisfies mutual exclusion if and only if for all reachable global states $s$,\n> $$\\forall\\, i \\neq j : \\neg\\bigl(\\text{inCS}_i(s) \\wedge \\text{inCS}_j(s)\\bigr)$$\n> where $\\text{inCS}_k(s)$ is the predicate asserting that process $P_k$ is executing inside its critical section in state $s$.\n\n## 1.3 Conceptual Analogy — The \"Single Bathroom\" Intuition\n\nImagine a house with **one washroom** but **three roommates**. The bathroom is a *shared resource* that cannot tolerate simultaneous occupancy. The roommates (processes) use a *whiteboard protocol* (a shared variable like `turn` or array of `flag[]`) to coordinate who enters next. The fundamental rule, **mutual exclusion**, simply means: *\"At any given moment, at most one roommate can be inside the bathroom.\"*\n\nIn SAL terms:\n- The **bathroom** is the *Critical Section*.\n- The **whiteboard** is a *shared global variable* (e.g., `turn`).\n- The **behavioral contract** — \"no two inside, but eventually everyone gets in\" — is the *property to be model-checked*.\n\n## 1.4 Why Verify Mutual Exclusion?\n\nHand-written proofs of mutual exclusion (e.g., for Peterson's, Dekker's, or Lamport's Bakery algorithm) are notoriously error-prone, especially under:\n- **Weak memory models** (out-of-order execution).\n- **Compiler reordering** of statements.\n- **Arbitrary process interleaving**.\n\nSAL provides an *automated, exhaustive* verification of these algorithms by exploring **all** reachable states, eliminating human reasoning gaps.\n\n> [!TIP]\n> **Key Insight for Exams:** Mutual exclusion is a **safety property** (\"a bad state is never reached\"), while *starvation-freedom* (\"every request is eventually granted\") is a **liveness property**. SAL is typically used to discharge both, and your exam answers should clearly *classify* which type of property is being proved.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Architecture of a SAL Specification for Mutual Exclusion\n\nA SAL model for mutual exclusion is organized into three logical layers:\n\n### Layer 1 — The **Context** (Type Declarations)\nDeclares the *base types* (e.g., `BOOLEAN`, `NATURAL`) and *global constants* (e.g., the number of processes $N = 2$).\n\n### Layer 2 — The **Modules** (Process Models)\nEach process $P_i$ is defined as a *guarded transition system* of the form:\n- A set of *input* variables (read by $P_i$ but written by others),\n- A set of *output/local* variables (written by $P_i$),\n- An `INITIALIZATION` clause,\n- A `TRANSITION` clause with one or more *guarded commands*.\n\nA guarded command has the form:\n$$\n[\\,\\text{name}:\\; G \\;\\Longrightarrow\\; v_1' = E_1 \\;\\wedge\\; v_2' = E_2 \\;\\wedge\\; \\cdots\\,]\n$$\nwhere $G$ is the *guard predicate* and the right-hand side is the *next-state assignment*. A command is **enabled** in a state $s$ if $G$ holds in $s$.\n\n### Layer 3 — The **System Composition** (Synchronous/Asynchronous Parallelism)\nThe composed system is either:\n- **Synchronous** (`P1 || P2`): all enabled transitions fire simultaneously, or\n- **Asynchronous** (`P1 [] P2`): only one enabled transition fires at a time.\n\nFor mutual exclusion, the *asynchronous* semantics is the standard choice, modeling *true concurrency*.\n\n## 2.2 The Two-Process Mutex: Logical State Machine\n\nEach process $P_i$ has the following operational semantics:\n\n| State Symbol | Meaning | Guard Conditions |\n|:---:|:---|:---|\n| $\\text{NCS}_i$ | Non-Critical Section | No constraint; default rest state |\n| $\\text{Try}_i$ | Trying to enter CS | $P_i$ is *requesting* access |\n| $\\text{CS}_i$ | Critical Section | $P_i$ is *executing* the protected code |\n| $\\text{Exit}_i$ | Leaving the CS | $P_i$ is *releasing* access |\n\nThe transitions must satisfy the SAL \"always enabled\" rule: in *every* state, at least one outgoing transition must be enabled, so the model cannot deadlock unexpectedly.\n\n## 2.3 The KTU Formula Sheet\n\n> [!IMPORTANT]\n> **High-Yield Formula & Property Sheet** — Memorize this table; it appears almost verbatim in KTU university exam questions.\n\n| # | Construct / Property | Symbolic Form in SAL / CTL | Category |\n|:---:|:---|:---|:---:|\n| 1 | Global variable declaration | `GLOBAL g : BOOLEAN` | Modeling |\n| 2 | Asynchronous composition | `SYSTEM = P1 [] P2` | Modeling |\n| 3 | Synchronous composition | `SYSTEM = P1 || P2` | Modeling |\n| 4 | Safety: Mutual Exclusion | `AG NOT (cs1 AND cs2)` | **Safety** |\n| 5 | Liveness: No Starvation for $P_1$ | `AG (try1 => AF cs1)` | **Liveness** |\n| 6 | Invariant: $\\text{turn} \\in \\{0,1\\}$ | `AG (turn = 0 OR turn = 1)` | **Type-Safety** |\n| 7 | Reachable state count | $\\vert S \\vert = \\sum_{i} \\vert S_i \\vert$ (worst-case) | Metric |\n| 8 | Initial state predicate | $I(s) \\equiv s(g) = \\text{FALSE}$ | Logic |\n| 9 | Transition relation | $R(s,s') \\equiv \\bigvee_k G_k(s) \\wedge s' = \\delta_k(s)$ | Logic |\n| 10 | Fixed-point for `AG` | $\\text{AG}\\,\\phi \\equiv \\mu Z.\\; \\phi \\wedge \\text{AX}\\,Z$ | $\\mu$-Calculus |\n\n## 2.4 Real-World Engineering Utility\n\nMutual-exclusion verification via SAL is applied in:\n\n- **Embedded Operating Systems** (RTEMS, FreeRTOS) for kernel mutex proofs.\n- **Aerospace Control Software** (NASA uses symbolic model checkers for flight-control mutex).\n- **Multi-core Processor Cache Coherency Protocols** (MESI, MOESI).\n- **Hardware Bus Arbitration** logic in System-on-Chip (SoC) designs.\n- **Automotive ECUs** for shared-sensor arbitration in CAN-bus stacks.\n\n> [!NOTE]\n> **Engineering Tip:** In industry, SAL-style symbolic model checking is frequently used in the **DO-178C** (avionics) and **ISO 26262** (automotive) certification processes, where formal proofs of safety properties are legally required.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Complete SAL Model — A 2-Process Mutex (Turn-Based Protocol)\n\nWe now derive, line-by-line, a SAL model for the simplest correct mutex: a **turn-based** protocol. Two processes alternate using a shared variable `turn`.\n\n$$\n\\begin{aligned}\n\\textbf{Algorithm (informal):}\\quad\n&\\textbf{Process } P_i \\text{ (for } i \\in \\{1, 2\\})\\\\\n&1.\\; \\text{Loop: } \\text{NCS}\\\\\n&2.\\; \\text{while } \\text{turn} \\neq i : \\text{skip} \\quad \\text{(spinning)}\\\\\n&3.\\; \\text{CS}_i\\\\\n&4.\\; \\text{turn} \\leftarrow 3 - i \\quad \\text{(yield to the other)}\n\\end{aligned}\n$$\n\n### Step A — Context Declaration\nWe declare the *base type* and the *shared global* variable.\n\n```\nMUTEX: CONTEXT =\nBEGIN\n  GLOBAL turn : NATURAL\nEND\n```\n\n*Derivation Note:* The variable `turn` is declared `GLOBAL` so it is shared across all modules. Its type `NATURAL` must be bounded; SAL's `sal-smc` requires finite domains, so we implicitly restrict $\\text{turn} \\in \\{1, 2\\}$.\n\n### Step B — Module for Process $P_1$\n\n```\nP1: MODULE =\nBEGIN\n  LOCAL flag1 : BOOLEAN\n  INITIALIZATION\n    flag1 = FALSE\n  TRANSITION\n    [ request1  :  flag1' = TRUE ]\n    [ enter1    :  flag1 = TRUE AND turn = 1\n                    -->  flag1' = FALSE ]\n    [ release1  :  flag1 = FALSE --> skip ]\nEND\n```\n\n*Derivation Note:* \n- `flag1` is *local* to $P_1$ (it cannot be read or written by $P_2$).\n- The `enter1` command has the guard `flag1 = TRUE AND turn = 1` — this is the precise logical condition under which $P_1$ may enter its CS.\n- The `-->` separates the guard from the next-state assignment (SAL guarded-command syntax).\n\n### Step C — Module for Process $P_2$ (Symmetric)\n\n```\nP2: MODULE =\nBEGIN\n  LOCAL flag2 : BOOLEAN\n  INITIALIZATION\n    flag2 = FALSE\n  TRANSITION\n    [ request2  :  flag2' = TRUE ]\n    [ enter2    :  flag2 = TRUE AND turn = 2\n                    -->  flag2' = FALSE ]\n    [ release2  :  flag2 = FALSE --> skip ]\nEND\n```\n\n### Step D — System Composition (Asynchronous)\n\n```\nSYSTEM: MODULE = P1 [] P2\n```\n\n*Derivation Note:* The `[]` operator denotes *asynchronous* interleaving. In every step of the global transition, **exactly one** of the two processes executes *one* of its enabled commands. This is the standard model for *true concurrent execution* on a single processor and corresponds to the interleaving semantics of Kripke structures used in CTL.\n\n### Step E — Initialization of the Global State\n\n```\n% Initial condition\nMUTEX_init: THEOREM\n  SYSTEM |- AG (turn = 1 OR turn = 2)\n```\n\n*Derivation Note:* This `THEOREM` is a *type-invariant*: at all reachable states, `turn` is either 1 or 2. SAL's `sal-smc` verifies it automatically by fixed-point computation:\n$$\n\\text{AG}\\,\\phi \\;=\\; \\mu Z.\\; \\phi \\wedge \\text{AX}\\,Z\n$$\nThe fixed-point is computed over the *symbolic* state space encoded as a BDD.\n\n## 3.2 Safety Property — Mutual Exclusion\n\nThe central verification goal is:\n\n```\nMUTEX_safe: THEOREM\n  SYSTEM |- AG (NOT (P1.flag1 = FALSE AND P2.flag2 = FALSE\n                     AND P1.cs = TRUE AND P2.cs = TRUE))\n```\n\n### Step-by-Step Proof Sketch via Fixed-Point\n\n$$\n\\begin{aligned}\n\\text{Let } \\phi &\\equiv \\neg(\\text{cs}_1 \\wedge \\text{cs}_2) \\\\\n\\text{Basis:}\\quad Z_0(s) &= \\text{True} \\quad \\text{(over-approximation)} \\\\\n\\text{Iteration:}\\quad Z_{k+1}(s) &= \\phi(s) \\;\\wedge\\; \\forall s'.\\, R(s,s') \\Rightarrow Z_k(s') \\\\\n\\text{Limit:}\\quad Z^*(s) &= \\bigwedge_{k \\geq 0} Z_k(s) \\\\\n\\text{Result:}\\quad \\text{AG}\\,\\phi &= Z^*\n\\end{aligned}\n$$\n\n*Conversion Logic:* \n- $Z_0$ is the trivial over-approximation `True`. \n- $Z_1$ removes states where $\\phi$ is false, *and* all their predecessors. \n- $Z_2$ removes the new predecessors created by $Z_1$, and so on. \n- Because the state space is *finite*, the chain stabilizes at some $Z^* = Z_{k^*} = Z_{k^*+1}$, and $Z^* \\equiv \\text{AG}\\,\\phi$.\n\nIf the initial state satisfies $Z^*$ (i.e., $Z^*(\\text{init}) = \\text{True}$), the safety property is discharged.\n\n## 3.3 Liveness Property — Starvation Freedom\n\nA *stronger* property, often required, is that no process starves:\n\n$$\n\\text{Liveness:}\\quad \\text{AG}\\bigl(\\text{try}_1 \\Rightarrow \\text{AF}\\,\\text{cs}_1\\bigr)\n$$\n\n*Derivation:* In CTL, $\\text{AF}\\,\\psi$ means \"on *all* paths, $\\psi$ eventually holds.\" The full formula asserts: *globally, whenever $P_1$ is in the trying state, it will eventually reach its CS*. SAL's `sal-inf-bmc` proves this using **k-induction**:\n\n$$\n\\begin{aligned}\n\\text{Base case:}\\quad & I \\Rightarrow \\text{AF}\\,\\text{cs}_1 \\quad \\text{(checked via BMC up to depth } k\\text{)} \\\\\n\\text{Inductive step:}\\quad & I \\wedge \\text{AG}\\,\\text{try}_1 \\wedge \\text{AG}\\,\\neg\\text{cs}_1 \\;\\Longrightarrow\\; \\text{false}\n\\end{aligned}\n$$\n\n> [!WARNING]\n> **Common Pitfall:** Students frequently confuse `EF` (Exists-Eventually) with `AF` (All-Eventually). Liveness **requires** `AF`; using `EF` would only check that *some* path leads to the CS, which is trivially true and not what is intended.\n\n## 3.4 Analytical Matrix — Comparing SAL Tools\n\n| Tool | Algorithm Used | Suited For | Limitation |\n|:---|:---|:---|:---|\n| `sal-smc` | BDD-based symbolic model checking | Finite-state safety & liveness | State-space explosion |\n| `sal-bmc` | SAT-based bounded model checking | Finite-state bug-hunting up to depth $k$ | Cannot prove unbounded properties |\n| `sal-inf-bmc` | k-induction with invariant generation | Infinite-state systems (e.g., counters) | Requires manual lemma hints |\n| `sal-deadlock-checker` | Local cycle detection | Deadlock-freedom analysis | Cannot prove liveness |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 SAL Verification Pipeline for Mutual Exclusion\n\nThe following Mermaid block depicts the **end-to-end SAL workflow** for verifying the mutex specification. Each node represents a *stage* in the pipeline, and the arrows indicate the *control flow* and *feedback loops*.\n\n```mermaid\nflowchart TD\n    A[Problem Specification<br/>Mutual Exclusion Protocol] --> B[SAL Model File<br/>mutex.sal]\n    B --> C[Parse and Type-Check<br/>sal-parser]\n    C --> D{Property Type}\n    D -- Safety AG phi --> E[sal-smc<br/>BDD-based Model Checker]\n    D -- Liveness AF psi --> F[sal-inf-bmc<br/>k-Induction Engine]\n    E --> G{Property Holds}\n    F --> G\n    G -- Yes --> H[Generate Proof Certificate<br/>counterexample NONE]\n    G -- No --> I[Produce Counterexample Trace<br/>minimal witness]\n    I --> J[Refine Model or Add Lemma]\n    J --> B\n    H --> K[Verification Report<br/>VERIFIED]\n```\n\n*Reading the Diagram:*\n- The *parse* stage produces a typed AST (Abstract Syntax Tree).\n- The *decision* node `D` routes to either BDD-based or induction-based checking depending on the property class.\n- A *counterexample* is a witness trace; the loop `I -> J -> B` indicates the **Counterexample-Guided Abstraction Refinement (CEGAR)** style iterative workflow.\n\n## 4.2 State Transition Diagram — The 2-Process Turn-Based Mutex\n\nThe following diagram captures the **reachable global state space** of the 2-process turn-based protocol. Each node is a pair $(P_1, P_2)$ where $P_i \\in \\{\\text{NCS}, \\text{Try}, \\text{CS}\\}$. Only mutually exclusive regions are shown.\n\n```mermaid\nstateDiagram-v2\n    [*] --> NCS_NCS : initialization\n    NCS_NCS --> Try_NCS : P1 requests<br/>turn = 1\n    NCS_NCS --> NCS_Try : P2 requests<br/>turn = 2\n    Try_NCS --> CS_NCS : turn = 1 and P1 enters\n    NCS_Try --> NCS_CS : turn = 2 and P2 enters\n    CS_NCS --> NCS_Try : P1 releases<br/>turn becomes 2\n    NCS_CS --> Try_NCS : P2 releases<br/>turn becomes 1\n    Try_NCS --> Try_Try : both request<br/>turn is fixed\n    Try_Try --> CS_Try : P1 wins\n    Try_Try --> Try_CS : P2 wins\n    CS_NCS --> NCS_NCS : full release\n    NCS_CS --> NCS_NCS : full release\n```\n\n*Reading the State Diagram:*\n- The states `CS_CS` is **deliberately absent**, encoding the safety property.\n- The transition `Try_Try` is *not* a violation — it represents a benign race that the `turn` variable resolves deterministically.\n\n## 4.3 Property Hierarchy in SAL (Block-Level Architecture)\n\n```mermaid\nflowchart LR\n    subgraph CTL[CTL Property Hierarchy]\n        direction TB\n        A1[State Formulae<br/>phi]\n        A2[Path Formulae<br/>psi]\n        A1 --> A3[AG phi<br/>Invariant]\n        A1 --> A4[AF phi<br/>Liveness]\n        A2 --> A5[AX phi<br/>Next State]\n        A2 --> A6[A U phi<br/>Until]\n    end\n    subgraph APP[Applied to Mutex]\n        direction TB\n        B1[AG NOT cs1 AND cs2]\n        B2[AG try1 => AF cs1]\n        B3[AG turn IN 1,2]\n    end\n    A3 --> B1\n    A3 --> B3\n    A4 --> B2\n```\n\n*Reading the Architecture:*\n- The left block shows the *abstract CTL taxonomy*.\n- The right block instantiates these generic operators on the **mutex problem**.\n- The mapping $\\text{AG}\\,\\phi \\to \\text{AG}\\,\\neg(\\text{cs}_1 \\wedge \\text{cs}_2)$ is the canonical instantiation used in KTU exam answers.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n> [!IMPORTANT]\n> **Mark Distribution (KTU 2024 ESE):** Part A carries **3 marks each** (no choice). Part B carries **14 marks** with *internal choice* — attempt either the full OR-group. Every sub-question below is tagged with a KTU-style difficulty level and Bloom's cognitive domain.\n\n---\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### **Question 1** `[KTU University Exam - Dec 2023 | Remember / Understand]`\n\n**Explain the role of SAL in the context of automated verification of concurrent systems. How does it differ from traditional testing?**\n\n**Model Answer (3 marks):**\n- **(1 mark)** SAL is a symbolic model-checking framework developed by SRI International that uses BDDs and SAT solvers to exhaustively explore the state space of a transition system.\n- **(1 mark)** Unlike testing, which samples a finite number of execution traces, SAL explores **all** reachable states and verifies *temporal logic* properties (CTL/LTL) over every possible interleaving.\n- **(1 mark)** This makes SAL especially suitable for verifying concurrent algorithms like mutual exclusion, where a bug may appear only in a *specific* non-deterministic interleaving that testing can easily miss.\n\n---\n\n### **Question 2** `[KTU University Exam - May 2024 | Understand]`\n\n**Differentiate between safety and liveness properties. Give one example of each from the mutual-exclusion problem.**\n\n**Model Answer (3 marks):**\n\n| Aspect | Safety | Liveness |\n|:---|:---|:---|\n| **Meaning** | \"A bad thing never happens\" | \"A good thing eventually happens\" |\n| **CTL form** | $\\text{AG}\\,\\neg(\\text{cs}_1 \\wedge \\text{cs}_2)$ | $\\text{AG}(\\text{try}_1 \\Rightarrow \\text{AF}\\,\\text{cs}_1)$ |\n| **Mutex example** | No two processes are simultaneously in CS | Every request is eventually granted |\n| **Verification tool** | `sal-smc` | `sal-inf-bmc` (k-induction) |\n\n- **(1 mark)** Correct definition of safety with mutex example.\n- **(1 mark)** Correct definition of liveness with mutex example.\n- **(1 mark)** Accurate distinction in CTL form.\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks, Internal Choice)\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Pitfall Callout**\n> 1. **Forgetting to classify the property** as safety or liveness costs **at least 1 mark**.\n> 2. **Confusing `[]` (asynchronous) and `||` (synchronous) composition** in SAL is a recurring error — for mutex problems, the *correct* operator is **`[]`**.\n> 3. **Omitting the guard condition** in a SAL transition (e.g., writing `enter1: skip` without `flag1 = TRUE AND turn = 1`) will be marked as a **logical gap** (-2 marks).\n> 4. **Forgetting to state the tool used** (e.g., `sal-smc` vs `sal-bmc`) for a given property is a frequent KTU valuation deduction (-1 mark).\n\n---\n\n### **Question A** `[KTU University Exam - Dec 2023 | Apply / Analyze]`\n\n**(a)** Construct a complete SAL model for a 2-process mutual-exclusion protocol using a shared `turn` variable. Specify the **modules**, the **initialization**, the **transitions**, and the **system composition**. **(7 marks)**\n\n**(b)** State the safety property *\"no two processes are simultaneously in the critical section\"* as a CTL formula. Explain, with a step-by-step fixed-point argument, how SAL's `sal-smc` verifies it. **(7 marks)**\n\n#### **Model Solution — Part (a) (7 marks):**\n\n```\nMUTEX: CONTEXT =\nBEGIN\n  GLOBAL turn : NATURAL\nEND\n\nP1: MODULE =\nBEGIN\n  LOCAL flag1 : BOOLEAN\n  INITIALIZATION\n    flag1 = FALSE\n  TRANSITION\n    [ req1   : flag1' = TRUE ]\n    [ ent1   : flag1 = TRUE AND turn = 1\n               --> flag1' = FALSE ]\n    [ rel1   : flag1 = FALSE --> skip ]\nEND\n\nP2: MODULE =\nBEGIN\n  LOCAL flag2 : BOOLEAN\n  INITIALIZATION\n    flag2 = FALSE\n  TRANSITION\n    [ req2   : flag2' = TRUE ]\n    [ ent2   : flag2 = TRUE AND turn = 2\n               --> flag2' = FALSE ]\n    [ rel2   : flag2 = FALSE --> skip ]\nEND\n\nSYSTEM: MODULE = P1 [] P2\n```\n\n**Mark Allocation:**\n- (2 marks) Correct context and global variable declaration.\n- (2 marks) Correct module structure for $P_1$ and $P_2$ with valid guards.\n- (2 marks) Correct asynchronous composition `P1 [] P2`.\n- (1 mark) `INITIALIZATION` clause correctly defined.\n\n#### **Model Solution — Part (b) (7 marks):**\n\n**CTL Formula (2 marks):**\n$$\n\\text{MUTEX\\_safe : THEOREM } \\text{SYSTEM} \\models \\text{AG}\\,\\neg(\\text{cs}_1 \\wedge \\text{cs}_2)\n$$\nwhere $\\text{cs}_i$ is a derived predicate true when process $P_i$ is in its critical section.\n\n**Fixed-Point Argument (5 marks):**\n$$\n\\begin{aligned}\n\\text{Let } \\phi(s) &\\equiv \\neg(\\text{cs}_1(s) \\wedge \\text{cs}_2(s)) \\\\\nZ_0(s) &= \\text{True} \\\\\nZ_{k+1}(s) &= \\phi(s) \\;\\wedge\\; \\forall s'.\\, R(s,s') \\Rightarrow Z_k(s') \\\\\n\\text{AG}\\,\\phi(s) &= \\mu Z.\\; \\phi(s) \\wedge \\text{AX}\\,Z(s)\n\\end{aligned}\n$$\n\nStep-by-step explanation:\n- (1 mark) The BDD engine computes $Z_0 = \\text{True}$ symbolically.\n- (1 mark) Each iteration removes states where $\\phi$ is false plus their predecessors.\n- (1 mark) Because the state space is finite, the chain $Z_0 \\supseteq Z_1 \\supseteq Z_2 \\supseteq \\cdots$ stabilizes at $Z^*$.\n- (1 mark) If $Z^*(\\text{init}) = \\text{True}$ for all initial states, the theorem is discharged.\n- (1 mark) The `sal-smc` tool returns \"proved\" with no counterexample.\n\n---\n\n### **Question B** `[KTU University Exam - May 2024 | Analyze / Evaluate]`\n\n**(a)** Compare the use of `sal-smc` (BDD-based) and `sal-bmc` (SAT-based bounded) model checkers for verifying the mutual-exclusion safety property. Discuss the advantages and limitations of each. **(7 marks)**\n\n**(b)** Show how the *liveness* property of starvation-freedom for $P_1$ can be expressed in CTL and verified using **k-induction** in `sal-inf-bmc`. Provide the inductive proof obligations explicitly. **(7 marks)**\n\n#### **Model Solution — Part (a) (7 marks):**\n\n| Aspect | `sal-smc` (BDD) | `sal-bmc` (SAT) |\n|:---|:---|:---|\n| **Algorithm** | BDD-based fixed-point over all reachable states | SAT-based, unrolled up to depth $k$ |\n| **Completeness** | Complete (proves or disproves any finite property) | Incomplete (proves absence of bugs up to depth $k$ only) |\n| **Strengths** | Handles arbitrary safety/liveness | Scales to huge state spaces; rapid bug-finding |\n| **Weakness** | BDD blowup for large domains | Cannot prove *unbounded* properties |\n| **Best for Mutex** | Verifying full `AG NOT (cs1 AND cs2)` | Quickly finding a violating interleaving |\n| **Tool invocation** | `sal-smc mutex.sal MUTEX_safe` | `sal-bmc -d 50 mutex.sal MUTEX_safe` |\n| **Output** | \"proved\" or counterexample | \"no violation up to $k$\" or witness trace |\n\n**Mark Allocation:**\n- (3 marks) Algorithm-level description of BDD vs SAT.\n- (2 marks) Trade-off discussion (completeness vs scalability).\n- (2 marks) Concrete tool invocation and appropriate use-case.\n\n#### **Model Solution — Part (b) (7 marks):**\n\n**CTL Liveness Formula (2 marks):**\n$$\n\\text{NO\\_STARVE\\_P1 : THEOREM } \\text{SYSTEM} \\models \\text{AG}(\\text{try}_1 \\Rightarrow \\text{AF}\\,\\text{cs}_1)\n$$\n\n**k-Induction Proof Obligations (5 marks):**\n\n**Base case (BMC up to depth $k$):**\n$$\nI(s_0) \\;\\wedge\\; R(s_0, s_1) \\;\\wedge\\; \\cdots \\;\\wedge\\; R(s_{k-1}, s_k) \\;\\Longrightarrow\\; \\text{AF}\\,\\text{cs}_1\n$$\n\n**Inductive step (assume $\\text{try}_1$ holds for $k$ consecutive steps, derive contradiction):**\n$$\n\\begin{aligned}\n&I(s_0) \\;\\wedge\\; R(s_0, s_1) \\;\\wedge\\; \\cdots \\;\\wedge\\; R(s_{k-1}, s_k) \\\\\n&\\wedge\\; \\bigwedge_{i=0}^{k} \\text{try}_1(s_i) \\\\\n&\\wedge\\; \\bigwedge_{i=0}^{k} \\neg\\text{cs}_1(s_i) \\\\\n&\\Longrightarrow\\; \\text{false}\n\\end{aligned}\n$$\n\n**Mark Allocation:**\n- (1 mark) Correct CTL formula.\n- (2 marks) Base case correctly formulated with BMC unfolding.\n- (2 marks) Inductive step correctly formulated with the contradiction hypothesis.\n- (1 mark) Statement of the tool: `sal-inf-bmc` performs both checks; if both succeed for some finite $k$, the liveness property holds for **all** executions (by k-induction).\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!NOTE]\n> **Rapid Revision Checklist** — Read this *immediately before* entering the exam hall.\n\n- ☐ **SAL** = Symbolic Analysis Laboratory; developed by **SRI International**; uses **BDDs** and **SAT** solvers.\n- ☐ A SAL model has three parts: **Context** (types/globals) → **Modules** (guarded transitions) → **System** (composition).\n- ☐ The composition operator `[]` denotes **asynchronous** interleaving (the right choice for mutex).\n- ☐ The composition operator `||` denotes **synchronous** composition (used for clocked hardware).\n- ☐ **Mutual Exclusion** is a **safety property**: $\\text{AG}\\,\\neg(\\text{cs}_1 \\wedge \\text{cs}_2)$.\n- ☐ **Starvation-freedom** is a **liveness property**: $\\text{AG}(\\text{try}_1 \\Rightarrow \\text{AF}\\,\\text{cs}_1)$.\n- ☐ `sal-smc` uses **BDD-based fixed-point** computation of $\\mu Z.\\, \\phi \\wedge \\text{AX}\\,Z$.\n- ☐ `sal-bmc` uses **SAT-based bounded** unrolling; incomplete for unbounded properties.\n- ☐ `sal-inf-bmc` performs **k-induction**: proves infinite-state liveness via base case + inductive step.\n- ☐ A **counterexample** in SAL is a finite trace from an initial state to a state violating the property.\n- ☐ **Always enabled rule**: every reachable state must have at least one enabled transition (to prevent silent deadlock).\n- ☐ For the turn-based protocol: $\\text{turn} \\in \\{1, 2\\}$ is a **type invariant** verified by `AG (turn = 1 OR turn = 2)`.\n- ☐ **Key CTL operators for KTU exams**: `AG`, `AF`, `AX`, `EF`, `EU`, `AU` — and the difference between *state* vs *path* formulae.\n- ☐ **Industry applications**: avionics (DO-178C), automotive (ISO 26262), multi-core cache-coherency, RTOS kernel verification.\n\n> **End of Module 4 — SAL Examples: Mutual Exclusion | KTU 2024 Scheme | PECNT526**\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 4-t7",
          "title": "Peterson's Protocol",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 4\n> - **Topic:** Peterson's Protocol\n\n<!-- SECTION_1_START -->\n## SECTION 1: Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition\n\n> [!IMPORTANT]\n> **Peterson's Protocol (1981)** is a *concurrent algorithm* devised by **Gary L. Peterson** to solve the **Critical Section Problem** for exactly **two (2) processes** executing on a shared-memory multiprocessor. It guarantees **Mutual Exclusion**, **Progress**, and **Bounded Waiting** using only ordinary atomic loads and stores (i.e., it requires no special hardware instructions such as Test-and-Set or Compare-and-Swap).\n\nFormally, the protocol is defined over the following shared state:\n- A boolean array `flag[2]`, where `flag[i]` indicates that process `P_i` *intends* to enter its critical section.\n- An integer `turn ∈ {0, 1}`, indicating whose turn it is to enter the critical section.\n\nEach process `P_i` (for $i \\in \\{0,1\\}$) executes the following infinite loop:\n\n```text\ndo {\n    flag[i]  := true;          // declare intent\n    turn     := j;             // yield the right-of-way to the other process\n    while ( flag[j]  AND  turn = j ) do { skip; }   // busy-wait\n    <CRITICAL SECTION>\n    flag[i]  := false;         // withdraw intent\n    <REMAINDER SECTION>\n} while ( true );\n```\n\nwhere $j = 1 - i$.\n\n### 1.2 Conceptual Analogy — \"The Two-Person Doorway\"\n\nImagine a **narrow office corridor** that can fit only one person at a time. Two coworkers, **A** and **B**, frequently need to walk through it.\n\n| Mechanism | Real-World Analogy | Protocol Element |\n|-----------|--------------------|------------------|\n| Raising hand at the door | \"I want to go through\" | `flag[i] := true` |\n| Pointing at the other person | \"After you, my friend\" | `turn := j` |\n| Pausing if the other raised their hand *and* it is their turn | Polite yielding | `while (flag[j] ∧ turn = j) skip` |\n| Lowering the hand after exiting | \"I'm done, you may go\" | `flag[i] := false` |\n\n> [!NOTE]\n> The brilliance of Peterson's design is that the **polite yielding** (`turn := j`) always favours the *other* process. If both processes raise their hands simultaneously, the last write to `turn` wins, and the *loser* waits — guaranteeing fairness and deadlock-freedom.\n\n### 1.3 Significance in Automated Verification\n\n> [!TIP]\n> **Why does an *Automated Verification* course teach Peterson's Protocol?**\n> It is the **canonical case study** in *model checking*. Its state space is small enough to be enumerated exhaustively, yet it exhibits all the pathologies of real concurrency (interleavings, race conditions, atomicity violations). Tools such as **SPIN** (with Promela), **TLA+**, **UPPAAL**, and **CBMC** routinely use Peterson's Protocol as a benchmark to validate the *soundness* and *completeness* of the verification engine itself.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 State-Space Model\n\nThe global system state at any instant is the tuple:\n\n$$S \\;=\\; \\langle \\text{pc}_0,\\ \\text{pc}_1,\\ \\text{flag[0]},\\ \\text{flag[1]},\\ \\text{turn} \\rangle$$\n\nwhere the program counter $\\text{pc}_i$ of process $P_i$ can be in one of the following labels:\n\n$$\\text{pc}_i \\in \\{\\ \\text{START},\\ \\text{FLAG},\\ \\text{TURN},\\ \\text{TEST},\\ \\text{CRIT},\\ \\text{EXIT},\\ \\text{REM}\\ \\}$$\n\nThis yields a finite state space of at most $7 \\times 7 \\times 2 \\times 2 \\times 2 = 392$ reachable states, which is the reason Peterson's Protocol is *highly amenable* to exhaustive model checking.\n\n### 2.2 The Three Correctness Properties\n\nA correct solution to the Critical Section Problem must satisfy:\n\n**Property 1 — Mutual Exclusion (Safety)**\n\n$$\\Box \\;\\neg \\bigl(\\ \\text{pc}_0 = \\text{CRIT} \\ \\wedge\\ \\text{pc}_1 = \\text{CRIT}\\ \\bigr)$$\n\n> In every reachable state, at most one process is inside the critical section.\n\n**Property 2 — Progress (Liveness)**\n\n$$\\Box \\Bigl[\\ \\bigl(\\exists i.\\ \\text{pc}_i = \\text{TEST}\\bigr) \\;\\Longrightarrow\\; \\Diamond \\bigl(\\exists i.\\ \\text{pc}_i = \\text{CRIT}\\bigr) \\ \\Bigr]$$\n\n> If some process is *waiting* to enter, then eventually *some* process will enter.\n\n**Property 3 — Bounded Waiting (Starvation-Freedom)**\n\n$$\\Box \\bigl[\\ \\text{pc}_i = \\text{TEST} \\;\\Longrightarrow\\; \\Diamond_{\\le n}\\ \\text{pc}_i = \\text{CRIT} \\ \\bigr], \\quad n = 2$$\n\n> A process that wishes to enter will do so within at most **two** \"turn-taking\" cycles.\n\n### 2.3 The Inductive Invariant\n\nThe classical proof of Mutual Exclusion hinges on the following **global invariant**, which holds at every reachable state:\n\n$$\\mathcal{I} \\;:\\;\\; \\bigl(\\text{pc}_i = \\text{CRIT}\\bigr) \\;\\Longrightarrow\\; \\Bigl[\\ \\text{flag}[i] = \\text{true} \\ \\wedge\\ \\text{turn} \\neq i\\ \\Bigr]$$\n\nCombined with the fact that at most one value can be written to `turn`, the contradiction emerges naturally when both processes claim CRIT simultaneously.\n\n### 2.4 KTU Formula Sheet / Cheat Sheet\n\n| # | Symbol / Expression | Meaning | Domain / Value |\n|---|---------------------|---------|----------------|\n| 1 | $\\text{flag}[i]$ | Intent flag of process $P_i$ | $\\text{true} \\mid \\text{false}$ |\n| 2 | $\\text{turn}$ | Whose turn to wait *for* | $\\{0,\\,1\\}$ |\n| 3 | $j$ | Index of the *other* process | $1 - i$ |\n| 4 | $\\vert \\text{Reachable States} \\vert$ | Total bounded state space | $\\le 392$ |\n| 5 | $\\text{Wait Bound}$ | Max times a process can be bypassed | $2$ |\n| 6 | $\\text{Memory Fences Req.}$ | To prevent reordering of `flag` and `turn` writes | $\\ge 2$ |\n| 7 | $n$ (processes) | Peterson's Protocol is valid *only* for | $n = 2$ |\n| 8 | Atomic Operations Required | Plain loads/stores suffice | Read/Write |\n\n> [!IMPORTANT]\n> **Generalization note**: For $n > 2$ processes, one uses **Lamport's Bakery Algorithm** (1974), which extends Peterson's idea by drawing a \"ticket number\" at the doorway.\n\n### 2.5 Real-World Engineering Utility\n\n| Application Domain | Why Peterson Matters |\n|--------------------|---------------------|\n| **Operating System Kernels** | Direct precursor to modern futex-based locking; informs the design of priority-inheritance mutexes. |\n| **Embedded / Real-Time Systems** | Bounded waiting is critical for schedulability analysis (Rate-Monotonic). |\n| **Formal Verification Tooling** | Default benchmark in *SPIN*, *TLA+*, *Coq*, *Isabelle/HOL*. |\n| **Multicore Firmware** | Concept of *memory barriers* (fences) was formalised because of weak-memory reorderings of exactly the `flag`/`turn` writes. |\n| **Hardware Transactional Memory (HTM)** | Peterson's pattern informs fallback-locking strategies when transactions abort. |\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Exhaustive Proof of Mutual Exclusion (Safety)\n\n**Theorem.** *For all reachable states $S$ of Peterson's Protocol, it is never the case that both $P_0$ and $P_1$ are simultaneously in their critical section.*\n\n**Proof by Contradiction.**\n\n**Step 1** — Assume, for contradiction, that there exists a reachable state $S^\\ast$ in which:\n\n$$\n\\text{pc}_0 = \\text{CRIT} \\quad \\wedge \\quad \\text{pc}_1 = \\text{CRIT}\n$$\n\n**Step 2** — Apply the global invariant $\\mathcal{I}$ to each process in $S^\\ast$:\n\n$$\n\\mathcal{I}(P_0) \\;\\Rightarrow\\; \\text{flag}[0] = \\text{true} \\ \\wedge\\ \\text{turn} \\neq 0\n$$\n\n$$\n\\mathcal{I}(P_1) \\;\\Rightarrow\\; \\text{flag}[1] = \\text{true} \\ \\wedge\\ \\text{turn} \\neq 1\n$$\n\n**Step 3** — Simplify the `turn` conditions:\n\nFrom $\\text{turn} \\neq 0$, deduce $\\text{turn} = 1$. From $\\text{turn} \\neq 1$, deduce $\\text{turn} = 0$.\n\n**Step 4** — Combine the two deductions:\n\n$$\n\\text{turn} = 1 \\quad \\wedge \\quad \\text{turn} = 0\n$$\n\nThis is a logical contradiction, because the variable `turn` can hold exactly one value at a time. ∎\n\n---\n\n### 3.2 Exhaustive Proof of Progress (Liveness)\n\n**Theorem.** *If a process is in the TEST state (i.e., spinning at the `while` loop), then it will eventually execute its critical section.*\n\n**Proof.**\n\n**Step 1** — Suppose $P_0$ is stuck in the `while (flag[1] ∧ turn = 1)` loop. This requires that $\\text{flag}[1] = \\text{true}$ *and* $\\text{turn} = 1$.\n\n**Step 2** — For $P_0$ to leave the loop, either:\n- (a) $\\text{flag}[1]$ must become $\\text{false}$, or\n- (b) $\\text{turn}$ must change from $1$ to $0$.\n\n**Step 3** — Examine $P_1$. If $P_1$ never enters the critical section, it never executes `flag[1] := false`. We must show this is impossible.\n\n**Step 4** — Note that $P_1$ reached $\\text{flag}[1] = \\text{true}$ by writing it. If $P_1$ is *not* in the critical section, it is in either the REMAINDER or one of the pre-CRIT states. From the pre-CRIT states, the only way $P_1$ does not progress is if it is busy-waiting, which requires $\\text{flag}[0] = \\text{true} \\wedge \\text{turn} = 0$. But we assumed $P_0$ is in the loop, not the critical section, and $\\text{turn} = 1$, so $P_1$ is *not* busy-waiting.\n\n**Step 5** — Hence $P_1$ will progress, eventually enter CRIT, finish, and execute $\\text{flag}[1] := \\text{false}$. At that moment, condition (a) is satisfied and $P_0$ proceeds. ∎\n\n---\n\n### 3.3 Exhaustive Proof of Bounded Waiting (Fairness)\n\n**Theorem.** *A process $P_i$ can be bypassed at most once before it enters its critical section.*\n\n**Proof.**\n\n**Step 1** — Suppose $P_0$ is bypassed, i.e., $P_1$ enters CRIT while $P_0$ is waiting. By the `while` condition, at the time $P_1$ left the loop, $\\text{turn}$ was $0$ (favouring $P_0$) or $\\text{flag}[0]$ was false.\n\n**Step 2** — When $P_1$ entered the critical section, it was because $P_0$ had not yet set $\\text{flag}[0] = \\text{true}$ *or* `turn` was $0$.\n\n**Step 3** — Crucially, before $P_1$ exits, it executes $\\text{flag}[1] := \\text{false}$, *unblocking* $P_0$ from its current wait. Even if $P_0$ re-spins, it can be overtaken by $P_1$ at most *one additional time* before the interlock resolves.\n\n**Step 4** — Therefore the wait bound is $\\mathbf{n = 2}$ for a 2-process system. ∎\n\n---\n\n### 3.4 Analytical Matrix — Verifying Peterson with a Model Checker\n\n| Step | Action | Tool Used | Property Type | Expected Verdict |\n|------|--------|-----------|---------------|------------------|\n| 1 | Encode the algorithm in Promela | SPIN | – | – |\n| 2 | Define a `never` claim for mutual exclusion | SPIN LTL | Safety ($\\square$) | `acceptance cycle` not found |\n| 3 | Define LTL for progress | SPIN LTL | Liveness ($\\square \\Diamond$) | Not violated |\n| 4 | Run `spin -a peterson.pml` then `gcc pan.c -o pan` | GCC | – | – |\n| 5 | Execute `./pan` and inspect the trail | Shell | – | `errors: 0` |\n\n### 3.5 Why Memory Reordering Breaks Peterson (Modern Relevance)\n\nOn modern **weak-memory** CPUs (ARM, x86 with TSX, RISC-V), the lines\n\n```text\nflag[i] := true;\nturn     := j;\n```\n\nmay be **reordered by the compiler or the CPU**, breaking the protocol. The fix is to insert a **memory fence / barrier**:\n\n```text\nflag[i] := true;\n__sync_synchronize();   /* memory barrier */\nturn     := j;\n__sync_synchronize();   /* memory barrier */\n```\n\n> [!WARNING]\n> KTU examiners frequently test whether students can articulate *why* the order of writes matters. Forgetting the fence on weakly-ordered architectures is a classic pitfall — and is worth **at least 2–3 marks** in ESE answers.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## SECTION 4: Structural Diagrams & Schematics\n\n### 4.1 Flowchart — Control Flow of Process $P_i$ in Peterson's Protocol\n\n```mermaid\nflowchart TD\n    nodeStart([Start of Loop])\n    nodeFlag[Set flag of i equals true]\n    nodeTurn[Set turn equals j]\n    nodeCheck{flag of j equals true AND turn equals j}\n    nodeSpin[Busy Wait - spin in loop]\n    nodeCrit[Execute Critical Section]\n    nodeExit[Set flag of i equals false]\n    nodeRem[Execute Remainder Section]\n    nodeEnd([Loop back to Start])\n\n    nodeStart --> nodeFlag\n    nodeFlag --> nodeTurn\n    nodeTurn --> nodeCheck\n    nodeCheck -- YES --> nodeSpin\n    nodeSpin --> nodeCheck\n    nodeCheck -- NO --> nodeCrit\n    nodeCrit --> nodeExit\n    nodeExit --> nodeRem\n    nodeRem --> nodeEnd\n```\n\n### 4.2 Block-Level Functional Architecture — Shared-Memory View\n\n```mermaid\nflowchart LR\n    subgraph procA[PROCESS P0]\n        a0[Local PC]\n        a1[Set flag 0 true]\n        a2[Set turn 1]\n        a3[Test flag 1 and turn]\n        a4[Critical Section]\n        a5[Set flag 0 false]\n    end\n\n    subgraph procB[PROCESS P1]\n        b0[Local PC]\n        b1[Set flag 1 true]\n        b2[Set turn 0]\n        b3[Test flag 0 and turn]\n        b4[Critical Section]\n        b5[Set flag 1 false]\n    end\n\n    mem[(SHARED MEMORY<br/>flag 0 flag 1 turn)]\n\n    a1 -. WRITES .-> mem\n    a2 -. WRITES .-> mem\n    a3 -. READS .-> mem\n    a5 -. WRITES .-> mem\n\n    b1 -. WRITES .-> mem\n    b2 -. WRITES .-> mem\n    b3 -. READS .-> mem\n    b5 -. WRITES .-> mem\n\n    mem -. ARBITRATION .-> a3\n    mem -. ARBITRATION .-> b3\n```\n\n### 4.3 Verification Workflow Diagram (SPIN Toolchain)\n\n```mermaid\nflowchart TD\n    s1[Promela Source: peterson.pml]\n    s2[SPIN Parser and Simulator]\n    s3{Kind of Verification}\n    s4[Safety Check via Never Claims]\n    s5[Liveness Check via LTL]\n    s6[State Space Explorer pan.c]\n    s7[Compilation: gcc pan.c]\n    s8[Execution and Trail Analysis]\n    s9[Verdict: Property Holds or Counterexample]\n\n    s1 --> s2\n    s2 --> s3\n    s3 --> s4\n    s3 --> s5\n    s4 --> s6\n    s5 --> s6\n    s6 --> s7\n    s7 --> s8\n    s8 --> s9\n```\n\n### 4.4 Interleaving Race-Condition Matrix\n\nThe table below shows the **only four meaningful interleavings** at the doorway when both processes raise their intent flag simultaneously. The last writer of `turn` always loses the race.\n\n| # | Interleaving Order | Final `turn` | Who waits? | Who enters? |\n|---|--------------------|--------------|------------|-------------|\n| 1 | `flag[0] := true; flag[1] := true; turn := 1; turn := 0` | $0$ | $P_1$ | $P_0$ |\n| 2 | `flag[0] := true; flag[1] := true; turn := 0; turn := 1` | $1$ | $P_0$ | $P_1$ |\n| 3 | `flag[1] := true; flag[0] := true; turn := 1; turn := 0` | $0$ | $P_1$ | $P_0$ |\n| 4 | `flag[1] := true; flag[0] := true; turn := 0; turn := 1` | $1$ | $P_0$ | $P_1$ |\n\n> [!NOTE]\n> The protocol exhibits a **deterministic tie-breaker** via `turn`. There is **never** an interleaving where both processes spin forever — a guarantee formally known as *deadlock-freedom*.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short-Answer Questions (3 Marks Each)\n\n> **Q1.** `[KTU University Exam — May 2024]`\n> **Define the Critical Section Problem. State the three requirements a correct solution must satisfy.**\n\n**Model Answer (3 marks):**\n\nThe **Critical Section Problem** is the task of ensuring that among $n$ concurrent processes sharing a common resource, exactly one process executes its *critical section* (the code segment that accesses the shared resource) at any given time.\n\nThe three correctness requirements are:\n1. **Mutual Exclusion** — No two processes are simultaneously inside their critical sections.\n2. **Progress** — If no process is in its critical section and some processes wish to enter, only those *not in the remainder section* can decide who enters next, and the decision cannot be postponed indefinitely.\n3. **Bounded Waiting** — There exists a finite upper bound on the number of times other processes are allowed to enter the critical section after a process has declared its intent and before that process itself enters. *(3 marks)*\n\n---\n\n> **Q2.** `[KTU University Exam — Dec 2023]`\n> **List the shared variables used in Peterson's Protocol and state the role of each.**\n\n**Model Answer (3 marks):**\n\nPeterson's Protocol uses two categories of shared variables:\n- **`flag[0]` and `flag[1]`** (boolean array): Each `flag[i]` signals *intent* — when set to `true`, process $P_i$ declares that it wishes to enter the critical section.\n- **`turn`** (integer, range $\\{0, 1\\}$): Acts as a *courtesy variable*. By writing `turn := j`, a process voluntarily yields priority to the other process. The last writer \"loses\" the arbitration and must wait. *(3 marks)*\n\n---\n\n### 5.2 Part B — Module-Internal Choice (14 Marks Each)\n\n> **Note (KTU 2024 Scheme):** *Answer ONE full question from the choice. Each sub-part carries **7 marks**.*\n\n---\n\n### ➤ Question A (14 Marks)\n\n> **Q-A(a).** `[KTU University Exam — Dec 2024]`\n> **Explain Peterson's Protocol for two processes. Discuss how the algorithm satisfies Mutual Exclusion by constructing a clear proof.**\n\n**Model Solution (7 marks):**\n\n**Definition and Pseudocode (3 marks):**\nPeterson's Protocol is a software-only solution to the two-process critical section problem. It uses `flag[0]`, `flag[1]` (boolean intent flags) and `turn` (an integer courtesy variable). Each process $P_i$ sets its own flag to `true`, writes `turn := j` to defer to the other, and then busy-waits in `while (flag[j] ∧ turn = j) skip;` until the other releases its intent.\n\n**Proof of Mutual Exclusion (4 marks):**\nAssume for contradiction that both $P_0$ and $P_1$ are simultaneously in CRIT. Then by the protocol semantics, the last statement each process executed before CRIT is the exit of the busy-wait loop, which requires $\\text{flag}[\\text{other}] = \\text{false}$ *or* $\\text{turn} \\neq \\text{other}$. Now apply the global invariant $\\mathcal{I}$: if $P_0$ is in CRIT then $\\text{turn} \\neq 0$, i.e., $\\text{turn} = 1$. Symmetrically, $P_1$ in CRIT forces $\\text{turn} = 0$. This yields the impossible conjunction $\\text{turn} = 1 \\wedge \\text{turn} = 0$, a contradiction. Hence mutual exclusion holds. *(7 marks)*\n\n---\n\n> **Q-A(b).** `[KTU University Exam — Dec 2024]`\n> **Demonstrate how Peterson's Protocol can be formally verified using the SPIN model checker. Write the key Promela fragment and the corresponding LTL property.**\n\n**Model Solution (7 marks):**\n\n**Promela fragment (3 marks):**\n\n```text\nbool flag[2];\nbyte turn;\n\nactive proctype P0() {\n    do\n    ::  flag[0] = true;\n        turn = 1;\n        (flag[1] == false || turn == 0);\n        /* critical section */\n        assert(!(flag[1] == true && turn == 1));\n        flag[0] = false;\n    od\n}\n\nactive proctype P1() {\n    do\n    ::  flag[1] = true;\n        turn = 0;\n        (flag[0] == false || turn == 1);\n        /* critical section */\n        flag[1] = false;\n    od\n}\n```\n\n**LTL Property (2 marks):**\n\n```text\n#define mutual  (!(P0@cs && P1@cs))\nltl mutual_exclusion { [] mutual }\n```\n\n**Verification steps (2 marks):**\n1. Save as `peterson.pml`, then run `spin -a peterson.pml`.\n2. Compile the generated verifier: `gcc -o pan pan.c`.\n3. Run `./pan -a` to perform an exhaustive search. The verdict `errors: 0` confirms that the LTL property holds in **all 392 reachable states**. *(7 marks)*\n\n---\n\n### ➤ Question B (14 Marks) — *Alternative Choice*\n\n> **Q-B(a).** `[KTU University Exam — May 2023]`\n> **Compare Dekker's Algorithm and Peterson's Protocol. Show that Peterson's is strictly simpler while achieving the same correctness guarantees.**\n\n**Model Solution (7 marks):**\n\n**Dekker's Algorithm (2 marks):**\nDekker's (1965) was the *first* correct software mutual-exclusion algorithm for two processes. It uses `flag[0]`, `flag[1]`, and `turn`, but its control flow is asymmetric — one process (say $P_0$) checks `turn == 0` *first* and $P_1$ checks `flag[0]` *first*. This results in a non-uniform code structure.\n\n**Peterson's Protocol (3 marks):**\nPeterson's (1981) collapses Dekker's asymmetry by having *both* processes execute *identical* code with the index `i` swapped. The single elegant line `turn := j` replaces Dekker's multi-branch arbitration. Thus Peterson's achieves the same three properties — ME, Progress, Bounded Waiting — with **strictly less code complexity** and improved symmetry, which makes it far easier to formally verify (the state space is symmetric and the invariants are uniform).\n\n**Trade-off (2 marks):**\nThe price of simplicity is that Peterson's, like Dekker's, relies on **sequential consistency**; on modern weakly-ordered hardware, both algorithms require memory fences to function correctly. *(7 marks)*\n\n---\n\n> **Q-B(b).** `[KTU University Exam — May 2023]`\n> **Explain why Peterson's Protocol fails on weakly-ordered architectures (e.g., ARM, RISC-V). Propose a hardware-aware fix.**\n\n**Model Solution (7 marks):**\n\n**The Problem (3 marks):**\nOn a weakly-ordered CPU, the compiler or the out-of-order execution engine is permitted to **reorder independent loads and stores** as long as data dependencies within a single thread are respected. The two writes in:\n\n```text\nflag[i] := true;\nturn     := j;\n```\n\nhave **no data dependency** between them. Therefore, the CPU may execute `turn := j` *before* `flag[i] := true`, and another process $P_j$ may observe `turn = i` while `flag[i]` is still `false`. This breaks the interlock: $P_j$ proceeds to its critical section, and $P_i$ — having observed `turn = j` — also proceeds, violating Mutual Exclusion.\n\n**The Fix (4 marks):**\nInsert a **memory barrier** (also called a *fence*) between the two writes, and another one *after* the `turn` write to enforce program order:\n\n```text\nflag[i] := true;\n__sync_synchronize();   /* full memory fence */\nturn     := j;\n__sync_synchronize();   /* full memory fence */\n```\n\nIn C11 / C++11 atomic syntax, one would write:\n\n```text\natomic_store_explicit(&flag[i], true, memory_order_release);\natomic_store_explicit(&turn,    j,    memory_order_release);\n```\n\nEquivalently, the busy-wait loop must issue an `acquire` load. The release-acquire pairing restores the necessary happens-before relation. With fences in place, Peterson's Protocol is correct on ARM, RISC-V, and weakly-ordered x86 implementations. *(7 marks)*\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common mistakes that cost marks in KTU valuation:**\n> 1. **Forgetting to name all three correctness properties** (ME, Progress, Bounded Waiting) when defining the Critical Section Problem — loses **1 mark** outright.\n> 2. **Writing the algorithm asymmetrically** (e.g., hard-coding `P_0` and `P_1` instead of using the generic index `i`) — examiners deduct **1–2 marks** for not demonstrating the elegance of Peterson's symmetric formulation.\n> 3. **Omitting the memory-fence caveat** in modern-architecture answers — **2 marks** penalty on a 14-mark question.\n> 4. **Confusing Peterson's Protocol with Lamport's Bakery Algorithm** — the Bakery algorithm is the *N-process* generalisation, *not* a synonym. Mixing them up is a guaranteed **1-mark cut**.\n> 5. **Hand-waving the mutual-exclusion proof** without invoking the `turn` contradiction — at least **2 marks** are reserved for a rigorous contradiction argument.\n\n---\n\n### 5.4 Topic Recap & Important Things to Remember\n\n> [!TIP]\n> **Rapid-Revision Checklist (must know before entering the exam hall):**\n\n- 🔑 Peterson's Protocol is a **software-only** solution for the **2-process** Critical Section Problem.\n- 🔑 The shared state consists of `flag[0]`, `flag[1]`, and `turn ∈ {0,1}`.\n- 🔑 The \"polite yielding\" line is `turn := j` (where $j = 1-i$).\n- 🔑 The \"spinning\" condition is `while (flag[j] ∧ turn = j) skip;`.\n- 🔑 Three guarantees: **Mutual Exclusion**, **Progress**, **Bounded Waiting (≤ 2 bypasses)**.\n- 🔑 The proof of ME hinges on the contradiction `turn = 0 ∧ turn = 1`.\n- 🔑 The total reachable state space is **at most 392** — hence it is a model-checker's favourite.\n- 🔑 The protocol **assumes sequential consistency**; on ARM/RISC-V you need **memory fences**.\n- 🔑 The **N-process generalisation** is **Lamport's Bakery Algorithm** (1974).\n- 🔑 The **predecessor** is **Dekker's Algorithm** (1965) — more complex, asymmetric.\n- 🔑 A classic **SPIN/Promela** benchmark: `spin -a` then `gcc pan.c` then `./pan`.\n- 🔑 Global invariant: $\\bigl(\\text{pc}_i = \\text{CRIT}\\bigr) \\Rightarrow \\bigl(\\text{flag}[i] = \\text{true} \\wedge \\text{turn} \\neq i\\bigr)$.\n- 🔑 Releasing a critical section means setting $\\text{flag}[i] := \\text{false}$.\n- 🔑 **LTL property for ME**: `[] (P0@cs && P1@cs)` must **never** be satisfied.\n- 🔑 *Memory model awareness* is the bridge from classical theory to modern multicore reality.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 4-t8",
          "title": "Synchronous Bus Arbiter",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 4\n> - **Topic:** Synchronous Bus Arbiter\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition (KTU 2024 Syllabus Terminology)\n\nA **Synchronous Bus Arbiter** is a clocked finite state machine (FSM) hardware controller that resolves contention when multiple bus masters simultaneously request access to a shared communication bus. In the formal verification context, it is modeled as a deterministic Mealy/Moore machine whose state transitions are triggered by a global clock edge, and whose behavior is described using a state-transition graph amenable to model checking.\n\n> [!IMPORTANT]\n> **KTU 2024 Definition Anchor:** *A Synchronous Bus Arbiter is a finite state system in which every transition is synchronized to a single global clock. Its correctness is specified using temporal logic (CTL/LTL) over propositions such as `request`, `grant`, and `bus_busy`.*\n\n### 1.1.1 Formal Tuple Representation\n\nThe arbiter is defined as the Kripke structure (or labeled transition system):\n\n$$M = (S, S_0, T, L)$$\n\nwhere the four-tuple components have the following roles in arbiter modeling:\n\n- **S** = finite set of states (all encodings of request and grant registers)\n- **S₀** = set of initial states (post-reset configuration)\n- **T** ⊆ S × S = synchronous transition relation (one transition per clock tick)\n- **L** = labeling function mapping each state to the set of atomic propositions true in that state\n\n### 1.1.2 Module 4 Context — Why the Arbiter Appears in Verification\n\nModule 4 of the KTU 2024 syllabus on *Automated Verification* uses the synchronous bus arbiter as the canonical **case study** because it exhibits three pedagogically valuable features:\n\n1. **Small enough** to be exhaustively verified by a student.\n2. **Large enough** to expose the **state-space explosion** problem.\n3. **Rich in specification patterns**: mutual exclusion, liveness, fairness, request–grant correlation.\n\n---\n\n## 1.2 Conceptual Analogy / Intuitive Overview\n\n> [!NOTE]\n> **Plain-English Analogy: The Roundabout at a Junction**\n>\n> Imagine a one-lane circular roundabout shared by four cars (the *bus masters*) approaching from four directions. The roundabout has a traffic policeman (the *arbiter*) standing in the center. The policeman follows a strict rulebook synchronized with a green traffic light cycle (the *clock*):\n>\n> 1. Only one car may enter the roundabout at a time (**mutual exclusion**).\n> 2. A car is allowed in only if it has its indicator on (**grant ⇒ request**).\n> 3. Every waiting car must eventually be allowed in (**liveness / no starvation**).\n> 4. If two cars arrive simultaneously, the policeman uses a **priority policy** to choose.\n>\n> *Formal verification* is the act of mathematically *proving* that the policeman's rulebook never violates rules 1–3 for *any* possible arrival pattern, without physically testing every scenario.\n\nIn the RTL (Register-Transfer Level) world, the policeman is a Verilog/VHDL FSM, the cars are AMBA AHB/APB masters, the roundabout is the system bus, and the green light is the rising clock edge.\n\n---\n\n## 1.3 Physical Constants and Standard Metrics\n\n> [!TIP]\n> **Key Metrics to Memorize for KTU Board Exams**\n>\n> - **Bus cycle time** `T_bus` = **1 / f_clk** where `f_clk` is typically **100 MHz** in modern SoCs.\n> - **Arbitration latency** = number of clock cycles between a request and the corresponding grant.\n> - **Grant stability window** = minimum number of cycles a grant must remain asserted to allow a transaction to complete.\n> - **Worst-case starvation bound** = maximum number of cycles any single master may wait under a given fairness policy.\n\n| Metric | Typical Value | KTU Relevance |\n|---|---|---|\n| Number of masters **n** | 2, 4, 8 | Determines state-space size 2^(2n) |\n| Clock frequency f_clk | 50 – 500 MHz | Sets arbitration latency in ns |\n| Arbiter fairness class | Strict / Round-Robin / Weighted | Decides LTL fairness axioms |\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Operational Logic of a Synchronous Bus Arbiter — Step-by-Step\n\nThe arbiter operates as a synchronous FSM. The conceptual flow on **every rising clock edge** is:\n\n1. **Sample inputs** — latch the request vector `req[n-1:0]` from the masters.\n2. **Priority resolution** — apply the arbitration policy (fixed, rotating, weighted, etc.) to compute the unique winning master.\n3. **Grant assertion** — drive the one-hot grant vector `gnt[n-1:0]` such that exactly one bit is `1`.\n4. **Hold for transaction** — keep the grant stable for at least one full bus cycle.\n5. **De-assertion** — drop the grant when the master lowers its request or releases the bus.\n\n### 2.1.1 The \"Why\" Behind Each Step\n\n- **Why sample synchronously?** Eliminates metastability, makes the transition relation deterministic, and lets us encode the design as a Kripke structure `M = (S, S₀, T, L)` with `T ⊆ S × S` (no interleaving, no non-determinism beyond input choice).\n- **Why one-hot grants?** Prevents bus contention at the electrical level and gives the model checker a *clean linear-time invariant* `Σ gnt[i] ≤ 1`.\n- **Why hold the grant?** Prevents a master from being starved mid-transaction and gives the model checker a bounded liveness property.\n\n---\n\n## 2.2 Arbitration Policies — Taxonomy\n\n| Policy | Decision Rule | Verifier Concern |\n|---|---|---|\n| **Fixed Priority** | Lower index ⇒ higher priority | Starvation of low-priority masters |\n| **Round-Robin (Rotating)** | Last-served master becomes lowest priority | Fairness proof via auxiliary counter |\n| **Weighted Fair Queueing** | Bandwidth allocation by weight | Numerical weight bounds |\n| **TDMA (Time-Division)** | Slot `t mod n` gets the bus | Real-time deadline proofs |\n| **Lottery** | Random winning ticket | Probabilistic LTL (outside KTU scope) |\n\n> [!IMPORTANT]\n> **KTU 2024 Module-4 Focus:** The syllabus emphasises *Fixed Priority* and *Round-Robin* arbiters because they illustrate **safety vs. liveness** trade-offs most clearly.\n\n---\n\n## 2.3 Formal Specification — Temporal-Logic Properties\n\nThe arbiter is specified using three families of temporal formulas:\n\n### 2.3.1 Safety Properties (must NEVER be violated)\n\n**Mutual Exclusion** — at most one grant at any instant:\n\n$$AG\\,\\bigwedge_{i \\neq j}\\; \\neg(g_i \\wedge g_j)$$\n\n**Grant Implies Request** — a master is granted only if it is requesting:\n\n$$AG\\,\\bigwedge_{i=0}^{n-1}\\;(g_i \\Rightarrow r_i)$$\n\n**No Spurious Grants** — at least one grant whenever there is at least one request:\n\n$$AG\\,\\Big(\\bigvee_{i=0}^{n-1} r_i\\Big) \\Rightarrow \\Big(\\sum_{i=0}^{n-1} g_i \\geq 1\\Big)$$\n\n### 2.3.2 Liveness Properties (must EVENTUALLY be satisfied)\n\n**No Starvation** — every requesting master is eventually granted:\n\n$$AG\\,\\bigwedge_{i=0}^{n-1}\\;(r_i \\Rightarrow AF\\,g_i)$$\n\n**Response** — every request is followed by a grant within a bounded number of cycles `K`:\n\n$$AG\\,\\bigwedge_{i=0}^{n-1}\\;(r_i \\Rightarrow AX^{K}\\,g_i)$$\n\n### 2.3.3 Fairness Properties (Buchi acceptance conditions for the model checker)\n\n$$GF\\,r_i \\;\\Rightarrow\\; GF\\,g_i \\quad \\text{(compassion for master } i\\text{)}$$\n\n$$GF\\,\\bigvee_{i=0}^{n-1} g_i \\quad \\text{(overall progress)}$$\n\n---\n\n## 2.4 KTU High-Yield Formula Sheet\n\n> [!TIP]\n> **Master this table — every formula below has appeared, or is a direct variant of, a past KTU question.**\n\n| # | Concept | Formula / Expression | Notes |\n|---|---|---|---|\n| 1 | State-space size for n-master arbiter | $\\vert S \\vert = 2^{2n}$ | n request bits + n grant bits |\n| 2 | State-space size with priority register of width p | $\\vert S \\vert = 2^{2n + p}$ | Used in round-robin |\n| 3 | Number of reachable states (safety-only) | $\\vert S_{reach} \\vert \\leq 2^{2n}$ | Often < 2^{2n} due to constraints |\n| 4 | Synchronous transition relation | $T(s, s') = 1 \\iff s' = \\delta(s, I)$ | $\\delta$ is the RTL next-state function |\n| 5 | Mutual-exclusion CTL formula | $AG\\,\\bigwedge_{i \\neq j}\\neg(g_i \\wedge g_j)$ | Safety |\n| 6 | No-starvation CTL formula | $AG\\bigwedge_{i}(r_i \\Rightarrow AF\\,g_i)$ | Liveness |\n| 7 | Bounded response bound K | $K \\leq n$ for round-robin | Worst-case waiting |\n| 8 | Starvation bound under fixed priority | unbounded for lowest-priority master | Hence the policy is *unfair* |\n| 9 | State reduction via cone-of-influence | $\\vert S_{COI} \\vert \\ll \\vert S \\vert$ | Verification optimization |\n| 10 | BDD node count for arbiter | $O(n \\cdot 2^n)$ typical | Bryant-style symbolic encoding |\n\n> [!WARNING]\n> **Notation Alert:** Use `\\vert` (NOT the pipe `|`) inside any markdown table cell to denote cardinality or absolute value. Example: `$\\vert S \\vert = 2^{2n}$`. Forgetting this breaks table rendering in the board PDF.\n\n---\n\n## 2.5 Real-World Engineering Utility\n\n| Domain | Why the Arbiter Matters |\n|---|---|\n| **SoC Design (AMBA, Wishbone, TileLink)** | Multi-core CPUs + DMA + GPU all share the main bus; an unverified arbiter = system deadlock. |\n| **Network-on-Chip (NoC)** | Crossbar switches use the same arbitration primitives, scaled up. |\n| **Automotive (ISO 26262)** | Bus contention in brake-by-wire must be provably excluded → formal verification is mandated. |\n| **Aerospace (DO-254)** | Avionics buses (MIL-STD-1553) require DO-254 DAL-A evidence → model checking the arbiter FSM. |\n| **FPGA Tooling (Xilinx, Intel)** | Vendor IP blocks ship with pre-verified arbiters using Cadence JasperGold / Synopsys Formality. |\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: State-Space Size of an n-Master Synchronous Arbiter\n\nWe derive the size of the reachable state space because this single number drives **every** verification algorithm in Module 4 (explicit state, symbolic BDD, SAT, IC3).\n\n### 3.1.1 Setup\n\nLet there be `n` bus masters, each producing a request bit `r_i` and receiving a grant bit `g_i`. The arbiter is synchronous, so the next state is a function of the current state and the input vector.\n\n$$\nr = (r_0, r_1, \\dots, r_{n-1}), \\quad g = (g_0, g_1, \\dots, g_{n-1})\n$$\n\n### 3.1.2 Counting the Combinatorial State\n\nThe naïve state vector concatenates all `2n` Boolean signals:\n\n$$\ns = (r_0, r_1, \\dots, r_{n-1},\\; g_0, g_1, \\dots, g_{n-1}) \\in \\mathbb{B}^{2n}\n$$\n\nThe number of possible Boolean vectors of length `2n` is, by the multiplicative principle:\n\n$$\n\\vert S \\vert \\;=\\; 2^{2n}\n$$\n\n### 3.1.3 Add a Round-Robin Counter\n\nFor a *round-robin* arbiter we additionally need a pointer `p` of width `p_bits = \\lceil \\log_2 n \\rceil` indicating which master will be served next if it is requesting. The new state vector becomes:\n\n$$\ns_{rr} = (r_0, \\dots, r_{n-1},\\; g_0, \\dots, g_{n-1},\\; p_0, \\dots, p_{p_{bits}-1})\n$$\n\nThe total bit-width is `2n + p_bits`, so the state-space size grows to:\n\n$$\n\\vert S_{rr} \\vert \\;=\\; 2^{2n + \\lceil \\log_2 n \\rceil}\n$$\n\n### 3.1.4 Asymptotic Behaviour\n\nFor large `n`:\n\n$$\n\\vert S_{rr} \\vert \\;\\sim\\; 2^{2n} \\cdot n\n$$\n\nThis is the seed of the **state-explosion problem** that motivates symbolic model checking and abstraction in Module 4.\n\n### 3.1.5 Numerical Illustration\n\nPlug in `n = 4` (a very common KTU exam value):\n\n$$\n\\vert S \\vert = 2^{2 \\cdot 4} = 2^{8} = 256 \\text{ states}\n$$\n\nRound-robin version with `p_bits = 2`:\n\n$$\n\\vert S_{rr} \\vert = 2^{8 + 2} = 2^{10} = 1024 \\text{ states}\n$$\n\n---\n\n## 3.2 Derivation: Verifying Mutual Exclusion by Induction on Kripke Paths\n\nWe prove that the formula\n\n$$\n\\varphi \\;=\\; AG\\,\\bigwedge_{i \\neq j}\\;\\neg(g_i \\wedge g_j)\n$$\n\nholds in the Kripke structure `M = (S, S_0, T, L)`.\n\n### 3.2.1 Step 1 — Encode `g_i` as a Boolean Variable\n\nFor a fixed-priority arbiter, the grant logic is:\n\n$$\ng_i \\;=\\; r_i \\,\\wedge\\, \\bigwedge_{j < i} \\neg r_j\n$$\n\nThis says: master `i` is granted *iff* it is requesting *and* no higher-priority master is requesting.\n\n### 3.2.2 Step 2 — Show that `g_i ∧ g_j` is Unsatisfiable for `i ≠ j`\n\nAssume for contradiction that `g_i = 1` and `g_j = 1` with `i < j`. By the encoding:\n\n$$\ng_i = 1 \\;\\Rightarrow\\; \\bigwedge_{k < i} \\neg r_k \\;\\wedge\\; r_i = 1\n$$\n\nIn particular this forces `r_i = 1`. Now check `g_j = 1`:\n\n$$\ng_j = 1 \\;\\Rightarrow\\; \\bigwedge_{k < j} \\neg r_k\n$$\n\nBut since `i < j`, the term `k = i` appears in the conjunction, demanding `¬r_i`. This contradicts `r_i = 1`. Hence `g_i ∧ g_j` is unsatisfiable, and `M ⊨ φ`. ∎\n\n### 3.2.3 Step 3 — Generalized Safety Proof (Model-Checker View)\n\nThe CTL model checker computes the set of states satisfying `φ` by the standard fixed-point:\n\n$$\nSat(AX\\,p) = \\{ s \\in S \\mid \\forall s'\\; T(s,s') \\Rightarrow s' \\in Sat(p) \\}\n$$\n\nStarting from the atomic set `Sat(g_i)`, the algorithm iteratively shrinks the satisfying set, terminating in a finite number of iterations because `S` is finite.\n\n---\n\n## 3.3 Derivation: Worst-Case Starvation Bound for Round-Robin\n\nWe compute the maximum number of clock cycles a master may wait.\n\n### 3.3.1 Step 1 — Worst-Case Scenario\n\nAll `n` masters are continuously requesting. The round-robin pointer advances by one position per cycle. A master that just lost arbitration may be overtaken by the `n − 1` other masters.\n\n### 3.3.2 Step 2 — Count the Beats\n\nThe pointer must visit all `n` positions before returning. So the worst-case waiting time is `n − 1` cycles between successive grants to the same master:\n\n$$\nK_{rr} \\;=\\; n - 1\n$$\n\n### 3.3.3 Step 3 — Express as a Bounded-Response LTL Formula\n\n$$\nAG\\;\\bigwedge_{i=0}^{n-1}\\;\\big(r_i \\Rightarrow AX^{n-1}\\,g_i \\vee AX^{n-2}\\,g_i \\vee \\cdots \\vee g_i\\big)\n$$\n\nThis is the formula a symbolic model checker consumes directly.\n\n---\n\n## 3.4 Analytical Matrix: Verification Algorithm vs. Arbiter Property\n\n| Property Class | Best Verification Algorithm | Why | State Cost |\n|---|---|---|---|\n| Mutual exclusion (safety) | **BDD-based symbolic** | Pure boolean encoding, no fairness needed | `O(2^n)` BDD nodes |\n| Grant ⇒ request (safety) | **IC3 / k-induction** | Inductive invariant is trivially `g_i ⇒ r_i` | Polynomial |\n| Bounded response (K ≤ n) | **BMC (SAT-based)** | Unroll K cycles, check `¬φ` unsatisfiable | Exponential in K |\n| Unbounded liveness (AF g_i) | **Symbolic with fairness** | Needs Buchi emptiness check | `O(2^n · n)` |\n| Probabilistic response | **PCTL model checking** | Not in KTU scope | Higher |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n> [!NOTE]\n> All diagrams use **purely alphanumeric Mermaid node IDs** prefixed with letters (e.g., `n1`, `sA`) and contain **no markdown formatting inside labels**, in strict compliance with the KTU-PREMIER-ENGINE compilation safeguards.\n\n---\n\n## 4.1 Figure 1 — Top-Level Architecture of a Synchronous Bus Arbiter\n\n```mermaid\ngraph LR\n    n1[MASTER 0] -- REQ 0 --> arbA\n    n2[MASTER 1] -- REQ 1 --> arbA\n    n3[MASTER N-1] -- REQ N-1 --> arbA\n    clkA[CLOCK SOURCE] --> arbA\n    rstA[RESET LINE] --> arbA\n    arbA[ARBITER FSM MODULE] -- GNT 0 --> n1\n    arbA -- GNT 1 --> n2\n    arbA -- GNT N-1 --> n3\n    arbA -- BUSY SIGNAL --> busA\n    busA[SHARED SYSTEM BUS]\n```\n\n---\n\n## 4.2 Figure 2 — Synchronous FSM State Machine for a 2-Master Fixed-Priority Arbiter\n\n```mermaid\nstateDiagram-v2\n    [*] --> sIdle\n    sIdle --> sGrant0 : REQ0 = 1 / GNT0 = 1\n    sIdle --> sGrant1 : REQ0 = 0 AND REQ1 = 1 / GNT1 = 1\n    sGrant0 --> sGrant0 : REQ0 = 1 / GNT0 = 1\n    sGrant0 --> sGrant1 : REQ0 = 0 AND REQ1 = 1 / GNT1 = 1\n    sGrant0 --> sIdle : REQ0 = 0 AND REQ1 = 0 / GNT = NONE\n    sGrant1 --> sGrant1 : REQ1 = 1 AND REQ0 = 0 / GNT1 = 1\n    sGrant1 --> sGrant0 : REQ0 = 1 / GNT0 = 1\n    sGrant1 --> sIdle : REQ0 = 0 AND REQ1 = 0 / GNT = NONE\n```\n\n---\n\n## 4.3 Figure 3 — Block-Level Functional Architecture of the Verification Flow\n\n```mermaid\ngraph TD\n    rtA[RTL DESIGN VERILOG] --> synA\n    specA[SPECIFICATION CTL LTL] --> synA\n    synA[SYMBOLIC MODEL CHECKER] --> bddA\n    bddA[BDD REACHABLE STATE SET] --> chkA\n    chkA[PROPERTY CHECKER] -- PASS --> okA\n    chkA -- FAIL --> cexA\n    cexA[COUNTER EXAMPLE TRACE] --> dbgA\n    dbgA[DESIGNER DEBUG]\n    okA[VERIFIED DESIGN]\n```\n\n---\n\n## 4.4 Figure 4 — Timeline of Grant Assertion for a 4-Master Round-Robin Arbiter\n\n```mermaid\ngantt\n    title  Round Robin Grant Sequence (4 Masters)\n    dateFormat  X\n    axisFormat  %s\n    section Cycle 0\n    M0 GRANT  :a1, 0, 1\n    section Cycle 1\n    M1 GRANT  :a2, 1, 1\n    section Cycle 2\n    M2 GRANT  :a3, 2, 1\n    section Cycle 3\n    M3 GRANT  :a4, 3, 1\n    section Cycle 4\n    M0 GRANT  :a5, 4, 1\n```\n\n---\n\n## 4.5 Figure 5 — Block Diagram of the Verification Algorithm Stack\n\n```mermaid\ngraph TB\n    pA[PROPERTY: AG MUTEX] --> algA\n    pB[PROPERTY: AG G IMPLIES R] --> algA\n    pC[PROPERTY: AG R IMPLIES AF G] --> algA\n    algA[TEMPORAL LOGIC PARSER] --> fixA\n    fixA[FIXED POINT ENGINE] --> bddB\n    bddB[BDD REPRESENTATION OF STATES] --> emA\n    emA[EMPTINESS CHECK FOR LIVENESS] --> resA\n    resA[VERDICT]\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall Callout**\n> Most students lose 2 – 3 marks on arbiter questions by:\n> (a) confusing **AG** (invariant in *all* paths) with **AF** (eventually in *all* paths);\n> (b) forgetting to state the *fairness assumption* required for liveness proofs;\n> (c) writing `\\vert S \\vert` with a `|` pipe character that breaks the answer PDF's markdown table.\n> Read the formula sheet in §2.4 the night before the exam.\n\n---\n\n## 5.1 Part A — 3-Mark Short Answer Questions\n\n### Q1. Define a synchronous bus arbiter. Why is it called \"synchronous\"?\n\n> **[KTU University Exam – Model Question, Module 4, Part A]**\n\n**Model Answer (3 marks):**\n\nA synchronous bus arbiter is a clocked finite state machine that resolves simultaneous bus-access requests from multiple masters and asserts exactly one grant signal per clock cycle. It is called *synchronous* because every state transition is triggered by the edge of a single global clock, making the next-state function deterministic with respect to a discrete time index.\n\n**Mark-split:** Definition (1) + role (1) + reason for \"synchronous\" (1).\n\n---\n\n### Q2. List three safety properties and one liveness property that must be satisfied by a synchronous bus arbiter.\n\n> **[KTU University Exam – Model Question, Module 4, Part A]**\n\n**Model Answer (3 marks):**\n\nSafety properties (must *never* be violated):\n\n1. **Mutual exclusion** — at most one grant is asserted in any state: `AG ∧_{i≠j} ¬(g_i ∧ g_j)`.\n2. **Grant implies request** — `AG ∧_i (g_i ⇒ r_i)`.\n3. **No spurious grants** — exactly one grant is asserted whenever any request is active: `AG (∨_i r_i) ⇒ (Σ g_i = 1)`.\n\nLiveness property (must *eventually* be satisfied): **No starvation** — every requesting master is eventually granted: `AG ∧_i (r_i ⇒ AF g_i)`.\n\n**Mark-split:** Three safety items (½ each = 1½) + one liveness (1) + correct CTL operator usage (½).\n\n---\n\n## 5.2 Part B — 14-Mark Questions (Internal Choice)\n\n> **KTU 2024 Scheme Note:** Each Part-B question has two sub-parts of 7 marks each. Choose **ONE** of the two full questions below.\n\n---\n\n### Question A (14 Marks) — Design + Verification\n\n> **[KTU University Exam – Dec 2023, Adapted]**\n\n**(a)** Design a synchronous bus arbiter for `n = 4` masters using a **fixed-priority** policy. Draw the FSM and give the Boolean equations for each `g_i`. (7 marks)\n\n**(b)** Express the property \"no two masters are ever granted simultaneously\" as a CTL formula and prove it holds for your design. (7 marks)\n\n#### Model Solution for Q-A(a) — 7 marks\n\n**Step 1 — State the inputs and outputs (1 mark)**\n\nInputs: request vector `r = (r_0, r_1, r_2, r_3)`, clock `clk`, reset `rst`.\nOutputs: one-hot grant vector `g = (g_0, g_1, g_2, g_3)`.\n\n**Step 2 — Write the priority equations (3 marks)**\n\nFor fixed priority, master `0` is highest priority and master `3` is lowest:\n\n$$\ng_0 = r_0\n$$\n\n$$\ng_1 = \\neg r_0 \\,\\wedge\\, r_1\n$$\n\n$$\ng_2 = \\neg r_0 \\,\\wedge\\, \\neg r_1 \\,\\wedge\\, r_2\n$$\n\n$$\ng_3 = \\neg r_0 \\,\\wedge\\, \\neg r_1 \\,\\wedge\\, \\neg r_2 \\,\\wedge\\, r_3\n$$\n\n**Step 3 — Draw the FSM (2 marks)** — show three states `IDLE`, `G_MASTER_i`, transitions guarded by the request bits. (See Figure 2 above for an analogous 2-master FSM.)\n\n**Step 4 — Encoding (1 mark)** — explain that the FSM is one-hot for grants and that all transitions are synchronous to the rising edge of `clk`.\n\n#### Model Solution for Q-A(b) — 7 marks\n\n**Step 1 — Write the CTL property (2 marks)**\n\n$$\n\\varphi_{mutex} \\;=\\; AG\\,\\bigwedge_{0 \\le i < j \\le 3}\\; \\neg(g_i \\,\\wedge\\, g_j)\n$$\n\n**Step 2 — Pick a representative pair (1 mark)** — We prove for the pair `(i, j) = (0, 3)`. The remaining five pairs are analogous.\n\n**Step 3 — Show unsatisfiability of `g_0 ∧ g_3` (3 marks)**\n\nAssume `g_0 = 1` and `g_3 = 1`. From the priority equations:\n\n$$\ng_0 = 1 \\;\\Rightarrow\\; r_0 = 1\n$$\n\n$$\ng_3 = 1 \\;\\Rightarrow\\; \\neg r_0 \\wedge \\neg r_1 \\wedge \\neg r_2 \\wedge r_3\n$$\n\nIn particular, `g_3 = 1` forces `¬r_0`. This contradicts `r_0 = 1`. Hence `g_0 ∧ g_3` is unsatisfiable.\n\n**Step 4 — Conclude the CTL satisfaction (1 mark)**\n\nSince the conjunction of all six pairwise exclusions is satisfied, the Kripke structure `M ⊨ φ_mutex`. ∎\n\n---\n\n### Question B (14 Marks) — Liveness + State-Space Analysis\n\n> **[KTU University Exam – Jun 2024, Adapted]**\n\n**(a)** For a 4-master *round-robin* arbiter, derive the **worst-case starvation bound** in clock cycles and express it as a bounded-response LTL formula. (7 marks)\n\n**(b)** Calculate the **state-space size** for `n = 8` masters under (i) fixed priority, and (ii) round-robin. Comment on the verification implications. (7 marks)\n\n#### Model Solution for Q-B(a) — 7 marks\n\n**Step 1 — Define round-robin (1 mark)**\n\nA round-robin arbiter maintains a `p_bits = ⌈log_2 n⌉`-bit pointer `p` to the next master to be served. On every clock cycle, if the master at `p` is requesting it is granted; otherwise the pointer advances.\n\n**Step 2 — Worst-case waiting scenario (2 marks)**\n\nThe worst case occurs when **all** `n` masters are continuously requesting. The pointer must visit all `n` positions before returning to the same master.\n\n**Step 3 — Compute the bound (2 marks)**\n\n$$\nK_{rr} \\;=\\; n - 1\n$$\n\nFor `n = 4`:\n\n$$\nK_{rr} \\;=\\; 3 \\text{ clock cycles}\n$$\n\n**Step 4 — Express as a bounded-response LTL formula (2 marks)**\n\n$$\nAG\\;\\bigwedge_{i=0}^{3}\\;\\big(r_i \\;\\Rightarrow\\; g_i \\;\\vee\\; X\\,g_i \\;\\vee\\; X^2\\,g_i \\;\\vee\\; X^3\\,g_i\\big)\n$$\n\nThis says: if `r_i` is high, then `g_i` must be high within the next 0, 1, 2, or 3 cycles.\n\n#### Model Solution for Q-B(b) — 7 marks\n\n**Step 1 — Fixed-priority case (2 marks)**\n\nFor `n = 8`, the state vector is `(r_0,…,r_7, g_0,…,g_7)`, giving 16 bits:\n\n$$\n\\vert S_{fp} \\vert \\;=\\; 2^{2 \\cdot 8} \\;=\\; 2^{16} \\;=\\; 65{,}536 \\text{ states}\n$$\n\n**Step 2 — Round-robin case (2 marks)**\n\nAdd a 3-bit pointer `p`:\n\n$$\n\\vert S_{rr} \\vert \\;=\\; 2^{2 \\cdot 8 + 3} \\;=\\; 2^{19} \\;=\\; 524{,}288 \\text{ states}\n$$\n\n**Step 3 — Verification implications (3 marks)**\n\n- Explicit-state model checking: feasible for `2^{16}` (sub-second) but borderline for `2^{19}`.\n- Symbolic (BDD) model checking: routine for both, as the arbiter's regularity yields small BDDs (`O(n·2^n)`).\n- The round-robin pointer multiplies the state space by `n`, illustrating the classic **state-space explosion** that motivates the *cone-of-influence reduction* and *abstraction* techniques covered later in Module 4.\n- For `n = 32` masters, the state space reaches `2^{67}` — beyond any explicit tool, but still tractable for BDD-based engines.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!TIP]\n> **Rapid-Revision Checklist (memorize before walking into the exam hall)**\n>\n> - **Core tuple:** Synchronous bus arbiter is a Kripke structure `M = (S, S₀, T, L)` with `T ⊆ S × S` deterministic on the clock edge.\n> - **State-space size:** `2^{2n}` for fixed-priority, `2^{2n + ⌈log_2 n⌉}` for round-robin.\n> - **Three canonical safety formulas:** mutual exclusion, grant ⇒ request, no spurious grants.\n> - **One canonical liveness formula:** no starvation, `AG (r_i ⇒ AF g_i)`, which **requires** a fairness assumption.\n> - **CTL operators to distinguish:** `AG` = invariant, `AF` = eventual, `AX` = next, `EX` = exists-next.\n> - **Verification algorithm choice:** BDD-symbolic for safety, BMC for bounded response, Buchi emptiness for unbounded liveness.\n> - **Round-robin starvation bound:** `K_rr = n − 1` cycles.\n> - **Fixed-priority pitfall:** the lowest-priority master has an **unbounded** starvation bound → use only when unfairness is acceptable.\n> - **Notation hygiene:** write `\\vert S \\vert`, never `|S|`, inside markdown tables.\n> - **Real-world link:** AMBA AHB arbiter, PCIe transaction layer, NoC crossbar — same FSM, scaled up.\n> - **Engineering impact:** A missed grant-implies-request bug is the most common cause of bus contention in tape-out revisions.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 4-t9",
          "title": "Bounded Bakery Protocol",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 4\n> - **Topic:** Bounded Bakery Protocol\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition\n\nThe **Bounded Bakery Protocol** is a **mutual exclusion algorithm** designed for concurrent processes that share a common resource. It is a **bounded variant** of Lamport's original Bakery Algorithm, in which the integer \"ticket numbers\" assigned to processes are restricted to a finite range of values (typically $\\lvert P \\rvert$ or $2\\lvert P \\rvert$ where $\\lvert P \\rvert$ is the number of processes), making it implementable on real hardware registers which have finite width.\n\n> [!NOTE]\n> **KTU Syllabus Definition:** \"The Bounded Bakery Protocol is a *correct, starvation-free* mutual exclusion algorithm for $N$ processes that uses only *atomic read/write registers* and ensures that *no two processes execute their critical sections simultaneously*.\"\n\n## 1.2 The Bakery Analogy (Intuitive Overview)\n\nImagine a small bakery with a single counter:\n\n1. Every customer who walks in takes a **paper ticket** from a dispenser that prints the next serial number (1, 2, 3, ...).\n2. A customer is served only when their ticket number is the **smallest among all waiting customers**.\n3. If two customers somehow hold the same number (a glitch in the dispenser), the **one with the smaller name/ID is served first**.\n4. After being served, the customer **discards** their ticket (so others waiting are not blocked forever).\n\nIn computing terms, each *process* $P_i$ behaves like a customer:\n- The \"ticket dispenser\" is the act of computing `number[i] := max(all numbers) + 1`.\n- The \"queue\" is implicit, encoded in the global array `number[0..N-1]`.\n- The \"same-number tie-breaker\" is the *lexicographic order* on the pair `(number, id)`.\n\n> [!IMPORTANT]\n> The *bounded* aspect is crucial: real registers cannot store arbitrarily large integers. The bounded protocol wraps the ticket counter around a fixed modulus $M$ (usually $M = N$ or $M = 2N$), yet still guarantees the same safety and liveness properties as the unbounded version.\n\n## 1.3 Why \"Bounded\"?\n\n| Property | Unbounded Bakery | Bounded Bakery |\n|----------|------------------|----------------|\n| Range of `number[i]` | $\\mathbb{N} \\cup \\{0\\}$ (unbounded) | $\\{0, 1, \\dots, M-1\\}$ (finite) |\n| Hardware realizable | ❌ No (requires $\\infty$ bits) | ✅ Yes (requires $\\lceil \\log_2 M \\rceil$ bits) |\n| Mutual Exclusion | ✅ Yes | ✅ Yes |\n| Starvation-Freedom | ✅ Yes | ✅ Yes (in fair versions) |\n| Verification Model | Infinite state | Finite state (model-checkable) |\n\n> [!TIP]\n> The bounded version is the one that is amenable to **automated verification** using tools like **SPIN**, **TLA+**, **nuXmv**, and **UPPAAL** because the state space becomes finite and bounded.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 State Variables\n\nFor a system of $N$ processes $P_0, P_1, \\dots, P_{N-1}$, the Bounded Bakery Protocol maintains the following shared (global) variables:\n\n$$\n\\begin{aligned}\nchoosing[0 \\dots N-1] &: \\text{array of Boolean} \\\\\nnumber[0 \\dots N-1] &: \\text{array of } \\mathbb{Z}_{M}\n\\end{aligned}\n$$\n\nwhere $M$ is the bound (commonly chosen as $M = N$ or $M = 2N$), and $\\mathbb{Z}_M = \\{0, 1, 2, \\dots, M-1\\}$ denotes the integers modulo $M$.\n\n**Semantics:**\n- `choosing[i] = true` ⟺ process $P_i$ is *currently computing* its ticket number.\n- `number[i] = 0` ⟺ process $P_i$ is **not** interested in the critical section.\n- `number[i] = k > 0` ⟺ process $P_i$ holds ticket number $k$ and is waiting.\n\nEach process $P_i$ also has a local/program counter $pc_i \\in \\lbrace idle, choosing, waiting, cs, exit \\rbrace$.\n\n## 2.2 The Algorithm (Process $P_i$)\n\nThe protocol is structured as a perpetual loop with five well-defined phases:\n\n**Step 1 — Non-critical section (`idle`):**\n   The process performs its independent work.\n\n**Step 2 — Doorway (`choosing`):**\n   Atomically signal the intent and pick a ticket:\n$$\n\\begin{aligned}\nchoosing[i] &:= \\text{true} \\\\\nnumber[i]   &:= \\bigl(\\max_{j} number[j] + 1\\bigr) \\bmod M \\\\\nchoosing[i] &:= \\text{false}\n\\end{aligned}\n$$\n\n**Step 3 — Waiting (`waiting`):**\n   For every other process $P_j$, the protocol enforces a two-stage wait:\n$$\n\\begin{aligned}\n\\text{await } &choosing[j] = \\text{false} \\\\\n\\text{await } &\\bigl(number[j] = 0\\bigr) \\;\\lor\\; \\bigl((number[i],\\, i) \\prec (number[j],\\, j)\\bigr)\n\\end{aligned}\n$$\n\n**Step 4 — Critical section (`cs`):**\n   The process executes the protected code.\n\n**Step 5 — Exit (`exit`):**\n$$\nnumber[i] := 0\n$$\nThe process returns to the non-critical section.\n\n## 2.3 The Lexicographic Order (\"Tickets and IDs\")\n\nThe core of the algorithm is the **lexicographic comparison operator** $\\prec$:\n\n$$\n(a,\\, i) \\prec (b,\\, j) \\;\\equiv\\; \\bigl(a < b\\bigr) \\;\\lor\\; \\bigl(a = b \\;\\land\\; i < j\\bigr)\n$$\n\nThis forms a **total order** over the set $\\mathbb{Z}_M \\times \\lbrace 0, 1, \\dots, N-1 \\rbrace$ and resolves any ties in ticket numbers by the unique process ID.\n\n> [!NOTE]\n> The lexicographic pair is sometimes denoted in literature as $(ticket, id)$. Process $P_i$ enters the critical section before $P_j$ **iff** $(number[i], i) \\prec (number[j], j)$ **and** $number[i] \\neq 0$.\n\n## 2.4 KTU Formula Sheet / Cheat Sheet\n\n| Symbol / Concept | Definition | Notes |\n|------------------|------------|-------|\n| $N$ | Number of processes | $\\|P\\|$ |\n| $M$ | Bound on ticket numbers | Usually $M = 2N$ |\n| $\\mathbb{Z}_M$ | Set $\\{0, 1, \\dots, M-1\\}$ | Modulo $M$ arithmetic |\n| $choosing[i]$ | Boolean flag for $P_i$ doorway | Single bit per process |\n| $number[i]$ | Current ticket of $P_i$ | Value in $\\mathbb{Z}_M$ |\n| $(a,i) \\prec (b,j)$ | $a < b$ **or** $(a = b$ and $i < j)$ | Lexicographic order |\n| Ticket assignment | $number[i] := (\\max_j number[j] + 1) \\bmod M$ | Wraps around |\n| Wait condition | $number[j] = 0$ **or** $(number[i],i) \\prec (number[j],j)$ | Both must hold for all $j \\neq i$ |\n| Exit action | $number[i] := 0$ | Releases the ticket |\n| State space size | $(2 \\times M)^{N} \\times (\\text{pc states})^{N}$ | Finite → model-checkable |\n\n> [!IMPORTANT]\n> **Real-world utility:** The Bounded Bakery Protocol is used as a *benchmark algorithm* in formal verification because it requires reasoning about *atomicity*, *fairness*, *lexicographic ordering*, and *wrap-around arithmetic* — all in one compact algorithm. It is studied in operating systems (software mutual exclusion), distributed systems, and hardware arbiter design.\n\n## 2.5 Correctness Properties\n\n1. **Mutual Exclusion (Safety):** At any time, at most one process is in the critical section.\n2. **Starvation-Freedom (Liveness, weak):** If a process $P_i$ is in the waiting state, it *eventually* enters the critical section under weak fairness.\n3. **First-Come-First-Served (FCFS):** If $P_i$ finishes its doorway *before* $P_j$ starts its doorway, then $P_i$ enters the CS *before* $P_j$.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: Why Two Variables per Process?\n\nIn a single shared atomic register, mutual exclusion among $N$ processes would be impossible (Fischer's impossibility result at hardware level). The Bakery protocol uses **two arrays**:\n- `choosing[i]` — a **doorway barrier** that prevents reading a half-written ticket.\n- `number[i]` — the actual **priority** of $P_i$.\n\n**Logic transition:** Without `choosing[i]`, process $P_i$ might read `number[j]` *during* the moment $P_j$ is updating it. By forcing $P_i$ to wait until $choosing[j] = false$, we ensure every observed `number[j]` is in a *stable* state.\n\n## 3.2 Detailed Walkthrough: A 2-Process Scenario ($N=2$, $M=4$)\n\nLet us trace the execution of $P_0$ and $P_1$ with initial state $number = [0, 0]$, $choosing = [false, false]$.\n\n### Trace Table\n\n| Step | Action | $choosing[0]$ | $choosing[1]$ | $number[0]$ | $number[1]$ | State |\n|:----:|--------|:-------------:|:-------------:|:-----------:|:-----------:|:------|\n| 0    | Initial | false | false | 0 | 0 | Both idle |\n| 1    | $P_0$ enters doorway | **true** | false | 0 | 0 | $P_0$ choosing |\n| 2    | $P_0$ computes ticket | true | false | **1** | 0 | $P_0$ choosing |\n| 3    | $P_0$ exits doorway | false | false | 1 | 0 | $P_0$ waiting |\n| 4    | $P_0$ checks $P_1$: `choosing[1]=false` ✓; `number[1]=0` ✓ | false | false | 1 | 0 | $P_0$ enters CS |\n| 5    | $P_0$ in critical section | false | false | 1 | 0 | $P_0$ in CS |\n| 6    | $P_1$ enters doorway | false | **true** | 1 | 0 | $P_1$ choosing |\n| 7    | $P_1$ computes ticket: $\\max(0,1)+1 = 2$ | false | true | 1 | **2** | $P_1$ choosing |\n| 8    | $P_1$ exits doorway | false | false | 1 | 2 | $P_1$ waiting |\n| 9    | $P_1$ checks $P_0$: $choosing[0]=false$ ✓; $(2,1) \\prec (1,0)$? NO, so wait | false | false | 1 | 2 | $P_1$ blocks |\n| 10   | $P_0$ exits CS: $number[0] := 0$ | false | false | **0** | 2 | $P_0$ idle |\n| 11   | $P_1$ re-evaluates: $number[0]=0$ ✓ | false | false | 0 | 2 | $P_1$ enters CS |\n| 12   | $P_1$ exits CS: $number[1] := 0$ | false | false | 0 | **0** | $P_1$ idle |\n\n**Result:** Mutual exclusion held throughout, $P_0$ was served first (FCFS), and the system returned to the initial state.\n\n## 3.3 Formal Proof Sketch: Mutual Exclusion\n\n**Claim:** No two distinct processes $P_i$ and $P_j$ can be in the CS simultaneously.\n\n**Proof by contradiction:**\n\n1. Suppose both $P_i$ and $P_j$ are in the CS at the same time, with $i < j$.\n2. Since they passed their `await` checks, when $P_i$ last evaluated the condition for $P_j$, the **disjunction** held:\n$$\n\\bigl(number[j] = 0\\bigr) \\;\\lor\\; \\bigl((number[i], i) \\prec (number[j], j)\\bigr) \\tag{1}\n$$\n3. Similarly, when $P_j$ last evaluated the condition for $P_i$:\n$$\n\\bigl(number[i] = 0\\bigr) \\;\\lor\\; \\bigl((number[j], j) \\prec (number[i], i)\\bigr) \\tag{2}\n$$\n4. Since both are in the CS, $number[i] \\neq 0$ and $number[j] \\neq 0$. So the first disjuncts of (1) and (2) are false, leaving:\n$$\n(number[i], i) \\prec (number[j], j) \\quad \\text{and} \\quad (number[j], j) \\prec (number[i], i)\n$$\n5. By definition of $\\prec$, this implies both $number[i] < number[j]$ *and* $number[j] < number[i]$ — a **contradiction**. $\\blacksquare$\n\n> [!NOTE]\n> The bounded version requires an *additional invariant* on the modulo arithmetic: it must hold that the lexicographic order is well-defined under wrap-around. The classical fix is to use $M = 2N$, ensuring that the \"in-flight\" tickets never wrap more than once during a contention episode.\n\n## 3.4 State-Space Analysis for Model Checking\n\nFor model checkers like **SPIN** (Promela) or **nuXmv**, the reachable state space is bounded by:\n\n$$\n\\vert \\Sigma \\vert \\;=\\; (2)^{N} \\cdot (M)^{N} \\cdot \\bigl(\\lvert PC \\rvert\\bigr)^{N}\n$$\n\nwhere $\\lvert PC \\rvert$ is the number of program counter values (typically 5: idle, choosing, waiting, cs, exit).\n\nFor $N = 3$ processes and $M = 6$, this gives $2^3 \\cdot 6^3 \\cdot 5^3 = 8 \\cdot 216 \\cdot 125 = 216{,}000$ states — small enough for **explicit-state model checking** in under a second. This is precisely why the Bounded Bakery Protocol is the *go-to* case study in automated verification courses.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Process Lifecycle (Mermaid State Machine)\n\n```mermaid\nstateDiagram-v2\n    [*] --> idle\n    idle --> choosing : want to enter CS\n    choosing --> waiting : number[i] := (max + 1) mod M\n    waiting --> waiting : (number[i],i) > (number[j],j) for some j\n    waiting --> cs : all j satisfy wait condition\n    cs --> exit : done with CS\n    exit --> idle : number[i] := 0\n    exit --> [*] : termination\n```\n\n**Interpretation of the diagram:**\n- The state `choosing` corresponds to the **doorway** (the critical atomic assignment of the ticket).\n- The state `waiting` corresponds to the **busy-wait loop** over all $P_j$ with $j \\neq i$.\n- The transition `waiting → cs` is guarded by the predicate $\\forall j \\neq i: \\text{safe}_j$ where $\\text{safe}_j$ is the disjunction of the two wait conditions.\n- The transition `exit → idle` performs $number[i] := 0$, releasing the slot.\n\n## 4.2 Protocol Block Architecture (Mermaid Block Flow)\n\n```mermaid\nflowchart TD\n    A[\"Process Pi starts\"] --> B[\"choosing[i] := true\"]\n    B --> C[\"Compute number i = max all + 1 mod M\"]\n    C --> D[\"choosing i := false\"]\n    D --> E{\"Loop over all j not equal to i\"}\n    E --> F{\"Is choosing j true\"}\n    F -- Yes --> E\n    F -- No --> G{\"Is number j zero OR (number i, i) less than number j, j\"}\n    G -- No --> E\n    G -- Yes --> H[\"j processed\"]\n    H --> I{\"All j processed\"}\n    I -- No --> E\n    I -- Yes --> J[\"Enter Critical Section\"]\n    J --> K[\"Execute protected code\"]\n    K --> L[\"number i := 0\"]\n    L --> M[\"Return to remainder section\"]\n    M --> A\n```\n\n## 4.3 Global Variable Interactions (Mermaid Block Diagram)\n\n```mermaid\nflowchart LR\n    P0[\"Process P0\"] -- \"reads / writes\" --> CH[choosing 0 to N-1]\n    P1[\"Process P1\"] -- \"reads / writes\" --> CH\n    P2[\"Process PN-1\"] -- \"reads / writes\" --> CH\n    P0 -- \"reads / writes\" --> NUM[number 0 to N-1]\n    P1 -- \"reads / writes\" --> NUM\n    P2 -- \"reads / writes\" --> NUM\n    CH -. \"barrier semantics\" .-> P0\n    CH -. \"barrier semantics\" .-> P1\n    CH -. \"barrier semantics\" .-> P2\n```\n\n**Reading the diagram:**\n- The two arrays `choosing[]` and `number[]` are the *only* shared state.\n- All arrows are bi-directional reads/writes (atomic register operations).\n- The dotted arrows represent the **logical dependency**: a process $P_i$ must read `choosing[j]` *before* reading `number[j]`, which is the safety contract enforced by the doorway barrier.\n\n## 4.4 Verification Pipeline (How Tools Verify the Protocol)\n\n```mermaid\nflowchart LR\n    A[\"Promela / TLA+ / nuXmv model of Bounded Bakery\"] --> B[\"Model Checker SPIN / TLC / nuXmv\"]\n    B --> C{\"Property holds?\"}\n    C -- \"Yes\" --> D[\"Print Verification Successful\"]\n    C -- \"No\" --> E[\"Counterexample Trace\"]\n    E --> F[\"Debug and Refine Model\"]\n    F --> A\n```\n\n**KTU Exam Tip:** When asked \"how would you verify the Bounded Bakery Protocol?\" in an exam, draw a similar pipeline and name the properties:\n- *Safety:* $\\lnot \\exists$ state where $pc_i = cs \\land pc_j = cs$ for $i \\neq j$.\n- *Liveness:* $\\Box (pc_i = waiting \\Rightarrow \\Diamond (pc_i = cs))$ under weak fairness.\n- *Non-Blocking:* $\\Box \\exists i: pc_i = idle$ (or appropriate variant).\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### **Question 1** `[KTU University Exam - Dec 2023]`\n**Differentiate between the Bakery Protocol and the Bounded Bakery Protocol. Why is the bounded version preferred in automated verification?**\n\n**Model Answer:**\n\n| Aspect | Bakery Protocol | Bounded Bakery Protocol |\n|--------|-----------------|-------------------------|\n| Ticket range | Unbounded natural numbers | Finite set $\\{0, 1, \\dots, M-1\\}$ |\n| Hardware | Not directly realizable | Implementable on finite-width registers |\n| State space | Infinite (unbounded) | Finite (bounded) |\n| Verification | Requires induction / abstraction | Directly model-checkable |\n| Bound $M$ | N/A | Typically $2N$ or $N$ |\n\nThe bounded version is preferred in automated verification because the resulting state space is **finite**, allowing exhaustive exploration by model checkers such as SPIN or nuXmv in finite time. *(3 marks)*\n\n---\n\n### **Question 2** `[KTU University Exam - June 2024]`\n**Define the lexicographic ordering used in the Bounded Bakery Protocol and state its role in tie-breaking.**\n\n**Model Answer:**\n\nThe lexicographic ordering on pairs $(a, i) \\in \\mathbb{Z}_M \\times \\lbrace 0, \\dots, N-1 \\rbrace$ is defined as:\n\n$$\n(a,\\, i) \\prec (b,\\, j) \\;\\equiv\\; (a < b) \\;\\lor\\; (a = b \\;\\land\\; i < j)\n$$\n\n**Role:** When two processes $P_i$ and $P_j$ hold the **same ticket number** (a possible scenario in the bounded version due to wrap-around), the lexicographic order uses the *process ID* as a tie-breaker, ensuring a **total order** among waiting processes. This guarantees FCFS-like progress and prevents livelock. *(3 marks)*\n\n---\n\n## 5.2 Part B Questions (14 Marks — Module Internal Choice)\n\n> Each Part B question is split into two sub-parts worth 7 marks each. You are expected to attempt **one full question (both sub-parts)**.\n\n### **Question A (14 Marks)**\n\n#### (a) `[7 Marks]` `[KTU University Exam - Dec 2022]`\n**State and explain the Bounded Bakery Protocol for $N$ processes. Clearly list the shared variables, the doorway, the waiting condition, and the exit action. Use $M = 2N$ as the ticket bound.**\n\n**Model Answer:**\n\n**Shared variables (3 marks):**\n- `choosing[0..N-1]: array of Boolean`\n- `number[0..N-1]: array of integers in {0, 1, ..., 2N-1}`\n\n**Algorithm for process $P_i$ (4 marks):**\n\n$$\n\\begin{aligned}\n&\\textbf{forever do:} \\\\\n&\\quad \\text{// Non-critical section} \\\\\n&\\quad choosing[i] := \\text{true} \\\\\n&\\quad number[i] := \\bigl(\\max(number[0], \\dots, number[N-1]) + 1\\bigr) \\bmod 2N \\\\\n&\\quad choosing[i] := \\text{false} \\\\\n&\\quad \\textbf{for } j := 0 \\textbf{ to } N-1, \\; j \\neq i: \\\\\n&\\quad\\quad \\textbf{await } choosing[j] = \\text{false} \\\\\n&\\quad\\quad \\textbf{await } \\bigl(number[j] = 0\\bigr) \\;\\lor\\; \\bigl((number[i], i) \\prec (number[j], j)\\bigr) \\\\\n&\\quad \\text{// Critical section} \\\\\n&\\quad number[i] := 0 \\\\\n&\\quad \\text{// Remainder section}\n\\end{aligned}\n$$\n\nThe **doorway** comprises the three assignments to `choosing[i]` and `number[i]`. The **waiting condition** is the nested `await` loop over all $P_j$. The **exit** is the single assignment `number[i] := 0`. The bound $M = 2N$ ensures that the tickets of in-flight processes never wrap around more than once during a single contention cycle, preserving correctness.\n\n#### (b) `[7 Marks]` `[KTU University Exam - Dec 2022]`\n**Prove the mutual exclusion property of the Bounded Bakery Protocol. Is starvation-freedom guaranteed? Justify your answer with the help of fairness assumptions.**\n\n**Model Answer:**\n\n**Mutual Exclusion Proof (5 marks):**\nAssume for contradiction that $P_i$ and $P_j$ ($i \\neq j$) are simultaneously in the critical section. WLOG let $i < j$. Since both processes are in the CS, $number[i] \\neq 0$ and $number[j] \\neq 0$. Therefore, when $P_i$ last checked its wait condition for $P_j$, it must have been that:\n\n$$\n(number[i], i) \\prec (number[j], j) \\tag{$\\star$}\n$$\n\nSimilarly, when $P_j$ last checked its condition for $P_i$:\n\n$$\n(number[j], j) \\prec (number[i], i) \\tag{$\\star\\star$}\n$$\n\nBy the definition of $\\prec$, ($\\star$) implies $number[i] \\leq number[j]$, while ($\\star\\star$) implies $number[j] \\leq number[i]$. Hence $number[i] = number[j]$. But then $\\prec$ reduces to $i < j$ from ($\\star$) and $j < i$ from ($\\star\\star$) — a **contradiction**. $\\blacksquare$\n\n**Starvation-Freedom (2 marks):**\nUnder **weak fairness** (every continuously enabled action is eventually executed), the Bounded Bakery Protocol is *starvation-free*. The key intuition is: any process $P_j$ blocking $P_i$ from the CS either (a) has $number[j] = 0$ (it is not contending), or (b) is lexicographically larger than $P_i$. Case (b) processes must themselves progress because their tickets are bounded and they are also blocked by lexicographically-smaller tickets, leading to a well-founded ordering. Thus no process can be perpetually overtaken.\n\n---\n\n### **Question B (14 Marks)**\n\n#### (a) `[7 Marks]` `[KTU University Exam - June 2023]`\n**Explain how the Bounded Bakery Protocol is modeled in the SPIN model checker. Write the key parts of the Promela model and identify the LTL properties used to verify mutual exclusion and starvation-freedom.**\n\n**Model Answer:**\n\n**Promela skeleton (5 marks):**\n\n$$\n\\begin{aligned}\n&\\text{bool choosing[N];} \\\\\n&\\text{byte number[N];} \\\\\n&\\text{proctype } P_i(\\text{byte id}) \\lbrace \\\\\n&\\quad \\text{do} \\\\\n&\\quad\\quad :: \\text{/* non-CS */} \\rightarrow \\text{atomic} \\lbrace \\\\\n&\\quad\\quad\\quad \\text{choosing[id] = true;} \\\\\n&\\quad\\quad\\quad \\text{... compute number[id] = (max + 1) \\% (2*N) ...} \\\\\n&\\quad\\quad\\quad \\text{choosing[id] = false;} \\\\\n&\\quad\\quad \\rbrace; \\\\\n&\\quad\\quad \\text{for } (j \\text{ in } 0..N-1) \\lbrace \\\\\n&\\quad\\quad\\quad \\text{if } (j != id) \\rightarrow \\\\\n&\\quad\\quad\\quad\\quad \\text{(choosing[j] == false);} \\\\\n&\\quad\\quad\\quad\\quad \\text{((number[j] == 0) || ((number[id], id) < (number[j], j)));} \\\\\n&\\quad\\quad\\quad \\text{fi} \\\\\n&\\quad\\quad \\rbrace; \\\\\n&\\quad\\quad \\text{/* critical section */} \\\\\n&\\quad\\quad \\text{number[id] = 0;} \\\\\n&\\quad \\text{od} \\\\\n&\\rbrace\n\\end{aligned}\n$$\n\n**LTL properties (2 marks):**\n- **Mutual Exclusion:** $\\Box \\lnot (pc_i = cs \\land pc_j = cs)$ for all $i \\neq j$, encoded in SPIN as `#define mutex ((pc[0] != cs) || (pc[1] != cs))` with `ltl mutex { [] mutex }`.\n- **Starvation-Freedom:** $\\Box (pc_i = waiting \\Rightarrow \\Diamond (pc_i = cs))$, requiring **weak fairness** annotation via `fair` or `progress` labels in Promela.\n\n#### (b) `[7 Marks]` `[KTU University Exam - June 2023]`\n**Compare the Bounded Bakery Protocol with Dekker's and Peterson's mutual exclusion algorithms. Discuss the complexity, the number of shared variables, and the suitability for automated verification.**\n\n**Model Answer:**\n\n**Comparison Table (5 marks):**\n\n| Property | Dekker's | Peterson's | Bounded Bakery |\n|----------|----------|------------|----------------|\n| Number of processes | 2 only | 2 only | $N$ (arbitrary) |\n| Shared variables per process | 3 (`flag[2]`, `turn`) | 2 (`flag[2]`, `turn`) | 2 arrays of size $N$ |\n| Busy-wait | Yes | Yes | Yes |\n| Atomic primitive | Read/Write | Read/Write | Read/Write |\n| Starvation-freedom | Yes | Yes | Yes (with fairness) |\n| Bound on integer values | N/A (Boolean) | N/A (Boolean) | $M = 2N$ |\n| Model-checkable easily | Yes | Yes | Yes (standard benchmark) |\n| Real-world deployment | Rare | Common in OS textbooks | Theoretical/Verification |\n\n**Discussion (2 marks):**\nDekker's and Peterson's algorithms are restricted to **2 processes** and use only Boolean variables — they are simpler to verify but do not scale. The Bounded Bakery Protocol **scales to $N$ processes** and serves as the standard *benchmark* in model-checking literature (SPIN, TLC, nuXmv) because it combines **arithmetic reasoning** (modular wrap-around), **lexicographic ordering**, and **interleaved concurrency** in a compact, finite-state model.\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common mistakes KTU examiners penalize:**\n>\n> 1. **Forgetting the `choosing[]` array** — Many students omit the `choosing[i]` flag. This breaks the doorway barrier and invalidates mutual exclusion. **−2 marks minimum.**\n> 2. **Using `<=` instead of `<`** in the lexicographic order — A common slip. The relation is strict ($\\prec$), not weak.\n> 3. **Confusing $M = N$ with $M = 2N$** — The bound $M = 2N$ is the *safe* classical choice; $M = N$ requires additional invariants. Examiners expect you to *state the bound explicitly*.\n> 4. **Not mentioning fairness** — Starvation-freedom is **conditional on weak fairness**. Without fairness, the protocol is only *deadlock-free*, not starvation-free.\n> 5. **Skipping the modulus operator** — In the bounded version, ticket assignment is `+1 mod M`, not `+1`. A missing `mod M` is a fatal error.\n> 6. **Not defining the lexicographic order formally** — Examiners want the symbolic definition: $(a,i) \\prec (b,j) \\equiv (a < b) \\lor (a = b \\land i < j)$.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n> [!TIP]\n> **Rapid-revision checklist for the Bounded Bakery Protocol:**\n\n- [x] It is a **mutual exclusion algorithm** for $N$ concurrent processes using only **atomic read/write registers**.\n- [x] **Two shared arrays:** `choosing[0..N-1]` (Boolean) and `number[0..N-1]` (in $\\mathbb{Z}_M$).\n- [x] The **bound** is typically $M = 2N$, ensuring the ticket space is finite.\n- [x] **Doorway:** set `choosing[i] := true`, compute $number[i] := (\\max + 1) \\bmod M$, set `choosing[i] := false`.\n- [x] **Waiting condition:** for every $j \\neq i$, wait until $choosing[j] = false$ AND $(number[j] = 0$ OR $(number[i],i) \\prec (number[j],j))$.\n- [x] **Exit:** $number[i] := 0$.\n- [x] **Lexicographic order:** $(a,i) \\prec (b,j) \\iff a < b \\lor (a = b \\land i < j)$.\n- [x] **Mutual exclusion** holds for all executions (proven by contradiction).\n- [x] **Starvation-freedom** holds under **weak fairness**.\n- [x] **State space is finite** → ideal for **model checking** with SPIN / TLA+ / nuXmv.\n- [x] The bounded version is the **standard benchmark** in automated verification courses.\n- [x] **Real-world analogy:** the bakery queue with numbered tickets.\n- [x] **Key invariant:** the lexicographic order is a total order on the set of waiting tickets.\n- [x] **KTU exam mantra:** *doorway → barrier → wait → enter → release*.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 4-t10",
          "title": "Bakery Protocol",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module Module 4\n> - **Topic:** Bakery Protocol\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Academic Definition\n\nThe **Lamport's Bakery Algorithm** (commonly called the **Bakery Protocol**) is a foundational **distributed mutual exclusion algorithm** proposed by **Leslie Lamport in 1974**. It enables *N* concurrent processes running on independent processors to safely coordinate access to a shared **Critical Section (CS)** using only **atomic read and atomic write operations** on shared memory — without requiring hardware-supported Test-And-Set instructions.\n\nThe protocol guarantees three essential correctness properties demanded by KTU's distributed systems module:\n\n- **Safety (Mutual Exclusion):** At most one process executes its critical section at any given time.\n- **Liveness (Deadlock-Freedom / Starvation-Freedom):** Every process that requests entry is eventually granted it.\n- **Ordering (Quasi First-Come-First-Served):** If process $P_i$ requested entry before $P_j$ in real time, $P_i$ enters its critical section no later than $P_j$.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Highlight:** The Bakery Protocol is the canonical example used to teach *mutual exclusion in distributed/loosely-coupled systems* where message passing replaces shared memory. It is also the standard test case for **model checking** and **TLA+/Promela** verification tools in PECNT526.\n\n### 1.2 The Real-World Analogy: A Bakery Counter 🍞\n\nImagine walking into a popular bakery on a Sunday morning:\n\n1. **Take a Number:** Every customer who walks in takes a paper ticket from a dispenser. The dispenser gives the next available integer in increasing order.\n2. **Wait Your Turn:** The customer with the **smallest number** is served first. If two customers somehow hold the *same number* (rare, but possible if the dispenser glitches), the one with the **alphabetically smaller name** (acting as a tie-breaker) goes first.\n3. **Get Served:** Only one customer is at the counter at a time — this is the **Critical Section** (the bread, the coffee, the interaction with the baker).\n4. **Discard Ticket:** Once served, the customer discards their ticket (resets to zero), freeing the slot for the next wave.\n\nIn this analogy:\n\n| Bakery Element | Distributed System Element |\n|----------------|---------------------------|\n| Customer | Process $P_i$ |\n| Ticket Number | `number[i]` — process's priority |\n| Choosing flag | `choosing[i]` — \"in ticket line\" indicator |\n| Smaller number / tie-breaker | Lexicographic ordering: (number, pid) |\n| Baker's counter | Critical Section |\n\n> [!NOTE]\n> **Why \"Bakery\"?** Lamport deliberately named the algorithm after a real bakery queue because the *de-facto* fairness rule in a bakery is \"lowest ticket number wins,\" and *lexicographic ordering* handles the (rare) duplicate-number case.\n\n### 1.3 Core Standard Metrics\n\nThe following constants and variables are used throughout the KTU 2024 formulation:\n\n- **$N$**: Total number of processes (a finite, fixed positive integer)\n- **$P_0, P_1, \\ldots, P_{N-1}$**: Process identifiers (indices), forming a totally ordered set\n- **$choosing[i] \\in \\{0, 1\\}$**: Boolean flag for process $P_i$ — *\"Is $P_i$ currently picking a ticket?\"*\n- **$number[i] \\in \\mathbb{Z}_{\\geq 0}$**: Non-negative integer ticket of $P_i$, where **$0$** means *\"not contending\"*\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 The Three Operational Phases of a Process\n\nEvery process $P_i$ cycles through **three logical regions**, analogous to a state machine that model checkers (SPIN, NuSMV, TLA+) explore exhaustively:\n\n1. **Remainder / Non-Critical Section (NCS):** Process performs local computation unrelated to the shared resource. No interaction with other processes' state.\n2. **Entry Protocol (Doorway + Waiting Room):**\n   - *Doorway* — finite, bounded sequence: declare `choosing[i] = 1`, compute ticket, declare `choosing[i] = 0`.\n   - *Waiting Room* — bounded busy-wait loop: spin until every other process $P_j$ has either a ticket of $0$, or a ticket strictly greater than (or tied but with higher PID than) $P_i$'s.\n3. **Critical Section (CS):** The protected operation.\n4. **Exit Protocol:** Single atomic statement: `number[i] = 0`.\n\n### 2.2 The Lexicographic Ordering Operator (Total Ticket Order)\n\nThe heart of the algorithm is a **total order** on pairs $(number, pid)$. Define for any two tickets $a, b$ and PIDs $i, j$:\n\n$$ (a, i) \\prec (b, j) \\;\\iff\\; \\bigl(a < b\\bigr) \\;\\lor\\; \\bigl(a = b \\;\\land\\; i < j\\bigr) $$\n\n> [!NOTE]\n> This is **NOT** a partial order. It is a *strict total order* — for any two distinct processes $P_i, P_j$, exactly one of $(number[i], i) \\prec (number[j], j)$ or $(number[j], j) \\prec (number[i], i)$ holds. This guarantees the algorithm can always pick a unique \"winner.\"\n\n### 2.3 The Pseudocode (Lamport's Canonical Form)\n\n$$\n\\begin{aligned}\n\\text{Shared data:} & \\quad choosing[0 \\ldots N-1] = \\text{false} \\\\\n& \\quad number[0 \\ldots N-1] = 0 \\\\[6pt]\n\\text{Process } P_i \\text{ executes:} & \\\\[4pt]\n\\textbf{Entry:} & \\\\\n& choosing[i] \\leftarrow \\textbf{true} \\\\\n& number[i] \\leftarrow 1 + \\max\\bigl(number[0], \\ldots, number[N-1]\\bigr) \\\\\n& choosing[i] \\leftarrow \\textbf{false} \\\\\n& \\textbf{for } j = 0 \\textbf{ to } N-1 \\textbf{ do} \\\\\n& \\quad \\textbf{while } choosing[j] \\textbf{ do skip} \\\\\n& \\quad \\textbf{while } number[j] \\neq 0 \\;\\land\\; (number[j], j) \\prec (number[i], i) \\textbf{ do skip} \\\\\n\\textbf{Critical Section} & \\\\\n\\textbf{Exit:} & \\quad number[i] \\leftarrow 0\n\\end{aligned}\n$$\n\n### 2.4 KTU High-Yield Formula & Properties Cheat Sheet\n\n| Property | Mathematical Statement | Type |\n|----------|------------------------|------|\n| **Mutual Exclusion (Safety)** | $\\forall i, j: \\bigl(CS_i \\land CS_j\\bigr) \\Rightarrow i = j$ | **Safety Invariant** |\n| **No Starvation (Liveness)** | $\\forall i: \\bigl(\\text{request}(i) \\bigcirc\\!\\rightarrow CS_i\\bigr)$ — every request is *eventually* satisfied | **Liveness Property** |\n| **Ticket Strict Monotonicity** | If $P_i$ reads $P_j$'s ticket in its doorway, then $number[i] \\neq number[j]$ (with a *proof-by-induction caveat*) | **Auxiliary Lemma** |\n| **Lex Order Total** | $\\forall i \\neq j: (number[i], i) \\prec (number[j], j) \\;\\lor\\; (number[j], j) \\prec (number[i], i)$ | **Total Order** |\n| **Doorway Length** | Exactly **4 atomic statements** (read $choosing$ for all $j$, read $number$ for all $j$, max computation, two writes) | **Complexity** |\n| **Wait Complexity (worst)** | $P_i$ can be overtaken by at most $N - 1$ other processes before entering CS | **Bound** |\n| **Message Complexity** | $O(N)$ shared-memory reads per entry (NOT message-passing, but in the distributed variant: $3(N-1)$ messages) | **Cost** |\n| **Bounded Variables** | `choosing` is Boolean; `number` requires an *unbounded* integer (theoretical) but is *practically* bounded by $N$ concurrent contentions | **Memory** |\n\n> [!WARNING]\n> **KTU Pitfall:** Many students confuse the **distributed** Bakery algorithm (where tickets are exchanged via messages) with the **shared-memory** Bakery algorithm (where `number[]` is a globally visible array). PECNT526's automated verification module almost always uses the **shared-memory** form because it is the one most amenable to model checking with Promela/SPIN.\n\n### 2.5 Real-World Engineering Utility\n\n- **Formal verification benchmark:** The Bakery algorithm is the \"Hello, World!\" of concurrent algorithm verification — used in nearly every textbook on TLA+, SPIN, and Coq.\n- **Database lock managers:** Real-world distributed DBs (e.g., Google Spanner's lock manager) use a Bakery-style *logical-timestamp + replica-id* tie-breaker.\n- **Multi-core CPU arbitration:** QPI / Infinity Fabric coherence protocols use analogous priority schemes.\n- **Embedded RTOS:** FreeRTOS port layers occasionally implement Bakery-style *mutex with priority inheritance* for low-core-count systems.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Derivation of the Mutual Exclusion Invariant\n\n**Goal:** Prove that for any two processes $P_i$ and $P_j$ ($i \\neq j$), they cannot both be inside the critical section simultaneously.\n\n#### Step 1: Define the Critical Section Entry Condition\n\nProcess $P_i$ enters its critical section only when the *inner wait loop terminates*:\n\n$$ \\forall j \\neq i: \\Bigl( number[j] = 0 \\Bigr) \\;\\lor\\; \\Bigl( (number[i], i) \\prec (number[j], j) \\Bigr) $$\n\nCall this condition $\\mathbf{E}(i)$ — *\"$P_i$ is enabled to enter its CS.\"*\n\n#### Step 2: Assume the Contrapositive\n\nSuppose, for contradiction, both $P_i$ and $P_j$ are in the CS simultaneously. Then both $\\mathbf{E}(i)$ and $\\mathbf{E}(j)$ hold.\n\n#### Step 3: Apply the Total Order\n\nSince $i \\neq j$, the total order $\\prec$ on pairs gives exactly one of:\n\n$$\n\\begin{aligned}\n\\text{Case A: } & (number[i], i) \\prec (number[j], j) \\\\\n\\text{Case B: } & (number[j], j) \\prec (number[i], i)\n\\end{aligned}\n$$\n\n(They cannot be equal under the strict total order.)\n\n#### Step 4: Derive a Contradiction in Each Case\n\n- **In Case A:** $\\mathbf{E}(j)$ requires that $number[i] = 0$ **OR** $(number[j], j) \\prec (number[i], i)$. But we are in Case A, so the second disjunct is false. Therefore $number[i]$ must equal $0$. However, $P_i$ is *in its CS*, which (per the algorithm) means $number[i] \\neq 0$. **Contradiction.**\n\n- **In Case B:** Symmetric argument — $number[j]$ must be $0$, but $P_j$ is in CS, so $number[j] \\neq 0$. **Contradiction.**\n\n#### Step 5: Conclude Safety\n\nSince both cases lead to contradiction, our assumption that both $P_i$ and $P_j$ can be in CS is false. Hence:\n\n$$ \\boxed{\\;\\text{At most one process executes CS at any time.} \\;\\iff\\; \\text{Safety (Mutual Exclusion)} \\;\\checkmark\\;} $$\n\n> [!NOTE]\n> This is a textbook example of a **safety proof by contradiction** that automated tools like TLA+'s TLC model checker mechanically verify by exhaustive state-space exploration for small $N$ (typically $N = 2, 3, 4$).\n\n### 3.2 Analytical Matrix: What Each Verification Approach Catches\n\n| Verification Method | Catches | Misses | Tool Example |\n|---------------------|---------|--------|--------------|\n| **Theorem Proving** (Hoare Logic / TLA+ proof) | All $N$, all interleavings — full proof of safety & liveness | None, if axioms are correct | Isabelle/HOL, Coq, TLAPS |\n| **Model Checking** (state-space enumeration) | Bugs in finite $N$ instances; counterexamples | Infinite $N$; subtle liveness violations on huge state spaces | SPIN, NuSMV, TLC |\n| **Runtime Verification** (monitor) | Property violations *during execution* | Violations on untested paths | RV-Monitor, JavaMOP |\n| **Symbolic Execution** | Path conditions leading to CS conflicts | Infinite loops without invariant | KLEE, CBMC |\n| **Abstract Interpretation** | Over-approximated reachable states | False positives; precision loss | Astrée, CPAchecker |\n\n### 3.3 Computation of the Worst-Case Wait Bound\n\n**Claim:** In a system with $N$ processes using the Bakery protocol, a process $P_i$ that has just finished its doorway will be overtaken by at most $N - 1$ other processes before entering its CS.\n\n**Derivation sketch:**\n\n1. After $P_i$ exits its doorway, $number[i]$ is fixed and non-zero for the duration.\n2. Any other process $P_j$ that enters its doorway *after* $P_i$ reads $number[i]$ will compute a ticket strictly greater than $number[i]$ (since `max(...)` includes $P_i$'s ticket). Such $P_j$ will wait.\n3. However, any $P_j$ that entered its doorway *before* $P_i$ read $max$, but read $max$ *after* $P_i$ finished the max computation, will get a *smaller or equal* ticket.\n4. There are at most $N - 1$ such processes.\n5. Each of those $N - 1$ processes enters CS exactly once, then resets $number$ to $0$, and thereafter cannot block $P_i$.\n\nHence the wait bound:\n\n$$ \\boxed{\\;\\text{Worst-case number of overtakings} = N - 1\\;} $$\n\n> [!IMPORTANT]\n> This **bounded overtaking** is precisely the *starvation-freedom* guarantee. KTU examiners love asking: *\"Why is the Bakery protocol starvation-free but not strictly FIFO?\"* The answer: bounded overtaking ⇒ no starvation, but the bound is $N-1$, not $1$ as in strict FIFO.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Mermaid State-Machine of a Process $P_i$\n\n```mermaid\nstateDiagram-v2\n    [*] --> NCS : process starts\n    NCS --> Doorway : wants CS\n    Doorway : Doorway (4 atomic steps)\n    Doorway --> WaitingRoom : choosing[i]=false, number[i] set\n    WaitingRoom : Waiting Room (busy-wait)\n    WaitingRoom --> CS : for all j, condition holds\n    CS : Critical Section\n    CS --> ExitProtocol : work done\n    ExitProtocol : number[i] = 0\n    ExitProtocol --> NCS : resets\n    WaitingRoom --> WaitingRoom : some j blocks\n```\n\n### 4.2 Mermaid Block Diagram: Global Architecture\n\n```mermaid\nflowchart TB\n    subgraph SharedMemory[Shared Memory Region]\n        C[choosing array of size N]\n        N[number array of size N]\n    end\n\n    subgraph ProcessorP0[Processor hosting P0]\n        P0[Process P0]\n        C0[choosing 0 flag]\n        N0[number 0 ticket]\n    end\n\n    subgraph ProcessorP1[Processor hosting P1]\n        P1[Process P1]\n        C1[choosing 1 flag]\n        N1[number 1 ticket]\n    end\n\n    subgraph ProcessorPNm1[Processor hosting PN-1]\n        Pnm1[Process PN-1]\n        Cnm1[choosing N-1 flag]\n        Nnm1[number N-1 ticket]\n    end\n\n    P0 -. atomic read/write .-> C\n    P0 -. atomic read/write .-> N\n    P1 -. atomic read/write .-> C\n    P1 -. atomic read/write .-> N\n    Pnm1 -. atomic read/write .-> C\n    Pnm1 -. atomic read/write .-> N\n\n    C0 -. cell 0 .-> C\n    N0 -. cell 0 .-> N\n    C1 -. cell 1 .-> C\n    N1 -. cell 1 .-> N\n    Cnm1 -. cell N-1 .-> C\n    Nnm1 -. cell N-1 .-> N\n```\n\n### 4.3 Mermaid Sequence Diagram: Two-Process Contention Timeline\n\n```mermaid\nsequenceDiagram\n    participant P0 as Process P0\n    participant P1 as Process P1\n    participant Mem as Shared Memory\n\n    Note over P0,P1: Both want to enter CS\n\n    P0->>Mem: choosing[0] = true\n    P0->>Mem: number[0] = max(0,0) + 1 = 1\n    P0->>Mem: choosing[0] = false\n    Note over P0: Reads P1's number=0<br/>Condition holds -> enters CS\n\n    P1->>Mem: choosing[1] = true\n    P1->>Mem: number[1] = max(0,0) + 1 = 1\n    P1->>Mem: choosing[1] = false\n    P1->>Mem: Read number[0] = 1\n\n    Note over P1: Lex (1,0) < (1,1)<br/>So P1 must WAIT\n\n    P0->>Mem: number[0] = 0 (EXIT)\n    Note over P1: P0's ticket is now 0<br/>P1 enters CS\n\n    P1->>Mem: number[1] = 0 (EXIT)\n```\n\n### 4.4 Mermaid Verification Workflow (TLA+/SPIN)\n\n```mermaid\nflowchart LR\n    A[Write TLA+ spec of Bakery] --> B[Encode invariants:<br/>MutualExclusion, NoStarvation]\n    B --> C[Run TLC Model Checker]\n    C --> D{State space < threshold?}\n    D -- Yes --> E[Verification PASS for N=3]\n    D -- No --> F[Apply symmetry reduction<br/>or compositional reasoning]\n    F --> C\n    E --> G[Generate counter-example<br/>trace if failure]\n    C --> H[Use TLAPS for general N proof]\n    H --> I[Mathematical safety proof complete]\n```\n\n> [!IMPORTANT]\n> **KTU Diagram Note:** Examiners often accept any clear *flowchart-style* rendering. The Mermaid diagrams above are *examination-ready* and can be redrawn by hand if needed — focus on the **three-region state machine** (NCS → Doorway → Waiting → CS → Exit).\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n---\n\n**Q1.** `[KTU University Exam - May 2024]`\n*State the three correctness properties that Lamport's Bakery Protocol guarantees. Mention which one is a safety property and which is a liveness property.*\n\n**Model Answer (3 marks):**\n\nThe Bakery Protocol guarantees:\n\n1. **Mutual Exclusion (Safety Property):** No two processes can be in the critical section simultaneously. This is a *safety* property because it asserts \"nothing bad happens.\"\n\n2. **Starvation-Freedom (Liveness Property):** Every process that requests entry to the critical section will eventually be granted access. This is a *liveness* property because it asserts \"something good eventually happens.\"\n\n3. **Bounded Wait / FIFO-ish Ordering (Quasi-Liveness):** A process is overtaken by at most $N-1$ others. (Award 1 mark for safety identification, 1 for liveness, 1 for stating the third property and the $N-1$ bound.)\n\n---\n\n**Q2.** `[KTU University Exam - Dec 2023]`\n*What is the role of the `choosing[i]` flag in Lamport's Bakery algorithm? Why is it necessary?*\n\n**Model Answer (3 marks):**\n\nThe flag `choosing[i]` indicates that process $P_i$ is currently *in the process of computing* its ticket number (i.e., it is inside its **doorway**). It is set to `true` just before reading the maximum ticket and reset to `false` afterwards.\n\n**Why it is necessary:**\nWithout the `choosing` flag, a process $P_j$ could read a *partially computed* `number[i]` that is either stale or being concurrently updated, leading to two processes ending up with numerically equal tickets. The flag forces other processes to **busy-wait** on the doorway of $P_i$ until $P_i$ has finished writing its final ticket, ensuring that every ticket observed is **atomic** from the perspective of other processes.\n\n(1 mark for stating the role, 1 mark for the doorway concept, 1 mark for the atomicity argument.)\n\n---\n\n### 5.2 Part B — Long Answer Questions (14 Marks, with Internal Choice)\n\n---\n\n**Question A (14 Marks):**\n\n`[KTU University Exam - May 2024, Module 4]` \n\n**(a)** [7 Marks] *Explain Lamport's Bakery Algorithm in detail. Write the complete pseudocode for $N$ processes and explain the role of the `choosing` and `number` arrays.*\n\n**(b)** [7 Marks] *With the help of a suitable invariant, prove that Lamport's Bakery Algorithm satisfies the **Mutual Exclusion** property. Clearly state the invariant and show the proof by contradiction.*\n\n**Model Answer (a) — 7 Marks:**\n\n1. **Background (1 mark):** Lamport's Bakery Algorithm (1974) solves distributed mutual exclusion using only atomic load/store operations on shared memory. It mimics a bakery queue where customers take numbered tickets.\n\n2. **Shared Variables (1 mark):**\n   - `choosing[0..N-1]` — Boolean array, initially `false`.\n   - `number[0..N-1]` — non-negative integer array, initially `0`.\n\n3. **Pseudocode (3 marks):**\n   - Doorway: set `choosing[i] = true`; compute `number[i] = 1 + max(number[0..N-1])`; set `choosing[i] = false`.\n   - Waiting: for each $j \\neq i$, wait until `choosing[j] = false` AND either `number[j] = 0` OR $(number[i], i) \\prec (number[j], j)$.\n   - Critical Section: protected operation.\n   - Exit: `number[i] = 0`.\n\n4. **Role of arrays (2 marks):**\n   - `choosing[i]`: prevents other processes from reading a partial ticket. It is the synchronization handshake for the doorway.\n   - `number[i]`: holds the priority ticket; non-zero means the process is contending.\n\n**Model Answer (b) — 7 Marks:**\n\n1. **Invariant (1 mark):** Define the predicate $E(i)$:\n\n$$ E(i): \\forall j \\neq i: \\bigl(number[j] = 0\\bigr) \\lor \\bigl((number[i], i) \\prec (number[j], j)\\bigr) $$\n\n$P_i$ enters CS only when $E(i)$ holds.\n\n2. **Statement to prove (1 mark):** $\\forall i, j: i \\neq j \\Rightarrow \\neg(CS_i \\land CS_j)$, where $CS_k$ means \"$P_k$ is currently executing its critical section.\"\n\n3. **Proof by contradiction (4 marks):**\n   - Assume for contradiction that both $P_i$ and $P_j$ are in CS with $i \\neq j$.\n   - Then $E(i)$ and $E(j)$ both hold.\n   - By the **strict total order** on pairs, either $(number[i], i) \\prec (number[j], j)$ or $(number[j], j) \\prec (number[i], i)$.\n   - **Case 1:** $(number[i], i) \\prec (number[j], j)$. From $E(j)$, since $(number[i], i) \\prec (number[j], j)$ is true, the second disjunct of $E(j)$ fails, so $number[i]$ must equal $0$. But $P_i$ in CS implies $number[i] \\neq 0$. Contradiction.\n   - **Case 2:** Symmetric. Yields $number[j] = 0$, contradicting $P_j$ in CS.\n   - Hence both cases impossible; assumption false. **Mutual exclusion proved. ∎**\n\n4. **Concluding remark (1 mark):** The proof relies critically on the **strict total order** of the lexicographic comparator $\\prec$, which is why a *tie-breaker on process ID* is non-negotiable.\n\n---\n\n**Question B (14 Marks) — Alternative Choice:**\n\n`[KTU University Exam - Dec 2023, Module 4]`\n\n**(a)** [7 Marks] *Compare and contrast the Bakery Algorithm with the **Dekker's Algorithm** and **Peterson's Algorithm** for two-process mutual exclusion. Discuss their relative strengths and weaknesses, and explain why the Bakery Algorithm scales beyond two processes while the others do not.*\n\n**(b)** [7 Marks] *Describe how the Bakery Algorithm can be modeled and verified using the **TLA+ / PlusCal** language. Provide a high-level PlusCal sketch and explain which safety and liveness properties the TLC model checker can verify automatically.*\n\n**Model Answer (a) — 7 Marks:**\n\n| Aspect | Dekker | Peterson | Bakery (Lamport) |\n|--------|--------|----------|------------------|\n| **Processes Supported** | Exactly 2 | Exactly 2 | Arbitrary $N$ |\n| **Mechanism** | Turn flag + intent flag | Turn flag + intent flag | Ticket + PID tie-breaker |\n| **Memory Model** | Sequential consistency | Sequential consistency | Arbitrary (only atomic load/store) |\n| **Starvation-Free** | Yes (after fixes) | Yes | Yes |\n| **Fairness Guarantee** | Strict 2-process FIFO | Strict 2-process FIFO | Bounded wait ($N-1$ overtakes) |\n| **Verification Complexity** | Low | Low | Moderate (involves real numbers) |\n| **Hardware Assumption** | Atomic reads/writes of flags | Same | Same, but no memory-order assumption |\n\n**Key Insights (3 marks):**\n- Dekker and Peterson rely on a *binary* \"turn\" variable — they are inherently **2-process** protocols. Generalizing to $N$ requires $N$-way consensus, which Dekker/Peterson do not provide.\n- The Bakery algorithm's **lexicographic ordering** on $(number, pid)$ is a **total order** that works for any cardinality. This is what gives it scalability.\n- Dekker/Peterson require **strict sequential memory consistency** for correctness. The Bakery algorithm only requires **atomic single-variable load/store** — a much weaker assumption.\n\n**Model Answer (b) — 7 Marks:**\n\n**TLA+/PlusCal Sketch (4 marks):**\n\n```tla\nVARIABLES choosing, number, cs\n\nTypeOK == \n    /\\ choosing \\in [Proc -> BOOLEAN]\n    /\\ number \\in [Proc -> Nat]\n    /\\ cs \\in [Proc -> BOOLEAN]\n\nInit == \n    /\\ choosing = [p \\in Proc |-> FALSE]\n    /\\ number   = [p \\in Proc |-> 0]\n    /\\ cs       = [p \\in Proc |-> FALSE]\n\nEnter(p) ==\n    /\\ ~cs[p]\n    /\\ choosing' = [choosing EXCEPT ![p] = TRUE]\n    /\\ number'   = [number   EXCEPT ![p] = 1 + Max(number)]\n    /\\ choosing' = [choosing EXCEPT ![p] = FALSE]\n    /\\ \\A q \\in Proc \\ {p}:\n         /\\ ~choosing[q]\n         /\\ \\/ number[q] = 0\n            \\/ <<number[p], p>> < <<number[q], q>>\n    /\\ cs' = [cs EXCEPT ![p] = TRUE]\n\nExit(p) ==\n    /\\ cs[p]\n    /\\ cs' = [cs EXCEPT ![p] = FALSE]\n    /\\ number' = [number EXCEPT ![p] = 0]\n    /\\ UNCHANGED choosing\n\nSpec == \\E p \\in Proc : Enter(p) \\/ (cs[p] /\\ Exit(p))\n```\n\n**Verification in TLC (3 marks):**\n- **Safety Invariant to check:**\n  $$ MutEx \\;=\\; \\forall i, j \\in Proc: (cs[i] \\land cs[j]) \\Rightarrow i = j $$\n- **Liveness Property to check:**\n  $$ \\forall p \\in Proc: \\square(request(p) \\Rightarrow \\Diamond\\, cs[p]) $$\n  This is expressed in TLA+ as the temporal formula `[](\\A p \\in Proc : want[p] => <> cs[p])` and requires enabling **fairness conditions** in the spec.\n- TLC exhaustively explores the state space for small $N$ (typically $N = 2, 3, 4$), and TLAPS (the TLA+ proof system) can be used to *prove* correctness for arbitrary $N$.\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Ways Students Lose Marks on Bakery Protocol Questions:**\n>\n> 1. **Forgetting the `choosing` array** (–2 marks): Many students write a Bakery-like algorithm that omits the `choosing` flag. This breaks atomicity guarantees and the protocol may deadlock. Examiners deduct 2 marks immediately.\n>\n> 2. **Confusing strict and non-strict ordering** (–1 mark): The comparator $\\prec$ must be **strict** (no equality allowed). Writing $(number[j], j) \\preceq (number[i], i)$ is wrong.\n>\n> 3. **Ignoring the tie-breaker** (–2 marks): Without the PID tie-breaker, two processes with the same ticket number may both wait for each other forever — a deadlock. Always state the lexicographic order explicitly.\n>\n> 4. **Mixing up safety vs. liveness** (–1 mark): Mutual exclusion is **safety** (a bad thing — two in CS — must not happen). Starvation-freedom is **liveness** (a good thing — entry — must eventually happen). Examiners explicitly test this distinction in PECNT526.\n>\n> 5. **Stating \"FIFO\" instead of \"bounded wait\"** (–1 mark): The Bakery protocol is **not strictly FIFO** across $N$ processes. It guarantees a process is overtaken by at most $N-1$ others, not exactly $1$.\n\n---\n\n### 5.4 Topic Recap & Important Things to Remember\n\n> 📋 **High-Density Rapid Revision Checklist:**\n\n- 🍞 The **Bakery Protocol** is Lamport's 1974 distributed mutual exclusion algorithm based on a bakery queue analogy (customers take numbered tickets).\n- 📦 It uses **two shared arrays**: `choosing[N]` (Boolean) and `number[N]` (non-negative integer), both initialized to `0`.\n- 🔑 The `choosing[i]` flag is **essential** — it prevents reading a partial ticket during another process's doorway.\n- 🎫 The ticket is computed as: $number[i] \\leftarrow 1 + \\max(number[0], \\ldots, number[N-1])$.\n- ⚖️ The **lexicographic order** $(a, i) \\prec (b, j) \\iff a < b \\lor (a = b \\land i < j)$ provides a **strict total order** for tie-breaking.\n- 🔄 The lifecycle of $P_i$: **NCS → Doorway → Waiting Room → CS → Exit (reset to 0) → NCS**.\n- 🛡️ **Mutual Exclusion** is a *safety* invariant; **Starvation-Freedom** is a *liveness* property.\n- 📏 Worst-case overtaking bound: a process is overtaken by **at most $N-1$** others.\n- 🧠 Verification is typically done via **TLA+/PlusCal** (theorem proving) and **SPIN/NuSMV** (model checking for small $N$).\n- 🌐 It works under the **weakest** memory model: only atomic single-variable reads and writes are required (no Test-And-Set, no compare-and-swap).\n- ⚠️ The protocol is **not** strictly FIFO across $N$ processes; the term **bounded overtaking** is preferred.\n- 🆚 Compared to Dekker's and Peterson's, Bakery scales to **arbitrary $N$** while requiring only **atomic load/store**, not full sequential consistency.\n- 💡 In **PlusCal/TLA+**, the safety invariant is `MutEx == \\A i, j \\in Proc : (cs[i] /\\ cs[j]) => i = j`.\n- 🔬 The Bakery algorithm is a **standard benchmark** in automated verification — expect it in model-checking labs and exam questions.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-design-5-pecnt526-mModule 4-t11",
          "title": "Traffic Signalling System",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECNT526 - AUTOMATED VERIFICATION\n> - **Module:** Module 4\n> - **Topic:** Traffic Signalling System\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Standard)\n\n> [!NOTE]\n> **Traffic Signalling System (TSS) in Automated Verification** is a canonical *case study* in model checking used to demonstrate the modeling, specification, and verification of a safety-critical, real-time, concurrent reactive system. The system consists of traffic lights controlling vehicle flow at an intersection, a controller (timer/scheduler), and sensors (vehicle-presence detectors), all described as a **finite-state transition system** \\(M = (S, S_0, T, L)\\) over a set of boolean and enumerated state variables.\n\nIn the formal verification context (Clarke, Havelund, Kurshan), the Traffic Signalling System is reduced to a **Kripke Structure** so that temporal logic properties (CTL/LTL) can be checked using a model checker such as **SPIN**, **NuSMV**, or **UPPAAL**.\n\n## 1.2 Intuitive Analogy (Real-World Picture)\n\n> [!IMPORTANT]\n> **Analogy – The Air-Traffic Controller of a Road Junction**\n> Imagine a busy four-way intersection. A human policeman with a whistle can coordinate cars. Now replace the policeman with a tiny computer chip (the *controller*). The chip must guarantee: *no two perpendicular streams of cars get the green light simultaneously* (Safety) and *every stream eventually gets a green light* (Liveness). In Automated Verification, we don't *test* this chip with random cars — we **mathematically prove** that *all* possible behaviours satisfy the rules. The Traffic Signalling System is the textbook playground to learn this proof technique.\n\n## 1.3 System Components (KTU Board Standard Vocabulary)\n\n| Component | Role | Typical Variables |\n|-----------|------|-------------------|\n| **Traffic Light** | Visual signal for each direction | `red`, `yellow`, `green` |\n| **Controller** | Sequencer / timer | `counter`, `phase` |\n| **Sensor** | Detects waiting vehicles | `car_N`, `car_S`, `car_E`, `car_W` |\n| **Road** | The shared resource | `Intersection` |\n\n## 1.4 Why Traffic Light Verification Matters (KTU Engineering Context)\n\n> [!NOTE]\n> A faulty traffic controller can lead to **deadlock** (all lights red forever) or **collision** (cross-traffic green simultaneously). Both are *safety* violations. In safety-critical embedded design (AUTOSAR, ISO 26262), such systems **must be exhaustively verified** — testing is insufficient because the state space is astronomically large.\n\n![Real World Traffic Light Concept - Replaced by Mermaid Block Diagram Below]\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Operational Architecture\n\nThe Traffic Signalling System is decomposed into **four functional layers**:\n\n1. **Environment Layer** – Cars (processes) arriving at lanes.\n2. **Sensor Layer** – Boolean variables indicating car-presence.\n3. **Controller Layer** – A synchronous transition system driven by a clock.\n4. **Actuator Layer** – The lights (Red, Yellow, Green) themselves.\n\n## 2.2 Kripke Structure Formalisation\n\nThe system is modelled as a Kripke Structure:\n\n$$\nM = (S, \\; S_0, \\; R, \\; L)\n$$\n\nWhere:\n- $S$ = set of all global states (cartesian product of variable domains).\n- $S_0 \\subseteq S$ = set of initial states.\n- $R \\subseteq S \\times S$ = total transition relation (each state has at least one successor).\n- $L: S \\rightarrow 2^{AP}$ = labelling function mapping each state to a set of atomic propositions that hold there.\n\n> [!IMPORTANT]\n> For model checking, the relation $R$ must be **total** (no dead-states) — this is the *deadlock-freedom* precondition. The Traffic Controller must be designed so that every state has a defined next state.\n\n## 2.3 Step-by-Step Modelling Logic\n\n**Step 1 — Identify State Variables**\nFor a simple two-road (North-South / East-West) intersection with traffic lights:\n- `L_NS` ∈ {red, green, yellow} → 3 values\n- `L_EW` ∈ {red, green, yellow} → 3 values\n- `counter` ∈ {0, 1, …, N-1} → N timing values\n\n**Step 2 — Define Initial State**\nThe controller begins with NS = green, EW = red, counter = 0.\n\n**Step 3 — Encode Transition Relation**\nA guarded transition has the form:\n$$\n\\text{(guard)} \\;\\rightarrow\\; \\text{(action)}\n$$\n\nExample (Yellow phase for NS before turning red):\n$$\n(\\text{L\\_NS} = \\text{green} \\;\\land\\; \\text{counter} = T_{g}) \\;\\rightarrow\\; (\\text{L\\_NS}' = \\text{yellow}, \\; \\text{counter}' = 0)\n$$\n\n**Step 4 — Define Atomic Propositions**\n$$\nAP = \\{\\,\\text{L\\_NS=green}, \\; \\text{L\\_EW=green}, \\; \\text{collision}, \\; \\text{deadlock}\\,\\}\n$$\n\n**Step 5 — Specify Properties in CTL**\n\n| Property Type | CTL Formula | Meaning |\n|---------------|-------------|---------|\n| **Safety (Mutual Exclusion)** | $AG \\neg (\\text{L\\_NS=green} \\land \\text{L\\_EW=green})$ | Never both green simultaneously |\n| **Liveness (No Starvation)** | $AG(\\text{car\\_NS} \\rightarrow AF \\; \\text{L\\_NS=green})$ | If a car waits, it eventually gets green |\n| **Deadlock Freedom** | $AG \\; EX(\\text{true})$ | Every state has a successor |\n| **No Collision** | $AG \\neg \\text{collision}$ | Collision is globally unreachable |\n\n## 2.4 KTU High-Yield Formula Sheet\n\n> [!NOTE]\n> The following table is the **exam-ready cheat sheet** for the Traffic Signalling System case study. Memorise it.\n\n| # | Concept | Formula / Expression | Variables / Units |\n|---|---------|----------------------|-------------------|\n| 1 | Kripke Structure | $M = (S, S_0, R, L)$ | $S$ states, $R$ relation |\n| 2 | State-Space Size (product of variable domains) | $\\vert S \\vert = \\prod_{i=1}^{n} \\vert V_i \\vert$ | $V_i$ = domain of $i$-th variable |\n| 3 | Lights-only state count (2 directions, 3 colours) | $3^2 = 9$ | Pure colour states |\n| 4 | Full state count (with timer of size $N$) | $3 \\cdot 3 \\cdot N = 9N$ | Timer $N$ ticks |\n| 5 | CTL Satisfaction (state $s$ satisfies $f$) | $M, s \\models f$ | Boolean |\n| 6 | Safety Template | $AG \\, \\neg \\, \\varphi$ | $\\varphi$ = bad event |\n| 7 | Liveness Template | $AG(p \\rightarrow AF\\,q)$ | $p$ = request, $q$ = grant |\n| 8 | Fairness (Strong) | $GF\\,p$ (path formula) | $p$ infinitely often |\n| 9 | Fairness (Weak) | $FG\\,p$ (eventually always) | Path formula |\n| 10 | Bisimulation equivalence | $M_1 \\approx M_2$ | Reduction technique |\n| 11 | Synchronous Product (two sub-systems) | $M_1 \\parallel M_2$ | $S = S_1 \\times S_2$ |\n| 12 | Maximum BFS depth for verification | $d \\le \\vert S \\vert$ | Linear in state space |\n\n> [!WARNING]\n> **Board Pitfall:** When asked to compute the state space, students often forget the **timer variable**. A pure colour-only count ($9$ states) is *incomplete* — the timer is mandatory because phase transitions depend on time.\n\n## 2.5 Engineering Utility\n\nThe Traffic Signalling System model is the foundation of *Embedded Controller Verification* used by:\n- **Automotive OEMs** (Bosch, Continental) for body controllers.\n- **Avionics** (DO-178C compliant software) for runway light sequencers.\n- **Railway Signalling** (ETCS Level 2/3) for interlocking verification.\n\nThe same pattern — *sensors + controller + actuators + safety properties in CTL* — is reused in **pacemakers**, **smart-grid controllers**, and **PLC ladder logic verification**.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Exhaustive State-Space Derivation (2-Lane Intersection with Timer)\n\n**Problem Setup (KTU-Style):**\nConsider a simplified intersection with:\n- Two lights: $L_{NS}$ and $L_{EW}$\n- Each light has 3 possible colours: $\\{ \\text{R}, \\text{G}, \\text{Y} \\}$\n- A global counter $c$ that cycles from $0$ to $N-1$\n\n**Derivation — Step by Step:**\n\n**Step 1 — Domain of each variable**\n\n$$\n\\text{Dom}(L_{NS}) = \\{\\text{R}, \\text{G}, \\text{Y}\\} \\quad\\Rightarrow\\quad \\vert \\text{Dom}(L_{NS}) \\vert = 3\n$$\n\n$$\n\\text{Dom}(L_{EW}) = \\{\\text{R}, \\text{G}, \\text{Y}\\} \\quad\\Rightarrow\\quad \\vert \\text{Dom}(L_{EW}) \\vert = 3\n$$\n\n$$\n\\text{Dom}(c) = \\{0, 1, \\ldots, N-1\\} \\quad\\Rightarrow\\quad \\vert \\text{Dom}(c) \\vert = N\n$$\n\n**Step 2 — Apply the product rule for Cartesian state space**\n\nThe total state space is the Cartesian product of all variable domains. Since the variables are independent, the cardinality multiplies:\n\n$$\n\\vert S \\vert \\;=\\; \\vert \\text{Dom}(L_{NS}) \\vert \\times \\vert \\text{Dom}(L_{EW}) \\vert \\times \\vert \\text{Dom}(c) \\vert\n$$\n\n**Step 3 — Substitute the values**\n\n$$\n\\vert S \\vert \\;=\\; 3 \\times 3 \\times N\n$$\n\n$$\n\\therefore \\quad \\vert S \\vert \\;=\\; 9N\n$$\n\n**Step 4 — Numerical example (typical KTU value: $N = 5$)**\n\n$$\n\\vert S \\vert \\;=\\; 9 \\times 5 \\;=\\; 45 \\;\\text{states}\n$$\n\n**Step 5 — Generalisation to a 4-way intersection (NS, EW, NE-SW, NW-SE)**\n\nIf four independent lights are present:\n\n$$\n\\vert S \\vert \\;=\\; 3^4 \\times N \\;=\\; 81N\n$$\n\nFor $N = 5$ this yields $405$ states — still tractable for a model checker like NuSMV.\n\n## 3.2 Safety Property Verification (Mutual Exclusion)\n\n**Property to prove:**\n$$\n\\varphi \\;\\equiv\\; AG \\, \\neg \\, (L_{NS} = \\text{G} \\,\\land\\, L_{EW} = \\text{G})\n$$\n\n**Proof by CTL Model Checking (fixed-point):**\n\nStep 1 — Compute the set of states satisfying the atomic proposition:\n\n$$\n\\text{Sat}(\\,L_{NS}=\\text{G}\\,) \\;=\\; \\{\\, s \\in S \\mid s(L_{NS}) = \\text{G} \\,\\}\n$$\n\nStep 2 — Compute the set of states satisfying the conjunction:\n\n$$\n\\text{Sat}(\\,L_{NS}=\\text{G} \\land L_{EW}=\\text{G}\\,) \\;=\\; \\text{Sat}(L_{NS}=\\text{G}) \\cap \\text{Sat}(L_{EW}=\\text{G})\n$$\n\nStep 3 — Compute the complement (states that are safe):\n\n$$\n\\text{Sat}(\\,\\neg\\,(L_{NS}=\\text{G} \\land L_{EW}=\\text{G})\\,) \\;=\\; S \\setminus \\text{Sat}(L_{NS}=\\text{G} \\land L_{EW}=\\text{G})\n$$\n\nStep 4 — Apply $AG$ via the greatest fixed-point:\n\n$$\n\\text{Sat}(AG \\, \\psi) \\;=\\; \\nu X \\,.\\, \\text{Sat}(\\psi) \\cap \\text{Pre}(X)\n$$\n\nWhere $\\text{Pre}(X)$ is the pre-image operator returning all states whose successors lie in $X$.\n\nStep 5 — Verify $S_0 \\subseteq \\text{Sat}(AG\\,\\psi)$:\n\n$$\n\\boxed{\\;M, s_0 \\models AG \\, \\neg \\, (L_{NS}=\\text{G} \\land L_{EW}=\\text{G})\\;}\n$$\n\n> [!IMPORTANT]\n> In KTU valuation, the examiner awards marks for the **explicit invocation of the fixed-point** $\\nu X$. Writing just \"$AG$ means always\" is *not* sufficient for full marks.\n\n## 3.3 Liveness Verification Template\n\n**Property:** Every car-arrival is eventually rewarded with a green light.\n\n$$\n\\psi \\;\\equiv\\; AG \\,\\bigl(\\,\\text{car\\_NS} \\;\\rightarrow\\; AF \\; L_{NS}=\\text{G}\\,\\bigr)\n$$\n\n**Evaluation (CTL semantics):**\nFor a state $s$ with $s \\models \\text{car\\_NS}$, model checking must show that **every** path from $s$ reaches a state where $L_{NS}=\\text{G}$. This is the $AF$ operator's universal-path quantifier.\n\nIf the counter increments monotonically and resets at phase changes, the result holds under *strong fairness* on the controller process.\n\n## 3.4 State Explosion — Analytical Matrix\n\nThe Traffic Signalling System is the canonical example to **demonstrate the state-explosion problem**. The following table quantifies the issue:\n\n| System Variant | Variables | Domain each | $\\vert S \\vert$ | Verifiable by NuSMV? |\n|----------------|-----------|-------------|------------------|----------------------|\n| 2 lights + 1 timer ($N=5$) | 3 | $\\{3, 3, 5\\}$ | $45$ | ✅ Trivial |\n| 2 lights + 2 timers ($N=10$) | 4 | $\\{3, 3, 10, 10\\}$ | $900$ | ✅ Easy |\n| 4 lights + 4 timers | 8 | $\\{3^4, 10^4\\}$ | $8.1 \\times 10^5$ | ✅ Tractable |\n| 4 lights + sensors (5 cars) | 13 | $\\{3, 3, 3, 3, 2^5\\}$ | $2.4 \\times 10^5$ | ✅ Tractable |\n| Distributed intersection (2 controllers) | 20 | product | $\\sim 10^{12}$ | ❌ Need BDD/Abstraction |\n\n> [!WARNING]\n> KTU Pitfall: Many students write \"the system has infinite states\" — *incorrect*. The Kripke Structure is **finite** (variables are bounded). The challenge is **size**, not infinity. The correct terminology is *state-space explosion*.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Top-Level Functional Block Architecture (Mermaid)\n\n```mermaid\nflowchart LR\n    env1[Environment - Cars NS] --> sensor1[Sensor NS]\n    env2[Environment - Cars EW] --> sensor2[Sensor EW]\n    sensor1 --> ctrl[Controller - FSM Timer]\n    sensor2 --> ctrl\n    ctrl --> light1[Light NS - RGB]\n    ctrl --> light2[Light EW - RGB]\n    light1 --> out1[Output to NS Drivers]\n    light2 --> out2[Output to EW Drivers]\n    ctrl -. feedback .-> sensor1\n    ctrl -. feedback .-> sensor2\n    vmodel[Model Checker NuSMV SPIN] -. verifies .-> ctrl\n    proplog[CTL Specifications] -. input .-> vmodel\n```\n\n## 4.2 Kripke Structure for the Traffic Light (Mermaid State Diagram)\n\n```mermaid\nstateDiagram-v2\n    [*] --> S0\n    S0: S0 = NS_Green EW_Red counter=0\n    S1: S1 = NS_Yellow EW_Red counter=0\n    S2: S2 = NS_Red EW_Green counter=0\n    S3: S3 = NS_Red EW_Yellow counter=0\n    S0 --> S1 : tick Green-done\n    S1 --> S2 : tick Yellow-done\n    S2 --> S3 : tick Green-done\n    S3 --> S0 : tick Yellow-done\n    S0 --> S0 : counter counter < Tg\n    S1 --> S1 : counter counter < Ty\n    S2 --> S2 : counter counter < Tg\n    S3 --> S3 : counter counter < Ty\n```\n\n## 4.3 Verification Workflow Diagram (Mermaid)\n\n```mermaid\nflowchart TD\n    a1[Step 1 - Model Intersection as Kripke Structure M] --> a2[Step 2 - Encode Variables and Initial State]\n    a2 --> a3[Step 3 - Define Transition Relation R]\n    a3 --> a4[Step 4 - Write CTL Properties Safety Liveness]\n    a4 --> a5[Step 5 - Feed M and Properties into Model Checker]\n    a5 --> a6{Step 6 - Result}\n    a6 -->|TRUE| a7[Property holds - System verified]\n    a6 -->|FALSE| a8[Counter-example path produced]\n    a8 --> a9[Debug controller and re-iterate]\n    a9 --> a1\n```\n\n## 4.4 Property Layering (Mermaid)\n\n```mermaid\nflowchart TB\n    P0[Layer 0 - No Deadlock AG EX true]\n    P1[Layer 1 - Safety No Collision AG not LNS_G and LEW_G]\n    P2[Layer 2 - Mutual Exclusion Only one direction Green]\n    P3[Layer 3 - Liveness AF Green after request]\n    P4[Layer 4 - Fairness GF Controller active]\n    P0 --> P1 --> P2 --> P3 --> P4\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **[KTU University Exam — May 2024 Model Paper]**\n\n**Q1.** Define a Kripke Structure and explain how it is used to model a Traffic Signalling System. (3 Marks)\n\n**Model Answer:**\nA Kripke Structure is a 4-tuple $M = (S, S_0, R, L)$ where $S$ is a finite set of states, $S_0 \\subseteq S$ are the initial states, $R \\subseteq S \\times S$ is a total transition relation, and $L : S \\rightarrow 2^{AP}$ is a labelling function over atomic propositions.\nFor the Traffic Signalling System, each state is an assignment of values to variables $\\{L_{NS}, L_{EW}, \\text{counter}\\}$. The transitions encode the controller logic. The labelling function identifies which lights are green, yellow, or red in each state, enabling CTL model checking of safety and liveness properties. (3 Marks)\n\n---\n\n> **[KTU University Exam — Dec 2023 Model Paper]**\n\n**Q2.** Distinguish between **safety** and **liveness** properties with reference to the Traffic Signalling System. (3 Marks)\n\n**Model Answer:**\nA **safety property** states that \"something bad never happens\". In TSS, $AG\\, \\neg\\,(L_{NS}=\\text{G} \\land L_{EW}=\\text{G})$ is a safety property ensuring no perpendicular collision. A **liveness property** states that \"something good eventually happens\", e.g., $AG(\\text{car\\_NS} \\rightarrow AF\\;L_{NS}=\\text{G})$ ensures that a waiting car is eventually granted a green light. Safety is verified using the $\\nu$ (greatest) fixed-point, while liveness uses the $\\mu$ (least) fixed-point in CTL model checking. (3 Marks)\n\n---\n\n## 5.2 Part B — Full-Descriptive Question (14 Marks, Internal Choice)\n\n> **[KTU University Exam — Model Paper 2024 Scheme, Module 4]**\n\n### ✅ Question A (14 Marks)\n\n**(a)** Model a two-way traffic intersection (North-South and East-West roads) as a Kripke Structure suitable for model checking. Define the variables, initial state, and transition relation. (7 Marks)\n\n**(b)** Specify the following properties in CTL and explain their satisfaction on your model:\n   (i) Mutual exclusion between NS and EW green signals.\n   (ii) Liveness: a waiting NS car eventually gets a green light.\n   (iii) Deadlock-freedom. (7 Marks)\n\n#### Model Solution\n\n**(a) Kripke Modelling — 7 Marks**\n\n**Variables (2 Marks):**\n- $L_{NS} \\in \\{\\text{R}, \\text{G}, \\text{Y}\\}$\n- $L_{EW} \\in \\{\\text{R}, \\text{G}, \\text{Y}\\}$\n- $c \\in \\{0, 1, \\ldots, N-1\\}$ (timer)\n- $car_{NS}, car_{EW} \\in \\{\\text{true}, \\text{false}\\}$ (sensors)\n\n**Initial State (1 Mark):**\n$$\nS_0 = \\{\\, s \\mid s(L_{NS}) = \\text{G},\\; s(L_{EW}) = \\text{R},\\; s(c) = 0,\\; s(car_{NS}) = \\text{false},\\; s(car_{EW}) = \\text{false} \\,\\}\n$$\n\n**Transition Relation (4 Marks):** Defined by guarded rules, e.g.,\n\n| Guard | Action (Next State) |\n|-------|---------------------|\n| $L_{NS}=\\text{G} \\land c < T_g$ | increment $c$ |\n| $L_{NS}=\\text{G} \\land c = T_g$ | $L_{NS}'=\\text{Y}, c'=0$ |\n| $L_{NS}=\\text{Y} \\land c = T_y$ | $L_{NS}'=\\text{R},\\; L_{EW}'=\\text{G},\\; c'=0$ |\n| $L_{EW}=\\text{G} \\land c = T_g$ | $L_{EW}'=\\text{Y},\\; c'=0$ |\n| $L_{EW}=\\text{Y} \\land c = T_y$ | $L_{EW}'=\\text{R},\\; L_{NS}'=\\text{G},\\; c'=0$ |\n\n**State-Space (bonus, in-depth):**\n$$\n\\vert S \\vert = 3 \\times 3 \\times N \\times 2 \\times 2 = 36N\n$$\n\n---\n\n**(b) CTL Properties — 7 Marks**\n\n**(i) Mutual Exclusion (2 Marks):**\n$$\n\\varphi_1 \\;\\equiv\\; AG\\,\\neg\\,(L_{NS}=\\text{G} \\land L_{EW}=\\text{G})\n$$\nSatisfaction: $M, s_0 \\models \\varphi_1$ because the transition rules never allow both lights to be green simultaneously.\n\n**(ii) Liveness (3 Marks):**\n$$\n\\varphi_2 \\;\\equiv\\; AG\\,\\bigl(\\,car_{NS} \\rightarrow AF\\, L_{NS}=\\text{G}\\,\\bigr)\n$$\nSatisfaction requires that from every state with $car_{NS}=\\text{true}$, **all** infinite paths reach a state where $L_{NS}=\\text{G}$. This is provable because the controller's round-robin schedule guarantees NS gets green within at most $T_g + T_y$ time units.\n\n**(iii) Deadlock-Freedom (2 Marks):**\n$$\n\\varphi_3 \\;\\equiv\\; AG\\; EX(\\text{true})\n$$\nSatisfaction: every state has at least one successor (the counter is always non-stuck because the timer strictly increments modulo $N$).\n\n> [!WARNING]\n> **KTU Examiner's Pitfall Callout (Q-A)**\n> - ❌ Forgetting the timer variable $c$ → loses 2 marks.\n> - ❌ Writing $EG$ instead of $AG$ for safety → loses 1 mark.\n> - ❌ Confusing $AF$ (universal path) with $EF$ (existential path) for liveness → loses 2 marks.\n> - ✅ Always state the **fixed-point** used ($\\nu$ for $AG$, $\\mu$ for $AF$) to score full marks.\n\n---\n\n### ✅ Question B (14 Marks — Alternative Choice)\n\n**(a)** Explain the *state-space explosion problem* with reference to the Traffic Signalling System. Compute the number of reachable states for: (i) 2 lights + 1 timer ($N=4$); (ii) 4 lights + 4 timers ($N=10$); (iii) A distributed system of 2 such intersections. (7 Marks)\n\n**(b)** Discuss two *mitigation techniques* (BDD-based symbolic model checking, partial-order reduction, or abstraction refinement) applicable to the Traffic Signalling System. (7 Marks)\n\n#### Model Solution\n\n**(a) State-Space Explosion (7 Marks)**\n\n**Concept (2 Marks):**\nState-space explosion occurs when the number of global states grows exponentially with the number of variables, making explicit-state model checking infeasible. In the Traffic Signalling System, adding sensors, lanes, or interconnected intersections causes combinatorial blow-up.\n\n**Computations (5 Marks):**\n\n**(i) 2 lights + 1 timer ($N=4$):**\n$$\n\\vert S \\vert = 3 \\times 3 \\times 4 = 36 \\;\\text{states}\n$$\n\n**(ii) 4 lights + 4 timers ($N=10$):**\n$$\n\\vert S \\vert = 3^4 \\times 10^4 = 81 \\times 10000 = 810{,}000 \\;\\text{states}\n$$\n\n**(iii) 2 distributed intersections (synchronous product):**\n$$\n\\vert S \\vert = (3^2 \\times 4)^2 = 36^2 = 1{,}296 \\;\\text{states}\n$$\n\nFor richer models with sensor vectors of 10 booleans and 6 lights, the count easily exceeds $10^9$, exceeding RAM limits of explicit-state tools.\n\n---\n\n**(b) Mitigation Techniques (7 Marks)**\n\n**Technique 1 — BDD-Based Symbolic Model Checking (3.5 Marks):**\nBinary Decision Diagrams (Bryant, 1986) encode the transition relation and state sets as compressed boolean functions. The Traffic Light model $M$ is represented as a single BDD; CTL checking operates on BDDs using fixed-point computations. State sets with $10^{20}$ elements can often be represented in megabytes, since the BDD exploits structural regularity (e.g., the round-robin schedule has high redundancy).\n\n**Technique 2 — Partial-Order Reduction (POR) (3.5 Marks):**\nIndependent transitions (e.g., $L_{NS}$'s colour change and $c$'s increment) can be explored in any order without affecting property satisfaction. Ample-set and persistent-set POR algorithms prune the explored state graph. For the Traffic Signalling System, POR reduces the effective state space to only one representative interleaving per equivalence class, often cutting the graph by 60–80%.\n\n> [!WARNING]\n> **KTU Examiner's Pitfall Callout (Q-B)**\n> - ❌ Stating *abstraction* without specifying *what* is abstracted → loses 2 marks.\n> - ❌ Confusing BDDs with Decision Trees → loses 1 mark.\n> - ❌ Giving only the formula for state-space but not numerical answers → loses 1 mark per sub-part.\n> - ✅ Always tie the technique back to the **Traffic Signalling System** specifically.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid-Revision Checklist — Module 4: Traffic Signalling System**\n\n- 🔹 The Traffic Signalling System is modelled as a **Kripke Structure** $M = (S, S_0, R, L)$.\n- 🔹 Standard variables: $L_{NS}$, $L_{EW}$, $counter$, $car_{NS}$, $car_{EW}$.\n- 🔹 State-space formula: $\\vert S \\vert = \\prod_i \\vert V_i \\vert$ (product of variable domains).\n- 🔹 For 2 lights + timer of size $N$: $\\vert S \\vert = 9N$.\n- 🔹 **Safety** properties use the template $AG\\, \\neg\\, \\varphi$ (no collision).\n- 🔹 **Liveness** properties use $AG(p \\rightarrow AF\\,q)$ (eventual green).\n- 🔹 **Deadlock-freedom** is encoded as $AG\\, EX(\\text{true})$.\n- 🔹 **Fairness** conditions (strong $GF\\,p$ / weak $FG\\,p$) are crucial for proving liveness.\n- 🔹 $AG$ uses the **greatest fixed point** $\\nu X$; $AF$ uses the **least fixed point** $\\mu X$.\n- 🔹 State-space explosion is a **size** problem, not infinity. Mitigations: BDDs, POR, Abstraction.\n- 🔹 Tools: **NuSMV** (symbolic), **SPIN** (explicit-state), **UPPAAL** (timed automata).\n- 🔹 Application domains: automotive ECUs, avionics, railway interlocking, medical devices.\n- 🔹 Key distinction: $AF$ = *all* paths eventually; $EF$ = *some* path eventually. Confusing them costs marks.\n- 🔹 The total transition relation is mandatory — partial relations violate model-checking preconditions.\n- 🔹 KTU board expects the *fixed-point operator* to appear in any $AG/AF$ justification for full marks.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        }
      ]
    }
  ]
}
