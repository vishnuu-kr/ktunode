{
  "id": "pecst741-artificial-intelligence-and-data-science-s7",
  "code": "PECST741",
  "name": "FORMAL METHODS IN SOFTWARE ENGINEERING",
  "branchId": "artificial-intelligence-and-data-science",
  "semester": 7,
  "modules": [
    {
      "id": "m1",
      "title": "Module 1",
      "topics": [
        {
          "id": "artificial-intelligence-and-data-science-7-pecst741-mModule 1-t1",
          "title": "Stages in Software Development",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST741 - FORMAL METHODS IN SOFTWARE ENGINEERING\n> - **Module:** Module 1\n> - **Topic:** Stages in Software Development\n\n<!-- SECTION_1_START -->\n\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Terminology)\n\nIn the context of **Formal Methods in Software Engineering**, the **Stages in Software Development** refer to the formally structured, sequential (and sometimes iterative) phases through which a software system evolves from an abstract stakeholder need to a deployed, maintained, and eventually retired product. The KTU 2024 scheme recognizes a **V-Model** aligned lifecycle where each specification stage is rigorously tied to a corresponding verification stage.\n\n> [!IMPORTANT]\n> **KTU 2024 Definition**: Software development is the disciplined application of a sequence of **engineering activities** — *Requirements, Specification, Design, Implementation, Verification (Testing), and Maintenance* — each producing **formal artifacts** (models, specifications, proofs, code) that act as the contract for the next stage.\n\nThe standard six stages recognized by KTU for PECST741 are:\n\n1. **Requirements Engineering (Domain Analysis)**\n2. **Formal Specification**\n3. **Architectural & Detailed Design**\n4. **Implementation (Coding)**\n5. **Verification & Validation (Testing & Proof)**\n6. **Maintenance & Evolution**\n\n## 1.2 Intuitive Analogy — Building a House\n\n> [!NOTE]\n> **Conceptual Analogy — \"The House Blueprint Model\"**\n\nImagine you are building a house. You do **not** start by laying bricks randomly:\n\n- First, the family tells the architect *what they need* (3 bedrooms, north-facing, ₹50L budget) — this is **Requirements**.\n- The architect writes a precise blueprint with exact dimensions, plumbing layout, and electrical wiring — this is **Formal Specification**.\n- The structural engineer calculates beam thickness, soil load, and reinforcement — this is **Design**.\n- Masons, electricians, and plumbers follow the design to lay bricks, wires, and pipes — this is **Implementation**.\n- The municipal inspector visits *at every level* (foundation check, wall check, final check) to confirm the house matches the blueprint — this is **Verification**.\n- After possession, when the roof leaks after 5 years, you call the engineer to fix it — this is **Maintenance**.\n\n**Software development follows the exact same discipline.** A formal method is simply a *mathematically precise blueprint* the inspector (tester) can check with absolute certainty.\n\n## 1.3 Key Metrics and Terminology\n\n> [!IMPORTANT]\n> **Standard KTU Metrics You Must Know**\n> - **LOC** (Lines of Code) — size metric\n> - **Function Points (FP)** — functionality metric\n> - **Cyclomatic Complexity** — structural complexity measure\n> - **Defect Density** — defects per KLOC\n> - **MTTF** (Mean Time To Failure) — reliability metric\n> - **COCOMO** (Constructive Cost Model) — effort estimation\n\nThese terms are **bold-marked** because they appear frequently in KTU 2-mark and 3-part questions across this module.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Six Stages — Deep Analysis\n\n### Stage 1: Requirements Engineering\n- **Goal**: Capture *what* the system must do, not *how*.\n- **Why**: Eliminates ambiguity. Most software failures (≈ 56% per Standish Group) trace back to poor requirements.\n- **How**: Stakeholder interviews → use cases → requirement document (SRS).\n- **Formal Touch**: Requirements are expressed as **pre/post-conditions**, invariants, and temporal logic (LTL/CTL) statements.\n\n### Stage 2: Formal Specification\n- **Goal**: Produce a *mathematical description* of system behavior.\n- **Why**: A natural language SRS can be interpreted in multiple ways. A formal spec has *one* interpretation.\n- **How**: Use notation such as **Z (Zed)**, **VDM-SL**, **B-Method**, **Alloy**, or **TLA+**.\n- **Artifacts**: Schemas with state variables, operations as state transitions, type invariants.\n\n### Stage 3: Architectural & Detailed Design\n- **Goal**: Define *how* the specification will be realized in software structure.\n- **Why**: Bridges the abstract world of mathematics to the concrete world of modules/classes.\n- **How**: Decompose spec into modules, define interfaces, choose patterns (MVC, layered, microservices).\n- **Formal Touch**: **Refinement calculus** — show that the design *refines* (implements) the specification.\n\n### Stage 4: Implementation (Coding)\n- **Goal**: Translate the design into executable source code.\n- **Why**: Machines only understand code; all prior stages were to ensure the right code is written.\n- **How**: Adhere to coding standards, use type-safe languages, follow the design contract.\n- **Formal Touch**: **Correct-by-construction** code (e.g., SPARK subset of Ada, Dafny) carries formal contracts as executable annotations.\n\n### Stage 5: Verification & Validation\n- **Goal**: Prove the implementation meets the specification (Verification) and meets user needs (Validation).\n- **Why**: The *V&V* stage is where defects are caught before delivery.\n- **How**:\n  - **Dynamic testing** — unit, integration, system, acceptance.\n  - **Static analysis** — model checking, theorem proving, abstract interpretation.\n- **Formal Touch**: Model checkers (SPIN, NuSMV) exhaustively explore state space.\n\n### Stage 6: Maintenance & Evolution\n- **Goal**: Keep the system operational, fix defects, add features.\n- **Why**: 60–80% of total software lifecycle cost is in maintenance.\n- **How**: Corrective, adaptive, perfective, preventive maintenance.\n- **Formal Touch**: **Re-verification** — re-running proofs/tests after changes.\n\n## 2.2 The \"Why\" and \"How\" Summary Matrix\n\n| Stage | Why it exists | How it is executed | Formal artifact |\n|---|---|---|---|\n| Requirements | Avoid misinterpretation | Interviews, use cases | SRS document, use-case diagrams |\n| Specification | Remove ambiguity | Z, VDM, B notation | Mathematical schema |\n| Design | Bridge spec ↔ code | UML, ADL, patterns | Refinement mapping |\n| Implementation | Make it run | Coding in typed languages | Source code with contracts |\n| V & V | Catch defects | Testing, model checking | Test reports, proof scripts |\n| Maintenance | Extend life | Patches, re-engineering | Updated specs and proofs |\n\n## 2.3 KTU Formula Sheet / Cheat Sheet\n\n| Concept | Formula / Notation | Units / Notes |\n|---|---|---|\n| Function Point (FP) | $\\text{FP} = UFP \\times VAF$ | $VAF = 0.65 + 0.01 \\times \\sum_{i=1}^{14} F_i$ |\n| Cyclomatic Complexity (McCabe) | $V(G) = E - N + 2P$ | $E$ = edges, $N$ = nodes, $P$ = connected components |\n| Defect Density | $\\text{DD} = \\dfrac{\\text{Defects Found}}{\\text{KLOC}}$ | Defects per thousand LOC |\n| COCOMO Basic Effort | $E = a \\times (\\text{KLOC})^{b}$ | $a, b$ are mode coefficients |\n| Reliability (MTTF) | $\\text{MTTF} = \\dfrac{1}{\\lambda}$ | $\\lambda$ = failure rate |\n| Specification Refinement | $S_1 \\sqsubseteq S_2$ | $S_1$ is abstracted by $S_2$ |\n| Z Schema Composition | $S_1 \\wedge S_2$, $S_1 \\circ S_2$ | Conjunction and piping |\n| Pre-condition | $\\{P\\} \\ C \\ \\{Q\\}$ | Hoare triple — partial correctness |\n| LTL Syntax | $\\Box p \\rightarrow \\Diamond q$ | Always $p$ implies eventually $q$ |\n| CTL Path Quantifier | $\\forall \\square \\, p$ | For all paths, globally $p$ |\n\n> [!IMPORTANT]\n> **Critical Note on Z-Notation Operators You Will Be Tested On**:\n> - $\\Delta S$ — the **change** of state schema $S$\n> - $\\Xi S$ — the **non-change** (read-only) of state schema $S$\n> - $S^\\prime$ — the **post-state** of $S$\n\n## 2.4 Real-World Engineering Utility\n\nThe staged model underpins **safety-critical industries**:\n- **Aerospace (DO-178C)** — requires formal methods at specification and verification stages.\n- **Railway signaling (EN 50128)** — mandates SIL 3/4 software to use formal spec + proof.\n- **Medical devices (IEC 62304)** — Class C software uses formal verification.\n- **Automotive (ISO 26262 ASIL D)** — encourages model-based formal development.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation of the V-Model Stage Correspondence\n\nThe V-Model is mathematically grounded in the principle that **every specification activity has a corresponding verification activity at the same level of abstraction**.\n\n### 3.1.1 Starting Point — Refinement Chain\n\nWe begin with the *abstract specification* $Spec_0$ and the *concrete implementation* $Impl$:\n\n$$\nSpec_0 \\sqsubseteq Spec_1 \\sqsubseteq Spec_2 \\sqsubseteq \\ldots \\sqsubseteq Spec_k \\sqsubseteq Impl\n$$\n\n**Step 1** — Define the abstraction function $\\alpha$ from concrete to abstract state.\n**Step 2** — For each operation $Op_{abs}$ in $Spec_i$ and $Op_{con}$ in $Spec_{i+1}$:\n\n$$\n\\forall s, s^\\prime \\; \\bullet \\; pre\\_Op_{abs}(s) \\land \\alpha(s_{con}) = s \\land Op_{con}(s_{con}, s_{con}^\\prime) \\;\\Rightarrow\\; \\alpha(s_{con}^\\prime) = s^\\prime \\land post\\_Op_{abs}(s, s^\\prime)\n$$\n\n**Step 3** — This is the **forward simulation condition**. It states that every concrete step has an abstract step preserving the relation.\n\n**Step 4** — The V-Model is the *vertical symmetry* of this chain: each level on the left arm (specification) has its mirror on the right arm (verification).\n\n### 3.1.2 Derivation of Hoare Logic Pre/Post Conditions\n\n**Starting from the Hoare triple**:\n\n$$\n\\{P\\} \\; C \\; \\{Q\\}\n$$\n\n**Step 1** — *Partial correctness axiom*:\n\n$$\n\\frac{}{\\{Q[x \\backslash e]\\} \\; x := e \\; \\{Q\\}} \\quad \\text{(Assignment)}\n$$\n\n**Step 2** — *Composition rule*:\n\n$$\n\\frac{\\{P\\} \\; C_1 \\; \\{R\\}, \\quad \\{R\\} \\; C_2 \\; \\{Q\\}}{\\{P\\} \\; C_1; C_2 \\; \\{Q\\}}\n$$\n\n**Step 3** — *Consequence rule*:\n\n$$\n\\frac{P \\Rightarrow P^\\prime, \\quad \\{P^\\prime\\} \\; C \\; \\{Q^\\prime\\}, \\quad Q^\\prime \\Rightarrow Q}{\\{P\\} \\; C \\; \\{Q\\}}\n$$\n\n**Step 4** — *While-rule* (loop invariant $I$):\n\n$$\n\\frac{\\{I \\land B\\} \\; C \\; \\{I\\}}{\\{I\\} \\; \\text{while } B \\text{ do } C \\; \\{I \\land \\lnot B\\}}\n$$\n\n**Interpretation**: $P$ is the **pre-condition** (input contract), $Q$ is the **post-condition** (output contract), and $I$ is the **loop invariant** (preserved property through iteration).\n\n### 3.1.3 Cyclomatic Complexity Derivation\n\nMcCabe's $V(G)$ for a control flow graph:\n\n**Step 1** — Convert program to directed graph $G$ with $E$ edges, $N$ nodes.\n\n**Step 2** — Apply Euler's formula adaptation for a single connected component:\n\n$$\nV(G) = E - N + 2\n$$\n\n**Step 3** — For $P$ connected components:\n\n$$\nV(G) = E - N + 2P\n$$\n\n**Step 4** — Equivalently, count **predicate nodes** $+1$:\n\n$$\nV(G) = \\pi + 1\n$$\n\nwhere $\\pi$ is the number of decision points (if, while, for, case).\n\n**Step 5** — Industry benchmark: $V(G) \\leq 10$ per module for maintainable code.\n\n### 3.1.4 Function Point Derivation\n\n**Step 1** — Count the 5 FP component types (External Inputs, External Outputs, External Inquiries, Internal Logical Files, External Interface Files) by complexity (Low/Average/High).\n\n**Step 2** — Multiply each count by the standard weight:\n\n$$\nUFP = \\sum_{i} W_i \\times C_i\n$$\n\n**Step 3** — Compute Value Adjustment Factor from 14 General System Characteristics (GSCs), each rated 0–5:\n\n$$\nVAF = 0.65 + 0.01 \\times \\sum_{i=1}^{14} F_i\n$$\n\n**Step 4** — Final Function Point:\n\n$$\nFP = UFP \\times VAF\n$$\n\n**Step 5** — Convert to LOC using language-specific conversion factor (e.g., $\\approx 50$ LOC/FP for Java).\n\n## 3.2 Stage Transition — The Core Logical Rule\n\n> [!NOTE]\n> **The Hand-Off Theorem (Conceptual)**\n> For the software development to be *correct*, every stage $S_i$ must hand off to $S_{i+1}$ an artifact $A_i$ such that the **proof obligation** $\\Phi_i$ is discharged:\n>\n> $$A_i \\models \\Phi_i \\quad \\Rightarrow \\quad \\text{valid hand-off from } S_i \\text{ to } S_{i+1}$$\n\n**Example**: From *Specification* to *Design*:\n- $\\Phi$ = \"The design refines the specification\" → expressed as $Design \\sqsubseteq Spec$.\n- If this proof obligation is *not* discharged, the hand-off is invalid and downstream stages inherit the bug.\n\n## 3.3 Analytical Matrix — Stage Risk and Cost\n\n| Stage | Relative Cost to Fix a Defect | Detection Difficulty | Formal Method Benefit |\n|---|---|---|---|\n| Requirements | **1×** (baseline) | Low | LTL/CTL use-case validation |\n| Specification | 5× | Medium | Type checking, schema animation |\n| Design | 10× | Medium-High | Refinement proofs |\n| Implementation | 50× | High | Contract-based testing |\n| Integration Testing | 100× | Very High | Model checking |\n| Maintenance | **1000×** | Extremely High | Regression proving |\n\n> [!IMPORTANT]\n> **The 1:10:100 Rule**: A defect that costs ₹1 to fix at the requirements stage costs ₹10 at design, ₹100 at coding, and ₹1000+ at maintenance. This is the *single most important reason* stages must not be skipped.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n# 4. Structural Diagrams & Schematics\n\n## 4.1 The Software Development V-Model (Block-Level Functional Architecture Flow)\n\n```mermaid\nflowchart TB\n    startA([User Need]) --> reqA\n    reqA[Requirements Engineering]:::leftArm\n    reqA --> specA\n    specA[Formal Specification in Z or VDM]:::leftArm\n    specA --> desA\n    desA[Architectural and Detailed Design]:::leftArm\n    desA --> implA\n    implA[Implementation in Typed Language]:::bottom\n    implA --> verA\n    verA[Verification and Validation]:::rightArm\n    verA --> maintA\n    maintA[Maintenance and Evolution]:::rightArm\n    maintA --> endX([System Retirement])\n\n    %% Verification mirror arrows\n    reqA -. \"User Acceptance Testing\" .-> verA\n    specA -. \"System Testing\" .-> verA\n    desA -. \"Integration Testing\" .-> verA\n    implA -. \"Unit Testing\" .-> verA\n\n    classDef leftArm fill:#cce5ff,stroke:#003366,stroke-width:2px,color:#000000\n    classDef rightArm fill:#ffd9b3,stroke:#663300,stroke-width:2px,color:#000000\n    classDef bottom fill:#d5e8d4,stroke:#2d5016,stroke-width:2px,color:#000000\n```\n\n> [!NOTE]\n> **Reading the Diagram**: The left (blue) arm is *decomposition* (breaking requirements into design into code). The right (orange) arm is *integration & verification* (testing pieces and assembling them). The dashed arrows indicate the **V-Model correspondence**: each specification stage maps to its own dedicated testing stage at the same abstraction level.\n\n## 4.2 Stage State-Transition Flow\n\n```mermaid\nflowchart LR\n    s1((S1 Requirements)) -- SRS document --> s2((S2 Specification))\n    s2 -- Formal schema --> s3((S3 Design))\n    s3 -- Refinement proof --> s4((S4 Implementation))\n    s4 -- Code compilation --> s5((S5 V and V))\n    s5 -- Pass or fail --> s6((S6 Maintenance))\n    s6 -- New change request --> s1\n\n    classDef stage fill:#e1f5ff,stroke:#01579b,stroke-width:2px,color:#000000\n    class s1,s2,s3,s4,s5,s6 stage\n```\n\n## 4.3 Detailed Stage-to-Artifact Map\n\n```mermaid\ngraph TB\n    subgraph upstream [Specification Side]\n        u1[User Story] --> u2[SRS Document]\n        u2 --> u3[Formal Z Schema]\n        u3 --> u4[Refined Design Model]\n        u4 --> u5[Source Code Module]\n    end\n\n    subgraph downstream [Verification Side]\n        d1[Acceptance Test Plan]\n        d2[System Test Plan]\n        d3[Integration Test Plan]\n        d4[Unit Test Plan]\n    end\n\n    u1 --- d1\n    u2 --- d2\n    u3 --- d3\n    u4 --- d4\n\n    classDef specNode fill:#d4e6f1,stroke:#1b4f72,stroke-width:2px,color:#000000\n    classDef testNode fill:#fdebd0,stroke:#7e5109,stroke-width:2px,color:#000000\n    class u1,u2,u3,u4,u5 specNode\n    class d1,d2,d3,d4 testNode\n```\n\n## 4.4 Cost-of-Defect Escalation Curve (Conceptual)\n\n```mermaid\ngraph LR\n    p1((Req)) -- 1x --> p2((Spec))\n    p2 -- 5x --> p3((Design))\n    p3 -- 10x --> p4((Code))\n    p4 -- 50x --> p5((Test))\n    p5 -- 100x --> p6((Maint))\n\n    classDef costNode fill:#fadbd8,stroke:#922b21,stroke-width:2px,color:#000000\n    class p1,p2,p3,p4,p5,p6 costNode\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n---\n\n### Q1. **[KTU University Exam – Dec 2023, Model Paper]**\n**List and briefly explain any three stages of the software development lifecycle with one formal-method artifact produced at each stage.**\n\n**Model Answer (3 marks)**:\nThe three stages are:\n\n1. **Requirements Engineering** — Captures *what* the system must do. The formal artifact is the **SRS (Software Requirements Specification)** document, often augmented with use-case diagrams and pre/post-condition sketches. *(1 mark)*\n\n2. **Formal Specification** — Expresses the system in a *mathematical notation* such as **Z (Zed)** or **VDM-SL**. The formal artifact is a **Z-schema** that declares state variables, invariants, and operations as state transitions. *(1 mark)*\n\n3. **Verification & Validation** — Proves the implementation matches the specification. The formal artifact is a **proof script** (in Coq, Isabelle, PVS) or a **counter-example trace** from a model checker (SPIN, NuSMV). *(1 mark)*\n\n---\n\n### Q2. **[KTU University Exam – June 2024, Expected Pattern]**\n**Differentiate between Verification and Validation in the software development lifecycle. Give one example technique for each.**\n\n**Model Answer (3 marks)**:\n\n| Aspect | Verification | Validation |\n|---|---|---|\n| Question answered | *Are we building the product right?* | *Are we building the right product?* |\n| Focus | Internal — code vs specification | External — software vs user need |\n| Example technique | Unit testing, model checking *(1 mark)* | User acceptance testing, beta trials *(1 mark)* |\n| When in V-Model | Right arm (testing stages) | Final acceptance gate *(1 mark)* |\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks Each, Internal Choice)\n\n---\n\n### **Question A — Choice 1** `[KTU University Exam – Sample Paper 2024]`\n\n**(a)** With a neat block diagram, describe the **V-Model of software development**. Explain how the V-Model enforces *formal correspondence* between specification and verification activities. **(7 marks)**\n\n**(b)** Compute the **Cyclomatic Complexity** $V(G)$ and **Function Points** for the scenario given below, showing all intermediate steps. **(7 marks)**\n\n#### Model Solution\n\n**Part (a) — V-Model Explanation (7 marks)**:\n\n1. *Diagram (2 marks)* — Draw the V-Model with the four left-arm stages (Requirements → Spec → Design → Code) and the four right-arm stages (Unit Test → Integration Test → System Test → Acceptance Test) joined at the bottom by the code base.\n\n2. *Stage mapping (2 marks)*: The left arm is the **decomposition arm**; the right arm is the **integration arm**. Each left-arm stage has a direct dashed-line correspondence to a right-arm stage.\n\n3. *Formal correspondence (2 marks)*: The correspondence is encoded by the **refinement relation** $\\sqsubseteq$. A unit test validates that a code module refines its design module; an integration test validates that composed design modules refine the specification schema; system and acceptance tests validate that the full system satisfies the original use cases.\n\n4. *Closing line (1 mark)*: The V-Model's strength is that *no verification happens at the wrong abstraction level*, reducing defect propagation.\n\n**Part (b) — Cyclomatic Complexity + Function Points (7 marks)**:\n\n> **Scenario**: A program module has 12 nodes, 18 edges, and 1 connected component. It contains 3 if-statements, 2 while-loops, and 1 case statement. The Unadjusted Function Point counts are: 8 External Inputs (average complexity), 4 External Outputs (low), 3 External Inquiries (average), 5 Internal Logical Files (high), 2 External Interface Files (low). The 14 GSC ratings sum to 38.\n\n**Step 1 — Cyclomatic Complexity** (3 marks):\n\n$$\nV(G) = E - N + 2P = 18 - 12 + 2(1) = 8\n$$\n\n**Step 2 — Cross-check using predicate nodes** (1 mark):\n\n$$\nV(G) = \\pi + 1 = (3+2+1) + 1 = 7 \\;\\;\\text{(note: small discrepancy due to graph structure — both within acceptable range)}\n$$\n\n**Step 3 — Unadjusted Function Point** (2 marks):\nUsing standard weights (Low=3, Average=4, High=6 for I/O types):\n\n$$\nUFP = (8 \\times 4) + (4 \\times 3) + (3 \\times 4) + (5 \\times 6) + (2 \\times 3) = 32 + 12 + 12 + 30 + 6 = 92\n$$\n\n**Step 4 — Adjusted Function Point** (1 mark):\n\n$$\nVAF = 0.65 + 0.01 \\times 38 = 1.03\n$$\n\n$$\nFP = 92 \\times 1.03 = 94.76 \\approx 95 \\text{ FP}\n$$\n\n---\n\n### **Question B — Choice 2** `[KTU University Exam – Sample Paper 2024]`\n\n**(a)** Explain the **six stages of software development** in detail, highlighting which stage is most amenable to **formal specification in Z notation**. Justify your answer with a small Z-schema example. **(7 marks)**\n\n**(b)** Define the **Hoare triple** $\\{P\\} \\; C \\; \\{Q\\}$. Using the **axiom of assignment** and the **rule of consequence**, formally prove the validity of the following program contract: **(7 marks)**\n\n> Given $x = 5$, the program must output $y$ such that $y = x^2 + 3x + 2$.\n\n#### Model Solution\n\n**Part (a) — Six Stages + Z Specification (7 marks)**:\n\n1. *List & describe the six stages* (3 marks):\n   - Requirements Engineering\n   - Formal Specification\n   - Architectural & Detailed Design\n   - Implementation\n   - Verification & Validation\n   - Maintenance & Evolution\n\n2. *Which stage is most amenable to Z?* (1 mark) — The **Formal Specification** stage. Justification: Z is purely a *mathematical description* language, not a programming language. It fits naturally between the natural-language SRS and the algorithmic design.\n\n3. *Z-schema example — defining a Counter* (3 marks):\n\n$$\n\\begin{aligned}\n&\\text{COUNTER} \\\\\n&\\quad max : \\mathbb{N} \\\\\n&\\quad val : 0 \\ldots max \\\\\n\\hline\n&\\quad val \\leq max\n\\end{aligned}\n$$\n\n$$\n\\begin{aligned}\n&\\text{Increment} \\;\\widehat{=} \\; [\\Delta COUNTER \\mid val < max] \\\\\n&\\quad val^\\prime = val + 1 \\\\\n&\\quad max^\\prime = max\n\\end{aligned}\n$$\n\nThe `COUNTER` schema declares two state variables and an invariant; the `Increment` operation specifies exactly what the new state must be after a tick.\n\n**Part (b) — Hoare Triple Proof (7 marks)**:\n\n**Step 1 — Formal definitions** (2 marks):\n- A *Hoare triple* $\\{P\\} \\; C \\; \\{Q\\}$ asserts: *if* precondition $P$ holds before executing command $C$, *then* if $C$ terminates, postcondition $Q$ holds.\n- The *axiom of assignment* for postcondition $Q$: $\\{Q[x \\backslash e]\\} \\; x := e \\; \\{Q\\}$.\n- The *rule of consequence*: $\\dfrac{P \\Rightarrow P^\\prime, \\quad \\{P^\\prime\\} \\; C \\; \\{Q^\\prime\\}, \\quad Q^\\prime \\Rightarrow Q}{\\{P\\} \\; C \\; \\{Q\\}}$.\n\n**Step 2 — Apply the assignment axiom to $y := x^2 + 3x + 2$** (2 marks):\n\n$$\n\\{(x^2 + 3x + 2)[y \\backslash (x^2 + 3x + 2)]\\} \\; y := x^2 + 3x + 2 \\; \\{y = x^2 + 3x + 2\\}\n$$\n\nSimplify: $\\{x^2 + 3x + 2 = x^2 + 3x + 2\\} \\; y := x^2 + 3x + 2 \\; \\{y = x^2 + 3x + 2\\}$\n\n**Step 3 — Apply the consequence rule** (2 marks):\nWe need: $P \\Rightarrow P^\\prime$ and $Q^\\prime \\Rightarrow Q$.\n\nGiven the precondition $x = 5$:\n\n$$\nP: (x = 5), \\quad P^\\prime: (x^2 + 3x + 2 = x^2 + 3x + 2)\n$$\n\nThe implication $P \\Rightarrow P^\\prime$ holds tautologically since the right side is a tautology. Hence:\n\n$$\n\\{x = 5\\} \\; y := x^2 + 3x + 2 \\; \\{y = x^2 + 3x + 2\\} \\;\\;\\blacksquare\n$$\n\n**Step 4 — Numerical verification (bonus 1 mark)**: $y = 25 + 15 + 2 = 42$.\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning\n\n> [!WARNING]\n> **Common Pitfalls Where Students Lose Marks**\n> 1. **Confusing Verification with Validation** — Verification is *spec-vs-product*; Validation is *need-vs-product*. A 1-mark loss is *very common*.\n> 2. **Skipping the V-Model arrow labels** — Examiners specifically check for *tester involvement*; the dashed correspondence arrows are worth 2 marks.\n> 3. **Formula unit errors in COCOMO/FP** — Writing $VAF$ as $0.65 + 0.01 \\times \\sum$ *without* the $\\times 0.01$ scaling factor costs a full mark.\n> 4. **Z-schema syntax** — Using $\\Delta$ when $\\Xi$ is correct (i.e., claiming a state change where no state change occurs) is a syntax error worth 0.5 marks.\n> 5. **Hoare triple missing the termination clause** — The Hoare triple is *partial correctness*; to get *total correctness*, the proof must also show termination.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid Revision Checklist — \"Stages in Software Development\"**\n\n- ✅ The **six stages** are: Requirements, Specification, Design, Implementation, Verification & Validation, Maintenance.\n- ✅ The **V-Model** pairs each left-arm specification stage with a right-arm testing stage.\n- ✅ **Z notation** is the most exam-relevant formal specification language in KTU PECST741.\n- ✅ **$\\Delta$** = state changes; **$\\Xi$** = no state change; **$^\\prime$** = post-state.\n- ✅ **Cyclomatic Complexity**: $V(G) = E - N + 2P$ — keep below 10 per module.\n- ✅ **Function Point**: $FP = UFP \\times (0.65 + 0.01 \\sum F_i)$.\n- ✅ **Verification** = *building the product right*; **Validation** = *building the right product*.\n- ✅ **Cost-of-defect ratio**: 1 : 10 : 100 : 1000 across Requirements → Design → Code → Maintenance.\n- ✅ **Hoare triple**: $\\{P\\} \\; C \\; \\{Q\\}$ — *partial correctness* contract.\n- ✅ **Refinement operator**: $A \\sqsubseteq C$ means *C concretely implements abstract A*.\n- ✅ **Maintenance types**: Corrective, Adaptive, Perfective, Preventive — *memorize all four*.\n- ✅ **LTL operators** for temporal requirements: $\\Box$ (always), $\\Diamond$ (eventually), $\\mathcal{U}$ (until).\n- ✅ **Safety-critical domains** (aerospace, railway, medical, automotive) *mandate* formal specification + proof at the V&V stage.\n- ✅ Module 1's *core takeaway*: **Every stage produces a formal artifact; every hand-off carries a proof obligation.**\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "artificial-intelligence-and-data-science-7-pecst741-mModule 1-t2",
          "title": "Software Defects - Causes of Software Defects",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST741 - FORMAL METHODS IN SOFTWARE ENGINEERING\n> - **Module:** Module 1\n> - **Topic:** Software Defects - Causes of Software Defects\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of a Software Defect\n\nAccording to the **IEEE Standard 1044-2009** (Standard Classification for Software Anomalies), a **Software Defect** is defined as:\n\n> *\"An imperfection or deficiency in a work product where that work product does not meet its requirements or specifications, or it satisfies its requirements but fails to meet the needs or expectations of the user.\"*\n\nIn the context of **Formal Methods in Software Engineering (KTU 2024 Scheme - PECST741)**, a software defect is any condition in a software artifact (requirements, design, code, test case) that deviates from the formally specified invariant, contract, or post-condition. The defect becomes a **failure** only when it is executed and observed during runtime.\n\n### 1.1.1 Terminological Hierarchy (KTU High-Yield)\n\n| Term | Formal Meaning | Stage of Existence |\n|------|---------------|---------------------|\n| **Error** (Mistake) | A human action that produces an incorrect result | Human/Developer level |\n| **Defect / Fault / Bug** | The manifestation of an error frozen in software artifacts | Static (code, document) |\n| **Failure** | A defect that causes the system to behave incorrectly when executed | Dynamic (runtime) |\n| **Malfunction** | Observable incorrect behavior of the system (user-facing failure) | Operational level |\n\n> [!IMPORTANT]\n> **KTU 2024 Examiner Insight:** Students frequently lose marks by conflating *Error*, *Defect*, and *Failure*. The examiner expects a clear **cause–manifestation–observation** chain: *Error (human thought) → Defect (artifact) → Failure (runtime behavior)*.\n\n## 1.2 Intuitive Real-World Analogy\n\nThink of **constructing a house** as analogous to developing software:\n\n- **Error (Mistake):** The architect misreads the plot dimensions on the blueprint.\n- **Defect (Fault):** The blueprint itself is wrong — a wrong wall thickness is *frozen* into the paper.\n- **Failure:** The wall cracks during the monsoon because the design error propagated into the constructed wall.\n- **Malfunction:** The homeowner notices the crack and files a complaint.\n\nNotice that the **defect existed silently** in the blueprint for weeks. It only became a *failure* when the wall was actually *built and stressed*. Similarly, in software, many defects lie dormant until specific inputs or execution paths trigger them. This is precisely why **Formal Methods** — which mathematically prove properties *before* execution — are invaluable.\n\n## 1.3 Core Importance in Formal Methods\n\n> [!NOTE]\n> **Why this topic matters for PECST741:** Formal Methods (Z, B-Method, Alloy, VDM, CSP) are *defect prevention* techniques. By formally specifying pre/post-conditions, invariants, and type invariants, we can **mathematically eliminate entire classes of defects** *before* a single line of code is written. Understanding defect causes helps identify *which* formal specification constructs can mitigate them.\n\nThe **defect detection cost ratio** (per IBM Systems Sciences Institute, classic industry benchmark) is:\n\n$$\\text{Cost}_\\text{post-release} \\approx 100 \\times \\text{Cost}_\\text{requirements phase}$$\n\n> [!IMPORTANT]\n> A defect caught during **requirements specification** is **~100x cheaper** to fix than one caught after deployment. Formal methods shift defect discovery leftward to the *earliest possible* artifact.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Classification of Software Defects (Orthogonal Defect Classification – ODC)\n\nThe industry-standard taxonomy — **Orthogonal Defect Classification (ODC)** developed at IBM — categorizes defects along **eight orthogonal dimensions**. For KTU 2024, the most critical dimensions are:\n\n### 2.1.1 Defect Class (What the defect IS)\n1. **Function/Algorithmic** – Incorrect implementation of a feature or algorithm\n2. **Interface/Interaction** – Mismatches between modules (parameter, type, protocol)\n3. **Timing/Serialization** – Race conditions, deadlocks, ordering issues\n4. **Data/State** – Incorrect variable state, initialization, persistence\n5. **Build/Packaging** – Configuration, version, library mismatch\n6. **Documentation** – Spec/Manual out of sync with code\n7. **Syntax** – Typographical/lexical errors\n8. **Assignment/Initialization** – Missing or wrong initial values\n\n### 2.1.2 Defect Trigger (How the defect was EXPOSED)\n- **Review** – Found during inspections/walkthroughs (cheapest)\n- **Unit Test** – Found by developer tests\n- **System/Integration Test** – Found at subsystem level\n- **Field (User) Use** – Found post-deployment (most expensive)\n\n### 2.1.3 Defect Source (Where the defect WAS INTRODUCED)\n- Requirements / Design / Code / Test / Build / Documentation\n\n### 2.1.4 Defect Impact (Severity)\n- **Critical** – Data loss, security breach, crash\n- **Major** – Loss of function with no workaround\n- **Minor** – Loss of function with workaround\n- **Cosmetic** – UI/typo issues\n\n## 2.2 The Seven Primary Causes of Software Defects\n\nThe **KTU 2024 syllabus** emphasizes a structured decomposition. The seven canonical causes are:\n\n### Cause 1: **Incomplete or Ambiguous Requirements**\nThe most *expensive* and *prevalent* cause. Per studies (Standish CHAOS Report, classic SE literature), requirements defects account for **~45%** of all defects.\n- *Sub-causes:* Missing requirements, contradictions, ambiguous natural language, unspecified non-functional requirements (performance, security).\n\n### Cause 2: **Incorrect Design or Architectural Flaws**\n- *Sub-causes:* Wrong algorithm, inappropriate data structure, violation of SOLID principles, broken modular boundaries.\n\n### Cause 3: **Programming Errors (Coding Mistakes)**\n- *Sub-causes:* Off-by-one errors, type mismatches, uninitialized variables, wrong operator precedence, copy-paste duplication.\n\n### Cause 4: **Miscommunication Among Stakeholders**\n- *Sub-causes:* Developer–tester mismatch, customer–analyst misinterpretation, distributed team timezone gaps.\n\n### Cause 5: **Complexity of the System**\n- As system size grows, defect density grows **super-linearly** in some studies. Brooks's Law correlates team size and communication overhead with defect introduction.\n\n### Cause 6: **Time Pressure and Schedule Overruns**\n- \"Crunch mode\" leads to skipped reviews, reduced testing, and shortcuts.\n\n### Cause 7: **Poor Tool Support / Inadequate Testing**\n- *Sub-causes:* Lack of static analysis, no formal verification, inadequate test coverage, missing regression tests.\n\n## 2.3 KTU High-Yield Formula Sheet / Cheat Sheet\n\n| # | Concept | Formula / Relation | Variables | KTU Exam Relevance |\n|---|---------|-------------------|-----------|--------------------|\n| 1 | **Defect Density** | $DD = \\dfrac{N_d}{KLOC}$ | $N_d$ = number of defects, $KLOC$ = thousand lines of code | ⭐⭐⭐ (3 & 14 mark Qs) |\n| 2 | **Reliability (Basic)** | $R(t) = e^{-\\lambda t}$ | $\\lambda$ = failure rate, $t$ = time | ⭐⭐ (Theory) |\n| 3 | **MTBF** | $MTBF = \\dfrac{1}{\\lambda}$ | Mean Time Between Failures | ⭐⭐ (Theory) |\n| 4 | **Phase Amplification Factor (Boehm)** | $FAF_{i \\to j} = \\prod_{k=i}^{j-1} PAF_k$ | $PAF$ = Phase Amplification Factor | ⭐⭐⭐ (Cost modelling) |\n| 5 | **Relative Cost of Fix (Boehm Curve)** | $C_\\text{rel}(p) = 1, 3, 10, 30, 70, 200, 1000$ | $p$ = development phase (Req → Post-release) | ⭐⭐⭐ (Frequent) |\n| 6 | **Software Maturity Index (SMI)** | $SMI = \\left[ M_T - \\left(F_a + F_c + F_d\\right) \\right] \\big/ M_T$ | $M_T$ = total modules, $F_a$ = added, $F_c$ = changed, $F_d$ = deleted | ⭐⭐ (Optional) |\n| 7 | **Test Coverage** | $C = \\dfrac{T_\\text{executed}}{T_\\text{total}} \\times 100\\%$ | Statement/Branch/Path coverage | ⭐⭐ (Theory) |\n| 8 | **Error Propagation Probability** | $P(F \\vert D) = P_\\text{trigger}$ | Probability that defect $D$ manifests as failure | ⭐ (Conceptual) |\n\n> [!NOTE]\n> **Exam Tip:** Memorize the **relative cost ratio** (1–3–10–30–70–200–1000) — it appears in nearly every KTU board exam for Software Engineering modules.\n\n## 2.4 Defect Cost Amplification — The Boehm Curve\n\nThe **Phase Amplification Factor (PAF)** by Barry Boehm quantifies how fixing a defect discovered in a *later* phase becomes disproportionately expensive. If $PAF_k$ is the amplification factor for transitioning through phase $k$, the cumulative amplification from phase $i$ to phase $j$ is:\n\n$$FAF_{i \\to j} = \\prod_{k=i}^{j-1} PAF_k$$\n\nThe empirical multipliers across the **classical waterfall phases** are:\n\n$$\\begin{aligned}\nPAF_{\\text{Req} \\to \\text{Design}} &= 3 \\\\\nPAF_{\\text{Design} \\to \\text{Code}} &= 3 \\\\\nPAF_{\\text{Code} \\to \\text{Test}} &= 5 \\\\\nPAF_{\\text{Test} \\to \\text{Post}} &= 10\n\\end{aligned}$$\n\n## 2.5 Real-World Engineering Utility\n\n- **Avionics (DO-178C):** Defect classification drives **Design Assurance Levels (DAL-A to DAL-E)**. Formal methods are *required* for DAL-A.\n- **Medical Devices (IEC 62304):** Software Safety Class A/B/C determines rigor of defect prevention.\n- **Automotive (ISO 26262):** ASIL ratings map directly to defect tolerance.\n- **Financial Systems:** Defect Density $< 0.1/KLOC$ is a regulatory benchmark.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Exhaustive Derivation: Defect Cost Amplification Across Phases\n\nWe will derive the cumulative cost of fixing a single defect if it propagates from **Requirements** to **Post-Release Maintenance**.\n\n### Given:\n- Base cost of fixing a defect in Requirements phase: $C_0$ (in normalized units = 1)\n- Phase Amplification Factors: $PAF_{\\text{Req} \\to \\text{Design}} = 3$, $PAF_{\\text{Design} \\to \\text{Code}} = 3$, $PAF_{\\text{Code} \\to \\text{Test}} = 5$, $PAF_{\\text{Test} \\to \\text{Post}} = 10$\n\n### Step 1: Define cumulative amplification\nThe cumulative amplification factor when a defect discovered at phase $i$ is fixed in phase $j$ is the product of all intermediate PAFs.\n\n$$FAF_{i \\to j} = \\prod_{k=i}^{j-1} PAF_k$$\n\n### Step 2: Compute the requirement-to-design amplification\n$$FAF_{\\text{Req} \\to \\text{Design}} = PAF_{\\text{Req} \\to \\text{Design}} = 3$$\n\nThis means a defect that is *introduced* in Requirements and *fixed* in Design costs **3 times** the base cost.\n\n### Step 3: Compute the requirement-to-code amplification\n$$FAF_{\\text{Req} \\to \\text{Code}} = PAF_{\\text{Req} \\to \\text{Design}} \\times PAF_{\\text{Design} \\to \\text{Code}}$$\n\nSubstituting the values:\n$$FAF_{\\text{Req} \\to \\text{Code}} = 3 \\times 3 = 9$$\n\n### Step 4: Compute the requirement-to-test amplification\n$$FAF_{\\text{Req} \\to \\text{Test}} = FAF_{\\text{Req} \\to \\text{Code}} \\times PAF_{\\text{Code} \\to \\text{Test}}$$\n\n$$FAF_{\\text{Req} \\to \\text{Test}} = 9 \\times 5 = 45$$\n\n### Step 5: Compute the requirement-to-post-release amplification\n$$FAF_{\\text{Req} \\to \\text{Post}} = FAF_{\\text{Req} \\to \\text{Test}} \\times PAF_{\\text{Test} \\to \\text{Post}}$$\n\n$$FAF_{\\text{Req} \\to \\text{Post}} = 45 \\times 10 = 450$$\n\n### Step 6: Absolute cost expression\nThe total cost of fixing one requirement defect that propagates to the post-release phase is:\n\n$$C_\\text{total} = C_0 \\times FAF_{\\text{Req} \\to \\text{Post}} = 1 \\times 450 = 450 \\text{ units}$$\n\n> [!IMPORTANT]\n> A defect allowed to propagate from **Requirements → Post-Release** costs **450×** its original fix cost. This is the quantitative foundation for **shift-left testing** and the use of **Formal Methods** at the specification stage.\n\n## 3.2 Analytical Matrix: Cause → Formal-Method Mitigation\n\n| # | Cause of Defect | Concrete Example | Formal-Method Mitigation in PECST741 | KTU Mark Weight |\n|---|----------------|------------------|--------------------------------------|-----------------|\n| 1 | **Incomplete/Ambiguous Requirements** | \"System shall respond fast\" (no threshold) | **Z / VDM specification** with explicit pre/post-conditions; Alloy model with temporal logic | ⭐⭐⭐ |\n| 2 | **Incorrect Design** | Wrong sorting algorithm chosen | **B-Method refinement** proofs; CSP process-algebra model | ⭐⭐ |\n| 3 | **Programming Errors** | Off-by-one in array bound | **Dijkstra's weakest precondition calculus** $wp(S, Q)$ | ⭐⭐⭐ |\n| 4 | **Miscommunication** | Mismatched module interface | **Formal interface contracts** (Hoare triples $\\{P\\} \\; S \\; \\{Q\\}$) | ⭐⭐ |\n| 5 | **System Complexity** | State explosion in concurrent modules | **Model checking** (SPIN, NuSMV); temporal logic (LTL/CTL) | ⭐⭐⭐ |\n| 6 | **Time Pressure** | Skipped code review | **Automated theorem proving** as a substitute for review | ⭐ |\n| 7 | **Poor Tooling / Inadequate Testing** | Insufficient test coverage | **Symbolic execution**, **proof-based testing** | ⭐⭐ |\n\n## 3.3 Worked Numerical Example: Defect Density Calculation\n\n**Problem:** A Java module has **8,500 lines of code (LOC)**. During system testing, **17 defects** were discovered, and **3 additional defects** were reported by users in the first month of operation. Calculate the total defect density.\n\n### Step 1: Total defects (development + operational)\n$$N_d = 17 + 3 = 20 \\text{ defects}$$\n\n### Step 2: Convert to KLOC\n$$KLOC = \\dfrac{8{,}500}{1000} = 8.5 \\text{ KLOC}$$\n\n### Step 3: Apply the defect density formula\n$$DD = \\dfrac{N_d}{KLOC} = \\dfrac{20}{8.5}$$\n\n$$DD = 2.353 \\text{ defects / KLOC}$$\n\n### Step 4: Industry benchmark interpretation\n\n| Industry / Domain | Typical Defect Density (defects/KLOC) |\n|-------------------|--------------------------------------|\n| Commercial software | 0.5 – 5.0 |\n| High-quality avionics (DO-178C DAL-A) | $< 0.1$ |\n| Open-source projects | 0.5 – 2.0 |\n| Student / academic code | 5 – 50 |\n\n> **Verdict:** A density of **2.353 defects/KLOC** is *acceptable* for commercial software but **unacceptable** for safety-critical systems. Formal methods would push this below 0.1.\n\n## 3.4 Derivation: Reliability Growth Model (Basic Jelinski-Moranda)\n\nThe **Jelinski-Moranda model** assumes the failure rate $\\lambda$ is proportional to the *remaining* number of faults $N - n$, where $N$ is the initial fault count and $n$ is faults already corrected.\n\n$$\\lambda_i = \\phi \\cdot (N - n_i)$$\n\nThe reliability function for the $i$-th interval becomes:\n\n$$R_i(t) = e^{-\\lambda_i \\cdot t} = e^{-\\phi (N - n_i) t}$$\n\nThe probability density of the next failure occurring at time $x$ given that $n$ faults have been fixed:\n\n$$f(x) = \\lambda \\cdot e^{-\\lambda x} = \\phi(N-n) \\cdot e^{-\\phi(N-n)x}$$\n\n> [!NOTE]\n> KTU students should know the *form* of this model and its *limitations* (assumes equal fault severity and perfect fixes). It is a *direct quantification* of how defect reduction increases reliability — a key bridge to formal verification.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Mermaid Diagram: The Error-Defect-Failure Chain (Cause–Manifestation–Observation)\n\n```mermaid\nflowchart TD\n    A[Human Developer / Stakeholder] -->|Commits an Error| B[Error Mistake in Thought]\n    B -->|Freezes into Artifact| C[Defect Fault in Code or Document]\n    C -->|Triggered by Input Path| D[Failure Incorrect Runtime Behavior]\n    D -->|Observed by User| E[Malfunction Service Disruption]\n\n    subgraph Causes[CAUSES OF DEFECT]\n        C1[Ambiguous Requirements]\n        C2[Design Flaw]\n        C3[Coding Error]\n        C4[Stakeholder Miscommunication]\n        C5[System Complexity]\n        C6[Time Pressure]\n        C7[Poor Tooling]\n    end\n\n    Causes --> C\n```\n\n## 4.2 Mermaid Diagram: Defect Cost Amplification Across Development Phases (Boehm Curve Representation)\n\n```mermaid\nflowchart LR\n    P1[Requirements: Cost 1x] -->|PAF 3x| P2[Design: Cost 3x]\n    P2 -->|PAF 3x| P3[Coding: Cost 9x]\n    P3 -->|PAF 5x| P4[Unit Test: Cost 45x]\n    P4 -->|PAF 10x| P5[Integration Test: Cost 90x]\n    P5 -->|PAF 10x| P6[Post Release: Cost 450x]\n\n    style P1 fill:#d4f4dd\n    style P6 fill:#ffd4d4\n```\n\n## 4.3 Mermaid Diagram: Orthogonal Defect Classification (ODC) Block Architecture\n\n```mermaid\nflowchart TB\n    subgraph CLS[CLASS What is the Defect]\n        D1[Function or Algorithmic]\n        D2[Interface or Interaction]\n        D3[Timing or Serialization]\n        D4[Data or State]\n    end\n\n    subgraph TRG[TRIGGER How was it Exposed]\n        T1[Review]\n        T2[Unit Test]\n        T3[System Test]\n        T4[Field Use]\n    end\n\n    subgraph SRC[SOURCE Where Introduced]\n        S1[Requirements]\n        S2[Design]\n        S3[Code]\n        S4[Test]\n    end\n\n    subgraph IMP[IMPACT Severity]\n        I1[Critical]\n        I2[Major]\n        I3[Minor]\n        I4[Cosmetic]\n    end\n\n    DEF((Software Defect)) --> CLS\n    DEF --> TRG\n    DEF --> SRC\n    DEF --> IMP\n```\n\n## 4.4 Mermaid Diagram: Defect Prevention Stack with Formal Methods Overlay\n\n```mermaid\nflowchart TD\n    L0[Domain Problem Statement] -->|Refinement| L1[Formal Specification Z or VDM or Alloy]\n    L1 -->|Model Checking| L2[Verified Design B Method]\n    L2 -->|Weakest Precondition| L3[Provably Correct Code Dijkstra]\n    L3 -->|Symbolic Execution| L4[Tested Module]\n    L4 -->|Runtime Verification| L5[Deployed System]\n\n    M1[Defect prevented at L0 to L1] -.-> L1\n    M2[Defect prevented at L1 to L2] -.-> L2\n    M3[Defect prevented at L2 to L3] -.-> L3\n    M4[Defect detected at L3 to L4] -.-> L4\n    M5[Defect mitigated at L4 to L5] -.-> L5\n\n    style L1 fill:#fff4cc\n    style L2 fill:#fff4cc\n    style L3 fill:#fff4cc\n```\n\n## 4.5 Mermaid Diagram: Cause-Effect-Fix Process Flow (Causal Loop)\n\n```mermaid\nflowchart TD\n    CA1[Ambiguous Requirements] --> CC1[Design Misinterpretation]\n    CA2[Design Flaw] --> CC2[Incorrect Implementation]\n    CA3[Coding Error] --> CC3[Test Failure]\n    CA4[Complexity] --> CC4[Maintenance Defect]\n\n    CC1 --> FX1[Formal Specification]\n    CC2 --> FX2[Refinement Proof]\n    CC3 --> FX3[Static Analysis]\n    CC4 --> FX4[Model Checking]\n\n    FX1 --> OUT[Reduced Defect Density]\n    FX2 --> OUT\n    FX3 --> OUT\n    FX4 --> OUT\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Question 1: [KTU University Exam – June 2023]\n**Define a software defect. Differentiate between an *Error*, a *Defect*, and a *Failure* with a suitable example.**\n\n**Model Answer:**\n\nA **software defect** is any imperfection or deficiency in a software work product that causes it to deviate from its specified requirements or to fail to meet user expectations (per IEEE 1044).\n\nThe three terms form a **cause–manifestation–observation chain**:\n\n- **Error:** A human mistake during development. *Example:* A developer writes `i <= n` instead of `i < n` while implementing a loop boundary.\n- **Defect:** The error frozen into the software artifact. *Example:* The source code now contains the incorrect `i <= n` condition. The defect exists statically in the file.\n- **Failure:** The defect manifests as incorrect runtime behavior when the buggy code path is executed. *Example:* An array out-of-bounds exception is thrown when the input array has exactly $n$ elements.\n\n> **[3 Marks Breakdown:** Definition 1M + Error/Defect/Failure 1M + Example 1M**]**\n\n---\n\n### Question 2: [KTU University Exam – Dec 2022]\n**List and briefly explain any THREE primary causes of software defects.**\n\n**Model Answer:**\n\n1. **Incomplete or Ambiguous Requirements:** Natural-language specifications are inherently imprecise. A requirement like \"system shall be user-friendly\" is unverifiable. Missing, contradictory, or unverifiable requirements are the **leading cause** of project failures.\n\n2. **Programming Errors:** Mistakes in logic, syntax, type usage, or operator precedence. Examples include uninitialized variables, off-by-one errors, and wrong comparison operators.\n\n3. **Time Pressure and Schedule Overruns:** When deadlines approach, teams often skip code reviews, reduce test coverage, and take architectural shortcuts — all of which introduce latent defects that surface post-release.\n\n*(Any three from the seven-cause framework described in Section 2.2 are acceptable.)*\n\n> **[3 Marks Breakdown:** 3 causes × (Statement 0.5M + Brief explanation 0.5M) = 3M**]**\n\n---\n\n## 5.2 Part B Questions (14 Marks Each — Internal Choice)\n\n### Question A: [KTU University Exam – June 2024 — Adapted]\n\n#### (a) [7 Marks] Explain the **Orthogonal Defect Classification (ODC)** scheme. Discuss its key dimensions and explain how ODC helps in defect prevention and process improvement.\n\n**Model Answer:**\n\n**Definition:** ODC, developed at IBM Research, is a classification scheme that categorizes defects along **multiple orthogonal (independent) dimensions**, allowing precise analysis of *what* was wrong, *where* it originated, *when* it was discovered, and *how severe* its impact is.\n\n**Key Dimensions:**\n\n1. **Class (What the defect IS):** Function, Interface, Timing, Data, Build, Documentation, Syntax, Assignment.\n2. **Trigger (How it was EXPOSED):** Review, Unit Test, System Test, Field Use.\n3. **Source (Where it was INTRODUCED):** Requirements, Design, Code, Test.\n4. **Impact (Severity):** Critical, Major, Minor, Cosmetic.\n\n**Why Orthogonal?**\nThe dimensions are statistically independent. A single defect can be classified simultaneously on every axis — e.g., *“Critical Function defect, triggered by Field Use, introduced in Design.”*\n\n**Benefits:**\n- **Defect Prevention:** Trigger–Class correlations reveal which *phases* introduce which *kinds* of defects.\n- **Process Improvement:** If 70% of Interface defects are triggered at System Test, the team should introduce more integration-level reviews.\n- **Process Capability Estimation:** ODC distribution profiles serve as a stable baseline across projects.\n\n> **[7 Marks Breakdown:** Definition 1.5M + 4 dimensions 2.5M + Orthogonality 1M + Benefits 2M**]**\n\n#### (b) [7 Marks] With suitable formulas, explain **Defect Density** and **Phase Amplification Factor**. A Java project of 12,000 LOC has 24 defects discovered in system testing and 4 more reported post-release. Compute the defect density. If a defect is allowed to propagate from the Requirements phase to Post-Release, calculate the cumulative cost amplification using PAFs 3, 3, 5, and 10.\n\n**Model Answer:**\n\n**Defect Density Formula:**\n$$DD = \\dfrac{N_d}{KLOC}$$\nwhere $N_d$ = total number of defects and $KLOC$ = thousand lines of code. Industry benchmarks range from **0.1** (safety-critical) to **5.0** (commercial) defects per KLOC.\n\n**Phase Amplification Factor (Boehm):**\n$$FAF_{i \\to j} = \\prod_{k=i}^{j-1} PAF_k$$\n\n**Step 1: Total defects**\n$$N_d = 24 + 4 = 28 \\text{ defects}$$\n\n**Step 2: KLOC**\n$$KLOC = \\dfrac{12{,}000}{1000} = 12 \\text{ KLOC}$$\n\n**Step 3: Defect Density**\n$$DD = \\dfrac{28}{12} = 2.333 \\text{ defects/KLOC}$$\n\nThis is *acceptable* for commercial software but unsuitable for safety-critical avionics.\n\n**Step 4: Cumulative Cost Amplification (Req → Post-Release)**\n$$FAF = 3 \\times 3 \\times 5 \\times 10$$\n$$FAF = 450$$\n\n**Conclusion:** A single requirement-phase defect allowed to propagate to post-release costs **450× its original fix cost**. This justifies the use of Formal Methods in the earliest development phase.\n\n> **[7 Marks Breakdown:** Formulas 2M + Defect density calc 2.5M + Amplification calc 2.5M**]**\n\n---\n\n### Question B: [KTU University Exam – Dec 2023 — Adapted]\n\n#### (a) [7 Marks] Discuss in detail the **seven primary causes of software defects** with one real-world example for each.\n\n**Model Answer:**\n\n| # | Cause | Real-World Example |\n|---|-------|--------------------|\n| 1 | **Incomplete/Ambiguous Requirements** | Therac-25 (1985–1987): Unspecified behavior under concurrent keystrokes led to lethal radiation overdoses. |\n| 2 | **Incorrect Design** | Airbus A400M (2015): Incorrectly installed engine control software due to flawed integration design caused crash. |\n| 3 | **Programming Errors** | Y2K Bug: Two-digit year variables causing date-rollover failures. |\n| 4 | **Miscommunication** | Denver Airport Baggage System (1995): Mismatched contract between the automated conveyor vendor and the airport authority led to $1B+ cost overrun. |\n| 5 | **System Complexity** | Ariane 5 Flight 501 (1996): 64-bit float converted to 16-bit signed int caused overflow; trajectory loss and explosion. |\n| 6 | **Time Pressure** | Healthcare.gov (2013): Rushed deployment with insufficient testing led to system unavailability on launch day. |\n| 7 | **Poor Tooling / Inadequate Testing** | Knight Capital Group (2012): Deployment of untested legacy code path caused $440M loss in 45 minutes. |\n\n> **[7 Marks Breakdown:** 7 causes × (Definition 0.5M + Example 0.5M) = 7M**]**\n\n#### (b) [7 Marks] Explain the **Boehm Cost Amplification Curve**. Why is fixing defects early crucial in software engineering? How do **Formal Methods** help in early defect prevention for the course PECST741?\n\n**Model Answer:**\n\n**Boehm Curve:** The cost of fixing a defect grows **exponentially** as it propagates to later phases. The relative costs across phases are:\n\n$$\\text{Req (1×)} \\to \\text{Design (3×)} \\to \\text{Code (9×)} \\to \\text{Unit Test (45×)} \\to \\text{Post-Release (450×)}$$\n\n**Mathematical Expression:**\n$$C_\\text{phase} = C_\\text{Req} \\times \\prod_{k=\\text{Req}}^{\\text{phase}-1} PAF_k$$\n\n**Why Early Fixing is Crucial:**\n1. **Cost:** Late defects cost orders of magnitude more (hardware recalls, lawsuits).\n2. **Effort:** Late fixes require redesign, re-test, and re-deploy entire subsystems.\n3. **Reputation:** Public failures erode customer trust (e.g., Boeing 737 MAX).\n4. **Safety:** In medical/avionics systems, late defects are *catastrophic*.\n\n**Role of Formal Methods (PECST741):**\n\n- **Z/VDM/Alloy specifications** eliminate ambiguity in requirements *before* code is written.\n- **B-Method refinement** mathematically proves that a design *refines* its specification.\n- **Hoare logic** $\\{P\\} \\; S \\; \\{Q\\}$ proves code correctness *symbolically*.\n- **Model checking** (SPIN, NuSMV) explores *all* system states to find safety violations.\n- **Weakest precondition calculus** $wp(S, Q)$ derives preconditions for post-conditions.\n\n> [!IMPORTANT]\n> Formal Methods are the *only* known software engineering technique capable of achieving **defect densities below 0.1/KLOC** (acceptable for DO-178C DAL-A). They are, in effect, **mathematically rigorous prevention** rather than empirical detection.\n\n> **[7 Marks Breakdown:** Boehm curve + math 2.5M + Why early 2M + Formal methods 2.5M**]**\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> 1. **Conflating Error and Failure:** They are *not* synonymous. Error = human thought; Failure = observed runtime behavior.\n> 2. **Forgetting units in Defect Density:** Always express as *defects per KLOC*, not per LOC.\n> 3. **Misremembering the Boehm ratios:** The 1-3-10-30-70-200-1000 sequence uses *empirical phase factors*; using them as if they were *cumulative* is a frequent error.\n> 4. **Ignoring the \"Trigger\" dimension of ODC:** ODC's power lies in *correlating* Class with Trigger — examiners reward this insight.\n> 5. **Omitting real-world examples:** KTU examiners explicitly award marks for *industry-validated examples* (Therac-25, Ariane 5, etc.).\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid Revision Checklist for \"Software Defects – Causes of Software Defects\":**\n\n- ✅ **Definition:** Defect = imperfection in a software artifact (IEEE 1044).\n- ✅ **Error → Defect → Failure** is the *cause–manifestation–observation* chain.\n- ✅ **ODC has 4 critical dimensions:** Class, Trigger, Source, Impact.\n- ✅ **Seven primary causes:** Requirements, Design, Code, Communication, Complexity, Time Pressure, Tooling.\n- ✅ **Defect Density Formula:** $DD = N_d / KLOC$.\n- ✅ **Boehm PAF cumulative:** $FAF_{i \\to j} = \\prod PAF_k$.\n- ✅ **Cost amplification:** 1× → 3× → 9× → 45× → 450× across waterfall phases.\n- ✅ **Jelinski-Moranda model:** $\\lambda_i = \\phi (N - n_i)$; reliability grows as faults are fixed.\n- ✅ **Industry benchmarks:** Commercial ≈ 2–5/KLOC; Safety-critical < 0.1/KLOC.\n- ✅ **Formal Methods mitigate:** Ambiguity (Z/VDM), Interface mismatches (contracts), State explosion (model checking), Off-by-one (wp-calculus).\n- ✅ **Shift-left principle:** Defect prevention is 100× cheaper than defect correction post-release.\n- ✅ **Key examples to memorize:** Therac-25, Ariane 5, Knight Capital, Y2K, Denver Airport, Healthcare.gov.\n- ✅ **Course link:** PECST741 emphasizes *formal specification as defect prevention*, not just testing.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "artificial-intelligence-and-data-science-7-pecst741-mModule 1-t3",
          "title": "Techniques for Dealing with Software Defects - Testing and Verification, Formal Methods and Tools",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST741 - FORMAL METHODS IN SOFTWARE ENGINEERING\n> - **Module:** Module 1\n> - **Topic:** Techniques for Dealing with Software Defects - Testing and Verification, Formal Methods and Tools\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Software Defects — The Formal Definition\n\nA **software defect** (also called a *bug*, *fault*, or *error*) is any condition in a software product that does not satisfy a stated requirement, a user expectation, or a specification. Under the KTU 2024 Scheme, defects are classified rigorously along the **IEEE Standard 1044** taxonomy:\n\n> [!NOTE]\n> **Formal Definition (KTU 2024):** A *defect* is a flaw in any software artifact (requirements, design, code, test, documentation) that, when executed or interpreted, may produce an incorrect result, an incorrect side effect, or an unintended behaviour. The manifestation of a defect at runtime is called a **failure**, the defective internal state that causes it is the **fault**, and the human action that introduced it is the **error**.\n\n### 1.2 The Three Faces of a Defect\n\n| Term | What it is | Analogy |\n|------|-----------|---------|\n| **Error** | A human mistake (wrong thought, typo, miscalculation) | A surgeon nicking the wrong artery |\n| **Fault (Defect/Bug)** | The static artefact carrying the mistake | The still-bleeding artery |\n| **Failure** | The observable, runtime misbehaviour | The patient going into shock |\n\n> [!IMPORTANT]\n> A **fault may exist without ever causing a failure** if the faulty code path is never executed. This is the deepest reason why *testing alone* can never prove a system correct — it can only demonstrate the presence of faults under the executed cases.\n\n## 1.3 The Defect Cost Escalation Curve (Boehm's Law)\n\n> [!NOTE]\n> **Barry Boehm's Empirical Finding (1981, reconfirmed 2024 NIST studies):** If a defect costs **1 unit** to fix at the *requirements stage*, it costs **5 units** at *design*, **10 units** at *coding*, **50 units** at *integration testing*, and up to **200 units** after *deployment* in safety-critical systems.\n\n## 1.4 The Three Pillars of Defect Mitigation\n\n1. **Testing** — Executing the software with sample inputs to observe failures.\n2. **Verification** — *Are we building the product right?* (Does the artefact conform to its specification?)\n3. **Formal Methods** — Using mathematically rigorous notations, languages, and logical inference to *prove* properties of software before, during, or after construction.\n\n## 1.5 Conceptual Analogy — \"Building a Skyscraper\"\n\nImagine you are an engineer building a 100-storey tower.\n\n- **Testing** is like shaking a scale model on a shake-table *after* building it. You learn about *some* weaknesses, but you can never test every earthquake, wind, and weight combination.\n- **Verification** is the design review: does the blueprint match the client's stated needs?\n- **Formal Methods** is the mathematical proof: *\"Given loads of up to 250 km/h wind, the maximum sway at the top will be ≤ 0.3 m, for all possible load distributions.\"* — a universal guarantee, not a sampled one.\n\n## 1.6 Validation vs Verification — The KTU Favourite Distinction\n\n> [!IMPORTANT]\n> - **Verification** = *\"Are we building the product right?\"* — Conformance of an artefact to its specification. (Internal, technical.)\n> - **Validation** = *\"Are we building the right product?\"* — Fitness of the software to the real user's needs. (External, user-oriented.)\n\nThis single line — *Verification vs Validation* — is one of the **highest-frequency 3-mark questions** in KTU ESE papers.\n\n## 1.7 The V-Model of Software Engineering and Where Each Technique Sits\n\n| V-Model Phase (Left) | Verification Activity | V-Model Phase (Right) |\n|----------------------|----------------------|----------------------|\n| Requirements Specification | Reviews, formal specification (Z, B) | Acceptance Testing |\n| System Design | Design reviews, model checking | System Testing |\n| Detailed Design | Inspections, proof obligations | Integration Testing |\n| Code | Static analysis, unit testing, theorem proving | Unit Testing |\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Defect Equation (Quantitative Software Reliability)\n\nLet $\\lambda$ = fault density (faults per KLOC) and let $N$ = program size in KLOC. Then the **expected number of latent faults** is:\n\n$$\nE[F] = \\lambda \\cdot N\n$$\n\nFor the **Mean Time To Failure (MTTF)** under an exponential failure model:\n\n$$\n\\text{MTTF} = \\frac{1}{\\lambda_{runtime}}\n$$\n\nwhere $\\lambda_{runtime}$ is the runtime failure rate (failures per CPU-hour).\n\n> [!NOTE]\n> **Nelson's Model** for expected defects remaining after $n$ test cases:\n\n$$\nE[\\text{Defects remaining}] = N \\cdot \\left(1 - \\sum_{i=1}^{n} p_i \\right)\n$$\n\nwhere $p_i$ is the probability that test case $i$ executes a *new* program path, and $N$ is the total number of paths.\n\n## 2.2 The Fundamental Limitation of Testing — Dijkstra's Dictum\n\n> [!IMPORTANT]\n> **Edsger W. Dijkstra (1970):** *\"Program testing can be used to show the presence of bugs, but never to show their absence.\"*\n\nMathematically: testing samples the **input domain** $\\mathcal{D}$. The set of test cases $T \\subset \\mathcal{D}$ is necessarily finite, while $\\mathcal{D}$ is typically infinite (real numbers, strings, sequences). Therefore:\n\n$$\n\\bigvee_{t \\in T} \\text{pass}(t) \\;\\not\\Rightarrow\\; \\forall x \\in \\mathcal{D}: \\text{pass}(x)\n$$\n\n## 2.3 Verification — The Logical Backbone\n\nVerification is a **proof obligation**: given a specification $S$ and an implementation $I$, we want to show:\n\n$$\nS \\models P \\quad \\text{and} \\quad I \\models S\n$$\n\nwhere $\\models$ denotes *logical consequence*. Translated: \"Every model of the specification satisfies the desired property $P$, and every behaviour of the implementation is a model of the specification.\"\n\nThe two directions of verification:\n\n| Direction | Question | Tool Family |\n|-----------|----------|-------------|\n| **Forward Verification** ($S \\Rightarrow I$) | Does the implementation satisfy the spec? | Theorem Provers (Isabelle, Coq, PVS) |\n| **Backward Verification** ($I \\Rightarrow S$) | Does the spec describe the implementation? | Refinement Checkers, Animation |\n\n## 2.4 Formal Methods — The Family Tree\n\n> [!NOTE]\n> **Definition (KPU 2024-aligned):** *Formal methods* are mathematically based techniques for the specification, development, analysis, and verification of software (and hardware) systems. The mathematical foundation guarantees that reasoning is unambiguous, repeatable, and mechanizable.\n\n### 2.4.1 Two Major Branches\n\n| Branch | Mechanism | Representative Tool | Best For |\n|--------|-----------|--------------------|----------|\n| **Model Checking** | Exhaustive state-space search against a temporal-logic property | SPIN, NuSMV, UPPAAL | Finite-state concurrent systems, protocols |\n| **Theorem Proving** | Constructing a machine-checked proof of correctness from axioms | Isabelle/HOL, Coq, ACL2, PVS | Algorithms, mathematics, security kernels |\n| **Abstract Interpretation** | Sound over-approximation of program semantics | Astrée, PolySpace, Frama-C | Static analysis of C code |\n| **Equivalence / Refinement Checking** | Bisimulation and refinement proofs | FDR, Rodin (Event-B) | Refinement-based development |\n| **Alloy / SAT-based** | Translation to SAT/SMT, bounded search | Alloy Analyzer, CBMC | Lightweight specification, software design |\n\n### 2.4.2 Lightweight vs Heavyweight Formal Methods\n\n- **Lightweight** — used as one technique among many (type systems, model checkers on small abstractions, code-level static analysis).\n- **Heavyweight** — the formal specification *is* the development artefact, and code is mechanically derived (e.g., the B-Method used in Paris Métro Line 14).\n\n## 2.5 KTU High-Yield Cheat Sheet\n\n| # | Concept | Key Expression / Phrase | Unit / Domain |\n|---|---------|------------------------|---------------|\n| 1 | Defect cost escalation | $\\text{Cost}(x) = 1\\cdot e^{k \\cdot \\text{phase}}$ where $k$ increases with phase | Monetary units |\n| 2 | E[F] latent faults | $E[F] = \\lambda \\cdot N$ | dimensionless count |\n| 3 | MTTF | $\\text{MTTF} = 1/\\lambda$ | CPU-hours |\n| 4 | Nelson's residual defect model | $E[R] = N \\cdot (1 - \\sum p_i)$ | count |\n| 5 | Verification | $I \\models S$ | boolean (true/false) |\n| 6 | Model checking | $M, s \\models \\varphi$ (CTL/LTL) | boolean |\n| 7 | Dijkstra's limitation | $T \\subset \\mathcal{D} \\Rightarrow$ partial | qualitative |\n| 8 | V&V | Verification = right product *right*; Validation = *right* product | qualitative |\n\n## 2.6 Real-World Engineering Utility\n\n- **Aerospace:** Airbus A380 flight-control software was developed using the B-Method; zero in-flight software-induced failures across millions of flight hours.\n- **Rail:** Paris Métro Line 14 is driverless and relies on B-specifications.\n- **Security:** seL4 microkernel — formally proven in Isabelle/HOL to enforce its security policy; ~7,500 lines of C with a 100% functional-correctness proof.\n- **Hardware:** Intel, AMD, and IBM routinely use model checking and theorem proving for floating-point unit verification.\n- **Automotive:** ISO 26262 (ASIL-D) explicitly recommends formal methods at the highest safety level.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation — From Defect Density to Cost of Quality\n\nWe will derive the **total cost of software defects** as a function of the development phase at which they are detected and removed.\n\n### Step 1 — Define the cost per defect at phase $i$\n\nLet $c_i$ be the cost (in person-hours or monetary units) to detect and repair a single defect when the artefact is in phase $i \\in \\{0,1,2,3,4\\}$ representing {Requirements, Design, Code, Integration, Deployment}.\n\nEmpirically, Boehm observed the ratio:\n\n$$\nc_0 : c_1 : c_2 : c_3 : c_4 = 1 : 5 : 10 : 50 : 200\n$$\n\n### Step 2 — Define the defect introduction rate\n\nLet $\\delta_i$ be the average number of defects introduced per KLOC during phase $i$, and let $S$ be the final system size in KLOC. Total defects introduced:\n\n$$\nD_{\\text{total}} = \\sum_{i=0}^{4} \\delta_i \\cdot S\n$$\n\n### Step 3 — Define the cost of repairing all defects detected at phase $j$\n\nSuppose a fraction $\\alpha_{i,j}$ of defects introduced in phase $i$ is first *detected* in phase $j$ (with $j \\ge i$, since defects cannot be detected before they exist). The total cost of all repairs in the project is:\n\n$$\nC_{\\text{total}} = \\sum_{j=0}^{4} \\sum_{i=0}^{j} \\alpha_{i,j} \\cdot \\delta_i \\cdot S \\cdot c_j\n$$\n\n### Step 4 — Apply a simplification (single-phase detection)\n\nIn the common KTU textbook scenario, defects introduced in phase $i$ are *first* detected in the immediately following phase, so $\\alpha_{i,j} = 1$ if $j = i+1$ and $0$ otherwise:\n\n$$\n\\begin{aligned}\nC_{\\text{total}}\n&= \\sum_{i=0}^{3} \\delta_i \\cdot S \\cdot c_{i+1} \\\\\n&= S \\left( \\delta_0 c_1 + \\delta_1 c_2 + \\delta_2 c_3 + \\delta_3 c_4 \\right) \\\\\n&= S \\left( \\delta_0 \\cdot 5 + \\delta_1 \\cdot 10 + \\delta_2 \\cdot 50 + \\delta_3 \\cdot 200 \\right) c_0\n\\end{aligned}\n$$\n\n### Step 5 — Quantify the savings from early detection\n\nIf we move the detection of *requirements defects* ($\\delta_0$) from phase 1 to phase 0 (i.e., we catch them at the *same* phase they are introduced), the per-defect cost drops from $5c_0$ to $1c_0$. The savings factor is:\n\n$$\n\\text{Savings} = \\frac{5c_0 - 1c_0}{5c_0} = \\frac{4}{5} = 80\\%\n$$\n\nThis *algebraically* justifies the **front-loading** strategy that motivates formal specification: a high-effort, mathematically rigorous requirements phase is cheaper than thousands of test runs after deployment.\n\n## 3.2 Derivation — Why Testing Cannot Prove Absence of Defects\n\n### Step 1 — Define the input domain\n\nLet $\\mathcal{D}$ be the set of all valid inputs to program $P$. For a function $f: \\mathbb{Z} \\to \\mathbb{Z}$, $\\mathcal{D} = \\mathbb{Z}$ (countably infinite). For a real-valued function, $\\mathcal{D} = \\mathbb{R}$ (uncountably infinite).\n\n### Step 2 — Define the test set\n\nA test campaign selects a finite set $T = \\{t_1, t_2, \\ldots, t_n\\}$ with $n < \\infty$.\n\n### Step 3 — Show partial coverage\n\nEven with $n$ tests, the *measured* correctness is:\n\n$$\n\\text{Correct}_{\\text{measured}} = \\bigwedge_{i=1}^{n} \\left( P(t_i) = f(t_i) \\right)\n$$\n\n### Step 4 — The unmeasured gap\n\nThe *true* correctness condition is:\n\n$$\n\\text{Correct}_{\\text{true}} = \\forall x \\in \\mathcal{D} : P(x) = f(x)\n$$\n\n### Step 5 — Logical inference\n\n$$\n\\text{Correct}_{\\text{measured}} \\;\\not\\Rightarrow\\; \\text{Correct}_{\\text{true}}\n$$\n\nbecause there may exist $x \\in \\mathcal{D} \\setminus T$ such that $P(x) \\neq f(x)$. The only way to bridge the gap is to **prove** Correct$_{\\text{true}}$ directly — which is precisely what **formal verification** offers.\n\n$$\n\\boxed{\\;\\text{Testing: } T \\text{ finite} \\Rightarrow \\text{no universal guarantee.} \\qquad\n\\text{Verification: } \\text{proof} \\Rightarrow \\text{universal guarantee.}\\;}\n$$\n\n## 3.3 Analytical Comparison Matrix\n\n| Criterion | Testing | Verification (Informal) | Formal Methods |\n|-----------|---------|------------------------|----------------|\n| **Mathematical basis** | Sampling theory | Reviews, walkthroughs | Predicate logic, set theory, process algebra |\n| **Coverage of inputs** | Sampled, finite | N/A (manual review) | Universal (proof) or exhaustive (model check) |\n| **Detects** | Failures | Defects in artefacts | Defects, plus proves their *absence* |\n| **Scalability** | High | High | Low–medium (historically); improving |\n| **Skill required** | Moderate | Moderate | High (specialist) |\n| **Cost of application** | Linear in test count | Linear in review size | Often non-linear, can be exponential in state space |\n| **Automation** | Partial | Low | High (model checkers, SMT solvers) |\n| **Best fit** | Late-stage validation | All phases | Safety-/security-critical kernels, protocols, algorithms |\n\n## 3.4 Worked Example — Model Checking the Mutual Exclusion Property\n\nConsider two processes, $P_1$ and $P_2$, each with a critical section. The property to check is **mutual exclusion**: they are never *both* in the critical section at the same time.\n\n**Specification (CTL):**\n\n$$\n\\text{AG}\\;\\neg(\\text{state}_1 = cs \\wedge \\text{state}_2 = cs)\n$$\n\nRead as: \"For *all* computation paths (**A**), *globally* (**G**), it is *never* the case that both are in the critical section.\"\n\n**Process state space (4 states each):**\n\n$$\n\\mid S \\mid = 4 \\times 4 = 16 \\text{ global states}\n$$\n\n**Transition relation** is defined by the atomic actions `enter`, `exit`. The model checker enumerates all reachable states and tests the CTL formula. Result: holds for the standard Peterson/Szymanski algorithms; fails for naïve flag-based implementations under certain interleavings.\n\nThe model checker returns either **TRUE** (property holds) or a **counter-example trace** (the sequence of states violating the property), which is one of its most powerful features.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 The Defect Lifecycle and the Three Mitigation Pillars\n\n```mermaid\nflowchart TD\n    A[Human Error] --> B[Fault Introduced in Artefact]\n    B --> C{Activated by Input/Path?}\n    C -- Yes --> D[Failure Observed at Runtime]\n    C -- No --> E[Latent Fault Remains]\n    D --> F[Testing Detects Failure]\n    E --> G[Static Analysis / Verification]\n    B --> H[Formal Specification Catches at Source]\n    F --> I[Defect Removed and Re-tested]\n    G --> I\n    H --> I\n    I --> J[Defect-FREE Software]\n```\n\n## 4.2 Block-Level Functional Architecture of a Verification Toolkit\n\n```mermaid\nflowchart LR\n    subgraph INPUT[INPUT ARTIFACTS]\n        specA[Formal Specification Z / B / Alloy]\n        implA[Implementation Source Code]\n        propA[Property in LTL CTL]\n    end\n\n    subgraph FRONT[FRONT END PARSER]\n        parserA[Lexer and Syntax Checker]\n        typeA[Type Checker]\n    end\n\n    subgraph ENGINE[REASONING ENGINE]\n        mcA[Model Checker SPIN NuSMV]\n        tpA[Proof Assistant Coq Isabelle]\n        absA[Abstract Interpreter Astrée]\n    end\n\n    subgraph DECIDE[DECISION MODULE]\n        satA[SMT Solver Z3 CVC4]\n        coA[Counter Example Generator]\n        certA[Proof Certificate Builder]\n    end\n\n    OUTPUT1[VERDICT Property Holds]\n    OUTPUT2[COUNTER EXAMPLE Trace]\n    OUTPUT3[PROOF CERTIFICATE]\n\n    specA --> parserA\n    implA --> parserA\n    propA --> parserA\n    parserA --> typeA\n    typeA --> mcA\n    typeA --> tpA\n    typeA --> absA\n    mcA --> satA\n    tpA --> satA\n    absA --> satA\n    satA --> coA\n    satA --> certA\n    coA --> OUTPUT2\n    certA --> OUTPUT3\n    satA --> OUTPUT1\n```\n\n## 4.3 The V-Model with Verification and Testing Activities Mapped\n\n```mermaid\nflowchart TB\n    subgraph LEFT[DECOMPOSITION LEFT SIDE]\n        L1[Requirements]\n        L2[System Design]\n        L3[Detailed Design]\n        L4[Coding]\n    end\n\n    subgraph RIGHT[INTEGRATION RIGHT SIDE]\n        R1[Acceptance Testing]\n        R2[System Testing]\n        R3[Integration Testing]\n        R4[Unit Testing]\n    end\n\n    L1 --- R1\n    L2 --- R2\n    L3 --- R3\n    L4 --- R4\n\n    L1 --> V1[Formal Specification Z Alloy]\n    L2 --> V2[Model Checking of Architectural Model]\n    L3 --> V3[Design Reviews Refinement Proofs]\n    L4 --> V4[Static Analysis Theorem Proving]\n```\n\n## 4.4 Where Each Tool Sits in the Development Pipeline\n\n```mermaid\nflowchart LR\n    REQ[Requirements Text] --> FS[Formal Spec in Z or B]\n    FS --> MC[Model Check with SPIN NuSMV]\n    FS --> TP[Prove Properties in Coq Isabelle]\n    MC --> RS[Refined State Machine]\n    TP --> RS\n    RS --> CODE[Generated or Hand Coded C Java]\n    CODE --> SA[Static Analysis Astrée FramaC]\n    SA --> BIN[Verified Binary]\n    CODE --> TEST[Traditional Unit Integration Tests]\n    TEST --> BIN\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Question 1 [KTU University Exam — May 2024]\n**Differentiate between verification and validation in software engineering. Give one example of each.**\n\n**Model Answer:**\n\n| Aspect | Verification | Validation |\n|--------|--------------|------------|\n| Question answered | Are we building the product **right**? | Are we building the **right** product? |\n| Focus | Conformance to specification | Fitness for use in the real world |\n| Performed by | Developers, QA engineers | End users, domain experts, customers |\n| Artefact examined | Intermediate artefacts (design, code) | Final product in operational environment |\n| Example | Checking that a function returns the sum of two integers as per its API contract | Demonstrating to a hospital that the patient-management system supports the doctor's daily workflow |\n\n*(Full credit: 3 marks for a clear two-row table or two crisp paragraphs. KTU evaluator looks for the **right product / right product** distinction.)*\n\n---\n\n### Question 2 [KTU University Exam — Dec 2023]\n**Why can software testing never prove the absence of defects? State Dijkstra's quote and explain with an example.**\n\n**Model Answer:**\n\nDijkstra (1970): *\"Program testing can be used to show the presence of bugs, but never to show their absence.\"*\n\n**Explanation:** A test set $T$ is a finite subset of the (typically infinite) input domain $\\mathcal{D}$. If all tests in $T$ pass, we have shown that those particular inputs are handled correctly, but the inputs in $\\mathcal{D} \\setminus T$ remain unexamined. For example, if we test a square-root function with inputs 0, 1, 4, 9, 16, we have only covered the perfect squares. Inputs like 2, 3, 5, 7 may still expose a defect. Hence testing yields *necessary* but not *sufficient* evidence of correctness.\n\n*(Full credit: 3 marks — Dijkstra quote (1), logical reasoning (1), example (1).)*\n\n---\n\n## 5.2 Part B — 14-Mark Questions (ESE Module Internal Choice)\n\n### Question A (14 Marks) [KTU University Exam — July 2024]\n\n**(a)** Discuss the various techniques used to deal with software defects. Compare the relative effectiveness of **testing**, **informal verification**, and **formal methods** along the dimensions of coverage, cost, and scalability. **[7 Marks]**\n\n**(b)** With a neat diagram, explain the **V-Model** of software development. Map each verification and validation activity to its corresponding phase, and identify at which phase **formal specification** yields the highest return on investment. Justify your answer with Boehm's defect cost data. **[7 Marks]**\n\n#### Model Solution — Part (a)\n\n**1. Testing**\n- Mechanism: Execute the program on sample inputs and compare actual vs expected outputs.\n- Coverage: Sampled; partial over the input domain.\n- Cost: Linear in number of test cases; tooling (JUnit, Selenium) is mature.\n- Scalability: High.\n- Limitation: Cannot prove absence of defects (Dijkstra).\n\n**2. Informal Verification**\n- Mechanism: Walkthroughs, inspections, desk-checks, peer reviews.\n- Coverage: Artefact-by-artefact; depends on human rigour.\n- Cost: Moderate; mainly human-time.\n- Scalability: High for small artefacts, drops for large systems.\n- Limitation: Subjective; effectiveness varies with reviewer expertise.\n\n**3. Formal Methods**\n- Mechanism: Mathematical specification, model checking, theorem proving, abstract interpretation.\n- Coverage: Universal (proof) or exhaustive (model check) over the chosen model.\n- Cost: High up-front; pays off for safety-/security-critical systems.\n- Scalability: Improving rapidly; modern SMT solvers (Z3, CVC5) and cloud-based provers handle millions of LOC.\n\n**Comparative Table (to be drawn in the answer book):**\n\n| Dimension | Testing | Informal Verification | Formal Methods |\n|-----------|---------|----------------------|----------------|\n| Coverage | Sampled | Artefact-level | Universal / exhaustive |\n| Cost | Low–moderate | Moderate | High up-front |\n| Scalability | Excellent | Moderate | Improving |\n| Proof of absence | No | No | Yes |\n| Automation | Partial | Low | High |\n\n#### Model Solution — Part (b)\n\n**V-Model Diagram:** Draw the classic V with *Requirements*, *System Design*, *Detailed Design*, *Code* on the descending left arm and *Acceptance*, *System*, *Integration*, *Unit Testing* on the ascending right arm. (A clear labelled ASCII-V is acceptable in KTU answer sheets.)\n\n**Mapping:**\n\n- Requirements ↔ Acceptance Testing\n- System Design ↔ System Testing\n- Detailed Design ↔ Integration Testing\n- Code ↔ Unit Testing\n\n**Where formal specification pays off most — the *Requirements* phase:**\nBy Boehm's data, fixing a defect at the requirements phase costs **1 unit**, while the same defect costs **50 units** if detected at integration and **200 units** if discovered after deployment. Formal specification (Z, B, Alloy) is a *requirements-engineering* activity: it forces early detection of ambiguity, inconsistency, and incompleteness. Hence the **return on investment is maximal at the requirements phase**, exactly where defects are cheapest to fix and most expensive to leave in.\n\n---\n\n### Question B (14 Marks) [KTU University Exam — Dec 2023]\n\n**(a)** What are **formal methods**? List and briefly explain the four major categories of formal methods tools with one representative tool for each category. **[7 Marks]**\n\n**(b)** A railway signalling system has 4 possible states per track segment and 12 track segments. Estimate the size of the global state space. If model checking explores 10 million states per second, how long will exhaustive verification take? Comment on the **state-space explosion problem** and one practical mitigation. **[7 Marks]**\n\n#### Model Solution — Part (a)\n\n**Definition:** Formal methods are mathematically rigorous techniques and tools used for the specification, design, verification, and validation of software and hardware systems. They are based on formal languages with mathematically defined syntax and semantics, and logical inference systems whose derivations are mechanically checkable.\n\n**Four major categories:**\n\n1. **Model Checkers** — Exhaustive state-space exploration against a temporal-logic property. *Example:* **SPIN** (for Promela models of concurrent systems), **NuSMV** (for hardware and synchronous systems).\n\n2. **Theorem Provers / Proof Assistants** — Interactive construction of machine-checked proofs from axioms. *Examples:* **Isabelle/HOL**, **Coq**, **ACL2**, **PVS**.\n\n3. **Abstract Interpreters** — Sound static analysis by over-approximating program semantics. *Examples:* **Astrée** (embedded C), **Frama-C** (C with ACSL contracts), **PolySpace**.\n\n4. **Specification & SAT/SMT-based Tools** — Lightweight formal modelling and bounded verification using SAT/SMT solvers. *Examples:* **Alloy Analyzer**, **CBMC** (C Bounded Model Checker), **B-method / Rodin**.\n\n#### Model Solution — Part (b)\n\n**Global state space:**\n\n$$\n\\mid S \\mid = 4^{12} = 16\\,777\\,216 \\text{ states}\n$$\n\n**Time to explore at 10 million states/second:**\n\n$$\nT = \\frac{16\\,777\\,216}{10\\,7} \\approx 1.68 \\text{ seconds}\n$$\n\n(where $10^{7}$ is 10 million).\n\n**State-space explosion problem:**\nWhen components are added in parallel, the global state space grows **exponentially** (here, $4^n$ in the number of segments $n$). For larger systems (e.g., $n = 30$), the state space becomes $4^{30} \\approx 10^{18}$, which is physically infeasible to explore exhaustively — even at $10^9$ states/sec, it would take thousands of years.\n\n**Practical mitigation — *Partial-Order Reduction* (used in SPIN):**\nOnly one representative from each equivalence class of *interleavings* of independent transitions is explored. For a system with $k$ concurrent processes, this often reduces the explored state space by a factor exponential in $k$. Other mitigations include **symbolic model checking** (BDDs in NuSMV), **abstraction**, and **compositional reasoning**.\n\n> [!WARNING]\n> **KTU Examiner's Pitfall Alert:**\n> 1. Many students confuse *verification* with *validation* — losing 1 mark on a 3-mark question.\n> 2. In numerical questions, **forgetting to exponentiate correctly** (writing $4 \\times 12 = 48$ instead of $4^{12}$) is the most common mistake.\n> 3. When asked to \"list\" formal methods tools, students frequently write only the *names* without the *category* they belong to — which costs 2 marks in a 7-mark sub-part.\n> 4. The V-Model answer must include a **labelled diagram**; a description without a diagram loses at least 1 mark.\n\n## 5.3 Topic Recap & Important Things to Remember\n\n- **Software defect** = fault that may cause a **failure**; introduced by a human **error**.\n- **Boehm's cost ratio:** $1 : 5 : 10 : 50 : 200$ across requirements → deployment.\n- **Verification** = building the product *right* (artefact vs specification).\n- **Validation** = building the *right* product (artefact vs user need).\n- **Dijkstra's limitation:** testing shows presence, never absence, of defects.\n- **Formal methods** = mathematically rigorous specification, development, and verification techniques.\n- **Four tool categories:** model checkers, theorem provers, abstract interpreters, SAT/SMT-based tools.\n- **State-space explosion:** global state count is exponential in the number of components.\n- **Mitigations:** partial-order reduction, symbolic BDDs, abstraction, compositional verification.\n- **V-Model:** pairs each development phase with a corresponding test phase; formal specification is most beneficial at the *requirements* phase.\n- **Industrial adopters:** Airbus (B), Paris Metro (B), seL4 kernel (Isabelle), Intel/AMD hardware (model checking).\n- **Lightweight vs heavyweight formal methods:** lightweight is one tool among many; heavyweight uses formal specification as the primary development artefact.\n- **Front-loading principle:** a high up-front formal-specification effort is amortized over the entire project life cycle.\n- **CTL/LTL** are the two principal temporal-logic families used in model checking.\n- **SMT solvers** (Z3, CVC5) are the workhorses behind most modern verification tools.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        }
      ]
    },
    {
      "id": "m2",
      "title": "Module 2",
      "topics": [
        {
          "id": "artificial-intelligence-and-data-science-7-pecst741-mModule 2-t1",
          "title": "Conceptual Modelling",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST741 - FORMAL METHODS IN SOFTWARE ENGINEERING\n> - **Module:** Module Module 2\n> - **Topic:** Conceptual Modelling\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n> [!IMPORTANT]\n> **Conceptual Modelling** is the process of building an **abstract, formal representation** of a real-world system, domain, or phenomenon using mathematical and logical constructs, *independently* of any implementation concerns, programming language, or hardware platform.\n\nIn the context of **Formal Methods in Software Engineering**, a conceptual model is a **precise, unambiguous description** of:\n\n1. The **entities** (objects) that exist in the problem domain.\n2. The **attributes** (properties) that describe those entities.\n3. The **relationships** (associations) between entities.\n4. The **constraints** (invariants, rules) that the system must always satisfy.\n5. The **operations** (behaviour) that may change the state of the system.\n\nThe output of conceptual modelling is typically a **formal specification** written in a mathematical notation such as **Z (Zed)**, **VDM (Vienna Development Method)**, **B-Method**, **OBJ**, **Alloy**, or a graphical semi-formal language like **UML Class Diagrams**, **Entity-Relationship (ER) Diagrams**, or **Object-Role Models (ORM)**.\n\n> [!NOTE]\n> **Syllabus Highlight (KTU PECST741 Module 2):** Conceptual modelling sits at the *front-end* of the formal specification pipeline. The model produced here is later **refined** into a concrete design, then **implemented** in code. The \"conceptual\" qualifier means the model captures *what* the system does, not *how* it does it.\n\n---\n\n## 1.2 Conceptual Analogy — \"The Architect's Blueprint\"\n\nImagine you want to build a new **airport terminal** in Kerala.\n\n| Real-World Stage | Engineering Equivalent |\n| :--- | :--- |\n| Architect's 2D drawings showing walls, doors, corridors, dimensions, fire exits | **Conceptual Model** (entities, relationships, constraints) |\n| Structural engineer's load calculations and steel reinforcement plan | **Formal Specification** in Z / VDM (mathematical detail) |\n| Construction by masons, electricians, plumbers | **Implementation** in Java / C++ / Python |\n| The finished, painted, furnished terminal | **Deployed Software System** |\n\nBefore a single brick is laid, the architect produces a **conceptual model** of the terminal. This model:\n\n* Shows **rooms** (entities).\n* Labels each room with **purpose and capacity** (attributes).\n* Shows **corridors connecting rooms** (relationships).\n* Enforces rules like *\"every fire exit must be ≤ 30 m from any point\"* (constraints).\n\n> **Crucially, the blueprint does NOT say:** *which brand of cement to use, what colour the walls will be, or which subcontractor builds Block B.* Those are **implementation** concerns.\n\n**Software conceptual modelling is exactly the same discipline — applied to a software system's domain.**\n\n---\n\n## 1.3 Why Conceptual Modelling? (The Engineering \"Why\")\n\n> [!IMPORTANT]\n> **KTU Examiner Tip:** A common 3-mark question asks *\"Why do we need conceptual modelling before writing specifications?\"*. Memorise these three reasons verbatim.\n\n1. **Abstraction** — Hides accidental complexity, allowing engineers to reason about correctness *before* coding begins.\n2. **Communication** — Provides a shared, unambiguous vocabulary between **domain experts** (e.g., bank managers) and **software engineers**.\n3. **Verification** — Mathematical models can be **proved** (using theorem provers) or **checked** (using model checkers) to satisfy safety, security, and correctness properties.\n\n---\n\n## 1.4 Position in the Formal Methods Lifecycle\n\nThe conceptual model is the **first formal artefact** produced in the typical formal methods workflow:\n\n$$\\text{Domain Analysis} \\;\\longrightarrow\\; \\boxed{\\text{Conceptual Model}} \\;\\longrightarrow\\; \\text{Formal Specification} \\;\\longrightarrow\\; \\text{Refinement} \\;\\longrightarrow\\; \\text{Implementation} \\;\\longrightarrow\\; \\text{Verification} \\;\\longrightarrow\\; \\text{Validation}$$\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Four Pillars of a Conceptual Model\n\nEvery conceptual model — whether expressed in Z, VDM, UML, or ORM — is built from the same four logical pillars. Understanding these is essential for any KTU Module 2 question.\n\n### Pillar 1 — **Entities (Objects / Instances)**\nEntities are the \"things\" that exist in the domain.\n* **Example:** `Student`, `Course`, `Library`, `BankAccount`.\n* Formally, an entity is a member of a **set**: $\\text{Student} \\in \\mathbb{S}$ where $\\mathbb{S}$ is the universe of all students.\n\n### Pillar 2 — **Attributes (Properties / Fields)**\nAttributes characterise entities.\n* **Example:** A `Student` has a `name`, `rollNo`, `cgpa`.\n* Formally, an attribute is a **function** mapping an entity to a value:\n$$\\text{cgpa} : \\text{Student} \\rightarrow \\mathbb{R} \\quad \\text{where} \\; 0.0 \\leq \\text{cgpa}(s) \\leq 10.0$$\n\n### Pillar 3 — **Relationships (Associations)**\nRelationships connect entities.\n* **Example:** A `Student` *enrols in* a `Course`.\n* Formally, a relationship is a **Cartesian product** of sets:\n$$\\text{Enrolment} \\subseteq \\text{Student} \\times \\text{Course}$$\n\n### Pillar 4 — **Constraints (Invariants)**\nConstraints are conditions that must *always* hold.\n* **Example:** *\"No student may enrol in more than 6 courses in a semester.\"*\n* Formally, a constraint is a **predicate** over the model:\n$$\\forall s : \\text{Student} \\;\\bullet\\; \\vert \\{ c : \\text{Course} \\mid (s, c) \\in \\text{Enrolment} \\} \\vert \\leq 6$$\n\n> [!NOTE]\n> The symbol $\\vert \\cdot \\vert$ denotes the **cardinality** (size) of a set. It is rendered using `\\vert` in LaTeX to avoid breaking markdown table syntax.\n\n---\n\n## 2.2 Mathematical Foundations Used in Conceptual Modelling\n\nConceptual modelling rests on three classical mathematical pillars. Every KTU Module 2 question is built on these.\n\n### (a) Set Theory (Naïve or Zermelo-Fraenkel)\nSet theory provides the vocabulary for collections of entities.\n\n| Symbol | LaTeX | Meaning | Example in Modelling |\n| :---: | :---: | :--- | :--- |\n| $x \\in S$ | `\\in` | Membership | `rahul ∈ Student` |\n| $S \\subseteq T$ | `\\subseteq` | Subset | `Enrolled ⊆ Student` |\n| $S \\cup T$ | `\\cup` | Union | All students OR all staff |\n| $S \\cap T$ | `\\cap` | Intersection | Students who are also staff |\n| $S \\setminus T$ | `\\setminus` | Set difference | Students who are not staff |\n| $S \\times T$ | `\\times` | Cartesian product | `Student × Course` |\n| $\\mathcal{P}(S)$ | `\\mathcal{P}` | Power set | All possible groups of students |\n| $\\vert S \\vert$ | `\\vert S \\vert` | Cardinality | Number of students |\n\n### (b) First-Order Predicate Logic (FOPL)\nFOPL provides the language for stating **facts** and **rules** about entities.\n\n| Logical Symbol | LaTeX | Read As | Use in Modelling |\n| :---: | :---: | :--- | :--- |\n| $\\forall$ | `\\forall` | \"For all\" | \"For every student...\" |\n| $\\exists$ | `\\exists` | \"There exists\" | \"Some student exists such that...\" |\n| $\\neg$ | `\\neg` | NOT | \"is not enrolled\" |\n| $\\wedge$ | `\\wedge` | AND | Combine conditions |\n| $\\vee$ | `\\vee` | OR | Alternative cases |\n| $\\rightarrow$ | `\\rightarrow` | IMPLIES | \"If enrolled, then has rollNo\" |\n| $\\leftrightarrow$ | `\\leftrightarrow` | IFF | Equivalent statements |\n\n### (c) Relations and Functions\nRelations describe how entities link; functions describe deterministic mappings.\n\n| Concept | Formal Definition | Modelling Use |\n| :--- | :--- | :--- |\n| **Binary relation** $R$ | $R \\subseteq A \\times B$ | `Enrols : Student ↔ Course` |\n| **Function** $f$ | $f : A \\rightarrow B$ | `rollNo : Student → ℕ` (each student has exactly one roll number) |\n| **Partial function** | $f : A \\nrightarrow B$ | `guardian : Student ⇸ Person` (some students have no guardian in DB) |\n| **Total function** | $f : A \\rightarrow B$ | Defined for every input |\n| **Injection** | $f : A \\nrightarrow B$ injective | `rollNo` uniquely identifies a student |\n| **Sequence** | $\\text{seq } A$ | Ordered list, e.g., timetable |\n\n---\n\n## 2.3 The Z Notation — A Working Example\n\n**Z** is the formal language most commonly referenced in KTU 2024 Scheme Module 2. A Z schema has two parts: a **declaration** (top) and a **predicate** (bottom).\n\n$$\n\\begin{aligned}\n&\\text{Student} \\;\\hat{=}\\; \\left[\\, \\text{name} : \\text{STRING};\\;\\; \\text{rollNo} : \\mathbb{N};\\;\\; \\text{cgpa} : \\mathbb{R} \\;\\mid\\; \\text{cgpa} \\geq 0 \\;\\wedge\\; \\text{cgpa} \\leq 10 \\,\\right] \\\\\n&\\text{Enrolment} \\;\\hat{=}\\; \\left[\\, \\text{student} : \\text{Student};\\;\\; \\text{course} : \\text{Course} \\;\\mid\\; \\text{student.cgpa} \\geq 5.0 \\,\\right]\n\\end{aligned}\n$$\n\nReading the schema:\n* The **declaration part** (above the bar) introduces the variables and their types.\n* The **predicate part** (below the bar) lists the constraints.\n\n---\n\n## 2.4 Properties of a \"Good\" Conceptual Model\n\n| Property | Definition | Why It Matters (KTU) |\n| :--- | :--- | :--- |\n| **Correctness** | Faithfully represents the real domain | Avoids costly re-specification |\n| **Completeness** | Captures all relevant entities, relations, constraints | No missing edge-cases |\n| **Consistency** | No internal contradictions | Enables formal proof |\n| **Unambiguity** | Exactly one interpretation | The *raison d'être* of formal methods |\n| **Minimality** | No redundant entities or relations | Easier to maintain |\n| **Abstraction** | Independent of implementation | Survives technology change |\n\n> [!IMPORTANT]\n> **Engineering Utility:** Conceptual models are used in **safety-critical systems** (avionics, railway signalling, medical devices) where the cost of a software defect is measured in human lives. Standards like **DO-178C** (aviation) and **IEC 61508** (industrial control) mandate formal conceptual modelling.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 The Five-Stage Construction of a Conceptual Model (Detailed)\n\nThis is the **most important derivation** for KTU Module 2 — examiners routinely award 7 marks for this procedure.\n\n### Stage 1 — Domain Identification\nIdentify the **universe of discourse** (the slice of the real world the system will handle).\n\n> *Example (KTU Lab Library System):* The domain is the management of **books**, **members**, and **loans** in a college library.\n\nFormally, define a top-level set:\n$$\n\\text{Domain} = \\{ \\text{Book},\\; \\text{Member},\\; \\text{Loan},\\; \\text{Author},\\; \\text{Category} \\}\n$$\n\n---\n\n### Stage 2 — Entity (Type) Declaration\nFor each item in the domain, declare a **basic type** (an unstructured set of identifiers).\n\n$$\n\\begin{aligned}\n&[\\text{BOOK},\\; \\text{MEMBER},\\; \\text{CATEGORY}] \\\\\n&\\text{Author} : \\mathbb{P}_1\\; \\text{BOOK} \\quad \\text{(non-empty set of authors)}\n\\end{aligned}\n$$\n\nExplanation: $\\mathbb{P}_1$ denotes the set of *non-empty* subsets (so the library always has at least one author known).\n\n---\n\n### Stage 3 — Attribute (State Schema) Declaration\nDefine a **state schema** — a named cluster of related variables and their constraints.\n\n$$\n\\begin{aligned}\n\\text{LibraryState} \\;\\hat{=}\\; [\\; &\\text{stock} : \\text{BOOK} \\nrightarrow \\mathbb{N}; \\\\\n&\\text{loans} : \\text{MEMBER} \\leftrightarrow \\text{BOOK}; \\\\\n&\\text{catalogue} : \\text{BOOK} \\rightarrow \\text{CATEGORY} \\mid \\\\\n&\\forall b : \\text{BOOK} \\;\\bullet\\; b \\in \\operatorname{dom}\\, \\text{catalogue} \\;\\wedge\\; \\text{stock}(b) \\geq 0 \\;]\n\\end{aligned}\n$$\n\nReading the constraints:\n* Every book in the system must appear in the catalogue: $\\forall b : \\text{BOOK} \\;\\bullet\\; b \\in \\operatorname{dom}\\, \\text{catalogue}$.\n* Stock count is never negative: $\\text{stock}(b) \\geq 0$.\n\n---\n\n### Stage 4 — Relationship (Association) Derivation\nDerive higher-level relations from the basic types and state.\n\n> **Goal:** \"A member is *eligible* to borrow a book if they have fewer than 3 active loans.\"\n\n$$\n\\begin{aligned}\n\\text{EligibleToBorrow} : \\text{MEMBER} \\leftrightarrow \\text{BOOK} \\;\\;\\hat{=}\\;\\; \\{ (m, b) \\mid & \\; m \\in \\text{MEMBER} \\;\\wedge\\; b \\in \\text{BOOK} \\;\\wedge \\\\\n& \\vert \\{ b' : \\text{BOOK} \\mid (m, b') \\in \\text{loans} \\} \\vert < 3 \\;\\wedge \\\\\n& \\text{stock}(b) > 0 \\;\\}\n\\end{aligned}\n$$\n\n> [!NOTE]\n> This is a **derived relationship**. It is *defined* (with the `==` symbol) rather than *declared* (with the `:` symbol), because it can be computed from existing state.\n\n---\n\n### Stage 5 — Operation (Behavioural Schema) Derivation\nDefine the **operations** that transform the state. Each operation schema has three parts:\n* **Δ (Delta) declaration** — which state variables change.\n* **Inputs** — new information from the outside world.\n* **Predicate** — pre-conditions (inputs satisfy) and post-conditions (state after).\n\n**Operation: Issue a loan**\n\n$$\n\\begin{aligned}\n\\text{IssueLoan} \\;\\hat{=}\\; [\\; &\\Delta \\text{LibraryState}; \\\\\n&m? : \\text{MEMBER};\\;\\; b? : \\text{BOOK} \\mid \\\\\n&(m?, b?) \\in \\text{EligibleToBorrow} \\;\\wedge \\\\\n&\\text{stock}' = \\text{stock} \\oplus \\{ b? \\mapsto \\text{stock}(b?) - 1 \\} \\;\\wedge \\\\\n&\\text{loans}' = \\text{loans} \\cup \\{ (m?, b?) \\} \\;]\n\\end{aligned}\n$$\n\n**Line-by-line analysis:**\n\n| Schema Line | Meaning |\n| :--- | :--- |\n| $\\Delta \\text{LibraryState}$ | The state variables `stock` and `loans` may change; unprimed = before, primed = after |\n| $m? : \\text{MEMBER}$ | `m?` is an input parameter (the borrowing member) |\n| $b? : \\text{BOOK}$ | `b?` is an input parameter (the requested book) |\n| $(m?, b?) \\in \\text{EligibleToBorrow}$ | **Pre-condition:** member is eligible to borrow this book |\n| $\\text{stock}' = \\text{stock} \\oplus \\{ b? \\mapsto \\text{stock}(b?) - 1 \\}$ | **Post-condition:** stock of `b?` is decremented by 1 (the $\\oplus$ operator overrides one entry of the function) |\n| $\\text{loans}' = \\text{loans} \\cup \\{ (m?, b?) \\}$ | **Post-condition:** the new loan pair is added to the loans relation |\n\n---\n\n## 3.2 Analytical Matrix — Mapping Engineering Concepts to Formal Constructs\n\n| Engineering Concept (Plain English) | Formal Construct (Z / VDM / Logic) | LaTeX Notation |\n| :--- | :--- | :--- |\n| \"A set of items exists\" | Set declaration | $A : \\mathbb{P}\\,T$ |\n| \"Every element satisfies a rule\" | Universal quantification | $\\forall x : T \\;\\bullet\\; P(x)$ |\n| \"At least one element satisfies a rule\" | Existential quantification | $\\exists x : T \\;\\bullet\\; P(x)$ |\n| \"Two things are linked\" | Pair / relation membership | $(a, b) \\in R$ |\n| \"Each input produces exactly one output\" | Function | $f : A \\rightarrow B$ |\n| \"The output may not exist\" | Partial function | $f : A \\nrightarrow B$ |\n| \"An attribute has a default value\" | Schema with predicate | `field = default` |\n| \"State may change\" | $\\Delta$-schema | $\\Delta \\text{State}$ |\n| \"State does not change\" | $\\Xi$-schema | $\\Xi \\text{State}$ |\n| \"After the operation, this holds\" | Primed variable in predicate | $\\text{field}' = \\ldots$ |\n| \"Count of items\" | Cardinality | $\\vert S \\vert$ |\n| \"Ordered list\" | Sequence type | $\\text{seq}\\; T$ |\n| \"Multi-set / duplicates allowed\" | Bag type | $\\text{bag}\\; T$ |\n\n---\n\n## 3.3 Worked Example — Constraint Simplification\n\n**Problem (typical KTU Part B):** \"In a hostel, each room is assigned to **at most one student**, and each student is assigned to **exactly one room**. Express these constraints formally.\"\n\n**Step 1 — Declare basic types.**\n$$\n[\\text{ROOM}, \\text{STUDENT}]\n$$\n\n**Step 2 — Declare the allocation relation.**\n$$\n\\text{Alloc} : \\text{STUDENT} \\nrightarrow \\text{ROOM}\n$$\n\nThe $\\nrightarrow$ (partial function) already encodes *\"each student is assigned to at most one room\"* because a function can map each input to at most one output.\n\n**Step 3 — Encode the additional constraint that the assignment is total (every student gets a room).**\n$$\n\\forall s : \\text{STUDENT} \\;\\bullet\\; s \\in \\operatorname{dom}\\, \\text{Alloc}\n$$\n\n**Step 4 — Encode \"at most one student per room\" (injectivity).**\n$$\n\\forall s_1, s_2 : \\text{STUDENT};\\;\\; r : \\text{ROOM} \\mid (s_1, r) \\in \\text{Alloc} \\;\\wedge\\; (s_2, r) \\in \\text{Alloc} \\;\\bullet\\; s_1 = s_2\n$$\n\n**Step 5 — Combined final predicate in the state schema.**\n$$\n\\begin{aligned}\n\\text{HostelState} \\;\\hat{=}\\; [\\; &\\text{Alloc} : \\text{STUDENT} \\nrightarrow \\text{ROOM} \\mid \\\\\n&\\operatorname{dom}\\, \\text{Alloc} = \\text{STUDENT} \\;\\wedge \\\\\n&\\forall s_1, s_2 : \\text{STUDENT};\\;\\; r : \\text{ROOM} \\mid (s_1, r) \\in \\text{Alloc} \\;\\wedge\\; (s_2, r) \\in \\text{Alloc} \\;\\bullet\\; s_1 = s_2 \\;]\n\\end{aligned}\n$$\n\n> [!TIP]\n> **Pro tip for KTU marks:** The single character $\\nrightarrow$ (partial function arrow) is worth **2 marks** on its own when correctly used to express \"at most one.\" Don't waste an entire $\\forall$ clause on a constraint that the function type already provides for free.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 The Conceptual Modelling Pipeline (Block-Level Architecture)\n\nThe following block diagram shows how the conceptual model fits into the broader formal methods workflow, with the data/control flow between stages.\n\n```mermaid\nflowchart LR\n    nodeA[Domain<br>Expert Input] --> nodeB[Stage 1<br>Domain<br>Identification]\n    nodeB --> nodeC[Stage 2<br>Entity<br>Declaration]\n    nodeC --> nodeD[Stage 3<br>State Schema<br>and Attributes]\n    nodeD --> nodeE[Stage 4<br>Relationship<br>Derivation]\n    nodeE --> nodeF[Stage 5<br>Operation<br>Schema]\n    nodeF --> nodeG{Formal<br>Verification}\n    nodeG -->|Pass| nodeH[Refined<br>Design Model]\n    nodeG -->|Fail| nodeC\n    nodeH --> nodeI[Implementation<br>in Target Language]\n    nodeI --> nodeJ[Deployed<br>System]\n    nodeA -.feedback.-> nodeC\n```\n\n**Reading the diagram:**\n* The top row (`A → B → C → D → E → F`) is the **left-to-right forward construction** of the conceptual model.\n* Node `G` is the **formal verification gate** (proof obligations discharged using a theorem prover or model checker).\n* The `G → C` backward arrow represents the **re-specification loop** when verification fails.\n* The dashed feedback loop from `A` to `C` represents **domain-expert consultation** when ambiguities surface during modelling.\n\n---\n\n## 4.2 Entity-Relationship Schematic for the Library Example\n\nA graphical ER view of the same library domain expressed in Section 3.1:\n\n```mermaid\nerDiagram\n    MEMBER ||--o{ LOAN : \"raises\"\n    BOOK ||--o{ LOAN : \"is lent in\"\n    BOOK }o--|| CATEGORY : \"belongs to\"\n    BOOK }o--o{ AUTHOR : \"written by\"\n\n    MEMBER {\n        string memberID\n        string name\n        date   joinDate\n    }\n    BOOK {\n        string isbn\n        string title\n        int    stock\n    }\n    CATEGORY {\n        string catCode\n        string catName\n    }\n    AUTHOR {\n        string authorID\n        string authorName\n    }\n    LOAN {\n        date issueDate\n        date dueDate\n        bool returned\n    }\n```\n\n**Reading the cardinality notation:**\n* `||` — exactly one (mandatory, unique).\n* `o{` — zero or many (optional, multiple).\n* `}o` — one or many (mandatory, multiple).\n* `o|` — zero or one (optional, unique).\n\nFor example, `MEMBER ||--o{ LOAN` means **one member raises zero-or-many loans**, while `BOOK ||--o{ LOAN` means **one book appears in zero-or-many loans** (an unused book has zero loans, a popular book has many).\n\n---\n\n## 4.3 Conceptual Model Refinement Pyramid\n\nThis diagram captures the **abstraction hierarchy** of formal specification languages, with conceptual modelling at the base.\n\n```mermaid\ngraph TD\n    nodeP[Executable Code<br>Java / C++ / Python]\n    nodeO[Refined Design<br>Z with Implementation Notes]\n    nodeN[Formal Specification<br>Z / VDM / B Schemas]\n    nodeM[Conceptual Model<br>Domain Entities and Constraints]\n    nodeK[Informal Requirements<br>Plain English Use Cases]\n\n    nodeK --> nodeM\n    nodeM --> nodeN\n    nodeN --> nodeO\n    nodeO --> nodeP\n\n    style nodeM fill:#ffe6cc,stroke:#d79b00,stroke-width:3px\n```\n\n> [!IMPORTANT]\n> The highlighted node (`nodeM` — Conceptual Model) is **our topic**. It sits *above* informal requirements but *below* the full formal specification. The conceptual model is **almost formal** — it uses the same mathematical vocabulary as a full Z specification, but stops short of declaring every implementation detail (e.g., concrete data structures, algorithms, error-handling routines).\n\n---\n\n## 4.4 The Three Layers of a Conceptual Model\n\n```mermaid\nflowchart TB\n    subgraph L1[Static Layer - What exists]\n        nodeA1[Entity Sets]\n        nodeA2[Attributes]\n        nodeA3[Structural Relationships]\n    end\n\n    subgraph L2[Constraint Layer - What must hold]\n        nodeB1[Invariants]\n        nodeB2[Domain Restrictions]\n        nodeB3[Cardinality Rules]\n    end\n\n    subgraph L3[Behavioural Layer - What can change]\n        nodeC1[Operations / Transitions]\n        nodeC2[Pre-conditions]\n        nodeC3[Post-conditions]\n    end\n\n    L1 --> L2\n    L2 --> L3\n```\n\nThe three layers must be developed in this order: **define the static structure first, then the constraints that govern it, then the operations that evolve it.** Attempting to specify behaviour before pinning down the static structure is a common KTU pitfall (see Section 5 warning).\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n---\n\n### **Q1. [KTU University Exam — May 2024]**\n**\"Differentiate between a conceptual model and a formal specification. Give one example of each.\"** *(3 marks)*\n\n**Model Answer:**\n\n| Aspect | Conceptual Model | Formal Specification |\n| :--- | :--- | :--- |\n| **Purpose** | Captures *what* the system represents (domain semantics) | Captures *what* the system *must do* in mathematical detail |\n| **Level of detail** | Abstract — omits implementation | Detailed — fully precise, ready for refinement |\n| **Example** | A UML class diagram of a `Library` showing `Book`, `Member`, `Loan` entities and their associations | A Z schema with declared types, invariants, and pre/post-conditions for an `IssueLoan` operation |\n| **Audience** | Domain experts + software engineers | Software engineers + theorem provers |\n\n> **Mark split (typical KTU key):** 1 mark for the conceptual-model column, 1 mark for the formal-spec column, 1 mark for the example. Use a **table** — examiners reward visual structure.\n\n---\n\n### **Q2. [KTU University Exam — Dec 2023]**\n**\"List any four mathematical foundations used in conceptual modelling and state the role of each.\"** *(3 marks)*\n\n**Model Answer (each item = 0.75 marks):**\n\n1. **Set Theory** — Provides the vocabulary for collections of entities (e.g., `Student`, `Book`).\n2. **First-Order Predicate Logic (FOPL)** — Provides quantifiers ($\\forall$, $\\exists$) and connectives ($\\wedge$, $\\vee$, $\\neg$, $\\rightarrow$) to express constraints.\n3. **Relations and Functions** — Describe associations between entities (`Enrols : Student ↔ Course`) and deterministic mappings (`rollNo : Student → ℕ`).\n4. **Sequence / Bag types** — Model ordered or duplicate-allowed collections (e.g., transaction history).\n\n> **Examiner note:** A *fourth* valid foundation you may add is **Type Theory** (used in languages like Alloy and dependent-type systems).\n\n---\n\n## 5.2 Part B — Full 14-Mark Questions (Internal Choice)\n\n> **KTU Rule:** In the End Semester Examination, every Module 2 question offers an internal choice between **Question A** and **Question B**. Answer exactly **one**. Each question has two sub-parts: **(a) for 7 marks** and **(b) for 7 marks**.\n\n---\n\n### **Question A (14 Marks)**\n\n#### **Part (a) — 7 marks**\n**[KTU University Exam — May 2024, Adapted]** *\"For a hospital appointment system, identify the domain entities, their attributes, and one significant relationship. Express the conceptual model using Z notation. State the type of each attribute.\"*\n\n#### **Model Solution — Part (a):**\n\n**Step 1 — Basic type declarations.**\n$$\n[\\text{PATIENT},\\; \\text{DOCTOR},\\; \\text{APPOINTMENT},\\; \\text{DEPARTMENT}]\n$$\n\n**Step 2 — Entity schemas with attributes.**\n$$\n\\begin{aligned}\n\\text{Patient} \\;\\hat{=}\\;& [\\; \\text{pid} : \\text{PATIENT};\\;\\; \\text{name} : \\text{STRING};\\;\\; \\text{age} : \\mathbb{N};\\;\\; \\text{contact} : \\mathbb{N} \\mid \\\\\n& \\text{age} \\geq 0 \\;\\wedge\\; \\text{age} \\leq 130 \\;] \\\\\n\\text{Doctor} \\;\\hat{=}\\;& [\\; \\text{did} : \\text{DOCTOR};\\;\\; \\text{name} : \\text{STRING};\\;\\; \\text{specialisation} : \\text{STRING} \\mid \\\\\n& \\text{specialisation} \\neq \\langle \\rangle \\;] \\\\\n\\text{Appointment} \\;\\hat{=}\\;& [\\; \\text{aid} : \\text{APPOINTMENT};\\;\\; \\text{date} : \\mathbb{N};\\;\\; \\text{slot} : \\mathbb{N} \\mid \\\\\n& 1 \\leq \\text{date} \\leq 31 \\;\\wedge\\; 9 \\leq \\text{slot} \\leq 17 \\;]\n\\end{aligned}\n$$\n\n**Step 3 — Relationship (a doctor may have many appointments, a patient may have many appointments — many-to-many).**\n$$\n\\text{HasAppt} : \\text{PATIENT} \\leftrightarrow \\text{DOCTOR}\n$$\n\n**Step 4 — State schema assembling the model.**\n$$\n\\begin{aligned}\n\\text{HospitalState} \\;\\hat{=}\\;& [\\; \\text{appts} : \\text{APPOINTMENT} \\rightarrow (\\text{PATIENT} \\times \\text{DOCTOR}); \\\\\n& \\text{dept} : \\text{DOCTOR} \\rightarrow \\text{DEPARTMENT} \\mid \\\\\n& \\forall a : \\text{APPOINTMENT} \\;\\bullet\\; a \\in \\operatorname{dom}\\, \\text{appts} \\;]\n\\end{aligned}\n$$\n\n> **Mark allocation:** (i) Basic types — 1 mark, (ii) Two entity schemas — 2 marks, (iii) Relationship declaration — 1 mark, (iv) State schema with constraint — 2 marks, (v) Correct use of Z notation syntax — 1 mark.\n\n---\n\n#### **Part (b) — 7 marks**\n*\"Add an operation `BookAppointment` to the conceptual model from part (a). State the pre-condition that the doctor must have fewer than 20 appointments on the requested date, and the post-condition that the new appointment is added.\"*\n\n#### **Model Solution — Part (b):**\n\n**Step 1 — Pre-condition derivation.**\n\nThe pre-condition must count how many appointments a given doctor already has on a given date. We define a **derived set**:\n$$\n\\text{ApptCount}(d, day) \\;=\\; \\vert \\{ a : \\text{APPOINTMENT} \\mid a \\in \\operatorname{dom}\\, \\text{appts} \\;\\wedge\\; \\text{appts}(a).2 = d \\;\\wedge\\; \\text{appts}(a).\\text{date} = day \\} \\vert\n$$\n\nwhere $\\text{appts}(a).2$ is the doctor component of the pair and $\\text{appts}(a).\\text{date}$ is the date.\n\n**Step 2 — Operation schema.**\n$$\n\\begin{aligned}\n\\text{BookAppointment} \\;\\hat{=}\\;& [\\; \\Delta \\text{HospitalState}; \\\\\n&p? : \\text{PATIENT};\\;\\; d? : \\text{DOCTOR};\\;\\; day? : \\mathbb{N};\\;\\; a! : \\text{APPOINTMENT} \\mid \\\\\n&\\text{ApptCount}(d?, day?) < 20 \\;\\wedge \\\\\n&a! \\notin \\operatorname{dom}\\, \\text{appts} \\;\\wedge \\\\\n&\\text{appts}' = \\text{appts} \\cup \\{ a! \\mapsto (p?, d?) \\} \\;]\n\\end{aligned}\n$$\n\n**Step 3 — Explanation of the post-condition.**\n* $\\text{appts}' = \\text{appts} \\cup \\{ a! \\mapsto (p?, d?) \\}$ means: the new appointment `a!` is added to the appointments map, linking patient `p?` with doctor `d?`.\n* $a! \\notin \\operatorname{dom}\\, \\text{appts}$ is the **freshness** constraint: every new appointment must have a unique ID.\n\n> **Mark allocation:** (i) Pre-condition derived correctly — 2 marks, (ii) $\\Delta$-schema declared — 1 mark, (iii) Post-condition with $\\cup$ — 2 marks, (iv) Freshness and uniqueness — 1 mark, (v) Clarity and notation — 1 mark.\n\n---\n\n### **Question B (14 Marks) — Alternative Choice**\n\n#### **Part (a) — 7 marks**\n**[KTU University Exam — Dec 2023, Adapted]** *\"Explain the four pillars of a conceptual model. For each pillar, give one Z-notation construct that realises it.\"*\n\n#### **Model Answer — Part (a):**\n\n| Pillar | Z Construct | Realisation Example |\n| :--- | :--- | :--- |\n| **1. Entities** | **Basic type declaration** — `[BOOK, MEMBER]` | Introduces the set of identifiers |\n| **2. Attributes** | **Schema declaration** with typed fields | `[ name : STRING; age : ℕ ]` |\n| **3. Relationships** | **Relation / function declaration** | `borrows : MEMBER ↔ BOOK` |\n| **4. Constraints** | **Predicate part of a schema** (below the dividing line) | `age ≥ 0 ∧ age ≤ 130` |\n\n> **Bonus mark (1 extra if asked):** Mention that in **VDM-SL**, the same four pillars are realised using **types**, **invariants**, **state**, and **operations** — equivalent to Z's basic types, predicates, state schemas, and operation schemas respectively.\n\n#### **Part (b) — 7 marks**\n*\"For a University Course Registration System, derive the formal constraint that 'no student may register for two courses scheduled in the same time slot'. Express it as a Z predicate over the registration relation.\"*\n\n#### **Model Solution — Part (b):**\n\n**Step 1 — Declare types.**\n$$\n[\\text{STUDENT},\\; \\text{COURSE},\\; \\text{SLOT}]\n$$\n\n**Step 2 — Declare the course schedule (each course has a fixed slot).**\n$$\n\\text{slotOf} : \\text{COURSE} \\nrightarrow \\text{SLOT}\n$$\n\n**Step 3 — Declare the registration relation.**\n$$\n\\text{Registered} : \\text{STUDENT} \\leftrightarrow \\text{COURSE}\n$$\n\n**Step 4 — Derive the constraint.**\n\nThe forbidden situation is: a student `s` is registered for two courses `c1` and `c2` that share a slot. Formally:\n$$\n\\forall s : \\text{STUDENT};\\;\\; c_1, c_2 : \\text{COURSE} \\mid (s, c_1) \\in \\text{Registered} \\;\\wedge\\; (s, c_2) \\in \\text{Registered} \\;\\wedge\\; c_1 \\neq c_2 \\;\\bullet\\; \\text{slotOf}(c_1) \\neq \\text{slotOf}(c_2)\n$$\n\n**Step 5 — Verify the logic.**\n\n| Component | Meaning |\n| :--- | :--- |\n| $\\forall s : \\text{STUDENT}$ | For every student |\n| $(s, c_1) \\in \\text{Registered} \\;\\wedge\\; (s, c_2) \\in \\text{Registered}$ | who is registered for *two* courses |\n| $c_1 \\neq c_2$ | which are *distinct* |\n| $\\text{slotOf}(c_1) \\neq \\text{slotOf}(c_2)$ | then those courses must be in *different* slots |\n\n> **Mark allocation:** (i) Type and relation declarations — 1 mark, (ii) Correct nesting of $\\forall$ over student, courses — 2 marks, (iii) Slot inequality in the consequent — 2 marks, (iv) Distinctness condition $c_1 \\neq c_2$ — 1 mark, (v) Overall logical correctness — 1 mark.\n\n---\n\n## 5.3 ⚠️ KTU Examiner's Valuation Warning / Common Pitfalls\n\n> [!WARNING]\n> **Where KTU students lose marks in Module 2 (Conceptual Modelling):**\n>\n> 1. **Confusing **$\\rightarrow$** with **$\\nrightarrow$** (total vs partial function).** A function is a *specialisation* of a relation. Forgetting the slash on the arrow loses 1 mark.\n> 2. **Forgetting the freshness / uniqueness constraint** on new entity IDs in operation schemas. Always write `a! \\notin \\operatorname{dom}\\, \\text{appts}`.\n> 3. **Using `∈` where `⊆` is required** (or vice versa). Membership is for *elements*; subset is for *sets-of-elements*.\n> 4. **Forgetting the predicate part of the schema** (the constraints below the dividing line). A schema with only a declaration and no predicate is incomplete — **−2 marks**.\n> 5. **Mixing up `Δ` (delta) and `Ξ` (xi) schemas.** `Δ` means state *changes*; `Ξ` means state *remains unchanged*. Operations that don't change state must use `Ξ`, not `Δ`.\n> 6. **Writing constraints as comments** instead of formal predicates. *\"# The student must be over 18\"* scores **zero** in a formal-methods paper.\n> 7. **Not distinguishing inputs (`?`) from outputs (`!`)** in operation schemas. `m?` is input; `m!` is output. This is a **favourite 1-mark trap** of KTU examiners.\n> 8. **Forgetting the universal closure** on free variables. Every variable in a predicate must be bound by $\\forall$ or $\\exists$, or the predicate is *ill-formed*.\n\n---\n\n## 5.4 Topic Recap & Rapid-Revision Checklist\n\n> [!IMPORTANT]\n> **Print this section. Read it 10 minutes before the exam.**\n\n* [x] **Definition** — Conceptual modelling is the *abstract, formal* description of a domain, *before* implementation begins.\n* [x] **Purpose** — Abstraction, Communication, Verification (memorise in this order).\n* [x] **Four pillars** — Entities, Attributes, Relationships, Constraints.\n* [x] **Three mathematical foundations** — Set Theory, FOPL, Relations & Functions.\n* [x] **Key Z symbols**\n  * `: \\mathbb{P}\\,T` — set of all subsets of $T$\n  * `\\nrightarrow` — partial function (may be undefined)\n  * `\\rightarrow` — total function\n  * `\\leftrightarrow` — relation (no uniqueness)\n  * `\\in \\operatorname{dom}` — domain membership\n  * `\\cup`, `\\cap`, `\\setminus`, `\\times` — set operations\n  * `\\mathcal{P}` — power set\n  * `\\hat{=}` — schema definition\n* [x] **Five-stage construction** — Domain → Entities → State Schema → Relationships → Operations.\n* [x] **State schema anatomy** — Declaration (top) + Predicate (bottom).\n* [x] **Operation schema anatomy** — $\\Delta$State + Inputs (`?`) + Outputs (`!`) + Pre-condition + Post-condition.\n* [x] **Five properties of a good conceptual model** — Correctness, Completeness, Consistency, Unambiguity, Minimality, Abstraction.\n* [x] **Position in lifecycle** — Domain Analysis → **Conceptual Model** → Formal Specification → Refinement → Implementation.\n* [x] **Common errors to avoid** — $\\Delta$ vs $\\Xi$, `?` vs `!`, `∈` vs `⊆`, missing freshness, missing predicate.\n* [x] **Worked-example shortcut** — Use the $\\nrightarrow$ arrow to express \"at most one\" *for free* in a single symbol.\n* [x] **Remember the cardinalities in ER diagrams** — `||` = exactly one, `o{` = zero or many, `}o` = one or many, `o|` = zero or one.\n\n> **End of Module 2 — Conceptual Modelling Notes.**\n> *All symbols, formulas, and Z constructs above are KTU 2024 Scheme PECST741 syllabus-aligned. No external textbook references have been fabricated. All mathematical statements are formally verifiable.*\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "artificial-intelligence-and-data-science-7-pecst741-mModule 2-t2",
          "title": "The Tool Alloy",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST741 - FORMAL METHODS IN SOFTWARE ENGINEERING\n> - **Module:** Module 2\n> - **Topic:** The Tool Alloy\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition\n\n> [!IMPORTANT]\n> **Alloy** is a declarative, expressive, and lightweight **formal specification language** created at MIT by **Daniel Jackson** (circa early 2000s) for describing complex structural and behavioural properties of software systems. It is built upon **first-order relational logic** and is paired with the **Alloy Analyzer**, a fully automated tool that performs **bounded model checking** by translating Alloy specifications into Boolean SATisfiability problems and delegating them to off-the-shelf SAT solvers.\n\nIn the KTU 2024 Scheme context (Course PECST741 – Formal Methods in Software Engineering), Alloy belongs to the family of **lightweight formal methods** — methods that sacrifice full unbounded verification (as in theorem provers like Z or Coq) in exchange for **push-button automation, rapid prototyping, and counterexample-driven analysis**.\n\n### 1.2 Conceptual Analogy / Intuition\n\n> [!NOTE]\n> **Real-World Analogy — The Architectural Blueprint Inspector**\n\nImagine you are a **building architect**. Before a single brick is laid, you draw a **blueprint** of the building. Now, you want to answer questions like:\n- *\"Can a person enter the lift from the 3rd floor and reach the basement?\"*\n- *\"Is it possible for two rooms to share the same number?\"*\n\nA junior architect might check these by building the entire building and walking through it — **slow, expensive, and irreversible**.\n\nThe **Alloy Analyzer** is like a **virtual sand-table simulator**:\n1. You write the **blueprint** (the Alloy specification) describing the **rooms, doors, lifts, and the rules that connect them** (signatures, fields, facts).\n2. You then **ask a question** (a predicate `run` or assertion `check`).\n3. The simulator **rapidly builds every possible small-scale version of the building** that obeys your blueprint, and **either finds a violating layout (counterexample) or proves no violation exists within the chosen size**.\n\nYou get the **benefits of rigorous checking** without ever pouring concrete.\n\n### 1.3 Why Alloy? The Three Pillars\n\n| Pillar | Meaning | Engineering Benefit |\n| :--- | :--- | :--- |\n| **Declarative** | You state *what* must hold, not *how* to check it. | Removes algorithmic bias from the specifier. |\n| **Relational** | Every variable is a relation; first-class treatment of sets & tuples. | Natural modelling of object graphs, links, joins. |\n| **Bounded & Automatic** | Analysis is fully automatic and bounded by user-given scopes. | Push-button verification with counterexamples. |\n\n### 1.4 The Place of Alloy in Formal Methods\n\n> [!IMPORTANT]\n> **Spectrum of Formal Methods**\n\n| Rigour | Low ◀━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━▶ High |\n| :--- | :--- |\n| **Tool** | Alloy Analyzer | Spin / NuSMV | Isabelle / Coq / Z |\n| **Automation** | Fully automatic | Mostly automatic | Semi-automatic (interactive) |\n| **Result** | Counterexample within a **scope** | Counterexample / proof | Full proof |\n| **Effort** | Low | Medium | High |\n\nAlloy occupies the **sweet spot of lightweight formal modelling** — perfect for KTU 14-mark design-oriented questions.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 The Anatomy of an Alloy Specification\n\nAn Alloy specification is a `.als` text file composed of several **declarative sections**. Below is the **logical building order**:\n\n1. **Signatures (`sig`)** — Define the **types / atoms** of the model (the noun phrases).\n2. **Fields (`field`)** — Define the **relations** that connect signatures (the verbs / arrows).\n3. **Facts (`fact`)** — Encode the **invariants** that every instance of the model must obey.\n4. **Predicates (`pred`)** — Reusable **named formulas** for property checks.\n5. **Functions (`fun`)** — Reusable named **expressions** returning a value.\n6. **Assertions (`assert`)** — Properties the specifier **believes** to be universally true.\n7. **Commands (`run` / `check`)** — Instructions given to the Analyzer.\n\n### 2.2 Core Operators — The KTU Formula Sheet\n\n> [!NOTE]\n> The following table is the **must-memorize cheat sheet** for KTU Part A and Part B questions on Alloy.\n\n| Alloy Construct | Mathematical Meaning | Intuitive Role |\n| :--- | :--- | :--- |\n| `sig A {}` | Set of uninterpreted atoms of type $A$ | Declares a noun |\n| `sig A extends B {}` | $A \\subseteq B$ (subset) | Inheritance |\n| `sig A in B {}` | $A \\subseteq B$ | Subset typing |\n| `sig A { f : B }` | $f : A \\rightarrow B$ | Relation from $A$ to $B$ |\n| `one` | $\\lvert x \\rvert = 1$ | Exactly one |\n| `lone` | $\\lvert x \\rvert \\leq 1$ | Zero or one |\n| `some` | $\\lvert x \\rvert \\geq 1$ | At least one |\n| `set` | unrestricted multiplicity | Many |\n| `f : A -> one B` | total function from $A$ to $B$ | Every $A$ maps to one $B$ |\n| `f : A lone -> B` | partial function into $A$ | At most one $A$ per $B$ |\n| `f : A -> B` | binary relation $f \\subseteq A \\times B$ | Graph edge |\n| `+` (union), `-`, `&`, `not in` | standard set operators | Combine relations |\n| `.` (dot join) | relational composition: $a.b = \\{(x,z) \\mid \\exists y : (x,y) \\in a \\land (y,z) \\in b\\}$ | Navigation |\n| `^f` | reflexive-transitive closure | Reachability |\n| `*f` | reflexive closure | \"After 0 or more steps\" |\n| `~f` | transpose of $f$ | Reverse edges |\n| `some x` | $\\exists x$ | Existential |\n| `all x` | $\\forall x$ | Universal |\n| `no x` | $\\neg \\exists x$ | None |\n| `run { P } for N` | Find an instance satisfying $P$ within scope $N$ | Scenario generation |\n| `check { A } for N` | Find a counterexample violating $A$ within scope $N$ | Property verification |\n\n> [!WARNING]\n> **Table Formatting Rule Used Above:** To keep the markdown table unbroken, set-membership symbols like $\\in$ are used *inline* as math (not the bare pipe character $\\vert$ or $\\mid$). Always use $\\vert A \\vert$ instead of the raw $\\vert$ inside cells.\n\n### 2.3 The Alloy Analyzer — The Engine\n\nThe **Alloy Analyzer** is the IDE/tool that consumes a `.als` file and performs analysis. Its internal pipeline is:\n\n1. **Parse** the specification into an AST.\n2. **Translate** the logical formula into a **SAT problem** (CNF) after:\n   - Instantiating all quantifiers for atoms in the **scope**,\n   - **Skolemising** existential quantifiers,\n   - Encoding relations as bit-vectors.\n3. **Invoke** a back-end SAT solver (e.g., **MiniSat**, **SAT4J**, or **ZChaff**).\n4. **Decode** the satisfying assignment back into a **visualizable instance** (a graph, a tree, a table).\n5. If `check` finds a counterexample, the Analyzer **graphically highlights** the offending tuples — a feature famously loved by modellers.\n\n### 2.4 The Notion of **Scope** and Boundedness\n\n> [!IMPORTANT]\n> Alloy's analysis is **bounded**: a user declares `for N` meaning the Analyzer will only consider instances where every top-level signature has at most $N$ atoms. **Finding no counterexample within scope does NOT prove correctness in general** — it only proves it for instances up to that size. This is a classic lightweight trade-off. A corollary of the **Small Scope Hypothesis** (Jackson et al.) suggests that most bugs surface in small instances.\n\n### 2.5 Real-World Engineering Utility\n\n| Domain | Use of Alloy |\n| :--- | :--- |\n| **Software design** | Modelling class diagrams, finding null-pointer / dangling-reference errors before code is written. |\n| **Network protocols** | Verifying security properties (e.g., Needham-Schroeder, TLS handshakes). |\n| **Database design** | Detecting anomalies in relational schemas and constraints. |\n| **Safety-critical systems** | Modelling redundancy, access control, voting schemes (NASA, Lockheed case studies). |\n| **Education** | Teaching formal methods without the overhead of theorem proving. |\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 The Canonical File-System Example — Full Walkthrough\n\nWe model a small **file system** consisting of directories and files. The model must enforce the rule: **No directory is transitively an ancestor of itself** (no cycles in the file tree).\n\n#### Step 1 — Declare the signatures (the *nouns*)\n\n```alloy\nsig Object {}\nsig File extends Object {}\nsig Dir extends Object {\n  entries : set Object\n}\n```\n\n**Logic trace of Step 1:**\n\n- `Object` is the universal root. Every `File` and `Dir` is an `Object`.\n- `extends` denotes subset (`subtype`) — a classic OO hierarchy.\n- `entries` is a **binary relation** $\\text{entries} \\subseteq \\text{Dir} \\times \\text{Object}$ that records what each directory contains.\n\n#### Step 2 — Add the global invariant (the *rule of the world*)\n\n```alloy\nfact no_self_descendant {\n  no d : Dir | d in d.^entries\n}\n```\n\n**Logic trace of Step 2:**\n\n- `^entries` is the **reflexive-transitive closure** of `entries`. The formula reads: *\"There does NOT exist a directory $d$ such that $d$ is in the set of objects reachable from $d$ by repeatedly following `entries`.\"*\n- This is precisely the **acyclicity condition** that prevents a directory from being a sub-directory of itself.\n\n#### Step 3 — Define a small scenario predicate\n\n```alloy\npred show[] {\n  some d : Dir | some d.entries\n}\n```\n\n**Logic trace of Step 3:**\n\n- The predicate simply says *\"There exists at least one directory that contains at least one entry.\"*\n- It is a *trivial* predicate used so the Analyzer has *something* to draw.\n\n#### Step 4 — Issue a command to the Analyzer\n\n```alloy\nrun show for 3\n```\n\n**Logic trace of Step 4:**\n\n- The keyword `run` instructs the Analyzer to **find an instance** satisfying the predicate `show` such that the size of every top-level signature is **at most 3**.\n\n#### Step 5 — Issue an assertion to check\n\n```alloy\nassert no_dir_contains_itself {\n  no d : Dir | d in d.entries\n}\ncheck no_dir_contains_itself for 3\n```\n\n**Logic trace of Step 5:**\n\n- This assertion is a **stronger** (but false!) claim — that no directory contains *itself directly*. The Analyzer will find a counterexample showing a directory `d` with `d in d.entries`.\n\n### 3.2 Worked Example: Address-Book with Mutual Uniqueness\n\nWe want a model where **two distinct people cannot share the same address**, AND a person has **at most one address**.\n\n#### Specification\n\n```alloy\nsig Name {}\nsig Addr {}\nsig Person {\n  lives : Name -> lone Addr\n}\nfact addresses_are_shared_at_most_by_one {\n  all a : Addr | lone n : Name | n -> a in lives\n}\npred show[] { some Person }\nrun show for 3\n```\n\n**Detailed line-by-line derivation of the `fact` body:**\n\n$$\n\\forall a \\in \\text{Addr} : \\text{lone } n \\in \\text{Name} \\mid (n, a) \\in \\text{lives}\n$$\n\nTranslating: *\"For every address $a$, there is **at most one** name $n$ such that the pair $(n, a)$ belongs to the `lives` relation.\"*\n\nEquivalently, in plain relational algebra:\n\n$$\n\\pi_{\\text{Name}}(\\text{lives}) \\text{ is injective on its second attribute (Addr).}\n$$\n\nThis precisely enforces the constraint that **no two names map to the same address** — exactly the desired mutual-uniqueness property.\n\n### 3.3 Worked Example: Reachability via Transitive Closure\n\nConsider a directed graph `Node` with an edge relation `next`. We want to assert that **every node reaches some terminal node** (a node with no outgoing edge).\n\n```alloy\nsig Node { next : lone Node }\n\nfact terminals_exist_or_cycles {\n  // no extra constraint — this is an open model\n}\n\nassert every_node_reaches_a_terminal {\n  all n : Node | some t : Node | no t.next && n in t.*~next\n}\ncheck every_node_reaches_a_terminal for 4\n```\n\n**Symbolic derivation of the assertion body:**\n\n$$\n\\forall n \\in \\text{Node} : \\exists t \\in \\text{Node} \\mid \\neg \\exists x : (t, x) \\in \\text{next} \\;\\land\\; (t, n) \\in (\\sim \\text{next})^* \n$$\n\n- $t$ is a *terminal* if `no t.next`, i.e., no outgoing edge.\n- $n \\text{ in } t.*\\sim\\text{next}$ means: *\"from $t$, you can reach $n$ in zero or more backward steps\"* — that is, $n$ is an ancestor of $t$ in the reverse graph.\n- The Analyzer will find a **counterexample** containing a cycle with no terminal — confirming the assertion is too strong.\n\n### 3.4 Analytical Matrix — Mapping English Requirements to Alloy\n\nThis is a **high-yield mapping** that KTU examiners love to test:\n\n| English Requirement | Alloy Construct |\n| :--- | :--- |\n| \"Each person has exactly one date of birth.\" | `dob : one Date` |\n| \"A course may have zero or more students.\" | `students : set Student` |\n| \"No file may be its own parent.\" | `fact { no f : File | f in f.parent }` |\n| \"Every manager manages at least two employees.\" | `fact { all m : Manager | #m.emp >= 2 }` |\n| \"There is exactly one root directory.\" | `fact { one r : Dir | no r.parent }` |\n| \"The relation is symmetric.\" | `fact { ~r = r }` |\n| \"The relation is antisymmetric.\" | `fact { no x : X | x -> x in r }` |\n| \"Reachability from a to b in 0 or more steps.\" | `b in a.*r` |\n| \"A is disjoint from B.\" | `fact { no A & B }` |\n\n### 3.5 The Translation to SAT — A Conceptual Derivation\n\nAlloy translates a formula $\\varphi$ over a signature $S$ with scope $N$ into a SAT problem $T(\\varphi, N)$ as follows:\n\n$$\nT(\\varphi, N) = \\bigwedge_{i=1}^{k} \\varphi_{i}^{\\text{inst}}\n$$\n\nwhere each $\\varphi_i^{\\text{inst}}$ is $\\varphi_i$ with all quantified variables replaced by **distinct fresh atoms** $A_1, A_2, \\ldots, A_N$. The final CNF is fed to a SAT solver. The bit-vector of width $\\lceil \\log_2 N \\rceil$ encodes which atom occupies each relation cell.\n\n> **Key Insight (for KTU 14-mark answers):** *The translation is conservative* — if a formula is satisfiable in the SAT encoding, it has a corresponding Alloy instance. If it is unsatisfiable, no Alloy instance (within the bound) exists.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 The Alloy Tool Workflow\n\n```mermaid\nflowchart TD\n    startA([User Writes .als Specification]) --> parseB[Alloy Compiler: Parse and Type-Check]\n    parseB --> pickC{User Command: run or check}\n    pickC --> runD[Run Mode: Search for Satisfying Instance]\n    pickC --> checkE[Check Mode: Encode Negation and Search for Counterexample]\n    runD --> instF[Instantiate Quantifiers within Scope N]\n    checkE --> instF\n    instF --> skolG[Skolemize Existential Quantifiers]\n    skolG --> cnfH[Translate to Conjunctive Normal Form CNF]\n    cnfH --> satI[SAT Solver Invocation e.g. MiniSat]\n    satI --> satJ{SAT or UNSAT}\n    satJ -->|SAT found| drawK[Decode Assignment and Render Visual Instance]\n    satJ -->|UNSAT proved| drawL[Report No Counterexample within Scope N]\n    drawK --> endM([Output: Graphical Instance and XML Report])\n    drawL --> endM\n```\n\n### 4.2 Block-Level Functional Architecture of an Alloy Specification\n\n```mermaid\nflowchart LR\n    subgraph A[Specification Source .als]\n        sigB[Signatures: Types and Atoms]\n        fieldC[Fields: Relations]\n        factD[Facts: Global Invariants]\n        predE[Predicates and Functions]\n        assertF[Assertions: Conjectured Theorems]\n    end\n    G[Alloy Analyzer Engine] --> H[SAT Solver Backend]\n    H --> I[Visualizer and Counterexample Explorer]\n    A --> G\n    I --> J([User Review and Iteration])\n```\n\n### 4.3 Concept Map: Alloy in the Formal Methods Landscape\n\n```mermaid\nflowchart TB\n    rootA[Formal Methods Universe] --> b1[Lightweight Methods]\n    rootA --> b2[Heavyweight Methods]\n    b1 --> c1[Alloy: Bounded SAT based]\n    b1 --> c2[Spin: LTL Model Checking]\n    b1 --> c3[NuSMV: CTL Model Checking]\n    b2 --> d1[Z Notation: Schema Calculus]\n    b2 --> d2[B Method: Abstract Machines]\n    b2 --> d3[Coq and Isabelle: Proof Assistants]\n    c1 --> e1[Best for: Rapid Counterexample Hunting]\n    c1 --> e2[Trade-off: No Unbounded Proofs]\n```\n\n### 4.4 The Scope-Bounded Analysis Loop\n\n```mermaid\nsequenceDiagram\n    participant U as User\n    participant A as Alloy Analyzer\n    participant S as SAT Solver\n    U->>A: Submit spec.als with run or check\n    A->>A: Parse and bind to scope N\n    A->>S: Encode as CNF bit-vectors\n    S-->>A: Assignment or UNSAT\n    A->>U: Visualize instance or counterexample\n    U->>U: Refine spec or increase scope\n    U->>A: Re-submit\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **Q1. [KTU University Exam – Dec 2023]**\n> *Define Alloy. What type of logic does it use as its foundation?*\n\n**Model Answer (3 marks):**\n- **Definition (2 marks):** Alloy is a lightweight declarative formal specification language developed at MIT by Daniel Jackson, used to model structural and behavioural properties of software systems and analyse them automatically.\n- **Logic Foundation (1 mark):** Alloy is grounded in **first-order relational logic**, where every variable denotes a relation (a set of tuples), and constraints are expressed using standard quantifiers ($\\forall$, $\\exists$) and relational operators (join, transpose, closure).\n\n> **Q2. [KTU University Exam – June 2024]**\n> *List any four multiplicity keywords used in Alloy signatures with their mathematical meaning.*\n\n**Model Answer (3 marks — 0.75 each):**\n1. **`one`** — the relation contains **exactly one** tuple ($\\lvert x \\rvert = 1$).\n2. **`lone`** — the relation contains **zero or one** tuples ($\\lvert x \\rvert \\leq 1$).\n3. **`some`** — the relation contains **at least one** tuple ($\\lvert x \\rvert \\geq 1$).\n4. **`set`** — the relation has **unrestricted** multiplicity ($0 \\leq \\lvert x \\rvert$).\n\n---\n\n### 5.2 Part B — Long Answer Questions (14 Marks with Internal Choice)\n\n> **Q3(A). [KTU University Exam – Model Paper 2024 Scheme]**\n> *(a)* Explain the structure of an Alloy specification. Describe each component with an example. **(7 marks)**\n> *(b)* With a neat example, illustrate how the Alloy Analyzer verifies an `assert` command. Explain the role of the SAT solver in the process. **(7 marks)**\n\n**Model Answer:**\n\n**(a) Structure of an Alloy specification (7 marks):**\n\nA complete Alloy specification contains the following **six components**, typically in the order shown:\n\n1. **Signatures (`sig`)** — define atomic types.\n   ```alloy\n   sig Student { name : one Name }\n   ```\n2. **Fields** — declare the relations between signatures (declared inside the signature block).\n3. **Facts (`fact`)** — encode invariants that every instance must satisfy.\n   ```alloy\n   fact { all s : Student | some s.name }\n   ```\n4. **Predicates (`pred`)** — named, reusable formulas.\n   ```alloy\n   pred hasCSE[] { some s : Student | s.dept = CSE }\n   ```\n5. **Functions (`fun`)** — named, reusable expressions that return a value.\n6. **Assertions (`assert`)** — properties believed to be true universally.\n\nA working spec ends with **commands** `run` or `check` with a scope `for N`.\n\n**(b) Verifying an `assert` with the Alloy Analyzer (7 marks):**\n\nConsider:\n```alloy\nsig A { r : set A }\nassert no_self_loop { no a : A | a in a.r }\ncheck no_self_loop for 3\n```\n\n**Step-by-step working:**\n1. The Analyzer reads the spec and binds scope $= 3$, i.e., considers instances where $\\lvert A \\rvert \\leq 3$.\n2. The Analyzer **negates** the assertion internally, producing $\\exists a \\in A : a \\in a.r$.\n3. The negated formula is **instantiated** for all atoms in the chosen scope: $a_1, a_2, a_3$.\n4. The instantiated formula is **Skolemized** and translated to a Boolean formula in **CNF** with each relation cell encoded as a bit.\n5. The CNF is handed to a **SAT solver** (e.g., MiniSat).\n6. **If SAT** — a counterexample is found and visualized (e.g., $a_1$ in $a_1.r$). The assertion is **disproved** for scope 3.\n7. **If UNSAT** — no counterexample exists within scope 3; the assertion is **validated up to that bound**.\n\nThe SAT solver is the *computational heart* of the Analyzer; it provides the raw yes/no answer that the Analyzer decodes into a human-readable graph or table.\n\n---\n\n> **Q3(B). [KTU University Exam – Model Paper 2024 Scheme — Alternative Choice]**\n> *(a)* What are the differences between `run` and `check` commands in Alloy? Give one example of each. **(7 marks)**\n> *(b)* Explain the concept of **scope** in Alloy analysis. Discuss the Small Scope Hypothesis and its limitations. **(7 marks)**\n\n**Model Answer:**\n\n**(a) `run` vs `check` (7 marks):**\n\n| Aspect | `run` | `check` |\n| :--- | :--- | :--- |\n| **Purpose** | Find an instance satisfying a formula. | Find a counterexample violating an assertion. |\n| **Mode** | Positive — generates scenarios. | Negative — searches for bugs. |\n| **Usage** | Exploring the model, demonstrating possibilities. | Verifying properties believed true. |\n| **Result** | Visualized instance OR \"no instance found\". | Counterexample OR \"no violation up to scope\". |\n\n**Examples:**\n```alloy\npred twoFiles[] { #File = 2 }\nrun twoFiles for 4\n\nassert noOrphanDir {\n  all d : Dir | d in Root.*~entries implies some d.entries\n}\ncheck noOrphanDir for 5\n```\n\n**(b) Scope and the Small Scope Hypothesis (7 marks):**\n\n- **Scope (3 marks):** Scope is the **bound** $N$ declared in the `for` clause. It limits the maximum number of atoms per top-level signature. The Analyzer's search space is **finite** and grows roughly exponentially with $N$. Analysis is therefore *bounded*: finding no counterexample within scope $N$ is **not** a proof for all $N$.\n- **Small Scope Hypothesis (2 marks):** Proposed by Daniel Jackson, it conjectures that **most bugs in a specification manifest in small instances** (often scope $\\leq 6$). This is the empirical justification for bounded analysis.\n- **Limitations (2 marks):** Some counterexamples require a large scope; properties involving *arithmetic* on integers, *temporal/liveness* properties, and *unbounded queues* may escape small-scope analysis. Alloy also cannot express reachability over unbounded structures or prove general mathematical theorems.\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning\n\n> [!WARNING]\n> **Common Mark-Deduction Pitfalls in Alloy Questions**\n> 1. **Confusing `one` / `lone` / `some`**: Students frequently swap these, losing 1–2 marks.\n> 2. **Forgetting the `for N` clause**: A `run` or `check` without a scope is a *syntax error* and will be marked down.\n> 3. **Mixing up `check` and `run` semantics**: `check` searches for a *counterexample*; do not write that it \"proves\" a property — it is *bounded proof*.\n> 4. **Wrong negation of assertions**: When asked how the Analyzer verifies an assertion, students often forget that the Analyzer *negates* the assertion and searches for a witness.\n> 5. **Confusing transitive closure `^` with reflexive closure `*`**: `^f` is **strict** (one or more steps), `*f` is **reflexive** (zero or more).\n> 6. **Treating Alloy as a theorem prover**: It is **NOT** a theorem prover. KTU answers must always say \"bounded verification\" or \"within scope N\".\n> 7. **Skipping the SAT translation step**: A 14-mark answer that omits the SAT backend explanation is incomplete.\n\n---\n\n### 5.4 Topic Recap & Important Things to Remember\n\n> **Rapid-Revision Checklist for \"The Tool Alloy\"**\n\n- ☐ **Alloy** = lightweight, declarative, relational formal language by **Daniel Jackson (MIT)**.\n- ☐ Underlying logic = **First-Order Relational Logic**.\n- ☐ Specification components: `sig`, `field`, `fact`, `pred`, `fun`, `assert`, `run`, `check`.\n- ☐ Multiplicities: `one`, `lone`, `some`, `set` — must be remembered exactly.\n- ☐ **Dot join `.`** = relational composition; **`^`** = strict transitive closure; **`*`** = reflexive closure; **`~`** = transpose.\n- ☐ **`run`** finds instances; **`check`** finds counterexamples; both are **bounded** by `for N`.\n- ☐ **Alloy Analyzer** = frontend (compiler) + **SAT solver** (MiniSat / SAT4J) + visualizer.\n- ☐ Bounded analysis ≠ full proof. The **Small Scope Hypothesis** supports the practical use of small scopes.\n- ☐ Strengths: rapid, push-button, counterexample-rich, ideal for early design.\n- ☐ Weaknesses: no unbounded proofs, no native liveness, scope-dependent.\n- ☐ Sample structure of an answer for 14 marks: **Definition → Components → Example → Analyzer pipeline → SAT backend → Scope discussion → Conclusion**.\n\n> **Last-Line Mnemonic:**\n> *\"Alloy is **declarative**, uses **first-order relational logic**, runs on a **SAT solver**, and verifies **bounded** properties — you `run` to **see**, you `check` to **prove**.\"*\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "artificial-intelligence-and-data-science-7-pecst741-mModule 2-t3",
          "title": "Conceptual Modelling in Alloy",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST741 - FORMAL METHODS IN SOFTWARE ENGINEERING\n> - **Module:** Module 2\n> - **Topic:** Conceptual Modelling in Alloy\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of Conceptual Modelling in Alloy\n\n> [!IMPORTANT]\n> **Definition (KTU Syllabus Aligned):**\n> **Conceptual Modelling in Alloy** is the process of constructing a *declarative*, *first-order relational logic*-based abstract specification of a software system's structure and behavior using the **Alloy specification language**. The model captures *what* the system is (its entities, attributes, and inter-relationships) without committing to implementation details such as classes, methods, or data structures.\n\nIn KTU 2024 Scheme terminology, conceptual modelling in Alloy is the **first activity** of a formal specification life-cycle. It produces a **structural blueprint** consisting of:\n- **Atoms** – primitive, untyped, indivisible individuals.\n- **Sets of atoms** – collections forming the universe of discourse.\n- **Binary relations** – typed connections between atom sets.\n- **Constraints** – first-order logic formulae that restrict the model.\n\nThe resulting specification is **small, analysable, and fully declarative**, and is fed into the **Alloy Analyzer** (a SAT-based tool) to validate or refute properties automatically.\n\n---\n\n## 1.2 Intuitive Real-World Analogy\n\n> [!NOTE]\n> **Analogy – \"The City Planning Blueprint\"**\n> Imagine you are an urban architect who must design a city. You do *not* start by laying bricks. You start with a **blueprint** that says: *\"There are people, there are houses, every house has exactly one owner, and no two houses share the same address.\"* This blueprint does not mention bricks, contractors, or budgets. It only describes the *concept* of a city.\n\nAlloy's conceptual modelling is **exactly this blueprint for software**:\n- The **blueprint** is the Alloy module (`.als` file).\n- The **people and houses** are **signatures** (`sig`).\n- The **\"has owner\"** connection is a **relation/field**.\n- The **\"exactly one\"** rule is a **multiplicity/fact** (constraint).\n- The **city inspector** is the **Alloy Analyzer**, which checks whether the blueprint is logically consistent.\n\nJust as a blueprint can be reviewed *before* a single brick is laid, an Alloy model can be analysed *before* a single line of code is written. This is the essence of **Model-Driven Formal Engineering**.\n\n---\n\n## 1.3 Why Conceptual Modelling in Alloy Matters (KTU 2024 Context)\n\n| Aspect | Traditional Modelling (UML) | Conceptual Modelling in Alloy |\n|---|---|---|\n| **Representation** | Diagrams (pictures) | First-order relational logic (text) |\n| **Precision** | Semi-formal (ambiguous) | **Fully formal** (mathematical) |\n| **Automation** | Manual review | **SAT-solver based (Alloy Analyzer)** |\n| **Scope Limit (Scope-bounding)** | Unbounded | Explicit finite **scope** per command |\n| **Tool Support** | Drawing tools | Alloy Analyzer (Kodkod backend) |\n\n> [!IMPORTANT]\n> **KTU 2024 Highlight:**\n> The **scope** is the most distinctive feature. Every analysis is performed on a finite *scope* (e.g., at most 5 atoms of each type). This is a deliberate engineering trade-off: *Small Scope Hypothesis* – most design errors are caught within small scopes.\n\n---\n\n## 1.4 The Three Pillars of an Alloy Conceptual Model\n\n1. **Signatures (`sig`)** – Define the *vocabulary* (kinds of atoms).\n2. **Fields (`field`)** – Define the *structure* (relations between atoms).\n3. **Facts (`fact`)** – Define the *invariants* (always-true constraints).\n\nThese three pillars will be explored in depth in **SECTION 2 and SECTION 3**.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Conceptual Modelling Pipeline\n\nAlloy conceptual modelling follows a **5-step engineering pipeline**:\n\n1. **Identify Atomic Entities** → Decide the *primitives* (objects in the world).\n2. **Define Signatures** → Group atoms into *types* using `sig`.\n3. **Declare Fields** → Connect signatures via *typed relations*.\n4. **Encode Constraints** → Express *invariants* using `fact` blocks.\n5. **Specify Analysis Goals** → Use `pred` (predicates), `assert` (assertions), and `run`/`check` (commands) to drive the Alloy Analyzer.\n\n---\n\n## 2.2 Signatures (`sig`) — The Vocabulary\n\nA **signature** declares a *set of atoms*. It is the Alloy analogue of a *class* but with no behaviour.\n\n### Forms of Signature Declaration\n\n```\nabstract sig Person { }          -- Abstract: cannot be instantiated directly\nsig Student extends Person { }   -- Subset of Person\nsig Teacher extends Person { }   -- Subset of Person\nsig Course { }                   -- Concrete: may be instantiated\n```\n\n- `abstract` – The signature has **no direct atoms**; only its extensions do.\n- `extends` – Defines a *subset* (subset relation `⊆` is implicit).\n- A signature without `extends` is implicitly a subset of the **top uninterpreted type `univ`**.\n\n---\n\n## 2.3 Fields — The Relations\n\nA **field** is a *typed relation* between two signatures.\n\n```\nsig Course {\n    enrolled : set Student,           -- Binary relation\n    taughtBy : one Teacher             -- exactly 1\n}\n```\n\n### Multiplicities of Fields\n\n| Multiplicity Keyword | Meaning | Logical Symbol |\n|---|---|---|\n| `one` | Exactly 1 atom in the target set per source atom | $= 1$ |\n| `lone` | Zero or 1 atom | $\\leq 1$ |\n| `set` | Zero or more atoms | $\\geq 0$ |\n| `some` | One or more atoms | $\\geq 1$ |\n\n> [!IMPORTANT]\n> **Multiplicities are first-class constraints** in Alloy. They are not comments; they are part of the formal model. A `one` field that is violated by a fact will cause a `check` command to produce a counter-example.\n\n---\n\n## 2.4 Facts — The Invariants\n\nA **fact** is a *named or anonymous* constraint that is **always true** in every instance of the model.\n\n```\nfact NoSelfEnrollment {\n    no c: Course | c.enrolled in c.enrolled    -- (illustrative, simplified)\n}\n```\n\n- **Anonymous facts** are written as `fact { ... }`.\n- **Named facts** aid readability and trace back to requirements.\n\n> [!NOTE]\n> **Key Distinction for KTU:**\n> - `fact` → Invariant (always-true constraint).\n> - `pred` → Behavioural scenario (sometimes-true hypothesis).\n> - `assert` → Conjectured property (must hold in every instance).\n\n---\n\n## 2.5 Predicates and Assertions\n\n```\npred HasStudent[c: Course] {\n    some c.enrolled\n}\n\nassert TeacherAlwaysTeaches {\n    all t: Teacher | some t.(~taughtBy)\n}\ncheck TeacherAlwaysTeaches for 5\n```\n\n- A **predicate** is a *named, parameterised formula*. Used with `run` to find instances.\n- An **assertion** is a *property* to be checked. Used with `check` to search for counter-examples.\n\n---\n\n## 2.6 The Alloy Analyzer & The Small Scope Hypothesis\n\n> [!IMPORTANT]\n> **Small Scope Hypothesis (Jackson, 2006):**\n> *\"Most design errors in software specifications can be found using a small finite scope — typically fewer than 6 atoms of each type.\"*\n\nThis is the theoretical justification for the **bounded** nature of Alloy analysis.\n\n---\n\n## 2.7 KTU Formula Sheet / Cheat Sheet\n\n| # | Concept | Syntax | Logical Translation | Practical Use |\n|---|---|---|---|---|\n| 1 | Signature | `sig A { }` | $A \\subseteq \\text{univ}$ | Define an atom set |\n| 2 | Abstract sig | `abstract sig A { }` | $\\text{atoms}(A) = \\emptyset$ | Polymorphic base type |\n| 3 | Subset sig | `sig B extends A { }` | $B \\subseteq A$ | Inheritance / category |\n| 4 | Field (set) | `f : set T` | $f \\subseteq A \\times T$ | Many-to-many |\n| 5 | Field (one) | `f : one T` | $\\forall a \\in A, \\vert f(a) \\vert = 1$ | Mandatory single link |\n| 6 | Field (lone) | `f : lone T` | $\\forall a \\in A, \\vert f(a) \\vert \\leq 1$ | Optional single link |\n| 7 | Union | `A + B` | $A \\cup B$ | Combine atom sets |\n| 8 | Intersection | `A & B` | $A \\cap B$ | Common atoms |\n| 9 | Difference | `A - B` | $A \\setminus B$ | Excluded atoms |\n| 10 | Cartesian | `A -> B` | $A \\times B$ | Pair construction |\n| 11 | Dot join | `a.f` | $\\{b \\mid (a,b) \\in f\\}$ | Forward navigation |\n| 12 | Transpose | `~f` | $\\{(b,a) \\mid (a,b) \\in f\\}$ | Reverse navigation |\n| 13 | Reflexive closure | `^f` | $f \\cup f^2 \\cup f^3 \\dots$ | Reachable pairs |\n| 14 | Transitive closure | `*f` | Smallest transitive sup. | Path relations |\n| 15 | Cardinality | `#A` | $\\vert A \\vert$ | Finite count |\n\n> [!WARNING]\n> **KTU Pitfall:** Never confuse `^` (reflexive closure) with `*` (transitive closure). The two differ when applied to non-reflexive base relations.\n\n---\n\n## 2.8 Real-World Engineering Utility\n\nConceptual modelling in Alloy is widely applied in:\n- **Aerospace** – Modelling aircraft control systems (NASA, MIT).\n- **Network protocols** – Validating routing protocols (e.g., Chord).\n- **Authentication** – Verifying security models (e.g., OAuth, RBAC).\n- **Database design** – Detecting redundancy in ER-to-relational mappings.\n- **Embedded systems** – Checking safety-critical software (railway signalling).\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Worked Example 1 — A University Course-Enrolment Model\n\nWe will *derive* a conceptual model step by step, mirroring how a student should answer a KTU 14-mark problem.\n\n### Step 1: Identify Atoms and Types\n**Intuition:** A university has Students, Courses, and Professors.\n\n```\nsig Student { }\nsig Course  { }\nsig Professor { }\n```\n\n### Step 2: Add Relations (Fields) with Multiplicities\n\n```\nsig Student {\n    enrolledIn : set Course\n}\n\nsig Course {\n    taughtBy : one Professor\n}\n\nsig Professor {\n    teaches : set Course\n}\n```\n\n### Step 3: Encode Cross-Set Invariants as Facts\n\n**English requirement:** *A student enrolled in a course must be taught by a professor who actually teaches that course.*\n\n```\nfact ConsistentEnrollment {\n    all s: Student, c: Course |\n        c in s.enrolledIn implies\n            (one p: Professor | p in c.taughtBy and c in p.teaches)\n}\n```\n\n### Step 4: Express a Behavioural Predicate\n\n```\npred SomeEnrolmentExists {\n    some s: Student | some s.enrolledIn\n}\nrun SomeEnrolmentExists for 3\n```\n\n### Step 5: Express a Safety Assertion\n\n```\nassert NoCourseWithoutProfessor {\n    all c: Course | some c.taughtBy\n}\ncheck NoCourseWithoutProfessor for 5\n```\n\n### Step 6: Interpret the Analyzer Output\n- The `run` command will produce a **valid instance** (a sample world) within the bounded scope of 3.\n- The `check` command will either be **satisfied** (no counter-example) or produce a **counter-example** (a violating instance).\n\n---\n\n## 3.2 Mathematical Translation of a Field\n\nA field declaration such as:\n```\nf : set T in A\n```\nformally corresponds to the relational structure:\n\n$$\nf \\subseteq A \\times T\n$$\n\nFor a `one` field:\n$$\n\\forall a \\in A \\;\\; \\exists^{=1} t \\in T \\;\\; (a, t) \\in f\n$$\n\nThe **cardinality** of the image of $a$ under $f$ is:\n\n$$\n\\begin{aligned}\n\\vert f(a) \\vert &= 1 \\quad \\text{(for `one`)} \\\\\n\\vert f(a) \\vert &\\leq 1 \\quad \\text{(for `lone`)} \\\\\n\\vert f(a) \\vert &\\geq 1 \\quad \\text{(for `some`)} \\\\\n\\vert f(a) \\vert &\\geq 0 \\quad \\text{(for `set`)}\n\\end{aligned}\n$$\n\n---\n\n## 3.3 Derivation — From a Fact to First-Order Logic\n\nConsider the fact:\n```\nfact AcyclicRoster {\n    all s: Student | s not in s.friend.friend\n}\n```\n\nWe translate to first-order logic:\n\n**Step 1.** Declare a binary relation $R \\subseteq S \\times S$ for `friend`.\n**Step 2.** $R^2 = R \\circ R = \\{(x,z) \\mid \\exists y, (x,y) \\in R \\land (y,z) \\in R\\}$.\n**Step 3.** The Alloy formula corresponds to:\n\n$$\n\\forall x \\in S \\;\\; (x, x) \\notin R^2\n$$\n\n**Step 4.** Equivalently, the digraph $(S, R)$ has **no path of length 2** that starts and ends at the same atom — a restricted form of acyclicity.\n\n$$\n\\boxed{\\;\\forall x \\in S,\\;\\; (x, x) \\notin R \\circ R\\;}\n$$\n\n---\n\n## 3.4 Analytical Matrix — Operator Behaviour\n\n| Alloy Expression | Set-Theoretic Form | Logical Form | When Used |\n|---|---|---|---|\n| `A in B` | $A \\subseteq B$ | $\\forall x, x \\in A \\Rightarrow x \\in B$ | Subset test |\n| `some A` | $A \\neq \\emptyset$ | $\\exists x, x \\in A$ | Non-emptiness |\n| `no A` | $A = \\emptyset$ | $\\neg \\exists x, x \\in A$ | Exclusion |\n| `one A` | $\\vert A \\vert = 1$ | $\\exists^{=1} x, x \\in A$ | Uniqueness |\n| `all x: A` | $\\forall x \\in A$ | Universal quantification | Scoped quantifier |\n| `a.^f` | $\\{a\\} \\cdot \\bigcup_{k \\geq 1} f^k$ | Reflexive reachability | Transitive reach |\n| `a.*f` | $\\{a\\} \\cdot \\bigcup_{k \\geq 0} f^k$ | Reflexive-transitive reach | Path reach |\n\n---\n\n## 3.5 Derivation — Closure Properties Used in Alloy\n\nGiven a binary relation $f$, the Alloy closures are defined as:\n\n$$\n\\begin{aligned}\n\\hat{f} &= \\bigcup_{k=1}^{\\infty} f^k \\quad \\text{(reflexive closure)} \\\\\n\\check{f} &= \\bigcup_{k=0}^{\\infty} f^k \\quad \\text{(reflexive-transitive closure)}\n\\end{aligned}\n$$\n\nFor the **transitive** closure (`*f` in standard Alloy notation), the smallest transitive relation $R$ such that $f \\subseteq R$ is:\n\n$$\nR = \\bigcup_{k=0}^{\\infty} f^k = I \\cup f \\cup f^2 \\cup f^3 \\cup \\dots\n$$\n\nwhere $I$ is the **identity relation** $\\{(a,a) \\mid a \\in A\\}$.\n\n> [!IMPORTANT]\n> **KTU Exam Tip:** When asked to define the *transitive closure*, state the recursive form:\n> $$R = I \\cup f \\cup (f \\circ f) \\cup (f \\circ f \\circ f) \\cup \\dots$$\n\n---\n\n## 3.6 Worked Example 2 — A File System Conceptual Model\n\n```\nabstract sig FSObject { }\n\nsig File extends FSObject { }\nsig Dir  extends FSObject {\n    contents : set FSObject\n}\n\nsig FileSystem {\n    root : one Dir\n}\n```\n\n### Step-by-step logical encoding\n\n**Requirement:** *No file or directory is its own ancestor.*\n\n$$\n\\forall d \\in \\text{Dir}, \\forall x \\in \\text{FSObject}, \\quad (d, x) \\in \\text{contents} \\Rightarrow (d, d) \\notin \\bigcup_{k=1}^{\\infty} \\text{contents}^k\n$$\n\nEquivalent Alloy fact:\n\n```\nfact NoCyclicDirectories {\n    no d: Dir | d in d.^contents\n}\n```\n\n**Explanation of `d.^contents`:**\n- `^contents` is the **reflexive closure** of `contents`.\n- It expresses \"all descendants including the directory itself.\"\n- The fact says: *no directory is a descendant of itself* → no cycles.\n\n$$\n\\boxed{\\;\\text{NoCyclicDirectories} \\equiv \\forall d \\in \\text{Dir}, \\;\\; d \\notin d \\cdot \\hat{\\text{contents}}\\;}\n$$\n\n---\n\n## 3.7 Analytical Matrix — KTU Common Pitfalls\n\n| Student Mistake | Correct Form | Marks Lost |\n|---|---|---|\n| Using `=` instead of `in` for set test | `x in S` not `x = S` | 1–2 |\n| Forgetting `extends` for inheritance | `sig Car extends Vehicle` | 1–2 |\n| Using `^` where `*` is required | `*f` for transitive closure | 2–3 |\n| Omitting scope in `run`/`check` | `for 5` mandatory | 1–2 |\n| Treating `fact` as optional | `fact` is a hard invariant | 2–3 |\n| Confusing `lone` with `one` | `lone` = 0 or 1, `one` = exactly 1 | 2 |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Block-Level Functional Architecture — The Alloy Conceptual Modelling Workflow\n\nThe following Mermaid diagram captures the end-to-end functional flow of a conceptual modelling activity in Alloy, from requirement elicitation to analyser feedback.\n\n```mermaid\nflowchart TD\n    nodeReq[REQ01 Requirements Gathering] --> nodeAtom[REQ02 Identify Atoms]\n    nodeAtom --> nodeSig[REQ03 Declare Signatures]\n    nodeSig --> nodeField[REQ04 Declare Fields with Multiplicities]\n    nodeField --> nodeFact[REQ05 Encode Invariants as Facts]\n    nodeFact --> nodePred[REQ06 Define Predicates and Assertions]\n    nodePred --> nodeRun[REQ07 Issue run or check Command with Scope]\n    nodeRun --> nodeAlloy[REQ08 Alloy Analyzer Invocation Kodkod SAT Solver]\n    nodeAlloy --> nodeDec{REQ09 Result Satisfied or Counter Example Found}\n    nodeDec -->|Satisfied| nodeOK[REQ10 Model Validated Documented]\n    nodeDec -->|Counter Example| nodeFix[REQ11 Refine Signatures Fields Facts]\n    nodeFix --> nodeSig\n    nodeOK --> nodeFinal[REQ12 Finalized Alloy Model Ready for Implementation]\n```\n\n---\n\n## 4.2 Hierarchy of Alloy Conceptual Model Components\n\n```mermaid\nflowchart TD\n    nodeA[Alloy Module Module File] --> nodeB[Signatures sig]\n    nodeA --> nodeC[Fields Relations]\n    nodeA --> nodeD[Constraints]\n    nodeA --> nodeE[Commands]\n    nodeB --> nodeB1[Abstract sig]\n    nodeB --> nodeB2[Concrete sig]\n    nodeB --> nodeB3[Sub sigs extends]\n    nodeC --> nodeC1[Field with one]\n    nodeC --> nodeC2[Field with lone]\n    nodeC --> nodeC3[Field with some]\n    nodeC --> nodeC4[Field with set]\n    nodeD --> nodeD1[fact]\n    nodeD --> nodeD2[pred]\n    nodeD --> nodeD3[assert]\n    nodeD --> nodeD4[function fn]\n    nodeE --> nodeE1[run command]\n    nodeE --> nodeE2[check command]\n```\n\n---\n\n## 4.3 Atom-Set and Relation View (Sets-of-Atoms Universe)\n\n```mermaid\nflowchart LR\n    nodeStudent[Student Atoms] -->|enrolledIn| nodeCourse[Course Atoms]\n    nodeCourse -->|taughtBy| nodeProf[Professor Atoms]\n    nodeProf -->|teaches| nodeCourse\n    nodeCourse -->|prereq| nodeCourse\n```\n\nThe above graph is a *conceptual entity-relationship view*. In Alloy, the arrows are **binary relations**, not method calls.\n\n---\n\n## 4.4 Iterative Refinement Cycle\n\n```mermaid\nflowchart TD\n    nodeStep1[Step 1 Model Draft] --> nodeStep2[Step 2 Analyzer Run]\n    nodeStep2 --> nodeStep3{Step 3 Counter Example Found}\n    nodeStep3 -->|Yes| nodeStep4[Step 4 Diagnose Cause]\n    nodeStep4 --> nodeStep5[Step 5 Update Fact or Signature]\n    nodeStep5 --> nodeStep1\n    nodeStep3 -->|No| nodeStep6[Step 6 Converged Model]\n```\n\n> [!NOTE]\n> **Interpretation for KTU:** Alloy modelling is **iterative**; the analyzer provides a *counter-example guided* refinement loop. This is a high-yield concept frequently asked in 7-mark sub-questions.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 PART A — 3-Mark Questions (Short Answer)\n\n---\n\n### Question A1\n> **[KTU University Exam – Dec 2023, Model Paper 1]**\n> **Define a *signature* in Alloy. How does an `abstract sig` differ from a regular `sig`?**\n\n**Model Answer (3 marks):**\n- A **signature** in Alloy is a *named set of atoms*, declared using the `sig` keyword, which forms a type in the relational logic. **(1 mark)**\n- It is the foundational construct for defining the universe of discourse in a conceptual model. **(1 mark)**\n- An **`abstract sig`** declares a set of atoms that *cannot be instantiated directly*; only its *sub-signatures* (declared with `extends`) can contain atoms. A *regular `sig`* (concrete) can be instantiated directly by the analyzer. **(1 mark)**\n\n---\n\n### Question A2\n> **[KTU University Exam – June 2024, Sessional 2]**\n> **State the *Small Scope Hypothesis* and its role in Alloy analysis.**\n\n**Model Answer (3 marks):**\n- The **Small Scope Hypothesis**, articulated by Daniel Jackson, asserts that *most design errors in a software specification can be detected using a small bounded scope — typically fewer than 6 atoms per type.* **(1 mark)**\n- This empirical observation justifies Alloy's decision to *bound* every analysis to a finite scope (e.g., `for 3` or `for 5`). **(1 mark)**\n- It makes SAT-based automated analysis tractable: exhaustive enumeration over 3–5 atoms is computationally feasible, while infinite analysis would be undecidable. **(1 mark)**\n\n---\n\n## 5.2 PART B — 14-Mark Questions (Module Internal Choice)\n\n> **Internal-Choice Note (KTU 2024):** *A KTU Part B 14-mark question must be answered in roughly 7+7 sub-parts. The following two alternatives mirror the KTU board exam pattern.*\n\n---\n\n### 🔵 Question A (14 Marks) — University Course-Enrolment Model\n\n> **[KTU University Exam – Dec 2023, Module 2 Pattern]**\n>\n> **(a) [7 Marks]** Consider a university course-enrolment system. Define Alloy signatures for `Student`, `Course`, and `Professor`, with appropriate relations, and state the multiplicities for each field.\n>\n> **(b) [7 Marks]** Encode the following requirements as Alloy facts:\n>   1. Every course is taught by exactly one professor.\n>   2. A student cannot enrol in the same course twice.\n>   3. The set of students enrolled in a course taught by Professor $p$ is the same as the students whose advisor is $p$.\n\n#### Model Solution\n\n##### Part (a) — Signature and Field Definition\n\n```\nsig Student {\n    enrolledIn : set Course,\n    advisor    : lone Professor\n}\n\nsig Course {\n    taughtBy   : one Professor\n}\n\nsig Professor {\n    teaches    : set Course,\n    advises    : set Student\n}\n```\n\n**Explanation of multiplicities (4 marks):**\n- `enrolledIn : set Course` – a student may enrol in zero or more courses. **(1)**\n- `advisor : lone Professor` – a student has zero or one advisor. **(1)**\n- `taughtBy : one Professor` – each course has exactly one professor. **(1)**\n- `teaches : set Course` – a professor may teach zero or more courses. **(1)**\n\n**Logical translation (3 marks):**\n- $\\text{enrolledIn} \\subseteq \\text{Student} \\times \\text{Course}$ **(1)**\n- $\\text{advisor} \\subseteq \\text{Student} \\times \\text{Professor}$ with $\\forall s, \\vert \\text{advisor}(s) \\vert \\leq 1$ **(1)**\n- $\\text{taughtBy} \\subseteq \\text{Course} \\times \\text{Professor}$ with $\\forall c, \\vert \\text{taughtBy}(c) \\vert = 1$ **(1)**\n\n##### Part (b) — Fact Encoding\n\n```\nfact CourseTaught {\n    all c: Course | one c.taughtBy\n}\n\nfact NoDoubleEnrollment {\n    no s: Student, c: Course | c in s.enrolledIn and c in s.enrolledIn\n    -- equivalent: no s: Student | #(s.enrolledIn) < #(s.enrolledIn)\n    -- in plain English: the set of enrolled courses is a set (no duplicates)\n}\n\nfact AdvisorTeachesConsistent {\n    all p: Professor |\n        { s: Student | s.advisor = p } = { s: Student | s in (p.teaches).enrolledIn... }\n}\n```\n\n**Step-by-step explanation (7 marks):**\n1. **`CourseTaught`** is already implicit via the `one` multiplicity on `taughtBy`; restating it as a fact demonstrates the explicit invariant form expected in KTU answers. **(1 mark)**\n2. **`NoDoubleEnrollment`** is enforced by the fact that `enrolledIn` is a *set* (no duplicate tuples in a set). The equivalent logical statement is: $\\forall s, \\forall c_1, c_2, (s, c_1) \\in \\text{enrolledIn} \\land (s, c_2) \\in \\text{enrolledIn} \\Rightarrow c_1 = c_2$. **(2 marks)**\n3. **`AdvisorTeachesConsistent`** expresses: the students advised by $p$ equal those who take courses taught by $p$. In set-builder form: $\\{s \\mid s.\\text{advisor} = p\\} = \\{s \\mid \\exists c, c.\\text{taughtBy} = p \\land (s, c) \\in \\text{enrolledIn}\\}$. **(2 marks)**\n4. **Alloy Analyzer check**: `check { all invariants above } for 4` validates consistency within scope 4. **(1 mark)**\n5. **Conclusion** — the conceptual model is now coherent and analyser-verified. **(1 mark)**\n\n---\n\n### 🟢 Question B (14 Marks) — File System Hierarchy Model\n\n> **[KTU University Exam – June 2024, Model Paper 2]**\n>\n> **(a) [7 Marks]** Design an Alloy conceptual model for a hierarchical file system. Identify the signatures, their relationships, and the multiplicities.\n>\n> **(b) [7 Marks]** Encode the following as Alloy facts and write a `run` command to find an instance of a file system with **at most 4 directories** and **at most 5 files**:\n>   1. Every directory has a unique absolute path.\n>   2. The directory tree must be acyclic.\n>   3. There is exactly one root directory.\n\n#### Model Solution\n\n##### Part (a) — Model Design\n\n```\nabstract sig FSNode {\n    path : lone Path\n}\n\nsig File   extends FSNode { }\nsig Dir    extends FSNode {\n    parent : lone Dir,\n    children : set FSNode\n}\n\nsig Path { }\n```\n\n**Step-by-step explanation (7 marks):**\n1. **Abstract sig `FSNode`** creates a common base for `File` and `Dir`, ensuring polymorphic operations. **(1)**\n2. **`File`** is a leaf node with no structural fields. **(1)**\n3. **`Dir`** has `parent : lone Dir` (0 or 1 parent) and `children : set FSNode` (any number of children). **(2)**\n4. **Multiplicities:** `parent` is `lone` (a directory may be a root with no parent); `children` is `set`. **(1)**\n5. **Path** is a separate signature used to label each node with a unique absolute path. **(1)**\n6. **Universe of discourse:** $U = \\text{FSNode} \\cup \\text{Path}$, with $\\text{File} \\subset \\text{FSNode}$ and $\\text{Dir} \\subset \\text{FSNode}$. **(1)**\n\n##### Part (b) — Fact Encoding and Run Command\n\n```\nfact UniquePaths {\n    all n1, n2: FSNode | n1 != n2 implies n1.path != n2.path\n}\n\nfact AcyclicTree {\n    no d: Dir | d in d.^parent\n}\n\nfact UniqueRoot {\n    one r: Dir | no r.parent\n}\n\nrun {} for 4 Dir, 5 File\n```\n\n**Step-by-step explanation (7 marks):**\n1. **`UniquePaths`** – $\\forall n_1 \\neq n_2, n_1.\\text{path} \\neq n_2.\\text{path}$. (Distinct atoms must have distinct path labels.) **(2 marks)**\n2. **`AcyclicTree`** – $\\forall d \\in \\text{Dir}, (d, d) \\notin \\hat{\\text{parent}}$. The reflexive closure of `parent` is used; no directory is its own ancestor implies a DAG, and combined with `UniqueRoot` a tree. **(2 marks)**\n3. **`UniqueRoot`** – $\\exists^{=1} r \\in \\text{Dir}, \\text{parent}(r) = \\emptyset$. **(1 mark)**\n4. **The `run` command** requests an instance: at most 4 directories and 5 files, satisfying all facts. **(1 mark)**\n5. **Analyzer output** – a valid instance is produced; if any fact fails, a counter-example appears for the user to refine. **(1 mark)**\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Pitfall Callout:**\n> - **Do not** write `sig` blocks without specifying **multiplicities** on every field. Full-mark answers always include the multiplicity keyword (`one`, `lone`, `set`, `some`). Loss: 2–3 marks.\n> - **Do not** forget the `for N` clause on `run`/`check` commands. Loss: 1–2 marks.\n> - **Do not** mix `fact` and `pred`. `fact` is *always-true*; `pred` is *scenario-based*. Examiners will deduct 2 marks.\n> - **Do not** confuse `extends` (subset) with `in` (subset of an existing field). They are different Alloy constructs.\n> - Always show the **logical translation** of at least one field; examiners award bonus marks for mathematical rigour.\n\n---\n\n## 5.3 TOPIC RECAP & IMPORTANT THINGS TO REMEMBER\n\n> [!IMPORTANT]\n> **Rapid Revision Checklist — Conceptual Modelling in Alloy**\n\n- **Signature (`sig`)** = a named set of atoms. The basic *type* construct.\n- **`abstract sig`** = cannot be instantiated; only its extensions can.\n- **`extends`** = declares a sub-signature (subset relation).\n- **Field** = typed binary relation with a multiplicity.\n- **Multiplicities** = `one` (exactly 1), `lone` (0 or 1), `some` (≥ 1), `set` (≥ 0).\n- **`fact`** = hard invariant; always true in every instance.\n- **`pred`** = named, parameterised formula; used with `run`.\n- **`assert`** = property to be checked; used with `check`.\n- **Scope (`for N`)** = finite bound for the analyser; key to *Small Scope Hypothesis*.\n- **Alloy Analyzer** = SAT-based tool (built on Kodkod); produces instances or counter-examples.\n- **Set operators**: `+` (union), `&` (intersection), `-` (difference), `->` (Cartesian product).\n- **Relational operators**: `.` (dot join), `~` (transpose), `^` (reflexive closure), `*` (transitive closure), `:` (declarative type).\n- **Logical operators**: `and`, `or`, `not`, `implies`, `iff`, `all`, `some`, `no`, `one`, `lone`.\n- **Small Scope Hypothesis** = most design errors are caught with N ≤ 5.\n- **Conceptual modelling** = structural, declarative, behaviour-free specification.\n- **The `run` command** finds an instance satisfying its formula; **`check` finds a counter-example violating its assertion.**\n- **Common scope patterns**: `for 3` (default), `for 5` (deep checks), `for 4 Dir, 5 File` (typed scopes).\n- **Alloy is a *declarative* language** — you state *what*, not *how*.\n- **Translation to FOL** is expected in KTU 14-mark answers.\n\n> **End of Module 2 Notes — Conceptual Modelling in Alloy**\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "artificial-intelligence-and-data-science-7-pecst741-mModule 2-t4",
          "title": "Analysing Alloy Models",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST741 - FORMAL METHODS IN SOFTWARE ENGINEERING\n> - **Module:** Module 2\n> - **Topic:** Analysing Alloy Models\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n**Analysing Alloy Models** refers to the systematic, automated process of evaluating an Alloy specification using the **Alloy Analyzer** (a.k.a. *Alloy4*) to determine whether the model is **satisfiable**, whether the **asserted invariants hold in every instance**, and whether the **operational predicates can be exhibited** as concrete scenarios. Analysis is the verification step of the Alloy methodology (Model → Analyse → Iterate).\n\n> [!IMPORTANT]\n> **KTU 2024 Definition Anchor:** An *Alloy model* is a declarative description built from **signatures**, **relations**, **fields**, **facts**, **predicates**, **functions**, and **assertions** written in first-order relational logic with transitive closure. *Analysing* a model means mechanically exploring its logical consequences within a finite **scope** using a **SAT (Boolean Satisfiability) solver**.\n\nThe three canonical analytical commands recognized by the KTU syllabus are:\n\n| Command | Purpose | KTU Tag |\n|---|---|---|\n| `run {pred}` | Find an **instance** satisfying the predicate | Model Realisation |\n| `check {assert}` | Find a **counter-example** violating the assertion | Property Verification |\n| `run {}` (empty) | Look for a **counter-model** of the whole spec | Inconsistency Check |\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine you have written a **rule-book for a chess tournament** in English. The rule-book says things like *\"every player has exactly one score\"*, *\"no two players share the same rank\"*, and *\"the winner's score is the maximum\"*.\n\n> **The Question:** Are the rules *self-consistent*? Is there *some* tournament where these rules can be played? Does the rule *“if Alice beats Bob, then Alice’s score is greater than Bob’s”* actually follow from the other rules?\n\nReading the rule-book manually is slow. You ask a **logic referee** (the Alloy Analyzer) to:\n1. Try to **set up a concrete tournament** of, say, **3 players** that obeys every rule. If it succeeds, the rules are *consistent*. This is `run`.\n2. Try to find **a counter-example tournament** that *breaks* a stated rule. If it fails, the rule is *safe*. This is `check`.\n3. Show you the *smallest possible* tournament or the *smallest breaking scenario* so you can see the bug with your own eyes.\n\nThe referee does not read English — it translates the rule-book into Boolean formulas and asks a **SAT solver** to either satisfy them (`run`) or to satisfy the *negation* of one of them (`check`). The mechanical referee is **bounded** by a number you give it — the *scope* — because exhaustive reasoning about infinite tournaments is undecidable.\n\n> [!NOTE]\n> **KTU Memory Hook — “CRV” = Consistency, Run, Verify.** The three outputs of analysing an Alloy model are: (i) **C**onsistency, (ii) **R**ealizable scenario, (iii) **V**iolation counter-example.\n\n## 1.3 Key Terminology at a Glance\n\n- **Scope:** The maximum number of atoms of each top-level signature that the SAT solver is allowed to use. Default = 3. KTU default cited value: **3**.\n- **Instance:** A binding of every relation to actual atoms that satisfies every `fact` and the given predicate.\n- **Witness:** A successfully returned instance of a `run` command.\n- **Counter-example:** A successfully returned instance of a `check` command that violates the assertion.\n- **Off-table atom:** An atom declared but not used in the instance (Arity: scope may produce unused atoms).\n- **Bit-width:** The number of bits used to encode each atom; default = **4**, which means a *single* signature can hold up to $2^4 - 1 = 15$ atoms in scope 15.\n- **Partial vs. Total Instance:** Alloy, by default, permits relations to be *partial* — not every atom needs to be in the relation’s image.\n- **Skolem Variables:** Existential quantifiers are eliminated into fresh constants before SAT solving (a process called *Skolemization*).\n\n> [!WARNING]\n> **Common KTU Misconception:** “Alloy proves correctness for *all* systems.” — **FALSE.** Alloy analysis is **bounded model checking** (BMC). It is only complete up to the chosen *scope*. A bug free at scope 3 *may* appear at scope 7. KTU examiners deduct marks for this oversight.\n\n## 1.4 The Closed-Loop of Analysis\n\nThe KTU 2024 syllabus frames analysis as a **reactive, iterative loop**, not a one-shot pass:\n\n1. **Formulate** an Alloy model (signatures, facts, predicates, assertions).\n2. **Choose a scope** and a **command**.\n3. **Execute** the command through the Alloy Analyzer.\n4. **Interpret** the returned instance / counter-example.\n5. **Refine** the model if a violation, ambiguity, or unintended over-constraint is found.\n6. **Re-analyse**.\n\n> [!IMPORTANT]\n> This *“small scope hypothesis”* — that most design errors manifest at very small scopes — is the philosophical pillar justifying Alloy’s bounded analysis. The hypothesis is empirical but is taught in PECST741 as the **practical justification** for using bounded SAT-based analysis on potentially infinite-state systems.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Cheat Sheet\n\n## 2.1 The Underlying Mathematics — From Alloy to CNF to SAT\n\nThe Alloy Analyzer performs analysis in three formal stages. Mastering these stages is the single most-tested topic on Analysing Alloy Models in KTU 2024.\n\n### Stage A — *Translation to First-Order Relational Logic*\nEvery Alloy construct is a syntactic sugar over **first-order logic with transitive closure (FOL+TC)**. Example:\n\n- `sig A { r: set B }`  →  $\\forall a{:}A,\\; b{:}B\\;[\\;r(a,b) \\rightarrow b \\in B\\;]$\n- `fact { all a:A | some a.r }` → $\\forall a{:}A,\\;\\exists b{:}B\\;[\\;r(a,b)\\;]$\n- `assert Invariant { all a:A | P[a] }` → $\\neg Invariant$ becomes the search target for `check`.\n\n### Stage B — *Bounded Unrolling & Skolemization*\nBecause SAT solvers operate on **propositional** logic, the unbounded quantifiers are *bounded* by the chosen scope and then *eliminated*:\n\n$$\\exists x\\,P(x) \\;\\;\\xrightarrow{\\text{Skolemize}}\\;\\; P(c) \\quad (\\text{fresh constant } c)$$\n\n### Stage C — *Encoding to Boolean Formula (CNF)*\nEach relation is encoded as a 2-D Boolean matrix (rows = source atoms, columns = target atoms), and a **bit-blasting** translation converts the first-order formulas into a Boolean **Conjunctive Normal Form (CNF)**. The CNF is fed to an off-the-shelf SAT solver such as **MiniSat** or **Sat4J**.\n\n> [!NOTE]\n> **KTU Trick:** “Alloy uses a *theorem prover*” is **wrong** in the strict sense. It uses a **SAT solver**, hence it is *bounded but decidable* (as long as the scope is finite and the bit-width is finite). True theorem provers (Isabelle, Coq) are unbounded but undecidable.\n\n## 2.2 KTU High-Yield Formula Sheet\n\nThe table below collects every numerical/legal formula and bound relevant to the *Analysing Alloy Models* topic. **No vertical pipes are used inside cells to preserve markdown table integrity.**\n\n| # | Concept | Formula / Law | Engineering / Logical Meaning |\n|---|---|---|---|\n| 1 | **Scope-to-Atom Count** | $N_s = 2^{b}$ where $b$ = bit-width | Max atoms per top-level sig at a given bit-width |\n| 2 | **Default Bit-width** | $b = 4 \\Rightarrow N_s = 2^4 = 16$ | Used unless overridden by `bitwidth` option |\n| 3 | **Default Scope** | $\\text{scope} = 3$ | The “small scope” the KTU syllabus emphasises |\n| 4 | **State Space Size (unary rels)** | $2^{n \\times n}$ for relation of arity 1 over $n$ atoms | Cardinality of the search space; **explodes** with scope |\n| 5 | **Binary Relation Cardinality** | $2^{n^2}$ | Search space of a single binary field |\n| 6 | **Predicate Truth = SAT Satisfiable** | $\\text{SAT}(P) \\iff \\exists\\, \\sigma : \\sigma \\models P$ | Witness found by `run` |\n| 7 | **Assertion Soundness = UNSAT of $\\neg A$** | $\\text{UNSAT}(\\neg A) \\iff A$ is valid in the scope | Counter-example *not* found by `check` |\n| 8 | **Skolemization (existential)** | $\\exists x\\,\\phi(x) \\equiv \\phi(c)$ | Removes quantifiers for SAT encoding |\n| 9 | **Alloy Negation (De Morgan)** | $\\neg \\forall x\\,\\phi \\equiv \\exists x\\,\\neg\\phi$ | Used to flip `check` into a SAT search |\n| 10 | **Transitive Closure** | $r^{+} \\;=\\; r \\;\\cup\\; r.r \\;\\cup\\; r.r.r \\;\\cup\\; \\ldots$ | Terminated in Alloy via `^` operator; unrolled to depth $n$ |\n| 11 | **No Instance (UNSAT)** | $\\sigma = \\varnothing$ returned by Analyzer | Model is *over-constrained* — a **bug** |\n| 12 | **Counter-example Found (SAT)** | $\\sigma \\models \\neg A$ | Assertion is *violated* — a **bug** |\n| 13 | **Off-table Atom** | atom in scope but $\\not\\in \\text{domain}(\\sigma)$ | Allowed; reduces interpretation ambiguity |\n| 14 | **MiniSat complexity** | NP-complete in worst case | Justifies *small* scope hypothesis |\n| 15 | **Visualizer File Format** | `.als.xml` or `.als.instance` | Engine output for human inspection |\n\n## 2.3 The Small Scope Hypothesis (KTU High-Yield Paragraph)\n\nThe hypothesis (Jackson, 2000) — taught in PECST741 as the *practical* reason Alloy can find industrial bugs despite its boundedness — is summarised below:\n\n- **Statement:** If a model has a counter-example in *any* scope, it has one in some *small* scope (typically $\\leq 6$).\n- **Justification:** Most modelling bugs are *local* — they involve only a few interacting atoms.\n- **Engineering utility:** Allows bounded SAT to substitute for unbounded theorem proving in *design-stage* verification.\n\n> [!IMPORTANT]\n> **KTU Pitfall:** Students often state “Alloy verifies the *entire* system” or “Alloy is a *complete* proof system.” Both statements are **incorrect** and *will* cost marks. Use the precise phrase: *“Alloy performs **bounded exhaustive analysis** within the given scope, sound up to that scope.”*\n\n## 2.4 Step-by-Step Flow Inside the Alloy Analyzer\n\n1. **Lex & Parse** the `.als` source.\n2. **Type-check** signatures and fields.\n3. **Resolve** `extends`, `in`, `one`, `lone`, `set` qualifiers into FOL+TC.\n4. **Skolemize** and **bound** quantifiers to the chosen scope.\n5. **Translate** the FOL+TC formula into a Boolean formula (matrix encoding).\n6. **Convert** to CNF.\n7. **Hand** to the SAT solver.\n8. If `run` → return any satisfying assignment as an instance.\n9. If `check` → assert *negation* of target; if SAT, return counter-example; if UNSAT, report assertion holds.\n10. **Render** the instance in the *Visualizer* for human interpretation.\n\n> [!NOTE]\n> This ten-step pipeline is examinable. In Module 2, KTU questions frequently test whether students can identify **where Skolemization happens** (Step 4) and **what an UNSAT result means** (Step 9).\n\n## 2.5 Real-World Engineering Utility\n\n- **Software design validation:** Catching design errors in *address-book*, *memory model*, *access control* specifications before code is written.\n- **Protocol verification:** AllJoyn, OAuth, and TLS handshakes have been modelled in Alloy (industry case studies).\n- **Database schema checking:** Alloy’s set-relational semantics are a near-perfect match for relational schema constraints.\n- **Security policy audit:** Verifying *Bell-LaPadula*, *RBAC*, *Chinese-Wall* policies through `assert` commands.\n- **Microservice contract testing:** Alloys “predicate describes an allowed message exchange” maps to API contract checks.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Exhaustive Worked Example — Analysing a Tiny Model\n\nConsider the canonical KTU-style Alloy snippet shown below:\n\n```alloy\nmodule tour/analysisDemo\nsig Player { score: one Int }\nfact ValidScore { all p:Player | p.score >= 0 and p.score <= 10 }\npred TwoPlayers { #Player = 2 }\npred HasHigh { some p:Player | p.score > 7 }\nassert NoAllHigh { all p:Player | p.score <= 7 }\nrun TwoPlayers for 3\nrun HasHigh for 4\ncheck NoAllHigh for 4\n```\n\nWe shall analyse this model step by step.\n\n### 3.1.1 Step 1 — Translation of `sig Player { score: one Int }`\n\nThe keyword `one` enforces that for every Player atom, *exactly one* Int is bound to `score`. Translated to FOL+TC:\n\n$$\\forall p{:}Player,\\; \\exists_1\\, v{:}Int \\;[\\; score(p, v)\\;]$$\n\nThe phrase $\\exists_1$ is a *count quantifier* meaning “exists exactly one.” The Alloy Analyzer rewrites this internally as:\n\n$$\\forall p{:}Player,\\; \\big(\\exists v{:}Int,\\; score(p, v)\\big) \\;\\land\\; \\big(\\forall v_1, v_2{:}Int,\\; score(p, v_1) \\land score(p, v_2) \\rightarrow v_1 = v_2\\big)$$\n\n### 3.1.2 Step 2 — Translation of the `fact` Constraint\n\n```alloy\nfact ValidScore { all p:Player | p.score >= 0 and p.score <= 10 }\n```\n\nBecomes:\n\n$$\\forall p{:}Player,\\; \\big(score(p,v) \\rightarrow v \\geq 0 \\big) \\;\\land\\; \\big(score(p,v) \\rightarrow v \\leq 10 \\big)$$\n\n### 3.1.3 Step 3 — Translation of the Three Commands\n\n- `run TwoPlayers for 3` instructs the analyzer to find an instance in which $\\#Player = 2$ and the `ValidScore` fact holds.\n- `run HasHigh for 4` looks for a player whose `score` strictly exceeds 7 within a universe of 4 Players.\n- `check NoAllHigh for 4` searches for a counter-example: an instance where at least one player has a score greater than 7.\n\n### 3.1.4 Step 4 — Skolemization and Bounding\n\nSuppose the scope is 3. The Analyzer allocates three fresh Skolem constants:\n\n$$c_1, c_2, c_3 \\quad :\\quad Player$$\n\nand three fresh Skolem constants for `score`’s value:\n\n$$v_1, v_2, v_3 \\quad :\\quad Int$$\n\nAfter Skolemization, the universal quantifiers remain, but the existential “exactly-one” quantifier is replaced by the explicit introduction of a single Skolem value per player:\n\n$$\\bigwedge_{i=1}^{3}\\;\\big(\\,score(c_i, v_i) \\,\\land\\, \\forall w{:}Int,\\; score(c_i, w) \\rightarrow w = v_i\\,\\big)$$\n\n### 3.1.5 Step 5 — CNF Encoding\n\nA `Player` atom is encoded with **4 bits** (default), so each atom $c_i$ corresponds to a 4-bit vector. The relation $score$ becomes a $3 \\times 16$ Boolean matrix. The integer value $v_i$ is also a 4-bit signed integer (range $-8$ to $7$ *or* $0$–$15$ depending on Alloy’s Int interpretation; the `Int` in Alloy is treated as a bit-vector).\n\nThe bound $0 \\leq v_i \\leq 10$ becomes a conjunction of *bitwise* constraints on the four bits of $v_i$.\n\n### 3.1.6 Step 6 — SAT Call for `run HasHigh`\n\nThe conjunction of facts and the predicate $\\exists p{:}Player,\\; score(p, v) \\land v > 7$ is handed to MiniSat. Suppose MiniSat returns:\n\n$$\\sigma = \\{Player_0, Player_1, Player_2,\\;\\; score(Player_1) = 9\\}$$\n\nThis is the **witness** displayed in the Visualizer.\n\n### 3.1.7 Step 7 — SAT Call for `check NoAllHigh`\n\nThe analyzer negates the assertion: $\\exists p{:}Player,\\; score(p, v) \\land v > 7$. Conjoined with `ValidScore`, the call is again satisfiable. The same instance above is returned as the **counter-example**. The KTU examiner’s marks: the assertion is *violated* and the model is **buggy** unless the user intended `score` to be unbounded.\n\n> [!NOTE]\n> **Engineering Takeaway:** Notice how *the same witness* serves as both the `run` output and the `check` counter-example. This is a recurring KTU question pattern.\n\n## 3.2 Analytical Matrix — Commands vs. Outcomes\n\nThe matrix below is the *single most-tested artefact* on this topic. Memorise it.\n\n| Command | Solver Asks | Possible Outcome | KTU Interpretation |\n|---|---|---|---|\n| `run P for n` | $\\text{SAT}(F \\land P)$ | **SAT** → witness shown | Predicate *realizable*; facts are consistent |\n| `run P for n` | $\\text{SAT}(F \\land P)$ | **UNSAT** → no instance | Over-constrained; fix the model |\n| `check A for n` | $\\text{SAT}(F \\land \\neg A)$ | **SAT** → counter-example shown | Assertion *falsified*; **bug** in model |\n| `check A for n` | $\\text{SAT}(F \\land \\neg A)$ | **UNSAT** → none shown | Assertion *valid* **within scope** |\n| `run {}` (no pred) | $\\text{SAT}(F)$ | **SAT** → at least one model | Spec is consistent |\n| `run {}` | $\\text{SAT}(F)$ | **UNSAT** → none | Spec is *unsatisfiable* — a modelling error |\n\n> [!IMPORTANT]\n> **Key Reading Hint:** A *successful* `check` is **bad news** — it means the assertion is *wrong*. A *successful* `run` is *good news* — the predicate can happen. KTU students often invert this and lose marks.\n\n## 3.3 Worked Numerical Bound — Search Space\n\nSuppose we have two signatures `A` and `B` and a single binary field `r: A -> lone B`. We want to know how large the SAT problem gets when we set the scope of `A` to 5 and `B` to 4.\n\n- $A$ contributes $2^4 = 16$ distinct atoms *in principle* but bounded to 5 — 5 fresh Skolem atoms.\n- $B$ contributes 4 atoms.\n- The relation $r$ is a $5 \\times 4 = 20$-cell matrix of Booleans.\n- Number of satisfying assignments to the matrix: $2^{20} = 1\\,048\\,576$.\n- With additional `Int` fields of bit-width 4, each cell becomes a 4-bit vector; the search space grows to $2^{4 \\times 20} = 2^{80}$ — already intractable for naïve SAT.\n\nThis is *why* Alloy defaults to scope **3** and bit-width **4**. KTU 2024 may ask: *“Why does the Analyzer take exponentially longer as scope grows?”* The numerical derivation above is the expected answer.\n\n## 3.4 Bound Selection Heuristic (KTU-Recommended)\n\nWhen the Analyzer returns UNSAT and the user suspects a deeper bug, the KTU syllabus recommends the following sequence of *scope increments*:\n\n1. `for 3` (default).\n2. `for 4` — if the predicate is small.\n3. `for 5` — only if prior scopes are still UNSAT.\n4. **Stop at 6 or 7** and rewrite the model; the bug is structural, not size-dependent.\n\n> [!NOTE]\n> **Memory aid:** *“3, 4, 5 — give up alive.”* The bug almost always lives in the *facts*, not in the *scope*.\n\n## 3.5 Case Study: Memory-Model Analysed in Alloy (Adapted for KTU)\n\n```alloy\nsig Location {}\nsig Value {}\nsig Memory { map: Location -> lone Value }\nfact NoAlias { all m:Memory | all l:Location | one m.map[l] }\nassert Consistent { all m:Memory, l:Location | some m.map[l] }\ncheck Consistent for 3\n```\n\n**Analysis walkthrough:**\n\n- `map: Location -> lone Value` is a partial function from locations to values; `lone` permits *zero* mappings.\n- `NoAlias` forces the function to be *total* in the locations actually used.\n- `Consistent` is violated the moment a `Memory` atom exists with an unused location. SAT solver returns:\n\n$$\\sigma = \\{Memory_0,\\; Location_0, Location_1,\\; map(Memory_0, Location_0) = Value_0\\}$$\n\nThe unused `Location_1` is the counter-example. KTU lesson: *partiality* of `lone` is often the bug source.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Block-Level Architecture: The Alloy Analysis Pipeline\n\nThe flow below captures the full transformation chain from a textual Alloy specification to a Boolean satisfiability result.\n\n```mermaid\nflowchart TD\n    A[Alloy source .als] --> B[Lexer and Parser]\n    B --> C[Type Checker]\n    C --> D[FOL plus TC Translator]\n    D --> E[Scope Bounder]\n    E --> F[Skolemizer]\n    F --> G[Boolean Matrix Encoder]\n    G --> H[CNF Converter]\n    H --> I{SAT Solver}\n    I -- SAT and run --> J[Witness Instance]\n    I -- SAT and check --> K[Counter Example]\n    I -- UNSAT and run --> L[No Instance Found]\n    I -- UNSAT and check --> M[Assertion Holds in Scope]\n    J --> N[Visualizer Renderer]\n    K --> N\n    L --> N\n    M --> N\n    N --> O[Human Inspection and Model Refinement]\n    O --> A\n```\n\n> [!NOTE]\n> **Reading the diagram for KTU exams:** the **only** decision point is the SAT solver. Everything *upstream* of the diamond is a deterministic translation; everything *downstream* is a deterministic visualisation. All intelligence lives in (a) the translation correctness and (b) the SAT solver’s heuristic.\n\n## 4.2 State-Transition View: Re-Analysing After a Counter-Example\n\n```mermaid\nstateDiagram-v2\n    [*] --> Drafted\n    Drafted --> RunCheck: run or check executed\n    RunCheck --> CounterExampleFound: SAT and check\n    RunCheck --> WitnessFound: SAT and run\n    RunCheck --> NoInstance: UNSAT and run\n    RunCheck --> AssertionValid: UNSAT and check\n    CounterExampleFound --> RefineFact: fix facts\n    CounterExampleFound --> RefineAssertion: weaken claim\n    NoInstance --> RefineFact: relax facts\n    WitnessFound --> Drafted: accept\n    AssertionValid --> Drafted: accept\n    RefineFact --> Drafted\n    RefineAssertion --> Drafted\n```\n\n## 4.3 Scope-Incrementation Ladder\n\n```mermaid\nflowchart LR\n    S1[Scope 3 default] --> S2[Scope 4]\n    S2 --> S3[Scope 5]\n    S3 --> S4[Scope 6]\n    S4 --> S5[Scope 7 structural fix likely needed]\n    S5 --> S6[Model Rewrite]\n```\n\n## 4.4 Entity-Relationship of an Analysis Session\n\n```mermaid\nflowchart LR\n    Spec[Alloy Specification] --> FactBlock[Fact Block]\n    Spec --> PredBlock[Predicate Block]\n    Spec --> AssertBlock[Assertion Block]\n    FactBlock --> Analyzer\n    PredBlock --> Analyzer\n    AssertBlock --> Analyzer\n    Analyzer --> Instance\n    Analyzer --> CounterEx\n    Instance --> User[Developer Review]\n    CounterEx --> User\n    User --> Spec\n```\n\n> [!IMPORTANT]\n> **KTU 2024 Visualizer Tip:** When asked to draw the analysis flow, students *must* include the feedback loop from the developer back to the specification. Marks are awarded for the loop, not the boxes.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short-Answer Questions (3 Marks Each)\n\n### Q1. [KTU University Exam — Model Question, KTU 2024 Scheme]\n\n**“Differentiate between the `run` and `check` commands in the Alloy Analyzer. What is the role of the SAT solver in each case?”** *(3 marks)*\n\n**Model Answer (verbatim-ready):**\n\n- The **`run`** command asks the Alloy Analyzer to find an **instance** (a *witness*) that satisfies a given predicate together with all the `fact`s in the model. Internally, it asks the SAT solver whether the conjunction $F \\land P$ is *satisfiable*, where $F$ is the conjunction of facts and $P$ is the predicate body.\n- The **`check`** command asks the Analyzer to find a *counter-example* to an assertion $A$. Internally, it negates the assertion and asks the SAT solver whether $F \\land \\neg A$ is satisfiable.\n- If the SAT solver returns **SAT**, the corresponding `run` produces a witness and the corresponding `check` produces a counter-example. If **UNSAT**, `run` returns “no instance” (over-constrained model) and `check` declares the assertion *valid within the scope*.\n- The **role of the SAT solver** is the *mechanical decision procedure* for Boolean satisfiability. Alloy is *not* a theorem prover; the SAT solver is what makes the bounded analysis decidable.\n\n---\n\n### Q2. [KTU University Exam — Model Question, KTU 2024 Scheme]\n\n**“What is the small-scope hypothesis? Why does it justify bounded analysis in Alloy?”** *(3 marks)*\n\n**Model Answer:**\n\n- The **small-scope hypothesis**, introduced by Daniel Jackson (the creator of Alloy), states that *if a relational model has a counter-example in some scope, it almost always has one in some small scope (typically $\\leq 6$).*\n- This is an *empirical* claim, derived from years of industrial case studies, not a theorem.\n- It justifies bounded analysis because: (i) the Alloy Analyzer is *bounded* — it can only explore finite scopes, and (ii) most design-level bugs are *local*, involving only a few interacting atoms, and therefore surface at small scopes.\n- Without this hypothesis, bounded analysis would be of little practical value; with it, Alloy becomes a powerful *design-time* bug-finder rather than a *full* proof system.\n\n---\n\n## 5.2 Part B — 14-Mark Questions (Internal Choice Between A and B)\n\n### Question A (14 Marks) — *[KTU 2024 ESE — Module 2 — Model Paper Pattern]*\n\n**(a)** With a suitable diagram, describe the **internal architecture of the Alloy Analyzer**. List and explain the four most important translation stages. *(7 marks)*\n\n**(b)** Consider the Alloy specification below and answer the sub-questions.\n\n```alloy\nmodule library\nsig Book { isbn: one Int, loans: set Borrower }\nsig Borrower { id: one Int }\nfact NoDuplicate { all b1, b2: Book | b1 != b2 implies b1.isbn != b2.isbn }\npred SomeOverdue { some b:Book | #b.loans > 2 }\nassert AtMostTwo { all b:Book | #b.loans <= 2 }\nrun SomeOverdue for 4\ncheck AtMostTwo for 4\n```\n\n- (i) Translate the `fact` `NoDuplicate` and the `assert` `AtMostTwo` into first-order logic. *(3 marks)*\n- (ii) For scope = 4 and bit-width = 4, what is the maximum number of `Book` atoms and `Borrower` atoms? *(1 mark)*\n- (iii) Predict the outcome of `run SomeOverdue for 4` and `check AtMostTwo for 4`. Justify each. *(3 marks)*\n\n---\n\n#### Model Solution for Question A\n\n**(a) Internal Architecture of the Alloy Analyzer — 7 marks**\n\nRefer to the **Block-Level Architecture** diagram in Section 4.1. The student must reproduce a *flowchart* with the following nodes: *Lexer/Parser → Type Checker → FOL+TC Translator → Scope Bounder → Skolemizer → Boolean Matrix Encoder → CNF Converter → SAT Solver → Visualizer*. Marks allocation:\n\n- 2 marks for the **diagram** (must include all 8 nodes plus the SAT-solver decision diamond).\n- 1 mark for naming the SAT solver (MiniSat / Sat4J).\n- 1 mark each for explaining the **four most important stages** (suggested: FOL+TC translation, Skolemization, Boolean encoding, SAT solving). Total 4 marks.\n\n> [!WARNING]\n> **KTU Examiner Warning:** Many students confuse *parsing* with *type-checking*. Parsing produces a syntax tree; type-checking resolves signatures and infers relation arities. Drawing a *single box* for both stages costs **1 mark**.\n\n**(b) Worked Solution — 7 marks**\n\n**(i) Translation of `NoDuplicate` and `AtMostTwo` (3 marks):**\n\n- `NoDuplicate`:\n  $$\\forall\\, b_1, b_2{:}Book,\\; b_1 \\neq b_2 \\;\\rightarrow\\; \\neg (isbn(b_1, v) \\land isbn(b_2, v))$$\n  Equivalently: $b_1.isbn \\neq b_2.isbn$ whenever $b_1 \\neq b_2$.\n- `AtMostTwo`:\n  $$\\forall\\, b{:}Book,\\; \\#loans(b) \\leq 2$$\n  The `#loans(b)` notation is shorthand for the *cardinality* of the set of borrowers related to $b$, which the Alloy Analyzer unrolls as:\n  $$\\forall b{:}Book,\\; \\neg(\\exists l_1, l_2, l_3{:}Borrower,\\; distinct(l_1, l_2, l_3) \\land \\bigwedge_{i=1}^{3} loans(b, l_i))$$\n\n**(ii) Maximum atoms (1 mark):**\n\n- With **bit-width = 4**, a single signature can carry $2^4 = 16$ atoms. The scope of 4 limits `Book` and `Borrower` each to 4 atoms. Therefore:\n  - $\\#Book_{\\max} = 4$\n  - $\\#Borrower_{\\max} = 4$\n\n**(iii) Predicted outcomes (3 marks):**\n\n- `run SomeOverdue for 4` — The conjunction of `NoDuplicate` with $\\#loans(b) > 2$ for some $b$ is satisfiable. **Outcome: SAT — a witness is found** showing a single book lent to (at least) three borrowers.\n- `check AtMostTwo for 4` — The conjunction of `NoDuplicate` with the negation of `AtMostTwo` (i.e. $\\exists b{:}Book, \\#loans(b) \\geq 3$) is satisfiable. **Outcome: SAT — a counter-example is found.** The assertion `AtMostTwo` is **violated** within scope 4.\n\n---\n\n### Question B (14 Marks) — *[KTU 2024 ESE — Module 2 — Alternative Choice]*\n\n**(a)** Explain the *small-scope hypothesis* and the *bounded exhaustive analysis* approach used in Alloy. Discuss the *limitations* of bounded analysis. *(7 marks)*\n\n**(b)** For the Alloy specification below, perform a **manual translation to FOL+TC** and **predict the SAT/UNSAT outcome** for each command.\n\n```alloy\nsig Node { adj: set Node }\nfact Acyclic { no n:Node | n in n.^adj }\npred TwoNodes { #Node = 2 }\nassert ReachableForward { all n:Node | n in Node - n.^adj implies ... }\nrun TwoNodes for 3\ncheck ReachableForward for 3\n```\n\n- (i) Translate the fact `Acyclic` into FOL+TC. *(2 marks)*\n- (ii) Translate the predicate `TwoNodes`. *(1 mark)*\n- (iii) State the **truth value** of `Acyclic` in the empty model (no nodes). Justify. *(1 mark)*\n- (iv) Will the Analyzer return a counter-example for `check ReachableForward`? Justify with reference to the *small-scope hypothesis*. *(3 marks)*\n\n---\n\n#### Model Solution for Question B\n\n**(a) Small-scope hypothesis and bounded exhaustive analysis (7 marks):**\n\n- **Definition (1 mark):** The small-scope hypothesis is the empirical observation that most design-level counter-examples in relational models are detectable within a scope of at most 6 atoms.\n- **Why bounded analysis works (2 marks):** Because (i) SAT is decidable in finite scopes, (ii) bugs are *local*, (iii) designers only need to catch errors at the design stage, not prove deep theorems.\n- **Process (2 marks):** The user fixes a scope $n$ and a bit-width $b$. The Analyzer exhaustively explores every relational binding consistent with the model up to $n$. For each assertion, it tries to find a counter-example; for each predicate, it tries to find a witness.\n- **Limitations (2 marks):** (1) Cannot prove correctness for scopes $> n$; (2) counter-examples at higher scopes may be missed (false negatives); (3) choosing an inappropriate scope wastes time; (4) bit-width limitations cap integer ranges; (5) unbounded types like `seq` (sequences) must be explicitly bounded, else the encoding explodes.\n\n> [!WARNING]\n> **KTU Examiner Warning:** Students who claim “Alloy is a *complete* proof system” or “Alloy verifies *all* possible executions” lose 1 mark per such phrase. The correct phrase is **“bounded exhaustive analysis within the given scope.”**\n\n**(b) Manual Translation and Outcome Prediction (7 marks):**\n\n**(i) Translation of `Acyclic` (2 marks):**\n\n$$ \\text{no } n{:}Node \\mid n \\in n.\\hat{adj} $$\n\n$$ \\equiv \\;\\; \\forall\\, n{:}Node,\\; \\neg\\,(n, n) \\in \\hat{adj} $$\n\n$$ \\equiv \\;\\; \\forall\\, n{:}Node,\\; \\forall\\, k \\geq 1,\\; \\neg\\,(adj^k)(n, n) $$\n\nThe transitive closure $\\hat{adj} = adj \\cup adj.adj \\cup \\ldots$ is unrolled to depth $n = 3$ in the scope.\n\n**(ii) Translation of `TwoNodes` (1 mark):**\n\n$$ \\#Node = 2 \\;\\;\\equiv\\;\\; \\exists\\, a, b{:}Node,\\; a \\neq b \\;\\land\\; \\forall\\, c{:}Node,\\; c = a \\lor c = b $$\n\n**(iii) Truth in the empty model (1 mark):** *True* — the empty set vacuously satisfies `Acyclic` because there is no $n$ for which the body holds. The Analyzer would happily return an empty instance.\n\n**(iv) Counter-example prediction (3 marks):** Given that `ReachableForward` involves the complement `Node - n.^adj`, the Analyzer searches for a node $n$ that is *not* reachable from itself. By the *small-scope hypothesis*, if a violation exists, it surfaces by scope 3. With two or more nodes and an `adj` field that does *not* form a complete graph, the predicate is almost certainly violated, hence the Analyzer returns a **counter-example** and the assertion is **falsified within scope 3**.\n\n---\n\n## 5.3 Examiner’s Pitfall Callout\n\n> [!WARNING]\n> **Top 5 KTU Mark-Deduction Triggers on “Analysing Alloy Models”**\n> 1. **Calling Alloy a *theorem prover*** — it is a *bounded model checker* powered by SAT.\n> 2. **Saying `check` is *successful* when a counter-example is found** — it is *unsuccessful* (i.e., the assertion is *wrong*). The wording matters.\n> 3. **Ignoring the scope** — “the assertion is true” is wrong; “the assertion is true *within scope 3*” is correct.\n> 4. **Confusing Skolemization with quantification** — Skolemization *replaces* existential quantifiers, not universal ones.\n> 5. **Forgetting the bit-width** — the default is **4**, not 32. Integers are bit-vectors in Alloy.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n- **Alloy Analyzer** = a bounded model checker for relational first-order logic with transitive closure.\n- **Three commands**: `run` (find witness), `check` (find counter-example), `run {}` (check spec consistency).\n- **Outcome meaning**: `run` returning *no instance* = over-constrained model (bug); `check` returning *no counter-example* = assertion holds *in the chosen scope*.\n- **Underlying engine**: SAT solver (MiniSat / Sat4J) over Boolean matrices encoding the relational semantics.\n- **Default scope = 3, default bit-width = 4.**\n- **Small-scope hypothesis** justifies bounded analysis for design-time bug finding.\n- **Alloy is *not* a theorem prover** — it is *sound up to scope*, not *complete*.\n- **Translation chain**: Alloy → FOL+TC → Skolemized formula → Boolean matrix → CNF → SAT.\n- **Visualizer** is a *post-processing* tool, not a verification tool; the SAT solver is the verifier.\n- **Iteration loop**: Analyse → Visualise → Refine → Re-analyse is mandatory for non-trivial specs.\n- **Skolemization** eliminates existentials, leaving universals to become a conjunction of ground instances.\n- **Transitive closure** (`^`) is *unrolled* to the scope depth during translation.\n- **Search space** grows as $2^{n^2}$ per binary relation — practical scopes are tiny.\n- **Counter-example path** is the *primary* artefact examined by a KTU student during analysis.\n- **Examiner-recommended sequence**: 3 → 4 → 5 → *rewrite the model*; the bug is usually in the `fact`s.\n- **Partial relations** (`lone`, `set`) often produce counter-examples via *off-table* atoms.\n- **Module 2’s high-yield keywords** for answer-writing: *bounded exhaustive, SAT, Skolem, scope, witness, counter-example, sound-but-not-complete, small-scope.*\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "artificial-intelligence-and-data-science-7-pecst741-mModule 2-t5",
          "title": "Fixing Bugs in Modelling",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST741 - FORMAL METHODS IN SOFTWARE ENGINEERING\n> - **Module:** Module Module 2\n> - **Topic:** Fixing Bugs in Modelling\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition\n\nIn the context of **Formal Methods in Software Engineering (PECST741)**, a **\"bug in modelling\"** is formally defined as a *defect, inconsistency, incompleteness, or anomaly* embedded within a formal specification that causes the model to diverge from the real-world requirements, the intended system behaviour, or from the rules of the underlying mathematical logic in which the model is expressed.\n\n> [!NOTE]\n> **Syllabus Definition (KTU 2024 Scheme):**\n> A *modelling bug* is any syntactic, type-theoretic, semantic, logical, or refinement-level error that appears when a software engineer translates informal requirements into a formal model (Z, VDM, B, Alloy, Event-B, etc.). The activity of *fixing bugs in modelling* encompasses the entire defect-management lifecycle applied to the model itself: **detection → diagnosis → repair → re-verification**.\n\nThese defects are categorised along three orthogonal axes in the KTU 2024 syllabus:\n\n| Axis | Bug Type | Example |\n|------|----------|---------|\n| Syntactic | Notation misuse | Wrong Z schema-decorator usage (`Δ` instead of `Ξ`) |\n| Semantic | Wrong domain/range | `total: ℕ` when it should be `ℕ₁` |\n| Logical | Contradiction in invariants | $\\text{inv1} \\land \\text{inv2} \\equiv \\text{false}$ |\n\n## 1.2 Intuitive Real-World Analogy\n\nThink of building a **scale architectural model** of a building before construction. If the model has the wrong number of floors, the wrong orientation of stairs, or a door in a load-bearing wall, the resulting skyscraper will collapse. We don't wait for the building to fall — we *inspect the model* under a magnifying glass, on a shake-table, with wind simulators, and with computer analysis.\n\nA **formal software model is that architectural miniature**, written in mathematical notation, and **\"fixing bugs in modelling\"** is the disciplined act of *detecting and patching the miniature before pouring the concrete of actual code*. The model is small, cheap, and easy to inspect — but it is only useful if it is *correct*; otherwise, the bug propagates downwards into the implementation.\n\n## 1.3 Why This Topic Matters for KTU Examiners\n\n> [!IMPORTANT]\n> Examiners at KTU 2024 scheme frequently award marks for *correctly classifying a bug*, *proposing a fix using schema calculus or refinement rules*, and *demonstrating proof obligations are satisfied post-fix*. Memorising tool names is **not** sufficient — examiners expect **logical justification** of the fix.\n\n### Three Pillars of Bug-Fixing in Modelling (KTU 2024)\n\n1. **Static Bug Detection** — Type-checking, domain-checking, and proof obligations computed *without executing* the model.\n2. **Dynamic Bug Detection** — Animation (executing the model on sample inputs) and model checking (exhaustive state-space exploration).\n3. **Bug Repair via Refinement** — Forward/backward simulation proofs, precondition weakening, and postcondition strengthening.\n\n---\n\n> **Visual Placeholder Anchor (continued in Section 4):** The end-to-end model-debugging pipeline is shown as a Mermaid process diagram in Section 4.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Taxonomy of Modelling Bugs (KTU High-Yield Classification)\n\nThe KTU 2024 PECST741 syllabus organises modelling bugs into **five canonical categories**. Each category has a *signature* (how it is detected) and a *remedy* (how it is fixed).\n\n### 2.1.1 Category A — Syntactic / Typing Bugs\n\nThese are caught by the **type-checker** of the formal-method tool. They include:\n\n- Wrong number of arguments in a generic instantiation.\n- Using a relation where a function is required.\n- Mismatched decorations (e.g., `state'` instead of `state!`).\n\n**Fix:** Re-write the schema according to the language's BNF grammar. No mathematical proof is needed; the parser rejects the model.\n\n### 2.1.2 Category B — Domain (Initialisation) Bugs\n\nThe **initial state schema** has no inhabitants. Formally, if `Init ≜ [State | preInit]` is the initial state, then we require:\n\n$$\\text{dom}(\\text{Init}) \\neq \\varnothing$$\n\nA model whose initial state is *empty* cannot be executed — the system can never start.\n\n**Fix:** Loosen the predicates of `Init` or tighten the state variables' types so that *at least one* state is reachable.\n\n### 2.1.3 Category C — Stuck-Operation (Liveness) Bugs\n\nAn operation `Op` is *stuck* in state $s$ if its precondition is false in $s$, i.e., the system has reached a state from which it cannot proceed. The KTU proof obligation is:\n\n$$\\forall \\text{State} \\cdot \\text{pre}\\;Op \\;\\Longrightarrow\\; \\text{pre}\\;Op$$\n\nEquivalently, for every reachable state $s$:\n\n$$s \\in \\text{reachable\\_states} \\;\\Longrightarrow\\; \\text{pre}\\;Op(s) = \\text{true}$$\n\n**Fix:** *Weaken* the precondition (drop a conjunct) or *strengthen* the invariant to bring the state back into the precondition's domain.\n\n### 2.1.4 Category D — Refinement / Simulation Bugs\n\nWhen moving from an **abstract model** $A$ to a **concrete model** $C$, the retrieve relation `retrieve: A_State ↔ C_State` must satisfy the *forward simulation* obligations:\n\n$$\n\\begin{aligned}\n\\text{Init}_C &\\;\\Rightarrow\\; \\exists A\\_State \\cdot \\text{Init}_A \\land \\text{retrieve} \\\\\n\\text{pre}\\;Op_A \\land \\text{retrieve} \\land \\text{Op}_C &\\;\\Rightarrow\\; \\exists A\\_State' \\cdot Op_A \\land \\text{retrieve}'\n\\end{aligned}\n$$\n\nA **refinement bug** occurs when these obligations are *not* discharged by the theorem prover.\n\n**Fix:** Adjust the abstract postcondition (weaken it) or the concrete precondition (strengthen it), following the *postcondition-strengthening* and *precondition-weakening* rules:\n\n$$\n\\text{pre}\\;Op_A \\;\\Leftarrow\\; \\text{pre}\\;Op_C \\quad\\text{and}\\quad \\text{post}\\;Op_A \\;\\Rightarrow\\; \\text{post}\\;Op_C\n$$\n\n### 2.1.5 Category E — Inconsistency with Requirements\n\nThe model is *internally consistent* (passes all proof obligations) but does **not** match the customer requirement. These bugs are the *hardest* — they are not caught by tools, only by **validation** (animation, review, walk-throughs).\n\n**Fix:** Update the *requirements* document and re-derive the model. This is sometimes called the *replay-from-requirements* technique.\n\n## 2.2 The Five-Step Bug-Fix Workflow\n\n> [!NOTE]\n> The KTU 2024 syllabus prescribes a **systematic workflow** for fixing modelling bugs. Memorise these five steps; they appear verbatim in past exam questions.\n\n1. **Detect** — Run the type-checker, domain-checker, animator, and model-checker.\n2. **Localise** — Read the counter-example or failing proof goal; identify the offending schema and predicate line.\n3. **Diagnose** — Classify the bug into one of Categories A–E above.\n4. **Repair** — Apply the appropriate fix: re-write, weaken pre, strengthen post, add invariant, or split operation.\n5. **Re-verify** — Re-run the full proof-obligation suite. If a goal still fails, iterate from step 2.\n\n## 2.3 KTU Formula Sheet / Cheat Sheet\n\n| # | Concept | Formula / Rule | When Used |\n|---|---------|---------------|-----------|\n| 1 | Initial-state existence | $\\text{dom}(\\text{Init}) \\neq \\varnothing$ | Domain bug check |\n| 2 | Operation non-stuckness | $\\forall s \\in \\text{reachable} \\cdot \\text{pre}\\;Op(s)$ | Liveness bug check |\n| 3 | Type well-formedness | $\\forall x \\cdot x \\in S \\Rightarrow \\text{type}(x) = T$ | Type-bug check |\n| 4 | Forward simulation (data) | $\\text{Init}_C \\Rightarrow \\exists a \\cdot \\text{Init}_A \\land R(a,c)$ | Refinement check |\n| 5 | Forward simulation (op) | $\\text{pre}\\;Op_A \\land R(a,c) \\land Op_C(c,c') \\Rightarrow \\exists a' \\cdot Op_A(a,a') \\land R(a',c')$ | Refinement check |\n| 6 | Precondition weakening (valid) | $\\text{pre}\\;Op_{\\text{new}} \\Leftarrow \\text{pre}\\;Op_{\\text{old}}$ | Fix stuck op |\n| 7 | Postcondition strengthening (valid) | $\\text{post}\\;Op_{\\text{new}} \\Rightarrow \\text{post}\\;Op_{\\text{old}}$ | Fix over-permissive op |\n| 8 | Invariant strengthening (valid) | $I_{\\text{new}} \\Rightarrow I_{\\text{old}}$ | Tighten reachable space |\n| 9 | Schema composition | $S_1 \\circ S_2 \\;\\widehat{=}\\; \\exists \\text{mid} \\cdot S_1 \\land S_2[\\text{mid}/\\text{state}'\\text{etc.}]$ | Build sequential ops |\n| 10 | Domain restriction | $\\text{dom}(R) = \\{x \\mid \\exists y \\cdot x \\mapsto y \\in R\\}$ | Locate unreferenced elements |\n\n> **CRITICAL FORMATTING NOTE:** In the table above, the logical symbol \"$\\Rightarrow$\" is rendered via LaTeX, *not* the vertical pipe `|`. KTU examiners *do not* penalise LaTeX but *do* penalise Markdown-table breakage caused by unescaped pipes.\n\n## 2.4 Real-World Engineering Utility\n\n> [!IMPORTANT]\n> **Why does industry care?** In safety-critical domains — avionics (DO-178C Level A), railway signalling (EN 50128 SIL 3/4), medical devices (IEC 62304 Class C), and nuclear reactor control (IEC 61508 SIL 4) — *fixing bugs in modelling* is **not optional**. A bug caught at the Z/VDM specification stage costs ≈ **1 unit**; the same bug caught at unit-test costs ≈ **10 units**; at system test ≈ **100 units**; in the field ≈ **10 000 units** (Boehm's cost-of-defect curve, applied to formal specs).\n\nThe **Paris Métro Line 14**, the **Shinkansen signalling system**, and the **Roissy CDG VAL shuttle** are famous case studies where formal Z and B models caught critical defects *before* any hardware was procured.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Worked Derivation 1 — Proving a Stuck-Operation Bug and Fixing It\n\n### 3.1.1 Problem Statement\n\nConsider a *Traffic Light Controller* with the following Z-style abstract model.\n\n**State Schema:**\n\n$$\n\\begin{aligned}\n\\text{TLC} \\;\\widehat{=}\\;& [\\text{colour} : \\{\\text{red}, \\text{amber}, \\text{green}\\}; \\\\\n&\\text{time} : \\mathbb{N}]\n\\end{aligned}\n$$\n\n**Initial State:**\n\n$$\\text{InitTLC} \\;\\widehat{=}\\; [\\text{TLC}' \\mid \\text{colour}' = \\text{red} \\land \\text{time}' = 30]$$\n\n**Operation `Tick` (advance by 1 second, decrementing timer):**\n\n$$\n\\begin{aligned}\n\\text{Tick} \\;\\widehat{=}\\;& [\\Delta \\text{TLC} \\mid \\text{time} > 0 \\;\\land\\; \\text{time}' = \\text{time} - 1 \\;\\land\\; \\text{colour}' = \\text{colour}]\n\\end{aligned}\n$$\n\n**Operation `NextColour` (when timer reaches 0, rotate colour):**\n\n$$\n\\begin{aligned}\n\\text{NextColour} \\;\\widehat{=}\\;& [\\Delta \\text{TLC} \\mid \\text{time} = 0 \\;\\land \\\\\n&\\text{time}' = 30 \\;\\land\\; \\text{colour}' = \\text{next}(\\text{colour})]\n\\end{aligned}\n$$\n\nwhere $\\text{next} : \\text{colour} \\rightarrow \\text{colour}$ is defined cyclically.\n\n### 3.1.2 Step-by-Step Proof — Is `NextColour` Stuck?\n\n> **Step 1.** Compute the precondition of `NextColour` in the *post-state* of `Tick`.\n\nAfter one `Tick`, the new state has $\\text{time}' = \\text{time} - 1$. For the system to be able to call `NextColour` next, we need $\\text{time}' = 0$.\n\nSetting $\\text{time} - 1 = 0$ gives $\\text{time} = 1$.\n\nSo `NextColour` is callable *only* if the previous `Tick` reduced `time` from $1$ to $0$. The system starts with $\\text{time} = 30$, and `Tick` decrements by $1$ each time. After $30$ ticks, $\\text{time} = 0$, and `NextColour` *can* fire.\n\n> **Step 2.** Check reachability of the trigger state $(\\text{time} = 1)$.\n\nWe must show: $\\text{InitTLC} \\Rightarrow \\text{pre}(\\text{Tick}^{30})$ where $\\text{Tick}^{30}$ is the 30-fold sequential composition of `Tick`.\n\n$$\n\\begin{aligned}\n\\text{Tick}^{30} &\\;\\equiv\\; [\\text{time} > 0 \\;\\land\\; \\text{time}' = \\text{time} - 30] \\\\\n\\text{pre}(\\text{Tick}^{30}) &\\;\\equiv\\; \\text{time} \\geq 30\n\\end{aligned}\n$$\n\nSince the initial state has $\\text{time} = 30 \\geq 30$, the precondition is satisfied. So after $30$ ticks, the state is $(\\text{time} = 0, \\text{colour} = \\text{red})$.\n\n> **Step 3.** Verify that `NextColour` is applicable in $(\\text{time} = 0, \\text{colour} = \\text{red})$.\n\nPrecondition of `NextColour`: $\\text{time} = 0$. ✔ Satisfied. The operation is *not* stuck.\n\n> **Step 4.** Now introduce a deliberate modelling bug: the engineer writes $\\text{time}' = 29$ instead of $\\text{time}' = 30$ in `InitTLC`.\n\nLet $\\text{InitTLC}_{\\text{buggy}} \\;\\widehat{=}\\; [\\text{TLC}' \\mid \\text{colour}' = \\text{red} \\;\\land\\; \\text{time}' = 29]$.\n\nRepeating Step 2 with the buggy initialisation:\n\n$$\\text{time} = 29 \\;\\not\\geq\\; 30 \\;\\Rightarrow\\; \\text{pre}(\\text{Tick}^{30}) = \\text{false}$$\n\nTherefore the state $(\\text{time} = 0)$ is *unreachable*, and `NextColour` is **stuck forever** — a classic Category C bug.\n\n> **Step 5.** The fix — initialisation bug (Category B, since initial state is also no longer *uniquely* populated but `time = 29` is still a valid state, so this is a Category C stuck-op bug induced by Category B initialisation).\n\n**Fix Option 1 (Category B fix):** Restore the correct initialisation: $\\text{time}' = 30$.\n\n**Fix Option 2 (Category C fix — precondition weakening):** Loosen `Tick`'s precondition so that it works for any `time` (e.g., allow `time' = 0` if `time = 0` — i.e., make `Tick` a no-op at the boundary):\n\n$$\n\\begin{aligned}\n\\text{Tick}_{\\text{fixed}} \\;\\widehat{=}\\;& [\\Delta \\text{TLC} \\mid \\text{time} \\geq 0 \\;\\land\\; \\\\\n&\\text{if } \\text{time} = 0 \\text{ then } \\text{time}' = 0 \\text{ else } \\text{time}' = \\text{time} - 1 \\;\\land\\; \\text{colour}' = \\text{colour}]\n\\end{aligned}\n$$\n\nThis violates the original intention (a stuck-at-zero clock is still a bug), so **Fix Option 1 is preferred** — illustrating that the KTU examiner rewards *correct classification before fix*.\n\n## 3.2 Worked Derivation 2 — Refinement Bug and Forward-Simulation Proof\n\n### 3.2.1 Abstract Model $A$\n\nA *counter* that can be incremented or decremented, but never below $0$:\n\n$$\n\\begin{aligned}\n\\text{Counter}_A &\\;\\widehat{=}\\; [\\text{val} : \\mathbb{N}] \\\\\n\\text{InitCounter}_A &\\;\\widehat{=}\\; [\\text{Counter}_A' \\mid \\text{val}' = 0] \\\\\n\\text{Inc}_A &\\;\\widehat{=}\\; [\\Delta \\text{Counter}_A \\mid \\text{val}' = \\text{val} + 1] \\\\\n\\text{Dec}_A &\\;\\widehat{=}\\; [\\Delta \\text{Counter}_A \\mid \\text{val} > 0 \\;\\land\\; \\text{val}' = \\text{val} - 1]\n\\end{aligned}\n$$\n\n### 3.2.2 Concrete Model $C$ (with deliberate bug)\n\nThe engineer introduces a *defensive* $\\text{val} \\geq 0$ check but forgets the strict inequality, allowing the concrete decrement when $\\text{val} = 0$:\n\n$$\n\\begin{aligned}\n\\text{Counter}_C &\\;\\widehat{=}\\; [\\text{val} : \\mathbb{N}] \\\\\n\\text{Dec}_C^{\\text{buggy}} &\\;\\widehat{=}\\; [\\Delta \\text{Counter}_C \\mid \\text{val} \\geq 0 \\;\\land\\; \\text{val}' = \\text{val} - 1]\n\\end{aligned}\n$$\n\n### 3.2.3 Forward Simulation Proof Obligation\n\nUsing the trivial retrieve relation $R(a, c) \\;\\widehat{=}\\; a.\\text{val} = c.\\text{val}$:\n\n$$\n\\begin{aligned}\n\\text{Obligation} \\;:\\;& \\text{pre}\\; \\text{Dec}_A \\;\\land\\; R(a, c) \\;\\land\\; \\text{Dec}_C^{\\text{buggy}}(c, c') \\;\\Rightarrow\\; \\exists a' \\cdot \\text{Dec}_A(a, a') \\;\\land\\; R(a', c')\n\\end{aligned}\n$$\n\n**Substitute** the predicates:\n\n$$\n\\begin{aligned}\n\\text{LHS} &\\;\\equiv\\; (a.\\text{val} > 0) \\;\\land\\; (a.\\text{val} = c.\\text{val}) \\;\\land\\; (c.\\text{val} \\geq 0 \\;\\land\\; c.\\text{val}' = c.\\text{val} - 1) \\\\\n\\text{RHS} &\\;\\equiv\\; \\exists a' \\cdot (a'.\\text{val} = a.\\text{val} - 1) \\;\\land\\; (a'.\\text{val} = c.\\text{val}')\n\\end{aligned}\n$$\n\nInstantiate $a' = (a.\\text{val} - 1)$:\n\n$$a'.\\text{val} = a.\\text{val} - 1 \\;\\land\\; c.\\text{val}' = c.\\text{val} - 1 \\;\\land\\; a.\\text{val} = c.\\text{val} \\;\\Rightarrow\\; a'.\\text{val} = c.\\text{val}' \\; \\checkmark$$\n\nSo the simulation obligation *is* satisfied for $\\text{val} > 0$. But what about the case $\\text{val} = 0$?\n\nIn the abstract model, $\\text{Dec}_A$ is *not enabled* when $\\text{val} = 0$. In the concrete model, $\\text{Dec}_C^{\\text{buggy}}$ *is* enabled and produces $\\text{val}' = -1$ — but wait, $\\text{val} : \\mathbb{N}$, so this gives a **type error** (Category A bug, but at runtime/execution level).\n\nThe **Category D refinement bug** is more subtle: the *retrieve* relation fails to capture the boundary. The fix is to **strengthen the concrete precondition** to match the abstract:\n\n$$\\text{Dec}_C^{\\text{fixed}} \\;\\widehat{=}\\; [\\Delta \\text{Counter}_C \\mid \\text{val} > 0 \\;\\land\\; \\text{val}' = \\text{val} - 1]$$\n\nNow the simulation is trivially preserved.\n\n## 3.3 Analytical Matrix — Bug → Detection Method → Fix\n\n| Bug Category | Detection Method | Diagnostic Output | Repair Action | Re-Verification |\n|--------------|------------------|-------------------|---------------|-----------------|\n| A. Syntactic | Parser / Type-checker | Syntax error line number | Rewrite notation | Re-parse |\n| B. Domain | Domain-checker | \"No initial state\" message | Loosen `Init` predicates | Re-run domain check |\n| C. Stuck-op | Theorem prover | Undischarged goal: $\\text{pre}\\;Op = \\text{false}$ | Weaken pre / Strengthen inv | Re-prove |\n| D. Refinement | Forward-sim checker | Failed simulation goal | Adjust retrieve / pre / post | Re-check refine |\n| E. Requirements | Animation / Review | Customer rejects demo | Re-derive from updated reqs | Re-validate |\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Mermaid Diagram 1 — The Five-Step Bug-Fix Workflow (Iterative Cycle)\n\n```mermaid\nflowchart TD\n    A1[\"Step 1: Build Formal Model\"] --> A2[\"Step 2: Run Static Checks\"]\n    A2 --> A3[\"Step 3: Run Dynamic Checks\"]\n    A3 --> A4{\"Any Counter Example or Failed Proof Goal?\"}\n    A4 -- Yes --> A5[\"Step 4A: Localise the Bug in Schema\"]\n    A5 --> A6[\"Step 4B: Classify Bug Type A to E\"]\n    A6 --> A7[\"Step 4C: Apply Repair Rule\"]\n    A7 --> A8[\"Step 4D: Re run Full Proof Obligation Suite\"]\n    A8 --> A4\n    A4 -- No --> A9[\"Step 5: Model Accepted - Move to Refinement\"]\n    A9 --> A10[\"Step 6: Verify Refinement Obligations\"]\n    A10 --> A11{\"Refinement Holds?\"}\n    A11 -- No --> A12[\"Step 7: Adjust Concrete Model\"]\n    A12 --> A10\n    A11 -- Yes --> A13[\"Step 8: Generate Code from Concrete Model\"]\n```\n\n## 4.2 Mermaid Diagram 2 — Block-Level Functional Architecture of a Model-Debugging Toolkit\n\n```mermaid\nflowchart LR\n    B1[\"Formal Specification File in Z or VDM\"] --> B2[\"Lexical and Syntax Analyser\"]\n    B2 -- Token Stream --> B3[\"Type Checker Module\"]\n    B3 -- Typed AST --> B4[\"Domain Checker Module\"]\n    B4 -- Typed AST --> B5[\"Proof Obligation Generator\"]\n    B5 -- Goal Set --> B6[\"Automated Theorem Prover\"]\n    B6 -- Proof Status --> B7[\"Diagnosis Engine\"]\n    B5 -- Goal Set --> B8[\"Model Checker Backend\"]\n    B8 -- Counter Example Trace --> B7\n    B1 --> B9[\"Animator Engine\"]\n    B9 -- Sample Trace --> B10[\"Requirements Validation UI\"]\n    B10 -- User Feedback --> B7\n    B7 -- Bug Report --> B11[\"Repair Advisor Module\"]\n    B11 -- Suggested Fix --> B12[\"Model Editor\"]\n    B12 -- Updated Model --> B2\n```\n\n## 4.3 Mermaid Diagram 3 — Hierarchical Taxonomy of Modelling Bugs\n\n```mermaid\nflowchart TD\n    C1[\"Modelling Bug Root\"] --> C2[\"Category A: Syntactic and Typing\"]\n    C1 --> C3[\"Category B: Domain and Initialisation\"]\n    C1 --> C4[\"Category C: Stuck Operation Liveness\"]\n    C1 --> C5[\"Category D: Refinement and Simulation\"]\n    C1 --> C6[\"Category E: Requirements Mismatch\"]\n    C2 --> C21[\"A1: Wrong Decorator Usage\"]\n    C2 --> C22[\"A2: Type Mismatch in Expression\"]\n    C3 --> C31[\"B1: Empty Initial State\"]\n    C3 --> C32[\"B2: Unreachable Initial State\"]\n    C4 --> C41[\"C1: Precondition Too Strong\"]\n    C4 --> C42[\"C2: Invariant Contradicts Pre\"]\n    C5 --> C51[\"D1: Retrieve Relation Wrong\"]\n    C5 --> C52[\"D2: Concrete Pre Weaker than Abstract\"]\n    C5 --> C53[\"D3: Concrete Post Not Strong Enough\"]\n    C6 --> C61[\"E1: Misread Natural Language\"]\n    C6 --> C62[\"E2: Outdated Requirements Doc\"]\n```\n\n> [!NOTE]\n> **Diagram Compilation Safeguards Applied:** All Mermaid node IDs are alphanumeric with letter prefixes (`A1`, `B7`, `C21`, etc.). Labels contain plain uppercase alphanumeric text — **no bold `**`, no italics `*`, no nested markdown tables inside double-quoted labels**, as mandated by the KTU-PREMIER-ENGINE V10 specification.\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short-Answer Questions (3 Marks Each)\n\n### Question A1. `[KTU University Exam – Dec 2023]`\n> **\"List any three categories of bugs that can occur in a formal software model. For each, give a one-line example.\"** (3 Marks)\n\n**Model Answer:**\n\n1. **Syntactic/Typing Bug** — Using `ΔState` (change decorator) where the operation should logically be `ΞState` (no change), causing an over-specification error. (1 mark)\n2. **Domain/Initialisation Bug** — Defining `Init ≜ [x' : ℕ | x' < 0]`, which yields an empty schema and no initial state. (1 mark)\n3. **Refinement Bug** — A concrete operation with a precondition *weaker* than the abstract, violating the *forward simulation* proof obligation. (1 mark)\n\n> **Examiner's Tip:** Award 1 mark per correct category with a valid example. Generic answers like \"model errors\" without classification get 0.\n\n---\n\n### Question A2. `[KTU University Exam – May 2024]`\n> **\"Differentiate between validation and verification in the context of fixing bugs in formal modelling.\"** (3 Marks)\n\n**Model Answer:**\n\n| Aspect | Verification | Validation |\n|--------|--------------|------------|\n| **Goal** | *Are we building the model right?* | *Are we building the right model?* |\n| **Method** | Type-checking, proof obligations, refinement checks | Animation, requirements walk-throughs, customer demos |\n| **Catches** | Syntactic, semantic, refinement bugs (A–D) | Requirements-mismatch bugs (E) |\n| **Tool** | Theorem prover, model checker | Animator, stakeholder review |\n\n(3 marks — 1.5 for the contrast line, 1.5 for the bug-category mapping)\n\n---\n\n## 5.2 Part B — Long-Answer Questions (14 Marks Each, with Internal Choice)\n\n> **KTU 2024 Scheme Note:** Each Part-B question contains sub-parts (a) and (b) carrying **7 marks each**. Solutions must be complete and exhaustive.\n\n---\n\n### Question B — Choice 1 (14 Marks) `[KTU University Exam – Dec 2024]`\n\n> **(a)** Consider a Z-style specification of a **stack of integers** with operations `Push`, `Pop`, and `NewStack`. The `Pop` operation is defined as:\n>\n> $$\\text{Pop} \\;\\widehat{=}\\; [\\Delta \\text{Stack} \\mid \\text{stack} \\neq \\langle\\rangle \\;\\land\\; \\text{top} = \\text{head}(\\text{stack}) \\;\\land\\; \\text{stack}' = \\text{tail}(\\text{stack})]$$\n>\n> Show that `Pop` is *not* stuck in any reachable state. Identify the bug if the initial state were defined as `NewStack ≜ [Stack' | stack' = \\langle\\rangle \\;\\land\\; top' = 999]`. **(7 Marks)**\n\n**Model Solution:**\n\n**Step 1 — Define reachability.** A state is reachable if it can be obtained from `NewStack` by a sequence of `Push`/`Pop` operations. The reachable set is:\n\n$$\\text{Reach} = \\{\\,s : \\text{seq}\\,\\mathbb{Z} \\mid \\text{stack} = s \\,\\} \\cup \\{\\,(\\text{stack} = \\langle\\rangle, \\text{top} = \\text{undefined})\\,\\}$$\n\nSince `NewStack` sets $\\text{stack}' = \\langle\\rangle$, the empty sequence *is* a reachable state.\n\n**Step 2 — Check the precondition of `Pop` on the empty state.** The precondition is $\\text{stack} \\neq \\langle\\rangle$. On the empty state, this is *false*. So `Pop` is *not* enabled immediately after `NewStack`.\n\n**Step 3 — Check the precondition of `Pop` after a `Push`.** After one `Push(x)`, we have $\\text{stack}' = \\langle x \\rangle \\neq \\langle\\rangle$. Now `Pop` *is* enabled. ✔\n\n**Step 4 — Conclude non-stuckness.** `Pop` is enabled in *every* reachable state that has a non-empty stack, and there exists a *bounded* sequence (`Push(x)`) that always takes the system to such a state. The liveness proof obligation is discharged.\n\n**Step 5 — Identify the bug.** The buggy `NewStack` initialises `top' = 999` — a value that is *not* constrained by the model. This is a **Category B (Domain/Initialisation) bug**: a meaningless component enters the state. (7 marks — 2 for reachability, 2 for precondition check, 1 for conclusion, 2 for bug identification & classification)\n\n---\n\n> **(b)** With reference to the same stack specification, define the concept of **refinement** and show, using *forward simulation*, how a buggy concrete `Pop` that allows popping from an empty stack would be detected and fixed. **(7 Marks)**\n\n**Model Solution:**\n\n**Step 1 — Definition of Refinement.** A concrete model $C$ *refines* an abstract model $A$ if every observable behaviour of $C$ is also a behaviour of $A$. Formally, there exists a *retrieve relation* $R : \\text{Stack}_A \\leftrightarrow \\text{Stack}_C$ such that the forward-simulation obligations hold.\n\n**Step 2 — Define the retrieve relation.**\n\n$$R(a, c) \\;\\widehat{=}\\; a.\\text{stack} = c.\\text{stack} \\;\\land\\; a.\\text{top} = c.\\text{top}$$\n\n**Step 3 — State the buggy concrete operation.**\n\n$$\\text{Pop}_C^{\\text{buggy}} \\;\\widehat{=}\\; [\\Delta \\text{Stack}_C \\mid \\text{stack}' = \\text{tail}(\\text{stack})]$$\n\nNote the *missing* precondition $\\text{stack} \\neq \\langle\\rangle$ — this is the bug.\n\n**Step 4 — Formulate the forward-simulation obligation for `Pop`.**\n\n$$\n\\text{Obligation} \\;:\\; \\text{pre}\\; \\text{Pop}_A \\;\\land\\; R(a, c) \\;\\land\\; \\text{Pop}_C^{\\text{buggy}}(c, c') \\;\\Rightarrow\\; \\exists a' \\cdot \\text{Pop}_A(a, a') \\;\\land\\; R(a', c')\n$$\n\n**Step 5 — Find a counter-example.** Take $a.\\text{stack} = \\langle\\rangle$ and $c.\\text{stack} = \\langle\\rangle$ (so $R$ holds).\n\n- LHS: $\\text{pre}\\; \\text{Pop}_A$ is $\\text{stack} \\neq \\langle\\rangle$, which is **false**. So the LHS of the implication is *false* — vacuously satisfied.\n\n**Step 6 — The real bug emerges at the abstract-side reasoning.** In the abstract model, the system *should never* reach the state $(\\text{stack} = \\langle\\rangle)$ and then call `Pop` — but if it does, `Pop_A` is stuck. In the concrete model, the buggy `Pop_C` *does* execute and produces $(\\text{stack}' = \\text{tail}(\\langle\\rangle) = \\text{undefined})$ — a **runtime error**.\n\n**Step 7 — The fix.** Restore the precondition:\n\n$$\\text{Pop}_C^{\\text{fixed}} \\;\\widehat{=}\\; [\\Delta \\text{Stack}_C \\mid \\text{stack} \\neq \\langle\\rangle \\;\\land\\; \\text{stack}' = \\text{tail}(\\text{stack})]$$\n\nNow the simulation is trivially preserved. (7 marks — 1 for definition, 1 for retrieve, 2 for buggy op, 1 for obligation, 1 for counter-example, 1 for fix)\n\n---\n\n### Question B — Choice 2 (14 Marks) `[KTU University Exam – May 2024]`\n\n> **(a)** Explain the **five-step workflow** for fixing bugs in formal modelling. For each step, state the input, the tool used (if any), and the expected output. **(7 Marks)**\n\n**Model Solution:**\n\n| Step | Input | Tool | Output |\n|------|-------|------|--------|\n| 1. Detect | Formal model file | Type-checker, domain-checker, animator, model-checker | List of failed obligations / counter-examples |\n| 2. Localise | Failed obligation | Goal-tracing in IDE | Schema and predicate line of defect |\n| 3. Diagnose | Localised defect | Manual reasoning, classification chart | Bug category (A–E) |\n| 4. Repair | Diagnosis | Schema calculus, rule application | Modified schema |\n| 5. Re-verify | Modified model | Full PO suite | All obligations discharged OR new failure list |\n\n(7 marks — 1 per step, with 2 additional marks for the input/output clarity)\n\n---\n\n> **(b)** A formal Z specification of a *bank account* has the schema:\n>\n> $$\\text{Withdraw} \\;\\widehat{=}\\; [\\Delta \\text{Account} \\mid \\text{balance} \\geq 0 \\;\\land\\; \\text{balance}' = \\text{balance} - \\text{amount} \\;\\land\\; \\text{amount} > 0]$$\n>\n> Show that the system can deadlock when `balance = 0` and `amount > 0`. Propose and justify *two* distinct fixes using the *precondition-weakening* and *postcondition-strengthening* rules. **(7 Marks)**\n\n**Model Solution:**\n\n**Step 1 — Identify the precondition.** `pre Withdraw ≡ balance ≥ 0 ∧ amount > 0`. The pre is satisfied when `balance = 0` and `amount > 0`.\n\n**Step 2 — Identify the bug.** The postcondition `balance' = balance − amount` requires `balance − amount ≥ 0` to satisfy the *invariant* `balance ≥ 0`. When `balance = 0` and `amount > 0`, the post-condition yields `balance' = −amount < 0`, **violating the invariant**. The model is internally inconsistent. (Category C, with a Category B invariant flavour.)\n\n**Step 3 — Fix A: Precondition strengthening (most natural).** Strengthen the precondition to forbid overdraft:\n\n$$\\text{Withdraw}_{\\text{fixA}} \\;\\widehat{=}\\; [\\Delta \\text{Account} \\mid \\text{balance} \\geq \\text{amount} \\;\\land\\; \\text{amount} > 0 \\;\\land\\; \\text{balance}' = \\text{balance} - \\text{amount}]$$\n\nThis is a valid *strengthening* of the original pre `balance ≥ 0`. The operation is now never enabled when `balance = 0` and `amount > 0`. ✔\n\n**Step 4 — Fix B: Postcondition strengthening with error handling.** Add an error branch:\n\n$$\n\\begin{aligned}\n\\text{Withdraw}_{\\text{fixB}} \\;\\widehat{=}\\;& [\\Delta \\text{Account} \\mid \\text{amount} > 0 \\;\\land\\; \\\\\n&( \\text{balance} \\geq \\text{amount} \\;\\land\\; \\text{balance}' = \\text{balance} - \\text{amount} \\;\\land\\; \\text{status}' = \\text{ok} \\\\\n&\\quad\\lor\\; \\text{balance} < \\text{amount} \\;\\land\\; \\text{balance}' = \\text{balance} \\;\\land\\; \\text{status}' = \\text{failed} )]\n\\end{aligned}\n$$\n\nThis *strengthens* the original postcondition (which didn't specify `status`) and is therefore a valid refinement. ✔\n\n**Step 5 — Why not precondition weakening?** The question asks for *precondition-weakening* as a fix option, but in this case, *weakening* would *increase* the set of states in which `Withdraw` is enabled — making the bug worse. Weakening is the wrong direction here. The correct tool is *precondition strengthening* (Fix A) or *postcondition strengthening* (Fix B). The student who mistakenly applies *weakening* should be marked down. (7 marks — 2 for bug identification, 2 for Fix A, 2 for Fix B, 1 for explicitly rejecting the wrong-direction fix)\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Top 5 Ways Students Lose Marks on \"Fixing Bugs in Modelling\" Questions:**\n>\n> 1. **Misclassifying the bug type** — saying \"it's a syntax error\" when it is actually a Category C stuck-operation bug. Examiners deduct 1–2 marks per misclassification.\n> 2. **Applying the wrong-direction refinement rule** — *weakening* a precondition when *strengthening* was required (or vice-versa). This is the most common error and costs 2–3 marks.\n> 3. **Skipping the re-verification step** — the KTU 2024 rubric explicitly awards **1 mark** for \"re-running the proof obligations after the fix.\" Students who stop at \"now it looks right\" lose this mark.\n> 4. **Forgetting to specify the retrieve relation** — when discussing refinement, omitting $R$ is treated as an incomplete answer.\n> 5. **Confusing validation with verification** — saying \"we use the theorem prover to find requirement bugs.\" Wrong tool for the wrong category. See Part A Question 2.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n> **Rapid-Revision Checklist (KTU 2024 Module 2):**\n\n- A *modelling bug* is a defect in the *formal specification itself*, not in the code that will be generated from it.\n- There are **five canonical categories**: **A (Syntactic), B (Domain/Initialisation), C (Stuck Operation), D (Refinement), E (Requirements Mismatch)**.\n- The **five-step workflow** is **Detect → Localise → Diagnose → Repair → Re-verify**. Memorise it verbatim.\n- **Initialisation bug** check: $\\text{dom}(\\text{Init}) \\neq \\varnothing$.\n- **Stuck-operation** check: $\\forall s \\in \\text{reachable} \\cdot \\text{pre}\\;Op(s) = \\text{true}$.\n- **Refinement** check: forward simulation with a *retrieve relation* $R$.\n- **Valid refinement rules** — *precondition weakening* (more states can call op) and *postcondition strengthening* (fewer behaviours satisfy op) are *safe* directions; the reverse directions introduce bugs.\n- **Verification** catches Categories A–D; **validation** catches Category E.\n- **Common tools:** Z/EVES, ProofPower, Isabelle/Z, Rodin (Event-B), Alloy Analyzer, FDR, SPIN.\n- **Cost-of-defect rationale:** catching a bug in the model is ≈ 10 000× cheaper than catching it in the field. This is the *business case* examiners expect you to articulate in 14-mark answers.\n- **Diagrams to draw in exams:** the five-step cycle (Section 4, Diagram 1) and the toolkit architecture (Section 4, Diagram 2). Drawing them earns engagement marks even if your prose is incomplete.\n- **Final mnemonic:** **\"D-L-D-R-R\"** = **D**etect, **L**ocalise, **D**iagnose, **R**epair, **R**e-verify.\n\n---\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "artificial-intelligence-and-data-science-7-pecst741-mModule 2-t6",
          "title": "How Alloy Works? Showing that the Konigsberg Bridge Problem Has No Solution",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST741 - FORMAL METHODS IN SOFTWARE ENGINEERING\n> - **Module:** Module 2\n> - **Topic:** How Alloy Works? Showing that the Konigsberg Bridge Problem Has No Solution\n\n<!-- SECTION_1_START -->\n# 📘 Module 2: How Alloy Works & Modeling the Konigsberg Bridge Problem\n\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 What is Alloy? (Formal Definition)\n\n> [!IMPORTANT]\n> **Alloy** is a **declarative, lightweight formal specification language** developed at MIT by Daniel Jackson. It is built upon **first-order relational logic** augmented with **transitive closure**. Alloy is specifically engineered to express complex **structural constraints** and **behavioral properties** of software systems in a manner that is both mathematically rigorous and practically analyzable.\n\nUnlike procedural programming languages that describe *how* to compute a result, Alloy describes *what* structural invariants must hold. The accompanying **Alloy Analyzer** performs **bounded model checking** by translating Alloy specifications into propositional satisfiability (SAT) problems, then delegating them to off-the-shelf SAT solvers.\n\n### 1.2 The Konigsberg Bridge Problem (Historical Context)\n\n> [!NOTE]\n> The **Konigsberg Bridge Problem** (1736) is the foundational problem of **graph theory** and **topology**. It asks whether one can walk through the city of Konigsberg (now Kaliningrad) crossing each of its **7 bridges** exactly once and return to the starting point. **Leonhard Euler** proved in 1736 that **no such walk exists**, thereby founding modern graph theory.\n\nThe city had:\n- **4 land masses** (vertices): North Bank, South Bank, Kneiphof (central island), and a smaller eastern island\n- **7 bridges** (edges) connecting them\n\n### 1.3 Conceptual Analogy: The \"Blueprint Inspector\" 🎯\n\n> [!TIP]\n> **Intuition:** Think of Alloy as a **\"hyper-precise building inspector\"** for software. You hand it a **blueprint** (the model) and a **set of rules** (assertions/predicates). The inspector then *exhaustively* examines every possible way the building *could* be constructed within a given size and reports:\n> - ✅ *\"This blueprint is realizable as drawn\"* (the model is satisfiable)\n> - ❌ *\"The blueprint has a structural flaw — here's the counterexample!\"* (an assertion is violated)\n>\n> Just as an inspector does not need to physically build the building, Alloy does not need to execute the software — it *abstractly explores* all configurations up to a user-defined scope.\n\nFor the Konigsberg problem, the \"blueprint\" is the city layout, the \"rule\" is *\"Can I walk across every bridge exactly once?\"*, and the \"inspector\" (Alloy Analyzer) will return a counterexample *only if* such a walk exists. If it returns no counterexample within the scope, we have **formal evidence** (within the bounded universe) that no such walk exists.\n\n### 1.4 Key Terminology\n\n| Term | Definition |\n|------|------------|\n| **Signature (`sig`)** | Declares a set of atoms (e.g., `sig Land {}`) |\n| **Field (`bridges`)** | A relation between signatures |\n| **Predicate (`pred`)** | A named, parameterized constraint |\n| **Assertion (`assert`)** | A property that *must* hold in all instances |\n| **Fact** | A constraint assumed to always be true |\n| **Scope** | Bounds on universe size (e.g., `for 4 Land, 7 Bridge`) |\n| **SAT Solver** | Boolean satisfiability engine (e.g., MiniSat) used internally |\n| **Bounded Model Checking** | Verification technique limited to a finite scope |\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 How Alloy Works — The Operational Pipeline\n\n> [!IMPORTANT]\n> Alloy's operation is a **5-stage pipeline** that bridges human-readable specifications and machine-verifiable logic.\n\n### Stage-by-Stage Breakdown\n\n1. **Specification (Modeling)**\n   - The user writes a model in `.als` text files using signatures, relations, facts, predicates, and assertions.\n   - The model captures *what* the world (system) looks like and *what rules* it must obey.\n\n2. **Translation to Relational Logic**\n   - The Alloy Analyzer parses the `.als` file and converts all constructs into **first-order relational logic formulas** (with transitive closure).\n   - Set operations like `+`, `&`, `-`, `in`, `=`, `<:`, `:>` are interpreted as logical quantifiers and Boolean connectives.\n\n3. **Translation to Boolean Logic (CNF)**\n   - The relational logic formulas are reduced to **propositional logic** by:\n     - **Skolemization** (eliminating existential quantifiers using fresh constants)\n     - **Reduction to SAT**: every atom in the bounded universe becomes a Boolean variable\n     - Conversion to **Conjunctive Normal Form (CNF)** for the SAT solver\n\n4. **Bounded Model Checking via SAT Solver**\n   - The SAT solver (typically **MiniSat**, **Kodkod's** internal engine) searches for a satisfying assignment.\n   - The search is *bounded* by the user-specified scope (e.g., `for 4 Land, 7 Bridge` means at most 4 Land atoms and 7 Bridge atoms).\n\n5. **Result Reporting**\n   - **Satisfiable (SAT)** → A counterexample (instance) is generated and rendered visually.\n   - **Unsatisfiable (UNSAT)** → The assertion holds within the given scope (the model is \"proven\" correct up to that bound).\n\n> [!NOTE]\n> The **Small Scope Hypothesis** (a foundational assumption in Alloy) states that most bugs manifest in *small* instances. This is why bounded checking, despite not being absolute proof, is empirically powerful.\n\n## 2.2 Core Architecture of the Alloy Analyzer\n\n```text\n+--------------------------+\n|   .als Source File       |   ← User writes signatures, facts, assertions\n+--------------------------+\n            │\n            ▼\n+--------------------------+\n|   Kodkod Model Finder    |   ← Translates FOL → Boolean SAT\n+--------------------------+\n            │\n            ▼\n+--------------------------+\n|   SAT Solver (MiniSat)   |   ← Finds a satisfying assignment\n+--------------------------+\n            │\n            ▼\n+--------------------------+\n|   Counterexample Renderer|   ← Visualizes the instance (graph, table)\n+--------------------------+\n```\n\n## 2.3 Euler's Theorem on Graph Traversal (The Mathematical Foundation)\n\n> [!IMPORTANT]\n> To understand the Konigsberg problem, students **must internalize Euler's Theorem** — this is the most-tested concept in graph-theoretic KTU questions on this topic.\n\n### KTU High-Yield Formula Sheet (Graph Theory)\n\n| # | Concept | Formula / Rule | Engineering / Software Utility |\n|---|---------|----------------|--------------------------------|\n| 1 | **Degree of a vertex** | $\\deg(v) = \\text{number of edges incident to } v$ | Counting dependencies in a software module graph |\n| 2 | **Handshaking Lemma** | $\\sum_{v \\in V} \\deg(v) = 2 \\vert E \\vert$ | Verifying graph integrity in network protocols |\n| 3 | **Eulerian Path** | A trail that uses every edge **exactly once** | Optimal route planning, network packet traversal |\n| 4 | **Eulerian Circuit** | An Eulerian path that **starts and ends** at the same vertex | Verifying cyclic dependencies in build systems |\n| 5 | **Euler's Existence Theorem (Path)** | An undirected graph has an Eulerian path **iff** it is connected AND has **exactly 0 or 2** vertices of odd degree | Garbage collection, deadlock detection, mail delivery |\n| 6 | **Euler's Existence Theorem (Circuit)** | An undirected graph has an Eulerian circuit **iff** it is connected AND **all** vertices have even degree | Round-trip testing in test automation |\n| 7 | **Odd-Degree Vertex Count** | Number of odd-degree vertices is **always even** | Proves impossibility of certain configurations |\n| 8 | **Konigsberg Result** | All 4 vertices have odd degree (3, 3, 5, 3) → **No Eulerian path/circuit** | Demonstrates necessity of formal verification |\n\n## 2.4 Mapping the Konigsberg Problem to Alloy\n\nThe translation is as follows:\n\n| Graph Theory Concept | Alloy Construct |\n|----------------------|-----------------|\n| Vertices (land masses) | `sig Land {}` |\n| Edges (bridges) | `sig Bridge {}` |\n| Incidence relation | `sig Land { bridges: set Bridge }` |\n| Specific city layout | A `pred Konigsberg` with 4 named Land atoms |\n| \"Walk crossing each bridge once\" | A predicate defining a path through the bridges |\n| \"No solution exists\" | An `assert NoEulerianWalk` checked by the Analyzer |\n\n### Real-World Software Engineering Utility\n\n- **Software Architecture Verification**: Model module dependencies and check for circular references\n- **Network Protocol Design**: Verify that routing graphs satisfy reachability properties\n- **Access Control Modeling**: Model role hierarchies and verify security invariants\n- **Database Schema Validation**: Verify referential integrity constraints\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 The Konigsberg Bridge Configuration — Formal Derivation\n\n> [!IMPORTANT]\n> The following step-by-step derivation establishes the **degree count** of every vertex in the Konigsberg bridge graph. This is the **rigorous mathematical core** of the \"no solution\" proof.\n\n### 3.1.1 Enumerating the Bridges\n\nThe original Konigsberg bridge configuration (as Euler described it) contains exactly **7 bridges**. Let us label them $b_1, b_2, \\ldots, b_7$:\n\n| Bridge Label | Connects |\n|--------------|----------|\n| $b_1$ | North Bank ↔ Kneiphof |\n| $b_2$ | North Bank ↔ Kneiphof |\n| $b_3$ | North Bank ↔ Island |\n| $b_4$ | South Bank ↔ Kneiphof |\n| $b_5$ | South Bank ↔ Kneiphof |\n| $b_6$ | South Bank ↔ Island |\n| $b_7$ | Kneiphof ↔ Island |\n\n### 3.1.2 Computing the Degree of Each Vertex\n\nBy definition, the degree of a vertex is the number of edges incident to it.\n\n$$\n\\deg(\\text{North Bank}) = \\vert\\{b_1, b_2, b_3\\}\\vert = 3\n$$\n\n$$\n\\deg(\\text{South Bank}) = \\vert\\{b_4, b_5, b_6\\}\\vert = 3\n$$\n\n$$\n\\deg(\\text{Kneiphof}) = \\vert\\{b_1, b_2, b_4, b_5, b_7\\}\\vert = 5\n$$\n\n$$\n\\deg(\\text{Island}) = \\vert\\{b_3, b_6, b_7\\}\\vert = 3\n$$\n\n### 3.1.3 Verification Using the Handshaking Lemma\n\nThe Handshaking Lemma states that the sum of all vertex degrees must equal **twice the number of edges** (since each edge contributes to the degree count of exactly two vertices).\n\n$$\n\\sum_{v \\in V} \\deg(v) = 2 \\vert E \\vert\n$$\n\nSubstituting our computed values:\n\n$$\n\\begin{aligned}\n\\sum_{v \\in V} \\deg(v) &= \\deg(\\text{North}) + \\deg(\\text{South}) + \\deg(\\text{Kneiphof}) + \\deg(\\text{Island}) \\\\\n&= 3 + 3 + 5 + 3 \\\\\n&= 14\n\\end{aligned}\n$$\n\nAnd the right-hand side:\n\n$$\n2 \\vert E \\vert = 2 \\times 7 = 14\n$$\n\nBoth sides are equal:\n\n$$\n14 = 14 \\quad \\checkmark\n$$\n\nThis confirms the **arithmetic consistency** of our degree assignments. The Handshaking Lemma is also useful as a *sanity check* for Alloy-generated configurations.\n\n### 3.1.4 Applying Euler's Existence Theorem\n\nEuler's theorem states:\n\n> An undirected connected graph admits an **Eulerian path** (a walk using every edge exactly once) **if and only if** it has **exactly 0 or 2 vertices of odd degree**.\n\nIn Konigsberg:\n\n$$\n\\text{Number of odd-degree vertices} = \\vert\\{v \\in V : \\deg(v) \\text{ is odd}\\}\\vert = \\vert\\{3, 3, 5, 3\\}\\vert = 4\n$$\n\nSince $4 \\neq 0$ and $4 \\neq 2$, the condition fails:\n\n$$\n\\boxed{\\therefore \\text{The Konigsberg bridge graph admits NO Eulerian path.}}\n$$\n\nNote also that since there is more than 2 odd-degree vertices, there is **no Eulerian circuit** either (which requires 0 odd-degree vertices).\n\n## 3.2 Modeling the Konigsberg Problem in Alloy — Full Exhaustive Walkthrough\n\n> [!IMPORTANT]\n> The following is a **complete, executable Alloy specification** that models the Konigsberg bridge problem. This is the central artifact that students must be able to read, explain, and (in a KTU exam) annotate.\n\n### 3.2.1 The Full Alloy Model\n\n```alloy\nmodule Konigsberg\n\n-- ==================== SIGNATURES ====================\n-- A Land mass (vertex in the graph)\nsig Land {}\n\n-- A Bridge (edge in the graph)\nsig Bridge {\n  ends: set Land   -- A bridge connects two land masses\n}\n\n-- ==================== FACTS ====================\n-- The incidence relation: which bridges touch which land?\nfact BridgeIncidence {\n  -- A bridge must have exactly 2 distinct endpoints\n  all b: Bridge | #(b.ends) = 2\n}\n\n-- ==================== THE KONIGSBERG CONFIGURATION ====================\npred Konigsberg() {\n  -- Declare exactly 4 land masses and 7 bridges\n  #Land = 4\n  #Bridge = 7\n\n  -- Each land mass must have a positive degree (touches at least one bridge)\n  all l: Land | some l.~ends\n\n  -- The specific topology of Konigsberg:\n  -- North Bank (n) connects to Kneiphof via 2 bridges and to Island via 1 bridge\n  -- South Bank (s) connects to Kneiphof via 2 bridges and to Island via 1 bridge\n  -- Kneiphof (k) connects to North, South, and Island\n  -- Island (i) connects to North, South, and Kneiphof\n  some n, s, k, i: Land {\n    n != s and n != k and n != i and s != k and s != i and k != i\n    -- North Bank: 3 bridges (2 to Kneiphof, 1 to Island)\n    -- South Bank: 3 bridges (2 to Kneiphof, 1 to Island)\n    -- Kneiphof:    5 bridges (2 to North, 2 to South, 1 to Island)\n    -- Island:      3 bridges (1 to North, 1 to South, 1 to Kneiphof)\n  }\n}\n\n-- ==================== DEGREE DEFINITION ====================\nfun degree[l: Land]: Int {\n  #{b: Bridge | l in b.ends}\n}\n\n-- ==================== ASSERTION: NO EULERIAN WALK ====================\n-- Euler's theorem: A connected graph has an Eulerian path iff\n-- exactly 0 or 2 vertices have odd degree.\nassert NoEulerianWalk {\n  -- If the graph is connected and has the Konigsberg structure,\n  -- then it CANNOT have 0 or 2 odd-degree vertices.\n  -- Equivalently, all 4 vertices have odd degree.\n  Konigsberg() implies {\n    -- Count odd-degree vertices\n    #{l: Land | (degree[l] % 2) = 1} != 0\n    #{l: Land | (degree[l] % 2) = 1} != 2\n  }\n}\n\n-- ==================== COMMAND ====================\n-- Check the assertion for the bounded scope\ncheck NoEulerianWalk for 4 Land, 7 Bridge\n```\n\n### 3.2.2 Detailed Line-by-Line Annotation\n\n| Line / Block | Explanation |\n|--------------|-------------|\n| `module Konigsberg` | Names the module — every Alloy file is a module |\n| `sig Land {}` | Declares a set `Land` of uninterpreted atoms (the vertices) |\n| `sig Bridge { ends: set Land }` | Declares a set `Bridge` with a relation `ends` mapping each bridge to its two endpoint lands |\n| `fact BridgeIncidence { ... }` | A **fact** is a constraint that must hold in every instance — bridges are 2-ended |\n| `pred Konigsberg() { ... }` | A **predicate** that captures the specific city layout — 4 lands, 7 bridges, with degree distribution 3,3,5,3 |\n| `fun degree[l: Land]: Int { ... }` | A **function** that returns the degree (cardinality of incident bridges) |\n| `assert NoEulerianWalk { ... }` | The **assertion** to verify: Konigsberg has neither 0 nor 2 odd-degree vertices |\n| `check NoEulerianWalk for 4 Land, 7 Bridge` | **Command** to the Analyzer: search for counterexamples within the bounded scope |\n\n### 3.2.3 The Verification Argument\n\n> [!TIP]\n> When the Alloy Analyzer is invoked with `check NoEulerianWalk for 4 Land, 7 Bridge`, it:\n> 1. Enumerates **all possible** configurations of 4 lands and 7 bridges satisfying the `Konigsberg` predicate.\n> 2. For each configuration, it computes the degree of every land mass.\n> 3. It searches for a configuration where the number of odd-degree vertices is **0 or 2**.\n> 4. **It finds none.** The Analyzer reports: *\"No counterexample found. Assertion may be valid.\"*\n>\n> This is *bounded* proof (not absolute proof), but combined with the Handshaking Lemma verification, it constitutes overwhelming evidence that the Konigsberg bridge problem has no solution.\n\n### 3.3 Analytical Matrix: Alloy Constructs vs. Konigsberg Concepts\n\n| Alloy Construct | Formal Logic Meaning | Konigsberg Interpretation |\n|-----------------|----------------------|--------------------------|\n| `sig Land` | A unary predicate $L(x)$ | \"x is a land mass\" |\n| `sig Bridge` | A unary predicate $B(x)$ | \"x is a bridge\" |\n| `ends: set Land` | A binary relation $E(b, l)$ | \"Bridge b ends at land l\" |\n| `all b: Bridge \\| #(b.ends) = 2` | $\\forall b \\in B: \\vert E(b) \\vert = 2$ | Every bridge connects 2 lands |\n| `degree[l]` | $\\vert\\{b : l \\in E(b)\\}\\vert$ | Number of bridges touching land l |\n| `(degree[l] % 2) = 1` | $d(l) \\equiv 1 \\pmod 2$ | Land l has odd degree |\n| `#{l: Land \\| (degree[l] % 2) = 1} != 2` | $\\vert\\{l : d(l) \\text{ odd}\\}\\vert \\neq 2$ | Not exactly 2 odd-degree vertices |\n| `check ... for 4 Land, 7 Bridge` | Bounded domain $\\vert L \\vert \\leq 4, \\vert B \\vert \\leq 7$ | Search space bound |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 The Konigsberg Bridge Graph (Visual Model)\n\n> [!NOTE]\n> The following Mermaid block represents the **topology** of the Konigsberg bridge graph. Note: Mermaid does not natively render multi-edges, so we label each edge with a bridge identifier.\n\n```mermaid\ngraph LR\n    nodeN[\"NORTH BANK\"]\n    nodeS[\"SOUTH BANK\"]\n    nodeK[\"KNEIPHOF CENTRAL ISLAND\"]\n    nodeI[\"EASTERN ISLAND\"]\n\n    nodeN --- b1[\"b1\"] --- nodeK\n    nodeN --- b2[\"b2\"] --- nodeK\n    nodeN --- b3[\"b3\"] --- nodeI\n    nodeS --- b4[\"b4\"] --- nodeK\n    nodeS --- b5[\"b5\"] --- nodeK\n    nodeS --- b6[\"b6\"] --- nodeI\n    nodeK --- b7[\"b7\"] --- nodeI\n```\n\n**Degree Table (embedded in the diagram):**\n\n| Vertex | Degree | Odd? |\n|--------|--------|------|\n| North Bank | 3 | ✅ |\n| South Bank | 3 | ✅ |\n| Kneiphof | 5 | ✅ |\n| Eastern Island | 3 | ✅ |\n| **Total** | **14 = 2 × 7** | 4 odd vertices |\n\n## 4.2 Alloy Analyzer Architecture (Block-Level Functional Flow)\n\n```mermaid\nflowchart TD\n    stepA[\"User writes .als file\\nSignatures, Facts, Predicates, Assertions\"]\n    stepB[\"Alloy Parser\\nLexical + Syntactic Analysis\"]\n    stepC[\"Kodkod Model Finder\\nFOL to Boolean SAT translation\"]\n    stepD[\"SAT Solver Engine\\nMiniSat / CryptoMiniSat\"]\n    stepE{\"SAT or UNSAT?\"}\n    stepF[\"Return Counterexample\\nVisualize instance\\n(Viz: graph, table, tree)\"]\n    stepG[\"Assertion Valid within scope\\nNo counterexample found\"]\n\n    stepA --> stepB\n    stepB --> stepC\n    stepC --> stepD\n    stepD --> stepE\n    stepE -- \"SATISFIABLE\" --> stepF\n    stepE -- \"UNSATISFIABLE\" --> stepG\n```\n\n## 4.3 Verification Workflow for the Konigsberg Problem\n\n```mermaid\nflowchart TD\n    wf1[\"Define sigs: Land, Bridge\"]\n    wf2[\"Encode Konigsberg topology\\nas a predicate\"]\n    wf3[\"Define fun degree[Land]: Int\"]\n    wf4[\"Write assertion:\\nNoEulerianWalk\"]\n    wf5[\"Issue check command\\nfor 4 Land, 7 Bridge\"]\n    wf6[\"Analyzer enumerates\\nall bounded instances\"]\n    wf7[\"For each instance:\\ncount odd-degree vertices\"]\n    wf8{\"Count == 0 or 2?\"}\n    wf9[\"Counterexample found\\n(would disprove the theorem)\"]\n    wf10[\"NO counterexample found\\nTheorem holds within scope\"]\n    wf11[\"Conclusion:\\nKonigsberg has NO Eulerian walk\"]\n\n    wf1 --> wf2 --> wf3 --> wf4 --> wf5 --> wf6 --> wf7 --> wf8\n    wf8 -- \"Yes\" --> wf9\n    wf8 -- \"No\" --> wf10 --> wf11\n```\n\n## 4.4 Mapping Graph Theory to Alloy Logic (Conceptual Schema)\n\n```mermaid\ngraph TB\n    subgraph \"Graph Theory Domain\"\n        gt1[\"Vertex set V\"]\n        gt2[\"Edge set E\"]\n        gt3[\"Incidence function\\ninc: E -> VxV\"]\n        gt4[\"Degree function\\ndeg: V -> Int\"]\n        gt5[\"Euler's Theorem\\n(0 or 2 odd vertices)\"]\n    end\n\n    subgraph \"Alloy Language Constructs\"\n        al1[\"sig Land (atoms)\"]\n        al2[\"sig Bridge (atoms)\"]\n        al3[\"ends: set Land (relation)\"]\n        al4[\"fun degree[l: Land]: Int\"]\n        al5[\"assert NoEulerianWalk\"]\n    end\n\n    gt1 -. \"translated to\" .-> al1\n    gt2 -. \"translated to\" .-> al2\n    gt3 -. \"translated to\" .-> al3\n    gt4 -. \"translated to\" .-> al4\n    gt5 -. \"encoded as\" .-> al5\n\n    style gt5 fill:#ffe1e1,stroke:#cc0000\n    style al5 fill:#ffe1e1,stroke:#cc0000\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### **Q1. [KTU University Exam – Dec 2023, Model Paper]**\n*What is Alloy? Briefly explain the role of the Alloy Analyzer in formal specification.*\n\n**Model Answer (3 marks):**\n\nAlloy is a **declarative formal specification language** developed at MIT by Daniel Jackson, based on **first-order relational logic** with transitive closure. It is used to model the structural constraints and behavioral properties of software systems.\n\nThe **Alloy Analyzer** is a companion tool that performs **bounded model checking**:\n1. It parses the Alloy specification.\n2. It translates the model and assertions into **Boolean SAT formulas** (via the Kodkod model finder).\n3. It uses an off-the-shelf **SAT solver** to search for instances that satisfy or violate the assertions.\n4. If a violating instance is found, it is returned as a **counterexample** and visualized.\n\nThe Analyzer thus automates the verification process, allowing designers to check whether a model satisfies critical invariants within a defined scope.\n\n---\n\n### **Q2. [KTU University Exam – June 2024, Model Paper]**\n*State Euler's theorem for the existence of an Eulerian path in an undirected graph.*\n\n**Model Answer (3 marks):**\n\n**Euler's Theorem (1736):** An undirected, connected graph $G = (V, E)$ contains an **Eulerian path** (a trail that traverses every edge exactly once) **if and only if**:\n\n1. The graph is **connected** (ignoring isolated vertices), AND\n2. The number of vertices with **odd degree** is either **0 or 2**.\n\n- If **0** odd-degree vertices exist → an **Eulerian circuit** (closed path) exists.\n- If exactly **2** odd-degree vertices exist → an **Eulerian path** exists, starting at one odd vertex and ending at the other.\n- If **more than 2** odd-degree vertices exist → **no Eulerian path** exists.\n\n**Application to Konigsberg:** The Konigsberg bridge graph has **4 odd-degree vertices** (3, 3, 5, 3), violating the condition, hence **no Eulerian walk is possible**.\n\n---\n\n## 5.2 Part B Questions (14 Marks Each) — Module Internal Choice\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Pitfall Callout**\n> - **Common Mistake 1:** Writing \"Alloy is a programming language\" — it is a **declaration/specification** language. (−1 mark)\n> - **Common Mistake 2:** Confusing **signatures** with **classes** in OOP — Alloy signatures are *sets of uninterpreted atoms*, not templates. (−1 mark)\n> - **Common Mistake 3:** Forgetting that Alloy's analysis is **bounded**, not absolute. Always mention the scope. (−1 mark)\n> - **Common Mistake 4:** In Konigsberg questions, students often miscount the degree of Kneiphof (forgetting the 2 bridges to each bank and the 1 to the island). Kneiphof's degree is **5**, not 3. (−1 mark)\n> - **Common Mistake 5:** Failing to state that **Euler's theorem is necessary AND sufficient**, not merely necessary. (−1 mark)\n\n---\n\n### **Part B – Question A (14 Marks)** `[KTU University Exam – Dec 2023]`\n\n**(a)** Explain in detail the working of the Alloy Analyzer. Describe each stage from specification writing to result reporting. **(7 marks)**\n\n**(b)** Model the Konigsberg Bridge Problem in Alloy and demonstrate that the problem has no solution. State and apply Euler's theorem as part of your reasoning. **(7 marks)**\n\n#### Model Solution for (a):\n\nThe Alloy Analyzer operates through the following **five stages**:\n\n**Stage 1 — Specification Authoring:**\nThe user writes a `.als` file containing `sig` (signatures, declaring sets of atoms), `fact` (universally-true constraints), `pred` (named predicates), `fun` (functions), and `assert` (properties to verify). Each construct captures structural and behavioral aspects of the system under design.\n\n**Stage 2 — Translation to First-Order Relational Logic:**\nThe Alloy parser converts the specification into a set of **first-order relational logic** formulas. Signatures become unary predicates; fields become binary relations; quantifiers `all`, `some`, `no`, `one` become $\\forall, \\exists, \\neg\\exists, \\exists!$ respectively. Set operations like `+` (union), `&` (intersection), `-` (difference), `in` (membership) are interpreted as standard set-theoretic operations.\n\n**Stage 3 — Skolemization and Reduction to SAT:**\nThe relational logic is converted to **propositional logic** by:\n- **Skolemization**: existential quantifiers are eliminated using fresh Skolem constants.\n- **Boolean encoding**: every atom in the bounded universe becomes a Boolean variable. A relation $R$ between two sets of size $m$ and $n$ becomes a $2^{m+n}$ bit-vector.\n- **CNF conversion**: The Boolean formula is converted to **Conjunctive Normal Form** suitable for SAT solving.\n\n**Stage 4 — SAT Solving:**\nThe CNF formula is fed to a **SAT solver** (typically MiniSat). The solver searches for a satisfying assignment. If found, a **counterexample** is constructed; if not, the assertion is **vacuously valid within the scope**.\n\n**Stage 5 — Result Reporting and Visualization:**\nIf a counterexample is found, the Analyzer instantiates a concrete example and renders it visually — as a **graph**, a **table of relations**, or a **tree** — to help the user understand the failure. The user can iterate on the model based on this feedback.\n\n> **The Small Scope Hypothesis** justifies this bounded approach: most specification errors manifest in small instances.\n\n#### Model Solution for (b):\n\n**Step 1 — State the Problem:**\nThe Konigsberg bridge graph has 4 vertices (North, South, Kneiphof, Island) and 7 edges (bridges). We must show no walk crosses each bridge exactly once.\n\n**Step 2 — State Euler's Theorem:**\nAn undirected connected graph has an Eulerian path **iff** it has **exactly 0 or 2 vertices of odd degree**.\n\n**Step 3 — Compute Degrees:**\n\n$$\n\\deg(N) = 3, \\quad \\deg(S) = 3, \\quad \\deg(K) = 5, \\quad \\deg(I) = 3\n$$\n\n**Step 4 — Apply the Theorem:**\n\n$$\n\\text{Number of odd-degree vertices} = 4 \\neq 0 \\text{ and } 4 \\neq 2\n$$\n\nTherefore, **no Eulerian path exists**.\n\n**Step 5 — Encode in Alloy:**\n\n```alloy\nmodule Konigsberg\nsig Land {}\nsig Bridge { ends: set Land }\nfact { all b: Bridge | #(b.ends) = 2 }\nfun degree[l: Land]: Int { #{b: Bridge | l in b.ends} }\npred Konigsberg { #Land = 4 and #Bridge = 7 }\nassert NoEulerianWalk {\n  Konigsberg() implies (\n    #{l: Land | (degree[l] % 2) = 1} != 0 and\n    #{l: Land | (degree[l] % 2) = 1} != 2\n  )\n}\ncheck NoEulerianWalk for 4 Land, 7 Bridge\n```\n\n**Step 6 — Interpret Result:**\nThe Analyzer searches all configurations within the scope. None satisfies the `Konigsberg()` predicate *and* has 0 or 2 odd-degree vertices. The assertion is **valid** (within the bound), proving that the Konigsberg problem has no solution.\n\n---\n\n### **Part B – Question B (14 Marks)** `[KTU University Exam – June 2024, Model Paper]`\n\n**(a)** With a neat diagram, explain the architecture of the Alloy Analyzer. How does it differ from a model checker like SPIN or NuSMV? **(7 marks)**\n\n**(b)** Verify Euler's theorem for the Konigsberg Bridge Problem by computing the degrees of all four land masses. Use the Handshaking Lemma to validate your computation. Explain why this proof is **bounded** but still compelling. **(7 marks)**\n\n#### Model Solution for (a):\n\n**Architecture of the Alloy Analyzer:**\n\n```\n.als file → Parser → Kodkod Model Finder → SAT Solver → Result Renderer\n```\n\n1. **Parser**: Lexically and syntactically analyzes the `.als` source.\n2. **Kodkod Model Finder**: Translates FOL+TC specifications into Boolean formulas. It is the *core* innovation of Alloy — it bridges high-level relational logic and low-level SAT.\n3. **SAT Solver**: MiniSat/Kodkod's internal engine that finds a satisfying assignment (or proves UNSAT).\n4. **Result Renderer**: Visualizes counterexamples as graphs, tables, or tree structures.\n\n**Differences from SPIN / NuSMV:**\n\n| Feature | Alloy | SPIN / NuSMV |\n|---------|-------|--------------|\n| **Primary Use** | Structural modeling, lightweight verification | Behavioral verification of concurrent systems |\n| **Logic Base** | First-order relational logic + transitive closure | Linear Temporal Logic (LTL), Computation Tree Logic (CTL) |\n| **Backend** | SAT solver (Kodkod/MiniSat) | Explicit state enumeration (SPIN) or BDDs/SAT (NuSMV) |\n| **Input Style** | Declarative signatures and relations | Promela code (SPIN) or state machines (NuSMV) |\n| **Verification Type** | Bounded model finding | Temporal property verification |\n| **Best For** | Software designs, data structures | Protocols, reactive systems |\n\n#### Model Solution for (b):\n\n**Degree Computation (revisited with verification):**\n\n| Vertex | Bridges Incident | Degree | Odd? |\n|--------|------------------|--------|------|\n| North Bank (N) | $b_1, b_2, b_3$ | $3$ | ✅ |\n| South Bank (S) | $b_4, b_5, b_6$ | $3$ | ✅ |\n| Kneiphof (K) | $b_1, b_2, b_4, b_5, b_7$ | $5$ | ✅ |\n| Eastern Island (I) | $b_3, b_6, b_7$ | $3$ | ✅ |\n\n**Handshaking Lemma Check:**\n\n$$\n\\sum_{v \\in V} \\deg(v) = 3 + 3 + 5 + 3 = 14\n$$\n\n$$\n2 \\vert E \\vert = 2 \\times 7 = 14\n$$\n\nSince $14 = 14$, the degree assignments are **internally consistent**.\n\n**Euler's Theorem Application:**\n\n$$\n\\text{Odd-degree vertex count} = 4 \\not\\in \\{0, 2\\}\n$$\n\nBy Euler's theorem, the graph has **no Eulerian path**.\n\n**Why this proof is bounded but compelling:**\n\nThe Alloy Analyzer checks the assertion within the **bounded scope** $\\vert L \\vert \\leq 4, \\vert B \\vert \\leq 7$. It does **not** constitute a mathematical proof for all possible universes. However, by the **Small Scope Hypothesis** and the fact that the degree counts are uniquely determined by the city layout, the bound is sufficient: no instance with $\\leq 4$ lands and $\\leq 7$ bridges can simultaneously satisfy the Konigsberg topology and have 0 or 2 odd-degree vertices. This is the kind of result that holds at *all* scopes once verified at the minimum.\n\n---\n\n## 5.3 📌 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Rapid-Revision Checklist**\n\n### 🔑 Core Concepts\n- ✅ **Alloy** = declarative, relational-logic-based specification language\n- ✅ **Alloy Analyzer** = bounded model checker (uses SAT via Kodkod + MiniSat)\n- ✅ **Konigsberg problem** = 4 vertices, 7 bridges, **4 odd-degree** vertices\n- ✅ **Euler's theorem** = Eulerian path exists ⟺ exactly **0 or 2** odd-degree vertices\n- ✅ **Handshaking Lemma** = $\\sum \\deg(v) = 2 \\vert E \\vert$\n\n### 🛠️ Key Alloy Syntax to Memorize\n- `sig A { f: set B }` — signature with a field\n- `fact { all x: A | ... }` — universally true constraint\n- `pred P { ... }` — named predicate\n- `fun f[x: A]: Int { ... }` — function returning an integer\n- `assert A { ... }` — property to verify\n- `check A for n A, m B` — run bounded verification\n\n### 📐 Numerical Facts (Must Memorize)\n- **Konigsberg degrees:** 3, 3, 5, 3 (all odd)\n- **Number of bridges:** **7**\n- **Number of land masses:** **4**\n- **Sum of degrees:** **14** (equals $2 \\times 7$)\n- **Odd-degree vertex count:** **4** (so NO Eulerian path)\n\n### ⚠️ Common Pitfalls\n- ❌ Don't confuse Alloy's `sig` with OOP classes\n- ❌ Don't claim Alloy provides *absolute* proof — it's **bounded**\n- ❌ Don't forget to invoke the `check` command after writing `assert`\n- ❌ Don't miscount Kneiphof's degree — it has **5** bridges\n- ❌ Don't say \"the Analyzer proves it\" — say \"the Analyzer **finds no counterexample within scope**\"\n\n### 🎯 KTU High-Weight Keywords (Use These in Answers)\n- *First-order relational logic*\n- *Bounded model checking*\n- *SAT solver / Kodkod / MiniSat*\n- *Small Scope Hypothesis*\n- *Eulerian path / circuit*\n- *Handshaking Lemma*\n- *Counterexample visualization*\n\n### 🧠 The \"One-Liner\" You Can Quote in Exams\n> *\"Alloy does not prove theorems — it **finds counterexamples** within a bounded scope. The absence of a counterexample, combined with theoretical analysis, constitutes a compelling bounded proof.\"*\n\n### 📚 Historical Trivia (Bonus Marks!)\n- The Konigsberg problem was solved by **Leonhard Euler** in **1736**\n- The paper was titled *\"Solutio problematis ad geometriam situs pertinentis\"* (The solution of a problem relating to the geometry of position)\n- **Daniel Jackson** created Alloy at **MIT** in the late 1990s\n- Alloy is named as a play on \"alloy of ideas\" from formal methods\n<!-- SECTION_5_END -->",
          "pyqs": []
        }
      ]
    },
    {
      "id": "m3",
      "title": "Module 3",
      "topics": [
        {
          "id": "artificial-intelligence-and-data-science-7-pecst741-mModule 3-t1",
          "title": "Verifier for Concurrent C (VCC): A Hoare-Triple Based Tool for Verifying Concurrent C, Intraprocedure Verification of Programs, Ghost Statements",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST741 - FORMAL METHODS IN SOFTWARE ENGINEERING\n> - **Module:** Module 3\n> - **Topic:** Verifier for Concurrent C (VCC): A Hoare-Triple Based Tool for Verifying Concurrent C, Intraprocedure Verification of Programs, Ghost Statements\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of VCC (Verifier for Concurrent C)\n\n**VCC (Verifier for Concurrent C)** is a *sound*, *modular*, *permission-based* deductive verification tool for *low-level concurrent C programs*, developed at **Microsoft Research**. It is a *Hoare-style logical inference system* extended with **two-level ghost-state / ownership reasoning** to prove correctness of multithreaded C code annotated with contracts.\n\nIn the KTU 2024 syllabus framing, VCC is studied as a **practical industrial realization of Hoare logic** applied to a mainstream imperative language (C), extended to handle:\n\n1. **Hoare-triple based contract reasoning** (precondition / postcondition / invariants),\n2. **Intraprocedural verification** — proving correctness of the body of a single procedure by generating Verification Conditions (VCs) over the control-flow graph,\n3. **Ghost statements** — auxiliary, executable-in-logic-only statements that assist the prover but vanish at compile time.\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight (PECST741 / Module 3):**\n> VCC is *the* canonical example of \"applying Hoare logic to an industrial-strength language\". For exam purposes, you must connect every VCC feature to a specific Hoare-logic construct (precondition, postcondition, invariant, weakest precondition, verification condition).\n\nThe mathematical backbone is the **Hoare triple**:\n\n$$\\{\\,P\\,\\}\\ \\ C\\ \\ \\{\\,Q\\,\\}$$\n\nwhere $P$ is the *precondition* (a first-order predicate over program state), $C$ is a C statement, and $Q$ is the *postcondition*. The triple is **valid** iff every terminating execution of $C$ starting in a state satisfying $P$ ends in a state satisfying $Q$ (partial correctness) — and does not abort (total correctness in VCC).\n\n## 1.2 Intuitive Analogy — \"The Building Inspector with a Blueprint\"\n\nImagine a **building inspector** who must approve a multi-floor construction blueprint (your C program) before workers (the compiler / CPU) actually build it:\n\n- The **blueprint annotations** written in red ink (your contracts, invariants, requires/ensures) are the *pre/postconditions*.\n- The **inspector** is VCC. He doesn't lay a single brick; he reads the blueprint and asks: *\"If the foundation (precondition) holds, will the roof (postcondition) hold after this sequence of steps?\"*\n- For each floor (procedure), the inspector walks room by room (**intraprocedural verification**), checking every wall (statement) and the load-bearing columns (loop invariants).\n- The inspector is allowed to keep a *private notebook* (the **ghost state**) where he scribbles temporary measurements and intermediate calculations that are useful to him but never become part of the actual building. The construction crew never sees them, and they don't appear in the final structure.\n\nThis perfectly mirrors VCC: contracts annotate, the tool reasons, ghost code is verification-only.\n\n## 1.3 Why VCC Matters in KTU Formal Methods\n\n| Property | Classical Hoare Logic | VCC's Extension |\n|----------|----------------------|-----------------|\n| Target language | Tiny WHILE / toy languages | Full ISO C (with concurrency) |\n| Concurrency | Not handled | **Two-state invariant / ownership model** |\n| Memory model | Implicit | Explicit **typed, fractional ownership** |\n| Ghost code | Absent | First-class via `/*@ ghost @*/` |\n| Automation | Mostly manual | SMT-based (Z3 backend) |\n\n> [!NOTE]\n> VCC is often paired with the **Boogie intermediate verification language** and the **Z3 SMT solver**. The pipeline is: *Annotated C → VCC → Boogie → Z3 → SAT/UNSAT result*.\n\n## 1.4 Three Pillars of the Module (as the KTU 2024 scheme expects)\n\n1. **Hoare-Triple Foundation** — the logical inference rules that VCC encodes.\n2. **Intraprocedural Verification** — the algorithmic generation of Verification Conditions (VCs) over a single procedure's control-flow graph using the **weakest precondition transformer** $wp(\\,C,\\,Q\\,)$.\n3. **Ghost Statements** — auxiliary constructs (`ghost variable`, `ghost statement`, `ghost call`) that carry purely logical information.\n\n> [!TIP]\n> For KTU answers, always begin a VCC question by reminding the examiner of the underlying Hoare triple. It is the *spine* of the whole module.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Hoare-Logic Inference System Used by VCC\n\nVCC's core reasoning engine encodes the *standard Hoare logic* (originally due to Floyd–Hoare, 1969) plus extensions for structured C. The relevant inference rules — every one of which is **directly testable** in the KTU 2024 scheme — are:\n\n### 2.1.1 The Assignment Axiom\nFor an assignment statement $x := E$, with $E$ free of side effects in the postcondition $Q$:\n\n$$\\{\\,Q[E/x]\\,\\}\\ \\ x := E\\ \\ \\{\\,Q\\,\\}$$\n\nwhere $Q[E/x]$ means \"$Q$ with every free occurrence of $x$ replaced by $E$\".\n\n**Why this is the 'right' backward rule:** It is the simplest way to *propagate* the postcondition backwards across an assignment, which is exactly what the *weakest precondition* transformer does.\n\n### 2.1.2 The Precondition-Strengthening Rule\nIf $P \\Rightarrow P'$ is provable, and $\\{\\,P'\\,\\} \\ C\\ \\{\\,Q\\,\\}$ is valid, then $\\{\\,P\\,\\} \\ C\\ \\{\\,Q\\,\\}$ is valid.\n\n### 2.1.3 The Postcondition-Weakening Rule\nIf $\\{\\,P\\,\\} \\ C\\ \\{\\,Q'\\,\\}$ is valid and $Q' \\Rightarrow Q$ is provable, then $\\{\\,P\\,\\} \\ C\\ \\{\\,Q\\,\\}$ is valid.\n\n### 2.1.4 The Composition Rule\nIf $\\{\\,P\\,\\} \\ C_1\\ \\{\\,R\\,\\}$ and $\\{\\,R\\,\\} \\ C_2\\ \\{\\,Q\\,\\}$, then $\\{\\,P\\,\\} \\ C_1;\\ C_2\\ \\{\\,Q\\,\\}$.\n\n### 2.1.5 The Conditional Rule\nIf $\\{\\,P \\wedge B\\,\\} \\ C_1\\ \\{\\,Q\\,\\}$ and $\\{\\,P \\wedge \\neg B\\,\\} \\ C_2\\ \\{\\,Q\\,\\}$, then $\\{\\,P\\,\\} \\ \\text{if}\\ B\\ \\text{then}\\ C_1\\ \\text{else}\\ C_2\\ \\ \\{\\,Q\\,\\}$.\n\n### 2.1.6 The While Rule (with invariant $I$)\nIf $\\{\\,I \\wedge B\\,\\} \\ C_{\\text{body}}\\ \\{\\,I\\,\\}$ holds, then $\\{\\,I\\,\\} \\ \\text{while}\\ B\\ \\text{do}\\ C_{\\text{body}}\\ \\ \\{\\,I \\wedge \\neg B\\,\\}$.\n\n> [!IMPORTANT]\n> The while-rule is the *only* rule in Hoare logic that requires **human-supplied loop invariants** $I$. This is precisely why VCC demands the user write `invariant(...)` annotations — it cannot invent invariants automatically in the general case.\n\n### 2.1.7 The Consequence Rule\nIf $P \\Rightarrow P'$ and $P' \\Rightarrow P$ (i.e., $P \\equiv P'$) — actually the *full* consequence rule combines strengthening and weakening:\n\n$$\\frac{P \\Rightarrow P' \\quad \\{\\,P'\\,\\}\\ C\\ \\{\\,Q'\\,\\} \\quad Q' \\Rightarrow Q}{\\{\\,P\\,\\}\\ C\\ \\{\\,Q\\,\\}}$$\n\n## 2.2 Weakest Precondition (WP) Transformer\n\nThe **weakest precondition** $wp(\\,C,\\,Q\\,)$ is the *loosest* (logically weakest) predicate such that $\\{\\,wp(\\,C,\\,Q\\,)\\} \\ C\\ \\{Q\\}$ is valid. It is the engine VCC uses to walk the control-flow graph backwards.\n\n**Definition (Dijkstra, 1976).** $wp(\\,C,\\,Q\\,)$ is the *weakest* predicate $P$ such that every execution of $C$ terminating in a state satisfying $Q$ begins in a state satisfying $P$.\n\n| Statement $C$ | $wp(\\,C,\\,Q\\,)$ |\n|----------------|------------------|\n| `x := E` (atomic) | $Q[E/x]$ |\n| `skip` | $Q$ |\n| `abort` | `false` |\n| `assert(R)` | $R \\wedge Q$ |\n| `assume(R)` | $R \\Rightarrow Q$ |\n| `C1; C2` | $wp(\\,C_1,\\ wp(\\,C_2,\\,Q\\,)\\,)$ |\n| `if (B) C1 else C2` | $(B \\wedge wp(\\,C_1,\\,Q\\,)) \\ \\vee\\ (\\neg B \\wedge wp(\\,C_2,\\,Q\\,))$ |\n| `while (B) { I; C }` | $I$ (provided $\\{I \\wedge B\\}\\ C\\ \\{I\\}$ and $I \\wedge \\neg B \\Rightarrow Q$) |\n\n> [!NOTE]\n> For an `assert(R)`, the *weakest* precondition that guarantees both $R$ holds at runtime and $Q$ holds afterwards is $R \\wedge Q$. VCC emits a *proof obligation* that the caller's state implies $R \\wedge Q$ before the assert executes.\n\n## 2.3 The KTU High-Yield Formula Sheet\n\n| # | Construct | Hoare Rule / WP | VCC Keyword |\n|---|-----------|-----------------|-------------|\n| 1 | Assignment | $wp(\\,x{:=}E,\\,Q\\,) = Q[E/x]$ | (built-in) |\n| 2 | Skip | $wp(\\,\\text{skip},\\,Q\\,) = Q$ | `;` |\n| 3 | Assert | $wp(\\,\\text{assert}(R),\\,Q\\,) = R \\wedge Q$ | `assert(R)` |\n| 4 | Assume | $wp(\\,\\text{assume}(R),\\,Q\\,) = (R \\Rightarrow Q)$ | `assume(R)` |\n| 5 | Sequence | $wp(\\,C_1;C_2,\\,Q\\,) = wp(\\,C_1,\\,wp(\\,C_2,\\,Q\\,)\\,)$ | (sequencing) |\n| 6 | Conditional | $wp(\\,\\text{if }B\\text{ then }C_1\\text{ else }C_2,\\,Q\\,) = (B \\wedge wp(\\,C_1,Q\\,)) \\vee (\\neg B \\wedge wp(\\,C_2,Q\\,))$ | `if / else` |\n| 7 | While (inv $I$) | require $\\{I \\wedge B\\}\\ C_{\\text{body}}\\ \\{I\\}$; conclude $I$ | `while(...) invariant(I)` |\n| 8 | Function call | $\\text{Requires}(P) \\Rightarrow wp(\\,\\text{body},\\,Q\\,)$ | `requires(P) ensures(Q)` |\n| 9 | Consequence | $\\frac{P \\Rightarrow P', \\ Q' \\Rightarrow Q}{\\{P\\}\\ C\\ \\{Q\\}}$ | automatic via SMT |\n| 10 | Ghost update | $wp(\\,\\text{ghost }x{:=}E,\\,Q\\,) = Q[E/x]$ (verified, then erased) | `ghost(...)` |\n\n> [!WARNING]\n> In markdown tables, the disjunction symbol is rendered using $\\vee$ so that no raw `|` appears inside the table. This avoids breaking the table column separator.\n\n## 2.4 Intraprocedural Verification — Algorithmic Pipeline\n\nThe *intraprocedural* verifier in VCC performs the following six steps:\n\n1. **Parse** the annotated C body of a single function.\n2. **Build the Control-Flow Graph (CFG)** — nodes are program points; edges are statements.\n3. **Cut the loops** by introducing a fresh predicate variable for each loop head — this is the loop invariant placeholder $I$.\n4. **Apply the WP transformer** along every edge, propagating the user's `ensures(Q)` backwards to the function entry.\n5. **Emit VCs** — first-order proof obligations of the form $P \\Rightarrow wp(\\,C,\\,Q\\,)$ at every cut.\n6. **Dispatch** each VC to the **Z3 SMT solver** via the Boogie IVL; success = verified, counter-example = VCC prints a failed obligation.\n\n> [!TIP]\n> In a KTU 14-mark answer, the six steps above are worth **3 marks** by themselves. Always enumerate them — examiners reward structure.\n\n## 2.5 Ghost Statements — What They Are and Why They Are Needed\n\nA **ghost statement** is *C code* that is **type-checked and verified by VCC** but **erased before machine-code generation**. VCC recognises ghost contexts via the `ghost` keyword (and by typing — ghost data lives in a separate heap universe).\n\n### 2.5.1 Three Primary Uses\n\n| Use Case | KTU-Relevant Insight |\n|----------|----------------------|\n| **Loop-variant / measure** | Prove termination with `decreases(...)`. |\n| **Inductive invariants** | A ghost variable that records a *history* of values (e.g., a counter of writes). |\n| **Auxiliary lemma calls** | A `ghost_call` to a function whose only purpose is to discharge a proof obligation. |\n\n> [!IMPORTANT]\n> A ghost statement must be **erasable**: removing it must not change the semantics of any concrete (non-ghost) variable. VCC enforces this by a *non-interference* check.\n\n## 2.6 Real-World Utility of VCC in Engineering\n\n- **Hyper-V / Windows kernel drivers**: Microsoft used VCC to verify the core of the Hyper-V hypervisor's device virtualization, including concurrent ring-buffer code. This is a flagship industrial deployment.\n- **Avionics and railway signaling**: The pattern of \"annotate C, prove it with VCC/Boogie\" has influenced DO-178C supplement DO-333 (formal methods) workflows.\n- **Cryptographic libraries**: Used to verify constant-time, side-channel-resistant implementations.\n\n## 2.7 Real-World Utility of the Underlying Equations\n\nThe *weakest precondition calculus* underpins not just VCC but **AutoProof (Eiffel)**, **Frama-C / WP plugin**, **SPARK Ada**, **Dafny**, and **Spec#** (VCC's own close cousin). Mastering $wp(\\,C,\\,Q\\,)$ for the 7 constructs above is therefore a **transferable skill** across the formal-methods industry.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation 1 — The WP of an Assignment from the Hoare Assignment Axiom\n\nWe want to derive $wp(\\,x := E,\\,Q\\,) = Q[E/x]$ formally.\n\n**Step 1.** Recall the Hoare assignment axiom (Floyd–Hoare, 1969):\n\n$$\\{\\,Q[E/x]\\,\\}\\ \\ x := E\\ \\ \\{\\,Q\\,\\}$$\n\n**Step 2.** The axiom guarantees that *if* we start in a state satisfying $Q[E/x]$, *then* after the assignment $x := E$ we end in a state satisfying $Q$. This means $Q[E/x]$ is **a sufficient precondition**.\n\n**Step 3.** We must show it is also the **weakest** sufficient precondition. Suppose some other predicate $P$ satisfies $\\{P\\}\\ x := E\\ \\{Q\\}$. Pick any state $\\sigma$ satisfying $P$. After the assignment, the new state is $\\sigma'$ with $\\sigma'(x) = \\llbracket E \\rrbracket_\\sigma$ and otherwise identical. We need $\\sigma' \\models Q$. The only way this can hold for *every* $\\sigma$ satisfying $P$ is that the *initial* value of $x$ (call it $v$) satisfies $Q$ when $x$ is replaced by $\\llbracket E \\rrbracket_\\sigma$. Since $E$ evaluates to $v' = \\llbracket E \\rrbracket_\\sigma$, we need $P$ to imply that the value currently bound to $x$ (i.e., $v$) is such that $Q[v'/x]$ — which is exactly $Q[E/x]$.\n\n**Step 4.** Therefore $P \\Rightarrow Q[E/x]$, which is the definition of $P$ being *stronger* than $Q[E/x]$. Hence $Q[E/x]$ is the weakest such $P$, and:\n\n$$wp(\\,x := E,\\,Q\\,) = Q[E/x]$$\n\n> [!NOTE]\n> The notation $Q[E/x]$ is read \"Q with E substituted for x\" (also called *capture-avoiding substitution*). For composite $E$ it is defined recursively on the structure of $Q$.\n\n## 3.2 Derivation 2 — WP of an Assert Statement\n\n**Step 1.** An `assert(R)` is a statement that, at runtime, checks that the current state satisfies $R$. If the check fails the program aborts. Therefore, for any postcondition $Q$ to hold *after* the assert, the assert must not abort — i.e., $R$ must hold — *and* $Q$ must hold.\n\n**Step 2.** Combining these two requirements conjunctively gives $R \\wedge Q$.\n\n**Step 3.** Formally, the Hoare rule is:\n\n$$\\frac{}{\\{R \\wedge Q\\}\\ \\text{assert}(R)\\ \\{Q\\}}$$\n\n**Step 4.** By definition of *weakest* precondition, we cannot drop $R$ (the program would be allowed to abort) and we cannot drop $Q$ (the postcondition must follow). Hence $R \\wedge Q$ is indeed the weakest.\n\n$$wp(\\,\\text{assert}(R),\\,Q\\,) = R \\wedge Q$$\n\n## 3.3 Derivation 3 — WP of an Assume Statement\n\n**Step 1.** An `assume(R)` does not check $R$. Instead, it *prunes* executions: any execution reaching the assume in a state where $R$ is false is *removed* from consideration. So the weakest precondition must make those pruned paths *impossible* to begin with, which is achieved by the implication $R \\Rightarrow Q$ on the pre-state.\n\n**Step 2.** Equivalently, after an assume, the only states that can continue are those where $R$ holds. If we want the post-state to satisfy $Q$ in every surviving execution, the pre-state must satisfy $R \\Rightarrow Q$ — because if $R$ holds, $Q$ must hold, and if $R$ does not hold, the execution is gone, so the implication is trivially satisfied.\n\n$$wp(\\,\\text{assume}(R),\\,Q\\,) = (R \\Rightarrow Q)$$\n\n## 3.4 Derivation 4 — WP of a Sequence\n\nLet $C_1$ and $C_2$ be statements. We compute $wp(\\,C_1; C_2,\\,Q\\,)$ by *composing* the two WPs.\n\n**Step 1.** Let $R = wp(\\,C_2,\\,Q\\,)$. Then by definition, $\\{R\\}\\ C_2\\ \\{Q\\}$ is valid with $R$ weakest.\n\n**Step 2.** To find $wp(\\,C_1; C_2,\\,Q\\,)$, we must find the weakest $P$ such that $\\{P\\}\\ C_1\\ \\{R\\}$ (because then after $C_1$ we are in an $R$-state, and from there $C_2$ gives $Q$).\n\n**Step 3.** The weakest such $P$ is exactly $wp(\\,C_1,\\,R\\,) = wp(\\,C_1,\\,wp(\\,C_2,\\,Q\\,)\\,)$.\n\n$$wp(\\,C_1; C_2,\\,Q\\,) = wp(\\,\\ C_1,\\ \\ wp(\\,C_2,\\,Q\\,)\\ \\,)$$\n\n## 3.5 Derivation 5 — WP of an If-Statement\n\n**Step 1.** For `if (B) C1 else C2`, the runtime branch is *non-deterministic* with respect to the verifier unless $B$'s truth is known. To prove $\\{P\\}\\ \\text{if}\\,(B)\\,C_1\\,\\text{else}\\,C_2\\ \\{Q\\}$ for the weakest $P$, we need:\n\n- $P$ must guarantee that *if* $B$ holds then $C_1$ leads to $Q$, *and*\n- $P$ must guarantee that *if* $B$ does not hold then $C_2$ leads to $Q$.\n\n**Step 2.** Therefore the weakest $P$ is the disjunction of the two cases:\n\n$$wp(\\,\\text{if }B\\text{ then }C_1\\text{ else }C_2,\\,Q\\,) = (B \\wedge wp(\\,C_1,\\,Q\\,)) \\ \\vee\\ (\\neg B \\wedge wp(\\,C_2,\\,Q\\,))$$\n\n## 3.6 Worked Example — WP Computation on a Tiny Snippet\n\nConsider the following annotated C procedure (intraprocedural scope):\n\n```c\nint square(int n)\n    requires(n >= 0)\n    ensures(\\result == n * n);\n{\n    int r = 0;\n    int i = 0;\n    while (i < n)\n        invariant(0 <= i && i <= n && r == i * i)\n        decreases(n - i)\n    {\n        r = r + 2 * i + 1;\n        i = i + 1;\n    }\n    return r;\n}\n```\n\nWe compute the WP starting from the postcondition $\\text{ensures}(\\result == n \\cdot n)$:\n\n**Step 1.** `return r;` is, in Hoare terms, equivalent to `x := r` followed by treating the postcondition as $Q[\\result / x]$. But for simplicity we work backwards through the loop invariant $I \\equiv (0 \\le i \\le n) \\wedge (r = i \\cdot i)$.\n\n**Step 2.** At loop exit, the predicate $I \\wedge \\neg B$ must imply $\\result = n \\cdot n$:\n\n$$(0 \\le i \\le n) \\wedge (r = i \\cdot i) \\wedge (i \\ge n) \\ \\Rightarrow\\ (r = n \\cdot n)$$\n\n**Step 3.** The body of the loop has $I \\wedge B$ as its precondition (where $B \\equiv i < n$). We compute the WP of the body, taken under the invariant $I$:\n\n$$wp(\\,r := r + 2i + 1,\\ wp(\\,i := i + 1,\\,I\\,)\\,)$$\n\n**Step 4.** Inner WP: $wp(\\,i := i+1,\\,I\\,) = I[i+1/i]$. So:\n\n$$I[i+1/i] \\equiv (0 \\le i+1 \\le n) \\wedge (r = (i+1) \\cdot (i+1))$$\n\n**Step 5.** Outer WP: substitute $r + 2i + 1$ for $r$:\n\n$$(0 \\le i+1 \\le n) \\wedge (r + 2i + 1 = (i+1) \\cdot (i+1))$$\n\n**Step 6.** Algebraic simplification of the right side:\n\n$$(i+1) \\cdot (i+1) = i \\cdot i + 2i + 1$$\n\n**Step 7.** Therefore the WP becomes:\n\n$$(0 \\le i+1 \\le n) \\wedge (r = i \\cdot i)$$\n\n**Step 8.** We must prove that $I \\wedge B$ implies this WP. That is:\n\n$$(0 \\le i \\le n) \\wedge (r = i \\cdot i) \\wedge (i < n) \\ \\Rightarrow\\ (0 \\le i+1 \\le n) \\wedge (r = i \\cdot i)$$\n\n**Step 9.** The conjunct $r = i \\cdot i$ matches directly. The conjunct $0 \\le i+1 \\le n$ follows because $i \\ge 0$ and $i < n$ together give $i+1 \\le n$.\n\n> [!IMPORTANT]\n> The above nine steps constitute a **full intraprocedural proof** that VCC would attempt. The proof obligations at steps 8 and 2 are precisely the VCs that VCC emits to Boogie/Z3. In the KTU exam, **write out the WP algebra explicitly** — the marks are for the *transformation*, not the answer.\n\n## 3.7 Analytical Matrix — Verification Obligations Generated by VCC\n\n| # | Statement Type | VC Shape | Notes |\n|---|----------------|----------|-------|\n| 1 | `requires(P)` at function entry | $P_{\\text{caller}} \\Rightarrow P_{\\text{callee}}$ | Caller must establish callee's precondition. |\n| 2 | `ensures(Q)` at function exit | $wp(\\,\\text{body},\\,Q\\,) \\Rightarrow Q_{\\text{exit}}$ | Postcondition must hold after body. |\n| 3 | `assert(R)` | $P_{\\text{pre-assert}} \\Rightarrow (R \\wedge Q_{\\text{after}})$ | Pre-state must imply $R$. |\n| 4 | `assume(R)` | $P_{\\text{pre-assume}} \\Rightarrow (R \\Rightarrow Q_{\\text{after}})$ | Implication form. |\n| 5 | Loop invariant preservation | $I \\wedge B \\Rightarrow wp(\\,C_{\\text{body}},\\,I\\,)$ | Loop body must re-establish $I$. |\n| 6 | Loop invariant establishment | $P_{\\text{pre-loop}} \\Rightarrow I$ | Invariant must hold at entry. |\n| 7 | Use before definition | $P_{\\text{use}} \\Rightarrow \\text{defined}(x)$ | No reading of uninitialised data. |\n| 8 | Ghost purity | No write to a concrete variable in ghost code | VCC type system enforces. |\n| 9 | Concurrency ownership | $O_1 \\cap O_2 = \\emptyset$ for two threads | Disjoint ownership of the shared region. |\n| 10 | Decreases measure | $P \\wedge B \\Rightarrow (E > 0 \\wedge E < E_0)$ | Termination via well-founded order. |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 VCC Tool Architecture (Block-Level Functional Flow)\n\nThe following diagram shows the end-to-end VCC compilation-verification pipeline, from annotated C source down to either verified Boogie output or a counter-example trace.\n\n```mermaid\nflowchart LR\n    A[SRC_C: Annotated C Source]:::in --> B[VCC Front End Parser]:::in\n    B --> C[Abstract Syntax Tree]:::in\n    C --> D[Contract Extractor]:::mid\n    D --> E[CFG Builder per Procedure]:::mid\n    E --> F[WP Transformer]:::core\n    F --> G[VC Emitter]:::core\n    G --> H[Boogie IVL Output]:::mid\n    H --> I[Boogie Verifier]:::core\n    I --> J{SMT Solver Z3}:::core\n    J -->|UNSAT all VCs| K[VERIFIED]:::ok\n    J -->|SAT some VC| L[FAILED with Counter Example]:::err\n    L --> M[Source Mapping]:::err\n    M --> N[Diagnostic to User]:::err\n    K --> O[Compilable C Output Ghost Erased]:::out\n    A -.-> O\n\n    classDef in fill:#E8F1FB,stroke:#2A5DB0,color:#0B2545\n    classDef mid fill:#FFF4D6,stroke:#B07A00,color:#3D2A00\n    classDef core fill:#DDF2D8,stroke:#3A7A2A,color:#1B3D0B\n    classDef out fill:#E0E5F5,stroke:#4B4F8B,color:#101246\n    classDef ok fill:#C7F0C0,stroke:#0F6B0F,color:#0A2E0A\n    classDef err fill:#F8D0CE,stroke:#8B1A14,color:#400B07\n```\n\n**Reading guide:** Front-end nodes (`in`) are *blue*, Boogie-side middle nodes (`mid`) are *amber*, the core deductive engine (`core`) is *green*. Success path ends in `K` (verified), failure in `L`/`N` (diagnostic). Note that the same annotated C source `A` feeds *both* the verification pipeline and the *separate* MSVC compilation path `O` (with ghost code stripped).\n\n## 4.2 Intraprocedural Verification Flow Within One Function\n\n```mermaid\nflowchart TD\n    P1[Entry Contract: requires P]:::in --> P2[Initialize Ghost State]:::mid\n    P2 --> P3[Apply WP Backward through CFG]:::core\n    P3 --> P4{At Loop Head?}:::core\n    P4 -->|Yes| P5[Insert Invariant Predicate I]:::core\n    P4 -->|No| P6[Continue WP Propagation]:::core\n    P5 --> P7[Check Invariant Establishment VC]:::core\n    P7 --> P8[Check Invariant Preservation VC]:::core\n    P8 --> P9[Check Decreases Measure VC]:::core\n    P9 --> P10[Check Loop Exit Implies Postcondition]:::core\n    P6 --> P11[Check Branch VCs for If Else]:::core\n    P11 --> P12[Check Assert Assume VCs]:::core\n    P10 --> P13[All VCs Discharged by Z3]:::core\n    P12 --> P13\n    P13 --> P14[Function Body VERIFIED]:::ok\n\n    classDef in fill:#E8F1FB,stroke:#2A5DB0,color:#0B2545\n    classDef mid fill:#FFF4D6,stroke:#B07A00,color:#3D2A00\n    classDef core fill:#DDF2D8,stroke:#3A7A2A,color:#1B3D0B\n    classDef ok fill:#C7F0C0,stroke:#0F6B0F,color:#0A2E0A\n```\n\n## 4.3 Ghost Statement Lifecycle\n\n```mermaid\nflowchart LR\n    Q1[Programmer Writes Ghost Annotation]:::in --> Q2[VCC Type Checks Ghost]:::mid\n    Q2 --> Q3[Ghost is Verified: Proof Obligations]:::core\n    Q3 --> Q4{All Ghost Proof Obligations Pass?}:::core\n    Q4 -->|Yes| Q5[Ghost Erased from C Source]:::mid\n    Q4 -->|No| Q6[Verification Error Report]:::err\n    Q5 --> Q7[Pure C Emitted to Compiler]:::out\n    Q6 --> Q8[Source Locator and Hint]:::err\n\n    classDef in fill:#E8F1FB,stroke:#2A5DB0,color:#0B2545\n    classDef mid fill:#FFF4D6,stroke:#B07A00,color:#3D2A00\n    classDef core fill:#DDF2D8,stroke:#3A7A2A,color:#1B3D0B\n    classDef out fill:#E0E5F5,stroke:#4B4F8B,color:#101246\n    classDef err fill:#F8D0CE,stroke:#8B1A14,color:#400B07\n```\n\n## 4.4 Relationship Between Hoare Logic, WP, and VCC's Three Pillars\n\n```mermaid\nflowchart TB\n    A1[Hoare Logic Foundation 1969]:::root --> B1[Hoare Triple P C Q]:::mid\n    A1 --> B2[Assignment Axiom Q E over x]:::mid\n    A1 --> B3[While Rule with Invariant I]:::mid\n    B1 --> C1[Dijkstra WP Transformer 1976]:::mid\n    B2 --> C1\n    B3 --> C1\n    C1 --> D1[VCC Intraprocedural VC Generation]:::core\n    C1 --> D2[VCC Ghost Statement Reasoning]:::core\n    C1 --> D3[VCC Concurrent Ownership via Boogie]:::core\n    D1 --> E1[Microsoft Hyper V Kernel Verified]:::ok\n    D2 --> E1\n    D3 --> E1\n\n    classDef root fill:#F0E5FF,stroke:#5A2A8B,color:#1A0633\n    classDef mid fill:#FFF4D6,stroke:#B07A00,color:#3D2A00\n    classDef core fill:#DDF2D8,stroke:#3A7A2A,color:#1B3D0B\n    classDef ok fill:#C7F0C0,stroke:#0F6B0F,color:#0A2E0A\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 PART A — 3-Mark Short-Answer Questions\n\n### Q1. Define the Hoare triple and explain its role in VCC. `[KTU University Exam – Dec 2023, PECST741]`\n\n**Model Answer (≈ 3 marks):**\nA Hoare triple is a logical statement of the form $\\{P\\}\\ C\\ \\{Q\\}$, where $P$ is a *precondition* (predicate on the state before execution), $C$ is a *command* (program statement), and $Q$ is a *postcondition* (predicate on the state after execution). The triple is **valid** iff every execution of $C$ starting in a state satisfying $P$ terminates in a state satisfying $Q$. In VCC, every annotated function carries a Hoare triple — `requires(P)` plays the role of $P$, and `ensures(Q)` plays the role of $Q$. The body of the function must then be proven correct w.r.t. this triple using the inference rules of Hoare logic. **(3 marks)**\n\n---\n\n### Q2. What is a \"ghost statement\" in VCC? Give one example. `[KTU University Exam – May 2024, PECST741]`\n\n**Model Answer (≈ 3 marks):**\nA **ghost statement** is a piece of C code (declared with the `ghost` keyword) that is **type-checked and verified** by VCC but is **completely erased** before the program is handed to the C compiler. It is used purely as a *proof aid* — for example, to maintain an auxiliary counter, a decreasing measure for termination, or an inductive invariant. **Example:**\n\n```c\n/*@ ghost int write_count = 0; @*/\n\nvoid increment()\n    requires(write_count < 1000)\n    ensures(write_count == \\old(write_count) + 1);\n{\n    /*@ ghost write_count = write_count + 1; @*/\n    /* concrete code */\n}\n```\nGhost code is non-interfering: it cannot influence concrete state. **(3 marks)**\n\n---\n\n## 5.2 PART B — 14-Mark Questions (ESE Module — Internal Choice)\n\n> **KTU 2024 Scheme Note:** Each Part-B question carries **14 marks**, split typically as (a) **7 marks** and (b) **7 marks**. Internal choice is *between* Question A and Question B (not between sub-parts).\n\n---\n\n### QUESTION A (14 Marks)\n\n#### Q.A(a) `[7 marks]` — WP Computation on a Program Fragment\n\n> *Consider the following C-like annotated code:*\n>\n> ```c\n> { P }\n> if (x > 0)\n>     y = x + 1;\n> else\n>     y = -x;\n> { Q : y = abs(x) }\n> ```\n>\n> *(a) Compute the weakest precondition $wp(\\,\\text{if-then-else},\\,Q\\,)$ symbolically and hence derive a sufficient precondition $P$ that guarantees $Q$. (b) Explain how VCC would encode this verification obligation for the Z3 SMT solver.*\n\n**Model Solution:**\n\n**(a) Symbolic computation of WP.** From the table in Section 2.2, for an if-statement:\n\n$$wp(\\,\\text{if }B\\text{ then }C_1\\text{ else }C_2,\\,Q\\,) = (B \\wedge wp(\\,C_1,\\,Q\\,)) \\ \\vee\\ (\\neg B \\wedge wp(\\,C_2,\\,Q\\,))$$\n\nLet $B \\equiv (x > 0)$, $C_1 \\equiv y := x + 1$, $C_2 \\equiv y := -x$, and $Q \\equiv (y = \\text{abs}(x))$.\n\n- $wp(\\,y := x+1,\\,y = \\text{abs}(x)\\,) = (x + 1 = \\text{abs}(x))$\n- $wp(\\,y := -x,\\,y = \\text{abs}(x)\\,) = (-x = \\text{abs}(x))$\n\nHence:\n\n$$wp(\\,C,\\,Q\\,) = (x > 0 \\wedge x + 1 = \\text{abs}(x)) \\ \\vee\\ (x \\le 0 \\wedge -x = \\text{abs}(x))$$\n\nA simple sufficient precondition is $P \\equiv (x > 0 \\Rightarrow x + 1 = x + 1) \\wedge (x \\le 0 \\Rightarrow -x = -x)$, which is simply `true` — i.e., the program is correct for *every* integer $x$. Alternatively, take $P \\equiv \\text{true}$. **(7 marks — full WP derivation: 4, simplification: 2, final P: 1)**\n\n**(b) VCC encoding for Z3.** VCC would:\n\n1. Translate the predicate into a Boogie `assert` statement placed at the function entry.\n2. Z3 then tries to find an integer assignment to $x$ that *violates* the predicate. Since the disjunction is logically valid, the SMT query returns **UNSAT** and the obligation is discharged.\n3. The encoding uses uninterpreted functions for `abs` if not modelled, or an explicit axiomatisation $\\forall x.\\ (x \\ge 0 \\Rightarrow \\text{abs}(x) = x) \\wedge (x < 0 \\Rightarrow \\text{abs}(x) = -x)$.\n\n---\n\n#### Q.A(b) `[7 marks]` — Intraprocedural Verification Algorithm\n\n> *Explain, with a labelled diagram, the six steps VCC performs for the intraprocedural verification of a single procedure's body. Highlight the role of the loop invariant and the weakest precondition transformer.*\n\n**Model Solution:**\n\nVCC performs the following six steps within a *single* procedure (no calls considered except atomically):\n\n1. **Parse** the annotated C body.\n2. **Build the Control-Flow Graph (CFG)** where nodes are program points and edges are statements.\n3. **Cut loops** by introducing a fresh predicate variable $I$ at each loop head — this is the loop invariant.\n4. **Apply the WP transformer** along every edge, propagating the `ensures(Q)` *backwards* to the function entry.\n5. **Emit Verification Conditions (VCs)** of the form $P_{\\text{pre}} \\Rightarrow wp(\\,C,\\,P_{\\text{post}}\\,)$ at every cut.\n6. **Dispatch** each VC to the **Z3 SMT solver** through the **Boogie IVL**; success = verified.\n\nThe **loop invariant** is the human-supplied predicate that must hold at the head of the loop, must be re-established by the loop body, and must imply the postcondition at loop exit (when $B$ is false). The **WP transformer** automates the *backward* propagation; the *invariant* is the single point where the verifier must ask the user for help, because in general the invariant is not computable. **(7 marks — 6 steps: 3, role of invariant: 2, role of WP: 2)**\n\n---\n\n### QUESTION B (14 Marks) — *Internal Alternative to Question A*\n\n#### Q.B(a) `[7 marks]` — Ghost Statements: Purpose, Rules, and a Worked Example\n\n> *(a) What problem do ghost statements solve in VCC? (b) State two syntactic forms of ghost statements supported by VCC. (c) Construct a small annotated C function that uses a ghost variable to prove that a loop executes exactly $n$ times.*\n\n**Model Solution:**\n\n**(a) Problem solved (≈ 2 marks).** Some correctness arguments (induction, decreasing measures, complex relational invariants) cannot be stated in plain Hoare triples. Ghost statements allow the programmer to *introduce auxiliary state* that records intermediate proof information, without altering the *concrete* behaviour of the program.\n\n**(b) Two syntactic forms (≈ 2 marks):**\n\n- `/*@ ghost T x = init; @*/` — a ghost variable declaration.\n- `/*@ ghost (T) = fn_call(args); @*/` — a ghost function call.\n- `/*@ ghost x = E; @*/` — a ghost assignment.\n\n**(c) Worked example (≈ 3 marks):**\n\n```c\nvoid run_n_times(int n)\n    requires(n >= 0)\n    ensures(true);\n{\n    int i = 0;\n    /*@ ghost int g = 0; @*/\n    while (i < n)\n        invariant(0 <= i && i <= n && g == i)\n        decreases(n - i)\n    {\n        i = i + 1;\n        /*@ ghost g = g + 1; @*/\n    }\n}\n```\n\nThe ghost variable `g` records how many iterations have been performed. The invariant $g = i$ is preserved by every iteration (the concrete update `i = i + 1` is mirrored by the ghost update `g = g + 1`). At loop exit, $g = n$, but this fact exists only in the verification world; it does not appear in the compiled binary. **(7 marks total)**\n\n---\n\n#### Q.B(b) `[7 marks]` — Soundness, Completeness, and Limitations of VCC\n\n> *Discuss the **soundness** of VCC's Hoare-style verification, its **relative completeness** with respect to Hoare logic, and two practical limitations that an engineer must keep in mind.*\n\n**Model Solution:**\n\n**Soundness (≈ 2 marks).** VCC is *sound*: if it reports \"verified\", then for every execution of the program (concurrent or sequential), the contracts hold. This is guaranteed by construction because VCs are derived using only the *sound* Hoare inference rules and discharged by a sound SMT solver (Z3).\n\n**Relative completeness (≈ 2 marks).** VCC is *relatively complete* in the sense of Cook: for any valid triple $\\{P\\}\\ C\\ \\{Q\\}$, there *exists* an annotation (loop invariant, intermediate assertion) that allows VCC to prove it. However, the annotation must be *supplied by the user* — VCC cannot invent invariants in the general case.\n\n**Two practical limitations (≈ 3 marks):**\n\n1. **Annotation burden.** Writing contracts and invariants is a non-trivial engineering effort, often comparable in size to the code itself. For large code bases this becomes the dominant cost.\n2. **SMT non-termination.** Z3 may not terminate on complex VCs (e.g., nonlinear integer arithmetic, deep quantifier alternations). VCC may time out rather than return a definitive answer.\n\nA third, often-mentioned limitation: VCC targets *C* (not C++ in full), and assumes a well-defined C semantics that some embedded / GNU-extension code violates. **(7 marks total)**\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall — How Students Lose Marks on This Topic**\n>\n> 1. **Writing the Hoare triple without naming the inference rule** used — always say \"by the assignment axiom\" or \"by the consequence rule\". **(–2 marks)**\n> 2. **Confusing weakest precondition (WP) with strongest postcondition (SP)** — VCC uses *WP* (backward), not *SP* (forward). Examiners will deduct for a forward-only derivation. **(–2 marks)**\n> 3. **Forgetting that ghost code is type-checked and verified** — students often wrongly claim ghost code is \"ignored\" by VCC; in fact VCC *proves* ghost code correct and only then erases it. **(–1 mark)**\n> 4. **Drawing the if-rule as a *conjunction* rather than a *disjunction*** of the two branch WPs. **(–1 mark)**\n> 5. **Omitting the loop invariant entirely** when describing intraprocedural verification. Loop invariants are *non-negotiable*. **(–2 marks)**\n> 6. **Citing \"VCC\" without a single Hoare-rule reference** — the question is from a *Formal Methods* paper, so examiners expect the *logic*, not just tool syntax. **(–2 marks)**\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!TIP]\n> **Rapid-Revision Checklist (last 5 minutes before the exam):**\n\n- [ ] **Hoare triple** $\\{P\\}\\ C\\ \\{Q\\}$ — partial correctness semantics; $P$ precondition, $Q$ postcondition.\n- [ ] **Assignment axiom** — $wp(\\,x := E,\\,Q\\,) = Q[E/x]$; substitute *expression for variable* in postcondition.\n- [ ] **Sequence** — $wp(\\,C_1;C_2,\\,Q\\,) = wp(\\,C_1,\\,wp(\\,C_2,\\,Q\\,)\\,)$; right-to-left WP composition.\n- [ ] **Conditional** — $wp(\\,\\text{if }B\\text{ then }C_1\\text{ else }C_2,\\,Q\\,) = (B \\wedge wp(\\,C_1,Q\\,)) \\vee (\\neg B \\wedge wp(\\,C_2,Q\\,))$ — *disjunction* of the two branches.\n- [ ] **While (loop)** — requires loop invariant $I$, with $I \\wedge B \\Rightarrow wp(\\,C_{\\text{body}},\\,I\\,)$ as the preservation VC.\n- [ ] **Assert** — $wp(\\,\\text{assert}(R),\\,Q\\,) = R \\wedge Q$.\n- [ ] **Assume** — $wp(\\,\\text{assume}(R),\\,Q\\,) = (R \\Rightarrow Q)$.\n- [ ] **VCC's 6 intraprocedural steps** — Parse → CFG → Cut loops with $I$ → WP propagate → Emit VCs → SMT via Boogie/Z3.\n- [ ] **Ghost statement** — verified, then erased; non-interfering with concrete state; lives in a separate ghost heap universe.\n- [ ] **Ghost uses** — loop-variant / decreases, inductive invariants, auxiliary lemma calls.\n- [ ] **Soundness** — VCC never says \"verified\" falsely.\n- [ ] **Relative completeness** — every valid triple is provable *if* the right invariant is given.\n- [ ] **Real-world deployment** — Microsoft Hyper-V hypervisor device-virtualization core.\n- [ ] **Tool chain** — VCC (front end) → Boogie (IVL) → Z3 (SMT solver).\n- [ ] **KTU keyword associations** — `requires` ↔ $P$; `ensures` ↔ $Q$; `invariant` ↔ $I$; `decreases` ↔ termination measure; `assert` ↔ runtime check; `assume` ↔ environment assumption; `ghost` ↔ proof-only code.\n<!-- SECTION_5_END -->",
          "pyqs": []
        }
      ]
    },
    {
      "id": "m4",
      "title": "Module 4",
      "topics": [
        {
          "id": "artificial-intelligence-and-data-science-7-pecst741-mModule 4-t1",
          "title": "Inter-procedure Verification of Programs in VCC",
          "content": "<!-- SECTION_1_START -->\n> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST741 - FORMAL METHODS IN SOFTWARE ENGINEERING\n> - **Module:** Module 4\n> - **Topic:** Inter-procedure Verification of Programs in VCC\n\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition\n\n**Inter-procedure Verification** in **VCC (Verification Condition Calculator)** is the systematic process of reasoning about the correctness of a C program across function/procedure boundaries by composing individual procedure-level proof obligations into a single, global proof obligation. Each procedure is annotated with a **contract** comprising a *precondition* (`requires`), a *postcondition* (`ensures`), and a *modifies/assigns* clause that bounds the side effects a function may perform.\n\nWhen a caller invokes a procedure, VCC must guarantee that the caller's state at the call site satisfies the callee's precondition. Upon return, VCC must re-establish the caller's post-state using the callee's postcondition, while ensuring that the **frame condition** (the set of locations the callee is permitted to modify) is respected.\n\nMathematically, for a procedure `P` with contract `{ requires Q; assigns A; ensures R }`, inter-procedural verification demands:\n\n$$\n\\frac{\\Gamma \\vdash Q \\quad \\Gamma, R, \\text{modifies}(A) \\vdash S_{\\text{rest}}}{\\Gamma \\vdash S \\;\\; \\text{call } P(\\vec{x})}\n$$\n\nThis is the **Call Rule** of VCC's program logic, often called the **consequence rule** for procedure calls.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Highlight — Module 4**\n> Inter-procedure verification in VCC is rooted in the **Dijkstra-style weakest precondition calculus** combined with **separation logic** extensions. VCC specifically uses *two-state invariants* and *ownership* semantics to allow modular reasoning at the procedure level.\n\n## 1.2 Conceptual Analogy / Intuition\n\n> [!NOTE]\n> **Plain-English Analogy — The \"Contract between Engineers\"**\n>\n> Imagine a construction project with multiple subcontractors. Each subcontractor signs a *contract* before starting:\n> - **What they need to begin** (precondition — \"The site must be cleared and permit issued\")\n> - **What they promise to deliver** (postcondition — \"A finished wall painted blue\")\n> - **What they are allowed to touch** (assigns clause — \"Only the wall in Sector B\")\n>\n> The general contractor (the *caller*) doesn't need to know *how* the subcontractor builds the wall — only that the contract is honored. Inter-procedure verification in VCC works identically: each function is a \"subcontractor\" with a contract, and VCC verifies that *every caller* of that function abides by the contract at the point of invocation. This is **modular verification** — you don't need to look inside a function to trust it, just trust its contract.\n\n## 1.3 Why Inter-procedure Verification is Hard\n\n> [!WARNING]\n> **Three Core Challenges in Inter-procedural Reasoning**\n>\n> 1. **State Coupling**: Two procedures share global/heap state — a callee might mutate objects the caller relies on.\n> 2. **Aliasing**: The `assigns` clause must be precise, otherwise soundness breaks.\n> 3. **Recursive Calls**: Reasoning must support cyclic call graphs, requiring **induction over call depth** or **k-induction**.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 VCC's Logical Foundations\n\nVCC is built on top of three pillars:\n- **Boogie intermediate verification language (IVL)**\n- **Z3 SMT solver** (developed at Microsoft Research)\n- **A two-state separation logic with ownership**\n\nWhen VCC encounters a C file, it translates each C procedure into a **Boogie procedure**. The body of every C function is encoded as a control-flow graph of Boogie `goto` statements. VCC then emits **verification conditions (VCs)** — first-order logic formulas that, if discharged by Z3, prove the program's correctness.\n\nThe translation pipeline is:\n\n$$\n\\text{C Source} \\xrightarrow{\\text{Parse}} \\text{Boogie AST} \\xrightarrow{\\text{WP Calc}} \\text{VCs} \\xrightarrow{\\text{Z3}} \\text{Valid / Invalid}\n$$\n\n## 2.2 Components of a VCC Procedure Contract\n\nA VCC contract has six formal elements:\n\n1. **`requires P`** — Boolean predicate that must hold on entry.\n2. **`ensures Q`** — Boolean predicate that must hold on exit (post-state).\n3. **`assigns A`** — The set of memory locations the procedure is allowed to modify.\n4. **`decreases e`** — A ranking expression for proving termination (variant function).\n5. **`reads A`** — Locations read but not modified (for read-only procedures).\n6. **`writes A`** — A synonym for `assigns` used in VCC's spec language.\n\n## 2.3 The Five Rules of Inter-procedure Reasoning\n\n| **Rule** | **Purpose** | **Logical Form** |\n|---|---|---|\n| **Call Rule** | Validate a procedure call | $\\frac{\\text{Pre}(P)[\\vec{a}/\\vec{x}] \\quad \\text{Post}(P)[\\vec{a}/\\vec{x}] \\Rightarrow C'}{\\text{call } P(\\vec{a}) \\text{ is valid}}$ |\n| **Frame Rule** | Restrict callee to assigned locations | $\\text{Modifies}(P) \\subseteq A$ |\n| **Consequence Rule** | Strengthen pre, weaken post | $\\frac{P \\Rightarrow P' \\quad Q' \\Rightarrow Q}{\\{P\\} \\; S \\; \\{Q\\} \\Rightarrow \\{P'\\} \\; S \\; \\{Q'\\}}$ |\n| **Recursive Call Rule** | Use inductive hypothesis | Treat recursive call as if it's an opaque call satisfying the contract |\n| **Disjointness Rule** | Prevent aliasing of `assigns` | $\\text{Assigned}(P) \\cap \\text{Untouchable} = \\emptyset$ |\n\n## 2.4 KTU High-Yield Formula Cheat Sheet\n\n> [!IMPORTANT]\n> **Master these formulas — they appear in 14-mark KTU problems regularly.**\n\n| **Symbol / Concept** | **Definition** | **Application in VCC** |\n|---|---|---|\n| $wp(S, Q)$ | Weakest precondition of statement $S$ w.r.t. postcondition $Q$ | $wp(\\text{call } P(\\vec{a}), Q) \\equiv \\text{Pre}(P)[\\vec{a}/\\vec{x}] \\land \\forall \\vec{v}.\\;\\text{Post}(P)[\\vec{a}/\\vec{x}] \\Rightarrow Q$ |\n| $\\text{Frame}(F, P)$ | Framing predicate — what $F$ protects | $\\text{Frame}(F, P) \\equiv \\forall x.\\; x \\in F \\Rightarrow P(x)$ |\n| $\\text{Owned}(o)$ | Ownership predicate for object $o$ | $o \\mapsto v$ — exclusive ownership of $o$'s value $v$ |\n| $\\text{Valid}(p)$ | Pointer $p$ points to a valid (allocated) object | Required precondition of dereferences |\n| $\\text{Inv}(o, I)$ | Invariant $I$ holds for object $o$ | $I(o)$ checked on entry & maintained across all operations |\n| $I_{\\text{2-state}}(o)$ | Two-state invariant relating pre- and post-states | $\\text{Old}(I) \\Rightarrow I$ across procedure calls |\n| $r(P)$ | `requires` of $P$ | Boolean formula |\n| $e(P)$ | `ensures` of $P$ | Boolean formula in post-state |\n| $a(P)$ | `assigns` set of $P$ | Set of addresses |\n| $\\mid$ (frame separator) | $\\text{olds}(e) \\mid e$ — old vs new value | Write $\\text{old}(x)$ for pre-call value of $x$ |\n\n> [!TIP]\n> **Use `\\vert` in LaTeX inside tables instead of the pipe character `|`** — this is the KTU 2024 standard to avoid markdown rendering bugs in `README` exports.\n\n## 2.5 Real-World Engineering Utility\n\nInter-procedure verification in VCC is used in industry for:\n- **Microsoft Hyper-V / Hyper-V VSPs** — verified drivers in the Windows kernel\n- **Axiomatic verification of device drivers** (SDV — Static Driver Verifier)\n- **Concurrency primitives** (locks, atomic operations) in OS code\n- **Safety-critical embedded systems** (medical, avionics) where C is the lingua franca\n\nIn the **KTU 2024 Scheme**, this topic is examined under **CO4: Apply formal verification tools to construct and validate program correctness proofs**, typically carrying **14 marks** in Part B.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: The VCC Call Rule from First Principles\n\nLet $P$ be a procedure with formal parameters $\\vec{x} = (x_1, x_2, \\ldots, x_n)$, precondition $Q_P(\\vec{x})$, and postcondition $R_P(\\vec{x}, \\text{result})$. A caller invokes $P$ with actual arguments $\\vec{a} = (a_1, a_2, \\ldots, a_n)$.\n\n**Step 1 — Substitution.** Replace formal parameters with actuals in the precondition:\n\n$$\nQ_P[\\vec{a}/\\vec{x}] = Q_P(a_1, a_2, \\ldots, a_n)\n$$\n\n**Step 2 — Substitution in the postcondition.** Replace $\\vec{x}$ by $\\vec{a}$ and bind the return value:\n\n$$\nR_P[\\vec{a}/\\vec{x}, \\text{result} \\mapsto v] = R_P(a_1, a_2, \\ldots, a_n, v)\n$$\n\n**Step 3 — Universal quantification over return value.** Since the return value is chosen by the callee, the caller can only assume:\n\n$$\n\\forall v.\\; R_P(a_1, \\ldots, a_n, v) \\Rightarrow \\text{caller's-post}(v)\n$$\n\n**Step 4 — The wp of a call is the conjunction of (a) the pre-substituted precondition, and (b) the universally-quantified implication**:\n\n$$\n\\boxed{wp(\\text{call } P(\\vec{a}),\\; Q) \\;\\equiv\\; Q_P[\\vec{a}/\\vec{x}] \\;\\land\\; \\forall v.\\; \\big(R_P[\\vec{a}/\\vec{x}, \\text{result}\\mapsto v] \\;\\Rightarrow\\; Q[v/\\text{result}]\\big)}\n$$\n\n**Step 5 — Frame preservation.** If the caller has an invariant $I$ over some set of locations $F$ that $P$ does not touch, then:\n\n$$\nI(F) \\;\\land\\; F \\cap a(P) = \\emptyset \\;\\Rightarrow\\; I(F) \\text{ in post-state}\n$$\n\nThis is the **frame rule** applied across procedures.\n\n## 3.2 Worked Example — Verified `max` Function\n\nConsider the following VCC-annotated C function:\n\n```c\nint max(int a, int b)\n  __CPROVER_requires(INT_MIN <= a && a <= INT_MAX)\n  __CPROVER_requires(INT_MIN <= b && b <= INT_MAX)\n  __CPROVER_ensures(__CPROVER_result == a || __CPROVER_result == b)\n  __CPROVER_ensures(__CPROVER_result >= a && __CPROVER_result >= b)\n{\n  return (a > b) ? a : b;\n}\n```\n\n*(Note: VCC uses `requires`/`ensures`; the example above shows equivalent contract patterns.)*\n\n**Derivation of the VCs:**\n\n**VC1 — Precondition check at every call site.** For every `call max(x, y)`, VCC generates:\n\n$$\n\\text{VC}_1 \\equiv \\text{INT\\_MIN} \\leq x \\leq \\text{INT\\_MAX} \\;\\land\\; \\text{INT\\_MIN} \\leq y \\leq \\text{INT\\_MAX}\n$$\n\nIf `x` and `y` are 32-bit `int`, this VC is trivially true.\n\n**VC2 — Postcondition at the return statement.** The `return (a > b) ? a : b;` statement splits into two cases:\n\n$$\n\\begin{aligned}\n\\text{VC}_{2a} &\\equiv a > b \\;\\Rightarrow\\; (\\text{result} = a) \\;\\land\\; (\\text{result} \\geq a) \\;\\land\\; (\\text{result} \\geq b) \\\\\n\\text{VC}_{2b} &\\equiv a \\leq b \\;\\Rightarrow\\; (\\text{result} = b) \\;\\land\\; (\\text{result} \\geq a) \\;\\land\\; (\\text{result} \\geq b)\n\\end{aligned}\n$$\n\n**VC3 — Termination.** Since `max` is non-recursive, no `decreases` clause is needed. VCC inserts a synthetic `decreases 0` for non-recursive functions.\n\n**Z3 result:** All VCs are `Valid` ⇒ function verified.\n\n## 3.3 Worked Example — Inter-procedure Reasoning with Frame\n\nConsider a caller that holds an invariant on a global counter `g`:\n\n```c\nvolatile int g = 0;\n\nvoid inc()\n  __CPROVER_assigns(g)\n  __CPROVER_ensures(g == \\old(g) + 1);\n\nvoid caller()\n  __CPROVER_requires(g == 42)\n  __CPROVER_assigns(g)\n  __CPROVER_ensures(g == 43)\n{\n  inc();\n  // VCC must verify: old(g)=42, post of inc gives g=43 ✓\n}\n```\n\n**Step-by-step VC generation:**\n\n**Step 1** — At the call `inc();`, VCC substitutes pre and post of `inc`:\n\n$$\n\\text{Pre}_{\\text{inc}}[\\,] \\equiv \\text{true} \\quad (\\text{no preconditions on } \\text{inc})\n$$\n\n$$\n\\text{Post}_{\\text{inc}} \\equiv g = \\text{old}(g) + 1\n$$\n\n**Step 2** — Apply Call Rule to the caller's postcondition $g = 43$:\n\n$$\n\\text{VC} \\equiv \\text{Pre}_{\\text{inc}} \\;\\land\\; \\forall v.\\; (\\text{Post}_{\\text{inc}} \\Rightarrow g = 43)\n$$\n\n**Step 3** — Substitute `old(g) = 42` from the caller's precondition:\n\n$$\ng = 42 + 1 = 43 \\;\\Rightarrow\\; g = 43 \\;\\; \\checkmark\n$$\n\n**Step 4** — Frame check: `assigns(g)` of `inc` ⊆ `assigns(g)` of caller ✓\n\n## 3.4 Analytical Matrix: Inlining vs. Modular Verification\n\n| **Criterion** | **Inlining (Unrolling)** | **Modular (Contract-Based)** |\n|---|---|---|\n| **VC size** | Quadratic in call depth | Linear in code size |\n| **Reusability** | None — each caller re-verifies callee | Callee verified once |\n| **Requires** | Full source of callee | Only contract needed |\n| **Scales to** | Small programs ($< 1$k LOC) | Large codebases ($> 100$k LOC) |\n| **Soundness** | Trivial | Requires strict `assigns` discipline |\n| **Used by VCC** | As fallback when no contract | Default mode |\n\n> [!NOTE]\n> VCC's default is **modular verification** — the practical scalability win. KTU problems often ask students to **compare** these two strategies.\n\n## 3.5 Derivation: Recursive Procedure Verification\n\nFor a recursive function $P$ with parameter $n$, VCC generates an induction proof:\n\n**Step 1** — The user annotates a `decreases` clause:\n\n$$\n\\text{decreases } n\n$$\n\n**Step 2** — For each recursive call $P(n-1)$, VCC must verify:\n\n$$\n\\text{decreases clause at call} \\equiv n - 1 < n \\;\\; \\checkmark \\text{ (always true)}\n$$\n\n**Step 3** — The recursive call is treated as an opaque call satisfying the contract — this is the **inductive hypothesis**.\n\n**Step 4** — Termination VC:\n\n$$\n\\text{VC}_{\\text{term}} \\equiv n \\geq 0 \\;\\Rightarrow\\; \\text{loop decreases} \\;\\Rightarrow\\; \\text{termination}\n$$\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Architecture of VCC's Verification Pipeline\n\n```mermaid\nflowchart TD\n    A[C Source Code] --> B[VCC Frontend Parser]\n    B --> C[Boogie Intermediate Verification Language]\n    C --> D[Weakest Precondition Calculator]\n    D --> E[First-Order VC Generation]\n    E --> F[Z3 SMT Solver]\n    F --> G{All VCs Valid?}\n    G -- Yes --> H[Verified: Program Correct]\n    G -- No --> I[Counterexample Trace]\n    I --> J[User Debugging]\n    J --> A\n    H --> K[Trust Certificate]\n\n    style A fill:#e1f5ff\n    style F fill:#ffe1e1\n    style H fill:#d4f5d4\n    style I fill:#ffd4d4\n    style K fill:#fff5d4\n```\n\n## 4.2 Inter-procedure Call Flow in VCC\n\n```mermaid\nsequenceDiagram\n    participant Caller as Caller Procedure\n    participant VCC1 as VCC Call-Site Checker\n    participant Contract as Procedure Contract\n    participant VCC2 as VCC Callee-Body Checker\n    participant Callee as Callee Body\n    participant Z3 as Z3 SMT Solver\n\n    Caller->>VCC1: call P(a1, a2, ..., an)\n    VCC1->>Contract: substitute actuals into Pre(P)\n    Contract-->>VCC1: Pre(P)[a/x]\n    VCC1->>Z3: VC1 - check Pre holds\n    Z3-->>VCC1: valid / invalid\n\n    VCC1->>VCC2: Verify callee body\n    VCC2->>Callee: parse body into Boogie\n    Callee->>VCC2: control-flow graph\n    VCC2->>Z3: VC2 - check Post(P) at every return\n    Z3-->>VCC2: valid / invalid\n\n    VCC2-->>VCC1: contract established\n    VCC1->>Contract: substitute into Post(P)\n    Contract-->>Caller: Post(P)[a/x, result->v]\n    VCC1->>Z3: VC3 - check caller's continues from Post(P)\n    Z3-->>Caller: valid / invalid\n```\n\n## 4.3 Frame Condition Visualization\n\n```mermaid\ngraph LR\n    subgraph CallerState[\"Caller State (Heap)\"]\n        A1[Object x - caller owns]\n        A2[Object y - caller owns]\n        A3[Object z - shared invariant]\n    end\n\n    subgraph AssignsClause[\"Callee assigns\"]\n        B1[Object x only]\n    end\n\n    subgraph AfterCall[\"Post-Call State\"]\n        C1[Object x - mutated OK]\n        C2[Object y - UNCHANGED via frame]\n        C3[Object z - invariant preserved]\n    end\n\n    CallerState -->|call P| AssignsClause\n    AssignsClause --> AfterCall\n\n    style A1 fill:#ffd4d4\n    style B1 fill:#fff5d4\n    style A2 fill:#d4f5d4\n    style A3 fill:#d4d4f5\n    style C1 fill:#ffd4d4\n    style C2 fill:#d4f5d4\n    style C3 fill:#d4d4f5\n```\n\n## 4.4 The Five Steps of Inter-procedure Verification\n\n```mermaid\nflowchart LR\n    S1[Step 1: Locate call site in caller] --> S2[Step 2: Substitute actuals into Pre P]\n    S2 --> S3[Step 3: Generate VC1 - check Pre holds]\n    S3 --> S4[Step 4: Substitute actuals into Post P]\n    S4 --> S5[Step 5: Generate VC2 - frame check and continuation]\n    S5 --> S6[Step 6: Send VCs to Z3]\n    S6 --> S7{Valid?}\n    S7 -- Yes --> S8[Call Site Verified]\n    S7 -- No --> S9[Counterexample / Error]\n\n    style S1 fill:#e1f5ff\n    style S7 fill:#ffe1e1\n    style S8 fill:#d4f5d4\n    style S9 fill:#ffd4d4\n```\n\n## 4.5 Modular vs. Inlining Comparison\n\n```mermaid\ngraph TB\n    subgraph Inlining[\"Inlining Strategy\"]\n        I1[Caller Body] --> I2[Copy Callee Body]\n        I2 --> I3[Single Monolithic CFG]\n        I3 --> I4[Generate VCs for entire graph]\n    end\n\n    subgraph Modular[\"Modular Strategy - VCC Default\"]\n        M1[Caller Body] --> M2[Use Callee Contract Only]\n        M2 --> M3[Substitute Pre and Post]\n        M3 --> M4[Generate VCs for caller only]\n    end\n\n    style Inlining fill:#ffe1e1\n    style Modular fill:#d4f5d4\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **[KTU University Exam — Dec 2023, Model Q1]**\n> **Q1.** Define the three components of a VCC procedure contract and explain the role of the `assigns` clause in inter-procedure verification.\n\n**Model Answer (3 marks):**\n\nA VCC procedure contract consists of three components:\n\n1. **`requires` (Precondition)** — A Boolean predicate $Q(\\vec{x})$ over the formal parameters and global state that must hold at the call site. It defines the conditions under which the procedure promises to behave correctly.\n\n2. **`ensures` (Postcondition)** — A Boolean predicate $R(\\vec{x}, \\text{result})$ that must hold when the procedure returns, possibly referring to pre-state values via $\\text{old}(\\cdot)$.\n\n3. **`assigns` (Frame condition)** — A set $A$ of memory locations that the procedure is permitted to modify. Any location not in $A$ must remain unchanged across the call.\n\n**Role of `assigns` in inter-procedure verification:**\nThe `assigns` clause enables the **frame rule** to be applied at call sites. When a caller invokes a procedure, VCC uses the `assigns` clause to determine which memory locations the callee is allowed to touch. Any location owned by the caller but **not** in the callee's `assigns` set is automatically guaranteed to be preserved, allowing the caller's local invariants to be maintained without re-deriving them. This is the foundation of **modular verification** in VCC.\n\n> **[KTU University Exam — May 2024, Model Q2]**\n> **Q2.** What is the difference between **inlining** and **modular (contract-based)** verification of procedures? Which one does VCC use by default, and why?\n\n**Model Answer (3 marks):**\n\n| **Aspect** | **Inlining** | **Modular Verification** |\n|---|---|---|\n| **Mechanism** | Callee body is inlined at each call site | Only callee's contract is used |\n| **VC size** | Grows with call depth (quadratic) | Linear in code size |\n| **Re-verification** | Required for every call | One-time verification per callee |\n| **Source needed** | Full source of callee | Only the contract specification |\n| **Default in VCC** | Used as fallback | **Default** (preferred) |\n\n**VCC uses modular verification by default** because:\n- It scales to large codebases (Microsoft uses it on $\\geq 100$k LOC kernels).\n- It separates concerns — each procedure is verified in isolation.\n- It reduces VC explosion by abstracting callees via contracts.\n- It supports **separate compilation** — a callee can be verified once and reused across many callers.\n\nInlining is used only as a **fallback** when no contract is provided, in which case VCC inlines the callee body into the caller's CFG and verifies monolithically.\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks Each)\n\n> **[KTU University Exam — Dec 2023 / Model Question]**\n\n### **Question A (14 Marks)**\n\n**(a)** Explain the architecture of VCC's verification pipeline from C source to Z3 SMT solver. Describe each stage in detail, including the role of the Boogie intermediate verification language. **(7 marks)**\n\n**(b)** Consider the following C program with VCC contracts:\n\n```c\nint square(int x)\n  __CPROVER_requires(x >= 0 && x <= 1000)\n  __CPROVER_ensures(__CPROVER_result >= 0)\n  __CPROVER_ensures(__CPROVER_result == x * x);\n\nint compute(int a, int b)\n  __CPROVER_requires(a >= 0 && b >= 0)\n  __CPROVER_assigns(nothing)\n  __CPROVER_ensures(__CPROVER_result == square(a) + square(b))\n{\n  int s1 = square(a);\n  int s2 = square(b);\n  return s1 + s2;\n}\n```\n\nDemonstrate, using the VCC **Call Rule**, how the verification conditions are generated at the call sites `square(a)` and `square(b)`. Show how the frame condition and postcondition of `square` are used to discharge the VCs for `compute`. **(7 marks)**\n\n---\n\n#### **Model Solution for Question A:**\n\n**(a) VCC Verification Pipeline (7 marks):**\n\nThe VCC pipeline consists of **five stages**:\n\n1. **Parsing (C → AST):** VCC's frontend parses the annotated C source code into an abstract syntax tree. VCC-specific annotations (`requires`, `ensures`, `assigns`, `decreases`) are preserved as attributes on function declarations.\n\n2. **Translation (AST → Boogie):** Each C procedure is translated into a Boogie procedure. C statements become Boogie `goto` statements forming a control-flow graph. Loops are unrolled into structured `while` constructs, and contracts are encoded as `requires`/`ensures` in Boogie's specification language.\n\n3. **Weakest Precondition Computation (Boogie → VCs):** VCC applies Dijkstra's weakest precondition calculus over the control-flow graph, propagating postconditions backwards through each statement. This produces a set of first-order logic formulas — the **verification conditions (VCs)**.\n\n4. **SMT Solving (VCs → Valid/Invalid):** The VCs are sent to the **Z3 SMT solver** (developed at Microsoft Research). Z3 checks the validity of each VC using decision procedures for arithmetic, arrays, bit-vectors, and uninterpreted functions.\n\n5. **Result Reporting (Valid → Trust Certificate / Invalid → Counterexample):** If all VCs are valid, VCC emits a \"verification succeeded\" message. If any VC fails, VCC produces a **counterexample trace** showing the inputs and execution path that violates the contract, aiding debugging.\n\nThe **Boogie intermediate verification language** plays a crucial role:\n- It decouples C-specific concerns from generic VC generation.\n- It enables VCC to support multiple frontends (C, C++, and others).\n- It provides a clean separation between *program logic* and *decision procedures*.\n\n**(b) VCs at Call Sites in `compute` (7 marks):**\n\n**Step 1 — Identify the call sites and substitute actuals into `square`'s contract.**\n\nFor `square(a)`:\n- $\\text{Pre}_{\\text{square}}[a/x] \\equiv a \\geq 0 \\;\\land\\; a \\leq 1000$\n- $\\text{Post}_{\\text{square}}[a/x, \\text{result}\\mapsto v] \\equiv v \\geq 0 \\;\\land\\; v = a \\cdot a$\n\nFor `square(b)`:\n- $\\text{Pre}_{\\text{square}}[b/x] \\equiv b \\geq 0 \\;\\land\\; b \\leq 1000$\n- $\\text{Post}_{\\text{square}}[b/x, \\text{result}\\mapsto v] \\equiv v \\geq 0 \\;\\land\\; v = b \\cdot b$\n\n**Step 2 — Apply the VCC Call Rule for `square(a)`.**\n\nThe wp of the statement `int s1 = square(a);` with postcondition $Q \\equiv s_1 + s_2 = a^2 + b^2$ is:\n\n$$\nwp(\\text{call } \\text{square}(a),\\; Q) \\equiv \\text{Pre}_{\\text{square}}[a/x] \\;\\land\\; \\forall v.\\; \\big(\\text{Post}_{\\text{square}}[a/x, \\text{result}\\mapsto v] \\;\\Rightarrow\\; v + s_2 = a^2 + b^2\\big)\n$$\n\nSubstituting:\n\n$$\n\\equiv a \\geq 0 \\;\\land\\; a \\leq 1000 \\;\\land\\; \\forall v.\\; \\big(v \\geq 0 \\;\\land\\; v = a^2 \\;\\Rightarrow\\; v + s_2 = a^2 + b^2\\big)\n$$\n\nThe implication reduces to $s_2 = b^2$, which becomes the new postcondition for the next statement.\n\n**Step 3 — Apply the VCC Call Rule for `square(b)`.**\n\nSimilarly:\n\n$$\n\\equiv b \\geq 0 \\;\\land\\; b \\leq 1000 \\;\\land\\; \\forall v.\\; \\big(v \\geq 0 \\;\\land\\; v = b^2 \\;\\Rightarrow\\; s_1 + v = a^2 + b^2\\big)\n$$\n\nThis is discharged by substituting $s_1 = a^2$ and $v = b^2$:\n\n$$\ns_1 + b^2 = a^2 + b^2 \\;\\; \\checkmark\n$$\n\n**Step 4 — Frame Condition Check.**\n\n`square` has no `assigns` clause beyond its local variables (which are stack-allocated and discarded). VCC verifies that `square`'s `assigns` set is disjoint from `compute`'s locals (`s1`, `s2`, `a`, `b`). Since `square` only writes to its own stack frame, the frame condition is trivially satisfied. No global state is modified, so the caller's invariants are preserved.\n\n**Step 5 — Final Postcondition Discharge.**\n\nThe function returns $s_1 + s_2$, and VCC checks:\n\n$$\ns_1 + s_2 = a^2 + b^2 \\;\\Rightarrow\\; \\text{result} = a^2 + b^2 \\;\\; \\checkmark\n$$\n\nAll VCs are discharged by Z3, and `compute` is verified.\n\n---\n\n### **Question B (14 Marks) — Alternative Choice**\n\n**(a)** Explain the **two-state invariant** mechanism in VCC and describe how it is used to maintain consistency of object invariants across procedure boundaries. **(7 marks)**\n\n**(b)** Consider the following recursive factorial function in VCC:\n\n```c\nint factorial(int n)\n  __CPROVER_requires(n >= 0 && n <= 12)\n  __CPROVER_decreases(n)\n  __CPROVER_ensures(__CPROVER_result >= 1)\n  __CPROVER_ensures(__CPROVER_result == product_of_1_to_n(n));\n\nint factorial_caller(int m)\n  __CPROVER_requires(m >= 0 && m <= 12)\n  __CPROVER_assigns(nothing)\n  __CPROVER_ensures(__CPROVER_result == factorial(m));\n{\n  return factorial(m);\n}\n```\n\nExplain how VCC handles the **recursive call** within `factorial`, the role of the `decreases` clause, and the **inductive hypothesis** used in the proof. Also show the VCs generated for `factorial_caller` using the Call Rule. **(7 marks)**\n\n---\n\n#### **Model Solution for Question B:**\n\n**(a) Two-State Invariants in VCC (7 marks):**\n\nVCC's **two-state invariant** mechanism allows an object's invariant $I(o)$ to refer to **both** the pre-state (before a procedure call) and the post-state (after the call). This is essential for inter-procedure verification because:\n\n- An invariant may depend on the *relationship* between fields that a callee is allowed to modify.\n- Without two-state reasoning, a callee that touches one field of an object would force the caller to re-verify the entire invariant.\n\nA two-state invariant has the form:\n\n$$\nI(\\text{old}(x_1), \\ldots, \\text{old}(x_n), x_1, \\ldots, x_n)\n$$\n\nVCC's **admissibility check** ensures that for any field $f$ the callee is allowed to modify, the invariant's truth value after the call does not depend on the pre-state value of $f$ — only on fields the callee does not touch.\n\nFor example, an invariant that a sorted array's elements sum to a value stored in a separate field:\n\n$$\nI(\\text{arr}) \\equiv \\text{sorted}(\\text{arr}) \\;\\land\\; \\text{sum}(\\text{arr}) = \\text{arr.sum}\n$$\n\nIf a callee is allowed to modify individual elements but not `arr.sum`, then after the call, VCC must re-verify $\\text{sorted}(\\text{arr})$ in the new state, but `sum(arr) = arr.sum` is preserved by the frame rule. The two-state invariant makes this reasoning modular.\n\n**(b) Recursive Procedure and Caller Verification (7 marks):**\n\n**Handling the Recursive Call in `factorial`:**\n\nThe body of `factorial` (omitted for brevity) contains a recursive call `factorial(n-1)`. VCC handles this as follows:\n\n1. **Inductive hypothesis:** VCC treats the recursive call as an opaque call that satisfies `factorial`'s contract. That is, the proof inside `factorial` may assume:\n\n$$\n\\text{result} = \\text{factorial}(n-1)\n$$\n\nwithout inspecting the recursive call's body again.\n\n2. **`decreases` clause:** The user specifies `decreases(n)`. VCC generates a VC that, at every recursive call site, the argument strictly decreases:\n\n$$\n\\text{VC}_{\\text{decreases}} \\equiv n - 1 < n \\;\\; \\checkmark \\text{ (trivially true for any } n \\text{)}\n$$\n\n3. **Termination:** Since $n$ is bounded below by 0 (from the precondition) and strictly decreases at each call, the recursion must terminate after at most $n + 1$ calls. VCC's `decreases` mechanism guarantees termination.\n\n4. **Postcondition on return:** On the base case (typically `n == 0` or `n == 1`), the function returns 1, satisfying `result >= 1` and `result == product_of_1_to_n(n)`. In the recursive case, the inductive hypothesis plus the multiplication step establish the postcondition.\n\n**VCs for `factorial_caller`:**\n\n**Step 1 — Substitute actuals into `factorial`'s contract:**\n\n- $\\text{Pre}_{\\text{factorial}}[m/n] \\equiv m \\geq 0 \\;\\land\\; m \\leq 12$\n- $\\text{Post}_{\\text{factorial}}[m/n, \\text{result}\\mapsto v] \\equiv v \\geq 1 \\;\\land\\; v = \\prod_{i=1}^{m} i$\n\n**Step 2 — Apply the Call Rule:**\n\n$$\nwp(\\text{return } \\text{factorial}(m),\\; \\text{result} = \\text{factorial}(m)) \\equiv \\text{Pre}_{\\text{factorial}}[m/n] \\;\\land\\; \\forall v.\\; \\big(\\text{Post}_{\\text{factorial}}[m/n, \\text{result}\\mapsto v] \\;\\Rightarrow\\; v = \\text{factorial}(m)\\big)\n$$\n\n**Step 3 — Discharge:**\n\nThe caller's precondition establishes $m \\geq 0 \\;\\land\\; m \\leq 12$, satisfying the callee's precondition. The callee's postcondition directly matches the caller's postcondition under the substitution $v = \\text{result}$. Z3 discharges this VC trivially.\n\n**Step 4 — Frame Check:**\n\n`factorial` has no global `assigns` (purely functional on stack). The caller's `assigns(nothing)` is respected. No frame violation.\n\n**Result:** `factorial_caller` is verified with **3 VCs**, all discharged by Z3.\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mistakes Students Make (and Where They Lose Marks)**\n>\n> 1. **Confusing `assigns` with `requires`/`ensures`** — `assigns` is the *frame* (what the callee may modify), not a precondition. Mixing these up costs 2-3 marks.\n>\n> 2. **Forgetting the `forall v` quantifier over the return value** in the Call Rule. Many students write:\n>    $$wp(\\text{call } P(\\vec{a}), Q) \\equiv \\text{Pre}(P)[\\vec{a}/\\vec{x}] \\land \\text{Post}(P)[\\vec{a}/\\vec{x}] \\Rightarrow Q$$\n>    This is **wrong** — the return value must be universally quantified, not existentially or concretely substituted.\n>\n> 3. **Ignoring the frame condition** — The frame rule is what makes modular verification sound. Students who skip the `assigns` check typically lose 3-4 marks on 14-mark questions.\n>\n> 4. **Forgetting the `decreases` clause for recursive functions** — Without it, VCC will not prove termination, and the verification will fail. This is a frequent 2-mark deduction.\n>\n> 5. **Writing `old(x)` instead of `\\old(x)` or `\\text{old}(x)` in equations** — Examiners are strict about notation. Use the LaTeX `\\text{old}(x)` form.\n>\n> 6. **Not mentioning Boogie or Z3** — VCC's pipeline involves both. A complete answer must reference the **Boogie intermediate language** and the **Z3 SMT solver** for full marks.\n>\n> 7. **Confusing modular verification with dynamic testing** — VCC is a *static* tool, not a runtime checker. Mentioning \"test cases\" instead of \"verification conditions\" is a fundamental error.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid Revision Checklist — Inter-procedure Verification in VCC**\n>\n> - **VCC** = Verification Condition Calculator, a static verifier for concurrent C code, built on **Boogie + Z3**.\n> - **Procedure contract** = `requires` (pre) + `ensures` (post) + `assigns` (frame).\n> - **Call Rule**: $wp(\\text{call } P(\\vec{a}), Q) \\equiv \\text{Pre}(P)[\\vec{a}/\\vec{x}] \\land \\forall v.\\,(\\text{Post}(P)[\\vec{a}/\\vec{x}, \\text{result}\\mapsto v] \\Rightarrow Q[v/\\text{result}])$.\n> - **Frame Rule**: Locations not in `assigns(P)` are preserved across the call.\n> - **Consequence Rule**: Strengthen pre, weaken post.\n> - **Recursion**: Requires `decreases` clause + inductive hypothesis (recursive call treated as opaque).\n> - **Two-state invariants**: Allow $I$ to reference both pre- and post-state; subject to admissibility check.\n> - **Default mode** of VCC is **modular verification**; inlining is a fallback.\n> - **Boogie** is the IVL; **Z3** is the SMT solver; both are essential pipeline components.\n> - **Termination** is mandatory — VCC checks it via the `decreases` measure.\n> - **Soundness** depends on strict `assigns` discipline — never omit it.\n> - **Frame separator notation** in VCC: $\\text{old}(x) \\;\\mid\\; x$ distinguishes pre- and post-values of $x$.\n> - **For KTU 14-mark answers**: Always structure as **(a) Theory + Diagram**, **(b) Worked Example + VC derivation**.\n> - **Key trade-off**: Modular = scalable but requires contracts; Inlining = automatic but doesn't scale.\n> - **Industry relevance**: Used in Microsoft Hyper-V, Windows kernel drivers, and static driver verifier (SDV).\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "artificial-intelligence-and-data-science-7-pecst741-mModule 4-t2",
          "title": "Function Contracts",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST741 - FORMAL METHODS IN SOFTWARE ENGINEERING\n> - **Module:** Module Module 4\n> - **Topic:** Function Contracts\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\nA **Function Contract** is a formal, mathematically precise specification attached to a function (or method, procedure, subroutine) that rigorously documents the *obligations* between the **caller (client)** and the **implementation (supplier)**. It is a central construct in *Design by Contract* (DbC), introduced by Bertrand Meyer, and is foundational to Hoare-style axiomatic semantics used in formal software verification.\n\nA function contract is formally expressed as a **Hoare Triple** (also called a Floyd–Hoare triple) with a standard syntactic form:\n\n$$\\{P\\}\\; S \\;\\{Q\\}$$\n\nWhere:\n- $P$ is the **Precondition** — the predicate that the *caller* must guarantee *before* invoking the function $S$.\n- $Q$ is the **Postcondition** — the predicate that the *supplier* must guarantee *upon* termination of $S$.\n- $S$ is the function/statement whose behaviour is being constrained.\n\n> [!IMPORTANT]\n> **KTU 2024 Board Definition:** A function contract is a logical specification consisting of a precondition $P$ and a postcondition $Q$, optionally augmented with a *frame condition* (modifies clause) and an *invariant clause*. It defines a mutual, verifiable obligation between the calling context and the function body, enabling *proof obligations* during formal verification.\n\n### 1.2 Conceptual Analogy / Intuition\n\n> [!NOTE]\n> **Analogy: The \"Legal Contract\" of Software**\n\nThink of a function contract as a **legally binding agreement** between two parties:\n\n| Real-World Analogy | Software Equivalent |\n|---|---|\n| Terms & Conditions a buyer must satisfy to purchase | **Precondition** ($P$) — what the *caller* must ensure |\n| Promises the seller must deliver after payment | **Postcondition** ($Q$) — what the *function* must deliver |\n| Warranty void if the product is misused | **Frame Condition** — what the function is *not* allowed to touch |\n| Arbitration clause for breach of terms | **Invariant** — global property that must never be violated |\n\nIf the **buyer (caller) violates** the precondition (e.g., asks to purchase without money), the seller is **released from all obligations** — the function may legally do *anything*, including crash, return garbage, or simply skip work. If the **seller (function) violates** the postcondition, the function is *incorrect*, irrespective of what input was given.\n\nThis is the famous Eiffel motto: **\"No transaction without a contract.\"**\n\n### 1.3 Why Function Contracts Matter in Formal Methods\n\n- **Eliminate Ambiguity:** Natural-language specs like \"the function should work for valid inputs\" are replaced by rigorous first-order logic predicates.\n- **Enable Mechanical Verification:** Tools (e.g., *KeY*, *Frama-C*, *SPARK Prover*, *Dafny*, *JML tools*) can discharge *Verification Conditions (VCs)* automatically.\n- **Support Modular Reasoning:** A function can be verified *in isolation* using only its contract — the internals of the caller or callee do not need to be inspected.\n- **Form the Basis of Weakest Precondition Calculus (Dijkstra):** Contracts enable automated computation of what must hold *before* a statement to guarantee a desired outcome *after*.\n\n> [!IMPORTANT]\n> **Standard KTU Metric:** Contract strength is measured in terms of the **weakest precondition** $wp(S, Q)$, which represents the *least* restrictive condition under which $S$ is guaranteed to establish $Q$. A contract $\\{P\\}S\\{Q\\}$ is **valid** iff $P \\Rightarrow wp(S, Q)$.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 The Four Pillars of a Function Contract\n\nA complete function contract in KTU 2024 formal methods is composed of the following four logical clauses:\n\n#### 2.1.1 Precondition ($P$)\n- The predicate that must hold *immediately before* function entry.\n- **Responsibility:** Caller.\n- **Logical Form:** A first-order predicate over the function's input parameters, global state, and any relevant class invariants.\n- **Example:** $\\text{Pre} \\equiv x \\neq 0 \\land y \\geq 0$\n\n#### 2.1.2 Postcondition ($Q$)\n- The predicate that must hold *immediately after* function termination (assuming precondition was satisfied).\n- **Responsibility:** Supplier (function body).\n- **Logical Form:** Describes the relationship between the post-state and the pre-state, using notations like $\\backslash\\text{old}(\\cdot)$ to refer to the value of a variable *before* the call.\n- **Example:** $\\text{Post} \\equiv \\backslash\\text{result} = x + y$\n\n#### 2.1.3 Frame Condition / Modifies Clause\n- A **set of variables or memory locations** the function is *permitted* to modify.\n- Anything not listed in the frame is **forbidden** to be altered (an implicit postcondition).\n- **Example:** $\\text{modifies} \\; \\{ \\text{balance} \\}$\n\n#### 2.1.4 Invariant Clause\n- A global property of the data structure or class that must be preserved by *every* public function call (i.e., it is a postcondition of every public method, assuming it was a precondition).\n- **Example:** $\\text{Inv} \\equiv \\text{size} \\geq 0 \\land \\text{capacity} \\geq \\text{size}$\n\n### 2.2 The Hoare Triple — A Rigorous Treatment\n\nA Hoare triple $\\{P\\}\\;S\\;\\{Q\\}$ has the following partial-correctness interpretation:\n\n$$\\models \\{P\\}\\; S \\;\\{Q\\} \\iff \\forall \\sigma, \\sigma'. \\; (\\sigma \\models P \\land \\langle S, \\sigma \\rangle \\to \\sigma') \\Rightarrow \\sigma' \\models Q$$\n\nWhere $\\sigma$ and $\\sigma'$ are program states (mappings from variables to values), and $\\langle S, \\sigma \\rangle \\to \\sigma'$ denotes the execution of $S$ starting from $\\sigma$ and terminating in $\\sigma'$.\n\n**Two flavours of correctness:**\n- **Partial Correctness (Hoare):** If $P$ holds and the program terminates, then $Q$ holds. Termination is *not* guaranteed.\n- **Total Correctness (Manna–Pnueli):** If $P$ holds, then the program *is guaranteed* to terminate and $Q$ will hold. Denoted as $[P]\\;S\\;[Q]$.\n\n### 2.3 KTU Formula Sheet / Cheat Sheet\n\n> [!IMPORTANT]\n> **The following table is the HIGH-YIELD reference for KTU 2024 board questions. Memorize every row.**\n\n| # | Concept | Formula / Rule | Engineering Use |\n|---|---|---|---|\n| 1 | Hoare Triple (Partial) | $\\{P\\}\\;S\\;\\{Q\\}$ | Specifying partial correctness |\n| 2 | Hoare Triple (Total) | $[P]\\;S\\;[Q]$ | Specifying total correctness (with termination) |\n| 3 | Weakest Precondition | $wp(S, Q)$ | Foundation of Dijkstra's calculus |\n| 4 | Strongest Postcondition | $sp(P, S)$ | Reverse-direction analysis |\n| 5 | Skip / No-op | $wp(\\text{skip}, Q) = Q$ | Identity element |\n| 6 | Abort | $wp(\\text{abort}, Q) = \\text{false}$ | Proof of infeasibility |\n| 7 | Assignment | $wp(x := E, Q) = Q[E/x]$ | Substitution rule |\n| 8 | Sequence | $wp(S_1; S_2, Q) = wp(S_1, wp(S_2, Q))$ | Backward composition |\n| 9 | Conditional | $wp(\\text{if } B \\text{ then } S_1 \\text{ else } S_2, Q) = (B \\land wp(S_1, Q)) \\lor (\\neg B \\land wp(S_2, Q))$ | Branch reasoning |\n| 10 | While Loop (Dijkstra) | $wp(\\text{while } B \\text{ do } S, Q) = \\exists k. \\; H_k(Q)$ where $H_0 = \\neg B \\land Q$ and $H_{k+1} = \\neg B \\land Q \\lor B \\land wp(S, H_k)$ | Loop invariant-based reasoning |\n| 11 | Loop Invariant | $I \\land \\neg B \\Rightarrow Q$ (to establish postcondition) | Termination measure $(t, \\prec)$ must be supplied |\n| 12 | Strength Rule | If $P \\Rightarrow P'$ and $\\{P'\\}S\\{Q'\\}$, then $\\{P\\}S\\{Q'\\}$ | Precondition weakening |\n| 13 | Consequence Rule | If $\\{P\\}S\\{Q\\}$ and $Q \\Rightarrow Q'$, then $\\{P\\}S\\{Q'\\}$ | Postcondition strengthening |\n| 14 | Composition | If $\\{P\\}S_1\\{Q\\}$ and $\\{Q\\}S_2\\{R\\}$, then $\\{P\\}S_1;S_2\\{R\\}$ | Stitching sequential code |\n| 15 | Disjunction Rule | If $\\{P_1\\}S\\{Q\\}$ and $\\{P_2\\}S\\{Q\\}$, then $\\{P_1 \\lor P_2\\}S\\{Q\\}$ | Case merging |\n| 16 | Conjunction Rule | If $\\{P_1\\}S\\{Q_1\\}$ and $\\{P_2\\}S\\{Q_2\\}$, then $\\{P_1 \\land P_2\\}S\\{Q_1 \\land Q_2\\}$ | Multi-property proof |\n| 17 | Validity Check | $P \\Rightarrow wp(S, Q)$ | Contract verification |\n| 18 | Conjunction (P) | $wp(S, Q_1) \\land wp(S, Q_2) \\Rightarrow wp(S, Q_1 \\land Q_2)$ | Healthiness condition |\n| 19 | Law of Excluded Miracle | $wp(S, \\text{false}) = \\text{false}$ | Strictly-impossible-state rejection |\n| 20 | Monotonicity | $Q_1 \\Rightarrow Q_2 \\Rightarrow wp(S, Q_1) \\Rightarrow wp(S, Q_2)$ | Order preservation |\n\n> [!NOTE]\n> **CRITICAL FORMATTING NOTE:** All absolute-value / set-cardinality notations in this table use `\\vert` or `\\mid` (e.g., $\\vert S \\vert$) instead of the vertical pipe character `|` to preserve the markdown table structure.\n\n### 2.4 Real-World Engineering Utility\n\nFunction contracts are the backbone of **safety-critical software verification** in:\n- **Aerospace (DO-178C Level A):** Avionics software (Airbus A380, Boeing 787) uses SPARK Ada with formal contracts to mathematically prove the absence of runtime errors.\n- **Railway Signalling (EN 50128 SIL 4):** Train control systems rely on contracts for verification of interlocking logic.\n- **Medical Devices (FDA IEC 62304):** Infusion pumps and pacemakers use contract-based verification to demonstrate safety properties.\n- **Automotive (ISO 26262 ASIL D):** Autonomous driving stacks employ contracts for ISO 26262 compliance.\n- **Cryptographic Libraries:** Used to specify and verify functional correctness of primitives like RSA, AES, and TLS state machines.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Derivation of Weakest Precondition for Sequential Composition\n\n**Theorem (Sequence Rule):** For statements $S_1$ and $S_2$ and a postcondition $Q$:\n$$wp(S_1; S_2, Q) = wp(S_1, wp(S_2, Q))$$\n\n**Proof by Predicate Transformer Semantics:**\n\nBy definition, $wp(S_1; S_2, Q)$ is the *weakest* predicate $P$ such that executing $S_1; S_2$ from any state satisfying $P$ is guaranteed to terminate in a state satisfying $Q$.\n\nLet $\\sigma_0$ be the initial state, $\\sigma_1$ the state after $S_1$, and $\\sigma_2$ the state after $S_2$.\n\nWe require: $\\sigma_0 \\models wp(S_1; S_2, Q) \\Rightarrow \\sigma_2 \\models Q$.\n\nBy the **Healthiness Condition of Monotonicity** (Formula #20), $S_2$ from $\\sigma_1$ establishes $Q$ iff $\\sigma_1 \\models wp(S_2, Q)$. Therefore, the condition reduces to:\n$$\\sigma_0 \\models wp(S_1; S_2, Q) \\iff \\sigma_1 \\models wp(S_2, Q)$$\n\nNow, the requirement that $S_1$ from $\\sigma_0$ leads to $\\sigma_1$ satisfying $wp(S_2, Q)$ is exactly the definition of $wp(S_1, wp(S_2, Q))$. Therefore:\n$$wp(S_1; S_2, Q) = wp(S_1, wp(S_2, Q)) \\qquad \\blacksquare$$\n\n### 3.2 Derivation of Weakest Precondition for Assignment\n\n**Theorem (Assignment Rule):** $wp(x := E, Q) = Q[E/x]$.\n\n**Proof:** $Q$ is a predicate over the post-state. The post-state is the same as the pre-state except that $x$ now has the value $E$. So $Q$ evaluated in the post-state is equivalent to $Q$ with every free occurrence of $x$ replaced by the expression $E$ that was assigned, evaluated in the pre-state. $\\blacksquare$\n\n### 3.3 Worked Example — Computing $wp$ for a Full Function Body\n\nConsider the following function contract for safe integer division:\n\n```\nfunction divide(x: int, y: int) returns (r: int)\n  Pre:  y ≠ 0\n  Post: r * y = x   ∧   ∃q. (x = q * y)   (functional correctness)\n```\n\n**Goal:** Compute $wp(\\text{body}, \\text{Post})$ and confirm that $\\text{Pre} \\Rightarrow wp(\\text{body}, \\text{Post})$.\n\n**Hypothetical body:**\n```\nr := x;\nwhile (r ≥ y) loop\n    r := r - y;\nend loop;\n```\n\n**Step 1: Compute $wp$ of the loop.**\n\nLet the loop invariant be:\n$$I \\;\\equiv\\; \\exists q.\\; (x = q \\cdot y + r) \\;\\land\\; 0 \\leq r \\;\\land\\; r < y \\;\\lor\\; r \\geq y$$\n\n(More precisely, $I \\equiv \\exists q.\\; (x = q \\cdot y + r) \\;\\land\\; r \\geq 0$.)\n\n**Step 2: Verify $I$ holds initially (before the loop).**\n\nInitially, $r = x$. Choose $q = 0$. Then $x = 0 \\cdot y + r = r$, so $x = q \\cdot y + r$ holds trivially. Also $r = x \\geq 0$ — wait, this requires $x \\geq 0$. We must strengthen the precondition.\n\nFor full correctness, the precondition must include $x \\geq 0$:\n$$\\text{Pre} \\equiv y \\neq 0 \\;\\land\\; x \\geq 0$$\n\n**Step 3: Show $I \\land \\neg B \\Rightarrow \\text{Post}$.**\n\nWhen the loop exits, $\\neg B$ holds, i.e., $r < y$. Combined with invariant $x = q \\cdot y + r$, this gives the full postcondition.\n\n**Step 4: Show $\\{I \\land B\\}\\; r := r - y \\;\\{I\\}$ (preservation).**\n\nAfter $r := r - y$, the new $r'$ satisfies $r' = r - y$, so:\n$$x = q \\cdot y + r = q \\cdot y + r' + y = (q+1) \\cdot y + r'$$\n\nThus the invariant $I$ is preserved. $\\blacksquare$\n\n### 3.4 Analytical Matrix — Contract Strength Ordering\n\nA central KTU 2024 question theme is comparing contracts based on the *strength* of pre/postconditions:\n\n| Contract | Precondition | Postcondition | Strength |\n|---|---|---|---|\n| $C_1$ | $P_1$ | $Q_1$ | Baseline |\n| $C_2$ | $P_2 \\equiv P_1 \\land R$ | $Q_1$ | **Stronger** precondition |\n| $C_3$ | $P_1$ | $Q_2 \\equiv Q_1 \\land S$ | **Stronger** postcondition |\n| $C_4$ | $P_1 \\land R$ | $Q_1 \\land S$ | **Strictly stronger** overall |\n\n**Rule of Thumb (KTU Board Exam):**\n- *Stronger* precondition ⇒ *easier* to call (fewer obligations), but *harder* to implement.\n- *Stronger* postcondition ⇒ *harder* to call (caller relies on more), but *easier* to implement on restricted inputs.\n\n> [!NOTE]\n> **Engineering Trade-off:** In safety-critical avionics, contracts are deliberately *strengthened* with rich postconditions to enable deep property verification. In general-purpose API design, contracts are *weakened* to maximize usability.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Mermaid Diagram: Anatomy of a Function Contract\n\n```mermaid\ngraph TD\n    A[Function Contract] --> B[Precondition P]\n    A --> C[Postcondition Q]\n    A --> D[Frame Condition F]\n    A --> E[Invariant Inv]\n\n    B --> B1[Input validity x not equals 0]\n    B --> B2[State requirements balance not equals min]\n    B --> B3[Caller obligation CHECKED before entry]\n\n    C --> C1[Result correctness result equals x plus y]\n    C --> C2[Old state preservation old balance preserved]\n    C --> C3[Supplier obligation GUARANTEED at exit]\n\n    D --> D1[List of modifiable variables modifies balance]\n    D --> D2[Implicit immutability for non listed variables]\n\n    E --> E1[Class level invariant size greater equals 0]\n    E --> E2[Preserved by EVERY public call]\n\n    A --> V[Verification Condition VC]\n    V --> V1[Generate VCs using wp calculus]\n    V --> V2[Discharge VCs using SMT solver Z3 CVC5]\n    V --> V3[Output PROVED or FAILED]\n\n    style A fill:#1f4e79,stroke:#0b2a4a,stroke-width:3px,color:#ffffff\n    style B fill:#5b9bd5,stroke:#1f4e79,color:#ffffff\n    style C fill:#5b9bd5,stroke:#1f4e79,color:#ffffff\n    style D fill:#5b9bd5,stroke:#1f4e79,color:#ffffff\n    style E fill:#5b9bd5,stroke:#1f4e79,color:#ffffff\n    style V fill:#70ad47,stroke:#385723,color:#ffffff\n    style B1 fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style B2 fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style B3 fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style C1 fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style C2 fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style C3 fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style D1 fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style D2 fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style E1 fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style E2 fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style V1 fill:#e2efda,stroke:#70ad47,color:#000000\n    style V2 fill:#e2efda,stroke:#70ad47,color:#000000\n    style V3 fill:#e2efda,stroke:#70ad47,color:#000000\n```\n\n### 4.2 Mermaid Diagram: Weakest Precondition Computation Flow\n\n```mermaid\nflowchart TD\n    Start([Start: Function Body S and Postcondition Q]) --> Split[Split S into atomic statements]\n    Split --> S1[Statement 1]\n    S1 --> S2[Statement 2]\n    S2 --> SN[Statement N]\n\n    S1 --> A1[Apply Assignment Rule Q E over x]\n    A1 --> A2[Apply Sequence Rule backward]\n    A2 --> A3[Apply Conditional Rule for branches]\n    A3 --> A4[Apply Loop Rule with invariant I]\n    A4 --> Final[Result wp S Q]\n\n    Final --> Check{Pre implies wp S Q}\n    Check -- YES --> Pass[Contract VALID]\n    Check -- NO --> Fail[Contract INVALID]\n    Fail --> Fix[Strengthen Pre or Weaken Post]\n\n    style Start fill:#1f4e79,stroke:#0b2a4a,color:#ffffff\n    style Split fill:#5b9bd5,stroke:#1f4e79,color:#ffffff\n    style S1 fill:#5b9bd5,stroke:#1f4e79,color:#ffffff\n    style S2 fill:#5b9bd5,stroke:#1f4e79,color:#ffffff\n    style SN fill:#5b9bd5,stroke:#1f4e79,color:#ffffff\n    style A1 fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style A2 fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style A3 fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style A4 fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style Final fill:#70ad47,stroke:#385723,color:#ffffff\n    style Check fill:#ffc000,stroke:#7f6000,color:#000000\n    style Pass fill:#70ad47,stroke:#385723,color:#ffffff\n    style Fail fill:#c00000,stroke:#7f0000,color:#ffffff\n    style Fix fill:#c00000,stroke:#7f0000,color:#ffffff\n```\n\n### 4.3 Mermaid Diagram: Hoare Inference Rule System\n\n```mermaid\ngraph LR\n    R1[Empty Statement Skip] --> R1F[wp skip Q equals Q]\n    R2[Assignment x equals E] --> R2F[wp x equals E Q equals Q E over x]\n    R3[Composition S1 then S2] --> R3F[wp S1 wp S2 Q]\n    R4[Conditional if B then S1 else S2] --> R4F[B and wp S1 Q or not B and wp S2 Q]\n    R5[While Loop while B do S] --> R5F[Loop Invariant I with Variant t]\n\n    R5F --> VC[Generate Verification Conditions]\n    VC --> VC1[VC1: Pre implies I]\n    VC --> VC2[VC2: I and B implies wp S I]\n    VC --> VC3[VC3: I and not B implies Post]\n\n    R6[Consequence] --> R6F[P implies P prime and P prime S Q prime and Q prime implies Q]\n    R7[Disjunction] --> R7F[P1 S Q and P2 S Q implies P1 or P2 S Q]\n\n    style R1 fill:#1f4e79,stroke:#0b2a4a,color:#ffffff\n    style R2 fill:#1f4e79,stroke:#0b2a4a,color:#ffffff\n    style R3 fill:#1f4e79,stroke:#0b2a4a,color:#ffffff\n    style R4 fill:#1f4e79,stroke:#0b2a4a,color:#ffffff\n    style R5 fill:#1f4e79,stroke:#0b2a4a,color:#ffffff\n    style R6 fill:#5b9bd5,stroke:#1f4e79,color:#ffffff\n    style R7 fill:#5b9bd5,stroke:#1f4e79,color:#ffffff\n    style R1F fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style R2F fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style R3F fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style R4F fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style R5F fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style R6F fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style R7F fill:#deebf7,stroke:#5b9bd5,color:#000000\n    style VC fill:#70ad47,stroke:#385723,color:#ffffff\n    style VC1 fill:#e2efda,stroke:#70ad47,color:#000000\n    style VC2 fill:#e2efda,stroke:#70ad47,color:#000000\n    style VC3 fill:#e2efda,stroke:#70ad47,color:#000000\n```\n\n### 4.4 Block-Level Functional Architecture: Contract Verification Pipeline\n\n```mermaid\nflowchart LR\n    SRC[Source Code with Annotations] --> PARSE[Parser JML ACSL SPARK FramaC]\n    PARSE --> AST[Abstract Syntax Tree plus Contract Annotations]\n\n    AST --> WPCAL[Weakest Precondition Calculator wp engine]\n    WPCAL --> VC[Verification Conditions VCs]\n\n    VC --> PROV[Prover Backend SMT Z3 Alt Ergo CVC5]\n    PROV --> DEC{Decidable?}\n\n    DEC -- YES and VALID --> OK[PROVED Contract holds]\n    DEC -- YES and INVALID --> BAD[FAILED Counterexample]\n    DEC -- TIMEOUT --> UNK[UNKNOWN Inconclusive]\n\n    OK --> CERT[Formal Certificate]\n    BAD --> DEBUG[Debug Output for Developer]\n    UNK --> MANUAL[Manual Inspection Required]\n\n    style SRC fill:#1f4e79,stroke:#0b2a4a,color:#ffffff\n    style PARSE fill:#5b9bd5,stroke:#1f4e79,color:#ffffff\n    style AST fill:#5b9bd5,stroke:#1f4e79,color:#ffffff\n    style WPCAL fill:#5b9bd5,stroke:#1f4e79,color:#ffffff\n    style VC fill:#70ad47,stroke:#385723,color:#ffffff\n    style PROV fill:#5b9bd5,stroke:#1f4e79,color:#ffffff\n    style DEC fill:#ffc000,stroke:#7f6000,color:#000000\n    style OK fill:#70ad47,stroke:#385723,color:#ffffff\n    style BAD fill:#c00000,stroke:#7f0000,color:#ffffff\n    style UNK fill:#c00000,stroke:#7f0000,color:#ffffff\n    style CERT fill:#e2efda,stroke:#70ad47,color:#000000\n    style DEBUG fill:#fce4d6,stroke:#c00000,color:#000000\n    style MANUAL fill:#fce4d6,stroke:#c00000,color:#000000\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n### PART A — Short Answer Questions (3 Marks Each)\n\n> **Q1. [KTU University Exam — May 2024]**\n> **Define a function contract in formal methods and list its four essential components.**\n\n**Model Answer (3 Marks):**\nA **function contract** is a formal, mathematically precise specification of a function's behaviour expressed using first-order logic predicates, defining mutual obligations between the *caller* and the *implementation*.\n\nIts **four essential components** are:\n1. **Precondition ($P$):** A predicate that must hold before the function is called. The *caller* is responsible for ensuring this.\n2. **Postcondition ($Q$):** A predicate that must hold after the function terminates (assuming the precondition held). The *implementation* is responsible for ensuring this.\n3. **Frame Condition (modifies clause):** The set of variables or memory locations the function is permitted to alter. All other state must remain unchanged.\n4. **Invariant:** A global property of the data structure that must be preserved across every public function invocation.\n\nTogether, these form the Hoare-style specification $\\{P\\}\\;S\\;\\{Q\\}$ augmented with frame and invariant clauses, which can be mechanically verified using weakest precondition calculus. **(3 Marks)**\n\n---\n\n> **Q2. [KTU University Exam — Dec 2023]**\n> **Explain the difference between partial correctness and total correctness in Hoare triples, with a suitable example.**\n\n**Model Answer (3 Marks):**\nThe difference lies in the **termination guarantee**:\n\n- **Partial Correctness $\\{P\\}\\;S\\;\\{Q\\}$:** Asserts that *if* the program terminates starting from a state satisfying $P$, *then* the final state satisfies $Q$. It does **not** guarantee that $S$ will actually halt.\n\n- **Total Correctness $[P]\\;S\\;[Q]$:** Asserts that starting from $P$, the program is **guaranteed to terminate** and the final state will satisfy $Q$. Denoted with square brackets and a **variant** (or *ranking function*) that strictly decreases with each loop iteration.\n\n**Example:** For an integer division routine:\n- Partial: $\\{y \\neq 0 \\land x \\geq 0\\}\\; \\text{divide}(x,y) \\;\\{ \\text{result} \\cdot y = x \\}$ — correct only if it terminates.\n- Total: $[y \\neq 0 \\land x \\geq 0]\\; \\text{divide}(x,y) \\;[ \\text{result} \\cdot y = x ]$ — also asserts termination after a finite number of subtraction steps. **(3 Marks)**\n\n---\n\n### PART B — Long Answer Questions (14 Marks Each, Internal Choice)\n\n> **Question A (14 Marks) — Option 1**\n\n**Q. (a) [7 Marks] Compute the weakest precondition $wp(S, Q)$ for the following program segment $S$ and postcondition $Q$, and verify whether the precondition $P$ is sufficient.**\n```\nS:  y := x * x;\n    z := y + x;\n    if (z > 10) then\n        w := z - 5;\n    else\n        w := z + 1;\n    end if;\n```\nGiven: $P \\equiv x \\geq 0$ and $Q \\equiv w > 6$.\n\n**(b) [7 Marks] With a neat diagram, explain the structure of a function contract. Discuss the role of *frame conditions* and *loop invariants* in contract-based verification.**\n\n---\n\n**Model Solution for (a) — Step by Step:**\n\n**Step 1: Apply the assignment rule to the first statement $y := x \\cdot x$.**\n\nBy Formula #7, $wp(y := x \\cdot x, Q') = Q'[x \\cdot x / y]$, where $Q'$ is the postcondition of the rest of the program.\n\n**Step 2: Apply the assignment rule to the second statement $z := y + x$.**\n\n$$wp(z := y + x, Q') = Q'[y + x / z]$$\n\n**Step 3: For the conditional, apply Formula #9.**\n\nLet $B \\equiv (z > 10)$. We need the wp of both branches:\n- $wp(w := z - 5, Q) = Q[z - 5 / w] \\equiv (z - 5) > 6 \\equiv z > 11$\n- $wp(w := z + 1, Q) = Q[z + 1 / w] \\equiv (z + 1) > 6 \\equiv z > 5$\n\nBy the conditional rule:\n$$wp(\\text{if-then-else}, Q) = (z > 10 \\land z > 11) \\lor (z \\leq 10 \\land z > 5) = z > 11 \\lor (5 < z \\leq 10)$$\n\n**Step 4: Substitute back to get $wp(S, Q)$.**\n\nReplacing $z$ with $y + x$, and $y$ with $x \\cdot x$:\n\n$$wp(S, Q) = (x^2 + x > 11) \\lor (5 < x^2 + x \\leq 10)$$\n\n$$\\boxed{wp(S, Q) \\equiv (x^2 + x > 5)}$$\n\n(after simplifying the disjunction with $5 < x^2 + x$ as the inclusive lower bound).\n\n**Step 5: Verify that $P \\Rightarrow wp(S, Q)$.**\n\nWe need: $x \\geq 0 \\Rightarrow x^2 + x > 5$.\n\nTest $x = 0$: $x^2 + x = 0 \\not> 5$. **Fails!**\n\nSo $P$ is **insufficient**. The corrected precondition would be $P' \\equiv x \\geq 3$ (since $3^2 + 3 = 12 > 5$, and for $x \\geq 3$, $x^2 + x$ is monotonically increasing). **(7 Marks)**\n\n---\n\n**Model Solution for (b) — Step by Step:**\n\nA function contract is a logical specification consisting of a precondition $P$, postcondition $Q$, frame condition $F$, and invariant $\\mathcal{I}$, as illustrated in the diagram from Section 4.1.\n\n**Frame Conditions:**\n- The *modifies clause* $F$ explicitly enumerates the variables the function may change.\n- By implicit postcondition, *all variables not in $F$* must be **unchanged** at function exit.\n- This enables **local reasoning**: the verifier can assume non-listed variables are untouched, dramatically reducing the proof state.\n- **Example:** `modifies \\result, balance` — only the return value and the `balance` field may be modified; all other fields remain invariant.\n\n**Loop Invariants in Verification:**\n- A *loop invariant* $I$ is a predicate that holds **before** the loop, is **preserved** by every iteration, and when combined with the negation of the loop guard, **implies the postcondition**.\n- For verification, three Verification Conditions (VCs) must be discharged:\n  - **VC1 (Entry):** $P \\Rightarrow I$ — the invariant holds initially.\n  - **VC2 (Preservation):** $I \\land B \\Rightarrow wp(S, I)$ — the body $S$ preserves $I$ when the guard $B$ holds.\n  - **VC3 (Exit):** $I \\land \\neg B \\Rightarrow Q$ — the invariant plus loop exit implies the postcondition.\n- For total correctness, an additional **variant** (a non-negative integer that strictly decreases) must be supplied to prove termination.\n\n**Conclusion:** Frame conditions and loop invariants are *indispensable* for making contract verification tractable — they localize reasoning and allow the prover to handle arbitrarily long loops through a single invariant predicate. **(7 Marks)**\n\n---\n\n> **Question B (14 Marks) — Option 2 (Alternative)**\n\n**Q. (a) [7 Marks] Explain Dijkstra's Weakest Precondition Calculus. Derive the wp rules for: (i) assignment, (ii) sequence, and (iii) the if-then-else statement.**\n\n**(b) [7 Marks] Consider the following function contract for an integer square-root routine:**\n```\nfunction isqrt(n: int) returns (r: int)\n  Pre:  n ≥ 0\n  Post: r*r ≤ n < (r+1)*(r+1)\n```\n**Show how to discharge the verification conditions using a suitable loop invariant, and explain why this proves the function correct.**\n\n---\n\n**Model Solution for (a) — Step by Step:**\n\n**Dijkstra's Weakest Precondition (wp) Calculus:**\n\nProposed by Edsger W. Dijkstra in 1975, $wp(S, Q)$ is defined as the *weakest* (least restrictive) predicate on the initial state that guarantees that executing $S$ terminates in a state satisfying $Q$. Formally:\n\n$$wp(S, Q) \\equiv \\bigwedge \\{P \\mid \\{P\\}\\;S\\;\\{Q\\}\\}$$\n\nThat is, $wp(S, Q)$ is the *meet* (greatest lower bound) of all valid preconditions for the same postcondition $Q$. It is a *predicate transformer* — it takes a postcondition $Q$ and transforms it into the weakest precondition that ensures $Q$.\n\n**Property:** $P \\Rightarrow wp(S, Q) \\iff \\{P\\}\\;S\\;\\{Q\\}$ is a valid Hoare triple. This single equivalence is the foundation of all mechanical contract verification.\n\n---\n\n**(i) Derivation of Assignment Rule: $wp(x := E, Q) = Q[E/x]$**\n\nWe require the weakest $P$ such that $\\{P\\}\\;x := E\\;\\{Q\\}$ is valid. After assignment, $x$ holds the value $E$ (evaluated in the pre-state). So $Q$ must hold in the post-state, meaning that *in the pre-state* the predicate $Q$ with $x$ replaced by $E$ must hold:\n\n$$\\boxed{wp(x := E, Q) = Q[E/x]}$$\n\n---\n\n**(ii) Derivation of Sequence Rule: $wp(S_1; S_2, Q) = wp(S_1, wp(S_2, Q))$**\n\nTo establish $Q$ after $S_1; S_2$, we need (a) executing $S_2$ to lead to $Q$, i.e., the state after $S_1$ must satisfy $wp(S_2, Q)$; and (b) executing $S_1$ must lead to a state satisfying $wp(S_2, Q)$. Combining:\n\n$$\\boxed{wp(S_1; S_2, Q) = wp(S_1, wp(S_2, Q))}$$\n\n---\n\n**(iii) Derivation of If-Then-Else Rule:**\n\nFor `if B then S_1 else S_2 end`, the precondition must guarantee that *whichever branch is taken* leads to $Q$:\n- If $B$ holds initially, then $S_1$ executes, and we need $wp(S_1, Q)$.\n- If $\\neg B$ holds initially, then $S_2$ executes, and we need $wp(S_2, Q)$.\n\nTherefore:\n$$\\boxed{wp(\\text{if } B \\text{ then } S_1 \\text{ else } S_2, Q) = (B \\land wp(S_1, Q)) \\lor (\\neg B \\land wp(S_2, Q))}$$\n\n**(7 Marks)**\n\n---\n\n**Model Solution for (b) — Step by Step:**\n\n**Hypothetical Implementation of `isqrt`:**\n```\nr := 0;\ns := 1;\nwhile (s ≤ n) loop\n    r := r + 1;\n    s := s + 2*r + 1;\nend loop;\n```\n\n**Note on correctness of the body:** The identity $s_{\\text{new}} = s_{\\text{old}} + 2r + 1$ uses the fact that $(r+1)^2 = r^2 + 2r + 1$, so $s$ tracks $r^2$ at every step.\n\n**Step 1: Propose a loop invariant $I$.**\n\n$$I \\;\\equiv\\; r \\geq 0 \\;\\land\\; s = r \\cdot r \\;\\land\\; s > n \\;\\lor\\; s \\leq n$$\n\nMore cleanly:\n$$I \\;\\equiv\\; r \\geq 0 \\;\\land\\; s = r^2 \\;\\land\\; (s \\leq n \\;\\lor\\; s > n)$$\n\n(But we need a more useful invariant that ties $r$, $s$, and $n$): \n$$\\boxed{I \\;\\equiv\\; 0 \\leq r \\;\\land\\; s = r^2 \\;\\land\\; s \\leq n \\;\\Rightarrow\\; r^2 \\leq n}$$\n\nActually, the precise invariant is:\n$$I \\;\\equiv\\; 0 \\leq r \\;\\land\\; s = r^2 \\;\\land\\; (r = 0 \\lor s \\leq n)$$\n\n**Step 2: Verify the three Verification Conditions (VCs).**\n\n**VC1 (Entry — Invariant holds initially):**\n- $r = 0, s = 1$ initially.\n- $r \\geq 0$ ✓\n- $s = r^2 \\Rightarrow 1 = 0$? **False!**\n\nSo this simple invariant fails at entry. We need a *different* invariant. The correct choice is:\n\n$$I \\;\\equiv\\; 0 \\leq r \\;\\land\\; s = (r+1)^2 \\;\\land\\; (r = 0 \\lor s \\leq n + 1)$$\n\nBut a cleaner approach uses the postcondition directly. Let $I \\equiv r \\geq 0 \\land s = (r+1)^2$.\n\nInitially $r = 0, s = 1$: $s = (0+1)^2 = 1$ ✓.\n\n**Induction through the loop body:** After one iteration, $r' = r+1$, $s' = s + 2r + 1 = (r+1)^2 + 2r + 1 = r^2 + 4r + 2$. Wait, recalculate:\n\n$s' = s + 2r' + 1 = (r+1)^2 + 2(r+1) + 1 = r^2 + 2r + 1 + 2r + 2 + 1 = r^2 + 4r + 4 = (r+2)^2 = (r'+1)^2$ ✓\n\n**VC2 (Preservation):** $I \\land B \\Rightarrow wp(\\text{body}, I)$\n- $B \\equiv s \\leq n$, $I$ holds, $s = (r+1)^2 \\leq n$.\n- After body: $r' = r+1$, $s' = (r'+1)^2$, so $I$ holds. ✓\n\n**VC3 (Exit — Implies Postcondition):** $I \\land \\neg B \\Rightarrow \\text{Post}$\n- $\\neg B \\equiv s > n$, so $(r+1)^2 > n$.\n- $I$ gives $r \\geq 0$, $s = (r+1)^2$, hence $r^2 \\leq (r+1)^2 = s$ (since $r \\geq 0$).\n- Wait — we need $r^2 \\leq n$ to satisfy `Post`. We have $s = (r+1)^2 > n$, so $(r+1)^2 > n$, which gives $n < (r+1)^2$ — this is the **upper bound** of Post.\n- For the **lower bound** $r^2 \\leq n$, we need to argue that $r^2 \\leq (r+1)^2 = s$. But this doesn't give $r^2 \\leq n$ directly.\n\nThe postcondition is $r^2 \\leq n < (r+1)^2$. From $\\neg B$: $(r+1)^2 > n$ ✓. We need $r^2 \\leq n$, which requires showing $r^2$ was *always* $\\leq n$ during the iterations when $s \\leq n$ held.\n\nThis is why a stronger invariant is needed:\n$$I \\;\\equiv\\; 0 \\leq r \\;\\land\\; s = (r+1)^2 \\;\\land\\; s \\leq n + 1$$\n\nThen at exit, $s > n$ gives $(r+1)^2 > n$ ✓, and $s \\leq n+1$ gives $(r+1)^2 \\leq n+1$, hence $r^2 \\leq n$ (since $r^2 = (r+1)^2 - 2r - 1 \\leq n+1 - 1 = n$ for $r \\geq 0$). ✓\n\n**Step 3: Total Correctness (Termination):** The variant $t \\equiv n - r^2$ is non-negative (from invariant), and strictly decreases each iteration because $s = (r+1)^2 > r^2$, so $(r+1)^2 - r^2 = 2r+1 > 0$ for $r \\geq 0$, meaning $n - (r+1)^2 < n - r^2$. Thus the loop terminates. ✓\n\n**Conclusion:** All VCs are discharged, and the variant proves termination. Therefore, the function contract $\\text{Pre} \\equiv n \\geq 0$ and $\\text{Post} \\equiv r^2 \\leq n < (r+1)^2$ is **fully verified**. **(7 Marks)**\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls**\n> 1. **Forgetting `\\old()` in postconditions:** When a postcondition refers to the *initial* value of a variable, students often write just `balance` (current value) instead of `\\old(balance)`. This is the **#1 mark-deduction error** in JML/ACSL-style contract questions.\n> 2. **Confusing precondition weakening with strengthening:** The *Consequence Rule* (Formula #12) lets you **weaken** the precondition ($P \\Rightarrow P'$) and **strengthen** the postcondition ($Q' \\Rightarrow Q$). Students often invert these directions.\n> 3. **Ignoring the frame condition:** A function that modifies an *unlisted* variable is **automatically incorrect**, even if the postcondition holds. Many students omit frame conditions entirely.\n> 4. **Partial vs. total correctness confusion:** KTU questions often ask for *termination proofs* (variants). Writing only the loop invariant without the variant costs **at least 3 marks** in Part B.\n> 5. **Forgetting the Law of Excluded Miracle (Formula #19):** $wp(S, \\text{false}) = \\text{false}$ for *all* statements $S$ — this healthiness condition is regularly tested as a 1-mark trick question.\n\n---\n\n### 📌 Topic Recap & Important Things to Remember\n\n- ✅ A **function contract** is the formal specification $\\{P\\}S\\{Q\\}$ augmented with frame and invariant clauses, encoding the **caller–supplier agreement**.\n- ✅ **Precondition** = caller's responsibility; **Postcondition** = supplier's responsibility.\n- ✅ **Hoare triple** = partial correctness (no termination guarantee); **square-bracket triple** = total correctness.\n- ✅ **Weakest Precondition** $wp(S, Q)$ is the *least restrictive* $P$ such that $\\{P\\}S\\{Q\\}$ holds.\n- ✅ **Validity check:** $\\{P\\}S\\{Q\\}$ is valid iff $P \\Rightarrow wp(S, Q)$.\n- ✅ **Assignment rule:** $wp(x := E, Q) = Q[E/x]$ — substitute the expression for the variable.\n- ✅ **Sequence rule:** $wp(S_1; S_2, Q) = wp(S_1, wp(S_2, Q))$ — apply backwards.\n- ✅ **Conditional rule:** $(B \\land wp(S_1, Q)) \\lor (\\neg B \\land wp(S_2, Q))$.\n- ✅ **Loop rule** uses invariants $I$ that satisfy **Entry, Preservation, Exit VCs**.\n- ✅ **Frame condition** = `modifies` clause; everything else is *implicitly unchanged*.\n- ✅ **Law of Excluded Miracle:** $wp(S, \\text{false}) = \\text{false}$ always.\n- ✅ **Consequence rule:** $P \\Rightarrow P'$ and $\\{P'\\}S\\{Q'\\}$ and $Q' \\Rightarrow Q$ implies $\\{P\\}S\\{Q\\}$.\n- ✅ **Termination proof** requires a **variant** (well-founded decreasing expression), not just an invariant.\n- ✅ **Old value notation:** Use $\\backslash\\text{old}(x)$ in postconditions to refer to pre-call value of $x$.\n- ✅ **Tools:** Frama-C (ACSL), KeY (JML), SPARK Prover, Dafny, VerCors, Why3 — all rely on wp-calculus internally.\n- ✅ **Industrial use:** Avionics (DO-178C), railways (EN 50128), automotive (ISO 26262), medical devices (IEC 62304).\n- ✅ **Discharge of VCs** is done by SMT solvers (Z3, CVC5, Alt-Ergo) — they are *semi-decidable* for first-order logic fragments.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "artificial-intelligence-and-data-science-7-pecst741-mModule 4-t3",
          "title": "Pure Functions",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST741 - FORMAL METHODS IN SOFTWARE ENGINEERING\n> - **Module:** Module 4\n> - **Topic:** Pure Functions\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Academic Definition\n\nIn the context of **Formal Methods in Software Engineering** (KTU 2024 Scheme, PECST741), a **Pure Function** is rigorously defined as a computational construct that satisfies two mathematically necessary and sufficient conditions:\n\n1. **Determinism**: For every input value $x$ drawn from the function's domain $D$, the function $f$ produces exactly one output value $y$ in the codomain $C$, such that $y = f(x)$ is uniquely determined.\n2. **Absence of Side Effects**: The function's evaluation alters **no observable state** outside of its own local scope. No external variable, data structure, file system, network resource, console output, or hardware state is modified as a consequence of evaluation.\n\nMathematically expressed, a pure function is a total mapping:\n\n$$f: D \\rightarrow C \\quad \\text{where} \\quad \\forall x \\in D, \\exists ! y \\in C \\mid y = f(x)$$\n\nIn formal specification languages such as **VDM-SL**, **Z notation**, **B-Method**, and **Alloy**, pure functions form the foundational primitive from which all larger behavioural specifications (operations, schemas, state machines) are constructed. They are the direct computer-science counterparts of mathematical functions as understood in classical set theory and lambda calculus ($\\lambda$-calculus).\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Highlight**: Pure functions are the cornerstone of *referential transparency* — a property essential to *equational reasoning*, *symbolic execution*, and *model-based testing* in formal verification tools.\n\n### 1.2 Conceptual Analogy — The \"Deterministic Coffee Vending Machine\"\n\nImagine a vending machine where you press a single button labelled \"Espresso\".\n\n- The machine has a **fixed, pre-defined recipe** (input: 1 token; output: exactly 90 ml of espresso at 92°C).\n- Every time you insert the same token and press the same button, you receive an **identical cup of espresso** — same volume, same temperature, same taste.\n- The machine **does not change the world around it**: it does not alter the time of day, the weather, your bank balance (only the inserted token is consumed locally), the position of the moon, or the temperature of the room.\n- You can give this machine to a mathematician in a black box. Without looking inside, the mathematician can **predict the output with 100 % certainty** purely from the input and the machine's published specification.\n\nA **pure function is exactly such a machine**. Given the same input, it always returns the same output, and it never \"leaks\" any side effect into the surrounding computational environment.\n\n> [!NOTE]\n> **Why this matters in Formal Methods**: A pure function's behaviour can be captured by a simple, finite set of input-output pairs (or a closed-form expression). This makes it directly amenable to *exhaustive verification*, *proof by induction*, and *property-based testing* — three pillars of formal software engineering.\n\n### 1.3 Contrast — Impure vs. Pure Functions\n\n| Aspect | Pure Function | Impure Function |\n|---|---|---|\n| **Determinism** | Always returns same output for same input | May return different outputs for same input |\n| **Side Effects** | None — no I/O, no mutation | May read/write global state, perform I/O |\n| **Referential Transparency** | Holds (expression == its value) | Fails (expression != its value) |\n| **Testability** | Trivial — given input, check output | Requires mocks, harnesses, ordering |\n| **Parallel Safety** | Inherently thread-safe | Requires locks, synchronization |\n| **Formal Verifiability** | Directly verifiable | Requires complex state machines |\n| **Example** | `square(x) = x * x` | `Date.now()`, `Math.random()` |\n\n> [!TIP]\n> **Key Insight for KTU Exams**: A student must always justify the *purity* of a function by demonstrating (a) it is a total mapping from a declared domain to a codomain, and (b) it produces no observable change in the program's external state. Mentioning only one of these two is **incomplete** and attracts partial mark loss.\n\n### 1.4 Formal Role in Specification Languages\n\n| Language | Pure Function Construct | Notation Example |\n|---|---|---|\n| **VDM-SL** | Explicit function definition | `square(x: nat) r: nat`<br>`pre true`<br>`post r = x * x` |\n| **Z Notation** | Schema-based function | $\\Delta$ schema-free function with decoration |\n| **B-Method** | Abstract Machine substitution | `res <-- square(x) = ...` |\n| **Alloy** | Pure relation (function symbol `->`) | `square: Int -> Int { x -> y: y = mul[x,x] }` |\n| **Haskell** | Function (all functions are pure by default) | `square x = x * x` |\n\n### 1.5 Axiomatic Properties (Statements without Proof — to be cited as-is)\n\n- **Property 1 (Determinism)**: $\\forall x \\in D, \\quad f(x) = f(x)$ (trivially unique).\n- **Property 2 (Idempotence of Substitution)**: If $e_1 \\equiv e_2$ (syntactically/extensionally equal), then $f(e_1) \\equiv f(e_2)$ in any context.\n- **Property 3 (Compositional Closure)**: The composition $g \\circ f$ of two pure functions is itself a pure function.\n- **Property 4 (Referential Transparency)**: Any sub-expression of a pure function can be replaced by its value without changing the program's overall behaviour.\n\n> [!IMPORTANT]\n> These four properties collectively form the **theoretical backbone** of equational reasoning in functional programming and form-based specification — both are *direct exam questions* in KTU Module 4 of PECST741.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Operational Breakdown — The \"Why\" and \"How\" of Pure Functions\n\nThe semantics of a pure function $f$ can be decomposed into a sequence of logical steps that are universally applied across formal specification frameworks:\n\n- **Step 1 — Domain Declaration**: The specifier explicitly states the type or set $D$ of legal input values. Example: `f: ℕ → ℕ` (a function from natural numbers to natural numbers). This is critical because *partial* functions (functions that are undefined on some inputs) are not considered pure in a strict sense — they require preconditions.\n\n- **Step 2 — Codomain Declaration**: The specifier declares the type or set $C$ of all possible output values. The codomain may be a strict superset of the actual *image* (range) of the function.\n\n- **Step 3 — Precondition Specification** (when required): If the function is partial, a precondition `pre` clause is added. For a *total* pure function, the precondition is implicitly `true` for the entire domain.\n\n- **Step 4 — Postcondition Specification**: A `post` clause or equivalent defines the output mathematically. The postcondition must reference only the function's arguments and the result variable — **never** any external state.\n\n- **Step 5 — Locality Enforcement**: The implementation must be verified (by proof, by type system, or by inspection) to access no global mutable state. In languages like Haskell, the type system itself prevents impurity at compile time. In VDM-SL, the specifier must justify purity through the `externals` clause being empty.\n\n- **Step 6 — Composition and Substitution**: Once purity is established, the function may be freely substituted into any other expression without altering the surrounding context. This is the *raison d'être* of pure functions in formal proof systems.\n\n### 2.2 Referiential Transparency — The Deep Principle\n\n> [!NOTE]\n> **Referential Transparency (RT)** is a property of expressions in a program: an expression is referentially transparent if it can be replaced by its value without changing the program's behaviour.\n\nFormally, an expression $e$ is referentially transparent if and only if:\n\n$$ \\forall \\text{ context } C[\\cdot], \\quad C[e] \\equiv C[\\text{val}(e)] $$\n\nwhere $\\text{val}(e)$ denotes the value of $e$, and $\\equiv$ denotes *observational equivalence* (i.e., the two programs produce the same output given the same input and have the same effect on the external world).\n\n**The Golden Link**: *A function is pure if and only if all of its applications are referentially transparent.*\n\nThis equivalence is the fundamental theorem that bridges functional programming and formal specification. It is provable by mutual implication:\n\n- **Forward**: If $f$ is pure (no side effects, deterministic), then $f(x)$ depends only on $x$. Substituting $f(x)$ for its value cannot change anything outside $f$'s scope. Hence, $f(x)$ is RT.\n- **Reverse**: If $f$ is not pure, then either (a) it depends on hidden state (RT fails because the value depends on context), or (b) it modifies hidden state (RT fails because substitution would lose the modification). Hence, $f$ is impure.\n\n### 2.3 KTU High-Yield Formula Sheet\n\n> [!IMPORTANT]\n> **Examination Cheat Sheet — Pin This Table**\n\n| Symbol / Concept | Mathematical Expression | Meaning in PECST741 Context |\n|---|---|---|\n| Pure Function | $f: D \\to C$ | Total mapping with no side effects |\n| Domain | $D$ | Set of legal input values |\n| Codomain | $C$ | Set of declared possible outputs |\n| Image / Range | $f(D) = \\{f(x) \\mid x \\in D\\}$ | Actual outputs produced |\n| Determinism | $\\forall x \\in D, \\forall y_1, y_2 \\in C, (f(x) = y_1 \\land f(x) = y_2) \\Rightarrow y_1 = y_2$ | Uniqueness of output |\n| Side-Effect Freedom | $\\text{observable}(f, \\text{world}) = \\text{const}$ | World state unchanged after $f$ |\n| Referential Transparency | $C[e] \\equiv C[\\text{val}(e)]$ | Substitution invariance |\n| Composition | $(g \\circ f)(x) = g(f(x))$ | Function chaining |\n| Precondition (VDM) | $\\text{pre } P(x)$ | Condition on inputs for partiality |\n| Postcondition (VDM) | $\\text{post } R(\\vec{x}, \\vec{r})$ | Relation between inputs and result |\n| Currying | $f(a, b) \\leftrightarrow f'(a)(b)$ | Multi-arg as nested single-arg |\n| $\\eta$-Reduction | $\\lambda x. (f x) \\to f$ | Equivalent expressions |\n| $\\beta$-Reduction | $(\\lambda x. e) \\, a \\to e[x := a]$ | Function application |\n| Fixed Point | $x = f(x)$ | Equational solution (FP) |\n| Map Fusion Law | $f \\circ \\text{map}(g) = \\text{map}(f \\circ g)$ | Composition through map |\n| Fold Law | $f(\\text{fold} \\, g \\, a \\, xs) = \\text{fold} \\, h \\, f(a) \\, \\text{map}(f, xs)$ | Structural recursion rule |\n\n> [!TIP]\n> The $\\beta$-reduction and $\\eta$-reduction rules come directly from $\\lambda$-calculus, the formal foundation of functional programming. KTU 2024 examiners frequently ask: *\"Show that two Haskell expressions are equivalent using $\\beta$- and $\\eta$-reduction.\"* Practice these reductions carefully.\n\n### 2.4 Real-World Engineering Utility\n\nPure functions are not a theoretical curiosity — they have tangible, industrial impact in:\n\n- **Cryptography**: SHA-256, AES round functions, and HMAC primitives are pure — given the same plaintext, they always produce the same ciphertext. This is essential for reproducibility and verification.\n- **Aerospace & Automotive (DO-178C, ISO 26262)**: Code at the highest safety integrity levels (DAL A / ASIL D) is required to be free of side effects on safety-critical state. Pure functions dominate these code bases.\n- **Distributed Systems (Apache Spark, Hadoop)**: All transformations in Spark RDDs are pure functions — this is what allows the framework to safely reorder, partition, and parallelize computations across thousands of nodes.\n- **Build Systems (Make, Bazel, Nix)**: Build rules are pure functions from source files to artifact hashes. A pure build is *reproducible* — given the same inputs, it produces the same outputs, byte-for-byte.\n- **Database Query Optimizers (SQL)**: Pure expressions in SELECT clauses can be evaluated in any order, on any node, with aggressive optimization — because order does not matter.\n- **Compilers and Symbolic Execution Tools**: Pure functions are the only construct that symbolic executors (KLEE, CBMC, Java Pathfinder) can reason about without state-space explosion.\n\n### 2.5 Deep Pitfalls — Common Student Errors in KTU Exams\n\n> [!WARNING]\n> **Common Pitfall 1**: Confusing *partial* functions with *impure* functions. A partial function is one that is undefined for some inputs in its declared domain. A *pure* function must be **total** (or, if partial, must carry an explicit precondition). These are orthogonal concepts.\n\n> [!WARNING]\n> **Common Pitfall 2**: Treating a function as pure merely because it does not perform I/O. A function that reads a global mutable variable is impure, even if it never performs I/O. Purity requires *total locality* of state.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Derivation: Equivalence of Purity and Referential Transparency\n\nWe prove the central theoretical claim that anchors Module 4 of PECST741.\n\n**Theorem (Purity $\\Leftrightarrow$ Referential Transparency)**:\nA function $f$ is pure if and only if every application $f(x)$ is a referentially transparent expression.\n\n**Proof (Forward Direction: Purity $\\Rightarrow$ RT)**:\n\nAssume $f$ is pure. By definition, this means:\n\n1. $\\forall x \\in D, f(x)$ is uniquely determined — i.e., $f$ is deterministic.\n2. The evaluation of $f(x)$ has no observable effect on the external world.\n\nWe must show that for any context $C[\\cdot]$ and any expression $e \\equiv f(x)$:\n\n$$C[e] \\equiv C[\\text{val}(e)]$$\n\nBy (1), $\\text{val}(e) = \\text{val}(f(x))$ is unique. By (2), neither $e$ nor $\\text{val}(e)$ has any side effect on $C$. Therefore, replacing $e$ with its value inside $C$ changes nothing observable. Hence $C[e] \\equiv C[\\text{val}(e)]$. $\\blacksquare$\n\n**Proof (Reverse Direction: RT $\\Rightarrow$ Purity)**:\n\nAssume every application of $f$ is referentially transparent. We prove purity by contradiction.\n\nSuppose $f$ is **not** pure. Then either:\n\n- (a) $f$ is non-deterministic: there exist $x \\in D$ and $y_1 \\neq y_2 \\in C$ such that $f(x)$ may yield $y_1$ on one call and $y_2$ on another.\n- (b) $f$ has a side effect on external state.\n\nConsider (a). The expression $f(x)$ is non-deterministic. Construct the context:\n\n$$C[\\cdot] = \\text{let } y = [\\cdot] \\text{ in } y + y$$\n\nIf $f(x)$ were referentially transparent, $C[f(x)]$ would equal $C[\\text{val}(f(x))]$. But since $f(x)$ may produce different values, the result is non-deterministic. By hypothesis, $f(x)$ is RT, so $C[f(x)]$ must be deterministic — contradiction.\n\nConsider (b). If $f$ mutates external state, then the context $C[\\cdot]$ that reads that state after $f$ runs will observe the mutation. But if we substitute $\\text{val}(f(x))$ for $f(x)$ and re-run, the side effect is lost. Hence $C[f(x)] \\not\\equiv C[\\text{val}(f(x))]$, contradicting RT. $\\blacksquare$\n\n**Conclusion**: The two notions are logically equivalent. This theorem is the formal basis for using *substitution-based reasoning* (the workhorse of every equational proof in functional programming and formal specification).\n\n### 3.2 Analytical Matrix — Decision Tree for Purity\n\n| Observation about $f$ | Pure? | Justification |\n|---|---|---|\n| $f$ is total, returns the same value for the same input, never reads/writes globals, never does I/O | **Yes** | Satisfies both determinism and locality |\n| $f$ returns the same value but writes to a log file | **No** | Side effect (file I/O) violates locality |\n| $f$ reads a global counter to compute its result | **No** | Reads external state — non-local |\n| $f$ uses `Date.now()` internally | **No** | Reads non-deterministic time source |\n| $f$ is a partial function (undefined for $x < 0$) with a precondition `pre x >= 0` | **Yes** | Pure, but with restricted domain |\n| $f$ is undefined for $x < 0$ and has **no** precondition | **No** | Not a total mapping — not a proper function |\n| $f$ calls a pure function $g$ and uses the result | **Yes** | Compositional closure (Property 3) |\n| $f$ is recursive but well-founded (base case terminates) | **Yes** | Termination makes it total |\n| $f$ is recursive but may not terminate (e.g., `f(x) = f(x)`) | **No** | Not total — not a function in the mathematical sense |\n\n### 3.3 Worked Example — VDM-SL Specification of a Pure Function\n\n**Specification**: Define a pure function `factorial` in VDM-SL that computes $n!$.\n\n**Step 1 — Identify the Domain and Codomain**:\n\n- Domain: $\\mathbb{N}_1$ (positive natural numbers, since $0! = 1$ is the base case).\n- Codomain: $\\mathbb{N}_1$.\n\n**Step 2 — Decide Total vs. Partial**:\n\n- We declare `factorial` as a total function on $\\mathbb{N}$, with the convention that $0! = 1$.\n\n**Step 3 — Write the Explicit Function Definition**:\n\n```\nfactorial: nat -> nat\nfactorial(n) ==\n  if n = 0\n  then 1\n  else n * factorial(n - 1)\npre true\npost RESULT = product {1, ..., n}\n```\n\n**Step 4 — Verify Purity**:\n\n- **Determinism**: Given any natural number $n$, the recursive descent is unique. The arithmetic operators are deterministic. Hence `factorial(n)` is unique. ✓\n- **Side-Effect Freedom**: The function reads only its argument `n` and the constant `1`. It writes only to the local `RESULT` variable. No global state, no I/O. ✓\n- **Compositional Closure**: We may freely substitute `factorial(5)` with `120` anywhere in a larger expression. ✓\n\n**Step 5 — Construct the Postcondition Proof Obligation**:\n\nThe postcondition `RESULT = product {1, ..., n}` must be proven by induction. This is precisely the kind of proof obligation a VDM-SL tool (e.g., the VDMTools, Overture) generates and discharges.\n\n### 3.4 Worked Example — $\\beta$-Reduction in Haskell\n\nConsider the Haskell expression:\n\n$$(\\lambda x. x^2 + 1) \\, 5$$\n\nApply $\\beta$-reduction step by step:\n\n**Step 1 — Identify the $\\lambda$-abstraction**: $\\lambda x. x^2 + 1$.\n\n**Step 2 — Identify the argument**: $5$.\n\n**Step 3 — Substitute $5$ for $x$ in the body** $x^2 + 1$:\n\n$$5^2 + 1$$\n\n**Step 4 — Evaluate the arithmetic**:\n\n$$5^2 = 25 \\quad \\Rightarrow \\quad 25 + 1 = 26$$\n\n**Final result**: $(\\lambda x. x^2 + 1) \\, 5 \\xrightarrow{\\beta} 26$.\n\nThis is a direct, mechanical application of the $\\beta$-reduction rule $(\\lambda x. e) \\, a \\to e[x := a]$.\n\n### 3.5 Derivation of the Map Fusion Law\n\n**Claim**: $f \\circ \\text{map}(g) = \\text{map}(f \\circ g)$ over a list $[x_1, x_2, \\ldots, x_n]$.\n\n**Proof by Structural Induction on the list**:\n\n**Base Case (empty list)**:\n\n$$f(\\text{map}(g, [])) = f([]) = [] = \\text{map}(f \\circ g, [])$$\n\n**Inductive Step**: Assume the law holds for a list of length $n$. Consider a list of length $n+1$: $[x_1, x_2, \\ldots, x_{n+1}] = (x_{n+1} : [x_1, \\ldots, x_n])$.\n\nWe apply the definition of `map` to the right-hand side:\n\n$$f(\\text{map}(g, x_{n+1} : xs)) = f(g(x_{n+1}) : \\text{map}(g, xs))$$\n\nSince $f$ is pure, we may apply it element-wise without affecting external state:\n\n$$= (f \\circ g)(x_{n+1}) : f(\\text{map}(g, xs))$$\n\nBy the inductive hypothesis, $f(\\text{map}(g, xs)) = \\text{map}(f \\circ g, xs)$:\n\n$$= (f \\circ g)(x_{n+1}) : \\text{map}(f \\circ g, xs) = \\text{map}(f \\circ g, x_{n+1} : xs)$$\n\nHence the law holds for lists of length $n+1$. By induction, the law holds for all finite lists. $\\blacksquare$\n\n> [!NOTE]\n> The map fusion law is a direct consequence of purity and is one of the most frequently tested **equational reasoning** laws in KTU Module 4. Mastering its proof is a high-yield exam investment.\n\n### 3.6 Analytical Matrix — When to Use Which Reasoning Technique\n\n| Scenario | Recommended Technique | Justification |\n|---|---|---|\n| Verify a single property of a pure function | Equational reasoning / substitution | Direct RT |\n| Prove a property over a recursive data structure | Structural induction | Purity + well-foundedness |\n| Show two programs are equivalent | Denotational semantics + bisimulation | Functional equality |\n| Show a property holds for all inputs | Property-based testing (QuickCheck) | Randomized input generation |\n| Discover counter-examples | Bounded model checking (Alloy, Spin) | Finite exhaustive search |\n| Verify a concurrent / impure program | Refinement calculus, process algebras | State-based reasoning |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Block-Level Functional Architecture — Anatomy of a Pure Function Call\n\nThe following Mermaid diagram depicts the *observational architecture* of a pure function invocation. The arrows crossing the **System Boundary** are the critical locus of purity — a pure function must have **zero** arrows crossing this boundary that represent state mutation or external reads.\n\n```mermaid\nflowchart LR\n    Caller[\"Caller / Expression<br>C[ . ]\"]\n    ArgIn[\"Input Value<br>x in D\"]\n    ResultOut[\"Output Value<br>y = f(x)\"]\n    LocalState[\"Local State<br>Arguments, Local Vars\"]\n    Body[\"Function Body<br>Deterministic Computation\"]\n    PrePost[\"Pre / Post Conditions<br>pre P(x) post R(x, RESULT)\"]\n    ReturnArrow[\"Return Value<br>y\"]\n    Boundary1[\"System Boundary\"]\n    Boundary2[\"System Boundary\"]\n\n    Caller -->|\"1. Call f(x)\"| ArgIn\n    ArgIn -->|\"2. Enter function scope\"| Boundary1\n    Boundary1 -->|\"3. Bind x to local\"| LocalState\n    LocalState --> Body\n    PrePost -.->|\"Validates\"| Body\n    Body -->|\"4. Compute y = f(x)\"| ReturnArrow\n    ReturnArrow -->|\"5. Return y to caller\"| Boundary2\n    Boundary2 --> ResultOut\n    ResultOut -->|\"6. Substitute\"| Caller\n```\n\n> [!IMPORTANT]\n> **Reading the Diagram**: Arrows numbered 1 through 6 trace the lifecycle of a single pure function call. The two **System Boundary** walls (Boundary1 on entry, Boundary2 on exit) are conceptual fences. The diagram shows that only the *input value* enters and only the *output value* exits. Crucially, **no arrows cross the boundaries for global state reads or writes**, which is the visual signature of purity.\n\n### 4.2 State Machine — Compositional Closure of Pure Functions\n\n```mermaid\nstateDiagram-v2\n    [*] --> Define_Pure_f\n    Define_Pure_f --> Verify_Determinism\n    Verify_Determinism --> Verify_Locality\n    Verify_Locality --> Purity_Established\n    Purity_Established --> Allow_Substitution\n    Allow_Substitution --> Allow_Composition\n    Allow_Composition --> Allow_Equational_Reasoning\n    Allow_Equational_Reasoning --> [*]\n    Verify_Determinism --> Reject_NonDeterministic\n    Reject_NonDeterministic --> [*]\n    Verify_Locality --> Reject_SideEffect\n    Reject_SideEffect --> [*]\n```\n\n> [!NOTE]\n> **Interpretation**: The state machine shows the *acceptance pipeline* for a candidate function. It must first pass determinism verification, then locality verification. Only upon passing both is the function labelled pure, after which all compositional reasoning rules become applicable. The `Reject_*` states are the failure exits and correspond to impure candidates.\n\n### 4.3 Block Diagram — Purity vs. Impurity in Program Structure\n\n```mermaid\nflowchart TB\n    subgraph Pure[\"PURE PROGRAM CONTEXT\"]\n        P1[\"f1: pure function\"]\n        P2[\"f2: pure function\"]\n        P3[\"f3: pure function\"]\n        P1 -->|\"x\"| P2\n        P2 -->|\"y\"| P3\n    end\n    subgraph Impure[\"IMPURE BOUNDARY (I/O WRAPPER)\"]\n        I1[\"main: impure driver\"]\n        I2[\"read input\"]\n        I3[\"print output\"]\n        I1 --> I2\n        I1 --> I3\n    end\n    Impure -->|\"invoke\"| Pure\n    Pure -->|\"return\"| Impure\n```\n\n> [!TIP]\n> **Architectural Insight**: The most successful industrial Haskell codebases (e.g., Facebook's Sigma, Galois's Cryptol) follow this *impure-shell, pure-core* architecture. The `main` function is the *only* impure construct, and it does no computation — it merely shuttles data into the pure core and prints the result. The pure core contains **all** business logic and is fully testable and verifiable.\n\n### 4.4 Comparison Flowchart — Decision: Pure or Impure?\n\n```mermaid\nflowchart TD\n    Start[\"Candidate function f\"]\n    Q1{\"Is f deterministic?<br>(Same input -> same output)\"}\n    Q2{\"Does f access external<br>mutable state?\"}\n    Q3{\"Does f perform I/O,<br>throw, or call impure fns?\"}\n    Out1[\"PURE FUNCTION\"]\n    Out2[\"IMPURE FUNCTION\"]\n    Out3[\"IMPURE FUNCTION\"]\n    Out4[\"IMPURE FUNCTION\"]\n\n    Start --> Q1\n    Q1 -->|\"Yes\"| Q2\n    Q1 -->|\"No\"| Out2\n    Q2 -->|\"No\"| Q3\n    Q2 -->|\"Yes\"| Out3\n    Q3 -->|\"No\"| Out1\n    Q3 -->|\"Yes\"| Out4\n```\n\n> [!WARNING]\n> **Exam Reading**: This flowchart is a high-yield visual for the KTU exam. A student can be asked: *\"Classify the following functions as pure or impure, justifying your answer using the two-pillar test.\"* The flowchart is the canonical answer structure.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A Questions (3 Marks Each)\n\n#### Question A1 `[KTU University Exam - Dec 2023, Model Question Paper Set A]`\n**Q: Define a pure function. State and briefly justify the two properties that a function must satisfy to be considered pure.**\n\n**Model Answer (3 Marks)**:\n\nA **pure function** is a function whose return value is determined entirely by its input parameters, and which produces **no observable side effects** on the program's external state.\n\nThe two essential properties are:\n\n1. **Determinism**: For any given input $x$ from the function's domain $D$, the function $f$ must return exactly the same output $y$ every time it is called. Formally, $\\forall x \\in D, \\forall y_1, y_2 \\in C, (f(x) = y_1 \\land f(x) = y_2) \\Rightarrow y_1 = y_2$.\n\n2. **Absence of Side Effects**: The evaluation of $f$ must not modify any external state, perform I/O, mutate global variables, or throw exceptions. The only observable change is the return value passed to the caller.\n\nJustification: These two properties together ensure **referential transparency**, which is the theoretical foundation for equational reasoning, substitution-based proofs, and compositional verification — all central to formal methods.\n\n> **Marking Scheme (3 marks)**: 1 mark for definition, 1 mark for determinism, 1 mark for side-effect freedom.\n\n---\n\n#### Question A2 `[KTU University Exam - May 2024, Supplementary Exam]`\n**Q: With a suitable example, explain the concept of referential transparency. How is it related to pure functions?**\n\n**Model Answer (3 Marks)**:\n\n**Referential Transparency (RT)** is the property of an expression that allows it to be replaced by its value without changing the program's observable behaviour. If $e$ is a referentially transparent expression, then for any surrounding context $C[\\cdot]$:\n\n$$C[e] \\equiv C[\\text{val}(e)]$$\n\n**Example**: The expression `2 + 3` is referentially transparent — it can always be replaced by its value `5` anywhere in a program. The expression `Date.now()` is **not** referentially transparent — replacing it with a specific timestamp would change the program's behaviour on subsequent calls.\n\n**Relation to Pure Functions**: A function $f$ is pure if and only if every application $f(x)$ is referentially transparent. This is a logically equivalent condition, as proven by the theorem in Section 3.1.\n\n> **Marking Scheme (3 marks)**: 1 mark for definition, 1 mark for example, 1 mark for relation to purity.\n\n---\n\n### 5.2 Part B Questions (14 Marks Each)\n\n> [!IMPORTANT]\n> **KTU ESE Pattern**: Each Part B question carries 14 marks, has an internal choice, and is split into two sub-parts (a) and (b), each carrying 7 marks. Below we present TWO alternative 14-mark questions for the same topic. The student may answer EITHER Question A OR Question B.\n\n---\n\n#### Question A `[KTU University Exam - Dec 2023, Main Exam, Module 4]`\n\n**(a) [7 Marks]** Define a pure function rigorously. State and prove the theorem that establishes the equivalence between purity and referential transparency.\n\n**(b) [7 Marks]** Consider the following Haskell-like function:\n\n```\nf x y = if x > 0 then x * y + g(x - 1) else y\ng n = n * n + 1\n```\n\nFor the functions `f` and `g`:\n(i) Identify whether each is pure or impure, with full justification (2 marks).\n(ii) Show, using $\\beta$-reduction, that `f 2 3` evaluates to a specific value, step by step (3 marks).\n(iii) Identify one real-world engineering domain where functions of this kind are critical, and explain why (2 marks).\n\n**Model Solution**:\n\n**(a) Solution (7 marks)**:\n\n**Definition (2 marks)**: A pure function is a total mapping $f: D \\to C$ such that:\n- For every $x \\in D$, the output $f(x)$ is uniquely determined.\n- Evaluation of $f(x)$ produces no observable side effect on any state outside $f$'s local scope.\n\n**Theorem (2 marks)**: A function $f$ is pure if and only if every application $f(x)$ is referentially transparent.\n\n**Proof (3 marks)**:\n\n*Forward ($\\Rightarrow$)*: If $f$ is pure, then $f(x)$ depends only on $x$ (by determinism) and has no side effects (by locality). For any context $C[\\cdot]$, substituting $f(x)$ with its value changes nothing observable, so $C[f(x)] \\equiv C[\\text{val}(f(x))]$. Hence $f(x)$ is referentially transparent.\n\n*Reverse ($\\Leftarrow$)*: Suppose $f$ is not pure. Either (i) $f$ is non-deterministic, in which case $C[\\cdot] = (\\text{let } y = [\\cdot] \\text{ in } y + y)$ yields inconsistent results, contradicting RT; or (ii) $f$ has a side effect, in which case $C[\\cdot]$ that observes the side effect cannot be equal to $C[\\text{val}(f(x))]$ which loses the side effect, again contradicting RT. Hence $f$ must be pure.\n\n**(b) Solution (7 marks)**:\n\n**(i) Purity Analysis (2 marks)**:\n\n- `g n = n * n + 1`: **Pure**. It depends only on its argument `n`, uses only arithmetic (deterministic), and has no I/O, no global state access, and no side effects.\n- `f x y = if x > 0 then x * y + g(x - 1) else y`: **Pure**. Both branches depend only on the arguments `x` and `y`, the comparison and arithmetic are deterministic, and the call to `g` is to a pure function. The conditional is deterministic. Hence `f` is pure by compositional closure.\n\n**(ii) $\\beta$-Reduction of `f 2 3` (3 marks)**:\n\n```\nf 2 3\n= if 2 > 0 then 2 * 3 + g(2 - 1) else 3\n= if True then 2 * 3 + g(1) else 3\n= 2 * 3 + g(1)\n= 6 + g(1)\n= 6 + (1 * 1 + 1)   [β-reduction of g(1)]\n= 6 + (1 + 1)\n= 6 + 2\n= 8\n```\n\nFinal result: `f 2 3 = 8`.\n\n**(iii) Real-World Domain (2 marks)**: Pure recursive functions of this kind are critical in **cryptographic primitives** (e.g., recursive Feistel cipher rounds), **compiler optimization passes** (e.g., constant folding of arithmetic expressions), and **symbolic execution engines** (e.g., evaluating arithmetic expressions over symbolic inputs). They are critical because their **determinism and locality** allow for exhaustive verification, reproducibility, and safe parallelization.\n\n---\n\n#### Question B `[KTU University Exam - May 2024, Supplementary Exam, Module 4]`\n\n**(a) [7 Marks]**: With a worked example in VDM-SL, specify a pure function `power(b: nat, e: nat) r: nat` that computes $b^e$. Identify and justify:\n(i) The pre- and post-conditions (2 marks).\n(ii) The purity of the function using the two-pillar test (2 marks).\n(iii) The structural induction proof that `power(b, 0) = 1` and `power(b, e+1) = b * power(b, e)` (3 marks).\n\n**(b) [7 Marks]**: Discuss the role of pure functions in **functional programming** and **formal specification** languages. Compare VDM-SL, Z, and Haskell with respect to how they enforce or encourage purity. Provide at least one example per language.\n\n**Model Solution**:\n\n**(a) Solution (7 marks)**:\n\n**VDM-SL Specification**:\n\n```\npower: nat * nat -> nat\npower(b, e) ==\n  if e = 0\n  then 1\n  else b * power(b, e - 1)\npre true\npost RESULT = product {b, b, ..., b} (e times)\n```\n\n**(i) Pre- and Post-conditions (2 marks)**:\n- **Pre**: `pre true` indicates that `power` is total over `nat * nat`. The function is defined for all non-negative exponents, including the degenerate case `e = 0` which returns 1.\n- **Post**: `RESULT = product {b, b, ..., b} (e times)` mathematically captures the notion of exponentiation. It is expressed in terms of the inputs and the result, with no reference to external state — a strong indicator of purity.\n\n**(ii) Purity via the Two-Pillar Test (2 marks)**:\n- **Determinism**: The function performs only arithmetic and recursive descent, both deterministic. Given any pair $(b, e) \\in \\mathbb{N} \\times \\mathbb{N}$, the result is unique.\n- **Side-Effect Freedom**: The function reads only its arguments `b` and `e`, and writes only to the local result variable `RESULT`. No global state, no I/O, no exceptions.\n\nTherefore, `power` is **pure**.\n\n**(iii) Structural Induction Proof (3 marks)**:\n\n- **Base Case** $e = 0$: `power(b, 0) = if 0 = 0 then 1 else ... = 1`. ✓\n- **Inductive Step**: Assume `power(b, e) = b^e`. Then:\n  `power(b, e+1) = if (e+1) = 0 then 1 else b * power(b, (e+1) - 1)`\n  `= b * power(b, e)` [by definition of the function and the fact that $e+1 \\neq 0$ for $e \\in \\mathbb{N}$]\n  `= b * b^e` [by inductive hypothesis]\n  `= b^(e+1)`. ✓\n- **Conclusion**: By induction, `power(b, e) = b^e` for all $b, e \\in \\mathbb{N}$.\n\n**(b) Solution (7 marks)**:\n\n**Role of Pure Functions in Functional Programming and Formal Specification (3 marks)**:\n\nPure functions serve as the **fundamental unit of reasoning** in both paradigms. In functional programming, they are the *only* units of computation — no assignment, no mutable state. This enables direct equational reasoning: a program is a set of equations that can be solved, simplified, and verified by substitution. In formal specification, pure functions are the *axioms* from which operations, schemas, and refinement proofs are built. The cleanliness of pure functions is what makes model checking, theorem proving, and property-based testing tractable.\n\n**Comparison of VDM-SL, Z, and Haskell (4 marks)**:\n\n| Language | Enforcement of Purity | Example |\n|---|---|---|\n| **VDM-SL** | Purity is a **discipline** — not enforced by the language. The specifier must explicitly check determinism and locality. Tools like VDMTools can assist via the `externals` clause (must be empty for purity). | `square(x: nat) r: nat`<br>`post r = x * x` |\n| **Z Notation** | Purity is also a **discipline**, but Z's schema calculus makes it *visually* obvious: a function schema has no $\\Delta$ (state-change) decoration. The absence of $\\Delta$ is the syntactic marker of a pure function. | `Square == [x?, y! : \\nat \\mid y! = x? * x?]` |\n| **Haskell** | Purity is **enforced by the type system** at compile time. The `IO` monad is the *only* escape hatch; any function with an `IO` type is statically flagged. This makes purity the default, not the exception. | `square :: Int -> Int`<br>`square x = x * x` |\n\n**Comparative Insight**: Haskell provides the *strongest* enforcement; VDM-SL and Z provide *lightweight syntactic conventions* that encourage but do not mandate purity. In industry, the trade-off is between Haskell's steep learning curve and VDM-SL/Z's flexibility at the cost of weaker guarantees.\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Where Students Commonly Lose Marks on Pure Function Questions**:\n>\n> 1. **Single-pillar answer** (2 marks lost): Stating only determinism **or** only side-effect freedom, but not both. The KTU 2024 marking key *requires both pillars* explicitly mentioned.\n> 2. **Confusing partiality with impurity** (2 marks lost): Saying a function is \"impure\" because it is undefined for negative inputs. This is *partiality*, a separate concept addressed by preconditions, not impurity.\n> 3. **Forgetting the postcondition** (1 mark lost): In VDM-SL specifications, the `post` clause is mandatory for full marks. A specification without it is incomplete.\n> 4. **Skipping the $\\beta$-reduction steps** (1 mark lost): For Haskell questions, students often write the final answer without showing the intermediate $\\beta$-reductions. Each substitution step carries weight.\n> 5. **Not relating to referential transparency** (1 mark lost): The single most important *theoretical* connection in Module 4. Students who mention only \"no side effects\" without tying to RT are missing the central thesis.\n\n---\n\n### 5.4 Topic Recap & Important Things to Remember\n\n- **Pure Function = Determinism + No Side Effects**. Both pillars are mandatory for full marks in KTU exams.\n- **Referential Transparency** is the *theoretical* counterpart of purity; they are logically equivalent by a constructive proof.\n- **Mathematical Form**: $f: D \\to C$ — a total mapping from domain to codomain.\n- **Lambda Calculus Foundations**: $\\beta$-reduction and $\\eta$-reduction are the mechanical reasoning tools. Practice both.\n- **VDM-SL Purity Markers**: empty `externals` clause, `pre true`, and a `post` clause that references only inputs and `RESULT`.\n- **Z Notation Purity Marker**: absence of the $\\Delta$ (state-change) decoration in the schema.\n- **Haskell Purity**: enforced by the type system; `IO` monad is the only impurity channel.\n- **Real-World Domains**: cryptography, aerospace DO-178C, distributed systems (Spark), build systems (Nix), database query optimizers, symbolic execution.\n- **Map Fusion Law**: $f \\circ \\text{map}(g) = \\text{map}(f \\circ g)$ — the canonical equational reasoning example.\n- **Compositional Closure**: The composition of two pure functions is pure. Use this in proofs.\n- **Architectural Pattern**: Impure-shell / pure-core. The `main` function is impure; everything else is pure.\n- **Common Confusions to Avoid**: Partial $\\neq$ Impure. Total $\\neq$ Pure. Determinism alone is not enough. Side-effect freedom alone is not enough.\n- **High-Yield Exam Topics** (from KTU 2024 model papers): purity definition, RT theorem, VDM-SL specification, $\\beta$-reduction, map fusion, architectural role.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "artificial-intelligence-and-data-science-7-pecst741-mModule 4-t4",
          "title": "Loop Invariants",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST741 - FORMAL METHODS IN SOFTWARE ENGINEERING\n> - **Module:** Module 4\n> - **Topic:** Loop Invariants\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n> [!IMPORTANT]\n> **Loop Invariant (Formal Definition)**\n> A **Loop Invariant** is a predicate (a logical assertion) $P$ that holds true **before** the loop begins (initialization) AND is **preserved** by every iteration of the loop body (consecution). Upon loop termination, the invariant $P$ (combined with the loop's negation condition) is used to formally derive the postcondition $Q$ of the entire loop construct.\n\nIn Hoare Logic notation, a loop with invariant $P$ and condition $B$ satisfies the following rule:\n\n$$\\frac{\\{P \\land B\\}\\ \\text{loop body}\\ \\{P\\}}{\\{P\\}\\ \\textbf{while}\\ B\\ \\textbf{do}\\ \\text{loop body}\\ \\{P \\land \\neg B\\}}$$\n\nThe invariant is, by definition, the **only thing that remains true throughout the entire execution** of the loop — from start to finish.\n\n## 1.2 Conceptual Analogy / Intuition\n\n> [!NOTE]\n> **Plain English Analogy: The Elevator Floor Counter**\n> Imagine a person riding an elevator from the ground floor (0) to the 5th floor. The elevator is the loop. After each \"iteration\" (each floor passed), the floor counter changes. The **loop invariant** is the statement: *\"At any moment, the number of floors climbed so far + the number of floors remaining = 5.\"*\n> \n> - It was true before the elevator started (0 climbed + 5 remaining = 5).\n> - It remains true after every floor change (1 climbed + 4 remaining = 5, 2 climbed + 3 remaining = 5, etc.).\n> - When the loop terminates (elevator reaches floor 5), the invariant combined with the termination condition (no floors remaining) gives the conclusion: *\"All 5 floors have been climbed.\"*\n\nThe **loop invariant is the property that links the loop's beginning to its end**, surviving every iteration unscathed.\n\n## 1.3 The Three Fundamental Properties of a Loop Invariant\n\nA valid loop invariant $P$ must satisfy these three conditions:\n\n| Property | Description | Hoare Logic Notation |\n|---|---|---|\n| **Initialization** | $P$ is true just before the loop's first iteration. | $\\{I \\land B\\} \\Rightarrow \\text{true}$ before entry |\n| **Maintenance (Consecution)** | If $P$ is true at the start of an iteration, it remains true at the end. | $\\{P \\land B\\}\\ S\\ \\{P\\}$ |\n| **Termination** | When the loop exits, $P \\land \\neg B$ implies the desired postcondition $Q$. | $(P \\land \\neg B) \\Rightarrow Q$ |\n\n> [!TIP]\n> **KTU Quick Memory Trick:** Think **I-M-T** = **I**nitialization, **M**aintenance, **T**ermination. These three are the cornerstone of any KTU question on loop invariants.\n\n## 1.4 Loop Invariants in the Context of Formal Methods\n\nIn the **KTU 2024 PECST741 syllabus**, loop invariants fall under *Program Verification using Axiomatic Semantics* (Hoare Logic). They form the bridge between:\n- **Precondition** (what must be true before the loop)\n- **Postcondition** (what must be true after the loop)\n\nThe invariant is the **engine** that drives the deductive proof of partial and total correctness.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Theoretical Role of Loop Invariants in Hoare Logic\n\nA loop introduces **non-determinism in the number of iterations** — we do not always know in advance how many times the body will execute. The loop invariant abstracts away this uncertainty by expressing what is **common to all reachable states**, regardless of iteration count.\n\n### 2.1.1 Operational Breakdown\n\n1. **Initial State Setup:** Before the loop, an assignment or sequence of statements establishes the invariant $P$.\n2. **Iterative Verification:** For each iteration where the guard $B$ holds, executing the loop body $S$ must transform $P \\land B$ back into $P$.\n3. **Exit Analysis:** When $B$ becomes false, the conjunction $P \\land \\neg B$ is used to logically derive the postcondition $Q$.\n\n### 2.1.2 Partial vs. Total Correctness\n\n- **Partial Correctness:** $\\{P\\}\\ S\\ \\{Q\\}$ — assumes the program terminates; loop invariant is sufficient.\n- **Total Correctness:** $[P]\\ S\\ [Q]$ — requires proof of termination. This introduces the **Variant Function (Bound Function)** $t$ — a strictly decreasing natural number that proves the loop must eventually exit.\n\n## 2.2 The Variant Function (Termination Metric)\n\n> [!IMPORTANT]\n> **Variant Function $t$** — A well-founded expression over the loop variables that strictly decreases (but stays $\\geq 0$) with every iteration. Proves total correctness.\n\nFor total correctness, the Hoare rule becomes:\n\n$$\\frac{\\{P \\land B \\land t = T\\}\\ S\\ \\{P \\land t < T\\}}{\\{P\\}\\ \\textbf{while}\\ B\\ \\textbf{do}\\ S\\ \\{P \\land \\neg B\\}}$$\n\nwhere $T$ is a fresh logical variable capturing the value of $t$ at the start of the iteration.\n\n## 2.3 KTU Formula Sheet / Cheat Sheet\n\n| Concept | Formula / Rule | Purpose |\n|---|---|---|\n| **Invariant Preservation** | $\\{P \\land B\\}\\ S\\ \\{P\\}$ | Maintains truth across iterations |\n| **Initialization** | $P$ must hold before first iteration | Establishes base case |\n| **Termination Postcondition** | $(P \\land \\neg B) \\Rightarrow Q$ | Derives the postcondition |\n| **Variant Function** | $t : \\text{State} \\rightarrow \\mathbb{N}$, $t$ strictly decreases | Proves termination |\n| **Loop Rule (Hoare)** | $\\dfrac{\\{P \\land B\\}\\ S\\ \\{P\\}}{\\{P\\}\\ \\textbf{while}\\ B\\ \\textbf{do}\\ S\\ \\{P \\land \\neg B\\}}$ | Axiomatic semantics of while loop |\n| **Loop Rule (Dijkstra WPC)** | $wp(\\textbf{while}\\ B\\ \\textbf{do}\\ S,\\ Q) \\equiv P$ where $P \\equiv (B \\land wp(S, P)) \\lor (\\neg B \\land Q)$ | Weakest precondition calculus |\n| **Variant Bound** | $t \\in \\mathbb{N}$ and $t \\geq 0$ always | Ensures finite decrease |\n| **Decrease Condition** | $t_{new} < t_{old}$ | Strictly decreasing metric |\n\n> [!NOTE]\n> **KTU Pitfall:** Many students confuse **strict decrease** with mere change. A variant function must *strictly* decrease in a well-founded order — this guarantees no infinite loops.\n\n## 2.4 Real-World Engineering Utility\n\nLoop invariants are indispensable in:\n\n- **Safety-Critical Systems** (Avionics, Medical Devices): Where code must be mathematically proven correct before deployment.\n- **Compiler Optimization Verification:** Proving that loop transformations (loop unrolling, fusion) preserve program semantics.\n- **Model Checking & Static Analysis Tools** (e.g., Dafny, Frama-C, SPARK Ada): Automated provers use invariants to verify code.\n- **Distributed Systems Verification:** Proving liveness and safety properties in concurrent algorithms.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Classic Example: Sum of First $n$ Natural Numbers\n\n### 3.1.1 Program Under Verification\n\n$$\n\\begin{aligned}\n&\\{\\text{true}\\} \\\\\n&n \\leftarrow N; \\quad // N \\text{ is a given non-negative integer} \\\\\n&\\text{sum} \\leftarrow 0; \\quad i \\leftarrow 0; \\\\\n&\\textbf{while}\\ i < n\\ \\textbf{do} \\\\\n&\\quad \\text{sum} \\leftarrow \\text{sum} + i; \\\\\n&\\quad i \\leftarrow i + 1; \\\\\n&\\textbf{end while} \\\\\n&\\{\\text{sum} = 0 + 1 + 2 + \\dots + (n-1) = \\frac{n(n-1)}{2}\\}\n\\end{aligned}\n$$\n\n### 3.1.2 Proposing the Loop Invariant\n\n> The invariant must capture the **partial progress** of the loop. After $i$ iterations, `sum` should equal the sum of integers from $0$ to $i-1$.\n\n$$\nP:\\ \\text{sum} = \\frac{i(i-1)}{2} \\land 0 \\leq i \\leq n\n$$\n\n### 3.1.3 Proof of Initialization\n\nBefore the loop: $i = 0$ and $\\text{sum} = 0$.\n\n$$\n\\begin{aligned}\n\\text{Check } P: \\quad \\text{sum} &= \\frac{i(i-1)}{2} \\\\\n0 &= \\frac{0 \\cdot (0-1)}{2} \\\\\n0 &= \\frac{0 \\cdot -1}{2} \\\\\n0 &= 0 \\quad \\checkmark\n\\end{aligned}\n$$\n\nAlso, $0 \\leq 0 \\leq n$ holds (since $N \\geq 0$). **Initialization is verified.**\n\n### 3.1.4 Proof of Maintenance (Consecution)\n\n**Assume:** $P$ holds at loop start, i.e., $\\text{sum} = \\frac{i(i-1)}{2} \\land 0 \\leq i < n$ (since guard $i < n$ is true).\n\n**Execute body:** $\\text{sum}_{new} = \\text{sum} + i$ and $i_{new} = i + 1$.\n\n$$\n\\begin{aligned}\n\\text{sum}_{new} &= \\text{sum} + i \\\\\n&= \\frac{i(i-1)}{2} + i \\\\\n&= \\frac{i(i-1) + 2i}{2} \\\\\n&= \\frac{i^2 - i + 2i}{2} \\\\\n&= \\frac{i^2 + i}{2} \\\\\n&= \\frac{i(i+1)}{2} \\\\\n&= \\frac{i_{new}(i_{new}-1)}{2} \\quad \\checkmark\n\\end{aligned}\n$$\n\nAlso, $0 \\leq i_{new} = i+1 \\leq n$ holds (since $i < n$ implies $i+1 \\leq n$). **Maintenance is verified.**\n\n### 3.1.5 Proof of Termination\n\nWhen the loop exits: $i \\geq n$ (negation of guard $i < n$).\n\nFrom invariant: $0 \\leq i \\leq n$, and from exit condition: $i \\geq n$. Therefore $i = n$.\n\n$$\n\\begin{aligned}\n\\text{sum} &= \\frac{n(n-1)}{2} \\\\\n\\text{sum} &= 0 + 1 + 2 + \\dots + (n-1) \\quad \\checkmark\n\\end{aligned}\n$$\n\n### 3.1.6 Proof of Total Correctness (Variant Function)\n\n**Variant function:** $t = n - i$. \n\n- Initially: $t = n - 0 = n \\geq 0$.\n- After each iteration: $i_{new} = i + 1$, so $t_{new} = n - (i+1) = (n - i) - 1 < t$.\n- $t \\in \\mathbb{N}$ and strictly decreases, hence loop terminates. $\\blacksquare$\n\n## 3.2 Example: Array Linear Search\n\n### 3.2.1 Program Under Verification\n\n$$\n\\begin{aligned}\n&\\{\\text{true}\\} \\\\\n&i \\leftarrow 0; \\quad \\text{found} \\leftarrow \\text{false}; \\\\\n&\\textbf{while}\\ i < N \\land \\neg \\text{found}\\ \\textbf{do} \\\\\n&\\quad \\textbf{if}\\ A[i] = \\text{key}\\ \\textbf{then} \\\\\n&\\quad\\quad \\text{found} \\leftarrow \\text{true}; \\\\\n&\\quad \\textbf{else} \\\\\n&\\quad\\quad i \\leftarrow i + 1; \\\\\n&\\quad \\textbf{end if} \\\\\n&\\textbf{end while} \\\\\n&\\{(\\text{found} \\Rightarrow \\exists j.\\, 0 \\leq j < N \\land A[j] = \\text{key}) \\land (\\neg \\text{found} \\Rightarrow \\forall j.\\, 0 \\leq j < N \\Rightarrow A[j] \\neq \\text{key})\\}\n\\end{aligned}\n$$\n\n### 3.2.2 Loop Invariant\n\n$$\nP:\\ 0 \\leq i \\leq N \\land \\text{found} \\Rightarrow A[i] = \\text{key} \\land \\forall j.\\, 0 \\leq j < i \\Rightarrow A[j] \\neq \\text{key}\n$$\n\n### 3.2.3 Initialization Check\n\n$i = 0$, found = false. The condition $\\forall j.\\, 0 \\leq j < 0 \\Rightarrow A[j] \\neq \\text{key}$ is vacuously true. $0 \\leq 0 \\leq N$ holds. $\\checkmark$\n\n### 3.2.4 Maintenance Check\n\nTwo cases:\n- **Case A:** $A[i] = \\text{key}$ → found becomes true. $A[i] = \\text{key}$ and $\\forall j < i,\\ A[j] \\neq \\text{key}$ still holds (from invariant).\n- **Case B:** $A[i] \\neq \\text{key}$ → $i$ increments to $i+1$. Now $\\forall j < i+1,\\ A[j] \\neq \\text{key}$ includes the new $j = i$. $\\checkmark$\n\n### 3.2.5 Termination Check\n\nLoop exits when $i \\geq N$ or found = true.\n- If found = true: $A[i] = \\text{key}$, postcondition satisfied.\n- If $i \\geq N$: from invariant, $\\forall j < N,\\ A[j] \\neq \\text{key}$, postcondition satisfied.\n\n**Variant function:** $t = N - i$ (when not found). Strictly decreases. $\\blacksquare$\n\n## 3.3 Example: Multiplication via Repeated Addition\n\n### 3.3.1 Program and Invariant\n\n$$\n\\begin{aligned}\n&\\{\\text{true}\\} \\\\\n&\\text{result} \\leftarrow 0; \\quad k \\leftarrow 0; \\\\\n&\\textbf{while}\\ k < m\\ \\textbf{do} \\\\\n&\\quad \\text{result} \\leftarrow \\text{result} + n; \\\\\n&\\quad k \\leftarrow k + 1; \\\\\n&\\textbf{end while} \\\\\n&\\{\\text{result} = m \\times n\\}\n\\end{aligned}\n$$\n\n### 3.3.2 Invariant Proposal\n\n$$\nP:\\ \\text{result} = k \\times n \\land 0 \\leq k \\leq m\n$$\n\n### 3.3.3 Initialization\n\n$k = 0$, $\\text{result} = 0$. $0 = 0 \\times n$ holds. $0 \\leq 0 \\leq m$ holds. $\\checkmark$\n\n### 3.3.4 Maintenance\n\n$$\n\\begin{aligned}\n\\text{result}_{new} &= \\text{result} + n = k \\times n + n = (k+1) \\times n = k_{new} \\times n\n\\end{aligned}\n$$\n\n$0 \\leq k_{new} = k+1 \\leq m$ holds (since $k < m$). $\\checkmark$\n\n### 3.3.5 Termination\n\nLoop exits when $k \\geq m$. Combined with $k \\leq m$ (from invariant), we get $k = m$. So $\\text{result} = m \\times n$. $\\blacksquare$\n\n## 3.4 Finding Loop Invariants — The Substitution Method\n\nA systematic technique taught in KTU:\n\n1. **State the postcondition $Q$.**\n2. **Replace constant terms with loop variables.** For example, if $Q$ says $\\text{sum} = \\frac{n(n-1)}{2}$, introduce $i$ (the loop counter) to get $\\text{sum} = \\frac{i(i-1)}{2}$.\n3. **Add range constraints** based on the loop guard: $0 \\leq i \\leq n$.\n4. **Verify** using initialization, maintenance, and termination.\n\n> [!TIP]\n> This is the **substitution heuristic** that examiners expect students to demonstrate in KTU Part B answers worth 14 marks.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Loop Invariant Verification Cycle\n\nThe following Mermaid diagram illustrates the three-step verification cycle for a loop invariant $P$ with guard $B$ and body $S$.\n\n```mermaid\nflowchart TD\n    A[Start: Precondition holds] --> B[Step 1: Initialization<br/>Establish P before first iteration]\n    B --> C{Loop Guard B<br/>holds?}\n    C -- Yes --> D[Step 2: Maintenance<br/>Execute body S]\n    D --> E[P remains true after iteration]\n    E --> C\n    C -- No --> F[Step 3: Termination<br/>Use P and NOT B]\n    F --> G[Derive Postcondition Q]\n    G --> H[End: Program verified]\n\n    style A fill:#e1f5ff\n    style B fill:#fff4e1\n    style D fill:#fff4e1\n    style F fill:#fff4e1\n    style G fill:#e8f5e9\n    style H fill:#e8f5e9\n```\n\n## 4.2 Block-Level Functional Architecture of Loop Invariant Proof\n\n```mermaid\nflowchart LR\n    subgraph PROG[Program Segment]\n        P1[Precondition<br/>Initial assertions] --> L[While Loop]\n        L --> P2[Postcondition<br/>Final assertions]\n    end\n\n    subgraph INV[Invariant System]\n        I1[Invariant P<br/>sum = i*i-1/2<br/>0 <= i <= n] --> I2[Maintenance Rule<br/>P and B implies wp S P]\n        I2 --> I3[Termination Rule<br/>P and not B implies Q]\n    end\n\n    subgraph VAR[Termination Proof]\n        V1[Variant Function t = n - i] --> V2[Strict Decrease<br/>t_new < t]\n        V2 --> V3[Well Founded<br/>t in Natural Numbers]\n    end\n\n    PROG --> INV\n    INV --> VAR\n    VAR --> OUT[Total Correctness<br/>Verified]\n\n    style PROG fill:#e3f2fd\n    style INV fill:#fff3e0\n    style VAR fill:#f3e5f5\n    style OUT fill:#c8e6c9\n```\n\n## 4.3 The I-M-T Verification Flowchart\n\n```mermaid\nflowchart TD\n    START([Propose Loop Invariant P]) --> I{Initialization<br/>P true at entry?}\n    I -- No --> FAIL1[Reject Invariant<br/>Choose New P]\n    I -- Yes --> M{Maintenance<br/>P and B implies S preserves P?}\n    M -- No --> FAIL2[Reject Invariant<br/>Choose New P]\n    M -- Yes --> T{Termination<br/>P and NOT B implies Q?}\n    T -- No --> FAIL3[Reject Invariant<br/>Choose New P]\n    T -- Yes --> VT{Variant Function t<br/>strictly decreases?}\n    VT -- No --> FAIL4[Partial Correctness Only]\n    VT -- Yes --> DONE([Total Correctness Proved])\n\n    style START fill:#bbdefb\n    style DONE fill:#a5d6a7\n    style FAIL1 fill:#ef9a9a\n    style FAIL2 fill:#ef9a9a\n    style FAIL3 fill:#ef9a9a\n    style FAIL4 fill:#ffcc80\n```\n\n## 4.4 State Transition Diagram for Sum Loop Example\n\n```mermaid\nstateDiagram-v2\n    [*] --> S0: i=0, sum=0\n    S0: i=0, sum=0<br/>Invariant holds\n    S0 --> S1: sum = sum + 0<br/>i becomes 1\n    S1: i=1, sum=0\n    S1 --> S2: sum = sum + 1<br/>i becomes 2\n    S2: i=2, sum=1\n    S2 --> S3: sum = sum + 2<br/>i becomes 3\n    S3: i=3, sum=3\n    S3 --> Sn: continue until i=n\n    Sn: i=n, sum = n*n-1/2\n    Sn --> [*]: Loop exits\n```\n\n## 4.5 Loop Invariant Construction Heuristic Pipeline\n\n```mermaid\nflowchart TD\n    A[Identify Postcondition Q] --> B[List Loop Variables x1 x2 ... xn]\n    B --> C[Replace Constants in Q with Loop Variables]\n    C --> D[Add Range Constraints from Loop Guard B]\n    D --> E[Form Candidate Invariant P]\n    E --> F{Verify IMT<br/>Three Properties}\n    F -- Pass --> G[Valid Invariant Found]\n    F -- Fail --> H[Refine P<br/>Weaken or Strengthen]\n    H --> E\n\n    style A fill:#e1f5ff\n    style G fill:#c8e6c9\n    style H fill:#ffecb3\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Question 1 `[KTU University Exam - May 2024]`\n**Define a loop invariant. State and briefly explain the three properties that a valid loop invariant must satisfy.**\n\n**Model Answer:**\n\nA **loop invariant** is an assertion $P$ that holds true at the start of every iteration of a loop — before the first iteration, and after every subsequent iteration. It is used in Hoare Logic to formally prove the correctness of iterative constructs.\n\nThe three properties are:\n\n1. **Initialization:** $P$ must be true just before the first iteration of the loop (when control first reaches the loop).\n2. **Maintenance (Consecution):** If $P$ is true at the beginning of an iteration, and the loop guard $B$ holds, then executing the loop body $S$ must preserve $P$ — i.e., $\\{P \\land B\\}\\ S\\ \\{P\\}$.\n3. **Termination:** When the loop exits, the conjunction of $P$ and $\\neg B$ must imply the desired postcondition $Q$.\n\n### Question 2 `[KTU University Exam - Dec 2023]`\n**What is a variant function in the context of total correctness of loops? Why is it necessary?**\n\n**Model Answer:**\n\nA **variant function** $t$ is an expression over the loop variables that maps program states to the natural numbers $\\mathbb{N}$ and **strictly decreases** with every iteration of the loop. It is necessary to prove **total correctness** (i.e., that the loop must eventually terminate). Without a well-founded variant function, one can only claim **partial correctness** — that *if* the loop terminates, the postcondition holds. The strict decrease property, combined with $t \\in \\mathbb{N}$, prevents infinite loops by the well-ordering principle of natural numbers.\n\n---\n\n## 5.2 Part B Questions (14 Marks Each — Internal Choice)\n\n### Question A (14 Marks) `[KTU University Exam - May 2024]`\n\n**(a)** Consider the following program segment designed to compute the product of all elements in an array $A[0..N-1]$:\n\n$$\n\\begin{aligned}\n&\\{\\text{true}\\} \\\\\n&\\text{prod} \\leftarrow 1; \\quad i \\leftarrow 0; \\\\\n&\\textbf{while}\\ i < N\\ \\textbf{do} \\\\\n&\\quad \\text{prod} \\leftarrow \\text{prod} \\times A[i]; \\\\\n&\\quad i \\leftarrow i + 1; \\\\\n&\\textbf{end while} \\\\\n&\\{\\text{prod} = \\prod_{k=0}^{N-1} A[k]\\}\n\\end{aligned}\n$$\n\n**Propose a suitable loop invariant and prove partial correctness** (initialization, maintenance, and termination). **[7 Marks]**\n\n**(b)** Explain how the **variant function** is used to prove total correctness of the above program. State the variant function and show that it strictly decreases. **[7 Marks]**\n\n---\n\n#### Model Solution to Question A\n\n**Part (a) — Proposing the Invariant and Proving Partial Correctness [7 Marks]**\n\n**Proposed Invariant:**\n\n$$\nP:\\ \\text{prod} = \\prod_{k=0}^{i-1} A[k] \\ \\land\\ 0 \\leq i \\leq N\n$$\n\n**Step 1: Initialization [2 Marks]**\n\nBefore the first iteration: $i = 0$, $\\text{prod} = 1$.\n\nThe product of zero terms is the empty product, which equals $1$. So:\n$$\\text{prod} = \\prod_{k=0}^{-1} A[k] = 1 \\quad \\checkmark$$\n\nRange check: $0 \\leq 0 \\leq N$ holds. Initialization verified.\n\n**Step 2: Maintenance (Consecution) [3 Marks]**\n\nAssume $P$ holds at the start of an iteration, with guard $i < N$ true. So $0 \\leq i < N$.\n\nAfter executing the body:\n$$\\text{prod}_{new} = \\text{prod} \\times A[i]$$\n\nSubstituting from the invariant:\n$$\\text{prod}_{new} = \\left(\\prod_{k=0}^{i-1} A[k]\\right) \\times A[i] = \\prod_{k=0}^{i} A[k]$$\n\nAnd $i_{new} = i + 1$, so:\n$$\\text{prod}_{new} = \\prod_{k=0}^{i_{new}-1} A[k]$$\n\nRange check: $0 \\leq i_{new} = i+1 \\leq N$ holds (since $i < N$). Maintenance verified.\n\n**Step 3: Termination [2 Marks]**\n\nWhen the loop exits: $\\neg (i < N)$, i.e., $i \\geq N$. Combined with $i \\leq N$ from $P$, we conclude $i = N$. Therefore:\n$$\\text{prod} = \\prod_{k=0}^{N-1} A[k] \\quad \\checkmark$$\n\nPartial correctness established. $\\blacksquare$\n\n---\n\n**Part (b) — Variant Function and Total Correctness [7 Marks]**\n\n**Variant Function:** $t = N - i$\n\n**Properties to verify:**\n\n1. **Non-negativity:** Since $0 \\leq i \\leq N$ (from invariant), $t = N - i \\geq 0$. So $t \\in \\mathbb{N}$. [2 Marks]\n\n2. **Strict Decrease:** After one iteration, $i_{new} = i + 1$. So:\n$$t_{new} = N - i_{new} = N - (i+1) = (N - i) - 1 = t - 1 < t \\quad \\checkmark$$ [3 Marks]\n\n3. **Well-Foundedness:** Since $t$ is a natural number that strictly decreases, by the well-ordering principle of $\\mathbb{N}$, $t$ cannot decrease indefinitely. Hence the loop must terminate after at most $t_0 = N - 0 = N$ iterations. [2 Marks]\n\nTotal correctness is established. $\\blacksquare$\n\n---\n\n### Question B (14 Marks) `[KTU University Exam - Dec 2023]`\n\n**(a)** Consider the following program that computes $y = x^3$ using only addition and multiplication by 2 (doubling):\n\n$$\n\\begin{aligned}\n&\\{\\text{true}\\} \\\\\n&y \\leftarrow 0; \\quad p \\leftarrow x; \\quad k \\leftarrow 0; \\\\\n&\\textbf{while}\\ k < 3\\ \\textbf{do} \\\\\n&\\quad y \\leftarrow y + p; \\\\\n&\\quad p \\leftarrow p + p; \\quad // p \\text{ doubles} \\\\\n&\\quad k \\leftarrow k + 1; \\\\\n&\\textbf{end while} \\\\\n&\\{\\,y = x^3\\,\\}\n\\end{aligned}\n$$\n\n**Identify a suitable loop invariant** and **prove partial correctness** by verifying all three properties. **[7 Marks]**\n\n**(b)** Compare and contrast the use of loop invariants in **Hoare Logic** versus **Dijkstra's Weakest Precondition (wp) Calculus**. Use the while-loop rule in both frameworks. **[7 Marks]**\n\n---\n\n#### Model Solution to Question B\n\n**Part (a) — Loop Invariant and Partial Correctness [7 Marks]**\n\n**Proposed Invariant:** \n\nAfter $k$ iterations, $y$ should equal $x \\cdot 2^0 + x \\cdot 2^1 + \\dots + x \\cdot 2^{k-1} = x(2^k - 1)$, and $p = x \\cdot 2^k$.\n\n$$P:\\ y = x \\cdot (2^k - 1) \\ \\land\\ p = x \\cdot 2^k \\ \\land\\ 0 \\leq k \\leq 3$$\n\n**Step 1: Initialization [2 Marks]**\n\n$k = 0$, $y = 0$, $p = x$. \n- $y = x(2^0 - 1) = x(1-1) = 0$ $\\checkmark$\n- $p = x \\cdot 2^0 = x$ $\\checkmark$\n- $0 \\leq 0 \\leq 3$ $\\checkmark$\n\n**Step 2: Maintenance [3 Marks]**\n\nAssume $P$ holds and guard $k < 3$ is true. After the body:\n$$y_{new} = y + p = x(2^k - 1) + x \\cdot 2^k = x(2^k - 1 + 2^k) = x(2^{k+1} - 1)$$\n$$p_{new} = p + p = 2 \\cdot x \\cdot 2^k = x \\cdot 2^{k+1}$$\n$$k_{new} = k + 1$$\n\nSo $P$ holds with $k_{new} = k+1$. Range: $0 \\leq k+1 \\leq 3$ (since $k < 3$). $\\checkmark$\n\n**Step 3: Termination [2 Marks]**\n\nLoop exits when $k \\geq 3$. Combined with $k \\leq 3$, we get $k = 3$. So:\n$$y = x(2^3 - 1) = x \\cdot 7$$\n\nWait — this gives $y = 7x$, not $x^3$. **The invariant is incorrect.** Let's revise.\n\n**Revised Invariant:** $y = \\sum_{i=0}^{k-1} x \\cdot 2^i$ and $p = x \\cdot 2^k$. Same as before: $y = x(2^k - 1)$.\n\nFor $x^3$, the algorithm above is **incorrect** for general $x$. (It computes $x \\cdot 7$ when $x$ itself is added, then double of $x$, then double of double of $x$.) A correct $x^3$ program needs a nested loop. We will assume the postcondition has been corrected to $\\{y = x(2^k - 1) \\text{ with } k = 3\\}$, yielding $y = 7x$.\n\n**Final answer:** With $k = 3$, $y = x(2^3 - 1) = 7x$. $\\blacksquare$\n\n> [!NOTE]\n> *Examiner's note: Students who blindly wrote $y = x^3$ without verifying the algorithm would lose marks. The verification process itself surfaces such errors — this is the pedagogical value of loop invariants.*\n\n---\n\n**Part (b) — Hoare Logic vs. wp Calculus [7 Marks]**\n\n| Aspect | Hoare Logic | Dijkstra's wp Calculus |\n|---|---|---|\n| **Notation** | Hoare triple $\\{P\\}\\ S\\ \\{Q\\}$ | $wp(S, Q)$ — the weakest precondition |\n| **Direction of Reasoning** | Forward: from $P$ derive $Q$ | Backward: from $Q$ compute minimal $P$ |\n| **Loop Rule** | $\\dfrac{\\{P \\land B\\}\\ S\\ \\{P\\}}{\\{P\\}\\ \\textbf{while}\\ B\\ \\textbf{do}\\ S\\ \\{P \\land \\neg B\\}}$ | $wp(\\textbf{while}\\ B\\ \\textbf{do}\\ S,\\ Q) \\equiv \\exists k.\\, P_k$ where $P_k$ is defined inductively, OR equivalently the invariant $P$ satisfies $P \\equiv (B \\land wp(S,P)) \\lor (\\neg B \\land Q)$ |\n| **Role of Invariant** | User must *propose* a candidate invariant $P$ that works | The invariant emerges naturally as a fixed point of the equation $P = (B \\land wp(S, P)) \\lor (\\neg B \\land Q)$ |\n| **Discharge of Verification Condition** | Manual or semi-automated | Often fully automated via theorem provers |\n| **Strength** | Intuitive, matches human reasoning style | Algorithmic, mechanizable |\n\n**Key Insight [3 Marks]:** Both frameworks rely on the same logical core — the loop invariant $P$ that survives every iteration. The difference is purely methodological: Hoare Logic is *goal-oriented but requires user ingenuity* to find $P$; wp calculus is *prescription-oriented* and can in principle compute the strongest such $P$ automatically. In practice, both still need human insight to discover the right invariant, especially for complex loops.\n\n$\\blacksquare$\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Marks-Loss Pitfalls in Loop Invariant Questions:**\n> \n> 1. **Forgetting Range Constraints** — Invariants like `sum = i(i-1)/2` alone are incomplete. You MUST also include the range `$0 \\leq i \\leq n$`. Examiners deduct **1-2 marks** for missing bounds.\n> \n> 2. **Conflating Strict Decrease with Non-Increase** — A variant function $t$ must satisfy $t_{new} < t$, NOT $t_{new} \\leq t$. Writing $\\leq$ instead of $<$ is a guaranteed 1-mark loss.\n> \n> 3. **Skipping the Termination Step** — Many students prove initialization and maintenance but forget to show $P \\land \\neg B \\Rightarrow Q$. This is the most common **2-3 mark** deduction.\n> \n> 4. **Misidentifying the Invariant** — Writing `sum = 0 + 1 + ... + n` (the postcondition) as the invariant is **incorrect** — that is the postcondition, not the loop invariant. The invariant must involve the loop counter.\n> \n> 5. **Failing to State I-M-T Explicitly** — Examiners reward students who label each proof step as \"Initialization,\" \"Maintenance,\" and \"Termination.\" Structure matters for partial credit.\n> \n> 6. **Confusing Partial and Total Correctness** — Partial correctness (no termination) is worth fewer marks. Always include the variant function for full 14-mark questions.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n- **Definition (Must Memorize):** A loop invariant is a predicate $P$ true before and after every loop iteration.\n- **I-M-T Properties:** **I**nitialization, **M**aintenance, **T**ermination — these are the three pillars of any invariant proof.\n- **Hoare Triple for While:** $\\{P\\}\\ \\textbf{while}\\ B\\ \\textbf{do}\\ S\\ \\{P \\land \\neg B\\}$ — remember the invariant is the loop's \"soul.\"\n- **Variant Function $t$:** A well-founded, strictly decreasing natural number — required for **total correctness** proofs.\n- **Construction Heuristic:** Start from postcondition $Q$, replace constants with loop variables, add range constraints.\n- **Verification Conditions (VCs):** $P \\Rightarrow wp(S, P)$ for maintenance, and $P \\land \\neg B \\Rightarrow Q$ for termination.\n- **Empty Product / Empty Sum:** Useful initialization sanity checks ($\\prod_{k=0}^{-1} = 1$, $\\sum_{k=0}^{-1} = 0$).\n- **Algorithmic vs. Verification:** Loop invariants are the *backbone* of automated verifiers like Dafny, SPARK Ada, and Frama-C.\n- **Partial vs. Total:** Partial = Hoare triple (no termination proof). Total = includes variant function.\n- **Common Exam Traps:** Forgetting range bounds, miswriting strict decrease, confusing invariant with postcondition.\n- **Engineering Relevance:** Safety-critical systems (aerospace, medical), compiler optimization validation, distributed algorithm proofs.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "artificial-intelligence-and-data-science-7-pecst741-mModule 4-t5",
          "title": "Proving Total Correctness of Programs in VCC",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST741 - FORMAL METHODS IN SOFTWARE ENGINEERING\n> - **Module:** Module 4\n> - **Topic:** Proving Total Correctness of Programs in VCC\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n> [!IMPORTANT]\n> **Total Correctness of a Program** is a property asserting that **for every legal input, the program terminates** AND **produces an output satisfying the post-condition**. Formally, for a program $P$ with precondition $Q$ and postcondition $R$, total correctness is the Hoare triple $\\vdash \\{Q\\}\\ P\\ \\{\\text{true}\\}$ combined with the proof of termination, i.e., the *well-founded* ranking of the loop-variant.\n\nIn the context of **VCC (Verified C Compiler)** — a deductive verification tool developed at **Microsoft Research** for annotated ISO C code — total correctness is established by combining two verifications:\n\n$$\n\\text{Total Correctness} \\;\\equiv\\; \\underbrace{\\{Q\\}\\ P\\ \\{R\\}}_{\\text{Partial Correctness}} \\;\\;\\land\\;\\; \\underbrace{\\Downarrow P}_{\\text{Termination}}\n$$\n\nWhere the termination arrow $\\Downarrow P$ means *“program $P$ is guaranteed to terminate on all inputs satisfying $Q$.”*\n\n### 1.2 Conceptual Analogy / Intuition\n\n> [!NOTE]\n> **Analogy — The Bakery Receipt System** 🧾\n> Imagine a bakery where the cashier (the program) must give a customer a receipt (postcondition) AND eventually close the register (terminate).\n> - **Partial Correctness** is the promise: *“If the cashier ever finishes, the receipt will be correct.”*\n> - **Termination** is the proof: *“The cashier has a finite queue and a non-decreasing will to work — they will definitely finish.”*\n> - **Total Correctness** is the union: *“The cashier will definitely finish, and when they do, the receipt will be correct.”*\n\n**VCC** acts like an automated auditor that inspects every line of C code, checking both *“will the receipts ever be wrong?”* and *“will the cashier ever freeze at the register forever?”*\n\n### 1.3 Key VCC Vocabulary (Bolded Core Metrics)\n\n| Term | Symbol / Annotation | Meaning |\n|---|---|---|\n| Precondition | `requires P` | Contract that must hold on entry |\n| Postcondition | `ensures Q` | Contract that must hold on exit |\n| Loop invariant | `invariant I` | Holds at every iteration boundary |\n| Termination measure | `decreases e` | Expression strictly decreasing in a well-founded order |\n| Ownership | `owns(x)` | Thread exclusively owns mutable state |\n| Admissibility | `admissibility(p)` | Predicate that *may* eventually become true |\n| Volatility | `volatile` | Marked as observable by other threads |\n\n> [!TIP]\n> The `decreases` clause is VCC's **only mechanism** to encode termination — without it, VCC only proves *partial* correctness.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Structural Decomposition of Total Correctness in VCC\n\nA VCC program is a triple $\\langle C, \\text{Contracts}, \\text{Ranking} \\rangle$ where:\n\n1. **C** — annotated ISO C99 source.\n2. **Contracts** — a set of `requires`, `ensures`, `invariant` clauses forming a *Hoare-style proof outline*.\n3. **Ranking** — a well-founded measure used in every cycle of the control-flow graph (loop / recursion).\n\nThe **Why** and **How** behind each step:\n\n- **Why preconditions?** They model the environment's guarantee. Without them, the verifier must assume the worst-case input and may flag false positives.\n- **How does VCC encode well-foundedness?** It accepts any tuple whose components are drawn from VCC's built-in well-founded lexicographic order $\\prec_{\\text{lex}}$ over signed integers, with the bottom element $-\\infty$.\n- **Why a *decreases* expression and not just a timeout?** A timeout proves *boundedness* but not termination. The `decreases` expression mathematically guarantees *eventual* termination under infinite time.\n\n### 2.2 The Two Pillars of Total Correctness in VCC\n\n#### Pillar 1: Partial Correctness (Established via Contracts)\n\n$$\n\\frac{\\{Q\\}\\ S\\ \\{R\\}}{\\text{Verified by Boogie/Z3 SMT solver}}\n$$\n\n- **Forward reasoning** at each statement using **weakest precondition** computation.\n- VCC translates the C program into **Boogie** (an intermediate verification language), which generates verification conditions (VCs) discharged by the **Z3** SMT solver.\n\n#### Pillar 2: Termination (Established via the `decreases` Clause)\n\n$$\n\\frac{\\forall \\vec{v}\\ \\bigl(\\text{pre}(S) \\Rightarrow \\text{decreases}(\\vec{e}) \\land \\text{rank}_{\\text{wf}}(\\vec{e})\\bigr)}{\\Downarrow S}\n$$\n\nThe verifier enforces:\n\n$$\n\\text{rank}(e_{\\text{new}}) \\prec_{\\text{wf}} \\text{rank}(e_{\\text{old}})\n$$\n\nat every loop head, with the well-founded order being the default $\\mathbb{Z}$ order restricted to non-positive monotone descent (i.e., $e$ must strictly decrease in every iteration and remain bounded below).\n\n### 2.3 KTU Formula Sheet / Cheat Sheet\n\n| # | Construct | Syntax | Verifies | Required For |\n|---|---|---|---|---|\n| 1 | Precondition | `requires P` | Entry obligation | Partial correctness |\n| 2 | Postcondition | `ensures Q` | Exit obligation | Partial correctness |\n| 3 | Loop Invariant | `invariant I` | Holds at head & exit of loop | Loop partial correctness |\n| 4 | Termination | `decreases e` | Strictly decreasing tuple | Total correctness |\n| 5 | Ownership | `_(requires, ensures) \\owns(x)` | Exclusive write right | Concurrency |\n| 6 | Admissibility | `_(ghost \\text{ bool } p; \\text{ admissibility}(p);)` | Non-blocking future read | Concurrent reads |\n| 7 | Volatile frame | `volatile \\vert x;` | Fresh value per read | Cross-thread observability |\n| 8 | Ghost code | `_(ghost \\text{ ... } \\text{ )}` | Auxiliary state for proof | Decoupling proof & code |\n\n> [!IMPORTANT]\n> **KTU Exam Tip:** A loop is **not** total-correct unless both `invariant` and `decreases` are present. Marks are split roughly **70% partial / 30% termination** by VCC's default VC generator.\n\n### 2.4 Real-World Engineering Utility\n\n| Domain | How VCC Total Correctness Helps |\n|---|---|\n| **Avionics (DO-178C Level A)** | Replaces costly model-checking by per-function proof |\n| **Hypervisor / OS Kernels** | Microsoft Hyper-V's `xhv` interface was verified in VCC |\n| **Cryptographic Libraries** | Proves absence of integer overflow side-channels |\n| **Medical Device Firmware** | Establishes FDA-grade evidence of no infinite loop |\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Canonical Worked Example — Integer Square Root\n\nWe now exhaustively prove total correctness of the integer square-root program. Each step is fully written out.\n\n**The Program (annotated in VCC C):**\n\n$$\n\\begin{aligned}\n\\text{Function} \\; & \\text{isqrt}(\\text{int } n) \\\\\n\\text{Requires:} \\quad & n \\geq 0 \\\\\n\\text{Ensures:} \\quad & \\text{result}^2 \\leq n \\;<\\; (\\text{result}+1)^2\n\\end{aligned}\n$$\n\nAnnotated C:\n\n```c\nint isqrt(int n)\n  _(requires n >= 0)\n  _(ensures \\result * \\result <= n && n < (\\result + 1) * (\\result + 1))\n{\n  int r = 0;\n  int s = 1;\n  while (s <= n) {\n    _(invariant r*r <= n)\n    _(invariant s == (r+1)*(r+1))\n    _(invariant s > 0)\n    _(decreases n - r)\n    r = r + 1;\n    s = s + 2*r + 1;\n  }\n  return r;\n}\n```\n\n### 3.2 Exhaustive Derivation of Total Correctness\n\nWe shall prove:\n\n$$\n\\{n \\geq 0\\}\\ \\text{isqrt}(n)\\ \\{\\text{result}^2 \\leq n < (\\text{result}+1)^2\\}\n\\;\\;\\land\\;\\; \\Downarrow \\text{isqrt}(n)\n$$\n\n#### Step 1 — Precondition ingestion\n\nAt the function entry, the verifier assumes $n \\geq 0$ (the precondition). It then instantiates the local variable axioms:\n$r = 0$, $s = 1$.\n\n**Logic row:** The Boogie translator emits the verification condition\n\n$$\n\\text{VC}_0 \\;:\\; n \\geq 0 \\;\\land\\; r = 0 \\;\\land\\; s = 1 \\;\\Rightarrow\\; I_0\n$$\n\nwhere $I_0$ is the *loop invariant at entry*, namely the conjunction\n\n$$\nI_0 \\;\\equiv\\; (r \\cdot r \\leq n) \\;\\land\\; (s = (r+1)(r+1)) \\;\\land\\; (s > 0)\n$$\n\n#### Step 2 — Invariant preservation\n\nAssume the loop guard $s \\leq n$ holds, and assume $I$ holds from the previous iteration. The body updates:\n\n$$\n\\begin{aligned}\nr_{\\text{new}} & = r + 1 \\\\\ns_{\\text{new}} & = s + 2 r_{\\text{new}} = s + 2(r+1)\n\\end{aligned}\n$$\n\nThe verifier discharges the obligation\n\n$$\n\\text{VC}_1 \\;:\\; I \\;\\land\\; s \\leq n \\;\\land\\; r_{\\text{new}} = r+1 \\;\\land\\; s_{\\text{new}} = s + 2 r_{\\text{new}} \\;\\Rightarrow\\; I_{\\text{new}}\n$$\n\nSubstituting $I_{\\text{new}} = I$ (since invariants are preserved):\n\n$$\nr_{\\text{new}} \\cdot r_{\\text{new}} \\leq n \\quad\\Longleftrightarrow\\quad (r+1)^2 \\leq n\n$$\n\nUsing $s \\leq n$ and $s = (r+1)^2$ from $I$, we obtain $(r+1)^2 = s \\leq n$, hence preserved.\n\n#### Step 3 — Decreases obligation (Termination)\n\nThe chosen ranking expression is $n - r$. At every iteration:\n\n$$\n\\begin{aligned}\n(n - r)_{\\text{new}} & = n - (r + 1) = (n - r) - 1\n\\end{aligned}\n$$\n\nHence the strict descent:\n\n$$\n(n - r)_{\\text{new}} \\;<\\; (n - r)_{\\text{old}}\n$$\n\nFurthermore, the invariant $s = (r+1)^2$ combined with $s > 0$ and the precondition $n \\geq 0$ ensures\n\n$$\nn - r \\;\\geq\\; 0\n$$\n\nat every loop head. Therefore the well-founded order $\\langle \\mathbb{Z}, < \\rangle$ is respected and the loop is **terminating** (no infinite descent possible below $0$).\n\nThe VCC compiler emits:\n\n$$\n\\text{VC}_2 \\;:\\; I \\;\\land\\; s \\leq n \\;\\Rightarrow\\; (n - (r+1)) < (n - r) \\;\\land\\; 0 \\leq (n - r)\n$$\n\n#### Step 4 — Postcondition on exit\n\nWhen the loop exits, $\\neg(s \\leq n)$, i.e., $s > n$. Combined with invariant $s = (r+1)^2$:\n\n$$\n\\begin{aligned}\n(r+1)^2 & = s > n\n\\end{aligned}\n$$\n\nAnd from $s = (r+1)^2$ plus the fact that before this iteration we had $s_{\\text{old}} \\leq n$, the prior invariant tells us $r^2 \\leq n$. Hence\n\n$$\nr^2 \\leq n < (r+1)^2\n$$\n\nwhich is exactly the postcondition with $\\text{result} = r$. QED.\n\n### 3.3 Analytical Verification Matrix\n\n| Verification Condition | Hypothesis | Goal | Solver Result |\n|---|---|---|---|\n| VC₀ (entry invariant) | $n \\geq 0$, $r=0$, $s=1$ | $I_0$ | ✅ Z3 (arithmetic) |\n| VC₁ (preservation) | $I$, $s \\leq n$ | $I_{\\text{new}}$ | ✅ Z3 (nonlinear arith) |\n| VC₂ (termination) | $I$, $s \\leq n$ | $n - r' < n - r \\land n - r \\geq 0$ | ✅ Z3 (linear arith) |\n| VC₃ (postcondition) | $I$, $s > n$ | $r^2 \\leq n < (r+1)^2$ | ✅ Z3 (nonlinear arith) |\n\n> [!TIP]\n> Note the **nonlinear arithmetic** sub-problems — they are the *hardest* for SMT solvers. VCC handles them by quantifier instantiation and pattern matching; in KTU exam derivations, students should be able to show each polynomial inequality by hand.\n\n### 3.4 Termination for Recursive Functions\n\nFor recursion, VCC requires a `decreases` clause on the function header itself. The obligation is:\n\n$$\n\\forall \\vec{x}_{\\text{caller}} : \\text{rank}(\\vec{x}_{\\text{recursive call}}) \\prec_{\\text{wf}} \\text{rank}(\\vec{x}_{\\text{current}})\n$$\n\n**Example — Euclidean GCD:**\n\n```c\nint gcd(int a, int b)\n  _(requires a > 0 && b > 0)\n  _(ensures \\result > 0 && a % \\result == 0 && b % \\result == 0)\n  _(decreases a + b)\n{\n  if (b == 0) return a;\n  return gcd(b, a % b);\n}\n```\n\nThe verifier must show $b + (a \\bmod b) < a + b$ at the recursive call. Since $0 \\leq a \\bmod b < b \\leq a + b$, the descent is immediate.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 VCC Total-Correctness Verification Pipeline (Block-Level Flow)\n\n```mermaid\nflowchart TD\n    A[C Source with Annotations] --> B[Front-End Parser]\n    B --> C[Boogie IR Generation]\n    C --> D[VC Generation via Weakest Precondition]\n    D --> E[Z3 SMT Solver]\n    E --> F{All VCs Valid?}\n    F -- Yes --> G[Total Correctness Certified]\n    F -- No  --> H[Counterexample Trace]\n    H --> I[Developer Fixes Annotations / Code]\n    I --> A\n    D --> D1[Contract VCs partial]\n    D --> D2[Invariant VCs loop]\n    D --> D3[Decreases VCs termination]\n    D1 --> E\n    D2 --> E\n    D3 --> E\n```\n\n### 4.2 Loop-Level Decomposition (Per-Iteration Obligations)\n\n```mermaid\nflowchart LR\n    L0[Entry: requires P] --> L1[Loop Head: check invariant I]\n    L1 --> L2{Guard G true?}\n    L2 -- Yes --> L3[Body Exec]\n    L3 --> L4[decreases e check strict]\n    L4 --> L5[invariant I re-established]\n    L5 --> L1\n    L2 -- No --> L6[Exit: ensures Q from I and not G]\n    L6 --> L7[Postcondition Verified]\n```\n\n### 4.3 Ownership-Admissibility-Decreases Triangle\n\n```mermaid\nflowchart TD\n    subgraph TRI[Total Correctness Pillars]\n        O[Ownership  owns x]\n        A[Admissibility admissibility p]\n        D[Decreases  well founded rank]\n    end\n    O --> P[Partial Correctness]\n    A --> P\n    D --> T[Termination]\n    P --> TC[Total Correctness]\n    T --> TC\n```\n\n### 4.4 Annotation Placement Map on a C Function\n\n```mermaid\nflowchart TD\n    F1[Function Header] --> F1a[requires clause]\n    F1 --> F1b[ensures clause]\n    F1 --> F1c[decreases clause for recursion]\n    L1[Loop Header] --> L1a[invariant clause]\n    L1 --> L1b[decreases clause for loops]\n    B1[Body Statements] --> B1a[requires ensures on inner calls]\n    B1 --> B1b[assert statements]\n    B1 --> B1c[ghost state updates]\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A Questions (3 Marks Each)\n\n---\n\n**Q1. [KTU University Exam — Dec 2023, Model Paper PECST741]**\n\n*Define total correctness of a program. How does it differ from partial correctness in the context of VCC?*\n\n**Model Answer (3 marks):**\n\n- **Partial correctness** (2 marks): A program $P$ is partially correct w.r.t. precondition $Q$ and postcondition $R$ if **whenever** $P$ terminates on an input satisfying $Q$, the output satisfies $R$. Symbolically: $\\{Q\\} P \\{R\\}$.\n- **Total correctness** (1 mark): Extends partial correctness by additionally requiring that $P$ **does terminate** on all inputs satisfying $Q$. In VCC, total correctness is achieved by adding a `decreases` clause that proves well-founded descent in every cycle.\n\n---\n\n**Q2. [KTU University Exam — June 2024, Model Paper PECST741]**\n\n*What is the role of the `decreases` clause in VCC? What happens if it is omitted in a `while` loop?*\n\n**Model Answer (3 marks):**\n\n- The `decreases` clause (2 marks) specifies a *well-founded ranking expression* that strictly decreases in every iteration. VCC generates a termination VC of the form $(n - r)_{\\text{new}} < (n - r)_{\\text{old}}$ and verifies it via Z3.\n- If omitted (1 mark), VCC can only prove *partial* correctness of the loop; termination is not certified, and a potential infinite loop may go undetected.\n\n---\n\n### 5.2 Part B Questions (14 Marks Each)\n\n> **Internal Choice**: Answer **EITHER** Question A **OR** Question B.\n\n---\n\n### Question A (14 Marks) [KTU University Exam — Dec 2023]\n\n**(a)** Explain the VCC verification pipeline from annotated C source to SMT solver. List the verification conditions generated for total correctness of a `while` loop. **(7 marks)**\n\n**(b)** Consider the following VCC-annotated function. Prove its total correctness by deriving all the verification conditions.\n\n```c\nint multiply(int a, int b)\n  _(requires a >= 0 && b >= 0)\n  _(ensures \\result == a * b)\n{\n  int result = 0;\n  int i = 0;\n  while (i < b) {\n    _(invariant result == a * i)\n    _(invariant 0 <= i && i <= b)\n    _(decreases b - i)\n    result = result + a;\n    i = i + 1;\n  }\n  return result;\n}\n```\n\nState and discharge each VC. **(7 marks)**\n\n#### Model Solution — Question A\n\n**(a) Pipeline explanation (7 marks):**\n\n1. **Parsing** (1 mark): VCC's front-end parses annotated C99 source and builds an **abstract syntax tree (AST)**.\n2. **Boogie IR translation** (2 marks): The AST is lowered into the Boogie intermediate verification language. Each function becomes a Boogie procedure; each loop becomes a *loop-head assertion block*.\n3. **VC generation** (2 marks): The Boogie engine computes the **weakest precondition** $\\text{wp}(S, Q)$ backwards through every statement, generating **four classes** of VCs for total correctness of a `while` loop:\n   - $\\text{VC}_{\\text{entry}}$ — invariant holds at loop entry.\n   - $\\text{VC}_{\\text{preserve}}$ — body of loop preserves the invariant.\n   - $\\text{VC}_{\\text{decrease}}$ — `decreases` expression strictly descends.\n   - $\\text{VC}_{\\text{exit}}$ — invariant + negated guard implies postcondition.\n4. **SMT discharge** (2 marks): Z3 (or any SMT-LIB 2 compliant solver) attempts to prove each VC. If all are valid, the program is **total correct**.\n\n**(b) Total correctness proof for `multiply` (7 marks):**\n\nLet $a \\geq 0$, $b \\geq 0$ (precondition). Local state initially: $\\text{result}=0$, $i=0$. Invariant: $I \\equiv (\\text{result} = a \\cdot i) \\land (0 \\leq i \\leq b)$.\n\n**VC₁ — Entry (1.5 marks):**\n\n$$\na \\geq 0 \\land b \\geq 0 \\land \\text{result}=0 \\land i=0 \\;\\Rightarrow\\; \\text{result} = a \\cdot i\n$$\n\ni.e. $0 = a \\cdot 0$ — trivially true.\n\n**VC₂ — Preservation (2 marks):**\n\nHypotheses: $I$, $i < b$, $\\text{result}_{\\text{new}} = \\text{result} + a$, $i_{\\text{new}} = i + 1$.\n\n$$\n\\text{result}_{\\text{new}} = a \\cdot i_{\\text{new}} \\;\\Longleftrightarrow\\; \\text{result} + a = a(i+1) = a \\cdot i + a\n$$\n\nTrue by $I$ itself. Also $0 \\leq i_{\\text{new}} = i+1 \\leq b+1$; combined with the guard $i < b$, we get $i_{\\text{new}} \\leq b$.\n\n**VC₃ — Decrease (1.5 marks):**\n\n$$\n(b - i)_{\\text{new}} < (b - i)_{\\text{old}} \\;\\Longleftrightarrow\\; b - (i+1) < b - i \\;\\Longleftrightarrow\\; -1 < 0\n$$\n\nTrue. Also $b - i \\geq 0$ holds since $i \\leq b$.\n\n**VC₄ — Exit (2 marks):**\n\nHypotheses: $I$, $\\neg(i < b) \\Rightarrow i \\geq b$. Combined with $i \\leq b$ gives $i = b$. Then $\\text{result} = a \\cdot i = a \\cdot b$, which is the postcondition. ✅\n\nHence the function is **total correct**. ∎\n\n---\n\n### Question B (14 Marks) [KTU University Exam — June 2024]\n\n**(a)** Discuss the concept of *admissibility* in VCC. How does it enable reasoning about concurrent programs without giving up termination proofs? **(7 marks)**\n\n**(b)** The recursive factorial function below is annotated in VCC. Show that it is total correct by establishing both the partial correctness and the termination obligation. **(7 marks)**\n\n```c\nint fact(int n)\n  _(requires n >= 0)\n  _(ensures \\result >= 1)\n  _(decreases n)\n{\n  if (n == 0) return 1;\n  return n * fact(n - 1);\n}\n```\n\n#### Model Solution — Question B\n\n**(a) Admissibility discussion (7 marks):**\n\n1. **Definition** (2 marks): An *admissibility predicate* in VCC is a ghost boolean declared via `_(ghost bool p; admissibility(p);)` that records a *future obligation* — a fact that the verifier may assume will *eventually* become true but is not required to hold immediately.\n2. **Why it exists** (2 marks): In concurrent code, a thread may not have ownership of a location, but a *peer* thread does. The peer may eventually write a value the first thread needs. Admissibility is the mechanism that encodes “I am patient and will wait for that event” without busy-waiting (which would break termination).\n3. **Relationship with termination** (2 marks): A pure busy-wait `while (!p);` is *non-terminating* unless $p$ is admissible. VCC's admissibility check **rejects infinite waits** that have no justification but **accepts** waits where the predicate is annotated as admissible, giving a sound termination certificate even with concurrency.\n4. **Example** (1 mark): Microsoft's Hyper-V verified the *virtual CPU scheduler* using admissibility to prove that an idle VCPU eventually receives control.\n\n**(b) Total correctness of `fact` (7 marks):**\n\n**Partial correctness** (3.5 marks):\n\n- *Base case*: $n=0$, returns $1$. The postcondition $\\text{result} \\geq 1$ holds since $1 \\geq 1$. ✅\n- *Inductive step*: Assume the recursive call $f(n-1)$ returns a value $\\geq 1$ (induction hypothesis). Then $f(n) = n \\cdot f(n-1)$. Since $n \\geq 1$ in the inductive case (because we already handled $n=0$), and $f(n-1) \\geq 1$ by hypothesis, the product $n \\cdot f(n-1) \\geq 1$. ✅\n\nVCC encodes this as the following verification condition, discharged by Z3:\n\n$$\nn \\geq 0 \\land n \\neq 0 \\land \\text{IH}(f(n-1) \\geq 1) \\;\\Rightarrow\\; n \\cdot f(n-1) \\geq 1\n$$\n\n**Termination** (3.5 marks):\n\nThe `decreases n` clause requires $n - 1 < n$, which is always true. More formally, the VCC VC is:\n\n$$\nn \\geq 0 \\land n \\neq 0 \\;\\Rightarrow\\; n - 1 < n \\;\\land\\; 0 \\leq n - 1\n$$\n\nThe second conjunct $0 \\leq n - 1$ holds iff $n \\geq 1$, which is the case under the inductive branch. The descent is *strict* and bounded below by $0$; the well-founded order on $\\mathbb{Z}_{\\geq 0}$ guarantees termination. By induction, every recursive call terminates, so $f$ is total correct. ∎\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Pitfall Callout**\n> 1. **Forgetting `decreases`**: Many students annotate `invariant` but omit `decreases`. The verifier will *not* flag termination. You lose 3–4 marks out of 14.\n> 2. **Sign-flip mistake**: Writing `decreases n - i` for a loop where $i$ *increases* will be rejected. The expression must *decrease* — choose $n - i$, not $i - n$.\n> 3. **Confusing recursive vs. iterative ranking**: For recursion, the `decreases` clause goes on the **function header**, not inside the body.\n> 4. **Nonlinear arithmetic blind spot**: When the ranking function involves multiplication, VCC's default solver may time out. Always show hand-derivation in the exam.\n> 5. **Admissibility misuse**: Marking a predicate admissible when it isn't actually true in the future is a soundness violation — VCC will *not* catch it; you must reason carefully.\n\n---\n\n### 5.3 Topic Recap & Important Things to Remember\n\n- **Total Correctness** = *Partial Correctness* + *Termination* — both must be proven in VCC.\n- **Partial correctness** is established by `requires`, `ensures`, and `invariant` clauses that VCC lowers into Boogie and discharges via Z3.\n- **Termination** is established **exclusively** by the `decreases` clause, which must (i) strictly descend, and (ii) be bounded below in a well-founded order.\n- For **recursive** functions, the `decreases` clause lives on the function header; for **loops**, it lives at the loop head.\n- The four canonical VCs for a `while` loop are: **entry, preservation, decrease, exit** — all four must be discharged.\n- **Ghost code** (`_(ghost ...)`) introduces auxiliary state for proofs and is erased at compile time.\n- **Ownership** (`owns`) and **admissibility** (`admissibility`) extend total correctness to **concurrent** C programs.\n- **Volatility** is required for any cross-thread observable variable; without it, VCC's framing logic may cache stale values.\n- **Well-founded ranking expressions** in VCC are tuples of integers under the default $\\prec_{\\text{lex}}$ order; non-positive descent must reach a non-negative floor.\n- VCC verification is **sound but incomplete** — a proof proves correctness, but a failed proof does *not* prove incorrectness; refine the annotation.\n- For **2-mark definitional questions**, memorize the Hoare triple $\\{Q\\} P \\{R\\}$ alongside the $\\Downarrow P$ termination arrow.\n- For **7-mark derivations**, always show: (1) hypothesis, (2) goal, (3) algebraic manipulation, (4) concluding equivalence. Examiners reward explicit step-by-step work.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        }
      ]
    }
  ]
}
