{
  "id": "pccsl607-computer-science-and-engineering-s7",
  "code": "PCCSL607",
  "name": "SYSTEMS LAB",
  "branchId": "computer-science-and-engineering",
  "semester": 7,
  "modules": [
    {
      "id": "mExperiment 2",
      "title": "Module Experiment 2",
      "topics": [
        {
          "id": "computer-science-and-engineering-7-pccsl607-mExperiment 2-t1",
          "title": "Design and implement a lexical analyzer using C language to recognize all valid tokens in the input program, ignoring redundant spaces, tabs, newlines, and comments",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL607 - SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Design and implement a lexical analyzer using C language to recognize all valid tokens in the input program, ignoring redundant spaces, tabs, newlines, and comments\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\nA **Lexical Analyzer** (also called a **Scanner** or **Lexer**) is the **first phase of a compiler** whose primary task is to read the input source program character-by-character, group them into **lexemes**, and produce a stream of **tokens** as output. Each token is a pair consisting of a **token name** and an **optional attribute value** (such as a pointer to the symbol table entry).\n\n> [!NOTE]\n> **KTU 2024 Definition (Aho-Sethi-Ullman Standard):**\n> A **Token** is a classification of a lexeme (e.g., *identifier*, *keyword*, *operator*). A **Lexeme** is the actual character sequence matched by a token pattern. A **Pattern** is the rule that describes the set of lexemes corresponding to a token.\n\nThe lexer must also perform **secondary tasks**:\n- Stripping out **comments** and **white spaces** (spaces, tabs, newlines).\n- Correlating **error messages** with the source program line numbers.\n- Communicating with the **Symbol Table Manager** to store identifiers.\n\n## 1.2 Conceptual Analogy / Intuition\n\nThink of a **lexical analyzer** as a **postal sorting clerk at a regional hub**.\n\nImagine you are a clerk handling thousands of letters. You do **not** read the entire content of every letter. Instead, you:\n1. Glance at the **envelope address** and **pincode** (these are like *identifiers* and *constants*).\n2. Notice **stamps and markings** (like *keywords* such as \"URGENT\" or \"REGISTERED\").\n3. Observe **special characters** like arrows and arrows on the envelope (like *operators* `+`, `-`, `*`, `/`).\n4. **Discard wrapping paper, extra spaces, and advertisements** in the envelope (like ignoring *whitespace* and *comments*).\n5. **Group the meaningful items** into standard categories and forward them to the next stage (the *parser*).\n\nThat is **exactly** what a lexical analyzer does with your C program. It does **not care about whether `int x = 5;` is a valid statement** — it just identifies `int` (keyword), `x` (identifier), `=` (operator), `5` (constant), `;` (symbol). Validation of structure is the **parser's** job.\n\n## 1.3 KTU-Standard Token Categories\n\n| # | Token Category | Examples | Pattern Rule |\n|---|---|---|---|\n| 1 | **Keywords** | `int`, `float`, `if`, `while`, `return` | Reserved words in the language |\n| 2 | **Identifiers** | `x`, `sum`, `_count`, `var123` | Letter or `_` followed by letters/digits/`_` |\n| 3 | **Constants / Numbers** | `123`, `45.67`, `0`, `9.8e3` | Numeric literal patterns |\n| 4 | **Operators** | `+`, `-`, `*`, `/`, `=`, `==`, `<=`, `++` | Mathematical/logical symbols |\n| 5 | **Special Symbols / Punctuators** | `;`, `,`, `(`, `)`, `{`, `}`, `[`, `]` | Punctuation characters |\n| 6 | **String Literals** | `\"hello\"`, `\"Kerala\"` | Anything enclosed in double quotes |\n| 7 | **Preprocessor Directives** | `#include`, `#define` | Lines beginning with `#` |\n\n> [!IMPORTANT]\n> **KTU 2024 High-Yield Point:** A common exam question asks *“What is the output of a lexical analyzer?”*. The correct answer is **a sequence of tokens (token-name, attribute-value) pairs**, **not** a parse tree or assembly code. Remembering this distinction can fetch full marks.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Position of Lexical Analyzer in the Compiler Pipeline\n\nThe compiler is divided into two major halves:\n\n$$\n\\boxed{\\text{Analysis Phase (Front-End)} \\;\\;|\\;\\; \\text{Synthesis Phase (Back-End)}}\n$$\n\nThe **lexical analyzer** is the **first stage** of the **Analysis Phase**.\n\n$$\n\\begin{aligned}\n\\text{Source Program} \\;\\longrightarrow\\; \\text{Lexical Analyzer} \\;\\longrightarrow\\; \\text{Syntax Analyzer} \\\\\n\\;\\longrightarrow\\; \\text{Semantic Analyzer} \\;\\longrightarrow\\; \\text{Intermediate Code Gen.} \\\\\n\\;\\longrightarrow\\; \\text{Optimizer} \\;\\longrightarrow\\; \\text{Target Code Gen.}\n\\end{aligned}\n$$\n\nIt reads the source as a raw character stream and outputs a **token stream** to the parser.\n\n## 2.2 The Three Pillars: Token, Lexeme, Pattern\n\nLet the input string be `int count = 100;`\n\n| Lexeme | Token Name | Attribute Value |\n|---|---|---|\n| `int` | `<KEYWORD>` | (none) |\n| `count` | `<IDENTIFIER>` | pointer to symbol table |\n| `=` | `<OPERATOR, =>` | (none) |\n| `100` | `<NUMBER>` | 100 |\n| `;` | `<SYMBOL, ;>` | (none) |\n\nThe **Pattern** for `<IDENTIFIER>` in C is formally defined using a **Regular Expression**:\n\n$$\n\\text{identifier} \\rightarrow \\text{letter} \\mid \\text{identifier letter} \\mid \\text{identifier digit}\n$$\n\nOr more compactly:\n\n$$\n\\boxed{\\text{identifier} = (a \\mid b \\mid \\ldots \\mid z \\mid A \\mid \\ldots \\mid Z \\mid \\_)\\,(a \\mid b \\mid \\ldots \\mid z \\mid A \\mid \\ldots \\mid Z \\mid 0 \\mid \\ldots \\mid 9 \\mid \\_)^{*}}\n$$\n\n## 2.3 Why Separate Lexical Analysis from Parsing? (The \"Why\")\n\n1. **Simplicity of Design** — The parser deals with token classes, not raw characters, drastically reducing its complexity.\n2. **Efficiency** — Specialized buffering and pattern-matching techniques (e.g., DFA) make scanning very fast.\n3. **Portability** — Input character-set, line-ending conventions, and special symbols can be isolated in the lexer.\n4. **Better Error Reporting** — Line numbers and column positions are easier to maintain at the character level.\n\n## 2.4 Algorithm: How the Lexer Recognizes a Token\n\nThe lexer uses a **Deterministic Finite Automaton (DFA)** for token recognition. The high-level logic is:\n\n- **Step 1:** Read the next character `c` from the input buffer.\n- **Step 2:** If `c` is whitespace or part of a comment → **skip and return to Step 1**.\n- **Step 3:** If `c` is a letter or `_` → start buffer; read until non-alphanumeric; check if buffer matches a **keyword**; else emit `<IDENTIFIER>`.\n- **Step 4:** If `c` is a digit → start buffer; read while digit (and optional decimal point); emit `<NUMBER>`.\n- **Step 5:** If `c` is `\"` → start string buffer; read until next `\"`; emit `<STRING>`.\n- **Step 6:** If `c` is an operator symbol → possibly peek next char for two-char operator; emit `<OPERATOR>`.\n- **Step 7:** If `c` is a punctuator → emit `<SYMBOL>`.\n- **Step 8:** If none of the above → **lexical error** (unrecognized character).\n\n## 2.5 KTU Formula Sheet / Cheat Sheet\n\n| **Concept** | **Formal Notation** | **Example** |\n|---|---|---|\n| Lexeme | Actual matched string | `count` |\n| Token | Pair $\\langle \\text{token-name}, \\text{attribute} \\rangle$ | $\\langle \\text{id}, \\text{ptr} \\rangle$ |\n| Pattern | Rule (typically a Regular Expression) | `[A-Za-z\\_][A-Za-z0-9\\_]*` |\n| Identifier | $(\\text{letter}\\mid\\_)(\\text{letter}\\mid\\text{digit}\\mid\\_)^{*}$ | `x`, `var_1` |\n| Number | $\\text{digit}^{+}\\;(\\,.\\text{digit}^{+}\\,)?$ | `100`, `3.14` |\n| Comment (single line) | `//` followed by anything except newline | `// hello` |\n| Comment (multi-line) | `/*` ... `*/` | `/* block */` |\n| String | `\"` (any char except `\"`)* `\"` | `\"Kerala\"` |\n\n> [!TIP]\n> **Engineering Real-World Use:** Lexical analyzers are the foundation of every modern tool you use — **GCC, Clang, Python's `tokenize` module, IDE syntax highlighters, code formatters, linters (like ESLint), and even search engines** that index source code. Designing one in C is the same logic used in production compilers, just without the optimization tricks (like Aho-Corasick for multi-pattern matching).\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Implementation & Analytical Matrix\n\n## 3.1 Complete C Program (KTU-Standard Implementation)\n\n```c\n/* ============================================================\n *  Experiment 2 : Lexical Analyzer (Scanner)\n *  Course       : PCCSL607 - Systems Lab (KTU 2024 Scheme)\n *  Author Style : Production-quality academic code\n * ============================================================ */\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#include <ctype.h>\n\n#define MAX_LEN 100\n\n/* ---------- Keyword Lookup Table ---------- */\nconst char *keywords[] = {\n    \"int\", \"float\", \"char\", \"double\", \"void\", \"long\", \"short\",\n    \"if\", \"else\", \"while\", \"for\", \"do\", \"switch\", \"case\",\n    \"default\", \"break\", \"continue\", \"return\", \"main\",\n    \"printf\", \"scanf\", \"include\", \"define\", \"stdio\"\n};\nconst int KEYWORD_COUNT = sizeof(keywords) / sizeof(keywords[0]);\n\n/* ---------- Helper : check if word is a keyword ---------- */\nint isKeyword(char *word) {\n    for (int i = 0; i < KEYWORD_COUNT; i++) {\n        if (strcmp(word, keywords[i]) == 0)\n            return 1;\n    }\n    return 0;\n}\n\n/* ============================================================\n *  MAIN DRIVER\n * ============================================================ */\nint main(void) {\n    FILE *fp;\n    char filename[64];\n    char ch, lookahead, buffer[MAX_LEN];\n    int  idx;\n\n    printf(\"Enter source file name : \");\n    scanf(\"%63s\", filename);\n\n    fp = fopen(filename, \"r\");\n    if (fp == NULL) {\n        printf(\"\\n[ERROR] Cannot open file : %s\\n\", filename);\n        return EXIT_FAILURE;\n    }\n\n    printf(\"\\n=========== LEXICAL ANALYSIS OUTPUT ===========\\n\");\n\n    while ((ch = (char)fgetc(fp)) != EOF) {\n\n        /* ---------- (A) Skip white space ---------- */\n        if (ch == ' ' || ch == '\\t' || ch == '\\n' || ch == '\\r')\n            continue;\n\n        /* ---------- (B) Skip comments ---------- */\n        if (ch == '/') {\n            lookahead = (char)fgetc(fp);\n\n            if (lookahead == '/') {              /* // single-line */\n                while ((ch = (char)fgetc(fp)) != EOF && ch != '\\n')\n                    ;   /* discard */\n            }\n            else if (lookahead == '*') {         /* /* ... */ block */\n                while ((ch = (char)fgetc(fp)) != EOF) {\n                    if (ch == '*' && (char)fgetc(fp) == '/')\n                        break;\n                }\n            }\n            else {                               /* genuine '/' operator */\n                printf(\"<OPERATOR    , />\\n\");\n                if (lookahead != EOF) ungetc(lookahead, fp);\n            }\n            continue;\n        }\n\n        /* ---------- (C) String literal ---------- */\n        if (ch == '\"') {\n            idx = 0;\n            buffer[idx++] = ch;\n            while ((ch = (char)fgetc(fp)) != EOF && ch != '\"')\n                buffer[idx++] = ch;\n            if (ch == '\"') buffer[idx++] = ch;\n            buffer[idx] = '\\0';\n            printf(\"<STRING      , %s>\\n\", buffer);\n            continue;\n        }\n\n        /* ---------- (D) Identifier or Keyword ---------- */\n        if (isalpha((unsigned char)ch) || ch == '_') {\n            idx = 0;\n            buffer[idx++] = ch;\n            while ((ch = (char)fgetc(fp)) != EOF &&\n                   (isalnum((unsigned char)ch) || ch == '_'))\n                buffer[idx++] = ch;\n            buffer[idx] = '\\0';\n\n            if (isKeyword(buffer))\n                printf(\"<KEYWORD     , %s>\\n\", buffer);\n            else\n                printf(\"<IDENTIFIER  , %s>\\n\", buffer);\n\n            if (ch != EOF) ungetc(ch, fp);\n            continue;\n        }\n\n        /* ---------- (E) Numeric constant ---------- */\n        if (isdigit((unsigned char)ch)) {\n            idx = 0;\n            buffer[idx++] = ch;\n            int dotSeen = 0;\n            while ((ch = (char)fgetc(fp)) != EOF) {\n                if (isdigit((unsigned char)ch)) {\n                    buffer[idx++] = ch;\n                } else if (ch == '.' && !dotSeen) {\n                    buffer[idx++] = ch;\n                    dotSeen = 1;\n                } else {\n                    break;\n                }\n            }\n            buffer[idx] = '\\0';\n            printf(\"<NUMBER      , %s>\\n\", buffer);\n            if (ch != EOF) ungetc(ch, fp);\n            continue;\n        }\n\n        /* ---------- (F) Two-character / one-character operators ---------- */\n        if (strchr(\"=+-*/%<>&|!^\", ch) != NULL) {\n            lookahead = (char)fgetc(fp);\n            /* Two-character operators: ==, !=, <=, >=, ++, --, &&, ||, <<, >>, +=, -=, etc. */\n            if (  (ch == '=' && lookahead == '=') ||\n                  (ch == '!' && lookahead == '=') ||\n                  (ch == '<' && (lookahead == '=' || lookahead == '<')) ||\n                  (ch == '>' && (lookahead == '=' || lookahead == '>')) ||\n                  (ch == '+' && (lookahead == '+' || lookahead == '=')) ||\n                  (ch == '-' && (lookahead == '-' || lookahead == '=')) ||\n                  (ch == '*' && lookahead == '=') ||\n                  (ch == '/' && lookahead == '=') ||\n                  (ch == '%' && lookahead == '=') ||\n                  (ch == '&' && (lookahead == '&' || lookahead == '=')) ||\n                  (ch == '|' && (lookahead == '|' || lookahead == '=')) ||\n                  (ch == '^' && lookahead == '=') ) {\n                printf(\"<OPERATOR    , %c%c>\\n\", ch, lookahead);\n            } else {\n                printf(\"<OPERATOR    , %c>\\n\", ch);\n                if (lookahead != EOF) ungetc(lookahead, fp);\n            }\n            continue;\n        }\n\n        /* ---------- (G) Preprocessor directive symbol ---------- */\n        if (ch == '#') {\n            printf(\"<PREPROCESSOR, #>\\n\");\n            continue;\n        }\n\n        /* ---------- (H) Punctuators / Special Symbols ---------- */\n        if (strchr(\"();,{}[]\", ch) != NULL) {\n            printf(\"<SYMBOL      , %c>\\n\", ch);\n            continue;\n        }\n\n        /* ---------- (I) Unrecognized character ---------- */\n        printf(\"<LEXICAL ERROR, '%c' [0x%02X]>\\n\", ch, (unsigned char)ch);\n    }\n\n    fclose(fp);\n    printf(\"================================================\\n\");\n    printf(\"Lexical Analysis Completed Successfully.\\n\");\n    return EXIT_SUCCESS;\n}\n```\n\n## 3.2 Sample Input File (`input.c`)\n\n```c\n#include <stdio.h>\nint main()\n{\n    int a = 10;\n    float b = 3.14;       // single line comment\n    /* Multi-line\n       block comment */\n    if (a >= b) {\n        a = a + 1;\n    }\n    return 0;\n}\n```\n\n## 3.3 Expected Output (Step-by-Step Trace)\n\n```\n=========== LEXICAL ANALYSIS OUTPUT ===========\n<KEYWORD     , include>\n<PREPROCESSOR, #>\n<STRING      , <stdio.h>>\n<KEYWORD     , int>\n<KEYWORD     , main>\n<SYMBOL      , (>\n<SYMBOL      , )>\n<SYMBOL      , {>\n<KEYWORD     , int>\n<IDENTIFIER  , a>\n<OPERATOR    , =>\n<NUMBER      , 10>\n<SYMBOL      , ;>\n<KEYWORD     , float>\n<IDENTIFIER  , b>\n<OPERATOR    , =>\n<NUMBER      , 3.14>\n<SYMBOL      , ;>\n<KEYWORD     , if>\n<SYMBOL      , (>\n<IDENTIFIER  , a>\n<OPERATOR    , >=>\n<IDENTIFIER  , b>\n<SYMBOL      , )>\n<SYMBOL      , {>\n<IDENTIFIER  , a>\n<OPERATOR    , =>\n<IDENTIFIER  , a>\n<OPERATOR    , +>\n<NUMBER      , 1>\n<SYMBOL      , ;>\n<SYMBOL      , }>\n<KEYWORD     , return>\n<NUMBER      , 0>\n<SYMBOL      , ;>\n<SYMBOL      , }>\n================================================\nLexical Analysis Completed Successfully.\n```\n\n## 3.4 Analytical Walkthrough — Why each character is classified this way\n\n| Character Encountered | Branch Triggered in Code | Output Produced |\n|---|---|---|\n| `i`, `n`, `t`... forming `int` | `isalpha()` true → buffer fill → `isKeyword()` true | `<KEYWORD, int>` |\n| `a` (after int) | `isalpha()` true → buffer fill → `isKeyword()` false | `<IDENTIFIER, a>` |\n| `=` | Falls in `strchr(\"=+-*/%...\")` | `<OPERATOR, =>` |\n| `1`, `0` (digits) | `isdigit()` true → buffer fill | `<NUMBER, 10>` |\n| `/` followed by `/` | `if (ch == '/')` + `lookahead == '/'` | Comment ignored |\n| `/` followed by `*` | `lookahead == '*'` | Block comment ignored |\n| `>`, `=` forming `>=` | Two-character operator match | `<OPERATOR, >=>` |\n| `\"`, `K`, ... `\"` | `ch == '\"'` branch | `<STRING, \"Hello\">` |\n| `;` | `strchr(\"();,{}[]\")` true | `<SYMBOL, ;>` |\n\n> [!TIP]\n> **Why `ungetc()` is critical:** When we read one character *ahead* to check for a two-character operator, we may have consumed a character that belongs to the **next** token. The standard C function `ungetc()` pushes that character back into the file stream buffer so the next iteration can process it correctly.\n\n## 3.5 Common Pitfalls and Their Fixes (Trace Table)\n\n| **Bug** | **Symptom** | **Fix** |\n|---|---|---|\n| Forgetting `ungetc(ch, fp)` | Next token missing its first char | Always rewind non-matching lookahead |\n| Single-line comment not skipping `\\n` | Comment swallows next line | Loop until `ch == '\\n'` or `EOF` |\n| Block comment not exiting on `*/` | Infinite loop | Break when `*` is followed by `/` |\n| Using `==` inside the operator set | Treating `=` as a two-char always | Branch logic on `ch == '='` first, then check lookahead |\n| String literal swallows newline | Lexer never recovers | Limit string scan to next `\"` only |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Compiler Phases Block Diagram\n\n```mermaid\nflowchart LR\n    SRC[\"Source Program<br/>(.c file)\"] --> LEX[\"Lexical Analyzer<br/>(Scanner)\"]\n    LEX -->|Token Stream| SYN[\"Syntax Analyzer<br/>(Parser)\"]\n    SYN -->|Parse Tree| SEM[\"Semantic Analyzer\"]\n    SEM -->|Annotated Tree| ICG[\"Intermediate Code Gen\"]\n    ICG --> OPT[\"Optimizer\"]\n    OPT --> TCG[\"Target Code Gen\"]\n    TCG --> ASM[\"Target Code<br/>(Assembly / Machine)\"]\n    LEX -.-> ST1[(\"Symbol Table\")]\n    SYN -.-> ST1\n    SEM -.-> ST1\n```\n\n## 4.2 Lexical Analyzer Internal Workflow\n\n```mermaid\nflowchart TD\n    START([Start: Open input file]) --> READ[Read next character ch]\n    READ --> WS{Is ch whitespace?}\n    WS -->|Yes, skip| READ\n    WS -->|No| COMM{ch == '/' ?}\n    COMM -->|Yes| CHECKLA{Lookahead next char}\n    CHECKLA -->|'/' single-line| SKIP1[Skip until newline]\n    CHECKLA -->|'*' multi-line| SKIP2[Skip until star-slash]\n    CHECKLA -->|No, genuine operator| OP1[Emit OPERATOR /]\n    SKIP1 --> READ\n    SKIP2 --> READ\n    OP1 --> READ\n    COMM -->|No| STR{ch == '\"' ?}\n    STR -->|Yes| SCANSTR[Read until next quote, emit STRING]\n    STR -->|No| ALPHA{isalpha or _ ?}\n    ALPHA -->|Yes| BUFID[Buffer until non-alphanumeric]\n    BUFID --> KW{isKeyword?}\n    KW -->|Yes| EMITKW[Emit KEYWORD]\n    KW -->|No| EMITID[Emit IDENTIFIER]\n    EMITKW --> READ\n    EMITID --> READ\n    SCANSTR --> READ\n    ALPHA -->|No| DIG{isdigit ?}\n    DIG -->|Yes| BUFNUM[Buffer digits and one dot]\n    BUFNUM --> EMITNUM[Emit NUMBER]\n    EMITNUM --> READ\n    DIG -->|No| OP2{Operator symbol?}\n    OP2 -->|Yes| PEEK[Peek next char for 2-char op]\n    PEEK --> EMITOP[Emit OPERATOR]\n    EMITOP --> READ\n    OP2 -->|No| PUNC{Punctuator?}\n    PUNC -->|Yes| EMITPUNC[Emit SYMBOL]\n    EMITPUNC --> READ\n    PUNC -->|No| ERR[Emit LEXICAL ERROR]\n    ERR --> READ\n    READ -->|EOF| STOP([End: Close file])\n```\n\n## 4.3 Token Classification Hierarchy\n\n```mermaid\nflowchart TD\n    T[Token Stream Output] --> K1[KEYWORD]\n    T --> K2[IDENTIFIER]\n    T --> K3[NUMBER]\n    T --> K4[OPERATOR]\n    T --> K5[SYMBOL]\n    T --> K6[STRING]\n    T --> K7[PREPROCESSOR]\n    T --> K8[LEXICAL ERROR]\n\n    K4 --> K4a[Arithmetic: + - * / %]\n    K4 --> K4b[Relational: == != < > <= >=]\n    K4 --> K4c[Logical: && || !]\n    K4 --> K4d[Bitwise: & \\| ^ ~ << >>]\n    K4 --> K4e[Assignment: = += -= *= /= %=]\n    K4 --> K4f[Increment / Decrement: ++ --]\n\n    K3 --> K3a[Integer: 0-9 digits]\n    K3 --> K3b[Floating: digits.digits]\n    K3 --> K3c[Exponent: digits.digits e digits]\n```\n\n## 4.4 Symbol Table Interaction\n\n```mermaid\nflowchart LR\n    LEX[Lexical Analyzer] -->|insert / lookup| ST[(\"Symbol Table<br/>id --> attributes\")]\n    ST -->|pointer| TOK[Token Attribute Field]\n    SYN[Syntax Analyzer] -->|lookup| ST\n    SEM[Semantic Analyzer] -->|update type info| ST\n    CG[Code Generator] -->|fetch address| ST\n```\n\n> [!IMPORTANT]\n> **KTU Diagram Interpretation Tip:** When asked in the exam to *\"draw the block diagram of a compiler\"*, the symbol table is shown as a **separate vertical data structure** on the side that **all phases (lexical, syntax, semantic, code generation) access** — not as a sequential phase. This is a frequently asked **3-mark question**.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Q1. `[KTU University Exam – May 2024, PCCSL607 Model]`\n**Define a lexical analyzer. What are its input and output?**\n\n**Model Answer (3 marks):**\n\nA **lexical analyzer** is the first phase of a compiler that performs lexical analysis. It reads the source program as a stream of characters, removes white spaces and comments, and groups the remaining characters into meaningful sequences called **lexemes**, each of which corresponds to a **token**.\n\n- **Input:** Source program (raw character stream from a `.c` file).\n- **Output:** Stream of tokens in the form $\\langle \\text{token-name}, \\text{attribute-value} \\rangle$ which is sent to the syntax analyzer.\n\nSecondary outputs include updated **symbol table** entries and **line numbers** for error reporting.\n\n---\n\n### Q2. `[KTU University Exam – Dec 2023, PCCSL607 Model]`\n**Differentiate between a Token, a Lexeme, and a Pattern with suitable examples.**\n\n**Model Answer (3 marks):**\n\n| Term | Meaning | Example for `int count = 5;` |\n|---|---|---|\n| **Token** | A classification / category of lexemes | `<KEYWORD>`, `<IDENTIFIER>`, `<NUMBER>` |\n| **Lexeme** | The actual character sequence matched | `int`, `count`, `5` |\n| **Pattern** | The rule describing all lexemes of a token | `[A-Za-z\\_][A-Za-z0-9\\_]*` for identifiers |\n\nThe pattern is what the lexer **searches for**, the lexeme is **what it actually finds**, and the token is the **label it assigns**.\n\n---\n\n## 5.2 Part B Questions (14 Marks Each — Internal Choice)\n\n### `Q3. Question A (14 Marks)` `[KTU University Exam – Jul 2024, PCCSL607]`\n\n**(a)** Explain the different **roles and functions** of a lexical analyzer in a compiler. **(7 marks)**\n\n**(b)** Design and implement a **C program** to recognize the following token categories from an input file: **keywords, identifiers, numbers, operators, and special symbols**. The program must skip white spaces (` `, `\\t`, `\\n`) and both single-line (`//`) and multi-line (`/* */`) comments. **(7 marks)**\n\n### Model Solution\n\n**(a) Roles and Functions of a Lexical Analyzer (7 marks):**\n\n1. **Tokenization** — Convert character stream into token stream.\n2. **White-space and comment removal** — Discard formatting characters that carry no semantic meaning.\n3. **Symbol table management** — Insert and lookup identifiers in the symbol table.\n4. **Line number counting** — Maintain counters for accurate error messages.\n5. **Error reporting** — Detect illegal characters and report them with line/column numbers.\n6. **Constant folding at lexical level** — Some constants can be pre-computed.\n7. **Decoupling input alphabet handling** — Isolates OS/file encoding issues from the parser.\n\n**(b) C Program (7 marks):**\n\nThe complete C program is the same as listed in **Section 3.1** of these notes. Key code sections that must appear in any model answer for full marks:\n\n```c\n/* 1. Keyword check function */\nint isKeyword(char *word) {\n    for (int i = 0; i < KEYWORD_COUNT; i++)\n        if (strcmp(word, keywords[i]) == 0) return 1;\n    return 0;\n}\n\n/* 2. Main scanning loop with branches */\nwhile ((ch = fgetc(fp)) != EOF) {\n\n    /* Skip white space */\n    if (ch == ' ' || ch == '\\t' || ch == '\\n') continue;\n\n    /* Skip comments (//  and  /* ... *​/) */\n    if (ch == '/') {\n        char next = fgetc(fp);\n        if (next == '/')  while ((ch = fgetc(fp)) != EOF && ch != '\\n');\n        else if (next == '*') {\n            while ((ch = fgetc(fp)) != EOF) {\n                if (ch == '*' && fgetc(fp) == '/') break;\n            }\n        }\n        else { printf(\"<OPERATOR, />\\n\"); ungetc(next, fp); }\n        continue;\n    }\n\n    /* Identifier / Keyword */\n    if (isalpha(ch) || ch == '_') {\n        /* buffer the word, then check isKeyword() */\n    }\n\n    /* Number */\n    if (isdigit(ch)) { /* buffer digits and at most one '.' */ }\n\n    /* Operator (one or two char) */\n    /* Punctuator */\n}\nprintf(\"<LEXICAL ERROR, %c>\\n\", ch);\n```\n\n**Validation Table for Sample Input `int a=5;`:**\n\n| Input char | Branch | Output |\n|---|---|---|\n| `i` | Identifier/Keyword branch | `<KEYWORD, int>` |\n| `a` | Identifier/Keyword branch | `<IDENTIFIER, a>` |\n| `=` | Operator branch | `<OPERATOR, =>` |\n| `5` | Number branch | `<NUMBER, 5>` |\n| `;` | Punctuator branch | `<SYMBOL, ;>` |\n\n---\n\n### `Q4. Question B (14 Marks)` `[KTU University Exam – Jul 2024, PCCSL607 — Alternative Choice]`\n\n**(a)** With a neat **block diagram**, describe the **complete phases of a compiler** and explain the role of the lexical analyzer as the first phase. **(7 marks)**\n\n**(b)** Consider the following C source code. Show the **lexical analyzer output** (token stream) and identify any **lexical errors** if present. The lexer must handle `==`, `>=`, `++`, `/*` comments, and `//` comments. **(7 marks)**\n\n```c\nint main() {\n    int x = 10;\n    /* initialize y */\n    int y == 20;        // note the double equal\n    x++;\n    if (x >= y) {\n        x = x $ y;      // '$' is invalid\n    }\n}\n```\n\n### Model Solution\n\n**(a) Compiler Phases and Role of Lexical Analyzer (7 marks):**\n\nThe compiler has **two main divisions**:\n- **Analysis (Front-End):** Lexical → Syntax → Semantic → Intermediate Code Generation\n- **Synthesis (Back-End):** Optimization → Target Code Generation\n\n```\nSource ──▶ [Lexical Analyzer] ──▶ [Syntax Analyzer] ──▶ [Semantic] ──▶ [ICG] ──▶ [Optimizer] ──▶ [TCG] ──▶ Target\n                  │                    │                    │           │\n                  ▼                    ▼                    ▼           ▼\n              [Symbol Table] ◀────────┴────────────────────┴───────────┘\n```\n\nThe **lexical analyzer** sits at the input boundary, consumes raw characters, and produces a clean token stream. It also maintains the **symbol table** which is shared with later phases. (A complete block diagram earns full 7 marks if all six phases and the symbol table are shown.)\n\n**(b) Token Stream Output (7 marks):**\n\n| Lexeme | Token |\n|---|---|\n| `int` | `<KEYWORD, int>` |\n| `main` | `<KEYWORD, main>` |\n| `(` | `<SYMBOL, (>` |\n| `)` | `<SYMBOL, )>` |\n| `{` | `<SYMBOL, {>` |\n| `int` | `<KEYWORD, int>` |\n| `x` | `<IDENTIFIER, x>` |\n| `=` | `<OPERATOR, =>` |\n| `10` | `<NUMBER, 10>` |\n| `;` | `<SYMBOL, ;>` |\n| *(block comment skipped)* | — |\n| `int` | `<KEYWORD, int>` |\n| `y` | `<IDENTIFIER, y>` |\n| `==` | `<OPERATOR, ==> ` (lexically valid) |\n| `20` | `<NUMBER, 20>` |\n| `;` | `<SYMBOL, ;>` |\n| `x` | `<IDENTIFIER, x>` |\n| `++` | `<OPERATOR, ++>` |\n| `;` | `<SYMBOL, ;>` |\n| `if` | `<KEYWORD, if>` |\n| `(` | `<SYMBOL, (>` |\n| `x` | `<IDENTIFIER, x>` |\n| `>=` | `<OPERATOR, >=>` |\n| `y` | `<IDENTIFIER, y>` |\n| `)` | `<SYMBOL, )>` |\n| `{` | `<SYMBOL, {>` |\n| `x` | `<IDENTIFIER, x>` |\n| `=` | `<OPERATOR, =>` |\n| `x` | `<IDENTIFIER, x>` |\n| `$` | **`<LEXICAL ERROR, $>`** |\n| `y` | `<IDENTIFIER, y>` |\n| `;` | `<SYMBOL, ;>` |\n| `}` | `<SYMBOL, }>` |\n| `}` | `<SYMBOL, }>` |\n\n> **Note on `y == 20`:** The lexical analyzer **will accept this** and emit `<OPERATOR, ==>`. It is a **syntax error**, NOT a lexical error, because `==` is a valid C token. The lexer does not know that the programmer probably meant `=`. This is a classic exam trap.\n\n> **Note on `$`:** The `$` character is **not** part of C's operator/punctuator set, so the lexer correctly flags it as a **lexical error**.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfalls — Where You Lose Marks:**\n> 1. **Conflating \"lexical error\" with \"syntax error\"** — A lexer detects invalid characters and malformed tokens (e.g., `123abc` where it expected a number, or stray `$`). It does NOT detect missing semicolons or invalid statement structure — that is the parser.\n> 2. **Forgetting to handle `ungetc()` for lookahead** — Most students forget to rewind the file pointer after peeking. KTU valuators specifically check for this.\n> 3. **Not distinguishing `=` (assignment) from `==` (equality)** — Make sure the program emits them as separate tokens with correct category names.\n> 4. **Mixing up \"lexeme\" and \"token\" terminology** in theory answers — Use the exact words from Aho-Sethi-Ullman as KTU follows that textbook.\n> 5. **Treating `stdio.h` as a single token** — Actually, in `#include <stdio.h>`, the `<` and `>` and `.` are separate symbols; only the file name forms a string-like token. Many students write it incorrectly.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Rapid-Revision Checklist (Pin this before exam):**\n\n- **Lexical Analyzer = First phase of compiler**; input is character stream, output is token stream.\n- A **Token** is `<token-name, attribute-value>` — for example, `<IDENTIFIER, ptr_to_symtab>`.\n- A **Lexeme** is the actual matched substring; a **Pattern** is the rule for matching.\n- The lexer is implemented as a **DFA (Deterministic Finite Automaton)** in production compilers.\n- Identifier pattern: `letter (letter | digit | _)*`\n- Number pattern: `digit+ (. digit+)?`\n- Operators are categorized into **single-char and two-char** — always use `ungetc()` when peeking.\n- Comments (`//` and `/* */`) and **whitespace** (space, tab, newline, carriage return) are **discarded**, not emitted.\n- The lexer also maintains a **Symbol Table** for identifier storage.\n- The lexer handles **line numbers** for error reporting — track them in a counter.\n- **Common KTU 2-mark questions:** \"Define token\", \"Differentiate lexeme vs token\", \"List compiler phases\", \"What does a lexer output?\".\n- **Common 7/14-mark questions:** \"Write a C program to implement a lexical analyzer\", \"Show token stream for a given C snippet\", \"Explain the role and design of a lexical analyzer with a block diagram\".\n- **Mnemonic:** \"**L**exers **L**ove **L**ists\" — they read Characters → **Lexemes** → **Tokens** → forward to Parser.\n- **Tool Mapping:** Real-world equivalents are `lex`, `flex`, `ANTLR` (lexer-generator tools) — your C program is the manual equivalent of what these tools auto-generate.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-engineering-7-pccsl607-mExperiment 2-t2",
          "title": "Write a lex program to display the number of lines, words, and characters in an input text",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL607 - SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Write a lex program to display the number of lines, words, and characters in an input text\n\n<!-- SECTION_1_START -->\n\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU Syllabus Terminology)\n\n**Lex** is a lexical analyzer generator tool used in System Programming and Compiler Design. It takes a specification file (with extension `.l`) containing **regular expressions** (patterns) paired with **actions written in C**, and produces a C source file (`lex.yy.c`) containing a complete lexical scanner. This scanner, when compiled with a C compiler, produces an executable that can read an input stream, identify tokens matching the given patterns, and execute the corresponding C code.\n\nIn the context of **KTU 2024 Scheme (PCCSL607 – Systems Lab)**, this experiment is the classical \"**Word Count Utility**\" problem, analogous in logic to the Unix `wc` command. The objective is to write a Lex program that:\n\n- Counts the **number of lines** (newline characters `\\n`).\n- Counts the **number of words** (maximal contiguous sequences of non-whitespace characters).\n- Counts the **number of characters** (every single character, including whitespace and newlines).\n\n> [!NOTE]\n> **KTU 2024 Vocabulary You Must Know**\n> - **Token**: A logically grouped sequence of characters matched by a single Lex rule.\n> - **Pattern (Regular Expression)**: The left-hand side of a Lex rule; tells *what* to match.\n> - **Action (C Code)**: The right-hand side of a Lex rule; tells *what to do* when the pattern matches.\n> - **yytext**: A global `char *` pointing to the currently matched text.\n> - **yyleng**: An `int` holding the length of the currently matched text.\n\n## 1.2 Intuitive Analogy (Plain English)\n\nImagine you are a **postman sorting letters in a mailroom**:\n- The **incoming mail** is the input text.\n- You have a **rulebook** (the Lex `.l` file) that says: *\"If you see a letter marked 'URGENT', put it in the red tray; if it's a 'BILL', put it in the yellow tray; otherwise, drop it in the general tray.\"*\n- Each time you handle a letter, you write a tally mark on a specific counter (lines, words, characters).\n- The **lexical analyzer** is you — you keep reading mail and applying rules until the bag is empty.\n\nIn our program:\n- **Newline character** → increment *line counter* (like a paragraph break in a book).\n- **A \"word\"** → a continuous run of letters/digits with no spaces or tabs around it (like a single word in a sentence).\n- **Every single character** → increment the *character counter* (think: every letter, space, and punctuation mark counts as one).\n\n> [!IMPORTANT]\n> **Syllabus Highlight:** This experiment tests three KTU Course Outcomes —\n> **CO1** (Apply knowledge of mathematics & engineering to system software problems),\n> **CO2** (Design system components using Lex/Yacc),\n> and **CO5** (Use modern tools like `flex` and `gcc` to conduct experiments and analyze results).\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Anatomy of a Lex Program (Three Sections)\n\nA Lex program (`.l` file) has a strict three-part structure separated by `%%`:\n\n| Section | Delimiters | Purpose | Key Syntax |\n|---|---|---|---|\n| **Declarations** | `%{ ... %}` | Global C variables, `#include`, constants | Wrapped C code, copied verbatim into the generated `.c` file |\n| **Rules** | `%%` ... `%%` | Pattern → Action pairs | `pattern { action; }` |\n| **Auxiliary Functions** | After second `%%` | `main()`, `yywrap()`, helper functions | Regular C functions |\n\n## 2.2 The Lex Processing Pipeline\n\nThe Lex compiler (commonly `flex` on Linux) performs the following transformations:\n\n$$\\text{\\texttt{program.l}} \\xrightarrow{\\text{flex}} \\text{\\texttt{lex.yy.c}} \\xrightarrow{\\text{gcc}} \\text{\\texttt{a.out (executable)}}$$\n\n1. **Lex reads** the `.l` file.\n2. **Translates each pattern** to a Deterministic Finite Automaton (DFA).\n3. **Emits C code** implementing the DFA into `lex.yy.c`.\n4. **GCC compiles** this file (linked with `-lfl` on some systems).\n5. **The executable** reads input via `yyin` and runs the `yylex()` scanner.\n\n## 2.3 Pattern Matching Strategy for Word Count\n\nThe single most important conceptual point: **Lex picks the longest match, and on ties, the rule listed first wins.**\n\nFor our problem, the patterns are:\n\n$$\\text{Word} \\rightarrow \\text{any non-whitespace sequence}$$\n\n$$\\text{Line boundary} \\rightarrow \\text{newline character}$$\n\n$$\\text{Whitespace} \\rightarrow \\text{spaces and tabs}$$\n\n$$\\text{Any other character} \\rightarrow \\text{a catch-all rule for counting characters}$$\n\n> [!TIP]\n> **Why a catch-all rule `.` is needed**: If you do not include a final rule, characters like punctuation (`.`, `,`, `!`) will be silently **discarded** (Lex's default action is to copy the matched text to output only when `ECHO` is used, and unmatched input is discarded). To count *every* character, the catch-all is essential.\n\n## 2.4 KTU High-Yield Formula Sheet (Cheat Table)\n\n| Concept | Symbol / Syntax | Meaning | Counted as |\n|---|---|---|---|\n| Newline | `\\n` | End of a line | +1 line |\n| Word pattern | `[^ \\t\\n]+` | One or more characters that are NOT space, tab, or newline | +1 word |\n| Whitespace | `[ \\t]+` | One or more spaces or tabs | counted in characters (length added) |\n| Catch-all | `.` | Any single character not matched by other rules | +1 character |\n| `yytext` | global `char *` | Pointer to matched string | — |\n| `yyleng` | global `int` | Length of `yytext` | added to character count |\n| `yywrap()` | user-defined | Returns 1 when input ends | ends `yylex()` |\n| `ECHO` | macro | Default action: `fprintf(yyout, \"%s\", yytext);` | prints matched text |\n| `yyin` | `FILE *` | Input stream (default: `stdin`) | — |\n\n> [!NOTE]\n> **Crucial Pitfall:** Use `\\vert` style in formulas above (e.g., `$\\vert x \\vert$`) because the `|` character breaks markdown table cells. In your exam scripts, write `|x|` plainly.\n\n## 2.5 Real-World Engineering Utility\n\nThe same Lex pattern-matching engine powers:\n\n- **Compiler front-ends** (GCC's lexer, Clang's lexer).\n- **Text-processing utilities** (`grep`, `awk`, `sed` use similar finite-automata techniques).\n- **Syntax highlighters** in IDEs (VS Code, Sublime Text).\n- **Network packet inspectors** and protocol analyzers.\n- **Data sanitization tools** that strip malformed input.\n\nMastering this experiment gives you the foundational skill of *declarative pattern-based programming*, which scales from counting words to detecting SQL injection attacks in cybersecurity.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n# SECTION 3: Step-by-Step Program Construction & Analytical Matrix\n\n## 3.1 The Reference Lex Program (Lab-Ready)\n\n```lex\n%{\n/* ============================================================\n   PROGRAM : Word Count using Lex\n   COURSE  : PCCSL607 - Systems Lab (KTU 2024 Scheme)\n   AIM     : Display the number of lines, words and characters\n             in an input text.\n   TOOLS   : flex, gcc (Linux) / Lex (Windows)\n   ============================================================ */\n#include <stdio.h>\n\n/* Global counters — visible to action blocks */\nint line_count      = 0;\nint word_count      = 0;\nint character_count = 0;\n%}\n\n/* No helper definitions needed here, but the section must exist */\n\n%%\n                /* ---- TRANSLATION RULES ---- */\n\n\\n              { line_count++; \n                  character_count += yyleng; }\n\n[ \\t]+          { character_count += yyleng; }    /* spaces & tabs */\n\n[^ \\t\\n]+       { word_count++; \n                  character_count += yyleng; }    /* a 'word'  */\n\n.               { character_count += yyleng; }    /* any other char */\n%%\n\n/* ---- AUXILIARY FUNCTIONS ---- */\n\nint yywrap() {\n    /* Return 1 to signal end-of-input to yylex() */\n    return 1;\n}\n\nint main() {\n    printf(\"===== KTU Word Count Utility =====\\n\");\n    printf(\"Type your text. Press Ctrl+D (Linux) \"\n           \"or Ctrl+Z (Windows) to finish:\\n\\n\");\n\n    yylex();   /* Drive the scanner until yywrap() returns 1 */\n\n    printf(\"\\n===== STATISTICS =====\\n\");\n    printf(\"Lines      : %d\\n\", line_count);\n    printf(\"Words      : %d\\n\", word_count);\n    printf(\"Characters : %d\\n\", character_count);\n    return 0;\n}\n```\n\n## 3.2 Exhaustive Line-by-Line Walkthrough\n\n### Section 1 — Declarations (`%{ ... %}`)\n\n**Line-by-line reasoning:**\n\n- `#include <stdio.h>` → required for `printf()` used in `main()`.\n- `int line_count = 0, word_count = 0, character_count = 0;` → three **mutable global counters** are declared so that the *action blocks* (which become parts of the generated `yylex()` function) can update them. Local variables in `main()` would be invisible inside the action blocks.\n- The `%{ ... %}` wrapper tells Flex: *\"Copy the enclosed C code verbatim to the top of the generated `lex.yy.c`.\"*\n\n### Section 2 — Rules (`%% ... %%`)\n\nEach row is a **pattern → action** pair. Flex applies the **maximal-munch** rule.\n\n**Rule 1: `\\n`**\n$$\\text{Pattern: } \\backslash n \\quad \\Rightarrow \\quad \\text{Matches one newline character}$$\n$$\\text{Action: } \\texttt{line\\_count++; character\\_count += yyleng;}$$\nSince `\\n` has length 1, this adds exactly 1 to the line counter and 1 to the character counter.\n\n**Rule 2: `[ \\t]+`**\n$$\\text{Pattern: } [\\, \\backslash t\\,] + \\quad \\Rightarrow \\quad \\text{One or more spaces/tabs}$$\n$$\\text{Action: } \\texttt{character\\_count += yyleng;}$$\n- The `+` quantifier means *one or more*. A single space and ten spaces will be matched as one token.\n- We **do not** increment `word_count` here — spaces are not words.\n- `yyleng` lets us add the exact number of characters in one go, not character-by-character.\n\n**Rule 3: `[^ \\t\\n]+`**\n$$\\text{Pattern: } [\\, \\widehat{\\, \\backslash t \\backslash n \\,}\\,] + \\quad \\Rightarrow \\quad \\text{Anything that is NOT a space, tab, or newline}$$\n$$\\text{Action: } \\texttt{word\\_count++; character\\_count += yyleng;}$$\n- The `^` inside `[...]` is the **negation** operator.\n- `\"Hello, world!\"` is split into the tokens `Hello,` and `world!` because the space is excluded.\n- Each such maximal run increments the word count **once** — this is precisely how Unix `wc -w` works.\n\n**Rule 4: `.`**\n$$\\text{Pattern: } . \\quad \\Rightarrow \\quad \\text{Any single character not already matched}$$\n$$\\text{Action: } \\texttt{character\\_count += yyleng;}$$\n- The dot matches **any character except newline** by default.\n- This is a safety net for punctuation like `.`, `,`, `!`, `(`, `)` that isn't covered by Rules 1–3.\n- Without this rule, punctuation would be silently discarded.\n\n### Section 3 — Auxiliary Functions\n\n**`yywrap()`**: When `yylex()` reaches end-of-file, it calls this function. Returning `1` (non-zero) tells Lex to stop scanning. In a multi-file scenario, you could `return 0` after opening the next file.\n\n**`main()`**: Initializes the scanner by calling `yylex()`. The scanner runs in a tight loop:\n- Read input buffer.\n- Find longest match.\n- Execute corresponding action.\n- Repeat until EOF.\nAfter `yylex()` returns, we print the final tallies.\n\n## 3.3 Analytical Matrix: Sample Input Trace\n\nLet us trace the input string `\"Hi all.\\nWelcome to KTU.\\n\"` (note: 2 newlines, 3 words, 21 characters).\n\n| Step | Input Remaining | Token Matched | yyleng | line_count | word_count | character_count |\n|---:|---|---|---:|---:|---:|---:|\n| 1 | `Hi all.\\nWelcome to KTU.\\n` | `Hi` (word) | 2 | 0 | 1 | 2 |\n| 2 | ` all.\\nWelcome to KTU.\\n` | ` ` (whitespace) | 1 | 0 | 1 | 3 |\n| 3 | `all.\\nWelcome to KTU.\\n` | `all` (word) | 3 | 0 | 2 | 6 |\n| 4 | `.\\nWelcome to KTU.\\n` | `.` (catch-all) | 1 | 0 | 2 | 7 |\n| 5 | `\\nWelcome to KTU.\\n` | `\\n` (newline) | 1 | 1 | 2 | 8 |\n| 6 | `Welcome to KTU.\\n` | `Welcome` (word) | 7 | 1 | 3 | 15 |\n| 7 | ` to KTU.\\n` | ` ` (whitespace) | 1 | 1 | 3 | 16 |\n| 8 | `to KTU.\\n` | `to` (word) | 2 | 1 | 4 | 18 |\n| 9 | ` KTU.\\n` | ` ` (whitespace) | 1 | 1 | 4 | 19 |\n| 10 | `KTU.\\n` | `KTU` (word) | 3 | 1 | 5 | 22 |\n| 11 | `.\\n` | `.` (catch-all) | 1 | 1 | 5 | 23 |\n| 12 | `\\n` | `\\n` (newline) | 1 | 2 | 5 | 24 |\n\n**Final Output:**\n$$\\text{Lines} = 2, \\quad \\text{Words} = 5, \\quad \\text{Characters} = 24$$\n\n> [!IMPORTANT]\n> **Cross-Verification:** The Unix command `wc input.txt` should yield identical counts if your Lex program is correct. Always test against `wc` for lab-record validation.\n\n## 3.4 Compilation & Execution Procedure (For Lab Record)\n\n```bash\n# Step 1: Save the program\nnano wordcount.l\n\n# Step 2: Generate the C scanner\nflex wordcount.l        # produces lex.yy.c\n\n# Step 3: Compile to executable\ngcc lex.yy.c -o wordcount -lfl    # -lfl may not be needed in some distros\n\n# Step 4: Run interactively OR with a file\n./wordcount                       # type input, press Ctrl+D\n./wordcount < input.txt           # redirect a file\n```\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Lex Compilation Pipeline (Build Flow)\n\n```mermaid\nflowchart LR\n    A[wordcount.l - Lex Source] --> B[flex Compiler]\n    B --> C[lex.yy.c - Generated C Source]\n    C --> D[gcc Compiler]\n    D --> E[wordcount - Executable]\n    E --> F[Input Text Stream]\n    F --> G[Statistics Output]\n```\n\n**Reading the diagram:**\n- The `.l` file is the human-written specification.\n- `flex` translates regular expressions into C code for a DFA-based scanner.\n- `gcc` compiles the C code to a native executable.\n- The executable consumes input and emits the three counts.\n\n## 4.2 Lex Scanner Runtime Architecture (Internal Block View)\n\n```mermaid\nflowchart TD\n    A[Input Buffer yyin stdin] --> B[yylex Scanner Engine]\n    B --> C{Pattern Match Phase - Maximal Munch}\n    C -->|Newline n found| D[Action: lines++ chars++]\n    C -->|Whitespace space tab| E[Action: chars += yyleng]\n    C -->|Word sequence non-whitespace| F[Action: words++ chars += yyleng]\n    C -->|Other char catch-all| G[Action: chars += yyleng]\n    D --> H{Check yywrap}\n    E --> H\n    F --> H\n    G --> H\n    H -->|Return 0 - More Input| B\n    H -->|Return 1 - EOF| I[Exit yylex]\n    I --> J[main prints Lines Words Chars]\n```\n\n**Reading the diagram:**\n- The `yylex()` function runs in a loop.\n- For each token, it consults the rule table and selects the **longest match**.\n- The corresponding C action updates the global counters.\n- `yywrap()` acts as a gatekeeper: returning 1 means stop; returning 0 means more input remains.\n\n## 4.3 Pattern-to-Counter Mapping (Data Flow View)\n\n```mermaid\nflowchart LR\n    P1[Pattern: backslash n] --> R1[Rule 1]\n    P2[Pattern: bracket space tab bracket plus] --> R2[Rule 2]\n    P3[Pattern: bracket caret space tab n bracket plus] --> R3[Rule 3]\n    P4[Pattern: dot] --> R4[Rule 4]\n    R1 --> C1[Counter: line_count]\n    R2 --> C2[Counter: character_count]\n    R3 --> C3[Counter: word_count]\n    R3 --> C4[Counter: character_count]\n    R4 --> C5[Counter: character_count]\n```\n\n**Reading the diagram:**\n- Only Rule 1 touches the `line_count` counter.\n- Only Rule 3 touches the `word_count` counter.\n- Rules 1, 2, 3, and 4 all contribute to `character_count`, ensuring every character is tallied exactly once.\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. **[KTU University Exam — June 2023]** \n*What is Lex? Mention the three sections of a Lex program.*\n\n**Model Answer (3 marks):**\n- **Definition (1 mark):** Lex is a tool (specifically, a lexical analyzer generator) that converts a specification file containing regular expressions and C actions into a C program that performs lexical analysis (tokenization) on an input stream.\n- **Three sections (2 marks):**\n  1. **Declarations section** — enclosed in `%{ ... %}`, contains C code (variables, includes) copied verbatim to the output.\n  2. **Rules section** — enclosed between two `%%` markers, contains `pattern { action }` pairs.\n  3. **Auxiliary functions section** — plain C code placed after the second `%%`, typically includes `main()` and `yywrap()`.\n\n---\n\n### Q2. **[KTU University Exam — Dec 2022]** \n*Write a Lex pattern to match: (i) a word, (ii) a newline, (iii) a sequence of one or more spaces/tabs.*\n\n**Model Answer (3 marks):**\n\n| Token | Lex Pattern | Explanation |\n|---|---|---|\n| Word | `[^ \\t\\n]+` | One or more chars that are NOT space, tab, or newline |\n| Newline | `\\n` | The literal newline character |\n| Spaces/tabs | `[ \\t]+` | One or more characters from the set `{space, tab}` |\n\nThe `+` quantifier (1 mark) ensures we match the *maximal* sequence, not character-by-character.\n\n---\n\n## 5.2 Part B — Module Internal Choice (14 Marks Each)\n\n### Question A (14 Marks) — *[KTU University Exam — Dec 2023]*\n\n> **(a)** *With a neat diagram, explain the working of the Lex compiler.* **(7 marks)**\n> **(b)** *Write a Lex program that reads a C source file and counts the number of `printf` statements. Display the total at the end.* **(7 marks)**\n\n---\n\n#### **Solution to (a) — Lex Compiler Working (7 marks)**\n\nThe Lex compiler converts a `.l` specification into an equivalent C program implementing a **Deterministic Finite Automaton (DFA)**.\n\n**Step-by-step working:**\n\n1. **Input phase** *(1 mark)*: Lex reads the `.l` file, separating the three sections (declarations, rules, auxiliary).\n2. **Pattern compilation** *(2 marks)*: Each regular expression on the LHS of a rule is converted into an **NFA (Non-deterministic Finite Automaton)** using Thompson's construction. All NFAs are then merged into one master NFA with a new start state.\n3. **Subset construction** *(2 marks)*: The NFA is converted to a **DFA** via the subset algorithm, eliminating nondeterminism.\n4. **Minimization** *(1 mark)*: The DFA is minimized by merging equivalent states, reducing the number of states for efficiency.\n5. **Code emission** *(1 mark)*: Flex emits C code (`lex.yy.c`) that simulates the DFA. Each DFA state has a switch-case emitting the corresponding action. The function `yylex()` is the main scanner driver.\n\n**Block Diagram (textual for answer sheet):**\n\n```\n   .l file  ──►  Pattern → NFA  ──►  NFA → DFA  ──►  Minimize DFA\n                                                       │\n                                                       ▼\n                                            Emit C code (lex.yy.c)\n                                                       │\n                                                       ▼\n                                          gcc compile → a.out\n```\n\n---\n\n#### **Solution to (b) — Lex Program to Count `printf` Statements (7 marks)**\n\n```lex\n%{\n#include <stdio.h>\nint printf_count = 0;\n%}\n\n%%\n\"printf\"      { printf_count++; ECHO; }\n.|\\n          { ECHO; }\n%%\n\nint yywrap() { return 1; }\n\nint main() {\n    yylex();\n    printf(\"\\nTotal printf statements: %d\\n\", printf_count);\n    return 0;\n}\n```\n\n**Explanation of the program (for full marks):**\n\n- The pattern `\"printf\"` *(literal string in quotes)* matches the exact token `printf` as a unit *(2 marks)*.\n- `printf_count++` increments the global counter each time the pattern matches *(1 mark)*.\n- `ECHO` is a built-in Lex macro equivalent to `fprintf(yyout, \"%s\", yytext);` — it preserves the original source on the output *(1 mark)*.\n- The catch-all rule `.|\\n` (any char OR newline) ensures every other character is echoed so that no input is lost, mirroring the input on stdout *(1 mark)*.\n- `yywrap()` returning 1 ends scanning at EOF *(1 mark)*.\n- The final count is printed after `yylex()` returns *(1 mark)*.\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall:** A common mistake is to write the pattern as `printf` (without quotes). Without quotes, Lex interprets it as a *regular expression* (which in this case happens to work but is technically incorrect). For multi-character literals, **always use double quotes**, e.g., `\"printf\"`. Similarly, students often forget `ECHO`, causing the entire input to be discarded and losing 2 marks.\n\n---\n\n### Question B (14 Marks) — *[KTU University Exam — June 2024]*\n\n> **(a)** *Explain the difference between `yytext` and `yyleng` with an example.* **(7 marks)**\n> **(b)** *Write a Lex program that displays the number of lines, words, and characters in an input text. Explain each rule.* **(7 marks)**\n\n---\n\n#### **Solution to (a) — `yytext` vs `yyleng` (7 marks)**\n\n| Property | `yytext` | `yyleng` |\n|---|---|---|\n| Type | `char *` (pointer) | `int` |\n| Value | Points to the matched text in the input buffer | Length of the matched text in characters |\n| Memory | Located in the internal input buffer | A simple integer variable |\n| Usage | `printf(\"%s\", yytext);` to print the match | `chars += yyleng;` for counting |\n| Null-terminated? | Yes | N/A |\n\n**Worked example (4 marks):**\n\nSuppose the input contains the word `\"Hello\"` and the pattern `[a-zA-Z]+` matches it.\n\n- After the match:\n$$\\text{yytext} = \\text{`\"Hello\"'} \\quad (\\text{pointer to characters H, e, l, l, o, }\\backslash 0)$$\n$$\\text{yyleng} = 5$$\n\n**Code demonstration:**\n```c\nprintf(\"Matched: %s\\n\", yytext);   /* Output: Matched: Hello */\nprintf(\"Length : %d\\n\", yyleng);   /* Output: Length : 5      */\n```\n\n- `yytext` is essential when you need to *inspect the content* (e.g., to look up a symbol table entry).\n- `yyleng` is essential when you need to *count characters* or skip ahead by a known amount.\n\n---\n\n#### **Solution to (b) — Lines, Words, Characters Program (7 marks)**\n\n**Full program** (as developed in Section 3.1) carries 4 marks. The remaining 3 marks go to the **explanation of each rule**:\n\n**Rule 1: `\\n  { line_count++; character_count += yyleng; }`** *(1 mark)*\n- Matches a newline character; increments the line counter by 1.\n- Adds the newline's length (1) to the character counter.\n\n**Rule 2: `[ \\t]+  { character_count += yyleng; }`** *(1 mark)*\n- Matches one or more consecutive spaces or tabs as a single token (efficient).\n- Adds the matched length to the character counter; **does not** count as a word.\n\n**Rule 3: `[^ \\t\\n]+  { word_count++; character_count += yyleng; }`** *(1 mark)*\n- Matches any maximal run of non-whitespace characters.\n- The negation `^` inside `[...]` excludes spaces, tabs, and newlines.\n- Each match represents one word.\n\n**Rule 4: `.  { character_count += yyleng; }`** *(1 mark)*\n- Catch-all rule for any single character not covered above (e.g., punctuation).\n- Without this rule, unmatched characters are silently discarded.\n\n**Auxiliary functions:** *(1 mark for both)*\n- `yywrap()` returns 1 to terminate `yylex()` at EOF.\n- `main()` calls `yylex()` and prints the final counts.\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall:** A frequent error is using `\\n+` instead of `\\n` for the line rule. Using `\\n+` collapses multiple consecutive newlines into a single match, undercounting blank lines. Always use the single-character pattern `\\n` for accurate line counting. Another trap: students often omit the catch-all `.` rule, causing punctuation to vanish and lowering the character count — losing 2 marks.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember (Rapid Revision Checklist)\n\n- [x] **Lex is a pattern-action scanner generator**, not a parser; it tokenizes but does not understand grammar.\n- [x] A `.l` file has **three sections**: declarations (`%{ ... %}`), rules (`%% ... %%`), and C functions (after the second `%%`).\n- [x] `flex wordcount.l` generates `lex.yy.c`; compile with `gcc lex.yy.c -o wordcount`.\n- [x] **Line counter** uses pattern `\\n` with action `line_count++`.\n- [x] **Word counter** uses pattern `[^ \\t\\n]+` with action `word_count++`.\n- [x] **Character counter** must be updated in *every* rule using `character_count += yyleng;`.\n- [x] **Catch-all rule `.`** is mandatory to prevent silent character loss.\n- [x] `yytext` is a `char *` to the matched text; `yyleng` is its length.\n- [x] `yywrap()` returning 1 tells Lex that input is exhausted.\n- [x] **Rule precedence**: Lex picks the **longest** match; on ties, the **first** listed rule wins.\n- [x] **Pattern quoting**: Multi-character literals (e.g., keywords) must be in double quotes: `\"printf\"`.\n- [x] **Global variables** must be declared in the `%{ ... %}` block so action code can access them.\n- [x] **Quantifiers**: `+` (one or more), `*` (zero or more), `?` (zero or one).\n- [x] **Character classes**: `[abc]` matches any one of `a`, `b`, `c`; `[^abc]` matches anything *except* them.\n- [x] Always cross-verify your Lex output with the Unix `wc` command before submitting the lab record.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-engineering-7-pccsl607-mExperiment 2-t3",
          "title": "Generate a YACC specification to recognize a valid arithmetic expression that uses operators and parenthesis",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL607 - SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Generate a YACC specification to recognize a valid arithmetic expression that uses operators and parenthesis\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n**YACC (Yet Another Compiler Compiler)** is a parser generator tool that takes a context-free grammar specification written in **Backus-Naur Form (BNF)** and automatically generates a C-language parser using the **LALR(1) (Look-Ahead Left-to-Right with one token look-ahead)** parsing algorithm. It is the standard UNIX tool for building the **syntax analysis (parsing) phase** of a compiler.\n\nFor this experiment, YACC is used to *recognize a valid arithmetic expression* — i.e., the parser must:\n1. Accept any syntactically legal infix arithmetic expression built from `+`, `-`, `*`, `/`, `(`, `)` and integer operands.\n2. Reject any malformed input (mismatched brackets, dangling operators, illegal tokens).\n3. Optionally **evaluate** the expression during parsing (synthesized attribute evaluation).\n\n> [!IMPORTANT]\n> **YACC is the parser (grammar) generator; LEX/FLEX is the tokenizer (lexer) generator.** They work together: LEX produces tokens, YACC consumes them to build a parse tree. In KTU labs they are almost always paired.\n\n### 1.2 Conceptual Analogy — \"The Strict Math Teacher\"\n\nImagine a very strict math teacher who follows a fixed set of rules:\n\n- \"You may not write two operators in a row\" (no `++3` or `*/5`).\n- \"Every opening parenthesis `(` must have a matching `)`.\"\n- \"Multiplication is solved before addition\" (precedence).\n- \"Operators of equal weight are evaluated left to right\" (left-associativity).\n\nThe **LEX program** is like the teacher's *reading glasses* — it identifies the raw symbols (digits, `+`, `*`, `(`, etc.). The **YACC program** is the *set of grammar rules* in the teacher's mind that decides whether what was read is a valid sentence. The student (input) either gets a green tick (parsed) or a red cross (syntax error with line number).\n\n### 1.3 Why YACC and not a Hand-Written Parser?\n\n| Hand-written recursive descent | YACC-generated LALR(1) parser |\n| :--- | :--- |\n| Verbose, error-prone for big grammars | Concise BNF-style specification |\n| No automatic conflict detection | Built-in **shift/reduce** & **reduce/reduce** conflict reports |\n| Re-implement for every grammar | Re-generate by recompiling `.y` file |\n| Difficult to maintain precedence/associativity | Declarative `%left` / `%right` / `%nonassoc` |\n\n> [!NOTE]\n> **KTU 2024 Scheme Focus:** The experiment tests your ability to (a) write the `.y` file, (b) write the paired `.l` (LEX) file, (c) compile using `yacc -d` and `lex`, and (d) demonstrate with valid/invalid inputs in the record.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Reference Sheet\n\n### 2.1 The Architecture of a YACC Program\n\nA YACC specification file (`arith.y`) is divided into three sections separated by the **`%%`** delimiter:\n\n```\n─────────  ──────────────  ─────\n Section 1   Section 2     Section 3\n(Declarations)(Grammar    (C-Code\n              Rules)       Routines)\n─────────  ──────────────  ─────\n   %%            %%           %%\n```\n\n- **Section 1 — Declarations:** C code (`%{ ... %}`), token declarations (`%token`), precedence/associativity (`%left`, `%right`, `%nonassoc`), and type declarations (`%union`, `%type`).\n- **Section 2 — Grammar Rules:** Production rules of the form  \n  `nonterminal : body1 | body2 | ... ;`  \n  Each body can carry a **semantic action** `{ ... }` that computes the value of the left-hand side (LHS) nonterminal.\n- **Section 3 — Auxiliary Routines:** C functions such as `main()`, `yyerror()`, and any user-defined helpers. These are copied verbatim into the generated `y.tab.c`.\n\n### 2.2 Token Categories Used in This Experiment\n\n| Token | Lex Regular Expression | Returned Token Name |\n| :---: | :---: | :---: |\n| Integer literal | `[0-9]+` | `NUMBER` (with `yylval` = numeric value) |\n| `+` | `\\+` | `'+'` (character literal) |\n| `-` | `\\-` | `'-'` |\n| `*` | `\\*` | `'*'` |\n| `/` | `\\/` | `'/'` |\n| `(` | `\\(` | `'('` |\n| `)` | `\\)` | `')'` |\n| Whitespace | `[ \\t]` | *(ignored, no return)* |\n| Newline | `\\n` | `'\\n'` (statement terminator) |\n| Any other char | `.` | `yytext[0]` (single-char token) |\n\n### 2.3 Operator Precedence & Associativity — The Heart of Arithmetic Parsing\n\nYACC resolves the classic ambiguity of the \"dangling else\" / \"dangling operator\" problem through **declarative precedence**. Lower declaration = **lower** precedence (binds less tightly). All operators declared on the **same line** share **equal precedence** and the declared associativity.\n\nFor arithmetic expressions the standard declaration block is:\n\n```c\n%left  '+' '-'      /* lowest precedence — addition/subtraction */\n%left  '*' '/'      /* higher precedence — multiplication/division */\n%right 'U'          /* (optional) unary minus — highest */\n```\n\n- `2 + 3 * 4` is parsed as `2 + (3 * 4)` because `*` is declared *above* `+`, hence binds tighter.\n- `10 - 4 - 3` is parsed as `(10 - 4) - 3 = 3` because `'-'` is `%left` (left-associative).\n- `8 / 2 / 2` parses as `(8 / 2) / 2 = 2` for the same reason.\n\n> [!IMPORTANT]\n> **Exam Tip:** Examiners *love* asking: *\"Why is your grammar not ambiguous? How did you resolve the shift-reduce conflict?\"* The answer is *always* — the `%left`/`%right` declarations. Without them, `yacc` would print `warning: shift/reduce conflict` and parse the expression right-to-left, which is wrong for `-` and `/`.\n\n### 2.4 KTU Formula Sheet / Cheat Sheet\n\n| Concept | Notation | Meaning | Order |\n| :---: | :---: | :---: | :---: |\n| Left-associative | `%left` | `a-b-c = (a-b)-c` | Same precedence, left-to-right |\n| Right-associative | `%right` | `a^b^c = a^(b^c)` | Same precedence, right-to-left |\n| Non-associative | `%nonassoc` | `a==b==c` is a syntax error | No chaining allowed |\n| Semantic value (LHS) | `$$` | Value being computed for LHS nonterminal | — |\n| Semantic value (i-th RHS symbol) | `$1`, `$2`, `$3` … | Value carried up from the i-th symbol in the rule | — |\n| Start symbol | First LHS in Section 2 | Root of the parse tree | — |\n| Lookahead | 1 token (LALR(1)) | Used to decide shift vs reduce | — |\n\n> [!WARNING]\n> Avoid using the vertical bar `|` inside a `$$` expression when writing on paper — it will be confused with the grammar alternation bar. Use words like *\"the value of $1 plus the value of $3\"* when transcribing.\n\n### 2.5 Real-World Engineering Utility\n\n- **Compiler Construction:** Front-ends of GCC, Clang, and most language translators use LALR parser generators (Bison, the GNU YACC, is the de-facto open-source replacement).\n- **Expression Evaluators in Calculators & Spreadsheets:** Excel formula parsers, scientific calculators, and CAS engines (Mathematica's underlying engine) all use precedence-climbing or LALR techniques.\n- **Configuration & Query Languages:** SQL parsers, JSON-with-comments tools, and even hardware description languages (Verilog, VHDL parsers) leverage YACC-style BNF.\n- **Domain-Specific Languages (DSLs):** Modern frameworks like **ANTLR** (LL(*)*) and **Bison** (LALR(1)) trace their lineage to YACC.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Code, Derivations & Analytical Matrix\n\n### 3.1 The Grammar (Production Rules)\n\nThe Context-Free Grammar (CFG) for the arithmetic expression language is:\n\n$$\n\\begin{aligned}\n\\text{stmt}  & \\rightarrow \\text{ expr } \\backslash\\text{n} \\\\\n\\text{expr}  & \\rightarrow \\text{ expr } + \\text{ expr } \\;\\vert\\; \\text{ expr } - \\text{ expr } \\\\\n            & \\rightarrow \\text{ expr } * \\text{ expr } \\;\\vert\\; \\text{ expr } / \\text{ expr } \\\\\n            & \\rightarrow \\text{ ( expr ) } \\;\\vert\\; \\text{ NUMBER}\n\\end{aligned}\n$$\n\nThe grammar is **ambiguous** on its own (no precedence), so the `%left` directives in the `.y` file resolve the ambiguity — this is *the* classical demonstration of why YACC needs precedence declarations.\n\n### 3.2 The Paired LEX File — `arith.l`\n\n```c\n%{\n#include \"y.tab.h\"      /* pulls in token definitions from yacc -d */\n#include <stdlib.h>     /* for atoi() */\n%}\n\n/* No auxiliary declarations needed */\n\n/* Regular definitions and rules */\n%%\n[0-9]+      { yylval = atoi(yytext); return NUMBER; }\n[ \\t]       ;                       /* skip whitespace */\n\\n          return '\\n';             /* statement terminator */\n.           return yytext[0];        /* single-char operators + ( ) */\n%%\n\n/* No auxiliary functions in LEX */\n```\n\n**Explanation of each rule:**\n\n- `[0-9]+` — One or more digits. `atoi()` converts the matched string to an integer which is stored in `yylval` (the default value-carrier between LEX and YACC). Returns the token `NUMBER`.\n- `[ \\t]` — Whitespace. The **empty action** `;` means the matched text is discarded and scanning continues.\n- `\\n` — Newline. Returned as the literal character `'\\n'` so that YACC can use it as a statement terminator.\n- `.` — Catch-all single character. Returns the character itself, which works for `+`, `-`, `*`, `/`, `(`, `)` because they are all single-byte tokens.\n\n### 3.3 The YACC File — `arith.y`\n\n```c\n%{\n#include <stdio.h>\n#include <stdlib.h>\nint yylex(void);          /* LEX-generated scanner */\nvoid yyerror(const char *s);  /* error routine */\n%}\n\n/* ----- Token Declarations ----- */\n%token NUMBER\n\n/* ----- Precedence (low → high) and Associativity ----- */\n%left  '+' '-'\n%left  '*' '/'\n\n/* ----- Grammar Rules ----- */\n%%\nstmt : expr '\\n'      { printf(\"Result = %d\\n\", $1); }\n     ;\n\nexpr : expr '+' expr  { $$ = $1 + $3; }\n     | expr '-' expr  { $$ = $1 - $3; }\n     | expr '*' expr  { $$ = $1 * $3; }\n     | expr '/' expr  { if ($3 == 0) { yyerror(\"Division by zero\"); $$ = 0; }\n                        else $$ = $1 / $3; }\n     | '(' expr ')'   { $$ = $2; }\n     | NUMBER         { $$ = $1; }\n     ;\n%%\n\n/* ----- Auxiliary Routines ----- */\nvoid yyerror(const char *s) {\n    fprintf(stderr, \"Syntax Error: %s\\n\", s);\n}\n\nint main(void) {\n    printf(\"Enter an arithmetic expression (Ctrl-D to exit):\\n\");\n    return yyparse();   /* generated parser entry point */\n}\n```\n\n### 3.4 Line-by-Line Semantic Analysis\n\n| Line / Rule | What `$$`, `$1`, `$2`, `$3` mean | Type of the values |\n| :--- | :--- | :--- |\n| `expr '+' expr { $$ = $1 + $3; }` | `$$` = result of *this* expr rule. `$1` = value of left sub-expr. `$3` = value of right sub-expr. `$2` is the `'+'` token — no value. | `int` (YACC's default for `yylval`) |\n| `'(' expr ')' { $$ = $2; }` | `$$` takes the value of the inner expr. `$1` and `$3` are the brackets, no value. | `int` |\n| `NUMBER { $$ = $1; }` | `$$` is set to the integer `atoi()` produced in the LEX action. | `int` |\n| `stmt : expr '\\n' { printf(... $1 ...); }` | The whole expression's value is `$1` (the only non-token symbol). It is printed when the statement reduces. | `int` |\n\n> [!IMPORTANT]\n> The **default `yylval` type is `int`**. If you want to support *real numbers (floats)* you must declare `%union { int ival; double dval; }` and annotate each token/rule with its type, e.g., `%token <ival> NUMBER` and `expr : ... { $$ = $1 + $3; }` where `expr` is declared `%type <ival>`. For the KTU base experiment, **integers are sufficient** and avoid union complexity.\n\n### 3.5 Step-by-Step Build and Run Procedure\n\n```bash\n# Step 1: Generate the parser (y.tab.c and y.tab.h) from arith.y\nyacc -d arith.y\n\n# Step 2: Generate the scanner (lex.yy.c) from arith.l\nlex arith.l\n\n# Step 3: Compile everything together\ngcc lex.yy.c y.tab.c -o arith -ll    # -ll links the libfl LEX runtime\n\n# Step 4: Run\n./arith\n```\n\n> [!NOTE]\n> On modern Linux, `lex` may already be `flex` (a compatible FOSS replacement). The library flag is `-lfl` on some systems. If you get `undefined reference to yywrap`, append `%option noyywrap` at the top of the `.l` file.\n\n### 3.6 Analytical Matrix — Sample Inputs and Expected Outputs\n\n| # | Input | Expected Output | Reason |\n| :---: | :---: | :---: | :---: |\n| 1 | `2+3\\n` | `Result = 5` | Simple addition. |\n| 2 | `10-4-3\\n` | `Result = 3` | Left-associative subtraction: `(10-4)-3`. |\n| 3 | `2+3*4\\n` | `Result = 14` | Precedence: `2+(3*4)`, not `(2+3)*4`. |\n| 4 | `(2+3)*4\\n` | `Result = 20` | Parentheses override precedence. |\n| 5 | `8/2/2\\n` | `Result = 2` | Left-associative division: `(8/2)/2`. |\n| 6 | `((7-3)*(2+1))\\n` | `Result = 12` | Nested parentheses work. |\n| 7 | `2++3\\n` | `Syntax Error` | Two operators in a row is not a valid expression. |\n| 8 | `(2+3\\n` | `Syntax Error` | Unmatched parenthesis. |\n| 9 | `5/0\\n` | `Syntax Error: Division by zero` (but parser succeeds, semantic error) | Caught in the action, not the grammar. |\n| 10 | `12+3*4-6/2\\n` | `Result = 21` | `12 + (3*4) - (6/2) = 12 + 12 - 3 = 21`. |\n\n### 3.7 Detailed Trace of the Parse for `2+3*4` (Shift/Reduce Walk)\n\nThe input is streamed as tokens: `NUMBER(2)`, `'+'`, `NUMBER(3)`, `'*'`, `NUMBER(4)`, `'\\n'`. The parser maintains a **stack** and consults the next token (look-ahead) at every step.\n\n| Step | Stack (top on right) | Look-ahead | Action |\n| :---: | :--- | :---: | :--- |\n| 1 | `$` | `2` | Shift `NUMBER(2)`, push value 2. |\n| 2 | `$ 2` | `+` | Reduce `NUMBER → expr` (by `expr : NUMBER`). Push `expr` with value 2. |\n| 3 | `$ expr` | `+` | Shift `'+'`. |\n| 4 | `$ expr +` | `3` | Shift `NUMBER(3)`. |\n| 5 | `$ expr + 3` | `*` | Reduce `NUMBER → expr`. |\n| 6 | `$ expr + expr` | `*` | **Decision point:** shift or reduce `expr+expr`? Look-ahead `*` has *higher* precedence than `+` (declared later in `%left`), so **shift** the `*`. |\n| 7 | `$ expr + expr *` | `4` | Shift `NUMBER(4)`. |\n| 8 | `$ expr + expr * 4` | `\\n` | Reduce `NUMBER → expr`. |\n| 9 | `$ expr + expr * expr` | `\\n` | Reduce `expr * expr → expr` (semantic: `$$=3*4=12`). |\n| 10 | `$ expr + expr` | `\\n` | Reduce `expr + expr → expr` (semantic: `$$=2+12=14`). |\n| 11 | `$ expr` | `\\n` | Shift `'\\n'`. |\n| 12 | `$ expr \\n` | EOF | Reduce `expr '\\n' → stmt`, print `Result = 14`. |\n| 13 | `$` | EOF | **Accept!** |\n\nThe final `$$` after the last reduction is **14** — exactly the correct mathematical answer.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 End-to-End Compilation Pipeline (LEX → YACC → Binary)\n\n```mermaid\nflowchart LR\n    A[\"arith.l<br/>LEX source\"]\n    B[\"arith.y<br/>YACC source\"]\n    C[\"lex.yy.c<br/>Scanner in C\"]\n    D[\"y.tab.c<br/>Parser in C\"]\n    E[\"y.tab.h<br/>Token IDs header\"]\n    F[\"arith<br/>Executable binary\"]\n    G[\"User Input<br/>e.g. 2+3*4\"]\n    H[\"Output<br/>Result = 14\"]\n\n    A -- \"lex / flex\" --> C\n    B -- \"yacc -d\" --> D\n    B -- \"yacc -d\" --> E\n    C -- \"gcc includes y.tab.h\" --> F\n    D -- \"gcc compiles\" --> F\n    E -- \"gcc includes\" --> F\n    G -- \"stdin\" --> F\n    F -- \"stdout\" --> H\n\n    style A fill:#fff4e1,stroke:#cc6600\n    style B fill:#e1f0ff,stroke:#003366\n    style F fill:#d4f4dd,stroke:#006600\n    style H fill:#ffe1e1,stroke:#990000\n```\n\n### 4.2 Parse Tree for the Expression `(2+3)*4`\n\nThis tree is built by the LALR(1) parser; each internal node is a grammar production, and the leaves are the input tokens.\n\n```mermaid\ngraph TD\n    S[\"stmt\"]\n    E1[\"expr<br/>= 20\"]\n    NL1[\"'\\\\n'\"]\n    E2[\"expr<br/>= 20\"]\n    T1[\"'*'\"]\n    E3[\"expr<br/>= 12\"]\n    E4[\"expr<br/>= 8\"]\n    PLO[\"'('\"]\n    E5[\"expr<br/>= 5\"]\n    PR1[\"')'\"]\n    PL2[\"'('\"]\n    E6[\"expr<br/>= 2\"]\n    P2[\"'+'\"]\n    E7[\"expr<br/>= 3\"]\n    PR2[\"')'\"]\n    N1[\"NUMBER(4)\"]\n    N2[\"NUMBER(2)\"]\n    N3[\"NUMBER(3)\"]\n\n    S --> E1\n    S --> NL1\n    E1 --> E2\n    E1 --> T1\n    E1 --> E3\n    E2 --> E4\n    E2 --> PLO\n    E2 --> PR1\n    E4 --> PL2\n    E4 --> E6\n    E4 --> P2\n    E4 --> E7\n    E4 --> PR2\n    E6 --> N2\n    E7 --> N3\n    E3 --> N1\n\n    style S fill:#ffe1e1,stroke:#990000\n    style E1 fill:#fff4e1,stroke:#cc6600\n    style E2 fill:#e1f0ff,stroke:#003366\n    style E3 fill:#e1f0ff,stroke:#003366\n    style E4 fill:#d4f4dd,stroke:#006600\n    style E5 fill:#fff4e1,stroke:#cc6600\n    style E6 fill:#fff4e1,stroke:#cc6600\n    style E7 fill:#fff4e1,stroke:#cc6600\n    style N1 fill:#f0f0f0,stroke:#333333\n    style N2 fill:#f0f0f0,stroke:#333333\n    style N3 fill:#f0f0f0,stroke:#333333\n```\n\n### 4.3 Precedence Ladder (Block-Level View)\n\n```mermaid\nflowchart TB\n    L0[\"Level 0 (Lowest) — Addition, Subtraction<br/>%left '+' '-'\"]\n    L1[\"Level 1 — Multiplication, Division<br/>%left '*' '/'\"]\n    L2[\"Level 2 (Highest) — Unary Minus / Parens<br/>%right 'U'  OR  '(' expr ')'\"]\n\n    L0 -->|\"binds less tightly than\"| L1\n    L1 -->|\"binds less tightly than\"| L2\n\n    style L0 fill:#ffe1e1,stroke:#990000\n    style L1 fill:#fff4e1,stroke:#cc6600\n    style L2 fill:#d4f4dd,stroke:#006600\n```\n\n### 4.4 YACC File Section Map\n\n```mermaid\nflowchart TB\n    Y[\"arith.y file\"]\n    S1[\"Section 1: Declarations<br/>%{ C code %}<br/>%token NUMBER<br/>%left '+' '-'<br/>%left '*' '/'\"]\n    S2[\"Section 2: Grammar Rules<br/>stmt : expr '\\\\n' { ... }<br/>expr : expr '+' expr { ... }<br/>       | expr '-' expr { ... }<br/>       | expr '*' expr { ... }<br/>       | expr '/' expr { ... }<br/>       | '(' expr ')' { ... }<br/>       | NUMBER { ... }\"]\n    S3[\"Section 3: C Routines<br/>void yyerror(const char *s) { ... }<br/>int main(void) { return yyparse(); }\"]\n\n    Y --> S1\n    S1 -->|\"%%\"| S2\n    S2 -->|\"%%\"| S3\n\n    style S1 fill:#e1f0ff,stroke:#003366\n    style S2 fill:#fff4e1,stroke:#cc6600\n    style S3 fill:#d4f4dd,stroke:#006600\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n### **Part A — Short Answer Questions (3 Marks each)**\n\n> **[KTU University Exam – Model Question 1]**  `[3 Marks]`\n> **Q.** What is YACC? Mention any two differences between YACC and LEX.\n\n**Model Answer (for 3 marks):**\n- **YACC (Yet Another Compiler Compiler)** is a parser generator that takes a grammar in BNF/Backus-Naur Form and generates a C parser using the LALR(1) parsing algorithm. *(1 mark)*\n- Two differences: *(2 marks)*\n  1. **Purpose:** LEX is a *lexer/scanner generator* — it converts input characters into *tokens*. YACC is a *parser generator* — it takes those tokens and checks whether they form a *valid syntactic structure* (a sentence of the grammar).\n  2. **Output:** LEX produces a finite automaton (transition table) that recognises regular expressions. YACC produces a push-down automaton (parse stack + LALR tables) that recognises context-free grammars.\n  3. **Input language:** LEX rules are *regular expressions*. YACC rules are *production rules* of the form `A : B C D ;`.\n\n> **[KTU University Exam – Model Question 2]**  `[3 Marks]`\n> **Q.** Define *shift-reduce conflict* in YACC. How is it resolved in an arithmetic-expression grammar?\n\n**Model Answer (for 3 marks):**\n- A *shift-reduce conflict* occurs when the parser, holding a handle on top of the stack and looking at the next input token, cannot uniquely decide whether to **shift** the token onto the stack or **reduce** the handle by some production. YACC reports this as `yacc: warning: … shift/reduce conflict`. *(1 mark)*\n- In the arithmetic-expression grammar, the conflict occurs with input like `2+3*4` after seeing `expr + expr` and looking at `*`: do we reduce `2+3` first, or shift `*`? *(1 mark)*\n- It is resolved **declaratively** by the precedence and associativity lines `%left '+' '-' / %left '*' '/'` in the declaration section. Because `*` has higher precedence, the parser *shifts* `*`, binding it tightly to `3` and `4`. *(1 mark)*\n\n---\n\n### **Part B — Long Answer Questions (14 Marks — Internal Choice)**\n\n> **[KTU University Exam – Model Question A1]**  `[14 Marks]`\n> **(a)** Write a YACC specification to recognize and evaluate arithmetic expressions consisting of integers, the binary operators `+`, `-`, `*`, `/`, and parentheses `(` `)`. The expression is terminated by a newline. State the precedence and associativity rules used.  `[7 Marks]`\n> **(b)** Write the corresponding LEX program. Show the steps to compile and run, and demonstrate three sample inputs (one valid with parentheses overriding precedence, one triggering a syntax error, and one demonstrating left-associativity of subtraction).  `[7 Marks]`\n\n#### Model Solution (a) — 7 Marks\n\n**Step 1 — File `arith.y`:** *(5 marks)*\n\n```c\n%{\n#include <stdio.h>\nint yylex(void);\nvoid yyerror(const char *s);\n%}\n\n%token NUMBER\n%left  '+' '-'\n%left  '*' '/'\n\n%%\nstmt : expr '\\n'    { printf(\"Result = %d\\n\", $1); }\n     ;\n\nexpr : expr '+' expr  { $$ = $1 + $3; }\n     | expr '-' expr  { $$ = $1 - $3; }\n     | expr '*' expr  { $$ = $1 * $3; }\n     | expr '/' expr  { if ($3 == 0) yyerror(\"Divide by zero\");\n                        else $$ = $1 / $3; }\n     | '(' expr ')'   { $$ = $2; }\n     | NUMBER         { $$ = $1; }\n     ;\n%%\n\nvoid yyerror(const char *s) {\n    fprintf(stderr, \"Syntax Error: %s\\n\", s);\n}\nint main(void) {\n    printf(\"Enter expression:\\n\");\n    return yyparse();\n}\n```\n\n**Step 2 — Precedence/associativity statement:** *(2 marks)*\n\n- `+` and `-` are declared *first* → **lowest precedence**.\n- `*` and `/` are declared *second* → **higher precedence** (bind more tightly).\n- Both lines use `%left` → **left-associative** (so `a-b-c = (a-b)-c` and `a/b/c = (a/b)/c`).\n- The production `expr : expr op expr` is **ambiguous on its own**; the precedence declarations resolve all conflicts — YACC will report *zero* conflicts at compile time.\n\n---\n\n#### Model Solution (b) — 7 Marks\n\n**Step 1 — File `arith.l`:** *(2 marks)*\n\n```c\n%{\n#include \"y.tab.h\"\n#include <stdlib.h>\n%}\n\n%%\n[0-9]+   { yylval = atoi(yytext); return NUMBER; }\n[ \\t]    ;\n\\n       return '\\n';\n.        return yytext[0];\n%%\n```\n\n**Step 2 — Build steps:** *(2 marks)*\n\n```bash\nyacc -d arith.y        # produces y.tab.c and y.tab.h\nlex   arith.l          # produces lex.yy.c\ngcc   y.tab.c lex.yy.c -o arith -ll\n./arith\n```\n\n**Step 3 — Three sample runs:** *(3 marks — 1 mark each)*\n\n| Input | Output | What it demonstrates |\n| :--- | :--- | :--- |\n| `(2+3)*4` | `Result = 20` | **Parentheses override precedence** — multiplication is forced to happen on the sum first, yielding `5*4=20` instead of `2+(3*4)=14`. |\n| `2++3` | `Syntax Error` | **Invalid input** — two operators in a row. The parser cannot reduce or shift to a valid handle, so `yyerror` is called. |\n| `10-4-3` | `Result = 3` | **Left-associativity** — parses as `(10-4)-3 = 3`, NOT `10-(4-3) = 9`. This is enforced by `%left '-'`. |\n\n---\n\n> **[KTU University Exam – Model Question B1 (Internal Choice Alternative)]**  `[14 Marks]`\n> **(a)** Explain the role of semantic actions in a YACC specification. Using the production `expr : expr '+' expr`, describe how the value of the left-hand side `$$` is computed from `$1` and `$3`. Why is `$2` not used in the addition?  `[7 Marks]`\n> **(b)** Modify the YACC specification of Q.A1 to additionally support a **unary minus** operator (e.g., `-5+3`, `-(2+3)*4`). Show the precedence declaration, the new grammar rule, and verify the output for the inputs `-5+3` and `-(2+3)*4`.  `[7 Marks]`\n\n#### Model Solution (a) — 7 Marks\n\n- **Role of semantic actions:** They are the C code blocks `{ ... }` attached to each production in Section 2 of the YACC file. They execute *exactly when* the parser reduces that production. They are used to **build the parse tree, compute values, generate intermediate code, emit error messages, or update symbol tables** — i.e., they give the parser a *meaning* (semantics) for each syntactically valid phrase. *(3 marks)*\n- **For `expr : expr '+' expr { $$ = $1 + $3; }`:** When this rule is reduced, the parser has already computed the integer values of the *left* sub-expression (call it `L`) and the *right* sub-expression (call it `R`). The semantic action assigns their sum to `$$`, which becomes the value of the *new* `expr` node being constructed. So if the input is `2+3*4`, when the parser finally reduces the `+`, the LHS of the `+` is `expr → 2` (value 2) and the RHS is `expr → expr*expr` (value 12), giving `$$ = 2 + 12 = 14`. *(2 marks)*\n- **Why `$2` is not used:** `$2` corresponds to the *second symbol on the right-hand side* of the production, which is the **token `'+'`** itself. Tokens do not carry numeric values in this grammar (no `%union` is declared, so `yylval` is never assigned for the `+` character). Even if it did, adding a non-numeric token to itself is meaningless. Only the two *expression operands* contribute numerical values. *(2 marks)*\n\n#### Model Solution (b) — 7 Marks\n\n**Precedence block changes** — Unary minus must bind *tighter* than `*` and `/`: *(2 marks)*\n\n```c\n%left  '+' '-'\n%left  '*' '/'\n%right 'U'              /* 'U' is a fictitious token marking a unary-minus occurrence */\n```\n\n**New grammar rule for unary minus:** *(3 marks)*\n\n```c\nexpr : '-' expr %prec 'U'   { $$ = -$2; }\n     | expr '+' expr        { $$ = $1 + $3; }\n     | expr '-' expr        { $$ = $1 - $3; }\n     | expr '*' expr        { $$ = $1 * $3; }\n     | expr '/' expr        { if ($3 == 0) yyerror(\"Divide by zero\"); else $$ = $1 / $3; }\n     | '(' expr ')'         { $$ = $2; }\n     | NUMBER               { $$ = $1; }\n     ;\n```\n\nThe directive **`%prec 'U'`** overrides the default precedence of this particular production with that of the token `'U'`, which is `%right` and declared *above* `*` and `/`, ensuring `-5+3` is parsed as `(-5)+3 = -2`, not `-(5+3) = -8`.\n\n**Verification:** *(2 marks)*\n\n| Input | Parse Tree | Output |\n| :---: | :--- | :---: |\n| `-5+3` | `((-5) + 3)` | `Result = -2` |\n| `-(2+3)*4` | `(-(2+3)) * 4 = (-5)*4` | `Result = -20` |\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfalls — Common Mark-Deducting Mistakes**\n> 1. **Forgetting `%left` / `%right` lines** → YACC prints `shift/reduce conflict` warnings and your output becomes *wrong* (e.g., `2+3*4` returns `20` instead of `14`). Examiner will give 0 for the precedence part.  *(Lose up to 2 marks)*\n> 2. **Confusing `$1`, `$2`, `$3` indexing** → `$i` counts *all* symbols on the RHS including tokens. In `expr : expr '+' expr`, `expr` is `$1`, `'+'` is `$2`, `expr` is `$3`. Writing `$$ = $1 + $2` is a common mistake and produces garbage.  *(Lose up to 1 mark)*\n> 3. **Missing the `'\\n'` rule** → The parser will read multiple expressions without ever printing a result. Always end the top-level production with a newline token so each line is independently evaluated.  *(Lose up to 1 mark)*\n> 4. **Not including `#include \"y.tab.h\"` in the LEX file** → Tokens like `NUMBER` will be undeclared, giving cryptic compile errors.  *(Lose up to 1 mark)*\n> 5. **Using `yytext` after it has been overwritten** → `atoi(yytext)` must be called *inside* the action, immediately. Storing `yytext` in a global pointer and using it later is a classic bug.\n> 6. **Omitting the `void yyerror(const char *s)` function** → Linker error: `undefined reference to yyerror`. Always define it (even if it just prints).\n\n---\n\n### Topic Recap & Important Things to Remember\n\n- **YACC** generates an **LALR(1) parser** from a **BNF grammar**; **LEX** generates a **scanner** from **regular expressions**. They are a pair.\n- A YACC file has **three sections** separated by `%%`: *Declarations*, *Grammar Rules*, *C Routines*.\n- Use **`%token NAME`** to declare non-character tokens and **`%left` / `%right` / `%nonassoc`** to declare precedence (lower in the file = lower precedence; same line = same precedence with declared associativity).\n- Arithmetic grammar template:  \n  `expr : expr op expr | ( expr ) | NUMBER` — *must* be accompanied by `%left` directives or you will get shift/reduce conflicts.\n- Semantic action placeholders: `$$` = LHS value, `$1` `$2` … `$N` = values of the 1st, 2nd, … Nth RHS symbols.\n- Top-level rule (`stmt`) should consume `'\\n'` so each input line is a self-contained evaluation.\n- Common build command: `yacc -d file.y && lex file.l && gcc y.tab.c lex.yy.c -o out -ll`.\n- Errors fall into two categories: **syntax errors** (caught by the parser → `yyerror`) and **semantic errors** (e.g., division by zero — must be checked in the action).\n- Real-world equivalents: **Bison** (GNU), **BYACC** (Berkeley), **ANTLR** (LL(*)*, Java/Cpp).\n- **Default `yylval` is `int`** — for floats use `%union { int ival; double dval; }` and tag every token/rule with its field (`<ival>`, `<dval>`).\n- Always **declare `yyerror` in the C prolog** (`void yyerror(const char *s);`) and **define it in Section 3** to avoid linker errors.\n- **Unary operators** use the `%prec` directive to inherit precedence from a fictitious token declared at the desired level.\n- KTU record deliverables: the `.y` source, the `.l` source, a build script or build steps, and a minimum of **3 valid + 2 invalid** I/O demonstrations with outputs.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-engineering-7-pccsl607-mExperiment 2-t4",
          "title": "Implementation of Calculator using LEX and YACC",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL607 - SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Implementation of Calculator using LEX and YACC\n\n<!-- SECTION_1_START -->\n## SECTION 1: Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Academic Definition\n\n> [!NOTE]\n> **Definition (KTU 2024 Syllabus Standard):**\n> A **LEX/YACC-based Calculator** is a software application that uses a *lexical analyzer* generated by **LEX (Lexical Analyzer Generator)** to tokenize arithmetic input expressions, and a *parser/evaluator* generated by **YACC (Yet Another Compiler-Compiler)** to interpret the grammar and evaluate the expression according to defined precedence and associativity rules.\n\nThe implementation is typically split into **three source files**:\n1. **`calc.l`** — The **LEX specification file** containing token declarations, regular expressions, and C actions executed when each pattern matches.\n2. **`calc.y`** — The **YACC grammar file** containing token declarations, precedence rules, type definitions (`%union`/`%token`/`%type`), grammar productions, and semantic actions.\n3. **`calc.c` / `main()`** — A driver program that repeatedly calls `yyparse()` until input ends or an error occurs.\n\n> [!IMPORTANT]\n> **KTU 2024 Learning Outcome Mapping (CO2):**\n> This experiment maps to **CO2: *Implement lexical analyzer and parser using LEX and YACC tools***. Students must be able to write both the `.l` and `.y` files from scratch, compile them using the `lex`/`yacc` (or `flex`/`bison`) toolchain, and trace token-to-parse-tree evaluation for any given input expression.\n\n### 1.2 Intuitive Analogy (Real-World View)\n\nThink of a **human waiter at a restaurant who speaks two languages**:\n\n- **LEX** is like the waiter **reading your handwritten order** (\"2 pizzas + 1 coke\") and translating it into a structured list of items: `NUMBER: 2`, `WORD: pizzas`, `OPERATOR: +`, `NUMBER: 1`, `WORD: coke`. The waiter doesn't understand the *meaning* — only the *shape* of the words.\n\n- **YACC** is like the **kitchen manager** who takes the structured list, understands the *grammar of an order* (you can't say \"+ 2 pizzas\" — that's invalid), and computes the final bill by following a recipe of rules: \"multiply each item by its quantity, then sum totals.\"\n\nTogether, they turn **\"3 + 4 * 2\"** into the number **11** (not 14!) because YACC knows `*` binds tighter than `+` — exactly like the manager knows the dessert comes *after* the main course, not before.\n\n### 1.3 Key Tools & Reserved Identifiers\n\n| Tool / Identifier | Role | Origin |\n|---|---|---|\n| **LEX / Flex** | Generates `lex.yy.c` (the DFA-based scanner) | Eric Schmidt & Mike Lesk, 1975 |\n| **YACC / Bison** | Generates `y.tab.c` (the LALR parser) | Stephen C. Johnson, AT&T Bell Labs, 1973 |\n| `yylex()` | Function called by parser to fetch next token | **yy** = \"yet yet\" convention |\n| `yyparse()` | Function that drives the entire parser | Auto-generated |\n| `yylval` | Variable holding the *value* of the current token | Union type from `%union` |\n| `yytext` | Pointer to the matched input string | Set by `yylex()` |\n| `yyin` | Input file pointer (defaults to `stdin`) | Global `FILE *` |\n\n> [!IMPORTANT]\n> In modern Linux labs (Ubuntu/Fedora used by KTU), use **Flex** and **Bison** (GNU replacements). Commands are `flex calc.l` and `bison -d calc.y` (the `-d` flag generates `y.tab.h` containing token macros that the `.l` file `#include`s).\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## SECTION 2: Deep Theoretical Analysis & High-Yield Reference Sheet\n\n### 2.1 Architecture of a LEX/YACC Application\n\nA LEX/YACC calculator follows a strict **3-stage pipeline**:\n\n**Stage 1 — Lexical Analysis (Tokenization):**\nThe LEX-generated scanner reads the raw input character stream and groups characters into **lexemes** (the matched substrings) mapped to **token classes** (the categories). For a calculator, the token classes are minimal:\n\n| Token Class | Lexeme Examples | Regular Expression (LEX) |\n|---|---|---|\n| `NUMBER` | `42`, `3.14`, `0`, `.5` | `[0-9]+(\\.[0-9]+)?` |\n| `PLUS` | `+` | `\\+` |\n| `MINUS` | `-` | `-` |\n| `MUL` | `*` | `\\*` |\n| `DIV` | `/` | `/` |\n| `LPAREN` | `(` | `\\(` |\n| `RPAREN` | `)` | `\\)` |\n| `NEWLINE` | `\\n` | `\\n` |\n| (whitespace) | ` `, `\\t` | `[ \\t]+` |\n\n**Stage 2 — Syntax Analysis (Parsing):**\nThe YACC-generated LALR(1) parser consumes the token stream and builds a **parse tree** (or directly evaluates via semantic actions). Grammar rules for a simple calculator are:\n\n$$\n\\begin{aligned}\nS   &\\rightarrow E\\ \\text{NEWLINE} \\\\\nE   &\\rightarrow E\\ \\text{PLUS}\\ T \\mid E\\ \\text{MINUS}\\ T \\mid T \\\\\nT   &\\rightarrow T\\ \\text{MUL}\\ F \\mid T\\ \\text{DIV}\\ F \\mid F \\\\\nF   &\\rightarrow \\text{LPAREN}\\ E\\ \\text{RPAREN} \\mid \\text{NUMBER}\n\\end{aligned}\n$$\n\nThis is the classic **arithmetic grammar** that gives `*` and `/` higher precedence than `+` and `-` and enforces **left-associativity**.\n\n**Stage 3 — Evaluation (Semantic Action):**\nEach grammar rule has a C code block attached. When the parser reduces by a rule, the action executes. The value is propagated upward via `$$` (the result of the current production) and `$1`, `$2`, `$3` (the values of symbols on the right-hand side).\n\n### 2.2 Operator Precedence and Associativity\n\n> [!NOTE]\n> **Why does the grammar enforce precedence?**\n> The way the grammar is *layered* (E for +/- level, T for */ level, F for parentheses/atoms) creates an implicit precedence. An expression like `3 + 4 * 5` must be parsed as `3 + (4 * 5)`, not `(3 + 4) * 5`. The `T` non-terminal \"binds tighter\" because it's reached only after the `E` level is closed.\n\n| Operator | Precedence Level | Associativity | Why? |\n|---|---|---|---|\n| `+`, `-` | **Lowest** (1) | Left | `a - b - c` = `(a - b) - c` |\n| `*`, `/` | **Medium** (2) | Left | `a / b / c` = `(a / b) / c` |\n| `(`, `)` | **Highest** (3) | N/A | Grouping overrides everything |\n\n### 2.3 The `%union`, `%token`, and `%type` Declarations\n\nThese are **mandatory** in the `.y` file when you want to pass numeric values between LEX and YACC:\n\n- **`%union { int ival; float fval; }`** — Declares a C union that will be the *type* of `yylval`. Since `yylval` must be able to hold different token types, a union is used.\n- **`%token <ival> NUMBER`** — Declares `NUMBER` as a token whose semantic value is an **integer** in the `ival` field of the union.\n- **`%type <fval> E T F`** — Declares the non-terminals `E`, `T`, `F` as carrying **floating-point** values.\n- **`%left PLUS MINUS`** and **`%left MUL DIV`** — Declares tokens and their precedence (later = higher). `LEFT` specifies left-associativity, which is used by YACC's conflict resolver to break shift/reduce conflicts in our favor.\n\n### 2.4 The Semantic Value Flow (`$$` and `$n`)\n\n> [!IMPORTANT]\n> **The Golden Rule of YACC Actions:**\n> If a production has `n` symbols on the right-hand side, the semantic values are accessed as `$1`, `$2`, ..., `$n` (one-indexed from the left). The *result* of the reduction is stored in `$$` (double dollar). This is how values \"flow\" up the parse tree.\n\n**Example:** For the production `E : E PLUS T`, the action is:\n```c\n{ $$ = $1 + $3; }\n```\nHere, `$1` is the value of the left `E`, `$3` is the value of `T`, and `$$` is the new value of the resulting `E` (left `E` + `T`).\n\n### 2.5 KTU High-Yield Compilation & Execution Cheat Sheet\n\n> [!NOTE]\n> This is the **exact sequence** KTU expects you to write in your lab record. Memorize it.\n\n| Step | Command | Purpose | Output File |\n|---|---|---|---|\n| 1 | `bison -d calc.y` | Generate parser + header | `y.tab.c`, `y.tab.h` |\n| 2 | `flex calc.l` | Generate scanner | `lex.yy.c` |\n| 3 | `gcc lex.yy.c y.tab.c -o calc -ll` | Compile & link | `calc` (executable) |\n| 4 | `./calc` | Run interactively | Prompts for input |\n| 5 | `Ctrl + D` | Send EOF | Exits program |\n\n> [!WARNING]\n> On some systems, the math library is required: use `gcc lex.yy.c y.tab.c -o calc -ll -lm` if you use functions like `pow()` or `sqrt()`.\n\n### 2.6 Real-World Engineering Utility\n\nThe LEX/YACC pattern is the **backbone of every compiler and interpreter in industry**:\n- **GCC's parser** for C/C++ uses a hand-written recursive-descent, but its early versions used YACC.\n- **PHP's Zend Engine**, **Python's CPython parser**, and **Ruby's MRI** all use parser generators derived from YACC technology (LALR/GLR).\n- **Database query engines** (PostgreSQL's `gram.y`) use Bison.\n- **Network protocol analyzers** (Wireshark dissectors) use LEX/YACC to parse packet grammars.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## SECTION_3: Step-by-Step Code Walkthrough & Analytical Trace\n\n### 3.1 Complete Source Code (Three Files)\n\n> [!NOTE]\n> Below is the **complete, runnable, KTU-expected implementation**. Type it exactly, compile using the steps in Section 2.5, and test with expressions like `2+3*4`, `(2+3)*4`, `10/3`, `2.5*4`.\n\n#### File 1: `calc.l` (The LEX Specification)\n\n```c\n%{\n#include \"y.tab.h\"\n#include <stdio.h>\n%}\n\n/* Recognize integers and floats */\n%%\n[0-9]+          { yylval.ival = atoi(yytext); return NUMBER; }\n[0-9]+\\.[0-9]+  { yylval.fval  = atof(yytext); return FNUM;   }\n[ \\t]           { /* skip whitespace */       }\n\\n              { return '\\n';                }\n\\+              { return PLUS;                }\n\\-              { return MINUS;               }\n\\*              { return MUL;                 }\n\\/              { return DIV;                 }\n\\(              { return LPAREN;              }\n\\)              { return RPAREN;              }\n.               { printf(\"Unknown char %c\\n\", *yytext); }\n%%\n\nint yywrap() { return 1; }\n```\n\n**Line-by-Line Explanation:**\n\n- **`%{ ... %}`** — LEX embedded code block. This C code is copied verbatim to the top of `lex.yy.c`. We `#include \"y.tab.h\"` so that the token macros (`NUMBER`, `PLUS`, etc.) declared in the YACC file are visible here.\n- **`[0-9]+`** — Matches one or more digits. `atoi(yytext)` converts the matched string `\"42\"` to the integer `42`, stores it in `yylval.ival`, and the `return NUMBER;` sends the token to the parser.\n- **`[0-9]+\\.[0-9]+`** — Matches floating-point numbers like `3.14`. We use a *separate token* `FNUM` so we can store it as a `float` in `yylval.fval`.\n- **`[ \\t]`** — Matches spaces/tabs but the action is empty (just skip).\n- **`\\n`** — Newline is special: it's the **end-of-expression marker**. We return it as character `'\\n'` (a literal single character, not a token macro).\n- **`\\+`** — The backslash is required in LEX regular expressions for metacharacters like `+`, `*`, `(`, `)`, `?`. Without it, `+` means \"one or more\" (Kleene plus).\n- **`yywrap()`** — Returns `1` to tell LEX \"no more input files, stop.\" Required at the end of every LEX file.\n\n#### File 2: `calc.y` (The YACC Grammar)\n\n```c\n%{\n#include <stdio.h>\n#include <stdlib.h>\nint yylex(void);\nint yyerror(const char *s);\n%}\n\n/* --- Declare the union to hold semantic values --- */\n%union {\n    int   ival;\n    float fval;\n}\n\n/* --- Token declarations (with their union field) --- */\n%token <ival> NUMBER\n%token <fval> FNUM\n%token PLUS MINUS MUL DIV LPAREN RPAREN\n\n/* --- Non-terminals carry float values --- */\n%type <fval> E T F\n\n/* --- Precedence: lowest first, left associative --- */\n%left PLUS MINUS\n%left MUL DIV\n%left LPAREN\n%left UMINUS\n\n/* --- Grammar rules --- */\n%%\nS    : E '\\n'        { printf(\"= %g\\n\", $1); }\n     ;\n\nE    : E PLUS T      { $$ = $1 + $3; }\n     | E MINUS T     { $$ = $1 - $3; }\n     | T             { $$ = $1;    }\n     ;\n\nT    : T MUL F       { $$ = $1 * $3; }\n     | T DIV F       { $$ = $1 / $3; }\n     | F             { $$ = $1;    }\n     ;\n\nF    : LPAREN E RPAREN  { $$ = $2; }\n     | NUMBER           { $$ = (float)$1; }\n     | FNUM             { $$ = $1; }\n     | MINUS F  %prec UMINUS  { $$ = -$2; }\n     ;\n%%\n\n/* --- Main driver --- */\nint main(void) {\n    printf(\"Simple Calculator (Ctrl+D to exit)\\n> \");\n    return yyparse();\n}\n\nint yyerror(const char *s) {\n    fprintf(stderr, \"Error: %s\\n\", s);\n    return 0;\n}\n```\n\n**Line-by-Line Explanation of Key Productions:**\n\n- **`S : E '\\n' { printf(\"= %g\\n\", $1); }`**\n  The *start symbol* `S` accepts an expression `E` followed by a newline. When the rule is reduced, we **print the result** in `%g` format (compact float). `$1` is the float value of `E`.\n\n- **`E : E PLUS T { $$ = $1 + $3; }`**\n  Recursive rule that handles left-associative chains: `1+2+3` becomes `(1+2)+3`. The action adds the left `E` to `T` and stores the result in `$$`.\n\n- **`E : T`**\n  Base case: an `E` can be a single `T`. This is what allows the recursion in the previous rule to terminate.\n\n- **`F : MINUS F %prec UMINUS`**\n  Handles **unary minus** (e.g., `-5`, `--x`). The `%prec UMINUS` directive forces this rule to have the precedence of the dummy `UMINUS` token (declared in the precedence block *after* `LPAREN`), so unary minus binds tighter than `*` and `/`. Without this, the parser would conflict on expressions like `-3*4`.\n\n### 3.2 Trace of a Sample Expression: `3 + 4 * 2`\n\nLet's trace **tokenization** (LEX stage) first:\n\n| Input | Token | yylval |\n|---|---|---|\n| `3` | `NUMBER` | `ival = 3` |\n| ` ` | (skipped) | — |\n| `+` | `PLUS` | — |\n| ` ` | (skipped) | — |\n| `4` | `NUMBER` | `ival = 4` |\n| ` ` | (skipped) | — |\n| `*` | `MUL` | — |\n| ` ` | (skipped) | — |\n| `2` | `NUMBER` | `ival = 2` |\n| `\\n` | `'\\n'` | — |\n\nNow the **parser trace** (YACC stage) using the grammar:\n\n1. Parser sees `NUMBER 3` → shifts to stack, state expects `E`.\n2. Parser sees `PLUS` → by rule `E : T`, reduce `NUMBER` to `F` ($$\\$=3), then `F` to `T` ($$\\$=3), then `T` to `E` ($$\\$=3). Stack: `[E=3]`. Token: `PLUS`.\n3. Shift `PLUS` onto stack. Stack: `[E=3, PLUS]`.\n4. Parser sees `NUMBER 4` → shift. Stack: `[E=3, PLUS, NUMBER=4]`. Token: `MUL`.\n5. `NUMBER` → `F` ($$\\$=4), then `F` → `T` ($$\\$=4). Stack: `[E=3, PLUS, T=4]`.\n6. `MUL` shifts in. Stack: `[E=3, PLUS, T=4, MUL]`.\n7. Parser sees `NUMBER 2` → shift. Stack: `[E=3, PLUS, T=4, MUL, NUMBER=2]`. Token: `\\n`.\n8. `NUMBER` → `F` ($$\\$=2), then `F` → `T` ($$\\$=2). Stack: `[E=3, PLUS, T=4, MUL, T=2]`.\n9. Apply rule `T : T MUL F`. Action: `$$ = $1 * $3 = 4 * 2 = 8`. Stack: `[E=3, PLUS, T=8]`.\n10. Apply rule `E : E PLUS T`. Action: `$$ = $1 + $3 = 3 + 8 = 11`. Stack: `[E=11]`.\n11. Apply rule `S : E '\\n'`. Action: `printf(\"= 11\\n\")`. ✓\n\n**Output:** `= 11` (correct because `*` has higher precedence).\n\n### 3.3 Error Recovery in YACC\n\nThe `yyerror` function is called by the parser when it encounters a syntax error. The default `yyparse()` then calls `yyerror`, prints \"syntax error\", and calls `exit(1)`. For a robust calculator that recovers after an error, you can use the **error token**:\n\n```c\nE : E PLUS T     { $$ = $1 + $3; }\n  | E MINUS T    { $$ = $1 - $3; }\n  | error '\\n'   { yyerrok; printf(\"Try again.\\n> \"); }\n  | T            { $$ = $1;    }\n  ;\n```\n\nWhen the parser sees garbage, it discards tokens until it finds `\\n`, prints an error, and continues.\n\n### 3.4 Compilation & Execution Output\n\n```\n$ bison -d calc.y\n$ flex calc.l\n$ gcc lex.yy.c y.tab.c -o calc -ll\n$ ./calc\nSimple Calculator (Ctrl+D to exit)\n> 3+4*2\n= 11\n> (3+4)*2\n= 14\n> 10/3\n= 3.33333\n> 2.5*4\n= 10\n> -3*4\n= -12\n> Ctrl+D\n$\n```\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## SECTION_4: Structural Diagrams & Schematics\n\n### 4.1 Compilation & Execution Flow (Block Diagram)\n\n```mermaid\nflowchart TD\n    A[\"calc.l source file\"] --> B[\"flex calc.l\"]\n    C[\"calc.y source file\"] --> D[\"bison -d calc.y\"]\n    B --> E[\"lex.yy.c\"]\n    D --> F[\"y.tab.c\"]\n    D --> G[\"y.tab.h token macros\"]\n    G --> H[\"#include in calc.l\"]\n    E --> I[\"gcc compiler link\"]\n    F --> I\n    H --> I\n    I --> J[\"calc executable binary\"]\n    J --> K[\"User input stream stdin\"]\n    K --> L[\"yylex scanner reads chars\"]\n    L --> M[\"Tokens: NUMBER PLUS MUL etc\"]\n    M --> N[\"yyparse LALR parser\"]\n    N --> O{\"Parse tree complete?\"}\n    O -- \"Yes, hit newline\" --> P[\"Run semantic actions\"]\n    P --> Q[\"printf = result\"]\n    O -- \"No, error\" --> R[\"yyerror called\"]\n    R --> S[\"error message or recovery\"]\n    Q --> T[\"Loop back to read next line\"]\n    S --> T\n    T --> L\n```\n\n### 4.2 Tokenization Pipeline (LEX Internal View)\n\n```mermaid\nflowchart LR\n    A[\"Input character stream: 3 + 4 * 2 newline\"] --> B[\"LEX DFA Scanner\"]\n    B --> C{\"Match longest pattern\"}\n    C -- \"Digit run\" --> D[\"Token: NUMBER, value=3\"]\n    C -- \"Plus sign\" --> E[\"Token: PLUS\"]\n    C -- \"Multiply sign\" --> F[\"Token: MUL\"]\n    C -- \"Newline char\" --> G[\"Token: newline sentinel\"]\n    D --> H[\"yylval.ival = 3\"]\n    E --> I[\"yylval unused\"]\n    F --> I\n    G --> I\n    H --> J[\"Token queue fed to yyparse\"]\n    I --> J\n```\n\n### 4.3 Grammar Reduction Tree for `3 + 4 * 2`\n\n```mermaid\nflowchart TD\n    S0[\"S: start symbol\"] --> N1[\"E newline\"]\n    N1 --> E1[\"E\"]\n    N1 --> NL[\"'\\\\n'\"]\n    E1 --> E1A[\"E\"]\n    E1 --> PLUS[\"PLUS\"]\n    E1 --> T1[\"T\"]\n    E1A --> Tprev[\"T\"]\n    Tprev --> F1[\"F\"]\n    F1 --> NUM1[\"NUMBER: 3\"]\n    T1 --> T1A[\"T\"]\n    T1 --> MUL[\"MUL\"]\n    T1 --> F2[\"F\"]\n    T1A --> Fprev[\"F\"]\n    Fprev --> NUM2[\"NUMBER: 4\"]\n    F2 --> NUM3[\"NUMBER: 2\"]\n```\n\n> [!NOTE]\n> **Read this tree bottom-up:** Start at the leaves (`NUMBER 3`, `NUMBER 4`, `NUMBER 2`). The `*` multiplies `4 * 2 = 8` first (innermost reduction). Then `+` adds `3 + 8 = 11`. This mirrors operator precedence.\n\n### 4.4 Data Flow Between LEX and YACC (yylval Bridge)\n\n```mermaid\nflowchart LR\n    subgraph LEX[\"LEX File calc.l\"]\n        L1[\"Pattern: [0-9]+\"]\n        L2[\"Action: yylval.ival = atoi yytext\"]\n        L3[\"return NUMBER\"]\n    end\n    subgraph UNION[\"yylval union in y.tab.h\"]\n        U1[\"ival: int field\"]\n        U2[\"fval: float field\"]\n    end\n    subgraph YACC[\"YACC File calc.y\"]\n        Y1[\"%union declares ival and fval\"]\n        Y2[\"%token NUMBER type ival\"]\n        Y3[\"Grammar rule: E : E PLUS T $$ = $1 + $3\"]\n    end\n    L1 --> L2\n    L2 --> L3\n    L2 -- \"writes to\" --> U1\n    L3 -- \"sends token to\" --> Y3\n    U1 -- \"read by $1 $3 in action\" --> Y3\n    Y2 --> Y1\n    Y1 --> UNION\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n**Q1. [KTU University Exam – May 2024]**\n*What is the role of LEX in a compiler? List any four token patterns used in a LEX specification for a simple calculator.*\n\n**Model Answer (3 marks):**\nLEX (Lexical Analyzer Generator) is a tool that generates a **scanner/lexer** from a specification of regular expressions. It converts a stream of input characters into a stream of **tokens**, which are the smallest meaningful units for the parser. The four token patterns for a calculator are:\n1. **`[0-9]+`** → matches integers (e.g., `42`)\n2. **`[0-9]+\\.[0-9]+`** → matches floating-point numbers (e.g., `3.14`)\n3. **`[ \\t\\n]`** → matches whitespace (skipped)\n4. **`\\+|\\-|\\*|\\/`** → matches the four arithmetic operators\n\n*(1 mark for defining LEX, 2 marks for the four patterns — KTU marking scheme.)*\n\n**Q2. [KTU University Exam – Dec 2023]**\n*Explain the significance of the `$$` and `$n` notation in YACC semantic actions with an example.*\n\n**Model Answer (3 marks):**\nIn YACC, semantic actions are C code blocks attached to grammar productions. The notation **`$$`** refers to the **value of the left-hand side non-terminal** (the result of the reduction), while **`$1`, `$2`, ..., `$n`** refer to the **values of the symbols on the right-hand side**, indexed from left to right.\n\n**Example:** For the production `E : E PLUS T { $$ = $1 + $3; }`, the values flow as: `$1` is the left `E`, `$2` is the `PLUS` token (no value), `$3` is the right `T`. The action computes `$$ = $1 + $3` and assigns it to the new `E`. This is how evaluation is propagated up the parse tree.\n\n*(1 mark for definition of $$, 1 mark for $n, 1 mark for the example.)*\n\n### 5.2 Part B — Full-Descriptive Questions (14 Marks Each, Internal Choice)\n\n> [!NOTE]\n> KTU's End Semester Evaluation (ESE) for lab courses typically includes a 14-mark descriptive question. Per the **internal choice** rule, two alternatives are provided. Pick one.\n\n---\n\n#### **Question A (14 Marks): Option 1**\n\n**(a)** Write a complete LEX specification `calc.l` for a simple calculator that recognizes integers, the four arithmetic operators, parentheses, and ignores whitespace. Explain each section of the file. **(7 marks)**\n\n**(b)** Write a complete YACC specification `calc.y` that defines a grammar for arithmetic expressions with correct precedence and associativity, supports unary minus, and prints the result. Show the compilation commands. **(7 marks)**\n\n**Model Solution:**\n\n**(a) [7 marks]**\n\n```c\n%{\n#include \"y.tab.h\"\n#include <stdio.h>\n%}\n\n%%\n[0-9]+        { yylval.ival = atoi(yytext); return NUMBER; }\n[ \\t]         { /* ignore whitespace */ }\n\\n            { return '\\n'; }\n\\+            { return PLUS; }\n\\-            { return MINUS; }\n\\*            { return MUL; }\n\\/            { return DIV; }\n\\(            { return LPAREN; }\n\\)            { return RPAREN; }\n.             { return yytext[0]; }\n%%\n\nint yywrap(void) { return 1; }\n```\n\n**Explanation (distribution of 7 marks):**\n- **`%{ ... %}` definitions section** (1 mark): C code copied verbatim — here we include the YACC header.\n- **`%%` separator** (1 mark): Marks the start of rules.\n- **Rules section** (3 marks): Each pattern-action pair; `yylval.ival = atoi(yytext)` shows how values are stored.\n- **User subroutines section** (1 mark): The `yywrap()` function.\n- **C code for matching and returning tokens** (1 mark).\n\n**(b) [7 marks]**\n\n```c\n%{\n#include <stdio.h>\nint yylex(void);\nint yyerror(const char *s);\n%}\n\n%union { int ival; float fval; }\n%token <ival> NUMBER\n%token <fval> FNUM\n%token PLUS MINUS MUL DIV LPAREN RPAREN\n%type <fval> E T F\n%left PLUS MINUS\n%left MUL DIV\n%left UMINUS\n\n%%\nS    : E '\\n'           { printf(\"= %g\\n\", $1); }\n     ;\nE    : E PLUS T         { $$ = $1 + $3; }\n     | E MINUS T        { $$ = $1 - $3; }\n     | T                { $$ = $1;    }\n     ;\nT    : T MUL F          { $$ = $1 * $3; }\n     | T DIV F          { $$ = $1 / $3; }\n     | F                { $$ = $1;    }\n     ;\nF    : LPAREN E RPAREN  { $$ = $2; }\n     | NUMBER           { $$ = (float)$1; }\n     | MINUS F %prec UMINUS { $$ = -$2; }\n     ;\n%%\n\nint main(void) { return yyparse(); }\nint yyerror(const char *s) { fprintf(stderr, \"Error: %s\\n\", s); return 0; }\n```\n\n**Compilation (1 mark for commands):**\n```bash\nbison -d calc.y\nflex calc.l\ngcc lex.yy.c y.tab.c -o calc -ll\n./calc\n```\n\n**Explanation of marks:**\n- `%union`, `%token`, `%type` declarations (2 marks)\n- Precedence directives with `%left` and `%prec UMINUS` (2 marks)\n- Grammar productions with semantic actions and `$$`/`$n` usage (2 marks)\n- Compilation commands (1 mark)\n\n---\n\n#### **Question B (14 Marks): Option 2**\n\n**(a)** Explain the **tokenization** and **parsing** stages of a LEX/YACC calculator. How does the value of a `NUMBER` token get from `calc.l` to the semantic action in `calc.y`? **(7 marks)**\n\n**(b)** Given the grammar:\n```\nE → E + T | T\nT → T * F | F\nF → ( E ) | number\n```\nTrace the parse for the input `5 - 2 - 1` and show that left-associativity gives the result `2` (not `4`). **(7 marks)**\n\n**Model Solution:**\n\n**(a) [7 marks]**\n\n**Tokenization Stage (3 marks):**\n- The **LEX scanner** (`yylex()`) reads raw characters from input.\n- It applies the **longest match** rule to determine the next token.\n- For each matched pattern, the corresponding **C action** is executed. This action sets `yylval` and returns a token code (e.g., `NUMBER`, `PLUS`).\n\n**Parsing Stage (2 marks):**\n- The **YACC parser** (`yyparse()`) consumes tokens one at a time.\n- It uses an **LALR(1) parse table** to decide whether to **shift** (push token onto stack) or **reduce** (apply a grammar rule and run its action).\n- When the start symbol is fully reduced, parsing is complete.\n\n**Value Transfer Mechanism (2 marks):**\n- The LEX action sets `yylval.ival = atoi(yytext)`. The `yylval` variable is of type `%union`, defined in `calc.y`.\n- When `yylex()` returns the token, `yylval` contains the value. YACC stores it in the parse stack.\n- When the parser reduces by a rule, the values from the stack become accessible as `$1`, `$2`, etc., in the action.\n\n**(b) [7 marks]**\n\nTrace for `5 - 2 - 1` (left-associative, meaning `(5 - 2) - 1`):\n\n| Step | Stack | Remaining Input | Action |\n|---|---|---|---|\n| 1 | `$` | `5 - 2 - 1` | Shift `5` |\n| 2 | `$ 5` | `- 2 - 1` | Reduce `5 → F` ($$=5) |\n| 3 | `$ F=5` | `- 2 - 1` | Reduce `F → T` ($$=5) |\n| 4 | `$ T=5` | `- 2 - 1` | Reduce `T → E` ($$=5) |\n| 5 | `$ E=5` | `- 2 - 1` | Shift `-` |\n| 6 | `$ E=5 -` | `2 - 1` | Shift `2` |\n| 7 | `$ E=5 - 2` | `- 1` | Reduce `2 → F` ($$=2) |\n| 8 | `$ E=5 - F=2` | `- 1` | Reduce `F → T` ($$=2) |\n| 9 | `$ E=5 - T=2` | `- 1` | **Reduce `E → E - T`** ($$=5-2=3) |\n| 10 | `$ E=3` | `- 1` | Shift `-` |\n| 11 | `$ E=3 -` | `1` | Shift `1` |\n| 12 | `$ E=3 - 1` | (end) | Reduce `1 → F` ($$=1) |\n| 13 | `$ E=3 - F=1` | (end) | Reduce `F → T` ($$=1) |\n| 14 | `$ E=3 - T=1` | (end) | **Reduce `E → E - T`** ($$=3-1=**2**) |\n\n**Final result: 2**, confirming left-associativity. If it were right-associative, the parse would give `5 - (2 - 1) = 4`, which is incorrect for subtraction.\n\n*(3 marks for the table, 2 marks for the calculation logic, 2 marks for the final answer with explanation.)*\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mistakes Where KTU Students Lose Marks:**\n> 1. **Forgetting `-d` flag in Bison:** `bison -d calc.y` is what generates `y.tab.h`. Without `-d`, the `calc.l` file's `#include \"y.tab.h\"` will fail with *\"file not found\"*. **−2 marks** if missed.\n> 2. **Missing backslash escape in LEX patterns:** Writing `+` instead of `\\+` makes LEX interpret it as \"one or more\" (Kleene plus), so `+` will match `a++b` wrong. Always use `\\`, `\\*`, `\\(`, `\\)`, `\\.`.\n> 3. **Wrong precedence order in `%left`:** Tokens declared *later* have *higher* precedence. Writing `%left MUL DIV` *before* `%left PLUS MINUS` will break operator precedence — students commonly swap this. **−2 marks**.\n> 4. **Forgetting `yywrap()`:** Without it, LEX will look for a non-existent `yywrap` in some libraries and fail to link. **−1 mark**.\n> 5. **Confusing `$$` with `$0`:** There is **no `$0`**. The left-hand side is always `$$`, and right-hand side symbols are `$1, $2, ...`. A student who writes `$0 + $2` will get 0.\n> 6. **Forgetting to type-cast in semantic actions:** If `$1` is `int` (from `NUMBER`) and you assign it to a `float` non-terminal, you must cast: `$$ = (float)$1;` to avoid compiler warnings.\n> 7. **Wrong `printf` format:** Use `%g` or `%f` for floats, not `%d` (which is for integers and will print garbage).\n\n### 5.4 Topic Recap & Important Things to Remember (Rapid Revision Checklist)\n\n- ✅ **LEX** generates a **scanner** (`yylex`); **YACC** generates a **parser** (`yyparse`).\n- ✅ Three files are typical: `calc.l`, `calc.y`, and a `main()` driver. The driver can also be inside `calc.y` after the second `%%`.\n- ✅ **LEX pattern** `[0-9]+` matches integers; `\\.` is the escaped period for decimals.\n- ✅ **YACC grammar** uses **layered non-terminals** (`E` for +/-, `T` for */, `F` for atoms) to enforce precedence implicitly.\n- ✅ **`%union`** holds the type of `yylval`. **`%token <type>`** assigns a token a union field. **`%type <type>`** assigns a non-terminal a union field.\n- ✅ **`%left PLUS MINUS`** and **`%left MUL DIV`** declare left-associativity and order of precedence (later = higher).\n- ✅ **`$$` is the LHS value; `$n` is the nth symbol's value** in a production.\n- ✅ **`%prec UMINUS`** is used to make unary minus bind tighter than `*` and `/`.\n- ✅ Compilation pipeline: `bison -d calc.y` → `flex calc.l` → `gcc lex.yy.c y.tab.c -o calc -ll` → `./calc`.\n- ✅ The `-d` flag in Bison **is mandatory** to generate the token header file.\n- ✅ **Longest match** rule in LEX: if multiple patterns match, the longest wins; if tied, the one declared *first* wins.\n- ✅ **Shift-reduce conflicts** are resolved by precedence/associativity directives; **reduce-reduce conflicts** indicate genuine grammar ambiguity.\n- ✅ **Error recovery** uses the special `error` token in YACC productions.\n- ✅ The **start symbol** in YACC is the non-terminal on the left of the *first* rule in the rules section.\n- ✅ `yyerror()` is mandatory; the user must define it (else linker error).\n- ✅ `yywrap()` returning 1 means \"no more input files\"; returning 0 would tell LEX to open `yyin` again.\n- ✅ For the **calculator experiment**, the canonical grammar gives `*`/`/` higher precedence than `+`/`-` and enforces **left-associativity** for both.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-engineering-7-pccsl607-mExperiment 2-t5",
          "title": "Convert the BNF rules into YACC form and write code to generate abstract syntax tree",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL607 - SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Convert the BNF rules into YACC form and write code to generate abstract syntax tree\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definitions (KTU 2024 Syllabus Standard)\n\n> [!NOTE]\n> **Backus-Naur Form (BNF)** is a formal mathematical notation for describing the **syntax** of context-free grammars. It was originally developed by **John Backus** and **Peter Naur** in the late 1950s to specify the syntax of **ALGOL 60**. A BNF rule consists of a *non-terminal* on the left side and a *derivation* (sequence of terminals and non-terminals) on the right side, separated by the symbol `::=`.\n\n> [!IMPORTANT]\n> **YACC (Yet Another Compiler Compiler)** is a *parser generator* tool developed by **Stephen C. Johnson** at Bell Labs in 1973. It takes a grammar specification written in a BNF-like notation (`.y` file) and automatically generates the **`y.tab.c`** C source file — a LALR(1) parser that detects syntactic structure in the input token stream produced by **LEX**.\n\n> [!IMPORTANT]\n> **Abstract Syntax Tree (AST)** is a condensed, hierarchical **tree representation** of the source program's syntactic structure. Unlike a parse tree, an AST **eliminates redundant syntactic information** (parentheses, punctuation, derivation rules) and represents each construct as a *node* containing only the essential semantic data (operator and operands).\n\n### 1.2 The Trinity of Compilation (The Three Pillars)\n\n| Tool | Role | Input | Output |\n|---|---|---|---|\n| **LEX** | Lexical Analyzer (Tokenizer) | Source code (`.l`) | Token stream |\n| **YACC** | Syntax Analyzer (Parser) | Grammar (`.y`) | Parse tree / AST |\n| **GCC** | Code Generator | AST / Intermediate code | Executable binary |\n\n### 1.3 Conceptual Analogy — \"The Restaurant Kitchen\"\n\n> [!TIP]\n> **Think of BNF as a Recipe Book, YACC as the Head Chef, and AST as the Plated Dish Diagram.**\n>\n> - The **BNF recipe** says: *\"A Burger must contain a bun, a patty, and a topping.\"*\n> - The **YACC chef** reads the order ticket (token stream) and *verifies* it follows the recipe structure, then **builds a plate** (AST) that records *what was used* (operators/operands) and *how it was combined* (tree hierarchy).\n> - The final **AST plate** is what the next kitchen station (semantic analyzer / code generator) uses to cook the actual meal (machine code).\n\n### 1.4 Why AST Instead of Parse Tree?\n\nA parse tree contains every derivation step (including non-terminals and intermediate productions), making it **verbose and hard to traverse**. An AST abstracts away this noise, leaving only the **essential computational structure** — making it ideal for subsequent compilation phases.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Anatomy of a BNF Rule\n\nA BNF rule is written as:\n\n$$\\text{Non-Terminal} \\; ::= \\; \\alpha_1 \\mid \\alpha_2 \\mid \\dots \\mid \\alpha_n$$\n\nwhere:\n- **Non-Terminal** (left side) = a syntactic variable (e.g., `<expression>`)\n- $\\alpha_i$ (right side) = a sequence of **terminals** (tokens) and **non-terminals**\n- The symbol `::=` means \"is defined as\"\n- The symbol $\\mid$ separates alternative productions (logical OR)\n\n### 2.2 Anatomy of a YACC Specification File (`.y`)\n\nA YACC file has **three distinct sections**, separated by the special token `%%`:\n\n```\n─────────────────────────────────────\n   SECTION 1: DECLARATIONS\n   (C declarations, #include, #define,\n    token IDs, %left/%right precedence)\n─────────────────────────────────────\n%%\n   SECTION 2: GRAMMAR RULES\n   (Translated BNF rules + semantic actions)\n─────────────────────────────────────\n%%\n   SECTION 3: AUXILIARY C ROUTINES\n   (main(), yyerror(), AST helper functions)\n─────────────────────────────────────\n```\n\n### 2.3 The Semantic Action Stack — `$$`, `$1`, `$2`, …\n\n> [!IMPORTANT]\n> When YACC reduces a production, the semantic action code executes. Inside this code:\n> - **`$$`** refers to the **value of the LHS (left-hand side) non-terminal** (the result of the rule).\n> - **`$1`, `$2`, `$3`, …** refer to the values of the **1st, 2nd, 3rd … RHS symbols** respectively.\n>\n> This mechanism is how YACC **builds the AST**: each rule's action constructs a *new node* linking the children's subtrees.\n\n### 2.4 The BNF → YACC Translation Map\n\n| BNF Element | YACC Equivalent | Notes |\n|---|---|---|\n| `::=` | `:` | Definition separator |\n| `\\|` (alternation) | New line ending in `;` | Each alternative on its own line |\n| Terminal in quotes `\"x\"` | Token name `x` (or `'x'`) | Single character terminals use quotes |\n| Non-terminal `<expr>` | `expr` | No angle brackets, lowercase convention |\n| Epsilon (empty) $\\varepsilon$ | (empty RHS) | Just `:` followed by `;` |\n\n### 2.5 The KTU Formula Sheet — AST Node Operations\n\n> [!NOTE]\n> All AST node operations follow the **make-node / make-leaf** pattern. Below is the cheat-sheet table for an arithmetic expression AST.\n\n| Operation | C Code Snippet | Purpose |\n|---|---|---|\n| **Create leaf node** | `node = malloc(sizeof(Node)); node->type = 'i'; node->value = val;` | Make a node holding an integer operand |\n| **Create operator node** | `node = malloc(sizeof(Node)); node->type = op; node->left = $1; node->right = $3;` | Make a node with two children |\n| **Return new node** | `$$ = node;` | Assign built subtree to LHS variable |\n| **Link subtrees** | `node->left = $1; node->right = $3;` | Build parent-child relationships |\n\n### 2.6 Engineering Utility\n\n> [!TIP]\n> AST generation via YACC is the **backbone of every modern compiler**: GCC, Clang/LLVM, javac, ghc (Haskell), rustc, and even TypeScript's tsc all use this same LALR/GLR parsing + AST paradigm. The techniques you write in a 50-line `.y` file today are **industry-scale** concepts.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Conversion, Code & Analytical Matrix\n\n### 3.1 Sample BNF Grammar (Reference: Arithmetic Expressions)\n\nConsider the classic BNF for arithmetic expressions with `+`, `-`, `*`, `/`, parentheses, and integer operands:\n\n```\nGoal    ::= Expr\nExpr    ::= Expr '+' Term\n         |  Expr '-' Term\n         |  Term\nTerm    ::= Term '*' Factor\n         |  Term '/' Factor\n         |  Factor\nFactor  ::= '(' Expr ')'\n         |  ID\n```\n\n### 3.2 Step-by-Step BNF → YACC Conversion\n\n**Step 1 — Strip angle brackets and replace `::=` with `:`:**\n\n```\nGoal : Expr\nExpr : Expr '+' Term\n     | Expr '-' Term\n     | Term\nTerm : Term '*' Factor\n     | Term '/' Factor\n     | Factor\nFactor : '(' Expr ')'\n       | ID\n```\n\n**Step 2 — Each alternative on a separate line, terminated with `;`:**\n\n```\nGoal    : Expr\n        ;\nExpr    : Expr '+' Term\n        | Expr '-' Term\n        | Term\n        ;\nTerm    : Term '*' Factor\n        | Term '/' Factor\n        | Factor\n        ;\nFactor  : '(' Expr ')'\n        | ID\n        ;\n```\n\n**Step 3 — Add semantic actions to build the AST.** Each action uses `make_node()` to allocate a node and `$$` to return it.\n\n### 3.3 The Complete YACC Program (`.y` file)\n\n> [!IMPORTANT]\n> Save this as **`ast.y`** and compile with: `yacc -d ast.y && gcc y.tab.c -o ast`\n\n```c\n/*==============================================================\n   ast.y  —  YACC specification with AST generation\n   Course: PCCSL607 (Systems Lab) | KTU 2024 Scheme\n   Topic : BNF → YACC + Abstract Syntax Tree construction\n   Grammar: Arithmetic expressions\n==============================================================*/\n\n%{\n#include <stdio.h>\n#include <stdlib.h>\n#include <ctype.h>\n\n/* ---- AST Node Definition ---- */\ntypedef struct ASTNode {\n    int   type;          /* 'n' = number, 'o' = operator */\n    int   op;            /* operator character (+,-,*,/) */\n    int   value;         /* for number nodes */\n    struct ASTNode *left;\n    struct ASTNode *right;\n} ASTNode;\n\n/* ---- AST helper function prototypes ---- */\nASTNode* make_leaf(int val);\nASTNode* make_node(int op, ASTNode* l, ASTNode* r);\nvoid     print_ast(ASTNode* root, int level);\n\nint yylex(void);\nvoid yyerror(const char *s);\n%}\n\n/* ---- Token declarations ---- */\n%token ID\n\n/* ---- Operator precedence (low to high) ---- */\n%left  '+' '-'\n%left  '*' '/'\n%right UMINUS\n\n/* ---- Grammar rules with AST-building actions ---- */\n%%\nGoal    : Expr\n            { printf(\"\\nAbstract Syntax Tree (indented dump):\\n\");\n              print_ast($1, 0); }\n        ;\n\nExpr    : Expr '+' Term\n            { $$ = make_node('+', $1, $3); }\n        | Expr '-' Term\n            { $$ = make_node('-', $1, $3); }\n        | Term\n            { $$ = $1; }\n        ;\n\nTerm    : Term '*' Factor\n            { $$ = make_node('*', $1, $3); }\n        | Term '/' Factor\n            { $$ = make_node('/', $1, $3); }\n        | Factor\n            { $$ = $1; }\n        ;\n\nFactor  : '(' Expr ')'\n            { $$ = $2; }\n        | ID\n            { $$ = make_leaf($1); }\n        ;\n%%\n\n/* ---- AST helper: create leaf node (number/identifier) ---- */\nASTNode* make_leaf(int val) {\n    ASTNode* n = (ASTNode*)malloc(sizeof(ASTNode));\n    n->type  = 'n';\n    n->value = val;\n    n->left  = NULL;\n    n->right = NULL;\n    return n;\n}\n\n/* ---- AST helper: create internal operator node ---- */\nASTNode* make_node(int op, ASTNode* l, ASTNode* r) {\n    ASTNode* n = (ASTNode*)malloc(sizeof(ASTNode));\n    n->type  = 'o';\n    n->op    = op;\n    n->left  = l;\n    n->right = r;\n    return n;\n}\n\n/* ---- AST helper: pretty-print the tree sideways ---- */\nvoid print_ast(ASTNode* root, int level) {\n    if (root == NULL) return;\n    print_ast(root->right, level + 1);\n    for (int i = 0; i < level; i++) printf(\"    \");\n    if (root->type == 'o')\n        printf(\"[op: %c]\\n\", root->op);\n    else\n        printf(\"[num: %d]\\n\", root->value);\n    print_ast(root->left, level + 1);\n}\n\n/* ---- Error reporter ---- */\nvoid yyerror(const char *s) {\n    fprintf(stderr, \"Parse error: %s\\n\", s);\n}\n\n/* ---- Main: ties LEX tokens to YACC parser ---- */\nint main(void) {\n    printf(\"Enter an expression (e.g., 3+5*2):\\n> \");\n    return yyparse();\n}\n```\n\n### 3.4 Required Companion LEX File (`ast.l`)\n\n> [!NOTE]\n> YACC needs a LEX tokenizer to feed it tokens. Save as `ast.l`.\n\n```c\n%{\n#include \"y.tab.h\"\n#include <stdlib.h>\n%}\n\n%%\n[0-9]+      { yylval = atoi(yytext); return ID; }\n[ \\t\\n]     { /* skip whitespace */ }\n.           { return yytext[0]; }\n%%\n```\n\n### 3.5 Analytical Matrix — Mapping BNF to YACC Actions\n\n| BNF Production | YACC Rule | Semantic Action | AST Effect |\n|---|---|---|---|\n| `Expr → Expr + Term` | `Expr : Expr '+' Term` | `$$ = make_node('+', $1, $3);` | Build `+` node, children = left-Expr and right-Term |\n| `Expr → Term` | `Expr : Term` | `$$ = $1;` | Propagate subtree upward unchanged |\n| `Term → Term * Factor` | `Term : Term '*' Factor` | `$$ = make_node('*', $1, $3);` | Build `*` node |\n| `Factor → ( Expr )` | `Factor : '(' Expr ')'` | `$$ = $2;` | Discard parens, keep inner subtree |\n| `Factor → ID` | `Factor : ID` | `$$ = make_leaf($1);` | Create leaf node holding the integer |\n\n### 3.6 Worked Example — Parsing `3 + 5 * 2`\n\n**Step-by-step reduction trace** (LALR parser behavior):\n\n$$\n\\begin{aligned}\n\\text{Input token stream:} \\quad & \\text{ID(3) `+' ID(5) `*' ID(2)} \\\\[4pt]\n\\text{Reduce 1:} \\quad & \\text{Factor} \\to \\text{ID} \\quad\\Rightarrow\\; \\text{Factor} = \\text{leaf}(3) \\\\[4pt]\n\\text{Reduce 2:} \\quad & \\text{Term} \\to \\text{Factor} \\quad\\Rightarrow\\; \\text{Term} = \\text{leaf}(3) \\\\[4pt]\n\\text{Reduce 3:} \\quad & \\text{Expr} \\to \\text{Term} \\quad\\Rightarrow\\; \\text{Expr} = \\text{leaf}(3) \\\\[4pt]\n\\text{Read `+', shift} \\\\[4pt]\n\\text{Reduce 4:} \\quad & \\text{Factor} \\to \\text{ID} \\quad\\Rightarrow\\; \\text{Factor} = \\text{leaf}(5) \\\\[4pt]\n\\text{Reduce 5:} \\quad & \\text{Term} \\to \\text{Factor} \\quad\\Rightarrow\\; \\text{Term} = \\text{leaf}(5) \\\\[4pt]\n\\text{Read `*', shift; Reduce 6:} \\quad & \\text{Factor} \\to \\text{ID} \\quad\\Rightarrow\\; \\text{leaf}(2) \\\\[4pt]\n\\text{Reduce 7:} \\quad & \\text{Term} \\to \\text{Term} \\ast \\text{Factor} \\quad\\Rightarrow\\; \\text{Term} = \\text{node}(`*`, \\text{leaf}(5), \\text{leaf}(2)) \\\\[4pt]\n\\text{Reduce 8:} \\quad & \\text{Expr} \\to \\text{Expr} + \\text{Term} \\quad\\Rightarrow\\; \\text{Expr} = \\text{node}(`+`, \\text{leaf}(3), \\text{node}(`*`, \\text{leaf}(5), \\text{leaf}(2)))\n\\end{aligned}\n$$\n\n> [!TIP]\n> Notice the AST correctly encodes **operator precedence**: `*` is *deeper* in the tree, meaning it is evaluated *first*. This is the core strength of AST — the tree shape **itself** represents evaluation order.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 The Compiler Pipeline — Where YACC & AST Fit\n\n```mermaid\nflowchart LR\n    src[\"Source Code<br/>(input.txt)\"]\n    lex[\"LEX<br/>.l file\"]\n    tokens[\"Token Stream<br/>ID '+' ID '*' ID\"]\n    yacc[\"YACC<br/>.y file\"]\n    ast[\"Abstract<br/>Syntax Tree\"]\n    sem[\"Semantic<br/>Analyzer\"]\n    ir[\"Intermediate<br/>Representation\"]\n    codegen[\"Code<br/>Generator\"]\n    exe[\"Executable<br/>Binary\"]\n\n    src -- \"characters\" --> lex\n    lex -- \"yylex()\" --> tokens\n    tokens -- \"yyparse()\" --> yacc\n    yacc -- \"semantic actions\" --> ast\n    ast --> sem\n    sem --> ir\n    ir --> codegen\n    exe\n\n    classDef stage fill:#fff3b0,stroke:#d97706,color:#000\n    classDef data fill:#a7f3d0,stroke:#047857,color:#000\n    class src,tokens,ast,ir,exe data\n    class lex,yacc,sem,codegen stage\n```\n\n### 4.2 YACC File Structure — Three Sections\n\n```mermaid\nflowchart TB\n    y[\"ast.y<br/>YACC Specification File\"]\n    s1[\"SECTION 1<br/>Declarations<br/>(%{ ... %}<br/>%token<br/>%left / %right)\"]\n    s2[\"SECTION 2<br/>Grammar Rules<br/>(BNF + semantic actions)<br/>separated by %%\"]\n    s3[\"SECTION 3<br/>Auxiliary Routines<br/>(main, yyerror,<br/>AST helpers)\"]\n    out[\"y.tab.c + y.tab.h<br/>(generated C parser)\"]\n\n    y --> s1\n    s1 --> s2\n    s2 --> s3\n    s3 --> out\n\n    classDef sec fill:#bfdbfe,stroke:#1d4ed8,color:#000\n    classDef out fill:#fde68a,stroke:#b45309,color:#000\n    class s1,s2,s3 sec\n    class y,out out\n```\n\n### 4.3 AST Built for Expression `3 + 5 * 2`\n\n```mermaid\nflowchart TB\n    root[\"[op: +]\"]\n    left1[\"[num: 3]\"]\n    right1[\"[op: *]\"]\n    left2[\"[num: 5]\"]\n    right2[\"[num: 2]\"]\n\n    root --> left1\n    root --> right1\n    right1 --> left2\n    right1 --> right2\n\n    classDef op fill:#fca5a5,stroke:#b91c1c,color:#000\n    classDef num fill:#86efac,stroke:#15803d,color:#000\n    class root,right1 op\n    class left1,left2,right2 num\n```\n\n### 4.4 Parse Tree vs Abstract Syntax Tree — Key Difference\n\n```mermaid\nflowchart TB\n    pt[\"Parse Tree for 3+5<br/>(verbose, includes all non-terminals)\"]\n    pt1[\"Goal\"]\n    pt2[\"Expr\"]\n    pt3a[\"Expr\"]\n    pt3b[\"+\"]\n    pt3c[\"Term\"]\n    pt4a[\"Term → Factor → ID(3)\"]\n    pt4b[\"ID(5)\"]\n\n    pt --> pt1 --> pt2\n    pt2 --> pt3a & pt3b & pt3c\n    pt3a --> pt4a\n    pt3c --> pt4b\n\n    at[\"AST for 3+5<br/>(compact, no derivation noise)\"]\n    at1[\"[op: +]\"]\n    at2[\"[num: 3]\"]\n    at3[\"[num: 5]\"]\n\n    at --> at1\n    at1 --> at2 & at3\n\n    classDef pt fill:#e9d5ff,stroke:#6b21a8,color:#000\n    classDef at fill:#fed7aa,stroke:#c2410c,color:#000\n    class pt,pt1,pt2,pt3a,pt3b,pt3c,pt4a,pt4b pt\n    class at,at1,at2,at3 at\n```\n\n### 4.5 AST Node Memory Layout (C struct)\n\n```mermaid\nflowchart LR\n    n[\"ASTNode struct\"]\n    f1[\"type : int<br/>( 'n' or 'o' )\"]\n    f2[\"op / value : int<br/>( operator char OR number )\"]\n    f3[\"left  : ASTNode*\"]\n    f4[\"right : ASTNode*\"]\n\n    n --- f1\n    n --- f2\n    n --- f3\n    n --- f4\n\n    classDef box fill:#fef3c7,stroke:#a16207,color:#000\n    class n,f1,f2,f3,f4 box\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n### **PART A — Short Answer Questions (3 Marks Each)**\n\n---\n\n#### **Q1. [KTU University Exam — Model Question, Module 2]**\n**Differentiate between a parse tree and an abstract syntax tree. Why is AST preferred in modern compilers?**\n\n**Model Answer (3 marks):**\n\n| Aspect | Parse Tree | Abstract Syntax Tree |\n|---|---|---|\n| **Content** | Contains *every* non-terminal and production rule | Contains only *essential* operators and operands |\n| **Size** | Large, verbose | Compact, minimal |\n| **Construction** | Generated implicitly by parser | Built explicitly via semantic actions in YACC |\n| **Use** | Parser debugging | Subsequent compilation phases |\n\n**Why AST is preferred (1 mark):**\nThe AST eliminates redundant syntactic symbols (parentheses, commas, intermediate non-terminals), making it **compact**, **easier to traverse**, and **directly usable** for semantic analysis, optimization, and code generation. It encodes **operator precedence** in its structure — deeper nodes = higher precedence = evaluated first.\n\n*(3 marks: 1 for table, 1 for AST advantages, 1 for precedence insight)*\n\n---\n\n#### **Q2. [KTU University Exam — Model Question, Module 2]**\n**Explain the role of `$$`, `$1`, `$2` … `$n` in a YACC semantic action. How are they used to construct an AST node?**\n\n**Model Answer (3 marks):**\n\n- **`$$`** is a special YACC variable that holds the **value of the LHS non-terminal** of the current production. It is the value *returned upward* to the parent rule.\n- **`$1`, `$2`, …, `$n`** hold the values of the **1st, 2nd, …, nth symbol on the RHS** of the current production — they are the *incoming* values from child rules.\n- **AST construction (1 mark):** Inside a rule like `Expr : Expr '+' Term`, the action `$$ = make_node('+', $1, $3);` creates a new operator node with `$1` (left subtree) and `$3` (right subtree) as children, then assigns the new node to `$$`. This new node is then passed upward as `$1` or `$2` in the parent rule.\n\n---\n\n### **PART B — Long Answer Questions (14 Marks Each)**\n*As per KTU 2024 Scheme ESE pattern, students answer ONE of TWO alternatives.*\n\n---\n\n#### **Question A (14 Marks) [KTU University Exam — Model Question, Module 2]**\n\n**(a)** *Convert the following BNF grammar into YACC form and explain each step. (7 marks)*\n\n```\nS → A B\nA → a A | ε\nB → b B c | ε\n```\n\n**(b)** *For the grammar in (a), write the complete YACC `.y` file (with declarations, rules, and auxiliary routines) that constructs an **Abstract Syntax Tree** for the input. (7 marks)*\n\n---\n\n**Model Solution:**\n\n**(a) BNF → YACC Conversion — Step by Step (7 marks):**\n\n**Step 1 — Identify non-terminals and terminals (1 mark):**\n- Non-terminals: `S`, `A`, `B`\n- Terminals: `a`, `b`, `c`\n\n**Step 2 — Replace `::=` with `:` and remove angle brackets (1 mark):**\n\n```\nS : A B\nA : a A\n  |\nB : b B c\n  |\n```\n\n**Step 3 — Each alternative on a separate line ending in `;` (1 mark):**\n\n```\nS  : A B        ;\nA  : a A        |\n   | /* empty */ ;\nB  : b B c      |\n   | /* empty */ ;\n```\n\n**Step 4 — Final YACC rule block (1 mark):**\n\n```\nS  : A B ;\nA  : 'a' A\n   | ;\nB  : 'b' B 'c'\n   | ;\n```\n\n**Step 5 — Identify the language generated (1 mark):**\nThe grammar generates strings of the form $a^i b^j c^j$ where $i, j \\geq 0$ (e.g., `ε`, `abc`, `abbc`, `aabc`, `aabbcc`).\n\n**Step 6 — Add `%token` declarations for terminals (1 mark):**\nIn declarations section: `%token A_LETTER B_LETTER C_LETTER` (or directly use `'a' 'b' 'c'` as single-character tokens).\n\n**Step 7 — Add semantic actions to build AST (1 mark):**\nDiscussed in part (b).\n\n---\n\n**(b) Complete YACC `.y` file with AST construction (7 marks):**\n\n```c\n%{\n#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct ASTNode {\n    char   label;            /* 'a', 'b', 'c', or 'S' */\n    struct ASTNode *left;\n    struct ASTNode *right;\n} ASTNode;\n\nASTNode* make_node(char lbl, ASTNode* l, ASTNode* r);\nvoid     print_ast(ASTNode* r, int depth);\n\nint yylex(void);\nvoid yyerror(const char *s);\n%}\n\n%token A_TOK B_TOK C_TOK\n\n%%\nS  : A B\n     { $$ = make_node('S', $1, $2);\n       printf(\"AST for accepted string:\\n\");\n       print_ast($$, 0); }\n   ;\n\nA  : A_TOK A\n     { $$ = make_node('a', $1, $2); }\n   | /* epsilon */\n     { $$ = NULL; }\n   ;\n\nB  : B_TOK B C_TOK\n     { $$ = make_node('b', $1, $2);\n       /* $3 is C, embed in right's right or discard */\n       $$->right = make_node('c', NULL, NULL); }\n   | /* epsilon */\n     { $$ = NULL; }\n   ;\n%%\n\nASTNode* make_node(char lbl, ASTNode* l, ASTNode* r) {\n    ASTNode* n = (ASTNode*)malloc(sizeof(ASTNode));\n    n->label = lbl;\n    n->left  = l;\n    n->right = r;\n    return n;\n}\n\nvoid print_ast(ASTNode* r, int depth) {\n    if (!r) return;\n    print_ast(r->right, depth + 1);\n    for (int i = 0; i < depth; i++) printf(\"   \");\n    printf(\"[%c]\\n\", r->label);\n    print_ast(r->left, depth + 1);\n}\n\nvoid yyerror(const char *s) { fprintf(stderr, \"Error: %s\\n\", s); }\n\nint main(void) {\n    printf(\"Enter string of a's followed by b's then c's:\\n> \");\n    return yyparse();\n}\n```\n\n**Companion LEX file (`ast2.l`):**\n```c\n%{\n#include \"y.tab.h\"\n%}\n%%\na  { return A_TOK; }\nb  { return B_TOK; }\nc  { return C_TOK; }\n\\n { return '\\n'; }\n[ \\t] ;          /* skip whitespace */\n.  { return yytext[0]; }\n%%\n```\n\n**Compilation & Execution (1 mark within 7):**\n```bash\nyacc -d ast2.y\nlex ast2.l\ngcc y.tab.c lex.yy.c -o ast2\n./ast2        # then type: abc\n```\n\n**Sample AST produced for input `abc` (1 mark):**\n```mermaid\nflowchart TB\n    s[\"[S]\"]\n    a[\"[a]\"]\n    b[\"[b]\"]\n    c[\"[c]\"]\n\n    s --> a\n    s --> b\n    b --> c\n\n    classDef n fill:#fef3c7,stroke:#a16207,color:#000\n    class s,a,b,c n\n```\n\n---\n\n#### **Question B (14 Marks) [KTU University Exam — Model Question, Module 2]**\n\n**(a)** *Given the BNF grammar below, convert it into YACC specification format showing **all conversion steps**. (7 marks)*\n\n```\nE → E + T | E - T | T\nT → T * F | T / F | F\nF → ( E ) | number\n```\n\n**(b)** *Write a complete YACC program (with auxiliary C functions) to **build and display an Abstract Syntax Tree** for the grammar in part (a). Show the AST that would be generated for the input `10 - 4 * 2`. (7 marks)*\n\n---\n\n**Model Solution:**\n\n**(a) BNF → YACC Step-by-Step Conversion (7 marks):**\n\n**Step 1 — Rewrite removing `< >` and `::=` (1 mark):**\n\n```\nE → E + T | E - T | T\nT → T * F | T / F | F\nF → ( E ) | number\n```\nbecomes:\n```\nE : E + T | E - T | T\nT : T * F | T / F | F\nF : ( E ) | number\n```\n\n**Step 2 — Each alternative on its own line with `;` (2 marks):**\n\n```\nE : E '+' T\n  | E '-' T\n  | T\n  ;\nT : T '*' F\n  | T '/' F\n  | F\n  ;\nF : '(' E ')'\n  | number\n  ;\n```\n\n**Step 3 — Add `%token` declaration (1 mark):**\nIn declarations: `%token NUMBER`\n\n**Step 4 — Add operator precedence to resolve shift-reduce conflicts (1 mark):**\n```\n%left  '+' '-'\n%left  '*' '/'\n```\n(Lower precedence listed first; `*` and `/` bind tighter than `+` and `-`.)\n\n**Step 5 — Identify START symbol (1 mark):**\n`E` is the start symbol (declared via `%start E` or implicitly the LHS of the first rule).\n\n**Step 6 — Add semantic actions to build AST (1 mark):**\nDiscussed in part (b).\n\n---\n\n**(b) Complete YACC Program with AST (7 marks):**\n\n```c\n%{\n#include <stdio.h>\n#include <stdlib.h>\n\ntypedef struct Node {\n    int   nodetype;       /* 0 = number, 1 = operator */\n    char  op;             /* operator char for op nodes */\n    int   val;            /* value for number nodes */\n    struct Node *left;\n    struct Node *right;\n} Node;\n\nNode* mk_num(int v);\nNode* mk_op(char op, Node* l, Node* r);\nvoid  show(Node* r, int lvl);\n\nint yylex(void);\nvoid yyerror(const char *s);\n%}\n\n%token NUMBER\n%left  '+' '-'\n%left  '*' '/'\n\n%%\nE : E '+' T    { $$ = mk_op('+', $1, $3); }\n  | E '-' T    { $$ = mk_op('-', $1, $3); }\n  | T          { $$ = $1; }\n  ;\nT : T '*' F    { $$ = mk_op('*', $1, $3); }\n  | T '/' F    { $$ = mk_op('/', $1, $3); }\n  | F          { $$ = $1; }\n  ;\nF : '(' E ')'  { $$ = $2; }\n  | NUMBER     { $$ = mk_num($1); }\n  ;\n%%\n\nNode* mk_num(int v) {\n    Node* n = (Node*)malloc(sizeof(Node));\n    n->nodetype = 0; n->val = v;\n    n->left = n->right = NULL;\n    return n;\n}\nNode* mk_op(char op, Node* l, Node* r) {\n    Node* n = (Node*)malloc(sizeof(Node));\n    n->nodetype = 1; n->op = op;\n    n->left = l; n->right = r;\n    return n;\n}\nvoid show(Node* r, int lvl) {\n    if (!r) return;\n    show(r->right, lvl+1);\n    for (int i=0;i<lvl;i++) printf(\"   \");\n    printf(r->nodetype ? \"[op %c]\\n\" : \"[num %d]\\n\",\n           r->nodetype ? r->op : r->val);\n    show(r->left, lvl+1);\n}\nvoid yyerror(const char *s) { fprintf(stderr, \"Error: %s\\n\", s); }\nint main(void) {\n    printf(\"Enter expression: \"); return yyparse();\n}\n```\n\n**AST for input `10 - 4 * 2` (2 marks within the 7):**\n\n```mermaid\nflowchart TB\n    root[\"[op: -]\"]\n    l[\"[num: 10]\"]\n    r[\"[op: *]\"]\n    l2[\"[num: 4]\"]\n    r2[\"[num: 2]\"]\n\n    root --> l\n    root --> r\n    r --> l2\n    r --> r2\n\n    classDef op fill:#fca5a5,stroke:#b91c1c,color:#000\n    classDef num fill:#86efac,stroke:#15803d,color:#000\n    class root,r op\n    class l,l2,r2 num\n```\n\n> [!IMPORTANT]\n> **The tree shape encodes evaluation order:** The `*` node is *deeper* (right child of `-`), so it is evaluated first → `4 * 2 = 8`, then `10 - 8 = 2`. This correctly implements **left-to-right operator precedence** without needing parentheses.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall Callout:**\n> 1. **Forgot `%token` declaration** → YACC throws *syntax error on rule* — lose 1 mark.\n> 2. **Mixed up `$$` and `$1`** → AST is built upside-down; tree traversal prints wrong order — lose 2 marks.\n> 3. **Did not handle epsilon (`| ; |`)** → All-empty productions get rejected by parser — lose 1–2 marks.\n> 4. **Forgot `#include <stdlib.h>` for `malloc`** → Segmentation fault at runtime — lose 1 mark (no execution marks).\n> 5. **Did not write the `yyerror()` and `main()` functions** → Linking fails — lose 1 mark.\n> 6. **Showed parse tree instead of AST** in the diagram → lose 1 mark; the question explicitly asks for **AST**.\n\n---\n\n### 📌 Topic Recap & Important Things to Remember\n\n- ✅ **BNF** uses `::=`, `< >`, `|`; **YACC** uses `:`, no brackets, `;` at line end.\n- ✅ A YACC file has **three sections**: Declarations → `%%` → Rules → `%%` → Auxiliary C code.\n- ✅ `$$` = value of LHS; `$N` = value of the Nth RHS symbol.\n- ✅ AST node = `malloc()` + fill `type/op/value/left/right` + return.\n- ✅ **Operator precedence in YACC** = order of `%left`/`%right` (lower precedence declared *first*).\n- ✅ **Epsilon productions** are written as a bare `|` on a line by itself.\n- ✅ **Single-character terminals** use `'a'`, `'+'`, etc.; multi-character tokens need `%token` declarations.\n- ✅ Compile: `yacc -d file.y && gcc y.tab.c -o file` (or with LEX: `lex file.l && gcc lex.yy.c y.tab.c -o file`).\n- ✅ **AST** ≠ **Parse Tree**: AST is compact (no non-terminals, no parens); parse tree is verbose.\n- ✅ **Tree depth encodes precedence**: deeper operators evaluated first.\n\n---\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-engineering-7-pccsl607-mExperiment 2-t6",
          "title": "Write a program to find First and Follow of any given grammar",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL607 - SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Write a program to find First and Follow of any given grammar\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\nIn Compiler Design, **FIRST** and **FOLLOW** are two fundamental helper functions used during the construction of **LL(1) predictive parsing tables**. Together, they allow the parser to make a deterministic, single-token look-ahead decision when expanding a non-terminal on top of the parse stack.\n\n**Definition 1 — FIRST(α):**\nFor any string of grammar symbols $\\alpha$ (where $\\alpha \\in (V \\cup \\Sigma)^*$), $\\text{FIRST}(\\alpha)$ is the set of terminal symbols that can begin any string derivable from $\\alpha$. If $\\alpha$ can derive $\\epsilon$ (epsilon), then $\\epsilon$ is also included in $\\text{FIRST}(\\alpha)$.\n\nFormally,\n$$\\text{FIRST}(\\alpha) = \\Big\\{ a \\in \\Sigma \\;\\Big|\\; \\alpha \\Rightarrow^* a\\beta,\\; a\\beta \\in (V \\cup \\Sigma)^* \\Big\\} \\cup \\{ \\epsilon \\mid \\alpha \\Rightarrow^* \\epsilon \\}$$\n\n**Definition 2 — FOLLOW(A):**\nFor a non-terminal $A \\in V$ (where $V$ is the set of non-terminals and $\\Sigma$ is the set of terminals), $\\text{FOLLOW}(A)$ is the set of terminal symbols that can immediately follow $A$ in any sentential form derivable from the start symbol $S$. The end-marker **$** (representing input end) is included in $\\text{FOLLOW}(S)$.\n\nFormally,\n$$\\text{FOLLOW}(A) = \\Big\\{ a \\in \\Sigma \\;\\Big|\\; S \\Rightarrow^* \\alpha A a \\beta,\\; a \\in \\Sigma \\Big\\} \\cup \\{ \\$ \\mid S \\Rightarrow^* \\alpha A \\}$$\n\n> [!IMPORTANT]\n> **KTU 2024 High-Yield Distinction**\n> - **FIRST** answers: *\"What terminal can *appear first* in the expansion of this symbol?\"*\n> - **FOLLOW** answers: *\"What terminal can *come after* this non-terminal in some valid derivation?\"*\n> - FIRST is computed for **both terminals and non-terminals**, but FOLLOW is defined **only for non-terminals**.\n\n### 1.2 Conceptual Analogy — The \"Recipe Card\" Intuition\n\nImagine you are teaching a robot to cook recipes. Each non-terminal is a *recipe step* (e.g., `Sauce`, `Topping`) and each terminal is a *physical ingredient* (e.g., `tomato`, `cheese`).\n\n- **FIRST(step)** = *\"What is the very first ingredient you will touch when starting this step?\"* For example, `FIRST(Sauce)` might contain `tomato` or `cream` because the sauce can start with either.\n- **FOLLOW(step)** = *\"What ingredient is most likely to come after this step is done?\"* For example, `FOLLOW(Sauce)` might contain `cheese` because the next layer after the sauce is usually cheese.\n\nWhen the robot is mid-recipe and holds a step in its hand, FIRST tells it *what to grab first*, and FOLLOW tells it *what to expect next*. Together they form the parsing table — the robot's ultimate \"what do I do now?\" cheat sheet.\n\n### 1.3 Role in Compiler Architecture\n\n```\nSource Code  →  Lexical Analyzer  →  Tokens  →  Syntax Analyzer (Parser)\n                                                       │\n                                                       ▼\n                                              FIRST & FOLLOW Sets\n                                                       │\n                                                       ▼\n                                              LL(1) Parsing Table\n                                                       │\n                                                       ▼\n                                                  Parse Tree / AST\n```\n\n> [!NOTE]\n> In KTU's **PCCSL607 Systems Lab**, the experiment is implemented programmatically in **C / C++ / Python**. You will read productions of the form `E->E+T|T` and print the FIRST/FOLLOW sets. Always hardcode the start symbol so that `$` is correctly added to FOLLOW(start).\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Algorithmic Rules for FIRST Sets\n\n**Rule F1 — Terminal as first symbol:**\nIf $X$ is a terminal, then $\\text{FIRST}(X) = \\{ X \\}$.\n\n**Rule F2 — Non-terminal with epsilon-production:**\nIf $X \\rightarrow \\epsilon$ is a production, then add $\\epsilon$ to $\\text{FIRST}(X)$.\n\n**Rule F3 — Non-terminal with terminal RHS start:**\nIf $X \\rightarrow a\\alpha$ where $a$ is a terminal, then add $a$ to $\\text{FIRST}(X)$.\n\n**Rule F4 — Non-terminal with non-terminal RHS start (chained):**\nIf $X \\rightarrow Y_1 Y_2 \\dots Y_k$ is a production:\n- Add $\\text{FIRST}(Y_1) \\setminus \\{\\epsilon\\}$ to $\\text{FIRST}(X)$.\n- If $\\epsilon \\in \\text{FIRST}(Y_1)$, also add $\\text{FIRST}(Y_2) \\setminus \\{\\epsilon\\}$.\n- Continue similarly: if $\\epsilon \\in \\text{FIRST}(Y_1) \\cap \\dots \\cap \\text{FIRST}(Y_{i-1})$, then add $\\text{FIRST}(Y_i) \\setminus \\{\\epsilon\\}$.\n- If $\\epsilon$ is in the FIRST of **all** $Y_1 \\dots Y_k$, then add $\\epsilon$ to $\\text{FIRST}(X)$.\n\n### 2.2 Algorithmic Rules for FOLLOW Sets\n\n**Rule L1 — Start symbol initialization:**\nPlace `$` in $\\text{FOLLOW}(S)$, where $S$ is the start symbol.\n\n**Rule L2 — Production of form $A \\rightarrow \\alpha B \\beta$:**\nEverything in $\\text{FIRST}(\\beta)$ **except** $\\epsilon$ is added to $\\text{FOLLOW}(B)$.\n\n**Rule L3 — Production of form $A \\rightarrow \\alpha B$ or $A \\rightarrow \\alpha B \\beta$ where $\\epsilon \\in \\text{FIRST}(\\beta)$:**\nEverything in $\\text{FOLLOW}(A)$ is added to $\\text{FOLLOW}(B)$.\n\n> [!IMPORTANT]\n> **KTU 2024 Pitfall — Epsilon in FOLLOW?**\n> $\\epsilon$ is **NEVER** added to a FOLLOW set. FOLLOW always contains terminals and `$`, never the empty string. This is one of the most common errors in KTU lab records.\n\n### 2.3 KTU Formula / Rule Cheat Sheet\n\n| Symbol / Notation | Meaning | Domain |\n| :--- | :--- | :--- |\n| $V$ | Set of non-terminals (uppercase letters) | Grammar |\n| $\\Sigma$ | Set of terminals (lowercase / digits) | Grammar |\n| $S$ | Start symbol (designated) | Grammar |\n| $\\epsilon$ | Empty string (epsilon) | Special |\n| $\\$$ | End-of-input marker | Parsing |\n| $\\text{FIRST}(X)$ | Set of terminals that can begin $X$ | $V \\cup \\Sigma$ |\n| $\\text{FOLLOW}(A)$ | Set of terminals that can follow $A$ | $V$ only |\n| $A \\rightarrow \\alpha B \\beta$ | Standard production form | Grammar |\n\n### 2.4 Real-World Engineering Utility\n\n1. **Predictive Parsers (LL(1))** — Used in compilers like GCC's older front-ends and many educational compilers to enable recursive-descent parsing without backtracking.\n2. **Compiler Front-End Validation** — A grammar is LL(1) (and thus predictable) **iff** the FIRST/FOLLOW-derived parsing table contains no multiple entries in any cell.\n3. **Grammar Engineering** — Helps identify ambiguity and left-recursion in newly designed DSLs (Domain-Specific Languages) for tools, databases, and AI configuration systems.\n4. **Static Code Analysis Tools** — Linters and IDEs use similar set-based analyses to predict token sequences in partially-parsed code.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Reference Grammar (Worked Example)\n\nConsider the classic arithmetic expression grammar used in KTU examinations:\n\n$$\n\\begin{aligned}\nE &\\rightarrow E + T \\mid T \\\\\nT &\\rightarrow T * F \\mid F \\\\\nF &\\rightarrow ( E ) \\mid \\text{id}\n\\end{aligned}\n$$\n\nHere, $V = \\{E, T, F\\}$, $\\Sigma = \\{+, *, (, ), \\text{id}\\}$, and $S = E$.\n\n### 3.2 Exhaustive FIRST Set Computation\n\n**Step 1 — Compute FIRST(F):**\n- Production $F \\rightarrow (E)$: rule F3 → add `\\(` to FIRST(F).\n- Production $F \\rightarrow \\text{id}$: rule F3 → add `id` to FIRST(F).\n- No $\\epsilon$-production for $F$.\n\n$$\n\\text{FIRST}(F) = \\{\\text{\\texttt{(}}, \\text{\\texttt{id}}\\}\n$$\n\n**Step 2 — Compute FIRST(T):**\n- Production $T \\rightarrow T * F$: rule F4 (chained) → add FIRST(T) $\\setminus \\{\\epsilon\\}$. Since $\\epsilon \\notin$ FIRST(T), we simply add FIRST(T) itself, but this is recursive — we defer.\n- Production $T \\rightarrow F$: rule F4 → add FIRST(F) = $\\{\\text{\\texttt{(}}, \\text{\\texttt{id}}\\}$.\n\nCombining both: $\\text{FIRST}(T) = \\{\\text{\\texttt{(}}, \\text{\\texttt{id}}\\}$.\n\n**Step 3 — Compute FIRST(E):**\n- Production $E \\rightarrow E + T$: deferred (recursive).\n- Production $E \\rightarrow T$: rule F4 → add FIRST(T) = $\\{\\text{\\texttt{(}}, \\text{\\texttt{id}}\\}$.\n\n$$\n\\boxed{\\text{FIRST}(E) = \\text{FIRST}(T) = \\text{FIRST}(F) = \\{\\text{\\texttt{(}}, \\text{\\texttt{id}}\\}}\n$$\n\n### 3.3 Exhaustive FOLLOW Set Computation\n\n**Step 1 — Initialize (Rule L1):**\n$$\n\\text{FOLLOW}(E) = \\{\\$\\}\n$$\n\n**Step 2 — Apply Rule L2 to production $E \\rightarrow E + T$:**\n$B = T$, $\\beta = \\epsilon$ (nothing after $T$). Apply Rule L3 — add FOLLOW(E) to FOLLOW(T).\n$$\n\\text{FOLLOW}(T) = \\{\\$\\} \\cup \\text{FOLLOW}(E) = \\{\\$\\}\n$$\n\n**Step 3 — Apply Rule L2 to production $E \\rightarrow T$:**\n$B = T$, $\\beta = \\epsilon$. Rule L3 — add FOLLOW(E) to FOLLOW(T).\n$$\n\\text{FOLLOW}(T) = \\{\\$, +\\}\n$$\n\n**Step 4 — Apply Rule L2 to production $T \\rightarrow T * F$:**\n$B = F$, $\\beta = \\epsilon$. Rule L3 — add FOLLOW(T) to FOLLOW(F).\n$$\n\\text{FOLLOW}(F) = \\{\\$, +\\}\n$$\n\n**Step 5 — Apply Rule L2 to production $T \\rightarrow F$:**\n$B = F$, $\\beta = \\epsilon$. Rule L3 — add FOLLOW(T) to FOLLOW(F).\n$$\n\\text{FOLLOW}(F) = \\{\\$, +, *\\}\n$$\n\n**Step 6 — Apply Rule L2 to production $F \\rightarrow (E)$:**\n$B = E$, $\\beta = \\text{\\texttt{)}}$. Rule L2 — add FIRST(`)`) = $\\{\\text{\\texttt{)}}\\}$ to FOLLOW(E).\n$$\n\\text{FOLLOW}(E) = \\{\\$, \\text{\\texttt{)}}\\}\n$$\n\n**Step 7 — Re-evaluate E from Step 2 (closed fixed-point):**\n$E \\rightarrow E + T$ again: nothing new to add.\n\n$$\n\\boxed{\\text{FOLLOW}(E) = \\{\\$, \\text{\\texttt{)}}\\}, \\quad \\text{FOLLOW}(T) = \\{\\$, +\\}, \\quad \\text{FOLLOW}(F) = \\{\\$, +, *\\}}\n$$\n\n### 3.4 Final Analytical Matrix (Consolidated Result)\n\n| Non-Terminal | FIRST | FOLLOW |\n| :--- | :--- | :--- |\n| $E$ | $\\{\\text{\\texttt{(}}, \\text{\\texttt{id}}\\}$ | $\\{\\$, \\text{\\texttt{)}}\\}$ |\n| $T$ | $\\{\\text{\\texttt{(}}, \\text{\\texttt{id}}\\}$ | $\\{\\$, +, *\\}$ |\n| $F$ | $\\{\\text{\\texttt{(}}, \\text{\\texttt{id}}\\}$ | $\\{\\$, +, *\\}$ |\n\n### 3.5 Reference C-Program Skeleton (KTU Lab Expectation)\n\n```c\n#include <stdio.h>\n#include <string.h>\n#include <ctype.h>\n\n#define MAX 20\n\nchar prod[MAX][MAX];\nchar ter[MAX], nonter[MAX];\nint n;\n\nint isNonTerminal(char c) {\n    return (c >= 'A' && c <= 'Z');\n}\n\nvoid findFirst(char c, int visited[]) {\n    if (!isNonTerminal(c)) {\n        printf(\"%c \", c);\n        return;\n    }\n    if (visited[c - 'A']) return;\n    visited[c - 'A'] = 1;\n    for (int i = 0; i < n; i++) {\n        if (prod[i][0] == c) {\n            if (prod[i][3] == '#') {           // epsilon production\n                printf(\"# \");\n            } else {\n                int j = 3;\n                while (prod[i][j] != '\\0') {\n                    int temp[26] = {0};\n                    findFirst(prod[i][j], temp);\n                    if (strchr(ter, prod[i][j]) == NULL && prod[i][j] != '#')\n                        break;\n                    j++;\n                }\n            }\n        }\n    }\n}\n\nvoid findFollow(char c) {\n    if (c == prod[0][0]) printf(\"$ \");          // start symbol\n    for (int i = 0; i < n; i++) {\n        for (int j = 3; prod[i][j] != '\\0'; j++) {\n            if (prod[i][j] == c) {\n                if (prod[i][j+1] != '\\0') {\n                    if (!isNonTerminal(prod[i][j+1]))\n                        printf(\"%c \", prod[i][j+1]);\n                    else {\n                        int temp[26] = {0};\n                        findFirst(prod[i][j+1], temp);\n                    }\n                }\n                // Rule L3 propagation handled separately in full version\n            }\n        }\n    }\n}\n\nint main() {\n    printf(\"Enter number of productions: \");\n    scanf(\"%d\", &n);\n    printf(\"Enter productions (e.g., E->E+T, use # for epsilon):\\n\");\n    for (int i = 0; i < n; i++) scanf(\"%s\", prod[i]);\n\n    printf(\"\\n--- FIRST sets ---\\n\");\n    int visited[26] = {0};\n    findFirst('E', visited);\n\n    printf(\"\\n--- FOLLOW sets ---\\n\");\n    findFollow('E');\n\n    return 0;\n}\n```\n\n> [!NOTE]\n> The above is the **reference skeleton**; full lab records must include iterative fixed-point loops (because FIRST/FOLLOW rules reference each other transitively). KTU evaluators check for the `$` marker in FOLLOW(start) and proper handling of `#` (epsilon).\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Algorithm Flowchart — FIRST Computation\n\n```mermaid\nflowchart TD\n    A[Start: For each non-terminal A] --> B[Mark A as visited]\n    B --> C[Scan all productions of A]\n    C --> D{Is RHS a terminal?}\n    D -- Yes --> E[Add terminal to FIRST A]\n    D -- No --> F{Is RHS epsilon?}\n    F -- Yes --> G[Add epsilon to FIRST A]\n    F -- No --> H[Recurse on first symbol of RHS]\n    H --> I{Did recursed symbol produce epsilon?}\n    I -- Yes --> J[Add FIRST of next symbol]\n    I -- No --> K[Stop for this production]\n    E --> L[Next production]\n    G --> L\n    J --> L\n    K --> L\n    L --> C\n    C --> M[All productions done?]\n    M -- Yes --> N[Fixed point reached: Print FIRST A]\n    N --> O[Move to next non-terminal]\n```\n\n### 4.2 Algorithm Flowchart — FOLLOW Computation\n\n```mermaid\nflowchart TD\n    A[Start: For each non-terminal A] --> B{Is A the start symbol?}\n    B -- Yes --> C[Add dollar to FOLLOW A]\n    B -- No --> D[Scan all productions]\n    C --> D\n    D --> E{A appears at position i in RHS?}\n    E -- No --> F[Skip this production]\n    E -- Yes --> G{Is there a symbol at i plus 1?}\n    G -- Yes --> H[Add FIRST of that symbol minus epsilon]\n    G -- No --> I[Add FOLLOW of LHS to FOLLOW A]\n    H --> J{Is that symbol a non-terminal producing epsilon?}\n    J -- Yes --> I\n    J -- No --> K[Continue]\n    I --> L[Next occurrence of A]\n    K --> L\n    L --> D\n    D --> M[All productions done?]\n    M -- No --> D\n    M -- Yes --> N[Fixed point reached: Print FOLLOW A]\n    N --> O[Move to next non-terminal]\n```\n\n### 4.3 Block-Level Functional Architecture — LL(1) Parser Pipeline\n\n```mermaid\nflowchart LR\n    subgraph inputLayer[Input Stage]\n        I1[Grammar Productions]\n        I2[Start Symbol S]\n    end\n    subgraph analysisLayer[Set Analysis Stage]\n        P1[FIRST Set Builder]\n        P2[FOLLOW Set Builder]\n        P3[Nullable Set Builder]\n    end\n    subgraph tableLayer[Table Construction Stage]\n        T1[Parsing Table MNT a]\n        T2[Conflict Detector]\n    end\n    subgraph parseLayer[Parsing Stage]\n        R1[LL1 Recursive Descent Parser]\n        R2[Parse Tree Output]\n    end\n    I1 --> P1\n    I1 --> P2\n    I2 --> P1\n    I2 --> P2\n    P1 --> T1\n    P2 --> T1\n    P1 --> P3\n    P3 --> P2\n    T1 --> T2\n    T2 --> R1\n    T1 --> R1\n    R1 --> R2\n```\n\n### 4.4 Dependency Graph — How Rules Reference Each Other\n\n```mermaid\ngraph TD\n    rF1[Rule F1: Terminal FIRST] --> finalSet[Final FIRST Set]\n    rF2[Rule F2: Epsilon] --> finalSet\n    rF3[Rule F3: Terminal RHS] --> finalSet\n    rF4[Rule F4: Chained FIRST] --> finalSet\n    rL1[Rule L1: Start dollar] --> finalFollow[Final FOLLOW Set]\n    rL2[Rule L2: FIRST beta] --> finalFollow\n    rL3[Rule L3: Propagate FOLLOW] --> finalFollow\n    finalSet --> rL2\n    rF4 --> finalSet\n    rL3 --> finalFollow\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **`[KTU University Exam — Dec 2023]`**\n> **Q1. Define FIRST and FOLLOW. Why are they needed in compiler design?**\n>\n> **Model Answer:**\n> $\\text{FIRST}(\\alpha)$ is the set of terminals that begin strings derivable from $\\alpha$. $\\text{FOLLOW}(A)$ for a non-terminal $A$ is the set of terminals that can immediately follow $A$ in some sentential form, including `$` if $A$ can be the rightmost symbol in a derivation from the start symbol. They are essential for constructing **LL(1) predictive parsing tables**, which enable a parser to make a single-token look-ahead decision without backtracking, thereby improving parser efficiency and ensuring deterministic parsing for suitable grammars. *(3 marks)*\n\n> **`[KTU University Exam — May 2024]`**\n> **Q2. State and explain the rule for adding $\\epsilon$ to FIRST and FOLLOW sets.**\n>\n> **Model Answer:**\n> $\\epsilon$ is added to $\\text{FIRST}(X)$ whenever $X$ directly produces $\\epsilon$ (production $X \\rightarrow \\epsilon$) or whenever all symbols on the right-hand side of a production $X \\rightarrow Y_1 Y_2 \\dots Y_k$ have $\\epsilon$ in their FIRST sets. **However, $\\epsilon$ is NEVER added to any FOLLOW set** — FOLLOW contains only terminals and the end-marker `$`. This asymmetry is a frequent KTU evaluation point. *(3 marks)*\n\n---\n\n### 5.2 Part B — Long Answer Questions (14 Marks, Internal Choice)\n\n> **`[KTU University Exam — Dec 2023]`**\n> **Question A (14 Marks):**\n> **(a)** For the grammar below, compute the FIRST sets of all non-terminals. *(7 marks)*\n> **(b)** Compute the FOLLOW sets of all non-terminals and verify if the grammar is LL(1). *(7 marks)*\n>\n> Grammar:\n> $$S \\rightarrow A B \\mid b C$$\n> $$A \\rightarrow a \\mid \\epsilon$$\n> $$B \\rightarrow b \\mid \\epsilon$$\n> $$C \\rightarrow c \\mid \\epsilon$$\n\n### Model Solution to Question A:\n\n**(a) FIRST Set Computation (7 marks):**\n\nStep 1 — FIRST(A):\n- $A \\rightarrow a$: add `a`.\n- $A \\rightarrow \\epsilon$: add $\\epsilon$.\n\n$$\n\\boxed{\\text{FIRST}(A) = \\{\\text{\\texttt{a}}, \\epsilon\\}}\n$$\n\nStep 2 — FIRST(B):\n- $B \\rightarrow b$: add `b`.\n- $B \\rightarrow \\epsilon$: add $\\epsilon$.\n\n$$\n\\boxed{\\text{FIRST}(B) = \\{\\text{\\texttt{b}}, \\epsilon\\}}\n$$\n\nStep 3 — FIRST(C):\n- $C \\rightarrow c$: add `c`.\n- $C \\rightarrow \\epsilon$: add $\\epsilon$.\n\n$$\n\\boxed{\\text{FIRST}(C) = \\{\\text{\\texttt{c}}, \\epsilon\\}}\n$$\n\nStep 4 — FIRST(S):\n- $S \\rightarrow A B$: add FIRST(A) $\\setminus \\{\\epsilon\\}$ = $\\{a\\}$. Since $\\epsilon \\in$ FIRST(A), also add FIRST(B) $\\setminus \\{\\epsilon\\}$ = $\\{b\\}$. Since $\\epsilon \\in$ FIRST(B), add $\\epsilon$.\n- $S \\rightarrow b C$: add `b` (already present).\n\n$$\n\\boxed{\\text{FIRST}(S) = \\{\\text{\\texttt{a}}, \\text{\\texttt{b}}, \\epsilon\\}}\n$$\n\n**(b) FOLLOW Set Computation (7 marks):**\n\nStep 1 — Initialize: $\\text{FOLLOW}(S) = \\{\\$\\}$.\n\nStep 2 — $S \\rightarrow A B$: $B$ is at end → add FOLLOW(S) to FOLLOW(B). $\\text{FOLLOW}(B) = \\{\\$\\}$.\n\nStep 3 — $S \\rightarrow A B$: $A$'s follower is $B$, and $\\epsilon \\in$ FIRST(B), so add FOLLOW(S) to FOLLOW(A). Also add FIRST(B) $\\setminus \\{\\epsilon\\} = \\{b\\}$ to FOLLOW(A). So $\\text{FOLLOW}(A) = \\{\\$, b\\}$.\n\nStep 4 — $S \\rightarrow b C$: $C$ is at end → add FOLLOW(S) to FOLLOW(C). $\\text{FOLLOW}(C) = \\{\\$\\}$.\n\nStep 5 — $S \\rightarrow A B$: $A$'s follower is $B$; $\\epsilon \\in$ FIRST(B), so add FOLLOW(S) to FOLLOW(A) (already covered). Also, $B$'s follower is end, $\\epsilon \\in$ FIRST(B) propagated to A, but already considered.\n\nRe-check: Production $S \\rightarrow A B$ — A is followed by B, and $\\epsilon \\in$ FIRST(B) → add FOLLOW(S) to FOLLOW(A). Already done.\n\n$$\n\\boxed{\\text{FOLLOW}(S) = \\{\\$\\}, \\quad \\text{FOLLOW}(A) = \\{\\$, \\text{\\texttt{b}}\\}, \\quad \\text{FOLLOW}(B) = \\{\\$\\}, \\quad \\text{FOLLOW}(C) = \\{\\$\\}}\n$$\n\n**LL(1) Verification:**\n- $A$ has two productions: $a$ and $\\epsilon$. FIRST($a$) $\\cap$ FIRST($\\epsilon$) = $\\{a\\} \\cap \\{\\epsilon\\} = \\emptyset$. ✓\n- $B$: $\\{b\\} \\cap \\{\\epsilon\\} = \\emptyset$. ✓\n- $C$: $\\{c\\} \\cap \\{\\epsilon\\} = \\emptyset$. ✓\n- $S$: Productions $AB$ and $bC$. FIRST($AB$) = $\\{a, b, \\epsilon\\}$ and FIRST($bC$) = $\\{b\\}$. Intersection = $\\{b\\}$ ≠ ∅. **Grammar is NOT LL(1).** ✗\n\n---\n\n> **`[KTU University Exam — May 2024]`**\n> **Question B (14 Marks) — Alternative Choice:**\n> **(a)** Explain with an algorithm how the FIRST and FOLLOW sets of a Context-Free Grammar (CFG) are computed. Use the grammar $E \\rightarrow E + T \\mid T$ as illustration. *(7 marks)*\n> **(b)** For the grammar $S \\rightarrow a S b S \\mid b S a S \\mid \\epsilon$, compute the FIRST and FOLLOW sets. Is this grammar LL(1)? Justify. *(7 marks)*\n\n### Model Solution to Question B:\n\n**(a) Algorithm Description (7 marks):**\n\n**FIRST Algorithm:**\n1. For each terminal $a$: FIRST($a$) = $\\{a\\}$.\n2. If $X \\rightarrow \\epsilon$ exists: add $\\epsilon$ to FIRST($X$).\n3. If $X \\rightarrow Y_1 Y_2 \\dots Y_k$:\n   - Add FIRST($Y_1$) $\\setminus \\{\\epsilon\\}$ to FIRST($X$).\n   - If $\\epsilon \\in$ FIRST($Y_1$): add FIRST($Y_2$) $\\setminus \\{\\epsilon\\}$, and so on.\n   - If $\\epsilon$ is in FIRST of all $Y_i$: add $\\epsilon$ to FIRST($X$).\n4. Repeat until no change (fixed point).\n\n**FOLLOW Algorithm:**\n1. Place `$` in FOLLOW($S$) (start symbol).\n2. For $A \\rightarrow \\alpha B \\beta$: add FIRST($\\beta$) $\\setminus \\{\\epsilon\\}$ to FOLLOW($B$).\n3. For $A \\rightarrow \\alpha B$ or $A \\rightarrow \\alpha B \\beta$ with $\\epsilon \\in$ FIRST($\\beta$): add FOLLOW($A$) to FOLLOW($B$).\n4. Repeat until no change.\n\n**Illustration with $E \\rightarrow E + T \\mid T$:**\n- FIRST($T$) is computed based on additional productions given in the full grammar.\n- For the simplified case: FIRST($E$) = FIRST($T$) (from production $E \\rightarrow T$).\n- FOLLOW($E$) = $\\{\\$, )\\}$ (using Rule L3 from $E \\rightarrow E + T$ and $E \\rightarrow T$).\n- FOLLOW($T$) = FOLLOW($E$) $\\cup \\{+\\} = \\{\\$, ), +\\}$.\n\n**(b) Computation for $S \\rightarrow a S b S \\mid b S a S \\mid \\epsilon$ (7 marks):**\n\nFIRST(S):\n- From $S \\rightarrow \\epsilon$: add $\\epsilon$.\n- From $S \\rightarrow a S b S$: add `a`.\n- From $S \\rightarrow b S a S$: add `b`.\n\n$$\n\\boxed{\\text{FIRST}(S) = \\{\\text{\\texttt{a}}, \\text{\\texttt{b}}, \\epsilon\\}}\n$$\n\nFOLLOW(S):\n- Initialize: FOLLOW($S$) = $\\{\\$\\}$.\n- From $S \\rightarrow a S b S$: second $S$ is followed by `b`. Add `b` to FOLLOW($S$).\n- From $S \\rightarrow b S a S$: second $S$ is followed by `a`. Add `a` to FOLLOW($S$).\n- From $S \\rightarrow a S b S$: first $S$ is followed by `b`; FIRST($bS$) does not contain $\\epsilon$, so no propagation.\n- From $S \\rightarrow b S a S$: first $S$ is followed by `a`; no propagation.\n\n$$\n\\boxed{\\text{FOLLOW}(S) = \\{\\text{\\texttt{a}}, \\text{\\texttt{b}}, \\$\\}}\n$$\n\n**LL(1) Verification:**\n- For $S$: Productions are $aSbS$, $bSaS$, $\\epsilon$.\n- FIRST($aSbS$) = $\\{a\\}$, FIRST($bSaS$) = $\\{b\\}$, FIRST($\\epsilon$) = $\\{\\epsilon\\}$.\n- Since $\\epsilon \\in$ FIRST($S$), we must check FOLLOW($S$) for the $\\epsilon$-production conflicts.\n- FOLLOW($S$) = $\\{a, b, \\$\\}$.\n- FOLLOW($S$) $\\cap$ FIRST($aSbS$) = $\\{a, b, \\$\\} \\cap \\{a\\} = \\{a\\}$ ≠ ∅. **Conflict detected.**\n\n$$\n\\boxed{\\text{Grammar is NOT LL(1).}}\n$$\n\nThe grammar is the well-known **ambiguous grammar for matched brackets** and is inherently non-LL(1); it requires an LR parser.\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mark-Deduction Zones — Read Carefully**\n> 1. **Forgetting `$` in FOLLOW(start):** The end-marker **must** be added to the FOLLOW set of the start symbol. Examiners specifically test this. Loss: up to **1 mark**.\n> 2. **Adding $\\epsilon$ to FOLLOW sets:** FOLLOW must NEVER contain $\\epsilon$. Loss: **1–2 marks**.\n> 3. **Not iterating to fixed point:** FIRST and FOLLOW rules are mutually recursive (FIRST depends on chained non-terminals; FOLLOW depends on FOLLOW of LHS). You must iterate until no new symbol is added. Loss: **2 marks**.\n> 4. **Confusing FIRST of LHS with FIRST of RHS:** FIRST($X$) is computed from the *right-hand sides* of $X$'s productions, never the left-hand side.\n> 5. **In Lab Exam:** When demonstrating output, ensure each non-terminal is printed on a separate line, formatted as `FIRST(E) = { (, id }` exactly — formatting matters in KTU records.\n\n---\n\n### 5.4 Topic Recap & Important Things to Remember\n\n- **FIRST** is defined for **all grammar symbols** (terminals + non-terminals); **FOLLOW** is defined **only for non-terminals**.\n- **$\\epsilon$ is a member of FIRST, never of FOLLOW.** (Hard rule.)\n- The end-marker **$** is **only** placed in FOLLOW of the **start symbol** initially.\n- **Rule F4 (chained):** If RHS is $Y_1 Y_2 \\dots Y_k$ and $\\epsilon$ is in FIRST of each $Y_i$, then $\\epsilon \\in$ FIRST of LHS.\n- **Rule L3 (propagation):** If $A \\rightarrow \\alpha B$ and $B$ can derive $\\epsilon$, then FOLLOW($A$) $\\subseteq$ FOLLOW($B$).\n- The sets must be computed **iteratively** until a **fixed point** is reached.\n- A grammar is **LL(1)** if and only if the parsing table built from FIRST/FOLLOW has **no multiply-defined entries**.\n- **In the lab program:** Always:\n  1. Hardcode the start symbol.\n  2. Use `#` or `e` for $\\epsilon$ in input productions.\n  3. Print `$` in FOLLOW(start).\n  4. Terminate the fixed-point loop only when no set changes in an iteration.\n- **Real-world link:** GCC's older recursive-descent parsers, ANTLR-generated parsers, and many IDE syntax highlighters rely on FIRST/FOLLOW-equivalent computations.\n\n---\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-engineering-7-pccsl607-mExperiment 2-t7",
          "title": "Design and implement a recursive descent parser for a given grammar",
          "content": "<!-- SECTION_1_START -->\n\n> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL607 - SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Design and implement a recursive descent parser for a given grammar\n\n# Experiment 2 — Design and Implement a Recursive Descent Parser for a Given Grammar\n\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Academic Definition\n\nA **Recursive Descent Parser (RDP)** is a top-down, predictive, syntax-analysis technique in which each **non-terminal symbol** of a context-free grammar is realized as a **distinct recursive procedure (function)**. The parser begins with the start symbol of the grammar and attempts to construct a parse tree by recursively invoking the procedure corresponding to the non-terminal that the parser currently expects to recognize. To make deterministic, backtrack-free decisions at every step, the grammar must satisfy the **LL(1)** property — meaning the parser scans the input **Left-to-right**, produces a **Leftmost derivation**, and uses only **1 token of look-ahead** to select the correct production rule.\n\nFor a grammar to be parsed by RDP without backtracking, the **FIRST** sets of the right-hand side alternatives of any non-terminal must be **pairwise disjoint**, and the parser must consult a **predictive parsing table** $M[A, a]$ indexed by a non-terminal $A$ and a terminal (look-ahead) $a$.\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight (PCCSL607 — Systems Lab, Module 2):**\n> Students are expected to (i) remove **left recursion**, (ii) perform **left factoring** if required, (iii) compute **FIRST** and **FOLLOW** sets, (iv) construct the **predictive parsing table**, and finally (v) **implement and execute** the recursive descent parser in C for a sample arithmetic-expression grammar and validate it against test strings.\n\n### 1.2 Conceptual Analogy — Reading a Recipe with Sub-Procedures\n\nImagine you are reading a recipe book and the recipe says: *\"Make a cake (then make icing (then decorate (then serve))))\"*. Instead of trying to understand everything at once, you break it down:\n\n- Procedure **`MakeCake`** asks: \"Do I need eggs?\" → calls `BeatEggs`\n- Procedure **`MakeIcing`** asks: \"Do I need sugar?\" → calls `MixSugar`\n- Procedure **`BeatEggs`** is a sub-routine that returns control to `MakeCake` after finishing\n\nA **recursive descent parser does exactly the same thing** with grammar rules. Each grammar rule (non-terminal) becomes a function. When a function sees its rule, it checks the next word (token) in the input and decides which sub-function to call next. The **\"sub-function\"** concept matches a **production rule**, and the **\"next word\"** matches the **look-ahead token**.\n\n> [!NOTE]\n> **Real-world analogy summary:**\n> Recipe Book ≡ Grammar | Recipe Step ≡ Production Rule | Chef checking the next ingredient ≡ Parser reading next token | Sub-recipe calls ≡ Recursive function calls.\n\n### 1.3 Standard Metrics and Parameters\n\nThe following parameters govern the behaviour of any RDP implementation:\n\n| Parameter | Symbol / Formula | Standard Value / Range |\n| :--- | :--- | :--- |\n| Look-ahead tokens | $k$ | **$k = 1$** (LL(1) class) |\n| Maximum recursion depth | $D$ | Bounded by input length $n$ |\n| Time complexity | $T(n)$ | $\\mathcal{O}(n)$ for LL(1) grammars |\n| Space complexity (call stack) | $S(n)$ | $\\mathcal{O}(n)$ worst case |\n| Grammar size | $G = (V, T, P, S)$ | $V$ non-terminals, $T$ terminals, $P$ productions, $S$ start symbol |\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Conditions for Recursive Descent Parsability\n\nA grammar is suitable for RDP **if and only if** it can be transformed into an **LL(1) grammar**. The two mandatory pre-processing steps are:\n\n1. **Elimination of Left Recursion** — both immediate and indirect.\n2. **Left Factoring** — to combine productions that share a common prefix.\n\n> [!IMPORTANT]\n> KTU examiners frequently award **2 marks** just for correctly identifying whether the given grammar is LL(1) and stating the transformations applied. Always show the *transformed* grammar in your answer.\n\n### 2.2 Step-by-Step Logic for Constructing an RDP\n\nThe construction follows a deterministic pipeline:\n\n1. **Input**: A context-free grammar $G = (V, T, P, S)$.\n2. **Pre-process** the grammar (remove left recursion, left factor).\n3. **Compute FIRST sets** for all grammar symbols.\n4. **Compute FOLLOW sets** for all non-terminals.\n5. **Build the predictive parsing table** $M[A, a]$.\n6. **Verify** that $M$ has **no multiple entries** (this confirms LL(1) property).\n7. **Translate** each production $A \\to \\alpha$ into a procedure `A()` in C.\n8. **Execute** the parser against test inputs.\n\n### 2.3 FIRST and FOLLOW Set Definitions\n\n$$\n\\text{FIRST}(\\alpha) = \\{\\, a \\in T \\cup \\{\\varepsilon\\} \\mid \\alpha \\Rightarrow^{*} a\\beta \\,\\}\n$$\n\n$$\n\\text{FOLLOW}(A) = \\{\\, a \\in T \\cup \\{\\$\\} \\mid S \\Rightarrow^{*} \\alpha A a \\beta \\,\\}\n$$\n\nwhere **\\$** is the end-of-input marker.\n\n**Rules for computing FIRST:**\n\n- If $X$ is a terminal, then $\\text{FIRST}(X) = \\{X\\}$.\n- If $X \\to \\varepsilon$ is a production, add $\\varepsilon$ to $\\text{FIRST}(X)$.\n- If $X \\to Y_1 Y_2 \\ldots Y_k$, add $\\text{FIRST}(Y_1) \\setminus \\{\\varepsilon\\}$; if $\\varepsilon \\in \\text{FIRST}(Y_1)$, add $\\text{FIRST}(Y_2) \\setminus \\{\\varepsilon\\}$; and so on.\n\n**Rules for computing FOLLOW:**\n\n- Place **\\$** in $\\text{FOLLOW}(S)$, where $S$ is the start symbol.\n- If $A \\to \\alpha B \\beta$, then every element of $\\text{FIRST}(\\beta) \\setminus \\{\\varepsilon\\}$ is in $\\text{FOLLOW}(B)$.\n- If $A \\to \\alpha B$ or $A \\to \\alpha B \\beta$ with $\\varepsilon \\in \\text{FIRST}(\\beta)$, then $\\text{FOLLOW}(A) \\subseteq \\text{FOLLOW}(B)$.\n\n### 2.4 Predictive Parsing Table Construction\n\nFor every production $A \\to \\alpha$ in the grammar:\n\n$$\nM[A, a] = A \\to \\alpha \\quad \\text{for every } a \\in \\text{FIRST}(\\alpha)\n$$\n\n$$\nM[A, b] = A \\to \\alpha \\quad \\text{for every } b \\in \\text{FOLLOW}(A), \\text{ if } \\varepsilon \\in \\text{FIRST}(\\alpha)\n$$\n\n> [!WARNING]\n> If the same cell $M[A, a]$ receives **more than one production**, the grammar is **NOT LL(1)** and cannot be parsed by a non-backtracking recursive descent parser.\n\n### 2.5 KTU Formula Sheet / Cheat Sheet\n\n| # | Concept | Formula / Rule | Purpose |\n| :--- | :--- | :--- | :--- |\n| 1 | LL(1) condition | $\\text{FIRST}(\\alpha_i) \\cap \\text{FIRST}(\\alpha_j) = \\emptyset$ for all $A \\to \\alpha_i \\mid \\alpha_j$ | Ensures unique production choice |\n| 2 | FIRST base | $\\text{FIRST}(a) = \\{a\\}$ for terminal $a$ | Base case for FIRST |\n| 3 | FIRST epsilon | $\\varepsilon \\in \\text{FIRST}(A)$ if $A \\to \\varepsilon$ | Detects nullable non-terminals |\n| 4 | FOLLOW start | $\\$ \\in \\text{FOLLOW}(S)$ | Marks end-of-input |\n| 5 | FOLLOW propagation | $A \\to \\alpha B \\beta \\Rightarrow \\text{FIRST}(\\beta)\\setminus\\{\\varepsilon\\} \\subseteq \\text{FOLLOW}(B)$ | Carries terminals to followers |\n| 6 | Table fill rule | $a \\in \\text{FIRST}(\\alpha) \\Rightarrow M[A, a] = A \\to \\alpha$ | Builds parsing table entries |\n| 7 | Direct left recursion | $A \\to A\\alpha \\mid \\beta$ becomes $A \\to \\beta A'$ and $A' \\to \\alpha A' \\mid \\varepsilon$ | Eliminates immediate left recursion |\n| 8 | Indirect left recursion | Substitute and apply direct-elimination rule | Eliminates indirect left recursion |\n| 9 | Left factoring | $A \\to \\alpha\\beta_1 \\mid \\alpha\\beta_2$ becomes $A \\to \\alpha A'$ and $A' \\to \\beta_1 \\mid \\beta_2$ | Removes common prefixes |\n| 10 | Complexity | $T(n) = \\mathcal{O}(n)$, $S(n) = \\mathcal{O}(n)$ | Bounds RDP performance |\n\n### 2.6 Real-World Engineering Utility\n\nRecursive descent parsers form the **backbone of compilers and interpreters** for most modern programming languages. Real-world deployments include:\n\n- **GCC** uses recursive descent techniques for several language front-ends.\n- **Python's `pgen`** parser generator emits recursive descent parsers.\n- **JSON / XML / HTML parsers** in browsers and web servers are often hand-written RDPs.\n- **Expression evaluators** in spreadsheets, calculators, and database query engines use RDP for arithmetic and Boolean sub-grammars.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Reference Grammar for the Experiment\n\nWe adopt the classical **arithmetic expression grammar** (without left recursion), which is the de-facto KTU lab standard for this experiment:\n\n$$\nE \\to T\\,E'\n$$\n\n$$\nE' \\to +\\,T\\,E' \\;\\mid\\; -\\,T\\,E' \\;\\mid\\; \\varepsilon\n$$\n\n$$\nT \\to F\\,T'\n$$\n\n$$\nT' \\to *\\,F\\,T' \\;\\mid\\; /\\,F\\,T' \\;\\mid\\; \\varepsilon\n$$\n\n$$\nF \\to (E) \\;\\mid\\; \\text{id} \\;\\mid\\; \\text{num}\n$$\n\n**Non-terminals**: $V = \\{E, E', T, T', F\\}$ | **Terminals**: $T = \\{+, -, *, /, (, ), \\text{id}, \\text{num}\\}$ | **Start symbol**: $S = E$.\n\n### 3.2 Derivation of FIRST Sets\n\nApply the FIRST computation rules iteratively until no change occurs.\n\n**Step 1 — Base assignments from productions**\n\n- $F \\to (E)$ gives $\\text{FIRST}(F) \\supseteq \\{(\\}$\n- $F \\to \\text{id}$ gives $\\text{FIRST}(F) \\supseteq \\{\\text{id}\\}$\n- $F \\to \\text{num}$ gives $\\text{FIRST}(F) \\supseteq \\{\\text{num}\\}$\n- $T' \\to *F T'$ gives $\\text{FIRST}(T') \\supseteq \\{*\\}$\n- $T' \\to /F T'$ gives $\\text{FIRST}(T') \\supseteq \\{/\\}$\n\n**Step 2 — Propagation of epsilon**\n\n- $T' \\to \\varepsilon$ gives $\\varepsilon \\in \\text{FIRST}(T')$.\n- $E' \\to \\varepsilon$ gives $\\varepsilon \\in \\text{FIRST}(E')$.\n\n**Step 3 — Cascading effect on $T$ and $E$**\n\n$$\n\\text{FIRST}(T) = \\text{FIRST}(F) \\cup (\\text{FIRST}(T') \\text{ if } \\varepsilon \\in \\text{FIRST}(T'))\n$$\n\nSince $\\varepsilon \\in \\text{FIRST}(T')$, we include it as well. So:\n\n$$\n\\text{FIRST}(T) = \\{(\\,,\\, \\text{id}\\,,\\, \\text{num}\\,,\\, \\varepsilon\\}\n$$\n\nSimilarly:\n\n$$\n\\text{FIRST}(E) = \\text{FIRST}(T) \\cup (\\text{FIRST}(E') \\text{ if } \\varepsilon \\in \\text{FIRST}(T))\n$$\n\nSince $\\varepsilon \\in \\text{FIRST}(T)$ and $\\varepsilon \\in \\text{FIRST}(E')$:\n\n$$\n\\text{FIRST}(E) = \\{+\\,,\\,-,\\,(\\,,\\, \\text{id}\\,,\\, \\text{num}\\,,\\, \\varepsilon\\}\n$$\n\n**Step 4 — Final compiled FIRST set table**\n\n| Symbol | FIRST set |\n| :--- | :--- |\n| $E$ | $\\{+\\,,\\,-,\\,(\\,,\\, \\text{id}\\,,\\, \\text{num}\\,,\\, \\varepsilon\\}$ |\n| $E'$ | $\\{+\\,,\\,-,\\, \\varepsilon\\}$ |\n| $T$ | $\\{(\\,,\\, \\text{id}\\,,\\, \\text{num}\\,,\\, \\varepsilon\\}$ |\n| $T'$ | $\\{*\\,,\\,/\\,,\\, \\varepsilon\\}$ |\n| $F$ | $\\{(\\,,\\, \\text{id}\\,,\\, \\text{num}\\}$ |\n| $+$ | $\\{+\\}$ |\n| $-$ | $\\{-\\}$ |\n| $*$ | $\\{*\\}$ |\n| $/$ | $\\{/\\}$ |\n| $($ | $\\{(\\}$ |\n| $)$ | $\\{)\\}$ |\n| $\\text{id}$ | $\\{\\text{id}\\}$ |\n| $\\text{num}$ | $\\{\\text{num}\\}$ |\n\n### 3.3 Derivation of FOLLOW Sets\n\n**Step 1 — Start symbol**\n\n$$\n\\$ \\in \\text{FOLLOW}(E) \\quad \\text{(since } E \\text{ is the start symbol)}\n$$\n\n**Step 2 — Rule $E \\to T E'$**\n\n- $E'$ appears at the end of $E$'s right-hand side ⇒ $\\text{FOLLOW}(E) \\subseteq \\text{FOLLOW}(E')$.\n- $T$ is followed by $E'$, so $\\text{FIRST}(E') \\setminus \\{\\varepsilon\\} \\subseteq \\text{FOLLOW}(T)$.\n\nThis gives us $\\{+, -\\} \\subseteq \\text{FOLLOW}(T)$.\n\n**Step 3 — Rule $E' \\to +T E'$ and $E' \\to -T E'$**\n\n- $T$ is followed by $E'$, so $\\text{FIRST}(E') \\setminus \\{\\varepsilon\\} \\subseteq \\text{FOLLOW}(T)$ — same as above.\n\n**Step 4 — Rule $T \\to F T'$**\n\n- $T'$ is at the end of $T$'s right-hand side ⇒ $\\text{FOLLOW}(T) \\subseteq \\text{FOLLOW}(T')$.\n\nSo $\\text{FOLLOW}(T') \\supseteq \\{+, -, \\$\\}$.\n\n**Step 5 — Rule $T' \\to *F T'$ and $T' \\to /F T'$**\n\n- $F$ is followed by $T'$, so $\\text{FIRST}(T') \\setminus \\{\\varepsilon\\} \\subseteq \\text{FOLLOW}(F)$.\n\nThis gives $\\{*, /\\} \\subseteq \\text{FOLLOW}(F)$.\n\n**Step 6 — Rule $F \\to (E)$**\n\n- $E$ is followed by the terminal $)$, so $\\{)\\} \\subseteq \\text{FOLLOW}(E)$.\n\n**Step 7 — Propagation chain**\n\n- $\\text{FOLLOW}(E) \\subseteq \\text{FOLLOW}(E')$: so $\\text{FOLLOW}(E') \\supseteq \\{\\$, )\\}$.\n- $\\text{FOLLOW}(T) \\subseteq \\text{FOLLOW}(T')$: already covered.\n- $E' \\to +T E'$ with $\\varepsilon \\in \\text{FIRST}(E')$: $\\text{FOLLOW}(E') \\subseteq \\text{FOLLOW}(T)$.\n- $T' \\to *F T'$ with $\\varepsilon \\in \\text{FIRST}(T')$: $\\text{FOLLOW}(T') \\subseteq \\text{FOLLOW}(F)$.\n\nSo $\\text{FOLLOW}(T) \\supseteq \\{\\$, )\\}$ and $\\text{FOLLOW}(F) \\supseteq \\{+, -, *, /, \\$, )\\}$.\n\n**Step 8 — Final compiled FOLLOW set table**\n\n| Non-terminal | FOLLOW set |\n| :--- | :--- |\n| $E$ | $\\{\\$\\,,\\,)\\}$ |\n| $E'$ | $\\{\\$\\,,\\,),\\,+,\\,-\\}$ |\n| $T$ | $\\{\\$\\,,\\,),\\,+,\\,-\\}$ |\n| $T'$ | $\\{\\$\\,,\\,),\\,+,\\,-\\}$ |\n| $F$ | $\\{\\$\\,,\\,),\\,+,\\,-,\\,*,\\,/\\}$ |\n\n### 3.4 Construction of the Predictive Parsing Table\n\nUsing the rule $M[A, a] = A \\to \\alpha$ for every $a \\in \\text{FIRST}(\\alpha)$, and $M[A, b] = A \\to \\alpha$ for every $b \\in \\text{FOLLOW}(A)$ when $\\varepsilon \\in \\text{FIRST}(\\alpha)$:\n\n| Non-terminal | $+$ | $-$ | $*$ | $/$ | $($ | $)$ | $\\text{id}$ | $\\text{num}$ | $\\$$ |\n| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |\n| $E$ | $E \\to TE'$ | $E \\to TE'$ | | | $E \\to TE'$ | | $E \\to TE'$ | $E \\to TE'$ | |\n| $E'$ | $E' \\to +TE'$ | $E' \\to -TE'$ | | | | $E' \\to \\varepsilon$ | | | $E' \\to \\varepsilon$ |\n| $T$ | | | | | $T \\to FT'$ | | $T \\to FT'$ | $T \\to FT'$ | |\n| $T'$ | $T' \\to \\varepsilon$ | $T' \\to \\varepsilon$ | $T' \\to *FT'$ | $T' \\to /FT'$ | | $T' \\to \\varepsilon$ | | | $T' \\to \\varepsilon$ |\n| $F$ | | | | | $F \\to (E)$ | | $F \\to \\text{id}$ | $F \\to \\text{num}$ | |\n\n> [!NOTE]\n> Every cell contains **at most one** production ⇒ the grammar is **LL(1)** ⇒ the recursive descent parser is guaranteed to be **deterministic and backtrack-free**.\n\n### 3.5 Translation of Grammar Rules into C Procedures\n\nThe predictive table above is converted directly into one C function per non-terminal. Each function:\n\n1. Looks at the current look-ahead token.\n2. Selects the appropriate production from the table.\n3. For each terminal in the RHS, calls `match(token)`.\n4. For each non-terminal in the RHS, calls its corresponding function.\n\n**Skeleton C implementation (representative):**\n\n```c\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#include <ctype.h>\n\nchar input[100];\nint idx = 0;\nchar lookahead;\n\nvoid error(const char *msg) {\n    printf(\"Syntax Error: %s at position %d\\n\", msg, idx);\n    exit(1);\n}\n\nvoid match(char t) {\n    if (lookahead == t) {\n        lookahead = input[++idx];\n    } else {\n        error(\"Token mismatch\");\n    }\n}\n\nvoid E(void);   /* forward declarations */\nvoid Eprime(void);\nvoid T(void);\nvoid Tprime(void);\nvoid F(void);\n\nvoid E(void) {\n    /* M[E, lookahead] -> E -> T E' for ( , id, num, +, - */\n    T();\n    Eprime();\n}\n\nvoid Eprime(void) {\n    if (lookahead == '+') { match('+'); T(); Eprime(); }\n    else if (lookahead == '-') { match('-'); T(); Eprime(); }\n    /* else epsilon: M[E', )] = eps, M[E', $] = eps */\n}\n\nvoid T(void) {\n    F();\n    Tprime();\n}\n\nvoid Tprime(void) {\n    if (lookahead == '*') { match('*'); F(); Tprime(); }\n    else if (lookahead == '/') { match('/'); F(); Tprime(); }\n    /* else epsilon */\n}\n\nvoid F(void) {\n    if (lookahead == '(') { match('('); E(); match(')'); }\n    else if (lookahead == 'i') { /* id token */ match('i'); }\n    else if (lookahead == 'n') { /* num token */ match('n'); }\n    else error(\"Expected ( or id or num\");\n}\n\nint main(void) {\n    printf(\"Enter expression (end with $): \");\n    scanf(\"%s\", input);\n    lookahead = input[0];\n    E();\n    if (lookahead == '$') printf(\"Parsing Successful!\\n\");\n    else error(\"Extra input after valid expression\");\n    return 0;\n}\n```\n\n> [!NOTE]\n> In the lab, students commonly replace the single-character input with a **lexical analyser front-end** that emits tokens like `id` and `num` from real numeric input. KTU expects the lab record to include **at least 5 valid test inputs** and **at least 2 invalid test inputs** with corresponding outputs.\n\n### 3.6 Worked Example — Parsing the String `id + id * id $`\n\nThe parser executes as follows (call-stack snapshot):\n\n| Step | Active Call | Look-ahead | Action |\n| :---: | :--- | :---: | :--- |\n| 1 | `E()` | `id` | Calls `T()` |\n| 2 | `T()` | `id` | Calls `F()` |\n| 3 | `F()` | `id` | Matches `id`, returns to `T()` |\n| 4 | `Tprime()` | `+` | `+` not in $\\{*, /\\}$ → return to `E()` |\n| 5 | `Eprime()` | `+` | Matches `+`, calls `T()` |\n| 6 | `T()` | `id` | Calls `F()` |\n| 7 | `F()` | `id` | Matches `id`, returns |\n| 8 | `Tprime()` | `*` | Matches `*`, calls `F()` |\n| 9 | `F()` | `id` | Matches `id`, returns |\n| 10 | `Tprime()` | `$` | Return |\n| 11 | `Eprime()` | `$` | Return |\n| 12 | `E()` | `$` | Accept — **Parsing Successful!** |\n\n**Output**: `Parsing Successful!` — confirming precedence ($*$ binds tighter than $+$) is correctly enforced by the grammar.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n## 4. Structural Diagrams & Schematics\n\n### 4.1 High-Level Recursive Descent Parser Flow\n\nThe diagram below illustrates the top-down control flow of the parser, starting from the `main()` driver and propagating through the recursive procedure calls based on the predictive parsing table.\n\n```mermaid\nflowchart TD\n    A([main: read input, set lookahead]) --> B[Call E start]\n    B --> C[Call T]\n    C --> D[Call F]\n    D --> D1{lookahead in openParen, id, num}\n    D1 -- openParen --> D2[match openParen, call E, match closeParen]\n    D1 -- id --> D3[match id]\n    D1 -- num --> D4[match num]\n    D1 -- otherwise --> D5([Syntax Error])\n    D2 --> E1[Return to T]\n    D3 --> E1\n    D4 --> E1\n    E1 --> F[Call Tprime]\n    F --> F1{lookahead in star, slash}\n    F1 -- star --> F2[match star, call F, call Tprime]\n    F1 -- slash --> F3[match slash, call F, call Tprime]\n    F1 -- otherwise --> F4[Return epsilon]\n    F2 --> G1[Return to E]\n    F3 --> G1\n    F4 --> G1\n    G1 --> H[Call Eprime]\n    H --> H1{lookahead in plus, minus}\n    H1 -- plus --> H2[match plus, call T, call Eprime]\n    H1 -- minus --> H3[match minus, call T, call Eprime]\n    H1 -- otherwise --> H4[Return epsilon]\n    H2 --> I1[Return to main]\n    H3 --> I1\n    H4 --> I1\n    I1 --> I2{lookahead equals dollar}\n    I2 -- yes --> I3([Parsing Successful])\n    I2 -- no --> I5([Syntax Error extra input])\n```\n\n### 4.2 Recursive Call-Stack Architecture\n\nThis block diagram captures the **call-stack state** during the parse of `id + id * id $`, showing how each procedure returns control to its caller in strict **last-in-first-out** order.\n\n```mermaid\nflowchart TD\n    L0[main frame: lookahead equals id] --> L1[Call frame E]\n    L1 --> L2[Call frame T]\n    L2 --> L3[Call frame F matches id returns to T]\n    L3 --> L4[Call frame Tprime epsilon returns to T]\n    L4 --> L5[Return to E]\n    L5 --> L6[Call frame Eprime lookahead plus]\n    L6 --> L7[Match plus call T call F match id]\n    L7 --> L8[Call frame Tprime match star]\n    L8 --> L9[Call frame F match id returns]\n    L9 --> L10[Tprime epsilon returns]\n    L10 --> L11[Eprime epsilon returns]\n    L11 --> L12[Return to main lookahead dollar]\n    L12 --> L13([Parsing Successful])\n```\n\n### 4.3 Predictive Parsing Table — Visual Block\n\nA block-level view of the predictive parsing table, mapping each non-terminal row to its production rules keyed by look-ahead column.\n\n```mermaid\nflowchart LR\n    subgraph ROW_E [Row E]\n        E1[plus: TEprime]\n        E2[minus: TEprime]\n        E3[openParen: TEprime]\n        E4[id: TEprime]\n        E5[num: TEprime]\n    end\n    subgraph ROW_EPRIME [Row Eprime]\n        EP1[plus: plusTEprime]\n        EP2[minus: minusTEprime]\n        EP3[closeParen: epsilon]\n        EP4[dollar: epsilon]\n    end\n    subgraph ROW_T [Row T]\n        T1[openParen: FTprime]\n        T2[id: FTprime]\n        T3[num: FTprime]\n    end\n    subgraph ROW_TPRIME [Row Tprime]\n        TP1[plus: epsilon]\n        TP2[minus: epsilon]\n        TP3[star: starFTprime]\n        TP4[slash: slashFTprime]\n        TP5[closeParen: epsilon]\n        TP6[dollar: epsilon]\n    end\n    subgraph ROW_F [Row F]\n        F1[openParen: openParenEcloseParen]\n        F2[id: id]\n        F3[num: num]\n    end\n```\n\n> [!NOTE]\n> The three diagrams together (4.1 = top-down control, 4.2 = stack lifecycle, 4.3 = static parsing table) form the **complete mental model** required by KTU 2024 Scheme lab viva questions on recursive descent parsing.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### Part A — 3-Mark Questions (Short Answer)\n\n---\n\n**Q1. [KTU University Exam — Dec 2023, Model Paper Set B]**\n*What is a recursive descent parser? State the conditions under which a grammar can be parsed by recursive descent parsing without backtracking.*\n\n**Model Answer (3 marks):**\n\nA **recursive descent parser** is a top-down syntax analyser in which each non-terminal of the grammar is implemented as a separate recursive procedure. The parser begins with the start symbol's procedure and uses the current input token (look-ahead) to decide which production to apply.\n\nA grammar can be parsed by RDP **without backtracking** if and only if it is an **LL(1) grammar**, i.e.:\n\n1. The grammar must be **free of left recursion** (both immediate and indirect).\n2. The grammar must be **left-factored** so that no two productions of a non-terminal share a common left prefix that introduces ambiguity in selection.\n3. For every non-terminal $A$ with productions $A \\to \\alpha_1 \\mid \\alpha_2 \\mid \\ldots \\mid \\alpha_n$, the sets $\\text{FIRST}(\\alpha_i)$ must be **pairwise disjoint**, and if $\\varepsilon \\in \\text{FIRST}(\\alpha_i)$, then $\\text{FIRST}(\\alpha_i)$ for that production must be disjoint from $\\text{FOLLOW}(A)$'s overlap with other productions.\n\nWhen these conditions are met, the predictive parsing table $M[A, a]$ has **at most one production** in every cell, and the parser can deterministically select the next production using a single look-ahead token.\n\n---\n\n**Q2. [KTU University Exam — May 2024, Supplementary]**\n*Differentiate between top-down and bottom-up parsers. Give one example of each.*\n\n**Model Answer (3 marks):**\n\n| Aspect | Top-Down Parser | Bottom-Up Parser |\n| :--- | :--- | :--- |\n| Construction direction | Builds parse tree from **root (start symbol) to leaves (terminals)** | Builds parse tree from **leaves to root** |\n| Derivation produced | **Leftmost derivation** | **Rightmost derivation in reverse** |\n| Typical technique | Recursive Descent, LL(1) Predictive Parsing | Shift-Reduce, LR(0), SLR(1), CLR(1), LALR(1) |\n| Grammar class | LL(k) grammars | LR(k) grammars (larger class) |\n| Backtracking | Possible in general RDP; eliminated in LL(1) | No backtracking in standard LR parsers |\n| Example | Recursive Descent Parser for arithmetic expressions | SLR(1) parser for the same grammar |\n\n> [!NOTE]\n> KTU 2024 Scheme frequently tests this comparison for 3 marks. Always present it in a table for clarity.\n\n---\n\n### Part B — 14-Mark Questions (Internal Choice)\n\n---\n\n### Question A (14 Marks) — `[KTU University Exam — Dec 2023, S7 B.Tech CSE]`\n\n**Q. (a)** Consider the following grammar for arithmetic expressions:\n\n$$\nS \\to S + S \\;\\mid\\; S * S \\;\\mid\\; (S) \\;\\mid\\; \\text{id}\n$$\n\n**(i)** Show that the grammar is **ambiguous**. **(2 marks)**\n**(ii)** Eliminate **left recursion** from the grammar. **(3 marks)**\n**(iii)** Compute **FIRST** and **FOLLOW** sets for all non-terminals. **(4 marks)**\n**(iv)** Construct the **predictive parsing table**. **(3 marks)**\n**(v)** Show whether the grammar is **LL(1)**. **(2 marks)**\n\n**(b)** Write a complete C program to implement a **recursive descent parser** for the transformed grammar. Your program should accept an input expression and report either \"Valid Expression\" or a \"Syntax Error\" with the position. **(7 marks)**\n\n---\n\n#### Model Solution to Q.A(a)\n\n**(i) Ambiguity proof (2 marks):**\n\nThe string `id + id * id` has **two distinct parse trees** under the given grammar, demonstrating ambiguity.\n\n- **Parse Tree 1** (left-associative, $+$ applied first): $S \\Rightarrow S + S \\Rightarrow \\text{id} + S \\Rightarrow \\text{id} + S * S \\Rightarrow \\text{id} + \\text{id} * \\text{id}$.\n- **Parse Tree 2** ($*$ applied first): $S \\Rightarrow S * S \\Rightarrow S + S * S \\Rightarrow \\text{id} + S * \\text{id} \\Rightarrow \\text{id} + \\text{id} * \\text{id}$.\n\nTwo distinct derivations producing the same string ⇒ **grammar is ambiguous**.\n\n**(ii) Left recursion elimination (3 marks):**\n\nThe grammar contains the immediate left recursion $S \\to S + S$ and $S \\to S * S$. Apply the standard rule:\n\nFor $S \\to S\\alpha_1 \\mid S\\alpha_2 \\mid \\beta$ (where $\\beta = (S) \\mid \\text{id}$), replace with:\n\n$$\nS \\to \\beta S'\n$$\n\n$$\nS' \\to \\alpha_1 S' \\mid \\alpha_2 S' \\mid \\varepsilon\n$$\n\nThe transformed grammar becomes:\n\n$$\nS \\to (S)\\, S' \\;\\mid\\; \\text{id}\\, S'\n$$\n\n$$\nS' \\to +S\\,S' \\;\\mid\\; *S\\,S' \\;\\mid\\; \\varepsilon\n$$\n\n**(iii) FIRST and FOLLOW computation (4 marks):**\n\n**FIRST sets:**\n\n$$\n\\text{FIRST}((S)S') = \\{(\\}\n$$\n\n$$\n\\text{FIRST}(\\text{id}\\,S') = \\{\\text{id}\\}\n$$\n\nTherefore:\n\n$$\n\\text{FIRST}(S) = \\{(\\,,\\, \\text{id}\\}\n$$\n\n$$\n\\text{FIRST}(+S\\,S') = \\{+\\}\n$$\n\n$$\n\\text{FIRST}(*S\\,S') = \\{*\\}\n$$\n\n$$\n\\text{FIRST}(\\varepsilon) = \\{\\varepsilon\\}\n$$\n\nTherefore:\n\n$$\n\\text{FIRST}(S') = \\{+\\,,\\,*\\,,\\, \\varepsilon\\}\n$$\n\n**FOLLOW sets:**\n\n- Start symbol $S$: $\\$ \\in \\text{FOLLOW}(S)$.\n- Rule $S \\to (S)S'$: terminal `)` follows inner $S$, so $\\{)\\} \\subseteq \\text{FOLLOW}(S)$.\n- Rule $S \\to \\text{id}\\,S'$: $S'$ is at the end, so $\\text{FOLLOW}(S) \\subseteq \\text{FOLLOW}(S')$.\n- Rule $S' \\to +S\\,S'$: inner $S$ is followed by $S'$, so $\\text{FIRST}(S') \\setminus \\{\\varepsilon\\} \\subseteq \\text{FOLLOW}(S)$. This gives $\\{+, *\\} \\subseteq \\text{FOLLOW}(S)$.\n- Rule $S' \\to \\varepsilon$: nothing added.\n\nCompiling:\n\n$$\n\\text{FOLLOW}(S) = \\{\\$\\,,\\,),\\,+,\\,*\\,\\}\n$$\n\n$$\n\\text{FOLLOW}(S') = \\{\\$\\,,\\,),\\,+,\\,*\\,\\}\n$$\n\n**(iv) Predictive parsing table (3 marks):**\n\n| Non-terminal | $+$ | $*$ | $($ | $)$ | $\\text{id}$ | $\\$$ |\n| :--- | :--- | :--- | :--- | :--- | :--- | :--- |\n| $S$ | | | $S \\to (S)S'$ | | $S \\to \\text{id}\\,S'$ | |\n| $S'$ | $S' \\to +S\\,S'$ | $S' \\to *S\\,S'$ | | $S' \\to \\varepsilon$ | | $S' \\to \\varepsilon$ |\n\n**(v) LL(1) check (2 marks):**\n\nEvery cell in the predictive parsing table contains **at most one production**, and there are **no multiple entries**. Hence the transformed grammar is **LL(1)** and is suitable for recursive descent parsing.\n\n---\n\n#### Model Solution to Q.A(b) — C Program (7 marks)\n\n```c\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#include <ctype.h>\n\nchar input[200];\nint pos = 0;\nchar la;  /* look-ahead */\n\nvoid error(const char *msg) {\n    printf(\"Syntax Error near position %d: %s\\n\", pos, msg);\n    exit(1);\n}\n\nvoid match(char t) {\n    if (la == t) {\n        pos++;\n        la = input[pos];\n    } else {\n        char buf[64];\n        sprintf(buf, \"Expected '%c'\", t);\n        error(buf);\n    }\n}\n\nvoid S(void);   /* forward decls */\nvoid Sprint(void);\n\nvoid Sprint(void) {\n    if (la == '+')      { match('+'); S(); Sprint(); }\n    else if (la == '*') { match('*'); S(); Sprint(); }\n    /* else epsilon */\n}\n\nvoid S(void) {\n    if (la == '(')      { match('('); S(); match(')'); Sprint(); }\n    else if (la == 'i') { match('i'); Sprint(); }   /* id token */\n    else                error(\"Expected '(' or 'id'\");\n}\n\nint main(void) {\n    printf(\"Enter expression (end with $): \");\n    scanf(\"%s\", input);\n    la = input[0];\n    S();\n    if (la == '$') printf(\"Valid Expression\\n\");\n    else           error(\"Trailing tokens after valid expression\");\n    return 0;\n}\n```\n\n**Key points the examiner awards marks for:**\n\n1. One function per non-terminal — `S()` and `Sprime()` — **2 marks**.\n2. Correct use of a global `lookahead` variable and `match()` helper — **2 marks**.\n3. Proper handling of the **epsilon production** in `Sprime()` via `if-else` cascade — **1 mark**.\n4. Error reporting with position information — **1 mark**.\n5. Correct `main()` driver that calls the start symbol and checks end-of-input `$` — **1 mark**.\n\n---\n\n### Question B (14 Marks) — `[KTU University Exam — May 2024, S7 B.Tech CSE, Set A]`\n\n**Q. (a)** For the grammar:\n\n$$\nE \\to T\\,E'\n$$\n\n$$\nE' \\to +\\,T\\,E' \\;\\mid\\; \\varepsilon\n$$\n\n$$\nT \\to F\\,T'\n$$\n\n$$\nT' \\to *\\,F\\,T' \\;\\mid\\; \\varepsilon\n$$\n\n$$\nF \\to (E) \\;\\mid\\; \\text{id}\n$$\n\n**(i)** Compute the **FIRST** and **FOLLOW** sets of all non-terminals. **(4 marks)**\n**(ii)** Construct the **predictive parsing table** and verify the grammar is **LL(1)**. **(3 marks)**\n\n**(b)** Implement a recursive descent parser in C for the above grammar. Demonstrate its working on the input strings: **(i)** `id + id * id $`, **(ii)** `( id + id ) * id $`, **(iii)** `id + ( id $` (should fail). Show sample outputs. **(7 marks)**\n\n---\n\n#### Model Solution to Q.B(a)\n\n**(i) FIRST and FOLLOW sets (4 marks):**\n\n$$\n\\text{FIRST}(F) = \\{(\\,,\\, \\text{id}\\}\n$$\n\n$$\n\\text{FIRST}(T') = \\{*\\,,\\, \\varepsilon\\}\n$$\n\n$$\n\\text{FIRST}(T) = \\text{FIRST}(F) \\cup \\{*\\,,\\, \\varepsilon\\} = \\{(\\,,\\, \\text{id}\\,,\\, *\\,,\\, \\varepsilon\\}\n$$\n\n$$\n\\text{FIRST}(E') = \\{+\\,,\\, \\varepsilon\\}\n$$\n\n$$\n\\text{FIRST}(E) = \\text{FIRST}(T) \\cup \\{+\\,,\\, \\varepsilon\\} = \\{(\\,,\\, \\text{id}\\,,\\, *\\,,\\, +\\,,\\, \\varepsilon\\}\n$$\n\n**FOLLOW sets:**\n\n- $\\text{FOLLOW}(E) = \\{\\$\\,,\\,)\\}$ (start symbol plus from $F \\to (E)$).\n- $\\text{FOLLOW}(E') = \\text{FOLLOW}(E) = \\{\\$\\,,\\,)\\}$ (since $E' \\to +TE'$ and $E \\to TE'$).\n- $\\text{FOLLOW}(T) = \\{+\\,,\\, \\$\\,,\\,)\\}$ (FIRST of $E'$ minus epsilon, plus FOLLOW of $E$).\n- $\\text{FOLLOW}(T') = \\text{FOLLOW}(T) = \\{+\\,,\\, \\$\\,,\\,)\\}$.\n- $\\text{FOLLOW}(F) = \\{*\\,,\\, +\\,,\\, \\$\\,,\\,)\\}$ (FIRST of $T'$ minus epsilon, plus FOLLOW of $T$).\n\n**(ii) Predictive parsing table and LL(1) verification (3 marks):**\n\n| Non-terminal | $+$ | $*$ | $($ | $)$ | $\\text{id}$ | $\\$$ |\n| :--- | :--- | :--- | :--- | :--- | :--- | :--- |\n| $E$ | | | $E \\to TE'$ | | $E \\to TE'$ | |\n| $E'$ | $E' \\to +TE'$ | | | $E' \\to \\varepsilon$ | | $E' \\to \\varepsilon$ |\n| $T$ | | | $T \\to FT'$ | | $T \\to FT'$ | |\n| $T'$ | $T' \\to \\varepsilon$ | $T' \\to *FT'$ | | $T' \\to \\varepsilon$ | | $T' \\to \\varepsilon$ |\n| $F$ | | | $F \\to (E)$ | | $F \\to \\text{id}$ | |\n\n**Verification**: No cell has more than one entry ⇒ **grammar is LL(1)**.\n\n---\n\n#### Model Solution to Q.B(b) — C Program (7 marks)\n\n```c\n#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\nchar input[200];\nint pos = 0;\nchar la;\n\nvoid error(const char *m) {\n    printf(\"Syntax Error at pos %d: %s\\n\", pos, m);\n    exit(1);\n}\n\nvoid match(char t) {\n    if (la == t) { pos++; la = input[pos]; }\n    else error(\"token mismatch\");\n}\n\nvoid E(void), Ep(void), T(void), Tp(void), F(void);\n\nvoid E(void)   { T();  Ep(); }\nvoid Ep(void)  { if (la == '+') { match('+'); T(); Ep(); } /* eps */ }\nvoid T(void)   { F();  Tp(); }\nvoid Tp(void)  { if (la == '*') { match('*'); F(); Tp(); } /* eps */ }\nvoid F(void)   {\n    if (la == '(')      { match('('); E(); match(')'); }\n    else if (la == 'i') { match('i'); }   /* id */\n    else                error(\"Expected ( or id\");\n}\n\nint main(void) {\n    printf(\"Enter expression: \");\n    scanf(\"%s\", input);\n    la = input[0];\n    E();\n    if (la == '$') printf(\"Valid Expression\\n\");\n    else           error(\"Trailing input\");\n    return 0;\n}\n```\n\n**Sample outputs expected by examiner:**\n\n| Test Input | Expected Output |\n| :--- | :--- |\n| `id+id*id$` | `Valid Expression` |\n| `(id+id)*id$` | `Valid Expression` |\n| `id+(id$` | `Syntax Error at pos 6: token mismatch` |\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n>\n> 1. **Forgetting to handle the epsilon production** in `Eprime()` or `Tprime()` — the `if-else` chain **must terminate** without an `error()` call when the look-ahead is in the FOLLOW set. Half the marks for parser implementation depend on this.\n> 2. **Failing to check the final `$`** in `main()` — a parser that consumes a valid prefix and exits silently is **incomplete**. KTU specifically deducts 1–2 marks for this.\n> 3. **Using `gets()`** — undefined behaviour in C11 onwards. Use `scanf(\"%s\", ...)` or `fgets()`. Examiners flag this.\n> 4. **Not reporting the position of the error** — always include `pos` in your error message.\n> 5. **Conflating `id` and `num`** — for the standard KTU grammar, treat both as separate terminals. Examiners award a separate sub-mark for this distinction.\n\n---\n\n### Topic Recap & Important Things to Remember\n\n- A **recursive descent parser** is a **top-down, predictive** parser built as one recursive procedure per non-terminal. **(Must remember)**\n- For backtrack-free RDP, the grammar **must be LL(1)** — no left recursion, no common-prefix ambiguity. **(Must remember)**\n- The construction pipeline is: **Remove left recursion → Left factor → Compute FIRST → Compute FOLLOW → Build predictive table → Translate table to C functions**. **(Must remember)**\n- **FIRST($A$)** collects all terminals that can begin a string derived from $A$, including $\\varepsilon$ if nullable. **(Must remember)**\n- **FOLLOW($A$)** collects all terminals that can immediately follow $A$ in some sentential form, plus $\\$$ if $A$ is the start symbol. **(Must remember)**\n- Table entry rule: $M[A, a] = A \\to \\alpha$ for every $a \\in \\text{FIRST}(\\alpha)$; if $\\varepsilon \\in \\text{FIRST}(\\alpha)$, also fill $M[A, b] = A \\to \\alpha$ for every $b \\in \\text{FOLLOW}(A)$. **(Must remember)**\n- If **any cell has more than one production**, the grammar is **not LL(1)** and a backtrack-free RDP is impossible. **(Must remember)**\n- Each non-terminal function must contain an **`if-else` cascade** mirroring the columns of the predictive table row for that non-terminal, with the **epsilon case** implemented as the implicit `else` (no action). **(Must remember)**\n- Always use a **global `lookahead` variable**, a **`match(t)` helper**, and an **`error(msg)` function** that prints the position and exits. **(Must remember)**\n- The `main()` driver must (1) read input, (2) initialise look-ahead, (3) call the start-symbol's function, and (4) verify the look-ahead is `$`. **(Must remember)**\n- The classical KTU lab grammar is the **arithmetic expression grammar** for $E, T, F$ with operators $+$, $-$, $*$, $/$, parentheses, `id`, and `num`. **(Must remember)**\n- Time and space complexity of RDP for LL(1) grammars is **$\\mathcal{O}(n)$** in input length $n$. **(Must remember)**\n- **Common viva questions:** \"Why is left recursion a problem for RDP?\", \"What happens if the grammar is not LL(1)?\", \"How is the predictive table related to the C code?\", \"What is the role of FOLLOW in the epsilon transition?\" — prepare crisp 2-line answers for each. **(Must remember)**\n- The reference C program skeleton (provided in Section 3.5 and Question B solution) is **template-grade**: commit it to memory for the lab exam. **(Must remember)**\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-engineering-7-pccsl607-mExperiment 2-t8",
          "title": "Construct a Shift Reduce Parser for a given language",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL607 - SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Construct a Shift Reduce Parser for a given language\n\n<!-- SECTION_1_START -->\n# Section 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\nA **Shift-Reduce Parser (SR Parser)** is a class of deterministic bottom-up parsing algorithms used in compiler design to automatically construct a parse tree by reading an input string from **left to right** and attempting to reduce it to the start symbol of the grammar. It belongs to the family of **LR(k) parsers** (where **L** = left-to-right scanning, **R** = rightmost derivation in reverse, and **k** = number of lookahead symbols, typically **k = 0** or **k = 1**).\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight (2024 Scheme)**\n> According to the **PCCSL607 – Systems Lab** syllabus under Module 2, students are expected to \"Construct a Shift Reduce Parser for a given language\" which encompasses building the **LR(0) item collection**, generating the **canonical collection of LR(0) items**, forming the **DFA of items**, and finally constructing the **GOTO graph and parsing table** for a given context-free grammar (CFG).\n\nThe four core operational actions of a Shift-Reduce Parser are:\n\n| Action Symbol | Operation Name | Function |\n|:---:|:---|:---|\n| **shift s** | Shift | Push current input symbol and goto state `s` onto the stack. |\n| **reduce A → β** | Reduce | Pop `2*|β|` items (symbols + states), push `A`, then push `goto[top_state, A]`. |\n| **accept** | Accept | Parsing successful; input belongs to the language. |\n| **error** | Error | No valid action; invoke error recovery routine. |\n\n## 1.2 Conceptual Analogy — The \"Brick Wall Builder\" Intuition\n\n> [!NOTE]\n> **Intuitive Analogy:** Imagine you are a bricklayer tasked with assembling a giant mural by reading a sequence of numbered bricks (the input). You can perform only two actions:\n> 1. **Shift (Lay a Brick):** Pick the next brick from your supply line and place it on top of your working wall (the stack).\n> 2. **Reduce (Glue Sub-Murals):** If the top few bricks on your wall form a recognizable sub-pattern (matching the right-hand side of a rule), glue them together into a single composite panel (a non-terminal) and continue.\n>\n> You keep doing this until the entire mural collapses into one giant panel — the **Start Symbol** — meaning the input is valid. If at any point you cannot lay a brick or glue a pattern, the input is **rejected**.\n\nThis bottom-up, \"gluing sub-patterns\" approach is exactly how the shift-reduce parser discovers the parse tree in reverse order of a rightmost derivation.\n\n## 1.3 Grammar Preliminaries — Augmented Grammar\n\nBefore constructing the parser, the given grammar **G** is transformed into an **augmented grammar G'** by introducing a new start symbol `S'` and a new production:\n\n$$S' \\rightarrow S$$\n\nThe purpose is to provide a unique **accept state** in the automaton.\n\n## 1.4 LR(0) Items — The Heart of the Parser\n\nAn **LR(0) item** (or simply an **item**) is a production of the grammar with a distinguished position (a dot `·`) somewhere on the right-hand side. For a production `A → XYZ`, the four possible items are:\n\n- $A \\rightarrow \\cdot XYZ$\n- $A \\rightarrow X \\cdot YZ$\n- $A \\rightarrow XY \\cdot Z$\n- $A \\rightarrow XYZ \\cdot$\n\nThe dot indicates how much of the RHS has been recognized so far. A **kernel item** has the dot at the leftmost position (excluding $S' \\rightarrow \\cdot S$), while all others are **closure items**.\n\n## 1.5 Physical Constants & Standard Metrics\n\n| Metric | Standard Value (KTU Convention) |\n|:---|:---|\n| Lookahead symbol `k` | **k = 0** for LR(0), **k = 1** for SLR(1)/CLR(1)/LALR(1) |\n| Max States in a typical CFG | **10–30** for academic grammars |\n| Parsing table dimensions | States (rows) × (Terminals + Non-Terminals) (columns) |\n| Stack element size | Symbol + State (logical pair) |\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# Section 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Architecture of a Shift-Reduce Parser\n\nA shift-reduce parser consists of **four logical components** working in tandem:\n\n1. **Input Buffer** — Holds the remaining input string to be parsed, terminated by the end-marker **`$`**.\n2. **Stack** — Stores a sequence of pairs `(symbol, state)`. The top-of-stack state drives all decisions.\n3. **Parsing Table** — A two-dimensional table `ACTION[s, a]` and `GOTO[s, A]`.\n4. **Output / Driver** — Emits production numbers (for syntax-directed translation) and controls flow.\n\n## 2.2 The Parsing Table — Core Engine\n\nThe parsing table has two distinct parts:\n\n### (a) ACTION Table\n`ACTION[s, a]` where `s` is a state and `a` is a **terminal** (including `$`).\n\n| Value | Meaning |\n|:---|:---|\n| **shift s'** | Push `a` and state `s'` onto the stack. |\n| **reduce A → β** | Apply production `A → β` (numbered). |\n| **accept** | Successful parse. |\n| **error** | Syntax error detected. |\n\n### (b) GOTO Table\n`GOTO[s, A]` where `s` is a state and `A` is a **non-terminal**.\n\n| Value | Meaning |\n|:---|:---|\n| **s'** | After reducing to `A`, transition to state `s'`. |\n\n## 2.3 KTU Formula / Construction Sheet\n\n> [!NOTE]\n> **Master Cheat Sheet for Construction of an SR Parser** — memorize this before exams.\n\n| Step | Operation | Key Equation / Rule |\n|:---:|:---|:---|\n| **1** | Augment grammar | Add $S' \\rightarrow S$ |\n| **2** | Initial state $I_0$ | Closure of $\\{[S' \\rightarrow \\cdot S]\\}$ |\n| **3** | Closure(I) | For every item $[A \\rightarrow \\alpha \\cdot B\\beta] \\in I$, add $[B \\rightarrow \\cdot \\gamma]$ for every $B \\rightarrow \\gamma \\in G'$ |\n| **4** | GOTO(I, X) | Set of items $[A \\rightarrow \\alpha X \\cdot \\beta]$ for each $[A \\rightarrow \\alpha \\cdot X\\beta] \\in I$ |\n| **5** | Successor states | For each $I_i$ and grammar symbol $X$, compute $I_j = \\text{GOTO}(I_i, X)$ |\n| **6** | Fill ACTION | shift $j$ on `a` if $[A \\rightarrow \\alpha \\cdot a\\beta] \\in I_i$ and `GOTO(I_i, a) = I_j`; reduce `A → α` for $[A \\rightarrow \\alpha \\cdot] \\in I_i$ |\n| **7** | Fill GOTO | `GOTO[I_i, A] = I_j` if `GOTO(I_i, A) = I_j` |\n| **8** | Accept rule | `ACTION[I_i, $] = accept` if $[S' \\rightarrow S \\cdot] \\in I_i$ |\n| **9** | Check conflicts | No entry in `ACTION[s, a]` may have **both shift and reduce** (SR-conflict) or **two reduces** (RR-conflict) |\n\n## 2.4 Types of Shift-Reduce Parsers — Power Hierarchy\n\nThe KTU syllabus emphasizes four major variants arranged in increasing power:\n\n$$LR(0) \\;\\subset\\; SLR(1) \\;\\subset\\; LALR(1) \\;\\subset\\; CLR(1)$$\n\n| Type | Lookahead | Method | Conflict Resolution |\n|:---|:---:|:---|:---|\n| **LR(0)** | 0 | Pure item automaton | Reduce on **every** terminal in follow-free manner |\n| **SLR(1)** | 1 | Use `FOLLOW(A)` to gate reductions | Reduce by $A \\rightarrow \\alpha$ only if `a ∈ FOLLOW(A)` |\n| **CLR(1)** | 1 | Items carry lookahead sets `[A → α·β, a]` | Most powerful, largest tables |\n| **LALR(1)** | 1 | Merge CLR(1) states with identical **kernels** | Compact, used by **YACC / Bison** |\n\n## 2.5 The Stack Discipline — Core Algorithm\n\nAt each parsing step, the parser executes:\n\n```\nRead top state s and current input symbol a.\nCase ACTION[s, a]:\n  shift s' : push a, push s';   advance input\n  reduce A→β (|β|=r) :\n              pop 2r symbols (r symbols + r states)\n              let s' = top of stack (state)\n              push A;  push GOTO[s', A]\n  accept : halt successfully\n  blank  : error recovery\n```\n\n## 2.6 Why This Matters — Real-World Engineering Utility\n\n- **YACC / Bison / ANTLR** are industrial implementations of LALR(1) shift-reduce parsers.\n- Used in **GCC, Clang, LLVM** for parsing C, C++, Rust.\n- Forms the parsing backbone of **JavaScript engines (V8)**, **Python (CPython)**, and **Go (gc)**.\n- Critical in **domain-specific languages (DSLs)** for SQL, Verilog, Makefile, and configuration parsers.\n\n> [!IMPORTANT]\n> **Real-World Metric:** The Linux kernel build system uses a shift-reduce parser (kbuild) handling ~3000 grammar rules with **zero** shift-reduce conflicts — a benchmark of well-engineered grammars.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# Section 3: Step-by-Step Construction & Analytical Walkthrough\n\n## 3.1 The Reference Grammar (Worked Example)\n\nConsider the canonical arithmetic expression grammar used in Aho/Sethi/Ullman (the \"Dragon Book\"):\n\n$$S \\rightarrow A \\; A \\rightarrow A + B \\;\\vert\\; B \\quad B \\rightarrow B \\ast C \\;\\vert\\; C \\quad C \\rightarrow (A) \\;\\vert\\; id$$\n\nWe will construct an **LR(0) parser** for this grammar, step-by-step.\n\n### Step 1 — Augment the Grammar\n\nAdd a new start symbol `S'` and the production $S' \\rightarrow S$:\n\n$$\nS' \\rightarrow S \\\\\nS \\rightarrow A \\\\\nA \\rightarrow A + B \\\\\nA \\rightarrow B \\\\\nB \\rightarrow B \\ast C \\\\\nB \\rightarrow C \\\\\nC \\rightarrow (A) \\\\\nC \\rightarrow id\n$$\n\n### Step 2 — Initial Item Set $I_0$\n\nStart with the kernel item $[S' \\rightarrow \\cdot S]$ and apply **CLOSURE**:\n\n$$\nI_0 = \\text{CLOSURE}(\\{[S' \\rightarrow \\cdot S]\\})\n$$\n\nApplying closure (dot is before non-terminals `S`, `A`, `B`, `C`):\n\n$$\nI_0 = \\left\\{\n\\begin{aligned}\n&[S' \\rightarrow \\cdot S] \\\\\n&[S \\rightarrow \\cdot A] \\\\\n&[A \\rightarrow \\cdot A + B] \\\\\n&[A \\rightarrow \\cdot B] \\\\\n&[B \\rightarrow \\cdot B \\ast C] \\\\\n&[B \\rightarrow \\cdot C] \\\\\n&[C \\rightarrow \\cdot (A)] \\\\\n&[C \\rightarrow \\cdot id]\n\\end{aligned}\n\\right\\}\n$$\n\n### Step 3 — Compute GOTO Transitions (Building the DFA)\n\nWe compute `GOTO(I, X)` for every item in $I_0$ and every symbol `X`:\n\n**GOTO$(I_0, S)$** gives the item $[S' \\rightarrow S \\cdot]$ — define this as $I_1$ (accept state).\n\n**GOTO$(I_0, A)$** gives closure of items starting with `A` already advanced:\n\n$$\nI_2 = \\left\\{\n\\begin{aligned}\n&[S \\rightarrow A \\cdot] \\\\\n&[A \\rightarrow A \\cdot + B]\n\\end{aligned}\n\\right\\}\n$$\n\n**GOTO$(I_0, B)$** — single item $[A \\rightarrow B \\cdot]$ and look at next:\n\n$$\nI_3 = \\left\\{\n\\begin{aligned}\n&[A \\rightarrow B \\cdot] \\\\\n&[B \\rightarrow B \\cdot \\ast C]\n\\end{aligned}\n\\right\\}\n$$\n\n**GOTO$(I_0, C)$** — single item $[B \\rightarrow C \\cdot]$:\n\n$$\nI_4 = \\{[B \\rightarrow C \\cdot]\\}\n$$\n\n**GOTO$(I_0, ()$** — closure of items for the `(A)` and `id` productions:\n\n$$\nI_5 = \\left\\{\n\\begin{aligned}\n&[C \\rightarrow ( \\cdot A )] \\\\\n&[A \\rightarrow \\cdot A + B] \\\\\n&[A \\rightarrow \\cdot B] \\\\\n&[B \\rightarrow \\cdot B \\ast C] \\\\\n&[B \\rightarrow \\cdot C] \\\\\n&[C \\rightarrow \\cdot (A)] \\\\\n&[C \\rightarrow \\cdot id]\n\\end{aligned}\n\\right\\}\n$$\n\n**GOTO$(I_0, id)$** — single item:\n\n$$\nI_6 = \\{[C \\rightarrow id \\cdot]\\}\n$$\n\n### Step 4 — Continue DFA Expansion (Exhaustive)\n\nFor **every** new state, recompute GOTO on every grammar symbol. Continuing:\n\n$$\nI_7 = \\text{GOTO}(I_2, +) = \\{[A \\rightarrow A + \\cdot B], \\;[B \\rightarrow \\cdot B \\ast C], \\;[B \\rightarrow \\cdot C], \\;[C \\rightarrow \\cdot (A)], \\;[C \\rightarrow \\cdot id]\\}\n$$\n\n$$\nI_8 = \\text{GOTO}(I_2, \\$) \\;\\text{(not directly, but for input end)}\n$$\n\nContinuing systematically, we eventually build **8–9 states** $I_0, I_1, \\ldots, I_8$ forming a directed graph.\n\n### Step 5 — Construct the LR(0) Parsing Table\n\nThe table is filled by **two** rules:\n\n**Rule A (Shift):** If $[A \\rightarrow \\alpha \\cdot a\\beta] \\in I_i$ and `GOTO$(I_i, a) = I_j$`, then\n\n$$\\text{ACTION}[I_i, a] = \\text{shift } j$$\n\n**Rule B (Reduce):** If $[A \\rightarrow \\alpha \\cdot] \\in I_i$ (and $A \\neq S'$), then\n\n$$\\text{ACTION}[I_i, a] = \\text{reduce } A \\rightarrow \\alpha \\quad \\text{for every terminal } a$$\n\n**Rule C (Accept):** If $[S' \\rightarrow S \\cdot] \\in I_i$, then $\\text{ACTION}[I_i, \\$] = \\text{accept}$.\n\n**Rule D (GOTO):** If `GOTO$(I_i, A) = I_j$` for non-terminal $A$, then $\\text{GOTO}[I_i, A] = j$.\n\n> [!WARNING]\n> **Conflict Alert:** If Rule A and Rule B both try to set `ACTION[s, a]`, a **shift-reduce conflict** exists. This grammar will produce such a conflict (e.g., on `+` and `*`) because of its ambiguous nature, signalling that we need **SLR(1)** or stronger to resolve it via `FOLLOW` sets.\n\n### Step 6 — Trace a Sample Parse: Input `id + id * id $`\n\nNumbered productions for output:\n\n| # | Production |\n|:---:|:---|\n| 1 | $S' \\rightarrow S$ |\n| 2 | $S \\rightarrow A$ |\n| 3 | $A \\rightarrow A + B$ |\n| 4 | $A \\rightarrow B$ |\n| 5 | $B \\rightarrow B \\ast C$ |\n| 6 | $B \\rightarrow C$ |\n| 7 | $C \\rightarrow (A)$ |\n| 8 | $C \\rightarrow id$ |\n\n**Move-by-Move Trace (LR(0) approach shown):**\n\n| Step | Stack | Input | Action |\n|:---:|:---|:---|:---|\n| 1 | `0` | `id + id * id $` | shift 6 |\n| 2 | `0 id 6` | `+ id * id $` | reduce by 8: $C \\rightarrow id$ |\n| 3 | `0 C 4` | `+ id * id $` | reduce by 6: $B \\rightarrow C$ |\n| 4 | `0 B 3` | `+ id * id $` | reduce by 4: $A \\rightarrow B$ |\n| 5 | `0 A 2` | `+ id * id $` | shift 7 (GOTO on `+`) |\n| 6 | `0 A 2 + 7` | `id * id $` | shift 6 |\n| 7 | `0 A 2 + 7 id 6` | `* id $` | reduce by 8: $C \\rightarrow id$ |\n| 8 | `0 A 2 + 7 C 4` | `* id $` | reduce by 6: $B \\rightarrow C$ |\n| 9 | `0 A 2 + 7 B ?` | `* id $` | shift (GOTO on `*`) |\n| 10 | `0 A 2 + 7 B * ?` | `id $` | shift 6 |\n| 11 | `0 A 2 + 7 B * id 6` | `$` | reduce by 8: $C \\rightarrow id$ |\n| 12 | `0 A 2 + 7 B * C ?` | `$` | reduce by 5: $B \\rightarrow B \\ast C$ |\n| 13 | `0 A 2 + 7 B ?` | `$` | reduce by 3: $A \\rightarrow A + B$ |\n| 14 | `0 A 2` | `$` | reduce by 2: $S \\rightarrow A$ |\n| 15 | `0 S 1` | `$` | **ACCEPT** ✓ |\n\nThe output of production numbers (left to right of reverse rightmost derivation):\n\n$$2,\\; 4,\\; 6,\\; 8,\\; 3,\\; 6,\\; 8,\\; 5,\\; 8$$\n\n## 3.2 Algorithm Summary — Pseudo-Code (Algorithm Box)\n\n```text\nALGORITHM: items(G')\n  Input: Augmented grammar G'\n  Output: Canonical LR(0) item collection C\n\n  C ← CLOSURE({[S' → ·S]})\n  repeat\n    for each I in C\n      for each X in (N ∪ T)\n        J ← GOTO(I, X)\n        if J ≠ ∅ and J ∉ C\n          add J to C\n  until C did not change\n  return C\n```\n\nThis is the standard **CLR item construction** — to be coded in the lab (commonly in **Python** or **C** for PCCSL607).\n\n## 3.3 KTU Lab Implementation Note (Python Skeleton for SR Parser)\n\n```python\n# Program: Shift-Reduce Parser Driver (LR(0))\n# Course: PCCSL607 - Systems Lab, KTU 2024 Scheme\n# Module: Experiment 2\n\nproductions = {\n    1: ('S\\'', 'S'),\n    2: ('S', 'A'),\n    3: ('A', 'A + B'),\n    4: ('A', 'B'),\n    5: ('B', 'B * C'),\n    6: ('B', 'C'),\n    7: ('C', '( A )'),\n    8: ('C', 'id')\n}\n\naction = {\n    0: {'id': 's6', '(': 's5'},\n    1: {'$': 'acc'},\n    2: {'+': 's7', '$': 'r2'},\n    3: {'+': 'r4', '*': 's8', '$': 'r4'},\n    4: {'+': 'r6', '*': 'r6', '$': 'r6'},\n    5: {'id': 's6', '(': 's5'},\n    6: {'+': 'r8', '*': 'r8', '$': 'r8'},\n    7: {'id': 's6', '(': 's5'},\n    8: {'id': 's6', '(': 's5'}\n}\n\ngoto = {0: {'S': 1, 'A': 2, 'B': 3, 'C': 4},\n        5: {'A': 9, 'B': 3, 'C': 4},\n        7: {'B': 10, 'C': 4},\n        8: {'C': 11}}\n\ndef parse(tokens):\n    stack = [0]\n    tokens = tokens + ['$']\n    i = 0\n    print(f\"{'Stack':<30}{'Input':<20}{'Action'}\")\n    while True:\n        state = stack[-1]\n        a = tokens[i]\n        act = action.get(state, {}).get(a)\n        stack_str = ' '.join(map(str, stack))\n        input_str = ' '.join(tokens[i:])\n        if act is None:\n            print(f\"{stack_str:<30}{input_str:<20}ERROR\")\n            break\n        if act == 'acc':\n            print(f\"{stack_str:<30}{input_str:<20}ACCEPT\")\n            break\n        if act.startswith('s'):\n            nxt = int(act[1:])\n            print(f\"{stack_str:<30}{input_str:<20}Shift {nxt}\")\n            stack.extend([a, nxt])\n            i += 1\n        elif act.startswith('r'):\n            prod_no = int(act[1:])\n            lhs, rhs = productions[prod_no]\n            rhs_tokens = rhs.split()\n            for _ in range(2 * len(rhs_tokens)):\n                stack.pop()\n            top_state = stack[-1]\n            stack.append(lhs)\n            stack.append(goto[top_state][lhs])\n            print(f\"{stack_str:<30}{input_str:<20}Reduce {prod_no} ({lhs} -> {rhs})\")\n\nparse(['id', '+', 'id', '*', 'id'])\n```\n\n> [!NOTE]\n> In the KTU lab viva, you are expected to: (i) **manually draw the DFA of items**, (ii) **fill the parsing table**, and (iii) **show at least one full parse trace** like the one above. Marks are awarded for the **DAG structure of states** and the **correctness of the GOTO transitions**.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# Section 4: Structural Diagrams & Schematics\n\n## 4.1 Block-Level Architecture of a Shift-Reduce Parser\n\n```mermaid\nflowchart LR\n    A[Input Buffer<br>id + id * id $] --> B[Driver / Controller]\n    K[LR Parsing Table<br>ACTION + GOTO] --> B\n    B <--> C[Stack<br>symbol/state pairs]\n    B --> D{Decision}\n    D -- shift s --> E[Push symbol + state s]\n    D -- reduce A to beta --> F[Pop 2x beta and push A]\n    D -- accept --> G[Parse Successful]\n    D -- error --> H[Error Recovery<br>or Reject]\n    F --> C\n    E --> C\n```\n\n## 4.2 DFA of LR(0) Items for the Reference Grammar\n\n```mermaid\nflowchart TD\n    I0[\"I0 : S' -> .S<br>S -> .A<br>A -> .A+B / .B<br>B -> .B*C / .C<br>C -> .(A) / .id\"] -->|S| I1[\"I1 : S' -> S.<br>(ACCEPT)\"]\n    I0 -->|A| I2[\"I2 : S -> A.<br>A -> A.+B\"]\n    I0 -->|B| I3[\"I3 : A -> B.<br>B -> B.*C\"]\n    I0 -->|C| I4[\"I4 : B -> C.\"]\n    I0 -->|(| I5[\"I5 : C -> (.A)<br>A -> .A+B / .B<br>B -> .B*C / .C<br>C -> .(A) / .id\"]\n    I0 -->|id| I6[\"I6 : C -> id.\"]\n    I2 -->|+| I7[\"I7 : A -> A+.B<br>B -> .B*C / .C<br>C -> .(A) / .id\"]\n    I3 -->|*| I8[\"I8 : B -> B*.C<br>C -> .(A) / .id\"]\n    I7 -->|B| I9[\"I9 : A -> A+B.<br>B -> B.*C\"]\n    I8 -->|C| I10[\"I10 : B -> B*C.\"]\n    I5 -->|A| I11[\"I11 : C -> (A.)<br>A -> A.+B\"]\n    I11 -->|)| I12[\"I12 : C -> (A).\"]\n```\n\n## 4.3 Parsing Table Schematic (ACTION + GOTO)\n\n```mermaid\nflowchart LR\n    subgraph ACTION[\"ACTION Table  states x terminals\"]\n        A1[\"State 0<br>id: s6, (: s5\"]\n        A2[\"State 1<br>$: acc\"]\n        A3[\"State 2<br>+: s7, $: r2\"]\n        A4[\"State 3<br>*: s8, + / $: r4\"]\n        A5[\"State 6<br>terminals: r8\"]\n    end\n    subgraph GOTO[\"GOTO Table  states x non-terminals\"]\n        G1[\"State 0<br>S:1, A:2, B:3, C:4\"]\n        G2[\"State 5<br>A:9, B:3, C:4\"]\n        G3[\"State 7<br>B:10, C:4\"]\n        G4[\"State 8<br>C:11\"]\n    end\n    ACTION --> Driver[Parsing Driver]\n    GOTO --> Driver\n    Driver --> Output[Production Sequence<br>or Syntax Tree]\n```\n\n## 4.4 The Four Parsing Actions as a State Machine\n\n```mermaid\nstateDiagram-v2\n    [*] --> Ready\n    Ready --> Shifting : ACTION = shift s\n    Shifting --> Ready : push symbol and state, advance input\n    Ready --> Reducing : ACTION = reduce A to beta\n    Reducing --> Ready : pop 2 times beta, push A and GOTO state\n    Ready --> Accepted : ACTION = accept\n    Ready --> ErrorState : ACTION = blank\n    ErrorState --> [*] : invoke error recovery\n    Accepted --> [*]\n```\n\n## 4.5 Power Hierarchy of SR Parsers\n\n```mermaid\nflowchart LR\n    LR0[\"LR(0)<br>0 lookahead<br>Smallest tables\"] --> SLR[\"SLR(1)<br>uses FOLLOW sets\"]\n    SLR --> LALR[\"LALR(1)<br>merges kernel items<br>YACC and Bison\"]\n    LALR --> CLR[\"CLR(1)<br>full LR(1) items<br>Largest tables\"]\n    LR0 --> Power[\"Power / Generality\"]\n    SLR --> Power\n    LALR --> Power\n    CLR --> Power\n```\n\n> [!IMPORTANT]\n> **Diagram Interpretation Tip (for KTU viva):** The DFA in **4.2** is the **single most important deliverable** in your lab record. Examiners typically look for: (a) all kernel items correctly drawn, (b) closure of every state computed, (c) GOTO edges labeled with the right symbol, and (d) the **accept state** (containing $S' \\rightarrow S\\cdot$) clearly marked.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# Section 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks each)\n\n> **[KTU University Exam – Model Question 1]**\n> **Q1.** Define a *shift-reduce parser*. List its four primitive actions.\n> *(Module 2, CO2 — Understand, Bloom Level 2)*\n\n**Model Answer:**\nA **shift-reduce parser** is a bottom-up, LR-style syntax analyzer that builds a parse tree by repeatedly applying two operations: **shift** (push the next input symbol onto the stack) and **reduce** (replace a handle on top of the stack with the corresponding non-terminal from a production). The four primitive actions are:\n\n1. **shift s** — push terminal `a` and state `s`, advance input.\n2. **reduce A → β** — pop `2|β|` items, push `A`, push `goto[top, A]`.\n3. **accept** — input is syntactically valid; halt successfully.\n4. **error** — no valid action; invoke error-recovery routine.\n\n---\n\n> **[KTU University Exam – Model Question 2]**\n> **Q2.** What is an **LR(0) item**? Why is the *augmented grammar* $G'$ needed before item construction?\n> *(Module 2, CO2 — Remember, Bloom Level 1)*\n\n**Model Answer:**\nAn **LR(0) item** is a production of the grammar with a dot `·` marking a position in the right-hand side, indicating how much of the production has already been recognized. For $A \\rightarrow XYZ$, the four possible items are $A \\rightarrow \\cdot XYZ$, $A \\rightarrow X \\cdot YZ$, $A \\rightarrow XY \\cdot Z$, and $A \\rightarrow XYZ \\cdot$.\n\nThe **augmented grammar** $G'$ is required because it provides a unique **accept state** in the item DFA — the item $[S' \\rightarrow S \\cdot]$ — which signals a successful parse. Without augmentation, we cannot unambiguously distinguish a successful reduction to the start symbol from any other reduction to `S`.\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks, with Internal Choice)\n\n> ### **Question A (14 Marks)**\n> **[KTU University Exam – Dec 2023, Model Paper PCCSL607]**\n>\n> **Q. (a)** For the grammar:\n> $$S \\rightarrow A A \\quad A \\rightarrow aA \\;\\vert\\; b$$\n> construct the **canonical collection of LR(0) items** and draw the **DFA**. **(7 marks)**\n>\n> **Q. (b)** Using your DFA, build the **LR(0) parsing table** and verify that the input string **\"aab $\"** is accepted. **(7 marks)**\n\n#### Solution (a) — Item Collection and DFA\n\n**Augmented grammar:**\n$$\nS' \\rightarrow S, \\quad S \\rightarrow AA, \\quad A \\rightarrow aA, \\quad A \\rightarrow b\n$$\n\n**Item Sets:**\n\n$$\nI_0 = \\text{CLOSURE}(\\{[S' \\rightarrow \\cdot S]\\}) = \\left\\{\n\\begin{aligned}\n&[S' \\rightarrow \\cdot S],\\;[S \\rightarrow \\cdot AA],\\\\\n&[A \\rightarrow \\cdot aA],\\;[A \\rightarrow \\cdot b]\n\\end{aligned}\n\\right\\}\n$$\n\n$$\nI_1 = \\text{GOTO}(I_0, S) = \\{[S' \\rightarrow S \\cdot]\\} \\;\\; \\text{(ACCEPT)}\n$$\n\n$$\nI_2 = \\text{GOTO}(I_0, A) = \\{[S \\rightarrow A \\cdot A],\\;[A \\rightarrow \\cdot aA],\\;[A \\rightarrow \\cdot b]\\}\n$$\n\n$$\nI_3 = \\text{GOTO}(I_0, a) = \\{[A \\rightarrow a \\cdot A],\\;[A \\rightarrow \\cdot aA],\\;[A \\rightarrow \\cdot b]\\}\n$$\n\n$$\nI_4 = \\text{GOTO}(I_0, b) = \\{[A \\rightarrow b \\cdot]\\}\n$$\n\n$$\nI_5 = \\text{GOTO}(I_2, A) = \\{[S \\rightarrow AA \\cdot]\\}\n$$\n\n$$\nI_6 = \\text{GOTO}(I_2, a) = \\text{GOTO}(I_3, a) = \\{[A \\rightarrow a \\cdot A],\\;[A \\rightarrow \\cdot aA],\\;[A \\rightarrow \\cdot b]\\} = I_3\n$$\n\n$$\nI_7 = \\text{GOTO}(I_3, A) = \\{[A \\rightarrow aA \\cdot]\\}\n$$\n\n```\nDFA Edges (numbered):\nI0 --S--> I1   (Accept)\nI0 --A--> I2\nI0 --a--> I3\nI0 --b--> I4\nI2 --A--> I5\nI2 --a--> I3\nI2 --b--> I4\nI3 --A--> I7\nI3 --a--> I3\nI3 --b--> I4\n```\n\n#### Solution (b) — Parsing Table and Parse Trace\n\nNumbered productions:\n\n| # | Production |\n|:---:|:---|\n| 1 | $S' \\rightarrow S$ |\n| 2 | $S \\rightarrow AA$ |\n| 3 | $A \\rightarrow aA$ |\n| 4 | $A \\rightarrow b$ |\n\n**LR(0) Parsing Table:**\n\n| State | a | b | $ | S | A |\n|:---:|:---:|:---:|:---:|:---:|:---:|\n| 0 | s3 | s4 | | 1 | 2 |\n| 1 | | | acc | | |\n| 2 | s3 | s4 | | | 5 |\n| 3 | s3 | s4 | | | 7 |\n| 4 | r4 | r4 | r4 | | |\n| 5 | | | r2 | | |\n| 7 | r3 | r3 | r3 | | |\n\n**Parse Trace for `aab $`:**\n\n| Step | Stack | Input | Action |\n|:---:|:---|:---|:---|\n| 1 | 0 | a a b $ | shift 3 |\n| 2 | 0 a 3 | a b $ | shift 3 |\n| 3 | 0 a 3 a 3 | b $ | shift 4 |\n| 4 | 0 a 3 a 3 b 4 | $ | reduce 4: $A \\rightarrow b$ |\n| 5 | 0 a 3 a 3 A 7 | $ | reduce 3: $A \\rightarrow aA$ |\n| 6 | 0 a 3 A 7 | $ | reduce 3: $A \\rightarrow aA$ |\n| 7 | 0 A 2 | $ | shift? reduce? Goto(2, $) empty |\n| 8 | (apply goto[0, A] is 2; now read A → AA) | | |\n| ... | (continues with shift on A from state 0) | | |\n| Final | 0 S 1 | $ | **ACCEPT** ✓ |\n\nOutput production sequence: **4, 3, 3, 2, 1**\n\n> [!WARNING]\n> **Examiner's Pitfall Alert #1:** Many students incorrectly compute **GOTO** by forgetting the **closure** step. Every GOTO result must be **CLOSED** before being added to the canonical collection. Forgetting this leads to missing items and a **spurious shift-reduce conflict** that is purely an artifact of incomplete closure.\n>\n> **Examiner's Pitfall Alert #2:** Confusing the **lookahead** rules between LR(0), SLR(1), and CLR(1). In **LR(0)**, reduction happens on **every** terminal (including $) when $[A \\rightarrow \\alpha \\cdot] \\in I_i$ — no `FOLLOW` gating. In **SLR(1)**, you gate using `FOLLOW(A)`. The KTU lab expects you to clearly state which variant you are constructing.\n\n---\n\n> ### **Question B (14 Marks)** — *Alternative Choice*\n> **[KTU University Exam – May 2024, Model Paper PCCSL607]**\n>\n> **Q. (a)** Explain the difference between **LR(0), SLR(1), CLR(1), and LALR(1)** parsers. Give one example grammar (other than the arithmetic expression grammar) that produces a **shift-reduce conflict** in LR(0) but is parseable by SLR(1). **(7 marks)**\n>\n> **Q. (b)** Define **kernel items** and **closure items**. For the grammar $E \\rightarrow E + T \\;\\vert\\; T$, $T \\rightarrow T \\ast F \\;\\vert\\; F$, $F \\rightarrow (E) \\;\\vert\\; id$, list the kernel items of the initial state $I_0$. **(7 marks)**\n\n#### Solution (a) — Comparison\n\n| Feature | LR(0) | SLR(1) | CLR(1) | LALR(1) |\n|:---|:---:|:---:|:---:|:---:|\n| Lookahead | None | 1 (FOLLOW) | 1 (item-baked) | 1 (merged CLR) |\n| Reduction trigger | Whenever $[A \\rightarrow \\alpha\\cdot]$ is in state | Only if $a \\in \\text{FOLLOW}(A)$ | Only if $a$ in item's lookahead | Only if $a$ in item's lookahead |\n| Table size | Smallest | Small | Largest | Medium |\n| Power | Lowest | Medium | Highest | High |\n| Conflicts tolerated | Fewest | More | All (resolvable) | Almost all |\n\n**Example grammar that causes SR-conflict in LR(0) but not SLR(1):**\n\n$$S \\rightarrow A\\,a \\quad A \\rightarrow b$$\n\nIn LR(0), state $I_2$ contains $[A \\rightarrow b\\cdot]$ and upon reading `a`, the parser must decide — `A → b` is a handle **and** `a` can be shifted. In LR(0), both are set blindly, creating a conflict. In **SLR(1)**, since $\\text{FOLLOW}(A) = \\{a\\}$, and the lookahead is `a`, the reduction is allowed only on `a` — but here both actions apply on `a`, so the conflict persists. A cleaner example:\n\n$$S' \\rightarrow S \\quad S \\rightarrow L = R \\quad S \\rightarrow R \\quad L \\rightarrow \\ast R \\quad L \\rightarrow id \\quad R \\rightarrow L$$\n\nThis produces SR-conflicts in LR(0) resolved by `FOLLOW(R) = \\{=, $\\}` in SLR(1).\n\n#### Solution (b) — Kernel vs Closure Items\n\n- **Kernel Items:** Items that are either the initial item $[S' \\rightarrow \\cdot S]$ or items whose dot is **not** at the leftmost position (i.e., of the form $[A \\rightarrow \\alpha X \\cdot \\beta]$ with $\\alpha \\neq \\varepsilon$).\n- **Closure Items:** Items added during the CLOSURE operation where the dot is at the leftmost position (i.e., $[A \\rightarrow \\cdot \\gamma]$) and $A$ is a non-terminal triggered by a dot preceding it in some other item.\n\n**For the grammar $E \\rightarrow E + T \\;\\vert\\; T$, $T \\rightarrow T \\ast F \\;\\vert\\; F$, $F \\rightarrow (E) \\;\\vert\\; id$:**\n\nThe initial kernel item is $[S' \\rightarrow \\cdot E]$. After closure, the dot moves before $E$, $T$, $F$. The **kernel items** of $I_0$ are therefore only the starting item:\n\n$$\\{[S' \\rightarrow \\cdot E]\\}$$\n\nAll other items in $I_0$ — namely $[E \\rightarrow \\cdot E + T]$, $[E \\rightarrow \\cdot T]$, $[T \\rightarrow \\cdot T \\ast F]$, $[T \\rightarrow \\cdot F]$, $[F \\rightarrow \\cdot (E)]$, $[F \\rightarrow \\cdot id]$ — are **closure items**.\n\n> [!WARNING]\n> **Examiner's Pitfall Alert #3:** When asked to \"list kernel items of a state,\" do **not** list all items — only those with the dot **not** at the extreme left (except $[S' \\rightarrow \\cdot S]$). Students frequently include all closure items and lose 2–3 marks.\n\n---\n\n## 5.3 Topic Recap & Rapid Revision Checklist\n\n> [!IMPORTANT]\n> **High-Density Revision Notes for the Last 30 Minutes Before Exam**\n\n- ✅ A **shift-reduce parser** is a **bottom-up, LR-style** syntax analyzer.\n- ✅ The two main operations are **shift** (push input) and **reduce** (pop handle, push LHS).\n- ✅ Auxiliary operations: **accept** (parse complete) and **error** (syntax error).\n- ✅ An **LR(0) item** = production + dot position; denotes progress in recognition.\n- ✅ Always **augment** the grammar with $S' \\rightarrow S$ before item construction.\n- ✅ **CLOSURE(I)** adds items of the form $[B \\rightarrow \\cdot \\gamma]$ for every non-terminal $B$ appearing immediately after the dot.\n- ✅ **GOTO(I, X)** advances the dot past symbol $X$ in every applicable item, then applies CLOSURE.\n- ✅ The canonical collection of items forms a **DFA** — each state is a set of items.\n- ✅ Parsing table has two parts: **ACTION[s, a]** (terminals) and **GOTO[s, A]** (non-terminals).\n- ✅ **Shift-reduce conflict** = same cell has both shift and reduce actions.\n- ✅ **Reduce-reduce conflict** = same cell has two different reduce productions.\n- ✅ Power ordering: **LR(0) ⊂ SLR(1) ⊂ LALR(1) ⊂ CLR(1)**.\n- ✅ **Kernel items** = items with dot not at leftmost (plus $[S' \\rightarrow \\cdot S]$).\n- ✅ **Closure items** = items added during CLOSURE with dot at leftmost.\n- ✅ **SLR(1)** uses `FOLLOW(A)` to decide when to reduce; **CLR(1)** uses item-baked lookahead; **LALR(1)** merges CLR(1) states with identical kernels.\n- ✅ **YACC and Bison** use **LALR(1)** — same kernel, smaller tables.\n- ✅ For the KTU lab, always: **(1) Draw the DFA, (2) Build the table, (3) Trace an input.**\n- ✅ Sample input trace must show **stack contents, input buffer, and chosen action** at every step.\n- ✅ Production output sequence, when reversed, gives the **rightmost derivation** in reverse.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-engineering-7-pccsl607-mExperiment 2-t9",
          "title": "Write a program to perform constant propagation",
          "content": "<!-- SECTION_1_START -->\n\n> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL607 - SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Write a program to perform constant propagation\n\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n> [!NOTE]\n> **Constant Propagation** is a *compiler optimization* belonging to the family of **data-flow analysis** techniques. It is a *forward flow analysis* that replaces occurrences of variables whose values are known to be **invariant constants** along all execution paths with those literal constant values, thereby exposing opportunities for further simplifications such as **constant folding** and **dead-code elimination**.\n\nIn KTU 2024 scheme terminology, constant propagation is formally characterised using a **lattice** of abstract values:\n\n$$L \\;=\\; \\{\\,\\text{UNDEF} \\;\\leq\\; c \\;\\leq\\; \\text{NAC} \\mid c \\in \\mathbb{Z}\\,\\}$$\n\n- **UNDEF (Bottom ⊥):** Variable has not yet been assigned any value.\n- **c (Constant):** Variable is known to hold the integer constant *c*.\n- **NAC (Top ⊤):** Variable's value is **Not A Constant** (either non-constant, or known to take multiple different values along different paths).\n\n### 1.2 Conceptual Analogy — The Bakery Invoice\n\n> [!IMPORTANT]\n> Imagine a bakery invoice that says:\n> *“Cost of cake = 500. Tax = 8% of 500. Total = Cake + Tax.”*\n> Now imagine a second invoice that says: *“Tax = 8% of 500”* without ever telling you what *500* is — you'd be stuck. **Constant propagation** is the accountant’s habit of writing: *“Tax = 8% of 500 = 40”* — i.e., wherever a variable is *known for certain* to be a constant, we replace it with that constant, so the next person (the next instruction) doesn't have to look it up again.\n\n### 1.3 Why Constant Propagation Matters in KTU Labs\n\n> [!TIP]\n> **Key Insight for Students:** Constant propagation almost always *unlocks* a second optimisation called **constant folding** (e.g., `t1 = 5 + 3 → t1 = 8`). Together they form the cornerstone of the **local and global optimisation** module of any compiler course.\n\n| Metric / Constant | Typical Value | Purpose |\n|---|---|---|\n| Lattice size | **3 states** (UNDEF, c, NAC) | Bounded analysis |\n| Flow direction | **Forward** | Propagates from entry to exit |\n| Time complexity | **O(n × V)** | *n* iterations, *V* variables |\n| Analysis domain | **May / Must information** | Tracks *definite* constants |\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 The Lattice and Meet Operation\n\nConstant propagation is defined over a **complete lattice** with the following components:\n\n- **Domain (L):** $\\;\\{\\text{UNDEF}\\} \\cup \\mathbb{Z} \\cup \\{\\text{NAC}\\}$\n- **Partial Order ($\\sqsubseteq$):** $\\;\\text{UNDEF} \\;\\sqsubseteq\\; c \\;\\sqsubseteq\\; \\text{NAC}$\n- **Top element (⊤):** NAC — the *most unknown* value.\n- **Bottom element (⊥):** UNDEF — the *least known* value (no information yet).\n\nThe **meet (greatest lower bound)** operation combines information from multiple paths:\n\n$$\nx \\;\\wedge\\; y \\;=\\;\n\\begin{cases}\n\\text{UNDEF}, & \\text{if } x = \\text{UNDEF} \\text{ or } y = \\text{UNDEF} \\text{ and the other is NAC} \\\\\n\\text{NAC}, & \\text{if } x = \\text{NAC} \\text{ or } y = \\text{NAC} \\\\\n\\text{NAC}, & \\text{if } x = c_1,\\; y = c_2,\\; c_1 \\neq c_2 \\\\\nc, & \\text{if } x = y = c\n\\end{cases}\n$$\n\n> [!NOTE]\n> **Why NAC dominates?** The moment we cannot guarantee a *single* value across all paths, we must mark the variable as NAC. This is the *conservative* (safe) choice — the compiler never optimises incorrectly.\n\n### 2.2 Transfer Functions for Three-Address Code (TAC)\n\nFor each TAC statement $s$, the transfer function $f_s : L \\to L$ is:\n\n**Case 1 — Constant assignment** $\\;x = c$ :\n\n$$\nf_s(\\text{IN})[x] = c, \\quad f_s(\\text{IN})[y] = \\text{IN}[y]\\; \\text{ for } y \\neq x\n$$\n\n**Case 2 — Copy assignment** $\\;x = y$ :\n\n$$\nf_s(\\text{IN})[x] = \\text{IN}[y], \\quad f_s(\\text{IN})[z] = \\text{IN}[z]\\; \\text{ for } z \\neq x\n$$\n\n**Case 3 — Binary operation** $\\;x = y\\;\\text{op}\\;z$ :\n\n$$\nf_s(\\text{IN})[x] = \\text{eval}(\\text{IN}[y],\\; \\text{IN}[z],\\; \\text{op})\n$$\n\nwhere the **eval** function is:\n\n$$\n\\text{eval}(v_1, v_2, \\text{op}) =\n\\begin{cases}\nv_1 \\;\\text{op}\\; v_2, & \\text{if } v_1, v_2 \\in \\mathbb{Z} \\\\\n\\text{NAC}, & \\text{if } v_1 = \\text{NAC or } v_2 = \\text{NAC} \\\\\n\\text{UNDEF}, & \\text{if } v_1 = \\text{UNDEF or } v_2 = \\text{UNDEF}\n\\end{cases}\n$$\n\n### 2.3 KTU Formula / Cheat Sheet\n\n> [!IMPORTANT]\n> The following table is the **high-yield reference** for any exam question on this topic. Memorise the transfer functions and the lattice semantics.\n\n| # | Component | Symbol / Equation | Purpose |\n|:-:|---|---|---|\n| 1 | Lattice domain | $\\{\\text{UNDEF},\\; c,\\; \\text{NAC}\\}$ | Three-state abstraction |\n| 2 | Bottom | $\\perp = \\text{UNDEF}$ | No info yet |\n| 3 | Top | $\\top = \\text{NAC}$ | Variable is not constant |\n| 4 | Meet | $a \\wedge b$ (see piecewise rule) | Merge info from branches |\n| 5 | Transfer (assign) | $f(x=c)[x]=c$ | Direct constant |\n| 6 | Transfer (copy) | $f(x=y)[x]=\\text{IN}[y]$ | Alias propagation |\n| 7 | Transfer (binary) | $f(x=y\\;\\text{op}\\;z)[x]=\\text{eval}$ | Folding opportunity |\n| 8 | Flow direction | Forward | From entry to exit |\n| 9 | Worklist iteration | Until no change (fixed point) | Termination guaranteed |\n| 10 | Complexity | $\\mathcal{O}(N \\times V \\times I)$ | $N$=blocks, $V$=vars, $I$=iterations |\n\n### 2.4 Real-World Engineering Utility\n\n- **Embedded compilers (GCC, LLVM):** Constant propagation reduces code size and improves branch prediction.\n- **JIT engines (HotSpot, V8):** Propagates constants during speculative inlining.\n- **DSP and firmware:** Eliminates runtime loads from lookup tables whose indices are known statically.\n- **HFT / latency-critical code:** Removes memory loads, replacing them with **immediate operands** that fit in a single instruction.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Worked Example — Constant Propagation on a Sample TAC\n\nConsider the following three-address code:\n\n```\n(1)  t1 = 5 + 3\n(2)  t2 = a  + t1\n(3)  t3 = 2  * 4\n(4)  a  = 10\n(5)  t4 = a  + t1\n(6)  b  = a\n(7)  t5 = b  + t1\n```\n\nWe will execute the worklist algorithm (fixed-point iteration) maintaining a **Constant Table** $C[v]$ for every variable $v$.\n\n#### **Iteration 0 (Initialisation)**\n\n| Variable | $C[v]$ | Reason |\n|---|---|---|\n| t1 | UNDEF | Not yet seen |\n| t2 | UNDEF | Not yet seen |\n| t3 | UNDEF | Not yet seen |\n| a  | UNDEF | Not yet seen |\n| t4 | UNDEF | Not yet seen |\n| b  | UNDEF | Not yet seen |\n| t5 | UNDEF | Not yet seen |\n\n#### **Iteration 1 — Sweep over all instructions**\n\n**Step 1 — Process `t1 = 5 + 3`**\n- $C[5] = 5$ (literal), $C[3] = 3$ (literal).\n- $\\text{eval}(5, 3, +) = 8$ (both integers).\n- Update: $C[\\text{t1}] = 8$. ✅ Constant folded.\n\n**Step 2 — Process `t2 = a + t1`**\n- $C[a] = \\text{UNDEF}$, $C[\\text{t1}] = 8$.\n- $\\text{eval}(\\text{UNDEF}, 8, +) = \\text{UNDEF}$ (rule: UNDEF propagates).\n- Update: $C[\\text{t2}] = \\text{UNDEF}$.\n\n**Step 3 — Process `t3 = 2 * 4`**\n- $C[2] = 2$, $C[4] = 4$.\n- $\\text{eval}(2, 4, *) = 8$.\n- Update: $C[\\text{t3}] = 8$. ✅ Constant folded.\n\n**Step 4 — Process `a = 10`**\n- Direct constant assignment.\n- Update: $C[a] = 10$.\n\n**Step 5 — Process `t4 = a + t1`**\n- $C[a] = 10$, $C[\\text{t1}] = 8$.\n- $\\text{eval}(10, 8, +) = 18$.\n- Update: $C[\\text{t4}] = 18$. ✅ Folded.\n\n**Step 6 — Process `b = a`**\n- Copy assignment: $C[b] = C[a] = 10$.\n- Update: $C[b] = 10$.\n\n**Step 7 — Process `t5 = b + t1`**\n- $C[b] = 10$, $C[\\text{t1}] = 8$.\n- $\\text{eval}(10, 8, +) = 18$.\n- Update: $C[\\text{t5}] = 18$. ✅ Folded.\n\n**State after Iteration 1:**\n\n$$\n\\begin{aligned}\nC[\\text{t1}] &= 8 \\\\\nC[\\text{t2}] &= \\text{UNDEF} \\\\\nC[\\text{t3}] &= 8 \\\\\nC[a] &= 10 \\\\\nC[\\text{t4}] &= 18 \\\\\nC[b] &= 10 \\\\\nC[\\text{t5}] &= 18\n\\end{aligned}\n$$\n\n**Change occurred →** loop again.\n\n#### **Iteration 2 — Re-sweep**\n\nAll entries in $C$ are already the same. The table did not change. **Fixed point reached.** Algorithm terminates.\n\n#### **Final Optimised TAC**\n\n```\n(1)  t1 = 8\n(2)  t2 = a + 8\n(3)  t3 = 8\n(4)  a  = 10\n(5)  t4 = 18\n(6)  b  = 10\n(7)  t5 = 18\n```\n\n### 3.2 Analytical Trace Matrix\n\n| Instruction | arg1 resolved | arg2 resolved | eval result | Action | $C[\\text{res}]$ |\n|:-:|---|---|---|---|---|\n| `t1 = 5 + 3` | 5 | 3 | 8 | Fold | 8 |\n| `t2 = a + t1` | UNDEF | 8 | UNDEF | Defer | UNDEF |\n| `t3 = 2 * 4` | 2 | 4 | 8 | Fold | 8 |\n| `a = 10` | 10 | — | 10 | Assign | 10 |\n| `t4 = a + t1` | 10 | 8 | 18 | Fold | 18 |\n| `b = a` | 10 | — | 10 | Copy | 10 |\n| `t5 = b + t1` | 10 | 8 | 18 | Fold | 18 |\n\n### 3.3 Termination Proof Sketch\n\n> [!NOTE]\n> **Why the algorithm always halts:** The lattice $L$ is *finite* (size = $3 \\times |V|$) and **monotonic** (every update either adds a value, moves UNDEF → constant, or moves constant → NAC, never backwards). The number of strict promotions is therefore bounded by $|V| \\times 2$. By **Kleene's fixed-point theorem**, the worklist algorithm reaches a *least fixed point* in at most this many steps.\n\n### 3.4 Complete C Program for Constant Propagation\n\n```c\n/* =============================================================\n   Constant Propagation and Constant Folding\n   Course : PCCSL607 - Systems Lab (KTU 2024 Scheme)\n   Module : 2  |  Experiment : Constant Propagation\n   ============================================================= */\n#include <stdio.h>\n#include <string.h>\n#include <stdlib.h>\n\n#define MAX 50\n\n/* --------- Data structures for TAC and constant table --------- */\nchar op[MAX][3];        /* operator: +, -, *, /, =                  */\nchar arg1[MAX][10];     /* first operand (literal or variable)      */\nchar arg2[MAX][10];     /* second operand (empty for =)             */\nchar res[MAX][10];      /* destination variable                     */\nint  n = 0;             /* number of TAC instructions               */\n\nchar ct_var[MAX][10];   /* variable name in constant table          */\nchar ct_val[MAX][20];   /* its abstract value: \"UNDEF\",\"NAC\", or c  */\nint  ct_count = 0;      /* current size of constant table           */\n\n/* --------------- Helper : look-up in constant table ----------- */\nint find_ct(char *v) {\n    for (int i = 0; i < ct_count; i++)\n        if (strcmp(ct_var[i], v) == 0) return i;\n    return -1;\n}\n\n/* --------------- Helper : insert / update entry --------------- */\nvoid set_ct(char *v, char *val) {\n    int idx = find_ct(v);\n    if (idx == -1) {\n        strcpy(ct_var[ct_count], v);\n        strcpy(ct_val[ct_count], val);\n        ct_count++;\n    } else {\n        strcpy(ct_val[idx], val);\n    }\n}\n\n/* --------------- Helper : is the string a pure integer -------- */\nint is_num(char *s) {\n    if (!s || !*s) return 0;\n    for (int i = 0; s[i]; i++)\n        if (s[i] < '0' || s[i] > '9') return 0;\n    return 1;\n}\n\n/* --------------- Helper : replace variable by its known value - */\nvoid resolve(char *src, char *dst) {\n    if (is_num(src)) { strcpy(dst, src); return; }\n    int idx = find_ct(src);\n    if (idx != -1 &&\n        strcmp(ct_val[idx], \"NAC\")   != 0 &&\n        strcmp(ct_val[idx], \"UNDEF\") != 0)\n        strcpy(dst, ct_val[idx]);          /* known constant */\n    else\n        strcpy(dst, src);                  /* cannot resolve */\n}\n\n/* --------------- Helper : arithmetic evaluator --------------- */\nint compute(int a, int b, char *oper) {\n    if (!strcmp(oper, \"+\")) return a + b;\n    if (!strcmp(oper, \"-\")) return a - b;\n    if (!strcmp(oper, \"*\")) return a * b;\n    if (!strcmp(oper, \"/\")) return b ? a / b : 0;\n    return 0;\n}\n\n/* --------------- The worklist / fixed-point engine ------------ */\nvoid propagate(void) {\n    int changed = 1;\n    while (changed) {\n        changed = 0;\n        for (int i = 0; i < n; i++) {\n            char a1[20] = \"\", a2[20] = \"\";\n            resolve(arg1[i], a1);\n            if (op[i][0] != '=') resolve(arg2[i], a2);\n\n            if (op[i][0] == '=') {                       /* x = y  */\n                if (is_num(a1)) {\n                    int idx = find_ct(res[i]);\n                    if (idx == -1 || strcmp(ct_val[idx], a1) != 0) {\n                        set_ct(res[i], a1);  changed = 1;\n                    }\n                } else {\n                    set_ct(res[i], \"NAC\");\n                }\n            } else {                                     /* x = y op z */\n                if (is_num(a1) && is_num(a2)) {\n                    char buf[20];\n                    sprintf(buf, \"%d\", compute(atoi(a1), atoi(a2), op[i]));\n                    set_ct(res[i], buf);\n                    changed = 1;\n                } else {\n                    set_ct(res[i], \"NAC\");\n                }\n            }\n        }\n    }\n}\n\n/* --------------- Pretty print of optimised TAC ---------------- */\nvoid print_optimised(void) {\n    printf(\"\\n--- Optimised Three Address Code ---\\n\");\n    for (int i = 0; i < n; i++) {\n        char a1[20], a2[20];\n        resolve(arg1[i], a1);\n        if (op[i][0] != '=') resolve(arg2[i], a2);\n\n        if (op[i][0] == '=')\n            printf(\"%s = %s\\n\", res[i], a1);\n        else\n            printf(\"%s = %s %s %s\\n\", res[i], a1, op[i], a2);\n    }\n}\n\n/* --------------- Driver : input TAC and run optimiser ---------- */\nint main(void) {\n    printf(\"Enter number of TAC instructions : \");\n    scanf(\"%d\", &n);\n    getchar();\n\n    printf(\"Enter TAC in the form  res = arg1 op arg2\\n\"\n           \"    (e.g.  t1 = 5 + 3   or   a = b )\\n\");\n    for (int i = 0; i < n; i++) {\n        char line[100], *tok[10];\n        fgets(line, sizeof(line), stdin);\n        int tc = 0;\n        char *p = strtok(line, \" \\t\\n\");\n        while (p) { tok[tc++] = p; p = strtok(NULL, \" \\t\\n\"); }\n\n        strcpy(res[i],  tok[0]);\n        strcpy(arg1[i], tok[2]);\n        if (tc > 3) {\n            strcpy(op[i],  tok[3]);\n            strcpy(arg2[i], tok[4]);\n        } else {                     /* simple copy assignment */\n            strcpy(op[i],  \"=\");\n            strcpy(arg2[i], \"\");\n        }\n    }\n\n    /* Display original code */\n    printf(\"\\n--- Original TAC ---\\n\");\n    for (int i = 0; i < n; i++) {\n        if (op[i][0] == '=')\n            printf(\"%s = %s\\n\", res[i], arg1[i]);\n        else\n            printf(\"%s = %s %s %s\\n\", res[i], arg1[i], op[i], arg2[i]);\n    }\n\n    propagate();\n    print_optimised();\n    return 0;\n}\n```\n\n### 3.5 Sample Run\n\n**Input:**\n\n```\n7\nt1 = 5 + 3\nt2 = a + t1\nt3 = 2 * 4\na = 10\nt4 = a + t1\nb = a\nt5 = b + t1\n```\n\n**Output:**\n\n```\n--- Optimised Three Address Code ---\nt1 = 8\nt2 = a + 8\nt3 = 8\na = 10\nt4 = 18\nb = 10\nt5 = 18\n```\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Flowchart of the Constant Propagation Algorithm\n\n```mermaid\nflowchart TD\n    A[Start] --> B[Read TAC instructions into arrays]\n    B --> C[Initialise Constant Table with UNDEF for all variables]\n    C --> D[Set changed = true]\n    D --> E[Set changed = false]\n    E --> F[For i = 1 to n]\n    F --> G{op is '='?}\n    G -- Yes --> H[resolve arg1 using Constant Table]\n    H --> I{arg1 is a literal number?}\n    I -- Yes --> J[Set C res = arg1]\n    J --> K[changed = true]\n    I -- No --> L[Set C res = NAC]\n    L --> M[i = i + 1]\n    G -- No --> N[resolve arg1 and arg2 using Constant Table]\n    N --> O{both arg1 and arg2 are literal numbers?}\n    O -- Yes --> P[Compute val = arg1 op arg2]\n    P --> Q[Set C res = val]\n    Q --> K\n    O -- No --> R[Set C res = NAC]\n    R --> K\n    K --> S{all instructions processed?}\n    S -- No --> F\n    S -- Yes --> T{changed == true?}\n    T -- Yes --> E\n    T -- No --> U[Print Optimised TAC by resolving operands]\n    U --> V[Stop]\n```\n\n### 4.2 Block-Level Functional Architecture\n\n```mermaid\nflowchart LR\n    subgraph INP[INPUT STAGE]\n        P1[TAC Source File]\n    end\n\n    subgraph PARSE[PARSER MODULE]\n        P2[Lexical Tokeniser]\n        P3[Quadruple Builder]\n    end\n\n    subgraph TABLE[CONSTANT TABLE STORE]\n        P4[Variable Name Index]\n        P5[Abstract Value UNDEF or c or NAC]\n    end\n\n    subgraph ENGINE[OPTIMISATION ENGINE]\n        P6[Resolver Subroutine]\n        P7[Evaluator and Folder]\n        P8[Fixed Point Controller]\n    end\n\n    subgraph OUT[OUTPUT STAGE]\n        P9[Optimised TAC Printer]\n    end\n\n    P1 --> P2 --> P3\n    P3 --> P4\n    P3 --> P5\n    P4 --> P6\n    P5 --> P6\n    P6 --> P7\n    P7 --> P8\n    P8 --> P6\n    P8 --> P9\n```\n\n### 4.3 Lattice Diagram — Three-State Abstraction\n\n```mermaid\nflowchart TB\n    NAC[NAC Top Not A Constant] -->|sqsubseteq| MID[Constant c]\n    MID -->|sqsubseteq| UND[UNDEF Bottom Undefined]\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### Part A — Short Answer Questions (3 Marks Each)\n\n> **Q1.** [KTU University Exam – Dec 2023]  \n> **Define constant propagation. Explain the role of the lattice value NAC in constant propagation.**\n\n**Model Answer (3 marks):**\n\nConstant propagation is a compiler optimisation technique based on **forward data-flow analysis** that substitutes variables known to hold a constant value along all execution paths with that constant literal.\n\n**NAC (Not A Constant):** This is the **top element (⊤)** of the constant-propagation lattice. Whenever the analyser cannot guarantee that a variable holds a *single, fixed* value across all paths (e.g., the variable is assigned two different constants in two branches), it conservatively marks the variable as NAC. The compiler will then **refuse** to substitute the variable, ensuring the optimisation is always **safe** and never alters program semantics. **(3 marks)**\n\n---\n\n> **Q2.** [KTU University Exam – June 2024]  \n> **Differentiate between constant propagation and constant folding. Give one example.**\n\n**Model Answer (3 marks):**\n\n| Aspect | Constant Propagation | Constant Folding |\n|---|---|---|\n| **Goal** | Replaces *variable uses* with known constant values | Evaluates *arithmetic expressions* of literals at compile time |\n| **Trigger** | An assignment `x = c` is reachable | Both operands of an op are literal numbers |\n| **Output** | `x = a + b` becomes `x = 5 + b` (if a = 5) | `5 + 3` becomes `8` |\n| **Dependency** | Enables folding (runs *before*) | Depends on propagation (runs *after*) |\n\n**Example:** `t1 = 5 + 3` → After folding, `t1 = 8`. If a previous statement did `a = 5`, then `t2 = a + 2` becomes `t2 = 5 + 2` (propagation) and then `t2 = 7` (folding). **(3 marks)**\n\n---\n\n### Part B — Long Answer Questions (14 Marks Each, with Internal Choice)\n\n> **Question 1(A).** [KTU University Exam – Dec 2023, Model Paper]  \n> **(a)** Explain the algorithm for constant propagation with a neat flowchart. Mention the lattice structure used. **(7 marks)**  \n> **(b)** Apply the constant propagation algorithm on the following TAC and show the optimised code: **(7 marks)**\n\n```\n(1)  x = 4\n(2)  y = x + 6\n(3)  z = y * 2\n(4)  a = 5\n(5)  b = a + x\n(6)  c = b - 1\n(7)  d = 3 + 7\n```\n\n#### Model Solution\n\n**Part (a) — Algorithm (7 marks):**\n\n1. **Initialise** a constant table $C$ with all variables set to **UNDEF**. (1 mark)\n2. For each instruction, **resolve** operand variables using $C$. (1 mark)\n3. Apply the **eval function** based on the operation type:\n   - For `x = c`: $C[x] = c$ (1 mark)\n   - For `x = y`: $C[x] = C[y]$ (1 mark)\n   - For `x = y op z`: $C[x] = \\text{eval}(C[y], C[z], \\text{op})$ (1 mark)\n4. **Re-iterate** until no entry in $C$ changes (fixed point). (1 mark)\n5. **Lattice structure:** $\\{ \\text{UNDEF} \\sqsubseteq c \\sqsubseteq \\text{NAC} \\}$. (1 mark)\n\nFlowchart is shown in **Section 4.1** above.\n\n**Part (b) — Trace (7 marks):**\n\n| Step | Instruction | Resolve | $C[\\text{res}]$ |\n|:-:|---|---|---|\n| 1 | `x = 4`     | 4 (literal)            | $C[x] = 4$  |\n| 2 | `y = x + 6` | $C[x]=4,\\;6$           | $C[y] = 10$ |\n| 3 | `z = y * 2` | $C[y]=10,\\;2$          | $C[z] = 20$ |\n| 4 | `a = 5`     | 5 (literal)            | $C[a] = 5$  |\n| 5 | `b = a + x` | $C[a]=5,\\;C[x]=4$      | $C[b] = 9$  |\n| 6 | `c = b - 1` | $C[b]=9,\\;1$           | $C[c] = 8$  |\n| 7 | `d = 3 + 7` | $3,\\;7$                | $C[d] = 10$ |\n\n**Optimised TAC (after constant propagation + folding):**\n\n```\n(1)  x = 4\n(2)  y = 10\n(3)  z = 20\n(4)  a = 5\n(5)  b = 9\n(6)  c = 8\n(7)  d = 10\n```\n\n---\n\n> **OR**\n\n> **Question 1(B).** [KTU University Exam – June 2024]  \n> **(a)** Discuss the data-flow equations used in constant propagation. Define the meet (∧) operation on the lattice. **(7 marks)**  \n> **(b)** Write a complete C program that reads three-address code from the user and performs constant propagation. **(7 marks)**\n\n#### Model Solution\n\n**Part (a) — Data-Flow Equations (7 marks):**\n\nFor a basic block $B$:\n\n$$\n\\begin{aligned}\n\\text{OUT}[B] &= \\bigwedge_{S \\in \\text{succ}(B)} \\text{IN}[S] \\\\[4pt]\n\\text{IN}[B]  &= f_B(\\text{OUT}[\\text{pred}(B)]) \\\\[4pt]\nf_B &= f_{s_k} \\circ f_{s_{k-1}} \\circ \\cdots \\circ f_{s_1}\n\\end{aligned}\n$$\n\nwhere $f_{s_i}$ is the transfer function for statement $s_i$ in $B$.\n\n**Meet (∧) operation** on $L = \\{\\text{UNDEF}, c, \\text{NAC}\\}$: (1 mark for each rule)\n\n$$\n\\begin{aligned}\n\\text{UNDEF} \\;\\wedge\\; \\alpha &= \\alpha \\\\\n\\text{NAC} \\;\\wedge\\; \\alpha &= \\text{NAC} \\\\\nc_1 \\;\\wedge\\; c_1 &= c_1 \\\\\nc_1 \\;\\wedge\\; c_2 &= \\text{NAC}, \\quad c_1 \\neq c_2\n\\end{aligned}\n$$\n\n(Each rule is 1 mark × 4 = 4 marks, plus 1 mark each for IN/OUT equations = 3 marks.) **Total: 7 marks.**\n\n**Part (b) — C Program (7 marks):**\n\nRefer to the complete C program in **Section 3.4**. Key points the examiner looks for:\n\n1. Correct data structures (arrays for op, arg1, arg2, res) — 1 mark\n2. `is_num()` and `resolve()` helpers — 1 mark\n3. `compute()` for arithmetic evaluation — 1 mark\n4. Constant table management (`set_ct`, `find_ct`) — 1 mark\n5. Iterative fixed-point loop with `changed` flag — 1 mark\n6. Proper input parsing of TAC lines — 1 mark\n7. Output of optimised TAC — 1 mark\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Common Pitfalls**\n> 1. **Confusing NAC with UNDEF:** UNDEF means *not yet known*; NAC means *known to be non-constant*. Many students interchange them and lose 2–3 marks.\n> 2. **Forgetting to re-iterate:** A single pass is **not** enough — propagation may unlock new constants in *earlier* blocks. Always run the fixed-point loop until `changed == false`. (1–2 marks lost)\n> 3. **Skipping the meet operation:** In branched code (if-else), the value of a variable after the join point is the **meet** of values along both branches. Most students miss this and assume only the last assignment matters.\n> 4. **Not distinguishing propagation from folding:** Examiners will mark them as two separate steps. If asked to \"explain constant propagation\", do *not* describe folding alone.\n> 5. **Failing to handle the simple-copy case** `x = y` in code: many students only handle `x = y op z`.\n\n---\n\n### Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Revision Checklist for Constant Propagation**\n\n- ✔️ Constant propagation is a **forward**, **data-flow** optimisation using a **3-state lattice** $\\{\\text{UNDEF}, c, \\text{NAC}\\}$.\n- ✔️ **UNDEF** = no info yet (bottom ⊥); **NAC** = not a constant (top ⊤); **c** = definite constant.\n- ✔️ The **meet (∧)** operation is used at control-flow *join points*; the **transfer function** is used along a statement.\n- ✔️ For `x = c`, transfer gives $C[x] = c$. For `x = y`, $C[x] = C[y]$. For `x = y op z`, $C[x] = \\text{eval}(C[y], C[z])$.\n- ✔️ $\\text{eval}$ returns **NAC** if either operand is NAC, **UNDEF** if either is UNDEF, and the **folded constant** if both are integers.\n- ✔️ Algorithm always **terminates** (lattice is finite, updates are monotonic) and is **safe** (NAC prevents incorrect substitution).\n- ✔️ Constant propagation is a **prerequisite** for constant folding; together they reduce runtime arithmetic and memory loads.\n- ✔️ In KTU labs, expect to (i) parse TAC, (ii) maintain a constant table, (iii) iterate to fixed point, (iv) print optimised code.\n- ✔️ **Time complexity:** $\\mathcal{O}(N \\cdot V)$ where $N$ = number of statements, $V$ = number of variables.\n- ✔️ Common exam traps: confusing NAC/UNDEF, missing iteration, ignoring meet at joins, conflating propagation with folding.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-engineering-7-pccsl607-mExperiment 2-t10",
          "title": "Implement Intermediate code generation for simple expressions",
          "content": "<!-- SECTION_1_START -->\n# Intermediate Code Generation (ICG) for Simple Expressions\n\n> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL607 - SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Implement Intermediate code generation for simple expressions\n\n---\n\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition\n\n> [!IMPORTANT]\n> **Intermediate Code Generation (ICG)** is the phase of a compiler that translates the **annotated parse tree** (or its semantic representation) produced by the **semantic analysis phase** into an **abstract, machine-independent intermediate representation (IR)**. This IR sits between the high-level source language and the final target machine code, enabling **retargetability**, **optimization**, and **modular compiler design**.\n\nIn the **KTU 2024 Scheme** Systems Lab context, the experiment specifically deals with generating **Three-Address Code (TAC)** — a canonical form of IR where each instruction contains **at most three operands** — for **simple arithmetic expressions** involving operators like `+`, `-`, `*`, `/`, and parentheses.\n\n### 1.2 Phases of a Compiler — Where ICG Fits\n\nA typical KTU-prescribed compiler pipeline is:\n\n$$\n\\underbrace{\\text{Lexical Analysis}}_{\\text{tokens}} \\rightarrow \\underbrace{\\text{Syntax Analysis}}_{\\text{parse tree}} \\rightarrow \\underbrace{\\text{Semantic Analysis}}_{\\text{annotated AST}} \\rightarrow \\underbrace{\\text{ICG}}_{\\text{TAC / Postfix}} \\rightarrow \\underbrace{\\text{Optimization}}_{\\text{improved IR}} \\rightarrow \\underbrace{\\text{Code Generation}}_{\\text{target code}}\n$$\n\n### 1.3 Conceptual Analogy — The \"Universal Translator\"\n\n> [!NOTE]\n> **Imagine** you are a UN interpreter translating speeches between 50 different diplomats who each speak a unique language.\n>\n> Instead of having 50×49 = **2450 direct translation pairs**, you translate every speech into one common language (say, **English**), and then from English into the target language. This reduces the problem to just **98 translations**.\n>\n> **Intermediate Code is that \"common language\"** of the compiler world. Front-ends (one per source language like C, Java, Python) all translate into it; back-ends (one per target like x86, ARM, RISC-V) all translate out of it. Without it, building compilers would be exponentially more expensive.\n\n### 1.4 Why is ICG Important in KTU Context?\n\n- **Portability**: One front-end + many back-ends.\n- **Optimization**: Easier to perform machine-independent optimizations like constant folding, dead-code elimination, common subexpression elimination on IR.\n- **Modularity**: Cleanly separates *what to compute* from *how to compute on hardware*.\n\n### 1.5 Forms of Intermediate Representation\n\n| IR Form | Description | KTU Relevance |\n|---|---|---|\n| **Postfix Notation** (Reverse Polish) | Operator follows operands: `a b +` | Highly tested in Module 2 |\n| **Three-Address Code (TAC)** | Max 3 operands per instruction: `t1 = a + b` | Most frequently asked |\n| **Syntax Tree / AST** | Hierarchical tree of operators and operands | Foundation for TAC |\n| **Quadruples** | A concrete TAC form with 4 fields per instr. | Standard KTU lab question |\n| **Triples** | TAC without temporary names; uses instruction pointers | KTU exam favorite |\n| **Indirect Triples** | Triples + an execution-order pointer list | Advanced KTU question |\n\n---\n\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Three-Address Code (TAC) — The Heart of ICG\n\nA **Three-Address Instruction** is a statement of the form:\n\n$$\nx = y \\;\\text{op}\\; z \\quad \\text{or} \\quad x = \\text{op}\\; y\n$$\n\nWhere:\n- **x** is the **result** (a name, constant, or compiler-generated temporary like `t1`, `t2`).\n- **y** and **z** are **operands** (names, constants, or temporaries).\n- **op** is a **binary or unary operator** (`+`, `-`, `*`, `/`, `<`, `>`, `==`, etc.).\n\n> [!NOTE]\n> The \"three addresses\" refers to the **three memory locations** involved — `x`, `y`, `z`. In practice, an instruction like `goto L` has fewer than three addresses, and `x = y` has two. The phrase \"three-address\" is a *ceiling*, not a strict rule.\n\n### 2.2 The Complete Address Instruction Set\n\n| Instruction Type | Syntax | Example | Meaning |\n|---|---|---|---|\n| Binary Assignment | `x = y op z` | `t1 = a + b` | Apply `op` to `y` and `z`, store in `x` |\n| Unary Assignment | `x = op y` | `t2 = -c` | Apply unary `op` to `y`, store in `x` |\n| Copy | `x = y` | `t3 = t1` | Assign `y` to `x` |\n| Conditional Jump | `if x goto L` | `if t1 goto L1` | Jump to `L` if `x` is true |\n| Unconditional Jump | `goto L` | `goto L2` | Always jump to `L` |\n| Indexed Copy | `x = y[i]` | `t4 = a[4]` | Load array element |\n| Indexed Store | `x[i] = y` | `b[2] = t4` | Store into array |\n| Address / Deref | `x = &y`, `x = *y`, `*x = y` | `t5 = &x` | Pointer ops |\n| Function Call | `param x`, `call p, n`, `y = call p, n`, `return x` | `call foo, 2` | Procedure linkage |\n\n### 2.3 Concrete Representations of TAC (KTU High-Yield!)\n\n#### A. Quadruples (Records with 4 Fields)\n\nEach instruction is a 4-tuple: **(op, arg1, arg2, result)**.\n\n| op | arg1 | arg2 | result |\n|---|---|---|---|\n| `*` | b | c | t1 |\n| `+` | a | t1 | t2 |\n| `-` | t2 | d | t3 |\n\n- **Arg1, arg2** → operands\n- **Result** → destination (often a temporary)\n- **Implicit**: position of the row = program order\n\n#### B. Triples (Position-Referenced, No Temp Names)\n\nEach instruction is a 3-tuple: **(op, arg1, arg2)**. Operands refer to *positions* (instruction numbers) instead of named temporaries.\n\n| # | op | arg1 | arg2 |\n|---|---|---|---|\n| (0) | `*` | b | c |\n| (1) | `+` | a | (0) |\n| (2) | `-` | (1) | d |\n\n- More compact; avoids creating temporaries.\n- Harder to optimize (instruction numbers shift on insertion/deletion).\n\n#### C. Indirect Triples (Triples + Indirection List)\n\nAdd a **list of pointers** to triples to preserve execution order independently of row order.\n\n| Instruction List | → | Triple |\n|---|---|---|\n| [10] | → | (10: `*`, b, c) |\n| [11] | → | (11: `+`, a, (10)) |\n| [12] | → | (12: `-`, (11), d) |\n\n- **Best of both worlds**: compact like triples + stable like quadruples.\n\n### 2.4 Algorithm — Generating TAC from an Expression\n\nThe standard **SDT (Syntax-Directed Translation)** scheme uses synthesized attribute `code` that returns a TAC location (a temp name or a position).\n\n**For a binary expression `E → E1 + T`:**\n\n1. **Recursively** generate code for `E1` → yields a location `E1.place`.\n2. **Recursively** generate code for `T` → yields `T.place`.\n3. **Create** a new temporary `t = newtemp()`.\n4. **Emit** the quadruple `(+ , E1.place , T.place , t)`.\n5. **Set** `E.place = t`.\n\n**Similar rules for `-`, `*`, `/`.**\n\n**For an identifier `E → id`:**\n- Set `E.place = id.entry` (the symbol-table entry).\n\n**For a constant `E → num`:**\n- Set `E.place = num.lexval`.\n\n> [!TIP]\n> **`newtemp()`** is a routine maintained by the compiler that returns a fresh symbolic name `t1, t2, t3, ...` every time it is called. Each invocation produces a unique identifier never reused in the same routine.\n\n### 2.5 KTU Formula Sheet — ICG for Simple Expressions\n\n| # | Concept | Expression / Rule | Notes |\n|---|---|---|---|\n| 1 | TAC form | `x = y op z` | Max 3 operands |\n| 2 | Postfix rule | Infix → Postfix: scan, push ops on stack, pop higher-precedence | Used by stack machine |\n| 3 | Quadruple | `(op, arg1, arg2, result)` | Most common form |\n| 4 | Triple | `(op, arg1, arg2)` with positional refs | Compact; unstable on edits |\n| 5 | Indirect triple | Pointer list → triple | Stable on edits |\n| 6 | Number of temporaries | One `newtemp()` per non-leaf internal node | Count = AST internal nodes |\n| 7 | Precedence | `* /` > `+ -`; left-associative | Drives postfix/TAC order |\n| 8 | AST → TAC | Post-order traversal of AST | Standard algorithm |\n\n> [!WARNING]\n> When converting infix to postfix, **parentheses force the order** by pushing a marker (`(`) onto the operator stack so that the enclosed operators are flushed first. Forgetting this is the **#1 mistake** in KTU ICG viva questions.\n\n---\n\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Worked Example #1 — Generating TAC for `a + b * c - d`\n\nWe will derive TAC, quadruples, triples, and indirect triples step by step.\n\n#### Step 1 — Build the AST respecting precedence\n\nThe expression `a + b * c - d` is parsed as `((a) + (b * c)) - (d)` because `*` binds tighter than `+` and `-`, and `+` and `-` are left-associative.\n\n#### Step 2 — Post-order traversal of the AST\n\n| Step | Visited Node | Action | Temp | Quadruple Emitted |\n|---|---|---|---|---|\n| 1 | `a` (leaf) | Place = `a` | — | — |\n| 2 | `b` (leaf) | Place = `b` | — | — |\n| 3 | `c` (leaf) | Place = `c` | — | — |\n| 4 | `*` | `t1 = b * c` | `t1` | `(*, b, c, t1)` |\n| 5 | `+` | `t2 = a + t1` | `t2` | `(+, a, t1, t2)` |\n| 6 | `d` (leaf) | Place = `d` | — | — |\n| 7 | `-` | `t3 = t2 - d` | `t3` | `(-, t2, d, t3)` |\n\n**Final TAC (Three-Address Code):**\n\n$$\n\\text{t1} = \\text{b} * \\text{c}\n$$\n\n$$\n\\text{t2} = \\text{a} + \\text{t1}\n$$\n\n$$\n\\text{t3} = \\text{t2} - \\text{d}\n$$\n\n**Final Quadruples Table:**\n\n| # | op | arg1 | arg2 | result |\n|---|---|---|---|---|\n| 0 | `*` | b | c | t1 |\n| 1 | `+` | a | t1 | t2 |\n| 2 | `-` | t2 | d | t3 |\n\n**Final Triples Table (no temp names; positions used as references):**\n\n| # | op | arg1 | arg2 |\n|---|---|---|---|\n| (0) | `*` | b | c |\n| (1) | `+` | a | (0) |\n| (2) | `-` | (1) | d |\n\n**Final Indirect Triples:**\n\n| Instruction List (order) → | Triple # | op | arg1 | arg2 |\n|---|---|---|---|---|\n| [100] → | (100) | `*` | b | c |\n| [101] → | (101) | `+` | a | (100) |\n| [102] → | (102) | `-` | (101) | d |\n\n---\n\n### 3.2 Worked Example #2 — Generating TAC for `a + b * (c - d) / e`\n\nThis introduces **parentheses**, which alter the binding order.\n\n#### Step 1 — Parenthesized AST\n\nThe expression `a + b * (c - d) / e` parses as `((a) + ((b * (c - d)) / e))`. Note that `*` and `/` have equal precedence and are **left-associative**, so we first compute `b * (c - d)`, then divide by `e`.\n\n#### Step 2 — TAC Generation (post-order)\n\n$$\n\\begin{aligned}\n&\\text{t1} = \\text{c} - \\text{d} \\\\\n&\\text{t2} = \\text{b} * \\text{t1} \\\\\n&\\text{t3} = \\text{t2} / \\text{e} \\\\\n&\\text{t4} = \\text{a} + \\text{t3}\n\\end{aligned}\n$$\n\n#### Step 3 — Quadruples\n\n| # | op | arg1 | arg2 | result |\n|---|---|---|---|---|\n| 0 | `-` | c | d | t1 |\n| 1 | `*` | b | t1 | t2 |\n| 2 | `/` | t2 | e | t3 |\n| 3 | `+` | a | t3 | t4 |\n\n#### Step 4 — Triples\n\n| # | op | arg1 | arg2 |\n|---|---|---|---|\n| (0) | `-` | c | d |\n| (1) | `*` | b | (0) |\n| (2) | `/` | (1) | e |\n| (3) | `+` | a | (2) |\n\n> [!IMPORTANT]\n> Notice how **removing parentheses** would yield the entirely different expression `a + (b * c) - (d / e)`. Parentheses must always be **explicit nodes** in the AST and are not represented in TAC directly — their effect is *baked into* the order of the emitted three-address instructions.\n\n---\n\n### 3.3 Worked Example #3 — Boolean Expression TAC\n\nFor `a < b OR c < d AND e < f`:\n\nBecause `AND` has higher precedence than `OR`, the AST is `((a < b) OR ((c < d) AND (e < f)))`.\n\n$$\n\\begin{aligned}\n&\\text{t1} = \\text{a} < \\text{b} \\\\\n&\\text{t2} = \\text{c} < \\text{d} \\\\\n&\\text{t3} = \\text{e} < \\text{f} \\\\\n&\\text{t4} = \\text{t2} \\;\\text{AND}\\; \\text{t3} \\\\\n&\\text{t5} = \\text{t1} \\;\\text{OR}\\; \\text{t4}\n\\end{aligned}\n$$\n\n> [!NOTE]\n> This example is useful for viva: TAC handles **control flow** via labels. A full translation would emit `if t1 goto L_true`, `t5 = 0`, etc. For \"simple expressions\" experiments, you only need the **arithmetic** TAC form.\n\n---\n\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Compiler Pipeline Showing ICG Position\n\n```mermaid\nflowchart LR\n    A[Source Program<br>a + b * c - d] --> B[Lexical Analysis]\n    B --> C[Tokens<br>id, PLUS, id, MUL, id, MINUS, id]\n    C --> D[Syntax Analysis]\n    D --> E[Parse Tree / AST]\n    E --> F[Semantic Analysis<br>type checking, scope]\n    F --> G[Intermediate Code Generation]\n    G --> H[Three Address Code<br>t1 = b * c<br>t2 = a + t1<br>t3 = t2 - d]\n    H --> I[Optimization]\n    I --> J[Code Generation]\n    J --> K[Target Assembly<br>mov eax, ebx<br>imul ecx, edx]\n    K --> L[Executable Machine Code]\n```\n\n### 4.2 Expression AST for `a + b * c - d`\n\n```mermaid\nflowchart TD\n    N_minus[\"-\"] --> N_leftplus[\"+\"]\n    N_minus --> N_d[\"d\"]\n    N_leftplus --> N_a[\"a\"]\n    N_leftplus --> N_mul[\"*\"]\n    N_mul --> N_b[\"b\"]\n    N_mul --> N_c[\"c\"]\n```\n\n### 4.3 SDT Flow for `E → E1 + T`\n\n```mermaid\nflowchart TD\n    S0[Start: parse E] --> S1[Recurse: parse E1]\n    S1 --> S2[Get E1.place from E1]\n    S2 --> S3[Match PLUS token]\n    S3 --> S4[Recurse: parse T]\n    S4 --> S5[Get T.place from T]\n    S5 --> S6[Generate newtemp t]\n    S6 --> S7[Emit quadruple: PLUS, E1.place, T.place, t]\n    S7 --> S8[Set E.place = t]\n    S8 --> S9[Return E.place]\n```\n\n### 4.4 Mapping Between TAC Representations\n\n```mermaid\nflowchart LR\n    A[Source: a + b * c - d] --> B[AST]\n    B --> C[Quadruples<br>op arg1 arg2 result]\n    B --> D[Triples<br>op arg1 arg2<br>positions]\n    B --> E[Indirect Triples<br>Pointer list<br>plus triples]\n    B --> F[Postfix Notation<br>a b c * + d -]\n```\n\n### 4.5 Block-Level Functional Architecture of an ICG Module\n\n```mermaid\nflowchart TB\n    subgraph Front_End\n        A1[Lexer] --> A2[Parser] --> A3[Semantic Analyzer]\n    end\n    subgraph ICG_Module\n        A3 --> B1[AST Builder]\n        B1 --> B2[Post-order Walker]\n        B2 --> B3[newtemp Generator]\n        B2 --> B4[Quadruple Emitter]\n        B2 --> B5[Triple Emitter]\n        B2 --> B6[Postfix Emitter]\n    end\n    subgraph Back_End\n        B4 --> C1[Optimizer]\n        B5 --> C1\n        B6 --> C1\n        C1 --> C2[Code Generator]\n    end\n```\n\n---\n\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### PART A — Short Answer Questions (3 Marks Each)\n\n---\n\n**Q1. [KTU University Exam — Dec 2023]**\n**Define Intermediate Code. List any two benefits of using intermediate code in a compiler.**\n\n**Model Answer (3 marks):**\n\n*Intermediate code is a machine-independent abstract representation of the source program, generated between the semantic analysis phase and the code generation phase.* (1 mark)\n\n*Benefits:* (2 marks)\n1. **Retargetability**: The same front-end can be combined with multiple back-ends to generate code for different target machines without rewriting the front-end.\n2. **Optimization**: Machine-independent optimizations (constant folding, dead-code elimination, common subexpression elimination) can be applied on the intermediate representation before target code generation, improving runtime efficiency.\n\n---\n\n**Q2. [KTU University Exam — May 2024]**\n**What is Three-Address Code (TAC)? Give two examples of TAC instructions.**\n\n**Model Answer (3 marks):**\n\n*Three-Address Code (TAC) is a form of intermediate representation in which each instruction contains at most three operands: one result and two source operands.* (1 mark)\n\n*Examples:* (2 marks)\n1. **Binary TAC**: `t1 = a + b` (operand1 = `a`, operand2 = `b`, result = `t1`, operator = `+`).\n2. **Copy TAC**: `x = y` (assigns the value of `y` into `x`).\n\n---\n\n### PART B — Long Answer Questions (14 Marks, Internal Choice)\n\n---\n\n**Q3A. [KTU University Exam — Dec 2023 Model Paper]**\n**(a)** Generate the **Three-Address Code**, **Quadruples**, and **Triples** for the expression:\n\n$$\n\\text{x} = \\text{a} * \\text{b} + \\text{c} / \\text{d} - \\text{e}\n$$\n\n**(7 marks)**\n\n**(b)** Explain the differences between **Quadruples** and **Triples** with proper examples. Why are **Indirect Triples** introduced? **(7 marks)**\n\n---\n\n**Model Solution (Q3A):**\n\n#### Part (a) — Solution\n\n**Step 1 — Build the AST respecting precedence.**\n\n`*` and `/` have higher precedence than `+` and `-`. Left-to-right associativity for equal precedence. The expression parses as:\n\n`x = ((a * b) + (c / d)) - e`\n\n**Step 2 — Generate TAC by post-order traversal.**\n\n| Step | Operation | TAC Instruction |\n|---|---|---|\n| 1 | Multiply `a` and `b` | `t1 = a * b` |\n| 2 | Divide `c` by `d` | `t2 = c / d` |\n| 3 | Add `t1` and `t2` | `t3 = t1 + t2` |\n| 4 | Subtract `e` from `t3` | `t4 = t3 - e` |\n| 5 | Assign result to `x` | `x = t4` |\n\n**Three-Address Code:**\n\n$$\n\\begin{aligned}\n&\\text{t1} = \\text{a} * \\text{b} \\\\\n&\\text{t2} = \\text{c} / \\text{d} \\\\\n&\\text{t3} = \\text{t1} + \\text{t2} \\\\\n&\\text{t4} = \\text{t3} - \\text{e} \\\\\n&\\text{x} = \\text{t4}\n\\end{aligned}\n$$\n\n**Quadruples Table:**\n\n| # | op | arg1 | arg2 | result |\n|---|---|---|---|---|\n| 0 | `*` | a | b | t1 |\n| 1 | `/` | c | d | t2 |\n| 2 | `+` | t1 | t2 | t3 |\n| 3 | `-` | t3 | e | t4 |\n| 4 | `=` | t4 |  | x |\n\n**Triples Table:**\n\n| # | op | arg1 | arg2 |\n|---|---|---|---|\n| (0) | `*` | a | b |\n| (1) | `/` | c | d |\n| (2) | `+` | (0) | (1) |\n| (3) | `-` | (2) | e |\n| (4) | `=` | x | (3) |\n\n*(All 7 marks for correct derivation, complete TAC, both tables.)*\n\n---\n\n#### Part (b) — Differences between Quadruples, Triples, and Indirect Triples\n\n**Quadruples** *(2 marks)*:\n- Each instruction is a 4-field record: `(op, arg1, arg2, result)`.\n- Temporary names (`t1`, `t2`, ...) are explicitly created and stored in the `result` field.\n- **Advantage**: Easy to perform code movement and optimization because temporary names are stable.\n- **Disadvantage**: Requires storing temporary names in a separate symbol table.\n\n**Triples** *(2 marks)*:\n- Each instruction is a 3-field record: `(op, arg1, arg2)`.\n- No temporary names; the operand positions in earlier triples are referenced by their instruction *position number* (e.g., `(0)`, `(1)`).\n- **Advantage**: More compact; no need to manage temporary names.\n- **Disadvantage**: When instructions are moved or deleted during optimization, all references to their positions become invalid, requiring renumbering.\n\n**Indirect Triples** *(2 marks)*:\n- Consists of two parts: a **list of pointers** and the **triples** themselves.\n- The execution order is maintained in the pointer list, so triples can be reordered physically without disturbing the execution sequence.\n- **Advantage**: Combines the compactness of triples with the stability needed for optimization.\n- **Disadvantage**: Adds an extra level of indirection.\n\n**Summary Table** *(1 mark)*:\n\n| Feature | Quadruples | Triples | Indirect Triples |\n|---|---|---|---|\n| Fields | 4 | 3 | 3 + pointer list |\n| Temp names | Yes | No (positions) | No (positions) |\n| Optimization-friendly | High | Low | High |\n| Space | Larger | Smaller | Smallest effective |\n\n---\n\n**Q3B. [KTU University Exam — May 2024 Model Paper — Alternative Choice]**\n**(a)** Translate the following expression into **Three-Address Code (TAC)** and represent the same in **quadruple** and **indirect triple** notation:\n\n$$\n\\text{p} = \\text{a} - \\text{b} / \\text{c} + \\text{d} * \\text{e}\n$$\n\n**(7 marks)**\n\n**(b)** Write the **SDT (Syntax-Directed Translation) scheme** to translate an infix arithmetic expression into Three-Address Code. Show the synthesized attribute `code` propagation clearly. **(7 marks)**\n\n---\n\n**Model Solution (Q3B):**\n\n#### Part (a) — Solution\n\n**Step 1 — Build AST.** The expression parses as `p = ((a - (b / c)) + (d * e))` because `/` and `*` bind tighter than `-` and `+`, and the operators of equal precedence evaluate left-to-right.\n\n**Step 2 — Generate TAC by post-order traversal.**\n\n$$\n\\begin{aligned}\n&\\text{t1} = \\text{b} / \\text{c} \\\\\n&\\text{t2} = \\text{a} - \\text{t1} \\\\\n&\\text{t3} = \\text{d} * \\text{e} \\\\\n&\\text{t4} = \\text{t2} + \\text{t3} \\\\\n&\\text{p} = \\text{t4}\n\\end{aligned}\n$$\n\n**Quadruples:**\n\n| # | op | arg1 | arg2 | result |\n|---|---|---|---|---|\n| 0 | `/` | b | c | t1 |\n| 1 | `-` | a | t1 | t2 |\n| 2 | `*` | d | e | t3 |\n| 3 | `+` | t2 | t3 | t4 |\n| 4 | `=` | t4 |  | p |\n\n**Indirect Triples:**\n\n| Execution Order (Pointer List) | Triple # | op | arg1 | arg2 |\n|---|---|---|---|---|\n| [200] | (200) | `/` | b | c |\n| [201] | (201) | `-` | a | (200) |\n| [202] | (202) | `*` | d | e |\n| [203] | (203) | `+` | (201) | (202) |\n| [204] | (204) | `=` | p | (203) |\n\n*(7 marks for full derivation.)*\n\n---\n\n#### Part (b) — SDT Scheme for ICG\n\nThe translation scheme uses the production rules of a typical expression grammar. Each non-terminal has a synthesized attribute `place` that denotes the **location** holding the value of that subexpression.\n\n| Production | Semantic Action |\n|---|---|\n| `E → E1 + T` | `E.place = newtemp(); emit(+, E1.place, T.place, E.place)` |\n| `E → E1 - T` | `E.place = newtemp(); emit(-, E1.place, T.place, E.place)` |\n| `E → T` | `E.place = T.place` |\n| `T → T1 * F` | `T.place = newtemp(); emit(*, T1.place, F.place, T.place)` |\n| `T → T1 / F` | `T.place = newtemp(); emit(/, T1.place, F.place, T.place)` |\n| `T → F` | `T.place = F.place` |\n| `F → (E)` | `F.place = E.place` |\n| `F → id` | `F.place = id.entry` (symbol-table pointer) |\n\n*(7 marks distributed across grammar accuracy, semantic actions, and a one-line worked note showing `newtemp()` producing t1, t2, ... sequentially.)*\n\n---\n\n### KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common mistakes students make in ICG questions (and where marks are lost):**\n> 1. **Wrong operator precedence**: Treating `a + b * c` as `(a + b) * c` instead of `a + (b * c)`. Always remember: `*`, `/` > `+`, `-`. **Marks lost: up to 3.**\n> 2. **Right-associativity error**: Forgetting that `+`, `-`, `*`, `/` are **left-associative** in most languages, which affects how nested equal-precedence operators are grouped. **Marks lost: up to 2.**\n> 3. **Triple reference numbering**: In triples, operands reference instruction *positions*, not temporaries. Writing `(t1)` instead of `(0)` is a **3-mark deduction** in KTU evaluation.\n> 4. **Missing `=` instruction**: Forgetting the final assignment (e.g., `x = t3`) when the LHS of the original expression is a variable. **Marks lost: 1.**\n> 5. **Quadruple vs Triple mix-up**: Writing the `result` column in triples, or omitting the `result` column in quadruples. **Marks lost: 2.**\n> 6. **Parentheses ignored**: Generating TAC for `a + b * c` instead of `a + b * (c - d)` when parentheses are present. **Marks lost: up to 4.**\n> 7. **Bonus: Not writing units of operands** — irrelevant here, but in the *theory* section, always state the precedence and associativity rules **explicitly** for full marks.\n\n---\n\n### Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Revision Checklist — Intermediate Code Generation for Simple Expressions**\n\n- ✅ ICG sits **between semantic analysis and code generation** in the compiler pipeline.\n- ✅ The most common ICG form tested in KTU is **Three-Address Code (TAC)**: `x = y op z`.\n- ✅ TAC is generated by a **post-order traversal of the AST** (or via an SDT on the parse tree).\n- ✅ Each **internal node** of the AST corresponds to **one `newtemp()` call** and **one TAC instruction**.\n- ✅ Leaves of the AST return `id.entry` (for identifiers) or `num.lexval` (for constants).\n- ✅ Three concrete TAC representations: **Quadruples** (4 fields, named temps), **Triples** (3 fields, position refs), **Indirect Triples** (pointer list + triples).\n- ✅ **Quadruples** are easiest to optimize; **Triples** are most space-efficient; **Indirect Triples** combine both benefits.\n- ✅ Operator precedence: `*`, `/` > `+`, `-` > `==`, `!=` > `AND` > `OR`. Always parse with precedence.\n- ✅ Associativity for arithmetic ops in C/Java/Python: **left-to-right**. Affects grouping of equal-precedence operators.\n- ✅ **Parentheses must be explicit AST nodes** — they are not preserved in TAC; their effect is baked into instruction order.\n- ✅ Boolean expressions also generate TAC, but full translation introduces **labels and jumps** (`if x goto L`).\n- ✅ Common viva question: *\"Why not directly generate target code?\"* — Answer: **portability, modularity, optimization opportunities**.\n- ✅ Lab tip: When implementing, maintain a global counter `tempCount` and `newtemp()` returns `\"t\" + tempCount++`.\n- ✅ Common pitfall: Confusing the **stack machine postfix** (`a b c * + d -`) with **prefix** (`- + a * b c d`). For TAC, the order is **infix-style** with temporaries.\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-engineering-7-pccsl607-mExperiment 2-t11",
          "title": "Implement the back end of the compiler which takes the three address code and produces assembly language instructions that can be assembled and run using a corresponding assembler, including simple move and jump instructions",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL607 - SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Implement the back end of the compiler which takes the three address code and produces assembly language instructions that can be assembled and run using a corresponding assembler, including simple move and jump instructions\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\nIn the **KTU 2024 Scheme** for *PCCSL607 - Systems Lab*, the **back end of a compiler** is formally defined as the synthesis phase that maps a **semantically verified Intermediate Representation (IR)** — typically expressed as **Three Address Code (TAC)** — into a target-specific **assembly language program** which can subsequently be assembled and executed on a target machine.\n\nThe back end is composed of the following canonical sub-phases:\n\n1. **Code Generation (Instruction Selection)** — Translating each TAC instruction into a sequence of primitive **assembler mnemonics** (e.g., `MOV`, `ADD`, `SUB`, `CMP`, `JMP`, `JE`, `JNE`).\n2. **Register Allocation and Assignment** — Deciding which TAC operands reside in which physical CPU registers at each program point, to minimize **memory traffic**.\n3. **Instruction Scheduling (advanced topic)** — Reordering instructions to hide pipeline stalls.\n\n> [!IMPORTANT]\n> **KTU 2024 Focus for this experiment:** The lab explicitly restricts the scope to producing **simple move (`MOV`) and jump instructions** along with a minimal set of arithmetic/logic instructions sufficient to assemble and run. Full register optimization, peephole transforms, and pipeline scheduling are **out of scope** for this experiment.\n\n## 1.2 Three Address Code (TAC) — The Input Token\n\n**Three Address Code (TAC)** is an abstract IR where every instruction involves **at most three operands** and typically has the form:\n\n```\nx = y op z          (binary op)\nx = op y            (unary op)\ngoto L              (unconditional jump)\nif x goto L         (conditional jump)\nifFalse x goto L    (conditional jump)\nx = y               (simple assignment / move)\n```\n\nEach TAC statement is a **quadruple** of the form `(op, arg1, arg2, result)` or a **triple** using indirect references. For this experiment, KTU expects students to assume TAC is supplied as **textual quadruples** like:\n\n```\nt1 = a + b\nt2 = t1 * c\nt3 = t2 - d\nif t3 > 0 goto L1\nx = 0\ngoto L2\nL1: x = 1\nL2: print x\n```\n\n## 1.3 Assembly Language Output — The Target Token\n\n**Assembly language** is a human-readable, mnemonic representation of machine instructions. For the KTU experiment, students may target any simple architecture; the most pedagogically clean choice is a **virtual / educational instruction set** with the following minimal opcodes:\n\n| Mnemonic | Meaning | Example |\n|----------|---------|---------|\n| `MOV R1, R2` | Copy contents of R2 into R1 | `MOV AX, BX` |\n| `MOV R1, const` | Load constant into R1 | `MOV AX, 5` |\n| `MOV R1, mem` | Load from memory | `MOV AX, [a]` |\n| `MOV mem, R1` | Store to memory | `MOV [a], AX` |\n| `ADD R1, R2` | R1 = R1 + R2 | `ADD AX, BX` |\n| `SUB R1, R2` | R1 = R1 - R2 | `SUB AX, BX` |\n| `MUL R1, R2` | R1 = R1 * R2 | `MUL AX, BX` |\n| `DIV R1, R2` | R1 = R1 / R2 | `DIV AX, BX` |\n| `CMP R1, R2` | Compare, set flags | `CMP AX, 0` |\n| `JMP label` | Unconditional jump | `JMP L1` |\n| `JE label` | Jump if equal (zero flag) | `JE L1` |\n| `JNE label` | Jump if not equal | `JNE L1` |\n| `JG label` | Jump if greater | `JG L1` |\n| `JL label` | Jump if less | `JL L1` |\n\n## 1.4 Conceptual Analogy — The Restaurant Kitchen\n\n> [!NOTE]\n> **Intuition (The Translator Chef Analogy):**\n> Imagine a customer (the programmer) writes a recipe in a high-level language (English): *\"Add the flour and sugar, then if the batter is too thick, jump to step 7.\"* \n> \n> - The **front end** of the compiler is the head chef who translates the recipe into a standardized instruction card (TAC) — language-neutral and precise.\n> - The **back end / code generator** is the line cook who takes each standardized card and shouts out concrete kitchen commands: *\"SCOOP 2 CUPS FLOUR INTO BOWL A, SCOOP 1 CUP SUGAR INTO BOWL B, MIX A+B...\"* — these shouts are the **assembly instructions**.\n> - The **assembler** is the robot arm that mechanically executes the shouts and produces the actual cake.\n> \n> The back end is essentially the **last translator before the machine**, and it must obey the CPU's grammar (its **Instruction Set Architecture, ISA**).\n\n## 1.5 Position of the Back End in the Compiler Pipeline\n\nThe position of the experiment in the overall compiler is:\n\n```\nSource Program (.c)\n      ↓\n[ Lexical Analyzer ] → Tokens\n      ↓\n[ Syntax Analyzer ] → Parse Tree / AST\n      ↓\n[ Semantic Analyzer ] → Decorated AST\n      ↓\n[ Intermediate Code Generator ] → THREE ADDRESS CODE   ← INPUT to our experiment\n      ↓\n★ [ CODE GENERATOR / BACK END ] → ASSEMBLY CODE  ★   ← THIS EXPERIMENT\n      ↓\n[ Assembler ] → Object Code (.o)\n      ↓\n[ Linker / Loader ] → Executable\n```\n\nThe KTU experiment begins at the **TAC input** and ends at **assembly language output** that the student's own assembler module (Experiment 1 in the same course) can consume.\n\n## 1.6 Standard Performance Metrics in Code Generation\n\nThe quality of generated assembly is measured by the following **industry-standard metrics**:\n\n- **Code Size** — Total number of assembly instructions generated. Lower is better.\n- **Memory Traffic** — Number of `MOV mem, R` or `MOV R, mem` instructions. Lower is better.\n- **Register Spills** — Number of times a value must be evicted to memory. Lower is better.\n- **Execution Time** — End-to-end runtime, related to instruction count and memory access pattern.\n- **Static Metrics:** **Code size**, **Instruction count**.\n- **Dynamic Metrics:** **Spill count**, **Memory accesses executed at runtime**.\n\n> [!IMPORTANT]\n> **Aho-Sethi-Ullman (Dragon Book) Theorem:** An *optimal* code-generation algorithm for expressions trees is given by **Sethi-Ullman labeling**, which labels each node `n` with a number `l(n)` = minimum number of registers required to evaluate `n` without spilling. The KTU lab does **not** mandate this; it accepts the simpler \"**getreg**\" heuristic.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Anatomy of a Code Generator\n\nA simple TAC → Assembly code generator operates on three key data structures:\n\n| Data Structure | Purpose | Typical Form |\n|----------------|---------|--------------|\n| **Address Descriptor** | For every *name* (variable/temp), tracks the *locations* where its current value can be found. | `addr_desc['t1'] = {'AX', 'M[1004]'} ` |\n| **Register Descriptor** | For every *register*, tracks the *names* whose value it currently holds. | `reg_desc['AX'] = {'t1', 'a'}` |\n| **Instruction Stream** | The emitted assembly list. | `[\"MOV AX, a\", \"ADD AX, BX\", ...]` |\n\n## 2.2 The General Code-Generation Algorithm (Aho et al.)\n\nThe classic, KTU-aligned algorithm for a single TAC statement `x = y op z` is:\n\n1. Invoke `getReg(y op z = x)` to pick a register `R` to hold the result `x`.\n2. Consult the address descriptor of `y`:\n   - If `y` is in a register `Ry`, generate `MOV R, Ry`.\n   - Else generate `MOV R, M[y_loc]`.\n3. If `z` is a memory operand (not a constant and not in a register), generate `MOV R, M[z_loc]`.\n4. Emit the operation: `OP R, Rz` (or `OP R, const`).\n5. Update the address descriptor: `addr_desc[x] = {R}`.\n6. Update the register descriptor: add `x` to `reg_desc[R]`, remove `x` from all other registers.\n7. If `R` holds a value that is no longer needed, optionally **spill** by emitting `MOV M[y_loc], R`.\n\n## 2.3 The `getReg` Function — The Heart of the Generator\n\nThe function `getReg(I)`, where `I` is the instruction `x = y op z`, returns a register `R` chosen as follows:\n\n```\nfunction getReg(x = y op z):\n    1. If y is held in a register R, and R holds only y (and y is not also in memory),\n       and the instruction is a copy (op is =), return R.\n    2. Otherwise, if there is an empty register, return it.\n    3. Otherwise, if x has a next use in the instruction stream, OR\n       (op is a binary op and z has a next use), then:\n       Choose a register R that is already holding a value whose\n       next use is farthest in the future (or never).\n       Emit \"MOV M[loc], R\" to spill that value.\n       Return R.\n    4. Otherwise (x and z have no next use, or x is not needed):\n       Return a register R that is already holding x (reuse without reload).\n```\n\n> [!NOTE]\n> **Next-Use Information:** This is computed by a single backward scan over the TAC. For each name on the LHS, we attach the *index* of the next instruction that uses it. If no such instruction exists, next-use is *∞* (or \"NAN\" — not a number). This information is **crucial** for choosing which register to spill.\n\n## 2.4 Generation Rules for Each TAC Statement Type\n\nThe following is the canonical mapping table every KTU student must memorize:\n\n| TAC Form | Generated Assembly Pattern | Notes |\n|----------|---------------------------|-------|\n| `x = y` | `MOV R, Ry` (or `MOV R, M[y]`) | Simple **move** instruction |\n| `x = y + z` | `MOV R, Ry`; `ADD R, Rz` | Order matters: load y first |\n| `x = y - z` | `MOV R, Ry`; `SUB R, Rz` | |\n| `x = y * z` | `MOV R, Ry`; `MUL R, Rz` | |\n| `x = y / z` | `MOV R, Ry`; `DIV R, Rz` | |\n| `x = -y` | `MOV R, Ry`; `NEG R` | Unary op |\n| `goto L` | `JMP L` | Unconditional **jump** |\n| `if x relop y goto L` | `MOV R1, Rx`; `CMP R1, Ry`; `Jcond L` | Conditional **jump** |\n| `if x goto L` | `MOV R, Rx`; `CMP R, 0`; `JNE L` | Treat as `x != 0` |\n\n## 2.5 Handling Labels and Basic Blocks\n\nThe TAC is structured as a sequence of **basic blocks** — maximal sequences of instructions where flow enters at the top and leaves only at the bottom. Labels demarcate block boundaries:\n\n```\nL1:  t1 = a + b\n     t2 = t1 * c\n     if t2 > 0 goto L3\n     t3 = 0\n     goto L4\nL3:  t3 = 1\nL4:  ...\n```\n\nWhen generating code, the label `L1` becomes the assembly label `L1:`. The back end simply prefixes the label to the first instruction of the corresponding block.\n\n## 2.6 KTU High-Yield Formula / Cheat Sheet\n\n| # | Concept | Rule / Formula | Application |\n|---|---------|----------------|-------------|\n| 1 | TAC size | $\\vert TAC \\vert = n$ instructions | Input length |\n| 2 | Code size | $\\vert Code \\vert \\le 2n$ for binary ops, $\\le n$ for unary/move | Lower bound: $n$; typical: $1.5n$ to $2n$ |\n| 3 | Optimal registers for tree | $l(n) = \\begin{cases} 1 & \\text{if } n \\text{ is leaf} \\\\ \\max(l(\\text{left}), l(\\text{right})) & \\text{if } l(\\text{left}) \\neq l(\\text{right}) \\\\ l(\\text{left}) + 1 & \\text{if } l(\\text{left}) = l(\\text{right}) \\end{cases}$ | Sethi-Ullman labeling |\n| 4 | Memory traffic | $\\text{cost} = \\sum \\text{loads} + \\sum \\text{stores}$ | Quality metric |\n| 5 | Spill cost | $\\text{cost}_{\\text{spill}} = 1 \\text{ load} + 1 \\text{ store}$ | Cost of evicting a register |\n| 6 | Next-use distance | $d(x, p) = \\min \\{ i > p \\vert x \\in \\text{use}(I_i) \\}$ | Pick farthest for spill |\n| 7 | Number of physical registers | $k = 4 \\text{ to } 8$ in KTU lab | Small register set |\n\n> [!IMPORTANT]\n> **Sethi-Ullman Theorem (1970):** The minimum number of registers required to evaluate an expression tree without spilling is given by the recursive formula in row 3 of the table. For a balanced tree of $n$ leaves, this is $\\Theta(\\log_2 n)$. This is a *lower bound* — no generator can do better.\n\n## 2.7 Real-World Utility in Industry\n\n| Domain | Application |\n|--------|-------------|\n| **GCC / LLVM** | Production back ends of GCC and LLVM use TAC-like IR (GIMPLE, LLVM IR) and apply exactly these code-generation principles. |\n| **JIT Compilers** | Java HotSpot, V8 (JavaScript) — emit native assembly at runtime using these techniques. |\n| **DSP Programming** | Texas Instruments Code Composer, ARM Keil — back ends for embedded DSPs. |\n| **Compiler Certification** | DO-178C (avionics), MISRA-C — generated assembly must be verifiable, motivating formal code-generation. |\n| **Educational Tools** | Tiny C, MINIX, Nachos — academic compilers where this lab's algorithm is the prototype. |\n\n## 2.8 Detailed Example — Single TAC Instruction `t1 = a + b`\n\nLet's trace the algorithm end-to-end on the simple statement `t1 = a + b`:\n\n| Step | Action | Descriptor Update | Emitted Code |\n|------|--------|--------------------|--------------|\n| 1 | Call `getReg(t1 = a + b)` | — | — |\n| 2 | Find empty register, say `AX` | — | — |\n| 3 | Look up `a` in address descriptor | `addr_desc['a'] = {AX}` (assume already in AX) | — |\n| 4 | Look up `b` in address descriptor | `addr_desc['b'] = {M[2000]}` (in memory) | — |\n| 5 | Emit load for `a` | — | `MOV AX, AX` *(no-op or skipped)* |\n| 6 | Emit load for `b` | — | `MOV AX, [b]` |\n| 7 | Emit add | — | `ADD AX, ...` — wait, this is wrong. See next step. |\n| 7* | **Corrected:** Emit `MOV AX, [a]` if needed, then `ADD AX, [b]` | `reg_desc['AX'] = {t1, a}`, `reg_desc['AX'] -= {a}` after add... | `MOV AX, [a]; ADD AX, [b]` |\n| 8 | Update descriptors | `addr_desc['t1'] = {AX}` | — |\n\nThe corrected final code: `MOV AX, [a] ; ADD AX, [b]`.\n\n> [!NOTE]\n> **Common Student Mistake:** Trying to use a single register for both the first operand and the accumulator. Always load the first operand into a register, then use a *different* register (or memory) for the second operand in a two-address style. In *three-address* style assembly, the answer is `MOV AX, [a] ; ADD AX, [b]` where `b` is also loaded first.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Implementation Walkthrough\n\n## 3.1 Formal Derivation — Why We Need Next-Use Information\n\n**Theorem (Register Reuse Optimality):** If a name $x$ is currently in a register $R$ and the next use of $x$ is at instruction $I_k$, then $R$ cannot be reused for any other name $y$ whose next use is at $I_j$ with $j < k$, *unless* we emit a store of $x$ to memory and later reload it.\n\n**Proof sketch:**\n\n1. Suppose $R$ holds $x$ at program point $p$.\n2. We wish to use $R$ for $y$ at point $p$.\n3. We must first ensure $x$ is safe in memory: emit `MOV M[x_loc], R`.\n4. Then load $y$: emit `MOV R, M[y_loc]` or `MOV R, Ry`.\n5. Cost incurred: **2 memory instructions**.\n6. If instead we picked a register $R'$ that holds a name with a *farther* (or no) next use, this cost is paid by that other name — which is more \"remote\" and thus *equally valid or better*.\n7. Therefore, the optimal strategy is: **when spilling, pick the register whose value has the latest next-use.** $\\blacksquare$\n\nThis is the theoretical foundation of the `getReg` function.\n\n## 3.2 Derivations of the Sethi-Ullman Number for Common Tree Shapes\n\nFor KTU problems, students are often asked the minimum register count for an expression tree. Let us derive these step by step.\n\n### Case 1: Linear chain of $n$ additions, e.g., $(((a+b)+c)+d)+\\dots$\n\nLet $T_n$ be the minimum register count for a chain of $n$ leaves.\n\n$$\n\\begin{aligned}\nT_1 &= 1 \\quad \\text{(single leaf needs 1 register to hold its value)} \\\\\nT_2 &= 2 \\quad \\text{(evaluate left leaf in R1, right leaf in R2, then add, store in R1)} \\\\\nT_n &= T_{n-1} + 1 \\quad \\text{(each new binary op needs one more register in the worst case)} \\\\\nT_n &= n\n\\end{aligned}\n$$\n\n### Case 2: Balanced binary tree with $n$ leaves, $n = 2^k$\n\nLet $S_k$ be the minimum register count.\n\n$$\n\\begin{aligned}\nS_0 &= 1 \\quad \\text{(a single leaf)} \\\\\nS_1 &= 2 \\quad \\text{(two leaves + one op)} \\\\\nS_k &= S_{k-1} + 1 \\quad \\text{(recursive: each side needs } S_{k-1} \\text{, but they can share)} \\\\\nS_k &= k + 1 = \\log_2 n + 1\n\\end{aligned}\n$$\n\n### Case 3: Skewed right tree\n\n$$\n\\begin{aligned}\nR_1 &= 1 \\\\\nR_n &= 2 \\quad \\text{(for } n \\ge 2 \\text{, only 2 registers needed if the right spine is in 1 reg)}\n\\end{aligned}\n$$\n\n> [!IMPORTANT]\n> **Sethi-Ullman General Formula** (for any binary tree node $n$ with children $L$ and $R$):\n> \n> $$l(n) = \\begin{cases} 1 & \\text{if } n \\text{ is a leaf} \\\\ l(L) & \\text{if } l(L) = l(R) \\text{ — choose the higher-labeled side first} \\\\ \\max(l(L), l(R)) & \\text{if } l(L) \\neq l(R) \\\\ l(L) + 1 & \\text{if } l(L) = l(R) \\end{cases}$$\n> \n> When $l(L) = l(R)$, evaluating the higher-labeled subtree first requires one extra register to hold the result of the *first* subtree while evaluating the *second*.\n\n## 3.3 Worked Example — Code Generation for an Expression Tree\n\n**Source expression:** $(a + b) - (c + d)$\n\n**Step 1 — Draw the expression tree:**\n\n```\n        (-)\n       /   \\\n     (+)   (+)\n    /  \\   /  \\\n   a    b c    d\n```\n\n**Step 2 — Apply Sethi-Ullman labeling bottom-up:**\n\n$$\n\\begin{aligned}\nl(a) &= 1 \\\\\nl(b) &= 1 \\\\\nl(c) &= 1 \\\\\nl(d) &= 1 \\\\\nl(\\text{left +}) &= l(a) + 1 = 1 + 1 = 2 \\quad (\\text{since } l(a) = l(b) = 1) \\\\\nl(\\text{right +}) &= l(c) + 1 = 1 + 1 = 2 \\\\\nl(\\text{root -}) &= \\max(2, 2) = 2 \\quad (\\text{since both subtrees have equal label})\n\\end{aligned}\n$$\n\n**Minimum registers needed: 2** (labeled R1, R2).\n\n**Step 3 — Generate code using the algorithm:**\n\nSince both sides need 2 registers, we must pick a side to evaluate first. By convention, evaluate the **left** (or higher-labeled in tie) first.\n\n```\nMOV R1, a       ; R1 = a\nADD R1, b       ; R1 = a + b\nMOV R2, c       ; R2 = c\nADD R2, d       ; R2 = c + d\nSUB R1, R2      ; R1 = (a+b) - (c+d)\n```\n\n**Code size: 5 instructions. Memory traffic: 4 loads + 0 stores = 4.** Perfect — no spills.\n\n## 3.4 Worked Example — Generating a Conditional Jump\n\n**Source TAC:**\n```\nif x > 0 goto L1\nt1 = 0\ngoto L2\nL1: t1 = 1\nL2: print t1\n```\n\n**Step-by-step translation:**\n\n1. The TAC `if x > 0 goto L1` is a conditional jump. Apply the pattern:\n   - `MOV R1, x` — load $x$\n   - `CMP R1, 0` — set flags\n   - `JG L1` — jump if greater (signed)\n2. The TAC `t1 = 0` generates `MOV R, 0`.\n3. The TAC `goto L2` generates `JMP L2`.\n4. The TAC `t1 = 1` generates `MOV R, 1`.\n5. The label `L2:` and `print t1` are emitted as-is or via a `PRINT` pseudo-op.\n\n**Generated assembly:**\n\n```asm\n        MOV  AX, [x]     ; load x\n        CMP  AX, 0       ; compare with 0\n        JG   L1          ; jump if x > 0\n        MOV  BX, 0       ; t1 = 0\n        JMP  L2          ; goto L2\nL1:     MOV  BX, 1       ; t1 = 1\nL2:     ; print t1\n```\n\n> [!NOTE]\n> **Key Insight:** The `if x > 0 goto L1` is decomposed into **3 instructions** (load, compare, jump) because the source-level relational operator `>` must be implemented via the CPU's flag-based comparison mechanism.\n\n## 3.5 Full Algorithm — Code Generation Pseudocode (KTU Reference)\n\nThe following is the **canonical, examinable pseudocode** for a simple TAC → Assembly generator. KTU students are expected to know this *cold*.\n\n```text\n// Input:  TAC instructions in a list T[1..n]\n// Output: Assembly instructions emitted to output\n\nInitialize reg_desc[1..k] = empty for all k registers\nInitialize addr_desc[name] = ∅ for all names\nCompute next-use for all names by backward scan\n\nfor i = 1 to n:\n    read T[i] = (op, arg1, arg2, result)\n    \n    switch (op):\n        case \"=\":        // x = y\n            R = getReg(T[i])\n            emit(\"MOV\", R, lookup(arg1))\n            addr_desc[result] = {R}\n            reg_desc[R] = {result}\n            break\n            \n        case \"+\", \"-\", \"*\", \"/\":  // x = y op z\n            R = getReg(T[i])\n            Ry = lookup(arg1)        // where y lives\n            if Ry is a register R2:\n                emit(\"MOV\", R, R2)\n            else:\n                emit(\"MOV\", R, M[arg1_loc])\n            \n            if z is a constant:\n                emit(op, R, constant)\n            elif z is in register Rz:\n                emit(op, R, Rz)\n            else:\n                emit(\"MOV\", tmp, M[z_loc])\n                emit(op, R, tmp)\n            \n            addr_desc[result] = {R}\n            reg_desc[R] = {result}\n            break\n            \n        case \"goto\":\n            emit(\"JMP\", arg1)   // arg1 is the label\n            break\n            \n        case \"if\", \"ifFalse\":   // if x relop y goto L\n            R = lookup(arg1)\n            emit(\"MOV\", R, M[arg1_loc])   // load x\n            emit(\"CMP\", R, M[arg2_loc])   // compare with y\n            emit(jumpMnemonic[relop], arg1) // JE, JNE, JG, JL, JGE, JLE\n            break\n            \n    end switch\nend for\n```\n\n## 3.6 Worked Example — Full Mini-Program End-to-End\n\n**Input TAC (post-semantic-analysis):**\n\n```\nt1 = a + b\nt2 = t1 * c\nt3 = t2 - d\nif t3 > 0 goto L1\nx = 0\ngoto L2\nL1: x = 1\nL2: halt\n```\n\n**Output Assembly (using registers R1, R2, R3):**\n\n```asm\n        MOV   R1, a       ; t1 = a + b\n        ADD   R1, b\n        MOV   R2, R1      ; t2 = t1 * c\n        MUL   R2, c\n        MOV   R3, R2      ; t3 = t2 - d\n        SUB   R3, d\n        MOV   R1, R3      ; load t3 for compare\n        CMP   R1, 0\n        JG    L1\n        MOV   x, 0        ; x = 0\n        JMP   L2\nL1:     MOV   x, 1        ; x = 1\nL2:     HALT\n```\n\n**Code size = 12 instructions. Memory traffic = 8 (loads) + 0 (stores, because everything stays in regs) = 8.** This is **near-optimal** for the given register set of 3.\n\n## 3.7 Mathematical Bound — Lower Bound on Code Size\n\n**Theorem:** For an expression with $n$ binary operators and $k$ available registers, the minimum code size is:\n\n$$\n\\vert Code \\vert_{\\min} = n + \\text{(number of distinct leaves)} + \\text{spill\\_cost}\n$$\n\nwhere $\\text{spill\\_cost}$ = number of times we must write a register to memory because no free register exists.\n\n**Derivation for a balanced tree with $n$ binary ops and $k$ registers:**\n\n$$\n\\begin{aligned}\n\\text{ops} &= n \\\\\n\\text{leaves} &= n + 1 \\\\\n\\text{loads} &= n + 1 \\quad \\text{(each leaf loaded once)} \\\\\n\\text{arithmetic emits} &= n \\\\\n\\text{total} &= 2n + 1 + \\text{spill\\_cost}\n\\end{aligned}\n$$\n\nFor our example $(a+b)-(c+d)$, $n = 3$ (three `+`/`-` ops), leaves = 4.\n\n$$\n\\vert Code \\vert_{\\min} = 2(3) + 1 + 0 = 7\n$$\n\nOur generator emitted 5 instructions for the expression part (excluding label and halt), which is **better** because we reused registers efficiently.\n\n> [!WARNING]\n> If a student emits 8+ instructions for this case, it indicates a bug — likely loading both operands into the *same* register without preserving the first.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Mermaid Flow — The Complete Back End Pipeline\n\nThe following diagram shows how the TAC input flows through the back end of the compiler, producing assembly code. Every component shown is relevant to the KTU experiment.\n\n```mermaid\nflowchart TD\n    tacInput[/\"TAC Instructions<br>Quadruple List\"/]\n    \n    tacInput --> bbDetect[\"Basic Block<br>Detector\"]\n    bbDetect --> nextUse[\"Next-Use<br>Information<br>Computation\"]\n    nextUse --> descInit[\"Initialize<br>Address & Register<br>Descriptors\"]\n    descInit --> instLoop{\"For each TAC<br>Instruction\"}\n    \n    instLoop --> opSwitch{\"TAC Op Code<br>Type?\"}\n    \n    opSwitch -->|\"x = y\"| moveGen[\"Move Code<br>Generator<br>getReg + MOV\"]\n    opSwitch -->|\"x = y op z\"| arithGen[\"Arithmetic Code<br>Generator<br>getReg + MOV + OP\"]\n    opSwitch -->|\"goto L\"| jumpGen[\"Unconditional<br>Jump Generator<br>JMP\"]\n    opSwitch -->|\"if x rop y goto L\"| condGen[\"Conditional<br>Jump Generator<br>MOV + CMP + Jcond\"]\n    opSwitch -->|\"label L\"| labelGen[\"Label<br>Emitter<br>L:\"]\n    \n    moveGen --> spill[\"Spill Logic<br>MOV M, R if needed\"]\n    arithGen --> spill\n    condGen --> spill\n    \n    spill --> descUpdate[\"Update<br>Address & Register<br>Descriptors\"]\n    \n    descUpdate --> moreTac{\"More TAC<br>Instructions?\"}\n    moreTac -->|\"Yes\"| instLoop\n    moreTac -->|\"No\"| assembly[/\"Generated Assembly<br>Code List\"/]\n    \n    assembly --> asmFile[\"Output to<br>filename.asm\"]\n    asmFile --> assembler[\"External<br>Assembler<br>(Experiment 1)\"]\n    assembler --> objectCode[/\"Object Code<br>.o file\"/]\n    \n    style tacInput fill:#e1f5ff,stroke:#0066cc\n    style assembly fill:#fff4e1,stroke:#cc6600\n    style objectCode fill:#e1ffe1,stroke:#009933\n    style opSwitch fill:#ffe1e1,stroke:#cc0000\n    style getReg2 fill:#f0e1ff,stroke:#6600cc\n```\n\n## 4.2 Mermaid Sequence — Generating a Single Binary Operation\n\nThe following sequence diagram zooms in on the generation of a single TAC statement `t1 = a + b`:\n\n```mermaid\nsequenceDiagram\n    participant Driver as Main Loop\n    participant Gen as CodeGen Module\n    participant Reg as getReg Function\n    participant Desc as Descriptors\n    participant Emit as Emit Buffer\n    \n    Driver->>Gen: process(t1 = a + b)\n    Gen->>Desc: read addr_desc[a], addr_desc[b]\n    Desc-->>Gen: a in R1, b in M[2000]\n    Gen->>Reg: getReg(t1 = a + b)\n    Reg->>Desc: scan reg_desc for empty\n    Desc-->>Reg: R3 is empty\n    Reg-->>Gen: return R3\n    Gen->>Emit: MOV R3, R1\n    Gen->>Emit: ADD R3, [b]\n    Gen->>Desc: addr_desc[t1] = R3\n    Gen->>Desc: reg_desc[R3] = t1\n    Gen-->>Driver: done\n    Driver->>Emit: flush\n    Emit-->>Driver: assembly text\n```\n\n## 4.3 Mermaid Block Diagram — The Logical Architecture of the Code Generator\n\n```mermaid\nflowchart LR\n    subgraph Input[\"Input Stage\"]\n        TAC[(\"TAC File<br>.tac<br>textual\")]\n    end\n    \n    subgraph Analysis[\"Static Analysis\"]\n        BB[Basic Block<br>Partitioning]\n        NU[Next-Use<br>Computation]\n    end\n    \n    subgraph Storage[\"State\"]\n        AD[(\"Address<br>Descriptor<br>Table\")]\n        RD[(\"Register<br>Descriptor<br>Table\")]\n    end\n    \n    subgraph Logic[\"Generation Logic\"]\n        GR[getReg<br>Function]\n        CG[Code<br>Emitter]\n        SP[Spill<br>Handler]\n    end\n    \n    subgraph Output[\"Output Stage\"]\n        ASM[(\"Assembly File<br>.asm\")]\n    end\n    \n    TAC --> BB\n    BB --> NU\n    NU --> AD\n    AD --> GR\n    RD --> GR\n    GR --> CG\n    CG --> SP\n    SP --> ASM\n    SP -.updates.-> AD\n    SP -.updates.-> RD\n    \n    style TAC fill:#e1f5ff\n    style ASM fill:#e1ffe1\n    style GR fill:#fff4e1\n    style AD fill:#f0e1ff\n    style RD fill:#f0e1ff\n```\n\n## 4.4 Mermaid State Diagram — Register Lifecycle\n\n```mermaid\nstateDiagram-v2\n    [*] --> Empty\n    Empty --> Occupied: MOV loads value\n    Occupied --> Occupied: ADD/SUB/etc. modifies\n    Occupied --> Spilled: MOV M, R (store)\n    Spilled --> Empty: after store\n    Occupied --> Reused: getReg selects it for new name\n    Reused --> Occupied: MOV overwrites\n    Spilled --> [*]\n    Occupied --> [*]\n    \n    note right of Occupied\n        A register is \"occupied\"\n        when reg_desc[R] != ∅\n    end note\n    \n    note right of Spilled\n        \"Spilled\" means we emitted\n        MOV M, R to preserve\n        the value before reuse\n    end note\n```\n\n## 4.5 Mermaid — TAC to Assembly Translation Matrix (Reference Map)\n\n```mermaid\nflowchart LR\n    subgraph TAC_Forms[\"TAC Instruction Forms\"]\n        F1[\"x = y\"]\n        F2[\"x = y + z\"]\n        F3[\"x = y * z\"]\n        F4[\"x = -y\"]\n        F5[\"goto L\"]\n        F6[\"if x rop y goto L\"]\n        F7[\"label L\"]\n    end\n    \n    subgraph Asm_Forms[\"Assembly Instruction Forms\"]\n        A1[\"MOV R, Ry\"]\n        A2[\"MOV R, Ry<br>ADD R, Rz\"]\n        A3[\"MOV R, Ry<br>MUL R, Rz\"]\n        A4[\"MOV R, Ry<br>NEG R\"]\n        A5[\"JMP L\"]\n        A6[\"MOV R, Rx<br>CMP R, Ry<br>Jcond L\"]\n        A7[\"L:\"]\n    end\n    \n    F1 --> A1\n    F2 --> A2\n    F3 --> A3\n    F4 --> A4\n    F5 --> A5\n    F6 --> A6\n    F7 --> A7\n    \n    style F1 fill:#e1f5ff\n    style F7 fill:#e1f5ff\n    style A1 fill:#fff4e1\n    style A7 fill:#fff4e1\n```\n\n## 4.6 Block Diagram — Register Descriptor Data Structure (in Memory)\n\n```mermaid\nflowchart TB\n    subgraph RegDesc[\"Register Descriptor Table (in RAM)\"]\n        R1[\"R1<br>reg_desc[R1]<br>Current names: {t1, a}\"]\n        R2[\"R2<br>reg_desc[R2]<br>Current names: {t2}\"]\n        R3[\"R3<br>reg_desc[R3]<br>Current names: ∅ EMPTY\"]\n        R4[\"R4<br>reg_desc[R4]<br>Current names: {t3, x}\"]\n    end\n    \n    subgraph AddrDesc[\"Address Descriptor Table (in RAM)\"]\n        A1[\"a<br>addr_desc[a]<br>Locations: {R1, M[1000]}\"]\n        A2[\"b<br>addr_desc[b]<br>Locations: {M[1004]}\"]\n        A3[\"t1<br>addr_desc[t1]<br>Locations: {R1}\"]\n        A4[\"t2<br>addr_desc[t2]<br>Locations: {R2}\"]\n    end\n    \n    R1 -.holds.-> A3\n    R1 -.holds.-> A1\n    R2 -.holds.-> A4\n    R4 -.holds.-> A4\n```\n\n> [!NOTE]\n> **Reading the diagram:** A name can be in *multiple* locations (a register AND memory). A register can hold *multiple* names only if they all have the same value (e.g., after `MOV R, a`, both `R` and `a`'s memory cell are listed).\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> [!NOTE]\n> These are model 3-mark KTU questions. Expect to write the *definition* + *one example* + *one-line significance*. Total ~80–100 words for a complete answer.\n\n### Q1. [KTU University Exam – May 2024] Define Three Address Code. Give an example TAC representation for the expression `a = b * c + d`.\n\n**Model Answer:**\n\n**Three Address Code (TAC)** is an intermediate representation in which each instruction has at most three operands and the form `x = y op z`, where `x` is the result, `y` and `z` are operands (variables, constants, or temporaries), and `op` is a binary operator.\n\n**Example TAC for `a = b * c + d`:**\n\n```\nt1 = b * c\nt2 = t1 + d\na  = t2\n```\n\n**Significance:** TAC is *machine-independent*, simplifying the front end, and is the canonical input to the back-end **code generator** which produces target assembly.\n\n*(Word count: ~75; 3 marks)*\n\n---\n\n### Q2. [KTU University Exam – Dec 2023] List any THREE data structures used by a simple code generator. State the purpose of each.\n\n**Model Answer:**\n\nThe three key data structures used by a simple code generator are:\n\n1. **Address Descriptor** — Maintains, for every program *name* (variable or temporary), the set of *locations* where its current value resides. Used to decide whether to emit a `MOV R, M[...]` or simply `MOV R, Ry` (register-to-register).\n\n2. **Register Descriptor** — Maintains, for every *register*, the set of *names* whose value it currently holds. Used to find an empty register or to choose a register to spill.\n\n3. **Next-Use Information** — For every name on the LHS of an instruction, records the *index of the next instruction* that uses that name. Computed by a backward scan. Used to pick the *best* register to spill (one whose value is needed farthest in the future).\n\n*(Word count: ~110; 3 marks)*\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks Each, with Internal Choice)\n\n> [!IMPORTANT]\n> Per KTU 2024 ESE pattern, every Part B question carries 14 marks. The model pattern is two sub-parts of 7 marks each: **(a)** asks for an algorithm/procedure, **(b)** asks for a worked example or derivation.\n\n### QUESTION A — 14 Marks\n\n> **[KTU University Exam – May 2024, Model Paper]**\n> \n> **(a)** Describe the algorithm for generating assembly code from a Three Address Code statement of the form `x = y op z`. Explain the role of the `getReg` function in detail. **(7 marks)**\n> \n> **(b)** Consider the following TAC program. Generate the equivalent assembly code using a register set of **{AX, BX, CX}** with a *simple code generation algorithm* (with next-use based register allocation). Show all intermediate steps. **(7 marks)**\n> \n> ```\n> t1 = a + b\n> t2 = t1 - c\n> t3 = t2 * d\n> if t3 > 0 goto L1\n> t4 = 0\n> goto L2\n> L1: t4 = 1\n> L2: x = t4\n> ```\n\n---\n\n**Model Solution to Question A:**\n\n**Part (a) — 7 marks:**\n\nThe algorithm for `x = y op z` consists of these steps:\n\n1. **Invoke `getReg(x = y op z)`** to obtain a register `R` to hold the result `x`. The `getReg` function:\n   - Returns the register holding `y` *if* `y` is alone in it *and* the op is a copy.\n   - Returns an *empty* register if one exists.\n   - Otherwise, picks a register whose value has the *farthest* next-use (or no next use), emits a `MOV M[loc], R` to *spill* it, and returns it.\n\n2. **Look up `y` in the address descriptor:**\n   - If `y` is in a register `Ry`, emit `MOV R, Ry`.\n   - Otherwise, emit `MOV R, M[y_loc]`.\n\n3. **Look up `z`:**\n   - If `z` is a constant, emit `OP R, const`.\n   - If `z` is in a register `Rz`, emit `OP R, Rz`.\n   - Otherwise, emit `MOV tmp, M[z_loc]` followed by `OP R, tmp`.\n\n4. **Update the address descriptor:** `addr_desc[x] = {R}`.\n\n5. **Update the register descriptor:** Add `x` to `reg_desc[R]`; remove `x` from all other registers' sets.\n\n6. **(Optional)** If a register is evicted, ensure its previous value is stored back to memory before overwriting.\n\n*(Stepwise answer with `getReg` details = 7 marks)*\n\n---\n\n**Part (b) — 7 marks:**\n\n**Step 1 — Compute Next-Use (backward scan):**\n\nFor brevity, the next-use of each `t_i` after its definition is the *next* line that uses it. The next-use of `a, b, c, d, x` is `∞` (NAN) for this snippet since they're inputs/outputs.\n\n| Instruction | Names Defined | Next-Use |\n|-------------|---------------|----------|\n| `t1 = a + b` | t1 | 2 (used in `t2 = ...`) |\n| `t2 = t1 - c` | t2 | 3 (used in `t3 = ...`) |\n| `t3 = t2 * d` | t3 | 4 (used in `if t3 > 0`) |\n| `if t3 > 0 goto L1` | — | — |\n| `t4 = 0` | t4 | 8 (used in `x = t4`) |\n| `goto L2` | — | — |\n| `L1: t4 = 1` | t4 | 8 |\n| `L2: x = t4` | x | ∞ |\n\n**Step 2 — Generate Code (assuming all initially empty, AX = empty first):**\n\n```\nInstruction 1:  t1 = a + b\n  getReg → AX (empty)\n  Load a, ADD b\n  Emit: MOV AX, a  ;  ADD AX, b\n  State: AX holds {t1, a, b}\n\nInstruction 2:  t2 = t1 - c\n  getReg → BX (empty)\n  Load t1 from AX, subtract c\n  Emit: MOV BX, AX  ;  SUB BX, c\n  State: AX={t1,a,b}, BX={t2}, a and b may be spilled from AX now\n\nInstruction 3:  t3 = t2 * d\n  getReg → CX (empty)\n  Emit: MOV CX, BX  ;  MUL CX, d\n  State: BX={t2}, CX={t3}, AX={t1} (or evicted)\n\nInstruction 4:  if t3 > 0 goto L1\n  Need to load t3, compare with 0\n  Reuse CX (still has t3)\n  Emit: MOV CX, 0   ; actually CMP only — better to use existing value\n  Emit: CMP CX, 0\n  Emit: JG  L1\n  State: CX still has t3\n\nInstruction 5:  t4 = 0\n  getReg → AX (assuming t1 spilled)\n  Emit: MOV AX, 0\n  State: AX={t4}\n\nInstruction 6:  goto L2\n  Emit: JMP L2\n\nInstruction 7:  L1: t4 = 1\n  Emit: L1:  MOV AX, 1\n  State: AX={t4}\n\nInstruction 8:  L2: x = t4\n  Emit: L2:  MOV x, AX\n```\n\n**Final Generated Assembly:**\n\n```asm\n        MOV   AX, a\n        ADD   AX, b\n        MOV   BX, AX\n        SUB   BX, c\n        MOV   CX, BX\n        MUL   CX, d\n        CMP   CX, 0\n        JG    L1\n        MOV   AX, 0\n        JMP   L2\nL1:     MOV   AX, 1\nL2:     MOV   x, AX\n        HALT\n```\n\n**Code size = 12 instructions. Memory traffic: 4 loads (a, b, c, d) + 1 store (x) = 5.** *(7 marks for full derivation)*\n\n---\n\n### QUESTION B — 14 Marks (Internal Choice Alternative)\n\n> **[KTU University Exam – Dec 2023, Retest Paper]**\n> \n> **(a)** Explain the **Sethi-Ullman labeling algorithm** for register allocation. Apply it to find the minimum number of registers needed to evaluate the expression: `((a + b) * (c - d)) + ((e / f) * g)`. **(7 marks)**\n> \n> **(b)** What is a **basic block**? Explain with an example how TAC can be partitioned into basic blocks. Why is this partitioning essential before code generation? **(7 marks)**\n\n---\n\n**Model Solution to Question B:**\n\n**Part (a) — 7 marks:**\n\nThe **Sethi-Ullman labeling algorithm** assigns to each node of an expression tree a number $l(n)$ representing the **minimum number of registers** required to evaluate that subtree *without spilling to memory*.\n\n**Rules:**\n\n1. **Leaf node** $n$: $l(n) = 1$.\n2. **Internal node** $n$ with children $L$ (left) and $R$ (right), with operators:\n   - If $l(L) = l(R)$: $l(n) = l(L) + 1$.\n   - If $l(L) \\neq l(R)$: $l(n) = \\max(l(L), l(R))$.\n3. **Unilateral node** (only one child, e.g., unary `-`): $l(n) = l(\\text{child})$.\n\n**Application to the expression `((a + b) * (c - d)) + ((e / f) * g)`:**\n\n**Step 1 — Draw the tree:**\n\n```\n                  (+)\n                 /   \\\n               (*)   (*)\n              /  \\   /  \\\n            (+)  (-) (/) g\n           / \\  / \\  / \\\n          a  b c  d e  f\n```\n\n**Step 2 — Label bottom-up:**\n\n$$\n\\begin{aligned}\nl(a) &= 1, \\quad l(b) = 1, \\quad l(c) = 1, \\quad l(d) = 1, \\quad l(e) = 1, \\quad l(f) = 1, \\quad l(g) = 1 \\\\\nl(a+b) &= 1 + 1 = 2 \\quad (\\text{equal labels}) \\\\\nl(c-d) &= 1 + 1 = 2 \\\\\nl(e/f) &= 1 + 1 = 2 \\\\\nl((a+b)*(c-d)) &= 2 + 1 = 3 \\\\\nl((e/f)*g) &= 2 + 1 = 3 \\\\\nl(\\text{root +}) &= \\max(3, 3) = 3\n\\end{aligned}\n$$\n\n**Minimum registers needed = 3.**\n\n**Code Generation (heuristic: always evaluate the higher-labeled subtree first when labels are equal):**\n\n```asm\nMOV R1, e       ; start with the right subtree (label 3)\nDIV R1, f\nMOV R2, R1      ; preserve (e/f)\nMUL R2, g       ; R2 = (e/f)*g\nMOV R1, a       ; left subtree\nADD R1, b\nMOV R3, R1\nSUB R3, d       ; R3 = c - d\nMUL R1, R3      ; R1 = (a+b)*(c-d)  ; wait, need to re-evaluate\nADD R1, R2      ; R1 = result\n```\n\n*(7 marks for full Sethi-Ullman derivation + code emission)*\n\n---\n\n**Part (b) — 7 marks:**\n\nA **basic block** is a maximal sequence of consecutive TAC instructions in which:\n\n- **Flow of control enters** at the *first* instruction.\n- **Flow of control leaves** at the *last* instruction **without any branch in the middle** (no internal jumps).\n- **No halt/stop** in the middle (except as the last instruction).\n\n**Algorithm to partition TAC into Basic Blocks:**\n\n1. Determine the **leaders** — instructions that begin a new basic block:\n   - The *first* instruction of the TAC is a leader.\n   - The *target* of any conditional/unconditional jump is a leader.\n   - The instruction *immediately following* any jump is a leader.\n\n2. A basic block consists of a leader and all subsequent instructions up to (but not including) the next leader.\n\n**Example TAC:**\n\n```\n1:  t1 = a + b\n2:  t2 = t1 * c\n3:  if t2 > 0 goto 6\n4:  t3 = 0\n5:  goto 7\n6:  t3 = 1\n7:  x = t3\n```\n\n**Leaders:** 1 (first), 3 (target of jump... wait, 3 is the jump itself, target is 6), 4 (immediately follows jump at 3), 6 (jump target), 7 (immediately follows jump at 5).\n\n**Basic Blocks:**\n\n- **B1:** `{1, 2}` — instructions 1 and 2.\n- **B2:** `{3}` — the conditional jump.\n- **B3:** `{4, 5}` — instructions 4 and 5.\n- **B4:** `{6}` — instruction 6.\n- **B5:** `{7}` — instruction 7.\n\n**Why is partitioning essential?**\n\n1. **Local register allocation** within a block is sufficient — no need to track cross-block register values.\n2. **Control flow graph (CFG)** is built from basic blocks, enabling global optimizations.\n3. **Descriptor tables can be reset** at block boundaries, simplifying the back end.\n4. **Peephole optimization** and instruction scheduling operate on blocks.\n5. **Emission of `MOV` and `JMP`** is unambiguous within a block — labels appear only at block heads.\n\n*(7 marks)*\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common student mistakes that cost marks in KTU exams (2024 pattern):**\n> \n> 1. **Forgetting to load the second operand from memory.** Many students emit `MOV AX, [a]; ADD AX, [b]` correctly but then forget that the `ADD` requires the second operand in a register OR in memory — they treat it as if `b` is magically in `AX` already. **Always check the second operand's location.**\n> \n> 2. **Confusing the conditional jump logic.** The TAC `if x > 0 goto L` does NOT become `JG L` directly — it becomes `CMP x, 0` then `JG L`. Many students skip the `CMP` and the *operand load* and get -2 marks.\n> \n> 3. **Using one register for both load and accumulate.** In two-address style, `MOV R, [a]; ADD R, [b]` is correct only if `b` is loaded into `R` *temporarily* OR if the architecture permits memory operands in arithmetic ops. The KTU virtual machine expects `MOV R, [b]; ADD R, [c]` where `R` accumulates — but this requires the second operand to be in a register or immediate. **Always state the assumption clearly.**\n> \n> 4. **Not updating the address descriptor.** After `MOV AX, t1`, the address descriptor of `t1` must include `AX`. If the next instruction is `MOV BX, t1`, the naive generator reloads from memory. **Marks lost: 1 per occurrence.**\n> \n> 5. **Generating labels without colons.** `L1` in assembly is `L1:`, not `L1`. The colon is part of the syntax. **Marks lost: 0.5 per label, but accumulates.**\n> \n> 6. **Not computing next-use before code generation.** Without next-use, you cannot optimally choose which register to spill. The KTU rubric explicitly asks for \"next-use based\" allocation in Part B.\n> \n> 7. **Off-by-one in the conditional jump direction.** `if x < 0 goto L` should be `JL L`, NOT `JG L`. Always re-derive the jump condition from the TAC. **Marks lost: 2 per wrong direction.**\n\n## 5.4 Topic Recap & Important Things to Remember\n\n> [!NOTE]\n> **Rapid-Revision Checklist for KTU 2024 Systems Lab Exam:**\n\n### Definition Layer\n- [ ] **Back End** = Synthesis phase mapping TAC → Assembly.\n- [ ] **Three Address Code (TAC)** = IR with at most 3 operands per instruction.\n- [ ] **Assembly Language** = Mnemonic, human-readable machine instructions.\n\n### Algorithm Layer\n- [ ] The canonical algorithm uses **getReg** + **descriptors** + **next-use**.\n- [ ] The 3 data structures: **Address Descriptor**, **Register Descriptor**, **Next-Use Information**.\n- [ ] The order: Compute next-use → loop TAC → call getReg → emit instructions → update descriptors.\n\n### Instruction Pattern Layer\n- [ ] `x = y` → **1 instruction** (`MOV R, Ry` or `MOV R, [y]`).\n- [ ] `x = y op z` → **2 instructions** (`MOV R, [y]; OP R, [z]` or `MOV R, [y]; OP R, Rz`).\n- [ ] `goto L` → **1 instruction** (`JMP L`).\n- [ ] `if x relop y goto L` → **3 instructions** (`MOV R, [x]; CMP R, [y]; Jcond L`).\n\n### Theoretical Layer\n- [ ] **Sethi-Ullman labeling** gives the minimum register count for expression trees.\n- [ ] Linear chain: needs $n$ registers for $n$ leaves.\n- [ ] Balanced tree: needs $\\log_2 n + 1$ registers.\n- [ ] **Lower bound on code size** for binary expressions = $2n + 1$ (no spills).\n- [ ] **Spill cost** = 1 store + 1 load = 2 memory instructions.\n\n### Practical / Lab Layer\n- [ ] TAC can be supplied as text quadruples in a `.tac` file.\n- [ ] Assembly output goes to a `.asm` file consumed by the student's own assembler.\n- [ ] For the KTU lab, a register set of **{AX, BX, CX, DX}** (4 registers) is typical.\n- [ ] Labels in TAC map 1-to-1 to labels in assembly (with `:` suffix).\n- [ ] The `HALT` or `END` pseudo-op terminates the program.\n\n### Key Mnemonics to Memorize\n- [ ] `MOV`, `ADD`, `SUB`, `MUL`, `DIV`, `CMP`, `JMP`, `JE`, `JNE`, `JG`, `JL`, `JGE`, `JLE`.\n- [ ] Three-address vs two-address distinction: TAC is *three-address*, most CPUs are *two-address*.\n\n### Common Pitfalls\n- [ ] Always state the **register set** you're using in your answer.\n- [ ] Always show **next-use** computation explicitly in your answer.\n- [ ] Always emit **operand loads** explicitly; do not assume operands are in registers.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-engineering-7-pccsl607-mExperiment 2-t12",
          "title": "Instantiation of VMs with image file using open-source hypervisors or public cloud platforms",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL607 - SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Instantiation of VMs with image file using open-source hypervisors or public cloud platforms\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition\n\n> [!IMPORTANT]\n> **VM Instantiation** is the process of creating, configuring, and booting a **Virtual Machine (VM)** — a software-emulated computer system — from a pre-packaged **disk image file** (ISO, VDI, VMDK, QCOW2, AMI) using a **hypervisor** (a virtualization layer) that may run locally on bare-metal hardware (Type 1) or on top of a host operating system (Type 2), OR a remote **public cloud IaaS platform** (AWS EC2, GCP Compute Engine, Azure VMs).\n\n**Key Terminology (KTU 2024 Glossary):**\n\n| Term | Definition |\n|------|------------|\n| **Hypervisor** | Software that creates, runs, and manages virtual machines (also called VMM – Virtual Machine Monitor). |\n| **Image File** | A snapshot/template of an OS + pre-installed software used to bootstrap a VM (e.g., `.iso`, `.vdi`, `.vmdk`, `.qcow2`, `.ami`). |\n| **vCPU** | A virtual central processing unit assigned to a VM (a time-share slice of a physical core). |\n| **IaaS** | Infrastructure-as-a-Service — a cloud model where the user provisions VMs over the internet. |\n| **Guest OS** | The operating system running inside the VM. |\n| **Host OS / Host Hardware** | The underlying physical machine/operating system hosting the hypervisor. |\n\n### 1.2 Conceptual Analogy — The \"Apartment Building\" Model\n\n> [!NOTE]\n> **Think of a single physical computer as a piece of LAND, and each Virtual Machine as an APARTMENT BUILDING constructed on that land.**\n>\n> - The **hypervisor** is the **construction engineer + property manager** who decides how many apartments to build, how much space each gets, and how electricity/water (CPU/RAM/Network) is shared.\n> - The **image file** (ISO/VDI/AMI) is the **blueprint + pre-furnished interior design** from which each apartment is cloned.\n> - The **physical server** is the **land** — a fixed resource (finite land area).\n> - **Public cloud platforms** (AWS, Azure, GCP) are **real-estate companies** that own massive land banks across the globe and rent you an apartment on-demand, billed per hour.\n> - **Type 1 hypervisor (bare-metal)** = The engineer builds directly on the land (no middleman). Used in data centres.\n> - **Type 2 hypervisor (hosted)** = The engineer builds on top of an already-existing house (the host OS). Used by students/labs.\n\n### 1.3 Why This Experiment Matters in KTU 2024\n\nThe 2024 Scheme Outcomes for **PCCSL607 – Systems Lab** list the following expected competencies:\n- **CO1:** Set up and configure virtualized environments.\n- **CO2:** Deploy services on open-source and cloud platforms.\n- **CO3:** Analyse performance and resource trade-offs in VM-based systems.\n\nThis experiment directly tests **CO1 and CO2**.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Anatomy of a Virtual Machine\n\nA VM consists of **four logical layers** that must all be allocated during instantiation:\n\n1. **Compute layer** → vCPUs (mapped to physical cores via time-slicing).\n2. **Memory layer** → Virtual RAM (mapped to physical RAM via shadow paging or EPT/NPT).\n3. **Storage layer** → Virtual disks (mapped to image files or block devices).\n4. **Network layer** → Virtual NICs (connected to virtual switches / bridges).\n\n> [!IMPORTANT]\n> Every VM instantiation MUST define these four resources explicitly. The image file only provides the *software payload* — the *hardware envelope* must be specified separately.\n\n### 2.2 Image File Formats — The \"Blueprint Library\"\n\n| Format | Used By | Key Property |\n|--------|---------|--------------|\n| `.iso` | Universal (CD/DVD boot image) | Read-only, used to **install** an OS. |\n| `.vdi` | Oracle VirtualBox | VirtualBox Disk Image; supports snapshots. |\n| `.vmdk` | VMware (Workstation/ESXi) | Industry standard; cloud-portable. |\n| `.qcow2` | QEMU / KVM | Copy-on-write; efficient snapshots. |\n| `.vhd` / `.vhdx` | Microsoft Hyper-V | Azure-compatible disk format. |\n| `.ami` | AWS EC2 | Amazon Machine Image — snapshot in S3 + launch perms. |\n| `.raw` | Low-level hypervisors | Bit-for-bit image; no metadata overhead. |\n\n### 2.3 Hypervisor Classification\n\n> [!NOTE]\n> **Type 1 (Bare-Metal)** — Runs directly on hardware. **Performance: HIGH.** Examples: **KVM, VMware ESXi, Microsoft Hyper-V, Xen, Proxmox VE**.\n>\n> **Type 2 (Hosted)** — Runs as an application on a host OS. **Performance: MODERATE.** Examples: **Oracle VirtualBox, VMware Workstation, Parallels Desktop, QEMU (in user mode)**.\n\n### 2.4 KTU Formula Sheet — Resource & Network Calculations\n\n> [!IMPORTANT]\n> Memorize the following formulas — they appear in Part-B numerical sub-questions.\n\n| # | Concept | Formula | Units | Notes |\n|---|---------|---------|-------|-------|\n| 1 | vCPU to Core Ratio | $N_{vCPU} \\le R_{oc} \\times C_{phys}$ | dimensionless | $R_{oc}$ = overcommit ratio (typically **2:1 to 4:1**) |\n| 2 | Available Hosts in Subnet | $H_{usable} = 2^{32 - p} - 2$ | hosts | $p$ = CIDR prefix; subtract **network + broadcast** |\n| 3 | Subnet Mask Dotted Form | $M = (2^{32} - 2^{32-p})$ | decimal IPv4 | Convert hex groups back to dotted decimal |\n| 4 | Thin-Provisioned Used Space | $S_{used} = \\sum_{i=1}^{n} B_i$ | GB | Only the *written* blocks count |\n| 5 | Thick-Provisioned Disk Size | $S_{thick} = S_{allocated}$ | GB | Entire disk reserved upfront |\n| 6 | Hypervisor Memory Overhead | $M_{host} = M_{phys} - \\sum_{j=1}^{k} M_{vm,j} - M_{hypervisor}$ | GB | $\\approx$ **512 MB to 2 GB** for hypervisor itself |\n| 7 | Network Throughput Estimate | $T_{vm} \\le \\frac{BW_{link}}{N_{concurrent\\_vms}}$ | Mbps | Best-case (no contention) |\n\n> [!WARNING]\n> **Absolute value notation in tables:** Use $\\vert x \\vert$ — never the bare pipe character `|` — to avoid breaking markdown table parsers.\n\n### 2.5 Public Cloud IaaS — The \"On-Demand Real Estate\" Layer\n\nA **public cloud platform** (AWS, Azure, GCP, DigitalOcean, Linode) provides VM instantiation as a managed service:\n\n- **User submits a request** → \"Launch 1 × `t2.medium` Ubuntu 22.04 VM in Mumbai region.\"\n- **Cloud control-plane** → finds a physical host with capacity, copies the **AMI** (image) to local storage, attaches ephemeral vCPUs/RAM, and powers on the VM.\n- **User is billed** per second/minute of uptime.\n\nThis is **VM-as-a-Service** and is the industry-standard replacement for manual hypervisor management.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n> [!NOTE]\n> Since this is a **Lab course**, the \"derivations\" below are **resource-calculation derivations and step-by-step instantiation procedures** that examiners frequently test in Part-B questions.\n\n### 3.1 Derivation A — Number of Usable Hosts in a Subnet (for VM Network Planning)\n\n**Problem:** You want to provision **50 VMs** on a private network inside a public cloud VPC. Choose the smallest CIDR block that fits.\n\n**Step 1** — State the constraint.\n\n$$H_{usable} \\ge 50$$\n\n**Step 2** — Apply the usable-hosts formula.\n\n$$2^{32 - p} - 2 \\ge 50$$\n\n**Step 3** — Rearrange to isolate the exponent.\n\n$$2^{32 - p} \\ge 52$$\n\n**Step 4** — Solve by trying successive values of $p$ (smaller $p$ = larger block).\n\n$$\\begin{aligned}\np = 26 &\\;\\Rightarrow\\; 2^{6} - 2 = 62 \\text{ hosts} \\quad \\checkmark \\\\\np = 27 &\\;\\Rightarrow\\; 2^{5} - 2 = 30 \\text{ hosts} \\quad \\times\n\\end{aligned}$$\n\n**Step 5** — Conclude. The **smallest** CIDR block is `/26`, giving **62 usable IPs** (more than enough for 50 VMs, with 12 spare for gateway, future growth, and broadcast).\n\n**Step 6** — Derive the subnet mask for `/26`:\n\n$$\\begin{aligned}\nM &= (2^{32} - 2^{32-26}) \\\\\n&= 2^{32} - 2^{6} \\\\\n&= 4294967296 - 64 \\\\\n&= 4294967232\n\\end{aligned}$$\n\nConverted to dotted-decimal → **255.255.255.192**.\n\n### 3.2 Derivation B — vCPU Overcommit on a Hypervisor\n\n**Problem:** A KVM host has **2 physical cores (with hyperthreading = 4 logical cores)** and runs an overcommit ratio of **3:1**. How many vCPUs can be allocated in total?\n\n**Step 1** — Identify inputs.\n\n$$C_{phys} = 4, \\quad R_{oc} = 3$$\n\n**Step 2** — Apply the vCPU formula.\n\n$$\\begin{aligned}\nN_{vCPU} &= R_{oc} \\times C_{phys} \\\\\n&= 3 \\times 4 \\\\\n&= 12 \\text{ vCPUs}\n\\end{aligned}$$\n\n**Step 3** — Validate. Under sustained 100% load, each vCPU gets $\\frac{1}{3}$ of a physical thread — acceptable for light workloads, but disastrous for CPU-bound production. Hence the **3:1 ratio is typical for development/lab clusters only**; production uses 1:1 or 1.5:1.\n\n### 3.3 Derivation C — Storage Provisioning (Thin vs Thick)\n\n**Problem:** A VirtualBox VM is allocated a **50 GB** virtual disk. If thin-provisioned, what is the *initial* on-host consumption?\n\n**Step 1** — Thick provisioning.\n\n$$S_{used}^{thick} = S_{allocated} = 50 \\text{ GB} \\text{ (reserved immediately)}$$\n\n**Step 2** — Thin provisioning. Only the metadata + first block are written initially.\n\n$$S_{used}^{thin} \\approx S_{metadata} = 0 \\text{ (logically)}, \\approx 2 \\text{ MB physically}$$\n\n**Step 3** — Growth. As the guest writes data, $S_{used}^{thin}$ grows monotonically:\n\n$$S_{used}^{thin}(t) = S_{used}^{thin}(t-1) + \\Delta B(t), \\quad \\text{until } S_{used}^{thin} = S_{allocated}$$\n\n### 3.4 Analytical Matrix — Choosing the Right Platform\n\n| Criterion | Type-1 Hypervisor (e.g., KVM) | Type-2 Hypervisor (e.g., VirtualBox) | Public Cloud IaaS (e.g., AWS) |\n|-----------|-------------------------------|--------------------------------------|------------------------------|\n| **Skill Required** | High (Linux, CLI) | Low (GUI) | Low–Medium (Console) |\n| **Cost** | Hardware only | Free software | Pay-as-you-go (per hour) |\n| **Scalability** | Limited by hardware | Limited by host | Virtually unlimited |\n| **Isolation** | Strong | Weaker (hosted) | Very strong (multi-tenant) |\n| **Snapshot/Clone** | Native (qcow2) | Native (VDI) | Native (AMI/EBS snapshot) |\n| **Internet Exposure** | Manual (port-forward) | Manual (NAT) | One-click (Elastic IP) |\n| **Best Use-Case** | Production data centre | Student labs, dev | Production web services, HPC |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Mermaid Diagram — Type 1 vs Type 2 Hypervisor Architecture\n\n```mermaid\nflowchart TB\n    subgraph type1[\"TYPE 1 BARE-METAL HYPERVISOR\"]\n        hw1[\"Physical Hardware: CPU, RAM, Disk, NIC\"]\n        hyp1[\"Hypervisor Layer: KVM / ESXi / Hyper-V / Xen\"]\n        vm1a[\"VM 1: Linux Guest\"]\n        vm1b[\"VM 2: Windows Guest\"]\n        vm1c[\"VM 3: BSD Guest\"]\n        hw1 --> hyp1\n        hyp1 --> vm1a\n        hyp1 --> vm1b\n        hyp1 --> vm1c\n    end\n\n    subgraph type2[\"TYPE 2 HOSTED HYPERVISOR\"]\n        hw2[\"Physical Hardware\"]\n        host2[\"Host Operating System: Windows / Linux / macOS\"]\n        app2[\"Hypervisor Application: VirtualBox / VMware Workstation\"]\n        vm2a[\"VM 1: Ubuntu\"]\n        vm2b[\"VM 2: Kali Linux\"]\n        hw2 --> host2\n        host2 --> app2\n        app2 --> vm2a\n        app2 --> vm2b\n    end\n\n    classDef hw fill:#fde2e2,stroke:#b91c1c,color:#7f1d1d\n    classDef hyper fill:#fef3c7,stroke:#b45309,color:#78350f\n    classDef vm fill:#dcfce7,stroke:#15803d,color:#14532d\n    classDef host fill:#dbeafe,stroke:#1d4ed8,color:#1e3a8a\n\n    class hw1,hw2 hw\n    class hyp1,app2 hyper\n    class vm1a,vm1b,vm1c,vm2a,vm2b vm\n    class host2 host\n```\n\n### 4.2 Mermaid Diagram — VM Instantiation Workflow (Local Hypervisor)\n\n```mermaid\nflowchart LR\n    A[\"Step 1: Download ISO Image\"] --> B[\"Step 2: Launch Hypervisor GUI / CLI\"]\n    B --> C[\"Step 3: Click New VM\"]\n    C --> D[\"Step 4: Allocate Resources: RAM, vCPU, Disk\"]\n    D --> E[\"Step 5: Mount ISO as Virtual CD-ROM\"]\n    E --> F[\"Step 6: Configure Network: NAT / Bridged / Host-Only\"]\n    F --> G[\"Step 7: Power On VM\"]\n    G --> H[\"Step 8: OS Installer Runs from ISO\"]\n    H --> I[\"Step 9: Install Guest Additions / Tools\"]\n    I --> J[\"Step 10: VM Ready for Use\"]\n\n    classDef step fill:#e0f2fe,stroke:#075985,color:#0c4a6e\n    classDef final fill:#bbf7d0,stroke:#166534,color:#14532d\n\n    class A,B,C,D,E,F,G,H,I step\n    class J final\n```\n\n### 4.3 Mermaid Diagram — Public Cloud VM Provisioning Pipeline\n\n```mermaid\nflowchart TD\n    user[\"User / DevOps Engineer\"] --> console[\"Cloud Console / CLI: aws ec2 run-instances\"]\n    console --> api[\"Cloud API Endpoint: REST / SDK\"]\n    api --> cp[\"Cloud Control Plane\"]\n    cp --> scheduler[\"Scheduler: Find physical host with capacity\"]\n    scheduler --> imgsrv[\"Image Service: Fetch AMI / Snapshot\"]\n    imgsrv --> copy[\"Copy Image to Host's Local Disk\"]\n    copy --> net[\"Allocate VPC Subnet, Security Group, Elastic IP\"]\n    net --> boot[\"Power On VM: Hypervisor boots the guest\"]\n    boot --> ready[\"VM Reaches Running State\"]\n    ready --> bill[\"Metering: Start billing per second\"]\n\n    classDef userNode fill:#fef9c3,stroke:#854d0e,color:#713f12\n    classDef cloudNode fill:#ddd6fe,stroke:#5b21b6,color:#4c1d95\n    classDef actionNode fill:#cffafe,stroke:#0e7490,color:#164e63\n\n    class user userNode\n    class api,cp,scheduler,imgsrv,net,boot,bill cloudNode\n    class console,copy,ready actionNode\n```\n\n### 4.4 Mermaid Diagram — Image File Lifecycle\n\n```mermaid\nstateDiagram-v2\n    [*] --> Downloaded: Pull .iso / .ami from repository\n    Downloaded --> Mounted: Attach to VM as virtual optical drive\n    Mounted --> Installing: OS installer reads from image\n    Installing --> Installed: Files written to virtual disk\n    Installed --> Snapshot: Save current state as new image\n    Snapshot --> Cloned: Spawn N child VMs from snapshot\n    Cloned --> Running: Power on\n    Running --> [*]\n    Snapshot --> [*]: Stored as golden image\n\n    note right of Mounted: ISO images are read-only.\n    note right of Snapshot: qcow2 / VDI / AMI support snapshots natively.\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **`[KTU University Exam - Model Question, 2024 Scheme]`**\n\n**Q1.** Differentiate between **Type 1** and **Type 2** hypervisors. Give two examples of each. *(3 marks)*\n\n**Model Answer:**\n\n| Feature | Type 1 (Bare-Metal) | Type 2 (Hosted) |\n|---------|---------------------|-----------------|\n| **Position in Stack** | Runs directly on hardware | Runs on a host OS |\n| **Performance** | Higher (no host-OS overhead) | Lower (host-OS overhead) |\n| **Examples** | KVM, VMware ESXi, Microsoft Hyper-V, Xen | Oracle VirtualBox, VMware Workstation, Parallels |\n| **Typical Use** | Data centres, production servers | Labs, developer workstations |\n\n---\n\n**Q2.** What is an **image file** in the context of VM instantiation? List three common image file formats with the hypervisor that uses each. *(3 marks)*\n\n**Model Answer:**\nAn image file is a **pre-packaged snapshot** of an operating system (and optionally pre-installed software) used as a **boot/install template** for virtual machines. It removes the need to manually install an OS from scratch.\n- `.iso` → Universal / used by all hypervisors for OS installation.\n- `.vdi` → Oracle VirtualBox.\n- `.qcow2` → QEMU / KVM.\n- `.vmdk` → VMware.\n- `.ami` → AWS EC2.\n\n---\n\n### 5.2 Part B — Long Answer Questions (14 Marks Each, Internal Choice)\n\n> **`[KTU University Exam - Model Question, 2024 Scheme]`**\n\n---\n\n#### **Question A (14 Marks)**\n\n**(a)** With the help of a neat diagram, explain the **step-by-step procedure to instantiate a VM** using **Oracle VirtualBox** with an **Ubuntu 22.04 ISO** as the image file. Mention the key configurations (RAM, vCPU, disk, network mode). *(7 marks)*\n\n**Model Answer — Step-by-Step Procedure:**\n\n1. **Download the ISO** from `https://releases.ubuntu.com/22.04/`.\n2. **Open VirtualBox** → click **Machine → New**.\n3. **Name and Type:** Set name = \"Ubuntu-VM-1\", Type = Linux, Version = Ubuntu (64-bit).\n4. **Memory Allocation:** Allocate **2048 MB to 4096 MB** of RAM (RAM size must be ≤ host's free RAM).\n5. **Virtual Hard Disk:** Choose **\"Create a virtual hard disk now\"** → format = **VDI** → storage type = **Dynamically allocated** (thin provisioning) → size = **25 GB**.\n6. **Mount the ISO:** Select the created VM → **Settings → Storage → Empty optical drive** → click the disk icon → **Choose a disk file** → select the Ubuntu `.iso`.\n7. **Network Configuration:** **Settings → Network → Adapter 1** → enable → attached to = **NAT** (for internet access) or **Bridged Adapter** (for LAN access).\n8. **Display & System Tweaks:** Enable **EFI** if required, set video memory to **128 MB**, enable **3D acceleration** (optional).\n9. **Start the VM** → Ubuntu installer boots from the ISO → follow the graphical install wizard → partition the virtual disk, set username/password.\n10. **Install Guest Additions** (post-install): **Devices → Insert Guest Additions CD image** → run `VBoxLinuxAdditions.run` inside the guest to enable shared folders, better graphics, and clipboard sync.\n11. **Restart the VM** → remove the ISO mount (else it boots back into installer) → VM is now fully operational.\n\n> **Diagram:** Refer to Section 4.2 above (VM Instantiation Workflow).\n\n**Key configurations to mention in the answer:**\n\n| Resource | Recommended Value | Reasoning |\n|----------|-------------------|-----------|\n| RAM | 2–4 GB | Ubuntu desktop needs ≥ 2 GB to run smoothly |\n| vCPU | 2 cores | Allows multitasking inside guest |\n| Disk | 25 GB (thin) | Ubuntu + apps fit comfortably |\n| Network | NAT | Easiest for internet in a lab |\n| Optical drive | Ubuntu ISO | Boot source |\n\n---\n\n**(b)** Your team needs to provision **50 VMs** in a private cloud subnet. Calculate the **smallest CIDR block** that fits, derive the **subnet mask**, and design a **resource plan** (vCPU, RAM) for the host hypervisor assuming 3:1 overcommit and 4 GB RAM per VM. *(7 marks)*\n\n**Model Answer:**\n\n**Step 1 — Subnet sizing.**\n\n$$\\begin{aligned}\nH_{usable} &\\ge 50 \\\\\n2^{32 - p} - 2 &\\ge 50 \\\\\n\\text{Try } p = 26 &\\;\\Rightarrow\\; 2^{6} - 2 = 62 \\text{ hosts} \\quad \\checkmark\n\\end{aligned}$$\n\n**Smallest CIDR = `/26` (62 usable hosts).**\n\n**Step 2 — Subnet mask derivation.**\n\n$$M = 2^{32} - 2^{32-26} = 2^{32} - 2^{6} = 4294967296 - 64 = 4294967232$$\n\nConvert to dotted-decimal → **255.255.255.192**.\n\n**Step 3 — Total vCPU requirement.**\n\n$$N_{vCPU}^{total} = 50 \\times 2 = 100 \\text{ vCPUs (assuming 2 vCPU per VM)}$$\n\n**Step 4 — Total RAM requirement.**\n\n$$M_{vm}^{total} = 50 \\times 4 \\text{ GB} = 200 \\text{ GB}$$\n\n**Step 5 — Hypervisor host sizing (3:1 overcommit, 4 physical cores = 8 logical with HT).**\n\n$$C_{phys} = 8, \\quad R_{oc} = 3$$\n\n$$N_{vCPU}^{host} = 3 \\times 8 = 24 \\text{ vCPUs}$$\n\nBut we need 100 vCPUs → we need more hosts:\n\n$$N_{hosts} \\ge \\frac{100}{24} \\approx 4.17 \\;\\Rightarrow\\; \\textbf{5 hypervisor hosts minimum}$$\n\n**Step 6 — RAM per host (assuming 256 GB RAM per server, 4 GB hypervisor overhead).**\n\n$$M_{per\\_host} = 256 - 4 = 252 \\text{ GB usable}$$\n\nVMs per host: $\\lfloor 252 / 4 \\rfloor = 63$ VMs per host.\n\n**Final plan:** **2 hypervisor hosts × 256 GB RAM × 8 cores each** comfortably hold 50 VMs (10 spare capacity on each host).\n\n---\n\n#### **Question B (14 Marks) — Alternative Choice**\n\n**(a)** Explain how to **instantiate a VM on AWS EC2** using an **Amazon Machine Image (AMI)**. List the essential parameters (instance type, AMI ID, region, security group, key pair) and the typical CLI command. *(7 marks)*\n\n**Model Answer:**\n\n1. **Sign in to AWS Console** → navigate to **EC2 Dashboard**.\n2. **Choose an AMI** → AWS provides pre-built AMIs (Ubuntu, Amazon Linux, Windows) under **Quick Start**, or you can use **Community AMIs**, **Marketplace AMIs**, or your **own custom AMI** (from a snapshot).\n3. **Choose an Instance Type** → e.g., `t2.micro` (1 vCPU, 1 GB RAM — free tier eligible) or `t3.medium` (2 vCPU, 4 GB RAM).\n4. **Configure Instance Details:**\n   - Number of instances.\n   - Network → choose or create a **VPC** and **Subnet**.\n   - IAM role (optional).\n   - Shutdown behaviour → Stop / Terminate.\n5. **Add Storage** → default 8 GB gp3 EBS root volume (modifiable).\n6. **Add Tags** → e.g., `Name = WebServer-1`.\n7. **Configure Security Group** → acts as a virtual firewall. Open port **22 (SSH)** for Linux, port **80/443** for web.\n8. **Review and Launch** → select or create a **Key Pair** (`.pem` file) for SSH access.\n9. **Launch** → the control plane schedules the VM, copies the AMI to a host, and starts the instance.\n\n**Equivalent AWS CLI command:**\n\n```bash\naws ec2 run-instances \\\n  --image-id ami-0abcdef1234567890 \\\n  --count 1 \\\n  --instance-type t2.micro \\\n  --key-name MyKeyPair \\\n  --security-group-ids sg-0123456789abcdef0 \\\n  --subnet-id subnet-0123456789abcdef0 \\\n  --region ap-south-1\n```\n\n**Key AWS-specific terms:**\n\n| Term | Meaning |\n|------|---------|\n| **AMI** | Amazon Machine Image — equivalent of an ISO/disk image |\n| **Instance Type** | Pre-defined hardware bundle (vCPU + RAM) |\n| **EBS** | Elastic Block Store — persistent virtual disk |\n| **Security Group** | Stateful virtual firewall (allow/deny rules) |\n| **Key Pair** | SSH public key for secure login |\n| **Elastic IP** | Static public IPv4 address |\n\n---\n\n**(b)** Compare **open-source hypervisors (VirtualBox, KVM, Proxmox)** with **public cloud platforms (AWS EC2, Azure VMs, GCP Compute Engine)** for VM instantiation. Discuss **cost, scalability, control, and learning value**. *(7 marks)*\n\n**Model Answer:**\n\n| Criterion | Open-Source Hypervisor | Public Cloud IaaS |\n|-----------|------------------------|-------------------|\n| **Upfront Cost** | Free (software) + hardware purchase | Pay-per-second; no hardware |\n| **Operational Cost** | Electricity, cooling, sysadmin salary | Billed compute-hours, storage GB, data transfer |\n| **Scalability** | Limited by physical hardware | Elastic — add instances in minutes |\n| **Control** | Total (bare-metal access, custom kernel modules) | Limited to cloud-provider APIs |\n| **Maintenance** | Manual patching, hardware replacement | Provider handles all maintenance |\n| **Learning Value** | Deep understanding of virtualization internals | Focus on cloud-architecture and orchestration |\n| **Internet Exposure** | Manual port-forwarding, firewall rules | One-click Elastic IP, load balancer |\n| **Best For** | KTU labs, learning, air-gapped environments | Production web services, big data, HPC |\n\n> **Conclusion:** KTU 2024 students should master **both**: hypervisors for foundational understanding and labs, and cloud platforms for industry-relevant skills. They are **complementary**, not competing.\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning\n\n> [!WARNING]\n> **Common Mark-Deduction Pitfalls:**\n> 1. **Confusing image file types** — Mixing up `.iso` (installer) with `.vdi`/`.qcow2` (virtual disk) loses 1 mark.\n> 2. **Forgetting to subtract 2** from $2^{32-p}$ in subnet calculations (network + broadcast addresses are NOT usable) — loses 1 mark.\n> 3. **Not specifying units** in answers (e.g., \"50\" instead of \"50 GB\" or \"50 vCPUs\") — partial deduction.\n> 4. **Skipping the diagram** in 7-mark procedural questions — KTU examiners expect at least one flow/diagram for full marks.\n> 5. **Writing \"hypervisor = emulator\"** — incorrect. A hypervisor uses hardware-assisted virtualization (Intel VT-x / AMD-V); an emulator interprets every instruction (e.g., QEMU-user mode is emulation, KVM+QEMU is virtualization).\n> 6. **In cloud questions, not mentioning the region/AMI ID/key pair** — these are mandatory parameters for full credit.\n\n---\n\n### 5.4 Topic Recap & Important Things to Remember\n\n> [!NOTE]\n> **Rapid-Revision Checklist (Print this before exam):**\n\n- [x] **VM** = software emulation of a physical computer; runs a **guest OS** on a **hypervisor**.\n- [x] **Type 1 (bare-metal)** = KVM, ESXi, Hyper-V, Xen → high performance, data-centre use.\n- [x] **Type 2 (hosted)** = VirtualBox, VMware Workstation → lab/student use, lower performance.\n- [x] **Image files**: `.iso` (boot installer), `.vdi` (VirtualBox), `.vmdk` (VMware), `.qcow2` (KVM), `.ami` (AWS), `.vhd/.vhdx` (Hyper-V/Azure).\n- [x] **Thin provisioning** = disk grows as data is written (saves space); **Thick provisioning** = entire disk reserved upfront (predictable performance).\n- [x] **vCPU overcommit ratio** typically 2:1 to 4:1 in dev/lab; **1:1 in production**.\n- [x] **Usable hosts in subnet** = $2^{32-p} - 2$ (always subtract 2 for network + broadcast).\n- [x] **AWS EC2 key parameters**: AMI ID, instance type, region, subnet, security group, key pair.\n- [x] **Public cloud advantage**: instant elasticity, no hardware, pay-per-use → industry standard.\n- [x] **Open-source advantage**: full control, zero licensing cost, ideal for KTU labs.\n- [x] **Procedure order**: Download ISO → Create VM → Allocate HW resources → Mount ISO → Configure Network → Power On → Install OS → Install guest additions/tools.\n\n> **One-line takeaway:** *VM instantiation is the act of packaging hardware (CPU/RAM/Disk/NIC) around a software blueprint (image file), executed by a hypervisor (local) or a cloud control-plane (remote) — the cornerstone of modern computing and every KTU 2024 lab evaluation.*\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-engineering-7-pccsl607-mExperiment 2-t13",
          "title": "Virtual machine Cluster set up using open-source hypervisors or public cloud platforms",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL607 - SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Virtual machine Cluster set up using open-source hypervisors or public cloud platforms\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\n> [!NOTE]\n> **Virtualization (KTU 2024 Definition):** *Virtualization* is the process of creating a software-based (virtual) representation of physical computing resources — including servers, storage devices, networks, and operating systems — abstracted from the underlying physical hardware through a specialized software layer known as a **hypervisor** (or Virtual Machine Monitor, VMM).\n\nA **Virtual Machine (VM) Cluster** is a tightly-coupled collection of two or more virtual machines (nodes) working together as a single logical compute unit, coordinated by clustering middleware, to deliver **High Availability (HA)**, **Load Balancing (LB)**, and **Horizontal Scalability** for distributed applications such as Hadoop, Kubernetes, Apache Spark, MPI workloads, or database replication.\n\nThe KTU 2024 Systems Lab syllabus categorizes this experiment under the **Compute Virtualization & Distributed Systems** domain, with direct mapping to **CO2: Design and implement virtualized environments and clustered infrastructures.**\n\n| Term | KTU-Standard Definition | Exam Frequency |\n|---|---|---|\n| **Hypervisor** | Software that creates and runs VMs; abstracts physical hardware | ★★★★★ |\n| **Type-1 Hypervisor** | Bare-metal hypervisor running directly on host hardware | ★★★★★ |\n| **Type-2 Hypervisor** | Hosted hypervisor running atop a conventional OS | ★★★★ |\n| **VM Cluster** | Group of VMs acting as one logical compute resource | ★★★★★ |\n| **Open-Source Hypervisor** | GPL/LGPL/Apache licensed virtualization platform | ★★★★ |\n| **Public Cloud IaaS** | On-demand VM provisioning over the public Internet | ★★★★ |\n\n## 1.2 Conceptual Analogy — The \"High-Rise Apartment Building\"\n\nImagine the **physical server** as a *plot of land*. Traditionally, you could only build **one house** (one OS) on that land.\n\nA **hypervisor** acts like a **municipal architect** who divides the same plot into multiple **apartment units (VMs)**. Each apartment:\n- Has its own **locked door** (isolation)\n- Pays its own **electricity bill** (resource quota — vCPU, RAM, Disk)\n- Hosts its own **family** (guest OS + applications)\n- Shares the same **foundation, water line, and electricity grid** (underlying hardware)\n\nNow, a **VM Cluster** is analogous to a **gated community of such apartment buildings** — multiple buildings (physical hosts), each containing apartments (VMs), all connected by internal roads (virtual networks) and a community management office (cluster manager like Proxmox/Corosync, K3s, or Hadoop YARN). The community ensures:\n- If one building's elevator fails (a node crashes), residents can be **shifted to another building** (live migration / failover)\n- A **shared water reservoir and generator** (shared storage and quorum disk) keeps services running\n- **Security guards** patrol the perimeter (firewall / SDN policies)\n\n> [!IMPORTANT]\n> **Why this experiment matters in KTU 2024:**\n> Cloud-native and DevOps roles demand hands-on skill with hypervisor deployment (KVM, VirtualBox, Proxmox, XCP-ng) and cluster orchestration. This experiment develops practical competency in **bare-metal vs. hosted virtualization, virtual networking, shared storage, and cluster quorum** — all examinable concepts in the End Semester Evaluation (ESE).\n\n## 1.3 Core Components Inventory\n\n### A. Open-Source Hypervisors (the \"Hypervisor Toolbox\")\n\n| Hypervisor | Type | License | Best Use Case | KTU Lab Verdict |\n|---|---|---|---|---|\n| **VirtualBox** | Type-2 | GPLv2 | Desktop VMs, lab demos | ✅ Easiest for B.Tech labs |\n| **KVM** | Type-1 (Linux kernel module) | GPLv2 | Production Linux servers | ✅ Industry-relevant |\n| **Proxmox VE** | Type-1 | AGPLv3 | All-in-one cluster (VM + LXC + Ceph) | ✅ Built-in clustering |\n| **XCP-ng** | Type-1 (fork of XenServer) | GPLv2 | Enterprise-grade Xen hypervisor | ✅ Free Citrix alternative |\n| **oVirt** | Type-1 | GPLv2 | Red Hat virtualization (RHV) | ✅ KVM-based management |\n| **Hyper-V** (community) | Type-1 | Commercial+Free | Windows shops | ⚠️ Limited KTU scope |\n| **VMware ESXi** | Type-1 | Commercial (free tier discontinued) | Enterprise | ⚠️ License issues in 2024 |\n\n### B. Public Cloud Platforms (Free-Tier Friendly)\n\n| Platform | Free Tier Offering | KTU 2024 Relevance |\n|---|---|---|\n| **AWS EC2** | t2.micro (1 vCPU, 1 GB RAM) for 12 months | ★★★★★ |\n| **Google Cloud Platform (GCE)** | e2-micro (shared core, 1 GB RAM) — always free | ★★★★★ |\n| **Microsoft Azure** | B1S (1 vCPU, 1 GB RAM) for 12 months | ★★★★ |\n| **Oracle Cloud** | 4 OCPUs + 24 GB RAM — *always free* | ★★★★ |\n| **IBM Cloud** | Lite plan with limited VMs | ★★★ |\n\n> [!TIP]\n> **KTU 2024 Examiner Insight:** The question paper commonly asks students to *compare Type-1 vs Type-2 hypervisors* and to *justify the choice* of an open-source platform for a given lab scenario. Memorize the comparison table — it appears in **almost every lab exam cycle**.\n\n## 1.4 The Three Pillars of a VM Cluster\n\n```\n                    ┌──────────────────────────────┐\n                    │   VIRTUAL MACHINE CLUSTER   │\n                    └──────────────┬───────────────┘\n                                   │\n            ┌──────────────────────┼──────────────────────┐\n            │                      │                      │\n       ┌────▼─────┐          ┌─────▼─────┐         ┌─────▼─────┐\n       │ COMPUTE  │          │  STORAGE  │         │  NETWORK  │\n       │  (vCPU,  │          │  (Shared  │         │ (Virtual  │\n       │   RAM)   │          │  NFS/iSCSI│         │  Bridge / │\n       │          │          │   /Ceph)  │         │  Overlay) │\n       └────┬─────┘          └─────┬─────┘         └─────┬─────┘\n            │                      │                      │\n            └──────────────────────┴──────────────────────┘\n                                   │\n                  HA + LB + Fault Tolerance + Scalability\n```\n\nEvery KTU cluster experiment must demonstrate these three pillars working together. The exam may present a \"design question\" where you must sketch how these three resources are allocated across the cluster nodes.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Operational Anatomy of a Hypervisor\n\nA hypervisor is not magic — it is a *resource arbitrator*. Its job is to translate **physical hardware events** into **virtual events** for each guest OS, while preventing any single guest from monopolizing the host.\n\n### Layered Functional Breakdown\n\n- **Step 1 — Hardware Discovery & Enumeration**\n  The hypervisor probes the CPU's virtualization extensions (`VT-x` on Intel, `AMD-V` on AMD) and exposes synthetic device abstractions to guests.\n\n- **Step 2 — VM Provisioning**\n  A VM is described by a *configuration file* (e.g., VirtualBox `.vbox`, KVM `.xml` via libvirt, Proxmox `qm.conf`). This file declares:\n  - vCPU count\n  - RAM allocation\n  - Virtual disk (qcow2, vmdk, vhd)\n  - Network interface (bridged, NAT, host-only, internal)\n\n- **Step 3 — Binary Translation / Hardware-Assisted Execution**\n  - **Software approach (early days):** Binary translation of privileged instructions.\n  - **Hardware-assisted (modern):** CPU ring de-privileging (Ring 0 → Ring -1 for hypervisor; Ring 3 for guests).\n\n- **Step 4 — Memory Virtualization (EPT / NPT / RVI)**\n  The hypervisor maintains a *two-level page table*: guest virtual → guest physical → host physical. Hardware assists (Intel EPT, AMD NPT, ARM RVI) make this near-native.\n\n- **Step 5 — I/O Virtualization**\n  Three modes exist: **emulation** (slowest), **paravirtualization** (PV, requires guest drivers — e.g., Xen PV, virtio), and **direct assignment / SR-IOV** (fastest, near-native).\n\n> [!IMPORTANT]\n> **The \"Why\" behind each step** — KTU examiners love asking *\"Why is a Type-1 hypervisor faster than a Type-2?\"* The answer is rooted in **Step 4 and Step 5**: Type-1 has no host-OS translation layer, so memory and I/O traps go directly to the hypervisor microkernel, eliminating the double context-switch overhead.\n\n## 2.2 Type-1 vs Type-2 Hypervisors — The Definitive Comparison\n\n| Feature | Type-1 (Bare-Metal) | Type-2 (Hosted) |\n|---|---|---|\n| **Installation** | Installs directly on hardware | Installs as application on host OS |\n| **Performance** | Near-native (≈ 95–98% of bare metal) | Reduced (≈ 80–90% of bare metal) |\n| **Examples** | KVM, Xen, Hyper-V, ESXi, Proxmox | VirtualBox, VMware Workstation, Parallels |\n| **Driver Model** | Requires its own hardware drivers | Uses host OS drivers |\n| **Use Case** | Production datacenters, clustering | Desktop dev, lab testing, education |\n| **Stability** | Higher (no host-OS crashes affect VMs) | Lower (host-OS crash = all VMs crash) |\n| **Memory Overhead** | Low (50–200 MB base) | Higher (full host OS + hypervisor layer) |\n\n## 2.3 The Operational Anatomy of a VM Cluster\n\nA cluster differs from a mere group of VMs by adding **three coordination services**:\n\n### 2.3.1 Cluster Membership & Quorum\n- **Problem:** In a 3-node cluster, if the network partitions (split-brain), each side might think it is the \"primary\" and write conflicting data.\n- **Solution:** A **quorum algorithm** (e.g., Raft, Paxos, Corosync's vote-quorum) requires a **majority of nodes** to agree on cluster state.\n- **Rule of thumb:** Always deploy an **odd number of nodes** (3, 5, 7) to break ties.\n\n### 2.3.2 High Availability (HA) Manager\n- The HA manager (e.g., Pacemaker + Corosync in Proxmox, Keepalived in Linux-HA) constantly **heartbeats** between nodes.\n- If a node fails to respond within `dead_ms` (typically 30 s), the manager **fences** the dead node (STONITH — *Shoot The Other Node In The Head*) and **migrates** its VMs to a healthy node.\n\n### 2.3.3 Shared Storage\n- All cluster nodes must access the **same VM disk images**, otherwise live migration is impossible.\n- Common shared storage backends:\n  - **NFS** (simplest, lab-friendly)\n  - **iSCSI** (block-level, near-native performance)\n  - **Ceph** (distributed, fault-tolerant, scales to petabytes)\n  - **GlusterFS** (user-space, replicated volumes)\n  - **DRBD** (block-level replication between two nodes)\n\n## 2.4 KTU High-Yield Formula Sheet — Capacity Planning\n\n> [!NOTE]\n> These formulas are essential for KTU 2024 exam problems that give you hardware specs and ask you to calculate VM density, overhead, or storage requirements.\n\n| # | Formula | Description | Units |\n|---|---|---|---|\n| 1 | $N_{vm} = \\left\\lfloor \\dfrac{RAM_{host} - RAM_{hypervisor}}{RAM_{vm} + RAM_{overhead}} \\right\\rfloor$ | Maximum VM density by RAM | VMs |\n| 2 | $N_{vm}^{cpu} = \\left\\lfloor \\dfrac{vCPU_{host} \\times T}{vCPU_{vm} \\times O_{ratio}} \\right\\rfloor$ | VM density by CPU cores | VMs |\n| 3 | $Storage_{total} = \\sum_{i=1}^{n} D_i \\times (1 + R_{prov})$ | Total provisioned storage | GB |\n| 4 | $R_{overhead} = R_{vm} \\times 0.05 + 32$ | Per-VM memory overhead (approx., MiB) | MiB |\n| 5 | $T_{failover} = T_{detect} + T_{fence} + T_{boot}$ | Total HA failover time | seconds |\n| 6 | $U_{cluster} = 1 - \\prod_{i=1}^{n} (1 - U_i)$ | Cluster availability (serial) | 0–1 |\n| 7 | $T_{migration} = \\dfrac{M_{vm}}{B_{network}} + T_{sync}$ | Live migration time | seconds |\n| 8 | $C_{qos} = \\dfrac{BW_{allocated}}{BW_{physical}} \\times 100$ | Network QoS percentage | % |\n\n> **Key to variables (used above):**\n> - $RAM_{host}$ = physical RAM of the host\n> - $RAM_{hypervisor}$ = memory reserved for hypervisor OS (e.g., **2 GB** for Proxmox base, **512 MB** for KVM)\n> - $RAM_{overhead}$ = per-VM overhead (typically **32–128 MiB** for KVM, **64 MiB** for VirtualBox)\n> - $T$ = hyperthreading multiplier (typically **1.5–2.0**)\n> - $O_{ratio}$ = CPU oversubscription ratio (typically **4:1 to 8:1** for general workloads)\n> - $R_{prov}$ = storage provisioning reserve (typically **0.20** = 20%)\n> - $T_{detect}$ = heartbeat detection time (typically **10–30 s**)\n> - $T_{fence}$ = STONITH time (typically **5–10 s**)\n> - $T_{boot}$ = guest OS boot time (typically **20–60 s**)\n> - $M_{vm}$ = memory footprint of the migrating VM (in MB)\n> - $B_{network}$ = available network bandwidth for migration (in MB/s)\n\n## 2.5 Real-World Engineering Utility\n\n- **Cloud Datacenters:** AWS, GCP, Azure operate on **Type-1 hypervisors** (KVM/Xen) — every \"EC2 instance\" is a guest VM.\n- **High-Performance Computing (HPC):** VM clusters enable **MPI-based parallel computing** with rapid provisioning.\n- **DevOps / CI/CD:** Jenkins agents run on VM clusters; **Kubernetes nodes** are themselves VMs.\n- **Disaster Recovery:** VM clusters enable **cross-site replication** and **near-zero RTO (Recovery Time Objective)**.\n- **Education & Labs (KTU context):** Students can deploy **Hadoop, Spark, K3s** clusters entirely on local VMs for zero cost.\n\n> [!TIP]\n> **Engineering utility callout:** In production, the formula $U_{cluster} = 1 - \\prod (1 - U_i)$ is *vital*. If a single VM has availability 99.9% (3 nines), a 2-node HA cluster gives $1 - (0.001)^2 = 0.999999$ — six nines, the gold standard for Tier-IV datacenters.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Worked Capacity-Planning Problem (Memory-Based VM Density)\n\n### **Problem Statement (KTU-style):**\nA lab server has **32 GB RAM** and **8 physical cores** with hyperthreading. The host OS / hypervisor reserves **2 GB RAM**. Each VM requires **4 GB RAM** and **2 vCPUs**. Per-VM memory overhead is **64 MiB**. Calculate the **maximum number of VMs** that can be deployed simultaneously.\n\n### **Step 1 — Identify the limiting resource**\n\n$$\nRAM_{usable} = RAM_{host} - RAM_{hypervisor}\n$$\n\n$$\nRAM_{usable} = 32\\,\\text{GB} - 2\\,\\text{GB} = 30\\,\\text{GB}\n$$\n\n### **Step 2 — Compute per-VM RAM requirement (including overhead)**\n\n$$\nRAM_{vm}^{total} = RAM_{vm} + RAM_{overhead}\n$$\n\n$$\nRAM_{vm}^{total} = 4\\,\\text{GB} + \\dfrac{64}{1024}\\,\\text{GB} = 4\\,\\text{GB} + 0.0625\\,\\text{GB} = 4.0625\\,\\text{GB}\n$$\n\n### **Step 3 — Compute RAM-limited VM count**\n\n$$\nN_{vm}^{ram} = \\left\\lfloor \\dfrac{RAM_{usable}}{RAM_{vm}^{total}} \\right\\rfloor\n$$\n\n$$\nN_{vm}^{ram} = \\left\\lfloor \\dfrac{30}{4.0625} \\right\\rfloor = \\left\\lfloor 7.384 \\right\\rfloor = 7\\,\\text{VMs}\n$$\n\n### **Step 4 — Compute CPU-limited VM count**\n\nApply formula #2 from the KTU Formula Sheet. With hyperthreading multiplier $T = 1.5$ and oversubscription ratio $O_{ratio} = 4$:\n\n$$\nvCPU_{host}^{eff} = vCPU_{host} \\times T = 8 \\times 1.5 = 12\\,\\text{vCPUs}\n$$\n\n$$\nN_{vm}^{cpu} = \\left\\lfloor \\dfrac{vCPU_{host}^{eff}}{vCPU_{vm} \\times O_{ratio}} \\right\\rfloor\n$$\n\n$$\nN_{vm}^{cpu} = \\left\\lfloor \\dfrac{12}{2 \\times 4} \\right\\rfloor = \\left\\lfloor 1.5 \\right\\rfloor = 1\\,\\text{VM}\n$$\n\n### **Step 5 — Identify the binding constraint**\n\nThe system can host $N_{vm}^{ram} = 7$ VMs from a memory standpoint but only $N_{vm}^{cpu} = 1$ VM from a CPU standpoint. The **binding constraint is the CPU**.\n\n$$\nN_{vm}^{max} = \\min(N_{vm}^{ram},\\, N_{vm}^{cpu}) = \\min(7,\\, 1) = \\mathbf{1\\,VM}\n$$\n\n> [!IMPORTANT]\n> **Examiner's Trap:** Students often report the RAM-allowed count (7) and lose **2 marks** out of 7 because they forget to check the CPU constraint. Always compute **both** and take the **minimum**.\n\n### **Step 6 — Analytical Matrix (Summary Table)**\n\n| Resource | Host Total | Per-VM Need | Usable After Overhead | VMs Allowed | Binding? |\n|---|---|---|---|---|---|\n| **RAM** | 32 GB | 4.0625 GB | 30 GB | 7 | ❌ No |\n| **vCPU (with HT ×1.5, ratio 4:1)** | 12 vCPUs | 2 vCPU × 4 = 8 | 12 vCPUs | 1 | ✅ **Yes** |\n| **Storage (assume 1 TB disk, 100 GB per VM)** | 1024 GB | 100 GB | 1024 GB | 10 | ❌ No |\n\n**Final Answer:** $N_{vm}^{max} = 1$ VM (CPU-bound).\n\n---\n\n## 3.2 Worked Storage-Provisioning Problem\n\n### **Problem Statement:**\nDeploy a 5-node VM cluster. Each VM gets a **50 GB thin-provisioned virtual disk**. The storage system reserves **20%** for snapshots, wear-leveling, and over-commitment. How much **physical storage** must be provisioned initially? If thin-provisioned but the VMs actually use only 30 GB each on average, what is the **storage efficiency gain**?\n\n### **Step 1 — Total logical (provisioned) storage**\n\n$$\nStorage_{logical} = N_{vm} \\times D_{vm}\n$$\n\n$$\nStorage_{logical} = 5 \\times 50\\,\\text{GB} = 250\\,\\text{GB}\n$$\n\n### **Step 2 — Apply provisioning reserve**\n\n$$\nStorage_{physical} = Storage_{logical} \\times (1 + R_{prov})\n$$\n\n$$\nStorage_{physical} = 250 \\times (1 + 0.20) = 250 \\times 1.20 = \\mathbf{300\\,GB}\n$$\n\n### **Step 3 — Compute actual physical usage (with thin-provisioning)**\n\n$$\nStorage_{actual} = N_{vm} \\times D_{actual}\n$$\n\n$$\nStorage_{actual} = 5 \\times 30\\,\\text{GB} = 150\\,\\text{GB}\n$$\n\n### **Step 4 — Compute storage efficiency gain**\n\n$$\n\\eta_{storage} = \\left(1 - \\dfrac{Storage_{actual}}{Storage_{physical}}\\right) \\times 100\n$$\n\n$$\n\\eta_{storage} = \\left(1 - \\dfrac{150}{300}\\right) \\times 100 = 50\\%\n$$\n\n> **Interpretation:** Even with the 20% reserve, the cluster is using only **50% of provisioned physical storage**, yielding a **2× over-commitment** capability — a key advantage of thin-provisioning.\n\n---\n\n## 3.3 Worked Cluster Availability Derivation\n\n### **Problem Statement:**\nEach VM in a 3-node HA cluster has an individual availability of **99.5%** (≈ 43.8 hours of downtime/year). What is the **cluster-level availability** assuming serial dependency (both nodes must fail for total outage)?\n\n### **Step 1 — Compute unavailability per node**\n\n$$\nU_i = 1 - A_i = 1 - 0.995 = 0.005\n$$\n\n### **Step 2 — Apply serial cluster availability formula**\n\n$$\nU_{cluster} = \\prod_{i=1}^{n} U_i\n$$\n\n$$\nU_{cluster} = 0.005 \\times 0.005 \\times 0.005 = 1.25 \\times 10^{-7}\n$$\n\n### **Step 3 — Convert to cluster availability**\n\n$$\nA_{cluster} = 1 - U_{cluster}\n$$\n\n$$\nA_{cluster} = 1 - 1.25 \\times 10^{-7} = 0.999999875\n$$\n\n### **Step 4 — Express in \"nines\" notation**\n\n$$\nA_{cluster} \\approx 99.9999875\\% \\approx \\mathbf{6\\text{ nines}}\n$$\n\n> **Engineering Insight:** Six nines = **31.5 milliseconds of downtime per year** — this is the **Tier-IV datacenter** standard achieved by **3-node HA clustering**.\n\n---\n\n## 3.4 Complete Step-by-Step Lab Procedure (Proxmox VE 3-Node Cluster)\n\n> [!NOTE]\n> This is the **canonical KTU 2024 lab procedure**. The same workflow applies to VirtualBox-based lab clusters (with manual file sharing replacing Corosync).\n\n### Phase A — Pre-flight Setup\n\n| Step | Action | Command / Configuration |\n|---|---|---|\n| **A1** | Download Proxmox VE ISO | From `proxmox.com/en/downloads` (latest stable ISO ≈ **1.2 GB**) |\n| **A2** | Create bootable USB | Use **Rufus** (Windows) or `dd` (Linux) |\n| **A3** | Enable virtualization in BIOS | Intel: `VT-x`; AMD: `SVM` |\n| **A4** | Install Proxmox on Node 1 | Set hostname (e.g., `pve1.lab.local`), static IP, root password |\n| **A5** | Repeat for Node 2, Node 3 | Use `pve2.lab.local`, `pve3.lab.local` |\n\n### Phase B — Network Configuration\n\n| Step | Action | Notes |\n|---|---|---|\n| **B1** | Configure `/etc/network/interfaces` | Set `vmbr0` as the cluster bridge |\n| **B2** | Ensure all nodes can ping each other | Test with `ping pve2.lab.local` |\n| **B3** | Synchronize time with NTP | `apt install chrony` then `systemctl enable chrony` |\n\n### Phase C — Cluster Creation\n\n| Step | Command (run on pve1) | Purpose |\n|---|---|---|\n| **C1** | `pvecm create mycluster` | Initialize the cluster on Node 1 |\n| **C2** | `pvecm status` | Verify cluster state — should show 1 node, quorum |\n| **C3** | `pvecm addnode pve2.lab.local` (run on pve2) | Join Node 2 |\n| **C4** | `pvecm addnode pve3.lab.local` (run on pve3) | Join Node 3 |\n| **C5** | `pvecm status` | Verify — should show 3 nodes, expected votes = 3 |\n\n### Phase D — Shared Storage Setup (NFS-based, lab-friendly)\n\n| Step | Action | Command |\n|---|---|---|\n| **D1** | Install NFS server on a fourth \"storage\" VM | `apt install nfs-kernel-server` |\n| **D2** | Export `/srv/nfs` directory | Edit `/etc/exports`: `/srv/nfs 10.0.0.0/24(rw,sync,no_subtree_check)` |\n| **D3** | Apply export | `exportfs -a` |\n| **D4** | On each PVE node, add NFS storage | Datacenter → Storage → Add → NFS |\n| **D5** | Upload an ISO to NFS share | Visible in Proxmox GUI under each node |\n\n### Phase E — Deploy a Sample VM Cluster (3 Linux VMs)\n\n| Step | Action |\n|---|---|\n| **E1** | Create VM template: install Ubuntu 22.04 LTS, convert to template |\n| **E2** | Clone template 3 times → `node1`, `node2`, `node3` |\n| **E3** | Distribute VMs across PVE nodes (1 per node for true HA) |\n| **E4** | Configure internal cluster network: `vmbr1` (e.g., `10.10.10.0/24`) |\n| **E5** | Test HA: enable HA in GUI for the 3 VMs |\n| **E6** | Simulate failure: `poweroff` the host running `node1`; watch VM auto-migrate |\n\n### Phase F — Verification & Documentation\n\n| Check | Expected Result |\n|---|---|\n| `pvecm status` | 3 nodes, all online, expected votes = 3 |\n| `ha-manager status` | All VMs in \"started\" state, monitored |\n| GUI Dashboard | All nodes green, VMs distributed |\n| Logout-test: `ssh pve1` then stop corosync | Quorum lost on pve1; VMs migrate to pve2/pve3 |\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Type-1 vs Type-2 Hypervisor Architecture\n\n```mermaid\nflowchart TB\n    subgraph Type1[\"TYPE-1 BARE-METAL ARCHITECTURE\"]\n        T1HW[\"Physical Hardware - CPU - RAM - Disk - NIC\"]\n        T1HV[\"Hypervisor Microkernel - KVM - Xen - ESXi\"]\n        T1VM1[\"Guest VM 1 - App + OS\"]\n        T1VM2[\"Guest VM 2 - App + OS\"]\n        T1VM3[\"Guest VM 3 - App + OS\"]\n        T1HW --> T1HV\n        T1HV --> T1VM1\n        T1HV --> T1VM2\n        T1HV --> T1VM3\n    end\n\n    subgraph Type2[\"TYPE-2 HOSTED ARCHITECTURE\"]\n        T2HW[\"Physical Hardware - CPU - RAM - Disk - NIC\"]\n        T2HOST[\"Host Operating System - Windows - Linux - macOS\"]\n        T2HV[\"Hypervisor Application - VirtualBox - VMware Workstation\"]\n        T2VM1[\"Guest VM 1 - App + OS\"]\n        T2VM2[\"Guest VM 2 - App + OS\"]\n        T2HW --> T2HOST\n        T2HOST --> T2HV\n        T2HV --> T2VM1\n        T2HV --> T2VM2\n    end\n```\n\n## 4.2 VM Cluster Architecture (3-Node Proxmox HA Cluster)\n\n```mermaid\nflowchart TB\n    subgraph Cluster[\"VIRTUAL MACHINE CLUSTER - 3-NODE HA\"]\n        direction TB\n        subgraph Node1[\"NODE 1 - pve1.lab.local\"]\n            N1HV[\"Proxmox Hypervisor - 8 vCPUs - 32 GB RAM\"]\n            N1VM1[\"VM node1 - 2 vCPU - 4 GB\"]\n            N1VM2[\"VM app1 - 2 vCPU - 4 GB\"]\n            N1HV --> N1VM1\n            N1HV --> N1VM2\n        end\n        subgraph Node2[\"NODE 2 - pve2.lab.local\"]\n            N2HV[\"Proxmox Hypervisor - 8 vCPUs - 32 GB RAM\"]\n            N2VM1[\"VM node2 - 2 vCPU - 4 GB\"]\n            N2VM2[\"VM db1 - 2 vCPU - 4 GB\"]\n            N2HV --> N2VM1\n            N2HV --> N2VM2\n        end\n        subgraph Node3[\"NODE 3 - pve3.lab.local\"]\n            N3HV[\"Proxmox Hypervisor - 8 vCPUs - 32 GB RAM\"]\n            N3VM1[\"VM node3 - 2 vCPU - 4 GB\"]\n            N3VM2[\"VM cache1 - 2 vCPU - 4 GB\"]\n            N3HV --> N3VM1\n            N3HV --> N3VM1\n            N3HV --> N3VM1\n            N3HV --> N3VM2\n        end\n        N1HV <--> N2HV\n        N2HV <--> N3HV\n        N1HV <--> N3HV\n    end\n\n    subgraph Corosync[\"CLUSTER MIDDLEWARE LAYER\"]\n        CS[\"Corosync - Cluster Membership - Quorum Voting\"]\n        PC[\"Pacemaker - HA Policy Engine - Resource Manager\"]\n        CRM[\"CRM CLI - crm_mon - crm status\"]\n    end\n\n    subgraph Shared[\"SHARED STORAGE LAYER\"]\n        NFS[\"NFS Server - 10.0.0.50 - 1 TB shared volume\"]\n        CEPH[\"Ceph Distributed Storage - Optional Production Tier\"]\n    end\n\n    Cluster <--> Corosync\n    Cluster <--> Shared\n\n    subgraph Mgmt[\"MANAGEMENT INTERFACE\"]\n        GUI[\"Web GUI - port 8006 - Datacenter View\"]\n        CLI[\"pvecm CLI - qm CLI - ha-manager CLI\"]\n    end\n    Mgmt --> Cluster\n```\n\n## 4.3 VM Cluster Setup Workflow (End-to-End Procedure)\n\n```mermaid\nflowchart TD\n    A[\"Start Lab Experiment\"] --> B[\"Verify Hardware Virtualization Support - Intel VT-x or AMD-V\"]\n    B --> C{\"Choose Platform - Open-Source or Public Cloud\"}\n    C -->|Open-Source| D[\"Install Hypervisor - Proxmox - KVM - VirtualBox\"]\n    C -->|Public Cloud| E[\"Provision Free-Tier VMs - AWS EC2 - GCP e2-micro - Oracle Cloud\"]\n    D --> F[\"Deploy 3 or More VMs\"]\n    E --> F\n    F --> G[\"Configure Virtual Networking - Bridges - VLANs - Overlay\"]\n    G --> H[\"Set Up Shared Storage - NFS - iSCSI - Ceph\"]\n    H --> I[\"Install Cluster Middleware - Corosync - Pacemaker - Keepalived\"]\n    I --> J[\"Join Nodes to Cluster - pvecm create - pvecm addnode\"]\n    J --> K[\"Enable High Availability - HA Groups - Fencing STONITH\"]\n    K --> L[\"Deploy Application Workload - Hadoop - K3s - Web Tier - DB Replication\"]\n    L --> M[\"Run Failure Tests - Kill Host - Disconnect Network - Power Off VM\"]\n    M --> N{\"Cluster Survived\"}\n    N -->|Yes| O[\"Document and Submit Lab Record\"]\n    N -->|No| P[\"Debug Logs - corosync.log - ha-manager status\"]\n    P --> I\n    O --> Q[\"End of Experiment\"]\n```\n\n## 4.4 Network Topology for a Lab VM Cluster\n\n```mermaid\nflowchart LR\n    subgraph MgmtNet[\"Management Network - 10.0.0.0/24\"]\n        PVE1[\"pve1 - 10.0.0.11\"]\n        PVE2[\"pve2 - 10.0.0.12\"]\n        PVE3[\"pve3 - 10.0.0.13\"]\n        ADM[\"Admin Laptop - 10.0.0.100\"]\n        NFS[\"NFS Storage - 10.0.0.50\"]\n    end\n\n    subgraph ClusterNet[\"Cluster Heartbeat Network - 10.10.10.0/24\"]\n        C1[\"corosync link - pve1\"]\n        C2[\"corosync link - pve2\"]\n        C3[\"corosync link - pve3\"]\n    end\n\n    subgraph VMNet[\"VM Guest Network - 10.20.20.0/24\"]\n        V1[\"VM node1 - 10.20.20.11\"]\n        V2[\"VM node2 - 10.20.20.12\"]\n        V3[\"VM node3 - 10.20.20.13\"]\n    end\n\n    ADM -->|Web GUI 8006| PVE1\n    ADM -->|Web GUI 8006| PVE2\n    ADM -->|Web GUI 8006| PVE3\n    PVE1 -->|NFS mount| NFS\n    PVE2 -->|NFS mount| NFS\n    PVE3 -->|NFS mount| NFS\n    PVE1 <-->|UDP 5405| C1\n    PVE2 <-->|UDP 5405| C2\n    PVE3 <-->|UDP 5405| C3\n    PVE1 -.->|bridge vmbr2| V1\n    PVE2 -.->|bridge vmbr2| V2\n    PVE3 -.->|bridge vmbr2| V3\n```\n\n## 4.5 Block-Level Functional Architecture (Public Cloud Equivalent)\n\n```mermaid\nflowchart TB\n    subgraph User[\"User Workstation\"]\n        BR[\"Web Browser - SSH Client - AWS Console\"]\n    end\n\n    subgraph Cloud[\"Public Cloud - AWS Free Tier\"]\n        subgraph Region[\"Region - us-east-1\"]\n            subgraph AZ[\"Availability Zone - us-east-1a\"]\n                EC2A[\"EC2 t2.micro - 1 vCPU - 1 GB - Node 1\"]\n                EC2B[\"EC2 t2.micro - 1 vCPU - 1 GB - Node 2\"]\n            end\n            subgraph AZ2[\"Availability Zone - us-east-1b\"]\n                EC2C[\"EC2 t2.micro - 1 vCPU - 1 GB - Node 3\"]\n            end\n            subgraph Storage[\"Cloud Storage Tier\"]\n                EBS[\"EBS Volumes - 30 GB per Node\"]\n                S3[\"S3 Bucket - Shared Artefacts\"]\n            end\n        end\n    end\n\n    subgraph IaaS[\"IaaS Services\"]\n        VPC[\"VPC - 10.0.0.0/16\"]\n        SG[\"Security Group - Allow 22 80 443\"]\n        IAM[\"IAM Roles - Cluster Service Accounts\"]\n    end\n\n    BR -->|SSH 22| EC2A\n    BR -->|SSH 22| EC2B\n    BR -->|SSH 22| EC2C\n    EC2A --> VPC\n    EC2B --> VPC\n    EC2C --> VPC\n    EC2A --> EBS\n    EC2B --> EBS\n    EC2C --> EBS\n    EC2A --> S3\n    EC2B --> S3\n    EC2C --> S3\n    SG --> VPC\n    IAM --> EC2A\n    IAM --> EC2B\n    IAM --> EC2C\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### **Q1. [KTU University Exam – Dec 2023, Model Paper]**\n**Differentiate between Type-1 and Type-2 hypervisors. Give two examples for each.**\n\n**Model Answer (3 Marks — key points only):**\n\n| Parameter | Type-1 (Bare-Metal) | Type-2 (Hosted) |\n|---|---|---|\n| Installation | Directly on hardware | As an application on host OS |\n| Performance | Near-native (95–98%) | Reduced (80–90%) |\n| Examples | KVM, Xen, ESXi, Hyper-V, Proxmox | VirtualBox, VMware Workstation, Parallels |\n| Use case | Production datacenters | Desktop development, labs |\n\n**Conclusion (1 mark):** Type-1 hypervisors are preferred for production VM clusters because they eliminate the host-OS overhead, providing better performance, stability, and direct hardware access.\n\n---\n\n### **Q2. [KTU University Exam – Jun 2024, Supplementary]**\n**List any three open-source hypervisors suitable for setting up a VM cluster in a B.Tech lab. State one key feature of each.**\n\n**Model Answer (3 Marks):**\n\n1. **Proxmox VE** — Integrated web-based cluster management with built-in Corosync/Pacemaker HA, Ceph storage, and live migration. *(1 mark)*\n2. **KVM (Kernel-based Virtual Machine)** — Native Linux kernel module, leverages hardware virtualization extensions (VT-x/AMD-V), near-native performance. *(1 mark)*\n3. **XCP-ng** — Community-driven fork of Citrix XenServer; supports Xen hypervisor with strong enterprise features (pooling, HA, distributed virtual switching). *(1 mark)*\n\n---\n\n## 5.2 Part B — Full Descriptive Questions (14 Marks, with Internal Choice)\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall:** In Part B cluster questions, students often *describe* steps but fail to include the **actual commands** or **configuration snippets**. Always pair each procedural step with a code block or command. Also, students frequently skip the **\"verification\"** step (e.g., `pvecm status` output) — this is where 2–3 marks are routinely lost.\n\n---\n\n### **Question A (14 Marks) — Option 1**\n\n**[KTU University Exam – May 2024, End Semester Evaluation]**\n\n**(a)** With the help of a neat architectural diagram, explain the **three-tier architecture of a virtual machine cluster** and the role of each tier. **(7 Marks)**\n\n**(b)** Design and document the **step-by-step procedure to set up a 3-node Proxmox VE cluster** using shared NFS storage. Include all necessary commands and verification steps. **(7 Marks)**\n\n---\n\n#### **Model Solution for Q.A(a) — 7 Marks**\n\nThe three-tier architecture of a VM cluster consists of:\n\n**Tier 1 — Compute Tier (Hypervisor + VMs):** The bottom-most layer comprises the physical hosts running Type-1 hypervisors (e.g., Proxmox VE). Each host runs multiple guest VMs. The hypervisor abstracts CPU, RAM, and I/O devices, presenting virtual resources to each guest. *(2 marks)*\n\n**Tier 2 — Coordination & Storage Tier (Cluster Middleware + Shared Storage):** The middle layer provides the \"glue\" that binds independent hosts into a single cluster. It consists of:\n- **Cluster membership service** (e.g., Corosync) — maintains a consistent view of all nodes and their liveness via heartbeats. *(1 mark)*\n- **HA policy engine** (e.g., Pacemaker) — decides which node should host which VM and orchestrates failover. *(1 mark)*\n- **Shared storage** (e.g., NFS, Ceph, iSCSI) — provides a unified, location-transparent storage pool that all nodes can read/write. *(1 mark)*\n\n**Tier 3 — Management & Application Tier (GUI + Workloads):** The top layer provides:\n- A **centralized web-based GUI** (Proxmox GUI on port 8006) for administrators to monitor and control the cluster. *(1 mark)*\n- The **guest application workloads** (Hadoop, Kubernetes, web servers, databases) that benefit from the cluster's HA and scalability. *(1 mark)*\n\n*(Diagram drawn from Section 4.2 of these notes — refer to it for full credit.)*\n\n---\n\n#### **Model Solution for Q.A(b) — 7 Marks**\n\n**Step 1 — Install Proxmox VE on 3 Physical Hosts** *(1 mark)*\nDownload the ISO from `proxmox.com`. Boot each host, install with hostname `pve1.lab.local`, `pve2.lab.local`, `pve3.lab.local`. Configure static IPs on the management network (e.g., `10.0.0.11/24`, `10.0.0.12/24`, `10.0.0.13/24`).\n\n**Step 2 — Configure Network Bridges** *(1 mark)*\nOn each node, edit `/etc/network/interfaces` to ensure `vmbr0` is the management bridge and create `vmbr1` for VM guest traffic. Restart networking with `systemctl restart networking`.\n\n**Step 3 — Set Up NFS Shared Storage** *(1 mark)*\nOn a separate storage VM (or one of the PVE nodes), install NFS:\n```\napt update && apt install nfs-kernel-server -y\nmkdir -p /srv/nfs\necho \"/srv/nfs 10.0.0.0/24(rw,sync,no_subtree_check,no_root_squash)\" >> /etc/exports\nexportfs -a\nsystemctl enable --now nfs-server\n```\n\n**Step 4 — Initialize the Cluster on Node 1** *(1 mark)*\nOn `pve1`, run:\n```\npvecm create mycluster\npvecm status\n```\nExpected output: 1 node, expected votes = 1, quorum = 1.\n\n**Step 5 — Join Nodes 2 and 3 to the Cluster** *(1 mark)*\nOn `pve2`:\n```\npvecm add pve1.lab.local -link0 10.10.10.12\n```\nOn `pve3`:\n```\npvecm add pve1.lab.local -link0 10.10.10.13\n```\n\n**Step 6 — Add NFS Storage to the Datacenter** *(1 mark)*\nIn Proxmox GUI: Datacenter → Storage → Add → NFS. Fill in:\n- ID: `nfs-cluster`\n- Server: `10.0.0.50`\n- Export: `/srv/nfs`\n- Content: `Disk image, ISO image, Snippets`\n\n**Step 7 — Deploy VMs and Enable HA** *(1 mark)*\nCreate 3 VMs (one per node) using an Ubuntu 22.04 ISO. Distribute VMs across the 3 nodes. Right-click each VM → HA → Add to HA. Verify with:\n```\nha-manager status\n```\nExpected: All 3 VMs listed as \"started\" and \"monitored\".\n\n**Verification Snapshot (must include in lab record):**\n```\n$ pvecm status\nCluster information\nName:             mycluster\nConfig Version:   3\nTransport:        knet\nSecure auth:      on\n\nQuorum information\nDate:             Mon Jan 15 10:00:00 2024\nQuorum provider:  corosync_votequorum\nNodes:            3\nExpected votes:   3\nTotal votes:      3\n```\n\n---\n\n### **Question B (14 Marks) — Option 2**\n\n**[KTU University Exam – Dec 2022, End Semester Evaluation]**\n\n**(a)** Compare and contrast the **deployment of a VM cluster using open-source hypervisors (on-premises)** vs. **public cloud platforms (IaaS)**. Cover at least 4 parameters. **(7 Marks)**\n\n**(b)** Write the complete **procedure to set up a 3-node Kubernetes (K3s) cluster on AWS free-tier EC2 instances**, including VPC configuration, security group rules, and K3s installation commands. **(7 Marks)**\n\n---\n\n#### **Model Solution for Q.B(a) — 7 Marks**\n\n| Parameter | On-Prem Open-Source | Public Cloud IaaS |\n|---|---|---|\n| **Capital Expenditure (CapEx)** | High — servers, networking gear, UPS, cooling | Zero — pay-as-you-go |\n| **Operational Expenditure (OpEx)** | Power, cooling, admin staff | Pay per vCPU-hour, GB-hour, GB-month |\n| **Time to Deploy** | Days to weeks (procurement, racking) | Minutes (API call) |\n| **Scalability** | Limited by physical hardware; weeks to add nodes | Elastic — add/remove VMs in seconds via API |\n| **High Availability** | Manual setup of redundant power, network, storage | Multi-AZ deployments built-in |\n| **Control & Customization** | Full control over hypervisor, kernel, hardware | Limited — \"noisy neighbor\", restricted hypervisor access |\n| **Free Tier Suitability (KTU)** | Ideal — zero cost for B.Tech labs | Limited — 1 vCPU/1 GB tier only |\n\n*(Fill 7 rows × 1 mark = 7 marks.)*\n\n**Conclusion (must include):** For a B.Tech lab environment, **open-source on-premises** is preferred due to zero cost, full control, and the ability to demonstrate cluster internals (Corosync, Pacemaker, Ceph). For production enterprise workloads, **public cloud IaaS** offers unmatched scalability and resilience.\n\n---\n\n#### **Model Solution for Q.B(b) — 7 Marks**\n\n**Step 1 — Create a VPC and Subnet** *(1 mark)*\nIn AWS Console: VPC → Create VPC → Name `k3s-vpc`, CIDR `10.0.0.0/16`. Create a public subnet `10.0.1.0/24` in availability zone `ap-south-1a`. Attach an Internet Gateway and configure the route table to allow `0.0.0.0/0` outbound.\n\n**Step 2 — Launch 3 EC2 t2.micro Instances** *(1 mark)*\nUse Ubuntu 22.04 LTS AMI. Configure:\n- Subnet: `k3s-subnet`\n- Auto-assign Public IP: Enabled\n- Key pair: `k3s-key.pem`\n\nTag instances as `k3s-server`, `k3s-worker1`, `k3s-worker2`.\n\n**Step 3 — Configure Security Group** *(1 mark)*\nCreate SG `k3s-sg` with inbound rules:\n\n| Type | Port | Source | Purpose |\n|---|---|---|---|\n| SSH | 22 | My IP | Admin access |\n| Custom TCP | 6443 | 10.0.0.0/16 | K3s API server |\n| Custom TCP | 10250 | 10.0.0.0/16 | Kubelet metrics |\n| Custom UDP | 8472 | 10.0.0.0/16 | Flannel VXLAN |\n| All Traffic | All | Self | Inter-node communication |\n\n**Step 4 — Install K3s Server** *(1 mark)*\nOn `k3s-server` (as `ubuntu` user):\n```\ncurl -sfL https://get.k3s.io | sh -s - server --node-external-ip <SERVER_PUBLIC_IP>\nsudo cat /var/lib/rancher/k3s/server/node-token  # Save this token\n```\n\n**Step 5 — Join Worker Nodes** *(1 mark)*\nOn `k3s-worker1` and `k3s-worker2`:\n```\ncurl -sfL https://get.k3s.io | sh -s - agent \\\n  --server https://<SERVER_PRIVATE_IP>:6443 \\\n  --token <NODE_TOKEN>\n```\n\n**Step 6 — Verify Cluster** *(1 mark)*\nOn `k3s-server`:\n```\nsudo kubectl get nodes\n```\nExpected output:\n```\nNAME           STATUS   ROLES                  AGE   VERSION\nk3s-server     Ready    control-plane,master   2m    v1.28.x\nk3s-worker1    Ready    <none>                 1m    v1.28.x\nk3s-worker2    Ready    <none>                 1m    v1.28.x\n```\n\n**Step 7 — Deploy a Sample Workload** *(1 mark)*\n```\nsudo kubectl create deployment nginx --image=nginx\nsudo kubectl expose deployment nginx --port=80 --type=LoadBalancer\nsudo kubectl get svc nginx  # Note the EXTERNAL-IP\n```\nTest the cluster by accessing the EXTERNAL-IP from a browser. Successful response confirms the cluster is fully operational.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Revision Checklist — KTU 2024 Exam Day**\n\n### **Core Concepts (Memorize These)**\n\n- ✅ **Virtualization** = abstraction of physical resources via a hypervisor\n- ✅ **Type-1 (bare-metal)** = directly on hardware → KVM, Xen, ESXi, Proxmox, Hyper-V\n- ✅ **Type-2 (hosted)** = on host OS → VirtualBox, VMware Workstation, Parallels\n- ✅ **VM cluster** = 2+ VMs coordinated by middleware for HA, LB, and scalability\n- ✅ **High Availability (HA)** = automatic failover on node failure\n- ✅ **Live migration** = moving a running VM between hosts with zero downtime\n- ✅ **STONITH** = \"Shoot The Other Node In The Head\" — fencing mechanism to prevent split-brain\n- ✅ **Quorum** = majority vote (N/2 + 1) required for cluster decisions\n- ✅ **Odd number of nodes** = always use 3, 5, 7 to break ties\n- ✅ **Shared storage** = NFS (lab), iSCSI (prod-block), Ceph (distributed), GlusterFS (replicated)\n\n### **Critical Formulas (Memorize All 8 from Section 2.4)**\n\n- ✅ VM density by RAM: $N_{vm} = \\lfloor (RAM_{host} - RAM_{hv}) / (RAM_{vm} + RAM_{oh}) \\rfloor$\n- ✅ VM density by CPU: $N_{vm} = \\lfloor (vCPU \\times T) / (vCPU_{vm} \\times O) \\rfloor$\n- ✅ Storage provisioning: $S_{phys} = S_{logic} \\times (1 + R_{prov})$\n- ✅ HA failover time: $T_{fail} = T_{detect} + T_{fence} + T_{boot}$\n- ✅ Cluster availability: $U_{cl} = 1 - \\prod (1 - U_i)$\n- ✅ Live migration time: $T_{mig} = M_{vm} / B_{net} + T_{sync}$\n\n### **Commands You Must Remember**\n\n- ✅ `pvecm create <name>` — initialize cluster\n- ✅ `pvecm addnode <peer>` — join node to cluster\n- ✅ `pvecm status` — check cluster health\n- ✅ `ha-manager status` — check HA resource state\n- ✅ `qm create <vmid>` — create a VM (Proxmox)\n- ✅ `virsh list --all` — list VMs (KVM/libvirt)\n- ✅ `kubectl get nodes` — verify K3s/K8s cluster\n\n### **Common Mistakes to Avoid (Exam Day)**\n\n- ❌ Reporting only RAM-based VM count — always check **CPU bottleneck**\n- ❌ Forgetting to include **verification commands** in procedure answers\n- ❌ Confusing **Type-1** with **Type-2** examples\n- ❌ Forgetting that **NFS requires** `no_root_squash` for Proxmox to write disk images\n- ❌ Designing a **2-node cluster** without a tie-breaker (quorum disk)\n- ❌ Not mentioning **STONITH/fencing** in HA answers\n\n### **Key Numbers to Remember**\n\n- 📌 Proxmox web GUI: **port 8006**\n- 📌 K3s API server: **port 6443**\n- 📌 NFS: **port 2049** (TCP)\n- 📌 iSCSI: **port 3260**\n- 📌 Corosync heartbeat: **UDP 5405**\n- 📌 Default STONITH timeout: **30 seconds**\n- 📌 Recommended oversubscription: **4:1 vCPU ratio** (general workloads)\n\n### **One-Line Definitions for Last-Minute Revision**\n\n- 📝 **Hypervisor:** Software that creates and manages virtual machines.\n- 📝 **VM Cluster:** A group of VMs acting as one logical compute unit.\n- 📝 **Quorum:** Minimum number of nodes required for cluster operation.\n- 📝 **Live Migration:** Moving a running VM between physical hosts without downtime.\n- 📝 **Fencing (STONITH):** Isolating a failed node to prevent data corruption.\n- 📝 **Shared Storage:** Storage accessible by all cluster nodes simultaneously.\n- 📝 **Oversubscription:** Allocating more virtual resources than physically available.\n\n---\n\n> [!TIP]\n> **Final Exam Tip:** If asked to \"set up a VM cluster\", always present your answer in the **3-phase structure**: (1) Infrastructure preparation (hypervisor install, network), (2) Cluster formation (middleware, quorum), (3) Workload deployment + verification. This structure matches KTU's marking scheme and guarantees full credit.\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-engineering-7-pccsl607-mExperiment 2-t14",
          "title": "Setting host name for virtual machine nodes in cluster and ssh set up for remote login",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL607 - SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Setting host name for virtual machine nodes in cluster and ssh set up for remote login\n\n<!-- SECTION_1_START -->\n## SECTION 1: Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Academic Definition (KTU 2024 Syllabus Terminology)\n\nIn a distributed computing cluster, a **Virtual Machine (VM) node** is a guest operating system instance running on a hypervisor (e.g., Oracle VirtualBox, VMware Workstation, KVM) that participates as a worker or master unit in a High-Performance Computing (HPC) or cloud-style cluster. The **hostname** is the unique human-readable alphanumeric label assigned to a node so that other nodes, network services, and the DNS resolver can identify and route requests to it.\n\n**SSH (Secure Shell)** is a cryptographic network protocol (defined in RFC 4251–4254) that operates on **TCP port 22** and provides authenticated, encrypted, bidirectional communication between two networked hosts. The SSH setup for a cluster mandates:\n\n1. A unique, persistent **Fully Qualified Domain Name (FQDN)** on each node.\n2. Resolution of these hostnames through the local `/etc/hosts` file (since the cluster is isolated, no external DNS is used).\n3. Installation and enablement of the `openssh-server` daemon on every node.\n4. Generation of an asymmetric key pair and propagation of the **public key** to remote nodes to enable **passwordless, key-based authentication**.\n\n> [!IMPORTANT]\n> **KTU 2024 Scheme Highlight (CO Mapping):** This experiment maps to **CO2 – *Configure networked services for distributed/clustered environments*** under the Bloom's Taxonomy cognitive levels: **Apply (Level 3)** and **Analyse (Level 4)**. Students are expected to *execute* the configuration AND *interpret* the network behaviour (e.g., why a hostname fails to resolve, why SSH asks for a password).\n\n---\n\n### 1.2 Conceptual Analogy / Plain-English Intuition\n\nImagine a new office building with **four floors**. Before the receptionist can forward your phone call to a colleague on the 4th floor, two things must happen:\n\n1. Every floor must be **labelled** — \"Floor 1 – Accounts\", \"Floor 2 – HR\", \"Floor 3 – R&D\", \"Floor 4 – Servers\". This label is the **hostname**.\n2. The receptionist needs a **private directory** that maps every label to a direct phone number. This is the **`/etc/hosts`** file.\n3. The 4th-floor engineer wants a **pre-approved security badge** so he can walk into the server room without signing a register every single time. This badge is the **SSH key pair** — the public key is the badge scanner's permission list, the private key is the engineer's actual badge.\n\nSo, in our cluster:\n- **Hostname** = the \"name tag\" worn by each VM node.\n- **`/etc/hosts`** = the in-house phone directory shared by all nodes.\n- **SSH key pair** = the cryptographic badge that lets nodes trust each other without passwords.\n\n> [!NOTE]\n> **Physical constants / standard values to remember:**\n> - SSH listens on **TCP port 22** (default; configurable in `/etc/ssh/sshd_config`).\n> - Default key algorithm: **RSA 2048-bit** (or **ED25519 256-bit** for stronger security).\n> - Default hostname limit: **64 characters**, must match RFC 1123 (alphanumeric + hyphens, no underscores at the start).\n\n---\n\n### 1.3 Why This Step Is Foundational\n\nIn any cluster (Hadoop, Kubernetes, Slurm, MPI, Proxmox, Eucalyptus, OpenStack), the master node orchestrates worker nodes *by name*. If two nodes share a hostname, the master cannot distinguish them. If a node's hostname does not resolve from a peer, the SSH connection will hang and eventually fail with *\"Could not resolve hostname\"*. Hence, this experiment is the **Gatekeeper Experiment** of the entire lab — no Hadoop, no Kube, no MPI will work without it being done correctly.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## SECTION 2: Deep Theoretical Analysis & KTU High-Yield Cheat Sheet\n\n### 2.1 Step-by-Step Logical Breakdown\n\n#### Phase A — Hostname Configuration (3 Sub-Steps)\n\n1. **Inspect Current Hostname**  \n   The kernel maintains a transient hostname in memory. Reading it confirms the present state and validates that the change is needed.  \n   Command family: `hostname`, `hostnamectl status`, `cat /proc/sys/kernel/hostname`.\n\n2. **Set the Static Hostname**  \n   The static hostname is written by `systemd-hostnamed` to `/etc/hostname` and persists across reboots. `hostnamectl set-hostname <name>` is the modern, atomic, distribution-agnostic way to do it.  \n   On legacy SysV-init systems, one would manually echo into `/etc/hostname`.\n\n3. **Update the Local Name Resolution File**  \n   `/etc/hosts` is consulted *before* DNS for every name lookup. For a private cluster, every node's hostname–IP mapping must be added to **every** node's `/etc/hosts` so that cross-node name resolution is immediate and offline.  \n   Format: `<IP-address>   <canonical-hostname>   <alias1> <alias2>`.\n\n#### Phase B — SSH Server Installation (2 Sub-Steps)\n\n1. **Package Installation**  \n   `openssh-server` is the daemon; `openssh-client` is the user tool. On Debian/Ubuntu (most common in KTU labs): `sudo apt install openssh-server`. On RHEL/CentOS: `sudo yum install openssh-server`. The daemon is `sshd`.\n\n2. **Daemon Enablement & Startup**  \n   `sudo systemctl enable ssh` creates a symlink so sshd auto-starts on every boot. `sudo systemctl start ssh` activates it immediately. `sudo systemctl status ssh` confirms the state.\n\n#### Phase C — SSH Key-Based Authentication (4 Sub-Steps)\n\n1. **Generate the Key Pair**  \n   `ssh-keygen -t rsa -b 2048` produces a private key (`~/.ssh/id_rsa`) and a public key (`~/.ssh/id_rsa.pub`). The private key **must never leave the local node**. Default permissions: private key `600`, directory `700`.\n\n2. **Propagate the Public Key**  \n   `ssh-copy-id user@nodeN` appends the public key into the remote node's `~/.ssh/authorized_keys` file. The `-i` flag explicitly specifies the key file.\n\n3. **Restrict Permissions on the Remote Side**  \n   The remote `~/.ssh` directory must be `700`, and `authorized_keys` must be `600`. If permissions are too lax, the SSH daemon *refuses* the key (this is by design — it prevents key theft from world-readable directories).\n\n4. **Test the Connection**  \n   `ssh user@nodeN \"hostname\"` should log in non-interactively and print the remote hostname without prompting for a password.\n\n> [!NOTE]\n> **The \"Why\" behind Key-Based Auth:** Passwords are vulnerable to brute-force, shoulder-surfing, and keylogging. A 2048-bit RSA key has ~$2^{2048}$ possible values — brute force is computationally infeasible. The protocol uses a **challenge-response** handshake where the client proves it holds the private key *without ever transmitting it* on the wire.\n\n---\n\n### 2.2 KTU Formula / Cheat Sheet Table\n\nBecause this experiment is procedural (not analytical), the \"formula\" equivalent is a **Command & Configuration Reference Table**. This is the high-yield surface KTU examiners test.\n\n| # | Operation | Command / Path | Effect / Output |\n|---|-----------|----------------|-----------------|\n| 1 | View current hostname | `hostname` or `hostnamectl` | Prints transient hostname |\n| 2 | Set persistent hostname | `sudo hostnamectl set-hostname nodeN` | Writes to `/etc/hostname` |\n| 3 | Local name resolution | `sudo nano /etc/hosts` | Add `<IP>  <FQDN>  <short>` |\n| 4 | Install SSH server | `sudo apt install openssh-server` | Installs `sshd` |\n| 5 | Enable SSH at boot | `sudo systemctl enable ssh` | Creates systemd symlink |\n| 6 | Start SSH now | `sudo systemctl start ssh` | Launches daemon on port 22 |\n| 7 | Verify SSH listening | `sudo ss -tlnp \\vert grep :22` | Confirms port 22 is open |\n| 8 | Generate RSA key pair | `ssh-keygen -t rsa -b 2048` | Creates `id_rsa` + `id_rsa.pub` |\n| 9 | Push public key to peer | `ssh-copy-id user@nodeN` | Adds key to `authorized_keys` |\n| 10 | Manual key copy (alt) | `cat ~/.ssh/id_rsa.pub \\vert ssh user@nodeN \"mkdir -p ~/.ssh && chmod 700 ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys\"` | When `ssh-copy-id` unavailable |\n| 11 | Test login | `ssh user@nodeN` | Should not ask password |\n| 12 | Test non-interactive | `ssh user@nodeN hostname` | Returns remote hostname |\n| 13 | Cluster-wide /etc/hosts | Edit on **every** node | Cluster-wide name resolution |\n| 14 | SSH config shortcut | `nano ~/.ssh/config` | Define `Host` aliases |\n| 15 | Allow firewall traffic | `sudo ufw allow 22/tcp` or `sudo firewall-cmd --permanent --add-service=ssh` | Permit SSH through firewall |\n\n> [!IMPORTANT]\n> **KTU Pitfall #1:** Students often edit `/etc/hosts` on only **one** node. The `/etc/hosts` file is **per-node local** — there is no automatic sync. You must edit it on **every node** in the cluster.\n\n---\n\n### 2.3 Real-World Engineering Utility\n\n- **DevOps / SRE:** Every Ansible playbook, every Kubernetes kubelet registration, every Hadoop `slaves`/`workers` file relies on hostname resolution.\n- **Cloud Computing (AWS, Azure, GCP):** EC2 instances are launched with user-data scripts that perform *exactly* this experiment at boot — `hostnamectl set-hostname` + writing peers to `/etc/hosts` + pushing SSH keys via cloud-init.\n- **Edge & IoT:** Lightweight K3s clusters on Raspberry Pi nodes perform the same `hostnamectl` + `/etc/hosts` step.\n- **High-Performance Computing (HPC):** MPI jobs (OpenMPI, MPICH) use `mpirun -hosts node1,node2,node3 ./a.out` — the host list is resolved via `/etc/hosts`.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## SECTION 3: Step-by-Step Derivations, Configuration Walkthrough & Analytical Matrix\n\n### 3.1 Assumption Set (Lab Environment)\n\nFor KTU lab uniformity, the following configuration is assumed. Your examiner may vary the subnet, but the *method* is identical.\n\n| Parameter | Value |\n|-----------|-------|\n| Hypervisor | Oracle VirtualBox 7.x (or VMware) |\n| Network Mode | Host-Only Adapter (or Internal Network) |\n| Subnet | 192.168.56.0/24 |\n| Master node | `node1` (192.168.56.11) |\n| Worker 1 | `node2` (192.168.56.12) |\n| Worker 2 | `node3` (192.168.56.13) |\n| OS | Ubuntu 22.04 LTS Server (recommended for KTU labs) |\n| User account | `keralastudent` (non-root, with `sudo`) |\n| SSH version | OpenSSH 8.9+ |\n\n---\n\n### 3.2 Phase A — Hostname Configuration (Detailed Walkthrough)\n\n#### Step A.1 — Verify Pre-Change State\n\n```bash\nhostname\nhostnamectl status\n```\n\nYou will see the auto-generated `ubuntu` or `localhost` name. This is the kernel transient value.\n\n> [!NOTE]\n> **Why verify first?** Demonstrates a methodical engineering approach. KTU examiners reward \"Observe → Hypothesize → Modify → Verify\" workflow.\n\n#### Step A.2 — Set the Static Hostname\n\nOn **node1**, run:\n```bash\nsudo hostnamectl set-hostname node1\n```\n\nOn **node2**, run:\n```bash\nsudo hostnamectl set-hostname node2\n```\n\nOn **node3**, run:\n```bash\nsudo hostnamectl set-hostname node3\n```\n\nThe change is **immediate** in the current shell after the next prompt refresh, and **persistent** because `systemd-hostnamed` rewrites `/etc/hostname`. No reboot required.\n\nVerify:\n```bash\nhostnamectl status | grep -i \"static hostname\"\n```\n\nExpected output on node1:\n```\n Static hostname: node1\n```\n\n#### Step A.3 — Edit `/etc/hosts` on **Every Node**\n\nOpen the file on **each** node:\n```bash\nsudo nano /etc/hosts\n```\n\nAppend the following block at the bottom of the file on **every** node (substitute your actual usernames and IPs):\n\n```\n192.168.56.11  node1.cluster.lab  node1\n192.168.56.12  node2.cluster.lab  node2\n192.168.56.13  node3.cluster.lab  node3\n```\n\nSave and exit (`Ctrl+O`, `Enter`, `Ctrl+X`).\n\nTest resolution from each node:\n```bash\nping -c 2 node1\nping -c 2 node2\nping -c 2 node3\n```\n\n> [!IMPORTANT]\n> **Analytical Insight (Bloom's Level 4 – Analyse):** The order in `/etc/hosts` does not matter, but the entries must be **mutually consistent across all nodes**. If node1 says \"192.168.56.12 = node2\" but node3 says \"192.168.56.12 = node4\", the cluster has a *split-brain naming* condition. This is a classic KTU exam question.\n\n---\n\n### 3.3 Phase B — SSH Server Installation & Enablement\n\n#### Step B.1 — Install on **Every Node**\n\n```bash\nsudo apt update\nsudo apt install -y openssh-server openssh-client\n```\n\nThe `-y` flag auto-confirms the APT prompt (use with care in production, but ideal for lab scripts).\n\n#### Step B.2 — Enable and Start\n\n```bash\nsudo systemctl enable ssh\nsudo systemctl start ssh\nsudo systemctl status ssh\n```\n\nThe `status` command should show `active (running)` in green.\n\n#### Step B.3 — Verify Port 22 is Listening\n\n```bash\nsudo ss -tlnp | grep :22\n```\n\nExpected output (truncated):\n```\nLISTEN 0  128  0.0.0.0:22  0.0.0.0:*  users:((\"sshd\",pid=812,fd=3))\n```\n\nIf you see `127.0.0.1:22` instead of `0.0.0.0:22`, sshd is bound to loopback only. Edit `/etc/ssh/sshd_config`, set `ListenAddress 0.0.0.0`, then `sudo systemctl restart ssh`.\n\n---\n\n### 3.4 Phase C — Key-Based Authentication Setup\n\n#### Step C.1 — Generate Keys on **Every Node** (Required for Bidirectional Trust)\n\n> [!NOTE]\n> **Why on every node?** A full-mesh key trust lets *any* node SSH to *any* other node. This is required for Hadoop, MPI, and Kube cluster members to communicate bidirectionally without password prompts.\n\n```bash\nssh-keygen -t rsa -b 2048\n```\n\nWhen prompted:\n- `Enter file in which to save the key (/home/keralastudent/.ssh/id_rsa):` → press Enter (accept default).\n- `Enter passphrase (empty for no passphrase):` → press Enter twice (lab convention; production should use a passphrase + ssh-agent).\n\nThis creates:\n- `~/.ssh/id_rsa` (private, mode 600)\n- `~/.ssh/id_rsa.pub` (public, mode 644)\n\n#### Step C.2 — Push the Public Key from Each Node to All Other Nodes\n\nFrom **node1**, run:\n```bash\nssh-copy-id keralastudent@node2\nssh-copy-id keralastudent@node3\n```\n\nAccept the host-key fingerprint on first contact (`yes`). Enter the *target* node's password once per target. The public key is appended to `~/.ssh/authorized_keys` on the target.\n\nRepeat from **node2** → node1, node3, and from **node3** → node1, node2.\n\n> [!IMPORTANT]\n> **Alternative Manual Method (no `ssh-copy-id`):** This is exam-favourite because it tests the student's understanding of the *underlying file plumbing*:\n\n```bash\ncat ~/.ssh/id_rsa.pub | ssh keralastudent@node2 \\\n\"mkdir -p ~/.ssh && \\\n chmod 700 ~/.ssh && \\\n cat >> ~/.ssh/authorized_keys && \\\n chmod 600 ~/.ssh/authorized_keys\"\n```\n\nThis single pipeline performs four operations atomically:\n1. Creates `~/.ssh` if absent.\n2. Sets `700` permission.\n3. Appends the public key.\n4. Sets `600` permission on the key file.\n\nThe double-quoted block executes *on the remote node* via SSH; the `cat` on the left side is executed *locally* and piped over the wire.\n\n#### Step C.3 — Test the Connections\n\nFrom node1:\n```bash\nssh keralastudent@node2 \"hostname && date\"\n```\n\nExpected: prints `node2` and the current date, **with no password prompt**.\n\nRepeat for all pairs to verify a full mesh.\n\n---\n\n### 3.5 Optional Enhancement — `~/.ssh/config` Shortcut\n\nOn **every node**, create `~/.ssh/config`:\n```bash\nnano ~/.ssh/config\n```\n\n```\nHost node1\n    HostName 192.168.56.11\n    User keralastudent\n    IdentityFile ~/.ssh/id_rsa\n\nHost node2\n    HostName 192.168.56.12\n    User keralastudent\n    IdentityFile ~/.ssh/id_rsa\n\nHost node3\n    HostName 192.168.56.13\n    User keralastudent\n    IdentityFile ~/.ssh/id_rsa\n```\n\nSet strict permissions:\n```bash\nchmod 600 ~/.ssh/config\n```\n\nNow you can simply type `ssh node2` instead of `ssh keralastudent@192.168.56.12`.\n\n---\n\n### 3.6 Verification Matrix (Analytical Summary)\n\n| Test | Command | Expected Result | Failure Diagnosis |\n|------|---------|-----------------|-------------------|\n| Hostname is persistent | `hostnamectl` | `Static hostname: node1` | Run `hostnamectl set-hostname node1` again |\n| Peer resolves by name | `ping -c1 node2` | 0% packet loss, reply from 192.168.56.12 | Re-check `/etc/hosts` on **both** ends |\n| SSH daemon active | `systemctl is-active ssh` | `active` | `sudo systemctl start ssh` |\n| Port 22 listening on 0.0.0.0 | `ss -tlnp \\| grep :22` | `0.0.0.0:22` | Edit `sshd_config`, restart |\n| Passwordless SSH | `ssh node2 hostname` | Prints `node2` | Check `~/.ssh/authorized_keys` perms (must be 600) and directory perms (must be 700) |\n| Bidirectional trust | SSH from every node to every other | No password prompt | `ssh-copy-id` may have only been run one way |\n\n> [!WARNING]\n> **KTU Pitfall #2:** If a student sets up keys only from `node1 → node2` but not `node2 → node1`, jobs orchestrated from node2 back to node1 will *still ask for a password*. KTU examiners frequently test this with a question like: *\"You can SSH from A to B, but not from B to A. Diagnose.\"* Answer: keys were pushed in one direction only.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## SECTION 4: Structural Diagrams & Schematics\n\n### 4.1 Cluster Architecture Overview\n\n```mermaid\ngraph TB\n    subgraph Hypervisor[\"ORACLE VIRTUALBOX HOST MACHINE\"]\n        subgraph ClusterNet[\"HOST-ONLY NETWORK 192.168.56.0/24\"]\n            nodeA[\"NODE1 MASTER<br/>192.168.56.11<br/>Hostname: node1<br/>Role: NameNode / Master\"]\n            nodeB[\"NODE2 WORKER<br/>192.168.56.12<br/>Hostname: node2<br/>Role: DataNode / Worker\"]\n            nodeC[\"NODE3 WORKER<br/>192.168.56.13<br/>Hostname: node3<br/>Role: DataNode / Worker\"]\n        end\n        ethA[\"eth0: 192.168.56.11\"]\n        ethB[\"eth0: 192.168.56.12\"]\n        ethC[\"eth0: 192.168.56.13\"]\n    end\n\n    user[\"ADMIN WORKSTATION<br/>SSH Client Terminal\"]\n\n    nodeA --- ethA\n    nodeB --- ethB\n    nodeC --- ethC\n\n    user -.SSH.-> nodeA\n    nodeA -.SSH KEY.-> nodeB\n    nodeA -.SSH KEY.-> nodeC\n    nodeB -.SSH KEY.-> nodeA\n    nodeB -.SSH KEY.-> nodeC\n    nodeC -.SSH KEY.-> nodeA\n    nodeC -.SSH KEY.-> nodeB\n```\n\n**Reading the diagram:** Solid lines are physical/virtual NIC connections. Dashed lines are SSH logical channels. Every node has a full mesh of SSH keys — this is *bidirectional trust*.\n\n---\n\n### 4.2 SSH Public-Key Authentication Flow\n\n```mermaid\nsequenceDiagram\n    autonumber\n    participant Client as SSH Client (Node1)\n    participant Network as TCP/IP Network\n    participant Server as SSHD Daemon (Node2)\n\n    Client->>Server: TCP SYN on port 22\n    Server->>Client: TCP SYN-ACK\n    Client->>Server: TCP ACK (connection established)\n\n    Server->>Client: SSH Version String + Host Key (RSA/ED25519)\n    Client->>Client: Verify Host Key against known_hosts\n\n    Client->>Server: Key Exchange Init (KEXINIT)\n    Server->>Client: KEXINIT\n    Note over Client,Server: Diffie-Hellman Key Exchange<br/>Session key derived (symmetric AES-256)\n\n    Client->>Server: Authentication Request: publickey (sends key fingerprint)\n    Server->>Client: Authentication Challenge (random nonce)\n\n    Note over Client: Sign nonce with PRIVATE key<br/>(never sent over wire)\n    Client->>Server: Send signed nonce\n\n    Server->>Server: Verify signature using PUBLIC key from authorized_keys\n    Server->>Client: Authentication Accepted\n\n    Note over Client,Server: Encrypted session established<br/>User shell granted\n    Client->>Server: Execute command e.g. hostname\n    Server->>Client: Return output\n```\n\n> [!IMPORTANT]\n> **Key Engineering Takeaway (Bloom's Level 4 – Analyse):** Notice that the **private key NEVER leaves the client machine**. The server only ever sees the *signature* of a challenge — the private key is mathematically bound to the public key, but extracting one from the other is computationally infeasible (RSA factoring problem). This is the cryptographic heart of \"passwordless\" login.\n\n---\n\n### 4.3 File-Level Configuration Map\n\n```mermaid\ngraph LR\n    subgraph Node1[\"NODE1 FILESYSTEM\"]\n        h1[\"/etc/hostname<br/>Contains: node1\"]\n        e1[\"/etc/hosts<br/>Contains: 192.168.56.11 node1 etc\"]\n        k1[\"~/.ssh/id_rsa<br/>PRIVATE KEY mode 600\"]\n        p1[\"~/.ssh/id_rsa.pub<br/>PUBLIC KEY\"]\n        a1[\"~/.ssh/authorized_keys<br/>Contains public keys of node2 and node3\"]\n        c1[\"~/.ssh/config<br/>Host aliases for cluster\"]\n    end\n\n    subgraph Node2[\"NODE2 FILESYSTEM\"]\n        h2[\"/etc/hostname<br/>Contains: node2\"]\n        e2[\"/etc/hosts<br/>Contains all 3 cluster IPs\"]\n        k2[\"~/.ssh/id_rsa<br/>PRIVATE KEY\"]\n        a2[\"~/.ssh/authorized_keys<br/>Contains public keys of node1 and node3\"]\n    end\n\n    subgraph Node3[\"NODE3 FILESYSTEM\"]\n        h3[\"/etc/hostname<br/>Contains: node3\"]\n        e3[\"/etc/hosts<br/>Contains all 3 cluster IPs\"]\n        k3[\"~/.ssh/id_rsa<br/>PRIVATE KEY\"]\n        a3[\"~/.ssh/authorized_keys<br/>Contains public keys of node1 and node2\"]\n    end\n\n    p1 -.COPY.-> a2\n    p1 -.COPY.-> a3\n    p2[\"./id_rsa.pub on node2\"] -.COPY.-> a1\n    p2 -.COPY.-> a3\n    p3[\"./id_rsa.pub on node3\"] -.COPY.-> a1\n    p3 -.COPY.-> a2\n```\n\nThis file-level map answers the KTU exam favourite: *\"List all the files modified in the cluster SSH setup experiment and state the purpose of each.\"*\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **[KTU University Exam – June 2024 Model Question]**\n\n**Q1.** Differentiate between transient hostname and static hostname in a Linux system. Which command sets the static hostname persistently?\n\n**Model Answer (3 marks):**\n- **Transient hostname:** A kernel-level, in-memory hostname set by the OS at boot; lost on reboot unless mirrored to static (1 mark).\n- **Static hostname:** The persistent hostname stored in `/etc/hostname`, managed by `systemd-hostnamed`. Survives reboots (1 mark).\n- **Command:** `sudo hostnamectl set-hostname <newname>` sets the static hostname atomically and persists it to `/etc/hostname` (1 mark).\n\n---\n\n> **[KTU University Exam – Dec 2023 Model Question]**\n\n**Q2.** Why is `/etc/hosts` edited on **every** node in a private cluster, even though the same content is repeated?\n\n**Model Answer (3 marks):**\n- The `/etc/hosts` file is a **local, per-node** name resolution table. There is no automatic synchronization mechanism between nodes (1 mark).\n- If node1's `/etc/hosts` contains node2's IP but node3's `/etc/hosts` does not, then node3 **cannot resolve** node2's hostname, and any SSH or service from node3 to node2 will fail (1 mark).\n- Hence, manual editing on every node is mandatory to ensure *uniform, offline, DNS-independent* name resolution across the cluster (1 mark).\n\n---\n\n### 5.2 Part B — Major Questions (14 Marks, with Internal Choice)\n\n> **[KTU University Exam – Module Internal Choice Pattern]**\n\n---\n\n#### ❖ Question A (14 Marks)\n\n> **[KTU University Exam – June 2024 Adapted]**\n\n**(a)** With neat diagrams, explain the architecture of a 3-node private cluster. List the steps to set unique hostnames for each VM and configure the `/etc/hosts` file for inter-node resolution. **(7 marks)**\n\n**Model Solution (7 marks):**\n\n1. **Cluster Architecture Description (2 marks):**  \n   A 3-node cluster consists of:\n   - 1 master node (`node1`, 192.168.56.11) — orchestrates jobs.\n   - 2 worker nodes (`node2` at 192.168.56.12 and `node3` at 192.168.56.13) — execute tasks.  \n   All three VMs are connected via a VirtualBox **Host-Only Adapter** so they can communicate with each other and the host, but are isolated from external networks.\n\n2. **Hostname Configuration Steps (2 marks):**  \n   On each node, execute:\n   ```bash\n   sudo hostnamectl set-hostname node1   # on node1\n   sudo hostnamectl set-hostname node2   # on node2\n   sudo hostnamectl set-hostname node3   # on node3\n   ```\n   Verify with `hostnamectl status`.\n\n3. **/etc/hosts Configuration Steps (2 marks):**  \n   On **each** of the three nodes, open `/etc/hosts`:\n   ```bash\n   sudo nano /etc/hosts\n   ```\n   Append:\n   ```\n   192.168.56.11  node1.cluster.lab  node1\n   192.168.56.12  node2.cluster.lab  node2\n   192.168.56.13  node3.cluster.lab  node3\n   ```\n   Test with `ping -c 2 node1`, `ping -c 2 node2`, `ping -c 2 node3` from every node.\n\n4. **Diagram (1 mark):** Include a cluster block diagram showing the three VMs, their IPs, and the Host-Only network (refer to Section 4.1).\n\n---\n\n**(b)** Explain the procedure to install and configure SSH for passwordless remote login between all three nodes. List the files involved and their required permissions. **(7 marks)**\n\n**Model Solution (7 marks):**\n\n1. **Install SSH Server on All Nodes (1 mark):**  \n   ```bash\n   sudo apt update\n   sudo apt install -y openssh-server openssh-client\n   ```\n\n2. **Enable and Start Daemon (1 mark):**  \n   ```bash\n   sudo systemctl enable ssh\n   sudo systemctl start ssh\n   ```\n\n3. **Generate Key Pair on Every Node (1 mark):**  \n   ```bash\n   ssh-keygen -t rsa -b 2048\n   ```\n   Creates `~/.ssh/id_rsa` (private) and `~/.ssh/id_rsa.pub` (public).\n\n4. **Distribute Public Keys to All Peers (2 marks):**  \n   From each node, run `ssh-copy-id` to all other nodes. Example from node1:\n   ```bash\n   ssh-copy-id keralastudent@node2\n   ssh-copy-id keralastudent@node3\n   ```\n   Repeat from node2 and node3 to establish a **full mesh**.\n\n5. **File Permission Table (2 marks):**\n\n   | File / Directory | Required Permission | Reason |\n   |------------------|---------------------|--------|\n   | `~/.ssh/` | `700` | Directory must be inaccessible to other users |\n   | `~/.ssh/id_rsa` (private) | `600` | Owner read/write only; sshd refuses if broader |\n   | `~/.ssh/id_rsa.pub` (public) | `644` | World-readable is safe (it is public) |\n   | `~/.ssh/authorized_keys` | `600` | sshd refuses if group/other writable |\n   | `~/.ssh/config` | `600` | Prevents other users from reading host aliases |\n\n---\n\n#### ❖ Question B (14 Marks — Alternative Choice)\n\n> **[KTU University Exam – Dec 2023 Adapted]**\n\n**(a)** Describe the SSH public-key authentication handshake in detail. Why is this method more secure than password-based authentication? **(7 marks)**\n\n**Model Solution (7 marks):**\n\n1. **Handshake Steps (4 marks):**\n   - **Step 1 – TCP Connection:** Client opens a TCP connection to port 22 on the server.\n   - **Step 2 – Protocol Negotiation:** Both sides exchange SSH version strings and supported algorithms (KEXINIT). A common key-exchange method (e.g., `curve25519-sha256`) is chosen.\n   - **Step 3 – Key Exchange:** Diffie-Hellman key exchange produces a *shared session key* known to both ends but never transmitted. All subsequent traffic is encrypted with AES-256 (or ChaCha20).\n   - **Step 4 – Server Authentication:** Server sends its *host key* (a long-term public key). Client verifies it against `~/.ssh/known_hosts` to detect man-in-the-middle attacks.\n   - **Step 5 – User Authentication (Public Key):**\n     - Client sends `ssh-rsa` + key fingerprint as the proposed auth method.\n     - Server checks if this fingerprint matches any line in `~/.ssh/authorized_keys`.\n     - If yes, server generates a random 256-bit nonce and sends it to the client.\n     - Client **signs the nonce with its private key** (the private key never leaves the client).\n     - Server **verifies the signature using the stored public key**.\n     - If valid → user is authenticated.\n\n2. **Security Advantages over Passwords (3 marks):**\n   - **No secret transmission:** The private key never leaves the client machine; the password would otherwise be sent (hashed, but still vulnerable to offline brute force).\n   - **Computational infeasibility of forgery:** A 2048-bit RSA key has 2^2048 entropy; brute force requires ~300 trillion years on current hardware.\n   - **Resistance to phishing and keylogging:** Even if the user types the wrong \"password,\" there is no password to steal.\n   - **Granular revocation:** Compromised keys can be removed from `authorized_keys` instantly; password changes must propagate through every system.\n\n---\n\n**(b)** A junior administrator sets up SSH keys from `master` to `worker1` successfully, but reports that SSH from `worker1` back to `master` still prompts for a password. Diagnose the issue, list the possible root causes, and provide a step-by-step remedy. **(7 marks)**\n\n**Model Solution (7 marks):**\n\n1. **Diagnosis (1 mark):**  \n   The SSH trust is **unidirectional** — keys were pushed from master to worker1, but the reverse direction (worker1 → master) was never configured.\n\n2. **Root Cause Enumeration (3 marks):**\n   - `ssh-copy-id keralastudent@worker1` was run from master, but the equivalent command was *not* run from worker1 to master.\n   - The `~/.ssh/authorized_keys` file on master does not contain worker1's public key.\n   - Possibly, the directory permissions on worker's `~/.ssh` are too lax (`chmod 755` instead of `chmod 700`), causing sshd to silently ignore the key.\n\n3. **Remedy Steps (3 marks):**\n   - On worker1, run:\n     ```bash\n     ssh-copy-id keralastudent@master\n     ```\n   - Verify on master:\n     ```bash\n     cat ~/.ssh/authorized_keys\n     ```\n     The output should contain worker1's public key.\n   - On worker1, fix permissions if needed:\n     ```bash\n     chmod 700 ~/.ssh\n     chmod 600 ~/.ssh/authorized_keys\n     ```\n   - Test:\n     ```bash\n     ssh keralastudent@master \"hostname\"\n     ```\n     Should return `master` with no password prompt.\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mark-Loss Zones (Examiner Insights):**\n> 1. **Editing `/etc/hosts` on only one node.** A surprising 30% of students do this. KTU examiners deduct 2 marks outright. *Always mention \"on every node\" explicitly in your answer.*\n> 2. **Confusing `ssh` (client) with `sshd` (server).** The package is `openssh-server`; the daemon is `sshd`. Writing \"install ssh\" gets 0.5 marks instead of 1.\n> 3. **Not mentioning permission requirements.** Stating \"create authorized_keys\" without specifying `chmod 600` loses 1 mark in Part B(b) of Question A.\n> 4. **Forgetting the firewall rule.** If `ufw` is enabled, SSH on port 22 must be explicitly allowed. Many real-world and exam scenarios test this.\n> 5. **Bidirectional vs unidirectional trust.** Examiners love asking \"your cluster has N nodes. How many `ssh-copy-id` commands must you run for full mesh?\" Answer: **N × (N−1)** — e.g., for 3 nodes, **6** commands.\n\n---\n\n### 5.4 Topic Recap & Important Things to Remember\n\n- **Hostname command:** `sudo hostnamectl set-hostname <name>` — persistent; sets the static hostname in `/etc/hostname`.\n- **Local name resolution:** `/etc/hosts` is consulted *before* DNS. **Edit on every node.**\n- **SSH server package:** `openssh-server`. **Daemon:** `sshd`. **Port:** TCP **22**.\n- **Enable and start:** `sudo systemctl enable ssh && sudo systemctl start ssh`.\n- **Key generation:** `ssh-keygen -t rsa -b 2048` (or `-t ed25519` for stronger / shorter keys).\n- **Key distribution:** `ssh-copy-id user@peer` — appends public key to peer's `~/.ssh/authorized_keys`.\n- **Full-mesh key count for N nodes:** `N × (N−1)` ssh-copy-id invocations.\n- **Critical file permissions:** `~/.ssh` = 700, `id_rsa` = 600, `authorized_keys` = 600. Anything more permissive → sshd **refuses** the key.\n- **Test commands:** `ping -c 2 <peer>`, `ssh user@<peer> hostname`, `sudo ss -tlnp | grep :22`.\n- **SSH config shortcut:** `~/.ssh/config` lets you write `ssh node2` instead of `ssh keralastudent@192.168.56.12`.\n- **Firewall:** `sudo ufw allow 22/tcp` (Ubuntu) or `sudo firewall-cmd --permanent --add-service=ssh` (RHEL).\n- **Bidirectional trust is mandatory** for any cluster framework (Hadoop, Kube, Slurm, MPI).\n- **Lab deliverable checklist (for KTU record):**\n  1. Screenshot of `hostnamectl` output on each node.\n  2. Screenshot of `/etc/hosts` on at least one node.\n  3. Screenshot of `systemctl status ssh` showing `active (running)`.\n  4. Screenshot of `ssh nodeX \"hostname\"` returning the peer's name without password prompt.\n  5. A short note (3–4 lines) explaining the role of `/etc/hosts` and SSH keys.\n\n> [!IMPORTANT]\n> **One-Line Mantra for the Exam:**  \n> *\"Set the name, tell everyone the name, install the doorbell, give every door a key, then ring the bell from every door to every other door.\"*\n\n<!-- SECTION_5_END -->",
          "pyqs": []
        },
        {
          "id": "computer-science-and-engineering-7-pccsl607-mExperiment 2-t15",
          "title": "Copy a file from one virtual machine to another virtual machine",
          "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PCCSL607 - SYSTEMS LAB\n> - **Module:** Module Experiment 2\n> - **Topic:** Copy a file from one virtual machine to another virtual machine\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Academic Definition\n\n**Virtual Machine (VM) to Virtual Machine File Copying** is the process of transferring a digital file from the local file system of one guest operating system running inside a hypervisor to the local file system of another guest operating system (on the same or a different host machine) over a logical or physical network link, using a secure file-transfer protocol such as **SSH (Secure Shell)**, **SCP (Secure Copy Protocol)**, **SFTP (SSH File Transfer Protocol)**, or a shared file-system service such as **NFS (Network File System)** or **SMB (Server Message Block)**.\n\nIn the context of the KTU 2024 *Systems Lab* syllabus, the experiment typically requires the student to:\n\n1. Bring up two virtual machines (often Linux) inside a Type-2 hypervisor such as **Oracle VirtualBox** or **VMware Workstation**.\n2. Configure their virtual network interfaces so that they are mutually reachable (i.e., they share an IP subnet).\n3. Install and start the **OpenSSH server (`openssh-server`)** on at least one VM (the *destination*).\n4. Authenticate from the *source* VM using a username/password or an **SSH key pair**.\n5. Execute a file-copy command (commonly `scp`, `rsync`, or `sftp`) and verify the file on the destination VM.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Terminology:** The experiment is officially classified under the *Networking & System Administration* cluster of PCCSL607. The expected learning outcome aligns with **CO2**: *\"Configure network services and perform file/process management across multiple systems.\"*\n\n### 1.2 Conceptual Analogy — The \"Two Houses\" Model\n\nImagine two independent houses (House-A and House-B) standing in the same gated colony. Each house has its own postal address, its own lock, and its own mailbox.\n\n- **The hypervisor (VirtualBox/VMware)** is the *gated colony* — it owns the compound walls and assigns house numbers.\n- **Each VM** is a *house* with its own operating system, users, and files.\n- **The virtual NIC (Network Interface Card)** assigned to each VM is the *front door* of that house.\n- **A network cable (virtual cable) inside the hypervisor** is the *intercom line* that connects the two front doors.\n- **SSH** is the *trusted courier* who knows the lock combination of House-B; you hand him the sealed envelope (file) and he delivers it safely and verifies the recipient.\n- **A password or SSH key** is the *lock combination* that authenticates the courier.\n- **SCP / SFTP / rsync** are the *delivery methods* — bike, car, or van — chosen based on the size and urgency of the parcel.\n\nSo, \"copying a file from one VM to another\" is, in plain English: **placing a sealed envelope into a trusted courier's hands, giving him the address and the lock code of the second house, and asking him to slip it through the second mailbox — all from the comfort of the first house.**\n\n### 1.3 Core Standards & Metrics Highlighted\n\n| Item | Standard / Default | Notes |\n|---|---|---|\n| **SSH default port** | **TCP 22** | Defined by IANA |\n| **SCP** | Runs over **SSH protocol (RFC 4251-4254)** | Legacy, simple one-shot copy |\n| **SFTP** | Subsystem of **SSH (not FTPS)** | Interactive, resume-capable |\n| **NFS** | Uses **TCP/UDP 2049** | Linux-native share |\n| **VM NIC modes** | **NAT, Bridged, Host-Only, Internal** | VirtualBox nomenclature |\n| **Maximum SCP file size** | Limited by **file-system (ext4 = 16 TiB, NTFS = 256 TiB)** | Not by the protocol |\n\n> [!NOTE]\n> In KTU examinations and lab records, students are expected to know **why TCP port 22 is used** (it is the well-known port registered with IANA for the SSH server) and **why a static or known IP is preferred** (so that the source VM can address the destination unambiguously each time).\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Cheat Sheet\n\n### 2.1 The Underlying \"Why\" — Layered Model\n\nThe operation of copying a file between two VMs is, conceptually, a textbook instance of the **TCP/IP four-layer model** in action. We can break the operation into five logical steps, each tied to a network layer:\n\n1. **Application Layer:** The user invokes `scp source.txt user@192.168.56.102:/home/user/`. This constructs a high-level *intent*: *\"Send the bytes of source.txt to the SSH server on host 192.168.56.102, into the path /home/user/.\"*\n2. **Transport Layer:** SSH runs on top of **TCP port 22**. The `scp` client opens a TCP control channel, optionally opens a second TCP data channel (older SCP implementations) or multiplexes data over the same encrypted channel (modern OpenSSH ≥ 5.4).\n3. **Network / Internet Layer:** The client resolves `192.168.56.102` is already a numeric IPv4 address (no DNS needed). It wraps the TCP segment inside an **IP packet** with source IP = VM-A's IP and destination IP = VM-B's IP.\n4. **Data-Link / Virtual Link Layer:** The IP packet is encapsulated inside an **Ethernet frame** whose MAC addresses are those of the two *virtual* NICs (or, in NAT mode, the host's NAT engine and VM-B's virtual NIC). The hypervisor's virtual switch delivers the frame.\n5. **Physical Layer:** The frame travels over the **virtual cable** that the hypervisor has internally wired between the two VMs (or, in Bridged mode, out through the host's real NIC and back in).\n\n> [!TIP]\n> **Why is this important for exams?** The KTU examiner will award marks if you can map each command (`scp`, `ifconfig`, `systemctl`) to its corresponding **OSI/TCP-IP layer**. This demonstrates conceptual depth beyond mere \"type-the-command\" recall.\n\n### 2.2 Authentication — The Two Acceptable Methods\n\n| Method | What is exchanged | Security posture | Typical lab use |\n|---|---|---|---|\n| **Password authentication** | User types the remote user's password over an *encrypted* channel | Moderate; vulnerable to brute force if exposed to the Internet | Quick lab demos |\n| **Public-key authentication (RSA / Ed25519)** | Client proves possession of the private key by signing a server challenge | **Strong**; resistant to brute force | Production systems, viva questions |\n\nIn KTU 2024 lab records, the **password method is sufficient** to earn full marks, but using **key-based authentication** is the examiner's preferred mark-distinction signal.\n\n### 2.3 KTU High-Yield Formula / Parameter Cheat Sheet\n\n| # | Concept | Value / Formula / Default | Why it matters |\n|---|---|---|---|\n| 1 | Default SSH port | **22 / TCP** | Firewall rules must allow it |\n| 2 | SCP transfer speed bound | $\\le \\min(B_{link},\\;B_{disk})$ where $B$ is bandwidth | No protocol-imposed cap |\n| 3 | Encrypted block size (AES-256-CTR) | **128 bits per block** | Determines the cipher's security level |\n| 4 | Round-trip time of SSH handshake | $T_{rtt} \\approx 2 \\cdot \\frac{d_{cable}}{v_{signal}}$ | Dominates the *latency* of small-file copy |\n| 5 | VirtualBox Host-Only subnet | **192.168.56.0/24** (default) | Lets two VMs talk without a router |\n| 6 | VirtualBox Internal network | **No host access, VM-to-VM only** | More isolated; ideal for lab |\n| 7 | `scp` exit code | **0 = success, non-zero = failure** | Used in shell scripts to check success |\n| 8 | File integrity check (post-copy) | `md5sum` / `sha256sum` must match on both VMs | Proves the copy was lossless |\n| 9 | SSH key size (recommended) | **RSA ≥ 2048 bits, Ed25519 = 256 bits** | Below 2048 is deprecated |\n| 10 | Default SSH config file | `/etc/ssh/sshd_config` on server, `~/.ssh/config` on client | The first place to debug |\n\n### 2.4 Real-World Engineering Utility\n\nIn the **IT industry**, the same `scp`/`rsync` workflow is used every day by:\n- **DevOps engineers** pushing configuration files (`*.conf`, `*.yml`) to production servers.\n- **Site Reliability Engineers (SREs)** distributing cryptographic keys across server clusters.\n- **Data engineers** staging large datasets (terabytes) onto Hadoop / Spark worker nodes.\n- **Embedded systems engineers** pushing firmware images to development boards over a LAN.\n\nThus, the small KTU lab experiment is, in fact, the **foundational micro-skill** behind a large fraction of the world's day-to-day infrastructure automation.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Procedure & Analytical Matrix\n\n> [!IMPORTANT]\n> The following procedure assumes **Oracle VirtualBox 7.x** with two **Ubuntu 22.04 LTS** guest VMs, both attached to a **Host-Only Adapter** (the most common KTU lab configuration). The procedure is **generic** and works equivalently on VMware Workstation, Fedora, CentOS, or Debian guests, with minor command-syntax adjustments.\n\n### 3.1 Phase 1 — Hardware / Hypervisor Configuration (No commands inside the guest)\n\n**Step 1.1** Open VirtualBox Manager. For *both* VMs (let's name them `VM-Source` and `VM-Dest`):\n\n1. Power off the VM.\n2. Open **Settings → Network → Adapter 2**.\n3. Check **Enable Network Adapter**.\n4. Attached to: **Host-Only Adapter**.\n5. Promiscuous Mode: **Allow All** (necessary for the virtual switch).\n6. Click **OK**.\n\n> [!NOTE]\n> **Why Adapter 2 and not Adapter 1?** Adapter 1 is typically kept as **NAT** so the VM retains Internet access. The Host-Only Adapter is added as a *second* NIC purely for VM-to-VM traffic. This is the standard KTU-2024 lab topology.\n\n**Step 1.2** Confirm the Host-Only network's subnet:\n\nOn the **host machine**, open a terminal and run:\n\n```\nVBoxManage list hostonlyifs\n```\n\nExpected output (truncated):\n\n```\nName:            VirtualBox Host-Only Ethernet Adapter\nIPAddress:       192.168.56.1\nNetworkMask:     255.255.255.0\n```\n\nThis confirms the host occupies **192.168.56.1** and the guest VMs must be assigned free addresses in the same **/24** subnet, e.g., `192.168.56.101` and `192.168.56.102`.\n\n### 3.2 Phase 2 — Inside-VM Network Configuration\n\n**Step 2.1** Start both VMs. In each VM, open a terminal and list interfaces:\n\n```\nip a\n```\n\nThe second interface (e.g., `enp0s8` in VirtualBox's default NIC naming) should be **DOWN** and have no IPv4 address. We will configure static IPs.\n\n**Step 2.2** On `VM-Source` (the sender), assign a static IP using Netplan (Ubuntu 18.04+):\n\nEdit the file `/etc/netplan/01-netcfg.yaml`:\n\n```\nnetwork:\n  version: 2\n  renderer: networkd\n  ethernets:\n    enp0s8:\n      dhcp4: no\n      addresses:\n        - 192.168.56.101/24\n```\n\nApply the configuration:\n\n```\nsudo netplan apply\n```\n\n**Step 2.3** On `VM-Dest` (the receiver), perform the analogous change with IP **192.168.56.102/24**.\n\n**Step 2.4** Test reachability from `VM-Source`:\n\n```\nping -c 4 192.168.56.102\n```\n\nYou should see four replies with `time=` values in the low-millisecond range. **If you do not, stop and re-check Steps 1.1–2.3** — the file copy will fail otherwise.\n\n### 3.3 Phase 3 — Install and Start the SSH Server (on the destination)\n\n**Step 3.1** On `VM-Dest`, install the OpenSSH server:\n\n```\nsudo apt update\nsudo apt install -y openssh-server\n```\n\n**Step 3.2** Enable and start the daemon:\n\n```\nsudo systemctl enable --now ssh\nsudo systemctl status ssh\n```\n\nThe status line should read **`active (running)`** in green. The SSH server is now listening on **TCP port 22**.\n\n**Step 3.3** Confirm with:\n\n```\nsudo ss -tlnp | grep :22\n```\n\nExpected output:\n\n```\nLISTEN 0 128 0.0.0.0:22 0.0.0.0:* users:((\"sshd\",pid=...,fd=...))\n```\n\n### 3.4 Phase 4 — File Copy (the actual experiment)\n\n**Step 4.1** Create a sample file on `VM-Source`:\n\n```\necho \"Hello from VM-Source at $(hostname) on $(date)\" > ~/demo.txt\ncat ~/demo.txt\n```\n\n**Step 4.2** Use `scp` to copy it to `VM-Dest`:\n\n```\nscp ~/demo.txt student@192.168.56.102:/home/student/\n```\n\n- **First-time prompt:** *\"Are you sure you want to continue connecting (yes/no/[fingerprint])?\"* — type `yes`.\n- **Password prompt:** Enter `student`'s password on `VM-Dest`.\n- **Output on success:** `demo.txt 100% 47 12.3KB/s 00:00`.\n\n**Step 4.3** Verify on `VM-Dest`:\n\n```\ncat /home/student/demo.txt\nmd5sum /home/student/demo.txt\n```\n\n**Step 4.4** Verify the source checksum matches (on `VM-Source`):\n\n```\nmd5sum ~/demo.txt\n```\n\nThe two MD5 hashes must be **identical** → integrity confirmed.\n\n### 3.5 Phase 5 — Alternative Methods (for higher marks / viva)\n\n#### 3.5.1 Using `sftp` (interactive)\n\n```\nsftp student@192.168.56.102\nsftp> put demo.txt\nsftp> ls\nsftp> bye\n```\n\n#### 3.5.2 Using `rsync` (delta transfer)\n\n```\nrsync -avz -e ssh ~/demo.txt student@192.168.56.102:/home/student/\n```\n\nFlag meanings:\n\n- **`-a`** → archive mode (preserves permissions, timestamps, symlinks).\n- **`-v`** → verbose.\n- **`-z`** → compress during transfer (saves bandwidth on slow links).\n\n#### 3.5.3 Using `sshpass` for non-interactive scripts\n\n```\nsudo apt install -y sshpass\nsshpass -p 'password' scp ~/demo.txt student@192.168.56.102:/home/student/\n```\n\n> [!WARNING]\n> `sshpass` is **acceptable in a closed lab** but **never used in production** — passwords in command-line history are a major security risk.\n\n### 3.6 Phase 6 — Analytical Comparison Matrix (the heart of the viva)\n\n| Feature | `scp` | `sftp` | `rsync` | `nc` (netcat) | NFS mount |\n|---|---|---|---|---|---|\n| **Encryption** | ✅ (SSH) | ✅ (SSH) | ✅ (SSH) | ❌ None | ❌ Plain or Kerberos |\n| **Interactive** | ❌ | ✅ | ❌ | ❌ | ❌ (mount-based) |\n| **Resumes partial transfers** | ❌ | ✅ (`reget`) | ✅ (`--partial`) | ❌ | ✅ |\n| **Delta transfer** | ❌ | ❌ | ✅ | ❌ | ❌ |\n| **Default port** | 22 | 22 | 22 | any | 2049 |\n| **Auth via SSH keys** | ✅ | ✅ | ✅ | N/A | N/A (uses Kerberos/UID) |\n| **Suitable for huge files (≥10 GB)** | ⚠️ | ⚠️ | ✅ | ✅ (fastest, unencrypted) | ✅ |\n| **KTU lab suitability** | **Best** | Good | Good | Rare | Rare |\n\n### 3.7 Mathematical / Quantitative Insight\n\nThe **effective throughput** of an `scp` transfer is governed by the classical capacity-time trade-off:\n\n$$\nT_{total} = T_{handshake} + T_{transfer} + T_{ack}\n$$\n\nwhere, in milliseconds,\n\n$$\nT_{handshake} \\approx 3 \\cdot RTT + 2 \\cdot \\text{server-side key-exchange latency}\n$$\n\n$$\nT_{transfer} = \\frac{S_{file}}{B_{link} \\cdot (1 - L_{loss})}\n$$\n\n$$\nT_{ack} = RTT \\cdot \\lceil \\log_2(W_{window}) \\rceil\n$$\n\nA worked example (a typical KTU calculation):\n\n- **File size** $S_{file} = 10$ MB $= 10 \\times 2^{20}$ bytes\n- **Link bandwidth** $B_{link} = 100$ Mbps $= 12.5$ MB/s\n- **Packet loss** $L_{loss} = 0.01$\n- **RTT** = 1 ms (host-only virtual link)\n\n$$\nT_{transfer} = \\frac{10 \\times 2^{20}}{12.5 \\times 10^6 \\times (1 - 0.01)} \\approx 0.85 \\text{ s}\n$$\n\n$$\nT_{handshake} \\approx 3 \\times 1 \\text{ ms} + 2 \\times 80 \\text{ ms} = 163 \\text{ ms}\n$$\n\n$$\nT_{ack} \\approx 1 \\text{ ms} \\cdot \\lceil \\log_2(64) \\rceil = 6 \\text{ ms}\n$$\n\n$$\nT_{total} \\approx 163 + 850 + 6 = 1019 \\text{ ms} \\approx 1.02 \\text{ s}\n$$\n\nThis result illustrates a key engineering insight: for a small file, the **SSH handshake** dominates; for a large file, the **bulk transfer** dominates.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 High-Level Network Topology (Mermaid)\n\n```mermaid\ngraph LR\n    subgraph HostPC[Host Physical Machine]\n        hostnic[Host NIC 192.168.56.1]\n    end\n\n    subgraph Hypervisor[VirtualBox Hypervisor]\n        subgraph VMSource[VM-Source Ubuntu 22.04]\n            srcApp[Application scp client]\n            srcSSH[OpenSSH Client]\n            srcIP[IP 192.168.56.101]\n        end\n\n        subgraph VMDest[VM-Dest Ubuntu 22.04]\n            destIP[IP 192.168.56.102]\n            destSSH[OpenSSH Server port 22]\n            destFS[File System home student]\n        end\n\n        vSwitch[Virtual Switch Host Only vboxnet0]\n    end\n\n    srcApp --> srcSSH\n    srcSSH --> srcIP\n    srcIP --> vSwitch\n    vSwitch --> destIP\n    destIP --> destSSH\n    destSSH --> destFS\n    hostnic --- vSwitch\n```\n\n### 4.2 Command / Control Flow Diagram\n\n```mermaid\nflowchart TD\n    A[Start Lab Experiment] --> B[Configure Both VMs with Host Only Adapter]\n    B --> C[Assign Static IPs 192.168.56.101 and 102]\n    C --> D[Ping Test Pass]\n    D --> E[Install openssh server on VM Dest]\n    E --> F[Start sshd and Verify Port 22]\n    F --> G[Create Sample File on VM Source]\n    G --> H[Run scp Command on VM Source]\n    H --> I{First Time Host Key Prompt}\n    I -->|yes| J[Enter Remote User Password]\n    I -->|no| J\n    J --> K{Authentication Success}\n    K -->|yes| L[File Transferred Over Encrypted SSH]\n    K -->|no| M[Permission Denied Check sshd config and password]\n    L --> N[Compute md5sum on Both VMs]\n    N --> O{Hashes Match}\n    O -->|yes| P[Experiment Successful]\n    O -->|no| Q[Investigate Corruption Check Network and Disk]\n    P --> R[Stop]\n    M --> R\n    Q --> R\n```\n\n### 4.3 Layered Stack Diagram (Why each command matters)\n\n```mermaid\ngraph TB\n    L5[Layer 7 Application scp sftp rsync]\n    L4[Layer 4 Transport TCP Port 22]\n    L3[Layer 3 Network IP 192.168.56.0 slash 24]\n    L2[Layer 2 Data Link Virtual Ethernet vboxnet0]\n    L1[Layer 1 Physical Virtual Backplane of the Hypervisor]\n    L5 --> L4 --> L3 --> L2 --> L1\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short-Answer Questions (3 Marks Each)\n\n> **[KTU University Exam – May 2024, Model Question]**\n> **Q1.** What is the default port number of the SSH server, and how do you check whether the SSH service is currently running on a Linux machine?\n\n**Model Answer (3 marks):**\n1. The default port number of the SSH server is **TCP 22**, registered with the IANA. (1 mark)\n2. The service is provided by the daemon `sshd`, whose configuration lives in `/etc/ssh/sshd_config`. (1 mark)\n3. To verify it is running, execute `sudo systemctl status ssh` (or `sudo service ssh status`) — a green `active (running)` line confirms it. Alternatively, `sudo ss -tlnp | grep :22` shows the listening socket. (1 mark)\n\n---\n\n> **[KTU University Exam – Dec 2023, Retest Paper]**\n> **Q2.** Differentiate between `scp` and `rsync` for copying files between two Linux machines.\n\n**Model Answer (3 marks):**\n1. `scp` is a **simple, non-interactive, one-shot copy** utility that always re-transmits the entire file, even if only a few bytes have changed. (1 mark)\n2. `rsync` is a **delta-transfer** utility: it computes block-level checksums on both ends and transmits only the blocks that differ, dramatically reducing bandwidth for repeated transfers of large files. (1 mark)\n3. Both can run over SSH for security, but `rsync` additionally supports compression (`-z`), archive mode (`-a`), and resume of partial transfers (`--partial`), making it the preferred choice for production backups. (1 mark)\n\n### 5.2 Part B — Long-Answer Questions (14 Marks, with Internal Choice)\n\n> **[KTU University Exam – Model Paper 2024 Scheme]**\n> **Question A (14 Marks):**\n> (a) With a neat diagram, explain the procedure to copy a file from one virtual machine to another virtual machine in VirtualBox. (7 marks)\n> (b) Demonstrate how `scp` and `rsync` can be used to copy files. Show the commands and explain the flags used. (7 marks)\n\n#### Model Solution — Part (a) [7 marks]\n\n**Step 1 (1 mark):** State the lab goal — *To copy a file from VM-A (source) to VM-B (destination) running on the same VirtualBox hypervisor, using a secure protocol.*\n\n**Step 2 (2 marks):** Network configuration. Attach both VMs to a **Host-Only Adapter** (Adapter 2) so they share a private subnet `192.168.56.0/24`. Assign static IPs:\n- VM-A → `192.168.56.101/24`\n- VM-B → `192.168.56.102/24`\n\nVerify with `ping -c 4 192.168.56.102` from VM-A.\n\n**Step 3 (2 marks):** SSH server installation. On VM-B:\n\n```\nsudo apt update\nsudo apt install -y openssh-server\nsudo systemctl enable --now ssh\n```\n\n**Step 4 (1 mark):** Diagram (as in Section 4.1 above). Mark the path of the file.\n\n**Step 5 (1 mark):** Verification of successful copy using `md5sum` on both VMs; the hashes must match.\n\n#### Model Solution — Part (b) [7 marks]\n\n**`scp` command (3 marks):**\n\n```\nscp -P 22 -i ~/.ssh/id_rsa ~/demo.txt student@192.168.56.102:/home/student/\n```\n\nFlag explanations:\n- **`-P 22`** → Non-default port specification (default is 22, so this is illustrative).\n- **`-i ~/.ssh/id_rsa`** → Identity file (private key) for key-based authentication.\n\nIf password authentication is used, omit `-i` and enter the password when prompted.\n\n**`rsync` command (3 marks):**\n\n```\nrsync -avz -e ssh --progress ~/demo.txt student@192.168.56.102:/home/student/\n```\n\nFlag explanations:\n- **`-a`** → Archive (recursive, preserves permissions, timestamps, symlinks).\n- **`-v`** → Verbose output.\n- **`-z`** → Compress during transfer.\n- **`-e ssh`** → Use SSH as the transport.\n- **`--progress`** → Show real-time progress.\n\n**Comparison summary (1 mark):** `scp` is simpler and ideal for one-off copies. `rsync` is preferable for large or repetitive transfers because of its delta-transfer algorithm.\n\n---\n\n> **[Internal Choice — Alternative Question B (14 Marks):]**\n> (a) Explain three different network adapter modes in VirtualBox. Which one is most appropriate for VM-to-VM file transfer inside a lab, and why? (7 marks)\n> (b) Write the step-by-step procedure to enable **SSH key-based authentication** between two VMs, and explain why it is preferred over password authentication. (7 marks)\n\n#### Model Solution — Part (a) [7 marks]\n\n**Three VirtualBox NIC modes (3 × 1.5 = 4.5 marks):**\n\n1. **NAT (Network Address Translation):** The VM shares the host's IP for outbound Internet access. VMs in NAT mode **cannot** directly see each other; they would need port-forwarding rules.\n2. **Bridged Adapter:** The VM appears as a peer on the host's physical LAN, with its own IP from the LAN's DHCP. Useful when the VM must be reachable from *other* physical machines.\n3. **Host-Only Adapter:** The VM is connected to a private virtual network shared with the host but **isolated from the physical LAN**. Two VMs attached to the *same* host-only network can communicate freely. (1.5 marks)\n\n**Most appropriate for KTU lab (1.5 marks):** **Host-Only Adapter**, because it provides a *clean, isolated* subnet for VM-to-VM communication **without** requiring the lab's physical network infrastructure, while still allowing the host to participate for debugging.\n\n**Why not NAT?** NAT requires port-forwarding per VM and is awkward to script. (1 mark)\n\n#### Model Solution — Part (b) [7 marks]\n\n**Step-by-step procedure (5 marks):**\n\n**Step 1 — On VM-Source, generate an SSH key pair:**\n\n```\nssh-keygen -t ed25519 -C \"lab-key\"\n```\n\nThis creates:\n- Private key: `~/.ssh/id_ed25519` (never shared)\n- Public key: `~/.ssh/id_ed25519.pub`\n\n**Step 2 — Push the public key to VM-Dest:**\n\n```\nssh-copy-id student@192.168.56.102\n```\n\nOr, manually:\n\n```\ncat ~/.ssh/id_ed25519.pub | ssh student@192.168.56.102 \\\n  \"mkdir -p ~/.ssh && chmod 700 ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys\"\n```\n\n**Step 3 — Test password-less login:**\n\n```\nssh student@192.168.56.102\n```\n\n**Step 4 — Disable password authentication on VM-Dest (optional, for full marks):**\n\nEdit `/etc/ssh/sshd_config`:\n\n```\nPasswordAuthentication no\n```\n\nThen restart:\n\n```\nsudo systemctl restart ssh\n```\n\n**Why key-based is preferred (2 marks):**\n1. **Immune to brute-force password guessing**, because no password ever leaves the client.\n2. **Cryptographically strong** — a 256-bit Ed25519 key provides ~128 bits of security, equivalent to a 3072-bit RSA key, at a fraction of the CPU cost.\n3. **Automatable** in scripts and CI/CD pipelines without exposing credentials.\n4. **Auditable** — each authorized key has a comment that identifies the originating user/host.\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Where KTU students typically lose marks on this experiment:**\n> 1. **Forgetting to install `openssh-server` on the destination VM.** Many students assume SSH is preinstalled; it is not on a minimal Ubuntu image. The error *\"Connection refused\"* is the symptom. (–2 marks)\n> 2. **Using NAT mode for both VMs.** NAT isolates VMs from each other; the `ping` test fails silently. Always use **Host-Only** (or **Internal Network**) for VM-to-VM traffic. (–2 marks)\n> 3. **Not verifying file integrity with `md5sum` or `sha256sum`.** Simply running `cat` on the destination is not enough for full marks. (–1 mark)\n> 4. **Forgetting to enable the second network adapter in VirtualBox settings.** The VM has a NIC, but it is *unplugged* in the virtual topology. (–2 marks)\n> 5. **Confusing `scp -P` (capital P, for port) with `scp -p` (lowercase, for preserving file modes).** This subtle error is a favourite viva trap. (–1 mark)\n> 6. **Not recording the output of `ifconfig`/`ip a` and `systemctl status ssh` in the lab record.** Examiners reward *evidence* — paste the actual terminal output into the record. (–1 mark)\n\n### 5.4 Topic Recap & Important Things to Remember\n\n- **Default SSH port:** **TCP 22**.\n- **Default VirtualBox host-only subnet:** **192.168.56.0/24**, host at **192.168.56.1**.\n- **Two main tools:** `scp` (simple) and `rsync` (delta, resumable).\n- **Three authentication modes:** password, key-based, host-based.\n- **Always verify** file integrity with `md5sum` or `sha256sum` after the copy.\n- **Always test reachability** with `ping` before attempting the copy.\n- **Always start `sshd`** on the destination: `sudo systemctl enable --now ssh`.\n- **NIC mode rule of thumb:** use **Host-Only** for VM-to-VM, keep **NAT** on Adapter 1 for Internet.\n- **Security:** prefer **Ed25519** keys over RSA-2048; disable `PasswordAuthentication` once keys work.\n- **Viva question to expect:** *\"What is the difference between `scp` and `sftp`? Which is more secure? Which is more feature-rich?\"* — `sftp` is more feature-rich; both share the same security because both run over SSH.\n- **Edge case:** If you see *\"REMOTE HOST IDENTIFICATION HAS CHANGED\"*, the destination's host key has been re-generated (e.g., after a VM clone). Remove the stale entry with `ssh-keygen -R 192.168.56.102` on the source.\n\n> [!TIP]\n> **Last-minute revision mnemonic — \"CPAS\":**\n> **C**onfigure NIC → **P**ing test → **A**uthenticate (SSH) → **S**ecurely copy.\n> Memorise these four stages in order and you will never miss a procedural mark.\n<!-- SECTION_5_END -->",
          "pyqs": []
        }
      ]
    }
  ]
}
