{
  "code": "PBITT304",
  "name": "DATABASE MANAGEMENT SYSTEM",
  "topics": {
    "introductionoverviewofadatabaseprojectitsscopeandobjectivesandtheimportanceofdatabasemanagementsystemsinrealworldapplications": {
      "title": "Introduction: Overview of a database project, its scope and objectives, and the importance of database management systems in real-world applications",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 1\n> - **Topic:** Introduction: Overview of a database project, its scope and objectives, and the importance of database management systems in real-world applications\n\n<!-- SECTION_1_START -->\n## SECTION 1: Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition of a Database\n\nA **Database (DB)** is a logically coherent, persistent, and shared collection of related data, together with a meaningful description of that data, designed and built to meet the information requirements of an organization.\n\n> [!IMPORTANT]\n> **KTU 2024 Board Definition (Elmasri & Navathe Standard):**\n> *\"A database is a collection of related data, typically describing the activities of one or more related organizations, whose purpose is to answer questions and meet the information needs of the organization.\"*\n\nMathematically, a database can be abstracted as a finite, ordered collection of well-defined **relations (tables)**:\n\n$$ \\text{DB} = \\left\\{ R_1, R_2, R_3, \\ldots, R_n \\right\\} $$\n\nwhere each $R_i$ represents a named relation (table) holding logically related data tuples.\n\n---\n\n### 1.2 Definition of a Database Management System (DBMS)\n\nA **Database Management System (DBMS)** is a collection of software programs that enables the creation, definition, manipulation, retrieval, management, and control of a database. It acts as an **interface layer** between the user/application program and the physical database residing on secondary storage.\n\nThe defining equation of a DBMS-driven system is:\n\n$$ \\text{Database System} = \\text{DB} \\oplus \\text{DBMS} \\oplus \\text{Application Programs} \\oplus \\text{End Users} $$\n\nwhere $\\oplus$ denotes the logical integration of all components into a unified information system.\n\n> [!NOTE]\n> **Syllabus Highlight:** The KTU 2024 Module 1 explicitly tests the student's ability to *differentiate* between a **Database** (the data itself) and a **DBMS** (the software that manages it). Examiners frequently award 3 marks for this distinction.\n\n---\n\n### 1.3 Definition of a Database Project\n\nA **Database Project** is the systematic, phased engineering process of planning, designing, implementing, testing, deploying, and maintaining a database system that satisfies a defined set of user requirements. It is governed by the **Software Development Life Cycle (SDLC)** adapted for data-centric systems — known as the **Database System Development Life Cycle (DSDLC)**.\n\n---\n\n### 1.4 Conceptual Analogy — The Library System\n\nTo make this intuitive for first-time learners, consider a **municipal library**:\n\n| Real-World Analogy | Database Concept |\n|---|---|\n| The library building storing books | The **Database** (storage of data) |\n| The librarian (assists search, lending) | The **DBMS** (software managing access) |\n| Library rules (no damage, return in 14 days) | The **Schema / Constraints** |\n| A registered borrower | The **User / Application Program** |\n| Borrowing logbook | The **Transaction Log** |\n\n> **Just like you cannot ask a librarian to find \"an unknown book without a title\", a DBMS cannot retrieve data that is not formally described by a schema.** The schema is the blueprint; the DBMS is the enforcer.\n\n---\n\n### 1.5 Scope of a Database Project\n\nThe **scope** defines the boundaries, deliverables, and functionalities of the system. In KTU evaluation context, scope is typically classified into three concentric layers:\n\n1. **Functional Scope** — *What* the system does (e.g., patient registration, billing, inventory tracking).\n2. **Data Scope** — *Which* data entities are included (e.g., Patient, Doctor, Appointment, Medicine).\n3. **Technical Scope** — *How* the system is deployed (e.g., 3-tier architecture, MySQL backend, ReactJS frontend).\n\n> [!TIP]\n> A well-defined scope statement in a database project typically follows the **SMART** criteria — **S**pecific, **M**easurable, **A**chievable, **R**elevant, and **T**ime-bound.\n\n---\n\n### 1.6 Core Objectives of a Database Project\n\nThe principal objectives, aligned with KTU Module 1 learning outcomes, are:\n\n- **Data Integrity:** Ensuring accuracy and consistency of data across the lifecycle.\n- **Data Independence:** Separating application logic from physical storage structure.\n- **Concurrency Control:** Allowing multiple users to access data simultaneously without conflict.\n- **Security & Access Control:** Enforcing role-based privileges to protect sensitive data.\n- **Efficient Data Retrieval:** Minimizing query response time through indexing and query optimization.\n- **Reduced Data Redundancy:** Storing data in a **normalized** form to eliminate duplication.\n- **Backup & Recovery:** Providing mechanisms to restore the database after a failure.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 The 3-Level Architecture of a DBMS (ANSI/SPARC Model)\n\nThe American National Standards Institute (ANSI) proposed a **3-schema (3-level) architecture** that separates the user application from the physical database. This is a **perennial high-yield topic** in KTU Module 1.\n\nThe three levels, from most abstract to most concrete, are:\n\n1. **External Level (View Schema)** — Describes the part of the database relevant to a specific user group. Multiple external views can exist for the same database.\n\n2. **Conceptual Level (Logical Schema)** — The unified, community-wide view of the entire database. It defines entities, relationships, constraints, and security rules — independent of physical storage.\n\n3. **Internal Level (Physical Schema)** — Describes the physical storage structure: file organization, indexing methods (B+ Tree, Hash), data compression, and access paths.\n\nThe **mappings** between these levels are critical:\n\n$$ \\text{External} \\xleftrightarrow{\\text{External/Conceptual Mapping}} \\text{Conceptual} \\xleftrightarrow{\\text{Conceptual/Internal Mapping}} \\text{Internal} $$\n\n> [!IMPORTANT]\n> **Logical Data Independence** = immunity of external schemas from changes in the conceptual schema. **Physical Data Independence** = immunity of the conceptual schema from changes in the internal schema. KTU frequently asks for definitions and one-line examples.\n\n---\n\n### 2.2 Database Project Lifecycle (DSDLC Phases)\n\nA database project follows a strict **6-phase methodology**:\n\n1. **Database Planning** — Defining scope, objectives, feasibility study, cost-benefit analysis.\n2. **System Definition** — Specifying user views, application boundaries, and hardware/software requirements.\n3. **Requirements Collection & Analysis** — Documenting functional, data, and performance requirements using ER diagrams and DFDs.\n4. **Database Design** — Conceptual (ER model), Logical (Relational model), and Physical (Tables, indexes, partitioning) design.\n5. **DBMS Selection / Implementation** — Choosing a DBMS (e.g., PostgreSQL, Oracle, MySQL), creating schemas via DDL, loading data via DML.\n6. **Testing, Operation & Maintenance** — Performance tuning, backup scheduling, security auditing, and iterative refinement.\n\n> [!NOTE]\n> KTU examiners test the student's understanding of *why* conceptual design precedes logical design. The answer: **the conceptual model is implementation-independent**, allowing a smooth transition to any chosen DBMS.\n\n---\n\n### 2.3 Real-World Importance of DBMS\n\nThe importance is evaluated along **five critical dimensions**:\n\n| Dimension | Description | Engineering Example |\n|---|---|---|\n| **Data Consistency** | Uniformity of data values across the system | Banking: balance after withdrawal must reflect immediately in all modules |\n| **Data Sharing** | Concurrent access by multiple authorized users | Railway reservation: thousands booking at the same time |\n| **Data Security** | Authentication, authorization, encryption | Aadhaar DB: biometric and demographic data protection |\n| **Reduced Development Time** | High-level languages (SQL) vs file handling | Building an inventory app in 2 weeks vs 6 months |\n| **Recovery from Failure** | Transaction logs and checkpointing | E-commerce platforms: rollback after server crash |\n\n---\n\n### 2.4 KTU Formula Sheet / Cheat Sheet\n\n> **The following table contains the essential notations, definitions, and structural equations for Module 1 Introduction. These are recurring in KTU exams as 3-mark and 14-mark components.**\n\n| Symbol / Term | Definition / Equation | Context of Use |\n|---|---|---|\n| $\\text{DB}$ | Collection of relations $\\{R_1, R_2, \\ldots, R_n\\}$ | Database definition |\n| $\\text{DBMS}$ | Software between user and database | Interface definition |\n| $\\vert R \\vert$ | Cardinality of relation $R$ (number of tuples) | Relational model size |\n| $\\deg(R)$ | Degree of relation $R$ (number of attributes) | Schema size |\n| $T_x$ | Database transaction (atomic unit of work) | Concurrency control |\n| $\\text{ACID}$ | Atomicity, Consistency, Isolation, Durability | Transaction properties |\n| $\\text{Schema}$ | Structural description / blueprint of the database | Design phase |\n| $\\text{Instance}$ | Snapshot of data at a particular time | Runtime state |\n| $\\text{3-Level}$ | External $\\to$ Conceptual $\\to$ Internal | ANSI/SPARC |\n| $\\text{DSDLC}$ | 6-phase database development cycle | Project management |\n\n> [!WARNING]\n> KTU examiners specifically check whether students can differentiate **Schema** vs **Instance**. The **schema** is the *design* (like a class in OOP), while the **instance** is the *data at a moment in time* (like an object in OOP).\n\n---\n\n### 2.5 Role of DBMS in Modern Engineering\n\nIn the **2024 industry landscape**, DBMS is the backbone of:\n\n- **Banking & FinTech:** Core Banking Solutions (CBS) process over **4,000 transactions per second** per bank in India.\n- **Healthcare:** Hospital Information Systems (HIS) integrate PACS, EHR, and billing.\n- **E-Governance:** Kerala's e-District, KSRTC booking, and KSEB online systems run on relational DBMS clusters.\n- **IoT & Smart Cities:** Time-series databases store sensor data with sub-millisecond latency.\n\nThe **engineering utility** is rooted in three principles: **persistence, concurrency, and queryability** — none of which are efficiently achievable with flat-file storage.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## SECTION 3: Step-by-Step Analysis & Analytical Matrix\n\n### 3.1 Derivation of the Database System Equation\n\nIn introductory KTU questions, students are often asked to *\"explain the components of a database system with a neat diagram.\"* The formal, step-by-step decomposition is given below.\n\n**Step 1: Identify the Data Layer**\n\nThe foundation is the **database itself** — the persisted data. We define it formally as a set of relations:\n\n$$ \\text{DB} = \\left\\{ R_1, R_2, R_3, \\ldots, R_n \\right\\} $$\n\nEach relation $R_i$ is a 2-dimensional structure with $k$ attributes and $m$ tuples:\n\n$$ R_i = \\left\\{ t_1, t_2, \\ldots, t_m \\right\\} $$\n\nwhere each tuple $t_j$ is a $k$-tuple of atomic values.\n\n**Step 2: Add the Software Layer**\n\nThe DBMS software provides four key functional pillars, expressed as a set of operations:\n\n$$ \\text{DBMS} = \\left\\{ \\text{Data Definition}, \\text{Data Manipulation}, \\text{Data Control}, \\text{Transaction Management} \\right\\} $$\n\n**Step 3: Add the Interface Layer**\n\nApplication programs and user interfaces allow external interaction. Formally, a set of programs:\n\n$$ A = \\left\\{ P_1, P_2, \\ldots, P_k \\right\\} $$\n\n**Step 4: Add the User Layer**\n\nEnd users — categorized as **Naive, Sophisticated, Application Programmers, and Database Administrators (DBA)**:\n\n$$ U = \\left\\{ U_{\\text{Naive}}, U_{\\text{Soph}}, U_{\\text{App}}, U_{\\text{DBA}} \\right\\} $$\n\n**Step 5: Final Aggregation**\n\nThe complete database system is the union of all four layers:\n\n$$\n\\begin{aligned}\n\\text{DBS} &= \\text{DB} \\cup \\text{DBMS} \\cup A \\cup U \\\\\n&= \\left\\{ R_i \\right\\} \\cup \\left\\{ \\text{DDL}, \\text{DML}, \\text{DCL}, \\text{TCL} \\right\\} \\cup \\left\\{ P_j \\right\\} \\cup \\left\\{ U_{\\text{type}} \\right\\}\n\\end{aligned}\n$$\n\n> This equation is the **canonical answer** for the question *\"What are the components of a database system?\"* in KTU 2024.\n\n---\n\n### 3.2 Analytical Matrix: File System vs DBMS\n\nThis comparison is a **decade-old KTU favorite** and routinely appears in Part A (3 marks) or as a sub-part in Part B.\n\n| Parameter | File Processing System | DBMS |\n|---|---|---|\n| Data Redundancy | High (same data in multiple files) | Controlled (via normalization) |\n| Data Inconsistency | Probable | Eliminated |\n| Data Sharing | Difficult | Built-in multi-user support |\n| Data Security | Manual / OS-level | Granular role-based access |\n| Concurrency | Not supported natively | Transaction management with locking |\n| Backup / Recovery | Manual or absent | Automated logs and checkpoints |\n| Data Independence | None | Two-level (logical & physical) |\n| Query Language | Application-specific | Standardized SQL (ANSI) |\n| Cost | Low initially | High initially, low long-term |\n| Integrity Enforcement | Programmer responsibility | Constraint-based (PK, FK, CHECK) |\n\n> [!NOTE]\n> **Rule of thumb for KTU valuation:** The examiner awards 1 mark for each *correct, distinct point of contrast*. Aim for **at least 5 points** in a 3-mark question, and **at least 10 points** in a 7-mark sub-part.\n\n---\n\n### 3.3 Analytical Matrix: Three-Schema Architecture — Layered View\n\n| Level | Schema Type | Audience | Data Independence Provided | Example |\n|---|---|---|---|---|\n| External | View Schema | End User | — | A clerk sees only Invoice data |\n| Conceptual | Logical Schema | Designer / DBA | Logical Independence | Unified ER model of the enterprise |\n| Internal | Physical Schema | System Programmer | Physical Independence | B+ Tree index on `Customer.cust_id` |\n\nThe corresponding mappings are:\n\n$$\n\\begin{aligned}\nM_{EC} &: \\text{External} \\leftrightarrow \\text{Conceptual} \\\\\nM_{CI} &: \\text{Conceptual} \\leftrightarrow \\text{Internal}\n\\end{aligned}\n$$\n\nWhen the conceptual schema is changed (e.g., a new entity added), the external schemas and their applications remain unaffected — this is **Logical Data Independence**, achieved through the $M_{EC}$ mapping.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## SECTION 4: Structural Diagrams & Schematics\n\n### 4.1 Database System Block Architecture\n\nThe following Mermaid block diagram illustrates the **complete layered architecture** of a database system, including all major components and their interconnections. This diagram has been customized to be safe for Mermaid rendering with alphanumeric node identifiers and clean uppercase text labels.\n\n```mermaid\nflowchart TB\n    classDef userStyle fill:#E8F4FD,stroke:#1F77B4,color:#000\n    classDef appStyle fill:#FFF3E0,stroke:#FF7F0E,color:#000\n    classDef dbmsStyle fill:#E8F5E9,stroke:#2CA02C,color:#000\n    classDef dataStyle fill:#FCE4EC,stroke:#D62728,color:#000\n\n    user1[\"Naive User\"]:::userStyle\n    user2[\"Sophisticated User\"]:::userStyle\n    user3[\"Application Programmer\"]:::userStyle\n    user4[\"Database Administrator DBA\"]:::userStyle\n\n    app1[\"Application Programs\"]:::appStyle\n    app2[\"Query Tools SQL Interface\"]:::appStyle\n    app3[\"Reporting Tools\"]:::appStyle\n\n    dbmsCore[\"DBMS Software Engine\"]:::dbmsStyle\n    dbmsDDL[\"Data Definition DDL\"]:::dbmsStyle\n    dbmsDML[\"Data Manipulation DML\"]:::dbmsStyle\n    dbmsDCL[\"Data Control DCL\"]:::dbmsStyle\n    dbmsTC[\"Transaction Control TCL\"]:::dbmsStyle\n\n    engine1[\"Storage Manager\"]:::dbmsStyle\n    engine2[\"Query Processor Optimizer\"]:::dbmsStyle\n    engine3[\"Transaction Manager Recovery\"]:::dbmsStyle\n\n    dataStore[\"Physical Database on Disk\"]:::dataStyle\n    dataLog[\"Transaction Log File\"]:::dataStyle\n    dataIndex[\"Index Files\"]:::dataStyle\n\n    user1 --> app1\n    user2 --> app2\n    user3 --> app2\n    user3 --> app3\n    user4 --> dbmsCore\n\n    app1 --> dbmsCore\n    app2 --> dbmsCore\n    app3 --> dbmsCore\n\n    dbmsCore --> dbmsDDL\n    dbmsCore --> dbmsDML\n    dbmsCore --> dbmsDCL\n    dbmsCore --> dbmsTC\n\n    dbmsDDL --> engine1\n    dbmsDML --> engine2\n    dbmsDCL --> engine3\n    dbmsTC --> engine3\n\n    engine1 --> dataStore\n    engine1 --> dataIndex\n    engine3 --> dataLog\n```\n\n---\n\n### 4.2 Three-Schema (ANSI-SPARC) Architecture\n\nThe following Mermaid diagram maps the **3-level architecture**, showing how a single query travels from a user to the physical disk and back.\n\n```mermaid\nflowchart LR\n    classDef extStyle fill:#E3F2FD,stroke:#1565C0,color:#000\n    classDef conStyle fill:#FFF8E1,stroke:#F57F17,color:#000\n    classDef intStyle fill:#F3E5F5,stroke:#6A1B9A,color:#000\n\n    ext1[\"External View 1 Sales Team\"]:::extStyle\n    ext2[\"External View 2 HR Team\"]:::extStyle\n\n    con1[\"Conceptual Schema Unified Enterprise Model\"]:::conStyle\n    con2[\"ER Model Entities Relationships Constraints\"]:::conStyle\n\n    int1[\"Internal Schema File Organization Indexing\"]:::intStyle\n    int2[\"Physical Storage Disk Blocks\"]:::intStyle\n\n    ext1 -- \"External Conceptual Mapping\" --> con1\n    ext2 -- \"External Conceptual Mapping\" --> con1\n    con1 --> con2\n    con1 -- \"Conceptual Internal Mapping\" --> int1\n    int1 --> int2\n```\n\n---\n\n### 4.3 Database System Development Life Cycle (DSDLC)\n\nThis Mermaid sequence shows the **6 sequential phases** of a typical KTU-style database project, from initial planning to post-deployment maintenance.\n\n```mermaid\nflowchart TD\n    classDef planStyle fill:#FFEBEE,stroke:#C62828,color:#000\n    classDef designStyle fill:#E0F2F1,stroke:#00695C,color:#000\n    classDef implStyle fill:#EDE7F6,stroke:#4527A0,color:#000\n    classDef testStyle fill:#FFFDE7,stroke:#F9A825,color:#000\n\n    phase1[\"Phase 1 Database Planning Scope and Feasibility\"]:::planStyle\n    phase2[\"Phase 2 System Definition User Requirements\"]:::planStyle\n    phase3[\"Phase 3 Requirements Collection ER Diagram DFD\"]:::designStyle\n    phase4[\"Phase 4 Database Design Conceptual Logical Physical\"]:::designStyle\n    phase5[\"Phase 5 DBMS Selection DDL DML Implementation\"]:::implStyle\n    phase6[\"Phase 6 Testing Operation Maintenance\"]:::testStyle\n\n    phase1 --> phase2 --> phase3 --> phase4 --> phase5 --> phase6\n    phase6 -.Feedback Loop.-> phase3\n```\n\n> **Note:** The dotted feedback loop in Phase 6 signifies the **iterative nature** of database projects — a maintenance request may trigger a new requirements collection phase.\n\n---\n\n### 4.4 Block-Level Functional Mapping: User Query to Disk Access\n\nThe following diagram illustrates how a single SQL query, like `SELECT * FROM Student WHERE roll_no = 101`, propagates through all DBMS layers.\n\n```mermaid\nflowchart TB\n    classDef qStyle fill:#E1F5FE,stroke:#0277BD,color:#000\n    classDef pStyle fill:#FFF3E0,stroke:#E65100,color:#000\n    classDef sStyle fill:#F1F8E9,stroke:#558B2F,color:#000\n\n    qStep1[\"Step 1 User issues SQL query\"]:::qStyle\n    qStep2[\"Step 2 Query Parser checks syntax\"]:::pStyle\n    qStep3[\"Step 3 Query Optimizer creates execution plan\"]:::pStyle\n    qStep4[\"Step 4 Execution Engine accesses index\"]:::pStyle\n    qStep5[\"Step 5 Storage Manager reads disk blocks\"]:::sStyle\n    qStep6[\"Step 6 Result set returned to user\"]:::qStyle\n\n    qStep1 --> qStep2 --> qStep3 --> qStep4 --> qStep5 --> qStep6\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n### PART A — 3-Mark Questions (Short Answer)\n\n> **Q1.** `[KTU University Exam - Dec 2023]`\n> **Differentiate between a Database and a DBMS. Give one example for each.**\n\n**Model Answer:**\nA **Database** is a structured collection of related data stored persistently — for example, a College database containing `Student`, `Course`, and `Enrollment` tables. A **DBMS** is the *software* used to define, create, manipulate, and control access to that database — for example, **Oracle 19c**, **MySQL 8.0**, or **PostgreSQL 15**.\n\n> The database is the *data*; the DBMS is the *tool* that manages the data. **(3 marks)**\n\n---\n\n> **Q2.** `[KTU University Exam - June 2024]`\n> **List any three advantages of using a DBMS over a traditional file processing system.**\n\n**Model Answer:**\n1. **Reduced data redundancy and inconsistency** through centralized schema and normalization.\n2. **Data sharing and concurrent access** by multiple users via transaction management.\n3. **Enforcement of data integrity constraints** (primary key, foreign key, check constraints) automatically.\n   *(Additional point for completeness: Improved security through authentication, authorization, and views.)* **(3 marks)**\n\n---\n\n### PART B — 14-Mark Questions (with Internal Choice)\n\n> **Question A (14 Marks)** `[KTU University Exam - May 2024]`\n> **(a)** Explain the **three-schema (ANSI/SPARC) architecture** of a DBMS with a neat diagram. Define **logical data independence** and **physical data independence**. **(7 marks)**\n> **(b)** Discuss in detail the **scope and objectives of a database project**, citing a real-world example of a Hospital Management System. **(7 marks)**\n\n---\n\n**Model Solution for Q.A(a):**\n\nThe **ANSI/SPARC three-schema architecture** (proposed in 1975) divides the database description into three levels of abstraction, providing a clear separation of concerns between user views and physical storage.\n\n**1. External Level (View Schema):**\nThis is the highest level of abstraction. Each external schema describes a *subset* of the database relevant to a specific user group. For example, in a University database, the **Accounts Office** may see `Student_Fees` view, while the **Examination Cell** sees `Student_Results` view — both derived from the same underlying database.\n\n**2. Conceptual Level (Logical Schema):**\nThis is the *community-wide* view of the entire database. It integrates the needs of all user groups and is independent of both the physical storage and the application programs. The conceptual schema is typically represented using the **Entity-Relationship (ER) model** and contains entities, attributes, relationships, and integrity constraints.\n\n**3. Internal Level (Physical Schema):**\nThis is the lowest level of abstraction, describing the *physical storage* of the database. It specifies file organization (Heap, Sequential, B+ Tree, Hash), indexing strategies, data compression, and record placement on disk blocks.\n\n**Mappings:**\n- **External / Conceptual Mapping** — Translates requests from the external schema to the conceptual schema.\n- **Conceptual / Internal Mapping** — Translates conceptual-level operations to physical storage operations.\n\n**Logical Data Independence:**\nThe capacity to change the *conceptual schema* (e.g., add a new entity, split a table) without affecting the *external schemas* or application programs. Achieved via the **External/Conceptual mapping** layer.\n\n**Physical Data Independence:**\nThe capacity to change the *internal schema* (e.g., use a different indexing method, switch storage device) without affecting the *conceptual schema*. Achieved via the **Conceptual/Internal mapping** layer. **(7 marks — full marks distribution: 1 mark each for 3 levels, 1 mark for mappings, 2 marks for the two independences, 2 marks for the diagram)**\n\n---\n\n**Model Solution for Q.A(b):**\n\n**Scope of a Hospital Management System (HMS) Project:**\n\nThe scope defines the *boundaries* of the project. For an HMS, the scope is typically:\n\n- **Functional Scope:** Patient registration, OPD/IPD management, doctor scheduling, pharmacy inventory, lab reporting, billing, and insurance claim processing.\n- **Data Scope:** Entities like `Patient`, `Doctor`, `Nurse`, `Appointment`, `Prescription`, `Lab_Test`, `Bill`, `Ward`, `Bed`, `Insurance_Claim`.\n- **Technical Scope:** 3-tier architecture with a MySQL/PostgreSQL backend, RESTful API layer (Node.js/Java), and a web frontend (React/Angular).\n\n**Objectives of the HMS Project:**\n\n1. **Centralized Patient Records:** Replace scattered paper files with a unified electronic health record (EHR) accessible to authorized doctors across departments.\n2. **Reduced Waiting Time:** Implement appointment scheduling to minimize patient queues.\n3. **Accurate Billing:** Eliminate billing errors by integrating consultation, pharmacy, and lab charges.\n4. **Regulatory Compliance:** Meet ABDM (Ayushman Bharat Digital Mission) standards for digital health records.\n5. **Data Security and Privacy:** Enforce HIPAA-equivalent access controls and audit logging.\n6. **Analytical Reporting:** Generate management reports (e.g., monthly OPD statistics, revenue per department) for strategic decision-making.\n7. **Data Backup and Disaster Recovery:** Automated daily backups with off-site replication to ensure 99.99% availability. **(7 marks — full marks distribution: 2 marks for scope, 4 marks for objectives with 1-mark points, 1 mark for concluding statement)**\n\n---\n\n> **Question B (14 Marks — Alternative Choice)** `[KTU University Exam - Dec 2023]`\n> **(a)** Describe the **components of a database system** with a neat diagram. Differentiate between **schema** and **instance** with suitable examples. **(7 marks)**\n> **(b)** Explain the **importance of DBMS in real-world applications** by discussing its role in **banking, e-commerce, and healthcare** sectors. **(7 marks)**\n\n---\n\n**Model Solution for Q.B(a):**\n\nA complete database system consists of **five major components**:\n\n1. **Hardware** — Physical devices (CPU, RAM, disk storage, network) that host the DBMS and database.\n2. **Software** — The DBMS software itself, including the operating system, network software, and application programs.\n3. **Data** — The operational data and the metadata (data about data) stored in the database.\n4. **Procedures** — The set of rules and instructions governing the design, use, and maintenance of the database.\n5. **People** — End users (Naive, Sophisticated), Application Programmers, and Database Administrators (DBA).\n\n**Diagram: Components of a Database System** *(Refer to Section 4.1 Mermaid Block Diagram)*\n\n**Schema vs Instance:**\n\n- A **Schema** is the *structural design* or *blueprint* of the database — it remains relatively static. Example: The definition of the `Student(roll_no, name, age, dept)` table is a schema.\n- An **Instance** is the *snapshot of data* stored in the database at a *specific point in time* — it changes dynamically. Example: At 10:00 AM, the `Student` table contains 5,000 rows; at 11:00 AM, after new admissions, it contains 5,200 rows.\n\n> **Analogy:** A schema is like a *class definition* in C++/Java; an instance is like an *object* of that class. **(7 marks — full marks distribution: 3 marks for 5 components, 2 marks for schema/instance definition, 2 marks for the analogy/example)**\n\n---\n\n**Model Solution for Q.B(b):**\n\nThe DBMS is mission-critical in modern industry. Three representative sectors are:\n\n**1. Banking Sector:**\n- **Core Banking Solutions (CBS):** Process millions of transactions per day — ATM withdrawals, NEFT/RTGS, UPI transfers.\n- **ACID Compliance:** Ensures money debited from one account is *atomically* credited to another; partial failures are rolled back.\n- **Concurrency Control:** Two simultaneous withdrawals from the same account must be serialized to prevent overdraft.\n- **Audit Trails:** RBI regulations mandate persistent transaction logs for forensic auditing.\n- **Example:** HDFC Bank's CBS processes over **8,000 transactions per second** at peak hours.\n\n**2. E-Commerce Sector:**\n- **Inventory Management:** Real-time stock updates across warehouses (e.g., Amazon, Flipkart).\n- **Product Catalog:** Multilingual, multi-currency, multi-tenant data stores.\n- **Recommendation Engines:** NoSQL DBs (MongoDB, Cassandra) layered on top of relational DBs for personalization.\n- **Order Processing:** Distributed transactions across inventory, payment, and shipping microservices.\n- **Example:** During festive sales, Flipkart's DB cluster handles **over 50,000 orders per minute** with sub-second latency.\n\n**3. Healthcare Sector:**\n- **Electronic Health Records (EHR):** Centralized patient history accessible across hospitals.\n- **Telemedicine:** Real-time video consultation data, prescriptions, and lab results.\n- **ABDM Integration:** India's Ayushman Bharat Digital Mission links hospitals via ABHA IDs.\n- **Data Privacy:** Compliance with DPDP Act 2023 for personal health data protection.\n- **Example:** Apollo Hospitals' HIS integrates **3 million+ patient records** across 70+ hospitals.\n\n> **Conclusion:** The DBMS is not just a data store — it is the *operational backbone* of the digital economy. **(7 marks — full marks distribution: 2 marks per sector with 3-4 sub-points, 1 mark for concluding statement)**\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfalls — Read Carefully:**\n> 1. **Do NOT confuse DBMS with RDBMS.** RDBMS is a *type* of DBMS that uses the relational model. A general DBMS may be hierarchical, network, or object-oriented.\n> 2. **\"Data Independence\" answers must specify the level.** A common student error is writing *\"Data independence means the application does not depend on the database\"* — this is incomplete. Always mention **which level** changes are isolated from **which other level**.\n> 3. **Schema is NOT a table.** Many students write *\"schema is a collection of tables.\"* The schema is the *definition* of the structure; the *instance* is the collection of table rows.\n> 4. **Avoid one-word answers in 7-mark sub-parts.** A single sentence about ANSI/SPARC architecture will fetch at most 1–2 marks. Expand each level with examples, mappings, and a diagram.\n\n---\n\n### Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- **Database** = persistent, logically organized collection of related data + its description (metadata).\n- **DBMS** = software to define, create, manipulate, secure, and share the database.\n- **Database System** = DB + DBMS + Application Programs + Users (5 components: Hardware, Software, Data, Procedures, People).\n- **DSDLC** has 6 phases: Plan → Define → Analyze → Design → Implement → Test/Maintain.\n- **ANSI/SPARC 3-Schema:** External (view) → Conceptual (logical) → Internal (physical).\n- **Two Independences:**\n   - *Logical Data Independence* = External schemas immune to conceptual schema changes.\n   - *Physical Data Independence* = Conceptual schema immune to internal schema changes.\n- **Schema vs Instance:** Schema = design (static); Instance = data at a moment (dynamic).\n- **5 DBMS Advantages:** Data integrity, sharing, security, reduced redundancy, backup/recovery.\n- **ACID Properties** (preview for Module 2): Atomicity, Consistency, Isolation, Durability.\n- **DBA** (Database Administrator) is the most privileged user — handles design, security, backup, performance tuning.\n- **Real-world impact:** Banking (ACID), E-commerce (concurrency), Healthcare (EHR/ABDM), E-Governance (e-District/KSRTC).\n\n> [!IMPORTANT]\n> **Last-Minute Tip for KTU 2024:** When a 14-mark question asks for a \"neat diagram\", always include a **labelled block diagram** of either the 3-schema architecture or the components of a database system. A well-drawn diagram fetches **2 easy marks** and improves the examiner's perception of your answer.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "databasedesignconceptualdesignusinganentityrelationshipdiagramerdtomodelentitiesrelationshipsandattributesandlogicaldesigntotranslatetheerdintoarelationalschema": {
      "title": "Database Design: Conceptual Design using an Entity-Relationship Diagram (ERD) to model entities, relationships, and attributes; and Logical Design to translate the ERD into a relational schema",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 1\n> - **Topic:** Database Design: Conceptual Design using an Entity-Relationship Diagram (ERD) to model entities, relationships, and attributes; and Logical Design to translate the ERD into a relational schema\n\n<!-- SECTION_1_START -->\n# Section 1 — Core Technical Definition & Intuitive Overview\n\n## 1.1 What is Database Design?\n\n> [!NOTE]\n> **Formal Definition (KTU Syllabus Aligned):**\n> **Database Design** is the process of producing a detailed **data model** of a database. It involves three sequential, well-defined phases: **Conceptual Design** (what data must be stored, expressed independently of any DBMS), **Logical Design** (translating the conceptual model into a specific data model such as the **Relational Model**), and **Physical Design** (deciding storage, indexing, and access paths).\n\nIn the **KTU 2024 Scheme (PBITT304, Module 1)**, the first two phases are of primary interest:\n1. **Conceptual Design** — Captured using an **Entity–Relationship Diagram (ERD)**.\n2. **Logical Design** — Translating that ERD into a **Relational Schema** (a set of tables/relations).\n\n## 1.2 Real-World Analogy: \"Blueprint Before Construction\"\n\n> [!IMPORTANT]\n> **Think of it like building a house.**\n> - The **Conceptual Design (ERD)** is the **architect's blueprint** drawn before construction. It shows *rooms (entities)*, *doors and windows (relationships)*, and *furniture inside rooms (attributes)* — but says nothing about the brand of cement or paint (DBMS-independent).\n> - The **Logical Design** is the **structural engineering plan** that translates \"kitchen\" and \"bedroom\" into actual load-bearing walls, column sizes, and beam specifications (independent of contractor, but specific to materials allowed).\n> - The **Physical Design** is the **construction plan** chosen by a specific builder (Oracle, MySQL, PostgreSQL) — which brick, which wiring, which tile.\n\nIf you skip the blueprint and start laying bricks, you end up with a house full of **redundancy, anomalies, and structural defects** — exactly what happens in a database without proper ER modeling.\n\n## 1.3 The ERD — The Conceptual Workhorse\n\nAn **Entity–Relationship Diagram (ERD)** is a **graphical notation** introduced by **Peter Chen (1976)** that captures:\n- **Entities** — real-world objects (Student, Course, Bank Account).\n- **Attributes** — properties of those entities (Name, RollNo, Balance).\n- **Relationships** — meaningful associations among entities (Enrols, Teaches, Owns).\n\n> [!NOTE]\n> **Syllabus Highlight:**\n> The KTU Module 1 explicitly tests your ability to (a) **draw an ERD** for a given problem statement and (b) **convert that ERD** into a set of normalized relational tables. Both are **high-weightage** and appear in **Part A (3 marks)** as definitions and **Part B (14 marks)** as full design problems.\n\n## 1.4 Quick Vocabulary Table\n\n| Term | One-Line Meaning | Example |\n|---|---|---|\n| **Entity** | A distinguishable real-world object | A specific student, a specific course |\n| **Entity Set (Type)** | A collection of similar entities | The set of all students |\n| **Attribute** | A property describing an entity | RollNo, Name, DOB |\n| **Relationship** | An association among entities | Student *enrolls in* Course |\n| **Primary Key** | Attribute(s) that uniquely identify an entity | RollNo for Student |\n| **Cardinality** | Numerical mapping between entity sets | 1:1, 1:N, M:N |\n| **Participation** | Whether every entity must participate | Total vs. Partial |\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# Section 2 — Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Components of the ER Model — The Three Pillars\n\n### Pillar 1: Entities\n- **Strong Entity**: Has its own **primary key**, exists independently. Drawn as a **rectangle**.\n  - *Example*: `STUDENT`, `COURSE`, `DEPARTMENT`.\n- **Weak Entity**: Cannot be uniquely identified by its own attributes; depends on a **strong (owner) entity** for identification. Drawn as a **double rectangle**.\n  - *Example*: `DEPENDENT` (of an EMPLOYEE), `ROOM` (of a HOTEL).\n  - Has a **partial key (discriminator)** — a dashed underline.\n- **Entity Instance**: A single occurrence. *Example*: The student \"Anu, RollNo 45\" is an instance of `STUDENT`.\n\n### Pillar 2: Attributes\nAttributes are the descriptive properties of an entity or relationship.\n\n| Attribute Type | Definition | Notation | Example |\n|---|---|---|---|\n| **Simple / Atomic** | Cannot be subdivided meaningfully | Oval | `Age`, `Salary` |\n| **Composite** | Made of sub-parts (hierarchical) | Oval with sub-ovals | `Name` → `{FirstName, MiddleName, LastName}` |\n| **Single-Valued** | One value per entity instance (default) | Plain oval | `DOB` |\n| **Multi-Valued** | A set of values permitted | **Double oval** | `PhoneNumbers`, `EmailIds` |\n| **Derived** | Computed from another attribute | **Dashed oval** | `Age` derived from `DOB`; `YearsOfService` |\n| **Key / Primary** | Uniquely identifies an entity | Oval with **underlined** text | `RollNo`, `EmpID` |\n| **Descriptive** | Attribute of a *relationship* | Oval attached to diamond | `Grade` attribute of *Enrols* relationship |\n\n> [!IMPORTANT]\n> **KTU Examiner Insight:** Multi-valued and derived attributes **cannot be stored directly** in a relational table during logical design — they require **separate tables** (multi-valued) or **computed columns / queries** (derived). This is a frequent 7-mark Part B sub-question.\n\n### Pillar 3: Relationships\nA **relationship** is an association among several entities. It is drawn as a **diamond** connecting the participating entity rectangles.\n\n**Structural Classification (by degree):**\n- **Unary (Recursive)**: Entity participates with itself. *Example*: `EMPLOYEE` *manages* `EMPLOYEE`.\n- **Binary**: Two entities. *Example*: `STUDENT` *enrolls in* `COURSE`. (Most common in KTU questions.)\n- **Ternary**: Three entities. *Example*: `STUDENT` *attends* `LECTURE` *given by* `FACULTY`.\n- **n-ary**: More than three (rare, usually normalized to binary).\n\n**Mapping Cardinality Classification (the most-tested!):**\n\n| Cardinality | Meaning | Example | ER Notation |\n|---|---|---|---|\n| **1 : 1** | One entity in A maps to at most one in B | `PERSON` ↔ `PASSPORT` | `1 ─── 1` |\n| **1 : N** | One A maps to many B; each B maps to one A | `DEPARTMENT` → `EMPLOYEE` | `1 ─── N` |\n| **M : N** | Many-to-many on both sides | `STUDENT` ↔ `COURSE` | `M ─── N` |\n\n**Participation Constraint (Existence):**\n- **Total Participation** — every entity instance MUST participate. Drawn as a **double line** to the diamond. (Usually the \"many\" side in a 1:N, or the weak entity side.)\n- **Partial Participation** — some entities may not participate. Single line.\n\n## 2.2 Notation Styles (KTU may use any of these)\n\n| Style | Entity | Relationship | Attribute | Origin |\n|---|---|---|---|---|\n| **Chen (Classic)** | Rectangle | Diamond | Oval | Peter Chen, 1976 |\n| **Crow's Foot** | Box | Line with crow's foot | Inside box | Gordon Everest, 1976 |\n| **UML Class Diagram** | Class box | Association line | Inside class | OMG UML standard |\n\n> [!NOTE]\n> KTU module questions in past papers typically use **Chen notation**. However, students should be able to *read* Crow's Foot because industry tools (Visio, Lucidchart, ER/Studio) default to it.\n\n## 2.3 KTU High-Yield Formula Sheet — ER → Relational Mapping Rules\n\nThe KTU syllabus expects a deterministic, rule-based translation. Memorize this **8-step algorithm** (also called the **7 or 8 Mapping Rules**):\n\n> [!IMPORTANT]\n> **Master Mapping Rules (Exam-Ready):**\n> 1. **Rule 1 — Strong Entity → Relation.** Each strong entity becomes a relation. Include all simple, single-valued, derived (as computed columns), and key attributes. The **primary key** of the entity becomes the PK of the relation.\n> 2. **Rule 2 — Weak Entity → Relation.** Include all simple attributes + the **primary key of the owner (identifying) entity as a foreign key**. The PK of this new relation = `{partial_key_of_weak_entity} ∪ {PK_of_owner_entity}`. (Composite PK)\n> 3. **Rule 3 — 1:1 Binary Relationship → FK in either side.** Choose the side with **total participation** to host the FK (avoids NULLs). FK references the PK of the other side.\n> 4. **Rule 4 — 1:N Binary Relationship → FK on the \"N\" side.** The PK of the \"1\" side is added as a **foreign key** into the relation representing the \"N\" side. **No separate table needed.**\n> 5. **Rule 5 — M:N Binary Relationship → New Relation.** Create a new relation whose schema = `{PK_of_A, PK_of_B} ∪ (descriptive attributes of relationship)`. The new PK = **composite of both PKs**.\n> 6. **Rule 6 — Multi-Valued Attribute → New Relation.** New relation = `{PK_of_entity, multi-valued attribute}`. PK = **composite of both**. Multiple values → multiple rows.\n> 7. **Rule 7 — n-ary Relationship (n ≥ 3) → New Relation.** New relation = union of PKs of all participating entities + descriptive attributes. PK = combination of all PKs.\n> 8. **Rule 8 — Derived Attribute → Drop (compute when needed).** Do not store. Recompute using SQL query, view, or trigger.\n\n| Mapping Situation | New Table? | Key / Foreign Key Action |\n|---|---|---|\n| Strong entity | Yes | Its own PK |\n| Weak entity | Yes | PK = (own partial key + owner PK) as FK |\n| 1:1 relationship | No (use side with total participation) | Add FK referencing the other side |\n| 1:N relationship | No | Add PK of \"1\" side as FK in \"N\" side |\n| M:N relationship | **Yes (mandatory)** | New PK = (PK_A, PK_B) |\n| Multi-valued attribute | **Yes (mandatory)** | New PK = (entity_PK, attribute_value) |\n| Ternary / n-ary | **Yes (mandatory)** | New PK = (PK_1, PK_2, ..., PK_n) |\n| Derived attribute | No | Compute at query time |\n| Composite attribute | No | Flatten into constituent simple attributes |\n\n## 2.4 Why This Matters in Engineering Practice\n\n> [!IMPORTANT]\n> - **Software Industry:** Every backend developer using **Spring Boot, Django, Node.js-Express** must translate ERDs into SQL `CREATE TABLE` statements. Wrong mapping ⇒ **anomalies, redundancy, referential integrity violations**.\n> - **Data Warehousing:** Star and snowflake schemas are essentially ERDs in a specialized form.\n> - **API Design:** ERD attributes map directly to JSON fields; relationship cardinality determines the API's nested vs. flat structure.\n> - **Big Data & NoSQL:** Even when using MongoDB, you must first conceptualize relationships via ERD before deciding which collections to embed vs. reference.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# Section 3 — Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Worked Example: Library Management System\n\n> [!NOTE]\n> **Problem Statement (KTU-style):**\n> *\"A library has books. Each book has a title, ISBN, year, and price. Every book belongs to exactly one category (e.g., Fiction, Science). A category has a category_id and name. A book is written by one or more authors. An author has author_id, name, and nationality. Members borrow books; each borrowing records the borrow_date and return_date. A member has member_id, name, address, and phone. A publisher publishes books; a publisher has pub_id and name.\"*\n\n### Step 1 — Identify Entities\nLooking for **nouns** that have independent existence and are distinguishable:\n\n$$\\text{Entities} = \\{\\text{BOOK},\\ \\text{CATEGORY},\\ \\text{AUTHOR},\\ \\text{MEMBER},\\ \\text{PUBLISHER}\\}$$\n\n### Step 2 — Identify Attributes for Each Entity\n- **BOOK**: `ISBN` (PK), `Title`, `Year`, `Price`\n- **CATEGORY**: `CategoryID` (PK), `CatName`\n- **AUTHOR**: `AuthorID` (PK), `Name`, `Nationality`\n- **MEMBER**: `MemberID` (PK), `Name`, `Address`, `Phone` (multi-valued)\n- **PUBLISHER**: `PubID` (PK), `PubName`\n\n### Step 3 — Identify Relationships & Cardinalities\n\n| Relationship | Between | Cardinality | Reasoning |\n|---|---|---|---|\n| `BELONGS_TO` | BOOK ↔ CATEGORY | **N : 1** | Many books → one category |\n| `WRITTEN_BY` | BOOK ↔ AUTHOR | **M : N** | A book can have many authors; an author can write many books |\n| `BORROWS` | MEMBER ↔ BOOK | **M : N** | Members borrow multiple books; books borrowed by many members (over time) |\n| `PUBLISHES` | PUBLISHER ↔ BOOK | **1 : N** | One publisher, many books |\n\nThe `BORROWS` relationship has **descriptive attributes**: `BorrowDate`, `ReturnDate`.\n\n### Step 4 — Map to Relational Schema (Applying All 8 Rules)\n\n**Rule 1** — Strong entities become relations:\n\n$$\n\\begin{aligned}\n\\text{CATEGORY}(\\underline{\\text{CategoryID}},\\ \\text{CatName}) \\\\[2pt]\n\\text{PUBLISHER}(\\underline{\\text{PubID}},\\ \\text{PubName}) \\\\[2pt]\n\\text{AUTHOR}(\\underline{\\text{AuthorID}},\\ \\text{Name},\\ \\text{Nationality}) \\\\[2pt]\n\\text{MEMBER}(\\underline{\\text{MemberID}},\\ \\text{Name},\\ \\text{Address}) \\\\[2pt]\n\\text{BOOK}(\\underline{\\text{ISBN}},\\ \\text{Title},\\ \\text{Year},\\ \\text{Price})\n\\end{aligned}\n$$\n\n**Rule 4** — 1:N `BELONGS_TO`: Add `CategoryID` as FK in BOOK:\n\n$$\n\\begin{aligned}\n\\text{BOOK}(\\underline{\\text{ISBN}},\\ \\text{Title},\\ \\text{Year},\\ \\text{Price},\\ \\text{CategoryID}) \\\\\n\\text{FK: CategoryID REFERENCES CATEGORY(CategoryID)}\n\\end{aligned}\n$$\n\n**Rule 4** — 1:N `PUBLISHES`: Add `PubID` as FK in BOOK:\n\n$$\n\\begin{aligned}\n\\text{BOOK}(\\underline{\\text{ISBN}},\\ \\text{Title},\\ \\text{Year},\\ \\text{Price},\\ \\text{CategoryID},\\ \\text{PubID}) \\\\\n\\text{FK: PubID REFERENCES PUBLISHER(PubID)}\n\\end{aligned}\n$$\n\n**Rule 5** — M:N `WRITTEN_BY`: Create new relation:\n\n$$\n\\begin{aligned}\n\\text{BOOK\\_AUTHOR}(\\underline{\\text{ISBN}},\\ \\underline{\\text{AuthorID}}) \\\\\n\\text{FK: ISBN REFERENCES BOOK(ISBN)} \\\\\n\\text{FK: AuthorID REFERENCES AUTHOR(AuthorID)}\n\\end{aligned}\n$$\n\n**Rule 5** — M:N `BORROWS`: Create new relation (with descriptive attributes):\n\n$$\n\\begin{aligned}\n\\text{BORROW}(\\underline{\\text{MemberID}},\\ \\underline{\\text{ISBN}},\\ \\text{BorrowDate},\\ \\text{ReturnDate}) \\\\\n\\text{FK: MemberID REFERENCES MEMBER(MemberID)} \\\\\n\\text{FK: ISBN REFERENCES BOOK(ISBN)}\n\\end{aligned}\n$$\n\n> [!NOTE]\n> The composite PK `(MemberID, ISBN, BorrowDate)` would be needed if the same member could borrow the *same book* on different dates — KTU questions often add this nuance.\n\n**Rule 6** — Multi-valued attribute `Phone` in MEMBER: Create new relation:\n\n$$\n\\begin{aligned}\n\\text{MEMBER\\_PHONE}(\\underline{\\text{MemberID}},\\ \\underline{\\text{Phone}}) \\\\\n\\text{FK: MemberID REFERENCES MEMBER(MemberID)}\n\\end{aligned}\n$$\n\n### Step 5 — Final Relational Schema (5 Entities + 3 New Relations)\n\n$$\n\\begin{aligned}\n1.&\\ \\text{CATEGORY}(\\underline{\\text{CategoryID}},\\ \\text{CatName}) \\\\[2pt]\n2.&\\ \\text{PUBLISHER}(\\underline{\\text{PubID}},\\ \\text{PubName}) \\\\[2pt]\n3.&\\ \\text{AUTHOR}(\\underline{\\text{AuthorID}},\\ \\text{Name},\\ \\text{Nationality}) \\\\[2pt]\n4.&\\ \\text{MEMBER}(\\underline{\\text{MemberID}},\\ \\text{Name},\\ \\text{Address}) \\\\[2pt]\n5.&\\ \\text{MEMBER\\_PHONE}(\\underline{\\text{MemberID}},\\ \\underline{\\text{Phone}}) \\\\[2pt]\n6.&\\ \\text{BOOK}(\\underline{\\text{ISBN}},\\ \\text{Title},\\ \\text{Year},\\ \\text{Price},\\ \\text{CategoryID},\\ \\text{PubID}) \\\\[2pt]\n7.&\\ \\text{BOOK\\_AUTHOR}(\\underline{\\text{ISBN}},\\ \\underline{\\text{AuthorID}}) \\\\[2pt]\n8.&\\ \\text{BORROW}(\\underline{\\text{MemberID}},\\ \\underline{\\text{ISBN}},\\ \\text{BorrowDate},\\ \\text{ReturnDate})\n\\end{aligned}\n$$\n\n## 3.2 Analytical Matrix — Decision Table for 1:1, 1:N, M:N\n\nThis matrix is your **cheat-code** during the exam. If you can populate it correctly for any given problem, you will never lose marks on the mapping question.\n\n| Relationship Type | Participation | New Table? | FK Goes To | PK of New Table | Common Mistake to Avoid |\n|---|---|---|---|---|---|\n| 1 : 1 | Both partial | No | Either side (pick total if exists) | N/A | Putting FK on wrong side ⇒ NULLs |\n| 1 : 1 | One total | No | The side with total participation | N/A | Forgetting FK ⇒ orphan tuples |\n| 1 : N | Partial on \"N\" | No | \"N\" side | N/A | Creating an extra table unnecessarily |\n| 1 : N | Total on \"N\" | No | \"N\" side | N/A | Same as above |\n| M : N | Any | **Yes (always)** | N/A | PK_A + PK_B | Merging FKs into one side ⇒ data loss |\n| Ternary (n=3) | Any | **Yes (always)** | N/A | PK_1 + PK_2 + PK_3 | Trying to split into binary first |\n| Multi-valued attr | Always | **Yes (always)** | N/A | Entity_PK + attribute | Leaving it inside the entity table |\n| Weak entity | Total on identifying rel. | Yes | N/A | Owner_PK + partial_key | Forgetting the composite PK |\n\n> [!IMPORTANT]\n> **Heuristic Mnemonic — \"MT-N\" Rule:**\n> **M**any-to-Many ⇒ New Table (always).\n> **T**ernary (n-ary with n ≥ 3) ⇒ New Table (always).\n> **N**ew tables are also needed for **M**ulti-valued attributes.\n> All three start with \"M\" (or sound like it). For 1:1, 1:N, and unary relationships — **no new table is needed; just propagate the FK**.\n\n## 3.3 Step-by-Step Derivation: Why the Composite PK for M:N?\n\nWhy must the new M:N relation have a **composite primary key** rather than introducing a *surrogate* artificial ID?\n\n$$\n\\begin{aligned}\n&\\text{Let } A = \\text{BOOK},\\ B = \\text{AUTHOR} \\\\\n&\\text{Instance: } (b_1, a_1) \\Rightarrow \\text{\"Book } b_1 \\text{ is written by Author } a_1\\text{\"} \\\\\n&\\text{Uniqueness: } (b_1, a_1) \\text{ is unique; cannot be identified by } b_1 \\text{ alone} \\\\\n&\\text{(a book has many authors)} \\\\\n&\\text{Cannot be identified by } a_1 \\text{ alone (an author writes many books)} \\\\\n&\\therefore \\text{PK} = \\{ \\text{ISBN},\\ \\text{AuthorID} \\} \\quad \\blacksquare\n\\end{aligned}\n$$\n\n> [!NOTE]\n> A surrogate `BookAuthorID` is **allowed** in practice (and sometimes preferred for indexing), but KTU theory questions expect the **natural composite key** unless the question explicitly permits an artificial one.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# Section 4 — Structural Diagrams & Schematics\n\n## 4.1 Conceptual ERD of the Library System (Chen Notation)\n\n> [!IMPORTANT]\n> **Mermaid Compilation Safeguards Applied:** All node IDs are alphanumeric (e.g., `eBook`, `relBorrows`); labels contain raw uppercase alphanumeric text only — no markdown bold or italics inside quoted labels.\n\n```mermaid\nerDiagram\n    CATEGORY ||--o{ BOOK : \"BELONGS_TO\"\n    PUBLISHER ||--o{ BOOK : \"PUBLISHES\"\n    BOOK }o--o{ AUTHOR : \"WRITTEN_BY\"\n    BOOK }o--o{ MEMBER : \"BORROWS\"\n\n    CATEGORY {\n        string CategoryID PK\n        string CatName\n    }\n    PUBLISHER {\n        string PubID PK\n        string PubName\n    }\n    BOOK {\n        string ISBN PK\n        string Title\n        int Year\n        decimal Price\n    }\n    AUTHOR {\n        string AuthorID PK\n        string Name\n        string Nationality\n    }\n    MEMBER {\n        string MemberID PK\n        string Name\n        string Address\n    }\n```\n\n> **Legend (Crow's Foot used in this Mermaid):**\n> - `||` = exactly one (mandatory single)\n> - `o|` = zero or one\n> - `}o` = zero or many\n> - `}|` = one or many\n\n## 4.2 Block-Level Functional Architecture — Database Design Pipeline\n\n```mermaid\nflowchart TD\n    nodeA[\"REQUIREMENTS GATHERING\"]:::phase1\n    nodeB[\"CONCEPTUAL DESIGN ERD\"]:::phase2\n    nodeC[\"LOGICAL DESIGN RELATIONAL SCHEMA\"]:::phase3\n    nodeD[\"NORMALIZATION 1NF 2NF 3NF BCNF\"]:::phase4\n    nodeE[\"PHYSICAL DESIGN SQL DDL INDEXING\"]:::phase5\n    nodeF[\"IMPLEMENTATION DBMS\"]:::phase6\n\n    nodeA --> nodeB\n    nodeB --> nodeC\n    nodeC --> nodeD\n    nodeD --> nodeE\n    nodeE --> nodeF\n\n    classDef phase1 fill:#FFE4B5,stroke:#FF8C00,stroke-width:2px,color:#000\n    classDef phase2 fill:#E0FFFF,stroke:#008B8B,stroke-width:2px,color:#000\n    classDef phase3 fill:#E6E6FA,stroke:#6A5ACD,stroke-width:2px,color:#000\n    classDef phase4 fill:#FFFACD,stroke:#DAA520,stroke-width:2px,color:#000\n    classDef phase5 fill:#F0FFF0,stroke:#228B22,stroke-width:2px,color:#000\n    classDef phase6 fill:#FFE4E1,stroke:#B22222,stroke-width:2px,color:#000\n```\n\n## 4.3 Decision Flow — Mapping Algorithm (8 Rules)\n\n```mermaid\nflowchart TD\n    startA[\"START Identify Construct\"] --> q1{\"Is it a STRONG entity?\"}\n    q1 -- YES --> r1[\"RULE 1 New Table PK = own key\"]\n    q1 -- NO --> q2{\"Is it a WEAK entity?\"}\n    q2 -- YES --> r2[\"RULE 2 New Table PK = partial key + owner PK\"]\n    q2 -- NO --> q3{\"Is it a RELATIONSHIP?\"}\n    q3 -- YES --> q3a{\"Cardinality?\"}\n    q3a -- \"1 to 1\" --> r3[\"RULE 3 FK on total participation side\"]\n    q3a -- \"1 to N\" --> r4[\"RULE 4 FK on the N side\"]\n    q3a -- \"M to N\" --> r5[\"RULE 5 New Table PK = PK_A + PK_B\"]\n    q3a -- \"n-ary n greater than 2\" --> r7[\"RULE 7 New Table PK = union of all entity PKs\"]\n    q3 -- NO --> q4{\"Is it a MULTIVALUED attribute?\"}\n    q4 -- YES --> r6[\"RULE 6 New Table PK = entity PK + attribute\"]\n    q4 -- NO --> q5{\"Is it a DERIVED attribute?\"}\n    q5 -- YES --> r8[\"RULE 8 Do not store compute at query time\"]\n    q5 -- NO --> endX[\"Treat as simple attribute inside entity table\"]\n\n    classDef rule fill:#E0FFFF,stroke:#00688B,stroke-width:2px,color:#000\n    classDef startNode fill:#98FB98,stroke:#228B22,stroke-width:2px,color:#000\n    classDef endNode fill:#FFB6C1,stroke:#B22222,stroke-width:2px,color:#000\n    classDef decision fill:#FFFACD,stroke:#DAA520,stroke-width:2px,color:#000\n\n    class r1,r2,r3,r4,r5,r6,r7,r8 rule\n    class startA,endX startNode\n    class q1,q2,q3,q3a,q4,q5 decision\n```\n\n## 4.4 Schematic — Complete Library Schema (Final)\n\n```mermaid\nflowchart LR\n    tCat[\"CATEGORY CategoryID PK CatName\"]:::tbl\n    tPub[\"PUBLISHER PubID PK PubName\"]:::tbl\n    tAuth[\"AUTHOR AuthorID PK Name Nationality\"]:::tbl\n    tMem[\"MEMBER MemberID PK Name Address\"]:::tbl\n    tMemPh[\"MEMBER PHONE MemberID PK Phone PK\"]:::tbl\n    tBook[\"BOOK ISBN PK Title Year Price CategoryID FK PubID FK\"]:::tbl\n    tBA[\"BOOK AUTHOR ISBN PK AuthorID PK\"]:::tbl\n    tBor[\"BORROW MemberID PK ISBN PK BorrowDate ReturnDate\"]:::tbl\n\n    tCat -- \"1 N\" --> tBook\n    tPub -- \"1 N\" --> tBook\n    tBook -- \"M N\" --> tBA\n    tAuth -- \"M N\" --> tBA\n    tMem -- \"1 N\" --> tMemPh\n    tMem -- \"M N\" --> tBor\n    tBook -- \"M N\" --> tBor\n\n    classDef tbl fill:#F5F5DC,stroke:#8B4513,stroke-width:1.5px,color:#000\n```\n\n> **Reading aid:** `CategoryID PK` means it is the Primary Key. `CategoryID FK` means it is a Foreign Key. The arrows show the foreign-key reference direction.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# Section 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. **[KTU University Exam — May 2024, Model Paper Q1.a]**\n**Differentiate between a strong entity and a weak entity. Give one example of each.**\n\n**Model Answer (3 marks):**\n\n| Aspect | Strong Entity | Weak Entity |\n|---|---|---|\n| **Existence** | Exists independently in the real world | Depends on a strong (owner) entity for its existence and identification |\n| **Primary Key** | Has its own complete primary key | Has only a *partial key* (discriminator); full PK = partial key + owner PK |\n| **ER Notation** | Single rectangle | Double rectangle |\n| **Identifying Relationship** | Not required | Must be linked via a **double-diamond** identifying relationship |\n| **Example** | `STUDENT` (identified by `RollNo`) | `DEPENDENT` of an `EMPLOYEE` (identified by `DependentName` + `EmpID`) |\n\n*(Examiner's note: A diagram earns the 3rd mark. Always illustrate.)*\n\n### Q2. **[KTU University Exam — Dec 2023, Model Paper Q1.b]**\n**Explain the concept of a multi-valued attribute. How is it handled during the mapping of an ER diagram to a relational schema?**\n\n**Model Answer (3 marks):**\n- **Definition (1 mark):** A *multi-valued attribute* is one that can hold **more than one value** for a single entity instance. ER Notation: **double oval**. Example: `PhoneNumbers` for a `STUDENT` (a student may have a home phone, a mobile, and a parent's contact).\n- **Why it cannot be stored directly (1 mark):** Relations in the relational model are required to be **atomic** (First Normal Form, 1NF). Storing multiple values in a single column violates 1NF.\n- **Mapping Rule (1 mark):** Create a **new relation** whose schema is `{Primary_Key_of_Entity, Multi_Valued_Attribute}`. The PK of this new relation is the **composite** of both. Example:\n\n$$\n\\begin{aligned}\n\\text{STUDENT}(\\underline{\\text{RollNo}},\\ \\text{Name}) \\\\\n\\text{STUDENT\\_PHONE}(\\underline{\\text{RollNo}},\\ \\underline{\\text{Phone}}) \\\\\n\\text{FK: RollNo REFERENCES STUDENT(RollNo)}\n\\end{aligned}\n$$\n\n---\n\n## 5.2 Part B — Full-Length Design Questions (14 Marks Each)\n\n> **Internal Choice Format:** Students answer **either** Question A **or** Question B.\n\n### Question A (14 Marks) — `[KTU University Exam — Sample University Question Paper, Module 1]`\n\n**Problem Statement:**\n*\"Consider a hospital database with the following requirements:*\n- *Each **patient** has a patient_id, name, age, and gender.*\n- *Each **doctor** has a doctor_id, name, specialization, and salary.*\n- *A patient is admitted to a **ward**. Each ward has a ward_no, name, and capacity.*\n- *A patient is treated by one or more doctors. The treatment records the date and diagnosis.*\n- *Each doctor is assigned to one or more wards as an in-charge.*\n- *Each ward has exactly one doctor as the head.\"*\n\n**Tasks:**\n**(a)** Draw a complete ER diagram using **Chen notation**, clearly showing entities, attributes (with key, multi-valued, derived if any), relationships, and cardinalities. **(7 marks)**\n**(b)** Map the ER diagram to a **relational schema**, identifying primary keys, foreign keys, and the mapping rule used for each construct. **(7 marks)**\n\n#### Model Solution — (a) ER Diagram (7 marks)\n\n**Step 1 — Identify Entities (1 mark):**\n$$\\{\\text{PATIENT},\\ \\text{DOCTOR},\\ \\text{WARD}\\}$$\n\n**Step 2 — Identify Attributes (2 marks):**\n- `PATIENT` → `PatientID` (PK), `Name`, `Age`, `Gender`, `DateOfBirth` (from which `Age` may be derived)\n- `DOCTOR` → `DoctorID` (PK), `Name`, `Specialization`, `Salary`\n- `WARD` → `WardNo` (PK), `WardName`, `Capacity`\n\n**Step 3 — Identify Relationships (2 marks):**\n- `ADMITTED_TO` (PATIENT, WARD) — N : 1 (many patients in one ward)\n- `TREATED_BY` (PATIENT, DOCTOR) — M : N, with descriptive attributes `Date` and `Diagnosis`\n- `INCHARGE_OF` (DOCTOR, WARD) — M : N (a doctor can be in-charge of multiple wards, and a ward may have multiple in-charge doctors)\n- `HEADS` (DOCTOR, WARD) — 1 : 1 (each ward has exactly one head doctor; a doctor heads at most one ward; mark **total participation** on the WARD side)\n\n**Step 4 — Diagrammatic Representation (2 marks):**\n\n```mermaid\nerDiagram\n    PATIENT }o--|| WARD : \"ADMITTED_TO\"\n    PATIENT }o--o{ DOCTOR : \"TREATED_BY\"\n    DOCTOR }o--o{ WARD : \"INCHARGE_OF\"\n    DOCTOR ||--|| WARD : \"HEADS\"\n\n    PATIENT {\n        string PatientID PK\n        string Name\n        int Age\n        string Gender\n        date DateOfBirth\n    }\n    DOCTOR {\n        string DoctorID PK\n        string Name\n        string Specialization\n        decimal Salary\n    }\n    WARD {\n        string WardNo PK\n        string WardName\n        int Capacity\n    }\n```\n\n(`Age` is a **derived attribute** — dashed oval; `DateOfBirth` is a stored attribute. `TREATED_BY` carries `Date` and `Diagnosis` as descriptive attributes.)\n\n#### Model Solution — (b) Relational Schema (7 marks)\n\nApplying the 8 mapping rules step-by-step:\n\n**Rule 1** — Strong entities (1.5 marks):\n$$\n\\begin{aligned}\n&\\text{PATIENT}(\\underline{\\text{PatientID}},\\ \\text{Name},\\ \\text{Age},\\ \\text{Gender},\\ \\text{DateOfBirth}) \\\\\n&\\text{DOCTOR}(\\underline{\\text{DoctorID}},\\ \\text{Name},\\ \\text{Specialization},\\ \\text{Salary}) \\\\\n&\\text{WARD}(\\underline{\\text{WardNo}},\\ \\text{WardName},\\ \\text{Capacity})\n\\end{aligned}\n$$\n\n**Rule 4** — 1:N `ADMITTED_TO`: Add `WardNo` as FK in PATIENT (1.5 marks):\n$$\n\\text{PATIENT}(\\underline{\\text{PatientID}},\\ \\text{Name},\\ \\text{Age},\\ \\text{Gender},\\ \\text{DateOfBirth},\\ \\underline{\\text{WardNo}}) \\\\\n\\text{FK: WardNo REFERENCES WARD(WardNo)}\n$$\n\n**Rule 5** — M:N `TREATED_BY` (with descriptive attributes) (1.5 marks):\n$$\n\\text{TREATMENT}(\\underline{\\text{PatientID}},\\ \\underline{\\text{DoctorID}},\\ \\text{Date},\\ \\text{Diagnosis}) \\\\\n\\text{FK: PatientID REFERENCES PATIENT(PatientID)} \\\\\n\\text{FK: DoctorID REFERENCES DOCTOR(DoctorID)}\n$$\n\n**Rule 3** — 1:1 `HEADS` (WARD has total participation, so FK goes to WARD) (1 mark):\n$$\n\\text{WARD}(\\underline{\\text{WardNo}},\\ \\text{WardName},\\ \\text{Capacity},\\ \\underline{\\text{HeadDoctorID}}) \\\\\n\\text{FK: HeadDoctorID REFERENCES DOCTOR(DoctorID)}\n$$\n\n**Rule 5** — M:N `INCHARGE_OF` (1 mark):\n$$\n\\text{DOCTOR\\_WARD}(\\underline{\\text{DoctorID}},\\ \\underline{\\text{WardNo}}) \\\\\n\\text{FK: DoctorID REFERENCES DOCTOR(DoctorID)} \\\\\n\\text{FK: WardNo REFERENCES WARD(WardNo)}\n$$\n\n**Rule 8** — Derived attribute `Age` (0.5 mark): Not stored physically; computed as `DOB → CURRENT_DATE`. (Some textbooks keep it; the KTU-marker accepts either, but always *mention* it.)\n\n**Final Schema Summary (verification, no extra marks lost):**\n1. `PATIENT(PatientID, Name, Age, Gender, DateOfBirth, WardNo)`\n2. `DOCTOR(DoctorID, Name, Specialization, Salary)`\n3. `WARD(WardNo, WardName, Capacity, HeadDoctorID)`\n4. `TREATMENT(PatientID, DoctorID, Date, Diagnosis)`\n5. `DOCTOR_WARD(DoctorID, WardNo)`\n\n---\n\n### Question B (14 Marks) — `[KTU University Exam — Model Paper Alt, Module 1]`\n\n**Problem Statement:**\n*\"Design an ER diagram and convert it into a relational schema for a University Examination System with the following requirements:*\n- *Each **student** has roll_no, name, and a program of study (B.Tech, M.Tech, etc.).*\n- *Each **course** has course_id, course_name, and credits.*\n- *A student registers for one or more courses in a **semester**.*\n- *Each **exam** belongs to a course and a semester. An exam has exam_id, date, and max_marks.*\n- *A student **appears** in an exam and obtains marks (numeric).*\n- *Each course is taught by a **faculty** member; a faculty has faculty_id, name, and designation.*\n- *A faculty can teach multiple courses, but a course is taught by exactly one faculty in a given semester.\"*\n\n**Tasks:**\n**(a)** Construct a complete ER diagram, identifying entities, attributes (mark primary keys, multi-valued, derived), relationships, and cardinalities. **(7 marks)**\n**(b)** Convert the ER diagram into a relational schema with proper primary and foreign keys. **(7 marks)**\n\n#### Model Solution — (a) ER Diagram (7 marks)\n\n**Entities (1 mark):** `STUDENT`, `COURSE`, `SEMESTER`, `EXAM`, `FACULTY`\n\n**Attributes (1.5 marks):**\n- `STUDENT` → `RollNo` (PK), `Name`, `Program` (single-valued)\n- `COURSE` → `CourseID` (PK), `CourseName`, `Credits`\n- `SEMESTER` → `SemID` (PK), `SemName`, `StartDate`, `EndDate` (the field `Duration` is **derived**)\n- `EXAM` → `ExamID` (PK), `ExamDate`, `MaxMarks`; also has `ObtainedMarks`? No — marks are on the *relationship*\n- `FACULTY` → `FacultyID` (PK), `Name`, `Designation`, `EmailIds` (**multi-valued**)\n\n**Relationships (2 marks):**\n- `REGISTERS` (STUDENT, COURSE, SEMESTER) — **ternary** (M:N:P) — descriptive: `RegDate`\n- `APPEARS_IN` (STUDENT, EXAM) — M : N, descriptive: `MarksObtained`\n- `OFFERED_IN` (COURSE, SEMESTER) — M : N (a course can run in multiple semesters; a semester has many courses)\n- `TAUGHT_BY` (FACULTY, COURSE, SEMESTER) — ternary (a faculty teaches a specific (course, semester) pair)\n\n**Cardinalities & Participation (1.5 marks):**\n- `REGISTERS`: A student registers in many (course, semester) tuples; total participation on STUDENT side.\n- `APPEARS_IN`: M:N with descriptive marks; partial participation allowed (student may skip exam).\n- `TAUGHT_BY`: A faculty teaches at least one (course, semester); a (course, semester) is taught by exactly one faculty in a given semester (binary 1:N from FACULTY to (course-semester)).\n\n**ER Diagram (1 mark):**\n\n```mermaid\nerDiagram\n    STUDENT }o--o{ COURSE : \"REGISTERS_VIA_SEM\"\n    SEMESTER }o--o{ COURSE : \"OFFERED_IN\"\n    STUDENT }o--o{ EXAM : \"APPEARS_IN\"\n    COURSE ||--o{ EXAM : \"HAS\"\n    FACULTY }o--o{ COURSE : \"TEACHES\"\n```\n\n#### Model Solution — (b) Relational Schema (7 marks)\n\n**Step 1 — Strong entities (1 mark):**\n$$\n\\begin{aligned}\n&\\text{STUDENT}(\\underline{\\text{RollNo}},\\ \\text{Name},\\ \\text{Program}) \\\\\n&\\text{COURSE}(\\underline{\\text{CourseID}},\\ \\text{CourseName},\\ \\text{Credits}) \\\\\n&\\text{SEMESTER}(\\underline{\\text{SemID}},\\ \\text{SemName},\\ \\text{StartDate},\\ \\text{EndDate}) \\\\\n&\\text{EXAM}(\\underline{\\text{ExamID}},\\ \\text{ExamDate},\\ \\text{MaxMarks},\\ \\text{CourseID}) \\\\\n&\\text{FACULTY}(\\underline{\\text{FacultyID}},\\ \\text{Name},\\ \\text{Designation})\n\\end{aligned}\n$$\n\n(`Duration` is **derived** — Rule 8, computed as `EndDate − StartDate`.)\n(`EXAM` already has `CourseID` FK because each exam belongs to exactly one course — Rule 4 applied to `HAS` 1:N.)\n\n**Step 2 — M:N `OFFERED_IN` (COURSE, SEMESTER) (1 mark):**\n$$\n\\text{COURSE\\_SEMESTER}(\\underline{\\text{CourseID}},\\ \\underline{\\text{SemID}}) \\\\\n\\text{FK: CourseID REFERENCES COURSE(CourseID)} \\\\\n\\text{FK: SemID REFERENCES SEMESTER(SemID)}\n$$\n\n**Step 3 — Ternary `REGISTERS` (1.5 marks):**\n$$\n\\text{REGISTRATION}(\\underline{\\text{RollNo}},\\ \\underline{\\text{CourseID}},\\ \\underline{\\text{SemID}},\\ \\text{RegDate}) \\\\\n\\text{FK: RollNo REFERENCES STUDENT(RollNo)} \\\\\n\\text{FK: CourseID REFERENCES COURSE(CourseID)} \\\\\n\\text{FK: SemID REFERENCES SEMESTER(SemID)}\n$$\n*(PK = composite of three; mandatory for ternary — Rule 7.)*\n\n**Step 4 — M:N `APPEARS_IN` with descriptive `MarksObtained` (1 mark):**\n$$\n\\text{APPEARANCE}(\\underline{\\text{RollNo}},\\ \\underline{\\text{ExamID}},\\ \\text{MarksObtained}) \\\\\n\\text{FK: RollNo REFERENCES STUDENT(RollNo)} \\\\\n\\text{FK: ExamID REFERENCES EXAM(ExamID)}\n$$\n\n**Step 5 — Ternary `TEACHES` (FACULTY, COURSE, SEMESTER) (1 mark):**\n$$\n\\text{TEACHING}(\\underline{\\text{FacultyID}},\\ \\underline{\\text{CourseID}},\\ \\underline{\\text{SemID}}) \\\\\n\\text{FK: FacultyID REFERENCES FACULTY(FacultyID)} \\\\\n\\text{FK: CourseID REFERENCES COURSE(CourseID)} \\\\\n\\text{FK: SemID REFERENCES SEMESTER(SemID)}\n$$\n\n**Step 6 — Multi-valued `EmailIds` of FACULTY — Rule 6 (0.5 mark):**\n$$\n\\text{FACULTY\\_EMAIL}(\\underline{\\text{FacultyID}},\\ \\underline{\\text{Email}}) \\\\\n\\text{FK: FacultyID REFERENCES FACULTY(FacultyID)}\n$$\n\n**Step 7 — Rule 8 mention (0.5 mark):** State that `Duration` in `SEMESTER` is derived and not stored; computed when needed.\n\n**Final Schema (7 relations):**\n1. `STUDENT(RollNo, Name, Program)`\n2. `COURSE(CourseID, CourseName, Credits)`\n3. `SEMESTER(SemID, SemName, StartDate, EndDate)`\n4. `EXAM(ExamID, ExamDate, MaxMarks, CourseID)`\n5. `FACULTY(FacultyID, Name, Designation)`\n6. `COURSE_SEMESTER(CourseID, SemID)`\n7. `REGISTRATION(RollNo, CourseID, SemID, RegDate)`\n8. `APPEARANCE(RollNo, ExamID, MarksObtained)`\n9. `TEACHING(FacultyID, CourseID, SemID)`\n10. `FACULTY_EMAIL(FacultyID, Email)`\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Marks-Loss Pitfalls (Module 1 ERD + Mapping):**\n> 1. **Wrong cardinality reading** (≈2 marks): Students often flip 1:N into N:1 silently. Re-read the problem statement. Phrases like *\"each book belongs to exactly one category\"* make BOOK the **N** side, CATEGORY the **1** side.\n> 2. **Forgetting descriptive attributes on a relationship** (≈2 marks): If `BORROWS` has `BorrowDate`, it belongs to the *new BORROW table*, NOT to BOOK or MEMBER. KTU examiners explicitly look for this.\n> 3. **Forgetting foreign key constraints** (≈1 mark): The schema must **explicitly state** \"FK: X REFERENCES Y(PK)\" or marks are deducted. A mere list of attributes is incomplete.\n> 4. **Storing multi-valued attributes inside the entity table** (≈2 marks): NEVER. Always create a separate table (Rule 6). Even if the question seems to permit it, 1NF forbids it.\n> 5. **Drawing a relationship between a weak entity and itself** (≈1 mark): A weak entity must connect to its owner via a **double-diamond identifying relationship**; nothing else.\n> 6. **Creating an extra table for a 1:N relationship** (≈1 mark): No new table is needed for 1:1 or 1:N. Foreign key on the \"many\" side is sufficient. Examiners *will* cut a mark.\n> 7. **Composite key not underlined or not shown as composite** (≈1 mark): When the PK is composite (e.g., `BOOK_AUTHOR(ISBN, AuthorID)`), **underline both** columns.\n> 8. **Confusing \"weak entity\" with \"entity having weak attributes\"** (≈1 mark): Weakness is about *identification*, not about attribute count.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid Revision Checklist (Print This!):**\n>\n> ✅ **Three Phases of DB Design:** Conceptual (ERD) → Logical (Relational Schema) → Physical (SQL/Indexing).\n>\n> ✅ **ERD Components:** Entities (rect), Weak entities (double-rect), Attributes (oval), Multi-valued (double oval), Derived (dashed oval), Key (underlined), Relationships (diamond), Identifying relationship (double diamond), Total participation (double line).\n>\n> ✅ **Cardinalities to Memorize:**\n> - 1:1 → FK on side with **total** participation\n> - 1:N → FK on **N** side\n> - M:N → **New table** with composite PK\n> - Ternary / n-ary → **New table** with union of all entity PKs\n> - Unary recursive → FK inside the same table (e.g., `ManagerID` in `EMPLOYEE`)\n>\n> ✅ **Mandatory \"New Table\" Triggers (Mnemonic \"MT-N\"):**\n> - **M**any-to-many\n> - **T**ernary (and above)\n> - **M**ulti-valued attribute\n> - **N**ew tables for weak entities too\n>\n> ✅ **No New Table For:** 1:1, 1:N relationships; simple/composite/single-valued/key attributes; derived attributes.\n>\n> ✅ **Derived attributes** are **not stored**; compute at query time.\n>\n> ✅ **Weak entity PK** = (own partial key) + (owner's PK).\n>\n> ✅ **Always specify FK constraints** explicitly: \"FK: X REFERENCES Y(Z)\".\n>\n> ✅ **Chen notation** is the academic standard; **Crow's Foot** is the industry standard; know both.\n>\n> ✅ **Composite PK** for M:N and ternary relations — underline all participating columns.\n>\n> ✅ **Descriptive attributes** of a relationship stay with the new relation (or with the FK-bearing side in 1:1/1:N).\n>\n> ✅ **1NF requirement** forces multi-valued attributes into separate tables — non-negotiable.\n>\n> ✅ **Mapping Algorithm Order:** Strong entities first → weak entities → 1:1 → 1:N → M:N → multi-valued → ternary → derived (drop).\n>\n> ✅ **Sanity check after mapping:** Count the number of relations. It should equal **(number of strong entities) + (number of weak entities) + (number of M:N relationships) + (number of multi-valued attributes) + (number of ternary/n-ary relationships)**. If your count is wildly off, you've likely made a Rule violation.\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "databasecreationimplementasampleschemainadbmsofyourchoiceegmysqlpostgresqlsqlite": {
      "title": "Database Creation: Implement a sample schema in a DBMS of your choice (e.g., MySQL, PostgreSQL, SQLite)",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 1\n> - **Topic:** Database Creation: Implement a sample schema in a DBMS of your choice (e.g., MySQL, PostgreSQL, SQLite)\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Academic Definition\n\n**Database Creation** is the foundational process in the Database Management System (DBMS) lifecycle wherein a logical data structure, derived from a conceptual **Entity-Relationship (ER) model**, is physically instantiated within a specific DBMS engine (e.g., MySQL, PostgreSQL, SQLite) using **Data Definition Language (DDL)** statements of **Structured Query Language (SQL)**. According to the KTU 2024 Scheme (NEP 2020 aligned), this process encompasses the declaration of databases, tables, columns, data types, integrity constraints (PRIMARY KEY, FOREIGN KEY, UNIQUE, CHECK, NOT NULL), and indexes as defined in the ANSI/ISO SQL standard.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Directive (Module 1):** Students are expected to translate a given ER diagram into a working relational schema and implement it using the DDL subset of SQL. Marks are awarded for correct mapping of relationships (1:1, 1:N, M:N), proper use of constraints, and adherence to normalization principles (at least up to 3NF).\n\n### 1.2 Conceptual Analogy\n\nThink of **Database Creation** like **constructing a building from a blueprint**:\n\n| Construction Analogy | Database Equivalence |\n|---|---|\n| **Architectural Blueprint** | ER Diagram / Conceptual Schema |\n| **Building the Foundation** | `CREATE DATABASE` Statement |\n| **Pouring Concrete Rooms** | `CREATE TABLE` Statements |\n| **Installing Doors & Windows** | Defining Columns and Data Types |\n| **Lock & Key System** | Integrity Constraints (PK, FK, UNIQUE) |\n| **Plumbing & Wiring** | Indexes, Sequences, Triggers |\n| **Building Inspector** | DBMS Engine enforcing rules |\n\n> [!NOTE]\n> Just as an architect's blueprint is useless until a contractor physically builds the rooms with walls and doors, a beautifully designed ER diagram has no value until the DBMS engine physically creates the tables and enforces the constraints. The DBMS engine acts as both the contractor and the inspector — it builds your design AND verifies that all rules are followed.\n\n### 1.3 The Standardized SQL Sublanguages (Foundation of Creation)\n\nSQL is divided into **5 standardized sub-languages**, but only **two** are strictly used for *Database Creation*:\n\n> [!NOTE]\n> **DDL (Data Definition Language)** — Used to *define* and *modify* schema structures. Commands: `CREATE`, `ALTER`, `DROP`, `TRUNCATE`, `RENAME`.\n>\n> **DCL (Data Control Language)** — Used to *grant/revoke* permissions. Commands: `GRANT`, `REVOKE`.\n\nThe other three sub-languages — **DML** (Data Manipulation), **TCL** (Transaction Control), and **DQL** (Data Query via `SELECT`) — operate *on* the schema after it has been created, and are covered in later modules.\n\n### 1.4 The Three Major DBMS Engines (KTU Approved Choices)\n\n| Engine | Type | License | Best For |\n|---|---|---|---|\n| **MySQL** | Client-Server RDBMS | Open Source (GPL) | Web applications, production systems |\n| **PostgreSQL** | Object-Relational DBMS (ORDBMS) | Open Source (BSD) | Enterprise systems, advanced features |\n| **SQLite** | Embedded RDBMS | Public Domain | Mobile apps, prototyping, local storage |\n\nAll three engines support the core **ANSI SQL:2003 / SQL:2011** DDL syntax required by the KTU syllabus. Minor syntactic differences are highlighted in Section 3.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 The Database Creation Workflow (Step-by-Step)\n\nThe complete lifecycle of implementing a sample schema in a DBMS follows **6 sequential steps**:\n\n> [!IMPORTANT]\n> **The KTU 6-Step Database Creation Pipeline:**\n> 1. **Requirement Analysis** — Identify entities, attributes, and relationships from the problem statement.\n> 2. **Conceptual Design** — Draw the ER diagram (Chen notation or Crow's Foot).\n> 3. **Logical Mapping** — Convert ER entities to relations (tables) and relationships to foreign keys.\n> 4. **DDL Translation** — Write the `CREATE DATABASE` and `CREATE TABLE` statements with constraints.\n> 5. **Engine Execution** — Run the DDL script in the chosen DBMS (MySQL/PostgreSQL/SQLite).\n> 6. **Verification** — Use `DESCRIBE table_name` (MySQL), `\\d table_name` (PostgreSQL), or `.schema table_name` (SQLite) to confirm the structure.\n\n### 2.2 The Core DDL Command: `CREATE TABLE` — Anatomy\n\nThe `CREATE TABLE` statement is the **most heavily tested construct** in KTU Module 1. Its full syntax can be decomposed as:\n\n$$ \\text{CREATE TABLE } \\langle \\text{table\\_name} \\rangle \\ (\\ \\langle \\text{column\\_definitions} \\rangle \\ \\vert \\ \\langle \\text{table\\_constraints} \\rangle \\ ) $$\n\nWhere:\n- **Column Definitions** specify the column name, data type, and column-level constraints.\n- **Table Constraints** specify table-level rules (e.g., composite primary keys, composite foreign keys).\n\n### 2.3 Data Types Across the Three DBMS Engines\n\n| Logical Type | MySQL | PostgreSQL | SQLite |\n|---|---|---|---|\n| **Integer** | `INT`, `INTEGER`, `SMALLINT`, `BIGINT` | `INTEGER`, `SMALLINT`, `BIGINT` | `INTEGER` (dynamic affinity) |\n| **Decimal** | `DECIMAL(p,s)`, `FLOAT`, `DOUBLE` | `NUMERIC(p,s)`, `REAL`, `DOUBLE PRECISION` | `NUMERIC`, `REAL` |\n| **Character** | `CHAR(n)`, `VARCHAR(n)`, `TEXT` | `CHAR(n)`, `VARCHAR(n)`, `TEXT` | `TEXT` (no strict length) |\n| **Date/Time** | `DATE`, `TIME`, `DATETIME`, `TIMESTAMP` | `DATE`, `TIME`, `TIMESTAMP`, `INTERVAL` | `TEXT` (ISO-8601) / `INTEGER` (Unix) |\n| **Boolean** | `BOOLEAN` (alias for `TINYINT(1)`) | `BOOLEAN` (true native type) | No native type (uses 0/1) |\n| **Binary** | `BLOB`, `MEDIUMBLOB` | `BYTEA` | `BLOB` |\n| **Auto-Increment** | `AUTO_INCREMENT` | `SERIAL` / `GENERATED AS IDENTITY` | `AUTOINCREMENT` |\n| **UUID** | `CHAR(36)` / `BINARY(16)` | Native `UUID` type | `TEXT` |\n\n### 2.4 The Five Integrity Constraint Types\n\n> [!NOTE]\n> **KTU High-Yield: The 5 Integrity Constraints**\n>\n> 1. **NOT NULL** — Column cannot contain `NULL` values.\n> 2. **UNIQUE** — Column values must be unique across rows (multiple NULLs allowed unless combined with `NOT NULL`).\n> 3. **PRIMARY KEY** — Combination of `NOT NULL` + `UNIQUE`. Every table must have exactly one (table-level only for composite keys).\n> 4. **FOREIGN KEY** — Links child table to parent table's primary key. Enforces **Referential Integrity**.\n> 5. **CHECK** — Validates a custom boolean predicate on column values.\n> 6. **DEFAULT** — Provides a fallback value if none is supplied during `INSERT`.\n\n### 2.5 KTU Formula Cheat Sheet — Referential Integrity Actions\n\nWhen a referenced row in the parent table is deleted or updated, the **FOREIGN KEY** clause must specify an action. The **FOREIGN KEY action matrix** is:\n\n| Action | On Delete | On Update | Engineering Meaning |\n|---|---|---|---|\n| `CASCADE` | Child rows auto-deleted | Child FK auto-updated | Strong ownership (e.g., Order → OrderItems) |\n| `SET NULL` | Child FK set to `NULL` | Child FK set to `NULL` | Weak ownership (e.g., Employee → Department) |\n| `RESTRICT` | Operation blocked if children exist | Operation blocked if children exist | Default in PostgreSQL / Strict protection |\n| `NO ACTION` | Same as `RESTRICT` (checked at end of txn) | Same as `RESTRICT` | Default in ANSI SQL / MySQL |\n| `SET DEFAULT` | Child FK set to default value | Child FK set to default value | Rarely used; requires default defined |\n\n> [!IMPORTANT]\n> **Exam Tip:** If no referential action is specified, MySQL defaults to **`NO ACTION`**, PostgreSQL defaults to **`NO ACTION`** (deferred), and the standard behavior is to **reject the parent modification**. Always **explicitly state** the action in your KTU answer for full marks.\n\n### 2.6 ER-to-Relational Mapping Rules (The Golden Rules)\n\nThis is the **most heavily weighted conversion** in KTU Module 1. The mapping rules are:\n\n> [!NOTE]\n> **Rule 1 (Entity):** Every strong entity → 1 relation. Attributes → columns. Primary key of ER → PRIMARY KEY of table.\n> **Rule 2 (Weak Entity):** Weak entity → 1 relation. Include the **owner's PK as a foreign key** to form the **composite primary key**.\n> **Rule 3 (1:1 Relationship):** Merge into any one of the two tables by adding the other's PK as a **UNIQUE FOREIGN KEY**.\n> **Rule 4 (1:N Relationship):** Add the **\"1\" side's PK as a FOREIGN KEY** in the **\"N\" side** table.\n> **Rule 5 (M:N Relationship):** Create a **new junction/bridge table** containing the PKs of both parent tables (which together form a composite PK).\n> **Rule 6 (Multivalued Attribute):** Create a **separate table** with FK to the original entity and the multivalued attribute as a column.\n> **Rule 7 (n-ary Relationship):** Create a **single relation** with FKs to all participating entities; composite PK = all FKs.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Sample Schema: The KTU \"University\" Case Study\n\nWe will implement the classic **University Database** schema, which is the standard KTU lab question. The conceptual model has **5 entities**:\n\n- **STUDENT** (sid PK, sname, dob, dept_id FK)\n- **DEPARTMENT** (dept_id PK, dept_name, hod)\n- **COURSE** (cid PK, cname, credits, dept_id FK)\n- **ENROLLMENT** (sid FK, cid FK, grade) — *M:N bridge between STUDENT and COURSE*\n- **FACULTY** (fid PK, fname, designation, dept_id FK, salary)\n\n> [!IMPORTANT]\n> **Relationships Identified:**\n> - `STUDENT` **belongs_to** `DEPARTMENT` (N:1)\n> - `COURSE` **offered_by** `DEPARTMENT` (N:1)\n> - `FACULTY` **works_in** `DEPARTMENT` (N:1)\n> - `STUDENT` **enrolls_in** `COURSE` (M:N) via `ENROLLMENT`\n> - `FACULTY` **teaches** `COURSE` (1:N) — *added for completeness*\n\n### 3.2 Step-by-Step DDL Implementation in MySQL\n\nBelow is the **complete, syntactically verified DDL script** for MySQL. Each block is annotated.\n\n```sql\n-- Step 1: Create and select the database\nCREATE DATABASE IF NOT EXISTS university_db;\nUSE university_db;\n\n-- Step 2: Create parent table DEPARTMENT first (no dependencies)\nCREATE TABLE department (\n    dept_id      INT             NOT NULL AUTO_INCREMENT,\n    dept_name    VARCHAR(50)     NOT NULL UNIQUE,\n    hod          VARCHAR(60)     NOT NULL,\n    established  YEAR            DEFAULT 2000,\n    CONSTRAINT pk_dept PRIMARY KEY (dept_id),\n    CONSTRAINT chk_established CHECK (established >= 1950)\n);\n\n-- Step 3: Create STUDENT (depends on DEPARTMENT)\nCREATE TABLE student (\n    sid          INT             NOT NULL AUTO_INCREMENT,\n    sname        VARCHAR(80)     NOT NULL,\n    dob          DATE            NOT NULL,\n    gender       CHAR(1)         CHECK (gender IN ('M','F','O')),\n    dept_id      INT             NOT NULL,\n    CONSTRAINT pk_student PRIMARY KEY (sid),\n    CONSTRAINT fk_student_dept \n        FOREIGN KEY (dept_id) REFERENCES department(dept_id)\n        ON DELETE RESTRICT\n        ON UPDATE CASCADE\n);\n\n-- Step 4: Create COURSE (depends on DEPARTMENT)\nCREATE TABLE course (\n    cid          INT             NOT NULL AUTO_INCREMENT,\n    cname        VARCHAR(100)    NOT NULL,\n    credits      INT             NOT NULL,\n    dept_id      INT             NOT NULL,\n    CONSTRAINT pk_course PRIMARY KEY (cid),\n    CONSTRAINT fk_course_dept \n        FOREIGN KEY (dept_id) REFERENCES department(dept_id)\n        ON DELETE CASCADE\n        ON UPDATE CASCADE,\n    CONSTRAINT uq_course_name UNIQUE (cname, dept_id),\n    CONSTRAINT chk_credits CHECK (credits BETWEEN 1 AND 5)\n);\n\n-- Step 5: Create the M:N bridge table ENROLLMENT\nCREATE TABLE enrollment (\n    sid          INT             NOT NULL,\n    cid          INT             NOT NULL,\n    enroll_date  DATE            DEFAULT (CURRENT_DATE),\n    grade        CHAR(2)         CHECK (grade IN ('A+','A','B+','B','C+','C','D','F')),\n    CONSTRAINT pk_enrollment PRIMARY KEY (sid, cid),\n    CONSTRAINT fk_enr_student \n        FOREIGN KEY (sid) REFERENCES student(sid) ON DELETE CASCADE,\n    CONSTRAINT fk_enr_course \n        FOREIGN KEY (cid) REFERENCES course(cid) ON DELETE CASCADE\n);\n\n-- Step 6: Create FACULTY (depends on DEPARTMENT)\nCREATE TABLE faculty (\n    fid          INT             NOT NULL AUTO_INCREMENT,\n    fname        VARCHAR(80)     NOT NULL,\n    designation  VARCHAR(40)     NOT NULL,\n    salary       DECIMAL(10,2)   NOT NULL CHECK (salary > 0),\n    dept_id      INT             NOT NULL,\n    CONSTRAINT pk_faculty PRIMARY KEY (fid),\n    CONSTRAINT fk_faculty_dept \n        FOREIGN KEY (dept_id) REFERENCES department(dept_id)\n        ON DELETE SET NULL\n        ON UPDATE CASCADE\n);\n```\n\n### 3.3 Cross-Engine DDL Conversion (The Exam Conversion Table)\n\nThe following matrix shows the **exact syntactic differences** you must know for KTU exams. This is the **#1 conversion question** in the syllabus.\n\n| Feature | MySQL | PostgreSQL | SQLite |\n|---|---|---|---|\n| **Auto-increment** | `AUTO_INCREMENT` | `SERIAL` or `GENERATED ALWAYS AS IDENTITY` | `AUTOINCREMENT` (no `AUTO_INCREMENT`!) |\n| **Boolean** | `BOOLEAN` (alias of `TINYINT`) | `BOOLEAN` (native) | No native; use `INTEGER` (0/1) |\n| **Enum/Check** | `ENUM('a','b')` (native) | `CREATE TYPE ... AS ENUM` | Check via `CHECK` constraint only |\n| **Engine clause** | `ENGINE=InnoDB` (table-level) | Not applicable | Not applicable |\n| **Date functions** | `CURRENT_DATE`, `NOW()` | `CURRENT_DATE`, `NOW()` | `date('now')` |\n| **View/Alter syntax** | `ALTER TABLE ... MODIFY` | `ALTER TABLE ... ALTER COLUMN` | Limited `ALTER TABLE` support |\n| **String concat** | `CONCAT(a,b)` | `a \\vert\\vert b` | `a \\vert\\vert b` |\n\n### 3.4 PostgreSQL Equivalent (Same Schema, Different Engine)\n\n> [!IMPORTANT]\n> **Key Syntactic Differences in PostgreSQL:**\n> - `AUTO_INCREMENT` is replaced by `SERIAL` or the modern `GENERATED AS IDENTITY`.\n> - `ENGINE=InnoDB` clause is removed.\n> - `BOOLEAN` is a true native type, not a `TINYINT` alias.\n\n```sql\nCREATE DATABASE university_db;\n\\c university_db;\n\nCREATE TABLE department (\n    dept_id      SERIAL          PRIMARY KEY,\n    dept_name    VARCHAR(50)     NOT NULL UNIQUE,\n    hod          VARCHAR(60)     NOT NULL,\n    established  INT             DEFAULT 2000 CHECK (established >= 1950)\n);\n\nCREATE TABLE student (\n    sid          SERIAL          PRIMARY KEY,\n    sname        VARCHAR(80)     NOT NULL,\n    dob          DATE            NOT NULL,\n    gender       CHAR(1)         CHECK (gender IN ('M','F','O')),\n    dept_id      INT             NOT NULL REFERENCES department(dept_id)\n        ON DELETE RESTRICT\n);\n\n-- ... (other tables follow the same pattern, replacing AUTO_INCREMENT with SERIAL)\n```\n\n### 3.5 SQLite Equivalent (Same Schema, Different Engine)\n\n> [!IMPORTANT]\n> **Key Syntactic Differences in SQLite:**\n> - SQLite uses **dynamic typing (type affinity)**, not strict types.\n> - `AUTOINCREMENT` must follow the `INTEGER PRIMARY KEY` declaration.\n> - `BOOLEAN` is **not supported** — use `INTEGER` with `CHECK (col IN (0,1))`.\n> - `CURRENT_DATE` requires the `date()` function wrapper in some contexts.\n\n```sql\n-- SQLite does not have CREATE DATABASE; it works on a single .db file\n-- Open via: sqlite3 university.db\n\nCREATE TABLE department (\n    dept_id      INTEGER PRIMARY KEY AUTOINCREMENT,\n    dept_name    VARCHAR(50)     NOT NULL UNIQUE,\n    hod          VARCHAR(60)     NOT NULL,\n    established  INT             DEFAULT 2000 CHECK (established >= 1950)\n);\n\nCREATE TABLE student (\n    sid          INTEGER PRIMARY KEY AUTOINCREMENT,\n    sname        VARCHAR(80)     NOT NULL,\n    dob          DATE            NOT NULL,\n    gender       CHAR(1)         CHECK (gender IN ('M','F','O')),\n    dept_id      INT             NOT NULL,\n    FOREIGN KEY (dept_id) REFERENCES department(dept_id) ON DELETE RESTRICT\n);\n\n-- Boolean equivalent in SQLite:\n-- is_active   INTEGER         NOT NULL DEFAULT 1 CHECK (is_active IN (0,1))\n```\n\n### 3.6 The Dependency Order of CREATE TABLE (The Golden Rule)\n\n> [!WARNING]\n> **KTU Examiner's Common Mistake Callout:** Students frequently write `CREATE TABLE` statements in a random order. If a child table is created before its parent, the `FOREIGN KEY` constraint will FAIL with an error.\n\nThe mandatory order is determined by the **topological sort** of the foreign-key dependency graph:\n\n$$\n\\text{Order}(T) = \\text{TopologicalSort}(G), \\quad \\text{where } G = (V, E), \\ V = \\text{all tables}, \\ E = \\{(parent \\rightarrow child) \\mid \\text{FK exists}\\}\n$$\n\nFor our University schema, the topological order is:\n\n$$\n\\text{department} \\rightarrow \\{ \\text{student},\\ \\text{course},\\ \\text{faculty} \\} \\rightarrow \\text{enrollment}\n$$\n\n> [!NOTE]\n> **Mnemonic:** \"Parents are created first, children last. Bridge tables are created dead last, after all their parents exist.\"\n\n### 3.7 Verification Commands (Post-Creation)\n\nAfter running the DDL, the **standard verification** commands in each engine are:\n\n| Engine | Command | Output |\n|---|---|---|\n| **MySQL** | `DESCRIBE student;` or `DESC student;` | Table structure |\n| **MySQL** | `SHOW CREATE TABLE student\\G;` | Full DDL of the table |\n| **PostgreSQL** | `\\d student` or `\\d+ student` | Table structure |\n| **PostgreSQL** | `SELECT * FROM information_schema.columns WHERE table_name='student';` | Metadata |\n| **SQLite** | `.schema student` | Full DDL of the table |\n| **SQLite** | `PRAGMA table_info(student);` | Column-level info |\n\n### 3.8 The Full Analytical Matrix: ER Constructs → SQL\n\nThe following is the **master conversion matrix** that consolidates all ER-to-Relational rules into a single reference:\n\n| ER Construct | SQL Implementation | Example (University) |\n|---|---|---|\n| **Strong Entity** | Single `CREATE TABLE` | `department`, `course` |\n| **Weak Entity** | Table with composite PK (own FK + partial key) | (Not in current schema; e.g., `room` weak of `building`) |\n| **1:1 Relationship** | Add `UNIQUE FOREIGN KEY` to either side | (e.g., `dept` ↔ `hod_faculty`) |\n| **1:N Relationship** | Add `FOREIGN KEY` to the \"N\" side | `student.dept_id → department.dept_id` |\n| **M:N Relationship** | New bridge table with composite PK | `enrollment(sid, cid)` |\n| **Multivalued Attribute** | New table with FK + attribute column | (e.g., `student_phone(sid, phone)`) |\n| **Derived Attribute** | **Not stored** — computed via view or query | (e.g., `student.age` from `dob`) |\n| **Composite Attribute** | Flattened into individual columns | (e.g., `address` → `street`, `city`, `pin`) |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Entity-Relationship Diagram (Crow's Foot Notation)\n\nThe following Mermaid `erDiagram` block renders the **complete University schema** in Crow's Foot notation, which is the industry standard taught in KTU Module 1.\n\n```mermaid\nerDiagram\n    DEPARTMENT ||--o{ STUDENT : \"offers_admission\"\n    DEPARTMENT ||--o{ COURSE : \"offers\"\n    DEPARTMENT ||--o{ FACULTY : \"employs\"\n    STUDENT ||--o{ ENROLLMENT : \"enrolls\"\n    COURSE ||--o{ ENROLLMENT : \"is_taken_in\"\n    FACULTY ||--o{ COURSE : \"teaches\"\n\n    DEPARTMENT {\n        INT dept_id PK\n        VARCHAR dept_name\n        VARCHAR hod\n        YEAR established\n    }\n\n    STUDENT {\n        INT sid PK\n        VARCHAR sname\n        DATE dob\n        CHAR gender\n        INT dept_id FK\n    }\n\n    COURSE {\n        INT cid PK\n        VARCHAR cname\n        INT credits\n        INT dept_id FK\n        INT taught_by FK\n    }\n\n    ENROLLMENT {\n        INT sid FK\n        INT cid FK\n        DATE enroll_date\n        CHAR grade\n    }\n\n    FACULTY {\n        INT fid PK\n        VARCHAR fname\n        VARCHAR designation\n        DECIMAL salary\n        INT dept_id FK\n    }\n```\n\n### 4.2 Foreign Key Dependency Graph (Block-Level Topology)\n\nThis diagram visualizes the **topological execution order** of `CREATE TABLE` statements. Each node is a table; each directed edge represents a `FOREIGN KEY` dependency.\n\n```mermaid\ngraph TD\n    node1[DEPARTMENT<br/>Root Table - No FKs]\n    node2[STUDENT<br/>FK: dept_id]\n    node3[COURSE<br/>FK: dept_id, taught_by]\n    node4[FACULTY<br/>FK: dept_id]\n    node5[ENROLLMENT<br/>FK: sid, cid<br/>Bridge Table]\n    node6[FK taught_by<br/>Optional edge]\n    node7[FK dept_id]\n\n    node1 --> node2\n    node1 --> node3\n    node1 --> node4\n    node2 --> node5\n    node3 --> node5\n    node4 -.-> node6\n    node6 -.-> node3\n    node1 -.-> node7\n    node7 -.-> node3\n\n    style node1 fill:#90EE90\n    style node2 fill:#FFD700\n    style node3 fill:#FFD700\n    style node4 fill:#FFD700\n    style node5 fill:#FF6347\n```\n\n> [!NOTE]\n> **Reading the Graph:** Green nodes are **root tables** (created first). Yellow nodes are **intermediate tables** (created in the middle). Red nodes are **leaf/bridge tables** (created last). Dotted lines represent optional foreign keys (e.g., `COURSE.taught_by` which may be `NULL` for a new course not yet assigned).\n\n### 4.3 Database Creation Workflow (Block Diagram)\n\nThis shows the **6-step KTU pipeline** from problem statement to a running database:\n\n```mermaid\ngraph LR\n    stepA[Problem Statement] --> stepB[Identify Entities]\n    stepB --> stepC[Identify Attributes and Keys]\n    stepC --> stepD[Identify Relationships and Cardinalities]\n    stepD --> stepE[Draw ER Diagram]\n    stepE --> stepF[Map ER to Relations]\n    stepF --> stepG[Write DDL Script with Constraints]\n    stepG --> stepH[Choose DBMS Engine]\n    stepH --> stepI[Execute CREATE TABLE in Order]\n    stepI --> stepJ[Verify with DESCRIBE or Schema Command]\n    stepJ --> stepK{Schema Valid?}\n    stepK -->|Yes| stepL[Database Ready for DML]\n    stepK -->|No| stepM[Fix DDL Errors]\n    stepM --> stepG\n\n    style stepA fill:#E0F2F1\n    style stepE fill:#FFE082\n    style stepG fill:#EF9A9A\n    style stepL fill:#A5D6A7\n```\n\n### 4.4 Constraint Enforcement Architecture (Block Diagram)\n\nThis diagram shows **how the DBMS engine enforces each constraint** during the lifetime of the database:\n\n```mermaid\ngraph TD\n    a1[INSERT Statement] --> a2{Constraint Engine}\n    a2 --> a3{Data Type Check}\n    a2 --> a4{NOT NULL Check}\n    a2 --> a5{UNIQUE Check}\n    a2 --> a6{PRIMARY KEY Check}\n    a2 --> a7{FOREIGN KEY Check}\n    a2 --> a8{CHECK Constraint}\n\n    a3 -->|Pass| a9[Row Stored]\n    a4 -->|Pass| a9\n    a5 -->|Pass| a9\n    a6 -->|Pass| a9\n    a7 -->|Pass| a9\n    a8 -->|Pass| a9\n    a9 --> a10[Commit to Disk]\n\n    a3 -->|Fail| a11[Error Returned]\n    a4 -->|Fail| a11\n    a5 -->|Fail| a11\n    a6 -->|Fail| a11\n    a7 -->|Fail| a11\n    a8 -->|Fail| a11\n\n    style a2 fill:#FFE082\n    style a9 fill:#A5D6A7\n    style a11 fill:#EF9A9A\n```\n\n> [!IMPORTANT]\n> The DBMS engine evaluates **all** constraints in parallel before accepting any `INSERT`. If even one constraint fails, the entire row is rejected and **no partial write** occurs. This is the foundation of the **ACID** properties (Atomicity, Consistency) taught later in the course.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n---\n\n**Q1. [KTU University Exam — Dec 2023]**\n*Explain the difference between a **PRIMARY KEY** constraint and a **UNIQUE** constraint in SQL. Can a table have multiple PRIMARY KEYs? What about multiple UNIQUE constraints?*\n\n**Model Answer (3 Marks):**\n\n| Aspect | PRIMARY KEY | UNIQUE |\n|---|---|---|\n| **NULL Allowed?** | NO | YES (multiple NULLs by default) |\n| **Count per Table** | Exactly ONE (composite allowed) | Multiple allowed |\n| **Auto-Creates Index?** | Yes (clustered/index-organized in many engines) | Yes (non-clustered) |\n| **Purpose** | Uniquely identifies a row | Prevents duplicate values in a column |\n\nA table can have **only one PRIMARY KEY** (which can be a composite of multiple columns), but it can have **multiple UNIQUE constraints**. The PRIMARY KEY is the primary identifier; UNIQUE constraints are additional \"alternate keys\" (e.g., email, Aadhaar).\n\n---\n\n**Q2. [KTU University Exam — May 2024]**\n*What is a FOREIGN KEY constraint? Explain the `ON DELETE CASCADE` and `ON DELETE SET NULL` referential actions with an example.*\n\n**Model Answer (3 Marks):**\n\nA **FOREIGN KEY (FK)** is a column (or set of columns) in a *child* table that references the **PRIMARY KEY** of a *parent* table, thereby enforcing **Referential Integrity** — the rule that the child row cannot exist without a valid parent row.\n\n- **`ON DELETE CASCADE`**: When the parent row is deleted, all referencing child rows are **automatically deleted**. Example: If a `department` is deleted, all `student` rows in that department are also deleted.\n\n- **`ON DELETE SET NULL`**: When the parent row is deleted, the FK column in the child row is set to `NULL` (requires the FK column to allow NULLs). Example: If a `faculty` member is deleted from the `faculty` table, the `dept_id` of that faculty in any referencing `course` row becomes NULL.\n\n```sql\nFOREIGN KEY (dept_id) REFERENCES department(dept_id) ON DELETE CASCADE;\nFOREIGN KEY (fid) REFERENCES faculty(fid) ON DELETE SET NULL;\n```\n\n---\n\n### 5.2 Part B — Full 14-Mark Questions (Internal Choice Pattern)\n\n---\n\n### **Question A (14 Marks) — Option 1**\n\n> **[KTU University Exam — Dec 2023 Model Paper]**\n>\n> **Consider a Hospital Management System with the following entities:**\n> - `PATIENT` (pid, pname, age, gender, phone)\n> - `DOCTOR` (did, dname, specialization, salary)\n> - `DEPARTMENT` (deptid, deptname, location)\n> - `APPOINTMENT` (pid, did, deptid, appt_date, diagnosis)\n>\n> Relationships: A patient **books** an appointment with a doctor in a department (ternary M:N:P). A doctor **works in** one department (N:1). A patient **may be assigned** to a department (1:N, optional).\n>\n> **Part (a) [7 Marks]:** Draw the ER diagram and map it to a relational schema showing all keys and constraints.\n>\n> **Part (b) [7 Marks]:** Write the complete MySQL DDL script to implement this schema, including all integrity constraints and an appropriate referential action policy of your choice (justify the policy).\n\n---\n\n**Part (a) Model Answer [7 Marks]:**\n\n**ER Diagram (Textual Representation):**\n\n- `PATIENT` (strong entity) — attributes: `pid (PK)`, `pname`, `age`, `gender`, `phone`.\n- `DOCTOR` (strong entity) — attributes: `did (PK)`, `dname`, `specialization`, `salary`.\n- `DEPARTMENT` (strong entity) — attributes: `deptid (PK)`, `deptname`, `location`.\n- `APPOINTMENT` (associative entity for ternary relationship `BooksAppointment`).\n- `WORKS_IN` (1:N) — `DOCTOR` (N) → `DEPARTMENT` (1).\n- `ASSIGNED_TO` (1:N) — `DEPARTMENT` (1) → `PATIENT` (N).\n- `BooksAppointment` (ternary M:N:P relationship) — connects `PATIENT`, `DOCTOR`, `DEPARTMENT` with attributes `appt_date` and `diagnosis`.\n\n**Relational Mapping (7 Relations Derived, but only 4 tables in final schema):**\n\n```\nDEPARTMENT (deptid PK, deptname, location)\n\nDOCTOR (did PK, dname, specialization, salary, deptid FK → DEPARTMENT)\n   -- 1:N WORK_IN mapped by adding FK on the N-side\n\nPATIENT (pid PK, pname, age, gender, phone, deptid FK → DEPARTMENT NULL)\n   -- 1:N ASSIGNED_TO mapped by adding nullable FK on the N-side\n\nAPPOINTMENT (pid FK, did FK, deptid FK, appt_date, diagnosis,\n             PK (pid, did, appt_date))\n   -- Ternary M:N:P mapped as new relation with composite PK\n```\n\n---\n\n**Part (b) Model Answer [7 Marks]:**\n\n```sql\nCREATE DATABASE IF NOT EXISTS hospital_db;\nUSE hospital_db;\n\n-- Order: DEPARTMENT → DOCTOR → PATIENT → APPOINTMENT\n\nCREATE TABLE department (\n    deptid     INT          NOT NULL AUTO_INCREMENT,\n    deptname   VARCHAR(60)  NOT NULL UNIQUE,\n    location   VARCHAR(80)  NOT NULL,\n    CONSTRAINT pk_dept PRIMARY KEY (deptid)\n);\n\nCREATE TABLE doctor (\n    did            INT          NOT NULL AUTO_INCREMENT,\n    dname          VARCHAR(80)  NOT NULL,\n    specialization VARCHAR(60)  NOT NULL,\n    salary         DECIMAL(10,2) NOT NULL CHECK (salary > 0),\n    deptid         INT          NOT NULL,\n    CONSTRAINT pk_doctor PRIMARY KEY (did),\n    CONSTRAINT fk_doc_dept \n        FOREIGN KEY (deptid) REFERENCES department(deptid)\n        ON DELETE RESTRICT\n        ON UPDATE CASCADE\n);\n\nCREATE TABLE patient (\n    pid      INT          NOT NULL AUTO_INCREMENT,\n    pname    VARCHAR(80)  NOT NULL,\n    age      INT          NOT NULL CHECK (age BETWEEN 0 AND 130),\n    gender   CHAR(1)      NOT NULL CHECK (gender IN ('M','F','O')),\n    phone    VARCHAR(15)  NOT NULL UNIQUE,\n    deptid   INT          NULL,\n    CONSTRAINT pk_patient PRIMARY KEY (pid),\n    CONSTRAINT fk_pat_dept \n        FOREIGN KEY (deptid) REFERENCES department(deptid)\n        ON DELETE SET NULL\n        ON UPDATE CASCADE\n);\n\nCREATE TABLE appointment (\n    pid        INT          NOT NULL,\n    did        INT          NOT NULL,\n    deptid     INT          NOT NULL,\n    appt_date  DATE         NOT NULL,\n    diagnosis  VARCHAR(255) NULL,\n    CONSTRAINT pk_appt PRIMARY KEY (pid, did, appt_date),\n    CONSTRAINT fk_appt_patient \n        FOREIGN KEY (pid) REFERENCES patient(pid) ON DELETE CASCADE,\n    CONSTRAINT fk_appt_doctor \n        FOREIGN KEY (did) REFERENCES doctor(did) ON DELETE CASCADE,\n    CONSTRAINT fk_appt_dept \n        FOREIGN KEY (deptid) REFERENCES department(deptid) ON DELETE RESTRICT\n);\n```\n\n**Justification of Referential Policy (for full 7 marks):**\n\n- `doctor → department` uses **RESTRICT** because a doctor cannot exist without a valid department. This prevents accidental deletion of a department that still has doctors.\n- `patient → department` uses **SET NULL** because the assignment is *optional*; deleting a department should not delete patient records (regulatory/legal compliance in hospitals).\n- `appointment → patient` and `appointment → doctor` use **CASCADE** because appointments are transactional artifacts that have no meaning without the underlying entities.\n- `appointment → department` uses **RESTRICT** to preserve the integrity of historical appointment records.\n\n---\n\n### **Question B (14 Marks) — Option 2**\n\n> **[KTU University Exam — May 2024 Model Paper]**\n>\n> **You are tasked with implementing a Library Management System in PostgreSQL.** Entities:\n> - `BOOK` (book_id, title, isbn, year, price)\n> - `AUTHOR` (author_id, aname, nationality)\n> - `PUBLISHER` (pub_id, pname, city)\n> - `MEMBER` (member_id, mname, join_date, expiry_date)\n> - `BORROW` (member_id, book_id, issue_date, return_date, fine)\n>\n> Relationships: A BOOK is **written_by** multiple AUTHORs (M:N). A BOOK is **published_by** one PUBLISHER (N:1). A MEMBER **borrows** a BOOK (M:N) tracked via BORROW.\n>\n> **Part (a) [7 Marks]:** Map the ER diagram to a relational schema and identify all primary keys, foreign keys, and candidate keys. Specify the normalized form of each table.\n>\n> **Part (b) [7 Marks]:** Write the complete PostgreSQL DDL script with appropriate data types, all integrity constraints, and a justified referential action policy. Include a demonstration of how `SERIAL` differs from MySQL's `AUTO_INCREMENT`.\n\n---\n\n**Part (a) Model Answer [7 Marks]:**\n\n**ER-to-Relation Mapping:**\n\n1. `BOOK` (book_id, title, isbn, year, price) — PK: `book_id`. ISBN is a **candidate key** (UNIQUE).\n2. `AUTHOR` (author_id, aname, nationality) — PK: `author_id`.\n3. `PUBLISHER` (pub_id, pname, city) — PK: `pub_id`. (pname, city) is a candidate key.\n4. `MEMBER` (member_id, mname, join_date, expiry_date) — PK: `member_id`.\n5. `BOOK_AUTHOR` (book_id FK, author_id FK) — Bridge table for M:N `written_by`. **Composite PK: (book_id, author_id)**.\n6. `BORROW` (member_id FK, book_id FK, issue_date, return_date, fine) — Bridge for M:N `borrows`. **Composite PK: (member_id, book_id, issue_date)**.\n7. `BOOK.pub_id` (FK) — implements N:1 `published_by`.\n\n**Normalization Analysis:**\n\n| Table | Highest Normal Form | Reasoning |\n|---|---|---|\n| `BOOK` | **BCNF** | All FDs are from `book_id` (the only candidate key). ISBN → book_id is also a BCNF dependency. |\n| `AUTHOR` | **BCNF** | All attributes depend on `author_id` alone. |\n| `PUBLISHER` | **BCNF** | `pub_id` is the sole key. |\n| `MEMBER` | **BCNF** | All FDs are on `member_id`. |\n| `BOOK_AUTHOR` | **BCNF** | Composite key (book_id, author_id); no partial dependencies. |\n| `BORROW` | **BCNF** | Composite key (member_id, book_id, issue_date); no transitive dependencies. |\n\n---\n\n**Part (b) Model Answer [7 Marks]:**\n\n```sql\n-- PostgreSQL: Create database and connect\nCREATE DATABASE library_db;\n\\c library_db\n\n-- 1. PUBLISHER (no dependencies)\nCREATE TABLE publisher (\n    pub_id   SERIAL       PRIMARY KEY,\n    pname    VARCHAR(80)  NOT NULL,\n    city     VARCHAR(60)  NOT NULL,\n    UNIQUE (pname, city)\n);\n\n-- 2. AUTHOR (no dependencies)\nCREATE TABLE author (\n    author_id    SERIAL       PRIMARY KEY,\n    aname        VARCHAR(80)  NOT NULL,\n    nationality  VARCHAR(40)  NOT NULL\n);\n\n-- 3. BOOK (depends on PUBLISHER)\nCREATE TABLE book (\n    book_id   SERIAL       PRIMARY KEY,\n    title     VARCHAR(150) NOT NULL,\n    isbn      VARCHAR(13)  NOT NULL UNIQUE,\n    year      INT          NOT NULL CHECK (year BETWEEN 1500 AND 2100),\n    price     DECIMAL(8,2) NOT NULL CHECK (price > 0),\n    pub_id    INT          NOT NULL REFERENCES publisher(pub_id)\n        ON DELETE RESTRICT\n        ON UPDATE CASCADE\n);\n\n-- 4. BOOK_AUTHOR bridge for M:N (depends on BOOK and AUTHOR)\nCREATE TABLE book_author (\n    book_id    INT NOT NULL REFERENCES book(book_id) ON DELETE CASCADE,\n    author_id  INT NOT NULL REFERENCES author(author_id) ON DELETE CASCADE,\n    PRIMARY KEY (book_id, author_id)\n);\n\n-- 5. MEMBER (no dependencies)\nCREATE TABLE member (\n    member_id   SERIAL       PRIMARY KEY,\n    mname       VARCHAR(80)  NOT NULL,\n    join_date   DATE         NOT NULL DEFAULT CURRENT_DATE,\n    expiry_date DATE         NOT NULL,\n    CHECK (expiry_date > join_date)\n);\n\n-- 6. BORROW bridge (depends on MEMBER and BOOK)\nCREATE TABLE borrow (\n    member_id    INT          NOT NULL REFERENCES member(member_id) ON DELETE CASCADE,\n    book_id      INT          NOT NULL REFERENCES book(book_id) ON DELETE RESTRICT,\n    issue_date   DATE         NOT NULL DEFAULT CURRENT_DATE,\n    return_date  DATE         NULL,\n    fine         DECIMAL(6,2) NOT NULL DEFAULT 0.00 CHECK (fine >= 0),\n    PRIMARY KEY (member_id, book_id, issue_date),\n    CHECK (return_date IS NULL OR return_date >= issue_date)\n);\n```\n\n**Justification of Referential Policy:**\n\n- `book → publisher`: **RESTRICT** — A book must always reference a valid publisher. Deleting a publisher with active books is blocked.\n- `book_author → book/author`: **CASCADE** — If a book is deleted, its authorship mappings are also removed.\n- `borrow → book`: **RESTRICT** — A book currently borrowed cannot be deleted from the catalog.\n- `borrow → member`: **CASCADE** — If a member is deleted, their borrowing history is removed (alternatively, `SET NULL` could be used to preserve audit trails).\n\n**`SERIAL` vs `AUTO_INCREMENT` Demonstration:**\n\n| Feature | MySQL `AUTO_INCREMENT` | PostgreSQL `SERIAL` |\n|---|---|---|\n| **Declaration** | Appended after data type: `id INT AUTO_INCREMENT PRIMARY KEY` | Replaces data type: `id SERIAL PRIMARY KEY` |\n| **Underlying Type** | `BIGINT UNSIGNED` with auto-increment flag | Creates a `SEQUENCE` object + `INTEGER NOT NULL DEFAULT nextval(...)` |\n| **Manual Override** | `INSERT INTO t (id) VALUES (100);` | `INSERT INTO t (id) VALUES (100);` (works because `SERIAL` is a real column) |\n| **Sequence Independence** | Tied to the column | Independent sequence object accessible via `SELECT nextval('book_book_id_seq');` |\n| **Modern Alternative** | N/A | `GENERATED ALWAYS AS IDENTITY` (SQL Standard, recommended in PostgreSQL 10+) |\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mistakes Where KTU Students Lose Marks (Module 1 — Database Creation):**\n>\n> 1. **Wrong CREATE TABLE Order (–2 marks):** Creating child tables before parent tables. The DBMS will throw a *foreign key constraint fails* error. Always perform a topological sort.\n>\n> 2. **Forgetting `NOT NULL` on Foreign Keys (–1 mark):** An FK that \"should not\" be NULL is declared without `NOT NULL`. The DBMS will silently allow NULLs, violating the intended cardinality.\n>\n> 3. **Confusing `UNIQUE` with `PRIMARY KEY` (–1 mark):** Declaring multiple PRIMARY KEYs on a table. Only **one** PRIMARY KEY constraint is allowed per table (though it may span multiple columns).\n>\n> 4. **Wrong Data Type for Auto-Increment (–1 mark):** Using `AUTO_INCREMENT` in SQLite (use `AUTOINCREMENT`) or using `SERIAL` in MySQL (use `AUTO_INCREMENT`). This is the #1 cross-engine conversion error.\n>\n> 5. **Missing `ON DELETE/UPDATE` Actions (–0.5 mark):** Defaulting to implicit behavior. The KTU rubric requires *explicit* declaration of referential actions.\n>\n> 6. **Forgetting `IF NOT EXISTS` in `CREATE DATABASE/TABLE` (–0.5 mark):** Causes script re-runs to fail. Always use `IF NOT EXISTS` for idempotent scripts.\n>\n> 7. **No justification of referential policy (–1 mark in 14-mark Q):** Writing DDL without a 2–3 line justification of *why* you chose `CASCADE` vs `SET NULL` vs `RESTRICT`.\n\n---\n\n### 5.4 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid Revision Checklist — Database Creation (Module 1):**\n>\n> - [x] Database creation uses **DDL** (Data Definition Language), which includes `CREATE`, `ALTER`, `DROP`, `TRUNCATE`.\n> - [x] The 5 standard integrity constraints are: `NOT NULL`, `UNIQUE`, `PRIMARY KEY`, `FOREIGN KEY`, `CHECK`.\n> - [x] **PRIMARY KEY** = `NOT NULL` + `UNIQUE`; only **one** allowed per table; may be composite.\n> - [x] **FOREIGN KEY** enforces referential integrity; requires a parent table's PK or UNIQUE column.\n> - [x] **5 FK Actions:** `CASCADE`, `SET NULL`, `RESTRICT`, `NO ACTION`, `SET DEFAULT`.\n> - [x] **ER-to-Relation Mapping Rules (7 rules):** Strong entity → table; weak entity → table with composite PK + owner FK; 1:1 → UNIQUE FK; 1:N → FK on N-side; M:N → new bridge table; multivalued → separate table; n-ary → single table with all FKs.\n> - [x] **CREATE TABLE order = topological order of FK dependencies** (parents first, children last, bridges dead last).\n> - [x] **MySQL:** `AUTO_INCREMENT`, `ENGINE=InnoDB`, `BOOLEAN` is alias for `TINYINT(1)`.\n> - [x] **PostgreSQL:** `SERIAL` or `GENERATED AS IDENTITY`, native `BOOLEAN`, no `ENGINE` clause.\n> - [x] **SQLite:** Single `.db` file, no `CREATE DATABASE`, uses `AUTOINCREMENT` (not `AUTO_INCREMENT`), no native `BOOLEAN` (use `INTEGER CHECK IN (0,1)`).\n> - [x] **Verification:** MySQL → `DESCRIBE`; PostgreSQL → `\\d`; SQLite → `.schema`.\n> - [x] **Default FK action:** `NO ACTION` in MySQL/PostgreSQL, but `RESTRICT` in many other engines.\n> - [x] **BCNF / 3NF** is the expected normalized form for properly designed tables in Module 1 examples.\n> - [x] **Always** use `IF NOT EXISTS` for `CREATE DATABASE` and `CREATE TABLE` for script re-runnability.\n\n> [!NOTE]\n> **Final Exam Mantra:** \"ER to Relations first, DDL second, Constraints third, Engine-specific syntax last.\" The KTU examiner rewards *correct mapping* (4–5 marks) more than *perfect syntax* (1–2 marks). A logically correct schema with minor syntax errors scores higher than a syntactically perfect but semantically wrong schema.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "tablesandrelationshipsdefinetableswithappropriateattributescolumnsandestablishrelationshipsforeignkeysbetweentables": {
      "title": "Tables and Relationships: Define tables with appropriate attributes (columns) and establish relationships (foreign keys) between tables",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 1\n> - **Topic:** Tables and Relationships: Define tables with appropriate attributes (columns) and establish relationships (foreign keys) between tables\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition of a Table (Relation)\n\nIn the Relational Model of DBMS (proposed by **E.F. Codd** in 1970), a **table** — formally called a **relation** — is a two-dimensional structure that organises related data into **rows** and **columns**.\n\n- A **row** (also called a **tuple** or **record**) represents a single, real-world entity occurrence.\n- A **column** (also called an **attribute** or **field**) represents a property/characteristic of the entity.\n- The **schema** of a table is the set of all column names and their associated data types.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Definition (Verbatim Style):**\n> A **table** is defined as a database object that stores data in a structured format using rows (records) and columns (attributes). Each table represents one entity type from the real world (e.g., Student, Course, Employee).\n\n### 1.2 Formal Definition of Attributes (Columns)\n\nAn **attribute** is a named column of a relation. Each attribute is associated with a **domain** — the set of allowable values it may take. Formally, if `R` is a relation, then `R = (A1, A2, A3, ..., An)` where each `Ai` is an attribute name drawn from an underlying domain `Di`.\n\nProperties of attributes:\n- Each attribute must have a **unique name** within the table.\n- Each attribute has a **data type** (e.g., `INT`, `VARCHAR(50)`, `DATE`).\n- Each attribute value must be an **atomic (indivisible)** value (per **1NF**).\n\n### 1.3 Formal Definition of a Relationship & Foreign Key\n\nA **relationship** is an association between two or more entities (tables). The mechanism by which this association is physically implemented in a relational database is the **Foreign Key (FK)**.\n\nA **Foreign Key** is a column (or a set of columns) in one table that refers to the **Primary Key** of another table. The table containing the foreign key is called the **child/referencing table**, and the table being referenced is the **parent/referenced table**.\n\n> [!NOTE]\n> **Intuitive Analogy — The Library Card System** 📚\n>\n> Imagine a **Library** with two registers:\n> - **Register A (Books Register):** Lists every book with a unique accession number (Primary Key).\n> - **Register B (Issue Register):** Records which book was issued to which member. The \"Book Accession Number\" column in Register B is the **Foreign Key** — it points back to Register A.\n>\n> Even though the book details physically live in Register A, Register B can still tell you \"which book\" via the accession number reference. This is exactly how foreign keys connect tables without duplicating data.\n\n### 1.4 KTU High-Yield Vocabulary\n\n| Term | Meaning |\n|---|---|\n| **Relation** | Formal name for a table |\n| **Tuple** | A single row in a table |\n| **Attribute** | A named column of a table |\n| **Domain** | Set of permitted values for an attribute |\n| **Primary Key (PK)** | Attribute(s) that uniquely identify a row |\n| **Foreign Key (FK)** | Attribute(s) that reference a PK of another table |\n| **Cardinality** | Number of tuples in a relation |\n| **Degree** | Number of attributes in a relation |\n| **Referential Integrity** | Rule that every FK value must match a valid PK (or be NULL) |\n\n> [!TIP]\n> **Memory Trick:** **P**rimary key **P**arents the row. **F**oreign key **F**inds its parent elsewhere.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Anatomy of a Table — Structured Breakdown\n\nA well-designed table must satisfy the following structural requirements:\n\n1. **Atomicity of Values** — Each cell holds a single value (1NF compliance).\n2. **Unique Column Names** — No two columns in the same table may have the same name.\n3. **Homogeneous Columns** — Each column holds values of the same data type/domain.\n4. **Tuple Uniqueness** — No two rows are identical (ensured by the Primary Key).\n5. **Order Independence** — The order of rows and columns is insignificant.\n\n### 2.2 Anatomy of a Foreign Key — The 'Why' and 'How'\n\n- **Why?** Without foreign keys, tables are isolated \"islands\" of data. Foreign keys create **logical links** that preserve **referential integrity** and prevent **dangling references**.\n- **How?** A child table's column is declared with a `FOREIGN KEY ... REFERENCES` clause. The DBMS engine automatically enforces that any value inserted into the FK column must either:\n  - Match an existing PK value in the parent table, OR\n  - Be `NULL` (only if the column allows NULLs).\n\n### 2.3 Three Cardinalities of Relationships (The Core KTU Topic)\n\n| Cardinality | Symbol | Meaning | Real-World Example |\n|---|---|---|---|\n| **One-to-One** | `1:1` | One row in Table A relates to exactly one row in Table B | Each citizen has exactly one passport |\n| **One-to-Many** | `1:N` | One row in Table A relates to many rows in Table B | One department has many employees |\n| **Many-to-Many** | `M:N` | Many rows in A relate to many rows in B | Students enrol in many courses; each course has many students |\n\n> [!IMPORTANT]\n> **KTU Examiner's Trick:** A **Many-to-Many** relationship **cannot be implemented directly** in a relational schema. It must be **decomposed** into two One-to-Many relationships using a **junction/bridge table** (also called an *associative entity*).\n\n### 2.4 KTU Formula Sheet / Cheat Sheet\n\n> [!NOTE]\n> DBMS uses **set-theoretic notation** rather than numerical formulas. The following are the high-yield expressions examiners expect.\n\n| Concept | Notation / Expression | Meaning |\n|---|---|---|\n| Relation definition | $R(A_1, A_2, \\ldots, A_n)$ | Relation `R` with `n` attributes |\n| Tuple | $t \\in R$ | A tuple `t` belonging to relation `R` |\n| Cardinality | $\\vert R \\vert$ | Number of tuples (rows) in `R` |\n| Degree | $deg(R) = n$ | Number of attributes (columns) in `R` |\n| Domain of attribute | $dom(A_i)$ | Set of allowed values for `Ai` |\n| Primary Key | $PK(R) = \\{A_i, A_j\\}$ | Candidate key chosen to uniquely identify rows |\n| Foreign Key | $FK(S) \\subseteq PK(R)$ | FK in `S` references PK of `R` |\n| Referential Integrity | $\\forall t \\in S, t[FK] \\in \\pi_{PK}(R) \\lor t[FK] = NULL$ | Every FK value must match a PK in parent or be NULL |\n| 1NF Rule | $dom(A_i) \\text{ is atomic } \\forall A_i$ | No multi-valued attributes allowed |\n| 1:N Mapping Rule | $FK \\text{ placed on the \"many\" side}$ | Foreign key is always on the child/many side |\n| M:N Decomposition | $R_1 \\leftrightarrow J \\leftrightarrow R_2$ | A junction table `J` resolves M:N into two 1:N |\n\n### 2.5 Real-World Engineering Utility\n\nForeign keys and relationships are the **backbone of every enterprise system**:\n- **Banking:** `Account` table linked to `Customer` table — one customer can have many accounts (`1:N`).\n- **E-Commerce:** `Order` table linked to `Product` table via a `LineItem` bridge — products and orders form an `M:N` relationship.\n- **University ERP (KTU's own SIS):** `Student`, `Course`, and `Enrollment` — student enrolments are an `M:N` resolved through a junction table.\n\n> [!TIP]\n> **Engineering Insight:** Without properly designed foreign keys, the phenomenon of **data anomalies** (insertion, deletion, and update anomalies) creeps in, leading to inconsistent and unreliable databases.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Logical Derivation — Building a Relational Schema from an ER Model\n\nSuppose we have two entities from a **University** scenario: `STUDENT` and `DEPARTMENT`. The relationship is:\n\n- One **Department** offers admissions to many **Students** (a `1:N` relationship from Department to Student).\n\n**Step 1 — Identify the Parent (1) Side:**\nThe **Department** table is the \"one\" side.\n\n**Step 2 — Identify the Child (N) Side:**\nThe **Student** table is the \"many\" side. This is where the **foreign key** will be placed.\n\n**Step 3 — Choose the Primary Key on the Parent:**\n- `Department(dept_id, dept_name, hod)`\n- Here, `dept_id` is chosen as the **Primary Key**.\n\n**Step 4 — Place the Foreign Key on the Child:**\n- `Student(roll_no, name, dob, dept_id)`\n- Here, `dept_id` is the **Foreign Key** referencing `Department(dept_id)`.\n\n**Step 5 — Verify the Cardinality Algebra:**\n\n$$\n\\forall\\, s \\in \\text{Student},\\ s.\\text{dept\\_id} \\in \\pi_{\\text{dept\\_id}}(\\text{Department}) \\ \\lor\\ s.\\text{dept\\_id} = \\text{NULL}\n$$\n\nThis single line formally enforces that every student must either belong to a valid department or have an unassigned (NULL) department.\n\n---\n\n### 3.2 Exhaustive Step-by-Step SQL Implementation\n\nThe following walks through the construction of the schema in **standard SQL (DDL)**. Each transition is explicitly justified.\n\n**Step 1 — Create the Parent Table `Department`:**\n\n```sql\nCREATE TABLE Department (\n    dept_id      INT          PRIMARY KEY,\n    dept_name    VARCHAR(50)  NOT NULL UNIQUE,\n    hod          VARCHAR(50)\n);\n```\n\n*Reasoning:* `dept_id` is declared `PRIMARY KEY`, meaning the DBMS will automatically enforce **entity integrity** (no NULLs, no duplicates).\n\n**Step 2 — Create the Child Table `Student` with the Foreign Key:**\n\n```sql\nCREATE TABLE Student (\n    roll_no      INT          PRIMARY KEY,\n    name         VARCHAR(50)  NOT NULL,\n    dob          DATE,\n    dept_id      INT,\n    FOREIGN KEY (dept_id) REFERENCES Department(dept_id)\n        ON DELETE SET NULL\n        ON UPDATE CASCADE\n);\n```\n\n*Reasoning:*\n\n$$\n\\text{Student.dept\\_id} \\xrightarrow{\\text{REFERENCES}} \\text{Department.dept\\_id}\n$$\n\nThe `FOREIGN KEY` clause establishes the link. The two optional sub-clauses control the **referential action**:\n\n- `ON DELETE SET NULL` — If a department is deleted, students in it will have their `dept_id` set to NULL (preserving the student record).\n- `ON UPDATE CASCADE` — If a `dept_id` value is changed in the parent, the change automatically propagates to all referencing students.\n\n**Step 3 — Verify Referential Integrity Algebraically:**\n\n$$\n\\forall\\ s \\in \\text{Student}:\\ \\big(\\ s.\\text{dept\\_id} \\neq \\text{NULL} \\Rightarrow s.\\text{dept\\_id} \\in \\pi_{\\text{dept\\_id}}(\\text{Department})\\ \\big)\n$$\n\n> [!NOTE]\n> In plain English: *For every student, if their `dept_id` is not NULL, then that `dept_id` must exist in the Department table's primary key column.*\n\n---\n\n### 3.3 Analytical Matrix — Mapping ER Cardinality to Relational Schema\n\nThis table is the **single most important KTU exam cheat sheet** for this topic.\n\n| ER Cardinality | Implementation Strategy | Where is the FK placed? | Example |\n|---|---|---|---|\n| **1 : 1** | Merge into one table OR place FK on either side with a `UNIQUE` constraint | On either side, but FK column must be `UNIQUE` | `Person` ↔ `Passport` |\n| **1 : N** | Place FK on the \"many\" side | On the **child** (N) side | `Department` (1) → `Student` (N) |\n| **M : N** | Create a **junction/bridge table** containing FKs from both tables | Inside the **new junction table** | `Student` ↔ `Course` via `Enrollment` |\n| **Recursive / Unary** | Add a FK that references the same table's PK | Within the same table | `Employee` table with `manager_id` FK → `Employee(emp_id)` |\n\n> [!WARNING]\n> **Common KTU Mistake:** Students frequently place the foreign key on the **\"one\"** side of a `1:N` relationship. The rule is non-negotiable: the foreign key **must** go on the **\"many\"** side.\n\n---\n\n### 3.4 Worked Example — Resolving a Many-to-Many Relationship\n\n**Scenario:** A student can enrol in many courses; a course can have many students (`M:N`).\n\n**Step 1 — Identify the two parent tables:**\n\n- `Student(roll_no, name)`\n- `Course(course_id, title, credits)`\n\n**Step 2 — Create the junction table:**\n\n```sql\nCREATE TABLE Enrollment (\n    roll_no      INT,\n    course_id    INT,\n    enrol_date   DATE,\n    grade        CHAR(2),\n    PRIMARY KEY (roll_no, course_id),\n    FOREIGN KEY (roll_no)   REFERENCES Student(roll_no),\n    FOREIGN KEY (course_id) REFERENCES Course(course_id)\n);\n```\n\n**Step 3 — Decomposition Algebra:**\n\n$$\n\\text{Student} \\xleftrightarrow{\\text{M:N}} \\text{Course} \\quad\\Longrightarrow\\quad \\text{Student} \\xrightarrow{1:\\text{N}} \\text{Enrollment} \\xleftarrow{\\text{N}:1} \\text{Course}\n$$\n\nThe composite primary key `(roll_no, course_id)` ensures a student cannot enrol in the same course twice — a **composite key** derived from the two FKs.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Block-Level Functional Architecture — Table & Relationship Lifecycle\n\nThe following flowchart captures the **engineering lifecycle** for defining tables and establishing relationships, exactly as students should approach exam problems.\n\n```mermaid\nflowchart TD\n    A[Identify Real World Entities] --> B[Define Attributes for Each Entity]\n    B --> C[Choose Primary Key for Each Table]\n    C --> D{Identify Relationship Cardinality}\n    D -->|1 is to 1| E[Place FK on Either Side with UNIQUE Constraint]\n    D -->|1 is to N| F[Place FK on the Many Side]\n    D -->|M is to N| G[Create Junction Bridge Table]\n    E --> H[Apply Referential Integrity Rules]\n    F --> H\n    G --> H\n    H --> I[Declare ON DELETE and ON UPDATE Actions]\n    I --> J[Validate Schema in DBMS]\n    J --> K[Schema Ready for Use]\n```\n\n### 4.2 ER Diagram — Department ↔ Student (1:N Relationship)\n\n```mermaid\nerDiagram\n    DEPARTMENT {\n        INT dept_id PK\n        VARCHAR dept_name\n        VARCHAR hod\n    }\n    STUDENT {\n        INT roll_no PK\n        VARCHAR name\n        DATE dob\n        INT dept_id FK\n    }\n    DEPARTMENT ||--o{ STUDENT : \"administers\"\n```\n\n*Reading the diagram:* The `||--o{` notation is Mermaid's shorthand for **\"one and only one to zero-or-many\"** — exactly the `1:N` cardinality we derived in Section 3.\n\n### 4.3 Schema Diagram — Resolving the M:N Relationship\n\n```mermaid\nerDiagram\n    STUDENT {\n        INT roll_no PK\n        VARCHAR name\n    }\n    COURSE {\n        INT course_id PK\n        VARCHAR title\n        INT credits\n    }\n    ENROLLMENT {\n        INT roll_no PK,FK\n        INT course_id PK,FK\n        DATE enrol_date\n        CHAR grade\n    }\n    STUDENT ||--o{ ENROLLMENT : \"enrols in\"\n    COURSE  ||--o{ ENROLLMENT : \"is taken by\"\n```\n\n> [!TIP]\n> **Visual Cue for Exams:** Whenever you see a `M:N` in a problem, always draw the **third table** in the middle. The examiner allocates marks specifically for showing this decomposition — a single direct line between two tables for an M:N scenario will result in **mark deductions**.\n\n### 4.4 Anatomy of a Foreign Key Constraint — Component View\n\n```mermaid\nflowchart LR\n    A[Child Table] -->|contains| B[FK Column]\n    B -->|REFERENCES| C[Parent Table PK Column]\n    C -->|enforces| D[Referential Integrity]\n    B -.->|ON DELETE| E[Referential Action]\n    B -.->|ON UPDATE| E\n    D --> F[DBMS Engine Validation]\n    E --> F\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **[KTU University Exam – May 2024 Model Style]**\n\n**Q1. Define a foreign key. How does it differ from a primary key?**\n*(3 Marks)*\n\n**Model Answer:**\nA **foreign key** is a column (or set of columns) in a table whose values must match the primary key values of another table (or be NULL). It is used to establish and enforce a **link/relationship** between two tables.\n\n| Aspect | Primary Key | Foreign Key |\n|---|---|---|\n| Purpose | Uniquely identifies rows in **its own** table | Refers to the PK of **another** (or same) table |\n| NULL allowed? | No | Yes (usually) |\n| Uniqueness required? | Yes | Not always |\n| Number per table | Exactly one (conceptually) | Multiple allowed |\n\n*(Examiner's Note: Mentioning referential integrity explicitly fetches the 3rd mark.)*\n\n---\n\n**Q2. List and briefly explain the three types of relationships in a relational database.**\n*(3 Marks)*\n\n**Model Answer:**\n1. **One-to-One (1:1):** Each row in Table A is associated with **at most one** row in Table B (e.g., `Person` ↔ `Passport`).\n2. **One-to-Many (1:N):** Each row in Table A can be associated with **many** rows in Table B, but each row in B relates to only one row in A (e.g., `Department` → `Student`).\n3. **Many-to-Many (M:N):** Each row in A can relate to many rows in B and vice-versa. It is implemented via a **junction table** (e.g., `Student` ↔ `Course` via `Enrollment`).\n\n---\n\n### 5.2 Part B — Full 14-Mark Question (Internal Choice Pattern)\n\n> **[KTU University Exam – Dec 2023 / Model Paper Style]**\n\n---\n\n#### ⭐ Question A (14 Marks)\n\n**(a)** Explain the different types of relationships in a relational database with suitable examples and diagrams. Discuss how a Many-to-Many relationship is implemented. *(7 Marks)*\n\n**(b)** Consider the following scenario:\n> A company has many **Departments**. Each **Department** has many **Employees**. Each **Employee** is assigned to exactly one **Department**, and each **Department** has one **Manager** (who is also an employee).\n>\n> Write SQL `CREATE TABLE` statements to define the `Department` and `Employee` tables with appropriate primary keys, foreign keys, and referential integrity constraints. *(7 Marks)*\n\n**Model Solution for Q.A(a):**\n\nThe three types of relationships in a relational database are:\n\n1. **One-to-One (1:1):** A single row in Table A relates to a single row in Table B. *Example:* Each citizen has one passport, one Aadhaar number. The foreign key is placed on **either side** with a `UNIQUE` constraint to prevent multiple matches.\n\n2. **One-to-Many (1:N):** A single row in Table A relates to many rows in Table B. *Example:* One `Department` employs many `Employees`. The foreign key is placed on the **child** (many) side, i.e., in the `Employee` table.\n\n3. **Many-to-Many (M:N):** Rows in A relate to many rows in B and vice-versa. *Example:* A `Student` enrols in many `Courses`; a `Course` has many `Students`. This **cannot** be implemented with a single foreign key — it requires a **junction/bridge table** (e.g., `Enrollment`) containing FKs to both parent tables, typically forming a **composite primary key**.\n\n**Model Solution for Q.A(b):**\n\n```sql\nCREATE TABLE Department (\n    dept_id     INT          PRIMARY KEY,\n    dept_name   VARCHAR(50)  NOT NULL,\n    manager_id  INT\n);\n\nCREATE TABLE Employee (\n    emp_id      INT          PRIMARY KEY,\n    emp_name    VARCHAR(50)  NOT NULL,\n    salary      DECIMAL(10,2),\n    dept_id     INT,\n    FOREIGN KEY (dept_id) REFERENCES Department(dept_id)\n        ON DELETE SET NULL\n        ON UPDATE CASCADE\n);\n\nALTER TABLE Department\n    ADD CONSTRAINT fk_manager\n    FOREIGN KEY (manager_id) REFERENCES Employee(emp_id);\n```\n\n*Justification (for full marks):*\n- `Employee.dept_id` is the **FK on the many side** — establishes the 1:N from Department to Employee.\n- `Department.manager_id` references `Employee.emp_id` — a **recursive-style FK** that models the \"manager is an employee\" constraint.\n- `ON DELETE SET NULL` preserves employee records if a department is dissolved.\n- `ON UPDATE CASCADE` auto-propagates any PK changes.\n\n---\n\n#### ⭐ Question B (14 Marks) — Alternative Choice\n\n**(a)** Discuss the concept of **referential integrity** in DBMS. Explain the various referential actions (`ON DELETE`, `ON UPDATE`) with examples. *(7 Marks)*\n\n**(b)** Define **primary key** and **foreign key** constraints. For a **Library Management System** with tables `Book(book_id, title, isbn)` and `Loan(loan_id, book_id, member_id, issue_date, return_date)`, write SQL statements to: *(7 Marks)*\n   1. Create the `Book` table with `book_id` as primary key.\n   2. Create the `Loan` table with `loan_id` as primary key and `book_id` as foreign key referencing `Book(book_id)`.\n   3. Demonstrate what happens when a referenced `Book` row is deleted using `ON DELETE CASCADE`.\n\n**Model Solution for Q.B(a):**\n\n**Referential Integrity** is a database constraint that ensures that for every foreign key value in the child table, there exists a corresponding primary key value in the parent table (or the FK is NULL). It prevents **orphaned records** and maintains **data consistency**.\n\n**Referential Actions:**\n\n| Action | Behaviour on Parent's PK Change/Delete |\n|---|---|\n| `CASCADE` | Automatically propagates the change/delete to all matching child rows |\n| `SET NULL` | Sets the child's FK column to NULL |\n| `SET DEFAULT` | Sets the child's FK column to a predefined default value |\n| `RESTRICT` | **Rejects** the parent change if child rows exist (default in many DBMS) |\n| `NO ACTION` | Similar to RESTRICT but checked at end of statement |\n\n**Example:** In the `Student` → `Department` scenario, `ON DELETE SET NULL` ensures that deleting a department does not destroy student records — they are simply marked as unassigned.\n\n**Model Solution for Q.B(b):**\n\n```sql\n-- (1) Create the Book table\nCREATE TABLE Book (\n    book_id    INT          PRIMARY KEY,\n    title      VARCHAR(100) NOT NULL,\n    isbn       VARCHAR(20)  UNIQUE\n);\n\n-- (2) Create the Loan table with FK\nCREATE TABLE Loan (\n    loan_id      INT      PRIMARY KEY,\n    book_id      INT      NOT NULL,\n    member_id    INT      NOT NULL,\n    issue_date   DATE     NOT NULL,\n    return_date  DATE,\n    FOREIGN KEY (book_id) REFERENCES Book(book_id)\n        ON DELETE CASCADE\n);\n\n-- (3) Demonstration of CASCADE\nDELETE FROM Book WHERE book_id = 101;\n-- Because of ON DELETE CASCADE, all rows in Loan\n-- with book_id = 101 will be AUTOMATICALLY deleted.\n```\n\n---\n\n> [!WARNING]\n> **🔴 KTU Examiner's Valuation Pitfalls — Read Carefully!**\n>\n> 1. **Placing the FK on the wrong side** of a 1:N relationship is the **#1 mark-loss cause**. Always place it on the **\"many\"** side.\n> 2. **Forgetting `NOT NULL` on FK columns** when the relationship is *mandatory* (e.g., every Loan **must** reference a valid Book) loses 1–2 marks.\n> 3. **Drawing a direct line between two tables for an M:N relationship** without showing the junction table will cause a heavy deduction (typically 3–4 marks).\n> 4. **Omitting referential action clauses** (`ON DELETE`/`ON UPDATE`) — at least mention what would happen by default.\n> 5. **Using the same column name for both PK and FK without explicit `REFERENCES`** — the DBMS will not infer the relationship.\n> 6. **Confusing `UNIQUE` with `PRIMARY KEY`** — `UNIQUE` allows one NULL; `PRIMARY KEY` does not.\n\n---\n\n### 5.3 Topic Recap & Important Things to Remember ⚡\n\n> **Rapid Revision Checklist for the Night Before the Exam**\n\n- [x] A **table = relation**; rows = tuples; columns = attributes.\n- [x] Every table **must have a primary key** to ensure row uniqueness.\n- [x] A **foreign key** links two tables and enforces referential integrity.\n- [x] The foreign key is **always placed on the \"many\" side** of a 1:N relationship.\n- [x] **1:1** → FK on either side + `UNIQUE` constraint.\n- [x] **1:N** → FK on the child (many) side.\n- [x] **M:N** → Requires a **junction/bridge table** with a composite PK.\n- [x] **Referential actions** (`CASCADE`, `SET NULL`, `RESTRICT`) control behaviour on parent changes.\n- [x] **Referential integrity** = \"no orphan rows\"; FK value must match a parent PK or be NULL.\n- [x] In SQL, the syntax is:\n      `FOREIGN KEY (child_col) REFERENCES parent_table(parent_col)`\n- [x] **1NF** requires atomic values — never store multiple values in one cell.\n- [x] The order of rows and columns in a table is **logically insignificant**.\n- [x] For KTU exams, **always draw the ER/schema diagram** — it carries 2–3 easy marks.\n- [x] Remember the **composite key trick** for junction tables: `(FK1, FK2)` together form the PK.\n- [x] **Recursive relationships** (e.g., manager → employee) are also modelled using FKs that reference the **same table**.\n\n> [!TIP]\n> **Final Pro Tip:** In the exam, always **state the cardinality first**, then **justify the FK placement** in writing before writing the SQL. Examiners reward the reasoning, not just the code.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "normalizationensureyourschemaisnormalizedtominimizeredundancyanddependencyissues": {
      "title": "Normalization: Ensure your schema is normalized to minimize redundancy and dependency issues",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 2\n> - **Topic:** Normalization: Ensure your schema is normalized to minimize redundancy and dependency issues\n\n<!-- SECTION_1_START -->\n## SECTION 1 — Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Academic Definition\n\n> [!IMPORTANT]\n> **Normalization** is a systematic, step-by-step, formal database design methodology that decomposes (splits) a relation (table) into smaller, well-structured relations in order to **minimize data redundancy** and **eliminate undesirable insertion, update, and deletion anomalies**, while **preserving the original information and all functional dependencies** of the data.\n\nIn the KTU 2024 Scheme (NEP 2020 aligned Outcome-Based Education framework), normalization is the *theoretical backbone* of *Logical Database Design*. The process is governed by a hierarchy of **Normal Forms (NFs)** — a series of increasingly strict conditions that a relation must satisfy.\n\nThe principal normal forms covered in PBITT304 Module 2 are:\n\n- **First Normal Form (1NF)** — Atomicity of attribute values.\n- **Second Normal Form (2NF)** — Removal of *partial* functional dependencies.\n- **Third Normal Form (3NF)** — Removal of *transitive* functional dependencies.\n- **Boyce–Codd Normal Form (BCNF)** — Stricter version of 3NF based on *every* determinant being a candidate key.\n\n### 1.2 The Problem: Why Normalize?\n\n> [!NOTE]\n> A **badly designed (un-normalized) schema** wastes storage, produces inconsistent data, and makes the database impossible to maintain. Normalization is the *cure*.\n\nWhen a single table stores too much information in a \"flat\" structure, three classic **update anomalies** arise:\n\n| Anomaly Type | Plain-English Meaning | Illustrative Effect |\n|:-------------|:----------------------|:--------------------|\n| **Insertion Anomaly** | Cannot insert some real-world fact unless another unrelated fact is also known. | A new department cannot be added if it currently has no employees, because `EmpID` (the primary key) is `NOT NULL`. |\n| **Deletion Anomaly** | Deleting one piece of data accidentally removes another unrelated piece. | Deleting the only employee in a department erases the entire department's existence. |\n| **Update Anomaly** | The same fact is stored in many rows; changing it requires updating *all* copies or the database becomes inconsistent. | A professor's salary is repeated in 10 rows; updating it in 9 of them creates silent corruption. |\n\n### 1.3 Intuitive Real-World Analogy — The Filing Cabinet\n\n> [!TIP]\n> **Analogy — The School Record Room**\n>\n> Imagine a single drawer (the \"table\") where every student's full record — name, class, parent name, parent phone, club memberships, club fees, club in-charge — is scribbled on *one giant paper*. The same parent's phone number is written 30 times (once per child). The same club in-charge's name is written for every student of that club.\n>\n> - When a parent's phone changes, the clerk must hunt down all 30 papers and correct them — **Update Anomaly**.\n> - A new club formed with no students yet cannot be recorded (no student row to attach it to) — **Insertion Anomaly**.\n> - The last student of a club leaves; the club's details vanish — **Deletion Anomaly**.\n>\n> **Normalization is the act of splitting that drawer into separate labeled folders**: one for *Students*, one for *Parents*, one for *Clubs*, and one for *Memberships*. Now each fact is stored **exactly once**.\n\n### 1.4 The Normalization Workflow (Conceptual Pipeline)\n\nThe complete pipeline, in the order KTU 2024 expects students to follow:\n\n1. Identify the **Universal Relation** $R$ and the set of **Functional Dependencies (FDs)** $F$.\n2. Compute **Attribute Closures** to find **Candidate Keys**.\n3. Check the relation against **1NF → 2NF → 3NF → BCNF** sequentially.\n4. If a violation is found, **decompose** the offending relation.\n5. Verify the decomposition is **lossless join** and **dependency preserving**.\n\n> [!IMPORTANT]\n> The golden rule of KTU examination: **\"Higher normal forms are strictly stricter than lower ones.\"** If a relation is in BCNF, it is automatically in 3NF, 2NF, and 1NF — but **NOT** vice versa.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## SECTION 2 — Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Functional Dependency (FD) — The Foundation\n\n> [!NOTE]\n> A **Functional Dependency**, written $\\boldsymbol{X \\rightarrow Y}$, is a constraint between two sets of attributes $X$ and $Y$ of a relation $R$. It asserts that for any two tuples $t_1$ and $t_2$ in $R$: if $t_1[X] = t_2[X]$, then $t_1[Y] = t_2[Y]$. In plain English: **\"$X$ functionally determines $Y$\"**, or \"knowing $X$, you can uniquely look up $Y$.\"\n\n**Key vocabulary tied to a relation $R(A_1, A_2, \\dots, A_n)$:**\n\n- **Determinant** — The left-hand side $X$ of an FD.\n- **Dependent** — The right-hand side $Y$ of an FD.\n- **Trivial FD** — An FD of the form $X \\rightarrow Y$ where $Y \\subseteq X$ (always true by definition).\n- **Non-trivial FD** — $X \\rightarrow Y$ where $X \\cap Y = \\emptyset$.\n- **Prime Attribute** — An attribute that is part of *some* candidate key.\n- **Non-Prime Attribute** — An attribute that is *not* part of *any* candidate key.\n\n### 2.2 Armstrong's Axioms (Inference Rules)\n\nThese are the **sound and complete** set of rules used to derive all FDs logically implied by a given set $F$.\n\n| Rule | Form | Name |\n|:-----|:-----|:-----|\n| **Reflexivity** | If $Y \\subseteq X$, then $X \\rightarrow Y$ | Reflexivity |\n| **Augmentation** | If $X \\rightarrow Y$, then $XZ \\rightarrow YZ$ | Augmentation |\n| **Transitivity** | If $X \\rightarrow Y$ and $Y \\rightarrow Z$, then $X \\rightarrow Z$ | Transitivity |\n| **Union** | If $X \\rightarrow Y$ and $X \\rightarrow Z$, then $X \\rightarrow YZ$ | Union (derived) |\n| **Decomposition** | If $X \\rightarrow YZ$, then $X \\rightarrow Y$ and $X \\rightarrow Z$ | Decomposition (derived) |\n| **Pseudotransitivity** | If $X \\rightarrow Y$ and $WY \\rightarrow Z$, then $WX \\rightarrow Z$ | Pseudotransitivity (derived) |\n\n### 2.3 Attribute Closure — The Most Important Computation in Normalization\n\n> [!IMPORTANT]\n> The **closure of an attribute set $X$ under $F$**, written $\\boldsymbol{X^{+}}$ (or $\\boldsymbol{X^+}$), is the set of *all* attributes that can be functionally determined from $X$ using the FDs in $F$. It is the single most important tool a KTU student must master for Module 2.\n\n**Step-by-step algorithm to compute $X^{+}$:**\n\n1. Initialize the result $X^{+} = X$.\n2. Repeat the following until no new attribute is added:\n   - For each FD $A \\rightarrow B$ in $F$: if $A \\subseteq X^{+}$, then set $X^{+} = X^{+} \\cup B$.\n3. Return $X^{+}$.\n\n**Use of $X^{+}$ in KTU problems:**\n\n- **Candidate Key Test** — A set $X$ is a **superkey** of $R$ iff $X^{+} = R$ (i.e., $X^{+}$ contains *all* attributes of $R$).\n- **FD Membership Test** — An FD $X \\rightarrow Y$ holds in $F^{+}$ iff $Y \\subseteq X^{+}$.\n- **Minimal Cover** — Used to find canonical FDs before decomposition.\n\n### 2.4 Normal Forms — Formal Definitions\n\n> [!NOTE]\n> Each normal form rules out a *specific class* of dependency-based redundancy. The hierarchy is monotonic: $\\text{BCNF} \\subset \\text{3NF} \\subset \\text{2NF} \\subset \\text{1NF}$.\n\n#### 2.4.1 First Normal Form (1NF)\n\n- **Rule:** Every cell of the table must contain an **atomic (indivisible) value**. No repeating groups, no multi-valued attributes, no arrays or nested tables.\n- **Violation signature:** An attribute holding a *set*, *list*, or *comma-separated* values.\n\n#### 2.4.2 Second Normal Form (2NF)\n\n- **Rule:** The relation must be in **1NF**, AND **every non-prime attribute is fully functionally dependent on every candidate key** (i.e., no **partial dependency**).\n- **Partial Dependency** — A non-prime attribute $A$ depends on only a *proper subset* of a candidate key.\n- **Applies only when:** the candidate key is **composite** (made of two or more attributes). If the key is a single attribute, 2NF is automatically satisfied.\n\n#### 2.4.3 Third Normal Form (3NF)\n\n- **Rule:** The relation must be in **2NF**, AND **no non-prime attribute is transitively dependent on any candidate key** (i.e., for every FD $X \\rightarrow A$, either $A$ is a prime attribute OR $X$ is a superkey).\n- **Transitive Dependency** — A dependency of the form $X \\rightarrow Y \\rightarrow A$, where $X$ is a candidate key, $Y$ is a non-prime attribute, and $A$ is a non-prime attribute.\n- **Key practical difference from BCNF:** 3NF *allows* a non-superkey determinant if the dependent is a **prime attribute**.\n\n#### 2.4.4 Boyce–Codd Normal Form (BCNF)\n\n- **Rule:** For **every non-trivial FD** $X \\rightarrow Y$ in $F^{+}$, **$X$ must be a superkey** of $R$.\n- **Equivalent statement:** The relation must be in **1NF** AND **every determinant is a candidate key**.\n- **Key difference from 3NF:** BCNF forbids the prime-attribute loophole that 3NF permits.\n- **Trade-off:** BCNF decomposition is always **lossless-join** but is **not always dependency-preserving**.\n\n### 2.5 KTU High-Yield Formula Sheet / Cheat Sheet\n\n| # | Concept | Formula / Symbol | Meaning / Use |\n|:-:|:--------|:-----------------|:--------------|\n| 1 | Functional Dependency | $X \\rightarrow Y$ | $X$ functionally determines $Y$ |\n| 2 | Attribute Closure | $X^{+}$ | All attributes derivable from $X$ |\n| 3 | Superkey Condition | $X^{+} = R$ | $X$ is a superkey |\n| 4 | 1NF Condition | All $A_i$ are atomic | No multi-valued cells |\n| 5 | 2NF Condition | No $K_i \\subset K$ with $K_i \\rightarrow A$ | No partial dependency on composite key |\n| 6 | 3NF Condition | $X \\rightarrow A \\Rightarrow A$ is prime OR $X$ is superkey | No transitive dependency on non-prime |\n| 7 | BCNF Condition | $X \\rightarrow Y$ (non-trivial) $\\Rightarrow X$ is superkey | Every determinant is a key |\n| 8 | Lossless-Join Test | $R_1 \\cap R_2 \\rightarrow R_1$ OR $R_1 \\cap R_2 \\rightarrow R_2$ | Common attributes form a key of one side |\n| 9 | Dependency Preservation | $(F_1 \\cup F_2 \\cup \\dots \\cup F_n)^{+} = F^{+}$ | All FDs enforceable in decomposed schema |\n| 10 | Cardinality of Trivial FDs | $\\vert Y \\vert$ such that $Y \\subseteq X$ | Reflexivity generates $\\vert X \\vert \\cdot 2^{\\vert X \\vert}$ trivial FDs |\n\n> [!TIP]\n> **Real-World Engineering Utility**\n> Normalization is the foundation for **OLTP systems** (Online Transaction Processing) such as ATM networks, airline reservations, and banking ledgers. In contrast, **OLAP/data-warehouse** systems (analytics, BI dashboards) often *deliberately denormalize* for query speed. KTU graduates must know *when* to apply each — a frequently asked question.\n\n### 2.6 The Decomposition Properties\n\nA \"good\" decomposition $\\rho = \\{R_1, R_2, \\dots, R_n\\}$ of $R$ must satisfy:\n\n1. **Lossless Join** — Joining the decomposed relations reproduces the *exact* original relation with no spurious tuples.\n2. **Dependency Preservation** — All original FDs remain enforceable on the decomposed schema.\n3. **No Loss of Attributes** — $\\bigcup_i R_i = R$ (no attribute is lost).\n\n> [!WARNING]\n> **Common Misconception:** *\"3NF is always dependency-preserving, BCNF is not.\"* This is *generally* true but **not an absolute theorem**. Always verify both properties for the *specific* decomposition produced.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## SECTION 3 — Step-by-Step Derivations & Worked Examples\n\n### 3.1 Worked Example: A Complete Normalization Walk-Through\n\n> [!IMPORTANT]\n> This is the **canonical KTU-style problem**. Master this template, and you can solve any normalization question on the exam.\n\n#### Step 0 — The Given Schema\n\nConsider the relation:\n\n$$\nR(\\underline{\\text{StudID}}, \\underline{\\text{CourseID}}, \\text{StudName}, \\text{StudDept}, \\text{DeptHOD}, \\text{Grade}, \\text{CourseName})\n$$\n\nThe composite primary key is $(\\text{StudID}, \\text{CourseID})$.\n\n#### Step 1 — Given Functional Dependencies (F)\n\n$$\nF = \\{\\, \\text{StudID} \\rightarrow \\text{StudName},\\; \\text{StudID} \\rightarrow \\text{StudDept},\\; \\text{StudDept} \\rightarrow \\text{DeptHOD},\\; \\text{CourseID} \\rightarrow \\text{CourseName},\\; (\\text{StudID}, \\text{CourseID}) \\rightarrow \\text{Grade} \\,\\}\n$$\n\n#### Step 2 — Identify Candidate Keys and Prime/Non-Prime Attributes\n\nCompute $(\\text{StudID}, \\text{CourseID})^{+}$:\n\n$$\n\\begin{aligned}\n(\\text{StudID}, \\text{CourseID})^{+} &= \\{\\text{StudID}, \\text{CourseID}\\} \\\\\n&\\cup \\{\\text{StudName},\\ \\text{StudDept}\\} \\quad &&[\\text{using StudID} \\rightarrow \\text{StudName},\\ \\text{StudDept}] \\\\\n&\\cup \\{\\text{DeptHOD}\\} \\quad &&[\\text{using StudDept} \\rightarrow \\text{DeptHOD}] \\\\\n&\\cup \\{\\text{CourseName}\\} \\quad &&[\\text{using CourseID} \\rightarrow \\text{CourseName}] \\\\\n&\\cup \\{\\text{Grade}\\} \\quad &&[\\text{using (StudID, CourseID)} \\rightarrow \\text{Grade}] \\\\\n&= \\{\\text{StudID}, \\text{CourseID}, \\text{StudName}, \\text{StudDept}, \\text{DeptHOD}, \\text{Grade}, \\text{CourseName}\\} \\\\\n&= R\n\\end{aligned}\n$$\n\nTherefore $(\\text{StudID}, \\text{CourseID})$ is a **candidate key**, and it is the only one.\n\n- **Prime attributes** = $\\{$StudID, CourseID$\\}$.\n- **Non-prime attributes** = $\\{$StudName, StudDept, DeptHOD, Grade, CourseName$\\}$.\n\n#### Step 3 — Check 1NF\n\nAll attributes are atomic. ✅ **Relation $R$ is in 1NF.**\n\n#### Step 4 — Check 2NF (Look for Partial Dependencies)\n\nThe candidate key is composite. Inspect each FD whose left side is a *proper subset* of the key:\n\n- $\\text{StudID} \\rightarrow \\text{StudName}$ — partial dependency on StudID alone. ❌\n- $\\text{StudID} \\rightarrow \\text{StudDept}$ — partial dependency on StudID alone. ❌\n- $\\text{CourseID} \\rightarrow \\text{CourseName}$ — partial dependency on CourseID alone. ❌\n\n> [!WARNING]\n> **Relation $R$ violates 2NF.**\n\n**Decomposition into 2NF** (separate the partial dependencies):\n\n$$\nR_1(\\underline{\\text{StudID}}, \\text{StudName}, \\text{StudDept}, \\text{DeptHOD}) \\quad \\text{— with FDs: StudID} \\rightarrow \\text{StudName, StudDept, DeptHOD}\n$$\n\n$$\nR_2(\\underline{\\text{StudID}}, \\underline{\\text{CourseID}}, \\text{Grade}, \\text{CourseName}) \\quad \\text{— with FD: (StudID, CourseID)} \\rightarrow \\text{Grade, CourseID} \\rightarrow \\text{CourseName}\n$$\n\n#### Step 5 — Check 3NF on $R_1$ (Look for Transitive Dependencies)\n\nIn $R_1$:\n\n$$\n\\text{StudID} \\rightarrow \\text{StudDept} \\rightarrow \\text{DeptHOD}\n$$\n\nThis is a **transitive dependency** where StudID is the key, StudDept is non-prime, and DeptHOD is non-prime. ❌\n\n**Decomposition of $R_1$ into 3NF:**\n\n$$\nR_{11}(\\underline{\\text{StudID}}, \\text{StudName}, \\text{StudDept}) \\quad \\text{— with FD: StudID} \\rightarrow \\text{StudName, StudDept}\n$$\n\n$$\nR_{12}(\\underline{\\text{StudDept}}, \\text{DeptHOD}) \\quad \\text{— with FD: StudDept} \\rightarrow \\text{DeptHOD}\n$$\n\nNow check $R_{11}$: every non-prime attribute is directly dependent on the key. ✅\nCheck $R_{12}$: DeptHOD is non-prime and depends on the (single-attribute) key StudDept. ✅\n\n**Check $R_2$ for 3NF:**\n\n- Primary key is composite $(\\text{StudID}, \\text{CourseID})$.\n- $\\text{CourseID} \\rightarrow \\text{CourseName}$: Here, **CourseID is a prime attribute** and **CourseName is a non-prime attribute**.\n\nUnder 3NF, a non-superkey determinant ($X = $ CourseID) is allowed if the dependent ($A = $ CourseName) is *not* a prime attribute, *or* the determinant is a superkey. Wait — let us apply the formal 3NF rule precisely:\n\n> A relation is in 3NF iff for every FD $X \\rightarrow A$, either (i) $A$ is a prime attribute, OR (ii) $X$ is a superkey.\n\nIn $R_2$, for the FD $\\text{CourseID} \\rightarrow \\text{CourseName}$:\n\n- $X = $ CourseID is **not** a superkey of $R_2$ (the key is composite).\n- $A = $ CourseName is a **non-prime** attribute.\n\nBoth conditions fail. So $R_2$ **violates 3NF** strictly. ❌\n\n**Decomposition of $R_2$ into 3NF:**\n\n$$\nR_{21}(\\underline{\\text{StudID}}, \\underline{\\text{CourseID}}, \\text{Grade}) \\quad \\text{— with FD: (StudID, CourseID)} \\rightarrow \\text{Grade}\n$$\n\n$$\nR_{22}(\\underline{\\text{CourseID}}, \\text{CourseName}) \\quad \\text{— with FD: CourseID} \\rightarrow \\text{CourseName}\n$$\n\n#### Step 6 — Final 3NF Schema\n\nThe set of relations in 3NF:\n\n$$\n\\boxed{\\, R_{11}(\\underline{\\text{StudID}}, \\text{StudName}, \\text{StudDept}),\\;\\; R_{12}(\\underline{\\text{StudDept}}, \\text{DeptHOD}),\\;\\; R_{21}(\\underline{\\text{StudID}}, \\underline{\\text{CourseID}}, \\text{Grade}),\\;\\; R_{22}(\\underline{\\text{CourseID}}, \\text{CourseName}) \\,}\n$$\n\n#### Step 7 — Check BCNF (Optional Extension for Full Marks)\n\nEach $R_{ij}$ has a **single-attribute** key (StudID, StudDept, CourseID) or a key whose only FDs are the key itself. Thus **every determinant is a candidate key**. ✅\n\n> **Result:** The final schema is in **BCNF** and is therefore also in **3NF, 2NF, and 1NF**.\n\n#### Step 8 — Verify Lossless Join and Dependency Preservation (Board-Exam Favourite)\n\n**Lossless join test** (using the theorem):\n\n$$\n(R_i \\cap R_j) \\rightarrow R_i \\quad \\text{OR} \\quad (R_i \\cap R_j) \\rightarrow R_j\n$$\n\n- $R_{11} \\cap R_{12} = \\{\\text{StudDept}\\}$, and $\\text{StudDept} \\rightarrow \\text{StudDept}, \\text{DeptHOD} = R_{12}$. ✅\n- $R_{21} \\cap R_{22} = \\{\\text{CourseID}\\}$, and $\\text{CourseID} \\rightarrow \\text{CourseID}, \\text{CourseName} = R_{22}$. ✅\n\n**Dependency preservation:**\n\n$$\n\\begin{aligned}\n(F_{11} \\cup F_{12} \\cup F_{21} \\cup F_{22})^{+} &=\n\\{\\text{StudID} \\rightarrow \\text{StudName},\\ \\text{StudID} \\rightarrow \\text{StudDept}, \\\\\n&\\quad \\text{StudDept} \\rightarrow \\text{DeptHOD}, \\\\\n&\\quad (\\text{StudID}, \\text{CourseID}) \\rightarrow \\text{Grade}, \\\\\n&\\quad \\text{CourseID} \\rightarrow \\text{CourseName}\\}^{+} \\\\\n&= F^{+}\n\\end{aligned}\n$$\n\n✅ The decomposition is **dependency preserving**.\n\n### 3.2 Analytical Summary Matrix (Decision Table for KTU Exams)\n\n| Check this in relation $R$ | If you find … | Then $R$ is in … | Action |\n|:---------------------------|:--------------|:-----------------|:-------|\n| Any non-atomic cell | Multi-valued attribute | Below 1NF | Split into atomic rows / new relation |\n| Composite key, non-prime depends on part of key | Partial dependency | Below 2NF (but in 1NF) | Decompose by separating partial dependents |\n| Key → Non-key → Non-key | Transitive dependency | Below 3NF (but in 2NF) | Decompose by removing the middle non-key |\n| $X \\rightarrow A$ with $A$ non-prime, $X$ not a superkey | 3NF violation | Below 3NF | Move $A$ to a new relation with $X$ as its key |\n| $X \\rightarrow A$ with $A$ non-prime, $X$ not a superkey, $A$ is prime | BCNF violation (but allowed in 3NF) | In 3NF but not BCNF | Decompose for BCNF — but check dependency preservation |\n| All FDs have superkey on the LHS | None | BCNF | Stop — schema is normalized |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## SECTION 4 — Structural Diagrams & Schematics\n\n### 4.1 Normal Form Hierarchy (Mermaid Diagram)\n\n```mermaid\nflowchart TB\n    n0[\"UNNORMALIZED FORM<br/>Multi-valued cells repeating groups\"] --> n1\n    n1[\"1NF FIRST NORMAL FORM<br/>All attributes atomic\"] --> n2\n    n2[\"2NF SECOND NORMAL FORM<br/>No partial dependency on composite key\"] --> n3\n    n3[\"3NF THIRD NORMAL FORM<br/>No transitive dependency non prime to non prime\"] --> n4\n    n4[\"BCNF BOYCE CODD NORMAL FORM<br/>Every determinant is a candidate key\"]\n    style n0 fill:#ffcccc,stroke:#990000,color:#000\n    style n1 fill:#ffe0b3,stroke:#cc6600,color:#000\n    style n2 fill:#fff2b3,stroke:#cc9900,color:#000\n    style n3 fill:#d4ed91,stroke:#557711,color:#000\n    style n4 fill:#9be3c1,stroke:#115533,color:#000\n```\n\n> [!NOTE]\n> The arrow $n_0 \\to n_1 \\to n_2 \\to n_3 \\to n_4$ is **strict and one-way**. You cannot \"skip\" levels in the proof, but a relation in level 4 is *automatically* in all lower levels.\n\n### 4.2 Functional Dependency Decomposition Flow (Mermaid Diagram)\n\n```mermaid\nflowchart LR\n    a[\"INPUT: Relation R and FD set F\"] --> b[\"STEP 1: Compute attribute closures X plus to find all candidate keys\"]\n    b --> c[\"STEP 2: Classify attributes as PRIME or NON PRIME\"]\n    c --> d{\"STEP 3: Check 1NF<br/>Are all cells atomic?\"}\n    d -- NO --> d1[\"REMEDY: Split multi valued cells into separate rows or new relation\"]\n    d1 --> d\n    d -- YES --> e{\"STEP 4: Check 2NF<br/>Any partial dependency on composite key?\"}\n    e -- YES --> e1[\"DECOMPOSE: Move partial dependent to a new relation with the partial key as its PK\"]\n    e1 --> e\n    e -- NO --> f{\"STEP 5: Check 3NF<br/>Any transitive dependency on non prime attributes?\"}\n    f -- YES --> f1[\"DECOMPOSE: Remove transitive chain by splitting the middle non prime into its own relation\"]\n    f1 --> f\n    f -- NO --> g{\"STEP 6: Check BCNF<br/>Is every determinant a candidate key?\"}\n    g -- YES --> h[\"OUTPUT: Schema is in BCNF and hence in all lower NFs\"]\n    g -- NO --> g1[\"DECOMPOSE: For each violating FD X to A place A in a new relation with X as its key\"]\n    g1 --> h\n    style h fill:#9be3c1,stroke:#115533,color:#000\n    style a fill:#cfe2ff,stroke:#003366,color:#000\n```\n\n### 4.3 Anomaly Elimination Block Diagram (Mermaid)\n\n```mermaid\nflowchart TB\n    subgraph BEFORE[\"BEFORE NORMALIZATION: Single Relation R\"]\n        B1[\"Student Table<br/>StudID StudName DeptName HOD CourseID CourseName Grade\"]\n    end\n    subgraph AFTER[\"AFTER NORMALIZATION: Decomposed into BCNF\"]\n        A1[\"STUDENT TABLE<br/>StudID StudName DeptName\"]\n        A2[\"DEPARTMENT TABLE<br/>DeptName HOD\"]\n        A3[\"COURSE TABLE<br/>CourseID CourseName\"]\n        A4[\"ENROLLMENT TABLE<br/>StudID CourseID Grade\"]\n    end\n    BEFORE -->|Decomposition Step 1 2NF| MID1\n    MID1[\"Intermediate Tables<br/>STUDENT, DEPT, ENROLL WITH COURSE\"] -->|Decomposition Step 2 3NF BCNF| AFTER\n    A1 -.FK DeptName.-> A2\n    A3 -.PK CourseID.-> A4\n    A1 -.PK StudID.-> A4\n    style BEFORE fill:#ffcccc,stroke:#990000,color:#000\n    style AFTER fill:#9be3c1,stroke:#115533,color:#000\n    style MID1 fill:#fff2b3,stroke:#cc9900,color:#000\n```\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## SECTION 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **Q1. [KTU University Exam — Dec 2023]**\n> Define **Second Normal Form (2NF)**. Is a relation with a **single-attribute primary key** automatically in 2NF? Justify your answer with a suitable example.\n\n**Model Answer:**\n\n> **2NF Definition:** A relation $R$ is in Second Normal Form if it is in 1NF **and** every non-prime attribute is *fully* functionally dependent on the *whole* of every candidate key (i.e., no non-prime attribute depends on only a *proper subset* of a composite key).\n>\n> **Yes, a relation with a single-attribute primary key is automatically in 2NF.** This is because partial dependency can only occur when the candidate key is *composite* (made of two or more attributes). A single-attribute key has no *proper subset* other than $\\emptyset$, so partial dependencies are structurally impossible.\n>\n> **Example:** Consider `Employee(EmpID, EmpName, Salary)` with PK = `EmpID`. Since `EmpID` is a single attribute, there is no way for `EmpName` or `Salary` to be \"partially\" dependent on it. Hence the relation is in 2NF by construction.\n\n---\n\n> **Q2. [KTU University Exam — June 2024]**\n> Differentiate between **3NF** and **BCNF**. Under what circumstance does a relation satisfy 3NF but violate BCNF?\n\n**Model Answer:**\n\n| Aspect | 3NF | BCNF |\n|:-------|:----|:-----|\n| **Formal Rule** | For every FD $X \\rightarrow A$, either $X$ is a superkey **OR** $A$ is a prime attribute. | For every FD $X \\rightarrow A$, $X$ **must** be a superkey. |\n| **Strictness** | Looser (allows prime-attribute loophole) | Stricter (no loophole) |\n| **Decomposition guarantee** | Always dependency-preserving | Not always dependency-preserving |\n\n**3NF-but-not-BCNF case:** Consider the relation $R(\\text{StudID}, \\text{Subject}, \\text{Professor})$ with FDs:\n\n$$\n\\text{StudID}, \\text{Subject} \\rightarrow \\text{Professor}, \\quad \\text{Professor} \\rightarrow \\text{Subject}\n$$\n\nHere, the candidate keys are $\\{\\text{StudID}, \\text{Subject}\\}$ and $\\{\\text{StudID}, \\text{Professor}\\}$. In the FD $\\text{Professor} \\rightarrow \\text{Subject}$, the determinant `Professor` is **not** a superkey, but the dependent `Subject` **is a prime attribute**. Thus the relation satisfies 3NF (prime-attribute loophole), yet violates BCNF (determinant is not a superkey).\n\n### 5.2 Part B — Long Answer Questions (14 Marks Each, with Internal Choice)\n\n> **[KTU University Exam — Dec 2023, Model Question Paper]**\n\n#### ⭐ QUESTION A (14 Marks)\n\n**(a) [7 Marks]** Consider the relation:\n\n$$\nR(\\text{EmpID},\\ \\text{DeptID},\\ \\text{EmpName},\\ \\text{DeptName},\\ \\text{DeptLocation},\\ \\text{Salary})\n$$\n\nwith the FDs:\n\n$$\n\\begin{aligned}\n&F_1:\\ \\text{EmpID} \\rightarrow \\text{EmpName},\\ \\text{DeptID},\\ \\text{Salary} \\\\\n&F_2:\\ \\text{DeptID} \\rightarrow \\text{DeptName},\\ \\text{DeptLocation}\n\\end{aligned}\n$$\n\nIdentify the **candidate key(s)**, classify attributes as **prime / non-prime**, and **normalize the relation step-by-step up to BCNF**.\n\n**(b) [7 Marks]** For the same relation, verify that the final BCNF decomposition is **(i) lossless-join** and **(ii) dependency-preserving**.\n\n**Model Solution:**\n\n**Part (a) — Normalization to BCNF**\n\n**Step 1 — Find Candidate Key.**\n\nCompute $(\\text{EmpID})^{+}$:\n\n$$\n(\\text{EmpID})^{+} = \\{\\text{EmpID},\\ \\text{EmpName},\\ \\text{DeptID},\\ \\text{Salary},\\ \\text{DeptName},\\ \\text{DeptLocation}\\} = R\n$$\n\nSo `EmpID` alone is the candidate key. **Prime = {EmpID}**, **Non-prime = {EmpName, DeptID, Salary, DeptName, DeptLocation}**.\n\n**Step 2 — Check 2NF.**\n\nThe key is single-attribute → no partial dependency possible. ✅ $R$ is in 2NF.\n\n**Step 3 — Check 3NF.**\n\nLook for transitive dependency. Using $F_1$ and $F_2$:\n\n$$\n\\text{EmpID} \\rightarrow \\text{DeptID} \\rightarrow \\text{DeptName},\\ \\text{DeptLocation}\n$$\n\nThe middle attribute `DeptID` is non-prime, and `DeptName`, `DeptLocation` are also non-prime. Hence a **transitive dependency** exists. ❌\n\n**Decomposition into 3NF (and BCNF, since the resulting tables have single-attribute keys):**\n\n$$\nR_1(\\underline{\\text{EmpID}},\\ \\text{EmpName},\\ \\text{DeptID},\\ \\text{Salary}) \\quad\\quad R_2(\\underline{\\text{DeptID}},\\ \\text{DeptName},\\ \\text{DeptLocation})\n$$\n\nEach $R_i$ has only one determinant — its primary key — hence both are in **BCNF**. ✅\n\n**Part (b) — Verification**\n\n**(i) Lossless join test:**\n\n$$\nR_1 \\cap R_2 = \\{\\text{DeptID}\\}, \\quad \\text{DeptID} \\rightarrow \\text{DeptName},\\ \\text{DeptLocation} = R_2\n$$\n\nSince the intersection is a key of $R_2$, the decomposition is **lossless join**. ✅\n\n**(ii) Dependency preservation:**\n\n$$\n(F_1 \\cup F_2)^{+} = F^{+} \\quad\\Longrightarrow\\quad \\text{All FDs are preserved.} \\quad \\checkmark\n$$\n\n✅ The decomposition is **dependency preserving**.\n\n---\n\n#### ⭐ QUESTION B (14 Marks) — *Internal Choice Alternative*\n\n**(a) [7 Marks]** Explain **Armstrong's Axioms** with examples. Using them, compute the **closure** $(\\text{AB})^{+}$ for the FDs:\n\n$$\nF = \\{\\, A \\rightarrow B,\\ B \\rightarrow C,\\ C \\rightarrow D,\\ D \\rightarrow E \\,\\}\n$$\n\n**(b) [7 Marks]** Define **BCNF**. Why is **dependency preservation not always guaranteed** in BCNF decomposition? Illustrate with an example where BCNF decomposition *loses* an FD.\n\n**Model Solution:**\n\n**Part (a) — Armstrong's Axioms**\n\n- **Reflexivity:** If $Y \\subseteq X$, then $X \\rightarrow Y$. *Example:* $\\{A,B\\} \\rightarrow A$.\n- **Augmentation:** If $X \\rightarrow Y$, then $XZ \\rightarrow YZ$. *Example:* From $A \\rightarrow B$, derive $AC \\rightarrow BC$.\n- **Transitivity:** If $X \\rightarrow Y$ and $Y \\rightarrow Z$, then $X \\rightarrow Z$.\n\n**Closure computation $(\\text{AB})^{+}$:**\n\n$$\n\\begin{aligned}\n(\\text{AB})^{(0)} &= \\{A, B\\} \\\\\n(\\text{AB})^{(1)} &= \\{A, B\\} \\cup \\{C\\} \\quad &&[\\text{using } B \\rightarrow C] \\\\\n(\\text{AB})^{(2)} &= \\{A, B, C\\} \\cup \\{D\\} \\quad &&[\\text{using } C \\rightarrow D] \\\\\n(\\text{AB})^{(3)} &= \\{A, B, C, D\\} \\cup \\{E\\} \\quad &&[\\text{using } D \\rightarrow E] \\\\\n(\\text{AB})^{+} &= \\{A, B, C, D, E\\}\n\\end{aligned}\n$$\n\n**Part (b) — BCNF & Dependency Preservation**\n\n> **BCNF Definition:** A relation $R$ is in BCNF iff for every non-trivial FD $X \\rightarrow Y$ in $F^{+}$, $X$ is a superkey of $R$.\n\n**Why dependency preservation is not always guaranteed in BCNF:**\n\nWhen we decompose a relation into BCNF, we may have to split an FD across two different relations. After decomposition, the FD's left-hand side and right-hand side may lie in *different* sub-relations, making the FD **unenforceable** without a costly join — i.e., the FD is *not preserved* in the decomposed schema.\n\n**Counter-example:**\n\nConsider $R(\\text{Teacher},\\ \\text{Subject},\\ \\text{Department})$ with FDs:\n\n$$\n\\text{Teacher} \\rightarrow \\text{Subject}, \\quad \\text{Subject} \\rightarrow \\text{Department}\n$$\n\nCandidate key: $\\{$Teacher$\\}$.\n\n- $\\text{Teacher} \\rightarrow \\text{Subject}$ — OK (Teacher is a superkey).\n- $\\text{Subject} \\rightarrow \\text{Department}$ — Violates BCNF (Subject is not a superkey).\n\n**BCNF decomposition:**\n\n$$\nR_1(\\underline{\\text{Subject}},\\ \\text{Department}), \\quad R_2(\\underline{\\text{Teacher}},\\ \\text{Subject})\n$$\n\nThe FD $\\text{Teacher} \\rightarrow \\text{Subject}$ spans only $R_2$ (preserved ✅), but the FD $\\text{Subject} \\rightarrow \\text{Department}$ spans only $R_1$ (preserved ✅). Now suppose we had an additional FD $\\text{Teacher} \\rightarrow \\text{Department}$ (derived by transitivity). This **derived FD crosses both relations** and cannot be enforced in either single relation — it is **lost**.\n\n> [!WARNING]\n> **KTU Examiner's Pitfall Callout**\n> Examiners deduct marks for the following common mistakes:\n> 1. **Confusing \"prime\" with \"primary key attribute.\"** A prime attribute is one that belongs to *any* candidate key — not just the chosen primary key. Many students only check the chosen PK and miss alternate keys.\n> 2. **Skipping the closure computation.** Jumping directly to a normal-form verdict without first computing $X^{+}$ to confirm a candidate key is a frequent 2-to-3-mark loss.\n> 3. **Forgetting 1NF before checking 2NF/3NF/BCNF.** A relation with multi-valued cells is automatically *not* in 2NF, 3NF, or BCNF — the student must first resolve 1NF violations.\n> 4. **Stating \"BCNF is always not dependency-preserving.\"** It is *not always*, only *not guaranteed*. A blank statement loses the precision marks.\n> 5. **Mixing up partial and transitive dependencies.** Partial = non-prime depends on *part* of a composite key. Transitive = key → non-key → non-key. Examiners check this distinction explicitly.\n> 6. **Not verifying lossless-join and dependency-preservation.** A complete KTU answer *always* ends with these two verifications for the produced decomposition.\n\n### 5.3 Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- [x] **Normalization** = systematic decomposition of relations to remove redundancy and anomalies.\n- [x] Three **anomalies**: insertion, deletion, update.\n- [x] **Normal form hierarchy**: 1NF ⊂ 2NF ⊂ 3NF ⊂ BCNF ⊂ 4NF ⊂ 5NF ⊂ DKNF.\n- [x] **1NF rule:** every cell atomic, no repeating groups.\n- [x] **2NF rule:** eliminate *partial* dependencies on composite keys.\n- [x] **3NF rule:** eliminate *transitive* dependencies on non-prime attributes.\n- [x] **BCNF rule:** for every non-trivial FD, LHS must be a superkey.\n- [x] **Attribute closure $X^{+}$** is the *central computational tool* — used to find candidate keys and test FD membership.\n- [x] **Armstrong's axioms** (reflexivity, augmentation, transitivity) are the inference engine for FDs.\n- [x] **Prime attribute** = belongs to *any* candidate key. **Non-prime** = belongs to *none*.\n- [x] **Lossless-join test:** $R_1 \\cap R_2 \\rightarrow R_1$ OR $R_1 \\cap R_2 \\rightarrow R_2$.\n- [x] **Dependency preservation:** $(F_1 \\cup F_2 \\cup \\dots \\cup F_n)^{+} = F^{+}$.\n- [x] **3NF guarantees** dependency preservation; **BCNF does not always**.\n- [x] **Use 3NF** when you need guaranteed FD enforcement; **use BCNF** when you need maximal redundancy elimination.\n- [x] **OLTP** systems = fully normalized (BCNF/3NF). **OLAP/data warehouses** = often denormalized for speed.\n- [x] Always **state the candidate key, prime/non-prime split, the violating FD, and the decomposition rationale** in KTU exam answers.\n- [x] Always **verify lossless-join and dependency-preservation** at the end of every decomposition answer.\n\n> [!TIP]\n> **One-line mnemonic for the BCNF vs 3NF difference:** *\"3NF forgives a non-superkey determinant **only if the dependent is a prime attribute**; BCNF never forgives.\"*\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "sqlqueriesdevelopasetofsqlqueriestodemonstratecrudoperationscreatereadupdatedeletecomplexjoinsaggregationsandanyspecializedqueriesrequiredbyyourproject": {
      "title": "SQL Queries: Develop a set of SQL queries to demonstrate CRUD operations (Create, Read, Update, Delete), complex joins, aggregations, and any specialized queries required by your project",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 2\n> - **Topic:** SQL Queries: Develop a set of SQL queries to demonstrate CRUD operations (Create, Read, Update, Delete), complex joins, aggregations, and any specialized queries required by your project\n\n<!-- SECTION_1_START -->\n# SECTION 1 — Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of SQL\n\n**Structured Query Language (SQL)** is the standard declarative, set-oriented, high-level database language defined by **ISO/IEC 9075** and used to define, manipulate, retrieve, and control data in a **Relational Database Management System (RDBMS)**. As per the KTU 2024 Scheme syllabus, the relevant sub-languages are:\n\n- **DDL (Data Definition Language):** `CREATE`, `ALTER`, `DROP`, `TRUNCATE`\n- **DML (Data Manipulation Language):** `SELECT`, `INSERT`, `UPDATE`, `DELETE`\n- **DCL (Data Control Language):** `GRANT`, `REVOKE`\n- **TCL (Transaction Control Language):** `COMMIT`, `ROLLBACK`, `SAVEPOINT`\n\n> [!IMPORTANT]\n> **CRUD** is the universal acronym that maps directly onto SQL DML operations:\n> - **C — Create** → `INSERT`\n> - **R — Read** → `SELECT`\n> - **U — Update** → `UPDATE`\n> - **D — Delete** → `DELETE`\n\n## 1.2 Conceptual Analogy — \"The Bank Locker Room\"\n\nImagine a bank's **locker room** as your database:\n\n| Locker Room Component | SQL Equivalent |\n|---|---|\n| The room itself with rows of lockers | **Database** (collection of tables) |\n| Each locker | A **Table / Relation** |\n| Each locker has a numbered key | A **Primary Key** |\n| Your signed authorization slip | A **SQL Query / Transaction** |\n| Putting a new document inside | **INSERT** (Create) |\n| Reading the document to know balance | **SELECT** (Read) |\n| Editing the name on the document | **UPDATE** |\n| Tearing up the document and removing it | **DELETE** |\n\nJust as a bank teller must follow strict protocols to ensure the locker integrity, SQL must respect **ACID properties** (Atomicity, Consistency, Isolation, Durability) when running CRUD operations.\n\n> [!NOTE]\n> **Why this matters in your KTU Mini-Project:** Every module in your project (registration, login, marks entry, inventory tracking) will eventually boil down to these four verbs. Mastering them guarantees clean DBMS viva answers and a working project.\n\n## 1.3 Reference Schema Used Throughout These Notes\n\nFor uniform explanation, all examples use the canonical **Student–Course–Enrollment** schema.\n\n**STUDENT** (*StudentID*, Name, DeptID, DOB, CGPA, Email)\n**COURSE** (*CourseID*, CourseName, Credits, DeptID)\n**ENROLLMENT** (*EnrollID*, StudentID, CourseID, Semester, Grade)\n**DEPARTMENT** (*DeptID*, DeptName, HOD)\n\nPrimary keys are **bold-italic**, foreign keys are plain. This is the exact standard ER notation followed in KTU exam answer scripts.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2 — Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The CRUD Operational Stack\n\nEach CRUD operation is a pipeline with three logical layers:\n\n1. **Authorization layer** — checked via DCL grants\n2. **Parsing & Optimization layer** — query rewritten by the optimizer\n3. **Execution layer** — runs against the storage engine, with `COMMIT`/`ROLLBACK` controlling visibility\n\n### 2.1.1 CREATE → `INSERT`\nLogical steps:\n- Verify the target table exists (or use `CREATE TABLE` first)\n- Validate **NOT NULL** and **CHECK** constraints\n- Validate **FOREIGN KEY** references\n- Insert the tuple, update indexes, log to **write-ahead log (WAL)**\n- Commit or roll back the transaction\n\n### 2.1.2 READ → `SELECT`\nLogical steps:\n- **FROM** clause resolves tables/views\n- **JOIN** merges rows using predicate (ON/USING)\n- **WHERE** filters rows\n- **GROUP BY** partitions into groups\n- **HAVING** filters groups\n- **SELECT** projects columns and applies expressions\n- **DISTINCT** removes duplicates\n- **ORDER BY** sorts\n- **LIMIT/OFFSET** slices the result\n\n### 2.1.3 UPDATE → `UPDATE`\nLogical steps:\n- Locate target rows via `WHERE` (omitting `WHERE` updates **all** rows — a classic KTU viva trap!)\n- Evaluate `SET` expressions\n- Apply referential integrity checks\n- Log to WAL\n- Commit\n\n### 2.1.4 DELETE → `DELETE`\nLogical steps:\n- Locate target rows via `WHERE`\n- Check **ON DELETE CASCADE / RESTRICT / SET NULL** constraints\n- Either mark row as tombstone or physically delete\n- Commit\n\n## 2.2 KTU High-Yield SQL Reference Table\n\n| Operation | SQL Keyword | Mandatory Clauses | Optional Clauses | Pitfall |\n|---|---|---|---|---|\n| Create row | `INSERT INTO … VALUES …` | Table name, column list or VALUES | `RETURNING` (PostgreSQL) | Column-count mismatch |\n| Read rows | `SELECT` | `SELECT` list, `FROM` | `WHERE`, `GROUP BY`, `HAVING`, `ORDER BY`, `LIMIT` | Cartesian explosion if join missing |\n| Update row | `UPDATE … SET …` | Table name, SET clause | `WHERE` | Forgetting `WHERE` |\n| Delete row | `DELETE FROM …` | Table name | `WHERE` | Forgetting `WHERE` |\n| Combine tables | `JOIN` | `ON` predicate | `USING`, NATURAL | Wrong join type |\n| Aggregate | `COUNT`, `SUM`, `AVG`, `MIN`, `MAX` | Function in SELECT | `GROUP BY` | Mixing aggregates with non-grouped columns |\n| Subquery | `(SELECT …)` | Enclosing parentheses | `IN`, `EXISTS`, `=` | Correlated subquery perf |\n| Set op | `UNION`, `INTERSECT`, `EXCEPT` | Same column count | `ALL` | Type mismatches |\n\n> [!NOTE]\n> **Engineering Utility:** CRUD operations power almost every real-world software — from ATM withdrawals (`UPDATE` balance) to UPI transactions (`INSERT` into transaction log) to Amazon product listings (`SELECT` with pagination).\n\n## 2.3 Theoretical Foundations of Joins\n\nA **join** is the relational algebra operator $\\Join$ that produces a combined relation from two operands using a predicate $P$.\n\n$$\nR \\Join_{P} S \\;=\\; \\sigma_{P}(R \\times S)\n$$\n\n- **INNER JOIN** returns rows where predicate holds in both tables.\n- **LEFT OUTER JOIN** preserves all left rows, padding unmatched right columns with `NULL`.\n- **RIGHT OUTER JOIN** is the mirror.\n- **FULL OUTER JOIN** preserves rows from both sides.\n- **CROSS JOIN** is the unrestricted Cartesian product $R \\times S$.\n- **SELF JOIN** joins a table to itself (e.g., employee–manager).\n\n## 2.4 Aggregation Theory\n\nThe SQL aggregate operators are defined over a multiset of values $V = \\{v_1, v_2, \\ldots, v_n\\}$:\n\n$$\n\\text{COUNT}(V) = n\n$$\n\n$$\n\\text{SUM}(V) = \\sum_{i=1}^{n} v_i\n$$\n\n$$\n\\text{AVG}(V) = \\frac{1}{n}\\sum_{i=1}^{n} v_i\n$$\n\n$$\n\\text{MIN}(V) = \\min_{i} v_i,\\qquad \\text{MAX}(V) = \\max_{i} v_i\n$$\n\nAll aggregates (except `COUNT(*)`) **ignore NULL** values — a viva favourite.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3 — Step-by-Step Derivations & Analytical Matrix\n\nThe following walkthrough uses the **Student–Course–Enrollment** schema. Every query is explained line-by-line to ensure 100% comprehension for KTU 2024 examinations.\n\n## 3.1 Schema Definition (DDL)\n\n```sql\nCREATE TABLE Department (\n    DeptID   INT PRIMARY KEY,\n    DeptName VARCHAR(50) NOT NULL UNIQUE,\n    HOD      VARCHAR(50)\n);\n\nCREATE TABLE Student (\n    StudentID INT PRIMARY KEY,\n    Name      VARCHAR(50) NOT NULL,\n    DeptID    INT,\n    DOB       DATE,\n    CGPA      DECIMAL(4,2) CHECK (CGPA BETWEEN 0 AND 10),\n    Email     VARCHAR(100) UNIQUE,\n    FOREIGN KEY (DeptID) REFERENCES Department(DeptID)\n);\n\nCREATE TABLE Course (\n    CourseID   INT PRIMARY KEY,\n    CourseName VARCHAR(50) NOT NULL,\n    Credits    INT CHECK (Credits > 0),\n    DeptID     INT,\n    FOREIGN KEY (DeptID) REFERENCES Department(DeptID)\n);\n\nCREATE TABLE Enrollment (\n    EnrollID  INT PRIMARY KEY,\n    StudentID INT NOT NULL,\n    CourseID  INT NOT NULL,\n    Semester  VARCHAR(10),\n    Grade     CHAR(2) CHECK (Grade IN ('A+','A','B+','B','C+','C','D','F')),\n    FOREIGN KEY (StudentID) REFERENCES Student(StudentID) ON DELETE CASCADE,\n    FOREIGN KEY (CourseID)  REFERENCES Course(CourseID)\n);\n```\n\n> [!NOTE]\n> **Line-by-line logic:** `PRIMARY KEY` enforces entity integrity. `CHECK` enforces domain integrity. `FOREIGN KEY … ON DELETE CASCADE` enforces referential integrity with cascading deletes — a frequently asked viva question.\n\n## 3.2 CREATE — Inserting Data\n\n```sql\n-- Single-row insert\nINSERT INTO Student (StudentID, Name, DeptID, DOB, CGPA, Email)\nVALUES (101, 'Ananya Nair', 1, '2004-05-12', 9.12, 'ananya@ktu.in');\n```\n\n**Step-by-step reasoning:**\n1. The `INSERT INTO` clause specifies the target table.\n2. The column list dictates the order of values in `VALUES`.\n3. The DBMS validates `CGPA` against the `CHECK (0..10)` constraint.\n4. `DeptID = 1` is verified against the `Department` table.\n5. Upon success, the row is written to disk and the unique index on `Email` is updated.\n\n```sql\n-- Multi-row insert (bulk loading)\nINSERT INTO Student (StudentID, Name, DeptID, DOB, CGPA, Email) VALUES\n(102, 'Rahul Menon', 1, '2003-11-04', 8.45, 'rahul@ktu.in'),\n(103, 'Sneha Pillai', 2, '2004-01-22', 8.95, 'sneha@ktu.in'),\n(104, 'Arjun Das',    3, '2003-07-18', 7.20, 'arjun@ktu.in'),\n(105, 'Meera Joseph', 1, '2004-03-09', 9.55, 'meera@ktu.in');\n```\n\n```sql\n-- Insert using a subquery (create-then-load pattern)\nINSERT INTO TopPerformers (StudentID, Name, CGPA)\nSELECT StudentID, Name, CGPA FROM Student WHERE CGPA > 9.0;\n```\n\n## 3.3 READ — Select Queries of Increasing Complexity\n\n### 3.3.1 Projection\n```sql\nSELECT Name, Email FROM Student;\n```\nReturns only the requested columns. If the table has 10,000 rows, all 10,000 are produced unless a `WHERE` clause filters them.\n\n### 3.3.2 Selection with predicate\n```sql\nSELECT Name, CGPA FROM Student\nWHERE DeptID = 1 AND CGPA >= 8.5;\n```\n**Logic:** the `WHERE` clause is evaluated **before** projection. Combining conditions with `AND`/`OR` requires parentheses for clarity.\n\n### 3.3.3 Sorting and limiting\n```sql\nSELECT Name, CGPA FROM Student\nORDER BY CGPA DESC\nLIMIT 5 OFFSET 0;\n```\nReturns the **top 5** students by CGPA. `OFFSET 5` would return students ranked 6–10 — essential for **pagination** in your project.\n\n### 3.3.4 DISTINCT\n```sql\nSELECT DISTINCT DeptID FROM Student;\n```\nRemoves duplicate `DeptID` values to show which departments currently have at least one student.\n\n### 3.3.5 LIKE pattern matching\n```sql\nSELECT * FROM Student WHERE Name LIKE 'A%';\n```\nThe `%` wildcard matches any sequence of zero or more characters. `_` matches exactly one character.\n\n## 3.4 UPDATE — Modifying Data\n\n```sql\n-- Single-row update\nUPDATE Student SET CGPA = 9.30 WHERE StudentID = 101;\n```\n\n```sql\n-- Bulk update with computation\nUPDATE Student SET CGPA = CGPA + 0.05 WHERE DeptID = 1;\n```\nAdds a 0.05 grace-point bonus to all CS students. **Explanation:** the right-hand side is evaluated first using the **old** value, then the new value is written.\n\n```sql\n-- Update with subquery (cross-table)\nUPDATE Enrollment\nSET Grade = 'A+'\nWHERE CourseID = (SELECT CourseID FROM Course WHERE CourseName = 'DBMS')\n  AND Semester = 'S5';\n```\n\n> [!WARNING]\n> **Critical pitfall:** `UPDATE Student SET CGPA = 9.5;` (no `WHERE`) modifies **every** row. In MySQL, the safe-update mode can be enabled with `SET SQL_SAFE_UPDATES = 1;`.\n\n## 3.5 DELETE — Removing Data\n\n```sql\n-- Single-row delete\nDELETE FROM Student WHERE StudentID = 110;\n```\n\n```sql\n-- Conditional delete\nDELETE FROM Enrollment WHERE Semester = 'S3' AND Grade = 'F';\n```\n\n```sql\n-- Truncate vs. Delete\nTRUNCATE TABLE TopPerformers;\n```\n**Difference:** `DELETE` is DML, row-by-row, transaction-logged, can use `WHERE`. `TRUNCATE` is DDL, resets the table wholesale, cannot be rolled back in many RDBMSs, and is much faster for emptying large tables.\n\n## 3.6 Complex Joins\n\n### 3.6.1 INNER JOIN — three-table join\n```sql\nSELECT s.Name, c.CourseName, e.Grade, d.DeptName\nFROM   Student s\nJOIN   Enrollment e ON s.StudentID = e.StudentID\nJOIN   Course     c ON e.CourseID  = c.CourseID\nJOIN   Department d ON s.DeptID    = d.DeptID\nWHERE  e.Semester = 'S5'\nORDER  BY s.Name;\n```\n**Logic:**\n1. Join `Student` with `Enrollment` on `StudentID`.\n2. Join the result with `Course` on `CourseID`.\n3. Join the result with `Department` on `DeptID`.\n4. Filter rows where `Semester = 'S5'`.\n5. Sort by student name alphabetically.\n\n### 3.6.2 LEFT OUTER JOIN — find students with no enrollment\n```sql\nSELECT s.StudentID, s.Name\nFROM   Student s\nLEFT   JOIN Enrollment e ON s.StudentID = e.StudentID\nWHERE  e.EnrollID IS NULL;\n```\n**Logic:** A `LEFT JOIN` keeps every student. If no matching enrollment exists, the right side is `NULL`. The `IS NULL` filter retains only those unmatched rows — the canonical \"anti-join\" pattern.\n\n### 3.6.3 SELF JOIN — students from the same department\n```sql\nSELECT a.Name AS Student1, b.Name AS Student2, a.DeptID\nFROM   Student a\nJOIN   Student b ON a.DeptID = b.DeptID AND a.StudentID < b.StudentID;\n```\nThe `a.StudentID < b.StudentID` predicate prevents pairing a student with themselves and avoids duplicate pairs.\n\n### 3.6.4 FULL OUTER JOIN — students OR courses with no link\n```sql\nSELECT s.Name, c.CourseName\nFROM   Student s\nFULL   OUTER JOIN Enrollment e ON s.StudentID = e.StudentID\nFULL   OUTER JOIN Course c     ON e.CourseID  = c.CourseID\nWHERE  s.StudentID IS NULL OR c.CourseID IS NULL;\n```\n\n## 3.7 Aggregations & GROUP BY\n\n### 3.7.1 Simple aggregation\n```sql\nSELECT COUNT(*)        AS TotalStudents,\n       AVG(CGPA)       AS AvgCGPA,\n       MAX(CGPA)       AS TopCGPA,\n       MIN(CGPA)       AS LowestCGPA\nFROM   Student;\n```\n\n### 3.7.2 GROUP BY with HAVING\n```sql\nSELECT d.DeptName,\n       COUNT(s.StudentID) AS StudentCount,\n       AVG(s.CGPA)        AS AvgDeptCGPA\nFROM   Department d\nLEFT   JOIN Student s ON d.DeptID = s.DeptID\nGROUP  BY d.DeptName\nHAVING COUNT(s.StudentID) > 5\nORDER  BY AvgDeptCGPA DESC;\n```\n**Logic:**\n- `GROUP BY` partitions the joined rows by department.\n- `COUNT(s.StudentID)` counts non-NULL matches (a `LEFT JOIN` puts `NULL` for empty depts).\n- `HAVING` filters **groups** (use `WHERE` for rows, `HAVING` for groups — common viva trap!).\n\n### 3.7.3 Aggregation with subquery — top scorers per course\n```sql\nSELECT c.CourseName, s.Name, e.Grade\nFROM   Enrollment e\nJOIN   Student s ON e.StudentID = s.StudentID\nJOIN   Course  c ON e.CourseID  = c.CourseID\nWHERE  (e.CourseID, e.Grade) IN (\n        SELECT CourseID, MIN(Grade)    -- assuming A+ sorts lowest alphabetically\n        FROM   Enrollment\n        GROUP  BY CourseID);\n```\n\n## 3.8 Subqueries & Set Operations\n\n```sql\n-- Students who took DBMS OR TOC\nSELECT Name FROM Student\nWHERE StudentID IN (\n    SELECT StudentID FROM Enrollment e\n    JOIN Course c ON e.CourseID = c.CourseID\n    WHERE c.CourseName IN ('DBMS','TOC'))\nUNION\nSELECT Name FROM Student WHERE CGPA > 9.0;\n```\n\n**Logic:** `UNION` combines and de-duplicates; `UNION ALL` retains duplicates; `INTERSECT` keeps only common rows; `EXCEPT` keeps rows in the first query but not the second.\n\n## 3.9 Analytical (Window) Functions — Project Favourite\n\n```sql\nSELECT s.Name, c.CourseName, e.Grade,\n       RANK() OVER (PARTITION BY c.CourseID ORDER BY e.Grade ASC) AS Position\nFROM   Enrollment e\nJOIN   Student s ON e.StudentID = s.StudentID\nJOIN   Course  c ON e.CourseID  = c.CourseID;\n```\n\n**Logic:**\n- `PARTITION BY` resets the rank for each course.\n- `ORDER BY Grade ASC` ranks grade-wise (A+ is best).\n- `RANK()` ties and skips; `DENSE_RANK()` ties but does not skip; `ROW_NUMBER()` is unique.\n\n## 3.10 Transaction Control (CRUD safety net)\n\n```sql\nSTART TRANSACTION;\n    UPDATE Student SET CGPA = CGPA + 0.10 WHERE StudentID = 101;\n    INSERT INTO AuditLog (Action, StudentID, Time) VALUES ('GRACE',101,NOW());\nCOMMIT;\n-- ROLLBACK;  -- uncomment to undo\n```\nThis guarantees that the grade update and the audit log are **atomic** — both happen or neither happens.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4 — Structural Diagrams & Schematics\n\n## 4.1 CRUD-to-Operations Mapping\n\n```mermaid\nflowchart TD\n    A[Application / User Request] --> B{Operation Type}\n    B -- C --> C1[INSERT INTO Table]\n    B -- R --> C2[SELECT with WHERE/JOIN/GROUP BY]\n    B -- U --> C3[UPDATE with SET and WHERE]\n    B -- D --> C4[DELETE FROM with WHERE]\n    C1 --> D[Constraint Validation]\n    C2 --> D\n    C3 --> D\n    C4 --> D\n    D -- Pass --> E[Query Optimizer]\n    D -- Fail --> F[Return Error]\n    E --> G[Execution Engine]\n    G --> H[Write-Ahead Log]\n    H -- COMMIT --> I[Persistent Storage]\n    H -- ROLLBACK --> J[Undo Log Restore]\n```\n\n## 4.2 SQL Logical Query Processing Order\n\n```mermaid\nflowchart TD\n    S1[Step 1: FROM & JOINs] --> S2[Step 2: WHERE]\n    S2 --> S3[Step 3: GROUP BY]\n    S3 --> S4[Step 4: HAVING]\n    S4 --> S5[Step 5: SELECT projection]\n    S5 --> S6[Step 6: DISTINCT]\n    S6 --> S7[Step 7: ORDER BY]\n    S7 --> S8[Step 8: LIMIT / OFFSET]\n```\n\n> [!NOTE]\n> **Why this matters:** Students often write `SELECT` first and `WHERE` last. SQL is **declarative** but the engine processes clauses in the order shown above. Aliases defined in `SELECT` cannot be used in `WHERE` for this reason.\n\n## 4.3 Entity-Relationship Overview of Reference Schema\n\n```mermaid\nerDiagram\n    DEPARTMENT ||--o{ STUDENT : \"has\"\n    DEPARTMENT ||--o{ COURSE : \"offers\"\n    STUDENT ||--o{ ENROLLMENT : \"registers\"\n    COURSE  ||--o{ ENROLLMENT : \"is taken in\"\n    DEPARTMENT {\n        int DeptID PK\n        string DeptName\n        string HOD\n    }\n    STUDENT {\n        int StudentID PK\n        string Name\n        int DeptID FK\n        date DOB\n        decimal CGPA\n        string Email\n    }\n    COURSE {\n        int CourseID PK\n        string CourseName\n        int Credits\n        int DeptID FK\n    }\n    ENROLLMENT {\n        int EnrollID PK\n        int StudentID FK\n        int CourseID FK\n        string Semester\n        string Grade\n    }\n```\n\n## 4.4 Project-Ready CRUD Function Pipeline\n\n```mermaid\nflowchart LR\n    UI[Frontend UI Form] --> API[Backend API Endpoint]\n    API --> V[Input Validation]\n    V --> P[Prepared SQL Statement]\n    P --> DB[(RDBMS Engine)]\n    DB --> R[Result Set / Rowcount]\n    R --> M[Map to JSON / View]\n    M --> UI\n    DB -.Error.-> EH[Exception Handler]\n    EH --> UI\n```\n\n> [!IMPORTANT]\n> **Engineering takeaway:** In your KTU mini-project (e.g., Library, Hospital, or Inventory), every UI button maps to one CRUD verb, which maps to one prepared statement. Using **parameterized queries** (`?` placeholders) prevents **SQL Injection** — a guaranteed viva question.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. [KTU University Exam – May 2024]\n**Differentiate between DELETE, TRUNCATE, and DROP in SQL.**\n\n**Model Answer:**\n\n| Aspect | `DELETE` | `TRUNCATE` | `DROP` |\n|---|---|---|---|\n| Type | DML | DDL | DDL |\n| Removes | Specific rows (`WHERE`) | All rows | Entire table (structure + data) |\n| Rollback | Possible (transactional) | Usually not | Not possible |\n| Triggers fire? | Yes | No | No |\n| Speed | Slow (row-by-row) | Fast | Fastest |\n\n*Mnemonic:* **D**ELETE deletes **D**ata, **D**ROP deletes **D**efinition, **T**RUNCATE **T**akes everything.\n\n### Q2. [KTU University Exam – Dec 2023]\n**What is the difference between WHERE and HAVING clause? Give an example.**\n\n**Model Answer:**\n- `WHERE` filters **rows** **before** aggregation; cannot use aggregate functions.\n- `HAVING` filters **groups** **after** aggregation; can use aggregate functions.\n\n**Example:**\n```sql\nSELECT DeptID, AVG(CGPA)\nFROM   Student\nWHERE  CGPA > 5.0            -- row filter (no aggregate)\nGROUP  BY DeptID\nHAVING AVG(CGPA) > 8.0;      -- group filter (aggregate allowed)\n```\n\n---\n\n## 5.2 Part B — Full 14-Mark Question (Internal Choice)\n\n### Question A — 14 Marks\n**[KTU University Exam – Model Paper 2024, Module 2]**\n\n(a) *For the Student–Course–Enrollment schema above, write SQL queries to perform the following:* **[7 Marks]**\n1. Insert a new student record.\n2. List the names of all students who scored 'A+' in any course along with the course name.\n3. Update the grade of all students in 'DBMS' course to 'B+' for Semester 'S4'.\n4. Delete all enrollment records where the grade is 'F' and the semester is 'S1'.\n\n(b) *Demonstrate a three-table INNER JOIN, a LEFT OUTER JOIN, and a GROUP BY query with HAVING on the same schema. Show the output for each.* **[7 Marks]**\n\n---\n\n#### Model Solution for Q A (a)\n\n**(i) Insert a new student:**\n```sql\nINSERT INTO Student (StudentID, Name, DeptID, DOB, CGPA, Email)\nVALUES (106, 'Vivek Kumar', 2, '2004-09-15', 8.75, 'vivek@ktu.in');\n```\n**Explanation:** The columns and values must align positionally. Constraints on `CGPA` and `Email` will be auto-validated.\n\n**(ii) List students with 'A+' grades:**\n```sql\nSELECT s.Name, c.CourseName\nFROM   Student s\nJOIN   Enrollment e ON s.StudentID = e.StudentID\nJOIN   Course     c ON e.CourseID  = c.CourseID\nWHERE  e.Grade = 'A+';\n```\n\n**(iii) Update grades:**\n```sql\nUPDATE Enrollment\nSET    Grade = 'B+'\nWHERE  Semester = 'S4'\n  AND  CourseID = (SELECT CourseID FROM Course WHERE CourseName = 'DBMS');\n```\n\n**(iv) Delete failed records:**\n```sql\nDELETE FROM Enrollment\nWHERE  Grade = 'F' AND Semester = 'S1';\n```\n\n---\n\n#### Model Solution for Q A (b)\n\n**Three-table INNER JOIN — S5 result sheet:**\n```sql\nSELECT s.StudentID, s.Name, c.CourseName, e.Grade, d.DeptName\nFROM   Student s\nINNER  JOIN Enrollment e ON s.StudentID = e.StudentID\nINNER  JOIN Course     c ON e.CourseID  = c.CourseID\nINNER  JOIN Department d ON s.DeptID    = d.DeptID\nWHERE  e.Semester = 'S5'\nORDER  BY s.StudentID;\n```\n**Output (sample):**\n| StudentID | Name | CourseName | Grade | DeptName |\n|---|---|---|---|---|\n| 101 | Ananya Nair | DBMS | A+ | CSE |\n| 102 | Rahul Menon | DBMS | A  | CSE |\n| 105 | Meera Joseph | DBMS | A+ | CSE |\n\n**LEFT OUTER JOIN — courses with no enrolments:**\n```sql\nSELECT c.CourseID, c.CourseName\nFROM   Course c\nLEFT   JOIN Enrollment e ON c.CourseID = e.CourseID\nWHERE  e.EnrollID IS NULL;\n```\n**Logic:** preserves every course; the `IS NULL` filter reveals orphans.\n\n**GROUP BY with HAVING — departments with avg CGPA > 8.0:**\n```sql\nSELECT d.DeptName,\n       COUNT(s.StudentID) AS NumStudents,\n       ROUND(AVG(s.CGPA),2) AS AvgCGPA\nFROM   Department d\nJOIN   Student s ON d.DeptID = s.DeptID\nGROUP  BY d.DeptName\nHAVING AVG(s.CGPA) > 8.0\nORDER  BY AvgCGPA DESC;\n```\n\n---\n\n### Question B — 14 Marks (Alternative Choice)\n**[KTU University Exam – Model Paper 2024, Module 2]**\n\n(a) *Explain the ACID properties of a transaction with a banking-style example demonstrating two concurrent `UPDATE` operations on the same row.* **[7 Marks]**\n\n(b) *Write SQL queries for:* **[7 Marks]**\n1. The second-highest CGPA student in each department (use subquery or window function).\n2. Students who have enrolled in **all** courses offered by the CSE department.\n3. Compute, for each course, the total credits earned by students who got an 'A' or 'A+'.\n\n---\n\n#### Model Solution for Q B (a)\n\n**ACID Properties:**\n\n| Letter | Property | Banking Example |\n|---|---|---|\n| **A** | Atomicity | Transfer ₹5000 from A→B: debit and credit must both happen. |\n| **C** | Consistency | Total balance remains constant; constraints not violated. |\n| **I** | Isolation | Two parallel transfers of A→B and A→C must not interleave incorrectly. |\n| **D** | Durability | Once committed, the transfer survives a power failure. |\n\n**Concurrent UPDATE demonstration:**\n```sql\n-- Session 1\nSTART TRANSACTION;\nUPDATE Account SET Balance = Balance - 5000 WHERE AccID = 'A';\n-- ... (delay)\nUPDATE Account SET Balance = Balance + 5000 WHERE AccID = 'B';\nCOMMIT;\n\n-- Session 2 (concurrent)\nSTART TRANSACTION;\nUPDATE Account SET Balance = Balance - 3000 WHERE AccID = 'A';\nCOMMIT;\n```\nWithout proper isolation (e.g., `SERIALIZABLE`), the lost-update problem can occur. The engine uses **row-level locks** and **write-ahead logging** to prevent it.\n\n---\n\n#### Model Solution for Q B (b)\n\n**(i) Second-highest CGPA per department (window function):**\n```sql\nSELECT Name, DeptID, CGPA\nFROM (\n    SELECT s.Name, s.DeptID, s.CGPA,\n           DENSE_RANK() OVER (PARTITION BY s.DeptID ORDER BY s.CGPA DESC) AS rnk\n    FROM   Student s\n) ranked\nWHERE rnk = 2;\n```\n\n**Alternative using correlated subquery:**\n```sql\nSELECT Name, DeptID, CGPA\nFROM   Student s1\nWHERE  1 = (\n    SELECT COUNT(DISTINCT s2.CGPA)\n    FROM   Student s2\n    WHERE  s2.DeptID = s1.DeptID AND s2.CGPA > s1.CGPA);\n```\n\n**(ii) Students enrolled in ALL CSE courses:**\n```sql\nSELECT s.Name\nFROM   Student s\nJOIN   Enrollment e ON s.StudentID = e.StudentID\nJOIN   Course     c ON e.CourseID  = c.CourseID\nWHERE  c.DeptID = (SELECT DeptID FROM Department WHERE DeptName = 'CSE')\nGROUP  BY s.StudentID, s.Name\nHAVING COUNT(DISTINCT c.CourseID) =\n       (SELECT COUNT(*) FROM Course\n        WHERE DeptID = (SELECT DeptID FROM Department WHERE DeptName='CSE'));\n```\n**Logic:** A student is \"enrolled in all\" if the count of distinct courses they take equals the total number of CSE courses. This is the classic **relational division** pattern — a KTU favourite.\n\n**(iii) Total credits earned by 'A' and 'A+' students per course:**\n```sql\nSELECT c.CourseName,\n       SUM(c.Credits) AS TotalCreditsByToppers\nFROM   Course c\nJOIN   Enrollment e ON c.CourseID = e.CourseID\nWHERE  e.Grade IN ('A','A+')\nGROUP  BY c.CourseID, c.CourseName;\n```\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfalls — Where Students Lose Marks**\n> 1. **Forgetting `WHERE` in `UPDATE` / `DELETE`** — wipes entire table; expect 0 marks.\n> 2. **Mixing aggregate and non-aggregate columns in `SELECT`** without `GROUP BY` — strict-mode SQL will throw an error; loose-mode MySQL hides the bug but examiner deducts.\n> 3. **Wrong join type** — using `INNER JOIN` where `LEFT JOIN` is required (e.g., for orphan rows) loses conceptual marks.\n> 4. **Confusing `UNION` (dedup) with `UNION ALL` (no dedup)** — read the question carefully.\n> 5. **Quoting string literals incorrectly** — single quotes for SQL strings, double quotes for identifiers (ANSI).\n> 6. **In viva:** Not knowing the **logical processing order** (FROM → WHERE → GROUP BY → HAVING → SELECT → DISTINCT → ORDER BY → LIMIT) costs easy marks.\n> 7. **Schema missing in the answer sheet** — always draw the **schema/ER diagram first** before writing queries; examiners award 1–2 marks just for this.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- **CRUD = INSERT, SELECT, UPDATE, DELETE.** All four are DML except `TRUNCATE` (DDL).\n- Always draw the **schema/ER diagram** before writing queries in the answer sheet.\n- SQL is **declarative** — you say *what* you want, the engine decides *how*.\n- **Logical order of execution:** `FROM` → `JOIN` → `WHERE` → `GROUP BY` → `HAVING` → `SELECT` → `DISTINCT` → `ORDER BY` → `LIMIT`.\n- `WHERE` filters **rows**; `HAVING` filters **groups**.\n- `COUNT(*)`, `SUM`, `AVG`, `MIN`, `MAX` are the five aggregate functions.\n- **Joins:** INNER, LEFT, RIGHT, FULL, CROSS, SELF — choose by what you want to keep.\n- `INNER JOIN` = intersection, `LEFT JOIN` = all left + matched right, `FULL JOIN` = union of both.\n- **Relational division pattern** (`HAVING COUNT = total`) is used for \"all/enrolled-in-every\" questions.\n- **Window functions** (`RANK`, `DENSE_RANK`, `ROW_NUMBER`) outperform correlated subqueries for ranking.\n- `UNION` removes duplicates; `UNION ALL` does not.\n- **Transactions** must follow **ACID**; use `COMMIT`/`ROLLBACK` to enforce atomicity.\n- In projects, always use **prepared/parameterized statements** to prevent **SQL injection**.\n- `TRUNCATE` resets the table, `DELETE` removes rows, `DROP` removes the table itself.\n- **NULL handling:** aggregates (except `COUNT(*)`) skip NULLs; use `IS NULL`, not `= NULL`.\n- **Indexes** on `WHERE`/`JOIN` columns dramatically improve `SELECT` performance.\n- For your KTU mini-project: structure the **backend** around CRUD endpoints — registration = `INSERT`, dashboard = `SELECT`, edit-profile = `UPDATE`, delete-account = `DELETE`.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "timelinecreateatimelineorprojectplanwithmilestonestoensuretimelycompletion": {
      "title": "Timeline: Create a timeline or project plan with milestones to ensure timely completion",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 2\n> - **Topic:** Timeline: Create a timeline or project plan with milestones to ensure timely completion\n\n<!-- SECTION_1_START -->\n# 📘 SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\nIn the context of the **PBITT304 – Database Management System** course (KTU 2024 Scheme), a **Project Timeline** is a formally structured, chronological representation of all tasks, deliverables, and checkpoints required to complete the DBMS mini-project or course project. It is constructed using the principles of **Project Management Scheduling (PMS)**, integrating the **Work Breakdown Structure (WBS)**, **Gantt Charts**, and the **Program Evaluation and Review Technique (PERT)** to allocate finite time, manpower, and computational resources.\n\nAccording to the **Project Management Body of Knowledge (PMBOK) 7th Edition**, a milestone is formally defined as a **significant point or event in the project schedule that has zero duration**, marking the completion of a major deliverable. In the DBMS course, milestones typically correspond to the end of each SDLC phase: *Requirement Gathering → ER Modeling → Relational Schema Mapping → Normalization → SQL Implementation → Front-end Integration → Testing & Deployment*.\n\n> [!IMPORTANT]\n> **KTU 2024 Scheme Highlight:** Every course project submitted without a formally documented **Project Timeline with Milestones** is liable for a **5-mark penalty** in the internal evaluation rubric, and may be rejected outright by the project coordinator if not approved during the *Phase 0 – Project Proposal Review*.\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine you are planning a **wedding ceremony**. You wouldn't just wake up one morning and start the wedding — that would be chaotic. Instead, you create a checklist:\n- **3 months before:** Book the venue, send invitations.\n- **2 months before:** Finalize the menu, order the cake.\n- **1 month before:** Send reminders, do dress fittings.\n- **1 week before:** Confirm all vendors.\n- **Day 0 (Milestone):** 🎉 The Wedding Day!\n\nA **Database Project Timeline** works exactly the same way. The *wedding day* is your final submission deadline, and every checklist item is a *task*. The major checkpoints (3 months, 2 months, 1 month before) are your **milestones**. Without this plan, you risk delivering an incomplete or buggy database application on submission day.\n\n> [!NOTE]\n> **Real-World Engineering Parallel:** In the IT industry, companies like **TCS, Infosys, and Oracle** use enterprise tools such as **Jira, Microsoft Project, and Asana** to track DBMS-based application development. Every sprint in their **Agile-Scrum methodology** has a sprint review milestone, mirroring the milestones you define in your KTU DBMS project.\n\n## 1.3 Key Terminology (Syllabus-Aligned)\n\n| Term | Formal Definition | DBMS Project Context |\n|------|-------------------|----------------------|\n| **Task** | A discrete unit of work with defined start and end dates | Designing the ER Diagram |\n| **Milestone** | A zero-duration checkpoint marking completion of a phase | End of Normalization Phase |\n| **Gantt Chart** | A horizontal bar chart visualizing tasks against time | Visual timeline for the entire DBMS project |\n| **Critical Path** | The longest sequence of dependent tasks determining the minimum project duration | The bottleneck phase that delays the project |\n| **Slack/Float** | The amount of time a task can be delayed without delaying the project | Buffer time for non-critical tasks like documentation |\n| **PERT** | Program Evaluation and Review Technique for time estimation | Estimating uncertain tasks like \"User Testing\" |\n\n## 1.4 Why This Topic Matters in KTU 2024 Scheme\n\nThe **KTU 2024 NEP-aligned curriculum** emphasizes **Outcome-Based Education (OBE)**. The ability to plan, schedule, and deliver a technical project on time directly maps to:\n\n- **CO2 (Module 2):** *Design and implement a relational database schema for a real-world problem.*\n- **CO5 (Project Component):** *Demonstrate professional project management skills including planning, documentation, and timely delivery.*\n- **PO5 (Modern Tool Usage):** *Create, select, and apply appropriate techniques, resources, and modern engineering tools.*\n\n> [!TIP]\n> **Examiner Insight:** Questions on project planning and timeline creation are frequently asked in **Part A (3 marks)** of the End Semester Examination (ESE) under the course project evaluation module. Students who can articulate *what* a milestone is, *why* it is needed, and *how* to compute critical path duration score significantly higher.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 📘 SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Theoretical Framework: The DBMS Project Lifecycle\n\nA DBMS course project, in alignment with the **Software Development Life Cycle (SDLC)** and the **KTU 2024 Project Rubric**, is decomposed into **six core phases**. Each phase produces a tangible deliverable and concludes with a **milestone**.\n\n### Phase 1: Problem Identification & Requirement Gathering\n- **Objective:** Identify a real-world problem (e.g., *Library Management System*, *Hospital Database*, *E-Commerce Backend*).\n- **Deliverable:** Software Requirements Specification (SRS) document.\n- **Key Activities:** Stakeholder interviews, use case identification, functional & non-functional requirement listing.\n- **Milestone M1:** *SRS Document Approved by Guide.*\n\n### Phase 2: Conceptual Design (ER Modeling)\n- **Objective:** Translate textual requirements into an Entity-Relationship (ER) diagram.\n- **Deliverable:** ER Diagram + Entity, Attribute, Relationship inventory.\n- **Key Activities:** Identifying entities, attributes, primary keys, relationships (1:1, 1:N, M:N), cardinality, participation (total/partial).\n- **Milestone M2:** *ER Diagram Validated & Frozen.*\n\n### Phase 3: Logical Design (Relational Schema Mapping)\n- **Objective:** Convert the ER model into a set of well-defined relational tables.\n- **Deliverable:** Relational schema with primary keys, foreign keys, and constraints.\n- **Key Activities:** Applying mapping rules, eliminating M:N relationships, handling inheritance (if any), defining referential integrity.\n- **Milestone M3:** *Relational Schema Finalized.*\n\n### Phase 4: Normalization & Refinement\n- **Objective:** Eliminate data redundancy and update anomalies.\n- **Deliverable:** Normalized tables (up to **3NF** or **BCNF** as per KTU 2024 syllabus).\n- **Key Activities:** Functional dependency analysis, identifying candidate keys, applying 1NF → 2NF → 3NF → BCNF transformations.\n- **Milestone M4:** *Database Schema in 3NF/BCNF Approved.*\n\n### Phase 5: Physical Implementation (SQL + DDL/DML)\n- **Objective:** Implement the schema in an actual RDBMS (MySQL, PostgreSQL, Oracle).\n- **Deliverable:** SQL script file (.sql) with `CREATE TABLE`, `INSERT`, `UPDATE`, `DELETE`, complex `SELECT` with joins, subqueries, and aggregate functions.\n- **Key Activities:** Writing DDL, DML, DCL, TCL commands; creating indexes and views; writing stored procedures (advanced).\n- **Milestone M5:** *Working Database with Sample Data Demonstrated.*\n\n### Phase 6: Front-End Integration, Testing & Deployment\n- **Objective:** Connect the database to a front-end (PHP, Python-Django, Java Swing, Node.js) and test the system.\n- **Deliverable:** Fully functional application + Test cases + Test report.\n- **Key Activities:** UI design, API/database connectivity, unit testing, integration testing, user acceptance testing (UAT).\n- **Milestone M6:** *Final Project Submission & Demo.*\n\n## 2.2 PERT (Program Evaluation and Review Technique) – Three-Point Estimation\n\nIn real-world DBMS projects, task durations are **uncertain**. The **Optimistic Time (O)**, **Most Likely Time (M)**, and **Pessimistic Time (P)** estimates are used to compute the **Expected Time (TE)** using the beta-distribution formula:\n\n$$T_E = \\frac{O + 4M + P}{6}$$\n\nThe **Variance (σ²)** of each task is computed as:\n\n$$\\sigma^2 = \\left( \\frac{P - O}{6} \\right)^2$$\n\nThe **Standard Deviation (σ)** is:\n\n$$\\sigma = \\frac{P - O}{6}$$\n\nFor a project with multiple parallel paths, the **Project Standard Deviation** is:\n\n$$\\sigma_{project} = \\sqrt{\\sum \\sigma_{critical\\_path}^2}$$\n\n> [!IMPORTANT]\n> **The Critical Path** is identified as the path with the **maximum sum of expected times**. Any delay along the critical path directly delays the entire project.\n\n## 2.3 KTU Formula Cheat Sheet (High-Yield for Examinations)\n\n| # | Concept | Formula / Rule | Notation | Units / Remarks |\n|---|---------|---------------|----------|-----------------|\n| 1 | Expected Time (PERT) | $T_E = \\frac{O + 4M + P}{6}$ | O, M, P in days | Beta-distribution approximation |\n| 2 | Variance of a Task | $\\sigma^2 = \\left( \\frac{P - O}{6} \\right)^2$ | days² | Measure of uncertainty |\n| 3 | Standard Deviation | $\\sigma = \\frac{P - O}{6}$ | days | Square root of variance |\n| 4 | Project Std. Deviation | $\\sigma_{project} = \\sqrt{\\sum \\sigma_{CP}^2}$ | days | Sum over critical path |\n| 5 | Earliest Start (ES) | $ES_j = \\max(ES_i + D_i)$ for all predecessors i | days | Forward pass |\n| 6 | Earliest Finish (EF) | $EF_i = ES_i + D_i$ | days | ES + Duration |\n| 7 | Latest Finish (LF) | $LF_i = \\min(LS_j) - D_i$ for successors j | days | Backward pass |\n| 8 | Latest Start (LS) | $LS_i = LF_i - D_i$ | days | LF − Duration |\n| 9 | Total Float / Slack | $Slack_i = LS_i - ES_i$ | days | If 0 → Critical |\n| 10 | Free Float | $FF_i = \\min(ES_{successors}) - EF_i$ | days | Independent delay allowed |\n| 11 | Critical Path Duration | $T_{CP} = \\sum D_{critical\\_tasks}$ | days | Project minimum duration |\n| 12 | Project Completion Probability | $Z = \\frac{T_S - T_E}{\\sigma_{project}}$ | dimensionless | Z-score for given deadline $T_S$ |\n\n> [!NOTE]\n> **Engineering Utility:** In the IT industry, these formulas are embedded in tools like **Microsoft Project**, **Primavera P6**, and **Open Source alternatives like ProjectLibre**. KTU students are expected to understand the math, even if they use software to compute the values.\n\n## 2.4 Real-World Utility in Database Engineering\n\n1. **Sprint Planning in Agile-Scrum:** DBMS development teams allocate story points and time-boxed sprints (typically **2 weeks**). The expected time formula helps in committing realistic sprint goals.\n2. **Database Migration Projects:** When migrating from Oracle to PostgreSQL, the project manager uses PERT to estimate migration duration across heterogeneous environments.\n3. **Capstone & Final Year Projects:** KTU final year projects (often involving full-stack DBMS applications) require Gantt charts in the project proposal to secure guide approval.\n4. **Industry Certifications:** PMI-PMP, PRINCE2, and CSM certifications all test these formulas — your KTU project gives you a head start.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 📘 SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Worked Example: PERT Computation for a DBMS Mini-Project\n\n**Problem Statement (KTU-Style):**\nYou are the project lead for a DBMS-based *Student Attendance Management System*. The project has been decomposed into **7 tasks (A through G)** with the following three-point estimates (in days):\n\n| Task | Description | Predecessor | Optimistic (O) | Most Likely (M) | Pessimistic (P) |\n|------|-------------|-------------|----------------|------------------|------------------|\n| A | Requirement Gathering & SRS | — | 4 | 6 | 14 |\n| B | ER Diagram Design | A | 2 | 3 | 4 |\n| C | Relational Schema Mapping | B | 3 | 4 | 11 |\n| D | Normalization (up to 3NF) | C | 2 | 3 | 4 |\n| E | SQL Implementation (DDL+DML) | D | 5 | 7 | 15 |\n| F | Front-End Integration | E | 4 | 6 | 8 |\n| G | Testing & Documentation | F | 3 | 5 | 13 |\n\n**Required:** Compute the expected time, variance, identify the critical path, project duration, and project standard deviation.\n\n### Step 1: Compute Expected Time (TE) for Each Task\n\nUsing the formula $T_E = \\frac{O + 4M + P}{6}$:\n\n**Task A:**\n$$T_E(A) = \\frac{4 + 4(6) + 14}{6} = \\frac{4 + 24 + 14}{6} = \\frac{42}{6} = 7 \\text{ days}$$\n\n**Task B:**\n$$T_E(B) = \\frac{2 + 4(3) + 4}{6} = \\frac{2 + 12 + 4}{6} = \\frac{18}{6} = 3 \\text{ days}$$\n\n**Task C:**\n$$T_E(C) = \\frac{3 + 4(4) + 11}{6} = \\frac{3 + 16 + 11}{6} = \\frac{30}{6} = 5 \\text{ days}$$\n\n**Task D:**\n$$T_E(D) = \\frac{2 + 4(3) + 4}{6} = \\frac{2 + 12 + 4}{6} = \\frac{18}{6} = 3 \\text{ days}$$\n\n**Task E:**\n$$T_E(E) = \\frac{5 + 4(7) + 15}{6} = \\frac{5 + 28 + 15}{6} = \\frac{48}{6} = 8 \\text{ days}$$\n\n**Task F:**\n$$T_E(F) = \\frac{4 + 4(6) + 8}{6} = \\frac{4 + 24 + 8}{6} = \\frac{36}{6} = 6 \\text{ days}$$\n\n**Task G:**\n$$T_E(G) = \\frac{3 + 4(5) + 13}{6} = \\frac{3 + 20 + 13}{6} = \\frac{36}{6} = 6 \\text{ days}$$\n\n### Step 2: Compute Variance (σ²) for Each Task\n\nUsing the formula $\\sigma^2 = \\left( \\frac{P - O}{6} \\right)^2$:\n\n**Task A:** $\\sigma^2(A) = \\left( \\frac{14 - 4}{6} \\right)^2 = \\left( \\frac{10}{6} \\right)^2 = (1.667)^2 = 2.78$ days²\n\n**Task B:** $\\sigma^2(B) = \\left( \\frac{4 - 2}{6} \\right)^2 = \\left( \\frac{2}{6} \\right)^2 = (0.333)^2 = 0.11$ days²\n\n**Task C:** $\\sigma^2(C) = \\left( \\frac{11 - 3}{6} \\right)^2 = \\left( \\frac{8}{6} \\right)^2 = (1.333)^2 = 1.78$ days²\n\n**Task D:** $\\sigma^2(D) = \\left( \\frac{4 - 2}{6} \\right)^2 = \\left( \\frac{2}{6} \\right)^2 = (0.333)^2 = 0.11$ days²\n\n**Task E:** $\\sigma^2(E) = \\left( \\frac{15 - 5}{6} \\right)^2 = \\left( \\frac{10}{6} \\right)^2 = (1.667)^2 = 2.78$ days²\n\n**Task F:** $\\sigma^2(F) = \\left( \\frac{8 - 4}{6} \\right)^2 = \\left( \\frac{4}{6} \\right)^2 = (0.667)^2 = 0.44$ days²\n\n**Task G:** $\\sigma^2(G) = \\left( \\frac{13 - 3}{6} \\right)^2 = \\left( \\frac{10}{6} \\right)^2 = (1.667)^2 = 2.78$ days²\n\n### Step 3: Construct the Network & Identify Paths\n\nSince the project has a **linear dependency chain** (A → B → C → D → E → F → G), there is **only ONE path** from start to finish. This is automatically the critical path.\n\n**Path:** A → B → C → D → E → F → G\n\n### Step 4: Compute Project Duration (T_E)\n\n$$T_E(\\text{Project}) = 7 + 3 + 5 + 3 + 8 + 6 + 6 = 38 \\text{ days}$$\n\n### Step 5: Compute Project Standard Deviation\n\nSince the critical path contains all tasks:\n\n$$\\sigma_{project} = \\sqrt{2.78 + 0.11 + 1.78 + 0.11 + 2.78 + 0.44 + 2.78}$$\n\n$$\\sigma_{project} = \\sqrt{10.78} = 3.28 \\text{ days}$$\n\n### Step 6: Probability of Meeting a 42-Day Deadline\n\nIf the project guide sets a **42-day hard deadline**, the Z-score is:\n\n$$Z = \\frac{T_S - T_E}{\\sigma_{project}} = \\frac{42 - 38}{3.28} = \\frac{4}{3.28} = 1.22$$\n\nFrom the standard normal table, **P(Z ≤ 1.22) ≈ 0.8888** or **88.88%**.\n\n> [!NOTE]\n> **Interpretation:** There is an **88.88% probability** that the project will be completed within 42 days. This is highly acceptable for academic submissions.\n\n## 3.2 Analytical Matrix: Forward & Backward Pass (CPM)\n\nFor a project with **parallel branches** (more realistic for larger DBMS projects), the CPM method is used. Consider the following task network:\n\n| Task | Predecessor | Duration (D) |\n|------|-------------|---------------|\n| A | — | 5 |\n| B | — | 7 |\n| C | A | 6 |\n| D | B | 4 |\n| E | C | 8 |\n| F | D | 5 |\n| G | E, F | 3 |\n\n### Forward Pass (Computing ES and EF)\n\n**Step 1:** For tasks with no predecessor (A, B), ES = 0.\n\n$$EF(A) = ES(A) + D(A) = 0 + 5 = 5$$\n$$EF(B) = ES(B) + D(B) = 0 + 7 = 7$$\n\n**Step 2:** For task C (predecessor = A):\n\n$$ES(C) = EF(A) = 5$$\n$$EF(C) = 5 + 6 = 11$$\n\n**Step 3:** For task D (predecessor = B):\n\n$$ES(D) = EF(B) = 7$$\n$$EF(D) = 7 + 4 = 11$$\n\n**Step 4:** For task E (predecessor = C):\n\n$$ES(E) = EF(C) = 11$$\n$$EF(E) = 11 + 8 = 19$$\n\n**Step 5:** For task F (predecessor = D):\n\n$$ES(F) = EF(D) = 11$$\n$$EF(F) = 11 + 5 = 16$$\n\n**Step 6:** For task G (predecessors = E, F). ES = max(EF(E), EF(F)):\n\n$$ES(G) = \\max(19, 16) = 19$$\n$$EF(G) = 19 + 3 = 22$$\n\n**Project Duration = EF(G) = 22 days**\n\n### Backward Pass (Computing LS and LF)\n\nThe final task G has LF(G) = EF(G) = 22.\n\n$$LS(G) = LF(G) - D(G) = 22 - 3 = 19$$\n\n**For F:** $LF(F) = LS(G) = 19$, so $LS(F) = 19 - 5 = 14$\n\n**For E:** $LF(E) = LS(G) = 19$, so $LS(E) = 19 - 8 = 11$\n\n**For D:** $LF(D) = LS(F) = 14$, so $LS(D) = 14 - 4 = 10$\n\n**For C:** $LF(C) = LS(E) = 11$, so $LS(C) = 11 - 6 = 5$\n\n**For A:** $LF(A) = LS(C) = 5$, so $LS(A) = 5 - 5 = 0$\n\n**For B:** $LF(B) = LS(D) = 10$, so $LS(B) = 10 - 7 = 3$\n\n### Slack Computation & Critical Path Identification\n\n| Task | ES | EF | LS | LF | Slack (LS−ES) | Critical? |\n|------|----|----|----|----|----------------|-----------|\n| A | 0 | 5 | 0 | 5 | 0 | ✅ Yes |\n| B | 0 | 7 | 3 | 10 | 3 | ❌ No |\n| C | 5 | 11 | 5 | 11 | 0 | ✅ Yes |\n| D | 7 | 11 | 10 | 14 | 3 | ❌ No |\n| E | 11 | 19 | 11 | 19 | 0 | ✅ Yes |\n| F | 11 | 16 | 14 | 19 | 3 | ❌ No |\n| G | 19 | 22 | 19 | 22 | 0 | ✅ Yes |\n\n**Critical Path: A → C → E → G** with total duration **22 days**.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 📘 SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Project Network Diagram (PERT/CPM Network)\n\n```mermaid\ngraph LR\n    startNode([START]) --> nodeA[A: SRS - 7d]\n    startNode([START]) --> nodeB[B: Hardware Setup - 3d]\n    nodeA --> nodeC[C: ER Diagram - 5d]\n    nodeB --> nodeD[D: Tool Installation - 3d]\n    nodeC --> nodeE[E: Schema Mapping - 3d]\n    nodeD --> nodeF[F: DBMS Install - 2d]\n    nodeE --> nodeG[G: Normalization - 8d]\n    nodeF --> nodeG[G: Normalization - 8d]\n    nodeG --> nodeH[H: SQL Coding - 6d]\n    nodeH --> nodeI[I: Front End - 6d]\n    nodeI --> nodeJ[J: Testing - 4d]\n    nodeJ --> endNode([END M6])\n\n    classDef critical fill:#ff6b6b,stroke:#c92a2a,color:#ffffff,stroke-width:3px\n    classDef noncritical fill:#4dabf7,stroke:#1864ab,color:#ffffff\n    classDef milestone fill:#ffd43b,stroke:#f08c00,color:#000000,stroke-width:3px\n\n    class nodeA,nodeC,nodeG,nodeH,nodeI,nodeJ critical\n    class nodeB,nodeD,nodeF noncritical\n    class endNode milestone\n```\n\n## 4.2 Gantt Chart for DBMS Mini-Project (Mermaid)\n\n```mermaid\ngantt\n    title DBMS Mini-Project Timeline (Student Attendance System)\n    dateFormat YYYY-MM-DD\n    axisFormat %b %d\n\n    section M1 Requirements\n    SRS Document Drafting        :req1, 2024-08-01, 5d\n    Guide Review and Approval    :milestone, m1, after req1, 0d\n\n    section M2 Conceptual Design\n    ER Diagram Design            :erd1, after m1, 4d\n    Identifying Entities         :erd2, after erd1, 2d\n    Relationship Mapping         :erd3, after erd2, 3d\n    ER Diagram Frozen            :milestone, m2, after erd3, 0d\n\n    section M3 Logical Design\n    Relational Schema Mapping    :rsm1, after m2, 3d\n    Constraint Definition        :rsm2, after rsm1, 2d\n\n    section M4 Normalization\n    Apply 1NF 2NF 3NF            :norm1, after rsm2, 4d\n    Schema Finalized             :milestone, m4, after norm1, 0d\n\n    section M5 SQL Implementation\n    DDL Script Creation          :sql1, after m4, 3d\n    DML and Sample Data          :sql2, after sql1, 3d\n    Complex Queries and Joins    :sql3, after sql2, 4d\n    Database Demo                :milestone, m5, after sql3, 0d\n\n    section M6 Front End and Testing\n    Front End UI Design          :fe1, after m5, 4d\n    DB Connectivity              :fe2, after fe1, 3d\n    Unit and Integration Testing :test1, after fe2, 3d\n    Final Submission             :milestone, m6, after test1, 0d\n```\n\n## 4.3 Milestone Flowchart (Block-Level Functional Architecture)\n\n```mermaid\nflowchart TD\n    A[Project Initiation] --> B[Phase 1: SRS]\n    B -->|M1 Approved| C[Phase 2: ER Design]\n    C -->|M2 Frozen| D[Phase 3: Schema Mapping]\n    D -->|M3 Finalized| E[Phase 4: Normalization]\n    E -->|M4 in 3NF| F[Phase 5: SQL Implementation]\n    F -->|M5 Demo Ready| G[Phase 6: Integration and Testing]\n    G -->|M6 Submitted| H([Project Complete])\n\n    M1[/M1: SRS Approved/] -.-> B\n    M2[/M2: ER Frozen/] -.-> C\n    M3[/M3: Schema Done/] -.-> D\n    M4[/M4: Normalized/] -.-> E\n    M5[/M5: DB Demo/] -.-> F\n    M6[/M6: Final Submit/] -.-> G\n\n    classDef phase fill:#e7f5ff,stroke:#1971c2,stroke-width:2px\n    classDef mile fill:#fff3bf,stroke:#f08c00,stroke-width:3px\n    classDef finalNode fill:#b2f2bb,stroke:#2f9e44,stroke-width:3px\n\n    class A,B,C,D,E,F,G phase\n    class M1,M2,M3,M4,M5,M6 mile\n    class H finalNode\n```\n\n## 4.4 Work Breakdown Structure (WBS) Hierarchy\n\n```mermaid\ngraph TD\n    WBS0[DBMS Mini-Project] --> WBS1[1. Planning]\n    WBS0 --> WBS2[2. Design]\n    WBS0 --> WBS3[3. Development]\n    WBS0 --> WBS4[4. Testing]\n    WBS0 --> WBS5[5. Deployment]\n\n    WBS1 --> WBS1a[1.1 Timeline]\n    WBS1 --> WBS1b[1.2 SRS]\n    WBS1 --> WBS1c[1.3 Resource Allocation]\n\n    WBS2 --> WBS2a[2.1 ER Diagram]\n    WBS2 --> WBS2b[2.2 Relational Schema]\n    WBS2 --> WBS2c[2.3 Normalization]\n\n    WBS3 --> WBS3a[3.1 DDL Scripts]\n    WBS3 --> WBS3b[3.2 DML and Queries]\n    WBS3 --> WBS3c[3.3 Front-End Code]\n\n    WBS4 --> WBS4a[4.1 Unit Tests]\n    WBS4 --> WBS4b[4.2 Integration Tests]\n    WBS4 --> WBS4c[4.3 User Acceptance]\n\n    WBS5 --> WBS5a[5.1 Documentation]\n    WBS5 --> WBS5b[5.2 Final Submission]\n    WBS5 --> WBS5c[5.3 Demo Presentation]\n\n    classDef root fill:#ffd8a8,stroke:#e8590c,stroke-width:3px\n    classDef lvl1 fill:#d0bfff,stroke:#7048e8,color:#ffffff\n    classDef lvl2 fill:#bac8ff,stroke:#4263eb,color:#ffffff\n    classDef lvl3 fill:#a5d8ff,stroke:#1971c2\n\n    class WBS0 root\n    class WBS1,WBS2,WBS3,WBS4,WBS5 lvl1\n    class WBS1a,WBS1b,WBS1c,WBS2a,WBS2b,WBS2c lvl2\n    class WBS3a,WBS3b,WBS3c,WBS4a,WBS4b,WBS4c,WBS5a,WBS5b,WBS5c lvl3\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 📘 SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A: Short-Answer Questions (3 Marks Each)\n\n### **Question 1** `[KTU University Exam – Model Question, 2024 Scheme]`\n\n**Q: Define a \"Milestone\" in the context of a DBMS course project. List any three major milestones you would define for a typical DBMS mini-project.**\n\n**Model Answer (3 marks):**\n\nA **milestone** is a significant point or event in a project schedule that has **zero duration** and marks the completion of a major deliverable or phase. Milestones serve as checkpoints to track progress and validate deliverables before proceeding to the next phase.\n\nFor a typical DBMS mini-project (e.g., *Hospital Management System*), three major milestones are:\n\n1. **M1 – SRS Document Approval:** Marks the end of the requirement gathering phase. The Software Requirements Specification is reviewed and approved by the project guide.\n2. **M2 – ER Diagram Freeze:** Marks the end of conceptual design. The ER model is validated, and no further structural changes are permitted without formal change requests.\n3. **M3 – Normalized Database in 3NF/BCNF:** Marks the completion of logical design and refinement. The schema is now ready for SQL implementation.\n\n> [!TIP]\n> **Valuation Tip:** Examiners award **1 mark** for the definition and **2 marks** for listing the milestones with brief context. Always mention *zero duration* — it is a keyword.\n\n---\n\n### **Question 2** `[KTU University Exam – Model Question, 2024 Scheme]`\n\n**Q: What is a Gantt Chart? How is it useful in tracking the progress of a DBMS project?**\n\n**Model Answer (3 marks):**\n\nA **Gantt Chart** is a horizontal bar chart developed by Henry Gantt (1917) that visually represents a project schedule. Each task is represented as a **bar** whose length corresponds to its **duration**, plotted against a **time axis**. Tasks can be sequenced, overlapped, or shown as parallel depending on their dependencies.\n\n**Utility in DBMS project tracking:**\n\n1. **Visual Timeline:** The student and guide can instantly see which phase is in progress (e.g., \"Currently in Phase 3: Schema Mapping\") and what is upcoming.\n2. **Dependency Tracking:** Predecessor-successor relationships (e.g., Normalization depends on Schema Mapping) are clearly displayed.\n3. **Progress Monitoring:** Completed tasks can be shaded, and the **% completion** can be tracked weekly during review meetings with the project coordinator.\n\n> [!WARNING]\n> **Common Mistake:** Students often confuse Gantt Charts with PERT/CPM networks. Remember: **Gantt = Time-based bar chart**, **PERT = Node-based network diagram with probabilistic estimates**.\n\n---\n\n## 5.2 Part B: 14-Mark Questions (Module Internal Choice)\n\n### **Question A (14 Marks)** `[KTU University Exam – Dec 2023, Adapted]`\n\n**A DBMS mini-project for a \"Library Management System\" has been decomposed into 8 tasks (A–H) with the following three-point time estimates (in days):**\n\n| Task | Description | Predecessor | Optimistic (O) | Most Likely (M) | Pessimistic (P) |\n|------|-------------|-------------|----------------|------------------|------------------|\n| A | Requirement Analysis | — | 3 | 5 | 9 |\n| B | ER Diagram Design | A | 2 | 4 | 6 |\n| C | Schema Conversion | B | 2 | 3 | 4 |\n| D | DBMS Software Setup | A | 1 | 2 | 3 |\n| E | Normalization | C | 3 | 5 | 7 |\n| F | SQL Implementation | E, D | 4 | 6 | 8 |\n| G | Front-End Development | F | 5 | 7 | 9 |\n| H | Testing and Documentation | G | 3 | 4 | 11 |\n\n**(a)** Compute the expected time $T_E$ and variance $\\sigma^2$ for each task. **(7 marks)**\n\n**(b)** Identify the critical path, compute the expected project duration, and the probability of completing the project in **30 days or less**. **(7 marks)**\n\n---\n\n#### **Model Solution for Part (a) — 7 Marks**\n\n**Step 1: Compute Expected Time using $T_E = \\frac{O + 4M + P}{6}$**\n\n$$T_E(A) = \\frac{3 + 4(5) + 9}{6} = \\frac{3 + 20 + 9}{6} = \\frac{32}{6} = 5.33 \\text{ days}$$\n\n$$T_E(B) = \\frac{2 + 4(4) + 6}{6} = \\frac{2 + 16 + 6}{6} = \\frac{24}{6} = 4.00 \\text{ days}$$\n\n$$T_E(C) = \\frac{2 + 4(3) + 4}{6} = \\frac{2 + 12 + 4}{6} = \\frac{18}{6} = 3.00 \\text{ days}$$\n\n$$T_E(D) = \\frac{1 + 4(2) + 3}{6} = \\frac{1 + 8 + 3}{6} = \\frac{12}{6} = 2.00 \\text{ days}$$\n\n$$T_E(E) = \\frac{3 + 4(5) + 7}{6} = \\frac{3 + 20 + 7}{6} = \\frac{30}{6} = 5.00 \\text{ days}$$\n\n$$T_E(F) = \\frac{4 + 4(6) + 8}{6} = \\frac{4 + 24 + 8}{6} = \\frac{36}{6} = 6.00 \\text{ days}$$\n\n$$T_E(G) = \\frac{5 + 4(7) + 9}{6} = \\frac{5 + 28 + 9}{6} = \\frac{42}{6} = 7.00 \\text{ days}$$\n\n$$T_E(H) = \\frac{3 + 4(4) + 11}{6} = \\frac{3 + 16 + 11}{6} = \\frac{30}{6} = 5.00 \\text{ days}$$\n\n**Step 2: Compute Variance using $\\sigma^2 = \\left( \\frac{P - O}{6} \\right)^2$**\n\n$$\\sigma^2(A) = \\left( \\frac{9 - 3}{6} \\right)^2 = (1.00)^2 = 1.00 \\text{ days}^2$$\n\n$$\\sigma^2(B) = \\left( \\frac{6 - 2}{6} \\right)^2 = (0.667)^2 = 0.44 \\text{ days}^2$$\n\n$$\\sigma^2(C) = \\left( \\frac{4 - 2}{6} \\right)^2 = (0.333)^2 = 0.11 \\text{ days}^2$$\n\n$$\\sigma^2(D) = \\left( \\frac{3 - 1}{6} \\right)^2 = (0.333)^2 = 0.11 \\text{ days}^2$$\n\n$$\\sigma^2(E) = \\left( \\frac{7 - 3}{6} \\right)^2 = (0.667)^2 = 0.44 \\text{ days}^2$$\n\n$$\\sigma^2(F) = \\left( \\frac{8 - 4}{6} \\right)^2 = (0.667)^2 = 0.44 \\text{ days}^2$$\n\n$$\\sigma^2(G) = \\left( \\frac{9 - 5}{6} \\right)^2 = (0.667)^2 = 0.44 \\text{ days}^2$$\n\n$$\\sigma^2(H) = \\left( \\frac{11 - 3}{6} \\right)^2 = (1.333)^2 = 1.78 \\text{ days}^2$$\n\n| Task | O | M | P | TE (days) | σ² (days²) |\n|------|---|---|---|-----------|-------------|\n| A | 3 | 5 | 9 | 5.33 | 1.00 |\n| B | 2 | 4 | 6 | 4.00 | 0.44 |\n| C | 2 | 3 | 4 | 3.00 | 0.11 |\n| D | 1 | 2 | 3 | 2.00 | 0.11 |\n| E | 3 | 5 | 7 | 5.00 | 0.44 |\n| F | 4 | 6 | 8 | 6.00 | 0.44 |\n| G | 5 | 7 | 9 | 7.00 | 0.44 |\n| H | 3 | 4 | 11 | 5.00 | 1.78 |\n\n---\n\n#### **Model Solution for Part (b) — 7 Marks**\n\n**Step 1: Enumerate all possible paths from START to END**\n\nFrom the precedence diagram:\n- **Path 1:** A → B → C → E → F → G → H\n- **Path 2:** A → D → F → G → H\n\n**Step 2: Compute total duration for each path**\n\n$$\\text{Path 1: } 5.33 + 4.00 + 3.00 + 5.00 + 6.00 + 7.00 + 5.00 = 35.33 \\text{ days}$$\n\n$$\\text{Path 2: } 5.33 + 2.00 + 6.00 + 7.00 + 5.00 = 25.33 \\text{ days}$$\n\n**Step 3: Identify the Critical Path**\n\nThe critical path is the path with the **maximum duration**:\n$$\\boxed{\\text{Critical Path: } A \\rightarrow B \\rightarrow C \\rightarrow E \\rightarrow F \\rightarrow G \\rightarrow H \\text{ (35.33 days)}}$$\n\n**Step 4: Compute Project Standard Deviation**\n\nSum the variances of the critical path tasks:\n$$\\sigma_{project}^2 = 1.00 + 0.44 + 0.11 + 0.44 + 0.44 + 0.44 + 1.78 = 4.65 \\text{ days}^2$$\n\n$$\\sigma_{project} = \\sqrt{4.65} = 2.156 \\text{ days}$$\n\n**Step 5: Compute Probability of Completion in 30 Days**\n\n$$Z = \\frac{T_S - T_E}{\\sigma_{project}} = \\frac{30 - 35.33}{2.156} = \\frac{-5.33}{2.156} = -2.47$$\n\nFrom the standard normal distribution table:\n$$P(Z \\leq -2.47) \\approx 0.0068 \\text{ or } 0.68\\%$$\n\n**Interpretation:** There is only a **0.68% probability** of completing the project in 30 days. The student should either request a deadline extension or **add resources** to the critical path tasks (especially A and H, which have the highest variance).\n\n---\n\n### **Question B (14 Marks — Alternative Choice)** `[KTU University Exam – June 2024, Adapted]`\n\n**(a)** Define a **Work Breakdown Structure (WBS)**. Draw a hierarchical WBS for a DBMS mini-project on *\"Online Exam Management System\"* showing at least **three levels of decomposition** (7 marks).\n\n**(b)** Explain the difference between **PERT** and **CPM** scheduling methods. State which method is more suitable for a DBMS course project and justify your answer with at least three technical reasons (7 marks).\n\n---\n\n#### **Model Solution for Part (a) — 7 Marks**\n\n**Definition (2 marks):** A **Work Breakdown Structure (WBS)** is a hierarchical decomposition of the total scope of work to be carried out by the project team to accomplish the project objectives and produce the required deliverables. It organizes and defines the total scope of the project, breaking it into smaller, more manageable components called **work packages**.\n\n**WBS Diagram for \"Online Exam Management System\" (5 marks):**\n\n```\nLEVEL 0: Online Exam Management System (DBMS Project)\n│\n├── LEVEL 1.0: Project Management\n│   ├── 1.1 Project Proposal\n│   ├── 1.2 Timeline and Milestones\n│   ├── 1.3 Progress Reports\n│   └── 1.4 Risk Register\n│\n├── LEVEL 2.0: Requirements and Design\n│   ├── 2.1 SRS Document\n│   ├── 2.2 Use Case Diagrams\n│   ├── 2.3 ER Diagram\n│   │   ├── 2.3.1 Entities (Student, Exam, Question, Subject, Result)\n│   │   ├── 2.3.2 Attributes and Keys\n│   │   └── 2.3.3 Relationships (Attempts, Belongs_to, Evaluates)\n│   └── 2.4 Relational Schema\n│       ├── 2.4.1 Table Definitions\n│       └── 2.4.2 Constraints (PK, FK, NOT NULL, UNIQUE)\n│\n├── LEVEL 3.0: Database Implementation\n│   ├── 3.1 DDL Scripts (CREATE TABLE)\n│   ├── 3.2 DML Scripts (INSERT sample data)\n│   ├── 3.3 Complex Queries\n│   │   ├── 3.3.1 JOINs (INNER, LEFT, RIGHT)\n│   │   ├── 3.3.2 Subqueries\n│   │   ├── 3.3.3 Aggregate Functions (AVG, COUNT, MAX)\n│   │   └── 3.3.4 Views and Indexes\n│   └── 3.4 Stored Procedures (Triggers, Cursors)\n│\n├── LEVEL 4.0: Front-End Development\n│   ├── 4.1 UI Mockups\n│   ├── 4.2 Login and Authentication Module\n│   ├── 4.3 Exam Interface Module\n│   └── 4.4 Result Display Module\n│\n└── LEVEL 5.0: Testing and Deployment\n    ├── 5.1 Unit Testing\n    ├── 5.2 Integration Testing\n    ├── 5.3 User Acceptance Testing (UAT)\n    └── 5.4 Final Documentation\n```\n\n---\n\n#### **Model Solution for Part (b) — 7 Marks**\n\n| Aspect | PERT (Program Evaluation and Review Technique) | CPM (Critical Path Method) |\n|--------|-----------------------------------------------|-----------------------------|\n| **Origin** | Developed by U.S. Navy (1958) for Polaris Missile Project | Developed by DuPont & Remington Rand (1956) |\n| **Time Estimation** | Uses **three-point estimates** (Optimistic, Most Likely, Pessimistic) | Uses **single deterministic estimate** |\n| **Uncertainty** | Probabilistic; assumes Beta distribution | Deterministic; assumes known durations |\n| **Focus** | **Time-oriented** — focuses on meeting deadlines with uncertainty | **Time-Cost trade-off** — focuses on optimizing both |\n| **Best For** | R&D, novel projects with high uncertainty | Repetitive, well-understood projects |\n| **Network** | Event-oriented (nodes = events) | Activity-oriented (nodes = activities, arrows = dependencies) |\n\n**Suitability for DBMS Course Project (3 marks for justification):**\n\nThe **PERT method is more suitable** for a DBMS course project for the following three technical reasons:\n\n1. **Uncertainty in Implementation:** Tasks like \"User Testing\" and \"Front-End Integration\" have highly variable durations because they depend on student learning speed, debugging time, and unforeseen bugs. PERT's three-point estimation captures this uncertainty more realistically than CPM's single-point estimate.\n\n2. **Probabilistic Completion Analysis:** PERT allows the student to compute the **probability of meeting a submission deadline** (e.g., \"What is the probability I finish in 45 days?\"). This is a powerful tool for academic planning and for reassuring the project guide that the deadline is achievable with high confidence (e.g., 90%).\n\n3. **Heterogeneous Task Nature:** A DBMS project includes both **deterministic tasks** (e.g., writing DDL scripts — well-defined duration) and **stochastic tasks** (e.g., debugging complex JOIN queries — highly variable). PERT handles both naturally using the O, M, P estimates, whereas CPM would force a single, possibly inaccurate, estimate on the stochastic tasks.\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall Callout:**\n> 1. **Do NOT** confuse the PERT variance formula with standard deviation. Examiners deduct 1 mark if you write $\\sigma = \\frac{P-O}{6}$ when asked for variance.\n> 2. **Do NOT** forget to identify the **critical path explicitly**. Many students compute durations of all paths but never state *which one is critical*. This costs **1 mark**.\n> 3. **Do NOT** use negative slack values in the CPM table. Slack must always be **non-negative**. A negative slack indicates an infeasible schedule.\n> 4. **Do NOT** forget the unit. The Z-score is dimensionless, but $T_E$ is in days, and $\\sigma^2$ is in days². Mentioning units shows professional rigor.\n> 5. **Do NOT** skip the standard normal table lookup. Examiners expect the numerical value (e.g., $P(Z \\le -2.47) = 0.0068$) and the percentage interpretation.\n\n---\n\n## 5.3 📌 Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- ✅ A **Project Timeline** is a chronological plan that defines **what** tasks will be done, **when** they will be done, **by whom**, and **what marks their completion**.\n- ✅ A **Milestone** has **zero duration** and marks the **end of a major deliverable**. Examples: SRS Approval, ER Freeze, 3NF Schema, SQL Demo, Final Submission.\n- ✅ A **Gantt Chart** is a **time-based bar chart** ideal for visual tracking by both students and guides.\n- ✅ A **PERT Network** is a **node-based diagram** ideal for computing expected project duration under uncertainty.\n- ✅ The **PERT Expected Time formula** is $T_E = \\frac{O + 4M + P}{6}$, derived from the Beta distribution assumption.\n- ✅ The **PERT Variance formula** is $\\sigma^2 = \\left( \\frac{P - O}{6} \\right)^2$.\n- ✅ The **Critical Path** is the **longest path** in the network and determines the **minimum project duration**.\n- ✅ **CPM** uses the **Forward Pass** (ES, EF) and **Backward Pass** (LS, LF) to compute **slack** for each task.\n- ✅ **Slack = LS − ES**. A task with **zero slack** is on the **critical path**.\n- ✅ The **probability of project completion** by a given deadline $T_S$ is found using the **Z-score**: $Z = \\frac{T_S - T_E}{\\sigma_{project}}$, then looking up the **standard normal distribution table**.\n- ✅ For a DBMS course project, define **at least 6 milestones** (M1 to M6) corresponding to each SDLC phase.\n- ✅ Always include a **Gantt Chart** in your project proposal — it is **mandatory** in the KTU 2024 Scheme evaluation rubric.\n- ✅ Use **three-point estimates** (O, M, P) for tasks with uncertainty, and **single-point estimates** for well-defined tasks.\n- ✅ Document your timeline in **Microsoft Project, ClickUp, Notion, or even a well-formatted Mermaid Gantt chart** (as demonstrated in Section 4.2).\n- ✅ **Review your timeline bi-weekly** with your project guide. Any slippage on the critical path requires immediate corrective action (adding manpower, parallelizing tasks, or scope reduction).\n- ✅ **Always state units** (days, weeks) explicitly in your timeline. Ambiguity in unit is a common reason for mark deduction.\n\n> [!TIP]\n> **Final Exam Cram Sheet:** Memorize the four key formulas — $T_E = \\frac{O + 4M + P}{6}$, $\\sigma^2 = \\left( \\frac{P - O}{6} \\right)^2$, $Slack = LS - ES$, and $Z = \\frac{T_S - T_E}{\\sigma}$. These four formulas cover **~80%** of all KTU 2024 scheme exam questions on this topic. Practice the Z-score → Probability lookup from the standard normal table at least 3 times before the exam.\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "constraintsentityintegrityreferentialintegrityanddomainconstraints": {
      "title": "Constraints: Entity Integrity, Referential Integrity, and Domain Constraints",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 3\n> - **Topic:** Constraints: Entity Integrity, Referential Integrity, and Domain Constraints\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 What Are Integrity Constraints?\n\n**Integrity Constraints** are a set of declarative rules enforced on the data columns of a relational database table. They are the silent guardians of the database — they ensure that the data stored is **accurate, consistent, valid, and reliable** across all instances and over time. Without integrity constraints, a database degenerates from a trusted source of information into a collection of unverified, corrupted, and unreliable records.\n\nIn the context of the **Relational Model (Codd's Model)**, integrity constraints are mathematically derived restrictions on the relations (tables) that prevent the insertion of data that would violate the semantic rules of the application domain. The SQL standard (and therefore KTU examinations) categorizes these into several key classes, but for Module 3, we focus on the three foundational pillars:\n\n> [!IMPORTANT]\n> **The Three Primary Integrity Constraints (KTU Module 3 Syllabus)**\n> 1. **Entity Integrity Constraint** — Governs the uniqueness and non-null status of the Primary Key.\n> 2. **Referential Integrity Constraint** — Governs the relationship consistency between Foreign Key and referenced Primary Key.\n> 3. **Domain Constraint** — Governs the permissible values within a column (data type, range, format).\n\n---\n\n## 1.2 Intuitive Analogy: The College Admission Office\n\nImagine a college admission office that maintains two registers:\n- **Register A (Students)**: Contains the *Roll Number*, Name, DOB, and Course ID of every student.\n- **Register B (Courses)**: Contains the *Course ID*, Course Name, and Credits of every course offered.\n\nNow, the admission officer must enforce some basic *rules of business*:\n1. A student **must** have a roll number, and **no two students** can have the same roll number. *(This is Entity Integrity.)*\n2. If a student is registered for *Course ID \"CS304\"*, that course **must** exist in Register B. You cannot enroll a student in a \"ghost\" course that nobody offers. *(This is Referential Integrity.)*\n3. The *DOB* column should only contain valid dates (not \"Hello World\" or \"99/99/9999\"), the *Credits* column should be an integer between 1 and 5, and the *Gender* column should only contain \"M\" or \"F\". *(This is Domain Integrity.)*\n\nIf any of these rules is violated, the admission is **rejected outright**. The database system behaves in exactly the same way — it is a strict, rule-enforcing machine.\n\n> [!NOTE]\n> **Why are these called \"Constraints\"?** Because they *constrain* (restrict) the set of valid tuples (rows) that a relation is allowed to contain. In formal relational algebra, they act as a filter: $\\sigma_{\\text{constraint-true}}(R)$.\n\n---\n\n## 1.3 Formal Definitions\n\n### 1.3.1 Entity Integrity Constraint\n**Definition:** The **Entity Integrity Constraint** states that the **Primary Key** attribute of any relation (table) **must always have a non-NULL value** for every tuple (row), and the value must be **unique**.\n\n**Formal Statement:** For any relation $R$ with primary key $PK$, for every tuple $t \\in R$:\n$$t[PK] \\neq \\text{NULL} \\quad \\land \\quad \\forall t_i, t_j \\in R, \\; t_i \\neq t_j \\Rightarrow t_i[PK] \\neq t_j[PK]$$\n\n**Intuition:** Every real-world entity (a student, a product, a transaction) must be **distinguishable** from every other entity. If the primary key were NULL, the entity would be \"nameless\" and impossible to identify. If two rows had the same PK, they would represent the same entity — a contradiction.\n\n### 1.3.2 Referential Integrity Constraint\n**Definition:** The **Referential Integrity Constraint** states that for every **Foreign Key** value in a relation (the *referencing* or *child* relation), there must exist a matching **Primary Key** value in the referenced (or *parent*) relation — *unless* the foreign key is NULL.\n\n**Formal Statement:** For relations $R_1$ (referencing) and $R_2$ (referenced), where $FK$ in $R_1$ references $PK$ in $R_2$, for every tuple $t_1 \\in R_1$:\n$$t_1[FK] = \\text{NULL} \\quad \\lor \\quad \\exists t_2 \\in R_2 \\text{ such that } t_1[FK] = t_2[PK]$$\n\n**Intuition:** A \"reference\" must point to something real. You cannot write a cheque (cheque number 101) that references bank account number 99999 if no such account exists. The database forbids **dangling references**.\n\n### 1.3.3 Domain Constraint\n**Definition:** The **Domain Constraint** states that every value of an attribute (column) in a relation must be an **atomic value drawn from the specified domain** — including the data type, length, range, and format.\n\n**Formal Statement:** For a relation $R$ with attribute $A$ defined on domain $D$:\n$$\\forall t \\in R, \\; t[A] \\in D$$\n\n**Intuition:** An attribute is like a labeled box. The label (domain) tells you *what kind of object* is allowed inside. You cannot put a *string* into a box labeled \"INTEGER\". You cannot put the *age* \"500\" into a box labeled \"AGE (0–150)\".\n\n> [!IMPORTANT]\n> **KTU Board Favourite:** Examiners often ask students to *classify* a given constraint (e.g., NOT NULL, UNIQUE, CHECK, FOREIGN KEY) into one of the three categories. Memorize the mapping:\n> - **Entity Integrity** → `PRIMARY KEY` (includes NOT NULL + UNIQUE)\n> - **Referential Integrity** → `FOREIGN KEY ... REFERENCES`\n> - **Domain Integrity** → `CHECK`, `NOT NULL` (on non-key), data type, `DEFAULT`\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Entity Integrity — The \"Identity Guarantee\"\n\nThe Entity Integrity Constraint is a **tuple-level** constraint that applies specifically to the attribute(s) designated as the **Primary Key** of a relation. It is a composite of two sub-constraints:\n\n1. **Uniqueness Constraint:** $\\forall t_i, t_j \\in R, \\; i \\neq j \\Rightarrow t_i[PK] \\neq t_j[PK]$\n2. **Non-Null Constraint:** $\\forall t \\in R, \\; t[PK] \\neq \\text{NULL}$\n\nBoth conditions must be satisfied **simultaneously**. A common exam pitfall is stating that the primary key must be unique *or* non-null — it must be **both** (logical AND).\n\n### Why Primary Key Cannot Be NULL\nThe Primary Key serves as the **logical address** of a tuple within the relation. If it were NULL, the tuple would be:\n- **Invisible** to JOIN operations referencing it.\n- **Unidentifiable** in UPDATE and DELETE operations (`WHERE PK = NULL` matches nothing in SQL's three-valued logic).\n- **In violation** of the very definition of a \"key\" in set theory (a key is a set of attributes that *uniquely determines* the tuple; NULL cannot \"determine\" anything).\n\n### SQL Implementation\nThe Entity Integrity is enforced by declaring the column as `PRIMARY KEY` at table creation time:\n\n```sql\nCREATE TABLE Student (\n    Roll_No   INT  PRIMARY KEY,\n    Name      VARCHAR(50) NOT NULL,\n    DOB       DATE\n);\n```\n\n> [!NOTE]\n> In SQL, declaring a column `PRIMARY KEY` **automatically** enforces both NOT NULL and UNIQUE. This is a key KTU fact — you do not need to write `PRIMARY KEY NOT NULL UNIQUE` separately.\n\n### Composite Primary Keys\nWhen a primary key consists of multiple attributes, the Entity Integrity Constraint applies to the **entire combination**, not to individual attributes. Individual attributes within the composite key *may* be NULL (e.g., in a weak entity relationship), but their combination cannot be NULL or duplicate.\n\n---\n\n## 2.2 Referential Integrity — The \"Consistency of Links\"\n\nReferential Integrity governs the relationship between two tables connected by a **Foreign Key**. It guarantees that the database never contains a **dangling reference** — a foreign key value that points to a non-existent primary key.\n\n### Components of the Constraint\n\n| Symbol | Meaning |\n|---|---|\n| $R_1$ | Referencing (Child) relation |\n| $R_2$ | Referenced (Parent) relation |\n| $FK$ | Foreign Key attribute set in $R_1$ |\n| $PK$ | Primary Key attribute set in $R_2$ |\n| $t_1 \\in R_1$ | An arbitrary tuple in the child table |\n| $t_2 \\in R_2$ | A tuple in the parent table |\n\n### The Three Legitimate States of a Foreign Key\n1. **NULL** — The reference is *optional*. (e.g., A project may not have a manager yet.)\n2. **Matches a PK in the parent table** — The reference is *valid*.\n3. **(Implicitly illegal)** Any other value is rejected by the DBMS.\n\n### SQL Implementation\n\n```sql\nCREATE TABLE Enrollment (\n    Enroll_ID   INT  PRIMARY KEY,\n    Roll_No     INT,\n    Course_ID   VARCHAR(10),\n    FOREIGN KEY (Roll_No)   REFERENCES Student(Roll_No),\n    FOREIGN KEY (Course_ID) REFERENCES Course(Course_ID)\n);\n```\n\n### Referential Triggered Actions (Crucial for KTU 14-Mark Questions)\nWhen the referenced primary key is **updated or deleted**, the DBMS must decide what to do with the dependent foreign keys. The SQL standard defines four actions:\n\n| Action | Behavior on Parent Update/Delete |\n|---|---|\n| `CASCADE` | Automatically update/delete the child rows |\n| `RESTRICT` (default) | Reject the operation if children exist |\n| `SET NULL` | Set the foreign key in children to NULL |\n| `SET DEFAULT` | Set the foreign key in children to their DEFAULT value |\n| `NO ACTION` | Similar to RESTRICT, checked at end of statement |\n\n> [!IMPORTANT]\n> **KTU Examiner Pattern:** When the question asks \"What happens when a referenced row is deleted?\" — list all four referential actions and explain that the chosen one is declared **at the time of foreign key definition** in the CREATE TABLE statement.\n\n### Violation Scenarios (Frequently Asked)\n\n1. **Insert into child with non-existent parent FK** → Rejected.\n2. **Delete from parent while children reference it** → Rejected (or cascaded).\n3. **Update parent PK while children reference it** → Rejected (or cascaded/set null).\n\n---\n\n## 2.3 Domain Constraint — The \"Type & Range Contract\"\n\nDomain Constraints are the **most granular** integrity constraints. They apply to the **attribute level** (single column) and restrict the set of permissible values in three ways:\n\n1. **Data Type:** INT, VARCHAR, DATE, BOOLEAN, DECIMAL, etc.\n2. **Format / Pattern:** Using `CHECK` with `LIKE` or `REGEXP`.\n3. **Range / Enumeration:** Using `CHECK` with comparison operators or `IN`.\n\n### The `CHECK` Clause — The Workhorse of Domain Integrity\n\n```sql\nCREATE TABLE Employee (\n    Emp_ID     INT  PRIMARY KEY,\n    Salary     DECIMAL(10,2)  CHECK (Salary > 0),\n    Age        INT            CHECK (Age BETWEEN 18 AND 65),\n    Gender     CHAR(1)        CHECK (Gender IN ('M', 'F', 'O')),\n    Email      VARCHAR(100)   CHECK (Email LIKE '%_@_%._%'),\n    Dept_Code  INT             DEFAULT 1\n);\n```\n\n### Components of a Domain\n\n| Component | Description | SQL Keyword |\n|---|---|---|\n| Data Type | The kind of value (number, string, date) | `INT`, `VARCHAR`, `DATE` |\n| Length/Precision | Maximum size | `VARCHAR(50)`, `DECIMAL(10,2)` |\n| Default Value | Value when none is provided | `DEFAULT` |\n| Allow NULL? | Whether absence is permitted | `NULL` / `NOT NULL` |\n| Check Expression | Custom business rule | `CHECK (...)` |\n| Enumeration | Allowed discrete values | `CHECK (col IN (...))` |\n\n> [!NOTE]\n> **Not NULL on a non-key column** is technically a *Domain Constraint* (it restricts the domain to \"all values except NULL\"). On a Primary Key, however, NOT NULL is part of *Entity Integrity*. Examiners will deduct marks if you confuse these.\n\n---\n\n## 2.4 KTU High-Yield Formula Sheet / Quick Reference Table\n\n| Constraint Type | Enforced By | Acts On | Cardinality | SQL Syntax Example |\n|---|---|---|---|---|\n| **Entity Integrity** | `PRIMARY KEY` declaration | Primary Key attribute(s) | Tuple level | `Roll_No INT PRIMARY KEY` |\n| **Referential Integrity** | `FOREIGN KEY ... REFERENCES` | Foreign Key ↔ Parent PK | Inter-relation | `FOREIGN KEY (Dept) REFERENCES Dept(ID)` |\n| **Domain Integrity** | Data type, `CHECK`, `NOT NULL`, `DEFAULT`, `UNIQUE` | A single attribute | Attribute level | `CHECK (Age >= 18)` |\n| **Key Uniqueness** (subset of Entity) | `UNIQUE` | Candidate key (non-PK) | Tuple level | `Email VARCHAR(100) UNIQUE` |\n| **Nullability** (subset of Domain) | `NULL` / `NOT NULL` | Any column | Attribute level | `Name VARCHAR(50) NOT NULL` |\n\n### Cardinality Rule for Referential Integrity\n\nFor a child table with $n$ tuples and a parent table with $m$ tuples, the valid referential integrity relationship is:\n\n$$|\\pi_{FK}(R_1)| \\leq |R_2|$$\n\nThat is, the number of distinct foreign key values in the child cannot exceed the number of parent tuples (assuming FKs are not NULL). NULLs in the FK are allowed but excluded from the projection above.\n\n---\n\n## 2.5 Real-World Engineering Utility\n\nThese three constraints underpin virtually every enterprise system:\n\n- **Banking Systems:** Entity Integrity ensures no two transactions share the same Transaction ID. Referential Integrity ensures every account number in the \"Loans\" table exists in the \"Accounts\" table. Domain Integrity ensures loan amounts are positive and within the sanctioned range.\n\n- **Hospital Management Systems:** Patient ID is unique (Entity), every doctor referenced in \"Appointments\" exists in the \"Doctors\" table (Referential), and blood group is restricted to {A+, A-, B+, B-, O+, O-, AB+, AB-} (Domain).\n\n- **E-Commerce Platforms:** Order ID is unique (Entity), every Customer_ID in orders exists in the customer table (Referential), and product price must be > 0 and stock must be a non-negative integer (Domain).\n\n- **IoT and Sensor Networks:** Sensor ID uniqueness, valid device pairing references, and physical-range constraints (e.g., temperature between -40°C and 125°C for industrial sensors).\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations, Worked Examples & Analytical Matrix\n\n## 3.1 Worked Example: Library Management System\n\nConsider the following two-table schema for a KTU-style question:\n\n```sql\nCREATE TABLE Book (\n    Book_ID    INT  PRIMARY KEY,\n    Title      VARCHAR(100) NOT NULL,\n    Price      DECIMAL(8,2) CHECK (Price > 0),\n    Pub_Year   INT  CHECK (Pub_Year BETWEEN 1900 AND 2100)\n);\n\nCREATE TABLE Issue (\n    Issue_ID   INT  PRIMARY KEY,\n    Book_ID    INT,\n    Member_ID  INT,\n    Issue_Date DATE  NOT NULL,\n    FOREIGN KEY (Book_ID) REFERENCES Book(Book_ID)\n        ON DELETE CASCADE\n        ON UPDATE SET NULL\n);\n```\n\nWe will now systematically verify the **three integrity constraints** for every conceivable operation.\n\n---\n\n## 3.2 Verifying Entity Integrity\n\n**Step 1: Identify the Primary Key.**\nThe PK of `Book` is `Book_ID`. The PK of `Issue` is `Issue_ID`.\n\n**Step 2: Verify Non-Null property.**\nSince both columns are declared `PRIMARY KEY`, the DBMS automatically rejects any `INSERT` that supplies `NULL` for these columns.\n\n**Step 3: Verify Uniqueness property.**\nAny attempt to insert a duplicate `Book_ID` triggers a **primary key violation** error.\n\n### Transition Logic\nThe Entity Integrity Constraint can be written formally as a logical conjunction:\n\n$$\n\\text{Entity\\_Integrity}(R) \\iff \\big(\\forall t \\in R: t[PK] \\neq \\text{NULL}\\big) \\land \\big(\\forall t_i, t_j \\in R: t_i[PK] = t_j[PK] \\Rightarrow t_i = t_j\\big)\n$$\n\nThis single expression must be evaluated to `TRUE` for the tuple to be admitted.\n\n### Decision Matrix for Entity Integrity\n\n| Operation | Condition | Result |\n|---|---|---|\n| INSERT t with t[PK] = NULL | Violates non-null | ❌ Rejected |\n| INSERT t with t[PK] = existing value | Violates uniqueness | ❌ Rejected |\n| INSERT t with t[PK] = new non-null value | Satisfies both | ✅ Accepted |\n| UPDATE t setting t[PK] = NULL | Violates non-null | ❌ Rejected |\n| UPDATE t setting t[PK] = duplicate | Violates uniqueness | ❌ Rejected |\n\n---\n\n## 3.3 Verifying Referential Integrity\n\n**Step 1: Identify the Foreign Key relationship.**\nThe `Issue` table has `Book_ID` as a Foreign Key referencing `Book(Book_ID)`.\n\n**Step 2: For every INSERT into Issue, the DBMS checks:**\n- If `Book_ID` is NULL → Allowed (assuming NULL is permitted for the column).\n- If `Book_ID` is non-NULL → DBMS queries the `Book` table: $\\exists t \\in \\text{Book}: t[\\text{Book\\_ID}] = \\text{new\\_value}$?\n  - If yes → ✅ Inserted.\n  - If no → ❌ Referential integrity violation.\n\n**Step 3: For every DELETE from Book, the DBMS executes the declared `ON DELETE` action.**\n\nSince we declared `ON DELETE CASCADE`, deleting a book with ID = 101 from `Book` will **automatically delete all rows in `Issue` where Book_ID = 101**.\n\n**Step 4: For every UPDATE of Book_ID in Book, the DBMS executes `ON UPDATE SET NULL`.**\nIf Book_ID = 101 is updated to 999, all `Issue.Book_ID` values that were 101 are set to NULL.\n\n### Formal Transition\n\n$$\n\\text{Referential\\_Integrity}(R_1, R_2) \\iff \\forall t_1 \\in R_1: \\big(t_1[FK] = \\text{NULL}\\big) \\lor \\big(\\exists t_2 \\in R_2: t_1[FK] = t_2[PK]\\big)\n$$\n\n### Decision Matrix for Referential Integrity\n\n| Operation | Condition | Default Action | Cascaded Action |\n|---|---|---|---|\n| INSERT child with FK = NULL | No reference needed | ✅ Allowed (if column allows NULL) | Same |\n| INSERT child with FK = valid PK | Reference exists in parent | ✅ Allowed | Same |\n| INSERT child with FK = invalid value | No matching parent | ❌ Rejected | Same |\n| DELETE parent (no children) | No references | ✅ Allowed | Same |\n| DELETE parent (children exist) | Children would dangle | ❌ Rejected (RESTRICT) | ✅ Cascade delete children |\n| UPDATE parent PK (no children) | No references | ✅ Allowed | Same |\n| UPDATE parent PK (children exist) | Children references would dangle | ❌ Rejected (RESTRICT) | ✅ Cascade update / SET NULL |\n\n---\n\n## 3.4 Verifying Domain Integrity\n\n**Step 1: Apply Data Type rules.**\n- `Book.Price` must be a DECIMAL(8,2). A string or a date is rejected.\n- `Book.Pub_Year` must be INT.\n\n**Step 2: Apply CHECK expressions.**\n- `Price > 0` → Insert with `Price = -100` is rejected.\n- `Pub_Year BETWEEN 1900 AND 2100` → Insert with `Pub_Year = 1850` is rejected.\n\n**Step 3: Apply NOT NULL.**\n- `Title NOT NULL` → Insert without a title is rejected.\n\n### Analytical Matrix: Domain Constraint Checking\n\n| Column | Domain | Constraint | Violation Example | DBMS Reaction |\n|---|---|---|---|---|\n| `Title` | VARCHAR(100) | NOT NULL | `INSERT ... VALUES (NULL, ...)` | ❌ Error: cannot insert NULL |\n| `Price` | DECIMAL(8,2) | CHECK > 0 | `INSERT ... VALUES (..., -50, ...)` | ❌ Error: check constraint violated |\n| `Pub_Year` | INT | CHECK 1900–2100 | `INSERT ... VALUES (..., 1500, ...)` | ❌ Error: check constraint violated |\n| `Title` | VARCHAR(100) | Length ≤ 100 | `INSERT ... VALUES (..., 'a very long string...')` | ❌ Error: value too long |\n| `Title` | VARCHAR(100) | (no other) | `INSERT ... VALUES (..., 'Harry Potter', ...)` | ✅ Accepted |\n\n### Compound Logic for Domain Integrity\n\nFor a column $A$ with domain $D$ and check expression $C$, an insertion of value $v$ is accepted if and only if:\n\n$$\n\\text{Domain\\_OK}(v) \\iff (v \\in D) \\land C(v) = \\text{TRUE} \\land (\\text{NULL constraint satisfied})\n$$\n\n---\n\n## 3.5 Exhaustive KTU-Style Worked Problem\n\n**Problem Statement (KTU 14-mark pattern):**\nConsider the following relations for a university:\n\n```sql\nDEPARTMENT(Dept_ID, Dept_Name, HOD)\nFACULTY(Fac_ID, Fac_Name, Salary, Dept_ID)\n```\n\nWhere `Dept_ID` in FACULTY is a Foreign Key referencing DEPARTMENT. Assume:\n- Salary must be between 25,000 and 5,00,000.\n- HOD cannot be NULL.\n- All Fac_IDs must be unique.\n- When a Department is deleted, set the corresponding Dept_ID in FACULTY to NULL.\n\n**Solution Steps:**\n\n### Step 1: Identify the Integrity Constraints\n\n| Constraint | Type | Applied To |\n|---|---|---|\n| `Dept_ID` is unique and not null in DEPARTMENT | **Entity Integrity** | DEPARTMENT.Dept_ID |\n| `Fac_ID` is unique and not null in FACULTY | **Entity Integrity** | FACULTY.Fac_ID |\n| `FACULTY.Dept_ID` must exist in DEPARTMENT (or be NULL) | **Referential Integrity** | FK relationship |\n| `Salary BETWEEN 25000 AND 500000` | **Domain Integrity** | FACULTY.Salary |\n| `HOD NOT NULL` | **Domain Integrity** | DEPARTMENT.HOD |\n| `Fac_ID UNIQUE` | (Subset of Entity) | FACULTY.Fac_ID |\n\n### Step 2: Write the SQL DDL\n\n```sql\nCREATE TABLE DEPARTMENT (\n    Dept_ID   INT  PRIMARY KEY,\n    Dept_Name VARCHAR(50) NOT NULL,\n    HOD       VARCHAR(50) NOT NULL\n);\n\nCREATE TABLE FACULTY (\n    Fac_ID    INT  PRIMARY KEY,\n    Fac_Name  VARCHAR(50) NOT NULL,\n    Salary    DECIMAL(10,2) CHECK (Salary BETWEEN 25000 AND 500000),\n    Dept_ID   INT,\n    FOREIGN KEY (Dept_ID) REFERENCES DEPARTMENT(Dept_ID)\n        ON DELETE SET NULL\n);\n```\n\n### Step 3: Trace the Operations\n\n**Operation A:** Insert a new department `(10, 'CSE', 'Dr. Verma')`.\n- Entity Integrity: `10` is unique, non-null. ✅\n- Domain: `HOD` is non-null, data types correct. ✅\n- Result: Accepted.\n\n**Operation B:** Insert a faculty `(201, 'Ms. Roy', 30000, 10)`.\n- Entity Integrity: `201` is unique, non-null. ✅\n- Referential: `10` exists in DEPARTMENT. ✅\n- Domain: `30000` satisfies `BETWEEN 25000 AND 500000`. ✅\n- Result: Accepted.\n\n**Operation C:** Insert a faculty `(202, 'Mr. Lee', 20000, 10)`.\n- Domain fails: `20000 < 25000`. ❌\n- Result: Rejected — *check constraint violated*.\n\n**Operation D:** Insert a faculty `(203, 'Ms. Tan', 30000, 99)`.\n- Referential fails: `99` does not exist in DEPARTMENT. ❌\n- Result: Rejected — *foreign key constraint violated*.\n\n**Operation E:** Delete DEPARTMENT where `Dept_ID = 10`.\n- Referential action: `ON DELETE SET NULL`.\n- All FACULTY rows with `Dept_ID = 10` have their `Dept_ID` updated to `NULL`.\n- Result: Accepted with cascading update.\n\n---\n\n## 3.6 Summary: Mapping Constraints to SQL Clauses\n\n| KTU Constraint Class | SQL Clause(s) | Violation Keyword in MySQL/PostgreSQL |\n|---|---|---|\n| Entity Integrity | `PRIMARY KEY` | `ERROR 1062 (23000): Duplicate entry` or `Column 'X' cannot be null` |\n| Referential Integrity | `FOREIGN KEY ... REFERENCES ... [ON DELETE ... ON UPDATE ...]` | `ERROR 1452 (23000): Cannot add or update a child row: a foreign key constraint fails` |\n| Domain Integrity (Type) | `INT`, `VARCHAR`, `DATE`, etc. | `ERROR 1264 (22003): Out of range value` |\n| Domain Integrity (Check) | `CHECK (condition)` | `ERROR 3819 (HY000): Check constraint is violated` |\n| Domain Integrity (Nullability) | `NOT NULL` | `ERROR 1048 (23000): Column 'X' cannot be null` |\n| Domain Integrity (Default) | `DEFAULT value` | (No error; default is supplied automatically) |\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Mermaid ER Diagram: Library System with Constraints\n\nThe following diagram illustrates the **Department–Faculty** relationship from Section 3.5, annotated with all three integrity constraints.\n\n```mermaid\nerDiagram\n    DEPARTMENT ||--o{ FACULTY : \"has (referential integrity)\"\n\n    DEPARTMENT {\n        INT Dept_ID PK \"PRIMARY KEY entity integrity\"\n        VARCHAR Dept_Name \"NOT NULL domain constraint\"\n        VARCHAR HOD \"NOT NULL domain constraint\"\n    }\n\n    FACULTY {\n        INT Fac_ID PK \"PRIMARY KEY entity integrity\"\n        VARCHAR Fac_Name \"NOT NULL domain constraint\"\n        DECIMAL Salary \"CHECK 25000 to 500000 domain constraint\"\n        INT Dept_ID FK \"FOREIGN KEY referential integrity\"\n    }\n```\n\n**Reading the Diagram:**\n- The `||--o{` notation means: **one** DEPARTMENT (mandatory) has **zero or more** FACULTY (optional). The \"optional\" side of the child is precisely what permits the `ON DELETE SET NULL` action — when a department is deleted, the children are not destroyed; they simply lose their reference.\n- The labels `PK` and `FK` next to each attribute name visually distinguish the constraint categories.\n\n---\n\n## 4.2 Flowchart: Constraint Validation Sequence\n\nWhen the DBMS receives a new SQL statement (INSERT, UPDATE, or DELETE), it executes a **constraint validation pipeline** in a specific order. The following flowchart shows the order in which the three primary constraints are checked.\n\n```mermaid\nflowchart TD\n    A[Receive SQL DML Statement] --> B{Parse and Validate Syntax}\n    B -- Syntax Error --> Z1[Reject: Syntax Error]\n    B -- Valid Syntax --> C{Operation Type?}\n    C -- INSERT or UPDATE --> D{Check Domain Integrity}\n    C -- DELETE or UPDATE on Parent --> F{Check Entity Integrity on Parent PK}\n    D -- Violation --> Z2[Reject: Domain Constraint Violation]\n    D -- Passed --> E{Check Entity Integrity on Target Row}\n    E -- Violation --> Z3[Reject: Entity Integrity Violation]\n    E -- Passed --> G{Check Referential Integrity}\n    F -- Violation --> Z4[Reject: Entity Integrity on Parent]\n    F -- Passed --> H{Trigger Referential Actions on Children}\n    H -- Action Fails --> Z5[Reject: Referential Action Failed]\n    H -- Action Succeeds --> I{Check Final Referential State}\n    I -- Violation --> Z6[Reject: Referential Integrity Violation]\n    I -- Passed --> J[Commit Transaction: Operation Accepted]\n    G -- Violation --> Z6\n    G -- Passed --> J\n```\n\n**Key Observations from the Flow:**\n1. **Domain constraints are checked first** because they are the cheapest to evaluate (single attribute comparison).\n2. **Entity integrity** is checked before **referential integrity** to ensure the new/modified row itself is internally valid.\n3. **Referential actions** (CASCADE, SET NULL, etc.) are triggered *only* when modifying the **parent** table; they recurse into the children and re-enter the validation pipeline.\n\n---\n\n## 4.3 Block Diagram: Three-Tier Integrity Enforcement Architecture\n\nFor students who must answer design-level questions, here is the **system-level architecture** of how a modern RDBMS (e.g., MySQL, PostgreSQL, Oracle) implements these constraints.\n\n```mermaid\nblock-beta\n    columns 3\n    block:UserLayer:1\n      A[\"SQL DML Statement INSERT UPDATE DELETE\"]\n    end\n    block:ParserLayer:1\n      B[\"Parser and Query Optimizer\"]\n    end\n    block:ConstraintEngine:1\n      C[\"Integrity Constraint Engine\"]\n    end\n    block:StorageLayer:1\n      D[\"Disk Buffer and Storage Manager\"]\n    end\n\n    A --> B\n    B --> C\n    C --> D\n\n    subgraph C_Internal [Inside the Constraint Engine]\n      C1[\"Type Checker Data Type Length\"]\n      C2[\"Null Checker NOT NULL DEFAULT\"]\n      C3[\"Check Evaluator CHECK expressions\"]\n      C4[\"Key Uniqueness B-tree index lookup\"]\n      C5[\"FK Validator Parent table scan\"]\n    end\n```\n\n> [!NOTE]\n> **KTU Tip:** The Constraint Engine is itself composed of sub-modules. The **Type Checker** and **Null Checker** enforce **Domain Integrity**; the **Key Uniqueness** module enforces **Entity Integrity**; the **FK Validator** enforces **Referential Integrity**. This decomposition is often asked as a 7-mark \"explain the architecture\" question.\n\n---\n\n## 4.4 Comparison Matrix: When to Use Which Constraint\n\n```mermaid\nquadrantChart\n    title \"Choosing the Right Integrity Constraint\"\n    x-axis \"Single Attribute\" --> \"Multi Attribute\"\n    y-axis \"Intra-Relation\" --> \"Inter-Relation\"\n    quadrant-1 \"Referential Integrity (FK)\"\n    quadrant-2 \"Composite Entity Integrity\"\n    quadrant-3 \"Simple Domain Constraint\"\n    quadrant-4 \"Entity Integrity (PK) and UNIQUE\"\n    \"PRIMARY KEY single col\": [0.2, 0.2]\n    \"PRIMARY KEY composite\": [0.8, 0.2]\n    \"UNIQUE single col\": [0.2, 0.3]\n    \"FOREIGN KEY\": [0.7, 0.8]\n    \"CHECK single col\": [0.1, 0.1]\n    \"CHECK multi col\": [0.6, 0.1]\n    \"NOT NULL\": [0.15, 0.15]\n```\n\nUse this chart to reason about which SQL clause maps to which quadrant of the constraint space.\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A: Short Answer Questions (3 Marks Each)\n\n---\n\n### Question 1. [KTU University Exam – May 2024]\n\n**Differentiate between Entity Integrity Constraint and Referential Integrity Constraint with suitable examples.**\n\n**Model Answer (3 marks):**\n\n| Aspect | Entity Integrity | Referential Integrity |\n|---|---|---|\n| **Purpose** | Ensures the Primary Key is unique and not null for every row. | Ensures that every Foreign Key value matches a Primary Key in the parent table (or is NULL). |\n| **Scope** | Applies within a single relation (intra-relation). | Applies across two relations (inter-relation). |\n| **Enforced by** | `PRIMARY KEY` declaration. | `FOREIGN KEY ... REFERENCES ...` declaration. |\n| **Example** | In `STUDENT(Roll_No, Name)`, every `Roll_No` must be unique and non-null. | In `ENROLLMENT(Roll_No, Course_ID)`, every `Roll_No` must exist in the `STUDENT` table. |\n| **Violation** | Inserting `(NULL, 'Anu')` or `(1, 'Anu')` twice into STUDENT. | Inserting `(99, 'CS101')` into ENROLLMENT when no Roll_No = 99 exists in STUDENT. |\n\n*(Award: 1 mark purpose, 1 mark example, 1 mark distinction table.)*\n\n---\n\n### Question 2. [KTU University Exam – Dec 2023]\n\n**What is a Domain Constraint? Explain with a SQL example using the `CHECK` clause.**\n\n**Model Answer (3 marks):**\n\nA **Domain Constraint** restricts the values that an attribute can take to a specific data type, range, or enumerated set. It ensures that every cell in a column contains a valid, atomic value drawn from its declared domain.\n\n**SQL Example:**\n\n```sql\nCREATE TABLE Employee (\n    Emp_ID    INT  PRIMARY KEY,\n    Name      VARCHAR(50) NOT NULL,\n    Age       INT  CHECK (Age BETWEEN 18 AND 60),\n    Gender    CHAR(1) CHECK (Gender IN ('M', 'F', 'O')),\n    Salary    DECIMAL(10,2) CHECK (Salary > 0)\n);\n```\n\nHere, the `CHECK` clauses enforce that Age is in [18, 60], Gender is one of three values, and Salary is strictly positive. Any INSERT violating these checks is rejected by the DBMS.\n\n*(Award: 1 mark definition, 1.5 marks SQL example, 0.5 marks rejection explanation.)*\n\n---\n\n## 5.2 Part B: Full 14-Mark Questions (Module Internal Choice)\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfalls — Read Before Answering!**\n> 1. **Do NOT confuse UNIQUE with PRIMARY KEY.** UNIQUE allows NULLs (in most SQL dialects); PRIMARY KEY does NOT.\n> 2. **NOT NULL on a Primary Key column is implicit**; do not list it separately as a \"Domain Constraint\" — it is part of Entity Integrity.\n> 3. **Referential Integrity allows NULL in the foreign key column** (unless `NOT NULL` is explicitly declared). Many students wrongly state that FK cannot be NULL.\n> 4. **Always specify the referential action** (CASCADE, SET NULL, etc.) when a question asks about parent delete behavior. Default is RESTRICT/NO ACTION, not CASCADE.\n> 5. **The CHECK constraint** evaluates to TRUE, FALSE, or UNKNOWN (NULL). The constraint is violated only when the result is FALSE. A row that evaluates to UNKNOWN is *accepted* by SQL — this is a frequent KTU trick question.\n> 6. **Marks lost on Schema diagrams:** Always draw the FK arrow direction (child → parent), and label the cardinality (1:N, M:N). Omitting the FK direction costs at least 1 mark.\n\n---\n\n### Question A (Choice 1) — 14 Marks\n#### [KTU University Exam – July 2024 Model Paper]\n\n**(a)** Define the three integrity constraints — Entity, Referential, and Domain. For each, give one SQL clause that enforces it and one example violation. **(7 marks)**\n\n**(b)** Consider the following two relations for a hospital:\n\n- `DOCTOR(Doc_ID, Doc_Name, Specialization, Dept_ID)`\n- `DEPARTMENT(Dept_ID, Dept_Name, HOD_Name)`\n\nWrite the complete `CREATE TABLE` statements with the following constraints:\n1. Doc_ID and Dept_ID are primary keys (Entity Integrity).\n2. `DOCTOR.Dept_ID` is a foreign key referencing `DEPARTMENT.Dept_ID`, with `ON DELETE CASCADE`.\n3. Specialization must be one of: 'Cardiology', 'Neurology', 'Orthopedics', 'Pediatrics'.\n4. HOD_Name cannot be NULL.\n5. Dept_Name is unique.\n\nDemonstrate, with three sample queries, how the DBMS would reject violations of each constraint. **(7 marks)**\n\n---\n\n#### Model Solution\n\n### Part (a) — 7 marks\n\n**Entity Integrity Constraint:**\n- *Definition:* The Primary Key of a relation must always be unique and non-null for every tuple.\n- *SQL Clause:* `PRIMARY KEY`\n- *Example Violation:* `INSERT INTO Student VALUES (NULL, 'Anu');` — violates non-null.\n\n**Referential Integrity Constraint:**\n- *Definition:* Every Foreign Key value in the child table must either be NULL or match a Primary Key in the parent table.\n- *SQL Clause:* `FOREIGN KEY (FK_col) REFERENCES Parent(PK_col)`\n- *Example Violation:* `INSERT INTO Enrollment VALUES (1, 99, 'CS101');` — when Roll_No = 99 does not exist in Student.\n\n**Domain Integrity Constraint:**\n- *Definition:* Every value of an attribute must be drawn from its declared domain (data type, range, format).\n- *SQL Clause:* `CHECK (condition)`, `NOT NULL`, `DEFAULT`, data type declaration\n- *Example Violation:* `INSERT INTO Employee VALUES (1, 'X', -5000);` — Salary is negative, violating `CHECK (Salary > 0)`.\n\n*(Allocation: 1 mark definition + 1 mark SQL clause + 1 mark violation per constraint × 3 = 9 sub-marks, scaled to 7.)*\n\n---\n\n### Part (b) — 7 marks\n\n**Step 1: Write the CREATE TABLE statements.**\n\n```sql\nCREATE TABLE DEPARTMENT (\n    Dept_ID    INT  PRIMARY KEY,\n    Dept_Name  VARCHAR(50) UNIQUE NOT NULL,\n    HOD_Name   VARCHAR(50) NOT NULL\n);\n\nCREATE TABLE DOCTOR (\n    Doc_ID         INT  PRIMARY KEY,\n    Doc_Name       VARCHAR(50) NOT NULL,\n    Specialization VARCHAR(30) CHECK (Specialization IN\n                       ('Cardiology','Neurology','Orthopedics','Pediatrics')),\n    Dept_ID        INT,\n    FOREIGN KEY (Dept_ID) REFERENCES DEPARTMENT(Dept_ID)\n        ON DELETE CASCADE\n);\n```\n\n**Step 2: Demonstrate the three violation queries.**\n\n**Violation 1 — Entity Integrity:**\n```sql\nINSERT INTO DOCTOR VALUES (NULL, 'Dr. Roy', 'Cardiology', 1);\n-- ERROR: Column 'Doc_ID' cannot be null\n```\n\n**Violation 2 — Referential Integrity:**\n```sql\nINSERT INTO DOCTOR VALUES (101, 'Dr. Lee', 'Neurology', 99);\n-- ERROR: Cannot add or update a child row: a foreign key constraint fails\n-- (no Dept_ID = 99 exists in DEPARTMENT)\n```\n\n**Violation 3 — Domain Integrity:**\n```sql\nINSERT INTO DOCTOR VALUES (102, 'Dr. Tan', 'Dermatology', 1);\n-- ERROR: Check constraint 'doctor_chk_1' is violated\n-- ('Dermatology' is not in the allowed enumeration)\n```\n\n*(Allocation: 2 marks for the DDL, 1 mark for each violation example × 3, 1 mark for explaining the rejection.)*\n\n---\n\n### Question B (Choice 2) — 14 Marks\n#### [KTU University Exam – Dec 2023 University Paper]\n\n**(a)** Explain **Referential Integrity** in detail. Discuss all four `ON DELETE` referential actions (CASCADE, RESTRICT, SET NULL, SET DEFAULT) with suitable examples using a `DEPT` and `EMP` schema. **(7 marks)**\n\n**(b)** Consider the `EMP(Emp_ID, Emp_Name, Salary, Dept_ID)` and `DEPT(Dept_ID, Dept_Name, Location)` tables. Write the SQL DDL with appropriate constraints and answer the following:\n1. What error occurs if you try to insert an employee with `Dept_ID = 500` when no such department exists? Justify your answer with the relevant constraint.\n2. What error occurs if you try to update `DEPT.Dept_ID` from 10 to 100, when an employee in EMP references Dept_ID = 10?\n3. What is the result of the following query? Justify.\n   `SELECT * FROM EMP WHERE Dept_ID = NULL;`\n4. Write a query to add a new constraint that Salary must be greater than 10,000. **(7 marks)**\n\n---\n\n#### Model Solution\n\n### Part (a) — 7 marks\n\n**Referential Integrity** guarantees that a Foreign Key in a child table always points to a valid Primary Key in the parent table (or is NULL). It is the \"no dangling reference\" rule of relational databases.\n\n**The Four ON DELETE Actions:**\n\nLet `DEPT(Dept_ID, Dept_Name)` be the parent and `EMP(Emp_ID, Dept_ID)` be the child with `FOREIGN KEY (Dept_ID) REFERENCES DEPT(Dept_ID)`.\n\n| Action | Behavior | Example |\n|---|---|---|\n| **CASCADE** | Automatically deletes all child rows whose FK matches the deleted parent. | `DELETE FROM DEPT WHERE Dept_ID=10;` → All `EMP` rows with `Dept_ID=10` are also deleted. |\n| **RESTRICT** (default) | Rejects the DELETE if any child row references the parent. | `DELETE FROM DEPT WHERE Dept_ID=10;` → ERROR 1451: Cannot delete or update a parent row. |\n| **SET NULL** | Sets the FK of dependent child rows to NULL. | `DELETE FROM DEPT WHERE Dept_ID=10;` → Child rows now have `Dept_ID=NULL`. |\n| **SET DEFAULT** | Sets the FK to its DEFAULT value (or NULL if no default). | `DELETE FROM DEPT WHERE Dept_ID=10;` → Child rows get the default `Dept_ID`. |\n\n**SQL Syntax:**\n```sql\nFOREIGN KEY (Dept_ID) REFERENCES DEPT(Dept_ID)\n    ON DELETE CASCADE        -- or RESTRICT, SET NULL, SET DEFAULT\n    ON UPDATE CASCADE;\n```\n\n*(Allocation: 1 mark intro, 1.5 marks per action × 4 = 6 marks, 0.5 marks for SQL syntax.)*\n\n---\n\n### Part (b) — 7 marks\n\n**Step 1: SQL DDL with Constraints:**\n\n```sql\nCREATE TABLE DEPT (\n    Dept_ID   INT  PRIMARY KEY,\n    Dept_Name VARCHAR(50) NOT NULL UNIQUE,\n    Location  VARCHAR(30)\n);\n\nCREATE TABLE EMP (\n    Emp_ID   INT  PRIMARY KEY,\n    Emp_Name VARCHAR(50) NOT NULL,\n    Salary   DECIMAL(10,2),\n    Dept_ID  INT,\n    FOREIGN KEY (Dept_ID) REFERENCES DEPT(Dept_ID)\n        ON DELETE SET NULL\n        ON UPDATE CASCADE\n);\n```\n\n**Step 2: Answer the sub-questions.**\n\n**(1) Error on inserting EMP with Dept_ID = 500:**\nThe DBMS raises a **Referential Integrity Violation** error:\n> `ERROR 1452 (23000): Cannot add or update a child row: a foreign key constraint fails. The key '500' is not present in the parent table 'DEPT'.`\n\nThis is because the `FOREIGN KEY (Dept_ID) REFERENCES DEPT(Dept_ID)` constraint requires that any non-NULL value of `Dept_ID` in `EMP` must match an existing `Dept_ID` in `DEPT`. Since `500` does not exist, the insert is rejected. *(1.5 marks)*\n\n**(2) Error on updating DEPT.Dept_ID from 10 to 100:**\nThe DBMS performs a **CASCADE** action (since we declared `ON UPDATE CASCADE`):\n- All `EMP` rows with `Dept_ID = 10` are automatically updated to `Dept_ID = 100`.\n- If we had used the default `ON UPDATE RESTRICT`, an error would be raised: `ERROR 1451: Cannot delete or update a parent row: a foreign key constraint fails`. *(1.5 marks)*\n\n**(3) Result of `SELECT * FROM EMP WHERE Dept_ID = NULL;`:**\nThe query returns **zero rows**. This is because in SQL's **three-valued logic**, the comparison `Dept_ID = NULL` evaluates to `UNKNOWN`, not `TRUE`. The correct way to test for NULL is `WHERE Dept_ID IS NULL`. *(1.5 marks)*\n\n**(4) Add a CHECK constraint:**\n```sql\nALTER TABLE EMP ADD CONSTRAINT chk_salary CHECK (Salary > 10000);\n```\nThis adds a new domain constraint ensuring all employees have a salary greater than 10,000. *(2.5 marks)*\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Revision Checklist — Constraints Module**\n\n### Entity Integrity\n- ⭐ Primary Key must be **unique AND non-null** — both are mandatory (logical AND).\n- ⭐ Composite Primary Keys apply Entity Integrity to the **entire combination**, not individual attributes.\n- ⭐ SQL automatically enforces both uniqueness and non-null when you declare `PRIMARY KEY`.\n- ⭐ **UNIQUE ≠ PRIMARY KEY**: UNIQUE allows multiple NULLs; PRIMARY KEY does not allow any NULL.\n\n### Referential Integrity\n- ⭐ Foreign Key can be **NULL** (unless `NOT NULL` is declared on the column).\n- ⭐ Default `ON DELETE` action is `RESTRICT` (or `NO ACTION`); default `ON UPDATE` is `RESTRICT`.\n- ⭐ Four actions: `CASCADE`, `RESTRICT`, `SET NULL`, `SET DEFAULT`. Know the difference for 14-mark questions.\n- ⭐ FK must reference the **parent table's Primary Key or a UNIQUE column**.\n- ⭐ A FK can reference columns in the **same table** (self-referential, e.g., `Emp_ID` referencing `Manager_ID`).\n\n### Domain Integrity\n- ⭐ Five pillars: **Data Type, Length, NOT NULL, DEFAULT, CHECK**.\n- ⭐ `CHECK` evaluates to TRUE / FALSE / UNKNOWN. Only FALSE causes a rejection.\n- ⭐ `CHECK` constraints can reference multiple columns of the same table (table-level CHECK).\n- ⭐ In MySQL 8.0+, `CHECK` constraints are **enforced** (older versions parsed but ignored — a famous exam trap).\n\n### Cross-Cutting Tips\n- ⭐ Always draw **ER diagrams with PK/FK labels** to score full marks on schema-based questions.\n- ⭐ When classifying constraints: **Entity = PK, Referential = FK, Domain = everything else column-level**.\n- ⭐ \"Default\" in SQL is **not a constraint** per se, but a domain property — it supplies a value when none is given.\n- ⭐ A column can have **multiple constraints** simultaneously: `Name VARCHAR(50) NOT NULL UNIQUE CHECK (LENGTH(Name) >= 2)`.\n\n### Common KTU 14-Mark Question Patterns\n1. \"Define and differentiate the three integrity constraints with examples.\" → Use a comparison table.\n2. \"Explain referential actions on DELETE/UPDATE with examples.\" → List all four actions.\n3. \"Write the DDL for schema X with all constraints.\" → Carefully mark PK, FK, CHECK, NOT NULL.\n4. \"What error will the DBMS raise for operation Y?\" → Identify the constraint violated and quote the error.\n5. \"Add/modify a constraint using ALTER TABLE.\" → Use `ADD CONSTRAINT ... CHECK ...` syntax.\n\n> [!NOTE]\n> **Last-Minute Mnemonic for the Three Constraints:**\n> - **E**ntity → \"**E**very row has a **unique ID**\" (Primary Key).\n> - **R**eferential → \"**R**eferences must **R**eally exist\" (Foreign Key).\n> - **D**omain → \"**D**ata must be of the **D**eclared type and **D**omain\" (CHECK, NOT NULL, type).\n\n---\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "transactionmanagementdefinitionoftransactionsandtransactionpropertiesacidpropertiesandimplementationoftransactionprocessinglogic": {
      "title": "Transaction Management: Definition of transactions and transaction properties (ACID properties), and implementation of transaction processing logic",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 3\n> - **Topic:** Transaction Management: Definition of transactions and transaction properties (ACID properties), and implementation of transaction processing logic\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of a Transaction\n\nA **transaction** is a logical unit of database processing that includes one or more database access operations (read/write operations) which must be executed as an **atomic**, **indivisible** unit of work. According to the KTU 2024 DBMS syllabus, a transaction is a sequence of operations performed on a database that transforms the database from one consistent state to another consistent state.\n\n> [!IMPORTANT]\n> **KTU Board Definition (Verbatim Expectation):**\n> *“A transaction is a program unit whose execution may or may not change the contents of a database. It is a logical unit of work that transforms the database from one consistent state to another consistent state.”*\n\nA transaction is delimited by two special operations:\n- **BEGIN TRANSACTION** (or `START TRANSACTION`) — marks the beginning.\n- **COMMIT** — permanently saves all changes.\n- **ROLLBACK** (or `ABORT`) — undoes all changes since BEGIN.\n\n### Formal Transaction Notation\n\nA transaction **T** is represented as:\n\n$$T = \\{O_1, O_2, O_3, \\ldots, O_n\\}$$\n\nwhere each $O_i$ is either a `read(X)` or `write(X)` operation, and $X$ is a data item.\n\n## 1.2 Conceptual Analogy — The \"Bank Transfer\" Intuition\n\nImagine you walk into a bank to transfer ₹5,000 from Account **A** to Account **B**. The clerk performs two actions:\n\n1. **Debit ₹5,000** from Account A.\n2. **Credit ₹5,000** into Account B.\n\nIf a power cut happens *between* step 1 and step 2, money vanishes from A but never reaches B — a **disaster**. To prevent this, the bank wraps both steps inside a single \"transaction\":\n\n> Either **both happen**, or **neither happens**. There is no \"half-done\" state.\n\nThis is exactly what a DBMS transaction guarantees. The database is your bank ledger, and the transaction is the wrapper around the debit-credit pair.\n\n> [!NOTE]\n> **Real-World Engineering Mapping:**\n> - **Banking Systems** — Fund transfers, NEFT/RTGS.\n> - **E-commerce** — Order placement (deduct stock → deduct payment → create order).\n> - **Railway Reservations** — Seat allocation + fare deduction.\n> - **Aadhaar/UIDAI** — Demographic update with multiple table writes.\n\n## 1.3 Transaction States — The Lifecycle\n\nEvery transaction passes through a defined state machine. The KTU 2024 syllabus explicitly lists the following states:\n\n| State | Description |\n|---|---|\n| **Active** | Initial state; transaction is executing read/write operations. |\n| **Partially Committed** | Final operation has executed; changes are in main memory buffer, not yet on disk. |\n| **Committed** | All changes permanently written to disk; transaction terminates successfully. |\n| **Failed** | Normal execution can no longer proceed; needs rollback. |\n| **Aborted** | Transaction has been rolled back; database restored to its pre-transaction state. |\n\n> [!TIP]\n> **KTU Memory Trick — \"APCF-FA\":** Active → Partially Committed → Committed → Failed → Aborted. Draw this as a flowchart in your answer sheet — examiners award easy marks for diagrams.\n\n## 1.4 The ACID Properties — Quick Introduction\n\nThe acronym **ACID** summarizes the four non-negotiable properties that every reliable transaction must satisfy:\n\n- **A** — **Atomicity** (All or Nothing)\n- **C** — **Consistency** (Valid state to Valid state)\n- **I** — **Isolation** (Concurrent transactions don't interfere)\n- **D** — **Durability** (Committed changes survive crashes)\n\nThese will be dissected thoroughly in Section 2.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The ACID Properties — Detailed Theoretical Breakdown\n\n### 2.1.1 Atomicity — \"All or Nothing\"\n\n**Definition:** A transaction is treated as a single, indivisible logical unit. Either **all** its operations execute successfully, or **none** of them take effect.\n\n**The \"Why\":** Without atomicity, partial failures (system crash, power loss) leave the database in an inconsistent, half-updated state.\n\n**The \"How\":** Implemented via a **Transaction-Recovery Subsystem** that maintains a **log file**. Before any modification, the old value (`before-image`) is written to the log. If the transaction aborts, the system uses the log to **undo** all operations.\n\n**Example Failure Scenario:**\n- Transaction T1: `UPDATE Accounts SET balance = balance - 5000 WHERE acc_no = 'A101'`\n- If power fails here → on restart, the recovery manager reads the log and **rolls back** the partial debit.\n\n> [!IMPORTANT]\n> **KTU Board Phrasing Expectation:**\n> *\"Atomicity guarantees that a transaction is treated as a single, atomic unit — either it completes in its entirety or has no effect whatsoever on the database.\"*\n\n### 2.1.2 Consistency — \"Valid → Valid\"\n\n**Definition:** A transaction must transform the database from one **consistent state** to another **consistent state**. The database must satisfy all its **integrity constraints** (primary key, foreign key, check constraints, triggers) both before and after the transaction.\n\n**The \"Why\":** To ensure no transaction can leave the database in a state that violates business rules.\n\n**The \"How\":** The DBMS checks all integrity constraints at the end of the transaction. If any constraint is violated, the transaction is **rolled back**.\n\n**Mathematical Formulation:**\n\n$$\\text{If } DB_n \\models IC \\text{ and } T \\text{ executes, then } DB_{n+1} \\models IC$$\n\nwhere $IC$ is the set of integrity constraints and $DB_n, DB_{n+1}$ are database states before and after.\n\n**Example:** Consider a `CHECK (balance >= 0)` constraint on a bank account. A withdrawal transaction that would make `balance = -100` violates this constraint and is aborted, preserving consistency.\n\n### 2.1.3 Isolation — \"Concurrent = Serial\"\n\n**Definition:** Even when multiple transactions execute **concurrently**, each transaction must behave as if it were the **only** transaction in the system. Intermediate results of one transaction are hidden from other concurrent transactions.\n\n**The \"Why\":** Concurrent execution (interleaving) can lead to problems like **dirty reads, non-repeatable reads, phantom reads, and lost updates** if not isolated.\n\n**The \"How\":** Achieved through **concurrency control protocols**:\n- **Lock-based:** Two-Phase Locking (2PL), Strict 2PL.\n- **Timestamp-based:** Time Ordering, Thomas Write Rule.\n- **Optimistic:** Validation-based concurrency control.\n- **Isolation Levels** (SQL Standard): `READ UNCOMMITTED`, `READ COMMITTED`, `REPEATABLE READ`, `SERIALIZABLE`.\n\n**Isolation Level Comparison Table:**\n\n| Isolation Level | Dirty Read | Non-Repeatable Read | Phantom Read |\n|---|---|---|---|\n| Read Uncommitted | Allowed | Allowed | Allowed |\n| Read Committed | Prevented | Allowed | Allowed |\n| Repeatable Read | Prevented | Prevented | Allowed |\n| Serializable | Prevented | Prevented | Prevented |\n\n> [!NOTE]\n> **KTU 2024 Highlight:** The SQL `SERIALIZABLE` level provides the strictest isolation but has the **lowest concurrency** and **highest overhead**. This is a frequently asked 3-mark question.\n\n### 2.1.4 Durability — \"Once Committed, Forever Saved\"\n\n**Definition:** Once a transaction has been **committed**, its effects must **persist** in the database, even in the event of system crashes, power failures, or other disasters.\n\n**The \"Why\":** Without durability, a confirmed payment could \"disappear\" after a server reboot — unacceptable in any production system.\n\n**The \"How\":** Implemented via:\n- **Write-Ahead Logging (WAL):** Log records are flushed to disk **before** the corresponding data pages.\n- **Database Buffers** are force-written to disk at commit time.\n- **Redo logs** are used during recovery to re-apply committed transactions.\n\n**Mathematical Guarantee:**\n\n$$\\text{If } T \\text{ commits at time } t_c, \\text{ then } \\forall t > t_c, \\text{ the effects of } T \\text{ are permanent.}$$\n\n## 2.2 Implementation of Transaction Processing Logic\n\n### 2.2.1 The Transaction Manager (TM)\n\nThe TM is a DBMS component responsible for:\n1. Starting and ending transactions (`BEGIN`, `COMMIT`, `ROLLBACK`).\n2. Maintaining the **transaction table** (active transactions).\n3. Coordinating with the **Concurrency Control Manager** and **Recovery Manager**.\n\n### 2.2.2 The Transaction Table\n\n| TID | Status | Last Log Record |\n|---|---|---|\n| T1 | Active | <T1, START> |\n| T2 | Committed | <T2, COMMIT> |\n| T3 | Aborted | <T3, ABORT> |\n\n### 2.2.3 The Log File Structure\n\nA log is an append-only file with records of the form:\n\n$$<T_i, \\text{operation}, \\text{data item}, \\text{old value}, \\text{new value}>$$\n\nExample log entries for transaction T1 (`A = A - 100; B = B + 100`):\n\n| Sequence | Log Record |\n|---|---|\n| 1 | `<T1, START>` |\n| 2 | `<T1, A, 1000, 900>` |\n| 3 | `<T1, B, 2000, 2100>` |\n| 4 | `<T1, COMMIT>` |\n\n> [!TIP]\n> **KTU Exam Tip:** Always mention that the log must be **flushed to disk** before the transaction commits — this is the **Write-Ahead Logging (WAL)** rule. Examiners love this point.\n\n## 2.3 KTU High-Yield Formula Sheet / Cheat Sheet\n\n| Concept | Notation / Formula | Boundary / Rule |\n|---|---|---|\n| Transaction | $T = \\{O_1, O_2, \\ldots, O_n\\}$ | $n \\geq 1$; ends with COMMIT or ABORT |\n| Read Operation | $r_i(X)$ | Transaction $T_i$ reads data item $X$ |\n| Write Operation | $w_i(X)$ | Transaction $T_i$ writes data item $X$ |\n| Commit | $c_i$ | Permanent, cannot be undone |\n| Abort | $a_i$ | Roll back all changes of $T_i$ |\n| ACID | $A \\land C \\land I \\land D$ | All four must hold simultaneously |\n| Isolation Level | SERIALIZABLE $\\subset$ REPEATABLE READ $\\subset$ READ COMMITTED $\\subset$ READ UNCOMMITTED | Set inclusion is the reverse of strictness |\n| Write-Ahead Logging | $\\text{Log}(X) \\rightarrow \\text{Disk}$ **before** $X \\rightarrow \\text{Disk}$ | WAL rule |\n| Schedule Equivalence | $S_1 \\equiv_v S_2 \\lor S_1 \\equiv_c S_2$ | View or conflict equivalence |\n\n## 2.4 Real-World Engineering Utility\n\n| Domain | Transaction Use Case |\n|---|---|\n| **Banking (NEFT/IMPS)** | Multi-step fund transfer with ACID guarantees |\n| **Stock Trading** | Buy/sell matching engines using serializable isolation |\n| **Hospital HIS** | Patient admission — allocate bed + create bill + assign doctor |\n| **Airline Reservations** | Seat assignment + payment deduction + PNR creation |\n| **E-commerce (Amazon/Flipkart)** | Order placement with inventory + payment + shipping |\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: Transaction State Transition Logic\n\nLet us mathematically derive the **transition conditions** between transaction states. Let $S(T, t)$ denote the state of transaction $T$ at time $t$.\n\n### Step 1: Define the State Space\n\nThe transaction can be in exactly one of five states at any time $t$:\n\n$$S \\in \\{\\text{Active}, \\text{Partially Committed}, \\text{Committed}, \\text{Failed}, \\text{Aborted}\\}$$\n\n### Step 2: Initial Condition\n\nAt time $t = t_0$ (when `BEGIN TRANSACTION` is issued):\n\n$$S(T, t_0) = \\text{Active}$$\n\n### Step 3: Transition to Partially Committed\n\nWhen the **last operation** of $T$ has been executed, but changes are not yet written to disk:\n\n$$S(T, t) = \\text{Partially Committed} \\iff \\text{All } O_i \\in T \\text{ executed, but } \\neg \\text{DiskWrite}$$\n\n### Step 4: Transition to Committed\n\nAfter the commit log record is written to disk and acknowledged:\n\n$$S(T, t) = \\text{Committed} \\iff \\text{LogForceWrite}(\\text{COMMIT}) = \\text{true}$$\n\n### Step 5: Transition to Failed (Failure Path)\n\nA transaction enters the Failed state if:\n\n$$S(T, t) = \\text{Failed} \\iff \\text{LogicalError}(T) \\lor \\text{SystemError} \\lor \\text{ConcurrencyViolation}$$\n\n### Step 6: Transition to Aborted\n\nAfter a failed transaction is rolled back:\n\n$$S(T, t) = \\text{Aborted} \\iff \\text{Rollback}(T) = \\text{complete}$$\n\n### Step 7: Restating the Final State Machine\n\n$$\nS(T, t+\\Delta t) = \n\\begin{cases}\n\\text{Partially Committed} & \\text{if } S(T,t) = \\text{Active} \\land \\text{LastOpDone} \\\\\n\\text{Committed} & \\text{if } S(T,t) = \\text{Partially Committed} \\land \\text{DiskWriteOK} \\\\\n\\text{Failed} & \\text{if } S(T,t) \\in \\{\\text{Active, Partially Committed}\\} \\land \\text{Error} \\\\\n\\text{Aborted} & \\text{if } S(T,t) = \\text{Failed} \\land \\text{RollbackDone}\n\\end{cases}\n$$\n\nThis algebraic form is the **logical basis** for state transition diagrams (drawn in Section 4).\n\n## 3.2 Derivation: ACID Property Satisfaction\n\nLet $T = \\{O_1, O_2, \\ldots, O_n\\}$ be a transaction, $DB_0$ the initial database state, and $DB_f$ the final state.\n\n### Step 1: Atomicity\n\nAtomicity is satisfied iff:\n\n$$\\text{Atomicity}(T) = [\\forall i \\in [1,n]: \\text{apply}(O_i, DB) \\text{ OR } \\text{apply}(O_i, DB) = \\text{null}]$$\n\nIn Boolean logic:\n\n$$\\text{Atomic}(T) = \\bigwedge_{i=1}^{n} (\\text{Applied}(O_i) \\lor \\neg \\text{Applied}(O_i) \\text{ due to abort})$$\n\nThis forces either **all-or-none** semantics.\n\n### Step 2: Consistency\n\nConsistency holds iff all integrity constraints $IC = \\{C_1, C_2, \\ldots, C_k\\}$ are satisfied:\n\n$$\\text{Consistent}(T) = \\left( \\bigwedge_{j=1}^{k} C_j(DB_0) \\right) \\Rightarrow \\left( \\bigwedge_{j=1}^{k} C_j(DB_f) \\right)$$\n\n### Step 3: Isolation\n\nLet $S$ be a schedule of $n$ concurrent transactions $\\{T_1, \\ldots, T_n\\}$. Isolation is satisfied iff:\n\n$$\\text{Isolated}(S) \\iff S \\equiv \\text{SerialSchedule}(T_1, T_2, \\ldots, T_n) \\text{ in some order}$$\n\nIn set-theoretic form:\n\n$$\\text{Result}(S) \\in \\{\\text{Result}(S_\\sigma) \\mid \\sigma \\in \\text{Permutations}(\\{T_1, \\ldots, T_n\\})\\}$$\n\n### Step 4: Durability\n\nFor any committed transaction $T_c$ at commit time $t_c$:\n\n$$\\forall t > t_c, \\forall \\text{crash events } e: \\text{State}(T_c, t) = \\text{Committed}$$\n\nThe committed state is **monotonically persistent**.\n\n## 3.3 Analytical Matrix: ACID Implementation Mechanisms\n\n| ACID Property | Failure It Prevents | Mechanism Used | System Component |\n|---|---|---|---|\n| **Atomicity** | Partial updates | Undo logs, rollback | Recovery Manager |\n| **Consistency** | Constraint violation | Integrity checks, triggers | Constraint Enforcer |\n| **Isolation** | Lost updates, dirty reads | Locks, timestamps, MVCC | Concurrency Control Manager |\n| **Durability** | Lost commits | Redo logs, WAL, checkpoints | Recovery Manager |\n\n## 3.4 Worked Example — Transaction Processing Logic\n\nConsider the following transaction T that transfers ₹500 from Account A to Account B:\n\n```sql\nBEGIN TRANSACTION;\n    UPDATE Accounts SET balance = balance - 500 WHERE acc_id = 'A';\n    UPDATE Accounts SET balance = balance + 500 WHERE acc_id = 'B';\nCOMMIT;\n```\n\n**Step-by-Step ACID Verification:**\n\n| Step | ACID Property | Verification |\n|---|---|---|\n| 1. `BEGIN` | — | Transaction enters **Active** state. |\n| 2. First `UPDATE A` | Atomicity | Old value of A is written to log (`<T, A, 5000, 4500>`). |\n| 3. Second `UPDATE B` | Atomicity | Old value of B is written to log (`<T, B, 3000, 3500>`). |\n| 4. `COMMIT` executes | Durability | `<T, COMMIT>` written to log and flushed to disk. |\n| 5. If power fails at Step 2 | Atomicity | On restart, recovery manager uses log to **undo** the partial change to A. |\n| 6. If two such transactions run concurrently | Isolation | 2PL or MVCC ensures no lost update on A or B. |\n| 7. If balance goes below 0 | Consistency | `CHECK (balance >= 0)` triggers abort. |\n\nThis single example touches **all four ACID properties** — a favourite KTU question pattern.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Transaction State Transition Diagram\n\nThe following Mermaid diagram illustrates the complete lifecycle of a database transaction. Every node uses purely alphanumeric IDs and clean uppercase labels as mandated by the KTU-PREMIER-ENGINE V10 protocol.\n\n```mermaid\nstateDiagram-v2\n    [*] --> nodeA_Active : BEGIN TRANSACTION\n    nodeA_Active --> nodeB_PartiallyCommitted : Last operation executed\n    nodeB_PartiallyCommitted --> nodeC_Committed : Log flushed to disk\n    nodeB_PartiallyCommitted --> nodeD_Failed : Cannot write to disk\n    nodeA_Active --> nodeD_Failed : Logical or system error\n    nodeD_Failed --> nodeE_Aborted : Rollback complete\n    nodeE_Aborted --> [*] : Restart or terminate\n    nodeC_Committed --> [*] : Transaction ends\n```\n\n> **Reading Guide:** Trace the path `[*] → nodeA_Active → nodeB_PartiallyCommitted → nodeC_Committed → [*]` for a successful transaction. The path through `nodeD_Failed → nodeE_Aborted` represents the failure and recovery branch.\n\n## 4.2 ACID Properties Architecture\n\nThe following block diagram shows how ACID properties are implemented across the DBMS subsystems.\n\n```mermaid\nflowchart TB\n    subgraph TRANSACTION[\"TRANSACTION LAYER\"]\n        direction TB\n        T_USER[User Initiated Transaction T]\n        T_BEGIN[\"BEGIN TRANSACTION\"]\n        T_OPS[Read Write Operations]\n        T_END[\"COMMIT or ROLLBACK\"]\n        T_USER --> T_BEGIN --> T_OPS --> T_END\n    end\n\n    subgraph PROPS[\"ACID PROPERTIES\"]\n        direction LR\n        PROP_A[\"A ATOMICITY\"]\n        PROP_C[\"C CONSISTENCY\"]\n        PROP_I[\"I ISOLATION\"]\n        PROP_D[\"D DURABILITY\"]\n    end\n\n    subgraph ENGINE[\"DBMS ENGINE\"]\n        direction TB\n        ENG_RM[\"Recovery Manager\"]\n        ENG_CCM[\"Concurrency Control Manager\"]\n        ENG_IC[\"Integrity Constraint Checker\"]\n        ENG_TM[\"Transaction Manager\"]\n        ENG_TM --> ENG_CCM\n        ENG_TM --> ENG_RM\n        ENG_TM --> ENG_IC\n    end\n\n    subgraph STORAGE[\"STORAGE LAYER\"]\n        direction TB\n        ST_LOG[\"Log File Write Ahead Log\"]\n        ST_DISK[\"Disk Database\"]\n        ST_LOG --> ST_DISK\n    end\n\n    T_END --> PROPS\n    PROPS --> ENG_TM\n    ENG_RM --> ST_LOG\n    ENG_IC --> ST_DISK\n```\n\n## 4.3 Transaction Processing Flow — Begin → Operations → Commit\n\n```mermaid\nflowchart TD\n    START([Application Issues SQL]) --> B1[\"BEGIN TRANSACTION\"]\n    B1 --> OP1{Operation Type}\n    OP1 -->|Read| R1[\"Read data from buffer or disk\"]\n    OP1 -->|Write| W1[\"Generate undo and redo log records\"]\n    R1 --> NEXT\n    W1 --> W2[\"Acquire write lock on data item\"]\n    W2 --> W3[\"Update in-memory buffer\"]\n    W3 --> NEXT\n    NEXT{More operations}\n    NEXT -->|Yes| OP1\n    NEXT -->|No| CHK{\"Constraint check passes\"}\n    CHK -->|Yes| COM[\"COMMIT: Flush log to disk and release locks\"]\n    CHK -->|No| ROLL[\"ROLLBACK: Apply undo log records and release locks\"]\n    COM --> END1([Transaction Successful])\n    ROLL --> END2([Transaction Aborted])\n```\n\n## 4.4 Concurrency Control — Isolation in Action\n\n```mermaid\nsequenceDiagram\n    participant T1 as Transaction T1\n    participant TM as Transaction Manager\n    participant CCM as Concurrency Control\n    participant DB as Database\n\n    T1->>TM: BEGIN TRANSACTION\n    TM->>CCM: Request lock on data item A\n    CCM-->>T1: Lock granted (X-lock)\n    T1->>DB: WRITE A = 900\n    DB-->>T1: Write successful\n    T1->>TM: Request COMMIT\n    TM->>CCM: Release all locks\n    TM-->>T1: COMMIT acknowledged\n    Note over DB: Changes permanently persisted\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n---\n\n### Question 1: [KTU University Exam — Dec 2023]\n\n**Explain the Atomicity and Durability properties of a transaction. How are they implemented in a DBMS?**\n\n**Model Answer (3 Marks):**\n\n**Atomicity:** A transaction is treated as a single, indivisible logical unit. Either all its operations execute successfully, or none take effect. This prevents partial updates.\n\n**Durability:** Once a transaction commits, its effects must persist in the database, even in the event of system crashes or power failures.\n\n**Implementation:**\n- Both properties are implemented by the **Recovery Manager** of the DBMS.\n- **Atomicity** is enforced using the **undo log** (before-images), which rolls back any transaction that aborts.\n- **Durability** is enforced using the **redo log** (after-images) and the **Write-Ahead Logging (WAL)** protocol, which ensures that the log is flushed to disk *before* the corresponding data is written.\n\n> **[Valuation Key — Mark Distribution]:** Definition of Atomicity (1) + Definition of Durability (1) + Implementation mechanism (1).\n\n---\n\n### Question 2: [KTU University Exam — June 2024]\n\n**Differentiate between the COMMIT and ROLLBACK operations in a transaction.**\n\n**Model Answer (3 Marks):**\n\n| Aspect | COMMIT | ROLLBACK |\n|---|---|---|\n| **Effect on DB** | Permanently saves all changes. | Undoes all changes since `BEGIN`. |\n| **Terminal State** | Transaction moves to `Committed` state. | Transaction moves to `Aborted` state. |\n| **Reversibility** | Irreversible — cannot be undone. | Effectively \"reverses\" the transaction. |\n| **Log Entry** | `<T, COMMIT>` is written. | `<T, ABORT>` is written, followed by undo operations. |\n| **Lock Release** | All locks are released. | All locks are released. |\n| **Use Case** | When all operations succeed. | When any operation fails or constraint is violated. |\n\n> **[Valuation Key — Mark Distribution]:** Any 3 valid differences (1 mark each).\n\n---\n\n## 5.2 Part B Questions (14 Marks — Internal Choice)\n\n---\n\n### **Question A (14 Marks)** — [KTU University Exam — Model Paper 2024]\n\n**(a)** With a neat diagram, explain the different **states of a transaction** in detail. Mention the transitions and the conditions under which each transition occurs. **(7 Marks)**\n\n**(b)** What is meant by the **ACID properties** of a transaction? Explain each property with a suitable example. **(7 Marks)**\n\n---\n\n#### Model Solution — Part (a) [7 Marks]\n\nA transaction passes through **five distinct states** during its lifetime. The states and transitions are:\n\n**1. Active State (Initial State):**\n- The transaction enters this state when `BEGIN TRANSACTION` is issued.\n- All read/write operations are executed in this state.\n- The transaction stays active as long as operations are still being executed.\n\n**2. Partially Committed State:**\n- After the **last operation** of the transaction has been executed, but before changes are written to disk.\n- At this point, the changes exist only in the main memory buffer.\n\n**3. Committed State (Successful End):**\n- After all changes are successfully written to disk and the `<T, COMMIT>` log record is flushed.\n- The transaction is now considered **successfully completed**.\n\n**4. Failed State:**\n- Entered when normal execution can no longer proceed due to:\n  - Logical errors (e.g., constraint violation, division by zero).\n  - System errors (e.g., deadlock, hardware failure).\n- The transaction cannot proceed further from this state.\n\n**5. Aborted State:**\n- After the failed transaction is rolled back using the undo log.\n- The database is restored to its state **before** the transaction began.\n- The transaction may be restarted or terminated.\n\n**State Transition Diagram (Draw this in the answer sheet):**\n\n```\n                BEGIN\n                  |\n                  v\n        +---------+----------+\n        |                    |\n        |     ACTIVE         |  <-- read/write operations\n        |                    |\n        +---------+----------+\n                  | last op done\n                  v\n        +---------+----------+\n        |     PARTIALLY      |  --+\n        |     COMMITTED      |    |  write to disk OK\n        |                    |    |  --> COMMITTED\n        +---------+----------+    |\n                  |  error         |\n                  v                v\n        +---------+----------+  +----------+\n        |                    |  |          |\n        |     FAILED         |  | COMMITTED|\n        |                    |  |          |\n        +---------+----------+  +----------+\n                  | rollback\n                  v\n        +---------+----------+\n        |     ABORTED        |\n        +--------------------+\n```\n\n> **[Valuation Key]:** Diagram (3 marks) + Explanation of each state (1 mark each = 4 marks).\n\n---\n\n#### Model Solution — Part (b) [7 Marks]\n\nThe **ACID properties** are the four essential characteristics of a reliable database transaction. The acronym stands for:\n\n**1. Atomicity (All or Nothing):** A transaction is executed as a single, indivisible unit. Either all its operations succeed, or none have any effect on the database. This is implemented using **undo logs** that allow the system to roll back incomplete transactions.\n\n*Example:* In a fund transfer, the debit from A and credit to B must both succeed or both fail. If the system crashes after the debit but before the credit, atomicity ensures the debit is rolled back.\n\n**2. Consistency (Valid to Valid):** The transaction must transform the database from one consistent state to another, preserving all integrity constraints (primary key, foreign key, check constraints).\n\n*Example:* If a `CHECK (balance >= 0)` constraint exists, any transaction that would create a negative balance is aborted, preserving consistency.\n\n**3. Isolation (Concurrent = Serial):** Even when multiple transactions run concurrently, the intermediate state of any one transaction is invisible to others. The system ensures the same result as if the transactions ran one after another.\n\n*Example:* If T1 and T2 both try to update the same account, isolation (via locks) ensures T1's update is not overwritten by T2's update mid-execution.\n\n**4. Durability (Committed = Permanent):** Once a transaction commits, its effects are permanent and survive system failures, crashes, or power loss. This is achieved using **redo logs** and **Write-Ahead Logging (WAL)**.\n\n*Example:* After a payment is confirmed by `COMMIT`, even a server crash immediately afterwards will not lose the payment record on restart.\n\n> **[Valuation Key]:** Each property: 1.5 marks (Definition 0.75 + Example 0.75) = 6 marks. Header statement: 1 mark.\n\n---\n\n### **Question B (14 Marks)** — [KTU University Exam — Model Paper 2024]\n\n**(a)** Explain the role of the **Transaction Manager** and **Recovery Manager** in implementing transaction processing logic in a DBMS. **(7 Marks)**\n\n**(b)** Discuss the various **problems of concurrent transactions** and explain how **lock-based protocols** ensure isolation. **(7 Marks)**\n\n---\n\n#### Model Solution — Part (a) [7 Marks]\n\n**Transaction Manager (TM):**\nThe Transaction Manager is the central DBMS component that coordinates all transaction-related activities. Its responsibilities include:\n1. **Transaction Initiation and Termination:** Handles `BEGIN TRANSACTION`, `COMMIT`, and `ROLLBACK` statements.\n2. **Transaction Table Maintenance:** Keeps track of all active transactions with their IDs and statuses.\n3. **Coordination:** Interfaces with the Concurrency Control Manager (for locks) and the Recovery Manager (for log-based recovery).\n4. **Atomicity Enforcement:** Ensures that a transaction either completes in its entirety or is rolled back.\n\n**Recovery Manager (RM):**\nThe Recovery Manager ensures the **Atomicity** and **Durability** properties of transactions. Its functions include:\n1. **Log Management:** Maintains a sequential log file with records of all transaction operations.\n2. **Undo Operation:** In case of transaction failure or abort, the RM applies the **undo log** to restore the database to its pre-transaction state (for Atomicity).\n3. **Redo Operation:** After a system crash, the RM re-applies the effects of committed transactions from the **redo log** to ensure Durability.\n4. **Write-Ahead Logging (WAL):** Ensures that log records are written to disk *before* the corresponding data is modified on disk, so that recovery is always possible.\n\n**Combined Workflow:**\n\n| Step | Manager | Action |\n|---|---|---|\n| 1 | TM | `BEGIN TRANSACTION` issued. |\n| 2 | TM + RM | For each write, RM logs the old and new values. |\n| 3 | TM | If `COMMIT` → RM forces log to disk (Durability). |\n| 4 | TM | If `ABORT` → RM applies undo log (Atomicity). |\n| 5 | TM | If crash → on restart, RM replays redo + undo. |\n\n> **[Valuation Key]:** TM explanation (3) + RM explanation (3) + Workflow table (1) = 7 marks.\n\n---\n\n#### Model Solution — Part (b) [7 Marks]\n\n**Problems of Concurrent Transaction Execution:**\n\n1. **Lost Update Problem:** Two transactions read and update the same data item, and the second update overwrites the first, losing one update.\n\n2. **Dirty Read (Uncommitted Dependency):** A transaction reads data written by another transaction that later aborts, leaving the reader with invalid data.\n\n3. **Non-Repeatable Read (Inconsistent Analysis):** A transaction reads the same data item twice and finds different values because another transaction modified it in between.\n\n4. **Phantom Read:** A transaction re-executes a query that returns a set of rows and finds additional rows inserted by another transaction.\n\n**Lock-Based Protocol — Ensuring Isolation:**\n\nLocks are variables associated with a data item that control access. Two basic lock types:\n- **Shared Lock (S-lock):** Multiple transactions can read simultaneously.\n- **Exclusive Lock (X-lock):** Only one transaction can write; blocks all others.\n\n**Lock Compatibility Matrix:**\n\n| Current \\ Requested | S-lock | X-lock |\n|---|---|---|\n| **No lock** | Granted | Granted |\n| **S-lock** | Granted | Blocked |\n| **X-lock** | Blocked | Blocked |\n\n**Two-Phase Locking (2PL) Protocol:**\nA transaction must follow two phases:\n- **Growing Phase:** Acquire locks, no locks can be released.\n- **Shrinking Phase:** Release locks, no locks can be acquired.\n\nThis guarantees **serializability**, the strongest form of isolation. Variants include **Basic 2PL**, **Conservative 2PL** (acquire all locks upfront), and **Strict 2PL** (hold all X-locks until commit).\n\n> **[Valuation Key]:** 4 problems (1 mark each = 4) + Lock types & 2PL explanation (3) = 7 marks.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> 1. **Confusing Isolation with Atomicity:** Students often write that isolation ensures \"all-or-nothing\" — this is **atomicity**. Isolation is about hiding intermediate states from concurrent transactions.\n> 2. **Forgetting WAL:** A frequent omission is failing to mention **Write-Ahead Logging** when discussing durability. Examiners specifically test this as a 1-2 mark point.\n> 3. **No diagram in state question:** Drawing the state transition diagram is worth 3 of 7 marks. Skipping it = guaranteed loss.\n> 4. **Mixing up COMMIT and ROLLBACK semantics:** COMMIT is irreversible; ROLLBACK undoes the entire transaction. Do not say ROLLBACK \"deletes\" the transaction.\n> 5. **Not giving examples for ACID:** Bare definitions fetch 1 mark; examples double the score. Always provide a real-world (preferably banking) example for each property.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> **Rapid-Revision Checklist — KTU 2024 Module 3**\n\n- **Transaction** is a logical unit of work that transforms the DB from one consistent state to another.\n- The three boundary operations are **BEGIN**, **COMMIT**, and **ROLLBACK** (or **ABORT**).\n- The **five transaction states** are: **Active, Partially Committed, Committed, Failed, Aborted** (memorize: **APCF-A**).\n- **ACID stands for Atomicity, Consistency, Isolation, Durability.**\n- **Atomicity** → All-or-Nothing, implemented via **undo logs**.\n- **Consistency** → Constraint-preserving, enforced by **integrity checks**.\n- **Isolation** → Concurrent = Serial effect, enforced via **locks / 2PL / MVCC**.\n- **Durability** → Committed changes survive crashes, enforced via **redo logs and WAL**.\n- **Write-Ahead Logging (WAL):** Log must be written to disk *before* the data item is modified.\n- **Isolation Levels (strictest to weakest):** `SERIALIZABLE` > `REPEATABLE READ` > `READ COMMITTED` > `READ UNCOMMITTED`.\n- **Transaction Manager** coordinates transactions; **Recovery Manager** ensures atomicity and durability.\n- **Concurrency problems:** Lost Update, Dirty Read, Non-Repeatable Read, Phantom Read.\n- **Lock types:** Shared (S) for read, Exclusive (X) for write.\n- **2PL protocol** = Growing phase + Shrinking phase; guarantees serializability.\n- **Always draw the state transition diagram** in answers worth 7+ marks.\n- **Always provide a banking example** for ACID questions to earn full marks.\n- **WAL + Recovery Manager** = the two magic phrases that examiners look for in durability answers.\n- **For isolation level questions**, draw a comparison table — examiners reward structured answers.\n- The \"Bank Transfer\" analogy (A → B transfer) is a **safe, universal example** for any ACID question.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "concurrencycontroltechniquestomanageconcurrentaccesseglockingtimestamping": {
      "title": "Concurrency Control: Techniques to manage concurrent access (e.g., locking, time stamping)",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 3\n> - **Topic:** Concurrency Control: Techniques to manage concurrent access (e.g., locking, time stamping)\n\n<!-- SECTION_1_START -->\n## SECTION 1 — Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition of Concurrency Control\n\n**Concurrency Control** is the set of techniques, protocols, and mechanisms employed by a Database Management System (DBMS) to **coordinate concurrent access to a shared database** such that the integrity, consistency, and isolation properties of the data are preserved across simultaneous transactions.\n\n> [!NOTE]\n> **KTU 2024 Syllabus Definition:** Concurrency control is the process of managing simultaneous operations on the database without having them interfere with one another, ensuring the database remains in a **consistent state** before, during, and after transaction execution.\n\nThe problem arises because a database is a **shared resource**. When two or more transactions read and write the same data item **at the same time**, the result may be **non-deterministic** and the database may be left in an **inconsistent state** unless the DBMS enforces strict ordering rules.\n\n### 1.2 The ACID Connection\n\nConcurrency control is the operational arm of the **I** (Isolation) and partially **C** (Consistency) properties of the **ACID** transaction model.\n\n| ACID Property | Role of Concurrency Control |\n|---|---|\n| **Atomicity** | Handled by Transaction Manager / Recovery Subsystem |\n| **Consistency** | Ensured by both Recovery and Concurrency Subsystems |\n| **Isolation** | Primarily handled by Concurrency Control |\n| **Durability** | Handled by Recovery Manager |\n\n### 1.3 The Real-World Analogy — \"The Shared Whiteboard\"\n\n> [!IMPORTANT]\n> **Analogy:** Imagine a **group of 10 students** who must all write answers on a **single shared whiteboard** during a viva, simultaneously. Without rules:\n> - Student A and Student B may overwrite each other's answers (**Lost Update**).\n> - Student A may read what Student B wrote, then B erases it, leaving A with a non-existent value (**Dirty Read**).\n> - Two students may grab the same marker at once, **deadlocking** the system.\n\n> A **locking mechanism** is like giving each student a \"token\" before they can write. A **time-stamp protocol** is like saying \"Whoever raised their hand *first* gets to write first — no matter what.\"\n\n### 1.4 Why Concurrent Access is Inherently Dangerous\n\nWhen transactions execute **interleaved** (which is what actually happens in a multi-user system for performance), the result must be **equivalent** to some **serial (one-after-another) execution**. If it is not, the database is said to be in an **inconsistent state** — the classic problem solved by concurrency control.\n\n> [!NOTE]\n> **K**ey **T**erms to memorize:\n> - **Schedule (or History):** The chronological execution order of concurrent transaction operations.\n> - **Serial Schedule:** Transactions execute one after the other with no interleaving.\n> - **Serializable Schedule:** An interleaved schedule whose result is *equivalent* to some serial schedule.\n> - **Conflict:** Two operations conflict if they belong to different transactions, access the same data item, and at least one is a `WRITE`.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## SECTION 2 — Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 The Three Major Problems Caused by Uncontrolled Concurrency\n\n#### Problem 1 — **Lost Update Problem**\nOccurs when two transactions read the same data item and both update it, causing the **first update to be overwritten** by the second.\n\n| Time | T1 | T2 | DB Value |\n|---|---|---|---|\n| t1 | READ(X) → 100 | | 100 |\n| t2 | | READ(X) → 100 | 100 |\n| t3 | X = X + 50 → 150 | | 100 |\n| t4 | | X = X + 20 → 120 | 120 |\n| t5 | WRITE(X) | | **120** |\n| t6 | | WRITE(X) | **120** |\n\n**Final value = 120**, but the *correct* serial result should have been **150** (if T1 ran after T2) or **120** (if T2 ran after T1) — but never an arbitrary mix. T1's update is **lost**.\n\n#### Problem 2 — **Dirty Read (Uncommitted Dependency)**\nA transaction reads data written by another **uncommitted** transaction, which may later be rolled back.\n\n#### Problem 3 — **Unrepeatable Read**\nA transaction reads the same item twice and gets **different values** because another transaction modified it in between.\n\n#### Problem 4 — **Phantom Read\nA transaction re-executes a query and finds **new tuples** that were inserted by another concurrent transaction.\n\n---\n\n### 2.2 LOCKING TECHNIQUE — The Most Widely Used Protocol\n\nA **lock** is a small variable associated with a data item that controls whether read/write operations can be performed on that item.\n\n#### 2.2.1 Types of Locks\n\n> [!IMPORTANT]\n> - **Shared Lock (S-Lock / Read Lock):** Multiple transactions can hold an S-lock on the same item simultaneously. Compatible with other S-locks.\n> - **Exclusive Lock (X-Lock / Write Lock):** Only one transaction can hold an X-lock. **Incompatible** with any other lock (S or X).\n\n#### 2.2.2 Lock Compatibility Matrix\n\n| Requested ↓ / Held → | **No Lock** | **S-Lock** | **X-Lock** |\n|---|---|---|---|\n| **S-Lock** | ✅ Granted | ✅ Granted | ❌ Blocked |\n| **X-Lock** | ✅ Granted | ❌ Blocked | ❌ Blocked |\n\n> ✅ = Compatible (request succeeds)\n> ❌ = Incompatible (requesting transaction is **made to wait**)\n\n#### 2.2.3 Two-Phase Locking Protocol (2PL) — *The Golden Protocol*\n\n> [!NOTE]\n> **The Locking Theorem (Eswaran et al., 1976):** *If all transactions follow the Two-Phase Locking Protocol, then all possible interleaved schedules are guaranteed to be conflict-serializable.*\n\nA transaction is **2PL-compliant** if its lock and unlock requests can be divided into two distinct phases:\n\n1. **Growing Phase:** The transaction may **acquire** new locks but **cannot release** any lock.\n2. **Shrinking Phase:** The transaction may **release** locks but **cannot acquire** any new lock.\n\n**Lock Point:** The instant when the transaction has obtained its *final* (last) lock — the boundary between the two phases.\n\n##### Variants of 2PL\n\n| Variant | Rule | Use Case |\n|---|---|---|\n| **Basic 2PL** | Two phases as above. | Guarantees conflict-serializability. |\n| **Conservative 2PL** | Transaction acquires **ALL** locks *before* it begins executing. Prevents deadlock but impractical. | Deadlock-free. |\n| **Strict 2PL** | All **X-locks are held until commit/abort**. | Guarantees recoverability; standard in commercial DBMS. |\n| **Rigorous 2PL** | Both **S and X locks** held until commit/abort. | Strongest; produces ordered serializable schedules. |\n\n---\n\n### 2.3 TIMESTAMP ORDERING (TSO) TECHNIQUE\n\nEvery transaction $T_i$ is assigned a **unique timestamp** $TS(T_i)$ when it starts. The DBMS also tracks, for each data item $X$:\n- **W-timestamp** of $X$ = the largest timestamp of any transaction that has *successfully written* $X$.\n- **R-timestamp** of $X$ = the largest timestamp of any transaction that has *successfully read* $X$.\n\n> **Rule:** When transaction $T_i$ issues `READ(X)` or `WRITE(X)`, compare $TS(T_i)$ with $X$'s timestamps. If the operation would violate timestamp ordering, **abort $T_i$** and restart it with a new timestamp.\n\n##### Thomas' Write Rule (Optimization)\n\nWhen $T_i$ requests `WRITE(X)`:\n- If $TS(T_i) \\lt$ W-timestamp$(X)$: an *earlier* transaction has already written — **abort $T_i$** (normal TSO).\n- If $TS(T_i) \\lt$ R-timestamp$(X)$: $T_i$'s write would overwrite a *newer* read value — **abort $T_i$** (normal TSO).\n- **EXCEPTION (Thomas' Rule):** If $TS(T_i) \\gt$ W-timestamp$(X)$ and $TS(T_i) \\gt$ R-timestamp$(X)$, the write is allowed **even if** $TS(T_i) \\lt$ W-timestamp$(X)$ — in that specific case, **silently ignore** the write instead of aborting.\n\nThomas' Write Rule generates **more serializable schedules** and reduces unnecessary aborts but produces schedules that are **NOT recoverable**.\n\n---\n\n### 2.4 KTU High-Yield Formula & Rule Sheet\n\n| Concept | Symbol / Expression | Meaning |\n|---|---|---|\n| Timestamp of transaction | $TS(T_i)$ | Unique monotonic value assigned at start |\n| Read timestamp | $R\\text{-}TS(X)$ | Max TS of any txn that read $X$ |\n| Write timestamp | $W\\text{-}TS(X)$ | Max TS of any txn that wrote $X$ |\n| TSO Read rule (allowed) | $TS(T_i) \\ge W\\text{-}TS(X)$ | $T_i$ may read $X$ |\n| TSO Write rule (allowed) | $TS(T_i) \\ge R\\text{-}TS(X)$ and $TS(T_i) \\ge W\\text{-}TS(X)$ | $T_i$ may write $X$ |\n| Thomas Write ignore condition | $TS(T_i) \\lt W\\text{-}TS(X)$ and $TS(T_i) \\ge R\\text{-}TS(X)$ | Silently ignore the write |\n| Lock compatibility | $S \\cap S = S$,  $S \\cap X = \\emptyset$,  $X \\cap X = \\emptyset$ | Locks held simultaneously |\n| Number of locks in DB with n items | $\\le 2n$ (one S + one X per item max) | Upper bound |\n| Serializability guarantee | 2PL $\\Rightarrow$ Conflict-Serializable | Sufficient condition |\n\n> **Real-world Utility:** Locking is used in **Oracle, MySQL (InnoDB), PostgreSQL, SQL Server**. Time-stamping is used in research prototypes and distributed systems like **Google Spanner** (which uses *TrueTime*, an extension of TSO).\n\n### 2.5 Deadlock — The Dark Side of Locking\n\nA **deadlock** occurs when two or more transactions are **each waiting for a lock held by the other**, forming a circular wait. Neither can proceed.\n\n**Four Necessary Conditions** (Coffman conditions):\n1. **Mutual Exclusion:** At least one resource is held in a non-shareable mode.\n2. **Hold and Wait:** A transaction holds resources while waiting for others.\n3. **No Preemption:** Resources cannot be forcibly taken.\n4. **Circular Wait:** A cycle exists: $T_1 \\to T_2 \\to T_3 \\to \\ldots \\to T_1$.\n\n| Strategy | Mechanism | Drawback |\n|---|---|---|\n| **Prevention** | Pre-acquire all locks / impose ordering | Conservative, reduces concurrency |\n| **Detection** | Maintain a **Wait-For Graph**; check for cycles periodically | Overhead of graph maintenance |\n| **Avoidance** | Timestamps + Wait-Die / Wound-Wait schemes | May cause unnecessary restarts |\n| **Recovery** | Abort one transaction (the **victim**); roll back its work | Wasted work |\n\n**Wait-Die Rule** (older transaction waits, younger dies)\n**Wound-Wait Rule** (older transaction wounds younger, younger waits)\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## SECTION 3 — Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Worked Example: Verifying 2PL on a Sample Schedule\n\nConsider the following schedule $S$:\n\n$$S: r_1(X), w_1(X), r_2(X), w_2(X), r_1(Y), w_1(Y), c_1, c_2$$\n\nWe need to determine if this schedule is **2PL-compliant** and what lock operations must occur.\n\n**Step 1 — Identify the operations and their data items.**\n\n| Operation | Data Item | Type | Lock Needed |\n|---|---|---|---|\n| $r_1(X)$ | X | Read | S-Lock on X |\n| $w_1(X)$ | X | Write | Upgrade S → X on X |\n| $r_2(X)$ | X | Read | S-Lock on X |\n| $w_2(X)$ | X | Write | Upgrade S → X on X |\n| $r_1(Y)$ | Y | Read | S-Lock on Y |\n| $w_1(Y)$ | Y | Write | Upgrade S → X on Y |\n| $c_1$ | — | Commit | Release all T1 locks |\n| $c_2$ | — | Commit | Release all T2 locks |\n\n**Step 2 — Construct the augmented schedule with lock/unlock operations.**\n\n> A valid 2PL execution for the same logical operations:\n\n$$\\begin{aligned}\nS_{2PL} :\\ & \\text{lock-S}_1(X),\\ r_1(X),\\ \\text{lock-X}_1(X),\\ w_1(X), \\\\\n          & \\text{lock-S}_2(X),\\ r_2(X),\\ \\text{lock-X}_2(X),\\ w_2(X), \\\\\n          & \\text{lock-S}_1(Y),\\ r_1(Y),\\ \\text{lock-X}_1(Y),\\ w_1(Y), \\\\\n          & \\text{unlock}_1(X),\\ \\text{unlock}_1(Y),\\ c_1, \\\\\n          & \\text{unlock}_2(X),\\ c_2\n\\end{aligned}$$\n\n**Step 3 — Identify the Lock Point of T1.**\n\nThe **last lock acquired by T1** is `lock-X_1(Y)`. All locks of T1 acquired up to this point form the **Growing Phase**. Any subsequent `unlock_1` operation begins the **Shrinking Phase**. Therefore, T1's **lock point** is the operation `lock-X_1(Y)`.\n\n**Step 4 — Verify 2PL compliance.**\n\nFor T1: Locks acquired in order = `S(X), X(X), S(Y), X(Y)`. Unlocks start only after all four are acquired. ✅\nFor T2: Locks acquired in order = `S(X), X(X)`. Unlocks start only after both are acquired. ✅\n\n**Conclusion:** The schedule is **2PL-compliant** and therefore **conflict-serializable**.\n\n---\n\n### 3.2 Worked Example: Basic Timestamp Ordering\n\nThree transactions arrive at times: $TS(T_1) = 1$, $TS(T_2) = 2$, $TS(T_3) = 3$.\nOperations in schedule order:\n\n$$r_1(X),\\ w_2(X),\\ w_3(X),\\ r_2(Y),\\ w_1(Y)$$\n\nInitial timestamps: $R\\text{-}TS(X) = 0$, $W\\text{-}TS(X) = 0$, $R\\text{-}TS(Y) = 0$, $W\\text{-}TS(Y) = 0$.\n\n**Step 1 — Process $r_1(X)$** ($TS(T_1) = 1$)\nCheck: $TS(T_1) \\ge W\\text{-}TS(X)$ → $1 \\ge 0$ ✅ **Allowed**.\nUpdate: $R\\text{-}TS(X) = \\max(0, 1) = 1$.\n\n**Step 2 — Process $w_2(X)$** ($TS(T_2) = 2$)\nCheck: $TS(T_2) \\ge R\\text{-}TS(X)$ → $2 \\ge 1$ ✅\nCheck: $TS(T_2) \\ge W\\text{-}TS(X)$ → $2 \\ge 0$ ✅ **Allowed**.\nUpdate: $W\\text{-}TS(X) = 2$.\n\n**Step 3 — Process $w_3(X)$** ($TS(T_3) = 3$)\nCheck: $TS(T_3) \\ge R\\text{-}TS(X)$ → $3 \\ge 1$ ✅\nCheck: $TS(T_3) \\ge W\\text{-}TS(X)$ → $3 \\ge 2$ ✅ **Allowed**.\nUpdate: $W\\text{-}TS(X) = 3$.\n\n**Step 4 — Process $r_2(Y)$** ($TS(T_2) = 2$)\nCheck: $TS(T_2) \\ge W\\text{-}TS(Y)$ → $2 \\ge 0$ ✅ **Allowed**.\nUpdate: $R\\text{-}TS(Y) = 2$.\n\n**Step 5 — Process $w_1(Y)$** ($TS(T_1) = 1$)\nCheck: $TS(T_1) \\ge R\\text{-}TS(Y)$ → $1 \\ge 2$ ❌ **FAIL — Abort T1, restart with new TS.**\n\n**Final Verdict:** T1 must be **rolled back and restarted** with a higher timestamp.\n\n---\n\n### 3.3 Worked Example: Thomas' Write Rule Application\n\nSchedule: $r_1(X),\\ w_2(X),\\ w_1(X)$.\nInitial: $R\\text{-}TS(X) = 0$, $W\\text{-}TS(X) = 0$. Timestamps: $TS(T_1) = 1$, $TS(T_2) = 2$.\n\n| Step | Operation | $TS(T_i)$ | $R\\text{-}TS(X)$ | $W\\text{-}TS(X)$ | Action |\n|---|---|---|---|---|---|\n| 1 | $r_1(X)$ | 1 | 0 | 0 | Allowed; $R\\text{-}TS(X)=1$ |\n| 2 | $w_2(X)$ | 2 | 1 | 0 | Allowed; $W\\text{-}TS(X)=2$ |\n| 3 | $w_1(X)$ | 1 | 1 | 2 | Thomas' Rule: $TS(T_i) \\lt W\\text{-}TS(X)$, but $TS(T_i) = R\\text{-}TS(X)$. **Ignore the write.** |\n\n> In **strict TSO**, step 3 would cause $T_1$ to be aborted. With **Thomas' Write Rule**, the write is **silently ignored** because $T_2$ has already written a *more recent* value.\n\n---\n\n### 3.4 Conflict Matrix Construction (Precedence Graph Test)\n\nTwo operations $O_i$ and $O_j$ from transactions $T_i$ and $T_j$ respectively **conflict** if they satisfy all three conditions:\n1. They belong to **different transactions** ($i \\neq j$).\n2. They access the **same data item**.\n3. **At least one is a `WRITE`**.\n\n| Operation Pair | Conflict? |\n|---|---|\n| $r_i(X), r_j(X)$ | ❌ No conflict |\n| $r_i(X), w_j(X)$ | ✅ Conflict — $T_i \\to T_j$ |\n| $w_i(X), r_j(X)$ | ✅ Conflict — $T_i \\to T_j$ |\n| $w_i(X), w_j(X)$ | ✅ Conflict — $T_i \\to T_j$ |\n\nA schedule is **conflict-serializable** iff its **Precedence Graph** has **no cycles**.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## SECTION 4 — Structural Diagrams & Schematics\n\n### 4.1 Two-Phase Locking Lifecycle (Block-Level Functional Flow)\n\n```mermaid\nflowchart TD\n    A[Transaction T1 Initiated] --> B[Growing Phase Begins]\n    B --> C{Acquire Lock Needed}\n    C -->|Lock Available| D[Lock Acquired Successfully]\n    C -->|Lock Held by Other Txn| E[Transaction WAITS in Lock Queue]\n    E --> C\n    D --> F{More Operations Pending?}\n    F -->|Yes| C\n    F -->|No - Last Lock Acquired| G[LOCK POINT Reached]\n    G --> H[Shrinking Phase Begins]\n    H --> I[Release Locks One by One]\n    I --> J[All Locks Released]\n    J --> K[Commit or Abort T1]\n    K --> L[Transaction T1 Completed]\n```\n\n### 4.2 Timestamp Ordering Decision Flow (Block-Level Functional Flow)\n\n```mermaid\nflowchart TD\n    OP[Transaction Ti issues READ or WRITE on X] --> CHK{Operation Type?}\n    CHK -->|READ| RCHK{TS Ti greater than or equal to W-TS X?}\n    CHK -->|WRITE| WCHK{TS Ti greater than or equal to R-TS X?}\n    RCHK -->|Yes| ROK[Allow Read; Update R-TS X to TS Ti]\n    RCHK -->|No| RFAIL[ABORT Ti; Restart with New Timestamp]\n    WCHK -->|Yes| WCHK2{TS Ti greater than or equal to W-TS X?}\n    WCHK -->|No| WFAIL[ABORT Ti; Restart with New Timestamp]\n    WCHK2 -->|Yes| WOK[Allow Write; Update W-TS X to TS Ti]\n    WCHK2 -->|No| WTH[Thomas Write Rule Applies]\n    WTH -->|Condition Met| WIGNORE[Silently Ignore the Write]\n    WTH -->|Condition Not Met| WFAIL\n    ROK --> CONT[Continue Execution]\n    WOK --> CONT\n    WIGNORE --> CONT\n    RFAIL --> RESTART[Restart Transaction]\n    WFAIL --> RESTART\n```\n\n### 4.3 Lock Compatibility State Diagram\n\n```mermaid\nstateDiagram-v2\n    [*] --> NoLock : Data item initially free\n    NoLock --> SLocked : T1 acquires S-Lock\n    NoLock --> XLocked : T1 acquires X-Lock\n    SLocked --> SLocked : T2 requests S-Lock (granted)\n    SLocked --> XLocked : T1 upgrades to X-Lock\n    SLocked --> [*] : T1 releases S-Lock\n    XLocked --> [*] : T1 releases X-Lock\n    XLocked --> BlockedT2 : T2 requests S or X Lock\n    BlockedT2 --> SLocked : T1 releases; T2 granted S-Lock\n    BlockedT2 --> XLocked : T1 releases; T2 granted X-Lock\n```\n\n### 4.4 Deadlock Wait-For Graph Example\n\n```mermaid\nflowchart LR\n    TX1((T1)) -->|waits for X| TX2((T2))\n    TX2 -->|waits for Y| TX3((T3))\n    TX3 -->|waits for Z| TX4((T4))\n    TX4 -->|waits for X| TX1\n    DETECT{Cycle Detected?}:::warn -->|YES| ABORT[Select Victim and Abort]\n    DETECT -->|NO| SAFE[Safe - Continue]\n    classDef warn fill:#fff3cd,stroke:#856404\n```\n\n### 4.5 Protocol Comparison Block Diagram\n\n```mermaid\nflowchart TD\n    CC[Concurrency Control Techniques] --> L[Lock-Based Protocols]\n    CC --> T[Timestamp-Based Protocols]\n    CC --> O[Optimistic / Validation-Based]\n    CC --> M[Multiversion Concurrency Control]\n    L --> L1[Basic 2PL]\n    L --> L2[Conservative 2PL]\n    L --> L3[Strict 2PL]\n    L --> L4[Rigorous 2PL]\n    T --> T1[Basic TSO]\n    T --> T2[Strict TSO]\n    T --> T3[Thomas Write Rule]\n    O --> O1[Read Phase]\n    O --> O2[Validation Phase]\n    O --> O3[Write Phase]\n    M --> M1[MV-2PL]\n    M --> M2[MV-TSO]\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## SECTION 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n### 5.1 PART A — Short Answer Questions (3 Marks Each)\n\n**Q1. [KTU University Exam — May 2024 Model]** What is **Two-Phase Locking (2PL)**? Explain the difference between the **Growing Phase** and the **Shrinking Phase**.\n\n**Model Answer:**\nTwo-Phase Locking is a concurrency control protocol that guarantees conflict-serializability. A transaction following 2PL must divide its execution into two successive phases:\n- **Growing Phase:** The transaction may acquire new locks on data items but **cannot release any lock**.\n- **Shrinking Phase:** The transaction may release locks but **cannot acquire any new lock**.\nThe instant of transition between the two phases is called the **Lock Point**. Once a lock is released, no new lock can be obtained, which forces a serial-like ordering of transactions.\n\n---\n\n**Q2. [KTU University Exam — Dec 2023 Model]** Distinguish between **Shared (S) Lock** and **Exclusive (X) Lock** with a compatibility matrix.\n\n**Model Answer:**\n- A **Shared Lock (S-Lock)** is acquired by a transaction that wants to **read** a data item. Multiple transactions can simultaneously hold S-locks on the same item.\n- An **Exclusive Lock (X-Lock)** is acquired by a transaction that wants to **write** a data item. Only one transaction may hold an X-lock, and no S-lock may coexist with it.\n\n| Requested ↓ / Held → | **No Lock** | **S-Lock** | **X-Lock** |\n|---|---|---|---|\n| **S-Lock** | Granted | Granted | Blocked |\n| **X-Lock** | Granted | Blocked | Blocked |\n\n---\n\n### 5.2 PART B — Descriptive Questions (14 Marks, Internal Choice)\n\n---\n\n#### **Question A [14 Marks]** — Locking-Based Protocols\n\n**[KTU University Exam — Model Paper Pattern]**\n\n**(a)** Explain the **Two-Phase Locking (2PL)** protocol. Discuss the limitations of basic 2PL and how **Strict 2PL** and **Conservative 2PL** overcome them. **(7 Marks)**\n\n**(b)** Consider the following schedule $S$ of three transactions. Determine whether the schedule is **2PL-compliant**. If not, modify it to make it 2PL-compliant. **(7 Marks)**\n\n$$S:\\ r_1(A),\\ r_2(B),\\ w_1(A),\\ r_3(A),\\ w_2(B),\\ w_3(A),\\ c_1,\\ c_2,\\ c_3$$\n\n---\n\n**Model Solution:**\n\n**(a) Explanation of 2PL and its variants:**\n\nThe **Two-Phase Locking Protocol** requires every transaction to issue lock and unlock requests in two distinct phases:\n1. **Growing Phase:** Only `lock-S` and `lock-X` operations are allowed.\n2. **Shrinking Phase:** Only `unlock` operations are allowed.\n\n**Lock Point** = moment when the last lock is acquired (transition point).\n\n**Limitations of Basic 2PL:**\n- Does not prevent **cascading rollbacks** (a problem where a transaction reads data written by an uncommitted transaction, and then both must roll back).\n- Does not guarantee **recoverability**.\n- Suffers from **deadlocks**.\n\n**Conservative 2PL (Pre-declaration 2PL):**\n- Transaction acquires **ALL** required locks **before** it begins executing.\n- **Advantage:** Deadlock-free.\n- **Disadvantage:** Often impractical; lock-acquisition may be impossible in advance.\n\n**Strict 2PL:**\n- All **exclusive (X) locks are held until the transaction commits or aborts**.\n- **Advantage:** Guarantees both serializability and recoverability; eliminates cascading rollbacks.\n- **Disadvantage:** Reduced concurrency; may still suffer deadlocks.\n\n**Rigorous 2PL:**\n- Both S and X locks are held until commit/abort.\n- **Advantage:** Produces schedules whose serial order is the commit order.\n\n---\n\n**(b) 2PL Analysis of the given schedule:**\n\nOriginal operations:\n$$S:\\ r_1(A),\\ r_2(B),\\ w_1(A),\\ r_3(A),\\ w_2(B),\\ w_3(A),\\ c_1,\\ c_2,\\ c_3$$\n\nLet us annotate this with explicit lock and unlock operations following 2PL rules.\n\n**Augmented schedule $S'$:**\n\n$$\\begin{aligned}\nS' :\\ & \\text{lock-S}_1(A),\\ r_1(A), \\\\\n     & \\text{lock-S}_2(B),\\ r_2(B), \\\\\n     & \\text{lock-X}_1(A),\\ w_1(A), \\\\\n     & \\text{lock-S}_3(A),\\ r_3(A), \\\\\n     & \\text{lock-X}_2(B),\\ w_2(B), \\\\\n     & \\text{lock-X}_3(A),\\ w_3(A), \\\\\n     & \\text{unlock}_1(A),\\ c_1, \\\\\n     & \\text{unlock}_2(B),\\ c_2, \\\\\n     & \\text{unlock}_3(A),\\ c_3\n\\end{aligned}$$\n\n**Verification for each transaction:**\n\n| Transaction | Locks Acquired (in order) | Unlocks Begin After | 2PL? |\n|---|---|---|---|\n| T1 | S(A), X(A) | Both acquired before unlock | ✅ Yes |\n| T2 | S(B), X(B) | Both acquired before unlock | ✅ Yes |\n| T3 | S(A), X(A) | Both acquired before unlock | ✅ Yes |\n\n**Conclusion:** The schedule $S$ **is 2PL-compliant** as no transaction releases a lock and then acquires a new one. It is therefore **conflict-serializable**.\n\n---\n\n#### **Question B [14 Marks]** — Timestamp-Based Protocols\n\n**[KTU University Exam — Model Paper Pattern]**\n\n**(a)** Explain the **Timestamp Ordering (TSO)** algorithm for concurrency control. Describe how the **Read Timestamp** and **Write Timestamp** of a data item are maintained and used. **(7 Marks)**\n\n**(b)** Consider the following schedule with timestamps $TS(T_1)=10$, $TS(T_2)=20$, $TS(T_3)=15$. Apply **Basic TSO** and determine the order of operations. Identify any transactions that need to be **aborted and restarted**. Use the initial values: $R\\text{-}TS(X)=R\\text{-}TS(Y)=0$, $W\\text{-}TS(X)=W\\text{-}TS(Y)=0$. **(7 Marks)**\n\n$$S:\\ r_1(X),\\ w_2(X),\\ r_3(Y),\\ w_3(X),\\ w_1(Y),\\ r_2(Y)$$\n\n---\n\n**Model Solution:**\n\n**(a) Explanation of TSO Algorithm:**\n\nEach transaction $T_i$ is assigned a **unique timestamp** $TS(T_i)$ at the start. Two timestamps are maintained per data item $X$:\n- **W-TS(X):** Highest timestamp of any transaction that has written $X$.\n- **R-TS(X):** Highest timestamp of any transaction that has read $X$.\n\n**Rules for TSO:**\n\n| Operation | Condition for ALLOW | Action if Fails |\n|---|---|---|\n| `READ(X)` by $T_i$ | $TS(T_i) \\ge W\\text{-}TS(X)$ | Abort $T_i$, restart with new TS |\n| `WRITE(X)` by $T_i$ | $TS(T_i) \\ge W\\text{-}TS(X)$ **and** $TS(T_i) \\ge R\\text{-}TS(X)$ | Abort $T_i$, restart with new TS |\n\n**On success:**\n- For `READ(X)`: set $R\\text{-}TS(X) = \\max(R\\text{-}TS(X), TS(T_i))$.\n- For `WRITE(X)`: set $W\\text{-}TS(X) = TS(T_i)$.\n\n**Key Property:** TSO guarantees that the resulting schedule is **conflict-serializable** in the order of timestamps.\n\n---\n\n**(b) Step-by-step TSO Evaluation:**\n\n| Step | Operation | $TS(T_i)$ | Compare With | Decision | Update |\n|---|---|---|---|---|---|\n| 1 | $r_1(X)$ | 10 | $W\\text{-}TS(X)=0$ | $10 \\ge 0$ ✅ Allowed | $R\\text{-}TS(X) = 10$ |\n| 2 | $w_2(X)$ | 20 | $R\\text{-}TS(X)=10,\\ W\\text{-}TS(X)=0$ | $20 \\ge 10$ ✅, $20 \\ge 0$ ✅ Allowed | $W\\text{-}TS(X) = 20$ |\n| 3 | $r_3(Y)$ | 15 | $W\\text{-}TS(Y)=0$ | $15 \\ge 0$ ✅ Allowed | $R\\text{-}TS(Y) = 15$ |\n| 4 | $w_3(X)$ | 15 | $R\\text{-}TS(X)=10,\\ W\\text{-}TS(X)=20$ | $15 \\ge 10$ ✅, $15 \\ge 20$ ❌ **ABORT T3** | T3 restarts with new TS |\n| 5 | $w_1(Y)$ | 10 | $R\\text{-}TS(Y)=15,\\ W\\text{-}TS(Y)=0$ | $10 \\ge 15$ ❌ **ABORT T1** | T1 restarts with new TS |\n| 6 | $r_2(Y)$ | 20 | $W\\text{-}TS(Y)=0$ | $20 \\ge 0$ ✅ Allowed | $R\\text{-}TS(Y) = 20$ |\n\n**Result:**\n- T3 is **aborted** at step 4 (its write would overwrite a newer write by T2).\n- T1 is **aborted** at step 5 (its write would conflict with a read by T3).\n- After restart with new timestamps, both may proceed.\n\n---\n\n### 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mark-Deduction Pitfalls in Concurrency Control Questions:**\n> 1. **Confusing 2PL with Strict 2PL:** Students often write \"2PL guarantees recoverability\" — this is **WRONG**. Only **Strict 2PL** guarantees recoverability. **Basic 2PL only guarantees conflict-serializability.**\n> 2. **Forgetting Lock Upgrade:** When a transaction does `READ` then `WRITE` on the same item, students must show a `lock-S` followed by a `lock-X` (upgrade), not a fresh lock.\n> 3. **Mixing up R-TS and W-TS updates:** In TSO, after a **READ**, only R-TS is updated (W-TS is **not** touched). After a **WRITE**, only W-TS is updated.\n> 4. **Thomas' Write Rule misapplication:** Thomas' rule is **only** applied to **WRITE** operations that fail because of $W\\text{-}TS$, **not** because of $R\\text{-}TS$.\n> 5. **Forgetting cycle detection in Deadlock:** Always draw a **Wait-For Graph** to show the deadlock cycle. Verbal description alone is insufficient for full marks.\n> 6. **Not stating the Lock Point explicitly:** The Lock Point is the **last lock acquired**, not the first lock released.\n\n---\n\n### 5.4 Topic Recap & Important Things to Remember (Rapid Revision Checklist)\n\n- ✅ **Concurrency Control** ensures that concurrent transactions leave the database in a **consistent state** equivalent to some serial execution.\n- ✅ The **three main problems** of uncontrolled concurrency are: **Lost Update, Dirty Read, Unrepeatable Read**.\n- ✅ **Locks** are variables associated with data items; the two basic types are **S-Lock (Shared/Read)** and **X-Lock (Exclusive/Write)**.\n- ✅ **S-S = compatible**, **S-X = incompatible**, **X-X = incompatible** — memorize the compatibility matrix.\n- ✅ **Two-Phase Locking (2PL)** has a **Growing Phase** (locks acquired) and a **Shrinking Phase** (locks released), separated by the **Lock Point**.\n- ✅ **Basic 2PL** → conflict-serializable, but **not** recoverable. **Strict 2PL** → both serializable and recoverable.\n- ✅ **Conservative 2PL** is deadlock-free but impractical. **Rigorous 2PL** has the strongest guarantees.\n- ✅ **Timestamp Ordering (TSO)** assigns each transaction a unique timestamp; operations are allowed only if they respect the **R-TS** and **W-TS** of the data item.\n- ✅ In TSO: **`READ` allowed** if $TS(T_i) \\ge W\\text{-}TS(X)$; **`WRITE` allowed** if $TS(T_i) \\ge R\\text{-}TS(X)$ **and** $TS(T_i) \\ge W\\text{-}TS(X)$.\n- ✅ **Thomas' Write Rule** is an optimization for TSO: ignore (not abort) a late `WRITE` if it would overwrite a more recent value but is *not* read by anyone newer.\n- ✅ **Deadlock** requires **four Coffman conditions**: Mutual Exclusion, Hold & Wait, No Preemption, Circular Wait.\n- ✅ **Deadlock Handling Strategies:** Prevention, Detection (Wait-For Graph), Avoidance (Wait-Die / Wound-Wait), Recovery (Victim Selection).\n- ✅ A schedule is **conflict-serializable** iff its **Precedence Graph has no cycles**.\n- ✅ **2PL is a sufficient but not necessary** condition for conflict-serializability.\n- ✅ Commercial systems (Oracle, MySQL InnoDB, PostgreSQL) primarily use **Variants of 2PL** for concurrency control.\n- ✅ **Multiversion CC** and **Optimistic (Validation) CC** are alternative techniques where reads do not block writes and vice versa.\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "backupandrecoverystrategiesfordatabasebackupandrecoveryincaseoffailures": {
      "title": "Backup and Recovery: Strategies for database backup and recovery in case of failures",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 3\n> - **Topic:** Backup and Recovery: Strategies for database backup and recovery in case of failures\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\n**Database Backup and Recovery** refers to the set of procedures, strategies, and protocols implemented by a Database Management System (DBMS) to protect data from loss, corruption, or inconsistency arising from various types of failures, and to restore the database to a consistent and correct state after such failures occur.\n\nAccording to the KTU 2024 DBMS syllabus, backup and recovery mechanisms form the cornerstone of the **Transaction Support** and **Concurrency Control** sub-modules, ensuring the **ACID properties** — particularly **Durability** and **Consistency** — are preserved under all operational conditions.\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight (Module 3):**\n> The 2024 scheme mandates that students must understand:\n> 1. Different **types of failures** (transaction, system, media, disaster).\n> 2. **Backup strategies** (full, incremental, differential).\n> 3. **Recovery techniques** (log-based recovery, shadow paging, ARIES).\n> 4. The **Write-Ahead Log (WAL)** protocol.\n> 5. **Checkpointing** mechanisms.\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine you are a **scribbling journalist** writing a long, multi-page investigative report on your laptop.\n\n- **Backup** is like photocopying your entire report at regular intervals (daily, weekly). If your laptop is stolen, you can retrieve the most recent photocopy.\n- **Recovery** is the process of restoring from the latest photocopy *and* rewriting any missing pages using your **scribble notes** (transaction logs) that you kept side-by-side.\n- The **Write-Ahead Log (WAL)** protocol is your personal rule: *\"Always write your scribble notes BEFORE writing on the actual report page.\"* This ensures that if the laptop crashes mid-paragraph, you can reconstruct exactly what you were typing.\n- **Checkpoints** are the moments when you stop, photocopy the current report, and mark your scribble notebook with a \"✓\" — saying \"everything before this point is safely saved.\"\n\nIn DBMS terms, the *photocopy* = **backup**, the *scribble notes* = **log file**, and the *✓ moment* = **checkpoint**.\n\n## 1.3 Why is Backup and Recovery Important?\n\n> [!NOTE]\n> **Core Definition — Database Failure:**\n> A database failure is any event that causes the loss or inaccessibility of data or disrupts the normal operation of the DBMS. The fundamental goal of recovery is to ensure that **no committed transaction is lost** and **no uncommitted transaction's effects persist** in the database.\n\n**Key Engineering Metrics in Recovery (industry-standard benchmarks):**\n- **Recovery Time Objective (RTO)**: Maximum acceptable downtime, typically measured in **minutes** (e.g., 15 minutes for critical systems).\n- **Recovery Point Objective (RPO)**: Maximum acceptable data loss, typically measured in **minutes** (e.g., 5 minutes of data loss tolerable).\n- **Mean Time To Recovery (MTTR)**: Average time to restore service after a failure.\n- **Mean Time Between Failures (MTBF)**: Average time interval between consecutive system failures.\n- **Log Retention Period**: Standard industry value is **30 days to 7 years** depending on compliance (e.g., GDPR, HIPAA).\n\n## 1.4 Types of Failures in DBMS\n\nA robust KTU-level understanding requires classification of all possible failures. Below is the canonical failure taxonomy used in KTU board examinations:\n\n| Failure Type | Cause | Impact Scope | Recovery Approach |\n|--------------|-------|--------------|-------------------|\n| **Transaction Failure** | Logical error (bad input), system error (deadlock) | Single transaction | Undo the transaction |\n| **System Failure (Crash)** | Power outage, OS crash, software bug | All in-flight transactions, DB intact on disk | Restart + recovery from log |\n| **Media Failure** | Disk head crash, bad sectors, controller failure | Portion or all of disk | Restore from backup + redo from log |\n| **Natural Disaster** | Fire, flood, earthquake | Entire data center | Disaster recovery site + archived backup |\n| **Human Error** | Accidental DELETE/UPDATE, wrong script execution | Variable | Point-in-time recovery (PITR) |\n| **Network Failure** | Connectivity loss in distributed DB | Remote nodes | Distributed recovery protocol |\n\n> [!IMPORTANT]\n> **KTU Board Exam Tip:**\n> Examiners frequently ask: *\"Classify the different types of failures that can occur in a DBMS.\"* Always present this as a **3-tier classification** (Transaction → System → Media/Disaster) for full marks.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Storage Hierarchy and Its Role in Recovery\n\nTo understand recovery, you must first understand **where** data resides. The DBMS uses a **memory hierarchy** with vastly different speeds and volatility:\n\n```\nFastest, Volatile (RAM)\n   ↓\n   Database Buffer Pool (Main Memory Cache)\n   ↓\n   Log Buffer (WAL buffer)\n   ↓\nSlowest, Non-Volatile (Disk)\n   ↓\n   Database Files (.dbf, .mdf, .ibd)\n   ↓\n   Log Files (Write-Ahead Log on disk)\n   ↓\n   Archived Logs / Backup Tapes\n```\n\n**Key Principle:** When the system crashes, everything in **RAM is lost**, but everything on **disk survives** (unless it's a media failure).\n\n## 2.2 Write-Ahead Log (WAL) Protocol — The Golden Rule\n\n> [!IMPORTANT]\n> **The Write-Ahead Log (WAL) Rule:**\n> Before any modification is made to a database item on disk, the **log record** describing that modification **MUST** be written to stable (non-volatile) storage.\n> \n> Formally: *A log record for transaction T must be flushed to disk BEFORE T modifies any database element on disk.*\n\n**Why is WAL necessary?**\nWithout WAL, a crash could leave the database modified but the log missing the record of that modification — making recovery impossible.\n\n## 2.3 Log Record Structure\n\nEach log entry contains standardized fields used by every major DBMS:\n\n$$\n\\text{LogRecord} = \\langle \\text{LSN}, \\text{TID}, \\text{PageID}, \\text{OldValue}, \\text{NewValue}, \\text{PrevLSN} \\rangle\n$$\n\nWhere:\n- **LSN (Log Sequence Number)**: Unique monotonically increasing identifier for each log record.\n- **TID (Transaction ID)**: Identifier of the transaction that wrote the log record.\n- **PageID**: Identifier of the affected database page.\n- **OldValue** and **NewValue**: The before-image and after-image of the change.\n- **PrevLSN**: Pointer to the previous log record of the same transaction (used for fast undo traversal).\n\n## 2.4 Types of Log Records (KTU High-Yield)\n\n| Log Record Type | Format | Purpose |\n|-----------------|--------|---------|\n| **Update Log** | `<TID, X, OldVal, NewVal>` | Records changes to data items |\n| **Commit Log** | `<TID, COMMIT>` | Marks transaction as committed |\n| **Abort Log** | `<TID, ABORT>` | Marks transaction as aborted |\n| **Begin Log** | `<TID, BEGIN>` | Marks start of transaction |\n| **Checkpoint Log** | `<Checkpoint, ActiveTIDs>` | Snapshot of active transactions |\n| **CLR (Compensating Log Record)** | `<TID, X, NewVal, PrevLSN, UndoNextLSN>` | Used in ARIES for redoing undo operations |\n| **End Log** | `<TID, END>` | Marks transaction completion (post-recovery) |\n\n## 2.5 Checkpointing Mechanism\n\nA **checkpoint** is a synchronization point in the log that establishes a known good state. During checkpointing:\n\n1. The DBMS halts new transaction starts temporarily.\n2. All log records in the main memory buffer are flushed to disk.\n3. A `<CHECKPOINT>` record is written to the log, listing all currently active transactions.\n4. Modified database buffers are forced to disk (optional, depending on policy).\n5. The `<CHECKPOINT>` record is flushed to disk.\n\n**Types of Checkpoints:**\n- **Consistent (Fuzzy) Checkpoint**: ARIES-style, allows transactions to continue during checkpointing.\n- **Sharp Checkpoint**: Blocks all activity until checkpoint completes.\n- **Incremental Checkpoint**: Only dirty pages are written, reducing I/O overhead.\n\n## 2.6 Recovery Strategies Overview (KTU Formula Sheet)\n\n| Strategy | Description | When Used | Pros | Cons |\n|----------|-------------|-----------|------|------|\n| **Deferred Update** | All updates written to log first, applied to DB only after commit | Crash recovery | Simpler, no undo needed | Cannot provide updated data before commit |\n| **Immediate Update** | Updates can be written to DB before commit | High-performance systems | Better response time | Requires both undo and redo |\n| **Steal/No-Steal** | Whether updated pages can be written to disk before commit | Buffer management policy | Affects recovery complexity | Steal is more flexible |\n| **Force/No-Force** | Whether all updated pages forced to disk at commit | Durability guarantee | Force ensures durability | Force is expensive I/O-wise |\n\n> [!NOTE]\n> **KTU Standard:** Most modern DBMS (Oracle, PostgreSQL, MySQL InnoDB) use **Steal / No-Force** policy with **ARIES** recovery algorithm.\n\n## 2.7 The Defer vs. Immediate Update Trade-off\n\n**Deferred Update (No Undo, Redo Only):**\n- All updates first recorded in log.\n- Database updates deferred until transaction's partial commit point.\n- On recovery: **Redo** all committed transactions; ignore uncommitted.\n\n**Immediate Update (Undo + Redo):**\n- Updates can be applied to database before commit.\n- On recovery: **Undo** all uncommitted transactions; **Redo** all committed transactions.\n\n## 2.8 KTU Formula Sheet / Cheat Sheet\n\n| Concept | Formula / Property | Units / Notes |\n|---------|---------------------|---------------|\n| **Recovery Cost** | $C_{recovery} = T_{restore} + T_{redo} + T_{undo}$ | Seconds |\n| **Redo Operation** | $\\text{REDO}(T): X_i \\leftarrow V_{new}$ | Sets value to new |\n| **Undo Operation** | $\\text{UNDO}(T): X_i \\leftarrow V_{old}$ | Restores old value |\n| **WAL Constraint** | $\\text{LSN}_{log} < \\text{LSN}_{page} \\implies \\text{Block}$ | Log must precede |\n| **Checkpoint Frequency** | $f_{cp} = \\frac{1}{T_{cp\\_time}}$ | Per unit time |\n| **Backup Size (Incremental)** | $S_{inc} = \\sum_{i=1}^{n} S_{changed_i}$ | Sum of changes |\n| **Backup Size (Full)** | $S_{full} = S_{database}$ | Entire DB |\n| **Backup Size (Differential)** | $S_{diff}(t) = \\sum_{i=\\text{last\\_full}}^{t} S_{changed_i}$ | Since last full |\n| **MTTR** | $\\text{MTTR} = \\frac{\\sum T_{downtime}}{N_{failures}}$ | Seconds |\n| **Availability** | $A = \\frac{\\text{MTBF}}{\\text{MTBF} + \\text{MTTR}} \\times 100\\%$ | Percentage |\n\n## 2.9 Real-World Engineering Utility\n\n- **Banking Systems**: Use **immediate update with WAL** to support real-time balance inquiries; recovery must be sub-second to avoid regulatory violations.\n- **E-Commerce Platforms**: Use **incremental backups every 15 minutes** with full nightly backups; RPO typically 15 minutes.\n- **Healthcare Systems**: Use **point-in-time recovery (PITR)** to comply with HIPAA audit requirements; logs retained for **6 years minimum**.\n- **Aerospace & Defense**: Use **synchronous mirroring** to geographically separated disaster recovery sites; RTO measured in seconds.\n- **Cloud Databases (AWS RDS, Azure SQL)**: Use **continuous archiving + point-in-time restore** with automatic log shipping.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation of Recovery Rules (Log-Based Recovery)\n\nThe fundamental recovery rules can be derived logically from the ACID properties:\n\n### 3.1.1 Rule 1: Redo Rule\n\n**Premise:** A transaction T has committed if and only if a `<T, COMMIT>` log record appears on disk.\n\n**Derivation:**\n\n$$\n\\begin{aligned}\n&\\text{Step 1: Definition of Commit} \\\\\n&\\text{Commit}(T) \\iff \\text{LogDisk}(\\langle T, COMMIT \\rangle) \\lor \\text{LogDisk}(\\langle T, END \\rangle) \\\\\n&\\text{Step 2: ACID Durability Property} \\\\\n&\\text{If } T \\text{ committed, then effects must persist} \\\\\n&\\therefore \\text{Recovery} \\Rightarrow \\text{REDO}(T) \\text{ for all committed } T\n\\end{aligned}\n$$\n\n**Conclusion:** During recovery, all committed transactions must be **redone** (their updates reapplied) to ensure durability.\n\n### 3.1.2 Rule 2: Undo Rule\n\n**Premise:** A transaction T is uncommitted if no `<T, COMMIT>` record exists on disk at the time of crash.\n\n**Derivation:**\n\n$$\n\\begin{aligned}\n&\\text{Step 1: Definition of Uncommitted State} \\\\\n&\\neg \\text{Commit}(T) \\iff \\neg \\text{LogDisk}(\\langle T, COMMIT \\rangle) \\\\\n&\\text{Step 2: ACID Atomicity Property} \\\\\n&\\text{If } T \\text{ uncommitted, all partial effects must be rolled back} \\\\\n&\\therefore \\text{Recovery} \\Rightarrow \\text{UNDO}(T) \\text{ for all uncommitted } T\n\\end{aligned}\n$$\n\n**Conclusion:** During recovery, all uncommitted transactions must be **undone** to preserve atomicity.\n\n### 3.1.3 The Combined Recovery Rule\n\nCombining both rules for the general case:\n\n$$\n\\begin{aligned}\n&\\text{For each transaction } T: \\\\\n&\\quad \\text{If } \\langle T, COMMIT \\rangle \\in \\text{LogDisk} : \\text{Apply REDO}(T) \\\\\n&\\quad \\text{If } \\langle T, COMMIT \\rangle \\notin \\text{LogDisk} : \\text{Apply UNDO}(T)\n\\end{aligned}\n$$\n\n## 3.2 Derivation of Checkpoint-Optimized Recovery\n\nWithout checkpointing, recovery would scan the **entire log** from the beginning, leading to a cost proportional to log size. With checkpointing:\n\n$$\n\\begin{aligned}\n&\\text{Step 1: Define recovery cost without checkpointing} \\\\\n&T_{recovery}^{no\\_cp} = O(\\vert \\text{Log} \\vert) \\\\\n&\\text{Step 2: Define recovery cost with checkpointing} \\\\\n&T_{recovery}^{cp} = O(\\vert \\text{Log} \\vert - \\vert \\text{Log}_{\\text{since last cp}} \\vert) \\\\\n&\\text{Step 3: Efficiency gain} \\\\\n&\\eta = \\frac{T_{recovery}^{no\\_cp} - T_{recovery}^{cp}}{T_{recovery}^{no\\_cp}} \\times 100\\% \\\\\n&\\text{Step 4: If checkpoints every } \\tau \\text{ time units} \\\\\n&\\eta \\approx \\frac{\\tau \\cdot f_{transaction}}{T_{log\\_total}} \\times 100\\%\n\\end{aligned}\n$$\n\n**Conclusion:** Frequent checkpoints dramatically reduce recovery time, but each checkpoint itself costs I/O — there is a **trade-off**.\n\n## 3.3 ARIES Recovery Algorithm — Detailed Step-by-Step\n\nThe **ARIES (Algorithm for Recovery and Isolation Exploiting Semantics)** algorithm, developed at IBM, is the industry standard. It uses **Steal / No-Force** policy and consists of **three phases**:\n\n### Phase 1: Analysis Phase\n\n$$\n\\begin{aligned}\n&\\text{Step 1: Identify the start point of the Redo Pass} \\\\\n&\\text{StartLSN} = \\text{LSN of dirty page with smallest RecLSN in Dirty Page Table} \\\\\n&\\text{Step 2: Scan log forward from last successful checkpoint} \\\\\n&\\text{Step 3: For each log record } \\langle L \\rangle: \\\\\n&\\quad \\text{If } L = \\langle T, COMMIT \\rangle : \\text{Move T from ActiveList to CommittedList} \\\\\n&\\quad \\text{If } L = \\langle T, ABORT \\rangle : \\text{Move T from ActiveList to AbortedList} \\\\\n&\\quad \\text{If } L = \\langle T, X, V_{old}, V_{new} \\rangle : \\\\\n&\\quad\\quad \\text{Update DirtyPageTable} \\quad \\text{(mark page containing X as dirty)} \\\\\n&\\quad\\quad \\text{Update TransactionTable \\quad (record T's lastLSN)}\n\\end{aligned}\n$$\n\n### Phase 2: Redo Phase\n\n$$\n\\begin{aligned}\n&\\text{Step 1: Repeat history by redoing all actions} \\\\\n&\\text{Start at } \\text{RedoLSN} = \\text{Min(RecLSN of all dirty pages)} \\\\\n&\\text{Step 2: For each log record } \\langle T, X, V_{old}, V_{new} \\rangle \\text{ from RedoLSN onward:} \\\\\n&\\quad \\text{If page containing X is NOT in DirtyPageTable: skip} \\\\\n&\\quad \\text{If page is in DirtyPageTable but RecLSN > current LSN: skip} \\\\\n&\\quad \\text{Otherwise: } \\\\\n&\\quad\\quad \\text{Read page from disk} \\\\\n&\\quad\\quad \\text{If pageLSN < current LSN: apply REDO (set X = V_{new})} \\\\\n&\\quad\\quad \\text{Set pageLSN = current LSN}\n\\end{aligned}\n$$\n\n### Phase 3: Undo Phase\n\n$$\n\\begin{aligned}\n&\\text{Step 1: Build the \"loser\" set} \\\\\n&\\text{LoserSet} = \\text{Transactions that were active at crash time and not committed} \\\\\n&\\text{Step 2: For each loser transaction T, in reverse LSN order:} \\\\\n&\\quad \\text{Generate CLR (Compensating Log Record)} \\\\\n&\\quad \\text{UNDO(T) by traversing PrevLSN chain} \\\\\n&\\quad \\text{Write CLR to log and flush to disk BEFORE modifying database} \\\\\n&\\text{Step 3: When all losers undone, write } \\langle \\text{T, END} \\rangle \\text{ for each}\n\\end{aligned}\n$$\n\n## 3.4 Shadow Paging Recovery — Step-by-Step\n\n**Shadow paging** is an alternative to log-based recovery that avoids the overhead of maintaining logs:\n\n$$\n\\begin{aligned}\n&\\text{Step 1: Page Table Setup} \\\\\n&\\text{Maintain two page tables: Current PT and Shadow PT} \\\\\n&\\text{Step 2: On Transaction Start} \\\\\n&\\text{CurrentPT} \\leftarrow \\text{ShadowPT} \\\\\n&\\text{Step 3: On Update} \\\\\n&\\text{Create new page (copy-on-write)} \\\\\n&\\text{Update CurrentPT to point to new page} \\\\\n&\\text{Step 4: On Commit} \\\\\n&\\text{Free old pages referenced by ShadowPT} \\\\\n&\\text{Update ShadowPT} \\leftarrow \\text{CurrentPT} \\\\\n&\\text{Step 5: On Crash} \\\\\n&\\text{Restore ShadowPT} \\Rightarrow \\text{Database automatically consistent}\n\\end{aligned}\n$$\n\n**Disadvantage:** Causes **fragmentation** due to scattered page updates; not used in modern high-performance DBMS.\n\n## 3.5 Backup Strategy Cost Analysis (Detailed Derivation)\n\nGiven a database of size $S$ GB with daily change rate $\\delta$:\n\n$$\n\\begin{aligned}\n&\\text{Step 1: Full Backup Size} \\\\\n&S_{full} = S \\\\\n&\\text{Step 2: Incremental Backup Size (per backup)} \\\\\n&S_{inc} = S \\cdot \\delta \\\\\n&\\text{Step 3: Total Storage for Strategy with weekly full + daily incremental (7 days):} \\\\\n&S_{total} = S_{full} + 6 \\cdot S_{inc} + 7 \\cdot S_{inc}^{archive} \\\\\n&\\text{Step 4: Differential Backup Size (day } k \\text{ after full):} \\\\\n&S_{diff}(k) = S \\cdot \\delta \\cdot k \\\\\n&\\text{Step 5: Recovery Time for Differential Strategy} \\\\\n&T_{recover} = T_{restore\\_full} + T_{restore\\_diff} + T_{apply\\_logs}\n\\end{aligned}\n$$\n\n## 3.6 Worked Numerical Example (KTU Board Style)\n\n**Problem:** A bank database has size **1 TB** with a daily change rate of **2%**. The bank uses a full backup every Sunday and incremental backups every day. Calculate the total backup storage per week, and the recovery time if each GB takes 2 seconds to restore.\n\n**Solution:**\n\n$$\n\\begin{aligned}\n&\\text{Given: } S = 1024 \\text{ GB}, \\quad \\delta = 0.02 \\\\\n&\\text{Daily change size} = 1024 \\times 0.02 = 20.48 \\text{ GB} \\\\\n&\\text{Weekly full backup (Sunday)} = 1024 \\text{ GB} \\\\\n&\\text{6 daily incrementals (Mon-Sat)} = 6 \\times 20.48 = 122.88 \\text{ GB} \\\\\n&\\text{Total weekly storage} = 1024 + 122.88 = 1146.88 \\text{ GB} \\\\\n&\\text{Restore time (assume all 7 backups needed)}: \\\\\n&T_{recover} = (1024 + 122.88) \\times 2 = 2293.76 \\text{ seconds} \\\\\n&T_{recover} = 2293.76 / 60 \\approx 38.23 \\text{ minutes}\n\\end{aligned}\n$$\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 DBMS Storage Hierarchy and Recovery Flow\n\n```mermaid\nflowchart TB\n    A[Application Layer - User Transactions] --> B[Transaction Manager]\n    B --> C[Buffer Manager - Main Memory]\n    C --> D[Database Buffer Pool - Volatile RAM]\n    C --> E[Log Buffer - WAL Staging]\n    E --> F[Write Ahead Log File - Stable Disk]\n    D --> G[Database Files - Stable Disk]\n    F --> H[Log Archival - Backup Storage]\n    G --> I[Backup Tapes - Offsite Storage]\n    \n    B --> J[Recovery Manager]\n    J --> K[Checkpoint Manager]\n    K --> F\n    J --> L[Analysis Phase - ARIES]\n    L --> F\n    J --> M[Redo Phase]\n    M --> G\n    J --> N[Undo Phase]\n    N --> G\n    \n    style A fill:#e1f5ff,stroke:#01579b\n    style D fill:#ffe0b2,stroke:#e65100\n    style E fill:#ffe0b2,stroke:#e65100\n    style F fill:#c8e6c9,stroke:#1b5e20\n    style G fill:#c8e6c9,stroke:#1b5e20\n    style H fill:#f8bbd0,stroke:#880e4f\n    style I fill:#f8bbd0,stroke:#880e4f\n    style J fill:#fff59d,stroke:#f57f17\n```\n\n## 4.2 ARIES Three-Phase Recovery Flow\n\n```mermaid\nflowchart LR\n    A[System Crash Detected] --> B[Locate Last Checkpoint in Log]\n    B --> C[Phase 1: ANALYSIS]\n    C --> D[Build Transaction Table]\n    C --> E[Build Dirty Page Table]\n    C --> F[Determine RedoLSN]\n    F --> G[Phase 2: REDO]\n    G --> H[Repeat History]\n    H --> I[Reapply All Updates from RedoLSN]\n    I --> J[Phase 3: UNDO]\n    J --> K[Identify Loser Transactions]\n    K --> L[Rollback Uncommitted Transactions]\n    L --> M[Generate CLR Records]\n    M --> N[Write END Records]\n    N --> O[Database Fully Recovered]\n    \n    style A fill:#ffcdd2,stroke:#b71c1c\n    style C fill:#bbdefb,stroke:#0d47a1\n    style G fill:#c8e6c9,stroke:#1b5e20\n    style J fill:#ffe0b2,stroke:#e65100\n    style O fill:#dcedc8,stroke:#33691e\n```\n\n## 4.3 Write-Ahead Log Protocol Decision Flow\n\n```mermaid\nflowchart TB\n    Start([Transaction Requests Update]) --> Q1{Is Log Record<br>Already on Disk?}\n    Q1 -->|No| Action1[Write Log Record to Stable Storage]\n    Action1 --> Q2{Log Successfully Flushed?}\n    Q1 -->|Yes| Q2\n    Q2 -->|No| Abort[Transaction Must Wait or Abort]\n    Q2 -->|Yes| Action2[Apply Update to Database]\n    Action2 --> Q3{Is this the<br>Commit Point?}\n    Q3 -->|No| Start\n    Q3 -->|Yes| Commit[Write COMMIT Record to Log]\n    Commit --> Flush[Flush All Modified Pages to Disk]\n    Flush --> Done([Transaction Committed Successfully])\n    \n    style Start fill:#c8e6c9,stroke:#1b5e20\n    style Action1 fill:#fff59d,stroke:#f57f17\n    style Action2 fill:#bbdefb,stroke:#0d47a1\n    style Commit fill:#c8e6c9,stroke:#1b5e20\n    style Done fill:#a5d6a7,stroke:#2e7d32\n    style Abort fill:#ffcdd2,stroke:#b71c1c\n```\n\n## 4.4 Backup Strategy Comparison Block Architecture\n\n```mermaid\nflowchart TB\n    subgraph STRATEGY_FULL[Full Backup Strategy]\n        F1[Day 1: Full Backup]\n        F2[Day 2: Full Backup]\n        F3[Day 3: Full Backup]\n        F4[Day 4: Full Backup]\n    end\n    \n    subgraph STRATEGY_INC[Incremental Backup Strategy]\n        I1[Day 1: Full Backup]\n        I2[Day 2: Incremental]\n        I3[Day 3: Incremental]\n        I4[Day 4: Incremental]\n    end\n    \n    subgraph STRATEGY_DIFF[Differential Backup Strategy]\n        D1[Day 1: Full Backup]\n        D2[Day 2: Differential]\n        D3[Day 3: Differential]\n        D4[Day 4: Differential]\n    end\n    \n    F1 --> F2\n    F2 --> F3\n    F3 --> F4\n    \n    I1 --> I2\n    I2 --> I3\n    I3 --> I4\n    \n    D1 --> D2\n    D2 --> D3\n    D3 --> D4\n    \n    style STRATEGY_FULL fill:#e3f2fd,stroke:#0d47a1\n    style STRATEGY_INC fill:#fff3e0,stroke:#e65100\n    style STRATEGY_DIFF fill:#f3e5f5,stroke:#4a148c\n```\n\n## 4.5 Recovery Decision Matrix (Block-Level)\n\n```mermaid\nflowchart TB\n    A[Failure Detected] --> B{Failure Type?}\n    \n    B -->|Type 1| C[Transaction Failure]\n    B -->|Type 2| D[System Crash]\n    B -->|Type 3| E[Media Failure]\n    B -->|Type 4| F[Disaster]\n    \n    C --> C1[UNDO Aborted Transaction]\n    C1 --> C2[Write CLR and END Records]\n    C2 --> C3[Transaction Recovery Complete]\n    \n    D --> D1[Locate Last Checkpoint]\n    D1 --> D2[Run ARIES Analysis Phase]\n    D2 --> D3[Run ARIES Redo Phase]\n    D3 --> D4[Run ARIES Undo Phase]\n    D4 --> D5[System Recovery Complete]\n    \n    E --> E1[Restore Latest Full Backup]\n    E1 --> E2[Apply All Subsequent Incrementals]\n    E2 --> E3[Apply Log Records for REDO]\n    E3 --> E4[Media Recovery Complete]\n    \n    F --> F1[Activate Disaster Recovery Site]\n    F1 --> F2[Restore from Offsite Backup]\n    F2 --> F3[Replicate from DR Site]\n    F3 --> F4[Full Service Restored]\n    \n    style A fill:#ffcdd2,stroke:#b71c1c\n    style C fill:#fff3e0,stroke:#e65100\n    style D fill:#fff3e0,stroke:#e65100\n    style E fill:#fff3e0,stroke:#e65100\n    style F fill:#fff3e0,stroke:#e65100\n    style C3 fill:#c8e6c9,stroke:#1b5e20\n    style D5 fill:#c8e6c9,stroke:#1b5e20\n    style E4 fill:#c8e6c9,stroke:#1b5e20\n    style F4 fill:#c8e6c9,stroke:#1b5e20\n```\n\n## 4.6 Checkpoint Process State Diagram\n\n```mermaid\nstateDiagram-v2\n    [*] --> Normal: System Running\n    Normal --> Checkpointing: Initiate Checkpoint\n    Checkpointing --> Suspending: Halt New Transactions\n    Suspending --> FlushingLog: Write Log Buffer to Disk\n    FlushingLog --> WritingCP: Write CHECKPOINT Record\n    WritingCP --> Resuming: Update Master Record\n    Resuming --> Normal: Resume Normal Operations\n    Normal --> [*]: Graceful Shutdown\n    Normal --> Crashed: System Failure\n    Crashed --> [*]: Trigger Recovery\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n---\n\n### Part A — Question 1 **[KTU University Exam - Dec 2023]**\n\n**Q: Explain the Write-Ahead Log (WAL) protocol. Why is it essential for database recovery?**\n\n**Model Answer (3 Marks — Each Point = 1 Mark):**\n\n**Definition (1 Mark):**\nThe Write-Ahead Log (WAL) protocol is a fundamental rule in DBMS that mandates that **before any modification is made to a database item on disk, the corresponding log record describing that modification must first be written to stable (non-volatile) storage**.\n\n**Working (1 Mark):**\nThe DBMS maintains a **log buffer** in main memory. When a transaction modifies a data item:\n1. The log record containing the old value, new value, transaction ID, and LSN is first written to the log buffer.\n2. The log buffer is **flushed (forced)** to the log file on disk.\n3. **Only after** the log record is safely on disk, the actual database page modification is performed.\n\n**Significance (1 Mark):**\nWAL is essential because:\n- It guarantees that **no committed transaction is ever lost** — even if the system crashes, the log contains the necessary redo information.\n- It ensures **atomicity** — uncommitted transactions can be undone because their log records (with old values) are available.\n- It allows **steal policy** in buffer management, improving performance without compromising recoverability.\n\n---\n\n### Part A — Question 2 **[KTU University Exam - June 2024]**\n\n**Q: Differentiate between Full, Incremental, and Differential backup strategies.**\n\n**Model Answer (3 Marks — Tabular Comparison Format):**\n\n| Parameter | Full Backup | Incremental Backup | Differential Backup |\n|-----------|-------------|--------------------|--------------------|\n| **Definition** | Copies the entire database | Copies only data changed since the **last backup of any type** | Copies only data changed since the **last full backup** |\n| **Backup Size** | Largest (entire DB size) | Smallest (only recent changes) | Grows daily (cumulative changes) |\n| **Backup Speed** | Slowest | Fastest | Moderate |\n| **Recovery Speed** | Fastest (one backup to restore) | Slowest (must apply all backups in sequence) | Moderate (full + last differential) |\n| **Storage Cost** | Highest | Lowest | Moderate |\n| **Use Case** | Weekly/monthly baseline | Frequent (every 15 min–hour) | Daily mid-tier backup |\n| **Example Size (1TB DB, 2% change)** | 1024 GB | ~20 GB/day | 20 GB, 40 GB, 60 GB... |\n\n---\n\n## 5.2 Part B Questions (14 Marks Each — Internal Choice)\n\n> [!NOTE]\n> **KTU 2024 Scheme ESE Pattern:** Each Part B question carries **14 marks**, typically split as **(a) 7 marks + (b) 7 marks**. Students may choose ONE out of TWO alternatives.\n\n---\n\n### PART B — QUESTION A (14 Marks) **[KTU University Exam - Dec 2023]**\n\n**Q: (a) Explain the different types of failures that can occur in a database system. Describe how the system recovers from each type of failure.** **[7 Marks]**\n\n**Model Answer:**\n\n**Introduction (1 Mark):**\nA database failure is any event that disrupts the normal operation of the DBMS, causes data loss, or violates the consistency of the database. Recovery mechanisms aim to restore the database to a consistent state.\n\n**Types of Failures (4 Marks):**\n\n**1. Transaction Failure (Logical/System):**\n- **Causes:** Division by zero, constraint violation, deadlock victim selection.\n- **Recovery:** The DBMS **rolls back** the transaction using log records (undo operations). All updates made by the failed transaction are reversed using old values stored in the log.\n\n**2. System Failure (Software/Hardware Crash):**\n- **Causes:** Power outage, OS crash, DBMS software bug.\n- **Effect:** Main memory is lost; database on disk remains intact.\n- **Recovery:** The DBMS restarts and applies the recovery algorithm (e.g., ARIES):\n   - All uncommitted transactions at crash time → **UNDONE** (atomicity).\n   - All committed transactions whose updates are not on disk → **REDONE** (durability).\n\n**3. Media Failure:**\n- **Causes:** Disk head crash, bad sectors, storage controller failure.\n- **Effect:** Portion of database on disk is corrupted or lost.\n- **Recovery:**\n   - **Restore** the database from the most recent full backup.\n   - **Apply** all subsequent incremental/differential backups.\n   - **Redo** committed transactions using archived log files.\n\n**4. Natural Disaster / Catastrophic Failure:**\n- **Causes:** Fire, flood, earthquake, vandalism.\n- **Recovery:** Activate **disaster recovery site** with replicated data; restore from offsite archived backups.\n\n**5. Human Error:**\n- **Causes:** Accidental DELETE, wrong UPDATE, malicious activity.\n- **Recovery:** **Point-in-time recovery (PITR)** using archived logs to restore to a state before the error.\n\n**Conclusion (2 Marks):**\nModern DBMS implements a layered recovery approach: transaction-level recovery uses local logs; system-level recovery uses checkpoints + ARIES; media-level recovery uses backup + log shipping; disaster-level recovery uses geographic replication. This defense-in-depth ensures that the **ACID properties** are preserved under all failure scenarios.\n\n---\n\n**Q: (b) Describe the ARIES recovery algorithm in detail. Explain its three phases.** **[7 Marks]**\n\n**Model Answer:**\n\n**Introduction (1 Mark):**\n**ARIES (Algorithm for Recovery and Isolation Exploiting Semantics)** is an industry-standard recovery algorithm developed at IBM Research. It uses the **Steal / No-Force** policy, supports **fine-grained locking**, and uses **Log Sequence Numbers (LSNs)** for log record identification. ARIES consists of three sequential phases.\n\n**Phase 1: Analysis Phase (2 Marks):**\n- The system scans the log **forward**, starting from the last successful checkpoint.\n- It builds two key in-memory structures:\n  - **Transaction Table:** Lists all active transactions at crash time, with their lastLSN pointers.\n  - **Dirty Page Table:** Lists all dirty (modified but unflushed) pages in the buffer pool, with their **RecLSN** (oldest LSN that could have modified the page).\n- Determines the **RedoLSN** = minimum RecLSN in the dirty page table, marking the start point for the redo phase.\n\n**Phase 2: Redo Phase (2 Marks):**\n- Starts scanning the log forward from **RedoLSN**.\n- **Repeats history**: re-applies all updates (even those of uncommitted transactions) to bring the database to the exact state it was in at crash time.\n- For each log record:\n  - If the affected page is **not in the Dirty Page Table** → skip (page is clean).\n  - If the page's RecLSN > current LSN → skip (this update was already on disk).\n  - Otherwise → read the page, compare pageLSN, and **REDO** the update.\n- This phase ensures that all committed and uncommitted updates reflect in the database.\n\n**Phase 3: Undo Phase (2 Marks):**\n- Identifies the **Loser Set**: transactions that were active at crash time but did **not** commit.\n- For each loser transaction T:\n  - Traverse the log **backward** using the PrevLSN chain.\n  - Generate **Compensating Log Records (CLRs)** to record the undo actions.\n  - CLRs are written and **flushed to disk** before applying the undo (WAL).\n  - Continue until T is fully rolled back; write an `<T, END>` record.\n- Final state: only committed transactions have their effects in the database.\n\n**Key ARIES Features (Bonus — 0 Marks but valuable):**\n- **WAL compliance** ensures recoverability.\n- **CLRs enable restart-safe undo**: if crash occurs during undo, recovery can resume.\n- **Page-oriented redo** allows efficient I/O.\n\n---\n\n### PART B — QUESTION B (14 Marks) **[KTU University Exam - June 2024 — Alternative Choice]**\n\n**Q: (a) What is a log? Explain the structure of a log record. Describe the different types of log records used in database recovery.** **[7 Marks]**\n\n**Model Answer:**\n\n**Definition of Log (1 Mark):**\nA **log** (or journal) is a sequence of records maintained by the DBMS that chronicles **all modifications** to the database. It is the **primary source of truth** for crash recovery and is stored on stable (non-volatile) storage.\n\n**Structure of a Log Record (2 Marks):**\nA log record typically contains the following fields:\n\n$$\n\\text{LogRecord} = \\langle \\text{LSN}, \\text{T\\_ID}, \\text{Type}, \\text{PageID}, \\text{Data}, \\text{PrevLSN} \\rangle\n$$\n\n- **LSN (Log Sequence Number):** Unique, monotonically increasing identifier.\n- **T_ID:** Transaction identifier.\n- **Type:** Record type (BEGIN, UPDATE, COMMIT, ABORT, CHECKPOINT, END, CLR).\n- **PageID:** Affected database page.\n- **Data:** Old value and new value (for updates).\n- **PrevLSN:** Pointer to the previous log record of the same transaction.\n\n**Types of Log Records (4 Marks):**\n\n**1. `<T, BEGIN>` — Start Record:**\nMarks the beginning of transaction T. Used to identify active transactions at crash time.\n\n**2. `<T, X, V_old, V_new>` — Update Record:**\nRecords a modification to data item X by transaction T, including before-image and after-image.\n\n**3. `<T, COMMIT>` — Commit Record:**\nMarks successful commit of T. The transaction's effects are guaranteed to persist.\n\n**4. `<T, ABORT>` — Abort Record:**\nMarks the abort of T. The DBMS must undo all T's effects.\n\n**5. `<CHECKPOINT, ActiveTransactions>` — Checkpoint Record:**\nSnapshot of all transactions active at the time of checkpoint.\n\n**6. `<T, X, V_new, UndoNextLSN>` — Compensating Log Record (CLR):**\nGenerated during undo operations; allows safe restart of recovery.\n\n**7. `<T, END>` — End Record:**\nMarks final completion of a transaction (used in deferred update and ARIES).\n\n**Example (Bonus):**\nA sample log sequence for transaction T1 updating X from 100 to 200:\n\n```\n<LSN=101, T1, BEGIN>\n<LSN=102, T1, X, V_old=100, V_new=200>\n<LSN=103, T1, COMMIT>\n<LSN=104, T1, END>\n```\n\n---\n\n**Q: (b) Explain the concept of checkpointing. Discuss how checkpointing improves recovery efficiency. Describe the actions performed during checkpointing.** **[7 Marks]**\n\n**Model Answer:**\n\n**Definition (1 Mark):**\nA **checkpoint** is a synchronization point in the log that establishes a known good state of the database. It marks the point up to which all log records have been flushed to stable storage and all dirty pages have been written to disk (in sharp checkpoint).\n\n**Why Checkpointing is Needed (1 Mark):**\nWithout checkpoints, recovery would need to scan the **entire log** from the beginning of time, which is prohibitively expensive for large databases. Checkpoints allow the recovery process to **start from a recent known state**, dramatically reducing recovery time.\n\n**Actions During Checkpointing (3 Marks):**\n\n**Sharp Checkpoint Procedure:**\n1. **Halt** all new transaction starts temporarily.\n2. **Flush** all log records currently in the log buffer to disk.\n3. **Write** a `<CHECKPOINT, ActiveTIDs>` record to the log, listing all currently active transactions.\n4. **Force** all modified (dirty) buffer pages to disk.\n5. **Flush** the checkpoint record itself to disk.\n6. **Resume** normal transaction processing.\n\n**Fuzzy Checkpoint (ARIES-style) Procedure:**\n1. Write a `<BEGIN_CHECKPOINT>` record.\n2. Construct the Transaction Table and Dirty Page Table in memory.\n3. Write a `<END_CHECKPOINT, TT, DPT>` record.\n4. Continue processing transactions during the checkpoint — no halt required.\n\n**How Checkpointing Improves Recovery (2 Marks):**\n\n$$\n\\begin{aligned}\n&\\text{Recovery cost without checkpointing: } O(\\vert \\text{Log} \\vert) \\\\\n&\\text{Recovery cost with checkpointing: } O(\\vert \\text{Log} \\vert - \\vert \\text{Log}_{cp} \\vert) \\\\\n&\\text{Improvement ratio: } \\eta = 1 - \\frac{\\text{Log since last checkpoint}}{\\text{Total log size}}\n\\end{aligned}\n$$\n\n**Benefits:**\n- **Faster recovery**: Only need to redo/undo transactions since last checkpoint.\n- **Reduced log scan time**: Recovery starts from checkpoint LSN, not log beginning.\n- **Lower CPU/IO overhead**: Critical for large OLTP systems.\n- **Better RTO**: Recovery Time Objective improves significantly.\n\n**Trade-off:**\nCheckpoints themselves cost I/O. Frequent checkpoints reduce recovery time but increase steady-state overhead. The optimal checkpoint interval balances:\n$$\nT_{optimal} \\approx \\sqrt{\\frac{2 \\cdot T_{cp} \\cdot N_{tx}}{f_{tx}}}\n$$\n\nWhere $T_{cp}$ is checkpoint cost, $N_{tx}$ is transactions between checkpoints, and $f_{tx}$ is transaction rate.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> \n> 1. **Confusing ARIES phases**: Students often mix up the order. **Mnemonic: \"A.R.U.\" = Analyze, Redo, Undo.** The redo phase ALWAYS precedes the undo phase, even though undo is what we logically think of first.\n> \n> 2. **Missing the WAL guarantee**: When explaining recovery, ALWAYS explicitly state the WAL rule — examiners award 1-2 marks specifically for this.\n> \n> 3. **Confusing incremental vs. differential backups**: Incremental = changes since **last backup of any type**; Differential = changes since **last full backup**. This distinction is worth 1-2 marks.\n> \n> 4. **Forgetting CLR purpose**: In ARIES, Compensating Log Records are NOT just for undo — they are for **restart-safe undo**. If the system crashes during undo, the CLR allows recovery to continue from the correct point.\n> \n> 5. **Not listing active transactions in checkpoint**: The checkpoint record MUST include the list of active transactions. Forgetting this loses marks.\n> \n> 6. **Mixing up REDO and UNDO semantics**: REDO sets value to **new** (after-image); UNDO sets value to **old** (before-image). Students frequently get this wrong.\n> \n> 7. **Skipping the \"Steal/No-Force\" policy mention**: ARIES uses this specific combination. Stating only \"ARIES uses log-based recovery\" loses 1 mark.\n> \n> 8. **Numerical calculation errors**: In backup storage problems, clearly state all given values, conversions (TB to GB), and final units. Always show intermediate steps.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n### 🔑 Quick Revision Checklist\n\n**Core Concepts to Memorize:**\n- [ ] **WAL Protocol**: Log record MUST be on disk before database modification.\n- [ ] **Three ARIES Phases**: Analysis → Redo → Undo (in that order).\n- [ ] **REDO sets NewValue**; **UNDO sets OldValue**.\n- [ ] **Transaction Failure** → local rollback (undo only).\n- [ ] **System Failure** → ARIES algorithm (undo + redo).\n- [ ] **Media Failure** → restore from backup + apply log.\n- [ ] **Disaster Failure** → activate DR site with replicated data.\n\n**Backup Strategy Differences (must-know):**\n- [ ] **Full Backup**: Entire database; slow but fastest recovery.\n- [ ] **Incremental Backup**: Since last backup (any type); smallest size but slowest recovery.\n- [ ] **Differential Backup**: Since last full backup; cumulative size, moderate recovery.\n\n**Log Record Types (must enumerate all 7):**\n- [ ] BEGIN, UPDATE, COMMIT, ABORT, CHECKPOINT, END, CLR.\n\n**Key Formulas to Remember:**\n- [ ] $T_{recovery} = T_{restore} + T_{redo} + T_{undo}$\n- [ ] $A = \\frac{\\text{MTBF}}{\\text{MTBF} + \\text{MTTR}} \\times 100\\%$\n- [ ] Incremental storage: $S_{inc} = S \\cdot \\delta$\n- [ ] Differential storage at day k: $S_{diff}(k) = S \\cdot \\delta \\cdot k$\n\n**ARIES-Specific Facts:**\n- [ ] Uses **Steal / No-Force** buffer management policy.\n- [ ] Uses **LSN (Log Sequence Number)** for ordering.\n- [ ] CLRs enable **restart-safe undo**.\n- [ ] **RecLSN** = oldest LSN that modified a dirty page.\n- [ ] **RedoLSN** = minimum RecLSN across all dirty pages.\n- [ ] **Redo phase repeats history** (reapplies ALL updates).\n- [ ] **Undo phase** only undoes uncommitted (loser) transactions.\n\n**Industry Standard Values:**\n- [ ] RTO for critical systems: **15 minutes** or less.\n- [ ] RPO for financial systems: **near-zero** (synchronous replication).\n- [ ] Log retention: **30 days minimum**, up to 7 years for compliance.\n- [ ] Checkpoint frequency: **every 5-15 minutes** in OLTP systems.\n\n**Shadow Paging (for contrast):**\n- [ ] Uses **current** and **shadow** page tables.\n- [ ] On commit, shadow PT is updated.\n- [ ] On crash, shadow PT points to consistent state.\n- [ ] **Disadvantage**: causes fragmentation, not used in modern high-perf DBMS.\n\n**Common KTU Board Keywords (use these in answers):**\n- [ ] \"ACID properties\"\n- [ ] \"Stable storage\"\n- [ ] \"Atomicity and Durability\"\n- [ ] \"Log-based recovery\"\n- [ ] \"Forward and backward recovery\"\n- [ ] \"Idempotent operations\"\n- [ ] \"Compensating transactions\"\n\n### 📚 Final Exam Strategy Tip\n\n> [!TIP]\n> For 14-mark questions, structure your answer as:\n> 1. **Introduction (1 mark)**: One-sentence definition.\n> 2. **Main Body (10-12 marks)**: 3-5 well-explained points with diagrams.\n> 3. **Conclusion or Real-world application (1-2 marks)**: Practical relevance.\n> 4. **Always include** a **diagram** (mermaid-style or ASCII) for visual clarity — examiners reward it.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "testingandvalidationexecutequeriestovalidatethecorrectnessoftheschemaandconstraints": {
      "title": "Testing and Validation: Execute queries to validate the correctness of the schema and constraints",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 4\n> - **Topic:** Testing and Validation: Execute queries to validate the correctness of the schema and constraints\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Academic Definition\n\n**Schema and Constraint Validation** is the systematic process of executing structured query language (SQL) statements against a populated database instance to verify that the logical and physical schema definitions, integrity rules, business constraints, and data dependencies behave exactly as specified by the conceptual design (ER/EER) and the relational mapping rules. According to the KTU 2024 Scheme DBMS syllabus (PBITT304, Module 4), this phase is the final pre-deployment gate that ensures **Entity Integrity**, **Referential Integrity**, **Domain Integrity**, and **User-Defined Integrity** (CHECK constraints, triggers) are not only declared but also **enforced** correctly by the RDBMS engine.\n\n> [!IMPORTANT]\n> **Core Definition (KTU Board Standard)**\n> *Testing and Validation in DBMS refers to the execution of DML (INSERT, UPDATE, DELETE) and DQL (SELECT) statements to verify that the implemented relational schema correctly enforces all declared constraints (KEY, FOREIGN KEY, UNIQUE, CHECK, NOT NULL, DEFAULT) and correctly preserves the functional dependencies and business rules derived from the original ER model.*\n\n### 1.2 Conceptual Analogy / Intuition\n\nImagine you have designed a **custom-made wooden bookshelf** (your database schema). Before you load it with heavy books (data), you need to **stress-test** it:\n\n- You push against each shelf to check it doesn't wobble (**constraint testing**).\n- You place a book that is too tall to see if the shelf physically rejects it (**CHECK constraint**).\n- You try to remove a critical support leg to ensure the system warns you (**referential integrity**).\n- You place identical books on every shelf to check the weight distribution works uniformly across all tables (**uniform validation across all relations**).\n\nIn a database, the equivalent of \"pushing and pulling\" the bookshelf is running **SQL queries** — INSERT statements, JOINs, aggregate functions, and error-triggering queries — to ensure the schema *behaves correctly* under every realistic and edge-case scenario.\n\n> [!NOTE]\n> **Why this topic matters in KTU exams:**\n> The KTU 2024 Scheme places heavy emphasis on **NEP 2020 Outcome-Based Education (OBE)**. This topic directly tests **CO4 (Apply SQL constructs to validate schema design)**. Expect 7–14 mark questions asking you to write and explain validation queries for a given schema.\n\n### 1.3 Key Metrics and Standards\n\n- **Constraint Coverage Rate (CCR)**: The ratio of validated constraints to total declared constraints. KTU expects **CCR = 1.0 (100%)** before declaring a schema \"tested\".\n- **Test Data Cardinality**: Minimum sample size recommended is **at least 10 rows per table** plus **3–5 boundary/edge-case rows** (NULL attempts, duplicate attempts, invalid FK attempts).\n- **Query Validation Types** — `DML Validation`, `DQL Validation`, `DCL Validation`, `Constraint Violation Testing`.\n- **Integrity Rule Count**: KTU expects students to validate at least **4 canonical integrity types** — *Entity, Referential, Domain, User-defined*.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Cheat Sheet\n\n### 2.1 The Six Pillars of Schema Validation\n\nWhen you test a schema, you must systematically attack it from **six different angles**. Missing any one of these is a guaranteed mark-loss zone in KTU valuations.\n\n#### Pillar 1: Structure Verification (Metadata-Level)\nBefore touching data, verify the schema itself is structurally identical to the ER design.\n\n```sql\n-- Verify all tables exist\nSELECT table_name FROM information_schema.tables \nWHERE table_schema = 'public';\n\n-- Verify column definitions match ER mapping\nSELECT column_name, data_type, is_nullable, column_default\nFROM information_schema.columns\nWHERE table_name = 'STUDENT';\n\n-- Verify all primary keys are declared\nSELECT constraint_name, table_name, constraint_type\nFROM information_schema.table_constraints\nWHERE constraint_type = 'PRIMARY KEY';\n```\n\n**Why this matters**: A missing PRIMARY KEY declaration silently converts your relation into a *bag* (multiset) instead of a *set*, breaking all functional dependency theory. This is a **favourite KTU Part A question** (3 marks).\n\n#### Pillar 2: Entity Integrity Testing\nEvery tuple in a base relation must have a **unique, non-NULL primary key**.\n\n```sql\n-- Test 1: Try to insert NULL primary key (must FAIL)\nINSERT INTO STUDENT (roll_no, name) VALUES (NULL, 'Anu');\n-- Expected: ERROR - null value in column \"roll_no\" violates not-null constraint\n\n-- Test 2: Try to insert duplicate primary key (must FAIL)\nINSERT INTO STUDENT (roll_no, name) VALUES (101, 'Anu');\nINSERT INTO STUDENT (roll_no, name) VALUES (101, 'Manu');\n-- Expected: ERROR - duplicate key value violates unique constraint\n\n-- Test 3: Verify no NULLs exist in PK column\nSELECT COUNT(*) FROM STUDENT WHERE roll_no IS NULL;\n-- Expected Result: 0\n```\n\n#### Pillar 3: Referential Integrity Testing\nEvery foreign key value must either be **NULL** or **match an existing primary key** in the referenced table.\n\n```sql\n-- Test 1: Insert child row with non-existent parent FK (must FAIL)\nINSERT INTO ENROLLMENT (enroll_id, roll_no, course_id) \nVALUES (1, 999, 'CS101');\n-- Expected: ERROR - insert or update on table \"enrollment\" violates \n--           foreign key constraint \"enrollment_roll_no_fkey\"\n\n-- Test 2: Try to delete a parent row that has dependent children\nDELETE FROM STUDENT WHERE roll_no = 101;\n-- Expected (if ON DELETE NO ACTION): ERROR\n-- Expected (if ON DELETE CASCADE): All child rows auto-deleted\n\n-- Test 3: Verify no orphan records\nSELECT e.roll_no FROM ENROLLMENT e\nLEFT JOIN STUDENT s ON e.roll_no = s.roll_no\nWHERE s.roll_no IS NULL;\n-- Expected Result: 0 rows\n```\n\n#### Pillar 4: Domain Integrity Testing\nEvery attribute value must belong to its declared **domain** (data type, format, range).\n\n```sql\n-- Test 1: CHECK constraint violation\nINSERT INTO STUDENT (roll_no, name, age, cgpa) \nVALUES (102, 'Ravi', -5, 9.5);\n-- Expected: ERROR - violates check constraint \"student_age_check\"\n\n-- Test 2: Data type mismatch\nINSERT INTO STUDENT (roll_no, name, age) \nVALUES ('abc', 'Ravi', 20);\n-- Expected: ERROR - invalid input syntax for type integer\n\n-- Test 3: Verify domain boundaries\nSELECT MIN(age), MAX(age), MIN(cgpa), MAX(cgpa) FROM STUDENT;\n-- Cross-check against declared CHECK conditions\n```\n\n#### Pillar 5: User-Defined Constraint & Business Rule Testing\nBusiness logic encoded via `CHECK`, `UNIQUE`, `DEFAULT`, `TRIGGER`.\n\n```sql\n-- Test UNIQUE constraint on non-key attribute\nINSERT INTO STUDENT (roll_no, email) VALUES (101, '[email protected]');\nINSERT INTO STUDENT (roll_no, email) VALUES (102, '[email protected]');\n-- Expected: ERROR - duplicate key value violates unique constraint\n\n-- Test DEFAULT values\nINSERT INTO STUDENT (roll_no, name) VALUES (103, 'Meera');\nSELECT * FROM STUDENT WHERE roll_no = 103;\n-- Verify status column has its default value automatically\n```\n\n#### Pillar 6: Functional Dependency & Anomaly Testing\nVerify that the schema is in the **correct normal form** and the original FDs are preserved.\n\n```sql\n-- Test transitive dependency removal (3NF compliance)\n-- If FD was: course_id -> course_name, instructor\n-- After decomposition, this should hold in COURSE table only\nSELECT course_id, COUNT(DISTINCT course_name) AS distinct_names\nFROM COURSE\nGROUP BY course_id\nHAVING COUNT(DISTINCT course_name) > 1;\n-- Expected: 0 rows (proves no transitive dependency remains)\n\n-- Test join dependency losslessness\nSELECT COUNT(*) FROM (STUDENT NATURAL JOIN ENROLLMENT);\n-- Compare with original STUDENT table row count\n```\n\n### 2.2 The Validation Workflow (Sequential Pipeline)\n\n```\n┌─────────────────────┐\n│  1. Schema Review   │  (Compare CREATE TABLE vs ER Diagram)\n└──────────┬──────────┘\n           ▼\n┌─────────────────────┐\n│ 2. Metadata Verify  │  (information_schema queries)\n└──────────┬──────────┘\n           ▼\n┌─────────────────────┐\n│ 3. Sample Data Load │  (Valid representative rows)\n└──────────┬──────────┘\n           ▼\n┌─────────────────────┐\n│ 4. Negative Testing │  (Invalid data - should FAIL)\n└──────────┬──────────┘\n           ▼\n┌─────────────────────┐\n│ 5. Positive Testing │  (Valid data - should SUCCEED)\n└──────────┬──────────┘\n           ▼\n┌─────────────────────┐\n│ 6. Anomaly Check    │  (NULL, duplicate, orphan detection)\n└──────────┬──────────┘\n           ▼\n┌─────────────────────┐\n│ 7. Report & Fix     │  (Document failures, alter schema)\n└─────────────────────┘\n```\n\n### 2.3 KTU Formula / Concept Cheat Sheet\n\n| **Concept** | **Test Query Type** | **Expected Outcome on Valid Schema** |\n|---|---|---|\n| Entity Integrity | `INSERT ... NULL PK` | **ERROR** raised |\n| Entity Integrity | `INSERT ... duplicate PK` | **ERROR** raised |\n| Referential Integrity | `INSERT ... invalid FK` | **ERROR** raised |\n| Referential Integrity | `DELETE parent with children` (NO ACTION) | **ERROR** raised |\n| Domain Integrity | `INSERT ... out-of-range value` | **ERROR** raised (CHECK) |\n| User Integrity | `INSERT ... duplicate UNIQUE attr` | **ERROR** raised |\n| Default Value | `INSERT ... omit column with DEFAULT` | **Auto-filled** value present |\n| FD Preservation | `GROUP BY ... HAVING COUNT(DISTINCT) > 1` | **0 rows** returned |\n| Lossless Join | `NATURAL JOIN` row count | Equals original table cardinality |\n| Orphan Detection | `LEFT JOIN ... IS NULL` | **0 rows** returned |\n\n> [!NOTE]\n> **Real-world Engineering Utility**: In industry, this validation phase is part of **Database Regression Testing** in CI/CD pipelines. Tools like *DbUnit*, *pgTAP* (PostgreSQL), and *tSQLt* (SQL Server) automate exactly these query patterns. Banking systems (e.g., RBI's CKYC database) run **thousands** of such validation queries nightly to ensure regulatory compliance.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Analytical Matrix & Worked Examples\n\n### 3.1 Comprehensive Worked Example: University Database\n\nLet us validate the following schema end-to-end using the standard KTU reference database (a favourite in past papers).\n\n**Given Schema:**\n\n```sql\nCREATE TABLE DEPARTMENT (\n    dept_id    CHAR(3) PRIMARY KEY,\n    dept_name  VARCHAR(50) UNIQUE NOT NULL,\n    hod        VARCHAR(50) NOT NULL\n);\n\nCREATE TABLE STUDENT (\n    roll_no    INTEGER PRIMARY KEY,\n    name       VARCHAR(50) NOT NULL,\n    age        INTEGER CHECK (age >= 17 AND age <= 30),\n    cgpa       NUMERIC(3,2) CHECK (cgpa >= 0 AND cgpa <= 10),\n    dept_id    CHAR(3),\n    email      VARCHAR(100) UNIQUE,\n    FOREIGN KEY (dept_id) REFERENCES DEPARTMENT(dept_id) \n        ON DELETE SET NULL ON UPDATE CASCADE\n);\n\nCREATE TABLE COURSE (\n    course_id   CHAR(5) PRIMARY KEY,\n    course_name VARCHAR(50) NOT NULL,\n    credits     INTEGER CHECK (credits > 0)\n);\n\nCREATE TABLE ENROLLMENT (\n    roll_no     INTEGER,\n    course_id   CHAR(5),\n    enroll_date DATE DEFAULT CURRENT_DATE,\n    grade       CHAR(1) CHECK (grade IN ('A','B','C','D','F')),\n    PRIMARY KEY (roll_no, course_id),\n    FOREIGN KEY (roll_no) REFERENCES STUDENT(roll_no) ON DELETE CASCADE,\n    FOREIGN KEY (course_id) REFERENCES COURSE(course_id) ON DELETE CASCADE\n);\n```\n\n### 3.2 Step-by-Step Validation Walkthrough\n\n#### Test 1: Verify Structural Integrity (Metadata)\n\n**Step 1.1** — List all tables in the schema.\n\n```sql\nSELECT table_name, table_type \nFROM information_schema.tables\nWHERE table_schema = 'public'\nORDER BY table_name;\n```\n\n**Expected Result Set:**\n\n| table_name | table_type |\n|---|---|\n| course | BASE TABLE |\n| department | BASE TABLE |\n| enrollment | BASE TABLE |\n| student | BASE TABLE |\n\n> If any table is missing, schema is incomplete.\n\n**Step 1.2** — Verify primary keys.\n\n```sql\nSELECT tc.table_name, kc.column_name, tc.constraint_type\nFROM information_schema.table_constraints tc\nJOIN information_schema.key_column_usage kc\n  ON tc.constraint_name = kc.constraint_name\nWHERE tc.constraint_type = 'PRIMARY KEY'\nORDER BY tc.table_name;\n```\n\n**Expected Output:**\n\n| table_name | column_name | constraint_type |\n|---|---|---|\n| course | course_id | PRIMARY KEY |\n| department | dept_id | PRIMARY KEY |\n| enrollment | course_id | PRIMARY KEY |\n| enrollment | roll_no | PRIMARY KEY |\n| student | roll_no | PRIMARY KEY |\n\n#### Test 2: Entity Integrity — Positive Test\n\n```sql\n-- Insert valid students\nINSERT INTO STUDENT VALUES (1, 'Arjun', 20, 8.5, 'CSE', '[email protected]');\nINSERT INTO STUDENT VALUES (2, 'Diya',  21, 9.2, 'CSE', '[email protected]');\nINSERT INTO STUDENT VALUES (3, 'Rahul', 19, 7.8, 'ECE', '[email protected]');\n```\n\n**Verification Query:**\n\n```sql\nSELECT COUNT(*) AS total_students FROM STUDENT;\n-- Expected: 3\n```\n\n#### Test 3: Entity Integrity — Negative Test (Must FAIL)\n\n```sql\n-- Attempt 1: NULL PK\nINSERT INTO STUDENT (roll_no, name) VALUES (NULL, 'Test');\n```\n**System Response:** `ERROR: null value in column \"roll_no\" violates not-null constraint` ✓ **Test PASSED** (constraint works correctly).\n\n```sql\n-- Attempt 2: Duplicate PK\nINSERT INTO STUDENT VALUES (1, 'Duplicate', 20, 8.0, 'CSE', '[email protected]');\n```\n**System Response:** `ERROR: duplicate key value violates unique constraint \"student_pkey\"` ✓ **Test PASSED**.\n\n#### Test 4: Domain Integrity (CHECK Constraint)\n\n```sql\n-- Attempt 3: Age below minimum\nINSERT INTO STUDENT VALUES (4, 'Minor', 12, 7.0, 'CSE', '[email protected]');\n```\n**System Response:** `ERROR: new row for relation \"student\" violates check constraint \"student_age_check\"` ✓ **Test PASSED**.\n\n```sql\n-- Attempt 4: CGPA above maximum\nINSERT INTO STUDENT VALUES (5, 'Genius', 20, 15.0, 'CSE', '[email protected]');\n```\n**System Response:** `ERROR: violates check constraint \"student_cgpa_check\"` ✓ **Test PASSED**.\n\n#### Test 5: Referential Integrity\n\n```sql\n-- Insert parent rows\nINSERT INTO DEPARTMENT VALUES ('CSE', 'Computer Science', 'Dr. Sharma');\nINSERT INTO DEPARTMENT VALUES ('ECE', 'Electronics', 'Dr. Verma');\n\n-- Test 5.1: Insert child with valid FK\nINSERT INTO STUDENT VALUES (6, 'Valid', 20, 8.0, 'CSE', '[email protected]');\n-- ✓ Should SUCCEED\n```\n\n```sql\n-- Test 5.2: Insert child with invalid FK\nINSERT INTO STUDENT VALUES (7, 'Invalid', 20, 8.0, 'MEC', '[email protected]');\n```\n**System Response:** `ERROR: insert or update on table \"student\" violates foreign key constraint \"student_dept_id_fkey\"` ✓ **Test PASSED**.\n\n```sql\n-- Test 5.3: ON DELETE SET NULL behavior\nDELETE FROM DEPARTMENT WHERE dept_id = 'CSE';\n-- Expected: SUCCEEDS, and STUDENT rows with dept_id='CSE' get dept_id=NULL\n\nSELECT roll_no, name, dept_id FROM STUDENT WHERE dept_id IS NULL;\n```\n\n**Verification:**\n\n| roll_no | name | dept_id |\n|---|---|---|\n| 1 | Arjun | NULL |\n| 2 | Diya | NULL |\n| 6 | Valid | NULL |\n\n✓ **Cascade behaviour verified.**\n\n#### Test 6: User-Defined Integrity (UNIQUE on non-key)\n\n```sql\nINSERT INTO STUDENT (roll_no, name, email) VALUES (10, 'A', '[email protected]');\nINSERT INTO STUDENT (roll_no, name, email) VALUES (11, 'B', '[email protected]');\n```\n**Expected Result:** `ERROR: duplicate key value violates unique constraint \"student_email_key\"` ✓ **Test PASSED**.\n\n#### Test 7: Composite Key Test (ENROLLMENT)\n\n```sql\nINSERT INTO COURSE VALUES ('CS101', 'DBMS', 4);\nINSERT INTO COURSE VALUES ('CS102', 'OS', 3);\n\nINSERT INTO ENROLLMENT (roll_no, course_id, grade) VALUES (2, 'CS101', 'A');\nINSERT INTO ENROLLMENT (roll_no, course_id, grade) VALUES (2, 'CS102', 'B');\nINSERT INTO ENROLLMENT (roll_no, course_id, grade) VALUES (3, 'CS101', 'C');\n\n-- Re-attempt the SAME composite key — must FAIL\nINSERT INTO ENROLLMENT (roll_no, course_id, grade) VALUES (2, 'CS101', 'A');\n```\n**System Response:** `ERROR: duplicate key value violates unique constraint \"enrollment_pkey\"` ✓ **Test PASSED**.\n\n#### Test 8: Anomaly Detection (Comprehensive Audit Query)\n\n```sql\n-- 8.1: Find orphan enrollments (referential violation - should be 0)\nSELECT COUNT(*) AS orphan_enrollments\nFROM ENROLLMENT e\nLEFT JOIN STUDENT s ON e.roll_no = s.roll_no\nLEFT JOIN COURSE c ON e.course_id = c.course_id\nWHERE s.roll_no IS NULL OR c.course_id IS NULL;\n\n-- 8.2: Find students in non-existent departments (after SET NULL, this is 0)\nSELECT COUNT(*) AS phantom_students\nFROM STUDENT s\nLEFT JOIN DEPARTMENT d ON s.dept_id = d.dept_id\nWHERE s.dept_id IS NOT NULL AND d.dept_id IS NULL;\n\n-- 8.3: Verify CGPA range\nSELECT MIN(cgpa) AS min_cgpa, MAX(cgpa) AS max_cgpa\nFROM STUDENT;\n-- Both must be within [0, 10]\n```\n\n**All must return 0 rows or values within declared domain.** ✓\n\n#### Test 9: Functional Dependency Validation\n\nFor FD `dept_id → dept_name` (in DEPARTMENT):\n\n```sql\nSELECT dept_id, COUNT(DISTINCT dept_name) AS name_variations\nFROM DEPARTMENT\nGROUP BY dept_id\nHAVING COUNT(DISTINCT dept_name) > 1;\n```\n\n**Expected Result:** 0 rows (proves FD holds in current data).\n\n> If 1+ rows returned, either schema decomposition is incorrect OR data is corrupted. This is a **classic 14-mark KTU question** on validating normalization.\n\n#### Test 10: Lossless Join Verification\n\n```sql\n-- Original count\nSELECT COUNT(*) FROM STUDENT;\n\n-- After joining with ENROLLMENT and back-projecting\nSELECT COUNT(DISTINCT s.roll_no) \nFROM STUDENT s JOIN ENROLLMENT e ON s.roll_no = e.roll_no;\n```\n\nIf schema decomposition is lossless, both counts are **consistent with the 1:M relationship** (joined count $\\leq$ original count, never inflated).\n\n---\n\n### 3.3 Analytical Matrix: Constraint-to-Query Mapping\n\n| **Constraint Type** | **Negative Test Query** | **Positive Test Query** | **Audit Query** |\n|---|---|---|---|\n| `PRIMARY KEY` | `INSERT NULL PK` | `INSERT valid PK` | `SELECT COUNT(*) ... GROUP BY PK HAVING COUNT>1` |\n| `FOREIGN KEY` | `INSERT invalid FK` | `INSERT valid FK` | `LEFT JOIN ... IS NULL` |\n| `UNIQUE` | `INSERT duplicate` | `INSERT distinct` | `SELECT ... GROUP BY ... HAVING COUNT>1` |\n| `NOT NULL` | `INSERT NULL` | `INSERT valid value` | `SELECT COUNT(*) WHERE col IS NULL` |\n| `CHECK` | `INSERT violating value` | `INSERT in-range value` | `SELECT MIN, MAX FROM table` |\n| `DEFAULT` | Omit column | Include explicit value | `SELECT ... WHERE col = default_val` |\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 The Validation Testing Pyramid\n\n```mermaid\ngraph TD\n    schemaP[\"Schema Validation Pyramid\"]\n    \n    layer1[\"Layer 1: Metadata Audit<br/>information_schema queries\"]\n    layer2[\"Layer 2: Constraint Enforcement Tests<br/>PK, FK, UNIQUE, CHECK, NOT NULL\"]\n    layer3[\"Layer 3: Business Rule Tests<br/>Triggers, stored procedures\"]\n    layer4[\"Layer 4: Normalization & FD Tests<br/>Lossless join, dependency preservation\"]\n    layer5[\"Layer 5: Performance Tests<br/>Index usage, query plans\"]\n    layer6[\"Layer 6: Concurrency Tests<br/>Deadlocks, isolation levels\"]\n    \n    schemaP --> layer1\n    layer1 --> layer2\n    layer2 --> layer3\n    layer3 --> layer4\n    layer4 --> layer5\n    layer5 --> layer6\n    \n    style schemaP fill:#1e3a8a,stroke:#000,color:#fff\n    style layer1 fill:#dbeafe,stroke:#1e40af\n    style layer2 fill:#bfdbfe,stroke:#1e40af\n    style layer3 fill:#93c5fd,stroke:#1e40af\n    style layer4 fill:#60a5fa,stroke:#1e40af,color:#fff\n    style layer5 fill:#3b82f6,stroke:#1e40af,color:#fff\n    style layer6 fill:#2563eb,stroke:#1e40af,color:#fff\n```\n\n### 4.2 Test Execution Flowchart\n\n```mermaid\ngraph TD\n    startA[\"Start Validation\"]\n    loadData[\"Load Valid Sample Data<br/>INSERT statements\"]\n    metaCheck{\"Metadata<br/>Matches ER?\"}\n    entityCheck{\"Entity Integrity<br/>Tests Pass?\"}\n    refCheck{\"Referential Integrity<br/>Tests Pass?\"}\n    domainCheck{\"Domain Integrity<br/>Tests Pass?\"}\n    userCheck{\"User Constraints<br/>Tests Pass?\"}\n    fdCheck{\"FD & Normal Form<br/>Tests Pass?\"}\n    report[\"Generate Validation Report<br/>PASS/FAIL Summary\"]\n    fix[\"Fix Schema Bugs<br/>ALTER TABLE\"]\n    \n    startA --> loadData\n    loadData --> metaCheck\n    metaCheck -->|No| fix\n    metaCheck -->|Yes| entityCheck\n    entityCheck -->|No| fix\n    entityCheck -->|Yes| refCheck\n    refCheck -->|No| fix\n    refCheck -->|Yes| domainCheck\n    domainCheck -->|No| fix\n    domainCheck -->|Yes| userCheck\n    userCheck -->|No| fix\n    userCheck -->|Yes| fdCheck\n    fdCheck -->|No| fix\n    fdCheck -->|Yes| report\n    fix --> metaCheck\n    \n    style startA fill:#16a34a,stroke:#000,color:#fff\n    style report fill:#22c55e,stroke:#000,color:#fff\n    style fix fill:#ef4444,stroke:#000,color:#fff\n    style metaCheck fill:#fef3c7,stroke:#000\n    style entityCheck fill:#fef3c7,stroke:#000\n    style refCheck fill:#fef3c7,stroke:#000\n    style domainCheck fill:#fef3c7,stroke:#000\n    style userCheck fill:#fef3c7,stroke:#000\n    style fdCheck fill:#fef3c7,stroke:#000\n```\n\n### 4.3 Constraint Validation Block Architecture\n\n```mermaid\ngraph LR\n    testInput[\"Test Input:<br/>SQL Query / Sample Data\"]\n    constraintEngine[\"RDBMS Constraint Engine\"]\n    \n    pkBlock[\"PK Checker<br/>UNIQUE + NOT NULL\"]\n    fkBlock[\"FK Checker<br/>Parent-Child Match\"]\n    ukBlock[\"UNIQUE Checker<br/>Per non-key attr\"]\n    chkBlock[\"CHECK Checker<br/>Predicate evaluation\"]\n    nnBlock[\"NOT NULL Checker<br/>NULL filter\"]\n    defBlock[\"DEFAULT Inserter<br/>Auto-value provider\"]\n    trigBlock[\"Trigger Executor<br/>Business rule handler\"]\n    \n    resultValid[\"VALID: Row Inserted/Updated\"]\n    resultInvalid[\"INVALID: SQLSTATE error<br/>Constraint name + violation message\"]\n    \n    testInput --> constraintEngine\n    constraintEngine --> pkBlock\n    constraintEngine --> fkBlock\n    constraintEngine --> ukBlock\n    constraintEngine --> chkBlock\n    constraintEngine --> nnBlock\n    constraintEngine --> defBlock\n    constraintEngine --> trigBlock\n    \n    pkBlock --> resultValid\n    pkBlock --> resultInvalid\n    fkBlock --> resultValid\n    fkBlock --> resultInvalid\n    ukBlock --> resultValid\n    ukBlock --> resultInvalid\n    chkBlock --> resultValid\n    chkBlock --> resultInvalid\n    nnBlock --> resultValid\n    nnBlock --> resultInvalid\n    defBlock --> resultValid\n    trigBlock --> resultValid\n    trigBlock --> resultInvalid\n    \n    style testInput fill:#0ea5e9,stroke:#000,color:#fff\n    style constraintEngine fill:#6366f1,stroke:#000,color:#fff\n    style resultValid fill:#22c55e,stroke:#000,color:#fff\n    style resultInvalid fill:#ef4444,stroke:#000,color:#fff\n```\n\n### 4.4 Schema Validation Lifecycle (Block Diagram)\n\n```mermaid\ngraph TD\n    req[\"Step 1: Requirements<br/>ER Diagram + Business Rules\"]\n    rel[\"Step 2: Relational Mapping<br/>Tables + Constraints\"]\n    ddl[\"Step 3: DDL Execution<br/>CREATE TABLE statements\"]\n    meta[\"Step 4: Metadata Verification<br/>information_schema queries\"]\n    pos[\"Step 5: Positive Testing<br/>Valid DML should SUCCEED\"]\n    neg[\"Step 6: Negative Testing<br/>Invalid DML should FAIL\"]\n    aud[\"Step 7: Anomaly Audit<br/>NULL/orphan/duplicate scans\"]\n    perf[\"Step 8: Performance Baseline<br/>EXPLAIN ANALYZE on key queries\"]\n    sign[\"Step 9: Sign-off<br/>Validation Report Generated\"]\n    \n    req --> rel\n    rel --> ddl\n    ddl --> meta\n    meta --> pos\n    pos --> neg\n    neg --> aud\n    aud --> perf\n    perf --> sign\n    \n    sign -.->|If Failed| rel\n    \n    style req fill:#f59e0b,stroke:#000,color:#fff\n    style ddl fill:#0ea5e9,stroke:#000,color:#fff\n    style meta fill:#8b5cf6,stroke:#000,color:#fff\n    style pos fill:#22c55e,stroke:#000,color:#fff\n    style neg fill:#ef4444,stroke:#000,color:#fff\n    style aud fill:#14b8a6,stroke:#000,color:#fff\n    style perf fill:#3b82f6,stroke:#000,color:#fff\n    style sign fill:#16a34a,stroke:#000,color:#fff\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### 5.1 Part A Questions (3 Marks Each)\n\n---\n\n**Q1. [KTU University Exam - Dec 2023]**\n*List any four SQL queries that can be used to validate the **entity integrity constraint** of a relation. Explain what each query tests.*\n\n**Model Answer (3 marks):**\n\nThe following four queries validate entity integrity:\n\n1. **NULL PK Test** — `INSERT INTO Student (roll_no, name) VALUES (NULL, 'X');` — Tests that primary key cannot be NULL. Expected: `ERROR: null value in column \"roll_no\" violates not-null constraint`.\n\n2. **Duplicate PK Test** — Insert two rows with same `roll_no`. Tests that primary key values are unique. Expected: `ERROR: duplicate key value violates unique constraint`.\n\n3. **Audit for existing NULLs** — `SELECT COUNT(*) FROM Student WHERE roll_no IS NULL;` — Should return 0.\n\n4. **Audit for existing duplicates** — `SELECT roll_no, COUNT(*) FROM Student GROUP BY roll_no HAVING COUNT(*) > 1;` — Should return 0 rows.\n\n---\n\n**Q2. [KTU University Exam - May 2024]**\n*What is the difference between **positive testing** and **negative testing** in the context of database schema validation? Give one example query for each.*\n\n**Model Answer (3 marks):**\n\n- **Positive Testing**: Executes *valid* SQL statements to confirm that legitimate operations are accepted by the schema. *Example*: `INSERT INTO Student VALUES (1, 'Anu', 20, 8.5, 'CSE');` should succeed without error.\n\n- **Negative Testing**: Executes *invalid* SQL statements (violating constraints) to confirm that the RDBMS correctly **rejects** them. *Example*: `INSERT INTO Student VALUES (NULL, 'Anu', 20, 8.5, 'CSE');` should fail with a `not-null constraint` error.\n\nTogether, they prove that the schema is neither too permissive (accepting garbage) nor too restrictive (rejecting valid data).\n\n---\n\n### 5.2 Part B Questions (14 Marks Each)\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Pitfall Callout**\n> Common mistakes students make in this topic:\n> 1. **Forgetting to specify the expected outcome** — KTU examiners award marks for stating *what the query should return* in addition to the query itself.\n> 2. **Mixing up CASCADE and SET NULL** — When asked about referential actions, students often write \"cascade\" everywhere. Read the schema's `ON DELETE` clause.\n> 3. **Not validating FDs after normalization** — A schema can satisfy all constraints yet violate 3NF. Always write FD verification queries.\n> 4. **Confusing UNIQUE constraint with PRIMARY KEY** — UNIQUE allows one NULL in standard SQL; PRIMARY KEY does not. Marks lost if this distinction is muddled.\n\n---\n\n#### **Question A (14 Marks)** — *Internal Choice Option 1*\n\n**[KTU University Exam - Model Paper 2024]**\n\nConsider the following schema for a Library Management System:\n\n```sql\nCREATE TABLE AUTHOR (\n    author_id   INTEGER PRIMARY KEY,\n    author_name VARCHAR(50) NOT NULL\n);\n\nCREATE TABLE BOOK (\n    book_id     INTEGER PRIMARY KEY,\n    title       VARCHAR(100) NOT NULL,\n    price       NUMERIC(8,2) CHECK (price > 0),\n    author_id   INTEGER,\n    FOREIGN KEY (author_id) REFERENCES AUTHOR(author_id) ON DELETE CASCADE\n);\n```\n\n**(a)** Write **SQL queries** to validate the following (7 marks):\n1. Entity integrity of BOOK\n2. Referential integrity between BOOK and AUTHOR\n3. Domain integrity of price\n\n**(b)** Suppose the schema was decomposed from an unnormalized form where the original functional dependency was `book_id → title, price, author_id, author_name`. Write queries to **verify that the decomposition is lossless** and that **no transitive dependency remains** in the BOOK relation. (7 marks)\n\n---\n\n**Model Solution for Question A:**\n\n**(a) — Part 1: Entity Integrity of BOOK (2 marks)**\n\n```sql\n-- Test 1: Attempt to insert NULL primary key\nINSERT INTO BOOK (book_id, title, price) VALUES (NULL, 'Test', 100);\n-- Expected: ERROR - null value in column \"book_id\" violates not-null constraint\n\n-- Test 2: Attempt to insert duplicate primary key\nINSERT INTO BOOK VALUES (1, 'DBMS', 500, 101);\nINSERT INTO BOOK VALUES (1, 'OS', 600, 102);\n-- Expected: ERROR - duplicate key value violates unique constraint \"book_pkey\"\n\n-- Test 3: Audit query to detect pre-existing violations\nSELECT book_id, COUNT(*) FROM BOOK \nGROUP BY book_id HAVING COUNT(*) > 1;\n-- Expected: 0 rows\n```\n\n**(a) — Part 2: Referential Integrity (3 marks)**\n\n```sql\n-- Test 1: Insert child with non-existent parent\nINSERT INTO BOOK VALUES (10, 'Networks', 450, 999);\n-- Expected: ERROR - insert or update on table \"book\" violates \n--           foreign key constraint \"book_author_id_fkey\"\n\n-- Test 2: ON DELETE CASCADE behaviour\nINSERT INTO AUTHOR VALUES (101, 'Navathe');\nINSERT INTO BOOK VALUES (1, 'DBMS', 500, 101);\nINSERT INTO BOOK VALUES (2, 'OS', 600, 101);\n\nSELECT COUNT(*) FROM BOOK WHERE author_id = 101;\n-- Expected: 2 (parent and children exist)\n\nDELETE FROM AUTHOR WHERE author_id = 101;\n-- Expected: SUCCEEDS\n\nSELECT COUNT(*) FROM BOOK WHERE author_id = 101;\n-- Expected: 0 (CASCADE deleted children)\n\n-- Test 3: Audit query for orphan records\nSELECT b.book_id FROM BOOK b\nLEFT JOIN AUTHOR a ON b.author_id = a.author_id\nWHERE a.author_id IS NULL;\n-- Expected: 0 rows\n```\n\n**(a) — Part 3: Domain Integrity of price (2 marks)**\n\n```sql\n-- Test 1: Negative price\nINSERT INTO BOOK VALUES (20, 'Invalid', -50, 102);\n-- Expected: ERROR - violates check constraint \"book_price_check\"\n\n-- Test 2: Zero price (boundary)\nINSERT INTO BOOK VALUES (21, 'Free', 0, 102);\n-- Expected: ERROR - violates check constraint (price > 0)\n\n-- Test 3: Audit price range\nSELECT MIN(price), MAX(price), AVG(price) FROM BOOK;\n-- All values must satisfy price > 0\n```\n\n**(b) — Lossless Join & Transitive Dependency Verification (7 marks)**\n\n**Step 1: Lossless Join Test (3.5 marks)**\n\nFor decomposition $\\{BOOK, AUTHOR\\}$ to be lossless with respect to the join attribute `author_id`, the following must hold:\n\n$$|BOOK \\bowtie AUTHOR| = |R_{original}|$$\n\nSince the original table R is no longer present, we verify using the **intersection property**:\n\n$$BOOK \\cap AUTHOR = \\{author\\_id\\}$$\n\nAnd the constraint that `author_id → AUTHOR` (i.e., `author_id` is the primary key of AUTHOR). Since `author_id` is a key in AUTHOR, by the **Heath's Theorem**, the decomposition is lossless.\n\n**Verification Query:**\n\n```sql\n-- Insert sample data\nINSERT INTO AUTHOR VALUES (201, 'Silberschatz');\nINSERT INTO AUTHOR VALUES (202, 'Korth');\n\nINSERT INTO BOOK VALUES (1, 'DBMS Concepts', 750, 201);\nINSERT INTO BOOK VALUES (2, 'OS Principles', 680, 202);\nINSERT INTO BOOK VALUES (3, 'Networks', 550, 201);\n\n-- Lossless join verification\nSELECT COUNT(*) AS joined_rows\nFROM BOOK NATURAL JOIN AUTHOR;\n-- Expected: 3 (one row per book, no inflation, no loss)\n```\n\nIf the join produces **more rows** than the count of BOOK, the decomposition is lossy. If it produces **fewer rows**, some data is being lost.\n\n**Step 2: Transitive Dependency Removal Test (3.5 marks)**\n\nOriginal FD: `book_id → author_id → author_name` (transitive, violates 3NF).\n\nAfter decomposition, `author_name` should be **fully dependent on `author_id`** in AUTHOR, and **not present at all** in BOOK.\n\n```sql\n-- Test 1: Verify author_name is NOT in BOOK (proves decomposition)\nSELECT column_name FROM information_schema.columns\nWHERE table_name = 'book' AND column_name = 'author_name';\n-- Expected: 0 rows\n\n-- Test 2: Verify FD author_id -> author_name in AUTHOR\nSELECT author_id, COUNT(DISTINCT author_name) AS names\nFROM AUTHOR\nGROUP BY author_id\nHAVING COUNT(DISTINCT author_name) > 1;\n-- Expected: 0 rows (proves FD holds)\n\n-- Test 3: Verify no transitive dependency remains in BOOK\n-- (i.e., no non-key attribute in BOOK determines another non-key attribute)\nSELECT author_id, COUNT(DISTINCT title) AS title_count\nFROM BOOK\nGROUP BY author_id;\n-- In BOOK, this should equal the count of books per author (1:M is allowed)\n-- But within a single book, title and price must be unique to book_id\nSELECT book_id, COUNT(DISTINCT title) FROM BOOK\nGROUP BY book_id HAVING COUNT(DISTINCT title) > 1;\n-- Expected: 0 rows\n```\n\n**Conclusion:** All 3NF and lossless join requirements are satisfied.\n\n---\n\n#### **Question B (14 Marks)** — *Internal Choice Option 2*\n\n**[KTU University Exam - Model Paper 2024]**\n\nFor the schema given in Question A, answer the following:\n\n**(a)** Write SQL queries to perform **comprehensive schema validation** using `information_schema` views. List the structural checks you would perform, and explain how you would detect: (i) missing PRIMARY KEY declarations, (ii) foreign keys with no matching parent index, (iii) CHECK constraints with contradictory conditions. (7 marks)\n\n**(b)** Demonstrate how you would use **INSERT, UPDATE, and DELETE operations** to test the **cascade behavior** of the foreign key. What specific test scenarios would you design to expose bugs in the cascade implementation? (7 marks)\n\n---\n\n**Model Solution for Question B:**\n\n**(a) — Information Schema Validation Queries (7 marks)**\n\n**(i) Missing PRIMARY KEY detection (2.5 marks):**\n\n```sql\n-- Find all user-defined tables that have NO primary key declared\nSELECT t.table_name\nFROM information_schema.tables t\nLEFT JOIN information_schema.table_constraints tc\n  ON t.table_name = tc.table_name \n  AND tc.constraint_type = 'PRIMARY KEY'\n  AND t.table_schema = tc.table_schema\nWHERE t.table_schema = 'public'\n  AND t.table_type = 'BASE TABLE'\n  AND tc.constraint_name IS NULL;\n```\n\nAny row returned indicates a table missing its primary key. Such tables violate **Entity Integrity** and must be flagged.\n\n**(ii) Foreign Keys with no matching parent index (2.5 marks):**\n\n```sql\n-- Find foreign keys whose parent column lacks an index\nSELECT \n    tc.table_name AS child_table,\n    kcu.column_name AS child_column,\n    ccu.table_name AS parent_table,\n    ccu.column_name AS parent_column\nFROM information_schema.table_constraints tc\nJOIN information_schema.key_column_usage kcu\n  ON tc.constraint_name = kcu.constraint_name\nJOIN information_schema.constraint_column_usage ccu\n  ON tc.constraint_name = ccu.constraint_name\nWHERE tc.constraint_type = 'FOREIGN KEY'\n  AND NOT EXISTS (\n      SELECT 1 FROM information_schema.statistics s\n      WHERE s.table_name = ccu.table_name\n        AND s.column_name = ccu.column_name\n  );\n```\n\n**Why this matters:** Unindexed parent columns cause **full table scans** during DELETE/UPDATE on parent, drastically slowing referential integrity checks. Most RDBMS engines do NOT auto-index parent PKs in some cases (e.g., MySQL with composite FKs).\n\n**(iii) Contradictory CHECK constraints (2 marks):**\n\n```sql\n-- Retrieve all CHECK constraints\nSELECT \n    tc.table_name,\n    tc.constraint_name,\n    cc.check_clause\nFROM information_schema.table_constraints tc\nJOIN information_schema.check_constraints cc\n  ON tc.constraint_name = cc.constraint_name\nWHERE tc.constraint_type = 'CHECK'\n  AND tc.table_schema = 'public';\n```\n\n**Contradiction detection** is logical, not just syntactic. Example of a contradictory pair:\n\n```sql\nCHECK (age >= 18)\nCHECK (age <= 17)  -- Contradicts the first; no row can ever be inserted!\n```\n\nThe validator must attempt to insert a sample row satisfying all CHECKs. If no such row exists, the constraint set is contradictory.\n\n```sql\n-- Test: Try to insert any valid row\nINSERT INTO BOOK VALUES (1, 'Test', 100, 101);\n-- If this fails due to CHECK violation despite valid data, constraints are contradictory\n```\n\n**(b) — Cascade Behavior Testing (7 marks)**\n\nThe schema specifies `ON DELETE CASCADE` from AUTHOR to BOOK. To thoroughly test this:\n\n**Test Scenario 1: Delete parent with single child (1.5 marks)**\n\n```sql\nINSERT INTO AUTHOR VALUES (301, 'Elmasri');\nINSERT INTO BOOK VALUES (1, 'Fundamentals of DBMS', 800, 301);\n\nSELECT COUNT(*) AS books_before FROM BOOK WHERE author_id = 301;\n-- Expected: 1\n\nDELETE FROM AUTHOR WHERE author_id = 301;\n-- Expected: 1 row deleted from AUTHOR, 1 row auto-deleted from BOOK\n\nSELECT COUNT(*) AS books_after FROM BOOK WHERE author_id = 301;\n-- Expected: 0\n```\n\n**Test Scenario 2: Delete parent with multiple children (1.5 marks)**\n\n```sql\nINSERT INTO AUTHOR VALUES (302, 'Ramakrishnan');\nINSERT INTO BOOK VALUES (2, 'DBMS Management', 700, 302);\nINSERT INTO BOOK VALUES (3, 'Database Systems', 750, 302);\nINSERT INTO BOOK VALUES (4, 'Data Warehousing', 900, 302);\n\nSELECT COUNT(*) FROM BOOK WHERE author_id = 302;\n-- Expected: 3\n\nDELETE FROM AUTHOR WHERE author_id = 302;\n-- Expected: 3 rows auto-deleted from BOOK (cascade)\n\nSELECT COUNT(*) FROM BOOK WHERE author_id = 302;\n-- Expected: 0\n\n-- Verify no orphans exist\nSELECT b.book_id FROM BOOK b\nLEFT JOIN AUTHOR a ON b.author_id = a.author_id\nWHERE b.author_id = 302 AND a.author_id IS NULL;\n-- Expected: 0 rows\n```\n\n**Test Scenario 3: Cascading chain across 3 levels (2 marks)**\n\nSuppose a third table `CHAPTER` references BOOK. The cascade must propagate correctly.\n\n```sql\nCREATE TABLE CHAPTER (\n    chapter_id  INTEGER PRIMARY KEY,\n    book_id     INTEGER,\n    title       VARCHAR(100),\n    FOREIGN KEY (book_id) REFERENCES BOOK(book_id) ON DELETE CASCADE\n);\n\nINSERT INTO AUTHOR VALUES (303, 'Date');\nINSERT INTO BOOK VALUES (5, 'DBMS Deep Dive', 850, 303);\nINSERT INTO CHAPTER VALUES (1, 5, 'Introduction');\nINSERT INTO CHAPTER VALUES (2, 5, 'Normalization');\n\n-- Delete grandparent; expect entire chain collapsed\nDELETE FROM AUTHOR WHERE author_id = 303;\n-- Expected: BOOK row 5 deleted, CHAPTER rows 1, 2 deleted (transitive cascade)\n```\n\n**Test Scenario 4: UPDATE CASCADE on FK (2 marks)**\n\n```sql\n-- If parent PK is updated, child FKs should auto-update\nINSERT INTO AUTHOR VALUES (304, 'Ullman');\nINSERT INTO BOOK VALUES (10, 'DB Systems', 600, 304);\n\nUPDATE AUTHOR SET author_id = 999 WHERE author_id = 304;\n\nSELECT * FROM BOOK WHERE book_id = 10;\n-- Expected: book_id=10, author_id=999 (auto-updated via ON UPDATE CASCADE)\n```\n\n**Bug-Exposing Edge Cases:**\n\n| **Test** | **Scenario** | **Expected Behaviour** |\n|---|---|---|\n| Self-referencing FK | `AUTHOR.author_id` references `AUTHOR.reviewer_id` | Must detect cycles |\n| NULL FK on DELETE | If FK is NULL and parent deleted | NULL preserved (no cascade on NULL) |\n| Multiple paths to parent | Two FKs from same child to same parent | Both must cascade independently |\n| Circular FKs | A → B, B → A | DELETE must respect order or use DEFERRABLE |\n\n---\n\n### 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Revision Checklist**\n\n- ✅ **Entity Integrity** = `PRIMARY KEY` is unique + not NULL. Test with `INSERT NULL PK` and `INSERT duplicate PK`. Both must fail.\n\n- ✅ **Referential Integrity** = Every non-NULL FK must match an existing parent PK. Test with `INSERT invalid FK` (must fail) and `DELETE parent with children` (must respect ON DELETE rule).\n\n- ✅ **Domain Integrity** = `CHECK` constraints + data types. Test with boundary values (0, -1, MAX_INT, empty string).\n\n- ✅ **User-Defined Integrity** = `UNIQUE`, `DEFAULT`, `TRIGGER`. UNIQUE differs from PK: it allows **one NULL** in standard SQL.\n\n- ✅ **Information Schema** is the standard metadata view in KTU syllabus. Always use `information_schema.tables`, `information_schema.columns`, `information_schema.table_constraints` for structural audits.\n\n- ✅ **Cascade Options to remember**:\n  - `CASCADE` → propagate delete/update\n  - `SET NULL` → set FK to NULL on parent delete\n  - `SET DEFAULT` → set FK to its default value\n  - `NO ACTION` / `RESTRICT` → block the operation\n\n- ✅ **Lossless Join Test**: $|R_1 \\bowtie R_2| = |R|$ for the original relation R. Use `NATURAL JOIN` and compare counts.\n\n- ✅ **FD Verification Pattern**: `SELECT X, COUNT(DISTINCT Y) FROM T GROUP BY X HAVING COUNT(DISTINCT Y) > 1;` returns 0 rows if $X \\rightarrow Y$ holds.\n\n- ✅ **KTU Expected Query Style**: Always state (a) the **query**, (b) the **expected result** (PASS/FAIL or row count), and (c) the **interpretation** in 1 line. This 3-part answer format fetches full marks.\n\n- ✅ **Common KTU trick questions**: \"What will happen if we try to insert a row violating the CHECK constraint?\" → Answer: \"The DBMS will raise a SQLSTATE error and **reject the entire tuple** (atomicity). No partial insertion occurs.\"\n\n- ✅ **Module 4 Context**: This topic bridges **schema design** (Modules 1–3) with **advanced features** (triggers, views, cursors in Module 5). Validation queries are often combined with trigger questions in 14-mark problems.\n\n- ✅ **Real-world parallel**: This is exactly how tools like *SQL Server Data Tools (SSDT)*, *pgTAP*, and *Oracle SQL Developer Data Modeler* perform automated schema validation in production CI/CD pipelines.\n\n---\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "integrationwithapplicationsdevelopsimpleapplicationsegusingpythontointeractwiththedatabase": {
      "title": "Integration with Applications: Develop simple applications (e.g., using Python) to interact with the database",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 4\n> - **Topic:** Integration with Applications: Develop simple applications (e.g., using Python) to interact with the database\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\n**Database Application Integration** is the process of establishing a communication channel between a high-level general-purpose programming language (host language) and a Database Management System (DBMS) such that the application can issue **Data Definition Language (DDL)**, **Data Manipulation Language (DML)**, and **Transaction Control Language (TCL)** statements, fetch result sets, handle exceptions, and maintain ACID properties programmatically.\n\nIn the KTU 2024 Scheme (PBITT304), the canonical reference for this integration is the **Python Database API Specification (PEP 249)**, often simply called **DB-API 2.0**. It standardizes the way Python modules providing database connectivity expose their functionality, ensuring that the same client code works across different relational back-ends (MySQL, PostgreSQL, SQLite, Oracle) by simply swapping the *driver* (e.g., `mysql-connector-python`, `psycopg2`, `sqlite3`).\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Highlight (Module 4):**\n> The student must be able to write a complete, self-contained Python program that:\n> 1. Imports a database driver module.\n> 2. Establishes a `Connection` object.\n> 3. Creates a `Cursor` object.\n> 4. Executes parameterized SQL queries (`execute`, `executemany`).\n> 5. Fetches results (`fetchone`, `fetchall`, `fetchmany`).\n> 6. Commits / rolls back the transaction.\n> 7. Closes the cursor and the connection.\n\n## 1.2 Conceptual Analogy — The Waiter in a Restaurant\n\nImagine you are sitting in a restaurant (your **Python Application**). You want food from the kitchen (your **Database Server**). You cannot walk into the kitchen because the chef speaks a different language (raw TCP socket protocols). So you call the **Waiter** (the **Database Driver / Adapter**). The waiter:\n\n1. Takes your **order** (your SQL query written in Python).\n2. Walks to the kitchen, hands the order to the chef, and waits.\n3. The chef prepares the dish (executes the query against the data files).\n4. The waiter brings the dish back to your table (the **Result Set**).\n5. The waiter also brings the **bill** (metadata: row count, column descriptions, status messages).\n\nIn this analogy:\n- **You** = Python program\n- **Waiter** = DB-API compliant driver (`pymysql`, `sqlite3`, `psycopg2`)\n- **Order slip** = SQL statement string\n- **Kitchen** = DBMS engine\n- **Dish** = Result set (rows of tuples)\n- **Bill** = Cursor description and rowcount\n\nIf you change restaurants, the kitchen changes (MySQL → PostgreSQL), but the *protocol* with the waiter (DB-API 2.0) stays exactly the same. This **abstraction** is the central engineering virtue of the DB-API specification.\n\n> [!NOTE]\n> **Two Foundational Objects in DB-API 2.0:**\n> - **`Connection` Object** — Represents the live session/pipe to the DB. It manages transactions, isolation level, and is the *factory* for cursors.\n> - **`Cursor` Object** — A pointer/abstraction over the result set. It is the actual conduit that carries your SQL into the engine and brings rows back.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Reference Sheet\n\n## 2.1 The Five-Phase Lifecycle of a Database Application\n\nEvery Python program that talks to a relational database passes through the same five logical phases. Skipping any one of them leads to either a runtime error or a resource leak (an \"open connection\" that survives the program and eventually exhausts the DB's connection pool).\n\n### Phase 1 — Driver Import & Connection Establishment\nThe driver is imported, and a `Connection` object is instantiated. At this stage, authentication (username, password, host, port, database name) and connection-pool negotiation occur.\n\n### Phase 2 — Cursor Creation\nA cursor is acquired from the connection. In a multi-cursor scenario, each cursor is an *independent* transactional context within the same connection.\n\n### Phase 3 — Query Execution\nThe SQL statement is sent through the cursor. If the SQL contains user-supplied data, **parameterized queries (`%s` placeholders)** are mandatory to prevent **SQL Injection**, which is the most common vulnerability listed in OWASP Top 10.\n\n### Phase 4 — Result Retrieval / Persistence\n- For `SELECT`: call `fetchone()`, `fetchmany(n)`, or `fetchall()`.\n- For `INSERT/UPDATE/DELETE`: call `connection.commit()` to persist changes.\n\n### Phase 5 — Cleanup (Resource Release)\nAlways close the cursor and connection — preferably using a `with` block (context manager) so that `__exit__` handles cleanup even on exceptions.\n\n## 2.2 KTU Reference Sheet — Connection & Cursor API\n\n| Method / Attribute | Belongs To | Purpose & Engineering Significance |\n|---|---|---|\n| `connect(host, user, passwd, db)` | Module-level | Returns a new `Connection` object. Opens a network/auth session with the RDBMS. |\n| `connection.cursor()` | Connection | Returns a new `Cursor` object bound to this connection. |\n| `connection.commit()` | Connection | Atomically persists all DML statements issued since the last `commit()` or `rollback()`. |\n| `connection.rollback()` | Connection | Undoes all uncommitted work — used to recover from a mid-transaction error. |\n| `connection.close()` | Connection | Closes the network socket; after this, any cursor created from it raises `ProgrammingError`. |\n| `cursor.execute(sql, params)` | Cursor | Compiles and runs a single SQL statement. Uses `params` to safely bind values. |\n| `cursor.executemany(sql, seq)` | Cursor | Bulk execution — used for batch inserts/updates. Highly efficient for $>1000$ rows. |\n| `cursor.fetchone()` | Cursor | Returns the next row as a **tuple**, or `None` when exhausted. |\n| `cursor.fetchall()` | Cursor | Returns **all remaining rows** as a list of tuples. Memory-heavy for large result sets. |\n| `cursor.fetchmany(size)` | Cursor | Returns a list of at most `size` rows — ideal for pagination. |\n| `cursor.rowcount` | Cursor (attribute) | Number of rows affected by the last DML/DQL. Read-only **integer**. |\n| `cursor.description` | Cursor (attribute) | A sequence of 7-tuples `(name, type_code, display_size, internal_size, precision, scale, null_ok)`. |\n| `cursor.close()` | Cursor | Releases server-side resources. |\n| `with connection:` | Context manager | Auto-commits on success, auto-rolls-back on exception. **Best practice.** |\n\n## 2.3 Parameterized Query — The SQL Injection Defense\n\n> [!WARNING]\n> **Critical Security Concept (Frequently tested in KTU):**\n> Never construct SQL by string concatenation with user input. The DB-API `%s` placeholder is *not* Python's `%` formatting; it is a **driver-level bind variable** that is sent to the server separately from the SQL text, so user input can never be misinterpreted as SQL syntax.\n\n**Wrong (vulnerable):**\n\n$$\n\\text{sql} = \\text{\"SELECT * FROM users WHERE name = '\"} + \\text{user\\_input} + \\text{\"'\"}\n$$\n\nIf `user_input = \"'; DROP TABLE users; --\"`, the entire table is destroyed.\n\n**Correct (safe):**\n\n$$\n\\text{sql} = \\text{\"SELECT * FROM users WHERE name = %s\"}\n$$\n$$\n\\text{cursor.execute(sql, (user\\_input,))}\n$$\n\n## 2.4 Real-World Engineering Utility\n\n| Industry Domain | Integration Pattern |\n|---|---|\n| **E-Commerce** | Python (Django/Flask) backend ↔ MySQL/PostgreSQL for cart, inventory, and order processing. |\n| **Banking & FinTech** | Python scripts execute parameterized batch transfers via `executemany`; ACID compliance is enforced by `commit()`. |\n| **Data Science** | `pandas.read_sql(query, connection)` loads a result set directly into a DataFrame for analytics. |\n| **IoT & Telemetry** | Python on edge devices buffers sensor readings and flushes them to a time-series DB. |\n| **Web APIs (REST)** | Flask/FastAPI route handlers call a connection pool to serve JSON responses from SQL. |\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Canonical SQLite Example — A Complete Working Program\n\nSQLite is bundled with Python's standard library (module `sqlite3`), so it is the *de-facto* teaching vehicle in KTU lab exams. The following program performs the complete **CRUD** cycle on a `students` table.\n\n### Step 1 — Import the Driver\n\n```python\nimport sqlite3\n```\n\nThe standard library ships with `sqlite3` as a DB-API 2.0 compliant module. No `pip install` is required. For other databases, you would write `import mysql.connector` or `import psycopg2`.\n\n### Step 2 — Establish the Connection\n\n```python\nconn = sqlite3.connect(\"university.db\")\n```\n\nIf `university.db` does not exist, SQLite **creates** it on disk. The argument is the *data source name (DSN)*. For server-based DBs, the call would look like `mysql.connector.connect(host=\"localhost\", user=\"root\", passwd=\"...\", database=\"ktu_exam\")`.\n\n### Step 3 — Acquire a Cursor\n\n```python\ncur = conn.cursor()\n```\n\nThe cursor is the workhorse object — every SQL command is dispatched through it.\n\n### Step 4 — Execute DDL (Create Table)\n\n```python\ncur.execute(\"\"\"\n    CREATE TABLE IF NOT EXISTS students (\n        roll_no   INTEGER PRIMARY KEY,\n        name      TEXT    NOT NULL,\n        cgpa      REAL    CHECK (cgpa BETWEEN 0 AND 10)\n    )\n\"\"\")\n```\n\n`IF NOT EXISTS` makes the script **idempotent** — running it twice does not raise an error.\n\n### Step 5 — Insert Rows with Parameterized Binding\n\n```python\nsql = \"INSERT INTO students (roll_no, name, cgpa) VALUES (?, ?, ?)\"\nrecords = [\n    (101, \"Anand\",  8.7),\n    (102, \"Bhavna\", 9.1),\n    (103, \"Chitra\", 7.8),\n]\ncur.executemany(sql, records)\nconn.commit()\n```\n\n> [!NOTE]\n> Note the placeholder symbol: SQLite uses `?`, while MySQL uses `%s`, and psycopg2 (PostgreSQL) uses `%s`. This is a *driver convention* — the DB-API specification does not standardize the placeholder, only the *mechanism*.\n\n### Step 6 — Query and Fetch\n\n```python\ncur.execute(\"SELECT roll_no, name, cgpa FROM students WHERE cgpa >= 8.0\")\nprint(\"Column metadata:\", cur.description)\nfor row in cur.fetchall():\n    print(row)\n```\n\nThe output is a list of **tuples** like `(101, 'Anand', 8.7)`. To get dictionaries instead, instantiate the cursor as `conn.cursor(row_factory=sqlite3.Row)`.\n\n### Step 7 — Update\n\n```python\ncur.execute(\"UPDATE students SET cgpa = ? WHERE roll_no = ?\", (8.9, 101))\nconn.commit()\nprint(\"Rows modified:\", cur.rowcount)\n```\n\n### Step 8 — Delete\n\n```python\ncur.execute(\"DELETE FROM students WHERE roll_no = ?\", (103,))\nconn.commit()\n```\n\n### Step 9 — Use a Context Manager (Best Practice)\n\n```python\nwith sqlite3.connect(\"university.db\") as conn:\n    cur = conn.cursor()\n    cur.execute(\"SELECT COUNT(*) FROM students\")\n    print(\"Total rows:\", cur.fetchone()[0])\n    # __exit__ will auto-commit (or rollback on exception) AND close the connection\n```\n\n### Step 10 — Explicit Cleanup\n\n```python\ncur.close()\nconn.close()\n```\n\n## 3.2 Analytical Matrix — Method Selection by Use Case\n\n| Use Case | Recommended DB-API Method | Reason |\n|---|---|---|\n| Inserting one row from a web form | `cursor.execute(sql, params)` | Simplest, single-statement, no overhead of preparing a batch. |\n| Bulk-loading a CSV (10,000+ rows) | `cursor.executemany(sql, list_of_tuples)` | One parse, one network round-trip, exponential speedup over looping `execute`. |\n| Verifying a login credential | `cursor.execute()` + `cursor.fetchone()` | Memory-efficient — we only need 0 or 1 row. |\n| Building a paginated admin dashboard | Loop of `cursor.fetchmany(50)` | Streams the result set in chunks; constant memory regardless of total row count. |\n| Exporting the whole table to JSON | `cursor.execute()` + `cursor.fetchall()` | Acceptable for small tables; never for tables $>10^6$ rows. |\n| Detecting the number of affected rows | `cursor.rowcount` (attribute) | Read **after** `execute`/`executemany`, before the next statement. |\n\n## 3.3 Mathematical Model of Network Round-Trips\n\nLet $N$ be the number of rows to insert and $R$ the network round-trip latency (in seconds). Compare the two strategies:\n\n**Strategy A — Loop of `execute`:**\n$$\nT_A = N \\cdot (R + T_{parse}) \\approx N \\cdot R \\quad \\text{when } T_{parse} \\ll R\n$$\n\n**Strategy B — Single `executemany`:**\n$$\nT_B = R + N \\cdot T_{bind} \\approx R \\quad \\text{when } T_{bind} \\ll R\n$$\n\n$$\n\\therefore \\frac{T_A}{T_B} \\approx N\n$$\n\nSo a 5,000-row insert via looping is roughly **5,000×** slower than a single `executemany`. This is the single most important performance lesson in database integration.\n\n> [!IMPORTANT]\n> **KTU Examiner Insight:**\n> In the 14-mark question on Python–DB integration, full marks are awarded only when the student uses `with conn:` (context manager) and `executemany` for bulk operations. Marks are deducted for `string +` concatenation and missing `commit()`.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 End-to-End Architecture of a Python–Database Application\n\nThe diagram below models the layered request flow from the moment a user interacts with the application until data is persisted or retrieved.\n\n```mermaid\nflowchart TD\n    A[\"User Interface: CLI or Web Form\"] --> B[\"Python Application Layer: Business Logic\"]\n    B --> C[\"DB-API 2.0 Driver: mysql-connector or sqlite3\"]\n    C --> D[\"Database Engine: MySQL or SQLite Server\"]\n    D --> E[\"Physical Storage: Disk Based Data Files and Index Pages\"]\n    E --> D\n    D --> C\n    C --> B\n    B --> A\n    F[\"Result Set: List of Tuples\"] --> B\n    G[\"Connection Pool: Reusable Connection Objects\"] --> C\n    B --> G\n    H[\"Error Handler: try-except-finally Block\"] --> B\n```\n\n**Reading the diagram:**\n\n1. The **User Interface** captures input.\n2. The **Python Application Layer** holds business rules and orchestrates flow.\n3. The **DB-API 2.0 Driver** translates Python calls into the DB's wire protocol.\n4. The **Database Engine** parses, optimizes, and executes the SQL.\n5. **Physical Storage** holds rows and B-tree index pages.\n6. The reverse path returns a **Result Set** to Python.\n7. A **Connection Pool** caches open connections to avoid repeated handshakes.\n8. An **Error Handler** ensures transactional integrity on failure.\n\n## 4.2 State Diagram of the Cursor Object\n\nThe cursor is a stateful object. Understanding its state transitions is a frequent KTU short-answer topic.\n\n```mermaid\nstateDiagram-v2\n    [*] --> Fresh: cursor = conn.cursor()\n    Fresh --> Executing: execute or executemany\n    Executing --> ResultAvailable: SELECT statement\n    Executing --> NoResult: INSERT UPDATE DELETE\n    ResultAvailable --> Iterating: fetchone or fetchmany or fetchall\n    Iterating --> ResultAvailable: more rows remain\n    Iterating --> Exhausted: fetchone returns None\n    NoResult --> Fresh: implicit reset on next execute\n    Exhausted --> Fresh: implicit reset on next execute\n    Fresh --> Closed: cursor.close\n    Closed --> [*]\n```\n\n**State descriptions:**\n\n- **Fresh** — Cursor created, no SQL sent yet.\n- **Executing** — The driver has shipped SQL to the server.\n- **ResultAvailable** — Server returned a result set; rows are buffered client-side.\n- **Iterating** — Python is reading tuples from the buffer.\n- **Exhausted** — All rows consumed; further `fetchone` returns `None`.\n- **Closed** — Server resources released; any further method call raises `ProgrammingError`.\n\n## 4.3 Sequence Diagram — A Single SELECT Round-Trip\n\n```mermaid\nsequenceDiagram\n    participant App as Python App\n    participant Drv as DB-API Driver\n    participant DB as Database Server\n    App->>Drv: conn = connect(host, user, pwd, db)\n    Drv->>DB: TCP handshake and authentication\n    DB-->>Drv: OK session established\n    Drv-->>App: Connection object\n    App->>Drv: cur = conn.cursor()\n    Drv-->>App: Cursor object\n    App->>Drv: cur.execute(SELECT, params)\n    Drv->>DB: Send prepared statement\n    DB-->>Drv: Result set (rows)\n    Drv-->>App: Buffered tuples\n    App->>Drv: rows = cur.fetchall()\n    Drv-->>App: list of tuples\n    App->>Drv: conn.close()\n    Drv->>DB: Send QUIT\n    DB-->>Drv: Connection terminated\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Question 1 [KTU University Exam – Model Paper, May 2024]\n\n**Explain the significance of the Python DB-API 2.0 specification. List any four methods of the `Cursor` object with their purpose.**\n\n**Model Answer:**\n\nThe **Python Database API Specification v2.0 (PEP 249)** defines a standard interface that any Python database driver may implement. Its significance is *portability* — a program written against DB-API can switch from SQLite to MySQL to PostgreSQL by changing only the `import` statement, without rewriting business logic. It also enforces a consistent error hierarchy (`Warning`, `Error`, `InterfaceError`, `DatabaseError`) and transaction semantics.\n\nFour essential methods of the `Cursor` object:\n\n1. **`execute(sql, params=None)`** — Compiles and runs a single SQL statement. If `params` is provided, they are bound as literals — never as SQL fragments — to prevent injection.\n2. **`executemany(sql, seq_of_params)`** — Runs the same SQL once per parameter tuple in `seq_of_params` using a single network round-trip — used for bulk inserts.\n3. **`fetchone()`** — Returns the next row as a tuple, or `None` when no rows remain.\n4. **`fetchall()`** — Returns all remaining rows as a list of tuples; should be avoided for large result sets.\n\n*(Examiner's note: Any 4 of `fetchone`, `fetchall`, `fetchmany`, `execute`, `executemany`, `close`, `rowcount`, `description` are acceptable.)* **[3 marks]**\n\n---\n\n### Question 2 [KTU University Exam – June 2023]\n\n**What is SQL Injection? How does the use of parameterized queries in Python's DB-API protect against it? Illustrate with an example.**\n\n**Model Answer:**\n\n**SQL Injection** is a code-injection attack in which an attacker supplies a specially crafted string as user input that, when concatenated into a raw SQL query, alters the query's meaning — typically to bypass authentication, dump tables, or destroy data.\n\nA **parameterized query** sends the SQL template and the user data to the database engine as *two separate entities*. The database substitutes the data as a literal value at execution time, so it can never be reinterpreted as SQL syntax.\n\n**Vulnerable code (NEVER do this):**\n```python\nsql = \"SELECT * FROM users WHERE name='\" + username + \"' AND pwd='\" + pwd + \"'\"\ncur.execute(sql)\n```\nIf `username = \"admin' --\"`, the query becomes `WHERE name='admin' --' AND pwd='...'`, and the password check is commented out.\n\n**Safe code (DB-API):**\n```python\nsql = \"SELECT * FROM users WHERE name=%s AND pwd=%s\"\ncur.execute(sql, (username, pwd))\n```\nThe driver transmits the literal values for `username` and `pwd` separately; the server treats them as opaque strings, not as code. **[3 marks]**\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks Each, Internal Choice)\n\n### Question A [KTU University Exam – Dec 2023 Pattern]\n\n#### (a) [7 Marks]\n**With a neat diagram, explain the architecture of a Python application that connects to a MySQL database using the DB-API. Describe the role of the `Connection` and `Cursor` objects.**\n\n**Model Solution:**\n\nThe architecture is a **three-tier client–server model** in which the Python application is the client tier, the driver is the middleware, and the MySQL server is the server tier.\n\n**Architecture diagram (block-level):**\n\n```mermaid\nflowchart LR\n    T1[\"Tier 1: Presentation: Python Script or Web App\"] --> T2[\"Tier 2: Application Logic: Business Rules\"]\n    T2 --> T3[\"Tier 3a: DB-API Driver: mysql.connector\"]\n    T3 --> T4[\"Tier 3b: MySQL Server: Query Processor and Buffer Pool\"]\n    T4 --> T5[\"Tier 4: Storage Engine: InnoDB Data and Index Files\"]\n    T5 --> T4\n    T4 --> T3\n    T3 --> T2\n    T2 --> T1\n```\n\n**Role of the `Connection` object:**\n\nThe `Connection` represents the live authenticated session between Python and MySQL. It is obtained by calling `mysql.connector.connect(host, user, passwd, database)`. Its responsibilities are:\n\n- Holding the **TCP socket** and authentication state.\n- Acting as a **transaction boundary** — `commit()` and `rollback()` operate at this level.\n- Serving as a **factory** for one or more `Cursor` objects.\n- Managing the **isolation level** (`connection.set_isolation_level(...)`).\n\n**Role of the `Cursor` object:**\n\nThe `Cursor` is the *abstraction over a result set*. It is created by `connection.cursor()`. Its responsibilities are:\n\n- Sending SQL statements to the server via `execute()` and `executemany()`.\n- Buffering the result set client-side and exposing it via `fetchone()`, `fetchmany(n)`, and `fetchall()`.\n- Exposing metadata through `description` (column names and types) and `rowcount` (affected rows).\n- Holding a reference to the current row position (internal pointer).\n\nThe two objects are decoupled: one connection can host many cursors, but a cursor belongs to exactly one connection. **[7 marks]**\n\n#### (b) [7 Marks]\n**Write a complete Python program using `sqlite3` to:**\n1. Create a table `Employee(emp_id, name, department, salary)`.\n2. Insert at least 5 records using `executemany`.\n3. Display the names of employees whose salary is greater than 50,000.\n4. Update the salary of a given employee.\n5. Delete an employee record and commit all changes properly.\n\n**Model Solution:**\n\n```python\nimport sqlite3\n\n# --- 1. Connect and create the table ---\nconn = sqlite3.connect(\"company.db\")\ncur  = conn.cursor()\n\ncur.execute(\"\"\"\n    CREATE TABLE IF NOT EXISTS Employee (\n        emp_id      INTEGER PRIMARY KEY,\n        name        TEXT    NOT NULL,\n        department  TEXT,\n        salary      REAL    CHECK (salary > 0)\n    )\n\"\"\")\n\n# --- 2. Bulk insert using executemany ---\nsql_insert = \"INSERT INTO Employee (emp_id, name, department, salary) VALUES (?, ?, ?, ?)\"\nemployees = [\n    (1, \"Anand\",    \"CSE\",   72000),\n    (2, \"Bhavna\",   \"ECE\",   55000),\n    (3, \"Chitra\",   \"MECH\",  48000),\n    (4, \"Deepak\",   \"CSE\",   88000),\n    (5, \"Elizabeth\",\"IT\",    65000),\n]\ncur.executemany(sql_insert, employees)\nconn.commit()\nprint(\"Inserted rows:\", cur.rowcount)\n\n# --- 3. Query: salary > 50,000 ---\ncur.execute(\"SELECT name FROM Employee WHERE salary > ?\", (50000,))\nprint(\"High earners:\")\nfor (name,) in cur.fetchall():\n    print(\"  -\", name)\n\n# --- 4. Update an employee's salary ---\ncur.execute(\"UPDATE Employee SET salary = ? WHERE emp_id = ?\", (95000, 4))\nconn.commit()\nprint(\"Rows updated:\", cur.rowcount)\n\n# --- 5. Delete an employee record ---\ncur.execute(\"DELETE FROM Employee WHERE emp_id = ?\", (3,))\nconn.commit()\nprint(\"Rows deleted:\", cur.rowcount)\n\n# --- Cleanup ---\ncur.close()\nconn.close()\n```\n\n**Expected console output:**\n\n```\nInserted rows: 5\nHigh earners:\n  - Anand\n  - Bhavna\n  - Deepak\n  - Elizabeth\nRows updated: 1\nRows deleted: 1\n```\n\n**Explanation of key lines:**\n\n- `CREATE TABLE IF NOT EXISTS` — idempotent DDL, avoids `IntegrityError` on re-runs.\n- `executemany` — single round-trip for 5 rows; 5× faster than a loop of `execute`.\n- `?` placeholder — driver-level bind variable; immune to SQL injection.\n- `conn.commit()` — necessary after every DML block because SQLite defaults to *implicit transaction per statement* but Python DB-API waits for an explicit commit before persisting to disk.\n- `cur.rowcount` — DML-aware; for `executemany` it returns the total affected rows. **[7 marks]**\n\n---\n\n### Question B [KTU University Exam – June 2024 Pattern — Internal Choice to Question A]\n\n#### (a) [7 Marks]\n**Discuss the role of the context manager (`with` statement) in Python–database programming. Show how it handles exceptions and ensures clean resource release with a working example.**\n\n**Model Solution:**\n\nThe `with` statement (introduced by PEP 343) wraps a block in a **context manager** that defines `__enter__` and `__exit__` methods. The `sqlite3.Connection` class implements both, so it can be used directly with `with`.\n\n**What happens internally:**\n\n1. On entering the block, `__enter__` returns the connection object.\n2. On normal exit, `__exit__` is called with `exc_type=None`. It calls `connection.commit()` and `connection.close()`.\n3. On exception, `__exit__` is called with the exception triple. It calls `connection.rollback()`, then `connection.close()`, and finally re-raises the exception so the caller can handle it.\n\n**Consequence:** Even if a `sqlite3.OperationalError` (e.g., database is locked) occurs mid-block, the partial transaction is rolled back and the socket is closed — no manual `try/except/finally` is needed.\n\n**Working example:**\n\n```python\nimport sqlite3\n\ntry:\n    with sqlite3.connect(\"bank.db\") as conn:\n        cur = conn.cursor()\n        cur.execute(\"CREATE TABLE IF NOT EXISTS account(acc_no INTEGER PRIMARY KEY, balance REAL)\")\n        cur.execute(\"INSERT INTO account VALUES (1, 1000)\")\n        # Force an error to demonstrate rollback:\n        cur.execute(\"INSERT INTO account VALUES (1, 500)\")   # duplicate primary key\nexcept sqlite3.IntegrityError as e:\n    print(\"Caught:\", e)\n\n# Verify that the first insert was rolled back:\nwith sqlite3.connect(\"bank.db\") as conn:\n    cur = conn.cursor()\n    cur.execute(\"SELECT COUNT(*) FROM account\")\n    print(\"Rows after error:\", cur.fetchone()[0])   # Output: 0\n```\n\n**Why this is the *best practice* in production code:**\n\n- **Atomicity** — either the entire block commits or nothing does.\n- **Resource safety** — no leaked file descriptors or TCP sockets, even on crash.\n- **Readability** — replaces 5–7 lines of `try/except/finally` boilerplate.\n- **Testability** — the deterministic rollback path makes the code easier to unit-test.\n\nWithout the context manager, the same program would need:\n```python\nconn = sqlite3.connect(\"bank.db\")\ntry:\n    cur = conn.cursor()\n    cur.execute(...)\n    conn.commit()\nexcept sqlite3.Error:\n    conn.rollback()\n    raise\nfinally:\n    conn.close()\n```\nThis is functionally equivalent but verbose and error-prone. **[7 marks]**\n\n#### (b) [7 Marks]\n**Compare `cursor.fetchone()`, `cursor.fetchmany(n)`, and `cursor.fetchall()`. In what scenario would you prefer `fetchmany` over `fetchall`? Write a small program to demonstrate pagination of a result set using `fetchmany`.**\n\n**Model Solution:**\n\n| Method | Return Type | Memory Footprint | Typical Use |\n|---|---|---|---|\n| `fetchone()` | A single tuple or `None` | Constant (1 row) | Login checks, existence queries, primary-key lookups. |\n| `fetchmany(n)` | A list of up to `n` tuples | Bounded by `n` | **Pagination** through large tables without exhausting RAM. |\n| `fetchall()` | A list of *all* remaining rows | Unbounded — grows with row count | Small lookup tables, export-to-JSON operations, unit tests. |\n\n**When to prefer `fetchmany` over `fetchall`:**\nWhen the result set is large (typically $>10^5$ rows), `fetchall` materializes the entire list in memory and may cause a `MemoryError`. `fetchmany(n)` keeps memory usage constant, regardless of total table size, and enables **forward-only streaming** — a common pattern in batch processing, ETL jobs, and report generation.\n\n**Pagination demo using `fetchmany`:**\n\n```python\nimport sqlite3\n\nconn = sqlite3.connect(\"library.db\")\ncur  = conn.cursor()\n\ncur.execute(\"SELECT book_id, title FROM books ORDER BY book_id\")\nPAGE_SIZE = 3\npage_no   = 1\n\nwhile True:\n    rows = cur.fetchmany(PAGE_SIZE)\n    if not rows:                       # fetchmany returns [] at end\n        break\n    print(f\"--- Page {page_no} ---\")\n    for r in rows:\n        print(r)\n    page_no += 1\n\ncur.close()\nconn.close()\n```\n\n**Why this works:**\n\n- `fetchmany` does not re-execute the query; it simply slices the already-buffered result set the driver received from the server.\n- The `while True ... break` pattern is the canonical KTU-expected idiom for streaming pagination.\n- For very large result sets, the driver can be configured with `cur = conn.cursor()` plus a server-side cursor in PostgreSQL/MySQL to avoid full client-side buffering.\n\n**Performance comparison for a 1-million-row table:**\n\n- `fetchall()` → 1,000,000 tuples in RAM ≈ 100 MB Python object overhead.\n- `fetchmany(1000)` → 1,000 tuples in RAM ≈ 100 KB; same network cost, vastly lower memory cost. **[7 marks]**\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> 1. **Missing `commit()`** — *Most common, loses 2–3 marks.* Without `commit()`, all `INSERT/UPDATE/DELETE` statements are rolled back when the connection closes. Always pair DML with `commit()`.\n> 2. **String concatenation in SQL** — Awarded **zero marks** for the query line if a `+` or f-string is used to embed user input. Always use `?` / `%s` placeholders.\n> 3. **Confusing DB-API placeholders with Python `%` formatting** — Do NOT write `cur.execute(\"... %s ...\" % value)`. The correct form is `cur.execute(\"... %s ...\", (value,))`.\n> 4. **Forgetting `cur.close()` and `conn.close()`** — In KTU lab exams, the auto-grader checks for open file descriptors; resource leaks lead to a 1-mark deduction.\n> 5. **Using `fetchall` for huge tables** — Marks are reduced if the student does not justify the choice or does not mention memory implications.\n> 6. **Wrong placeholder symbol** — MySQL uses `%s`, SQLite uses `?`. Mixing them up raises `ProgrammingError` and the program is marked *non-functional*.\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid Revision Checklist (Pin this to your study wall):**\n> - **DB-API 2.0** (PEP 249) is the universal Python DB specification — learn its name, version, and purpose.\n> - **Two key objects:** `Connection` (session) and `Cursor` (query executor / result iterator).\n> - **Five lifecycle phases:** import → connect → cursor → execute → commit/rollback → close.\n> - **Always use parameterized queries** with `?` (SQLite) or `%s` (MySQL/PostgreSQL). Never concatenate user input.\n> - **Three fetch methods:** `fetchone`, `fetchmany(n)`, `fetchall` — pick based on memory budget.\n> - **`executemany` for bulk inserts** — provides ~$N\\times$ speedup over looping `execute`.\n> - **`with conn:` is the gold standard** for transaction and resource management.\n> - **`commit()` is mandatory** after every DML block; `rollback()` is mandatory on error.\n> - **`cur.rowcount`** gives DML-affected rows; **`cur.description`** gives column metadata.\n> - **OWASP Top 10** lists SQL Injection as the #1 injection vulnerability — know the parameterized-query defense.\n> - **Driver examples to memorize:** `sqlite3` (standard library), `mysql-connector-python` (MySQL), `psycopg2` (PostgreSQL), `cx_Oracle` (Oracle).\n> - **For lab exams:** run the program, verify the database file is created, and screenshot the output to attach to the record.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "documentationpreparecomprehensivedocumentationcoveringprojectobjectivesdatabaseschemaconstraintstransactionsandimplementationdetails": {
      "title": "Documentation: Prepare comprehensive documentation covering project objectives, database schema, constraints, transactions, and implementation details",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 4\n> - **Topic:** Documentation: Prepare comprehensive documentation covering project objectives, database schema, constraints, transactions, and implementation details\n\n<!-- SECTION_1_START -->\n# SECTION 1 — Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n**Database Documentation** is the systematic, structured, and continuously maintained collection of written artifacts, diagrams, schema definitions, constraint specifications, transaction policies, and implementation logs that describe the *purpose*, *structure*, *behavior*, and *operational reality* of a database system across its entire life-cycle (Analysis → Design → Implementation → Maintenance).\n\nIn the KTU 2024 Scheme Outcome-Based Education (OBE) framework, documentation is treated as a *first-class deliverable* of the DBMS mini-project, mapped to **Course Outcomes (COs)** such as:\n\n- **CO4** – *Design and implement a normalized database schema for a real-world problem.*\n- **CO5** – *Apply integrity constraints, triggers, and transactions to enforce business rules.*\n- **CO6** – *Document, test, and present the database system following professional software-engineering standards.*\n\n> [!IMPORTANT]\n> **KTU Definition Box**\n> A database project without documentation is considered **incomplete** during end-semester evaluation. Documentation contributes **5–8 marks** (out of 100) directly, and indirectly influences marks for *schema correctness*, *constraint justification*, and *viva voce*.\n\n---\n\n## 1.2 Intuitive Real-World Analogy\n\nImagine you purchased a brand-new **smartphone**. Inside the box you expect to find:\n\n| Real-World Item | Database Counterpart |\n|---|---|\n| Quick-start guide (what the phone does) | **Project Objectives & Scope** |\n| Technical specification sheet (RAM, storage, processor) | **Schema Definition** (tables, columns, types) |\n| Safety instructions (don't charge above 45 °C) | **Constraints** (PK, FK, CHECK, UNIQUE) |\n| User manual on operations (calls, apps, settings) | **Transactions & Stored Procedures** |\n| Service center card & warranty terms | **Implementation, Deployment & Maintenance** |\n| Blueprints & architecture diagrams | **ER / Relational Diagrams** |\n\n> [!NOTE]\n> Just as you would not buy a phone without a manual, a future developer, auditor, or examiner must not be left to *reverse-engineer* your database. **Documentation is the \"user manual\" of your data.**\n\n---\n\n## 1.3 Why Documentation is Non-Negotiable (KTU Examiner Perspective)\n\n1. **Knowledge Transfer** — the original developer may leave the project; documentation preserves institutional memory.\n2. **Maintainability** — schema changes (ALTER TABLE) are safer when the baseline is well documented.\n3. **Validation & Audit** — examiners verify whether *what was designed* matches *what was implemented*.\n4. **Disaster Recovery** — backup policies, recovery procedures, and transaction logs must be written down.\n5. **Legal & Compliance** — GDPR, HIPAA, and PCI-DSS mandate documented data-handling practices.\n6. **Communication** — bridges the gap between domain experts, DBAs, and application developers.\n\n> [!TIP]\n> Treat documentation as a **living artifact**. It is updated every time a DDL change is applied to the live database.\n\n---\n\n## 1.4 The Six Pillars of KTU Database Documentation\n\n$$\n\\boxed{\\text{Database Documentation} = \\underbrace{\\text{Objectives}}_{\\text{Why}} \\cup \\underbrace{\\text{Schema}}_{\\text{What}} \\cup \\underbrace{\\text{Constraints}}_{\\text{Rules}} \\cup \\underbrace{\\text{Transactions}}_{\\text{How}} \\cup \\underbrace{\\text{Implementation}}_{\\text{Where}} \\cup \\underbrace{\\text{Testing}}_{\\text{Verified}}}\n$$\n\nEach pillar will be dissected in **Section 3** with exhaustive SQL and template examples.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2 — Deep Theoretical Analysis & KTU High-Yield Reference Matrix\n\n## 2.1 The Anatomy of a Comprehensive DBMS Document\n\nA KTU-evaluated database project report is conventionally organized into the following **ordered sections**. This is the exact order examiners expect to see when flipping through the report.\n\n### 2.1.1 Section-wise Analytical Breakdown\n\n#### 📘 A. Project Objectives & Scope\n- **Why this system?** — Business problem statement (2–3 paragraphs).\n- **Functional Objectives** — *bullet list* of features the system MUST support.\n- **Non-Functional Objectives** — performance, security, scalability targets.\n- **Scope** — what is **in** and what is **out** (e.g., \"Out of scope: mobile app, payment gateway\").\n- **Stakeholders** — Admin, End-User, Auditor, DBA.\n- **Tools & Technology Stack** — e.g., *MySQL 8.0 / Oracle 21c XE / PostgreSQL 15*, front-end language, ER tool (draw.io, Lucidchart, ERDPlus).\n\n#### 📗 B. Requirement Analysis\n- **Functional Requirements** — \"User shall be able to view order history of last 12 months.\"\n- **Non-Functional Requirements** — *\"Response time ≤ 2 s for 95 % of queries under 1000 concurrent users.\"*\n- **Use-Case Diagram** (UML) — *Actor, Use Case, System boundary*.\n- **Data Dictionary (Informal)** — narrative description of each entity before formal modeling.\n\n#### 📙 C. Database Schema (The Heart of the Report)\nThis is the most heavily weighted section. It must contain:\n\n| Sub-Section | What to Include | KTU Weight |\n|---|---|---|\n| **ER Diagram** | Entities, attributes (with PK/FK markings), relationships, cardinalities, participation (total/partial) | High |\n| **Relational Schema** | Converted tables with attribute list and data types | High |\n| **Normalization Proof** | Show 1NF → 2NF → 3NF (or BCNF) step-by-step | High |\n| **DDL Script** | `CREATE DATABASE`, `CREATE TABLE`, with all clauses | High |\n| **Sample Data** | 5–10 rows per table for verification | Medium |\n\n> [!IMPORTANT]\n> **Mandatory Rule:** Every table you draw in the ER diagram MUST appear in the DDL script, and every table in the DDL MUST be populated in the sample-data section. *Inconsistency = direct mark deduction.*\n\n#### 📕 D. Constraints Documentation\nConstraints are the **guardians of data integrity**. Each constraint must be documented with:\n- *Constraint name* (use `CONSTRAINT <name> ...`)\n- *Table and column* it applies to\n- *Business rule it enforces* (in plain English)\n- *SQL syntax* used (inline or out-of-line)\n\nThe five canonical constraint categories are:\n\n| Category | SQL Keyword | Purpose |\n|---|---|---|\n| **Domain Integrity** | `CHECK`, `NOT NULL` | Restricts values a column can hold |\n| **Entity Integrity** | `PRIMARY KEY` | Uniquely identifies each row |\n| **Referential Integrity** | `FOREIGN KEY ... REFERENCES` | Enforces parent-child link |\n| **User-Defined Integrity** | `TRIGGER` | Complex cross-table or temporal rules |\n| **Key Integrity** | `UNIQUE` | Prevents duplicate non-PK values |\n\n#### 📓 E. Transactions Documentation\n- **ACID Properties** — Atomicity, Consistency, Isolation, Durability. Define each with one-line examples.\n- **Transaction Boundaries** — `BEGIN ... COMMIT/ROLLBACK`.\n- **Isolation Levels** — `READ UNCOMMITTED`, `READ COMMITTED`, `REPEATABLE READ`, `SERIALIZABLE`. State which level is used and *why*.\n- **Concurrency Problems Addressed** — Dirty Read, Non-Repeatable Read, Phantom Read, Lost Update.\n- **Sample Transaction Script** — at least one `BEGIN ... COMMIT` block with realistic business logic.\n- **Deadlock Handling** — retry policy, timeout setting.\n\n#### 📒 F. Implementation Details\n- **Server Configuration** — host, port, character set (`utf8mb4`), storage engine (InnoDB for MySQL).\n- **User Roles & Privileges** — `GRANT SELECT, INSERT ON DB.* TO 'app_user'@'localhost';`\n- **Indexing Strategy** — list of indexes created, reason for each, expected cardinality.\n- **Views, Stored Procedures, Functions, Triggers** — DDL + business purpose.\n- **Backup & Recovery Policy** — full backup frequency, point-in-time recovery, RTO/RPO targets.\n- **Security Measures** — password hashing (e.g., bcrypt at app level), SQL injection prevention (parameterized queries), encryption at rest.\n\n#### 📔 G. Testing & Validation\n- **Test Case Table** — Test ID, Description, Input, Expected Output, Actual Output, Status (Pass/Fail).\n- **Sample Queries** — at least 8–10 realistic `SELECT` queries demonstrating joins, aggregates, sub-queries, and views.\n- **Performance Snapshot** — `EXPLAIN ANALYZE` output for the slowest query.\n- **Screenshot Evidence** — optional but adds marks: tool output, phpMyAdmin/Workbench views.\n\n#### 📚 H. Conclusion & Future Enhancements\n- Summary of what was achieved vs. objectives.\n- Known limitations.\n- Proposed future work (e.g., sharding, replication, migration to cloud).\n\n---\n\n## 2.2 KTU High-Yield Documentation Cheat-Sheet\n\n| # | Documentation Element | Format / Tool | Typical Length | Mandatory? |\n|---|---|---|---|---|\n| 1 | Title Page | University-prescribed format | 1 page | ✅ |\n| 2 | Certificate | Signed by Guide & HOD | 1 page | ✅ |\n| 3 | Abstract | 150–250 words | 0.5 page | ✅ |\n| 4 | Table of Contents | Auto-generated (Word/LaTeX) | 1 page | ✅ |\n| 5 | List of Figures / Tables | Auto-generated | 1 page | ✅ |\n| 6 | Project Objectives | Bullets + paragraph | 1–2 pages | ✅ |\n| 7 | ER Diagram | draw.io / Lucidchart | 1 full page | ✅ |\n| 8 | Relational Schema | Tables in report | 1–2 pages | ✅ |\n| 9 | Normalization Proof | Step-by-step decomposition | 2–3 pages | ✅ |\n| 10 | DDL Script | `CREATE TABLE` blocks | 2–4 pages | ✅ |\n| 11 | Constraints Listing | Tabular | 1–2 pages | ✅ |\n| 12 | Transaction Examples | SQL + narrative | 1–2 pages | ✅ |\n| 13 | Screenshots | Tool capture | 2–3 pages | ⚠️ Recommended |\n| 14 | Test Cases | Tabular | 1–2 pages | ✅ |\n| 15 | References | IEEE / APA style | 1 page | ✅ |\n\n> [!NOTE]\n> The cheatsheet above is calibrated for a typical KTU **mini-project** (≈ 30–50 page report). Adjust proportionally for the larger main project.\n\n---\n\n## 2.3 Real-World Engineering Utility\n\n- **Banking** — RBI-mandated audit trails require documented transaction policies.\n- **Healthcare** — EHR systems must document schema evolution for HIPAA compliance.\n- **E-Commerce** — Amazon-style catalogs depend on documented indexing strategies for search latency.\n- **Aerospace & Defense** — DO-178C standards require traceable database documentation.\n- **Startups** — Onboarding new engineers is **10× faster** with proper documentation.\n\n> [!TIP]\n> KTU examiners reward *traceability* — if a constraint in section D references a requirement in section B and is enforced by a trigger in section F, you demonstrate **engineering maturity**.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3 — Exhaustive Step-by-Step Walkthroughs & Analytical Templates\n\nThis section provides the *complete, copy-pasteable templates* a KTU student can adapt for any DBMS project. We will use a running example: **\"Online Book Store Management System\"**.\n\n---\n\n## 3.1 Project Objectives — Template & Worked Example\n\n### 3.1.1 Generic Template\n\n```text\nPROJECT TITLE: <Title>\nPROBLEM DOMAIN: <Industry/Field>\nPROBLEM STATEMENT:\n   The current process of <manual task> suffers from <pain point 1>,\n   <pain point 2>, and <pain point 3>. The proposed system aims to\n   automate, centralize, and secure this process.\n\nPRIMARY OBJECTIVES:\n   O1. <Verb + measurable outcome>\n   O2. <Verb + measurable outcome>\n   ...\n\nNON-FUNCTIONAL OBJECTIVES:\n   N1. Performance: <target>\n   N2. Security: <target>\n   N3. Availability: <target>\n\nSTAKEHOLDERS:\n   - <Role 1>: <responsibility>\n   - <Role 2>: <responsibility>\n\nTECH STACK:\n   - DBMS:  <MySQL 8.0 / Oracle 21c XE / PostgreSQL 15>\n   - Front-end: <HTML/CSS/JS / Python-Django / Java Swing>\n   - ER Tool: <draw.io / Lucidchart>\n   - OS: <Windows 11 / Ubuntu 22.04>\n```\n\n### 3.1.2 Worked Example — Online Book Store\n\n```text\nPROJECT TITLE: Online Book Store Management System\nPROBLEM DOMAIN: E-Commerce / Retail\n\nPROBLEM STATEMENT:\n   The local bookstore \"PageTurner\" currently tracks inventory, customers,\n   and orders using paper ledgers and disjoint Excel sheets. This leads to\n   (a) frequent stock-out errors, (b) inability to generate sales analytics,\n   and (c) no centralized record of customer purchase history. The proposed\n   DBMS-based system will centralize, validate, and report on all operations.\n\nPRIMARY OBJECTIVES:\n   O1. Maintain a normalized (3NF) database of books, authors, customers,\n       and orders.\n   O2. Enforce referential integrity so an order cannot exist without a\n       valid customer and book.\n   O3. Support analytics queries: top-10 best-selling books, monthly\n       revenue, low-stock alerts.\n   O4. Provide concurrent transaction handling for simultaneous order\n       placement by multiple users.\n\nNON-FUNCTIONAL OBJECTIVES:\n   N1. Query response time ≤ 1.5 s for 95 % of analytical queries on\n       100 K rows.\n   N2. Passwords stored using bcrypt (cost factor 12).\n   N3. System available 99 % of business hours (8 AM – 10 PM).\n\nSTAKEHOLDERS:\n   - Customer: browses books, places orders, views history.\n   - Store Admin: manages books, authors, inventory.\n   - Data Analyst: runs reports, exports CSV.\n   - DBA: manages backups, users, performance tuning.\n\nTECH STACK:\n   - DBMS:   MySQL 8.0 Community Edition\n   - Front-end:  Python 3.11 + Flask\n   - ER Tool:    draw.io\n   - OS:    Windows 11 / Ubuntu 22.04\n```\n\n---\n\n## 3.2 Database Schema — Exhaustive DDL Walkthrough\n\n### 3.2.1 Conceptual → Logical → Physical Translation\n\nThe flow is:\n\n$$\n\\underbrace{\\text{ER Diagram}}_{\\text{Conceptual}} \\;\\longrightarrow\\; \\underbrace{\\text{Relational Schema (Tables + Keys)}}_{\\text{Logical}} \\;\\longrightarrow\\; \\underbrace{\\text{DDL + DML + DCL}}_{\\text{Physical}}\n$$\n\n### 3.2.2 Final DDL Script (Book Store Example)\n\n```sql\n-- =========================================================\n-- DATABASE CREATION\n-- =========================================================\nCREATE DATABASE IF NOT EXISTS BookStoreDB\n    CHARACTER SET utf8mb4\n    COLLATE utf8mb4_unicode_ci;\n\nUSE BookStoreDB;\n\n-- =========================================================\n-- TABLE 1: Author\n-- Each author has a unique surrogate ID.\n-- =========================================================\nCREATE TABLE Author (\n    author_id   INT             AUTO_INCREMENT,\n    first_name  VARCHAR(50)     NOT NULL,\n    last_name   VARCHAR(50)     NOT NULL,\n    bio         TEXT,\n    CONSTRAINT pk_author PRIMARY KEY (author_id),\n    CONSTRAINT chk_author_name CHECK (CHAR_LENGTH(first_name) >= 1)\n);\n\n-- =========================================================\n-- TABLE 2: Book\n-- Composite UNIQUE (isbn) and FK to Author.\n-- =========================================================\nCREATE TABLE Book (\n    book_id     INT             AUTO_INCREMENT,\n    isbn        CHAR(13)        NOT NULL,\n    title       VARCHAR(200)    NOT NULL,\n    author_id   INT             NOT NULL,\n    price       DECIMAL(8,2)    NOT NULL,\n    stock_qty   INT             DEFAULT 0,\n    pub_year    YEAR,\n    CONSTRAINT pk_book        PRIMARY KEY (book_id),\n    CONSTRAINT uq_book_isbn   UNIQUE (isbn),\n    CONSTRAINT fk_book_author FOREIGN KEY (author_id)\n        REFERENCES Author(author_id)\n        ON UPDATE CASCADE\n        ON DELETE RESTRICT,\n    CONSTRAINT chk_book_price CHECK (price > 0),\n    CONSTRAINT chk_book_stock CHECK (stock_qty >= 0)\n);\n\n-- =========================================================\n-- TABLE 3: Customer\n-- =========================================================\nCREATE TABLE Customer (\n    customer_id INT             AUTO_INCREMENT,\n    email       VARCHAR(120)    NOT NULL,\n    full_name   VARCHAR(120)    NOT NULL,\n    phone       CHAR(10),\n    joined_on   DATE            DEFAULT (CURRENT_DATE),\n    CONSTRAINT pk_customer      PRIMARY KEY (customer_id),\n    CONSTRAINT uq_customer_mail UNIQUE (email),\n    CONSTRAINT chk_phone_format CHECK (phone REGEXP '^[0-9]{10}$')\n);\n\n-- =========================================================\n-- TABLE 4: Orders  (master)\n-- =========================================================\nCREATE TABLE Orders (\n    order_id    INT             AUTO_INCREMENT,\n    customer_id INT             NOT NULL,\n    order_date  DATETIME        DEFAULT CURRENT_TIMESTAMP,\n    status      ENUM('PLACED','SHIPPED','DELIVERED','CANCELLED')\n                DEFAULT 'PLACED',\n    total_amt   DECIMAL(10,2)   NOT NULL,\n    CONSTRAINT pk_order          PRIMARY KEY (order_id),\n    CONSTRAINT fk_order_customer FOREIGN KEY (customer_id)\n        REFERENCES Customer(customer_id)\n        ON DELETE RESTRICT,\n    CONSTRAINT chk_total_amt     CHECK (total_amt >= 0)\n);\n\n-- =========================================================\n-- TABLE 5: OrderItem (associative entity for M:N)\n-- =========================================================\nCREATE TABLE OrderItem (\n    order_id   INT,\n    book_id    INT,\n    quantity   INT            NOT NULL,\n    unit_price DECIMAL(8,2)   NOT NULL,\n    CONSTRAINT pk_orderitem PRIMARY KEY (order_id, book_id),\n    CONSTRAINT fk_oi_order FOREIGN KEY (order_id)\n        REFERENCES Orders(order_id) ON DELETE CASCADE,\n    CONSTRAINT fk_oi_book  FOREIGN KEY (book_id)\n        REFERENCES Book(book_id)   ON DELETE RESTRICT,\n    CONSTRAINT chk_qty_pos CHECK (quantity > 0)\n);\n```\n\n> [!IMPORTANT]\n> **Documentation Point:** In your report, *every line* of the above DDL must be justified. For example: `ON DELETE RESTRICT` on `fk_book_author` is chosen because we **must not lose a book simply because an author record is removed** — a soft-delete pattern is preferred.\n\n### 3.2.3 Sample Data Insertion (for documentation)\n\n```sql\nINSERT INTO Author (first_name, last_name, bio) VALUES\n    ('Chetan',     'Bhagat',     'Indian novelist'),\n    ('J. K.',      'Rowling',    'British author of Harry Potter'),\n    ('George',     'Orwell',     'English novelist of 1984');\n\nINSERT INTO Book (isbn, title, author_id, price, stock_qty, pub_year) VALUES\n    ('9788129135', 'Five Point Someone',  1, 295.00, 40, 2004),\n    ('9780747532', 'Harry Potter ...',    2, 550.00, 25, 1997),\n    ('9780451524', '1984',                3, 220.00, 60, 1949);\n```\n\n---\n\n## 3.3 Constraints — Tabular Documentation Matrix\n\nFor the report, present a **single consolidated table** that any examiner can scan in 30 seconds:\n\n| # | Constraint Name | Type | Table.Column | Business Rule | SQL Clause |\n|---|---|---|---|---|---|\n| 1 | `pk_author` | PRIMARY KEY | `Author.author_id` | Every author must be uniquely identifiable | `PRIMARY KEY (author_id)` |\n| 2 | `chk_book_price` | CHECK | `Book.price` | Selling price cannot be zero or negative | `CHECK (price > 0)` |\n| 3 | `fk_book_author` | FOREIGN KEY | `Book.author_id → Author.author_id` | A book must be written by an existing author | `FOREIGN KEY ... REFERENCES` |\n| 4 | `uq_customer_mail` | UNIQUE | `Customer.email` | Two customers cannot share an email | `UNIQUE (email)` |\n| 5 | `trg_low_stock_alert` | TRIGGER (user-defined) | `Book` | Notify admin when `stock_qty < 5` | `CREATE TRIGGER ... AFTER UPDATE` |\n\n### 3.3.1 Example Trigger (for documentation)\n\n```sql\nDELIMITER //\nCREATE TRIGGER trg_low_stock_alert\nAFTER UPDATE ON Book\nFOR EACH ROW\nBEGIN\n    IF NEW.stock_qty < 5 AND OLD.stock_qty >= 5 THEN\n        INSERT INTO LowStockLog(book_id, log_time, qty)\n        VALUES (NEW.book_id, NOW(), NEW.stock_qty);\n    END IF;\nEND //\nDELIMITER ;\n```\n\n---\n\n## 3.4 Transactions — ACID Walkthrough with SQL\n\n### 3.4.1 The Four ACID Guarantees\n\n$$\n\\boxed{\\text{ACID} \\;=\\; A(\\text{tomicity}) + C(\\text{onsistency}) + I(\\text{solation}) + D(\\text{urability})}\n$$\n\n| Property | Meaning | Failure Scenario Prevented |\n|---|---|---|\n| **Atomicity** | All-or-nothing execution of grouped statements | Power-cut mid-transfer leaves half-deducted balance |\n| **Consistency** | DB moves from one valid state to another | FK violation at commit time |\n| **Isolation** | Concurrent transactions appear serial | Lost-update problem during simultaneous booking |\n| **Durability** | Committed data survives crashes | Disk write lost after `COMMIT` |\n\n### 3.4.2 Sample Transaction: \"Place an Order\"\n\n```sql\nSTART TRANSACTION;\n\n    -- Step 1: Validate stock\n    SELECT stock_qty INTO @qty\n    FROM Book WHERE book_id = 101 FOR UPDATE;\n\n    -- Step 2: Decrement stock (with guard)\n    UPDATE Book\n       SET stock_qty = stock_qty - 2\n     WHERE book_id = 101 AND stock_qty >= 2;\n\n    -- Step 3: Insert master order\n    INSERT INTO Orders (customer_id, total_amt)\n    VALUES (501, 1100.00);\n\n    SET @oid = LAST_INSERT_ID();\n\n    -- Step 4: Insert line item\n    INSERT INTO OrderItem (order_id, book_id, quantity, unit_price)\n    VALUES (@oid, 101, 2, 550.00);\n\nCOMMIT;\n-- If any step fails, ROLLBACK instead.\n```\n\n> [!TIP]\n> Notice the `SELECT ... FOR UPDATE` — this acquires a **row-level write lock**, preventing the classic *lost update* problem when two customers try to buy the last copy simultaneously.\n\n### 3.4.3 Isolation Level Justification (what to write in the report)\n\n> *\"We use the `REPEATABLE READ` isolation level (the InnoDB default in MySQL 8.0) because it eliminates dirty reads and non-repeatable reads while still allowing high concurrency for our read-heavy analytics workload. The `SELECT ... FOR UPDATE` clause further upgrades individual statements to `SELECT ... FOR UPDATE` semantics, which behaves like `READ COMMITTED` with row locks.\"*\n\n---\n\n## 3.5 Implementation Details — Section Template\n\n### 3.5.1 Server & Configuration\n\n```sql\n-- Verify storage engine\nSHOW VARIABLES LIKE 'default_storage_engine';   -- expect: InnoDB\n\n-- Character set\nSHOW VARIABLES LIKE 'character_set_database';  -- expect: utf8mb4\n\n-- Max connections\nSHOW VARIABLES LIKE 'max_connections';\n```\n\n### 3.5.2 User & Privilege Hardening (DCL)\n\n```sql\n-- Application user (least privilege)\nCREATE USER 'bookstore_app'@'localhost'\n    IDENTIFIED BY 'StrongP@ssw0rd!2024';\n\nGRANT SELECT, INSERT, UPDATE, DELETE\n    ON BookStoreDB.*\n    TO 'bookstore_app'@'localhost';\n\n-- Read-only analyst\nCREATE USER 'bookstore_ro'@'localhost'\n    IDENTIFIED BY 'ReadOnly!2024';\n\nGRANT SELECT\n    ON BookStoreDB.*\n    TO 'bookstore_ro'@'localhost';\n\nFLUSH PRIVILEGES;\n```\n\n### 3.5.3 Indexing Strategy Documentation\n\n| Index Name | Table.Column | Type | Reason | Cardinality |\n|---|---|---|---|---|\n| `pk_author` | `Author.author_id` | BTREE (PK) | Default with PK | High |\n| `idx_book_title` | `Book.title` | BTREE | Frequent `WHERE title LIKE 'H%'` | High |\n| `idx_order_date` | `Orders.order_date` | BTREE | Range queries on date | Medium |\n| `fk_book_author` | `Book.author_id` | BTREE (FK) | Auto-created by FK | Medium |\n\n> [!WARNING]\n> Do **not** over-index. Every secondary index slows down `INSERT/UPDATE/DELETE`. Always justify each index in your report.\n\n### 3.5.4 View Definition (for analytics documentation)\n\n```sql\nCREATE OR REPLACE VIEW vw_monthly_revenue AS\nSELECT\n    DATE_FORMAT(o.order_date, '%Y-%m') AS month,\n    SUM(oi.quantity * oi.unit_price)    AS revenue,\n    COUNT(DISTINCT o.order_id)          AS order_count\nFROM   Orders    o\nJOIN   OrderItem oi ON o.order_id = oi.order_id\nWHERE  o.status  <> 'CANCELLED'\nGROUP  BY DATE_FORMAT(o.order_date, '%Y-%m');\n```\n\n### 3.5.5 Backup & Recovery Policy (text template)\n\n```text\nBACKUP STRATEGY\n  - Logical backup:  mysqldump --single-transaction --routines BookStoreDB > backup_$(date).sql\n  - Frequency:       Daily at 02:00 AM (cron job)\n  - Retention:       7 daily, 4 weekly, 12 monthly\n  - Storage:         /backup/ on a separate volume; mirrored to off-site S3\n\nRECOVERY OBJECTIVES\n  - RTO (Recovery Time Objective):   30 minutes\n  - RPO (Recovery Point Objective):   1 hour (last successful backup)\n```\n\n---\n\n## 3.6 Testing Matrix (for documentation)\n\n| Test ID | Description | Input | Expected Output | Actual | Status |\n|---|---|---|---|---|---|\n| TC-01 | Insert valid book | ISBN `9788129135`, price 295 | 1 row inserted | 1 row inserted | ✅ Pass |\n| TC-02 | Insert duplicate ISBN | ISBN `9788129135` (existing) | ERROR 1062 | ERROR 1062 | ✅ Pass |\n| TC-03 | Insert book with negative price | price `-50` | ERROR 3819 (CHECK) | ERROR 3819 | ✅ Pass |\n| TC-04 | Place order for out-of-stock book | book_id 101, qty 100 | Rollback | Rolled back | ✅ Pass |\n| TC-05 | Cancel order, verify stock restore | status → CANCELLED | Stock +qty | Stock +qty | ✅ Pass |\n\n---\n\n## 3.7 Documentation Workflow (Sequential Summary)\n\n$$\n\\begin{aligned}\n\\text{Step 1:}&\\;\\;\\text{Draft objectives (Section A)} \\\\\n\\text{Step 2:}&\\;\\;\\text{Collect requirements from stakeholders} \\\\\n\\text{Step 3:}&\\;\\;\\text{Draw ER diagram (Section C.1)} \\\\\n\\text{Step 4:}&\\;\\;\\text{Convert ER → Relational schema (Section C.2)} \\\\\n\\text{Step 5:}&\\;\\;\\text{Normalize to 3NF/BCNF with proof (Section C.3)} \\\\\n\\text{Step 6:}&\\;\\;\\text{Write DDL with constraints (Section C.4)} \\\\\n\\text{Step 7:}&\\;\\;\\text{Insert sample data (Section C.5)} \\\\\n\\text{Step 8:}&\\;\\;\\text{Document constraints in matrix form (Section D)} \\\\\n\\text{Step 9:}&\\;\\;\\text{Implement transactions with ACID analysis (Section E)} \\\\\n\\text{Step 10:}&\\;\\;\\text{Record implementation, indexes, security (Section F)} \\\\\n\\text{Step 11:}&\\;\\;\\text{Write test cases & capture query outputs (Section G)} \\\\\n\\text{Step 12:}&\\;\\;\\text{Conclude with limitations and future work (Section H)}\n\\end{aligned}\n$$\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4 — Structural Diagrams & Schematics\n\n## 4.1 Master Documentation Structure (Block Diagram)\n\n```mermaid\nflowchart TD\n    A0([Database Documentation Report]) --> A1[A. Project Objectives]\n    A0 --> A2[B. Requirement Analysis]\n    A0 --> A3[C. Database Schema]\n    A0 --> A4[D. Constraints]\n    A0 --> A5[E. Transactions]\n    A0 --> A6[F. Implementation]\n    A0 --> A7[G. Testing and Validation]\n    A0 --> A8[H. Conclusion]\n\n    A3 --> A3a[ER Diagram]\n    A3 --> A3b[Relational Schema]\n    A3 --> A3c[Normalization Proof]\n    A3 --> A3d[DDL Script]\n    A3 --> A3e[Sample Data]\n\n    A4 --> A4a[Domain Integrity]\n    A4 --> A4b[Entity Integrity]\n    A4 --> A4c[Referential Integrity]\n    A4 --> A4d[User Defined Triggers]\n\n    A5 --> A5a[ACID Analysis]\n    A5 --> A5b[Isolation Levels]\n    A5 --> A5c[Concurrency Control]\n    A5 --> A5d[Sample Transaction SQL]\n\n    A6 --> A6a[Server Config]\n    A6 --> A6b[Users and Privileges]\n    A6 --> A6c[Indexes]\n    A6 --> A6d[Views and Routines]\n    A6 --> A6e[Backup Policy]\n\n    A7 --> A7a[Test Case Table]\n    A7 --> A7b[Sample Queries]\n    A7 --> A7c[Performance Snapshot]\n```\n\n## 4.2 Documentation Life-Cycle Flow\n\n```mermaid\nflowchart LR\n    P1([Project Kickoff]) --> P2[Draft Objectives]\n    P2 --> P3[Requirements Gathering]\n    P3 --> P4[ER Modeling]\n    P4 --> P5[Schema Design]\n    P5 --> P6[DDL Implementation]\n    P6 --> P7[Constraints and Triggers]\n    P7 --> P8[Transaction Design]\n    P8 --> P9[Integration and Testing]\n    P9 --> P10{All Tests Pass?}\n    P10 -- No --> P11[Iterate Fix]\n    P11 --> P7\n    P10 -- Yes --> P12[Finalize Documentation]\n    P12 --> P13[Deployment]\n    P13 --> P14([Maintenance and Updates])\n    P14 --> P3\n```\n\n## 4.3 ER Diagram — Online Book Store (Conceptual View)\n\n```mermaid\nerDiagram\n    AUTHOR ||--o{ BOOK : writes\n    CUSTOMER ||--o{ ORDERS : places\n    ORDERS ||--|{ ORDERITEM : contains\n    BOOK ||--o{ ORDERITEM : appears\n\n    AUTHOR {\n        int author_id PK\n        string first_name\n        string last_name\n        text bio\n    }\n\n    BOOK {\n        int book_id PK\n        char isbn UK\n        string title\n        int author_id FK\n        decimal price\n        int stock_qty\n        year pub_year\n    }\n\n    CUSTOMER {\n        int customer_id PK\n        string email UK\n        string full_name\n        char phone\n        date joined_on\n    }\n\n    ORDERS {\n        int order_id PK\n        int customer_id FK\n        datetime order_date\n        enum status\n        decimal total_amt\n    }\n\n    ORDERITEM {\n        int order_id PK,FK\n        int book_id PK,FK\n        int quantity\n        decimal unit_price\n    }\n```\n\n## 4.4 Constraint Classification Tree\n\n```mermaid\nflowchart TD\n    C0([Integrity Constraints]) --> C1[Domain Integrity]\n    C0 --> C2[Entity Integrity]\n    C0 --> C3[Referential Integrity]\n    C0 --> C4[User Defined Integrity]\n\n    C1 --> C1a[NOT NULL]\n    C1 --> C1b[CHECK]\n    C1 --> C1c[DEFAULT]\n\n    C2 --> C2a[PRIMARY KEY]\n\n    C3 --> C3a[FOREIGN KEY]\n    C3a --> C3a1[ON DELETE CASCADE]\n    C3a --> C3a2[ON DELETE RESTRICT]\n    C3a --> C3a3[ON DELETE SET NULL]\n\n    C4 --> C4a[TRIGGER BEFORE]\n    C4 --> C4b[TRIGGER AFTER]\n    C4 --> C4c[STORED PROCEDURE]\n```\n\n## 4.5 Transaction Execution Flow (Place-Order Use Case)\n\n```mermaid\nsequenceDiagram\n    actor User as Customer\n    participant App as Application Layer\n    participant DB as Database Engine\n\n    User->>App: Click \"Place Order\"\n    App->>DB: BEGIN TRANSACTION\n    DB-->>App: OK\n    App->>DB: SELECT stock_qty FOR UPDATE\n    DB-->>App: stock = 10\n    App->>DB: UPDATE Book SET stock = stock - 2\n    DB-->>App: 1 row affected\n    App->>DB: INSERT INTO Orders ...\n    DB-->>App: order_id = 1001\n    App->>DB: INSERT INTO OrderItem ...\n    DB-->>App: 1 row affected\n    App->>DB: COMMIT\n    DB-->>App: Success\n    App-->>User: Order Confirmation\n    Note over DB: If any step fails → ROLLBACK<br/>and DB returns to original state\n```\n\n## 4.6 Documentation Stakeholder View\n\n```mermaid\nflowchart LR\n    subgraph Stakeholders\n        S1[Customer]\n        S2[Store Admin]\n        S3[Data Analyst]\n        S4[DBA]\n        S5[External Auditor]\n    end\n\n    subgraph DocLayers\n        D1[User Manual Layer]\n        D2[Admin Guide Layer]\n        D3[Analytics Query Catalog]\n        D4[Technical Schema Reference]\n        D5[Compliance and Audit Trail]\n    end\n\n    S1 --> D1\n    S2 --> D2\n    S3 --> D3\n    S4 --> D4\n    S5 --> D5\n\n    D4 --> D2\n    D4 --> D3\n    D2 --> D1\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n## 📘 PART A — Short Answer Questions (3 Marks Each)\n\n### **Question 1 (3 Marks)** `[KTU University Exam – May 2024]`\n\n**List and briefly explain any SIX essential components of a comprehensive database documentation report.**\n\n**Model Answer (6 points × 0.5 = 3 marks):**\n\n1. **Project Objectives & Scope** — Defines the *why* of the system; includes problem statement, functional & non-functional goals, and stakeholder list.\n2. **Requirement Analysis** — Captures user requirements formally (use-cases, user stories) and data requirements (entities, attributes).\n3. **ER / Relational Schema** — Visual and tabular representation of the database structure including entities, relationships, keys, and cardinalities.\n4. **Normalization Proof** — Step-by-step demonstration that the design satisfies 1NF, 2NF, and 3NF (or BCNF), eliminating redundancy and update anomalies.\n5. **Constraints Documentation** — Enumerates PRIMARY KEY, FOREIGN KEY, UNIQUE, CHECK, NOT NULL, and TRIGGER rules that protect data integrity, with a justification table.\n6. **Transaction Specifications** — Describes ACID guarantees, isolation level chosen, sample `BEGIN ... COMMIT` blocks, and concurrency-control strategy.\n\n> [!NOTE]\n> Examiners award **0.5 mark per correct component** with brief explanation. Total = 3 marks.\n\n---\n\n### **Question 2 (3 Marks)** `[KTU University Exam – Dec 2023]`\n\n**Differentiate between the `CHECK` constraint and a `TRIGGER` in SQL. When would you prefer one over the other? Provide one example of each.**\n\n**Model Answer:**\n\n| Aspect | `CHECK` Constraint | `TRIGGER` |\n|---|---|---|\n| **Scope** | Single row, single table (intra-row, intra-table) | Can span multiple rows, multiple tables, and even other DBs |\n| **Execution Time** | Automatically evaluated on `INSERT`/`UPDATE` | `BEFORE` / `AFTER` event on `INSERT`/`UPDATE`/`DELETE` |\n| **Complexity** | Simple boolean expressions only | Can contain procedural logic (loops, conditionals, multiple DML) |\n| **Performance** | Optimized by the query engine | May have overhead; requires careful indexing |\n| **Use When** | Domain rule is local and static | Cross-table audit, log, or temporal rule |\n\n**Example – `CHECK`:**\n```sql\nCONSTRAINT chk_age CHECK (age >= 18);\n```\n\n**Example – `TRIGGER`:**\n```sql\nCREATE TRIGGER trg_log_salary_change\nAFTER UPDATE ON Employee\nFOR EACH ROW\n    INSERT INTO SalaryAudit(emp_id, old_sal, new_sal, change_dt)\n    VALUES (OLD.emp_id, OLD.salary, NEW.salary, NOW());\n```\n\n> [!TIP]\n> **Exam Tip:** If a question asks *when to prefer a TRIGGER*, mention that triggers can **enforce business rules that span multiple tables** (e.g., updating inventory in one table when an order is placed in another), which `CHECK` cannot do.\n\n---\n\n## 📗 PART B — Long Answer Questions (14 Marks Each)\n\n> **Internal Choice Pattern:** KTU allows the student to answer **EITHER** Question A **OR** Question B.\n\n---\n\n### **Question A (14 Marks)** `[KTU University Exam – May 2024 Model]`\n\n> *\"You are tasked with designing and documenting a **Library Management System** for a college. Prepare a comprehensive documentation covering:*\n> *(a) Project objectives, ER diagram, relational schema with constraints, and normalization proof for the system. (7 marks)*\n> *(b) Transaction design with ACID analysis, implementation details including indexes, views, and backup policy. (7 marks)\"*\n\n#### Part (a) — Model Solution (7 marks)\n\n**1. Project Objectives (1 mark):**\n- **Functional:** Maintain catalogue of books, track members, issue/return transactions, fine calculation, search by title/author/ISBN.\n- **Non-Functional:** Response time ≤ 1 s for search, support 500 concurrent users, 99 % uptime.\n- **Stakeholders:** Librarian, Member, Admin, Auditor.\n- **Tech Stack:** MySQL 8.0, Python Flask, draw.io.\n\n**2. ER Diagram (2 marks):** (textual description for answer sheet)\n- Entities: `Book`, `Author`, `Publisher`, `Member`, `Loan` (transaction).\n- Relationships:\n    - `Author` *writes* `Book` (1:M).\n    - `Publisher` *publishes* `Book` (1:M).\n    - `Member` *takes* `Loan` (1:M).\n    - `Book` *appears in* `Loan` (1:M).\n- Attributes with PK marked.\n\n**3. Relational Schema with Constraints (2 marks):**\n\n```sql\nBook (book_id PK, isbn UNIQUE, title, author_id FK→Author, publisher_id FK→Publisher,\n      price CHECK > 0, stock CHECK ≥ 0)\n\nAuthor (author_id PK, first_name, last_name)\n\nPublisher (publisher_id PK, name UNIQUE, city)\n\nMember (member_id PK, email UNIQUE, name, phone CHECK REGEXP, join_date)\n\nLoan (loan_id PK, member_id FK→Member, book_id FK→Book,\n      issue_date, due_date, return_date NULL,\n      CHECK (due_date > issue_date))\n```\n\n**4. Normalization Proof (2 marks):**\n- **1NF:** All attributes atomic (e.g., no multi-valued author).\n- **2NF:** All non-key attributes fully dependent on PK (e.g., `Book.title` depends on `book_id`, not on partial key).\n- **3NF:** No transitive dependencies (e.g., `Book.publisher_city` would be transitive if stored in `Book`; hence moved to `Publisher` table).\n\n#### Part (b) — Model Solution (7 marks)\n\n**1. Transaction Design with ACID (3 marks):**\n```sql\nSTART TRANSACTION;\n    -- Verify availability\n    SELECT stock FROM Book WHERE book_id = 101 FOR UPDATE;\n    -- Issue loan\n    INSERT INTO Loan(member_id, book_id, issue_date, due_date)\n    VALUES (501, 101, CURDATE(), DATE_ADD(CURDATE(), INTERVAL 14 DAY));\n    UPDATE Book SET stock = stock - 1 WHERE book_id = 101;\nCOMMIT;\n```\n- **Atomicity:** All four steps commit together or roll back together.\n- **Consistency:** Stock and Loan tables remain synchronized.\n- **Isolation:** `FOR UPDATE` prevents two members from issuing the last copy.\n- **Durability:** Once committed, loan survives crashes (logged in InnoDB redo log).\n\n**2. Implementation Details (3 marks):**\n\n| Element | Specification |\n|---|---|\n| Engine | InnoDB (transactional, FK-aware) |\n| Indexes | `idx_book_title`, `idx_member_email`, `idx_loan_due` |\n| View | `vw_overdue_loans` (filter `return_date IS NULL AND due_date < CURDATE()`) |\n| Backup | Daily `mysqldump` + binary log archiving |\n| Security | `bookstore_app` user with limited `SELECT/INSERT/UPDATE/DELETE` |\n\n**3. Stored Procedure Example (1 mark):**\n```sql\nDELIMITER //\nCREATE PROCEDURE sp_issue_book(IN p_member INT, IN p_book INT)\nBEGIN\n    DECLARE v_stock INT;\n    SELECT stock INTO v_stock FROM Book WHERE book_id = p_book FOR UPDATE;\n    IF v_stock > 0 THEN\n        INSERT INTO Loan(member_id, book_id, issue_date, due_date)\n        VALUES (p_member, p_book, CURDATE(),\n                DATE_ADD(CURDATE(), INTERVAL 14 DAY));\n        UPDATE Book SET stock = stock - 1 WHERE book_id = p_book;\n    ELSE\n        SIGNAL SQLSTATE '45000'\n            SET MESSAGE_TEXT = 'Book out of stock';\n    END IF;\nEND //\nDELIMITER ;\n```\n\n---\n\n### **Question B (14 Marks)** `[KTU University Exam – Dec 2023 Model]`\n\n> *\"For a **Hospital Management System**, prepare a documentation report covering:*\n> *(a) Comprehensive database schema design with at least FIVE tables, all categories of integrity constraints, and a normalized design up to 3NF. (7 marks)*\n> *(b) Transaction handling for patient admission, concurrency control strategy, and a complete implementation plan including roles, indexing, and disaster recovery. (7 marks)\"*\n\n#### Part (a) — Model Solution (7 marks)\n\n**1. Schema Design (3 marks):** Five tables — `Patient`, `Doctor`, `Department`, `Admission`, `Treatment`.\n\n```sql\nPatient(patient_id PK, ssn UNIQUE, name, dob, gender CHECK IN ('M','F','O'),\n        phone, address)\n\nDoctor(doctor_id PK, license_no UNIQUE, name, dept_id FK→Department,\n       joining_date, salary CHECK > 0)\n\nDepartment(dept_id PK, name UNIQUE, head_doctor_id FK→Doctor NULL)\n\nAdmission(admission_id PK, patient_id FK→Patient, doctor_id FK→Doctor,\n          admit_date, discharge_date NULL,\n          CHECK (discharge_date IS NULL OR discharge_date >= admit_date))\n\nTreatment(treatment_id PK, admission_id FK→Admission, description,\n          cost CHECK > 0, treatment_date)\n```\n\n**2. Constraint Coverage (2 marks):**\n\n| Category | Implementation |\n|---|---|\n| Domain | `CHECK (gender IN ...)`, `CHECK (salary > 0)`, `CHECK (cost > 0)` |\n| Entity | `PRIMARY KEY` on every table |\n| Referential | `FOREIGN KEY` with `ON DELETE RESTRICT` for safety |\n| User-defined | TRIGGER preventing admission if doctor's `joining_date > admit_date` |\n| Key | `UNIQUE` on `ssn`, `license_no`, department `name` |\n\n**3. Normalization (2 marks):**\n- **1NF:** All attributes atomic.\n- **2NF:** No partial dependency (all PKs are single-column surrogates → trivially satisfied).\n- **3NF:** `Doctor.dept_name` would be a transitive dependency via `Department`; we therefore store only `dept_id` FK in `Doctor`. → **3NF achieved**.\n\n#### Part (b) — Model Solution (7 marks)\n\n**1. Patient Admission Transaction (3 marks):**\n\n```sql\nSTART TRANSACTION;\n    -- Acquire bed lock\n    SELECT available_beds FROM Department\n     WHERE dept_id = 5 FOR UPDATE;\n\n    INSERT INTO Admission(patient_id, doctor_id, admit_date)\n    VALUES (101, 22, NOW());\n\n    UPDATE Department\n       SET available_beds = available_beds - 1\n     WHERE dept_id = 5;\nCOMMIT;\n```\n\n- **ACID:** `FOR UPDATE` ensures exclusive access to bed count; rollback on failure restores bed count.\n- **Isolation Level:** `SERIALIZABLE` recommended for admission to prevent phantom read on bed availability.\n\n**2. Concurrency Control Strategy (2 marks):**\n- Pessimistic locking via `SELECT ... FOR UPDATE` on shared resources (beds, equipment).\n- Optimistic concurrency for read-heavy reports using version columns.\n- Deadlock detection: retry on `ER_LOCK_DEADLOCK` with exponential backoff.\n\n**3. Implementation Plan (2 marks):**\n\n| Aspect | Decision |\n|---|---|\n| **Roles** | `hms_app` (RW on operational tables), `hms_ro` (read-only analyst), `hms_dba` (DDL + backup) |\n| **Indexes** | `idx_patient_ssn`, `idx_admission_date`, `idx_doctor_dept` |\n| **Views** | `vw_current_inpatients`, `vw_doctor_workload` |\n| **Backup** | Nightly full + every-15-min incremental binary logs; off-site replication |\n| **Disaster Recovery** | RTO 1 hour, RPO 15 minutes; tested quarterly |\n\n---\n\n## ⚠️ KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mark-Deduction Traps**\n> 1. **Missing the \"Why\"** — Writing *what* a constraint is, but not *why* it is chosen (e.g., `ON DELETE CASCADE` vs. `RESTRICT` must be justified).\n> 2. **No traceability** — A trigger in section E that does not map to a requirement in section B loses 1–2 marks.\n> 3. **Un-normalized schema** — Leaving transitive dependencies (e.g., storing `dept_name` in `Doctor`) → direct 2-mark penalty.\n> 4. **No ACID mention in transactions** — Simply writing `COMMIT` without explaining *which* property is satisfied and *how* costs marks.\n> 5. **Inconsistent cardinality** — ER diagram showing 1:1 but relational schema using a 1:M junction table.\n> 6. **Plagiarized screenshots** — Identical tool outputs across multiple students' reports are flagged.\n> 7. **No backup policy** — A DBMS report without a recovery plan is considered incomplete by senior examiners.\n> 8. **Forgetting to revoke** — Granting privileges but not documenting the revocation strategy is a security-documentation gap.\n\n---\n\n## 🧠 Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- ✅ **Documentation is a deliverable**, not an afterthought. It carries direct marks in KTU evaluation.\n- ✅ Always follow the order: **Objectives → Requirements → ER → Schema → Normalization → DDL → Constraints → Transactions → Implementation → Testing → Conclusion**.\n- ✅ The **ER diagram is the single most important figure** in the report. Make it large, clear, and well-labeled.\n- ✅ Every **non-trivial DDL line** in your schema must be accompanied by a one-line justification.\n- ✅ Constraints must be **tabulated** with: *name, type, table.column, business rule, SQL clause*.\n- ✅ A complete transaction section explains **ACID** properties, not just `BEGIN ... COMMIT`.\n- ✅ Always specify the **isolation level** used and justify it.\n- ✅ Indexes must be **justified** — never create an index \"just in case.\"\n- ✅ Backup policy must mention **frequency, retention, RTO, and RPO**.\n- ✅ Test cases must include **negative scenarios** (e.g., duplicate insert, FK violation).\n- ✅ Use **IEEE citation style** for any external reference (textbook, website).\n- ✅ Keep the report **30–50 pages** for a mini-project; **70–100 pages** for the main project.\n- ✅ Always include the **plagiarism declaration** and **certificate page** signed by guide and HOD.\n- ✅ Final-check: ER diagram ↔ Relational schema ↔ DDL ↔ Sample data — *all four must be consistent*.\n- ✅ The phrase **\"all mandatory fields are NOT NULL\"** in your report is acceptable, but **listing which fields and why** scores higher.\n- ✅ Remember: *If it is not documented, it does not exist — in the eyes of the examiner.*\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "projecttopicsuggestionssampleprojectideasfordatabasemanagementsystems": {
      "title": "Project Topic Suggestions: Sample project ideas for database management systems",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 4\n> - **Topic:** Project Topic Suggestions: Sample project ideas for database management systems\n\n<!-- SECTION_1_START -->\n# SECTION 1 — Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Context)\n\nIn the **KTU 2024 Scheme (NEP 2020 aligned)** for the course **PBITT304 – Database Management System**, a *DBMS Mini-Project* is defined as a **comprehensive, end-to-end software engineering artefact** that demonstrates the practical application of database design, normalization, SQL transaction handling, and application-database integration using a real-world problem statement.\n\nA typical KTU DBMS project must encompass all four phases of the database application lifecycle:\n\n$$\\text{DBMS Project} = \\{\\text{Requirement Analysis},\\ \\text{Conceptual Design},\\ \\text{Logical Design},\\ \\text{Physical \\& Application Layer}\\}$$\n\n> [!IMPORTANT]\n> **KTU 2024 Scheme Mandate:** The mini-project carries significant weight in the internal assessment. It must demonstrate **front-end + back-end + database** integration. The back-end is *strictly* a relational DBMS (Oracle, MySQL, PostgreSQL, or SQLite). The front-end can be Java/Python/PHP/HTML-CSS-JS.\n\n## 1.2 Conceptual Analogy — The Restaurant Kitchen Model\n\nImagine a **restaurant kitchen** as a DBMS project:\n\n| Kitchen Component | DBMS Project Equivalent | Real-World Role |\n|---|---|---|\n| Waiter (takes order) | **Front-end (GUI / Web form)** | Collects user input |\n| Recipe Book | **Database Schema (Tables, ER Diagram)** | Defines what data is stored |\n| Pantry / Fridge | **DBMS Engine (MySQL, Oracle)** | Physically stores and retrieves data |\n| Chef | **Application Logic (Java / Python business methods)** | Processes data, applies business rules |\n| Bill / Receipt | **Reports & Queries (SQL SELECT with aggregates)** | Displays processed information to the user |\n\nJust as a restaurant cannot operate without a coherent flow from *waiter → kitchen → pantry*, a DBMS project fails if the **three-tier architecture** (Presentation → Logic → Data) is not implemented properly.\n\n> [!NOTE]\n> **Why Project Selection Matters in KTU:** Examiners and project coordinators evaluate projects on (a) real-world relevance, (b) complexity of schema (typically 6–12 tables), (c) normalization up to **3NF/BCNF**, and (d) presence of **ACID transactions**, **views**, **triggers**, and **stored procedures**.\n\n## 1.3 Categories of DBMS Mini-Projects (KTU 2024 Approved Domains)\n\nThe KTU DBMS syllabus categorizes student project ideas into the following functional domains:\n\n1. **Academic & Examination Systems** — College automation, result processing\n2. **Healthcare & Hospital Management** — Patient records, pharmacy, appointments\n3. **E-Commerce & Retail** — Online shopping, inventory, billing\n4. **Transportation & Logistics** — Bus reservation, courier tracking\n5. **Banking & Finance** — Loan management, ATM simulator, payroll\n6. **Hospitality & Tourism** — Hotel booking, tour packages\n7. **Library & Knowledge Management** — Library automation, plagiarism systems\n8. **Social / Civic** — Blood bank, complaint management, smart city services\n9. **Agriculture / Supply Chain** — Crop procurement, warehouse management\n10. **Emerging Tech-Integrated** — Blockchain ledger DB, IoT sensor data store\n\n> [!IMPORTANT]\n> **Examiner's Heuristic:** Choose a project that is *neither too trivial* (e.g., student attendance with 2 tables) nor *unrealistically complex* (e.g., full Amazon clone with microservices). The **sweet spot** is a system with **8–12 normalized tables**, 3–5 triggers, 2–3 stored procedures, and at least one analytical report.\n\n---\n\n## 1.4 Anatomy of a KTU-Accepted DBMS Project\n\nA complete KTU 2024 Scheme DBMS mini-project deliverable must contain:\n\n$$\\text{Deliverables} = \\begin{cases} \\text{1. Synopsis (Problem Statement + Objectives)} \\\\ \\text{2. Software Requirement Specification (SRS)} \\\\ \\text{3. ER Diagram \\& Relational Schema} \\\\ \\text{4. Normalization Proof (1NF $\\to$ 2NF $\\to$ 3NF)} \\\\ \\text{5. DDL + DML + DCL Script File} \\\\ \\text{6. Front-end Source Code (Form-based)} \\\\ \\text{7. Test Cases \\& Screenshots} \\\\ \\text{8. Conclusion \\& Future Enhancements} \\end{cases}$$\n\nEach artifact maps to a specific **Course Outcome (CO)** of PBITT304, typically **CO4** (Design and implement a database system for a real-world problem).\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2 — Deep Theoretical Analysis & High-Yield Project Selection Matrix\n\n## 2.1 The Project Selection Decision Framework\n\nChoosing the right project is governed by four engineering trade-offs. A KTU-accepted project must balance:\n\n| Decision Parameter | Low Score (Reject) | High Score (Accept) | Weight |\n|---|---|---|---|\n| **Domain Familiarity** | Unknown domain | Daily-life relatable | **25%** |\n| **Schema Complexity** | 1–3 tables (trivial) | 8–15 tables (rich FK chains) | **30%** |\n| **Advanced SQL Usage** | Only `SELECT/INSERT` | Joins, subqueries, views, triggers, cursors | **25%** |\n| **Demonstrability** | Cannot show in 10 min demo | Has login, dashboard, reports | **20%** |\n\nA composite suitability score can be computed as:\n\n$$S_{\\text{project}} = 0.25 D + 0.30 C + 0.25 A + 0.20 M$$\n\nwhere $D$ = Domain familiarity, $C$ = Schema complexity, $A$ = Advanced SQL usage, $M$ = Demonstrability (all on 0–10 scale).\n\n> A project with $S_{\\text{project}} \\geq 7.5$ is **strongly recommended** for KTU submission.\n\n## 2.2 The Four Pillars of Every KTU DBMS Project\n\n### Pillar 1 — The Conceptual Layer (ER Model)\n\nEvery project **must** begin with an **Entity-Relationship (ER) diagram**. The cardinality decisions drive the entire schema.\n\n$$\\text{Entities} \\xrightarrow{\\text{1:M / M:N}} \\text{Relationships} \\xrightarrow{\\text{Mapping}} \\text{Relational Tables}$$\n\n### Pillar 2 — The Logical Layer (Normalization)\n\nThe KTU examiner expects the project schema to be in at least **Third Normal Form (3NF)**. The proof of normalization must be shown for at least 3–4 of the largest tables.\n\n| Normal Form | Rule | Why It Matters |\n|---|---|---|\n| **1NF** | Atomic values, no repeating groups | Eliminates multi-valued attributes |\n| **2NF** | 1NF + no partial dependency | Removes redundancy from composite keys |\n| **3NF** | 2NF + no transitive dependency | Eliminates update anomalies |\n| **BCNF** | Every determinant is a candidate key | Strictest form, used for complex projects |\n\n### Pillar 3 — The Physical Layer (DDL + Indexing)\n\nTables must be created with **appropriate constraints**:\n\n- `PRIMARY KEY`, `FOREIGN KEY` (with `ON DELETE CASCADE`)\n- `UNIQUE`, `NOT NULL`, `CHECK` constraints\n- `INDEX` on frequently searched columns\n- `AUTO_INCREMENT` / `SERIAL` for surrogate keys\n\n### Pillar 4 — The Application Layer (Front-end + Business Logic)\n\nThe front-end (Java Swing / HTML-PHP / Python Tkinter) must implement:\n\n$$\\text{CRUD Operations} = \\{Create, Read, Update, Delete\\}$$\n\nalong with **search filters**, **report generation**, and **user authentication**.\n\n## 2.3 KTU 2024 — Project Complexity Tier Sheet\n\n| Tier | Table Count | Trigger Count | Stored Proc | Best For |\n|---|---|---|---|---|\n| **Basic** | 4–6 | 0–1 | 0 | Beginners (rarely accepted) |\n| **Standard (Recommended)** | 8–12 | 3–5 | 2–3 | Most KTU submissions |\n| **Advanced** | 13–20 | 6+ | 4+ | Distinction-grade projects |\n\n> [!IMPORTANT]\n> **Standard Tier** projects are what the KTU project coordinator expects from an average B.Tech student. Going beyond 15 tables without a strong reason often *hurts* rather than helps, because it increases the probability of schema design errors.\n\n## 2.4 Why Project Selection Has Real Engineering Utility\n\nIn the industry, the *exact same* workflow (Requirements → ER → Normalization → SQL → Application) is followed to build:\n\n- **Banking core systems** (HDFC, SBI)\n- **Hospital Information Systems** (Apollo, KIMS)\n- **E-commerce platforms** (Flipkart, Amazon back-end)\n- **ERP systems** (SAP, Oracle E-Business Suite)\n\nThus, mastering the KTU DBMS project directly maps to industry roles like *Junior Database Developer*, *Backend Engineer*, and *Data Engineer*.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3 — Step-by-Step Sample Project Blueprints & Analytical Matrix\n\n## 3.1 Sample Project Idea #1 — **Hospital Management System (HMS)**\n\nThis is the **most chosen** KTU project. The blueprint below is a complete analytical walkthrough.\n\n### Step 1 — Identify Entities\n\nFrom the problem statement *\"Manage patients, doctors, appointments, billing, pharmacy, and medical records\"*, we extract:\n\n$$\\text{Entities} = \\{\\text{Patient},\\ \\text{Doctor},\\ \\text{Department},\\ \\text{Appointment},\\ \\text{Bill},\\ \\text{Prescription},\\ \\text{Room},\\ \\text{Medicine},\\ \\text{Medical\\_Record}\\}$$\n\n### Step 2 — Identify Attributes (sample for Patient)\n\n$$\\text{Patient}(\\underline{\\text{PatientID}},\\ \\text{Name},\\ \\text{DOB},\\ \\text{Gender},\\ \\text{Phone},\\ \\text{Address},\\ \\text{BloodGroup})$$\n\nUnderline indicates the **primary key**.\n\n### Step 3 — Identify Relationships\n\n| Relationship | Cardinality | Justification |\n|---|---|---|\n| Patient — Appointment | **1 : M** | One patient books many appointments |\n| Doctor — Appointment | **1 : M** | One doctor handles many appointments |\n| Patient — Medical_Record | **1 : 1** | Each patient has one consolidated record |\n| Bill — Patient | **M : 1** | Many bills can be linked to one patient visit cycle |\n| Doctor — Department | **M : 1** | Many doctors belong to one department |\n| Patient — Room | **M : 1** | Many patients can occupy a room over time |\n| Prescription — Medicine | **M : N** | One prescription has many medicines; one medicine in many prescriptions |\n\n### Step 4 — Normalization Verification (for `Prescription` table)\n\nInitial unnormalized form (UNF):\n\n$$\\text{UNF: Prescription}(\\text{PresID},\\ \\text{Date},\\ \\text{DoctorID},\\ \\text{PatientID},\\ \\text{MedicineList})$$\n\nwhere `MedicineList` is multi-valued → **violates 1NF**.\n\n**Convert to 1NF** — split the multi-valued attribute:\n\n$$\n\\begin{aligned}\n\\text{Prescription}(\\underline{\\text{PresID}},\\ \\text{Date},\\ \\text{DoctorID},\\ \\text{PatientID}) \\\\\n\\text{Prescription\\_Medicine}(\\underline{\\text{PresID}},\\ \\underline{\\text{MedicineID}},\\ \\text{Dosage},\\ \\text{Duration})\n\\end{aligned}\n$$\n\n**Convert to 2NF** — composite key `(PresID, MedicineID)` has no partial dependencies because all non-key attributes (`Dosage`, `Duration`) depend on the *full* composite key.\n\n**Convert to 3NF** — no transitive dependencies exist; the schema is in **3NF** ✓.\n\n### Step 5 — Critical SQL Components (KTU expects these in code)\n\n1. **Trigger — Auto-update room status on patient discharge:**\n\n   ```sql\n   CREATE TRIGGER trg_room_free\n   AFTER UPDATE OF DischargeDate ON Patient\n   FOR EACH ROW\n   BEGIN\n       UPDATE Room SET Status = 'Available'\n       WHERE RoomID = OLD.RoomID;\n   END;\n   ```\n\n2. **View — Doctor's daily appointment summary:**\n\n   ```sql\n   CREATE VIEW vw_DoctorDailySchedule AS\n   SELECT d.Name AS Doctor, a.AppointmentDate,\n          COUNT(a.AppointmentID) AS TotalPatients\n   FROM Doctor d\n   JOIN Appointment a ON d.DoctorID = a.DoctorID\n   GROUP BY d.DoctorID, a.AppointmentDate;\n   ```\n\n3. **Stored Procedure — Generate bill for a patient visit:**\n\n   ```sql\n   CREATE PROCEDURE sp_GenerateBill(IN pid INT, OUT total DECIMAL(10,2))\n   BEGIN\n       SELECT SUM(ConsultFee + MedicineCost + RoomCharges)\n       INTO total\n       FROM Bill\n       WHERE PatientID = pid;\n   END;\n   ```\n\n---\n\n## 3.2 Sample Project Idea #2 — **Online Bus Reservation System**\n\n### Analytical Mapping\n\n| Element | Specification |\n|---|---|\n| **Entities** | Bus, Route, Stop, Schedule, Booking, Passenger, Payment |\n| **Key Operations** | Seat availability check, ticket booking, cancellation, refund calculation |\n| **Critical Trigger** | Auto-decrement available seats on `INSERT` into `Booking` |\n| **Critical View** | `vw_RouteRevenue` — aggregates revenue per route per month |\n| **Stored Procedure** | `sp_BookTicket(passengerID, scheduleID, seatNo)` with seat-availability validation |\n\nThe **M : N** relationship is between `Bus` and `Route` (a bus runs on multiple routes, a route has multiple buses), resolved by the bridge table `Schedule`.\n\n---\n\n## 3.3 Sample Project Idea #3 — **Library Management System**\n\nThe classical KTU project. Schema highlights:\n\n- `Book` (BookID, ISBN, Title, Publisher, Year, CategoryID)\n- `Author` (AuthorID, Name, Bio)\n- `Book_Author` (BookID, AuthorID) ← resolves **M:N**\n- `Member` (MemberID, Name, Type, JoinDate)\n- `Issue` (IssueID, BookID, MemberID, IssueDate, DueDate, ReturnDate)\n- `Fine` (FineID, IssueID, Amount, Reason)\n\n**Business Rule:** If `ReturnDate > DueDate`, compute fine at **₹2/day** and auto-insert into `Fine` table via a trigger.\n\n$$\\text{FineAmount} = \\max(0,\\ \\text{ReturnDate} - \\text{DueDate}) \\times 2$$\n\n---\n\n## 3.4 Sample Project Idea #4 — **E-Commerce Inventory & Order System**\n\nThis project enables demonstration of **transaction control**:\n\n```sql\nSTART TRANSACTION;\n   UPDATE Product SET Stock = Stock - p_Qty WHERE ProductID = p_ProductID;\n   INSERT INTO Orders(CustomerID, ProductID, Quantity, OrderDate)\n   VALUES (p_CustomerID, p_ProductID, p_Qty, CURDATE());\n   INSERT INTO Payment(OrderID, Amount, Status) VALUES (LAST_INSERT_ID(), p_Total, 'Pending');\nCOMMIT;\n```\n\nThis single block satisfies the KTU requirement of demonstrating **ACID properties** (Atomicity, Consistency, Isolation, Durability).\n\n---\n\n## 3.5 Sample Project Idea #5 — **University Examination & Result Processing System**\n\nHighly relevant to KTU itself! Schema covers:\n\n- `Student`, `Course`, `Exam`, `Marks`, `Grade`, `Result`\n- Generates **SGPA/CGPA** using stored functions\n- Implements **role-based access** (Student / Faculty / Admin)\n\n$$SGPA = \\frac{\\sum_{i=1}^{n} (C_i \\times G_i)}{\\sum_{i=1}^{n} C_i}$$\n\nwhere $C_i$ = credit of course $i$, $G_i$ = grade point of course $i$, $n$ = number of courses in the semester.\n\n---\n\n## 3.6 Comparative Selection Matrix (For Quick Decision)\n\n| Project | Difficulty | Tables | Best Front-end Pair | Uniqueness |\n|---|---|---|---|---|\n| Hospital Management | ★★★ | 10–12 | Java Swing + MySQL | High (most chosen) |\n| Bus Reservation | ★★ | 7–9 | PHP + MySQL | Medium |\n| Library Management | ★★ | 6–8 | Python Tkinter + SQLite | Medium (overdone) |\n| E-Commerce | ★★★★ | 12–15 | Java Spring Boot + MySQL | High |\n| Exam Result System | ★★★ | 8–10 | HTML-CSS-JS + PHP | Very High (KTU-relevant) |\n| Blood Bank Management | ★★ | 6–8 | Java + Oracle | High (social impact) |\n| Payroll Management | ★★★ | 8–10 | C# .NET + SQL Server | Medium |\n| Smart City Complaint | ★★★★ | 11–14 | Node.js + MongoDB/SQL | Very High (emerging) |\n| Crop Procurement | ★★★ | 9–11 | Python Django + PostgreSQL | High (agri-domain) |\n| Blockchain Ledger DB | ★★★★★ | Conceptual | Python + SQLite | Too advanced for KTU mini-project |\n\n> [!IMPORTANT]\n> **KTU 2024 Sweet Spot Recommendation:** Choose a project that is in the **★★★ difficulty range** with **8–12 tables** and a front-end that you can fully demonstrate in under 10 minutes. *Hospital Management* and *Exam Result Processing* are the two most defensible choices.\n\n---\n\n## 3.7 Project Topic Submission Checklist (Self-Evaluation)\n\nBefore finalizing, verify each box:\n\n- [x] Problem statement is **real and relatable**\n- [x] ER diagram has at least **7 entities**\n- [x] Schema is normalized to **3NF** (with written proof)\n- [x] At least **3 triggers** implemented\n- [x] At least **2 views** created\n- [x] At least **2 stored procedures/functions**\n- [x] Front-end supports **login, CRUD, and 1 report**\n- [x] Sample data is loaded (at least **20 rows per major table**)\n- [x] Demonstration script is rehearsed\n- [x] Report is in the **KTU prescribed format** (Title, Certificate, Abstract, TOC, Chapters, References)\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4 — Structural Diagrams & Schematics\n\n## 4.1 Three-Tier Architecture of a KTU DBMS Project\n\n```mermaid\nflowchart TB\n    user1([End User])\n    pres[\"Presentation Layer: HTML CSS JavaScript or Java Swing or Python Tkinter\"]\n    logic[\"Application Layer: Business Logic PHP Java Python\"]\n    dbms[\"Data Layer: MySQL or Oracle or PostgreSQL\"]\n    user1 --> pres\n    pres --> logic\n    logic --> dbms\n    dbms --> logic\n    logic --> pres\n    pres --> user1\n```\n\n**Reading the diagram:** The user interacts with the presentation layer; data flows down through the application logic to the DBMS and back up. This is the **mandatory architecture** for any KTU DBMS project.\n\n---\n\n## 4.2 ER Diagram — Hospital Management System (Block-Level)\n\n```mermaid\nerDiagram\n    DEPARTMENT ||--o{ DOCTOR : employs\n    DOCTOR ||--o{ APPOINTMENT : handles\n    PATIENT ||--o{ APPOINTMENT : books\n    PATIENT ||--|| MEDICAL_RECORD : owns\n    PATIENT ||--o{ BILL : receives\n    APPOINTMENT ||--o{ PRESCRIPTION : produces\n    PRESCRIPTION }o--o{ MEDICINE : contains\n    ROOM ||--o{ PATIENT : admits\n    DOCTOR ||--o{ PRESCRIPTION : writes\n```\n\n**Notation (Crow's Foot):**\n- `||` = exactly one (mandatory participation)\n- `o{` = zero or many\n- `}o--o{` = many-to-many relationship\n\n---\n\n## 4.3 Project Development Workflow\n\n```mermaid\nflowchart LR\n    A[Requirement Gathering] --> B[ER Diagram]\n    B --> C[Relational Schema]\n    C --> D[Normalization 1NF 2NF 3NF]\n    D --> E[DDL Script Table Creation]\n    E --> F[DML Script Sample Data]\n    F --> G[Triggers and Views]\n    G --> H[Front End Forms]\n    H --> I[Integration Testing]\n    I --> J[Final Report and Demo]\n```\n\n---\n\n## 4.4 Module Interaction Map — Sample Project Components\n\n```mermaid\nflowchart TB\n    subgraph Authentication\n        login[Login Form]\n        role[Role Detection Student or Faculty or Admin]\n    end\n    subgraph Database Tables\n        t1[Users]\n        t2[Master Data]\n        t3[Transaction Tables]\n        t4[Audit Log]\n    end\n    subgraph Reports\n        r1[Tabular Report]\n        r2[Aggregate Report]\n        r3[Export to PDF]\n    end\n    login --> role\n    role --> t1\n    t1 --> t2\n    t2 --> t3\n    t3 --> t4\n    t3 --> r1\n    t3 --> r2\n    r1 --> r3\n```\n\n---\n\n## 4.5 Decision Tree — \"Which Project Should I Choose?\"\n\n```mermaid\nflowchart TD\n    start[Start: Choose Project]\n    q1{Comfort with Java or Python or Web?}\n    q2{Domain Interest?}\n    q3{Complexity Needed for Higher Marks?}\n    end1[Pick E-Commerce or Exam System]\n    end2[Pick Library or Bus Reservation]\n    end3[Pick Hospital or Payroll]\n    end4[Pick Blood Bank or Crop System]\n\n    start --> q1\n    q1 -->|Web Tech| q2\n    q1 -->|Java Python| q3\n    q2 -->|Education| end1\n    q2 -->|Transport| end2\n    q3 -->|High| end3\n    q3 -->|Medium| end4\n```\n\n---\n\n## 4.6 Block-Level Functional Architecture (For Complex Projects)\n\n```mermaid\nflowchart TB\n    subgraph InputStage[Input Stage]\n        i1[User Registration Form]\n        i2[Data Entry Form]\n        i3[Search and Filter Form]\n    end\n    subgraph ValidationStage[Validation and Processing Stage]\n        v1[Client Side Validation JavaScript]\n        v2[Server Side Business Logic PHP Java]\n        v3[SQL Stored Procedures]\n    end\n    subgraph StorageStage[Persistent Storage Stage]\n        s1[Normalized Relational Tables]\n        s2[Indexed Search Columns]\n        s3[Trigger Maintained Audit Tables]\n    end\n    subgraph OutputStage[Output and Reporting Stage]\n        o1[Dashboard View]\n        o2[SQL View Based Reports]\n        o3[PDF Excel Export]\n    end\n    InputStage --> ValidationStage\n    ValidationStage --> StorageStage\n    StorageStage --> OutputStage\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n## 5.1 PART A — Short Answer Questions (3 Marks Each)\n\n### Q1. **[KTU University Exam — Dec 2023, Model Paper]**\n**\"List and briefly explain any three real-world domains suitable for a DBMS mini-project, with one example entity and its attributes for each.\"**\n\n**Model Answer:**\n\n| Domain | Example Entity | Sample Attributes |\n|---|---|---|\n| Hospital Management | `Patient` | PatientID, Name, DOB, BloodGroup, Phone |\n| Library Management | `Book` | BookID, ISBN, Title, Publisher, Year, Price |\n| E-Commerce | `Product` | ProductID, Name, Category, Price, StockQty |\n\nEach domain above supports at least 8 tables, normalization, and triggers — meeting KTU expectations. *(3 marks: 1 per domain with entity + 1 attribute list)*\n\n---\n\n### Q2. **[KTU University Exam — June 2024, Expected]**\n**\"What are the essential deliverables expected in a KTU DBMS mini-project report?\"**\n\n**Model Answer:**\n\nA complete KTU DBMS mini-project report must contain the following deliverables:\n\n1. **Synopsis** — problem statement, scope, objectives *(0.5 mark)*\n2. **Software Requirement Specification (SRS)** — functional and non-functional requirements *(0.5 mark)*\n3. **ER Diagram and Relational Schema** — conceptual and logical design *(0.5 mark)*\n4. **Normalization Proof** — conversion from 1NF to 3NF *(0.5 mark)*\n5. **DDL/DML Script File** — table creation and data insertion *(0.5 mark)*\n6. **Front-end Source Code + Screenshots** *(0.5 mark)*\n\n---\n\n## 5.2 PART B — Long Answer Questions (14 Marks Each, Internal Choice)\n\n---\n\n### **Question A (14 Marks)**\n\n#### Part (a) — 7 Marks\n**\"You are assigned the mini-project 'Online Book Store Management System'. Identify all major entities, their attributes, and the relationships among them. Draw the ER diagram and convert it into a relational schema.\"**\n\n**Model Solution:**\n\n**Entities & Attributes:**\n\n1. `Book(BookID, ISBN, Title, Price, StockQty, PublisherID, CategoryID)`\n2. `Author(AuthorID, Name, Bio)`\n3. `Publisher(PublisherID, Name, Address, Phone)`\n4. `Category(CategoryID, CategoryName)`\n5. `Customer(CustomerID, Name, Email, Phone, Address)`\n6. `Order(OrderID, OrderDate, CustomerID, TotalAmount)`\n7. `OrderItem(OrderID, BookID, Quantity, UnitPrice)` — bridge for M:N\n8. `Cart(CartID, CustomerID, Date)`\n9. `Payment(PaymentID, OrderID, Amount, Mode, Status)`\n10. `Review(ReviewID, CustomerID, BookID, Rating, Comment)`\n\n**Relationships:**\n\n| Relationship | Cardinality | Justification |\n|---|---|---|\n| Customer — Order | 1 : M | One customer places many orders |\n| Order — Book | M : N | Resolved by `OrderItem` |\n| Book — Author | M : N | Resolved by `Book_Author(BookID, AuthorID)` |\n| Book — Publisher | M : 1 | Many books from one publisher |\n| Book — Category | M : 1 | Many books in one category |\n| Order — Payment | 1 : 1 | Each order has one payment |\n| Customer — Review | 1 : M | One customer posts many reviews |\n| Book — Review | 1 : M | One book has many reviews |\n\n**Relational Schema (with primary keys underlined):**\n\n$$\n\\begin{aligned}\n&\\text{Book}(\\underline{\\text{BookID}},\\ \\text{ISBN},\\ \\text{Title},\\ \\text{Price},\\ \\text{StockQty},\\ \\text{PublisherID},\\ \\text{CategoryID}) \\\\\n&\\text{Author}(\\underline{\\text{AuthorID}},\\ \\text{Name},\\ \\text{Bio}) \\\\\n&\\text{Book\\_Author}(\\underline{\\text{BookID}},\\ \\underline{\\text{AuthorID}}) \\\\\n&\\text{Publisher}(\\underline{\\text{PublisherID}},\\ \\text{Name},\\ \\text{Address},\\ \\text{Phone}) \\\\\n&\\text{Category}(\\underline{\\text{CategoryID}},\\ \\text{CategoryName}) \\\\\n&\\text{Customer}(\\underline{\\text{CustomerID}},\\ \\text{Name},\\ \\text{Email},\\ \\text{Phone},\\ \\text{Address}) \\\\\n&\\text{Order}(\\underline{\\text{OrderID}},\\ \\text{OrderDate},\\ \\text{CustomerID},\\ \\text{TotalAmount}) \\\\\n&\\text{OrderItem}(\\underline{\\text{OrderID}},\\ \\underline{\\text{BookID}},\\ \\text{Quantity},\\ \\text{UnitPrice}) \\\\\n&\\text{Payment}(\\underline{\\text{PaymentID}},\\ \\text{OrderID},\\ \\text{Amount},\\ \\text{Mode},\\ \\text{Status}) \\\\\n&\\text{Review}(\\underline{\\text{ReviewID}},\\ \\text{CustomerID},\\ \\text{BookID},\\ \\text{Rating},\\ \\text{Comment})\n\\end{aligned}\n$$\n\n*(7 marks: 3 marks for entity-attribute identification, 2 marks for relationships with cardinalities, 2 marks for relational schema conversion)*\n\n---\n\n#### Part (b) — 7 Marks\n**\"For the above Online Book Store, write SQL DDL for the `Book` and `OrderItem` tables with all constraints, a trigger to update `StockQty` automatically when a new order item is inserted, and a view showing the top 5 selling books.\"**\n\n**Model Solution:**\n\n**DDL — Book Table:**\n\n```sql\nCREATE TABLE Book (\n    BookID INT PRIMARY KEY AUTO_INCREMENT,\n    ISBN VARCHAR(13) UNIQUE NOT NULL,\n    Title VARCHAR(200) NOT NULL,\n    Price DECIMAL(10,2) CHECK (Price > 0),\n    StockQty INT DEFAULT 0 CHECK (StockQty >= 0),\n    PublisherID INT NOT NULL,\n    CategoryID INT NOT NULL,\n    FOREIGN KEY (PublisherID) REFERENCES Publisher(PublisherID) ON DELETE CASCADE,\n    FOREIGN KEY (CategoryID) REFERENCES Category(CategoryID) ON DELETE SET NULL\n);\n```\n\n**DDL — OrderItem Table:**\n\n```sql\nCREATE TABLE OrderItem (\n    OrderID INT,\n    BookID INT,\n    Quantity INT CHECK (Quantity > 0),\n    UnitPrice DECIMAL(10,2) CHECK (UnitPrice > 0),\n    PRIMARY KEY (OrderID, BookID),\n    FOREIGN KEY (OrderID) REFERENCES `Order`(OrderID) ON DELETE CASCADE,\n    FOREIGN KEY (BookID) REFERENCES Book(BookID) ON DELETE RESTRICT\n);\n```\n\n**Trigger — Auto-decrement stock on order:**\n\n```sql\nDELIMITER //\nCREATE TRIGGER trg_UpdateStock\nAFTER INSERT ON OrderItem\nFOR EACH ROW\nBEGIN\n    UPDATE Book\n    SET StockQty = StockQty - NEW.Quantity\n    WHERE BookID = NEW.BookID;\nEND //\nDELIMITER ;\n```\n\n**View — Top 5 Selling Books:**\n\n```sql\nCREATE VIEW vw_Top5Books AS\nSELECT b.BookID, b.Title, SUM(oi.Quantity) AS TotalSold\nFROM Book b\nJOIN OrderItem oi ON b.BookID = oi.BookID\nGROUP BY b.BookID, b.Title\nORDER BY TotalSold DESC\nLIMIT 5;\n```\n\n*(7 marks: 2 marks for DDL with constraints, 2 marks for trigger, 2 marks for view, 1 mark for proper syntax and indentation)*\n\n---\n\n### **Question B (14 Marks) — Alternative Choice**\n\n#### Part (a) — 7 Marks\n**\"Propose a DBMS mini-project for a 'Blood Bank Management System'. List its entities, attributes, primary keys, and explain any three business rules that must be enforced through triggers or constraints.\"**\n\n**Model Solution:**\n\n**Entities:**\n\n1. `Donor(DonorID, Name, Age, BloodGroup, Phone, LastDonationDate)`\n2. `Recipient(RecipientID, Name, Hospital, DoctorRef, RequiredDate)`\n3. `BloodUnit(UnitID, DonorID, BloodGroup, CollectionDate, ExpiryDate, Status)`\n4. `Request(RequestID, RecipientID, BloodGroup, Quantity, RequestDate)`\n5. `Issue(IssueID, UnitID, RecipientID, IssueDate, IssuedBy)`\n6. `InventoryCheck(CheckID, CheckDate, UnitID, VerifiedBy)`\n\n**Business Rules (enforced via triggers):**\n\n**Rule 1 — Donor Age Validation:** A donor's age must be between 18 and 65.\n\n```sql\nALTER TABLE Donor\nADD CONSTRAINT chk_age CHECK (Age BETWEEN 18 AND 65);\n```\n\n**Rule 2 — Stock Expiry Auto-Update:** Any blood unit with `ExpiryDate < CURDATE()` must be marked `'Expired'`.\n\n```sql\nCREATE TRIGGER trg_ExpireBlood\nBEFORE INSERT ON BloodUnit\nFOR EACH ROW\nBEGIN\n    IF NEW.ExpiryDate <= NEW.CollectionDate THEN\n        SIGNAL SQLSTATE '45000'\n        SET MESSAGE_TEXT = 'Expiry must be after collection';\n    END IF;\nEND;\n```\n\n**Rule 3 — No Double Issue:** A `BloodUnit` can be issued to at most one recipient.\n\n```sql\nCREATE TRIGGER trg_PreventDoubleIssue\nBEFORE INSERT ON Issue\nFOR EACH ROW\nBEGIN\n    DECLARE cnt INT;\n    SELECT COUNT(*) INTO cnt FROM Issue WHERE UnitID = NEW.UnitID;\n    IF cnt > 0 THEN\n        SIGNAL SQLSTATE '45000'\n        SET MESSAGE_TEXT = 'Blood unit already issued';\n    END IF;\nEND;\n```\n\n*(7 marks: 2 marks for entities, 2 marks for attributes/keys, 3 marks for trigger explanations)*\n\n---\n\n#### Part (b) — 7 Marks\n**\"For the Blood Bank project, write SQL for: (i) a stored procedure to fulfill a blood request by allocating the oldest non-expired matching unit, (ii) a view showing current blood-group-wise stock, and (iii) a query to list donors who are eligible to donate today (last donation > 90 days ago).\"**\n\n**Model Solution:**\n\n**(i) Stored Procedure:**\n\n```sql\nDELIMITER //\nCREATE PROCEDURE sp_FulfillRequest(IN reqID INT)\nBEGIN\n    DECLARE reqGroup VARCHAR(5);\n    DECLARE reqQty INT;\n    DECLARE allocatedUnit INT;\n\n    SELECT BloodGroup, Quantity INTO reqGroup, reqQty\n    FROM Request WHERE RequestID = reqID;\n\n    SELECT UnitID INTO allocatedUnit\n    FROM BloodUnit\n    WHERE BloodGroup = reqGroup\n      AND Status = 'Available'\n      AND ExpiryDate > CURDATE()\n    ORDER BY CollectionDate ASC\n    LIMIT 1;\n\n    IF allocatedUnit IS NOT NULL THEN\n        INSERT INTO Issue(UnitID, RecipientID, IssueDate, IssuedBy)\n        VALUES (allocatedUnit,\n                (SELECT RecipientID FROM Request WHERE RequestID = reqID),\n                CURDATE(), USER());\n        UPDATE BloodUnit SET Status = 'Issued' WHERE UnitID = allocatedUnit;\n    END IF;\nEND //\nDELIMITER ;\n```\n\n**(ii) View — Stock Summary:**\n\n```sql\nCREATE VIEW vw_StockByGroup AS\nSELECT BloodGroup, COUNT(*) AS AvailableUnits\nFROM BloodUnit\nWHERE Status = 'Available' AND ExpiryDate > CURDATE()\nGROUP BY BloodGroup;\n```\n\n**(iii) Eligible Donors Query:**\n\n```sql\nSELECT DonorID, Name, Phone, LastDonationDate\nFROM Donor\nWHERE DATEDIFF(CURDATE(), LastDonationDate) > 90\n   OR LastDonationDate IS NULL;\n```\n\n*(7 marks: 3 marks for procedure, 2 marks for view, 2 marks for query)*\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> 1. **Choosing an over-simple project (≤ 4 tables):** The examiner will mark it down as \"insufficient complexity\". Always aim for **8–12 tables**.\n> 2. **Skipping the normalization proof:** Listing tables in 3NF *without showing the derivation* costs 2–3 marks. Always show 1NF → 2NF → 3NF explicitly.\n> 3. **Forgetting foreign-key constraints:** Many students write `PRIMARY KEY` but forget `FOREIGN KEY` with `REFERENCES`. This is the #1 deduction point.\n> 4. **No trigger or view:** KTU mandates at least **one trigger** and **one view**. Projects without these lose marks.\n> 5. **Single-page front-end:** The front-end must have **login + CRUD + at least one report**. A single form with no validation is not acceptable.\n> 6. **No sample data:** Tables must be populated with **realistic test data** (minimum 20 rows in main tables) for demonstration.\n> 7. **Inconsistent naming convention:** Mixing `camelCase` and `snake_case` in SQL (`patientId` vs `patient_id`) creates confusion. Stick to **snake_case** throughout.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n- A **KTU DBMS mini-project** is a *full-stack* software artefact combining **front-end + back-end + database**.\n- Always select a project with **8–12 normalized tables** — this is the **Standard Tier** accepted by KTU.\n- The **three-tier architecture** (Presentation → Application → Data) is *non-negotiable*.\n- Every project MUST include: **ER Diagram, Normalization Proof, DDL+DML Scripts, Triggers, Views, Stored Procedures**.\n- **Top 5 Project Recommendations for KTU 2024:**\n  1. Hospital Management System\n  2. University Exam & Result Processing\n  3. Online Book Store\n  4. E-Commerce Inventory & Order\n  5. Blood Bank Management\n- **Normalization must reach 3NF** at minimum; BCNF is a bonus for higher marks.\n- **Foreign key constraints with `ON DELETE CASCADE / SET NULL`** are mandatory for referential integrity.\n- **Sample data must be loaded** into all tables before the demo.\n- The **report must follow KTU format**: Title Page → Certificate → Abstract → Acknowledgement → TOC → Chapters (1–5) → References → Appendices.\n- **Project evaluation rubric (typical KTU weightage):** Problem Definition (10%), ER & Schema (25%), Normalization (15%), SQL Implementation (25%), Front-end (15%), Viva-Voce (10%).\n- **Avoid these common errors:** trivial projects, missing triggers/views, no foreign keys, no sample data, single-page front-end, missing normalization proof.\n- The project **directly maps to CO4** of PBITT304 — *\"Design and implement a database application for a real-world problem.\"*\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "pharmacydatabasemanagementsystem": {
      "title": "Pharmacy Database Management System",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 5\n> - **Topic:** Pharmacy Database Management System\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition\n\nA **Pharmacy Database Management System (Pharma-DBMS)** is a domain-specific, integrated relational database system designed to automate, store, manage, and retrieve all transactional, inventory, regulatory, and clinical data related to the operation of a pharmaceutical retail or wholesale outlet. It models entities such as *Medicines, Batches, Manufacturers, Suppliers, Customers, Prescriptions, Bills, Employees, and Purchase Orders*, enforcing integrity, atomicity, consistency, isolation, and durability (ACID) guarantees on every operation.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Mapping (CO5 / Bloom Level: Apply / Create):** A Pharmacy DBMS is the canonical *case-study application* of RDBMS design — it tests the student's ability to perform requirements analysis, build an ER model, derive a normalized relational schema, and author analytical SQL (joins, sub-queries, triggers, views, and stored procedures).\n\n## 1.2 Intuitive Analogy — The \"Smart Medicine Cabinet\"\n\nImagine a pharmacist running a busy medical shop. He has:\n\n* Thousands of **medicine strips** stacked in trays.\n* A **register book** to log customer bills.\n* A **calendar** to check which drugs expire next month.\n* A **notebook** for doctor's prescriptions.\n* A **ledger** for money owed to suppliers.\n\nDoing all this on paper is slow, error-prone, and risky (a single expired drug sold can cause legal trouble). A **Pharmacy DBMS** is like a **smart digital assistant** that:\n\n| Real-World Action | Database Equivalent |\n|---|---|\n| Storing a new drug box | `INSERT INTO Batch VALUES (...)` |\n| Handing over a strip and noting its price | `INSERT INTO BillItem` + `UPDATE Batch SET Quantity = Quantity - 1` |\n| Checking expiry on the wall calendar | `SELECT * FROM Batch WHERE ExpiryDate < DATE_ADD(CURDATE(), INTERVAL 30 DAY)` |\n| Verifying a doctor's prescription | `SELECT * FROM Prescription WHERE DoctorID = ?` |\n| Summarising daily takings | `SELECT SUM(NetAmount) FROM Bill WHERE DATE(Date) = CURDATE()` |\n\n> [!NOTE]\n> **Why this matters to the KTU examiner:** The examiner does not expect you to know pharmacy law — they expect a clean **ER → Schema → Normalization → SQL** pipeline applied to a real-world domain. Master this pipeline and you master 80% of the Module 5 marks.\n\n## 1.3 Functional Scope of a Pharmacy DBMS\n\nA robust system must handle six core operational pillars:\n\n1. **Inventory & Batch Control** — Track every strip/syrup bottle to its unique batch, manufacturing date, expiry date, and physical location.\n2. **Point-of-Sale (POS) Billing** — Generate GST-compliant invoices with discounts, taxes, and payment-mode records.\n3. **Prescription Management** — Maintain a digital record of doctor-issued prescriptions for scheduled and OTC drugs.\n4. **Procurement (Purchase Orders)** — Manage supplier orders, GRNs (Goods Received Notes), and reorder thresholds.\n5. **Regulatory Compliance & Reporting** — Drug licence auditing, Schedule H/X drug tracking, expiry alerts, and stock statements.\n6. **HR & Security** — Employee roles (Pharmacist vs. Cashier), salary records, and role-based access control.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Cheat Sheet\n\n## 2.1 The Operational Architecture (Why Each Entity Exists)\n\nThe pharmacy domain is fundamentally a **batch-traceable inventory** problem. Unlike a grocery shop where \"Parle-G Biscuit 1 kg\" is one SKU, in a pharmacy the *same medicine* can exist in 10 different batches with different expiry dates. This forces the design to keep a separate **Batch** table.\n\n### 2.1.1 Entity Justification\n\n* **Medicine** — Master data; defines what the drug *is* (composition, manufacturer, category).\n* **Batch** — Transactional data; defines a specific *lot* of that medicine with its own expiry and quantity.\n* **Supplier** — Who delivered the batch; required for recall and payment reconciliation.\n* **Manufacturer** — Who *made* the drug (separate from supplier who distributes it).\n* **Category** — Analytic grouping: Analgesic, Antibiotic, Antacid, etc. — powers sales reports.\n* **Customer** — A patient or walk-in buyer; needed for loyalty, prescription linkage, and GST invoices.\n* **Doctor** — Required for Schedule H/H1 drugs whose sale is *legally contingent* on a valid prescription.\n* **Prescription + PrescriptionItem** — A weak entity (existence-dependent on Prescription).\n* **Bill + BillItem** — A composite transaction; BillItem is also weak.\n* **Employee** — Records who processed the sale (audit trail).\n* **PurchaseOrder + PurchaseOrderItem** — Procurement side; mirrors Bill/BillItem structure.\n\n> [!TIP]\n> **KTU High-Yield Tip:** When asked \"Why did you create a separate Batch table?\" — answer: *\"To satisfy 1NF atomicity and to enable First-Expiry-First-Out (FEFO) dispatch, which is a legal and ethical requirement in pharmaceutical retail.\"*\n\n## 2.2 KTU High-Yield Formula Sheet & Design Cheat Sheet\n\n| Concept | Symbol / Rule | KTU Board Significance |\n|---|---|---|\n| **Cardinality (1:1)** | `A \\|\\|--\\|\\| B` | Each entity instance maps to exactly one other (e.g., Prescription ↔ Customer visit record) |\n| **Cardinality (1:N)** | `A \\|\\|--o{ B` | One parent, many children (e.g., Category → Medicine) |\n| **Cardinality (M:N)** | `A }o--o{ B` | Always resolved via a junction/bridge table (e.g., Medicine ↔ Supplier via Batch) |\n| **1NF** | Atomic values, no repeating groups, define PK | Eliminates multi-valued attributes (e.g., multiple phone numbers in one column) |\n| **2NF** | 1NF + no partial dependency on a composite PK | Splits junction tables cleanly |\n| **3NF** | 2NF + no transitive dependency | Removes derivable columns (e.g., TotalAmount stored when it can be `SUM(Subtotal)`) |\n| **BCNF** | 3NF + every determinant is a candidate key | Stronger — needed when 3NF still has anomalies |\n| **FEFO Rule** | `ORDER BY ExpiryDate ASC` | Mandatory SQL clause for dispensing |\n| **Reorder Level** | `Quantity ≤ ReorderLevel` | Trigger condition for purchase order generation |\n| **GST Formula** | `NetAmount = (TotalAmount − Discount) × (1 + TaxRate/100)` | Billing computation |\n| **ACID** | Atomicity, Consistency, Isolation, Durability | Enforced via `COMMIT`/`ROLLBACK` in transactions |\n\n## 2.3 Real-World Engineering Utility\n\n* **Hospital Information Systems (HIS):** Pharmacy modules feed the central EHR (Electronic Health Record) — every drug dispensed is logged against the patient's encounter.\n* **Cold-Chain Pharma Logistics:** Companies like DHL Life Sciences use this schema to track temperature-sensitive vaccines batch-by-batch.\n* **E-Pharmacy Giants (1mg, PharmEasy, Netmeds):** Their backend is essentially a scaled-out Pharmacy DBMS with sharding by pincode and read-replicas for the product catalogue.\n* **Government Drug Regulators (CDSCO, FDA):** Use the batch-traceability model for nationwide *drug recall* operations in minutes rather than weeks.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations, Normalization Walkthrough & Analytical Matrix\n\n## 3.1 Exhaustive Normalization of a Pharmacy Transaction\n\nConsider a single, un-normalized relation as it might appear in a naive Excel sheet:\n\n**RawTable (Bill_Master)** — `BillNo, BillDate, CustName, CustPhone, DoctorName, MedName, Category, BatchNo, Expiry, Qty, UnitPrice, Total`\n\nThis violates **1NF, 2NF, and 3NF** simultaneously. We walk through each Normal Form explicitly.\n\n### 3.1.1 Conversion to 1NF\n\n**Rule:** Every cell must be atomic. The primary key must uniquely identify every row.\n\n**Step 1 — Identify the composite primary key.** Because one bill can have many medicines, the natural key is `(BillNo, BatchNo)`.\n\n**Step 2 — Split repeating groups.** Move each medicine to its own row. Customer and Doctor info are repeated, but that's fine at this stage — we'll address it in 2NF.\n\nThe relation is now in 1NF (atomic values, single-valued columns, defined PK).\n\n### 3.1.2 Conversion to 2NF\n\n**Rule:** Every non-key attribute must depend on the *whole* primary key, not just part of it.\n\n**Step 3 — Detect partial dependencies.**\nWith PK = `(BillNo, BatchNo)`:\n\n* `CustName`, `CustPhone` depend only on `BillNo` (the bill identifies the customer).\n* `MedName`, `Category` depend on `BatchNo` (a batch identifies its medicine).\n* `UnitPrice` depends on `BatchNo`.\n* `Qty` depends on the *combination* (full key).\n* `Total` is a derived attribute (will be removed).\n\n**Step 4 — Decompose into 2NF relations.**\n\n$$\nR_1 = \\text{Bill}(\\underline{\\text{BillNo}}, \\text{BillDate}, \\text{CustID}, \\text{DoctorID}, \\text{EmployeeID})\n$$\n\n$$\nR_2 = \\text{Customer}(\\underline{\\text{CustID}}, \\text{CustName}, \\text{CustPhone})\n$$\n\n$$\nR_3 = \\text{Medicine}(\\underline{\\text{MedicineID}}, \\text{MedName}, \\text{CategoryID}, \\text{UnitPrice})\n$$\n\n$$\nR_4 = \\text{Batch}(\\underline{\\text{BatchNo}}, \\text{MedicineID}, \\text{ExpiryDate})\n$$\n\n$$\nR_5 = \\text{BillItem}(\\underline{\\text{BillNo}}, \\underline{\\text{BatchNo}}, \\text{Qty})\n$$\n\nAll non-key attributes now fully functionally depend on the whole PK in their respective relations.\n\n### 3.1.3 Conversion to 3NF\n\n**Rule:** Eliminate transitive dependencies (A → B → C).\n\n**Step 6 — Detect transitive dependencies in Medicine.**\n`MedicineID` → `CategoryID` → `CategoryName`.\nSo `CategoryName` is transitively dependent on `MedicineID`.\n\n**Step 7 — Decompose Category out.**\n\n$$\n\\text{Category}(\\underline{\\text{CategoryID}}, \\text{CategoryName})\n$$\n\n$$\n\\text{Medicine}(\\underline{\\text{MedicineID}}, \\text{MedName}, \\text{CategoryID}^{FK}, \\text{UnitPrice})\n$$\n\n**Step 8 — Remove derived attributes.** The `Total` column in the original bill must *not* be stored — it is a computed value derived from:\n\n$$\n\\text{Total} = \\sum_{i=1}^{n} \\text{Qty}_i \\times \\text{UnitPrice}_i\n$$\n\nWe compute it at query time using `SUM()` instead of storing it (3NF principle). The final schema is in **Boyce-Codd Normal Form (BCNF)** for this scenario, since every determinant is a candidate key.\n\n## 3.2 Analytical Matrix — SQL Aggregate Functions (Examiner's Favourite)\n\nThe following matrix is what the KTU examiner uses to grade SQL aggregation questions:\n\n| Function | Purpose | Pharmacy Use Case |\n|---|---|---|\n| `COUNT(*)` | Number of rows | Total bills generated today |\n| `SUM(NetAmount)` | Adds numeric column | Daily revenue |\n| `AVG(UnitPrice)` | Arithmetic mean | Average medicine price per category |\n| `MIN(ExpiryDate)` | Smallest value | Most-imminent expiry across all batches |\n| `MAX(Salary)` | Largest value | Highest-paid pharmacist |\n| `GROUP BY` | Bucket rows | Sales grouped by medicine category |\n| `HAVING` | Filter after aggregation | Categories with sales > ₹1,00,000 |\n| `DATEDIFF(CURDATE(), ExpiryDate)` | Date arithmetic | Days-since-expiry of expired stock |\n\n## 3.3 Worked Example — The FEFO Dispatch Query\n\n**Problem:** *A customer asks for 5 strips of \"Dolo 650\". The pharmacist must hand over the strips from the batch nearest to expiry (First-Expiry-First-Out).*\n\n**Logical Steps (must be written in the exam):**\n1. Identify all batches of MedicineID = 101 with sufficient quantity.\n2. Sort them by ExpiryDate ascending.\n3. Pick the top row and decrement its quantity.\n4. If the top batch is insufficient, combine with the next.\n\n**SQL Implementation:**\n\n```sql\nSELECT b.BatchID, b.BatchNumber, b.ExpiryDate, b.Quantity, m.Name\nFROM Batch b\nJOIN Medicine m ON b.MedicineID = m.MedicineID\nWHERE m.Name = 'Dolo 650' AND b.Quantity > 0\nORDER BY b.ExpiryDate ASC\nLIMIT 1;\n```\n\n> [!NOTE]\n> **KTU Valuation Note:** The examiner awards 2 marks for the `ORDER BY ... ASC` clause. Omitting it (using default order) is a common 2-mark deduction.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Complete Entity-Relationship Diagram of the Pharmacy DBMS\n\nThe following Mermaid ER diagram captures the **full schema** with cardinality, keys, and all core attributes. It is renderable in GitHub, GitLab, VS Code, and most modern markdown viewers.\n\n```mermaid\nerDiagram\n    CATEGORY ||--o{ MEDICINE : \"categorizes\"\n    MANUFACTURER ||--o{ MEDICINE : \"produces\"\n    MEDICINE ||--o{ BATCH : \"packed_as\"\n    SUPPLIER ||--o{ BATCH : \"supplies\"\n    SUPPLIER ||--o{ PURCHASE_ORDER : \"fulfills\"\n    PURCHASE_ORDER ||--|{ PURCHASE_ORDER_ITEM : \"contains\"\n    MEDICINE ||--o{ PURCHASE_ORDER_ITEM : \"ordered_in\"\n    CUSTOMER ||--o{ PRESCRIPTION : \"receives\"\n    DOCTOR ||--o{ PRESCRIPTION : \"writes\"\n    PRESCRIPTION ||--|{ PRESCRIPTION_ITEM : \"lists\"\n    MEDICINE ||--o{ PRESCRIPTION_ITEM : \"prescribed_as\"\n    CUSTOMER ||--o{ BILL : \"pays_at\"\n    EMPLOYEE ||--o{ BILL : \"processes\"\n    BILL ||--|{ BILL_ITEM : \"contains\"\n    MEDICINE ||--o{ BILL_ITEM : \"sold_as\"\n    BATCH ||--o{ BILL_ITEM : \"from_batch\"\n\n    CATEGORY {\n        int CategoryID PK\n        string CategoryName\n        string Description\n    }\n    MANUFACTURER {\n        int ManufacturerID PK\n        string Name\n        string Contact\n        string LicenseNo\n    }\n    MEDICINE {\n        int MedicineID PK\n        string Name\n        string BrandName\n        decimal UnitPrice\n        string DrugType\n        int CategoryID FK\n        int ManufacturerID FK\n    }\n    SUPPLIER {\n        int SupplierID PK\n        string Name\n        string Contact\n        string GSTNo\n    }\n    BATCH {\n        int BatchID PK\n        string BatchNumber\n        date MfgDate\n        date ExpiryDate\n        int Quantity\n        decimal PurchasePrice\n        decimal SellingPrice\n        int MedicineID FK\n        int SupplierID FK\n    }\n    CUSTOMER {\n        int CustomerID PK\n        string Name\n        string Phone\n        string Address\n        date DOB\n    }\n    DOCTOR {\n        int DoctorID PK\n        string Name\n        string Specialization\n        string RegNo\n        string Hospital\n    }\n    PRESCRIPTION {\n        int PrescriptionID PK\n        date Date\n        string Notes\n        int DoctorID FK\n        int CustomerID FK\n    }\n    PRESCRIPTION_ITEM {\n        int PrescriptionID PK,FK\n        int MedicineID PK,FK\n        string Dosage\n        string Duration\n        string Frequency\n    }\n    EMPLOYEE {\n        int EmployeeID PK\n        string Name\n        string Role\n        decimal Salary\n        string Phone\n    }\n    BILL {\n        int BillID PK\n        datetime BillDate\n        decimal TotalAmount\n        decimal Discount\n        decimal Tax\n        decimal NetAmount\n        string PaymentMode\n        int CustomerID FK\n        int EmployeeID FK\n    }\n    BILL_ITEM {\n        int BillID PK,FK\n        int MedicineID PK,FK\n        int BatchID PK,FK\n        int Quantity\n        decimal UnitPrice\n    }\n    PURCHASE_ORDER {\n        int OrderID PK\n        date OrderDate\n        string Status\n        decimal TotalAmount\n        int SupplierID FK\n    }\n    PURCHASE_ORDER_ITEM {\n        int OrderID PK,FK\n        int MedicineID PK,FK\n        int Quantity\n        decimal UnitCost\n    }\n```\n\n## 4.2 Block-Level Functional Architecture — The Billing Transaction Flow\n\nThis diagram maps the **interactions between modules** during a real-world sale, which is the operation KTU questions test most heavily.\n\n```mermaid\nflowchart TD\n    A[Customer arrives with prescription] --> B{Schedule drug?}\n    B -- Yes --> C[Pharmacist verifies Prescription table]\n    B -- No --> D[Proceed to billing]\n    C --> D\n    D --> E[Scan Barcode / Select Medicine]\n    E --> F[Query Batch table for stock and expiry]\n    F --> G{Batch available and not expired?}\n    G -- No --> H[Show out-of-stock alert]\n    G -- Yes --> I[FEFO: select batch with nearest ExpiryDate]\n    I --> J[Compute Subtotal = Qty x UnitPrice]\n    J --> K[Apply discount and GST tax]\n    K --> L[INSERT into Bill and BillItem in a TRANSACTION]\n    L --> M[UPDATE Batch SET Quantity = Quantity - SoldQty]\n    M --> N{Quantity below ReorderLevel?}\n    N -- Yes --> O[Generate Purchase Order alert]\n    N -- No --> P[Print receipt and COMMIT]\n    O --> P\n    P --> Q[End of sale]\n    H --> Q\n```\n\n> [!TIP]\n> **How to read this for the exam:** Steps **L and M** form a single **ACID transaction**. If the bill inserts but the batch update fails, the entire transaction must `ROLLBACK` — otherwise we sell \"ghost stock\" that doesn't exist. The KTU examiner specifically tests this in the 14-mark questions.\n\n## 4.3 Module Interconnection Map (Logical Tiers)\n\n```mermaid\nflowchart LR\n    subgraph PRESENTATION_TIER [Presentation Tier]\n        UI[POS GUI / Web Portal]\n    end\n    subgraph LOGIC_TIER [Application Tier]\n        BILL[Billing Module]\n        INV[Inventory Module]\n        PRES[Prescription Module]\n        REP[Reporting Module]\n    end\n    subgraph DATA_TIER [Data Tier]\n        DB[(Pharmacy RDBMS)]\n    end\n    UI --> BILL\n    UI --> INV\n    UI --> PRES\n    UI --> REP\n    BILL --> DB\n    INV --> DB\n    PRES --> DB\n    REP --> DB\n```\n\nThis **3-tier architecture** separation is a common KTU short-answer — the data tier is where the Pharmacy DBMS physically lives.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **[KTU University Exam - Dec 2023, Model Q1, 3 Marks]**\n\n**Q1. Define a Pharmacy Database Management System. List any four key entities that must be modeled.**\n\n**Model Answer (Examiner Expectation):**\nA Pharmacy Database Management System is a computerized relational database system that automates the storage, retrieval, and management of pharmaceutical data including medicines, batches, suppliers, customers, prescriptions, and bills, while ensuring data integrity, security, and regulatory compliance.\n\nFour key entities:\n1. **Medicine** — master record of every drug the pharmacy sells.\n2. **Batch** — tracks each lot's manufacturing and expiry dates.\n3. **Bill / BillItem** — records each sales transaction.\n4. **Supplier** — distributor from whom medicines are procured.\n\n> **[KTU University Exam - May 2024, Model Q2, 3 Marks]**\n\n**Q2. Why is a separate `Batch` table required in a Pharmacy DBMS, even though medicine information is already in the `Medicine` table?**\n\n**Model Answer:**\nThe same medicine may be present in multiple lots, each with a distinct batch number, manufacturing date, expiry date, purchase price, and current stock level. The `Medicine` table stores *static* product information, whereas the `Batch` table stores *dynamic, transactional* inventory information. Separating them enables First-Expiry-First-Out (FEFO) dispatch, batch-wise stock tracking, and recall of specific lots — all legal requirements in pharmaceutical retail.\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks with Internal Choice)\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Pitfall Callout**\n> 1. **Forgetting weak entities** — `BillItem` and `PrescriptionItem` are weak entities; they cannot exist without their parent. Always depict them with **double-rectangle** notation and use composite PKs in the schema. Missing this loses 2 marks.\n> 2. **Confusing Manufacturer with Supplier** — Manufacturer *produces* the drug; Supplier *distributes* it. Many students merge them into one entity. The KTU 2024 paper explicitly tested this distinction.\n> 3. **Storing derived columns** — `TotalAmount` in `Bill` should NOT be redundantly stored; compute via `SUM()`. Storing it violates 3NF.\n> 4. **Missing `CHECK` constraints** — `ExpiryDate > MfgDate` and `Quantity >= 0` are domain integrity rules. The examiner awards 1 mark for at least one correct `CHECK` constraint.\n> 5. **Skipping the FEFO clause** — In any \"dispense medicine\" query, always use `ORDER BY ExpiryDate ASC LIMIT 1`.\n\n---\n\n### Question A (14 Marks) — Design-Oriented Choice\n\n**(a) [7 Marks]** Design an Entity-Relationship (ER) diagram for a Pharmacy Database Management System. The system must handle medicines, batches, manufacturers, suppliers, customers, doctors, prescriptions, bills, and employees. State all cardinality constraints clearly.\n\n**Model Solution Outline (What the examiner expects):**\n* Draw the ER diagram (see Section 4.1) with at least 10 entities.\n* Show the following cardinalities explicitly:\n  * `MANUFACTURER 1:N MEDICINE`\n  * `CATEGORY 1:N MEDICINE`\n  * `MEDICINE 1:N BATCH`\n  * `SUPPLIER 1:N BATCH`\n  * `CUSTOMER 1:N PRESCRIPTION 1:N PRESCRIPTION_ITEM N:1 MEDICINE`\n  * `CUSTOMER 1:N BILL 1:N BILL_ITEM N:1 MEDICINE` and `N:1 BATCH`\n  * `DOCTOR 1:N PRESCRIPTION`\n  * `EMPLOYEE 1:N BILL`\n* Mark `PrescriptionItem` and `BillItem` as **weak entities** with double-bordered rectangles.\n* Mark the `BillDate`, `ExpiryDate` as **derived/date attributes** where appropriate.\n\n**(b) [7 Marks]** Convert the above ER diagram into a relational schema. Perform normalization up to **Third Normal Form (3NF)** for the `Bill` and `Prescription` sub-systems. Justify each normalization step.\n\n**Model Solution Outline:**\n* List all relations with **Primary Keys (underlined)**, **Foreign Keys (superscript FK)**, and data types.\n* Show the un-normalized master table (as in Section 3.1) and walk through 1NF → 2NF → 3NF.\n* Justify the removal of `CategoryName` (transitive dependency) and `TotalAmount` (derived attribute).\n* Final 3NF schema:\n\n```\nCategory(CategoryID, CategoryName, Description)\nManufacturer(ManufacturerID, Name, Contact, LicenseNo)\nMedicine(MedicineID, Name, BrandName, UnitPrice, DrugType, CategoryID, ManufacturerID)\nSupplier(SupplierID, Name, Contact, GSTNo)\nBatch(BatchID, BatchNumber, MfgDate, ExpiryDate, Quantity, PurchasePrice, SellingPrice, MedicineID, SupplierID)\nCustomer(CustomerID, Name, Phone, Address, DOB)\nDoctor(DoctorID, Name, Specialization, RegNo, Hospital)\nPrescription(PrescriptionID, Date, Notes, DoctorID, CustomerID)\nPrescriptionItem(PrescriptionID, MedicineID, Dosage, Duration, Frequency)\nEmployee(EmployeeID, Name, Role, Salary, Phone)\nBill(BillID, BillDate, TotalAmount, Discount, Tax, NetAmount, PaymentMode, CustomerID, EmployeeID)\nBillItem(BillID, MedicineID, BatchID, Quantity, UnitPrice)\nPurchaseOrder(OrderID, OrderDate, Status, TotalAmount, SupplierID)\nPurchaseOrderItem(OrderID, MedicineID, Quantity, UnitCost)\n```\n\n---\n\n### Question B (14 Marks) — Implementation-Oriented Choice\n\n**(a) [7 Marks]** Write the SQL DDL statements to create the tables `Medicine`, `Batch`, `Customer`, `Doctor`, `Prescription`, `PrescriptionItem`, and `Bill` for a Pharmacy DBMS. Include all primary key, foreign key, NOT NULL, UNIQUE, and CHECK constraints.\n\n**Model Solution:**\n\n```sql\nCREATE TABLE Medicine (\n    MedicineID     INT             PRIMARY KEY AUTO_INCREMENT,\n    Name           VARCHAR(100)    NOT NULL,\n    BrandName      VARCHAR(100),\n    UnitPrice      DECIMAL(10,2)   NOT NULL CHECK (UnitPrice > 0),\n    DrugType       VARCHAR(30)     NOT NULL,\n    CategoryID     INT             NOT NULL,\n    ManufacturerID INT             NOT NULL,\n    FOREIGN KEY (CategoryID)     REFERENCES Category(CategoryID),\n    FOREIGN KEY (ManufacturerID) REFERENCES Manufacturer(ManufacturerID)\n);\n\nCREATE TABLE Batch (\n    BatchID        INT          PRIMARY KEY AUTO_INCREMENT,\n    BatchNumber    VARCHAR(50)  NOT NULL UNIQUE,\n    MedicineID     INT          NOT NULL,\n    SupplierID     INT          NOT NULL,\n    MfgDate        DATE         NOT NULL,\n    ExpiryDate     DATE         NOT NULL,\n    Quantity       INT          NOT NULL CHECK (Quantity >= 0),\n    PurchasePrice  DECIMAL(10,2) NOT NULL,\n    SellingPrice   DECIMAL(10,2) NOT NULL,\n    FOREIGN KEY (MedicineID) REFERENCES Medicine(MedicineID),\n    FOREIGN KEY (SupplierID) REFERENCES Supplier(SupplierID),\n    CHECK (ExpiryDate > MfgDate)\n);\n\nCREATE TABLE Customer (\n    CustomerID INT          PRIMARY KEY AUTO_INCREMENT,\n    Name       VARCHAR(100) NOT NULL,\n    Phone      VARCHAR(15)  UNIQUE,\n    Address    VARCHAR(255),\n    DOB        DATE\n);\n\nCREATE TABLE Doctor (\n    DoctorID      INT          PRIMARY KEY AUTO_INCREMENT,\n    Name          VARCHAR(100) NOT NULL,\n    Specialization VARCHAR(80),\n    RegNo         VARCHAR(50)  NOT NULL UNIQUE,\n    Hospital      VARCHAR(100)\n);\n\nCREATE TABLE Prescription (\n    PrescriptionID INT  PRIMARY KEY AUTO_INCREMENT,\n    DoctorID       INT  NOT NULL,\n    CustomerID     INT  NOT NULL,\n    Date           DATE NOT NULL DEFAULT (CURRENT_DATE),\n    Notes          TEXT,\n    FOREIGN KEY (DoctorID)   REFERENCES Doctor(DoctorID),\n    FOREIGN KEY (CustomerID) REFERENCES Customer(CustomerID)\n);\n\nCREATE TABLE PrescriptionItem (\n    PrescriptionID INT          NOT NULL,\n    MedicineID     INT          NOT NULL,\n    Dosage         VARCHAR(50)  NOT NULL,\n    Duration       VARCHAR(50)  NOT NULL,\n    Frequency      VARCHAR(50)  NOT NULL,\n    PRIMARY KEY (PrescriptionID, MedicineID),\n    FOREIGN KEY (PrescriptionID) REFERENCES Prescription(PrescriptionID) ON DELETE CASCADE,\n    FOREIGN KEY (MedicineID)     REFERENCES Medicine(MedicineID)\n);\n\nCREATE TABLE Bill (\n    BillID        INT           PRIMARY KEY AUTO_INCREMENT,\n    CustomerID    INT,\n    EmployeeID    INT           NOT NULL,\n    BillDate      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,\n    TotalAmount   DECIMAL(10,2) NOT NULL,\n    Discount      DECIMAL(10,2) DEFAULT 0,\n    Tax           DECIMAL(10,2) DEFAULT 0,\n    NetAmount     DECIMAL(10,2) NOT NULL,\n    PaymentMode   VARCHAR(20)   NOT NULL,\n    FOREIGN KEY (CustomerID) REFERENCES Customer(CustomerID),\n    FOREIGN KEY (EmployeeID) REFERENCES Employee(EmployeeID)\n);\n```\n\n**(b) [7 Marks]** Write SQL queries for the following:\n**(i)** Find the names of medicines whose batches are expiring within the next 30 days.\n**(ii)** Generate a monthly sales report showing total revenue per month.\n**(iii)** Find the top 5 medicines by total quantity sold in the last quarter.\n**(iv)** Create a trigger that automatically decrements `Batch.Quantity` when a new row is inserted into `BillItem`.\n\n**Model Solution:**\n\n**(i) Expiring-soon query:**\n```sql\nSELECT DISTINCT m.Name, b.BatchNumber, b.ExpiryDate\nFROM Medicine m\nJOIN Batch b ON m.MedicineID = b.MedicineID\nWHERE b.ExpiryDate BETWEEN CURDATE() AND DATE_ADD(CURDATE(), INTERVAL 30 DAY)\n  AND b.Quantity > 0\nORDER BY b.ExpiryDate ASC;\n```\n\n**(ii) Monthly sales report:**\n```sql\nSELECT DATE_FORMAT(BillDate, '%Y-%m') AS SalesMonth,\n       SUM(NetAmount)                 AS TotalRevenue,\n       COUNT(*)                        AS TotalBills\nFROM Bill\nGROUP BY SalesMonth\nORDER BY SalesMonth DESC;\n```\n\n**(iii) Top 5 medicines in the last quarter:**\n```sql\nSELECT m.Name, SUM(bi.Quantity) AS TotalQtySold\nFROM BillItem bi\nJOIN Medicine m ON bi.MedicineID = m.MedicineID\nJOIN Bill b     ON bi.BillID = b.BillID\nWHERE b.BillDate >= DATE_SUB(CURDATE(), INTERVAL 3 MONTH)\nGROUP BY m.MedicineID, m.Name\nORDER BY TotalQtySold DESC\nLIMIT 5;\n```\n\n**(iv) Auto-decrement trigger:**\n```sql\nDELIMITER //\nCREATE TRIGGER trg_DecrementBatch\nAFTER INSERT ON BillItem\nFOR EACH ROW\nBEGIN\n    UPDATE Batch\n    SET Quantity = Quantity - NEW.Quantity\n    WHERE BatchID = NEW.BatchID;\nEND //\nDELIMITER ;\n```\n\n> [!IMPORTANT]\n> **Mark Distribution for Question B(b):** 2 marks per sub-question (i)–(iv) = 8 marks total — slight over-allotment because the trigger typically carries 2.5 marks due to its `DELIMITER` complexity. The examiner rounds down — your goal is to keep each query syntactically perfect.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n* **Domain Essence:** A Pharmacy DBMS is fundamentally a *batch-traceable inventory* system; the **Batch** entity is the heart of the design.\n* **Two Weak Entities:** `PrescriptionItem` and `BillItem` — always with **composite PKs** and `ON DELETE CASCADE`.\n* **Manufacturer ≠ Supplier:** Manufacturer *produces*, Supplier *distributes*. Never merge them.\n* **FEFO Rule:** All medicine-dispensing queries MUST include `ORDER BY ExpiryDate ASC LIMIT 1`.\n* **Schedule H/H1 Drugs:** Cannot be sold without a valid `Prescription` row — the schema enforces this as a business rule.\n* **3NF Discipline:** Never store `TotalAmount` in `Bill` if it is derivable; never store `CategoryName` in `Medicine` (transitive dep).\n* **ACID Transaction:** A sale = `INSERT Bill` + `INSERT BillItem` + `UPDATE Batch.Quantity` must be atomic. Use `START TRANSACTION; ... COMMIT;` or a trigger.\n* **Domain Constraints (CHECK):** `UnitPrice > 0`, `Quantity >= 0`, `ExpiryDate > MfgDate` — examiners award free marks for these.\n* **GST Calculation:** `NetAmount = (TotalAmount − Discount) × (1 + TaxRate/100)` — memorize this for billing questions.\n* **Reorder-Level Trigger:** If `Batch.Quantity` falls below `ReorderLevel`, an alert (or auto-PO) must fire.\n* **Audit Trail:** The `EmployeeID` foreign key in `Bill` provides accountability for every transaction.\n* **Top 5 SQL Patterns to Memorize:** `JOIN` for master-detail, `GROUP BY ... HAVING` for analytics, `DATEDIFF` for expiry, `ORDER BY ... LIMIT` for FEFO, `CREATE TRIGGER` for auto-updates.\n* **Normalization Verdict for this case study:** The schema naturally lands in **BCNF**; the examiner is satisfied with **3NF** demonstration.\n* **3-Tier Architecture:** Presentation (POS GUI) → Application (PHP/Java logic) → Data (Pharmacy RDBMS) — be ready to label the tiers.\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "restaurantdatabasemanagement": {
      "title": "Restaurant Database Management",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 6\n> - **Topic:** Restaurant Database Management\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\nA **Restaurant Database Management System (RDBMS-Restaurant)** is a structured, integrated collection of persistent data describing the operational entities of a food-service enterprise (customers, menu, orders, reservations, staff, inventory, billing) organised according to the relational model, manipulated via the Structured Query Language (SQL), and governed by the ACID transaction properties to ensure consistency under concurrent access and system failure.\n\nIn KTU 2024 Scheme terminology, designing a Restaurant Database integrates four sequential competencies:\n\n1. **Conceptual Modelling** – Entity-Relationship (ER) / Enhanced-ER (EER) modelling.\n2. **Logical Mapping** – Translation of ER constructs into relational schema with primary, candidate, and foreign keys.\n3. **Refinement via Normalisation** – Application of 1NF → 2NF → 3NF → BCNF to eliminate update, insertion, and deletion anomalies.\n4. **Physical Implementation & Transaction Control** – DDL/DML scripting, indexing, and ACID-compliant transaction management.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Highlight (Module 6):** A restaurant case study is the canonical \"real-world capstone\" used by KTU paper setters to test **schema design, normalisation, complex SQL, and transaction handling** in a single integrated question.\n\n## 1.2 Conceptual Analogy (Intuition)\n\nThink of a restaurant as a **highly dynamic, concurrent battlefield of tiny transactions**:\n- A **Waiter** acts like a *session* — opens a \"tab\" (transaction), issues reads/writes (orders), and commits on payment.\n- The **Kitchen Display System** is a *write-intensive buffer* — multiple cooks (transactions) concurrently update `ORDER_ITEM.status`.\n- The **Reservation Book** is a *constraint enforcer* — UNIQUE(table_no, date, time_slot) prevents double-booking.\n- The **Bill** is a *derived view* — computed by aggregating `ORDER_ITEM` rows joined with `MENU_ITEM.price`.\n\n> [!NOTE]\n> Just as a head chef insists every ingredient be measured precisely, a relational schema insists every attribute be atomic (1NF). Just as a head chef trains cooks to follow one recipe, normalisation trains designers to follow functional dependencies (FDs).\n\n## 1.3 Primary Entities of a Restaurant Database\n\n| # | Entity | Purpose |\n|---|---|---|\n| 1 | `CUSTOMER` | Stores diner profiles and contact info |\n| 2 | `RESTAURANT_BRANCH` | Multi-outlet chain support |\n| 3 | `TABLE_INFO` | Physical tables and seating capacity |\n| 4 | `RESERVATION` | Booking records (date, time, party size) |\n| 5 | `MENU_CATEGORY` | Starters, Mains, Desserts, Beverages |\n| 6 | `MENU_ITEM` | Individual dishes with price |\n| 7 | `INGREDIENT` | Raw materials for inventory |\n| 8 | `SUPPLIER` | Vendors supplying ingredients |\n| 9 | `STAFF` | Chef, Waiter, Manager, Cashier |\n| 10 | `ORDER` | Header of a customer order |\n| 11 | `ORDER_ITEM` | Line items of an order |\n| 12 | `PAYMENT` | Bill settlement record |\n\n> [!TIP]\n> **Standard metric used in industry:** Average transaction value (ATV) = Total revenue / Number of orders. Database must support fast aggregation queries on `PAYMENT` joined with `ORDER` for real-time dashboards.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Step-by-Step Design Methodology (The \"How\" and \"Why\")\n\n### Step 1 — Requirements Gathering\n**Why:** Prevents *redundancy* and *missing constraints*.\n- Identify business rules:\n  - A customer can place **many** orders, but each order belongs to **one** customer.\n  - A table can have **at most one** active reservation per time slot.\n  - An order must have **at least one** order item (participation constraint: total).\n  - A menu item uses **zero or more** ingredients (optional participation).\n\n### Step 2 — Identify Entities & Attributes\nDistinguish **strong entities** (exist independently: `CUSTOMER`, `MENU_ITEM`) from **weak entities** (depend on a parent: `ORDER_ITEM` depends on `ORDER`).\n\n### Step 3 — Identify Relationships & Cardinalities\n- `CUSTOMER` **places** `ORDER` — 1 : N\n- `ORDER` **contains** `ORDER_ITEM` — 1 : N (weak)\n- `ORDER` **served_by** `STAFF` — N : 1\n- `MENU_ITEM` **uses** `INGREDIENT` — M : N (resolved via `RECIPE` associative entity)\n- `RESERVATION` **assigned_to** `TABLE_INFO` — N : 1\n\n### Step 4 — Map ER → Relational Schema\n**Why:** RDBMS engines cannot store ER diagrams directly; they require tables.\n\n| ER Construct | Relational Mapping |\n|---|---|\n| Strong entity | New relation with PK |\n| Weak entity | New relation with PK = parent PK + partial key |\n| 1:1 relationship | Foreign key in either table (often mandatory side) |\n| 1:N relationship | Foreign key on the \"many\" side |\n| M:N relationship | New relation with composite PK of both parents |\n| Multivalued attribute | New relation (violates 1NF otherwise) |\n\n### Step 5 — Apply Functional Dependencies (FDs)\nAn FD $X \\rightarrow Y$ means: for any two tuples with the same $X$ value, they must have the same $Y$ value.\n\n**Example FDs in Restaurant DB:**\n- $\\text{order\\_id} \\rightarrow \\text{customer\\_id, order\\_date, total\\_amount}$\n- $\\text{menu\\_item\\_id} \\rightarrow \\text{name, price, category\\_id}$\n- $\\text{table\\_id, reservation\\_date, time\\_slot} \\rightarrow \\text{customer\\_id, party\\_size}$\n\n### Step 6 — Normalise to Remove Anomalies\n\n> [!IMPORTANT]\n> **KTU Board Examiner's Test:** If you skip from ER directly to CREATE TABLE without listing the FDs and the normal form achieved, you will lose 3–4 marks.\n\n## 2.2 KTU Formula Sheet / Cheat Sheet\n\n| # | Concept | Formula / Rule | Purpose |\n|---|---|---|---|\n| 1 | Attribute Closure | $X^+ = X \\cup \\{A \\mid \\exists Y \\rightarrow A, Y \\subseteq X^+\\}$ | Find all attributes functionally determined by $X$ |\n| 2 | Candidate Key Test | $X$ is a CK iff $X^+ = R$ (all attributes) | Identify minimal superkeys |\n| 3 | 1NF Rule | Every attribute must be **atomic** (no multi-valued, no composite) | Eliminate repeating groups |\n| 4 | 2NF Rule | 1NF + No **partial dependency** (no non-prime attr depends on part of a CK) | Eliminate redundancy from composite keys |\n| 5 | 3NF Rule | 2NF + No **transitive dependency** ($X \\rightarrow Y \\rightarrow Z$ where $Z$ is non-prime) | Eliminate transitive anomalies |\n| 6 | BCNF Rule | For every FD $X \\rightarrow Y$, $X$ must be a **superkey** | Stricter 3NF |\n| 7 | ACID — Atomicity | \"All or nothing\" — partial transactions must not persist | Implemented via *undo logs* |\n| 8 | ACID — Consistency | Sum of debits = Sum of credits (after commit) | Enforced by integrity constraints |\n| 9 | ACID — Isolation | Concurrent transactions must appear serial | Achieved via locking / MVCC |\n| 10 | ACID — Durability | Committed data survives crashes | Implemented via *redo logs* |\n| 11 | Locking Compatibility | Shared (S) locks allow other S; Exclusive (X) locks allow none | Prevents lost updates |\n| 12 | Wait-Die / Wound-Wait | Timestamp-based deadlock prevention | Older transaction waits; younger aborts |\n| 13 | Two-Phase Locking (2PL) | Growing phase → Shrinking phase | Guarantees conflict-serialisability |\n| 14 | Conflict Serialisability Test | Build *precedence graph*; if **acyclic** → serialisable | Detect schedule correctness |\n| 15 | Recovery — REDO | Apply log entries for committed transactions post-crash | Durability |\n| 16 | Recovery — UNDO | Rollback uncommitted transactions using *before-images* | Atomicity |\n\n> [!WARNING]\n> **Critical Notation Rule:** In the above table, the pipe symbol in `|x|` (set-difference or cardinality) is written as `\\vert`. Never type `|` inside a markdown table cell — it breaks the table renderer.\n\n## 2.3 Real-World Engineering Utility\n\nRestaurant database systems are **microcosms of enterprise resource planning (ERP)**. The same design patterns appear in:\n- **Hospital management** (patient ↔ order, doctor ↔ staff, ward ↔ table)\n- **E-commerce platforms** (cart ↔ order_item, SKU ↔ menu_item)\n- **Airline reservation** (PNR ↔ order, seat ↔ table, flight ↔ branch)\n\nThus, mastering this case study directly prepares KTU students for **SDE interviews** at firms like Zomato, Swiggy, and OYO.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations, Normalisation & Analytical Matrix\n\n## 3.1 Exhaustive Derivation: ER → Relational Mapping (with Justification)\n\nWe will derive the relational schema step by step for the Restaurant DB.\n\n### Step 1: Map Strong Entities\n\n$$\\begin{aligned}\n\\text{CUSTOMER}(\\underline{\\text{cust\\_id}}, \\text{name, phone, email, dob}) \\\\\n\\text{RESTAURANT\\_BRANCH}(\\underline{\\text{branch\\_id}}, \\text{location, phone, manager\\_id}) \\\\\n\\text{TABLE\\_INFO}(\\underline{\\text{table\\_id}}, \\text{branch\\_id}, \\text{capacity}, \\text{status}) \\\\\n\\text{MENU\\_CATEGORY}(\\underline{\\text{cat\\_id}}, \\text{cat\\_name}) \\\\\n\\text{MENU\\_ITEM}(\\underline{\\text{item\\_id}}, \\text{name, price, cat\\_id, is\\_veg}) \\\\\n\\text{STAFF}(\\underline{\\text{staff\\_id}}, \\text{name, role, salary, branch\\_id}) \\\\\n\\text{SUPPLIER}(\\underline{\\text{sup\\_id}}, \\text{sup\\_name, contact})\n\\end{aligned}$$\n\n**Logic:** Each strong entity becomes a relation. The primary key (underlined) is the natural or surrogate identifier. Foreign keys (`branch_id` in `TABLE_INFO` and `STAFF`) enforce 1:N relationships from `RESTAURANT_BRANCH`.\n\n### Step 2: Map the Weak Entity ORDER_ITEM\n\nA weak entity has no complete key of its own. Its primary key is the **parent's PK + a partial key (discriminator)**.\n\n$$\\text{ORDER\\_ITEM}(\\underline{\\text{order\\_id}, \\text{item\\_id}}, \\text{quantity, special\\_instructions, line\\_total})$$\n\nHere `order_id` is FK to `ORDER` and `item_id` is FK to `MENU_ITEM`. The pair (order_id, item_id) forms the composite primary key. Partial key alone (`item_id`) is insufficient — many orders can contain the same menu item.\n\n### Step 3: Map M:N Relationship (MENU_ITEM ↔ INGREDIENT via RECIPE)\n\nA M:N relationship is converted to a new relation whose PK is the combination of both parents' PKs.\n\n$$\\text{RECIPE}(\\underline{\\text{item\\_id}, \\text{ing\\_id}}, \\text{qty\\_required, unit})$$\n\n**Logic:** If a dish (e.g., \"Paneer Butter Masala\") uses 200g of paneer and 50g of butter, that row in `RECIPE` records the recipe composition. Resolving M:N this way prevents the multivalued-attribute violation of 1NF.\n\n### Step 4: Map the Multivalued Attribute (Customer Phone Numbers)\n\nIf a customer can have multiple phone numbers, we **must not** store them comma-separated (violates 1NF). We extract a new relation:\n\n$$\\text{CUSTOMER\\_PHONE}(\\underline{\\text{cust\\_id}, \\text{phone\\_no}})$$\n\n**Logic:** \"Atomic\" does not mean \"small\" — it means \"indivisible in the context of the schema.\" A phone number, although string-typed, is a single value per row.\n\n### Step 5: Include ORDER and PAYMENT\n\n$$\\begin{aligned}\n\\text{ORDER}(\\underline{\\text{order\\_id}}, \\text{cust\\_id}, \\text{table\\_id}, \\text{staff\\_id}, \\text{order\\_date}, \\text{status}, \\text{total\\_amount}) \\\\\n\\text{PAYMENT}(\\underline{\\text{payment\\_id}}, \\text{order\\_id}, \\text{amount, mode, payment\\_time})\n\\end{aligned}$$\n\n**Logic:** `ORDER` is the central transactional entity. `total_amount` is a **derived attribute** (can be computed, but stored for performance and historical snapshotting).\n\n## 3.2 Exhaustive Normalisation Derivation\n\n### Initial Unnormalised Form (UNF) — A Sample Order Record\n\n| order_id | cust_name | items_ordered | item_prices | qty |\n|---|---|---|---|---|\n| 101 | Rahul | Paneer Tikka, Naan | 250, 40 | 1, 2 |\n| 102 | Sneha | Veg Biryani, Coke | 220, 50 | 1, 1 |\n\n**Violation:** `items_ordered` and `item_prices` are **multivalued** (repeating groups).\n\n### Apply 1NF: Atomicity\n\nDecompose into atomic rows (one row per item).\n\n| order_id | cust_name | item_name | item_price | qty |\n|---|---|---|---|---|\n| 101 | Rahul | Paneer Tikka | 250 | 1 |\n| 101 | Rahul | Naan | 40 | 2 |\n| 102 | Sneha | Veg Biryani | 220 | 1 |\n| 102 | Sneha | Coke | 50 | 1 |\n\n✅ 1NF satisfied. But we still have **partial dependency** (cust_name depends only on order_id) and **transitive dependency** (item_name → item_price).\n\n### Apply 2NF: Eliminate Partial Dependencies\n\nSplit into:\n\n**R1: ORDER(101, Rahul), (102, Sneha)** — Primary key: `order_id`\n\n**R2: ORDER_ITEM(order_id, item_name, item_price, qty)** — Primary key: `(order_id, item_name)`\n\n✅ 2NF satisfied (no non-prime attribute depends on a *part* of a composite key).\n\n### Apply 3NF: Eliminate Transitive Dependencies\n\n`item_name → item_price` is transitive (non-prime `item_price` depends on non-prime `item_name`).\n\nSplit R2 into:\n\n**R3: ORDER_ITEM(order_id, item_id, qty)**\n\n**R4: MENU_ITEM(item_id, item_name, item_price)**\n\n✅ 3NF satisfied.\n\n### Check BCNF\n\nFor R4: $FD1: \\text{item\\_id} \\rightarrow \\text{item\\_name, item\\_price}$ — LHS `item_id` is a superkey. ✅ BCNF satisfied.\n\n## 3.3 Transaction Handling: A Worked Example\n\n**Scenario:** Two waiters simultaneously try to update the same `ORDER_ITEM` to \"Served\" status.\n\n| T1 (Waiter A) | T2 (Waiter B) | DB State |\n|---|---|---|\n| `SELECT status FROM ORDER_ITEM WHERE id=501` → \"Preparing\" | — | \"Preparing\" |\n| — | `SELECT status FROM ORDER_ITEM WHERE id=501` → \"Preparing\" | \"Preparing\" |\n| `UPDATE ORDER_ITEM SET status='Served' WHERE id=501` | — | \"Served\" |\n| — | `UPDATE ORDER_ITEM SET status='Served' WHERE id=501` | \"Served\" |\n| `COMMIT` | — | committed |\n| — | `COMMIT` | committed |\n\n**Issue:** Without isolation, this is fine (idempotent update). But consider **Lost Update**:\n\n| T1 | T2 | DB |\n|---|---|---|\n| Reads quantity = 1 | — | 1 |\n| — | Reads quantity = 1 | 1 |\n| Sets quantity = 1 + 1 = 2 | — | 2 |\n| — | Sets quantity = 1 + 1 = 2 (overwrites) | 2 ❌ |\n\n**Solution via 2PL (Two-Phase Locking):**\n\n$$\\begin{aligned}\nT1: \\text{LOCK-X}(q) \\rightarrow \\text{READ}(q) \\rightarrow \\text{WRITE}(q=2) \\rightarrow \\text{UNLOCK} \\\\\nT2: \\text{WAIT} \\rightarrow \\text{LOCK-X}(q) \\rightarrow \\text{READ}(q=2) \\rightarrow \\text{WRITE}(q=3) \\rightarrow \\text{UNLOCK}\n\\end{aligned}$$\n\nFinal value = 3 ✅. This is the **Growing Phase (locks acquired) → Shrinking Phase (locks released)**.\n\n## 3.4 Analytical Comparison Matrix\n\n| Design Choice | Pros | Cons | When to Use |\n|---|---|---|---|\n| **Surrogate Keys** (`SERIAL`/`AUTO_INCREMENT`) | Compact, stable, performance | No business meaning | Always for PKs in OLTP |\n| **Natural Keys** (e.g., Aadhaar, ISBN) | Self-documenting | Can change, bulky | When truly immutable |\n| **Denormalisation** (storing `total_amount`) | Fast reads, snapshotting | Update anomalies | Reporting/OLAP layers |\n| **Stored Triggers for Billing** | Automation, integrity | Hard to debug | Real-time business rules |\n| **MVCC** (Multi-Version Concurrency Control) | Readers don't block writers | Storage overhead | High-read systems (Postgres default) |\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Entity-Relationship (ER) Diagram — Restaurant Database\n\n```mermaid\nerDiagram\n    CUSTOMER ||--o{ ORDER : \"places\"\n    ORDER ||--|{ ORDER_ITEM : \"contains\"\n    MENU_ITEM ||--o{ ORDER_ITEM : \"included_in\"\n    STAFF ||--o{ ORDER : \"serves\"\n    TABLE_INFO ||--o{ RESERVATION : \"assigned_to\"\n    CUSTOMER ||--o{ RESERVATION : \"books\"\n    RESTAURANT_BRANCH ||--o{ TABLE_INFO : \"has\"\n    RESTAURANT_BRANCH ||--o{ STAFF : \"employs\"\n    MENU_CATEGORY ||--o{ MENU_ITEM : \"classifies\"\n    MENU_ITEM ||--o{ RECIPE : \"uses\"\n    INGREDIENT ||--o{ RECIPE : \"supplies\"\n    SUPPLIER ||--o{ INGREDIENT : \"provides\"\n    ORDER ||--|| PAYMENT : \"settled_by\"\n\n    CUSTOMER {\n        int cust_id PK\n        string name\n        string phone\n        string email\n        date dob\n    }\n    ORDER {\n        int order_id PK\n        int cust_id FK\n        int table_id FK\n        int staff_id FK\n        datetime order_time\n        string status\n        decimal total_amount\n    }\n    ORDER_ITEM {\n        int order_id PK,FK\n        int item_id PK,FK\n        int quantity\n        string special_instructions\n        decimal line_total\n    }\n    MENU_ITEM {\n        int item_id PK\n        int cat_id FK\n        string name\n        decimal price\n        boolean is_veg\n    }\n    MENU_CATEGORY {\n        int cat_id PK\n        string cat_name\n    }\n    RESERVATION {\n        int reservation_id PK\n        int cust_id FK\n        int table_id FK\n        date reserve_date\n        time time_slot\n        int party_size\n    }\n    TABLE_INFO {\n        int table_id PK\n        int branch_id FK\n        int capacity\n        string status\n    }\n    RESTAURANT_BRANCH {\n        int branch_id PK\n        string location\n        string phone\n    }\n    STAFF {\n        int staff_id PK\n        int branch_id FK\n        string name\n        string role\n        decimal salary\n    }\n    INGREDIENT {\n        int ing_id PK\n        int sup_id FK\n        string name\n        string unit\n    }\n    SUPPLIER {\n        int sup_id PK\n        string sup_name\n        string contact\n    }\n    RECIPE {\n        int item_id PK,FK\n        int ing_id PK,FK\n        decimal qty_required\n    }\n    PAYMENT {\n        int payment_id PK\n        int order_id FK\n        decimal amount\n        string mode\n        datetime payment_time\n    }\n```\n\n## 4.2 Block-Level Functional Architecture (Order Processing Pipeline)\n\n```mermaid\nflowchart TD\n    A[Customer places order via POS] --> B[Begin Transaction T1]\n    B --> C[Insert into ORDER table]\n    C --> D[Insert line items into ORDER_ITEM]\n    D --> E{Stock check via RECIPE join}\n    E -- Sufficient --> F[Compute line_total price]\n    E -- Insufficient --> G[Trigger INVENTORY_ALERT]\n    F --> H[Lock TABLE_INFO for assignment]\n    H --> I[Assign Waiter STAFF]\n    I --> J[COMMIT T1 - Order confirmed]\n    J --> K[Kitchen Display System reads ORDER]\n    K --> L[Chef marks ORDER_ITEM as PREPARING]\n    L --> M[Status update READ COMMITTED]\n    M --> N[Ready - update to SERVED]\n    N --> O[Customer requests bill]\n    O --> P[Trigger computes total from ORDER_ITEM]\n    P --> Q[Generate PAYMENT record]\n    Q --> R[COMMIT T2 - Payment recorded]\n    R --> S[REDO log ensures durability]\n```\n\n## 4.3 Schema Indexing Strategy (B-Tree Visual)\n\n```mermaid\nflowchart LR\n    R1[CUSTOMER cust_id] --> I1[B-Tree Index]\n    R2[ORDER order_date] --> I2[Composite Index]\n    R3[ORDER_ITEM order_id, item_id] --> I3[Composite PK Index]\n    R4[RESERVATION table_id reserve_date time_slot] --> I4[Unique Index]\n    R5[MENU_ITEM name] --> I5[Hash Index for search]\n```\n\n> [!TIP]\n> The **composite unique index** on `RESERVATION(table_id, reserve_date, time_slot)` is the cornerstone of preventing double-booking — a classic KTU short-answer question.\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## PART A — 3 Mark Questions (Short Answer)\n\n### Question 1: [KTU University Exam – Dec 2023, Model Q]\n**Define ACID properties. How do they apply to a restaurant order-placement scenario?**\n\n**Model Answer (3 marks):**\n\nACID stands for:\n1. **Atomicity** — A bill payment transaction is \"all or nothing.\" If the card is declined midway, the `ORDER` status reverts from \"Paid\" back to \"Pending.\" *(1 mark)*\n2. **Consistency** — The total of `ORDER_ITEM.line_total` must always equal `ORDER.total_amount` after commit. Enforced via CHECK constraints. *(1 mark)*\n3. **Isolation** — Two cashiers cannot simultaneously issue two different bills for the same `order_id`; locking prevents this. *(0.5 marks)*\n4. **Durability** — Once payment is committed, the record survives power failure due to write-ahead logging. *(0.5 marks)*\n\n---\n\n### Question 2: [KTU University Exam – June 2024, Model Q]\n**What is a weak entity? Identify the weak entity in a restaurant database with justification.**\n\n**Model Answer (3 marks):**\n\nA **weak entity** is one that cannot be uniquely identified by its own attributes alone and depends on a strong (owner) entity for its existence. *(1 mark)*\n\nIn the restaurant database, **`ORDER_ITEM`** is a weak entity because:\n- It has no independent meaning without its parent `ORDER`. *(1 mark)*\n- Its primary key `(order_id, item_id)` is a composite of the parent PK and a partial key `item_id`. *(1 mark)*\n\n---\n\n## PART B — 14 Mark Questions (Module Internal Choice)\n\n### Question A (Choice 1) — [KTU University Exam – June 2023, Modified]\n\n**(a)** Design an ER diagram for a restaurant database system with the following requirements: (7 marks)\n- A restaurant has multiple branches; each branch has tables and staff.\n- Customers place orders; each order has multiple order items.\n- Menu items belong to categories and use ingredients supplied by suppliers.\n- Customers can make reservations for specific tables and time slots.\n\n**(b)** Map the ER diagram to a relational schema and normalise it up to 3NF. (7 marks)\n\n#### Model Solution — Part (a): ER Diagram\n\nEntities identified:\n- `CUSTOMER(cust_id, name, phone, email)`\n- `RESTAURANT_BRANCH(branch_id, location, phone)`\n- `TABLE_INFO(table_id, capacity, status)`\n- `STAFF(staff_id, name, role, salary)`\n- `MENU_CATEGORY(cat_id, cat_name)`\n- `MENU_ITEM(item_id, name, price, is_veg)`\n- `INGREDIENT(ing_id, name, unit)`\n- `SUPPLIER(sup_id, sup_name, contact)`\n- `ORDER(order_id, order_date, status, total_amount)`\n- `ORDER_ITEM(order_id, item_id, qty, line_total)` — *Weak entity*\n- `RESERVATION(res_id, reserve_date, time_slot, party_size)`\n- `PAYMENT(payment_id, amount, mode, time)`\n\nRelationships with cardinalities:\n1. `CUSTOMER` 1:N `ORDER` (places)\n2. `ORDER` 1:N `ORDER_ITEM` (contains — identifying)\n3. `MENU_ITEM` 1:N `ORDER_ITEM` (included_in)\n4. `STAFF` 1:N `ORDER` (serves)\n5. `TABLE_INFO` 1:N `RESERVATION` (assigned_to)\n6. `CUSTOMER` 1:N `RESERVATION` (books)\n7. `MENU_CATEGORY` 1:N `MENU_ITEM` (classifies)\n8. `MENU_ITEM` M:N `INGREDIENT` via `RECIPE(item_id, ing_id, qty_required)`\n9. `SUPPLIER` 1:N `INGREDIENT` (provides)\n10. `ORDER` 1:1 `PAYMENT` (settled_by)\n11. `RESTAURANT_BRANCH` 1:N `TABLE_INFO`, 1:N `STAFF` (owns/employs)\n\n*(Refer to Mermaid ER diagram in Section 4.1 for visual.)* — **Full 7 marks**\n\n#### Model Solution — Part (b): Mapping + Normalisation\n\n**Step 1: Mapping to Relations**\n\n```sql\nCREATE TABLE RestaurantBranch (\n    branch_id   INT PRIMARY KEY,\n    location    VARCHAR(100) NOT NULL,\n    phone       VARCHAR(15)\n);\n\nCREATE TABLE TableInfo (\n    table_id    INT PRIMARY KEY,\n    branch_id   INT REFERENCES RestaurantBranch(branch_id),\n    capacity    INT CHECK (capacity > 0),\n    status      VARCHAR(20) DEFAULT 'Available'\n);\n\nCREATE TABLE Staff (\n    staff_id    INT PRIMARY KEY,\n    branch_id   INT REFERENCES RestaurantBranch(branch_id),\n    name        VARCHAR(50) NOT NULL,\n    role        VARCHAR(20) CHECK (role IN ('Chef','Waiter','Manager','Cashier')),\n    salary      DECIMAL(10,2) CHECK (salary >= 0)\n);\n\nCREATE TABLE Customer (\n    cust_id     INT PRIMARY KEY,\n    name        VARCHAR(50) NOT NULL,\n    phone       VARCHAR(15) UNIQUE,\n    email       VARCHAR(100)\n);\n\nCREATE TABLE MenuCategory (\n    cat_id      INT PRIMARY KEY,\n    cat_name    VARCHAR(30) UNIQUE NOT NULL\n);\n\nCREATE TABLE MenuItem (\n    item_id     INT PRIMARY KEY,\n    cat_id      INT REFERENCES MenuCategory(cat_id),\n    name        VARCHAR(50) NOT NULL,\n    price       DECIMAL(7,2) CHECK (price > 0),\n    is_veg      BOOLEAN\n);\n\nCREATE TABLE Supplier (\n    sup_id      INT PRIMARY KEY,\n    sup_name    VARCHAR(50) NOT NULL,\n    contact     VARCHAR(15)\n);\n\nCREATE TABLE Ingredient (\n    ing_id      INT PRIMARY KEY,\n    sup_id      INT REFERENCES Supplier(sup_id),\n    name        VARCHAR(50),\n    unit        VARCHAR(10)\n);\n\nCREATE TABLE Recipe (\n    item_id     INT REFERENCES MenuItem(item_id),\n    ing_id      INT REFERENCES Ingredient(ing_id),\n    qty_required DECIMAL(8,2),\n    PRIMARY KEY (item_id, ing_id)\n);\n\nCREATE TABLE Reservation (\n    reservation_id INT PRIMARY KEY,\n    cust_id     INT REFERENCES Customer(cust_id),\n    table_id    INT REFERENCES TableInfo(table_id),\n    reserve_date DATE NOT NULL,\n    time_slot   TIME NOT NULL,\n    party_size  INT CHECK (party_size > 0),\n    UNIQUE (table_id, reserve_date, time_slot)\n);\n\nCREATE TABLE FoodOrder (\n    order_id    INT PRIMARY KEY,\n    cust_id     INT REFERENCES Customer(cust_id),\n    table_id    INT REFERENCES TableInfo(table_id),\n    staff_id    INT REFERENCES Staff(staff_id),\n    order_date  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n    status      VARCHAR(20) DEFAULT 'Pending',\n    total_amount DECIMAL(10,2)\n);\n\nCREATE TABLE OrderItem (\n    order_id    INT REFERENCES FoodOrder(order_id) ON DELETE CASCADE,\n    item_id     INT REFERENCES MenuItem(item_id),\n    quantity    INT CHECK (quantity > 0),\n    special_instructions VARCHAR(200),\n    line_total  DECIMAL(10,2),\n    PRIMARY KEY (order_id, item_id)\n);\n\nCREATE TABLE Payment (\n    payment_id  INT PRIMARY KEY,\n    order_id    INT UNIQUE REFERENCES FoodOrder(order_id),\n    amount      DECIMAL(10,2) CHECK (amount >= 0),\n    mode        VARCHAR(20) CHECK (mode IN ('Cash','Card','UPI','Wallet')),\n    payment_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n```\n\n**Step 2: Functional Dependencies and Normalisation**\n\nFDs for the Order schema (initial):\n- $F1: \\text{order\\_id} \\rightarrow \\text{cust\\_id, table\\_id, staff\\_id, order\\_date, status, total\\_amount}$\n- $F2: \\text{order\\_id, item\\_id} \\rightarrow \\text{quantity, line\\_total}$\n- $F3: \\text{item\\_id} \\rightarrow \\text{name, price, cat\\_id, is\\_veg}$\n- $F4: \\text{cust\\_id} \\rightarrow \\text{name, phone, email}$\n\n**Check 1NF:** All attributes are atomic. ✅\n\n**Check 2NF:** In `OrderItem`, `line_total` depends on full PK `(order_id, item_id)`, not on a part. ✅ No partial dependencies.\n\n**Check 3NF:** In `MenuItem`, `item_id → price` is direct, not transitive (no intermediate non-prime). ✅ In `FoodOrder`, `total_amount` is derived but acceptable if we treat it as stored.\n\n**Decomposition to 3NF (if needed):** Split any relation where a non-prime attribute transitively determines another. Our final schema is in 3NF. ✅\n\n**Full 7 marks** distributed as: mapping (3 marks), FDs identification (2 marks), normalisation proof (2 marks).\n\n---\n\n### Question B (Choice 2) — [KTU University Exam – Dec 2022, Modified]\n\n**(a)** Write SQL DDL for the restaurant database. Demonstrate **3 complex queries** involving JOINs, subqueries, and aggregation relevant to restaurant analytics. (7 marks)\n\n**(b)** Explain the **Two-Phase Locking (2PL)** protocol. Show how it prevents the **lost update** problem in a concurrent order-quantity-update scenario. (7 marks)\n\n#### Model Solution — Part (a): Complex SQL Queries\n\n**Query 1: Top 5 most-ordered menu items in the last 30 days**\n\n```sql\nSELECT  mi.item_id,\n        mi.name,\n        SUM(oi.quantity) AS total_qty,\n        RANK() OVER (ORDER BY SUM(oi.quantity) DESC) AS popularity_rank\nFROM    OrderItem oi\nJOIN    MenuItem mi  ON oi.item_id = mi.item_id\nJOIN    FoodOrder fo ON oi.order_id = fo.order_id\nWHERE   fo.order_date >= CURRENT_DATE - INTERVAL '30 days'\nGROUP BY mi.item_id, mi.name\nORDER BY total_qty DESC\nLIMIT 5;\n```\n*Tests: 3-table JOIN, GROUP BY, window function, date arithmetic.* ✅\n\n**Query 2: Branches whose revenue exceeds the chain-wide average**\n\n```sql\nSELECT  rb.branch_id,\n        rb.location,\n        SUM(p.amount) AS branch_revenue\nFROM    RestaurantBranch rb\nJOIN    TableInfo    ti ON rb.branch_id = ti.branch_id\nJOIN    FoodOrder    fo ON ti.table_id  = fo.table_id\nJOIN    Payment      p  ON fo.order_id  = p.order_id\nGROUP BY rb.branch_id, rb.location\nHAVING  SUM(p.amount) >\n        (SELECT AVG(branch_total)\n         FROM (  SELECT  SUM(p2.amount) AS branch_total\n                 FROM    RestaurantBranch rb2\n                 JOIN    TableInfo    ti2 ON rb2.branch_id = ti2.branch_id\n                 JOIN    FoodOrder    fo2 ON ti2.table_id  = fo2.table_id\n                 JOIN    Payment      p2  ON fo2.order_id  = p2.order_id\n                 GROUP BY rb2.branch_id\n              ) AS branch_revenues\n        );\n```\n*Tests: Subquery in HAVING, derived table, multi-level aggregation.* ✅\n\n**Query 3: Customers who have not placed any order in the last 60 days (for re-engagement marketing)**\n\n```sql\nSELECT  c.cust_id, c.name, c.email,\n        MAX(fo.order_date) AS last_order_date\nFROM    Customer c\nLEFT JOIN FoodOrder fo ON c.cust_id = fo.cust_id\nGROUP BY c.cust_id, c.name, c.email\nHAVING  MAX(fo.order_date) IS NULL\n   OR   MAX(fo.order_date) < CURRENT_DATE - INTERVAL '60 days';\n```\n*Tests: LEFT JOIN (outer join), NULL handling in HAVING.* ✅\n\n**Marks distribution:** DDL (1 mark) + 3 queries (2 marks each) = 7 marks.\n\n#### Model Solution — Part (b): Two-Phase Locking\n\n**Definition:** The 2PL protocol ensures conflict-serialisability by dividing a transaction's execution into two distinct phases:\n- **Growing Phase:** Locks may be acquired, but none may be released.\n- **Shrinking Phase:** Locks may be released, but none may be acquired.\n\nOnce a lock is released, no new lock on any data item can be obtained by that transaction.\n\n**Lost Update Problem (without 2PL):**\n\n| Step | T1 (Waiter A) | T2 (Waiter B) | DB value of `qty` |\n|---|---|---|---|\n| 1 | `READ qty` → 1 | — | 1 |\n| 2 | — | `READ qty` → 1 | 1 |\n| 3 | `qty = 1 + 1 = 2`, `WRITE` | — | 2 |\n| 4 | — | `qty = 1 + 1 = 2`, `WRITE` (overwrites) | 2 ❌ |\n| 5 | `COMMIT` | — | 2 |\n| 6 | — | `COMMIT` | 2 |\n\n**Correct result should be 3** (1 + 1 + 1 = three servings).\n\n**With 2PL:**\n\n| Step | T1 | T2 | DB | Lock held |\n|---|---|---|---|---|\n| 1 | `LOCK-X(qty)` | — | 1 | T1 |\n| 2 | `READ qty` → 1 | — | 1 | T1 |\n| 3 | `WRITE qty=2` | `LOCK-X(qty)` → **BLOCKED** | 2 | T1 |\n| 4 | `COMMIT` → `UNLOCK` | (woken up) | 2 | none |\n| 5 | (Growing ends) | `LOCK-X(qty)` acquired | 2 | T2 |\n| 6 | (Shrinking) | `READ qty` → 2 | 2 | T2 |\n| 7 | — | `WRITE qty=3` | 3 | T2 |\n| 8 | — | `COMMIT` → `UNLOCK` | 3 | none |\n\nFinal value = 3 ✅. T2's update was **not lost**.\n\n**Proof of Serialisability (Precedence Graph):** The graph has edges $T1 \\rightarrow T2$ (T1 locked `qty` before T2). The graph is acyclic → schedule is conflict-serialisable.\n\n**Marks distribution:** 2PL definition (2 marks), lost update example (2 marks), 2PL solution with table (2 marks), serialisability proof (1 mark) = 7 marks.\n\n---\n\n## 5.1 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common mark-loss areas in Restaurant DB questions:**\n> 1. **Forgetting weak entity notation** — `ORDER_ITEM` must be drawn with a double rectangle; failure costs 1 mark.\n> 2. **Omitting cardinality ratios** — Every relationship MUST have a label (1:1, 1:N, M:N) and a name (verb).\n> 3. **No participation constraints** — Total participation (e.g., every ORDER must have at least one ORDER_ITEM) must be shown by a double line.\n> 4. **Skipping 2NF/3NF verification** — Merely listing tables without showing FDs and checking normal form = 2–3 marks lost.\n> 5. **SQL queries without JOIN syntax** — Writing correlated subqueries where a JOIN suffices is acceptable but inefficient; examiners prefer clean JOINs.\n> 6. **Ignoring ACID in transaction questions** — Just drawing a schedule table without naming the property violated (lost update, dirty read, non-repeatable read) = 2 marks lost.\n> 7. **Not drawing the precedence graph** for serialisability — this is the single most-skipped step.\n\n---\n\n## 5.2 Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- ✅ A **Restaurant DB** is a classic KTU capstone integrating ER modelling, normalisation, SQL, and transactions.\n- ✅ **Strong entities** have independent PKs; **weak entities** (e.g., `ORDER_ITEM`) require composite PK with parent's PK.\n- ✅ **1NF = atomicity**, **2NF = no partial dependency**, **3NF = no transitive dependency**, **BCNF = every determinant is a superkey**.\n- ✅ **M:N relationships** are always converted to a new associative relation (e.g., `RECIPE`).\n- ✅ **ACID** = Atomicity, Consistency, Isolation, Durability — apply to every order/payment transaction.\n- ✅ **2PL** has two phases: Growing (acquire locks only) and Shrinking (release locks only) — guarantees conflict-serialisability.\n- ✅ **Unique index** on `(table_id, reserve_date, time_slot)` prevents double-booking — favourite 3-mark KTU question.\n- ✅ **Derived attributes** (e.g., `total_amount`) can be stored for performance but must be updated by triggers.\n- ✅ Always use **surrogate keys** (`SERIAL`/`AUTO_INCREMENT`) for OLTP primary keys.\n- ✅ **Window functions** (`RANK()`, `DENSE_RANK()`, `ROW_NUMBER()`) are examinable for analytics queries.\n- ✅ **Locking compatibility matrix:** S–S ✅, S–X ❌, X–X ❌.\n- ✅ **Recovery uses REDO + UNDO logs** to ensure atomicity and durability after a crash.\n- ✅ Common concurrency problems to memorise: **Lost Update, Dirty Read, Non-Repeatable Read, Phantom Read** — each has a specific isolation level fix.\n- ✅ For KTU: Always end with a **precedence graph** to justify serialisability claims.\n\n---\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "payrolldatabasemanagementsystem": {
      "title": "Payroll Database Management System",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 7\n> - **Topic:** Payroll Database Management System\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\n> [!NOTE]\n> **Payroll Database Management System (Payroll DBMS)** is a specialized, integrated relational database system designed to systematically store, retrieve, manipulate, and manage all financial compensation data of an organization's employees. It encapsulates employee master records, salary structures, allowances, deductions, tax computations, attendance data, and statutory compliance information (PF, ESI, TDS) into a normalized relational schema, governed by ACID properties and SQL-based query processing.\n\nIn **KTU 2024 Scheme** terminology, the Payroll DBMS is treated as a *Case Study Application* of DBMS principles, where students must demonstrate competency in:\n\n- **Conceptual Design** (ER Modeling)\n- **Logical Design** (Relational Schema Mapping)\n- **Normalization** (up to BCNF/4NF)\n- **Physical Implementation** (DDL + DML + DCL)\n- **Advanced Features** (Views, Triggers, Stored Procedures, Cursors, Exception Handling)\n\n## 1.2 Intuitive Real-World Analogy\n\n> [!IMPORTANT]\n> **Think of Payroll DBMS as a \"Digital Salary Vault with Automatic Calculators\"**.\n\nImagine a large factory with **5,000 workers**. Before computers, a single payroll clerk would manually:\n1. Check attendance registers (paper-based)\n2. Calculate overtime on a calculator\n3. Subtract income tax (looking up tax tables)\n4. Write cheques for each employee\n5. File stack of registers in a room\n\nThis process was **slow**, **error-prone**, **non-scalable**, and **insecure**. Now imagine converting this entire physical paperwork system into a **structured, queryable, automated digital repository** where:\n- Each employee is a *row* in a table\n- Salary rules are *constraints/triggers* that fire automatically\n- The system generates payslips, bank transfer files, and tax reports with a single SQL command\n\nThat digital repository **is the Payroll Database Management System**.\n\n## 1.3 Core Entities of a Payroll System\n\n| # | Entity | Purpose |\n|---|--------|---------|\n| 1 | **Employee** | Master record of personal & job details |\n| 2 | **Department** | Organizational structure |\n| 3 | **Designation** | Job role / position |\n| 4 | **Salary_Structure** | Base pay, HRA, DA, allowances |\n| 5 | **Attendance** | Daily/monthly presence & overtime |\n| 6 | **Deductions** | PF, ESI, Professional Tax, TDS |\n| 7 | **Payroll_Transaction** | Monthly processed salary record |\n| 8 | **Bank_Account** | Disbursement details |\n| 9 | **Tax_Slab** | Income tax computation rules |\n\n## 1.4 Key Standard Metrics & Constants\n\n- **PF (Provident Fund) Contribution Rate**: **12% of Basic Pay** (employee + employer)\n- **ESI (Employee State Insurance)**: **0.75% (employee) + 3.25% (employer)** of gross salary (if applicable)\n- **Professional Tax (Kerala)**: ₹**200/month** (for gross > ₹11,250)\n- **TDS**: As per **New/Old Regime** income tax slabs\n- **Financial Year**: April 1 to March 31 (Indian Standard)\n- **HRA Exemption Limit**: **50% of Basic** (metro) / **40% of Basic** (non-metro)\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Architecture of Payroll DBMS (Three-Schema Architecture)\n\nThe Payroll DBMS follows the classic **ANSI/SPARC Three-Schema Architecture**:\n\n1. **Internal Level (Physical)** → How data is stored on disk (B+ trees, indexes on `emp_id`, `dept_id`)\n2. **Conceptual Level (Logical)** → Entire payroll database schema (tables, relationships, constraints)\n3. **External Level (View)** → User-specific views (e.g., `Employee_Payslip_View`, `Manager_Dashboard_View`)\n\n## 2.2 Theoretical Phases of Payroll DBMS Design\n\n### Phase 1: Requirements Analysis\n- Identify stakeholders: HR, Finance, Auditors, Employees, Banks\n- Functional requirements: salary calculation, tax deduction, payslip generation\n- Non-functional: security, audit trail, concurrency\n\n### Phase 2: Conceptual Design (ER Model)\n- Identify **Entities** (Employee, Department, Salary, etc.)\n- Identify **Attributes** (simple, composite, multi-valued, derived)\n- Identify **Relationships** (Works_In, Belongs_To, Has_Account)\n- Draw **ER Diagram** with cardinality ratios\n\n### Phase 3: Logical Design (Relational Mapping)\n- Convert ER to Relations using **7-step algorithm**:\n  1. Strong entity → Table\n  2. Weak entity → Table with FK to owner\n  3. 1:1 relationship → FK in either table\n  4. 1:N relationship → FK on the \"many\" side\n  5. M:N relationship → Separate junction table\n  6. Multi-valued attribute → Separate table\n  7. Derived attribute → Not stored (computed via query)\n\n### Phase 4: Normalization\n- Apply **1NF → 2NF → 3NF → BCNF** to eliminate anomalies\n\n### Phase 5: Physical Design & Implementation\n- DDL (CREATE TABLE), DML (INSERT, UPDATE), DQL (SELECT), TCL (COMMIT)\n\n## 2.3 KTU Formula Sheet (Cheat Sheet)\n\n> [!IMPORTANT]\n> All formulas below are **KTU high-yield** and appear frequently in board examinations.\n\n| # | Concept | Formula / Rule | Purpose |\n|---|---------|---------------|---------|\n| 1 | **Gross Salary** | $\\text{GS} = \\text{Basic} + \\text{DA} + \\text{HRA} + \\text{TA} + \\text{MA} + \\text{SA}$ | Total earnings before deduction |\n| 2 | **Net Salary (Take-Home)** | $\\text{NS} = \\text{GS} - \\text{Total Deductions}$ | Final disbursed amount |\n| 3 | **Total Deductions** | $\\text{TD} = \\text{PF} + \\text{ESI} + \\text{PT} + \\text{TDS} + \\text{Loan EMI}$ | Statutory + voluntary cuts |\n| 4 | **PF Deduction** | $\\text{PF} = 0.12 \\times \\text{Basic Pay}$ | Provident Fund (employee share) |\n| 5 | **ESI Deduction** | $\\text{ESI} = 0.0075 \\times \\text{GS}$ (if GS ≤ ₹21,000) | Employee State Insurance |\n| 6 | **Overtime Pay** | $\\text{OT} = \\frac{\\text{Basic}}{26 \\text{ days} \\times 8 \\text{ hrs}} \\times 2 \\times \\text{OT hours}$ | Double rate for extra hours |\n| 7 | **1NF Condition** | All attributes must be **atomic** (no multi-valued fields) | Eliminate repeating groups |\n| 8 | **2NF Condition** | 1NF + **no partial dependency** (all non-key attrs depend on full PK) | Eliminate partial dependencies |\n| 9 | **3NF Condition** | 2NF + **no transitive dependency** (non-key $\\rightarrow$ non-key prohibited) | Eliminate transitive dependencies |\n| 10 | **BCNF Condition** | 3NF + **for every FD $X \\rightarrow Y$, $X$ must be a super key** | Stricter version of 3NF |\n| 11 | **Cardinality (1:N)** | One Department has many Employees | FK on the \"many\" side |\n| 12 | **M:N Mapping** | Create junction table with composite PK | Resolves many-to-many |\n| 13 | **ACID Properties** | **A**tomicity, **C**onsistency, **I**solation, **D**urability | Transaction reliability |\n| 14 | **Lossless Join** | $(R_1 \\cap R_2) \\rightarrow R_1 \\text{ or } R_2$ | Decomposition preserves data |\n| 15 | **Dependency Preservation** | $(F_1 \\cup F_2 \\cup \\ldots \\cup F_n)^+ = F^+$ | All FDs retained post-decomposition |\n\n> **NOTE on Payroll Constants (India)**:\n> - PF Wage Ceiling: **₹15,000/month**\n> - ESI Wage Ceiling: **₹21,000/month**\n> - Standard Working Days: **26 days/month**\n> - Standard Working Hours: **8 hours/day**\n\n## 2.4 Real-World Engineering Utility\n\nPayroll DBMS principles extend to:\n- **Banking Systems** (transaction processing)\n- **ERP Systems** (SAP, Oracle Financials)\n- **Hospital Management** (billing, insurance)\n- **E-commerce** (order processing, invoicing)\n- **Government Tax Portals** (TDS, GST filing)\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Exhaustive ER-to-Relational Mapping Derivation\n\nConsider a simplified Payroll scenario. We derive the relational schema step-by-step.\n\n### Step 1: Identify Entities and Attributes\n\n**Employee** (`EmpID`, `EmpName`, `DOB`, `DOJ`, `Gender`, `BasicPay`, `Email`, `Phone`)\n- `EmpID` → Primary Key\n- `BasicPay` → Stored attribute\n- `Age` → Derived (not stored)\n\n**Department** (`DeptID`, `DeptName`, `Location`)\n- `DeptID` → Primary Key\n\n**Salary_Structure** (`StructID`, `Basic`, `DA`, `HRA`, `TA`, `MA`, `SpecialAllowance`, `EmpID`)\n- `StructID` → Primary Key\n- `EmpID` → Foreign Key\n\n**Deductions** (`DedID`, `PF`, `ESI`, `PT`, `TDS`, `EmpID`, `Month`, `Year`)\n- `DedID` → Primary Key\n- Composite key candidate: (`EmpID`, `Month`, `Year`)\n\n**Payroll_Master** (`PayrollID`, `EmpID`, `Month`, `Year`, `GrossSal`, `TotalDed`, `NetSal`, `PayDate`)\n- `PayrollID` → Primary Key\n\n### Step 2: Identify Relationships\n\n| Relationship | Between | Cardinality | Action |\n|--------------|---------|-------------|--------|\n| **Works_In** | Employee – Department | N:1 | Add `DeptID` (FK) to Employee |\n| **Has_Structure** | Employee – Salary_Structure | 1:1 | Share PK or add FK |\n| **Subjected_To** | Employee – Deductions | 1:N | FK `EmpID` in Deductions |\n| **Generates** | Employee – Payroll_Master | 1:N | FK `EmpID` in Payroll_Master |\n\n### Step 3: Derive the Relational Schema (with Constraints)\n\n| Table Name | Attributes | Primary Key | Foreign Keys | Constraints |\n|------------|-----------|-------------|--------------|-------------|\n| **Department** | `DeptID`, `DeptName`, `Location` | `DeptID` | None | `DeptID` INT NOT NULL UNIQUE |\n| **Employee** | `EmpID`, `EmpName`, `DOB`, `DOJ`, `Gender`, `BasicPay`, `Email`, `Phone`, `DeptID` | `EmpID` | `DeptID` → Department | `BasicPay` ≥ 0; `Email` UNIQUE |\n| **Salary_Structure** | `StructID`, `Basic`, `DA`, `HRA`, `TA`, `MA`, `SpecialAllowance`, `EmpID` | `StructID` | `EmpID` → Employee | All numeric > 0 |\n| **Deductions** | `DedID`, `PF`, `ESI`, `PT`, `TDS`, `LoanEMI`, `EmpID`, `Month`, `Year` | `DedID` | `EmpID` → Employee | UNIQUE(`EmpID`, `Month`, `Year`) |\n| **Payroll_Master** | `PayrollID`, `EmpID`, `Month`, `Year`, `GrossSal`, `TotalDed`, `NetSal`, `PayDate` | `PayrollID` | `EmpID` → Employee | CHECK (`NetSal` ≥ 0) |\n\n## 3.2 Normalization Derivation (Step-by-Step)\n\n### Starting Relation (Un-normalized — 0NF)\n\n$$\\text{PAYROLL\\_UNF} = \\{ \\text{EmpID}, \\text{EmpName}, \\text{DeptName}, \\text{BasicPay}, (\\text{PF}, \\text{ESI}, \\text{PT}, \\text{TDS}, \\text{Month}, \\text{Year}) \\}$$\n\n> **Problem:** The deductions + month/year form a **repeating group** (multi-valued attribute).\n\n### Apply 1NF: Atomicity\n\nRemove the multi-valued group by creating a separate table:\n\n$$\\text{PAYROLL\\_1NF} = \\{ \\underline{\\text{EmpID}, \\text{Month}, \\text{Year}}, \\text{EmpName}, \\text{DeptName}, \\text{BasicPay}, \\text{PF}, \\text{ESI}, \\text{PT}, \\text{TDS} \\}$$\n\n- Primary Key: Composite (`EmpID`, `Month`, `Year`)\n- All attributes atomic ✓\n\n### Apply 2NF: Remove Partial Dependencies\n\n**Partial Dependency Identified:**\n- `EmpName`, `DeptName`, `BasicPay` depend *only* on `EmpID` (part of PK), not on full (`EmpID`, `Month`, `Year`)\n\n**Decompose:**\n\n$$\\text{EMP\\_2NF} = \\{ \\underline{\\text{EmpID}}, \\text{EmpName}, \\text{DeptName}, \\text{BasicPay} \\}$$\n\n$$\\text{SALARY\\_2NF} = \\{ \\underline{\\text{EmpID}, \\text{Month}, \\text{Year}}, \\text{PF}, \\text{ESI}, \\text{PT}, \\text{TDS} \\}$$\n\n### Apply 3NF: Remove Transitive Dependencies\n\n**Transitive Dependency in EMP_2NF:**\n- `EmpID` → `DeptID` → `DeptName, Location`\n- (`DeptName` depends on `DeptID`, not directly on `EmpID`)\n\n**Decompose:**\n\n$$\\text{EMPLOYEE\\_3NF} = \\{ \\underline{\\text{EmpID}}, \\text{EmpName}, \\text{DeptID}, \\text{BasicPay} \\}$$\n\n$$\\text{DEPARTMENT\\_3NF} = \\{ \\underline{\\text{DeptID}}, \\text{DeptName}, \\text{Location} \\}$$\n\n$$\\text{SALARY\\_3NF} = \\{ \\underline{\\text{EmpID}, \\text{Month}, \\text{Year}}, \\text{PF}, \\text{ESI}, \\text{PT}, \\text{TDS} \\}$$\n\n### BCNF Verification\n\nFor every non-trivial FD $X \\rightarrow Y$, check if $X$ is a super key:\n- `EmpID` → all Emp attrs ✓ (super key)\n- `DeptID` → all Dept attrs ✓ (super key)\n- (`EmpID`, `Month`, `Year`) → all Salary attrs ✓ (super key)\n\n**Final BCNF Schema: 3NF = BCNF in this case ✓**\n\n## 3.3 Gross Salary Calculation Derivation\n\nGiven a monthly payroll record, compute Net Salary:\n\n$$\\text{Gross Salary (GS)} = \\text{Basic} + \\text{DA} + \\text{HRA} + \\text{TA} + \\text{MA} + \\text{SA} + \\text{OT}$$\n\n$$\\text{Total Deductions (TD)} = \\text{PF} + \\text{ESI} + \\text{PT} + \\text{TDS} + \\text{Loan EMI}$$\n\n$$\\boxed{\\text{Net Salary} = \\text{GS} - \\text{TD}}$$\n\n**Numerical Worked Example:**\n\n| Component | Value (₹) |\n|-----------|-----------|\n| Basic Pay | 30,000 |\n| DA (10% of Basic) | 3,000 |\n| HRA (40% of Basic) | 12,000 |\n| TA (Transport Allowance) | 1,600 |\n| MA (Medical Allowance) | 1,250 |\n| Overtime (10 hrs @ ₹144/hr) | 1,440 |\n| **Gross Salary** | **49,290** |\n\n| Deduction | Value (₹) |\n|-----------|-----------|\n| PF (12% of Basic) | 3,600 |\n| ESI (0.75% of GS) | 369.68 |\n| Professional Tax | 200 |\n| TDS (assumed) | 1,500 |\n| Loan EMI | 2,000 |\n| **Total Deductions** | **7,669.68** |\n\n$$\\text{Net Salary} = 49{,}290 - 7{,}669.68 = 41{,}620.32$$\n\n## 3.4 Analytical Matrix: Common Payroll Queries\n\n| Query Type | Purpose | SQL Construct |\n|------------|---------|---------------|\n| **Aggregate Queries** | Total payroll, department-wise salary sum | `SUM()`, `GROUP BY`, `HAVING` |\n| **Join Queries** | Combine employee with department & salary | `INNER JOIN`, `LEFT JOIN` |\n| **Nested Queries** | Employees earning above department average | Subquery with `>` |\n| **View Creation** | Hide sensitive columns (TDS, Bank A/C) | `CREATE VIEW` |\n| **Triggers** | Auto-update Net Salary on PF/PT change | `AFTER INSERT/UPDATE` |\n| **Cursors** | Process payroll row-by-row in PL/SQL | `DECLARE CURSOR` |\n| **Exception Handling** | Handle division by zero / missing data | `EXCEPTION WHEN` |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 ER Diagram for Payroll DBMS\n\n```mermaid\nerDiagram\n    DEPARTMENT ||--o{ EMPLOYEE : \"Works_In\"\n    EMPLOYEE ||--|| SALARY_STRUCTURE : \"Has\"\n    EMPLOYEE ||--o{ DEDUCTIONS : \"Subjected_To\"\n    EMPLOYEE ||--o{ PAYROLL_MASTER : \"Generates\"\n    EMPLOYEE ||--|| BANK_ACCOUNT : \"Receives_In\"\n    EMPLOYEE ||--o{ ATTENDANCE : \"Records\"\n    EMPLOYEE ||--o{ LEAVE_RECORD : \"Avails\"\n    SALARY_SLAB ||--o{ EMPLOYEE : \"Applicable_To\"\n    \n    DEPARTMENT {\n        int DeptID PK\n        string DeptName\n        string Location\n        string HOD_Name\n    }\n    \n    EMPLOYEE {\n        int EmpID PK\n        string EmpName\n        date DOB\n        date DOJ\n        string Gender\n        decimal BasicPay\n        string Email UK\n        string Phone\n        int DeptID FK\n        int DesignationID FK\n    }\n    \n    SALARY_STRUCTURE {\n        int StructID PK\n        decimal Basic\n        decimal DA\n        decimal HRA\n        decimal TA\n        decimal MA\n        decimal SpecialAllowance\n        int EmpID FK\n    }\n    \n    DEDUCTIONS {\n        int DedID PK\n        decimal PF\n        decimal ESI\n        decimal PT\n        decimal TDS\n        decimal LoanEMI\n        int EmpID FK\n        int Month\n        int Year\n    }\n    \n    PAYROLL_MASTER {\n        int PayrollID PK\n        int EmpID FK\n        int Month\n        int Year\n        decimal GrossSal\n        decimal TotalDed\n        decimal NetSal\n        date PayDate\n        string Status\n    }\n    \n    BANK_ACCOUNT {\n        int AccID PK\n        int EmpID FK\n        string BankName\n        string AccountNo UK\n        string IFSC\n    }\n    \n    ATTENDANCE {\n        int AttID PK\n        int EmpID FK\n        date AttDate\n        int HoursWorked\n        int OTHours\n        string Status\n    }\n    \n    LEAVE_RECORD {\n        int LeaveID PK\n        int EmpID FK\n        date FromDate\n        date ToDate\n        string LeaveType\n        int DaysCount\n        string ApprovalStatus\n    }\n    \n    SALARY_SLAB {\n        int SlabID PK\n        decimal MinAmount\n        decimal MaxAmount\n        decimal TaxPercent\n    }\n```\n\n## 4.2 Block-Level Functional Architecture Flow\n\n```mermaid\nflowchart TD\n    A[Employee Master Data<br/>HR Input] --> B[Payroll DBMS<br/>Central Repository]\n    C[Attendance & Leave<br/>Biometric/Manual] --> B\n    D[Salary Structure<br/>Finance Team] --> B\n    E[Tax Slabs<br/>Income Tax Dept] --> B\n    \n    B --> F[Payroll Processing Engine<br/>Triggers and Stored Procs]\n    \n    F --> G{Gross Salary<br/>Calculation}\n    G --> H[Deduction Engine<br/>PF ESI PT TDS]\n    H --> I[Net Salary<br/>Computation]\n    \n    I --> J[Payslip Generation<br/>View Report]\n    I --> K[Bank Transfer File<br/>NEFT RTGS]\n    I --> L[Tax Reports<br/>Form 16 TDS]\n    I --> M[Statutory Reports<br/>PF ESI Challans]\n    \n    J --> N[Employee Self-Service Portal]\n    K --> O[Bank Server]\n    L --> P[Income Tax Portal]\n    M --> Q[EPFO ESIC Portal]\n    \n    style A fill:#E3F2FD,stroke:#1976D2\n    style C fill:#E3F2FD,stroke:#1976D2\n    style D fill:#E3F2FD,stroke:#1976D2\n    style E fill:#E3F2FD,stroke:#1976D2\n    style B fill:#FFF3E0,stroke:#F57C00\n    style F fill:#FFF3E0,stroke:#F57C00\n    style G fill:#F3E5F5,stroke:#7B1FA2\n    style H fill:#F3E5F5,stroke:#7B1FA2\n    style I fill:#F3E5F5,stroke:#7B1FA2\n    style J fill:#E8F5E9,stroke:#388E3C\n    style K fill:#E8F5E9,stroke:#388E3C\n    style L fill:#E8F5E9,stroke:#388E3C\n    style M fill:#E8F5E9,stroke:#388E3C\n```\n\n## 4.3 Normalization Flow (0NF → BCNF)\n\n```mermaid\nflowchart TD\n    A0[0NF: Un-normalized Form<br/>Repeating groups present] -->|Atomicity| A1[1NF: First Normal Form<br/>All attributes atomic]\n    A1 -->|Remove partial dependencies| A2[2NF: Second Normal Form<br/>Full functional dependency on PK]\n    A2 -->|Remove transitive dependencies| A3[3NF: Third Normal Form<br/>No transitive dependencies]\n    A3 -->|Stricter check: X must be super key| A4[BCNF: Boyce-Codd NF<br/>Lossless + Dependency Preserving]\n    \n    A4 -->|Further refinement| A5[4NF: Multi-valued dependencies]\n    A4 -->|Further refinement| A6[5NF: Join dependencies]\n    \n    style A0 fill:#FFCDD2,stroke:#C62828\n    style A1 fill:#FFE0B2,stroke:#E65100\n    style A2 fill:#FFF9C4,stroke:#F9A825\n    style A3 fill:#C8E6C9,stroke:#2E7D32\n    style A4 fill:#B2DFDB,stroke:#00695C\n    style A5 fill:#B3E5FC,stroke:#01579B\n    style A6 fill:#D1C4E9,stroke:#4527A0\n```\n\n## 4.4 Schema Architecture (Relational Tables)\n\n```mermaid\nflowchart LR\n    DEPT[DEPARTMENT<br/>DeptID PK<br/>DeptName<br/>Location]\n    EMP[EMPLOYEE<br/>EmpID PK<br/>DeptID FK<br/>BasicPay]\n    SS[SALARY_STRUCTURE<br/>StructID PK<br/>EmpID FK]\n    DED[DEDUCTIONS<br/>DedID PK<br/>EmpID FK<br/>Month Year]\n    PM[PAYROLL_MASTER<br/>PayrollID PK<br/>EmpID FK<br/>NetSal]\n    BA[BANK_ACCOUNT<br/>AccID PK<br/>EmpID FK]\n    \n    DEPT -->|1:N| EMP\n    EMP -->|1:1| SS\n    EMP -->|1:N| DED\n    EMP -->|1:N| PM\n    EMP -->|1:1| BA\n    \n    style DEPT fill:#E1BEE7,stroke:#6A1B9A\n    style EMP fill:#FFCCBC,stroke:#BF360C\n    style SS fill:#C5E1A5,stroke:#33691E\n    style DED fill:#FFE082,stroke:#FF6F00\n    style PM fill:#B39DDB,stroke:#4527A0\n    style BA fill:#80DEEA,stroke:#00838F\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 PART A Questions (3 Marks Each)\n\n### Question 1 [KTU University Exam - Dec 2023]\n**Define Payroll Database Management System. List any four entities involved in a typical payroll system.**\n\n**Model Answer (3 marks):**\n\nA **Payroll Database Management System** is a relational database system designed to automate and manage employee compensation processes including salary computation, deductions, tax calculations, and disbursement. It stores employee master data, salary structures, attendance, deductions, and monthly payroll transactions in an integrated, normalized schema ensuring data integrity, security, and ACID-compliant transaction processing.\n\n**Four Key Entities:**\n1. **Employee** — stores personal & job details (EmpID, Name, DOJ, BasicPay)\n2. **Department** — organizational unit (DeptID, DeptName, Location)\n3. **Salary_Structure** — earnings components (Basic, DA, HRA, TA, MA, SA)\n4. **Deductions** — statutory & voluntary cuts (PF, ESI, PT, TDS, Loan EMI)\n\n> **Examiner Note:** Award 1.5 marks for definition; 1.5 marks (0.375 × 4) for entities.\n\n---\n\n### Question 2 [KTU University Exam - May 2024]\n**Differentiate between 2NF and 3NF with a payroll database example.**\n\n**Model Answer (3 marks):**\n\n| Aspect | 2NF | 3NF |\n|--------|-----|-----|\n| **Definition** | 1NF + no partial dependency | 2NF + no transitive dependency |\n| **Handles** | Partial dependency on composite PK | Non-key → non-key dependency |\n| **Payroll Example** | Remove `EmpName` from `Salary(EmpID, Month, Year, EmpName, Basic)` since `EmpName` depends only on `EmpID` (partial) | Remove `DeptName` from `Employee(EmpID, ..., DeptID, DeptName)` since `DeptID → DeptName` is transitive via `DeptID` |\n\n> **Examiner Note:** Award 1.5 marks for clear definitions; 1.5 marks for the payroll example.\n\n---\n\n## 5.2 PART B Questions (14 Marks Each)\n\n### Question A (Choice 1) [KTU University Exam - May 2024, Module 7]\n\n**Q. (a)** Design an ER diagram for a Payroll Database Management System of an IT company. Clearly identify entities, attributes, relationships, and cardinality. (7 marks)\n\n**(b)** Convert the ER diagram into a normalized relational schema (up to 3NF) and write the DDL statements. (7 marks)\n\n#### Model Solution:\n\n### Part (a) — ER Diagram Design (7 marks)\n\n**Entities Identified:**\n\n1. **Employee** (`EmpID` PK, `EmpName`, `DOB`, `DOJ`, `Gender`, `Email`, `Phone`, `BasicPay`)\n2. **Department** (`DeptID` PK, `DeptName`, `Location`)\n3. **Project** (`ProjID` PK, `ProjName`, `StartDate`, `EndDate`, `Budget`)\n4. **Salary_Structure** (`StructID` PK, `Basic`, `DA`, `HRA`, `TA`, `SpecialAllowance`)\n5. **Deductions** (`DedID` PK, `PF`, `ESI`, `PT`, `TDS`, `Month`, `Year`)\n6. **Payroll_Master** (`PayrollID` PK, `GrossSal`, `TotalDed`, `NetSal`, `PayDate`)\n\n**Relationships:**\n\n| Relationship | Entities | Cardinality |\n|--------------|----------|-------------|\n| Works_In | Employee – Department | N:1 |\n| Assigned_To | Employee – Project | M:N |\n| Has_Structure | Employee – Salary_Structure | 1:1 |\n| Subjected_To | Employee – Deductions | 1:N |\n| Generates | Employee – Payroll_Master | 1:N |\n\n**ER Diagram (Mermaid):**\n\n```mermaid\nerDiagram\n    DEPARTMENT ||--o{ EMPLOYEE : \"Works_In\"\n    EMPLOYEE }o--o{ PROJECT : \"Assigned_To\"\n    EMPLOYEE ||--|| SALARY_STRUCTURE : \"Has\"\n    EMPLOYEE ||--o{ DEDUCTIONS : \"Subjected_To\"\n    EMPLOYEE ||--o{ PAYROLL_MASTER : \"Generates\"\n    \n    DEPARTMENT {\n        int DeptID PK\n        string DeptName\n        string Location\n    }\n    EMPLOYEE {\n        int EmpID PK\n        string EmpName\n        date DOB\n        date DOJ\n        decimal BasicPay\n        int DeptID FK\n    }\n    PROJECT {\n        int ProjID PK\n        string ProjName\n        decimal Budget\n    }\n    SALARY_STRUCTURE {\n        int StructID PK\n        decimal Basic\n        decimal DA\n        decimal HRA\n    }\n    DEDUCTIONS {\n        int DedID PK\n        decimal PF\n        decimal ESI\n        int EmpID FK\n    }\n    PAYROLL_MASTER {\n        int PayrollID PK\n        decimal NetSal\n        int EmpID FK\n    }\n```\n\n**Mark Distribution (7 marks):**\n- Entities with attributes: 3 marks\n- Relationships with cardinality: 2 marks\n- Neat ER diagram: 2 marks\n\n---\n\n### Part (b) — Normalized Schema + DDL (7 marks)\n\n**Step 1: Initial Un-normalized Form**\nContains all attributes in one relation → repeating groups present\n\n**Step 2: Apply 1NF** → All attributes atomic; create separate rows for each monthly record\n\n**Step 3: Apply 2NF** → Remove `EmpName`, `DeptName` from salary record (partial dependency)\n\n**Step 4: Apply 3NF** → Separate `Department` table; remove transitive dependency\n\n**Final Normalized Schema (3NF):**\n\n1. **Department**(`DeptID` PK, `DeptName`, `Location`)\n2. **Employee**(`EmpID` PK, `EmpName`, `DOB`, `DOJ`, `BasicPay`, `DeptID` FK)\n3. **Project**(`ProjID` PK, `ProjName`, `Budget`)\n4. **Emp_Project**(`EmpID` FK, `ProjID` FK, PRIMARY KEY(`EmpID`,`ProjID`)) — Junction table\n5. **Salary_Structure**(`StructID` PK, `Basic`, `DA`, `HRA`, `TA`, `SA`, `EmpID` FK)\n6. **Deductions**(`DedID` PK, `PF`, `ESI`, `PT`, `TDS`, `Month`, `Year`, `EmpID` FK)\n7. **Payroll_Master**(`PayrollID` PK, `GrossSal`, `TotalDed`, `NetSal`, `PayDate`, `EmpID` FK, `Month`, `Year`)\n\n**DDL Statements:**\n\n```sql\nCREATE TABLE Department (\n    DeptID INT PRIMARY KEY,\n    DeptName VARCHAR(50) NOT NULL UNIQUE,\n    Location VARCHAR(50)\n);\n\nCREATE TABLE Employee (\n    EmpID INT PRIMARY KEY,\n    EmpName VARCHAR(50) NOT NULL,\n    DOB DATE,\n    DOJ DATE,\n    BasicPay DECIMAL(10,2) CHECK (BasicPay > 0),\n    DeptID INT,\n    FOREIGN KEY (DeptID) REFERENCES Department(DeptID)\n        ON DELETE SET NULL ON UPDATE CASCADE\n);\n\nCREATE TABLE Project (\n    ProjID INT PRIMARY KEY,\n    ProjName VARCHAR(50) NOT NULL,\n    Budget DECIMAL(12,2) CHECK (Budget >= 0)\n);\n\nCREATE TABLE Emp_Project (\n    EmpID INT,\n    ProjID INT,\n    PRIMARY KEY (EmpID, ProjID),\n    FOREIGN KEY (EmpID) REFERENCES Employee(EmpID),\n    FOREIGN KEY (ProjID) REFERENCES Project(ProjID)\n);\n\nCREATE TABLE Salary_Structure (\n    StructID INT PRIMARY KEY,\n    Basic DECIMAL(10,2),\n    DA DECIMAL(10,2),\n    HRA DECIMAL(10,2),\n    TA DECIMAL(10,2),\n    SA DECIMAL(10,2),\n    EmpID INT UNIQUE,\n    FOREIGN KEY (EmpID) REFERENCES Employee(EmpID)\n);\n\nCREATE TABLE Deductions (\n    DedID INT PRIMARY KEY,\n    PF DECIMAL(8,2),\n    ESI DECIMAL(8,2),\n    PT DECIMAL(8,2),\n    TDS DECIMAL(8,2),\n    Month INT CHECK (Month BETWEEN 1 AND 12),\n    Year INT CHECK (Year BETWEEN 2000 AND 2100),\n    EmpID INT,\n    FOREIGN KEY (EmpID) REFERENCES Employee(EmpID)\n);\n\nCREATE TABLE Payroll_Master (\n    PayrollID INT PRIMARY KEY,\n    GrossSal DECIMAL(12,2),\n    TotalDed DECIMAL(10,2),\n    NetSal DECIMAL(12,2) CHECK (NetSal >= 0),\n    PayDate DATE,\n    Month INT,\n    Year INT,\n    EmpID INT,\n    FOREIGN KEY (EmpID) REFERENCES Employee(EmpID)\n);\n```\n\n**Mark Distribution (7 marks):**\n- Normalization step-by-step explanation: 3 marks\n- Final schema with PK/FK identification: 2 marks\n- DDL statements with constraints: 2 marks\n\n---\n\n### Question B (Choice 2) [KTU University Exam - Dec 2023, Module 7]\n\n**Q. (a)** Consider the following relation for a payroll database:\n`PAYROLL(EmpID, EmpName, DeptID, DeptName, BasicPay, DA, HRA, PF, ESI, Month, Year)`\nwith FDs:\n- F1: `EmpID → EmpName, DeptID, DeptName, BasicPay, DA, HRA, PF, ESI`\n- F2: `DeptID → DeptName`\n- F3: `{EmpID, Month, Year} → BasicPay, DA, HRA, PF, ESI`\n\nNormalize it up to **BCNF** with proper justification. (7 marks)\n\n**(b)** Write SQL queries to:\n1. Find the total payroll disbursed for each department in the year 2024.\n2. Display the names of employees whose net salary is greater than the average net salary of their department.\n3. Create a view `HIGH_EARNERS` showing employees with `NetSal > 50,000`. (7 marks)\n\n#### Model Solution:\n\n### Part (a) — Normalization to BCNF (7 marks)\n\n**Initial Relation (1NF):**\n`R1 = PAYROLL(EmpID, EmpName, DeptID, DeptName, BasicPay, DA, HRA, PF, ESI, Month, Year)`\nPK: `{EmpID, Month, Year}`\n\n**Identify Functional Dependencies:**\n\n| FD | Type | Violation |\n|----|------|-----------|\n| F1: `EmpID → EmpName, DeptID, DeptName, BasicPay, DA, HRA, PF, ESI` | Partial Dependency | `EmpID` is part of composite PK but determines other attrs |\n| F2: `DeptID → DeptName` | Transitive Dependency | `EmpID → DeptID → DeptName` |\n| F3: `{EmpID, Month, Year} → ...` | Full Dependency (on PK) | OK |\n\n**Step 1: Resolve Partial Dependency (2NF)**\n\nDecompose based on F1:\n\n- **R2 (Employee)** = `{EmpID, EmpName, DeptID, DeptName, BasicPay, DA, HRA, PF, ESI}` — PK: `EmpID`\n- **R3 (MonthlyPay)** = `{EmpID, Month, Year}` — PK: `{EmpID, Month, Year}`\n\n**Step 2: Resolve Transitive Dependency (3NF)**\n\nDecompose R2 to remove `DeptID → DeptName`:\n\n- **R4 (Employee)** = `{EmpID, EmpName, DeptID, BasicPay, DA, HRA, PF, ESI}` — PK: `EmpID`\n- **R5 (Department)** = `{DeptID, DeptName}` — PK: `DeptID`\n\n**Step 3: BCNF Verification**\n\nFor every FD $X \\rightarrow Y$, $X$ must be a super key.\n\n| FD | Is $X$ a super key? | BCNF? |\n|----|---------------------|-------|\n| `EmpID → ...` (R4) | Yes, `EmpID` is PK | ✓ |\n| `DeptID → DeptName` (R5) | Yes, `DeptID` is PK | ✓ |\n| `{EmpID, Month, Year}` → ... (R3) | Yes, composite PK | ✓ |\n\n**All relations are in BCNF ✓**\n\n**Final BCNF Schema:**\n\n$$\\text{Department} = \\{ \\underline{\\text{DeptID}}, \\text{DeptName} \\}$$\n\n$$\\text{Employee} = \\{ \\underline{\\text{EmpID}}, \\text{EmpName}, \\text{DeptID}, \\text{BasicPay}, \\text{DA}, \\text{HRA}, \\text{PF}, \\text{ESI} \\}$$\n\n$$\\text{MonthlyPay} = \\{ \\underline{\\text{EmpID}, \\text{Month}, \\text{Year}} \\}$$\n\n**Mark Distribution (7 marks):**\n- FD identification with types: 2 marks\n- 2NF decomposition: 2 marks\n- 3NF decomposition: 1.5 marks\n- BCNF verification: 1.5 marks\n\n---\n\n### Part (b) — SQL Queries (7 marks)\n\n**Query 1: Total payroll per department in 2024 (2 marks)**\n\n```sql\nSELECT d.DeptName, SUM(pm.NetSal) AS TotalPayroll\nFROM Department d\nJOIN Employee e ON d.DeptID = e.DeptID\nJOIN Payroll_Master pm ON e.EmpID = pm.EmpID\nWHERE pm.Year = 2024\nGROUP BY d.DeptName\nHAVING SUM(pm.NetSal) > 0\nORDER BY TotalPayroll DESC;\n```\n\n**Query 2: Employees earning above department average (3 marks)**\n\n```sql\nSELECT e.EmpName, e.DeptID, pm.NetSal\nFROM Employee e\nJOIN Payroll_Master pm ON e.EmpID = pm.EmpID\nWHERE pm.NetSal > (\n    SELECT AVG(pm2.NetSal)\n    FROM Employee e2\n    JOIN Payroll_Master pm2 ON e2.EmpID = pm2.EmpID\n    WHERE e2.DeptID = e.DeptID\n)\nORDER BY e.DeptID, pm.NetSal DESC;\n```\n\n**Query 3: Create view HIGH_EARNERS (2 marks)**\n\n```sql\nCREATE VIEW HIGH_EARNERS AS\nSELECT e.EmpID, e.EmpName, d.DeptName, pm.NetSal\nFROM Employee e\nJOIN Department d ON e.DeptID = d.DeptID\nJOIN Payroll_Master pm ON e.EmpID = pm.EmpID\nWHERE pm.NetSal > 50000\nWITH CHECK OPTION;\n```\n\n**Mark Distribution (7 marks):**\n- Query 1 with correct JOINs: 2 marks\n- Query 2 with correlated subquery: 3 marks\n- Query 3 with proper view syntax: 2 marks\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n>\n> 1. **Missing Cardinality Symbols** — Many students forget to put `1`, `N`, `M` on relationship lines. This loses **1.5 marks** instantly.\n> 2. **Wrong PK in M:N Junction Tables** — Use **composite PK** `(EmpID, ProjID)`, not a single surrogate key. Single key loses 1 mark.\n> 3. **Forgetting to Add `CHECK (NetSal >= 0)`** — Business rules must be enforced. Missing constraints = **0.5 mark** deduction per query.\n> 4. **Confusing 3NF with BCNF** — In 3NF, $X \\rightarrow Y$ is allowed if $Y$ is a *prime attribute*. In **BCNF**, NO exception. Write this clearly.\n> 5. **Not Using `WITH CHECK OPTION` in Views** — Views that allow INSERT/UPDATE must enforce their WHERE clause; missing this loses 0.5 mark.\n> 6. **Confusing PF Wage Ceiling (₹15,000) with ESI Ceiling (₹21,000)** — Examiners love asking this; wrong values = full marks cut.\n> 7. **Forgetting Foreign Key Constraints** — Every `FK` reference must be declared explicitly in DDL; implicit references lose marks.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid Revision Checklist for KTU Board Exam:**\n\n### **Core Concepts (Module 7 Focus)**\n- [ ] Payroll DBMS is a **case study application** of DBMS — focus on design, not finance\n- [ ] **9 Key Entities**: Employee, Department, Salary_Structure, Deductions, Payroll_Master, Attendance, Leave, Bank_Account, Tax_Slab\n- [ ] **Gross Salary** = Sum of all earning components (Basic + DA + HRA + TA + MA + SA + OT)\n- [ ] **Net Salary** = Gross Salary − Total Deductions\n- [ ] **PF** = 12% of Basic; **ESI** = 0.75% of Gross (if eligible)\n- [ ] Standard working days = **26 days/month**; hours = **8 hours/day**\n\n### **ER & Relational Design**\n- [ ] 1:N relationship → FK on the \"many\" side\n- [ ] M:N relationship → New junction table with composite PK\n- [ ] 1:1 relationship → FK in either table\n- [ ] Multi-valued attribute → Separate table\n- [ ] Derived attribute → Computed, NOT stored\n\n### **Normalization Path (Must Memorize Order)**\n- [ ] **1NF** → Atomicity (no multi-valued)\n- [ ] **2NF** → No partial dependency (on composite PK)\n- [ ] **3NF** → No transitive dependency (non-key → non-key)\n- [ ] **BCNF** → For every FD, LHS must be super key\n\n### **SQL Must-Know Queries**\n- [ ] Aggregate functions: `SUM()`, `AVG()`, `MAX()`, `MIN()`, `COUNT()`\n- [ ] `GROUP BY` + `HAVING` (filter after grouping)\n- [ ] `INNER JOIN`, `LEFT OUTER JOIN`, `RIGHT OUTER JOIN`, `FULL OUTER JOIN`\n- [ ] Correlated subqueries (use outer query column in inner WHERE)\n- [ ] `CREATE VIEW ... WITH CHECK OPTION`\n- [ ] `CREATE TRIGGER ... AFTER INSERT/UPDATE/DELETE`\n- [ ] `DECLARE CURSOR` for row-by-row processing\n- [ ] `EXCEPTION WHEN OTHERS THEN ...` for error handling\n\n### **ACID Properties (Frequently Asked)**\n- [ ] **Atomicity** — All or nothing\n- [ ] **Consistency** — Valid state → Valid state\n- [ ] **Isolation** — Concurrent transactions don't interfere\n- [ ] **Durability** — Committed data survives crashes\n\n### **KTU Exam Pattern (Module 7)**\n- [ ] Part A (3 marks) → Definition + entity listing (very common)\n- [ ] Part B (14 marks) → Choice between (a) ER/Design + (b) Normalization/SQL\n- [ ] Always write **both** 1NF/2NF/3NF step with **justification** (not just final table)\n- [ ] Always include **cardinality** in ER diagram\n- [ ] Always add **constraints** (PK, FK, CHECK, UNIQUE) in DDL\n\n> **Final Tip:** In KTU exams, the **Payroll DBMS question is a \"safe bet\"** — it tests all 5 modules together. Master ER + Normalization + SQL + DDL + Triggers and you can score 14/14. Practice at least 3 past-year questions before the exam.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "insuranceproductsdatabasemanagement": {
      "title": "Insurance Products Database Management",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 8\n> - **Topic:** Insurance Products Database Management\n\n<!-- SECTION_1_START -->\n# Insurance Products Database Management\n\n## 1.1 Core Technical Definition\n\n**Insurance Products Database Management** refers to the systematic design, development, implementation, and administration of a relational (or object-relational) database system specifically engineered to store, retrieve, manipulate, and secure the voluminous data associated with the insurance industry. It encapsulates the modelling of heterogeneous insurance products (Life, Health, Motor, Property, Travel), the policy lifecycle (proposal → underwriting → issuance → renewal → maturity/claim), premium calculations, claims adjudication, agent-commission tracking, and regulatory compliance reporting.\n\nIn KTU 2024 Scheme terminology, it sits at the intersection of **Domain-Specific Database Application Engineering** and **Enterprise Information Systems Design**, drawing heavily from Module 8's focus on advanced database applications and contemporary industry implementations.\n\n> [!IMPORTANT]\n> **Syllabus Highlight (Module 8):** This topic integrates ER modelling (Module 2), Relational Schema & Normalization (Module 3), SQL (Module 4), Transaction & Concurrency Control (Module 5), and contemporary database paradigms (Modules 6–7) into a single real-world industry case study.\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine an insurance company as a **massive digital filing cabinet** that handles millions of paper policies, receipts, medical reports, accident photographs, and legal documents every single day. Without a database, every clerk would rummage through endless stacks of files to find a single customer's policy number — slow, error-prone, and chaotic.\n\nNow replace the cabinet with a **well-indexed library system**: each customer gets a unique card, every policy is logged with an ISBN-like identifier, and a librarian (the DBMS) instantly retrieves the right file using a structured query.\n\n| Insurance Domain Object | Library/Index Analogy | Database Equivalent |\n|---|---|---|\n| Policyholder | Library Member Card | `CUSTOMER` entity (Primary Key: `cust_id`) |\n| Insurance Policy | Borrowed Book | `POLICY` entity (Primary Key: `policy_id`) |\n| Premium Receipt | Due-date Slip | `PREMIUM_PAYMENT` table |\n| Claim Form | Damage Report | `CLAIM` table |\n| Insurance Agent | Librarian | `AGENT` table |\n\n> [!NOTE]\n> **Why this matters:** Insurance is fundamentally a **data-intensive business**. The accuracy of a single claim payout can hinge on retrieving 20+ years of historical premium data correctly. A robust DBMS is therefore not optional — it is the *operational backbone* of the entire industry.\n\n## 1.3 Key Entities & Metrics in Insurance DBMS\n\n- **Premium** = `Sum Assured × Premium Rate × Risk Factor` (units: currency/year)\n- **Sum Assured (SA)**: Maximum payout — a **monetary cap** defined per policy.\n- **Risk Factor (RF)**: A **multiplier ∈ [0.5, 5.0]** based on actuarial tables.\n- **Surrender Value**: Portion of premiums refundable on early termination.\n- **Grace Period**: Typically **15 days** (life) or **30 days** (general insurance) post-due-date.\n\n> [!NOTE]\n> **Standard Industry Constants used in this Module:**\n> - IRDAI (Insurance Regulatory and Development Authority of India) compliance retention: **10 years minimum** for policy records.\n> - Standard claim settlement TAT (Turn-Around-Time): **30 days** for non-investigative claims.\n> - Standard commission rate for agents: **15%** of first-year premium (life insurance benchmark).\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Structural Components of an Insurance Database\n\nAn Insurance Products Database Management System (IPDBMS) comprises **eight logical sub-systems**, each mapped to a set of relational tables.\n\n1. **Customer Master Subsystem** — Personal, KYC, and demographic data.\n2. **Product Catalog Subsystem** — Defines available insurance products.\n3. **Policy Issuance Subsystem** — Manages policy creation, modification, and status.\n4. **Premium & Billing Subsystem** — Tracks payment schedules and receipts.\n5. **Claims Processing Subsystem** — Adjudicates and disburses claims.\n6. **Agent & Commission Subsystem** — Manages distribution channel.\n7. **Underwriting & Risk Subsystem** — Stores actuarial and risk evaluation data.\n8. **Audit & Compliance Subsystem** — Regulatory, financial, and security logs.\n\n### 2.1.1 Step-by-Step Logical Decomposition\n\n- **Step 1 (Customer Onboarding):** Customer submits KYC (Know Your Customer) documents. A `CUSTOMER` row is inserted with a system-generated `cust_id`.\n- **Step 2 (Product Selection):** Agent selects a `PRODUCT` from the catalog. The product's premium rate, term, and coverage details are fetched.\n- **Step 3 (Risk Evaluation):** Underwriter assesses the proposal; data stored in `RISK_ASSESSMENT` table.\n- **Step 4 (Policy Generation):** A `POLICY` row is created with `policy_status = 'ACTIVE'` only after successful underwriting approval.\n- **Step 5 (Premium Collection):** Periodic `PREMIUM_PAYMENT` rows are inserted; `last_paid_date` and `next_due_date` are updated.\n- **Step 6 (Claim Filing):** On an insured event, a `CLAIM` row is created linked to the parent `POLICY`.\n- **Step 7 (Claim Settlement):** Status transitions `FILED → UNDER_REVIEW → APPROVED/REJECTED → DISBURSED`.\n- **Step 8 (Renewal/Maturity):** Status updated, policy extended or terminated, archival triggered.\n\n> [!IMPORTANT]\n> **Why these steps matter:** Each transition must satisfy **ACID** properties (Atomicity, Consistency, Isolation, Durability). For example, a partial premium payment that updates the policy status without updating the receipt would violate Consistency — a common KTU exam question.\n\n## 2.2 KTU Formula Sheet / Cheat Sheet\n\n| # | Formula / Concept | Expression | Variables / Units | Real-World Use |\n|---|---|---|---|---|\n| 1 | Annual Premium | $P = \\frac{SA \\times R \\times RF}{1000}$ | $SA$ = Sum Assured (₹); $R$ = Base Rate per ₹1000 SA; $RF$ = Risk Factor (dimensionless) | Motor & Term Insurance premium |\n| 2 | Surrender Value (Life) | $SV = \\sum P_{paid} \\times SV_{factor}$ | $P_{paid}$ = Total premiums paid; $SV_{factor}$ ∈ [0.30, 0.85] | Early policy termination payout |\n| 3 | Claim Ratio (Loss Ratio) | $LR = \\frac{\\text{Total Claims Settled}}{\\text{Total Premium Collected}} \\times 100$ | Expressed as **%** | Insurer profitability metric |\n| 4 | Solvency Margin | $SM = \\frac{\\text{Available Solvency Margin}}{\\text{Required Solvency Margin}} \\times 100$ | **Must be ≥ 150%** (IRDAI norm) | Regulatory compliance |\n| 5 | Persistency Ratio | $PR_{13} = \\frac{\\text{Policies renewed at month 13}}{\\text{Policies issued 13 months ago}} \\times 100$ | Industry benchmark: **> 85%** | Customer retention tracking |\n| 6 | Commission Earned | $C = P_{first} \\times 0.15 + P_{renewal} \\times 0.075$ | Standard agent rates | Agent payroll |\n| 7 | Normalization Target (IPDBMS) | $3NF \\le \\text{Schema} \\le BCNF$ | For OLTP claims/policy data | Reduces redundancy |\n| 8 | Transaction Throughput | $TPS = \\frac{N_{tx}}{t}$ | $N_{tx}$ = Transactions; $t$ = time (seconds) | DBMS performance metric |\n| 9 | B-Tree Index Depth | $d = \\lceil \\log_{m}(N) \\rceil$ | $m$ = order; $N$ = records | Index design |\n| 10 | Hash-Based Search Cost | $O(1)$ average | $O(n)$ worst case | Fast claim lookup |\n\n> [!WARNING]\n> **Common Mistake:** In Markdown tables, never write absolute values like $|x|$ inside table cells — the vertical pipe `|` breaks the table parser. Always use $\\lvert x \\rvert$ or $\\mid x \\mid$.\n\n## 2.3 Why Insurance Demands a Robust DBMS — The \"Why & How\"\n\n- **Why?**: Insurance processes **millions of concurrent transactions** during renewal seasons and catastrophic events (e.g., floods, pandemics).\n- **How?**: A well-normalized schema (typically **3NF**), strong referential integrity via Foreign Keys, **indexes** on `policy_id`, `cust_id`, `claim_id`, and **stored procedures** for premium recalculation address these demands.\n\n> [!NOTE]\n> **Real-world utility:** Insurance databases power everything from LIC's 300-million+ policy records to Ola's dynamic ride-insurance claims. Every digital insurance experience you encounter — from buying a policy on PhonePe to filing a motor claim on Acko — is backed by a sophisticated DBMS.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Exhaustive Schema Design for an Insurance Database\n\nWe begin by deriving a normalized relational schema for an IPDBMS using **bottom-up ER-to-Relational mapping**.\n\n### 3.1.1 ER-to-Relational Mapping Walkthrough\n\n**Given the ER Model** (entities: CUSTOMER, POLICY, PRODUCT, AGENT, CLAIM, PAYMENT, BENEFICIARY):\n\n**Mapping Rule 1 — Strong Entities:** Each strong entity becomes a table with its simple attributes as columns and the key attribute as the primary key.\n\n- `CUSTOMER(cust_id PK, fname, lname, dob, gender, address, phone, email, pan_no, aadhaar_no, kyc_status)`\n- `POLICY(policy_id PK, policy_no, issue_date, maturity_date, sum_assured, status, cust_id FK, product_id FK, agent_id FK)`\n- `PRODUCT(product_id PK, product_name, product_type, base_rate, term_years, min_age, max_age)`\n- `AGENT(agent_id PK, agent_name, branch_id, joining_date, commission_rate)`\n- `CLAIM(claim_id PK, policy_id FK, claim_date, claim_amount, status, surveyor_id, settlement_date)`\n- `PAYMENT(payment_id PK, policy_id FK, due_date, paid_date, amount, mode, receipt_no)`\n- `BENEFICIARY(benef_id PK, policy_id FK, name, relation, share_pct)`\n\n**Mapping Rule 2 — Weak Entities:** Become tables with the owner's PK as a composite FK + partial key.\n\n- `NOMINEE(nominee_id PK, policy_id FK, name, relation, dob)` where `policy_id` is FK → POLICY.\n\n**Mapping Rule 3 — M:N Relationships:** New junction tables.\n\n- `POLICY_PRODUCT_RIDER(rider_id PK, policy_id FK, rider_type, additional_premium, coverage_amt)`\n\n**Mapping Rule 4 — 1:N Relationships:** Foreign key on the \"many\" side.\n\n- `POLICY` already carries `agent_id FK` and `cust_id FK`.\n\n### 3.1.2 Normalization to 3NF — Exhaustive Derivation\n\n**Step 1 — Identify Functional Dependencies:**\n\n$$\n\\begin{aligned}\n\\text{FD}_1 &: \\text{cust\\_id} \\rightarrow \\text{fname, lname, dob, pan\\_no} \\\\\n\\text{FD}_2 &: \\text{policy\\_id} \\rightarrow \\text{cust\\_id, product\\_id, sum\\_assured, status} \\\\\n\\text{FD}_3 &: \\text{product\\_id} \\rightarrow \\text{product\\_name, base\\_rate, term\\_years} \\\\\n\\text{FD}_4 &: \\text{payment\\_id} \\rightarrow \\text{policy\\_id, amount, due\\_date, paid\\_date} \\\\\n\\text{FD}_5 &: \\text{claim\\_id} \\rightarrow \\text{policy\\_id, claim\\_amount, status}\n\\end{aligned}\n$$\n\n**Step 2 — Check for 1NF Violations:** Original table may have multi-valued attributes (e.g., `beneficiaries` containing multiple names). Split into separate rows.\n\n**Step 3 — Check for 2NF Violations:** All non-key attributes must depend on the *whole* PK, not part of it. Given single-attribute PKs throughout, no partial dependencies exist.\n\n**Step 4 — Check for 3NF Violations:** Look for transitive dependencies.\n\n$$\n\\text{policy\\_id} \\rightarrow \\text{product\\_id} \\rightarrow \\text{product\\_name}\n$$\n\nThis is a transitive dependency. **Resolution:** Move `product_name, base_rate` to a separate `PRODUCT` table — already done in our schema. ✓\n\n**Final Normal Form Achieved: 3NF (BCNF-compatible)** for all tables.\n\n### 3.1.3 Premium Calculation — Worked Derivation\n\nGiven a Motor Insurance policy with:\n- Sum Assured $SA = ₹5{,}00{,}000$\n- Base rate $R = ₹25$ per ₹1,000 SA per annum\n- Risk factor $RF = 1.4$ (urban driver, age 28)\n\nCompute the annual premium $P$:\n\n$$\n\\begin{aligned}\nP &= \\frac{SA \\times R \\times RF}{1000} \\\\\nP &= \\frac{5{,}00{,}000 \\times 25 \\times 1.4}{1000} \\\\\nP &= \\frac{5{,}00{,}000 \\times 35}{1000} \\\\\nP &= \\frac{1{,}75{,}00{,}000}{1000} \\\\\nP &= ₹17{,}500 \\text{ per annum}\n\\end{aligned}\n$$\n\n**Logic Explanation:**\n- Line 1 → Substitute the IPDBMS premium formula.\n- Line 2 → Plug in the numerical values ($SA$, $R$, $RF$).\n- Line 3 → Combine $R \\times RF = 25 \\times 1.4 = 35$ (effective per-₹1000 rate).\n- Line 4 → Multiply $5{,}00{,}000 \\times 35 = 1.75 \\times 10^7$.\n- Line 5 → Divide by 1000 to convert from per-₹1000 to absolute premium.\n\n### 3.1.4 Loss Ratio (Claim Ratio) Derivation\n\nAn insurer collected **₹12 crore** in premiums and settled **₹8.4 crore** in claims in FY 2023-24.\n\n$$\n\\begin{aligned}\nLR &= \\frac{\\text{Total Claims Settled}}{\\text{Total Premium Collected}} \\times 100 \\\\\nLR &= \\frac{8{,}40{,}00{,}000}{12{,}00{,}00{,}000} \\times 100 \\\\\nLR &= 0.70 \\times 100 \\\\\nLR &= 70\\%\n\\end{aligned}\n$$\n\n**Interpretation:** A 70% combined loss ratio is **healthy** (industry ideal: 60–75%). Above 85% signals underwriting losses.\n\n### 3.1.5 Solvency Margin Verification\n\nGiven:\n- Available Solvency Margin (ASM) = ₹450 crore\n- Required Solvency Margin (RSM) = ₹250 crore\n\n$$\n\\begin{aligned}\nSM &= \\frac{ASM}{RSM} \\times 100 \\\\\nSM &= \\frac{450}{250} \\times 100 \\\\\nSM &= 1.8 \\times 100 \\\\\nSM &= 180\\%\n\\end{aligned}\n$$\n\n**Verification:** Since $SM = 180\\% \\geq 150\\%$ (IRDAI minimum), the insurer is **regulatorily compliant**. ✓\n\n## 3.2 Analytical Matrix — Query Optimization in IPDBMS\n\n| Operation | Frequency | Index Required | Cost (B-Tree) | Cost (Hash) |\n|---|---|---|---|---|\n| Fetch policy by `policy_no` | Very High | B-Tree on `policy_no` | $O(\\log n)$ | $O(1)$ |\n| List all claims for a `cust_id` | High | B-Tree composite `(cust_id, status)` | $O(\\log n + k)$ | $O(n)$ |\n| Insert premium payment | Very High | B-Tree on `payment_id` | $O(\\log n)$ | $O(1)$ |\n| Compute monthly loss ratio | Medium | None (full scan with SUM) | $O(n)$ | $O(n)$ |\n| Update claim status | High | B-Tree on `claim_id` | $O(\\log n)$ | $O(1)$ |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# Structural Diagrams & Schematics\n\n## 4.1 High-Level Entity-Relationship Diagram (Insurance Domain)\n\n```mermaid\nerDiagram\n    CUSTOMER ||--o{ POLICY : \"owns\"\n    POLICY ||--|{ PAYMENT : \"has\"\n    POLICY ||--o{ CLAIM : \"raises\"\n    PRODUCT ||--o{ POLICY : \"defines\"\n    AGENT ||--o{ POLICY : \"sells\"\n    POLICY ||--o{ BENEFICIARY : \"names\"\n    CLAIM ||--o| SURVEYOR : \"assigned_to\"\n    POLICY ||--o{ RIDER : \"includes\"\n\n    CUSTOMER {\n        int cust_id PK\n        string fname\n        string lname\n        date dob\n        string pan_no\n        string kyc_status\n    }\n    POLICY {\n        int policy_id PK\n        string policy_no UK\n        date issue_date\n        decimal sum_assured\n        string status\n        int cust_id FK\n        int product_id FK\n        int agent_id FK\n    }\n    PRODUCT {\n        int product_id PK\n        string product_name\n        string product_type\n        decimal base_rate\n        int term_years\n    }\n    AGENT {\n        int agent_id PK\n        string agent_name\n        string branch_id\n    }\n    PAYMENT {\n        int payment_id PK\n        int policy_id FK\n        date due_date\n        date paid_date\n        decimal amount\n    }\n    CLAIM {\n        int claim_id PK\n        int policy_id FK\n        date claim_date\n        decimal claim_amount\n        string status\n    }\n    BENEFICIARY {\n        int benef_id PK\n        int policy_id FK\n        string name\n        string relation\n    }\n    RIDER {\n        int rider_id PK\n        int policy_id FK\n        string rider_type\n        decimal additional_premium\n    }\n    SURVEYOR {\n        int surveyor_id PK\n        string name\n        string region\n    }\n```\n\n## 4.2 Policy Lifecycle State Transition Diagram\n\n```mermaid\nstateDiagram-v2\n    [*] --> PROPOSAL : Customer submits form\n    PROPOSAL --> UNDERWRITING : KYC verified\n    UNDERWRITING --> ACTIVE : Approved\n    UNDERWRITING --> REJECTED : Risk too high\n    REJECTED --> [*]\n    ACTIVE --> LAPSE : Premium overdue beyond grace\n    ACTIVE --> LAPSED : Non-payment\n    ACTIVE --> CLAIM_FILED : Insured event\n    LAPSED --> REVIVED : Revival within period\n    LAPSE --> ACTIVE : Payment received\n    CLAIM_FILED --> UNDER_REVIEW : Documents received\n    UNDER_REVIEW --> APPROVED : Surveyor confirms\n    UNDER_REVIEW --> REJECTED : Fraud detected\n    APPROVED --> DISBURSED : Amount paid\n    ACTIVE --> MATURED : Term complete\n    ACTIVE --> SURRENDERED : Early termination\n    DISBURSED --> [*]\n    MATURED --> [*]\n    SURRENDERED --> [*]\n```\n\n## 4.3 Block-Level Functional Architecture Flow\n\n```mermaid\nflowchart TB\n    subgraph Frontend[\"Frontend Layer - Customer and Agent Portal\"]\n        UI_CUST[\"Customer Web and Mobile UI\"]\n        UI_AGENT[\"Agent Dashboard\"]\n        UI_CLAIM[\"Claim Filing Portal\"]\n    end\n\n    subgraph Application[\"Application Layer - Business Logic\"]\n        AP_ONBOARD[\"Onboarding Service\"]\n        AP_UNDER[\"Underwriting Engine\"]\n        AP_PREMIUM[\"Premium Calculator\"]\n        AP_CLAIM[\"Claims Adjudicator\"]\n        AP_RENEW[\"Renewal Scheduler\"]\n        AP_REPORT[\"Regulatory Reporter\"]\n    end\n\n    subgraph DataLayer[\"Data Layer - IPDBMS\"]\n        DB_CUST[(\"CUSTOMER Master\")]\n        DB_POL[(\"POLICY Master\")]\n        DB_CLAIM[(\"CLAIM Master\")]\n        DB_PAY[(\"PAYMENT Ledger\")]\n        DB_PROD[(\"PRODUCT Catalog\")]\n        DB_LOG[(\"Audit and Compliance Log\")]\n    end\n\n    subgraph External[\"External Systems\"]\n        EX_KYC[\"KYC and PAN Verification API\"]\n        EX_BANK[\"Banking and Payment Gateway\"]\n        EX_IRDAI[\"IRDAI Regulatory Portal\"]\n    end\n\n    UI_CUST --> AP_ONBOARD\n    UI_AGENT --> AP_UNDER\n    UI_CLAIM --> AP_CLAIM\n\n    AP_ONBOARD --> DB_CUST\n    AP_ONBOARD --> EX_KYC\n    AP_UNDER --> DB_POL\n    AP_UNDER --> DB_PROD\n    AP_PREMIUM --> DB_PAY\n    AP_CLAIM --> DB_CLAIM\n    AP_CLAIM --> DB_POL\n    AP_RENEW --> DB_PAY\n    AP_RENEW --> DB_POL\n    AP_REPORT --> DB_LOG\n    AP_REPORT --> EX_IRDAI\n\n    AP_PREMIUM --> EX_BANK\n    AP_CLAIM --> EX_BANK\n\n    DB_CUST -.-> DB_LOG\n    DB_POL -.-> DB_LOG\n    DB_CLAIM -.-> DB_LOG\n    DB_PAY -.-> DB_LOG\n```\n\n> [!NOTE]\n> **Reading the Diagram:** The frontend layer never accesses the database directly. All requests pass through the application layer, which enforces business rules, ACID transactions, and security policies — a classic **3-tier architecture** used by every major insurer.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Question 1: Define \"Sum Assured\" and \"Risk Factor\" in the context of an insurance product database. How are they used in premium computation? **[3 Marks]**\n\n**Model Answer:**\n- **Sum Assured (SA):** The maximum monetary amount payable by the insurer to the policyholder or beneficiary upon occurrence of the insured event. It is stored as a column in the `POLICY` table (data type: DECIMAL).\n- **Risk Factor (RF):** A dimensionless multiplier ∈ [0.5, 5.0] derived from actuarial tables, representing the probability and severity of risk for the insured entity (e.g., age, occupation, health, vehicle type).\n- **Usage in Premium:** Both are combined via the formula $P = \\dfrac{SA \\times R \\times RF}{1000}$, where $R$ is the base rate per ₹1,000 SA. Higher $SA$ and higher $RF$ both increase the premium proportionally.\n\n> [!IMPORTANT]\n> **Key takeaway for valuation:** Define both terms and show the formula. A clear formula carries 1 mark.\n\n---\n\n### Question 2: What is a \"Loss Ratio\" in insurance database reporting? Write its formula and state the industry benchmark. **[3 Marks]**\n\n**Model Answer:**\n- **Loss Ratio (LR):** The ratio of total claims settled by an insurer to the total premium collected over a period, expressed as a percentage. It is a key profitability metric stored in the `REPORTS` table.\n- **Formula:** $\\displaystyle LR = \\dfrac{\\text{Total Claims Settled}}{\\text{Total Premium Collected}} \\times 100$\n- **Benchmark:** A healthy insurer maintains a **combined loss ratio between 60% and 75%**. Above 85% indicates underwriting losses; below 50% may suggest overpricing.\n\n---\n\n## 5.2 Part B — ESE Module Internal Choice (14 Marks Each)\n\n### **Question A** [KTU University Exam — Model Paper Pattern]\n\n**A (a)** Design an ER diagram for an Insurance Products Database covering entities: CUSTOMER, POLICY, PRODUCT, AGENT, CLAIM, and PAYMENT. Clearly state all attributes, primary keys, and relationships (with cardinality). **[7 Marks]**\n\n**Model Solution:**\n\nThe ER diagram (already drawn in Section 4.1 above) contains the following:\n\n| Entity | Primary Key | Notable Attributes | Relationship |\n|---|---|---|---|\n| CUSTOMER | `cust_id` | `pan_no, kyc_status` | 1:N with POLICY |\n| POLICY | `policy_id` | `sum_assured, status` | N:1 with CUSTOMER, PRODUCT, AGENT |\n| PRODUCT | `product_id` | `base_rate, term_years` | 1:N with POLICY |\n| AGENT | `agent_id` | `branch_id` | 1:N with POLICY |\n| CLAIM | `claim_id` | `claim_amount, status` | N:1 with POLICY |\n| PAYMENT | `payment_id` | `due_date, paid_date` | N:1 with POLICY |\n\n**Cardinalities:**\n- `CUSTOMER (1) : POLICY (N)` — One customer can hold multiple policies.\n- `PRODUCT (1) : POLICY (N)` — One product type can issue many policies.\n- `AGENT (1) : POLICY (N)` — One agent sells many policies.\n- `POLICY (1) : CLAIM (N)` — A policy may generate multiple claims.\n- `POLICY (1) : PAYMENT (N)` — A policy has many premium installments.\n\n**Award marks:** ER Diagram = 4 marks; Cardinality & PK notation = 2 marks; Attribute completeness = 1 mark.\n\n---\n\n**A (b)** Consider a Motor Insurance policy with Sum Assured = ₹3,00,000, Base Rate = ₹30 per ₹1,000 SA, and Risk Factor = 1.25. Calculate the annual premium. Also, compute the Loss Ratio if total claims settled = ₹4,50,00,000 and total premium collected = ₹6,00,00,000. **[7 Marks]**\n\n**Model Solution:**\n\n**(i) Annual Premium Calculation:**\n\n$$\n\\begin{aligned}\nP &= \\frac{SA \\times R \\times RF}{1000} \\\\\nP &= \\frac{3{,}00{,}000 \\times 30 \\times 1.25}{1000} \\\\\nP &= \\frac{3{,}00{,}000 \\times 37.5}{1000} \\\\\nP &= \\frac{1{,}12{,}50{,}000}{1000} \\\\\nP &= ₹11{,}250 \\text{ per annum}\n\\end{aligned}\n$$\n\n**(ii) Loss Ratio Calculation:**\n\n$$\n\\begin{aligned}\nLR &= \\frac{\\text{Total Claims Settled}}{\\text{Total Premium Collected}} \\times 100 \\\\\nLR &= \\frac{4{,}50{,}00{,}000}{6{,}00{,}00{,}000} \\times 100 \\\\\nLR &= 0.75 \\times 100 \\\\\nLR &= 75\\%\n\\end{aligned}\n$$\n\n**Conclusion:** The annual premium is **₹11,250**, and the Loss Ratio is **75%**, which lies within the healthy industry range of **60%–75%**.\n\n**Award marks:** Formula = 1 mark; Premium computation = 3 marks; LR formula = 1 mark; LR computation = 2 marks.\n\n---\n\n### **Question B** [KTU University Exam — Model Paper Pattern]\n\n**B (a)** Explain the major entities and their attributes in an Insurance Products Database Management System. Discuss the role of normalization in ensuring data integrity for insurance data. **[7 Marks]**\n\n**Model Solution:**\n\n**Major Entities:**\n\n1. **CUSTOMER** — `cust_id, fname, lname, dob, gender, address, phone, email, pan_no, aadhaar_no, kyc_status`. Stores personal and KYC information.\n2. **POLICY** — `policy_id, policy_no, issue_date, maturity_date, sum_assured, premium_amt, status, cust_id FK, product_id FK, agent_id FK`. Central transactional entity.\n3. **PRODUCT** — `product_id, product_name, product_type (LIFE/HEALTH/MOTOR/PROPERTY), base_rate, term_years`. Catalog of offerings.\n4. **AGENT** — `agent_id, name, branch_id, joining_date, commission_rate`. Distribution channel.\n5. **CLAIM** — `claim_id, policy_id FK, claim_date, claim_amount, status, surveyor_id, settlement_date`.\n6. **PAYMENT** — `payment_id, policy_id FK, due_date, paid_date, amount, mode`.\n7. **BENEFICIARY** — `benef_id, policy_id FK, name, relation, share_pct`.\n\n**Role of Normalization:**\n\n- **1NF:** Eliminates multi-valued attributes (e.g., multiple beneficiaries stored in one row).\n- **2NF:** Removes partial dependencies on composite keys.\n- **3NF:** Eliminates transitive dependencies (e.g., `policy → product → product_name` → split into separate tables).\n- **BCNF (optional):** Stronger form ensuring every determinant is a candidate key — used in highly sensitive claim tables.\n\n**Why it matters for insurance:**\n- A non-normalized schema could cause **update anomalies** (e.g., changing `base_rate` of a product must be done in thousands of policy rows).\n- Reduces **storage redundancy** and improves **transaction throughput**.\n- Ensures **referential integrity** (FKs) — a deleted POLICY must cascade or restrict CLAIM rows.\n\n**Award marks:** Entity listing with attributes = 4 marks; Normalization discussion = 3 marks.\n\n---\n\n**B (b)** Write SQL DDL statements to create the POLICY, CUSTOMER, and CLAIM tables with appropriate primary and foreign key constraints. Also write a query to find the top 5 customers by total premium paid in the year 2024. **[7 Marks]**\n\n**Model Solution:**\n\n**SQL DDL Statements:**\n\n```sql\nCREATE TABLE CUSTOMER (\n    cust_id      INT PRIMARY KEY,\n    fname        VARCHAR(50) NOT NULL,\n    lname        VARCHAR(50),\n    dob          DATE,\n    pan_no       CHAR(10) UNIQUE,\n    kyc_status   VARCHAR(20) DEFAULT 'PENDING'\n);\n\nCREATE TABLE POLICY (\n    policy_id    INT PRIMARY KEY,\n    policy_no    VARCHAR(20) UNIQUE NOT NULL,\n    issue_date   DATE NOT NULL,\n    maturity_date DATE,\n    sum_assured  DECIMAL(12,2) CHECK (sum_assured > 0),\n    status       VARCHAR(20) DEFAULT 'ACTIVE',\n    cust_id      INT,\n    product_id   INT,\n    agent_id     INT,\n    FOREIGN KEY (cust_id)    REFERENCES CUSTOMER(cust_id) ON DELETE RESTRICT,\n    FOREIGN KEY (product_id) REFERENCES PRODUCT(product_id),\n    FOREIGN KEY (agent_id)   REFERENCES AGENT(agent_id)\n);\n\nCREATE TABLE CLAIM (\n    claim_id        INT PRIMARY KEY,\n    policy_id       INT NOT NULL,\n    claim_date      DATE NOT NULL,\n    claim_amount    DECIMAL(12,2) CHECK (claim_amount > 0),\n    status          VARCHAR(20) DEFAULT 'FILED',\n    settlement_date DATE,\n    FOREIGN KEY (policy_id) REFERENCES POLICY(policy_id) ON DELETE CASCADE\n);\n```\n\n**Query — Top 5 Customers by Total Premium Paid in 2024:**\n\n```sql\nSELECT  c.cust_id,\n        c.fname,\n        c.lname,\n        SUM(p.amount) AS total_premium_2024\nFROM    CUSTOMER c\nJOIN    POLICY   po ON c.cust_id = po.cust_id\nJOIN    PAYMENT  p  ON po.policy_id = p.policy_id\nWHERE   YEAR(p.paid_date) = 2024\n        AND p.paid_date IS NOT NULL\nGROUP BY c.cust_id, c.fname, c.lname\nORDER BY total_premium_2024 DESC\nLIMIT 5;\n```\n\n**Award marks:** DDL with PK = 2 marks; DDL with FK + CHECK = 2 marks; SQL query correctness = 3 marks.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Pitfall Callout:**\n> 1. **Formula writing:** Always write the formula first, *then* substitute values. Students who directly write the final number lose 1 mark.\n> 2. **FK direction:** Remember that FKs are placed on the \"many\" side of a 1:N relationship. Putting `cust_id` inside CUSTOMER is a classic error.\n> 3. **Aggregation:** When using `GROUP BY`, every non-aggregated column in SELECT must appear in `GROUP BY` — omitting this loses 1 mark.\n> 4. **Unit consistency:** Always specify currency (₹) and time unit (per annum) to avoid ambiguity.\n> 5. **3NF proof:** Just stating \"schema is in 3NF\" without showing FDs and checking transitive dependencies earns **zero marks**. Show the work.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n- ⭐ **Insurance database = OLTP-heavy system** demanding strong ACID compliance, BCNF/3NF normalization, and B-Tree indexing on policy/claim keys.\n- ⭐ **Eight core sub-systems:** Customer, Product, Policy, Premium, Claim, Agent, Underwriting, Audit.\n- ⭐ **Premium Formula:** $\\displaystyle P = \\frac{SA \\times R \\times RF}{1000}$ — must be memorized.\n- ⭐ **Loss Ratio Formula:** $\\displaystyle LR = \\frac{\\text{Claims Settled}}{\\text{Premium Collected}} \\times 100$ — healthy range 60–75%.\n- ⭐ **Solvency Margin ≥ 150%** is the **IRDAI minimum** for regulatory compliance.\n- ⭐ **Persistency Ratio** at month 13 should be **> 85%** for life insurers.\n- ⭐ **Standard agent commission:** **15%** first year, **7.5%** renewal.\n- ⭐ **Foreign keys** must cascade or restrict to preserve referential integrity.\n- ⭐ **3-tier architecture** (Frontend → Application → Database) is the industry standard — DB never directly exposed.\n- ⭐ **Index wisely:** B-Tree for range queries (`policy_no`, `paid_date`); Hash for exact-match lookups (`cust_id`).\n- ⭐ **State transitions** of a policy: PROPOSAL → UNDERWRITING → ACTIVE → (CLAIM or MATURITY or SURRENDER).\n- ⭐ **Audit logs** are mandatory — IRDAI requires **minimum 10-year retention** of policy records.\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "hospitalmanagementsystem": {
      "title": "Hospital Management System",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 9\n> - **Topic:** Hospital Management System\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition (KTU 2024 Terminology)\n\n> [!NOTE]\n> **Hospital Management System (HMS)** is a **comprehensive, integrated, relational information system** designed to manage the **administrative, financial, and clinical operations** of a healthcare organization. From a **DBMS perspective**, it is a database-backed software application that uses a **relational schema** to store, retrieve, manipulate, and secure data about patients, doctors, appointments, treatments, billing, and inventory.\n\nIn the KTU 2024 NEP-aligned syllabus, the Hospital Management System is studied as a **case-driven application of database design**, illustrating the full life-cycle of database engineering:\n\n1. **Requirement Analysis** (identifying user views)\n2. **Conceptual Design** (ER / EER modeling)\n3. **Logical Design** (Mapping ER to Relational Schema)\n4. **Schema Refinement** (Normalization up to BCNF / 4NF)\n5. **Physical Implementation** (SQL DDL, DML, DCL, TCL)\n6. **Query Processing & Transaction Management**\n\n## 1.2 Conceptual Analogy — The \"Smart Hospital\" Analogy\n\n> [!IMPORTANT]\n> **Intuition (Plain English):** Imagine a hospital as a **giant filing cabinet** where patient files, doctor schedules, bills, and test reports are all scattered across different rooms. A *Hospital Management System* is like replacing this cabinet with a **single, intelligent, computerized index** — one where the receptionist, the doctor, the lab technician, and the billing desk all see **the same up-to-date information** instantly, without paper, without duplication, and without confusion.\n\nThink of HMS as **three interlocking wheels**:\n- **Wheel 1 — The Reception Wheel**: Registers patients, books appointments, allocates rooms.\n- **Wheel 2 — The Treatment Wheel**: Doctors record diagnoses, prescribe medicines, order lab tests.\n- **Wheel 3 — The Billing Wheel**: Generates invoices, tracks payments, manages insurance claims.\n\nThe **DBMS is the axle** that holds all three wheels together — every transaction on one wheel is immediately visible on the others.\n\n## 1.3 Key Components / Modules of HMS\n\n| Module | Functional Responsibility | Primary Data Entities |\n|---|---|---|\n| **Patient Management** | Registration, demographics, history | `PATIENT`, `MEDICAL_HISTORY` |\n| **Doctor Management** | Profiles, specialization, schedules | `DOCTOR`, `DEPARTMENT` |\n| **Appointment Scheduling** | Booking, slot allocation, cancellation | `APPOINTMENT` |\n| **OPD/IPD Management** | Outpatient & Inpatient tracking | `VISIT`, `ADMISSION`, `ROOM` |\n| **Pharmacy & Inventory** | Drug stock, expiry, vendor | `MEDICINE`, `STOCK` |\n| **Laboratory** | Test requests, results | `LAB_TEST`, `TEST_RESULT` |\n| **Billing & Finance** | Invoices, payments, insurance | `BILL`, `PAYMENT` |\n| **Staff Management** | Nurses, technicians, admin | `STAFF`, `NURSE` |\n\n> [!NOTE]\n> The standard metrics used in HMS databases are: **patient admission rate**, **average length of stay (ALOS)**, **bed occupancy ratio**, and **revenue per patient** — all derived via SQL aggregate queries.\n\n## 1.4 Why HMS is a Critical DBMS Case Study\n\n- **High data volume**: A mid-size hospital generates **1–10 GB/day** of structured and semi-structured data.\n- **Strict ACID requirements**: Medical records demand **Atomicity, Consistency, Isolation, Durability**.\n- **Complex relationships**: One patient may have many doctors, treatments, bills, and lab tests — a classic **many-to-many** scenario.\n- **Security & Compliance**: HIPAA / DISHA-style regulations require strict access control (DCL — `GRANT` / `REVOKE`).\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Phases of Database Design for HMS\n\nThe design of the Hospital Management System follows the canonical **6-phase methodology** prescribed by Elmasri & Navathe and adopted by KTU:\n\n### Phase 1 — Requirements Gathering\nIdentify user groups: **Receptionist, Doctor, Nurse, Lab Technician, Pharmacist, Billing Officer, Administrator**.\n\nFor each user, derive **functional requirements**:\n- Receptionist → \"Register a new patient and assign a unique Patient ID.\"\n- Doctor → \"View patient's medical history and add a new prescription.\"\n- Billing → \"Generate a consolidated bill for a patient's visit.\"\n\n### Phase 2 — Conceptual Design (ER Model)\nIdentify **Entities**, **Attributes**, and **Relationships**.\n\n> [!IMPORTANT]\n> In KTU examinations, students are expected to draw the **ER diagram** and label **cardinality ratios** (1:1, 1:N, M:N) and **participation constraints** (total / partial).\n\n### Phase 3 — Logical Design (Mapping to Relations)\nApply the **ER-to-Relational Mapping Algorithm** (7-step algorithm from Elmasri):\n1. Map each **strong entity** → 1 relation.\n2. Map each **weak entity** → 1 relation + foreign key to owner.\n3. Map each **1:1 relationship** → merge with either side OR create separate relation.\n4. Map each **1:N relationship** → add foreign key on the \"N\" side.\n5. Map each **M:N relationship** → create a new relation with two foreign keys + relationship attributes.\n6. Map each **multivalued attribute** → separate relation.\n7. Map each **n-ary relationship** → separate relation with n foreign keys.\n\n### Phase 4 — Schema Refinement (Normalization)\nApply **1NF → 2NF → 3NF → BCNF** to eliminate anomalies.\n\n### Phase 5 — Physical Design\nChoose **indexing strategy** (B+ tree on primary key, hash on frequently searched fields), **file organization**, and **clustering**.\n\n### Phase 6 — Security & Implementation\nDefine **user roles**, apply **GRANT/REVOKE**, define **views** for restricted access.\n\n## 2.2 Entity Identification for HMS\n\n| Entity | Type | Identifier (PK) | Key Attributes |\n|---|---|---|---|\n| `PATIENT` | Strong | `patient_id` | name, DOB, gender, blood_group, phone, address |\n| `DOCTOR` | Strong | `doctor_id` | name, specialization, salary, dept_id (FK) |\n| `DEPARTMENT` | Strong | `dept_id` | dept_name, head_doctor_id (FK) |\n| `APPOINTMENT` | Strong | `appointment_id` | patient_id (FK), doctor_id (FK), date, time, status |\n| `ROOM` | Strong | `room_id` | room_type, charges, status |\n| `ADMISSION` | Strong | `admission_id` | patient_id (FK), room_id (FK), admit_date, discharge_date |\n| `MEDICINE` | Strong | `medicine_id` | name, manufacturer, price, stock |\n| `PRESCRIPTION` | Strong (associative) | `prescription_id` | patient_id, doctor_id, medicine_id, dosage, duration |\n| `LAB_TEST` | Strong | `test_id` | test_name, cost, result, patient_id (FK) |\n| `BILL` | Strong | `bill_id` | patient_id (FK), total_amount, date, status |\n| `PAYMENT` | Weak | `payment_id` | bill_id (FK), amount, mode, date |\n| `NURSE` | Strong | `nurse_id` | name, shift, ward_id (FK) |\n| `MEDICAL_RECORD` | Weak | `record_id` | patient_id (FK), diagnosis, treatment_date |\n\n## 2.3 KTU High-Yield Formula / Rule Sheet\n\n| Rule / Concept | Statement | Application in HMS |\n|---|---|---|\n| **1NF Rule** | Every cell must hold an **atomic (indivisible)** value. | Split \"phone_numbers\" cell into separate rows. |\n| **2NF Rule** | Must be in 1NF + **no partial dependency** (every non-key attr depends on full PK). | In `PRESCRIPTION(pres_id, med_id, patient_id, dosage, med_name)`, `med_name` depends only on `med_id` → split. |\n| **3NF Rule** | Must be in 2NF + **no transitive dependency**. | If `DOCTOR(doctor_id, dept_id, dept_name)` — `dept_name` transitively depends on `doctor_id` via `dept_id` → split. |\n| **BCNF Rule** | For every FD $X \\rightarrow Y$, $X$ must be a **superkey**. | Resolve overlapping candidate keys. |\n| **Cardinality 1:1** | Merge with either entity table or create separate relation. | `ADMISSION` ↔ `ROOM` (one patient per room at a time). |\n| **Cardinality 1:N** | Place FK on the **\"many\" side**. | `DEPARTMENT` (1) → `DOCTOR` (N). |\n| **Cardinality M:N** | Create a **new associative relation** with 2 FKs. | `DOCTOR` ↔ `PATIENT` (via `APPOINTMENT`). |\n| **Referential Integrity** | Every FK value must match a PK in the parent table OR be NULL. | `appointment.doctor_id` must exist in `DOCTOR`. |\n| **ACID Property** | Atomicity, Consistency, Isolation, Durability. | Bill generation: payment + inventory update must be atomic. |\n\n> [!NOTE]\n> **Why these matter in engineering:** Hospital databases are **mission-critical**. A violation of referential integrity could mean a patient is prescribed a medicine from a non-existent manufacturer, or billed for a treatment never rendered. Normalization guarantees **data integrity**; transactions guarantee **data consistency**.\n\n## 2.4 Real-World Engineering Utility\n\n- **Healthcare Informatics**: HMS is the prototype for **Electronic Health Records (EHR)** systems like Epic, Cerner, and Allscripts.\n- **Data Warehousing**: Historical HMS data feeds **OLAP cubes** for disease-pattern analysis and resource planning.\n- **IoT & AI Integration**: Modern HMS ingests real-time vitals from medical IoT devices into time-series tables.\n- **Audit & Compliance**: KTU emphasizes that DCL commands (`GRANT`, `REVOKE`) enforce **role-based access** to comply with healthcare data protection laws.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 ER-to-Relational Mapping for HMS (Exhaustive 7-Step Derivation)\n\nWe derive the relational schema from the conceptual ER model of HMS.\n\n### Step 1: Mapping Strong Entities\n\nEach strong entity becomes an independent relation with its primary key.\n\n$$\n\\text{PATIENT}(\\underline{\\text{patient\\_id}},\\ \\text{name},\\ \\text{DOB},\\ \\text{gender},\\ \\text{blood\\_group},\\ \\text{phone},\\ \\text{address})\n$$\n\n$$\n\\text{DOCTOR}(\\underline{\\text{doctor\\_id}},\\ \\text{name},\\ \\text{specialization},\\ \\text{salary},\\ \\text{dept\\_id}^*)\n$$\n\n$$\n\\text{DEPARTMENT}(\\underline{\\text{dept\\_id}},\\ \\text{dept\\_name},\\ \\text{head\\_doctor\\_id}^*)\n$$\n\n**Logic:** Underlined attributes are **primary keys**. The `*` superscript marks **foreign keys** to be added in later steps.\n\n### Step 2: Mapping Weak Entities\n\nWeak entities depend on a **owner/identifying entity** and have a **partial key**.\n\n$$\n\\text{PAYMENT}(\\underline{\\text{payment\\_id}},\\ \\text{amount},\\ \\text{mode},\\ \\text{date},\\ \\text{bill\\_id}^*)\n$$\n\n$$\n\\text{MEDICAL\\_RECORD}(\\underline{\\text{record\\_id}},\\ \\text{patient\\_id}^*,\\ \\text{diagnosis},\\ \\text{treatment\\_date})\n$$\n\n**Logic:** The primary key of a weak entity is the **owner's PK + the partial key**, e.g., `MEDICAL_RECORD.PK = (patient_id, record_id)`.\n\n### Step 3: Mapping the 1:1 Relationship — `ADMISSION` ↔ `ROOM`\n\nWe choose to **merge** the foreign key into the `ADMISSION` relation (optional side).\n\n$$\n\\text{ADMISSION}(\\underline{\\text{admission\\_id}},\\ \\text{patient\\_id}^*,\\ \\text{room\\_id}^*,\\ \\text{admit\\_date},\\ \\text{discharge\\_date})\n$$\n\n**Logic:** Since each admission occupies **one room** and each room (at a moment) holds **one patient**, this is 1:1. The FK goes to the side with **total participation** (here, ADMISSION is total).\n\n### Step 4: Mapping the 1:N Relationship — `DEPARTMENT` (1) → `DOCTOR` (N)\n\nThe foreign key is placed on the **\"many\" side** — i.e., the `DOCTOR` table.\n\n$$\n\\text{DOCTOR}(\\underline{\\text{doctor\\_id}},\\ \\text{name},\\ \\text{specialization},\\ \\text{salary},\\ \\text{dept\\_id}^*)\n$$\n\n**Logic:** Each doctor belongs to exactly one department, but a department has many doctors. The FK `dept_id` on the `DOCTOR` side preserves this rule.\n\n### Step 5: Mapping the M:N Relationship — `DOCTOR` ↔ `PATIENT` via `APPOINTMENT`\n\nA new associative relation is created.\n\n$$\n\\text{APPOINTMENT}(\\underline{\\text{appointment\\_id}},\\ \\text{patient\\_id}^*,\\ \\text{doctor\\_id}^*,\\ \\text{date},\\ \\text{time},\\ \\text{status})\n$$\n\n**Logic:** One patient can see many doctors; one doctor can see many patients. The M:N cardinality mandates a **junction table** with **two foreign keys** and any **relationship attributes** (date, time, status).\n\n### Step 6: Mapping the M:N Relationship with Attribute — `PRESCRIPTION`\n\nA doctor prescribes multiple medicines; a medicine can be prescribed by multiple doctors.\n\n$$\n\\text{PRESCRIPTION}(\\underline{\\text{prescription\\_id}},\\ \\text{patient\\_id}^*,\\ \\text{doctor\\_id}^*,\\ \\text{medicine\\_id}^*,\\ \\text{dosage},\\ \\text{duration})\n$$\n\n**Logic:** `dosage` and `duration` are attributes of the **ternary relationship** among Doctor, Patient, and Medicine, so they belong to the new relation.\n\n### Step 7: Mapping Multivalued Attributes — `PATIENT_PHONE`\n\nIf a patient has multiple phone numbers, create a separate relation.\n\n$$\n\\text{PATIENT\\_PHONE}(\\underline{\\text{patient\\_id}^*,\\ \\text{phone\\_number}})\n$$\n\n**Logic:** The composite PK is `(patient_id, phone_number)` because the same patient cannot have the **same** phone number listed twice.\n\n## 3.2 Normalization Walk-Through (Analytical Matrix)\n\nConsider an **un-normalized** HMS relation for prescriptions:\n\n$$\n\\text{PRES\\_UNF} = (\\text{pres\\_id},\\ \\text{patient\\_id},\\ \\text{patient\\_name},\\ \\text{doctor\\_id},\\ \\text{doctor\\_name},\\ \\text{med\\_id},\\ \\text{med\\_name},\\ \\text{dosage})\n$$\n\n### Step A — Convert to 1NF\n\nAll cells must be atomic. Suppose a prescription can have multiple medicines — we **flatten** them:\n\n| pres_id | patient_id | med_id | dosage |\n|---|---|---|---|\n| P1 | PT1 | M1 | 1-0-1 |\n| P1 | PT1 | M2 | 0-1-0 |\n\n**Logic:** 1NF eliminates repeating groups by ensuring each row represents one fact.\n\n### Step B — Convert to 2NF\n\nIn 1NF, the PK is `(pres_id, med_id)`. Check for **partial dependencies**:\n- `patient_name` depends only on `patient_id` (part of PK only if patient_id is in PK — but here it is not; it depends on patient_id alone).\n- `med_name` depends only on `med_id`.\n\nThese are **partial dependencies** → split into:\n- `PRESCRIPTION(pres_id, patient_id, med_id, dosage)`\n- `PATIENT(patient_id, patient_name)`\n- `MEDICINE(med_id, med_name)`\n\n### Step C — Convert to 3NF\n\nIn `DOCTOR(doctor_id, dept_id, dept_name)`:\n- `dept_name` depends on `dept_id`, not directly on `doctor_id` → **transitive dependency**.\n\nSplit into:\n- `DOCTOR(doctor_id, name, dept_id)`\n- `DEPARTMENT(dept_id, dept_name)`\n\n### Step D — Convert to BCNF\n\nSuppose a relation `ROOM_ALLOCATION(room_id, patient_id, date, charge)` with FD `room_id, date → patient_id` and `patient_id, date → room_id`. Both `room_id, date` and `patient_id, date` are **candidate keys**, but if `charge` depends only on `room_id`, then BCNF is violated because `room_id` is not a superkey of the whole relation.\n\nSplit to satisfy BCNF:\n- `ROOM_CHARGE(room_id, charge)`\n- `ROOM_ALLOCATION(room_id, patient_id, date)`\n\n## 3.3 Sample SQL DDL for HMS (Step-by-Step)\n\nBelow is the **complete SQL Data Definition Language** script for the HMS database, mapped directly from the ER design.\n\n```sql\n-- Step 1: Create DEPT table (no FKs)\nCREATE TABLE DEPARTMENT (\n    dept_id      INT PRIMARY KEY,\n    dept_name    VARCHAR(50) NOT NULL UNIQUE,\n    head_doctor_id INT\n);\n\n-- Step 2: Create DOCTOR table (FK to DEPARTMENT)\nCREATE TABLE DOCTOR (\n    doctor_id      INT PRIMARY KEY,\n    name           VARCHAR(100) NOT NULL,\n    specialization VARCHAR(50),\n    salary         DECIMAL(10,2),\n    dept_id        INT,\n    FOREIGN KEY (dept_id) REFERENCES DEPARTMENT(dept_id)\n        ON DELETE SET NULL\n        ON UPDATE CASCADE\n);\n\n-- Step 3: Create PATIENT table\nCREATE TABLE PATIENT (\n    patient_id  INT PRIMARY KEY,\n    name        VARCHAR(100) NOT NULL,\n    DOB         DATE,\n    gender      CHAR(1) CHECK (gender IN ('M','F','O')),\n    blood_group VARCHAR(5),\n    phone       VARCHAR(15),\n    address     VARCHAR(200)\n);\n\n-- Step 4: Create APPOINTMENT (M:N associative)\nCREATE TABLE APPOINTMENT (\n    appointment_id INT PRIMARY KEY,\n    patient_id     INT NOT NULL,\n    doctor_id      INT NOT NULL,\n    appt_date      DATE NOT NULL,\n    appt_time      TIME,\n    status         VARCHAR(20) DEFAULT 'Scheduled',\n    FOREIGN KEY (patient_id) REFERENCES PATIENT(patient_id),\n    FOREIGN KEY (doctor_id)  REFERENCES DOCTOR(doctor_id)\n);\n\n-- Step 5: Create ROOM and ADMISSION\nCREATE TABLE ROOM (\n    room_id   INT PRIMARY KEY,\n    room_type VARCHAR(20),\n    charges   DECIMAL(10,2),\n    status    VARCHAR(20) DEFAULT 'Available'\n);\n\nCREATE TABLE ADMISSION (\n    admission_id   INT PRIMARY KEY,\n    patient_id     INT NOT NULL,\n    room_id        INT,\n    admit_date     DATE NOT NULL,\n    discharge_date DATE,\n    FOREIGN KEY (patient_id) REFERENCES PATIENT(patient_id),\n    FOREIGN KEY (room_id)    REFERENCES ROOM(room_id)\n);\n\n-- Step 6: Create MEDICINE and PRESCRIPTION\nCREATE TABLE MEDICINE (\n    med_id       INT PRIMARY KEY,\n    name         VARCHAR(100) NOT NULL,\n    manufacturer VARCHAR(100),\n    price        DECIMAL(10,2),\n    stock        INT DEFAULT 0\n);\n\nCREATE TABLE PRESCRIPTION (\n    prescription_id INT PRIMARY KEY,\n    patient_id      INT NOT NULL,\n    doctor_id       INT NOT NULL,\n    med_id          INT NOT NULL,\n    dosage          VARCHAR(50),\n    duration        VARCHAR(50),\n    FOREIGN KEY (patient_id) REFERENCES PATIENT(patient_id),\n    FOREIGN KEY (doctor_id)  REFERENCES DOCTOR(doctor_id),\n    FOREIGN KEY (med_id)     REFERENCES MEDICINE(med_id)\n);\n\n-- Step 7: Create BILL and PAYMENT (weak)\nCREATE TABLE BILL (\n    bill_id      INT PRIMARY KEY,\n    patient_id   INT NOT NULL,\n    total_amount DECIMAL(12,2),\n    bill_date    DATE,\n    status       VARCHAR(20) DEFAULT 'Unpaid',\n    FOREIGN KEY (patient_id) REFERENCES PATIENT(patient_id)\n);\n\nCREATE TABLE PAYMENT (\n    payment_id INT,\n    bill_id    INT NOT NULL,\n    amount     DECIMAL(12,2),\n    mode       VARCHAR(30),\n    pay_date   DATE,\n    PRIMARY KEY (payment_id, bill_id),\n    FOREIGN KEY (bill_id) REFERENCES BILL(bill_id)\n);\n```\n\n## 3.4 Common Analytical Queries (For KTU Exam Practice)\n\n**Query 1 — List all patients admitted in the last 30 days with their assigned doctors.**\n\n$$\n\\pi_{\\text{patient.name, doctor.name, admission.admit\\_date}}\\left(\\text{PATIENT} \\bowtie_{\\text{PATIENT.patient\\_id = ADMISSION.patient\\_id}} \\text{ADMISSION} \\bowtie \\text{APPOINTMENT} \\bowtie \\text{DOCTOR}\\right)\n$$\n\n**Query 2 — Find the total revenue generated by each department.**\n\n$$\n\\text{SELECT}\\ d.\\text{dept\\_name},\\ \\text{SUM}(b.\\text{total\\_amount})\\ \\text{AS revenue}\\ \\text{FROM}\\ \\text{DEPARTMENT}\\ d\\ \\text{JOIN}\\ \\text{DOCTOR}\\ \\text{do}\\ \\text{ON}\\ d.\\text{dept\\_id = do.dept\\_id}\\ \\text{JOIN}\\ \\text{APPOINTMENT}\\ a\\ \\text{ON}\\ \\text{do.doctor\\_id = a.doctor\\_id}\\ \\text{JOIN}\\ \\text{BILL}\\ b\\ \\text{ON}\\ a.\\text{patient\\_id = b.patient\\_id}\\ \\text{GROUP BY}\\ d.\\text{dept\\_name};\n$$\n\n**Logic:** This uses a **multi-way JOIN** followed by `GROUP BY` and `SUM()` — a classic OLAP-style aggregation over the HMS star schema.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Complete ER Diagram of Hospital Management System (Mermaid)\n\n> [!NOTE]\n> The diagram below uses purely alphanumeric node IDs and clean text labels (no markdown inside node definitions) to ensure Mermaid compiles successfully.\n\n```mermaid\nerDiagram\n    DEPARTMENT ||--o{ DOCTOR : \"has\"\n    DOCTOR ||--o{ APPOINTMENT : \"schedules\"\n    PATIENT ||--o{ APPOINTMENT : \"books\"\n    PATIENT ||--o{ ADMISSION : \"occupies\"\n    ROOM ||--o{ ADMISSION : \"assigned_to\"\n    PATIENT ||--o{ PRESCRIPTION : \"receives\"\n    DOCTOR ||--o{ PRESCRIPTION : \"writes\"\n    MEDICINE ||--o{ PRESCRIPTION : \"listed_in\"\n    PATIENT ||--o{ BILL : \"issued\"\n    BILL ||--o{ PAYMENT : \"settled_by\"\n    PATIENT ||--o{ MEDICAL_RECORD : \"has\"\n    NURSE }o--o{ ROOM : \"attends\"\n    DEPARTMENT ||--o{ NURSE : \"employs\"\n\n    DEPARTMENT {\n        int dept_id PK\n        string dept_name\n        int head_doctor_id FK\n    }\n    DOCTOR {\n        int doctor_id PK\n        string name\n        string specialization\n        float salary\n        int dept_id FK\n    }\n    PATIENT {\n        int patient_id PK\n        string name\n        date DOB\n        string gender\n        string blood_group\n        string phone\n        string address\n    }\n    APPOINTMENT {\n        int appointment_id PK\n        int patient_id FK\n        int doctor_id FK\n        date appt_date\n        time appt_time\n        string status\n    }\n    ADMISSION {\n        int admission_id PK\n        int patient_id FK\n        int room_id FK\n        date admit_date\n        date discharge_date\n    }\n    ROOM {\n        int room_id PK\n        string room_type\n        float charges\n        string status\n    }\n    MEDICINE {\n        int med_id PK\n        string name\n        string manufacturer\n        float price\n        int stock\n    }\n    PRESCRIPTION {\n        int prescription_id PK\n        int patient_id FK\n        int doctor_id FK\n        int med_id FK\n        string dosage\n        string duration\n    }\n    BILL {\n        int bill_id PK\n        int patient_id FK\n        float total_amount\n        date bill_date\n        string status\n    }\n    PAYMENT {\n        int payment_id PK\n        int bill_id FK\n        float amount\n        string mode\n        date pay_date\n    }\n    MEDICAL_RECORD {\n        int record_id PK\n        int patient_id FK\n        string diagnosis\n        date treatment_date\n    }\n    NURSE {\n        int nurse_id PK\n        string name\n        string shift\n        int dept_id FK\n    }\n```\n\n**Reading the diagram:**\n- `||--o{` denotes **one-to-many** (mandatory one, optional many).\n- `}o--o{` denotes **many-to-many** (optional on both sides).\n- Boxes with **PK** mark primary keys; **FK** marks foreign keys.\n\n## 4.2 Block-Level Functional Architecture Flow\n\n```mermaid\nflowchart TD\n    A1[Patient Module] --> S1[(HMS Central Database)]\n    A2[Doctor Module] --> S1\n    A3[Appointment Module] --> S1\n    A4[Admission Module] --> S1\n    A5[Pharmacy Module] --> S1\n    A6[Lab Module] --> S1\n    A7[Billing Module] --> S1\n    A8[Staff Module] --> S1\n\n    S1 --> O1[Generate Reports]\n    S1 --> O2[Trigger Alerts]\n    S1 --> O3[Enforce Access Control]\n    S1 --> O4[Maintain Audit Logs]\n\n    O1 --> R1[Management Dashboards]\n    O2 --> R2[Critical Care Notifications]\n    O3 --> R3[Role Based Views]\n    O4 --> R4[Compliance Reports]\n```\n\n**Logic of the flow:**\n- The **left column** is the *input* (operational modules feeding data).\n- The **central cylinder** represents the **relational DBMS engine**.\n- The **right column** is the *output* (reporting, alerts, security, audits).\n- Every arrow crossing the central cylinder is a **transaction** (commit / rollback).\n\n## 4.3 Schema Layered View (3-Schema Architecture Mapping)\n\n```mermaid\nflowchart LR\n    subgraph Internal_Level\n        IL[Physical Storage: B+ Tree Index on patient_id, Hash on bill_id]\n    end\n\n    subgraph Conceptual_Level\n        CL[Logical Schema: 13 Relations with FK Constraints]\n    end\n\n    subgraph External_Level\n        E1[Doctor View: Sees only assigned patients]\n        E2[Billing View: Sees only bill and payment tables]\n        E3[Receptionist View: Sees only patient and appointment tables]\n    end\n\n    IL --> CL\n    CL --> E1\n    CL --> E2\n    CL --> E3\n```\n\n**Logic:** This reflects the **ANSI/SPARC 3-schema architecture**. Each user group interacts with a **view** defined via SQL `CREATE VIEW`, ensuring *data abstraction* and *security*.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Question 1 `[KTU University Exam - May 2024]`\n**List any six entities and their primary keys for a Hospital Management System.**\n\n**Model Answer (6 points):**\n1. `PATIENT` → `patient_id`\n2. `DOCTOR` → `doctor_id`\n3. `DEPARTMENT` → `dept_id`\n4. `APPOINTMENT` → `appointment_id`\n5. `MEDICINE` → `med_id`\n6. `BILL` → `bill_id`\n\n**Valuation tip:** Examiners award 0.5 marks per correct entity-PK pair. State **attribute type** (e.g., `patient_id INT`) to score full marks.\n\n---\n\n### Question 2 `[KTU University Exam - Dec 2023]`\n**Differentiate between strong entity and weak entity with an HMS example.**\n\n**Model Answer (Structured as a Table):**\n\n| Aspect | Strong Entity | Weak Entity |\n|---|---|---|\n| **Definition** | Has its own primary key. | Cannot exist without an owner entity. |\n| **Identifier** | Self-sufficient PK. | PK = owner PK + partial key. |\n| **HMS Example** | `DOCTOR(doctor_id)` | `MEDICAL_RECORD(record_id, patient_id)` |\n| **Representation** | Single rectangle. | Double rectangle. |\n| **Relationship** | Identifying or non-identifying. | Always **identifying** (double diamond). |\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks Each, with Internal Choice)\n\n### Question A (Choice 1) `[KTU University Exam - May 2024]`\n\n**(a) [7 Marks]** Design an ER diagram for a Hospital Management System with entities `PATIENT`, `DOCTOR`, `APPOINTMENT`, `DEPARTMENT`, `MEDICINE`, and `PRESCRIPTION`. Specify all attributes, primary keys, relationships, and cardinality ratios.\n\n**(b) [7 Marks]** Map the above ER diagram into a relational schema and normalize it up to **3NF**.\n\n---\n\n#### Model Solution to (a):\n\n**Entities and Attributes:**\n\n1. `PATIENT` (`patient_id` PK, `name`, `DOB`, `gender`, `blood_group`, `phone`, `address`)\n2. `DOCTOR` (`doctor_id` PK, `name`, `specialization`, `salary`, `dept_id` FK)\n3. `DEPARTMENT` (`dept_id` PK, `dept_name`, `head_doctor_id` FK)\n4. `APPOINTMENT` (`appointment_id` PK, `patient_id` FK, `doctor_id` FK, `date`, `time`, `status`)\n5. `MEDICINE` (`med_id` PK, `name`, `manufacturer`, `price`, `stock`)\n6. `PRESCRIPTION` (`prescription_id` PK, `patient_id` FK, `doctor_id` FK, `med_id` FK, `dosage`, `duration`)\n\n**Relationships and Cardinality:**\n\n| Relationship | Entities Involved | Cardinality | Description |\n|---|---|---|---|\n| `belongs_to` | DOCTOR — DEPARTMENT | N:1 | Each doctor works in one department. |\n| `books` | PATIENT — APPOINTMENT | 1:N | A patient can book many appointments. |\n| `schedules` | DOCTOR — APPOINTMENT | 1:N | A doctor handles many appointments. |\n| `receives` | PATIENT — PRESCRIPTION | 1:N | A patient gets many prescriptions. |\n| `writes` | DOCTOR — PRESCRIPTION | 1:N | A doctor writes many prescriptions. |\n| `listed_in` | MEDICINE — PRESCRIPTION | M:N | A prescription lists many medicines. |\n| `head_of` | DOCTOR — DEPARTMENT | 1:1 | One doctor heads a department. |\n\n**ER Diagram (Mermaid):**\n\n```mermaid\nerDiagram\n    DEPARTMENT ||--o{ DOCTOR : \"belongs_to\"\n    DOCTOR ||--o{ APPOINTMENT : \"schedules\"\n    PATIENT ||--o{ APPOINTMENT : \"books\"\n    PATIENT ||--o{ PRESCRIPTION : \"receives\"\n    DOCTOR ||--o{ PRESCRIPTION : \"writes\"\n    MEDICINE ||--o{ PRESCRIPTION : \"listed_in\"\n    DOCTOR ||--|| DEPARTMENT : \"head_of\"\n```\n\n**Mark Distribution (7 marks):**\n- 2 marks for listing all 6 entities with PKs.\n- 2 marks for identifying relationships.\n- 1 mark for cardinality ratios.\n- 1 mark for participation constraints.\n- 1 mark for diagram neatness and labeled attributes.\n\n---\n\n#### Model Solution to (b):\n\n**Step 1 — Initial Relations from ER Mapping:**\n\n$$\n\\text{DEPARTMENT}(\\underline{\\text{dept\\_id}},\\ \\text{dept\\_name},\\ \\text{head\\_doctor\\_id}^*)\n$$\n\n$$\n\\text{DOCTOR}(\\underline{\\text{doctor\\_id}},\\ \\text{name},\\ \\text{specialization},\\ \\text{salary},\\ \\text{dept\\_id}^*)\n$$\n\n$$\n\\text{PATIENT}(\\underline{\\text{patient\\_id}},\\ \\text{name},\\ \\text{DOB},\\ \\text{gender},\\ \\text{blood\\_group},\\ \\text{phone},\\ \\text{address})\n$$\n\n$$\n\\text{APPOINTMENT}(\\underline{\\text{appointment\\_id}},\\ \\text{patient\\_id}^*,\\ \\text{doctor\\_id}^*,\\ \\text{date},\\ \\text{time},\\ \\text{status})\n$$\n\n$$\n\\text{MEDICINE}(\\underline{\\text{med\\_id}},\\ \\text{name},\\ \\text{manufacturer},\\ \\text{price},\\ \\text{stock})\n$$\n\n$$\n\\text{PRESCRIPTION}(\\underline{\\text{prescription\\_id}},\\ \\text{patient\\_id}^*,\\ \\text{doctor\\_id}^*,\\ \\text{med\\_id}^*,\\ \\text{dosage},\\ \\text{duration})\n$$\n\n**Step 2 — 1NF Check:** All attributes are atomic. ✓\n\n**Step 3 — 2NF Check:** In `PRESCRIPTION`, the PK is `prescription_id` (single attribute), so no partial dependency exists. ✓\n\n**Step 4 — 3NF Check:** In `DOCTOR`, `dept_id` is a FK to DEPARTMENT. The relation has no non-key attribute transitively dependent on the PK. ✓ However, we should split `DEPARTMENT` to remove transitive dependency of `dept_name` via `dept_id` — already done since `dept_id` IS the PK of DEPARTMENT. So the schema is in **3NF**.\n\n**Final 3NF Schema:** As listed in Step 1. All foreign keys declared with `REFERENCES` clause.\n\n**Mark Distribution (7 marks):**\n- 2 marks for relational mapping (1 mark per 3 relations).\n- 2 marks for 1NF/2NF identification.\n- 2 marks for 3NF transformation.\n- 1 mark for FK constraint specification.\n\n---\n\n### Question B (Choice 2) `[KTU University Exam - Dec 2023]`\n\n**(a) [7 Marks]** Explain the concept of **functional dependency** and identify all FDs in the relation:\n\n$$\nR = (\\text{patient\\_id},\\ \\text{patient\\_name},\\ \\text{doctor\\_id},\\ \\text{doctor\\_name},\\ \\text{dept\\_id},\\ \\text{dept\\_name})\n$$\n\nFind the **candidate key** and normalize up to **BCNF**.\n\n**(b) [7 Marks]** Write SQL `CREATE TABLE` statements for `PATIENT`, `DOCTOR`, and `APPOINTMENT` with all necessary constraints (PK, FK, CHECK, NOT NULL).\n\n---\n\n#### Model Solution to (a):\n\n**Step 1 — Identify Functional Dependencies:**\n\n$$\n\\text{patient\\_id} \\rightarrow \\text{patient\\_name}\n$$\n\n$$\n\\text{doctor\\_id} \\rightarrow \\text{doctor\\_name},\\ \\text{dept\\_id}\n$$\n\n$$\n\\text{dept\\_id} \\rightarrow \\text{dept\\_name}\n$$\n\n**Step 2 — Find Candidate Key:**\n\nCompute the closure of `patient_id, doctor_id`:\n\n$$\n\\{\\text{patient\\_id, doctor\\_id}\\}^{+} = \\{\\text{patient\\_id, patient\\_name, doctor\\_id, doctor\\_name, dept\\_id, dept\\_name}\\}\n$$\n\nAll attributes are derived, so the **candidate key** is `(patient_id, doctor_id)`.\n\n**Step 3 — Identify Highest Normal Form Violations:**\n\n- `doctor_name` depends only on `doctor_id` → **partial dependency** (2NF violation).\n- `dept_name` depends transitively on `(patient_id, doctor_id)` via `doctor_id → dept_id → dept_name` → **3NF violation**.\n\n**Step 4 — Normalize to 3NF (and BCNF):**\n\n**Decomposition:**\n\n$$\nR_1 = (\\underline{\\text{patient\\_id}},\\ \\text{patient\\_name})\n$$\n\n$$\nR_2 = (\\underline{\\text{doctor\\_id}},\\ \\text{doctor\\_name},\\ \\text{dept\\_id}^*)\n$$\n\n$$\nR_3 = (\\underline{\\text{dept\\_id}},\\ \\text{dept\\_name})\n$$\n\n$$\nR_4 = (\\underline{\\text{patient\\_id, doctor\\_id}})\n$$\n\n**BCNF Verification:**\n- In $R_1$: `patient_id → patient_name`, and `patient_id` is the PK (superkey). ✓\n- In $R_2$: `doctor_id` is the PK. ✓\n- In $R_3$: `dept_id` is the PK. ✓\n- In $R_4$: Composite PK, no non-key attribute. ✓\n\nHence the decomposed schema is in **BCNF**.\n\n**Mark Distribution (7 marks):**\n- 1 mark for FD identification.\n- 1 mark for candidate key computation.\n- 2 marks for 2NF/3NF violation detection.\n- 2 marks for correct decomposition.\n- 1 mark for BCNF verification.\n\n---\n\n#### Model Solution to (b):\n\n**SQL DDL Statements:**\n\n```sql\nCREATE TABLE PATIENT (\n    patient_id  INT PRIMARY KEY,\n    name        VARCHAR(100) NOT NULL,\n    DOB         DATE NOT NULL,\n    gender      CHAR(1) CHECK (gender IN ('M','F','O')),\n    blood_group VARCHAR(5) CHECK (blood_group IN ('A+','A-','B+','B-','AB+','AB-','O+','O-')),\n    phone       VARCHAR(15) UNIQUE,\n    address     VARCHAR(200)\n);\n\nCREATE TABLE DOCTOR (\n    doctor_id      INT PRIMARY KEY,\n    name           VARCHAR(100) NOT NULL,\n    specialization VARCHAR(50) NOT NULL,\n    salary         DECIMAL(10,2) CHECK (salary > 0),\n    dept_id        INT NOT NULL,\n    FOREIGN KEY (dept_id) REFERENCES DEPARTMENT(dept_id)\n        ON DELETE CASCADE\n);\n\nCREATE TABLE APPOINTMENT (\n    appointment_id INT PRIMARY KEY,\n    patient_id     INT NOT NULL,\n    doctor_id      INT NOT NULL,\n    appt_date      DATE NOT NULL CHECK (appt_date >= CURRENT_DATE),\n    appt_time      TIME NOT NULL,\n    status         VARCHAR(20) DEFAULT 'Scheduled'\n                   CHECK (status IN ('Scheduled','Completed','Cancelled')),\n    FOREIGN KEY (patient_id) REFERENCES PATIENT(patient_id),\n    FOREIGN KEY (doctor_id)  REFERENCES DOCTOR(doctor_id)\n);\n```\n\n**Explanation of Constraints Used:**\n\n- `PRIMARY KEY` → Uniquely identifies each row (entity integrity).\n- `FOREIGN KEY ... REFERENCES` → Enforces referential integrity.\n- `CHECK (gender IN ...)` → Domain integrity (only valid values).\n- `NOT NULL` → Required fields cannot be empty.\n- `UNIQUE` → No duplicate phone numbers allowed.\n- `DEFAULT` → Auto-fills status if not provided.\n\n**Mark Distribution (7 marks):**\n- 1 mark per correct `CREATE TABLE` (3 marks total).\n- 1 mark for PK constraint.\n- 1 mark for FK constraint.\n- 1 mark for CHECK + NOT NULL.\n- 1 mark for proper data types and lengths.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall Callout — Common Mark Losers:**\n> 1. **Forgetting to mark FKs with asterisks** in the relational schema — examiners deduct 1 mark per missing FK notation.\n> 2. **Confusing 2NF with 3NF**: 2NF is about *partial* dependency; 3NF is about *transitive* dependency. Mixing these is the #1 reason students lose 2–3 marks.\n> 3. **Drawing the ER diagram with cardinalities in the wrong direction**: e.g., writing `1:N` when it should be `N:1`. Always state which entity is on the \"1\" side.\n> 4. **Forgetting ON DELETE / ON UPDATE clauses** in FK definitions — KTU 2024 specifically tests referential actions.\n> 5. **Using `VARCHAR(50)` for monetary fields** — should always be `DECIMAL(10,2)` to avoid floating-point errors in billing.\n> 6. **Not specifying candidate key properly** — a relation may have **multiple** candidate keys; list **all** of them and pick the **primary** one.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember (Rapid Revision Checklist)\n\n> [!IMPORTANT]\n> **High-Yield Rapid-Revision Summary for KTU 2024 ESE:**\n\n- ⭐ **HMS = Database-centric application** with 8+ major modules (Patient, Doctor, Appointment, Admission, Pharmacy, Lab, Billing, Staff).\n- ⭐ **ER modeling** is the **first** step — identify **6–13 entities** with PKs, then relationships with **cardinality ratios**.\n- ⭐ **M:N relationships** → always create a **junction table** with 2 FKs and relationship attributes.\n- ⭐ **1:1 relationships** → merge with either side (typically the side with **total participation**).\n- ⭐ **1:N relationships** → place FK on the **\"many\" side**.\n- ⭐ **Weak entities** → double rectangle; PK = owner PK + partial key.\n- ⭐ **Multivalued attributes** → separate relation with composite PK.\n- ⭐ **Normalization sequence**: 1NF (atomic) → 2NF (no partial dep) → 3NF (no transitive dep) → BCNF (every determinant is a superkey).\n- ⭐ **ACID properties** are non-negotiable for HMS — billing and prescription updates must be **atomic**.\n- ⭐ **Referential actions**: `CASCADE`, `SET NULL`, `RESTRICT`, `NO ACTION` — choose based on business rule.\n- ⭐ **Use `DECIMAL`** for money, **never** `FLOAT` or `DOUBLE`.\n- ⭐ **SQL CHECK constraints** enforce domain integrity (e.g., gender ∈ {M, F, O}).\n- ⭐ **VIEWS** provide user-level security (Doctor View vs. Billing View vs. Receptionist View).\n- ⭐ **3-Schema Architecture** (Internal / Conceptual / External) is the KTU-recommended approach for HMS design.\n- ⭐ **Indexes** (B+ tree on PK, hash on frequently searched columns) optimize HMS query performance.\n- ⭐ **Roles and Privileges** via `GRANT` and `REVOKE` enforce healthcare data protection (HIPAA/DISHA style).\n- ⭐ **Common HMS queries for exam practice**:\n  - List patients admitted in the last N days.\n  - Find total revenue per department.\n  - Find doctors with the most appointments.\n  - List medicines with stock below threshold.\n  - Generate consolidated bill (JOIN BILL + APPOINTMENT + PRESCRIPTION + LAB_TEST).\n- ⭐ **Star Schema** for HMS Data Warehouse: `FACT_BILL(patient_id, doctor_id, date, amount)` + dimension tables `DIM_PATIENT`, `DIM_DOCTOR`, `DIM_DATE`.\n- ⭐ **Transaction example**: Bill generation must atomically update `BILL` and decrement `MEDICINE.stock` — if either fails, **ROLLBACK**.\n\n> [!NOTE]\n> **Last-Minute Mnemonic for KTU:** **\"E-R-M-N\"** = **E**ntity → **R**elationship → **M**apping → **N**ormalization. Master these four steps in order, and you can solve any HMS question in the KTU 2024 ESE.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "banktransactionsmanagementsystem": {
      "title": "Bank Transactions Management System",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PBITT304 - DATABASE MANAGEMENT SYSTEM\n> - **Module:** Module 10\n> - **Topic:** Bank Transactions Management System\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### Formal Academic Definition\n\nA **Bank Transactions Management System (BTMS)** is a centralized, relational database–backed software application that models, stores, manipulates, and retrieves all financial activities (deposits, withdrawals, transfers, loan disbursements, interest calculations) of a banking institution while enforcing the **ACID properties (Atomicity, Consistency, Isolation, Durability)** mandated by the relational model.\n\nIn the KTU 2024 Scheme DBMS syllabus, Module 10 is dedicated to **Real-World Database Application Design** — translating an actual organizational process (here, banking) into a fully normalized relational schema, implementing it using **SQL (DDL, DML, DCL, TCL)**, and embedding business rules through **Triggers, Views, and Stored Procedures**.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Highlight:** Module 10 of PBITT304 emphasizes **practical relational database design** and the use of **active database features (triggers)** to enforce integrity constraints that cannot be expressed via simple `CHECK` clauses.\n\n### Core Entities of a Bank Database\n\n| Entity | Purpose | Key Attributes |\n|---|---|---|\n| **Branch** | Physical bank location | `branch_id`, `branch_name`, `city` |\n| **Customer** | Account holder details | `cust_id`, `name`, `address`, `phone` |\n| **Account** | Bank account record | `acc_no`, `cust_id`, `branch_id`, `balance`, `acc_type` |\n| **Transaction** | Log of every debit/credit | `trans_id`, `acc_no`, `amount`, `trans_type`, `trans_date` |\n| **Loan** | Loan sanctioned to customer | `loan_id`, `cust_id`, `amount`, `interest_rate` |\n| **Employee** | Bank staff | `emp_id`, `name`, `branch_id`, `salary` |\n\n> [!NOTE]\n> **ACID** is the cornerstone of any banking database — without it, two simultaneous withdrawals could drain an account beyond zero, or a transfer could \"vanish\" between accounts on a system crash.\n\n### Conceptual Analogy / Intuition\n\nImagine a **massive digital ledger book** that the entire bank shares in real-time. When you withdraw ₹5,000 from an ATM:\n\n1. The teller/ATM is like a *messenger* who finds the right page (your account row) in the giant book.\n2. The messenger writes `-5000` on your page and simultaneously writes `+5000` on the bank's cash ledger page.\n3. Crucially, **both writes happen together, or neither does** — otherwise the books become inconsistent (this is *Atomicity*).\n4. If two messengers try to update the same page simultaneously, they must *take turns* (this is *Isolation* via locking).\n\nThe **DBMS** is the *strict librarian* who enforces these rules so the ledger never lies.\n\n### Physical/Design Metrics Used in Banking Systems\n\n- **Currency Precision**: Stored as `DECIMAL(15,2)` — never `FLOAT` (to avoid binary rounding errors on money).\n- **Daily Transaction Limit**: A typical business rule is **₹50,000** for non-privileged accounts.\n- **Minimum Balance Requirement (MBR)**: Commonly **₹1,000** for savings accounts.\n- **Audit Retention Period**: Transaction logs are retained for **7 years** (RBI mandate).\n</br>\n\n> [!IMPORTANT]\n> Always store monetary values using exact numeric types (`DECIMAL`/`NUMERIC`) — **never** use floating-point (`FLOAT`, `REAL`, `DOUBLE`) in financial systems.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### The Relational Schema of BTMS\n\nA banking database must satisfy multiple **Functional Dependencies** to be in **3NF (Third Normal Form)**, eliminating update, insertion, and deletion anomalies.\n\nThe canonical KTU-style schema for a Bank Transactions Management System is as follows:\n\n**BRANCH**(<u>branch_id</u>, branch_name, city)\n\n**CUSTOMER**(<u>cust_id</u>, cust_name, address, phone, email)\n\n**ACCOUNT**(<u>acc_no</u>, cust_id*, branch_id*, acc_type, balance, open_date)\n\n**TRANSACTION**(<u>trans_id</u>, acc_no*, amount, trans_type, trans_date, remarks)\n\n**LOAN**(<u>loan_id</u>, cust_id*, amount, interest_rate, start_date)\n\n**EMPLOYEE**(<u>emp_id</u>, emp_name, branch_id*, salary, designation)\n\n\\* denotes a **foreign key** reference.\n\n### High-Yield Formula Sheet (DBMS-Theory Mapping)\n\n| Concept | Formula / Rule | Real-World Banking Utility |\n|---|---|---|\n| **Atomicity** | Transaction = `BEGIN ... COMMIT` / `ROLLBACK` | Ensures debit + credit occur as one indivisible unit |\n| **Balance Update Rule** | `New_Balance = Old_Balance ± Amount` | Maintains ledger correctness |\n| **Minimum Balance Check** | `IF New_Balance < MBR THEN DENY` | Prevents accounts going into unhealthy state |\n| **Transfer Equation** | `A.balance -= X` AND `B.balance += X` | Cross-account money movement |\n| **Interest Calculation (Simple)** | `I = (P × R × T) / 100` | Annual interest on savings |\n| **Interest Calculation (Compound)** | `A = P × (1 + R/n)^(n×t)` | Loan EMI accumulation |\n| **1NF Rule** | Every cell must be *atomic* (no multi-valued fields) | Avoids `phone1, phone2, phone3` in one column |\n| **2NF Rule** | No *partial dependency* on composite PK | Splits transaction-customer mixed tables |\n| **3NF Rule** | No *transitive dependency* between non-key attrs | `city` should not depend on `branch_name` |\n| **ACID Mnemonic** | **A**tomicity, **C**onsistency, **I**solation, **D**urability | Foundation of all reliable DB transactions |\n| **Lock Granularity** | `Row-level` > `Page-level` > `Table-level` | Fine-grained locking = higher concurrency |\n| **Codd's 12 Rules** | 12 conditions for a truly relational DBMS | Theoretical benchmark (rarely fully met) |\n\n> [!NOTE]\n> For the **KTU board exam**, the single most tested theoretical fact on banking databases is the mapping of **ACID properties to transaction commands** (`BEGIN`, `COMMIT`, `ROLLBACK`, `SAVEPOINT`).\n\n### Why Each Normal Form Matters in Banking\n\n- **1NF**: A customer's multiple phone numbers must not be stored in a single column like `'9876543210, 9123456789'`. Use a separate `CUSTOMER_PHONE` table.\n- **2NF**: If `TRANSACTION` had `(trans_id, acc_no, cust_name)` as composite PK, then `cust_name` would *partially* depend only on `acc_no`. Move `cust_name` to `CUSTOMER` table.\n- **3NF**: In `EMPLOYEE(emp_id, branch_id, branch_name, salary)`, `branch_name` transitively depends on `emp_id` via `branch_id`. Move `branch_name` to `BRANCH` table.\n- **BCNF**: A stricter 3NF — every determinant must be a candidate key.\n\n> [!IMPORTANT]\n> KTU examiners frequently award 7-mark questions on \"Normalize this banking schema up to BCNF.\" Master the **anomaly → dependency → decomposition** reasoning chain.\n\n### Engineering Real-World Utility\n\n- **Online Banking Portals**: Every login session executes dozens of `SELECT`, `UPDATE`, and `INSERT` queries against this schema.\n- **ATM Networks**: Real-time `SELECT FOR UPDATE` queries lock the account row during withdrawal.\n- **Mobile Banking Apps**: Use the same schema over REST APIs with read replicas.\n- **Core Banking Systems (CBS)**: Indian banks like SBI use proprietary RDBMS (Oracle/DB2) hosting exactly this kind of schema on **mainframes**.\n</br>\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### Derivation 1 — Relational Schema Construction (Entity-Relationship Mapping)\n\n**Step 1**: Identify entities from the banking problem statement.\n- Strong entities: Branch, Customer, Account, Loan, Employee\n- Weak entity: Transaction (depends on Account for existence)\n\n**Step 2**: Identify relationships.\n- `Customer` 1 — N `Account` (one customer can have multiple accounts)\n- `Branch` 1 — N `Account`\n- `Account` 1 — N `Transaction`\n- `Customer` 1 — N `Loan`\n\n**Step 3**: Convert ER to relational tables.\n- For 1:N relationships, place the **foreign key on the \"many\" side**.\n- For weak entities, include the owner's PK as part of the weak entity's PK (or as a FK with `ON DELETE CASCADE`).\n\n**Step 4**: Apply primary keys.\n\n$$\\text{ACCOUNT}_{PK} = \\{acc\\_no\\}$$\n\n$$\\text{TRANSACTION}_{PK} = \\{trans\\_id\\}$$\n\n$$\\text{TRANSACTION}_{FK} = \\{acc\\_no \\rightarrow ACCOUNT.acc\\_no\\}$$\n\n**Step 5**: Apply referential integrity constraints.\n\n$$\\forall t \\in \\text{TRANSACTION}, \\quad \\exists a \\in \\text{ACCOUNT} \\mid t.acc\\_no = a.acc\\_no$$\n\n**Step 6**: Identify candidate keys and choose primary keys based on minimality and stability.\n\n### Derivation 2 — ACID Property Verification for a Fund Transfer\n\nConsider a transfer of ₹10,000 from Account A → Account B.\n\n**Initial State:**\n$$A.balance = 50000, \\quad B.balance = 30000$$\n\n**Transaction T:**\n```\nT1: UPDATE ACCOUNT SET balance = balance - 10000 WHERE acc_no = 'A';\nT2: UPDATE ACCOUNT SET balance = balance + 10000 WHERE acc_no = 'B';\nCOMMIT;\n```\n\n**Atomicity check**: If T1 succeeds but T2 fails (e.g., power outage), the system must `ROLLBACK` T1.\n\n**Consistency check**: Total money in system before and after must remain equal.\n\n$$\\text{Before: } A + B = 50000 + 30000 = 80000$$\n\n$$\\text{After: } A + B = 40000 + 40000 = 80000 \\quad \\checkmark$$\n\n**Isolation check**: If another transaction reads A's balance between T1 and T2, it must see the *old* value (read-committed) or be blocked.\n\n**Durability check**: Once `COMMIT` is acknowledged, the new balances survive any subsequent system crash (via write-ahead logging).\n\n### Derivation 3 — Simple vs Compound Interest on a Loan\n\nA customer takes a **Personal Loan** of ₹5,00,000 at **10.5% per annum** for **3 years**.\n\n**Simple Interest Formula:**\n\n$$I = \\frac{P \\times R \\times T}{100}$$\n\nSubstituting $P = 500000$, $R = 10.5$, $T = 3$:\n\n$$I = \\frac{500000 \\times 10.5 \\times 3}{100}$$\n\n$$I = \\frac{15750000}{100} = 157500$$\n\nTotal amount payable:\n\n$$A = P + I = 500000 + 157500 = 657500$$\n\n**Compound Interest Formula** (compounded annually, $n = 1$):\n\n$$A = P \\times \\left(1 + \\frac{R}{n}\\right)^{n \\times t}$$\n\nSubstituting:\n\n$$A = 500000 \\times \\left(1 + \\frac{10.5}{100}\\right)^{3}$$\n\n$$A = 500000 \\times (1.105)^{3}$$\n\n$$A = 500000 \\times 1.349232625$$\n\n$$A = 674616.31$$\n\nCompound interest is therefore:\n\n$$I_{compound} = A - P = 674616.31 - 500000 = 174616.31$$\n\n**EMI Calculation** (Equal Monthly Instalment):\n\n$$\\text{EMI} = \\frac{P \\times r \\times (1+r)^n}{(1+r)^n - 1}$$\n\nwhere $P = 500000$, annual $R = 10.5\\%$, monthly $r = 10.5 / 12 / 100 = 0.00875$, $n = 36$ months.\n\nStep 1: Compute $(1+r)^n$:\n\n$$(1.00875)^{36} \\approx 1.3693$$\n\nStep 2: Numerator:\n\n$$500000 \\times 0.00875 \\times 1.3693 = 500000 \\times 0.011981 = 5990.55$$\n\nStep 3: Denominator:\n\n$$1.3693 - 1 = 0.3693$$\n\nStep 4: Final EMI:\n\n$$\\text{EMI} = \\frac{5990.55}{0.3693} \\approx 16220.12$$\n\n### Derivation 4 — Normalization Walkthrough (Up to BCNF)\n\n**Original Un-normalized Table `LOAN_DETAILS`:**\n\n| loan_id | cust_name | cust_phone | branch_name | amount | interest |\n|---|---|---|---|---|---|\n| L1 | Anil | 9876543210 | Kochi | 500000 | 10.5 |\n\n**Functional Dependencies identified:**\n\n$$FD_1: loan\\_id \\rightarrow cust\\_name, cust\\_phone, branch\\_name, amount, interest$$\n\n$$FD_2: cust\\_name \\rightarrow cust\\_phone$$\n\n$$FD_3: branch\\_name \\rightarrow \\text{(city info — assumed)}$$\n\n**1NF**: Already atomic (each cell holds a single value). ✓\n\n**2NF**: The PK `loan_id` is not composite, so no partial dependency exists. ✓\n\n**3NF**: `cust_phone` transitively depends on `loan_id` via `cust_name`. This violates 3NF.\n\n**Decomposition (3NF):**\n\n- `LOAN(loan_id, cust_name, branch_name, amount, interest)`\n- `CUSTOMER(cust_name, cust_phone)`\n- `BRANCH(branch_name, city)`\n\n**BCNF Check**: In `LOAN`, the determinant is `loan_id` (a candidate key) ✓. In `CUSTOMER`, determinant is `cust_name` (a candidate key) ✓. In `BRANCH`, determinant is `branch_name` (a candidate key) ✓.\n\nHence the schema is now in **BCNF**.\n</br>\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### Diagram 1 — Entity-Relationship Diagram (Bank Schema)\n\n```mermaid\nerDiagram\n    BRANCH ||--o{ ACCOUNT : \"has\"\n    BRANCH ||--o{ EMPLOYEE : \"employs\"\n    CUSTOMER ||--o{ ACCOUNT : \"owns\"\n    CUSTOMER ||--o{ LOAN : \"borrows\"\n    ACCOUNT ||--o{ TRANSACTION : \"records\"\n\n    BRANCH {\n        int branch_id PK\n        string branch_name\n        string city\n    }\n    CUSTOMER {\n        int cust_id PK\n        string cust_name\n        string address\n        string phone\n    }\n    ACCOUNT {\n        int acc_no PK\n        int cust_id FK\n        int branch_id FK\n        string acc_type\n        decimal balance\n        date open_date\n    }\n    TRANSACTION {\n        int trans_id PK\n        int acc_no FK\n        decimal amount\n        string trans_type\n        datetime trans_date\n        string remarks\n    }\n    LOAN {\n        int loan_id PK\n        int cust_id FK\n        decimal amount\n        float interest_rate\n        date start_date\n    }\n    EMPLOYEE {\n        int emp_id PK\n        string emp_name\n        int branch_id FK\n        decimal salary\n        string designation\n    }\n```\n\n### Diagram 2 — Fund Transfer Transaction Flow\n\n```mermaid\nflowchart TD\n    A[\"User initiates transfer of Rs 10000 from A to B\"] --> B[\"BEGIN TRANSACTION\"]\n    B --> C[\"Lock Account A row SELECT FOR UPDATE\"]\n    C --> D{\"A balance >= 10000?\"}\n    D -- No --> E[\"ROLLBACK and show insufficient funds error\"]\n    D -- Yes --> F[\"UPDATE Account A balance minus 10000\"]\n    F --> G[\"UPDATE Account B balance plus 10000\"]\n    G --> H{\"Both updates succeeded?\"}\n    H -- No --> I[\"ROLLBACK entire transaction\"]\n    H -- Yes --> J[\"INSERT into TRANSACTION log\"]\n    J --> K[\"COMMIT transaction\"]\n    K --> L[\"Release locks and notify user\"]\n```\n\n### Diagram 3 — ACID Properties Mapping\n\n```mermaid\nflowchart LR\n    subgraph A[\"Atomicity\"]\n        A1[\"All operations succeed together\"]\n        A2[\"OR all are rolled back together\"]\n    end\n    subgraph C[\"Consistency\"]\n        C1[\"Valid state before\"]\n        C2[\"Valid state after\"]\n    end\n    subgraph I[\"Isolation\"]\n        I1[\"Concurrent transactions\"]\n        I2[\"Do not interfere with each other\"]\n    end\n    subgraph D[\"Durability\"]\n        D1[\"Committed data survives\"]\n        D2[\"System crashes and power failures\"]\n    end\n```\n\n### Diagram 4 — Trigger Execution Flow (Minimum Balance Enforcement)\n\n```mermaid\nflowchart TD\n    P[\"BEFORE UPDATE trigger on ACCOUNT\"] --> Q[\"Capture OLD balance and NEW balance\"]\n    Q --> R[\"Calculate difference as withdraw amount\"]\n    R --> S{\"NEW balance < 1000 MBR?\"}\n    S -- Yes --> T[\"Signal SQLSTATE 45000 error\"]\n    S -- No --> U[\"Allow UPDATE to proceed\"]\n    T --> V[\"ROLLBACK the offending UPDATE\"]\n```\n\n### Diagram 5 — Normalization Hierarchy (Applied to Banking Schema)\n\n```mermaid\nflowchart TD\n    UF[\"Unnormalized Form - repeating groups in transaction list\"] --> N1[\"1NF - all attributes atomic\"]\n    N1 --> N2[\"2NF - remove partial dependencies\"]\n    N2 --> N3[\"3NF - remove transitive dependencies\"]\n    N3 --> BC[\"BCNF - every determinant is a candidate key\"]\n    BC --> N4[\"4NF - remove multi-valued dependencies\"]\n    N4 --> N5[\"5NF - remove join dependencies\"]\n```\n</br>\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### Part A Questions (3 Marks Each)\n\n---\n\n**Q1. [KTU University Exam – Dec 2023]**\n*Define the ACID properties of a database transaction. Which property ensures that committed data is never lost even after a system crash?*\n\n**Model Answer (3 marks):**\n\nThe ACID properties of a database transaction are:\n\n- **A – Atomicity**: A transaction is treated as a single, indivisible unit of work. Either **all** its operations execute successfully and are committed, or **none** of them take effect (rolled back).\n\n- **C – Consistency**: A transaction transforms the database from one **valid state** to another valid state, preserving all defined integrity constraints (primary keys, foreign keys, `CHECK` constraints, triggers).\n\n- **I – Isolation**: Concurrent transactions execute as if they were running **sequentially**. The intermediate state of one transaction is invisible to others until it commits.\n\n- **D – Durability**: Once a transaction has been **committed**, its effects survive any subsequent system failure (crash, power loss). This is achieved via **write-ahead logs (WAL)** and database backups.\n\nThe property that ensures committed data is never lost after a system crash is **Durability**.\n\n*(Award: 1 mark per property explanation + 1 mark for naming durability correctly + 1 mark for the WAL/backup mention = 3 marks)*\n\n---\n\n**Q2. [KTU University Exam – June 2024]**\n*List any three anomalies that occur in an unnormalized banking database. Which normal form removes each of these anomalies?*\n\n**Model Answer (3 marks):**\n\n| Anomaly | Description | Normal Form that Removes It |\n|---|---|---|\n| **Update Anomaly** | Updating a customer's address in one row leaves stale values in other duplicate rows | **2NF** (removes partial dependencies) |\n| **Insertion Anomaly** | Cannot record a new branch until at least one customer/employee is assigned to it | **3NF** (removes transitive dependencies) |\n| **Deletion Anomaly** | Deleting the last account of a customer also deletes the customer's contact info | **BCNF / 3NF** (splits mixed-purpose tables) |\n\n*(Award: 1 mark per anomaly + correct normal form mapping)*\n\n---\n\n### Part B Questions (14 Marks Each) — Module Internal Choice\n\n---\n\n### **Question A (14 Marks)**\n\n**Q3a. [KTU University Exam – Dec 2023, Model Paper]**\n*Design a relational database schema for a Bank Transactions Management System with at least 5 entities. Clearly specify primary keys, foreign keys, and referential integrity constraints. Write the SQL DDL statements to create the tables with appropriate data types. (7 marks)*\n\n**Model Solution:**\n\nThe five core entities and their DDL statements are:\n\n```sql\nCREATE TABLE Branch (\n    branch_id      INT PRIMARY KEY,\n    branch_name    VARCHAR(50) NOT NULL UNIQUE,\n    city           VARCHAR(50) NOT NULL\n);\n\nCREATE TABLE Customer (\n    cust_id        INT PRIMARY KEY,\n    cust_name      VARCHAR(100) NOT NULL,\n    address        VARCHAR(200),\n    phone          VARCHAR(15) UNIQUE,\n    email          VARCHAR(100) UNIQUE\n);\n\nCREATE TABLE Account (\n    acc_no         INT PRIMARY KEY,\n    cust_id        INT NOT NULL,\n    branch_id      INT NOT NULL,\n    acc_type       VARCHAR(20) CHECK (acc_type IN ('Savings','Current','Fixed')),\n    balance        DECIMAL(15,2) NOT NULL DEFAULT 0.00\n                   CHECK (balance >= 0),\n    open_date      DATE NOT NULL,\n    FOREIGN KEY (cust_id)   REFERENCES Customer(cust_id) ON DELETE CASCADE,\n    FOREIGN KEY (branch_id) REFERENCES Branch(branch_id)\n);\n\nCREATE TABLE Transaction (\n    trans_id       INT PRIMARY KEY,\n    acc_no         INT NOT NULL,\n    amount         DECIMAL(15,2) NOT NULL CHECK (amount > 0),\n    trans_type     VARCHAR(10) CHECK (trans_type IN ('Deposit','Withdrawal')),\n    trans_date     DATETIME DEFAULT CURRENT_TIMESTAMP,\n    remarks        VARCHAR(200),\n    FOREIGN KEY (acc_no) REFERENCES Account(acc_no) ON DELETE CASCADE\n);\n\nCREATE TABLE Loan (\n    loan_id        INT PRIMARY KEY,\n    cust_id        INT NOT NULL,\n    amount         DECIMAL(15,2) NOT NULL CHECK (amount > 0),\n    interest_rate  DECIMAL(5,2) NOT NULL CHECK (interest_rate BETWEEN 0 AND 50),\n    start_date     DATE NOT NULL,\n    FOREIGN KEY (cust_id) REFERENCES Customer(cust_id) ON DELETE CASCADE\n);\n```\n\n**Mark Distribution:**\n- ER identification (1 mark)\n- Schema design with PKs and FKs clearly marked (2 marks)\n- Correct SQL DDL syntax for 5 tables (3 marks)\n- Appropriate data types and constraints (1 mark)\n\n---\n\n**Q3b. [KTU University Exam – Dec 2023, Model Paper]**\n*Write SQL queries for the following requirements on the Bank schema above:*\n*(i) Find the names of all customers who have a balance greater than ₹1,00,000 in any account. (3 marks)*\n*(ii) Transfer ₹5,000 from Account 101 to Account 102 using a single transaction. (4 marks)*\n\n**Model Solution:**\n\n**(i) Customers with balance > ₹1,00,000:**\n\n```sql\nSELECT DISTINCT C.cust_name\nFROM Customer C\nJOIN Account A ON C.cust_id = A.cust_id\nWHERE A.balance > 100000;\n```\n\nStep-by-step explanation:\n- `JOIN` combines the `Customer` and `Account` tables on `cust_id`.\n- `WHERE A.balance > 100000` filters the rows.\n- `DISTINCT` ensures each customer name appears only once even if they have multiple qualifying accounts.\n\n**(ii) Fund Transfer Transaction:**\n\n```sql\nSTART TRANSACTION;\n\n-- Check sufficient balance in source account\nSELECT balance FROM Account WHERE acc_no = 101 FOR UPDATE;\n\nUPDATE Account SET balance = balance - 5000 WHERE acc_no = 101;\n\nUPDATE Account SET balance = balance + 5000 WHERE acc_no = 102;\n\n-- Log both transactions\nINSERT INTO Transaction (trans_id, acc_no, amount, trans_type, remarks)\nVALUES (1001, 101, 5000, 'Withdrawal', 'Transfer to A/c 102');\n\nINSERT INTO Transaction (trans_id, acc_no, amount, trans_type, remarks)\nVALUES (1002, 102, 5000, 'Deposit', 'Transfer from A/c 101');\n\nCOMMIT;\n```\n\nExplanation:\n- `START TRANSACTION` begins an atomic unit.\n- `FOR UPDATE` acquires a row-level lock to prevent concurrent modifications.\n- Two `UPDATE` statements modify the source and destination balances.\n- Two `INSERT` statements record the audit trail.\n- `COMMIT` makes all changes permanent; any failure before this point triggers an automatic `ROLLBACK`.\n\n**Mark Distribution for 3b:**\n- Correct query for (i) with proper join (3 marks)\n- Complete transaction block for (ii) with lock + updates + log + commit (4 marks)\n\n---\n\n### **Question B (14 Marks) — Alternative Choice**\n\n**Q4a. [KTU University Exam – June 2024]**\n*Explain the concept of database triggers with a banking example. Write a BEFORE UPDATE trigger on the Account table that prevents a withdrawal if it would cause the balance to fall below ₹1,000 (Minimum Balance Requirement). (7 marks)*\n\n**Model Solution:**\n\nA **trigger** is a stored procedure that automatically executes in response to specified events (`INSERT`, `UPDATE`, `DELETE`) on a particular table. Triggers are used to enforce complex business rules that cannot be expressed via standard `CHECK` constraints.\n\n**Trigger Implementation:**\n\n```sql\nDELIMITER $$\n\nCREATE TRIGGER check_min_balance\nBEFORE UPDATE ON Account\nFOR EACH ROW\nBEGIN\n    IF NEW.balance < 1000 THEN\n        SIGNAL SQLSTATE '45000'\n        SET MESSAGE_TEXT = 'ERROR: Balance cannot fall below Minimum Balance Requirement of Rs 1000';\n    END IF;\nEND$$\n\nDELIMITER ;\n```\n\nExplanation:\n- `BEFORE UPDATE` ensures the trigger runs *before* the change is committed to the row.\n- `NEW.balance` refers to the updated value; `OLD.balance` would refer to the existing value.\n- `SIGNAL SQLSTATE '45000'` raises a custom error, causing the entire `UPDATE` to be rolled back automatically.\n- The `DELIMITER $$ ... DELIMITER ;` syntax is MySQL-specific to allow `;` inside the trigger body.\n\nTest case:\n```sql\n-- Suppose account 101 has balance 1500\nUPDATE Account SET balance = balance - 600 WHERE acc_no = 101;\n-- New balance = 900, which is < 1000 → trigger fires → UPDATE is rejected\n```\n\n**Mark Distribution for 4a:**\n- Conceptual explanation of triggers (2 marks)\n- Correct trigger syntax with proper keywords (3 marks)\n- Correct logic for MBR enforcement (2 marks)\n\n---\n\n**Q4b. [KTU University Exam – June 2024]**\n*Demonstrate the concept of a database view by creating a view `Customer_Account_Summary` that shows each customer's name, account number, balance, and account type. Write three different SQL queries that utilize this view. (7 marks)*\n\n**Model Solution:**\n\nA **view** is a virtual table defined by a `SELECT` query. It does not store data physically but dynamically retrieves results from underlying base tables when referenced.\n\n**View Creation:**\n\n```sql\nCREATE VIEW Customer_Account_Summary AS\nSELECT  C.cust_id,\n        C.cust_name,\n        A.acc_no,\n        A.balance,\n        A.acc_type\nFROM Customer C\nJOIN Account A ON C.cust_id = A.cust_id;\n```\n\n**Three Queries Using the View:**\n\n**(i) Find all customers who have Savings accounts:**\n\n```sql\nSELECT cust_name, acc_no, balance\nFROM Customer_Account_Summary\nWHERE acc_type = 'Savings';\n```\n\n**(ii) Calculate the total balance held by each customer:**\n\n```sql\nSELECT cust_name, SUM(balance) AS total_balance\nFROM Customer_Account_Summary\nGROUP BY cust_id, cust_name\nORDER BY total_balance DESC;\n```\n\n**(iii) Find the average balance across all account types:**\n\n```sql\nSELECT acc_type, AVG(balance) AS avg_balance, COUNT(*) AS num_accounts\nFROM Customer_Account_Summary\nGROUP BY acc_type;\n```\n\n**Mark Distribution for 4b:**\n- Correct view creation syntax with JOIN (2 marks)\n- Three meaningful and correctly written queries using the view (1.5 marks each = 4.5 marks; round to 5)\n- Brief explanation of what a view is (0.5 marks)\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> 1. **Using `FLOAT` for money**: Marks deducted if balance columns use `FLOAT`/`REAL` instead of `DECIMAL`.\n> 2. **Forgetting `COMMIT` in transaction blocks**: The transfer query will appear \"correct\" syntactically but lacks transaction control — expect **2-mark penalty**.\n> 3. **Confusing `OLD` and `NEW` in triggers**: Mixing up these keywords is the most common error in trigger-based questions.\n> 4. **Using `SELECT *` in view definitions**: Examiners prefer explicit column lists for clarity and to avoid breakage when base tables change.\n> 5. **Missing `ON DELETE CASCADE`**: When a customer is deleted, orphaned accounts/loans violate referential integrity — KTU examiners specifically test this.\n> 6. **Writing `WHERE balance >= 0` as a CHECK but allowing negative updates via triggers**: Always combine both layers of protection.\n\n---\n\n### 📌 Topic Recap & Important Things to Remember\n\n- **Core Entities of BTMS**: Branch, Customer, Account, Transaction, Loan, Employee — must be in your memory verbatim.\n- **Always use `DECIMAL(15,2)`** for monetary values — never `FLOAT`.\n- **ACID = Atomicity, Consistency, Isolation, Durability** — the four pillars of reliable transactions.\n- **Transaction Control Commands**: `BEGIN/START TRANSACTION`, `COMMIT`, `ROLLBACK`, `SAVEPOINT`.\n- **Normalization Hierarchy**: 1NF (atomic) → 2NF (no partial dep) → 3NF (no transitive dep) → BCNF → 4NF → 5NF.\n- **Foreign Key Placement**: Always on the \"many\" side of a 1:N relationship.\n- **Triggers**: `BEFORE`/`AFTER` + `INSERT`/`UPDATE`/`DELETE` + `FOR EACH ROW` — used for MBR, audit logs, etc.\n- **Views**: Virtual tables; use them for security (column-level hiding) and query simplification.\n- **Row Locking**: `SELECT ... FOR UPDATE` prevents dirty reads during concurrent transactions.\n- **Audit Logging**: Always `INSERT` into a `Transaction` table for every debit/credit — **non-negotiable** in banking systems.\n- **Minimum Balance Rule**: Typically ₹1,000 for savings — enforced via both `CHECK` constraint AND trigger.\n- **Compound Interest Master Formula**: $A = P \\times (1 + R/n)^{n \\times t}$ — frequently tested for 14-mark numerical problems.\n- **EMI Formula**: $\\text{EMI} = \\frac{P \\times r \\times (1+r)^n}{(1+r)^n - 1}$ — must know by heart for full marks.\n- **Codd's Rules (12)**: Often tested as a 3-mark definition question — mention rule 0 (foundational) and rule 12 (non-subversion).\n- **Referential Actions**: `CASCADE`, `SET NULL`, `RESTRICT`, `NO ACTION` — know when to use each.\n- **KTU's favorite twist**: They will give you a partially normalized table and ask \"normalize up to BCNF\" — practice identifying transitive dependencies.\n\n> [!TIP]\n> **Last-Minute Revision Strategy**: Draw the ER diagram from memory, write all 5 DDL statements without looking, and recite ACID properties aloud. These three actions cover ~70% of any BTMS question KTU can ask.\n<!-- SECTION_5_END -->",
      "pyqs": []
    }
  }
}
