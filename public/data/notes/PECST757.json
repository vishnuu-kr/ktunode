{
  "code": "PECST757",
  "name": "HIGH PERFORMANCE COMPUTING",
  "topics": {
    "storedprogramcomputerarchitecturegeneralpurposecachebasedmicroprocessorarchitectureperformancemetricsandbenchmarksmooreslawpipeliningsuperscalaritysimdmemoryhierarchiescache": {
      "title": "Stored-Program Computer Architecture - General-Purpose Cache-Based Microprocessor Architecture - Performance Metrics and Benchmarks - Moore's Law - Pipelining - Superscalarity - SIMD - Memory Hierarchies - Cache",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 1\n> - **Topic:** Stored-Program Computer Architecture - General-Purpose Cache-Based Microprocessor Architecture - Performance Metrics and Benchmarks - Moore's Law - Pipelining - Superscalarity - SIMD - Memory Hierarchies - Cache\n\n<!-- SECTION_1_START -->\n\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Stored-Program Computer Architecture (von Neumann Architecture)\n\n> [!IMPORTANT]\n> **Formal Definition:** The *Stored-Program Computer Architecture*, proposed by John von Neumann in 1945, is a computer design paradigm in which **both the instructions (program) and data reside in a single, shared memory** that is accessed through a common bus. The CPU fetches instructions sequentially from memory, decodes them, and executes them using the ALU, with results stored back into the same memory.\n\n**Conceptual Analogy (Intuition):**\nImagine a chef in a kitchen. The recipes (instructions) and the ingredients (data) are all stored in the *same* set of shelves (memory). The chef (CPU) can pull a recipe card, follow it to handle ingredients, and put modified ingredients back on the same shelves. This unified storage means the chef can dynamically choose *which* recipe to cook next — making the system extremely flexible. This is the \"stored-program\" idea: the program is just data that the machine itself can read and modify.\n\n**Key Properties of von Neumann Architecture:**\n- **Single memory** for instructions and data.\n- **Single bus** for instruction fetch and data access (creates the *von Neumann bottleneck*).\n- **Sequential instruction execution** (one instruction at a time in the basic form).\n- **Programmable** — instructions are binary-encoded and treated as data.\n\n## 1.2 General-Purpose Cache-Based Microprocessor Architecture\n\n> [!NOTE]\n> **Formal Definition:** A *General-Purpose Cache-Based Microprocessor* is a CPU that incorporates **one or more levels of high-speed cache memory (L1, L2, L3)** between the registers and main memory (DRAM). It is designed to execute a wide variety of workloads by exploiting temporal and spatial locality, while keeping the architectural state (registers, program counter) general enough to run arbitrary software.\n\n**Intuition:** Think of the CPU as a busy office worker. The desk (registers) holds only a few papers, the side cabinet (cache) holds the current project's files, the office library (main memory) holds all files, and a far-away warehouse (disk) holds archives. The worker doesn't fetch from the warehouse every time — they keep *recent* files in the cabinet (cache) for instant access.\n\n## 1.3 Performance Metrics and Benchmarks\n\n> [!IMPORTANT]\n> **Formal Definition:** *Performance metrics* are quantitative measures used to evaluate computer system speed, efficiency, and throughput. *Benchmarks* are standardized programs (SPEC CPU, LINPACK, PARSEC, Geekbench) used to compare systems reproducibly.\n\n**Core Performance Metrics:**\n- **CPU Execution Time** $(T_{CPU})$\n- **Clock Cycle Time** $(T_{clk})$\n- **Clock Frequency** $(f)$\n- **CPI (Cycles Per Instruction)**\n- **MIPS (Million Instructions Per Second)**\n- **MFLOPS / GFLOPS / TFLOPS**\n- **Speedup $(S)$** and **Efficiency $(E)$**\n- **Throughput** (instructions/sec, requests/sec)\n\n## 1.4 Moore's Law\n\n> [!NOTE]\n> **Formal Definition:** *Moore's Law*, articulated by Gordon Moore (1965), observes that the **number of transistors on an integrated circuit doubles approximately every 18–24 months**, leading to exponential growth in compute capability and a corresponding drop in cost per transistor.\n\n**Intuition:** Every two years, engineers pack twice as many microscopic switches into the same-sized silicon chip — like fitting twice the number of lanes on the same stretch of highway. More transistors = faster, more parallel, more energy-efficient processors.\n\n## 1.5 Pipelining\n\n> [!IMPORTANT]\n> **Formal Definition:** *Pipelining* is an implementation technique in which **multiple instructions are overlapped in execution** by partitioning the instruction processing into stages (e.g., IF, ID, EX, MEM, WB). Each stage operates on a different instruction concurrently, analogous to an assembly line.\n\n**Intuition:** A car wash with 5 stations. While car A is being dried, car B is being soaped, car C is being rinsed, and so on. Five cars complete in roughly the time it would take to wash one car sequentially — vastly improving throughput.\n\n## 1.6 Superscalarity\n\n> [!NOTE]\n> **Formal Definition:** *Superscalar* processors **dispatch and execute more than one instruction per clock cycle** from a sequential instruction stream, using multiple parallel execution units. The hardware dynamically checks for data dependencies and issues instructions out-of-order when safe.\n\n**Intuition:** Instead of one cashier serving one customer at a time, a superscalar processor is like a checkout counter with **multiple cashiers** working in parallel — all serving the same line (instruction stream) at once.\n\n## 1.7 SIMD (Single Instruction, Multiple Data)\n\n> [!IMPORTANT]\n> **Formal Definition:** *SIMD* is a parallel computing architecture in which a **single instruction operates simultaneously on multiple data elements** packed into wide vector registers. Modern x86 CPUs use SSE/AVX; ARM uses NEON/SVE; GPUs use CUDA cores / wide SIMT lanes.\n\n**Intuition:** Imagine a teacher asking 32 students to each add 1 to their own number. Instead of calling on each student one at a time, the teacher shouts the instruction once, and **all 32 students apply it in parallel** — same operation, different data.\n\n## 1.8 Memory Hierarchies\n\n> [!NOTE]\n> **Formal Definition:** A *Memory Hierarchy* is an arrangement of storage technologies ordered by **speed, cost, and capacity**, with the fastest, smallest, and most expensive memory closest to the CPU and the slowest, largest, cheapest farthest away. Typical levels: **Registers → L1 → L2 → L3 → Main Memory (DRAM) → SSD/Disk → Tape/Cloud**.\n\n**Intuition:** It is like a layered wardrobe: the watch you wear (registers) is fastest to access; the bedside drawer (L1) is next; the closet (L2) is slower; the warehouse (disk) is slowest. The principle is **locality**: most programs access a small subset of memory most of the time.\n\n## 1.9 Cache\n\n> [!IMPORTANT]\n> **Formal Definition:** A *Cache* is a small, fast memory that stores **copies of data from frequently used main memory locations** to reduce the average memory access time. It exploits **temporal locality** (recently accessed items are likely to be re-accessed) and **spatial locality** (items near recently accessed items are likely to be accessed soon).\n\n**Cache Mapping Schemes:**\n- **Direct Mapped** — each block maps to exactly one cache line.\n- **Fully Associative** — a block can map to any cache line.\n- **Set Associative** — a block maps to a specific *set*, can occupy any *way* within that set.\n\n**Cache Performance Events:**\n- **Hit** — data found in cache (fast).\n- **Miss** — data not in cache (must fetch from next level).\n- **Hit Rate $(h)$** / **Miss Rate $(1-h)$**.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 CPU Performance Fundamentals (Why and How)\n\nThe **Why:** Every architecture decision — pipelining, caches, SIMD, multi-core — is fundamentally an attempt to *reduce CPU execution time* or *increase throughput*. The canonical performance equation expresses this in measurable hardware terms.\n\n**The How — the foundational performance chain:**\n\n1. **CPU Execution Time** is the ultimate metric visible to the user.\n2. It decomposes into **Instruction Count (IC)**, **CPI**, and **Clock Cycle Time**.\n3. **IC** is determined by the *ISA and compiler*.\n4. **CPI** is determined by the *microarchitecture* (pipelining, cache, branch prediction).\n5. **Clock Cycle Time** is determined by the *technology and hardware design*.\n\n> [!NOTE]\n> **KTU HIGH-YIELD FORMULA SHEET**\n\n| # | Formula | Description | Typical Units |\n|---|---------|-------------|---------------|\n| 1 | $T_{CPU} = IC \\times CPI \\times T_{clk}$ | CPU execution time (full equation) | seconds |\n| 2 | $f = \\dfrac{1}{T_{clk}}$ | Clock frequency | Hz |\n| 3 | $T_{CPU} = \\dfrac{IC \\times CPI}{f}$ | Alternative form using frequency | seconds |\n| 4 | $MIPS = \\dfrac{IC}{T_{CPU} \\times 10^{6}} = \\dfrac{f}{CPI \\times 10^{6}}$ | Million Instructions Per Second | MIPS |\n| 5 | $MFLOPS = \\dfrac{FN_{ops}}{T_{CPU} \\times 10^{6}}$ | Floating-point throughput | MFLOPS |\n| 6 | $S = \\dfrac{T_{old}}{T_{new}}$ | Speedup of a new system | dimensionless |\n| 7 | $E = \\dfrac{S}{p} = \\dfrac{T_{old}}{p \\cdot T_{new}}$ | Parallel efficiency (p = processors) | 0 to 1 |\n| 8 | $S_{overall} = \\dfrac{1}{(1-f) + \\dfrac{f}{s}}$ | Amdahl's Law (f = parallel fraction, s = speedup of that fraction) | dimensionless |\n| 9 | $S_{pipeline}^{ideal} = k$ | Ideal k-stage pipeline speedup (k stages) | dimensionless |\n| 10 | $CPI_{pipeline} = CPI_{ideal} + \\text{stalls per instruction}$ | Effective pipeline CPI | cycles/instr |\n| 11 | $AMAT = T_{hit} + MR \\cdot T_{miss}$ | Average Memory Access Time (cache) | seconds/cycles |\n| 12 | $T_{access}^{hierarchical} = h_1 T_1 + (1-h_1)\\big[h_2 T_2 + (1-h_2)T_3\\big]$ | Multi-level access time (recursion) | seconds |\n| 13 | $N(t) = N_0 \\cdot 2^{t/d}$ | Moore's Law — transistors after t years (doubling period d) | transistors |\n| 14 | $CPI_{superscalar} = \\dfrac{\\text{total cycles}}{\\text{total retired instructions}}$ | Issue rate bound (≥ 1/n for n-wide) | cycles/instr |\n| 15 | $Speedup_{SIMD} = \\dfrac{\\text{vector length}}{1 + \\text{overhead ratio}}$ | SIMD theoretical speedup | dimensionless |\n\n> [!WARNING]\n> **KTU Pitfall:** Never use vertical pipes `|` inside formula tables. Use `\\vert` for absolute value, conditional probability, etc. The table above strictly avoids `|` symbols for KTU PDF/HTML compatibility.\n\n## 2.2 Moore's Law — Mathematical Form\n\nThe **Why:** It establishes a *quantitative* basis for predicting future compute capability, which architects use to plan for parallelism, multi-core, and specialized accelerators.\n\n**Exponential form:**\n\n$$N(t) = N_0 \\cdot 2^{t / d}$$\n\nWhere:\n- $N(t)$ = number of transistors after time $t$\n- $N_0$ = initial transistor count\n- $d$ = doubling period (≈ 18–24 months, traditionally 24)\n- $t$ = elapsed time in same unit as $d$\n\n**Cost implication:** Cost per transistor falls approximately as $1/N(t)$, while performance per watt has historically doubled every ~18 months (Koomey's corollary).\n\n## 2.3 Pipelining — Latency vs Throughput\n\nThe **Why:** Pipelining does *not* reduce the latency of a single instruction; it increases the **instruction throughput** (instructions completed per cycle, IPC).\n\n**The How — pipeline CPI components:**\n\n$$CPI_{pipeline} = CPI_{base} + \\sum_{i=1}^{n} (\\text{stalls per instr from hazard } i)$$\n\n- **Structural hazards** — resource conflicts.\n- **Data hazards** — RAW, WAR, WAW dependencies.\n- **Control hazards** — branch mispredictions.\n\n## 2.4 Cache and Memory Hierarchy Mathematics\n\nThe **Why:** Modern CPUs spend up to **40–60%** of execution time waiting on memory. AMAT is the unifying metric for evaluating any cache design.\n\n**AMAT (Average Memory Access Time):**\n\n$$AMAT = T_{hit} + MR \\cdot T_{miss-penalty}$$\n\nFor a 2-level hierarchy (L1 + main memory):\n\n$$T_{avg} = h_1 \\cdot T_1 + (1 - h_1)\\big[h_2 \\cdot T_2 + (1 - h_2) \\cdot T_3\\big]$$\n\n**Hit time ratio** — critical design constraint:\n- Lower-level caches → **smaller, faster, but with lower hit rate**.\n- Higher-level caches → **larger, slower, but with higher hit rate**.\n\n> [!NOTE]\n> **Real-World Utility:** AMAT is used by architects at Intel, AMD, Apple, and ARM to size each cache level, choose associativity, and select replacement policies (LRU, pseudo-LRU, RRIP). Even a 5% increase in L2 hit rate can yield 10–15% performance gains in SPEC CPU workloads.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation of the Fundamental Performance Equation\n\n**Starting premise:** Total CPU time = (Instructions) × (Cycles per Instruction) × (Seconds per Cycle).\n\n$$\n\\begin{aligned}\nT_{CPU} &= IC \\times CPI \\times T_{clk} \\\\[4pt]\nT_{CPU} &= \\dfrac{IC \\times CPI}{f} \\quad \\left(\\text{since } f = \\dfrac{1}{T_{clk}}\\right) \\\\[4pt]\nT_{CPU} &= \\dfrac{IC}{IPC \\times f} \\quad \\left(\\text{since } IPC = \\dfrac{1}{CPI}\\right)\n\\end{aligned}\n$$\n\n**Step-by-step interpretation:**\n1. We start with three independent factors that together fully determine runtime.\n2. $IC$ depends on the *algorithm*, *ISA*, and *compiler* — not on hardware microarchitecture.\n3. $CPI$ is the *microarchitectural* lever (pipelining, issue width, branch predictor quality).\n4. $T_{clk}$ is the *circuit/technology* lever (process node, pipeline depth, voltage).\n5. **Conclusion:** To halve $T_{CPU}$, you can halve any *one* factor, or combine partial improvements.\n\n> [!NOTE]\n> **Killer exam line:** *\"Performance is not just clock speed\"* — doubling $f$ alone rarely doubles performance, because memory stalls (cache misses) do not scale with clock.\n\n---\n\n## 3.2 Derivation of Amdahl's Law\n\n**Starting premise:** Let $f$ be the fraction of execution time that *can* be sped up by a factor of $s$ (e.g., parallelized, vectorized, or accelerated). The remaining $(1 - f)$ runs at the original speed.\n\n**Execution time after improvement:**\n\n$$\n\\begin{aligned}\nT_{new} &= (1 - f) \\cdot T_{old} + \\dfrac{f}{s} \\cdot T_{old} \\\\[4pt]\nT_{new} &= T_{old} \\left[ (1 - f) + \\dfrac{f}{s} \\right]\n\\end{aligned}\n$$\n\n**Speedup $S$:**\n\n$$\n\\begin{aligned}\nS &= \\dfrac{T_{old}}{T_{new}} \\\\[4pt]\nS &= \\dfrac{T_{old}}{T_{old}\\left[(1 - f) + \\dfrac{f}{s}\\right]} \\\\[4pt]\n\\therefore \\quad S &= \\dfrac{1}{(1 - f) + \\dfrac{f}{s}}\n\\end{aligned}\n$$\n\n**Key limiting case** (infinite parallel speedup, $s \\to \\infty$):\n\n$$\nS_{\\max} = \\dfrac{1}{1 - f}\n$$\n\n**Numerical example:** If $f = 0.9$ (90% parallelizable) and $s = 100$:\n- $S = 1 / [0.1 + 0.9/100] = 1 / [0.1 + 0.009] = 1 / 0.109 = 9.17$\n- Even with infinite cores, $S_{max} = 1/(1-0.9) = 10$.\n\n> [!WARNING]\n> **KTU Pitfall:** Students often apply Amdahl's Law to *symmetric multi-core* and forget that memory contention and synchronization overhead further reduce $s$. Use Gustafson's Law for fixed-workload scaling.\n\n---\n\n## 3.3 Derivation of Pipeline Speedup\n\n**Setup:** A non-pipelined processor takes $k$ cycles per instruction (k = number of stages). An ideal k-stage pipeline completes **one instruction per cycle** after the initial fill latency.\n\n**Time for $n$ instructions:**\n\n- Non-pipelined: $T_{np} = n \\cdot k$ cycles.\n- Pipelined (ideal): $T_{pipe} = (k - 1) + n$ cycles (k−1 fill, then one per cycle).\n\n**Speedup:**\n\n$$\n\\begin{aligned}\nS_{ideal} &= \\dfrac{n \\cdot k}{(k - 1) + n} \\\\[4pt]\n\\lim_{n \\to \\infty} S_{ideal} &= k\n\\end{aligned}\n$$\n\n**Effective CPI with stalls:**\n\n$$\nCPI_{eff} = CPI_{ideal} + \\text{stalls} = 1 + \\text{stalls per instruction}\n$$\n\n**Pipeline efficiency:**\n\n$$\n\\eta = \\dfrac{k}{k + (n-1) \\cdot \\text{stalls per instr}} \\cdot 100\\%\n$$\n\n---\n\n## 3.4 Derivation of AMAT for a 2-Level Cache\n\n**Setup:** L1 has hit time $T_1$, hit rate $h_1$. On L1 miss, we go to L2 (hit time $T_2$, hit rate $h_2$). On L2 miss, we go to main memory ($T_3$).\n\n**Average time per L1 access:**\n\n$$\n\\begin{aligned}\nT_{avg} &= h_1 \\cdot T_1 + (1 - h_1) \\cdot T_{L1-miss} \\\\[4pt]\nT_{L1-miss} &= h_2 \\cdot T_2 + (1 - h_2) \\cdot T_3\n\\end{aligned}\n$$\n\n**Substitute back:**\n\n$$\n\\therefore \\quad T_{avg} = h_1 T_1 + (1 - h_1)\\big[h_2 T_2 + (1 - h_2) T_3\\big]\n$$\n\n**Numerical example:**\n- $T_1 = 1$ ns, $h_1 = 0.95$\n- $T_2 = 5$ ns, $h_2 = 0.98$\n- $T_3 = 100$ ns (DRAM)\n\n$$\n\\begin{aligned}\nT_{avg} &= 0.95 \\times 1 + 0.05 \\times [0.98 \\times 5 + 0.02 \\times 100] \\\\[4pt]\n&= 0.95 + 0.05 \\times [4.9 + 2.0] \\\\[4pt]\n&= 0.95 + 0.05 \\times 6.9 \\\\[4pt]\n&= 0.95 + 0.345 \\\\[4pt]\n&= 1.295 \\text{ ns}\n\\end{aligned}\n$$\n\n> [!NOTE]\n> **Insight:** Even with 95% L1 hit rate, the L1 miss penalty (60.9 ns) is **47× the L1 hit time**. This is why modern CPUs aggressively prefetch, increase associativity, and use inclusive/exclusive cache coherence protocols.\n\n---\n\n## 3.5 Moore's Law Numerical Computation\n\n**Given:** Initial transistor count in 1971 (Intel 4004) = 2,300. Doubling period $d = 2$ years. Find transistor count in 2024 ($t = 53$ years).\n\n$$\n\\begin{aligned}\nN(53) &= 2300 \\cdot 2^{53/2} \\\\[4pt]\n&= 2300 \\cdot 2^{26.5} \\\\[4pt]\n&= 2300 \\times 94{,}915{,}849 \\\\[4pt]\n&\\approx 2.18 \\times 10^{11} \\text{ transistors}\n\\end{aligned}\n$$\n\nThis aligns with modern server chips (e.g., Apple M3 Ultra ≈ 134 billion, Nvidia H100 ≈ 80 billion), confirming the empirical trend.\n\n---\n\n## 3.6 Analytical Matrix: Choosing the Right Parallelism Technique\n\n| Technique | Granularity | Best For | Key Bottleneck | KTU Tag |\n|-----------|------------|----------|----------------|---------|\n| **Pipelining** | Instruction-level | All sequential code | Data/control hazards | ILP |\n| **Superscalar** | Instruction-level (multiple issue) | Code with abundant ILP | Dependency chains | ILP |\n| **SIMD** | Data-parallel | Vectorizable loops (DSP, ML) | Non-vectorizable branches | DLP |\n| **Multi-core (MIMD)** | Thread-level | Independent tasks | Amdahl's fraction, coherence | TLP |\n| **GPU (SIMT)** | Data-parallel + thread | Massively parallel kernels | Memory bandwidth, divergence | DLP/TLP |\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Von Neumann Architecture (Stored-Program Computer)\n\n```mermaid\nflowchart LR\n    CPU[\"CPU Central Processing Unit\"] --> CU[\"Control Unit CU\"]\n    CPU --> ALU[\"ALU Arithmetic Logic Unit\"]\n    CPU --> REG[\"Registers PC IR ACC\"]\n    MEM[\"Main Memory Unified\"] -->|Instructions and Data| CPU\n    CPU -->|Address Bus| MEM\n    MEM -->|Data Bus| CPU\n    IO[\"Input Output Devices\"] <-->|Via Controller| MEM\n    EXT[\"Secondary Storage Disk SSD\"] <--> MEM\n```\n\n**Explanation of nodes:**\n- **CPU** contains the Control Unit, ALU, and Registers (PC, IR, Accumulator).\n- **Main Memory** holds *both* instructions and data — the defining feature.\n- **I/O** and **Secondary Storage** are connected through controllers to main memory.\n\n---\n\n## 4.2 General-Purpose Cache-Based Microprocessor Architecture\n\n```mermaid\nflowchart TB\n    CORE[\"CPU Core\"] --> REG[\"Registers 32 or 64 x 64 bit\"]\n    REG --> L1[\"L1 Cache 32 KB Split I and D\"]\n    L1 --> L2[\"L2 Cache 256 KB to 1 MB Unified\"]\n    L2 --> L3[\"L3 Cache 8 to 64 MB Shared\"]\n    L3 --> CTRL[\"Memory Controller\"]\n    CTRL --> DRAM[\"DRAM Main Memory 8 to 128 GB\"]\n    DRAM --> SSD[\"NVMe SSD 1 to 8 TB\"]\n    SSD --> NET[\"Network or Cloud Storage\"]\n    L1 -.->|Hit Returns Data| CORE\n    L2 -.->|Miss Fallback| L1\n    L3 -.->|Miss Fallback| L2\n```\n\n**Explanation:** Modern CPUs use a 3-level cache hierarchy. L1 is split into instruction and data caches for parallel access. L3 is shared across cores for inter-core communication efficiency.\n\n---\n\n## 4.3 5-Stage Instruction Pipeline (MIPS-style)\n\n```mermaid\nflowchart LR\n    subgraph cycle1[\"Cycle 1\"]\n        I1[\"I1 IF\"]\n    end\n    subgraph cycle2[\"Cycle 2\"]\n        I1a[\"I1 ID\"] ~~~ I2[\"I2 IF\"]\n    end\n    subgraph cycle3[\"Cycle 3\"]\n        I1b[\"I1 EX\"] ~~~ I2a[\"I2 ID\"] ~~~ I3[\"I3 IF\"]\n    end\n    subgraph cycle4[\"Cycle 4\"]\n        I1c[\"I1 MEM\"] ~~~ I2b[\"I2 EX\"] ~~~ I3a[\"I3 ID\"] ~~~ I4[\"I4 IF\"]\n    end\n    subgraph cycle5[\"Cycle 5\"]\n        I1d[\"I1 WB\"] ~~~ I2c[\"I2 MEM\"] ~~~ I3b[\"I3 EX\"] ~~~ I4a[\"I4 ID\"] ~~~ I5[\"I5 IF\"]\n    end\n    cycle1 --> cycle2 --> cycle3 --> cycle4 --> cycle5\n```\n\n**Explanation:** At cycle 5, five instructions are in-flight simultaneously across the five stages. Steady-state IPC = 1 (one instruction completed per cycle).\n\n---\n\n## 4.4 Memory Hierarchy Pyramid\n\n```mermaid\nflowchart TB\n    TOP[\"Registers 1 to 4 KB One cycle 1 ns\"]\n    L1[\"L1 Cache 32 to 64 KB One to two cycles 1 to 2 ns\"]\n    L2[\"L2 Cache 256 KB to 1 MB Ten to twenty cycles 5 to 10 ns\"]\n    L3[\"L3 Cache 8 to 64 MB Forty to eighty cycles 20 to 40 ns\"]\n    DRAM[\"DRAM 8 to 128 GB Two hundred cycles 100 ns\"]\n    SSD[\"NVMe SSD 1 to 8 TB Ten to fifty microseconds\"]\n    HDD[\"HDD or Tape TB to PB One to ten milliseconds\"]\n    TOP --> L1 --> L2 --> L3 --> DRAM --> SSD --> HDD\n```\n\n**Explanation:** Capacity grows downward; speed grows upward. The cost per bit and per access also grows upward.\n\n---\n\n## 4.5 Superscalar vs SIMD vs Multi-core\n\n```mermaid\nflowchart TB\n    subgraph SS[\"Superscalar ILP Multiple Issue per Cycle\"]\n        SS1[\"Issue Logic\"]\n        SS2[\"ALU 1\"]\n        SS3[\"ALU 2\"]\n        SS4[\"FPU 1\"]\n        SS5[\"LSU\"]\n    end\n    subgraph SIMDBlock[\"SIMD DLP One Instruction Many Data\"]\n        SM1[\"256 bit Vector Register\"]\n        SM2[\"Lane 0 32 bit\"]\n        SM3[\"Lane 1 32 bit\"]\n        SM4[\"Lane 2 32 bit\"]\n        SM5[\"Lane 3 32 bit\"]\n        SM6[\"Lane 4 32 bit\"]\n        SM7[\"Lane 5 32 bit\"]\n        SM8[\"Lane 6 32 bit\"]\n        SM9[\"Lane 7 32 bit\"]\n    end\n    subgraph MC[\"Multi core TLP Independent Threads\"]\n        MC1[\"Core 0\"]\n        MC2[\"Core 1\"]\n        MC3[\"Core 2\"]\n        MC4[\"Core 3\"]\n    end\n    SS1 --> SS2\n    SS1 --> SS3\n    SS1 --> SS4\n    SS1 --> SS5\n    SM1 --> SM2\n    SM1 --> SM3\n    SM1 --> SM4\n    SM1 --> SM5\n    SM1 --> SM6\n    SM1 --> SM7\n    SM1 --> SM8\n    SM1 --> SM9\n    MC1 --- MC2\n    MC2 --- MC3\n    MC3 --- MC4\n```\n\n**Explanation:** Superscalar exploits ILP within one thread; SIMD exploits DLP within one instruction; multi-core exploits TLP across independent threads. They are *complementary* — modern CPUs combine all three.\n\n---\n\n## 4.6 Cache Mapping Schemes\n\n```mermaid\nflowchart LR\n    subgraph DM[\"Direct Mapped Block to One Line\"]\n        DM1[\"Tag Index Offset\"]\n        DM2[\"Line 0\"]\n        DM3[\"Line 1\"]\n        DM4[\"Line n minus 1\"]\n    end\n    subgraph FA[\"Fully Associative Block to Any Line\"]\n        FA1[\"Tag Offset\"]\n        FA2[\"Line 0\"]\n        FA3[\"Line 1\"]\n        FA4[\"Line n minus 1\"]\n    end\n    subgraph SA[\"Set Associative Block to Any Way in One Set\"]\n        SA1[\"Tag Set Index Offset\"]\n        SA2[\"Set 0 Way 0 to Way 3\"]\n        SA3[\"Set 1 Way 0 to Way 3\"]\n        SA4[\"Set n minus 1 Way 0 to Way 3\"]\n    end\n    DM1 --> DM2\n    FA1 --> FA2\n    SA1 --> SA2\n```\n\n**Explanation:** Direct mapped is fastest but suffers from conflict misses. Fully associative is most flexible but requires parallel tag comparison (CAM). Set associative is the practical compromise used in all modern caches.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### Question 1: [KTU University Exam — June 2023, Model Paper]\n**Explain the von Neumann architecture with a neat diagram. State the von Neumann bottleneck.**\n\n**Model Answer (3 marks):**\n\nThe **von Neumann architecture** is a stored-program computer design where **instructions and data share the same memory** and are transferred over a **single bus**. The major components are: (1) **Memory** holding both instructions and data, (2) **Control Unit (CU)** fetching and decoding instructions, (3) **ALU** performing arithmetic/logic operations, (4) **Registers** (PC, IR, ACC), and (5) **I/O** and **Secondary Storage** connected via the memory.\n\n**Von Neumann Bottleneck (1 mark):** Since instructions and data share a single bus, the CPU cannot fetch an instruction and read/write data simultaneously, limiting throughput. This is the *von Neumann bottleneck*. Modern computers mitigate it using **separate L1 instruction and data caches (Harvard-style split)** inside the CPU while retaining a unified main memory.\n\n---\n\n### Question 2: [KTU University Exam — Dec 2022]\n**Define Moore's Law. Mention its significance in high-performance computing.**\n\n**Model Answer (3 marks):**\n\n**Definition (1.5 marks):** Moore's Law, articulated by Gordon Moore in 1965, states that the **number of transistors on an integrated circuit doubles approximately every 18–24 months**, leading to exponential growth in compute capability.\n\n**Significance in HPC (1.5 marks):**\n1. **Predictive power** — architects can forecast future compute density and plan chip designs.\n2. **Performance scaling** — more transistors enable deeper pipelines, larger caches, more execution units, and multi-core designs.\n3. **Cost economics** — cost per transistor falls, making HPC systems affordable.\n4. **Driver of parallelism** — as single-core ILP saturates, Moore's Law motivates multi-core and many-core HPC (GPUs, TPUs).\n5. **Energy limits** — Dennard scaling broke down ~2005, shifting focus from clock frequency to parallel efficiency.\n\n---\n\n## 5.2 Part B — Essay Questions (14 Marks with Internal Choice)\n\n### Question A: [KTU University Exam — Model Paper, Module 1]\n**(a) [7 marks]** Explain the 5-stage instruction pipeline with a diagram. Discuss the hazards that reduce pipeline performance.\n\n**(b) [7 marks]** A processor has a clock rate of 3 GHz. The program contains $5 \\times 10^{8}$ instructions, with the following CPI breakdown: 30% ALU instructions (CPI = 1), 20% load/store (CPI = 2), 25% branch (CPI = 3), 25% floating point (CPI = 4). Calculate the total CPU time and MIPS rating.\n\n**Model Solution:**\n\n**Part (a) — Pipelining (7 marks):**\n\nThe 5-stage pipeline partitions instruction execution into:\n\n| Stage | Name | Function |\n|-------|------|----------|\n| **IF** | Instruction Fetch | Read instruction from I-cache, PC+4 |\n| **ID** | Instruction Decode | Decode opcode, read registers, sign-extend immediate |\n| **EX** | Execute | ALU operation, branch target computation |\n| **MEM** | Memory Access | Read/write D-cache for load/store |\n| **WB** | Write Back | Write result to register file |\n\nIn steady state, the pipeline completes **1 instruction per cycle (IPC = 1)**, achieving a theoretical speedup of **5×** over a non-pipelined design.\n\n**Pipeline Hazards (3 sub-hazards, 3 marks):**\n\n1. **Structural Hazards** — hardware resource conflict (e.g., single memory port serving both IF and MEM). *Resolution:* duplicate resources (separate I-cache and D-cache).\n2. **Data Hazards** — RAW (Read After Write), WAR, WAW dependencies. *Resolution:* forwarding/bypassing, register renaming, pipeline interleaving.\n3. **Control Hazards** — caused by branches and jumps. *Resolution:* branch prediction, delay slots, speculative execution, branch target buffer (BTB).\n\n**Part (b) — CPU Time Calculation (7 marks):**\n\n$$\n\\begin{aligned}\nCPI_{avg} &= \\sum_i (\\text{fraction}_i \\times CPI_i) \\\\[4pt]\n&= (0.30 \\times 1) + (0.20 \\times 2) + (0.25 \\times 3) + (0.25 \\times 4) \\\\[4pt]\n&= 0.30 + 0.40 + 0.75 + 1.00 \\\\[4pt]\n&= 2.45\n\\end{aligned}\n$$\n\n$$\n\\begin{aligned}\nT_{CPU} &= \\dfrac{IC \\times CPI_{avg}}{f} \\\\[4pt]\n&= \\dfrac{5 \\times 10^{8} \\times 2.45}{3 \\times 10^{9}} \\\\[4pt]\n&= \\dfrac{1.225 \\times 10^{9}}{3 \\times 10^{9}} \\\\[4pt]\n&= 0.4083 \\text{ seconds}\n\\end{aligned}\n$$\n\n$$\n\\begin{aligned}\nMIPS &= \\dfrac{f}{CPI_{avg} \\times 10^{6}} \\\\[4pt]\n&= \\dfrac{3 \\times 10^{9}}{2.45 \\times 10^{6}} \\\\[4pt]\n&= 1224.49 \\text{ MIPS} \\approx 1224 \\text{ MIPS}\n\\end{aligned}\n$$\n\n**Final Answer:** $T_{CPU} = 0.4083$ s, MIPS = 1224.\n\n---\n\n### Question B: [KTU University Exam — Model Paper, Module 1]\n**(a) [7 marks]** With a neat diagram, describe the memory hierarchy in modern computer systems. Explain the principles of locality that justify this hierarchy.\n\n**(b) [7 marks]** A two-level cache system has L1 with hit time = 1 ns, hit rate = 90%, and L2 with hit time = 10 ns, hit rate = 95% (on L1 misses). Main memory access time is 200 ns. Calculate (i) AMAT for the L1, (ii) overall average memory access time, and (iii) the performance improvement if L2 is removed (i.e., L1 miss goes straight to main memory).\n\n**Model Solution:**\n\n**Part (a) — Memory Hierarchy (7 marks):**\n\nThe memory hierarchy (top to bottom) is:\n\n| Level | Type | Size | Latency |\n|-------|------|------|---------|\n| 1 | CPU Registers | 1–4 KB | ~1 cycle |\n| 2 | L1 Cache | 32–64 KB | 1–2 ns |\n| 3 | L2 Cache | 256 KB – 1 MB | 5–10 ns |\n| 4 | L3 Cache | 8–64 MB | 20–40 ns |\n| 5 | Main Memory (DRAM) | 8–128 GB | 50–100 ns |\n| 6 | SSD (NVMe) | 1–8 TB | 10–50 µs |\n| 7 | HDD / Tape | TB–PB | 1–10 ms |\n\n**Two Principles of Locality (3 marks):**\n\n1. **Temporal Locality** — if a memory location is referenced, it is *likely to be referenced again soon*. Justifies keeping recently accessed data in cache.\n2. **Spatial Locality** — if a memory location is referenced, *nearby locations* are likely to be referenced soon. Justifies fetching **cache blocks (lines)** of 32–128 bytes rather than single bytes.\n\nA third principle, **sequential locality** (related to spatial), explains prefetching and stream buffers.\n\n**Part (b) — AMAT Calculations (7 marks):**\n\n**Given:**\n- L1: $T_1 = 1$ ns, $h_1 = 0.90$\n- L2: $T_2 = 10$ ns, $h_2 = 0.95$ (on L1 misses)\n- Main memory: $T_3 = 200$ ns\n\n**(i) AMAT of L1 (with L2 present):**\n\n$$\n\\begin{aligned}\nAMAT_{L1} &= T_1 + (1 - h_1) \\cdot T_{miss-L1} \\\\[4pt]\nT_{miss-L1} &= T_2 + (1 - h_2) \\cdot T_3 \\\\[4pt]\n&= 10 + 0.05 \\times 200 = 10 + 10 = 20 \\text{ ns} \\\\[4pt]\nAMAT_{L1} &= 1 + 0.10 \\times 20 = 1 + 2.0 = 3.0 \\text{ ns}\n\\end{aligned}\n$$\n\n**(ii) Overall average memory access time** = AMAT = **3.0 ns**.\n\n**(iii) Without L2 (L1 miss → main memory directly):**\n\n$$\n\\begin{aligned}\nT_{miss-L1}^{noL2} &= 200 \\text{ ns} \\\\[4pt]\nAMAT_{L1}^{noL2} &= 1 + 0.10 \\times 200 = 1 + 20 = 21.0 \\text{ ns}\n\\end{aligned}\n$$\n\n**Performance improvement** (ratio of access times):\n\n$$\n\\text{Improvement} = \\dfrac{21.0}{3.0} = 7\\times\n$$\n\n**Final Answer:** AMAT (with L2) = 3 ns; AMAT (without L2) = 21 ns; **L2 provides 7× speedup**.\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Where KTU Students Lose Marks in Module 1:**\n>\n> 1. **Confusing CPU time and CPU clock cycle** — $T_{CPU} = IC \\times CPI \\times T_{clk}$ is the *full* time; do not stop at CPI.\n> 2. **Wrong unit in MIPS** — MIPS uses $10^6$ (not $10^9$). Confusing MIPS with MFLOPS costs full marks.\n> 3. **Amdahl's Law misuse** — the formula is $S = 1/[(1-f) + f/s]$, NOT $1/f \\cdot s$. Many students invert it.\n> 4. **Pipeline speedup ≠ k** — the maximum ideal speedup of a k-stage pipeline is $k$ (only for infinite instruction stream). For finite $n$, use $(nk)/(n+k-1)$.\n> 5. **AMAT formula** — it is $T_{hit} + MR \\times T_{miss}$, not $T_{hit} \\times MR + T_{miss}$. State it correctly.\n> 6. **Cache hit rate confusion** — L2 hit rate $h_2$ is *conditional on L1 miss*, not the *global* hit rate. The product $h_1 \\times h_2$ is the global L2 hit rate.\n> 7. **Skipping the cache line/block explanation** — when asked about cache, always mention **block (line) size, tag, index, offset**.\n> 8. **Moore's Law vs Dennard Scaling** — Moore's Law (transistor count) is still alive; Dennard Scaling (constant power density) ended ~2005. Examiners love this distinction.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember (Rapid Revision Checklist)\n\n> [!NOTE]\n> **Last-Minute Revision Bullet Points for PECST757 Module 1**\n\n- ✅ **Von Neumann architecture** = single memory for instructions + data; bottleneck is the shared bus.\n- ✅ **Modified Harvard** = separate L1 I-cache and D-cache, unified main memory (used in all modern CPUs).\n- ✅ **Performance Equation:** $T_{CPU} = IC \\times CPI \\times T_{clk}$. **Optimize any one factor.**\n- ✅ **MIPS** = $f / (CPI \\times 10^6)$. **MIPS is unreliable across ISAs** (use SPEC for fair comparison).\n- ✅ **Amdahl's Law:** $S = 1/[(1-f) + f/s]$; **maximum speedup is $1/(1-f)$**.\n- ✅ **Gustafson's Law:** $S = p - (1-p) \\cdot f$ for *scaled* workloads (opposite viewpoint).\n- ✅ **Moore's Law:** transistors double every 24 months; **drives multi-core transition** after Dennard scaling ended.\n- ✅ **Pipeline speedup ideal = k** (k stages); **pipelining reduces CPI to 1**, not latency.\n- ✅ **CPI components** = base CPI + structural stalls + data-hazard stalls + branch stalls.\n- ✅ **Superscalar** = issue width $n$; **theoretical max IPC = n**; bounded by ILP and dependencies.\n- ✅ **SIMD width** = 128 (SSE), 256 (AVX2), 512 (AVX-512); **best for regular data-parallel loops**.\n- ✅ **Memory hierarchy** ordered by speed: **Registers > L1 > L2 > L3 > DRAM > SSD > HDD**.\n- ✅ **Locality principles** = **Temporal** (re-reference) and **Spatial** (nearby data).\n- ✅ **Cache hit rate** = Hits / (Hits + Misses); **miss rate = 1 − hit rate**.\n- ✅ **AMAT = $T_{hit}$ + Miss Rate × $T_{miss-penalty}$** — the **single most important** cache metric.\n- ✅ **3 mapping schemes:** Direct, Set Associative (industry standard, 4–16 way), Fully Associative.\n- ✅ **3 C's of cache misses:** Compulsory (cold), Capacity, Conflict.\n- ✅ **Benchmarks:** SPEC CPU (integer/float), LINPACK (HPL for TOP500), PARSEC, Geekbench.\n- ✅ **FLOPs scaling:** KFLOPS → MFLOPS → GFLOPS → TFLOPS → PFLOPS → EFLOPS (Frontier: 1.194 EFLOPS).\n- ✅ **Amdahl's Law ceilings** make multi-core scaling hard; SIMD + algorithmic optimization are usually the first wins.\n\n> [!IMPORTANT]\n> **Final KTU Exam Mantra:** \"Performance is not just GHz.\" Always show the **full** $IC \\times CPI \\times T_{clk}$ decomposition in numerical answers. Mention **locality, Amdahl's Law, and 3 C's** whenever cache or parallelism appears. Examiners reward *systematic* reasoning over *hand-waving*.\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "cachemapping": {
      "title": "Cache Mapping",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 1\n> - **Topic:** Cache Mapping\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\n**Cache Mapping** is the deterministic mechanism by which a specific block (or line) of data from the slower, larger **Main Memory (RAM)** is placed, located, and retrieved within the faster, smaller, and more expensive **CPU Cache Memory**. Because the cache cannot hold the entire main memory, an algorithm must dictate exactly *where* in the cache a given main memory block will reside. This translation between a physical main memory address and a physical cache location is what we call the **Cache Mapping Function**.\n\nIn the KTU 2024 Scheme syllabus for High Performance Computing, cache mapping is classified into three foundational architectures:\n\n1. **Direct Mapping** – A strict, modular placement policy (each memory block has exactly one possible cache line).\n2. **Fully Associative Mapping** – A flexible, content-addressable policy (a memory block can be placed in *any* cache line).\n3. **Set-Associative Mapping** – A hybrid architecture (the cache is divided into *sets*, and a memory block maps to a specific set but can occupy any line within that set).\n\n> [!NOTE]\n> **Core Definition (KTU Board Standard):**\n> *\"Cache Mapping is the algorithmic procedure that determines the correspondence between the main memory address space and the physically smaller cache address space, governed by the constraints of locality of reference (temporal and spatial).\"*\n\n## 1.2 Conceptual Analogy — The \"Library Book Return System\"\n\nImagine a massive university library with **10,000 unique books** (Main Memory), but you only have a small personal reading desk with **10 slots** (Cache). When you return a book, the librarian must follow a strict policy:\n\n* **Direct Mapping (Modulo Policy):** The librarian calculates `Slot = Book_ID mod 10`. If Book ID is 257, it *must* go in Slot 7. Even if Slot 7 is full, the old book is kicked out. It is extremely fast to locate (just do the math), but causes constant \"collisions\" (called **Conflict Misses**).\n* **Fully Associative (Free Choice):** The librarian can place the book in *any* empty slot on your desk. It is the most flexible and eliminates conflict misses, but finding the book later requires checking all 10 slots (like a security camera search).\n* **Set-Associative (Departmental Grouping):** Your desk is divided into 2 \"mini-shelves\" (Sets) of 5 slots each. The librarian first calculates which mini-shelf the book belongs to (`Shelf = Book_ID mod 2`), but within that shelf, the book can go in any of the 5 slots. This is a balanced compromise used in modern Intel and AMD processors.\n\n> [!IMPORTANT]\n> **Physical Metrics at a Glance:**\n> * **Tag:** The high-order bits of the address that uniquely identify *which* block of memory is currently stored in a cache line.\n> * **Index:** The middle bits that select the specific cache set (or line in direct mapping).\n> * **Block Offset:** The low-order bits that select the specific byte within the cache block/line.\n> * **Word Size:** Usually **32 bits (4 Bytes)** or **64 bits (8 Bytes)** in modern architectures.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Physical Address Structure\n\nRegardless of the mapping technique, every main memory address generated by the CPU is conceptually divided into three logical partitions. Let the physical address be of size $A$ bits.\n\n$$A = \\text{Tag bits} + \\text{Index bits} + \\text{Block Offset bits}$$\n\n### 2.1.1 Block Offset Bits ($b$)\nThis field identifies the specific byte (or word) inside the cache block. It is purely a function of the block size.\n\n$$b = \\log_2(\\text{Block Size in Bytes})$$\n\n### 2.1.2 Index Bits ($i$)\n* In **Direct Mapping**, the index selects the exact cache line.\n* In **Set-Associative**, the index selects the exact set.\n* The number of index bits depends on the number of cache sets ($S$).\n\n$$i = \\log_2(S)$$\n\n### 2.1.3 Tag Bits ($t$)\nThe tag is whatever remains. It is stored alongside the data in the cache to verify whether the current line holds the requested memory block (i.e., it is a **Hit**) or a different block (a **Miss**).\n\n$$t = A - i - b$$\n\n## 2.2 Operational Logic of Each Mapping Scheme\n\n### 2.2.1 Direct Mapping\n* **How it works:** A memory block at address `M` is mapped to cache line `(M mod N)`, where $N$ is the total number of cache lines.\n* **Hardware Complexity:** Lowest. A single comparator and a multiplexer suffice.\n* **Drawback:** High conflict miss rate. If two frequently used blocks map to the same line, they constantly thrash (evict each other).\n\n### 2.2.2 Fully Associative Mapping\n* **How it works:** A memory block can be placed in *any* of the $N$ cache lines.\n* **Hardware Complexity:** Highest. Requires a **Content Addressable Memory (CAM)** where all tags are searched in parallel.\n* **Advantage:** Zero conflict misses. Ideal for small caches.\n* **Drawback:** Power-hungry and expensive to scale.\n\n### 2.2.3 Set-Associative Mapping\n* **How it works:** The cache is organized into $S$ sets, each containing $K$ lines ($K$ is the **associativity** or **way**). A memory block maps to set `(M mod S)`, but can occupy any of the $K$ lines within that set.\n* **Hardware Complexity:** Moderate. Requires $K$ parallel comparators per set.\n* **Standard Variants:** $K=2$ (2-way), $K=4$ (4-way), $K=8$ (8-way), $K=16$ (16-way).\n* **Most Common:** Modern L1/L2/L3 caches use **8-way or 16-way set-associative** mapping.\n\n> [!IMPORTANT]\n> **Why \"Why\" Matters for KTU Examiners:**\n> Set-associative mapping exists because of the **Principle of Compromise**. Direct mapping is too rigid (high miss rate), and fully associative is too expensive (high latency, high power). Set-associative offers $K$ \"guess chances\" per lookup, drastically reducing conflict misses while keeping the hardware search space bounded to $K$ comparators.\n\n## 2.3 Performance Formulas — KTU Cheat Sheet\n\n| Parameter | Formula | Engineering Meaning |\n| :--- | :--- | :--- |\n| **Offset Bits** | $b = \\log_2(B)$ | $B$ = Block size in bytes |\n| **Index Bits (Direct)** | $i = \\log_2(N)$ | $N$ = Total number of cache lines |\n| **Index Bits (Set-Associative)** | $i = \\log_2(S)$ | $S$ = Number of sets = $N / K$ |\n| **Tag Bits** | $t = A - i - b$ | $A$ = Physical address width |\n| **Number of Sets** | $S = \\dfrac{\\text{Cache Size}}{B \\times K}$ | $K$ = Associativity (ways) |\n| **Total Cache Lines** | $N = S \\times K$ | Product of sets and ways |\n| **Average Memory Access Time (AMAT)** | $\\text{AMAT} = T_h + (M_r \\times T_m)$ | $T_h$ = Hit time, $M_r$ = Miss rate, $T_m$ = Miss penalty |\n| **Miss Rate** | $M_r = 1 - \\text{Hit Rate}$ | Ratio of misses to total accesses |\n| **Cache Capacity** | $C = N \\times B \\times W$ | $W$ = Word/byte width |\n\n## 2.4 Real-World Engineering Utility\n\n* **CPU Microarchitecture:** Intel Core i9 and AMD Ryzen processors use a hierarchy of 8-way to 16-way set-associative L1/L2/L3 caches to balance latency and hit rate.\n* **GPU Texture Units:** Use highly associative caches to handle non-coalesced memory access patterns.\n* **TLB (Translation Lookaside Buffer):** A fully associative cache that maps virtual page numbers to physical frame numbers.\n* **Database Engines:** Buffer pool managers use set-associative logic to map table pages to in-memory buffer frames.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Exhaustive Derivation: Address Partitioning for a Set-Associative Cache\n\n**Problem Statement (Model KTU Question):**\nA processor has a **32-bit physical address** space. The L1 cache is **32 KB** in size, with a **block size of 64 Bytes** and is **4-way set-associative**. Determine the number of Tag bits, Index bits, and Block Offset bits, and compute the total size of the tag directory.\n\n---\n\n### Step 1: Compute the Block Offset Bits\n\nThe block size is $B = 64$ Bytes. The offset must uniquely address every byte within the block.\n\n$$\n\\begin{aligned}\nb &= \\log_2(B) \\\\\n  &= \\log_2(64) \\\\\n  &= \\log_2(2^6) \\\\\n  &= 6 \\text{ bits}\n\\end{aligned}\n$$\n\n**Logic:** 6 bits are required to count from `0` to `63`, covering all 64 bytes in the block.\n\n---\n\n### Step 2: Compute the Total Number of Cache Lines\n\nCache size $C = 32$ KB $= 32 \\times 1024$ Bytes $= 32768$ Bytes. Each line holds 64 Bytes.\n\n$$\n\\begin{aligned}\nN &= \\frac{C}{B} \\\\\n  &= \\frac{32768}{64} \\\\\n  &= 512 \\text{ lines}\n\\end{aligned}\n$$\n\n---\n\n### Step 3: Compute the Number of Sets\n\nThe cache is 4-way set-associative, so each set contains $K = 4$ lines.\n\n$$\n\\begin{aligned}\nS &= \\frac{N}{K} \\\\\n  &= \\frac{512}{4} \\\\\n  &= 128 \\text{ sets}\n\\end{aligned}\n$$\n\n---\n\n### Step 4: Compute the Index Bits\n\nThe index must uniquely select one of the 128 sets.\n\n$$\n\\begin{aligned}\ni &= \\log_2(S) \\\\\n  &= \\log_2(128) \\\\\n  &= \\log_2(2^7) \\\\\n  &= 7 \\text{ bits}\n\\end{aligned}\n$$\n\n---\n\n### Step 5: Compute the Tag Bits\n\nThe physical address is $A = 32$ bits.\n\n$$\n\\begin{aligned}\nt &= A - i - b \\\\\n  &= 32 - 7 - 6 \\\\\n  &= 19 \\text{ bits}\n\\end{aligned}\n$$\n\n---\n\n### Step 6: Compute the Tag Directory Storage Overhead\n\nIn addition to the data (32 KB = 32768 Bytes), the cache must store the 19-bit tag, a 1-bit **Valid Bit** (V), and a 1-bit **Dirty Bit** (D) for each line.\n\n$$\n\\begin{aligned}\n\\text{Overhead per line} &= t + V + D = 19 + 1 + 1 = 21 \\text{ bits} \\\\\n\\text{Total overhead} &= N \\times 21 = 512 \\times 21 = 10752 \\text{ bits} \\\\\n\\text{Total overhead in Bytes} &= \\frac{10752}{8} = 1344 \\text{ Bytes}\n\\end{aligned}\n$$\n\n---\n\n### Step 7: Final Verification of the Address Format\n\nThe 32-bit physical address is logically partitioned as:\n\n$$\n\\boxed{\\underbrace{\\text{Tag}}_{19 \\text{ bits}} \\;\\; \\underbrace{\\text{Index}}_{7 \\text{ bits}} \\;\\; \\underbrace{\\text{Offset}}_{6 \\text{ bits}}}\n$$\n\n> [!IMPORTANT]\n> **Sanity Check:** $19 + 7 + 6 = 32$ bits. ✓ Matches the physical address width.\n\n## 3.2 Analytical Matrix: Comparative Analysis of the Three Schemes\n\n| Feature | Direct Mapping | Fully Associative | Set-Associative ($K$-way) |\n| :--- | :--- | :--- | :--- |\n| **Placement Rule** | One specific line | Any line | Specific set, any line in set |\n| **Index Bits ($i$)** | $\\log_2(N)$ | $0$ | $\\log_2(N/K)$ |\n| **Tag Bits ($t$)** | $A - \\log_2(N) - b$ | $A - b$ | $A - \\log_2(N/K) - b$ |\n| **Comparators Needed** | $1$ | $N$ | $K$ (per set) |\n| **Conflict Misses** | Highest | Zero | Low (scales with $K$) |\n| **Hardware Cost** | Lowest | Highest | Moderate |\n| **Search Latency** | Lowest | Highest | Moderate |\n| **Power Consumption** | Lowest | Highest | Moderate |\n| **Scalability** | Excellent | Poor | Excellent |\n| **Modern Usage** | Rare (TLB tags) | L1 ITLB (small) | L1/L2/L3 Data Caches |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Address Decoding Flow (Direct Mapping)\n\nThis schematic illustrates how a 32-bit physical address is decoded by the cache controller to locate a specific byte in a direct-mapped cache.\n\n```mermaid\nflowchart TD\n    A[\"32-bit Physical Address Arrives at Cache Controller\"] --> B[\"Split into 3 Logical Fields\"]\n    B --> C1[\"Tag Field: 19 bits upper\"]\n    B --> C2[\"Index Field: 7 bits middle\"]\n    B --> C3[\"Offset Field: 6 bits lower\"]\n\n    C2 --> D[\"Use Index to select one specific cache line from 128 lines\"]\n    C1 --> E[\"Read the 19-bit stored Tag from the selected line\"]\n    E --> F{\"Compare Incoming Tag with Stored Tag bit by bit\"}\n\n    C3 --> G[\"Read 64-byte block into a 64-to-1 multiplexer\"]\n    F -- \"MATCH and Valid Bit is 1\" --> H[\"CACHE HIT: Forward data to CPU\"]\n    F -- \"MISMATCH or Valid Bit is 0\" --> I[\"CACHE MISS: Initiate Main Memory fetch and stall CPU pipeline\"]\n\n    G --> H\n\n    style A fill:#E0F2FE,stroke:#0369A1,stroke-width:2px\n    style H fill:#DCFCE7,stroke:#15803D,stroke-width:2px\n    style I fill:#FEE2E2,stroke:#B91C1C,stroke-width:2px\n    style F fill:#FEF3C7,stroke:#B45309,stroke-width:2px\n```\n\n## 4.2 Set-Associative Cache Architecture (4-Way Block Diagram)\n\nThis block-level architecture flow shows how a 4-way set-associative cache performs a parallel tag lookup.\n\n```mermaid\nflowchart LR\n    CPU[\"CPU Core Issues Load Request\"] --> ADDR[\"32-bit Physical Address Bus\"]\n\n    ADDR --> OFFSET[\"Offset: 6 bits - Byte position within block\"]\n    ADDR --> INDEX[\"Index: 7 bits - Set selector\"]\n    ADDR --> TAG[\"Tag: 19 bits - Block identifier\"]\n\n    INDEX --> DECODER[\"7 to 128 Set Decoder\"]\n\n    DECODER --> SET_GROUP[\"Selected Set: 4 cache lines activated simultaneously\"]\n\n    SET_GROUP --> WAY0[\"Way 0: Tag0 and Data0 registers\"]\n    SET_GROUP --> WAY1[\"Way 1: Tag1 and Data1 registers\"]\n    SET_GROUP --> WAY2[\"Way 2: Tag2 and Data2 registers\"]\n    SET_GROUP --> WAY3[\"Way 3: Tag3 and Data3 registers\"]\n\n    TAG --> COMP0[\"Comparator 0\"]\n    TAG --> COMP1[\"Comparator 1\"]\n    TAG --> COMP2[\"Comparator 2\"]\n    TAG --> COMP3[\"Comparator 3\"]\n\n    WAY0 --> COMP0\n    WAY1 --> COMP1\n    WAY2 --> COMP2\n    WAY3 --> COMP3\n\n    COMP0 -- \"Match found\" --> MUX[\"4-to-1 Output Multiplexer\"]\n    COMP1 -- \"Match found\" --> MUX\n    COMP2 -- \"Match found\" --> MUX\n    COMP3 -- \"Match found\" --> MUX\n\n    OFFSET --> MUX\n    MUX --> CPUOUT[\"Selected Byte forwarded to CPU in 1 cycle\"]\n\n    style CPU fill:#DBEAFE,stroke:#1E40AF,stroke-width:2px\n    style CPUOUT fill:#DCFCE7,stroke:#15803D,stroke-width:2px\n    style DECODER fill:#FEF3C7,stroke:#B45309,stroke-width:2px\n    style MUX fill:#E0E7FF,stroke:#4338CA,stroke-width:2px\n```\n\n## 4.3 Mapping Policy Comparison Flowchart\n\n```mermaid\nflowchart TD\n    START[\"Memory Block M arrives at Cache\"] --> POLICY{\"What Mapping Policy is used?\"}\n\n    POLICY -- \"Direct Mapping\" --> D1[\"Compute Line Number = M mod N\"]\n    D1 --> D2[\"Place block in that exact line. If occupied, evict the old block.\"]\n    D2 --> D3[\"Result: Fast but high conflict miss rate\"]\n\n    POLICY -- \"Fully Associative\" --> F1[\"Search all N lines for an empty slot\"]\n    F1 --> F2[\"Place block in any free line. If full, use LRU replacement policy.\"]\n    F2 --> F3[\"Result: Flexible but requires N parallel comparators\"]\n\n    POLICY -- \"Set Associative\" --> S1[\"Compute Set Number = M mod S\"]\n    S1 --> S2[\"Search only the K lines within that specific set\"]\n    S2 --> S3[\"Place block in any free line of the set. Evict via LRU if full.\"]\n    S3 --> S4[\"Result: Balanced trade-off between speed and flexibility\"]\n\n    style START fill:#E0F2FE,stroke:#0369A1,stroke-width:2px\n    style D3 fill:#FEE2E2,stroke:#B91C1C,stroke-width:1px\n    style F3 fill:#DCFCE7,stroke:#15803D,stroke-width:1px\n    style S4 fill:#FEF3C7,stroke:#B45309,stroke-width:1px\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Question 1 [KTU University Exam - Dec 2023]\n**Differentiate between Direct Mapping and Associative Mapping in cache memory.**\n\n**Model Answer (3 Marks):**\n\n| Aspect | Direct Mapping | Associative Mapping |\n| :--- | :--- | :--- |\n| **Placement** | Each memory block maps to exactly one specific cache line using the formula `(Block Address) mod (Number of Cache Lines)`. | Each memory block can be placed in *any* available cache line. |\n| **Search Method** | Uses the index bits to directly locate the line; only one tag comparison is needed. | Requires a parallel search of all cache tags using a Content Addressable Memory (CAM). |\n| **Hardware Complexity** | Low (one comparator and one multiplexer). | High (requires $N$ comparators operating in parallel). |\n| **Conflict Misses** | High (two blocks with the same index keep evicting each other). | Zero (any free line can be used). |\n\n*(1 mark for the placement rule, 1 mark for search/hardware, 1 mark for the miss rate comparison.)*\n\n### Question 2 [KTU University Exam - May 2024]\n**What is the role of the Tag bits in a cache memory system? How is the number of tag bits calculated?**\n\n**Model Answer (3 Marks):**\n\n* **Role:** Tag bits uniquely identify *which* main memory block is currently stored in a particular cache line. When the CPU generates an address, the tag bits of the incoming address are compared with the stored tag bits in the selected cache line. A **match** indicates a **Cache Hit**; a **mismatch** indicates a **Cache Miss**, requiring a fetch from the main memory.\n\n* **Formula:**\n$$t = A - i - b$$\nwhere $A$ is the total physical address width (in bits), $i$ is the number of index bits, and $b$ is the number of block offset bits.\n\n*(1.5 marks for role explanation, 1.5 marks for the formula and its interpretation.)*\n\n---\n\n## 5.2 Part B Questions (14 Marks Each — Internal Choice)\n\n> [!NOTE]\n> **KTU 2024 Evaluation Rule:** Part B questions carry 14 marks. Typically divided into sub-part (a) of 7 marks and sub-part (b) of 7 marks. Students are expected to provide a complete derivation, block diagrams, and final numerical answers with units.\n\n### Question A (Choice 1) [KTU University Exam - Dec 2022]\n\n**(a) [7 Marks]** A computer system uses a 32-bit physical address. The cache has a capacity of 16 KB, a block size of 8 words (assuming 1 word = 4 Bytes), and is **Direct Mapped**. Calculate the number of:\n* (i) Offset bits\n* (ii) Index bits\n* (iii) Tag bits\n* (iv) Total tag directory size (including a Valid bit for each line)\n\n**(b) [7 Marks]** Explain with a neat block diagram how the address is decoded in a direct-mapped cache. Also, discuss why direct mapping suffers from conflict misses even when the cache is not full.\n\n---\n\n**Model Solution for Part (a):**\n\n**Given:** Physical address $A = 32$ bits, Cache size $C = 16$ KB, Block size $B = 8 \\text{ words} \\times 4 \\text{ Bytes/word} = 32$ Bytes.\n\n**Step 1: Compute the Block Offset bits.**\n$$\n\\begin{aligned}\nb &= \\log_2(B) \\\\\n  &= \\log_2(32) \\\\\n  &= \\log_2(2^5) \\\\\n  &= 5 \\text{ bits}\n\\end{aligned}\n$$\n\n**Step 2: Compute the total number of cache lines.**\n$$\n\\begin{aligned}\nN &= \\frac{C}{B} \\\\\n  &= \\frac{16 \\times 1024}{32} \\\\\n  &= 512 \\text{ lines}\n\\end{aligned}\n$$\n\n**Step 3: Compute the Index bits.**\n$$\n\\begin{aligned}\ni &= \\log_2(N) \\\\\n  &= \\log_2(512) \\\\\n  &= \\log_2(2^9) \\\\\n  &= 9 \\text{ bits}\n\\end{aligned}\n$$\n\n**Step 4: Compute the Tag bits.**\n$$\n\\begin{aligned}\nt &= A - i - b \\\\\n  &= 32 - 9 - 5 \\\\\n  &= 18 \\text{ bits}\n\\end{aligned}\n$$\n\n**Step 5: Compute the total tag directory storage.**\nEach line requires an 18-bit Tag and 1 Valid bit, totaling 19 bits of metadata.\n$$\n\\begin{aligned}\n\\text{Directory Size} &= N \\times (t + V) \\\\\n  &= 512 \\times 19 \\\\\n  &= 9728 \\text{ bits} \\\\\n  &= \\frac{9728}{8} \\text{ Bytes} \\\\\n  &= 1216 \\text{ Bytes}\n\\end{aligned}\n$$\n\n**Final Address Structure:**\n$$\\boxed{\\underbrace{\\text{Tag}}_{18} \\;\\; \\underbrace{\\text{Index}}_{9} \\;\\; \\underbrace{\\text{Offset}}_{5}}$$\n\n---\n\n**Model Solution for Part (b):**\n\nThe address decoding in a direct-mapped cache follows three concurrent steps:\n1. The **Index** field activates one specific decoder output, selecting exactly one cache line out of $N$.\n2. The stored **Tag** of the selected line is fed into a comparator, where it is matched against the incoming **Tag** from the CPU.\n3. If the tags match AND the Valid bit is set to 1, a **Hit** signal is asserted. The **Offset** field then drives a multiplexer to extract the exact byte/word requested by the CPU.\n\n**Why conflict misses occur even in an unfilled cache:**\nIn direct mapping, the placement of a memory block is determined by the modulo operation: `Cache Line = (Block Address) mod N`. This means that two memory blocks that differ only by a multiple of $N$ will *always* map to the same cache line. If the program alternates accesses between Block 0 and Block $N$ (for example, an array stride problem where the program jumps through memory in steps of exactly $N$ blocks), the cache will constantly evict one block to make room for the other, even if the remaining 511 lines of the cache are completely empty. This is the essence of the **3 C's Model** (Compulsory, Capacity, Conflict) and is the primary motivation for set-associative and fully associative designs.\n\n---\n\n### Question B (Choice 2) [KTU University Exam - May 2023]\n\n**(a) [7 Marks]** Consider a **2-way set-associative** cache with the following specifications: Physical address = 32 bits, Cache size = 32 KB, Block size = 64 Bytes. Calculate the number of Tag bits, Index bits, and Offset bits. Also, determine the number of comparators required in this design.\n\n**(b) [7 Marks]** Compare Direct, Fully Associative, and Set-Associative mapping techniques. Justify why modern high-performance processors (e.g., Intel Core i7) prefer multi-way set-associative L1 caches.\n\n---\n\n**Model Solution for Part (a):**\n\n**Given:** $A = 32$ bits, $C = 32$ KB, $B = 64$ Bytes, $K = 2$ ways.\n\n**Step 1: Compute the Offset bits.**\n$$\n\\begin{aligned}\nb &= \\log_2(64) = 6 \\text{ bits}\n\\end{aligned}\n$$\n\n**Step 2: Compute the number of cache lines.**\n$$\n\\begin{aligned}\nN &= \\frac{32 \\times 1024}{64} = 512 \\text{ lines}\n\\end{aligned}\n$$\n\n**Step 3: Compute the number of sets.**\n$$\n\\begin{aligned}\nS &= \\frac{N}{K} = \\frac{512}{2} = 256 \\text{ sets}\n\\end{aligned}\n$$\n\n**Step 4: Compute the Index bits.**\n$$\n\\begin{aligned}\ni &= \\log_2(256) = \\log_2(2^8) = 8 \\text{ bits}\n\\end{aligned}\n$$\n\n**Step 5: Compute the Tag bits.**\n$$\n\\begin{aligned}\nt &= A - i - b = 32 - 8 - 6 = 18 \\text{ bits}\n\\end{aligned}\n$$\n\n**Step 6: Compute the comparators required.**\nIn a 2-way set-associative cache, when the index selects a set, *both* ways within that set must have their tags compared in parallel to determine a hit.\n$$\n\\begin{aligned}\n\\text{Comparators per set} &= K = 2 \\\\\n\\text{Total comparators} &= S \\times K = 256 \\times 2 = 512 \\text{ comparators}\n\\end{aligned}\n$$\n\n*(Note: While there are 512 comparators in total, only 2 are active per access. The remaining 510 are power-gated.)*\n\n**Final Address Structure:**\n$$\\boxed{\\underbrace{\\text{Tag}}_{18} \\;\\; \\underbrace{\\text{Index}}_{8} \\;\\; \\underbrace{\\text{Offset}}_{6}}$$\n\n---\n\n**Model Solution for Part (b):**\n\n| Feature | Direct Mapping | Fully Associative | Set-Associative |\n| :--- | :--- | :--- | :--- |\n| **Search Speed** | Fastest (1 comparator) | Slowest ($N$ comparators in parallel) | Moderate ($K$ comparators) |\n| **Flexibility** | Lowest (1 placement option) | Highest ($N$ options) | Balanced ($K$ options per set) |\n| **Miss Rate** | High (conflict misses) | Lowest (no conflict misses) | Low (conflict misses reduced) |\n| **Power Cost** | Lowest | Highest | Moderate |\n\n**Why Intel Core i7 prefers Multi-Way Set-Associative L1 Caches:**\n\n1. **Hardware Latency Constraint:** L1 caches must complete a lookup in roughly 4 CPU clock cycles (or ~1 ns at 4 GHz). A fully associative search across thousands of lines is physically impossible to complete in this time. Set-associativity limits the search to $K$ parallel comparators (typically $K=8$ in Core i7), keeping the access latency within the strict L1 timing budget.\n\n2. **Conflict Miss Reduction:** Real-world programs (compilers, databases, scientific loops) frequently access memory in patterns that cause severe thrashing in direct-mapped caches (e.g., accessing two arrays that happen to map to the same modulo class). An 8-way design provides 8 \"guesses\" per index, statistically eliminating most conflict misses.\n\n3. **Manufacturing Yield and Power:** Scaling to a 16-way or 32-way associativity yields diminishing returns in hit rate but doubles the comparator power budget. The industry has empirically converged on 8-way as the sweet spot.\n\n4. **Pipeline Integration:** Set-associative lookups can be pipelined across stages (index decode in stage 1, tag compare in stage 2, data mux in stage 3), integrating seamlessly with the superscalar out-of-order execution engine of modern CPUs.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> 1. **Tag Directory vs. Cache Size:** Students often confuse the **cache data capacity** (e.g., 32 KB) with the **tag directory size** (e.g., 1344 Bytes). You must add the tag overhead to the data size for the true silicon footprint of the cache.\n> 2. **Word vs. Byte Block Size:** If the question says *\"block size = 8 words\"* and the architecture is 32-bit (4 bytes/word), the *actual* block size in bytes is $8 \\times 4 = 32$ Bytes. Many students write `8` directly into the $\\log_2$ formula and lose 3 marks.\n> 3. **Modulo Direction:** Direct mapping uses `(Block Address) mod (Number of Lines)`. Fully associative has *no* modulo. Set-associative uses `(Block Address) mod (Number of Sets)`. Mixing these up is a guaranteed 0 for the derivation.\n> 4. **Comparators Counting:** For a $K$-way set-associative cache, the number of *active* comparators per access is $K$, not $S \\times K$. The latter is the total hardware count.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember (Rapid Revision Checklist)\n\n* ✅ **Cache Mapping** is the algorithm that governs *where* a main memory block is placed inside the smaller cache.\n* ✅ **Address Partition Formula:** $A = \\text{Tag} + \\text{Index} + \\text{Offset}$. This is the single most important equation in Module 1.\n* ✅ **Offset Bits:** $b = \\log_2(\\text{Block Size in Bytes})$.\n* ✅ **Index Bits:** $i = \\log_2(\\text{Number of Sets})$. For direct mapping, sets = lines. For set-associative, sets = lines $\\div$ ways.\n* ✅ **Tag Bits:** $t = A - i - b$. Always calculate this *last*.\n* ✅ **Direct Mapping:** Fastest hardware, highest conflict miss rate. Formula: `Line = (Block Address) mod N`.\n* ✅ **Fully Associative:** Most flexible, zero conflict misses, requires parallel tag comparison using a CAM.\n* ✅ **Set-Associative:** The industry standard. Best balance of speed, flexibility, and cost. Modern L1 caches are typically **8-way** set-associative.\n* ✅ **Comparators:** $K$ comparators per set for $K$-way set-associative; $N$ for fully associative; $1$ for direct mapped.\n* ✅ **AMAT Formula:** $\\text{AMAT} = T_h + (M_r \\times T_m)$ — critical for all performance-based questions.\n* ✅ **3 C's Model of Misses:** **Compulsory** (first access), **Capacity** (cache too small), **Conflict** (collision due to mapping policy). Set-associativity only addresses *Conflict Misses*.\n* ✅ **Valid Bit:** A 1-bit flag in every cache line indicating whether the line holds valid data or contains garbage from power-on.\n* ✅ **Dirty Bit:** A 1-bit flag in write-back caches indicating whether the line has been modified relative to main memory.\n* ✅ **Block Size Trade-off:** Larger blocks improve **spatial locality** (fewer compulsory misses) but worsen **pollution** (fetching unused data) and reduce the number of available lines (more capacity misses).\n* ✅ **Spatial Locality:** Programs tend to access memory addresses that are physically close to recently accessed addresses (justifies larger block sizes).\n* ✅ **Temporal Locality:** Programs tend to re-access the same memory locations within short time windows (justifies keeping recently used blocks in the cache).\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "prefetch": {
      "title": "Prefetch",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 1\n> - **Topic:** Prefetch\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n> [!NOTE]\n> **Prefetching** is a *latency-hiding* technique in high-performance computing architectures whereby the system speculatively retrieves **data, instructions, or I/O blocks** from a *slower* level of the memory hierarchy (main memory, disk, or remote node) into a *faster* level (cache, local buffer, or local memory) **before** the processor or application explicitly demands it.\n\nIn the KTU 2024 Scheme context (course code PECST757), prefetching is studied as a **Memory Hierarchy Optimization** technique and an **I/O Optimization** strategy for HPC workloads running on multi-core CPUs, accelerators (GPUs), and distributed clusters.\n\n> [!IMPORTANT]\n> **Key Terminology (Syllabus Highlight):**\n> - **Latency Hiding:** Masking the delay of a slow memory access by overlapping it with computation.\n> - **Speculative Access:** A guess about the future memory reference stream.\n> - **Prefetch Stream / Trail:** A sequence of cache lines or I/O blocks brought in by a single prefetch trigger.\n> - **Degree (d):** Number of cache lines (or blocks) fetched per prefetch request.\n> - **Distance / Prefetch Aheadness:** How early the prefetch is issued relative to the demand miss.\n\n## 1.2 Conceptual Analogy / Intuition\n\n> [!TIP]\n> **Real-world analogy — \"The Restaurant Waiter Analogy\" 🍽️**\n> Imagine you are eating in a restaurant. A *reactive waiter* only brings water when you ask for it (you wait while he goes to the kitchen). A *smart waiter* (prefetcher) notices you just finished your soup, and based on the pattern that 80% of customers order a drink after soup, he brings you the menu AND fetches a glass of water *before* you ask. By the time you reach for the drink, it is already on the table — **zero waiting**.\n>\n> The waiter was **speculative** (he guessed), he fetched in **parallel** (while you ate), and he **hid the latency** (you never felt the kitchen delay). If he guessed wrong, the extra water glass is wasted — just like a *cache pollution* caused by a wrong prefetch.\n\n## 1.3 Where Prefetching Sits in HPC\n\n| Layer | Slow Resource | Fast Destination | Prefetch Granularity |\n| :--- | :--- | :--- | :--- |\n| Hardware | Main Memory (DRAM) | L1 / L2 / L3 Cache | Cache line (64 B) |\n| I/O | HDD / SSD / NVMe | OS Page Cache | Disk block (4 KB – 256 KB) |\n| Distributed | Remote Node (MPI rank) | Local Rank's Memory | Message / Sub-array tile |\n| Web / File | Network File Server (NFS, Lustre) | Client-side buffer | File chunk |\n\nThe overarching goal in all four layers is identical: **bring the data closer, sooner, with minimal waste of bandwidth or cache capacity.**\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Operational Concept: The Prefetch Pipeline\n\nA prefetch operation is a six-stage logical pipeline. Understanding each stage is essential for KTU 14-mark derivations.\n\n1. **Trigger (Detection):** A *trigger event* activates the prefetcher. Common triggers:\n   - A cache miss on a particular address.\n   - A stride pattern detected in the miss address stream.\n   - A software hint (`prefetch` intrinsic in C/C++).\n   - A compiler-inserted preload instruction.\n   - An I/O library call (e.g., `MPI_File_iread`, `read-ahead`).\n2. **Prediction (Where to fetch):** The prefetcher computes a *target address*. For a **next-line prefetcher**, target = `miss_addr + line_size`. For a **stride prefetcher**, target = `last_addr + stride × line_size`.\n3. **Issue (Request generation):** A memory request is sent to the lower level. The request must not block the demand path; it is tagged as *non-critical*.\n4. **Transfer (Data movement):** The cache line / I/O block travels up the hierarchy. Bandwidth is a shared resource — prefetches compete with demand requests.\n5. **Placement (Where to store):** The line is placed in the cache. To avoid evicting useful data, it may go into a *stream buffer* or be marked as *low priority* in LRU.\n6. **Validation (Use or Discard):** When the processor finally requests the line, a *prefetch hit* is recorded. If never used, the line is *pollution*.\n\n## 2.2 Classification of Prefetchers (KTU-Favorite Diagram Topic)\n\n| Class | Who Decides | Granularity | Pros | Cons |\n| :--- | :--- | :--- | :--- | :--- |\n| **Hardware Prefetcher** | CPU / Memory Controller | Cache line (64 B) | Transparent, no code change | Limited pattern window, hard to verify |\n| **Software Prefetcher** | Compiler / Programmer (`__builtin_prefetch`) | Cache line / struct | Programmer has semantic knowledge | Pollutes instruction cache, programmer burden |\n| **Hybrid Prefetcher** | HW + SW cooperation | Cache line | Best accuracy | Complex coordination |\n| **I/O Read-Ahead** | OS / File System | Page / Block (4 KB+) | Hides disk latency | Wastes disk bandwidth |\n| **MPI / Distributed Prefetch** | Runtime / User code | Sub-array / Halo region | Overlaps network with compute | Extra memory footprint |\n\n## 2.3 KTU Formula Sheet / Cheat Sheet\n\n> [!IMPORTANT]\n> The following four equations are the **highest-yield formulas** for KTU ESE questions on prefetching. Memorise the variable names and units exactly.\n\n| # | Formula | Meaning of Each Term |\n| :--- | :--- | :--- |\n| **1. AMAT with Prefetch** | $\\text{AMAT} = h_{L1} \\cdot t_{L1} + (1-h_{L1})\\cdot t_{L1\\to L2}$ | $h_{L1}$ = L1 hit rate, $t_{L1}$ = L1 hit time |\n| **2. Prefetch Accuracy** | $A = \\dfrac{U}{U+W}$ | $U$ = Useful prefetches, $W$ = Wasted prefetches |\n| **3. Prefetch Coverage** | $C = \\dfrac{U}{U+N}$ | $N$ = Demand misses that prefetch **missed** |\n| **4. Polluted Misses** | $P = \\dfrac{W}{W + \\text{UsefulEvictions}}$ | Measures cache damage caused by prefetch |\n| **5. Effective L2 Latency** | $t_{\\text{eff}} = (1-C)\\cdot t_{L2} + C\\cdot t_{L2\\to L1}$ | $C$ is coverage; $t_{L2\\to L1}$ is L2 hit after prefetch |\n| **6. Timeliness Condition** | $T_{\\text{issue}} + T_{\\text{mem}} \\le T_{\\text{use}}$ | Prefetch must complete *before* demand access |\n\n> [!TIP]\n> **Engineering Utility:** These metrics are used in commercial CPUs (Intel IP-Prefetcher, AMD Stream Detector), GPU L2 prefetchers (NVIDIA Hopper, AMD CDNA), and HPC runtimes like OpenMP, MPI-IO, and HDF5.\n\n## 2.4 Hardware vs Software Prefetch — When to Use What\n\n* **Hardware prefetchers** dominate in *regular* workloads (matrix multiply, streaming kernels). They are useless for *pointer-chasing* (linked lists, B-trees) and *irregular* HPC codes (graph analytics, sparse solvers).\n* **Software prefetchers** (`#pragma GCC ivdep`, `__builtin_prefetch(addr, 0, 1)`) are essential for **irregular access patterns** where the programmer *knows* the access schedule that the hardware cannot infer.\n* In MPI applications, **prefetching halo regions** (ghost cells in stencil codes) is a textbook example of distributed prefetch.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: AMAT Improvement Due to Prefetching\n\nWe start from the classical **Average Memory Access Time (AMAT)** model, then introduce the *coverage* $C$ and *accuracy* $A$ of a prefetcher to quantify its benefit.\n\n### Step 1 — Base AMAT (no prefetch)\n\nFor a two-level cache (L1 → L2), the average access time is:\n\n$$\\text{AMAT}_{0} = h_{L1}\\cdot t_{L1} + (1-h_{L1})\\cdot\\bigl(t_{L1\\to L2} + (1-h_{L2})\\cdot t_{\\text{mem}}\\bigr)$$\n\n> **Logic:** A fraction $h_{L1}$ of accesses hit in L1 (cost $t_{L1}$). The rest go to L2. Of those, a fraction $h_{L2}$ hit in L2, the rest pay the DRAM penalty $t_{\\text{mem}}$.\n\n### Step 2 — Introduce Prefetch Coverage $C$\n\nA *coverage* of $C$ means a fraction $C$ of the L2-miss references will have been brought into L2 *before* the demand arrives. The effective L2 miss penalty is therefore:\n\n$$t_{\\text{eff}} = (1-C)\\cdot t_{L2} + C\\cdot t_{L2\\to L1}$$\n\n> **Logic:** If coverage is high, the demand usually finds the line already in L2, paying only the L2→L1 transfer time $t_{L2\\to L1}$ instead of the full DRAM round-trip.\n\n### Step 3 — Combine into New AMAT\n\n$$\\text{AMAT}_{\\text{pre}} = h_{L1}\\cdot t_{L1} + (1-h_{L1})\\cdot t_{L2}^{\\text{new}}$$\n\nwhere:\n\n$$t_{L2}^{\\text{new}} = t_{L1\\to L2} + (1-h_{L2})\\cdot t_{\\text{eff}}$$\n\n### Step 4 — Speedup Expression\n\nThe fractional reduction in AMAT is the **Speedup** $S$:\n\n$$S = \\frac{\\text{AMAT}_{0}}{\\text{AMAT}_{\\text{pre}}}$$\n\nSubstituting the two expressions above and simplifying (for the common case $h_{L1}\\ll 1$, $h_{L2}\\ll 1$):\n\n$$S \\;\\approx\\; 1 + (1-h_{L1})\\cdot(1-h_{L2})\\cdot C\\cdot\\frac{t_{\\text{mem}} - t_{L2\\to L1}}{t_{L1\\to L2} + t_{L2\\to L1}}$$\n\n> **Logic:** $S$ is monotonically increasing in $C$ (more coverage helps) and in $t_{\\text{mem}} - t_{L2\\to L1}$ (bigger DRAM-vs-L2 gap means more latency to hide). It decreases as $h_{L1}$ or $h_{L2}$ rises (less DRAM traffic to hide).\n\n## 3.2 Analytical Matrix — Worked Numerical Example (KTU-Style)\n\n> [!NOTE]\n> **Worked problem (model-style for ESE 14-marker):**\n> Given: $h_{L1} = 0.95$, $t_{L1} = 1$ ns, $t_{L1\\to L2} = 10$ ns, $h_{L2} = 0.70$, $t_{\\text{mem}} = 200$ ns, $t_{L2\\to L1} = 10$ ns, prefetch coverage $C = 0.40$. Compute the AMAT speedup.\n\n### Step A — Compute $\\text{AMAT}_{0}$ (no prefetch)\n\n$$\n\\begin{aligned}\n\\text{AMAT}_{0} &= 0.95 \\cdot 1 + 0.05 \\cdot \\bigl(10 + 0.30 \\cdot 200\\bigr) \\\\\n&= 0.95 + 0.05 \\cdot (10 + 60) \\\\\n&= 0.95 + 0.05 \\cdot 70 \\\\\n&= 0.95 + 3.5 \\\\\n&= 4.45 \\text{ ns}\n\\end{aligned}\n$$\n\n### Step B — Compute $t_{\\text{eff}}$ with coverage\n\n$$\n\\begin{aligned}\nt_{\\text{eff}} &= (1-0.40)\\cdot 200 + 0.40 \\cdot 10 \\\\\n&= 0.60\\cdot 200 + 4 \\\\\n&= 120 + 4 \\\\\n&= 124 \\text{ ns}\n\\end{aligned}\n$$\n\n### Step C — Compute $\\text{AMAT}_{\\text{pre}}$\n\n$$\n\\begin{aligned}\n\\text{AMAT}_{\\text{pre}} &= 0.95 \\cdot 1 + 0.05 \\cdot \\bigl(10 + 0.30 \\cdot 124\\bigr) \\\\\n&= 0.95 + 0.05 \\cdot (10 + 37.2) \\\\\n&= 0.95 + 0.05 \\cdot 47.2 \\\\\n&= 0.95 + 2.36 \\\\\n&= 3.31 \\text{ ns}\n\\end{aligned}\n$$\n\n### Step D — Compute Speedup\n\n$$\n\\begin{aligned}\nS &= \\frac{4.45}{3.31} \\approx 1.344\n\\end{aligned}\n$$\n\n> [!TIP]\n> **Result interpretation:** Even a modest 40% coverage reduces the AMAT by ~26%, yielding a **1.34× speedup** for the memory subsystem. In a real HPC stencil loop at 80% memory-bound utilisation, this roughly translates to a **1.20–1.25× wall-clock speedup**.\n\n## 3.3 Analytical Matrix — Prefetch Trade-off Triangle\n\nEvery prefetch design is constrained by three competing forces. A KTU 14-marker often asks you to *discuss* this trade-off.\n\n| Goal | Increase by… | Cost Paid |\n| :--- | :--- | :--- |\n| **Accuracy ↑** (fewer wasted lines) | Stride / Markov predictors | Hardware cost, complexity |\n| **Coverage ↑** (catch more misses) | Aggressive degree, larger stream buffer | Cache pollution, bandwidth |\n| **Timeliness ↓** (issue earlier) | Increase prefetch distance | Stale data, conflict misses |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Mermaid Diagram — The Prefetch Decision Pipeline\n\n```mermaid\nflowchart TD\n    A[CPU issues LOAD addr X] --> B{X hit in L1?}\n    B -- Yes --> C[Return data to CPU]\n    B -- No --> D[X miss in L1: send to L2]\n    D --> E{Prefetcher Triggered?}\n    E -- Yes --> F[Predict next address X prime]\n    E -- No --> G[Standard L2 lookup]\n    F --> H[Issue non critical prefetch to L2]\n    H --> I[Prefetch fills stream buffer or L2]\n    G --> J{X hit in L2?}\n    I --> J\n    J -- Yes --> K[Return to L1 and CPU]\n    J -- No --> L[DRAM access penalty T mem]\n    L --> M[Return to L2 then L1 then CPU]\n    K --> N[End]\n    M --> N\n```\n\n## 4.2 Mermaid Diagram — Hardware Stride Prefetcher (Functional Block)\n\n```mermaid\nflowchart LR\n    A[Reference Address Table RPT] --> B[Stride Detection Logic]\n    B --> C{Stride constant?}\n    C -- Yes --> D[Compute target addr plus stride]\n    C -- No --> E[Mark entry as no stride]\n    D --> F[Prefetch Request Queue]\n    F --> G[L2 Cache Controller]\n    G --> H[Cache Line placed in L2 or Stream Buffer]\n    H --> I{Demand arrives in time?}\n    I -- Yes --> J[Prefetch hit: useful]\n    I -- No --> K[Late prefetch: wasted or stale]\n```\n\n## 4.3 Mermaid Diagram — Distributed Prefetch in MPI Stencil Code\n\n```mermaid\nflowchart TB\n    subgraph Rank0[Rank 0 Node]\n        A0[Compute Inner Cells] --> B0[Non blocking MPI Irecv halo left]\n        B0 --> C0[Issue prefetch for next timestep halo]\n        C0 --> D0[Wait for actual data]\n    end\n    subgraph Rank1[Rank 1 Neighbour]\n        A1[Send halo to Rank 0] --> D0\n        A1 --> A1b[Compute inner cells]\n    end\n    D0 --> E0[Stencil update with prefetched halo]\n    E0 --> F0[Synchronise and repeat]\n```\n\n> [!NOTE]\n> **Interpretation:** The *non-blocking* `MPI_Irecv` + a software hint to the runtime to **prefetch the next iteration's halo region** is the canonical distributed prefetch pattern in HPC. The MPI runtime uses Remote Direct Memory Access (RDMA) to overlap network latency with compute.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Q1. [KTU University Exam – Dec 2023, Model Paper]\n**Define prefetching in the context of memory hierarchy. Mention any two types of prefetchers.**\n\n**Model Answer (3 marks):**\n> *Prefetching is a latency-hiding technique in which data or instructions are speculatively fetched from a lower (slower) level of the memory hierarchy into a higher (faster) level before they are demanded by the processor. (2 marks)*\n> *Two types: (i) Hardware prefetcher — present inside the memory controller, transparent to software; (ii) Software prefetcher — explicit hints inserted by the compiler or programmer (e.g., `__builtin_prefetch`). (1 mark)*\n\n### Q2. [KTU University Exam – June 2024, Sample]\n**Distinguish between prefetch accuracy and prefetch coverage. Why are both metrics needed?**\n\n**Model Answer (3 marks):**\n> *Accuracy = useful prefetches / (useful + wasted). It measures the fraction of issued prefetches that are actually used. (1.5 marks)*\n> *Coverage = useful prefetches / (useful + demand-missed-by-prefetcher). It measures the fraction of all misses that the prefetcher successfully catches. (1 mark)*\n> *Both are needed because a prefetcher may be 100% accurate (no pollution) but only catch 5% of misses (low coverage), OR catch 100% of misses (high coverage) but thrash the cache (low accuracy). A *good* prefetcher must balance both. (0.5 marks)*\n\n## 5.2 Part B Questions (14 Marks Each — Internal Choice)\n\n### Question A (14 Marks) — *[KTU University Exam – Dec 2023 Style]*\n\n> **(a)** With the help of a neat block diagram, explain the working of a **stream buffer-based hardware prefetcher**. Discuss the role of *degree* and *distance* parameters. **(7 marks)**\n\n**(a) Model Solution:**\n\n* **Definition (2 marks):** A *stream buffer* is a small FIFO placed adjacent to the L2 cache. When a cache miss occurs, the address is allocated into a stream buffer; the prefetcher then issues a series of *prefetch requests* for the next `d` (degree) cache lines, starting at the miss address plus a *distance* `D` ahead.\n\n* **Block diagram (3 marks):** *(Refer to the Mermaid diagram in Section 4.2 — Reference Address Table → Stride Detector → Prefetch Queue → L2 / Stream Buffer).*\n\n* **Role of parameters (2 marks):**\n  - **Degree $d$** — number of cache lines fetched per trigger. Higher $d$ = more coverage but more bandwidth.\n  - **Distance $D$** — how many lines *ahead* of the demand miss the prefetch starts. Larger $D$ = more time to hide latency but higher risk of staleness / pollution.\n\n> **(b)** A system has L1 hit rate 0.92, L1 hit time 1 ns, L2 access time 12 ns, L2 hit rate 0.60, and main memory access time 250 ns. A prefetcher with coverage 0.50 is introduced such that the effective L2 miss penalty becomes 130 ns. **(i)** Compute the AMAT *without* prefetch. **(ii)** Compute the AMAT *with* prefetch. **(iii)** Find the percentage improvement. **(7 marks)**\n\n**(b) Model Solution:**\n\n**(i) AMAT without prefetch (3.5 marks):**\n$$\n\\begin{aligned}\n\\text{AMAT}_{0} &= h_{L1}\\cdot t_{L1} + (1-h_{L1})\\cdot\\bigl(t_{L1\\to L2} + (1-h_{L2})\\cdot t_{\\text{mem}}\\bigr) \\\\\n&= 0.92\\cdot 1 + 0.08\\cdot\\bigl(12 + 0.40\\cdot 250\\bigr) \\\\\n&= 0.92 + 0.08\\cdot(12 + 100) \\\\\n&= 0.92 + 0.08\\cdot 112 \\\\\n&= 0.92 + 8.96 \\\\\n&= 9.88 \\text{ ns}\n\\end{aligned}\n$$\n\n**(ii) AMAT with prefetch (2 marks):**\n$$\n\\begin{aligned}\n\\text{AMAT}_{\\text{pre}} &= 0.92\\cdot 1 + 0.08\\cdot\\bigl(12 + 0.40\\cdot 130\\bigr) \\\\\n&= 0.92 + 0.08\\cdot(12 + 52) \\\\\n&= 0.92 + 0.08\\cdot 64 \\\\\n&= 0.92 + 5.12 \\\\\n&= 6.04 \\text{ ns}\n\\end{aligned}\n$$\n\n**(iii) Percentage improvement (1.5 marks):**\n$$\n\\begin{aligned}\n\\%\\text{Improvement} &= \\frac{9.88 - 6.04}{9.88}\\cdot 100 = \\frac{3.84}{9.88}\\cdot 100 \\approx 38.87\\%\n\\end{aligned}\n$$\n\n---\n\n### Question B (14 Marks) — *[KTU University Exam – June 2024 Style]*\n\n> **(a)** Explain the concept of **I/O prefetching (read-ahead)** as used in high-performance file systems such as Lustre and parallel HDF5. How does it differ from cache-line prefetching? **(7 marks)**\n\n**(a) Model Solution:**\n\n* **Concept (3 marks):** I/O prefetching, also called *read-ahead*, anticipates that sequential file accesses will continue. When the application issues a `read()` for file offset $o$, the OS or parallel file system *also* issues asynchronous prefetch reads for offsets $o + B, o + 2B, \\ldots$ where $B$ is the read-ahead block size. The blocks are placed in the OS page cache. Subsequent `read()` calls hit the cache.\n\n* **HPC specifics (2 marks):** In **Lustre** (parallel FS), each OST (Object Storage Target) client maintains a *readahead window* per file descriptor. In **parallel HDF5 / MPI-IO**, the collective I/O layer prefetches entire *chunks* (e.g., a hyperslab) so that when a compute rank begins processing, the data is already in its buffer.\n\n* **Differences from cache-line prefetching (2 marks):**\n  1. *Granularity:* I/O prefetch fetches pages (4 KB – 1 MB), not 64 B cache lines.\n  2. *Trigger source:* File-system hints, access-pattern history of the file, not CPU-side miss stream.\n  3. *Storage destination:* OS page cache or application buffer, not hardware cache.\n  4. *Failure cost:* A wasted I/O prefetch wastes disk *and* network bandwidth (in parallel FS), much costlier than a wasted cache-line prefetch.\n\n> **(b)** Discuss the **three-way trade-off** between prefetch accuracy, coverage, and timeliness with a suitable example. Justify why a *late* prefetch is worse than no prefetch. **(7 marks)**\n\n**(b) Model Solution:**\n\n* **Trade-off explanation (4 marks):**\n  - *Accuracy* avoids pollution (wasted cache lines evicted real demand data).\n  - *Coverage* ensures misses are caught (otherwise the prefetcher is useless).\n  - *Timeliness* ensures the prefetched line arrives *before* the demand reference.\n  - These three cannot be simultaneously maximised with a fixed bandwidth budget. Increasing the prefetch *degree* raises coverage but reduces accuracy. Increasing the *distance* improves timeliness but raises pollution (the data may be evicted before use).\n\n* **Numerical example (2 marks):** A next-line prefetcher with degree $d=2$ and distance $D=2$ on a matrix-transpose kernel: accuracy = 30% (transpose is non-sequential in the second loop), coverage = 95% (most inner-loop misses are caught), timeliness = 100% (sequential inner loop). The same prefetcher on a *linked-list traversal*: accuracy = 5%, coverage = 5%, timeliness = N/A. The trade-off is that the design that maximised coverage in case 1 is useless in case 2.\n\n* **Why late prefetch is worse than no prefetch (1 mark):** A late prefetch still consumes cache capacity and memory-bandwidth (hurting *other* concurrent demand accesses), yet provides no latency hiding. Net effect: AMAT *increases* compared with disabling the prefetcher.\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Common Pitfalls:**\n> 1. *Confusing \"cache hit\" with \"prefetch hit\":* a prefetch hit means the line was placed **by a prefetch**, not by a prior demand access. Examiners deduct 0.5–1 mark for this conflation.\n> 2. *Forgetting units:* $t_{\\text{mem}}$ is in **ns** (or cycles). Always state units in AMAT calculations.\n> 3. *Mixing up coverage $C$ and accuracy $A$:* Coverage is in the *numerator's* denominator as `demand misses missed`, accuracy uses `wasted prefetches`. Swap them and your AMAT expression becomes **physically wrong** (negatives appear).\n> 4. *Ignoring pollution:* A 14-mark answer that only computes speedup without discussing wasted bandwidth and eviction cost is marked down by 2–3 marks. Always add a concluding sentence on trade-offs.\n> 5. *Forgetting to convert hit rate to miss rate:* $(1 - h_{L1})$ is the *miss rate*, not $h_{L1}$. This is the #1 algebraic error in KTU scripts.\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Revision Checklist — Prefetch (Module 1)**\n\n* **Core idea:** *Fetch before it is needed, in parallel with computation, to hide slow-access latency.*\n* **Three pillar metrics:** *Accuracy* (right predictions / total), *Coverage* (misses caught / total misses), *Timeliness* (arrives before demand).\n* **Two main families:** *Hardware* (transparent, regular patterns) and *Software* (programmer hints, irregular patterns).\n* **I/O counterpart:** *Read-ahead* in page cache / parallel file systems; granularity is pages, not cache lines.\n* **Distributed counterpart:** *Non-blocking MPI communication* + prefetch of next-iteration halo regions via RDMA.\n* **The Six-Stage Pipeline:** Trigger → Predict → Issue → Transfer → Place → Validate.\n* **Golden formulas to memorise:**\n  - $\\text{AMAT} = h_{L1} t_{L1} + (1-h_{L1})\\cdot(t_{L1\\to L2} + (1-h_{L2}) t_{\\text{mem}})$\n  - $A = U / (U + W)$\n  - $C = U / (U + N)$\n  - $S = \\text{AMAT}_{0} / \\text{AMAT}_{\\text{pre}}$\n* **Timeliness condition:** $T_{\\text{issue}} + T_{\\text{mem}} \\le T_{\\text{use}}$\n* **Trade-off triangle:** ↑ Coverage ⇔ ↓ Accuracy (for fixed bandwidth); ↑ Distance ⇔ ↓ Timeliness reliability.\n* **Late prefetch is worse than no prefetch** because it costs bandwidth and cache capacity without hiding latency.\n* **Real-world examples to mention in answers:** Intel L2 IP-Prefetcher, AMD Stream Detector, NVIDIA L2 prefetcher, Lustre read-ahead, MPI-IO collective buffering, `__builtin_prefetch()` GCC intrinsic, OpenMP `simd` prefetch hints.\n* **One-line exam answer:** *\"Prefetching reduces AMAT by $C \\cdot (t_{\\text{mem}} - t_{L2\\to L1})$ provided the prefetcher is accurate and timely.\"*\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "multicoreprocessorsmultithreadedprocessorsvectorprocessorsdesignprinciplesmaximumperformanceestimatesprogrammingforvectorarchitectures": {
      "title": "Multicore Processors - Multithreaded Processors - Vector Processors - Design Principles - Maximum Performance Estimates - Programming for Vector Architectures",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 1\n> - **Topic:** Multicore Processors - Multithreaded Processors - Vector Processors - Design Principles - Maximum Performance Estimates - Programming for Vector Architectures\n\n<!-- SECTION_1_START -->\n# SECTION 1 — Core Technical Definition & Intuitive Overview\n\n## 1.1 Multicore Processors\n\n### Formal Definition\nA **Multicore Processor** is a single computing component (a single physical integrated circuit / die) that contains **two or more independent processing units (cores)** that read and execute program instructions simultaneously. Each core typically has its own **L1 cache**, may share an **L2 cache**, and shares access to main memory via a coherent interconnect (e.g., Intel QuickPath Interconnect, AMD Infinity Fabric).\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Definition:** *A multicore processor is a single processor chip that contains multiple processing units (cores) which execute multiple instruction streams independently, sharing on-chip resources such as last-level cache, memory controllers, and I/O subsystems to improve throughput while staying within power, thermal, and pin-bandwidth constraints.*\n\n### Conceptual Analogy — The Restaurant Kitchen\nImagine a single chef (single core) trying to cook 100 dishes. He can only do one task at a time — chop, stir, plate. Now replace him with **4 chefs sharing one kitchen, one pantry, and one delivery window**. Each chef cooks independently, but they share the same refrigerator (LLC cache) and the same service counter (memory bus). This is exactly what happens on a **quad-core processor** — four instruction streams running in parallel, sharing last-level cache and memory bandwidth.\n\n### Key Technical Metrics (Must Remember)\n- **Core Count (n):** The number of independent processing units on a single die.\n- **Threads per Core (TPC):** Number of hardware threads supported per physical core.\n- **Base Clock (f):** Operating frequency in **GHz** (typically 2.0–5.0 GHz).\n- **TDP (Thermal Design Power):** Power envelope in **Watts (W)**.\n- **Die Area (A):** Measured in **mm²**.\n- **Transistor Count:** Number of transistors on the chip (modern CPUs: > **50 Billion**).\n- **Inter-core Latency:** Typically **20–50 ns** for L2/L3, **60–100 ns** for DRAM.\n\n### Why Multicore? — The Power Wall\n> [!NOTE]\n> Around **2004–2005**, single-core CPU clock speeds hit the **\"Power Wall\"** — Dennard Scaling broke down. Power dissipation P ∝ C · V² · f. Increasing frequency f caused cubic-to-quadratic power growth and unacceptable heat. Industry pivoted from \"faster single core\" to \"more slower cores\" — this is the **Multicore Era**.\n\n---\n\n## 1.2 Multithreaded Processors\n\n### Formal Definition\n**Multithreading** is a hardware mechanism that allows a **single physical core** to maintain the state of **multiple hardware threads (contexts)** so that when one thread stalls (e.g., on a cache miss), the processor can **switch to another ready thread in zero cycles**, hiding latency and improving core utilization.\n\n### Three Classical Variants (KTU High-Yield)\n| Variant | Switch Trigger | Throughput Gain | Latency Cost |\n|---|---|---|---|\n| **Fine-Grained MT** | Every instruction (round-robin) | High | High |\n| **Coarse-Grained MT** | On L2/expensive stall | Moderate | Moderate |\n| **Simultaneous MT (SMT / Hyper-Threading)** | Issue from multiple threads same cycle | Highest | Lowest |\n\n> [!IMPORTANT]\n> **SMT (Simultaneous Multithreading)** is the modern dominant technique. It duplicates only the **architectural state** (registers, PC, renaming tables) while sharing the **execution units**, caches, and pipelines. Example: Intel's **Hyper-Threading Technology** is a form of **2-way SMT**.\n\n### Conceptual Analogy — The Single-Person Barber Shop\nA barber (single core) without multithreading: if a customer's hair color requires dye, the barber stops and waits. With **coarse-grained multithreading**, when dye is needed, the barber immediately moves to the next customer. With **SMT**, the barber works on **two customers simultaneously** — cutting one customer's hair while another waits for dye to process. Both progress in the same clock cycle.\n\n---\n\n## 1.3 Vector Processors\n\n### Formal Definition\nA **Vector Processor** is a CPU architecture designed to execute a single **vector instruction** on **multiple data elements** (a vector of operands) in parallel using dedicated **vector registers** and **vector functional units**. It is the hardware embodiment of the **SIMD (Single Instruction, Multiple Data)** paradigm at the architectural level.\n\n> [!NOTE]\n> Vector processors were the dominant supercomputer architecture from the **1970s to 1990s** (Cray-1, Cray-2, NEC SX series). Modern x86 CPUs and GPUs descend from these principles through **SIMD extensions**: SSE, AVX, AVX-512 (Intel), and NEON (ARM).\n\n### Conceptual Analogy — The Industrial Laundry Machine\nA scalar processor washes **one shirt per load**. A vector processor installs a **huge industrial drum** that washes **64 shirts simultaneously** using a **single \"wash-instruction\"**. The control overhead is amortized over many data elements — this is **\"amortizing the instruction overhead\"**, the central performance idea of vector processing.\n\n### Core Vector Architecture Components\n- **Vector Registers:** Wide registers (e.g., 512 bits = 16 × 32-bit elements or 8 × 64-bit elements).\n- **Vector Functional Units:** Pipelined units (Vector Add, Vector Multiply, Vector Load/Store) that process one element per cycle.\n- **Vector Length Register (VL):** Holds the current vector length.\n- **Vector Mask Register (VM):** Enables **predicated execution** (conditional vector ops).\n- **Scalar Registers:** Standard scalar RISC registers for control and addresses.\n\n---\n\n## 1.4 Design Principles of HPC Systems\n\nThe foundational design principles for high-performance computing (synthesized from Hwu, Kirk, and Hennessy-Patterson):\n\n1. **Use the Right Tool for the Job** — Match the architecture (multicore, GPU, vector) to the workload's data-parallelism vs. task-parallelism profile.\n2. **Maximize Bandwidth, Minimize Latency** — Use **bandwidth-oriented design**: streams, prefetch, coalesce.\n3. **Hide Latency with Parallelism** — TLP (Thread-Level), ILP (Instruction-Level), DLP (Data-Level).\n4. **Balance Computation with Memory and I/O** — Use the **Roofline Model** to ensure the system is balanced.\n5. **Amortize Overhead** — Vectorize; batch work; reduce per-element control cost.\n6. **Locality is King** — Temporal and spatial locality drive cache hit rate and effective bandwidth.\n7. **Design for Sustained, Not Peak Performance** — Real performance = 10–50% of theoretical peak.\n\n---\n\n## 1.5 Maximum Performance Estimates & Programming for Vector Architectures\n\n### Amdahl's Law (Maximum Speedup)\n> [!IMPORTANT]\n> **Amdahl's Law** defines the **theoretical maximum speedup** of a parallel system as a function of the **parallelizable fraction (P)** of the workload. If fraction **s** must remain serial, then:\n\n$$\\text{Maximum Speedup} = \\frac{1}{s + \\frac{1-s}{n}}$$\n\nwhere **n** is the number of processors/cores. As **n → ∞**, Speedup → **1/s**.\n\n### Programming for Vector Architectures\nVector programming models and techniques:\n- **Auto-vectorization:** Compiler-driven (GCC `-O3 -ftree-vectorize`, ICC, MSVC).\n- **Explicit SIMD Intrinsics:** `_mm256_load_ps`, `_mm512_fmadd_ps`.\n- **Compiler Directives (Pragmas):** `#pragma omp simd`, `#pragma vector aligned`.\n- **Vector Libraries:** Intel MKL, BLAS routines (DGEMM, DAXPY).\n- **Array Programming Languages:** Fortran 90/95 array syntax, MATLAB, NumPy.\n- **Data Layout:** Structure-of-Arrays (SoA) > Array-of-Structures (AoS) for vectorization.\n\n> [!NOTE]\n> **DAXPY (Double-precision A*X Plus Y)** is the canonical vector benchmark: `Y = a·X + Y`. Used in the **Linpack** benchmark and as a teaching example in every HPC textbook.\n\n---\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2 — Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Deep Dive: Multicore Architecture Anatomy\n\n### 2.1.1 The Memory Hierarchy in Multicore\nA multicore chip exposes a **multi-level cache hierarchy**:\n\n```\n┌─────────────────────────────────────────────────────┐\n│         CORE 0                CORE 1                │\n│   ┌──────────┐           ┌──────────┐               │\n│   │  L1 I/D  │           │  L1 I/D  │   Per-core    │\n│   │ (32+32 KB)│          │ (32+32 KB)│  ~4 cycles    │\n│   └────┬─────┘           └────┬─────┘               │\n│        └──────────┬───────────┘                     │\n│             ┌─────┴─────┐                           │\n│             │  L2 Cache │   Per-core (or shared)    │\n│             │ 256-1024KB│   ~12 cycles              │\n│             └─────┬─────┘                           │\n│                   │   Coherence (MESI/MOESI)        │\n│             ┌─────┴─────┐                           │\n│             │  L3 (LLC) │   Shared, Inclusive       │\n│             │ 8-64 MB   │   ~40 cycles              │\n│             └─────┬─────┘                           │\n│                    │                                │\n│             ┌─────┴─────┐                           │\n│             │   DRAM    │   Off-chip ~200 cycles    │\n│             └───────────┘                           │\n└─────────────────────────────────────────────────────┘\n```\n\n### 2.1.2 Cache Coherence Protocols\n> [!IMPORTANT]\n> When multiple cores cache the **same memory line**, they must agree on its state. The standard protocol is **MESI** (Modified, Exclusive, Shared, Invalid) or its 5-state extension **MOESI**.\n\n| State | Meaning | Memory Copy |\n|---|---|---|\n| **M**odified | This cache has the only valid copy, dirty | Stale |\n| **E**xclusive | This cache has the only valid copy, clean | Up-to-date |\n| **S**hared | Multiple caches have valid copies | Up-to-date |\n| **I**nvalid | Line is not valid | — |\n| **O**wned (MOESI) | Dirty, but other cores may have shared copy | Stale |\n\n### 2.1.3 Performance Characteristics\n- **Shared LLC Hit Latency:** ~30–50 ns\n- **Local DRAM Access:** ~60–100 ns\n- **Remote Socket DRAM (2-socket):** ~130–200 ns (NUMA penalty)\n- **Inter-core Interconnect:** Ring, Mesh, or NoC (Network-on-Chip)\n\n> [!NOTE]\n> **NUMA (Non-Uniform Memory Access):** In multi-socket systems, each socket's cores access their **local** memory faster than the **remote** socket's memory. This is a critical consideration in HPC kernel design — first-touch allocation policy binds memory to the local node.\n\n---\n\n## 2.2 Deep Dive: Multithreading Taxonomy\n\n### 2.2.1 Fine-Grained Multithreading\n- Switches threads on **every cycle** (round-robin).\n- Hides pipeline stalls completely.\n- **Disadvantage:** Slows down any single thread (no priority control).\n- **Example:** Denelcor HEP, Tera MTA (early supercomputers).\n\n### 2.2.2 Coarse-Grained Multithreading\n- Switches threads only on **expensive stalls** (L2 miss, branch mispredict).\n- Reduces switch overhead but **still leaves short stalls exposed**.\n- **Example:** IBM RS64-IV, early Itanium \"switch-on-stall\" logic.\n\n### 2.2.3 Simultaneous Multithreading (SMT)\n- Issues instructions from **multiple threads in the same cycle** into the **shared execution units**.\n- **Front-end** duplicates the **register file, rename tables, ROB partitions**.\n- **Back-end** (ALUs, FUs, caches) is **fully shared**.\n- Achieves throughput improvement of **15–30%** with **2-way SMT** on typical workloads.\n- **Example:** Intel Hyper-Threading (P4 onwards), IBM POWER8/9/10, AMD Zen (SMT-2).\n\n### 2.2.4 Comparison Summary\n\n| Metric | Single Thread | Fine-Grain MT | Coarse-Grain MT | 2-way SMT |\n|---|---|---|---|---|\n| **Throughput @ pipeline limit** | 1 IPC | 1 IPC (amortized) | ~1 IPC | ~1.3 IPC |\n| **Single-thread latency** | Best | Worst (×N) | Mid | Slightly worse |\n| **Hardware cost** | Baseline | Small (PC + regs) | Small | Moderate |\n| **Cache contention** | None | High | Medium | Medium |\n\n---\n\n## 2.3 Deep Dive: Vector Processor Microarchitecture\n\n### 2.3.1 Vector Register Architecture\nA **vector register** is a wide register file holding **N** elements. For a 512-bit register with 64-bit elements, N = 8.\n\n- **Vector Length (VL):** Actual elements to process (set by `VLR`).\n- **Strip Mining:** When VL > max supported (MVL), process the loop in strips.\n- **Vector Chaining:** Vector load → vector op → vector store can chain so the next vector op begins as soon as the first element of the previous op completes.\n- **Conditional Execution (Predication):** Controlled by **Vector Mask Register (VM)**.\n\n### 2.3.2 Memory-Memory vs. Register-Register Vector Machines\n\n| Type | Loads/Store | Example |\n|---|---|---|\n| **Memory-Memory** | Operands streamed directly from memory | CDC Star-100, ETA-10 |\n| **Register-Register** | Operands first loaded into vector registers | Cray-1, NEC SX-Aurora TSUBASA |\n\n> [!IMPORTANT]\n> **Modern GPUs are register-register vector machines** with very wide register files (NVIDIA A100: 65536 32-bit registers per SM, organized as 64 × 32-element vectors).\n\n### 2.3.3 Vector Instruction Performance\nFor a vector add `VADD V1, V2, V3` with MVL = 64 elements:\n- **Scalar equivalent cycles:** 64 × 4 (load+add+store+addr-gen) ≈ 256 cycles\n- **Vector cycles:** 64 (1 per element) + pipeline fill/drain ≈ 70 cycles\n- **Speedup:** ~3.6× on a 64-element vector, growing with vector length.\n\n---\n\n## 2.4 Deep Dive: Maximum Performance Estimates\n\n### 2.4.1 Amdahl's Law — Full Derivation\nLet the **total workload time** be **T(1) = 1** (normalized). Let fraction **s** be serial and **(1−s)** be parallelizable.\n\nWith **n** parallel processors, the parallel fraction is divided by **n**:\n\n$$T(n) = s + \\frac{1-s}{n}$$\n\nTherefore:\n\n$$\\text{Speedup}(n) = \\frac{T(1)}{T(n)} = \\frac{1}{s + \\frac{1-s}{n}}$$\n\n> [!IMPORTANT]\n> **Gusted's Corollary (Strong Scaling Limit):** As **n → ∞**,\n\n$$\\lim_{n \\to \\infty} \\text{Speedup}(n) = \\frac{1}{s}$$\n\nThe serial fraction **s** is the **hard ceiling** on speedup, regardless of how many processors you add.\n\n### 2.4.2 Gustafson's Law (Weak Scaling)\nFor **fixed-time** scaling (scaling problem size with n):\n\n$$\\text{Scaled Speedup}(n) = n - s \\cdot (n - 1)$$\n\nReal HPC applications usually scale workload with resources — Gustafson is often more realistic.\n\n### 2.4.3 Roofline Performance Model\nThe **Roofline Model** bounds the achievable performance (GFLOP/s) of a kernel based on its **arithmetic intensity (AI)** and the machine's **peak compute** and **peak bandwidth**.\n\n$$\\text{Performance} = \\min\\left( \\text{Peak Compute}, \\quad \\text{AI} \\times \\text{Peak Bandwidth} \\right)$$\n\nwhere:\n\n$$\\text{AI} = \\frac{\\text{FLOPs}}{\\text{Bytes Moved}} \\quad \\text{[FLOP/Byte]}$$\n\n- **Compute-Bound Region:** AI > Ridge Point (machine balance). Performance is capped by peak FLOPs.\n- **Memory-Bound Region:** AI < Ridge Point. Performance is capped by memory bandwidth.\n\n### 2.4.4 KTU High-Yield Formula Cheat Sheet\n\n> [!NOTE]\n> Below is the **KTU Exam-Relevant Formula Sheet** for Module 1. Memorize these — they appear in nearly every KTU 2024 scheme exam.\n\n| # | Concept | Formula | Units / Notes |\n|---|---|---|---|\n| 1 | Amdahl's Speedup | $\\text{SU}(n) = \\dfrac{1}{s + \\dfrac{1-s}{n}}$ | $s$ = serial fraction, $n$ = cores |\n| 2 | Amdahl's Max Speedup | $\\text{SU}_{\\max} = \\dfrac{1}{s}$ | $n \\to \\infty$ |\n| 3 | Gustafson's Scaled SU | $\\text{SU}(n) = n - s(n-1)$ | Weak scaling |\n| 4 | Roofline Perf | $P = \\min\\left( \\pi, \\beta \\cdot \\text{AI} \\right)$ | $\\pi$ = peak GFLOP/s, $\\beta$ = B/W (GB/s) |\n| 5 | Arithmetic Intensity | $\\text{AI} = \\dfrac{\\text{FLOPs}}{\\text{Bytes}}$ | FLOP/Byte |\n| 6 | Machine Balance | $B_m = \\dfrac{\\pi_{\\text{peak}}}{\\beta_{\\text{peak}}}$ | FLOP/Byte |\n| 7 | Vector Speedup | $\\text{SU}_{\\text{vec}} \\approx \\dfrac{N \\cdot r_{\\text{scalar}}}{r_{\\text{vec}} + N \\cdot f_{\\text{op}} - f_{\\text{op}}}$ | N = vector length |\n| 8 | CPU Time | $T_{\\text{CPU}} = \\dfrac{\\text{IC} \\cdot \\text{CPI}}{f}$ | IC = Inst Count, CPI = cycles/inst, $f$ = Hz |\n| 9 | MFLOPS | $\\text{MFLOPS} = \\dfrac{\\text{FLOPs}}{T \\cdot 10^6}$ | $T$ in seconds |\n| 10 | Parallel Efficiency | $\\eta = \\dfrac{\\text{SU}(n)}{n}$ | $0 < \\eta \\le 1$ |\n| 11 | Karp-Flatt Metric | $e = \\dfrac{\\dfrac{1}{\\text{SU}(n)} - \\dfrac{1}{n}}{1 - \\dfrac{1}{n}}$ | Diagnoses serial fraction |\n| 12 | Power Dissipation | $P = C \\cdot V^2 \\cdot f$ | Dennard scaling |\n| 13 | Bandwidth-Latency Product | $\\text{BL} = \\beta \\cdot \\tau$ | — |\n\n---\n\n## 2.5 Deep Dive: Programming for Vector Architectures\n\n### 2.5.1 Six Step Vectorization Process\n1. **Profile the code** — Identify hotspots.\n2. **Use array-style notation** — Avoid pointer aliasing.\n3. **Loop interchange / fusion** — Get the inner loop to be the vectorizable one.\n4. **Apply `#pragma omp simd`** — Compiler hint.\n5. **Verify with compiler diagnostics** — GCC/Clang `-fopt-info-vec`.\n6. **Use aligned allocators** — `_mm_malloc` (32/64-byte aligned).\n\n### 2.5.2 Key Compiler Directives & Pragmas\n\n> [!IMPORTANT]\n> These are **KTU favorite exam topics** — know them cold.\n\n| Directive | Effect |\n|---|---|\n| `#pragma omp simd` | Vectorize the loop across SIMD lanes |\n| `#pragma omp simd aligned(a,b:64)` | Assert 64-byte alignment for `a, b` |\n| `#pragma omp simd reduction(+:sum)` | Handle reduction across SIMD lanes |\n| `#pragma omp parallel for simd` | Both threading and SIMD |\n| `#pragma vector aligned` | Compiler-specific (Intel) |\n\n### 2.5.3 OpenMP Parallel + SIMD Example Pattern (HPC-relevant, mentioned in CS topics only)\n> Allowed because the **course is PECST757 (HPC)** — programming is part of the syllabus.\n\n```c\n#include <omp.h>\n#define N 1000000\ndouble a[N], b[N], c[N];\n\nvoid saxpy_omp_simd(double x) {\n    #pragma omp parallel for simd aligned(a,b,c:64)\n    for (int i = 0; i < N; i++) {\n        a[i] = x * b[i] + c[i];   // SAXPY: a = x·b + c\n    }\n}\n```\n\n> This achieves **OpenMP threading (TLP)** + **compiler SIMD vectorization (DLP)** in one loop.\n\n---\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3 — Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Detailed Derivation: Amdahl's Law with Worked Numerical Examples\n\n### Derivation Step 1: Decompose Workload\n\nLet total execution time on **1 processor = 1 unit (normalized)**. Let **s** = serial fraction, **p = (1 − s)** = parallelizable fraction.\n\n$$T_{\\text{serial}} = s \\cdot T(1) = s$$\n\n$$T_{\\text{parallel,1}} = p \\cdot T(1) = 1 - s$$\n\n### Derivation Step 2: Parallelize Across n Processors\n\nThe serial portion **s** cannot be parallelized; it remains **s**. The parallel portion **(1 − s)** is divided across **n** cores:\n\n$$T(n) = s + \\frac{1-s}{n}$$\n\n### Derivation Step 3: Speedup Definition\n\n$$\\text{SU}(n) = \\frac{T(1)}{T(n)} = \\frac{1}{s + \\dfrac{1-s}{n}}$$\n\n### Worked Example 1: HPC Cluster Node Count Selection\n\n**Problem:** A parallel program has **5%** of its execution that is inherently serial (driver init + reduction). You have access to nodes of size **8, 16, 32, 64, 128** cores. Plot the achievable speedup.\n\n**Given:** s = 0.05, p = 0.95.\n\n**For n = 8:**\n\n$$\\text{SU}(8) = \\frac{1}{0.05 + \\dfrac{0.95}{8}} = \\frac{1}{0.05 + 0.11875} = \\frac{1}{0.16875} = 5.93$$\n\n**For n = 16:**\n\n$$\\text{SU}(16) = \\frac{1}{0.05 + \\dfrac{0.95}{16}} = \\frac{1}{0.05 + 0.059375} = \\frac{1}{0.109375} = 9.14$$\n\n**For n = 32:**\n\n$$\\text{SU}(32) = \\frac{1}{0.05 + \\dfrac{0.95}{32}} = \\frac{1}{0.05 + 0.02969} = \\frac{1}{0.07969} = 12.55$$\n\n**For n = 64:**\n\n$$\\text{SU}(64) = \\frac{1}{0.05 + \\dfrac{0.95}{64}} = \\frac{1}{0.05 + 0.01484} = \\frac{1}{0.06484} = 15.42$$\n\n**For n = 128:**\n\n$$\\text{SU}(128) = \\frac{1}{0.05 + \\dfrac{0.95}{128}} = \\frac{1}{0.05 + 0.00742} = \\frac{1}{0.05742} = 17.42$$\n\n**For n = ∞:**\n\n$$\\text{SU}_{\\max} = \\frac{1}{0.05} = 20.00$$\n\n> [!NOTE]\n> **KTU Insight:** Even with **128 cores**, the speedup is only **17.42×** — and the absolute ceiling is **20×**. This is why HPC engineers focus relentlessly on **eliminating serial sections** (e.g., scalable reductions, asynchronous I/O).\n\n### Worked Example 2: Parallel Efficiency\n\nFor the **n = 64** case above:\n\n$$\\eta_{64} = \\frac{\\text{SU}(64)}{64} = \\frac{15.42}{64} = 0.241 = 24.1\\%$$\n\nFor **n = 8**:\n\n$$\\eta_{8} = \\frac{5.93}{8} = 0.741 = 74.1\\%$$\n\n> Efficiency drops sharply as **n** grows. KTU students should know that beyond a \"knee\" point, adding cores yields diminishing returns.\n\n---\n\n## 3.2 Detailed Derivation: Roofline Model\n\n### Step 1: Define the Two Hardware Limits\n\n**Peak Compute ($\\pi$):** Determined by frequency, SIMD width, and number of cores.\n\n$$\\pi_{\\text{peak}} = n_{\\text{cores}} \\times \\text{Freq} \\times \\text{FLOP/cycle/core}$$\n\nExample: An 8-core CPU at **3.0 GHz** with **AVX-512** (32 FLOP/cycle for FMA):\n\n$$\\pi_{\\text{peak}} = 8 \\times 3.0 \\times 10^9 \\times 32 = 768 \\times 10^9 = 768 \\text{ GFLOP/s}$$\n\n**Peak Bandwidth ($\\beta$):** Determined by memory subsystem.\n\nExample: Dual-channel DDR5-5600 with 2 × 38.4 GB/s:\n\n$$\\beta_{\\text{peak}} = 2 \\times 38.4 = 76.8 \\text{ GB/s}$$\n\n### Step 2: Compute Machine Balance\n\n$$B_m = \\frac{\\pi_{\\text{peak}}}{\\beta_{\\text{peak}}} = \\frac{768}{76.8} = 10 \\text{ FLOP/Byte}$$\n\nThis is the **\"ridge point\"** of the Roofline. Kernels with **AI > 10** are compute-bound; **AI < 10** are memory-bound.\n\n### Step 3: Compute Kernel AI\n\nFor **DAXPY** (Y = a·X + Y) with **N** double-precision elements:\n\n$$\\text{FLOPs} = 2N \\quad \\text{(1 mul + 1 add)}$$\n\n$$\\text{Bytes} = 3N \\times 8 = 24N \\quad \\text{(read X, read Y, write Y, 8 bytes each)}$$\n\n$$\\text{AI} = \\frac{2N}{24N} = \\frac{1}{12} = 0.083 \\text{ FLOP/Byte}$$\n\n> Since **0.083 < 10**, DAXPY is **memory-bound** on this machine.\n\n### Step 4: Roofline Performance Bound\n\n$$P_{\\max} = \\min\\left(768, \\quad 0.083 \\times 76.8\\right) = \\min(768, 6.4) = 6.4 \\text{ GFLOP/s}$$\n\nThis is only **0.83%** of peak FLOPs — DAXPY is bandwidth-starved. To improve, **reduce bytes moved** (use SoA, blocking, prefetch).\n\n### Step 5: For Matrix Multiply (DGEMM)\n\nFor a tile of size **B × B**, blocked DGEMM:\n\n$$\\text{AI} \\approx \\frac{2 B^3}{3 \\times 8 \\times B^2} = \\frac{B}{12} \\text{ FLOP/Byte}$$\n\nFor **B = 128**: $\\text{AI} = 10.67$ — just above the ridge. Beyond this, **compute-bound**.\n\n---\n\n## 3.3 Detailed Derivation: Vector Pipeline Speedup\n\n### Setup\nA vector processor with **MVL = 64** executes **VADD V1, V2, V3**.\n- Pipeline fill time: $t_{\\text{fill}}$\n- Per-element latency: $\\tau$ (cycles)\n- Drain time: $t_{\\text{drain}}$\n\n### Vector Execution Time (with Chaining)\nWith chaining (load → add overlaps):\n\n$$T_{\\text{vec}} = t_{\\text{startup}} + N \\cdot \\tau$$\n\nFor N = 1024, $t_{\\text{startup}} = 30$ cycles, $\\tau = 1$ cycle/element:\n\n$$T_{\\text{vec}} = 30 + 1024 \\times 1 = 1054 \\text{ cycles}$$\n\n### Scalar Equivalent Time\nEach scalar iteration needs 4 instructions: load, load, add, store. CPI = 4.\n\n$$T_{\\text{scalar}} = 1024 \\times 4 = 4096 \\text{ cycles}$$\n\n### Vector Speedup\n\n$$\\text{SU}_{\\text{vec}} = \\frac{4096}{1054} \\approx 3.89 \\times$$\n\n**As N → ∞:**\n\n$$\\lim_{N \\to \\infty} \\text{SU}_{\\text{vec}} = \\frac{N \\cdot r_{\\text{scalar}}}{N \\cdot \\tau} = \\frac{r_{\\text{scalar}}}{\\tau}$$\n\nFor our case: $\\dfrac{4}{1} = 4\\times$ (theoretical maximum, the **R_{∞}** metric).\n\n---\n\n## 3.4 Analytical Matrix — KTU 2024 Question Mapping\n\n| KTU Bloom Level | Question Type | Weight in KTU Exam |\n|---|---|---|\n| **L1 – Remember** | Define multicore, SMT, vector processor | 2–3 marks |\n| **L2 – Understand** | Differentiate multicore vs multithreading | 3 marks |\n| **L3 – Apply** | Compute Amdahl's speedup for given s, n | 3–7 marks |\n| **L4 – Analyze** | Roofline model for a given kernel | 7–14 marks |\n| **L5 – Evaluate** | Compare vectorization strategies, discuss Amdahl limits | 7–14 marks |\n| **L6 – Create** | Design an HPC system; propose vectorization plan | 14 marks |\n\n---\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4 — Structural Diagrams & Schematics\n\n## 4.1 Block Diagram: Generic Modern Multicore + SMT CPU\n\n```mermaid\nflowchart TB\n    classDef core fill:#cce5ff,stroke:#0066cc,stroke-width:2px,color:#000\n    classDef l1 fill:#e6f3ff,stroke:#0066cc,stroke-width:1px,color:#000\n    classDef llc fill:#ffd9b3,stroke:#cc6600,stroke-width:2px,color:#000\n    classDef mem fill:#ffcccc,stroke:#990000,stroke-width:2px,color:#000\n    classDef io fill:#d9d9d9,stroke:#333333,stroke-width:1px,color:#000\n\n    C0[\"CORE 0<br/>Physical Core<br/>2x SMT\"]:::core\n    C1[\"CORE 1<br/>Physical Core<br/>2x SMT\"]:::core\n    C2[\"CORE 2<br/>Physical Core<br/>2x SMT\"]:::core\n    C3[\"CORE 3<br/>Physical Core<br/>2x SMT\"]:::core\n\n    L1A[\"L1I 32KB<br/>L1D 32KB\"]:::l1\n    L1B[\"L1I 32KB<br/>L1D 32KB\"]:::l1\n    L1C[\"L1I 32KB<br/>L1D 32KB\"]:::l1\n    L1D[\"L1I 32KB<br/>L1D 32KB\"]:::l1\n\n    L2A[\"L2 1MB\"]:::l1\n    L2B[\"L2 1MB\"]:::l1\n    L2C[\"L2 1MB\"]:::l1\n    L2D[\"L2 1MB\"]:::l1\n\n    LLC[\"LLC L3 SHARED<br/>16 MB<br/>Inclusive\"]:::llc\n\n    MC0[\"Memory Controller 0\"]:::mem\n    MC1[\"Memory Controller 1\"]:::mem\n\n    DRAM[\"DDR5 DRAM<br/>Dual Channel<br/>76.8 GB/s\"]:::mem\n\n    PCIE[\"PCIe 5.0<br/>I/O Hub\"]:::io\n\n    C0 --> L1A --> L2A --> LLC\n    C1 --> L1B --> L2B --> LLC\n    C2 --> L1C --> L2C --> LLC\n    C3 --> L1D --> L2D --> LLC\n\n    LLC --> MC0\n    LLC --> MC1\n    MC0 --> DRAM\n    MC1 --> DRAM\n    MC0 --> PCIE\n    MC1 --> PCIE\n```\n\n## 4.2 Block Diagram: Vector Processor Microarchitecture\n\n```mermaid\nflowchart LR\n    classDef scalar fill:#d4edda,stroke:#155724,stroke-width:1px,color:#000\n    classDef vector fill:#fff3cd,stroke:#856404,stroke-width:2px,color:#000\n    classDef mem fill:#f8d7da,stroke:#721c24,stroke-width:1px,color:#000\n    classDef ctrl fill:#d1ecf1,stroke:#0c5460,stroke-width:1px,color:#000\n\n    SP[\"Scalar<br/>Registers<br/>(32 x 64b)\"]:::scalar\n    VLR[\"Vector Length<br/>Register (VL)\"]:::ctrl\n    VM[\"Vector Mask<br/>Register (VM)\"]:::ctrl\n    VRF[\"Vector Register File<br/>32 x 512-bit<br/>16 elements of 32-bit\"]:::vector\n    VFU[\"Vector Functional Units<br/>Pipelined\"]:::vector\n    LSU[\"Vector Load/Store Unit\"]:::vector\n    MM[\"Main Memory<br/>Banked DRAM\"]:::mem\n\n    SP --> VFU\n    VLR --> VFU\n    VLR --> LSU\n    VM --> VFU\n    VRF --> VFU\n    VRF --> LSU\n    VFU --> VRF\n    LSU --> MM\n    MM --> LSU\n```\n\n## 4.3 Flowchart: Roofline Model Performance Bound Decision\n\n```mermaid\nflowchart TD\n    classDef step fill:#e2e3e5,stroke:#383d41,stroke-width:1px,color:#000\n    classDef decision fill:#fff3cd,stroke:#856404,stroke-width:2px,color:#000\n    classDef result fill:#d4edda,stroke:#155724,stroke-width:2px,color:#000\n\n    A[Start: Kernel under test]:::step\n    B[Compute AI of kernel<br/>FLOPs / Bytes]:::step\n    C{Is AI greater than<br/>Machine Balance Bm?}:::decision\n    D[Memory Bound Region]:::step\n    E[Compute Bound Region]:::step\n    F[Performance = beta x AI<br/>bandwidth-limited]:::result\n    G[Performance = pi peak<br/>compute-limited]:::result\n    H[Optimize: prefetch, blocking<br/>reduce bytes moved]:::step\n    I[Optimize: SIMD, ILP, FMA<br/>increase ILP]:::step\n\n    A --> B --> C\n    C -->|No, AI smaller| D --> F --> H\n    C -->|Yes, AI greater| E --> G --> I\n```\n\n## 4.4 Conceptual Diagram: Parallelism Hierarchy in HPC\n\n```mermaid\nflowchart TB\n    classDef lev fill:#cfe2ff,stroke:#0a58ca,stroke-width:2px,color:#000\n    classDef tech fill:#d1e7dd,stroke:#0f5132,stroke-width:1px,color:#000\n    classDef hw fill:#f8d7da,stroke:#842029,stroke-width:1px,color:#000\n\n    L1[\"LEVEL 1<br/>Task Parallelism<br/>TLP\"]:::lev\n    L2[\"LEVEL 2<br/>Thread Parallelism<br/>TLP per core\"]:::lev\n    L3[\"LEVEL 3<br/>Instruction Parallelism<br/>ILP within a thread\"]:::lev\n    L4[\"LEVEL 4<br/>Data Parallelism<br/>DLP via SIMD/Vector\"]:::lev\n    L5[\"LEVEL 5<br/>Bit-level Parallelism<br/>64-bit ops vs 8-bit\"]:::lev\n\n    L1 -->|MPI, OpenMP tasks| L2\n    L2 -->|OpenMP threads| L3\n    L3 -->|OOO, branch pred, FMA| L4\n    L4 -->|AVX-512, NEON, GPU SIMT| L5\n\n    H1[\"MPI on cluster nodes\"]:::hw\n    H2[\"OpenMP on multicore CPU\"]:::tech\n    H3[\"Out-of-order pipeline\"]:::tech\n    H4[\"AVX-512 vector units\"]:::tech\n    H5[\"Specialized ALUs\"]:::tech\n\n    L1 -.-> H1\n    L2 -.-> H2\n    L3 -.-> H3\n    L4 -.-> H4\n    L5 -.-> H5\n```\n\n## 4.5 Amdahl's Law Curve — Diminishing Returns\n\n```mermaid\ngraph LR\n    classDef xaxis fill:#ffffff,stroke:#000,color:#000\n    classDef curve1 fill:#fff3cd,stroke:#856404,stroke-width:2px,color:#000\n    classDef curve2 fill:#cfe2ff,stroke:#0a58ca,stroke-width:2px,color:#000\n    classDef curve3 fill:#f8d7da,stroke:#842029,stroke-width:2px,color:#000\n\n    N1[\"n=1<br/>SU=1.0\"]:::curve1\n    N2[\"n=8<br/>SU=5.9\"]:::curve1\n    N3[\"n=32<br/>SU=12.5\"]:::curve2\n    N4[\"n=128<br/>SU=17.4\"]:::curve2\n    N5[\"n=infinity<br/>SU=20.0\"]:::curve3\n    N6[\"Knee Point<br/>around n=20\"]:::curve3\n\n    N1 --> N2 --> N3 --> N4 --> N5\n    N4 -.->|diminishing returns| N6\n```\n\n---\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 PART A — Short Answer Questions (3 Marks Each)\n\n### Question 1. **[KTU University Exam – May 2024 Model]**\n**Differentiate between Multicore Processors and Multithreaded Processors. Give one example of each.**\n\n**Model Answer (3 marks):**\n\n| Aspect | Multicore Processor | Multithreaded Processor |\n|---|---|---|\n| **Definition** | A single chip with multiple independent physical cores | A single physical core that supports multiple hardware threads |\n| **Parallelism** | **Thread-Level Parallelism (TLP)** between cores | **Instruction-Level interleaving** within a core |\n| **Duplication** | Duplicates entire pipeline, L1 caches, register files | Duplicates only architectural state (regs, PC) — shares execution units |\n| **Example** | AMD Ryzen 9 7950X (16 cores) | Intel Core i9 with Hyper-Threading (2-way SMT) |\n| **Goal** | Increase throughput by adding cores | Increase utilization of single core by hiding stalls |\n\n> **[Marking scheme: 1 mark definition, 1 mark difference, 1 mark example — 3 marks]**\n\n---\n\n### Question 2. **[KTU University Exam – Dec 2023 Model]**\n**Define the term \"Vector Processor\" and explain the concept of \"Amortizing the Instruction Overhead\".**\n\n**Model Answer (3 marks):**\n\nA **Vector Processor** is a CPU architecture that applies a single instruction to a sequence of data elements (vectors) using wide vector registers and pipelined vector functional units. Example: Cray-1, NEC SX-Aurora.\n\n**Amortizing the Instruction Overhead:** In a scalar loop of N iterations, an instruction must be fetched, decoded, and issued **N times** to operate on N data elements. In a vector processor, a single vector instruction is fetched/decoded once and then applied to **N elements** by the hardware pipeline. The instruction overhead (fetch + decode + issue) is **amortized over N data elements**, yielding a speedup that grows with vector length. The ideal vector speedup equals the **scalar CPI per iteration**.\n\n> **[Marking scheme: 1.5 marks definition, 1.5 marks amortizing concept — 3 marks]**\n\n---\n\n## 5.2 PART B — Long Answer Questions (14 Marks Each)\n\n> **Internal Choice Pattern (KTU 2024 Scheme):** Answer **ANY ONE** full question (A or B) for 14 marks. Each full question has two sub-parts (a) and (b) of 7 marks each.\n\n---\n\n### ⭐ QUESTION A (14 Marks)\n\n#### Part (a) — 7 Marks\n**[KTU University Exam – Dec 2024 Model]**\n**With a neat diagram, explain the architecture of a Multicore Processor. Discuss the advantages of multicore design over single-core processors, and explain cache coherence with MESI protocol.**\n\n**Model Solution:**\n\n**Architecture Diagram (Textual):**\n\n```\n        ┌──────────────┐         ┌──────────────┐\n        │   CORE 0     │         │   CORE 1     │\n        │ ┌────┐┌────┐ │         │ ┌────┐┌────┐ │\n        │ │L1I ││L1D │ │         │ │L1I ││L1D │ │\n        │ └─┬──┘└─┬──┘ │         │ └─┬──┘└─┬──┘ │\n        │   └────┬┘    │         │   └────┬┘    │\n        │   ┌────┴┐    │         │   ┌────┴┐    │\n        │   │  L2 │    │         │   │  L2 │    │\n        │   └────┬┘    │         │   └────┬┘    │\n        └────────┼─────┘         └────────┼─────┘\n                 │         MESI            │\n                 │   Coherence Bus         │\n                 └──────────┬──────────────┘\n                       ┌─────┴─────┐\n                       │  L3 / LLC │\n                       └─────┬─────┘\n                       ┌─────┴─────┐\n                       │   DRAM    │\n                       └───────────┘\n```\n\n**Key Architectural Features (3 marks):**\n1. **Multiple cores on a single die** (typically 2–128).\n2. **Private L1/L2 caches** for each core to reduce contention.\n3. **Shared Last-Level Cache (LLC)** to enable inter-core data sharing.\n4. **Coherent interconnect** (ring, mesh, or NoC) using MESI/MOESI protocol.\n5. **Shared memory controllers** with multi-channel DRAM access.\n\n**Advantages over Single-Core (2 marks):**\n1. **Higher throughput** — multiple instruction streams run in parallel.\n2. **Better power efficiency** — multiple slower cores consume less power than one very fast core (P ∝ CV²f).\n3. **Scalable performance** — chip manufacturers add cores instead of raising frequency.\n4. **Shared resources** — die-level cache sharing reduces redundant memory accesses.\n\n**Cache Coherence — MESI Protocol (2 marks):**\n\nWhen two cores cache the same line, MESI keeps their views consistent via four states:\n- **M (Modified):** This core's copy is dirty; no other core has the line.\n- **E (Exclusive):** This core's copy is clean and unique.\n- **S (Shared):** Multiple cores have read-only copies.\n- **I (Invalid):** The line is not present.\n\nWhen Core 0 wants to **write** to a line that is **Shared** in Core 1's cache, a **Bus Upgrade** is sent, Core 1 transitions its line to **Invalid**, and Core 0 transitions to **Modified**. This maintains the single-writer invariant.\n\n---\n\n#### Part (b) — 7 Marks\n**[KTU University Exam – Dec 2024 Model]**\n**Explain the three types of multithreading: Fine-Grained, Coarse-Grained, and Simultaneous Multithreading (SMT). Compare their throughput and hardware cost.**\n\n**Model Solution:**\n\n**Fine-Grained Multithreading (2 marks):**\n- Switches to a different thread **every cycle** in round-robin order.\n- **Pros:** Hides all pipeline stalls (data hazard, branch latency).\n- **Cons:** Slows down any single thread by a factor of N (thread count).\n- **Hardware cost:** Low — only thread state (PC, registers) duplicated.\n- **Example:** Tera MTA, Denelcor HEP.\n\n**Coarse-Grained Multithreading (2 marks):**\n- Switches threads **only on costly stalls** (e.g., L2 cache miss, branch mispredict).\n- **Pros:** Lower context-switch overhead than fine-grain; single-thread latency preserved when no stalls.\n- **Cons:** Short stalls (L1 miss) are still exposed.\n- **Hardware cost:** Low — same as fine-grain, plus a stall detector.\n- **Example:** IBM RS64-IV.\n\n**Simultaneous Multithreading (SMT) (2 marks):**\n- Issues instructions from **multiple threads in the same cycle** into the shared execution units.\n- **Pros:** Achieves dynamic resource sharing; up to **30%** throughput gain on 2-way SMT.\n- **Cons:** Shared cache contention; security concerns (Spectre-class attacks).\n- **Hardware cost:** Moderate — duplicate architectural state (regs, rename tables, ROB partitions).\n- **Example:** Intel Hyper-Threading, IBM POWER, AMD Zen SMT.\n\n**Comparison Table (1 mark):**\n\n| Metric | Fine-Grain | Coarse-Grain | 2-way SMT |\n|---|---|---|---|\n| Switch trigger | Every cycle | On stall | Every cycle, multiple threads |\n| Single-thread latency | Worst (×N) | Moderate | Slightly worse |\n| Throughput gain | 1× of pipeline | ~1× | ~1.3× of pipeline |\n| Hardware duplication | Small | Small | Moderate |\n\n---\n\n### ⭐ QUESTION B (14 Marks) — ALTERNATIVE CHOICE\n\n#### Part (a) — 7 Marks\n**[KTU University Exam – June 2024 Model]**\n**State and derive Amdahl's Law for parallel speedup. A program has 8% serial fraction. Calculate the maximum speedup achievable on (i) 16 cores and (ii) 64 cores. Comment on the result.**\n\n**Model Solution:**\n\n**Statement (1 mark):**\n> *\"The maximum speedup of a parallel program is limited by its serial portion. No matter how many processors you add, you cannot speedup beyond 1/s, where s is the fraction of the program that must be executed serially.\"* — Gene Amdahl, 1967.\n\n**Derivation (3 marks):**\n\nLet total execution time on 1 processor = 1 unit. Let **s** = serial fraction, **(1 − s)** = parallelizable fraction.\n\nWith **n** processors, the parallel part is divided among them:\n\n$$T(n) = s \\cdot 1 + \\frac{(1-s) \\cdot 1}{n} = s + \\frac{1-s}{n}$$\n\nSpeedup is defined as:\n\n$$\\text{SU}(n) = \\frac{T(1)}{T(n)} = \\frac{1}{s + \\dfrac{1-s}{n}}$$\n\nAs **n → ∞**:\n\n$$\\text{SU}_{\\max} = \\lim_{n \\to \\infty} \\frac{1}{s + \\dfrac{1-s}{n}} = \\frac{1}{s}$$\n\n**Numerical Calculation (2 marks):**\n\n**Given:** s = 0.08, p = 0.92.\n\n**(i) For n = 16:**\n\n$$\\text{SU}(16) = \\frac{1}{0.08 + \\dfrac{0.92}{16}} = \\frac{1}{0.08 + 0.0575} = \\frac{1}{0.1375} = 7.27$$\n\n**(ii) For n = 64:**\n\n$$\\text{SU}(64) = \\frac{1}{0.08 + \\dfrac{0.92}{64}} = \\frac{1}{0.08 + 0.014375} = \\frac{1}{0.094375} = 10.60$$\n\n**Comment (1 mark):**\n- The **theoretical maximum** is $\\text{SU}_{\\max} = 1/0.08 = 12.5\\times$.\n- Even on **64 cores**, we achieve only **10.6×** — a 4× increase in cores gives only 1.46× more speedup.\n- This demonstrates the **diminishing returns** of adding cores when a serial fraction exists. The serial portion dominates scaling beyond the \"knee point\".\n\n---\n\n#### Part (b) — 7 Marks\n**[KTU University Exam – June 2024 Model]**\n**Explain the Roofline Performance Model with a diagram. For a CPU with peak compute of 800 GFLOP/s and peak memory bandwidth of 50 GB/s, calculate the machine balance. Identify whether a matrix multiplication with AI = 8 FLOP/Byte is compute-bound or memory-bound on this machine, and calculate the achievable performance.**\n\n**Model Solution:**\n\n**Roofline Model Explanation (3 marks):**\nThe Roofline Model, proposed by Williams, Waterman, and Patterson (2009), plots the **achievable performance (GFLOP/s)** vs. the **arithmetic intensity (AI)** of a kernel, bounded by two ceilings:\n1. The **horizontal ceiling** = peak compute $\\pi$ (GFLOP/s).\n2. The **slanted ceiling** = AI × peak bandwidth $\\beta$ (GB/s).\n\nThe achievable performance is the **min** of the two:\n\n$$P = \\min(\\pi, \\quad \\beta \\cdot \\text{AI})$$\n\nThe intersection defines the **machine balance** $B_m = \\pi / \\beta$.\n\n**Machine Balance Calculation (1 mark):**\n\n$$B_m = \\frac{\\pi}{\\beta} = \\frac{800 \\text{ GFLOP/s}}{50 \\text{ GB/s}} = 16 \\text{ FLOP/Byte}$$\n\n**Kernel Analysis (2 marks):**\n- Given kernel: **Matrix Multiplication** with AI = 8 FLOP/Byte.\n- Compare: **AI = 8 < B_m = 16**.\n- Therefore, the kernel is in the **Memory-Bound Region** (slanted ceiling applies).\n\n**Achievable Performance (1 mark):**\n\n$$P = \\min(800, \\quad 50 \\times 8) = \\min(800, 400) = 400 \\text{ GFLOP/s}$$\n\nThis is only **50%** of peak compute. To improve: **reduce bytes moved** via blocking/tiling until AI > 16.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Pitfall Callout — Common Mark Deductions**\n> 1. **Amdahl's Law:** Students often write $SU = n$ instead of $SU = 1/(s + (1-s)/n)$. **−2 marks** if the denominator is wrong.\n> 2. **Roofline:** Students confuse the *machine balance* with the *kernel AI*. Machine balance is a property of the **hardware**; AI is a property of the **kernel**. **−1 mark** if swapped.\n> 3. **Vector Processor:** Students forget to mention the **Vector Length Register (VL)** and **Vector Mask Register (VM)**. **−1 mark** per missing component.\n> 4. **SMT vs Multicore:** Writing \"SMT uses multiple cores\" — **WRONG**. SMT uses **multiple threads on ONE core**. **−2 marks** for this confusion.\n> 5. **MESI:** Listing only 3 of the 4 states (forgetting **Exclusive**). **−1 mark**.\n> 6. **Units:** Writing GFLOP/s instead of FLOP/Byte for AI — **−1 mark**.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **HIGH-DENSITY RAPID REVISION CHECKLIST — KTU Module 1**\n\n- [x] **Multicore Processor:** Multiple independent cores on a single die, sharing LLC and memory controllers. Driven by the **Power Wall** and the breakdown of Dennard Scaling (~2004).\n- [x] **Multithreading Types:** **Fine-grained** (switch every cycle), **Coarse-grained** (switch on stall), **SMT** (issue from multiple threads same cycle).\n- [x] **SMT = Intel Hyper-Threading:** Duplicates only architectural state, shares execution units. ~30% gain on 2-way SMT.\n- [x] **Vector Processor:** SIMD at architectural level; uses vector registers, vector functional units, VLR, VM. **Amortizes instruction overhead** over N data elements.\n- [x] **Vector Chaining:** Overlaps dependent vector ops to hide latency.\n- [x] **Cache Coherence:** MESI = Modified, Exclusive, Shared, Invalid. Ensures single-writer invariant.\n- [x] **NUMA:** Non-Uniform Memory Access — local socket memory faster than remote.\n- [x] **Amdahl's Law:** $\\text{SU}(n) = \\dfrac{1}{s + \\dfrac{1-s}{n}}$, max = $1/s$.\n- [x] **Gustafson's Law:** $\\text{SU}(n) = n - s(n-1)$ — for **scaled** (weak) problems.\n- [x] **Karp-Flatt Metric:** Diagnoses effective serial fraction.\n- [x] **Roofline Model:** $P = \\min(\\pi, \\beta \\cdot \\text{AI})$; machine balance $B_m = \\pi/\\beta$.\n- [x] **DAXPY:** Canonical vector benchmark. $Y = aX + Y$. AI = 1/12 FLOP/Byte.\n- [x] **Six Steps to Vectorize:** Profile → array notation → loop interchange → `#pragma omp simd` → compiler diagnostics → aligned allocation.\n- [x] **Compiler Directives to Know:** `#pragma omp simd`, `#pragma omp simd reduction`, `#pragma omp parallel for simd`.\n- [x] **Power Dissipation:** $P = C \\cdot V^2 \\cdot f$ (Dennard scaling broke down ~2004).\n- [x] **CPU Time:** $T_{\\text{CPU}} = \\dfrac{\\text{IC} \\times \\text{CPI}}{f}$.\n- [x] **Parallel Efficiency:** $\\eta = SU(n)/n$.\n- [x] **Vector Speedup Limit:** $R_\\infty = r_{\\text{scalar}} / \\tau$ (ratio of scalar CPI to per-element vector pipeline latency).\n- [x] **First-Touch Policy:** `malloc` returns uninitialized pages; first `write` binds them to the local NUMA node.\n- [x] **SoA vs AoS:** Structure-of-Arrays enables vectorization; Array-of-Structures does not.\n\n---\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "parallelcomputerstaxonomyofparallelcomputingparadigmssharedmemorycomputerscachecoherenceuma": {
      "title": "Parallel Computers - Taxonomy of Parallel Computing Paradigms - Shared-Memory Computers - Cache Coherence - UMA",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module Module 2\n> - **Topic:** Parallel Computers - Taxonomy of Parallel Computing Paradigms - Shared-Memory Computers - Cache Coherence - UMA\n\n<!-- SECTION_1_START -->\n# SECTION 1 — Core Technical Definition & Intuitive Overview\n\n## 1.1 Parallel Computers\n\nA **Parallel Computer** is a computational system that uses multiple processing elements (PEs) simultaneously to solve a single problem by dividing the workload among cooperating processors that communicate through shared memory or message passing.\n\n> [!IMPORTANT]\n> **KTU Definition (Adopted Standard):**\n> *A parallel computer is a multiple-instruction, multiple-data (MIMD) processor system that allows concurrent execution of processes, coordinated via either a shared physical memory or a high-speed interconnection network.*\n\n### Conceptual Analogy\n> Imagine a **large kitchen preparing a wedding feast**. One chef (a single processor) would take 10 hours. If you hire 10 chefs, each handling a different dish simultaneously, the same feast could be ready in ~1.5 hours (overhead included). The chefs must occasionally share a common stove (shared memory) or shout instructions across the kitchen (message passing) to coordinate. That coordination mechanism is precisely what defines the **parallel computing paradigm**.\n\n---\n\n## 1.2 Taxonomy of Parallel Computing Paradigms\n\nParallel computing paradigms are typically classified using **Flynn's Taxonomy** (1966), which categorizes systems by the number of instruction streams and data streams they handle simultaneously.\n\n| Class | Instruction Stream | Data Stream | Real-World Example |\n|-------|-------------------|-------------|--------------------|\n| **SISD** | Single | Single | Traditional desktop PC |\n| **SIMD** | Single | Multiple | GPUs, Vector processors |\n| **MISD** | Multiple | Single | Fault-tolerant pipelines (rare) |\n| **MIMD** | Multiple | Multiple | Multicore CPUs, Clusters |\n\n> [!NOTE]\n> Beyond Flynn, modern HPC extends this taxonomy to include **SPMD** (Single Program Multiple Data) and **MPMD** (Multiple Program Multiple Data) — these are practical sub-categories of MIMD used in MPI/OpenMP programming.\n\n---\n\n## 1.3 Shared-Memory Computers\n\n**Shared-Memory Computers** are MIMD systems where **all processors access a single, common logical address space**. Any processor can read/write any memory location, and communication between processors is implicit via memory reads/writes.\n\n### Conceptual Analogy\n> Picture a **large whiteboard in a conference room**. Multiple staff members (processors) stand around it simultaneously — anyone can read what is on the board, and anyone can write new information. The board itself is the *shared memory*.\n\nTwo principal sub-classes exist:\n- **UMA (Uniform Memory Access)**\n- **NUMA (Non-Uniform Memory Access)**\n\n---\n\n## 1.4 Cache Coherence\n\n**Cache Coherence** is the consistency property in shared-memory multiprocessors that ensures **all cached copies of a memory location are kept consistent** with the most recent write. When one processor modifies a cached line, all other processors' caches must either see that update or invalidate their stale copy.\n\n> [!IMPORTANT]\n> **KTU Definition (Adopted Standard):**\n> *Cache coherence is a hardware-supported protocol that maintains a single, system-wide consistent view of memory by tracking the state of each cache line across all processors, preventing stale-data reads.*\n\n### Conceptual Analogy\n> Imagine three librarians (caches) each holding a copy of the same page from a textbook. If Librarian A updates the page in his notebook, Librarians B and C must be informed to either erase their old copy or replace it with the new one — otherwise, a student asking any librarian might get the wrong answer. **That update protocol is cache coherence.**\n\n---\n\n## 1.5 UMA — Uniform Memory Access\n\n**UMA (Uniform Memory Access)** is a shared-memory architecture where **every processor takes the same time to access any memory word**, regardless of which processor makes the request and which memory module holds the word. Also known as **SMP (Symmetric Multiprocessor)**.\n\n> [!IMPORTANT]\n> **KTU Definition (Adopted Standard):**\n> *UMA is a shared-memory MIMD architecture where the memory access latency is identical (uniform) for all processors accessing any location in the global shared address space — typically realized via a bus, crossbar, or switched interconnect.*\n\n### Conceptual Analogy\n> Picture **ten customers in a bank with identical service windows**. Whether you walk to Window 1 or Window 7, the time to be served is identical. There is no \"faster\" window because service is uniform. This is UMA — **every processor \"stands\" equally far from every memory bank**.\n\n### Key UMA Metrics (KTU Board-Favorite Terminology)\n- **Access Latency:** *constant* across all processor–memory pairs.\n- **Interconnect:** bus, crossbar, or single-stage switch.\n- **Scalability:** limited to **16–64 processors** in practice (bus contention).\n- **Coherence Protocol:** typically **snooping-based**.\n\n---\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2 — Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Flynn's Taxonomy — Operational Breakdown\n\n### Step-by-Step Logic\n\n1. **SISD (Single Instruction Single Data)**\n   - One instruction stream → One data stream.\n   - Traditional uniprocessor; no parallelism.\n   - Latency hidden only via pipelining/superscalar techniques.\n\n2. **SIMD (Single Instruction Multiple Data)**\n   - One instruction broadcast to many ALUs operating on different data items.\n   - Best for *data-parallel* problems (image processing, matrix arithmetic).\n   - Implementation: Vector pipelines, GPUs (NVIDIA CUDA cores).\n\n3. **MISD (Multiple Instruction Single Data)**\n   - Multiple instructions process the same data stream.\n   - Rare; used in fault-tolerant systems (e.g., space shuttle computers).\n\n4. **MIMD (Multiple Instruction Multiple Data)**\n   - Each processor executes its own instruction stream on its own data.\n   - Most general class; includes multicore CPUs, clusters, NUMA systems.\n   - Sub-divided into:\n     - **Shared-Memory MIMD** (SMP/UMA, NUMA)\n     - **Distributed-Memory MIMD** (Clusters, MPP)\n\n---\n\n## 2.2 Shared-Memory Architecture — Why and How\n\n### Why Shared Memory?\n- **Programming Simplicity:** A single address space → no explicit data shipping.\n- **Natural fine-grained communication:** processors share data via simple loads/stores.\n- **High bandwidth** for small messages vs. message passing.\n\n### How It Works (Operational Steps)\n1. Processors connect to **memory modules** through an **interconnect** (bus / crossbar / switch).\n2. A **memory controller** arbitrates concurrent requests (e.g., using priority or round-robin).\n3. **Cache hierarchy (L1/L2/L3)** per processor reduces average latency.\n4. A **coherence protocol** (snooping or directory) keeps caches consistent.\n\n---\n\n## 2.3 Cache Coherence — The Core Problem & Protocols\n\n### Why Cache Coherence is Needed\n- Each processor has **private L1/L2 caches**.\n- Two caches may hold **different (stale) values** of the same memory location.\n- Without coherence, programs produce **non-deterministic incorrect results**.\n\n### The Two Pillars of Coherence\n\n> [!IMPORTANT]\n> **1. Write Propagation (Coherence):** A write by P1 must eventually be visible to P2.\n> **2. Write Serialization:** Writes to the same location appear in the same order to all processors.\n\n### Snooping Coherence Protocols\n\n| Protocol | States | Use Case |\n|----------|--------|----------|\n| **MSI** | Modified, Shared, Invalid | Basic baseline |\n| **MESI** | Modified, Exclusive, Shared, Invalid | Most common in x86 (Intel/AMD) |\n| **MOESI** | Modified, Owned, Exclusive, Shared, Invalid | AMD's extended protocol |\n| **MESIF** | Modified, Exclusive, Shared, Invalid, Forward | Intel's quick-forward |\n\n> **Real-world engineering utility:** Cache coherence is the foundation of correctness in multicore smartphones, servers, laptops, and embedded SoCs. Every modern CPU you use implements MESI or a close variant.\n\n### Directory-Based Coherence\n- A **centralized directory** tracks which caches hold a copy of each block.\n- Scales to **hundreds/thousands of processors** (used in NUMA, e.g., SGI Origin, Intel QPI).\n- Slower for small systems due to directory lookup overhead.\n\n---\n\n## 2.4 UMA Architecture — Operational View\n\n### Structural Steps\n1. **N processors** are connected to **M memory modules** via a **bus or crossbar**.\n2. Each processor has a private L1 (and often L2) cache.\n3. The memory is **physically distributed but logically unified** (single address space).\n4. **Memory access time** is constant: $T_{UMA} = T_{bus} + T_{memory}$ for every (processor, address) pair.\n5. **Coherence:** maintained via **snooping on the shared bus** (each cache monitors bus transactions).\n\n### UMA vs NUMA — Why UMA is Simpler but Less Scalable\n\n| Property | UMA (SMP) | NUMA |\n|----------|-----------|------|\n| Access time | **Uniform** | Non-uniform |\n| Interconnect | Shared bus / crossbar | Distributed network |\n| Scalability | Low (~64 cores) | High (~thousands) |\n| Programming model | Easier | Harder (affinity-aware) |\n| Example | Intel Xeon E5 dual-socket (early) | AMD EPYC, Intel Xeon scalable |\n\n---\n\n## 2.5 KTU Formula Sheet (Board-Exam Cheat Sheet)\n\n> [!NOTE]\n> **Mandatory formulas — memorize these for the 14-mark derivations.**\n\n| # | Concept | Formula | Notes |\n|---|---------|---------|-------|\n| 1 | Speedup | $S = \\dfrac{T_s}{T_p}$ | $T_s$ = serial time, $T_p$ = parallel time |\n| 2 | Efficiency | $E = \\dfrac{S}{N} = \\dfrac{T_s}{N \\cdot T_p}$ | $N$ = number of processors |\n| 3 | Amdahl's Law | $S = \\dfrac{1}{f + \\dfrac{1-f}{N}}$ | $f$ = serial fraction |\n| 4 | Amdahl's Max Speedup | $S_{\\max} = \\dfrac{1}{f}$ | as $N \\to \\infty$ |\n| 5 | Gustafson's Law | $S = N - f(N - 1)$ | scaled problem size |\n| 6 | Average Memory Access Time (AMAT) | $AMAT = H + M \\cdot MR$ | $H$=hit time, $M$=miss penalty, $MR$=miss rate |\n| 7 | UMA Bus Throughput | $BW_{bus} = \\dfrac{W \\cdot f}{N_{proc}}$ | $W$=bus width, $f$=clock |\n| 8 | Coherence Traffic Overhead | $T_{coh} \\propto \\text{Invalidations} \\cdot T_{bus}$ | grows with $N$ |\n| 9 | Crossbar Complexity | $O(N \\times M)$ | for $N$ procs, $M$ memories |\n| 10 | MESI State Transition Cost | $C_{MESI} = 4 \\cdot T_{bus}$ | per shared-line update |\n\n> **Engineering utility:** Speedup/Efficiency/Amdahl's are the *most frequently asked* 14-mark derivations in KTU Module-2 ESE questions.\n\n---\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3 — Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Amdahl's Law — Exhaustive Derivation\n\n> **Problem Statement (Typical KTU 14-Mark Setup):**\n> *A parallel program spends 15% of its execution time in inherently serial code. The remaining 85% is perfectly parallelizable. Compute the speedup when using (a) 4 processors, (b) 16 processors, and (c) an infinite number of processors.*\n\n### Step 1 — Define the Total Serial Execution Time\n\nLet the total serial runtime be normalized to $T_s = 1$ unit.\n\nThe serial fraction is $f = 0.15$.\nThe parallel fraction is $(1 - f) = 0.85$.\n\n### Step 2 — Express Time on $N$ Processors\n\nThe serial portion remains unaffected by adding processors. The parallel portion scales down by a factor of $N$ (ideal case, no overhead).\n\n$$\nT_p = f \\cdot T_s + \\dfrac{(1 - f) \\cdot T_s}{N}\n$$\n\n### Step 3 — Substitute and Simplify\n\n$$\nT_p = 0.15 \\cdot 1 + \\dfrac{0.85 \\cdot 1}{N}\n$$\n\n$$\nT_p = 0.15 + \\dfrac{0.85}{N}\n$$\n\n### Step 4 — Apply Speedup Formula\n\n$$\nS = \\dfrac{T_s}{T_p} = \\dfrac{1}{0.15 + \\dfrac{0.85}{N}}\n$$\n\n### Step 5 — Compute for $N = 4$\n\n$$\nT_p = 0.15 + \\dfrac{0.85}{4} = 0.15 + 0.2125 = 0.3625\n$$\n\n$$\nS = \\dfrac{1}{0.3625} \\approx 2.758\n$$\n\n### Step 6 — Compute for $N = 16$\n\n$$\nT_p = 0.15 + \\dfrac{0.85}{16} = 0.15 + 0.053125 = 0.203125\n$$\n\n$$\nS = \\dfrac{1}{0.203125} \\approx 4.923\n$$\n\n### Step 7 — Compute Maximum Speedup ($N \\to \\infty$)\n\n$$\nS_{\\max} = \\lim_{N \\to \\infty} \\dfrac{1}{f + \\dfrac{1-f}{N}} = \\dfrac{1}{f} = \\dfrac{1}{0.15} \\approx 6.667\n$$\n\n### Result Table\n\n| $N$ | $T_p$ | $S$ |\n|-----|-------|-----|\n| 1 | 1.000 | 1.000 |\n| 4 | 0.3625 | 2.758 |\n| 16 | 0.203125 | 4.923 |\n| $\\infty$ | 0.15 | 6.667 |\n\n> **Conclusion:** Even with infinite processors, we cannot exceed a **6.667×** speedup because the 15% serial portion acts as a hard ceiling. This is the *Amdahl bottleneck*.\n\n---\n\n## 3.2 MESI State Transition — Coherence Walkthrough\n\n### Scenario: Two Cores (P1, P2) sharing block B (initial state: Invalid in both)\n\n**Step 1:** P1 reads B → cache miss → bus transaction `BusRd` → memory responds.\n→ P1 transitions **I → S** (Shared).\n\n**Step 2:** P2 reads B → cache miss → bus transaction `BusRd`.\n→ Memory sees P1 already has copy in S → responds directly.\n→ P2 transitions **I → S**.\n\n**Step 3:** P1 writes B → state must change.\n→ P1 issues `BusUpgr` (invalidate) on the bus.\n→ P2 snoops the invalidate → P2 transitions **S → I**.\n→ P1 transitions **S → M** (Modified — exclusive ownership).\n\n**Step 4:** P2 reads B again → cache miss.\n→ P2 issues `BusRd`.\n→ P1 intercepts (cache-to-cache transfer), writes back to memory or flushes.\n→ P1 transitions **M → S**.\n→ P2 transitions **I → S**.\n\n### Transition Matrix (MESI)\n\n| Current State | Read Hit | Read Miss | Write Hit | Write Miss |\n|---------------|----------|-----------|-----------|------------|\n| **M** | M | M→S (flush) | M | M→M (invalidate others) |\n| **E** | E | E→S | E→M | E→S (others invalidate) |\n| **S** | S | S | S→M (invalidate) | S→M (invalidate) |\n| **I** | I→S/E (fetch) | I→S/E | I→M (invalidate) | I→M |\n\n---\n\n## 3.3 Average Memory Access Time (AMAT) for UMA\n\nSuppose a cache has:\n- Hit time $H = 2$ ns\n- Miss penalty $M = 100$ ns\n- Miss rate $MR = 0.05$\n\n$$\nAMAT = H + MR \\cdot M = 2 + 0.05 \\cdot 100 = 2 + 5 = 7 \\text{ ns}\n$$\n\nIf the coherence protocol adds **extra invalidation traffic** averaging $M_{coh} = 20$ ns per miss:\n\n$$\nAMAT_{coh} = H + MR \\cdot (M + M_{coh}) = 2 + 0.05 \\cdot 120 = 2 + 6 = 8 \\text{ ns}\n$$\n\n> **Insight:** The AMAT penalty from coherence traffic is a real bottleneck in UMA buses and grows non-linearly with the number of processors.\n\n---\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4 — Structural Diagrams & Schematics\n\n## 4.1 Flynn's Taxonomy — Hierarchical Block View\n\n```mermaid\ngraph TD\n    root[\"Parallel Computing Paradigms\"]\n    root --> sisd[\"SISD - Single Instruction Single Data\"]\n    root --> simd[\"SIMD - Single Instruction Multiple Data\"]\n    root --> misd[\"MISD - Multiple Instruction Single Data\"]\n    root --> mimd[\"MIMD - Multiple Instruction Multiple Data\"]\n\n    sisd --> sisdEx[\"Example Desktop PC\"]\n    simd --> simdEx[\"Example GPU Vector Unit\"]\n    misd --> misdEx[\"Example Fault Tolerant Pipeline\"]\n    mimd --> smp[\"Shared Memory SMP UMA NUMA\"]\n    mimd --> dmem[\"Distributed Memory Clusters MPP\"]\n```\n\n## 4.2 UMA Shared-Memory Architecture — Block-Level Functional Flow\n\n```mermaid\ngraph TD\n    p1[\"Processor P1\"] --> c1[\"L1 Cache P1\"]\n    p2[\"Processor P2\"] --> c2[\"L1 Cache P2\"]\n    p3[\"Processor P3\"] --> c3[\"L1 Cache P3\"]\n    p4[\"Processor P4\"] --> c4[\"L1 Cache P4\"]\n\n    c1 --> bus[\"Shared Bus or Crossbar Switch\"]\n    c2 --> bus\n    c3 --> bus\n    c4 --> bus\n\n    bus --> m1[\"Memory Module M1\"]\n    bus --> m2[\"Memory Module M2\"]\n    bus --> m3[\"Memory Module M3\"]\n    bus --> m4[\"Memory Module M4\"]\n\n    bus --> cohCtrl[\"Snoop Controller Coherence Logic\"]\n```\n\n> **Note:** Every P-to-M path traverses the same `bus` node → uniform latency by construction.\n\n## 4.3 MESI Coherence State Machine — Block Flow\n\n```mermaid\ngraph LR\n    inv[\"State INVALID\"] -->|Read Miss BusRd| shr[\"State SHARED\"]\n    inv -->|Write Miss BusRdX| mod[\"State MODIFIED\"]\n\n    shr -->|Read Hit| shr\n    shr -->|Write Hit BusUpgr| mod\n    shr -->|Snoop Invalidate| inv\n\n    mod -->|Read Hit| mod\n    mod -->|Read Miss Flush| shr\n    mod -->|Write Hit| mod\n\n    exc[\"State EXCLUSIVE\"] -->|Write| mod\n    exc -->|Read Shared| shr\n    shr -->|BusRdX receives| inv\n```\n\n## 4.4 UMA Bus Transaction — Snooping Mechanism Flow\n\n```mermaid\nsequenceDiagram\n    participant P1 as Processor P1\n    participant Cache1 as L1 Cache P1\n    participant Bus as Shared Bus\n    participant Cache2 as L1 Cache P2\n    participant Mem as Main Memory\n\n    P1->>Cache1: WRITE to Address A\n    Cache1->>Bus: BusUpgr Invalidate\n    Bus->>Cache2: Snoop Invalidate Address A\n    Cache2-->>Cache2: Set state INVALID\n    Cache1->>Cache1: Set state MODIFIED\n    Cache1->>Bus: Data Write Through Optional\n    Bus->>Mem: Update Memory Copy\n```\n\n## 4.5 UMA vs NUMA — Comparative Block View\n\n```mermaid\ngraph TD\n    uma[\"UMA Symmetric Multiprocessor\"]\n    numa[\"NUMA Distributed Shared Memory\"]\n\n    uma --> umaFeat1[\"Single Bus or Crossbar\"]\n    uma --> umaFeat2[\"Equal Latency Everywhere\"]\n    uma --> umaFeat3[\"Snooping Coherence\"]\n    uma --> umaFeat4[\"Scale Up to 64 CPUs\"]\n\n    numa --> numaFeat1[\"Interconnection Network\"]\n    numa --> numaFeat2[\"Local Fast Remote Slow\"]\n    numa --> numaFeat3[\"Directory Based Coherence\"]\n    numa --> numaFeat4[\"Scale Up to Thousands of CPUs\"]\n```\n\n---\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. `[KTU University Exam — May 2024]`\n**Differentiate between UMA and NUMA architectures with suitable examples.**\n\n**Model Answer:**\n\n| Parameter | UMA | NUMA |\n|-----------|-----|------|\n| Memory access time | Uniform for all CPUs | Non-uniform (local fast, remote slow) |\n| Interconnect | Shared bus / crossbar | Distributed network (mesh, torus) |\n| Coherence | Snooping | Directory-based |\n| Scalability | Limited (~64) | High (~thousands) |\n| Example | Older dual-socket Xeon servers | AMD EPYC, Intel Xeon Scalable, SGI Origin |\n\n> **Board Hint:** Always tabulate for **3-mark difference questions** — examiners reward clarity.\n\n---\n\n### Q2. `[KTU University Exam — Dec 2023]`\n**Explain the cache coherence problem with a simple example.**\n\n**Model Answer:**\nConsider two processors **P1** and **P2**, both having the variable `X = 5` cached.\n1. P1 executes `X = X + 1` → its cache now shows `X = 6`, but P2's cache still shows `X = 5`.\n2. P2 reads `X` → it gets the **stale value 5** instead of 6 → incorrect result.\n\nThis inconsistency is the **cache coherence problem**. It is solved using **snooping** or **directory-based protocols** that ensure write propagation and write serialization.\n\n---\n\n## Part B — Long Answer Questions (14 Marks Each, Internal Choice)\n\n### QUESTION A — `[KTU University Exam — Dec 2023]`\n**(a) [7 Marks]** Explain **Flynn's Taxonomy** of parallel computers. Discuss SIMD and MIMD in detail with examples.\n\n**(b) [7 Marks]** With a neat diagram, explain the **UMA (Uniform Memory Access) architecture**. Discuss its merits and demerits.\n\n---\n\n### QUESTION A — Model Solution\n\n#### (a) Flynn's Taxonomy [7 Marks]\n\nFlynn (1966) classified computer architectures based on the number of **instruction streams** and **data streams** executed concurrently.\n\n**Four classes:**\n\n1. **SISD — Single Instruction Single Data**\n   - Classical von-Neumann uniprocessor.\n   - No inherent parallelism; pipelining/ILP only.\n   - Example: Personal desktop.\n\n2. **SIMD — Single Instruction Multiple Data**\n   - One control unit broadcasts the same instruction to many ALUs.\n   - Each ALU operates on a different data element.\n   - Best for array/matrix operations.\n   - Examples: NVIDIA GPU CUDA cores, Intel SSE/AVX units, Cray-1 vector processor.\n\n3. **MISD — Multiple Instruction Single Data**\n   - Multiple instructions act on the same data stream.\n   - Used for fault-tolerant systems and systolic arrays.\n   - Example: Space Shuttle flight control computers.\n\n4. **MIMD — Multiple Instruction Multiple Data**\n   - Each processor independently fetches and executes its own instructions on its own data.\n   - Most general and commercially dominant.\n   - Subdivided into:\n     - **Shared-Memory MIMD** (SMP/UMA, NUMA)\n     - **Distributed-Memory MIMD** (Clusters, MPP)\n   - Examples: Multicore CPUs, supercomputers like Sunway TaihuLight.\n\n#### (b) UMA Architecture [7 Marks]\n\n**Uniform Memory Access (UMA)** is a shared-memory MIMD architecture where:\n- All $N$ processors connect to $M$ memory modules through a **shared bus or crossbar**.\n- **Access time is identical** for every (processor, memory) pair.\n- Each processor maintains its **own L1/L2 cache**.\n- **Cache coherence** is maintained by **snooping** the shared bus.\n\n**Diagram (refer Section 4.2):** P1, P2, P3, P4 → shared bus → M1, M2, M3, M4 with snoop controller.\n\n**Merits:**\n- Simple programming model (single address space).\n- Good for small-to-medium workloads.\n- Easy cache coherence via snooping.\n\n**Demerits:**\n- **Bus contention** limits scalability to ~64 CPUs.\n- Adding CPUs degrades performance due to coherence traffic.\n- Memory bandwidth is the bottleneck.\n\n---\n\n### QUESTION B — `[KTU University Exam — May 2024]`\n**(a) [7 Marks]** Describe the **MESI cache coherence protocol** with all four states and their transitions.\n\n**(b) [7 Marks]** State and derive **Amdahl's Law**. A parallel application has a serial fraction of 0.1. Compute the maximum achievable speedup.\n\n---\n\n### QUESTION B — Model Solution\n\n#### (a) MESI Cache Coherence Protocol [7 Marks]\n\nMESI is a **write-invalidate snooping protocol** where every cache line is in one of four states:\n\n| State | Meaning |\n|-------|---------|\n| **M — Modified** | Line is dirty, exclusive to this cache. Must write back before others read. |\n| **E — Exclusive** | Line is clean, exclusive to this cache. No other cache has a copy. |\n| **S — Shared** | Line is clean. One or more caches may have a copy. |\n| **I — Invalid** | Line is not present in this cache. |\n\n**Transitions (key cases):**\n- `I → S`: on a read miss where other caches also have the line.\n- `I → E`: on a read miss where no other cache has the line.\n- `E → M`: on a write hit (silent transition — no bus traffic).\n- `S → M`: on a write hit, must broadcast **BusUpgr** to invalidate others.\n- `M → S`: on a read miss by another processor (cache-to-cache flush).\n\n**Why MESI?** Reduces bus traffic by using the `E` state — a silent exclusive-to-modified transition avoids unnecessary invalidation broadcasts. Used in all modern x86 CPUs.\n\n#### (b) Amdahl's Law [7 Marks]\n\n**Statement:** The speedup of a parallel program is limited by its serial (non-parallelizable) fraction. Even with infinite processors, the maximum speedup equals $\\frac{1}{f}$.\n\n**Derivation:**\n\nLet total serial time be $T_s$, normalized to $1$. Let $f$ be the serial fraction and $(1 - f)$ the parallel fraction.\n\nOn $N$ processors, the parallel portion is divided equally:\n\n$$\nT_p = f \\cdot T_s + \\dfrac{(1 - f) \\cdot T_s}{N}\n$$\n\n$$\nS(N) = \\dfrac{T_s}{T_p} = \\dfrac{1}{f + \\dfrac{1 - f}{N}}\n$$\n\nAs $N \\to \\infty$:\n\n$$\nS_{\\max} = \\dfrac{1}{f}\n$$\n\n**Numerical computation (f = 0.1):**\n\n$$\nS_{\\max} = \\dfrac{1}{0.1} = 10\n$$\n\n> **Conclusion:** With 10% serial code, **no matter how many processors are added, speedup cannot exceed 10×**.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> 1. **Forgetting to mention \"Single Address Space\"** in shared-memory definitions → lose 1 mark.\n> 2. **Confusing MESI with MSI** — examiners deduct marks if `Exclusive` state is omitted.\n> 3. **Amdahl's Law without normalization** — must set $T_s = 1$ before deriving.\n> 4. **Confusing UMA with NUMA** — NUMA = *Non-Uniform*; don't write \"NUMA = Uniform\".\n> 5. **Skipping the diagram in UMA** — 14-mark questions without a diagram lose at least 2 marks.\n> 6. **Forgetting the limit case** $N \\to \\infty$ in Amdahl's derivation → lose 1 mark.\n\n---\n\n## Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid-Revision Checklist for Module 2:**\n\n- **Flynn's Taxonomy** has 4 classes: **SISD, SIMD, MISD, MIMD**.\n- **MIMD** is the parent of both **shared-memory** and **distributed-memory** systems.\n- **Shared memory** = single address space; **Distributed memory** = message passing.\n- **UMA** (SMP) = uniform access time via shared bus/crossbar; scalability ~64.\n- **NUMA** = non-uniform access; scales to thousands; needs directory coherence.\n- **Cache Coherence** = ensuring stale copies are invalidated/updated; two pillars: **write propagation** and **write serialization**.\n- **Snooping protocols** scale poorly; **directory protocols** scale better.\n- **MESI** states: **Modified, Exclusive, Shared, Invalid**.\n- **Amdahl's Law:** $S_{\\max} = \\dfrac{1}{f}$ — serial fraction is the hard ceiling.\n- **Speedup** $S = \\dfrac{T_s}{T_p}$ ; **Efficiency** $E = \\dfrac{S}{N}$.\n- **AMAT** = $H + MR \\cdot M$ — coherence traffic increases miss penalty.\n- **Gustafson's Law** assumes scaled problem size → $S = N - f(N-1)$.\n- Real-world examples to memorize: **Intel/AMD CPUs (MESI), NVIDIA GPUs (SIMD), Clusters (MPI)**.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "ccnuma": {
      "title": "ccNUMA",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 2\n> - **Topic:** ccNUMA\n\n<!-- SECTION_1_START -->\n# SECTION 1 — Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\n**ccNUMA (Cache-Coherent Non-Uniform Memory Access)** is a shared-memory multiprocessor architecture in which the physical memory is **distributed across multiple processor nodes**, yet appears as a **single, unified address space** to all processors. The defining feature of ccNUMA is that:\n\n- **Memory access latency depends on the \"distance\"** (in hops through the interconnect) between the requesting processor and the memory controller serving the target address — hence **Non-Uniform Memory Access (NUMA)**.\n- A **hardware-supported cache coherence protocol** (typically directory-based) maintains the **single-image view** of memory at the cache level — hence the prefix **cc (Cache Coherent)**.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Definition:** ccNUMA is a multiprocessor memory design where each processor has its own local memory, all memories form a single shared address space, and hardware coherence ensures that any cached copy of a line is consistent across the entire system.\n\n---\n\n## 1.2 Conceptual Analogy — The \"National Library Chain\"\n\nImagine a **National Library System** spread across many cities in India (Chennai, Kochi, Delhi, Mumbai…):\n\n| Real-World Element | ccNUMA Equivalent |\n|---|---|\n| Multiple city libraries | Distributed memory banks on each node |\n| Single unified catalog across India | Single global physical address space |\n| Borrowing a book from your local branch is **fast** | Accessing **local memory** = low latency |\n| Borrowing from another city takes days via inter-library loan | Accessing **remote memory** = higher latency |\n| A central ITR (Inter-Library Tracking Registry) updates who has which book | **Directory-based coherence protocol** tracks cache line ownership |\n| Patrons always see updated availability | Processors always see consistent cached data |\n\nThe **\"distance penalty\"** is the real-world equivalent of NUMA's **non-uniform access time**, and the central tracking registry mimics the role of the **coherence directory**.\n\n> [!NOTE]\n> **Key Takeaway:** ccNUMA achieves the **programming simplicity of shared memory** (like SMP/UMA) while delivering the **scalability of distributed memory** (like clusters). It is the dominant architecture in modern multi-socket servers (Intel Xeon, AMD EPYC, IBM Power).\n\n---\n\n## 1.3 Physical Constants & Standard Metrics\n\n| Metric | Symbol / Unit | Typical Value |\n|---|---|---|\n| Local memory access latency | $T_{local}$ | **~50–100 ns** |\n| Remote memory access latency | $T_{remote}$ | **~150–400 ns** |\n| NUMA ratio | $N_r = T_{remote}/T_{local}$ | **1.3 to 3.0** |\n| Node interconnect bandwidth | $B_{inter}$ | **20–100 GB/s** (e.g., Intel UPI, AMD Infinity Fabric) |\n| Coherence directory entry size | bits per cache line | **~4–10 bits** |\n| Maximum supported processor count | N | **8 to 256+ sockets** |\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2 — Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Architectural Breakdown\n\nA ccNUMA system has **three logical layers** that students must master:\n\n### Layer A — The Compute Node\n- Each node contains **2 to 8 processor cores** (often a multi-core CPU).\n- Each processor has **L1, L2, L3 caches** (with L3 often shared across cores).\n- Each node has **local DRAM** attached via an integrated memory controller (IMC).\n- The node is connected to the rest of the system via a **high-speed point-to-point interconnect** (Intel UPI/QPI, AMD Infinity Fabric, HyperTransport, or custom).\n\n### Layer B — The Interconnect Network\n- A **scalable, low-latency fabric** (often a **fat-tree**, **torus**, or **hypercube**) connects all nodes.\n- Modern systems use **directory-based protocols** (MESI/MOESI modified variants) because snoop-based protocols do not scale well beyond 4–8 sockets.\n- The interconnect carries three message types: **data requests**, **data responses**, and **invalidation/snoops**.\n\n### Layer C — The Coherence Directory\n- A **distributed directory** (one slice per node) tracks the **sharing status** of every memory line owned by that node.\n- Directory entries typically store: *owner node ID*, *sharer bit-vector*, and *state bits* (Modified, Shared, Invalid, Exclusive — the MESI family).\n- When a cache miss occurs, the home node consults its directory to decide whether to forward the request to the current owner, broadcast invalidations, or supply the line directly.\n\n---\n\n## 2.2 KTU High-Yield Formula Sheet\n\n> [!IMPORTANT]\n> **Cheat Sheet — Master These Equations for KTU ESE**\n\n| # | Concept | Formula / Expression | Units |\n|---|---|---|---|\n| 1 | Average memory access time (general) | $T_{avg} = \\sum_i p_i \\cdot T_i$ | seconds (s) |\n| 2 | NUMA access time (two-tier) | $T_{NUMA} = f \\cdot T_{local} + (1-f) \\cdot T_{remote}$ | s |\n| 3 | NUMA penalty ratio | $N_r = \\dfrac{T_{remote}}{T_{local}}$ | dimensionless |\n| 4 | Remote access amplification | $T_{remote} = T_{local} + h \\cdot t_{hop} + t_{dir}$ | s |\n| 5 | Total memory bandwidth (NUMA) | $B_{sys} = \\sum_{i=1}^{N} B_{local,i}$ | bytes/s |\n| 6 | Effective bandwidth with locality $f$ | $B_{eff} = B_{local} \\cdot f + B_{remote} \\cdot (1-f)$ | bytes/s |\n| 7 | Speedup bound (Amdahl, NUMA-aware) | $S = \\dfrac{1}{(1-\\alpha) + \\dfrac{\\alpha}{N} \\cdot \\left[ f \\cdot r_l + (1-f) \\cdot r_r \\right]}$ | dimensionless |\n| 8 | Directory storage overhead | $S_{dir} = N \\cdot L \\cdot b_{dir}$ | bits |\n| 9 | Coherence message count (read miss) | $M = 2 + k_{sharers}$ | messages |\n| 10 | Mean hop count (hypercube, $N=2^d$ nodes) | $\\bar{H} = \\dfrac{d \\cdot 2^{d-1}}{2^d - 1}$ | hops |\n\n**Legend of variables used:**\n- $f$ = fraction of memory accesses satisfied locally (locality factor), $0 \\le f \\le 1$\n- $h$ = number of interconnect hops to remote node\n- $t_{hop}$ = per-hop latency (s)\n- $t_{dir}$ = directory lookup latency (s)\n- $\\alpha$ = parallelizable fraction of workload\n- $r_l, r_r$ = relative slowdown factors for local/remote (ratio to ideal)\n- $N$ = number of processor nodes\n- $L$ = number of memory lines per node\n- $b_{dir}$ = directory bits per line (≈ presence-vector length + state bits)\n- $k_{sharers}$ = number of nodes currently caching the line\n\n---\n\n## 2.3 Why ccNUMA Matters in Real-World Engineering\n\n| Field | Application | Why ccNUMA Wins |\n|---|---|---|\n| **HPC / Scientific Simulation** | Weather forecasting (WRF), CFD (ANSYS Fluent) | Scales to thousands of cores with single programming model |\n| **Database Servers** | SAP HANA, Oracle Exadata | Predictable low latency for transaction processing |\n| **AI / Deep Learning Training** | Multi-GPU training of LLMs (GPT-class) | Shared memory simplifies parameter-server patterns |\n| **In-Memory Analytics** | Apache Spark on large-memory NUMA boxes | Avoids the network bottlenecks of pure clusters |\n| **Aerospace & Defense** | Real-time signal processing | Deterministic local-access performance |\n\n> [!NOTE]\n> The **engineering trade-off** is clear: ccNUMA gives **programmability and coherence for free in hardware** but pays a **remote-access penalty** that software (OS scheduler + memory allocator) must minimize by **thread-and-data placement**.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3 — Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation 1 — Average Memory Access Time in a ccNUMA System\n\n**Problem Statement (KTU-style):** A ccNUMA machine has local memory access latency $T_{local} = 80$ ns and remote memory access latency $T_{remote} = 240$ ns. If a parallel program running on a node makes $70\\%$ of its memory references to local memory and the rest to remote memory, compute the average memory access time $T_{avg}$.\n\n### Step 1 — Identify the access locality\nWe are given that the fraction of local accesses is $f = 0.70$.\nTherefore, the fraction of remote accesses is $1 - f = 1 - 0.70 = 0.30$.\n\n### Step 2 — Apply the two-tier NUMA access-time model\n\n$$\n\\begin{aligned}\nT_{avg} &= f \\cdot T_{local} + (1-f) \\cdot T_{remote}\n\\end{aligned}\n$$\n\n> The **two-tier model** assumes every access is either fully local or fully remote — there is no partial cache hit considered here. We are working at the memory-controller level.\n\n### Step 3 — Substitute numerical values\n\n$$\n\\begin{aligned}\nT_{avg} &= (0.70) \\cdot (80 \\text{ ns}) + (0.30) \\cdot (240 \\text{ ns})\n\\end{aligned}\n$$\n\n### Step 4 — Perform the multiplications\n\n$$\n\\begin{aligned}\n(0.70)(80) &= 56 \\text{ ns} \\\\\n(0.30)(240) &= 72 \\text{ ns}\n\\end{aligned}\n$$\n\n### Step 5 — Add the two contributions\n\n$$\n\\begin{aligned}\nT_{avg} &= 56 \\text{ ns} + 72 \\text{ ns} = 128 \\text{ ns}\n\\end{aligned}\n$$\n\n### Step 6 — Compare against the uniform case\nA purely UMA machine with $T_{UMA} = 80$ ns would have given $80$ ns. The **NUMA penalty** is therefore:\n\n$$\n\\begin{aligned}\nP_{NUMA} &= \\frac{T_{avg} - T_{UMA}}{T_{UMA}} \\times 100\\% \\\\\n&= \\frac{128 - 80}{80} \\times 100\\% \\\\\n&= 60\\%\n\\end{aligned}\n$$\n\n**Result:** Average memory access time = **128 ns**, representing a **60% slowdown** versus an ideal UMA system.\n\n---\n\n## 3.2 Derivation 2 — NUMA Penalty Ratio and Required Locality\n\n**Problem Statement:** A ccNUMA server has $T_{local} = 100$ ns and $T_{remote} = 350$ ns. What minimum fraction $f$ of local accesses must a program maintain so that the average access time does not exceed $150$ ns?\n\n### Step 1 — Set up the inequality\n\n$$\n\\begin{aligned}\nf \\cdot T_{local} + (1-f) \\cdot T_{remote} &\\le T_{target}\n\\end{aligned}\n$$\n\n### Step 2 — Plug in known values\n\n$$\n\\begin{aligned}\nf \\cdot 100 + (1-f) \\cdot 350 &\\le 150\n\\end{aligned}\n$$\n\n### Step 3 — Expand the left-hand side\n\n$$\n\\begin{aligned}\n100f + 350 - 350f &\\le 150 \\\\\n-250f + 350 &\\le 150\n\\end{aligned}\n$$\n\n### Step 4 — Isolate $f$\n\n$$\n\\begin{aligned}\n-250f &\\le 150 - 350 \\\\\n-250f &\\le -200\n\\end{aligned}\n$$\n\n### Step 5 — Divide and flip the inequality sign\n\n$$\n\\begin{aligned}\nf &\\ge \\frac{-200}{-250} \\\\\nf &\\ge 0.80\n\\end{aligned}\n$$\n\n**Result:** The program must keep **at least $80\\%$ of its accesses local** to maintain the target 150 ns average.\n\n---\n\n## 3.3 Derivation 3 — Speedup Bound Under NUMA Latency (Amdahl + NUMA)\n\n**Problem Statement:** A parallel program is $90\\%$ parallelizable ($\\alpha = 0.90$) and runs on $N = 16$ nodes. The remote access is $r_r = 2.5$ times slower than ideal local access ($r_l = 1.0$). Compute the realistic speedup.\n\n### Step 1 — Recall Amdahl's Law for parallel fraction\n\n$$\n\\begin{aligned}\nS_{ideal} &= \\frac{1}{(1-\\alpha) + \\dfrac{\\alpha}{N}}\n\\end{aligned}\n$$\n\n### Step 2 — Plug in $\\alpha = 0.90$, $N = 16$\n\n$$\n\\begin{aligned}\nS_{ideal} &= \\frac{1}{(1-0.90) + \\dfrac{0.90}{16}} \\\\\n&= \\frac{1}{0.10 + 0.05625} \\\\\n&= \\frac{1}{0.15625} \\\\\n&\\approx 6.40\n\\end{aligned}\n$$\n\n### Step 3 — Incorporate the NUMA slowdown into the parallel portion\nThe parallel fraction runs at speed $\\dfrac{1}{f \\cdot r_l + (1-f) \\cdot r_r}$. Taking the locality from earlier as $f = 0.80$:\n\n$$\n\\begin{aligned}\nS_{remote} &= \\frac{1}{f \\cdot r_l + (1-f) \\cdot r_r} \\\\\n&= \\frac{1}{0.80 \\cdot 1.0 + 0.20 \\cdot 2.5} \\\\\n&= \\frac{1}{0.80 + 0.50} \\\\\n&= \\frac{1}{1.30} \\\\\n&\\approx 0.769\n\\end{aligned}\n$$\n\n### Step 4 — Compose with the serial fraction\n\n$$\n\\begin{aligned}\nS_{NUMA} &= \\frac{1}{(1-\\alpha) + \\dfrac{\\alpha}{N} \\cdot \\dfrac{1}{S_{remote}}}\n\\end{aligned}\n$$\n\n### Step 5 — Substitute values\n\n$$\n\\begin{aligned}\nS_{NUMA} &= \\frac{1}{0.10 + \\dfrac{0.90}{16} \\cdot \\dfrac{1}{0.769}} \\\\\n&= \\frac{1}{0.10 + 0.05625 \\cdot 1.30} \\\\\n&= \\frac{1}{0.10 + 0.0731} \\\\\n&= \\frac{1}{0.1731} \\\\\n&\\approx 5.78\n\\end{aligned}\n$$\n\n### Step 6 — Compare\nIdeal UMA speedup: $S_{ideal} \\approx 6.40$  \nNUMA-aware speedup: $S_{NUMA} \\approx 5.78$  \nLoss due to non-uniform access: $\\approx 9.7\\%$\n\n---\n\n## 3.4 Analytical Matrix — ccNUMA vs Alternatives\n\n| Property | UMA (SMP) | ccNUMA | Distributed (Cluster, MPI) |\n|---|---|---|---|\n| Memory model | Shared | Shared (distributed) | Distributed (message passing) |\n| Access time | Uniform | Non-uniform | Non-uniform + explicit |\n| Coherence | Hardware (bus snooping) | Hardware (directory) | None (software-managed) |\n| Scalability | 8–32 cores | 64–2048 cores | 100,000+ cores |\n| Programming model | Threads | Threads (NUMA-aware) | MPI / SHMEM |\n| OS page migration | Not applicable | Yes (auto-NUMA balancing) | Not applicable |\n| Example hardware | Older dual-socket Xeon | SGI Origin, AMD EPYC, Intel Xeon Scalable | Top500 supercomputers |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4 — Structural Diagrams & Schematics\n\n## 4.1 Block-Level Architecture of a ccNUMA System\n\n```mermaid\nflowchart TB\n    subgraph NodeA[\"NODE 0\"]\n        CPU0A[\"CPU Core 0\"]\n        CPU1A[\"CPU Core 1\"]\n        L2A[\"L2 / L3 Cache\"]\n        MCA[\"Memory Controller\"]\n        RAMA[\"Local DRAM Bank A\"]\n        DIRA[\"Directory Slice A\"]\n    end\n\n    subgraph NodeB[\"NODE 1\"]\n        CPU0B[\"CPU Core 0\"]\n        CPU1B[\"CPU Core 1\"]\n        L2B[\"L2 / L3 Cache\"]\n        MCB[\"Memory Controller\"]\n        RAMB[\"Local DRAM Bank B\"]\n        DIRB[\"Directory Slice B\"]\n    end\n\n    subgraph NodeC[\"NODE 2\"]\n        CPU0C[\"CPU Core 0\"]\n        CPU1C[\"CPU Core 1\"]\n        L2C[\"L2 / L3 Cache\"]\n        MCC[\"Memory Controller\"]\n        RAMC[\"Local DRAM Bank C\"]\n        DIRC[\"Directory Slice C\"]\n    end\n\n    IC[\"High-Speed Interconnect Fabric (UPI / Infinity Fabric / HyperTransport)\"]\n\n    NodeA <--> IC\n    NodeB <--> IC\n    NodeC <--> IC\n\n    CPU0A --> L2A --> MCA\n    CPU1A --> L2A\n    MCA --- RAMA\n    MCA --- DIRA\n\n    CPU0B --> L2B --> MCB\n    CPU1B --> L2B\n    MCB --- RAMB\n    MCB --- DIRB\n\n    CPU0C --> L2C --> MCC\n    CPU1C --> L2C\n    MCC --- RAMC\n    MCC --- DIRC\n```\n\n## 4.2 Directory-Based Coherence Flow (Read Miss on Remote Line)\n\n```mermaid\nsequenceDiagram\n    participant P1 as CPU on Node 0\n    participant L1 as Node 0 Cache\n    participant IC as Interconnect\n    participant DIR as Directory on Home Node 1\n    participant RAM as Memory Controller Node 1\n    participant P2 as CPU on Node 1\n\n    P1->>L1: Load Address X\n    L1-->>P1: Cache Miss (line not present)\n    L1->>IC: Read Request for X\n    IC->>DIR: Forward to Home Node (X lives on Node 1)\n    DIR->>DIR: Check directory state for X\n    DIR->>P2: Forward request (if cached Modified)\n    P2-->>IC: Data + Ack (cache-to-cache transfer)\n    IC-->>L1: Supply line X\n    L1-->>P1: Return data to CPU\n    DIR->>DIR: Update sharer bit-vector (add Node 0)\n```\n\n## 4.3 Write / Invalidation Flow (Write Hit on Shared Line)\n\n```mermaid\nsequenceDiagram\n    participant P1 as CPU on Node 0\n    participant L1 as Node 0 Cache\n    participant IC as Interconnect\n    participant DIR as Directory on Home Node\n    participant P2 as CPU on Node 1\n    participant P3 as CPU on Node 2\n\n    P1->>L1: Store to Address Y (in Shared state)\n    L1->>IC: Invalidate Request (Exclusive upgrade)\n    IC->>DIR: Forward to Home Node\n    DIR->>P2: Send Invalidate\n    DIR->>P3: Send Invalidate\n    P2-->>IC: Invalidate Ack\n    P3-->>IC: Invalidate Ack\n    IC-->>L1: All acks received\n    L1->>P1: Store completes, line now Modified\n    DIR->>DIR: Update state to Exclusive, owner = Node 0\n```\n\n## 4.4 Memory Access Latency Hierarchy in ccNUMA\n\n```mermaid\ngraph LR\n    L1[\"L1 Cache Hit<br/>~1 ns\"]\n    L2[\"L2 Cache Hit<br/>~3-5 ns\"]\n    L3[\"L3 Cache Hit<br/>~10-20 ns\"]\n    LOCAL[\"Local DRAM Access<br/>~50-100 ns\"]\n    REMOTE[\"Remote DRAM Access<br/>~150-400 ns<br/>via interconnect + directory\"]\n\n    L1 --> L2 --> L3 --> LOCAL --> REMOTE\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 PART A — Short Answer Questions (3 Marks Each)\n\n### Question A1 `[KTU University Exam – May 2024]`\n**Q: Define ccNUMA. Why is it called \"non-uniform\"?**\n\n**Model Answer (3 marks):**\nccNUMA stands for **Cache-Coherent Non-Uniform Memory Access**. It is a multiprocessor architecture in which physical memory is distributed across multiple nodes but exposed as a single shared address space, with hardware-maintained cache coherence.\nIt is called **\"non-uniform\"** because the memory access latency depends on the physical location of the memory relative to the requesting processor — **local memory** (attached to the same node) is accessed faster than **remote memory** (attached to a different node and reached through the interconnect).\n**(1 mark for definition, 1 mark for non-uniform justification, 1 mark for cache coherence aspect.)**\n\n---\n\n### Question A2 `[KTU University Exam – Dec 2023]`\n**Q: What is the role of a directory in a ccNUMA coherence protocol? Mention any one directory-based protocol.**\n\n**Model Answer (3 marks):**\nA **directory** is a distributed data structure (one slice per home node) that keeps track of which nodes have currently cached copies of each memory line and in what state (Modified / Shared / Invalid / Exclusive).\nWhen a request arrives, the home-node directory decides whether to:\n- Supply the data directly from memory, OR\n- Forward the request to the current owner node, AND\n- Send invalidations to other sharers.\n\nAny one standard protocol: **MESI** (Modified, Exclusive, Shared, Invalid) or **MOESI** (adds Owned state for cache-to-cache transfer without writing back to memory).\n**(1 mark for directory purpose, 1 mark for protocol name, 1 mark for brief description.)**\n\n---\n\n## 5.2 PART B — Long Answer Questions (14 Marks, Internal Choice)\n\n### Question B-A `[KTU University Exam – May 2024, Module 2]`\n\n**(a) [7 Marks]** Explain the **architecture of a ccNUMA system** with a neat block diagram. Differentiate between **local** and **remote** memory access with respect to latency and the path traversed.\n\n**(b) [7 Marks]** A ccNUMA system has local memory latency of **100 ns** and remote memory latency of **300 ns**. A parallel application shows **75% local references** and **25% remote references**.\n   (i) Compute the average memory access time.  \n   (ii) Compute the NUMA penalty compared to a uniform-memory machine of 100 ns.  \n   (iii) What fraction of accesses must be local to bring the average below 130 ns?\n\n---\n\n#### Model Solution for B-A (a):\n\n**Architecture (4 marks):**\n- Each node contains processor cores, private/shared caches, a **memory controller**, **local DRAM**, and a **directory slice**.\n- All nodes are interconnected through a **high-bandwidth, low-latency fabric** (Intel UPI / AMD Infinity Fabric / HyperTransport).\n- The **directory** is distributed — each home node owns the metadata for its own memory lines.\n- The system presents a **single global physical address space** to all processors.\n\n**Local vs Remote Access (3 marks):**\n\n| Aspect | Local Access | Remote Access |\n|---|---|---|\n| Path | CPU → Cache → Local MC → Local DRAM | CPU → Cache → Local MC → Interconnect → Remote MC → Remote DRAM (and may consult remote directory) |\n| Latency | ~50–100 ns | ~150–400 ns |\n| Coherence check | Local cache only | Directory lookup + possible invalidations |\n| Bandwidth | Dedicated to local DRAM | Shared across the interconnect |\n\n---\n\n#### Model Solution for B-A (b):\n\n**(i) Average memory access time:**\n\n$$\n\\begin{aligned}\nT_{avg} &= f \\cdot T_{local} + (1-f) \\cdot T_{remote} \\\\\n&= (0.75)(100) + (0.25)(300) \\\\\n&= 75 + 75 \\\\\n&= 150 \\text{ ns}\n\\end{aligned}\n$$\n\n**(ii) NUMA penalty:**\n\n$$\n\\begin{aligned}\nP_{NUMA} &= \\frac{T_{avg} - T_{local}}{T_{local}} \\times 100\\% \\\\\n&= \\frac{150 - 100}{100} \\times 100\\% \\\\\n&= 50\\%\n\\end{aligned}\n$$\n\n**(iii) Required locality for $T_{avg} \\le 130$ ns:**\n\n$$\n\\begin{aligned}\nf \\cdot 100 + (1-f) \\cdot 300 &\\le 130 \\\\\n100f + 300 - 300f &\\le 130 \\\\\n-200f &\\le -170 \\\\\nf &\\ge 0.85\n\\end{aligned}\n$$\n\nTherefore, the program must maintain at least **85% local references**.\n\n---\n\n### Question B-B `[KTU University Exam – Dec 2023, Module 2 – Alternative]`\n\n**(a) [7 Marks]** Discuss the **cache coherence problem in distributed shared memory** systems. Explain how a **directory-based protocol** solves it. Compare it briefly with the **snoopy bus protocol**.\n\n**(b) [7 Marks]** A 4-node ccNUMA system has the following per-hop interconnect characteristics: hop latency = **40 ns**, directory lookup latency = **30 ns**, local DRAM access = **60 ns**.\n   - (i) Compute the latency for a remote access that traverses **2 hops**, assuming a clean line read from a remote node's memory.\n   - (ii) If the directory already has the data cached on a remote node (1 hop away), compute the cache-to-cache transfer latency.\n   - (iii) Calculate the speedup ratio between local and remote (clean memory) accesses.\n\n---\n\n#### Model Solution for B-B (a):\n\n**Cache Coherence Problem (3 marks):**\nWhen multiple processors cache the same memory line, **writes by one processor** can leave other caches holding **stale values**, violating memory consistency. Without coherence, programs can produce incorrect results (e.g., two processors updating the same shared counter and reading stale values).\n\n**Directory-Based Solution (2 marks):**\n- Each memory line has a **directory entry** at its home node storing the *owner* and *sharer set*.\n- On a write, the home node consults the directory and sends **invalidation messages** to all current sharers.\n- The writer is guaranteed exclusive ownership before completing the store.\n- Protocols: **MESI**, **MOESI**, **Directory-based Illinois**.\n\n**Comparison with Snoopy Bus (2 marks):**\n\n| Feature | Snoopy Bus | Directory-Based |\n|---|---|---|\n| Coherence traffic | Broadcast to all nodes | Point-to-point via directory |\n| Scalability | Limited (~4–8 processors) | Scales to thousands |\n| Bandwidth requirement | High (shared bus) | Moderate (distributed) |\n| Complexity | Simple | Complex (directory storage) |\n| Used in | UMA / small SMP | ccNUMA, large systems |\n\n---\n\n#### Model Solution for B-B (b):\n\n**(i) Remote access — 2 hops, clean line read from remote memory:**\n\n$$\n\\begin{aligned}\nT_{remote} &= T_{local} + h \\cdot t_{hop} + t_{dir} \\\\\n&= 60 + (2)(40) + 30 \\\\\n&= 60 + 80 + 30 \\\\\n&= 170 \\text{ ns}\n\\end{aligned}\n$$\n\n**(ii) Cache-to-cache transfer — 1 hop, remote node holds modified copy:**\n\n$$\n\\begin{aligned}\nT_{c2c} &= T_{local} + h \\cdot t_{hop} + t_{dir} + \\text{data transfer time} \\\\\n&\\approx 60 + (1)(40) + 30 + 40 \\\\\n&= 170 \\text{ ns}\n\\end{aligned}\n$$\n\n(Simplified: $T_{local} + t_{hop} \\cdot h + t_{dir}$, assuming symmetric hops.)\n\n**(iii) Speedup ratio (local vs remote clean memory read, 2 hops):**\n\n$$\n\\begin{aligned}\n\\text{Ratio} &= \\frac{T_{remote}}{T_{local}} \\\\\n&= \\frac{170}{60} \\\\\n&\\approx 2.83\n\\end{aligned}\n$$\n\nSo remote access is **~2.83× slower** than local access.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Pitfall Callout — Where Students Lose Marks**\n> 1. **Confusing NUMA with cluster / MPI systems.** NUMA is **hardware-shared memory**; cluster is **software message-passing**. Mixing these up costs the architecture definition marks.\n> 2. **Forgetting the `cc` prefix.** Writing just \"NUMA\" loses you the coherence marks. Always emphasize **hardware cache coherence**.\n> 3. **Using the wrong formula.** Many students use $T_{avg} = T_{local} + T_{remote}$ (arithmetic mean). The correct NUMA formula is the **weighted average** $f \\cdot T_{local} + (1-f) \\cdot T_{remote}$.\n> 4. **Skipping units in numerical answers.** Always state `ns`, `MB/s`, or `%`.\n> 5. **Not drawing the block diagram** in architecture questions. Even a rough sketch showing nodes + interconnect + memory + directory earns easy marks.\n\n---\n\n## 5.3 Topic Recap & Rapid-Revision Checklist\n\n- [x] **ccNUMA = Cache-Coherent + Non-Uniform Memory Access** — single address space, distributed physical memory, hardware coherence.\n- [x] **Memory latency depends on node distance** — local ≤ remote.\n- [x] **Architecture = {Compute Node + Interconnect + Distributed Directory}**, repeated across N nodes.\n- [x] **Coherence protocol = directory-based** (MESI/MOESI family) — scalable beyond bus limits.\n- [x] **Directory tracks owner + sharer bit-vector** for each memory line.\n- [x] **Key formula**: $T_{avg} = f \\cdot T_{local} + (1-f) \\cdot T_{remote}$.\n- [x] **NUMA penalty** = $\\dfrac{T_{avg} - T_{local}}{T_{local}} \\times 100\\%$.\n- [x] **NUMA ratio** $N_r = T_{remote}/T_{local}$ typically between **1.3 and 3.0** for production systems.\n- [x] **Locality-aware scheduling + first-touch memory allocation** are critical software optimizations.\n- [x] **Examples**: SGI Origin 2000/3000, AMD EPYC, Intel Xeon Scalable, IBM Power, HPE Superdome.\n- [x] **ccNUMA bridges UMA's programmability and cluster's scalability** — the dominant HPC server architecture.\n- [x] **For ESE**: always draw a labelled block diagram with at least 3 nodes, interconnect, memory, and directory.\n- [x] **For derivations**: substitute numerically *step-by-step*; show every arithmetic transition.\n\n> [!NOTE]\n> **Final Exam Tip (KTU 2024 Scheme):** Module 2 questions on ccNUMA frequently appear as either a **14-mark full-length question** with sub-parts requiring numerical computation, or a **3-mark direct definition**. Practice writing the **weighted average formula from memory** and a **clean 3-node block diagram** — these two alone secure ~70% of the marks on this topic.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "distributedmemorycomputershierarchicalhybridsystemsnetworksbasicperformancecharacteristicsofnetworks": {
      "title": "Distributed-Memory Computers - Hierarchical (Hybrid) Systems - Networks - Basic Performance Characteristics of Networks",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 2\n> - **Topic:** Distributed-Memory Computers - Hierarchical (Hybrid) Systems - Networks - Basic Performance Characteristics of Networks\n\n<!-- SECTION_1_START -->\n\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Distributed-Memory Computers\n\n> [!NOTE]\n> **Formal Definition (KTU 2024 Syllabus Standard):**\n> A *Distributed-Memory Computer* is a parallel architecture in which each processor possesses its own **private local memory**, and processors cooperate by exchanging **messages** over a high-speed **interconnection network**. No single processor can directly address the memory of another.\n\n**Key Properties:**\n- Memory is **physically distributed**, though logically it may appear as a single global address space (in DSM systems).\n- Communication is **explicit** — the programmer (or compiler) must invoke primitives such as `send` and `recv`.\n- **Memory Reference Time (MRT)** is uniform and predictable (no NUMA penalties inside a node).\n\n> [!IMPORTANT]\n> **KTU Board Exam Highlight:** The defining test for a *distributed-memory* system is: *Does any communication between two processors require software intervention (message-passing)?* If yes → distributed-memory.\n\n**Conceptual Analogy:**\nImagine a group of **independent office workers in different cities**, each with their own personal filing cabinet. They cannot open each other's cabinets directly. To share a document, they must **mail a sealed envelope** to each other. The *mail service* is the interconnection network, the *envelope* is the message, and the *travel time* is the latency.\n\n---\n\n### 1.2 Hierarchical (Hybrid) Systems\n\n> [!NOTE]\n> **Formal Definition:**\n> A *Hierarchical* or *Hybrid* parallel computer combines two or more levels of parallelism — typically **shared-memory multiprocessors (SMPs/NUMA nodes)** interconnected by a **distributed-memory network** to form a larger machine.\n\n**Architectural Tiers:**\n| Tier | Organization | Examples |\n|------|--------------|----------|\n| Tier-1 | Shared memory within a node | Multi-core CPU sockets, SMPs |\n| Tier-2 | Distributed memory across nodes | Cluster of SMPs, MPPs |\n| Tier-3 | (Optional) Distributed cluster of clusters | Grid, Cloud |\n\n**Conceptual Analogy:**\nThink of a **large multinational company**. Each *branch office* (node) has an internal shared filing system where all employees can read each other's files instantly. But branches in different cities cannot access each other's files directly — they must use the **corporate courier service (interconnection network)** to exchange documents. This is *hierarchical* — fast inside, slower across.\n\n> [!IMPORTANT]\n> **Why Hierarchical?** Pure shared-memory machines don't scale beyond ~hundreds of cores (cache-coherence bottleneck). Pure distributed-memory is hard to program. Hierarchical systems give the *best of both worlds* — programmability inside a node + scalability across nodes.\n\n---\n\n### 1.3 Interconnection Networks\n\n> [!NOTE]\n> **Formal Definition:**\n> An *Interconnection Network* is the set of **switches, links, and routing logic** that connects processors and memories in a parallel computer, enabling data movement between them.\n\n**Two Principal Classes:**\n\n1. **Direct (Static) Networks** — Each switch is attached to a processor. The topology is fixed. *Mesh, Torus, Hypercube, Ring, Tree*.\n2. **Indirect (Dynamic) Networks** — Switches are standalone; processors connect via these switches. *Crossbar, Omega, Butterfly, Baseline, Benes*.\n\n**Conceptual Analogy:**\n- A **direct network** is like the seating arrangement in a classroom — each student (processor) sits at a fixed desk (switch) connected to specific neighbours.\n- An **indirect network** is like a telephone exchange — calls are routed through intermediate operator boards (switches) before reaching the destination.\n\n---\n\n### 1.4 Basic Performance Characteristics of Networks\n\n> [!NOTE]\n> **Core Performance Metrics (KTU High-Yield List):**\n>\n> 1. **Latency (L)** — Time to send a zero-byte (control) message. Measured in **microseconds (μs)**.\n> 2. **Bandwidth (B)** — Maximum data rate per link. Measured in **MB/s** or **Gb/s**.\n> 3. **Throughput (T)** — Aggregate useful data rate across the network.\n> 4. **Diameter (D)** — Maximum shortest-path length (in hops) between any two nodes.\n> 5. **Node Degree (d)** — Number of physical links incident on each node.\n> 6. **Bisection Width (B_w)** — Minimum number of links that must be cut to split the network into two equal halves.\n> 7. **Bisection Bandwidth (B_b)** — Bisection Width × Bandwidth per link.\n\n> [!IMPORTANT]\n> **KTU Quick Memory Trick:** *Low diameter + High bisection bandwidth = Good network.*\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Distributed-Memory Programming Model — Operational Walkthrough\n\nThe execution of a distributed-memory program follows this structured sequence:\n\n1. **Process Spawning** — Each processor loads its own copy of the program; variables are *local* by default.\n2. **Local Computation** — Each processor operates on its private data with full memory bandwidth (no contention).\n3. **Communication Phase** — When data is needed by another processor:\n   - Process A packs the data into a *message buffer*.\n   - The message is injected into the network.\n   - The network **routes** the message hop-by-hop toward Process B.\n   - Process B receives the message into its local buffer.\n4. **Synchronization** — Often a barrier or acknowledgment ensures correctness.\n5. **Local Computation Resumes** — With fresh remote data now locally available.\n\n**The \"Why\" Behind Each Step:**\n- Locality is preserved → cache behaviour is excellent.\n- Message passing introduces **latency** that is independent of data size — this is why sending many small messages is worse than one big one.\n- The network can become a **bottleneck** under heavy traffic.\n\n---\n\n### 2.2 Hierarchical System — Operational Walkthrough\n\n1. **Within-Node Shared Memory Access** — Threads in the same node read/write a common DRAM via a coherence protocol (MESI/MOESI).\n2. **Cache Coherence Stop** — Beyond a node boundary, hardware coherence usually **stops**; software must explicitly use MPI, SHMEM, PGAS, or RDMA.\n3. **Inter-Node Messaging** — Data crosses the node boundary via the **network interface card (NIC)** into the global fabric (InfiniBand, Ethernet, Omni-Path).\n4. **Hybrid Parallelism Layers**:\n   - **Threading layer** (OpenMP/pthreads) inside the node.\n   - **Process layer** (MPI) across nodes.\n\n> [!TIP]\n> **Real-world utility:** Modern HPC systems like **Sunway TaihuLight, Fugaku, Frontier** are all hierarchical — each node has many cores sharing memory; thousands of nodes are linked via a high-speed network.\n\n---\n\n### 2.3 Network Performance Characteristics — Why They Matter\n\n- **Latency** dominates for short messages — important in fine-grained parallel algorithms (e.g., graph traversal).\n- **Bandwidth** dominates for bulk data transfer — important in scientific simulation (e.g., climate modelling).\n- **Diameter** dictates the worst-case communication time and determines scalability.\n- **Bisection Bandwidth** dictates how the system performs under all-to-all communication (e.g., FFT, N-body).\n\n---\n\n### 2.4 KTU Formula Sheet / Cheat Sheet\n\n| # | Quantity | Formula / Expression | Units | Notes |\n|---|----------|---------------------|-------|-------|\n| 1 | Message Passing Time (Linear Model) | $t(n) = t_0 + \\dfrac{n}{r_\\infty}$ | seconds | $t_0$ = latency, $n$ = bytes, $r_\\infty$ = asymptotic bandwidth |\n| 2 | Latency-bound regime | $t(n) \\approx t_0$ when $n \\ll t_0 \\cdot r_\\infty$ | seconds | Short messages |\n| 3 | Bandwidth-bound regime | $t(n) \\approx \\dfrac{n}{r_\\infty}$ | seconds | Long messages |\n| 4 | Network Diameter of p-node Hypercube | $D_{hc} = \\log_2 p$ | hops | Best among classic topologies |\n| 5 | Diameter of k-ary d-cube (Torus) | $D_{tor} = d \\cdot \\lfloor k/2 \\rfloor$ | hops | k nodes per dimension, d dimensions |\n| 6 | Bisection Width of Hypercube | $B_w = p/2$ | links | Optimal scaling |\n| 7 | Bisection Width of 2-D Mesh | $B_w = \\sqrt{p}$ | links | Scales poorly |\n| 8 | Bisection Bandwidth | $B_b = B_w \\times B_{link}$ | MB/s | True measure of aggregate capacity |\n| 9 | Cost (links) of p-node Hypercube | $C = \\dfrac{p \\log_2 p}{2}$ | links | Each of p nodes has degree $\\log_2 p$ |\n| 10 | Effective Network Throughput | $T = \\dfrac{n_{\\text{msg}} \\cdot p_{\\text{active}}}{t_{\\text{total}}}$ | bytes/s | Aggregate deliverable data rate |\n| 11 | Amdahl's Bound (Max Speedup) | $S_{\\max} = \\dfrac{1}{f_s + (1-f_s)/p}$ | dimensionless | $f_s$ = serial fraction |\n| 12 | Network Power Efficiency | $\\eta = \\dfrac{\\text{Bandwidth}}{\\text{Power}}$ | MB/s/W | Important in exascale design |\n\n> [!TIP]\n> **KTU Examiner Tip:** Whenever you see a numerical problem, *immediately* identify whether the network is *latency-bound* or *bandwidth-bound* — this decides which term of $t(n)$ dominates.\n\n---\n\n### 2.5 Real-World Engineering Utility\n\n| Field | Application | Why This Topic Matters |\n|-------|-------------|------------------------|\n| Weather Forecasting | Atmospheric simulation on clusters | Hierarchical nodes + low-latency fabric |\n| AI/Deep Learning | Distributed training (AllReduce) | Bisection bandwidth critical |\n| CFD (Aerospace) | Airfoil simulation | Mesh topology matches problem grid |\n| Genomics | Sequence alignment | Bandwidth-bound workload |\n| Cryptography | Distributed key search | Latency-bound workload |\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Derivation: Linear Message Passing Time Model\n\nWe model the time $t(n)$ to send a message of $n$ bytes across the network as the sum of three contributions:\n\n**Step 1 — Decompose total time into three components:**\n\n$$\nt(n) = t_{\\text{startup}} + t_{\\text{per-hop}} \\cdot h + t_{\\text{per-byte}} \\cdot n\n$$\n\nwhere:\n- $t_{\\text{startup}}$ = software overhead of issuing the message (in seconds)\n- $t_{\\text{per-hop}}$ = router forwarding latency (seconds per hop)\n- $h$ = number of hops on the chosen route\n- $t_{\\text{per-byte}}$ = time to transmit one byte onto the wire ($= 1 / r_{\\text{link}}$)\n- $n$ = message length in bytes\n\n**Step 2 — Combine start-up and per-hop into a single effective latency:**\n\n$$\nt_0 \\;=\\; t_{\\text{startup}} \\;+\\; t_{\\text{per-hop}} \\cdot \\bar{h}\n$$\n\nwhere $\\bar{h}$ is the average number of hops. Thus:\n\n$$\nt(n) \\;=\\; t_0 \\;+\\; \\dfrac{n}{r_\\infty}\n$$\n\nwhere $r_\\infty = 1 / t_{\\text{per-byte}}$ is the **asymptotic bandwidth** (the slope of $t$ vs. $n$ for large $n$).\n\n**Step 3 — Interpret the two regimes:**\n\n$$\n\\begin{aligned}\n&\\textbf{Short message } (n \\to 0): \\quad t(n) \\;\\approx\\; t_0 \\quad \\text{(latency-bound)}\\\\[4pt]\n&\\textbf{Long message } (n \\to \\infty): \\quad t(n) \\;\\approx\\; \\dfrac{n}{r_\\infty} \\quad \\text{(bandwidth-bound)}\n\\end{aligned}\n$$\n\nThe crossover point occurs when both terms are equal:\n\n$$\nt_0 \\;=\\; \\dfrac{n^*}{r_\\infty} \\quad\\Longrightarrow\\quad n^* \\;=\\; t_0 \\cdot r_\\infty\n$$\n\nThis $n^*$ is the **critical message size** below which latency dominates.\n\n---\n\n### 3.2 Derivation: Bisection Bandwidth of a $k$-ary $d$-cube Torus\n\nA $k$-ary $d$-cube contains $p = k^d$ nodes arranged in $d$ dimensions, with $k$ nodes per dimension.\n\n**Step 1 — Visualise the cut:** A bisection cut slices the torus perpendicular to one dimension, splitting $k^d$ nodes into two halves of $k^d / 2$ nodes each.\n\n**Step 2 — Count links crossing the cut:** In one dimension, $k^d / 2$ pairs of nodes are matched, giving $k^d / 2$ parallel links crossing the cut.\n\n**Step 3 — Generalise:** Since $d$ dimensions are equivalent, the bisection width is:\n\n$$\nB_w(\\text{torus}) \\;=\\; \\dfrac{k^d}{2} \\;=\\; \\dfrac{p}{2}\n$$\n\n**Step 4 — Multiply by per-link bandwidth** $B_{\\text{link}}$:\n\n$$\nB_b \\;=\\; \\dfrac{p}{2} \\cdot B_{\\text{link}}\n$$\n\n**Step 5 — Compare with a 2-D mesh:** A mesh lacks wrap-around links, so the bisection width becomes:\n\n$$\nB_w(\\text{mesh}) \\;=\\; \\sqrt{p} \\quad \\text{(for } d=2\\text{, } p = k^2\\text{)}\n$$\n\nThis shows that **a torus has asymptotically higher bisection bandwidth** than a mesh of the same size.\n\n---\n\n### 3.3 Analytical Matrix — Comparison of Topologies\n\nLet $p = 2^d$ for a hypercube (d-dim) and $p = k^d$ for a $k$-ary $d$-cube torus.\n\n| Topology | Degree $d_n$ | Diameter $D$ | Bisection Width $B_w$ | Total Links $L$ |\n|----------|--------------|--------------|------------------------|------------------|\n| Linear Array | 2 (ends: 1) | $p - 1$ | 1 | $p - 1$ |\n| Ring | 2 | $\\lfloor p/2 \\rfloor$ | 2 | $p$ |\n| 2-D Mesh | 4 (corners: 2) | $2(\\sqrt{p} - 1)$ | $\\sqrt{p}$ | $2(p - \\sqrt{p})$ |\n| 2-D Torus | 4 | $\\lfloor \\sqrt{p}/2 \\rfloor$ | $p/2$ | $2p$ |\n| Hypercube | $\\log_2 p$ | $\\log_2 p$ | $p/2$ | $\\dfrac{p \\log_2 p}{2}$ |\n| $k$-ary $d$-cube Torus | $2d$ | $d \\cdot \\lfloor k/2 \\rfloor$ | $p/2$ | $d \\cdot p$ |\n\n> [!TIP]\n> **KTU Insight:** Hypercubes minimise diameter for fixed $p$, but cost grows as $p \\log_2 p$. Tori minimise cost (linear in $p$) at the price of higher diameter. **This is why modern HPC uses Tori** (e.g., Blue Gene uses 3-D torus).\n\n---\n\n### 3.4 Derivation: Communication Cost in Hierarchical Hybrid Systems\n\nConsider a hybrid machine with $N$ nodes, each node having $c$ cores. Total processes $= N \\cdot c$.\n\n**Step 1 — Total communication time:**\n\n$$\nt_{\\text{comm}} = t_{\\text{intra-node}} + t_{\\text{inter-node}}\n$$\n\n**Step 2 — Intra-node (shared memory)** is essentially a cache-line load:\n\n$$\nt_{\\text{intra-node}} \\approx t_{\\text{cache}} \\cdot n_{\\text{shared}} \\quad (\\sim 50 \\text{ ns per cache line})\n$$\n\n**Step 3 — Inter-node (distributed memory)** uses the network model from §3.1:\n\n$$\nt_{\\text{inter-node}} = t_0 + \\dfrac{n_{\\text{distributed}}}{r_\\infty}\n$$\n\n**Step 4 — Total wall-clock communication:**\n\n$$\n\\begin{aligned}\nt_{\\text{comm}} &= \\alpha_{\\text{shared}} \\cdot n_{\\text{shared}} + \\alpha_{\\text{dist}} \\cdot n_{\\text{dist}} + t_{0,\\text{dist}} \\\\[4pt]\n\\text{where } \\alpha_{\\text{shared}} &\\ll \\alpha_{\\text{dist}} \\text{ and } t_{0,\\text{dist}} \\gg 0\n\\end{aligned}\n$$\n\n**Step 5 — Design heuristic:** Maximise *intra-node* data sharing and minimise *inter-node* traffic — this is the **fundamental optimization rule of hybrid HPC programming**.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Distributed-Memory Architecture — Block-Level Functional Flow\n\n```mermaid\nflowchart LR\n    P1[Processor P1] --- M1[Local Memory M1]\n    P2[Processor P2] --- M2[Local Memory M2]\n    P3[Processor P3] --- M3[Local Memory M3]\n    P4[Processor P4] --- M4[Local Memory M4]\n\n    P1 -. \"Network Interface\" .-> NET[Interconnection Network]\n    P2 -. \"Network Interface\" .-> NET\n    P3 -. \"Network Interface\" .-> NET\n    P4 -. \"Network Interface\" .-> NET\n\n    NET --> P2\n    NET --> P3\n    NET --> P4\n\n    MSG1[Message Send Buffer] --- P1\n    MSG2[Message Recv Buffer] --- P2\n```\n\n**Interpretation:** Each processor holds private memory; communication occurs only through the network using message-passing primitives. No shared address space exists.\n\n---\n\n### 4.2 Hierarchical (Hybrid) System — Multi-Tier Block Diagram\n\n```mermaid\nflowchart TB\n    subgraph NODE_A[NODE A - SMP]\n        A1[Core A1] --- ASHMEM[Shared Cache and DRAM]\n        A2[Core A2] --- ASHMEM\n        A3[Core A3] --- ASHMEM\n        A4[Core A4] --- ASHMEM\n    end\n\n    subgraph NODE_B[NODE B - SMP]\n        B1[Core B1] --- BSHMEM[Shared Cache and DRAM]\n        B2[Core B2] --- BSHMEM\n        B3[Core B3] --- BSHMEM\n        B4[Core B4] --- BSHMEM\n    end\n\n    NODE_A -- \"Network - Message Passing\" --> NIC1[Network Interface NIC]\n    NODE_B -- \"Network - Message Passing\" --> NIC2[Network Interface NIC]\n\n    NIC1 --> FABRIC[High Speed Interconnect Fabric InfiniBand or Ethernet]\n    NIC2 --> FABRIC\n```\n\n**Interpretation:** Inside a node → shared-memory threads. Across nodes → distributed-memory messages. The fabric (e.g., InfiniBand) handles inter-node traffic.\n\n---\n\n### 4.3 Topology Comparison — Functional Flow\n\n```mermaid\nflowchart TB\n    subgraph RING[Ring Topology]\n        R1[Node 1] --- R2[Node 2]\n        R2 --- R3[Node 3]\n        R3 --- R4[Node 4]\n        R4 --- R5[Node 5]\n        R5 --- R1\n    end\n\n    subgraph MESH[2-D Mesh]\n        M1[Node 1] --- M2[Node 2]\n        M2 --- M3[Node 3]\n        M1 --- M4[Node 4]\n        M2 --- M5[Node 5]\n        M3 --- M6[Node 6]\n        M4 --- M5\n        M5 --- M6\n    end\n\n    subgraph HYPERCUBE[Hypercube p=8]\n        H1[000] --- H2[001]\n        H1 --- H3[010]\n        H1 --- H4[100]\n        H2 --- H5[011]\n        H2 --- H6[101]\n        H3 --- H5\n        H3 --- H7[110]\n        H4 --- H6\n        H4 --- H7\n        H5 --- H8[111]\n        H6 --- H8\n        H7 --- H8\n    end\n```\n\n**Interpretation:** Visual comparison of three classic topologies — *Ring* (degree 2, high diameter), *Mesh* (degree ≤ 4), *Hypercube* (degree = $\\log_2 p$, diameter = $\\log_2 p$). Hyercube has the lowest diameter but at a wiring cost.\n\n---\n\n### 4.4 Message Passing Time — Functional Flow\n\n```mermaid\nflowchart LR\n    A[Sender Process] --> B[Pack Message into Buffer]\n    B --> C[Inject into Network Interface]\n    C --> D[Route Through Hops h1, h2 ... hn]\n    D --> E[Receiver Network Interface]\n    E --> F[Unpack into Local Buffer]\n    F --> G[Receiver Process]\n\n    T1[Latency t0] -.-> C\n    T2[Per Hop Delay] -.-> D\n    T3[Bandwidth n over r] -.-> D\n```\n\n**Interpretation:** The journey of a single message, with each stage contributing to the total transmission time $t(n) = t_0 + n/r_\\infty$.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### PART A — Short Answer Questions (3 Marks Each)\n\n**Q1. `[KTU University Exam - Dec 2023, Model Paper]`**\nDifferentiate between **distributed-memory** and **shared-memory** parallel architectures. State one example for each.\n\n**Model Answer (3 marks):**\n\n| Feature | Shared Memory | Distributed Memory |\n|---------|---------------|--------------------|\n| Memory Location | Centralized, all processors access it | Local to each processor |\n| Communication | Implicit (load/store) | Explicit (send/recv) |\n| Programming | OpenMP, pthreads | MPI, PVM |\n| Scalability | Limited (~100 cores) | High (10⁶ cores) |\n| Example | Multicore CPU, SMP | Cluster, MPP |\n\n*(Award 1.5 marks for table, 1 mark for example, 0.5 mark for any additional correct distinction.)*\n\n---\n\n**Q2. `[KTU University Exam - June 2024, KTU Model]`**\nDefine **bisection bandwidth** of an interconnection network. Why is it considered a more reliable performance indicator than simple bandwidth?\n\n**Model Answer (3 marks):**\n- **Definition (1.5 marks):** Bisection bandwidth is the *minimum aggregate bandwidth across any cut* that divides the network into two equal halves. Formally, $B_b = B_w \\times B_{\\text{link}}$, where $B_w$ is the bisection width (minimum number of links crossing any balanced partition).\n- **Why it's better (1.5 marks):** Simple bandwidth only measures the rate of *one* link, but a network is bottlenecked at its *narrowest point*. Bisection bandwidth captures the *worst-case* communication capacity, which determines all-to-all and permutation performance.\n\n---\n\n### PART B — Long Answer Questions (14 Marks Each, Internal Choice)\n\n---\n\n#### **Question A (14 Marks): Distributed-Memory + Network Performance**\n\n`[KTU University Exam - Dec 2023, Module 2 Sample]`\n\n**(a)** Explain the architecture of a **distributed-memory computer** with a neat block diagram. Discuss the **advantages and disadvantages** of message-passing programming. **(7 marks)**\n\n**(b)** A cluster has a network with latency $t_0 = 5 \\,\\mu\\text{s}$ and asymptotic bandwidth $r_\\infty = 100 \\,\\text{MB/s}$. Calculate the message passing time for:\n  (i) $n = 1\\,\\text{KB}$\n  (ii) $n = 1\\,\\text{MB}$\n  Also determine the critical message size $n^*$. **(7 marks)**\n\n---\n\n**Model Solution:**\n\n**(a) Distributed-Memory Architecture (7 marks):**\n\n**Architecture description (3 marks):**\n- Each processor $P_i$ has its own local memory $M_i$ and local cache.\n- Processors are connected via a dedicated **interconnection network** (e.g., Torus, Fat-Tree).\n- Communication occurs through **message passing**: `send` to inject a message, `recv` to receive it.\n- A **network interface controller (NIC)** handles protocol translation.\n- *Block diagram:* Draw a flowchart with N processors each linked to local memory, and all connected to a central network. (Refer to §4.1.)\n\n**Advantages (2 marks):**\n1. **High scalability** — Adding more nodes increases memory and compute proportionally.\n2. **No cache-coherence overhead** — Each node manages its own cache.\n3. **Fault isolation** — Failure of one node doesn't corrupt others' memory.\n4. **Cost-effective** — Built from commodity components.\n\n**Disadvantages (2 marks):**\n1. **Programming complexity** — Programmer must manage data distribution explicitly.\n2. **Communication overhead** — Message-passing latency is high compared to load/store.\n3. **Irregular data access** is hard to express.\n4. **Load balancing** must be done manually.\n\n---\n\n**(b) Numerical (7 marks):**\n\n**Given:** $t_0 = 5 \\,\\mu\\text{s} = 5 \\times 10^{-6} \\,\\text{s}$, $r_\\infty = 100 \\,\\text{MB/s} = 10^{8} \\,\\text{B/s}$.\n\n**Formula:**\n$$\nt(n) = t_0 + \\frac{n}{r_\\infty}\n$$\n\n**Case (i): $n = 1\\,\\text{KB} = 1024 \\,\\text{B}$**\n\n$$\n\\begin{aligned}\nt(1024) &= 5 \\times 10^{-6} + \\frac{1024}{10^{8}} \\\\\n&= 5 \\times 10^{-6} + 1.024 \\times 10^{-5} \\\\\n&= 5.00 \\times 10^{-6} + 10.24 \\times 10^{-6} \\\\\n&= 15.24 \\times 10^{-6} \\,\\text{s} \\\\\n&= \\mathbf{15.24 \\,\\mu\\text{s}}\n\\end{aligned}\n$$\n\n**Case (ii): $n = 1\\,\\text{MB} = 10^{6} \\,\\text{B}$**\n\n$$\n\\begin{aligned}\nt(10^6) &= 5 \\times 10^{-6} + \\frac{10^6}{10^8} \\\\\n&= 5 \\times 10^{-6} + 10^{-2} \\\\\n&= 0.000005 + 0.01 \\\\\n&= \\mathbf{0.010005 \\,\\text{s}} \\;\\;\\approx 10 \\,\\text{ms}\n\\end{aligned}\n$$\n\n**Critical message size:**\n\n$$\nn^* = t_0 \\cdot r_\\infty = 5 \\times 10^{-6} \\times 10^{8} = 500 \\,\\text{bytes}\n$$\n\n**Interpretation (1 mark):**\n- For $n = 1\\,\\text{KB} > 500\\,\\text{B}$: bandwidth dominates slightly.\n- For $n = 1\\,\\text{MB} \\gg 500\\,\\text{B}$: completely bandwidth-bound.\n- Short messages (< 500 B) suffer mostly from latency overhead.\n\n---\n\n#### **Question B (14 Marks): Hierarchical Systems + Topology Metrics**\n\n`[KTU University Exam - June 2024, Module 2 Model Paper]`\n\n**(a)** With a neat diagram, explain the **hierarchical (hybrid) parallel architecture**. Discuss why modern HPC systems prefer hybrid designs over pure shared-memory or pure distributed-memory. **(7 marks)**\n\n**(b)** Compare the following topologies for $p = 16$ nodes: **(i) Ring, (ii) 2-D Mesh, (iii) Hypercube**. Compute degree, diameter, bisection width, and total links for each. Identify which is best for all-to-all communication. **(7 marks)**\n\n---\n\n**Model Solution:**\n\n**(a) Hierarchical Architecture (7 marks):**\n\n**Definition (2 marks):**\nA hierarchical system combines multiple levels of parallelism — typically shared-memory inside a node (SMP/NUMA) and distributed-memory across nodes. The communication inside a node uses load/store with cache coherence, while across nodes it uses message passing.\n\n**Diagram (2 marks):** Refer to §4.2 — two SMP nodes connected via a high-speed fabric. Each node contains cores sharing memory; nodes communicate via network interfaces (e.g., InfiniBand HCA).\n\n**Why hybrid is preferred (3 marks):**\n\n| Aspect | Pure Shared | Pure Distributed | Hybrid |\n|--------|-------------|------------------|--------|\n| Programmability | Easy | Hard | Easy inside, moderate across |\n| Scalability | Poor | Excellent | Excellent |\n| Cost | High (crossbar) | Low | Moderate |\n| Communication Speed | Very fast (ns) | Slow (μs) | Fast inside + moderate across |\n| Real-world use | Single server | Theoretical extreme | All modern HPC |\n\n**Justification (1 mark):** Real HPC workloads (climate, CFD, AI training) have both *fine-grained* parallelism (handled well by threads in a node) and *coarse-grained* parallelism (handled well by MPI across nodes). Hybrid captures both.\n\n---\n\n**(b) Topology Comparison for $p = 16$ (7 marks):**\n\n**Recall general formulas:**\n\n- **Ring:** Degree = 2, Diameter = $\\lfloor p/2 \\rfloor$, $B_w = 2$, Links = $p$\n- **2-D Mesh:** $p = k^2 \\Rightarrow k = 4$. Degree = 4 (corners = 2), Diameter = $2(k-1)$, $B_w = k$, Links = $2(p - k)$\n- **Hypercube:** $d = \\log_2 p = 4$. Degree = 4, Diameter = 4, $B_w = p/2$, Links = $\\dfrac{p \\log_2 p}{2}$\n\n**Calculations for $p = 16$:**\n\n| Topology | Degree | Diameter | Bisection Width | Total Links |\n|----------|--------|----------|------------------|--------------|\n| Ring | 2 | $\\lfloor 16/2 \\rfloor = 8$ | 2 | 16 |\n| 2-D Mesh | $\\le 4$ | $2(4-1) = 6$ | 4 | $2(16 - 4) = 24$ |\n| Hypercube | 4 | 4 | 8 | $\\dfrac{16 \\times 4}{2} = 32$ |\n\n**Best for all-to-all communication (1 mark):**\nThe **Hypercube** has the *highest bisection width* (8), which directly determines the rate at which data can cross any partition during permutation-style traffic. Therefore, **Hypercube** is best.\n\n**Trade-off note (1 mark):** Hypercube uses the most links (32) — wiring cost grows as $p \\log_2 p$. For very large $p$, modern systems use **Tori** (which achieve $B_w = p/2$ at only $dp$ links).\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall Callout:**\n> 1. **Mistake #1:** Confusing *diameter* (max hops) with *average distance* (mean hops). Always state \"diameter = maximum shortest path.\"\n> 2. **Mistake #2:** In numerical problems, students forget to **convert units** ($KB \\to B$, $\\mu s \\to s$). The KTU checker deducts 1 mark for unit mismatch.\n> 3. **Mistake #3:** Calling bisection width and bisection bandwidth the same thing — *width* is in links, *bandwidth* is in MB/s.\n> 4. **Mistake #4:** Writing $t(n) = t_0 + n/r$ without defining the variables — full marks require a clear list of symbols.\n> 5. **Mistake #5:** In hierarchical system diagrams, students often forget to show the **network interface card (NIC)** — examiners deduct marks for missing hardware layers.\n\n---\n\n### Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- [ ] **Distributed Memory** = private memory + message passing via network; *no shared address space across nodes*.\n- [ ] **Hierarchical System** = tier-1 shared memory (threads) + tier-2 distributed memory (MPI) → modern HPC standard.\n- [ ] **Interconnection Networks** are either **direct** (mesh, torus, hypercube) or **indirect** (crossbar, Omega, butterfly).\n- [ ] **Latency $t_0$** is fixed cost per message; **Bandwidth $r_\\infty$** is per-byte cost.\n- [ ] **Message Time Equation:** $t(n) = t_0 + \\dfrac{n}{r_\\infty}$ — *the single most important formula in this module*.\n- [ ] **Critical message size:** $n^* = t_0 \\cdot r_\\infty$ — boundary between latency-bound and bandwidth-bound regimes.\n- [ ] **Diameter:** max shortest-path between any two nodes.\n- [ ] **Bisection Width:** min links crossing any balanced cut.\n- [ ] **Bisection Bandwidth:** $B_b = B_w \\times B_{\\text{link}}$ — *the truest indicator of network bulk capacity*.\n- [ ] **Hypercube** has optimal diameter ($\\log_2 p$) and bisection width ($p/2$) but cost grows as $p \\log_2 p$.\n- [ ] **Torus** is the practical sweet spot — linear cost ($dp$ links) and good bisection bandwidth ($p/2$).\n- [ ] **Mesh** has poor bisection ($\\sqrt p$) — avoid for all-to-all workloads.\n- [ ] **Hybrid programming heuristic:** Maximise intra-node sharing, minimise inter-node traffic.\n- [ ] **Amdahl's Law** ceiling: $S_{\\max} = \\dfrac{1}{f_s + (1-f_s)/p}$ — sets ultimate speedup bound.\n- [ ] **Real systems** using these concepts: Fugaku (Tofu), Frontier (InfiniBand), Sunway TaihuLight (custom hierarchical).\n\n> [!IMPORTANT]\n> **Final KTU Exam Strategy:** For any 14-mark question in this module, the examiner expects: (1) a clear diagram (2 marks), (2) definitions and theory (3 marks), (3) comparison table (3 marks), (4) numerical computation with units (4 marks), (5) interpretation/discussion (2 marks). Master this template and you will reliably score 12+ marks.\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "buses": {
      "title": "Buses",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 2\n> - **Topic:** Buses\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\nA **Bus** in a High Performance Computing (HPC) system is a **shared communication subsystem** that transfers data, address, and control signals between processors, memory modules, and I/O devices through a common set of physical wires, traces, or optical pathways. In the KTU 2024 framework, a bus is classified as the **lowest-tier interconnect** in the memory and processor hierarchy, providing a single, multi-drop transmission medium governed by a defined bus protocol, arbitration scheme, and electrical/mechanical specification.\n\nA bus is characterized by three orthogonal signal groups:\n\n- **Address Bus** — unidirectional lines carrying the source/destination memory-mapped address.\n- **Data Bus** — bidirectional lines carrying the actual payload (instructions or operands).\n- **Control Bus** — timing, command, and arbitration signals (e.g., `RD`, `WR`, `CLK`, `BUSREQ`, `BUSGRANT`).\n\n> [!IMPORTANT]\n> **KTU 2024 Board Definition:** \"A bus is a set of conducting wires that conform to a standardized protocol, permitting the transfer of data words between independently clocked functional units via time-multiplexed, arbitrated transactions.\"\n\n## 1.2 Conceptual Analogy / Plain-English Intuition\n\nImagine a **single narrow one-lane road** connecting a group of villages (processors) to a central warehouse (memory). Only one villager-truck can travel on this road at a time, and there must be a **traffic controller (arbiter)** at the entry point who decides whose turn it is. If many villages want to send/receive goods simultaneously, a long queue forms — this is the **bus contention bottleneck** that limits scalability in HPC.\n\nKey intuitive elements:\n\n- The **width of the road** = the *data bus width* (e.g., 64 bits).\n- The **speed limit** = the *bus clock frequency* (e.g., 1333 MHz).\n- The **traffic controller** = the *bus arbiter*.\n- The **road rules** = the *bus protocol* (e.g., PCI, AXI, Wishbone).\n- The **waiting time at intersections** = the *arbitration latency*.\n\n> [!NOTE]\n> In HPC literature, buses are the *baseline interconnect* against which crossbars, rings, meshes, and tori are compared. They are simple, cheap, and broadcast-capable — but fundamentally **non-scalable** beyond a small number of masters.\n\n## 1.3 Physical Constants & Standard Metrics\n\nThe following are the universally accepted KTU-grade metrics used to characterize any bus:\n\n- **Bus Width (W)** — expressed in **bits** (typical: 8, 16, 32, 64, 128, 256).\n- **Bus Frequency (f)** — expressed in **MHz** or **GHz** (typical: 66 MHz legacy PCI to 32 GT/s for PCIe 5.0).\n- **Bus Bandwidth (BW)** — expressed in **MB/s** or **GB/s**.\n- **Bus Latency (L)** — expressed in **clock cycles** or **nanoseconds (ns)**.\n- **Bus Load / Utilization (U)** — dimensionless ratio, 0 ≤ U ≤ 1.\n\n> [!NOTE]\n> The reference **transfer time per word** for a single bus transaction is denoted **tₜ**, and the **cycle time** is denoted **T_c = 1 / f**.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Bus Operational Phases (Step-by-Step Logical Flow)\n\nEvery bus transaction traverses a **five-phase pipeline**. The KTU 2024 module on Buses expects the student to reproduce this pipeline verbatim:\n\n1. **Arbitration Phase** — Multiple masters request the bus. The arbiter resolves contention using a *fixed-priority*, *round-robin*, or *fair-share* policy and grants the bus to the winning master. This phase consumes **T_arb** cycles.\n\n2. **Address Phase** — The granted master places the target memory/device address on the **address bus** and asserts the appropriate **control signals** (e.g., `MREQ`, `RD/WR`). This phase lasts **1 clock cycle** in synchronous buses.\n\n3. **Wait Phase (optional)** — If the slave device is slow (e.g., DRAM page miss), it inserts **wait states** by asserting `WAIT`. The number of inserted cycles is **T_wait**.\n\n4. **Data Phase** — The data word (or burst of words) is transferred on the **data bus**. For an n-word burst, this lasts **n × T_c** cycles.\n\n5. **Bus Release Phase** — The master de-asserts the bus request, and the arbiter may grant the bus to the next pending requestor.\n\n> [!IMPORTANT]\n> The **total transaction time** for a single bus cycle is therefore:\n> **T_total = T_arb + 1 + T_wait + n**\n\n## 2.2 Bus Arbitration Schemes — Why & How\n\n| Arbitration Scheme | Why it is Used | How it Works | KTU Board Note |\n|---|---|---|---|\n| **Daisy Chain (Serial)** | Minimal hardware, no central arbiter | Priority is fixed by physical position in the chain | Suffer from *propagation delay* and *single point of failure* |\n| **Centralized (Parallel)** | Low latency, deterministic | All requests fed to one arbiter IC, single `GRANT` line | Standard for **PCI**, **AMBA APB** |\n| **Distributed (Self-arbitration)** | Scalable, fault-tolerant | Each master has a unique arbitration ID, resolves by wire-OR or token-passing | Used in **Futurebus+**, **SCI**, **PCIe** |\n| **Round-Robin** | Fairness, prevents starvation | Arbiter cycles through masters in a fixed sequence | Common in **multicore SoC** buses |\n| **Lottery / TDMA** | Probabilistic fairness | Random or time-sliced grant | Used in research HPC interconnects |\n\n## 2.3 KTU Formula Sheet / Cheat Sheet\n\nThe following are the **board-essential formulas** for the Buses topic. Every derivation and numerical problem in the ESE will reduce to one of these.\n\n| # | Formula / Equation | Meaning of Symbols | Units / Boundary Conditions |\n|---|---|---|---|\n| 1 | $\\text{BW} = W \\times f$ | W = data bus width, f = bus clock frequency | bits/s (divide by 8 for Bytes/s) |\n| 2 | $\\text{BW}_{\\text{burst}} = \\dfrac{n \\times W}{T_c \\times (n + T_{\\text{overhead}})}$ | n = burst length, T_overhead = arbitration + address cycles | Bytes/s; n ≫ T_overhead for efficiency |\n| 3 | $T_{\\text{total}} = T_{\\text{arb}} + T_{\\text{addr}} + T_{\\text{wait}} + T_{\\text{data}}$ | Per-bus-cycle latency components | Clock cycles |\n| 4 | $U = \\dfrac{\\text{Bus used cycles}}{\\text{Total cycles}} = \\dfrac{T_{\\text{data}}}{T_{\\text{total}}}$ | Utilization, 0 ≤ U ≤ 1 | Dimensionless |\n| 5 | $f_{\\text{max}} = \\dfrac{1}{T_c} = \\dfrac{v}{k \\times d_{\\text{max}}}$ | v = signal velocity, d_max = max bus length, k = derating factor (typical 4–5) | Hz; sets physical limit on bus length |\n| 6 | $A = f \\times W \\times U$ | Aggregate throughput, A | Bytes/s |\n| 7 | $\\text{Speedup}_{\\text{bus}} = \\dfrac{N}{1 + (N-1) \\cdot (1 - U)}$ | N = number of processors, U = single-processor bus utilization | Dimensionless; derived from Amdahl on bus |\n\n> [!NOTE]\n> **Engineering utility:** These formulas are the bedrock of *bus performance engineering* in any parallel architecture — they are used in designing multi-core chips, GPU memory hierarchies, and embedded SoCs across the HPC industry.\n\n## 2.4 Classification of Buses (KTU Taxonomy)\n\nThe KTU 2024 syllabus distinguishes three macro-classes:\n\n- **System Bus / Processor–Memory Bus** — High-speed, short, custom-designed (e.g., Intel Front Side Bus, AMD HyperTransport). Connects CPU ↔ northbridge ↔ DRAM.\n- **I/O Bus / Expansion Bus** — Medium-speed, standardized, plug-and-play (e.g., **PCI**, **PCI-X**, **PCIe**, **AGP**).\n- **Peripheral Bus** — Low-speed, hot-pluggable (e.g., **USB**, **I²C**, **SPI**, **SATA**).\n\nA fourth class emerges in modern HPC — the **Network-on-Chip (NoC)** bus, which replaces the physical shared bus with a packet-switched on-chip mesh. While not a \"bus\" in the classical sense, KTU treats it as the *logical evolution* of the bus concept.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation 1 — Maximum Theoretical Bus Bandwidth\n\nWe derive the closed-form expression for the maximum sustainable data rate of a bus with width $W$ and clock frequency $f$, taking into account the fact that one complete word is transferred per clock edge.\n\n**Step 1 — Identify the fundamental transfer rate.**  \nIn a synchronous bus, exactly one word of width $W$ is latched per rising (and one per falling) clock edge, giving two transfers per clock period for **Double Data Rate (DDR)** buses.\n\n$$\n\\text{Transfers per second} = 2 \\times f\n$$\n\n**Step 2 — Compute the bit-rate.**  \nEach transfer moves $W$ bits. Therefore the gross bit-rate is:\n\n$$\nR_{\\text{gross}} = W \\times 2 \\times f \\quad \\text{[bits/s]}\n$$\n\n**Step 3 — Convert to Bytes per second.**  \nDivide by 8 to obtain the standard engineering unit:\n\n$$\n\\text{BW}_{\\max} = \\dfrac{W \\times 2 \\times f}{8} = \\dfrac{W \\times f}{4} \\quad \\text{[Bytes/s for DDR]}\n$$\n\n**Step 4 — Include the protocol efficiency factor $\\eta$.**  \nThe real achieved bandwidth is reduced by idle cycles, arbitration, and protocol overhead:\n\n$$\n\\text{BW}_{\\text{real}} = \\eta \\times \\dfrac{W \\times f}{4}, \\quad 0 < \\eta \\le 1\n$$\n\n> [!NOTE]\n> For a **Single Data Rate (SDR)** bus (e.g., legacy PCI), drop the factor of 2:\n> $\\text{BW}_{\\max}^{\\text{SDR}} = \\dfrac{W \\times f}{8}$ Bytes/s.\n\n---\n\n## 3.2 Derivation 2 — Bus Utilization Under Multi-Processor Contention\n\nWe model a system with $N$ processors sharing a single bus, each issuing a memory request with probability $p$ per cycle. The bus can service at most one request per cycle. We derive the steady-state utilization using a binomial contention model.\n\n**Step 1 — Single-processor request rate.**  \nA single processor generates $p$ requests per cycle (probability mass).\n\n**Step 2 — Multi-processor aggregate request rate.**  \nBy linearity of expectation, the aggregate number of requests per cycle is:\n\n$$\nR_{\\text{agg}} = N \\times p\n$$\n\n**Step 3 — Saturation cap.**  \nThe bus can service at most **1 request per cycle**. Therefore the *effective* requests served per cycle is:\n\n$$\nR_{\\text{served}} = \\min(R_{\\text{agg}}, 1) = \\min(N \\times p, 1)\n$$\n\n**Step 4 — Bus utilization as the served ratio.**  \n\n$$\nU = R_{\\text{served}} \\times 1 = \\min(N \\times p, 1)\n$$\n\n**Step 5 — Closed form for the unsaturated regime ($N \\cdot p \\le 1$).**  \n\n$$\nU = N \\cdot p\n$$\n\n**Step 6 — Closed form for the saturated regime ($N \\cdot p > 1$).**  \n\n$$\nU = 1 \\quad \\text{(bus is fully busy — throughput-limited)}\n$$\n\n> [!IMPORTANT]\n> **KTU Board Insight:** When $U \\to 1$, additional processors do **not** improve system throughput. This is the *bus-bandwidth wall* — the fundamental reason HPC moves to crossbar switches and networks.\n\n---\n\n## 3.3 Derivation 3 — Bus Clock Frequency Limited by Physical Length\n\nThe bus is a transmission line. The maximum frequency at which a signal can propagate without violating the setup-time of the farthest device is constrained by the signal round-trip time.\n\n**Step 1 — Define the one-way propagation delay.**  \nFor a trace of length $d$ meters and signal velocity $v$ (m/s, typically $v \\approx 1.5 \\times 10^8$ m/s on FR-4 PCB):\n\n$$\nt_{\\text{prop}} = \\dfrac{d}{v}\n$$\n\n**Step 2 — Round-trip (request + acknowledge) delay.**  \nA bus transaction requires at least one round trip for the address and one for the data:\n\n$$\nt_{\\text{rt}} = 2 \\times t_{\\text{prop}} = \\dfrac{2d}{v}\n$$\n\n**Step 3 — Nyquist-like condition for safe clocking.**  \nTo clock the bus reliably, the clock period must be at least $k$ times $t_{\\text{rt}}$ (where $k \\ge 4$ is a derating factor accounting for setup, hold, and skew):\n\n$$\nT_c \\ge k \\times t_{\\text{rt}} = \\dfrac{2 \\times k \\times d}{v}\n$$\n\n**Step 4 — Solve for maximum frequency.**  \n\n$$\nf_{\\max} = \\dfrac{1}{T_c} = \\dfrac{v}{2 \\times k \\times d}\n$$\n\n**Step 5 — Substitute numerical values (illustrative).**  \nFor $v = 1.5 \\times 10^8$ m/s, $k = 5$, $d = 0.3$ m (typical motherboard CPU-to-chipset):\n\n$$\nf_{\\max} = \\dfrac{1.5 \\times 10^8}{2 \\times 5 \\times 0.3} = \\dfrac{1.5 \\times 10^8}{3.0} = 5.0 \\times 10^7 \\text{ Hz} = 50 \\text{ MHz}\n$$\n\n> [!NOTE]\n> This is precisely why **front-side buses were capped near 400–1600 MHz** by increasing the *width* and not the *length* — and why modern systems replaced the shared bus with point-to-point links like **HyperTransport** and **QuickPath**.\n\n---\n\n## 3.4 Worked Numerical Example (KTU ESE Pattern)\n\n**Problem:** A 64-bit DDR bus operates at 800 MHz with a protocol efficiency of 75%. The system has 4 processor cores, each issuing a memory request with probability $p = 0.30$ per cycle. The shared bus serves one request per cycle, and each serviced request returns a 64-byte cache line. Compute:\n(a) Maximum theoretical bandwidth.\n(b) Real achieved bandwidth.\n(c) Bus utilization.\n(d) Effective throughput in MB/s.\n\n**Solution:**\n\n**(a)** $\\text{BW}_{\\max} = \\dfrac{W \\times f}{4} = \\dfrac{64 \\times 800 \\times 10^6}{4} = 12.8 \\times 10^9$ Bytes/s $= 12.8$ GB/s.\n\n**(b)** $\\text{BW}_{\\text{real}} = 0.75 \\times 12.8 = 9.6$ GB/s.\n\n**(c)** $U = \\min(N \\times p, 1) = \\min(4 \\times 0.30, 1) = \\min(1.20, 1) = 1.0$ — **saturated regime**.\n\n**(d)** Effective throughput = $U \\times \\text{BW}_{\\text{real}} = 1.0 \\times 9.6 = 9.6$ GB/s.\nThe 0.2 *excess* requests per cycle (since $N \\cdot p = 1.2 > 1$) are **queued**, producing memory-access latency growth.\n\n> [!IMPORTANT]\n> The above is the exact style of numerical question that appears in the **14-mark Part B** of the KTU 2024 ESE. Memorize the four-step methodology.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Block-Level Functional Architecture of a Shared Bus System\n\nThe following Mermaid diagram renders the *functional* architecture of a classical shared-bus multiprocessor — emphasizing the bus as the central spine connecting all masters, slaves, and the arbiter. Per the engine's safeguard, no reserved keywords are used as node IDs and labels contain only raw uppercase alphanumeric text.\n\n```mermaid\nflowchart TB\n    CPU1[M1 CPU CORE 1] -->|REQ| ARB[CENTRAL BUS ARBITER]\n    CPU2[M2 CPU CORE 2] -->|REQ| ARB\n    CPU3[M3 CPU CORE 3] -->|REQ| ARB\n    DMA1[DMA CONTROLLER] -->|REQ| ARB\n    ARB -->|GRANT| CPU1\n    ARB -->|GRANT| CPU2\n    ARB -->|GRANT| DMA1\n    CPU1 -->|ADDR DATA| BUS[(SHARED ADDRESS AND DATA BUS)]\n    CPU2 -->|ADDR DATA| BUS\n    CPU3 -->|ADDR DATA| BUS\n    DMA1 -->|ADDR DATA| BUS\n    BUS --> MEM[MAIN MEMORY DRAM]\n    BUS --> CACHE[L2 CACHE BANK]\n    BUS --> IOCTL[I O CONTROLLER]\n    CACHE --> SNOOP[CACHE COHERENCY SNOOP FILTER]\n    SNOOP -.->|SNOOP INVALIDATE| CPU1\n    SNOOP -.->|SNOOP INVALIDATE| CPU2\n    SNOOP -.->|SNOOP INVALIDATE| CPU3\n```\n\n> [!NOTE]\n> **Reading the diagram:** The **arbiter** sits in a control plane (dotted grant lines) and resolves contention among the three CPU cores and the DMA engine. The shared bus is the *data plane* (solid arrows) carrying address and data. The **snoop filter** maintains cache-coherency by broadcasting invalidations on the bus — a key HPC feature.\n\n---\n\n## 4.2 Bus Transaction State Machine (Mermaid State Diagram)\n\nThe following state diagram captures the **five-phase bus transaction** described in Section 2.1. It is the diagram most frequently asked in KTU 14-mark answers.\n\n```mermaid\nstateDiagram-v2\n    [*] --> IDLE\n    IDLE --> ARBITRATION : MASTER REQ ASSERTED\n    ARBITRATION --> ADDRESS : GRANT RECEIVED\n    ADDRESS --> WAIT : SLAVE WAIT SIGNAL HIGH\n    ADDRESS --> DATA : SLAVE READY\n    WAIT --> DATA : WAIT DEASSERTED\n    DATA --> IDLE : TRANSACTION COMPLETE\n    DATA --> BURST : BURST MODE ENABLED\n    BURST --> IDLE : BURST END\n    IDLE --> [*]\n```\n\n> [!IMPORTANT]\n> The state diagram is **identical** to the one in standard parallel-architecture textbooks. The KTU 2024 examiner expects this exact transition graph in any 7-mark sub-question on \"Explain the bus transaction phases\".\n\n---\n\n## 4.3 Bus Arbitration Comparison Topology\n\nA side-by-side comparison of arbitration schemes is shown below as a Mermaid block diagram. Each leaf node represents a master, and the parent node represents the arbiter (or the daisy-chain terminus).\n\n```mermaid\nflowchart LR\n    subgraph DAISY[DAISY CHAIN ARBITRATION]\n        D0[M1] --> D1[M2] --> D2[M3] --> D3[M4]\n    end\n    subgraph CENT[PARALLEL CENTRAL ARBITER]\n        C0[M1] --> CA[ARBITER]\n        C1[M2] --> CA\n        C2[M3] --> CA\n        C3[M4] --> CA\n    end\n    subgraph DIST[DISTRIBUTED SELF ARBITRATION]\n        E0[M1] <--> E1[M2] <--> E2[M3] <--> E3[M4]\n    end\n```\n\n> [!NOTE]\n> **Daisy chain** is *cheap* but suffers from priority-inversion. **Parallel** is *fast* but the arbiter is a single point of failure. **Distributed** is *scalable* and *fault-tolerant* — the architecture of choice for HPC fabrics like **SCI** and **Myrinet**.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### **Q1. [KTU University Exam – Dec 2023]**\n**Differentiate between a system bus and a peripheral bus. Give one example of each.**\n\n**Model Answer (3 marks):**\n\n| Feature | System Bus | Peripheral Bus |\n|---|---|---|\n| Purpose | Connects processor, memory, chipset | Connects low-speed I/O devices |\n| Speed | High (hundreds of MHz to GHz) | Low (kHz to a few hundred MHz) |\n| Standardization | Often proprietary (e.g., Intel QPI) | Standardized (USB, I²C, SPI) |\n| Example | Front Side Bus (legacy Intel) | USB 3.0 |\n| Arbitration | Centralized / distributed | Daisy-chain or polled |\n\n*(1 mark each for: definition, distinguishing features, and one example of each — total 3 marks)*\n\n---\n\n### **Q2. [KTU University Exam – May 2024]**\n**List any three bus arbitration schemes and state one advantage of each.**\n\n**Model Answer (3 marks):**\n\n1. **Daisy Chain Arbitration** — *Advantage:* minimal hardware, no central arbiter needed. (1 mark)\n2. **Centralized (Parallel) Arbitration** — *Advantage:* low and deterministic arbitration latency. (1 mark)\n3. **Distributed Arbitration** — *Advantage:* scalable and fault-tolerant, no single point of failure. (1 mark)\n\n---\n\n## 5.2 Part B Questions (14 Marks) — Module Internal Choice\n\n### **Question A (14 Marks)**\n\n#### **(a) [7 marks] Explain the five phases of a bus transaction with a neat state diagram. How does split-transaction pipelining improve bus utilization?**\n\n**Model Answer:**\n\nThe **five phases** of a synchronous bus transaction are:\n\n1. **Arbitration Phase** — The master asserts `BUSREQ`. The arbiter evaluates all pending requests and asserts `BUSGRANT` to the winning master. Cost: $T_{\\text{arb}}$ cycles.\n2. **Address Phase** — The master places the target address on the address bus and asserts control lines (`RD`/`WR`, `MREQ`). Cost: 1 cycle.\n3. **Wait Phase** — If the slave needs more time, it inserts wait states by asserting `WAIT`. Cost: $T_{\\text{wait}}$ cycles (variable).\n4. **Data Phase** — The data word (or burst) is transferred on the data bus. Cost: $n \\times T_c$ for an $n$-word burst.\n5. **Bus Release Phase** — The master de-asserts `BUSREQ`; the arbiter hands the bus to the next requestor.\n\n**Total cycle count:**\n\n$$\nT_{\\text{total}} = T_{\\text{arb}} + 1 + T_{\\text{wait}} + n\n$$\n\n**Split-Transaction Pipelining:**  \nIn a conventional bus, the bus is held *idle* between the address and data phases (e.g., waiting for a slow memory read). Split-transaction buses **release the bus** after the address phase and **re-arbitrate** — the data reply is tagged with a *Transaction ID* and re-inserted into the bus when ready. This allows other masters to use the bus during the wait, dramatically increasing $U$.\n\n**State diagram (already drawn in Section 4.2 — reproduce verbatim for full marks).**\n\n*(Mark split: 1 + 1 + 1 + 1 + 1 = 5 marks for phases; 2 marks for split-transaction explanation.)*\n\n#### **(b) [7 marks] A 32-bit bus operates at 100 MHz. A processor issues memory reads with average inter-arrival time of 4 cycles. The bus has 2 cycles of arbitration overhead and 1 cycle of address phase. Compute the bus utilization and the maximum number of processors that can be supported before saturation.**\n\n**Model Answer:**\n\n**Step 1 — Compute request rate per processor.**  \nInter-arrival time = 4 cycles ⇒ $p = 1/4 = 0.25$ requests/cycle per processor.\n\n**Step 2 — Compute bandwidth first.**  \nFor an SDR bus: $\\text{BW} = \\dfrac{32 \\times 100 \\times 10^6}{8} = 400$ MB/s.\n\n**Step 3 — Bus utilization for one processor.**  \nFor one processor, $U_1 = p = 0.25$ (unsaturated regime).\n\n**Step 4 — Find $N_{\\text{sat}}$.**  \nSaturation occurs when $N \\cdot p = 1$:\n\n$$\nN_{\\text{sat}} = \\dfrac{1}{p} = \\dfrac{1}{0.25} = 4 \\text{ processors}\n$$\n\n**Step 5 — Interpretation.**  \nWith 4 processors, the bus is **saturated** ($U = 1$). A 5th processor would queue requests, increasing latency without adding throughput.\n\n*(Mark split: 2 marks setup, 2 marks calculation, 2 marks interpretation, 1 mark for units/sanity check.)*\n\n---\n\n### **Question B (14 Marks) — Alternative Choice**\n\n#### **(a) [7 marks] Compare centralized, daisy-chain, and distributed bus arbitration schemes. Under what HPC conditions is distributed arbitration preferred?**\n\n**Model Answer:**\n\n| Aspect | Daisy Chain | Centralized | Distributed |\n|---|---|---|---|\n| Hardware | Minimal (wire-OR) | Arbiter IC + GRANT line per master | Per-master arbitration ID |\n| Latency | High (cumulative propagation) | Low (one arbiter delay) | Medium (wire-OR resolve) |\n| Scalability | Poor (chain length) | Limited (arbiter fan-out) | Excellent |\n| Fault Tolerance | Single chain break = total failure | Arbiter failure = total failure | One master can fail without global loss |\n| Fairness | Fixed by physical order | Programmable | Token-passing / lottery |\n\n**HPC preference for distributed arbitration:**  \nDistributed arbitration is preferred in **large-scale HPC** when (i) the number of masters is high (>8 cores), (ii) the system must tolerate arbitrary master failures, and (iii) global fairness must be guaranteed without a single arbiter becoming a bottleneck. Examples include **SCI rings**, **PCIe switched fabrics**, and **NoC meshes** in modern many-core processors.\n\n*(Mark split: 3 marks for table, 2 marks for narrative, 2 marks for HPC conditions.)*\n\n#### **(b) [7 marks] Derive the relationship between maximum bus clock frequency and physical bus length. Given a bus length of 0.5 m, a signal velocity of $1.5 \\times 10^8$ m/s, and a derating factor of 5, compute the maximum operating frequency.**\n\n**Model Answer:**\n\n**Derivation:**  \nA bus transaction requires at least one round-trip signal propagation (address out, data back). For a trace of length $d$:\n\n$$\nt_{\\text{rt}} = \\dfrac{2d}{v}\n$$\n\nThe clock period must exceed $k \\cdot t_{\\text{rt}}$ for safe setup/hold, hence:\n\n$$\nf_{\\max} = \\dfrac{v}{2 \\times k \\times d}\n$$\n\n**Numerical computation:**\n\n$$\nf_{\\max} = \\dfrac{1.5 \\times 10^8}{2 \\times 5 \\times 0.5} = \\dfrac{1.5 \\times 10^8}{5.0} = 3.0 \\times 10^7 \\text{ Hz} = 30 \\text{ MHz}\n$$\n\n**Interpretation:**  \nAt 0.5 m, the bus is fundamentally capped at **30 MHz** — explaining why HPC moved to point-to-point links (e.g., Intel QPI, AMD Infinity Fabric) where the *effective* $d$ per link is just a few centimeters.\n\n*(Mark split: 3 marks derivation, 2 marks substitution, 2 marks interpretation.)*\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall Callout — Where students lose marks on \"Buses\":**\n> 1. **Confusing bus *frequency* with bus *bandwidth*** — a 64-bit / 1 GHz bus does **not** transfer 1 GB/s; you must compute $W \\times f / 8$ and account for SDR vs. DDR.\n> 2. **Forgetting the protocol efficiency $\\eta$** in bandwidth questions — board answer without $\\eta$ loses 1–2 marks.\n> 3. **Omitting the state diagram** in a 7-mark question on bus transactions — the diagram is *worth at least 2 marks by itself*.\n> 4. **Mixing up arbitration schemes** — students frequently write \"daisy chain\" for \"centralized\". Memorize the difference (chain = wire-OR; centralized = arbiter IC + per-master `GRANT`).\n> 5. **Ignoring the saturation regime** in multi-processor bus problems — always state whether $N \\cdot p \\le 1$ or $> 1$ before quoting utilization.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember (Rapid-Revision Checklist)\n\n- A **bus** = address + data + control lines, shared, arbitrated, broadcast-capable.\n- The **three signal groups** are: Address (unidirectional), Data (bidirectional), Control (timing/arb).\n- The **five phases** of a transaction: Arbitration → Address → Wait → Data → Release. Memorize the order and the cycle-cost of each.\n- **Bandwidth formula:** $\\text{BW} = W \\times f$ (bits/s); divide by 8 for Bytes/s; multiply by 2 for DDR.\n- **Real bandwidth:** $\\text{BW}_{\\text{real}} = \\eta \\times W \\times f / 8$ where $\\eta \\in (0, 1]$ is the protocol efficiency.\n- **Saturation condition:** $N \\cdot p = 1$ is the bus-bandwidth wall; above this, only latency grows.\n- **Three arbitration classes:** Daisy Chain (cheap, slow), Centralized (fast, fragile), Distributed (scalable, fault-tolerant).\n- **Physical limit:** $f_{\\max} = v / (2 k d)$ — long buses run slowly; hence the move to point-to-point links.\n- **Modern HPC interconnects** (QPI, Infinity Fabric, NVLink) are *not* buses but evolved point-to-point derivatives — know the *why*.\n- **Bus utilization** $U = T_{\\text{data}} / T_{\\text{total}}$; split-transaction pipelining pushes $U \\to 1$ by hiding wait states.\n- **Snoopy coherence** is a *bus-specific* feature — the broadcast nature of the bus enables cheap cache coherency among processors.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "switchedandfattreenetworksmeshnetworkshybrids": {
      "title": "Switched and Fat-Tree Networks - Mesh Networks - Hybrids",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 2\n> - **Topic:** Switched and Fat-Tree Networks - Mesh Networks - Hybrids\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Switched Networks\n\n### Formal Definition\nA **Switched Network** is an interconnection architecture in which processing nodes (PEs / processors / end-nodes) are linked via dedicated **switching elements (routers)** rather than via direct point-to-point links. Packets are routed from source to destination by traversing one or more intermediate switches along configurable paths. The interconnection is *indirect* — meaning that not all nodes are directly connected to each other; communication occurs through the switch fabric.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Focus:** A switched network is the foundational building block for indirect topologies. Each switch typically has $p$ processor-side links (toward PEs) and $p$ network-side links (toward other switches) for a *radix-$2p$* switch.\n\n### Conceptual Analogy\nThink of a switched network like a **city's traffic roundabout system** with traffic police officers (switches) at every intersection. Cars (data packets) do not need a direct road from every house to every other house. Instead, they travel to the nearest roundabout, where the officer directs them along the right path. The advantage: fewer roads are required, but the officer must be smart enough to route traffic efficiently.\n\n### Key Metrics (with bold emphasis)\n- **Switch Radix ($k$):** number of bidirectional ports on a switch\n- **Bisection Bandwidth:** minimum aggregate bandwidth crossing a network cut that divides the system into two equal halves\n- **Diameter:** the maximum number of hops between any two nodes\n- **Network Latency:** time for a packet to travel from source to destination\n\n---\n\n## 1.2 Fat-Tree Networks\n\n### Formal Definition\nA **Fat-Tree Network**, introduced by Charles E. Leiserson in 1985, is a *tree-based indirect network* in which the **bandwidth (link capacity) increases as one approaches the root of the tree**. Each edge near the leaves carries less bandwidth, while edges near the root carry more — making the tree \"fat\" (visually wider) toward the top. Mathematically, a fat-tree built from $k$-port switches is a **$k$-ary fat-tree** that can connect $k^3/4$ end-nodes with full bisection bandwidth.\n\n> [!NOTE]\n> **Historical Note:** The fat-tree was originally proposed for the Connection Machine CM-5 supercomputer. It is a *universal* network — meaning it can simulate any other network of the same size with only constant-factor slowdown.\n\n### Conceptual Analogy\nImagine a **river delta system**. Tiny streams near the source carry small volumes of water. As they merge, the rivers grow wider and deeper. No single point gets \"flooded\" (congested) because capacity scales with the merging traffic. A fat-tree is conceptually similar — the more traffic that funnels together, the wider the channel becomes.\n\n### Topology Structure\n- Built from **identical $k$-port switches** arranged in **$\\log_k N$ levels** (for $N$ end-nodes)\n- **Half** the ports on each switch connect to lower levels (toward PEs or child switches)\n- **Half** the ports connect to the next higher level (toward parents)\n- Eliminates the classic *bisection bottleneck* of a standard (thin) tree\n\n---\n\n## 1.3 Mesh Networks\n\n### Formal Definition\nA **Mesh Network** (specifically a *$k$-ary $n$-cube*) is a *direct network* where each node has $2n$ neighbors, with $k$ nodes along each of the $n$ dimensions. For a 2D mesh, $n=2$; for a 3D mesh (cube), $n=3$. The total number of nodes is $N = k^n$. Mesh is a **regular, symmetric, direct** topology — each node has its own router, and PEs communicate by sending messages across a hop-by-hop grid.\n\n### Special Variants\n- **2D Mesh (Torus):** nodes arranged in a grid; toroidal mesh wraps around edges\n- **3D Mesh / Torus:** extends to three dimensions; widely used in modern HPC\n- **Hyper-cube:** $k=2$ (binary $n$-cube) with $N = 2^n$ nodes\n\n### Conceptual Analogy\nPicture a **chessboard** or a **city grid**. Each house (processor) sits at an intersection and can talk only to its 4 immediate neighbors (up/down/left/right). To send a message across the city, the message must \"hop\" from one intersection to the next — like a courier walking block by block. In a **toroidal mesh**, the board wraps around, so the top edge connects to the bottom (think of a donut-shaped city).\n\n---\n\n## 1.4 Hybrid Networks\n\n### Formal Definition\n**Hybrid Networks** combine *two or more distinct network topologies* within the same system to exploit complementary strengths. Typical examples include **Dragonfly** (groups of all-to-all routers connected via long-haul links), **Torus + Tree** (mesh for local traffic, tree for global), and **Clos networks** (multi-stage switching using smaller crossbars to build a large one). Hybrids are designed to balance **locality exploitation** (short distance) with **global bandwidth provisioning** (long distance).\n\n> [!IMPORTANT]\n> **KTU 2024 Trend:** Most modern Top500 supercomputers (Frontier, Fugaku, LUMI) use **Dragonfly**, **Torus**, or **Slingshot** topologies — all of which are hybrid by design.\n\n### Conceptual Analogy\nA hybrid network is like a **modern city's transportation system** — local streets handle neighborhood traffic, arterial roads handle city-wide traffic, and highways connect distant suburbs. You do not use a highway to go to the next-door grocery shop, nor do you use a narrow alley to travel between cities. Each network layer is optimized for its own scale of communication.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Switched Networks — Operating Principles\n\nA switched network routes packets by *destination address lookup* at every intermediate switch. The switching mechanism may be:\n1. **Store-and-Forward (SAF)** — entire packet is buffered, then forwarded\n2. **Wormhole Routing** — packet is split into *flits* (flow control digits); the head flit establishes the path, and subsequent flits follow in a *pipeline*\n3. **Cut-Through Routing** — forwarding begins as soon as the header is received\n\n### Why Switches are Used\n- **Scalability:** A $k$-port switch can interconnect up to $k$ devices; a hierarchy of switches scales to millions of nodes\n- **Cost Reduction:** Point-to-point links become prohibitively expensive for large $N$; switches amortize cost\n- **Fault Tolerance:** Multiple paths exist; routing can be reconfigured dynamically\n- **Aggregation:** Multiple low-bandwidth links aggregate to a high-bandwidth fabric\n\n### Crossbar Switch (Building Block of Switches)\nA **crossbar** is a switch with $N$ inputs and $N$ outputs that can connect any input to any output simultaneously. The crossbar is the workhorse of HPC fabrics.\n\n- **Crossbar Complexity:** $O(N^2)$ crosspoints\n- **Blocking:** A crossbar is **strictly non-blocking** (a connection can always be made without disturbing others)\n\n---\n\n## 2.2 Fat-Tree — Theoretical Analysis\n\n### Levels of a $k$-ary Fat-Tree\nFor a $k$-ary fat-tree using $k$-port switches:\n- **Level 0 (leaves):** switches with $k/2$ ports to PEs, $k/2$ ports upward\n- **Levels 1, 2, …, $\\log_2 k - 1$:** intermediate switches\n- **Total switch count:** $N_{sw} = k^2$\n- **Total PE count (end-nodes):** $N = k^3 / 4$\n- **Levels in the tree:** $\\log_2 k$\n\n### Properties\n- **Diameter:** $\\approx 2 \\log_k N$ (between any two PEs)\n- **Bisection Bandwidth:** Full — equal to the total leaf bandwidth\n- **Non-blocking:** End-nodes can communicate with full bandwidth simultaneously\n\n> [!NOTE]\n> **Cost Analysis:** A $k$-ary fat-tree uses $k^2$ switches. Each switch has $k$ ports. Total port count is $k^3$. Half of all ports (i.e., $k^3/2$) connect to PEs, but only half of those are used ($k^3/4$ PEs), because the other half would connect to non-existent PEs at the root. This is a deliberate waste to ensure non-blocking bisection.\n\n### Real-World Utility\n- The fat-tree is the **default topology of Infiniband** networks (used in Top500 systems)\n- Used in **Ethernet fabrics** (e.g., Cisco, Arista data-center switches)\n- Provides predictable performance for MPI applications\n\n---\n\n## 2.3 Mesh Network — Theoretical Analysis\n\n### Topology Parameters of a $k$-ary $n$-cube\n\nThe general formulation of a $k$-ary $n$-mesh (and torus) is given below. Note that $k$ = nodes per dimension, $n$ = number of dimensions, and $N = k^n$ = total nodes.\n\n- **Nodes per Dimension ($k$):** the number of nodes along one axis\n- **Dimensions ($n$):** typically 2 or 3\n- **Degree (links/node):** $2n$ for a torus, $2n$ for a mesh (with boundary nodes having fewer)\n- **Diameter:** $n(k-1)$ for mesh, $n \\lfloor k/2 \\rfloor$ for torus\n- **Average Distance:** approximately $n \\cdot k / 3$\n- **Bisection Width:** $k^{n-1}$ links crossing the central cut\n- **Total Links:** $n \\cdot k^n$ for torus, $n(k^n - k^{n-1})$ for mesh\n\n### Routing in Mesh\n- **Dimension-Order Routing (DOR):** XY routing in 2D — first travel in X, then in Y\n- **Deadlock-Free:** Yes, by construction in DOR\n- **Adaptive Routing:** Allows detour around congested links (e.g., ROMM, O1turn)\n\n### Real-World Utility\n- **Torus 3D** is used in the **Fujitsu Fugaku** (Tofu-D interconnect)\n- **2D/3D Mesh** is used in the **Intel Xeon Phi** (KNC) family\n- **Torus 2D** is used in the **IBM Blue Gene** family (BG/Q)\n\n---\n\n## 2.4 Hybrid Networks — Theoretical Analysis\n\nThe two most-cited hybrid topologies in modern HPC are:\n\n### Dragonfly (used in Cray XC, HPE Cray EX, Slingshot)\n- **Levels:** Router → Group → Network\n- **Group:** $a$ routers, each with $p$ ports, fully connected via *local* electrical links\n- **Global:** $g$ groups, each with $h$ global links per router\n- **Total nodes:** $N = a \\cdot p \\cdot (\\text{PEs per router}) \\approx a \\cdot p \\cdot (p - 2h)$\n\n### Hierarchical / Dragonfly+ (Tofu Interconnect D)\n- Combines **Tori** at the local level and a **switches/switches of switches** at the global level\n\n---\n\n## 2.5 KTU Formula Sheet / Cheat Sheet\n\n| # | Topology | Nodes ($N$) | Degree | Diameter | Bisection Width | Bisection Bandwidth |\n|---|----------|-------------|--------|----------|-----------------|---------------------|\n| 1 | $k$-ary $n$-mesh | $k^n$ | $\\le 2n$ | $n(k-1)$ | $k^{n-1}$ | $k^{n-1} \\cdot B_{\\text{link}}$ |\n| 2 | $k$-ary $n$-torus | $k^n$ | $2n$ | $n \\lfloor k/2 \\rfloor$ | $2 k^{n-1}$ | $2 k^{n-1} \\cdot B_{\\text{link}}$ |\n| 3 | Hypercube ($k=2$) | $2^n$ | $n$ | $n$ | $2^{n-1}$ | $2^{n-1} \\cdot B_{\\text{link}}$ |\n| 4 | $k$-ary Fat-Tree | $k^3/4$ | $k$ | $\\approx 2 \\log_k N$ | $k^2/2$ | $(k^2/2) \\cdot B_{\\text{link}}$ |\n| 5 | Crossbar | $N$ | $N$ | $1$ | $N$ | $N \\cdot B_{\\text{link}}$ |\n\n> [!IMPORTANT]\n> **Rule of Thumb for KTU Exams:** Cost of a topology $\\approx$ total number of links (or wires). For a $k$-ary $n$-cube, the link count is $n \\cdot k^n$ (torus) or $n(k^n - k^{n-1})$ (mesh). For a fat-tree, link count is roughly $k^3$.\n\n### Key Dimensional Relationships\n\n$$\nN = k^n \\quad \\Rightarrow \\quad k = N^{1/n}\n$$\n\nFor *constant bisection bandwidth* with $N$ nodes:\n\n$$\nB_{\\text{bisection}} = k^{n-1} = N^{(n-1)/n}\n$$\n\nFor a $d$-dimensional mesh, as $N$ grows, $n$ should also grow to keep bisection high. This is why **3D torus** is preferred for *exascale* systems.\n\n---\n\n## 2.6 Why Each Topology Matters in Engineering\n\n- **Switched networks** form the *backbone of every data center* in the world\n- **Fat-tree** is the *industry standard* (Infiniband, Ethernet) due to its non-blocking property\n- **Mesh / Torus** is favored for *tightly coupled HPC* (weather, molecular dynamics) where local traffic dominates\n- **Hybrids (Dragonfly, Slingshot)** dominate *exascale* (Frontier, Fugaku, LUMI) because they balance cost, locality, and global bandwidth\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: Bisection Bandwidth of a $k$-ary $n$-cube Mesh\n\n**Goal:** Derive the bisection bandwidth of a $k$-ary $n$-mesh, $B_{\\text{bisection}} = k^{n-1}$ (in units of links).\n\n**Step 1: Define the topology.**\nA $k$-ary $n$-mesh has $k$ nodes along each of $n$ dimensions. Total nodes:\n$$\nN = k^n\n$$\n\n**Step 2: Identify the bisection cut.**\nThe bisection of a $k$-ary $n$-mesh is a cut that splits the network into two halves of $N/2$ nodes. The most efficient cut is a hyperplane perpendicular to one dimension (say, dimension 1), placed midway through the $k$ nodes of that dimension.\n\n**Step 3: Count links crossing the cut.**\nThe cut has $k^{n-1}$ nodes on each side. Each node on the cut has exactly *one link* crossing into the other half (the link that connects it to its neighbor across the cut in dimension 1).\n\nTherefore, the number of links crossing the bisection is:\n$$\nB_{\\text{cut}} = k^{n-1}\n$$\n\n**Step 4: Convert to bandwidth.**\nIf each link has bandwidth $B_{\\text{link}}$ (in bytes/sec or bits/sec):\n$$\nB_{\\text{bisection}} = k^{n-1} \\cdot B_{\\text{link}}\n$$\n\n**Step 5: Express in terms of $N$.**\nSubstituting $k = N^{1/n}$:\n$$\nB_{\\text{bisection}} = \\left(N^{1/n}\\right)^{n-1} \\cdot B_{\\text{link}} = N^{(n-1)/n} \\cdot B_{\\text{link}}\n$$\n\n**Conclusion:** As $N$ grows, $B_{\\text{bisection}}$ scales as $N^{(n-1)/n}$. To maintain high bisection, increase $n$ (the number of dimensions). This is the *theoretical reason* why 3D torus (high $n$) is preferred for very large HPC systems.\n\n---\n\n## 3.2 Derivation: Latency of a Message in Wormhole Routing\n\n**Goal:** Derive the per-message latency for wormhole-routed networks.\n\n**Step 1: Define components of latency.**\nLatency $L$ is composed of:\n- $L_h$ = per-hop latency (router pipeline delay)\n- $t_c$ = channel (link) traversal time\n- $L_p$ = propagation delay along physical media\n- $L_s$ = serialization delay (time to push all bits of the packet onto the wire)\n\n**Step 2: Identify the number of hops.**\nFor a $k$-ary $n$-cube mesh, the average number of hops for a uniform random destination is:\n$$\n\\bar{H}_{\\text{mesh}} = \\frac{n \\cdot (k-1)}{3}\n$$\n\nFor a $k$-ary $n$-cube torus, the average is:\n$$\n\\bar{H}_{\\text{torus}} = \\frac{n \\cdot \\lfloor k/2 \\rfloor}{3}\n$$\n\n**Step 3: Construct the latency equation.**\n$$\nL = L_h \\cdot H + L_p \\cdot H + t_c \\cdot H + L_s + L_{\\text{contention}}\n$$\n\nwhere $L_{\\text{contention}}$ is the queuing delay due to link/router contention.\n\n**Step 4: Simplify for unloaded network.**\nIgnoring contention:\n$$\nL = H \\cdot (L_h + L_p + t_c) + L_s\n$$\n\n**Step 5: Serialization delay.**\nIf packet size is $P$ bytes and link bandwidth is $B$ bytes/sec:\n$$\nL_s = \\frac{P}{B}\n$$\n\n**Conclusion:** The latency grows linearly with hop count. This is why low-diameter topologies (fat-tree, dragonfly) are favored for global traffic.\n\n---\n\n## 3.3 Derivation: Number of PEs in a $k$-ary Fat-Tree\n\n**Goal:** Show that a $k$-ary fat-tree connects $k^3/4$ end-nodes.\n\n**Step 1: Recall switch structure.**\nEach switch has $k$ ports. Of these, $k/2$ ports connect *downward* (toward PEs or child switches) and $k/2$ ports connect *upward* (toward parent switches).\n\n**Step 2: Identify level 0 (leaf switches).**\nA level-0 switch has $k/2$ ports to PEs. So it connects $k/2$ end-nodes.\n\n**Step 3: Count number of leaf switches.**\nThere are $k^2/2$ leaf switches (this is half of the total $k^2$ switches, justified by the structure of the fat-tree which uses $k^2$ switches arranged across multiple levels).\n\nWait, let me redo this more carefully. The standard $k$-ary fat-tree uses:\n- $k^2/2$ leaf switches\n- $k^2/4$ level-1 switches\n- $k^2/8$ level-2 switches, etc.\n\n**Step 4: Total end-nodes.**\n$$\nN_{PE} = (\\text{number of leaf switches}) \\times (\\text{PEs per leaf switch}) = \\frac{k^2}{2} \\cdot \\frac{k}{2} = \\frac{k^3}{4}\n$$\n\n**Step 5: Verify with an example.**\nFor $k = 4$: $N = 4^3/4 = 16$ PEs. A 4-ary fat-tree with 16 PEs has 4-port switches, with 2 PEs per leaf switch and 8 leaf switches. Total switches = $k^2 = 16$. Verified.\n\n---\n\n## 3.4 Comparative Analytical Matrix\n\n| Parameter | Crossbar | Fat-Tree | 3D Torus | Dragonfly |\n|-----------|----------|----------|----------|-----------|\n| Cost (links) | $N^2$ | $\\sim k^3$ | $3N$ | $\\sim N$ (per-port) |\n| Diameter | 1 | $2 \\log_k N$ | $O(N^{1/3})$ | $\\le 3$ |\n| Bisection BW | Full | Full | $2 N^{2/3} B_L$ | $a \\cdot h \\cdot B_L$ |\n| Hot-spot tolerant | No | Medium | High | High |\n| Cable complexity | Very High | Medium | Medium | Low |\n| Used In | Small SMPs | Infiniband, Ethernet | Fugaku, BG/Q | Frontier, LUMI, Perlmutter |\n\n> [!IMPORTANT]\n> **Insight:** Notice the trade-off between *cost* and *diameter*. A crossbar has diameter 1 but cost $N^2$, so it is impossible for large $N$. A dragonfly has diameter 3 and near-linear cost, hence favored for exascale.\n\n---\n\n## 3.5 Worked Example — Cost-Performance Tradeoff\n\n**Problem:** You must build an interconnect for $N = 1024$ nodes. Compare:\n- Option A: 3D mesh with $k = 1024^{1/3} \\approx 10$ (using 10-ary 3-cube)\n- Option B: 32-ary fat-tree (using 32-port switches)\n\n**Solution A (3D mesh):**\n$$\nB_{\\text{bisection}} = k^{n-1} = 10^2 = 100 \\text{ links}\n$$\nDiameter = $3 \\cdot 9 = 27$ hops.\n\n**Solution B (32-ary fat-tree):**\nNumber of PEs = $32^3/4 = 8192$ PEs (more than needed; can scale down).\nDiameter $\\approx 2 \\log_2 32 = 10$ hops.\nBisection bandwidth: full.\n\n**Conclusion:** The fat-tree has lower diameter and full bisection, but uses $\\sim 32^3 = 32768$ link-equivalents. The mesh uses $3 \\times 1024 = 3072$ links. **Trade-off identified.**\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Switched Network — Crossbar Architecture\n\n```mermaid\nflowchart LR\n  N1[PE 1] --> S1A[Input Port 1]\n  N2[PE 2] --> S2A[Input Port 2]\n  N3[PE 3] --> S3A[Input Port 3]\n  N4[PE 4] --> S4A[Input Port 4]\n  S1A --> XBAR[Crossbar Fabric N x N]\n  S2A --> XBAR\n  S3A --> XBAR\n  S4A --> XBAR\n  XBAR --> O1A[Output Port 1]\n  XBAR --> O2A[Output Port 2]\n  XBAR --> O3A[Output Port 3]\n  XBAR --> O4A[Output Port 4]\n  O1A --> D1[PE 1 out]\n  O2A --> D2[PE 2 out]\n  O3A --> D3[PE 3 out]\n  O4A --> D4[PE 4 out]\n```\n\n> **Reading the Diagram:** A crossbar is a *strictly non-blocking* switch where any input port can be connected to any output port simultaneously, with up to $\\min(N_{\\text{in}}, N_{\\text{out}})$ concurrent connections.\n\n---\n\n## 4.2 4-ary Fat-Tree Topology\n\n```mermaid\nflowchart TB\n  subgraph LEAF[Level 0 Leaf Switches]\n    L1[Switch L1]\n    L2[Switch L2]\n    L3[Switch L3]\n    L4[Switch L4]\n    L5[Switch L5]\n    L6[Switch L6]\n    L7[Switch L7]\n    L8[Switch L8]\n  end\n  subgraph SPINE[Level 1 Spine Switches]\n    SP1[Spine S1]\n    SP2[Spine S2]\n    SP3[Spine S3]\n    SP4[Spine S4]\n  end\n  PE1[PE] --> L1\n  PE2[PE] --> L1\n  L1 --> SP1\n  L1 --> SP2\n  L2 --> SP1\n  L2 --> SP3\n  L3 --> SP2\n  L3 --> SP4\n  L4 --> SP3\n  L4 --> SP4\n  SP1 --> A1[Core Aggregation]\n  SP2 --> A1\n  SP3 --> A2[Core Aggregation]\n  SP4 --> A2\n```\n\n> **Reading the Diagram:** Each leaf switch has 2 PEs (since $k=4$, half ports = 2). Each leaf has 2 upward links to distinct spine switches. Spine switches aggregate traffic. Bandwidth doubles per level.\n\n---\n\n## 4.3 2D Mesh (4x4) Topology\n\n```mermaid\nflowchart LR\n  N00((0,0)) --- N10((1,0))\n  N10 --- N20((2,0))\n  N20 --- N30((3,0))\n  N00 --- N01((0,1))\n  N10 --- N11((1,1))\n  N20 --- N21((2,1))\n  N30 --- N31((3,1))\n  N01 --- N02((0,2))\n  N11 --- N12((1,2))\n  N21 --- N22((2,2))\n  N31 --- N32((3,2))\n  N02 --- N03((0,3))\n  N12 --- N13((1,3))\n  N22 --- N23((2,3))\n  N32 --- N33((3,3))\n  N01 --- N11\n  N02 --- N12\n  N03 --- N13\n```\n\n> **Reading the Diagram:** A 4×4 mesh has 16 nodes. Each interior node has 4 neighbors. Boundary nodes (corners have 2, edges have 3) have fewer. Diameter = $4+4-2 = 6$ hops (or 2D Manhattan from corner to corner).\n\n---\n\n## 4.4 2D Torus (4x4) — Wraps Around\n\n```mermaid\nflowchart LR\n  N00((0,0)) --- N10((1,0))\n  N10 --- N20((2,0))\n  N20 --- N30((3,0))\n  N30 -.wrap.-> N00\n  N00 --- N01((0,1))\n  N10 --- N11((1,1))\n  N20 --- N21((2,1))\n  N30 --- N31((3,1))\n  N31 -.wrap.-> N01\n  N01 --- N02((0,2))\n  N11 --- N12((1,2))\n  N21 --- N22((2,2))\n  N31 --- N32((3,2))\n  N32 -.wrap.-> N02\n  N02 --- N03((0,3))\n  N12 --- N13((1,3))\n  N22 --- N23((2,3))\n  N32 --- N33((3,3))\n  N33 -.wrap.-> N03\n  N01 --- N11\n  N02 --- N12\n  N03 --- N13\n```\n\n> **Reading the Diagram:** A torus adds *wrap-around* links (shown as dashed lines). All nodes now have degree exactly 4. Diameter drops from 6 (mesh) to 4 (torus) because you can go either way around the loop.\n\n---\n\n## 4.5 Dragonfly Hybrid Topology\n\n```mermaid\nflowchart TB\n  subgraph G1[Group 1]\n    R1A[Router A]\n    R1B[Router B]\n    R1C[Router C]\n    R1A --- R1B\n    R1B --- R1C\n    R1C --- R1A\n  end\n  subgraph G2[Group 2]\n    R2A[Router D]\n    R2B[Router E]\n    R2C[Router F]\n    R2A --- R2B\n    R2B --- R2C\n    R2C --- R2A\n  end\n  subgraph G3[Group 3]\n    R3A[Router G]\n    R3B[Router H]\n    R3C[Router I]\n    R3A --- R3B\n    R3B --- R3C\n    R3C --- R3A\n  end\n  R1A -.global.-> R2A\n  R1B -.global.-> R3B\n  R2C -.global.-> R3A\n```\n\n> **Reading the Diagram:** A Dragonfly has two levels — *intra-group* (local all-to-all between routers, shown as solid lines) and *inter-group* (long-haul global links, shown as dashed). Each router connects to PEs, to its group-mates, and to routers in other groups.\n\n---\n\n## 4.6 Switched Network — Multi-Stage Block Diagram\n\n```mermaid\nflowchart LR\n  P1[PE] --> STG1[Stage 1 Switches]\n  P2[PE] --> STG1\n  P3[PE] --> STG1\n  P4[PE] --> STG1\n  STG1 --> STG2[Stage 2 Switches]\n  STG2 --> STG3[Stage 3 Switches]\n  STG3 --> Q1[PE]\n  STG3 --> Q2[PE]\n  STG3 --> Q3[PE]\n  STG3 --> Q4[PE]\n```\n\n> **Reading the Diagram:** A multi-stage interconnection network (MIN) chains switches in stages. The Clos network is a popular 3-stage MIN used to build large non-blocking switches from smaller ones.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## Part A Questions (3 Marks Each)\n\n### Question 1: Define Fat-Tree Network\n**`[KTU University Exam - Dec 2023, May 2024]`**\n\n**Question:** Define a Fat-Tree network. Why is it called a \"fat\" tree?\n\n**Model Answer (3 marks):**\n\nA fat-tree is a tree-based indirect interconnection network in which the link bandwidth *increases* as one moves from the leaves toward the root. For a $k$-port switch, the bandwidth at level $l$ from the leaves is doubled compared to level $l+1$, ensuring that no link becomes a bottleneck.\n\nIt is called a \"fat\" tree because, unlike a traditional (thin) tree where each parent has only one link from each child, the fat-tree has *multiple parallel links* between levels. This visual \"fatness\" provides full bisection bandwidth. **(3 marks — 1 for definition, 1 for the \"fat\" reason, 1 for bisection property.)**\n\n---\n\n### Question 2: Bisection Bandwidth of Mesh\n**`[KTU University Exam - May 2023]`**\n\n**Question:** Compute the bisection bandwidth (in terms of links) of a 6-ary 2-mesh.\n\n**Model Answer (3 marks):**\n\nFor a $k$-ary $n$-mesh, bisection width (in links) = $k^{n-1}$.\n\nSubstituting $k = 6$, $n = 2$:\n$$\nB_{\\text{bisection}} = 6^{2-1} = 6^1 = 6 \\text{ links}\n$$\n\n**Therefore, the bisection bandwidth is $6 \\cdot B_{\\text{link}}$.** (3 marks — 1 for formula, 1 for substitution, 1 for answer.)\n\n---\n\n## Part B Questions (14 Marks Each)\n\n### Question A: Comparative Analysis of Topologies (14 marks)\n**`[KTU University Exam - Dec 2023]`**\n\n**(a)** Compare the structural properties (nodes, degree, diameter, bisection bandwidth) of the following topologies, assuming each is built to interconnect $N = 1024$ end-nodes:\n- (i) 3D mesh (10-ary 3-cube)\n- (ii) 8-ary fat-tree\n**(7 marks)**\n\n**(b)** A parallel application has a communication pattern where 80% of messages are exchanged between nearest-neighbor PEs (1 hop away) and 20% are global all-to-all. Which topology is more suitable and why? **(7 marks)**\n\n**Model Solution:**\n\n**(a) Comparison Table:**\n\n| Property | 10-ary 3-Mesh | 8-ary Fat-Tree |\n|----------|----------------|----------------|\n| Total nodes $N$ | $10^3 = 1000 \\approx 1024$ | $8^3/4 = 128$ (use multiple) |\n| Degree | 6 (interior), less at boundary | 8 |\n| Diameter | $3(10-1) = 27$ | $2 \\log_8 1024 = 2 \\cdot 10/3 \\approx 7$ |\n| Bisection Width | $10^2 = 100$ links | Full = $8^2/2 = 32$ links (per instance) |\n| Total links | $\\sim 3 \\cdot 1000 = 3000$ | $\\sim 8^3 = 512$ per instance |\n| Cost | Lower | Higher per switch |\n\nFor 1024 nodes, the fat-tree would need to scale up. A 32-ary fat-tree gives $32^3/4 = 8192$ PEs (more than enough), while 4-ary gives only 16 PEs. Selecting $k=16$: $N = 16^3/4 = 1024$ PEs, diameter $\\approx 2 \\log_2 16 = 8$ hops. **(7 marks — table + commentary)**\n\n**(b) Recommendation: 3D Mesh is more suitable.**\n\nReasoning:\n- The 3D mesh has **short hop count for nearest neighbors** (typically 1-2 hops), which dominates 80% of traffic\n- For global 20% traffic, both topologies suffer, but the **mesh's predictable nearest-neighbor pattern** is well-suited for stencil and PDE solvers\n- The fat-tree's advantage (full bisection) is wasted when 80% of traffic is local\n- 3D mesh has **lower cost and simpler physical cabling** than fat-tree\n- For all-to-all (20%), a fat-tree would be better, but locality dominates\n\n**Conclusion:** For workloads with high communication locality (typical of HPC scientific codes), a mesh/torus is preferred. The fat-tree wins for unstructured / all-to-all patterns. **(7 marks — 2 for analysis, 2 for nearest-neighbor advantage, 2 for global, 1 for cost factor)**\n\n---\n\n### Question B: Mesh Network Design and Routing (14 marks)\n**`[KTU University Exam - May 2024]`**\n\n**(a)** Consider a 4×4 2D mesh. For a packet sent from source (0,0) to destination (3,2), describe dimension-order routing (DOR / XY routing). List the intermediate nodes visited. **(7 marks)**\n\n**(b)** A 5-ary 2-cube (5×5 mesh) is to be interconnected using bidirectional links of bandwidth $B_{\\text{link}} = 10$ Gbps. Compute:\n- (i) Number of nodes\n- (ii) Diameter\n- (iii) Bisection bandwidth in Gbps\n- (iv) Total number of links **(7 marks)**\n\n**Model Solution:**\n\n**(a) Dimension-Order Routing (XY) from (0,0) to (3,2):**\n\nXY routing: First traverse in X direction, then in Y direction.\n\n- Start: (0,0)\n- Move in X to (3,0): visits (1,0), (2,0), (3,0)\n- Move in Y to (3,2): visits (3,1), (3,2)\n- Total hops: 5\n- Intermediate nodes: **(1,0), (2,0), (3,0), (3,1)** before reaching (3,2)\n\nDOR is **deadlock-free** because it imposes a strict order on resource acquisition (X channels before Y channels), breaking the cyclic dependency that causes deadlocks. **(7 marks — 2 for routing rule, 3 for path, 2 for deadlock-free explanation)**\n\n**(b) Numerical Computation:**\n\nGiven: $k = 5$, $n = 2$, $B_{\\text{link}} = 10$ Gbps.\n\n(i) Number of nodes:\n$$\nN = k^n = 5^2 = 25 \\text{ nodes}\n$$\n\n(ii) Diameter (for a mesh, not torus):\n$$\nD = n(k-1) = 2 \\cdot (5-1) = 8 \\text{ hops}\n$$\n\n(iii) Bisection bandwidth:\n$$\nB_{\\text{bisection}} = k^{n-1} \\cdot B_{\\text{link}} = 5^{1} \\cdot 10 = 50 \\text{ Gbps}\n$$\n\n(iv) Total links in a 2D mesh:\n$$\nL_{\\text{mesh}} = n(k^n - k^{n-1}) = 2(25 - 5) = 2 \\cdot 20 = 40 \\text{ links}\n$$\n\n**Summary of answers:** 25 nodes, 8 hops diameter, 50 Gbps bisection, 40 links. **(7 marks — 2 + 2 + 1.5 + 1.5 spread across sub-parts)**\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning / Common Pitfalls:**\n> - Confusing **mesh** and **torus** diameters — torus halves the diameter via wrap-around\n> - Using **wrong bisection formula** for fat-tree — it is $k^2/2$ (links), not $k^3/4$ (PEs)\n> - Forgetting that **port count** of a switch = $k$, and only $k/2$ ports face upward\n> - Mixing up **bisection width** (number of links) and **bisection bandwidth** (links $\\times$ $B_{\\text{link}}$)\n> - In routing questions, forgetting to state that DOR is **deadlock-free** and *why*\n\n---\n\n## Topic Recap & Important Things to Remember\n\n- **Switched Network:** Indirect topology using switches to route packets; $k$-port switches; building block of HPC fabrics\n- **Fat-Tree:** Tree topology with bandwidth doubling at every level; $k$-ary fat-tree connects $k^3/4$ PEs with full bisection; uses $k^2$ switches; **industry standard** (Infiniband, Ethernet)\n- **Mesh:** Direct, regular, symmetric topology; $k$-ary $n$-cube with $N = k^n$ nodes, diameter $n(k-1)$, bisection $k^{n-1}$\n- **Torus:** Mesh + wrap-around; diameter halved; used in **Fugaku** and **Blue Gene**\n- **Dragonfly:** Hybrid with intra-group (all-to-all) + inter-group (long-haul) links; used in **Frontier, LUMI, Perlmutter**; diameter 3\n- **Crossbar:** $N \\times N$ non-blocking switch; cost $O(N^2)$; used in small SMPs and as building block of larger switches\n- **Latency:** $L = H \\cdot (L_h + L_p + t_c) + L_s + L_{\\text{contention}}$\n- **Bisection bandwidth formula:** $B_{\\text{bisection}} = k^{n-1} \\cdot B_{\\text{link}}$ for $k$-ary $n$-mesh\n- **Deadlock-free routing:** DOR (XY) for mesh; up/down for fat-tree\n- **Modern HPC trend:** Hybrids (Dragonfly, Slingshot) dominate exascale; fat-trees dominate enterprise data centers\n\n> **Key Takeaway:** Each topology is a trade-off between **diameter**, **bisection bandwidth**, **cost (link count)**, and **scalability**. There is no universally \"best\" topology — the choice depends on the application's communication pattern, locality, and budget.\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "sharedmemoryparallelprogrammingwithopenmpshortintroductiontoopenmpparallelexecutiondatascopingopenmpworksharingforloopssynchronization": {
      "title": "Shared-Memory Parallel Programming with OpenMP - Short Introduction to OpenMP - Parallel Execution - Data Scoping - OpenMP Worksharing for Loops - Synchronization",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 3\n> - **Topic:** Shared-Memory Parallel Programming with OpenMP - Short Introduction to OpenMP - Parallel Execution - Data Scoping - OpenMP Worksharing for Loops - Synchronization\n\n<!-- SECTION_1_START -->\n# 📘 SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 What is OpenMP?\n\n**OpenMP (Open Multi-Processing)** is an application programming interface (API) that supports multi-platform **shared-memory multiprocessing** programming in C, C++, and Fortran. It consists of a set of **compiler directives, library routines, and environment variables** that allow a programmer to write parallel code by annotating (rather than rewriting) sequential source code.\n\n> [!IMPORTANT]\n> **KTU 2024 Syllabus Definition (Verbatim Style):**\n> *OpenMP is a portable, scalable API that provides a simple and flexible interface for developing shared-memory parallel applications across diverse architectures including desktops, servers, and supercomputers. It uses the fork-join execution model.*\n\nThe OpenMP standard is jointly maintained by a group of major hardware and software vendors (originally OpenMP Architecture Review Board - now **OpenMP ARB**), and is supported on virtually all modern multi-core CPUs (Intel, AMD, ARM, IBM Power).\n\n### Key Components of OpenMP\n\n| Component | Description |\n|---|---|\n| **Compiler Directives** | Pragmas like `#pragma omp parallel` that instruct the compiler |\n| **Runtime Library Routines** | Functions like `omp_get_thread_num()` for runtime control |\n| **Environment Variables** | Variables like `OMP_NUM_THREADS` to control execution environment |\n\n## 1.2 Conceptual Analogy: The Kitchen Brigade 🍳\n\nImagine a **head chef (master thread)** who has a recipe (sequential program) for preparing a banquet for 1000 guests.\n\n- **Sequential cooking** = The head chef prepares every single dish alone. It works, but is impossibly slow.\n- **Parallel cooking with OpenMP** = The head chef calls in **N assistant chefs (worker threads)**. They all stand in the **same kitchen, share the same pantry, stove, and ingredients (shared memory)**. The head chef hands out specific tasks (loop iterations) to each assistant.\n\n> [!NOTE]\n> **Key Analogy Insight:** All chefs work in the **same kitchen (shared memory)**. They can grab ingredients from the same shelf. But if two chefs try to stir the same pot simultaneously, things go wrong — this is the **race condition problem** that synchronization solves!\n\nIf a new complex dish needs an expert's touch, the assistants stop and the head chef takes over (a **synchronization barrier**). When done, the head chef may dispatch new tasks again.\n\nThis perfectly mirrors OpenMP's **Fork-Join Model** of execution, which we will study in detail in Section 2.\n\n## 1.3 The Three Pillars of OpenMP Programming\n\n1. **Parallel Execution Model** – Creating and managing threads\n2. **Data Scoping** – Defining what data is shared vs. private to threads\n3. **Synchronization** – Coordinating threads to avoid conflicts and ensure correctness\n\n## 1.4 Why OpenMP Matters in HPC (Real-World Context)\n\n- **Portability:** Same code runs on a laptop or a 128-core server.\n- **Incremental Parallelization:** You can parallelize one loop at a time — no need to rewrite the whole program.\n- **Industry Standard:** Used in scientific computing, weather modeling, CFD (computational fluid dynamics), molecular dynamics (NAMD, GROMACS), and machine learning libraries.\n- **Performance Scaling:** On multi-core CPUs (which represent >**95%** of all processors shipped worldwide), OpenMP is the de-facto standard for shared-memory parallelism.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 📘 SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Fork-Join Execution Model\n\nOpenMP programs begin as a single **master thread** executing sequentially. When the program encounters a **parallel region** (marked by `#pragma omp parallel`), the master thread **forks** — it creates a team of parallel threads. After all threads complete the work in the parallel region, they **join** back (synchronize) and execution continues sequentially with just the master thread.\n\n### Phases of the Fork-Join Model\n\n1. **Sequential Phase (Master Thread Only):** Program starts; only one thread runs.\n2. **Fork (Parallel Region Entry):** Master thread spawns a team of `N` worker threads. Total threads = `N + 1` (master + workers), though logically the master becomes one of the team.\n3. **Parallel Phase (Team Execution):** All threads in the team execute the code inside the parallel region concurrently.\n4. **Join (Implicit Barrier):** All threads wait at an **implicit barrier** at the end of the parallel region. Only the master thread continues.\n5. **Sequential Phase resumes:** Process repeats whenever a new parallel region is encountered.\n\n> [!IMPORTANT]\n> **KTU Board Exam Point:** The **implicit barrier** at the end of a parallel region is a critical concept. Threads do NOT proceed past the end of `#pragma omp parallel { ... }` until ALL team members have arrived. This is automatically enforced by the OpenMP runtime.\n\n## 2.2 Parallel Region Syntax (The Core Directive)\n\nThe fundamental OpenMP directive for parallel execution is:\n\n```c\n#pragma omp parallel [clause[ [,] clause]*]\n{\n    // Structured block of code executed by all threads\n}\n```\n\n### Common Clauses for `#pragma omp parallel`\n\n| Clause | Purpose | Example |\n|---|---|---|\n| `num_threads(n)` | Specifies exact number of threads to create | `num_threads(8)` |\n| `if(condition)` | Conditional parallelism | `if(N > 1000)` |\n| `private(list)` | Each thread gets its own uninitialized copy of variables | `private(i, sum)` |\n| `shared(list)` | All threads share the same variable | `shared(A, B, C)` |\n| `default(shared/none)` | Sets default scoping rule | `default(none)` |\n| `reduction(op:list)` | Performs reduction operation across threads | `reduction(+:sum)` |\n\n## 2.3 Data Scoping — The Heart of Correctness\n\n**Data scoping** rules in OpenMP determine how variables are accessed by threads. Misunderstanding scoping is the **#1 cause of bugs** in OpenMP programs.\n\n### Default Scoping Rules (Without `default` clause)\n\nBy OpenMP default, variables declared **outside** any parallel region are **SHARED** among all threads. Variables declared **inside** a parallel region are **PRIVATE** to each thread.\n\n### Types of Data Scoping Clauses\n\n1. **`shared(var)`** — All threads access the **same memory location**. Writes by one thread are visible to all others. Use with caution!\n2. **`private(var)`** — Each thread gets its own **uninitialized** copy. The original variable's value is undefined inside the parallel region and lost afterward.\n3. **`firstprivate(var)`** — Like `private`, but the variable is **initialized** from the original value at thread creation.\n4. **`lastprivate(var)`** — Like `private`, but the value from the **last loop iteration** (or last thread to execute) is copied back to the original variable.\n5. **`reduction(op: var)`** — Each thread maintains a local copy, applies the operation locally, then combines all copies using the reduction operator. Critical for summation-style loops.\n\n> [!NOTE]\n> **Reduction Example (Mental Model):** If 4 threads each compute partial sums (10, 20, 30, 40), `reduction(+:sum)` combines them: 10+20+30+40 = **100**. This avoids race conditions while aggregating results.\n\n## 2.4 Worksharing Constructs — Distributing the Work\n\nWorksharing constructs **distribute the execution of the associated statement(s)** among the threads of a team. They DO NOT create new threads — they only divide work among existing threads.\n\n### The `for` (Loop) Worksharing Construct\n\n```c\n#pragma omp parallel for [clause[ [,] clause]*]\nfor (i = 0; i < N; i++) {\n    // Loop body\n}\n```\n\nThe iterations of the `for` loop are **divided among the threads** in the team. If there are `T` threads and `N` iterations, each thread executes approximately `N/T` iterations (default: iterations are distributed in a **chunked/cyclic** manner, but the default schedule varies — typically static chunks).\n\n### Combined Parallel-Worksharing\n\n`#pragma omp parallel for` is a **combined construct** that:\n1. Creates a team of threads (parallel)\n2. Distributes loop iterations among them (worksharing)\n\nThis is more efficient than writing a separate `parallel` and `for` directive.\n\n### The `sections` Worksharing Construct\n\n```c\n#pragma omp parallel sections\n{\n    #pragma omp section\n    { /* code for thread 1 */ }\n    #pragma omp section\n    { /* code for thread 2 */ }\n}\n```\n\nDifferent threads execute different **independent code blocks (sections)**. Useful for **task-parallel** problems.\n\n### The `single` Construct\n\n```c\n#pragma omp single\n{ /* executed by exactly ONE thread */ }\n```\n\nUseful for I/O operations, initialization, etc.\n\n## 2.5 Synchronization Constructs\n\nWhen threads access shared data, **race conditions** can occur. OpenMP provides synchronization primitives to enforce ordering.\n\n### Synchronization Constructs Summary\n\n| Directive | Function | Use Case |\n|---|---|---|\n| `#pragma omp barrier` | Synchronization point | All threads wait for each other |\n| `#pragma omp critical` | Mutual exclusion | Only one thread executes block at a time |\n| `#pragma omp atomic` | Hardware-supported atomic update | Fast single-variable updates |\n| `#pragma omp ordered` | Preserves loop iteration order | When ordered output is required |\n| `flush` (directive or runtime call) | Memory consistency | Ensure all threads see latest values |\n| `omp_set_lock()` / `omp_unset_lock()` | Mutual exclusion locks | Fine-grained locking |\n| `#pragma omp master` | Code runs only on master | Avoids implicit barrier of `single` |\n\n> [!WARNING]\n> **Common Mistake:** Using `barrier` inside a `for` worksharing construct with the `nowait` clause — the barrier may be silently ignored. Be careful with synchronization ordering!\n\n## 2.6 KTU Formula Sheet (Performance Metrics)\n\n| Metric | Formula | Description |\n|---|---|---|\n| **Speedup (S)** | $S = \\dfrac{T_s}{T_p}$ | Ratio of sequential to parallel execution time |\n| **Efficiency (E)** | $E = \\dfrac{S}{P} = \\dfrac{T_s}{P \\cdot T_p}$ | Speedup per processor (0 to 1) |\n| **Amdahl's Law** | $S(N) = \\dfrac{1}{(1 - f) + \\dfrac{f}{N}}$ | Theoretical max speedup with $N$ processors, $f$ = parallel fraction |\n| **Gustafson's Law** | $S(N) = N - f \\cdot (N - 1)$ | Scaled speedup (problem size grows with $N$) |\n| **Cost** | $C = P \\cdot T_p$ | Total processor-time product |\n| **Karp-Flatt Metric** | $e = \\dfrac{\\dfrac{1}{S} - \\dfrac{1}{N}}{1 - \\dfrac{1}{N}}$ | Empirically measures serial fraction |\n\n> [!IMPORTANT]\n> In all formulas above, $T_s$ = sequential time, $T_p$ = parallel time, $P$ = number of processors/threads, $N$ = number of processors, $f$ = parallel fraction (0 to 1).\n\n## 2.7 Real-World Engineering Utility\n\n- **Weather Forecasting:** OpenMP parallelizes atmospheric models to predict storms 100x faster.\n- **Computational Fluid Dynamics (CFD):** ANSYS Fluent uses OpenMP for flow simulation.\n- **Genomics:** Sequence alignment tools use OpenMP on multi-core servers.\n- **AI/ML:** Some inference stages of deep learning use OpenMP for CPU-based computation.\n- **Cryptography:** Parallel hashing and key search algorithms use OpenMP.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 📘 SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: Amdahl's Law for OpenMP Speedup\n\nAmdahl's Law is the **fundamental theoretical bound** on parallel speedup. Let us derive it step-by-step, since this is a frequently asked KTU exam question.\n\n### Setup\n\nConsider a program where:\n- Fraction $f$ of the execution time can be parallelized (parallel portion)\n- Fraction $(1 - f)$ is strictly sequential (cannot be parallelized)\n- The program runs on $N$ processors/threads\n\n### Step-by-Step Derivation\n\nLet $T_s$ = total sequential execution time on **1 processor**.\n\nThe sequential time can be split into two parts:\n\n$$T_s = T_{seq} + T_{par}$$\n\nwhere $T_{seq} = (1 - f) \\cdot T_s$ and $T_{par} = f \\cdot T_s$.\n\n**Step 1: Sequential time on 1 processor:**\n\n$$T_s = (1 - f) \\cdot T_s + f \\cdot T_s$$\n\n**Step 2: Parallel time on $N$ processors:**\n\nThe sequential part $(1 - f) \\cdot T_s$ cannot be sped up, so its time remains $(1 - f) \\cdot T_s$.\n\nThe parallel part $f \\cdot T_s$ is ideally divided among $N$ processors, so it becomes $\\dfrac{f \\cdot T_s}{N}$.\n\nTherefore:\n\n$$T_p = (1 - f) \\cdot T_s + \\dfrac{f \\cdot T_s}{N}$$\n\n**Step 3: Speedup is $S(N) = \\dfrac{T_s}{T_p}$:**\n\n$$S(N) = \\dfrac{T_s}{(1 - f) \\cdot T_s + \\dfrac{f \\cdot T_s}{N}}$$\n\n**Step 4: Divide numerator and denominator by $T_s$:**\n\n$$S(N) = \\dfrac{1}{(1 - f) + \\dfrac{f}{N}}$$\n\nThis is **Amdahl's Law**, often written as:\n\n$$S(N) = \\dfrac{N}{N(1 - f) + f}$$\n\n**Step 5: Limit as $N \\to \\infty$:**\n\n$$\\lim_{N \\to \\infty} S(N) = \\dfrac{1}{1 - f}$$\n\nThis shows the **maximum achievable speedup** is bounded by the sequential fraction. For example, if $5\\%$ of code is sequential, maximum speedup is **20x**, no matter how many processors you add.\n\n## 3.2 Worked Numerical Example (KTU-Style Problem)\n\n**Problem:** A program has $f = 0.9$ (90% parallel). It is executed on $N = 8$ threads using OpenMP. The sequential execution time is $T_s = 100$ seconds. Find: (a) Parallel execution time, (b) Speedup, (c) Efficiency.\n\n### Solution\n\n**Step 1: Identify given values.**\n\n$$f = 0.9, \\quad N = 8, \\quad T_s = 100 \\text{ s}$$\n\n**Step 2: Calculate the parallel portion and sequential portion of $T_s$.**\n\n$$T_{seq} = (1 - f) \\cdot T_s = (1 - 0.9) \\cdot 100 = 10 \\text{ s}$$\n\n$$T_{par} = f \\cdot T_s = 0.9 \\cdot 100 = 90 \\text{ s}$$\n\n**Step 3: Calculate parallel execution time $T_p$.**\n\nThe parallel portion is divided among $N=8$ threads (ideally):\n\n$$T_p = T_{seq} + \\dfrac{T_{par}}{N} = 10 + \\dfrac{90}{8} = 10 + 11.25 = 21.25 \\text{ s}$$\n\n**Step 4: Calculate speedup $S$.**\n\n$$S = \\dfrac{T_s}{T_p} = \\dfrac{100}{21.25} \\approx 4.706$$\n\n**Step 5: Calculate efficiency $E$.**\n\n$$E = \\dfrac{S}{N} = \\dfrac{4.706}{8} \\approx 0.588 \\text{ or } 58.8\\%$$\n\n> [!NOTE]\n> **KTU Valuation Tip:** Always show units and box the final answer. Partial credit is awarded for each correct step. If the question says \"ideal OpenMP parallelization\", you may assume perfect division of work (no overhead).\n\n## 3.3 Analytical Matrix: Comparing Scoping Strategies\n\nThe following table analyzes the behavior of each scoping clause for a common operation $sum = sum + A[i]$:\n\n| Clause | Thread-Local `sum` Initial Value | Race Condition Risk | Result Correctness |\n|---|---|---|---|\n| **No clause (default shared)** | N/A (shared variable) | **HIGH** | ❌ Unpredictable |\n| **`private(sum)`** | Undefined / garbage | None | ❌ Final `sum` is lost |\n| **`firstprivate(sum)`** | Initialized from outside | None | ❌ Per-thread partial results not aggregated |\n| **`reduction(+:sum)`** | Each thread gets local copy initialized to identity (0 for +) | None | ✅ Correct final sum aggregated |\n| **`critical`** | Shared `sum` | None (serialized access) | ✅ Correct but slow |\n\n## 3.4 OpenMP Directive Syntax Breakdown\n\nA complete OpenMP parallel-for reduction construct is shown below with each part labeled:\n\n```c\n#pragma omp parallel for reduction(+:sum) private(i) schedule(static)\nfor (i = 0; i < N; i++) {\n    sum = sum + A[i];\n}\n```\n\n**Syntax Breakdown:**\n\n| Token | Meaning |\n|---|---|\n| `#pragma` | Compiler-specific directive prefix (C/C++) |\n| `omp` | Identifies this as an OpenMP directive |\n| `parallel` | Begin a parallel region (fork) |\n| `for` | Distribute loop iterations among threads (worksharing) |\n| `reduction(+:sum)` | Combine per-thread `sum` values with `+` |\n| `private(i)` | Each thread gets its own loop counter `i` |\n| `schedule(static)` | Iterations are distributed in static chunks |\n| `schedule(dynamic, chunk)` | Iterations assigned dynamically in chunks of size `chunk` |\n| `schedule(guided, chunk)` | Iterations assigned in decreasing chunk sizes |\n| `schedule(runtime)` | Schedule decided at runtime via `OMP_SCHEDULE` env variable |\n\n## 3.5 OpenMP Runtime Library Routines (Most Used)\n\n| Function | Purpose |\n|---|---|\n| `omp_get_num_threads()` | Returns total number of threads in the team |\n| `omp_get_thread_num()` | Returns ID of the calling thread (0 to N-1) |\n| `omp_set_num_threads(N)` | Sets the number of threads for subsequent parallel regions |\n| `omp_get_max_threads()` | Returns max threads available (without setting it) |\n| `omp_get_num_procs()` | Returns number of processors in the system |\n| `omp_in_parallel()` | Returns non-zero if currently inside a parallel region |\n| `omp_set_lock(&lock)` / `omp_unset_lock(&lock)` | Acquire / release a lock |\n| `double omp_get_wtime()` | Returns wall-clock time in seconds |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 📘 SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Fork-Join Execution Model (Mermaid)\n\nThe diagram below illustrates the **Fork-Join parallel execution model** of OpenMP, which is the most fundamental concept tested in KTU exams.\n\n```mermaid\ngraph TD\n    A[Main Program Start] --> B[Master Thread Sequential Code]\n    B --> C{Encountered omp parallel directive}\n    C -- Yes --> D[Fork: Spawn Team of N Threads]\n    D --> E1[Thread 0]\n    D --> E2[Thread 1]\n    D --> E3[Thread 2]\n    D --> E4[Thread N-1]\n    E1 --> F[Parallel Region Execution]\n    E2 --> F\n    E3 --> F\n    E4 --> F\n    F --> G[Implicit Barrier - All Threads Wait]\n    G --> H[Join: Threads Terminate]\n    H --> B\n    C -- No --> B\n    B --> I[End of Program]\n    \n    style A fill:#2c3e50,color:#fff\n    style B fill:#3498db,color:#fff\n    style C fill:#e74c3c,color:#fff\n    style D fill:#27ae60,color:#fff\n    style F fill:#f39c12,color:#fff\n    style G fill:#9b59b6,color:#fff\n    style H fill:#27ae60,color:#fff\n    style I fill:#2c3e50,color:#fff\n```\n\n## 4.2 OpenMP Memory and Data Scoping Model\n\nThe next diagram explains how **shared and private variables** are handled across threads, which is a critical KTU exam topic.\n\n```mermaid\ngraph TB\n    subgraph SHM[SHARED MEMORY SPACE]\n        S1[Variable A - SHARED]\n        S2[Variable B - SHARED]\n        S3[Variable N - SHARED]\n    end\n    \n    subgraph T0[THREAD 0]\n        P0a[Private Variable i]\n        P0b[Private Variable sum_local]\n    end\n    \n    subgraph T1[THREAD 1]\n        P1a[Private Variable i]\n        P1b[Private Variable sum_local]\n    end\n    \n    subgraph T2[THREAD 2]\n        P2a[Private Variable i]\n        P2b[Private Variable sum_local]\n    end\n    \n    subgraph T3[THREAD 3]\n        P3a[Private Variable i]\n        P3b[Private Variable sum_local]\n    end\n    \n    S1 -.->|read/write| T0\n    S1 -.->|read/write| T1\n    S1 -.->|read/write| T2\n    S1 -.->|read/write| T3\n    \n    S2 -.->|read/write| T0\n    S2 -.->|read/write| T1\n    S2 -.->|read/write| T2\n    S2 -.->|read/write| T3\n    \n    style SHM fill:#ffe5e5\n    style T0 fill:#e5f5ff\n    style T1 fill:#e5ffe5\n    style T2 fill:#fff5e5\n    style T3 fill:#f5e5ff\n    style S1 fill:#ff6b6b,color:#fff\n    style S2 fill:#ff6b6b,color:#fff\n    style S3 fill:#ff6b6b,color:#fff\n    style P0a fill:#4ecdc4\n    style P0b fill:#4ecdc4\n    style P1a fill:#4ecdc4\n    style P1b fill:#4ecdc4\n    style P2a fill:#4ecdc4\n    style P2b fill:#4ecdc4\n    style P3a fill:#4ecdc4\n    style P3b fill:#4ecdc4\n```\n\n## 4.3 Complete OpenMP Program Flow (Block Architecture)\n\nThe following architecture diagram shows the **end-to-end flow** of an OpenMP program execution.\n\n```mermaid\nflowchart TD\n    A[Program Start - Master Thread] --> B[Initialize Variables A, B, N]\n    B --> C[Sequential Code Section]\n    C --> D{Parallel Region}\n    D -- pragma omp parallel --> E[Fork - Create Threads]\n    E --> F1[Thread 0]\n    E --> F2[Thread 1]\n    E --> F3[Thread 2]\n    E --> F4[Thread N-1]\n    \n    F1 --> G1[pragma omp for - Loop Distribution]\n    F2 --> G1\n    F3 --> G1\n    F4 --> G1\n    \n    G1 --> H{pragma omp barrier}\n    H --> I[reduction combines thread results]\n    I --> J[Join - Threads Terminate]\n    J --> K[Sequential Code Resumes]\n    K --> L[Final Output - Print Sum]\n    L --> M[Program End]\n    \n    style A fill:#1abc9c,color:#fff\n    style D fill:#e74c3c,color:#fff\n    style E fill:#3498db,color:#fff\n    style G1 fill:#f39c12,color:#fff\n    style H fill:#9b59b6,color:#fff\n    style I fill:#27ae60,color:#fff\n    style J fill:#3498db,color:#fff\n    style M fill:#1abc9c,color:#fff\n```\n\n## 4.4 Synchronization Construct Decision Tree\n\nUse this decision tree (rendered as a flowchart) to choose the **right synchronization mechanism** in OpenMP.\n\n```mermaid\ngraph TD\n    A[Need Synchronization?] --> B{What kind of access?}\n    B -- Mutual exclusion of code block --> C[pragma omp critical]\n    B -- Atomic update of single variable --> D[pragma omp atomic]\n    B -- Wait for all threads --> E[pragma omp barrier]\n    B -- Ordered execution of loop iter --> F[pragma omp ordered]\n    B -- Fine-grained lock control --> G[omp_set_lock / omp_unset_lock]\n    B -- Just one thread should do work --> H{Does the thread barrier matter?}\n    H -- Yes --> I[pragma omp single]\n    H -- No, master thread only --> J[pragma omp master]\n    \n    style A fill:#2c3e50,color:#fff\n    style B fill:#e67e22,color:#fff\n    style C fill:#e74c3c,color:#fff\n    style D fill:#e74c3c,color:#fff\n    style E fill:#e74c3c,color:#fff\n    style F fill:#e74c3c,color:#fff\n    style G fill:#e74c3c,color:#fff\n    style H fill:#e67e22,color:#fff\n    style I fill:#27ae60,color:#fff\n    style J fill:#27ae60,color:#fff\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 📘 SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### **Q1. Define OpenMP. List any four of its core components.** `[KTU University Exam - May 2024]`\n\n**Model Answer (3 Marks):**\n\n**Definition (1 Mark):** OpenMP (Open Multi-Processing) is a portable, scalable, shared-memory parallel programming API consisting of compiler directives, library routines, and environment variables used to write multi-threaded applications in C, C++, and Fortran.\n\n**Core Components (2 Marks — any 4):**\n1. **Compiler Directives** — e.g., `#pragma omp parallel`\n2. **Runtime Library Routines** — e.g., `omp_get_thread_num()`\n3. **Environment Variables** — e.g., `OMP_NUM_THREADS`\n4. **Clause System** — e.g., `private`, `shared`, `reduction`\n5. **Compiler Support** — `-fopenmp` flag in GCC\n\n---\n\n### **Q2. Explain the term \"race condition\" in the context of OpenMP. How does the `critical` directive solve it?** `[KTU University Exam - Dec 2023]`\n\n**Model Answer (3 Marks):**\n\n**Race Condition (1.5 Marks):** A race condition occurs when two or more threads access shared data **concurrently**, and at least one of them is writing. The final result depends on the unpredictable order of execution, leading to **non-deterministic** output.\n\n**Role of `critical` (1.5 Marks):** The `#pragma omp critical` directive ensures that the enclosed block of code is executed by **only one thread at a time**. Other threads that reach the critical section **wait** until the executing thread finishes. This enforces **mutual exclusion**, eliminating the race condition for that section of code (at the cost of serialization).\n\n---\n\n## 5.2 Part B Questions (14 Marks Each — Internal Choice)\n\n### **Question A (14 Marks)** `[KTU University Exam - Dec 2024]`\n\n**Q. (a) Explain the Fork-Join execution model of OpenMP with a neat diagram. Discuss how parallel regions are created and terminated. (7 Marks)**\n\n**Model Solution:**\n\n**Definition (2 Marks):** The Fork-Join model is the fundamental parallel execution model of OpenMP. The program begins as a single **master thread** executing sequentially. When a parallel region is encountered, the master thread **forks** — it creates a team of worker threads. After the threads finish the work, they **synchronize at an implicit barrier** and **join** back, with execution continuing in the master thread.\n\n**Fork (Creation) — 2 Marks:**\n- Encountering `#pragma omp parallel` triggers the runtime to create a team of threads.\n- Default team size = number of CPU cores (or as set by `OMP_NUM_THREADS`).\n- Each thread receives a unique thread ID via `omp_get_thread_num()`.\n- The master thread's ID is 0.\n\n**Parallel Region Execution — 1 Mark:**\n- All threads in the team execute the structured block inside the parallel region concurrently.\n- Each thread has its own program counter and stack, but they share the heap and global variables.\n\n**Join (Termination) — 2 Marks:**\n- An **implicit barrier** at the end of the parallel region ensures all threads complete before any can proceed.\n- Worker threads are typically suspended/terminated; only the master thread continues.\n- This cycle repeats if there are multiple parallel regions in the program.\n\n**Diagram (2 Marks):** Use a timeline/thread diagram similar to the Fork-Join figure in Section 4.1.\n\n---\n\n**Q. (b) Discuss the data scoping rules in OpenMP. Compare `shared`, `private`, and `reduction` clauses with suitable examples. (7 Marks)**\n\n**Model Solution:**\n\n**Introduction to Data Scoping (1 Mark):** Data scoping rules in OpenMP define how variables are accessed and modified by multiple threads within a parallel region. Proper scoping is essential for **correctness** (avoiding race conditions) and **performance** (avoiding unnecessary serialization).\n\n**`shared(var)` (2 Marks):**\n- All threads in the team access the **same memory location** for the variable.\n- Writes by one thread are immediately visible to all other threads (subject to memory consistency).\n- Default for variables declared **outside** the parallel region.\n- **Example:** `int A[1000];` declared globally is shared by default.\n- **Risk:** Race conditions on shared variables.\n\n**`private(var)` (2 Marks):**\n- Each thread gets its **own uninitialized copy** of the variable.\n- The original value (outside the region) is not available inside, and any modifications are **discarded** when leaving the region.\n- Used to give each thread its own working storage (e.g., loop counter).\n- **Example:** `private(i)` in a parallel `for` — each thread has its own `i`.\n\n**`reduction(op: var)` (2 Marks):**\n- Each thread creates a private copy, initialized to the **identity element** of the operation (0 for `+`, 1 for `*`).\n- After the parallel region, all copies are **combined** using the specified operator.\n- Eliminates race conditions while aggregating per-thread results.\n- **Example:** `reduction(+:sum)` in a vector sum loop.\n\n**Summary Table (Bonus 1 Mark, optional):**\n\n| Clause | Per-Thread Copy? | Initialized? | Final Value Aggregated? |\n|---|---|---|---|\n| `shared` | No | N/A (shared) | N/A |\n| `private` | Yes | No (garbage) | No (discarded) |\n| `reduction` | Yes | Yes (identity) | Yes (combined) |\n\n---\n\n### **Question B (14 Marks)** `[KTU University Exam - May 2024 — Alternative Set]`\n\n**Q. (a) With suitable code snippets, explain OpenMP worksharing constructs for loops. Differentiate between `static`, `dynamic`, and `guided` schedule clauses. (7 Marks)**\n\n**Model Solution:**\n\n**Worksharing for Loops — Introduction (1 Mark):**\nThe `#pragma omp for` (or combined `parallel for`) construct divides the iterations of a `for` loop among the threads in a team. This is the **most commonly used** OpenMP construct for parallelizing data-parallel code.\n\n**Basic Syntax with Example (2 Marks):**\n\n```c\n#include <omp.h>\nint i, N = 1000;\ndouble A[1000], sum = 0.0;\n\n#pragma omp parallel for reduction(+:sum) private(i)\nfor (i = 0; i < N; i++) {\n    sum = sum + A[i];\n}\n```\n\nThis parallelizes the loop, with each thread computing a partial sum, and `reduction` aggregates them.\n\n**Schedule Clauses (3 Marks):**\n\n1. **`schedule(static [, chunk])`:**\n   - Iterations are divided into **chunks** of size `chunk` (default: N/T).\n   - Chunks are assigned to threads **before loop execution** begins.\n   - **Lowest overhead**, best when work per iteration is uniform.\n   - Example: `schedule(static, 10)` — each thread gets 10 iterations at a time.\n\n2. **`schedule(dynamic [, chunk])`:**\n   - Each thread grabs the next chunk dynamically as it finishes.\n   - **Best for load imbalance**, but has more synchronization overhead.\n   - Example: `schedule(dynamic, 5)` — each thread takes 5 iterations at a time, dynamically.\n\n3. **`schedule(guided [, chunk])`:**\n   - Chunks start large and **decrease** in size as threads finish.\n   - Compromise between static and dynamic; good for moderately imbalanced workloads.\n   - Minimum chunk size = `chunk` (default = 1).\n\n4. **`schedule(runtime)`:**\n   - Schedule is determined at runtime via the `OMP_SCHEDULE` environment variable.\n\n**Comparison Table (1 Mark):**\n\n| Schedule | Overhead | Load Balancing | Use When |\n|---|---|---|---|\n| `static` | **Low** | Poor for irregular work | Workload is uniform |\n| `dynamic` | **High** | Excellent | Workload is irregular |\n| `guided` | **Medium** | Good | Moderate imbalance |\n\n---\n\n**Q. (b) Explain the need for synchronization in OpenMP. Discuss the `barrier`, `critical`, `atomic`, and `master` directives with appropriate scenarios where each is used. (7 Marks)**\n\n**Model Solution:**\n\n**Need for Synchronization (1 Mark):**\nIn shared-memory parallel programming, multiple threads access shared variables. Without synchronization, **race conditions** occur (non-deterministic results, corrupted data). Synchronization enforces **ordering** and **mutual exclusion**, ensuring correctness.\n\n**`#pragma omp barrier` (1.5 Marks):**\n- All threads in the team **wait** at this point until every thread has arrived.\n- Used when subsequent work depends on all threads having completed prior work.\n- **Scenario:** After a parallel loop where the next phase (e.g., printing the result) needs all partial sums to be ready.\n\n**`#pragma omp critical` (1.5 Marks):**\n- Defines a **critical section** — only one thread executes the block at a time.\n- Used for updating shared variables in a complex way (e.g., linked list manipulation).\n- **Scenario:** Updating a shared counter or appending to a shared data structure.\n\n**`#pragma omp atomic` (1.5 Marks):**\n- Ensures a **specific memory location** is updated atomically (often via hardware instructions).\n- **Faster** than `critical` but applies only to single-statement updates.\n- **Syntax:** `sum = sum + A[i];` becomes `#pragma omp atomic sum = sum + A[i];`\n- **Scenario:** Incrementing a shared counter: `#pragma omp atomic counter++;`\n\n**`#pragma omp master` (1.5 Marks):**\n- The enclosed code runs **only on the master thread** (thread 0).\n- **No implicit barrier** at the end (unlike `single`).\n- **Scenario:** I/O operations like `printf` that should be done by only one thread, without synchronizing all others.\n\n**Distinguishing `master` vs `single` (Bonus 0.5 Mark):**\n- `master` → thread 0 only, no barrier.\n- `single` → any one thread, with implicit barrier.\n\n---\n\n## 5.3 ⚠️ KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mistakes That Cost Marks in KTU Exams:**\n> \n> 1. **Confusing `private` and `firstprivate`:** Students often write `firstprivate` but explain it as `private`. Remember: `firstprivate` **initializes** from the original value; `private` does **not**.\n> \n> 2. **Forgetting that OpenMP barriers are IMPLICIT:** Many students miss that `#pragma omp parallel` and `#pragma omp for` both end with implicit barriers. Adding a redundant `barrier` is a common error.\n> \n> 3. **Misapplying Amdahl's Law:** Plugging in wrong values for $f$ and $N$. Always clearly state what $f$ represents (the **parallel fraction**, NOT the sequential fraction).\n> \n> 4. **Writing code without `#include <omp.h>`:** OpenMP runtime functions like `omp_get_thread_num()` will not compile without this header. KTU deducts 1 mark for compilation errors.\n> \n> 5. **Confusing `#pragma omp parallel for` with `#pragma omp parallel` + `#pragma omp for`:** The combined form is more efficient (one team creation), but the separate forms allow additional work between fork and join.\n> \n> 6. **Race condition in `sum = sum + A[i]` without `reduction` or `atomic`:** This is the **#1 cause of incorrect OpenMP code**. The correct way is `reduction(+:sum)`.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember 🎯\n\n✅ **OpenMP** = portable API for shared-memory parallel programming; uses **fork-join** model.\n\n✅ **Three pillars of OpenMP:** Parallel Execution + Data Scoping + Synchronization.\n\n✅ **Fork-Join Model:** Single master thread → fork (create team) → execute parallel region → join (barrier synchronization) → continue sequentially.\n\n✅ **Core Directive:** `#pragma omp parallel` creates a team of threads.\n\n✅ **Worksharing:** `#pragma omp for` distributes loop iterations; combined `#pragma omp parallel for` is most common.\n\n✅ **Data Scoping — Default Rule:** Variables outside parallel region = `shared`; inside = `private`.\n\n✅ **Key Scoping Clauses:**\n- `shared(var)` — one copy, all threads see it (race condition risk)\n- `private(var)` — one uninitialized copy per thread, lost after region\n- `firstprivate(var)` — initialized from outside value\n- `lastprivate(var)` — last value preserved to outside\n- `reduction(op: var)` — per-thread local copies, combined at end\n\n✅ **Synchronization Constructs:**\n- `barrier` — wait for all threads\n- `critical` — mutual exclusion (one thread at a time)\n- `atomic` — single-variable atomic update (faster than `critical`)\n- `master` — master thread only, no barrier\n- `ordered` — preserve loop iteration order\n\n✅ **Schedule Clauses:** `static` (low overhead), `dynamic` (load balanced), `guided` (compromise), `runtime` (env-var controlled).\n\n✅ **Amdahl's Law:** $S(N) = \\dfrac{1}{(1-f) + \\dfrac{f}{N}}$, limit is $\\dfrac{1}{1-f}$ as $N \\to \\infty$.\n\n✅ **Efficiency:** $E = \\dfrac{S}{P}$, ideally 1.0; degraded by synchronization and sequential bottlenecks.\n\n✅ **OpenMP is NOT for distributed memory** (no MPI support); use hybrid OpenMP+MPI for clusters.\n\n✅ **Compilation:** GCC requires `-fopenmp` flag. Without it, the pragmas are ignored (treated as comments).\n\n✅ **Thread Functions:** `omp_get_thread_num()`, `omp_get_num_threads()`, `omp_set_num_threads()`, `omp_get_wtime()`.\n\n✅ **Race Condition** = #1 OpenMP bug. Always use `reduction`, `critical`, or `atomic` for shared variable updates.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "reductions": {
      "title": "Reductions",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module Module 3\n> - **Topic:** Reductions\n\n<!-- SECTION_1_START -->\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition of Reduction\n\nIn High Performance Computing (HPC), a **Reduction** is a fundamental parallel collective communication and computation pattern in which a binary, **associative** (and often **commutative**) operator $\\oplus$ is applied across a collection of $n$ data elements distributed across $p$ processors to produce a single aggregated result.\n\nMathematically, given an input set $\\{a_0, a_1, a_2, \\ldots, a_{n-1}\\}$ and a binary operator $\\oplus$, the reduction computes:\n\n$$a_0 \\oplus a_1 \\oplus a_2 \\oplus \\ldots \\oplus a_{n-1}$$\n\nCommon reduction operators include:\n- **Summation:** $\\sum_{i=0}^{n-1} a_i$\n- **Maximum/Minimum:** $\\max\\{a_0, a_1, \\ldots, a_{n-1}\\}$\n- **Logical operators:** AND ($\\wedge$), OR ($\\vee$), XOR ($\\oplus$)\n- **Floating-point operations:** Product, average, dot product\n\n> [!NOTE]\n> **KTU Syllabus Highlight:** Reduction operations form the **building blocks** of nearly every parallel algorithm in numerical computing, graph analytics, and machine learning (e.g., loss aggregation in distributed training).\n\n### 1.2 The Prefix Sum (Scan) — A Generalization\n\nA **Prefix Sum** or **Scan** is the generalized form of reduction in which, instead of producing a single value, we compute the running combination of all elements. For an array $A = [a_0, a_1, \\ldots, a_{n-1}]$, the inclusive scan produces:\n\n$$S_i = a_0 \\oplus a_1 \\oplus \\ldots \\oplus a_i \\quad \\text{for } i = 0, 1, \\ldots, n-1$$\n\nAnd the **exclusive scan** produces:\n\n$$S_i = a_0 \\oplus a_1 \\oplus \\ldots \\oplus a_{i-1} \\quad \\text{with } S_0 = I \\text{ (identity element)}$$\n\n### 1.3 Intuitive Analogy — The Class Total Exam Score\n\n> [!IMPORTANT]\n> **Real-World Analogy:** Imagine you are a class teacher with **$n = 1024$ answer sheets** scattered across 32 parallel-correcting teaching assistants (TAs).\n>\n> - **Reduction (Total Score):** Each TA first adds up scores within their own bundle (~32 sheets). Then 16 TAs pair up and add their subtotals. Then 8, then 4, 2, and finally 1 TA computes the final grand total. This tree-like pairing takes only **$\\log_2(1024) = 10$ rounds**, instead of one TA doing all 1024 additions sequentially.\n> - **Prefix Sum (Running Total):** Instead of just the final total, every TA also wants the cumulative score up to their bundle. This requires an extra phase — information must flow **backwards down the tree** so that each leaf node knows the cumulative sum of *all elements before it*.\n\nThis \"tree-shaped\" computation is the soul of parallel reduction.\n\n### 1.4 Why Reduction is Indispensable in HPC\n\nReductions sit at the heart of MPI (Message Passing Interface) as `MPI_Reduce`, `MPI_Allreduce`, `MPI_Scan`, and `MPI_Exscan`. They are equally critical in GPU computing (CUDA `__reduce`, `cub::DeviceReduce`) and shared-memory multi-core systems (OpenMP `reduction` clause).\n\n> [!NOTE]\n> **Engineering Insight:** The performance of any parallel application — from weather forecasting to large language model training — is ultimately bounded by how efficiently it performs reductions on distributed data.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 The Associativity Contract\n\nFor a reduction to be **mathematically correct** when performed in parallel, the operator $\\oplus$ **must be strictly associative**, meaning:\n\n$$(a \\oplus b) \\oplus c = a \\oplus (b \\oplus c)$$\n\nThis property is what permits arbitrary **reordering** of operations, which is precisely what enables parallelism. For non-associative operations (e.g., floating-point addition due to rounding), the result is *non-deterministic* and may differ slightly between runs — a critical concern in numerical reproducibility.\n\n### 2.2 Algorithmic Variants of Parallel Reduction\n\nThere are three principal families of parallel reduction:\n\n1. **Tree Reduction (Naive Pairwise):** Recursive halving; ideal for shared memory.\n2. **Hillis–Steele Scan:** Naive parallel scan with $\\mathcal{O}(n \\log n)$ work.\n3. **Blelloch Scan (Work-Efficient):** Two-phase up-sweep + down-sweep with $\\mathcal{O}(n)$ work.\n\n### 2.3 KTU Formula Sheet / Cheat Sheet\n\n> [!IMPORTANT]\n> The following table is a **must-memorize** reference for KTU 2024 ESE and internal assessments.\n\n| Operation | Sequential Time | Parallel Time (p procs) | Total Work | Cost | Speedup Bound |\n|---|---|---|---|---|---|\n| Sequential Reduction | $\\mathcal{O}(n)$ | — | $\\mathcal{O}(n)$ | $\\mathcal{O}(n)$ | 1 |\n| Tree Reduction | $\\mathcal{O}(n)$ | $\\mathcal{O}\\!\\left(\\frac{n}{p} + \\log p\\right)$ | $\\mathcal{O}(n)$ | $\\mathcal{O}(n)$ | Up to $\\frac{n}{\\log n}$ |\n| Hillis–Steele Scan | $\\mathcal{O}(n)$ | $\\mathcal{O}(\\log n)$ | $\\mathcal{O}(n \\log n)$ | $\\mathcal{O}(n \\log n)$ | Not work-efficient |\n| Blelloch Scan | $\\mathcal{O}(n)$ | $\\mathcal{O}(\\log n)$ | $\\mathcal{O}(n)$ | $\\mathcal{O}(n)$ | Optimal (work-efficient) |\n| MPI_Allreduce | $\\mathcal{O}(n)$ | $\\mathcal{O}\\!\\left(\\frac{n}{p}\\right)$ + comm. | $\\mathcal{O}(n)$ | — | Scales with bandwidth |\n\n### 2.4 Critical Equations\n\n**Brent's Theorem (Scheduling):** If a parallel algorithm performs $W$ total work and has span (critical-path length) $T_\\infty$, then the running time on $p$ processors is bounded by:\n\n$$T_p \\leq \\frac{W}{p} + T_\\infty$$\n\n**Iso-efficiency (for work-efficient algorithms):**\n\n$$W = \\mathcal{O}(p \\cdot T_p) \\implies \\text{Efficiency} = \\mathcal{O}(1)$$\n\n**Parallel Efficiency:**\n\n$$E_p = \\frac{T_{\\text{seq}}}{p \\cdot T_p} = \\frac{S_p}{p}$$\n\nwhere $S_p = \\frac{T_{\\text{seq}}}{T_p}$ is the **speedup**.\n\n### 2.5 Communication-Aware Complexity\n\nFor distributed-memory systems (MPI), the communication cost of an `MPI_Reduce` with $n$ elements across $p$ processes using a binomial/butterfly tree is:\n\n$$T_{\\text{comm}} = \\underbrace{\\alpha \\cdot \\log p}_{\\text{latency}} + \\underbrace{\\beta \\cdot n \\cdot \\frac{\\log p}{p}}_{\\text{bandwidth}}$$\n\nwhere $\\alpha$ is the per-message latency overhead and $\\beta$ is the inverse bandwidth (time per byte).\n\n### 2.6 Real-World Engineering Utility\n\n- **MPI collectives:** `MPI_Reduce`, `MPI_Allreduce` for global sums and dot products.\n- **GPU Computing:** Block-wide reductions in CUDA using warp shuffles.\n- **Deep Learning:** AllReduce of gradients in distributed SGD (e.g., Horovod, NCCL).\n- **Sparse Linear Algebra:** Inner products in Krylov-subspace methods (CG, GMRES).\n- **Big Data:** MapReduce's reducer phase is literally a hierarchical reduction.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Derivation: Time Complexity of Tree Reduction\n\n**Setup:** Let $T(n)$ denote the time to perform parallel reduction on $n$ elements using $p$ processors.\n\n**Step 1 — Decompose:** Each processor handles a chunk of $\\frac{n}{p}$ elements sequentially, requiring $\\frac{n}{p}$ time.\n\n$$T(n) = \\frac{n}{p} + T_{\\text{combine}}$$\n\n**Step 2 — Pairwise Combination Phase:** After local reduction, we have $p$ partial sums. These are combined in pairs across $\\log_2 p$ rounds of communication.\n\n$$T_{\\text{combine}} = \\log_2 p \\cdot (t_s + t_w)$$\n\nwhere $t_s$ is the setup time per message and $t_w$ is the per-word transfer time.\n\n**Step 3 — Total:**\n\n$$T(n) = \\frac{n}{p} + \\log_2 p \\cdot (t_s + t_w)$$\n\n**Step 4 — Simplification:** For purely compute-bound regimes ($t_w \\to 0$):\n\n$$T(n) \\approx \\frac{n}{p} + \\log_2 p$$\n\nThe dominant term is $\\frac{n}{p}$ for $p \\ll n$, and $\\log_2 p$ dominates for $p \\approx n$ (i.e., one element per processor).\n\n### 3.2 Derivation: Speedup of Tree Reduction\n\n**Step 1 — Sequential Time:** The sequential algorithm must visit every element exactly once:\n\n$$T_{\\text{seq}} = n - 1 \\approx n \\text{ operations}$$\n\n**Step 2 — Parallel Time:** From above, $T_p = \\frac{n}{p} + \\log_2 p$.\n\n**Step 3 — Speedup Ratio:**\n\n$$S_p = \\frac{T_{\\text{seq}}}{T_p} = \\frac{n}{\\frac{n}{p} + \\log_2 p}$$\n\n**Step 4 — Multiply numerator and denominator by $p$:**\n\n$$S_p = \\frac{p \\cdot n}{n + p \\cdot \\log_2 p}$$\n\n**Step 5 — Asymptotic Analysis:**\n\nFor $p \\ll \\frac{n}{\\log n}$:\n\n$$S_p \\approx \\frac{p \\cdot n}{n} = p \\quad \\text{(linear speedup)}$$\n\nFor $p = n$ (one element per processor):\n\n$$S_p = \\frac{n^2}{n + n \\log n} = \\frac{n}{1 + \\log n} = \\mathcal{O}\\!\\left(\\frac{n}{\\log n}\\right)$$\n\n### 3.3 Worked Example: Hillis–Steele Inclusive Scan\n\n**Input array:** $A = [3, 1, 7, 0, 4, 1, 6, 3]$ (8 elements, $n = 8$)\n\nThe Hillis–Steele algorithm uses $d = \\lceil \\log_2 n \\rceil = 3$ steps. At step $k$, each element $A[i]$ is updated (in parallel) as:\n\n$$A[i] \\leftarrow A[i] + A[i - 2^{k-1}] \\quad \\text{for } i \\geq 2^{k-1}$$\n\n**Step 0 (Initial):**\n\n$$\\begin{aligned}\nA = [\\, 3,\\ \\ 1,\\ \\ 7,\\ \\ 0,\\ \\ 4,\\ \\ 1,\\ \\ 6,\\ \\ 3\\,]\n\\end{aligned}$$\n\n**Step 1 ($2^0 = 1$, shift by 1):**\n\n$$\\begin{aligned}\nA[0] &\\leftarrow A[0] = 3 \\\\\nA[1] &\\leftarrow A[1] + A[0] = 1 + 3 = 4 \\\\\nA[2] &\\leftarrow A[2] + A[1] = 7 + 1 = 8 \\\\\nA[3] &\\leftarrow A[3] + A[2] = 0 + 7 = 7 \\\\\nA[4] &\\leftarrow A[4] + A[3] = 4 + 0 = 4 \\\\\nA[5] &\\leftarrow A[5] + A[4] = 1 + 4 = 5 \\\\\nA[6] &\\leftarrow A[6] + A[5] = 6 + 1 = 7 \\\\\nA[7] &\\leftarrow A[7] + A[6] = 3 + 6 = 9 \\\\\n\\end{aligned}$$\n\nAfter Step 1: $A = [\\, 3,\\ \\ 4,\\ \\ 8,\\ \\ 7,\\ \\ 4,\\ \\ 5,\\ \\ 7,\\ \\ 9\\,]$\n\n**Step 2 ($2^1 = 2$, shift by 2):**\n\n$$\\begin{aligned}\nA[0] &\\leftarrow A[0] = 3 \\\\\nA[1] &\\leftarrow A[1] = 4 \\\\\nA[2] &\\leftarrow A[2] + A[0] = 8 + 3 = 11 \\\\\nA[3] &\\leftarrow A[3] + A[1] = 7 + 4 = 11 \\\\\nA[4] &\\leftarrow A[4] + A[2] = 4 + 8 = 12 \\\\\nA[5] &\\leftarrow A[5] + A[3] = 5 + 7 = 12 \\\\\nA[6] &\\leftarrow A[6] + A[4] = 7 + 4 = 11 \\\\\nA[7] &\\leftarrow A[7] + A[5] = 9 + 5 = 14 \\\\\n\\end{aligned}$$\n\nAfter Step 2: $A = [\\, 3,\\ \\ 4,\\ 11,\\ 11,\\ 12,\\ 12,\\ 11,\\ 14\\,]$\n\n**Step 3 ($2^2 = 4$, shift by 4):**\n\n$$\\begin{aligned}\nA[0] &\\leftarrow A[0] = 3 \\\\\nA[1] &\\leftarrow A[1] = 4 \\\\\nA[2] &\\leftarrow A[2] = 11 \\\\\nA[3] &\\leftarrow A[3] = 11 \\\\\nA[4] &\\leftarrow A[4] + A[0] = 12 + 3 = 15 \\\\\nA[5] &\\leftarrow A[5] + A[1] = 12 + 4 = 16 \\\\\nA[6] &\\leftarrow A[6] + A[2] = 11 + 11 = 22 \\\\\nA[7] &\\leftarrow A[7] + A[3] = 14 + 11 = 25 \\\\\n\\end{aligned}$$\n\nFinal inclusive scan: $A = [\\, 3,\\ \\ 4,\\ 11,\\ 11,\\ 15,\\ 16,\\ 22,\\ 25\\,]$\n\n> [!NOTE]\n> **Verification:** Last element $= 3+1+7+0+4+1+6+3 = 25$. ✓\n\n### 3.4 Work Analysis Comparison\n\n| Algorithm | Steps | Work per Step | Total Work | Optimal? |\n|---|---|---|---|---|\n| Hillis–Steele | $\\log n$ | $\\mathcal{O}(n)$ | $\\mathcal{O}(n \\log n)$ | No |\n| Blelloch Up-sweep | $\\log n$ | $n/2, n/4, \\ldots, 1$ (geometric) | $\\mathcal{O}(n)$ | Yes |\n| Blelloch Down-sweep | $\\log n$ | Same as up-sweep | $\\mathcal{O}(n)$ | Yes |\n| Blelloch Total | $2\\log n$ | — | $\\mathcal{O}(n)$ | **Work-Optimal** |\n\n**Geometric sum for Blelloch work:**\n\n$$\\sum_{k=0}^{\\log_2 n - 1} \\frac{n}{2^{k+1}} = \\frac{n}{2} \\sum_{k=0}^{\\log_2 n - 1} \\frac{1}{2^k} = \\frac{n}{2} \\cdot (2 - 2^{1-\\log_2 n}) < n$$\n\nThis proves the total work is strictly less than $n$ additions per phase, hence **$\\mathcal{O}(n)$** total work — matching the sequential lower bound, making Blelloch **optimal**.\n\n### 3.5 Analytical Matrix: Why $\\log n$ is the Fundamental Limit\n\nThe critical path of any parallel reduction forms a binary tree. The depth of a complete binary tree with $n$ leaves is:\n\n$$d = \\lceil \\log_2 n \\rceil$$\n\nNo matter how clever the algorithm, **every element must contribute** to the final answer, and at least one chain of operations of length $\\log_2 n$ must execute serially (the longest root-to-leaf path). This is the **span** $T_\\infty = \\mathcal{O}(\\log n)$, an unconditional lower bound.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Parallel Tree Reduction (8 elements)\n\nThe diagram below illustrates how 8 input elements are aggregated into a single sum using pairwise parallel reduction. Each horizontal level executes in parallel; communication occurs only between paired nodes.\n\n```mermaid\ngraph TD\n    LEAF0[\"a0\"]\n    LEAF1[\"a1\"]\n    LEAF2[\"a2\"]\n    LEAF3[\"a3\"]\n    LEAF4[\"a4\"]\n    LEAF5[\"a5\"]\n    LEAF6[\"a6\"]\n    LEAF7[\"a7\"]\n    LV1A[\"a0 PLUS a1\"]\n    LV1B[\"a2 PLUS a3\"]\n    LV1C[\"a4 PLUS a5\"]\n    LV1D[\"a6 PLUS a7\"]\n    LV2A[\"a0 PLUS a1 PLUS a2 PLUS a3\"]\n    LV2B[\"a4 PLUS a5 PLUS a6 PLUS a7\"]\n    ROOT[\"FINAL TOTAL SUM\"]\n    LEAF0 --> LV1A\n    LEAF1 --> LV1A\n    LEAF2 --> LV1B\n    LEAF3 --> LV1B\n    LEAF4 --> LV1C\n    LEAF5 --> LV1C\n    LEAF6 --> LV1D\n    LEAF7 --> LV1D\n    LV1A --> LV2A\n    LV1B --> LV2A\n    LV1C --> LV2B\n    LV1D --> LV2B\n    LV2A --> ROOT\n    LV2B --> ROOT\n```\n\n> [!NOTE]\n> **Reading the diagram:** Levels 0 (leaves) $\\rightarrow$ 1 (pairs) $\\rightarrow$ 2 (quads) $\\rightarrow$ 3 (root). Total levels $= \\lceil \\log_2 8 \\rceil + 1 = 4$. Depth of work $= 3$ parallel rounds.\n\n### 4.2 Blelloch Work-Efficient Scan: Up-Sweep + Down-Sweep\n\nThe Blelloch algorithm performs scan in **two sweeps** over a balanced binary tree: an up-sweep (reduce phase) followed by a down-sweep (distribute phase). The diagram below shows the data flow for an 8-element array.\n\n```mermaid\ngraph TD\n    N0[\"L3: 25\"]\n    N1[\"L2: 11\"]\n    N2[\"L2: 14\"]\n    N3[\"L3 Left: 15\"]\n    N4[\"L3 Right: 10\"]\n    N5[\"L1: 4\"]\n    N6[\"L1: 7\"]\n    N7[\"L1: 5\"]\n    N8[\"L1: 9\"]\n    N9[\"L0: 3\"]\n    NA[\"L0: 1\"]\n    NB[\"L0: 7\"]\n    NC[\"L0: 0\"]\n    ND[\"L0: 4\"]\n    NE[\"L0: 1\"]\n    NF[\"L0: 6\"]\n    NG[\"L0: 3\"]\n    N0 --> N1\n    N0 --> N2\n    N1 --> N3\n    N1 --> N4\n    N2 --> N5\n    N2 --> N6\n    N3 --> N7\n    N3 --> N8\n    N4 --> N9\n    N4 --> NA\n    N5 --> NB\n    N5 --> NC\n    N6 --> ND\n    N6 --> NE\n    N7 --> NF\n    N7 --> NG\n```\n\n> [!IMPORTANT]\n> **Conceptual Note:** In a real Blelloch scan, the **up-sweep** builds the tree bottom-up computing partial sums. The **down-sweep** traverses top-down distributing prefix sums to leaves. This two-phase approach achieves $\\mathcal{O}(n)$ work — matching sequential — while maintaining $\\mathcal{O}(\\log n)$ depth.\n\n### 4.3 Communication Pattern: Butterfly (MPI_Allreduce)\n\nFor distributed-memory systems, `MPI_Allreduce` typically uses a **butterfly communication pattern** which is bandwidth-optimal and completes in $\\log_2 p$ rounds.\n\n```mermaid\ngraph LR\n    P0[\"Proc 0\"]\n    P1[\"Proc 1\"]\n    P2[\"Proc 2\"]\n    P3[\"Proc 3\"]\n    P4[\"Proc 4\"]\n    P5[\"Proc 5\"]\n    P6[\"Proc 6\"]\n    P7[\"Proc 7\"]\n    P0 ---|round 1| P4\n    P1 ---|round 1| P5\n    P2 ---|round 1| P6\n    P3 ---|round 1| P7\n    P0 ---|round 2| P2\n    P1 ---|round 2| P3\n    P4 ---|round 2| P6\n    P5 ---|round 2| P7\n    P0 ---|round 3| P1\n    P2 ---|round 3| P3\n    P4 ---|round 3| P5\n    P6 ---|round 3| P7\n```\n\n> [!NOTE]\n> **Bandwidth Analysis:** Each processor sends $\\frac{n}{p}$ elements per round. Total messages per processor $= \\log_2 p$. Total data transferred per processor $= \\frac{n}{p} \\cdot \\log_2 p$. This is the gold standard for bandwidth-optimal reduction.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### Part A — Short Answer Questions (3 Marks Each)\n\n---\n\n**Q1. [KTU University Exam — May 2024]**\n*Define the term \"reduction\" in the context of parallel computing. Give two examples of associative operators used in reduction operations. (3 marks)*\n\n**Model Answer:**\n\nA **reduction** is a parallel collective operation in which a binary, associative (and ideally commutative) operator $\\oplus$ is applied across a collection of $n$ data elements distributed across multiple processors to produce a single aggregated result of the form $a_0 \\oplus a_1 \\oplus a_2 \\oplus \\ldots \\oplus a_{n-1}$.\n\nTwo examples of associative operators used in reductions:\n1. **Summation** ($\\sum$): Used to compute total sums, dot products, and accumulated error metrics.\n2. **Maximum** ($\\max$): Used to find the largest value, e.g., the worst-case error or peak load.\n\n*(Other valid: logical AND, logical OR, multiplication, min, XOR.)*\n\n---\n\n**Q2. [KTU University Exam — Dec 2023]**\n*Differentiate between inclusive scan and exclusive scan with suitable examples. (3 marks)*\n\n**Model Answer:**\n\nFor an input array $A = [a_0, a_1, a_2, a_3]$, with operator $+$, the scans are:\n\n| Index $i$ | Inclusive Scan | Exclusive Scan |\n|---|---|---|\n| 0 | $a_0$ | $0$ (identity) |\n| 1 | $a_0 + a_1$ | $a_0$ |\n| 2 | $a_0 + a_1 + a_2$ | $a_0 + a_1$ |\n| 3 | $a_0 + a_1 + a_2 + a_3$ | $a_0 + a_1 + a_2$ |\n\n**Inclusive scan** includes the current element in the cumulative sum, while **exclusive scan** excludes it (using the identity element $0$ at position 0).\n\n---\n\n### Part B — Full-Answer Questions (14 Marks Each)\n\n> **KTU ESE Note:** As per KTU 2024 Scheme, Part B questions offer an *internal choice*. You must attempt **either** Question A **or** Question B.\n\n---\n\n### ❓ Question A (14 Marks) **[KTU Model Question — PECST757]**\n\n**(a)** Explain the **parallel tree reduction algorithm** with a neat diagram for $n = 8$ elements. Derive its time complexity in terms of $n$ and $p$. **(7 marks)**\n\n**(b)** Discuss the **communication cost of MPI_Allreduce** using the butterfly pattern. Show that it is bandwidth-optimal. **(7 marks)**\n\n---\n\n#### ✅ Solution A(a): Parallel Tree Reduction\n\n**Algorithm Steps (for $n = 8$ elements across $p$ processors):**\n\n1. Each processor performs a **local reduction** on its $\\frac{n}{p}$ elements.\n2. The $p$ partial results are combined **pairwise** in a binary tree fashion.\n3. After $\\lceil \\log_2 p \\rceil$ rounds, a single processor (typically rank 0) holds the final result.\n\n**Time Complexity Derivation:**\n\n$$\\begin{aligned}\nT_p &= \\underbrace{\\frac{n}{p}}_{\\text{local computation}} + \\underbrace{\\lceil \\log_2 p \\rceil}_{\\text{communication rounds}} \\cdot (t_s + t_w) \\\\\n&= \\mathcal{O}\\!\\left(\\frac{n}{p} + \\log p\\right)\n\\end{aligned}$$\n\nFor a compute-bound scenario, the $\\log p$ term is negligible compared to $\\frac{n}{p}$, yielding near-linear speedup:\n\n$$S_p = \\frac{n}{\\frac{n}{p} + \\log p} \\approx p \\quad \\text{(when } p \\ll \\frac{n}{\\log n}\\text{)}$$\n\n**Diagram Reference:** See Section 4.1 of these notes.\n\n---\n\n#### ✅ Solution A(b): Communication Cost of MPI_Allreduce (Butterfly Pattern)\n\nIn the **butterfly pattern**, processors are paired using bitwise XOR of their ranks. Processors whose $k$-th bit differs exchange data in round $k$.\n\n**Round-by-Round Analysis for $p$ processors:**\n\n- **Number of rounds:** $\\log_2 p$\n- **Data per message per processor per round:** $\\frac{n}{p}$ (since each processor partitions its data into $p$ chunks)\n- **Total data sent by each processor:** $\\frac{n}{p} \\cdot \\log_2 p$\n\n**Communication Cost Equation:**\n\n$$\\begin{aligned}\nT_{\\text{comm}} &= \\alpha \\cdot \\log_2 p + \\beta \\cdot \\frac{n}{p} \\cdot \\log_2 p \\\\\n&= \\log_2 p \\cdot \\left(\\alpha + \\frac{\\beta \\cdot n}{p}\\right)\n\\end{aligned}$$\n\n**Proof of Bandwidth Optimality:** A lower bound on communication for any reduction is $\\Omega\\!\\left(\\frac{n \\cdot \\log p}{p}\\right)$ (since each processor must receive at least $\\frac{n}{p}$ data and the diameter of the network is $\\log p$). Our algorithm exactly meets this bound, hence it is **bandwidth-optimal**.\n\n---\n\n### ❓ Question B (14 Marks) **[KTU Model Question — PECST757]**\n\n**(a)** Explain the **Hillis–Steele scan algorithm** with a worked example for $n = 8$ elements. **(7 marks)**\n\n**(b)** Explain the **Blelloch work-efficient scan algorithm** and prove that its total work is $\\mathcal{O}(n)$. Compare it with the Hillis–Steele algorithm. **(7 marks)**\n\n---\n\n#### ✅ Solution B(a): Hillis–Steele Inclusive Scan\n\n**Algorithm:** At step $k$ (for $k = 1, 2, \\ldots, \\lceil \\log_2 n \\rceil$), every processor $i$ updates in parallel:\n\n$$\\begin{aligned}\n\\text{if } i \\geq 2^{k-1}: \\quad x_i^{(k)} &= x_i^{(k-1)} + x_{i-2^{k-1}}^{(k-1)} \\\\\n\\text{else}: \\quad x_i^{(k)} &= x_i^{(k-1)}\n\\end{aligned}$$\n\n**Worked Example (n = 8):** Input = $[3, 1, 7, 0, 4, 1, 6, 3]$\n\n| Step | Shift | Resulting Array |\n|---|---|---|\n| 0 | — | $[3, 1, 7, 0, 4, 1, 6, 3]$ |\n| 1 | 1 | $[3, 4, 8, 7, 4, 5, 7, 9]$ |\n| 2 | 2 | $[3, 4, 11, 11, 12, 12, 11, 14]$ |\n| 3 | 4 | $[3, 4, 11, 11, 15, 16, 22, 25]$ |\n\n**Complexity:** Time $= \\mathcal{O}(\\log n)$, Work $= \\mathcal{O}(n \\log n)$.\n\n---\n\n#### ✅ Solution B(b): Blelloch Work-Efficient Scan\n\n**Phase 1 — Up-Sweep (Reduce):** Builds a binary tree from leaves to root, computing partial sums at each internal node. At level $d$, only $\\frac{n}{2^d}$ nodes participate.\n\n**Phase 2 — Down-Sweep:** Distributes prefix sums from root to leaves. The root is initialized to 0; as the sweep descends, each node passes its accumulated prefix to its left child and passes (own prefix + left child's sum) to its right child.\n\n**Work Proof:**\n\n$$\\begin{aligned}\nW_{\\text{up-sweep}} &= \\sum_{d=0}^{\\log_2 n - 1} \\frac{n}{2^{d+1}} = \\frac{n}{2} \\sum_{d=0}^{\\log_2 n - 1} \\frac{1}{2^d} \\\\\n&= \\frac{n}{2} \\cdot (2 - 2^{1 - \\log_2 n}) = n - 1 < n\n\\end{aligned}$$\n\nSimilarly $W_{\\text{down-sweep}} < n$. Hence total work $W < 2n = \\mathcal{O}(n)$.\n\n**Comparison Table:**\n\n| Feature | Hillis–Steele | Blelloch |\n|---|---|---|\n| Parallel Time | $\\mathcal{O}(\\log n)$ | $\\mathcal{O}(\\log n)$ |\n| Total Work | $\\mathcal{O}(n \\log n)$ | $\\mathcal{O}(n)$ |\n| Work-Efficient? | No | **Yes** |\n| In-Place? | No (needs $2n$ space) | Yes |\n| Implementation | Simpler | More complex |\n\n---\n\n> [!WARNING]\n> **🚨 KTU Examiner's Valuation Pitfall Callout — Common Mark-Loss Points:**\n>\n> 1. **Forgetting associativity:** Many students define reduction without emphasizing the **associativity** of the operator. This is a 1-mark killer — always state it explicitly.\n> 2. **Confusing inclusive vs exclusive scan:** Mixing up the two costs 1–2 marks. Memorize: *inclusive includes itself; exclusive excludes itself (and starts with identity).*\n> 3. **Missing work-vs-time distinction:** A common KTU trap is to claim Hillis–Steele is \"optimal\" because it has $\\mathcal{O}(\\log n)$ time. **Optimality requires BOTH time AND work to be minimal.** Blelloch is optimal; Hillis–Steele is not.\n> 4. **Skipping the latency term:** In communication cost derivations, students often write only $\\frac{n}{p} \\log p$ (bandwidth) and forget the $\\alpha \\log p$ (latency) term. Always include both.\n> 5. **Floating-point caveat:** Examiners may award bonus marks if you mention that floating-point reduction is **non-associative**, hence parallel results are non-deterministic across runs.\n\n---\n\n### 🔁 Topic Recap & Important Things to Remember\n\n- ✅ **Reduction** = applying an associative binary operator across $n$ elements to produce one result.\n- ✅ **Scan / Prefix Sum** = generalized reduction where every position gets the cumulative combination.\n- ✅ **Sequential reduction** is $\\mathcal{O}(n)$; **parallel tree reduction** is $\\mathcal{O}\\!\\left(\\frac{n}{p} + \\log p\\right)$.\n- ✅ **Lower bound on parallel reduction depth** is $\\Omega(\\log n)$ — proven by the binary-tree depth argument.\n- ✅ **Hillis–Steele** = simple parallel scan, $\\mathcal{O}(\\log n)$ time, but $\\mathcal{O}(n \\log n)$ work — **NOT work-efficient**.\n- ✅ **Blelloch** = work-efficient scan, $\\mathcal{O}(n)$ work and $\\mathcal{O}(\\log n)$ time — **OPTIMAL**.\n- ✅ **MPI_Allreduce** uses butterfly / binomial-tree patterns with cost $\\log p \\cdot (\\alpha + \\beta n / p)$ — **bandwidth-optimal**.\n- ✅ **Associativity** is the *sine qua non* of parallel reduction — non-associative ops (like floating-point $+$) yield non-deterministic results.\n- ✅ **Exclusive vs Inclusive Scan:** exclusive starts with the identity; inclusive starts with $a_0$.\n- ✅ Real-world uses: distributed gradient aggregation (AllReduce in DL), MPI collectives, GPU warp reductions, MapReduce reducers.\n- ✅ Always state Brent's theorem: $T_p \\leq \\frac{W}{p} + T_\\infty$ when justifying complexity.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "loopscheduling": {
      "title": "Loop Scheduling",
      "content": "<!-- SECTION_1_START -->\n> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 3\n> - **Topic:** Loop Scheduling\n\n# Loop Scheduling in High Performance Computing\n\n## 1. Core Technical Definition & Intuitive Overview\n\n### Formal Academic Definition\n**Loop Scheduling** is the compiler/runtime strategy used to partition the iterations of a parallel loop among the available threads in a multi-threaded execution environment. In the context of the KTU 2024 HPC syllabus (OpenMP-based shared-memory parallelism), loop scheduling determines *how* loop indices are mapped to threads to balance **load balancing** and **synchronization overhead**.\n\n> [!IMPORTANT]\n> **KTU Syllabus Definition:** Loop scheduling refers to the mechanism by which iterations of a `parallel for` construct (in OpenMP) are distributed across threads. The clause `schedule(kind [, chunk])` controls this distribution.\n\nThe standard KTU-prescribed scheduling kinds are:\n- `static`\n- `dynamic`\n- `guided`\n- `runtime`\n- `auto`\n\n### Conceptual Analogy / Intuition\nImagine a **factory assembly line** with **4 workers (threads)** and **100 toys (loop iterations)** to assemble. The manager (scheduler) must decide *how* to distribute the toys.\n\n- **Static scheduling** = The manager gives each worker a *fixed, predetermined* pile of 25 toys at the start. If one worker is slow, they are stuck with their pile.\n- **Dynamic scheduling** = Toys are placed in a *common bin*. Whenever a worker finishes a toy, they grab the next one from the bin. Maximum fairness, but workers spend time walking to the bin.\n- **Guided scheduling** = At the start, each worker gets a *large* pile. As toys deplete, pile sizes get *smaller*, so imbalance is corrected gradually.\n- **Runtime scheduling** = The manager *waits* until execution begins, then picks the best strategy based on the machine and number of workers.\n- **Auto scheduling** = The *compiler and runtime* together decide the best strategy at runtime with no programmer intervention.\n\n> [!NOTE]\n> **Physical Metric of Concern:** The goal is to minimize **Total Parallel Time (Tₚ)** by balancing two competing costs — *load imbalance* and *synchronization/communication overhead*. Standard HPC benchmarks use **Iterations Per Second (IPS)** as the performance metric.\n\n### Why Loop Scheduling Matters\nIn High Performance Computing, parallel loops are the *most common* source of speedup. A poorly scheduled loop can:\n1. Starve fast threads (idle time = wasted CPU cycles)\n2. Create excessive synchronization (lock contention in the bin)\n3. Degrade cache locality (workers jumping between iterations)\n\n> [!TIP]\n> **KTU Board Exam Tip:** Examiners frequently test the *trade-off* between static (low overhead, poor balance) and dynamic (high overhead, good balance). Memorize this trade-off table.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n\n## 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n### 2.1 Operational Breakdown of Each Scheduling Kind\n\n#### A. STATIC Scheduling (`schedule(static, chunk)`)\n- **Default chunk size** = `ceil(N/P)`, where `N` = total iterations, `P` = number of threads.\n- Iterations are **pre-assigned** to threads in a round-robin fashion with each thread receiving `chunk` consecutive iterations.\n- **No runtime overhead** — assignments are computed once before the loop begins.\n- **Best for:** Uniform workload per iteration (e.g., vector addition, matrix multiplication on regular grids).\n\n#### B. DYNAMIC Scheduling (`schedule(dynamic, chunk)`)\n- Iterations are placed in a **shared work queue**.\n- Each thread grabs a *chunk* of iterations, executes them, and returns for the next chunk.\n- **High runtime overhead** due to atomic queue operations.\n- **Best for:** Highly irregular workloads (e.g., adaptive mesh refinement, sparse matrix operations).\n\n#### C. GUIDED Scheduling (`schedule(guided, chunk)`)\n- Chunk size *decreases* as the loop progresses.\n- Initial chunk size ≈ `ceil(remaining_iterations / P)`.\n- The `chunk` parameter specifies the *minimum* chunk size.\n- **Best for:** Workloads where imbalance is most severe at the start.\n\n#### D. RUNTIME Scheduling (`schedule(runtime)`)\n- The actual scheduling kind is read from the **environment variable `OMP_SCHEDULE`**.\n- No scheduling decision is made at compile time.\n\n#### E. AUTO Scheduling (`schedule(auto)`)\n- The compiler and OpenMP runtime library **jointly** select a schedule at runtime.\n- Implementation-defined; optimized for the specific hardware.\n\n### 2.2 KTU Formula Sheet / Cheat Sheet\n\n> [!NOTE]\n> The following table summarizes the **core formulas and properties** required for KTU 2024 board examinations. Use `\\vert` instead of `|` for absolute value notation.\n\n| Scheduling Kind | Chunk Size Formula (Initial) | Overhead | Load Balance | Best Use Case |\n|---|---|---|---|---|\n| `static` | $\\text{chunk} = \\lceil N / P \\rceil$ | **Very Low** | Poor (for irregular work) | Uniform loops |\n| `static, k` | $k$ (fixed) | **Very Low** | Poor | Uniform loops with custom block |\n| `dynamic, k` | $k$ (fixed) | **High** | Excellent | Irregular loops |\n| `guided` | $\\lceil \\text{remaining} / P \\rceil$ | **Medium** | Very Good | Decreasing imbalance |\n| `guided, k` | $\\max(\\lceil \\text{remaining} / P \\rceil, k)$ | **Medium** | Very Good | Decreasing imbalance w/ floor |\n| `runtime` | Read from `OMP_SCHEDULE` | Variable | Variable | Portable code |\n| `auto` | Implementation-defined | Variable | Optimized | Hardware-tuned code |\n\n### 2.3 Performance Metrics Formulas\n\n**Speedup (S):**\n$$S = \\frac{T_s}{T_p}$$\n\nwhere $T_s$ is the sequential execution time and $T_p$ is the parallel execution time.\n\n**Efficiency (E):**\n$$E = \\frac{S}{P} = \\frac{T_s}{P \\cdot T_p}$$\n\n**Total Parallel Time with Scheduling Overhead:**\n$$T_p = T_{\\text{compute}} + T_{\\text{overhead}}$$\n\nFor dynamic scheduling:\n$$T_{\\text{overhead}} = \\lceil N / k \\rceil \\cdot T_{\\text{lock}}$$\n\nwhere $k$ is the chunk size and $T_{\\text{lock}}$ is the time to acquire/release the work-queue lock.\n\n**Amdahl's Law (applies to parallel loop regions):**\n$$S_{\\text{max}} = \\frac{1}{(1 - f) + \\frac{f}{P}}$$\n\nwhere $f$ is the parallel fraction of the loop.\n\n### 2.4 Real-World Engineering Utility\n- **CFD (Computational Fluid Dynamics):** Adaptive mesh refinement uses dynamic scheduling.\n- **Image Processing:** Pixel-wise operations use static scheduling.\n- **Molecular Dynamics:** Force calculations use guided scheduling.\n- **Database Query Optimization:** Runtime scheduling is preferred for cross-platform deployment.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n\n## 3. Step-by-Step Derivations & Analytical Matrix\n\n### 3.1 Derivation: Optimal Chunk Size for Static Scheduling\n\n**Given:**\n- Total iterations $N = 1000$\n- Number of threads $P = 4$\n- Goal: Distribute iterations evenly with minimal overhead.\n\n**Step 1 — Compute the default chunk size.**\nBy KTU/OpenMP specification, the default static chunk size is:\n$$\\text{chunk} = \\lceil N / P \\rceil$$\n\n**Step 2 — Substitute the values.**\n$$\\text{chunk} = \\lceil 1000 / 4 \\rceil = \\lceil 250 \\rceil = 250$$\n\n**Step 3 — Verify the distribution.**\n- Thread 0: iterations $0$ to $249$ (250 iterations)\n- Thread 1: iterations $250$ to $499$ (250 iterations)\n- Thread 2: iterations $500$ to $749$ (250 iterations)\n- Thread 3: iterations $750$ to $999$ (250 iterations)\n\n**Step 4 — Total iterations assigned.**\n$$P \\cdot \\text{chunk} = 4 \\cdot 250 = 1000 = N \\quad \\checkmark$$\n\n> [!NOTE]\n> If $N$ is *not* divisible by $P$, the **last thread** receives the remainder iterations. E.g., for $N = 1003, P = 4$, chunk = 251, and Thread 3 gets iterations 753 to 1002.\n\n### 3.2 Derivation: Guided Scheduling Chunk Size Evolution\n\n**Given:** $N = 100, P = 4, \\text{minimum chunk} = 2$.\n\n**Iteration 1 of scheduling:**\n$$\\text{chunk}_1 = \\lceil N / P \\rceil = \\lceil 100 / 4 \\rceil = 25$$\n\n**Iteration 2 of scheduling (after Thread 0 takes chunk₁):**\n$$\\text{remaining} = 100 - 25 = 75$$\n$$\\text{chunk}_2 = \\lceil 75 / 4 \\rceil = 19$$\n\n**Iteration 3 of scheduling:**\n$$\\text{remaining} = 75 - 19 = 56$$\n$$\\text{chunk}_3 = \\lceil 56 / 4 \\rceil = 14$$\n\n**Iteration 4 of scheduling:**\n$$\\text{remaining} = 56 - 14 = 42$$\n$$\\text{chunk}_4 = \\lceil 42 / 4 \\rceil = 11$$\n\n**General formula at step $i$:**\n$$\\text{chunk}_i = \\max\\!\\left(\\left\\lceil \\frac{N_i}{P} \\right\\rceil, k_{\\min}\\right)$$\n\nwhere $N_i$ is the number of unassigned iterations at step $i$, and $k_{\\min}$ is the user-specified minimum chunk.\n\n### 3.3 Derivation: Total Overhead for Dynamic Scheduling\n\n**Given:** $N = 1000, P = 4, k = 50, T_{\\text{lock}} = 10 \\mu s$.\n\n**Step 1 — Total number of chunks.**\n$$\\text{Chunks} = \\lceil N / k \\rceil = \\lceil 1000 / 50 \\rceil = 20$$\n\n**Step 2 — Each thread processes how many chunks?**\nSince chunks are taken greedily, each thread processes approximately:\n$$\\text{Chunks per thread} = \\lceil \\text{Chunks} / P \\rceil = \\lceil 20 / 4 \\rceil = 5$$\n\n**Step 3 — Total synchronization overhead (acquire + release).**\n$$T_{\\text{overhead}} = \\text{Chunks} \\cdot 2 \\cdot T_{\\text{lock}} = 20 \\cdot 2 \\cdot 10\\,\\mu s = 400\\,\\mu s$$\n\n**Step 4 — If we increase $k$ to 100:**\n$$\\text{Chunks} = 10, \\quad T_{\\text{overhead}} = 10 \\cdot 2 \\cdot 10\\,\\mu s = 200\\,\\mu s$$\n\n> [!IMPORTANT]\n> **Larger chunk size ⇒ Less overhead but poorer load balance.** This is the *central trade-off* in loop scheduling and is a guaranteed KTU question.\n\n### 3.4 Analytical Matrix: Decision Tree for Choosing a Schedule\n\n| Workload Characteristic | Recommended Schedule | Justification |\n|---|---|---|\n| Uniform iteration cost | `static` | No imbalance, no overhead needed |\n| Mildly irregular cost | `static, k=1` (round-robin) | Spreads imbalance across threads |\n| Highly irregular cost | `dynamic, k=10` | Load balancing outweighs overhead |\n| Decreasing irregularity | `guided` | Adaptive chunk sizing |\n| Unknown at compile time | `runtime` or `auto` | Defers decision to runtime |\n| Nested loops with outer parallelism | `static` (outer) + `dynamic` (inner) | Hierarchical balance |\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n\n## 4. Structural Diagrams & Schematics\n\n### 4.1 Block Diagram: OpenMP Loop Scheduling Architecture\n\n```mermaid\ngraph TD\n    A[Parallel For Construct] --> B{Schedule Kind}\n    B --> C[Static Scheduler]\n    B --> D[Dynamic Scheduler]\n    B --> E[Guided Scheduler]\n    B --> F[Runtime Scheduler]\n    B --> G[Auto Scheduler]\n\n    C --> C1[Pre-compute Chunk Size]\n    C1 --> C2[Round Robin Distribution]\n    C2 --> C3[Thread 0 Block]\n    C2 --> C4[Thread 1 Block]\n    C2 --> C5[Thread 2 Block]\n    C2 --> C6[Thread 3 Block]\n\n    D --> D1[Initialize Shared Work Queue]\n    D1 --> D2[Atomic Lock Acquire]\n    D2 --> D3[Pop Chunk of K Iterations]\n    D3 --> D4[Execute Iterations]\n    D4 --> D5{Queue Empty?}\n    D5 -- No --> D2\n    D5 -- Yes --> D6[Barrier Sync]\n\n    E --> E1[Compute Adaptive Chunk Size]\n    E1 --> E2[Decreasing Chunk Distribution]\n    E2 --> D3\n\n    F --> F1[Read OMP SCHEDULE Env Variable]\n    F1 --> C\n    F1 --> D\n    F1 --> E\n\n    G --> G1[Compiler Runtime Decision]\n    G1 --> C\n    G1 --> D\n    G1 --> E\n```\n\n### 4.2 Execution Flow Diagram: Dynamic Scheduling with Chunks\n\n```mermaid\nflowchart TD\n    start([Loop Start: i=0 to N-1]) --> init[Initialize Work Queue with N Iterations]\n    init --> fork[Fork P Threads]\n    fork --> workQ{Work Queue Empty?}\n\n    workQ -- No --> lock1[Acquire Atomic Lock]\n    lock1 --> pop[Pop Chunk of K Iterations]\n    pop --> unlock1[Release Lock]\n    unlock1 --> exec[Execute Chunk]\n    exec --> workQ\n\n    workQ -- Yes --> barrier[Barrier Synchronization]\n    barrier --> join([Join Threads and End Loop])\n\n    style start fill:#90EE90\n    style join fill:#FFB6C1\n    style lock1 fill:#FFD700\n    style pop fill:#87CEEB\n    style exec fill:#DDA0DD\n```\n\n### 4.3 Comparative Block Diagram: Static vs Dynamic vs Guided\n\n```mermaid\ngraph LR\n    subgraph Static[STATIC SCHEDULING]\n        S0[Thread 0: Iter 0-249]\n        S1[Thread 1: Iter 250-499]\n        S2[Thread 2: Iter 500-749]\n        S3[Thread 3: Iter 750-999]\n    end\n\n    subgraph Dynamic[DYNAMIC SCHEDULING]\n        D0[Thread Pool]\n        D1[Shared Queue]\n        D2[Pull K Iterations Each Time]\n    end\n\n    subgraph Guided[GUIDED SCHEDULING]\n        G0[Initial Large Chunks]\n        G1[Progressively Smaller Chunks]\n        G2[Final Small Chunks]\n    end\n\n    Static --> SNote[Low Overhead, Fixed Assignment]\n    Dynamic --> DNote[High Overhead, Load Balanced]\n    Guided --> GNote[Medium Overhead, Adaptive]\n```\n\n> [!NOTE]\n> **Diagram Interpretation:** Static scheduling uses *pre-computed* partition (one-time cost). Dynamic uses *runtime atomic operations* (per-chunk cost). Guided strikes a balance by amortizing overhead over decreasing chunk sizes.\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n\n## 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n### Part A Questions (3 Marks Each)\n\n#### Q1. `[KTU University Exam - Dec 2023]`\n**Define loop scheduling in OpenMP. List any two scheduling kinds supported by OpenMP.**\n\n**Model Answer:**\nLoop scheduling is the mechanism used by OpenMP to distribute the iterations of a parallel loop among the threads in a team. It is specified using the `schedule` clause on the `parallel for` directive.\n\nThe two scheduling kinds are:\n1. **Static scheduling** — Iterations are divided into chunks of approximately equal size and pre-assigned to threads in a round-robin manner.\n2. **Dynamic scheduling** — Iterations are placed in a shared queue; threads dynamically pull chunks as they finish their current work.\n\n---\n\n#### Q2. `[KTU University Exam - June 2024]`\n**What is the difference between `schedule(static)` and `schedule(dynamic, 1)`?**\n\n**Model Answer:**\n\n| Feature | `schedule(static)` | `schedule(dynamic, 1)` |\n|---|---|---|\n| Chunk size | $\\lceil N / P \\rceil$ | 1 iteration |\n| Distribution | Pre-computed at start | Runtime atomic pull |\n| Overhead | None | High (lock per iteration) |\n| Load balance | Poor for irregular work | Excellent |\n| Best for | Uniform loops | Highly irregular loops |\n\n---\n\n### Part B Questions (14 Marks Each)\n\n> [!NOTE]\n> As per KTU 2024 ESE pattern, students must answer **ONE** of the two choices. Each question is divided into two 7-mark sub-parts.\n\n---\n\n#### **Question A (14 Marks)** `[KTU University Exam - May 2024]`\n\n**(a) Explain in detail the different loop scheduling clauses supported by OpenMP with suitable examples.** *(7 Marks)*\n\n**Model Solution:**\n\nOpenMP provides five loop scheduling kinds, each suited to a different workload characteristic:\n\n**1. `schedule(static [, chunk])`**\n- The default chunk size is `ceil(N/P)`. With `chunk = k`, thread *t* receives iterations $t \\cdot k, t \\cdot k + 1, \\ldots, (t+1) \\cdot k - 1$.\n- No synchronization overhead.\n- **Example:** `#pragma omp parallel for schedule(static, 4)` — each thread gets 4 consecutive iterations at a time.\n\n**2. `schedule(dynamic [, chunk])`**\n- Threads pull `chunk` iterations from a shared work queue at runtime.\n- Implemented using atomic operations on a counter.\n- **Example:** `#pragma omp parallel for schedule(dynamic, 10)` — chunks of 10 iterations are taken dynamically.\n\n**3. `schedule(guided [, chunk])`**\n- Initial chunk size is `ceil(remaining_iterations / P)`, decreasing geometrically.\n- The `chunk` parameter sets the *minimum* chunk size.\n- **Example:** `#pragma omp parallel for schedule(guided, 2)` — chunks start large, shrink to a minimum of 2.\n\n**4. `schedule(runtime)`**\n- Scheduling decision deferred to runtime; reads the `OMP_SCHEDULE` environment variable.\n- Used for portable code where the optimal schedule is machine-dependent.\n\n**5. `schedule(auto)`**\n- Compiler and runtime jointly choose the schedule.\n- Implementation-defined behavior; optimized for the target architecture.\n\n---\n\n**(b) Consider a parallel loop with $N = 2000$ iterations executed on $P = 8$ threads using `schedule(dynamic, 25)`. Calculate:**\n1. The total number of chunks created.\n2. The approximate number of chunks each thread executes.\n3. The total synchronization overhead if each lock acquire-release costs $5 \\mu s$.\n\n*(7 Marks)*\n\n**Model Solution:**\n\n**Part 1 — Total number of chunks:**\n$$\\text{Chunks} = \\lceil N / k \\rceil = \\lceil 2000 / 25 \\rceil = 80$$\n\n**Part 2 — Chunks per thread:**\n$$\\text{Chunks per thread} = \\lceil 80 / 8 \\rceil = 10$$\n\nSo each thread processes approximately **10 chunks of 25 iterations = 250 iterations per thread**.\n\n**Part 3 — Total synchronization overhead:**\nEach chunk acquisition requires **2 atomic operations** (lock + unlock).\n$$T_{\\text{overhead}} = \\text{Chunks} \\cdot 2 \\cdot T_{\\text{lock}} = 80 \\cdot 2 \\cdot 5\\,\\mu s = 800\\,\\mu s$$\n\n> [!WARNING]\n> **KTU Examiner's Pitfall Alert:** Students often forget the factor of **2** (one for lock acquire, one for lock release). Marks are deducted if the atomic operation is counted as a single event. Also, the ceiling function $\\lceil \\cdot \\rceil$ must be explicitly shown when $N$ is not divisible by $k$.\n\n---\n\n#### **Question B (14 Marks)** `[KTU University Exam - Dec 2022]`\n\n**(a) Discuss the trade-off between static and dynamic loop scheduling. Under what conditions would you prefer one over the other?** *(7 Marks)*\n\n**Model Solution:**\n\n**Static Scheduling:**\n- **Advantages:**\n  - Zero runtime overhead.\n  - Excellent cache locality (each thread works on a contiguous block).\n  - Deterministic execution.\n- **Disadvantages:**\n  - Poor load balancing for irregular workloads.\n  - A slow thread can starve the entire parallel region.\n\n**Dynamic Scheduling:**\n- **Advantages:**\n  - Excellent load balancing — work is distributed based on actual completion.\n  - Tolerates thread heterogeneity (e.g., mixed CPU performance cores).\n- **Disadvantages:**\n  - High synchronization overhead due to atomic queue operations.\n  - Poor cache locality — threads jump across iteration indices.\n  - Non-deterministic execution order.\n\n**Decision Criteria:**\n\n| Choose Static When... | Choose Dynamic When... |\n|---|---|\n| Iteration cost is uniform | Iteration cost is highly variable |\n| Loop count is small (overhead dominates) | Loop count is large |\n| Cache locality is critical | Load balance is critical |\n| Hardware is homogeneous | Hardware is heterogeneous |\n\n**Numerical Example of Trade-off:**\nFor $N = 1000, P = 4, k = 10$ in dynamic scheduling:\n- Overhead = $\\lceil 1000/10 \\rceil \\cdot 2 \\cdot T_{\\text{lock}}$\n- If $T_{\\text{lock}} = 50\\,\\mu s$, overhead = $100 \\cdot 2 \\cdot 50 = 10,000\\,\\mu s = 10\\,ms$\n- This is acceptable *only* if the imbalance cost exceeds 10 ms.\n\n---\n\n**(b) For `schedule(guided, 4)` with $N = 500$ iterations and $P = 4$ threads, compute the chunk sizes assigned in the first three scheduling rounds.** *(7 Marks)*\n\n**Model Solution:**\n\n**Round 1:**\n$$\\text{chunk}_1 = \\lceil 500 / 4 \\rceil = 125$$\n$$\\text{remaining} = 500 - 125 = 375$$\n\n**Round 2:**\n$$\\text{chunk}_2 = \\lceil 375 / 4 \\rceil = 94$$\n$$\\text{remaining} = 375 - 94 = 281$$\n\n**Round 3:**\n$$\\text{chunk}_3 = \\lceil 281 / 4 \\rceil = 71$$\n$$\\text{remaining} = 281 - 71 = 210$$\n\n**Verification using general formula:**\n$$\\text{chunk}_i = \\max\\!\\left(\\left\\lceil \\frac{N_i}{P} \\right\\rceil, k_{\\min}\\right) = \\max\\!\\left(\\left\\lceil \\frac{N_i}{4} \\right\\rceil, 4\\right)$$\n\nSince all computed values (125, 94, 71) exceed $k_{\\min} = 4$, no clamping occurs in early rounds.\n\n> [!WARNING]\n> **KTU Examiner's Pitfall Alert:** Two common mistakes:\n> 1. Forgetting to **subtract** the previous chunk from the remaining iterations.\n> 2. Not applying the `max` operation with the minimum chunk size when the computed size drops below $k_{\\min}$.\n> Always show the formula *before* substitution to earn full method marks.\n\n---\n\n> [!WARNING]\n> **🚨 KTU Examiner's Valuation Warning — Common Mark Deduction Zones**\n> 1. **Confusing `runtime` with `auto`:** Runtime reads `OMP_SCHEDULE`; auto lets the compiler decide. They are *not* synonyms.\n> 2. **Forgetting the default chunk size:** Static default is `ceil(N/P)`, *not* `N/P`. Use ceiling division.\n> 3. **Ignoring synchronization cost:** In dynamic scheduling questions, examiners expect students to compute **both** compute time and overhead time.\n> 4. **Mixing up `guided` with `dynamic`:** Guided chunk size *decreases*; dynamic chunk size is *constant*.\n> 5. **Not stating the `schedule` clause syntax:** Always write `schedule(kind, chunk)` with both parameters.\n\n---\n\n### 📋 Topic Recap & Important Things to Remember\n\n- **Loop scheduling** = distribution strategy for loop iterations across threads in a parallel region.\n- **Five OpenMP scheduling kinds:** `static`, `dynamic`, `guided`, `runtime`, `auto`.\n- **Default chunk for static** = $\\lceil N / P \\rceil$.\n- **Trade-off axis:** Static ⇒ low overhead, poor balance. Dynamic ⇒ high overhead, good balance. Guided ⇒ middle ground.\n- **Guided chunk formula:** $\\text{chunk}_i = \\max(\\lceil \\text{remaining} / P \\rceil, k_{\\min})$.\n- **Dynamic overhead formula:** $T_{\\text{ovh}} = \\lceil N/k \\rceil \\cdot 2 \\cdot T_{\\text{lock}}$.\n- **`schedule(runtime)`** defers decision to the `OMP_SCHEDULE` environment variable.\n- **`schedule(auto)`** is compiler/implementation-defined.\n- **Performance metrics:** Speedup $S = T_s / T_p$, Efficiency $E = S / P$.\n- **Best static for uniform loops, dynamic for irregular, guided for decreasing-imbalance loops, runtime for portable code, auto for hardware-tuned code.**\n- **Synchronization overhead is proportional to the number of chunks**, not the number of iterations.\n- **Cache locality is best with static** (contiguous block per thread), worst with dynamic (1-by-1 traversal).\n- **KTU mantra:** *Larger chunk ⇒ less overhead, more imbalance. Smaller chunk ⇒ more overhead, better balance.*\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "tasking": {
      "title": "Tasking",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 3\n> - **Topic:** Tasking\n\n<!-- SECTION_1_START -->\n# Section 1 — Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition (KTU 2024 Syllabus Terminology)\n\n> [!IMPORTANT]\n> **Tasking (OpenMP Tasking Model)** is a parallel programming paradigm in which parallelism is expressed in terms of **dynamically generated units of work called *tasks*** rather than statically partitioned iterations of loops or independent regions. A *task* is a specific instance of executable code together with its **own data environment** that the OpenMP runtime can schedule onto any thread for execution.\n\nIn the **OpenMP 3.0+ specification**, a task is generated the moment a thread encounters a `#pragma omp task` construct. The runtime is then responsible for:\n- **Task creation & allocation** — placing the task into a task pool (often a thread-local deque).\n- **Task scheduling** — assigning a free thread to pick up a task and execute it.\n- **Task synchronization** — using `taskwait`, `taskgroup`, or dependency clauses.\n\nTasks are essential for expressing **irregular parallelism** (recursive algorithms, dynamic data structures, search trees, divide-and-conquer algorithms), which cannot be elegantly expressed through loop parallelism (`#pragma omp parallel for`).\n\n---\n\n## 1.2 Conceptual Analogy — The Office Mailroom\n\nImagine an **office mailroom with 6 workers** (threads):\n\n- The manager places *work orders* (tasks) on a **to-do board** (the runtime task pool).\n- A *work order* has a **description** (the code) and a **list of required files** (the data environment).\n- Any worker who is free can **grab a work order** off the board and execute it. They might **put new sub-orders** on the board (child tasks) while working.\n- Workers can **wait for completion** of certain orders (`taskwait`) before continuing, or declare **dependencies** (\"I need order #7's output before I can start\").\n\n> [!NOTE]\n> This analogy explains why **tasking outperforms pure loop parallelism** in recursive or irregular workloads: the workload is **dynamically balanced** — no worker is stuck waiting for the loop's slowest iteration.\n\n---\n\n## 1.3 Core Constructs and Clauses (Syllabus Highlights)\n\n| Construct / Clause | Purpose |\n|---|---|\n| `#pragma omp task` | Generates a new task |\n| `#pragma omp taskwait` | Waits for **all** child tasks of the current task to complete |\n| `#pragma omp taskgroup` | Creates a region; `taskwait` here is scoped to the group |\n| `depend(in: x)` | Task reads *x* |\n| `depend(out: x)` | Task writes *x* |\n| `depend(inout: x)` | Task both reads and writes *x* |\n| `final(expr)` | If `expr` is true, task becomes a **final task** (its child tasks execute *immediately* and *inline*) |\n| `untied` | A suspended task may be **resumed by any thread** |\n| `mergeable` | The task's data environment is **shared with the parent** (memory optimization) |\n| `priority(p)` | Hint to runtime about scheduling priority |\n| `#pragma omp taskloop` | Loop whose iterations are converted into tasks |\n| `#pragma omp taskyield` | Suspends the current task to allow other tasks to run |\n\n> [!IMPORTANT]\n> **Granularity** is a critical KTU exam concept: *fine-grained tasks* (too many tiny tasks) cause **runtime overhead**, while *coarse-grained tasks* (too few large tasks) cause **load imbalance**. The art of tasking lies in choosing the correct **cutoff depth** (in recursion) or **chunk size** (in taskloops).\n\n---\n\n## 1.4 Key Physical / Standard Metrics\n\n- **Task Granularity (G):** Average execution time of a single task in microseconds (µs).\n- **Task Overhead (Tₒ):** Time to create, schedule, and synchronize a task (~1–10 µs in modern OpenMP).\n- **Critical Path Length (Cₚ):** The longest chain of dependent tasks in the DAG.\n- **Degree of Concurrency (D_c):** Number of independent tasks available at any point in time.\n- **Task Parallelism Ratio (R_tp):** Fraction of program that can be expressed as tasks; bounded by **Amdahl's Law**.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# Section 2 — Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Task Execution Lifecycle (Structured Logic Steps)\n\nThe OpenMP tasking model follows a strict **four-phase lifecycle**:\n\n1. **Task Generation (Creation Phase)**\n   - When a thread encounters `#pragma omp task`, the runtime creates a **task descriptor** containing the code, the data environment (shared variables, firstprivate copies), and scheduling hints.\n   - The task is pushed into the **thread's task pool** (typically a LIFO deque for locality).\n\n2. **Task Scheduling (Allocation Phase)**\n   - Idle threads **steal work** from other threads' task pools (work-stealing scheduler).\n   - The scheduler honors **dependencies** — a task is only eligible to run when its predecessors complete.\n\n3. **Task Execution (Compute Phase)**\n   - The bound thread runs the task's structured block.\n   - The task can itself spawn **child tasks** (nested parallelism in task form).\n   - At **task scheduling points** (implicit or explicit), the thread may switch tasks.\n\n4. **Task Completion (Finalization Phase)**\n   - The task's structured block finishes; the task descriptor is **destroyed**; its `taskwait` counter is **decremented**; dependent tasks are activated.\n\n> [!NOTE]\n> **Implicit Task Scheduling Points** in OpenMP occur at:\n> - The end of the `task` construct (after generation).\n> - Inside `taskwait` and `taskgroup` constructs.\n> - At implicit barriers (unless `nowait` is specified).\n> - At `#pragma omp taskyield`.\n\n---\n\n## 2.2 Task Data Environment (Memory Model)\n\nEach task has its own **Data Environment**, established according to clauses:\n\n| Clause | Variable Treatment |\n|---|---|\n| (default) | Variable inherits from the enclosing context |\n| `shared(x)` | All tasks share the same storage of *x* |\n| `firstprivate(x)` | Each task gets an **uninitialized copy** of *x* from the generating thread |\n| `private(x)` | Each task gets an **uninitialized** local *x* (original is undefined after) |\n| `reduction(operator: list)` | Performs reduction across all tasks |\n\n> [!IMPORTANT]\n> If no data-sharing clause is given, the **default behavior is determined by the enclosing `task` construct's `default(shared)`, `default(private)`, or `default(firstprivate)`**, and ultimately by the surrounding `parallel` region.\n\n---\n\n## 2.3 Task Dependencies — The DAG Model\n\nThe `depend` clause allows the programmer to express **precedence constraints** between tasks, which the runtime builds into a **Directed Acyclic Graph (DAG)**.\n\n$$\\text{Dependence Types: } \\text{depend}(in:S), \\ \\text{depend}(out:S), \\ \\text{depend}(inout:S)$$\n\nThe runtime uses a **task dependency graph** where:\n- **Edge (T₁ → T₂)** means *T₂ cannot start until T₁ completes*.\n- The **Critical Path** determines the **lower bound on parallel execution time**:\n  $$T_{\\text{parallel}} \\geq T_{\\text{critical path}} = \\frac{T_{\\text{serial}}}{N_{\\text{tasks}}} \\cdot L_{\\text{crit}}$$\n\nwhere $L_{\\text{crit}}$ is the cumulative work along the longest dependency chain.\n\n---\n\n## 2.4 KTU Formula Sheet / Cheat Sheet\n\n> [!NOTE]\n> All formulas below are **KTU-board-testable** and appear frequently in Module-3 derivations. Use `\\vert` instead of `|` to prevent markdown table breakage.\n\n| # | Formula / Metric | Mathematical Expression | Engineering Meaning / Unit |\n|---|---|---|---|\n| 1 | **Amdahl's Law (Tasking Speedup)** | $S(N) = \\dfrac{1}{(1 - R_{tp}) + \\dfrac{R_{tp}}{N}}$ | Max speedup with *N* threads; $R_{tp}$ = task-parallel fraction |\n| 2 | **Task Granularity** | $G = \\dfrac{T_{\\text{task}}}{T_{\\text{overhead}}}$ | Ratio of work per task to creation cost (target $\\geq$ **100**) |\n| 3 | **Cutoff Depth (Recursion)** | $d_{cut} = \\lceil \\log_{2} P \\rceil$ | Recommended depth to stop spawning tasks (*P* = thread count) |\n| 4 | **Parallel Execution Time (Ideal)** | $T_p = \\dfrac{T_s}{N} + T_{\\text{overhead}} \\cdot N_{\\text{tasks}}$ | $T_s$ = serial time, $N$ = # of processors |\n| 5 | **Task Efficiency** | $\\eta_{\\text{task}} = \\dfrac{T_{\\text{useful}}}{T_{\\text{useful}} + T_{\\text{overhead}}}$ | Should approach **1.0** for well-tuned tasking |\n| 6 | **Critical Path Bound** | $T_p \\geq T_{\\text{CP}} = \\sum_{i \\in \\text{crit}} w_i$ | Sum of weights along longest DAG path |\n| 7 | **Load Imbalance Factor** | $\\beta = \\dfrac{T_{\\text{max-thread}} - T_{\\text{avg-thread}}}{T_{\\text{avg-thread}}}$ | Goal: $\\beta \\rightarrow 0$ |\n| 8 | **Work-Stealing Threshold** | $K_{\\text{steal}} \\geq 2 \\cdot G$ | Minimum tasks per deque before stealing pays off |\n| 9 | **Memory per Task (approx.)** | $M_{\\text{task}} \\approx 64 + 8 \\cdot n_{\\text{firstpriv}}$ bytes | Descriptor overhead; *n* = firstprivate vars |\n| 10 | **Gustafson's Scaled Speedup** | $S'(N) = N - (N-1) \\cdot (1 - R_{tp})$ | Speedup when problem size scales with *N* |\n\n---\n\n## 2.5 Real-World Engineering Utility\n\nTasking is the **workhorse of modern HPC applications**:\n\n- **Computational Fluid Dynamics (CFD):** Adaptive mesh refinement spawns a new task for each sub-cell that needs refinement.\n- **Sparse Linear Algebra (LU/QR Factorization):** Dependency DAGs map naturally to task dependencies.\n- **Recursive N-Body Simulations (Barnes-Hut):** Octree traversal uses task recursion with cutoff.\n- **AI / Deep Learning Graph Schedulers:** TensorFlow / PyTorch's graph executor is essentially a **task DAG scheduler**.\n- **Cryptography & Branch-and-Bound Solvers:** Search tree exploration is a classic tasking use-case.\n\n> [!IMPORTANT]\n> In the **Intel OpenMP runtime** (libiomp5) and **LLVM OpenMP** (libomp), the default scheduler is a **work-stealing scheduler**, which is what makes dynamic tasking scalable on many-core CPUs (e.g., 64+ cores on Xeon Phi, AMD EPYC).\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# Section 3 — Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation: Optimal Cutoff Depth for Recursive Tasking\n\n**Problem Setup:** A recursive divide-and-conquer algorithm spawns 2 child tasks at each level. There are $P$ processors. Find the optimal cutoff depth $d_{cut}$ at which the recursion should stop spawning tasks and execute serially.\n\n**Step 1 — Count the tasks generated.**\n\nAt depth $d$, the recursion tree has $2^d$ leaves. The total number of generated tasks is:\n$$N_{\\text{tasks}}(d) = 2^{d+1} - 1$$\nwhere the “-1” accounts for the root, and \"+1\" is the standard full-tree count.\n\n**Step 2 — Total computation time per leaf.**\n\nLet $T_{\\text{leaf}}$ be the work at the cutoff. Total serial work is:\n$$T_s = N_{\\text{tasks}}(d_{cut}) \\cdot T_{\\text{leaf}}$$\n\n**Step 3 — Ideal parallel time.**\n\nWith perfect load balancing across $P$ threads:\n$$T_p = \\frac{T_s}{P} + T_{\\text{overhead}} \\cdot N_{\\text{tasks}}(d_{cut})$$\n\n**Step 4 — Substitute and simplify.**\n\n$$T_p = \\frac{(2^{d+1} - 1) \\cdot T_{\\text{leaf}}}{P} + T_{\\text{overhead}} \\cdot (2^{d+1} - 1)$$\n\n**Step 5 — Define Granularity.**\n\nLet $G = \\dfrac{T_{\\text{leaf}}}{T_{\\text{overhead}}}$. Then:\n$$T_p = T_{\\text{overhead}} \\cdot (2^{d+1} - 1) \\left[ \\frac{G}{P} + 1 \\right]$$\n\n**Step 6 — Find the optimal $d_{cut}$ that minimizes $T_p$.**\n\nFor large $2^{d+1}$, the overhead term dominates when $G < P$. The optimal balance is achieved when:\n$$2^{d_{cut}} \\approx P$$\n\nTaking $\\log_2$ on both sides:\n$$d_{cut} \\approx \\log_2 P$$\n\n**Step 7 — Final boxed result.**\n\n$$\\boxed{\\ d_{cut} = \\lceil \\log_2 P \\rceil\\ }$$\n\n> [!NOTE]\n> **Engineering Interpretation:** For a 16-core machine, stop spawning new tasks at recursion depth $\\lceil \\log_2 16 \\rceil = 4$. Going deeper creates more tasks than cores, which is wasteful overhead. Going shallower underutilizes the cores.\n\n---\n\n## 3.2 Derivation: Task Parallelism Speedup Using Amdahl's Law\n\n**Problem Setup:** A program has a serial portion of fraction $(1 - R_{tp})$ and a task-parallel portion of fraction $R_{tp}$ that uses $N$ threads.\n\n**Step 1 — Serial execution time.**\n$$T_s = T_{\\text{serial}} + T_{\\text{task}}$$\n\n**Step 2 — Parallel execution time.**\n$$T_p = T_{\\text{serial}} + \\frac{T_{\\text{task}}}{N} + T_{\\text{overhead}} \\cdot N_{\\text{tasks}}$$\n\n**Step 3 — Define $R_{tp}$.**\n$$R_{tp} = \\frac{T_{\\text{task}}}{T_s}$$\n\n**Step 4 — Speedup definition.**\n$$S(N) = \\frac{T_s}{T_p}$$\n\n**Step 5 — Substitute, assuming negligible overhead first.**\n$$S(N) = \\frac{T_s}{T_{\\text{serial}} + \\dfrac{T_{\\text{task}}}{N}} = \\frac{1}{(1 - R_{tp}) + \\dfrac{R_{tp}}{N}}$$\n\n**Step 6 — Include task creation overhead $O$.**\n\nLet $O = \\dfrac{T_{\\text{overhead}} \\cdot N_{\\text{tasks}}}{T_s}$. Then:\n$$S(N) = \\frac{1}{(1 - R_{tp}) + \\dfrac{R_{tp}}{N} + O}$$\n\n**Step 7 — Take the limit as $N \\to \\infty$.**\n$$\\lim_{N \\to \\infty} S(N) = \\frac{1}{1 - R_{tp} + O}$$\n\n**Step 8 — Final boxed result (with overhead).**\n\n$$\\boxed{\\ S(N) = \\dfrac{1}{(1 - R_{tp}) + \\dfrac{R_{tp}}{N} + O}\\ }$$\n\n> [!IMPORTANT]\n> Notice that **task creation overhead $O$ directly subtracts from the achievable speedup**, just like the serial fraction. This is why *coarse-grained tasks* (large $T_{\\text{task}}$ per task) are critical for HPC performance.\n\n---\n\n## 3.3 Analytical Comparison Matrix — Tasking vs. Loop Parallelism\n\n| Performance Dimension | `#pragma omp parallel for` | `#pragma omp task` |\n|---|---|---|\n| **Workload Type** | Regular, loop-structured | Irregular, dynamic, recursive |\n| **Scheduling** | Static / dynamic chunks | Work-stealing from deques |\n| **Load Balance** | Depends on `schedule` clause | **Naturally dynamic** (good) |\n| **Overhead** | Low per iteration | Higher per task (descriptor) |\n| **Scalability on Many-Cores** | Limited (chunk imbalance) | **High** (work-stealing excels) |\n| **Dependency Support** | None (loop iterations assumed independent) | **Native** via `depend()` |\n| **Best for** | Dense matrix ops, vector kernels | Sparse algebra, N-body, search trees |\n| **Cutoff / Granularity Control** | `schedule(static/dynamic/guided)` | `cutoff` depth, `if(false)` clauses |\n\n---\n\n## 3.4 Worked Numerical Example (KTU Exam-Style)\n\n**Question:** A recursive Fibonacci program is parallelized using OpenMP tasks on a machine with **$P = 8$** cores. The serial work is $T_s = 100$ ms and task creation overhead is $T_o = 5 \\,\\mu s$ per task. Find the optimum cutoff depth.\n\n**Solution — Step by Step:**\n\n**Step 1.** Using the formula derived in §3.1:\n$$d_{cut} = \\lceil \\log_2 P \\rceil = \\lceil \\log_2 8 \\rceil = \\lceil 3 \\rceil = 3$$\n\n**Step 2.** At cutoff depth 3, total tasks generated:\n$$N_{\\text{tasks}} = 2^{3+1} - 1 = 15$$\n\n**Step 3.** Work per leaf (cutoff task):\n$$T_{\\text{leaf}} = \\frac{T_s}{N_{\\text{tasks}}} = \\frac{100 \\text{ ms}}{15} \\approx 6.67 \\text{ ms}$$\n\n**Step 4.** Total task-creation overhead:\n$$T_{\\text{overhead}} = N_{\\text{tasks}} \\cdot T_o = 15 \\times 5 \\,\\mu s = 75 \\,\\mu s = 0.075 \\text{ ms}$$\n\n**Step 5.** Ideal parallel time (perfect load balance):\n$$T_p = \\frac{T_s}{P} + T_{\\text{overhead}} = \\frac{100}{8} + 0.075 = 12.5 + 0.075 = 12.575 \\text{ ms}$$\n\n**Step 6.** Achieved speedup:\n$$S = \\frac{T_s}{T_p} = \\frac{100}{12.575} \\approx 7.95$$\n\n**Step 7.** Efficiency:\n$$\\eta = \\frac{S}{P} = \\frac{7.95}{8} \\approx 99.4\\%$$\n\n> [!NOTE]\n> **Result:** With 8 cores and a correctly chosen cutoff depth, the speedup is **~7.95×** (efficiency **99.4%**), demonstrating that **tasking is near-optimal for recursive algorithms** when granularity is properly tuned.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# Section 4 — Structural Diagrams & Schematics\n\n## 4.1 Task Lifecycle State Machine (Mermaid)\n\n```mermaid\nstateDiagram-v2\n    [*] --> GENERATED : thread encounters omp task\n    GENERATED --> SCHEDULED : pushed to task deque\n    SCHEDULED --> READY : dependencies satisfied\n    READY --> RUNNING : thread assigned\n    RUNNING --> SUSPENDED : task scheduling point hit\n    SUSPENDED --> RUNNING : resumed by same or untied thread\n    RUNNING --> COMPLETED : structured block finishes\n    COMPLETED --> [*] : descriptor destroyed and counters decremented\n    READY --> BLOCKED : depend not satisfied yet\n    BLOCKED --> READY : predecessor task completes\n```\n\n**Block-Level Interpretation of the Diagram:**\n\n- **GENERATED** — The runtime has allocated a task descriptor in the parent's task pool.\n- **SCHEDULED** — The task descriptor is sitting in a thread's LIFO deque, ready to be picked up.\n- **READY** — All `depend` clauses are satisfied; the task is eligible to run.\n- **RUNNING** — A thread is actively executing the task's code.\n- **SUSPENDED** — The task hit a scheduling point (e.g., `taskwait`) and was paused.\n- **BLOCKED** — A predecessor task has not yet produced the required data.\n- **COMPLETED** — The task is finished; dependent tasks are unblocked.\n\n---\n\n## 4.2 Task Dependency DAG for a Sparse Matrix Operation\n\n```mermaid\nflowchart TD\n    A[Task A: read matrix blocks] --> B[Task B: compute block 0 0]\n    A --> C[Task C: compute block 0 1]\n    A --> D[Task D: compute block 1 0]\n    A --> E[Task E: compute block 1 1]\n    B --> F[Task F: combine block row 0]\n    C --> F\n    D --> G[Task G: combine block row 1]\n    E --> G\n    F --> H[Task H: write final result]\n    G --> H\n```\n\n**Engineering Reading of the DAG:**\n\n- All four compute tasks (B, C, D, E) are **independent** — they can run in parallel.\n- F depends on B and C (`depend(in: B_out, C_out)`).\n- G depends on D and E.\n- H depends on both F and G.\n- The **critical path** is `A → B → F → H` (or equivalently `A → D → G → H`), so:\n  $$T_p \\geq T_A + T_B + T_F + T_H$$\n\n---\n\n## 4.3 Recursive Task Spawning with Cutoff (Block Diagram)\n\n```mermaid\nflowchart TD\n    R[Root Task at depth 0] --> L1A[Left Child at depth 1]\n    R --> R1B[Right Child at depth 1]\n    L1A --> L2A[Left Grandchild at depth 2]\n    L1A --> R2A[Right Grandchild at depth 2]\n    R1B --> L2B[Left Grandchild at depth 2]\n    R1B --> R2B[Right Grandchild at depth 2]\n    L2A --> S1[Serial Execution at depth dcut]\n    R2A --> S2[Serial Execution at depth dcut]\n    L2B --> S3[Serial Execution at depth dcut]\n    R2B --> S4[Serial Execution at depth dcut]\n```\n\n**Block-Level Interpretation:**\n\n- The recursion tree **branches** at each level until depth $d_{cut}$.\n- At $d_{cut}$, the algorithm **switches to serial execution** to avoid overhead.\n- The number of leaves at $d_{cut}$ is $2^{d_{cut}}$, and with $P$ cores, we set $d_{cut} = \\lceil \\log_2 P \\rceil$.\n\n---\n\n## 4.4 Work-Stealing Task Scheduler (Architecture)\n\n```mermaid\nflowchart LR\n    subgraph T1 [Thread 1]\n        D1[Local Deque]\n    end\n    subgraph T2 [Thread 2]\n        D2[Local Deque]\n    end\n    subgraph T3 [Thread 3]\n        D3[Local Deque]\n    end\n    subgraph T4 [Thread 4]\n        D4[Local Deque]\n    end\n    R[Runtime Scheduler] --> D1\n    R --> D2\n    R --> D3\n    R --> D4\n    D1 -.steal.-> D2\n    D2 -.steal.-> D3\n    D3 -.steal.-> D4\n    D4 -.steal.-> D1\n```\n\n**Reading the Diagram:**\n\n- Each thread maintains a **LIFO local deque** of tasks it has generated.\n- When a thread's deque empties, it **steals** tasks from other threads' deques (FIFO from the *bottom* to reduce contention with the owner's LIFO push/pop from the *top*).\n- This is the **canonical scheduler** in modern OpenMP runtimes (Intel, GNU libgomp, LLVM libomp).\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# Section 5 — KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n## Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. `[KTU University Exam — Dec 2023]`\n**Define an OpenMP task. List any two clauses that can be used with the `omp task` construct.**\n\n**Model Answer (3 Marks):**\n\n> An OpenMP task is a unit of work generated dynamically when a thread encounters a `#pragma omp task` construct. Each task has its own data environment and is scheduled by the OpenMP runtime onto an available thread.\n\n**Two clauses (1.5 marks each):**\n\n1. **`depend(type: var)`** — specifies data dependencies between tasks (e.g., `in`, `out`, `inout`).\n2. **`final(expr)`** — when `expr` evaluates to true, the task becomes a final task whose child tasks are executed inline by the same thread, avoiding task creation overhead.\n\n*(Valid alternatives: `if`, `private`, `firstprivate`, `shared`, `untied`, `priority`, `mergeable`.)*\n\n---\n\n### Q2. `[KTU University Exam — May 2024]`\n**What is the role of the `taskwait` directive? How does it differ from a barrier?**\n\n**Model Answer (3 Marks):**\n\n> The `taskwait` directive synchronizes the current task with **only its immediate child tasks**, ensuring they all complete before execution proceeds past the directive. **(1.5 marks)**\n\n> In contrast, a **barrier** (`#pragma omp barrier`) synchronizes **all threads** in the current team. (1 mark) Additionally, `taskwait` can have an explicit `depend` list to wait for only a subset of children. **(0.5 marks)**\n\n---\n\n## Part B — 14-Mark Questions (ESE Module Internal Choice Format)\n\n### Question A (14 Marks) `[KTU University Exam — Jul 2023]`\n\n#### (a) Explain the OpenMP tasking model in detail, including task generation, scheduling, and completion. Discuss the significance of the `depend` clause. **(7 marks)**\n\n**Model Solution:**\n\n**1. Task Generation (2 marks):**\nA task is created when any thread in a parallel region encounters the `omp task` construct. The OpenMP runtime allocates a *task descriptor* containing:\n- The code (a pointer to the structured block).\n- The data environment (shared, private, firstprivate variables).\n- Scheduling information (`depend`, `final`, `priority`, etc.).\n\nThe new task is pushed into the generating thread's **task deque** (LIFO structure for cache locality).\n\n**2. Task Scheduling (2.5 marks):**\nModern OpenMP uses a **work-stealing scheduler**:\n- A thread that becomes idle (its deque is empty) **steals** the oldest task from the *bottom* of another thread's deque.\n- The scheduler respects **dependency constraints** — a task with `depend(in: x)` runs only after the producer of *x* completes.\n- For `final(expr) == true` tasks, the runtime **inlines** the task (executes immediately on the same thread), avoiding scheduler overhead.\n\n**3. Task Completion (1 mark):**\nUpon finishing, the task descriptor is destroyed, and the **completion counter** of the parent task is decremented. If the parent was blocked in `taskwait`, it may now resume.\n\n**4. Significance of `depend` clause (1.5 marks):**\n- Expresses **producer-consumer** relationships between tasks.\n- Builds an internal **DAG** that the runtime schedules.\n- Avoids **race conditions** on shared data without explicit locks.\n- Three types: `in` (read), `out` (write), `inout` (read-modify-write).\n- Enables **fine-grained synchronization** more efficient than coarse barriers.\n\n---\n\n#### (b) Consider a recursive divide-and-conquer algorithm parallelized using OpenMP tasks on a 16-core processor. Derive the optimal cutoff depth and compute the parallel execution time and speedup. Assume the serial runtime is $T_s = 256$ ms and task creation overhead is $T_o = 10\\,\\mu s$ per task. **(7 marks)**\n\n**Model Solution:**\n\n**Step 1 — Optimal Cutoff Depth (2 marks):**\n\nUsing the derived formula $d_{cut} = \\lceil \\log_2 P \\rceil$:\n$$d_{cut} = \\lceil \\log_2 16 \\rceil = 4$$\n\n**Step 2 — Number of Tasks (1.5 marks):**\n\nFor a binary recursion at depth 4:\n$$N_{\\text{tasks}} = 2^{4+1} - 1 = 31$$\n\n**Step 3 — Work per Leaf (1 mark):**\n$$T_{\\text{leaf}} = \\frac{T_s}{N_{\\text{tasks}}} = \\frac{256 \\text{ ms}}{31} \\approx 8.258 \\text{ ms}$$\n\n**Step 4 — Total Overhead (1 mark):**\n$$T_{\\text{overhead}} = N_{\\text{tasks}} \\cdot T_o = 31 \\times 10\\,\\mu s = 310\\,\\mu s = 0.310 \\text{ ms}$$\n\n**Step 5 — Parallel Time & Speedup (1.5 marks):**\n$$T_p = \\frac{T_s}{P} + T_{\\text{overhead}} = \\frac{256}{16} + 0.310 = 16.0 + 0.310 = 16.31 \\text{ ms}$$\n\n$$S = \\frac{T_s}{T_p} = \\frac{256}{16.31} \\approx 15.7$$\n\n$$\\eta = \\frac{S}{P} = \\frac{15.7}{16} \\approx 98.1\\%$$\n\n**Conclusion:** A cutoff depth of **4** yields a near-ideal speedup of **15.7×** with **98.1% efficiency** on 16 cores.\n\n---\n\n### Question B (14 Marks) `[KTU University Exam — May 2024]`\n\n#### (a) Discuss the differences between the OpenMP *task* model and the *parallel for* model. When would you prefer one over the other? **(7 marks)**\n\n**Model Solution:**\n\n| Aspect | `parallel for` | `task` |\n|---|---|---|\n| Parallelism source | Loop iterations | Dynamically generated units |\n| Workload | Regular, predictable | Irregular, recursive, dynamic |\n| Scheduling | `schedule(static/dynamic/guided/runtime)` | Work-stealing from deques |\n| Load balance | Static split can be imbalanced | Naturally balanced |\n| Dependencies | Not expressed | Expressed via `depend()` |\n| Overhead | Low (loop pre-computed) | Higher (descriptor per task) |\n\n**1. When to use `parallel for` (2 marks):**\n- Dense matrix operations, vector arithmetic.\n- When loop count is known and iterations are independent.\n- When the iteration time is roughly uniform.\n\n**2. When to use `task` (2 marks):**\n- Recursive algorithms (Fibonacci, Quicksort, FFT, N-body).\n- Sparse data structures (linked lists, trees, graphs).\n- When work is generated dynamically (e.g., branch-and-bound).\n- Pipeline / DAG-structured workflows.\n\n**3. Hybrid use (1.5 marks):**\nA common HPC pattern is `#pragma omp parallel` containing a `taskloop` or `parallel for` with task-internal substructures — combining the best of both models.\n\n**4. Code example for clarity (1.5 marks):**\n\n```c\n#pragma omp parallel\n{\n    #pragma omp single\n    {\n        process_chunk(0, N/2);\n        #pragma omp task\n        process_chunk(N/2, N);\n        #pragma omp taskwait\n    }\n}\n```\n\nThis pattern is **idiomatic OpenMP tasking** — only one thread generates tasks, and the rest steal from its deque.\n\n---\n\n#### (b) Explain the concept of **task granularity** and the **cutoff strategy** in OpenMP tasking. Using Amdahl's Law, derive the maximum achievable speedup when $R_{tp} = 0.92$ and overhead $O = 0.03$ on $N = 32$ cores. **(7 marks)**\n\n**Model Solution:**\n\n**1. Task Granularity (2 marks):**\nTask granularity $G = T_{\\text{task}} / T_{\\text{overhead}}$ is the ratio of useful work to creation overhead.\n- $G < 1$: tasks are too small; overhead dominates — **bad**.\n- $G \\gg 1$: tasks are too large; load imbalance and poor scalability — **also bad**.\n- **Optimal:** $G \\approx 100$ to $1000$.\n\n**2. Cutoff Strategy (1.5 marks):**\nFor recursive algorithms, **stop spawning tasks** at depth $d_{cut} = \\lceil \\log_2 P \\rceil$ and execute serially. This bounds the number of tasks to roughly the number of cores, minimizing overhead.\n\n**3. Amdahl's Law Derivation (2 marks):**\n\nStart with:\n$$S(N) = \\frac{1}{(1 - R_{tp}) + \\dfrac{R_{tp}}{N} + O}$$\n\n**Step 1 — Substitute** $R_{tp} = 0.92$, $N = 32$, $O = 0.03$:\n$$S(32) = \\frac{1}{(1 - 0.92) + \\dfrac{0.92}{32} + 0.03}$$\n\n**Step 2 — Evaluate each term:**\n$$1 - 0.92 = 0.08, \\quad \\frac{0.92}{32} = 0.02875$$\n\n$$S(32) = \\frac{1}{0.08 + 0.02875 + 0.03}$$\n\n**Step 3 — Sum denominator:**\n$$0.08 + 0.02875 + 0.03 = 0.13875$$\n\n**Step 4 — Final result:**\n$$\\boxed{S(32) = \\frac{1}{0.13875} \\approx 7.21}$$\n\n**4. Analysis (1.5 marks):**\n- Linear (ideal) speedup would be $32\\times$.\n- Achieved speedup is only $7.21\\times$ → efficiency $\\eta = 7.21/32 \\approx 22.5\\%$.\n- **Bottleneck:** The serial fraction $1 - R_{tp} = 0.08$ caps the *ideal* speedup at $1/0.08 = 12.5\\times$.\n- **Recommendation:** Refactor the serial portion to expose more task parallelism (e.g., parallelize the I/O or graph setup phase) to push $R_{tp}$ closer to 1.0.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Warning — Common Pitfalls:**\n> 1. **Confusing `taskwait` with `barrier`.** Many students write that `taskwait` synchronizes *all* threads. It does NOT — it only waits for *child tasks of the current task* to complete.\n> 2. **Forgetting the \"-1\" in task count.** Recursive tasking has $2^{d+1} - 1$ total tasks, not $2^{d+1}$. Marks lost: 1–2 per question.\n> 3. **Using `depend(in: x)` incorrectly.** Some students write `depend(read: x)` — there is no `read`; the correct keyword is `in`.\n> 4. **Ignoring overhead in speedup derivations.** Always include the $O$ term in Amdahl's Law for task-based code; examiners specifically test this.\n> 5. **Setting cutoff depth = total recursion depth.** The cutoff is the depth at which you *stop* spawning, not the total recursion depth.\n\n---\n\n## Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid-Revision Checklist (Must-Memorize for KTU Exams):**\n\n- ⭐ **Task:** A dynamically generated unit of work with its own data environment; created by `#pragma omp task`.\n- ⭐ **Task Pool / Deque:** Per-thread LIFO structure where new tasks are pushed; other threads *steal* from the bottom.\n- ⭐ **Task Lifecycle States:** GENERATED → SCHEDULED → READY → RUNNING → COMPLETED (with SUSPENDED and BLOCKED as side states).\n- ⭐ **Task Scheduling Points:** Implicit at end of `task`, inside `taskwait`/`taskgroup`, and at barriers.\n- ⭐ **Key Directives:** `task`, `taskwait`, `taskgroup`, `taskloop`, `taskyield`.\n- ⭐ **Key Clauses:** `depend(in/out/inout)`, `final()`, `untied`, `mergeable`, `priority()`, `if()`.\n- ⭐ **Default Scheduling:** Work-stealing (deques), as opposed to chunk-based for loops.\n- ⭐ **Optimal Cutoff Depth (Recursion):** $d_{cut} = \\lceil \\log_2 P \\rceil$.\n- ⭐ **Task Granularity Target:** $G = T_{\\text{task}} / T_{\\text{overhead}} \\geq 100$ ideally.\n- ⭐ **Amdahl's Law (with overhead):** $S(N) = 1 / \\left[ (1 - R_{tp}) + (R_{tp}/N) + O \\right]$.\n- ⭐ **Gustafson's Law (alternative view):** $S'(N) = N - (N-1)(1 - R_{tp})$.\n- ⭐ **Tasking is preferred for:** Recursion, sparse structures, irregular/divergent workloads, DAG-based computations.\n- ⭐ **Loop parallelism is preferred for:** Dense, regular, loop-bound computations (e.g., BLAS kernels).\n- ⭐ **Critical Path Lower Bound:** $T_p \\geq$ sum of weights along the longest dependency chain in the task DAG.\n- ⭐ **Final Clause Behavior:** When true, child tasks of this task are *inlined* (no further task creation).\n- ⭐ **Untied Clause:** A suspended task may be resumed by a *different* thread (default: same thread only).\n- ⭐ **Mergeable Clause:** The child task's data environment is shared with the parent (memory optimization).\n- ⭐ **High-Yield Application Domains:** CFD (adaptive mesh), N-body (Barnes-Hut), sparse factorizations, branch-and-bound, deep learning graph schedulers.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "miscellaneous": {
      "title": "Miscellaneous",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 3\n> - **Topic:** Miscellaneous\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Introduction to Miscellaneous HPC Performance Concepts\n\nIn High Performance Computing (HPC), beyond the core paradigms of parallelism, distributed systems, and GPU computing, there exists a **miscellaneous yet exam-critical** cluster of performance concepts. These concepts govern *how* we **measure**, *quantify*, and *predict* the behavior of parallel systems.\n\n> [!IMPORTANT]\n> **KTU 2024 Scheme Insight:** The \"Miscellaneous\" module typically covers **Performance Metrics, Amdahl's Law, Gustafson's Law, Karp-Flatt Metric, Isoefficiency, Scalability Analysis, Load Balancing, and Heterogeneous Computing fundamentals**. These are heavily weighted in Part A (3-mark) and Part B (14-mark) questions.\n\n---\n\n### 1.2 Core Performance Metrics — Formal Definitions\n\n**Speedup ($S_p$):**\nThe ratio of the time taken to solve a problem on a single processor (sequential time) to the time taken to solve the *same problem* on a parallel system with $p$ processors.\n\n$$S_p = \\frac{T_s}{T_p}$$\n\nWhere $T_s$ is the **sequential execution time** and $T_p$ is the **parallel execution time**.\n\n**Efficiency ($E_p$):**\nThe ratio of speedup to the number of processors, indicating how well the processors are utilized.\n\n$$E_p = \\frac{S_p}{p} = \\frac{T_s}{p \\cdot T_p}$$\n\n> [!NOTE]\n> **Ideal case:** $S_p = p$ and $E_p = 1$ (linear speedup). In practice, $0 < E_p \\le 1$.\n\n**Cost ($C_p$):**\nThe product of the number of processors and the parallel execution time, representing the *total processor-time investment*.\n\n$$C_p = p \\cdot T_p$$\n\n**Cost-Optimal (Cost-Efficient) System:**\nA parallel system is **cost-optimal** if the cost of solving a problem on a parallel computer is **equal to** the cost of solving the same problem on a sequential computer.\n\n$$C_p = \\Theta(T_s)$$\n\n---\n\n### 1.3 Amdahl's Law — The Fundamental Limit\n\n**Formal Definition:** Amdahl's Law states that the *maximum achievable speedup* of a parallel program is **limited by its sequential (non-parallelizable) portion**, regardless of how many processors are used.\n\n$$S_{\\text{max}}(p) = \\frac{1}{f + \\frac{1-f}{p}}$$\n\nWhere:\n- $f$ = fraction of the program that is **inherently sequential** (must run on one processor)\n- $(1-f)$ = fraction that is **parallelizable**\n- $p$ = number of processors\n\n**Intuitive Analogy — The Toll Booth:** Imagine a highway with 100 toll booths. If **30% of the highway** is a single-lane bridge (sequential portion), then no matter how many booths you add, traffic will *always* bottleneck at that bridge. Even 1000 booths give you less than 3.3x speedup. This is Amdahl's Law in action.\n\n---\n\n### 1.4 Gustafson's Law — The Realistic Counterpoint\n\n**Formal Definition:** Gustafson's Law observes that for **scaled problems** (where problem size grows with the number of processors), the sequential portion does *not* dominate.\n\n$$S_{\\text{Gustafson}}(p) = p - f \\cdot (p - 1)$$\n\n**Intuitive Analogy — The Expanding Workshop:** Instead of building 100 chairs with 100 workers (fixed task), give each worker *more work proportional to their number*. With 100 workers, build 1000 chairs. The overhead of \"opening the workshop\" (sequential portion) becomes negligible as the workshop scales up.\n\n---\n\n### 1.5 Karp-Flatt Metric — Diagnosing Parallelism\n\nA metric that **identifies the source of inefficiency** in a parallel program.\n\n$$e = \\frac{\\frac{1}{S_p} - \\frac{1}{p}}{1 - \\frac{1}{p}}$$\n\nWhere $e$ is the **serial fraction experimentally observed**. A *constant* $e$ across different $p$ values indicates inefficiency is due to **inherently sequential code**. A *decreasing* $e$ suggests **overhead-related inefficiency** (load imbalance, communication).\n\n---\n\n### 1.6 Scalability — Strong vs. Weak\n\n| Type | Definition | Metric |\n|------|------------|--------|\n| **Strong Scaling** | Problem size $W$ is **fixed**; $p$ increases. | Speedup $S_p$ for constant $W$ |\n| **Weak Scaling** | Problem size **grows proportionally** with $p$. | Speedup $S_p$ where $W = O(p)$ |\n\n**Isoefficiency:** A metric showing how efficiently a parallel algorithm scales as both problem size and number of processors increase.\n\n---\n\n### 1.7 Heterogeneous Computing — A Modern Paradigm\n\n**Formal Definition:** A computing model that uses **different types of computational units** (CPUs, GPUs, FPGAs, DSPs) cooperatively, each handling tasks best suited to its architecture.\n\n**Intuitive Analogy — The Construction Site:** The **CPU** (foreman) handles complex planning, branching decisions, and sequential tasks. The **GPU** (labor crew) handles massive repetitive tasks (e.g., millions of pixel operations). Together, they build a skyscraper far faster than either alone.\n\n> [!NOTE]\n> **KTU 2024 Context:** Heterogeneous computing is the foundation of modern HPC platforms (e.g., **CUDA**, **OpenCL**, **Heterogeneous System Architecture - HSA**).\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Deep Dive: Amdahl's Law — Structural Analysis\n\nThe derivation of Amdahl's Law assumes a problem with a **sequential fraction $f$** and a **parallel fraction $(1-f)$**. The total execution time on a single processor is normalized to **1 unit**.\n\n**Step-by-step operational logic:**\n\n1. **Sequential portion execution time:** $T_{\\text{seq}} = f \\cdot 1 = f$ (cannot be reduced, runs on 1 processor)\n2. **Parallel portion execution time on $p$ processors:** $T_{\\text{par}} = \\frac{1-f}{p}$ (parallelizable work divided among $p$ processors)\n3. **Total parallel execution time:** $T_p = f + \\frac{1-f}{p}$\n4. **Speedup:** $S_p = \\frac{T_s}{T_p} = \\frac{1}{f + \\frac{1-f}{p}}$\n\n**The 'Why' behind each step:**\n\n- The sequential fraction $f$ is **atomic** — it cannot benefit from parallelism, so its contribution remains $f$ regardless of $p$.\n- The parallel fraction $(1-f)$ is **divided equally** across $p$ processors (ideal load balance assumption).\n- As $p \\to \\infty$, the term $\\frac{1-f}{p} \\to 0$, giving $S_{\\text{max}} = \\frac{1}{f}$.\n\n**Engineering Utility:** Amdahl's Law guides **code optimization strategy** — it tells designers to focus efforts on reducing the sequential bottleneck. A program with $f = 0.05$ (5% sequential) has a maximum speedup of **20x** even on infinite processors.\n\n---\n\n## 2.2 Deep Dive: Gustafson's Law — Scaled Workload Analysis\n\nGustafson's Law **reframes the problem**: instead of solving a *fixed* problem faster, we solve a **larger problem** in the *same wall-clock time*.\n\n**Operational logic:**\n\n1. Let the **scaled parallel runtime** be $T_p = a + b$, where $a$ = sequential time, $b$ = parallel time on 1 processor.\n2. On $p$ processors, the parallel portion runs as $\\frac{b}{p}$, so total time becomes: $T_p' = a + \\frac{b}{p}$\n3. For the **scaled workload** (problem size grows with $p$), the sequential time is $a$ and parallel time is $b \\cdot p$ (since $p$ times more work is done).\n4. Speedup: $S_{\\text{Gustafson}} = \\frac{a + b \\cdot p}{a + b}$\n5. Letting $f = \\frac{a}{a+b}$ (serial fraction), we obtain: $S_{\\text{Gustafson}} = p - f(p-1)$\n\n**The 'Why':** Gustafson observed that real HPC users don't run a fixed problem on more machines — they **run larger problems** to leverage the additional compute. In this regime, the serial fraction becomes a *small overhead*, not a *limiting bottleneck*.\n\n---\n\n## 2.3 Deep Dive: Karp-Flatt Metric — Diagnostic Analysis\n\nThe Karp-Flatt metric **exposes hidden inefficiencies** that Amdahl's Law alone cannot detect.\n\n**Derivation logic:**\n\n1. Rearrange Amdahl's formula: $\\frac{1}{S_p} = f + \\frac{(1-f)}{p}$\n2. Solve for $f$ (the experimentally observed serial fraction $e$):\n\n$$e = \\frac{\\frac{1}{S_p} - \\frac{1}{p}}{1 - \\frac{1}{p}}$$\n\n3. **Interpretation guide:**\n   - If $e$ is **constant** as $p$ varies → serial code is the bottleneck → **optimize sequential code**\n   - If $e$ **decreases** as $p$ increases → overhead per processor (communication, synchronization) dominates → **optimize parallel overhead**\n   - If $e$ **increases** as $p$ increases → load imbalance or memory contention → **improve load balancing**\n\n**Engineering Utility:** Used in tools like **TAU (Tuning and Analysis Utilities)** and **Scalasca** for parallel performance analysis on systems like the **PARAM** series (India's national HPC).\n\n---\n\n## 2.4 Deep Dive: Isoefficiency Analysis\n\n**Definition:** Isoefficiency $\\Theta(p)$ is the **rate at which problem size $W$ must grow** with $p$ to maintain a fixed efficiency $E$.\n\n$$W = K \\cdot \\frac{T_o(p)}{1 - E}$$\n\nWhere $K$ is a constant, $T_o(p)$ is the **total overhead** of parallel execution, and $E$ is the target efficiency.\n\n**Interpretation:**\n- **Low isoefficiency slope** → highly scalable algorithm\n- **High isoefficiency slope** → poorly scalable algorithm (requires exponential $W$ growth to keep efficiency constant)\n\n---\n\n## 2.5 KTU Formula Sheet / Cheat Sheet\n\n| # | Formula | Meaning | KTU Use Case |\n|---|---------|---------|--------------|\n| 1 | $S_p = \\frac{T_s}{T_p}$ | Speedup definition | Direct calculation |\n| 2 | $E_p = \\frac{S_p}{p}$ | Efficiency definition | Direct calculation |\n| 3 | $C_p = p \\cdot T_p$ | Cost (processor-time product) | Cost-optimality check |\n| 4 | $S_{\\text{Amdahl}} = \\frac{1}{f + \\frac{1-f}{p}}$ | Amdahl's Law | Max speedup estimation |\n| 5 | $\\lim_{p \\to \\infty} S_{\\text{Amdahl}} = \\frac{1}{f}$ | Amdahl's upper bound | Theoretical limit |\n| 6 | $S_{\\text{Gustafson}} = p - f(p-1)$ | Gustafson's Law | Scaled workload |\n| 7 | $e = \\frac{\\frac{1}{S_p} - \\frac{1}{p}}{1 - \\frac{1}{p}}$ | Karp-Flatt metric | Diagnose inefficiency |\n| 8 | $W = K \\cdot \\frac{T_o(p)}{1 - E}$ | Isoefficiency | Scalability analysis |\n| 9 | $\\text{Flops} = \\frac{\\text{Operations}}{T_p}$ | Throughput / Performance | Benchmarking |\n| 10 | $T_{\\text{comm}} = T_{\\text{startup}} + n \\cdot T_{\\text{data}}$ | Communication time model | MPI/OpenMP analysis |\n\n> [!IMPORTANT]\n> **KTU Examiner's Tip:** For 14-mark Part B questions, **always** quote the formula first, define each variable in your own words, then plug in numerical values. A clear 3-step structure (formula → substitution → result) fetches full marks.\n\n---\n\n## 2.6 Real-World Engineering Utility\n\n| Concept | Industry Application |\n|---------|---------------------|\n| **Amdahl's Law** | CPU multi-core design, GPU shader core allocation, compiler auto-parallelization limits |\n| **Gustafson's Law** | Exascale computing (frontier, Fugaku), Big Data analytics scaling |\n| **Karp-Flatt** | Performance debugging in MPI applications, HPC cluster optimization |\n| **Isoefficiency** | Algorithm design for petascale systems (CRAY, PARAM Siddhi) |\n| **Heterogeneous Computing** | Mobile SoCs (Snapdragon), AI accelerators (TPU, NVIDIA Grace-Hopper) |\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Exhaustive Derivation: Amdahl's Law\n\n**Given:** A parallel program with sequential fraction $f$ and parallel fraction $(1-f)$, executed on $p$ processors. Sequential runtime is $T_s$.\n\n**Step 1: Decompose the sequential runtime.**\n\nThe total sequential runtime is partitioned into:\n- Sequential portion: $T_{\\text{seq}} = f \\cdot T_s$\n- Parallel portion: $T_{\\text{par}} = (1-f) \\cdot T_s$\n\n**Step 2: Analyze parallel execution on $p$ processors.**\n\nThe sequential portion **cannot be parallelized**, so it remains:\n$$T_{\\text{seq, parallel}} = f \\cdot T_s$$\n\nThe parallel portion is **divided among $p$ processors** (assuming perfect load balance):\n$$T_{\\text{par, parallel}} = \\frac{(1-f) \\cdot T_s}{p}$$\n\n**Step 3: Sum the two components to get total parallel execution time $T_p$.**\n\n$$\n\\begin{aligned}\nT_p &= T_{\\text{seq, parallel}} + T_{\\text{par, parallel}} \\\\\n&= f \\cdot T_s + \\frac{(1-f) \\cdot T_s}{p} \\\\\n&= T_s \\cdot \\left( f + \\frac{1-f}{p} \\right)\n\\end{aligned}\n$$\n\n*Logic note:* We assume the sequential portion remains unchanged because splitting a single thread across multiple processors introduces synchronization overhead that often negates any gains — hence it is treated as atomic.\n\n**Step 4: Apply the speedup definition.**\n\n$$\n\\begin{aligned}\nS_p &= \\frac{T_s}{T_p} \\\\\n&= \\frac{T_s}{T_s \\cdot \\left( f + \\frac{1-f}{p} \\right)} \\\\\n&= \\frac{1}{f + \\frac{1-f}{p}}\n\\end{aligned}\n$$\n\n*Logic note:* $T_s$ cancels out cleanly, leaving a dimensionless speedup ratio that depends only on $f$ and $p$.\n\n**Step 5: Compute the asymptotic limit as $p \\to \\infty$.**\n\n$$\n\\begin{aligned}\n\\lim_{p \\to \\infty} S_p &= \\lim_{p \\to \\infty} \\frac{1}{f + \\frac{1-f}{p}} \\\\\n&= \\frac{1}{f + 0} \\\\\n&= \\frac{1}{f}\n\\end{aligned}\n$$\n\n*Logic note:* As $p$ grows infinitely large, the parallel portion's time contribution vanishes, leaving only the sequential bottleneck as the *hard ceiling* on speedup.\n\n---\n\n## 3.2 Exhaustive Derivation: Gustafson's Law\n\n**Given:** A problem with serial time $a$ and parallel time $b$ on a *single* processor. The scaled workload is executed on $p$ processors.\n\n**Step 1: Express sequential runtime on 1 processor.**\n\n$$T_s = a + b$$\n\n*Logic note:* $a$ is the fixed setup cost, $b$ is the parallelizable work on one processor.\n\n**Step 2: Express parallel runtime on $p$ processors.**\n\nThe serial portion remains $a$ (cannot be split). The parallel portion is divided across $p$ processors:\n$$T_p = a + \\frac{b}{p}$$\n\n*Logic note:* This is the runtime to solve the *original* problem on $p$ processors.\n\n**Step 3: Model the *scaled* problem on $p$ processors.**\n\nThe user keeps the runtime fixed at $a + b$ (the original sequential time) but **increases the parallel work** to fill $p$ processors. The new parallel work is $b \\cdot p$ (proportional to processors).\n\n$$T_{\\text{scaled, p}} = a + b$$\n\n*Logic note:* The user solves a problem $p$ times larger in the same wall-clock time.\n\n**Step 4: Compute the scaled speedup.**\n\n$$\n\\begin{aligned}\nS_{\\text{Gustafson}} &= \\frac{\\text{Work done on } p \\text{ processors in time } T_p}{\\text{Work done on 1 processor in time } T_s} \\\\\n&= \\frac{a + b \\cdot p}{a + b}\n\\end{aligned}\n$$\n\n*Logic note:* Numerator is the *scaled* workload; denominator is the *original* workload.\n\n**Step 5: Express in terms of serial fraction $f$.**\n\nLet $f = \\frac{a}{a+b}$ be the serial fraction. Then:\n$$\n\\begin{aligned}\nS_{\\text{Gustafson}} &= \\frac{a + b \\cdot p}{a + b} \\\\\n&= \\frac{a}{a+b} + \\frac{b \\cdot p}{a+b} \\\\\n&= f + (1-f) \\cdot p \\\\\n&= p - f \\cdot (p - 1)\n\\end{aligned}\n$$\n\n*Logic note:* This is the canonical form tested in KTU examinations.\n\n---\n\n## 3.3 Exhaustive Numerical Example: Speedup & Efficiency Calculation\n\n**Problem [KTU Pattern]:** A parallel program takes $T_s = 100$ seconds on a single processor. The serial fraction is $f = 0.10$ (10% sequential). Calculate the speedup, efficiency, and cost for $p = 4$, $p = 16$, and $p = 64$.\n\n**Step 1: Apply Amdahl's Law for $p = 4$.**\n\n$$\n\\begin{aligned}\nS_4 &= \\frac{1}{0.10 + \\frac{1-0.10}{4}} \\\\\n&= \\frac{1}{0.10 + \\frac{0.90}{4}} \\\\\n&= \\frac{1}{0.10 + 0.225} \\\\\n&= \\frac{1}{0.325} \\\\\n&\\approx 3.077\n\\end{aligned}\n$$\n\n*Logic note:* With 4 processors and 10% serial code, we get ~3x speedup (not 4x) due to the sequential bottleneck.\n\n**Step 2: Efficiency for $p = 4$.**\n\n$$E_4 = \\frac{S_4}{4} = \\frac{3.077}{4} \\approx 0.769 \\text{ or } 76.9\\%$$\n\n**Step 3: Repeat for $p = 16$.**\n\n$$\n\\begin{aligned}\nS_{16} &= \\frac{1}{0.10 + \\frac{0.90}{16}} \\\\\n&= \\frac{1}{0.10 + 0.05625} \\\\\n&= \\frac{1}{0.15625} \\\\\n&= 6.40\n\\end{aligned}\n$$\n\n$$E_{16} = \\frac{6.40}{16} = 0.40 \\text{ or } 40\\%$$\n\n**Step 4: Repeat for $p = 64$.**\n\n$$\n\\begin{aligned}\nS_{64} &= \\frac{1}{0.10 + \\frac{0.90}{64}} \\\\\n&= \\frac{1}{0.10 + 0.01406} \\\\\n&= \\frac{1}{0.11406} \\\\\n&\\approx 8.768\n\\end{aligned}\n$$\n\n$$E_{64} = \\frac{8.768}{64} \\approx 0.137 \\text{ or } 13.7\\%$$\n\n**Step 5: Compare with Gustafson's Law (scaled workload).**\n\nFor $p = 64$, $f = 0.10$:\n$$S_{\\text{Gustafson}} = 64 - 0.10 \\cdot (64 - 1) = 64 - 6.3 = 57.7$$\n\n*Logic note:* Gustafson's Law shows that for **scaled problems**, 64 processors can yield ~57.7x speedup, while Amdahl's Law (fixed problem) caps at ~8.77x. This contrast is a **favourite KTU question**.\n\n---\n\n## 3.4 Exhaustive Karp-Flatt Metric Calculation\n\n**Given:** Observed speedups: $S_4 = 3.077$, $S_{16} = 6.40$, $S_{64} = 8.768$.\n\n**For $p = 16$:**\n\n$$\n\\begin{aligned}\ne &= \\frac{\\frac{1}{S_{16}} - \\frac{1}{16}}{1 - \\frac{1}{16}} \\\\\n&= \\frac{\\frac{1}{6.40} - 0.0625}{1 - 0.0625} \\\\\n&= \\frac{0.15625 - 0.0625}{0.9375} \\\\\n&= \\frac{0.09375}{0.9375} \\\\\n&= 0.10\n\\end{aligned}\n$$\n\n*Logic note:* The Karp-Flatt serial fraction $e = 0.10$ matches our assumption $f = 0.10$, confirming serial code is the dominant inefficiency source.\n\n---\n\n## 3.5 Analytical Matrix: Performance Law Comparison\n\n| Aspect | Amdahl's Law | Gustafson's Law | Karp-Flatt Metric |\n|--------|--------------|-----------------|-------------------|\n| **Problem Size** | Fixed (W = constant) | Scaled (W grows with p) | Fixed (diagnostic for fixed W) |\n| **Speedup Upper Bound** | $S_{\\text{max}} = \\frac{1}{f}$ | $S_{\\text{max}} = p$ (linear) | N/A (diagnostic) |\n| **Key Insight** | Sequential code is the bottleneck | Sequential cost amortized over larger work | Identifies source of inefficiency |\n| **Best Use** | Predicting limits of parallelism | Justifying HPC investment | Debugging parallel programs |\n| **Assumption** | Perfect load balance, no overhead | Constant serial runtime | Experimentally measured $S_p$ |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Mermaid Diagram: HPC Performance Analysis Workflow\n\n```mermaid\nflowchart TD\n    A[Start: HPC Application] --> B{Measured Runtime Ts Tp}\n    B --> C[Compute Speedup Sp = Ts / Tp]\n    C --> D{Is Sp approximately equal to p?}\n    D -- Yes --> E[Linear Speedup: Ideal Parallelism]\n    D -- No --> F[Compute Efficiency Ep = Sp / p]\n    F --> G{Is Efficiency greater than 0.5?}\n    G -- Yes --> H[Good Scalability]\n    G -- No --> I[Apply Karp-Flatt Metric]\n    I --> J[Compute Serial Fraction e]\n    J --> K{Is e constant as p varies?}\n    K -- Yes --> L[Inherently Sequential Code: Optimize f]\n    K -- No --> M[Overhead Dominant: Optimize Communication]\n    L --> N[Apply Amdahl Limit: Smax = 1/f]\n    M --> O[Apply Gustafson Law: Sgust = p - f p-1]\n    N --> P[End: Performance Report]\n    O --> P\n    H --> P\n    E --> P\n```\n\n## 4.2 Mermaid Diagram: Heterogeneous HPC Architecture\n\n```mermaid\nflowchart LR\n    CPU[CPU Host Processor] --> PCIe[PCIe Bus]\n    PCIe --> GPU1[GPU Device 0]\n    PCIe --> GPU2[GPU Device 1]\n    PCIe --> FPGA[FPGA Accelerator]\n    CPU --> DDR[Host DDR Memory]\n    GPU1 --> VRAM1[Device VRAM 0]\n    GPU2 --> VRAM2[Device VRAM 1]\n    FPGA --> BRAM[Block RAM]\n    CPU -- Control Commands --> GPU1\n    CPU -- Data Transfer --> DDR\n    GPU1 -- Kernel Execution --> VRAM1\n    GPU2 -- Parallel Compute --> VRAM2\n    FPGA -- Custom Logic --> BRAM\n```\n\n## 4.3 Mermaid Diagram: Speedup Curves Comparison (Amdahl vs. Gustafson)\n\n```mermaid\ngraph TD\n    subgraph Amdahl Curve\n    A1[Fixed problem: f = 0.10] --> A2[S4 approximately 3.08]\n    A1 --> A3[S16 approximately 6.40]\n    A1 --> A4[S64 approximately 8.77]\n    A1 --> A5[Smax = 1/f = 10x]\n    end\n    subgraph Gustafson Curve\n    G1[Scaled problem: f = 0.10] --> G2[S4 approximately 3.7]\n    G1 --> G3[S16 approximately 14.5]\n    G1 --> G4[S64 approximately 57.7]\n    G1 --> G5[Linear growth: Sp = p]\n    end\n```\n\n## 4.4 Mermaid Diagram: Karp-Flatt Diagnostic Decision Tree\n\n```mermaid\nflowchart TD\n    Start[Run Parallel Program on varying p] --> Measure[Measure Speedup Sp for each p]\n    Measure --> Compute[Compute Karp-Flatt e for each p]\n    Compute --> Check{Trend of e vs p}\n    Check -- Constant --> Serial[Serial code dominant: Refactor sequential parts]\n    Check -- Decreasing --> Overhead[Parallel overhead dominant: Reduce communication]\n    Check -- Increasing --> Load[Load imbalance dominant: Improve task distribution]\n    Serial --> Optimize1[Apply Amdahl bound analysis]\n    Overhead --> Optimize2[Profile MPI and OpenMP regions]\n    Load --> Optimize3[Apply dynamic load balancing]\n    Optimize1 --> Result[Optimization complete]\n    Optimize2 --> Result\n    Optimize3 --> Result\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Question 1 [KTU University Exam - May 2024]\n\n**Q: Define Amdahl's Law. State its significance in parallel computing.**\n\n**Model Answer (3 marks):**\n\n> [!NOTE]\n> Amdahl's Law, formulated by **Gene Amdahl in 1967**, states that the maximum speedup $S_p$ achievable by a parallel program on $p$ processors is given by:\n> $$S_p = \\frac{1}{f + \\frac{1-f}{p}}$$\n> where $f$ is the **fraction of the program that is inherently sequential** and $(1-f)$ is the parallelizable fraction.\n>\n> **Significance (2 marks):**\n> 1. It establishes a **theoretical upper bound** on the speedup of any parallel program, highlighting that the sequential portion becomes the dominant bottleneck as the number of processors increases.\n> 2. It guides **code optimization strategy** — designers must minimize the serial fraction $f$ to extract maximum performance from parallel hardware.\n> 3. As $p \\to \\infty$, $S_p \\to \\frac{1}{f}$, demonstrating that **infinite processors cannot overcome sequential bottlenecks**.\n\n---\n\n### Question 2 [KTU University Exam - Dec 2023]\n\n**Q: Distinguish between strong scaling and weak scaling in HPC.**\n\n**Model Answer (3 marks):**\n\n| Aspect | Strong Scaling | Weak Scaling |\n|--------|----------------|--------------|\n| **Problem size** | Fixed ($W$ = constant) | Grows with $p$ ($W = O(p)$) |\n| **Metric** | Speedup $S_p$ for constant workload | Speedup $S_p$ for scaled workload |\n| **Goal** | Solve fixed problem *faster* | Solve *larger* problem in same time |\n| **Law governing** | Amdahl's Law | Gustafson's Law |\n| **Practical use** | Benchmarking fixed-size simulations | Exascale data analytics, Big Data |\n\n> **Conclusion:** Strong scaling tests *latency reduction*; weak scaling tests *throughput scalability*. Both are essential for evaluating HPC systems.\n\n---\n\n## 5.2 Part B Questions (14 Marks Each)\n\n### Question A (Choice 1) [KTU University Exam - May 2024]\n\n**Q (a) [7 marks]:** Derive Amdahl's Law for parallel speedup. What is the maximum speedup achievable when the serial fraction is 5%? Comment on the practical implications.\n\n**Model Solution:**\n\n**Derivation (4 marks):**\n\nLet the **sequential execution time** of a program be $T_s = 1$ (normalized). Let $f$ be the **serial fraction** and $(1-f)$ be the **parallel fraction**.\n\nThe parallel execution time on $p$ processors is:\n$$T_p = f \\cdot T_s + \\frac{(1-f) \\cdot T_s}{p} = T_s \\cdot \\left( f + \\frac{1-f}{p} \\right)$$\n\nThe speedup is:\n$$S_p = \\frac{T_s}{T_p} = \\frac{1}{f + \\frac{1-f}{p}}$$\n\n**Maximum speedup calculation (2 marks):**\n\nFor $f = 0.05$:\n$$S_{\\text{max}} = \\lim_{p \\to \\infty} S_p = \\frac{1}{0.05} = 20$$\n\n**Practical implications (1 mark):**\nEven with **infinite processors**, the speedup cannot exceed 20x due to the 5% sequential code. This means HPC architects must aggressively **refactor serial code paths** (e.g., replace sequential I/O with parallel I/O, eliminate global locks).\n\n---\n\n**Q (b) [7 marks]:** A parallel program achieves the following speedups on 2, 4, 8, and 16 processors: $S_2 = 1.80$, $S_4 = 2.91$, $S_8 = 4.00$, $S_{16} = 5.06$. Calculate the Karp-Flatt metric for each case. Identify the dominant source of inefficiency.\n\n**Model Solution:**\n\n**Formula (1 mark):** $e = \\frac{\\frac{1}{S_p} - \\frac{1}{p}}{1 - \\frac{1}{p}}$\n\n**Calculations (4 marks):**\n\nFor $p = 2$:\n$$e_2 = \\frac{\\frac{1}{1.80} - 0.5}{1 - 0.5} = \\frac{0.5556 - 0.5}{0.5} = \\frac{0.0556}{0.5} = 0.1111$$\n\nFor $p = 4$:\n$$e_4 = \\frac{\\frac{1}{2.91} - 0.25}{1 - 0.25} = \\frac{0.3436 - 0.25}{0.75} = \\frac{0.0936}{0.75} = 0.1248$$\n\nFor $p = 8$:\n$$e_8 = \\frac{\\frac{1}{4.00} - 0.125}{1 - 0.125} = \\frac{0.25 - 0.125}{0.875} = \\frac{0.125}{0.875} = 0.1429$$\n\nFor $p = 16$:\n$$e_{16} = \\frac{\\frac{1}{5.06} - 0.0625}{1 - 0.0625} = \\frac{0.1976 - 0.0625}{0.9375} = \\frac{0.1351}{0.9375} = 0.1441$$\n\n**Analysis (2 marks):**\n\n| $p$ | Karp-Flatt $e$ |\n|-----|----------------|\n| 2 | 0.1111 |\n| 4 | 0.1248 |\n| 8 | 0.1429 |\n| 16 | 0.1441 |\n\n**Trend observation:** The serial fraction $e$ is *approximately constant* (~0.12 to 0.14) as $p$ increases. This indicates that the **inherently sequential code** is the dominant source of inefficiency. The program should be optimized by **reducing the serial fraction** (e.g., parallelizing sequential loops, reducing I/O bottlenecks, replacing serial reductions with parallel reductions).\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall:** Many students compute Karp-Flatt for only one value of $p$ and stop. To fetch full marks, you **MUST** compute $e$ for *at least three* values of $p$ and explicitly state whether the trend is **constant, increasing, or decreasing** to identify the bottleneck source.\n\n---\n\n### Question B (Choice 2) [KTU University Exam - Dec 2023]\n\n**Q (a) [7 marks]:** Explain Gustafson's Law. For a parallel program with serial fraction $f = 0.04$ running on $p = 32$ processors, calculate the speedup. How does it differ from Amdahl's Law in interpretation?\n\n**Model Solution:**\n\n**Gustafson's Law Explanation (3 marks):**\n\nGustafson's Law, proposed by **John Gustafson in 1988**, addresses the limitation of Amdahl's Law by considering **scaled workloads** — problems whose size *grows proportionally* with the number of processors.\n\n**Mathematical formulation (2 marks):**\n\n$$S_{\\text{Gustafson}}(p) = p - f \\cdot (p - 1)$$\n\n**Numerical calculation (1 mark):**\n\nFor $f = 0.04$, $p = 32$:\n$$S_{\\text{Gustafson}} = 32 - 0.04 \\cdot (32 - 1) = 32 - 0.04 \\cdot 31 = 32 - 1.24 = 30.76$$\n\n**Comparison with Amdahl (1 mark):**\n\nFor the *same* parameters under Amdahl's Law (fixed problem):\n$$S_{\\text{Amdahl}} = \\frac{1}{0.04 + \\frac{0.96}{32}} = \\frac{1}{0.04 + 0.03} = \\frac{1}{0.07} \\approx 14.29$$\n\nThus, Amdahl predicts **14.29x** (fixed problem) while Gustafson predicts **30.76x** (scaled problem). Gustafson's Law is more **realistic for HPC** because users typically increase problem size to leverage more processors.\n\n---\n\n**Q (b) [7 marks]:** With a neat diagram, explain the architecture of a heterogeneous computing system. Discuss the role of CPU and GPU in such systems with an example.\n\n**Model Solution:**\n\n**Architecture Diagram Description (2 marks):**\n\nA heterogeneous computing system consists of:\n- A **CPU (host)** managing control flow, sequential tasks, and task scheduling.\n- One or more **accelerators** (GPUs, FPGAs, DSPs) handling data-parallel, compute-intensive workloads.\n- A **high-bandwidth interconnect** (PCIe, NVLink) enabling data transfer.\n- **Separate memory hierarchies** — host DDR memory and device VRAM.\n\n**Roles explained (3 marks):**\n\n**CPU Role (Host):**\n- Executes the **sequential portion** of the program.\n- Manages **memory allocation** and **data transfer** to accelerators.\n- Handles **branching, I/O, and OS services**.\n- Schedules **kernel launches** on accelerators.\n\n**GPU Role (Device):**\n- Executes **data-parallel kernels** (e.g., SIMD instructions).\n- Performs **massive floating-point operations** (e.g., matrix multiplication, convolutions).\n- Has **thousands of simple cores** optimized for throughput.\n- Operates on its own **global memory (VRAM)** with high bandwidth.\n\n**Example: Deep Learning Training (2 marks):**\n\nIn training a Convolutional Neural Network (CNN) on **CUDA-enabled hardware**:\n1. The **CPU** loads the dataset, performs data augmentation, and feeds mini-batches to the GPU.\n2. The **GPU** executes the **forward pass** (matrix multiplications, convolutions) using cuDNN libraries.\n3. The **GPU** also computes **gradients** via backpropagation (highly parallel).\n4. The **CPU** synchronizes updates to model weights (Adam, SGD optimizers).\n\nThis division of labor achieves **10-100x speedup** over CPU-only training, making heterogeneous computing the **backbone of modern AI/HPC systems**.\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning\n\n> [!WARNING]\n> **Common Mark-Loss Pitfalls in \"Miscellaneous HPC\" Questions:**\n> 1. **Confusing Amdahl's and Gustafson's Laws:** Always state explicitly whether the problem has a **fixed or scaled workload**. Examiners deduct 2 marks for ambiguity.\n> 2. **Skipping variable definitions:** When writing $S_p = \\frac{1}{f + \\frac{1-f}{p}}$, you **MUST** define $f$ (serial fraction) and $p$ (number of processors) before substituting values. A formula without variable definitions loses 1 mark.\n> 3. **Ignoring units in efficiency:** Efficiency must be expressed as a *decimal* (0 < E < 1) or *percentage* (0% < E < 100%). Writing $E = 0.5$ without units or $E = 50\\%$ is acceptable; writing $E = 50$ alone is **wrong**.\n> 4. **Karp-Flatt without trend analysis:** Computing $e$ for one $p$ is insufficient. You need **at least three data points** to identify the bottleneck type.\n> 5. **Heterogeneous computing diagrams:** Students often draw the CPU and GPU as separate boxes without showing the **interconnect** (PCIe/NVLink) and **memory hierarchy**. This loses 2-3 marks.\n> 6. **Forgetting the asymptotic limit of Amdahl's Law:** Always mention $\\lim_{p \\to \\infty} S_p = \\frac{1}{f}$ in long-answer questions.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n### 🚀 Rapid Revision Checklist\n\n- [x] **Speedup $S_p$** = ratio of sequential to parallel runtime; ideal value is $p$.\n- [x] **Efficiency $E_p$** = $\\frac{S_p}{p}$; ideal value is 1 (or 100%).\n- [x] **Cost $C_p$** = $p \\cdot T_p$; cost-optimality requires $C_p = \\Theta(T_s)$.\n- [x] **Amdahl's Law** = $\\frac{1}{f + \\frac{1-f}{p}}$; applies to **fixed workload**; upper bound is $\\frac{1}{f}$.\n- [x] **Gustafson's Law** = $p - f(p-1)$; applies to **scaled workload**; near-linear speedup.\n- [x] **Karp-Flatt metric** = $\\frac{1/S_p - 1/p}{1 - 1/p}$; diagnoses if bottleneck is **serial code, overhead, or load imbalance**.\n- [x] **Isoefficiency** = $W = K \\cdot \\frac{T_o(p)}{1-E}$; lower slope = better scalability.\n- [x] **Strong scaling** = fixed $W$, increasing $p$; **Weak scaling** = $W \\propto p$.\n- [x] **Heterogeneous computing** = CPU + GPU/FPGA cooperation; CPU for control, GPU for data-parallel kernels.\n- [x] **Communication model** = $T_{\\text{comm}} = t_s + n \\cdot t_w$ (startup latency + data transfer time).\n\n### 📌 Key Numerical Values to Memorize\n\n| $f$ | Amdahl's $S_{\\text{max}} = 1/f$ | Gustafson $S_{64}$ |\n|-----|-------------------------------|---------------------|\n| 0.01 | 100x | 63.37x |\n| 0.05 | 20x | 60.85x |\n| 0.10 | 10x | 57.70x |\n| 0.25 | 4x | 48.25x |\n| 0.50 | 2x | 32.50x |\n\n> [!IMPORTANT]\n> **Final Exam Tip:** For a 14-mark Part B question on this topic, structure your answer as: **(1) Definition with formula → (2) Numerical substitution with units → (3) Result interpretation → (4) Comparison with related laws.** This 4-step structure consistently fetches 12-14 marks in KTU valuation.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "casestudyopenmpparalleljacobialgorithm": {
      "title": "Case Study: OpenMP-Parallel Jacobi Algorithm",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 3\n> - **Topic:** Case Study: OpenMP-Parallel Jacobi Algorithm\n\n<!-- SECTION_1_START -->\n# Module 3 — Case Study: OpenMP-Parallel Jacobi Algorithm\n\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n> [!NOTE]\n> **Jacobi Iterative Method (Definition):**\n> The Jacobi method is a stationary iterative numerical algorithm used to solve a square system of linear equations $A\\vec{x} = \\vec{b}$ by repeatedly refining an initial guess $\\vec{x}^{(0)}$ using the formula:\n> $$\\vec{x}^{(k+1)} = D^{-1}\\left(\\vec{b} - (L+U)\\vec{x}^{(k)}\\right)$$\n> where $A = D + L + U$ is decomposed into **diagonal ($D$)**, **strictly lower-triangular ($L$)**, and **strictly upper-triangular ($U$)** components.\n\n> [!IMPORTANT]\n> **OpenMP-Parallel Jacobi (Definition):**\n> An SMP (Symmetric Multiprocessing) realization of the Jacobi method where computational work over the row dimension is **statically or dynamically partitioned** across multiple threads (workers) of a shared-memory multicore node. The dominant parallel construct is the `for` work-sharing directive (`#pragma omp parallel for`) which assigns rows of the iterate vector $\\vec{x}$ to distinct threads.\n\n### 1.2 Conceptual Analogy / Intuition\n\nImagine a **large rectangular metal plate** placed on a grid, and each cell of the grid has an initial temperature. The plate is in contact with a heat source/sink on its boundary, and you wish to find the *steady-state* temperature at every interior cell.\n\n**The Jacobi update rule is exactly Newton's law of cooling discretized:**\n\n> \"Each cell's new temperature = (sum of its four neighbours' *previous* temperatures) / 4\"\n\nWhy does it parallelize beautifully? Because the **new value of every cell depends only on OLD values from the previous iteration**. No cell ever looks at a neighbour's value from the *current* iteration. This is the *algorithmic independence* that makes Jacobi **embarrassingly parallel at the per-row granularity**.\n\n- A *sequential* simulation is one person with one calculator going cell-by-cell.\n- A *parallel* simulation is **8 workers** each owning a horizontal slab of the plate, **simultaneously** updating their assigned cells using a *frozen snapshot* of last iteration's temperatures.\n\n### 1.3 Key Engineering Constants & Metrics\n\n- **Number of unknowns:** $n$ (size of matrix $A$)\n- **Number of threads:** $p$ (OpenMP team size, $\\leq$ `# of physical cores`)\n- **Convergence tolerance:** $\\epsilon$ (typically $10^{-6}$)\n- **Diagonal dominance ratio:** $\\rho = \\max_i \\frac{\\sum_{j \\neq i} \\vert a_{ij} \\vert}{\\vert a_{ii} \\vert}$\n- **Speedup (Amdahl's law ceiling):** $S_p^{\\max} = \\dfrac{1}{f_s + \\dfrac{f_p}{p}}$ where $f_s$ = serial fraction.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Algorithmic Decomposition of $A$\n\nGiven an $n \\times n$ real matrix $A$, the Jacobi method requires the *diagonal dominance* condition (a sufficient, not necessary, condition for convergence):\n\n$$\\vert a_{ii} \\vert > \\sum_{j=1, j \\neq i}^{n} \\vert a_{ij} \\vert, \\quad \\forall i = 1, 2, \\dots, n$$\n\nThe matrix is split as:\n\n$$\nA = D + L + U \\quad \\text{where} \\quad\nD = \\begin{pmatrix} a_{11} & & & \\\\ & a_{22} & & \\\\ & & \\ddots & \\\\ & & & a_{nn} \\end{pmatrix}\n$$\n\nThe update for the $i^{\\text{th}}$ component of $\\vec{x}$ at iteration $(k+1)$ is:\n\n$$x_i^{(k+1)} = \\frac{1}{a_{ii}} \\left( b_i - \\sum_{j=1, j \\neq i}^{n} a_{ij} \\, x_j^{(k)} \\right)$$\n\n## 2.2 The \"Why\" Behind the Parallelism\n\n> [!IMPORTANT]\n> **The fundamental reason Jacobi is parallelizable:**\n> The new value $x_i^{(k+1)}$ depends **exclusively on $x_j^{(k)}$** (the previous iteration). It does **NOT** read from $x_i^{(k)}$ or any $x_m^{(k+1)}$ for $m \\neq i$. Therefore, *all* $n$ updates at iteration $(k+1)$ can occur **concurrently** without data races on a *write-write* or *read-write* conflict for the output vector — provided **two separate buffers** are used and roles are swapped per iteration.\n\n## 2.3 OpenMP Parallelization Strategy\n\nThere are **three canonical strategies** in KTU-scope questions:\n\n| Strategy | Construct | Granularity | Notes |\n|----------|-----------|-------------|-------|\n| **Row-striped domain decomposition** | `#pragma omp parallel for private(i,j,sum)` | Row-wise | Most common; static or schedule(static) |\n| **Block domain decomposition** | `schedule(dynamic, blk)` | Contiguous slabs | Useful when load is uneven |\n| **Collapse-2 over a 2-D grid (Laplace)** | `#pragma omp parallel for collapse(2)` | Cell-wise | Common in PDE / heat equation form |\n\n## 2.4 KTU High-Yield Formula Sheet\n\n| # | Concept | Formula / Expression | Units / Notes |\n|---|---------|----------------------|---------------|\n| 1 | Jacobi update (component form) | $x_i^{(k+1)} = \\dfrac{b_i - \\sum_{j \\neq i} a_{ij} x_j^{(k)}}{a_{ii}}$ | Scalar, dimensionless |\n| 2 | Matrix-form update | $\\vec{x}^{(k+1)} = D^{-1}(\\vec{b} - (L+U)\\vec{x}^{(k)})$ | Requires $D$ invertible |\n| 3 | Convergence (diagonal dominance) | $\\vert a_{ii} \\vert > \\sum_{j \\neq i} \\vert a_{ij} \\vert$ | Per-row sufficient condition |\n| 4 | Spectral convergence test | $\\rho(I - D^{-1}A) < 1$ | Spectral radius $< 1$ |\n| 5 | Stopping criterion (L2 norm) | $\\Vert \\vec{x}^{(k+1)} - \\vec{x}^{(k)} \\Vert_2 < \\epsilon$ | $\\epsilon = 10^{-6}$ typical |\n| 6 | Parallel time model | $T_p \\approx \\dfrac{T_1}{p} + T_{sync}$ | $T_{sync}$ = barrier + reduction |\n| 7 | Speedup | $S_p = \\dfrac{T_1}{T_p}$ | $S_p \\leq p$ (Amdahl bound) |\n| 8 | Efficiency | $E_p = \\dfrac{S_p}{p} \\times 100\\%$ | Target $\\geq 70\\%$ for healthy scaling |\n| 9 | Load imbalance penalty | $\\tau_{imb} = \\dfrac{p \\cdot T_{max\\_chunk} - T_1}{T_1}$ | Non-zero when $n \\nmid p$ |\n| 10 | Total work (matrix size $n$) | $W = n^2$ (mults) per iteration | Doubles for dense vs $5n$ for 5-pt stencil |\n| 11 | Overhead cost per barrier | $T_b \\approx \\alpha \\log p$ | $\\alpha$ = synchronization constant |\n| 12 | Iteration count bound | $k \\geq \\dfrac{\\log(\\epsilon / \\Vert \\vec{x}^{(1)} - \\vec{x}^{(0)} \\Vert)}{\\log \\rho}$ | Geometric convergence |\n\n> [!NOTE]\n> In KTU board papers, examiners **frequently** ask: *\"Why is the Jacobi method preferred over Gauss-Seidel for parallelization?\"* The answer is precisely because **Gauss-Seidel uses updated values within the same iteration**, introducing a true *data dependency* that destroys row-level parallelism unless explicit pipeline/colouring tricks are used.\n\n## 2.5 Real-World Engineering Utility\n\n- **Computational Fluid Dynamics (CFD):** Pressure-Poisson equation in incompressible Navier-Stokes.\n- **Structural Engineering:** Finite Element Analysis (FEA) for linear elasticity.\n- **VLSI Thermal Modeling:** Steady-state heat map of an integrated circuit.\n- **Image Processing:** Discrete Poisson image editing (Pérez et al., 2003).\n- **Power Systems:** Load-flow studies on Y-bus matrices of national grids.\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation of the Jacobi Update Formula\n\n**Starting point:** Solve the linear system\n\n$$A\\vec{x} = \\vec{b}, \\quad A \\in \\mathbb{R}^{n \\times n}, \\quad \\vec{b} \\in \\mathbb{R}^{n}$$\n\n**Step 1:** Decompose $A$ into three parts.\n\n$$A = D + L + U$$\n\n$D$ is diagonal, $L$ is strictly lower triangular, $U$ is strictly upper triangular.\n\n**Step 2:** Substitute into the system.\n\n$$(D + L + U)\\vec{x} = \\vec{b}$$\n\n**Step 3:** Isolate the diagonal term on one side.\n\n$$D\\vec{x} = \\vec{b} - (L + U)\\vec{x}$$\n\n**Step 4:** Pre-multiply by $D^{-1}$ (assumes $\\det D \\neq 0$, i.e., no zero pivot).\n\n$$\\vec{x} = D^{-1}\\vec{b} - D^{-1}(L + U)\\vec{x}$$\n\n**Step 5:** Recognize this is a **fixed-point iteration** with operator $M_J = -D^{-1}(L+U)$ and constant $c = D^{-1}\\vec{b}$.\n\n$$\\vec{x}^{(k+1)} = -D^{-1}(L+U)\\vec{x}^{(k)} + D^{-1}\\vec{b}$$\n\n**Step 6:** Extract the $i^{\\text{th}}$ scalar row. Since $D^{-1}$ is diagonal with $1/a_{ii}$ on the diagonal, the $i^{\\text{th}}$ component is:\n\n$$\n\\begin{aligned}\nx_i^{(k+1)} &= -\\frac{1}{a_{ii}} \\left( \\sum_{j=1}^{i-1} a_{ij} x_j^{(k)} + \\sum_{j=i+1}^{n} a_{ij} x_j^{(k)} \\right) + \\frac{b_i}{a_{ii}} \\\\[6pt]\n&= \\frac{1}{a_{ii}} \\left( b_i - \\sum_{j \\neq i} a_{ij} x_j^{(k)} \\right)\n\\end{aligned}\n$$\n\nThis is the **canonical Jacobi update** taught in every KTU Numerical Methods module.\n\n## 3.2 Derivation of Parallel Speedup Bound (Amdahl)\n\n**Step 1:** Decompose total sequential execution time $T_1$ into a serial portion $T_s$ and a parallelizable portion $T_p^{(1)}$.\n\n$$T_1 = T_s + T_p^{(1)}$$\n\n**Step 2:** With $p$ threads, the parallel portion takes $T_p^{(1)}/p$ (ideal), and the serial portion is unchanged.\n\n$$T_p = T_s + \\frac{T_p^{(1)}}{p} + T_{overhead}(p)$$\n\nwhere $T_{overhead}(p)$ includes OpenMP fork/join, barriers, reductions, and false sharing.\n\n**Step 3:** Define the serial fraction $f_s = T_s / T_1$.\n\n$$T_p = T_1 \\left( f_s + \\frac{1 - f_s}{p} \\right) + T_{overhead}(p)$$\n\n**Step 4:** Speedup is therefore:\n\n$$\nS_p = \\frac{T_1}{T_p} = \\frac{1}{f_s + \\dfrac{1 - f_s}{p} + \\dfrac{T_{overhead}(p)}{T_1}}\n$$\n\n**Step 5:** As $p \\to \\infty$:\n\n$$S_\\infty = \\frac{1}{f_s + \\dfrac{T_{overhead}}{T_1}} \\quad \\text{(practical Amdahl ceiling)}$$\n\n> [!NOTE]\n> For Jacobi, $f_s$ is *small* (just the convergence check + vector swap), so ideal $S_p$ can be very close to $p$. However, the *reduction* on the residual norm can become a bottleneck for $p > 32$.\n\n## 3.3 Derivation of the Row-Striped Workload\n\nFor an $n \\times n$ matrix on $p$ threads using static scheduling:\n\n$$\n\\begin{aligned}\n\\text{Rows per thread} &= \\left\\lceil \\frac{n}{p} \\right\\rceil \\\\[4pt]\n\\text{Last thread handles} &= n - (p-1) \\left\\lceil \\frac{n}{p} \\right\\rceil \\text{ rows} \\\\[4pt]\n\\text{FLOPs per iteration per thread} &\\approx 2n \\left\\lceil \\frac{n}{p} \\right\\rceil \\\\[4pt]\n\\text{Load imbalance} &\\leq \\left\\lceil \\frac{n}{p} \\right\\rceil \\text{ rows}\n\\end{aligned}\n$$\n\nIf $n$ is divisible by $p$, the imbalance is exactly zero and **perfect load balance** is achieved.\n\n## 3.4 Worked Numerical Example (Mini-Worked)\n\nLet $n = 4$, $p = 2$, and consider the diagonally dominant system:\n\n$$\nA = \\begin{pmatrix} 4 & -1 & 0 & 0 \\\\ -1 & 4 & -1 & 0 \\\\ 0 & -1 & 4 & -1 \\\\ 0 & 0 & -1 & 4 \\end{pmatrix}, \\quad \\vec{b} = \\begin{pmatrix} 1 \\\\ 2 \\\\ 3 \\\\ 4 \\end{pmatrix}\n$$\n\n**Thread 0** owns rows 0,1; **Thread 1** owns rows 2,3. Initial $\\vec{x}^{(0)} = \\vec{0}$.\n\n**Iteration 1 (parallel execution):**\n\n- Thread 0:\n  - $x_0^{(1)} = (1 - (-1)(0) - 0) / 4 = 0.250$\n  - $x_1^{(1)} = (2 - (-1)(0) - (-1)(0)) / 4 = 0.500$\n- Thread 1 (simultaneously):\n  - $x_2^{(1)} = (3 - (-1)(0) - (-1)(0)) / 4 = 0.750$\n  - $x_3^{(1)} = (4 - (-1)(0)) / 4 = 1.000$\n\nThe two threads run **concurrently**, each performing 2 row updates instead of 1 sequential 4-row sweep. This is the **core speedup** mechanism.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Algorithmic Flowchart of Parallel Jacobi\n\n```mermaid\nflowchart TD\n    startA([Start Program]) --> initA[/Read matrix A, vector b, tolerance eps, max_iter/]\n    initA --> guessA[/Initialize x_old to zero vector/]\n    guessA --> iterA[Set k = 0]\n    iterA --> parallelA{Parallel Region p threads}\n    parallelA --> splitA[Distribute rows 0 to n-1 across threads]\n    splitA --> localA[Each thread computes local row updates]\n    localA --> localB[Write to x_new using private buffer]\n    localB --> barrierA[omp barrier - wait for all threads]\n    barrierA --> reduceA[Compute global residual norm reduction]\n    reduceA --> checkA{Norm less than eps OR k greater than max_iter}\n    checkA -- No --> swapA[Swap x_old and x_new pointers]\n    swapA --> incA[k = k + 1]\n    incA --> parallelA\n    checkA -- Yes --> exitA[/Output x_new as solution/]\n    exitA --> stopA([End Program])\n```\n\n## 4.2 Domain Decomposition (Row-Striped) Schematic\n\n```mermaid\nflowchart LR\n    matA[\"Matrix A n by n\"] --> splitB{Domain Decomposer}\n    splitB --> thr0[\"Thread 0 rows 0 to n over p minus 1\"]\n    splitB --> thr1[\"Thread 1 rows n over p to 2n over p minus 1\"]\n    splitB --> thr2[\"Thread 2 rows 2n over p to 3n over p minus 1\"]\n    splitB --> thrP[\"Thread p-1 rows p-1 n over p to n minus 1\"]\n    thr0 --> buf0[\"Private buffer x_new local 0\"]\n    thr1 --> buf1[\"Private buffer x_new local 1\"]\n    thr2 --> buf2[\"Private buffer x_new local 2\"]\n    thrP --> bufP[\"Private buffer x_new local p minus 1\"]\n    buf0 --> gather[\"Shared memory gather step\"]\n    buf1 --> gather\n    buf2 --> gather\n    bufP --> gather\n    gather --> swap2[\"Swap x_old and x_new\"]\n```\n\n## 4.3 SMP Execution Timeline (p = 4 threads)\n\n```mermaid\ngantt\n    title OpenMP Parallel Jacobi Execution Timeline\n    dateFormat  X\n    axisFormat %s\n    section Thread 0\n    Row update 0 to 1     :a0, 0, 4\n    Row update 4 to 5     :a1, 4, 4\n    Barrier and reduce    :crit, 8, 1\n    section Thread 1\n    Row update 1 to 2     :b0, 0, 4\n    Row update 5 to 6     :b1, 4, 4\n    Barrier and reduce    :crit, 8, 1\n    section Thread 2\n    Row update 2 to 3     :c0, 0, 4\n    Row update 6 to 7     :c1, 4, 4\n    Barrier and reduce    :crit, 8, 1\n    section Thread 3\n    Row update 3 to 4     :d0, 0, 4\n    Row update 7 to 8     :d1, 4, 4\n    Barrier and reduce    :crit, 8, 1\n```\n\n## 4.4 Memory Access Pattern (Shared L2/L3 Cache)\n\n```mermaid\nflowchart TB\n    cacheL1T0[\"L1 Cache Thread 0\"] --- cacheL2[\"L2 Cache Shared Slice 0\"]\n    cacheL1T1[\"L1 Cache Thread 1\"] --- cacheL2\n    cacheL1T2[\"L1 Cache Thread 2\"] --- cacheL2\n    cacheL1T3[\"L1 Cache Thread 3\"] --- cacheL2\n    cacheL2 --- cacheL3[\"L3 Shared Last Level Cache\"]\n    cacheL3 --- dram[\"Main DRAM holding A, b, x_old, x_new\"]\n```\n\n> [!IMPORTANT]\n> The L3 cache (Last-Level Cache) is where all threads read the *shared* `x_old` snapshot. **False sharing** on the per-thread chunk of `x_new` must be avoided by padding to cache-line boundaries (typically 64 bytes).\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## Part A — Short Answer Questions (3 Marks Each)\n\n### Q1. **[KTU University Exam — Model Paper 2024, Module 3]**\n**State the convergence condition for the Jacobi iterative method. Why is the Jacobi method preferred over Gauss-Seidel for parallelization on shared-memory systems?**\n\n**Model Answer (3 marks):**\n\nThe Jacobi method converges (sufficiently) when the coefficient matrix $A$ is **strictly diagonally dominant**, i.e., for every row $i$:\n\n$$\\vert a_{ii} \\vert > \\sum_{j=1, j \\neq i}^{n} \\vert a_{ij} \\vert$$\n\nGauss-Seidel updates components *in-place* within a single sweep, meaning $x_i^{(k+1)}$ depends on already-updated $x_{i-1}^{(k+1)}$ values, which **introduces a true read-after-write (RAW) data dependency** between successive row updates, destroying row-level parallelism. In contrast, Jacobi's update uses *only* values from iteration $k$ (frozen), so all $n$ rows can be computed **independently and concurrently**. **(1 + 2 = 3 marks)**\n\n---\n\n### Q2. **[KTU University Exam — Sample Paper]**\n**List the three main OpenMP synchronization points that arise in a parallel Jacobi implementation and briefly state the role of each.**\n\n**Model Answer (3 marks):**\n\n1. **Implicit barrier** at the end of the `omp for` directive — ensures all threads finish writing their rows of `x_new` before the convergence check reads them. **(1 mark)**\n2. **Explicit `omp barrier`** (if used between update and convergence check) — guarantees a consistent global view of `x_new` for the residual norm. **(1 mark)**\n3. **`omp critical` or `reduction` clause** — protects the accumulation of the global residual norm $\\Vert \\vec{x}_{new} - \\vec{x}_{old} \\Vert_2$ from race conditions, since each thread computes a *local* sum. **(1 mark)**\n\n---\n\n## Part B — Long Answer Questions (14 Marks Each, with Internal Choice)\n\n### Question A — Option (14 marks)\n\n**[KTU University Exam — Dec 2023, Adapted]**\n**(a) [7 marks]** Derive the Jacobi iteration formula from the system $A\\vec{x} = \\vec{b}$. Explain the algorithm with a clear flowchart and state the diagonal dominance convergence condition.\n\n**(b) [7 marks)** Design an OpenMP-parallel implementation strategy for Jacobi on a 1000 × 1000 system. Discuss (i) data partitioning, (ii) loop construct and clause choices, (iii) the convergence test, and (iv) the buffer-swap mechanism. State two potential performance pitfalls.\n\n#### Model Solution for Q-A(a):\n\n**Step 1 — Decomposition:** Write $A = D + L + U$ as in Section 3.1.\n\n**Step 2 — Iteration formula:**\n\n$$\n\\begin{aligned}\nx_i^{(k+1)} &= \\frac{1}{a_{ii}}\\left(b_i - \\sum_{j=1}^{i-1} a_{ij} x_j^{(k)} - \\sum_{j=i+1}^{n} a_{ij} x_j^{(k)}\\right) \\\\\n&= \\frac{1}{a_{ii}}\\left(b_i - \\sum_{j \\neq i} a_{ij} x_j^{(k)}\\right)\n\\end{aligned}\n$$\n\n**Step 3 — Algorithm outline:** Initialize $\\vec{x}^{(0)}$; loop until $\\Vert \\vec{x}^{(k+1)} - \\vec{x}^{(k)} \\Vert < \\epsilon$; update each $x_i$ using the formula above.\n\n**Step 4 — Convergence condition:** $\\vert a_{ii} \\vert > \\sum_{j \\neq i} \\vert a_{ij} \\vert$ for all $i$.\n\n**Mark split (a):** Decomposition = 1, Update formula derivation = 3, Algorithm steps = 2, Convergence = 1.\n\n#### Model Solution for Q-A(b):\n\n**(i) Data partitioning (2 marks):** Use **row-striped decomposition**. With 1000 rows and $p$ threads, thread $t$ gets rows $\\lfloor tn/p \\rfloor$ to $\\lfloor (t+1)n/p \\rfloor - 1$.\n\n**(ii) OpenMP construct (2 marks):**\n\n```c\n#pragma omp parallel for default(none) \\\n        shared(A, b, x_old, x_new, n) private(i, j, sum) \\\n        schedule(static)\nfor (i = 0; i < n; i++) {\n    sum = b[i];\n    for (j = 0; j < n; j++) {\n        if (j != i) sum -= A[i*n + j] * x_old[j];\n    }\n    x_new[i] = sum / A[i*n + i];\n}\n```\n\nThe `default(none)` clause is mandatory in the KTU 2024 scheme; the `private(i, j, sum)` clause prevents loop-carried false sharing on the sum accumulator.\n\n**(iii) Convergence test (1.5 marks):** Compute $\\delta = \\sqrt{\\sum_i (x_{new,i} - x_{old,i})^2}$ using:\n\n```c\ndouble local_norm = 0.0, global_norm = 0.0;\n#pragma omp parallel for reduction(+:local_norm) private(i)\nfor (i = 0; i < n; i++) {\n    double diff = x_new[i] - x_old[i];\n    local_norm += diff * diff;\n}\nglobal_norm = sqrt(local_norm);\n```\n\n**(iv) Buffer swap (1.5 marks):** Maintain `x_old` and `x_new`. After the convergence check passes (or after each iteration in case of non-convergence), **swap the pointers** using `double *tmp = x_old; x_old = x_new; x_new = tmp;` — O(1) instead of O(n) memory copy.\n\n**Two performance pitfalls (1 mark):**\n\n1. **False sharing on `x_new[]`:** Adjacent threads writing to adjacent elements of `x_new` cause cache-line ping-pong. Mitigation: align each chunk to a 64-byte boundary or use thread-local buffers flushed at the barrier.\n2. **Load imbalance when $n \\nmid p$:** Last thread does less work, others stall at the implicit barrier. Mitigation: use `schedule(static, chunk_size)` to balance or pad $n$ to a multiple of $p$.\n\n---\n\n### Question B — Option (14 marks)\n\n**[KTU University Exam — June 2024, Adapted]**\n**(a) [7 marks]** Solve the following $3 \\times 3$ system using **two iterations** of the sequential Jacobi method. Take $\\vec{x}^{(0)} = (0, 0, 0)^T$. State whether the system is diagonally dominant.\n\n$$\n\\begin{pmatrix} 5 & 1 & 0 \\\\ 1 & 5 & 1 \\\\ 0 & 1 & 5 \\end{pmatrix}\n\\begin{pmatrix} x_1 \\\\ x_2 \\\\ x_3 \\end{pmatrix}\n=\n\\begin{pmatrix} 7 \\\\ 9 \\\\ 11 \\end{pmatrix}\n$$\n\n**(b) [7 marks]** Explain how the iterative update in (a) is parallelized using OpenMP on $p = 3$ threads via row-striped decomposition. Show the work distribution, the barrier semantics, and the buffer swap. Compute the **ideal speedup** for $p = 3$ assuming $f_s = 0.05$ and zero overhead.\n\n#### Model Solution for Q-B(a):\n\n**Diagonal dominance check (1 mark):**\n- Row 1: $\\vert 5 \\vert > \\vert 1 \\vert + \\vert 0 \\vert = 1$ ✓\n- Row 2: $\\vert 5 \\vert > \\vert 1 \\vert + \\vert 1 \\vert = 2$ ✓\n- Row 3: $\\vert 5 \\vert > \\vert 0 \\vert + \\vert 1 \\vert = 1$ ✓\n\nSystem is **strictly diagonally dominant** → Jacobi guaranteed to converge.\n\n**Iteration formulas (2 marks):**\n$$\nx_1^{(k+1)} = \\frac{7 - x_2^{(k)}}{5}, \\quad\nx_2^{(k+1)} = \\frac{9 - x_1^{(k)} - x_3^{(k)}}{5}, \\quad\nx_3^{(k+1)} = \\frac{11 - x_2^{(k)}}{5}\n$$\n\n**Iteration 1 (2 marks):** With $\\vec{x}^{(0)} = (0,0,0)$:\n- $x_1^{(1)} = (7 - 0)/5 = 1.400$\n- $x_2^{(1)} = (9 - 0 - 0)/5 = 1.800$\n- $x_3^{(1)} = (11 - 0)/5 = 2.200$\n\n**Iteration 2 (2 marks):** With $\\vec{x}^{(1)} = (1.4, 1.8, 2.2)$:\n- $x_1^{(2)} = (7 - 1.8)/5 = 5.2/5 = 1.040$\n- $x_2^{(2)} = (9 - 1.4 - 2.2)/5 = 5.4/5 = 1.080$\n- $x_3^{(2)} = (11 - 1.8)/5 = 9.2/5 = 1.840$\n\n**Final answer:** $\\vec{x}^{(2)} \\approx (1.040, \\ 1.080, \\ 1.840)^T$.\n\n#### Model Solution for Q-B(b):\n\n**Work distribution on p = 3 threads (2 marks):** For $n = 3$, each thread gets exactly one row:\n- Thread 0: row 0 → computes $x_1$\n- Thread 1: row 1 → computes $x_2$\n- Thread 2: row 2 → computes $x_3$\n\nAll three threads execute **concurrently** in the same parallel region.\n\n**Barrier semantics (2 marks):** A `barrier` is required at the *end* of the `omp for` loop, BEFORE the convergence test reads the new vector. This ensures that Thread 1 (which needs $x_1^{(k+1)}$ from Thread 0 in the *next* iteration) waits until Thread 0 has stored its value. Without this barrier, a thread may read a *stale* value, causing non-deterministic results (a KTU board-favourite error point).\n\n**Buffer swap (1.5 marks):** At the end of every iteration:\n\n```c\ndouble *tmp = x_old;  x_old = x_new;  x_new = tmp;\n```\n\nThis makes the previously-written `x_new` become the `x_old` for the next iteration. Constant-time O(1) operation; no data movement.\n\n**Ideal speedup computation (1.5 marks):**\n\nUsing Amdahl's law with $f_s = 0.05$ and zero overhead:\n\n$$\nS_3 = \\frac{1}{f_s + \\dfrac{1 - f_s}{p}} = \\frac{1}{0.05 + \\dfrac{0.95}{3}} = \\frac{1}{0.05 + 0.3167} = \\frac{1}{0.3667} \\approx 2.727\n$$\n\nSo the ideal 3-thread speedup is **≈ 2.73×**, and the parallel efficiency is $2.727/3 = 90.9\\%$.\n\n---\n\n## KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Where students typically lose marks in OpenMP-Jacobi questions:**\n> 1. **Confusing Jacobi with Gauss-Seidel formulas** — examiners specifically deduct 1–2 marks if the student writes $x_i^{(k+1)}$ depending on $x_{i-1}^{(k+1)}$. Always stress that Jacobi uses *all* values from iteration $k$.\n> 2. **Forgetting to declare `sum` and `i`, `j` as `private`** — without this, race conditions occur; examiners test if the student understands OpenMP data-sharing clauses.\n> 3. **Computing residual before the barrier** — reading `x_new` while another thread is still writing causes a *data race*. Examiner will deduct 1 mark.\n> 4. **Using `schedule(dynamic)` for uniform work** — this *introduces* overhead. For balanced rows, `schedule(static)` is correct.\n> 5. **Forgetting to swap buffers** — copying $O(n)$ data per iteration destroys performance; pointer swap is the correct idiom.\n> 6. **Failing to state the convergence condition** — examiners expect an explicit *diagonal dominance* statement in any 7+ mark question.\n\n---\n\n## Topic Recap & Important Things to Remember\n\n- **Jacobi update formula:** $x_i^{(k+1)} = \\frac{1}{a_{ii}} \\left( b_i - \\sum_{j \\neq i} a_{ij} x_j^{(k)} \\right)$.\n- **Convergence sufficient condition:** Strict diagonal dominance: $\\vert a_{ii} \\vert > \\sum_{j \\neq i} \\vert a_{ij} \\vert$.\n- **Why parallel:** All updates at iteration $(k+1)$ use only values from $(k)$ → row-level independence → no data race on output.\n- **OpenMP construct of choice:** `#pragma omp parallel for schedule(static) private(...)` over the row index.\n- **Mandatory `private` variables:** loop indices `i`, `j`, and the running `sum` accumulator.\n- **Buffer swap:** Use two vectors `x_old` and `x_new`; swap pointers each iteration to avoid O(n) copies.\n- **Convergence test:** Use a `reduction(+:norm)` to compute the L2 norm of the difference, then compare with tolerance $\\epsilon$.\n- **Speedup:** $S_p = \\dfrac{1}{f_s + (1 - f_s)/p + T_{overhead}/T_1}$ (Amdahl-derived).\n- **Efficiency target:** $\\geq 70\\%$ is considered healthy; below 50% indicates severe load imbalance or excessive synchronization.\n- **False sharing risk:** Adjacent threads writing to adjacent `x_new` elements; mitigate via padding or thread-local accumulators.\n- **Iteration count bound:** $k \\geq \\dfrac{\\log(\\epsilon / \\Vert \\vec{x}^{(1)} - \\vec{x}^{(0)} \\Vert)}{\\log \\rho}$ where $\\rho$ is the spectral radius of $D^{-1}(L+U)$.\n- **Engineering applications:** Poisson equation solvers, CFD pressure-correction, VLSI thermal analysis, image editing, power-flow studies.\n- **Comparison with Gauss-Seidel:** Jacobi = O(n) work/iter, parallel-friendly. Gauss-Seidel = O(n) work/iter, sequential only, but converges roughly 2× faster in iterations.\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "distributedmemoryparallelprogrammingwithmpimessagepassingashortintroductiontompi": {
      "title": "Distributed-Memory Parallel Programming with MPI - Message Passing - A Short Introduction to MPI",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 4\n> - **Topic:** Distributed-Memory Parallel Programming with MPI - Message Passing - A Short Introduction to MPI\n\n<!-- SECTION_1_START -->\n# 📘 MODULE 4 — Distributed-Memory Parallel Programming with MPI\n\n## 1. Core Technical Definition & Intuitive Overview\n\n### 1.1 What is Distributed-Memory Parallel Programming?\n\n**Distributed-Memory Parallel Programming** is a parallel computing paradigm in which each processing element (PE) has its **own private local memory**, and processors communicate with one another explicitly by **passing messages** over an interconnection network. There is **no shared global memory**, and consequently, no automatic visibility of one processor's data by another. The programmer (or the language/library) is fully responsible for orchestrating data movement, synchronization, and coordination.\n\n> [!IMPORTANT]\n> **Formal KTU 2024 Definition:**\n> *A distributed-memory parallel program is a collection of autonomous processes, each with its own address space, that cooperate to solve a single problem by exchanging data through explicit message-passing primitives. The Message Passing Interface (MPI) is the de-facto standard library that specifies the syntax and semantics of these primitives in a portable, vendor-neutral manner.*\n\n### 1.2 The Message Passing Model\n\nThe **Message Passing Model** abstracts a parallel machine as a set of processes connected by a (logical) communication network. Every process:\n1. **Owns** a strictly private memory region.\n2. **Computes** locally on its own data.\n3. **Communicates** with other processes by *sending* and *receiving* messages.\n4. **Synchronizes** with other processes only when the program logic demands it (often implicitly, through blocking message operations).\n\n> [!NOTE]\n> **Key Distinction from Shared Memory (OpenMP):**\n> - In **OpenMP**, threads *see* each other's variables through a shared address space — coordination uses locks, barriers, or atomics.\n> - In **MPI**, processes are *blind* to each other's memory — coordination happens *only* by exchanging data via explicit messages.\n\n### 1.3 Conceptual Analogy — \"The Branch-Office Network\"\n\n> [!TIP]\n> **🏢 Real-World Analogy: A Multi-Branch Company**\n>\n> Imagine a company headquartered in **Kochi** with branch offices in **Trivandrum, Calicut, and London**. Each branch has its own *locked filing cabinet* (local memory) and its own staff (process). The branches **do not share a common filing system** — the Kochi branch cannot simply reach into the Trivandrum cabinet.\n>\n> - To cooperate on a project, branches must **post letters** (send messages) to one another.\n> - Each letter has a **sender address, receiver address, contents, and an envelope tag** (matching the receiver's expectation).\n> - The **postal service** is the *interconnection network* (Ethernet, InfiniBand, etc.).\n> - The **postal rules** — what an envelope must look like, what happens if the recipient isn't home, registered post vs. ordinary post — are the **MPI standard**.\n>\n> The chairman (programmer) writes the master *workflow*: \"Kochi prepares sales data, posts it to Trivandrum; Trivandrum analyses it, posts the report to London; London collates, posts the final report back to HQ.\" This workflow is the **MPI program**.\n\n### 1.4 Why MPI? Why Now?\n\n| Reason | Engineering Justification |\n|---|---|\n| **Scalability** | Distributed memory scales to **hundreds of thousands** of cores (e.g., Frontier, Fugaku) — shared memory does not. |\n| **Portability** | A single MPI program runs on a laptop, a cluster, or a supercomputer unchanged. |\n| **Standardization** | MPI is governed by the **MPI Forum** (open consortium). |\n| **Rich Functionality** | Supports point-to-point, collective, one-sided, and parallel-IO communication. |\n| **Performance** | Implementations (OpenMPI, MPICH, Intel MPI) are heavily tuned; they overlap communication with computation. |\n\n> [!NOTE]\n> **Standard Metrics to Remember (for KTU exams):**\n> - **Latency (L):** Time to send a *zero-length* message — typical cluster value ≈ **1–5 μs**.\n> - **Bandwidth (B):** Achievable data rate for large messages — typical cluster value ≈ **10–200 Gbps**.\n> - Both *L* and *B* are **bold-named** because they are the canonical MPI performance parameters that appear in Amdahl/LogP-style models.\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Anatomy of an MPI Program — The Skeleton\n\nEvery MPI program — regardless of the algorithm it implements — follows the same **six-stage skeleton**. Memorize this; it is the single most-tested structure in KTU exams.\n\n### Step-by-Step Skeleton\n1. **Include the MPI header** — `#include <mpi.h>` makes all MPI constants, types, and prototypes visible.\n2. **Initialize the MPI environment** — `MPI_Init(&argc, &argv)`. Before this call, the program is a *sequential* program with one process.\n3. **Determine identity (rank)** — `MPI_Comm_rank(MPI_COMM_WORLD, &rank)`. Rank is the **0-based integer ID** of the calling process within the communicator.\n4. **Determine group size** — `MPI_Comm_size(MPI_COMM_WORLD, &size)`. Size is the **total number of processes** spawned by `mpirun`.\n5. **Do the parallel work** — a combination of local computation and `MPI_Send` / `MPI_Recv` / collective calls.\n6. **Finalize** — `MPI_Finalize()`. After this call, no MPI function may be used.\n\n> [!IMPORTANT]\n> **Why the Skeleton Matters:**\n> If you forget `MPI_Init`, no parallel environment is established — only process 0 runs. If you forget `MPI_Finalize`, buffered messages may be lost, and the runtime cannot clean up resources. Both are **guaranteed mark-deductions in KTU valuation** if omitted in a code-trace question.\n\n## 2.2 The Six Foundational MPI Functions (Must-Know for KTU)\n\n| # | Function | Purpose | KTU Significance |\n|---|---|---|---|\n| 1 | `MPI_Init` | Boot the MPI runtime | Every program |\n| 2 | `MPI_Comm_rank` | Get this process's ID | Required for `if (rank == 0)` style logic |\n| 3 | `MPI_Comm_size` | Get total process count | Required for distributing work in chunks |\n| 4 | `MPI_Send` | Blocking send of a message | Standard synchronous / buffered / ready mode |\n| 5 | `MPI_Recv` | Blocking receive of a message | Must be matched by a send |\n| 6 | `MPI_Finalize` | Shut down the MPI runtime | Every program |\n\n> [!NOTE]\n> The list above is the **MPI \"Hello World\" set** — questions on these six functions account for >70% of the marks in Module 4 across previous university exam papers.\n\n## 2.3 MPI Data Types — The Type-Matching Rule\n\nMPI is **strongly typed**. The datatype you send *must match* the datatype you receive. Mismatched types result in undefined behaviour and zero marks in code-trace questions.\n\n| MPI Datatype | C Equivalent | Fortran Equivalent |\n|---|---|---|\n| `MPI_INT` | `int` | `INTEGER` |\n| `MPI_FLOAT` | `float` | `REAL` |\n| `MPI_DOUBLE` | `double` | `DOUBLE PRECISION` |\n| `MPI_CHAR` | `char` | `CHARACTER` |\n| `MPI_LONG` | `long` | — |\n| `MPI_BYTE` | raw bytes | — |\n| `MPI_PACKED` | heterogeneous packed | — |\n\n## 2.4 The Communicator — `MPI_COMM_WORLD`\n\nA **communicator** is a *named group of processes* that can communicate with each other. The predefined communicator **`MPI_COMM_WORLD`** contains *every* process launched by `mpirun -np N ./a.out`. The constant value of `MPI_COMM_WORLD` is implementation-defined (typically a small handle, not an integer).\n\n> [!TIP]\n> **KTU Trick Question Alert:**\n> *\"What is the value of `MPI_COMM_WORLD`?\"* — The correct answer is **\"it is an opaque handle, not a numeric value; comparing it to an integer is invalid MPI.\"** This is a favourite 3-mark question setter.\n\n## 2.5 Communication Modes — Blocking vs. Non-Blocking\n\n### Blocking Communication\nA blocking call **does not return until the operation is locally complete**.\n- `MPI_Send` returns when the *send buffer can be safely reused* by the caller (either the message has been delivered, or it has been buffered internally).\n- `MPI_Recv` returns when the *receive buffer contains the message*.\n\n> [!NOTE]\n> Blocking does **not** mean \"synchronous handshake.\" A blocking send may be implemented as a *buffered* copy in RAM and return immediately.\n\n### Non-Blocking Communication (Preview)\n`MPI_Isend` / `MPI_Irecv` return **immediately**, allowing overlap of communication with computation. Completion is detected by `MPI_Wait` or `MPI_Test`. This is examined in the next sub-topic of the module.\n\n## 2.6 Performance Models — The LogP Family\n\nThe **LogP model** (Culler, Karp, Patterson et al., 1993) is the canonical theoretical model used to reason about MPI performance.\n\n| Symbol | Name | Meaning | Typical Cluster Value |\n|---|---|---|---|\n| **L** | **Latency** | Upper bound on time to send a small message one way | **1–5 μs** |\n| **o** | **Overhead** | Time the processor is *busy* issuing/receiving a message | **0.5–2 μs** |\n| **g** | **Gap** | Minimum time between successive sends or receives | **0.1–1 μs** |\n| **P** | **Processors** | Number of processes | user-defined |\n\n> [!IMPORTANT]\n> **Hockney's Simplified Model** (often tested):\n> $$T(n) = \\underbrace{L}_{\\text{latency}} \\;+\\; \\underbrace{\\frac{n}{B}}_{\\text{transfer time}}$$\n> where $T(n)$ is the time to send a message of $n$ bytes and $B$ is the per-link bandwidth in bytes/sec. This is the formula KTU expects when a problem says \"estimate the time to broadcast 1 MB across 32 nodes.\"\n\n## 2.7 KTU Formula Sheet — Master Cheat Sheet\n\n| # | Concept | Formula / Statement | Units / Notes |\n|---|---|---|---|\n| 1 | Message time (Hockney) | $T(n) = L + \\dfrac{n}{B}$ | seconds |\n| 2 | Speedup | $S_p = \\dfrac{T_1}{T_p}$ | dimensionless |\n| 3 | Efficiency | $E_p = \\dfrac{S_p}{p} = \\dfrac{T_1}{p \\cdot T_p}$ | $0 < E_p \\le 1$ |\n| 4 | Amdahl's Law | $S_p \\le \\dfrac{1}{f + \\dfrac{1-f}{p}}$ | $f$ = serial fraction |\n| 5 | Gustafson's Law | $S_p \\le p - f(p-1)$ | scaled problem size |\n| 6 | LogP model | $T_{msg} \\ge L + 2o$ (one-way) | $L$, $o$ in seconds |\n| 7 | MPI_Init | Bootstraps the runtime | Called once per process |\n| 8 | Rank range | $0 \\le \\text{rank} < \\text{size}$ | strictly 0-indexed |\n| 9 | Communicator size | $p = \\text{MPI\\_Comm\\_size}$ | user-defined |\n| 10 | Buffered send | `MPI_Bsend` | uses `MPI_Buffer_attach` |\n\n> [!WARNING]\n> When writing inline absolute values inside the table above, the KTU engine uses `\\vert` instead of the bare pipe `|` to avoid breaking markdown rendering. For example, write $\\vert x \\vert$, not $\\vert x\\vert$ — and never `|x|` in a table cell.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Exhaustive Walkthrough — \"MPI Hello World\"\n\nThe simplest complete MPI program illustrates the full skeleton. We trace it line-by-line so you can answer any code-trace variant in the exam.\n\n### Source Listing (C)\n\n```c\n#include <stdio.h>\n#include <mpi.h>\n\nint main(int argc, char *argv[]) {\n    MPI_Init(&argc, &argv);\n\n    int rank, size;\n    MPI_Comm_rank(MPI_COMM_WORLD, &rank);\n    MPI_Comm_size(MPI_COMM_WORLD, &size);\n\n    printf(\"Hello from process %d of %d\\n\", rank, size);\n\n    MPI_Finalize();\n    return 0;\n}\n```\n\n### Line-by-Line Operational Trace\n\n| Line | What the Runtime Does | What You Write in the Exam |\n|---|---|---|\n| `#include <mpi.h>` | Pulls in `MPI_COMM_WORLD`, `MPI_Init`, etc. | \"Header for MPI symbols.\" |\n| `int main(int argc, char *argv[])` | Standard C entry. **MPI hijacks argc/argv** for its own flags (e.g., `-ppn`). | \"The MPI runtime may modify argc/argv to consume its flags.\" |\n| `MPI_Init(&argc, &argv)` | Spawns the MPI daemon connection, sets up per-process state. **Must be the first MPI call.** | \"Initialises the MPI execution environment. From this call onwards, the program is parallel.\" |\n| `int rank, size;` | Local stack variables — each process has its *own* private copy. | \"Each process has a private `rank` and `size`.\" |\n| `MPI_Comm_rank(MPI_COMM_WORLD, &rank)` | Queries the runtime and stores this process's 0-based ID in `rank`. | \"Stores the process identifier in `rank`.\" |\n| `MPI_Comm_size(MPI_COMM_WORLD, &size)` | Stores the total number of processes in the group in `size`. | \"Stores the total number of processes in `size`.\" |\n| `printf(...)` | Each process independently prints its message. | \"Each process performs local I/O.\" |\n| `MPI_Finalize()` | Flushes any buffered messages, disconnects, and cleans up. | \"Terminates the MPI environment. No MPI call may follow.\" |\n\n### Expected Output for `mpirun -np 4 ./hello`\n\n```\nHello from process 0 of 4\nHello from process 1 of 4\nHello from process 2 of 4\nHello from process 3 of 4\n```\n\n> [!WARNING]\n> The order of the four lines is **non-deterministic**. Each process prints independently, and the order depends on the OS scheduler. Examiners accept any order as correct; do *not* lose marks by claiming the order is fixed.\n\n## 3.2 Derivative Walkthrough — Point-to-Point Ping-Pong\n\nTo deepen understanding, consider a **ping-pong** program: process 0 sends a message of size $n$ to process 1, which immediately sends it back. We use this to **measure latency and bandwidth**.\n\n### Conceptual Flow\n1. Process 0 records `t0 = MPI_Wtime()`.\n2. Process 0 calls `MPI_Send(buf, n, MPI_CHAR, 1, 0, MPI_COMM_WORLD)`.\n3. Process 1 calls `MPI_Recv(buf, n, MPI_CHAR, 0, 0, MPI_COMM_WORLD, &status)`.\n4. Process 1 calls `MPI_Send` back.\n5. Process 0 calls `MPI_Recv` and records `t1 = MPI_Wtime()`.\n6. Round-trip time = $t1 - t0$. One-way time ≈ $(t1 - t0)/2$.\n\n### The One-Way Message Time — Derivation\n\nWe model the round trip as two identical one-way messages (symmetric network assumption). Each one-way message has:\n\n$$\nT_{\\text{one-way}}(n) = L_{\\text{link}} + \\frac{n}{B_{\\text{link}}} + o_{\\text{cpu}}\n$$\n\nFor a single round trip (send + receive), the host overhead appears **twice**:\n\n$$\nT_{\\text{rt}}(n) = 2L + 2\\frac{n}{B} + 2o\n$$\n\nTherefore the *measured* one-way time is:\n\n$$\n\\begin{aligned}\nT_{\\text{measured}}(n) &= \\frac{T_{\\text{rt}}(n)}{2} \\\\\n&= L + \\frac{n}{B} + o\n\\end{aligned}\n$$\n\nThe KTU-friendly simplification (ignoring the small $o$ term for large $n$) is the **Hockney model**:\n\n$$\n\\boxed{\\,T(n) \\approx L + \\frac{n}{B}\\,}\n$$\n\n### Worked Example (KTU-style numerical)\n\n> **Q.** A cluster has $L = 2\\ \\mu\\text{s}$ and $B = 10\\ \\text{GB/s}$. Estimate the time to send a 1 MB message from process 0 to process 1. Assume the host overhead $o$ is negligible.\n>\n> **Solution.**\n>\n> Convert units carefully:\n> - $L = 2\\ \\mu\\text{s} = 2 \\times 10^{-6}\\ \\text{s}$\n> - $n = 1\\ \\text{MB} = 10^{6}\\ \\text{bytes}$\n> - $B = 10\\ \\text{GB/s} = 10 \\times 10^{9}\\ \\text{bytes/s}$\n>\n> Transfer time:\n> $$\\frac{n}{B} = \\frac{10^{6}}{10 \\times 10^{9}} = 10^{-4}\\ \\text{s} = 100\\ \\mu\\text{s}$$\n>\n> Total:\n> $$T = L + \\frac{n}{B} = 2\\ \\mu\\text{s} + 100\\ \\mu\\text{s} = 102\\ \\mu\\text{s}$$\n>\n> The **latency is 50× smaller than the transfer time** at this message size — typical for HPC networks.\n\n## 3.3 Speedup & Efficiency — Analytical Matrix\n\n| Scenario | Formula | KTU-Relevant Insight |\n|---|---|---|\n| Strong scaling (fixed problem) | $S_p = T_1 / T_p$ | $S_p$ saturates at $1/f$ as $p \\to \\infty$ |\n| Weak scaling (scaled problem) | $S_p^{\\text{weak}} \\approx p \\cdot E$ | $E$ should stay ≈ 1 (ideal) |\n| Amdahl bound | $S_p \\le 1/[f + (1-f)/p]$ | famous \"5% serial = 20× max speedup\" rule |\n| Gustafson bound | $S_p \\le p - f(p-1)$ | assumes the *problem* grows with $p$ |\n\n### Amdahl Derivation (for KTU)\n\nLet $T_1$ be the time on one processor. Split it as:\n\n$$\n\\begin{aligned}\nT_1 &= T_s + T_p \\\\\n&= f \\cdot T_1 + (1-f) \\cdot T_1\n\\end{aligned}\n$$\n\nwhere $f$ is the **serial fraction** ($0 \\le f \\le 1$). On $p$ processors, only the parallel part benefits:\n\n$$\nT_p = f \\cdot T_1 + \\frac{(1-f) \\cdot T_1}{p}\n$$\n\nSpeedup:\n\n$$\n\\begin{aligned}\nS_p &= \\frac{T_1}{T_p} \\\\\n&= \\frac{T_1}{f \\cdot T_1 + (1-f) \\cdot T_1 / p} \\\\\n&= \\frac{1}{f + (1-f)/p}\n\\end{aligned}\n$$\n\n> [!IMPORTANT]\n> **KTU 14-Mark Favourite:**\n> *A program has a serial fraction of 5%. Plot / calculate the maximum speedup with 8, 64, 512, 1024 processors.* Use the Amdahl formula above. Maximum possible speedup (as $p \\to \\infty$) is $1/f = 1/0.05 = \\mathbf{20}$. This single fact is the basis of 8–10 marks in many past papers.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Block Diagram — Anatomy of an MPI Process\n\n```mermaid\nflowchart TB\n    A[Process 0<br/>rank = 0] --> B[Local Memory<br/>private address space]\n    C[Process 1<br/>rank = 1] --> D[Local Memory<br/>private address space]\n    E[Process 2<br/>rank = 2] --> F[Local Memory<br/>private address space]\n    G[Process 3<br/>rank = 3] --> H[Local Memory<br/>private address space]\n    B <-->|MPI_Send / MPI_Recv| D\n    D <-->|MPI_Send / MPI_Recv| F\n    F <-->|MPI_Send / MPI_Recv| H\n    H <-->|MPI_Send / MPI_Recv| B\n    I[Interconnection Network<br/>Ethernet / InfiniBand] -.interconnects.-> B\n    I -.interconnects.-> D\n    I -.interconnects.-> F\n    I -.interconnects.-> H\n```\n\n> [!NOTE]\n> **Reading the diagram:** Each rectangle is a *process* with a *private* memory region (no overlap with others). The double-headed arrows between memories represent *explicit message-passing* calls; the dashed line to the network box represents the *physical transport*. There is no shared memory bus.\n\n## 4.2 Flowchart — MPI Program Execution Lifecycle\n\n```mermaid\nflowchart TD\n    S([Start: mpirun -np N ./a.out]) --> I[MPI_Init]\n    I --> R[MPI_Comm_rank]\n    R --> Z[MPI_Comm_size]\n    Z --> Q{rank == 0?}\n    Q -- Yes --> W0[Worker 0 logic<br/>e.g. distribute data]\n    Q -- No --> WN[Worker N logic<br/>e.g. receive chunk and compute]\n    W0 --> X[MPI_Send / MPI_Recv<br/>point-to-point]\n    WN --> X\n    X --> C[MPI collective calls<br/>optional: Barrier, Reduce]\n    C --> F[MPI_Finalize]\n    F --> E([End: all processes exit])\n```\n\n> [!IMPORTANT]\n> **KTU Exam Tip:** The diamond labelled \"rank == 0?\" is the **most-common branching pattern** in MPI. Process 0 typically *distributes work* (master), and the others *compute on their assigned chunk* (workers). This is the **master–worker pattern**, a guaranteed 14-mark question every December.\n\n## 4.3 Communication Pattern Diagram — Broadcast vs. Scatter\n\n```mermaid\nflowchart LR\n    subgraph BROADCAST\n      B0[Process 0<br/>holds data D] -->|D| B1[Process 1]\n      B0 -->|D| B2[Process 2]\n      B0 -->|D| B3[Process 3]\n    end\n    subgraph SCATTER\n      S0[Process 0<br/>holds D0, D1, D2, D3] -->|D0| S1[Process 1]\n      S0 -->|D1| S2[Process 2]\n      S0 -->|D2| S3[Process 3]\n    end\n```\n\n> [!TIP]\n> **Distinction to memorize:**\n> - **Broadcast (`MPI_Bcast`):** *Same* data D goes to *every* process.\n> - **Scatter (`MPI_Scatter`):** *Different* chunks (D0, D1, D2, D3) go to *different* processes. After a scatter, the data is *partitioned*.\n\n## 4.4 Buffer-State Diagram — `MPI_Send` Modes\n\n```mermaid\nflowchart LR\n    subgraph STANDARD_SEND\n      A1[User buffer] -->|MPI_Send copies<br/>or hands off| B1[System buffer / NIC]\n      B1 -->|Network| C1[Receiver]\n    end\n    subgraph BUFFERED_SEND\n      A2[User buffer] -->|MPI_Bsend copies<br/>into user-allocated buffer| B2[User buffer via MPI_Buffer_attach]\n      B2 -->|Network| C2[Receiver]\n    end\n    subgraph SYNCHRONOUS_SEND\n      A3[User buffer] -->|MPI_Ssend waits for<br/>matching Recv| B3[Receiver]\n      B3 -->|handshake OK| C3[Network transfer]\n    end\n```\n\n> [!WARNING]\n> The three send modes (`MPI_Send` standard, `MPI_Bsend` buffered, `MPI_Ssend` synchronous) are a **favourite 7-mark sub-question**. Key difference: *standard* send implementation-defined (may buffer or may not); *buffered* always copies into a user-attached buffer; *synchronous* does not return until the matching receive has started.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n> **[KTU University Exam — Dec 2023, Model Q1]**\n> **Q1. Define the Message Passing Interface (MPI). List any four of its core features.**\n\n**Model Answer (3 marks):**\n- **Definition (1 mark):** MPI is a *standardized, language-independent* communication library specification for parallel computers, libraries, and applications. It defines the syntax and semantics of message-passing routines.\n- **Four features (2 marks, ½ mark each):**\n  1. Point-to-point communication (`MPI_Send` / `MPI_Recv`).\n  2. Collective communication (`MPI_Bcast`, `MPI_Reduce`, `MPI_Gather`).\n  3. Support for both **blocking** and **non-blocking** operations.\n  4. **Communicator** groups for safe subset communication.\n  5. (Bonus) Datatype system for heterogeneous packing.\n\n---\n\n> **[KTU University Exam — June 2024, Model Q2]**\n> **Q2. Distinguish between blocking and non-blocking MPI communication. Give one example of each.**\n\n**Model Answer (3 marks):**\n- **Blocking (1 mark):** The call does not return until the *local* operation is complete (buffer reusable for send; message received for recv). Example: `MPI_Send`, `MPI_Recv`.\n- **Non-blocking (1 mark):** The call returns *immediately*, allowing overlap of communication with computation. Completion checked by `MPI_Wait` or `MPI_Test`. Example: `MPI_Isend`, `MPI_Irecv`.\n- **Practical difference (1 mark):** Non-blocking is used to *hide communication latency* behind computation; blocking is simpler but cannot overlap.\n\n---\n\n## 5.2 Part B — Full 14-Mark Questions (Internal Choice Pattern)\n\n> **[KTU University Exam — Dec 2023, Module 4, Q. Pattern]**\n>\n> **PART B — Attempt any ONE full question.**\n> Each sub-part carries 7 marks.\n\n---\n\n### ✍️ **Question A — 14 Marks (Master–Worker with Point-to-Point Messages)**\n\n**(a)** Explain the **master–worker (manager–worker) pattern** in MPI. With a neat diagram, describe how a process of rank 0 distributes an array of **N integers** to **p** worker processes, where each worker computes the **sum of its assigned chunk** and sends the result back to the master. Use `MPI_Send` and `MPI_Recv` only. **[7 marks]**\n\n**(b)** Write a complete MPI program in C that implements the scenario in (a) for $N = 8$ and $p = 4$. Include `MPI_Init`, `MPI_Comm_rank`, `MPI_Comm_size`, distribution, computation, gathering, and `MPI_Finalize`. Show the expected output. **[7 marks]**\n\n---\n\n#### ✅ Model Solution for Q-A (a) — 7 marks\n\n**Concept (2 marks):** The master–worker pattern is the canonical *task-parallel* decomposition. Process 0 (master) **owns the data initially**, **divides it into chunks**, **sends each chunk to a worker**, and **collects the partial results**. Workers each receive their chunk, perform local computation, and return the result.\n\n**Distribution logic (2 marks):**\n- Let $N$ = total elements, $p$ = total processes. Chunk size $k = N / p$ (assumed divisible).\n- For $i = 1, 2, \\dots, p-1$, master sends the slice `data[i*k ... (i+1)*k - 1]` to worker $i$.\n- Master itself processes the slice `data[0 ... k-1]`.\n\n**Gather logic (2 marks):**\n- Master receives $p-1$ partial sums via `MPI_Recv` with `MPI_ANY_SOURCE`.\n- Master adds them to its own partial sum.\n- The total is the final answer.\n\n**Diagram (1 mark):** See the *flowchart* in Section 4.2.\n\n---\n\n#### ✅ Model Solution for Q-A (b) — 7 marks\n\n**Source Program (5 marks):**\n\n```c\n#include <stdio.h>\n#include <mpi.h>\n\nint main(int argc, char *argv[]) {\n    int rank, size;\n    int data[8] = {1, 2, 3, 4, 5, 6, 7, 8};\n    int chunk = 2, local_sum = 0, i;\n\n    MPI_Init(&argc, &argv);\n    MPI_Comm_rank(MPI_COMM_WORLD, &rank);\n    MPI_Comm_size(MPI_COMM_WORLD, &size);   /* expect 4 */\n\n    if (rank == 0) {\n        /* Master distributes data to workers 1, 2, 3 */\n        for (i = 1; i < size; i++) {\n            MPI_Send(&data[i * chunk], chunk, MPI_INT,\n                     i, 0, MPI_COMM_WORLD);\n        }\n        /* Master processes its own chunk */\n        for (i = 0; i < chunk; i++)\n            local_sum += data[i];\n    } else {\n        /* Workers receive their chunk */\n        int recv_buf[2];\n        MPI_Recv(recv_buf, chunk, MPI_INT, 0, 0,\n                 MPI_COMM_WORLD, MPI_STATUS_IGNORE);\n        for (i = 0; i < chunk; i++)\n            local_sum += recv_buf[i];\n    }\n\n    /* All processes send partial sum to master */\n    MPI_Send(&local_sum, 1, MPI_INT, 0, 1, MPI_COMM_WORLD);\n\n    if (rank == 0) {\n        int total = local_sum, partial;\n        for (i = 1; i < size; i++) {\n            MPI_Recv(&partial, 1, MPI_INT, i, 1,\n                     MPI_COMM_WORLD, MPI_STATUS_IGNORE);\n            total += partial;\n        }\n        printf(\"Total sum = %d\\n\", total);\n    }\n\n    MPI_Finalize();\n    return 0;\n}\n```\n\n**Trace (1 mark):**\n- Process 0 partial = $1+2 = 3$\n- Process 1 partial = $3+4 = 7$\n- Process 2 partial = $5+6 = 11$\n- Process 3 partial = $7+8 = 15$\n- Total = $3+7+11+15 = \\mathbf{36}$\n\n**Expected output (1 mark):** `Total sum = 36`\n\n---\n\n### ✍️ **Question B — 14 Marks (Alternative Choice — Performance & Theory Focus)**\n\n**(a)** With suitable diagrams, explain the **point-to-point communication** operations in MPI. Compare **standard mode (`MPI_Send`)** with **buffered mode (`MPI_Bsend`)** and **synchronous mode (`MPI_Ssend`)** in terms of completion semantics. **[7 marks]**\n\n**(b)** A cluster has link **latency $L = 3\\ \\mu\\text{s}$** and **bandwidth $B = 8\\ \\text{GB/s}$**.\n- (i) Estimate the time to send a **4 MB** message. **[3 marks]**\n- (ii) If the cluster runs an MPI program with **serial fraction $f = 0.02$**, compute the **maximum possible speedup** (Amdahl). **[4 marks]**\n\n---\n\n#### ✅ Model Solution for Q-B (a) — 7 marks\n\n**Point-to-Point (2 marks):**\n- Point-to-point = communication between **exactly two** processes: a *sender* and a *receiver*.\n- The two sides are matched by **communicator, rank (source/dest), tag, and datatype**.\n- Both sides issue calls; MPI buffers the message at the receiver (or in transit) until matched.\n\n**Modes of Send (4 marks):**\n\n| Mode | Return Condition | Buffering Guarantee |\n|---|---|---|\n| **Standard (`MPI_Send`)** | Buffer reusable | Implementation-defined |\n| **Buffered (`MPI_Bsend`)** | Buffer reusable | Always copies to user buffer |\n| **Synchronous (`MPI_Ssend`)** | Matching receive has *started* | May or may not buffer |\n| **Ready (`MPI_Rsend`)** | Buffer reusable | Receive must already be posted |\n\n**Diagrams (1 mark):** Reuse the buffer-state diagram from Section 4.4.\n\n**Conclusion (0 marks extra — implicit):** Choose `MPI_Bsend` when you must return immediately and the system buffer is small; choose `MPI_Ssend` for tight synchronization; choose `MPI_Send` for the common case where the runtime can decide.\n\n---\n\n#### ✅ Model Solution for Q-B (b) — 7 marks\n\n**(i) Time for 4 MB (3 marks):**\n\n$$\n\\begin{aligned}\nn &= 4\\ \\text{MB} = 4 \\times 10^{6}\\ \\text{bytes} \\\\\nB &= 8\\ \\text{GB/s} = 8 \\times 10^{9}\\ \\text{bytes/s} \\\\\nL &= 3\\ \\mu\\text{s} = 3 \\times 10^{-6}\\ \\text{s} \\\\\n\\frac{n}{B} &= \\frac{4 \\times 10^{6}}{8 \\times 10^{9}} = 5 \\times 10^{-4}\\ \\text{s} = 500\\ \\mu\\text{s} \\\\\nT &= L + \\frac{n}{B} = 3\\ \\mu\\text{s} + 500\\ \\mu\\text{s} = \\mathbf{503\\ \\mu\\text{s}}\n\\end{aligned}\n$$\n\n**(ii) Amdahl's maximum speedup (4 marks):**\n\nThe maximum speedup is achieved as $p \\to \\infty$:\n\n$$\nS_\\infty = \\frac{1}{f} = \\frac{1}{0.02} = \\mathbf{50}\n$$\n\nWith $p = 8$ (often a sub-part):\n\n$$\n\\begin{aligned}\nS_8 &= \\frac{1}{f + (1-f)/p} \\\\\n&= \\frac{1}{0.02 + 0.98/8} \\\\\n&= \\frac{1}{0.02 + 0.1225} \\\\\n&= \\frac{1}{0.1425} \\\\\n&\\approx \\mathbf{7.02}\n\\end{aligned}\n$$\n\nThis shows the **diminishing return** of adding more processors once serial overhead dominates.\n\n---\n\n## 5.3 ⚠️ KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Where students lose marks on this topic:**\n>\n> 1. **Omitting `MPI_Finalize()`** in code listings — 1 mark deducted. *\"No, `MPI_Finalize` is not optional.\"*\n> 2. **Confusing `MPI_Comm_size` and `MPI_Comm_rank`** in trace questions — the former returns the *total count*, the latter returns *this process's ID*.\n> 3. **Tag mismatches** — if sender uses tag `0` and receiver uses tag `1`, the message will *deadlock* and never be received. Always check tags match.\n> 4. **Assuming output order is deterministic** — printing order from concurrent processes is *not* guaranteed.\n> 5. **Datatype mismatches** — sending `MPI_INT` and receiving `MPI_FLOAT` is undefined behaviour. Examiners check this carefully.\n> 6. **Forgetting `#include <mpi.h>`** — non-compilable code is graded leniently, but missing header costs half a mark.\n> 7. **Conflating `MPI_Send` modes** — many students think \"blocking\" means \"synchronous.\" It does not. Read Section 2.5 again.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n> ✅ **Rapid-Revision Checklist — Module 4, Sub-topic 1**\n>\n> - **MPI** is a *library specification* (not a language) for **distributed-memory message passing**.\n> - Every MPI program **must** call `MPI_Init` first and `MPI_Finalize` last.\n> - `MPI_Comm_rank` → my ID (0 to size−1); `MPI_Comm_size` → total count.\n> - `MPI_COMM_WORLD` is the **default communicator** containing all launched processes.\n> - **Strongly typed** — send and receive datatypes must match.\n> - **Point-to-point** = `MPI_Send` / `MPI_Recv`. **Collective** = `Bcast`, `Reduce`, `Gather`, `Scatter`, `Allreduce`.\n> - **Master–worker pattern** = process 0 distributes, others compute, master collects.\n> - **Performance model:** $T(n) = L + n/B$ (Hockney), with $L$ = latency and $B$ = bandwidth.\n> - **Amdahl's limit:** $S_\\infty = 1/f$, where $f$ is the serial fraction.\n> - **Standard send** behaviour is implementation-defined; **buffered** always copies; **synchronous** waits for matching receive.\n> - **Output from parallel processes is unordered** — never claim determinism in the exam.\n> - **Tag, source, dest, communicator, datatype** must all be consistent for a successful message match.\n> - **MPI is portable** — same source code runs on a laptop, a Beowulf cluster, or a Top-500 supercomputer.\n> - For KTU exam: *memorize the six-function skeleton, the master–worker pattern, the Hockney model, and Amdahl's law* — these four items cover 80% of marks.\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "asimpleexample": {
      "title": "A Simple Example",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module Module 4\n> - **Topic:** A Simple Example\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\nIn the context of High Performance Computing (HPC) under the KTU 2024 Scheme, the topic **\"A Simple Example\"** within Module 4 introduces a canonical, foundational parallel program used to illustrate the end-to-end workflow of heterogeneous and homogeneous parallel execution. The most pedagogically accepted \"Simple Example\" in HPC literature is the **Parallel Vector Addition** program, which demonstrates how a single computational kernel is decomposed, scheduled, and executed across multiple threads or processing elements.\n\nMathematically, given two vectors $\\vec{A} = (a_0, a_1, ..., a_{n-1})$ and $\\vec{B} = (b_0, b_1, ..., b_{n-1})$, the result vector $\\vec{C} = (c_0, c_1, ..., c_{n-1})$ is computed as:\n\n$$c_i = a_i + b_i \\quad \\text{for } i \\in [0, n-1]$$\n\nEach element $c_i$ is **independent** of every other element, which makes this operation an **embarrassingly parallel** problem — a classification used by KTU examiners to denote computations with no inter-task data dependency.\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight:** The \"Simple Example\" in Module 4 of PECST757 is the gateway demonstration for GPU-CPU (host-device) programming models such as **CUDA** and **OpenCL**, and for shared-memory models like **OpenMP**. It validates the student's understanding of *host invocation*, *kernel launch*, and *memory transfer* — the three pillars of any HPC application.\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine a **kitchen with one chef versus a kitchen with 20 chefs**:\n- **Serial execution (1 chef):** The chef must boil rice, chop vegetables, fry paneer, and plate the dish — one task at a time. This represents a CPU thread processing vector elements sequentially.\n- **Parallel execution (20 chefs):** Each chef is assigned **one specific vegetable** to chop. While Chef 1 chops onions, Chef 2 chops tomatoes, and so on. The final curry is ready **20× faster** (in theory). This represents multiple GPU threads, each computing one element $c_i = a_i + b_i$ simultaneously.\n\nThe \"Simple Example\" is the kitchen test case — the smallest dish that proves the parallel kitchen works. Once this is understood, every larger HPC algorithm (matrix multiplication, FFT, convolution, molecular dynamics) is just a more complex kitchen with more chefs and more intricate recipes.\n\n> [!NOTE]\n> **Key Standard Metrics in HPC (Kerala B.Tech context):**\n> - **Speedup** ($S_p$) — ratio of serial time to parallel time\n> - **Efficiency** ($E_p$) — Speedup divided by the number of processors\n> - **FLOPs** — Floating Point Operations per second (computational throughput)\n> - **Bandwidth** — measured in **GB/s**, representing memory transfer rate\n\n---\n\n## 1.3 Relevance in the KTU 2024 Scheme\n\nThe \"Simple Example\" topic is **high-yield** because:\n1. It is the **first program** students write when learning CUDA/OpenCL/OpenMP.\n2. It appears in **Part A (3-mark)** questions as a direct definition or short code-trace.\n3. It is the **Part B (14-mark)** favorite — examiners ask students to write a complete parallel program, identify the kernel, and compute speedup.\n\nThe example also maps to **Course Outcome CO4** of PECST757: *\"Develop parallel programs using contemporary HPC frameworks and evaluate their performance.\"*\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Operational Decomposition of a Simple Parallel Example\n\nThe \"Simple Example\" is typically broken into **six logical stages** that every KTU student must master:\n\n### Stage 1 — Host-Side Memory Allocation\nThe CPU (host) allocates three arrays in RAM: `A_h`, `B_h`, and `C_h` (the `_h` suffix denotes *host memory*). The size of each array is $N \\times \\text{sizeof}(\\text{double})$.\n\n### Stage 2 — Data Initialization\nThe host populates `A_h` and `B_h` with sample values. In the canonical example, $a_i = i$ and $b_i = 2i$ so that the expected output is $c_i = 3i$.\n\n### Stage 3 — Memory Transfer (Host → Device)\nIn a GPU model (CUDA/OpenCL), the host memory is copied to the **device global memory** using functions like `cudaMemcpy()` with the flag `cudaMemcpyHostToDevice`. This is a **bandwidth-bound** operation governed by:\n\n$$T_{\\text{transfer}} = \\frac{\\text{Data Size (bytes)}}{\\text{Bandwidth (GB/s)}}$$\n\n### Stage 4 — Kernel Launch (The Parallel Core)\nThe kernel is launched with a specific **grid-block-thread hierarchy**:\n- `gridDim` — number of blocks in the grid\n- `blockDim` — number of threads per block\n- Total parallel threads: $\\text{gridDim} \\times \\text{blockDim} \\geq N$\n\nEach thread computes **one element** $c_i$ using its globally unique thread index:\n\n$$i = \\text{blockIdx.x} \\times \\text{blockDim.x} + \\text{threadIdx.x}$$\n\n### Stage 5 — Result Transfer (Device → Host)\nThe computed array `C_d` is copied back to the host using `cudaMemcpyDeviceToHost`.\n\n### Stage 6 — Validation and Cleanup\nThe host verifies $c_i = a_i + b_i$ for a sample of indices, then frees memory using `cudaFree()` and `free()`.\n\n> [!IMPORTANT]\n> **Why each stage matters:**\n> - Stages 1–2 and 5–6 are **sequential overhead** (Amdahl's Law penalty).\n> - Stage 4 is the **only fully parallel** segment.\n> - Reducing sequential overhead is the *entire* challenge of HPC engineering.\n\n## 2.2 Amdahl's Law — The Theoretical Ceiling\n\nFor a parallel program with fraction $f$ of code that is strictly serial, the maximum theoretical speedup using $p$ processors is:\n\n$$S_p = \\frac{1}{f + \\frac{1-f}{p}}$$\n\nIn the vector addition example, if data transfer takes 5% of total runtime ($f = 0.05$), even with $p = 1024$ GPU cores:\n\n$$S_{1024} = \\frac{1}{0.05 + \\frac{0.95}{1024}} = \\frac{1}{0.05 + 0.000928} \\approx 19.61$$\n\nThis is a **classic KTU 14-mark question** — students are expected to compute the achievable speedup and discuss the bottleneck.\n\n## 2.3 KTU Formula Sheet / Cheat Sheet\n\n| **Formula** | **Meaning** | **Units** | **Use Case** |\n|---|---|---|---|\n| $S_p = \\frac{T_s}{T_p}$ | Speedup definition | dimensionless | Direct speedup calculation |\n| $E_p = \\frac{S_p}{p} = \\frac{T_s}{p \\cdot T_p}$ | Parallel efficiency | dimensionless (0 to 1) | Quality of parallelization |\n| $S_p = \\frac{1}{f + \\frac{1-f}{p}}$ | Amdahl's Law bound | dimensionless | Theoretical max speedup |\n| $i = \\text{blockIdx.x} \\cdot \\text{blockDim.x} + \\text{threadIdx.x}$ | Global thread index | integer | CUDA/OpenCL indexing |\n| $T_{\\text{transfer}} = \\frac{D}{B}$ | Memory transfer time | seconds | Bandwidth-bound ops |\n| $\\text{Throughput} = \\frac{N}{T_p}$ | Performance rate | ops/second | HPC benchmarking |\n| $\\text{FLOPs} = 2 \\times N$ (for add) | Computational cost | FLOPs | Vector add cost is 1 add = 1 FLOP per element |\n| $\\text{Grid Size} = \\left\\lceil \\frac{N}{B_s} \\right\\rceil$ | Number of blocks | integer | CUDA launch configuration |\n\n> [!NOTE]\n> **Engineering Utility:** This Simple Example forms the **microbenchmark foundation** for real HPC applications in CFD (Computational Fluid Dynamics), weather modeling (WRF), molecular dynamics (GROMACS), and AI training (cuDNN). Every GPU library in production traces its lineage back to this elementary kernel.\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Exhaustive Derivation: Computing Speedup for Vector Addition\n\n### Problem Statement\nA vector addition program takes **$T_s = 100\\,\\text{ms}$** to run sequentially on a single CPU core. Of this, the **kernel computation** takes **80 ms**, and the **memory transfer + setup** takes **20 ms**. If the kernel is parallelized on a GPU with **$p = 256$ threads**, compute:\n1. The serial fraction $f$\n2. The Amdahl's Law speedup $S_p$\n3. The parallel efficiency $E_p$\n\n### Step 1 — Identify the Serial and Parallel Portions\n- Total serial time: $T_s = T_{\\text{serial}} + T_{\\text{parallel}} = 20 + 80 = 100\\,\\text{ms}$\n- Parallel portion (kernel): $T_{\\text{parallel}} = 80\\,\\text{ms}$\n- Serial portion (transfer + setup): $T_{\\text{serial}} = 20\\,\\text{ms}$\n\nThe serial fraction $f$ is the fraction of code that **cannot** be parallelized:\n\n$$f = \\frac{T_{\\text{serial}}}{T_s} = \\frac{20}{100} = 0.20$$\n\nTherefore, the parallel fraction is:\n\n$$1 - f = 1 - 0.20 = 0.80$$\n\n### Step 2 — Apply Amdahl's Law\nWith $p = 256$ parallel threads, the parallel portion executes in:\n\n$$T_p = f \\cdot T_s + \\frac{(1-f) \\cdot T_s}{p}$$\n\nSubstituting values:\n\n$$T_p = 0.20 \\times 100 + \\frac{0.80 \\times 100}{256}$$\n\n$$T_p = 20 + \\frac{80}{256} = 20 + 0.3125 = 20.3125\\,\\text{ms}$$\n\n### Step 3 — Compute Speedup\n\n$$S_p = \\frac{T_s}{T_p} = \\frac{100}{20.3125} = 4.923$$\n\n### Step 4 — Compute Parallel Efficiency\n\n$$E_p = \\frac{S_p}{p} = \\frac{4.923}{256} = 0.01923 \\approx 1.923\\%$$\n\n> [!IMPORTANT]\n> **Key Insight (for KTU valuation):** Even though we used 256 threads, the speedup is only **~4.92×** because the serial 20% dominates. This is the **Amdahl bottleneck** — the central reason real HPC systems invest heavily in reducing host-device transfer overhead (e.g., using pinned memory, asynchronous streams, and unified memory).\n\n## 3.2 Analytical Matrix: Performance Scaling for Vector Addition\n\n| **Processors (p)** | **$T_p$ (ms)** | **$S_p$** | **$E_p$ (%)** | **Comment** |\n|---|---|---|---|---|\n| 1 | 100.000 | 1.00 | 100.00 | Baseline serial |\n| 2 | 60.000 | 1.67 | 83.33 | Sub-linear |\n| 4 | 40.000 | 2.50 | 62.50 | Sub-linear |\n| 8 | 30.000 | 3.33 | 41.67 | Sub-linear |\n| 16 | 25.000 | 4.00 | 25.00 | Approaching limit |\n| 32 | 22.500 | 4.44 | 13.89 | Diminishing returns |\n| 64 | 21.250 | 4.71 | 7.35 | Diminishing returns |\n| 128 | 20.625 | 4.85 | 3.79 | Diminishing returns |\n| 256 | 20.313 | 4.92 | 1.92 | Diminishing returns |\n| 1024 | 20.078 | 4.98 | 0.49 | Near ceiling |\n| $\\infty$ | 20.000 | 5.00 | 0.00 | Amdahl's hard limit |\n\n**Limit analysis** — as $p \\to \\infty$:\n\n$$\\lim_{p \\to \\infty} S_p = \\frac{1}{f} = \\frac{1}{0.20} = 5.00$$\n\nNo matter how many GPU cores are added, the program can never run faster than **5×** because the 20% serial portion acts as a hard floor. This is the **Amdahl asymptote**.\n\n## 3.3 Index Mapping Derivation (CUDA/OpenCL)\n\nFor an array of size $N = 1{,}000{,}000$ elements using `blockDim.x = 256` threads per block:\n\n$$\\text{gridDim.x} = \\left\\lceil \\frac{N}{\\text{blockDim.x}} \\right\\rceil = \\left\\lceil \\frac{1{,}000{,}000}{256} \\right\\rceil = \\lceil 3906.25 \\rceil = 3907 \\text{ blocks}$$\n\nFor a thread in block `blockIdx.x = 5` and `threadIdx.x = 12`:\n\n$$i = 5 \\times 256 + 12 = 1280 + 12 = 1292$$\n\nThis thread computes:\n\n$$C[1292] = A[1292] + B[1292]$$\n\nA **boundary check** is mandatory because `gridDim.x × blockDim.x = 3907 × 256 = 1{,}000{,}192 > 1{,}000{,}000`. Threads with $i \\geq 1{,}000{,}000$ must return early to prevent out-of-bounds memory access — a **common deduction point** in KTU valuation.\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Block-Level Functional Architecture Flow of the Simple Example\n\n```mermaid\nflowchart TD\n    A[Host CPU Application Starts] --> B[Allocate Host Memory A_h, B_h, C_h]\n    B --> C[Initialize A_h and B_h with sample data]\n    C --> D[Allocate Device Memory A_d, B_d, C_d]\n    D --> E[Copy A_h and B_h from Host to Device]\n    E --> F[Configure Grid and Block Dimensions]\n    F --> G[Launch Parallel Kernel]\n    G --> H{Kernel Thread i active}\n    H -->|Yes| I[Compute C_d i = A_d i + B_d i]\n    I --> J[Write Result to Global Memory]\n    J --> K{All threads done}\n    H -->|No| K\n    K -->|No| H\n    K -->|Yes| L[Copy C_d from Device to Host C_h]\n    L --> M[Validate Results on Host]\n    M --> N[Free Device and Host Memory]\n    N --> O[Host Application Terminates]\n```\n\n## 4.2 Thread Hierarchy Diagram (CUDA-style)\n\n```mermaid\nflowchart TB\n    G1[Grid 1D] --> B0[Block 0 - 256 threads]\n    G1 --> B1[Block 1 - 256 threads]\n    G1 --> B2[Block 2 - 256 threads]\n    G1 --> B3[Block N - boundary threads]\n    B0 --> T0[Thread 0 - index 0]\n    B0 --> T1[Thread 1 - index 1]\n    B0 --> T2[Thread 2 - index 2]\n    B0 --> T3[Thread 255 - index 255]\n    B1 --> T4[Thread 0 - index 256]\n    B1 --> T5[Thread 1 - index 257]\n```\n\n## 4.3 Memory Transfer and Execution Timeline\n\n```mermaid\nsequenceDiagram\n    participant CPU as Host CPU\n    participant BUS as PCIe Bus\n    participant GPU as Device GPU\n    CPU->>CPU: Allocate and initialize A_h, B_h\n    CPU->>GPU: cudaMalloc A_d, B_d, C_d\n    CPU->>BUS: cudaMemcpy HostToDevice\n    BUS->>GPU: Transfer A_h, B_h\n    CPU->>GPU: Launch kernel grid\n    Note over GPU: Threads compute C_d in parallel\n    GPU->>BUS: cudaMemcpy DeviceToHost\n    BUS->>CPU: Transfer C_d to C_h\n    CPU->>CPU: Validate and free\n```\n\n> [!NOTE]\n> **Diagram Reading Tip for KTU Exams:** Notice how the PCIe bus transfer creates a **bottleneck** between Stages 3 and 5. This visual is exactly what examiners draw when they ask \"Identify the performance bottleneck in this parallel program.\" The answer is always the **memory transfer phase**, governed by Amdahl's serial fraction $f$.\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A Questions (3 Marks Each)\n\n### Question 1\n**[KTU University Exam – Dec 2023]** Define **embarrassingly parallel** computation. State why vector addition qualifies as one.\n\n**Model Answer (3 marks):**\nAn embarrassingly parallel computation is one in which the sub-tasks (or work units) can be executed **independently and concurrently** with **minimal or no inter-process communication**. (1 mark)\n\nIn vector addition $\\vec{C} = \\vec{A} + \\vec{B}$, each output element $c_i = a_i + b_i$ depends **only** on the corresponding input elements $a_i$ and $b_i$. (1 mark) There is no data dependency between any two elements $c_i$ and $c_j$ for $i \\neq j$, so all $N$ additions can be assigned to $N$ parallel threads with no synchronization required. (1 mark)\n\n### Question 2\n**[KTU University Exam – June 2024]** Write the CUDA expression to compute the global thread index $i$ for a 1D grid of 1D blocks.\n\n**Model Answer (3 marks):**\nThe global thread index in a 1D grid of 1D blocks is computed as:\n\n$$i = \\text{blockIdx.x} \\times \\text{blockDim.x} + \\text{threadIdx.x}$$\n\nHere, $\\text{blockIdx.x}$ is the block index within the grid, $\\text{blockDim.x}$ is the number of threads per block, and $\\text{threadIdx.x}$ is the thread's index within its block. (3 marks — full expression 2 marks, variable explanation 1 mark)\n\n---\n\n## 5.2 Part B Questions (14 Marks with Internal Choice)\n\n### Question A (14 Marks) — Comprehensive Program + Analysis\n\n**[KTU University Exam – Model Question, Module 4, KTU 2024 Scheme]**\n\n**(a)** Write a complete CUDA program for parallel vector addition of two arrays of size $N = 500{,}000$. The program should include host memory allocation, device memory allocation, host-to-device transfer, kernel launch, device-to-host transfer, and result validation. Assume 256 threads per block. **(7 marks)**\n\n**(b)** If the sequential execution time on a CPU is **40 ms** and the parallel execution time on a GPU is **5 ms** using **$p = 200$** threads, calculate the **speedup**, **efficiency**, and identify the **bottleneck** using **Amdahl's Law** if the serial fraction is **$f = 0.05$**. **(7 marks)**\n\n#### Model Solution\n\n**(a) Complete CUDA Program (7 marks):**\n\n```c\n#include <stdio.h>\n#include <cuda.h>\n\n#define N 500000\n#define BLOCK_SIZE 256\n\n__global__ void vectorAdd(int *A, int *B, int *C, int n) {\n    int i = blockIdx.x * blockDim.x + threadIdx.x;\n    if (i < n) {\n        C[i] = A[i] + B[i];\n    }\n}\n\nint main(void) {\n    int *A_h, *B_h, *C_h;\n    int *A_d, *B_d, *C_d;\n    size_t size = N * sizeof(int);\n\n    // Stage 1: Host allocation\n    A_h = (int*)malloc(size);\n    B_h = (int*)malloc(size);\n    C_h = (int*)malloc(size);\n\n    // Stage 2: Initialize\n    for (int i = 0; i < N; i++) {\n        A_h[i] = i;\n        B_h[i] = 2 * i;\n    }\n\n    // Stage 3: Device allocation\n    cudaMalloc((void**)&A_d, size);\n    cudaMalloc((void**)&B_d, size);\n    cudaMalloc((void**)&C_d, size);\n\n    // Stage 4: Host to Device transfer\n    cudaMemcpy(A_d, A_h, size, cudaMemcpyHostToDevice);\n    cudaMemcpy(B_d, B_h, size, cudaMemcpyHostToDevice);\n\n    // Stage 5: Kernel launch\n    int gridSize = (N + BLOCK_SIZE - 1) / BLOCK_SIZE;\n    vectorAdd<<<gridSize, BLOCK_SIZE>>>(A_d, B_d, C_d, N);\n    cudaDeviceSynchronize();\n\n    // Stage 6: Device to Host transfer\n    cudaMemcpy(C_h, C_d, size, cudaMemcpyDeviceToHost);\n\n    // Stage 7: Validate\n    for (int i = 0; i < N; i++) {\n        if (C_h[i] != A_h[i] + B_h[i]) {\n            printf(\"Mismatch at index %d\\n\", i);\n            break;\n        }\n    }\n\n    // Cleanup\n    cudaFree(A_d);\n    cudaFree(B_d);\n    cudaFree(C_d);\n    free(A_h);\n    free(B_h);\n    free(C_h);\n    return 0;\n}\n```\n\n**Mark Distribution (a):**\n- Host allocation + initialization — 1.5 marks\n- Device allocation + transfer — 1.5 marks\n- Kernel definition with correct index formula — 2 marks\n- Kernel launch with correct `gridSize` — 1 mark\n- Validation and cleanup — 1 mark\n\n**(b) Performance Calculation (7 marks):**\n\n**Given:**\n- $T_s = 40\\,\\text{ms}$, $T_p = 5\\,\\text{ms}$, $p = 200$, $f = 0.05$\n\n**Step 1 — Speedup:**\n\n$$S_p = \\frac{T_s}{T_p} = \\frac{40}{5} = 8.00$$\n\n**Step 2 — Efficiency:**\n\n$$E_p = \\frac{S_p}{p} = \\frac{8.00}{200} = 0.04 = 4\\%$$\n\n**Step 3 — Amdahl's Law theoretical bound:**\n\n$$S_{\\text{Amdahl}} = \\frac{1}{f + \\frac{1-f}{p}} = \\frac{1}{0.05 + \\frac{0.95}{200}}$$\n\n$$S_{\\text{Amdahl}} = \\frac{1}{0.05 + 0.00475} = \\frac{1}{0.05475} \\approx 18.26$$\n\n**Step 4 — Bottleneck identification:**\n\nThe achieved speedup (8×) is far below the Amdahl bound (18.26×), indicating **non-ideal scaling**. The bottleneck is the **serial fraction $f = 0.05$** (memory transfer + setup), which cannot be parallelized. To improve performance, reduce $f$ by:\n- Using **pinned (page-locked) memory** for faster host-device transfer\n- Using **CUDA streams** to overlap transfer with computation\n- Using **unified memory** to avoid explicit transfers\n\n**Mark Distribution (b):**\n- Correct speedup formula and value — 1.5 marks\n- Correct efficiency formula and value — 1.5 marks\n- Correct Amdahl's Law substitution — 2 marks\n- Bottleneck identification and optimization suggestions — 2 marks\n\n---\n\n### Question B (14 Marks) — Alternative Comprehensive Question\n\n**[KTU University Exam – Model Question, Module 4, KTU 2024 Scheme]**\n\n**(a)** Explain the **six stages** of a typical simple parallel program (vector addition) on a GPU. For each stage, indicate whether it is **host-side**, **device-side**, or **transfer** operation, and whether it is **parallelizable**. **(7 marks)**\n\n**(b)** A simple parallel program runs in **12 seconds** on a single core. When parallelized on **$p = 8$ cores**, it takes **2 seconds**. The serial fraction of the program is found to be **$f = 0.10$**. Verify the result using **Amdahl's Law**, and comment on whether the parallelization is **efficient**. If not, suggest **two techniques** to improve performance. **(7 marks)**\n\n#### Model Solution\n\n**(a) Six Stages of a Simple Parallel Program (7 marks):**\n\n| **Stage** | **Operation** | **Location** | **Parallelizable?** |\n|---|---|---|---|\n| 1 | Host memory allocation (`malloc`) | Host | No |\n| 2 | Data initialization | Host | No |\n| 3 | Device memory allocation (`cudaMalloc`) | Host-API call to device | No |\n| 4 | Host → Device transfer (`cudaMemcpy`) | Transfer (PCIe) | No (bandwidth-limited) |\n| 5 | Kernel launch and execution | Device (GPU) | **Yes (fully parallel)** |\n| 6 | Device → Host transfer + validation | Transfer (PCIe) + Host | No |\n\n**Mark Distribution (a):**\n- Six stages listed correctly — 3 marks\n- Correct classification of each stage — 2 marks\n- Parallelizability identified correctly — 2 marks\n\n**(b) Amdahl's Law Verification (7 marks):**\n\n**Step 1 — Compute actual speedup:**\n\n$$S_p = \\frac{T_s}{T_p} = \\frac{12}{2} = 6.00$$\n\n**Step 2 — Compute parallel efficiency:**\n\n$$E_p = \\frac{S_p}{p} = \\frac{6.00}{8} = 0.75 = 75\\%$$\n\n**Step 3 — Apply Amdahl's Law with $f = 0.10$ and $p = 8$:**\n\n$$S_{\\text{Amdahl}} = \\frac{1}{0.10 + \\frac{0.90}{8}} = \\frac{1}{0.10 + 0.1125} = \\frac{1}{0.2125} \\approx 4.71$$\n\n**Step 4 — Observation:**\n\nThe actual speedup (6.00) is **greater** than the Amdahl prediction (4.71). This seems contradictory, but it indicates that the **measured $f = 0.10$** was likely **overestimated**, or there is some **super-linear** effect due to cache reuse. The efficiency of 75% is reasonably good but not excellent.\n\n**Step 5 — Improvement techniques:**\n\n1. **Reduce serial fraction** by overlapping computation with communication using asynchronous streams.\n2. **Increase workload granularity** by using larger block sizes or grid-stride loops to reduce kernel launch overhead.\n3. **Use shared memory** to reduce redundant global memory accesses.\n\n**Mark Distribution (b):**\n- Speedup and efficiency calculation — 2 marks\n- Amdahl's Law computation — 2 marks\n- Comparative analysis (actual vs theoretical) — 1.5 marks\n- Two valid optimization techniques — 1.5 marks\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall — Common Marks Lost:**\n> 1. **Forgetting the boundary check** `if (i < n)` inside the kernel — **−1.5 marks** deduction. If $N$ is not a multiple of `BLOCK_SIZE`, threads with $i \\geq N$ will cause **out-of-bounds memory access** and undefined behavior.\n> 2. **Missing `cudaDeviceSynchronize()`** after kernel launch — **−0.5 marks**. Without it, `cudaMemcpy` may begin before the kernel finishes, leading to **race conditions**.\n> 3. **Confusing host and device pointers** in `cudaMemcpy` arguments — **−1 mark**. The first argument is the **destination**, not the source.\n> 4. **Incorrect grid size** — using `N / BLOCK_SIZE` instead of `(N + BLOCK_SIZE - 1) / BLOCK_SIZE` causes the **last chunk of elements to be skipped** — **−1 mark**.\n> 5. **Not stating the Amdahl asymptote** $1/f$ in 14-mark answers — **−1 mark** deduction. Examiners expect the student to explicitly derive the theoretical limit.\n\n---\n\n## 5.3 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid Revision Checklist — \"A Simple Example\" (Module 4, PECST757)**\n\n### Core Concepts\n- **Embarrassingly Parallel:** No inter-task data dependency. Vector addition is the canonical example.\n- **Vector Addition Formula:** $c_i = a_i + b_i$, each $c_i$ is computed by one thread.\n- **CUDA Index Formula:** $i = \\text{blockIdx.x} \\times \\text{blockDim.x} + \\text{threadIdx.x}$\n- **Grid Size Formula:** $\\text{gridDim} = \\left\\lceil \\frac{N}{\\text{blockDim}} \\right\\rceil$\n\n### Performance Metrics\n- **Speedup:** $S_p = T_s / T_p$\n- **Efficiency:** $E_p = S_p / p$\n- **Amdahl's Law:** $S_p = \\frac{1}{f + \\frac{1-f}{p}}$\n- **Amdahl's Asymptote:** $\\lim_{p \\to \\infty} S_p = 1/f$ — the **hard speedup ceiling**\n\n### Six Stages of a Simple GPU Program\n1. Host memory allocation\n2. Host data initialization\n3. Device memory allocation\n4. Host → Device memory transfer\n5. Kernel launch (only parallel stage)\n6. Device → Host transfer + validation + cleanup\n\n### Must-Remember Code Safeguards\n- Always include the **boundary check** `if (i < n)` in the kernel.\n- Always use **`cudaDeviceSynchronize()`** before reading device results.\n- Always use the **ceiling division** `(N + B - 1) / B` for grid size.\n- Always **free** both host (`free()`) and device (`cudaFree()`) memory.\n\n### Common Exam Traps\n- Confusing **grid dimension** with **block dimension**.\n- Forgetting that **memory transfer** is serial and counts toward $f$.\n- Assuming **more threads = linear speedup** (Amdahl's Law disproves this).\n- Forgetting to mention that the **last block may have idle threads** if $N$ is not a multiple of `BLOCK_SIZE`.\n\n### Real-World Connection\n- The \"Simple Example\" is the **microbenchmark** for every production GPU library: cuBLAS, cuDNN, cuFFT, and TensorRT all contain vector addition as a primitive operation.\n- The **6-stage flow** is identical for all GPU programs — only the kernel changes.\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "messagesandpointtopointcommunication": {
      "title": "Messages and Point-to-Point Communication",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 4\n> - **Topic:** Messages and Point-to-Point Communication\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition of a Message\n\nIn the context of High Performance Computing (HPC) and the Message Passing Interface (MPI) standard, a **message** is a logically contiguous block of data that is exchanged between two or more cooperating processes executing on potentially different processing elements. Formally, a message $M$ is a tuple defined as:\n\n$$ M = (\\text{buffer}, \\text{count}, \\text{datatype}, \\text{communicator}, \\text{tag}, \\text{source}, \\text{destination}) $$\n\nwhere the **buffer** is the contiguous memory location, **count** specifies the number of elements, **datatype** defines the elemental type, **communicator** defines the process group, **tag** is a non-negative integer used for matching, and **source/destination** identify the rank(s) of the participating process(es).\n\n## 1.2 Formal Definition of Point-to-Point Communication\n\n**Point-to-Point Communication** (PPC) is the fundamental communication paradigm in HPC where exactly **one** sending process transmits a message to exactly **one** receiving process. The complete communication operation involves three distinct phases:\n\n1. **Local preparation phase** – the sender assembles data into a contiguous send buffer.\n2. **Transport phase** – data traverses the underlying network fabric (shared memory, InfiniBand, Ethernet, etc.).\n3. **Receipt phase** – the destination process places the data into a receive buffer.\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight:** Point-to-Point Communication is the atomic building block upon which all collective operations (broadcasts, reductions, barriers) are constructed. Mastering PPC is mandatory for solving all subsequent HPC problems on parallel architectures.\n\n## 1.3 Conceptual Analogy\n\nThink of **Point-to-Point Communication** like a **registered postal courier system** between two houses in a neighborhood:\n\n- The **letter (message)** contains the actual content (payload).\n- The **envelope** contains the *recipient address (destination rank)*, the *sender address (source rank)*, and a *reference number (tag)* for tracking.\n- The **post office (MPI runtime / network)** ensures the letter is delivered reliably.\n- The **recipient must be home** to accept the letter (receive operation must be posted) — otherwise the letter may bounce back (deadlock) or remain unclaimed.\n\nSimilarly, just like a courier offers multiple service levels — *standard mail*, *registered post*, *speed post*, *overnight express* — MPI provides multiple **communication modes** (standard, buffered, synchronous, ready), each offering different trade-offs between speed, safety, and synchronization.\n\n> [!NOTE]\n> **Standard Reference:** All operations discussed follow the **MPI 3.1 standard** (Message Passing Interface Forum, 2015), which remains the canonical reference for HPC message-passing on distributed memory systems.\n\n## 1.4 Message Envelope vs. Message Payload\n\n| Component | Purpose | Analogy |\n|-----------|---------|---------|\n| **Envelope** | Routing information (source, destination, tag, communicator) | Address on an envelope |\n| **Payload** | Actual data being transferred (count × datatype) | Letter content inside |\n| **Datatype** | Type signature defining how bytes map to elements | Language of the letter |\n\nThe **datatype signature** ensures portability — the same MPI program runs correctly regardless of whether the underlying architecture is 32-bit or 64-bit, big-endian or little-endian.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Anatomy of a Message Transfer\n\nThe complete point-to-point message transfer consists of **five logical steps**, which map to the operation of every MPI send/receive call:\n\n1. **Marshalling:** The application packs user data from non-contiguous memory into a contiguous send buffer, converting native types into a network-portable representation.\n2. **Enveloping:** The MPI runtime attaches metadata (source rank, destination rank, tag, communicator handle, datatype map).\n3. **Transmission:** Data is serialized, segmented into packets, and pushed onto the network interface card (NIC) via Direct Memory Access (DMA).\n4. **Routing & Reception:** The destination NIC receives packets, the MPI layer verifies the envelope, and data is copied into the destination process's receive buffer.\n5. **Demarshalling & Synchronization:** The runtime signals completion via the request handle, allowing the application to safely reuse the buffer.\n\n## 2.2 Communication Modes — The Core Conceptual Hierarchy\n\nThe MPI standard defines two orthogonal dimensions: **communication type** (blocking vs. non-blocking) and **communication mode** (standard, buffered, synchronous, ready).\n\n> [!NOTE]\n> **Why so many modes?** Each mode provides different guarantees about when the send buffer can be reused and what happens when no matching receive is posted. Choosing the correct mode is critical for **performance tuning** and **deadlock prevention** — both are favorite KTU exam questions.\n\n### 2.2.1 Blocking Communication\nA blocking send/receive **does not return** until the operation is *logically* complete. For blocking sends, \"logically complete\" depends on the mode:\n\n- **Standard mode:** May complete when the buffer is safely buffered by the runtime, when the message is sent, or when the matching receive posts — *the standard leaves the implementation free to choose*.\n- **Buffered mode (`MPI_Bsend`):** Completes once the message has been copied into a user-managed buffer.\n- **Synchronous mode (`MPI_Ssend`):** Completes only when the matching receive has *started* receiving the message.\n- **Ready mode (`MPI_Rsend`):** Requires that the matching receive has *already been posted* before the send is invoked.\n\n### 2.2.2 Non-Blocking Communication\nNon-blocking operations (`MPI_Isend`, `MPI_Irecv`) return **immediately** with a request handle. The actual data transfer happens in the background. The application must later call `MPI_Wait`, `MPI_Test`, or any of the variants to confirm completion.\n\n## 2.3 Why the Mode Matters — Engineering Trade-offs\n\n| Mode | Buffer Required? | Sync Required? | Deadlock Risk | Typical Use |\n|------|------------------|----------------|---------------|-------------|\n| Standard (`MPI_Send`) | No (implementation dependent) | No | Medium | General purpose |\n| Buffered (`MPI_Bsend`) | **Yes (user-allocated)** | No | Low | Unmatched sends safe |\n| Synchronous (`MPI_Ssend`) | No | **Yes (receive start)** | High | Strong progress guarantees |\n| Ready (`MPI_Rsend`) | No | **Yes (receive posted)** | **Highest** | Hot-path performance |\n\n## 2.4 KTU Formula Sheet / Cheat Sheet\n\nThe following table lists every formula you must memorize for KTU 2024 Scheme examinations on this topic.\n\n| Formula / Concept | Expression | Variables | Units |\n|-------------------|------------|-----------|-------|\n| Hockney Communication Time | $t_s = \\alpha + \\frac{n}{\\beta}$ | $\\alpha$ = latency, $n$ = bytes, $\\beta$ = bandwidth | seconds |\n| Half-bandwidth criterion | $n_0 = \\alpha \\cdot \\beta$ | crossover message size | bytes |\n| Bandwidth-Delay Product | $BDP = \\alpha \\cdot R_{peak}$ | $\\alpha$ = latency, $R_{peak}$ = link rate | bytes |\n| LogP Model Latency | $t_{comm} = L + 2o + \\frac{s}{g}$ | $L$ = latency, $o$ = overhead, $s$ = size, $g$ = gap | seconds |\n| Speedup Bound (Amdahl) | $S = \\frac{1}{(1-p) + \\frac{p}{n}}$ | $p$ = parallel fraction, $n$ = processors | dimensionless |\n| Message Tag Range | $0 \\leq \\text{tag} \\leq 32767$ | MPI standard upper bound | integer |\n| MPI_ANY_TAG constant | $\\text{tag} = -1$ (wildcard) | matches any incoming tag | integer |\n| MPI_ANY_SOURCE constant | $\\text{source} = -2$ (wildcard) | matches any sender rank | integer |\n\n> [!IMPORTANT]\n> **Critical Examination Note:** The Hockney model is the *single most important* formula for this module. It expresses communication time $t_s$ for a message of $n$ bytes as the sum of a **fixed startup latency** $\\alpha$ and a **transfer time** proportional to message size. The constant $\\alpha$ is typically $\\mathbf{1}$ to $\\mathbf{10}$ microseconds for modern HPC interconnects, while $\\beta$ ranges from $\\mathbf{10}$ Gbps to $\\mathbf{200}$ Gbps.\n\n## 2.5 Real-World Engineering Utility\n\nPoint-to-Point Communication underlies every large-scale scientific simulation in production today:\n\n- **Climate Modeling:** Processes simulating different atmospheric cells exchange boundary data.\n- **Computational Fluid Dynamics (CFD):** Domain decomposition requires halo-cell exchange every iteration.\n- **Molecular Dynamics (GROMACS, NAMD):** Cutoff radii demand neighbor-list particle exchange.\n- **Deep Learning:** Parameter Server and Ring-AllReduce architectures are built from point-to-point primitives.\n- **Computational Finance:** Monte Carlo simulations require massive independent workload exchange.\n\nA poorly chosen communication mode in any of these applications can cause **deadlocks**, **unbounded buffer growth**, or **lost synchronization**, leading to catastrophic simulation failure.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation of the Hockney Communication Model\n\nThe Hockney model is derived by modeling the communication channel as a pipeline with a fixed startup cost and a steady-state transfer rate. We begin from the observation that every network transfer incurs:\n\n- A **fixed latency** $\\alpha$ — the time from the moment the send call is invoked to the moment the first byte arrives at the receiver.\n- A **per-byte transfer cost** — the inverse of the link bandwidth $\\beta$.\n\nLet $n$ be the total number of bytes to be transferred. The total transfer time $t_s(n)$ is given by the sum of the latency cost and the proportional transmission cost.\n\n$$t_s(n) = \\alpha + \\frac{n}{\\beta}$$\n\n**Derivation logic (row-by-row):**\n\n1. The send call takes time $\\alpha$ regardless of message size — this is the fixed cost of protocol negotiation, DMA setup, and packet header construction.\n\n2. Once the pipeline is established, each byte travels at a rate governed by the link bandwidth $\\beta$ (bytes per second).\n\n3. The time to push $n$ bytes through the pipeline is the length divided by the rate: $\\frac{n}{\\beta}$.\n\n4. These two costs are additive and independent — yielding the final expression.\n\n**Half-power (half-bandwidth) point:** The crossover message size where latency and transfer time contribute equally is found by setting $\\alpha = \\frac{n}{\\beta}$ and solving for $n$.\n\n$$n_{1/2} = \\alpha \\cdot \\beta$$\n\nMessages smaller than $n_{1/2}$ are **latency-bound**; messages larger than $n_{1/2}$ are **bandwidth-bound**. This crossover determines the optimal blocking factor for collective operations.\n\n## 3.2 Analytical Comparison of Communication Modes\n\nWe now derive the completion conditions for each blocking send mode analytically.\n\n### 3.2.1 Standard Mode `MPI_Send`\n\nThe standard specifies only that the call returns when the send buffer can be safely reused. The runtime is free to implement this via:\n\n- **Eager protocol:** copy to internal buffer and return immediately (small messages).\n- **Rendezvous protocol:** wait for the matching receive to be posted (large messages).\n\n$$T_{\\text{standard}} = \\begin{cases} \\alpha + \\frac{n}{\\beta} & \\text{if eager protocol used} \\\\ \\alpha + 2 \\cdot \\alpha + \\frac{n}{\\beta} & \\text{if rendezvous protocol used} \\end{cases}$$\n\nThe choice is **implementation-defined**, which is why standard mode is generally not portable for performance-critical paths.\n\n### 3.2.2 Buffered Mode `MPI_Bsend`\n\nThe application must first call `MPI_Buffer_attach` to provide a user-managed buffer of size $B$. The send call completes once the message has been copied into this buffer:\n\n$$T_{\\text{buffered}} = \\alpha + \\frac{n}{\\beta_{\\text{mem}}}$$\n\nHere $\\beta_{\\text{mem}}$ is the **memory bandwidth** (typically $20$–$50$ GB/s), which is far higher than $\\beta_{\\text{network}}$. Hence, buffered mode is faster for short messages but **wastes memory** proportional to the application's communication volume.\n\n### 3.2.3 Synchronous Mode `MPI_Ssend`\n\nCompletion requires a *handshake* with the receiver. The total time includes an extra round-trip latency for synchronization:\n\n$$T_{\\text{synchronous}} = 2\\alpha + \\frac{n}{\\beta}$$\n\nThe factor of $2\\alpha$ arises because the sender must (a) send a control packet, (b) wait for the receiver's ready signal, and (c) push the data.\n\n### 3.2.4 Ready Mode `MPI_Rsend`\n\nIf the matching receive is already posted (which is the programmer's responsibility), no handshake is needed:\n\n$$T_{\\text{ready}} = \\alpha + \\frac{n}{\\beta}$$\n\nThis is the **fastest** mode but carries the **highest programming burden** — invoking `MPI_Rsend` without a matching receive is undefined behavior.\n\n## 3.3 Deadlock Analysis — The Cyclic Dependency Problem\n\nA **deadlock** occurs when two processes are each waiting for an event that only the other can produce. The classic pattern is:\n\n$$ \\text{Process 0: } \\texttt{MPI\\_Send}(\\text{data}, 1, ...) $$\n$$ \\text{Process 1: } \\texttt{MPI\\_Send}(\\text{data}, 0, ...) $$\n\nIf the runtime uses a **rendezvous** protocol for both sends, neither send can complete because each is waiting for the *other* process to post a receive. The system hangs indefinitely.\n\n**Solution via non-blocking primitives:**\n\nProcess 0: post `MPI_Isend`, then `MPI_Recv`.  \nProcess 1: post `MPI_Isend`, then `MPI_Recv`.  \n\nBoth sends return immediately; both receives eventually match. **Deadlock-free.**\n\n**Solution via `MPI_Sendrecv`:** MPI provides a single call that performs both a send and a receive, internally preventing cyclic dependency.\n\n## 3.4 Comprehensive Analytical Matrix\n\nThe following matrix summarizes the *exact* completion guarantees for each mode — this is the most-tested table in KTU ESE papers.\n\n| Mode | Returns When | Handshake | Buffer-Free Safe? | Cost (small msg) | Cost (large msg) |\n|------|--------------|-----------|-------------------|------------------|------------------|\n| Standard | Buffer reusable | Implementation-defined | Maybe | $\\alpha$ | $2\\alpha + n/\\beta$ |\n| Buffered | Copied to user buffer | No | **Yes (guaranteed)** | $\\alpha$ | $\\alpha + n/\\beta$ |\n| Synchronous | Receiver started receive | **Yes (full)** | Maybe | $2\\alpha$ | $2\\alpha + n/\\beta$ |\n| Ready | (assumed pre-posted receive) | None required | **No (UB if missing)** | $\\alpha$ | $\\alpha + n/\\beta$ |\n\n## 3.5 Latency Hiding Through Non-Blocking Communication\n\nNon-blocking sends allow the application to **overlap computation with communication**. The total time for a computation sandwiched between two non-blocking transfers becomes:\n\n$$T_{\\text{total}} = \\alpha_{\\text{iss}} + T_{\\text{comp}} + \\alpha_{\\text{wait}}$$\n\nwhere $\\alpha_{\\text{iss}}$ is the cost of issuing the non-blocking send (very small), $T_{\\text{comp}}$ is the computation time, and $\\alpha_{\\text{wait}}$ is the cost of waiting for completion. If $T_{\\text{comp}} \\geq t_{\\text{comm}}$, the communication is **fully hidden**.\n\n$$T_{\\text{overlapped}} = \\max(T_{\\text{comp}}, t_{\\text{comm}}) + \\alpha_{\\text{iss}} + \\alpha_{\\text{wait}}$$\n\nThis overlap is the primary reason MPI applications use `MPI_Isend` and `MPI_Irecv` in performance-critical loops.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Message Structure Block Diagram\n\nThe diagram below shows the internal structure of an MPI message, separating the envelope (metadata) from the payload (data).\n\n```mermaid\ngraph TD\n    A[\"MPI MESSAGE STRUCTURE\"] --> B[\"ENVELOPE SECTION\"]\n    A --> C[\"PAYLOAD SECTION\"]\n    B --> B1[\"SOURCE RANK\"]\n    B --> B2[\"DESTINATION RANK\"]\n    B --> B3[\"TAG INTEGER\"]\n    B --> B4[\"COMMUNICATOR HANDLE\"]\n    B --> B5[\"DATATYPE MAP\"]\n    C --> C1[\"BUFFER ADDRESS\"]\n    C --> C2[\"COUNT ELEMENTS\"]\n    C --> C3[\"ELEMENT TYPES\"]\n    C --> C4[\"RAW BYTES\"]\n    B1 --> D[\"USED BY NETWORK ROUTER\"]\n    B2 --> D\n    B3 --> E[\"USED BY MESSAGE MATCHING\"]\n    B4 --> F[\"USED BY PROCESS GROUP\"]\n    B5 --> G[\"USED BY TYPE CONVERSION\"]\n```\n\n## 4.2 Point-to-Point Communication Flow\n\nThis flowchart illustrates the complete lifecycle of a single point-to-point send/receive operation.\n\n```mermaid\ngraph LR\n    P0[\"PROCESS 0 SENDER\"] --> S1[\"MARSHAL DATA\"]\n    S1 --> S2[\"ATTACH ENVELOPE\"]\n    S2 --> S3[\"COPY TO NIC BUFFER\"]\n    S3 --> N1[\"NETWORK FABRIC\"]\n    N1 --> N2[\"ROUTING SWITCH\"]\n    N2 --> N3[\"NIC DESTINATION\"]\n    N3 --> R1[\"VERIFY ENVELOPE\"]\n    R1 --> R2[\"MATCH TAG AND RANK\"]\n    R2 --> R3[\"COPY TO USER BUFFER\"]\n    R3 --> R4[\"SIGNAL COMPLETION\"]\n    R4 --> P1[\"PROCESS 1 RECEIVER\"]\n```\n\n## 4.3 Communication Mode Decision Architecture\n\nUse the following block-level decision diagram to select the correct communication mode for a given application scenario.\n\n```mermaid\ngraph TD\n    Q1[\"NEED POINT-TO-POINT SEND?\"] --> Q2{\"IS RECEIVE PRE-POSTED?\"}\n    Q2 -->|YES| M1[\"USE MPI_RSEND READY MODE\"]\n    Q2 -->|NO| Q3{\"WANT STRONG SYNC GUARANTEE?\"}\n    Q3 -->|YES| M2[\"USE MPI_SSEND SYNCHRONOUS MODE\"]\n    Q3 -->|NO| Q4{\"NEED TO PREVENT DEADLOCK?\"}\n    Q4 -->|YES| M3[\"USE MPI_BSEND BUFFERED MODE\"]\n    Q4 -->|NO| M4[\"USE MPI_SEND STANDARD MODE\"]\n    M1 --> OUT[\"FASTEST BUT UNSAFE\"]\n    M2 --> OUT2[\"SLOWEST BUT SAFEST\"]\n    M3 --> OUT3[\"BALANCED MEMORY TRADE-OFF\"]\n    M4 --> OUT4[\"GENERAL PURPOSE DEFAULT\"]\n```\n\n## 4.4 Blocking vs. Non-Blocking Timeline\n\nThe timeline below contrasts blocking and non-blocking communication, showing how non-blocking primitives enable **computation/communication overlap**.\n\n```mermaid\ngraph LR\n    subgraph BLOCKING\n        B1[\"SEND CALL\"] --> B2[\"WAIT FOR TRANSFER\"]\n        B2 --> B3[\"RESUME COMPUTATION\"]\n    end\n    subgraph NONBLOCKING\n        N1[\"ISEND CALL\"] --> N2[\"COMPUTATION RUNS IN PARALLEL\"]\n        N2 --> N3[\"MPI_WAIT FOR COMPLETION\"]\n    end\n```\n\n## 4.5 Deadlock Scenario Visualization\n\nThe cyclic dependency that causes deadlock is illustrated below.\n\n```mermaid\ngraph TD\n    P0[\"PROCESS 0\"] -->|MPI_Send to 1, waits| W1[\"WAITING FOR ACK\"]\n    P1[\"PROCESS 1\"] -->|MPI_Send to 0, waits| W2[\"WAITING FOR ACK\"]\n    W1 --> P1\n    W2 --> P0\n    W1 --> DL[\"DEADLOCK HANG\"]\n    W2 --> DL\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## Part A — Short Answer Questions (3 Marks Each)\n\n### Question 1. `[KTU University Exam - Dec 2023]`\n\n**Explain the difference between blocking and non-blocking point-to-point communication in MPI.**\n\n**Model Answer (3 marks):**\n\nIn **blocking communication**, the send or receive call does not return to the application until the operation has *logically completed*. For blocking sends, this means the send buffer can be safely reused; for blocking receives, it means the message has arrived and the receive buffer contains valid data. Examples: `MPI_Send`, `MPI_Recv`.\n\nIn **non-blocking communication**, the call returns *immediately* with a request handle, allowing the application to perform computation concurrently with the data transfer. The application must subsequently call `MPI_Wait` or `MPI_Test` on the request handle to confirm completion. Examples: `MPI_Isend`, `MPI_Irecv`. (1 mark for definition each, 1 mark for examples and use-case distinction.)\n\n### Question 2. `[KTU University Exam - May 2024]`\n\n**What is a message tag in MPI? Why is it required?**\n\n**Model Answer (3 marks):**\n\nA **message tag** is a non-negative integer label (range $0 \\leq \\text{tag} \\leq 32767$) attached by the sender to identify the *purpose* or *context* of a message. (1 mark)\n\nIt is required because a process may receive messages from multiple sources or multiple messages from the same source; without tags, the receiver cannot distinguish between them. Tags act like *reference numbers* that allow the receiver to selectively match specific messages using `MPI_Recv` with an explicit tag, or use the wildcard constant `MPI_ANY_TAG` to accept any incoming message. (2 marks for justification)\n\n---\n\n## Part B — Long Answer Questions (14 Marks Each)\n\n### Question A. `[KTU University Exam - Dec 2022]`\n\n**(a)** Describe the **four communication modes** of MPI blocking point-to-point operations with their completion semantics. **(7 marks)**\n\n**(b)** Analyze a **classical deadlock scenario** in MPI point-to-point communication. Show how the same logic can be rewritten using `MPI_Sendrecv` to eliminate the deadlock, and derive the corresponding communication cost. **(7 marks)**\n\n#### Model Solution for Part (a)\n\nThe four blocking communication modes in MPI are:\n\n1. **Standard Mode (`MPI_Send`):** The send call returns when the send buffer can be safely reused. The MPI implementation is free to choose between an *eager protocol* (buffer internally and return) or a *rendezvous protocol* (handshake with receiver before sending). The exact behavior is **implementation-defined**, making standard mode unsuitable for performance-critical paths. (2 marks)\n\n2. **Buffered Mode (`MPI_Bsend`):** The application pre-allocates a buffer using `MPI_Buffer_attach`. The send call completes as soon as the message has been copied into this user buffer. This guarantees the send is **local and non-blocking** from the application's perspective, but consumes additional memory proportional to the total message volume. (2 marks)\n\n3. **Synchronous Mode (`MPI_Ssend`):** The send call completes only when the matching receive operation has *started receiving* the message. This requires a full handshake between sender and receiver, providing the strongest synchronization guarantee. It is the **slowest** mode for small messages but offers the most predictable behavior. (2 marks)\n\n4. **Ready Mode (`MPI_Rsend`):** The application guarantees that the matching receive has *already been posted* before invoking the send. If this guarantee is violated, the behavior is **undefined**. When used correctly, this is the **fastest** mode because no handshake is required. (1 mark)\n\n#### Model Solution for Part (b)\n\n**Deadlock Scenario:**\n\nConsider two processes exchanging data with blocking standard sends:\n\nProcess 0 code:\n```c\nMPI_Send(data_to_1, count, MPI_FLOAT, 1, 0, MPI_COMM_WORLD);\nMPI_Recv(data_from_1, count, MPI_FLOAT, 1, 0, MPI_COMM_WORLD, &status);\n```\n\nProcess 1 code:\n```c\nMPI_Send(data_to_0, count, MPI_FLOAT, 0, 0, MPI_COMM_WORLD);\nMPI_Recv(data_from_0, count, MPI_FLOAT, 0, 0, MPI_COMM_WORLD, &status);\n```\n\nIf the MPI implementation chooses the **rendezvous** protocol for both `MPI_Send` calls, neither send can proceed because:\n- Process 0's send waits for Process 1 to post a receive — but Process 1 is stuck in its own send.\n- Process 1's send waits for Process 0 to post a receive — but Process 0 is stuck in its own send.\n\nThis is the classic **cyclic wait** deadlock. (3 marks)\n\n**Solution using `MPI_Sendrecv`:**\n\n```c\nMPI_Sendrecv(data_to_1, count, MPI_FLOAT, 1, 0,\n             data_from_1, count, MPI_FLOAT, 1, 0,\n             MPI_COMM_WORLD, &status);\n```\n\nBoth processes execute a single call that atomically performs a send and a receive. Since the send and receive happen simultaneously within the same call, the runtime cannot deadlock. (2 marks)\n\n**Cost derivation:** The total communication time for the original (deadlocking) case under eager protocol would be $2 \\cdot (\\alpha + n/\\beta)$ — but this assumes non-blocking sends. Under rendezvous, the cost is unbounded (deadlock). Using `MPI_Sendrecv`, the cost is $2\\alpha + n/\\beta$ (one handshake plus one data transfer), which is **deterministic and deadlock-free**. (2 marks)\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall:** Students commonly lose marks by (1) confusing *communication mode* with *communication type* (blocking vs. non-blocking are types; standard/buffered/synchronous/ready are modes), (2) forgetting to specify which process posts the receive in deadlock scenarios, and (3) failing to mention that `MPI_Sendrecv` is the official deadlock-safe primitive. State these distinctions explicitly in your answer.\n\n---\n\n### Question B. `[KTU University Exam - May 2023]`\n\n**(a)** Derive the **Hockney communication model** $t_s = \\alpha + \\frac{n}{\\beta}$ from first principles. Explain the significance of the **half-bandwidth crossover point** $n_{1/2} = \\alpha \\beta$. **(7 marks)**\n\n**(b)** Compare **eager** and **rendezvous** protocols used internally by MPI for point-to-point communication. Under what circumstances does each perform better? Use the derived model to compute the threshold message size for switching protocols. **(7 marks)**\n\n#### Model Solution for Part (a)\n\n**Derivation:** A network communication operation consists of two distinct phases:\n\n**Phase 1 — Latency cost:** Before any data can be transmitted, the sender must (i) construct the message envelope, (ii) set up the DMA descriptor, (iii) signal the NIC, and (iv) propagate the first byte to the receiver. All of these steps take a fixed time $\\alpha$, **independent of message size**. (2 marks)\n\n**Phase 2 — Transfer cost:** Once the pipeline is established, each subsequent byte is transmitted at a rate determined by the physical link bandwidth $\\beta$ (in bytes per second). To push $n$ bytes through the pipeline, the time required is $\\frac{n}{\\beta}$. (2 marks)\n\nSince the two phases occur in sequence and are independent, the total communication time is their sum:\n\n$$t_s(n) = \\alpha + \\frac{n}{\\beta}$$\n\n**Significance of $n_{1/2}$:** Setting the two terms equal gives the crossover message size:\n\n$$\\alpha = \\frac{n_{1/2}}{\\beta} \\quad \\Longrightarrow \\quad n_{1/2} = \\alpha \\beta$$\n\nFor messages $n < n_{1/2}$, the **latency term dominates**, and the channel is said to be *latency-bound*. For $n > n_{1/2}$, the **transfer term dominates**, and the channel is *bandwidth-bound*. (2 marks for derivation + 1 mark for significance)\n\n#### Model Solution for Part (b)\n\n**Eager Protocol:** The sender immediately pushes the entire message into the network (or an internal buffer) without waiting for the receiver to post a matching receive. (1 mark)\n\n- **Cost:** $T_{\\text{eager}} = \\alpha + \\frac{n}{\\beta}$\n- **Advantage:** No handshake overhead — ideal for **small messages** where the handshake cost would dominate. (1 mark)\n- **Disadvantage:** Wastes memory if the receiver is not ready; can cause congestion for large messages. (1 mark)\n\n**Rendezvous Protocol:** The sender first sends a *ready-to-send* control packet and waits for the receiver to post a matching receive before transmitting the data payload. (1 mark)\n\n- **Cost:** $T_{\\text{rendezvous}} = 2\\alpha + \\frac{n}{\\beta}$\n- **Advantage:** Zero memory waste; receiver is guaranteed ready before transfer. (1 mark)\n- **Disadvantage:** The extra round-trip latency $\\alpha$ is prohibitive for small messages; can deadlock if no receive is posted. (1 mark)\n\n**Threshold Computation:** Setting $T_{\\text{eager}} = T_{\\text{rendezvous}}$ yields:\n\n$$\\alpha + \\frac{n}{\\beta} = 2\\alpha + \\frac{n}{\\beta}$$\n\nThis equation has **no finite solution** — eager is *always* faster than rendezvous on a single send! However, this ignores the fact that eager mode reserves buffer memory in the receiver. The practical threshold is therefore:\n\n$$n_{\\text{threshold}} \\approx \\frac{\\alpha \\beta}{2}$$\n\nor approximately the half-bandwidth point. For typical HPC systems with $\\alpha = 5 \\, \\mu s$ and $\\beta = 10$ GB/s, this gives $n_{\\text{threshold}} \\approx 25$ KB. (2 marks for threshold derivation and numerical interpretation)\n\n> [!WARNING]\n> **KTU Examiner's Pitfall:** Students often claim rendezvous is *never* better, ignoring memory pressure. In production MPI libraries, the switch is driven by a configurable **eager-limit** parameter — typically between $16$ KB and $256$ KB. State this practical nuance in your answer to earn full marks.\n\n---\n\n## Topic Recap & Important Things to Remember\n\n- **Point-to-Point Communication** involves exactly one sender and one receiver, forming the atomic primitive of all HPC communication.\n- A **message** has two parts: the **envelope** (routing metadata) and the **payload** (actual data).\n- **MPI defines 4 communication modes** for blocking sends: Standard, Buffered, Synchronous, and Ready — each differing in completion semantics.\n- **Standard mode** is implementation-defined; **Buffered** copies to user buffer; **Synchronous** requires receiver handshake; **Ready** assumes pre-posted receive.\n- **Blocking** operations do not return until logically complete; **Non-blocking** operations return immediately with a request handle.\n- **Tags** are non-negative integers (range $0$ to $32767$) used to distinguish messages; `MPI_ANY_TAG` is the wildcard.\n- **Sources** are identified by rank; `MPI_ANY_SOURCE` is the wildcard for receiving from any sender.\n- **Deadlock** occurs in cyclic send/receive patterns; use `MPI_Sendrecv` or non-blocking primitives to avoid it.\n- The **Hockney model** $t_s = \\alpha + n/\\beta$ is the fundamental performance equation; $n_{1/2} = \\alpha\\beta$ is the latency/bandwidth crossover.\n- **Non-blocking communication** enables overlap of computation with communication, hiding latency.\n- **MPI_Bsend** requires `MPI_Buffer_attach` before use; failing to detach causes memory leaks.\n- **MPI_Rsend** invoked without a matching receive is **undefined behavior** — never use it without verifying the receive is posted.\n- The **eager vs. rendezvous** protocol switch inside standard mode is controlled by the MPI library's `eager-limit` parameter.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "collectivecommunication": {
      "title": "Collective Communication",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 4\n> - **Topic:** Collective Communication\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Definition (KTU 2024 Syllabus Terminology)\n\n**Collective Communication** is defined as a class of **Message Passing Interface (MPI)** operations in which a group of processes, identified by a *communicator*, participate in a single coordinated data movement or synchronization operation. Unlike point-to-point communication, which involves exactly one sender and one receiver, collective communication is invoked by **all processes in the communicator** simultaneously and uses a unique *tag-space* that is distinct from point-to-point tags.\n\nThe fundamental operations belonging to this family are:\n\n- **Synchronization:** `MPI_Barrier`\n- **Data Movement (One-to-All / All-to-One):** `MPI_Bcast`, `MPI_Scatter`, `MPI_Gather`\n- **Data Movement (All-to-All):** `MPI_Allgather`, `MPI_Alltoall`\n- **Computation / Reduction:** `MPI_Reduce`, `MPI_Allreduce`, `MPI_Scan`, `MPI_Reduce_scatter`\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight:** Collective communication is not merely a \"convenience wrapper\" over point-to-point sends/receives. MPI libraries are **free to choose** the most efficient internal algorithm (binomial tree, ring, Bruck, pairwise exchange, etc.) for a given collective, depending on message size and number of processes. This is why the cost-model is **algorithm-dependent** and a favourite KTU exam topic.\n\n## 1.2 Intuitive Analogy (Plain-English Explanation)\n\nImagine a **classroom of 100 students** and the teacher is **Process 0**.\n\n- **Point-to-Point (P2P):** The teacher whispers a secret note to *only one* student. One sender, one receiver.\n- **Broadcast (`MPI_Bcast`):** The teacher announces \"Tomorrow is a holiday!\" to **every student**. *One source → All destinations.*\n- **Scatter (`MPI_Scatter`):** The teacher distributes 100 different exam papers, one per student. *One source → Each process gets a unique chunk.*\n- **Gather (`MPI_Gather`):** Each student submits their answer sheet, and the teacher collects all of them onto their desk. *All sources → One destination.*\n- **Allgather (`MPI_Allgather`):** Every student ends up with **a complete copy** of everyone's answer sheet.\n- **Reduce (`MPI_Reduce`):** Each student holds a number; the teacher adds them all up and announces the total. *All sources → One destination, with a combining operation.*\n- **Allreduce (`MPI_Allreduce`):** Same as reduce, but **every student now knows the total**, not just the teacher.\n- **Barrier (`MPI_Barrier`):** \"Nobody opens their exam booklet until **everyone** is seated.\" A synchronization point — *no data moves*, just timing.\n- **Scan (Prefix):** Cumulative sum along a process rank ordering.\n\n> [!NOTE]\n> **Why collective instead of loops of P2P?** Three reasons:\n> 1. **Optimisation** — The MPI runtime can use topology-aware algorithms (e.g., using shared memory when available).\n> 2. **Hardware-offload** — On InfiniBand, *Single-Sided RDMA* and `MPI_Sendrecv` are accelerated.\n> 3. **Cleaner code** — One call replaces `p` explicit sends/receives, reducing deadlocks and bugs.\n\n## 1.3 Physical Constants and Standard Metrics\n\n| Symbol | Meaning | Typical Magnitude (Modern HPC) | Unit |\n|:------:|:--------|:-------------------------------|:----:|\n| $\\alpha$ | **Message startup latency** | $1\\ \\mu s$ – $5\\ \\mu s$ | seconds |\n| $\\beta$ | **Per-word transfer time** (= $1/B$, where $B$ is bandwidth) | $\\sim 10^{-9}$ s/byte on InfiniBand | s/byte |\n| $m$ | **Message size** | bytes / words | bytes |\n| $p$ | **Number of processes** | $1$ to $10^6$ | dimensionless |\n| $B$ | **Network bandwidth** | $10$ – $200$ Gbps | bits/s |\n\n> [!TIP]\n> The **$\\alpha$–$\\beta$ model** (sometimes called the *Hockney model*) is the universal yardstick for predicting collective communication cost on KTU exams.\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Operational Breakdown of Each Collective\n\n### 2.1.1 `MPI_Bcast` (Broadcast) — One-to-All\n1. Root process initiates the call with data buffer.\n2. Data is disseminated to all $(p-1)$ other processes in the group.\n3. **Tree (binomial) algorithm:** Step $k$ doubles the number of informed processes; therefore $\\lceil \\log_2 p \\rceil$ steps.\n4. **Flat algorithm:** Root sends directly to all $(p-1)$ processes — used for small messages and small $p$.\n\n### 2.1.2 `MPI_Scatter` / `MPI_Scatterv` — One-to-All (partitioned)\n1. Root divides a buffer of size $m$ into $p$ chunks.\n2. Process $i$ receives chunk $i$.\n3. **Variants:** `Scatterv` allows *unequal* chunks (vector variant).\n\n### 2.1.3 `MPI_Gather` / `MPI_Gatherv` — All-to-One\n1. Each process contributes a chunk of size $m/p$ (typically).\n2. Root concatenates them in rank order.\n3. `Gatherv` allows variable-sized contributions.\n\n### 2.1.4 `MPI_Allgather` — All-to-All (replicated)\n1. Equivalent to `Gather` followed by `Bcast`, but **no intermediate root**.\n2. Every process ends with a complete copy of all data.\n\n### 2.1.5 `MPI_Alltoall` — Total Exchange\n1. Process $i$ sends a distinct chunk to every other process.\n2. Each process both **sends** $(p-1)$ chunks and **receives** $(p-1)$ chunks.\n3. **Most expensive collective** in terms of network traffic — total volume $\\approx m \\cdot (p-1)$.\n\n### 2.1.6 `MPI_Reduce` — All-to-One Reduction\n1. Each process applies a local value of size $m$.\n2. A reduction operator ($\\oplus$) — e.g., `MPI_SUM`, `MPI_MAX`, `MPI_LAND` — is applied globally.\n3. Result is delivered only to the root.\n\n### 2.1.7 `MPI_Allreduce` — Reduce + Bcast\n1. Reduction result is delivered to **every** process.\n2. Used in HPC for global sums, norms, and convergence checks.\n\n### 2.1.8 `MPI_Scan` — Inclusive Prefix Reduction\n1. Process $i$ receives the reduction of values from processes $0, 1, \\ldots, i$.\n2. Used in parallel prefix computations (e.g., load balancing).\n\n### 2.1.9 `MPI_Barrier` — Synchronization\n1. Each process blocks until **all** members of the group have entered the barrier.\n2. No data is moved; only control/synchronization.\n\n## 2.2 Communication Pattern Categories\n\n| Category | Operations | Topology Used (Typical) |\n|----------|-----------|------------------------|\n| **One-to-All** | Bcast, Scatter | Binomial tree, Flat |\n| **All-to-One** | Gather, Reduce | Binomial tree, Flat |\n| **All-to-All** | Allgather, Allreduce, Alltoall, Reduce_scatter | Ring, Bruck, Pairwise-exchange, Hypercube |\n| **Synchronization** | Barrier | Dissemination, Tournament, MCS |\n\n## 2.3 KTU High-Yield Formula Cheat Sheet (Hockney $\\alpha$–$\\beta$ Model)\n\n> [!IMPORTANT]\n> **Master this table — every KTU numerical problem on collectives reduces to applying these formulas.**\n\n| # | Operation | Algorithm | Total Time $T$ |\n|:-:|:---------:|:---------:|:--------------:|\n| 1 | Point-to-Point (Send/Recv) | — | $T_{p2p} = \\alpha + \\beta \\cdot m$ |\n| 2 | `MPI_Bcast` (one-to-all) | Flat | $T = (\\alpha + \\beta m)(p-1)$ |\n| 3 | `MPI_Bcast` (one-to-all) | **Binomial Tree** | $T = (\\alpha + \\beta m)\\lceil \\log_2 p \\rceil$ |\n| 4 | `MPI_Scatter` (one-to-all partitioned) | Binomial Tree | $T = \\alpha \\lceil \\log_2 p \\rceil + \\beta m \\left(1 - \\tfrac{1}{p}\\right)$ |\n| 5 | `MPI_Gather` (all-to-one) | Binomial Tree | $T = \\alpha \\lceil \\log_2 p \\rceil + \\beta m \\left(1 - \\tfrac{1}{p}\\right)$ |\n| 6 | `MPI_Allgather` | Ring | $T = \\alpha (p-1) + \\beta m (p-1)$ |\n| 7 | `MPI_Allgather` | Bruck (for small $m$, large $p$) | $T = \\alpha \\log p + \\beta m (p-1)$ |\n| 8 | `MPI_Reduce` | Binomial Tree (no in-network reduce) | $T = 2\\alpha \\lceil \\log_2 p \\rceil + \\beta m \\left(1 - \\tfrac{1}{p}\\right)$ |\n| 9 | `MPI_Allreduce` | Binomial + Bcast | $T = 2\\alpha \\lceil \\log_2 p \\rceil + 2\\beta m \\left(1 - \\tfrac{1}{p}\\right)$ |\n| 10 | `MPI_Reduce_scatter` | Ring | $T = \\alpha (p-1) + \\beta m \\left(1 - \\tfrac{1}{p}\\right)$ |\n| 11 | `MPI_Alltoall` | Pairwise Exchange (Brucks) | $T = \\alpha (p-1) + \\beta m (p-1)$ |\n| 12 | `MPI_Alltoall` | Hypercube / Bruck optimized | $T = \\alpha \\log p + \\beta m (p-1)$ |\n| 13 | `MPI_Barrier` | Centralised | $T = \\alpha (p-1)$ |\n| 14 | `MPI_Barrier` | Dissemination / Tree | $T = \\alpha \\lceil \\log_2 p \\rceil$ |\n\n> [!NOTE]\n> In the **hypercube** algorithm for $p$ processes (where $p$ is a power of 2, i.e., $p=2^d$), the number of steps is $d = \\log_2 p$. This is the conceptual foundation of many Allreduce implementations (e.g., `Open MPI`'s `KNEM`).\n\n## 2.4 The \"Why\" Behind the Algorithm Choice\n\n- **Small messages + Large $p$** → Tree algorithms dominate (startup $\\alpha$ is the bottleneck; minimising $\\log p$ beats minimising $p$).\n- **Large messages + Small $p$** → Flat / Ring algorithms can be better (bandwidth $\\beta$ dominates; overlapping concurrent links help).\n- **Reduction operations** → If the network supports *in-network reduction* (e.g., InfiniBand's `MPI_Allreduce` via SHARP), cost is bounded by $\\sim \\alpha \\log p + \\beta m$.\n\n## 2.5 Real-World Engineering Utility\n\n| Domain | Application | Collective Used |\n|--------|-------------|-----------------|\n| **Climate Modelling** | Global energy sum across MPI ranks | `MPI_Allreduce` |\n| **Deep Learning** | Gradient averaging in data-parallel SGD | `MPI_Allreduce` (NCCL/RCCL wraps this) |\n| **CFD** | Halo-exchange between sub-domains | `MPI_Sendrecv` (point-to-point) + `MPI_Barrier` |\n| **N-Body Simulation** | Force matrix transposition | `MPI_Alltoall` |\n| **Graph Algorithms** | Frontier aggregation in BFS | `MPI_Allreduce` for min-distance |\n| **Sparse Linear Algebra** | Sparse matrix-vector multiply load balancing | `MPI_Alltoallv` |\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation of Broadcast Cost (Binomial Tree Algorithm)\n\nWe derive the cost of `MPI_Bcast` using a **recursive-doubling (binomial) tree** on $p$ processes, where $p$ is a power of 2 for clarity.\n\n### Setup\n- Let $p = 2^d$ processes, indexed $0, 1, \\ldots, p-1$.\n- Root = Process 0 holds initial message of size $m$.\n- $\\alpha$ = startup cost, $\\beta$ = per-word transfer cost.\n\n### Step 1: Stage 0 (initial state)\nProcess 0 (the root) has the data. The remaining $p - 1$ processes do not.\n\n### Step 2: Stage 1 (after step 1)\nProcess 0 sends the message to Process 1. Now 2 processes have the data. **Cost incurred:** $\\alpha + \\beta m$.\n\n### Step 3: Stage 2 (after step 2)\nThe 2 informed processes each send to a new partner:\n- Process 0 → Process 2\n- Process 1 → Process 3\n\nNow 4 processes have the data. **Cost incurred:** $\\alpha + \\beta m$.\n\n### Step 4: Inductive pattern\nAt each subsequent step $k$, every informed process sends to exactly one new partner. The number of *newly informed* processes at step $k$ equals the number of already-informed processes at the start of step $k$. So the number of *informed* processes doubles every step.\n\n$$\n\\begin{aligned}\n\\text{Processes informed at step } k &= 2^{k+1} \\\\\n\\text{Steps required to inform all } p \\text{ processes} &= \\log_2 p\n\\end{aligned}\n$$\n\n### Step 5: Cost at each step\nEach step involves one send of size $m$ **per informed sender**; in a *non-overlapping* implementation, the cost of step $k$ is the cost of one send:\n\n$$\nT_{\\text{step}} = \\alpha + \\beta m\n$$\n\n### Step 6: Total cost\nSumming across all $\\log_2 p$ steps:\n\n$$\n\\begin{aligned}\nT_{Bcast} &= \\sum_{k=0}^{\\log_2 p - 1} (\\alpha + \\beta m) \\\\\n&= (\\alpha + \\beta m) \\cdot \\log_2 p\n\\end{aligned}\n$$\n\n### Step 7: Generalisation to non-power-of-two\nIf $p$ is not a power of 2, use $\\lceil \\log_2 p \\rceil$:\n\n$$\n\\boxed{\\,T_{Bcast}^{\\text{tree}} = (\\alpha + \\beta m)\\lceil \\log_2 p \\rceil\\,}\n$$\n\n### Step 8: Comparison with flat algorithm\nFor the *flat* algorithm (root sends to $p-1$ destinations, possibly pipelined):\n\n$$\n\\boxed{\\,T_{Bcast}^{\\text{flat}} = (\\alpha + \\beta m)(p - 1)\\,}\n$$\n\nThe crossover point (where tree beats flat) is when $\\log_2 p < p - 1$, i.e., for all $p \\ge 3$ — confirming that the tree algorithm is **almost always preferred**, especially as $p$ grows.\n\n## 3.2 Derivation of Allreduce Cost (Ring Algorithm)\n\nWe now derive the cost of `MPI_Allreduce` using a **ring** algorithm, where each process forwards the partial reduction result around a logical ring.\n\n### Setup\n- $p$ processes arranged in a logical ring $0 \\to 1 \\to 2 \\to \\cdots \\to (p-1) \\to 0$.\n- Each process begins with a local value of size $m$.\n- Reduction operator: $\\oplus$ (assumed associative & commutative, e.g., SUM).\n\n### Step 1: Communication model\nIn each step, every process:\n1. Sends its accumulated partial result to its successor.\n2. Receives a partial result from its predecessor.\n3. Reduces the received result with its accumulated value.\n\n### Step 2: Cost per step\nEach step has one send and one receive of the **same accumulated size $m$**:\n\n$$\nT_{\\text{step}} = \\alpha + \\beta m\n$$\n\n### Step 3: Number of steps\nAfter $k$ steps, process $i$ has received partial sums from processes $(i-1), (i-2), \\ldots, (i-k)$ (mod $p$). To receive from **all** $p-1$ other processes:\n\n$$\n\\text{Steps required} = p - 1\n$$\n\n### Step 4: Total cost\n$$\n\\begin{aligned}\nT_{Allreduce}^{ring} &= (p - 1) \\cdot (\\alpha + \\beta m) \\\\\n&= \\alpha(p-1) + \\beta m (p-1)\n\\end{aligned}\n$$\n\n### Step 5: Refinement — segmented ring for large messages\nIf $m$ is large, we can split the data into $s$ segments and pipeline them. The optimal $s$ reduces the per-step cost to $\\sim \\beta m / s + \\alpha$, but increases steps to $s + p - 2$. Optimising gives a balanced term — the famous *Thakur–Gropp–Rabenseifner* analysis.\n\n### Step 6: Comparison with binomial-tree allreduce\nFor a binomial-tree-based allreduce (used in many NCCL/SYCL implementations):\n\n$$\nT_{Allreduce}^{\\text{tree}} = 2\\alpha \\log_2 p + 2\\beta m \\left(1 - \\tfrac{1}{p}\\right)\n$$\n\nThe factor of $2$ on the data term arises because the reduction is followed by a broadcast of the result.\n\n## 3.3 Numerical Worked Example (KTU-Style Problem)\n\n> **Problem:** Consider 8 processes running on a cluster with $\\alpha = 50\\ \\mu s$ and $\\beta = 0.01\\ \\mu s/\\text{byte}$. A broadcast of a $1\\ \\text{MB}$ message is performed. Compute the time taken by (a) the flat algorithm and (b) the binomial tree algorithm. State which is faster.\n\n**Given:**\n- $p = 8$\n- $\\alpha = 50 \\times 10^{-6}\\ \\text{s}$\n- $\\beta = 0.01 \\times 10^{-6}\\ \\text{s/byte} = 10^{-8}\\ \\text{s/byte}$\n- $m = 1\\ \\text{MB} = 10^6\\ \\text{bytes}$\n\n**(a) Flat algorithm:**\n\n$$\n\\begin{aligned}\nT_{\\text{flat}} &= (\\alpha + \\beta m)(p - 1) \\\\\n&= (50 \\times 10^{-6} + 10^{-8} \\times 10^6)(8 - 1) \\\\\n&= (50 \\times 10^{-6} + 10^{-2})(7) \\\\\n&= (0.000050 + 0.010000)(7) \\\\\n&= 0.010050 \\times 7 \\\\\n&= 0.07035\\ \\text{s}\n\\end{aligned}\n$$\n\n**(b) Binomial Tree algorithm:**\n\n$$\n\\begin{aligned}\nT_{\\text{tree}} &= (\\alpha + \\beta m)\\lceil \\log_2 8 \\rceil \\\\\n&= (50 \\times 10^{-6} + 10^{-2})(\\log_2 8) \\\\\n&= (0.000050 + 0.010000)(3) \\\\\n&= 0.010050 \\times 3 \\\\\n&= 0.03015\\ \\text{s}\n\\end{aligned}\n$$\n\n**Conclusion:**\n\n$$\nT_{\\text{tree}} = 0.03015\\ \\text{s} < T_{\\text{flat}} = 0.07035\\ \\text{s}\n$$\n\nThe binomial tree algorithm is **$\\approx 2.33\\times$ faster** in this case. The gap widens dramatically for larger $p$.\n\n## 3.4 Analytical Matrix — Algorithm-to-Workload Mapping\n\n| Message size $m$ / # Processes $p$ | Small $m$ | Medium $m$ | Large $m$ |\n|:--:|:--:|:--:|:--:|\n| **Small $p$ ($\\le 8$)** | Binomial Bcast | Ring / Bruck | Pipeline / Ring |\n| **Medium $p$ (16–128)** | Binomial tree | Pairwise exchange | Ring with segmentation |\n| **Large $p$ ($\\ge 256$)** | Dissemination / Bruck | Bruck + ring | RDMA + topology-aware (e.g., `Portals`) |\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Binomial-Tree Broadcast for $p = 8$\n\n```mermaid\nflowchart TD\n    P0[\"P0 ROOT has data\"]:::root\n    P1[\"P1\"]:::leaf\n    P2[\"P2\"]:::leaf\n    P3[\"P3\"]:::leaf\n    P4[\"P4\"]:::leaf\n    P5[\"P5\"]:::leaf\n    P6[\"P6\"]:::leaf\n    P7[\"P7\"]:::leaf\n\n    S0[\"Step 1: P0 -> P1\"]:::step\n    S1[\"Step 2: P0 -> P2, P1 -> P3\"]:::step\n    S2[\"Step 3: P0 -> P4, P1 -> P5, P2 -> P6, P3 -> P7\"]:::step\n\n    P0 -- S0 --> P1\n    P0 -- S1 --> P2\n    P1 -- S1 --> P3\n    P0 -- S2 --> P4\n    P1 -- S2 --> P5\n    P2 -- S2 --> P6\n    P3 -- S2 --> P7\n\n    classDef root fill:#ff6b6b,stroke:#900,stroke-width:3px,color:#fff\n    classDef leaf fill:#4ecdc4,stroke:#066,stroke-width:1px,color:#000\n    classDef step fill:#ffe66d,stroke:#aa0,stroke-width:1px,color:#000\n```\n\n## 4.2 Ring Allreduce — Partial Sum Circulation\n\n```mermaid\nflowchart LR\n    R0[\"P0: a0\"]:::p0\n    R1[\"P1: a1\"]:::p1\n    R2[\"P2: a2\"]:::p2\n    R3[\"P3: a3\"]:::p3\n\n    S1[\"Step 1: P0->P1, P1->P2, P2->P3, P3->P0\"]:::step\n    S2[\"Step 2: continue circulation\"]:::step\n    S3[\"Step 3: final reduction complete\"]:::step\n\n    R0 -- S1 --> R1\n    R1 -- S1 --> R2\n    R2 -- S1 --> R3\n    R3 -- S1 --> R0\n\n    R0 -- S2 --> R1\n    R1 -- S2 --> R2\n    R2 -- S2 --> R3\n    R3 -- S2 --> R0\n\n    R0 -- S3 --> R1\n    R1 -- S3 --> R2\n    R2 -- S3 --> R3\n    R3 -- S3 --> R0\n\n    classDef p0 fill:#e74c3c,stroke:#600,color:#fff\n    classDef p1 fill:#3498db,stroke:#036,color:#fff\n    classDef p2 fill:#2ecc71,stroke:#060,color:#fff\n    classDef p3 fill:#f39c12,stroke:#a60,color:#fff\n    classDef step fill:#fff,stroke:#333,stroke-dasharray: 5 5,color:#000\n```\n\n## 4.3 Functional Block Architecture — Collective Communication Stack\n\n```mermaid\nflowchart TB\n    APP[\"Application Layer<br/>Scientific Code, ML, CFD\"]:::app\n    API[\"MPI Collective API<br/>Bcast, Reduce, Allreduce, Alltoall, Barrier\"]:::api\n    OPT[\"Algorithm Selector<br/>tree, ring, bruck, pairwise\"]:::opt\n    HW[\"Hardware Layer<br/>Shared Mem, TCP, InfiniBand, NVLink, RoCE\"]:::hw\n\n    APP --> API\n    API --> OPT\n    OPT --> HW\n\n    classDef app fill:#1abc9c,stroke:#066,color:#fff\n    classDef api fill:#3498db,stroke:#036,color:#fff\n    classDef opt fill:#9b59b6,stroke:#306,color:#fff\n    classDef hw fill:#e67e22,stroke:#a60,color:#fff\n```\n\n## 4.4 Collective Operation Classification Map\n\n```mermaid\nflowchart TB\n    CC[\"Collective Communication\"]:::root\n\n    SYNC[\"Synchronization\"]:::sync\n    DM[\"Data Movement\"]:::dm\n    RD[\"Reduction\"]:::rd\n\n    BCAST[\"MPI Bcast\"]:::dmop\n    SCAT[\"MPI Scatter\"]:::dmop\n    GAT[\"MPI Gather\"]:::dmop\n    AGAT[\"MPI Allgather\"]:::dmop\n    A2A[\"MPI Alltoall\"]:::dmop\n\n    RED[\"MPI Reduce\"]:::rdop\n    ARED[\"MPI Allreduce\"]:::rdop\n    SCN[\"MPI Scan\"]:::rdop\n    RDS[\"MPI Reduce Scatter\"]:::rdop\n\n    BAR[\"MPI Barrier\"]:::syncop\n\n    CC --> SYNC\n    CC --> DM\n    CC --> RD\n\n    SYNC --> BAR\n    DM --> BCAST\n    DM --> SCAT\n    DM --> GAT\n    DM --> AGAT\n    DM --> A2A\n    RD --> RED\n    RD --> ARED\n    RD --> SCN\n    RD --> RDS\n\n    classDef root fill:#2c3e50,stroke:#000,color:#fff\n    classDef sync fill:#f1c40f,stroke:#a80,color:#000\n    classDef dm fill:#3498db,stroke:#036,color:#fff\n    classDef rd fill:#e74c3c,stroke:#600,color:#fff\n    classDef dmop fill:#5dade2,stroke:#036,color:#000\n    classDef rdop fill:#ec7063,stroke:#600,color:#000\n    classDef syncop fill:#f9e79f,stroke:#a80,color:#000\n```\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n---\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### **Q1.** `[KTU University Exam — May 2024, Model]`\n**Define collective communication. List any four standard MPI collective operations.**\n\n**Model Answer:**\n\nCollective communication refers to communication operations in MPI that involve **all processes within a communicator** simultaneously. Unlike point-to-point communication, the call must be made by **every process** in the group, and the operation may involve data movement, computation, or synchronization across the entire group.\n\n**Four standard MPI collective operations:**\n\n1. **`MPI_Bcast`** — Broadcasts a message from one root process to all other processes in the group.\n2. **`MPI_Reduce`** — Combines data from all processes using a reduction operator (e.g., `MPI_SUM`) and delivers the result to a single root process.\n3. **`MPI_Allreduce`** — Performs a reduction and broadcasts the result to **every** process in the group.\n4. **`MPI_Barrier`** — Synchronizes all processes; no process proceeds past the barrier until all have reached it.\n\n*(Other acceptable answers: `MPI_Scatter`, `MPI_Gather`, `MPI_Allgather`, `MPI_Alltoall`, `MPI_Scan`.)*\n\n---\n\n### **Q2.** `[KTU University Exam — Dec 2023, Model]`\n**Distinguish between `MPI_Bcast` and `MPI_Scatter` with a suitable example.**\n\n**Model Answer:**\n\n| Aspect | `MPI_Bcast` | `MPI_Scatter` |\n|--------|-------------|---------------|\n| **Data Distribution** | Every process receives an **identical copy** of the data | Each process receives a **unique, disjoint portion** of the data |\n| **Data Volume at Root** | Full message $m$ | Full message $m$ (divided into $p$ chunks) |\n| **Data Volume per Process** | Full message $m$ | Message chunk of size $m/p$ |\n| **Cardinality** | One-to-All (replicated) | One-to-All (partitioned) |\n\n**Example (with $p = 4$ and $m = 16$):**\n- **Bcast:** All four processes receive the entire 16-element array.\n- **Scatter:** Process 0 holds the 16 elements; Process 0 receives elements 0–3, Process 1 receives 4–7, Process 2 receives 8–11, Process 3 receives 12–15.\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks with Internal Choice)\n\n---\n\n### **Question A (14 Marks)** `[KTU University Exam — May 2024, Model]`\n\n**(a) [7 Marks]** Explain the **binomial tree algorithm** for `MPI_Bcast` with an example for $p = 8$ processes. Derive the cost formula for the algorithm using the $\\alpha$–$\\beta$ model.\n\n**(b) [7 Marks]** Compare `MPI_Reduce`, `MPI_Allreduce`, and `MPI_Alltoall` in terms of communication volume and typical use cases.\n\n---\n\n#### Model Solution for (a):\n\n**Binomial Tree Algorithm for Broadcast (7 marks):**\n\nThe binomial tree algorithm recursively doubles the set of informed processes. At step $k$, the number of processes that possess the message becomes $2^{k+1}$.\n\n**Example for $p = 8$ processes:**\n\n- **Step 1:** Root (P0) sends to P1. → 2 processes informed.\n- **Step 2:** P0 sends to P2; P1 sends to P3. → 4 processes informed.\n- **Step 3:** P0→P4, P1→P5, P2→P6, P3→P7. → 8 processes informed.\n\n**Total steps = $\\log_2 8 = 3$**.\n\n**Cost Derivation:**\n\nIn a non-overlapping (sequential) implementation, each step has one send of size $m$ from each informed process. The cost of one step is:\n\n$$\nT_{\\text{step}} = \\alpha + \\beta m\n$$\n\nTotal cost over $\\log_2 p$ steps:\n\n$$\n\\begin{aligned}\nT_{Bcast} &= (\\alpha + \\beta m) \\cdot \\log_2 p \\\\\n&= (\\alpha + \\beta m) \\lceil \\log_2 p \\rceil \\quad \\text{(for non-power-of-2)}\n\\end{aligned}\n$$\n\nIf links are *pipelined* (overlapped), the per-step bandwidth term may reduce; however, $\\alpha$ remains dominant.\n\n#### Model Solution for (b):\n\n**Comparison of `MPI_Reduce`, `MPI_Allreduce`, and `MPI_Alltoall` (7 marks):**\n\n| Feature | `MPI_Reduce` | `MPI_Allreduce` | `MPI_Alltoall` |\n|---------|--------------|------------------|------------------|\n| **Data Flow** | All → One | All → All (with reduction) | All → All (no reduction) |\n| **Output Recipient** | Only root | Every process | Every process |\n| **Operation Type** | Reduction ($\\oplus$) | Reduction ($\\oplus$) | Plain data exchange |\n| **Comm. Volume** | $m$ (final result) | $m \\cdot p$ (replicated) | $m \\cdot p$ (distinct data) |\n| **Network Traffic** | $m \\cdot (p-1)$ worst case | $m \\cdot 2(p-1)$ worst case | $m \\cdot (p-1)$ per process |\n| **Cost (Hockney, tree)** | $2\\alpha \\log p + \\beta m (1 - 1/p)$ | $2\\alpha \\log p + 2\\beta m (1 - 1/p)$ | $\\alpha (p-1) + \\beta m (p-1)$ (ring) |\n| **Typical Use Case** | Global energy/integral summation at master | Gradient averaging in distributed ML | Sparse matrix transpose, FFT transpose |\n| **Example** | Total kinetic energy per timestep | Average gradient across all GPUs | Halo exchange in spectral methods |\n\n**Key takeaway:** `MPI_Alltoall` is the most network-intensive; `MPI_Allreduce` is the most communication-balanced and is the workhorse of HPC and ML.\n\n---\n\n### **Question B (14 Marks)** `[KTU University Exam — Dec 2023, Model — Alternative Choice]`\n\n**(a) [7 Marks]** With neat sketch, explain the **ring algorithm** for `MPI_Allreduce`. Derive its cost using the $\\alpha$–$\\beta$ model and identify the conditions under which it outperforms the binomial tree algorithm.\n\n**(b) [7 Marks]** A parallel program running on 32 processes performs an `MPI_Allreduce` of a $2\\ \\text{MB}$ message. Given $\\alpha = 10\\ \\mu s$ and $\\beta = 5 \\times 10^{-9}\\ \\text{s/byte}$, compute the time taken by (i) the **binomial tree** algorithm and (ii) the **ring** algorithm. State which is faster and by what factor.\n\n---\n\n#### Model Solution for (a):\n\n**Ring Algorithm for `MPI_Allreduce` (7 marks):**\n\nIn the ring algorithm, processes are arranged in a logical ring. Each process sends its accumulated partial reduction to its successor and receives a partial reduction from its predecessor, which it merges with its local value.\n\n**Algorithm Steps:**\n1. Initially, process $i$ holds local value $a_i$ of size $m$.\n2. At each of $p-1$ steps, process $i$ sends its accumulated value $S_i^{(k)}$ to process $(i+1) \\mod p$ and receives from process $(i-1) \\mod p$, updating:\n   $$S_i^{(k+1)} = S_i^{(k)} \\oplus a_{(i-1-k)\\mod p}$$\n3. After $p - 1$ steps, every process holds the full reduction.\n\n**Cost Derivation (Hockney Model):**\n\n- Cost per step: one send + one receive of size $m$ → $\\alpha + \\beta m$.\n- Number of steps: $p - 1$.\n\n$$\nT_{Allreduce}^{\\text{ring}} = (p - 1)(\\alpha + \\beta m)\n$$\n\n**When Ring outperforms Binomial Tree:**\nThe binomial tree cost is approximately $2\\alpha \\log_2 p + 2\\beta m (1 - 1/p)$. Comparing the $\\beta m$ terms, ring has $(p-1)\\beta m$ while tree has $\\approx 2\\beta m$. The ring's bandwidth term is **$O(p)$ times larger**, so:\n\n- **Ring is better when:** $\\alpha$ is very small (low-latency network) and message $m$ is large, *or* when the network's bisection bandwidth is high.\n- **Tree is better when:** $p$ is large and message size is small (latency-bound regime).\n\nIn modern HPC (InfiniBand NDR, NVLink), both are competitive and **pipelined segmented rings** close the gap further.\n\n#### Model Solution for (b):\n\n**Numerical Computation (7 marks):**\n\n**Given:**\n- $p = 32$\n- $\\alpha = 10 \\times 10^{-6}\\ \\text{s}$\n- $\\beta = 5 \\times 10^{-9}\\ \\text{s/byte}$\n- $m = 2\\ \\text{MB} = 2 \\times 10^6\\ \\text{bytes}$\n\n**(i) Binomial Tree Algorithm:**\n\n$$\n\\begin{aligned}\nT_{\\text{tree}} &= 2\\alpha \\lceil \\log_2 32 \\rceil + 2\\beta m \\left(1 - \\tfrac{1}{p}\\right) \\\\\n&= 2 \\times 10 \\times 10^{-6} \\times 5 + 2 \\times 5 \\times 10^{-9} \\times 2 \\times 10^6 \\times \\left(1 - \\tfrac{1}{32}\\right) \\\\\n&= 100 \\times 10^{-6} + 2 \\times 5 \\times 10^{-9} \\times 2 \\times 10^6 \\times 0.96875 \\\\\n&= 1.00 \\times 10^{-4} + 20 \\times 10^{-3} \\times 0.96875 \\\\\n&= 0.0001 + 0.019375 \\\\\n&= 0.019475\\ \\text{s}\n\\end{aligned}\n$$\n\n**(ii) Ring Algorithm:**\n\n$$\n\\begin{aligned}\nT_{\\text{ring}} &= (p - 1)(\\alpha + \\beta m) \\\\\n&= 31 \\times (10 \\times 10^{-6} + 5 \\times 10^{-9} \\times 2 \\times 10^6) \\\\\n&= 31 \\times (10 \\times 10^{-6} + 0.01) \\\\\n&= 31 \\times 0.010010 \\\\\n&= 0.31031\\ \\text{s}\n\\end{aligned}\n$$\n\n**Comparison:**\n\n$$\n\\frac{T_{\\text{ring}}}{T_{\\text{tree}}} = \\frac{0.31031}{0.019475} \\approx 15.93\n$$\n\n**Conclusion:** The **binomial tree** algorithm is approximately **$15.93\\times$ faster** than the ring algorithm for this configuration. This dramatic speedup is because the message size ($2\\ \\text{MB}$) is large but $p$ is moderate, making the $\\log p$ scaling of the tree vastly superior to the linear $p$ scaling of the ring.\n\n---\n\n> [!WARNING]\n> **KTU Examiner's Valuation Pitfall — Where Students Lose Marks:**\n>\n> 1. **Forgetting the ceiling operator:** When $p$ is not a power of 2 (e.g., $p = 12$), always use $\\lceil \\log_2 p \\rceil$ — not $\\log_2 p$.\n> 2. **Mixing up the data-volume factors:** Reduce delivers $m$ to *one* process; Allreduce delivers $m$ to *every* process — the data term in cost is **multiplied by 2 for Allreduce** (reduce + broadcast).\n> 3. **Unit errors:** $\\beta$ is often given in s/byte, but sometimes in s/word (8 bytes). Always check and convert $m$ to consistent units.\n> 4. **Ignoring the $(1 - 1/p)$ factor:** Scatter/Gather do **not** transmit the full $m$ to every process — the total data communicated is $m(p-1)/p$, not $m(p-1)$.\n> 5. **Confusing Allgather and Alltoall:** Allgather **replicates** data (every process ends with the same complete set), while Alltoall **exchanges** distinct data (each process ends with a *different* combined set).\n\n---\n\n## 5.3 Topic Recap — Rapid Revision Checklist\n\n- [x] **Collective vs Point-to-Point:** Collective involves *all* processes of a communicator in one call; P2P involves exactly two.\n- [x] **Main Operations:** Bcast, Scatter, Gather, Allgather, Reduce, Allreduce, Scan, Alltoall, Barrier.\n- [x] **Cost Model:** Use Hockney $\\alpha$–$\\beta$ model; remember $T_{p2p} = \\alpha + \\beta m$.\n- [x] **Bcast (Tree):** $T = (\\alpha + \\beta m)\\lceil \\log_2 p \\rceil$.\n- [x] **Bcast (Flat):** $T = (\\alpha + \\beta m)(p - 1)$.\n- [x] **Scatter / Gather (Tree):** $T = \\alpha \\lceil \\log_2 p \\rceil + \\beta m (1 - 1/p)$.\n- [x] **Allgather (Ring):** $T = \\alpha(p-1) + \\beta m (p-1)$.\n- [x] **Reduce (Tree):** $T = 2\\alpha \\lceil \\log_2 p \\rceil + \\beta m (1 - 1/p)$.\n- [x] **Allreduce (Tree):** $T = 2\\alpha \\lceil \\log_2 p \\rceil + 2\\beta m (1 - 1/p)$.\n- [x] **Allreduce (Ring):** $T = (p-1)(\\alpha + \\beta m)$.\n- [x] **Alltoall (Ring):** $T = (p-1)(\\alpha + \\beta m/p)$ per send direction; total traffic $\\sim m(p-1)$.\n- [x] **Barrier:** Pure synchronization; tree algorithm gives $\\alpha \\log_2 p$, centralised gives $\\alpha(p-1)$.\n- [x] **Algorithm Selection Rule of Thumb:**\n  - Small $m$, large $p$ → **Tree / Bruck / Dissemination**\n  - Large $m$, small $p$ → **Ring / Pipelined Ring**\n  - Reduction-heavy → **Binomial Tree with in-network combine**\n- [x] **Use Cases to Memorise:** Allreduce → ML gradient sync; Alltoall → FFT transpose; Bcast → distributing parameters; Barrier → timestep synchronisation.\n- [x] **MPI 4.0 Features (advanced):** Non-blocking collectives (`MPI_Ibcast`, `MPI_Iallreduce`), persistent collectives (`MPI_Bcast_init`), partitioned communication — exam favourite.\n- [x] **Pitfalls:** Forgetting to call collective on **every** process → deadlock or undefined behaviour. `MPI_IN_PLACE` for in-situ reductions.\n\n> [!TIP]\n> **Last-minute exam mantra:**\n> *Tree* scales as $\\log p$, *Ring* scales as $p$. For Allreduce, always write the binomial-tree cost with the factor of $2$ on the bandwidth term. Master the worked example in §3.3 — it's the template for any numerical KTU question.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "nonblockingpointtopointcommunication": {
      "title": "Nonblocking Point-to-Point Communication",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 4\n> - **Topic:** Nonblocking Point-to-Point Communication\n\n<!-- SECTION_1_START -->\n# 1. Core Technical Definition & Intuitive Overview\n\n## 1.1 Formal Academic Definition\n\n**Nonblocking Point-to-Point Communication** is a class of Message Passing Interface (MPI) communication operations in which the communication call (such as `MPI_Isend` or `MPI_Irecv`) returns **immediately** to the calling process, without waiting for the message to be actually transmitted or received. The operation hands back an opaque **communication request object** (`MPI_Request`) that the programmer must subsequently probe (`MPI_Test`) or wait on (`MPI_Wait`) to confirm completion.\n\nIn the precise terminology of the **MPI Standard 3.1/4.0** and the **KTU 2024 Scheme syllabus (PECST757, Module 4)**, nonblocking point-to-point communication is defined as:\n\n> A communication mechanism that **decouples the issuance of the data-transfer request from its actual execution**, allowing the calling process to continue computation or issue further communication operations while the underlying transport layer handles data movement asynchronously.\n\nThe three foundational properties of a nonblocking call are:\n\n1. **Local Non-Blocking:** The function returns *as soon as the call arguments are copied internally* — buffers may be modified by the user before actual transfer completes (only safe after a successful `MPI_Wait`/`MPI_Test`).\n2. **Asynchronous Progress:** Data movement occurs concurrently with the calling process's execution.\n3. **Explicit Completion:** Completion is verified only through request objects (`MPI_Request`) and the `MPI_Wait*` / `MPI_Test*` family of functions.\n\n> [!IMPORTANT]\n> **KTU 2024 Highlight (Module 4):** Nonblocking communication is the *primary mechanism* for **latency hiding** and **communication–computation overlap** in HPC applications. It is the cornerstone of scalable parallel algorithm design.\n\n## 1.2 Conceptual Analogy / Intuition\n\nImagine two office workers, **Alice** (sender) and **Bob** (receiver), separated by a long corridor:\n\n- **Blocking Send (MPI_Send):** Alice prepares a box of documents, walks down the corridor, hands the box to Bob, and only then returns to her desk. Alice is *idle* during the entire walk.\n- **Nonblocking Send (MPI_Isend):** Alice hands the sealed box to an **intern (MPI runtime)** and immediately returns to her desk to continue other work. The intern carries the box to Bob. Alice can ask later: *\"Is the box delivered yet?\"* — that is `MPI_Test`, or *\"Wait until the box is delivered\"* — that is `MPI_Wait`.\n- **Nonblocking Receive (MPI_Irecv):** Bob tells the intern, *\"When a box from Alice arrives, set it on my desk and notify me.\"* Bob keeps working on other tasks. He checks the desk later with `MPI_Test` or blocks on `MPI_Wait` until the box arrives.\n\nThis \"**fire-and-forget-with-tracking**\" semantics is exactly what makes nonblocking communication powerful: **Alice and Bob do not waste CPU cycles waiting on the corridor**.\n\n## 1.3 Standard Metrics & Constants\n\nThe following standard HPC performance metrics govern nonblocking communication:\n\n| Symbol | Meaning | Typical Magnitude |\n|:------:|:--------|:-----------------:|\n| **T₀ (s)** | Message **latency / startup overhead** | 1–10 µs |\n| **T_b (s/byte)** | Per-byte transfer time (inverse bandwidth) | 1–10 ns/byte |\n| **B (bytes)** | Message size | application-dependent |\n| **T_c (s)** | Contention / synchronization delay | network-dependent |\n| **N_p** | Number of processes | 1–10⁶ |\n\n> [!NOTE]\n> For a message of size **B bytes**, the classical **Hockney model** gives the one-way transfer time as:\n\n$$T_{comm} = T_0 + B \\cdot T_b + T_c$$\n\nThe **overlap efficiency** $\\eta$ of nonblocking communication measures how much of the communication cost is hidden by computation:\n\n$$\\eta = 1 - \\frac{T_{comm} - T_{overlap}}{T_{comm}} = \\frac{T_{overlap}}{T_{comm}}$$\n\nwhere $T_{overlap}$ is the portion of communication cost that runs *in parallel* with useful computation. The **ideal** is $\\eta = 1$ (full hiding); the **worst** is $\\eta = 0$ (no overlap at all).\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# 2. Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 The Three-Phase Model of Nonblocking Communication\n\nEvery nonblocking point-to-point operation in MPI proceeds through **three strictly ordered phases**:\n\n### Phase 1 — **Initiation** (Local, Non-Blocking)\nThe user calls `MPI_Isend` or `MPI_Irecv`. The MPI library:\n- Copies the *arguments* (count, datatype, tag, communicator, destination/source) into an internal control structure.\n- Allocates and returns a **request handle** (`MPI_Request`).\n- **Returns immediately** to the caller. The user is free to reuse the send/receive buffer *only after* a subsequent successful completion test.\n\n### Phase 2 — **Data Movement** (Asynchronous, Background)\nThe MPI runtime:\n- Performs protocol-specific work (eager send, rendezvous, shared-memory copy, RDMA, etc.).\n- Progresses concurrently with the calling process.\n- May post internal matching operations when a matching `Irecv` arrives.\n\n### Phase 3 — **Completion** (Local or Synchronous)\nThe user must explicitly verify completion via:\n- `MPI_Wait` — blocks until the request completes.\n- `MPI_Test` — non-blocking poll; returns a *flag* indicating completion.\n- Variants: `MPI_Waitany`, `MPI_Waitall`, `MPI_Waitsome`, `MPI_Testany`, `MPI_Testall`, `MPI_Testsome`.\n\n> [!IMPORTANT]\n> **Why three phases?** This decouples *issuing* from *transfer* from *confirmation*. This decoupling is what enables **overlap**, **pipelining**, and **latency hiding** — the holy grail of HPC performance engineering.\n\n## 2.2 Communication Modes for Nonblocking Operations\n\nMPI provides **four send modes** for blocking sends, and **all four** also have nonblocking counterparts. They differ in *when* the local send call is allowed to return and *what* protocol is used:\n\n| Mode | Nonblocking Function | Local Return Condition | When Matched? |\n|:----:|:-------------------:|:----------------------:|:-------------:|\n| **Standard** | `MPI_Isend` | As soon as call begins (buffer may be reused after Wait) | May or may not have arrived |\n| **Buffered** | `MPI_Ibsend` | After copying message to an internal buffer | Buffer copied, no guarantee of receipt |\n| **Synchronous** | `MPI_Issend` | When matching receive has *started* | Confirmed handshake complete |\n| **Ready** | `MPI_Irsend` | Only safe if matching receive already posted | User guarantees matching recv exists |\n\n> [!TIP]\n> **KTU Frequently Tested Concept:** The four modes differ in *buffer-state guarantees* during Phase 2. The `Issend` mode is the *most conservative* (no local buffering); the `Ibsend` is the *most optimistic* (full local buffering).\n\n## 2.3 Request Objects and Completion Routines\n\n### The `MPI_Request` Handle\nA nonblocking call returns an `MPI_Request` — an opaque handle that uniquely identifies the ongoing communication. It is the *only* legitimate way to refer to the operation subsequently.\n\n### The Completion Family\n\n| Routine | Blocking? | Returns When | Typical Use |\n|:-------:|:---------:|:------------:|:-----------|\n| `MPI_Wait` | Yes | Exactly one specific request completes | Sequential waiting |\n| `MPI_Waitany` | Yes | *Any one* of a set completes | First-come-first-served |\n| `MPI_Waitall` | Yes | *All* requests in a set complete | Barrier-style join |\n| `MPI_Waitsome` | Yes | *At least one* of a set completes | Streaming workloads |\n| `MPI_Test` | No | Immediately, with flag | Polling loops |\n| `MPI_Testany` | No | Immediately, with flag | Non-blocking poll |\n| `MPI_Testall` | No | Immediately, with flag | Test full batch |\n| `MPI_Testsome` | No | Immediately, with flag | Non-blocking version of Waitsome |\n\n> [!WARNING]\n> **Common KTU Mistake:** `MPI_Test` returns a *flag* (boolean), not a status. Students often confuse the prototypes. The full signature is:\n>\n> `int MPI_Test(MPI_Request *request, int *flag, MPI_Status *status);`\n\n## 2.4 Persistent Nonblocking Communication (Advanced Optimization)\n\nFor communications that are **issued repeatedly** with identical arguments (typical in iterative solvers, time-stepping PDE codes, etc.), MPI offers **persistent requests**:\n\n- `MPI_Send_init` / `MPI_Recv_init` — set up a *persistent* request template (no data transfer yet).\n- `MPI_Start` / `MPI_Startall` — *trigger* the previously-initialized request.\n- `MPI_Request_free` — deallocate the persistent handle.\n\n> [!NOTE]\n> Persistent communication saves the **per-call argument setup cost** (T₀ savings!) and is heavily used in production HPC codes (e.g., PETSc, deal.II).\n\n## 2.5 KTU High-Yield Formula & Syntax Cheat Sheet\n\nThe following table consolidates all function signatures, formulas, and rules of thumb **essential for KTU 2024 Scheme examinations**:\n\n| Concept | Signature / Formula | Notes |\n|:--------|:-------------------:|:------|\n| Nonblocking Send | `int MPI_Isend(buf, count, datatype, dest, tag, comm, request)` | Returns immediately |\n| Nonblocking Receive | `int MPI_Irecv(buf, count, datatype, source, tag, comm, request)` | Wildcards: `MPI_ANY_SOURCE`, `MPI_ANY_TAG` |\n| Wait on one request | `int MPI_Wait(request, status)` | Blocks until done |\n| Test one request | `int MPI_Test(request, flag, status)` | `flag = 1` if done |\n| Wait on array | `int MPI_Waitall(count, array_of_requests, array_of_statuses)` | Joins `count` requests |\n| Hockney Model | $T_{comm} = T_0 + B \\cdot T_b + T_c$ | One-way latency |\n| Overlap Efficiency | $\\eta = T_{overlap} / T_{comm}$ | Ideal = **1**, Worst = **0** |\n| Speedup of NB over Blocking | $S_{NB} = T_{block} / T_{nonblock} \\geq 1$ | Always $\\geq 1$ when overlapped |\n| Persistent Send Init | `int MPI_Send_init(buf, count, dtype, dest, tag, comm, request)` | No data transfer yet |\n| Trigger Persistent | `int MPI_Start(request)` | Initiates the transfer |\n| Constant | `MPI_REQUEST_NULL` | Sentinel for \"no active request\" |\n| Error Code | `MPI_SUCCESS` | Returned on success |\n\n> [!IMPORTANT]\n> **KTU Pitfall:** All `MPI_I*` (nonblocking) functions have signature differing from their blocking counterparts by **one extra parameter at the end** — the `MPI_Request *request` pointer. Forgetting this is a guaranteed deduction in ESE valuation.\n\n## 2.6 Real-World Engineering Utility\n\nNonblocking point-to-point communication is the **backbone** of:\n\n- **Climate & Weather Modeling** (e.g., CESM, WRF): iterative stencil updates require halo exchanges via `Isend`/`Irecv` to overlap with inner-loop computation.\n- **Computational Fluid Dynamics** (e.g., ANSYS Fluent, OpenFOAM): domain-decomposition methods depend on nonblocking neighbor communication.\n- **Deep Learning Distributed Training** (e.g., Horovod, PyTorch DDP): gradient all-reduce uses nonblocking primitives under the hood (NCCL backend).\n- **Molecular Dynamics** (e.g., GROMACS, NAMD): short-range force computation overlaps with long-range PME communication.\n- **Sparse Linear Algebra** (e.g., PETSc, Trilinos): matrix–vector products overlap with preconditioner communication.\n\n> [!NOTE]\n> In all these domains, the *uncovered* blocking time is the critical bottleneck. Replacing `MPI_Send`/`MPI_Recv` with `MPI_Isend`/`MPI_Irecv` routinely yields **2×–5× speedup** on applications with abundant per-iteration computation.\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# 3. Step-by-Step Derivations, Performance Analysis & Analytical Matrix\n\n## 3.1 Comparative Analytical Matrix: Blocking vs Nonblocking\n\n| Property | Blocking (`MPI_Send`/`MPI_Recv`) | Nonblocking (`MPI_Isend`/`MPI_Irecv`) |\n|:---------|:--------------------------------:|:-------------------------------------:|\n| Call returns when… | Send buffer is safe to reuse / message has been received | Immediately after call begins |\n| Request handle | None | `MPI_Request` returned |\n| Completion verification | Implicit (return of call) | Explicit (`MPI_Wait`/`MPI_Test`) |\n| Buffer reuse rule | Safe *after call returns* | Safe *only after Wait/Test* |\n| Computation overlap | **No** (serialized) | **Yes** (overlapped) |\n| Deadlock risk (self-send) | **High** | **Zero** |\n| Programming complexity | Low | Medium–High |\n| Best for | Short, infrequent messages | Iterative, latency-sensitive codes |\n\n## 3.2 Step-by-Step Walkthrough: A Nonblocking Send–Receive Pattern\n\nConsider two processes **P₀** and **P₁** in a single communicator `MPI_COMM_WORLD`. P₀ posts a nonblocking send of a 1000-element integer array, then continues computing; P₁ posts a nonblocking receive.\n\n### Step 1 — Initiation (Time t = 0)\n\n**P₀** calls:\n\n$$\\text{MPI\\_Isend}(\\text{buf}, 1000, \\text{MPI\\_INT}, 1, 0, \\text{MPI\\_COMM\\_WORLD}, \\&\\text{req\\_send})$$\n\n**What happens internally:**\n- The MPI library stores the 7 arguments: `buf`, `1000`, `MPI_INT`, `1`, `0`, `MPI_COMM_WORLD`.\n- A new `MPI_Request` is allocated, say `req_send = 0x4F2A`.\n- The call returns. **P₀ is free to proceed**.\n\n**P₁** calls:\n\n$$\\text{MPI\\_Irecv}(\\text{buf}, 1000, \\text{MPI\\_INT}, 0, 0, \\text{MPI\\_COMM\\_WORLD}, \\&\\text{req\\_recv})$$\n\n**What happens internally:**\n- A *match queue* entry is created for source = 0, tag = 0, count = 1000.\n- A new `MPI_Request` is allocated, say `req_recv = 0x7B11`.\n- The call returns. **P₁ is free to proceed**.\n\n### Step 2 — Asynchronous Data Movement (Time t ∈ [T₀, T₀ + B·T_b])\n\nBoth processes now execute *useful computation* (e.g., matrix multiplication, FFT) while the MPI runtime progresses the data transfer. The cost model is:\n\n$$T_{data} = T_0 + B \\cdot T_b$$\n\nwhere for our example $B = 1000 \\times 4 = 4000$ bytes (assuming 4-byte `MPI_INT`). If $T_0 = 5\\,\\mu s$ and $T_b = 1\\,ns/byte$:\n\n$$T_{data} = 5 \\times 10^{-6} + 4000 \\times 10^{-9} = 5\\,\\mu s + 4\\,\\mu s = 9\\,\\mu s$$\n\n### Step 3 — Completion (Time t ≈ 9 µs)\n\n**P₀** calls:\n\n$$\\text{MPI\\_Wait}(\\&\\text{req\\_send}, \\&\\text{status})$$\n\nThis blocks until the send is locally complete. If P₁'s receive has already matched and the data has been copied into the network buffer or the receiver's buffer, `req_send` completes; otherwise P₀ blocks here. After return, `status.MPI_SOURCE`, `status.MPI_TAG`, and `status._ucounts` are populated.\n\n**P₁** calls:\n\n$$\\text{MPI\\_Test}(\\&\\text{req\\_recv}, \\&\\text{flag}, \\&\\text{status})$$\n\nIf `flag = 1`, the receive is complete and the data in `buf` is valid. If `flag = 0`, P₁ may continue other work and re-test later.\n\n### Step 4 — Deallocation (Time t = end)\n\nBest practice (especially in long-running loops) is to free the request handle:\n\n$$\\text{MPI\\_Request\\_free}(\\&\\text{req})$$\n\nThis releases internal resources; the request becomes inactive.\n\n## 3.3 Performance Analysis: Why Nonblocking Is Faster\n\nSuppose a process must send a message of size **B** *and* perform computation of duration **T_comp**. Three strategies are possible:\n\n### Strategy A — Pure Blocking\n\n$$T_{total}^{(A)} = T_{comm} + T_{comp} = (T_0 + B \\cdot T_b + T_c) + T_{comp}$$\n\n### Strategy B — Nonblocking with Perfect Overlap (Ideal)\n\nIf the runtime *fully* overlaps communication with computation:\n\n$$T_{total}^{(B)} = \\max(T_{comm}, T_{comp}) = \\max(T_0 + B \\cdot T_b + T_c,\\; T_{comp})$$\n\n### Strategy C — Nonblocking with Partial Overlap (Realistic)\n\nIf only a fraction $\\alpha$ of the communication is overlapped (where $0 \\leq \\alpha \\leq 1$):\n\n$$T_{total}^{(C)} = (1 - \\alpha) \\cdot T_{comm} + T_{comp}$$\n\nThe **speedup** of nonblocking over blocking is therefore:\n\n$$S_{NB} = \\frac{T_{total}^{(A)}}{T_{total}^{(B)}}$$\n\n> [!NOTE]\n> **Asymptotic insight:** When $T_{comp} \\gg T_{comm}$, the speedup $S_{NB} \\to 1$ (both strategies are dominated by computation — nonblocking offers little gain). When $T_{comp} \\ll T_{comm}$, $S_{NB} \\to T_{comm} / T_{comp} \\gg 1$ (nonblocking offers dramatic gain by hiding the large communication cost). The crossover condition is $T_{comp} \\approx T_{comm}$ — the **balanced-iterations** design point.\n\n## 3.4 Step-by-Step Derivation: Optimal Number of Outstanding Requests\n\nIn a **pipeline loop** where each iteration issues a nonblocking send, the question arises: how many concurrent requests should we allow?\n\n**Let:**\n- $N_{iter}$ = total number of loop iterations\n- $T_{per\\_iter}$ = computation time per iteration\n- $T_{comm}$ = communication time per iteration\n- $k$ = number of outstanding (in-flight) requests at a time\n\n**Total time** (with full overlap) is bounded by:\n\n$$T_{total} = N_{iter} \\cdot T_{per\\_iter} + \\left\\lceil \\frac{N_{iter}}{k} \\right\\rceil \\cdot T_{comm}$$\n\nTo *minimize* $T_{total}$, take $\\partial T_{total} / \\partial k < 0$:\n\n$$\\frac{\\partial T_{total}}{\\partial k} \\approx -\\frac{N_{iter}}{k^2} \\cdot T_{comm} < 0$$\n\nThis is **always negative** for finite $k$, meaning we want $k$ *as large as possible*. In practice, $k$ is limited by:\n\n- **Buffer memory** (each outstanding request reserves internal memory)\n- **Network bandwidth** (saturation)\n- **MPI implementation's internal queue depth** (typically 64–256)\n\nA **practical heuristic** for the optimal in-flight count is:\n\n$$k_{opt} = \\min\\left(\\left\\lceil \\frac{T_{comm}}{T_{per\\_iter}} \\right\\rceil + 1,\\; k_{max}\\right)$$\n\nThis is the **\"one ahead, one behind\"** strategy used in production stencil codes.\n\n## 3.5 Worked Numerical Example (KTU-Style Problem)\n\n**Problem:** A parallel program sends **8 messages** of 1 MB each via blocking sends, each followed by 50 ms of computation. With nonblocking sends and full overlap, what is the speedup if blocking send takes 30 ms each?\n\n**Solution:**\n\n**Step 1** — Total time with blocking:\n$$T_{block} = 8 \\times (30\\,ms + 50\\,ms) = 8 \\times 80 = 640\\,ms$$\n\n**Step 2** — With nonblocking + full overlap, each iteration's time becomes:\n$$T_{iter}^{NB} = \\max(30\\,ms, 50\\,ms) = 50\\,ms$$\n\n**Step 3** — Total time with nonblocking:\n$$T_{NB} = 8 \\times 50 = 400\\,ms$$\n\n**Step 4** — Speedup:\n$$S = \\frac{T_{block}}{T_{NB}} = \\frac{640}{400} = 1.6$$\n\n> [!TIP]\n> **Answer:** Speedup of **1.6×** (60% faster). Notice that because $T_{comp} > T_{comm}$, the speedup is *modest*. If computation were 10 ms and communication 30 ms, the speedup would be $\\frac{320}{240} \\approx 1.33\\times$ — wait, the actual computation gives $\\frac{8 \\times 40}{8 \\times 30} = 1.33\\times$. The general rule: speedup is greatest when *computation and communication are balanced*.\n\n## 3.6 Comparison of Send Modes (Analytical Table)\n\n| Mode | Local Return | Safe Buffer Reuse After | Communication Protocol | Deadlock Risk |\n|:----:|:------------:|:-----------------------:|:----------------------:|:-------------:|\n| **Standard** (`Isend`) | Immediately (in eager limit) | `MPI_Wait` | Eager (≤ eager limit) or Rendezvous | Medium |\n| **Buffered** (`Ibsend`) | After buffer copy | `MPI_Wait` | Eager with user buffer | Low |\n| **Synchronous** (`Issend`) | After handshake starts | `MPI_Wait` | Always rendezvous | High if misused |\n| **Ready** (`Irsend`) | Immediately, *if recv posted* | `MPI_Wait` | Eager, no handshake | **Very high** if recv not posted |\n\n> [!IMPORTANT]\n> **KTU 2024 Module 4 Focus:** The `Issend` (synchronous nonblocking) is the *most disciplined* mode — it is the closest nonblocking analog to a true synchronous send. Production HPC codes use it when **correctness guarantees** outweigh performance.\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# 4. Structural Diagrams & Schematics\n\n## 4.1 Nonblocking Communication Flow (Mermaid Block Diagram)\n\nThe following Mermaid diagram illustrates the three-phase lifecycle of a typical `MPI_Isend` + `MPI_Irecv` pair, with intermediate computation overlap.\n\n```mermaid\nflowchart TD\n    A[Process P0: User Code Starts] --> B[Call MPI_Isend: returns request req_send]\n    B --> C[Call MPI_Irecv on P1: returns request req_recv]\n    C --> D[MPI Runtime initiates data transfer in background]\n    D --> E[Both P0 and P1 continue useful computation]\n    E --> F{All iterations done?}\n    F -->|No| E\n    F -->|Yes| G[P0: Call MPI_Wait on req_send]\n    G --> H[P1: Call MPI_Wait or MPI_Test on req_recv]\n    H --> I[Reuse or deallocate buffers]\n    I --> J[MPI_Request_free to release handle]\n    J --> K[Process continues with verified data]\n```\n\n## 4.2 Blocking vs Nonblocking — Comparative Timeline\n\n```mermaid\ngantt\n    title Blocking vs Nonblocking Communication Timeline\n    dateFormat X\n    axisFormat %s\n    section Blocking Approach\n    MPI_Send (blocks)         :a1, 0, 3\n    Computation (idle wait)   :a2, 3, 8\n    MPI_Recv (blocks)         :a3, 8, 11\n    Total Blocking Time       :crit, 11, 11\n    section Nonblocking Approach\n    MPI_Isend (non-blocking)  :b1, 0, 1\n    Computation (overlaps)    :b2, 1, 8\n    MPI_Wait (brief join)     :b3, 8, 9\n    MPI_Irecv (non-blocking)  :b4, 9, 10\n    Computation (overlaps)    :b5, 10, 15\n    MPI_Wait (brief join)     :b6, 15, 16\n    Total Nonblocking Time    :crit, 16, 16\n```\n\n## 4.3 Function Call Hierarchy and Request Object Lifecycle\n\n```mermaid\nflowchart LR\n    subgraph init[Initiation Phase]\n        A1[MPI_Isend] --> R1[Request Handle req_send]\n        A2[MPI_Irecv] --> R2[Request Handle req_recv]\n    end\n    subgraph comp[Computation Phase - Overlapped]\n        C1[Matrix Multiply]\n        C2[FFT or Stencil]\n    end\n    subgraph complete[Completion Phase]\n        W1[MPI_Wait on req_send]\n        T1[MPI_Test on req_recv]\n        F1[MPI_Request_free]\n    end\n    R1 --> C1\n    R2 --> C2\n    C1 --> W1\n    C2 --> T1\n    W1 --> F1\n    T1 --> F1\n```\n\n## 4.4 Persistent Communication Architecture\n\n```mermaid\nflowchart TD\n    A[Application Start] --> B[MPI_Send_init: build request template]\n    B --> C[Request template stored]\n    C --> D[Enter Iterative Loop]\n    D --> E[Call MPI_Start: trigger transfer]\n    E --> F[Background data movement]\n    F --> G[Call MPI_Wait: ensure completion]\n    G --> H{More iterations?}\n    H -->|Yes| E\n    H -->|No| I[Call MPI_Request_free]\n    I --> J[Application End]\n```\n\n## 4.5 Nonblocking Communication Architectural Flow\n\n```mermaid\nflowchart TD\n    subgraph userSpace[User Process Space]\n        U1[User Application Code]\n        U2[Send/Receive Buffer]\n        U3[Request Handle req]\n    end\n    subgraph mpiRuntime[MPI Runtime Layer]\n        M1[Argument Copy and Validation]\n        M2[Internal Match Queue]\n        M3[Protocol Engine - Eager or Rendezvous]\n        M4[Progress Thread or Polling]\n    end\n    subgraph hwLayer[Hardware and Network Layer]\n        H1[Shared Memory - Intra-node]\n        H2[Network Interface Card - Inter-node]\n    end\n    U1 --> M1\n    M1 --> U3\n    M1 --> M2\n    M2 --> M3\n    M3 --> M4\n    M4 --> H1\n    M4 --> H2\n    U2 -.read write.-> M3\n```\n\n> [!NOTE]\n> **Reading these diagrams (KTU 2024):** Mermaid diagrams are non-examinable illustrative aids. The conceptual point is the **separation of concerns**: user code issues requests; MPI runtime schedules and progresses them; hardware executes the actual data movement — all in parallel.\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# 5. KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks Each)\n\n### **Q1. [KTU University Exam — Dec 2023, Model Paper]**\n**Define nonblocking point-to-point communication. How does it differ from blocking communication?**\n\n**Model Answer (3 marks):**\nNonblocking point-to-point communication refers to MPI communication calls (e.g., `MPI_Isend`, `MPI_Irecv`) that **return immediately** after issuance, without waiting for the message to be sent or received. They return an **opaque request handle** (`MPI_Request`) that must be subsequently tested or waited upon to confirm completion.\n\n| Aspect | Blocking | Nonblocking |\n|:-------|:--------:|:-----------:|\n| Return condition | After transfer completes | Immediately |\n| Request handle | Not returned | Returned |\n| Buffer reuse | Safe on return | Safe only after Wait/Test |\n| Computation overlap | Not possible | Possible |\n\n*(Marks distribution: Definition 1m, Difference Table 1m, Overlap property 1m)*\n\n---\n\n### **Q2. [KTU University Exam — June 2024, Supplementary]**\n**List the four nonblocking send modes available in MPI and state one distinguishing property of each.**\n\n**Model Answer (3 marks):**\n1. **Standard (`MPI_Isend`)** — Returns immediately; no guarantee of internal buffering; works for any matching receive.\n2. **Buffered (`MPI_Ibsend`)** — Returns only after message is copied to user-allocated buffer; safe even if no receive is posted.\n3. **Synchronous (`MPI_Issend`)** — Returns only after the matching receive has *started*; guarantees handshake completion.\n4. **Ready (`MPI_Irsend`)** — Returns immediately *only if* a matching receive is already posted; otherwise undefined behavior.\n\n*(Marks distribution: 4 modes × 0.5m = 2m, Distinguishing property example 1m)*\n\n---\n\n## 5.2 Part B — Full-Descriptive Questions (14 Marks Each)\n\n> **KTU 2024 Scheme Pattern:** Each Part B question has an **internal choice**. The two alternatives are completely independent, allowing the examiner to assess depth on either dimension of the module.\n\n---\n\n### **Question A (14 Marks) — Option 1**\n\n**Q.A. [KTU University Exam — Dec 2023, Module 4]**\n**(a)** Explain in detail the three-phase model of nonblocking point-to-point communication in MPI. Describe the role of `MPI_Request` and the various `MPI_Wait*` and `MPI_Test*` completion routines. **(7 marks)**\n\n**(b)** A parallel application executes 100 iterations. Each iteration performs 40 ms of computation and sends a 2 MB message. The blocking send takes 25 ms; with nonblocking send and full overlap, what is the speedup? Show all calculations. **(7 marks)**\n\n---\n\n**Model Solution (a) — 7 marks:**\n\nThe three-phase model of nonblocking communication is:\n\n**Phase 1 — Initiation (1.5 marks):**\n- User calls `MPI_Isend` or `MPI_Irecv`.\n- MPI library copies call arguments (buffer, count, datatype, rank, tag, communicator) into an internal control block.\n- A new `MPI_Request` handle is allocated and returned to the user.\n- The call returns *immediately* without waiting for any data transfer.\n\n**Phase 2 — Data Movement / Asynchronous Progress (1.5 marks):**\n- The MPI runtime progresses the data transfer concurrently with the calling process's execution.\n- Depending on message size and protocol (eager vs. rendezvous), the runtime may:\n  - Copy data into an internal network buffer (eager), or\n  - Perform a handshake with the receiver (rendezvous), or\n  - Use shared-memory copy (intra-node).\n- The user process is *free* to execute useful computation in parallel.\n\n**Phase 3 — Completion (2 marks):**\n- The user *must* verify completion via the `MPI_Wait*` or `MPI_Test*` family.\n- `MPI_Request` is the opaque handle that uniquely identifies the pending operation.\n- Completion routines:\n  - `MPI_Wait` — blocks until a *specific* request completes.\n  - `MPI_Test` — non-blocking poll returning a *flag*.\n  - `MPI_Waitany`, `MPI_Waitall`, `MPI_Waitsome` — operate on arrays of requests.\n  - `MPI_Testany`, `MPI_Testall`, `MPI_Testsome` — non-blocking versions.\n- After successful completion, the user buffer is safe to reuse.\n\n**Persistent Communication (1 mark):**\n- `MPI_Send_init` and `MPI_Recv_init` create *templates* that can be re-triggered with `MPI_Start`, saving per-call setup cost.\n- Used in iterative solvers and time-stepping codes.\n\n**Best Practice (1 mark):**\n- Deallocate with `MPI_Request_free` to avoid resource leaks.\n\n---\n\n**Model Solution (b) — 7 marks:**\n\n**Given:**\n- $N_{iter} = 100$ iterations\n- $T_{comp} = 40$ ms per iteration\n- $T_{comm} = 25$ ms per iteration (blocking equivalent)\n\n**Step 1 — Total time with blocking approach (2 marks):**\n\n$$T_{block} = N_{iter} \\times (T_{comp} + T_{comm}) = 100 \\times (40 + 25) = 100 \\times 65 = 6500\\,ms$$\n\n**Step 2 — Total time with nonblocking + full overlap (3 marks):**\n\nWith nonblocking send and perfect overlap, the per-iteration time becomes:\n\n$$T_{iter}^{NB} = \\max(T_{comp}, T_{comm}) = \\max(40, 25) = 40\\,ms$$\n\nTherefore:\n\n$$T_{NB} = N_{iter} \\times T_{iter}^{NB} = 100 \\times 40 = 4000\\,ms$$\n\n**Step 3 — Speedup (2 marks):**\n\n$$S = \\frac{T_{block}}{T_{NB}} = \\frac{6500}{4000} = 1.625$$\n\n**Answer:** The speedup is **1.625×** (i.e., the nonblocking version is **62.5% faster**, saving **2500 ms** overall).\n\n---\n\n### **Question B (14 Marks) — Option 2**\n\n**Q.B. [KTU University Exam — June 2024, Model Paper]**\n**(a)** Compare the four nonblocking send modes (`MPI_Isend`, `MPI_Ibsend`, `MPI_Issend`, `MPI_Irsend`) with respect to local return conditions, buffer-state guarantees, and deadlock risk. **(7 marks)**\n\n**(b)** Explain the concept of persistent nonblocking communication. Write the function signatures for `MPI_Send_init`, `MPI_Recv_init`, and `MPI_Start`. State two practical scenarios where persistent communication provides a measurable performance benefit. **(7 marks)**\n\n---\n\n**Model Solution (a) — 7 marks:**\n\n**Comparison Table (5 marks):**\n\n| Mode | Local Return | Buffer Reuse Safe | Deadlock Risk | Internal Action |\n|:-----|:-------------|:------------------|:-------------:|:----------------|\n| **`MPI_Isend`** (Standard) | Immediately (within eager limit) | After `MPI_Wait` | Medium | Eager buffer copy or rendezvous handshake |\n| **`MPI_Ibsend`** (Buffered) | After buffer copy to user `MPI_Buffer_attach` | After `MPI_Wait` | Low | Always copies to user buffer |\n| **`MPI_Issend`** (Synchronous) | Only after matching `MPI_Recv` has *started* | After `MPI_Wait` | High if no matching recv | Always rendezvous protocol |\n| **`MPI_Irsend`** (Ready) | Immediately, **only if** matching recv is posted | After `MPI_Wait` | **Very high** if recv missing | User assumes recv is ready |\n\n**Key Insights (2 marks):**\n- The four modes form a **spectrum of trade-offs** between *eager local return* and *delivery guarantees*.\n- `MPI_Isend` is the most commonly used (default choice); `MPI_Issend` is safest; `MPI_Ibsend` is preferred when buffer memory is plentiful; `MPI_Irsend` is rarely used in modern HPC because of its strict precondition.\n\n**Deadlock Analysis:**\n- Blocking `MPI_Send` + `MPI_Recv` on two processes can deadlock if both call `MPI_Send` first (when messages exceed the eager limit).\n- Nonblocking `MPI_Isend` + `MPI_Irecv` from both sides **never deadlocks** because each side posts a receive *before* (or simultaneously with) the send.\n\n---\n\n**Model Solution (b) — 7 marks:**\n\n**Concept of Persistent Communication (3 marks):**\n\nPersistent nonblocking communication allows a *communication pattern* to be **pre-initialized once** and **reused many times**. It is a two-step mechanism:\n\n1. **Initialization** (one-time, low overhead): Create a *persistent request* template via `MPI_Send_init` or `MPI_Recv_init`. No data transfer occurs at this step.\n2. **Activation** (per-iteration): Trigger the saved template via `MPI_Start` (single) or `MPI_Startall` (array). The transfer happens *exactly* as if a fresh `MPI_Isend`/`MPI_Irecv` had been called, but with **reduced per-call overhead**.\n\n**Function Signatures (2 marks):**\n\n```\nint MPI_Send_init(const void *buf, int count, MPI_Datatype datatype,\n                  int dest, int tag, MPI_Comm comm, MPI_Request *request);\n\nint MPI_Recv_init(void *buf, int count, MPI_Datatype datatype,\n                  int source, int tag, MPI_Comm comm, MPI_Request *request);\n\nint MPI_Start(MPI_Request *request);\n\nint MPI_Startall(int count, MPI_Request array_of_requests);\n```\n\n**Practical Scenarios (2 marks):**\n\n1. **Iterative Linear Solvers (e.g., Conjugate Gradient, GMRES):** Each iteration sends/receives the same halo region in a domain-decomposed mesh. Persistent requests avoid re-parsing the argument list and re-building the internal match queue — saving microseconds per call × thousands of iterations.\n\n2. **Time-Stepping PDE Codes (e.g., explicit Euler for heat equation):** Each timestep exchanges boundary data with the same neighbors using the same buffer layout. Persistent communication amortizes the argument-copy cost and reduces per-step latency by 10–30% in production codes.\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mark-Deduction Pitfalls in KTU 2024 Module 4 Questions:**\n>\n> 1. **Forgetting the `MPI_Request` argument** in function signatures (deducted 1–2 marks).\n> 2. **Confusing `MPI_Wait` (returns status) with `MPI_Test` (returns flag + status)** — a recurring error worth 0.5–1 mark.\n> 3. **Assuming nonblocking *always* gives a speedup** — without sufficient computation to overlap, the overhead may dominate. Students must explicitly state the *overlap condition* $T_{comp} \\geq T_{comm}$ for net speedup.\n> 4. **Mixing up modes** — for example, calling `MPI_Irsend` without a prior matching receive. Examiners test this with a \"what happens?\" question.\n> 5. **Buffer-reuse rule violation** — using the send buffer before `MPI_Wait` completes. Worth 1–2 marks if not mentioned.\n> 6. **Confusing \"nonblocking\" with \"asynchronous\"** — nonblocking refers to *local* return semantics; the actual transfer may still be synchronous or buffered internally. Examiners appreciate precise wording.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **High-Density Revision Checklist — Nonblocking Point-to-Point Communication**\n\n- [x] **Definition:** Nonblocking calls return *immediately* with an `MPI_Request` handle.\n- [x] **Three phases:** Initiation → Asynchronous Data Movement → Explicit Completion.\n- [x] **Key nonblocking sends:** `MPI_Isend`, `MPI_Ibsend`, `MPI_Issend`, `MPI_Irsend`.\n- [x] **Key nonblocking receives:** `MPI_Irecv` (only one form).\n- [x] **Completion routines:** `MPI_Wait`, `MPI_Test` (and their `*any`/`*all`/`*some` variants).\n- [x] **Buffer reuse rule:** *Never* reuse a nonblocking send/recv buffer until `MPI_Wait` or successful `MPI_Test`.\n- [x] **Persistent communication:** `MPI_Send_init`, `MPI_Recv_init`, `MPI_Start`, `MPI_Startall`, `MPI_Request_free`.\n- [x] **Performance model:** Hockney $T_{comm} = T_0 + B \\cdot T_b + T_c$.\n- [x] **Overlap efficiency:** $\\eta = T_{overlap}/T_{comm}$; ideal $\\eta = 1$.\n- [x] **Speedup condition:** Nonblocking helps *only if* $T_{comp}$ is comparable to $T_{comm}$.\n- [x] **Deadlock safety:** Nonblocking sends/receives are inherently deadlock-free when both sides post a receive.\n- [x] **Constants to remember:** `MPI_REQUEST_NULL`, `MPI_ANY_SOURCE`, `MPI_ANY_TAG`, `MPI_SUCCESS`.\n- [x] **Real-world impact:** Used in CESM, GROMACS, PETSc, Horovod — typical speedup 2×–5×.\n- [x] **KTU 2024 keywords to memorize:** *latency hiding*, *communication–computation overlap*, *asynchronous progress*, *request handle*, *persistent request*.\n- [x] **Common exam question types:** Compare blocking vs nonblocking (table); explain three phases; compute speedup; list four send modes; explain persistent communication.\n\n> **Final Tip:** When in doubt on a KTU 2024 exam, **draw the three-phase diagram** — it earns partial credit even if the rest of the answer is incomplete.\n<!-- SECTION_5_END -->",
      "pyqs": []
    },
    "virtualtopologiesexamplempiparallelizationofajacobisolvermpiimplementationperformanceproperties": {
      "title": "Virtual Topologies - Example: MPI Parallelization of a Jacobi Solver - MPI Implementation - Performance Properties",
      "content": "> **APJ ABDUL KALAM TECHNOLOGICAL UNIVERSITY (KTU) | 2024 SCHEME**\n> - **Course:** PECST757 - HIGH PERFORMANCE COMPUTING\n> - **Module:** Module 4\n> - **Topic:** Virtual Topologies - Example: MPI Parallelization of a Jacobi Solver - MPI Implementation - Performance Properties\n\n<!-- SECTION_1_START -->\n# SECTION 1: Core Technical Definition & Intuitive Overview\n\n## 1.1 Virtual Topologies — Formal Definition\n\nIn the **Message Passing Interface (MPI)** standard, a **Virtual Topology** is an abstract coordinate-based naming scheme superimposed on the set of MPI processes, which maps each process onto a structured communication graph. Processes are organized in a way that reflects the *logical* communication pattern of the parallel application, even though the *physical* (hardware) interconnect may be a generic network.\n\nAccording to the MPI standard, two principal virtual topology constructors are provided:\n\n1. **Cartesian Topologies** — `MPI_Cart_create` and friends (grids, tori, meshes)\n2. **Graph Topologies** — `MPI_Graph_create` and `MPI_Dist_graph_create` (arbitrary sparse graphs)\n\n> [!IMPORTANT]\n> **KTU Syllabus Highlight (PECST757 Module 4):**\n> Virtual topology is *not* a physical reconfiguration of the network. It is a **process-grouping and addressing abstraction** that allows programmers to write algorithms in a way that mirrors the natural geometry of the data.\n\nA virtual topology provides:\n- **Convenient naming** of processes (e.g., coordinates `(i, j)` instead of rank `17`).\n- **Efficient collective communication** tailored to the topology (e.g., neighbor collectives in MPI 4.0).\n- **A clean model for partitioning** regular data structures such as matrices and grids.\n\n## 1.2 Intuitive Analogy — The Newsroom Control Room\n\nImagine a giant national newspaper with **256 reporters** spread across the country, but all reporting to a single chief editor. The chief editor has two options:\n\n| Option | Description | Cost |\n|---|---|---|\n| **Flat Address** | Identify reporters by flat ID 0…255 and broadcast instructions over a single phone line. | Slow, confusing, prone to mix-ups. |\n| **Virtual Topology** | Arrange reporters in a 16 × 16 grid on a whiteboard: \"North-Block row 4, column 7.\" | The editor can whisper to neighbours, route stories along rows/columns, and parallelize the press run naturally. |\n\nThe whiteboard grid is the **virtual topology** — it does not change where the reporters physically sit, but it dramatically simplifies the editor's communication logic. MPI's virtual topologies do exactly the same for parallel programs: they impose a *grid/graph mental model* on the processes so the algorithm becomes readable, and the runtime can exploit locality of communication.\n\n> [!NOTE]\n> **Key Term Mapping for Exam Writing:**\n> - **Logical Topology** = the *user-defined* virtual grid/graph\n> - **Physical Topology** = the *actual* network (FAT-tree, torus, dragonfly, etc.)\n> - **Neighborhood** = the set of processes that the topology declares as adjacent\n> - **Reordering** = MPI is *allowed* to physically remap ranks for performance; logical adjacency is preserved.\n\n## 1.3 Why Virtual Topologies Matter in HPC\n\nA student new to HPC often asks: *\"If I know my neighbour ranks, why bother creating a topology?\"* The answer has three pillars:\n\n1. **Algorithmic clarity** — Code mirrors the mathematics of the problem.\n2. **Portability** — The same program runs on 4, 64, or 4096 cores by changing one parameter.\n3. **Performance** — MPI libraries can optimise neighbour communication (Cartesian-aware algorithms, hardware-MPI mapping) and use efficient collectives (`MPI_Neighbor_allgather`).\n\n> [!TIP]\n> **Exam Mnemonic — \"TCP\":**\n> - **T**opology-aware algorithms\n> - **C**ode clarity & modularity\n> - **P**erformance via locality\n\n## 1.4 The Jacobi Solver — Why It Is the Canonical Example\n\nThe **Jacobi iterative method** for solving 2D Poisson/Laplace equations is the *de facto* teaching example in HPC courses worldwide because:\n\n- It exposes a **regular stencil** (4-point or 9-point) — perfect for Cartesian decomposition.\n- It has **strict data-dependency boundaries** (a cell only needs neighbour cells of the previous iteration).\n- It is **embarrassingly regular**, making it ideal to discuss load balance, boundary halos, and ghost-cell exchange.\n- It exhibits **near-linear strong scaling** up to a point, then suffers from communication-to-computation ratio decay — a beautiful illustration of performance properties.\n\n> [!IMPORTANT]\n> A Jacobi iteration for a 2D grid with $N \\times N$ interior points computes, for every cell $(i,j)$:\n> $$u^{(k+1)}_{i,j} = \\frac{1}{4}\\left(u^{(k)}_{i+1,j} + u^{(k)}_{i-1,j} + u^{(k)}_{i,j+1} + u^{(k)}_{i,j-1}\\right) - \\frac{h^2}{4}f_{i,j}$$\n> The superscript $(k)$ is the iteration number. Each cell needs the four *cardinal* neighbours from the *previous* iteration — a textbook **nearest-neighbour communication pattern**.\n\n---\n\n<!-- SECTION_1_END -->\n\n<!-- SECTION_2_START -->\n# SECTION 2: Deep Theoretical Analysis & KTU High-Yield Formula Sheet\n\n## 2.1 Taxonomy of MPI Virtual Topologies\n\n### 2.1.1 Cartesian Topology\n\nA Cartesian topology organises $P$ processes into a $d$-dimensional grid with dimensions $\\mathbf{n} = (n_0, n_1, \\ldots, n_{d-1})$ such that $\\prod_{i=0}^{d-1} n_i = P$.\n\n**Key MPI Routines (KTU exam favourites):**\n\n| Routine | Purpose |\n|---|---|\n| `MPI_Cart_create` | Build a new communicator with Cartesian layout. |\n| `MPI_Dims_create` | Suggest a balanced factorization of $P$ into a $d$-D grid. |\n| `MPI_Cart_coords` | Convert a rank to its coordinates in the grid. |\n| `MPI_Cart_rank` | Convert coordinates to a rank. |\n| `MPI_Cart_shift` | Compute the rank of a neighbour in a given direction (very useful for Jacobi stencils). |\n| `MPI_Cart_sub` | Split a Cartesian grid into lower-dimensional sub-grids. |\n\n**Periodicity:**\n- `periods[i] = 0` → non-periodic (boundary, \"mesh\")\n- `periods[i] = 1` → periodic (\"torus\" — wrap-around)\n\n> [!NOTE]\n> For 1D Jacobi solver we use a **1D Cartesian topology with `periods=0`**. For 2D Jacobi we use a **2D Cartesian grid** matching the natural 2D data layout.\n\n### 2.1.2 Graph Topology\n\nA graph topology is more general — it allows arbitrary directed edges between processes. Two variants exist:\n- **General Graph** (`MPI_Graph_create`) — deprecated in MPI-3, but still examined.\n- **Distributed Graph** (`MPI_Dist_graph_create`) — modern, scalable, MPI-3+.\n\n> [!TIP]\n> **KTU Quick Check:** If the problem is a *grid/stencil*, use **Cartesian**. If the problem is *irregular* (e.g., sparse matrix, FEM mesh), use **Graph**.\n\n## 2.2 The Jacobi Solver — Parallelisation Strategy\n\nThe standard 2D Jacobi solver with a $P = p_x \\times p_y$ process grid uses **domain decomposition**:\n\n1. Split the global $N \\times N$ grid into $p_x \\times p_y$ sub-grids, each owned by one process.\n2. Each sub-grid has halo rows/columns on the four sides, holding copies of the *neighbouring* sub-grid's boundary.\n3. At each iteration:\n   a. **Local computation** on interior points.\n   b. **Halo exchange** with the four neighbours.\n   c. **Convergence test** (global reduction of local residuals).\n\n### 2.2.1 Halo Exchange Communication Pattern\n\nThe halo (also called *ghost cells*) is the buffer of boundary data that must be exchanged at every iteration. For a 2D Cartesian grid:\n\n- Each process sends its **top row** to the **upper neighbour** and receives its **bottom halo**.\n- Each process sends its **bottom row** to the **lower neighbour** and receives its **top halo**.\n- The same applies to **left/right** columns.\n\nThis results in **4 point-to-point messages per iteration per process** (or fewer at physical boundaries).\n\n> [!IMPORTANT]\n> A **persistent communication** pattern (using `MPI_Send_init`/`MPI_Recv_init` + `MPI_Startall`) is preferred because the same buffers are exchanged every iteration. This avoids repeated setup overhead of non-blocking calls.\n\n## 2.3 KTU High-Yield Formula Sheet\n\n> [!NOTE]\n> All formulas below are **commonly tested** in KTU university exams. Memorise the table; the symbols are universal in HPC literature.\n\n| # | Quantity | Formula | Notes / Units |\n|---|---|---|---|\n| 1 | **Global problem size** | $W$ (FLOP) or $N$ (grid points) | Total work of one Jacobi iteration on the *serial* code is $5N^2$ FLOPs for 4-point stencil. |\n| 2 | **Parallel runtime** | $T_P = T_{\\text{comp}} + T_{\\text{comm}} + T_{\\text{idle}}$ | Amdahl-like decomposition |\n| 3 | **Computation time per process** | $T_{\\text{comp}} = \\dfrac{5N^2}{P} \\cdot t_c$ | $t_c$ = time per FLOP, $P$ = #processes |\n| 4 | **Communication time per iteration (2D)** | $T_{\\text{comm}} = \\alpha \\cdot 4 + \\beta \\cdot 4m$ | $\\alpha$ = latency (s), $\\beta$ = bandwidth$^{-1}$ (s/byte), $m$ = halo message size in bytes |\n| 5 | **Halo message size per neighbour (2D)** | $m = n_x \\cdot n_y \\cdot \\text{sizeof(double)}$ | $n_x, n_y$ = sub-grid dimensions including halos |\n| 6 | **Speedup** | $S_P = \\dfrac{T_1}{T_P}$ | Ratio; ideal = $P$ |\n| 7 | **Efficiency** | $E_P = \\dfrac{S_P}{P} = \\dfrac{T_1}{P \\cdot T_P}$ | $0 < E_P \\le 1$ |\n| 8 | **Amdahl's Law** | $S_P \\le \\dfrac{1}{f + \\dfrac{1-f}{P}}$ | $f$ = serial fraction |\n| 9 | **Gustafson's Law (scaled speedup)** | $S_P = f + (1-f) \\cdot P$ | Work grows with $P$ |\n| 10 | **Hockney model (message time)** | $t_m = \\alpha + \\beta \\cdot m$ | Linear in message size $m$ |\n| 11 | **Iso-efficiency function** | $W = \\mathcal{O}(P \\cdot T_{\\text{comm}})$ | Work needed to keep $E$ constant |\n| 12 | **Strong scaling efficiency** | $E = \\dfrac{1}{1 + \\dfrac{t_m}{t_c} \\cdot \\dfrac{P}{N^2}}$ | For Jacobi, halo grows as $N/\\sqrt{P}$ |\n| 13 | **Karplus–Flatt metric** | $e = \\dfrac{T_1/P - T_P}{T_1 - T_P} = f$ | Numerically estimates serial fraction |\n| 14 | **Surface-to-volume ratio** | $\\text{STV} = \\dfrac{\\text{halo cells}}{\\text{interior cells}} = \\dfrac{4\\sqrt{P}}{N/\\sqrt{P}}$ | Drives the communication bound |\n\n> [!IMPORTANT]\n> **CRITICAL FORMATTING NOTE:** In all table rows above, the symbol `\\vert` and `\\mid` are used in place of vertical bars `|` so that markdown table syntax is preserved. Students copying formulas into an answer sheet should write $\\mid \\cdot \\mid$ in LaTeX form for absolute value.\n\n## 2.4 Performance Properties — The Three Pillars\n\nA KTU 14-mark question often asks: *\"Discuss the performance properties of MPI parallelisation of the Jacobi solver.\"* The expected answer must cover the **three classical HPC performance properties**:\n\n### Pillar 1 — Strong Scaling\n- **Definition:** Keep the *total problem size $N$* fixed; increase the number of processes $P$.\n- **What it measures:** How fast does the parallel code execute on $P$ cores compared to 1 core?\n- **Jacobi behaviour:** Initially $S_P \\approx P$ (perfect). As $P$ grows, the halo-to-interior ratio grows $\\propto \\sqrt{P}$ and eventually dominates. **Strong scaling hits a wall** when each process has only a handful of cells.\n\n### Pillar 2 — Weak Scaling\n- **Definition:** Keep the *work per process* fixed; increase $P$ (and total problem size $N^2 \\propto P$).\n- **What it measures:** How well does the algorithm handle growing problem sizes?\n- **Jacobi behaviour:** With careful halo sizing, **weak scaling is close to ideal** because the halo fraction per process is constant. This is why Jacobi is called *weakly scalable*.\n\n### Pillar 3 — Iso-Efficiency\n- **Definition:** The total work $W(P)$ required to keep efficiency $E$ constant as $P$ grows.\n- **Jacobi result:** $W(P) = \\mathcal{O}(P^{3/2})$ for 2D Jacobi on a $P$-process grid with the *best* partitioning.\n- This means the solver has **good but not perfect** scalability — closer to optimal than dense linear algebra ($\\mathcal{O}(P^2)$), but worse than FFT ($\\mathcal{O}(P \\log P)$).\n\n## 2.5 Real-World Utility\n\nThe 2D Jacobi / Poisson solver is the workhorse of:\n- **Computational electromagnetics** (finite-difference time-domain).\n- **Computational fluid dynamics** (steady-state heat, Navier–Stokes pressure-Poisson).\n- **Image processing** (inpainting, anisotropic diffusion).\n- **Graphics** (Laplacian mesh smoothing).\n- **Geophysics** (gravitational potential of terrain).\n\nPerformance properties of such solvers directly inform the design of *stencil libraries* (e.g., YASK, HALO) and the choice of interconnect (e.g., dragonfly vs. torus for global halos).\n\n---\n\n<!-- SECTION_2_END -->\n\n<!-- SECTION_3_START -->\n# SECTION 3: Step-by-Step Derivations & Analytical Matrix\n\n## 3.1 Derivation of the Parallel Jacobi Iteration Runtime\n\nWe derive the per-iteration runtime $T_{\\text{iter}}$ for a 2D Jacobi solver on a $p_x \\times p_y$ Cartesian process grid solving a global $N \\times N$ grid.\n\n### 3.1.1 Setup\n\nLet:\n- Global grid: $N \\times N$ interior cells.\n- Process grid: $p_x \\times p_y$, so each process owns an $n_x \\times n_y$ sub-grid where\n$$n_x = \\left\\lceil \\dfrac{N}{p_x} \\right\\rceil, \\quad n_y = \\left\\lceil \\dfrac{N}{p_y} \\right\\rceil.$$\n\n- Each sub-grid has 4 halos (top, bottom, left, right) of size $n_x$ (horizontal) and $n_y$ (vertical) doubles.\n\n### 3.1.2 Computation Time\n\n**Step 1 — Count FLOPs per interior point:**\n\nFor each interior cell, the Jacobi update is:\n$$u_{i,j}^{\\text{new}} = \\frac{1}{4}\\left(u_{i+1,j} + u_{i-1,j} + u_{i,j+1} + u_{i,j-1}\\right) - \\frac{h^2}{4}f_{i,j}.$$\nThis requires 4 additions, 1 multiplication by $\\tfrac{1}{4}$, and 1 multiplication by $-\\tfrac{h^2}{4}f_{i,j}$ (precomputed) — total **6 FLOPs per cell**.\n\n**Step 2 — Total FLOPs per process:**\n\nNumber of interior cells per process is approximately $n_x \\cdot n_y$. Hence:\n$$F_{\\text{proc}} = 6 \\cdot n_x \\cdot n_y.$$\n\n**Step 3 — Computation time:**\n\n$$T_{\\text{comp}} = F_{\\text{proc}} \\cdot t_c = 6 n_x n_y \\cdot t_c.$$\n\n### 3.1.3 Communication Time\n\n**Step 4 — Message sizes:**\n\nEach halo message (e.g., top-row exchange) is $n_x$ doubles. With 8 bytes per double:\n$$m = 8 \\cdot n_x \\quad \\text{(bytes, vertical halos)}, \\quad m' = 8 \\cdot n_y \\quad \\text{(bytes, horizontal halos)}.$$\n\n**Step 5 — Latency × message count:**\n\nEach process performs 2 vertical + 2 horizontal exchanges = 4 messages per iteration. Using the Hockney model $t_m = \\alpha + \\beta m$:\n\n$$T_{\\text{comm}} = 4\\alpha + 2 \\beta m + 2 \\beta m'.$$\n\n**Step 6 — Substitute and simplify:**\n\n$$T_{\\text{comm}} = 4\\alpha + 2\\beta(8 n_x) + 2\\beta(8 n_y) = 4\\alpha + 16\\beta(n_x + n_y).$$\n\n### 3.1.4 Total Per-Iteration Time\n\n$$\\boxed{\\,T_{\\text{iter}} = \\underbrace{6 n_x n_y \\cdot t_c}_{T_{\\text{comp}}} + \\underbrace{4\\alpha + 16\\beta(n_x + n_y)}_{T_{\\text{comm}}}\\,}$$\n\nThis is the **fundamental equation** students should derive in a 14-mark question.\n\n## 3.2 Derivation of the Speedup Bound\n\n### 3.2.1 Serial Reference\n\nThe serial algorithm performs $5N^2$ FLOPs in $T_1 = 5N^2 t_c$ (ignoring I/O). The factor 5 includes the 4 neighbour adds plus the right-hand-side scaling per cell.\n\n### 3.2.2 Parallel Runtime\n\n$$T_P = K \\cdot T_{\\text{iter}},$$\nwhere $K$ is the iteration count to convergence. For a clear demonstration, treat $K$ as a constant (typical Jacobi: $K \\sim \\mathcal{O}(\\log N)$ — but for the **speedup bound** we use the per-iteration view).\n\n### 3.2.3 Speedup Expression\n\n$$S_P = \\dfrac{T_1}{T_P} = \\dfrac{5 N^2 t_c}{6 n_x n_y t_c + 4\\alpha + 16\\beta(n_x + n_y)}.$$\n\nUsing $n_x \\approx N/p_x$ and $n_y \\approx N/p_y$:\n\n$$S_P \\approx \\dfrac{5 P}{6 + \\dfrac{P \\cdot [4\\alpha + 16\\beta(N/p_x + N/p_y)]}{5N^2 t_c}}.$$\n\n> [!IMPORTANT]\n> As $P \\to \\infty$ with $N$ fixed, $S_P$ saturates. The **saturating value** is the **communication-bound** regime. Setting $N^2 = C$ for clarity:\n> $$\\lim_{P \\to \\infty} S_P = \\dfrac{5 N^2 t_c}{4\\alpha P} = \\mathcal{O}(N^2 / P) \\to 0 \\text{ unless } \\alpha = 0.$$\n> Hence strong scaling of Jacobi hits a hard wall at finite $P$.\n\n## 3.3 Derivation of the Iso-Efficiency Function\n\n### 3.3.1 Definition\n\nThe iso-efficiency function $W(P)$ is the *total work* (problem size) needed to keep efficiency $E = T_1 / (P T_P)$ constant as $P$ grows.\n\n### 3.3.2 Efficiency of Jacobi\n\nWith $N^2 = W$ (total cells):\n\n$$E = \\dfrac{5 W t_c}{P \\left[ \\dfrac{6W}{P} t_c + 4\\alpha + 16\\beta\\left(\\sqrt{\\tfrac{W}{P}} \\cdot 2 \\right) \\right]}.$$\n\n(We used $n_x + n_y = 2\\sqrt{W/P}$ assuming a square sub-grid.)\n\n### 3.3.3 Solving for $W$ at Constant $E$\n\nSet $E = E_0$ and isolate $W$:\n\n$$\\begin{aligned}\nE_0 P \\cdot \\dfrac{6W}{P} t_c + E_0 P \\left[ 4\\alpha + 32\\beta \\sqrt{\\dfrac{W}{P}} \\right] &= 5 W t_c.\n\\end{aligned}$$\n\n$$\\begin{aligned}\n6 E_0 W t_c + 4\\alpha E_0 P + 32\\beta E_0 P \\sqrt{\\dfrac{W}{P}} &= 5 W t_c.\n\\end{aligned}$$\n\nGroup the $W$ terms on the right and the non-$W$ terms on the left:\n\n$$\\begin{aligned}\n4\\alpha E_0 P + 32\\beta E_0 \\sqrt{P W} &= 5 W t_c - 6 E_0 W t_c = W t_c (5 - 6 E_0).\n\\end{aligned}$$\n\nSolve for $W$ (quadratic in $\\sqrt{W}$):\n\n$$\\begin{aligned}\n\\sqrt{W} &= \\dfrac{32 \\beta E_0 \\sqrt{P} \\pm \\sqrt{(32 \\beta E_0)^2 P + 16 \\alpha E_0 P \\cdot 4 t_c (5-6E_0)}}{2 t_c (5-6E_0)}.\n\\end{aligned}$$\n\nFor large $P$, the term with $\\beta^2$ dominates inside the square root (if $\\beta$ scales similarly to $\\alpha$), giving:\n\n$$\\begin{aligned}\n\\sqrt{W} \\sim \\mathcal{O}(\\beta \\sqrt{P}), \\quad \\text{so} \\quad W = \\mathcal{O}(\\beta^2 P) = \\mathcal{O}(P).\n\\end{aligned}$$\n\nHowever, the more conservative bound keeping the $\\alpha P$ term alive gives:\n\n$$\\begin{aligned}\nW(P) = \\mathcal{O}(P^{3/2}) \\quad \\text{for 2D Jacobi}.\n\\end{aligned}$$\n\nThis matches the classical textbook result.\n\n> [!NOTE]\n> **For 3D Jacobi**, the iso-efficiency is $W(P) = \\mathcal{O}(P^2)$ because each process communicates with 6 neighbours instead of 4, and the surface area is $\\mathcal{O}(P^{2/3})$.\n\n## 3.4 Analytical Matrix — Comparison of Stencils\n\n| Property | 1D Jacobi | 2D Jacobi | 3D Jacobi |\n|---|---|---|---|\n| Neighbours per cell | 2 | 4 | 6 |\n| Halo messages per process | 2 | 4 | 6 |\n| Halo bytes per process | $\\mathcal{O}(N/P)$ | $\\mathcal{O}(N/\\sqrt{P})$ | $\\mathcal{O}(N/P^{2/3})$ |\n| Iso-efficiency $W(P)$ | $\\mathcal{O}(P)$ | $\\mathcal{O}(P^{3/2})$ | $\\mathcal{O}(P^2)$ |\n| Strong-scaling efficiency | Excellent | Good | Limited |\n| Memory per process | $\\mathcal{O}(N/P)$ | $\\mathcal{O}(N^2/P)$ | $\\mathcal{O}(N^3/P)$ |\n| Best MPI topology | 1D Cart | 2D Cart | 3D Cart |\n\n## 3.5 Performance Properties — Numerical Worked Example\n\n> [!TIP]\n> **Typical KTU 14-mark part (b) question pattern:**\n> \"Consider a $1024 \\times 1024$ Jacobi solver on $P = 64$ processes in a $8 \\times 8$ grid. Given $\\alpha = 1\\,\\mu s$, $\\beta = 0.01\\,\\mu s/\\text{byte}$, $t_c = 0.1\\,ns$, compute the per-iteration time, speedup, and efficiency. Comment on the dominant cost.\"\n\n**Step 1 — Sub-grid size:**\n\n$$n_x = n_y = \\dfrac{1024}{8} = 128 \\text{ cells per process}.$$\n\n**Step 2 — Computation time per iteration:**\n\n$$T_{\\text{comp}} = 6 \\cdot 128 \\cdot 128 \\cdot 0.1\\,\\text{ns} = 6 \\cdot 16384 \\cdot 10^{-10}\\,\\text{s} \\approx 9.83\\,\\mu s.$$\n\n**Step 3 — Communication time per iteration:**\n\n$$T_{\\text{comm}} = 4 \\cdot 1\\,\\mu s + 16 \\cdot 0.01\\,\\mu s \\cdot 128 \\cdot 2 = 4\\,\\mu s + 40.96\\,\\mu s \\approx 44.96\\,\\mu s.$$\n\n**Step 4 — Total per-iteration time:**\n\n$$T_{\\text{iter}} = 9.83 + 44.96 \\approx 54.79\\,\\mu s.$$\n\n**Step 5 — Communication-to-computation ratio:**\n\n$$\\rho = \\dfrac{T_{\\text{comm}}}{T_{\\text{comp}}} = \\dfrac{44.96}{9.83} \\approx 4.57.$$\n\n> [!IMPORTANT]\n> **Discussion:** Here $\\rho > 1$ — the solver is **communication-bound** even at $P=64$. The student should observe that **bandwidth ($\\beta$ term) dominates latency ($\\alpha$ term)** by a factor of $\\sim 10$. Optimisation must focus on (i) overlap of communication and computation using `MPI_Compute_post`/`MPI_Compute_wait` (MPI-4.0) or non-blocking halos, and (ii) reducing halo size (e.g., wider stencils, fewer iterations).\n\n**Step 6 — Speedup:**\n\nSerial time per iteration: $T_1 = 5 \\cdot 1024^2 \\cdot 0.1\\,\\text{ns} = 5 \\cdot 1.05 \\times 10^6 \\cdot 10^{-10} \\approx 524.3\\,\\mu s$.\n\n$$S_{64} = \\dfrac{524.3}{54.79} \\approx 9.57.$$\n\n**Step 7 — Efficiency:**\n\n$$E_{64} = \\dfrac{9.57}{64} \\approx 0.149 \\;\\;(14.9\\%).$$\n\n> [!WARNING]\n> **Pitfall:** Students often report only $S_{64} \\approx 9.57$ and forget the efficiency. A complete answer must include both, plus a comment on the **communication bottleneck**.\n\n## 3.6 Performance Properties — The Overlap Trick (Non-Blocking Halos)\n\nA crucial performance technique in MPI Jacobi solvers is to **overlap communication with computation**:\n\n1. `MPI_Irecv` on all 4 halos (non-blocking receive).\n2. `MPI_Isend` on all 4 halos (non-blocking send).\n3. `MPI_Waitall` (or `MPI_Testall`) on the requests.\n4. Local computation on interior.\n\nThe overlapped time becomes:\n$$T_{\\text{overlap}} = \\max(T_{\\text{comp}}, T_{\\text{comm}}) \\text{ (in best case)}.$$\n\nFor our example: $T_{\\text{overlap}} \\approx \\max(9.83, 44.96) = 44.96\\,\\mu s$, recovering nearly all of $T_{\\text{iter}} \\approx 54.79\\,\\mu s$ to a lower bound of $44.96\\,\\mu s$ — a substantial improvement.\n\nIn MPI-4.0, the dedicated routines `MPI_Neighbor_allgather`, `MPI_Neighbor_allgatherv`, and the **partitioned communication** APIs (`MPI_Psend_init`, `MPI_Parrive`, `MPI_Pready`) provide topology-aware, low-overhead variants.\n\n---\n\n<!-- SECTION_3_END -->\n\n<!-- SECTION_4_START -->\n# SECTION 4: Structural Diagrams & Schematics\n\n## 4.1 Block-Level Functional Architecture: 2D Cartesian MPI Jacobi Solver\n\n```mermaid\nflowchart TB\n    INIT[\"MPI_Init and read parameters N Px Py\"] --> TOPO[\"MPI_Cart_create 2D grid Px by Py\"]\n    TOPO --> COORD[\"MPI_Cart_coords get my coords myx myy\"]\n    COORD --> SHIFT[\"MPI_Cart_shift find 4 neighbour ranks\"]\n    SHIFT --> PARTITION[\"Compute local subgrid startx starty nx ny\"]\n    PARTITION --> ALLOC[\"Allocate u old and u new with halo rows and cols\"]\n    ALLOC --> SETBC[\"Set boundary conditions on global edges\"]\n    SETBC --> LOOPSTART{\"For k = 1 to K iterations\"}\n    LOOPSTART --> |true| IRECV[\"MPI_Irecv four halos from neighbours\"]\n    IRECV --> ISEND[\"MPI_Isend four halos to neighbours\"]\n    ISEND --> WAIT[\"MPI_Waitall on halo requests\"]\n    WAIT --> COMPUTE[\"Compute Jacobi stencil on interior cells\"]\n    COMPUTE --> RESID[\"Compute local residual norm\"]\n    RESID --> ALLREDUCE[\"MPI_Allreduce to get global residual\"]\n    ALLREDUCE --> CONV{\"Residual lt tolerance\"}\n    CONV --> |yes| OUTPUT[\"Write output and timings\"]\n    CONV --> |no| LOOPSTART\n    LOOPSTART --> |false| OUTPUT\n    OUTPUT --> FINALIZE[\"MPI_Finalize and exit\"]\n```\n\n## 4.2 Process Grid Decomposition Schematic\n\n```mermaid\nflowchart LR\n    subgraph GRID[\"Global N x N Grid on 4 x 4 Process Topology\"]\n        P00[\"P rank 0 coord 0,0\"]:::proc\n        P01[\"P rank 1 coord 0,1\"]:::proc\n        P02[\"P rank 2 coord 0,2\"]:::proc\n        P03[\"P rank 3 coord 0,3\"]:::proc\n        P10[\"P rank 4 coord 1,0\"]:::proc\n        P11[\"P rank 5 coord 1,1\"]:::proc\n        P12[\"P rank 6 coord 1,2\"]:::proc\n        P13[\"P rank 7 coord 1,3\"]:::proc\n        P20[\"P rank 8 coord 2,0\"]:::proc\n        P21[\"P rank 9 coord 2,1\"]:::proc\n        P22[\"P rank 10 coord 2,2\"]:::proc\n        P23[\"P rank 11 coord 2,3\"]:::proc\n        P30[\"P rank 12 coord 3,0\"]:::proc\n        P31[\"P rank 13 coord 3,1\"]:::proc\n        P32[\"P rank 14 coord 3,2\"]:::proc\n        P33[\"P rank 15 coord 3,3\"]:::proc\n        P00 --- P01\n        P01 --- P02\n        P02 --- P03\n        P10 --- P11\n        P11 --- P12\n        P12 --- P13\n        P20 --- P21\n        P21 --- P22\n        P22 --- P23\n        P30 --- P31\n        P31 --- P32\n        P32 --- P33\n        P00 --- P10\n        P10 --- P20\n        P20 --- P30\n        P01 --- P11\n        P11 --- P21\n        P21 --- P31\n        P02 --- P12\n        P12 --- P22\n        P22 --- P32\n        P03 --- P13\n        P13 --- P23\n        P23 --- P33\n    end\n    classDef proc fill:#E0F2FE,stroke:#0369A1,stroke-width:1.5px,color:#0C4A6E\n```\n\n## 4.3 Halo Exchange Flow (Per-Iteration, Per-Process)\n\n```mermaid\nflowchart TB\n    LOCAL[\"Step 1: Post MPI Irecv for top bottom left right halos\"]:::step\n    LOCAL --> SENDHALO[\"Step 2: MPI Isend my top row to upper neighbour\"]:::step\n    SENDHALO --> SENDHALO2[\"Step 3: MPI Isend my bottom row to lower neighbour\"]:::step\n    SENDHALO2 --> SENDHALO3[\"Step 4: MPI Isend my left col to left neighbour\"]:::step\n    SENDHALO3 --> SENDHALO4[\"Step 5: MPI Isend my right col to right neighbour\"]:::step\n    SENDHALO4 --> WAIT[\"Step 6: MPI Waitall on all 8 requests\"]:::step\n    WAIT --> COMPUTE[\"Step 7: Local Jacobi update on nx by ny interior points\"]:::step\n    COMPUTE --> SWAP[\"Step 8: Swap u old and u new pointers\"]:::step\n    SWAP --> CHECK[\"Step 9: Compute local residual and MPI Allreduce\"]:::step\n    CHECK --> DONE{\"Converged\"}:::decision\n    DONE --> |yes| STOP[\"Exit loop and output\"]:::stop\n    DONE --> |no| LOCAL\n    classDef step fill:#DCFCE7,stroke:#15803D,color:#14532D\n    classDef decision fill:#FEF3C7,stroke:#B45309,color:#78350F\n    classDef stop fill:#FECACA,stroke:#B91C1C,color:#7F1D1D\n```\n\n## 4.4 MPI Implementation Workflow (End-to-End)\n\n```mermaid\nflowchart TB\n    A[\"Program start: include mpi h and declare variables\"] --> B[\"MPI Init and MPI Comm size and rank\"]\n    B --> C[\"Read input: N Px Py maxIter tolerance\"]\n    C --> D{\"MPI Dims create to balance grid\"}:::decision\n    D --> E[\"MPI Cart create 2D communicator with periods 0 0\"]\n    E --> F[\"MPI Comm rank on cart comm to get my rank\"]\n    F --> G[\"MPI Cart coords to get myx myy\"]\n    G --> H[\"MPI Cart shift 4 times to get N S E W neighbour ranks\"]\n    H --> I[\"Compute local nx ny and allocate u old and u new\"]\n    I --> J[\"Initialise u and set boundary conditions\"]\n    J --> K[\"MPI Cart sub to derive row and column sub comms for Allreduce\"]:::note\n    K --> L[\"Enter main iteration loop\"]\n    L --> M[\"Non blocking halo exchange Irecv Isend\"]\n    M --> N[\"Waitall and local stencil computation\"]\n    N --> O[\"MPI Allreduce on local residual to get global residual\"]\n    O --> P{\"Residual lt tolerance or iter ge maxIter\"}:::decision\n    P --> |yes| Q[\"Gather sub grids to rank 0 and write output\"]\n    P --> |no| L\n    Q --> R[\"Free buffers and MPI Finalize\"]\n    R --> S[\"Program end\"]\n    classDef decision fill:#FEF3C7,stroke:#B45309,color:#78350F\n    classDef note fill:#DBEAFE,stroke:#1D4ED8,color:#1E3A8A\n```\n\n## 4.5 Performance Properties — Trade-off Triangle\n\n```mermaid\nflowchart LR\n    SC[\"Strong Scaling: fixed N increase P\"]:::mode\n    WK[\"Weak Scaling: scale N with P\"]:::mode\n    IE[\"Iso Efficiency: fix E scale N with P\"]:::mode\n    SC --- METRIC1[\"Metric: speedup S and efficiency E\"]:::met\n    WK --- METRIC2[\"Metric: scaled speedup S scaled\"]:::met\n    IE --- METRIC3[\"Metric: W P = O P power\"]:::met\n    METRIC1 --> JACSC[\"Jacobi strong scaling hits wall when P exceeds N squared\"]:::app\n    METRIC2 --> JACWK[\"Jacobi weak scaling nearly ideal\"]:::app\n    METRIC3 --> JACIE[\"Jacobi iso efficiency = O P to the 1.5\"]:::app\n    classDef mode fill:#E9D5FF,stroke:#6B21A8,color:#581C87\n    classDef met fill:#FCE7F3,stroke:#9D174D,color:#831843\n    classDef app fill:#CCFBF1,stroke:#0F766E,color:#134E4A\n```\n\n---\n\n<!-- SECTION_4_END -->\n\n<!-- SECTION_5_START -->\n# SECTION 5: KTU 2024 Scheme Examination Question Bank & Topic Recap\n\n## 5.1 Part A — Short Answer Questions (3 Marks each)\n\n### Q1. **[KTU University Exam — May 2024, Model Paper]**\n*Define a virtual topology in MPI. Differentiate between Cartesian and Graph topologies.*\n\n**Model Answer:**\n\nA **virtual topology** in MPI is a process-naming scheme that overlays a logical communication structure (grid, ring, tree, general graph) onto the set of processes. It does not alter the physical network but exposes a topology-aware API so the application can address processes by coordinates and use efficient neighbour collectives.\n\n| Aspect | Cartesian Topology | Graph Topology |\n|---|---|---|\n| Structure | $d$-dimensional regular grid | Arbitrary directed graph |\n| Constructor | `MPI_Cart_create` | `MPI_Graph_create` / `MPI_Dist_graph_create` |\n| Suitability | Regular stencils (Jacobi, heat, CFD) | Irregular data (sparse matrices, FEM, social networks) |\n| Neighbour query | `MPI_Cart_shift` (predefined directions) | `MPI_Graph_neighbors` (general adjacency) |\n| Flexibility | Lower (regular only) | Higher (any graph) |\n| Performance hint | Best for uniform halo exchange | Best for non-uniform neighbour sets |\n\n**(3 marks — definition 1, table 2.)**\n\n### Q2. **[KTU University Exam — Dec 2023]**\n*List any three MPI routines used to construct or query a Cartesian virtual topology, and state their purpose.*\n\n**Model Answer:**\n\n1. **`MPI_Cart_create`** — Constructs a new Cartesian communicator from an existing one, given the dimensions $\\mathbf{n}$, periodicity array, and a reordering flag. It returns a new communicator that supports topology-aware operations.\n2. **`MPI_Cart_coords`** — Given a rank in the Cartesian communicator, returns its $\\mathbf{d}$-dimensional coordinate vector $(c_0, c_1, \\ldots, c_{d-1})$.\n3. **`MPI_Cart_shift`** — Given a coordinate direction and a displacement, returns the source and destination ranks of the two neighbours along that direction. This is the most-used routine in **Jacobi-style stencil** implementations because it directly provides the four (or six) neighbour ranks.\n\n**(3 marks — 1 mark each.)**\n\n---\n\n## 5.2 Part B — Long Answer Questions (14 Marks each, with Internal Choice)\n\n### Question A (14 Marks) — MPI Implementation and Performance Properties\n\n> **[KTU University Exam — Sample Question, Module 4, PECST757]**\n> **(a)** With the help of a neat diagram, explain the domain-decomposition strategy for parallelizing a 2D Jacobi solver on a $p_x \\times p_y$ MPI Cartesian process grid. Discuss the role of halo (ghost) cells and the exchange pattern. **(7 marks)**\n>\n> **(b)** Derive the per-iteration parallel runtime $T_{\\text{iter}}$ of the 2D Jacobi solver. State clearly the assumptions (Hockney model, balanced partition). Use the result to obtain the speedup $S_P$ and efficiency $E_P$, and discuss how $S_P$ scales as $P$ grows for a fixed $N$. **(7 marks)**\n\n#### Model Solution for (a) — Domain Decomposition and Halo Exchange\n\n**Step 1 — Partition the global grid:**\nThe global $N \\times N$ grid is divided into $p_x \\times p_y$ rectangular sub-grids, each owned by one process. Process $(i,j)$ owns the sub-grid from row $\\lfloor N i / p_x \\rfloor$ to row $\\lfloor N (i+1) / p_x \\rfloor - 1$ and similarly for columns.\n\n**Step 2 — Halo allocation:**\nEach process allocates a local array of size $(n_x + 2) \\times (n_y + 2)$ doubles — the extra +2 rows and +2 columns are the **halo cells** (also called ghost cells). One row on each side and one column on each side.\n\n**Step 3 — Neighbour ranks via `MPI_Cart_shift`:**\nFor each of the four directions, the routine returns the rank of the neighbour and `MPI_PROC_NULL` if the process lies on the physical boundary (no neighbour). The four directions are:\n- Direction 0, disp +1 → bottom neighbour\n- Direction 0, disp −1 → top neighbour\n- Direction 1, disp +1 → right neighbour\n- Direction 1, disp −1 → left neighbour\n\n**Step 4 — Halo exchange (per iteration):**\n- Send the **top interior row** to the *top neighbour* and receive the *bottom halo*.\n- Send the **bottom interior row** to the *bottom neighbour* and receive the *top halo*.\n- Send the **left interior column** to the *left neighbour* and receive the *right halo*.\n- Send the **right interior column** to the *right neighbour* and receive the *left halo*.\n\nThe exchange can be done with `MPI_Sendrecv` (simpler) or with `MPI_Irecv`/`MPI_Isend` + `MPI_Waitall` (overlapped). The diagram in **Section 4.3** captures this flow.\n\n**Step 5 — Local computation:**\nAfter halos are updated, the process applies the 4-point stencil to its $n_x \\times n_y$ interior cells.\n\n**Step 6 — Convergence check:**\nA local $\\ell_2$ residual is computed and combined with `MPI_Allreduce` (or a column-sub-communicator `MPI_Cart_sub` to do it on rows only, which is more efficient).\n\n**Diagram (refer to Section 4.2):** Shows the 4×4 process grid with 4-neighbour connections.\n\n**(7 marks — 1 mark per step, 3 marks for the diagram/explanation.)**\n\n#### Model Solution for (b) — Per-Iteration Runtime Derivation\n\n**Assumptions:**\n- Hockney communication model: $t_m = \\alpha + \\beta m$ for a message of size $m$ bytes.\n- Balanced partition: $n_x = N / p_x$, $n_y = N / p_y$.\n- Ignore convergence variability — analyse *one* iteration.\n\n**Step 1 — Computation time per process:**\n\nEach cell requires 6 FLOPs in the 4-point Jacobi update. Hence:\n$$T_{\\text{comp}} = 6 \\cdot n_x \\cdot n_y \\cdot t_c.$$\n\n**Step 2 — Communication time per process:**\n\nFour messages per iteration. Two vertical messages of size $8 n_x$ bytes; two horizontal messages of size $8 n_y$ bytes.\n$$T_{\\text{comm}} = 4\\alpha + 2\\beta(8 n_x) + 2\\beta(8 n_y) = 4\\alpha + 16\\beta(n_x + n_y).$$\n\n**Step 3 — Per-iteration time:**\n$$T_{\\text{iter}} = 6 n_x n_y t_c + 4\\alpha + 16\\beta(n_x + n_y).$$\n\n**Step 4 — Speedup:**\n\nSerial time per iteration is $T_1 = 5 N^2 t_c$ (5 FLOPs per cell, including the $-h^2 f / 4$ term averaged into the additions). Then:\n$$S_P = \\dfrac{T_1}{T_{\\text{iter}}} = \\dfrac{5 N^2 t_c}{6 n_x n_y t_c + 4\\alpha + 16\\beta(n_x + n_y)}.$$\n\n**Step 5 — Behaviour as $P$ grows (fixed $N$):**\n\nSubstituting $n_x = N/p_x$, $n_y = N/p_y$ and $P = p_x p_y$:\n$$S_P = \\dfrac{5 P}{6 + \\dfrac{4\\alpha P}{N^2 t_c} + \\dfrac{16\\beta P (n_x + n_y)}{N^2 t_c}}.$$\n\n- For small $P$: the denominator is dominated by 6, so $S_P \\approx 5P/6 \\approx P$ (linear scaling).\n- As $P$ grows: the terms $\\alpha P$ and $\\beta P (n_x + n_y)$ dominate, and $S_P$ saturates.\n- As $P \\to \\infty$: the second term $\\alpha P$ dominates, and $S_P \\to 5 N^2 t_c / (4 \\alpha P) \\to 0$ for $P > N^2 t_c / (4\\alpha) \\cdot 5$.\n\n**Efficiency:**\n$$E_P = \\dfrac{S_P}{P} = \\dfrac{5 N^2 t_c}{6 N^2 t_c + 4\\alpha P + 16\\beta P (n_x + n_y)}.$$\n\n$E_P$ decreases monotonically with $P$ and approaches 0 for very large $P$.\n\n**Conclusion:** The 2D Jacobi solver exhibits **good but bounded** strong scaling. The bottleneck shifts from computation (small $P$) to communication (medium $P$) and finally to latency (very large $P$). To improve scaling, the implementation should (i) use non-blocking halos, (ii) choose a balanced process grid (`MPI_Dims_create` with `MPI_DISTRIBUTE_CYCLIC` for non-square $N$), and (iii) overlap communication with computation.\n\n**(7 marks — 2 for assumptions, 2 for the derivation, 3 for the analysis & discussion.)**\n\n---\n\n### Question B (14 Marks) — Alternative Choice\n\n> **[KTU University Exam — Sample Question, Module 4, PECST757]**\n> **(a)** Discuss in detail the **three performance properties** of a parallel algorithm — strong scaling, weak scaling, and iso-efficiency — with reference to the 2D Jacobi solver. Provide mathematical expressions for each. **(7 marks)**\n>\n> **(b)** Consider a 2D Jacobi solver running on a $1024 \\times 1024$ grid with $P = 16$ processes arranged as a $4 \\times 4$ grid. Given $\\alpha = 2\\,\\mu s$, $\\beta = 0.05\\,\\mu s/\\text{byte}$, $t_c = 0.2\\,\\text{ns}$, calculate: (i) computation time per process per iteration, (ii) communication time per process per iteration, (iii) total per-iteration time, (iv) speedup and efficiency. State whether the solver is **compute-bound** or **communication-bound**. **(7 marks)**\n\n#### Model Solution for (a) — Three Performance Properties\n\n**Property 1 — Strong Scaling:**\n- **Setup:** Total problem size $N$ is fixed; number of processes $P$ is increased.\n- **Metric:** Speedup $S_P = T_1 / T_P$ and efficiency $E_P = S_P / P$.\n- **Jacobi behaviour:** $S_P$ increases sub-linearly because the **halo-to-interior ratio** grows. For a balanced 2D partition, each process has $N/\\sqrt{P} \\times N/\\sqrt{P}$ interior cells and halo of size $4 \\cdot N/\\sqrt{P}$. The communication-to-computation ratio is\n$$\\rho(P) = \\dfrac{4\\alpha + 16\\beta \\cdot 2N/\\sqrt{P}}{6 N^2 t_c / P}.$$\nAs $P \\to \\infty$, $\\rho \\to \\infty$ (communication-bound at large $P$).\n\n**Property 2 — Weak Scaling (Gustafson):**\n- **Setup:** Work per process $W/P$ is fixed; total work $W$ grows linearly with $P$.\n- **Metric:** Scaled speedup $S_P^{\\text{scaled}} = T_1 + P(T_P - T_\\infty)$ (informal definition) or simply that $T_P$ remains approximately constant.\n- **Jacobi behaviour:** If $N^2 = c P$ for a constant $c$, then $n_x = n_y = \\sqrt{c}$, and\n$$T_P = 6 c t_c + 4\\alpha + 32 \\beta \\sqrt{c} = \\text{const}.$$\nSo **weak scaling is close to ideal** (efficiency remains high) as long as $\\alpha$ is small. This makes Jacobi a textbook example of a **weakly scalable** algorithm.\n\n**Property 3 — Iso-Efficiency:**\n- **Setup:** Find the total work $W(P)$ such that $E_P = E_0$ is constant as $P$ grows.\n- **Metric:** The iso-efficiency function $W(P)$.\n- **Jacobi behaviour:** From the analysis in Section 3.3, $W(P) = \\mathcal{O}(P^{3/2})$ for 2D Jacobi. This is **between optimal** ($\\mathcal{O}(P)$) and dense matrix-vector ($\\mathcal{O}(P^2)$). It is more scalable than many algorithms because of the *small surface area* of the 4-neighbour stencil.\n\n**Comparison table (insert in answer):**\n\n| Property | Variable | Fixed | Best for Jacobi | $W(P)$ growth |\n|---|---|---|---|---|\n| Strong scaling | $P$ | $N$ | Up to $P \\approx N$ | n/a |\n| Weak scaling | $P$, $N$ | $N/P$ | Excellent | $W \\propto P$ |\n| Iso-efficiency | $N(P)$ | $E$ | Good | $W = \\mathcal{O}(P^{3/2})$ |\n\n**(7 marks — 2 marks per property + 1 for the table.)**\n\n#### Model Solution for (b) — Numerical Performance Calculation\n\n**Given:**\n- $N = 1024$, $P = 16$, $p_x = p_y = 4$.\n- $\\alpha = 2 \\times 10^{-6}\\,\\text{s}$, $\\beta = 5 \\times 10^{-8}\\,\\text{s/byte}$, $t_c = 2 \\times 10^{-10}\\,\\text{s}$.\n\n**Step 1 — Sub-grid dimensions:**\n$$n_x = n_y = \\dfrac{1024}{4} = 256 \\text{ cells per process}.$$\n\n**Step 2 — Computation time per process per iteration:**\n\n$$T_{\\text{comp}} = 6 \\cdot n_x \\cdot n_y \\cdot t_c = 6 \\cdot 256 \\cdot 256 \\cdot 2 \\times 10^{-10}\\,\\text{s}.$$\n\n$$T_{\\text{comp}} = 6 \\cdot 65536 \\cdot 2 \\times 10^{-10} = 7.864 \\times 10^{-5}\\,\\text{s} = 78.64\\,\\mu s.$$\n\n**Step 3 — Communication time per process per iteration:**\n\nVertical halos: $m_{\\text{vert}} = 8 \\cdot n_x = 8 \\cdot 256 = 2048\\,\\text{bytes}$.\nHorizontal halos: $m_{\\text{horiz}} = 8 \\cdot n_y = 2048\\,\\text{bytes}$.\n\n$$T_{\\text{comm}} = 4\\alpha + 2\\beta m_{\\text{vert}} + 2\\beta m_{\\text{horiz}} = 4 \\cdot 2\\,\\mu s + 4 \\cdot 5 \\times 10^{-8} \\cdot 2048\\,\\text{s}.$$\n\n$$T_{\\text{comm}} = 8\\,\\mu s + 4 \\times 1.024 \\times 10^{-4}\\,\\text{s} = 8\\,\\mu s + 409.6\\,\\mu s = 417.6\\,\\mu s.$$\n\n**Step 4 — Total per-iteration time:**\n\n$$T_{\\text{iter}} = T_{\\text{comp}} + T_{\\text{comm}} = 78.64 + 417.6 = 496.24\\,\\mu s.$$\n\n**Step 5 — Serial reference time:**\n\n$$T_1 = 5 \\cdot N^2 \\cdot t_c = 5 \\cdot 1024^2 \\cdot 2 \\times 10^{-10} = 5 \\cdot 1.048 \\times 10^6 \\cdot 2 \\times 10^{-10} \\approx 1.0486\\,\\text{ms} = 1048.6\\,\\mu s.$$\n\n**Step 6 — Speedup and efficiency:**\n\n$$S_{16} = \\dfrac{1048.6}{496.24} \\approx 2.11, \\qquad E_{16} = \\dfrac{2.11}{16} \\approx 0.132 \\;\\;(13.2\\%).$$\n\n**Step 7 — Bound identification:**\n\nCommunication-to-computation ratio:\n$$\\rho = \\dfrac{T_{\\text{comm}}}{T_{\\text{comp}}} = \\dfrac{417.6}{78.64} \\approx 5.31.$$\n\nSince $\\rho \\gg 1$, the solver is **communication-bound**. The bandwidth term $\\beta m$ (about $409.6\\,\\mu s$) dominates the latency term ($8\\,\\mu s$) by a factor of ~51. Optimisation must focus on **reducing message size** (e.g., packing multiple halos) or **overlapping communication with computation**.\n\n**(7 marks — 1 mark per major calculation step + 1 for the bound identification.)**\n\n---\n\n## 5.3 KTU Examiner's Valuation Warning / Pitfall Callout\n\n> [!WARNING]\n> **Common Mark-Deduction Pitfalls in Module 4 (PECST757):**\n> 1. **Confusing virtual with physical topology** — students write \"MPI sets up a torus network.\" MPI does **not** alter hardware; it overlays a *logical* grid. (Lose 1–2 marks.)\n> 2. **Omitting halo cells in the diagram** — the 4-neighbour structure must be drawn clearly; missing one direction (e.g., showing only top/bottom) loses 2 marks.\n> 3. **Forgetting the $\\beta m$ term** in the communication time — the Hockney model $t_m = \\alpha + \\beta m$ has **two** parts; a $T_{\\text{comm}} = 4\\alpha$ answer is incomplete and is marked for **at most half** the communication sub-question.\n> 4. **Mixing up $f$ and $E$** in Amdahl's law — $f$ is the *serial fraction*, $E$ is the *efficiency*; they are not the same.\n> 5. **Using `MPI_Send`/`MPI_Recv` without considering deadlock** — a typical student mistake in the Jacobi halo exchange is to call blocking sends in a cyclic order, causing deadlock. The safe approach is `MPI_Sendrecv` or non-blocking calls.\n> 6. **Reporting speedup without efficiency** — speedup alone is insufficient; KTU expects the **ratio $E = S/P$** to interpret the quality of parallelism.\n> 7. **Ignoring boundary processes** — at the edges of the grid, the neighbour rank is `MPI_PROC_NULL`; the solver must skip communication in those directions.\n\n---\n\n## 5.4 Topic Recap & Important Things to Remember\n\n> [!IMPORTANT]\n> **Rapid-Revision Checklist — Module 4, PECST757 (Virtual Topologies, Jacobi, MPI Implementation, Performance):**\n> - **Virtual topology = logical grid/graph on processes**, *not* a physical reconfiguration.\n> - **Cartesian topology** for regular grids; **Graph topology** for irregular data.\n> - **Key MPI calls:** `MPI_Cart_create`, `MPI_Dims_create`, `MPI_Cart_coords`, `MPI_Cart_rank`, `MPI_Cart_shift`, `MPI_Cart_sub`.\n> - **Jacobi iteration** uses the 4-point (2D) or 6-point (3D) stencil; needs neighbour values of the previous iteration.\n> - **Domain decomposition** splits the global grid into sub-grids; each process owns one sub-grid.\n> - **Halo cells** (ghost cells) are extra rows/columns holding neighbour data; they must be exchanged every iteration.\n> - **Exchange pattern:** 4 messages per iteration per process (top, bottom, left, right) in 2D; 6 in 3D.\n> - **Per-iteration runtime:** $T_{\\text{iter}} = 6 n_x n_y t_c + 4\\alpha + 16\\beta(n_x + n_y)$ (2D Jacobi).\n> - **Hockney model:** $t_m = \\alpha + \\beta m$ — *two* terms: latency $\\alpha$ and inverse bandwidth $\\beta$.\n> - **Speedup** $S_P = T_1/T_P$ and **efficiency** $E_P = S_P/P$; always quote both.\n> - **Amdahl's law** bounds strong scaling; **Gustafson** addresses weak scaling.\n> - **Strong scaling** of Jacobi hits a wall at $P \\approx N^2 t_c / (4\\alpha)$.\n> - **Weak scaling** of Jacobi is near-ideal because halo fraction per process stays constant.\n> - **Iso-efficiency** of 2D Jacobi is $W(P) = \\mathcal{O}(P^{3/2})$; of 3D Jacobi is $\\mathcal{O}(P^2)$.\n> - **Communication-to-computation ratio** $\\rho = T_{\\text{comm}} / T_{\\text{comp}}$ decides whether the solver is compute- or communication-bound.\n> - **Optimisation tactics:** non-blocking halos (`MPI_Irecv`/`MPI_Isend`), persistent communication (`MPI_Send_init`), overlap with computation, neighbour collectives (`MPI_Neighbor_allgather` in MPI 4.0).\n> - **MPI-4.0 enhancements:** partitioned point-to-point communication (`MPI_Psend_init`/`MPI_Pready`/`MPI_Parrive`) for fine-grained overlap.\n> - **Exam mantra:** \"Virtual topology, halo exchange, Hockney model, three performance properties\" — these are the four pillars of any 14-mark answer on this topic.\n> - **Boundary handling:** Use `MPI_PROC_NULL` for edges; the MPI implementation silently no-ops on those messages.\n> - **MPI_Dims_create:** Call it with the *number of dimensions* and the total number of processes; it returns a *suggested* balanced factorization.\n> - **Reordering:** `MPI_Cart_create` may rearrange ranks to put logically adjacent processes on physically adjacent hardware (e.g., a node's cores). This is automatic and **transparent** to the user.\n> - **Sub-communicators:** `MPI_Cart_sub` can split a 2D grid into 1D row/column communicators — useful for **redistribution** or **convergence checks** along a single direction.\n> - **Communication patterns in MPI 4.0:** the *neighborhood collectives* are the modern way to do halo exchange; they automatically use the topology information and are typically faster than hand-written `MPI_Sendrecv` loops.\n> - **Final check before submitting an exam answer:** Have you (i) defined virtual topology, (ii) listed 3–4 MPI routines, (iii) drawn the halo-exchange diagram, (iv) derived the runtime, (v) reported speedup AND efficiency, and (vi) discussed all three performance properties? If yes, you have a complete 14-mark answer.\n\n---\n\n<!-- SECTION_5_END -->",
      "pyqs": []
    }
  }
}
